T2 마이그레이션을 시작하기 전에 규격, 규칙집, 골든 샘플, 소스 DESIGN.md를 순서대로 읽겠습니다.규칙집과 골든 샘플, catchtable 원문을 이어서 읽습니다.catchtable 원문 나머지와 골든 샘플, 최근 통과 이관본 형태를 확인합니다.최근 통과한 T2 이관본의 상태표·B2a·원장 형태를 대조해 catchtable에 맞춥니다.게이트가 hex·px·% 손실을 막으므로 원문의 토큰을 전부 뽑아 누락 없이 옮기겠습니다.원문 토큰·상태·세 도메인 경계를 유지한 Core v2 본문을 작성합니다. YAML `4`는 `4px`로 바꾸지 않습니다.본문 행 번호를 확보한 뒤 B2a 스캔과 토큰 게이트를 돌리겠습니다.발명된 `4px`와 원문에 없는 WCAG 표현을 고친 뒤 원장을 행 번호에 맞춰 씁니다.원장과 마이그레이션 로그를 실제 행 번호에 맞춰 작성합니다.스코프 claim이 `not … product` 패턴에 걸려 Portable Core가 실패한 문장을 원문 의미를 유지한 채 고칩니다.CatchTable legacy `DESIGN.md`를 Core v2로 옮겼습니다. 산출물은 `docs/design-md-weight/migrated/catchtable/`의 `DESIGN.md`, `provenance.md`, `migration-log.md`입니다. 규칙집은 v7입니다.

세 증거 도메인(consumer home / merchant marketing / careers marketing)은 합치지 않았습니다. `#ff3d00`는 careers 액션·catalog `primary_color`이고, merchant CTA `#002d4e`와 별개입니다. YAML `lineHeight` `1.50`/`1.35`는 비율로 남겼고, `Type: input` + `button`×4는 컴포넌트별로 보존했습니다. Search·Merchant·Careers의 loading/error/success는 역할 매핑이 안 닫혀 생략했고, Filter·Discovery는 선택/목적지 역할로 `not-applicable`입니다. 미관측을 비적용 사유로 쓰지 않았습니다.

기계 검사: `--gate-only` PASS, `portable_core: true`. SHA-256 `01ee03fe3956eda6cf4047bb4bf69bfc47da5fd0453aedb4acecadd7d937c552`. F3 감사와 sol 의미 레인은 아직입니다. 카탈로그 채택이 아닙니다.

DONE migrated=1
