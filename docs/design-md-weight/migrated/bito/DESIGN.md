# Bito Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Bito (滿滿大平台) is a creative studio based in Taipei, Taiwan, working at the intersection of motion design, branding, and live-experience visual systems. Catalog homepage identity is `https://bito.tv/`. Treating that opening identity sentence — the Taipei studio, motion-design / branding / live-experience span — as narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Treating the following two URLs as the named live-inspect evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. This contract covers two current first-party web surfaces from the 2026-06-17 live inspect: the home Work grid `https://bito.tv/` and the project page `https://bito.tv/work/148/The-60th-Golden-Horse-Awards-Branding`.

Source token note: Studio-as-reference (motion-design / branding studio, like Goodpatch). Stark editorial portfolio: pure black ink (`#000000`) on white canvas (`#ffffff`), 0px-radius everywhere, no shadows. Signature warm accent = Bito red-orange family (`#f92c16` / `#ff5529` / `#e74118`) used as full-bleed project-tile backgrounds; electric blue (`#4419fe`) as secondary accent. Type = neue-haas-unica (Latin) + Noto Sans TC (Traditional Chinese). Treating that note as a register split — `#f92c16` is catalog `primary_color` and the harvested warm project-tile fill rather than a button or link; `#ff5529` / `#e74118` / `#ff2700` stay unmerged warm-family variants; `#4419fe` / `#126dff` / `#fb76ff` / `#166b22` are project-art color fields rather than interactive chrome; `#000000` is ink rather than a second identity color; `#ffffff` canvas is unmerged from YAML `on-accent` `#ffffff`; Neue Haas Unica is the Latin face rather than Noto Sans TC — is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

The following evidence-domain readings, including stay-attached-to-the-establishing-page, home-Work-grid-not-a-proxy-for-every-other-captured-page, and YAML-interest-as-craft-presentation-rather-than-application-chrome, are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. The source HTML comment attaches token-level claims in §1–9 to playwright `getComputedStyle` on those two URLs. Values below stay attached to the page that established them. A home Work-grid value is not a proxy for every other captured page. The source YAML note names this reconstruction as studio-as-reference; the interest recorded there is how a motion/branding house presents craft rather than application chrome.

The next paragraphs’ monastic-plain, chrome-disappears, work-can-shout, classic-agency-move, studio-as-reference / Goodpatch, personality-erupts, energy-leaks-into-tokens, architectural-color-fields, separation-via-whitespace-never-elevation, and typography-as-the-other-half-of-the-identity readings are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. The source reads the portfolio site as almost monastically plain: a pure white canvas (`#ffffff`), pure black ink (`#000000`) for nearly all type, and absolutely no rounding — every corner is a hard `0px`. There are no drop shadows anywhere; separation comes from raw whitespace and full-bleed image tiles, never elevation. The source calls this the classic agency move: the chrome disappears so the work can shout. Bito is named as a studio-as-reference (in the spirit of Goodpatch).

The studio's personality is described as erupting through its imagery, with one leak into system tokens: color. The source names a vivid red-orange family running from `#f92c16` through `#ff5529` and `#e74118` as full-bleed backgrounds behind project thumbnails and the contact block. Around it orbit other electric accents lifted from project art: electric blue (`#4419fe`), cobalt (`#126dff`), hot magenta (`#fb76ff`), and deep green (`#166b22`). The source says none of these are buttons or links — they are the work itself, used architecturally as full-screen color fields.

Typography is recorded as the other half of the identity. Latin text is set in Neue Haas Unica, paired with Noto Sans TC for Traditional Chinese. Weights stay 400 for nav, body, and tags; 500 for labels and project titles. The source records no heavy display weight and no italics.

Treating the following public-history facts as narrative rather than interface tokens, including Golden Horse visual identity and title sequences across multiple editions for Taiwan’s premier film awards, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. The live project title “The 60th Golden Horse Awards Branding” is on the captured project page. The source HTML comment records that Bito is a Taipei, Taiwan motion-design / branding studio known for the Golden Horse Awards identity, Taiwan Lantern Festival, and public/cultural commissions — confirmed by the live Work index project titles — and that broader studio history is general public knowledge, not quoted from a verified Bito statement this turn. Source §11 records that Golden Horse work includes visual identity and title sequences, performed across multiple editions, for Taiwan’s premier film awards, and also names Taipei Metro brand films, tourism rebrandings, and government/cultural campaigns under that same public-knowledge class.

The following craft-first, silent-white-frame, refuses-chrome, embraces-editorial-canvas, posture-as-reason-the-site-is-restrained, and opposite-silent-frame-for-saturated-kinetic-maximal-reels readings are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. The source reads the studio's positioning as craft-first: it sells creative direction and execution rather than a SaaS. That posture is offered as the reason the site is restrained — a motion studio whose reels are saturated, kinetic, and maximal needs a presentation frame that is the opposite: silent, white, sharp-cornered, and shadow-free. What the source treats the design as refusing: decorative UI chrome, rounded "friendly" product geometry, drop shadows, and marketing superlatives. What it treats the design as embracing: a pure black-on-white editorial canvas, hard `0px` corners, full-bleed saturated color fields lifted from the work, and a neutral bilingual grotesque (Neue Haas Unica + Noto Sans TC).
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three live control strings below as the independently verified user outcomes, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

- Scan full-bleed project tiles on the home Work grid (`https://bito.tv/`).
- Read a project page title and Credits / Genre / Service / Sector block (`The 60th Golden Horse Awards Branding` on `/work/148`).
- Switch language with the header EN / 中 toggle.
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, source §13 fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. Source §13 fictional archetypes are deleted rather than promoted. No individual personas are promoted.

### Distinctive traits

- Catalog `primary_color` / warm project-tile fill `#f92c16` (not a button or link)
- White canvas `#ffffff`; ink `#000000`; muted `#707070`; faint `#bebebe`; hairline `#e2e2e2`
- Warm family `#ff5529` / `#e74118` / `#ff2700` as full-bleed tile variants; electric `#4419fe`, cobalt `#126dff`, magenta `#fb76ff`, green `#166b22` as project-art color fields
- `neue-haas-unica` Latin + `Noto Sans TC` Traditional Chinese; weights 400 and 500 only
- Zero-radius geometry at 0px on every recorded tile, label, and container
- YAML `shadow.none` is `none`; live inspect records `box-shadow: none`
- 96px transparent header bar; 32px 40px header padding; Work tiles at full viewport width (1440px) or half (668px)
- YAML `on-accent` `#ffffff` is reversed-out type on warm/dark tiles, not a second canvas

Treating `#f92c16` as the harvested warm-tile fill rather than interactive chrome, treating `#ff5529` / `#e74118` / `#ff2700` as unmerged warm-family variants, treating `#4419fe` / `#126dff` / `#fb76ff` / `#166b22` as project-art fields rather than buttons or links, treating `#ffffff` canvas as unmerged from YAML `on-accent` `#ffffff`, treating 0px as the recorded geometry rather than a universal radius for unlisted controls, treating 96px / 32px 40px / 1440px / 668px as surface measurements rather than a complete layout token set, and treating bilingual Neue Haas Unica + Noto Sans TC as the recorded pair rather than a substitute stack, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

### Principles

These five items, including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. The source HTML comment marks interpretive claims such as “the work is the interface” and “restraint amplifies craft as a deliberate frame for maximal motion work” as editorial readings connecting Bito's observed, restrained site design to its studio positioning, not directly sourced Bito statements.

1. **The work is the interface.** The site exists to display reels and case studies, not to be admired itself. *UI implication:* keep chrome to near-zero — transparent nav, no cards, no shadows — so full-bleed project media dominates every viewport.
2. **Sharp, not soft.** Hard `0px` corners everywhere signal precision and editorial seriousness. *UI implication:* never round a corner; let tiles butt edge-to-edge as flat planes.
3. **Color belongs to the work.** The saturated accents (`#f92c16`, `#4419fe`) are project art used architecturally, never decoration applied to UI. *UI implication:* reserve accent color for large full-bleed fields; keep functional UI strictly black and white.
4. **Bilingual by default.** A Taipei studio speaks Traditional Chinese and English in the same breath. *UI implication:* every type style must fall back to `Noto Sans TC`; the language toggle (EN / 中) is first-class.
5. **Restraint amplifies craft.** A loud reel needs a quiet room. *UI implication:* use whitespace and neutral grotesque type as the matte; resist every urge to add visual flourish to the frame.

Treating the following as a capture-bound application of source §7 Do’s and live inspect, including reserved-red-orange-for-full-bleed-tiles, secondary-accents-as-full-screen-fields-not-buttons-or-links, and whitespace-and-full-bleed-tiles-do-the-separating, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

- Keep the canvas pure white (`#ffffff`) and ink pure black (`#000000`).
- Use `0px` radius on every recorded element.
- Reserve the Bito red-orange family (`#f92c16` / `#ff5529` / `#e74118`) for full-bleed project/contact tiles.
- Use the secondary accents (`#4419fe` blue, `#fb76ff` magenta, `#166b22` green) as full-screen color fields, not as buttons or links.
- Set Latin type in Neue Haas Unica and Traditional Chinese in Noto Sans TC.
- Stay within weights 400 (nav/body/tags) and 500 (labels/titles).
- Let whitespace and full-bleed tiles do the separating.
- Use faint grey (`#bebebe`) for inactive tags and mid grey (`#707070`) for muted meta.

### Avoid

The following items copy source §7 Don’ts. Their extra causal readings — flat-shadow-free-system, entire-recorded-system-0px, architectural-color-fields-not-interactive-chrome, type-stays-even-at-400-500, tiles-butt-edge-to-edge-full-bleed, Neue-Haas-Unica-owns-the-Latin-voice, red-orange-lives-only-on-large-saturated-tiles, and white-canvas-is-the-recorded-frame — are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification.

- Do not use drop shadows for elevation — the source records a flat, shadow-free system.
- Do not round any corner — the recorded system is `0px` radius.
- Do not turn the accent colors into buttons or links — they are architectural color fields, not interactive chrome.
- Do not introduce a heavy display weight or italics — type stays even at 400/500.
- Do not add gutters between project tiles — they butt edge-to-edge, full-bleed.
- Do not use a serif or a non-grotesque Latin face — Neue Haas Unica owns the Latin voice.
- Do not spread the red-orange thinly across small UI — it lives only on large saturated tiles.
- Do not add decorative cards-with-shadows chrome — the white canvas is the recorded frame. The last item’s white-canvas-is-the-recorded-frame reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-primary-as-warm-tile-not-button, warm-family-variants-unmerged, project-art-fields-not-links, ink-not-a-second-identity, canvas-unmerged-from-on-accent, muted-not-faint, hairline-not-elevation, most-intense-tiles, decorative-accent, grounded-forest-green, otherwise-shadow-free-layout, and canvas-line-shared-hex-with-on-accent-named-fields-unmerged, are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. Catalog `primary_color` `#f92c16` is YAML `primary` and the Warm Project Tile fill; it is not a button or link. Accent Orange `#ff5529`, Accent Deep `#e74118`, and Accent Red `#ff2700` stay unmerged warm-family variants. Electric Blue `#4419fe`, Cobalt `#126dff`, Hot Magenta `#fb76ff`, and Deep Green `#166b22` are project-art color fields, not interactive chrome. Ink `#000000` is primary text, nav, headings, and labels; it is not catalog `primary_color`. Canvas `#ffffff` is page background and card surfaces; YAML `on-accent` `#ffffff` is text reversed out over warm/dark tiles and stays an unmerged named field. Mid Grey `#707070` is secondary metadata; Faint Grey `#bebebe` is inactive filter tags; Hairline `#e2e2e2` is thin dividers — they stay unmerged.

**Primary Accent (Bito Red-Orange)**

- **Bito Red-Orange** (`#f92c16`): YAML `primary`. Catalog `primary_color`. Full-bleed background behind project thumbnails and contact blocks.
- **Accent Orange** (`#ff5529`): YAML `accent-orange`. Brighter orange variant on alternate full-bleed tiles.
- **Accent Deep** (`#e74118`): YAML `accent-deep`. Deeper red-orange variant on the contact/social full-bleed block.
- **Accent Red** (`#ff2700`): YAML `accent-red`. Pure-red end of the warm family for the most intense tiles.

**Secondary Accents (from project art)**

- **Electric Blue** (`#4419fe`): YAML `accent-blue`. Vivid cobalt-violet blue pulled from project covers — used as a full-screen color field, never as a link.
- **Cobalt** (`#126dff`): YAML `accent-cobalt`. Brighter blue accent on other project tiles.
- **Hot Magenta** (`#fb76ff`): YAML `accent-magenta`. Saturated pink-magenta decorative accent.
- **Deep Green** (`#166b22`): YAML `accent-green`. A grounded forest green accent for contrast tiles.

**Ink & Canvas**

- **Ink Black** (`#000000`): YAML `ink`. Primary text, nav, headings, labels.
- **Canvas White** (`#ffffff`): YAML `canvas`. Page background, card surfaces, and text reversed out over the warm/dark tiles as a shared hex with YAML `on-accent`.
- **On Accent** (`#ffffff`): YAML `on-accent`. Reversed-out type on warm/dark tiles. Same hex as Canvas; named fields stay unmerged.

**Neutral Scale**

- **Mid Grey** (`#707070`): YAML `muted`. Secondary metadata and muted captions.
- **Faint Grey** (`#bebebe`): YAML `faint`. Inactive filter tags (genre/service labels), lowest-emphasis text.
- **Hairline** (`#e2e2e2`): YAML `hairline`. Thin dividers and subtle separators in the otherwise shadow-free layout.

### Spacing

YAML `spacing` is unitless: xs 4, sm 8, md 16, base 24, lg 32, xl 40, section 96. Treating those YAML numbers as unitless token numbers rather than a claimed px scale, while keeping the §5 observed px scale as a separate observation, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Observed scale in the body: 4px, 8px, 16px, 24px, 32px, 40px, 96px (section). Header padding measured at 32px 40px (vertical / horizontal) on the 96px-tall top bar. Treating that 32px 40px header padding and 96px bar as recorded header measurements rather than a universal padding token is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Treating harvested control padding as staying with those controls, not as a universal padding token, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. YAML component records do not list per-control padding; 0px radius and the header 32px 40px measurement stay with those surfaces.

### Shape

YAML `rounded`: sm 0, md 0, lg 0, full 0.

The following local-geometry reading, including Zero as the recorded geometry on every tile, label, and container, no-radius-scale, and 0px-not-a-universal-radius-for-unlisted-controls, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

- Zero (0px): every recorded tile, label, and container — the source says there is no radius scale

0px remains local harvested geometry, not a universal radius for every unlisted control.

### Elevation

The following table Use assignments (everything-nav-tiles-footer-project-pages; project-tiles-read-as-raised-purely-through-saturated-color-not-shadow) are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Everything — nav, tiles, footer, project pages |
| Color field (Level 1) | Full-bleed accent background (`#f92c16` / `#4419fe` …) | Project tiles read as "raised" purely through saturated color, not shadow |

YAML `tokens.shadow.none` is `none`. Live inspect found `box-shadow: none` across the nav, Work grid, project tiles, and footer.

The following divider-not-elevation and shadow-philosophy readings, including the table Use assignments, figure-ground contrast, brilliant-color-field-as-foreground, black-tiles-by-tonal-weight, no-skeuomorphic-elevation, depth-not-simulated, agency-aesthetic-in-its-purest-form, and flat-planes-of-color-and-type, are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. The source reads Bito as a fully shadowless system. Depth is not simulated — the studio relies on raw figure-ground contrast: a brilliant full-bleed color field (the Bito red-orange `#f92c16`, or electric blue `#4419fe`) reads as foreground against the white canvas, and black tiles (`#000000`) recede or advance by tonal weight alone. The source calls this the agency aesthetic in its purest form: no skeuomorphic elevation, just flat planes of color and type.

### Motion

Source-stated duration roles. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list. Treating §15 as philosophy-layer rather than live-inspect, treating the duration-table Use assignments (nav-hover-tag-selection-language-toggle / tile-hover-reveal-project-media / full-bleed-section-crossfades-hero-reel-reveals), treating the easing-table Use assignments (arriving / dismissals-tile-hover-out / two-way-transitions), treating easing names, chrome-stays-calm / no-bounce / no-spring readings, and the reduced-motion line as source-stated rather than computed CSS, treating the omitted `ease-exit` curve as matching the legacy spec-template example, and treating the “expressive motion lives inside the embedded reels” reading as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Nav hover, tag selection, language toggle |
| `motion-standard` | 240ms | Tile hover reveal, project media transitions |
| `motion-slow` | 400ms | Full-bleed section crossfades, hero reel reveals |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Tile and media arriving |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals, tile hover-out |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including chrome-stays-calm, reels-carry-expression, never-competes-with-the-work, no-bounce, no-spring, tile-hover-reveal, section-crossfade, chrome-transitions-collapse-to-instant, autoplaying-reels-respect-the-preference, and reduced-motion-fully-navigable, are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. As a motion-design studio, Bito's most expressive motion is described as living inside the embedded reels and project films, not the site chrome — the UI framing stays deliberately calm so it never competes with the work. Tile hovers reveal project titles with a quick `motion-standard / ease-enter` fade; full-bleed sections crossfade at `motion-slow`. `ease-enter` here is the source-stated token name, not a computed curve. There is no bounce or spring on the chrome. Under `prefers-reduced-motion: reduce`, all chrome transitions collapse to instant and autoplaying reels respect the preference; the portfolio remains fully navigable.

Treating exact cubic-bezier curves as unattributed — `ease-exit` matching the legacy spec template — and remaining omitted rather than promoted is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including pairing Neue Haas Unica as the Latin face with Noto Sans TC as the Traditional Chinese companion rather than a substitute for the Latin face, are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Live inspect records Latin as `neue-haas-unica` and Traditional Chinese as `Noto Sans TC` on the two captured URLs. Nav, titles, body, and tags use Neue Haas Unica at the sizes in Type roles. |

The following font-use boundary, including system-stack-not-Neue-Haas-Unica, no-replacement-Latin-family, and Noto-Sans-TC-not-the-Latin-face, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. Do not present a system stack as Neue Haas Unica. Do not replace Neue Haas Unica with a different claimed Latin family. Do not present Noto Sans TC as the Latin face.

The following type-rule readings (neutral grotesque not display; two weights only; bilingual by default; normal tracking; type-recedes-so-imagery-dominates; hierarchy-from-size-and-color-not-weight-extremes; no-serif-no-italic-no-heavy-display) are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. Neue Haas Unica is described as chosen for its evenness — the type recedes so the motion/branding imagery dominates. No serif, no italic, no heavy display weight. 400 carries nav, body, and tags; 500 carries section labels and project titles. Hierarchy is built from size and color, not weight extremes. Every text style falls back to `Noto Sans TC`. Letter-spacing stays `normal` across the system.

### Family

- **Latin:** `neue-haas-unica` — YAML `family.latin`, with `Noto Sans TC` and system fallbacks named in the body
- **Traditional Chinese:** `Noto Sans TC` — YAML `family.cjk`

### Type roles

Verified YAML line-height values are the unitless ratios `1.50` and `1.20`. The following ratio-versus-size-local, rem-as-size-local, and omitted-tracking-not-invented readings are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. 42px beside Project Title `1.50`, 28.8px beside Section / Filter Label `1.20`, 19.2px beside Nav Link / Tag / Meta `1.20`, and 24px beside Body `1.50` are size-local observations, not replacements for the ratios. 28px (1.75rem), 24px (1.50rem), and 16px (1.00rem) are size-local rem/px pairs, not a second scale. YAML roles without a tracking field stay without one; body “Normal tracking” remains a type rule, not a invented numeric tracking token.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Use (YAML / body) |
|---|---|---:|---:|---:|---|---|
| Project Title | Neue Haas Unica | 28px (1.75rem) | 500 | 1.50 | 42px at this size | YAML `Project page H3 title, Neue Haas Unica medium`; body “The 60th Golden Horse Awards Branding” |
| Section / Filter Label | Neue Haas Unica | 24px (1.50rem) | 500 | 1.20 | 28.8px at this size | YAML `Filter / section labels (Genre, Service, Sector, Credits)` |
| Nav Link | Neue Haas Unica | 16px (1.00rem) | 400 | 1.20 | 19.2px at this size | YAML `Top nav links (Work, News, About, Bitween)` |
| Body | Neue Haas Unica | 16px (1.00rem) | 400 | 1.50 | 24px at this size | YAML `Standard reading text, Neue Haas Unica regular` |
| Tag / Meta | Neue Haas Unica | 16px (1.00rem) | 400 | 1.20 | 19.2px at this size; faint `#bebebe` | YAML `Faint metadata / tag chips (#bebebe)` |

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Treating recorded project art, reels, and full-bleed color fields as not replaceable with invented brand-color decoration on functional chrome, without classifying those assets’ ownership, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated.

The following classification of the source §14 table as philosophy-layer implementation guidance (sections 10–15), not live-inspect paints, including filter-labels-still-visible-to-adjust, surrounding-title-credits-stay-visible, tiles-fill-with-media-as-it-loads, media-area-holds-full-bleed-dimensions, retry-affordance-in-functional-copy, and stays-sharp-cornered-and-flat, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. The source HTML comment attaches live inspect to token-level claims in §1–9. These rows describe empty/loading/error/success/skeleton/disabled treatments. They are not copied onto harvested controls as computed paints.

| State | Treatment |
|---|---|
| **Empty (no projects match filter)** | White canvas, single black (`#000000`) Neue Haas Unica line stating no matching work, with the filter labels (Genre/Service/Sector) still visible to adjust. No illustration. |
| **Empty (news, none yet)** | Faint-grey (`#bebebe`) single line indicating nothing posted; calm and minimal, consistent with the editorial tone. |
| **Loading (Work grid)** | Full-bleed tile placeholders in flat neutral fields — no shimmer, no shadow, consistent with the shadowless system; tiles fill with media as it loads. |
| **Loading (project media)** | Media area holds its full-bleed dimensions in a flat neutral block; surrounding title/credits text stays visible. |
| **Error (media failed to load)** | Black (`#000000`) plain-language line within the tile's footprint, no generic error chrome; a retry affordance in functional copy. |
| **Error (form / contact)** | Field-level black text below the input describing what is invalid; no decorative error styling, in keeping with the stark canvas. |
| **Success (contact sent)** | Brief inline black confirmation line in functional copy; no celebratory emoji or color burst. |
| **Skeleton** | Flat neutral blocks at final full-bleed dimensions, `0px` radius, no shimmer — a quiet flat pulse at most. |
| **Disabled** | Reduced-emphasis faint grey (`#bebebe`) on the label; the element stays sharp-cornered and flat. |

Characterizations in that table such as “No illustration”, “calm and minimal, consistent with the editorial tone”, “no shimmer, no shadow, consistent with the shadowless system”, “tiles fill with media as it loads”, “surrounding title/credits text stays visible”, “no generic error chrome”, “a retry affordance in functional copy”, “no decorative error styling, in keeping with the stark canvas”, “no celebratory emoji or color burst”, “a quiet flat pulse at most”, and “the element stays sharp-cornered and flat” are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source never records `focus-visible` or a generic Focus capture. The `focus-visible` row does not carry a colour. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows, except where source §14 names the role directly (Work grid / project media tiles). This is not a complete state-coverage claim.

Treating Filter Label as C4 omit because YAML type `badge` is a heading without interactive-kind confirmation, and treating Warm Project Tile and Dark Project Tile as Kind: interactive from source §8 tap-target evidence (“the entire image is interactive”) rather than from YAML `type: card` alone, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. Filter Label has YAML type `badge` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Warm Project Tile and Dark Project Tile keep Kind: interactive from source §8.

### Top Nav Link

- Role: header nav items (Work, News, About, Bitween)
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: transparent
- Text: `#000000`
- Radius: 0px
- Font: 16px Neue Haas Unica weight 400
- Active: ink `#000000` label, no underline indicator
- Use: YAML `Top nav item (Work/News/About/Bitween)`
- Observed: default only. The active appearance is a named variant, not an observed click transition
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#000000` is this control’s label and Ink. Transparent fill is this control’s field, not Canvas. Treating the static active appearance as a named variant rather than an observed click transition is the same class of reading.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header nav item |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav tab selects a site section (Work / News / About / Bitween); the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

Additional named state: static active appearance (ink `#000000` label, no underline indicator). Treating that appearance as a named variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

### Language Toggle

- Role: EN / 中 language switch in the header
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: transparent
- Text: `#000000`
- Radius: 0px
- Font: 16px Neue Haas Unica weight 400
- Use: YAML `EN / 中 language switch in header`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#000000` is this control’s label and Ink. Transparent fill is this control’s field, not Canvas.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header language switch |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A language option can be unavailable; visual treatment omitted |
| loading | not-applicable | A language tab selects a locale (EN / 中); the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is locale selection, not action-outcome confirmation |

### Warm Project Tile

- Role: full-bleed warm project thumbnail in the Work grid
- Kind: interactive
- Type: card
- Anatomy: surface
- Background: `#f92c16`
- Text: `#ffffff`
- Radius: 0px
- Use: YAML `Full-bleed warm project thumbnail in the Work grid (also #ff5529 / #e74118 variants)`
- Observed: default only
- Field note: The following unmerged-variant reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#f92c16` is this tile’s fill and catalog `primary_color`. `#ff5529` and `#e74118` are named fill variants on this same tile recipe, not a second component and not buttons. `#ffffff` is this tile’s overlay text and YAML `on-accent`, not Canvas. Kind: interactive is from source §8 tap-target evidence, not from YAML `type: card` alone.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the warm Work-grid project tile |
| hover | applicable | Pointer-web tile; visual treatment omitted. Source §15 names a tile-hover title reveal as `motion-standard / ease-enter`; that row is philosophy-layer guidance, not a computed paint |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A project tile can be unavailable; visual treatment omitted |
| loading | applicable | Source §14 names Work-grid / project-media loading: full-bleed tile placeholders in flat neutral fields; media area holds full-bleed dimensions. Those rows are §14 guidance, not computed paints on this harvest |
| error | applicable | Source §14 names media-failed-to-load: black plain-language line within the tile's footprint. That row is §14 guidance, not a computed paint on this harvest |

Success applicability is omitted. Source §14 names success as contact sent, not a tile-outcome confirmation, so that field stays omitted at this boundary rather than closed from the §14 rows.

### Dark Project Tile

- Role: black project cover tile
- Kind: interactive
- Type: card
- Anatomy: surface
- Background: `#000000`
- Text: `#ffffff`
- Radius: 0px
- Use: YAML `Black project tile / cover, sharp-cornered`; body “Black project cover tile, sharp-cornered, no shadow”
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#000000` is this tile’s fill, not Ink used as text on white. `#ffffff` is this tile’s overlay text and YAML `on-accent`, not Canvas. Kind: interactive is from source §8 tap-target evidence, not from YAML `type: card` alone.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the black Work-grid project tile |
| hover | applicable | Pointer-web tile; visual treatment omitted. Source §15 names a tile-hover title reveal as `motion-standard / ease-enter`; that row is philosophy-layer guidance, not a computed paint |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A project tile can be unavailable; visual treatment omitted |
| loading | applicable | Source §14 names Work-grid / project-media loading. Those rows are §14 guidance, not computed paints on this harvest |
| error | applicable | Source §14 names media-failed-to-load within the tile's footprint. That row is §14 guidance, not a computed paint on this harvest |

Success applicability is omitted. Source §14 names success as contact sent, not a tile-outcome confirmation, so that field stays omitted at this boundary rather than closed from the §14 rows.

### Filter Label

- Role: Genre / Service / Sector filter heading
- Type: badge
- Anatomy: label
- Text: `#000000`
- Radius: 0px
- Font: 24px Neue Haas Unica weight 500
- Use: YAML `Genre / Service / Sector filter heading`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#000000` is this heading’s label and Ink. 24px / 500 is this heading and YAML `label-lg`, not Nav 16px / 400.

No interactive-kind evidence is given for this heading. Kind and a state-applicability map are omitted.

### Tag Chip (Inactive)

- Role: inactive genre/service tag
- Kind: interactive
- Type: badge
- Anatomy: label
- Text: `#bebebe`
- Radius: 0px
- Font: 16px Neue Haas Unica weight 400
- Use: YAML `Inactive genre/service tag (Live Action, Event, Branding…)`; body also names Ceremony
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#bebebe` is this chip’s label and Faint Grey, not Mid Grey `#707070` and not Ink. Kind: interactive is from the inactive/active filter-tag role plus source §15 “tag selection”, not from YAML `type: badge` alone. Treating source §14 Disabled as not copied here as a computed paint is the same class of reading.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the inactive genre/service tag |
| hover | applicable | Pointer-web chip; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A filter tag can be unavailable; visual treatment omitted. Source §14 Disabled names `#bebebe` on the label; that row is §14 guidance, not a computed paint |
| loading | not-applicable | A genre/service tag selects a filter; the chip itself does not enter a loading state |
| error | not-applicable | Chip meaning is selected versus inactive, not a request or validation failure on the chip |
| success | not-applicable | Chip meaning is filter selection, not action-outcome confirmation |

### Footer Link

- Role: footer contact / social link
- Kind: interactive
- Type: listItem
- Anatomy: label
- Text: `#000000`
- Font: 16px Neue Haas Unica weight 400
- Use: YAML `Footer contact / social link (Facebook, Instagram, info@bito.tv)`; body also names Behance
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#000000` is this link’s label and Ink, not the §9 contact-footer local white `#ffffff` on `#e74118`. Type: `listItem` is the YAML primitive; it is not rewritten as `link` or `tab`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the footer contact / social link |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A footer contact / social link can be unavailable; visual treatment omitted |
| loading | not-applicable | This control is an outbound contact or social destination; waiting does not live as a loading state of this list item |
| error | not-applicable | Failure is not reported as an error state of this footer link |
| success | not-applicable | Opening a contact or social destination is not a success paint on this list item |

### Contact Footer Block

Treating the following as a source §9 local composition — full-bleed `#e74118` parent with white `#ffffff` Neue Haas Unica 16px links — and not as a rewrite of YAML Footer Link (`#000000`), is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

- Role: full-bleed contact / social parent surface
- Anatomy: surface
- Background: `#e74118` (full-bleed)
- Radius: 0px
- Shadow: none
- Local links (this composition only): white `#ffffff` Neue Haas Unica 16px — Facebook, Instagram, Behance, `info@bito.tv`
- Use: source §9 contact footer block — full-bleed deep red-orange (`#e74118`) background, white (`#ffffff`) Neue Haas Unica 16px links — Facebook, Instagram, Behance, info@bito.tv — sharp corners, no shadow
- Kind, Type, and a state-applicability map are omitted. YAML Footer Link remains `Type: listItem` with Text `#000000`.
- Field note: The following unmerged-composition reading, including parent-fill-not-Footer-Link-background, white-on-orange-not-a-rewrite-of-YAML-ink, and sharp-corners-no-shadow-as-this-block, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. `#e74118` is this block’s parent fill and Accent Deep. `#ffffff` is this block’s local link text, not Canvas and not YAML Footer Link. YAML Footer Link stays `#000000`. Radius 0px and no shadow stay on this parent; they are not a second Footer Link recipe.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 16, base 24, lg 32, xl 40, section 96. Body layout repeats 4px, 8px, 16px, 24px, 32px, 40px, 96px. Header padding is 32px 40px on a 96px-tall top bar. Work tiles are recorded at full viewport width (1440px) or half (668px). Project titles are 28px. Filter labels are 24px.

The “negative space as frame” / gallery-first / image-led / dense-Work-index / edge-to-edge tiles / white-canvas-as-the-matte-around-the-work / no decorative chrome readings in this section are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification.

Recorded layout:

- Header: 96px transparent bar with nav left/center and language + socials right
- Work index: a dense, edge-to-edge grid of full-bleed project tiles — many at full viewport width (1440px) or half (668px)
- Project pages: large media stacked with a centered title (28px) and a Credits / Genre / Service / Sector metadata block
- No max-width "reading column" chrome — the layout is gallery-first, image-led
- Negative space as frame: the white canvas is the matte around the work
- Edge-to-edge tiles: project thumbnails extend full-bleed, color fields with no rounding or border, abutting directly
- No decorative chrome: no cards-with-shadows, no rounded containers

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single-column full-bleed tile stack, hamburger nav, 28px title compresses |
| Tablet | 640-1024px | 2-up tile grid, header padding tightens |
| Desktop | 1024-1440px | Multi-column full-bleed Work grid, 96px header bar |

Collapsing strategy recorded in the source: Work grid multi-column full-bleed → 2-up → single-column stacked, always edge-to-edge; project pages large media remains full-width, title + Credits/Genre/Service block stacks; header full nav row → hamburger toggle on narrow viewports; type scale compresses: 28px titles and 24px labels step down on mobile, weights preserved.

Touch-target record: nav links sit in the generous 96px header; full-bleed project tiles are themselves the tap target; language toggle (EN / 中) and social links grouped at the header right.

Image behavior recorded in the source: project thumbnails and covers carry no shadow and no radius at any size — flat full-bleed; motion/video work plays edge-to-edge; the white canvas only reappears between sections; color-field tiles maintain their saturated accent backgrounds across breakpoints.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 96px / 32px 40px / 1440px / 668px / 28px / 24px figures as surface measurements in this packet rather than universal layout tokens, treating the collapsing-strategy and image-behavior rows as recorded packet application rather than a complete responsive specification, treating always-edge-to-edge collapsing, generous-96px-header, white-canvas-only-reappears-between-sections, and saturated-accent-backgrounds-across-breakpoints as those packet readings, and treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-17)

The live strings below are source-stated. Treating the live-inspect attributions and the harvested-control-string grouping as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

- “Work / News / About / Bitween” — the entire primary nav, single-word functional labels. *(verified live 2026-06-17, https://bito.tv/)*
- “The 60th Golden Horse Awards Branding” — project title, descriptive and proper-noun-led. *(verified live 2026-06-17, /work/148)*
- “© Bito 2026 — Taipei, Taiwan · info@bito.tv” — footer contact line, plain and direct. *(verified live 2026-06-17, https://bito.tv/)*
- Control strings from harvested components and §4: EN / 中; Genre, Service, Sector, Credits; Live Action, Event, Ceremony, Branding; Facebook, Instagram, Behance, info@bito.tv

Parenthetical tone labels on the three verified-live lines (“single-word functional labels”, “descriptive and proper-noun-led”, “plain and direct”) are part of the source’s sample captions. Treating those caption labels as citation-character rather than extra Observed strings is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

Treating “Typography” and “TAIWAN Tourism Rebranding” as derived genre-tone table examples rather than live Observed samples is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — including spare-bilingual-craft-forward, words-get-out-of-the-way, functional-English-nav-over-Traditional-Chinese-project-context, no-taglines-shouting, portfolio-carries-the-persuasion, copy-reduced-to-labels-credits-contact, table tone labels (single-word-functional / structured-and-neutral / descriptive-proper-noun-led / plain-and-direct / terse-categorical), and work-wins-the-argument-words-only-label-it — are a derived editorial implementation inference from the verified surfaces; they are not Bito-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Bito's verbal voice as spare, bilingual, and craft-forward — the words get out of the way so the work can speak. The site's vocabulary is almost entirely functional English nav (Work, News, About, Bitween) over Traditional-Chinese project context, with no marketing superlatives and no taglines shouting from the homepage. The studio lets the portfolio carry the persuasion; copy is reduced to labels, credits, and contact lines.

| Context | Tone |
|---|---|
| Navigation | Single-word, functional. "Work", "News", "About", "Bitween". |
| Project metadata | Structured and neutral. "Genre", "Service", "Sector", "Credits" as plain labels. |
| Project titles | Descriptive, proper-noun-led. "The 60th Golden Horse Awards Branding", "TAIWAN Tourism Rebranding". |
| Contact / footer | Plain and direct. "© Bito 2026 — Taipei, Taiwan", "info@bito.tv". |
| Genre tags | Terse, categorical. "Live Action", "Event", "Ceremony", "Branding", "Typography". |

**Forbidden register** (source): marketing hype, superlatives ("award-winning", "world-class") in body chrome, exclamation-heavy copy, or any verbal flourish that competes with the imagery. The work wins the argument; the words only label it.

Not promoting synthetic voice samples beyond the quoted live lines is a derived editorial implementation inference from the verified surfaces; it is not Bito-authored or a separately published UI specification. No synthetic voice samples are promoted.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- loading, error, and success visual treatments on harvested controls, and the omitted success applicability field on Warm Project Tile and Dark Project Tile
- interactive kind and state-applicability map for Filter Label
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
- form / contact field geometry (source §14 names form/contact error and contact-sent success; no form component is harvested)
