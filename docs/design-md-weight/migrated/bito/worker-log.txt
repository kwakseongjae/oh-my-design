T2 마이그레이션을 시작합니다. 지정된 규격·규칙집·골든 샘플부터 순서대로 읽고, 그다음 bito legacy를 Core v2로 옮기겠습니다.규칙집을 확인했습니다. 이어서 대응표, T1 제약, 의미 보존 조건, musinsa 골든 샘플, 그리고 bito legacy를 읽겠습니다.골든 샘플과 bito 원문을 읽고, 최근 통과 이관본의 형태도 맞춰 보겠습니다.최근 통과본과 게이트 규칙을 대조해, C2·B2a·B3를 어떻게 닫는지 확인하겠습니다.ASOS와 같은 철학층(§10–15)·라이브 인스펙트(§1–9) 구조를 기준으로 bito 본문을 작성합니다. 값·타입·B2a 한정을 원문에 맞춰 옮기겠습니다.Portable Core가 scope claim을 통과시키지 못했습니다. 검사기가 무엇을 요구하는지 확인하겠습니다.Scope claim 안의 `not a product` / `does not sell a product`가 Portable Core 부정 검사에 걸렸습니다. 그 문장만 재구성합니다.Portable Core는 통과했습니다. 이제 본문 전체를 다시 읽어 B2a 스캔을 하고, grep으로 로그 목적지를 잡겠습니다.게이트는 PASS입니다. 로그에 가상 페르소나 이름이 남아 있어 D2에 맞게 이름만 빼고 역할 배제만 남기겠습니다.`web/references/bito/DESIGN.md`를 Core v2로 옮겼습니다. 산출물은 `docs/design-md-weight/migrated/bito/`의 `DESIGN.md`, `provenance.md`, `migration-log.md`입니다. 규칙집은 v7입니다.

핵심 보존·분리:

- 검증된 색·간격·0px radius·`box-shadow: none`·Neue Haas Unica / Noto Sans TC·YAML `lineHeight` `1.50` / `1.20`(비율)과 컴포넌트 primitive(`tab` ×2, `card` ×2, `badge` ×2, `listItem`)는 본문에 남겼습니다.
- 가상 persona는 Audience/과업으로 올리지 않았고 provenance에도 재수록하지 않았습니다. 과업 3건은 Work 그리드, Golden Horse 프로젝트 페이지, EN / 中 토글입니다.
- 무출처 cubic-bezier 3개(그중 `ease-exit`는 규격 템플릿)는 provenance 생략 원장으로 분리했습니다. `120ms` / `240ms` / `400ms`와 reduced-motion, B3 다섯 증거 종류 게이트는 Foundations에 남겼습니다.
- Filter Label은 kind/map을 생략했고(C4), 타일 loading/error는 §14 역할로 applicable, success는 contact-sent와 분리해 생략했습니다(C2).

기계 검사: `--gate-only` PASS, `portable_core: true`. SHA-256 `b26008e2d502315d72c584faffb84d69dc8b4ae780d37589f052935fd22ff203`. 카탈로그 채택은 아닙니다.

DONE migrated=1
