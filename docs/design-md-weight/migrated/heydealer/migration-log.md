# heydealer migration log

Source: `web/references/heydealer/DESIGN.md`
Sibling read (not the migration input): `web/references/heydealer/.verification.md`
Destination: `docs/design-md-weight/migrated/heydealer/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/heydealer/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `c68d57ba7e181c873700cd1a678a8b05deecc2ba334d1fe27b069c11ac2d5058` (`web/references/heydealer/DESIGN.md`). Sibling SHA-256 `6f89787a6e79017b28fb04477e6dbf49e4a1000a1fefa22291a5663ded6fb7f3` (`web/references/heydealer/.verification.md`). Worker-close portable DESIGN SHA-256 `5224d006c9a76f15e8c8e5f4096e43353ebce225e365ef09484b79fa0d1412d7`. Auditor F3 portable DESIGN SHA-256 `65e31c5e95d7604e093e4e84a76c45afbe8d2f5216e22e44fc1a2e19566d3d45` (Scope ¶1 qualifier expanded; Official distributed asset qualifier added; 537 lines unchanged).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Heydealer Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–22. Homepage `https://www.heydealer.com/` as its own URL (not a prefix of `/market/…` or `/sell`) is dual: `DESIGN.md` 9/26 + `provenance.md` 14/45/52 (E2a). `#396eff` is dual: `DESIGN.md` 11/39/54/63/94 + `provenance.md` 15 (E2a). Favicon slug is dual: `DESIGN.md` 220 + `provenance.md` 17 (E2a). `헤이딜러` is dual: `DESIGN.md` 9/13/475 + `provenance.md` 11. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 19–22, 32–35. The source footer's **Verified:** 2026-07-02 is at `provenance.md` 37. `live-extract` DESIGN 0 / provenance 1. `components_harvested` DESIGN 0 / provenance 2. These are ledger keys with no portable slot. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations; 분리 → provenance | Full note transcribed at `provenance.md` 66. The facts it names (live CTA blue on 바로 구매예약, secondary pure black, near-black ink never a soft grey, `#e9eaec` hairlines + `#70727c` tint, 4px workhorse) land in `DESIGN.md` 11/94–103. `rgb 57,110,255` DESIGN 0 / provenance 1 (ledger spelling of the same hex). Dual (E2a). |
| YAML `tokens.colors` (12 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 94–116. All twelve roles kept with names, values, and token-set keys: primary, ink, ink-pure, ink-navy, chip-dark, muted, muted-alt, muted-grey, surface, canvas, on-dark, hairline. `#000000` as `ink-pure` is the secondary-button fill, not body text (`DESIGN.md` 97). `#0d0d0e` as Ink is restated under Text hierarchy at 113 without merging the keys. |
| YAML `tokens.typography.family.sans` (`spoqaHanSansNeo`) | 옮김 → Typography & Assets Family | Family stack `DESIGN.md` **193**. Declared-only fallback classing **187** (not Family). Live computed stack also at **185**. `spoqaHanSansNeo` dest 9. |
| YAML `tokens.typography.display / section / card-head / emphasis / body / meta` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 201–206. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.38` dest 2 · line-height `1.40` dest 2 (201/207; `grep -o '1.40'` = 3 because Desktop `1024-1440px` at 441 shares the digits) · `1.44` dest 2 · `1.47` dest 3 · `1.50` dest 2. All six YAML `use` strings restored verbatim in the Token-set use column (A1, A3). YAML `tokens.typography.card-head.tracking: -0.25` stays unitless at `DESIGN.md` 207 beside visible `-0.252px` (A1; neither form was chosen as a replacement). |
| YAML `tokens.spacing` (6 steps) / `tokens.rounded` (4 steps) / `tokens.shadow.none` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 119 (`xs 4 · sm 8 · md 12 · base 16 · lg 24 · section 48`). Shape: `DESIGN.md` 123 (`sm 2 · md 4 · lg 16 · full 9999` plus the named uses). `full: 9999` (colon form) dest 5 (`DESIGN.md` 130/131/133/431/536). The unitless step `full 9999` is at `DESIGN.md` 123. Body-named pill `100px` dest 5 and circle `50%` dest 4 are different keys (`DESIGN.md` 128–133). `tokens.spacing.base: 16` is disambiguated from `tokens.rounded.lg: 16` at 119/133. `tokens.shadow.none`: `none` at `DESIGN.md` 143. `box-shadow: none` dest 2 (`DESIGN.md` 11/143). |
| YAML `tokens.components` (7 records) | 옮김 → Components & States | `DESIGN.md` 237–403. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×3 (240/265/290 = YAML primary + secondary + §4 Nav Text Button), `tab` ×1 (384), `badge` ×1 (335), `card` ×2 (351/363), `input` ×1 (313). All seven YAML `use` strings restored verbatim as `Token-set use:` lines (249/274/319/344/356/366/392). YAML `font` shorthands kept as `token-set font record` lines: `15px / 700 spoqaHanSansNeo` dest 2 · `16px / 400 spoqaHanSansNeo` dest 3. |
| §1 Visual Theme & Atmosphere (legacy 57–74) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–15; Distinctive traits 38–46. The atmosphere readings (engineered tool, financial dashboard, precise instrument, reserved blue) carry an adjacent complete qualifier at 11 (B2/B2a). |
| §2 Color Palette & Roles (legacy 76–97) | 옮김 → Foundations semantic color | `DESIGN.md` 93–116. Characterizing phrases covered by the adjacent qualifier at 90 (B2/B2a). |
| §3 Typography Rules — family, hierarchy, principles (legacy 99–119) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 184–189; family 192–194; hierarchy table 201–206; type rules 209–216. The Official-product-use, Official-distributed-asset, Declared-only, License, Outside, Family, Type-roles ratio, and Type-rules readings carry adjacent complete qualifiers at 184, 186, 187, 188, 189, 194, 207, and 216 (B2/B2a). |
| §4 Component Stylings (legacy 121–197) | 옮김 → Components & States | `DESIGN.md` 237–403. §4 body values and YAML `use` / font / radius / height forms are both kept where they differ (Nav Text Button 15px / 700 / 8px at 290–297 is not YAML `nav-link` 16px / 400 at 384–392; Blue Info Strip at 370–377 has no YAML row). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 201–204) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 32–35; Tier 1 list 52–55; Tier 2 59–60. `https://www.heydealer.com/market/cars/gQ60AKy0` is dual: `DESIGN.md` 9/241/266 + `provenance.md` 47/53 (E2a). `https://www.heydealer.com/sell` is dual: `DESIGN.md` 9/25 + `provenance.md` 48/54 (E2a). `https://medium.com/prnd` is dual: `DESIGN.md` 9 + `provenance.md` 49/55 (E2a). Conflicts unresolved: none — `provenance.md` 39. |
| §5 Layout Principles (legacy 206–229) | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 425–431 (grid, gallery + sticky CTA, tint bands, spacing 4/8/12/16/24/48, shape 2/4/16/9999) and 433 (dense-but-breathable / flat segmentation / sticky-CTA isolation). 52px CTA height at 246/271/443 (441 is Desktop `1024-1440px`; 442 is the blank line after the breakpoint table). Pill `100px` and circle `50%` at 128–131. |
| §6 Depth & Elevation (legacy 231–239) | 옮김 → Foundations elevation | `DESIGN.md` 137–143: three-level table, `tokens.shadow.none`, `box-shadow: none`. The near-shadowless / anti-classified-stack reading carries an adjacent qualifier at 143 (B2/B2a). |
| §7 Do's (legacy 243–251) | 옮김 → Experience application rules | `DESIGN.md` 62–69 (eight Do rules), under the grouping qualifier at 60. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 253–261) | 옮김 → Experience avoid | `DESIGN.md` 75–82, under the qualifier at 73. The eight prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior (legacy 263–285) | 옮김 → Layout & Platforms | `DESIGN.md` 435–452: Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` at 437–441, 52px / ~36px targets at 443, collapsing strategy 445–450, 외부/실내 tiles at 452 (not inside 435–447). The Desktop row keeps the source body's `1024-1440px` range at 441. The system-level-not-cross-viewport reading carries an adjacent qualifier at 435 (B2/B2a). |
| §9 Agent Prompt Guide (legacy 287–315) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 144. |
| §10 Voice & Tone (legacy 319–336) | 옮김 → Content & Locales | Published strings `DESIGN.md` 467–496; tone table 461–465; forbidden register 496. The qualifier at 457 covers the voice reading and the tone-table contract (B2/B2a, A5). Byte-exact / gloss-beside close at 498. |
| §11 Brand Narrative (legacy 338–344) | 옮김 → Experience Scope | `DESIGN.md` 13: 2015, PRND, 피알앤디컴퍼니, reverse auction, 내차팔기, 2016, 헤이딜러 방지법, anti-Heydealer law, automobile-management law, 내차사기, 1-year free warranty, return-on-change-of-mind, 폐차, hidden-history checks. Refuse/embrace pairing at 15, including app-native interface and a single trustworthy blue. `app-first` at 9. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 189. |
| §12 Principles — 5 numbered (legacy 346–352) | 옮김 → Experience principles | `DESIGN.md` 52–56 under the B2a form at 50: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Heydealer-authored or a separately published UI specification." Heydealer has no published first-party design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 152–182 (31 rows). |
| §13 Personas — 3 entries (legacy 354–362) | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, or occupation appears in either output (D2, D2a). Disposition at `provenance.md` 138. Audience at `DESIGN.md` 32 carries only the group-level description the captured surfaces and §11 establish (owners in a reverse auction; buyers in the certified marketplace; scrap-car quotes; hidden-history checks), under an adjacent qualifier. The five `primary-tasks` at 23–27 come from labels and surfaces the source records; 21 says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows (legacy 364–376) | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at `DESIGN.md` 409–419 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "오류가 발생했습니다" and "필수" as the source's forbidden bare forms. The applicability rule is at 231–235, whose qualifier at 235 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary Purchase CTA opens loading/error/success because it is the purchase-reservation commit named by the source's state contract (`loading \| applicable` dest 1 · `error \| applicable` dest 2 · `success \| applicable` dest 1). Overlay Search opens error (form-validation row) and closes loading/success because the field does not commit the capture. Black Secondary, Nav Text Button, and Top Nav close loading/error/success with a role reason (C2 v10) — they commit no operation in place (`loading \| not-applicable` dest 4). The filter chip and Blue Info Strip declare `kind: non-interactive` with a reason instead of a map (Core §4.4) — `Kind: non-interactive` dest 2. The two cards get no `kind` and no map because the source supplies no interaction evidence for the containers — `DESIGN.md` 358/368 (C4). |
| §15 Motion & Easing — durations, easing roles, motion rules, reduced-motion (legacy 379–396) | 옮김 → Foundations motion | `DESIGN.md` 151–155 (120ms / 200ms / 320ms with uses), 159–163 (ease-enter / ease-exit / ease-standard roles and uses, curves omitted), 167–173 (functional and quiet, fade-in at `motion-standard / ease-enter`, no bounce or spring, `prefers-reduced-motion: reduce` collapses to instant). |
| §15 Motion & Easing — three curve values (legacy 390–394) | 삭제 → provenance omission ledger | `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are named as omitted at `DESIGN.md` 157 and are not promoted as tokens. Durations stay. Ledger at `provenance.md` 139–141. B3 is held: the promotion condition at `DESIGN.md` 165 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 534. |
| Opening product / atmosphere sentence (legacy 59) | 옮김 → Experience Scope | `DESIGN.md` 9–11: app-first used-car platform; three inspected surfaces; white canvas; reserved Heydealer Blue; Spoqa Han Sans Neo at 700/400. |

## Sibling handling (`web/references/heydealer/.verification.md`)

The sibling exists — confirmed with `find web/references/heydealer -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 74–93 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: nav height `38px`; reassurance height `48px`; `외부 17`; `모든 차 1년 무료 보증`; frequency counts; `4px 4px 0px 0px`; `dealer's eye`; `apple-touch-icon.png`; `heydealer — 0 DESIGN.md files | getdesign.md`. Recorded at `provenance.md` 97–105.
- Measured `DESIGN.md` 0 for those sibling-only strings: `38px` 0 · `외부 17` 0 · `모든 차 1년 무료 보증` 0 · `4px 4px 0px 0px` 0 · `dealer's eye` 0 · `apple-touch-icon` 0.
- `~36px` filter-chip height is already in the source §8 (`DESIGN.md` 346/443) and is corroboration, not a sibling-only promotion. 441 is Desktop `1024-1440px`; 442 is the blank line after that table.
- `바로 구매예약` / `인증 리포트` / `2.0 가솔린` / `모든 차량 1년 무료 보증` / document.title are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Heydealer is Korean-first, so a hand sweep of published labels is mandatory even when the needle set is non-empty.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 22 distinct | 0 | 0 | 헤이딜러 / 바로 구매예약 / 인증 리포트 / 색상 / 옵션 / 비슷한 차 / 내차사기 / 내차팔기 / 2.0 가솔린 / 모든 차량 1년 무료 보증 / 단순 변심도 무료 환불 가능 / 헤이딜러 홈 / 폐차 견적받기 / 중고차 숨은 이력 / 헤이딜러 – 인증중고차, 내차팔기, [번호판]시세 / 외부 / 실내 / 피알앤디컴퍼니 / 헤이딜러 방지법 / 오류가 발생했습니다 / 필수 / 최고의 / 혁신적인. |
| `node test-v2/tools/latin-copy-audit.mjs --brand heydealer` | 0 lost / 1 scanned | 0 | 0 | `scanned: 1`, `clean: 1`, `withLoss: 0`, `totalLost: 0`. English glosses that sit beside Korean lines (`book purchase`, `certified report`, `anti-Heydealer law`) are kept as glosses, not replacements (A5). |
| Sibling published strings | 2 distinct sibling-only | 0 | 0 | `외부 17` and `모든 차 1년 무료 보증` survive in provenance; none are promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 바로 구매예약, 인증 리포트, 내차사기, 내차팔기, 폐차 견적받기, 중고차 숨은 이력, 모든 차량 1년 무료 보증, 단순 변심도 무료 환불 가능, 헤이딜러 방지법, 2015, 2016, PRND, 피알앤디컴퍼니, app-first, app-native.

A5 분모: hand sweep of source published labels 22 extracted / 0 missing; latin-copy-audit 0 lost / 1 scanned; gate `copy-loss` compared 26 / candidates 175.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand heydealer --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 26, candidates: 175 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 26 < `candidates` 175.

## Deviations recorded

- `DESIGN.md` is 6,522 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: seven declared component records plus two §4-only records, a nine-row surface state contract, a seven-state applicability matrix on five interactive controls, `tokens.rounded.full: 9999` kept off the 100px pill and the 50% circle, the full §11 founding narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Heydealer has no published first-party design system, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Worker-close measure was 30=30. Auditor F3: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 31 = `not Heydealer-authored` 31 = `separately published UI specification` 31. Provenance derived ledger 31 rows (E1 1:1). Official distributed asset at `DESIGN.md` 186 was a third-class reading without an adjacent complete qualifier; Scope ¶1's qualifier now names the three-surface contract coverage.

## F1 B2a rescan (mandatory final pass)

The portable body was re-read from the title down. Every sentence that states a cause, a reading, or a judgment — Scope three-surface contract coverage + surface-split + PRND-not-tokens, atmosphere characterizations, narrative-not-token classification, refuse/embrace as instruction, primary-task selection, audience grouping, distinctive-trait grouping, the five principles, Do/Don't reasons, semantic-color characterizations, spacing/shape key-path splits, elevation philosophy, motion evidence class, font evidence-class readings including Official distributed asset, family fallback, type-role ratio keep, type-rule titles, favicon-pointer reading, applicability procedure, state-record editorial class, state-record non-attachment, layout density reading, breakpoint system-level reading, voice characterization, and the byte-exact / gloss-beside rule — has an adjacent complete qualifier. Principles 50 uses the toss-form example in full. No "does not say" closer was written. No invented domain (`native application` / `mobile app` / `back-office` / `product application` / `measures 1440px`) appears in `DESIGN.md` (each dest 0).

## F2 E2 rescan (mandatory final pass)

Each destination in the table above was grepped after the files were written. Dual destinations are both named. B3 is claimed only because `DESIGN.md` 165 contains the five evidence kinds plus the per-component gate and the partial-confirmation exclusion in full text; 534 restates the five kinds and the gate, not the partial-confirmation sentence (E2c). §13 deletion is unidentified in the omission ledger (`provenance.md` 138). Sibling-only strings are mentioned as disposition, not used as portable facts. Auditor F3 B2a 31=31.
