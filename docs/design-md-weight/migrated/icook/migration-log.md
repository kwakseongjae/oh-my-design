# icook migration log

Source: `web/references/icook/DESIGN.md`
Sibling read (not the migration input): `web/references/icook/.verification.md`
Destination: `docs/design-md-weight/migrated/icook/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/icook/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `961f6b3aa289bf22d25caf2487df9104506103780c3d9414eff88c07c7c2569b` (`web/references/icook/DESIGN.md`). Sibling SHA-256 `7e217fb482d7a970f2119864af4d8fd96a991a99a5c85ab61468d92cdd80fcb9` (`web/references/icook/.verification.md`). Worker-close portable DESIGN SHA-256 `f9cbdeeece9ebfdd4415eba694280d405d76030653f7fb93363b470393b626f0`. Provenance SHA-256 `5c5462e739bd981dcabfaeffecf8e760797bd48dd96b21887ffb2aaef24578a1`. Auditor-close hashes are recorded in `audit-log.md`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# iCook Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–22. Homepage `https://icook.tw` as its own URL (not a prefix of `/recipes/popular`) is dual: `DESIGN.md` 9/237 + `provenance.md` 14/44/49 (E2a). `#f04646` is dual: `DESIGN.md` 11/35/58/88/238/263/406/408/437 + `provenance.md` 15/59/61/71/72/75/77/149 (E2a). Favicon slug is dual: `DESIGN.md` 217 + `provenance.md` 17 (E2a). `愛料理` is dual: `DESIGN.md` 9/13/472/490 + `provenance.md` 11. |
| YAML `omd: "0.1"`, `verified`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 19–22, 32–35. The source footer's **Verified:** 2026-06-22 is at `provenance.md` 36. `live-extract` DESIGN 0 / provenance 1 (`provenance.md` 20). `components_harvested` DESIGN 0 / provenance 2 (`provenance.md` 22/194). These are ledger keys with no portable slot. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations; 분리 → provenance | Full note transcribed at `provenance.md` 61. The hex facts it names (coral-red `#f04646`; accent-warm `#f06354`; amber `#f0993c`; canvas warm-white `#fbfaf8`; ink-brown `#564e4a`) land in `DESIGN.md` 11/35–42/88–111. Dual (E2a). The live `rgb(240,70,70)` form is ledger-only: DESIGN 0 / provenance 2 (`provenance.md` 59/61). |
| YAML `tokens.colors` (14 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 88–111. All fourteen roles kept with names, values, and token-set keys: primary, primary-light, accent-warm, amber, ink, muted, muted-light, faint, canvas, surface, surface-warm, surface-mid, hairline, on-primary. `#ffffff` as `canvas` is the page/card/search/header role (`DESIGN.md` 98); `#ffffff` as `on-primary` stays a separate key (`DESIGN.md` 103). `#fbfaf8` is `tokens.colors.surface` (`DESIGN.md` 99), not canvas. |
| YAML `tokens.typography.family.display` / `family.body` | 옮김 → Typography & Assets Family | Shared stack at `DESIGN.md` **189**. Paths named on that line. Live computed `DESIGN.md` **181**. Declared-only fallback classing **183** (not Family). Family qualifier at **190** covers the display/body unmerge and the fallback prohibition. |
| YAML `tokens.typography.hero / section / card-heading / feature-title / nav / body / caption / button` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 196–203. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.4` dest 5 · `1.0` dest 3 · `1.15` dest 2 · `1.5` dest 2. All eight YAML `use` strings restored verbatim in the Token-set use column (A1, A3). Paths `tokens.typography.hero` … `tokens.typography.button` dest 1 each at 196–203. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (5 steps) / `tokens.shadow` (2 keys) | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing paths: `DESIGN.md` 114 (`tokens.spacing.xs: 4` · `sm: 8` · `md: 16` · `base: 15` · `lg: 24` · `xl: 32` · `section: 48`). Shape paths: `DESIGN.md` 118 (`tokens.rounded.none: 0` · `sm: 4` · `md: 8` · `lg: 16` · `full: 9999`). `tokens.rounded.full: 9999` dest 4 (`DESIGN.md` 118/126/129/555). Visible `9999px` dest 7 (`DESIGN.md` 126/127/129/449/555). Body-named circle `50%` dest 7 is kept beside the source pairing `50% / 9999px` (`DESIGN.md` 127/129/311/449/555). `tokens.spacing.base: 15` dest 2 (`DESIGN.md` 114 twice) is not `tokens.spacing.md: 16`. `tokens.spacing.md: 16` is disambiguated from `tokens.rounded.lg: 16` at 114/129. `tokens.shadow.none` dest 1 at `DESIGN.md` 139. `tokens.shadow.card` dest 4 at `DESIGN.md` 139/554. YAML `0 1px 4px rgba(0,0,0,0.08)` dest 2 (139/554) is not attached to the story-card or event-card `none`. White `56px` header from §9 dest 1 (`DESIGN.md` 444) + provenance 119/147/149 (A3, E2a). |
| YAML `tokens.components` (10 records) | 옮김 → Components & States | `DESIGN.md` 233–418. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×4 (236/261/285/307 = YAML primary + app-open + more + ghost), `input` ×1 (328), `card` ×2 (353/365), `badge` ×2 (377/387), `tab` ×1 (400). All ten YAML `use` strings restored verbatim as `Token-set use:` lines (245/269/291/314/339/359/371/383/394/409), including the source typo `Browse category category with image overlay`. YAML `font` shorthands kept as `token-set font record` lines: `14px / 500` dest 2 · `16px / 400` dest 1 · `14px / 400` dest 1 · `16px / 700` dest 2 (403/407). YAML `border` shorthand `1px solid #f06354` dest 2 (ghost 310 + chip 392). YAML `recipe-card.fg` `#564e4a` is the Homepage Story Card `Text` row at 356 (same key path as Event card Text at 368 — not ink or search). No §4-only component borrowed a `Primitive type` — `not in the token set` dest 0 because every named component has a YAML row. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 35–42. The atmosphere readings (warmth / kitchen-heat / utilitarian / photography-as-hero) carry an adjacent complete qualifier at 11 (B2/B2a). The largest-platform reading in ¶1 is named in the qualifier at 9. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 88–111. Characterizing phrases covered by the adjacent qualifier at 84 (B2/B2a). Canvas / on-primary key-unmerge at 103 has its own adjacent complete qualifier. |
| §3 Typography Rules — family, hierarchy, principles | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 178–185; family 188–190; hierarchy table 196–203; type rules 207–213. The Official-product-use, Official-distributed-asset, Declared-only, License, Outside, Family fallback-plus-display/body-unmerge, Type-roles ratio, and Type-rules readings carry adjacent complete qualifiers at 180, 182, 183, 184, 185, 190, 205, and 213 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 233–418. §4 body values and YAML `use` / font / radius / height forms are both kept. Search placeholder `#89817d` at 332. Ghost height 32px at 313. Tab active `border-bottom #f04646 2px + text #f04646` at 406/408. Homepage Story Card Text `#564e4a` at 356. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 32–34; Tier 1 list 49–50; Tier 2 53–55. `https://icook.tw` is dual as its own URL: `DESIGN.md` 9/237 + `provenance.md` 14/44/49 (E2a). `https://icook.tw/recipes/popular` is dual: `DESIGN.md` 9/21 + `provenance.md` 45/50/69 (E2a). Conflicts unresolved: none — `provenance.md` 38. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 444–449 (full-width header including white `56px` bar, 16px story cards, 2-column browse, 40px tabs, spacing 4/8/15/16/24/32/48, shape 0/4/8/16/9999 / `9999px`) and 451 (photography-first / flat-but-warm / 16px-as-soft-container). 44px search height at 336/461. Circle `50%` dest 7 at 127/129/311/449/555. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 133–139: three-level table, `tokens.shadow.none` / `tokens.shadow.card`. The shadow-free / image-first-consumer-app reading and the YAML-card-shadow unmerge carry an adjacent qualifier at 139 (B2/B2a). Source phrase `consumer app` dest 1 at 139 — kept as the source's own elevation sentence, not as a native-application scope claim. |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 58–64 (seven Do rules), under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 70–76, under the qualifier at 68. The seven prohibitions are the source's own list, including `#000000` as the forbidden body-text color at 75. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 453–467: Mobile `<768px` / Tablet `768–1024px` / Desktop `1024px+` at 457–459, 44px / 32px / 40px targets at 461, collapsing strategy 463–467. The Desktop row keeps the source body's `1024px+` range at 459. The system-level-not-cross-viewport reading carries an adjacent qualifier at 453 (B2/B2a). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The deletion row is `provenance.md` 147; the itemised check is `provenance.md` 149. |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings `DESIGN.md` 484–513; tone table 474–480; forbidden register 515. The qualifier at 472 covers the voice reading and the tone-table contract (B2/B2a, A5). The forbidden-register close at 515 has its own adjacent complete qualifier. Byte-exact / gloss-beside close at 517. Source English glosses (`Begin a beautiful life with iCook`, `Just throw it all in the rice cooker`, `Quick & Simple`) sit beside the Chinese lines and do not replace them. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: 2011, Polydice Inc., Taipei, Taiwan, 愛料理, ài liào lǐ, 300,000+ recipes, 每天都有新食譜, 生活誌, 市集, 愛料理 TV, iGood. Refuse/embrace pairing in the same paragraph, including warm colors / system fonts / flat design, cooking is for everyone, `anyone can post a recipe`, and `curated editorial voice` — the source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 196. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 48–52 under the B2a form at 46: "These five items are a derived editorial implementation inference from the verified surfaces; they are not iCook-authored or a separately published UI specification." iCook has no separately published first-party UI specification in the source, so the toss-form applies (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 157–189 (33 rows). |
| §13 Personas — 4 entries | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or motivation appears in either output (D2, D2a). Disposition at `provenance.md` 143. Audience at `DESIGN.md` 29 carries only the group-level description the source independently records (Taiwanese home cooks, food enthusiasts, health-conscious eaters), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 10 rows | 옮김 → Components & States surface state contract + per-component applicability | Full ten-row body preserved at `DESIGN.md` 428–437 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings `已加入收藏` and `升級 VIP`. The applicability rule is at 227–231, whose qualifier at 231 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary Download CTA, App Open CTA, More-link, Ghost Circle, and Category Navigation Tabs close loading/error/success with a role reason (C2 v10) — they commit no operation in place (`loading \| not-applicable` dest 6 · `loading \| applicable` dest 0 · `success \| applicable` dest 0). Global Search opens error (search-failed row) and closes loading/success because the field does not commit the fetch (`error \| applicable` dest 1 · `error \| not-applicable` dest 5). Seasonal Badge and Category Chip declare `kind: non-interactive` with a reason instead of a map (Core §4.4) — `Kind: non-interactive` dest 2. The two cards get no `kind` and no map because the source supplies no interaction evidence for the containers — `DESIGN.md` 361/373 (C4). Seasonal Badge and Category Chip `Kind: non-interactive` at 379/389. |
| §15 Motion & Easing — durations, easing roles, motion rules, reduced-motion | 옮김 → Foundations motion | `DESIGN.md` 149–151 (100ms / 200ms / 300ms with uses), 157–159 (ease-enter / ease-exit / ease-standard roles and uses, curves omitted), 165–168 (quiet and functional, carousel at `motion-standard / ease-enter`, no bounce or spring, `prefers-reduced-motion: reduce` collapses to instant). |
| §15 Motion & Easing — three curve values | 삭제 → provenance omission ledger | `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are named as omitted at `DESIGN.md` 153 and are not promoted as tokens. Durations stay. Ledger at `provenance.md` 144–146. B3 is held: the promotion condition at `DESIGN.md` 161 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 553. |
| Opening product / atmosphere sentence | 옮김 → Experience Scope | `DESIGN.md` 9–11: Taiwan's largest recipe and cooking community platform; two inspected surfaces; `#ffffff` canvas; reserved coral-red; system CJK stack. |

## Sibling handling (`web/references/icook/.verification.md`)

The sibling exists — confirmed with `find web/references/icook -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 69–88 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: search-submit radius `0px 4px 4px 0px`; placeholder `搜尋食譜名`; H4 `一日三餐，美國雞肉陪伴你`; ghost/more `background-color: rgba(0, 0, 0, 0)`; overlay `rgba(0, 0, 0, 0.32)`; frequency counts; sibling title hyphen form `愛料理 - 300,000 道食譜，每天都有新食譜！`; footer `© 2011-2026 Polydice, Inc.`; extra text-frequency `rgb(143, 139, 137)`; compound lockup `愛料理 iCook`. Recorded at `provenance.md` 92–101.
- Measured `DESIGN.md` 0 for those sibling-only strings: `搜尋食譜名` 0 · `一日三餐，美國雞肉陪伴你` 0 · `0px 4px 4px 0px` 0 · `氣炸鍋` 0 · `com.polydice.icook` 0 · `愛料理 iCook` 0.
- `下載APP` / `在 iCook App 開啟` / `更多` / `升級 VIP` / `最新食譜` / `人氣食譜` / `鑄鐵鍋燜油飯-一鍋到底` / `精彩活動` / `開啟美好生活，愛料理` are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. iCook is Traditional-Chinese-first, so a hand sweep of published labels is mandatory even when the needle set is non-empty.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 25 distinct | 0 | 0 | 愛料理 / 開啟美好生活，愛料理 / 都丟給電鍋 / 300,000 道食譜，每天都有新食譜！ / 每天都有新食譜 / 人氣食譜 / 最新食譜 / 低卡瘦身 / 簡單快速 / 飲料冰品 / 精彩活動 / 當季食材 / 下載APP / 在 iCook App 開啟 / 更多 / 醬料運用 / 排骨湯 / 鑄鐵鍋燜油飯-一鍋到底 / 已加入收藏 / 升級 VIP / 生活誌 / 市集 / 愛料理 TV / iGood / 書面語. |
| `node test-v2/tools/latin-copy-audit.mjs --brand icook` | 1 lost / 21 scanned | 1 | 0 | Lost needle `real cook` is a §13 persona quote, not published product copy (D2a). English glosses that sit beside Chinese lines (`Begin a beautiful life with iCook`, `Just throw it all in the rice cooker`, `Quick & Simple`) are kept as glosses, not replacements (A5). YAML use `Open in iCook App CTA on recipe page` is a token-set use string, not a published CTA. YAML border `1px solid #f06354` dest 2. Source §2 `"secondary."` dest 1. Sibling compound `愛料理 iCook` dest DESIGN 0 / provenance 101. |
| Sibling published strings | 3 distinct sibling-only | 0 | 0 | `搜尋食譜名`, `一日三餐，美國雞肉陪伴你`, and the hyphen title form survive in provenance; none are promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 愛料理, 下載APP, 在 iCook App 開啟, 開啟美好生活，愛料理, 都丟給電鍋, 人氣食譜, 低卡瘦身, 簡單快速, 已加入收藏, 升級 VIP, 2011, Polydice Inc., 生活誌, 市集, 愛料理 TV, iGood, anyone can post a recipe, curated editorial voice.

A5 분모: hand sweep of source published labels 25 extracted / 0 missing; latin-copy-audit 1 lost / 21 scanned (the lost needle is D2a persona, not published copy); gate `copy-loss` compared 21 / candidates 172. A5a was mandatory because `compared` 21 < `candidates` 172.

§13 identifiers were extracted in the hand sweep and dropped under D2a — they are not published copy and are not listed here as names.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand icook --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 21, candidates: 172 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `format: core-v2`, `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 21 < `candidates` 172.

## Deviations recorded

- `DESIGN.md` is 6,474 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: ten declared component records, a ten-row surface state contract, a seven-state applicability matrix on six interactive controls, `tokens.spacing.base: 15` kept off `tokens.spacing.md: 16` and off matching radius keys, `tokens.rounded.full: 9999` kept off the 50% circle, YAML `tokens.shadow.card` kept off the cards' recorded `none`, the full §11 founding-and-ecosystem narrative including its last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- iCook has no separately published first-party UI specification in the source, so every derived-editorial close uses the toss-form `not iCook-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Worker-close measure: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 33 = `not iCook-authored` 33 = `separately published UI specification` 33. Provenance derived ledger is 33 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

DESIGN.md was re-read from line 1 after the body was complete. Every causal, interpretive, or judging sentence — Scope ¶1 surface-assignment, Scope ¶2 atmosphere, Scope ¶3 narrative-not-token, Primary-tasks selection, Audience grouping, Distinctive-traits grouping, Principles, Application rules, Avoid, Semantic characterizations, canvas/on-primary unmerge, Spacing/Shape key unmerge, Elevation shadow-free reading, Motion attribution including curve-omission, each Font evidence class, Family fallback and display/body unmerge, Type-roles ratios, Type rules, Assets favicon/photography, Components how-to-read, State-record attribution, State non-attachment, Layout whitespace, Layout responsive, Content voice, Forbidden register, byte-exact close, Recorded unresolved — has an adjacent complete B2a form (derived editorial implementation inference / not iCook-authored or a separately published UI specification). Hex values, published Chinese strings, YAML `use` strings, and the §11 year / Polydice / Taipei / 愛料理 / 300,000+ / 生活誌 / 市集 / 愛料理 TV / iGood / last-sentence facts are source records and were not qualified as if they were editorial.

## F2 E2 check (mandatory final pass)

Each log row's destination was grepped in the named file before the row was written. Dual destinations (homepage URL, `#f04646`, favicon slug, `愛料理`, token note hex facts, popular URL) name both files. The live `rgb(240,70,70)` form is not a DESIGN destination. Compliance claims used only where the full text is present: B3 five kinds at `DESIGN.md` 161 and 553; B2a complete form 33 = 33; C2 role-closed L/E/S as counted above; D2a omission ledger names field kinds, not identifiers.

## Revision 2026-08-28 (wave33 review)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**. Trigger: semantic review FAIL 1, re-confirmed by the orchestrator. One defect only. Token values, component-table structure, other state rows, B2a complete qualifiers, and the derived-inventory 1:1 were not opened. Source `web/references/icook/DESIGN.md` and sibling `.verification.md` were not modified.

`find` confirmed the six files before any count: dest `DESIGN.md` / `provenance.md` / `migration-log.md` / `audit-log.md`, source `DESIGN.md`, sibling `.verification.md`. Counts below are `grep -oF '<needle>' <file> | wc -l`, file by file. A file that exists and prints no match is 0 for that file.

**1. A1 값 소실 — YAML `tokens.components.recipe-card.fg`.** Source YAML `:53` `recipe-card: { … fg: "#564e4a" … }`. Source §9 `:308` binds the recipe-card title to `20px` / `700` / `#564e4a`. Source §4 `:176–180` listed only Background / Radius / Shadow / Use, so `fg` is a YAML-only field. Portable Homepage Story Card (`:351–360` before this revision) had Background `#f0f0f0` · Radius 16px · Shadow none · Token-set use, and no Text/fg row. The same hex already sat on Search `:331` and Event card `:367`; that is ink / search / event-card, not `tokens.components.recipe-card.fg` (easywallet `lg:16` / spacing 16). Restored `Text: \`#564e4a\`` as its own row on Homepage Story Card at `DESIGN.md` 356, after Background and before Radius, matching Event card's field order. Type-role `20px` / `700` stays on Recipe Card H2; this row restores the YAML key path only.

| needle | orig | sib | dest after | provenance after |
|---|---:|---:|---:|---:|
| `Text: \`#564e4a\`` | 2 | 0 | **3** | 0 |
| `#564e4a` | 21 | 2 | **11** | 7 |
| `Homepage story cards with photo` | 1 | 0 | **1** | 0 |

`Text: \`#564e4a\`` dest **3** = Search 331 + Story Card **356** + Event card 368. Before the restore dest was 2 (Search + Event). orig 2 is source §4 Search `:166` and Event `:184` — YAML `recipe-card` has `fg`, not a `Text:` line. `#564e4a` dest **11** at 11/39/60/75/107/211/331/**356**/368/402/428: the previous 10 plus the new Story Card row. provenance `#564e4a` **7** = 59/61/70 + `:149` ×4 (A3 restatement), not a second body fact. sibling 2 is corroboration of the hex, not a Story Card Text row. This revision section names the defect; those mentions are this file's denominator (E2d).

**줄 포인터.** dest DESIGN.md 555→**556** (+1 Text row after 355). Every `DESIGN.md` pointer ≥356 in this log was re-read against the current file and bumped: identity `#f04646` 405/407/436→406/408/437 · `愛料理` 471/489→472/490 · spacing `9999`/`9999px`/`50%`/`shadow.card`/`56px` 448/553/554/443→449/554/555/444 · components 233–417→233–418, card 364→365, badge 376/386→377/387, tab 397→**400**, Token-set use 358/370/382/393/408→359/371/383/394/409, `16px / 700` 402/406→403/407, chip 391→392 · §4 tab active 405/407→406/408 · §5 443–448/450/460/448/554→444–449/451/461/449/555 · §8 452–466→453–467 · §10 471/473–479/483–512/514/516→472/474–480/484–513/515/517 · §14 427–436→428–437 · C4 cards 360/372→361/373 · chip Kind 378/388→379/389 · B3 restatement 552→553. provenance has no `DESIGN.md:<n>` cites. Historical `audit-log.md` keeps the F3 snapshot (DESIGN 555); it is not rewritten as a current pointer.

B2a `derived editorial implementation inference` dest **33** = provenance inventory **33** data rows. gate `PASS` / `problems []`. `inspectDesignMd` `portable_core: true` / `level: portable-core` / `reasons: []`. SHA-256 `3766527653ba9933d46bb2a289d4c2111833af051254f3f5e563cba634d6dfcf`.

**안 건드린 것.** 토큰 값 · 컴포넌트 표의 다른 행 · state 표 · B2a 완전형 33=원장 33 · 원본 `web/references/icook/**`.
