# Hahow Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Hahow (好學校) is Taiwan's leading EdTech course marketplace. This contract covers the first-party web surfaces the source records as Tier 1 on 2026-06-01: the live marketplace at `https://hahow.in`, and the brand-owned business surface at `https://business.hahow.in`. Color-scale tokens also come from Hahow's open-source design-system theme `hahow-design` at `https://github.com/hahow/hahow-design`, hosted by the brand-owned org `https://github.com/hahow`. Every value below stays attached to the surface that established it. The boundary that stops a hahow.in live measurement from standing in for a business.hahow.in treatment — and that keeps a `hahow-design` token attached to the published theme rather than treated as a live computed sample unless the source also records it on hahow.in — is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

The captured marketplace layer is a soft gray-blue page canvas of `#F5F7F9` with white (`#FFFFFF`) content cards, a vivid brand teal of `#00CCB4` drawn from Hahow's own open-source design tokens, and text in near-black `#262626` set in PingFang TC. Cards and buttons settle on an 8px radius. Warm promotional accents in orange `#FA8C16` and coral `#F65F55` punctuate sales and featured callouts. Reading that canvas as letting bright content breathe, reading the teal as signaling interactivity and energy without shouting, reading the 8px radius as rounding every hard edge into something inviting, and reading the whole as encouraging, personable, and the opposite of corporate sterility — a learning surface that feels like a good teacher: clear, warm, and on your side — is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

Hahow built Taiwan's leading EdTech course marketplace on a simple, human premise: everyone has something to learn and something to teach. As 好學校 — "the good school" — it positions itself as an open, encouraging place where knowledge moves between people rather than being handed down from an institution. The visual identity reinforces that story: a bright, friendly teal pulled from its own open-source design system, a soft and unintimidating canvas, rounded approachable shapes, and native Traditional-Chinese typography. Every choice tells the same narrative — learning here is warm, accessible, and on your side. Those sentences are narrative context; they do not by themselves supply interface tokens. This contract does not treat the hahow.in live inspect as a proxy for an uninspected business.hahow.in treatment. Classing that narrative as not a token source, and that refusal, is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

These three name what the captured marketplace surface is for. That naming is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation. They do not come from the source's persona section.

- Browse courses on the live marketplace at `https://hahow.in`.
- Act on the teal outline primary CTA (`#00CCB4` text, 2px solid `#DCF9F3` border, 42px height).
- Scan metadata, category, and filter tags on the same marketplace.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source's persona section is not carried here and is not re-hosted in the sidecar. Use only what the captured marketplace and the source's published terms establish at a group level: people who learn and people who teach on 好學校. That grouping is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Distinctive traits

The values are recorded; the groupings and the readings inside them are a derived editorial implementation inference from the verified surfaces — they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

- Soft gray-blue canvas (`#F5F7F9`) with white (`#FFFFFF`) cards
- Brand teal Primary 500 (`#00CCB4` / `#00ccb4`) as the interactive signal, from `hahow-design`
- Primary scale steps 100 / 400 / 800 / 900 (`#A3FFE8` / `#23D9BD` / `#006166` / `#00444D`)
- Secondary 500 amber (`#FFB940` / `#ffb940`)
- Promotional orange `#FA8C16` and coral `#F65F55`
- PingFang TC for the visible UI, including the token-set `mono` path
- 8px radius on cards and buttons; 20px on tag chips; token-set `full: 9999`
- Teal outline primary CTA at 42px height with a 2px `#DCF9F3` border
- Near-black body `#262626` and secondary text `rgba(0,0,0,0.65)`

### Principles

These five items are a derived editorial implementation inference from the verified surfaces; they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

1. **Warmth over polish** — feel like a good teacher, not a sales funnel.
2. **Calm canvas, bright signals** — let the soft `#F5F7F9` base carry quiet content while teal and warm accents earn attention.
3. **Rounded and approachable** — consistent 8px / 20px radii keep every surface inviting.
4. **Native and legible** — PingFang TC and near-black `#262626` text serve a Traditional-Chinese audience comfortably.
5. **Restraint with the brand color** — teal `#00CCB4` marks interaction; it is never wallpaper.

### Application rules

The source's four Do rules, kept as brand rules rather than as universal governance. The justifications inside them — why a color belongs to one role, why a radius is reserved — are a derived editorial implementation inference from the verified surfaces; they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

- Anchor interactive emphasis in the brand teal `#00CCB4` from the official tokens.
- Keep body text in near-black `#262626` and secondary text in `rgba(0,0,0,0.65)` for calm, readable hierarchy.
- Use the soft `#F5F7F9` canvas to let white cards and bright accents stand out.
- Reserve warm orange `#FA8C16` and coral `#F65F55` for genuinely promotional moments.

### Avoid

The first four avoidances are the source's own Don't list. The fifth restates this contract's scope boundary as a prohibition. The reasons attached to them — why a color or a radius must stay in one role — are a derived editorial implementation inference from the verified surfaces; they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

- Flood surfaces with teal — it is a signal, not a background.
- Sharpen the corners; the 8px (and 20px chip) radii are core to the friendly feel.
- Introduce a heavy, corporate shadow language that contradicts the flat, warm aesthetic.
- Substitute a non-native typeface for PingFang TC in Traditional-Chinese contexts.
- Present a hahow.in live value as evidence for an uninspected business.hahow.in treatment.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Where a line also characterizes a value — "signature teal", "soft gray-blue", "energetic accent" — that characterization is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

- **Brand / Primary 500** (`#00CCB4` / `tokens.colors.primary` `#00ccb4`): the signature teal; interactive emphasis, brand identity, primary action text. Live hahow.in records the same teal as `#00CCB4`. Token source: `hahow-design` theme Primary 500.
- **Primary 100** (`#A3FFE8` / `tokens.colors.primary-100` `#a3ffe8`): lightest teal tint for subtle fills and soft backgrounds.
- **Primary 400** (`#23D9BD` / `tokens.colors.primary-400` `#23d9bd`): a slightly deeper teal step for hover and gradient transitions.
- **Primary 800** (`#006166` / `tokens.colors.primary-800`): deep teal for high-contrast text or pressed states on teal surfaces.
- **Primary 900** (`#00444D` / `tokens.colors.primary-900` `#00444d`): darkest teal anchor for the scale.
- **Secondary 500** (`#FFB940` / `tokens.colors.secondary` `#ffb940`): warm amber accent; complementary highlight to the teal.
- **Page background** (`#F5F7F9` / `tokens.colors.canvas` `#f5f7f9`): soft gray-blue canvas behind all content. Live hahow.in records the same canvas as `#F5F7F9`.
- **Body text** (`#262626` / `tokens.colors.text`): near-black primary text, with secondary text at `rgba(0,0,0,0.65)`.
- **Promotional orange** (`#FA8C16` / `tokens.colors.promo-orange` `#fa8c16`): energetic accent for sales and featured callouts.
- **Coral** (`#F65F55` / `tokens.colors.coral` `#f65f55`): warm secondary promotional accent.
- **Chip fill** (`rgba(0,0,0,0.04)`): faint neutral wash for tags and metadata chips.
- **Card / card-button fill** (`#FFFFFF` / `#ffffff`): white content cards and the white card-level action.
- **Teal outline border** (`#DCF9F3` / `#dcf9f3`): 2px pale teal ring on the primary CTA. Live inspect on hahow.in; not a `tokens.colors.*` key.
- **Card-button text** (`#595959`): neutral white card-level action foreground.

Primary 400 and Primary 800 are scale steps the source names for hover and pressed treatments. Those steps stay palette roles. They are not promoted onto any component hover or pressed row. Keeping those named hover and pressed steps as palette roles, and not writing them onto any component hover or pressed row, is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Spacing

Token-set steps, unitless: `xs 4 · sm 8 · base 16 · lg 24 · xl 32 · section 48`. Visible-section px strings the source already uses (8px, 16px, 24px) stay; `xs: 4`, `xl: 32`, and `section: 48` stay unitless because the source never suffixes those three steps. Keeping `tokens.spacing.sm: 8` off `tokens.rounded.sm: 8` and `tokens.rounded.md: 8`, and keeping `tokens.spacing.base: 16` off the 16px body and button type size, is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Shape

Token-set steps, unitless: `sm 8 · md 8 · lg 20 · full 9999`.

- Cards and buttons: 8px — `tokens.rounded.sm: 8` and `tokens.rounded.md: 8` are two keys that share the number.
- Tag chips: 20px — `tokens.rounded.lg: 20`.
- Token-set pill step: `full: 9999`. The live tag chip the source records is 20px, not this step.

Calling 8px / 20px inviting, and keeping `full: 9999` off the live 20px chip, is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Elevation

White cards (`#FFFFFF`) lift off the gray-blue `#F5F7F9` canvas through tonal contrast and rounded 8px corners. The teal outline button uses a 2px solid `#DCF9F3` border — a pale teal ring — to define its edge instead of a drop shadow. Exact shadow values were not captured in the blob and are intentionally not specified. Reading that stack as restrained, soft, flat, and friendly — depth implied by layering light surfaces over the muted background — is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Motion

Hahow's motion language was not captured with specific values in the blob, so no durations or easing curves are specified here.

The source's own motion prose — that transitions should read as gentle and encouraging rather than abrupt, soft enough to match the rounded geometry and calm canvas, with the teal token scale (Primary 400 `#23D9BD` into Primary 500 `#00CCB4`) available for smooth interactive color transitions — is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation. The two hex values in that sentence are the already-recorded Primary 400 and Primary 500 steps.

An exact curve or duration may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or the published hahow-design documentation — does not satisfy that condition.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Official product-use | `hahow-design` publishes Primary/Secondary color scales. Classing the published theme as a color-token source rather than a type specimen is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation. |
| Live computed surface-use | The live hahow.in marketplace computes visible text as PingFang TC. Body is 16px / `#262626`. Heading is 24px / 600. Secondary text is `rgba(0,0,0,0.65)`. |
| Official distributed asset | PingFang TC is the observed face. |
| Declared-only | YAML `tokens.typography.family.mono` is `PingFang TC`, the same face as `sans`. |
| License | Treating PingFang TC as an upstream system face used for Traditional-Chinese rendering, not a Hahow-owned brand asset, is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation. |
| Outside these captures | Typography on `https://business.hahow.in` was not computed in the live inspect. |

### Family

- **Current visible UI family:** `PingFang TC`. Token-set paths: `tokens.typography.family.sans` and `tokens.typography.family.mono`, both `PingFang TC`.
- Do not substitute a system face for PingFang TC and present it as the Hahow face in Traditional-Chinese contexts. A fallback member of a stack is never presented as the brand face. That fallback prohibition is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Type roles

| Role | Font | Size | Weight | Line height | Token-set use |
|---|---|---:|---:|---:|---|
| Heading | PingFang TC | 24px | 600 | 1.3 | Section headings |
| Body | PingFang TC | 16px | 400 | 1.5 | Primary body text |
| Chip | PingFang TC | 14px | 400 | 1.4 | Tag / chip labels |

Line heights are unitless ratios in the source token set (`1.3`, `1.5`, `1.4`) and stay ratios here. Token-set paths: `tokens.typography.heading` · `body` · `chip`.

The teal outline button records `16px / 600` on the component, not as a fourth type-role key. Keeping that `16px / 600` on the component record and not opening a fourth type-role key is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Type rules

The source states this hierarchy principle: lean on size and the near-black body color for contrast; reserve teal `#00CCB4` for interactive text rather than headings, keeping reading surfaces calm. Reading the three roles as that principle is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

### Assets

- Logo pointer recorded by the source: `https://www.google.com/s2/favicons?domain=hahow.in&sz=128`. This is a favicon-service URL keyed to the domain, not a Hahow-hosted brand file; the source supplies no first-party logo asset. Classing that entry as a third-party favicon service is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

<!-- design-md:section components-states -->
## 4. Components & States

### Surface state contract

The four rows below are the source's state contract. They describe surface-level treatments, not per-control hover or focus treatments, and they name values this contract carries with their source forms. Reading them as the state contract for the captured marketplace, and keeping the source-noted Primary 400 / 800 steps as palette roles rather than writing them onto component hover or pressed rows, is a derived editorial implementation inference from the verified surfaces; they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

| State | Treatment |
|---|---|
| **Default action** | Teal outline button — transparent background, `#00CCB4` text, 2px solid `#DCF9F3` border, 8px radius, 42px height. |
| **Neutral / card action** | White card button — `#FFFFFF` background, `#595959` text, 8px radius. |
| **Metadata / tag** | Chip — `rgba(0,0,0,0.04)` fill, `rgba(0,0,0,0.65)` text, 20px radius, 33px height, 14px/400. |
| **Promotional emphasis** | Warm orange `#FA8C16` and coral `#F65F55` accents for sales and featured callouts. |

Hover, pressed, focus, and disabled state values were not captured in the blob and are intentionally not specified. The source notes that the teal token scale — Primary 400 `#23D9BD`, Primary 800 `#006166` — provides the natural darker steps for any hover/pressed treatment. Those steps remain palette roles; they are not written onto component hover or pressed rows.

### How applicability is decided here

The source declares each component with a primitive type (`button`, `badge`) and a value set; those types are preserved per component. Applicability below is judged by each control's role, never by whether a visual treatment for that state happens to be recorded: where the source supplies no treatment for an applicable state, the value is omitted and the state stays applicable. `not-applicable` is used only where the control's role makes the state meaningless — a destination CTA that commits no operation in place, or a display element with no action at all — and the reason given is always that semantic one. Where the source supplies no interaction evidence for a container at all, its kind and applicability map are omitted rather than decided.

The role-based decision procedure above, every interactive-kind verdict, every applicability verdict, and the reason given for either is a derived editorial implementation inference from the verified surfaces; they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation. This is not a complete state-coverage claim.

### Teal Outline (primary action)

- Role: primary call-to-action on the live hahow.in surface
- Primitive type: `button` · Kind: interactive
- Background: transparent
- Text: `#00CCB4`
- Border: 2px solid `#DCF9F3`
- Radius: 8px
- Padding: not separately verified
- Height: 42px
- Font: 16px / 600
- Token-set font record: `16px / 600`
- Token-set use: `Teal outline primary CTA, 2px #dcf9f3 border, 42px height`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | not-applicable | A marketplace primary CTA that leads to a public destination has no precondition to gate — the meaning of a disabled state is absent from the role, not merely unrecorded |
| loading | not-applicable | The CTA sends the reader to a course or marketplace destination; it commits no operation in place, so there is no in-progress state on the control itself |
| error | not-applicable | The CTA commits no operation in place, so a failure of that operation cannot be reported on it |
| success | not-applicable | The CTA commits no operation in place, so completion cannot be confirmed on it |

### White Card Button

- Role: neutral card-level action sitting on the gray-blue canvas
- Primitive type: `button` · Kind: interactive
- Background: `#FFFFFF`
- Text: `#595959`
- Border: not separately verified
- Radius: 8px
- Padding: not separately verified
- Height: not separately verified
- Font: not separately verified
- Token-set use: `Neutral white card-level action`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | not-applicable | A card-level action that leads to a public destination has no precondition to gate — the meaning of a disabled state is absent from the role, not merely unrecorded |
| loading | not-applicable | The control sends the reader onward; it commits no operation in place, so there is no in-progress state on the control itself |
| error | not-applicable | The control commits no operation in place, so a failure of that operation cannot be reported on it |
| success | not-applicable | The control commits no operation in place, so completion cannot be confirmed on it |

### Tag Chip

- Role: metadata, category, and filter tags
- Primitive type: `badge`
- Kind: non-interactive — recorded as a badge for metadata, category, and filter tags, not as a control that commits an operation
- Background: `rgba(0,0,0,0.04)`
- Text: `rgba(0,0,0,0.65)`
- Border: none
- Radius: 20px
- Padding: not separately verified
- Height: 33px
- Font: 14px / 400
- Token-set font record: `14px / 400`
- Token-set use: `Metadata, category, filter tags, 33px height`

White content cards appear in elevation and layout as `#FFFFFF` surfaces at 8px radius. The source supplies no interaction evidence for those card containers, so no kind and no applicability map are declared for them.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Content sits on a soft gray-blue `#F5F7F9` canvas, letting white cards and bright accents read as the foreground. Rounded geometry is consistent: 8px on cards and buttons, 20px on tag chips. Teal is used sparingly as an interactive signal, never as a flood, preserving the quiet neutral base. Spacing restated from `tokens.spacing`: `xs 4 · sm 8 · base 16 · lg 24 · xl 32 · section 48`. Shape restated from `tokens.rounded`: `sm 8 · md 8 · lg 20 · full 9999`.

Reading that canvas as letting white cards and bright accents sit as the foreground, reading teal as used sparingly and never as a flood, reading that spacing as generous and calm, and reading the marketplace as browsable rather than dense — a surface that invites scanning across many courses — is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

The live hahow.in marketplace is a course-browsing surface built to scale from desktop grids to mobile stacks. Cards keep their 8px radius and white `#FFFFFF` fill across breakpoints, and tag chips stay fully rounded at 20px so metadata remains legible when reflowed. Touch targets hold the recorded heights — the teal outline button at 42px and tag chips at 33px. Specific breakpoint widths were not captured in the blob and are not specified; the verified pattern is consistent rounding, consistent color roles, and those recorded heights across viewports. Reading those heights as translating naturally to mobile tapping is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The source characterizes Hahow's voice as encouraging and human — the tone of a supportive teacher and a welcoming community of learners (好學校 literally means "good school"). It is warm and approachable rather than corporate or transactional, celebrating curiosity and the act of learning itself. Microcopy should feel like gentle encouragement: inviting people to start, to keep going, to share what they know. Even promotional moments, signaled by the warm orange and coral accents, carry friendliness over hard-sell urgency. That characterization is a derived editorial implementation inference from the verified surfaces; it is not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation. The published name 好學校 and the premise "everyone has something to learn and something to teach" are source-stated strings.

Published strings, kept byte-exact:

- 好學校 — product name; the source's own gloss is "good school" / "the good school"
- everyone has something to learn and something to teach
- hahow-design

Reproduce 好學校 byte-exact rather than translating or re-casing it. An English gloss may sit beside it; it never replaces it.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Recorded unresolved decisions

These are named values the source itself left open, not permissions to invent.

- **Hover, pressed, focus, and disabled treatments.** Hover, pressed, focus, and disabled state values were not captured in the blob and are intentionally not specified. Primary 400 `#23D9BD` and Primary 800 `#006166` stay palette steps.
- **Exact shadow values.** Exact shadow values were not captured in the blob and are intentionally not specified.
- **Specific breakpoint widths.** Specific breakpoint widths were not captured in the blob and are not specified.
- **Teal outline padding; white card-button height, font, and border.** Each is not separately verified.
- **Motion duration, easing, animation name, transition properties, and reduced-motion behavior.** No durations or easing curves are specified here. Promote a motion value for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed.
- **business.hahow.in as a token source.** The URL is a named brand-owned surface. Computed values in this capture attach to hahow.in and to `hahow-design`.
