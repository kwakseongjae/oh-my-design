# hahow migration log

Source: `web/references/hahow/DESIGN.md`
Sibling read (not the migration input): `web/references/hahow/.verification.md`
Destination: `docs/design-md-weight/migrated/hahow/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hahow/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `dd53676743c65733ad6cfa0b8486a82c58eae7ebf095f3156d13dd3f2195f010` (`web/references/hahow/DESIGN.md`). Sibling SHA-256 `95de9ef6d6cc691488becbc332a5036c210df57b0536f54a435e7a8f9bc594de` (`web/references/hahow/.verification.md`). Worker-close portable DESIGN SHA-256 `de5355d819346e29388e1cbefb3f9b2e947445996bd392354644a9a2ab0ca11a`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Hahow Design System` (`DESIGN.md` 1). Identity table `provenance.md` 7–24. `https://hahow.in` is dual: `DESIGN.md` 9/21 + `provenance.md` 13/46/53 (E2a). Frontmatter `primary_color` `#00CCB4` is dual: `DESIGN.md` 11/22/35/52/58/81/120/158/173/191 + `provenance.md` 14/28 (E2a). Token-set `#00ccb4` is dual: `DESIGN.md` 35/81 + `provenance.md` 24/28 (E2a). The favicon slug is dual: `DESIGN.md` 162 + `provenance.md` 15 (E2a). |
| YAML `omd: "0.1"`, `verified`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true`, `ds.name` / `ds.url` / `ds.type: system` / `ds.description` | 분리 → provenance; `ds.url` / `ds.name` 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–24, 34–36, 128. The source footer's **Verified:** 2026-06-01 is at `provenance.md` 38. `hahow-design` and `https://github.com/hahow/hahow-design` are dual: `DESIGN.md` 9 + `provenance.md` 21/22/48/57 (E2a). The string `ds.type: system` has no portable slot: `provenance.md` 123/130 (the identity table at 23 is the split form `ds.type` / `system`, not that string). |
| YAML `tokens.colors` (10 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 81–94. All ten roles kept with names, values, and token-set keys: primary, primary-100, primary-400, primary-800, primary-900, secondary, canvas, text, promo-orange, coral. Lowercase YAML forms sit beside the uppercase prose forms. Chip fill `rgba(0,0,0,0.04)` at 91; secondary text `rgba(0,0,0,0.65)` at 88. Live-only `#DCF9F3` / `#dcf9f3` at 93; card-button `#FFFFFF` / `#ffffff` at 92 and `#595959` at 94. |
| YAML `tokens.typography.family` (`sans: "PingFang TC"`, `mono: "PingFang TC"`) | 옮김 → Typography & Assets Family | Declared-only `family.mono` at `DESIGN.md` 135; Family section at 141. Both paths named. |
| YAML `tokens.typography.heading` / `body` / `chip` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 148–150. Unitless line heights stay ratios and are never converted to px (A1a): `1.3` at 148/152; `1.5` at 149/152; `1.4` at 150/152. All three YAML `use` strings restored verbatim in the table Use column (A1, A3): `Section headings`, `Primary body text`, `Tag / chip labels`. |
| YAML `tokens.spacing` (6 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Unitless steps kept: `DESIGN.md` 100 (`xs 4 · sm 8 · base 16 · lg 24 · xl 32 · section 48`). Shape at 104–108 (`sm 8 · md 8 · lg 20 · full 9999`). The same two scales are restated at Layout `DESIGN.md` 253 (`full 9999` without the colon). `full: 9999` (colon form) stays a step (`DESIGN.md` 40/108/110). `tokens.spacing.sm: 8` is disambiguated from `tokens.rounded.sm: 8` and `tokens.rounded.md: 8` at 100. `tokens.spacing.base: 16` is disambiguated from the 16px type size at 100. Live chip 20px stays `tokens.rounded.lg: 20`; `full: 9999` is not that chip (`DESIGN.md` 108/110). |
| YAML `tokens.components` (3 records) | 옮김 → Components & States | `DESIGN.md` 186–246. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×2 (189/213), `badge` ×1 (236). All three YAML `use` strings restored verbatim as `Token-set use:` lines (198/221/246). YAML `font` shorthands kept as `Token-set font record:` lines (197/245). The lowercase string `token-set font record` is DESIGN 0. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–42. The atmosphere readings (breathe, without shouting, inviting, good teacher) carry an adjacent complete qualifier at 11 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 81–96. Characterizing phrases covered by the adjacent qualifier at 79 (B2/B2a). Primary 400 / 800 stay palette roles and are not written onto hover/pressed rows; that slotting decision now carries its own complete qualifier at 96, and the Surface-state restatement at 178 is named by the expanded qualifier at 169. |
| §3 Typography Rules — family, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 132–137; family 141–142; hierarchy table 148–150. The License-row reading that treats PingFang TC as an upstream face carries an adjacent complete qualifier at 136 (B2/B2a). Keeping `16px / 600` on the component and not as a fourth type-role key carries a complete qualifier at 154. |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 186–246. Teal outline, white card button, and tag chip keep the source's "not separately verified" padding / height / font / border gaps as gaps. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** / **Proof** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 31–38; Tier 1 list 53–55; Tier 2 61–62. `https://business.hahow.in` is dual: `DESIGN.md` 9/137 + `provenance.md` 47/54 (E2a). Conflicts unresolved: none — `provenance.md` 40. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 253–255. Canvas, 8px / 20px geometry, teal-as-signal, spacing and shape restated from token-set paths. The qualifier at 255 now names the foreground / sparingly-never-as-flood readings as well as the generous/browsable reading (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 114. Tonal contrast, 8px corners, 2px `#DCF9F3` ring. Exact shadow values stay unnamed. The flat/friendly reading carries an adjacent qualifier in the same paragraph (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 58–61, under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 67–70, under the qualifier at 65. One extra line at 71 restates this contract's scope boundary as a prohibition; 65 says so. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 257: desktop grids to mobile stacks, 8px / `#FFFFFF` cards, 20px chips, 42px / 33px recorded heights. Specific breakpoint widths stay unnamed. The mobile-tapping reading carries an adjacent qualifier in the same paragraph (B2/B2a). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompt and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 107. |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings `DESIGN.md` 266–268; 好學校 at 9/13/28/262/266/270. The qualifier at 262 covers the voice reading (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: Taiwan's leading EdTech course marketplace, everyone has something to learn and something to teach, 好學校 / "the good school", knowledge moves between people rather than being handed down from an institution, teal from the open-source design system, soft canvas, rounded shapes, native Traditional-Chinese typography. Marked there as narrative context that supplies no interface tokens; the refusal that the hahow.in inspect is not a proxy for an uninspected business.hahow.in treatment carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 131. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 48–52 under the B2a form at 46: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Hahow-authored or taken from a separately published UI specification, including the published hahow-design documentation." Hahow publishes `hahow-design`, so the close names that specification (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 139–164 (26 rows). |
| §13 Personas — 3 entries | 삭제 | Fictional role biographies. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no role label, age, city, or biography appears in either output (D2, D2a). Disposition at `provenance.md` 103. Audience at `DESIGN.md` 28 carries only the group-level description the published 好學校 learn-and-teach premise establishes, under an adjacent qualifier. The three `primary-tasks` at 21–23 come from modules the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 4 rows + uncaptured hover/pressed/focus/disabled | 옮김 → Components & States surface state contract + per-component applicability | Full four-row body preserved at `DESIGN.md` 173–176 (A2; the catalog graph is still 0/440, so nothing is delegated). The uncaptured-state note and the Primary 400 / 800 palette-only reminder are at 178; the expanded qualifier at 169 names that palette-role remainder. The applicability rule is at 182–184, whose qualifier at 184 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 2 interactive buttons × 7 states = 14 rows: 6 `applicable` and 8 `not-applicable`, the latter only with a role reason (C2 v10) — Teal Outline 205–208 and White Card Button 228–231 (destination CTAs; disabled + L/E/S closed together). Tag Chip declares `Kind: non-interactive` with a reason instead of a map — 237 (Core §4.4). The lowercase string `kind: non-interactive` is DESIGN 0. White content-card containers get no `kind` and no map because the source supplies no interaction evidence for them — `DESIGN.md` 248 (C4). |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 118: the source specifies no durations or easing curves, and none are promoted. The gentle-and-encouraging prose and the Primary 400 → 500 color-transition availability stay at 120 under an adjacent complete qualifier. No unsourced curve was present to delete. B3 is held at `DESIGN.md` 122 in full text: five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — the per-component gate, and the partial-confirmation exclusion. `DESIGN.md` 310 names the five kinds and the per-component gate; it does not repeat the partial-confirmation sentence (E2c). |

## Sibling handling (`web/references/hahow/.verification.md`)

The sibling exists — confirmed with `find web/references/hahow -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 70–80 and is **not** promoted into `DESIGN.md` as a new fact.
- Forms it carries that the visible source body does not: raw theme path `https://raw.githubusercontent.com/hahow/hahow-design/master/src/theme.js`; RGB samples `rgb(245,247,249)` and `rgb(0,204,180)`; method string `playwright getComputedStyle`; the TW ≥2 non-Western regional-source requirement sentence. Recorded at `provenance.md` 84–87.
- Measured `DESIGN.md` 0 for those sibling-only strings: `theme.js` 0 · `rgb(` 0 · `playwright getComputedStyle` 0.
- Hex values those RGB samples convert to (`#F5F7F9`, `#00CCB4`) are already in the source body and remain there.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All CJK runs in the source, any length | 1 distinct (好學校) | 0 | 0 | Survives in `DESIGN.md` (also in provenance). |
| All CJK runs in the sibling, any length | 0 | 0 | 0 | Sibling has no CJK. |
| `node test-v2/tools/latin-copy-audit.mjs --brand hahow` | 0 brands with loss | 0 | 0 published-copy losses | Tool reported `scanned: 1`, `clean: 1`, `totalLost: 0`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 好學校, the good school, good school, everyone has something to learn and something to teach, knowledge moves between people, hahow-design, Section headings, Primary body text, Tag / chip labels, Teal outline primary CTA, 2px #dcf9f3 border, 42px height, Neutral white card-level action, Metadata, category, filter tags, 33px height.

A5 분모: latin-copy-audit 0 published-copy missing; CJK hand sweep 1 extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hahow --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 55 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 55.

## Deviations recorded

- `DESIGN.md` is 4,139 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: three declared component records with dual `use`/`font` forms, a four-row surface state contract, a 14-row applicability matrix, unmerged uppercase/lowercase hex pairs, the full §11 learn-and-teach narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Hahow publishes a first-party design-system theme, so every derived-editorial close names that specification (rulebook v12 B2a 전제 주석). Worker-close measure was 24=24. Auditor measure `grep -o 'derived editorial implementation inference' DESIGN.md \| wc -l` = 26 = `not Hahow-authored` 26 = `including the published hahow-design documentation` 26. Provenance derived ledger 26 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

The finished `DESIGN.md` was re-read from line 1. Every causal, interpretive, or judgment sentence — including Scope so/therefore boundaries, Content voice readings, and Docs-citation characterizations — was asked whether it is a Hahow-issued fact or an observation-derived reading. Sibling-only RGB samples were removed from Semantic color so they would not be promoted as portable facts. Worker-close listed twenty-four complete-form qualifications in `provenance.md` 139–162. Auditor added two (Semantic palette-role at 96, Type-roles fourth-key at 154) and expanded two existing closes (Surface state 169, Layout spacing 255). The inventory is now `provenance.md` 139–164 (26 rows). Source-stated narrative (好學校, everyone has something to learn and something to teach, knowledge moves between people) and published copy were left unqualified.

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep` located the value in the named file and section. Dual destinations (homepage, `#00CCB4`, `#00ccb4`, favicon slug, `hahow-design` URL, `https://business.hahow.in`, spacing/shape at Foundations 100/104–108 and Layout 253) name both files. The B3 claim is backed by the full five-kind + gate + partial-confirmation sentence at `DESIGN.md` 122; 310 names the five kinds and the gate only. The B2a 26=26 claim is backed by `grep -o` on the finished body and by the 26-row provenance inventory. The §13 deletion row does not restate role labels, ages, or cities (D2a). The omission ledger does not assert absence of an item in the same sentence that lists it (E2d). Auditor-close portable DESIGN SHA-256 `15fea53a84fa8fa3a3a320ce2a060ed51df64e1082e5516096cc1be65105bd8e`.
