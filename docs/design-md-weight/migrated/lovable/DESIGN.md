# Lovable Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Lovable is a full-stack AI development platform for turning natural-language requests into editable web applications, from early prototypes through deployment. This contract covers three current first-party public marketing/product captures inspected on 2026-07-12: home at `https://lovable.dev/ko`, pricing at `https://lovable.dev/ko/pricing`, and the product-managers page at `https://lovable.dev/ko/product-managers`. Official documentation at `https://docs.lovable.dev/introduction/welcome` and `https://docs.lovable.dev/introduction/getting-started` supplies product context and workflow language; its documentation chrome is a separate source domain and does not populate the tokens below. Treating those three Korean-localized public marketing/product routes as this contract's token surfaces, treating documentation chrome as product context rather than as a token surface, and keeping the source bound that the capture does not authorize claims about the authenticated builder, native clients, or any generated app, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

The source records that current public experience as giving that technical promise a deliberately quiet interface: a `#fafafa` canvas, `#030303` ink, warm hairlines, Camera Plain Variable, and a near-black action rather than a saturated universal brand fill. The contrast is purposeful: a chat-driven way to create software is presented with a compact, legible public shell instead of a visually noisy “AI” motif. Those three routes share a loaded Camera Plain Variable family and the same header action geometry, while the pricing route adds local tabs, comboboxes, menus, and dialogs. Public header menu triggers remain square (0px); full-pill geometry is local to pricing tabs and small actions; pricing menus and dialogs are 12px rounded overlays with a measured layered shadow. The hex values, the `lab()` action pair, Camera Plain Variable, the 0px / 8px / 9999px / 12px geometry split, and the overlay shadow string are recorded. Readings of that captured layer as a deliberately quiet public shell, as a compact legible marketing surface rather than a visually noisy “AI” motif, or as a quiet shell that lets the creation workflow, examples, and templates carry the product story, are a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

Brand narrative recorded by the source, kept separate from the interface evidence above. Lovable frames itself as a full-stack AI development platform rather than a static website generator. Its official documentation says an individual or team can describe an application in natural language and receive editable code spanning frontend, backend, database, authentication, and integrations. The current homepage compresses that lifecycle into a public story: begin with an idea, see a working prototype emerge, then refine and ship it. The public visual system supports that narrative through a quiet shell that lets the creation workflow, examples, and templates carry the product story. The documentation also draws a boundary that matters for this reference: projects live in shared workspaces, can be synced to GitHub, and can be governed and deployed within existing workflows. That product context is not evidence for private workspace controls, so this reference keeps the public marketing/product capture distinct from authenticated product claims. Treating that full-stack platform framing, the natural-language-to-editable-code span across frontend, backend, database, authentication, and integrations, the homepage idea / prototype / refine-and-ship sequence, shared workspaces, GitHub sync, and governance-and-deploy workflow language as official context facts that do not by themselves supply interface tokens, and keeping public marketing/product capture distinct from authenticated product claims, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a captured public control or route, and not taking them from the source's persona section, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

- Open top-level public navigation from the captured header menu trigger on `https://lovable.dev/ko`.
- Use the current header primary action on the three captured public routes.
- Inspect public pricing on `https://lovable.dev/ko/pricing` with the captured pricing-cycle tab, combobox, or information dialog.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source states that official documentation establishes task contexts rather than named personas, and that specific job titles, company sizes, technical fluency, goals, or success metrics should come from a product brief rather than be invented for Lovable. What official documentation independently records at a group level is:

- Individual builders, founders, students, and independent makers starting apps or prototypes.
- Product managers, designers, and marketers creating realistic prototypes, interfaces, and campaign surfaces.
- Developers, engineering teams, agencies, and enterprises extending generated code through existing workflows.

Reading those official-documentation task contexts as this product's audience, and not promoting named personas or inventing job titles, company sizes, technical fluency, goals, or success metrics, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside it, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

- Quiet public canvas: `#fafafa` with `#030303` text and `#eceae4` hairlines
- Camera Plain Variable is visibly loaded across all three captured routes
- Header primary action is `lab(0 0 0 / 0.88)`, 8px-rounded, and compact
- Public header menu triggers remain square; full-pill geometry is local to pricing tabs and small actions
- Pricing menus and dialogs are 12px rounded overlays with a measured layered shadow

### Principles

These 4 items — numbered stems resting on official documentation recorded by the source — are a derived editorial implementation inference from the verified surfaces; they are not Lovable-authored or a separately published UI specification.

1. **Start from an idea.** Natural-language requests, screenshots, and other inputs are presented as valid starting points for a project.
2. **Make iteration visible.** Public and documentation language emphasize reviewing, editing, versioning, and refining instead of treating generation as a final state.
3. **Keep code usable beyond the tool.** Official documentation describes editable code and GitHub synchronization; do not represent this as a claim about every integration or private control.
4. **Separate public evidence domains.** Marketing/product routes, documentation chrome, webfont delivery, and authenticated builder UI answer different questions and must not be merged into a fictional universal system.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Lovable-authored or a separately published UI specification.

- Use `#fafafa`, `#030303`, and `#eceae4` for the verified public neutral structure.
- Use Camera Plain Variable only where the loaded public family is appropriate.
- Keep the current 8px compact action distinct from the 0px header trigger and 9999px pricing tab.
- Treat pricing menu, option, and dialog states as pricing-route evidence with their recorded selectors.

### Avoid

The source states these four as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Lovable-authored or a separately published UI specification.

- Do not use the retired coral token as a current primary color.
- Do not turn the selected pricing-option violet wash into a universal CTA color.
- Do not infer authenticated builder inputs, cards, toasts, errors, or status components from marketing routes.
- Do not render Roboto Mono Variable as a UI family: it was declared but had no visible computed use.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Token-set keys follow the YAML paths. Pairing each hex to the token-set path named beside it, keeping `tokens.colors.canvas` `#fafafa` unmerged from header-action text `lab(98.2716 0 0)` and from dialog background `lab(99.9884 -0.0000298023 0)`, keeping `tokens.colors.ink` `#030303` unmerged from header-action fill `lab(0 0 0 / 0.88)` and from combobox text `lab(0.903296 0 0)`, keeping `tokens.colors.border` `#eceae4` unmerged from `tokens.colors.overlay-border` `#e7e7e6`, keeping the selected-option violet wash as a pricing-option observation rather than a YAML color key, keeping the `lab(0 0 0 / 0.88)` / `lab(98.2716 0 0)` pair as `tokens.components.header-primary-action` fields rather than extra `tokens.colors.*` keys, keeping header-action text `lab(98.2716 0 0)` unmerged from that same lab() token inside `pricing-info-dialog.shadow`, keeping local warm surfaces in pricing as component-local observations rather than a universal primary palette, and keeping retired coral `#ff6f61`, generic focus blue, and a universal cream card surface omitted because this capture does not establish those roles, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

- **Ink** (`#030303`): `tokens.colors.ink`. Repeated public headings, body copy, and navigation labels. Catalog identity `primary_color` is `#030303`.
- **Canvas** (`#fafafa`): `tokens.colors.canvas`. Repeated page background across the captured routes.
- **Hairline** (`#eceae4`): `tokens.colors.border`. Repeated public boundary color.
- **Overlay border** (`#e7e7e6`): `tokens.colors.overlay-border`. Observed on pricing overlays and options.

**Recorded component writings that are not YAML `tokens.colors.*` keys**

- **Primary action** (`lab(0 0 0 / 0.88)`) with **on-action** (`lab(98.2716 0 0)`): current compact header action. These are `tokens.components.header-primary-action` fields, not extra color keys.
- **Selected-option violet wash:** local to the pricing option row. Not a universal primary or CTA color.

The capture also contains local warm surfaces in pricing. They remain component-local observations rather than a universal primary palette.

### Spacing

YAML `tokens.spacing` steps, recorded without a px suffix: `xxs: 4` · `xs: 6` · `sm: 8` · `md: 10` · `lg: 12` · `xl: 16` · `xxl: 24` · `3xl: 32`. Source §5 writes the same compact public control rhythm with a unit: 4, 6, 8, 10, 12, 16, 24, and 32px occur repeatedly in the capture. `tokens.spacing.xxs: 4` is not header-trigger padding `4px 0px 4px 6px`. `tokens.spacing.xs: 6` is not header-action padding `6px 10px`. `tokens.spacing.sm: 8` is not `tokens.rounded.control: 8` and not control radius 8px. `tokens.spacing.md: 10` is not the `10px` in header-action padding. `tokens.spacing.lg: 12` is not `tokens.rounded.overlay: 12`, not overlay radius 12px, and not tab padding `0px 12px`. `tokens.spacing.xl: 16` is not dialog padding `16px` and not body size 16px. `tokens.spacing.xxl: 24` is not navigation line-height 24px. `tokens.spacing.3xl: 32` is not control height 32px. Keeping those YAML steps unitless beside the source's own px list, not treating a spacing step as a type size, radius, padding, or control height, and treating 32px control heights as local captured geometry that is not a spacing-scale step, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

### Shape

YAML `tokens.rounded` (unitless steps, kept on their own path): `tokens.rounded.nav-trigger: 0` · `tokens.rounded.control: 8` · `tokens.rounded.option: 6` · `tokens.rounded.overlay: 12` · `tokens.rounded.full: 9999`.

- 0px: public header menu trigger — YAML `tokens.rounded.nav-trigger`. Component records write `0px`.
- 8px: compact header action and pricing combobox — YAML `tokens.rounded.control`. Component records write `8px`.
- 6px: pricing option — YAML `tokens.rounded.option`. Local to the pricing option row, not a promoted menu-system token.
- 12px: pricing menu/dialog overlay — YAML `tokens.rounded.overlay`. Component records write `12px`.
- 9999 / 9999px: pricing-cycle tab — YAML `tokens.rounded.full` `9999`; component records write `9999px`.

Reading 0 / 8 / 6 / 12 / 9999 as local harvested geometry for those observed controls, keeping 6px option local to the pricing option row rather than as a promoted menu-system token, keeping unitless `9999` beside `9999px`, keeping `tokens.rounded.control: 8` unmerged from `tokens.spacing.sm: 8`, keeping `tokens.rounded.overlay: 12` unmerged from `tokens.spacing.lg: 12`, and not treating that split as a universal radius for every unlisted Lovable surface, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

### Elevation

Public base controls are flat. The measured exception is the pricing menu/dialog overlay: a 12px rounded surface with white and near-white rings, a `rgba(119,119,113,0.16)` inset layer, and progressively larger low-opacity black shadows. YAML records that shadow on `tokens.components.pricing-info-dialog.shadow`: `rgb(255, 255, 255) 0px 0px 0px 1px, lab(98.2716 0 0) 0px 0px 0px 1px, rgba(119, 119, 113, 0.16) 0px 0px 0px 2px, rgba(0, 0, 0, 0.04) 0px 2px 1px 0px, rgba(0, 0, 0, 0.04) 0px 2px 1px -0.5px, rgba(0, 0, 0, 0.04) 0px 4px 3px -1.5px, rgba(0, 0, 0, 0.04) 0px 7px 6px -3px, rgba(0, 0, 0, 0.04) 0px 13px 12px -6px, rgba(0, 0, 0, 0.04) 0px 25px 24px -12px`. That is overlay-local depth, not a default card shadow. YAML has no `tokens.shadow.*` keys. Reading those samples as flat public base controls with overlay-local depth on the pricing dialog rather than as a global shadow scale, and refusing to invent a `tokens.shadow` path, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

### Motion

Current public controls include transition classes, but this packet did not record a reliable duration or easing value. No motion token is promoted. Preserve the observed interaction-state boundaries without fabricating animation behavior. Omitting duration and easing because this packet recorded none, holding the five-kind per-component promotion gate rather than treating transition classes or official documentation of a single curve as sufficient, and holding that official documentation of a single curve or duration is not that gate, are derived editorial implementation inferences from the verified surfaces; they are not Lovable-authored or a separately published UI specification.

Do not promote a motion duration, easing, animation name, transition, or reduced-motion behavior as a new component token until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, treating Camera Plain Variable as the captured UI family because computed visible use and loaded FontFace/source evidence agree, treating Roboto Mono Variable as declared-only with zero visible computed uses, refusing to substitute a system font while calling it Camera Plain Variable, reading first-party WOFF2 delivery as live delivery rather than as redistribution or license proof, and reading FontFaceSet presence as specimen corroboration for Camera Plain Variable rather than as a license, are derived editorial implementation inferences from the verified surfaces; they are not Lovable-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | Lovable’s public pages and documentation position the platform as a natural-language app-building workflow; they do not publish a separate typography system. |
| Live surface-use | Camera Plain Variable is computed on 758 visible captured elements and backed by loaded `document.fonts` entries plus first-party WOFF2 source URLs. |
| Official distributed asset | The first-party WOFF2 files establish live delivery only; redistribution or license terms were not established by this packet. |
| Declared-only | Roboto Mono Variable has six declared first-party source URLs but zero visible computed uses in the capture. |
| Outside this public capture | Authenticated builder, native/desktop, and generated-app typography remain outside the public capture. The source names this evidence class Unresolved. |

### Family

- **Current visible UI family:** `Camera Plain Variable` — YAML `tokens.typography.family.ui`.
- **Loaded source boundary:** first-party WOFF2 `https://lovable.dev/fonts/CameraPlainVariable-c48bd243.woff2`; the family is loadable on the three captured public routes.
- Do not replace unavailable or unobserved brand type with Camera Plain Variable. It is canonical here only because computed visible use and loaded FontFace/source evidence agree.
- Do not render Roboto Mono Variable as a UI family.

Reading computed visible use plus loaded FontFace/source evidence as the reason Camera Plain Variable is canonical on these three public routes, refusing to substitute a system font and call it Camera Plain Variable, and keeping Roboto Mono Variable declared-only rather than as a UI family, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

### Type roles

Line heights stay in the form the source verified them: YAML unitless `1.1` / `1.1` / `1.1` / `1.5` / `1.6` / `1.5` beside §3 `66px` / `52.8px` / `39.6px` / `24px` / `24px` / `21px`. Tracking stays YAML `-1.5` / `-1.2` / `-0.9` beside §3 `-1.5px` / `-1.2px` / `-0.9px`. YAML sizes stay `60` / `48` / `36` / `16` / `15` / `14` beside §3 `60px` / `48px` / `36px` / `16px` / `15px` / `14px`. Keeping those YAML unitless ratios and tracking numbers beside the source's own px writings, keeping YAML `heading` as Feature heading rather than collapsing it into Display or Section, keeping Navigation YAML use `Top-level public navigation trigger` unmerged from the component use `Top-level public navigation menu trigger`, keeping action YAML use `Current compact public action` unmerged from the component use `Current header primary action`, keeping each YAML `use` string beside the hierarchy-table note without replacing either, keeping `tokens.typography.body.size` `16` unmerged from `tokens.spacing.xl: 16`, and keeping `tokens.typography.action.size` `14` as a type size rather than a spacing step, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Token-set use |
|---|---|---|---:|---|---|---|
| Display | Camera Plain Variable | 60 / 60px | 600 | 1.1 / 66px | -1.5 / -1.5px | Current public display heading |
| Section | Camera Plain Variable | 48 / 48px | 600 | 1.1 / 52.8px | -1.2 / -1.2px | Repeated public section heading |
| Feature heading | Camera Plain Variable | 36 / 36px | 600 | 1.1 / 39.6px | -0.9 / -0.9px | Current public feature heading |
| Body | Camera Plain Variable | 16 / 16px | 400 | 1.5 / 24px | normal | Public body and list copy |
| Navigation | Camera Plain Variable | 15 / 15px | 400 | 1.6 / 24px | normal | Top-level public navigation trigger |
| Compact action | Camera Plain Variable | 14 / 14px | 400 | 1.5 / 21px | normal | Current compact public action |

Token-set paths: `tokens.typography.display` · `tokens.typography.section` · `tokens.typography.heading` · `tokens.typography.body` · `tokens.typography.navigation` · `tokens.typography.action`. `tokens.typography.body.size` `16` is not `tokens.spacing.xl: 16`. `tokens.typography.action.size` `14` is not a spacing step. Navigation line-height `1.6` is not converted away; `24px` stays beside it.

### Assets

- Catalog favicon: `https://lovable.dev/favicon-192x192.png`. Frontmatter records `logo.type: favicon`.
- Camera Plain Variable WOFF2: `https://lovable.dev/fonts/CameraPlainVariable-c48bd243.woff2`. Live delivery on the captured public routes; redistribution or license terms were not established by this packet.

Reading the first-party favicon URL as identity metadata for the captured public routes, and reading the WOFF2 URL as live delivery rather than as a redistribution license, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The collector observed public header hover, pressed, and focus states; pricing tab hover, pressed, and selected states; pricing selector pressed, focus, and menu-open states; plus menu-open and dialog-open overlays. Loading, empty, error, success, toast, validation, and authenticated builder states were not established and are omitted. Card systems, app prompt inputs, toasts, status treatments, and generated-app controls are omitted because this packet did not establish matching public variants.

The generic observed-state name Focus on the header menu trigger and the pricing combobox is a captured Focus treatment, not a `focus-visible` treatment. The source records no `focus-visible` capture; no `focus-visible` row carries a treatment. Pressed and selected stay as captured local observations; they are not extra Core §4.4 rows. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover applies wherever a pointer control exists. Loading, error, and success follow the control’s product role, not its primitive kind. Where a state applies by role and no treatment was observed, the state stays applicable and only its visual treatment is omitted. Absence of a capture is not a `not-applicable` reason. Every interactive-kind verdict, every applicability verdict, and the reason given for either — including treating the generic observed-state name Focus as a captured Focus treatment and not a `focus-visible` treatment, keeping pressed and selected as captured local observations rather than extra Core §4.4 rows, keeping each YAML `use` string as a Token-set use row, keeping YAML font / padding / radius / height / states / bg / fg / border / shadow byte forms beside the §4 writings, treating the header menu trigger as a menu trigger, treating the header primary action as a public destination action, treating the pricing-cycle tab as a tab, treating the pricing selector as a combobox trigger, treating the pricing information dialog as an information overlay, and treating Capture-record loading / empty / error / success / toast / validation rows as omitted product-level treatments rather than as per-control computed state tokens — is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification. This is not a complete state-coverage claim.

### Header navigation trigger

- Role: top-level public navigation menu trigger
- Token-set use: Top-level public navigation menu trigger
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled menu trigger
- Background: transparent. YAML bg: `transparent`
- Text: `#030303`. YAML fg: `#030303`
- Radius: 0px. YAML radius: `0px`
- Padding: 4px 0px 4px 6px. YAML padding: `4px 0px 4px 6px`
- Height: 32px. YAML height: `32px`
- Font: Camera Plain Variable 15px / 400 / 24px. YAML font: `15px / 400 / 24px`
- YAML states: hover, pressed, and focus captured on home::[data-omd-capture="1"]
- Hover: `oklab(0.0983389 0.00000446842 0.00000196737 / 0.998508)` text observed
- Pressed: `oklab(0.100978 0.00000458921 0.00000201977 / 0.993438)` text observed
- Focus: `oklab(0.0980805 0.00000445659 0.00000196224 / 0.999007)` text observed
- Use: top-level menu trigger on `home::[data-omd-capture="1"]`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public header menu trigger |
| hover | applicable | Pointer-web menu trigger; captured text treatment recorded above |
| focus-visible | applicable | Interactive control; visual treatment omitted. The captured generic-focus text color is not this row. |
| disabled | applicable | A menu trigger can be unavailable; visual treatment omitted |
| loading | not-applicable | This header menu trigger opens top-level navigation; it does not commit an operation whose pending result this trigger would report. |
| error | not-applicable | Same role reason: opening a menu is not an operation with an error result this trigger would report. |
| success | not-applicable | Same role reason: opening a menu is not an operation with a success result. |

### Header primary action

- Role: current compact header primary action
- Token-set use: Current header primary action
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled filled button
- Background: `lab(0 0 0 / 0.88)`. YAML bg: `lab(0 0 0 / 0.88)`
- Text: `lab(98.2716 0 0)`. YAML fg: `lab(98.2716 0 0)`
- Radius: 8px. YAML radius: `8px`
- Padding: 6px 10px. YAML padding: `6px 10px`
- Height: 32px. YAML height: `32px`
- Font: Camera Plain Variable 14px / 400 / 21px. YAML font: `14px / 400 / 21px`
- YAML states: default captured on all three public routes; no role-specific hover, pressed, or focus variant promoted
- Use: repeated header action on home, pricing, and product-managers; selector `*[data-omd-capture="7"]`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the header primary action on all three public routes |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination action can be unavailable; visual treatment omitted |
| loading | not-applicable | This header primary action presents a public destination; it does not commit an operation whose pending result this button would report. |
| error | not-applicable | Same role reason: choosing that destination is not an operation with an error result this button would report. |
| success | not-applicable | Same role reason: choosing that destination is not an operation with a success result. |

### Pricing-cycle tab

- Role: pricing-cycle tab on the public pricing route
- Token-set use: Pricing-cycle tab on the public pricing route
- Primitive type: `tab` · Kind: interactive
- Anatomy: full-pill tab
- Background: transparent. YAML bg: `transparent`
- Text: `lab(42.0087 -0.102207 0.363302)`. YAML fg: `lab(42.0087 -0.102207 0.363302)`
- Radius: 9999px. YAML radius: `9999px`
- Padding: 0px 12px. YAML padding: `0px 12px`
- Height: 32px. YAML height: `32px`
- Font: Camera Plain Variable 14px / 400 / 21px. YAML font: `14px / 400 / 21px`
- YAML states: hover, pressed, and selected captured on pricing::[data-omd-capture="9"]
- Hover: `oklab(0.499999 -0.000297248 0.000967979)` text observed
- Pressed: `oklab(0.496991 -0.000295021 0.000960718)` text observed
- Use: pricing-cycle tab at `pricing::[data-omd-capture="9"]`; selected state was also captured and remains local to this control

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the unselected pricing-cycle tab |
| hover | applicable | Pointer-web tab; captured text treatment recorded above |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A tab can be unavailable; visual treatment omitted |
| loading | not-applicable | This pricing-cycle tab selects a pricing cycle; it does not commit an operation whose pending result this tab would report. |
| error | not-applicable | Same role reason: switching a pricing cycle is not an operation with an error result this tab would report. |
| success | not-applicable | Same role reason: switching a pricing cycle is not an operation with a success result. |

### Pricing selector

- Role: public pricing combobox trigger
- Token-set use: Public pricing combobox trigger
- Primitive type: `button` · Kind: interactive
- Anatomy: combobox trigger
- Background: `lab(100 0 0 / 0.8)`. YAML bg: `lab(100 0 0 / 0.8)`
- Text: `lab(0.903296 0 0)`. YAML fg: `lab(0.903296 0 0)`
- Border: `lab(0.903296 0 0)`. YAML border: `lab(0.903296 0 0)`
- Radius: 8px. YAML radius: `8px`
- Height: 32px. YAML height: `32px`
- Font: Camera Plain Variable 14px / 400 / 21px. YAML font: `14px / 400 / 21px`
- YAML states: pressed, focus, and menu-open captured on pricing::[data-omd-capture="12"]
- Pressed: `rgba(119, 119, 113, 0.4) 0px 0px 0px 1px inset` observed
- Focus: inset boundary and focus state observed
- Use: public pricing combobox at `pricing::[data-omd-capture="12"]`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public pricing combobox trigger |
| hover | applicable | Pointer-web combobox trigger; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted. The captured generic-focus inset is not this row. |
| disabled | applicable | A combobox trigger can be unavailable; visual treatment omitted |
| loading | not-applicable | This pricing selector opens a menu; it does not commit an operation whose pending result this trigger would report. |
| error | not-applicable | Same role reason: opening a menu is not an operation with an error result this trigger would report. |
| success | not-applicable | Same role reason: opening a menu is not an operation with a success result. |

### Pricing information dialog

- Role: public pricing information dialog
- Token-set use: Public pricing information dialog
- Primitive type: `dialog` · Kind: interactive
- Anatomy: open information overlay
- Background: `lab(99.9884 -0.0000298023 0)`. YAML bg: `lab(99.9884 -0.0000298023 0)`
- Text: `lab(0.903296 0 0)`. YAML fg: `lab(0.903296 0 0)`
- Radius: 12px. YAML radius: `12px`
- Padding: 16px. YAML padding: `16px`
- Font: Camera Plain Variable 16px / 400 / 24px. YAML font: `16px / 400 / 24px`
- Shadow: `rgb(255, 255, 255) 0px 0px 0px 1px, lab(98.2716 0 0) 0px 0px 0px 1px, rgba(119, 119, 113, 0.16) 0px 0px 0px 2px, rgba(0, 0, 0, 0.04) 0px 2px 1px 0px, rgba(0, 0, 0, 0.04) 0px 2px 1px -0.5px, rgba(0, 0, 0, 0.04) 0px 4px 3px -1.5px, rgba(0, 0, 0, 0.04) 0px 7px 6px -3px, rgba(0, 0, 0, 0.04) 0px 13px 12px -6px, rgba(0, 0, 0, 0.04) 0px 25px 24px -12px`
- YAML states: dialog-open captured on pricing::[data-omd-interaction-capture="dialog-3-0"]
- Use: dialog-open capture at `pricing::[data-omd-interaction-capture="dialog-3-0"]`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the open pricing information dialog |
| hover | applicable | Pointer-web overlay; visual treatment omitted |
| focus-visible | applicable | Interactive overlay; visual treatment omitted |
| disabled | applicable | An information dialog can be unavailable; visual treatment omitted |
| loading | not-applicable | This pricing information dialog presents overlay copy; it does not commit an operation whose pending result this dialog would report. |
| error | not-applicable | Same role reason: an information overlay is not an operation with an error result this dialog would report. |
| success | not-applicable | Same role reason: an information overlay is not an operation with a success result. |

The captured menu list, option row, selected option, and dialog are pricing-route overlays.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The packet establishes a compact public control rhythm: 4, 6, 8, 10, 12, 16, 24, and 32px occur repeatedly in the capture. It does not establish a reusable authenticated-app grid, dashboard layout, or breakpoint system. Keep public page composition and pricing overlays separate from any generated product UI. Reading those measurements as a measured local cluster rather than as a complete grid declaration, refusing to average the 0px header trigger, 8px action, and 9999px tab, treating the 32px header trigger, 32px header action, 32px pricing tab, and 32px combobox as desktop-capture measurements rather than cross-viewport specifications, and keeping public page composition separate from generated product UI, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

The 32px header trigger, 32px header action, 32px pricing tab, and 32px combobox are desktop-capture measurements, not cross-viewport specifications.

### Responsive behavior

The supplied collector evidence is desktop `1440×900`. Documentation says the project preview can be switched between web and mobile views, but the packet contains no public responsive measurements or breakpoint values. Those values remain unresolved. Reading `1440×900` as the capture viewport rather than as a cross-viewport specification, and reading the documentation preview switch as product-context language rather than as a public breakpoint table, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Lovable’s public language is direct, constructive, and sequence-oriented. The homepage asks people to “Build something Lovable,” then moves through “Start with an idea,” “Watch it come to life,” and “Refine and ship.” Documentation turns the same cadence into practical instructions: describe what to build, review and iterate, sync code to GitHub, then deploy and operate. Use clear action verbs and name the next outcome; avoid unsupported speed, funding, or technical-performance claims. Characterizing that voice as direct, constructive, and sequence-oriented implementation context rather than as a separately published copy manual, requiring the quoted strings below byte-exact, and treating documentation cadence as first-party workflow language rather than as a token source, is a derived editorial implementation inference from the verified surfaces; it is not Lovable-authored or a separately published UI specification.

Recorded strings, kept byte-exact:

- “Build something Lovable”
- “Start with an idea”
- “Watch it come to life”
- “Refine and ship”

Reproduce those quoted strings byte-exact rather than translating or re-casing them.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover, pressed, and focus visual treatments on the header primary action
- `focus-visible` visual treatment (generic Focus text color and inset boundary are a different evidence class)
- duration and easing values for the recorded transition classes
- loading, empty, error, success, toast, and validation visual treatments
- authenticated builder inputs, cards, toasts, errors, or status components
- generated-app controls
- documentation-chrome tokens
- a reusable authenticated-app grid, dashboard layout, or breakpoint system
- public responsive measurements
- Roboto Mono Variable as a UI family
- font redistribution or license terms
- retired coral `#ff6f61`, generic focus blue, and a universal cream card surface
- promote a motion value for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed
