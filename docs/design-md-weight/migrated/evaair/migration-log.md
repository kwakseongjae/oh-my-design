# evaair migration log

Source: `web/references/evaair/DESIGN.md` (unmodified)
Destination: `docs/design-md-weight/migrated/evaair/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/evaair/provenance.md`
Rulebook version used: **v9** (`docs/design-md-weight/MIGRATION_RULEBOOK.md`)
Date: 2026-08-26

Every row below was grep-checked against the actual output files before it was written (F2), and re-checked against them after the separate-session audit (`audit-log.md`), which corrected seven destination statements (rows for `name`, `primary_color`, `omd`, `verification_v2.surfaces`, `verification_v2.sources`, `tokens.extracted`, `tokens.note`), restated one compliance claim (§10 voice row), changed two portable sentences, and added the derived-range record to provenance §Proof notes. Where a value legitimately lands in two places, both destinations are named.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML `id`, `country`, `category`, `homepage` | 분리 → provenance | provenance §Identity. The portable file carries no frontmatter. |
| YAML `name: 長榮航空` | 분리 → provenance **및** 옮김 → DESIGN.md | provenance H1 heading + §Identity table row (×2 in that file); DESIGN.md H1 `# 長榮航空 (EVA Air) Design System`, Experience §Scope opening, and the Content & Locales closing sentence (×3). The traditional-Chinese bytes are kept as published and the English rendering is set beside them, never in place of them. |
| YAML `primary_color: "#4b7d6b"` | 분리 → provenance **및** 옮김 → DESIGN.md | provenance §Identity + §Proof notes (recorded as catalog identity metadata, matched to the measured green-panel value); DESIGN.md Experience §Scope and §Distinctive traits, the §Principles applied rules, Foundations §Semantic color, and the Booking Widget Tab and Green Information Card values — six occurrences of the hex. §Avoid names the green panel in words and carries no hex, so it is not a destination for this value. |
| YAML `logo` (type `favicon`, Google favicon-service slug) | 분리 → provenance | provenance §Identity only. It is a third-party favicon-service URL rather than an EVA-published asset file, so it is not promoted into the portable Assets subsection; that reasoning is recorded in provenance §Identity. |
| YAML `verified`, `omd`, `verification_v2.schema` / `.checked` | 분리 → provenance | provenance §Freshness for `verified`, `verification_v2.schema` and `.checked`; `omd: "0.1"` sits in the §Identity table as `omd format (source)`, not in §Freshness. |
| YAML `verification_v2.surfaces` (home / about / awards) | 분리 → provenance | provenance §Surfaces (ids, kinds, URLs, inspected dates) **및** §Freshness, which restates the inspected date as one aggregate row. The three-surface **boundary** stays in the portable body (Experience §Scope) because standalone reading needs it; only ids, kinds, URLs and dates moved out. |
| YAML `verification_v2.sources` (3 live + 3 official-doc + brand-asset + license) | 분리 → provenance | provenance §Sources and §Narrative and license context **및** §Freshness, which restates the captured dates as the product-surface and official-doc/brand-asset/license rows. |
| YAML `verification_v2.claims` (all anchors and methods) | 분리 → provenance | provenance §Claim ledger. |
| YAML `verification_v2.conflicts: []` | 분리 → provenance | provenance §Freshness ("Conflicts unresolved: none") and §Proof notes. |
| YAML `tokens.source: live-extract`, `tokens.extracted` | 분리 → provenance | provenance §Identity table for both; `tokens.extracted` also appears as its own row in §Freshness (dual destination inside the ledger). |
| YAML `tokens.note` | 분리 → provenance | provenance §Identity, quoted verbatim. Its substance is restated in the portable body so it stands alone, split across two sections: the three-surfaces-only bound and the corporate-material-is-not-a-license bound in Experience §Scope, and the font-declaration and external-licensing half in §3 Font evidence (the declared-only and system/license rows). |
| YAML `tokens.colors` (canvas, foreground, on-green, green-panel, accent, hairline) | 옮김 → Foundations | DESIGN.md §2 Semantic color: all six roles with values and observed use. |
| YAML `tokens.typography.body` (16 / 300 / **1.5**) + `use` | 옮김 → Typography & Assets | §3 Type roles row `Body | 16px | 300 | 24px (1.5)`. The unitless ratio is kept beside the computed px so the scale semantics survive. The `use` text (Dotum-first stack has no matching loaded FontFace) landed in §3 Font evidence and §3 Family. |
| YAML `tokens.typography.nav-label` (14 / 300 / **1.2**) + `use` | 옮김 → Typography & Assets | §3 Type roles row `Navigation label | 14px | 300 | 16.8px (1.2)`; the `use` clause "no named EVA UI family is claimed" is carried in §3 Family. |
| YAML `tokens.typography.accordion-label` (30 / 500 / `normal`) + `use` | 옮김 → Typography & Assets | §3 Type roles row `Awards accordion label | 30px | 500 | normal`, with the `use` restriction carried in the Captured-context column ("Awards-page accordion trigger"). |
| §3 `Booking tab` role row (16px / 500 / 22.4px) — present only in the legacy §3 table, not in frontmatter | 옮김 → Typography & Assets | §3 Type roles row `Booking tab | 16px | 500 | 22.4px`. Kept because that table is the only place the value exists. |
| YAML `tokens.spacing` (xs 5, sm 10, md 20, lg 30) | 옮김 → Foundations **및** Layout & Platforms | §2 Spacing ("5px, 10px, 20px, and 30px") and §5 Layout, which carries the context restriction from legacy §5. |
| YAML `tokens.rounded` (square 0, media 3, input 3) | 옮김 → Foundations | §2 Shape, with the "geometry is contextual, not a single radius scale" restriction. |
| YAML `tokens.shadow.travel-card` | 옮김 → Foundations | §2 Elevation, value verbatim `rgba(0,0,0,0.16) 0px 3px 6px 0px`. |
| YAML `tokens.components.*` values (bg, fg, border, radius, padding, height, font) ×5 | 옮김 → Components & States | §4, one subsection per component, values unchanged. |
| YAML `tokens.components.*.type` (`input`, `tab`, `button`, `card`, `listItem`) | 옮김 → Components & States | §4, a `Type:` line on each of the five components. The verified primitive type is kept per component rather than collapsed into one interactive label. |
| YAML `tokens.components.*.states` (booking-field, booking-widget-tab, award-accordion) | 옮김 → Components & States | §4, the `Observed:` line on each of those three components, wording preserved. |
| YAML `tokens.components.*.use` selectors (`[data-omd-capture="151"]` / `"145"` / `"144"`, `div.card-info.card-info--green`, `li.footer-listItem`) | 분리 → provenance | provenance §Capture selectors. The role description inside each `use` string stayed in the body as the component's `Role:` line. |
| Class name `card-info--green` | 분리 → provenance **및** 옮김 → Foundations | provenance §Capture selectors (as part of the about-page selector) **and** DESIGN.md §2 Semantic color, where legacy §2 already names it as the observed green-panel context. Both occurrences confirmed by grep. |
| YAML `components_harvested: true` | 분리 → provenance | provenance §Identity table and §Proof notes. |
| §1 Visual Theme & Atmosphere (paras 1–2) | 옮김 → Experience §Scope | Founding, first service, restrained white/black surface, `#4b7d6b` concentration, `#cc4b00` awards label, the three-surface boundary, and the "not a license to expand into cabin / mobile app / booking-flow" limit. The "orderly and operational rather than … saturated marketing fills" reading is the source author's evaluation, so the body attaches a derived-editorial-inference qualification immediately after it. |
| §1 Key characteristics (5 bullets) | 옮김 → Experience §Distinctive traits | All five bullets, values intact. |
| §2 Color Palette & Roles (6 roles) | 옮김 → Foundations §Semantic color | See the `tokens.colors` row. |
| §2 `#ed5500` / `#d8d8d8` raw-evidence sentence | 옮김 → Foundations §Semantic color **및** Governance §Named gaps | Kept in Foundations as a non-canonical-role statement, and named in Named gaps as an unresolved role assignment so neither hex can be promoted. |
| §2 "do not turn the green panel into an inferred success state or the orange accordion label into a universal action color" | 옮김 → Experience §Avoid | Placed with the other prohibitions rather than duplicated in Foundations. |
| §3 Font evidence classes (5 rows) | 옮김 → Typography & Assets **및** 분리 → provenance | §3 Font evidence table carries all five classes, including declared-only `Roboto Mono` and the Roboto Apache-2.0 boundary; provenance §Proof notes and §Narrative and license context repeat the same classification and hold the license URL. `Dotum`, `Roboto Mono` and `Apache-2.0` were each grep-confirmed in both files. |
| §3 "Dotum-first stack … not promoted" + "No fallback typeface may be rendered as though it were an EVA-owned face" | 옮김 → Typography & Assets §Family **및** Experience §Avoid | §3 Family states the non-promotion; §1 Avoid restates the substitution prohibition, matching the legacy §7 Don't line. |
| §3 "No EVA-owned distributed type asset was located" | 옮김 → Typography & Assets §Font evidence + §Assets **및** Governance §Named gaps | The evidence row keeps the classification; §Assets states that this contract carries no first-party EVA image, icon, logo, or type file; Named gaps names the unresolved family and distributed asset. No intellectual-property rule was written into the body, because the legacy body establishes none — the IP URL stays in provenance §Sources. |
| §4 Component Stylings (5 components) | 옮김 → Components & States | §4. Anatomy, values and observed-state notes preserved; selectors split to provenance (row above). |
| §5 Layout Principles | 옮김 → Layout & Platforms **및** Foundations **및** Components & States | §5 carries the three-module separation, the spacing-context restriction, and the form-vs-information-control rule. "Keep flat containers as the default" went to §2 Elevation, where the shadow evidence lives; "the green information card is a contained panel, not evidence that all travel cards receive a green fill" went to §4 Green Information Card. All three locations grep-confirmed. |
| §6 Depth & Elevation | 옮김 → Foundations §Elevation | Shadow value, flat-default observation, and the "no modal/overlay/toast/floating-menu token" boundary. |
| §7 Do (4 items) | 옮김 → Experience §Principles, "Applied rules" list | Kept as brand application rules rather than folded into Governance boilerplate. The section's derived-editorial qualifier is worded to cover the numbered principles **and** this list. |
| §7 Don't (4 items) | 옮김 → Experience §Avoid | All four carried. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms **및** Governance §Named gaps | Desktop `1440x900` only, with no breakpoint / mobile / touch-target / responsive-typography rule established; component heights restated as desktop measurements. The same four unresolved items are named in Named gaps. |
| §9 Agent Prompt Guide — the construction prompt itself | 삭제 | A tool-directed prompt wrapper with no receiving Core slot. Its design constraints (white canvas, black hierarchy, square chrome, 3px form/media edges, narrow `#4b7d6b` scope, the 30px `#cc4b00` awards-only pattern, Dotum left unresolved) are restatements of §1/§2/§3/§7; each was grep-confirmed present in Experience, Foundations or Typography & Assets before this row was written. |
| §9 out-of-scope list (unobserved interaction, responsive, authenticated booking, cabin, native-app) | 옮김 → Experience §Scope **및** Governance §Named gaps | This boundary exists only in §9 and has receiving slots, so it moved rather than being dropped with the prompt. Scope closes with it and Named gaps names "authenticated booking, cabin, and native-app behavior"; both occurrences grep-confirmed. |
| §10 Voice & Tone prose + do/don't table | 옮김 → Content & Locales | §6, the table carried with all three row pairs. The source's own limit ("not a claim about an unmeasured UI copy system") is kept adjacent and extended to name the reading as a derived content-tone interpretation rather than EVA-authored voice guidance; the audit widened that same qualifier to state that the Do / Don't pairs applying the reading are covered by it. |
| §10 observed labels “Plan & Book,” “Manage Your Trip,” “Safety First.” | 옮김 → Content & Locales | §6, byte-for-byte including the source's curly quotation marks, together with the "they do not establish a broader copy taxonomy" limit. |
| §11 Brand Narrative (3 paras) | 옮김 → Experience §Scope | Evergreen heritage, the EVA Values framing, the Taoyuan hub, digital dining / e-library, the company chronicle (digital cargo, network expansion), and the marketing-policy commitment. The source's inference that this "shows the brand is evolving" is carried as a derived editorial inference with the qualification adjacent. Narrative URLs → provenance §Narrative and license context. |
| §12 Principles (4 + UI implications) | 옮김 → Experience §Principles | All four with their UI implications, under an adjacent derived-editorial-implementation-inference qualifier that also states they are not EVA-authored or a separately published UI specification. |
| §13 Personas (4 archetypes) | 옮김 → Experience §Audience | Carried as stakeholder groups with the source's own qualification ("stakeholder archetypes derived from EVA's published public service categories, not synthetic research personas or behavioral claims"), extended to say they are not EVA-authored audience definitions. No biography, no demographic invention, and nothing rehosted in provenance. Not promoted into the `primary-tasks` claim — those three come from the measured surfaces instead. |
| §14 States prose + 11-row evidence table | 옮김 → Components & States §State record | Body preserved: the booking tab's selected state only; empty / loading / error / success / skeleton / disabled / hover / focus / pressed / dialog left without values. All eleven table rows carried. |
| §14 → per-component applicability | 옮김 → Components & States **및** 분리 → provenance | Each declared interactive component (Booking Field, Booking Widget Tab, Awards Accordion) closes the seven canonical states by control role; unobserved treatments stay `applicable` with the value omitted. `not-applicable` appears only for loading/error/success on the mode tab and the disclosure trigger, each with a role reason and never a missing-observation reason. Green Information Card and Footer List Item have no interaction evidence, so no kind and no applicability map is asserted for them. The body states "Absence of an observation is never a `not-applicable` reason" and "This is not a complete state-coverage claim"; provenance §Proof notes carries the equivalent pair in its own wording. |
| §4 award-accordion "no expanded … value was captured" | 옮김 → Components & States **및** Governance §Named gaps | §4 records the expanded/collapsed role state as unresolved beyond the seven canonical states; Named gaps names "the awards accordion's expanded treatment". |
| §15 Motion & Easing | 옮김 → Foundations §Motion **및** Governance §Named gaps | No duration, easing curve, transition, or motion-reduction preference present; the tab selection is a state record, not an animation contract. The source states no promotion condition, so none is asserted here and none is claimed in this log. Named gaps repeats the four unresolved motion fields. |
| Locale split of the evidence (`ko-kr` surfaces vs `en-global` / `en-au` / `en-sg` documents) | 분리 → provenance **및** 옮김 → Content & Locales | provenance §Sources holds the URLs and the note explaining the split; DESIGN.md §6 states the locale boundary without the URLs, so the standalone body cannot be read as a cross-locale copy contract. |
| Footer **Verified**, Tier 1, Tier 2, **Conflicts unresolved** | 분리 → provenance | provenance §Freshness, §Tier 1, §Tier 2 (no usable record). |

## Checks performed

- Token parity by key, not by presence: the legacy file holds 27 distinct hex/px values; all 27 appear in the portable `DESIGN.md` itself, with 0 lost and 0 invented.
- Unitless line-heights `1.5` and `1.2` survive as ratios beside their computed px (`24px (1.5)`, `16.8px (1.2)`).
- Verified primitive types `input` / `tab` / `button` / `card` / `listItem` survive on their own components.
- Published strings kept as bytes: `長榮航空`, “Plan & Book,” “Manage Your Trip,” “Safety First.” The English brand rendering sits beside 長榮航空 and does not replace it.
- No `[FILL IN]` in any output; the legacy source contains none.
- Portable Core: `portable_core: true`, level `portable-core`, 7/7 claims once each, 0 dropped segments (`scripts/design-md-core.cjs` inspectDesignMd).
