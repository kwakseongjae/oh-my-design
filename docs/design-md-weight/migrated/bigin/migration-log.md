# Bigin migration log

Source: `web/references/bigin/DESIGN.md`
Destination: `docs/design-md-weight/migrated/bigin/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/bigin/provenance.md`
Date: 2026-08-24
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Worker Portable Core SHA-256 `5c501a61223069c85a0c40cad21f0c1fd4702c58a5e810d4a4a9a087b2ac3683` and post-F3 SHA-256 `2d8ca6cc44201d2aa79af7ca0a768cf1d28da315e0ba287df15729c6344fa47f` are SUPERSEDED dest 2026-08-24 wave11 sol resubmit. Current SHA-256 `cff360f291eb360e45ef92fcfb8f3054c003f41b2e76688623bf832c92eaf5f7`. `node test-v2/tools/migrate-reference.mjs --brand bigin --gate-only` → PASS, problems []. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/bigin/DESIGN.md --check --require-portable-core --json` → exit 0 / `portable_core: true`. This records those command outputs; it is not a catalog-adoption claim (E2c). New F3 was not re-ran.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, display_name_kr, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage 옮김 → Experience Scope; `primary_color` 옮김 → Scope token-note + Distinctive + Principles item 2 + capture-bound + Foundations Bigin Blue / Semantic unmerged-role + Capture-record Empty + Primary/Lift/Connect fills + Form Input Focus + Header §9-only right-aligned CTA recipe + Inline Link field-note contrast; logo URL 옮김 → Typography & Assets + provenance | Portable file has no frontmatter. Name kept as H1 `Bigin Design System`. Homepage `https://www.bigin.io` is dual Scope `DESIGN.md:9` + provenance identity/surfaces (E2a). `https://www.bigin.io/pricing` and `https://www.bigin.io/company` are dual Scope `DESIGN.md:9,32,33` + provenance surfaces/Tier 1 (E2a). Catalog `primary_color` `#006fff` grep hits: identity + portable Scope token-note `11` + Scope atmosphere `15` + Distinctive limiter `42` / bullet `44` + Principles item 2 `58` + capture-bound `66` + Foundations Bigin Blue `94` / Semantic unmerged-role `92` + Capture-record Empty `235` + Capture-record generic Focus `247` + Primary CTA Background/field-note `257,266` + Lift CTA Background/field-note `283,292` + Connect Chip Background `309` + Form Input named Focus `337,341,352,354` + Header / Navigation band §9-only right-aligned CTA recipe `441` + Inline Link field-note contrast `482` (E2a). Motion CTA glow uses `rgba(0,104,255,0.4)`, not this hex as a motion token. Avoid does not contain the hex. Content Observed `545` restates the live string "상담 신청" without this hex. Connect Chip glow is `rgba(0,111,255,0.4)`, not this hex. Catalog favicon URL `https://www.bigin.io/favicon.ico` is dual: provenance identity `16,24` + portable Typography & Assets identity-boundary `DESIGN.md:220` (E2a). Named gaps has no first-party-mark sentence and does not restate the URL. `display_name_kr` 빅인 is provenance identity; Scope restates Bigin (빅인). |
| YAML `omd`, `verified`, `added`, token claims, `tokens.source` / `extracted` / `note`, `components_harvested` | mixed: `omd` / `verified` / `added` / `extracted` / `components_harvested` / `tokens.source` 분리 → provenance; `tokens.note` 옮김 → Experience Scope + provenance | `tokens.source: live-extract` and `components_harvested: true` are provenance-only (`provenance.md:18,20,30,99,142`) (A1c). Not in portable DESIGN.md. Token note is dual Scope `DESIGN.md:11` + provenance; the register-split assignment has adjacent derived editorial implementation inference / not Bigin-authored or a separately published UI specification limiter in Scope (E2a, B2/B2a). YAML `verified` / `added` / `extracted` 2026-06-26 are provenance freshness. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. YAML unitless `lineHeight` 1.3 / 1.4 / 1.5는 비율로 보존하고 표의 `3.50rem` / `2.50rem` / `2.00rem` / `1.50rem` / `1.25rem` / `1.13rem` / `1.00rem` / `0.88rem`와 합치지 않음 (A1a) (`DESIGN.md:201,207-214`). Body-table `~1.3` / `~1.4` / `~1.5` / `normal`은 YAML 비율과 합치지 않음. YAML typography `use` 여덟 field를 Type roles에 복원 (A1). 검증된 primitive type은 컴포넌트별로 보존: button×3 `255,281,307` + tab `449` + card×4 `359,376,392,407` + badge `422` + listItem `476`. `Kind: interactive`로 뭉개지 않음 (A1b). Form Input / Footer / Header / Navigation band는 YAML type 없음 — Type 발명 없음. `#006fff`와 `#0066cc` / `#0b1335`, 다섯 slate inks, 세 tint, mint pair, `rgba(0,104,255,0.4)`와 `rgba(0,111,255,0.4)`, YAML `button-primary.shadow` `2px 2px 8px`와 `cta-glow-tight` trailing `0px`와 lift `0px 4px 16px -4px`, YAML `feature-card.shadow`와 `card-ambient` trailing `0px`, YAML `rounded.full` 9999/`9999px`는 비합침 (A4). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(home / pricing / company). Product-origin / MarTech-definition `9`, three-URL evidence-domain assignment `9`, token-note register-split `11`, stay-attached / homepage-header-not-inner-page-nav / pricing-table-heading-not-navy-hero-headline `13`, marketing-site character extras `15` (covers `17,19`) 문단 인접에 derived editorial implementation inference / not Bigin-authored or a separately published UI specification 한정 (B2/B2a). Key characteristics는 Distinctive traits `40-51`. Distinctive-trait bullets / unmerged-role 독해는 목록 **앞** 인접 완전 B2a `42` (trailing limiter SUPERSEDED dest 2026-08-24 wave11 sol resubmit). |
| §1 / §4 / footer 공식 URL | 분리 → provenance; 본문 Scope에도 | 서사·freshness 원장. 본문은 토큰 경계와 live 문자열을 유지. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | 15 YAML hex 전부. Unmerged-role readings plus extra list characterizations `92` and role-list restatement `110` have adjacent complete B2a (B2/B2a). Connect Chip glow unmerged from CTA glow `112`. Role rgb tuples are restated on portable Semantic bullets `94-96` (E2a). |
| §3 Typography Rules | 옮김 → Typography & Assets | 증거 등급. SpoqaHanSansNeo display/body. YAML unitless line-height 보존 (A1a). YAML `use` 여덟 field restored. rem companions restored. ratio-versus-size-local `201` / unmerged-ink `203` / type-rule including softening-the-dense-Bold-type `197` / font-use boundary `195`는 각 인접 완전 B2a (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States; Form Input / Footer / Header / Navigation band는 YAML 밖 본문 값 | Primary CTA / Lift CTA / Connect Chip `Type: button` `255,281,307`; Nav Link `Type: tab` `449`; White Feature Card / Tinted Section Container / Blue Accent Block / Mint Integration Card `Type: card` `359,376,392,407` (kind/map 생략, C4); Eyebrow Label `Type: badge` `422` (kind/map 생략, C4); Inline Link `Type: listItem` `476`; Form Input / Footer / Header / Navigation band는 YAML type 없음 — Type 발명 없음. Capture selector 없음 — 발명하지 않음. YAML `cta-glow-tight`는 Elevation `142` (A3). Primary / Lift / Connect loading·error·success omitted (C2) `275,301,326`. Form Input error applicable as form field `350`; loading/success omitted (C2) `352`. Nav Link / Inline Link loading/error/success are destination/selection role-based, not `not captured` (C1/C2) `466-468,490-492`. Named Focus `#006fff`는 additional observed Focus; focus-visible 행에 hex 없음 (B1) `247,337,341,354`. White Feature Card / Tinted / Accent / Mint / Eyebrow / Header / Footer kind/map 생략 (C4). Field notes have adjacent complete B2a unmerged-field readings (`266,292,317,340,368,384,400,415,428,482`); White Feature Card §9-only title/body child tuple `369`; Tinted Section §9-only eyebrow/title child tuple `385`; White Feature Card fade-in-not-tap-target `371`; Header parent-surface `440` / §9-only right-aligned CTA recipe `441`; Nav captured-variant `470`. Source Navigation `Background: #ffffff` is the Header / Navigation band parent `436`; Nav Link child has no Background field `458` (A4). Capture-record graph-not-adopted / philosophy-layer preservation has adjacent complete B2a `229` (B2/B2a). |
| §5 Layout | 옮김 → Layout & Platforms + Foundations spacing/shape | YAML scale + ~8px base + 26px compact + 40px/48px padding + ~1200px column. Spacing ~8px-base / generous-48px / tighter-26px-on-compact-item-cards / typical-SaaS-marketing-rhythm 독해는 그 읽기 앞에 인접 완전 B2a `118`. Shape local-geometry / Small-Medium-Large-Extra-Full role labels / 20px-workhorse 독해는 라벨 목록 앞 인접 완전 B2a `124`. Layout Breathing-room / airy-marketing-surface / Tint-segmentation / Rounded-cadence / typical-SaaS-rhythm / extra recorded-layout names 독해는 Layout 인접 완전 B2a `506` (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Flat / Tint / Hairline / Ambient / Glow YAML shadows + `cta-glow-tight`. Table Use assignments 한정은 표 앞 `134`. YAML shadow writings unmerged `144`. Shadow-philosophy 독해는 Elevation 인접 완전 B2a `146` (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | Capture-bound grouping of §7 Do’s / live inspect / token note including Bold-is-the-brand-voice / single-action-color / softer-read has adjacent complete B2a `63` (B2/B2a). Governance 통제 문구에 넣지 않음. |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. Avoid 머리 인접 완전 B2a `76` (named Don’ts). Last-bullet no-body-copy-in-Bold is adjacent complete B2a `84`, not only the list-head (B2/B2a). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` 기록 표 `521-523`. Hero 56px weight 700 maintained. collapsing-strategy / image-behavior / touch-target purpose reading / recorded span / surface measurements 독해는 Layout 인접 완전 B2a `525` (B2/B2a). 원본에 없는 최소폭 발명 없음. |
| §9 Agent Prompt Guide | mixed: 도구 프롬프트 삭제; §9-only 고유 결합은 receiving component/local recipe로 옮김 | **[SUPERSEDED dest 2026-08-24 wave11 sol resubmit — unique §9-only renderable field 없음]** 도구별 복붙 프롬프트·Example Component Prompts·Iteration Guide 번호 목록 삭제. Color reference의 검증된 hex는 Foundations에 있음. Unique §9-only 결합 (A3/A4, 전역 type token 비승격): White Feature Card title `24px / 700 / #3d4046` + body `16px / 400 / #53585f` as one child tuple `369`; Tinted Section eyebrow `20px / 700 / #7e8696` + section title `40px / 700 / #000000` as one child tuple `385`; white top-nav with blue `#006fff` 20px-radius CTA pill right-aligned on Header / Navigation band `441`. Remaining prompt-only constructions omitted as deleted tool prompts, not as harvested components. |
| §10 Voice & Tone | mixed: 관측 live string 옮김 → Content + provenance; derived 표/독해 옮김 → Content (인접 B2a); 금지는 Content | Observed (2026-06-26): hero / feature / product-update / company strings — Content `540-543` + provenance, including the 2026-06-26 date. Parenthetical citation-character has adjacent complete B2a `538`. Derived editorial voice reading, context table labels, and forbidden register sit under adjacent complete B2a `551` and are not labeled Observed (B2/B2a, E1). Treating §14 empty/loading strings as state-contract not extra voice samples has adjacent complete B2a in Content `547` (E2a). 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance | Opening product-origin / MarTech-definition sentence has adjacent complete B2a in Scope `9` (separate from the later public-history limiter `21`). Applicat 2015 → Biginsight 2018, timeline, investors, acquisitions, awards — homepage/narrative facts restated in portable Scope under public-history B2a `21`. Refusal / embrace / consultative-CTA readings under adjacent complete B2a `23` (A1, B2/B2a). |
| §12 Principles | 옮김 → Experience principles | 다섯 항목 전체 editorial readings. 인접 본문에 derived editorial implementation inference / not Bigin-authored or a separately published UI specification `55`. Capture-bound grouping has adjacent complete B2a `63`. Governance 일반 문구는 그 한정의 대체물이 아님 (B2/B2a, E1). |
| §13 Personas | mixed: 가상 biography 삭제·sidecar 재수록 없음; Audience는 배제 경계만 | 원본이 fictional archetypes라고 명시. Names/ages/cities/occupations/biographies **and the source’s publicly-observable-segment labels**는 portable Audience에도 provenance에도 없음 (D2). Generic deletion disposition only. Audience no-individual-personas-promoted + exclusion + observable-work-follows-three-tasks application have adjacent complete B2a `38` (B2/B2a). Independently verified Primary tasks 3건은 §13이 아니라 live CTA / pricing / company surfaces (`count=3`) `31-33`. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | 본문 보존: Empty (no campaign data) / Empty (no integrations) / Loading (dashboard fetch) / Loading (report compute) / Error (sync/connection failed) / Error (form validation) / Success (consultation submitted) / Skeleton / Disabled (A2) `235-243`. Capture-record graph-not-adopted / philosophy-layer preservation has adjacent complete B2a `229`. Table characterizations (including previous-values-stay-visible) have adjacent complete B2a **immediately before the table** `231` (B2/B2a, E1). Trailing limiter after the table is SUPERSEDED dest 2026-08-24 wave11 sol resubmit. 선언 컴포넌트는 §4.4를 닫되 미관측 시각값은 발명하지 않음. `not captured` / `not named`를 `not-applicable` 사유로 쓰지 않음 (C1). Primary / Lift / Connect loading·error·success omitted (exact mapping unresolved; §14 rows are not those paints) (C2). Form Input loading/success omitted; error applicable as form field. White Feature Card / Tinted / Accent / Mint / Eyebrow / Header / Footer는 kind/map 생략 (C4). Named Focus는 Form Input additional observed; focus-visible 행에 hex 없음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브 값 분리 → provenance omission ledger; 커브 이름 생략 사실은 Named gaps에도 | Duration (`motion-fast` 120ms / `motion-standard` 220ms / `motion-slow` 320ms) `154-156`, easing 이름 (`ease-enter` / `ease-exit` / `ease-standard`) `162-164`, signature CTA glow-lift / feature-card reveal / no bounce `170-172`, `prefers-reduced-motion: reduce` `173` 유지. Exact cubic-bezier `cubic-bezier(0.2, 0.6, 0.25, 1)` / `cubic-bezier(0.4, 0.0, 1, 1)` / `cubic-bezier(0.25, 0.1, 0.25, 1)` 값은 무출처라 portable에서 생략하고 provenance omission ledger `119-121`에 보관 (E2b). 커브가 생략됐다는 사실은 portable Motion 표 + Named gaps `599`에도 있다 (E2a). `ease-exit` 커브는 legacy spec-template 예시와 일치. Duration-table Use `150`, easing-table Arriving / Dismissals / Two-way `158`, signature-motion list (CTA glow lift / feature-card reveal / no-bounce-off-brand / functional-and-quiet / competence-not-playfulness) `166`, reduce-motion instant-collapse / product-remains-fully-functional `173`는 각 인접 완전 B2a (B2/B2a). Remote “signature motions” limiter on the duration table is SUPERSEDED dest 2026-08-24 wave11 sol resubmit. Foundations Motion `175`은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 표 밖 값을 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps `604` says “all five kinds” / “a single named duration is not that gate” without enumerating the five evidence kinds; the B3 full text is Foundations Motion only. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | mixed: freshness 분리 → provenance; live-inspect URL은 portable에도 | Dual (E2a): home/pricing/company URLs는 Scope + provenance. Footer verified 2026-06-26는 provenance freshness only. Conflicts unresolved: none. HTML-comment rgb sample dump is provenance Tier 1 only. Source HTML comment Proof pointer to `.verification.md` Raw samples is provenance Tier 1 (file exists; method + three surfaces recorded). Portable Semantic bullets restate role rgb tuples `94-96` (E2a). |

### F1 / F2 (v7 mandatory final passes)

Worker F1/F2 and the F3 auditor inventory below the original scan are SUPERSEDED dest 2026-08-24 wave11 sol resubmit. Reconstruction-boundary exemption not used. No “no unqualified sentence remains” claim (E2c). Trailing Distinctive/state-table B2a and remote signature-motion B2a are not adjacent-complete. Unique §9-only local tuples exist. `.verification.md` exists. Persona-derived segment labels are not re-listed. New F3 was not re-ran.

### F1 B2a scan (F3 auditor, full DESIGN.md reread)

**[SUPERSEDED dest 2026-08-24 wave11 sol resubmit — trailing Distinctive `51` / trailing state-table `241` / remote signature-motion `150` / Font `181` / favicon `218` / Capture `227,241` / Nav `453` / Layout `489,508` / Content `521,530,534` and F1-complete current claim.]** Historical F3 inventory is not the current dest map. Current F1 inventory is the Revision below.

### F2 E2 grep (F3 auditor)

**[SUPERSEDED dest 2026-08-24 wave11 sol resubmit — `#006fff` Distinctive `42,51` / Empty `231` / Focus `245` / Primary `255,264` / favicon `218` / Type roles `199,205-212` / cubic-bezier `112-114` / B3 `173` / Named gaps `587` / types `253,279,305`/`432`/`357…` / C2 `273,299,324` / Nav L/E/S `449-451` / §14 `231-239` / Content Observed `528`.]** Historical F3 greps are not the current dest map. Current F2 greps are the Revision below.

### Revision 2026-08-24 (F3 audit)

F3 B2a·E2 auditor (fresh session) grepped destinations against the then-current three files. That SHA `2d8ca6cc44201d2aa79af7ca0a768cf1d28da315e0ba287df15729c6344fa47f` and its F1-complete/no-§9-only/no-verification.md/trailing-B2a claims are SUPERSEDED dest 2026-08-24 wave11 sol resubmit. New F3 was not re-ran.

## Revision 2026-08-24 (wave11 sol resubmit)

Worker: grok-4.6 T2-1. Rulebook v7 A1/A1c/A3/A4/B2/B2a/D2/E1/E2/E2c/F1/F2. New F3 not required and was not re-ran (E2c).

Portable DESIGN SHA-256 `cff360f291eb360e45ef92fcfb8f3054c003f41b2e76688623bf832c92eaf5f7`. `--gate-only` PASS, problems []. Core `--check --require-portable-core --json` exit 0 / `portable_core: true`. Not a catalog-adoption claim (E2c).

### F1 B2a inventory AFTER the B2a moves (current grepped dests)

Adjacent complete B2a (`derived editorial implementation inference` / `not Bigin-authored or a separately published UI specification`) on: Scope product-origin / MarTech-definition `9`; Scope three-URL evidence-domain assignment `9`; token-note register-split `11`; Scope evidence-domain stay-attached / homepage-header-not-inner-page-nav / pricing-table-heading-not-navy-hero-headline `13`; Scope atmosphere extras `15` (covers `17,19`); Scope public-history / narrative-not-interface-token `21`; Scope refusal / embrace / consultative-CTA `23`; Primary-tasks application `29`; Audience no-individual-personas-promoted / exclusion / observable-work `38`; Distinctive-trait bullets / unmerged-role readings **immediately before the bullets** `42`; numbered Principles (five items) `55`; capture-bound grouping including Bold-is-the-brand-voice / single-action-color / softer-read `63`; Avoid named Don’ts `76`; Avoid last-bullet no-body-copy-in-Bold `84`; Semantic unmerged-role extras `92`; Semantic role-list restatement `110`; Semantic Connect Chip glow unmerged `112`; Spacing ~8px-base / generous-48px / tighter-26px / typical-SaaS-marketing-rhythm `118`; Shape local-geometry / Small-Medium-Large-Extra-Full / 20px-workhorse `124`; Elevation table Use `134`; Elevation YAML shadows unmerged `144`; Elevation shadow-philosophy extras `146`; Motion duration-table Use `150`; Motion easing-table Arriving / Dismissals / Two-way `158`; Motion signature CTA glow lift / feature-card reveal / no-bounce-off-brand / functional-and-quiet / competence-not-playfulness **immediately before the signature list** `166`; Motion reduce-motion instant-collapse / product-remains-fully-functional `173`; Font evidence-class `183`; Family font-use `195`; Typography type-rule including softening-the-dense-Bold-type `197`; Type roles ratio-versus-size-local `201`; Type roles unmerged-ink `203`; Assets favicon identity-only `220`; Assets imagery-not-invented-decoration `222`; Capture-record graph-not-adopted / philosophy-layer `229`; Capture-record table characterizations including previous-values-stay-visible **immediately before the table** `231`; Capture-record generic-Focus `247`; Primary CTA unmerged-field `266`; Lift CTA unmerged-field `292`; Connect Chip unmerged-field `317`; Form Input unmerged-field `340`; Form Input additional-observed generic-Focus `354`; White Feature Card unmerged-field `368`; White Feature Card §9-only title/body child tuple `369`; White Feature Card fade-in-not-tap-target `371`; Tinted unmerged-field `384`; Tinted §9-only eyebrow/title child tuple `385`; Accent unmerged-field `400`; Mint unmerged-field `415`; Eyebrow unmerged-field `428`; Header parent-surface / header-fill-not-nav-item-background `440`; Header §9-only white-header + right-aligned blue CTA pill `441`; Nav captured-variant / unmerged homepage-inner inks / active-not-focus-visible / item-not-header-fill `470`; Inline Link unmerged-field `482`; Layout Breathing-room extras `506`; Layout collapsing-strategy / image-behavior / touch-target / recorded-span / surface-measurements `525`; Content Observed citation-character `538`; Content empty/loading as state-contract `547`; Content derived voice + table labels + forbidden register `551`. Left without B2a (verified hex/geometry, C2 omission sentences, C4 omit-kind sentences, B3 five-kind gate `175`, Governance boilerplate, Named gaps inventory). This is the wave11 inventory, not a closed-completeness claim (E2c).

### F2 E2 grep (current)

- `#006fff` grep-matched Scope token-note `11` / atmosphere `15`, Distinctive limiter `42` / bullet `44`, Principles item 2 `58`, capture-bound `66`, Foundations Bigin Blue `94` / Semantic unmerged-role `92`, Capture-record Empty `235`, Capture-record generic Focus `247`, Primary CTA Background/field-note `257,266`, Lift CTA Background/field-note `283,292`, Connect Chip Background `309`, Form Input Focus `337,341,352,354`, Header §9-only right-aligned CTA recipe `441`, Inline Link field-note contrast `482` — not Link Blue `#0066cc` and not Hero Navy `#0b1335`. Avoid does not contain `#006fff`. Content Observed `545` does not contain `#006fff`. Connect Chip glow is `rgba(0,111,255,0.4)`, not this hex.
- Favicon URL grep-matched Assets `220` + provenance identity `16,24` only (not Named gaps URL).
- Homepage `https://www.bigin.io` grep-matched Scope `9` + provenance identity. pricing/company URLs grep-matched Scope `9,32,33` + provenance surfaces/Tier 1.
- `tokens.source` / `components_harvested` grep-matched provenance only as YAML keys (`18,20,30,99,142`).
- YAML `use` strings grep-matched Type roles. `1.3` / `1.4` / `1.5` grep-matched Type roles `201,207-214`. `3.50rem` / `0.88rem` grep-matched Type roles.
- `120ms` / `220ms` / `320ms` grep-matched Motion table `154-156`. Exact cubic-bezier values grep-matched provenance omission ledger `119-121`, not portable numeric promotion (portable says “omitted”).
- B3 five-kind gate grep-matched Foundations Motion `175` (`transition properties, animation name, duration, easing, and reduced-motion behavior` + per component + “Official documentation of a single curve or duration is not that gate”). Named gaps `604` does not enumerate those five kinds.
- `9999px` grep-matched Distinctive + Shape. `cta-glow-tight` grep-matched Elevation `142` + Primary field-note `266`.
- `Type: button` ×3 `255,281,307`, `Type: tab` `449`, `Type: card` ×4 `359,376,392,407`, `Type: badge` `422`, `Type: listItem` `476` grep-matched.
- Primary/Lift/Connect grep-matched omission sentences `275,301,326`, not destination `not-applicable` rows except Nav Link `466-468` / Inline Link `490-492` role-based L/E/S.
- Local recipes grep-matched White Feature Card `369`, Tinted Section `385`, Header right-aligned CTA `441`. Header parent Background `#ffffff` grep-matched `436`. Nav Link has no `Background:` field.
- Persona names 김도현 / 이서연 / 박준영 absent from DESIGN.md and provenance. Segment labels Korean e-commerce marketers / growth leads / agency operators absent from DESIGN.md and provenance.
- `omd-apply` / `npx omd` absent from portable body.
- Empty/Loading/Error/Success/Skeleton/Disabled §14 rows grep-matched Capture record `235-243`.
- `.verification.md` exists at `web/references/bigin/.verification.md`; provenance Tier 1 records Proof / method / three surfaces. Absence claim deleted.
- These are command outputs, not a catalog-adoption claim (E2c).

## Revision 2026-08-26 (A5 카피 복원, 규칙집 v9)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v9 — A5(브랜드 발행 문자열 바이트 보존) 신설 + 게이트 `copy-loss` 신설. v9 게이트가 이 이관본을 `MIGRATION_BLOCKED / copy-loss` 로 차단했다: `고객 획득부터 충성고객 확보까지 마케팅 전략은 선택이 아닌 필수입니다`. **오탐 아님 — 실제 A5 손실이었다.**

### 손실의 정체 (legacy grep)

`grep -n '고객 획득' web/references/bigin/DESIGN.md` → 4행. 원본은 히어로 헤드라인을 **두 가지 바이트 형태**로 인용한다:

| legacy | 인용 형태 | 절 |
|---|---|---|
| `72` | `"고객 획득부터 충성고객 확보까지 마케팅 전략은 선택이 아닌 필수입니다."` (단일 공백) | §1 Visual Theme & Atmosphere — 700 weight가 만드는 voice의 예시로 괄호 안 인용 |
| `346` | `"고객 획득부터 충성고객 확보까지 마케팅 전략은 선택이 아닌 필수입니다."` + 영문 병기 `("From customer acquisition to loyalty — marketing strategy is a necessity, not a choice")` (단일 공백) | §10 Voice & Tone — "The hero line … sets the register" |
| `350` | `"고객 획득부터 충성고객 확보까지…"` (말줄임) | §10 tone 표 Hero headlines 행 |
| `357` | `"고객 획득부터 충성고객 확보까지  마케팅 전략은 선택이 아닌 필수입니다."` (**이중 공백**) | §10 verified live copy 목록 |

즉 **본문 카피가 아니라 히어로 헤드라인**이며, 원본 §10이 스스로 `hero headline`으로 명기한 발행 문자열이다.

이관본은 `357`의 이중 공백 형태(→ Content Observed)와 `350`의 말줄임 형태(→ Content tone 표)는 옮겼으나, `72`·`346`이 공유하는 **단일 공백 전문**은 어느 파일에도 없었다. 워커가 §10 문장을 "The hero line is read as setting the register"로 재서술하면서 인용을 통째로 떨어뜨렸고, §1의 괄호 인용도 같이 사라졌다.

### 복원

`옮김 → Content & Locales / Derived editorial voice` — `DESIGN.md:553`. §10 원문 문장 자리에 인용을 **바이트 그대로** 되돌리고 원본이 달아둔 영문 병기도 함께 복원했다 (A5: 영문은 원문 **옆에** 병기, 대체 금지):

> The hero line "고객 획득부터 충성고객 확보까지 마케팅 전략은 선택이 아닌 필수입니다." ("From customer acquisition to loyalty — marketing strategy is a necessity, not a choice") is read as setting the register: …

`… is read as …` 라는 파생 독해 표기는 그대로 두었다 — 인접 B2a 한정(`551`)이 이 문단 전체를 덮고 있고, 이번 복원은 문자열만 되돌린 것이지 권위 등급을 바꾸지 않는다. §1(`legacy:72`)의 인용은 §10과 **동일 바이트**이므로 `553` 한 자리가 두 인용을 모두 보존한다 — Scope(`9-23`)에는 새 인용을 넣지 않았다. 토큰 값·컴포넌트 표·state applicability·섹션 구조는 손대지 않았다.

### 복원하지 않은 것 (사유 명기)

- **§13 페르소나 대사 안의 인용 2종** — `"모든 마케팅 액션, 한 번에"`(`legacy:384`), `"상담 신청"`(`legacy:386`). 삭제 — 가상 페르소나 진술이라 D2로 제거된 문맥이며 승격·재수록 금지. 두 문자열의 **발행 형태 자체**는 페르소나와 무관하게 이미 Content Observed(`541`, `545`)에 살아 있으므로 A5 손실이 아니다.
- **§10 두 번째 인용의 영문 병기** — `("모든 마케팅 액션, 한 번에 집행하세요" — run every marketing action in one place)`(`legacy:346`). 한국어 원문은 Content Observed `541`에 바이트 그대로 있고 게이트 needle도 충족한다. 이번 복원 범위를 차단 문자열로 한정했으므로 영문 주석만 미복원 — 값 손실 없음.

### F2 E2 대조 (실측 grep, 복원 후)

- `grep -n '고객 획득' docs/design-md-weight/migrated/bigin/DESIGN.md` → `540`(Observed, legacy `357` 이중 공백 형태) / `553`(복원, legacy `72`·`346` 단일 공백 형태 + 영문 병기) / `557`(tone 표, legacy `350` 말줄임 형태). 세 형태가 각각 제 원본 바이트를 유지한다.
- `grep -n '고객 획득' docs/design-md-weight/migrated/bigin/provenance.md` → 0행. 이 문자열의 목적지는 portable 본문 단일이며 provenance 이중 목적지 아님 (E2a 해당 없음).
- legacy 문자열과 복원 문자열 `diff` 동일 확인 (영문 병기 포함 전체 인용 단위 비교) → BYTE-IDENTICAL.
- 페르소나 이름 김도현 / 이서연 / 박준영 은 여전히 DESIGN.md·provenance 양쪽에 부재 (D2 유지).

### 게이트

`cd test-v2/tools && node migrate-reference.mjs --brand bigin --gate-only` → **PASS, problems []**. 복원 후 portable DESIGN SHA-256 `06bf082a8f98f2b0a7d72c3279423202963791e04d3eb6e3a3dba4e42068e4a2`. 이 절은 명령 출력 기록이며 카탈로그 채택 주장이 아니다 (E2c). 게이트 오탐 회피용 표기 왜곡 없음 (E3).
