# Bigin Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating that product-origin / MarTech-definition sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. Bigin (빅인) is a Korean MarTech product — a full-funnel CRM marketing-automation platform built by Biginsight (빅인사이트). Catalog homepage identity is `https://www.bigin.io`. Treating the following three URLs as the named evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. This contract covers three brand-owned surfaces from the 2026-06-26 live inspect: the homepage `https://www.bigin.io`, the pricing surface `https://www.bigin.io/pricing` (플랜 안내), and the company surface `https://www.bigin.io/company` (회사 소개).

Source token note: primary = live CTA electric blue (`#006fff`); inline text links sit on a slightly deeper `#0066cc`. Hero band is a deep navy (`#0b1335`) with white type. Feature surfaces ladder through tinted blues (`#f4f9ff` -> `#f1f5fd` -> `#e1f1ff`) plus one mint accent (`#dbf4eb` / `#7edbb9`). Everything is SpoqaHanSansNeo at weight 700. Treating that note as a register split — `#006fff` is catalog `primary_color` and the harvested 상담 신청 CTA fill, `#0066cc` is inline-link ink rather than that CTA fill, `#0b1335` is the navy hero band rather than page canvas `#ffffff`, the tint ladder and mint pair stay unmerged, and the note’s “Everything is … weight 700” wording is not a collapse of body Regular 400 / arrow Medium 500 — is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

The following evidence-domain readings — stay-attached-to-the-establishing-surface, homepage-header-not-inner-page-nav, and pricing-table-heading-not-navy-hero-headline — are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. Color, type, and component values below stay attached to the surface that established them. A homepage header value is not a proxy for every inner-page nav treatment. A pricing-table heading is not a proxy for the navy hero headline.

The next paragraphs’ marketing-site character, confident-SaaS-dashboard-turned-outward, one-blue-action, reserved-almost-exclusively, navy-band-against-white, product-narrative-peaks, airy-white-editorial-flow, more-legible-link-blue, typographic-personality-entirely-700, source-treats-700-as-unusual, warmer-slate, size-not-weight hierarchy, distinguishing-from-fintech-blue-peers, rounded-low-shadow tint-ladder, 20px-workhorse, barely-there-depth, whisper-soft elevation, friendly-modern-data-product-aesthetic, and capable-but-unintimidating readings are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. The source reads the marketing site like a confident SaaS dashboard turned outward: clean white canvas, one electric-blue action color, and a single dramatic dark band where the product narrative peaks. The page opens on a deep navy hero (`#0b1335`) with white display type, then drops into an airy white (`#ffffff`) editorial flow where feature stories sit inside soft blue-tinted cards. The single saturated accent is an electric blue (`#006fff`) that is reserved almost exclusively for the "상담 신청" (request consultation) call-to-action and a handful of connect/convert chips. Inline text links use a slightly deeper, more legible blue (`#0066cc`).

The typographic personality is entirely **Spoqa Han Sans Neo at weight 700**. The source treats this as unusual: headlines, subheads, nav items, and even button labels all run Bold — Bigin builds hierarchy through *size* (56px hero down to 14px nav) rather than weight contrast. Only body copy and the arrow more-links relax to weight 400-500. Heading color shifts by context: pure black (`#000000`) on the brightest sections, a warmer slate (`#3d4046`) for inner-page titles and subheads, with descriptive copy in `#53585f` and eyebrow category labels in muted `#7e8696`.

What the source treats as distinguishing Bigin from its fintech-blue peers is its **rounded, low-shadow, tint-laddered** surface language. The geometry is soft and consistent — a 20px radius is the workhorse (cards, sections, header pills), stretching to 24px on the hero CTA and 32px on the largest accent block, with one 8px exception for the small inline connect button. Depth is barely-there: most cards are flat, separated by a ladder of pale blue surfaces (`#f4f9ff` -> `#f1f5fd` -> `#e1f1ff`) plus a single mint accent block (`#dbf4eb` with a `#7edbb9` stroke), thin hairlines (`#e8eaee`), and faint nav text (`#b2c0cb`). When elevation appears it is whisper-soft (`rgba(0,0,0,0.04)` ambient on white feature cards) or a colored glow under the blue CTA. The source reads the result as a friendly, modern, data-product aesthetic — capable but unintimidating.

Treating the following public-history facts as narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. Bigin is the MarTech product of **Biginsight (주식회사 빅인사이트 / Biginsight Inc.)**, a Seoul-based company that, per its own company page, was founded in **May 2015** as **(주)어플리켓 (Applicat)** and rebranded to **(주)Biginsight** in **October 2018**. The product line evolved from a deep-learning web-analytics tool ("Bigin", 2018-04) into an AI marketing-automation solution (2019-05) and a big-data AI analytics platform (2019-12), then matured through **Bigin 3.0** (2021-05, with a global launch and Southeast-Asia expansion in 2021-12) to the current **Bigin 4.0** (2023-07). Along the way the company raised investment from **Crescendo Equity Partners** (2021-03), signed a **Mixpanel** partnership and MOUs with **Naver Cloud** and **제일기획 (Cheil)**, and acquired **(주)태거스 (Taggers)** and **(주)오피노마케팅 (Opinno Marketing)** in 2022. Recognition includes selection as an **APAC MarTech Startup TOP 10** by *MarTech Outlook*, a Minister of Science and ICT award at the Korea Internet Awards, **Korea AI Startup 100**, and a **TIPS** program selection — all listed on the brand-owned company timeline. The brand positions itself as "데이터로 MarTech 산업을 선도하는 빅인사이트" and frames its product as "The Ultimate Guide to Full-funnel Marketing."

The following refusal, embrace, and consultative-CTA readings are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. What the source treats Bigin as refusing, visible in its design: the heavy, intimidating chrome of legacy enterprise analytics, and the dark-pattern urgency of growth-hack marketing. What it treats the brand as embracing: a clean, rounded, mostly-flat interface; a single trustworthy electric blue; Bold Spoqa Han Sans Neo type that states capabilities plainly; and a consultative CTA ("상담 신청") that invites a conversation rather than forcing a signup.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three captured marketing-surface jobs below as the independently verified user outcomes, and keeping source §13 fictional archetypes out of this list, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

- Request a consultation from the harvested 상담 신청 / 상담 신청하기 CTAs.
- Compare plans on 플랜 안내 (`https://www.bigin.io/pricing`).
- Read company positioning on 회사 소개 (`https://www.bigin.io/company`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes, not individual people. Those names and biographies are omitted here. Treating that no-individual-personas-promoted restriction, restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

### Distinctive traits

The following distinctive-trait bullets, including treating `#006fff` as the single action fill rather than `#0066cc`, treating `#0b1335` as the one navy band rather than page canvas, treating 700 as the persuasive weight rather than a collapse of 400/500, treating 20px / 24px / 32px / 8px / `9999px` as local harvested geometry rather than one interchangeable radius, treating the tint ladder and mint pair as unmerged surfaces, treating `rgba(0,104,255,0.4)` and `rgba(0,111,255,0.4)` as unmerged glows, and treating the slate ladder as unmerged inks, are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

- Catalog `primary_color` / Bigin Blue `#006fff` as the harvested 상담 신청 CTA and convert-chip fill
- Deeper Link Blue `#0066cc` for inline text links and footer links
- One deep-navy hero band `#0b1335` with white type against an otherwise white `#ffffff` page
- Spoqa Han Sans Neo at weight 700 for headlines, nav, and buttons; body 400 and arrow links 500
- Soft 20px radius as the default geometry — 24px hero CTA, 32px largest accent block, 8px micro connect button, YAML `full` 9999 (`9999px`)
- Pale blue tint ladder `#f4f9ff` / `#f1f5fd` / `#e1f1ff` plus mint `#dbf4eb` / `#7edbb9` and hairline `#e8eaee`
- Blue-tinted CTA glow `rgba(0,104,255,0.4)` rather than a neutral grey lift; connect-chip glow `rgba(0,111,255,0.4)` stays unmerged
- Slate text ladder `#000000` → `#3d4046` → `#53585f` → `#7e8696` → faint nav `#b2c0cb`

### Principles

These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

1. **Full-funnel, one place.** Bigin's promise is consolidating acquisition through loyalty into one solution. *UI implication:* group capabilities into clear feature bands; the hero asserts end-to-end coverage, not a single trick.
2. **One action, one blue.** Electric blue (`#006fff`) means "do this." *UI implication:* reserve the saturated blue for the primary CTA and convert chips so the next step is never ambiguous; inline links use the deeper `#0066cc`.
3. **Capable but unintimidating.** A data product should feel approachable. *UI implication:* soft 20px radius, pale blue tints, and whisper-soft shadows instead of heavy enterprise chrome.
4. **Bold states it plainly.** Spoqa Han Sans Neo weight 700 carries the message. *UI implication:* build hierarchy with size, keep type confident and direct.
5. **Consult, don't pressure.** The primary CTA invites a conversation ("상담 신청"). *UI implication:* low-pressure, advisory CTA language; no countdown-timer urgency.

Treating the following as a capture-bound application of source §7 Do’s, live inspect, and the token note — including Bold-is-the-brand-voice, single-action-color, and softer-read — is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

- Use Spoqa Han Sans Neo at weight 700 for headlines, nav, and button labels — Bold is the brand voice.
- Reserve electric blue (`#006fff`) for the primary 상담 신청 CTA and convert chips — keep it the single action color.
- Use deeper `#0066cc` for inline text links and footer links.
- Anchor the page with one deep-navy band (`#0b1335`) and white type, keeping the rest white.
- Separate sections with the pale blue tint ladder (`#f4f9ff` / `#f1f5fd` / `#e1f1ff`) and `#e8eaee` hairlines.
- Keep geometry soft — 20px radius default, 24px on the hero CTA, 32px on the largest accent block.
- Use the blue-tinted glow (`rgba(0,104,255,0.4)`) under the CTA instead of a neutral shadow.
- Step heading color from `#000000` to slate `#3d4046` on inner pages for a softer read.

### Avoid

The following items copy source §7 Don’ts (no-light-weight-hierarchy, no-spread-electric-blue, no-heavy-drop-shadows / tint-and-hairline-separation, no-sharp-square-corners, no-second-saturated-accent, no-neutral-grey-cta-shadow, no-body-copy-in-Bold). They are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

- Build hierarchy with light weights — headings stay Bold (700), size carries the hierarchy.
- Spread electric blue across many elements — it dilutes the single-action signal.
- Use heavy drop shadows for elevation — Bigin separates with tint and hairlines.
- Use sharp/square corners on cards — 20px rounding is the default (8px only for the micro connect chip).
- Introduce a second saturated accent — blue leads, with mint (`#dbf4eb` / `#7edbb9`) as the only secondary tint.
- Use neutral grey shadows under the CTA — the lift is always blue-tinted.
- Set body copy in Bold — only headings/nav/buttons are 700; body relaxes to 400. Treating this last Don’t as no-body-copy-in-Bold is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including extra list characterizations (single-action-color, tuned-for-legibility, product-narrative-hero-band, maximum-contrast, warmer-near-black, lowest-emphasis-homepage-nav, three-rungs-not-one-fill, cooler-blue-grey, headline-new-feature, one-non-blue-accent, mint-border-not-second-mint-fill, yaml-fg-not-extra-general-inks), are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. Catalog `primary_color` `#006fff` is the live CTA electric blue and the harvested 상담 신청 / convert-chip fill; it is not Link Blue `#0066cc` and not Hero Navy `#0b1335`. Link Blue is inline and footer text-link ink, tuned for legibility on white. Hero Navy is the product-narrative hero band carrying white display type, not page canvas `#ffffff`. Ink Black `#000000` is maximum-contrast headings on the brightest white sections; Slate Heading `#3d4046` is inner-page H1s, feature subheads, and active nav; Body Slate `#53585f` is standard body copy; Muted Slate `#7e8696` is eyebrow labels, inactive inner-page nav, and captions; Faint Blue-Grey `#b2c0cb` is lowest-emphasis homepage-header nav text. Those five inks are not merged. Surface Blue `#f4f9ff`, Surface Tint `#f1f5fd`, and Accent Surface `#e1f1ff` are three rungs of the pale-blue ladder, not one fill. Mint `#dbf4eb` is the one non-blue accent surface; Mint Border `#7edbb9` is its green hairline, not a second mint fill. Hairline `#e8eaee` is the thin neutral border on white feature cards and dividers. YAML component fields such as Primary CTA `fg` `#ffffff` stay on those controls. They are not extra general inks.

- **Bigin Blue / Primary** (`#006fff`): catalog `primary_color`. Primary brand color and CTA background. The electric blue on the "상담 신청" pill and convert chips — the system's single "action" color. YAML `primary`. Live inspect rgb(0,111,255).
- **Link Blue** (`#0066cc`): Deeper blue used for inline text links and footer links, tuned for legibility on white. YAML `link`. Live inspect rgb(0,102,204).
- **Hero Navy** (`#0b1335`): Deep navy background of the product-narrative hero band, carrying white display type. YAML `navy`. Live inspect rgb(11,19,53).
- **Ink Black** (`#000000`): Maximum-contrast headings and body on the brightest white sections. YAML `ink`.
- **Slate Heading** (`#3d4046`): Inner-page H1s, feature subheads, and active nav — a warmer near-black. YAML `heading`.
- **Body Slate** (`#53585f`): Standard body copy and feature descriptions. YAML `body`.
- **Muted Slate** (`#7e8696`): Eyebrow category labels, inactive inner-page nav, captions. YAML `muted`.
- **Faint Blue-Grey** (`#b2c0cb`): Lowest-emphasis nav text on the homepage header. YAML `faint`.
- **Pure White / Canvas** (`#ffffff`): Page background, white feature cards, text on blue/navy. YAML `canvas`.
- **Surface Blue** (`#f4f9ff`): The lightest blue tint for full-width section containers. YAML `surface-blue`.
- **Surface Tint** (`#f1f5fd`): A slightly cooler blue-grey block tint for description panels. YAML `surface-tint`.
- **Accent Surface** (`#e1f1ff`): A stronger blue accent block (32px radius) for the headline "new" feature. YAML `accent-surface`.
- **Mint** (`#dbf4eb`): The one non-blue accent surface, used for an integration callout block. YAML `mint`.
- **Mint Border** (`#7edbb9`): The green hairline stroke that outlines mint-themed integration cards. YAML `mint-border`.
- **Hairline** (`#e8eaee`): Thin neutral border on white feature cards and dividers. YAML `hairline`.

Role-list characterizations in the bullets (single-action-color, tuned-for-legibility, warmer-near-black, headline-new-feature, one-non-blue-accent) are the same derived editorial implementation inference as the unmerged-role paragraph above; they are not Bigin-authored or a separately published UI specification.

Treating YAML component fields such as Connect Chip `shadow` `rgba(0,111,255,0.4)` as staying on that control, not merged into the CTA glow `rgba(0,104,255,0.4)`, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

### Spacing

YAML scale: xs 4, sm 8, base 16, card 26, lg 40, section 48. Scale named in the body: 4px, 8px, 16px, 26px, 40px, 48px.

The following ~8px-base / generous-48px / tighter-26px-on-compact-item-cards / typical-SaaS-marketing-rhythm readings are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. Body layout states a ~8px base unit. Feature cards pad to 40px, tinted section containers to 48px, and a tighter 26px on compact item cards.

### Shape

YAML `rounded`: sm 8, md 20, lg 24, xl 32, full 9999 (`9999px`).

The following local-geometry reading, including the Small / Medium / Large / Extra / Full role labels and calling 20px the workhorse, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. 8px / 20px / 24px / 32px / `9999px` remain local harvested geometry, not a universal radius for every unlisted control.

- Small (8px): inline connect button, form fields
- Medium (20px): cards, section containers, header pills — the workhorse
- Large (24px): hero / pricing lift CTA
- Extra (32px): the largest blue accent feature block
- Full (`9999px`): pill/round decorative elements

### Elevation

The following table Use assignments (most-surfaces-tinted-blocks; card/section-separation-without-elevation; white-feature-card-outlines; elevated-white-feature-cards; blue-primary-CTA-colored-lift) are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow. YAML `shadow.none`: `none` | Most surfaces, tinted section blocks |
| Tint (Level 1) | Pale blue background shift (`#f4f9ff` / `#f1f5fd` / `#e1f1ff`) | Card/section separation without elevation |
| Hairline (Level 2) | `1px solid #e8eaee` border | White feature card outlines, dividers |
| Ambient (Level 3) | `rgba(0,0,0,0.04) 0px 5px 51px`. YAML `shadow.card-ambient`: `rgba(0,0,0,0.04) 0px 5px 51px 0px` | Elevated white feature cards |
| Glow (CTA) | YAML `shadow.cta-glow`: `rgba(0,104,255,0.4) 0px 4px 16px -4px`. YAML `shadow.cta-glow-tight`: `rgba(0,104,255,0.4) 2px 2px 8px 0px` | Blue primary CTA colored lift |

YAML `button-primary.shadow` is `rgba(0,104,255,0.4) 2px 2px 8px` (no trailing `0px`). YAML `button-cta-lift.shadow` is the `cta-glow` string. YAML `button-connect.shadow` is `rgba(0,111,255,0.4) 0px 4px 16px -4px`. YAML `feature-card.shadow` is `rgba(0,0,0,0.04) 0px 5px 51px` (no trailing `0px`). Treating those writings as kept and not collapsed into one shadow is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

The following shadow-philosophy readings (near-flat; tint-and-hairline-not-heavy-drop; whisper-soft-ambient; color-not-neutral-grey-does-the-lifting; lifted-and-clickable; staying-on-brand; mint-border-rather-than-shadow) are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. Bigin is a near-flat system. Most cards rely on the pale-blue tint ladder and thin `#e8eaee` hairlines for separation; real elevation appears only as a whisper-soft ambient shadow (`rgba(0,0,0,0.04)`) on white feature cards. The one expressive shadow is the **blue-tinted CTA glow** (`rgba(0,104,255,0.4)`), which makes the electric-blue "상담 신청" pill feel lifted and clickable while staying on-brand — color, not neutral grey, does the lifting. The mint integration card adds depth through its `#7edbb9` border rather than a shadow.

### Motion

Source-stated duration roles. Treating the duration table, including the table Use assignments (hover-button-press-focus; card/section-reveal-sheet-dropdown; page-level-and-hero-reveal) as source-stated rather than computed CSS, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 220ms | Card/section reveal, sheet, dropdown |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Source-stated easing names (uncomputed cubic-bezier omitted). Characterizations such as “Arriving — cards, sheets, dropdowns”, “Dismissals”, and “Two-way transitions” in this table are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, sheets, dropdowns |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following signature-motion readings — CTA glow lift, feature-card reveal, no bounce or spring as off-brand for a B2B data product, and the "functional and quiet" / "competence, not playfulness" characterizations — are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

Signature motions (source-stated):

1. **CTA glow lift.** The primary CTA responds to hover with a subtle lift of its blue glow (`rgba(0,104,255,0.4)`).
2. **Feature-card reveal.** Feature cards fade in from below at `motion-standard / ease-enter` as the user scrolls the narrative.
3. **No bounce or spring.** The source treats bounce/spring as off-brand for a B2B data product.
4. **Reduce motion.** The following reduced-motion application readings, including instant-collapse and product-remains-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including the one-font / mostly-one-weight assignment, are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Entire captured marketing interface uses `SpoqaHanSansNeo` (Spoqa Han Sans Neo). Hero H1 on the navy band is 56px / 700 / white. Bold (700) carries every heading, nav item, and button label; Regular (400) and Medium (500) carry body copy and arrow links. |
| Declared-only | YAML `family.display` and `family.body` are both `SpoqaHanSansNeo`. |

### Family

- **Display (YAML `family.display`):** `SpoqaHanSansNeo`
- **Body (YAML `family.body`):** `SpoqaHanSansNeo`

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. Do not present a system fallback as Spoqa Han Sans Neo. Do not replace Spoqa Han Sans Neo on these captured surfaces with another family.

The following type-rule readings (one-font-mostly-one-weight; Bold-everywhere-it-persuades; hangul-first-16px; slate-not-pure-black-for-inner-pages; softening-the-dense-Bold-type) are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. Spoqa Han Sans Neo Bold (700) carries the brand voice from 56px hero to 14px nav. Hierarchy comes from size, not from a light/heavy split. Headings, nav, and button labels are all 700; only body copy and arrow links step down to 400-500. Body sits at a comfortable 16px, generous for hangul legibility in a marketing context. Titles on the pricing and blog surfaces use `#3d4046` rather than `#000000`, softening the dense Bold type.

### Type roles

Verified YAML line-height values are the unitless ratios `1.3`, `1.4`, and `1.5`. The following ratio-versus-size-local reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Body-table figures such as `3.50rem` are size-local observations written beside the px size, not replacements for the ratios. YAML `plan-title`, `eyebrow`, `nav`, and `link-arrow` have no `lineHeight` field; the body table writes `normal` for those rows. Both writings are kept; a ratio is not invented for them. Body-table `~1.3` / `~1.4` / `~1.5` are approximate companions to the YAML ratios; they are not collapsed into one writing.

The following unmerged-ink reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. Display Hero paints white on `#0b1335`. Subhead paints slate `#3d4046`. Eyebrow paints muted `#7e8696`. Arrow Link paints `#0066cc`. Inner-page titles use `#3d4046` rather than `#000000`. Those assignments stay unmerged.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Use (YAML) |
|---|---|---:|---:|---|---|---|
| Display Hero (YAML `display-hero`) | SpoqaHanSansNeo | 56px (3.50rem) | 700 | 1.3 | ~1.3 | Hero headline on navy band, Spoqa Han Sans Neo Bold |
| Section / Feature (YAML `section`) | SpoqaHanSansNeo | 40px (2.50rem) | 700 | 1.4 | ~1.4 | Section / feature titles |
| Pricing Title (YAML `plan-title`) | SpoqaHanSansNeo | 32px (2.00rem) | 700 | (YAML field absent) | normal | Pricing comparison heading |
| Subhead (YAML `subhead`) | SpoqaHanSansNeo | 24px (1.50rem) | 700 | 1.5 | ~1.5 | Feature subheads in slate #3d4046 |
| Eyebrow Label (YAML `eyebrow`) | SpoqaHanSansNeo | 20px (1.25rem) | 700 | (YAML field absent) | normal | Section eyebrow category label in muted #7e8696 |
| Arrow Link (YAML `link-arrow`) | SpoqaHanSansNeo | 18px (1.13rem) | 500 | (YAML field absent) | normal | Arrow more-links in #0066cc |
| Body (YAML `body`) | SpoqaHanSansNeo | 16px (1.00rem) | 400 | 1.3 | ~1.3 | Body copy and inline links |
| Nav / Button (YAML `nav`) | SpoqaHanSansNeo | 14px (0.88rem) | 700 | (YAML field absent) | normal | Nav links and pill button labels |

Pricing Title body also names the comparison heading "플랜 별 기능 비교". Arrow Link body also names "자세히 보기 →".

### Assets

Treating catalog logo metadata as the first-party favicon `https://www.bigin.io/favicon.ico`, and not promoting it as a harvested mark file beyond that identity, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

Treating product screenshots inside 20px-radius cards as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated. Source §10–§15 are labelled a philosophy layer in the source HTML comment; the empty/loading/error/success rows below are that source-stated contract, not a computed interaction harvest from the three marketing URLs.

Characterizations in the following table such as “No clutter”, “flat pulse consistent with the near-shadowless system”, “previous values stay visible”, “States what to do next, not just that something failed”, “describes what's valid, not just 필수”, “Brief inline confirmation in a calm tone”, “No celebratory emoji”, and “the blue CTA fades rather than turning grey to preserve brand read” are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no campaign data)** | White canvas. Single slate (`#3d4046`) line explaining no campaigns yet, with one blue (`#006fff`) CTA to create or connect. No clutter. |
| **Empty (no integrations)** | Muted slate (`#7e8696`) line prompting a data connection, with a mint integration card (`#dbf4eb` / `#7edbb9`) offering connect chips. |
| **Loading (dashboard fetch)** | Skeleton blocks at final card dimensions, 20px radius, on a `#f4f9ff` tinted surface — flat pulse consistent with the near-shadowless system. |
| **Loading (report compute)** | Inline progress; previous values stay visible. |
| **Error (sync/connection failed)** | Inline message in slate with a plain-language explanation and a retry. States what to do next, not just that something failed. |
| **Error (form validation)** | Field-level message below the input; describes what's valid, not just "필수". |
| **Success (consultation submitted)** | Brief inline confirmation in a calm tone; the team will follow up via sales@bigin.io. No celebratory emoji. |
| **Skeleton** | `#f4f9ff` blocks at final dimensions, 20px radius, flat pulse. |
| **Disabled** | Faint blue-grey (`#b2c0cb`) text on reduced-opacity surface; the blue CTA fades rather than turning grey to preserve brand read. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records a generic Input Focus (`#006fff` accent). Treating that appearance as generic Focus, not `focus-visible` treatment evidence, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. The `focus-visible` row does not carry a colour.

White Feature Card, Tinted Section Container, Blue Accent Block, and Mint Integration Card have YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Eyebrow Label has YAML `type: badge` and no interactive-kind confirmation, so kind and a map are omitted. Form Input, Footer, and Header / Navigation band have no YAML `type`; none is invented.

### Primary CTA (상담 신청)

- Role: header consultation-request CTA — the system's single primary action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#006fff`
- Text: `#ffffff`
- Radius: 20px
- Padding: 10px 15px
- Height: 40px
- Font: YAML `14px / 700`; body 14px Spoqa Han Sans Neo weight 700
- Shadow: YAML `rgba(0,104,255,0.4) 2px 2px 8px`
- Use: YAML `Header 상담 신청 CTA, electric-blue pill`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#006fff` is this control’s fill and catalog `primary_color`. It is not Link Blue `#0066cc`. YAML shadow `2px 2px 8px` is not `cta-glow-tight`’s trailing `0px` and not the lift CTA’s `0px 4px 16px -4px`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header 상담 신청 CTA |
| hover | applicable | Pointer-web button; visual treatment omitted. Source §15 names a subtle lift of the blue glow; that row is not copied here as a computed paint |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says the blue CTA fades rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the header consultation-request CTA; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 Success (consultation submitted) row.

### Lift CTA (상담 신청하기)

- Role: hero and pricing closing CTA with a stronger colored glow
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#006fff`
- Text: `#ffffff`
- Radius: 24px
- Padding: 0px 16px
- Height: 58px
- Font: YAML `18px / 700`; body 18px Spoqa Han Sans Neo weight 700
- Shadow: YAML `rgba(0,104,255,0.4) 0px 4px 16px -4px` (`shadow.cta-glow`)
- Use: YAML `Hero / pricing 상담 신청하기 lift CTA`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. 24px radius / 58px height / 18px label are this lift variant. They are not the header pill’s 20px / 40px / 14px. `#006fff` is this control’s fill, not a second primary.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the hero / pricing lift CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A closing CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the hero / pricing 상담 신청하기 lift CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Connect Chip (연동)

- Role: small inline connect button inside integration cards — the one square-ish blue control
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#006fff`
- Text: `#ffffff`
- Radius: 8px
- Height: 36px
- Shadow: YAML `rgba(0,111,255,0.4) 0px 4px 16px -4px`
- Use: YAML `Inline 연동 connect chip, the one square-ish blue button`
- Observed: default only
- YAML `tokens.components.button-connect` records no padding and no font; none is invented.
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. 8px radius / 36px height are this micro chip. They are not the header pill’s 20px / 40px. `rgba(0,111,255,0.4)` is this control’s glow. It is not `rgba(0,104,255,0.4)`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the inline 연동 chip |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A connect chip can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as an inline connect chip inside integration cards; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 Empty (no integrations) row.

### Form Input

- Role: consultation / contact form fields (sales@bigin.io intake)
- Kind: interactive
- Anatomy: value field
- Background: `#ffffff`
- Border: 1px solid `#e8eaee`
- Radius: 8px
- Text: 16px Spoqa Han Sans Neo, `#3d4046`
- Focus (named): `#006fff` accent
- Use: Consultation / contact form fields (sales@bigin.io intake)
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.
- Field note: The following unmerged-field reading, including treating the named Focus as generic Focus rather than `focus-visible` treatment, and treating this field’s value color and 8px radius as unmerged from Body Slate and the 20px card default, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.
- Named Focus `#006fff` is generic Focus, not a `focus-visible` treatment.
- `#3d4046` is this field’s value color, not Body Slate `#53585f`. Radius 8px is this field’s geometry, not the 20px card default.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named form field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted. Source §14 names a field-level message below the input; that row is not copied here as a computed paint |

Loading and success applicability are omitted. Source records default geometry plus named Focus `#006fff`. Exact loading/success mapping for this field is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: Focus `#006fff` accent. Treating that appearance as a captured generic Focus, not an observed `focus-visible` treatment, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

### White Feature Card

- Role: elevated white feature card
- Type: card
- Anatomy: container
- Background: `#ffffff`
- Border: 1px solid `#e8eaee`
- Radius: 20px
- Padding: 40px
- Shadow: body `rgba(0,0,0,0.04) 0px 5px 51px`; YAML `shadow.card-ambient` adds a trailing `0px`
- Use: YAML `Elevated white feature card`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#ffffff` / `1px solid #e8eaee` / 20px / 40px are this card’s fields. They are not Tinted Section Container `#f4f9ff` / `rgba(0,0,0,0.05)` / 48px.
- Local recipe (source §9-only): title `24px / 700 / #3d4046` with body `16px / 400 / #53585f` as one child tuple on this card. Treating that child tuple as attached to this card, not as a global Subhead/Body type-role pair, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted. Treating the source §15 fade-in-from-below as an entrance motion, not a tap-target confirmation, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

### Tinted Section Container

- Role: full-width light-blue section grouping campaign/feature content
- Type: card
- Anatomy: container
- Background: `#f4f9ff`
- Border: 1px solid `rgba(0,0,0,0.05)`
- Radius: 20px
- Padding: 48px
- Use: YAML `Light-blue section container`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#f4f9ff` is this container’s fill. It is not `#f1f5fd` and not `#e1f1ff`. `rgba(0,0,0,0.05)` is this container’s border, not `#e8eaee`.
- Local recipe (source §9-only): eyebrow `20px / 700 / #7e8696` with section title `40px / 700 / #000000` as one child tuple on this section. Treating that child tuple as attached to this container, not as a global Eyebrow/Section type-role pair, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Blue Accent Block

- Role: headline accent feature block carrying the "new" tag
- Type: card
- Anatomy: container
- Background: `#e1f1ff`
- Radius: 32px
- Padding: 28px 57px 48px 77px
- Use: YAML `Blue accent feature block carrying the new tag`
- Observed: default only
- YAML records no border on this control; none is invented.
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#e1f1ff` / 32px / `28px 57px 48px 77px` are this block’s fields. They are not Surface Blue `#f4f9ff` and not the 20px card default.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Mint Integration Card

- Role: integration callout card — the one non-blue accent surface
- Type: card
- Anatomy: container
- Background: `#dbf4eb`
- Border: 1px solid `#7edbb9`
- Radius: 20px
- Use: YAML `Mint integration card / callout`
- Observed: default only
- YAML records no padding on this control; none is invented.
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#dbf4eb` is this card’s fill. `#7edbb9` is this card’s border. They are not merged, and they are not a blue tint.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Eyebrow Label

- Role: section category eyebrow above feature titles
- Type: badge
- Anatomy: label
- Text: `#7e8696`
- Font: YAML `20px / 700`; body 20px Spoqa Han Sans Neo weight 700
- Use: YAML `Section eyebrow category label (캠페인, 자동화)`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#7e8696` is this label’s foreground. It is not Body Slate `#53585f`. The 20px / 700 pair is this eyebrow’s field-role binding, not a substitute for Nav / Button 14px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Header / Navigation band

- Role: header/navigation band (parent surface)
- Anatomy: surface
- Background: `#ffffff`
- Use: White header behind top nav items
- Observed: default only
- YAML `tokens.components` does not record a header parent; `nav-link` is the child. Type is not invented.
- Field note: The following parent-surface reading, including header-fill-not-nav-item-background, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#ffffff` is this band’s fill and Canvas. YAML `nav-link` records fg/font/active/use only; this header fill is the parent surface, not a Nav Link item Background.
- Local recipe (source §9-only): white header with 14px Spoqa weight 700 links in `#b2c0cb`, slate `#3d4046` on active, and a blue `#006fff` 20px-radius CTA pill right-aligned. Treating that right-aligned parent/child relation as attached to this header, not as a Nav Link Background and not as a global layout token, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Nav Link

- Role: top horizontal nav item
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: YAML `fg` `#b2c0cb`; body also names `#7e8696` on inner pages
- Active: YAML `text #3d4046`; body slate `#3d4046`
- Font: YAML `14px / 700`; body 14px Spoqa Han Sans Neo weight 700
- Height: ~40px nav items (body; YAML has no height field)
- Use: YAML `Top nav item; muted #7e8696 on inner pages, slate on active`
- Observed: default; named active appearance
- Nav labels named in the source: "블로그", "플랜 안내", "회사 소개", "로그인/회원가입".
- This item has no Background field; the header band fill `#ffffff` stays on the parent surface.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top nav item |
| hover | applicable | Pointer-web nav control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`#3d4046`). Treating that active appearance as a captured variant, not an observed click transition, treating homepage `#b2c0cb` and inner-page `#7e8696` as unmerged source-stated inks rather than one nav color, and treating named active slate as not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. Treating this item as having no Background field so the header fill stays on the parent surface is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

### Inline Link

- Role: inline body / footer text link
- Kind: interactive
- Type: listItem
- Anatomy: label
- Text: `#0066cc`
- Font: YAML `16px / 400`; body 16px Spoqa Han Sans Neo weight 400
- Use: YAML `Inline body / footer text link`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification. `#0066cc` is this link’s foreground. It is not CTA fill `#006fff`. Arrow more-links at 18px / 500 are the Arrow Link type role, not this 16px / 400 field.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the inline body / footer text link |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A text link can be unavailable; visual treatment omitted |
| loading | not-applicable | An inline text link selects a destination; the link itself does not enter a loading state |
| error | not-applicable | Link meaning is destination, not a request or validation failure on the link |
| success | not-applicable | Link meaning is navigation, not action-outcome confirmation |

### Footer

- Role: footer navigation and contact
- Links: `#0066cc` inline blue / muted slate
- Use: Footer navigation and contact (sales@bigin.io, 02-558-8867)
- YAML `tokens.components` does not record this control. Kind and a state-applicability map are omitted. Type is not invented.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, base 16, card 26, lg 40, section 48. Body layout repeats 4px, 8px, 16px, 26px, 40px, 48px. Header CTA height 40px. Lift CTA height 58px. Connect chips 36px. Nav items ~40px. Hero H1 is 56px. Feature cards use 20px radius and 40px padding. Tinted containers use 48px padding.

The “Breathing room over density”, “despite being a data product, the marketing surface is airy”, “Tint segmentation”, “Rounded cadence”, and “generous, breathable rhythm typical of a SaaS marketing page” readings, the ~8px-base restatement, and the recorded-layout “centered column” / “alternating bands” / two-up-and-grid-feature-cards / pricing-comparison-plus-closing-navy-CTA-band / repeated-20px-radius characterizations in this section are derived editorial implementation inferences from the verified surfaces; they are not Bigin-authored or a separately published UI specification.

Recorded layout:

- Centered ~1200px content column
- Deep-navy (`#0b1335`) hero band anchors the top with a 56px white headline
- Feature stories alternate between white (`#ffffff`) and tinted-blue (`#f4f9ff`) full-width bands
- Two-up and grid feature cards at 20px radius group related capabilities
- Pricing uses a comparison table plus a closing navy CTA band
- Repeated 20px radius across cards, sections, and pills

Source breakpoint table:

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, hero headline compresses, nav collapses to a drawer |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024-1440px | Full ~1200px layout, multi-column feature bands |

Treating the collapsing-strategy changes (hero 56px scales down, weight 700 maintained; feature bands stack; tinted/white bands stay full-width; nav becomes hamburger drawer), the screenshot-in-20px-cards / navy-band-full-width image-behavior as source-stated rather than a complete image specification, the touch-target record as a purpose reading of 40px / 58px / ~40px / 36px rather than a complete target-size specification, the breakpoint table as a recorded span of named widths not a complete specification of every unlisted control, and the 56px / 40px / 58px / 36px / ~40px / 20px / 1200px figures as surface measurements in this packet rather than universal layout tokens, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

Collapsing strategy recorded in the source: hero 56px Spoqa headline scales down on mobile, weight 700 maintained; feature bands: multi-column grid -> stacked single column; tinted/white alternating sections maintain full-width treatment; nav: horizontal links -> hamburger drawer (icons-ic-drawer).

Image behavior: product screenshots sit inside 20px-radius cards across breakpoints; the navy hero band retains its full-width treatment, reducing internal padding on mobile.

Touch-target record: primary CTA pill at 40px height; lift CTA at 58px for an unmistakable target; nav items at ~40px height within the header; connect chips at 36px height.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-26)

The live strings below are source-stated. Treating the parenthetical characterizations (hero headline / feature heading / product-update heading / company-page positioning) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

- "고객 획득부터 충성고객 확보까지  마케팅 전략은 선택이 아닌 필수입니다." — hero headline. *(verified live 2026-06-26)*
- "모든 마케팅 액션, 한 번에 집행하세요" — feature heading. *(verified live 2026-06-26)*
- "새로운 기능으로 더 강력해진 Bigin 4.0" — product-update heading. *(verified live 2026-06-26)*
- "데이터로 MarTech 산업을 선도하는 빅인사이트" — company page positioning. *(verified live 2026-06-26)*

Additional harvested control strings from the source body: "상담 신청", "상담 신청하기", "브랜드 메시지 자세히 보기 →", "캠페인", "자동화 new", "자세히 보기 →", "플랜 별 기능 비교", "블로그", "플랜 안내", "회사 소개", "로그인/회원가입".

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Bigin-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — including direct / capability-forward / confidently-consultative, MarTech-expert-discipline-not-bolt-on, declarative-full-funnel-slightly-imperative, growth-minded-operators, table tone labels (Declarative, full-funnel / Confident, mission-framed / Capability-forward / Terse category words / Consultative, low-pressure / Authority-framed), and forbidden-register (hype-without-substance / undefined-buzzwords / exclamation-heavy-urgency / one-click-trick) — are a derived editorial implementation inference from the verified surfaces; they are not Bigin-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Bigin's voice as **direct, capability-forward, and confidently consultative** — a MarTech expert who frames marketing as a discipline you should run end-to-end, not a feature you bolt on. The hero line "고객 획득부터 충성고객 확보까지 마케팅 전략은 선택이 아닌 필수입니다." ("From customer acquisition to loyalty — marketing strategy is a necessity, not a choice") is read as setting the register: declarative, full-funnel, and slightly imperative. Copy is read as speaking to growth-minded operators and treating the reader as someone ready to consolidate their stack.

| Context | Tone |
|---|---|
| Hero headlines | Declarative, full-funnel. "고객 획득부터 충성고객 확보까지…" Confident, mission-framed. |
| Feature titles | Capability-forward. "모든 마케팅 액션, 한 번에 집행하세요", "원하는 모든 유형을 하나의 솔루션에서". |
| Eyebrow labels | Terse category words. "캠페인", "자동화 new". |
| CTAs | Consultative, low-pressure. "상담 신청", "상담 신청하기", "브랜드 메시지 자세히 보기 →". |
| Company / about | Authority-framed. "데이터로 MarTech 산업을 선도하는 빅인사이트". |

**Forbidden register** (source): hype without substance, undefined buzzwords left unexplained, exclamation-heavy urgency, and any tone that treats marketing as a one-click trick rather than a strategy.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exceptions remain the Primary CTA source-stated glow-lift and the named Nav Link active `#3d4046`
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Primary CTA, Lift CTA, and Connect Chip; omitted loading/success on Form Input
- interactive kind and state-applicability map for White Feature Card, Tinted Section Container, Blue Accent Block, Mint Integration Card, Eyebrow Label, Header / Navigation band, and Footer
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
