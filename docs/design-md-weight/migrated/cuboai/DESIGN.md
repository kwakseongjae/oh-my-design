# Cubo Ai Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Cubo Ai is an AI smart baby monitor — a bird-shaped camera plus sleep sensor — built by Taiwan’s Cubo brand (parent company Yun Yun AI Baby Camera Co.). Catalog homepage identity is `https://us.getcubo.com/`. Treating that catalog homepage as identity rather than a fourth captured surface, treating the three 2026-06-17 live-inspect routes as this contract’s coverage, and treating token extraction as `live-extract` for those three surfaces, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. This contract covers three first-party live-inspect surfaces from 2026-06-17: homepage `https://us.getcubo.com/`, product `https://us.getcubo.com/products/cuboai-smart-baby-monitor` (“CuboAi Smart Baby Monitor 3”), and About Us `https://us.getcubo.com/pages/cuboai-about-us`. Token extraction is `live-extract`.

Treating the source token note as reconstruction coverage for these three surfaces, including primary-as-live-teal-CTA-pill, deeper-teal-as-headings-and-accents-not-CTA-fill, warm-parent-friendly-accents-coral-amber-sky, text-as-soft-charcoal-never-pure-black-for-body, and museo-sans-rounded-throughout-with-headings-weight-600, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Source token note: primary = live teal CTA pill (`#5be3d3`); deeper teal (`#24cdb9`) carries headings/accents. Warm parent-friendly accents: coral (`#ff8784`), amber (`#ffb516`), sky (`#4cc3e5`). Text is soft charcoal (`#4b4b4b`), never pure black for body. `museo-sans-rounded` throughout, headings weight 600.

The following atmosphere readings — storefront-engineered-to-lower-the-heart-rate-of-an-anxious-new-parent, white-canvas-broken-up-by-cool-grey-and-faint-mint-so-the-page-reads-as-airy-and-calm-rather-than-busy-or-salesy, charcoal-deliberately-never-pure-black-so-long-form-reassurance-stays-gentle-instead-of-clinical, teal-as-the-single-saturated-action-color-that-trains-the-eye-to-read-that-one-mint-teal-capsule-as-the-safe-next-step, Museo-Sans-Rounded-circular-terminals-echoing-the-bird-shaped-device, pediatric-trust-forward-warmth, commitment-to-the-pill-and-to-warmth, and warm-accents-as-the-palette-of-a-nursery-rather-than-a-gadget-catalog — and treating source prose `1.2` / `1.5` as unmerged writings of YAML `1.20` / `1.50` — are a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification. The captured canvas is `#ffffff`, broken up by `#ebebef` surfaces and `#e9f4f3` mint tints. Text sits in `#4b4b4b`. The live CTA teal is `#5be3d3`. Deeper teal `#24cdb9` is used for accent headings and feature-category chips. Headlines are Museo Sans Rounded SemiBold (600): 40px page H1s (“CuboAi Smart Baby Monitor 3”, “About Us”), 32px section titles, 24px card heads, at YAML line-height `1.20` (source prose also writes that ratio as `1.2`). Body is weight 400 at 16px / YAML `1.50` (source prose also writes `1.5`). Interactive geometry on the captured surfaces is capsule: teal CTA and newsletter inputs at 50px radius, feature-category chips at 9999px full-round. Surfaces separate by flat tint (`#ebebef`, `#dddddd`) and `#dedede` hairlines. YAML `shadow.soft` is `rgba(0,0,0,0.08) 0px 4px 6px 0px`; the compact add-to-cart control records `rgba(0,0,0,0.08) 0px 4px 6px` without that trailing `0px`. Warm accents named in the source are coral `#ff8784`, amber `#ffb516`, amber-soft `#ffcf68`, and sky `#4cc3e5`, plus mint-tint `#bbefe9` and mint-faint `#e9f4f3`.

Treating the following first-party live-site product-scope facts as narrative rather than interface tokens — Cubo-headquartered-in-Taiwan-as-an-award-winning-hardware-company, grainy-video-and-constant-false-alarms-as-the-legacy-problem-answered-by-proactive-AI, soft-industrial-design-that-does-not-look-like-surveillance-equipment, Parenthood-is-an-exploration-as-emotional-thesis-rather-than-the-founding-premise, the-monitor-exists-to-support-parents-through-an-uncertain-journey-rather-than-to-police-them, outcome-numbers-as-trust-earned-at-scale-while-the-design-language-stays-soft-and-reassuring-rather-than-boastful, treating About Us copy as providing that narrative context without by itself supplying interface tokens, and treating broader corporate facts beyond the inspected surfaces as omitted rather than reconstructed from general public knowledge — is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Cubo (parent company Yun Yun AI Baby Camera Co.) is headquartered in Taiwan and is an award-winning hardware company whose signature product is a bird-shaped AI camera paired with a sleep sensor pad. The founding premise, stated plainly on the About page, is that “parents deserve better” — that the legacy baby-monitor category had left worried new parents with grainy video and constant false alarms instead of genuine peace of mind. Cubo's answer was to put proactive AI (covered-face and rollover detection, danger-zone alerts, true cry detection, sleep analytics) behind a soft, friendly industrial design that doesn't look like surveillance equipment. The brand frames “Parenthood is an exploration” as its emotional thesis rather than the founding premise: the monitor exists to support parents through an uncertain journey rather than to police them. The About page surfaces hard outcome numbers — hundreds of millions of hours of sleep monitored and hundreds of thousands of families served across more than a hundred-plus countries — as evidence of trust earned at scale, while the design language stays soft and reassuring rather than boastful. Those About-page sentences are narrative context; they do not by themselves supply interface tokens.

The following refuse-and-embrace reading — refusing-the-cold-alarm-coded-aesthetic-of-security-cameras-no-hard-reds-no-surveillance-grid-imagery-and-the-dark-pattern-urgency-of-fear-driven-baby-safety-marketing, embracing-a-rounded-bird-shaped-hardware-form-echoed-in-Museo-Sans-Rounded, a-single-calming-teal, warm-nursery-accents, and-copy-that-decodes-a-stressful-domain-into-reassurance — is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Source §11 records that refuse/embrace contrast, including no surveillance-grid imagery. Source HTML comment names those interpretive claims as editorial readings, not directly sourced Cubo statements.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the following three captured-surface outcomes as Primary tasks — go to shop or checkout from the captured teal CTA (“Go to Shop”, “Checkout”); scan the CuboAi Smart Baby Monitor 3 product page; read About Us mission copy (“Parenthood is an exploration.”, “We know parents deserve better”) and use the About newsletter pill input — and not lifting source §13 fictional archetypes into this list, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

- Go to Shop or Checkout from the captured teal CTA on the homepage and product page.
- Scan the CuboAi Smart Baby Monitor 3 product page.
- Read About Us mission copy (“Parenthood is an exploration.”, “We know parents deserve better”) and use the About newsletter pill input.
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, tying observable work only to the three primary tasks, not substituting those entries, and treating About-page copy that addresses parents as product-scope copy rather than a researched persona segment, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. No individual personas are promoted. Source §13 is labeled fictional archetypes informed by publicly observable user segments; names in that section are illustrative and do not refer to real people. Do not substitute those entries. About-page copy addresses parents; that is product-scope copy, not a researched persona segment.

### Distinctive traits

Treating `#5be3d3` as catalog `primary_color` and the live CTA teal rather than as `#24cdb9`, treating canvas `#ffffff` as unmerged from feature-pill on-fill `#ffffff`, treating ink `#4b4b4b` as unmerged from on-primary `#4b4b4b` and from input placeholder `#4b4b4b`, treating ink-soft `#6d6d6d` as unmerged from muted `#9a9a9a` and from §9 feature-card body `#6d6d6d`, treating YAML `rounded` 4 / 12 / 50 / 9999 as unmerged from one another, treating YAML `shadow.soft` `0px 4px 6px 0px` as unmerged from the compact-add `0px 4px 6px`, treating YAML button type-role 20px / 700 as unmerged from compact 14px / 600, and treating hover, focus, pressed, disabled, loading, error, and success paints as not promoted from the 2026-06-17 default computed-style notes, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

- Catalog `primary_color` / YAML `primary` `#5be3d3` as the live teal CTA pill (“Go to Shop”, “Checkout”)
- Soft charcoal ink `#4b4b4b` for text; canvas `#ffffff`; surface `#ebebef`; surface-deep `#dddddd`; hairline `#dedede`
- One rounded family: `museo-sans-rounded` (Museo Sans Rounded) for headlines, labels, body, and buttons
- Headlines at SemiBold (600); CTA label at Bold (700) 20px; body at 400 / 16px / YAML `1.50`
- Pill geometry: 50px CTA and pill-input radius; 9999px feature-category chips; 12px cards
- Near-flat depth: tinted `#ebebef` / `#dddddd` surfaces and `#dedede` hairlines; YAML `shadow.soft` `rgba(0,0,0,0.08) 0px 4px 6px 0px`; compact-add `rgba(0,0,0,0.08) 0px 4px 6px`
- Deeper teal `#24cdb9` for accent headings and feature chips; warm accents `#ff8784`, `#ffb516`, `#ffcf68`, `#4cc3e5`; mint `#bbefe9` / `#e9f4f3`
- The 2026-06-17 inspect notes record default computed styles on the three surfaces. Hover, focus, pressed, disabled, loading, error, and success paints are not promoted from those notes

### Principles

These 5 items — Reduce anxiety, don’t manufacture it, with the product-thesis reading that a safety product that scares parents has failed at its job and the UI implication to lead with reassurance and peace of mind, avoid alarm-coded reds and fear framing, and keep the teal calm, not urgent; Softness is trust, with the reading that the rounded device, rounded type, and pill geometry signal a product that is gentle, not clinical, and the UI implication to favor capsule shapes (50px CTA/input radius), rounded letterforms, and soft charcoal text over hard black; One action, one color, with the reading that teal (`#5be3d3`) means “do this” and the UI implication to reserve the saturated teal exclusively for the primary CTA so the next step is never ambiguous; Calm and near-flat, with the reading that clean, uncluttered surfaces beat decorative depth and the UI implication to separate with tint and hairlines and reserve shadow for the smallest controls only; Warmth in the accents, with the reading that the supporting palette is a nursery, not a gadget rack, and the UI implication to use coral, amber, and sky for feature iconography and highlights to keep the product human — are a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification.

1. **Reduce anxiety, don’t manufacture it.** A safety product that scares parents has failed at its job. *UI implication:* lead with reassurance and peace of mind; avoid alarm-coded reds and fear framing; keep the teal calm, not urgent.
2. **Softness is trust.** The rounded device, rounded type, and pill geometry all signal a product that is gentle, not clinical. *UI implication:* favor capsule shapes (50px CTA/input radius), rounded letterforms, and soft charcoal text over hard black.
3. **One action, one color.** Teal (`#5be3d3`) means “do this.” *UI implication:* reserve the saturated teal exclusively for the primary CTA so the next step is never ambiguous.
4. **Calm and near-flat.** Clean, uncluttered surfaces beat decorative depth. *UI implication:* separate with tint and hairlines; reserve shadow for the smallest controls only.
5. **Warmth in the accents.** The supporting palette is a nursery, not a gadget rack. *UI implication:* use coral, amber, and sky for feature iconography and highlights to keep the product human.

Treating the following as a capture-bound application of source §7 Do’s — Museo-Sans-Rounded-for-everything including the-rounded-family-is-the-brand’s-voice, SemiBold-600-for-headlines-and-Bold-700-only-for-the-primary-CTA-label, reserve-`#5be3d3`-for-the-primary-CTA-pill including keep-it-the-single-action-color, deep-teal-`#24cdb9`-for-accent-headings-and-feature-category-chips, soft-charcoal-`#4b4b4b`-instead-of-pure-black, separate-with-flat-tint-`#ebebef`-and-`#dedede`-hairlines-not-heavy-shadows, pill-geometry-50px-CTA-and-inputs-full-round-feature-chips, and warm-accent-trio-coral-amber-sky-for-feature-highlights — is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

- Use Museo Sans Rounded for everything — the rounded family is the brand’s voice.
- Use SemiBold (600) for headlines and Bold (700) only for the primary CTA label.
- Reserve teal (`#5be3d3`) for the primary CTA pill — keep it the single “action” color.
- Use deep teal (`#24cdb9`) for accent headings and feature-category chips.
- Use soft charcoal (`#4b4b4b`) for text instead of pure black.
- Separate sections with flat tint (`#ebebef`) and `#dedede` hairlines, not heavy shadows.
- Use pill geometry — 50px CTA and inputs, full-round feature chips.
- Reach for the warm accent trio (coral `#ff8784`, amber `#ffb516`, sky `#4cc3e5`) for feature highlights.

### Avoid

The following items copy source §7 Don’ts, including no-sharp-technical-typeface-because-the-rounded-family-is-non-negotiable, no-spread-teal-because-it-dilutes-the-single-action-signal, no-pure-black-body-text-use-soft-charcoal-`#4b4b4b`, no-sharp-square-corners-on-interactive-elements-because-CTAs-and-inputs-are-pills, no-stack-heavy-shadows-because-Cubo-Ai-is-near-flat, no-alarm-coded-reds-or-hard-security-system-styling-because-the-tone-is-calm-and-reassuring, no-headline-heavy-black-weight-because-SemiBold-600-keeps-them-friendly, and no-second-saturated-brand-hue-to-compete-with-teal. They are a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification.

- Don’t use a sharp / technical typeface — the rounded family is non-negotiable.
- Don’t spread teal across many elements — it dilutes the single-action signal.
- Don’t use pure black (`#000000`) for body text — reserve soft charcoal `#4b4b4b`.
- Don’t use sharp / square corners on interactive elements — CTAs and inputs are pills.
- Don’t stack heavy shadows or elevation — Cubo Ai is near-flat.
- Don’t use alarm-coded reds or hard security-system styling — the tone is calm and reassuring.
- Don’t set headlines in a heavy black weight — SemiBold (600) keeps them friendly.
- Don’t introduce a second saturated brand hue to compete with teal.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings — catalog-primary-`#5be3d3`-as-live-CTA-teal-not-`#24cdb9` including Cubo-Teal-as-the-system’s-single-action-color, canvas-`#ffffff`-unmerged-from-feature-pill-on-fill-`#ffffff`, ink-`#4b4b4b`-unmerged-from-on-primary-`#4b4b4b`-and-from-input-placeholder-`#4b4b4b`, ink-soft-`#6d6d6d`-unmerged-from-muted-`#9a9a9a`, coral-`#ff8784`-as-feature-icon-and-alert-accent-not-as-§14-error-paint, and component-fields-staying-on-those-controls-rather-than-as-extra-general-inks — are a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification.

- **Cubo Teal / primary** (`#5be3d3`): catalog `primary_color`. YAML `primary`. Live CTA background on every “Go to Shop” / “Checkout” pill — the system’s single “action” color.
- **Deep Teal** (`#24cdb9`): YAML `primary-deep`. Accent teal for emphasis headings (e.g. “CuboAi Smart Baby Monitor” in the comparison block) and feature-category chips. A more saturated companion to the primary. Not the primary CTA fill.
- **Soft Charcoal / ink** (`#4b4b4b`): YAML `ink`. Primary text and heading color. A warm dark grey used instead of pure black (`#000000`) for body.
- **On Primary** (`#4b4b4b`): YAML `on-primary`. Label color on the teal CTA. Same hex as ink; the YAML jobs stay named separately.
- **Ink Soft** (`#6d6d6d`): YAML `ink-soft`. Secondary text, nav links, sub-headings, captions.
- **Muted** (`#9a9a9a`): YAML `muted`. Tertiary text, fine print, lowest-emphasis labels.
- **Canvas** (`#ffffff`): YAML `canvas`. Page background, white feature cards, pill input fills.
- **Surface Grey** (`#ebebef`): YAML `surface`. Cool-grey tinted surface for product cards and segmented blocks.
- **Surface Deep** (`#dddddd`): YAML `surface-deep`. Slightly deeper grey for nested card / media wells on product pages.
- **Hairline** (`#dedede`): YAML `hairline`. Thin borders, dividers, and white-card outlines.
- **Coral** (`#ff8784`): YAML `coral`. Warm red-pink for feature icons, alerts, and accent badges. Source color role for those accents; not a §14 error-banner paint.
- **Amber** (`#ffb516`): YAML `amber`. Golden highlight for tips, ratings, and decorative emphasis.
- **Amber Soft** (`#ffcf68`): YAML `amber-soft`. A lighter amber for tinted highlight surfaces.
- **Sky** (`#4cc3e5`): YAML `sky`. Calm blue accent for secondary feature highlights and links.
- **Mint Tint** (`#bbefe9`): YAML `mint-tint`. Pale mint fill for soft circular badges and decorative discs.
- **Mint Faint** (`#e9f4f3`): YAML `mint-faint`. The faintest mint wash for tinted section bands.

Keeping those component fields on the controls rather than as extra general inks is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. YAML component fields such as button-primary `bg` `#5be3d3` / `fg` `#4b4b4b`, feature-pill `fg` `#ffffff`, and input placeholder `#4b4b4b` stay on those controls. They are not extra general inks.

### Spacing

Treating YAML `spacing` numbers as recorded without a required px suffix, treating body 4px / 8px / 12px / 16px / 20px / 25px / 48px / 64px as body-recorded observations rather than a converted YAML scale, treating YAML `xl` 25 as unmerged from type-size 24px, and treating CTA padding 12px 20px, compact-add 5px 8px, pill-input 16px 20px, and region-row 10px 25px 10px 20px as component fields rather than a universal gutter, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. YAML `spacing`: xs 4, sm 8, md 12, base 16, lg 20, xl 25, xxl 48, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names a ~4px base unit and the scale 4px, 8px, 12px, 16px, 20px, 25px, 48px, 64px.

### Shape

YAML `rounded`: sm 4, md 12, lg 50, full 9999.

The following local-geometry reading, including 4px-inner-media-wells, 12px-cards-as-the-workhorse, 50px-CTA-pills-and-pill-inputs, 9999px-feature-category-chips-and-soft-circular-badges, and YAML-`full`-9999-as-unmerged-from-YAML-`lg`-50, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

- Small (YAML `sm` 4): inner media wells, small containers — observed 4px
- Medium (YAML `md` 12): cards and content containers — the workhorse, observed 12px
- Large (YAML `lg` 50): CTA pills and pill inputs — observed 50px
- Full (YAML `full` 9999): feature-category chips, soft circular badges — observed 9999px. Not the 50px CTA/input radius

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Tint (Level 1) | `#ebebef` background shift | Card / section separation without elevation |
| Hairline (Level 2) | `1px solid #dedede` border | White card outlines, dividers |
| Soft (Level 3) | `rgba(0,0,0,0.08) 0px 4px 6px` | Small floating controls (compact add button) |

YAML `shadow.soft` is `rgba(0,0,0,0.08) 0px 4px 6px 0px`. Compact-add `shadow` is `rgba(0,0,0,0.08) 0px 4px 6px` without the trailing `0px`. Those two tuples stay unmerged.

Treating Cubo Ai as a near-flat system, treating live inspection as finding `box-shadow: none` across the hero, nav, headings, and most cards, treating the only observed shadow as the compact add-to-cart control, treating YAML `shadow.soft` `0px 4px 6px 0px` as unmerged from the compact-add `0px 4px 6px`, treating depth as carried by flat tinted surfaces and thin hairlines, and treating emphasis as reaching for color (teal `#5be3d3`, coral `#ff8784`, amber `#ffb516`) never elevation, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Source §6 records that shadow philosophy.

### Motion

Source §15 names three durations. Treating those durations as source-stated named tokens rather than per-component computed observations, and treating the easing role names and signature-motion sentences as source §15 rules rather than live transition captures, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, pill press, focus |
| `motion-standard` | 220ms | Card / section reveal, sheet, dropdown |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Treating easing role names and uses from source §15 as recorded without promoting an exact curve is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Easing role names and uses from source §15:

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, sheets, chips |
| `ease-exit` | Dismissals |
| `ease-standard` | Two-way transitions |

Treating the exact curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` as omitted unattributed unnamed values in Named gaps rather than as promoted motion tokens is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Exact curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are omitted as unattributed. They are unnamed values in Named gaps, not promoted motion tokens.

Source §15 motion rules: motion is gentle and functional; pill chips and CTAs respond to press with a subtle scale/opacity shift; feature cards fade-in from below at `motion-standard / ease-enter`; no harsh bounce or jolt — a baby-monitor brand signals steadiness and reassurance, not playful surprise. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional. Treating those sentences, including baby-monitor-brand-signals-steadiness-and-reassurance-not-playful-surprise, as source-stated motion rules rather than computed per-component evidence is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Treating the duration-use word `focus` on `motion-fast` as a duration use rather than `focus-visible` treatment evidence is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Source duration row `motion-fast` names “Hover, pill press, focus” as a duration use. That duration-use word `focus` is not `focus-visible` treatment evidence.

Do not promote an easing curve, animation name, transition property, or a duration as computed per-component evidence until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-inspect-museo-sans-rounded-throughout and declared-only-sans-serif-and-Corbel-Corbel-Bold-fallbacks-not-promoted-as-the-brand-face, are a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Homepage, product, and About inspect notes compute visible text as `museo-sans-rounded`. H1 40px / H2 32px / H3 24px all weight 600, color `rgb(75,75,75)`. YAML `family.display` and `family.body` are both `museo-sans-rounded`. |
| Declared-only fallbacks — not promoted | Body copy names `sans-serif` and `Corbel` / `Corbel Bold` as fallbacks. They remain fallback declarations rather than UI-family claims. |

### Family

- **Display & Body:** `museo-sans-rounded` (Museo Sans Rounded), with `sans-serif` and `Corbel` / `Corbel Bold` fallbacks — a single rounded humanist sans used for headlines, labels, body, and buttons alike. SemiBold (600) at display sizes, Regular (400) for body, Bold (700) for the CTA

Treating Museo Sans Rounded as the observed family rather than a substitute, treating `sans-serif` and `Corbel` / `Corbel Bold` as fallbacks rather than the UI family, treating a system-face replacement of `museo-sans-rounded` on these captured surfaces as forbidden, and treating setting captured headlines in a sharp / technical typeface as forbidden, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Do not replace `museo-sans-rounded` with a system face on these captured surfaces. Do not set captured headlines in a sharp / technical typeface.

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1.20` and `1.50`. Treating those ratios as scaling with font size rather than fixed px, treating 48px / 38.4px / 28.8px / 21.6px as size-local observations rather than replacements for the ratios, treating source prose `1.2` / `1.5` as unmerged writings of those YAML ratios, and treating compact-add 14px / 600 as a component field rather than a replacement for the YAML button type-role 20px / 700, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | YAML use |
|---|---|---:|---:|---:|---|---|
| Display Hero | Museo Sans Rounded | 40px (2.50rem) | 600 | 1.20 | 48px | Page H1 (product / About hero), Museo Sans Rounded SemiBold |
| Section Heading | Museo Sans Rounded | 32px (2.00rem) | 600 | 1.20 | 38.4px | Section titles, Museo Sans Rounded SemiBold |
| Sub-section | Museo Sans Rounded | 24px (1.50rem) | 600 | 1.20 | 28.8px | Card / feature heads |
| Feature Label | Museo Sans Rounded | 18px (1.13rem) | 600 | 1.20 | 21.6px | Feature pill labels (Safety/Sleep/Health/Memories) |
| Button | Museo Sans Rounded | 20px (1.25rem) | 700 | 1.20 | — | Primary CTA label (Bold) |
| Body | Museo Sans Rounded | 16px (1.00rem) | 400 | 1.50 | — | Standard reading text, charcoal |
| Button Small | Museo Sans Rounded | 14px (0.88rem) | 600 | 1.20 | — | Compact add-to-cart label |

Treating 40 × 1.20 = 48, 32 × 1.20 = 38.4, 24 × 1.20 = 28.8, and 18 × 1.20 = 21.6 as showing that the body-table px figures are ratio products at the captured sizes, treating those px figures as not replacements for the YAML ratios, treating compact-add 14px / 600 as a component field rather than a replacement for this YAML button row, and treating source typography principles — one rounded family, three weights; SemiBold heads, regular body; rounded as brand DNA; charcoal over black — as source §3 restatement rather than a separately published Cubo Ai type specification, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Do not replace the YAML ratios with those px figures. Compact-add 14px / 600 is a component field; it is not a replacement for this YAML button row. Text is `#4b4b4b`, never `#000000` for body.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Catalog logo metadata is a Google favicon lookup, not a captured first-party mark.

Treating product and lifestyle photography as sitting on white or tinted-grey wells with minimal shadow, treating cards as maintaining a 12px radius across breakpoints, and treating device close-ups as keeping their soft, rounded framing consistent with the brand geometry, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Product and lifestyle photography sits on white or tinted-grey wells, minimal shadow. Cards maintain a 12px radius across breakpoints. Device close-ups keep their soft, rounded framing consistent with the brand geometry.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, and treating YAML `components_harvested: true` as a harvest flag rather than complete state coverage, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

YAML `components_harvested` is true. Treating the 2026-06-17 inspect notes as default computed styles on the three surfaces, and treating hover, focus, pressed, and other interaction paints as omitted from those notes rather than as a complete interaction contract, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. The 2026-06-17 inspect notes record default computed styles on the three surfaces. Hover, focus, pressed, and other interaction paints for the harvested controls stay omitted from those notes.

Treating the following source §14 rows as preserved source-stated state copy while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Source §14 state contract:

| State | Treatment |
|---|---|
| Empty (no recorded events) | White canvas. Single Soft Charcoal (`#4b4b4b`) line at body size in a calm tone explaining nothing has been recorded yet, with one teal CTA. No alarm imagery. |
| Empty (saved memories, none yet) | Muted (`#9a9a9a`) single line: nothing saved yet, plus a gentle path forward. Reassuring, never empty-scary. |
| Loading (feed / results fetch) | Skeleton blocks on `#ebebef` tinted surface at final card dimensions, 12px radius. Flat pulse consistent with the near-flat system — no harsh shimmer. |
| Loading (in-place refresh) | Subtle inline progress in teal; previous content stays visible. Never blank the safety feed during refresh. |
| Error (connection lost) | Inline message in Soft Charcoal with a plain-language explanation and a retry. Calm, not alarmist — states what to do next, no scary red banner. |
| Error (form validation) | Field-level message below the pill input in a warm tone; describes what’s valid, not just “Required”. |
| Success (order placed / setup complete) | Brief inline confirmation in a calm tone with teal accent; next-step detail linked immediately below. No loud celebration. |
| Skeleton | `#ebebef` blocks at final dimensions, 12px radius, flat pulse. |
| Disabled | Muted (`#9a9a9a`) text on reduced-opacity surface; teal actions fade rather than turn grey to preserve brand read. |

Treating those §14 rows as source-stated state copy rather than live interaction captures, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, treating YAML `type: card` and `type: badge` as having no interactive-kind confirmation for a §4.4 map, treating Region Selector Row YAML `type: listItem` plus selector/dropdown-row use as interactive-kind evidence, treating Compact Add as an add-to-cart / quantity control so loading, error, and success stay applicable by that action role while unobserved visual paint is omitted, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source never records `focus-visible`; that applicability stays, and the visual treatment is omitted. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. Grey Surface Card and White Feature Card have YAML `type: card`, and Feature Category Pill has YAML `type: badge`, with no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Region Selector Row keeps YAML `type: listItem` and uses selector/dropdown-row use as interactive-kind evidence, so kind and a role-based map are declared. Compact Add is an add-to-cart / quantity control, so loading, error, and success stay applicable by that action role; unobserved visual paint is omitted. This is not a complete state-coverage claim.

### Primary CTA Pill

- Role: primary teal CTA pill
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#5be3d3`
- Text: `#4b4b4b`
- Radius: 50px
- Padding: 12px 20px
- Height: 48px
- Font: 20px Museo Sans Rounded weight 700
- YAML font: `20px / 700 museo-sans-rounded`
- Use: Primary teal CTA — “Go to Shop”, “Checkout” — the system’s single primary action
- Observed: default computed style

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the primary teal CTA pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary CTA can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this primary teal CTA because “Go to Shop” is named as a destination action and “Checkout” request/outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a primary teal CTA pill (“Go to Shop”, “Checkout”); exact destination, request, and outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Treating the following as a source §9-only local recipe on this CTA with the hero H1 and feature-chip row — white background; headline at 40px Museo Sans Rounded weight 600, line-height 1.2, color `#4b4b4b`; below it a row of full-round deep-teal feature chips (`#24cdb9` bg, white text, 18px weight 600): Safety, Sleep, Health, Memories; one teal CTA pill: `#5be3d3` background, `#4b4b4b` text, 50px radius, 12px 20px padding, 20px weight 700 — “Go to Shop” — is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Hero local recipe: white background; 40px Museo Sans Rounded weight 600 H1 at line-height 1.2 in `#4b4b4b`; a row of full-round `#24cdb9` feature chips (white text, 18px weight 600) labeled Safety / Sleep / Health / Memories; one `#5be3d3` CTA pill, `#4b4b4b` text, 50px radius, 12px 20px padding, 20px weight 700 — “Go to Shop”.

### Compact Add Button

- Role: compact add-to-cart / quantity control
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#5be3d3`
- Text: `#4b4b4b`
- Radius: 50px
- Padding: 5px 8px
- Height: 28px
- Font: 14px Museo Sans Rounded weight 600
- YAML font: `14px / 600 museo-sans-rounded`
- Shadow: `rgba(0,0,0,0.08) 0px 4px 6px`
- Use: Compact add-to-cart / quantity control
- Observed: default computed style

Treating this compact add-to-cart / quantity control as an action that can be in flight, fail, or complete, so loading, error, and success stay applicable by that role while unobserved visual paint is omitted, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the compact add-to-cart / quantity control |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An add control can be unavailable; visual treatment omitted |
| loading | applicable | Add-to-cart / quantity action can be in flight; visual treatment omitted |
| error | applicable | Add-to-cart / quantity action can fail; visual treatment omitted |
| success | applicable | Add-to-cart / quantity action can complete; visual treatment omitted |

### Pill Input

- Role: newsletter / lead-capture pill input
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#4b4b4b`
- Radius: 50px
- Padding: 16px 20px
- Height: 52px
- Font: 16px Museo Sans Rounded
- YAML font: `16px museo-sans-rounded`
- Placeholder: `#4b4b4b`
- Use: Newsletter / form pill input
- Observed: default computed style

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the About newsletter / form pill input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; source §14 names field-level validation below the pill input; visual treatment omitted |

Treating loading and success applicability as omitted on this pill input because newsletter-submit loading and confirmation are not mapped as paints on this field, and exact request/outcome mapping to the input is unresolved, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Loading and success applicability are omitted. Exact request/outcome mapping to the input is unresolved.

Treating the following as a source §9-only local recipe — white `#ffffff` background, 50px radius, 16px 20px padding, 52px height, 16px Museo Sans Rounded, `#4b4b4b` text and placeholder, with a `#5be3d3` teal submit pill beside it — is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Newsletter local recipe: pill input as harvested, with a `#5be3d3` teal submit pill beside it.

### Feature Category Pill

- Role: feature category chip
- Type: badge
- Anatomy: label
- Background: `#24cdb9`
- Text: `#ffffff`
- Radius: 9999px (full)
- Font: 18px Museo Sans Rounded weight 600
- YAML font: `18px / 600 museo-sans-rounded`
- Use: Feature category chip (“Safety”, “Sleep”, “Health”, “Memories”)
- Observed: default computed style

Treating this feature category pill as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. No interactive-kind evidence is given for this badge. Kind and a state-applicability map are omitted.

### Grey Surface Card

- Role: grey product / content card surface
- Type: card
- Anatomy: surface
- Background: `#ebebef`
- Text: `#4b4b4b`
- Radius: 12px
- Use: Product / content card on the cool-grey surface
- Observed: default computed style

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### White Feature Card

- Role: white feature card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Text: `#4b4b4b`
- Border: 1px solid `#dedede`
- Radius: 12px
- Use: White feature card with hairline outline (near-flat, soft shadow at most)
- Observed: default computed style

Treating YAML `fg` `#4b4b4b` as unmerged from the source §9-only feature-card body `#6d6d6d`, and treating near-flat / hairline outline as this card’s local fields rather than generic Elevation, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. YAML card-canvas `fg` is `#4b4b4b`. Source §9-only local recipe: title 24px Museo Sans Rounded weight 600, `#4b4b4b`; body 16px weight 400, line-height 1.5, `#6d6d6d`. Those two text colors stay unmerged.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Nav Link

- Role: top nav / footer link
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#ffffff`
- Text: `#6d6d6d`
- Font: 16px Museo Sans Rounded weight 400
- YAML font: `16px / 400 museo-sans-rounded`
- Active: deep teal `#24cdb9` text on active item
- Use: Top horizontal nav and footer link rows (“About Us”, feature links)
- Observed: default computed style; active as named

Treating this top nav / footer link as a destination/select control, so loading, error, and success are not-applicable on the control itself, and treating named active `#24cdb9` text as a captured variant rather than `focus-visible` treatment evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as top nav / footer link |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive tab; visual treatment omitted |
| disabled | applicable | A nav link can be unavailable; visual treatment omitted |
| loading | not-applicable | A top nav / footer link selects a destination; the control itself does not enter a loading state |
| error | not-applicable | Nav-link selection is not a validation or request-failure state on the control |
| success | not-applicable | Following a nav / footer link is not an action-outcome confirmation on the control |

### Region Selector Row

- Role: country/region dropdown row
- Kind: interactive
- Type: listItem
- Anatomy: row
- Background: `#ffffff`
- Text: `#4b4b4b`
- Height: 38px
- Padding: 10px 25px 10px 20px
- Font: 16px Museo Sans Rounded
- YAML font: `16px museo-sans-rounded`
- Use: Country/region dropdown rows (United States, Taiwan, Japan, etc.)
- Observed: default computed style

Treating this country/region dropdown row as a selector control so kind and a role-based map are declared from selector/dropdown-row use, and treating loading, error, and success as not-applicable on the row itself because the row selects a region rather than submitting a request, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. YAML `type: listItem` is preserved. Selector / dropdown-row use is interactive-kind evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as a country/region dropdown row |
| hover | applicable | Pointer-web selector row; visual treatment omitted |
| focus-visible | applicable | Interactive selector; visual treatment omitted |
| disabled | applicable | A region row can be unavailable; visual treatment omitted |
| loading | not-applicable | A region selector row selects a locale; the row itself does not enter a loading state |
| error | not-applicable | Region selection is not a validation or request-failure state on the row |
| success | not-applicable | Choosing a region is not an action-outcome confirmation on the row |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Treating YAML spacing numbers and the body ~4px / 4px–64px scale as captured-surface figures rather than a cross-viewport specification, treating CTA 12px 20px, compact-add 5px 8px, pill-input 16px 20px, and region-row 10px 25px 10px 20px as component fields rather than a global gutter, treating white / tinted-grey full-width bands as source §5 layout on the three live-inspect surfaces, treating centered single-column heroes as anchored by a 40px Museo Sans Rounded H1, treating feature categories as a row of full-round teal pills, and treating cards as 12px surfaces that group related features, specs, and comparison rows, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. YAML spacing is xs 4, sm 8, md 12, base 16, lg 20, xl 25, xxl 48, section 64. Body layout names a ~4px base and 4px, 8px, 12px, 16px, 20px, 25px, 48px, 64px. Centered single-column heroes are anchored by a 40px Museo Sans Rounded H1. Feature categories are arranged as a row of full-round teal pills (Safety / Sleep / Health / Memories). Product / About sections alternate white (`#ffffff`) and tinted grey (`#ebebef`) full-width bands. Cards use a 12px radius and group related features, specs, and comparison rows.

Treating the following as a source §9-only local recipe for a tinted section — `#ebebef` background, full-width; section title 32px Museo Sans Rounded weight 600, `#4b4b4b`; cards inside use white `#ffffff` with `#dedede` hairline and 12px radius — is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Tinted-section local recipe: `#ebebef` full-width band, 32px weight 600 title in `#4b4b4b`, white 12px cards with `#dedede` hairline.

The following whitespace-and-rhythm readings — calm-over-density-generous-vertical-rhythm, flat-segmentation-by-background-tint-and-hairlines-not-heavy-elevation, and pill-rhythm-from-the-repeated-50px-radius-capsule — are a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification. Source §5 records that whitespace philosophy.

Treating source §8 Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` as source-stated layout rules rather than a captured cross-viewport pass, treating 48px / 52px / 38px as desktop-named control heights rather than proven touch-target tokens from a mobile capture, and treating hero-H1-scale-down / feature-pill-wrap / multi-column-to-stacked as source §8 collapsing strategy rather than observed interaction expansion, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero H1 compresses, feature pills wrap |
| Tablet | 640-1024px | Moderate padding, 2-up feature / product cards |
| Desktop | 1024-1440px | Full layout, centered hero, multi-column feature bands |

Touch-target sentences from source §8: Primary CTA pill at 48px height, full pill; pill inputs at 52px height with 16px 20px padding; region rows at 38px height with generous horizontal padding; feature chips spaced for touch in the pill row. Collapsing strategy: 40px Museo Sans Rounded H1 scales down on mobile, weight 600 maintained; feature pill row wraps on narrow viewports; product / feature bands multi-column → stacked single column; tinted / white alternating sections maintain full-width treatment. Treating product and lifestyle photography as sitting on white or tinted-grey wells with minimal shadow, treating cards as maintaining a 12px radius across breakpoints, and treating device close-ups as keeping their soft, rounded framing consistent with the brand geometry, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Product and lifestyle photography sits on white or tinted-grey wells, minimal shadow; cards maintain a 12px radius across breakpoints; device close-ups keep their soft, rounded framing consistent with the brand geometry.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating Cubo Ai’s voice as calm, reassuring, and parent-first — speaking to an exhausted, worried new parent and lowering the temperature rather than raising urgency — treating that register as a derived reading of the live About-page lines, treating the table below as observed guidance from live copy including Hero-headlines-warm-declarative-confident-never-hype, Feature-labels-plain-single-word, CTAs-direct-low-pressure, Mission-About-empathetic-and-human, and Feature-descriptions-benefit-first-reassurance-led-explains-how-the-feature-reduces-parental-worry, is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Source §10 records that voice reading. Copy samples below are verbatim from the live brand surfaces (verified 2026-06-17).

| Context | Tone |
|---|---|
| Hero headlines | Warm, declarative. “CuboAi Smart Baby Monitor 3”, “Why choose CuboAi?”. Confident, never hype. |
| Feature labels | Plain, single-word categories. “Safety”, “Sleep”, “Health”, “Memories”. |
| CTAs | Direct, low-pressure. “Go to Shop”, “Checkout”, “Add”. |
| Mission / About copy | Empathetic and human. “Parenthood is an exploration.”, “We know parents deserve better”. |
| Feature descriptions | Benefit-first, reassurance-led. Explains how the feature reduces parental worry. |

Treating the following live-copy parentheticals — homepage-and-product-heading-as-reassurance-framed, About-Us-hero-as-empathetic-mission-line, and About-Us-section-heading-as-advocacy-register — as a derived editorial implementation inference from the verified surfaces; they are not Cubo Ai-authored or a separately published UI specification. Voice samples (verbatim from live brand surfaces):

- “Our parenting-focused features reduce parental [stress/anxiety]” — homepage + product section heading (reassurance-framed). *(verified live 2026-06-17)*
- “Parenthood is an exploration.” — About Us hero (empathetic mission line). *(verified live 2026-06-17)*
- “We know parents deserve better” — About Us section heading (advocacy register). *(verified live 2026-06-17)*

Treating the following forbidden-register sentences as source §10 constraints rather than Cubo Ai-authored doctrine is a derived editorial implementation inference from the verified surfaces; it is not Cubo Ai-authored or a separately published UI specification. Forbidden register from source §10: fear-based danger framing, alarm-coded urgency, undefined technical jargon, exclamation-heavy hype, anything that increases rather than reduces a new parent’s anxiety.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- exact easing curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)`
- hover, focus, pressed, and `focus-visible` visual treatments on the harvested controls
- loading, error, and success visual treatments on Primary CTA Pill and Compact Add Button; loading and success paints on Pill Input
- founding chronology and other corporate facts beyond the inspected surfaces
- motion transition properties, animation names, and per-component computed duration / easing / reduced-motion behavior — promote those as computed evidence only after per-component capture of all five: transition properties, animation name, duration, easing, and reduced-motion behavior
