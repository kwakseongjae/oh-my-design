# 中華航空 Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating the product-origin sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. China Airlines (中華航空) is Taiwan’s international carrier. Official company material dates the founding to 16 December 1959. Catalog homepage identity is `https://www.china-airlines.com/`.

Treating the following URLs as the named evidence domains of this reconstruction, including this-contract-covers-the-2026-07-13-public-captures-of-the-three-named-hosts, values-stay-attached-to-the-surface-that-established-them, Korean-public-passenger-host-not-a-proxy-for-every-locale-or-authenticated-app-or-cabin-or-documentation-surface, corporate-about-and-governance-pages-as-repeated-public-header-geometry-not-booking-workflow-tokens, design-story-and-sustainability-vision-as-official-narrative-not-web-tokens, and Brand-Center-as-an-authenticated-portal-boundary-not-a-font-or-asset-fact, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. This contract covers the 2026-07-13 public captures of the Korean public passenger site at `https://www.china-airlines.com/kr/ko`, the corporate about page at `https://www.china-airlines.com/kr/ko/about-china-airlines/about-us`, and the corporate governance page at `https://www.china-airlines.com/kr/ko/about-china-airlines/corporate-governance`. Color, type, and component values below stay attached to those three public-web surfaces. A public-web value is not a proxy for cabin UI, an authenticated product, a mobile app, or a documentation system. Official Design Story (`https://www.china-airlines.com/it/en/about-us/design-story`) and sustainability vision (`https://calec.china-airlines.com/csr/en/aboutus.html`) are official narrative sources in the packet; they are not extra token captures. `https://brandcenter.china-airlines.com/` is recorded as an official authenticated Brand Center; the reviewed session had no downloadable or inspectable font or usage-rule asset from it, so it contributes no font or asset claim.

Source token extraction is `live-extract`. Catalog `primary_color` is `#23569d`. Treating that extraction class and catalog color as a register split — `#23569d` is catalog `primary_color` and the observed 56px public-web blue action / selected-option text rather than bright circular `#1d6dcd` and rather than local booking accent `#d81159`; `#1d6dcd` stays on the observed circular public action; `#d81159` stays a measured 64px home action rather than a global brand-primary or status color; canvas `#ffffff` is page / menu-option / dialog surface rather than a second paint from on-primary `#ffffff`; ink `#17181c` is public control and body text rather than dialog shell text `#000000`; muted `#69707a` and hairline `#8e959f` stay utility roles; soft-blue `#f1f8fd` / `#9fd1f1` stay dialog-open chip controls — is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

The following atmosphere readings — digital-surface-more-utilitarian-than-cabin-identity, white-planes-dark-ink-disciplined-blue-actions-muted-gray-utility-compact-8px-corners, booking-and-navigation-carry-the-page, distinctive-expression-in-the-relationship-between-the-calm-service-layer-and-the-cabin-design-story, Oriental-aesthetics-local-inspirations-home-away-from-home-warm-blessings-as-cabin-narrative-not-web-tokens, white-and-navy-operational-plane, keep-high-density-travel-tasks-legible-with-white-canvas-dark-ink-and-muted-gray-utility, and 2024-corporate-direction-as-official-language-not-an-extrapolation-of-the-three-captured-web-surfaces — are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification. The source describes the captured public passenger site as connecting ticketing and travel-service tasks with a broader corporate story of safe, reliable global connection. Observed public-web canvas `#ffffff`, ink `#17181c`, and gray utility text `#69707a` keep high-density travel tasks legible. `#23569d` is the observed standard blue action and selected-option text; `#1d6dcd` appears on a circular public action. `#d81159` is a measured home action fill. Compact 8px corners are recorded on public controls. Official company and cabin-design narratives name Oriental aesthetics, local inspirations, a home-away-from-home feeling, and warm blessings for the cabin experience. The current corporate direction was refreshed in 2024 around “Believe we can always do better,” a mission to create more wonderful moments through flying, and a vision of leading Asia-Pacific while flying worldwide. These are official company and cabin-design narratives, not an extrapolation of the three captured web surfaces.

Treating the following public-history facts as narrative rather than interface tokens, including founding-16-December-1959, 2024-refresh-after-a-review-of-market-and-operating-conditions, safe-reliable-efficient-excellent-service, and cabin-persimmon-Song-dynasty-sapphire-as-source-specific-material-not-the-captured-website-visual-system, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Official company profile dates the airline’s founding to 16 December 1959. Current corporate direction, refreshed in 2024, joins a global-connection vision with the mission of creating more wonderful moments through flying and explicitly connects that strategy to safe, reliable, efficient, and excellent service rather than to visual style alone. The official design story locates experiential expression in cultural continuity: Oriental aesthetics, local inspirations, home-like comfort, and warm blessings inform the cabin. Persimmon wood, mountain landscapes, Song-dynasty references, and sapphire details are described as part of that cabin experience. These are source-specific material and service narratives, not a claim that the captured public website implements the same visual system.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings below as Primary tasks, and not lifting tasks from source §13 audience groups, and treating independently-verified-in-this-packet as the inspected Korean public passenger URL rather than the harvested action, menu-option, and dialog controls, which remain harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Independently verified in this packet is the inspected public passenger URL `https://www.china-airlines.com/kr/ko`; named action, menu-option, and dialog uses are harvested strings/controls, not independently verified destination routes.

- Use the harvested primary blue public-web action (YAML use: Primary blue public-web action; `home::[data-omd-capture="99"]`).
- Select a harvested option in the public booking/menu control (YAML use: Selected option button in the captured public booking/menu control; `home::[data-omd-capture="88"]`).
- Open the harvested public search dialog shell (YAML use: Public search dialog shell; `home::div[role=dialog]`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 official material groups as a scope boundary rather than user-research personas or authenticated-product roles, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. No named or fictional personas are asserted. Official material addresses passengers seeking a satisfactory flight experience and describes a worldwide network supported by safe, reliable, efficient service. The official sustainability framework also names employees, communities, partners, and other stakeholders. These audience groups are a scope boundary, not user-research personas or a claim about authenticated-product roles.

### Distinctive traits

The following unmerged-role extras — treating `#23569d` as catalog `primary_color` and the 56px public-web action / selected-option text rather than `#1d6dcd` or `#d81159`, treating `#1d6dcd` as the circular public action rather than a second primary, treating `#d81159` as a local 64px home action rather than a global brand-primary or status color, treating canvas `#ffffff` as the same hex as on-primary rather than a second paint, treating ink `#17181c` as unmerged from dialog `#000000`, treating muted `#69707a` and hairline `#8e959f` as unmerged utility roles, treating soft-blue `#f1f8fd` / `#9fd1f1` as dialog-open chips rather than canvas, treating 8px control / 20px dialog / YAML `pill` 40 as unmerged radii, treating 56px action / 48px selected option / 40px menu row / 64px booking accent as unmerged heights, treating Roboto / Arial / `Noto Sans KR` as a computed operating-system stack rather than a brand family token, and treating hover-focus-pressed-disabled-error-motion as not promoted — are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification.

- Catalog `primary_color` / primary blue `#23569d` as the 56px public-web action fill and selected menu-option text
- Bright blue `#1d6dcd` on a circular public action; scoped to that control
- Booking accent `#d81159` as a measured 64px home action fill, not a universal semantic status color
- Canvas `#ffffff` with ink `#17181c`; dialog shell text `#000000`; muted utility `#69707a`; hairline `#8e959f`
- Soft-blue surface `#f1f8fd` with soft-blue border `#9fd1f1` on dialog-open chip controls
- Computed stack `Roboto, Arial, "Noto Sans KR", sans-serif` classified as an operating-system `system` family; no `tokens.typography.family.ui`
- 8px public-web control corners; 20px search-dialog radius; YAML `rounded.pill` 40 recorded as a YAML step
- Heights: 56px primary blue action; 48px selected menu option; 40px captured dark menu row; 64px local booking accent
- No captured hover, focus, pressed, disabled, error, success, or motion token is promoted

### Principles

The four numbered stems restate official vision, mission, cabin-design, and value language. The UI implications that follow each stem are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification. The UI implications are this reference’s interpretation of official positioning, not published China Airlines product rules.

1. **Safety and reliability anchor the journey.** The official vision calls out safe, reliable, efficient, and excellent service. *UI implication:* make important travel decisions explicit and calm before adding brand decoration.
2. **Warmth is part of service, not an interruption to it.** The company mission focuses on wonderful moments through flying, while the cabin story frames welcome and home-like comfort. *UI implication:* use human, reassuring language around passenger moments without obscuring operational information.
3. **Local culture can travel globally.** Official cabin material connects Asian aesthetics and local inspirations to an international journey. *UI implication:* use culturally specific imagery or stories only with direct rights, relevance, and a clear contextual role.
4. **Improve continuously.** “Believe we can always do better” is the company’s stated value. *UI implication:* prefer transparent recovery and clear next steps over pretending an uncertain travel task succeeded.

Treating the following as a capture-bound application of source §7 Do’s, including white-calm-text-led-travel-task-surfaces-with-measured-`#23569d`-in-the-documented-action-context, 8px-control-and-20px-dialog-corners-only-in-observed-public-web-contexts, distinction-between-observed-service-UI-and-official-cabin-narrative, unresolved-brand-typeface-when-a-licensed-China-Airlines-font-is-not-proved, culturally-grounded-imagery-only-with-project-rights-and-evidence, and cabin-metaphors-as-official-context-not-a-request-to-reproduce-airline-assets, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

- Keep public travel-task surfaces white, calm, and text-led, using the measured `#23569d` blue for the documented action context.
- Use the 8px control and 20px dialog corners only in their observed public-web contexts.
- Preserve the distinction between observed service UI and the official cabin narrative of Asian aesthetics and local inspiration.
- Leave the brand typeface unresolved when the target project cannot prove a licensed China Airlines font.
- Pair service clarity with culturally grounded imagery only when the project has its own rights and evidence; cabin metaphors such as persimmon, Song-dynasty calm, and local landscape are official context, not a request to reproduce airline assets.

### Avoid

The following items copy source §7 Don’ts, including Roboto-Arial-or-system-fallback-not-a-proprietary-UI-font, local-pink-booking-action-not-a-global-error-success-or-brand-primary, no-inferred-hover-focus-pressed-disabled-validation-or-accessibility-from-class-names-or-unmeasured-CSS, and public-web-tokens-not-carried-into-cabin-mobile-app-authenticated-product-or-documentation-without-direct-evidence. They are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification.

- Do not represent Roboto, Arial, or a system fallback as China Airlines’ proprietary UI font.
- Do not turn the locally observed pink booking action into a global error, success, or brand-primary color.
- Do not infer hover, focus, pressed, disabled, validation, or accessibility behavior from class names or unmeasured CSS.
- Do not carry public web tokens into the cabin, mobile app, authenticated product, or documentation experience without direct evidence. Treating that last-bullet characterization as a reconstruction reading of the capture boundary rather than China Airlines-authored platform doctrine is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-primary-not-bright-circular-not-booking-accent, canvas-white-as-the-same-hex-as-on-primary-rather-than-a-second-paint, ink-not-dialog-shell-text, muted-not-hairline, soft-blue-surface-not-canvas, cabin-sapphire-not-folded-into-this-public-web-palette, and no-error-or-success-role-promoted — are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification. Catalog `primary_color` `#23569d` is YAML `primary` and the harvested 56px public action / selected-option text; it is not bright blue `#1d6dcd` and not booking accent `#d81159`. YAML `canvas` `#ffffff` is visible page, menu-option, and dialog surfaces; YAML `on-primary` `#ffffff` is text on the primary blue fill; those keys share the hex and are not split into two paints. YAML `ink` `#17181c` is primary public control and body text. Search-dialog `fg` `#000000` is that shell’s text field, not ink. YAML `muted` `#69707a` is search/utility control text; YAML `hairline` `#8e959f` is utility borders; they are not merged. YAML `soft-blue` `#f1f8fd` and `soft-blue-border` `#9fd1f1` are dialog-open chip controls, not canvas. YAML `primary-bright` `#1d6dcd` is the circular public action. YAML `booking-accent` `#d81159` is the measured 64px home action. The cabin’s sapphire and other material/color references are documented by the official design story but are not folded into this public-web palette. No error or success role is promoted: the supplied capture does not establish one.

- **Canvas** (`#ffffff`): YAML `canvas`. Visible page, menu-option, and dialog surfaces across the captured public site.
- **On-primary** (`#ffffff`): YAML `on-primary`. Text on the primary blue public-web action. Same hex as canvas; not a second paint.
- **Ink** (`#17181c`): YAML `ink`. Primary public control and body text.
- **Primary blue** (`#23569d`): catalog `primary_color`. YAML `primary`. 56px public action fill and selected menu-option text.
- **Bright blue** (`#1d6dcd`): YAML `primary-bright`. Observed circular public action; scoped to that control.
- **Booking accent** (`#d81159`): YAML `booking-accent`. Observed 64px home action; local measured surface, not a universal semantic status color.
- **Muted utility** (`#69707a`): YAML `muted`. Search/utility control text.
- **Hairline** (`#8e959f`): YAML `hairline`. Search/utility control borders.
- **Soft blue surface** (`#f1f8fd`): YAML `soft-blue`. Dialog-open chip controls.
- **Soft blue border** (`#9fd1f1`): YAML `soft-blue-border`. Dialog-open chip controls.

### Spacing

YAML scale: action-x 20, menu-end 24, dialog-viewport 40. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names 20px horizontal padding on the 56px blue action, 12px horizontal padding on the 48px selected menu option, `6px 24px 6px 0px` padding and a 40px row height on the captured dark menu item, and 40px padding on the surrounding modal viewport in the desktop capture. Treating those YAML numbers as recorded without a required px suffix, treating body 20px / 12px / 24px / 40px as body-recorded steps rather than a rewrite of the YAML keys, treating the 24px end spacing as an observed local metric rather than a universal grid, and treating harvested control heights 56px / 48px / 40px / 64px as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

### Shape

YAML `rounded`: control 8, dialog 20, pill 40.

The following local-geometry reading, including 8px-on-public-web-controls, 20px-on-the-search-dialog, YAML-pill-40-as-a-YAML-step-not-a-harvested-pill, and those-radii-not-universal-for-every-unlisted-control, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

- Control (8px): YAML `rounded.control`. Public-web primary action and selected menu option.
- Dialog (20px): YAML `rounded.dialog`. Public search dialog shell.
- YAML `rounded.pill` 40: recorded as a YAML step. No harvested control in this packet uses it as a promoted component radius.

Treating 8px corners as local harvested geometry on the named public-web controls rather than a universal radius for every unlisted control, treating 20px as the search-dialog shell rather than merged with 8px, and treating YAML `pill` 40 as a YAML step rather than converted into a harvested pill, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

### Elevation

The following elevation reading, including most-captured-public-controls-report-no-shadow, one-reusable-observation-is-the-white-public-search-dialog, layered-low-opacity-ink-shadows-separate-it-from-the-backdrop, and dialog-specific-depth-not-a-general-card-or-floating-panel-scale, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Most captured public controls report no shadow. The one reusable elevation observation is the white public search dialog. YAML `tokens.shadow.dialog`:

`rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(23, 24, 28, 0.12) 0px 2px 4px 0px, rgba(23, 24, 28, 0.09) 0px 4px 28px 0px, rgba(23, 24, 28, 0.04) 0px 4px 16px 0px, rgba(23, 24, 28, 0.04) 0px 60px 50px 0px, rgba(23, 24, 28, 0.1) 0px 20px 32px 0px, rgba(23, 24, 28, 0.1) 0px 0px 0px 0px`

That string is a dialog-specific depth value, not a general card or floating-panel scale.

### Motion

Treating the nine interaction captures as menu-and-dialog-opens-only, treating the absence of a measured duration, easing curve, or reduced-motion rule as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior, and treating static-component-geometry-remains-valid / unobserved-animated-states-omitted-rather-than-invented as source-stated rather than computed CSS, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. The bundle records nine interaction captures across menu and dialog opens, but it does not expose a measured duration, easing curve, or reduced-motion rule. No motion token is established. Static component geometry remains valid; unobserved animated states are omitted rather than invented.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-computed-Roboto-Arial-Noto-Sans-KR-stack-classified-as-system, 1161-Roboto-uses-not-a-brand-family-token, no-official-named-product-or-brand-typeface, Brand-Center-authenticated-with-no-downloadable-font-asset, declared-only-Noto-and-swiper-icons-with-zero-visible-uses, third-party-Roboto-Apache-2.0-and-Noto-OFL-not-a-China-Airlines-brand-asset-grant, and unobserved-authenticated-mobile-documentation-cabin-typography-absent-rather-than-inferred, are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed public-web use | Visible Korean public-site samples resolve to `Roboto, Arial, "Noto Sans KR", sans-serif`; the bundle reports 1,161 Roboto uses but classifies the family as `system`, explicitly describing it as an operating-system stack. Measured sizes, weights, line heights, and tracking are retained, but no `tokens.typography.family.ui` is emitted and no fallback is presented as a China Airlines typeface. Token extraction is `live-extract`. |
| Official product-use | No official announcement in the reviewed sources says that a named typeface is the airline’s product or brand typeface. None is promoted. |
| Official distributed brand asset | The official Brand Center is an authenticated asset portal in the reviewed session; no downloadable or inspectable font asset was available. It contributes no font claim. |
| Declared-only | Noto Sans JP, KR, SC, TC, Thai, and swiper-icons have supplied `@font-face` declarations but zero visible uses. They remain declared-only. Noto’s OFL and Roboto’s Apache-2.0 sources describe third-party font licensing, not a grant of China Airlines brand assets. |
| Unobserved domains | No authenticated product app, mobile app, documentation system, or cabin UI was captured; their typography is absent rather than inferred. |

### Family

- **Current visible computed stack:** `Roboto, Arial, "Noto Sans KR", sans-serif`
- **Bundle classification:** `system` (operating-system stack). No YAML `family.ui` / `tokens.typography.family.ui`.

Treating that stack as a live computed surface-use record rather than a proprietary China Airlines UI font, treating do-not-represent-Roboto-Arial-or-a-system-fallback-as-the-brand-face as a reconstruction of source §7 rather than a separately published font specification, and treating do-not-name-or-substitute-a-China-Airlines-font as a reconstruction of source §9 rather than a separately published font specification, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Do not represent Roboto, Arial, or a system fallback as China Airlines’ proprietary UI font. Do not name or substitute a China Airlines font.

### Type roles

YAML `lineHeight` values are 28, 28, and 56. Those YAML numbers are not unitless ratios below 3; the body table records them as 28px and 56px. YAML `tracking` values are 0.16, 0.16, and 0. Treating YAML 28 / 56 as the recorded line-height numbers rather than converting them into a ratio, treating body 28px / 56px as size-local companions rather than a rewrite of those YAML numbers, treating YAML tracking 0.16 as unmerged from body `0.16px`, and treating family-unresolved-as-a-brand-fact as a reason not to fill a Font column with Roboto, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

| Role | Size | Weight | Line height | Tracking | YAML use | Boundary |
|---|---:|---:|---:|---:|---|---|
| Body/list text | 16px | 400 | 28px (YAML 28) | 0.16px (YAML 0.16) | Measured public-web body and list text; no brand family token | Public web only; family unresolved as a brand fact |
| Primary action | 16px | 500 | 28px (YAML 28) | 0.16px (YAML 0.16) | Measured public-web primary-action text; no brand family token | `home::[data-omd-capture="99"]` |
| Section heading | 40px | 500 | 56px (YAML 56) | 0px (YAML 0) | Measured public-web section heading; no brand family token | Observed public-web heading cluster |

### Assets

Treating catalog logo metadata as a Google favicon lookup (`https://www.google.com/s2/favicons?domain=www.china-airlines.com&sz=128`), not a captured China Airlines mark file, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

Treating cabin persimmon / landscape / sapphire references as official design-story narrative rather than downloadable public-web assets, treating culturally-grounded-imagery-only-with-project-rights as a reconstruction of source §9 / §12 rather than a separately published asset specification, and treating do-not-replace-observed-public-web-surfaces-with-invented-brand-color-decoration-or-reproduced-airline-cabin-assets as that same reconstruction rather than a separately published asset specification, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Do not replace observed public-web surfaces with invented brand-color decoration or reproduced airline cabin assets.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, treating hex-and-geometry in the harvested components as remaining source-stated, treating the §14 rows below as that source-stated contract rather than computed CSS, treating a-component-as-included-only-where-the-packet-provides-selector-surface-backed-default-geometry, and treating states-summaries-as-observed-menu-dialog-state-that-do-not-imply-hover-focus-pressed-disabled-error-or-motion, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. All component values are measured on the supplied 1440×900 public passenger and corporate surfaces. A component is included only where the packet provides a selector/surface-backed default geometry. The `states` summaries report only observed menu/dialog state; they do not imply hover, focus, pressed, disabled, error, or motion values unless stated.

Characterizations in the following table such as “Preserve the measured selected option only; no hover/focus claim,” “Preserve shell geometry and shadow only,” “Omit,” and “Do not generalize a disabled treatment” are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification.

| Category | Evidence | Guidance boundary |
|---|---|---|
| Menu open | Observed on all three captured public surfaces | Preserve the measured selected option only; no hover/focus claim. |
| Dialog open | Observed on all three captured public surfaces | Preserve shell geometry and shadow only. |
| Empty | Not captured | Omit. |
| Loading | Not captured | Omit. |
| Error | Not captured | Omit. |
| Success | Not captured | Omit. |
| Disabled | A separate circular control was captured disabled, but it is not a promoted component variant | Do not generalize a disabled treatment. |
| Skeleton | Not captured | Omit. |

Only menu expansion and a search-dialog opening were observed in the supplied public-web capture. No empty, loading, success, error, form-validation, or transactional recovery state is established.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating a generic Focus observation as not `focus-visible` evidence, treating loading/error/success as following product role rather than primitive kind, and treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A generic Focus observation is not `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating Search-dialog YAML-type-dialog-with-kind-and-map-omitted, and treating the separate-circular-disabled-control as not a promoted component variant, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. The Search dialog keeps `Type: dialog` and verified geometry, shadow, and public search-dialog-shell use. Kind and a state-applicability map are omitted because dialog internals being interactive, and an observed dialog-open shell, are not evidence that the dialog surface itself has hover, disabled, or loading. YAML `tokens.components` records Search dialog as `type: dialog`. A separate circular control that was captured disabled is not a promoted component variant; no circular-action component is declared.

### Primary blue action

- Role: primary blue public-web action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#23569d`
- Text: `#ffffff`
- Radius: `8px`
- Padding: `0px 20px`
- Height: `56px`
- Font: `16px / 500` measured metrics; family is not a brand token
- Use: YAML Primary blue public-web action; `home::[data-omd-capture="99"]`
- Observed: default observed on all three captured public surfaces; no altered pseudo-state captured
- Field note: The following unmerged-field reading, including this-fill-not-bright-circular-not-booking-accent, on-fill-white-not-ink, 8px-not-20px-dialog-not-YAML-pill-40, 56px-not-48px-option-not-64px-booking, and default-only-not-hover, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.
- `#23569d` is this control’s fill and catalog `primary_color`. It is not bright blue `#1d6dcd` and not booking accent `#d81159`. `#ffffff` is this control’s on-fill label (on-primary), not ink `#17181c`. Radius 8px is this control; it is not dialog 20px and not YAML `pill` 40. Height 56px is this action; it is not selected-option 48px and not the 64px local booking accent. Treating the captured default as not a hover treatment and not keyboard-focus evidence is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on all three public surfaces as the primary blue action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled is a separate circular control, not this variant; that treatment is not copied here |

Loading, error, and success applicability are omitted. Source names this control as the primary blue public-web action and records selector `home::[data-omd-capture="99"]`; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Selected menu option

- Role: selected option button in the public booking/menu control
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#23569d`
- Radius: `8px`
- Padding: `0px 12px`
- Height: `48px`
- Font: `16px / 500` measured metrics; family is not a brand token
- Use: YAML Selected option button in the captured public booking/menu control; `home::[data-omd-capture="88"]`
- Observed: expanded menu and selected option state observed (`aria-selected=true`)
- Field note: The following unmerged-field reading, including this-fill-as-the-same-hex-as-canvas-rather-than-a-second-paint, selected-text-not-on-primary-white, 48px-not-56px, `0px 12px`-not-`0px 20px`, Type-button-not-invented-as-option, and named-selected-not-hover, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.
- `#ffffff` is this control’s fill; it shares the canvas hex and is not a second paint. `#23569d` is this control’s selected label, not on-primary white. Height 48px is this option; it is not the 56px primary action. YAML `type` is `button`; `role=option` in the capture is not converted into a YAML type.
- The selected appearance (`aria-selected=true`) is a named selected observation, not hover. Treating that appearance as not keyboard-focus evidence is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured selected option (`aria-selected=true`) in the public booking/menu control |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A menu option can be unavailable; visual treatment omitted |
| loading | not-applicable | A selected menu option records a choice; the option itself does not enter a loading state |
| error | not-applicable | Option meaning is selected versus resting, not a request or validation failure on the option |
| success | not-applicable | Option meaning is selection, not action-outcome confirmation |

Additional observed named state: expanded menu and selected option (`aria-selected=true`). Treating that appearance as a named selected observation, not hover and not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification.

### Search dialog

- Role: public search dialog shell
- Type: dialog
- Kind: omitted. Treating dialog-internals-being-interactive-as-not-evidence-that-the-dialog-surface-itself-has-hover-disabled-or-loading, so no `Kind: interactive` confirmation and no §4.4 state-applicability map, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. The source records type, geometry, shadow, default shell, and dialog-open.
- Anatomy: dialog shell
- Background: `#ffffff`
- Text: `#000000`
- Radius: `20px`
- Shadow: `rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(0, 0, 0, 0) 0px 0px 0px 0px, rgba(23, 24, 28, 0.12) 0px 2px 4px 0px, rgba(23, 24, 28, 0.09) 0px 4px 28px 0px, rgba(23, 24, 28, 0.04) 0px 4px 16px 0px, rgba(23, 24, 28, 0.04) 0px 60px 50px 0px, rgba(23, 24, 28, 0.1) 0px 20px 32px 0px, rgba(23, 24, 28, 0.1) 0px 0px 0px 0px`
- Use: YAML Public search dialog shell; `home::div[role=dialog]`
- Observed: default shell and dialog-open state observed
- Field note: The following unmerged-field reading, including this-fill-as-the-same-hex-as-canvas-rather-than-a-second-paint, dialog-text-`#000000`-not-ink-`#17181c`, 20px-not-8px-not-YAML-pill-40, shadow-as-dialog-specific-not-a-card-scale, and Type-dialog-with-kind-map-omitted, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. `#ffffff` is this shell’s fill. `#000000` is this shell’s text field, not ink `#17181c`. Radius 20px is this dialog; it is not control 8px and not YAML `pill` 40. The layered ink shadow is this dialog’s field, not a general elevation token for unlisted cards.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing action-x 20, menu-end 24, dialog-viewport 40. Harvested heights: 56px primary blue action, 48px selected menu option, 40px captured dark menu row, 64px local booking accent. 8px radius on public-web controls; 20px on the search dialog.

The following layout readings — public-task-controls, menu-rhythm, modal-restraint, 24px-end-spacing-as-observed-local-metric-not-a-universal-grid, and 1440×900-desktop-capture-not-a-cross-viewport-specification — are a derived editorial implementation inference from the verified surfaces; they are not China Airlines-authored or a separately published UI specification.

- **Public task controls:** the blue action uses 20px horizontal padding at 56px height; selected menu options use 12px horizontal padding at 48px height.
- **Menu rhythm:** the captured dark menu item uses `6px 24px 6px 0px` padding and a 40px row height. Its 24px end spacing is an observed local metric, not a universal grid.
- **Modal restraint:** the default search dialog uses a 20px radius. The surrounding modal viewport carries 40px padding in the desktop capture.

The supplied bundle records a 1440×900 desktop viewport only. It does not establish breakpoints, touch targets, mobile navigation, responsive card behavior, or dialog resizing rules. Treating the following measurement-boundary reading — official-product-site-is-global-and-likely-responsive-as-reconstruction-inference-not-a-breakpoint-token, 56px-48px-40px-64px-as-desktop-capture-measurements-not-cross-viewport-specifications, and those-values-absent-at-the-smallest-unresolved-boundary — as editorial rather than a responsive contract, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. The official product site is global and likely responsive, but those values are absent at the smallest unresolved boundary. The 56px, 48px, 40px, and 64px figures are desktop-capture measurements, not a cross-viewport specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating “official current language is aspirational but practical,” cabin-design-story-adds-warmth-welcome-cultural-depth-and-a-sense-of-journey, “this is public brand direction, not a documented transactional-error-copy specification,” the tone-table labels Travel-task / Welcome / Brand-narrative, and the tone-table directions Be-concise-clear-and-reliable / Offer-warmth-without-becoming-casual-or-decorative / Connect-travel-with-Taiwan-culture-and-thoughtful-service as reconstruction readings rather than a China Airlines microcopy guide, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. Official current language is aspirational but practical: safe, reliable, efficient service connects people worldwide, while the mission centers on wonderful moments through flying. The cabin-design story adds warmth, welcome, cultural depth, and a sense of journey. This is public brand direction, not a documented transactional-error-copy specification.

| Context | Direction |
|---|---|
| Travel task | Be concise, clear, and reliable. |
| Welcome | Offer warmth without becoming casual or decorative. |
| Brand narrative | Connect travel with Taiwan, culture, and thoughtful service. |

**Official wording samples** (official mission, vision, and value):

- *“Create more wonderful moments through flying.”* — official mission.
- *“Leading Asia-Pacific, Flying Worldwide.”* — official vision.
- *“Believe we can always do better.”* — official value.

Treating §14 rows as the state contract rather than extra voice samples, treating official-wording-samples as official-mission-vision-value-quotes-not-generated-product-copy, treating captured `kr/ko` routes as the recorded public surfaces rather than a claimed product-locale specification, treating official-English-Design-Story-and-sustainability-pages as narrative-sources-not-a-claimed-product-locale-specification, and not promoting synthetic transactional-error or support-copy samples, is a derived editorial implementation inference from the verified surfaces; it is not China Airlines-authored or a separately published UI specification. The captured public surfaces are the Korean public passenger and corporate routes (`kr/ko`). Official English Design Story and sustainability pages in the packet are narrative sources, not a claimed product-locale specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- error and success color roles (source: the supplied capture does not establish one)
- hover, focus, pressed, disabled, validation, and accessibility visual treatments inferred from class names or unmeasured CSS
- empty, loading, error, success, form-validation, transactional-recovery, and skeleton visual treatments
- Primary blue action loading·error·success applicability (exact request/outcome unresolved)
- Search dialog kind and state-applicability map
- `focus-visible` visual treatments; a generic Focus observation is a different evidence type
- disabled treatment from the separate circular control, generalized onto promoted components
- brand typeface / `tokens.typography.family.ui`
- YAML `rounded.pill` 40 harvested onto a promoted control
- breakpoints, touch targets, mobile navigation, responsive card behavior, and dialog resizing rules
- authenticated product, mobile app, documentation, and cabin typography and tokens
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
- transactional-error-copy specification beyond the official mission / vision / value samples
