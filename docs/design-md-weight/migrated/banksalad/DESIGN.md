# Banksalad Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Banksalad is a Korean data-driven financial platform that helps people compare products, understand credit and spending, and connect finance with a broader health-asset proposition. Catalog homepage identity is `https://www.banksalad.com`. Official design-collaboration material = Banksalad Product Language (BPL), with public GitHub org metadata `ds.type: brand` at `https://github.com/banksalad` (styleguide repos and BPL reference material).

Treating the following five URLs as the named public-web evidence domains of this reconstruction, treating the two BPL articles as official-doc process evidence rather than extra product-surface captures, and treating those BPL publications as explaining how Banksalad names collaboration rather than as current public-web color or radius tokens, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. This contract covers five current first-party web surfaces from the 2026-07-12 packet: home `https://www.banksalad.com/`; contents directory `https://www.banksalad.com/contents`; public loan interest-rate flow `https://www.banksalad.com/loan/interest-rate-cut`; customer-safety `https://www.banksalad.com/customer-safety`; and one public card-product detail `https://www.banksalad.com/product/cards/CARD002319`. Official BPL publications `https://blog.banksalad.com/tech/banksalad-product-language-design/` and `https://blog.banksalad.com/tech/banksalad-product-language-ios/` explain how Banksalad names collaboration, not current public-web color or radius tokens.

Source token note: five current first-party web surfaces. Pretendard is loaded and used; `swiper-icons` is declared-only. Legacy `#04c584`, BM JUA, 2px-default components, inputs, charts, and modal claims were not promoted. Treating that note as a register split — `#13bd7e` is catalog `primary_color` and the repeated current accent, `#06a96c` is the harvested full-width loan-action fill rather than a second identity green, BPL is process history rather than a 2020 token sheet for these five pages, and the 2026-05 CSS-bundle snapshot (`#04c584`, `#10df99`, 2px-default, inputs, charts, modals) is superseded rather than current — is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

The following evidence-domain readings — values stay attached to the establishing surface; a home marketing value is not a proxy for the loan-flow action; a BPL process sentence is not a current public-web token; a 2026-05 CSS-bundle value is not a 2026-07-12 capture value — are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. Color, type, and component values below stay attached to the surface that established them. A home marketing value is not a proxy for the loan-flow action. A BPL process sentence is not a current public-web token. A 2026-05 CSS-bundle value is not a 2026-07-12 capture value.

The following informational-rather-than-decorative, dense-but-readable, comparison-and-next-steps-visible, role-specific-geometry, most-repeated-accent, and consequential-loan-action-deeper-green readings are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. The source reads the current public web surfaces as informational rather than decorative: a white `#ffffff` canvas, dense but readable Pretendard typography, near-black `#111111` content, and green actions that keep comparison and next steps visible. The most repeated current accent is `#13bd7e`; a consequential full-width loan action uses the deeper `#06a96c`. Current geometry is role-specific rather than one sharp-corner default: contents directory 6px green chip, public loan action 16px radius, customer-safety cards 24px corners with a `#f0f2f5` border, carousel indicator as a full pill, plain lists and disclosure rows square (`0px`). Typography is Pretendard across the five inspected surfaces; the collector matched loaded Pretendard faces to 603 visible elements. `swiper-icons` was declared but not used for visible text. BM JUA was not declared or visibly used in this fresh capture.

Treating official BPL history and the present five-surface implementation as two evidence layers, treating neither layer as a substitute for inaccessible native-app evidence, treating the BPL sentences as verified process principles that do not prove 2020 colors, radius, or typography remain current tokens, and treating the present web implementation as built from white surfaces, Pretendard, green actions, explicit comparison copy, and role-specific geometry, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. The official BPL publications explain how this product is made. Banksalad describes Product Language as broader than a visual design system: designers and platform engineers share names, abstraction boundaries, component structures, implementation guidance, and examples. BPL emerged alongside a major service redesign, moved collaborative design work to Figma, established ground rules, and maintained versioned libraries and sample applications so platform differences could be reviewed rather than hidden. The BPL implementation article states “Communication cost is most expensive. Code and Show first, argue after that.” These are verified process principles. They do not prove that colors, radius, or typography observed in 2020 remain current tokens; the current five-surface measurements are reconciled independently. The present web implementation is built from white surfaces, Pretendard, green actions, explicit comparison copy, and role-specific geometry.

Treating the following live home statements as the present product narrative, and not using third-party founder mythology, funding figures, or fabricated competitive comparisons as interface tokens, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. Banksalad's current first-party web presence presents a data-based financial platform spanning product comparison, credit and loan guidance, financial content, customer safety, and a wider health-asset direction. The current home page describes a large comparison inventory and a recommendation engine designed to help people choose favorable products.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=5 lang=en -->
### Primary tasks

- Compare cards, loans, deposits, or insurance and read product conditions.
- Check whether an interest-rate reduction request may be available.
- Read benefits, disclosures, and product metadata on a public card-product detail.
- Find fraud, identity-theft, voice-phishing, or mistaken-transfer guidance on customer-safety.
- Use the financial contents directory to understand a decision before entering a product flow.
<!-- design-md:claim-end -->

### Audience

Restricting Audience so that no individual personas are promoted, tying observable work only to the five primary tasks, and treating names, ages, quotes, income, behavior frequency, and authenticated-app habits as not Audience and not primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. Banksalad does not publish validated demographic personas in the sources used for this pass. Source §13 records observable jobs, not invented people. Names, ages, quotes, income, behavior frequency, and authenticated-app habits are not Audience and are not primary tasks.

### Distinctive traits

Treating `#13bd7e` as catalog `primary_color` and repeated accent rather than the loan-action fill, treating `#06a96c` as the full-width loan action rather than a second identity green, treating `#606874` as unmerged from `#555c68`, treating 6px / 16px / 24px / YAML `full` 9999 / `0px` as local harvested geometry rather than one radius scale, treating Pretendard’s 603 visible uses as live-surface evidence rather than a BPL font token, treating 8px, 12px, 16px, and 24px as recurring useful spacing values rather than a full spacing ladder, and treating inputs, charts, dialogs, and authenticated app states as unpromoted rather than inferred from the captured buttons, cards, and list/disclosure rows, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

- Loaded Pretendard across all five current public web surfaces (603 visible elements)
- Current accent `#13bd7e`; deeper action green `#06a96c`
- White `#ffffff` canvas with `#111111` foreground, `#555c68` secondary text, `#606874` explanatory copy in loan and customer-safety contexts, and `#9fa4b0` muted metadata
- Role-specific 6px / 16px / 24px / full-pill geometry; disclosure rows remain `0px`
- 8px, 12px, 16px, and 24px as recurring useful spacing values
- Public product evidence covers buttons, cards, and list/disclosure rows; inputs, charts, dialogs, and authenticated app states remain unpromoted

### Principles

These five items, including each stem and each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. Source §12 records them as editorial paraphrases of BPL publications. The only exact first-party quote kept here is “Communication cost is most expensive. Code and Show first, argue after that.”

1. **Share one product language.** BPL exists so design and platform teams name and structure the same UI concepts consistently.
   *UI implication:* component names and anatomy should map across design and implementation rather than being reinvented per platform.
2. **Code and show before abstract debate.** The official BPL article uses working prototypes to expose where teams agree or disagree. First-party quote: “Communication cost is most expensive. Code and Show first, argue after that.”
   *UI implication:* validate a component in a representative screen before promoting a generalized rule.
3. **Keep design and implementation structures aligned.** BPL describes matching Figma layout structure closely enough that a design change produces a corresponding implementation change.
   *UI implication:* avoid premature abstraction that obscures the authored component anatomy.
4. **Version the shared language.** The official design article describes fixed platform versions, update tracking, and component-list tooling.
   *UI implication:* reference claims need dates and surface provenance; old BPL values cannot silently become current web tokens.
5. **Test components alone and in realistic composition.** BPL used component examples, snapshot tests, and an integral sample app.
   *UI implication:* validate both the isolated spec and the assembled product flow.

Treating the following as a capture-bound application of the 2026-07-12 five-surface packet and YAML token note, with first-party BPL names marked separately from the remaining token-role rules (Pretendard as the only current visible text family on the five captured surfaces; `#13bd7e` as the repeated current accent rather than the loan-action fill; role-specific 6px / 16px / 24px / YAML `full` 9999 / `0px` geometry; BPL process principles kept separate from current public-web color, radius, and typography tokens; `#ffffff` canvas, `#111111` foreground, and the 8 / 12 / 16 / 24 spacing rhythm on the captured surfaces), is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

- First-party (BPL articles / GitHub org): Banksalad Product Language; `ds.type: brand`; GitHub org including styleguide repos and BPL reference material; quote “Communication cost is most expensive. Code and Show first, argue after that.”
- Use Pretendard as the only current visible text family on the five captured surfaces.
- Use `#13bd7e` as the repeated current accent; use `#06a96c` with `#ffffff` text for the full-width public loan action.
- Keep geometry role-specific: 6px chip, 16px loan action, 24px safety card, YAML `full` 9999 carousel pill, `0px` disclosure row.
- Keep BPL process principles separate from current public-web color, radius, and typography tokens.
- Keep `#ffffff` canvas, `#111111` foreground, and the 8 / 12 / 16 / 24 spacing rhythm on the captured surfaces.

### Avoid

The following items restate source-stated current-pass omissions and reverts. Grouping them here as capture-bound Avoid, including the unmerged-legacy reading that `#04c584` / `#10df99` / 2px-default are not current, the unobserved-state-not-invented reading, the BPL-2020-not-current-tokens reading, the unobserved-accent-face-not-Pretendard / BM-JUA-not-current reading, and the one-captured-surface-not-native-or-unlisted-authenticated reading, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

- Do not promote `#04c584`, `#10df99`, BM JUA, or 2px as current canonical primary, hover, display face, or default radius.
- Do not infer error, success, selected, hover, focus, pressed, disabled, authenticated-app, input, chart, or modal styling from this snapshot.
- Do not present BPL 2020 component values as current public-web tokens.
- Do not substitute an unobserved accent face for Pretendard; do not restore BM JUA as a current UI or marketing token.
- Do not treat one of the five captured surfaces as proof of native-app or unlisted authenticated behavior. Treating that one-surface-not-native-or-unlisted-authenticated limit as a capture-bound Avoid rule is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings — catalog `#13bd7e` is not loan-action `#06a96c` and not superseded `#04c584`; `#06a96c` is not a second identity green; Foreground `#111111` is not Secondary `#555c68`, not Neutral `#606874`, and not Muted `#9fa4b0`; `#606874` is body-only and not a YAML color; Line `#f0f2f5` is not Canvas; transparent disclosure and utility fills are not a second canvas token; previous `#04c584` and `#10df99` are not current canonical tokens — are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. Catalog `primary_color` `#13bd7e` is the repeated current accent (green text and border on home, contents, loan, and card-detail). It is not loan-action `#06a96c` and not superseded `#04c584`. `#06a96c` is the full-width loan-flow action background, paired with `#ffffff` text; it is not a second identity green. Foreground `#111111` is dominant heading, body, and list text; it is not Secondary `#555c68`, not Neutral `#606874`, and not Muted `#9fa4b0`. `#606874` is body-only explanatory copy in loan and customer-safety contexts; it is not a YAML color and is not merged into Secondary. Line `#f0f2f5` is the observed 1px customer-safety card border; it is not Canvas. Transparent fills on disclosure and utility controls are those controls’ fields, not a second canvas token. Previous `#04c584` and `#10df99` were not observed in the five-surface capture and are not current canonical tokens.

- **Current accent / Primary** (`#13bd7e`): repeated green text and border color across home, contents, loan, and card-detail. YAML `primary`. Catalog `primary_color`.
- **Primary action** (`#06a96c`): full-width loan-flow action background, paired with `#ffffff` text. YAML `action`.
- **Foreground** (`#111111`): dominant heading, body, and list text across all five surfaces. YAML `foreground`.
- **Secondary** (`#555c68`): supporting copy on home, contents, and card detail. YAML `secondary`.
- **Neutral** (`#606874`): explanatory copy in loan and customer-safety contexts. Body §2 only; not a YAML color.
- **Muted** (`#9fa4b0`): product metadata and subdued actions on contents and card detail. YAML `muted`.
- **Canvas** (`#ffffff`): default current page and card surface. YAML `canvas`.
- **Line** (`#f0f2f5`): observed 1px customer-safety card border. YAML `line`.
- **Transparent**: most navigation, list, disclosure, and utility controls stay unfilled.

No current public chart, authenticated dashboard, input validation, warning, error, or success-state palette was safely captured. Those legacy groups are omitted rather than mapped to plausible greens, reds, or teals. Treating that omitted-rather-than-mapped-to-plausible-greens-reds-or-teals reading as a reconstruction boundary is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

### Spacing

YAML scale: sm 8, md 12, base 16, lg 24. Body §1 records 8px, 12px, 16px, and 24px as recurring useful spacing values. Harvested padding: Primary Loan Action `16px 20px`; Content Filter Chip `10px 12px`; Customer Safety Card `24px`; Product Disclosure Row `24px 20px`.

The following local-spacing readings — harvested padding remains captured-surface measurement; YAML sm/md/base/lg stays the YAML scale; source §5’s 4px-through-64px ladder is the current body spacing system after the footer, not a second YAML token — are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. Harvested padding remains captured-surface measurement. YAML sm 8 / md 12 / base 16 / lg 24 stays the YAML scale. Source §5, which follows the footer that reverted only `#04c584`, `#10df99`, BM JUA, 2px default radius, and uncaptured input/chart/modal/state claims, records the current body spacing system: base unit 4px; scale 4px, 8px, 12px, 16px, 20px, 24px, 32px, 40px, 48px, 64px; card internal padding 20px / 24px; table-row vertical padding 12px.

### Shape

YAML `rounded`: chip 6, action 16, card 24, full 9999.

Observed radii: Content Filter Chip 6px; Primary Loan Action 16px; Customer Safety Card 24px; carousel indicator as a full pill (YAML `full` 9999); Product Disclosure Row `0px`.

The following local-geometry readings — 6px / 16px / 24px / YAML `full` 9999 / `0px` remain local harvested geometry, not a universal radius for every unlisted control; the earlier 2px-dominant-current-system claim is a legacy CSS-bundle reading and is not promoted; source §5’s 4px / 8px / 10–12px / 30–41px / 50% radius roles remain current body after the footer and stay unmerged from those harvested values; 4px small-badge radius is not the 4px spacing-ladder step; 8px soft-marketing is not the snapshot Soft Variant card; 30–41px filter-pill / tag / avatar is not snapshot Status Pill 41px; 50% avatar / icon-backdrop is not YAML `full` 9999 — are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. 6px / 16px / 24px / YAML `full` 9999 / `0px` remain local harvested geometry, not a universal radius for every unlisted control. The earlier claim that 2px was the dominant current system came from a legacy CSS bundle and is not promoted in this pass.

Current body radius roles (source §5; 2px default remains reverted):

- 4px: Small badges, inline pills
- 8px: Soft marketing cards (rare)
- 10–12px: Promotional banner corners (rare)
- 30–41px: Filter pills, tag chips, avatar circles
- 50%: Avatars, icon backdrops

### Elevation

YAML `tokens.shadow.flat` is `none`. The harvested Customer Safety Card uses a `1px solid #f0f2f5` border rather than a drop shadow.

The following border-not-elevation readings — flat `none` and the safety-card hairline support a flat treatment for these observed elements only, not a general elevation ladder; the source §6 Soft/Standard/Elevated/Deep/Modal/Inset/Halo shadow ladder is not current elevation — are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. Flat `none` and the safety-card hairline support a flat treatment for these observed elements only. Source §6 still records a Soft/Standard/Elevated/Deep/Modal/Inset/Halo shadow ladder (`0 1px 1px rgba(0,0,0,.08)`, `0 2px 5px rgba(0,0,0,.12)`, `0 4px 9px rgba(0,0,0,.15)`, `0px 2px 8px rgba(0,0,0,.1)`, `0 17px 50px rgba(0,0,0,.19)`, `inset 0 1px 1px rgba(0,0,0,.12)`, `0 0 2px rgba(0,0,0,.26)`) from the earlier CSS-bundle snapshot; that ladder is not promoted as current elevation.

### Motion

One current public accordion panel exposed a `350ms` height/opacity transition using `cubic-bezier(0.25, 0.1, 0.25, 1)`. No broader duration scale, spring policy, chart animation, score count-up, or reduced-motion implementation was verified in this capture.

Treating that accordion tuple as one panel’s computed observation rather than a universal motion scale, and treating source §15’s superseded duration/easing/signature-motion tables as not current tokens, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

Do not promote that accordion observation as a page-wide motion token, and do not promote any additional easing curve, animation name, transition property, duration, or reduced-motion behavior until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve beyond that one accordion observation remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings — BPL publications are official product-language material but not a current universal font token; native-app-only typography stays a captured-scope boundary rather than a promoted family — are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | BPL publications establish an official product-language system, but do not publish a current universal font token in the inspected text. |
| Live surface-use | Five public web surfaces loaded and visibly used Pretendard on 603 elements. |
| Official distributed asset | No Banksalad-exclusive UI font asset is promoted in this reference. |
| Declared-only | `swiper-icons` was declared with zero visible text usage. |
| Captured scope unresolved | Native-app-only typography remains a captured-scope boundary without an inspectable current surface. It is not promoted as a family. |

Specimen availability requires a loadable font source and is separate from family truth.

### Family

- **Primary:** `Pretendard, -apple-system, BlinkMacSystemFont, system-ui, "Segoe UI", Roboto, "Apple SD Gothic Neo", "Noto Sans KR", "Malgun Gothic", sans-serif`
- Loaded sources were observed for Pretendard Regular, Medium, SemiBold, Bold, and ExtraBold subsets.
- BM JUA was not declared, loaded, or visibly used in this fresh capture and is omitted.

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. Do not present `-apple-system`, `BlinkMacSystemFont`, `system-ui`, `"Segoe UI"`, Roboto, `"Apple SD Gothic Neo"`, `"Noto Sans KR"`, or `"Malgun Gothic"` as Pretendard. Do not replace Pretendard with those fallbacks or with BM JUA.

The following type-rule readings (Pretendard as the only current visible text family; 400 carries reading / 700 carries decisions; tracking is subtle at product sizes; surface evidence stays local) are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. Current body/list copy is commonly 400, while headings and the primary loan action are 700. Current 14–18px roles use approximately `-0.06px` to `-0.09px`; no `-1px` display token is promoted. Authenticated app typography and native-only financial amount roles remain unresolved.

### Type roles

Verified YAML line-height values are the unitless ratios `1.4`, `1.5`, `1.625`, and `1.43`. The following ratio-versus-size-local and unmerged-role readings are a derived editorial implementation inference from the verified surfaces; they are not Banksalad-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. YAML Heading tracking `0` is not body Body tracking (Body YAML has no tracking field). YAML Caption is 14px / 500 / `1.43` / `-0.07` (“Product metadata and helper text”); body-table Metadata matches that 14px role; body-table Caption at 12px / 500–700 / 18px / `-0.06px` is a denser observed role and is not YAML Caption. Supporting Heading 18px is body-table only and is not YAML Heading 20px.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking | Use |
|---|---|---:|---:|---:|---|---|---|
| Product Heading | Pretendard | 20px | 700 | 1.4 | 28px at this size | YAML `0` / body `normal` | Product and safety section headings |
| Supporting Heading | Pretendard | 18px | 700 | — | 26px | `-0.09px` | Compact product heading (body table only) |
| Body | Pretendard | 16px | 400 | 1.5 | 24px at this size | body `normal` (YAML has no tracking field) | Shared product body and list content |
| Prominent Label | Pretendard | 16px | 700 | 1.625 | 26px at this size | YAML `-0.08` / body `-0.08px` | Prominent product labels and actions |
| Metadata | Pretendard | 14px | 500 | 1.43 | 20px at this size | YAML `-0.07` / body `-0.07px` | Product metadata and helper text |
| Caption | Pretendard | 12px | 500–700 | — | 18px | `-0.06px` | Dense metadata where observed (body table only) |

### Assets

Treating catalog logo metadata as a Google s2 identity record in the source ledger, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

Treating verified product, comparison, and safety content as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Treating the following source state contract as preserved here while the catalog graph is not adopted, treating that paragraph as the current state contract, and treating the source’s superseded synthetic empty/loading/error/success/skeleton/disabled/pressed table as not verified product facts, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted: the collector recorded default render states only and performed zero safe interaction expansions. No canonical empty, loading, error, success, disabled, hover, focus, or pressed treatment is promoted. The loan accordion was captured in its rendered state, but this does not establish a complete product-state system.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the superseded §14 table. This is not a complete state-coverage claim.

The source never records `focus-visible` or a generic Focus capture. The `focus-visible` row does not carry a colour. A later generic Focus observation would not be `focus-visible` evidence.

Customer Safety Card has no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Primary Loan Action

- Role: full-width primary action in the public interest-rate flow
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#06a96c`
- Text: `#ffffff`
- Radius: 16px
- Padding: 16px 20px
- Height: 56px
- Font: 16px / 700 / Pretendard
- Use: Full-width primary action in the public interest-rate flow
- YAML `states`: default captured; no safe active interaction expansion
- Observed: default only
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. `#06a96c` is this control’s fill and Primary action. It is not Current accent `#13bd7e` and not superseded `#04c584`. YAML `states` is this component’s legacy metadata field; it is not merged into Use.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public interest-rate flow |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary loan action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a full-width primary action in the public interest-rate flow; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the superseded §14 table.

### Content Filter Chip

- Role: category and filter action on the public contents directory
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `rgba(19,189,126,0.15)` (body also writes `rgba(19, 189, 126, 0.15)`)
- Text: `#13bd7e`
- Radius: 6px
- Padding: 10px 12px
- Font: 16px / 500 / Pretendard
- Use: Content-directory category or filter action
- YAML `states`: default captured; no safe active interaction expansion
- Observed: default only
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. `#13bd7e` and the `rgba(19,189,126,0.15)` wash are this control’s fields, not a page-wide green fill and not loan-action `#06a96c`. YAML `states` is this component’s legacy metadata field; it is not merged into Use.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public contents directory |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A filter chip can be unavailable; visual treatment omitted |
| loading | not-applicable | A contents filter chip selects a category; the chip itself does not enter a loading state |
| error | not-applicable | Chip meaning is selected versus resting, not a request or validation failure on the chip |
| success | not-applicable | Chip meaning is category selection, not action-outcome confirmation |

### Customer Safety Card

- Role: customer-safety action and guidance card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Border: 1px solid `#f0f2f5`
- Radius: 24px
- Padding: 24px
- Use: Customer-safety action and guidance card
- Observed: default only
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. `#f0f2f5` is this card’s border and Line. `#ffffff` fill is this card’s field and Canvas. 24px is this card’s harvested radius, not the loan-action 16px and not the chip 6px.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Product Disclosure Row

- Role: expandable disclosure row on a public card product detail
- Kind: interactive
- Type: listItem
- Anatomy: label row
- Background: transparent
- Text: `#111111`
- Radius: 0px
- Padding: 24px 20px
- Font: 16px / 400 / Pretendard
- Use: Expandable disclosure row on a public card product detail
- Observed: default only
- Field note: The following unmerged-role reading, including that this card-detail row is not identified as the loan-flow accordion that exposed the `350ms` height/opacity transition, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. Transparent fill is this row’s field, not Canvas. `#111111` is this row’s label and Foreground. `0px` is this row’s harvested radius, not chip 6px, loan-action 16px, or safety-card 24px. This card-detail row is not identified as the loan-flow accordion that exposed the `350ms` height/opacity transition.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public card-product detail |
| hover | applicable | Pointer-web disclosure row; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A disclosure row can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as an expandable disclosure row; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the superseded §14 table.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is sm 8, md 12, base 16, lg 24. Harvested measurements: Primary Loan Action height 56px and padding `16px 20px`; Content Filter Chip padding `10px 12px`; Customer Safety Card padding 24px and radius 24px; Product Disclosure Row padding `24px 20px`. Product Heading is 20px; Supporting Heading is 18px.

Source §5 and §8 follow the footer that reverted only `#04c584`, `#10df99`, BM JUA, 2px default radius, and uncaptured input/chart/modal/state claims. Treating the remaining §5/§8 values below as current body layout, treating density-as-brand and `#ffffff` / `#fbfbfb` section-rhythm as current body whitespace, treating 4px / 8px / 10–12px / 30–41px / 50% as current body radius roles unmerged from YAML harvested radii, treating `#fbfbfb` as this section-rhythm fill rather than Canvas `#ffffff` or a second YAML canvas token, treating the 56px / `16px 20px` / `10px 12px` / 24px / `24px 20px` figures as desktop-capture measurements on the named controls rather than cross-viewport specifications, and treating 2px-default radius as still reverted, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

Current body spacing system (source §5):

- Base unit: 4px
- Scale: 4px, 8px, 12px, 16px, 20px, 24px, 32px, 40px, 48px, 64px
- Card internal padding: 20px / 24px
- Table-row vertical padding: 12px

Current body grid and container (source §5):

- Marketing/landing: max content width ~1080–1200px, centered
- App / data surfaces: full-width with 16–24px gutters; tables go edge-to-edge on mobile
- Card-recommendation lists: single-column on mobile, 2–3 column grid on desktop
- Charts: full-width within their card container, never bleed outside

Current body grouping-gap (source §5): inside a comparison card, rows sit on 12px vertical gaps; between cards, 24–32px. The hierarchy of grouping is communicated through gap-size.

Current body whitespace (source §5):

- Density is the brand. Where Toss says "breathing room for money", Banksalad says "more facts visible without scrolling". A balance, a comparison rate, an interest rate, and three filter chips can coexist in one card.
- Section rhythm by surface fill. White (`#ffffff`) and off-white (`#fbfbfb`) sections alternate quietly — not for drama, but to chunk dense data into scannable bands. `#fbfbfb` is this section-rhythm fill, not Canvas `#ffffff`.

Current body radius roles (source §5; 2px default remains reverted; unmerged from YAML chip 6 / action 16 / card 24 / full 9999 / disclosure `0px`):

- 4px: Small badges, inline pills
- 8px: Soft marketing cards (rare)
- 10–12px: Promotional banner corners (rare)
- 30–41px: Filter pills, tag chips, avatar circles
- 50%: Avatars, icon backdrops

Current body §8 breakpoints:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <600px | Single column, full-width tables (horizontal scroll if needed), 16px gutters |
| Tablet | 600–1024px | 2-column card grids, 20px gutters |
| Desktop | 1024–1280px | Full marketing layout, 3-column feature grids |
| Large | >1280px | Centered content max-width ~1200px |

Current body collapsing (source §8): Hero 52px → 36px on mobile, weight 700 maintained; marketing 3-col feature grids → 2-col → single column; comparison tables maintain table form on tablet and switch to stacked cards on mobile; charts stay full-width and keep aspect ratio, with axis-label font 12px → 10px on mobile; filter chip rows horizontal-scroll on mobile.

Current body image behavior (source §8): card/product logos 24–40px, consistent square framing within rows; chart screenshots in marketing full-width on mobile, 2-col on desktop; decorative illustrations rare, locked to a single accent illustration per landing surface.

Touch-target record (source §8): buttons minimum 42px height (default), 56px for hero display CTAs; table rows minimum 44px; filter pill chips 28–32px height; input minimum 48px height; amount-input 56px. Input and amount-input heights stay with this §8 record; they are not promoted as captured input/chart/modal components.

2px as a default radius remains reverted with `#04c584`, `#10df99`, BM JUA, and uncaptured input/chart/modal/state claims. YAML current radii (chip 6 / action 16 / card 24 / full 9999 / disclosure `0px`) stay the harvested geometry.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-07-12)

The live strings below are source-stated. Treating the parenthetical characterizations (home positioning / comparison copy / concrete product action / risk plus next action) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

- “금융을 넘어 건강 자산까지” — current home positioning. *(verified: https://www.banksalad.com/)*
- “신용대출, 햇살론, 직장인 대출까지 한 번에 비교하세요” — comparison copy. *(verified: https://www.banksalad.com/contents)*
- “내 대출금리 조회” — concrete product action. *(verified: https://www.banksalad.com/contents)*
- “명의 도용 의심 도용사고·보이스피싱 신고 및 행동 요령을 확인하세요” — risk plus next action. *(verified: https://www.banksalad.com/customer-safety)*

Treating the superseded voice-hypothesis table (CTAs, section headlines, recommendations, financial figures, success, error, empty, legal, health-asset) as historical hypothesis and not as an official writing standard, and treating the superseded §14 empty/loading/error strings as not extra Observed samples, is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading — current public copy is direct, explanatory, and comparison-oriented; this supports a factual Korean product voice but does not establish invented notification, validation, or authenticated-app copy — is a derived editorial implementation inference from the verified surfaces; it is not Banksalad-authored or a separately published UI specification. It is not the Observed strings above.

Current public copy is direct, explanatory, and comparison-oriented. The home page connects financial and health assets; the contents directory names products, benefits, and comparison tasks explicitly; customer-safety copy gives the risk and the next action. This supports a factual Korean product voice, but it does not establish the invented notification, validation, or authenticated-app copy previously included in the source. No complete official voice-and-tone manual was found. Push notifications, authenticated errors, sentence-ending rules, and prohibited phrase lists remain unresolved.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- error, success, selected-filter, promotional, warning, chart, authenticated-dashboard, and input-validation colors
- hover, pressed, disabled, loading, error, success, empty, skeleton, dialog, toast, and form-error visual treatments
- loading, error, and success applicability fields on Primary Loan Action and Product Disclosure Row
- interactive kind and state-applicability map for Customer Safety Card
- reduced-motion behavior, and any duration beyond the one accordion `350ms` height/opacity / `cubic-bezier(0.25, 0.1, 0.25, 1)` observation — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
- native-app typography and native-only financial amount roles
- inputs, charts, dialogs, and authenticated-app components
- official voice-and-tone manual; push notifications; authenticated errors; sentence-ending rules; prohibited phrase lists
