# loom migration log

Source: `web/references/loom/DESIGN.md`
Sibling read (not the migration input): `web/references/loom/.verification.md`
Destination: `docs/design-md-weight/migrated/loom/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/loom/provenance.md`
Date: 2026-08-29
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines. `Primitive type` counts use Python `str.count` because backticks break `grep -oF`.

Source SHA-256 `c81e1ab4453dfbb2aa15e0973aa292c4107ddc0c1b7ba87f6d984f12e2cc3ab8` (`web/references/loom/DESIGN.md`). Sibling SHA-256 `4fb29794b95c44557c67d3f2713f2412df851fab8b6b082b17f147b4bc22b432` (`web/references/loom/.verification.md`). Worker-close portable DESIGN SHA-256 `b6375fee33f41311ec8664a4326b018b51c63cc006962a56a31f3f467e16bc79`. F3-audit portable DESIGN SHA-256 `e693b2ae50f38425c15065e87cf64e4cb0da50a19c33bc00604a213be1b4b943`. Meaning-review restoration SHA-256 `e568649ea50e547b5d0072cd4b50c67834bc336dc369780a310b222440d57db6`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + Primary CTA; `logo.type` / favicon slug 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# Loom Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. Catalog homepage `https://www.loom.com` is dual: DESIGN dest **4** + provenance dest **7** (`E2a`). `#1868db` is dual: DESIGN dest **13** + provenance dest **7** (`E2a`). The Google s2 favicon slug `https://www.google.com/s2/favicons?domain=loom.com&sz=128` is dual: DESIGN dest **1** + provenance dest **1** (`E2a`). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance; `live-extract` 옮김 → Foundations Motion (attribution boundary) | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. `live-extract` DESIGN dest **2** / provenance dest **4** (`E2a`). `2026-06-08` DESIGN dest **0** / provenance dest **13**. `components_harvested` DESIGN dest **0** / provenance dest **2**. `ds.type` is absent from the source and is not invented (provenance dest **1** records the absence, not a filled type). |
| YAML token note (pill CTA + illustration accents) | 옮김 → Foundations Accent + 분리 → provenance Identity | Dual: DESIGN Accent paragraph dest **1** of the note's coral/purple/deep pairing + provenance Token note (`E2a`). `#ff613d` DESIGN dest **5** + provenance dest **2**. `#bf63f3` DESIGN dest **5** + provenance dest **2**. `#48245d` DESIGN dest **5** + provenance dest **2**. |
| YAML `tokens.colors` (13 keys) | 옮김 → Foundations semantic color | Paths named in `DESIGN.md` Semantic color: `tokens.colors.primary` dest **4** · `tokens.colors.primary-hover` dest **1** · `tokens.colors.brand` dest **2** · `tokens.colors.canvas` dest **2** · `tokens.colors.surface-blue` dest **1** · `tokens.colors.foreground` dest **2** · `tokens.colors.body` dest **2** · `tokens.colors.muted` dest **1** · `tokens.colors.on-primary` dest **2** · `tokens.colors.ink` dest **2** · `tokens.colors.accent-coral` dest **1** · `tokens.colors.accent-purple` dest **1** · `tokens.colors.accent-deep` dest **1**. primary/brand stay two keys that share `#1868db`. canvas/on-primary stay two keys that share `#ffffff`. foreground/ink stay two keys that share `#101214`. `#0052cc` DESIGN dest **12** + provenance dest **5** (`E2a`). `#e9f2fe` DESIGN dest **9** + provenance dest **7** (`E2a`). `#292a2e` DESIGN dest **10** + provenance dest **6**. `#7d818a` DESIGN dest **3** + provenance dest **2**. `#000000` DESIGN dest **2** (Don't-list contrast only; not a heading token). |
| YAML `tokens.typography.family` (`display: Charlie Display` / `text: Charlie Text`) | 옮김 → Typography & Assets Family | `tokens.typography.family.display` dest **2** · `tokens.typography.family.text` dest **2**. The two keys stay two keys (`DESIGN.md` Family B2a). Charlie Display DESIGN dest **7**. Charlie Text DESIGN dest **13**. Fallback `sans-serif` remains a fallback. |
| YAML `tokens.typography` 7 roles | 옮김 → Typography & Assets Type roles | Unitless `1.03` dest **4** · `1.14` dest **2** · `1.27` dest **3** · `1.52` dest **3** · `1.5` dest **11** · `1.0` dest **9** stay ratios (A1a). Source §3 `1.00` dest **2** and `3.95rem` dest **2** kept beside YAML size `63`. YAML tracking `0` dest **3** beside `normal`. Source §3 Notes kept beside YAML `use` (A1 item 11): `Hero headline, max size, declarative` dest **1** · `Major section headlines` dest **1** · `Primary/dark button label` dest **1** · `Header CTA, lighter weight` dest **1**. YAML `Hero headline, bold confident statement` dest **1** · `Section headlines` dest **1** · `Primary button label` dest **1** · `Header CTA label` dest **3**. Source §3 `the warmth comes from the typeface` dest **1** · `tracking manipulation` dest **1**. `across the system` dest **2**. All seven YAML `use` strings restored verbatim (`check-yaml-use-landing.mjs` 12/12 including component `use`). Paths `tokens.typography.display-hero` dest **1** · `tokens.typography.button-light` dest **2**. |
| YAML `tokens.spacing` (`xs: 4` / `sm: 8` / `md: 16` / `base: 16` / `lg: 24` / `xl: 32` / `xxl: 48` / `section: 64`) | 옮김 → Foundations spacing | `tokens.spacing.md: 16` dest **3** · `tokens.spacing.base: 16` dest **4** · `tokens.spacing.section: 64` dest **2**. `tokens.spacing.md: 16` is not `tokens.spacing.base: 16`. `tokens.spacing.sm: 8` is not `tokens.rounded.sm: 8`. `tokens.spacing.md: 16` is not `tokens.rounded.md: 16`. `tokens.spacing.lg: 24` is not `tokens.rounded.lg: 24`. Padding `16px 23px` DESIGN dest **5** is not a spacing-scale step. |
| YAML `tokens.rounded` (`sm: 8` / `md: 16` / `lg: 24` / `full: 9999`) | 옮김 → Foundations shape | `tokens.rounded.sm` dest **2** · `tokens.rounded.full` dest **1**. Body `9999px` DESIGN dest **12** + provenance dest **8** (`E2a`). Unitless `9999` kept beside `9999px`. |
| YAML `tokens.shadow` (`soft` / `standard`) | 옮김 → Foundations elevation | `tokens.shadow.soft` DESIGN dest **2** + provenance dest **1** (`E2a`). `tokens.shadow.standard` DESIGN dest **2**. `6.4px` DESIGN dest **6**. `9.6px` DESIGN dest **6**. YAML `"rgba(0,0,0,0.03) 0px 4px 6.4px 0px"` kept beside §6 Control stacked writing that omits trailing `0px` on each layer. |
| YAML `tokens.components` (5 records) | 옮김 → Components & States | Verified primitive types preserved (A1b): `Primitive type: \`button\`` dest **5** = source YAML `type: button` **4** + source §4 Primary Compact (not a YAML id). `Primitive type: \`card\`` dest **1** = YAML `type: card` **1**. All five YAML `use` strings restored verbatim. YAML `button-primary` / `button-light` / `button-dark` `font` `16px / 700` DESIGN dest **7** beside those bodies (`16px Charlie Text weight 700` dest **1`). Compact is not a YAML component id; Compact body is `16px Charlie Text weight 400` dest **2** beside type-role `button-light`. `not in the token set` dest **2** (Primary Compact as own id; Cards & Containers). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits under the grouping qualifier. Atmosphere readings carry the adjacent qualifier (B2/B2a). `#ffffff` dest **12** · `#101214` dest **10** · `#1868db` dest **13**. Source comparison “Where Stripe murmurs, Loom announces” dest **1**. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** / **`.verification.md`** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. Conflicts unresolved: none — DESIGN does not invent a conflict. getdesign.md/loom DESIGN dest **1** + provenance dest **1** (`E2a`). refero DESIGN dest **1** + provenance dest **1** (`E2a`). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Role names from the source (Loom Blue / Ink / Pure White / Surface Blue / Body Charcoal / Muted Gray / Primary Hover / Coral / Bright Purple / Deep Aubergine). Pairing/keep-both qualifier at Semantic color. |
| §3 Typography Rules — family, hierarchy, principles | 옮김 → Typography & Assets | Evidence classes; family; roles. `63px (3.95rem)` dest **2**. Tight-headline range `1.03–1.27` dest **1**. Body/lead `1.5–1.52` dest **1**. Source §3 Notes beside YAML `use`: `Hero headline, max size, declarative` dest **1** · `Major section headlines` dest **1** · `Primary/dark button label` dest **1** · `Header CTA, lighter weight` dest **1**. `the warmth comes from the typeface` dest **1** · `tracking manipulation` dest **1**. |
| §4 Component Stylings | 옮김 → Components & States | Primary / Primary Compact / Light / Dark / Icon Buttons / Cards & Containers / Navigation / Footer. YAML hover `bg #0052cc` DESIGN dest **4** + provenance dest **2** (`E2a`). Card Kind maps omitted dest **2** (C4: Cards & Containers + Footer). Nav Kind omitted dest **1** (C4). |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` Layout principles. Open-and-breathable / section-tinting / pill-rhythm kept as source §5. `base unit of 8px` dest **1** (`8px base unit` DESIGN dest **0**). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Level 0–2 table + Control stacked writing + Shadow Philosophy paragraph. YAML strings kept on their own keys (Elevation B2a). |
| §7 Do's | 옮김 → Experience application rules | Eight items under the grouping qualifier. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | Seven Don’t-list items under the qualifier. No invented domain (`native application` dest **0** · `mobile app` dest **0** · `back-office` dest **0** · `authenticated` dest **0** · `storefront` dest **0** · `measures 1440px` dest **0** · `native-client` dest **0**). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Breakpoints `<640px` / `640-1024px` / `1024-1280px` / `>1280px` kept. Touch `48–58px` dest **2**. Hero `~36–40px` dest **1**. `menu toggle` dest **1** (`Menu toggle` DESIGN dest **0**). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present or moved (A2, A3). Unique §9 lead-color prompt `#101214` is not promoted as a lead token (source §2 assigns it to headings). The check is itemised at `provenance.md` §9 deletion check. |
| §10 Voice & Tone | 옮김 → Content & Locales | Warm / direct / human under the voice qualifier. Tone table; hero line; CTAs; forbidden phrases. `"One video is worth a thousand words"` DESIGN dest **1** + provenance dest **2** (`E2a`). `"Get Loom for free"` DESIGN dest **9** + provenance dest **1** (`E2a`). `"Record your screen, voice, and face."` dest **2**. `"Request a demo gate"` dest **1**. `"Revolutionary"` dest **1**. `"game-changer"` dest **1**. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` Scope ¶3: **2015** dest **1** + provenance dest **1** (`E2a`); *Opentest* dest **1** + provenance dest **1**; *OpenVid* dest **1** + provenance dest **1**; Joe Thomas dest **1** + provenance dest **1**; Shahed Khan dest **1** + provenance dest **1**; Vinay Hiremath dest **1** + provenance dest **1**; `975M` DESIGN dest **2** + provenance dest **1** (`E2a`); Atlassian dest **5** + provenance dest **5** (`E2a`). Source §11 restored as written: `shaped everything` DESIGN dest **1** + provenance dest **1** (`E2a`); `friendly consumer-grade` DESIGN dest **1** + provenance dest **1** (`E2a`); `lowers the barrier` DESIGN dest **1** + provenance dest **1** (`E2a`); `pressing record` DESIGN dest **1** + provenance dest **1** (`E2a`); `bold blue-and-white` DESIGN dest **1** + provenance dest **1** (`E2a`); `design expression` DESIGN dest **1** + provenance dest **1** (`E2a`); `bright, immediate, and unintimidating` DESIGN dest **1** + provenance dest **1** (`E2a`); `unintimidating` DESIGN dest **1** + provenance dest **1** (`E2a`). Dual with `provenance.md` Proof notes (`E2a`). Marked as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §12 Principles — 5 numbered | 옮김 → Experience principles | Under the B2a form. Source-stated principle names stay; UI implications are the qualified derived layer. Loom has no published first-party design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). |
| §13 Personas | **삭제 (sidecar 재수록도 안 함)** | D2 / D2a. 가상 원형 3인(이름·나이·도시 포함). D2a가 금하는 이름·나이·도시·전기 문구는 네 산출물 전부에서 0회다. Audience는 원본이 독립적으로 세운 그룹(people inside companies adopting async video bottom-up; Atlassian team-collaboration context)만. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source body preserved in the Capture record (A2; the catalog graph is still 0/440, so nothing is delegated), including Empty / Loading / Error / Success / Skeleton / Disabled. `"Something went wrong"` dest **1**. The applicability rule, card/footer Kind omission, and dark/carousel L/E/S closures are in the Capture qualifier. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Committing CTAs (primary, compact, light) keep loading / error / success `applicable` (`loading \| applicable` dest **3**). Dark destination CTA and carousel icon-button close L/E/S (`loading \| not-applicable` dest **2**). Cards, nav, and footer omit Kind and the map (C4). `default \| applicable` dest **5**. `focus-visible \| applicable` dest **5**. Hover captured `#0052cc` is recorded as a value, not omitted. YAML `bg #0052cc` is not written onto `focus-visible` rows (B1). |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브 3개 분리 → provenance Proof notes | Duration `motion-instant` dest **2** / `0ms` dest **4** / `motion-fast` dest **3** / `120ms` dest **1** / `motion-standard` dest **2** / `220ms` dest **1** / `motion-slow` dest **1** / `320ms` dest **1**. Role names `ease-enter` / `ease-exit` / `ease-standard` kept; curve values DESIGN dest **0** / provenance dest **1** each (`E2a`): `cubic-bezier(0.2, 0.6, 0.25, 1)` · `cubic-bezier(0.4, 0.0, 1, 1)` · `cubic-bezier(0.25, 0.1, 0.25, 1)`. Signature pairings `motion-standard / ease-standard` dest **1**. `prefers-reduced-motion: reduce` dest **1**. B3 is held: the promotion condition names all five evidence kinds — computed transition properties dest **1**, animation name dest **1**, duration, easing, and reduced-motion behavior dest **1** — and the per-component gate, in full text, plus the partial-confirmation exclusion (`E2c`). |
| Opening product sentence (legacy title + §1) | 옮김 → Experience Scope | `DESIGN.md` 9: work-communication product for asynchronous video; one-click recorder for screen, voice, and face; instant shareable links. |

## Sibling handling (`web/references/loom/.verification.md`)

The sibling exists — confirmed with `find web/references/loom -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: `padding: 15.6154px 23.4231px`; `font-size: 15.6154px`; Secondary `height: 48px` against YAML light `58px`; Secondary `color: rgb(0, 0, 0)` against YAML `#101214`; h1 `font-size: 63.2692px` / `line-height: 65.104px`; h2 `"Millions of people across 400,000 companies"` / `44.1538px` / `50.4678px`; carousel accessible name `"Next testimonial"`.
- Measured `DESIGN.md` 0 for those sibling-only strings: `15.6154px` 0 · `23.4231px` 0 · `7.80769px` 0 · `63.2692px` 0 · `65.104px` 0 · `Millions of people across 400,000 companies` 0 · `Next testimonial` 0. Each is provenance dest ≥ **1**.

## A5 / A5a verification

The gate’s `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Loom is Latin-only in the source body, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published English labels in the source body | 12 distinct | 0 | 0 | One video is worth a thousand words / Get Loom for free / Download now / Learn more / Contact Sales / Install Chrome Extension / See all use cases / Explore our blog / Record your screen, voice, and face. / Request a demo gate / Opentest / OpenVid |
| YAML `use` | 12 distinct | 0 | 0 | Hero headline, bold confident statement / Section headlines / Sub-section / card headings / Lead paragraph, intro copy / Standard reading text, nav links / Primary button label / Header CTA label / Primary CTA (Get Loom for free, Download now, Learn more) / Secondary actions (Contact Sales, Install Chrome Extension, See all use cases) / Alternate emphasis CTA (Learn more, Explore our blog) / Testimonial carousel previous/next controls / Footer background, links hover toward #1868db |
| Sibling published strings | 2 sibling-only English lines | 0 from portable body (kept in provenance as inspect context) | 0 portable-body loss | `Millions of people across 400,000 companies` and `Next testimonial`. They are sibling inspect context, not chrome labels in the source body. Mention in provenance Sibling handling is inspect context, not portable-body use. |

Sub-needle labels confirmed present individually in `DESIGN.md`: One video is worth a thousand words, Get Loom for free, Download now, Learn more, Contact Sales, Install Chrome Extension, See all use cases, Explore our blog, Record your screen, voice, and face., Joe Thomas, 2015, 975M.

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 12 English + 12 YAML `use` extracted / 0 missing.

Persona identifiers were not extracted as needles: they are illustrative biographies, not brand-issued copy, and they are deleted (D2/D2a), not lost copy.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand loom --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 137, detail: "바늘 0개 — 이 브랜드에서 A5는 기계 검사되지 않았다. 발행 라틴 문자열을 손으로 전수 대조하라." }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `format: core-v2`, `valid: true`, `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. `scripts/check-limiter-ledger.mjs loom` → 본문 22 / 원장 22 (167–188) 1:1 OK. `scripts/check-yaml-use-landing.mjs loom` → use 12/12 (100%) OK. All are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 137.

## Deviations recorded

- `DESIGN.md` is above the spec’s 600–1,800-word SHOULD budget (`wc -w` 6197). The budget yielded to A1: thirteen YAML color keys plus keep-both on shared hexes, seven type roles with dual YAML/`use` forms, five declared component records plus the §4 compact variant with dual YAML/body fields, seven-state applicability matrices on five interactive controls, `tokens.spacing` unmerged from `tokens.rounded`, the full §11 founding narrative, the full §14 state list, recorded durations and signature motions with B3, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Loom has no published first-party design system in the source, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). F3-audit measure: `grep -oF -- 'derived editorial implementation inference' DESIGN.md | wc -l` = **22** = `not Loom-authored` 22 = `separately published UI specification` 22. Provenance B2a ledger **22** rows (167–188) (E1 1:1; row text aligned to the eight named readings F3 folded in). Meaning-review portable DESIGN SHA-256 `e568649ea50e547b5d0072cd4b50c67834bc336dc369780a310b222440d57db6`.

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is a Loom-issued fact or a derived reading. F3 folded eight unnamed third-class readings into existing complete-form lines (occurrence count stayed **22**): Semantic same-hex component uses plus YAML hover ≠ `focus-visible`; Spacing inequalities named inside the close; Motion `ease-exit` template-match ≠ live-computed and official single curve/duration ≠ the five-kind gate; Font no foundry license URL and recordings-library/recorder-product outside the marketing type capture; Type roles YAML size `16` ≠ `tokens.spacing.base: 16` and size `63` ≠ a spacing step; Capture YAML `16px / 700` belongs to Primary/Light/Dark not Compact, Compact weight 400 beside `button-light`, Navigation Kind omitted, Capture-record rows = product-level writings; Layout 58/48/56/~63px also recorded as desktop-capture measurements; Content copy-mirrors / knowing wink / never gated or salesy as the same derived register reading.

Scope token-surface boundary (including refusing Atlassian-suite membership as a token source), atmosphere characterizations plus the Stripe comparison, narrative-as-context, primary-task selection (including not taking tasks from the persona section), audience grouping and biography-drop, trait restatement and grouping, Principles / Do / Don’t headers (rules and reasons), Semantic keep-both (shared hexes, component uses of those hexes, illustration accents off UI fills, ink undertone as description, hover ≠ `focus-visible`), spacing/shape key-path separation, elevation YAML vs §6 table vs Control stacked writing, motion five-kind gate plus unattributed-curve omission, font evidence class readings, family two-keys and fallback prohibition, type-role non-merge and unitless-ratio keep, favicon-as-pointer, applicability procedure plus Kind omission on cards/nav/footer, YAML `use` beside Role, YAML `16px / 700` beside Primary/Light/Dark body font, Primary Compact as not a YAML id, layout §5/§8 as source contract, voice characterization, and Named-gaps classification each carry an adjacent complete toss-form qualifier. Source facts (dates, hexes, YAML keys, published strings, recorded geometry, §14 treatments, duration tokens) were left unqualified.

Interpretive sentences checked and left as source facts, not newly qualified: the five principle names and the sentences that attribute them to the source; the §14 body in the Capture record; the §15 signature-motion names and reduced-motion rule; the layout bullets. No “does not cover native-app” denial was written (D1). Named gaps do not list `authenticated` or an invented product domain. Exact cubic-bezier values are unnamed in Named gaps and omitted from Motion; they are not listed as a new product domain.

Complete-form lines (all three fragments on one line): Scope ¶1, Scope ¶2, Scope ¶3, Primary tasks, Audience, Distinctive traits, Principles, Application rules, Avoid, Semantic color, Spacing, Shape, Elevation, Motion, Font evidence, Family, Type roles, Assets, Capture record, Layout, Content voice, Named gaps. Count **22**.

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep -oF --` on the destination file named in that row (file-level, never `grep -c`), then remeasured after the F3 body edits (lablup) and again after the meaning-review restorations. Dual destinations (homepage host D4/P7, `#1868db` D13/P7, favicon slug D1/P1, `#0052cc` D12/P5, `#e9f2fe` D9/P7, `9999px` D12/P8, `live-extract` D2/P4, `getdesign.md/loom` D1/P1, `One video is worth a thousand words` D1/P2, `Get Loom for free` D9/P1, 2015 D1/P1, Opentest D1/P1, OpenVid D1/P1, Joe Thomas D1/P1, Shahed Khan D1/P1, Vinay Hiremath D1/P1, 975M D2/P1, Atlassian D5/P5, `tokens.shadow.soft` D2/P1, `bg #0052cc` D4/P2, three cubic-bezier values D0/P1 each, `shaped everything` D1/P1, `friendly consumer-grade` D1/P1, `lowers the barrier` D1/P1, `pressing record` D1/P1, `bold blue-and-white` D1/P1, `design expression` D1/P1, `bright, immediate, and unintimidating` D1/P1, `unintimidating` D1/P1) list both files (`E2a`). F3 dest corrections: YAML `16px / 700` DESIGN dest **7** not **3** and not Compact; `base unit of 8px` dest **1** (`8px base unit` dest **0**); `menu toggle` dest **1** (`Menu toggle` dest **0**); `48–58px` dest **2** not **1**; after qualifier folds `#ffffff` **12**, Charlie Text **13**, `tokens.spacing.base: 16` **4**, `16px 23px` **5**. Meaning-review dest: `Hero headline, max size, declarative` **1** · `Major section headlines` **1** · `Primary/dark button label` **1** · `Header CTA, lighter weight` **1** · `the warmth comes from the typeface` **1** · `tracking manipulation` **1** · `across the system` **2** · YAML `Section headlines` dest **1** (not a prefix of `Major section headlines`) · `Header CTA label` dest **3**. `separately published UI specification` DESIGN dest **22** / provenance dest **2**. Compliance claims are no stronger than the body: “B3 is held” is used only because the Motion promotion paragraph names all five evidence kinds and the per-component gate in full text (`E2c`). Omission of §13 names the source section and field kinds only; identifier strings dest **0** in DESIGN and provenance (D2a, E2d). The Omission ledger does not assert “this file does not contain” those identifier strings while listing them.
