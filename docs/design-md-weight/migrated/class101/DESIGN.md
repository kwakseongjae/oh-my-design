# Class101 Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating Class101 (클래스101) as an online learning marketplace and subscription service, and treating catalog homepage identity as `https://class101.net` rather than a captured-page URL, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Class101 (클래스101) is an online learning marketplace and subscription service. Catalog homepage identity is `https://class101.net`.

Treating the following three 2026-07-12 capture records as the named evidence domains of this reconstruction, treating CLASS101+ and the creator centre as official-doc product-scope sources rather than live token-capture surfaces, and treating values as attached to the surface that established them, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. This contract covers three named first-party captures from 2026-07-12: the Korean product home at `https://class101.net/ko?viewMode=svod`, gift-card terms at `https://class101.net/ko/docs/terms/giftcard?viewMode=svod`, and privacy terms at `https://class101.net/ko/docs/terms/privacy?viewMode=svod`. Official CLASS101+ (`https://class101.net/ko/pages/subscription-year2`) and the creator centre (`https://creator.class101.net/`) are official-doc sources for product scope and narrative. They are not token-capture surfaces in this pass. Token extraction is `live-extract`. Token note: values are limited to the supplied 2026-07-12 capture. Product home, legal-document chrome, and unobserved states remain separate evidence domains.

Treating the following CLASS101+ and creator-centre sentences as first-party product-scope narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Public CLASS101+ material describes a subscription that lets members use a range of existing Class101 content. The official creator centre presents classes and products across hobbies, work skills, and side projects, and says completed classes can be translated to meet class members globally.

The following atmosphere readings — white typographic storefront, black and near-black text dominating, a small `#FF5D00` orange accent in home-title text, Pretendard Variable with compact neutral controls, that combination as captured home chrome rather than a published design-system token set, and product-home-separated-from-legal-document-chrome-as-evidence-for-those-pages-only — are a derived editorial implementation inference from the verified surfaces; they are not Class101-authored or a separately published UI specification. The captured Korean home is a white, typographic storefront: black and near-black text dominate, with a small `#FF5D00` orange accent appearing in home-title text. Its live web interface uses Pretendard Variable and compact, neutral controls rather than a captured public design-system token set. The current record separates that product surface from the terms and privacy pages, whose legal-document chrome is evidence for those pages only.

Treating the following first-party CLASS101+ and creator-centre facts as a two-sided learning marketplace, and treating founding history, named founders, rebrand chronology, and a comprehensive corporate manifesto as omitted rather than reconstructed from secondary reporting, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Together those first-party surfaces describe an audience that discovers and subscribes to learning content, and creators who receive onboarding and publishing support. The reviewed sources do not establish founding history, named founders, rebrand chronology, or a comprehensive corporate manifesto. Those claims are omitted rather than reconstructed from secondary reporting.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three outcomes below as the user outcomes of this reconstruction from the captured Korean product home and from official CLASS101+ and creator-centre pages, and not lifting tasks from source §13, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

- Discover Class101 classes on the captured Korean product home.
- Use CLASS101+ as a subscription that provides access to existing Class101 content.
- Begin a creator journey and publish classes or products on the official creator centre.
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, recording that source §13 found no first-party user-research or persona material, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. No individual personas are promoted. Source §13 found no first-party user-research or persona material. Do not substitute fictional learners or creators for evidence.

### Distinctive traits

Treating `#FF5D00` as catalog `primary_color` and an observed home-title accent rather than a general CTA fill, treating canvas `#FFFFFF` as unmerged from outlined-button border `rgba(255,255,255,0.298)`, treating foreground `#0C0C0C` as unmerged from `#000000`, treating muted `#373737` as unmerged from `rgba(55, 55, 55, 0.8)`, treating Pretendard Variable as the only promoted UI family, treating 4 / 8 / 12 YAML radii as local harvested geometry rather than a universal radius scale, and treating hover, focus, pressed, disabled, error, dialog, toast, input, card, and selected-tab paints as not promoted from this capture, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

- `#FF5D00` catalog `primary_color` as a home-title orange accent (3 medium-confidence occurrences); the capture does not establish a general CTA role
- White canvas `#FFFFFF` on product home and both official documents (3 high-confidence occurrences)
- Foreground `#0C0C0C` (498 occurrences) with a widely observed `#000000` foreground-strong
- Muted `#373737` (12 high-confidence occurrences); one home title sample resolves to `rgba(55, 55, 55, 0.8)`
- Loaded Pretendard Variable (753 captured uses; 92 jsDelivr FontFace source URLs)
- Compact 16px home controls and tabs; 40px / 800 home display heading
- YAML radii 4, 8, 12 as local harvested geometry
- Collector `interactionCount: 0`; only default-state components are promoted

### Principles

These three items (Creator participation is a first-party stated focus; The catalogue spans multiple learning and product categories; CLASS101+ is a subscription offering), including each stem and each *UI implication* (creator-centre-language-belongs-to-creator-acquisition-not-automatically-to-learner-checkout-or-account-flows, category-navigation-may-be-evidenced-separately-from-generic-marketing-claims-this-capture-only-confirms-neutral-16px-home-controls, subscription-specific-benefits-or-purchase-states-require-their-own-observed-surface-evidence), are a derived editorial implementation inference from the verified surfaces; they are not Class101-authored or a separately published UI specification.

1. **Creator participation is a first-party stated focus.** The creator centre invites people to begin their creator journey with Class101. *UI implication:* creator-centre language belongs to creator acquisition, not automatically to learner checkout or account flows.
2. **The catalogue spans multiple learning and product categories.** The creator centre names hobby, job, and side-project categories. *UI implication:* category navigation may be evidenced separately from generic marketing claims; this capture only confirms neutral 16px home controls.
3. **CLASS101+ is a subscription offering.** The official subscription page describes access to a range of existing Class101 content. *UI implication:* subscription-specific benefits or purchase states require their own observed surface evidence.

Treating the following as a capture-bound application of source §7 Do’s is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

- Reuse the confirmed Pretendard Variable family when reproducing the captured web surface.
- Keep product-home evidence distinct from legal-document chrome.
- Treat `#FF5D00` as an observed orange title accent, not as evidence for an all-purpose action color.

### Avoid

The following items copy source §7 Don’ts. They are a derived editorial implementation inference from the verified surfaces; they are not Class101-authored or a separately published UI specification.

- Don't restore the legacy black `#202020` CTA, orange CTA, semantic state colors, or hover shadows without new surface evidence.
- Don't treat declared system-font fallbacks as loaded Class101 font assets.
- Don't infer interaction variants from this capture: it recorded no interactions.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Treating the absence of a public token specification in the sources reviewed as a reason to keep these as raw computed capture values rather than a reconstructed palette is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Class101 does not publish a public token specification in the sources reviewed. These are raw computed values from the supplied capture, not a reconstructed palette.

The following unmerged-role readings — catalog-primary-as-home-title-accent-not-CTA, canvas-unmerged-from-outlined-button-border, foreground-unmerged-from-foreground-strong, muted-unmerged-from-title-sample-alpha, legal-list-item-`#0C0C0C`-as-that-control’s-field-not-extra-general-ink, and home-and-tab-`#000000`-as-those-controls-fields-not-extra-general-ink — are a derived editorial implementation inference from the verified surfaces; they are not Class101-authored or a separately published UI specification.

- **Orange accent** (`#FF5D00`): catalog `primary_color`. Observed on product-home title text (3 medium-confidence occurrences). The capture does not establish a general CTA role.
- **Canvas** (`#FFFFFF`): observed body background across product home and both official documents (3 high-confidence occurrences).
- **Foreground** (`#0C0C0C`): high-confidence text on the product home and legal documents (498 occurrences). Legal-document list item `fg` uses this hex as that control’s renderable field; it is not extra general Ink.
- **Foreground strong** (`#000000`): widely observed in both product-home and legal-document domains. Home navigation button, outlined button, and tab text use this hex as those controls’ fields.
- **Muted text** (`#373737`): observed text / border value (12 high-confidence occurrences). One home title sample resolves to `rgba(55, 55, 55, 0.8)`; that sample is not merged into this hex.

Treating the supplied evidence’s failure to establish success, error, warning, CTA-background, hover, or shadow roles as a reason not to promote those colors is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. No success, error, warning, CTA-background, hover, or shadow color is promoted: the supplied evidence does not establish those roles.

### Spacing

YAML `spacing` is unitless: xs 4, sm 8, md 12, lg 24, xl 36. Treating those YAML numbers as unitless token numbers rather than a claimed universal px scale, while keeping recorded control padding as component fields, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

Harvested control spacing stays with those controls: home outlined button padding `14px 13px`; legal-document tab padding `12px 0px 10px`; legal-document list item padding `0px 0px 0px 24px` and 4px bottom margin.

### Shape

YAML `rounded`: sm 4, md 8, lg 12.

The following local-geometry reading, including 8px home-navigation, 12px outlined-button, and 0px tab corners as recorded-control geometry rather than a universal radius for every unlisted control, and YAML `rounded` numbers recorded without a px suffix while body observations use `8px` / `12px` / `0px`, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

- Home navigation button: `8px`
- Home outlined button: `12px`
- Home tab wrapper and legal-document tab: `0px`

### Elevation

The following elevation reading, including `box-shadow: none` as a flat treatment for these observed controls and list items only rather than a universal elevation token, and unobserved-hover-and-overlay-states-omitted-rather-than-reconstructed, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. The representative captured controls and list items have `box-shadow: none`. No elevation scale is promoted; unobserved hover and overlay states are omitted.

### Motion

Treating the supplied evidence’s lack of transition, animation, easing, or reduced-motion observations as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. The supplied evidence contains no transition, animation, easing, or reduced-motion observations. Do not promote durations or curves.

Do not promote a motion duration, easing, animation name, transition, or reduced-motion behavior until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Pretendard-Variable-as-only-promoted-UI-family, declared-only-fallbacks-not-promoted, jsDelivr-FontFace-as-load-corroboration-not-a-Class101-owned-face, and SIL-OFL-as-font-asset-licence-not-a-Class101-brand-asset, are a derived editorial implementation inference from the verified surfaces; they are not Class101-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live product and document use | `Pretendard Variable` is the computed family on the captured home and official legal-document pages. 753 captured uses across body, controls, headings, list items, tabs, and text, with a matching loaded FontFace set (92 jsDelivr source URLs). This is the only family promoted to `tokens.typography.family.sans`. |
| Declared-only fallbacks — not promoted | The computed stack also names `Pretendard JP Variable`, `Pretendard JP`, `Pretendard`, `system-ui`, platform UI fonts, Roboto, `Noto Sans KR`, and `Malgun Gothic`. The collector did not report a loaded match for these individual fallback faces, so they remain fallback declarations rather than UI-family claims. |
| Official distributed font asset and license | Pretendard’s official project documents its variable dynamic subset under the family name `"Pretendard Variable"`; it is distributed under the SIL Open Font License. That source confirms the font asset and licence boundary, not a separate Class101-owned font. |

### Family

Treating Pretendard Variable as the current visible UI family rather than a Class101-owned face, treating the 92 jsDelivr URLs as load corroboration rather than a distributed Class101 font, treating declared fallbacks as not loaded Class101 assets, and treating a system-face replacement of Pretendard Variable on these captured surfaces as forbidden, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

- **Current visible UI family:** `Pretendard Variable`
- **Loaded source boundary:** matching FontFace set with 92 jsDelivr source URLs on the captured home and legal-document pages
- Do not present declared fallbacks as loaded Class101 font assets. Do not replace Pretendard Variable with a system face on these captured surfaces.

### Type roles

Verified YAML line-height values are the unitless ratios `1.25` (display), `1.29` (page-title), `1.38` (section-heading), `1.67` (body), and `1` (control). Treating those ratios as scaling with font size rather than fixed px, treating the body-table px figures as size-local observations rather than replacements for the ratios, and treating body-table `normal` tracking at section-heading, body, and control captured sizes as a size-local observation rather than invented YAML tracking, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. YAML tracking is `-1.12px` (display) and `-0.616px` (page-title). Section-heading, body, and control have no YAML tracking field; the body table records `normal` at those captured sizes. That `normal` is not invented YAML tracking.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking (YAML / table) | YAML use |
|---|---|---:|---:|---:|---|---|---|
| Home display | Pretendard Variable | 40px | 800 | 1.25 | 50px | -1.12px / -1.12px | Home display heading |
| Page title (h1) | Pretendard Variable | 28px | 700 | 1.29 | 36px | -0.616px / -0.616px | Observed h1 |
| Section heading (h2) | Pretendard Variable | 26px | 700 | 1.38 | 36px | — / normal | Observed h2 |
| Reading body | Pretendard Variable | 18px | 400 | 1.67 | 30px (269 captured occurrences) | — / normal | Observed reading text |
| Control / tab | Pretendard Variable | 16px | 400 | 1 | 16px / normal | — / normal | Observed home controls and tabs |

Treating 40 × 1.25 = 50, 28 × 1.29 ≈ 36, 26 × 1.38 ≈ 36, 18 × 1.67 ≈ 30, and 16 × 1 = 16 as showing that the body-table px figures are ratio products at the captured sizes, and treating control’s additional `normal` keyword beside `16px` as a size-local observation rather than a replacement for YAML `1`, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. 40 × 1.25 = 50, 28 × 1.29 ≈ 36, 26 × 1.38 ≈ 36, 18 × 1.67 ≈ 30, 16 × 1 = 16. Do not replace the YAML ratios with those px figures.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

Treating Pretendard Variable as a licensed sourced font asset (SIL OFL) rather than a Class101-owned face, and treating a Class101-exclusive distributed type family as unverified, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Pretendard Variable is a licensed sourced font asset (SIL OFL), not a Class101-owned face. No Class101-exclusive distributed type family was verified.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, and treating default-state-only as a capture bound rather than a complete state system, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

Only the following default-state components have selector and surface provenance. The collector recorded `interactionCount: 0`; hover, focus, pressed, disabled, error, dialog, toast, input, card, and selected-tab variants are not asserted. Empty, loading, success, error, disabled, skeleton, and validation states were not observed in the supplied capture. No state token or copy should be inferred from the default-state components. YAML `components_harvested` is true.

Treating the 2026-07-12 capture’s lack of support for the legacy black `#202020` CTA, its hover, semantic palette, shadows, cards, inputs, snackbar, dialog, responsive rules, states, and motion as a reason those were removed rather than promoted is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Legacy black `#202020` CTA, its hover, semantic palette, shadows, cards, inputs, snackbar, dialog, responsive rules, states, and motion were not supported by the supplied 2026-07-12 capture and were removed rather than promoted.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating `focus-visible` as applicable with treatment omitted because the source never records `focus-visible`, treating a generic Focus capture as not `focus-visible` evidence, treating absence of a capture as not a `not-applicable` reason, treating loading/error/success as following product role rather than primitive kind, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source never records `focus-visible`; that applicability stays, and the visual treatment is omitted. A generic Focus capture would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating legal-document list item’s YAML `type: listItem` as preserved without inventing interactive kind, and treating home navigation button and home outlined button as having no YAML `type` so Type is not invented, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Legal-document list item has YAML `type: listItem` and no interactive-kind evidence, so kind and a state-applicability map are omitted. Home navigation button and home outlined button have no YAML `type`; Type is not invented.

### Home navigation button

- Role: home control on the product home
- Kind: interactive
- Anatomy: control
- Background: transparent
- Text: `#000000`
- Border: none
- Radius: 8px
- Font: 16px / 400 / Pretendard Variable
- Size: 108px × 38px observed
- Observed: default only
- Surface: product home
- Use: Home control at the captured product home; 108px × 38px observed

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as a home control on the product home |
| hover | applicable | Pointer-web home control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A home navigation control can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a home navigation button; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Home outlined button

- Role: home button on the product home
- Kind: interactive
- Anatomy: control
- Background: transparent
- Text: `#000000`
- Border: 1px solid rgba(255,255,255,0.298)
- Radius: 12px
- Padding: 14px 13px
- Font: 16px / 400 / Pretendard Variable
- Size: 240px × 50px observed
- Observed: default only
- Surface: product home
- Use bound: treating source §9’s “only when its context warrants it” as a capture-bound use limit on this observed button, not as a Class101-authored component spec, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Create this observed home outlined button only when its context warrants it.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as a home button on the product home |
| hover | applicable | Pointer-web home button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A home button can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a home button; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Home tab wrapper

- Role: unselected home tab on the product home
- Kind: interactive
- Anatomy: `div[role="tab"]`
- Background: transparent
- Text: `#000000`
- Border: none
- Radius: 0px
- Font: 16px / 400 / Pretendard Variable
- Size: 97px × 44px observed
- Observed: unselected default (`aria-selected="false"`) only
- Surface: product home

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured unselected on the product home |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive tab; visual treatment omitted |
| disabled | applicable | A tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A tab selects a panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab selection is not a validation or request-failure state on the tab |
| success | not-applicable | Selecting a tab is not an action-outcome confirmation on the tab |

### Legal-document tab

- Role: official documentation chrome tab
- Kind: interactive
- Anatomy: `button[role="tab"]`
- Background: transparent
- Text: `#000000`
- Border: 0px 0px 1px rgb(0,0,0)
- Radius: 0px
- Padding: 12px 0px 10px
- Font: 16px / 400 / Pretendard Variable
- Size: 61px × 45px observed
- Observed: default only
- Surface: treating this tab as official-documentation chrome rather than product-navigation evidence is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. Official documentation chrome; not evidence for product navigation.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as official documentation chrome |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive tab; visual treatment omitted |
| disabled | applicable | A document tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A document tab selects a panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab selection is not a validation or request-failure state on the tab |
| success | not-applicable | Selecting a document tab is not an action-outcome confirmation on the tab |

### Legal-document list item

- Role: official privacy-document list item
- Type: listItem
- Anatomy: list item
- Background: transparent
- Text: `#0C0C0C`
- Border: none
- Padding: 0px 0px 0px 24px
- Font: 16px / 400 / Pretendard Variable
- Observed: default only; 28px line height and 4px bottom margin
- Surface: privacy document
- YAML use: Observed official privacy-document list item only
- Field note: treating `#0C0C0C` as this component’s renderable foreground rather than extra general Ink is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. `#0C0C0C` is this component’s renderable foreground, not extra general Ink.

No interactive-kind or state-applicability evidence. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Treating the following desktop-capture measurements as not a cross-viewport specification, and treating grid, max width, carousel behavior, card ratio, mobile layout, and sticky behavior as unresolved rather than as negative coverage of unmentioned surfaces, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification.

The supplied product-home capture is desktop `1440×900` and records a white body extending 6,121px vertically. It contains 16px controls/tabs and a 40px display heading. It does not establish a grid, max width, carousel behavior, card ratio, mobile layout, or sticky behavior; those remain unresolved.

Treating the absence of a captured responsive transition as not a complete responsive specification, treating mobile, tablet, touch, safe-area, and media behavior as unresolved rather than as negative coverage, and treating the harvested control sizes below as desktop-capture measurements rather than cross-viewport specifications, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. No responsive transition was captured. The only recorded viewport is desktop `1440×900`; mobile, tablet, touch, safe-area, and media behavior are unresolved. The 108px × 38px home navigation button, 240px × 50px outlined button, 97px × 44px home tab, 61px × 45px legal-document tab, and 28px list-item line height are desktop-capture measurements, not cross-viewport specifications.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating the reviewed official material as supporting a creator-facing invitation to begin, treating “warm polite Korean register” as a source-stated capture bound rather than a Class101-authored microcopy guide, treating that material as insufficient to prescribe a universal consumer-product voice, error style, or system-message rule, and treating the table use-boundary labels as official-creator-centre-headline-creator-acquisition-context / official-creator-centre-headline-creator-journey-context / official-creator-centre-mission-line-not-a-universal-UI-string, is a derived editorial implementation inference from the verified surfaces; it is not Class101-authored or a separately published UI specification. The reviewed official material supports a creator-facing invitation to begin. The creator centre addresses creators in a warm polite Korean register. That is insufficient to prescribe a universal consumer-product voice, error style, or system-message rules.

| Evidence | Use boundary |
|---|---|
| "여러분이 사랑하는 일로 더 나은 세상을 만들어 보세요" | Official creator-centre headline; creator acquisition context. |
| "세상을 바꾸는 크리에이터로서의 여정, 클래스101에서 시작하세요" | Official creator-centre headline; creator journey context. |
| "Live the way you love" | Official creator-centre mission line; do not treat as a universal UI string. |

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- success, error, warning, CTA-background, hover, and shadow colors
- legacy black `#202020` CTA and an orange CTA fill
- hover, focus, pressed, disabled, error, dialog, toast, snackbar, input, card, and selected-tab visual treatments
- modal sheets
- empty, loading, success, error, disabled, skeleton, and validation visual treatments and copy
- grid, max width, carousel behavior, card ratio, mobile layout, and sticky behavior
- mobile, tablet, touch, safe-area, and media behavior
- founding history, named founders, rebrand chronology, and a comprehensive corporate manifesto
- universal consumer-product voice, error style, and system-message rules
- Class101-owned font family; declared-only fallbacks as loaded assets
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five: transition properties, animation name, duration, easing, and reduced-motion behavior
