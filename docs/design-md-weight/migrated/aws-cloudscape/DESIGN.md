# AWS Cloudscape Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

AWS Cloudscape is an open-source design system for cloud application experiences, created for and used by AWS services since 2016. Official design system = Cloudscape Design System (`https://cloudscape.design/`, `ds.type: system`). Catalog homepage identity is `https://cloudscape.design/`. Catalog `primary_color` is `#295eff`.

Treating the following URLs as the named evidence domains of this reconstruction, and keeping an AWS authenticated console, a generic AWS marketing site, and an unobserved theme outside that same-surface set, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. This contract covers three public routes from the 2026-07-13 packet: Cloudscape home `https://cloudscape.design/` (verification kind: marketing); the component directory `https://cloudscape.design/components/` (documentation); and About Cloudscape `https://cloudscape.design/about/` (corporate). Those three public Cloudscape routes are the product-surface scope of this reconstruction; an AWS authenticated console, a generic AWS marketing site, and an unobserved theme stay outside that same-surface set.

Treating those measurements as live-extract on the three public routes, and not promoting `#295eff` as an unmeasured solid CTA fill, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. Source token note: values are live-extract measurements on those three public routes. `#295eff` is the repeated public link/accent color in the supplied capture; it is not promoted as an unmeasured solid CTA fill.

The next paragraph is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. The source reads the captured public slice as deliberately operational rather than ornamental: white and near-white working surfaces, near-black information text, a clear blue link/action accent, Open Sans hierarchy, and compact geometry for navigating dense technical material.

Cloudscape’s About page says AWS created the system in 2016 to improve experience consistency across AWS service web applications and help teams implement them faster, and that customers and research have continued to shape it. It has evolved through customer feedback and research, adding responsiveness, accessibility, usability, visual modes, theming, and content-density options, and describes a more approachable visual identity intended to extend beyond a solely technical audience. It publishes guidelines, React components, design resources, patterns, and demos so designers and developers can work from the same public system. [About Cloudscape](https://cloudscape.design/about/) · [Cloudscape home](https://cloudscape.design/) · [Start designing](https://cloudscape.design/get-started/for-designers/start-designing/)

Treating that About-page history as narrative context rather than as interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

- Browse the public Cloudscape component directory on `https://cloudscape.design/components/`.
- Start from Cloudscape home (`https://cloudscape.design/`) to get started, explore components, use patterns, or apply global styles.
- Read About Cloudscape on `https://cloudscape.design/about/` for the system’s published purpose and evolution.

Selecting these three captured-route outcomes rather than source §13 roles as primary tasks is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, source §13’s two evidence-bounded roles remain stakeholder groups rather than primary tasks, and observable work is tied only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. No individual personas are promoted. Cloudscape’s first-party materials identify designers and developers as primary users of its resources, and describe cloud-product builders as the broader intended community. Use those groups only:

- Cloud-product designer: uses the visual foundation, component library, and patterns to move from wireframe to prototype while maintaining accessible, consistent application structure.
- React application developer: installs Cloudscape packages, uses component APIs and global styles, and relies on documented testing, responsiveness, and accessibility guidance.

### Distinctive traits

- Near-black `#0f141a` text on a measured `#ffffff` canvas, with `#8c8c94` border use
- `#295eff` as the repeated public link/accent; not an unmeasured solid CTA fill
- Open Sans as both the observed loaded public-web family and the system’s documented default typeface
- Observed type pairs: 42px/400/48px display with -1.26px tracking; 20px/600/24px headings with -0.2px tracking; 14px/400/20px body/list text
- The observed static component-directory card is sharp and flat (0px radius, `box-shadow: none`); 8px and 12px corners occur elsewhere in the captured public routes

Treating `#295eff` as a captured link/accent rather than a global solid-button token, treating the directory card’s 0px radius as unmerged from YAML `rounded.card` 12 and `rounded.control` 8, and treating Open Sans as canonical here only because official product-use and live computed use agree, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

### Principles

These four items, including their titles, *UI implication* notes, and stems that paraphrase Cloudscape’s first-party About and home descriptions, are a derived editorial implementation inference from the verified surfaces; they are not Cloudscape-authored or a separately published UI specification. The stems paraphrase Cloudscape’s first-party About and home descriptions.

1. **Build cloud experiences at scale.** Cloudscape presents itself as a shared foundation of guidelines, resources, and front-end components.
   *UI implication:* use repeatable patterns and explicit hierarchy before adding decorative variation.
2. **Keep complex work approachable.** Cloudscape says its visual identity has adapted toward a more approachable feel while serving technical tasks.
   *UI implication:* pair dense information with clear grouping, readable type, and direct action labels.
3. **Design inclusively and responsively.** The system describes accessibility, responsiveness, and configurable modes as core features.
   *UI implication:* respect semantic structure and reflow, but do not invent this packet’s missing breakpoint values.
4. **Let implementation and guidance meet.** Cloudscape publishes components, patterns, playgrounds, and design resources together.
   *UI implication:* preserve the distinction between official intent and the selector-backed values actually observed on a route.

Treating the following as a capture-bound application of source §7 Do’s (Open Sans on the bounded public-web pattern; `#0f141a` / `#ffffff` contrast with `#295eff` as observed link/accent; directory card flat and sharp at measured default geometry; official documentation as system context while retaining selector-backed public-route values; visual modes, density, responsive guidance, and accessibility as documented capabilities rather than unmeasured packet values) is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

- Use Open Sans where implementing the bounded captured Cloudscape public-web pattern.
- Preserve the `#0f141a` / `#ffffff` information contrast and use `#295eff` as the observed link/accent treatment.
- Keep the component-directory card flat and sharp when applying its measured default geometry.
- Use official Cloudscape documentation as system context while retaining selector-backed public-route values as the token source.
- Treat visual modes, density, responsive guidance, and accessibility as documented system capabilities rather than unmeasured values in this packet.

### Avoid

Treating the following as a capture-bound application of source §7 Don’ts (Open Sans not replaced by Amazon Ember, a declared fallback, or a system font; `#295eff` not turned into an unmeasured global solid-button token; unobserved card hover/focus/pressed/disabled/error/selected not invented; documentation measurements not transferred to an authenticated AWS console or unrelated AWS marketing surface; unmeasured shadow, card fill, mobile breakpoint, or density-specific geometry not inferred) is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

- Do not replace Open Sans with Amazon Ember, a declared fallback, or a system font and call it observed Cloudscape use.
- Do not turn the observed `#295eff` link treatment into an unmeasured global solid-button token.
- Do not invent hover, focus, pressed, disabled, error, or selected values for the static card token.
- Do not transfer Cloudscape documentation measurements to an authenticated AWS console or unrelated AWS marketing surface.
- Do not infer a shadow, card fill, mobile breakpoint, or density-specific geometry that the supplied evidence did not measure. Treating that last reconstruction constraint as capture-bound rather than as a Cloudscape-authored prohibition is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Observed public-route palette. Treating the extra role characterizations below (Cloudscape link accent not a measured solid CTA fill; working canvas; operational foreground; supporting text; control boundary; catalog `primary_color` as this same hex rather than a fill) is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

- **Cloudscape link accent** (`#295eff`): repeated text and border color across the home, component directory, and About surfaces. Catalog `primary_color` is this same hex; it is not a measured solid CTA fill.
- **Working canvas** (`#ffffff`): measured public background surface.
- **Operational foreground** (`#0f141a`): repeated text and border color across all three captured routes. Component-directory-card text uses this same captured field.
- **Supporting text** (`#424650`): observed secondary text/border value.
- **Control boundary** (`#8c8c94`): observed border color on public control chrome.

Treating that card text as the card’s renderable field rather than a second ink token is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

Cloudscape’s official color guidance says most of the UI is built from white and grayscale, with colorful calls to action and status indicators. It also asks implementers to use design tokens for visual-mode-compliant work. That is system guidance; the canonical values above remain only the values actually measured in the supplied public-route evidence. [Colors](https://cloudscape.design/foundation/visual-foundation/colors/)

Treating the Colors-page guidance as system context rather than as permission to invent unmeasured CTA or status fills is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

### Spacing

Repeated captured values: 4, 8, 12, 20, and 40px. YAML `spacing` is xs 4, sm 8, md 12, lg 20, xl 40. Treating these as a public-route rhythm rather than a complete grid or breakpoint contract is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. Treat these as a public-route rhythm, not as a complete grid or breakpoint contract.

### Shape

- Sharp (directory card): 0px
- Control: 8px
- Card (elsewhere on the captured public routes): 12px

8px control corners and 12px card corners occur elsewhere in the captured public routes. Treating those corners as local defaults rather than a universal radius scale, and keeping them unmerged from the component-directory card’s 0px radius, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

### Elevation

The representative static component-directory card measures `box-shadow: none`, 0px border width, and a transparent background. Treating `none` as only this observed flat treatment, and not collapsing dialog overlays or other floating layers into a shared shadow token, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. Use `none` only for this observed flat treatment; dialog overlays and other floating layers are not collapsed into a shared shadow token.

The following hierarchy reading is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. The source reads that card’s hierarchy as coming from typography, spacing, and the enclosing page structure rather than a tokenized raised surface.

### Motion

The supplied evidence identifies dialog-open interactions but contains no measured duration, easing curve, transition property, reduced-motion behavior, or motion token. Do not derive a motion scale from a static card or from general Cloudscape documentation. Do not promote a motion duration, easing, animation name, transition, or reduced-motion behavior until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists. No motion token is promoted.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

Applying the evidence-class boundaries below (official product-use as system/product facts separate from the computed capture; distributed `@font-face` files as availability on captured web surfaces rather than a substitute for the loaded family; `amazonEmber` / `amazonEmber Fallback` / `openSans Fallback` as declared-only and not UI-family tokens; `Times` as neither a Cloudscape type token nor a fallback substitute) is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | Cloudscape’s typography documentation names Open Sans as its primary and default font, and documents normal, bold, and light use in the system. The developer installation guide says the global-styles package includes Open Sans. These are system/product facts, separate from the computed capture. [Typography](https://cloudscape.design/foundation/visual-foundation/typography/) · [Using components](https://cloudscape.design/get-started/for-developers/using-cloudscape-components/) |
| Live computed surface-use | The supplied 2026-07-13 evidence reports Open Sans as loaded with high confidence and 865 visible uses across body, button, card, dialog, heading, input, list-item, and text roles. |
| Official distributed brand asset | Cloudscape’s global-styles documentation says its package ships `@font-face` Open Sans files, and the design-resources page offers Open Sans for designers. The exact captured webfont URLs are public Cloudscape-hosted assets; this supports availability on the captured web surfaces, not a substitute for the loaded family. [Global styles](https://cloudscape.design/get-started/for-developers/global-styles/) · [Design resources](https://cloudscape.design/get-started/for-designers/design-resources/) |
| Declared-only | The artifact records `amazonEmber`, `amazonEmber Fallback`, and `openSans Fallback` as declared but with zero visible uses in these three routes. They are not UI-family tokens. |
| Captured but uncorroborated | `Times` appears in a small number of dialog/text capture records with unresolved, low-confidence status. It is neither a Cloudscape type token nor a fallback substitute here. |

### Family

Treating Open Sans as the current visible UI family, treating Cloudscape-hosted `@font-face` files as the loaded-source boundary on captured public-web surfaces, and not replacing Open Sans with Amazon Ember, a declared fallback, or a system font as observed Cloudscape use, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

- **Current visible UI family:** Open Sans
- **Loaded source boundary:** Open Sans is loadable on the captured public-web surfaces via Cloudscape-hosted `@font-face` files.
- Do not replace Open Sans with Amazon Ember, a declared fallback, or a system font and call it observed Cloudscape use.

### Type roles

Verified line-height values are the YAML px figures `48`, `24`, and `20` at the captured sizes. Treating those figures as size-local observations rather than unitless ratios is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Provenance |
|---|---|---:|---:|---:|---:|---|
| Display | Open Sans | 42px | 400 | 48px | -1.26px | Observed public home display text |
| Heading | Open Sans | 20px | 600 | 24px | -0.2px | Observed repeated heading and card-title text |
| Body | Open Sans | 14px | 400 | 20px | | Observed public documentation and about-page body/list text |

### Assets

Treating catalog logo metadata as a Google favicon capture-method for `cloudscape.design`, and not promoting that lookup as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract is preserved here. Treating that preservation as required while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

| State | Evidence boundary |
|---|---|
| Component-directory card — default | Observed static card: `#0f141a` text, 0px radius, 272px height, 14px/400 Open Sans, no shadow. |
| Component-directory card — hover | Not captured; intentionally unspecified. |
| Component-directory card — focus | Not captured; intentionally unspecified. |
| Component-directory card — pressed | Not captured; intentionally unspecified. |
| Component-directory card — disabled | Not captured; intentionally unspecified. |
| Component-directory card — error | Not captured; intentionally unspecified. |
| Dialog — open | Three dialog-opening interactions were captured across home, components, and About; no dialog token is promoted because a stable dialog shell was not selected. |
| Loading | Not captured; intentionally unspecified. |
| Empty | Not captured; intentionally unspecified. |
| Success | Not captured; intentionally unspecified. |
| Skeleton | Not captured; intentionally unspecified. |

Dialog-opening interaction capture exists on all three routes, but it does not provide a hover, focus, pressed, disabled, or error state for this static card. A generic or named `focus` capture is not `focus-visible` treatment evidence; this packet has no focus-visible treatment.

The only harvested component is the static component-directory card. It has no interactive-kind evidence, so kind and a state-applicability map are omitted. Absence of a capture is not a `not-applicable` reason. This is not a complete state-coverage claim.

### Component directory card

- Role: static documentation card on the component-directory surface
- Type: card
- Kind: omitted. The source names a static documentation card (`div`, class `card`) and records no interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Text: `#0f141a`
- Radius: 0px
- Height: 272px
- Width (repeated geometry): 305px
- Font: 14px / 400 / Open Sans
- Background: transparent
- Border width: 0px
- Padding: 0px
- Box shadow: none
- Observed: default only (36 instances of this 305px-wide, 272px-high default card geometry)
- Use: Static component-directory card on https://cloudscape.design/components/

The following token-scope reading is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. The source says those measured properties explain why this token preserves only the stable default geometry and text treatment rather than inventing a filled or elevated card.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The captured routes repeatedly use 4, 8, 12, 20, and 40px spacing values. Treating those values as a public-route rhythm rather than a complete grid or breakpoint contract, and treating the directory’s 305px-wide static cards as information-dense cataloging without requiring added elevation, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. Treat these as a public-route rhythm, not as a complete grid or breakpoint contract. The component directory’s repeated 305px-wide static cards show information-dense cataloging without requiring added elevation; their measured default card is 272px high in the representative variant.

Treating official responsive/visual-mode/density documentation as system context rather than a cross-viewport measurement, and treating “keep public information layouts clear and structured” plus a dedicated viewport capture before AWS-specific mobile columns, navigation collapse, or density transitions as reconstruction application rather than a published Cloudscape layout spec, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification. Cloudscape’s official documentation describes responsive design, visual modes, and density settings, but this packet has no cross-viewport comparison. Keep public information layouts clear and structured, then obtain a dedicated viewport capture before asserting AWS-specific mobile columns, navigation collapse, or density transitions.

The evidence packet covers three public routes at one captured desktop viewport and records no viewport comparison. Cloudscape officially supports responsive design and content-density modes, but no exact breakpoint, reflow, or compact-mode values are promoted here. Preserve normal accessible reflow in implementation and collect a separate comparison before representing it as a Cloudscape-specific responsive contract.

Treating “information-dense cataloging” and “keep public information layouts clear and structured” as a reading of the captured directory geometry plus official responsive/density documentation, rather than as a published Cloudscape grid spec, and treating the one-desktop-viewport packet as having no exact breakpoint, reflow, or compact-mode values to promote, with normal accessible reflow preserved and a dedicated comparison required before a Cloudscape-specific responsive contract, is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Cloudscape’s public materials name get started, explore components, use patterns, and apply global styles for designers and developers. The system frames its goal as building intuitive, engaging, and inclusive experiences at scale. [Get started](https://cloudscape.design/get-started/) · [About Cloudscape](https://cloudscape.design/about/)

Treating those public materials as using direct, instructional language is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

The following application note, table, and samples (concrete task labels; compact technical nouns; explanatory support text over promotional superlatives; illustrative writing patterns rather than first-party quotations) are a derived editorial implementation inference from the verified surfaces; they are not Cloudscape-authored or a separately published UI specification. For a Cloudscape-aligned public documentation surface, favor concrete task labels, compact technical nouns, and explanatory support text over promotional superlatives.

| Context | Treatment |
|---|---|
| Primary action | Use a short verb-led label that names the task. |
| Component guidance | State the component purpose, then the usage constraint. |
| Technical note | Be specific about behavior, scope, or configuration; do not imply unverified capability. |

Voice samples are illustrative writing patterns, not first-party quotations: “View component guidance”; “Choose a pattern for this task”; “This setting changes the current density.” Treating those samples as illustrative writing patterns rather than first-party quotations is a derived editorial implementation inference from the verified surfaces; it is not Cloudscape-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover, focus, pressed, disabled, error, and selected visual treatments for the static component-directory card
- a dialog shell token (dialog-open interactions were captured; a stable shell was not selected)
- loading, empty, success, and skeleton visual treatments
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
- exact breakpoint, reflow, compact-mode, visual-mode, and density-specific geometry values
- authenticated AWS product console, generic AWS marketing, and unobserved-theme surfaces
- Amazon Ember, `amazonEmber Fallback`, `openSans Fallback`, and `Times` as UI-family tokens
