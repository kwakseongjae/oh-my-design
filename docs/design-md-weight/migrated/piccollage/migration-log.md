# piccollage migration log

Source: `web/references/piccollage/DESIGN.md`
Sibling read (not the migration input): `web/references/piccollage/.verification.md`
Destination: `docs/design-md-weight/migrated/piccollage/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/piccollage/provenance.md`
Date: 2026-09-03
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Counts use ripgrep match lists per file, never a remembered count.

Source SHA-256 `022e758cc9cf08c38f8814eb8e5d075787f95296ac8f0586035901a8d3e7e051` (`web/references/piccollage/DESIGN.md`). Sibling SHA-256 `8fba2eb3d4d844a3bb6e0ce876570763b8d1c727e4f8c39de3bb3a20b0b74a83` (`web/references/piccollage/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# PicCollage Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. YAML homepage `https://piccollage.com` is dual: DESIGN dest 2 + provenance dest 5 (E2a). `#4FC3C4` is dual: DESIGN dest 7 + provenance dest 7 (E2a). Exact `logo.type: favicon` DESIGN dest 1 at 252 / provenance dest 1 at Identity. `s2/favicons` DESIGN dest 1 / provenance dest 1 (E2a). |
| YAML `omd: "0.1"`, `verified`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance; `prose-derived` 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): identity rows `provenance.md`. The source footer's **Verified:** 2026-06-03 is at `provenance.md` Freshness. `prose-derived` DESIGN dest 2 / provenance dest 5 — ledger metadata plus portable Scope naming the class. Exact `tokens.source: prose-derived` DESIGN dest 1 at 9 / provenance dest in Identity + Token source. `components_harvested` DESIGN dest 0 / provenance dest 2. |
| YAML `tokens.colors` (18 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 80–117. All eighteen roles kept with names, values, and token-set keys: primary `#4fc3c4` / teal-nav `#b7e1da` / teal-footer `#7ad2c3` / teal-border `#2db59e` / hero-bg `#fbf2eb` / nav-bg `#f5f4ef` / surface `#ece9df` / surface-hover `#e8e4d9` / divider `#d9d2bf` / body `#292929` / body-secondary `#4d4d4d` / accent-pink `#f85482` / accent-yellow `#ffcf3d` / gradient-1 `#8235b8` / gradient-2 `#974dcb` / gradient-3 `#ef4967` / gradient-4 `#ee604d` / on-primary `#ffffff`. YAML lowercase beside §2 mixed-case. `#ffffff` on-primary unmerged from Feature Card fill. `#e8e4d9` unmerged across surface-hover / card border / icon hover. Four non-YAML hexes (`#e2ddcf`, `#f19daf`, `#298e7d`, `#AB7624`) dest 119 and also on the capture record / type-roles table (E2a). |
| YAML `tokens.typography.family` | 옮김 → Typography & Assets Family | Family `DESIGN.md` 223–228. YAML `sans: Poppins` and `display: Zilla Slab` both keys stay. |
| YAML `tokens.typography.display / section-title / sub-headline / body / label` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 230–246. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.12` dest 2; `1.4` dest 3. All five YAML `use` strings restored verbatim in the Token-set use column (A1, A3, wave 37 keep-both). Source §3 notes that are longer sit beside them: 60px / line-height 67px / text-shadow `0px 0px 10px #AB7624`; 36px / weight bold; 25px / line-height 35px / white; 18px / weight 400; 14px / tracking -0.28px. Mobile section headline 25px / bold stays a §3 row, not merged into YAML `sub-headline`. Base scale `10 / 12 / 14 / 16 / 18 / 20 / 24 / 28 / 32 / 36 / 40 / 44 / 48 / 72px` dest 1. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) / `tokens.shadow` (3 keys) | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing table `DESIGN.md` 123–134 (`xs: 4 · sm: 8 · md: 12 · base: 16 · lg: 24 · xl: 32 · xxl: 48 · section: 64`). Shape table `DESIGN.md` 142–147 (`sm: 4 · md: 8 · lg: 16 · full: 9999`). Colon-form `tokens.rounded.full: 9999` DESIGN dest 0; the Shape table at 147 writes `tokens.rounded.full` in the Token-set path column and `9999` in the Value column (grep -o per file). `tokens.spacing.base: 16` is disambiguated from `tokens.rounded.lg: 16` and from body 18px. `tokens.spacing.xl: 32` is disambiguated from nav-download height 32px and mobile hero 32px. `tokens.spacing.lg: 24` is disambiguated from sticky `bottom: 24px`. YAML shadow three writings dest at 157–159; §6 five-level table dest at 165–170 including `slide-out drawer` dest 1 at 169 and overlay `rgba(0,0,0,0.40)` at 170. Sticky CTA 10px shadow stays on that control, not `tokens.shadow.card`. |
| YAML `tokens.components` (6 records) | 옮김 → Components & States | `DESIGN.md` 272–423. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×3 (button-primary / button-nav / icon-button), `tab` ×1 (nav-item), `card` ×2 (card / thumbnail). All six YAML `use` strings restored verbatim as `Token-set use:` lines. YAML `font` / `padding` / `radius` shorthands kept on each block. Top Nav is §4-only: `Primitive type: not in the token set` (`DESIGN.md` 417–423). No §4-only component borrowed a YAML `type`. |
| §1 Visual Theme & Atmosphere (legacy 62–64) | 옮김 → Experience `scope` claim, distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 30–39. The atmosphere readings (scrapbook paper; energetic without being aggressive; crafted and accessible; celebratory, multicolour spirit; creative friend's studio) carry an adjacent complete qualifier at 11 (B2/B2a). Opening product sentence dest 9. |
| §2 Color Palette & Roles (legacy 66–80) | 옮김 → Foundations semantic color | `DESIGN.md` 80–117. Characterizing phrases covered by the adjacent qualifier at 82 (B2/B2a). Feature Gradient chain `#8235B8 → #974DCB → #EF4967 → #EE604D` dest in Semantic color + Distinctive traits. |
| §3 Typography Rules — family, scale (legacy 82–91) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 210–221; family 223–228; hierarchy table 234–240. The Official-product-use, Font-evidence wrap, Family, and Type-roles readings carry adjacent complete qualifiers at 212, 216, 228, and 232 (B2/B2a). |
| §4 Component Stylings (legacy 93–153) | 옮김 → Components & States | `DESIGN.md` 272–425. §4 body values and YAML `use` / font / radius / padding forms are both kept. Mobile Sticky 294px / 44px / 30px / 10px shadow dest 278–287. Nav Download 32px / `8px 12px` / 1.5px `#2db59e` dest 310–328. Icon button 44×44 / hover `#e8e4d9` dest 340–354. Feature Card `#E8E8E8` dest 392–404 (Shadow row 399 and keep-both reading 404). Collage Thumbnail keep-both reading dest 415. Footer **Verified** / Tier 1 / Tier 2 / Conflicts at legacy 155–158 are provenance (next row). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 155–158) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness table `provenance.md`; Verified paragraph; Tier 1 list; Tier 2. Homepage / CSS / company / Play URLs are dual as listed in the identity row (E2a). Conflicts unresolved: none — `provenance.md` Freshness. `--color-pic-*` DESIGN dest 1 at 9 / provenance dest in Surfaces + Sibling (E2a). |
| §5 Layout Principles (legacy 160–167) | 옮김 → Layout & Platforms + Foundations spacing | `DESIGN.md` 432–439: ~1221px hero, 690px editor panels, 4px base unit, sticky 70px nav, hamburger below sm 640px, 44px inset, 622px hero text column, 2-column md, marquee, footer 550px lg / 900px mobile (438), QR + app badge, rhythm 8–96px, `32px 0 96px` (439). The recorded-measurements reading carries an adjacent qualifier at 430 (B2/B2a). |
| §6 Depth & Elevation (legacy 169–177) | 옮김 → Foundations elevation | `DESIGN.md` 151–172: YAML three keys + five-level table + overlay tint + sticky 10px local shadow. `slide-out drawer` dest 1 at 169 (Level 4 Use cell). The soft-and-diffused reading carries an adjacent qualifier at 153 (B2/B2a). |
| §7 Do's (legacy 181–188) | 옮김 → Experience application rules | `DESIGN.md` 51–61 (seven Do rules), under the grouping qualifier at 53. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 190–196) | 옮김 → Experience avoid | `DESIGN.md` 63–74, under the qualifier at 65. The six prohibitions are the source's own list (67–72). Two further Avoid items from §9 (dark overlays, serif body text) land on the same list at 73–74 (A3). No invented domain is added. |
| §8 Responsive Behavior (legacy 198–204) | 옮김 → Layout & Platforms | `DESIGN.md` 441–447: four ranges including sticky `bottom: 24px` / 294px / 30px, hero 32px, footer 900 / 650 / 550, editor 690px, 80px block, 622px + phone mockup, 1221px, `sm:px-[44px]`, `md:px-[71px]`, xs 480px. |
| §9 Agent Prompt Guide (legacy 206–219) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion; the §9-only sentences (87.36deg gradient with stops; dark overlays; serif body text; never clinical) land on Semantic color, Avoid, and Content (A2, A3). The check is itemised at `provenance.md` Omission ledger. |
| §10 Voice & Tone (legacy 221–240) | 옮김 → Content & Locales | Register, adjectives, Do/Don't table `DESIGN.md` 453–470; voice samples 472–478; mission and company taglines at 480. The qualifier at 460 covers the voice reading (B2/B2a, A5). The voice-sample reading carries its own adjacent complete qualifier at 480. Byte-exact: "The easiest photo and video editing app to add magic to your treasured memories." / "Create & Celebrate." / "Try PicCollage now!" / "Tap to add your first photo" stay on Content or the surface state contract. |
| §11 Brand Narrative (legacy 242–248) | 옮김 → Experience `scope` | `DESIGN.md` 13: Cardinal Blue Software, Inc.; Taipei-based; Founded in 2011; drag-and-drop collage tool for iOS; tens of millions of downloads; Asia and North America; 270 million people worldwide; "Create, Celebrate, Connect."; OnBeat, BEAM, Noodle, MemeMe; "Creative AI"; "Make the World Fun & Creative."; closing paragraph through `Every template update, sticker pack, and holiday campaign is an act of celebration: an invitation to document life's small moments with the same ceremony reserved for big milestones`. Each source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §12 Principles — 5 numbered (legacy 250–270) | 옮김 → Experience principles | `DESIGN.md` 41–49 under the B2a form at 43: "These five items are a derived editorial implementation inference from the verified surfaces; they are not PicCollage-authored or a separately published UI specification." No separately published PicCollage UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` Derived editorial inventory (32 data rows). |
| §13 Personas — 4 entries (legacy 272–286) | 삭제 | The source's own header labels them illustrative archetypes inferred from brand copy, user reviews, and app-store description, not from first-party research. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, motivation, or affiliation classification appears in either output (D2, D2a). Disposition at `provenance.md` Omission ledger. Audience at `DESIGN.md` 26–28 carries only the group-level description the source independently records (270 million users worldwide; users who wanted to tell stories with multiple photos in a single frame; Asia and North America; first-time creators, not expert designers), under an adjacent qualifier. The three `primary-tasks` at 21–23 come from surfaces and controls the source records; 19 says so and qualifies the step from surface to "primary task". Source grep for each Audience/Primary-task item is non-zero. |
| §14 States — 8 rows (legacy 288–297) | 옮김 → Components & States capture record + per-component applicability | Full eight-row body preserved at `DESIGN.md` 259–267 (A2; the catalog graph is still 0/440, so nothing is delegated), including Empty "Tap to add your first photo", `#e2ddcf` 1.5s skeleton, `#f19daf` "Couldn't load — tap to retry", `rgba(0,0,0,0.80)` / 4s / "Try again", 0.7s confetti `cubic-bezier(.22,1,.36,1)`, staggered 50ms, `opacity: 0.5` / `cursor: not-allowed` / `data-disabled`, `outline: 1px solid #298e7d` / `outline-offset: 2px` (A3). Those four non-YAML writings are also restated at Semantic color 119 as not extra color keys (E2a). The applicability rule is at 270, whose qualifier at 270 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary CTA and Nav Download close loading/error/success because they are app-download destination CTAs (`DESIGN.md` 300–308 / 330–338). Icon Button closes loading/error/success as a toolbar / nav icon that commits no operation in place (`DESIGN.md` 356–364). Nav Menu Item closes loading/error/success with a tab / destination reason (C2 v10) (`DESIGN.md` 382–390). Feature Card, Collage Thumbnail, and Top Nav omit kind and map (C4). `focus-visible` stays applicable; visual treatments on those Core table rows are omitted. The source's named **Focus visible** outline (`#298e7d`, `outline-offset: 2px`) stays on the capture record as that named observation and is not copied onto Core `focus-visible` table rows (B1). The gate matches hyphenated `focus-visible` in the source; the source writes **Focus visible** with a space. E3: the table-row hex was removed because B1 forbids promoting a generic/spaced Focus capture onto the Core row, not to dodge the check. |
| §15 Motion & Easing — durations, named easings, rules (legacy 299–320) | 옮김 → Foundations motion | `DESIGN.md` 174–204 (150ms / 200ms / 700ms / 40s / 3s / `ease-in-out` / `ease-out` / `cubic-bezier(.22, 1, .36, 1)` / `linear` / `reveal-from-rect` / `translateX(-100%)` / `@media (prefers-reduced-motion)` / `animation: none` / Never animate the collage canvas). 0.7s confetti writing stays beside 700ms (wave 37 keep-both). `scale(1.02)` stays as the source's unmeasured follow-standard note, not a promoted token. The cubic-bezier is not a `spec/omd-v0.1.md` template curve (`0.4, 0.0, 1, 1` / `0.0, 0.0, 0.2, 1` / `0.4, 0.0, 0.2, 1` / `0.34, 1.56, 0.64, 1`); it is kept. |
| §15 Motion — promotion gate | 옮김 → Foundations motion | B3 is held: the promotion condition at `DESIGN.md` 204 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The five-kind gate itself carries an adjacent complete B2a close on the same line. |
| Opening product / atmosphere sentence (legacy 60) | 옮김 → Experience Scope | `DESIGN.md` 9–11: Taiwan-born photo-collage and greeting-card app; 270 million users worldwide; inspected homepage, CSS bundle, company page, Play listing. |

## Sibling handling (`web/references/piccollage/.verification.md`)

The sibling exists — confirmed with `find web/references/piccollage -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: company-page gradient `#604BB6` / `#7B2E8E`; Play `4.8 stars` / `1.82M` / `100M+ Downloads`; CSS custom-property names `--color-pic-teal-200` and siblings; Tailwind class strings; file sizes; hero `overflow:hidden`; reveal fill-mode `both`; getdesign miss-page sentence. Recorded at `provenance.md` Sibling-only strings.
- Measured `DESIGN.md` 0 for those sibling-only strings: `#604BB6` 0 · `#7B2E8E` 0 · `1.82M` 0 · `100M+` 0 · `4.8 stars` 0.
- `#4FC3C4` / 44px / 30px / Zilla Slab 60px / 67px / `#AB7624` / `#FBF2EB` / the 87.36deg gradient / 200ms ease-in-out / `reveal-from-rect` / Cardinal Blue Software, Inc. are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. A hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 24 distinct | 0 | 0 | The easiest photo and video editing app to add magic to your treasured memories. / Our promise to you — our AI tools will always act as an assistant to super-charge your creative ideas. They blend in seamlessly and never take the creative process out of your hands. / We love a holiday! But we also believe every moment is worth romanticizing. / Create & Celebrate. / Give it a try. Have some fun. / Try PicCollage now! / Enter your photos. / leverage your assets / your memories, / your camera roll / Create, / Celebrate, / Share / Create, Celebrate, Connect. / Make the World Fun & Creative. / Creative AI / OnBeat / BEAM / Noodle / MemeMe / Tap to add your first photo / Couldn't load — tap to retry / Try again / Download the App / Cardinal Blue Software, Inc. |
| YAML `use` strings | 5+6 distinct | 0 | 0 | All five type-role Token-set use lines and all six component Token-set use lines restore the YAML `use` byte-exact. |
| `node test-v2/tools/latin-copy-audit.mjs --brand piccollage` | 0 lost / 1 scanned | 0 published | 0 | `scanned: 1`, `clean: 1`, `withLoss: 0`, `totalLost: 0`. |
| Sibling published strings | sibling-only rating / CSS / class names | 0 | 0 | `4.8 stars` / `1.82M` / `100M+ Downloads` / `#604BB6` survive in provenance; they are not promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: Create, Celebrate, Connect. / Make the World Fun & Creative. / Create & Celebrate. / Try PicCollage now! / Tap to add your first photo / Couldn't load — tap to retry / Founded in 2011 / Cardinal Blue Software, Inc. / OnBeat / BEAM / Noodle / MemeMe / Download the App.

A5 분모: hand sweep of source published labels 24 extracted / 0 missing; YAML use 11 / 0; latin-copy-audit published-copy lost 0 / 1 scanned; gate `copy-loss` compared 0 / candidates 147. A5a was mandatory because `compared` 0 < `candidates` 147.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand piccollage --gate-only` → first run `verdict: MIGRATION_BLOCKED`, `problems: [{ check: "outputs", detail: "migration-log.md missing" }]`. Second run `verdict: MIGRATION_BLOCKED`, `problems: [{ check: "focus-visible-promotion", detail: "1 focus-visible table row(s) carry treatments but the source never records focus-visible" }]`. The Primary CTA `focus-visible` row had carried `#298e7d`. The source writes **Focus visible** (space, not hyphen). B1: that named observation stays on the capture record; Core `focus-visible` table rows omit the treatment. Third run → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 147, detail: "바늘 0개 — 이 브랜드에서 A5는 기계 검사되지 않았다. 발행 라틴 문자열을 손으로 전수 대조하라." }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 147.

## Deviations recorded

- `DESIGN.md` is 7,316 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: eighteen color keys, five type roles plus the mobile section-headline row and base scale, six declared component records plus Top Nav, an eight-row surface state contract, a seven-state applicability matrix on four interactive controls, the Shape table's `tokens.rounded.full` step `9999` kept off matching spacing keys, the full §11 founding-and-mission narrative including each paragraph's last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published PicCollage UI specification is named in the source, so every derived-editorial close uses the toss-form `not PicCollage-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Measure after F3 (grep -o per file): `derived editorial implementation inference` DESIGN = 32, `not PicCollage-authored` DESIGN = 32. Provenance derived ledger 32 data rows (E1 1:1). Header is exactly `## Derived editorial inventory` with `| Location in DESIGN.md | Qualified reading |`. Hedge lines: 9, 11, 13, 19, 28, 32, 43, 53, 65, 82, 119, 136, 149, 153, 176, 204, 212, 216, 228, 232, 252, 270, 298, 328, 354, 380, 404, 415, 430, 460, 480, 514.
- Worker-close portable DESIGN SHA-256 `95be163f459ff814ef1138569c8ffb058a84117af8b9faf0622a19c7afaf5c13`. F3 auditor SHA-256 after B2a/E2 pass: `ed06d93b2f13c607ec3b3bd2e0cf3baf5cb65fb7e79a0baa5634e7078ac1f508` (also in `audit-log.md`).

## Mandatory final passes

### Pass 1 (F1 / B2a)

Re-read the portable body from H1 through Named gaps. Every causal, interpretive, or judging sentence was classified. Brand-authored strings (mission, taglines, voice samples, hexes, metrics, years, component geometry) stay unqualified as facts. Derived readings (surface attachment, atmosphere, narrative-as-not-tokens, task selection, audience drop, trait grouping, principles, Do/Don't, unmerges, elevation/motion class, font-evidence sorting, family fallback, type-role keep-both, assets pointer, applicability verdicts, layout-as-measurements, voice class, named-gaps class, four non-YAML hexes as not extra color keys, Card keep-both, Thumbnail keep-both) each have an adjacent complete qualifier of the form "derived editorial implementation inference from the verified surfaces; … not PicCollage-authored or a separately published UI specification". Count after F3: 32 complete forms, 32 inventory rows.

### Pass 2 (F2 / E2)

Each log row's destination was grepped in `DESIGN.md` and/or `provenance.md` before the row was written, and F3 remeasured dests after body edits (grep -o per file, not grep -c). Dual destinations (homepage URL, `#4FC3C4`, favicon slug, `prose-derived`, `--color-pic-*`, Cardinal Blue Software, Inc., 87.36deg gradient, dark overlays, never clinical, `#e2ddcf` / `#f19daf` / `#298e7d` / `#AB7624` at capture record and Semantic color 119) name both homes. Compliance claims: B3 held because `DESIGN.md` 204 names transition properties, animation name, duration, easing, reduced-motion behavior, and the per-component gate in full text. "B3 유지" is not written more strongly than that sentence. C1: `Not captured` as a `not-applicable` reason dest 0. D2a: persona given-names dest 0 in DESIGN and provenance. E2d: the omission ledger names field kinds and does not assert absence in the same row that lists those strings.

### Unique-phrase restoration (wave 43)

Hand sweep of source-unique expressions (years, proper nouns, quotes, §11 causal last sentences, value modifiers, §15/§7 constraints): extracted 145 / missing-then-restored 1 (`slide-out drawer`, restored on the Level 4 elevation row). Final missing 0 among that set.

DONE migrated=1
