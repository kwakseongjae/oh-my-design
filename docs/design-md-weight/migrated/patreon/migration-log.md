# patreon migration log

Source: `web/references/patreon/DESIGN.md` (unmodified)
Sibling read (not the migration input): `web/references/patreon/.verification.md`
Destination: `docs/design-md-weight/migrated/patreon/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/patreon/provenance.md`
Rulebook version: **v12** (`docs/design-md-weight/MIGRATION_RULEBOOK.md`)
Date: 2026-09-02
Worker: grok-4.6 T2
Not a catalog-adoption claim. Gate: `node test-v2/tools/migrate-reference.mjs --brand patreon --gate-only` → `verdict: PASS`, problems []. Portable Core: `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/patreon/DESIGN.md --check --require-portable-core --json` → status pass, `portable_core: true`. Gate output is a run result, not evidence of semantic adequacy (E2c).

Source SHA-256 `fd4d2274fb22bcc8622a6b8d4849bb9d52976c4bbfda5dd40f3fe5232b702b0f` (`web/references/patreon/DESIGN.md`). Sibling SHA-256 `31b1c07c3f65e445b53c1b8129ce2811a8b99cd164f854f1bd0d1a76486c064d` (`web/references/patreon/.verification.md`).

Every destination line below was checked with a file read / `in`-line search against the three output files before it was written (F2). Bare numbers are `DESIGN.md` lines; provenance lines are marked `prov`. Counts are from those searches, never a remembered count.

## Frontmatter

| Legacy | Disposition | Destination / reason |
|---|---|---|
| `id`, `country`, `category`, `homepage`, `verified`, `added`, `omd: "0.1"` | 분리 → provenance | Portable file has no frontmatter. Identity table `prov` 9–22. Freshness `prov` 32–37. Conflicts `none` at `prov` 41. |
| `name: Patreon` | 옮김 → DESIGN.md H1 · 분리 → provenance | H1 `# Patreon Design System` (`DESIGN.md` 1) and `prov` 10. Two destinations (E2a). |
| `primary_color: "#000000"` | 옮김 → Foundations · 분리 → provenance | Semantic color Stage Black / catalog `primary_color` (`DESIGN.md` 99) and `prov` 14. Hex `#000000` is also `tokens.colors.black` — two keys kept unmerged (`DESIGN.md` 95, 99, 118). Dual: body + ledger (E2a). |
| `logo.type: favicon` / `logo.slug` | 옮김 → Typography & Assets · 분리 → provenance | Favicon URL `https://www.google.com/s2/favicons?domain=patreon.com&sz=128` at `DESIGN.md` 219 and `prov` 16. Two destinations (E2a). Assets qualifier at `DESIGN.md` 224 reads it as a catalog identity pointer, and reads the planned creator-mark tool as a source-narrative name rather than a harvested asset file. |
| `tokens.source: live-extract`, `tokens.extracted: 2026-06-17` | 분리 → provenance; **meaning** 옮김 → Scope | Exact `live-extract` at `DESIGN.md` 9 and `prov` 20, 74. `tokens.extracted` date 2026-06-17 at `DESIGN.md` 9 and freshness `prov` 34–37 (E2a). |
| `tokens.note` | 옮김 → Experience Scope · 분리 → provenance | YAML note restated as the facts it names at `DESIGN.md` 9 and `prov` 28 (E2a): 2023 Wolff Olins / Dinamo / Active Theory rebrand; primary black CTA inverting to white; Oracle weight 250; rem-scaled ≈1.5×; design-intent ≈ live computed ÷ 1.5; periwinkle/cornflower/brand blue as accents, not chrome. |
| `ds.name` / `ds.url` / `ds.type` | 분리 → provenance (absence recorded) | Not present in source frontmatter. Absence recorded at `prov` 24 (A1c). Not filled. |
| `components_harvested: true` | 분리 → provenance | `DESIGN.md` 0 / `prov` 22 and `prov` 207 (A1c). |
| YAML `tokens.colors` (12 keys) | 옮김 → Foundations Semantic color · 분리 → provenance claim ledger | `DESIGN.md` 99–114. Paths named beside each role: `tokens.colors.primary` / `on-primary` / `canvas` / `ink` / `black` / `muted` / `hairline` / `near-black` / `brand-blue` / `accent-cornflower` / `accent-periwinkle` / `accent-sage`. `primary` and `black` share `#000000` and stay two keys (`DESIGN.md` 118). `canvas` and `on-primary` share `#ffffff` and stay two keys (`DESIGN.md` 118). Live `color(srgb 0.1 0.1 0.1)` / `0.6 0.6 0.6` / `0.8 0.8 0.8` kept (`DESIGN.md` 101, 105, 106). Translucent `rgba(255,255,255,0.16)` and `rgba(255,255,255,0.6)` kept off the solid map (`DESIGN.md` 116). Claim ledger `prov` 82. |
| YAML `tokens.typography.family` (display + body both `Oracle`) | 옮김 → Typography Family / Font evidence · 분리 → provenance | Family `DESIGN.md` 192–194. Live scan Oracle on 1,556 elements at `DESIGN.md` 13, 42, 185. `tokens.typography.family.display` and `tokens.typography.family.body` both write `Oracle` (`DESIGN.md` 192). Claim ledger `prov` 83. |
| YAML `tokens.typography.display-hero` 125 / 250 / `0.98` / `-7.5` | 옮김 → Type roles | `DESIGN.md` 206. Unitless `0.98` kept as a ratio (A1a). YAML use "Hero wordmark headline, Oracle ultra-light (live computed 187.5px @1.5× root)" verbatim in the Token-set use column. Longer §3 notes (≈ -0.06em, ultra-light) sit beside it. |
| YAML `tokens.typography.display-section` 85 / 250 / `0.87` / `-5.1` | 옮김 → Type roles | `DESIGN.md` 207. Unitless `0.87` kept. YAML use "Section display headline, Oracle ultra-light (live 127.5px)" verbatim. |
| YAML `tokens.typography.stat` 48 / 250 / `0.80` / `-1.92` | 옮김 → Type roles | `DESIGN.md` 208. Unitless `0.80` kept. YAML use "Big stat figure e.g. '10%' (live 72px)" verbatim. |
| YAML `tokens.typography.label-lg` 20 / 400 / `1.50` | 옮김 → Type roles | `DESIGN.md` 209. Unitless `1.50` kept. Size `20` disambiguated from `tokens.rounded.md: 20`. YAML use "Section label / eyebrow, Oracle (live 30px)" verbatim. |
| YAML `tokens.typography.subhead` 15 / 400 / `1.33` / `-0.3` | 옮김 → Type roles | `DESIGN.md` 210. Unitless `1.33` kept. YAML use "Feature subheading, Oracle (live 22.5px)" verbatim. |
| YAML `tokens.typography.body` 15 / 400 / `1.50` | 옮김 → Type roles | `DESIGN.md` 211. YAML use "Body / card copy, Oracle" verbatim. Live computed column is `—` as in source §3. |
| YAML `tokens.typography.nav` 9 / 400 | 옮김 → Type roles | `DESIGN.md` 212. YAML has no numeric lineHeight or tracking; "normal" kept from §3. YAML use "Nav item label, Oracle (live 13.5px)" verbatim. |
| YAML `tokens.typography.button` 9 / 350 | 옮김 → Type roles | `DESIGN.md` 213. YAML has no numeric lineHeight or tracking. YAML use "Button / CTA label, Oracle (live 13.5px)" verbatim. |
| YAML `tokens.spacing` (8 named steps) | 옮김 → Foundations Spacing · Layout | Unitless `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 24` · `xl: 32` · `xxl: 48` · `section: 64` at `DESIGN.md` 122. §5 px restatement at 124 and 448. `tokens.spacing.md: 12` off radius; `base: 16` off type (body is 15); harvested padding `15px 21px` off `tokens.rounded.sm: 15` (`DESIGN.md` 124). Claim ledger `prov` 85. |
| YAML `tokens.rounded` (`sm` 15 / `md` 20 / `lg` 30 / `pill` 45 / `full` 9999) | 옮김 → Foundations Shape | `DESIGN.md` 128–135. `full` 9999 at 128, 135. `50%` kept beside 9999 as the source's dual writing for chips and avatars (`DESIGN.md` 135, 138). `pill: 45` disambiguated from search height `45px` (`DESIGN.md` 134, 138). ≈25px larger pills kept as a body observation, not a sixth YAML key (`DESIGN.md` 136). |
| YAML `tokens.shadow.none: "none"` | 옮김 → Foundations Elevation | YAML path at `DESIGN.md` 149. Live `box-shadow: none` at 149. Elevation table 143–147. |
| YAML `tokens.components.button-primary-dark` (`type: button`) | 옮김 → Components Primary (on light surface) | `DESIGN.md` 253–279. `Primitive type: \`button\`` at 256 (A1b). YAML use "Primary CTA on light surfaces — 'Get started'" at 264. Height `47px`, padding `15px 21px`, radius `30px`, font `9px / 350 Oracle`. |
| YAML `tokens.components.button-primary-light` (`type: button`) | 옮김 → Components Primary (on dark / photo hero) | `DESIGN.md` 281–306. `Primitive type: \`button\`` at 283. YAML use "Primary CTA on dark/photo hero — 'Get Started'" at 291. Quoted "Get Started" unmerged from "Get started" (295). |
| YAML `tokens.components.button-outline` (`type: button`) | 옮김 → Components Outline (on dark) | `DESIGN.md` 308–333. `Primitive type: \`button\`` at 310. YAML use "Secondary outline on dark — 'Log in', 'Updates'" at 318. Named hover fill `rgba(255,255,255,0.16)` at 317, 327. |
| YAML `tokens.components.nav-item` (`type: tab`) | 옮김 → Components Navigation item | `DESIGN.md` 335–360. `Primitive type: \`tab\`` at 337 (A1b). YAML use "Top nav menu item on dark header" at 345. Longer §4 Use with "Creators", "Features", "Pricing", "Resources", "Updates", "PATREON" wordmark, "Get Started" pill at 346. Padding `11px 18px`. Active/hover `underline / hover bg rgba(255,255,255,0.16)`. |
| YAML `tokens.components.search-input` (`type: input`) | 옮김 → Components Find-a-Creator Search | `DESIGN.md` 362–384. `Primitive type: \`input\`` at 364. YAML use "'Find a Creator' pill search on dark, translucent fill rgba(255,255,255,0.16)" at 370. Radius `45px` unmerged from height `45px` (374). |
| YAML `tokens.components.card-pricing` (`type: card`) | 옮김 → Components Pricing / Feature Card | `DESIGN.md` 386–400. `Primitive type: \`card\`` at 389. Kind and map withheld (C4) at 400. YAML use "Pricing / feature card — shadow-free, hairline outline" at 394. |
| YAML `tokens.components.chip-translucent` (`type: badge`) | 옮김 → Components Translucent Utility Chip | `DESIGN.md` 402–425. `Primitive type: \`badge\`` at 405. YAML use "Language / utility chip on dark, fill rgba(255,255,255,0.16)" at 411. Radius `9999px` at 407, 414. |
| YAML `tokens.components.accent-band` (`type: card`) | 옮김 → Components Expressive Accent Band | `DESIGN.md` 427–439. `Primitive type: \`card\`` at 429. Kind and map withheld (C4) at 439. YAML use "Expressive periwinkle feature band — color as creator energy, not chrome" at 433. |

## Body sections

| Legacy | Disposition | Destination / reason |
|---|---|---|
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–15; Distinctive traits 42–49. Oracle 1,556; weight 250; ≈125px / 187.5px; ≈ -0.06em; "Where podcasts grow", "Start Your Patreon for Free", "Earning made easy"; pill 30px / 45px / ≈25px; shadow-free; surface-inverting CTA. Atmosphere readings qualified at 11, 13, 15 (B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations Semantic color | `DESIGN.md` 99–118. Stage Black / Paper White / Obsidian Ink / Muted Grey / Hairline Grey / Near-Black Plum / Brand Blue / Cornflower / Periwinkle / Sage. Role-name and unmerged-key readings qualified at 95 (B2a). |
| §3 Typography Rules | 옮김 → Typography & Assets | Font evidence 182–188; Family 192–196; Type roles 204–213; type-rule readings 215. Unitless line heights kept as ratios (A1a). YAML `use` strings and longer §3 live-computed / tracking columns both kept (wave 37). Fallbacks `"Noto Sans JP/KR/SC/TC"`, `"Helvetica Neue"`, `Helvetica`, `Arial`, `sans-serif` are fallbacks, not Oracle (`DESIGN.md` 193, 196). |
| §4 Component Stylings | 옮김 → Components & States | Eight YAML components at 253–439. Verified primitive types preserved per component, not flattened to Kind (A1b): `button` ×3, `tab` ×1, `input` ×1, `card` ×2, `badge` ×1. No §4-only component borrowed a `Primitive type`. Capture record + applicability at 229–251. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `prov` 32–41; Tier 1 `prov` 52–57; Tier 2 `prov` 59–62. Home / pricing URLs dual: Scope `DESIGN.md` 9, 31 and Font evidence `DESIGN.md` 185 and `prov` 47–48, 54–55 (E2a). `grep -o 'https://www.patreon.com'` DESIGN dest 5 (the `/pricing` URL shares that prefix; exact `https://www.patreon.com/pricing` DESIGN dest 3 at 9/31/185). Conflicts unresolved: none — `prov` 41. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | Layout `DESIGN.md` 444–488. Base unit 4px; scale 4/8/12/16/24/32/48/64; `15px 21px`; 64px+ section gaps; full-bleed heroes; centered columns; band rhythm; photography-over-ornament; headline-as-graphic. Shape named uses at 130–136. Layout qualifier at 444 (B2a). |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | Table `DESIGN.md` 143–147. `box-shadow: none` at 149. Shadow-philosophy reading qualified at 149 (B2a). |
| §7 Do's | 옮김 → Experience Application rules | `DESIGN.md` 66–74, under the grouping qualifier at 64. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience Avoid | `DESIGN.md` 80–87, under the qualifier at 78. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Breakpoints `<640px` / `640-1024px` / `1024-1440px` at `DESIGN.md` 467–469. Touch targets 47px / 45px / 47px at 473–475. Collapsing strategy 479–482. Image behavior 486–488. Desktop `1024-1440px` is the source's own named span; `measures 1440px` is not written (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing recreate-the-control prompts. No receiving slot and no delegation. Every hex, size, radius, padding, height, family, and quoted label inside it was checked against the portable body before deletion; each already stands in Scope / Foundations / Typography / Components (A3). The check is itemised at `prov` 156. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 493–515. Voice samples byte-exact: "Where Creator Communities Thrive"; "Start Your Patreon for Free"; the visual-language quote. Tone table keeps "Where podcasts grow", "Your wildest creative reality", "Complete creative control", "Earning made easy", "Get started", "Start Your Patreon for Free", "Secure payments handled for you", "We handle taxes", "No setup headaches", "Make it making art", "Creator is now a career", "Get Started for Free". Forbidden register at 511. Harvested language / utility chip as captured control at 513. `locale matrix` DESIGN dest **0**. `does not name` DESIGN dest **0**. `locale` DESIGN dest **1** (`content-locales` marker only). Register reading qualified at 493; caption-character at 509 (B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope · 분리 → provenance Narrative URLs | Full paragraphs including last sentences kept as one unit (`DESIGN.md` 17–21): 2013 Jack Conte / Sam Yam / Pomplamoose / YouTube ad revenue / college roommate / ad-funded attention economy / about-page quote / 2021 `$4 billion` / `$10 billion`; October 2023 Wolff Olins / Dinamo / Active Theory / David McLeod / no-canonical-form quote / motion-first quote / 20th-century retail conventions; refuses / embraces / planned creator-mark tool / "The design is, in essence, a brand that gets out of the way." Narrative URLs at `prov` 63–69 (E2a for facts in Scope + URLs in ledger). Classification qualified at 21 (B2a). |
| §12 Principles (6) | 옮김 → Experience Principles | `DESIGN.md` 55–60 under the B2a form at 53: "These 6 items are a derived editorial implementation inference from the verified surfaces; they are not Patreon-authored or a separately published UI specification." No separately published Patreon UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Source HTML-comment interpretive claims named in the same qualifier. Portable derived-editorial inventory: `prov` 158–201 (40 data rows). |
| §13 Personas (4 fictional archetypes) | 삭제; grouping string only 옮김 → Audience | Fictional biographies dropped. Source §13 header grouping kept verbatim in Audience (`DESIGN.md` 36): "publicly observable Patreon user segments (independent creators monetizing via membership, and their paying fans)". Identifier and biography fields (name, age, city, occupation, motivation) are not promoted or re-hosted (D2, D2a). Primary tasks at 29–31 come from captured CTAs / search / pricing URL, not from §13. Disposition at `prov` 153. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source table at `DESIGN.md` 237–245 (A2; catalog graph still 0/440, so nothing is delegated). Philosophy-layer classification at 233 (B2a). Table characterizations at 247 (B2a). Applicability rule at 249: non-observation is never a `not-applicable` reason (C1). Destination CTAs and nav tabs close loading / error / success from role (C2). Search error applicable; loading / success not-applicable from field role. Card and band omit kind/map (C4). This is not a complete state-coverage claim (C3, 249). Generic Focus is not promoted to `focus-visible` treatment (B1, 249). Graph delegation none. |
| §15 Motion & Easing | 옮김 → Foundations Motion + Named gaps; 무출처 커브 분리 → provenance omission ledger | Duration `120ms` / `240ms` / `400ms` / `600ms+` preserved as source-stated (`DESIGN.md` 157–160). Easing names preserved; exact cubic-bezier omitted: `ease-enter` `cubic-bezier(0.2, 0.6, 0.25, 1)`; `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` (matches spec-template `ease-exit`); `ease-standard` `cubic-bezier(0.25, 0.1, 0.25, 1)` — `DESIGN.md` 0 / `prov` 154 only. `prefers-reduced-motion: reduce` at 170. Morphing logo / variable wordmark / ambient color / pill scale-opacity / bands-reveal-from-below / "motion-first brand for a digital-first world" at 170. Philosophy-layer qualifier at 153; motion-rule qualifier at 170 (B2a). |
| §15 promotion condition | 신설 근거 규칙 → Foundations Motion | B3 is written out in full at `DESIGN.md` 172, verified by reading the line: transition properties, animation name, duration, easing, and reduced-motion behavior, plus the per-component computed-observation gate and the "Official documentation of a single curve or duration is not that gate" clause. This row claims only what line 172 actually contains (E2c). |

## Sibling file

`web/references/patreon/.verification.md` **exists** — confirmed with `find web/references/patreon -type f`, not with `ls` or a `*` glob. It was adopted as Proof corroboration (`prov` 99–129). Inspection date 2026-06-17, method playwright getComputedStyle, and the URL set match the source. No extra hex or metric was taken into the portable body.

Sibling-only strings, measured with `grep -o` per file (not `grep -c`). `English (United States)` DESIGN dest 0 / prov dest 1 at 121. `Explore our features` DESIGN dest 0 / prov dest 1 at 118. `Patreon pricing` DESIGN dest 0 / prov dest 1 at 119. document.title em-dash forms DESIGN dest 0 / prov dest 1 at 122. `#959595` DESIGN dest 0 / prov dest 1 at 123. `Times` DESIGN dest 0 / prov dest 1 at 113. Live tracking `-11.25px` / `-7.65px` / `-2.88px` / `-0.45px` DESIGN dest 0 / prov dest 1 each at 114–117. Bare `11.25px` DESIGN dest 0 / prov dest 3 at 112, 114, 120. Radius-scan `37.5px` DESIGN dest 0 / prov dest 1 at 124. Radius-scan `50px` DESIGN dest 0 / prov dest 1 at 125. Body rem `font-size: 7.5px` as that sibling-only writing is not in DESIGN.md; `grep -o '7.5px'` DESIGN dest 7 because it is a suffix of source-shared `187.5px` / `127.5px` (13, 43, 206, 207). Radius-scan `22.5px` (pricing-surface chips) is sibling-only as a radius bin; `grep -o '22.5px'` DESIGN dest 2 at 210 is the source §3 Subhead live computed size, not that radius bin. Provenance list at `prov` 112–127 names these as mention, not as a three-file absence (E2d).

## F1 — B2a scan

Re-read the whole portable body after drafting. Every causal / interpretive / judgment sentence is either a Patreon-authored quote, a live observation, or a derived editorial implementation inference with an adjacent complete qualifier (`derived editorial implementation inference` / `not Patreon-authored` / `separately published UI specification`). Measured with `grep -o` per file: `derived editorial implementation inference` DESIGN dest **40** (includes 7 plural `inferences`); `not Patreon-authored` DESIGN dest **40**; `separately published UI specification` DESIGN dest **42** (complete qualifications 40; lines 21 and 95 repeat the spec phrase inside the same qualification). Complete qualification sites = **40**, listed one-to-one at `prov` 162–201.

No separately published Patreon UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). The 2023 brand announcement is quoted as Patreon-authored fact, not treated as absent.

## F2 — destination check

Rows above were written after the searches in this session, then remeasured after the B2a folds (line numbers unchanged). Dual destinations name both: homepage / pricing URLs at `DESIGN.md` 9 / 31 / 185 (`grep -o 'https://www.patreon.com'` dest 5; exact `/pricing` dest 3); `#000000` primary_color; favicon URL; YAML token note; live-extract; issued quotes that survive in both files. Compliance claims name only text that is present: B3 is claimed only because line 172 contains the five evidence kinds and the per-component gate. Complete B2a sites remain 40 = inventory 40. D1 locale: `locale matrix` DESIGN dest **0** / provenance dest **0**. `does not name` DESIGN dest **0**. `locale` DESIGN dest **1** (`content-locales` marker only). Harvested chip sentence remains at 513.

## A5 / A5a — issued-copy sweep

Trigger: gate `copy-loss` reported `compared: 0` / `candidates: 182` (바늘 0개). A5a hand sweep was therefore required.

Extracted issued or sibling-measured strings (needles): `Get started`, `Get Started`, `Get Started for Free`, `Start Your Patreon for Free`, `Find a Creator`, `Log in`, `Updates`, `Creators`, `Features`, `Pricing`, `Resources`, `PATREON`, `Where podcasts grow`, `Your wildest creative reality`, `Make it making art`, `Creator is now a career`, `Complete creative control`, `Where Creator Communities Thrive`, `Secure payments handled for you`, `We handle taxes`, `No setup headaches`, `10%`, the visual-language quote, the no-canonical-form quote, `motion-first for a digital-first world`, `animation, interactivity, contextual adaptation, and color transformation`, `heroizing creators in a way that feels inviting and warm`, `afford the freedom to do their best work, and the stability to build an independent creative career`, `shifts in density and softness depending on its context`, `to put creators in the spotlight`, and the eight color hexes.

Hand-sweep result: source-body issued copy 0 missing from `DESIGN.md`. Sibling-only issued copy (`English (United States)`, `Explore our features`, `Patreon pricing`, `Where Creator Communities Thrive — Patreon`, `Patreon Pricing Plans — Patreon`) is not in `DESIGN.md` and is recorded as sibling-only on `prov` 112–127 (disposition, not promotion). Denominator: 29 source-body issued strings extracted / 0 missing from portable body; 5 sibling-only strings extracted / 0 missing from the three-file set after ledger disposition.

`verdict: PASS` from a later gate, if obtained, is not itself an A5 compliance claim (A5a).

## Unique-phrase restoration (wave 43)

Pulled source-unique expressions (years, proper nouns, quotes, §11 causal last-paragraph unit, value qualifiers, §15/§7 constraints) and counted them in the portable body before submit. Extracted 153 expressions / 0 were missing at the count (none restored in a second pass because none were 0). Includes 2013 / Jack Conte / Sam Yam / Pomplamoose / YouTube ad revenue / `$4 billion` / `$10 billion` / October 2023 / Wolff Olins / Dinamo / Active Theory / David McLeod / the no-canonical-form quote / planned creator-mark tool / "The design is, in essence, a brand that gets out of the way."

## Token-path self-check

YAML `tokens.colors.*` (12 keys), `tokens.typography.*` (family display/body + 8 roles), `tokens.spacing` (8 steps), `tokens.rounded` (5 steps), `tokens.shadow.none`, and `tokens.components.*` (8 records) were each confirmed by searching the named path or the unitless `key: value` writing in `DESIGN.md`, not by counting a reused integer on another scale. `tokens.spacing.base: 16` is not a type size. `tokens.rounded.lg: 30` is not a type size. `tokens.rounded.pill: 45` is not search height `45px`. `tokens.rounded.sm: 15` is not the `15px` in `15px 21px`. `tokens.colors.primary` `#000000` is not collapsed into `tokens.colors.black`.
