# Corca Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating Corca (코르카) as a Korean AI company whose corporate site frames the business around the live homepage H1 “코르카는 AI 기술로 세상을 바꾸고 있습니다” (“Corca is changing the world with AI technology”), treating catalog homepage identity as `https://www.corca.ai/` rather than a third captured token surface, treating token extraction as `live-extract` for the two 2026-07-02 live-inspect surfaces, and treating the source token note’s Wix-built corporate-site reading as reconstruction coverage for those two surfaces rather than a published UI specification, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Corca (코르카) is a Korean AI company. Catalog homepage identity is `https://www.corca.ai/`. Token extraction is `live-extract`. Source token note: Wix-built corporate site; primary = navy ink `#1a2352` that anchors every section heading; `#046cb8` is the achievement/eyebrow blue; product surfaces (Moonlight) run a night-time deep-purple `#402060` + cream `#fff3b7` theme; headings use a custom uploaded display webfont; body/UI resolve to Pretendard + Wix Madefor.

Treating this contract as covering two current first-party live-inspect surfaces from 2026-07-02, keeping color, type, and component values attached to the surface that established them, treating `https://corca.co.kr/` as a brand-owned `.kr` host rather than a token-capture route, treating `https://github.com/corca-ai` as a brand-owned GitHub organization rather than a token-capture route, and treating Trace as a source-named sibling product rather than a captured token route, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. This contract covers two current first-party live-inspect surfaces from 2026-07-02: corporate homepage `https://www.corca.ai/` and Moonlight product surface `https://www.corca.ai/en/moonlight`. Color, type, and component values below stay attached to the surface that established them. `https://corca.co.kr/` is a brand-owned `.kr` host in the source footer; it stays outside this pass’s token-capture routes. `https://github.com/corca-ai` is a brand-owned GitHub organization in the source footer; it stays outside this pass’s token-capture routes. Source §11 names **Trace** as a sibling product in the lineup; Trace stays outside this pass’s token-capture routes.

The following atmosphere readings — young-benchmark-winning-research-lab-that-wants-to-read-as-both-credible-and-playful, dark-hero-with-a-large-white-`#ffffff`-~60px-headline-then-a-bright-white-canvas-with-navy-`#1a2352`-section-headings, Moonlight-deep-plum-purple-`#402060`-lit-by-cream-`#fff3b7`-that-also-becomes-the-active-nav-color, feature-bands-`#419eff`-`#faeeff`-`#fdd484`-`#d9e5f5`-`#36597d`, deliberately-optimistic-multi-hue-spectrum-that-keeps-a-dense-corporate-page-from-feeling-grey, fully-rounded-pills-white-50px-Try-it-now-and-transparent-999px-기사-보기-ringed-with-`1px solid #000000`, content-cards-10px-and-16px, live-`box-shadow-none`-across-hero-nav-and-cards, and custom-uploaded-display-webfont-for-headlines-over-Pretendard-and-Madefor-for-body-and-UI — are a derived editorial implementation inference from the verified surfaces; they are not Corca-authored or a separately published UI specification. Source §1 records the Wix-built corporate site as a young, benchmark-winning research lab that wants to read as both credible and playful. The captured homepage opens on a dark hero with a large white (`#ffffff`) ~60px headline, then a bright white canvas where section headings are set in navy ink (`#1a2352`). Moonlight runs a deep plum-purple stage (`#402060`) lit by moonlight cream (`#fff3b7`) that also becomes the active-nav color on that surface. Other feature bands pull in bright electric blue (`#419eff`), pale lavender (`#faeeff`), warm amber (`#fdd484`), pale sky (`#d9e5f5`), and steel blue (`#36597d`) — a deliberately optimistic, multi-hued spectrum that keeps a dense corporate page from feeling grey. Calls to action are fully rounded pills — a white 50px-radius “Try it now” fill on product pages, and a transparent 999px-radius outline pill (“기사 보기”) ringed with `1px solid #000000`. Content cards use 10px and 16px corners. Live inspection returned `box-shadow: none` across the hero, nav, and cards. Headlines use a custom uploaded display webfont; body and UI text resolve to Pretendard and Wix Madefor.

Treating the following first-party live-site product-scope facts as narrative rather than interface tokens — the-mission-sentence-as-the-site’s-H1-and-the-frame-for-everything-else, Corca-presenting-as-a-lab-that-ships-concrete-products-rather-than-a-horizontal-AI-platform, Moonlight-as-an-AI-colleague-for-research-papers, Trace-as-a-named-sibling, 2024-아기유니콘-as-a-Korean-government-backed-high-potential-startup-designation-and-세계-7위-국내-기업-중-1위-as-homepage-stated-claims, Moonlight-adoption-numbers-100,000+-1.7M+-150+-1,000+-as-social-proof, evidence-first-here-is-the-technology-here-is-who-recognized-it-here-is-how-many-people-use-it, live-homepage-and-Moonlight-providing-that-narrative-context-without-by-themselves-supplying-interface-tokens, and founding-details-beyond-what-the-site-states-are-not-independently-verified-in-the-source-turn — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Source §11 records that product-scope narrative. The homepage foregrounds selection as a 2024 아기유니콘 (Baby Unicorn) — a Korean government-backed high-potential-startup designation — and a benchmark result billed as “세계 7위, 국내 기업 중 1위”. Specific founding details beyond what the site states are not independently verified in the source turn.

The following refuse-and-embrace reading — refusing-the-heavy-shadow-stacked-chrome-of-legacy-enterprise-software-and-hype-that-isn’t-backed-by-a-number, embracing-a-trustworthy-navy-spine-per-product-color-worlds-flat-web-native-surfaces-and-big-proof-numbers-given-room — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Source §11 records that refuse/embrace contrast.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the following three captured-surface outcomes as Primary tasks — review the corporate homepage at `https://www.corca.ai/` (mission H1, product bands, achievement eyebrows); use the Moonlight product surface at `https://www.corca.ai/en/moonlight` (EN hero, stats, “Try it now”); follow captured homepage destination CTAs (“자세히 보기”, “기사 보기”) — and not lifting source §13 fictional archetypes into this list, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

- Review the corporate homepage at `https://www.corca.ai/` (mission H1, product bands, achievement eyebrows).
- Use the Moonlight product surface at `https://www.corca.ai/en/moonlight` (EN hero, stats, “Try it now”).
- Follow captured homepage destination CTAs (“자세히 보기”, “기사 보기”).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, tying observable work only to the three primary tasks, not substituting those entries, and treating named products Moonlight and Trace as offerings rather than researched persona segments, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. No individual personas are promoted. Source §13 is labeled fictional archetypes informed by publicly observable Corca product surfaces; names in that section are illustrative and do not refer to real people. Do not substitute those entries. Product scope names Moonlight and Trace as offerings, not as researched persona segments.

### Distinctive traits

Treating catalog `primary_color` `#1a2352` as heading navy rather than as `#000000` / `#181818` / `#324158` / `#046cb8`, treating canvas `#ffffff` as unmerged from hero-headline `#ffffff` and from primary-CTA fill `#ffffff` with same-hex-named-jobs-stay-separate, treating YAML `button-cta` `fg` `#1a2352` as inferred rather than a measured leaf label, and treating hover, focus, pressed, disabled, loading, error, and success paints as not promoted from the 2026-07-02 default computed-style notes, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

- Catalog `primary_color` / YAML `primary` `#1a2352` as section-heading navy on the captured homepage
- Canvas `#ffffff`; hero headline `#ffffff` on dark; primary CTA fill `#ffffff` with inferred navy label — same hex, named jobs stay separate
- Moonlight night world: deep purple `#402060` + cream `#fff3b7` (active nav on that surface)
- Achievement blue `#046cb8` on eyebrow labels (“2024 아기유니콘 선정!”, “세계 7위, 국내 기업 중 1위 쾌거!”)
- Multi-hue bands: `#419eff`, `#faeeff`, `#fdd484`, `#d9e5f5`, `#36597d`; themed card fills `#777cdc` and `#605070`
- Pill CTAs: filled 50px / height 60px or 51px; outline 999px / height 38px; padding `8px 15px`
- Flat: `box-shadow: none` on hero, nav, headings, and cards; 78px header
- Custom uploaded display webfont for headlines and stats; Pretendard sub-nav; Madefor UI chrome
- The 2026-07-02 inspect notes record default computed styles on the two surfaces. Hover, focus, pressed, disabled, loading, error, and success paints are not promoted from those notes

### Principles

These 5 items — Proof over adjectives, with the product-thesis reading that Corca states rankings and adoption numbers not superlatives and the UI implication to give metrics a large dedicated stat tier (46px) and let numbers carry credibility; A world per product, with the reading that Moonlight gets its purple-and-cream night theme while the corporate frame stays navy and the UI implication to theme each product surface distinctly but keep navy `#1a2352` as the shared connective tissue; Flat and fast, with the reading that modern web-native clarity beats decorative depth and the UI implication of no shadows and separation with full-width color blocks and whitespace; Present tense, plain claim, with the reading “We are changing the world,” not “we aspire to,” and the UI implication of declarative headlines, direct CTAs (“자세히 보기”, “Try it now”), no hedging; Heavy where it declares, quiet where it explains, with the UI implication of the display webfont for poster headlines and Pretendard / Madefor for calm, legible body and UI — are a derived editorial implementation inference from the verified surfaces; they are not Corca-authored or a separately published UI specification.

1. **Proof over adjectives.** Corca states rankings and adoption numbers, not superlatives. *UI implication:* give metrics a large, dedicated stat tier (46px) and let numbers carry credibility.
2. **A world per product.** Moonlight gets its purple-and-cream night theme; the corporate frame stays navy. *UI implication:* theme each product surface distinctly, but keep the navy `#1a2352` spine as the shared connective tissue.
3. **Flat and fast.** Modern web-native clarity beats decorative depth. *UI implication:* no shadows; separate with full-width color blocks and whitespace.
4. **Present tense, plain claim.** “We are changing the world,” not “we aspire to.” *UI implication:* declarative headlines, direct CTAs (“자세히 보기”, “Try it now”), no hedging.
5. **Heavy where it declares, quiet where it explains.** *UI implication:* the display webfont for poster headlines; Pretendard / Madefor for calm, legible body and UI.

Treating the following as a capture-bound application of source §7 Do’s — navy-ink-`#1a2352`-for-every-section-heading-it-is-the-brand-spine, achievement-blue-`#046cb8`-reserved-for-eyebrow-labels, Moonlight-as-deep-purple-`#402060`-plus-cream-`#fff3b7`, fully-rounded-pills-50px-filled-999px-outline, keep-the-system-flat-separate-with-color-blocks, headlines-in-the-heavy-display-webfont-and-body-in-Pretendard-/-Madefor, big-proof-numbers-in-a-46px-stat-tier, and white-`#ffffff`-fills-for-primary-CTAs-on-dark-or-colored-hero-bands — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

- Use navy ink (`#1a2352`) for every section heading — it is the brand spine.
- Reserve achievement blue (`#046cb8`) for eyebrow labels above credibility claims.
- Give each product its own color world (Moonlight = deep purple `#402060` + cream `#fff3b7`).
- Use fully rounded pills for CTAs — 50px filled, 999px outline.
- Keep the system flat — separate sections with color blocks, not shadows.
- Set headlines in the heavy display webfont and body in Pretendard / Madefor.
- Give big proof numbers their own large stat tier (46px).
- Use white (`#ffffff`) fills for primary CTAs sitting on dark or colored hero bands.

### Avoid

The following items copy source §7 Don’ts, including no-pure-black-for-headings-reserve-navy-`#1a2352`-keep-`#000000`-for-thin-outlines-and-nav-only, no-spread-achievement-blue-`#046cb8`-across-body-text-it-is-an-eyebrow-accent, no-drop-shadows-for-elevation-Corca-is-a-flat-shadow-free-system, no-sharp-square-corners-on-CTAs-CTAs-are-always-pills, no-mix-product-themes-on-one-surface, no-body-copy-in-the-display-webfont-Pretendard-and-Madefor-own-reading-and-UI-text, and no-bury-the-metrics-the-big-numbers-are-social-proof-and-deserve-room. They are a derived editorial implementation inference from the verified surfaces; they are not Corca-authored or a separately published UI specification.

- Don’t use pure black for headings — reserve navy `#1a2352`; keep `#000000` for thin outlines and nav only.
- Don’t spread the achievement blue (`#046cb8`) across body text — it is an eyebrow accent.
- Don’t add drop shadows for elevation — Corca is a flat, shadow-free system.
- Don’t use sharp/square corners on CTAs — CTAs are always pills.
- Don’t mix product themes on one surface — keep Moonlight’s purple/cream separate from the corporate navy.
- Don’t set body copy in the display webfont — Pretendard and Madefor own reading and UI text.
- Don’t bury the metrics — the big numbers are social proof and deserve room.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings — catalog-primary-`#1a2352`-as-heading-navy-not-`#000000`-not-`#181818`-not-`#324158`-not-`#046cb8`, canvas-`#ffffff`-unmerged-from-hero-headline-`#ffffff`-and-from-primary-CTA-fill-`#ffffff`, YAML-`button-cta`-`fg`-`#1a2352`-as-inferred-not-a-measured-leaf, Moonlight-cream-`#fff3b7`-unmerged-from-amber-`#fdd484`, card-violet-`#777cdc`-unmerged-from-card-plum-`#605070`, YAML-Pure-Black-`#000000`-as-thin-ring-not-Proof-nav-leaf, YAML-Soft-Ink-`#181818`-unmerged-from-Proof-nav-leaf, source-§2-maximum-contrast-nav-labels-as-source-naming-not-this-YAML-job, and component-fields-staying-on-those-controls-rather-than-as-extra-general-inks — are a derived editorial implementation inference from the verified surfaces; they are not Corca-authored or a separately published UI specification.

- **Corca Navy / primary** (`#1a2352`): catalog `primary_color`. YAML `primary`. Ink for section headings (H2) on the captured homepage.
- **Pure Black** (`#000000`): YAML `ink-pure`. Thin ring on outline pills. Source §2 also names maximum-contrast nav labels; Proof nav-leaf color is a separate raw sample and is not this YAML job.
- **Soft Ink** (`#181818`): YAML `ink-soft`. YAML / §4 top-nav item text. Unmerged from Proof nav-leaf `rgb(0,0,0)`.
- **Body Slate** (`#324158`): YAML `body-slate`. Secondary body copy and descriptive paragraphs.
- **Muted Grey** (`#bbbbbb`): YAML `muted`. Low-emphasis captions, meta, and disabled labels.
- **Achievement Blue** (`#046cb8`): YAML `accent-blue`. Eyebrow color above credibility claims (“2024 아기유니콘 선정!”, “세계 7위, 국내 기업 중 1위 쾌거!”).
- **Bright Blue** (`#419eff`): YAML `bright-blue`. Full-bleed section background on product surfaces.
- **Steel Blue** (`#36597d`): YAML `steel-blue`. Muted mid-blue for secondary illustration blocks and quieter bands.
- **Moonlight Cream** (`#fff3b7`): YAML `moonlight-cream`. Moonlight accent and active-nav text on the Moonlight surface.
- **Moonlight Deep** (`#402060`): YAML `moonlight-deep`. Deep plum-purple night stage behind the Moonlight hero.
- **Lavender** (`#faeeff`): YAML `lavender`. Pale lavender wash used as a soft full-width section background.
- **Amber** (`#fdd484`): YAML `amber`. Warm golden accent block used for highlight sections.
- **Sky** (`#d9e5f5`): YAML `sky`. Pale blue tint for soft section backgrounds and cards.
- **Card Violet** (`#777cdc`): YAML `card-violet`. Themed violet fill on the “자세히 보기” product-card button.
- **Card Plum** (`#605070`): YAML `card-plum`. Alternate themed plum fill on a sibling product-card button.
- **Pure White / canvas** (`#ffffff`): YAML `canvas`. Page canvas, white cards, hero headline text, and the filled CTA background. Same hex; YAML jobs stay named separately.
- **Surface Grey** (`#f1f5f9`): YAML `surface`. Cool light-grey surface for panels and grouped content cards.

Keeping those component fields on the controls rather than as extra general inks, and treating YAML `button-cta` `fg` `#1a2352` as an inferred dark-on-white choice from brand navy rather than a measured leaf label color, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. YAML `button-cta` `fg` `#1a2352` is inferred, not a measured leaf value. YAML component fields such as button-cta `bg` `#ffffff` stay on those controls.

### Spacing

Treating YAML `spacing` numbers as recorded without a required px suffix, treating body 8px / 15px / 16px / 24px / 48px / 64px as body-recorded observations rather than a converted YAML scale, and treating CTA padding `8px 15px` as a component field rather than a universal gutter, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. YAML `spacing`: xs 8, sm 15, md 16, base 24, lg 48, xl 64. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names a base rhythm of 8px and the scale 8px, 15px, 16px, 24px, 48px, 64px, with a notable `8px 15px` button padding measured on the pill CTAs.

### Shape

YAML `rounded`: sm 10, md 16, lg 24, pill 50, full 9999.

The following local-geometry reading, including 10px-surface-cards, 16px-feature-cards, YAML-`lg`-24-as-a-YAML-field-for-larger-containers, 50px-filled-CTAs, outline-`999px` unmerged-from-YAML-`full`-9999, badge-`8px` unmerged-from-YAML-`sm`-10, source-body-`9999px`-not-a-replacement-for-YAML-`full`-9999, and the-Proof-circular-radius-sample-as-Proof-only, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

- Small (YAML `sm` 10): surface cards, small panels — observed 10px
- Medium (YAML `md` 16): feature cards — observed 16px
- Large (YAML `lg` 24): YAML field for larger containers; not in the Proof radius-frequency scan
- Pill (YAML `pill` 50): filled CTAs — observed 50px
- Outline pill: `999px` on “기사 보기” — not YAML `full` 9999
- Badge eyebrow: 8px — not YAML `sm` 10
- YAML `full` 9999: YAML field. Source §5 also writes Full (`999px` / `9999px`): outline pills, circular icon buttons. `9999px` is that source-body form, not a replacement for YAML 9999. The Proof circular-radius sample stays on the Proof ledger; it is not this YAML number.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, headings, most surfaces |
| Color block (Level 1) | Full-width background color shift | Section separation without elevation |
| Ring (Level 2) | `1px solid #000000` outline pill | Low-emphasis article links |

YAML `shadow.none` is `none`. Live inspection returned `box-shadow: none` across the hero, navigation, headings, and cards.

Treating Corca as a near-shadowless flat system, treating depth as communicated by stacking full-width color bands (white, lavender `#faeeff`, amber `#fdd484`, deep purple `#402060`) rather than by elevation, and treating a tinted surface (`#f1f5f9`) or a colored fill as the distinct-card treatment rather than a drop shadow, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Source §6 records that shadow philosophy.

### Motion

Source §15 names three durations. Treating those durations as source-stated named tokens rather than per-component computed observations is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, pill press, focus |
| `motion-standard` | 220ms | Card / section reveal, dropdown, sheet |
| `motion-slow` | 340ms | Page-level transitions, hero reveal |

Treating the easing role names and uses from source §15 as source-stated roles rather than live transition captures, without promoting an exact curve, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Easing role names and uses from source §15, without promoting an exact curve:

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, sections, dropdowns |
| `ease-exit` | Dismissals |
| `ease-standard` | Two-way transitions |

Treating the exact curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` as omitted unattributed unnamed values in Named gaps rather than as promoted motion tokens is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Exact curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are omitted as unattributed. They are unnamed values in Named gaps, not promoted motion tokens.

Source §15 motion rules: motion is functional and calm; pill CTAs respond to press with a subtle scale/opacity shift; colored product bands and metrics fade-in from below at `motion-standard / ease-enter` as the reader scrolls; no bounce or spring. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant and scroll reveals become immediate; the site stays fully functional. Treating those sentences as source-stated motion rules rather than computed per-component evidence — including functional-and-calm, subtle-scale-opacity-shift, fade-in-from-below, no-bounce-or-spring, transitions-collapse-to-instant, scroll-reveals-become-immediate, and the-site-stays-fully-functional — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

Do not promote an easing curve, animation name, transition property, or a duration as computed per-component evidence until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-inspect-custom-uploaded-display-webfont-for-headlines-and-stats, Pretendard-resolved-sub-nav, Madefor-menu-chrome, computed-font-weight-400-because-uploaded-display-faces-bake-weight-into-the-font-file, declared-resolved-families-not-a-substitute-stack, and do-not-present-Pretendard-or-Madefor-as-the-uploaded-display-webfont, are a derived editorial implementation inference from the verified surfaces; they are not Corca-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Homepage hero H1 “코르카는 AI 기술로 세상을 바꾸고 있습니다.” is a custom uploaded display webfont at YAML 60px / 400 (heavy cut), white on dark. Section H2 navy `#1a2352` at 32px / 400. Stat numbers 46px / 400. Eyebrow YAML 30px / 400 in `#046cb8`. Nav YAML 17px / 400 Madefor / Pretendard. Sub-nav 15px / 400 Pretendard (`orig_pretendard_medium`). Body 16px / 400 Madefor. YAML `family.display` is `brand-webfont`; YAML `family.body` is Pretendard; YAML `family.ui` is Madefor. Computed `font-weight` reports `400` because the uploaded display faces bake weight into the font file. |
| Declared/resolved families — not a substitute stack | Sub-nav resolves to Pretendard. Menu chrome uses Wix Madefor (`madefor-text` 16px). Do not present those families as the uploaded display webfont. |

### Family

- **Display:** custom uploaded webfont (YAML `brand-webfont`; served by Wix as `wfont_…` faces) — headlines and stat numbers
- **Body:** `Pretendard` — dropdown / sub-nav (`orig_pretendard_medium`)
- **UI:** `Madefor` (Wix Madefor) — menu chrome and small labels

Treating the uploaded display webfont as headline-and-stat-only rather than a substitute for Pretendard or Madefor, treating Pretendard and Madefor as reading and UI faces rather than the display family, treating a system-face replacement of the uploaded display webfont on these captured surfaces as forbidden, treating inventing a typeface name for the unresolved Wix `wfont_…` display family as forbidden, and treating setting body copy in the display webfont as forbidden, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Do not replace the uploaded display webfont, Pretendard, or Madefor with a system face on these captured surfaces. Do not invent a typeface name for the unresolved Wix `wfont_…` display family. Do not set body copy in the display webfont.

### Type roles

Source YAML records no `lineHeight`. Treating size-local Proof samples as Proof-only rather than replacements for the YAML sizes 60 / 32 / 30 / 46 / 17, treating YAML nav `17px` as unmerged from the Moonlight active-nav Proof sample, and treating YAML eyebrow `30px` as unmerged from the Proof second-eyebrow sample, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. No line-height ratio is invented here.

| Role | Font | Size | Weight | Notes |
|---|---|---:|---:|---|
| Display Hero | display webfont | 60px | 400 (heavy cut) | Hero H1, white on dark hero |
| Stat Number | display webfont | 46px | 400 | Big metrics (100,000+, 1.7M+, 150+, 1,000+) |
| Section Heading | display webfont | 32px | 400 | Section H2, navy `#1a2352` |
| Sub-section | display webfont | 28px | 400 | Feature sub-heads |
| Eyebrow | display webfont | 30px | 400 | Achievement label, blue `#046cb8`. YAML size; the Proof second-eyebrow sample is not this row |
| Nav Item | Madefor / Pretendard | 17px | 400 | Primary top-nav item. YAML size; the Moonlight active-nav Proof sample is not this row |
| Sub-nav | Pretendard | 15px | 400 | Dropdown menu item |
| Body | Madefor | 16px | 400 | Body / UI reading text |

Source typography principles — heavy display, quiet body; navy owns headings; navy-warmth-keeps-a-benchmark-heavy-page-feeling-human; numbers are hero-sized at 46px; hangul-first body around 15–17px for-comfortable-dense-hangul with Pretendard as the resolved Korean face — are restated from source §3. Treating those four stems, including navy-warmth-keeps-a-benchmark-heavy-page-feeling-human and 15–17px-body-for-comfortable-dense-hangul, as source §3 principles rather than a separately published Corca type specification is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Section headings are `#1a2352`, not black — the warmth of the navy keeps a benchmark-heavy page feeling human. Body sits around 15–17px for comfortable dense hangul, with Pretendard as the resolved Korean face.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Catalog logo metadata is a Google favicon lookup, not a captured first-party mark.

Treating product screenshots and illustrations as carrying no shadow at all sizes is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Product screenshots and illustrations carry no shadow at all sizes.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, and treating YAML `components_harvested: true` as a harvest flag rather than complete state coverage, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

YAML `components_harvested` is true. Treating the 2026-07-02 inspect notes as default computed styles on the two surfaces, and treating hover, focus, pressed, and other interaction paints as omitted from those notes rather than as a complete interaction contract, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. The 2026-07-02 inspect notes record default computed styles on the two surfaces. Hover, focus, pressed, and other interaction paints for the harvested controls stay omitted from those notes.

Treating the following source §14 rows as source-stated state copy rather than live interaction captures — including no-clutter, a-calm-path-back, a-flat-pulse-consistent-with-the-shadowless-system, states-what-to-do-next-not-just-오류, not-a-bare-필수, no-celebratory-emoji, and pill-CTAs-fade-rather-than-turn-to-a-foreign-grey-preserving-the-brand-read — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Source §14 state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| Empty (no results) | White canvas with a single navy (`#1a2352`) line explaining there is nothing yet, plus one white pill CTA to take the next step. No clutter. |
| Empty (saved / history none) | Muted grey (`#bbbbbb`) single line noting nothing saved yet, with a calm path back. |
| Loading (content fetch) | Flat skeleton blocks on the `#f1f5f9` surface at final card dimensions (10–16px radius). No shadow shimmer — a flat pulse consistent with the shadowless system. |
| Loading (product compute) | Inline progress within the surface; previous values stay visible. |
| Error (request failed) | Inline message in navy `#1a2352` with a plain-language explanation and a retry — states what to do next, not just "오류". |
| Error (form validation) | Field-level message below the input describing what is valid, not a bare "필수". |
| Success (action complete) | Brief inline confirmation in a calm tone; next-step detail linked immediately below. No celebratory emoji. |
| Skeleton | `#f1f5f9` blocks at final dimensions with 10–16px radius, flat pulse. |
| Disabled | Muted grey (`#bbbbbb`) text on a reduced-opacity surface; pill CTAs fade rather than turn to a foreign grey, preserving the brand read. |

Treating YAML `button-cta` `fg` `#1a2352` as an inferred dark-on-white choice from brand navy, not a measured leaf label color, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. YAML `button-cta` `fg` `#1a2352` is inferred, not a measured leaf label color.

Treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, treating YAML `type: card` as having no interactive-kind confirmation for a §4.4 map, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source never records `focus-visible`; that applicability stays, and the visual treatment is omitted. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. White Feature Card and Surface Card have YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. This is not a complete state-coverage claim.

### Primary CTA (Try it now)

- Role: product-page primary pill CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#1a2352` (YAML `fg`; inferred, not a measured leaf label)
- Radius: 50px
- Padding: 8px 15px
- Height: 60px
- Font: 16px / 400
- Use: Product-page primary pill CTA — a white fill with a dark navy label sitting on the dark hero (“Try it now”)
- Observed: default computed style on Moonlight
- Surface: Moonlight

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the Moonlight “Try it now” pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary CTA can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this product-page primary CTA because exact destination, request, and outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a product-page primary pill CTA (“Try it now”); exact destination, request, and outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Learn-More (자세히 보기)

- Role: section “자세히 보기” filled pill
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#777cdc` (sibling card `#605070`)
- Text: `#ffffff`
- Radius: 50px
- Padding: 8px 15px
- Height: 51px
- Font: 16px / 400
- Use: Section “자세히 보기” filled pill; the fill is themed per product card (a sibling card uses `#605070`)
- Observed: default computed style on homepage
- Surface: homepage

Treating this “자세히 보기” pill as a destination/info action, so loading, error, and success are not-applicable on the control itself, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the homepage “자세히 보기” pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A section pill can be unavailable; visual treatment omitted |
| loading | not-applicable | A “자세히 보기” pill is a destination/info action; the control itself does not enter a loading state |
| error | not-applicable | Pill meaning is the destination action, not a request or validation failure on the control |
| success | not-applicable | Following the pill is not an action-outcome confirmation on the control |

### Outline Pill (기사 보기)

- Role: article-link outline pill
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#1a2352`
- Border: 1px solid `#000000`
- Radius: 999px
- Padding: 8px 15px
- Height: 38px
- Use: “기사 보기” article link — a low-emphasis outline pill with a thin dark ring
- Observed: default computed style on homepage
- Surface: homepage

Treating this “기사 보기” outline as a destination action, so loading, error, and success are not-applicable on the control itself, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the homepage “기사 보기” outline pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An outline pill can be unavailable; visual treatment omitted |
| loading | not-applicable | An “기사 보기” outline is a destination action; the control itself does not enter a loading state |
| error | not-applicable | Outline-action meaning is the destination action, not a request or validation failure on the control |
| success | not-applicable | Following the outline action is not an action-outcome confirmation on the control |

### White Feature Card

- Role: white product / feature card
- Type: card
- Anatomy: surface; heading 32px display webfont / `#1a2352`; body 16px Pretendard / `#324158`
- Background: `#ffffff`
- Radius: 16px
- Shadow: none
- Heading: 32px display webfont / `#1a2352`
- Body: 16px Pretendard / `#324158`
- Use: White product / feature card on the main canvas (flat, no shadow)
- Observed: default computed style on homepage
- Surface: homepage

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Surface Card

- Role: light-grey surface panel
- Type: card
- Anatomy: surface
- Background: `#f1f5f9`
- Radius: 10px
- Use: Light-grey surface panel grouping related content
- Observed: default computed style
- Surface: homepage / Moonlight section scan

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Achievement Eyebrow

- Role: blue eyebrow label above credibility headings
- Kind: non-interactive
- Type: badge
- Anatomy: label
- Text: `#046cb8`
- Radius: 8px
- Font: display webfont 30px / 400 (YAML eyebrow size)
- Use: Blue eyebrow label above credibility headings (“아기유니콘 선정”, “세계 7위”)
- Observed: default computed style on homepage
- Surface: homepage

Treating this achievement eyebrow as a status marker rather than a control, so kind is non-interactive and a state-applicability map is omitted, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Kind is non-interactive: an achievement eyebrow is a status marker, not a control. A state-applicability map is omitted.

### Navigation

- Role: top horizontal nav
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#ffffff`
- Text: `#181818`
- Font: 17px / 400 Madefor
- Height: 78px header
- Active: moonlight cream `#fff3b7` text on the active product surface (Moonlight)
- Use: Top horizontal nav (“제품 소개”, “회사 소개”, “채용”, “블로그”)
- Observed: default computed style; active as named on Moonlight
- Surface: homepage nav, Moonlight

Treating this top nav as a destination-select control, so loading, error, and success are not-applicable on the nav item itself, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as top navigation items |
| hover | applicable | Pointer-web nav item; visual treatment omitted |
| focus-visible | applicable | Interactive nav item; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Nav selection is not a validation or request-failure state on the item |
| success | not-applicable | Selecting a nav item is not an action-outcome confirmation on the item |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Treating YAML spacing numbers and the body 8px–64px scale as captured-surface figures rather than a cross-viewport specification, treating CTA `8px 15px` as a component field rather than a global gutter, treating full-width colored bands as source §5 layout on the two live-inspect surfaces rather than uninspected hosts beyond those two routes, treating the-centered-single-column-hero-as-anchored-by-the-oversized-display-headline, treating product-showcases-as-full-width-colored-bands-each-themed-to-its-product, treating feature-metric-cards-as-horizontal-rows-beneath-section-headings, and treating achievement-press-blocks-as-eyebrow-plus-navy-heading-plus-outline-기사-보기-pill, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. YAML spacing is xs 8, sm 15, md 16, base 24, lg 48, xl 64. Body layout names an 8px base and 8px, 15px, 16px, 24px, 48px, 64px. Centered single-column hero is anchored by the oversized display headline. Product showcases alternate as full-width colored bands (white, lavender `#faeeff`, amber `#fdd484`, deep purple `#402060`), each themed to its product. Feature/metric cards arrange in horizontal rows beneath section headings. Achievement/press blocks pair an eyebrow (`#046cb8`) + navy heading + outline “기사 보기” pill.

The following whitespace-and-rhythm readings — color-blocks-over-borders, breathing-room-despite-dense-corporate-content, and proof-gets-space-for-big-stat-numbers — are a derived editorial implementation inference from the verified surfaces; they are not Corca-authored or a separately published UI specification. Source §5 records that whitespace philosophy.

Treating source §8 Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` as source-stated layout rules rather than a captured cross-viewport pass is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero headline compresses, colored bands stack |
| Tablet | 640-1024px | Moderate padding, 2-up feature/metric cards |
| Desktop | 1024-1440px | Full layout, centered hero, multi-column feature bands |

Touch-target sentences from source §8: Pill CTAs at 51–60px height with 8px 15px padding; outline “기사 보기” pill at 38px height; nav items spaced within the 78px header. Collapsing strategy: oversized display headline scales down on mobile; colored product bands maintain full-width treatment, reduce internal padding; feature/metric rows: multi-column → stacked single column; big stat numbers wrap to a 2×2 grid on narrow viewports. Product screenshots and illustrations carry no shadow at all sizes; cards maintain their 10px / 16px radius across breakpoints. Treating those touch-target, collapsing, and image-behavior sentences as source §8 rules rather than a captured cross-viewport pass — including pill-CTAs-at-51–60px-height-with-8px-15px-padding, outline-기사-보기-pill-at-38px-height, nav-items-spaced-within-the-78px-header, oversized-display-headline-scales-down-on-mobile, colored-product-bands-maintain-full-width-treatment, reduce-internal-padding, feature-metric-rows-multi-column-to-stacked-single-column, big-stat-numbers-wrap-to-a-2×2-grid-on-narrow-viewports, illustrations-no-shadow-at-all-sizes, and cards-maintain-10px-16px-radius-across-breakpoints — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating Corca’s voice as confident, mission-framed, and proof-forward — a young AI lab that has receipts and is not shy about them — treating that register as a derived reading of the live headlines, treating the table below as observed guidance from live copy including Corporate-hero-declarative-present-tense, Product-headlines-plain-benefit-first, Achievement-eyebrows-proud-but-factual, CTAs-direct-low-friction, and Metrics-bare-numbers-as-proof, is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Source §10 records that voice reading. Copy samples below are verbatim from the live homepage and Moonlight surface (verified 2026-07-02).

| Context | Tone |
|---|---|
| Corporate hero | Declarative, present tense, mission-framed. "AI 기술로 세상을 바꾸고 있습니다." |
| Product headlines | Plain, benefit-first. "논문을 더 빠르고 쉽게 이해하기", "AI Colleague for Research Papers!" |
| Achievement eyebrows | Proud but factual. "2024 아기유니콘 선정!", "세계 7위, 국내 기업 중 1위 쾌거!" |
| CTAs | Direct, low-friction. "자세히 보기", "기사 보기", "Try it now". |
| Metrics | Bare numbers as proof. "100,000+", "1.7M+", "150+", "1,000+". |

Voice samples (verbatim from live surfaces, verified 2026-07-02):

- "코르카는 AI 기술로 세상을 바꾸고 있습니다." — homepage hero H1 (mission, present tense).
- "AI Colleague for Research Papers!" — Moonlight EN hero H1 (product promise).
- "세계 7위, 국내 기업 중 1위 쾌거!" — homepage achievement eyebrow.

Treating the following forbidden-register sentences as source §10 constraints rather than Corca-authored doctrine — including vague-hype-without-numbers, fear-based-urgency, buzzword-stacking-revolutionary-next-gen-cutting-edge, copy-that-hides-the-product-behind-jargon, and Corca-prefers-a-plain-claim-plus-a-verifiable-number — is a derived editorial implementation inference from the verified surfaces; it is not Corca-authored or a separately published UI specification. Forbidden register from source §10: vague hype without numbers, fear-based urgency, buzzword stacking ("revolutionary, next-gen, cutting-edge"), and copy that hides the product behind jargon. Corca prefers a plain claim plus a verifiable number.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- exact easing curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)`
- hover, focus, pressed, and `focus-visible` visual treatments on the harvested controls
- loading, error, and success visual treatments on Primary CTA (Try it now); exact leaf label color on that white pill
- `https://corca.co.kr/`, `https://github.com/corca-ai`, and Trace as token surfaces
- founding chronology and other corporate facts beyond the inspected surfaces
- motion transition properties, animation names, and per-component computed duration / easing / reduced-motion behavior — promote those as computed evidence only after per-component capture of all five: transition properties, animation name, duration, easing, and reduced-motion behavior
