T2 마이그레이션을 시작합니다. 지정된 규격·규칙집·골든 샘플부터 순서대로 읽고, 그 다음에 Corca legacy를 Core v2로 옮기겠습니다.세션 맥락은 T2 웨이브 작업입니다. 지정된 규격·규칙집·골든 샘플을 순서대로 읽고 Corca legacy를 옮기겠습니다.규격과 규칙집 v8을 확인했습니다. 골든 샘플 형태와 Corca legacy 본문을 이어서 읽겠습니다.Corca 폴더는 비어 있습니다. Proof 원장, 템플릿 커브, 최근 PASS 이관본 형태를 대조한 뒤 본문을 쓰겠습니다.`.verification.md` Proof가 있습니다. 이 원장과 게이트 스크립트를 읽고 값 손실 없이 옮기겠습니다.Proof sidecar를 채택하고, 측정값과 YAML 반올림을 합치지 않은 채로 DESIGN.md를 작성합니다.본문 초안을 썼습니다. 줄 번호를 확인한 뒤 provenance와 migration-log를 실제 dest에 맞춥니다.provenance 줄번호가 어긋난 곳을 실제 grep 결과에 맞춥니다. Proof sidecar 인용 번호도 원본과 같게 고칩니다.게이트가 Proof 전용 수치를 발명으로, `9999px` 누락을 손실로 잡았습니다. scope claim 검사 방식도 확인합니다.scope claim의 `not … surface` 패턴을 고치고, Proof 전용 px는 provenance로만 두며, 원문의 `9999px`를 본문에 복원합니다.Corca legacy `DESIGN.md`를 Core v2로 옮겼습니다. 산출물은 `docs/design-md-weight/migrated/corca/`의 `DESIGN.md`, `provenance.md`, `migration-log.md`입니다. 규칙집은 v8입니다.

보존한 핵심만 적으면 이렇습니다.

- 범위는 2026-07-02 라이브 검사 두 면(`https://www.corca.ai/`, `https://www.corca.ai/en/moonlight`)입니다. `corca.co.kr`·GitHub org·Trace는 이름만 두고 토큰 표면으로 올리지 않았습니다.
- YAML `type: button`×3, `card`×2, `tab`, `badge`는 컴포넌트별로 유지했습니다. 카드는 kind/map을 생략했고, Primary CTA의 loading/error/success는 생략, Learn-More·Outline·Nav는 역할상 not-applicable입니다.
- `Try it now` 라벨 `#1a2352`는 측정값이 아니라 inferred입니다. 무출처 cubic-bezier 3개는 빼고 120/220/340ms와 reduced-motion, B3 다섯 종류 게이트는 본문에 남겼습니다.
- §13 가상 persona는 본문·provenance 모두에 없습니다. Canonical Proof `web/references/corca/.verification.md`는 채택했고, 분수 px는 provenance-only입니다.

`--gate-only`는 PASS/`[]`, Portable Core는 `portable_core: true`입니다. SHA-256 `79580b2d981a83e916ad9e2ef1be4c0c766c64e22224700244903d69c3676f4f`. F3와 sol 의미 레인은 대기입니다. 카탈로그 채택이 아닙니다.

DONE migrated=1
