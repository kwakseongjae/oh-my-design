# kakaogames migration log

Source: `web/references/kakaogames/DESIGN.md`
Sibling read (not the migration input): `web/references/kakaogames/.verification.md`
Destination: `docs/design-md-weight/migrated/kakaogames/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/kakaogames/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `496f6aed4ef011278ee527b2015d988d811a170cb138ec3413bb6e7ca21e8516` (`web/references/kakaogames/DESIGN.md`). Sibling SHA-256 `a5d2b580edb5c33ced68021d3144a54bb24524fc25fd60bca5bc59763cf14ee4` (`web/references/kakaogames/.verification.md`). Worker-close portable DESIGN SHA-256 `307d8d5447cdebd61be4fc2f91b06d9ff11fa51f821be288b26ec60a199deeec`. Auditor-close portable DESIGN SHA-256 `03f763da814067444e08c69ce8385dc37560fc0b2e0538446c7d2a42a185ecbd`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope (inspected host); `primary_color` 옮김 → Scope + Foundations + hero arrow; `logo.type` / favicon slug 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# 카카오게임즈 Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. Catalog homepage `https://www.kakaogames.com/` is provenance-only (Identity; dest **0**). Inspected `https://kakaogames.com/` is dual: `DESIGN.md` 9/21 and provenance Surfaces (E2a). `#000000` is dual: `DESIGN.md` 34/74/76/186 (dest **4**) + `provenance.md` Identity (E2a). The Google s2 favicon slug is dual: `DESIGN.md` 150 + `provenance.md` Identity (E2a). |
| YAML `omd: "0.1"`, `verified`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source footer’s **Verified:** 2026-07-13 is in Freshness. These are ledger keys with no portable slot. `live-extract` dest **0** / provenance **2**. `components_harvested` dest **0** / provenance **3**. `ds.type` is absent from the source (`grep -oF 'ds.type'` dest 0 / provenance 1 as the recorded absence) and is not invented. |
| YAML `tokens.colors` (`ink` / `hero-on-dark` / `footer-muted`) | 옮김 → Foundations semantic color | `DESIGN.md` 76–78. Paths named: `tokens.colors.ink` dest **1** · `tokens.colors.hero-on-dark` dest **1** · `tokens.colors.footer-muted` dest **1**. `#000000` dest **4** · `#ffffff` dest **3** · `#898989` dest **3**. Browser-default `#0000ee` dest **1** at 80, not promoted as a brand color. |
| YAML `tokens.typography.family.ui: SUIT Variable` | 옮김 → Typography & Assets Family | `DESIGN.md` 131. Path `tokens.typography.family.ui` dest **1**. `SUIT Variable` dest **11**. |
| YAML `tokens.typography.hero` / `section-title` / `footer` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 140–142. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.2` dest **2** · `1.4` dest **2** · `| 1 (\`normal\`)` dest **1**. Computed spellings sit beside them: `60px` on the hero row, `normal` on the section-title row, `19.6px` on the footer row. All three YAML `use` strings restored verbatim (A1, A5): `Homepage desktop hero title only.` dest **1** · `Homepage section heading only.` dest **1** · `Homepage footer information only.` dest **1**. Selectors `.title-main.only-pc` / `.section__title` / `.footer-info__item` dest **1** each. Paths `tokens.typography.hero` / `section-title` / `footer` dest **1** each at 144. Hero metrics stay on the homepage desktop hero; they are not written onto the error utility surface. |
| YAML `tokens.spacing.card-bottom: 40` | 옮김 → Foundations spacing | Path kept separate from radius (easywallet-type check): `tokens.spacing.card-bottom` dest **1** at `DESIGN.md` 88. That 40 is not `tokens.rounded.news-item: 4` dest **2** at 90/101. |
| YAML `tokens.rounded.none: 0` / `news-item: 4` | 옮김 → Foundations shape | `DESIGN.md` 98–101. `tokens.rounded.none: 0` dest **1** at 101. `tokens.rounded.news-item: 4` dest **2** at 90/101. `tokens.components.home-game-card.radius: 0` dest **1** at 101 is named as a component key, not merged into the rounded scale. No news-item component was invented. |
| YAML `tokens.shadow.none` | 옮김 → Foundations elevation | `tokens.shadow.none` dest **1** at `DESIGN.md` 105. `box-shadow: none` dest **1** at 105. |
| YAML `tokens.components` (2 records) | 옮김 → Components & States | `DESIGN.md` 168–203. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`card\`` dest **1** at 171 = source YAML `type: card` **1**. `Primitive type: \`button\`` dest **1** at 183 = source YAML `type: button` **1**. `not in the token set` dest **0** (no §4-only component was given a borrowed type). Both YAML `use` strings restored: `home::div.card` dest **1** · `home::[data-omd-capture="1"]` dest **1**. YAML `states` string restored at 190/193. Height unitless `60` at 189; `60px × 60px` dest **3**. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 31–38. 2016 founding and the no-separate-rebrand sentence stay in Scope. Atmosphere readings carry the adjacent complete qualifier at 11 (B2/B2a). |
| §1 / §11 official-profile URL | 분리 → provenance; URL 옮김 → Experience Scope | `https://kakaogames.com/en-us/about/` is dual: Scope 9/22 + provenance Surfaces (E2a). SUIT license URL is dual: Scope 9 + Assets 151 + provenance Surfaces (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 76–80. Role-naming readings carry the adjacent qualifier at 74 (B2/B2a). `#0000ee` and transparent surfaces stay unpromoted at 80. |
| §3 Typography Rules — evidence classes, hierarchy, fallbacks | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 119–127; family 131–132; hierarchy table 140–142. 224 uses dest **2** at 36/123. `swiper-icons` dest **1**. AppleSDGothicNeo-Regular / 맑은 고딕 / 돋움 dest **2** each. SIL Open Font License 1.1 dest **2**. |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 168–203. 19 variants / zero interaction kinds / zero interaction snapshots at 162. Capture selectors are also in provenance. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. Conflicts unresolved: none — `DESIGN.md` does not invent a conflict; provenance Freshness records the source footer. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 208: promotional hero then content modules; 248px × 437px dest **5**; 40px bottom padding. The application-shell contrast is named by the adjacent qualifier at 210. Authenticated game-client grid / inventory layout / player profile arrangement stay as the source’s own non-assertion, not as a new domain. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 105. The observed-only flat reading carries the adjacent qualifier at 105 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 53–56, under the grouping qualifier at 51. Not placed in Governance controlled copy. Four items, matching the source Do list. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 62–66, under the qualifier at 60. Four Don’t-list items plus the §9 unique constraint at 66. No invented domain (`native application` dest **0** · `mobile app` dest **0** · `back-office` dest **0** · `product application` dest **0** · `measures 1440px` dest **0**). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 210: `1440px-wide` dest **2**. Exact string `measures 1440px` dest **0** / provenance **0**. The capture-width-not-breakpoint reading carries an adjacent qualifier at 210 (B2/B2a). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present or moved (A2, A3). The check is itemised at `provenance.md` §9 deletion check. Unique brand constraints `in-game navigation` dest **1** at 66 · `combat HUDs` dest **1** at 66 landed in Avoid. |
| §10 Voice & Tone | 옮김 → Content & Locales | Mission and values `DESIGN.md` 215; Do/Don't table 217–221; illustrative samples 223 under the source’s own “Illustrative, not official UI copy” label. The qualifier at 215 covers the “global, energetic, and inviting” characterization. The illustrative-sample reading is qualified at 223. `"Uniting the World Through Games"` dest **2** (13/215). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: **2016** dest **3**; **Europe** dest **2**; **Japan** dest **2**; publishing / development studios / partnerships; “Uniting the World Through Games”; **This narrative is company context, not proof of any unobserved visual token.** dest **1** at 13 (the paragraph’s last sentence is kept). Dual with `provenance.md` Proof notes (E2a). Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §12 Principles — 4 numbered | 옮김 → Experience principles | `DESIGN.md` 44–47 under the B2a form at 42. Company-authored value names stay as official-profile material; UI implications are the qualified derived layer. Kakao Games has no published first-party design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). |
| §13 Personas — 3 entries | 삭제 | The source’s own header labels them stakeholder archetypes derived from published audience and operating scope, not synthetic research personas. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, motivation, or affiliation classification appears in either output (D2, D2a). Identifier strings `Global player` dest **0** / provenance **0** · `Studio or publishing partner` dest **0** / provenance **0** · `Portfolio visitor` dest **0** / provenance **0**. Audience at `DESIGN.md` 27 carries only the group-level wording the source records independently of that section (global users, and publishing, development studios, and partnerships), under an adjacent qualifier. The two `primary-tasks` at 21–22 come from surfaces the source records; 19 says so and qualifies the step from surface to “primary task”. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source body preserved at `DESIGN.md` 160 (A2; the catalog graph is still 0/440, so nothing is delegated), including `/error/` as a public utility surface that does not provide selector-backed error-component styling. The applicability rule is at 164. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | The hero arrow declares the seven canonical states. loading / error / success close because the control is a carousel previous-arrow that commits no operation in place (`loading \| not-applicable` dest **1** at 201). hover and disabled stay applicable; treatments omitted. The game card gets no `kind` and no map because the source supplies no interaction evidence for the container — `DESIGN.md` 166 (C4). |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 109–111: no duration, easing curve, transition property, or animation-state value; carousel-related controls with zero interaction snapshots; no motion token promoted. The omission and the five-kind gate carry the adjacent complete qualifier at 109 (B2/B2a). No `cubic-bezier(0.4, 0.0, 1, 1)` dest **0** / provenance **0**. B3 is held: the promotion condition at `DESIGN.md` 111 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus the partial-confirmation exclusion (E2c). `transition properties` dest **1** at 111. |
| Opening product / atmosphere sentence (legacy §1) | 옮김 → Experience Scope | `DESIGN.md` 9–11: 카카오게임즈; mobile, PC online, and console; casual, subculture, and hardcore MMORPG; high-contrast editorial shell; `#000000` / `#ffffff` / `#898989`. |

## Sibling handling (`web/references/kakaogames/.verification.md`)

The sibling exists — confirmed with `find web/references/kakaogames -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: artifact timestamp `2026-07-13T15:02:24.705Z`; coverage score `65`; hero padding `30px 0px 0px`; section-title padding `0px 0px 28px`; arrow background `rgba(255, 255, 255, 0.05)`; `li.news-box__item` `650px × 230px`; full jsDelivr URL `https://cdn.jsdelivr.net/gh/sun-typeface/SUIT@2/fonts/variable/woff2/SUIT-Variable.woff2`; news-list and ESG-report URLs; evidence-limitation nouns social / customer-support / developer-console.
- Measured `DESIGN.md` 0 for those sibling-only strings: `2026-07-13T15:02:24.705Z` 0 · `650px × 230px` 0 · `rgba(255, 255, 255, 0.05)` 0 · `30px 0px 0px` 0 · `SUIT-Variable.woff2` 0 · `2024-kakaogames-esg` 0.

## A5 / A5a verification

The gate’s `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. 카카오게임즈 is Korean-and-English, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published Korean labels in the source body | 3 distinct | 0 | 0 | 카카오게임즈 / 맑은 고딕 / 돋움. |
| Published English / YAML `use` / mission / illustrative | 10 distinct | 0 | 0 | Uniting the World Through Games / Challenge through new experiences / Connection across players and regions / Expansion across genres and platforms / Joyful creation / Homepage desktop hero title only. / Homepage section heading only. / Homepage footer information only. / Discover a new world to play together. / Find your next adventure across platforms. / Share the game with players everywhere. |
| Sibling published strings | 0 sibling-only brand strings | 0 | 0 | Sibling names no additional published Kakao Games copy. News-list and ESG URLs are source-adjacent corporate URLs, kept in provenance only. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 카카오게임즈, 맑은 고딕, 돋움, Uniting the World Through Games, 2016, Europe, Japan, This narrative is company context, not proof of any unobserved visual token, Challenge through new experiences, Joyful creation, Discover a new world to play together.

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 3 Korean + 10 English/use/mission/illustrative extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand kakaogames --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 75 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 75.

## Deviations recorded

- `DESIGN.md` is above the spec’s 600–1,800-word SHOULD budget (`wc -w` 3707). The budget yielded to A1: two declared component records with dual `use`/`states` forms, a seven-state applicability matrix on the hero arrow, `tokens.rounded.news-item: 4` kept off `tokens.spacing.card-bottom: 40`, the full §11 founding narrative including its closing sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Kakao Games has no published first-party design system, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Auditor-close measure: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = **23** = `not Kakao Games-authored` 23 = `separately published UI specification` 23. Provenance derived ledger **23** rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is a Kakao Games-issued fact or a derived reading. Scope surface-boundary, atmosphere, narrative-as-context, primary-task selection, audience grouping and biography-drop, trait grouping, Principles / Do / Don’t headers, Semantic role names, spacing/shape key-path separation and news-item non-promotion, elevation observed-only reading, motion omission and five-kind gate, font evidence, fallback prohibition, type-role non-merge and unitless-ratio keep, favicon-as-pointer and SUIT-license-not-brand-asset, applicability procedure, layout application-shell and capture-width readings, voice characterization, illustrative-sample classification, and Named-gaps classification each carry an adjacent complete toss-form qualifier. Source facts (years, hexes, YAML keys, published strings, recorded geometry) were left unqualified. Auditor inventory: 23 lines.

Interpretive sentences checked and left as source facts, not newly qualified: the official-profile value names and the sentences that attribute them to the company (`DESIGN.md` 44–47); the §14 body at 160; the `#0000ee` non-promotion sentence at 80, which the source itself states. The §5 layout sentence at 208 is named by the qualifier at 210. No “does not say that anything measures …” denial was written (D1).

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep -oF` on the destination file named in that row. Dual destinations (homepage host forms, `#000000` at 34/74/76/186, favicon slug, About URL, SUIT license URL, §11 closing sentence) list both files (E2a). `224` dest **2** at 36/123. Compliance claims are no stronger than the body: “B3 is held” is used only because `DESIGN.md` 111 names all five evidence kinds and the per-component gate in full text (E2c).
