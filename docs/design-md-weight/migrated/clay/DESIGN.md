# Clay Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Clay is a go-to-market infrastructure company for teams that build revenue systems from data, agents, orchestration, execution, and governance. Catalog homepage identity is `https://www.clay.com`. Treating that product-origin sentence as first-party positioning narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

Treating the following URLs as the named evidence domains of this reconstruction, including this-contract-covers-three-current-first-party-public-marketing-pages-from-the-2026-07-13-packet, values-stay-attached-to-the-public-marketing-surface-that-established-them, public-home-not-a-stand-in-for-the-authenticated-application, public-marketing-not-a-stand-in-for-documentation-chrome, and partner-solutions-and-pricing-not-a-stand-in-for-uninspected-application-tables, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. This contract covers three current first-party public marketing pages from the 2026-07-13 packet: home `https://www.clay.com/` (YAML `home`, marketing), partner solutions `https://www.clay.com/partners/solutions` (YAML `surface-2`, marketing), and pricing `https://www.clay.com/pricing` (YAML `surface-3`, marketing). Color, type, and component values below stay attached to the surface that established them. A public-home value is not a stand-in for Clay’s authenticated application. A public-marketing value is not a stand-in for documentation chrome. Partner-solutions and pricing observations are not a stand-in for uninspected application tables.

Clay’s own account of the 2026 redesign, at `https://www.clay.com/blog/new-homepage-2026`, says the old page no longer represented the breadth of the product and that the current public homepage frames that work as infrastructure rather than a single enrichment tool. Treating that first-party marketing narrative as reconstruction coverage rather than interface tokens, including prior-site-as-data-enrichment-offer, new-public-position-as-GTM-infrastructure, five-primitives-as-connected-building-blocks, visual-transition-as-continuity-rather-than-a-reset, Rube-Goldberg-machine-as-playful-understandable-metaphor, and first-party-marketing-narrative-does-not-by-itself-supply-interface-tokens-or-historical-company-facts-beyond-itself, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. The new public position is GTM infrastructure: teams combine data, agents, orchestration, execution, and governance to build their own revenue systems. Clay explains the visual transition as continuity rather than a reset, using a Rube Goldberg machine to make an interconnected engineered system feel playful and understandable. The source is a first-party marketing narrative; that account does not, by itself, supply interface tokens or historical company facts beyond itself.

The following atmosphere readings — crisp-neutral-base, playful-local-accents, soft-geometry, system-plus-illustration-story, almost-paper-white-field, unusually-cheerful-color-moments, intended-tension-make-infrastructure-and-engineering-feel-fun-and-creative, official-redesign-account-as-narrative-context-not-interface-tokens, captured-public-pages-answer-that-technical-story, this-reference-deliberately-bounded-to-the-three-captured-public-marketing-pages, and does-not-generalize-their-visual-system-into-the-authenticated-product-or-documentation-chrome — are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification. The captured public pages answer that technical story with a light, almost paper-white field, black type and actions, rounded compact controls, and unusually cheerful color moments in tabs, cards, and pricing. Clay describes the intended tension directly: make infrastructure and engineering feel fun and creative. Official redesign writing provides that narrative context; it does not by itself supply interface tokens. This reference is deliberately bounded to the three captured public marketing pages. It does not generalize their visual system into the authenticated product or documentation chrome, neither of which was supplied as evidence.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the YAML compact-action `use` string and the two §4 Use strings below as Primary tasks, and not lifting §13 GTM-engineer / GTM-leader groups, and treating independently-verified-in-this-packet as the three captured public-marketing URLs rather than the harvested header-action / tab / select uses, which remain harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Independently verified in this packet are the three captured public-marketing URLs; named header-action, tab, and select uses are harvested strings/controls, not independently verified destination routes.

- Compact public-marketing header action (YAML use: `Compact public-marketing header action`).
- Use a public home tab (Use: public home tab; evidence `home::[data-omd-capture="116"]`).
- Use a pricing-page select (Use: pricing-page select; evidence `surface-3::[data-omd-capture="104"]`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no named or fictional personas are promoted, treating source §13 first-party positioning as an audience boundary rather than verified biographical personas, treating GTM engineers and GTM leaders as not Audience biographies and not primary tasks, tying observable work only to the three primary tasks, and treating this-is-an-audience-boundary-not-a-substitute-for-Clay’s-user-research, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. No named or fictional personas are asserted. First-party positioning identifies GTM engineers and GTM leaders as the public audience for systems built from the five primitives; its examples span revenue-critical work such as RevOps, growth, customer success, and data-oriented operations. This is an audience boundary, not a substitute for Clay’s user research.

### Distinctive traits

The following unmerged-role extras — treating catalog `primary_color` `#000000` as Ink and the dark compact header-action fill rather than tab text `#1b1a18` and rather than Focus `rgb(2, 2, 2)` or Pressed `rgb(9, 10, 12)`, treating Paper canvas `#fefdfb` as unmerged from pricing-select `#ffffff` and from on-dark action text `#ffffff`, treating Oat action `#f3f2ed` as unmerged from default tab fill `#f4f3f0` and from Disabled `rgb(244, 243, 240)`, treating `#aaebfd` as an observed active tab fill rather than a global app-color scale, treating YAML compact-action `13.92` / `20.88` / `-0.1392` as unmerged from tab `16px` / `24px` / `normal` and from logo-card `16px` / `400` / `24px` and from pricing-select `14px` / `400`, treating YAML rounded action/tab `12` as unmerged from logo-card `18px` and from pricing-select `8px`, and treating `1px 0px solid` mixed logo-card borders as unmerged from `1px solid transparent` header-action borders and from `1px solid #d1cdc7` select borders — are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification.

- `#fefdfb` paper field with `#000000` type and actions
- Roobertvf loaded on the three captured public-marketing pages, with 1,564 visible uses and two Clay-hosted WOFF2 sources
- Compact header actions at YAML `13.92` / `500` / `20.88` / `-0.1392` and `12` corners; logo-card `18px`; pricing select `8px`
- Active home tab fill `#aaebfd` as a local story accent, not a global app-color scale
- Collector `interactionCount: 0`; pseudo-state snapshots for header actions and the pricing select only

### Principles

These 3 items are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification.

1. **Systems over a single feature.** Clay’s published five-primitives framing connects data through governance. *Reference UI implication:* explain relationships and flow before decorating isolated widgets.
2. **Customer-specific building blocks.** Clay says each GTM system looks different. *Reference UI implication:* favor composable examples over a one-size-fits-all outcome claim.
3. **Technical clarity with play.** Clay describes the challenge of making infrastructure fun and creative. *Reference UI implication:* use an intentional, concrete visual metaphor only when it clarifies the system.

The UI implications are this reference’s interpretation of official positioning, not published Clay product principles. Treating those UI-implication tails as a derived editorial implementation inference rather than a separately published Clay UI specification, including explain-relationships-and-flow-before-decorating-isolated-widgets, favor-composable-examples-over-a-one-size-fits-all-outcome-claim, and use-an-intentional-concrete-visual-metaphor-only-when-it-clarifies-the-system, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do’s, including public-marketing-neutral-base-crisp-and-legible, bright-tab-partner-and-pricing-colors-as-local-story-accents, 12px-compact-action-and-tab-corners-only-in-the-public-contexts-documented-above, and preserve-Roobertvf-disclose-unavailable-or-declared-only-faces, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

- Keep the public-marketing neutral base (`#fefdfb`, `#000000`, and oat action `#f3f2ed`) crisp and legible.
- Treat bright tab, partner, and pricing colors as local story accents unless another surface verifies a broader role.
- Use the measured 12px compact-action and tab corners only in the public contexts documented above.
- Preserve Roobertvf as the observed web family; disclose unavailable or declared-only faces rather than substituting them.

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including no-public-marketing-colors-or-components-on-authenticated-product-without-product-surface-evidence, no-pseudo-state-snapshots-as-animation-focus-ring-or-accessibility-support, no-Canela-Inter-Space-Mono-or-Roobert-Mono-from-declarations-to-live-UI-fonts, no-mobile-breakpoints-or-universal-card-radius-from-the-single-captured-viewport, not-a-clone-of-Clay’s-authenticated-application, paper-white-cards-black-compact-calls-to-action-12px-control-corners-and-one-locally-meaningful-bright-tab-or-plan-accent, and Roobertvf-only-when-actually-available-and-licensed-otherwise-leave-the-family-unresolved. They are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification.

- Do not apply public marketing colors or components to Clay’s authenticated product without product-surface evidence.
- Do not turn the recorded pseudo-state snapshots into claims about animation, focus-ring behavior, or accessibility support.
- Do not promote Canela, Inter, Space Mono, or Roobert Mono from declarations to live UI fonts without a computed loaded-use match.
- Do not infer mobile breakpoints or a universal card radius from the single captured viewport.
- Do not use this reference as a clone of Clay’s authenticated application.
- Do not present a substitute as Roobert; keep typography to the observed Roobertvf web family only when it is actually available and licensed for the target project, otherwise leave the family unresolved.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#000000`-as-Ink-and-dark-compact-fill-not-tab-text-`#1b1a18`-not-Focus-`rgb(2, 2, 2)`-not-Pressed-`rgb(9, 10, 12)`, Paper-canvas-`#fefdfb`-not-pricing-select-`#ffffff`-not-on-dark-`#ffffff`, Oat-action-`#f3f2ed`-not-default-tab-`#f4f3f0`-not-Disabled-`rgb(244, 243, 240)`, muted-navigation-`#79756d`-not-Ink, `#aaebfd`-as-observed-active-tab-fill-not-a-global-app-color-scale, partner-testimonial-fills-and-pricing-plan-colors-local-to-observed-components, YAML-component-fields-staying-on-those-controls, and observed-card-surface-not-a-claim-about-an-authenticated-app-canvas — are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification. Catalog `primary_color` `#000000` is YAML `ink` and the dark compact header-action fill; it is not tab text `#1b1a18` and not the recorded Focus or Pressed snapshots. YAML `canvas` `#fefdfb` is the home customer-logo card fill (`home::div.logo-card`); it is an observed card surface, not a claim about an authenticated app canvas. YAML `oat-action` `#f3f2ed` is the compact light header action on home, partner-solutions, and pricing. YAML `nav-muted` `#79756d` is home navigation-content label text. YAML `tab-default` `#f4f3f0` is home `.tab-btn` default. YAML `tab-active` `#aaebfd` is home `.tab-btn.cc-active`. Other bright surfaces appear in the bundle—for example partner testimonial fills and pricing-plan colors—but each is local to its observed component. They are not promoted to a universal status or product palette.

- **Ink / catalog primary** (`#000000`): body, navigation, and the compact dark header action across all three captured pages. Catalog `primary_color`. YAML `ink`.
- **Paper canvas** (`#fefdfb`): YAML `canvas`. Home customer-logo card fill. Observed card surface, not an authenticated-app canvas.
- **Oat action** (`#f3f2ed`): YAML `oat-action`. Compact light header action on home, partner-solutions, and pricing.
- **Muted navigation label** (`#79756d`): YAML `nav-muted`. Home navigation-content label text.
- **Default tab fill** (`#f4f3f0`): YAML `tab-default`. Home `.tab-btn` default.
- **Active tab fill** (`#aaebfd`): YAML `tab-active`. Home `.tab-btn.cc-active`. Local story accent, not a global app-color scale.
- **On-dark action text** (`#ffffff`): compact dark header-action label. Same hex as pricing-select fill; named jobs stay unmerged.
- **Pricing-select fill** (`#ffffff`): pricing-page select background. Not Paper canvas `#fefdfb`.
- **Tab text** (`#1b1a18`): default and active public home tab label. Not Ink `#000000`.
- **Pricing-select border** (`#d1cdc7`): `1px solid #d1cdc7` on the pricing-page select. Not logo-card mixed borders.

### Spacing

Treating YAML `spacing` action-y 8 / action-x 16 as recorded without a required px suffix, including none-is-added-here, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. YAML `spacing`: action-y 8, action-x 16. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names 8px vertical and 16px horizontal padding on the repeated compact header pair at the captured 1440×900 viewport. Observed home tabs use 10px 16px padding; logo cards use 16px 20px. YAML logo-card `padding` is already `16px 20px`. Pricing-select padding is `12px 16px 12px 40px`.

Treating those YAML numbers as recorded without a required px suffix, treating body 8px / 16px / 10px 16px / 16px 20px / `12px 16px 12px 40px` as body-recorded observations rather than a converted YAML scale, treating harvested control padding as component fields rather than replacements for YAML `action-y` / `action-x`, and treating no-site-wide-spacing-scale-as-asserted-from-those-local-values, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. No site-wide spacing scale is asserted from those local values.

### Shape

YAML `rounded`: action 12, tab 12, logo-card 18. YAML logo-card `radius` is already `18px`.

The following local-geometry reading, including compact-action-and-tab-12, logo-card-18px, pricing-select-8px, and not-a-universal-card-radius, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

- Compact action (YAML `action` 12): small public header actions at 12px
- Tab (YAML `tab` 12): public home tabs at 12px
- Logo card (YAML `logo-card` 18 / YAML `radius` `18px`): home customer-logo card at 18px
- Pricing select: `8px` (body-only; YAML has no select radius)

Treating 12 / 18 / `18px` / `8px` as local harvested geometry rather than a universal card radius for every unlisted control, and treating 12px compact-action and tab corners as not transferable to Clay’s authenticated product, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

### Elevation

Captured customer-logo card, tabs, and header actions report `boxShadow: none`. Treating no-reusable-elevation-ladder-or-shadow-token-is-established is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. No reusable elevation ladder or shadow token is established.

The following elevation readings, including color-border-treatment-and-corner-radius-not-shadow-carry-the-observed-separation, are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification. Color, border treatment, and corner radius—not shadow—carry the observed separation on these public surfaces.

### Motion

The evidence bundle contains no observed interaction actions (`interactionCount: 0`) and no measured transition or easing values. Although pseudo-state snapshots exist for some public actions, no motion token, duration, or reduced-motion rule is established. Treating that source-stated absence, including source-stated-absence, no-motion-token-duration-or-reduced-motion-rule-is-established, and pseudo-state-snapshots-are-visual-snapshots-not-evidence-of-motion-transition-timing-or-a-full-interaction-contract, as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. No motion token is promoted.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Roobertvf-live-public-web-1564-with-two-Clay-hosted-WOFF2-sources, sole-machine-UI-family-token-because-computed-usage-FontFaceSet-and-source-URLs-agree, Displaay-licence-as-foundry-terms-not-a-reusable-Clay-asset-licence, declared-only-Canela-Canela-Web-Inter-Roobert-static-Roobert-mono-Space-Mono-Phosphor-and-Webflow-icon-faces, and authenticated-product-and-documentation-chrome-typography-unnamed, are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed public-web use | Visible text on the three captured marketing pages resolves to `Roobertvf, Arial, sans-serif`. The collector reports `Roobertvf` as loaded with high confidence, 1,564 visible uses, and two Clay-hosted WOFF2 sources. YAML `family.ui` `Roobertvf`. It is the sole machine UI-family token here because computed usage, FontFaceSet status, and source URLs agree. |
| Official foundry and licence context | Displaay identifies Roobert as a mono-linear geometric sans with variable weight, slant, and mono axes. Its web licence covers WOFF/WOFF2 use through `@font-face`; that licence describes the foundry’s terms, not a reusable Clay asset licence. |
| Declared-only | Canela, Canela Web, Inter, Roobert (static family name), Roobert mono, Space Mono, Phosphor, and Webflow icon faces appear in declarations, but the supplied collector found no visible loaded use for them. They remain declared-only and are not substituted or promoted to UI tokens. |
| Unobserved domains | No authenticated product surface or documentation chrome was captured, so this reference makes no product-font or docs-font claim. |

### Family

- **Current visible UI family:** `Roobertvf, Arial, sans-serif` — YAML `family.ui` `Roobertvf`
- **Loaded source boundary:** `Roobertvf` is loadable on the three captured public-marketing pages (collector: loaded, high confidence, two Clay-hosted WOFF2 sources).

The following font-use boundary, including Arial-not-Roobertvf, declared-only-Canela-Inter-Space-Mono-Roobert-static-Roobert-mono-Phosphor-Webflow-icon-faces-not-loaded-Clay-faces, do-not-replace-unavailable-or-unobserved-brand-type-with-Arial, and Roobertvf-only-when-actually-available-and-licensed-otherwise-leave-the-family-unresolved, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Do not present Arial as Roobertvf. Do not present Canela, Inter, Space Mono, Roobert (static), Roobert mono, Phosphor, or Webflow icon faces as loaded Clay faces. Do not replace unavailable or unobserved brand type with Arial. Keep typography to the observed Roobertvf web family only when it is actually available and licensed for the target project; otherwise leave the family unresolved rather than presenting a substitute as Roobert.

### Type roles

YAML `compact-action` records size `13.92`, weight `500`, lineHeight `20.88`, tracking `-0.1392` as numbers without a px suffix. Body measures the same role as `13.92px` / `20.88px` / `-0.1392px`. Treating those YAML numbers as recorded without a required px suffix, treating body `13.92px` / `20.88px` / `-0.1392px` as size-local observations rather than a converted ratio, treating tab `16px` / `500` / `24px` / `normal` as unmerged from compact-action, treating logo-card `16px` / `400` / `24px` / `normal` as unmerged from tab `16px` / `500`, treating pricing-select `14px` / `400` as a component-local field rather than a YAML type role, treating yaml-has-no-unitless-lineHeight-none-invented, and treating yaml-lineHeight-20.88-preserved-as-that-number-not-rewritten-as-a-unitless-ratio, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. YAML `lineHeight` `20.88` is preserved as that number; it is not rewritten as a unitless ratio. Tab and logo-card line heights are body-table observations; YAML has no `lineHeight` field for them, and none is invented as a ratio.

| Role | Font | Size | Weight | Line height | Tracking | Use (YAML) / Evidence boundary |
|---|---|---:|---:|---:|---:|---|
| Compact header action | Roobertvf | YAML `13.92` / body `13.92px` | 500 | YAML `20.88` / body `20.88px` | YAML `-0.1392` / body `-0.1392px` | YAML use: `Compact public-marketing header action`. `home::[data-omd-capture="9"]` and `"10"` |
| Tab control | Roobertvf | 16px | 500 | 24px | normal | Public home tab. `home::[data-omd-capture="116"]` |
| Logo card text context | Roobertvf | 16px | 400 | 24px | normal | `home::div.logo-card` |

### Assets

Treating catalog logo metadata as type `github`, slug `clay-run`, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

Treating customer logos on the home customer-logo card as captured/recorded public-marketing-surface imagery; ownership unclaimed, not replacing them with invented brand-color decoration, and treating the playful Rube Goldberg visual as first-party redesign brand context rather than a token claim, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, including because-no-authenticated-product-empty-loading-success-or-error-state-surface-was-captured, only-public-marketing-pseudo-state-snapshots-for-the-header-actions-and-the-pricing-select, interactionCount-0, must-not-be-extrapolated-into-application-state-guidance, all-variants-below-are-public-marketing-observations, and pseudo-state-values-are-visual-snapshots-not-evidence-of-motion-transition-timing-or-a-full-interaction-contract, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted: No authenticated product, empty, loading, success, or error-state surface was captured. The only state evidence retained in §4 is the collector’s public-marketing pseudo-state snapshots for the header actions and the pricing select; it must not be extrapolated into application-state guidance. All variants below are public-marketing observations. The bundle reports `interactionCount: 0`; pseudo-state values are visual snapshots, not evidence of motion, transition timing, or a full interaction contract.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating recorded Focus snapshots as not `focus-visible` evidence, treating Pressed as an additional named observed state rather than a Core applicability row or a `focus-visible` treatment, treating the active tab color as a captured variant rather than a Core row, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, and treating Customer-logo card as having no interactive-kind confirmation, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records Focus snapshots (`rgb(2, 2, 2)` and `rgb(242, 241, 236)` backgrounds); those are not `focus-visible` evidence. `focus-visible` visual treatment remains omitted. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Pressed (header actions) and Focus (header actions) are additional named observed states; they are not among the seven Core applicability rows and are not copied onto a `focus-visible` row.

Treating the active public home tab `#aaebfd` as a captured color variant rather than a Core applicability row is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. The active public home tab is a captured color variant (`#aaebfd`), not a Core applicability row.

Treating Customer-logo card YAML `type: card` with no interactive-kind confirmation so kind and a state-applicability map are omitted, and treating header-action-marketing-tab-and-pricing-select YAML-type-as-absent-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Customer-logo card has YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Header action, Marketing tab, and Pricing select have no YAML `type`; none is invented.

### Dark compact header action

- Role: compact public header action on `home`, `surface-2`, and `surface-3`
- Kind: interactive
- Anatomy: label
- Background: `#000000`
- Text: `#ffffff`
- Border: `1px solid transparent`
- Radius: `12px`
- Padding: `8px 16px`
- Font: `13.92px / 500 / Roobertvf`
- Use: compact public header action on `home`, `surface-2`, and `surface-3`; default evidence `home::[data-omd-capture="10"]`
- YAML use: `Compact public-marketing header action`
- Observed: default; Pressed `rgb(9, 10, 12)` background at `home::[data-omd-capture="10"]::state-pressed`; Focus `rgb(2, 2, 2)` background at `home::[data-omd-capture="10"]::state-focus`
- Field note: The following unmerged-field reading, including this-fill-as-catalog-`primary_color`-not-tab-text-`#1b1a18`, on-fill-white-not-Paper-canvas-`#fefdfb`, 12px-not-logo-card-18px-not-select-8px, 13.92px-500-not-tab-16px-500, `1px solid transparent`-not-select-`#d1cdc7`, Pressed-and-Focus-as-additional-named-observed-states-not-Core-rows, and Focus-`rgb(2, 2, 2)`-not-that-Core-row, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. `#000000` is this control’s fill and catalog `primary_color`. `#ffffff` is this control’s on-fill label. Radius `12px` is this compact action (YAML `action` 12); it is not logo-card `18px` or select `8px`. Font 13.92px / 500 is this control’s field, not Tab control 16px / 500. Focus snapshots on this control are not that Core row.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the compact dark public header action on home, partner-solutions, and pricing |
| hover | applicable | Pointer-web header action; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A compact header action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a compact public header action; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Treating Pressed `rgb(9, 10, 12)` and Focus `rgb(2, 2, 2)` as additional named observed states rather than Core applicability rows, and treating Focus as not a `focus-visible` treatment, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Additional observed named states: Pressed `rgb(9, 10, 12)` background; Focus `rgb(2, 2, 2)` background. Neither is a Core applicability row. Focus is not a `focus-visible` treatment.

### Oat compact header action

- Role: paired compact public header action
- Kind: interactive
- Anatomy: label
- Background: `#f3f2ed`
- Text: `#000000`
- Border: `1px solid transparent`
- Radius: `12px`
- Padding: `8px 16px`
- Font: `13.92px / 500 / Roobertvf`
- Use: paired compact public header action; default evidence `home::[data-omd-capture="9"]`
- YAML use: `Compact public-marketing header action`
- Observed: default; Hover `rgb(237, 234, 227)` background at `home::[data-omd-capture="9"]::state-hover`; Pressed `rgb(233, 229, 221)` background at `home::[data-omd-capture="9"]::state-pressed`; Focus `rgb(242, 241, 236)` background at `home::[data-omd-capture="9"]::state-focus`
- Field note: The following unmerged-field reading, including Oat-`#f3f2ed`-not-tab-default-`#f4f3f0`-not-Disabled-`rgb(244, 243, 240)`, label-`#000000`-as-this-control-fg-not-a-second-Ink-paint, 12px-not-logo-card-18px, Hover-`rgb(237, 234, 227)`-as-captured-hover-not-Pressed, and Focus-`rgb(242, 241, 236)`-not-that-Core-row, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. `#f3f2ed` is this control’s fill (Oat action). `#000000` is this control’s label. Radius `12px` matches the dark compact action; it is not logo-card `18px`. Hover `rgb(237, 234, 227)` is the captured hover snapshot for this control. Focus snapshots on this control are not that Core row.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the paired compact public header action |
| hover | applicable | Pointer-web header action; captured Hover `rgb(237, 234, 227)` background |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A compact header action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a paired compact public header action; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Treating Pressed `rgb(233, 229, 221)` and Focus `rgb(242, 241, 236)` as additional named observed states rather than Core applicability rows, and treating Focus as not a `focus-visible` treatment, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Additional observed named states: Pressed `rgb(233, 229, 221)` background; Focus `rgb(242, 241, 236)` background. Neither is a Core applicability row. Focus is not a `focus-visible` treatment.

### Marketing tab

- Role: public home tab
- Kind: interactive
- Anatomy: label
- Background: `#f4f3f0` (default); `#aaebfd` (active color variant)
- Text: `#1b1a18`
- Radius: `12px`
- Padding: `10px 16px`
- Font: `16px / 500 / Roobertvf`
- Use: public home tab; default evidence `home::[data-omd-capture="116"]`; active evidence `home::[data-omd-capture="123"]`
- Observed: default; active color variant
- Field note: The following unmerged-field reading, including default-`#f4f3f0`-not-Oat-`#f3f2ed`-not-Disabled-`rgb(244, 243, 240)`, active-`#aaebfd`-as-captured-color-variant-not-a-Core-row-not-a-global-app-color-scale, text-`#1b1a18`-not-Ink-`#000000`, 12px-not-logo-card-18px, 10px-16px-not-header-8px-16px, and 16px-500-not-compact-action-13.92-500, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. `#f4f3f0` is this tab’s default fill. `#aaebfd` is the active color variant, not a Core applicability row. `#1b1a18` is this tab’s label. Radius `12px` is this tab (YAML `tab` 12). Font 16px / 500 is this control’s field, not Compact header action 13.92px / 500. The active color variant is not that Core row.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public home tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A public home tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A public home tab selects a tab; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is default versus active color variant, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is tab selection, not action-outcome confirmation |

Treating the additional captured variant active color `#aaebfd` at `home::[data-omd-capture="123"]` as not a Core applicability row is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Additional captured variant: active color `#aaebfd` at `home::[data-omd-capture="123"]`. That variant is not a Core applicability row.

### Customer-logo card

- Role: customer-logo card on the public home marketing surface
- Type: card
- Anatomy: surface
- Background: `#fefdfb`
- Border: `1px 0px solid` with observed mixed border colors
- Radius: `18px`
- Padding: `16px 20px`
- Font: `16px / 400 / Roobertvf`
- Use: YAML `Customer-logo card on the public home marketing surface`
- Evidence: `home::div.logo-card`
- Observed: default only
- Field note: The following unmerged-field reading, including Paper-canvas-fill-not-pricing-select-`#ffffff`, `1px 0px solid`-mixed-borders-not-header-`1px solid transparent`-not-select-`#d1cdc7`, 18px-not-action-12-not-select-8px, 16px-20px-not-header-8px-16px, and 16px-400-not-tab-16px-500, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. `#fefdfb` is this card’s fill (Paper canvas). `1px 0px solid` with observed mixed border colors is this card’s border. Radius `18px` is this logo card (YAML `logo-card` 18 / YAML `radius` `18px`); it is not YAML `action`/`tab` 12. Padding `16px 20px` is this captured variant.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Pricing select

- Role: pricing-page select
- Kind: interactive
- Anatomy: value field
- Background: `#ffffff`
- Border: `1px solid #d1cdc7`
- Radius: `8px`
- Padding: `12px 16px 12px 40px`
- Font: `14px / 400 / Roobertvf`
- Use: pricing-page select; default evidence `surface-3::[data-omd-capture="104"]`
- Observed: default; Disabled `rgb(244, 243, 240)` background at `surface-3::[data-omd-capture="100"]`
- Field note: The following unmerged-field reading, including fill-`#ffffff`-not-Paper-canvas-`#fefdfb`, border-`#d1cdc7`-not-logo-card-mixed-borders, 8px-not-action-12-not-logo-card-18px, `12px 16px 12px 40px`-not-header-8px-16px, 14px-400-not-compact-action-13.92-500, and Disabled-`rgb(244, 243, 240)`-not-tab-default-`#f4f3f0`-as-a-second-paint, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. `#ffffff` is this select’s fill. `1px solid #d1cdc7` is this select’s border. Radius `8px` is this control; YAML has no select radius. Font 14px / 400 is this control’s field. Disabled `rgb(244, 243, 240)` is the captured disabled snapshot; it is not merged into YAML `tab-default` `#f4f3f0`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the pricing-page select |
| hover | applicable | Pointer-web select; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Captured Disabled `rgb(244, 243, 240)` background at `surface-3::[data-omd-capture="100"]` |
| error | applicable | Pricing-page select as a form field; visual treatment omitted |

Loading and success applicability are omitted. Source names this control as a pricing-page select; exact request/outcome mapping is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML-spacing-action-y-8-action-x-16-without-a-px-suffix, compact-header-pair-8px-vertical-and-16px-horizontal-at-1440×900, home-tabs-10px-16px, logo-cards-16px-20px, no-site-wide-spacing-scale-asserted-from-those-local-values, supplied-capture-has-one-desktop-viewport, and does-not-establish-responsive-breakpoints-authenticated-product-tables-or-documentation-layouts — are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification. YAML spacing is action-y 8, action-x 16. Body layout names the repeated compact header pair at 8px vertical and 16px horizontal padding at the captured 1440×900 viewport. Observed home tabs use 10px 16px padding; logo cards use 16px 20px.

- Header controls: the repeated compact header pair uses 8px vertical and 16px horizontal padding at the captured 1440×900 viewport.
- Local grouping: observed home tabs use 10px 16px padding; logo cards use 16px 20px. No site-wide spacing scale is asserted from those local values.
- Bounded scope: the supplied capture has one desktop viewport. It does not establish responsive breakpoints, authenticated-product tables, or documentation layouts.

Only a 1440×900 desktop capture was supplied. The public pages may of course be responsive, but no breakpoint, reflow, touch-target, or mobile-navigation behavior is recorded here. Treating those 8px / 16px / 10px 16px / 16px 20px / 1440×900 figures as captured-surface measurements rather than a cross-viewport specification, including public-pages-may-be-responsive-but-no-breakpoint-reflow-touch-target-or-mobile-navigation-is-recorded and should-not-be-inferred-as-mobile-breakpoints-from-the-single-captured-viewport, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating Clay’s official redesign account as balancing systems language with creative explanation, including data-agents-orchestration-execution-and-governance-as-connected-building-blocks-rather-than-opaque-AI-magic, desired-brand-world-as-playful-expansive-creative-and-precise, and useful-public-marketing-guidance-not-a-documented-product-error-copy-system, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. Clay’s official redesign account balances systems language with creative explanation: data, agents, orchestration, execution, and governance are presented as connected building blocks rather than opaque AI magic. The same post describes the desired brand world as playful, expansive, creative, and precise. That is useful public-marketing guidance; it is not a documented product-error-copy system.

The following voice-table directions — Positioning-state-the-infrastructure-and-the-outcome-plainly, Explanation-name-the-building-blocks-and-show-their-relationship, and Brand-story-pair-technical-clarity-with-a-playful-visual-metaphor — are a derived editorial implementation inference from the verified surfaces; they are not Clay-authored or a separately published UI specification.

| Context | Observed direction |
|---|---|
| Positioning | State the infrastructure and the outcome plainly. |
| Explanation | Name the building blocks and show their relationship. |
| Brand story | Pair technical clarity with a playful visual metaphor. |

**Official wording samples**

- *“Clay is the infrastructure GTM engineers build on.”* — official homepage-relaunch account.
- *“Data” / “Agents” / “Orchestration” / “Execution” / “Governance”* — five official primitives.
- *“fun and creative”* — Clay’s stated design challenge for the infrastructure story.

Treating the three public-marketing pages as the named hosts in this packet rather than a complete locale profile, and not promoting synthetic voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Clay-authored or a separately published UI specification. This packet names three public marketing pages. No synthetic voice samples are promoted.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- authenticated product and documentation chrome colors, type, components, and elevation
- hover visual treatment on the Dark compact header action; `focus-visible` visual treatments (recorded Focus snapshots are not that evidence)
- Dark compact header action / Oat compact header action loading·error·success applicability (exact request/outcome unresolved)
- Pricing select loading·success applicability (exact request/outcome unresolved)
- interactive kind and state-applicability map for Customer-logo card
- YAML `type` for header action, marketing tab, and pricing select
- site-wide spacing scale, responsive breakpoints, reflow, touch-target, and mobile-navigation
- reusable elevation ladder or shadow token
- product-error-copy system; synthetic voice samples
- Canela, Canela Web, Inter, Roobert (static), Roobert mono, Space Mono, Phosphor, and Webflow icon faces as live UI fonts
- partner testimonial fills and pricing-plan colors as a universal status or product palette
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
