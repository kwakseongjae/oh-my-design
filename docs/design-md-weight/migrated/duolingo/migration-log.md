# Duolingo migration log

Source: `web/references/duolingo/DESIGN.md`
Destination: `docs/design-md-weight/migrated/duolingo/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/duolingo/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2 wave 5
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd, `portable_core: true`, placeholders 0). `migrate-reference.mjs --brand duolingo --gate-only` PASS, problems 0.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage 옮김 → Experience Scope + Foundations live corroboration + Font evidence live surface; `primary_color` 옮김 → Distinctive + Foundations Feather Green; logo 경계 문장 옮김 → Typography & Assets + Named gaps | Portable file has no frontmatter. Name kept as H1 `Duolingo Design System`. Catalog homepage `https://www.duolingo.com` is Scope catalog-homepage identity + provenance identity/surfaces/Tier 1. Live host `www.duolingo.com` is also portable Foundations Semantic color corroboration and Font evidence live computed surface-use (E2a). Catalog `primary_color` `#58CC02` is triple: identity + portable Distinctive (`catalog primary_color`) + portable Foundations Feather Green (`catalog primary_color`) (E2a). Catalog logo type `simpleicons` / slug `duolingo` is triple: provenance identity + portable Typography & Assets boundary sentence (`Catalog logo metadata is Simple Icons identity (duolingo)`, not a captured first-party mark) + Named gaps identity-only mark file (E2a). |
| YAML `omd`, `verified`, `added`, token claims, `tokens.source` / `extracted`, `components_harvested` | 분리 → provenance; `tokens.source: reconciled`와 `components_harvested: true`는 Scope에도 유지 | Dual destination (E2a): portable Scope keeps `Token extraction is reconciled` and `components_harvested is true`; provenance identity keeps `tokens.source: reconciled`, `tokens.extracted: 2026-06-08`, `components_harvested: true` (A1c). No `ds.type` on source; none invented. Source has no `verification_v2` and no claim-to-surface mapping — none invented; global `verified` 2026-06-06 is freshness only, not a per-surface inspected date. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations Feather Green + Experience Avoid; 원장 분리 → provenance | Quadruple destination (E2a): portable Scope and Foundations keep the `#1a73e8` Google sign-in embed false-positive / canonical Feather Green note; Avoid forbids `#1a73e8` as primary; provenance keeps the source note. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. YAML unitless `lineHeight` 1.2 / 1.25 / 1.5는 비율로 보존 (A1a). 검증된 primitive type은 컴포넌트별로 보존: YAML `type: button` ×2 (Primary, Accent) + body Destructive/Outline도 `Type: button`; YAML `type: card`는 Lesson / Path Card에 유지하고 Highlight Card / Stat Card에도 `Type: card` (A1b). `Kind: interactive`로 뭉개지 않음. YAML `shadow.button-3d` `0 4px 0 0 #58a700`와 body lip `0 4px 0 #58A700` 비합침. YAML `shadow.card` `0 2px 0 0 rgba(0,0,0,0.1)`와 컴포넌트 `shadow: none` 비합침. YAML `title` 1.25와 body Heading Large `32px (1.33)` 비합침. YAML `button` 1.2와 body Button Label `20px` 비합침. YAML spacing 4/8/12/16/24/32/48와 body extra 20px/40px 비합침. YAML `family.sans` `din-round`와 body `DIN Next Rounded` 비합침. YAML tracking `-0.5`와 body `-0.5px` 비합침. Primary Button `fg` `#ffffff` / disabled `#e5e5e5` `#afafaf`는 해당 컨트롤 필드이며 일반 Ink가 아님 (A4). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(www.duolingo.com 레슨/패스/스트릭/가입/설정/하단 내비; `design.duolingo.com/identity/color` 브랜드 컬러 포털; 마케팅/랜딩은 ~1080px 별도 증거 도메인). Atmosphere 문단과 fun-first / chalkboard / children’s-book 독해, 그리고 lesson-value-is-not-a-proxy evidence-domain 문장은 각각 인접 derived editorial implementation inference / not Duolingo-authored or a separately published UI specification 한정 (B2/B2a). Distinctive의 vivid-palette / white-dominant 독해도 인접 완전 B2a. Key characteristics의 토큰 사실은 Distinctive traits. Catalog homepage `https://www.duolingo.com`는 Scope catalog-homepage identity + provenance identity/surfaces/Tier 1. Live host `www.duolingo.com`는 Scope + Foundations Semantic color corroboration + Font evidence live computed surface-use + provenance (E2a). `design.duolingo.com` / `design.duolingo.com/identity/color`는 Scope + Foundations Semantic color corroboration + provenance surfaces/sources/Tier 1 삼중 (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Animal-named tokens, YAML lowercase / body uppercase 동일 역할 비합침, Cardinal Dark `#EA2B2B` body-only 유지, `#1a73e8` 제외 제약. |
| §3 Typography Rules | 옮김 → Typography & Assets; Nunito≠Feather Bold 경계는 Avoid + Named gaps에도 | 다섯 증거 등급. Official product-use는 미수립 — 원본은 official product-family와 computed-family를 나누지 않음. Feather Bold owl-curve / Duo-shape 맥락은 body §1 + Tier-2 narrative로 Font evidence에 복원 (A1). YAML `Feather Bold` / `din-round`와 body 스택 둘 다 유지. Nunito는 stand-in이지 브랜드 페이스가 아님 — Typography Family/Unresolved + Avoid + Named gaps 삼중 목적지 (E2a). YAML unitless line-height 보존 (A1a). Type-character 문단 인접 B2a (B2/B2a). 시스템/Nunito 스택을 Feather Bold로 표시하지 않음. Unresolved Feather Bold webfont는 Named gaps에도 (E2a). |
| §4 Component Stylings | 옮김 → Components & States | Buttons / input / answer tile / cards / pills / badges / progress / bottom tab / feedback bars / modal / toggle. YAML `type: button`·`type: card` 보존. Capture selector 없음 — 발명하지 않음. Sign-up Input “uses inputs sparingly (tap-first)”와 Progress “glossy, gamey sheen”과 Capture-record 뒤 lip-always-solid 독해는 각 문장 인접 완전 B2a (B2/B2a). |
| §4 footer **Verified** / Tier 1 / Tier 2 / Note | mixed: Verified/Tier 2 분리 → provenance; Tier 1 URL 옮김 → Scope + Foundations corroboration + Font evidence + provenance; Note platform/release 옮김 → Scope | Catalog homepage `https://www.duolingo.com`는 Scope catalog-homepage identity + provenance. Live host `www.duolingo.com`는 Scope + Foundations Semantic color corroboration + Font evidence live computed surface-use + provenance (E2a). `design.duolingo.com` / `design.duolingo.com/identity/color`는 Scope + Foundations Semantic color corroboration + provenance 삼중 (E2a). Footer global `Verified` 2026-06-06 is freshness only — not a per-surface inspected date. Note platform/release 변동 주석은 Scope에 유지 (E1). Note “animal color names and 3D lip are signature traits” 문장은 provenance에 재수록하지 않음 — 해당 값 자체는 Distinctive/Foundations. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | 8px base, YAML+body spacing, 8/12/16/pill radii, ~375–414px / ~600px / ~1080px containers. Whitespace 철학 문단, one-focal-action composition, Foundations lesson-screen composition, Foundations Shape local-geometry (12px/16px not a universal radius), Layout closing measurement-application (named sizes are not a claim that every unlisted surface shares them)은 각각 인접 완전 B2a (B2/B2a). Answer-tile rapid-tapping은 원본 §8 Touch Targets 행 — 이 행의 출처가 아님. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Flat/outlined/lip/raised/modal + YAML button-3d / card-shadow 충돌 보존. Physical-not-atmospheric 독해 인접 B2a (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 레슨/컨트롤 제품 규칙. Governance 통제 문구에 넣지 않음. Numbered editorial Principles와 분리하되, capture-bound 목록 자체에 인접 완전 B2a (`derived editorial implementation inference` / `not Duolingo-authored or a separately published UI specification`) — source §7/§3 표지는 그 한정의 대체물이 아님 (B2/B2a, E1). |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. 원본 Don'ts 처방(lip vs blur, Eel, one action, 400/700, muted greens, targets, radii)은 인접 완전 B2a (B2/B2a). Nunito≠Feather Bold는 Avoid + Typography + Named gaps 삼중 (E2a). `#1a73e8`≠primary는 Avoid + Scope + Foundations + provenance 사중 (E2a). 미관측을 `not-applicable`로 바꾸는 근거가 아님 (C1). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | 768 / 1024 / ~600px / ~1080px, touch ≥50/44/48, collapsing, vector/Lottie. 원본에 없는 최소폭 발명 없음. Answer-tile “comfortable spacing for rapid tapping” purpose 독해는 이 행이 출처이며 Layout 인접 완전 B2a (B2/B2a). |
| §9 Agent Prompt Guide | mixed: 도구 프롬프트 삭제; §9-only 우측 배치 옮김 → Correct Feedback Bar | 도구별 복붙 프롬프트 삭제. Color reference와 컴포넌트 예시는 이미 Foundations/Components/Motion에 있음 (progress sheen, answer-tile selected `#DDF4FF`, feedback-bar hex, streak stat card). §9-only 고유값: green `CONTINUE` button **on the right** — portable Correct Feedback Bar `Nested CTA: green primary CONTINUE on the right` (A3). 다른 feedback/component/state 행은 CTA 존재만 기록. |
| §10 Voice & Tone | mixed: 컴포넌트/§14 문자열 옮김 → Content; derived 표/금지/독해 옮김 → Content (인접 B2a) | Source-stated lesson/control strings: `CONTINUE` / `START` / `CHECK ANSWER` / `CHECK` / `SKIP` / `MAYBE LATER` / `START LEARNING` / `Nice!` / `Correct solution:` — portable Content. Derived table (`GET STARTED`, `You're on fire!`, `7 day streak!`, `+10 XP`), forbidden list, and mischievous-friend / passive-aggressive-owl / friendly-casual-register reading sit under adjacent complete B2a and are not labeled Observed (B2/B2a, E1). Live WebFetch 문자열은 원본에 없음 — 발명하지 않음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance | 2011 창립, von Ahn / Hacker, CMU, reCAPTCHA, Nasdaq 2021, Duo는 공개 사실. Habit-driven 인과·school/SaaS 거절 독해는 Scope 인접 B2a (B2/B2a). |
| §12 Principles | 옮김 → Experience principles | 여덟 항목 전체가 원본 HTML 주석이 명시한 editorial readings. 인접 본문에 derived editorial implementation inference / not Duolingo-authored or a separately published UI specification. Capture-bound application은 §7 Do’s / §3 type rules에서 왔지만 별도 인접 완전 B2a 아래 둔다 — source-section 표지는 그 한정의 대체물이 아님. Governance 일반 문구도 대체물이 아님 (B2/B2a, E1). |
| §13 Personas | mixed: 가상 biography 삭제·sidecar 재수록 없음; Audience는 배제 경계 + independently named Super/hearts | 원본이 fictional archetypes라고 명시. Names/ages/cities/employers/biographies (Maya / Daniel / Sofia)는 portable Audience에도 provenance에도 없음. Daniel의 `ads`는 Audience product fact로 승격하지 않음 (D2). Super/Plus accents, Beetle Super branding, out-of-hearts upsell, hearts mechanic은 §13 밖 독립 source. Independently verified Primary tasks 3건은 §13이 아니라 레슨 완료 / CHECK 피드백 / 스트릭·XP (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability | 본문 보존: Empty / Loading first-paint / Loading checking-answer / Correct / Incorrect / Error form-field / Disabled CHECK / Success lesson-complete / Streak milestone / Out of hearts / Skeleton (A2). 선언 컴포넌트는 §4.4 7상태 표를 닫되 미관측 시각값은 발명하지 않음. `not captured` / `not named`를 `not-applicable` 사유로 쓰지 않음 (C1). loading·error·success는 역할 의미로 판정: Primary Button loading applicable (CHECK inline spinner); Outline/Answer Tile/Bottom Tab/Toggle loading·error·success는 역할상 not-applicable; Sign-up Input error applicable (captured), loading/success not-applicable. Accent Button과 Destructive Button은 mixed-role이라 loading·error·success field를 이 경계에서 생략 — not-applicable로 닫지 않음 (C2). Named `Focus` `#1CB0F6` 2px는 `focus-visible` treatment로 승격하지 않음; focus-visible 행에 hex 없음 (B1). 그 생략은 Components 적용표 + Named gaps 이중 (E2a). Cards / pills / badges / progress / feedback bars / modal은 kind/map 생략 또는 non-interactive (C4). graph 위임 없음. State coverage 완료 주장 없음 (C3) — 같은 부정 경계는 Named gaps에도 (E2a). Capture-record 성격 규정 (“Never cold” 등)과 Capture-record 뒤 lip-always-solid 독해는 인접 B2a. |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브 값 분리 → provenance omission ledger; 생략 이름은 Named gaps | Duration (`motion-instant` 0ms / `motion-fast` 150ms / `motion-standard` 250ms / `motion-celebrate` 400–600ms / `motion-confetti` 800ms+), easing 이름/용도 (`ease-enter` / `ease-exit` / `ease-standard` / `ease-bounce`), signature motions 4, `prefers-reduced-motion` 유지. Celebration “licensed overshoot / joy is the point”와 reduce-motion “still celebratory in copy, just less kinetic” 판단은 각 문장 인접 완전 B2a (B2/B2a). Exact cubic-bezier `cubic-bezier(0.0, 0.0, 0.2, 1)` / `cubic-bezier(0.4, 0.0, 1, 1)` / `cubic-bezier(0.4, 0.0, 0.2, 1)` / `cubic-bezier(0.34, 1.56, 0.64, 1)`는 무출처(`ease-exit`는 legacy 규격 템플릿과 일치)라 portable에서 생략하고 provenance omission ledger에 값을 보관 (E2b); 네 이름 생략은 Named gaps에도 (E2a). Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 표 밖 값을 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps는 같은 다섯 종류를 말하되 B3 전문(“Official documentation of a single curve or duration is not that gate”)은 Foundations Motion에만 있다. |
| HTML comment (OmD v0.1 Sources) | mixed: Tier 1/2·narrative·경고 분리 → provenance; interpretive 한정은 본문 | design.duolingo.com identity/color, www.duolingo.com, animal palette corroboration, Nunito stand-in, fictional-persona 경고, interpretive-claim 경고는 provenance. Interpretive 한정은 portable 본문에 남김 (E1). Live WebFetch 날짜/문자열은 원본에 없음. |

### F1 / F2 (v7 mandatory final passes)

Superseded by the wave5 sol resubmit scan below. The prior F1 list omitted Layout composition/purpose readings; the prior F2 omitted Distinctive `primary_color` and Foundations identity/color URL hits.

## Revision 2026-08-23 (wave5 sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-wave5-2026-08-23-sol-sample.md` §1.

| Item | Correction |
|---|---|
| Claim-surface mapping / per-surface dates A1/E1 | Removed invented Surfaces `inspected`/`captured` 2026-06-06 rows and the claim-to-surface token table. Source has no `verification_v2`. Global `verified` stays freshness. |
| Font Official product-use A1 | Official product-use withdrawn to “not established”. Owl-curve / Duo-shape context restored as source-stated body + Tier-2 narrative. |
| Layout B2/B2a | Adjacent complete B2a on one-focal-action composition, answer-tile rapid-tapping purpose, and Foundations lesson-screen composition. Whitespace limiter no longer treated as covering those later sentences. |
| identity/color URL + `primary_color` E2/E2a | `design.duolingo.com/identity/color` is Scope + Foundations corroboration + provenance (triple). Catalog `primary_color` `#58CC02` is identity + Distinctive + Foundations (triple). |
| §9-only right alignment A3 | Correct Feedback Bar nested CTA is `CONTINUE` on the right. Log no longer claims §9-only unique values were absent. |
| Accent/Destructive C2 | Mixed-role loading/error/success omitted at the field boundary. Not closed as `not-applicable`. |
| Daniel `ads` D2 | Audience no longer promotes `ads`. Super/Plus, Beetle, out-of-hearts, hearts remain from independent source rows. |

### F1 / F2 (resubmit)

- **F1 B2a scan (full DESIGN.md reread after F3 audit):** Adjacent complete B2a on: Scope atmosphere + fun-first / chalkboard / children’s-book; Scope habit-driven causal + school/SaaS refusal; Scope evidence-domain (lesson value is not a proxy for every marketing/Super/settings screen); Distinctive vivid-palette / white-dominant; Principles numbered list (eight items); capture-bound application; Avoid source Don’ts; Elevation physical-not-atmospheric; Typography type-character; Layout whitespace; Layout one-focal-action composition; Layout rapid-tapping purpose; Layout measurement-application (named sizes are not a claim that every unlisted surface shares them); Foundations lesson-screen composition; Foundations Shape local-geometry (12px/16px not a universal radius); Content voice table + forbidden list + owl reading; Motion celebration-license; Motion reduce-motion still-celebratory-in-copy; Capture-record “Never cold” / “No shaming language” / “peak emotional moment” / “light, not punitive”; Capture-record lip-always-solid; Sign-up Input tap-first; Progress “glossy, gamey sheen”. Font owl-curve paragraph is labeled source-stated body + Tier-2 narrative, not Official product-use. Accent/Destructive omission sentences record unresolved mixed-role fields; they do not close applicability. Nunito/`#1a73e8` Avoid items are catalog evidence-class boundaries. Scope coverage, catalog homepage, token-note facts, and public founding facts stay unqualified; the prior “remaining Scope sentences are facts” exemption is withdrawn because it hid the not-a-proxy judgement.
- **F2 E2 grep (value + field/role context):** `design.duolingo.com/identity/color` grep-matched Scope, Foundations Semantic color, and provenance Surfaces/Sources/Tier 1 (triple). Catalog `primary_color` / Distinctive `catalog primary_color` and Foundations `catalog primary_color` plus provenance identity `#58CC02` grep-matched (triple). Catalog homepage `https://www.duolingo.com` grep-matched Scope and provenance identity/surfaces/Tier 1. Live host `www.duolingo.com` also grep-matched Foundations Semantic color corroboration and Font evidence live computed surface-use (not dual). `#1a73e8` grep-matched Scope/Foundations/Avoid and provenance token note. `reconciled` and `components_harvested` grep-matched Scope and provenance identity. Simple Icons (`duolingo`) grep-matched Assets + Named gaps and provenance identity. Nunito≠Feather Bold grep-matched Typography + Avoid + Named gaps. `on the right` grep-matched Correct Feedback Bar nested CTA. Owl-curve / “echo the shape of Duo” grep-matched Font evidence. Audience grep-matched no `ads` product fact. Accent/Destructive grep-matched omission sentences, not `not-applicable` loading/error/success rows. Provenance Surfaces table grep-matched named URLs without per-surface inspected dates. Provenance Claim ledger grep-matched no token-to-surface map. Maya/Daniel/Sofia absent from DESIGN.md and provenance. Unitless `1.2` / `1.25` / `1.5` grep-matched Type roles. `Type: button` ×4 and `Type: card` ×3 grep-matched Components. Cubic-bezier four numeric values grep-matched provenance omission ledger only. B3 five-kind gate and the sentence “Official documentation of a single curve or duration is not that gate” grep-matched Foundations Motion only. Footer Note signature-traits sentence is not in provenance. Rapid-tapping B2a is a §8 source-row, not §5.
