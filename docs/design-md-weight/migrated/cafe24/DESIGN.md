# Cafe24 Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Cafe24 (카페24) is Korea's foundational e-commerce platform — the infrastructure that builds, operates, and markets a huge share of the country's online stores. Treating that product-origin / foundational-platform sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. Catalog homepage identity is `https://www.cafe24.com`.

Treating the following four URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, marketing-homepage-value-not-a-proxy-for-merchant-admin-or-developers-docs, eclogin-input-submit-pair-not-a-proxy-for-every-other-login-control, and developers-docs-chrome-not-a-proxy-for-marketing-Pretendard, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. This contract covers four named first-party evidence domains from the 2026-06-26 packet: the modern marketing homepage at `https://www.cafe24.com` (Pretendard chrome), the merchant login/app surface at `https://eclogin.cafe24.com/Shop/`, the developer design-docs surface at `https://developers.cafe24.com/design/front/smart` (legacy Noto Sans KR chrome), and the Newsroom at `https://news.cafe24.com`. Color, type, and component values below stay attached to the surface that established them. A marketing-homepage value is not a proxy for every merchant-admin or developers-docs surface. An eclogin input/submit pair is not a proxy for every other login control beyond the harvested field and button. Developers-docs chrome is not a proxy for the marketing Pretendard system.

Source token note: primary = live marketing CTA blue (`#084fff`, full pill); the product/login app surface uses a slightly lighter blue (`#3971ff`) on sharp 4px buttons. Ink near-black (`#1c1c1c`). Lime (`#bbf94f`) is the single saturated accent reserved for dark hero sections. Treating that note as a register split — `#084fff` is catalog `primary_color` and the harvested marketing pill CTA fill, `#3971ff` is the eclogin submit fill rather than a second marketing primary and rather than a login input fill, 4px is shared geometry for that submit and for login inputs rather than App Blue as an input fill, `#1c1c1c` is marketing ink rather than input ink `#1b1e26` or pure `#000000`, and `#bbf94f` is a dark-band accent rather than a light-canvas accent or a second action blue — is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

The following atmosphere readings — confident, approachable SaaS marketing rather than the dense merchant-admin tooling underneath; calm, breathable bands; a single blue trained as “do this”; Korean-modern Pretendard with a heavy bold/regular split; engineered and friendly at once; lime as the one moment of swagger; geometry that is friendly and rounded; depth handled with restraint; a tighter app/product variant signalling “tool” where the marketing site signals “invitation”; never-pure-black-for-body-copy; reserved-almost-entirely-for-the-primary-CTA; occasional-deep-navy; app-blue-`#3971ff`-as-submit-fill-not-input-fill; 4px-shared-geometry-for-submit-and-inputs — are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification. The source describes a pure-white canvas (`#ffffff`) broken up by soft cool-grey surfaces (`#f9fafb`, `#f0f2f3`). Text sits in a warm near-black (`#1c1c1c`) — never pure black for body copy. The single saturated action color is an electric royal blue (`#084fff`), reserved almost entirely for the primary “지금 무료로 시작하기” call-to-action. Headlines run at weight 700 across 48px / 40px / 30px / 24px / 20px; body copy is 18px regular at line-height 1.50. Dark sections use charcoal (`#323232`), near-black slate (`#1a1d22`), and an occasional deep navy (`#012255`), with lime-chartreuse (`#bbf94f`) as the accent. The primary CTA is a full pill (`9999px`); persona cards round at 20px; selector chips at 24px; smaller chrome (nav, tags) at 6px. Live inspection found `box-shadow: none` across the hero CTAs, persona cards, nav, and selector chips. The product/login app surface (eclogin) uses `#3971ff` as the submit fill and shares 4px corners on that submit and on login inputs; the input fill is `#ffffff` with `#1b1e26` text and a `#d6dae1` border, not App Blue.

Treating the following public-history facts as narrative rather than interface tokens, including the Naver stake as deepening the two companies' commerce partnership, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. Cafe24 was founded in 1999 by 이재석 (Lee Jae-suk, Founder & CEO) and is operated by Cafe24 Corp., headquartered in Seoul. In 2018 it became the first company to enter the KOSDAQ market via Korea's “Tesla listing” route (ticker 042000), and in 2021 Naver acquired roughly a 20% stake to deepen the two companies' commerce partnership. Source §11 cross-references those listing/ownership facts via [KED Global](https://www.kedglobal.com/m-as/newsView/ked202108090008) and [Cafe24 Wikipedia](https://en.wikipedia.org/wiki/Cafe24). Founder quotes from the Cafe24 Newsroom Q&A: *“With a merchant-centric approach, Cafe24 aims to help merchants concentrate on their creativity”* and *“Merchants need to focus on their brand content to differentiate their products. Thus, they must be free as much as possible from administrative work”* ([Cafe24 Newsroom Q&A](https://news.cafe24.com/global/qna-with-jaesuk-lee-founder-and-ceo-of-cafe24/)). Treating the source's founding-premise reading — merchant empowerment, and that premise being legible in the brand's own one-stop service framing and its first-timer encouragement — as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. The source states the company's founding premise is merchant empowerment — take the operational burden off the seller so they can focus on creativity and brand — and reads that philosophy as visible in everything from the "원스톱 운영대행" (one-stop operations agency) framing to the "처음이어도 할 수 있어요" encouragement. Public-history narrative from those KED Global, Wikipedia, and Newsroom sources provides that context; they do not by themselves supply interface tokens.

The following refusal, embrace, and dual-register readings — intimidating-density-of-legacy-commerce-admin / fear-based-sales-urgency / airy-encouraging-marketing-surface / single-trustworthy-blue / bold-Pretendard-headlines-that-speak-plainly / merchant-app-tight-utilitarian-versus-public-site-warm-inviting / lime-on-dark-as-one-note-of-momentum / energetic-not-bureaucratic — are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification. What the source treats Cafe24’s design as refusing: the intimidating density of legacy commerce-admin software and fear-based sales urgency. What it treats the brand as embracing: an airy, encouraging marketing surface; a single trustworthy blue for action; bold Pretendard headlines that speak plainly; and a dual register that keeps the merchant-facing app tight and utilitarian while the public site stays warm and inviting. The lime-on-dark accent is the brand’s one note of momentum — a signal that commerce here is energetic, not bureaucratic.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings / live CTA labels below as Primary tasks, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Start free on the marketing homepage (YAML use: `Marketing primary CTA — 지금 무료로 시작하기, full pill`).
- Sign in on the merchant login (YAML use: `Product / login app submit — 로그인, sharp corner`).
- Scan the captured hero audience-segment entry cards (YAML use: `Persona / segment entry card, shadowless`; live labels include “신규 창업자”, “크리에이터”, “기업형”, “글로벌”).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes, not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

### Distinctive traits

The following unmerged-role extras — treating `#084fff` as the marketing pill CTA rather than eclogin `#3971ff`, treating `#3971ff` as the eclogin submit fill rather than a login input fill, treating 4px as shared geometry for that submit and for login inputs rather than App Blue as an input fill, treating `#1c1c1c` as marketing ink rather than input ink `#1b1e26` or body-forbidden pure `#000000`, treating `#bbf94f` as a dark-band accent rather than a light-canvas accent, treating `#f9fafb` / `#f0f2f3` / `#f7f8fa` as unmerged greys, treating `#e0e0e0` / `#e6e8eb` / `#d6dae1` as unmerged borders, treating `#616161` / `#5f5f5f` / `#757575` / `#bfbfbf` as unmerged text steps, treating Pretendard 700/400 as the marketing hierarchy engine rather than developers-docs Noto Sans KR, treating 9999px pill / 20px cards / 24px chips / 6px nav-tags as local marketing geometry rather than one radius for the 4px app chrome, treating circular-icon `100%` as unmerged from YAML `rounded.full` 9999, and treating `box-shadow: none` as captured flatness rather than an extra elevation token — are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

- Marketing royal blue `#084fff` as catalog `primary_color` and the harvested “지금 무료로 시작하기” pill fill
- App/login blue `#3971ff` as the eclogin submit fill, not the login input fill
- 4px radius shared by the eclogin submit and login inputs (input fill remains `#ffffff`)
- Warm near-black ink `#1c1c1c` for marketing text; input ink `#1b1e26` on the login field; pure `#000000` reserved for maximum-contrast dark sections, not body copy
- Lime `#bbf94f` as the single saturated accent on dark bands (`#323232`, `#1a1d22`, `#012255`)
- White canvas `#ffffff` with surface greys `#f9fafb` / `#f0f2f3` and chip surface `#f7f8fa`
- Pretendard on the marketing site (weight 700 headlines / 400 body); Noto Sans KR on `developers.cafe24.com`
- Full-pill CTA `9999px` at 56px / 18px / 700; app submit 4px at 56px / 16px / 700
- Persona cards 20px / `40px 32px` padding; selector chips 24px; nav/tags 6px; step badges 8px
- Circular icon button radius `100%` at 48px on `#e6e8eb`, unmerged from the pill `9999px`
- Flat depth: live `box-shadow: none` on hero CTAs, persona cards, nav, and selector chips; tint `#f9fafb` and hairline `#e0e0e0`

### Principles

These six items (Merchant-centric — free them to create, Encouragement over intimidation, One action, one blue, Flat and friendly, Two registers, kept distinct, Restrained swagger) are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

1. **Merchant-centric — free them to create.** Cafe24's stated mission is to let merchants “concentrate on their creativity” by absorbing administrative work. *UI implication:* reduce perceived effort everywhere — “3가지만 결정하면” step compression, one-stop framing, free-first CTAs.
2. **Encouragement over intimidation.** Commerce should feel achievable for a first-timer. *UI implication:* can-do headlines (“처음이어도 할 수 있어요”), inclusive segment labels, low-pressure language.
3. **One action, one blue.** Royal blue (`#084fff`) means “do this.” *UI implication:* reserve the saturated blue for the primary CTA so the next step is never ambiguous.
4. **Flat and friendly.** Clarity beats decorative depth. *UI implication:* no shadow stacks; separate with tint, hairlines, and bold light/dark band alternation; round the corners.
5. **Two registers, kept distinct.** A warm, inviting marketing site and a tight, utilitarian merchant app. *UI implication:* pill/`#084fff` chrome for marketing; sharp 4px/`#3971ff` chrome for the product/login surfaces — never blur the two.
6. **Restrained swagger.** *UI implication:* the lime accent (`#bbf94f`) appears rarely and only on dark bands — energy that lands because it is scarce.

Treating the following as a capture-bound application of source §7 Do's, including Pretendard-700-headlines-400-body, royal-blue-`#084fff`-as-the-single-marketing-action, warm-near-black-`#1c1c1c`-not-pure-black, tint-`#f9fafb`-and-hairline-`#e0e0e0`-plus-light/dark-bands, lime-`#bbf94f`-only-on-dark, pill-9999px-and-20px-cards, app-variant-`#3971ff`-4px, and body-18px-at-line-height-1.50, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Use Pretendard weight 700 for every headline and weight 400 for body — the weight split is the hierarchy.
- Reserve royal blue (`#084fff`) for the primary marketing CTA — keep it the single “action” color.
- Use warm near-black (`#1c1c1c`) for text instead of pure black.
- Separate sections with flat tint (`#f9fafb`) and `#e0e0e0` hairlines, plus light/dark band alternation.
- Deploy the lime accent (`#bbf94f`) sparingly and only on dark sections (`#323232`, `#1a1d22`).
- Use full pill geometry (`9999px`) for the primary CTA and 20px for cards.
- Switch to the tighter app variant (`#3971ff`, 4px corners) on product/login/tool surfaces.
- Keep body copy at a generous 18px / 1.50 line-height for long marketing pages.

### Avoid

The following items copy source §7 Don'ts, including blue-scarcity-of-`#084fff`, lime-`#bbf94f`-not-on-light, no-pure-black-body, no-drop-shadows, two-register-split, no-second-saturated-accent, display-always-700, and no-heavy-decorative-tracking. They are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

- Do not spread the blue across many elements — it dilutes the single-action signal.
- Do not use the lime (`#bbf94f`) on white/light backgrounds — it only reads on dark bands.
- Do not use pure black (`#000000`) for body text — reserve near-black `#1c1c1c`.
- Do not lean on drop shadows for elevation — separate with tint, hairlines, and dark bands.
- Do not mix the marketing pill chrome and the app 4px chrome on the same surface — keep the two registers distinct.
- Do not add a second saturated accent — blue is the action color, lime is the lone dark-section accent.
- Do not set headlines in a light weight — display is always bold (700).
- Do not add heavy decorative letter-spacing — the system is size- and weight-driven. Treating that size-and-weight-driven characterization as a reconstruction reading rather than Cafe24-authored tracking doctrine is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including marketing-pill-not-eclogin-submit, input-ink-not-marketing-ink, canvas-white-not-a-second-action, lime-not-a-second-blue, charcoal-slate-navy-unmerged, surface-grey-not-surface-alt-not-chip-surface, hairline-not-border-soft-not-input-border, nav-grey-not-body-grey-not-muted-not-faint, on-blue/on-dark white as the same hex as canvas rather than a second paint, utilitarian-sibling-of-marketing-blue, warm-near-black-ink, occasional-navy-accent-dark-blocks, and pure-black-not-body-copy — are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification. Catalog `primary_color` `#084fff` is YAML `primary` and the marketing CTA / step-badge foreground; it is not App Blue `#3971ff`. YAML `primary-app` `#3971ff` is the eclogin submit fill. YAML `ink` `#1c1c1c` is marketing text; YAML `ink-input` `#1b1e26` is login-field text. YAML `canvas` `#ffffff` is page background and text on blue/dark surfaces; it is not a second action color. YAML `lime` `#bbf94f` is the dark-section accent, not a light-canvas fill and not a second action blue. Charcoal `#323232`, slate-deep `#1a1d22`, navy `#012255`, and pure-black `#000000` are unmerged dark-section fills. Surface `#f9fafb`, surface-alt `#f0f2f3`, and chip-surface `#f7f8fa` are unmerged greys. Tint-blue `#e6edff` is the step-badge wash, not a general canvas. Hairline `#e0e0e0`, border-soft `#e6e8eb`, and input-border `#d6dae1` are unmerged 1px borders. Nav-grey `#616161`, body-grey `#5f5f5f`, muted `#757575`, and faint `#bfbfbf` are unmerged text steps. YAML component fields such as marketing CTA `fg` `#ffffff` and tag-chip `fg` `#5f5f5f` stay on those controls. They are not extra general inks.

- **Cafe24 Blue / Primary** (`#084fff`): catalog `primary_color`. YAML `primary`. Marketing “지금 무료로 시작하기” CTA fill and numbered-step-badge text.
- **App Blue** (`#3971ff`): YAML `primary-app`. Eclogin submit fill — the utilitarian sibling of the marketing blue.
- **Ink** (`#1c1c1c`): YAML `ink`. Primary marketing text and heading color — a warm near-black.
- **Input Ink** (`#1b1e26`): YAML `ink-input`. Text inside eclogin form fields.
- **Pure White / Canvas** (`#ffffff`): YAML `canvas`. Page background, white cards, and text on blue/dark surfaces.
- **Surface Grey** (`#f9fafb`): YAML `surface`. Persona cards and segmented sections.
- **Surface Alt** (`#f0f2f3`): YAML `surface-alt`. Secondary grey band for alternating content sections.
- **Chip Surface** (`#f7f8fa`): YAML `chip-surface`. Fill for category/filter tag chips.
- **Tint Blue** (`#e6edff`): YAML `tint-blue`. Pale wash behind numbered step badges.
- **Hairline** (`#e0e0e0`): YAML `hairline`. Thin borders and dividers; selector-chip border.
- **Border Soft** (`#e6e8eb`): YAML `border-soft`. A slightly cooler border / circular icon-button fill.
- **Input Border** (`#d6dae1`): YAML `input-border`. 1px border on app/login text fields.
- **Charcoal** (`#323232`): YAML `charcoal`. Common dark-section background.
- **Slate Deep** (`#1a1d22`): YAML `slate-deep`. Near-black slate for high-contrast dark hero sections.
- **Navy** (`#012255`): YAML `navy`. Occasional accent dark blocks.
- **Pure Black** (`#000000`): YAML `pure-black`. Maximum-contrast dark sections and overlays — not body copy.
- **Lime** (`#bbf94f`): YAML `lime`. Single saturated accent on dark sections. Source Accent rule: never used on light backgrounds. Treating that never-on-light clause as a reconstruction reading of the live dark-band observation rather than a separately published Cafe24 paint specification is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.
- **Nav Grey** (`#616161`): YAML `nav-grey`. Top-navigation link text.
- **Body Grey** (`#5f5f5f`): YAML `body-grey`. Tag-chip labels and secondary copy.
- **Muted** (`#757575`): YAML `muted`. Tertiary text, metadata.
- **Faint** (`#bfbfbf`): YAML `faint`. Lowest-emphasis labels, placeholder-level text.

### Spacing

YAML scale: xs 4, sm 8, md 12, base 16, lg 20, xl 32, xxl 40, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names a scale of 4px, 8px, 9px, 12px, 16px, 20px, 32px, 40px, 64px. Treating those YAML numbers as recorded without a required px suffix, treating body 9px as a body-recorded step rather than a YAML key, and treating harvested control heights 56px / 48px / 46px / 40px as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

The following ~8px-base / generous-40px-32px-card-padding / tight-4px-9px-tag-padding readings are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification. Body layout states a base unit of 8px. Persona cards use a generous `40px 32px` internal padding; tag chips a tight `4px 9px`.

### Shape

YAML `rounded`: sm 4, md 6, lg 8, xl 20, pill 24, full 9999.

The following local-geometry reading, including Small-4px-app-login, Medium-6px-nav-tags, Large-8px-step-badges, XL-20px-persona-cards, Pill-24px-selector-chips, Full-9999px-primary-CTA, and circular-icon-100%-unmerged-from-9999, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Small (4px): app/login buttons and inputs (YAML `rounded.sm`)
- Medium (6px): nav items, tag chips (YAML `rounded.md`)
- Large (8px): numbered step badges (YAML `rounded.lg`)
- XL (20px): persona / segment cards (YAML `rounded.xl`)
- Pill (24px): selector chips (YAML `rounded.pill`)
- Full (`9999px`): primary CTA (YAML `rounded.full` 9999). Source §5 also names carousel arrows and circular icon buttons on this step; source §4 records the circular icon button as radius `100%`. Both records are kept; `100%` is not converted into `9999px`.

Treating 4px app corners, 6px nav/tags, 8px step badges, 20px cards, 24px selector chips, and `9999px` pill CTAs as local harvested geometry rather than a universal radius for every unlisted control, and treating YAML `full` 9999 as a YAML step and as `9999px` on the marketing CTA rather than merged with circular-icon `100%`, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. 4px app corners, 6px nav/tags, 8px step badges, 20px cards, 24px selector chips, and `9999px` pill CTAs are local harvested geometry, not a universal radius for every unlisted control. YAML `full` 9999 is recorded as a YAML step and as `9999px` on the marketing CTA; it is not merged with circular-icon `100%`.

### Elevation

The following elevation-table Use readings, including separation-without-elevation and immersive-feature-hero-with-lime, plus the near-shadowless-marketing / grouping-by-tint-hairline-and-band-alternation / premium-or-energetic-as-a-whole-band-swap-rather-than-stacked-elevation / app-surface-same-flat-philosophy-with-sharper-4px / hairline-`#e0e0e0`-not-merged-with-border-soft-`#e6e8eb` readings that follow the table, are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, most cards and CTAs |
| Tint (Level 1) | `#f9fafb` / `#f0f2f3` background shift | Card/section separation without elevation |
| Hairline (Level 2) | `1px solid #e0e0e0` (or `#e6e8eb`) border | Selector chips, dividers, white card outlines |
| Dark Band (Level 3) | `#323232` / `#1a1d22` / `#012255` background | Immersive feature/hero sections with lime accent |

YAML `tokens.shadow.none`: `none`.

Live inspection found `box-shadow: none` across the hero CTAs, persona cards, nav, and selector chips. Depth and grouping are communicated through flat tinted surfaces (`#f9fafb`), thin hairlines (`#e0e0e0`), and light/dark band alternation. When a section needs to feel premium or energetic, the source swaps the entire band to charcoal (`#323232`) or slate (`#1a1d22`) and reaches for the lime accent (`#bbf94f`), rather than stacking elevation. The app/product surface keeps the same flat philosophy with sharper 4px geometry. `#e0e0e0` hairline is not merged with `#e6e8eb` border-soft.

### Motion

Source-stated duration roles. Treating the duration table, easing names, signature motions, and reduced-motion line as source-stated rather than computed CSS, and treating Arriving / Dismissals / Two-way / spec-template-ease-exit-match / functional-and-friendly / no-bounce-or-spring / dependability-not-gimmickry / “intentional rather than jarring” / “the page remains fully functional” readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 220ms | Card/section reveal, carousel slide, dropdown |
| `motion-slow` | 360ms | Page-level transitions, light/dark band crossfade |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, sheets, carousel |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

Signature motions (source-stated):

Treating Motion as functional-and-friendly — consistent with the flat, approachable aesthetic — and treating no-bounce-or-spring on core chrome as a signal of dependability, not gimmickry, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

Motion is functional and friendly — consistent with the flat, approachable aesthetic.

1. **Hero carousel.** The hero carousel advances on `motion-standard / ease-enter`.
2. **Viewport fade-in.** Persona cards and feature bands fade-in from below as they enter the viewport.
3. **Dark-band crossfade.** Transitions into dark bands use a `motion-slow` background crossfade so the shift in register feels intentional rather than jarring.
4. **No bounce or spring** on core chrome — a commerce platform signals dependability, not gimmickry. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant and the carousel pauses; the page remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including marketing-Pretendard-versus-docs-Noto, fallback-stack-not-the-brand-face, YAML-legacy-family-as-a-separate-docs-chrome, live-computed-marketing-versus-docs, YAML-family-keys-naming-those-live-computed-families, outside-the-four-named-URLs, and merchant-centric-Newsroom-philosophy-as-Scope-narrative-not-a-font-Official-product-use-row, are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Marketing homepage body computes `Pretendard, -apple-system, system-ui`; section H2 48px / 700 / line-height 58px; store H3 40px / 700 / 54px / tracking -0.4px. Developers-docs body computes `"Noto Sans KR", Dotum`; 14px; `rgb(51, 51, 51)` on `#ffffff`. YAML `family.sans` `Pretendard` and YAML `family.legacy` `Noto Sans KR` name those live computed families. |
| Outside these captures | Surfaces other than the four named URLs. |

### Family

- **Current visible marketing UI family:** `Pretendard` (with `-apple-system`, `system-ui` fallbacks)
- **YAML `family.sans`:** `Pretendard`
- **YAML `family.legacy`:** `Noto Sans KR` (with Dotum fallback) — older developer-docs surface (`developers.cafe24.com`)

The following font-use boundary, including do-not-present-system-fallbacks-as-Pretendard, do-not-replace-Pretendard-on-marketing-with-Noto-Sans-KR, and docs-chrome-not-marketing-face, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. Do not present `-apple-system`, `system-ui`, or Dotum as Pretendard. Do not replace Pretendard on the marketing homepage with Noto Sans KR. Do not present a system stack as Pretendard. Noto Sans KR is the developers-docs chrome in this packet, not the marketing UI family.

The following type-rule readings (bold display / regular body as the hierarchy engine; size-driven not tracking-driven; one family many jobs; generous body 1.50) are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification. Weight 700 carries every headline; weight 400 carries every paragraph. Letter-spacing is almost always normal; only the 40px store title pulls -0.4px. Pretendard does display, body, nav, and UI on the marketing surface. Body sits at 18px / 1.50.

### Type roles

Verified YAML line-height values are the unitless ratios `1.21`, `1.35`, `1.53`, `1.42`, `1.40`, `1.50`, and `1.00`. The following ratio-versus-size-local reading, including unitless-ratios-not-fixed-px, px-companions-local, and YAML-tracking-minus-0.4px-on-Store-title-versus-normal-elsewhere-unmerged, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. YAML tracking is recorded only on Store / Feature Title (`-0.4px`); other roles are `normal` in the body table.

The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. Button 18px / 700 / 1.00 is the marketing CTA label; eclogin submit 16px / 700 is a component field, not this type-role row. Caption 14px / 400 is unmerged from Tag Chip 14px / 400 (component field) and from Step Badge 14px / 800 (component field). Selector-chip 15px / 700 is a component field, not a YAML typography role.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking | Use (YAML) | Notes (body table) |
|---|---|---:|---:|---:|---|---|---|---|
| Section Hero (YAML `display`) | Pretendard | 48px (3.00rem) | 700 | 1.21 | 58px at this size | normal | Section hero headline (H2) | H2 section heroes |
| Store / Feature Title (YAML `heading-xl`) | Pretendard | 40px (2.50rem) | 700 | 1.35 | 54px at this size | -0.4px | Store / feature section title | Store hub & feature section titles |
| Sub-section (YAML `heading-lg`) | Pretendard | 30px (1.88rem) | 700 | 1.53 | 46px at this size | normal | Sub-section headline | Recommendation headlines |
| Card Headline (YAML `heading-md`) | Pretendard | 24px (1.50rem) | 700 | 1.42 | 34px at this size | normal | Card headline | Feature card heads |
| Small Card / Persona (YAML `heading-sm`) | Pretendard | 20px (1.25rem) | 700 | 1.40 | 28px at this size | normal | Small card / persona title | Persona/segment card titles |
| Body (YAML `body`) | Pretendard | 18px (1.13rem) | 400 | 1.50 | 27px at this size | normal | Body copy, Pretendard | Standard reading text |
| Nav Link (YAML `nav`) | Pretendard | 16px (1.00rem) | 500 | 1.50 | (no extra px in the body table) | normal | Top navigation links | Top navigation |
| Button (YAML `button`) | Pretendard | 18px (1.13rem) | 700 | 1.00 | (no extra px in the body table) | normal | Primary CTA label | Primary CTA label |
| Caption / Tag (YAML `caption`) | Pretendard | 14px (0.88rem) | 400 | 1.50 | (no extra px in the body table) | normal | Tag chips, captions, fine print | Tag chips, captions |

### Assets

Treating catalog logo metadata as a Google favicon lookup (`https://www.google.com/s2/favicons?domain=cafe24.com&sz=128`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

Treating verified product screenshots and illustrations as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated.

Characterizations in the following table such as “No intimidation, no clutter”, “Calm and reductive”, “Flat pulse, consistent with the shadowless system”, “the field stays visible”, “describing what's valid, not just 필수”, “never a bare 오류가 발생했습니다”, “No celebratory emoji”, and “blue actions fade rather than turn grey to preserve brand read” are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no stores / no products yet)** | White canvas. Single Ink (`#1c1c1c`) line at body size with an encouraging next step, plus one blue CTA (`#084fff`) to start. No intimidation, no clutter. |
| **Empty (dashboard, no data)** | Muted (`#757575`) single line explaining nothing's here yet, with a path to the first action. Calm and reductive. |
| **Loading (page/section)** | Skeleton blocks on `#f9fafb` tint at final card dimensions, 20px radius. Flat pulse, consistent with the shadowless system. |
| **Loading (app submit)** | Inline progress on the `#3971ff` button; the field stays visible. |
| **Error (form validation)** | Field-level message below the input describing what's valid, not just "필수". Border tone shifts on the affected field. |
| **Error (operation failed)** | Inline message in Ink with a plain-language explanation and a retry — never a bare "오류가 발생했습니다". |
| **Success (action saved)** | Brief inline confirmation in a calm tone; next-step detail linked below. No celebratory emoji. |
| **Skeleton** | `#f9fafb` blocks at final dimensions, 20px radius, flat pulse. |
| **Disabled** | Faint (`#bfbfbf`) text on a reduced-opacity surface; blue actions fade rather than turn grey to preserve brand read. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Persona / Segment Card, Tag Chip, and Numbered Step Badge have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Circular Icon Button restores `Type: button` and interactive kind from source §4 heading and Use (round icon/utility button). YAML `tokens.components` does not record this control; YAML type absence does not invert that body-stated primitive.

### Primary CTA (Marketing)

- Role: the single primary marketing call-to-action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#084fff`
- Text: `#ffffff`
- Radius: `9999px`
- Padding: `0px 32px`
- Height: 56px
- Font: 18px / 700 / Pretendard
- Use: YAML `Marketing primary CTA — 지금 무료로 시작하기, full pill`
- Observed: default only
- Field note: The following unmerged-field reading, including Role-as-the-single-primary-marketing-action, this-fill-not-eclogin-`#3971ff`, on-fill-white-not-ink, and 9999px-pill-not-selector-24px-not-circular-100%, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#084fff` is this control’s fill and catalog `primary_color`. It is not App Blue `#3971ff`. `#ffffff` is this control’s on-fill label, not marketing ink `#1c1c1c`. Radius `9999px` is this pill; it is not selector-chip 24px and not circular-icon `100%`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the marketing primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says blue actions fade rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the marketing primary CTA “지금 무료로 시작하기”; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### App Submit (Product / Login)

- Role: submit action on the eclogin product surface
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#3971ff`
- Text: `#ffffff`
- Radius: 4px
- Padding: `0px 20px`
- Height: 56px
- Font: 16px / 700 / Pretendard
- Use: YAML `Product / login app submit — 로그인, sharp corner`
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-marketing-`#084fff`, 16px-700-not-Button-18px-700, and 4px-not-marketing-pill-9999px, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#3971ff` is this control’s fill. It is not marketing primary `#084fff`. Font 16px / 700 is this control’s field, not Type-role Button 18px / 700 / 1.00. Radius 4px is this submit; it is not the marketing pill `9999px`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the eclogin submit |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A submit action can be unavailable; visual treatment omitted |
| loading | applicable | A login submit can be in flight; visual treatment omitted. Source §14 Loading (app submit) names inline progress on the `#3971ff` button; that row is not copied here as a computed paint |

Error and success applicability are omitted. Source names this control as the eclogin “로그인” submit; form-validation error is field-level on the text input, and exact success-outcome mapping is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

### Selector Chip

- Role: family-site / country selector pill
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#f9fafb`
- Text: `#1c1c1c`
- Border: `1px solid #e0e0e0`
- Radius: 24px
- Padding: `10px 16px`
- Height: 46px
- Font: 15px / 700 / Pretendard
- Use: YAML `Family-site / country selector pill`
- Observed: default only (“패밀리 사이트”, “대한민국”)
- Field note: The following unmerged-field reading, including this-fill-not-chip-surface-`#f7f8fa`, 24px-not-pill-9999px, 15px-700-not-a-YAML-type-role, and not-Nav-16px-500-not-Button-18px-700, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#f9fafb` is this pill’s fill (YAML `surface`), not tag-chip `#f7f8fa`. Radius 24px is YAML `rounded.pill`; it is not marketing CTA `9999px`. Font 15px / 700 is this control’s field, not Nav 16px / 500 and not Button 18px / 700.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the family-site / country selector |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A selector can be unavailable; visual treatment omitted |
| loading | not-applicable | A family-site / country pill selects a destination or locale grouping; the pill itself does not enter a loading state |
| error | not-applicable | Selector meaning is chosen versus resting, not a request or validation failure on the pill |
| success | not-applicable | Selector meaning is destination/locale choice, not action-outcome confirmation |

### Nav Link

- Role: top horizontal nav item
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#ffffff`
- Text: `#616161`
- Radius: 6px
- Padding: `8px 12px`
- Height: 40px per item
- Font: 16px / 500 / Pretendard
- Active (named): text shifts to ink `#1c1c1c` on the active item
- Use: YAML `Top nav item`
- Observed: default (“서비스 소개”, “시작 가이드”, “쇼핑몰 솔루션 이전”) plus named active
- Field note: The following unmerged-field reading, including nav-grey-not-ink-except-on-active and captured-variant-not-a-second-nav-token, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. Resting `#616161` is this link’s label (nav-grey), not body-grey `#5f5f5f`. Active `#1c1c1c` is a captured variant on this item, not a second nav token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top nav item |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`#1c1c1c`). Treating that appearance as a captured variant, not an observed click transition, and treating named active ink as not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

### Persona / Segment Card

- Role: audience-segment entry card on the hero
- Type: card
- Anatomy: title + supporting copy on a tinted tile
- Background: `#f9fafb`
- Text: `#1c1c1c`
- Radius: 20px
- Padding: `40px 32px`
- Use: YAML `Persona / segment entry card, shadowless`
- Observed: default (“신규 창업자”, “크리에이터”, “기업형”, “글로벌”); `box-shadow: none`
- Field note: The following unmerged-field reading, including this-fill-not-surface-alt, 20px-not-selector-24px, and 20px-700-title-as-heading-sm-not-a-second-family, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#f9fafb` is this card’s fill, not surface-alt `#f0f2f3`. Radius 20px is this card; it is not selector 24px and not app 4px. Title 20px / 700 is Type-role Small Card / Persona, not Body 18px / 400.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Text Field (App / Login)

- Role: login and form text input
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#1b1e26`
- Border: `1px solid #d6dae1`
- Radius: 4px
- Padding: `14px 12px`
- Height: 48px
- Font: 14px / 400 / Pretendard
- Use: YAML `Login / form text field`
- Observed: default (placeholder e.g. “아이디를 입력해 주세요.”)
- Field note: The following unmerged-field reading, including input-ink-not-marketing-ink, input-border-not-hairline, and 4px-matching-app-submit-not-marketing-pill, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#1b1e26` is this field’s value color, not marketing ink `#1c1c1c`. `#d6dae1` is this field’s 1px border, not hairline `#e0e0e0`. Radius 4px is this input, matching the app submit, not the marketing pill.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the eclogin text field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted. Source §14 names a field-level message and a border-tone shift; that row is not copied here as a computed paint |

Loading and success applicability are omitted. Source records default geometry plus a placeholder example. Exact loading/success mapping for this field is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

### Tag Chip

- Role: category / filter tag
- Type: badge
- Anatomy: label
- Background: `#f7f8fa`
- Text: `#5f5f5f`
- Radius: 6px
- Padding: `4px 9px`
- Font: 14px / 400 / Pretendard
- Use: YAML `Category / filter tag chip`
- Observed: default (“창업 구축”, “쇼핑몰 설정”, “상품/주문/배송”)
- Field note: The following unmerged-field reading, including chip-surface-not-persona-surface, body-grey-not-nav-grey, 6px-matching-nav-not-selector-24px, and 14px-400-not-Step-Badge-14px-800, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#f7f8fa` is this chip’s fill, not persona `#f9fafb`. `#5f5f5f` is this chip’s label (body-grey), not nav-grey `#616161`. Radius 6px is this chip, matching nav radius, not selector 24px. The 14px / 400 pair is this chip’s field; it is not Step Badge 14px / 800.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Numbered Step Badge

- Role: numbered step indicator
- Type: badge
- Anatomy: label
- Background: `#e6edff`
- Text: `#084fff`
- Radius: 8px
- Font: 14px / 800 / Pretendard
- Use: YAML `Numbered step indicator`
- Observed: default (flows titled “3가지만 결정하면 시작할 수 있어요”)
- Field note: The following unmerged-field reading, including tint-blue-not-canvas, this-foreground-not-a-second-CTA-fill, and 14px-800-not-Caption-14px-400, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#e6edff` is this badge’s fill (tint-blue), not canvas `#ffffff`. `#084fff` here is this badge’s foreground, not a second pill fill. Font 14px / 800 is this badge’s field, not Caption 14px / 400.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Circular Icon Button

- Role: round icon/utility button in the hero carousel chrome
- Kind: interactive
- Type: button
- Anatomy: icon
- Background: `#e6e8eb`
- Radius: `100%`
- Height: 48px
- Use: Round icon/utility buttons in the hero carousel chrome
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. `Type: button` is from the source heading and Use, not invented from YAML absence.
- Field note: The following unmerged-field reading, including border-soft-fill-not-hairline-and-100%-not-9999px, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#e6e8eb` is this control’s fill (border-soft), not hairline `#e0e0e0`. Radius `100%` is this control’s field from §4; source §5 also lists circular icon buttons on the Full `9999px` step. Both records are kept; they are not merged.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the hero carousel chrome utility button |
| hover | applicable | Pointer-web utility button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A carousel utility button can be unavailable; visual treatment omitted |
| loading | not-applicable | A carousel chrome utility button is not a request-in-flight control |
| error | not-applicable | Carousel chrome meaning is not a form-validation or operation-failure control |
| success | not-applicable | Carousel chrome meaning is not an action-outcome confirmation |

### White Hero (local recipe)

Treating the following as a source §9 local composition — white `#ffffff` parent + persona-card row + one primary pill CTA — and not as a rewrite of YAML Persona Card or Primary CTA, and not as a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Role: white-hero parent surface
- Anatomy: surface
- Background: `#ffffff`
- Local children (this composition only): section headline 48px Pretendard weight 700, line-height 1.21, color `#1c1c1c`; below it a row of rounded persona cards (`#f9fafb` background, 20px radius, `40px 32px` padding, no shadow, 20px/700 titles); one primary CTA (`#084fff` background, white text, `9999px` pill radius, `0 32px` padding, 56px height, 18px/700 — “지금 무료로 시작하기”)
- Use: source §9 white hero — white background, 48px/700/1.21 `#1c1c1c` headline, persona-card row, one primary pill CTA
- Kind, Type, and a state-applicability map are omitted. Harvested Persona / Segment Card and Primary CTA (Marketing) stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-white-not-a-second-canvas-token, persona-row-plus-one-pill-as-this-hero, and not-a-rewrite-of-YAML-Persona-or-Primary-CTA, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#ffffff` is this parent’s fill. The persona-card row and the one pill CTA stay children of this hero; they are not promoted as a global hero token.

### Dark Feature Band (local recipe)

Treating the following as a source §9 local composition — full-width dark feature band `#1a1d22` parent + 40px/700/-0.4px white headline + single lime emphasis + no shadow — and not as a global dark-band token or a rewrite of YAML `slate-deep` / Store title / Lime, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Role: full-width dark feature-band parent
- Anatomy: surface
- Background: `#1a1d22` (full-width)
- Shadow: none
- Local children (this composition only): headline 40px Pretendard weight 700, letter-spacing -0.4px, white text; lime `#bbf94f` sparingly for one emphasis word
- Use: source §9 dark feature band — `#1a1d22` full-width, 40px/700/-0.4px white headline, one lime emphasis word, no shadow
- Kind, Type, and a state-applicability map are omitted. YAML `slate-deep`, Store / Feature Title, and Lime stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-`#1a1d22`-not-charcoal-and-not-a-second-canvas, white-40px-headline-plus-single-lime-plus-no-shadow-as-this-band, and not-promoted-as-global-tokens, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. `#1a1d22` is this band’s parent fill (slate-deep), not charcoal `#323232`. The 40px/700/-0.4px white headline, the single lime word, and no shadow stay on this parent.

### Login White Card (local recipe)

Treating the following as a source §9 local composition — white-card parent containing login input (`#ffffff`, `#d6dae1`, 4px, `14px 12px`, 48px, `#1b1e26`) + submit (`#3971ff`, white, 4px, 56px, 16px/700, `로그인`) — and not as a global white-card token or a rewrite of YAML Text Field / App Submit, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Role: login white-card parent
- Anatomy: surface
- Background: white card
- Local children (this composition only): text input — `#ffffff` background, `1px solid #d6dae1` border, 4px radius, `14px 12px` padding, 48px height, `#1b1e26` text; submit — `#3971ff` background, white text, 4px radius, 56px height, 16px/700 — “로그인”
- Use: source §9 login form — white card, login input + submit pair
- Kind, Type, and a state-applicability map are omitted. Harvested Text Field (App / Login) and App Submit stay in their own slots.
- Field note: The following unmerged-composition reading, including white-card-parent-not-a-global-card-token-and-not-persona-`#f9fafb`, `#3971ff`-as-submit-fill-not-input-fill, and 4px-shared-geometry-on-both-children, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. The white-card parent is this composition’s container. `#3971ff` stays the submit fill, not the input fill. 4px is shared geometry on both children.

### Step Section (local recipe)

Treating the following as a source §9 local composition — step-section parent containing numbered badge + category-tag pairing — and not as a rewrite of YAML Numbered Step Badge / Tag Chip or a global pairing token, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- Role: step-section parent
- Anatomy: surface
- Local children (this composition only): numbered badges with `#e6edff` background, `#084fff` text, 8px radius, 14px/800; category tags with `#f7f8fa` background, `#5f5f5f` text, 6px radius, `4px 9px` padding
- Use: source §9 step section — numbered badge + category-tag pairing
- Kind, Type, and a state-applicability map are omitted. Harvested Numbered Step Badge and Tag Chip stay in their own slots.
- Field note: The following unmerged-composition reading, including numbered-badge-plus-category-tag-as-this-section and not-a-global-pairing-token, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification. The badge and tag stay children of this step-section parent; they are not promoted as a global pairing token.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 20, xl 32, xxl 40, section 64. Body layout repeats 4px, 8px, 9px, 12px, 16px, 20px, 32px, 40px, 64px. Harvested heights: 56px primary CTA and app submit, 48px login input and circular icon button, 46px selector chip, 40px nav item.

The following layout readings — breathing room on a dense product; airy marketing surface with generous vertical rhythm; flat segmentation by tint and dark/light alternation; rounded rhythm from 20px cards / 24px chips / pill CTA; centered wide marketing column with full-width alternating bands; hero-audience-segment-horizontal-row; feature-white-tint-dark-full-width-alternation; numbered-3-step-badge-sequence — are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification.

Recorded layout:

- Centered, wide marketing column with full-width alternating bands
- Hero audience-segment cards arranged in a horizontal row of rounded 20px tiles (“신규 창업자”, “크리에이터” …)
- Feature sections alternate white (`#ffffff`), tinted grey (`#f9fafb` / `#f0f2f3`), and dark (`#323232` / `#1a1d22`) full-width bands
- Numbered “3-step” flows lay out blue step badges in sequence

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 56px / 48px / 46px / 40px / 20px / 48px-headline figures as surface measurements in this packet rather than a cross-viewport specification, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating no-shadow screenshots / 20px-radius-across-breakpoints / full-bleed dark bands as source-stated image behavior rather than a complete image specification, and treating headline-scale-down-weight-700-maintained / segment-card-horizontal-scroll-or-wrap / feature-bands-multi-column-to-stacked / full-width-alternating-reduce-internal-padding as recorded collapsing rather than a complete responsive specification, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero headline compresses, segment cards stack/scroll |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024-1440px | Full layout, wide centered column, multi-column feature bands |

Collapsing strategy recorded in the source: hero 48px section headlines scale down on mobile, weight 700 maintained; segment-card row: horizontal scroll / wrap on narrow viewports; feature bands: multi-column → stacked single column; light/dark alternating sections maintain full-width treatment, reduce internal padding.

Image behavior: product screenshots and illustrations carry no shadow at any size, consistent with the flat system; cards maintain their 20px radius across breakpoints; dark hero bands keep full-bleed treatment with the lime accent intact.

Touch-target record: primary CTA at 56px height, full pill; selector chips at 46px height; circular icon buttons at 48px; app/login inputs at 48px height with `14px 12px` padding; nav items at 40px height with `8px 12px` padding.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-26)

The live strings below are source-stated. Treating the parenthetical characterizations (positioning / ease promise / encouragement / effort reduction / one-stop promise) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

- "Cafe24 - No.1 E-Commerce Platform" — homepage H1 (positioning). *(verified live)*
- "모든 단계를 PRO처럼 쉽게 시작하세요" — section hero (ease promise). *(verified live)*
- "처음이어도 할 수 있어요!" — section hero (encouragement). *(verified live)*
- "3가지만 결정하면 시작할 수 있어요" — step section (effort reduction). *(verified live)*
- "쇼핑몰 운영에 필요한 모든 솔루션, 카페24 스토어 하나로 연결" — store hub headline (one-stop promise). *(verified live)*
- "지금 무료로 시작하기" — marketing primary CTA. *(verified live)*
- "로그인" — eclogin submit. *(verified live)*

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Cafe24-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — encouraging-plain-spoken-enabling / capable-colleague / removing-intimidation-from-starting-and-running / reassuring-and-can-do / promising-ease-without-hype / partner-that-handles-administrative-parts / Encouraging-capability-framed / Confident-never-fear-based / Plain-and-inclusive / names-the-user-not-a-feature / Direct-low-pressure-free-first / Benefit-first-ease-led / Reductive-in-a-good-way / shrinks-perceived-effort / fear-based-urgency / jargon-heavy-enterprise-speak / get-rich-hype / gated-behind-expertise — are a derived editorial implementation inference from the verified surfaces; they are not Cafe24-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Cafe24's voice as encouraging, plain-spoken, and enabling — it talks to would-be merchants the way a capable colleague would, removing intimidation from the act of starting and running an online store. The register is read as reassuring and can-do (“처음이어도 할 수 있어요!” / “You can do it even your first time”), promising ease without hype. Copy is read as framing Cafe24 as the partner that handles the hard, administrative parts so the merchant can focus on their product and brand.

| Context | Tone |
|---|---|
| Hero headlines | Encouraging, capability-framed. "모든 단계를 PRO처럼 쉽게 시작하세요." Confident, never fear-based. |
| Segment labels | Plain and inclusive. "신규 창업자", "크리에이터", "기업형", "글로벌" — names the user, not a feature. |
| CTAs | Direct, low-pressure, free-first. "지금 무료로 시작하기", "체험하기". |
| Feature descriptions | Benefit-first, ease-led. Explains what Cafe24 does *for* the merchant. |
| Step / onboarding copy | Reductive in a good way. "3가지만 결정하면 시작할 수 있어요" — shrinks the perceived effort. |

**Forbidden register** (source): fear-based urgency, jargon-heavy enterprise speak that intimidates first-time sellers, over-promising “get rich” hype, and any tone that makes commerce feel gated behind expertise.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exception remains the Nav Link active ink `#1c1c1c`
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Primary CTA (Marketing); omitted error/success on App Submit; omitted loading/success on Text Field
- interactive kind and state-applicability map for Persona / Segment Card, Tag Chip, and Numbered Step Badge
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
