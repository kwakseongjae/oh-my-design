# LINE migration log

Source: `web/references/line/DESIGN.md`
Destination: `docs/design-md-weight/migrated/line/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/line/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: post-F3 re-run. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/line/DESIGN.md --check --require-portable-core --json` → exit 0, `portable_core: true`, SHA-256 `f87784fa51262197a88ac4223c35b346674ce06bb20b3f25ee4cb726c03b3e62`. Worker pre-F3 SHA `2c74ac46031426f3eb3e025ce16c3cfe08dfad464bebd292a0f8d1fb3cf3dbd9` withdrawn (E2c). `node test-v2/tools/migrate-reference.mjs --brand line --gate-only` → PASS, problems [].

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; captured homepage URL 옮김 → Experience `scope`; logo 경계 옮김 → Typography & Assets | Portable file has no frontmatter. Name kept as H1 `LINE Design System`. Catalog homepage identity `https://line.me` is provenance-only. Captured consumer URL `https://www.line.me/ko/` is dual-destination Experience Scope + provenance Surfaces/Sources/Tier 1 (E2a). Catalog `primary_color` `#06c755` matches official LINE Green (Foundations + Distinctive + provenance identity). Catalog logo Simple Icons slug `line` is dual: provenance Identity + portable Typography & Assets (`Catalog logo metadata is Simple Icons identity (`line`)`) (E2a). The identity-not-captured reading has adjacent complete B2a in Assets. |
| YAML `ds` (name/url/type/description/og_image) | 분리 → provenance; ds.url 옮김 → Experience `scope` | 출처 원장. YAML `ds.type: system` 보존 (A1c). `ds.url` `https://designsystem.line.me` is dual-destination Experience Scope + provenance (E2a). `og_image` is identity metadata, not an interface token (provenance-only). |
| YAML `omd`, `verified`, `verification_v2`, token claims, `tokens.source` / `extracted` / `note` | 분리 → provenance | 출처 원장·freshness·Proof. Footer `Verified: 2026-07-11 (eight-surface collector plus in-app visual verification)` keeps the method `in-app visual verification` in provenance Freshness, not date-only plus collector counts (A1c, E2). Token note (Messenger system fonts vs LINESeed headings; v3.5 `#06c755` replacing `#07b53b`; LY chrome separate) remains as portable surface boundaries in Experience/Foundations/Typography and as provenance metadata (E2a). LY chrome-not-tokens destinations: Scope publisher-chrome B2a + Avoid remaining B2a + Assets catalog-boundary B2a + Named gaps + provenance token note (E2a). |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. LINE Green `#06c755`와 Android Green `#4cc764`와 Blue 500 `#638dff`를 합치지 않음 (A4). The Android Green / Blue 500 unmerged-role readings have adjacent complete B2a in Semantic color (B2/B2a). Input underline `#949494`는 그 필드의 renderable field이며 일반 Gray 500 역할과 같은 값이되 컴포넌트 border 문자열 `0 0 1px solid #949494`를 일반 Border `#e8e8e8`과 합치지 않음 (A4). YAML unitless `lineHeight` 1.334는 비율로 보존하고 고정 px로 바꾸지 않음 (A1a). Product type sizes remain `pt`; marketing 70px/60px remain px. 검증된 primitive type은 컴포넌트별로 보존: button ×4 (primary/destructive/outline/capsule), input, dialog, tab, badge. `Kind: interactive`로 뭉개지 않음 (A1b). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits, Capture record; collector counts 분리 → provenance | 두 표면(marketing LINESeed vs Messenger system), LINE Green, Chat/everyday-life 범위. Identity / `direct, approachable tone` / surface-separation, publisher-chrome (LY not Messenger tokens), 과 surface-application 판단은 각 문단 인접 완전 B2a (B2/B2a). “Keeping those layers separate makes the reference useful…” 목적 해석은 삭제 (B2/B2a). Official first-party component-system range (sheets, navigation, lists, cards, feedback each with defined anatomy, usage, and state behavior) is Capture record narrative (first-party official docs). Harvest-boundary (do not promote tokens for unharvested members) has adjacent complete B2a on Capture record; tokens for those unharvested members stay Named gaps (E2a). Capture 8 surfaces / 41 colors / 10 fonts / 45 variants / five tab interactions / coverage 87/100는 provenance Proof. Live-use 682 `system-ui` / 107 LINESeed는 Typography font evidence에도 남김 (E2a). |
| §1 footer **Verified** / Tier 1 / Tier 2 / Conflicts / LY boundary | 분리 → provenance; LY 금지 옮김 → Experience Scope + Avoid + Assets + Named gaps | freshness·원장. Footer method `in-app visual verification` is provenance Freshness (A1c). LY corporate chrome ≠ Messenger tokens 경계는 portable Scope publisher-chrome B2a + Avoid remaining B2a + Assets catalog-boundary B2a + Named gaps에도 유지 (E1, E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color; Gray 400·Gray 900 hex 생략은 Named gaps에도 | 10 hex 역할, opacity/contrast/HSV/40% disabled-label 규칙, `#07b53b` 제거. Android Green / Blue 500 unmerged-role readings (do not merge into LINE Green) have adjacent complete B2a (B2/B2a). Gray 400·Gray 900 hex는 원본에 없어 생략 — Foundations “No Gray 400 or Gray 900 hex is established here” + Named gaps (E2a). |
| §3 Typography Rules | 옮김 → Typography & Assets; Unresolved `SFPro`는 Named gaps에도 | 다섯 증거 등급. Font evidence application readings (LINESeed distribution is not the Messenger UI default; neither family is a substitute) have adjacent complete B2a (B2/B2a). `SFPro`는 `Unresolved claim` (captured marketing surface, no resolved loaded face) + Named gaps (E2a). Outside this capture로 바꾸지 않음. Product `pt` 스케일 전체 행, marketing 70px/60px/1.334, docs 48px/900·32px/700. Font-mapping / font-use-boundary 판단은 Family 인접 완전 B2a (B2/B2a). Assets catalog-boundary (LINESeed not Messenger default; Simple Icons identity-not-captured; LY chrome) has adjacent complete B2a. SF Pro 하드코드 금지는 Avoid remaining items (인접 B2a). |
| §4 Component Stylings | 옮김 → Components & States | Box button ×3 (`Type: button`), capsule (`Type: button`), text input (`Type: input`), popup (`Type: dialog`), tabs (`Type: tab`), notification badge (`Type: badge`). YAML 기하와 본문 기하를 같은 슬롯에 유지. SVG 29–30px/36px/43px/48px·5px/6px radius 보존. |
| §4 Popup | 옮김 → Components & States | `Type: dialog`와 검증된 geometry·variants·dismissal/affirmative 규칙 보존. §9-only `optional area` anatomy는 Components Anatomy 필드에 보존 (A3). Dialog surface에 `Kind: interactive`와 7-state map을 발명하지 않음 (C4). Affirmative-disabled는 내부 action 상태. |
| §4 Notification Badge | 옮김 → Components & States | `Type: badge`. Kind `non-interactive`; applicability map 생략 (C4). Green content vs red GNB 보존. |
| §5 Layout Principles | 옮김 → Foundations spacing + Layout & Platforms | 16pt margin, 2/3/4-column pt grid, resolution-adaptive column count. Spacing/Layout “not a claim about the wide marketing site’s layout” 판단은 각 절 인접 완전 B2a (B2/B2a). Layout “not a breakpoint-specific CSS contract” layout-contract reading has its own adjacent complete B2a (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `box-shadow: none` 관측은 한정 없이. Hierarchy/elevation synthesis는 인접 완전 B2a (B2/B2a). Invented modal shadow 없음. |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. Capture-bound 머리 인접 완전 B2a — Principles UI-implication 한정이 이 목록을 덮지 않음 (B2/B2a, E1). Governance 통제 문구에 넣지 않음. |
| §7 Don'ts | 옮김 → Experience avoid | First-party Messenger Don'ts (legacy green, popup, tabs, placeholder, capsule placement)는 한정 없이. Remaining catalog-boundary items (hard-code SF Pro, merge LY chrome, transfer 70px LINESeed)는 인접 완전 B2a (B2/B2a). 70px 금지는 Typography “The public 70px LINESeed headline is marketing typography”에도 있음 (E2a). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms + Content & Locales | Resolution-adaptive, 16pt margins, EN/JA/KO/ZH/Thai length, horizontal→vertical actions, scrollable single-line tabs. Layout-contract reading (not a breakpoint-specific CSS contract) has adjacent complete B2a (B2/B2a). Canonical CSS breakpoints 없음 — Layout 문장 + Named gaps (E2a). 원본에 없는 number/date/address/legal locale coverage 문장은 신설하지 않음 (D1). |
| §9 Agent Prompt Guide | 삭제 | 도구별 명령·복붙 프롬프트. 색·font·16pt·body 수치는 이미 Foundations/Typography에 있음. 고유 값만 옮김 (A3): password visibility toggle → Text Input; Popup `optional area` → Components Popup Anatomy; “Treat the public 70px LINESeed headline as marketing typography. Do not transfer it into Messenger product screens.” → Experience avoid remaining items + Typography. 슬롯 없는 위임 없음. |
| §10 Voice & Tone | 옮김 → Content & Locales | Official LINE Voice 세 성질 + 공식 예시. First-party writing rules — derived-editorial 한정을 붙이지 않음 (증거 종류 유지, E1). 합성 보이스 샘플 없음. number/date/address/legal locale coverage 부정 claim은 없음 (D1). |
| §10 voice URL | 분리 → provenance | 보이스 출처. 본문은 first-party Voice 계약을 유지. |
| §11 Brand Narrative | 옮김 → Experience `scope`; URL·서사 원장 분리 → provenance | Chat comes first, more-than-messenger, LY publisher fact, LINESeed vs native type. LY chrome-not-tokens application has adjacent complete B2a on Scope (B2/B2a). 목적 문장(“makes the reference useful”)은 삭제 (B2/B2a). Principles URL / Voice URL / LY Design Style URL은 provenance. 실제 목적지: Experience Scope publisher-chrome B2a + Avoid remaining B2a + Assets catalog-boundary B2a + Named gaps + provenance (E2a). |
| §12 Principles | 옮김 → Experience principles | 여섯 공식 LINE Design Principles는 first-party. *UI implication* 인접 본문에 derived editorial implementation inference / not LINE-authored or a separately published UI specification 한정을 둠 (B2/B2a 예문). Capture-bound application은 §7 행이며 이 UI-implication 한정 밖 — 자체 인접 완전 B2a. Governance 일반 문구는 그 한정의 대체물이 아님. |
| §12 principles URL | 분리 → provenance | 원칙 출처. |
| §13 Personas | mixed: no-persona 경계 + task contexts 옮김 → Experience Audience; fictional demographics 삭제; sidecar 재수록 없음 | 원본 “No current first-party LINE persona definitions…” 관측은 portable Audience에 한정 없이. Observable-work / research prescription은 인접 완전 B2a (B2/B2a). 가상 biography 없음. sidecar 재수록 없음 (D2). Independently verified Primary tasks 3건은 §13 허구가 아니라 official “Chat comes first”, official notification badge, official popup decision (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability | 본문 보존: 10행 표 (Box button, Text/Password/Code input, Capsule, Tabs, Popup, Badge, Destructive action, Disabled label)와 collector five-tab / no form-submit / no destructive / no popup-action 한정 (A2). Official component-system range narrative is Capture record (first-party). Harvest-boundary (do not promote tokens for unharvested members) has adjacent complete B2a; unharvested sheets/navigation/lists/cards/feedback tokens are also Named gaps (E2a, B2/B2a). 선언 컴포넌트는 §4.4를 닫되 미관측 시각값은 발명하지 않음. Popup·Badge는 kind/map 생략이라 C2 표를 닫지 않음 (C4). `not captured`를 `not-applicable` 사유로 쓰지 않음 (C1). Primary/destructive/outline loading는 공식 상태라 applicable; 세 Box Button의 error·success와 Text Input loading·success는 exact label/destination/request/outcome 부재로 field 생략 — `not-applicable`로 닫지 않음 (C2). Capsule loading/disabled/error/success와 Tabs loading/error/success는 식별된 scroll-jump / grouping 역할로 not-applicable. generic focused `#06c755`는 `focus-visible` treatment로 승격하지 않음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion; placeholder wrapper 분리 → provenance omission ledger; 다섯 종류 게이트는 Named gaps에도 | Interaction outcomes 5개(capsule disappear, tab swipe/gesture-priority, box-button loading progress, code auto-submit, popup scroll + fixed actions)는 본문 보존. 무출처 커브 없음(삭제할 cubic-bezier 없음). `[FILL IN: capture computed transition-duration, transition-timing-function, animation-duration, and reduced-motion behavior before adding motion tokens.]`는 portable에 방출하지 않고 provenance 생략 원장에 보관 (E2b). Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 승격한다는 게이트를 전문 명시. Named gaps가 같은 다섯 종류 게이트를 말함 (E2a). “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). |

### F1 / F2 (v7 mandatory final passes)

Superseded by the retro sol resubmit scan below.

## Revision 2026-08-23 (retro sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-retro-2026-08-23-sol-2.md` §3 LINE. Fresh-session F3 is recorded in `audit-log.md` and in the F3 section below.

| Item | Correction |
|---|---|
| Footer method A1c | Provenance Freshness restores `Verified: 2026-07-11 (eight-surface collector plus in-app visual verification)`. Method is not date-only plus collector counts. |
| Official component system A1 | Capture record restores first-party official-docs narrative: sheets, navigation, lists, cards, and feedback each have defined anatomy, usage, and state behavior. Harvest-boundary (do not promote tokens for unharvested members) has adjacent complete B2a. Tokens for those members stay outside the harvested set / Named gaps. |
| Popup `optional area` A3 | Components Popup Anatomy restores §9-only `content, optional area, mandatory dismissal path, action area, and scrim`. Log no longer claims §9 unique values were only password visibility toggle and 70px. |
| B2a / F1 | Adjacent complete B2a on the F1 list below. Official numbered LINE Design Principles stay first-party. Provenance derived inventory matches that list. |
| Box Button / Text Input C2 | Three Box Buttons omit error and success rows; official loading stays applicable. Text Input omits loading and success; official error stays applicable with captured treatment. Not closed as not-applicable. |
| Locale D1 | Deleted `Locale-specific number, date, address, and legal rules ... are omitted`. Not replaced with another coverage negation. |
| Simple Icons E2a | Dual provenance Identity + portable Assets. `og_image` remains provenance-only. Header Rulebook v5 → v7. |

### F1 / F2 (resubmit)

Superseded by the F3 audit scan below. Worker F1 omitted Scope publisher-chrome, Semantic unmerged-role, Font evidence application, Assets catalog-boundary, Capture-record harvest-boundary, and Layout layout-contract. Worker F2 LY destinations omitted Scope. Reconstruction-boundary exemption is not used.

## F3 audit 2026-08-23

Auditor, not the T2 worker. B2a/E2 only. Reconstruction-boundary exemption is not used.

- **F1 B2a scan (full DESIGN.md reread):** Adjacent complete B2a (`derived editorial implementation inference` / `not LINE-authored or a separately published UI specification`) on: Scope identity / `direct, approachable tone` / surface-separation; Scope publisher-chrome boundary; Scope surface-application; Audience observable-work and research prescription; Principles *UI implication* notes only (official numbered LINE Design Principles stay first-party); Capture-bound application; Avoid remaining catalog-boundary items (hard-code SF Pro, merge LY chrome, transfer 70px LINESeed); Semantic color unmerged-role readings (do not merge Android Green or Blue 500 into LINE Green); Spacing surface-application; Shape local-geometry; Elevation hierarchy synthesis; Font evidence application readings; Family font-mapping; Family font-use boundary; Assets catalog-boundary (LINESeed not Messenger default; Simple Icons identity-not-captured; LY chrome / organizational-context-only); Capture-record harvest-boundary; Layout layout-contract (not a breakpoint-specific CSS contract); Layout surface-application (box-button heights / 16pt margins are not a wide-marketing-layout claim). Left unqualified as brand-published or observed-technical, not as a reconstruction-boundary exemption: coverage URLs; official mission / “Chat comes first” / more-than-messenger / LY publisher fact; Distinctive measurements; official numbered LINE Design Principles; first-party Messenger Don'ts; official palette/type/layout/Voice facts; Capture-record official component-system range and definitions; Core C1/C2/C3 policy and per-control C2 omission / B1 notes; B3 five-kind gate; YAML token fields; A4 field-identity of named palette entries (roles themselves); component anatomy and identified-role maps; Box Button and Text Input omission sentences; Governance; Named gaps inventory. This log does not claim that no unqualified sentence remains.
- **F2 E2 grep (value + field/role context):** Footer `in-app visual verification` → provenance Freshness, not portable DESIGN.md. Simple Icons (`line`) → provenance Identity + portable Assets (`Catalog logo metadata is Simple Icons identity (`line`)`) — dual, not provenance-only; identity-not-captured reading under Assets B2a. `ds.og_image` → provenance only. `ds.type` `system` → provenance only. Catalog homepage `https://line.me` → provenance only; captured `https://www.line.me/ko/` and `https://designsystem.line.me` → Scope + provenance. `optional area` → Popup Anatomy. Official component-system sheets/navigation/lists/cards/feedback → Capture record official narrative + harvest-boundary B2a + Named gaps. `Locale-specific number, date, address` → no portable hit. Three Box Buttons → omission sentences, not `not-applicable` error/success rows; loading rows remain applicable. Text Input → official error applicable and loading/success omission, not `not-applicable` loading/success rows. Capsule and Tabs → identified-role `not-applicable` maps. Password visibility toggle → Text Input. 70px LINESeed → Avoid remaining items + Typography. Live-use 682 / 107 → Typography + provenance. Gray 400 / Gray 900 hex → Foundations + Named gaps. `SFPro` → Font evidence Unresolved claim + Named gaps. Canonical CSS breakpoints → Layout + Named gaps. B3 five-kind gate → Foundations Motion + Named gaps. `[FILL IN: capture computed transition-duration…` → provenance omission ledger only. Unitless `1.334` → Type roles. `Type: button` ×4, `Type: input`, `Type: dialog`, `Type: tab`, `Type: badge` → Components. LY chrome → Scope publisher-chrome B2a + Avoid remaining B2a + Assets catalog-boundary B2a + Named gaps + provenance token note. Android Green / Blue 500 unmerged-role → Semantic color B2a. Font evidence application → Font evidence B2a. Harvest-boundary → Capture record B2a. Layout-contract → Layout B2a. Pre-F3 SHA `2c74ac…3dbd9` withdrawn. Post-F3 Core SHA-256 `f87784fa51262197a88ac4223c35b346674ce06bb20b3f25ee4cb726c03b3e62`; gate PASS, problems []. This F2 does not claim compliance stronger than the body.
