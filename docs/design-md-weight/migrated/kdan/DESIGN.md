# Kdan Mobile Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Kdan Mobile is a Tainan, Taiwan digital-workflow SaaS built around PDF productivity, e-signature, and document SDKs. This contract covers the first-party public surface the source inspected for live tokens: `https://www.kdan.com`. Named first-party sources that frame the open-source token library — and that the source does not treat as that live token surface — are `https://github.com/kdan-mobile-software-ltd` and `https://github.com/kdan-mobile-software-ltd/kdan-ui-revamp`. The YAML `ds` record names kdan-ui as Kdan's open-source UI token library (kdanGreen brand token + neutral scale + semantic colors). The YAML token set is `prose-derived`. Every value stays attached to the surface or evidence class that established it. Live button geometry, Clear Sans, the 56px/700 hero, and 16px body attach to kdan.com. kdanGreen, the gray100–gray1000 neutrals, hyperlink, error, and the hover-layer overlay attach to kdan-ui-revamp. Reading kdan.com as this contract's live token surface, keeping kdan-ui-revamp as the named token-library source rather than as that live surface, and keeping every value attached to the surface or evidence class that established it, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

Kdan Mobile presents itself as bold, modern, and developer-credible document software, the kind of digital-workflow SaaS that signs contracts and ships SDKs without losing its sense of energy. The atmosphere is built on a structural deep teal-black base (`#002d37` / `#002D37`) that grounds the interface like ink on a signed page, punctuated by an electric-lime pop (`#caff00` / `#CAFF00`) reserved for the loudest hero moments and the brand's own kdanGreen token (`#00dc87` / `#00DC87`) carried over from its open-source UI library. Type is set in Clear Sans. Corners are crisp at 4px. The overall effect is confident and high-contrast: a serious productivity tool from Tainan, Taiwan that still wants you to feel the charge of getting work done fast. The hex values, Clear Sans, the 4px corners, Tainan, Taiwan, and the source's own "bold, modern, and developer-credible" wording are recorded. Calling the teal-black base ink on a signed page, calling the lime a reserved pop, calling the corners never soft or playful, and reading the whole as energetic document software that respects both the signer and the integrating engineer, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

Brand narrative recorded by the source, kept as narrative context. Kdan Mobile is a Tainan, Taiwan digital-workflow SaaS built around PDF productivity, e-signature, and document SDKs. Its visual identity carries that dual story — the seriousness of signed documents and the credibility of developer tooling — through a deep teal-black structural base, an electric-lime pop accent, and the brand's own kdanGreen token surfaced from its open-source kdan-ui library. Setting everything in Clear Sans with crisp 4px corners, Kdan reads as energetic, modern, and confident: software that handles real contracts and real integrations while still feeling charged with momentum. Tainan, Taiwan, PDF productivity, e-signature, document SDKs, the dual story of signed documents and developer tooling, kdanGreen from kdan-ui, Clear Sans, 4px corners, and that closing charged-with-momentum sentence are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that Tainan-and-document-SDK narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, classifying them as surface-or-control outcomes rather than fictional biographies, and recording that they do not come from the source's persona section, is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library. Each names a surface or control the source records. They do not come from the source's persona section.

- Read the live kdan.com surface for PDF productivity, e-signature, and document SDK framing.
- Use the solid primary action or the single loud electric-lime hero CTA on live kdan.com.
- Read the open-source kdan-ui-revamp token library at `https://github.com/kdan-mobile-software-ltd/kdan-ui-revamp`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source's three persona entries are role archetypes with served-by design notes; those biographies are dropped rather than promoted, and no name, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is the audience at a group level: the signer and the integrating engineer. Reading those source-named groups as this product's audience, and dropping the source's archetype biographies rather than promoting them, is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

### Distinctive traits

The list restates the source's recorded characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, are a derived editorial implementation inference from the verified surfaces — they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

- Structural deep teal-black `#002d37` / `#002D37` as primary-action surface and grounding base on live kdan.com
- kdanGreen `#00dc87` / `#00DC87` as the catalog primary, from the kdan-ui-revamp brand token
- Electric lime `#caff00` / `#CAFF00` reserved for the single loud hero CTA
- Clear Sans across the interface; 56px / 700 hero over 16px body
- Crisp 4px corners and 1.5px solid `#002D37` borders on the solid and outline actions
- Hover-layer overlay `rgba(0,0,0,0.2)` as the primary interaction-depth cue

### Principles

These five items are a derived editorial implementation inference from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library. The source states them in its own Principles section.

- **Confident and high-contrast:** ground in deep teal-black, let one electric-lime accent carry the energy.
- **Developer-credible:** lean on Kdan's own open-source token library (kdanGreen, neutral scale, semantic colors) as the source of truth.
- **Precise, not soft:** crisp 4px corners and 1.5px borders over rounded, shadow-heavy styling.
- **Restraint with accent:** the loud lime is a single exclamation point, never a refrain.
- **Legible by default:** Clear Sans and a steep hierarchy keep document-heavy work readable.

### Application rules

The source states these five as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

- Anchor structure and primary actions with deep teal-black `#002D37` and white `#FFFFFF` text.
- Reserve electric lime `#CAFF00` for a single loud hero CTA, with black `#000000` text for contrast.
- Keep the kdanGreen `#00DC87` token as the brand identity color.
- Use a consistent 4px radius and 1.5px borders across controls.
- Set type in Clear Sans, with a steep 56px/700 hero over 16px body.

### Avoid

The source states these four as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

- Scatter the electric lime across many elements — it loses its punch when overused.
- Soften the 4px corners into large rounded shapes; the brand reads precise and technical.
- Use error red `#F25858` or hyperlink blue `#007AFF` for decoration — they are semantic.
- Invent shadow-heavy elevation; lean on color contrast and the hover overlay.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Role names below are the source's own labels. Pairing each hex to the token-set path named beside it, taking those role names from the source's own labels, keeping catalog identity `primary_color` `#00DC87` on `tokens.colors.brand` rather than on `tokens.colors.primary`, keeping `#191919` on both `foreground` and `body`, keeping `#ffffff` on both `canvas` and `on-primary`, and attaching live-surface roles to kdan.com and library roles to kdan-ui-revamp, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library. The hex values and recorded uses are the source's own.

- **kdanGreen / Brand primary** (`#00dc87` / `#00DC87`): the catalog primary, drawn directly from the kdan-ui-revamp brand token. Token-set path `tokens.colors.brand`. Catalog identity `primary_color` is this hex. Claim surface: kdan-ui-revamp.
- **Structural deep teal-black** (`#002d37` / `#002D37`): solid primary button surface and the grounding structural base on live kdan.com. Token-set path `tokens.colors.primary`. Claim surface: kdan.com.
- **Electric lime** (`#caff00` / `#CAFF00`): loud hero CTA accent, used sparingly for the highest-emphasis moment. Token-set path `tokens.colors.accent-lime`. Claim surface: kdan.com.
- **Hyperlink blue** (`#007aff` / `#007AFF`): semantic link color from the token library. Token-set path `tokens.colors.link`. Claim surface: kdan-ui-revamp.
- **Error red** (`#f25858` / `#F25858`): semantic error state from the token library. Token-set path `tokens.colors.error`. Claim surface: kdan-ui-revamp.
- **Gray 1000** (`#191919`): darkest neutral in the scale. Token-set path `tokens.colors.foreground`. `tokens.colors.body` is the same hex on a second key. Claim surface: kdan-ui-revamp.
- **Gray 100** (`#fafafa` / `#FAFAFA`): lightest neutral in the scale. Token-set path `tokens.colors.surface`. Claim surface: kdan-ui-revamp.
- **Hover layer** (`rgba(0,0,0,0.2)`): semi-transparent overlay token applied on hover. Token-set path `tokens.shadow.hover`. Claim surface: kdan-ui-revamp.
- **Disabled surface** (`#afafaf` / `#AFAFAF`): muted neutral pairing for disabled controls, with text `#4b4b4b` / `#4B4B4B`. Token-set path `tokens.colors.disabled`. The text hex is also `tokens.colors.muted`. Claim surface: kdan.com for the disabled pairing; the muted key is a library neutral.
- **White** (`#ffffff` / `#FFFFFF`): primary button text on the teal-black surface. Token-set path `tokens.colors.on-primary`. `tokens.colors.canvas` is the same hex on a second key.
- **Black** (`#000000`): text on the electric-lime CTA, for maximum contrast. No YAML color key; it lives on `tokens.components.button-hero.fg`.

Roles the source states: kdanGreen anchors the brand identity, deep teal-black does the structural and primary-action work, and electric lime is the single loud accent. The neutral gray scale (gray100 to gray1000) handles surfaces and text, while hyperlink, error, and hover-layer tokens cover semantic and interaction states.

### Spacing

Token-set path `tokens.spacing`, unitless steps as the YAML recorded them.

| Step | Value | Token-set path |
|---|---:|---|
| xs | 4 | `tokens.spacing.xs` |
| sm | 8 | `tokens.spacing.sm` |
| md | 12 | `tokens.spacing.md` |
| base | 16 | `tokens.spacing.base` |
| lg | 24 | `tokens.spacing.lg` |
| xl | 32 | `tokens.spacing.xl` |

`tokens.spacing.xs: 4` is not `tokens.rounded.sm: 4`, not `tokens.rounded.md: 4`, not `tokens.rounded.lg: 4`, and not the component `4px` radius. `tokens.spacing.sm: 8` is a spacing step. `tokens.spacing.md: 12` is a spacing step. `tokens.spacing.base: 16` is not the 16px body or button type-role size and not the `16px / 500` control font. `tokens.spacing.lg: 24` is a spacing step. `tokens.spacing.xl: 32` is a spacing step. Keeping each number on its own key path, rather than treating a shared numeral as the same token, is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

### Shape

Token-set path `tokens.rounded`, unitless steps as the YAML recorded them.

| Step | Value | Token-set path |
|---|---:|---|
| sm | 4 | `tokens.rounded.sm` |
| md | 4 | `tokens.rounded.md` |
| lg | 4 | `tokens.rounded.lg` |
| full | 9999 | `tokens.rounded.full` |

`tokens.rounded.sm: 4`, `tokens.rounded.md: 4`, and `tokens.rounded.lg: 4` are three YAML steps that share the numeral 4. None of them is `tokens.spacing.xs: 4`. The component records write `4px` on those controls; that `4px` is the control radius, not a spacing step. `tokens.rounded.full: 9999` is a YAML step; no component record writes `9999`. Keeping the three 4-steps as three keys, keeping `full: 9999` on its own key path, and keeping component `4px` on the controls, is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

### Elevation

Depth is handled with restraint and high contrast rather than heavy shadow. The primary interaction-depth cue is the hover layer — a semi-transparent `rgba(0,0,0,0.2)` overlay token from the kdan-ui library — which darkens a surface on hover to signal interactivity. Token-set path `tokens.shadow.hover`, with the YAML writing `rgba(0,0,0,0.2) overlay token — darkens surface on hover; depth via contrast + borders, not Z-axis`. Borders do structural work too: solid controls carry a 1.5px solid border in their own surface color (`#002D37`), giving edges definition without relying on drop shadows. The visual stack is built primarily from solid color contrast — deep teal-black against light neutrals — so elevation is felt through tone and the hover overlay more than through layered shadow. Reading that stack as contrast-and-overlay depth rather than a Z-axis shadow scale is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

### Motion

The source does not capture explicit animation durations, easing curves, or transition timing for Kdan, so no specific motion values are asserted. Qualitatively, the documented interaction model is a hover state that applies the `rgba(0,0,0,0.2)` overlay token. Unsourced easing curves from the catalog template are omitted at the curve-value boundary. Treating that absence as an unnamed motion set rather than a default curve, leaving reduced-motion behavior unnamed, keeping the overlay as a hover-layer token rather than as a duration or curve, keeping the source's qualitative overlay line as that qualitative claim rather than as a restored duration or curve, holding the five-kind per-component promotion gate rather than treating a single official overlay as sufficient, and stating that gate in full — including that a partial confirmation does not satisfy it — are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

The source's own qualitative line — that the overlay implies a quick, subtle tonal shift on hover rather than elaborate motion, and that any motion should stay restrained and snappy, matching the confident, developer-credible tone rather than drawing attention to itself — is kept as that qualitative claim. It does not restore a duration or a curve.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings — that kdan-ui-revamp publishes color and overlay tokens and is not a typography specification and does not name a universal current type family; that live kdan.com records Clear Sans, body 16px, and hero 56px/700; that no Kdan-exclusive distributed type family was verified; that `SF Mono` is declared-only with no live use or role recorded; and that no font-license sentence is recorded in the source — are a derived editorial implementation inference from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

| Evidence class | Resolution |
|---|---|
| Official product-use | kdan-ui-revamp publishes color and overlay tokens. It is not a typography specification and does not name a universal current type family. |
| Live computed surface-use | Live kdan.com records Clear Sans; body 16px; hero heading 56px/700. |
| Official distributed asset | No Kdan-exclusive distributed type family was verified. |
| Declared-only | YAML `tokens.typography.family.mono` is `SF Mono`. No live use or role is recorded. |
| License | No font-license sentence is recorded in the source. |

### Family

- **sans:** `Clear Sans` — Token-set path `tokens.typography.family.sans`. Claim surface: kdan.com.
- **mono:** `SF Mono` — Token-set path `tokens.typography.family.mono`. Declared-only; no use is recorded.

Do not replace Clear Sans with a system substitute, and do not present a fallback as Clear Sans. Do not invent a use for `SF Mono`. That fallback prohibition, and leaving `SF Mono` as a declared family with no invented role, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

### Type roles

Token-set `use` strings are kept verbatim. The longer §3 spellings sit beside them; neither was chosen as a replacement. YAML sizes stay the numbers the YAML recorded; the parenthetical px figures are the source §3 spelling. Keeping the YAML `use` strings verbatim, keeping the YAML singles and the §3 longer spellings on separate readings, and refusing to invent a line-height ratio the YAML does not record, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

| Role | Font | Size | Weight | Token-set use | §3 longer spelling |
|---|---|---:|---:|---|---|
| Hero heading | Clear Sans | 56 (`56px`) | 700 | Hero heading, steep declarative top | 56px at weight 700; a 56px/700 hero headline sits far above 16px body |
| Hero CTA label | Clear Sans | 22 (`22px`) | 500 | Hero CTA label | Hero CTA label: 22px at weight 500 |
| Body | Clear Sans | 16 (`16px`) | 400 | Standard body, document-heavy reading | Body: 16px; sustained legibility in document-heavy contexts |
| Button label (standard) | Clear Sans | 16 (`16px`) | 500 | Standard button label, medium weight | Button label (standard): 16px at weight 500; medium 500 weight for a solid, tappable feel without shouting |

Clear Sans is a humanist sans-serif. The hierarchy is steep — a 56px/700 hero headline sits far above 16px body — giving pages a confident, declarative top while keeping reading text calm. Button labels favor the medium 500 weight. The humanist-sans and steep-hierarchy sentences are the source's own; calling them implementation rules for every unobserved surface is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

### Assets

- Catalog identity pointer: `logo.type: favicon`, `logo.slug: https://www.kdan.com/apple-touch-icon-152x152.png`.
- Open-source token library: `https://github.com/kdan-mobile-software-ltd/kdan-ui-revamp` (kdan-ui).

Reading the favicon URL as an identity pointer, and reading kdan-ui-revamp as the named token-library source rather than as a hosted brand file download, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted:

- **Default (solid primary):** background `#002D37`, text `#FFFFFF`, 1.5px solid `#002D37` border, 4px radius, 38px height.
- **Hover:** `rgba(0,0,0,0.2)` hover-layer overlay token darkens the surface to signal interactivity.
- **Disabled:** background `#AFAFAF`, text `#4B4B4B`, 4px radius — clearly muted and non-interactive.
- **Error (semantic):** error red `#F25858` reserved for error messaging and validation.
- **Link (semantic):** hyperlink blue `#007AFF` for inline links.

Error and Link in that list are semantic color roles. They are not a button error treatment and not a declared link component.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. `not-applicable` is used only where the control's role makes the state meaningless — a destination action on the live kdan.com surface, a paired secondary action, or a non-interactive disabled record — and the reason given is always that semantic one. Preserving the source state contract here rather than delegating it to an unadopted catalog graph, reading Error and Link in that list as semantic color roles rather than as a button error treatment or a declared link component, the role-based decision procedure above, every interactive-kind verdict, every applicability verdict, the reason given for either, and the refusal to treat this as a complete state-coverage claim, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library. This is not a complete state-coverage claim.

### Solid primary

- Role: primary action on live kdan.com
- Primitive type: `button` · Kind: interactive
- Background: `#002d37` / `#002D37`
- Text: `#ffffff` / `#FFFFFF`
- Border: `1.5px solid #002d37` / `1.5px solid #002D37`
- Radius: `4px`
- Height: `38px`
- Font: `16px / 500`
- Hover: `rgba(0,0,0,0.2)` overlay
- Token-set use: `Primary action on live kdan.com`
- The 38px height is this control's geometry. The `4px` radius is this control's radius; it is not `tokens.spacing.xs: 4` and not `tokens.rounded.sm: 4` written as a spacing step. The `16px / 500` font is this control's font; it is not `tokens.spacing.base: 16` and not the body type-role. Reading those figures as this control's geometry rather than those YAML steps is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web action; `rgba(0,0,0,0.2)` overlay is the recorded hover-layer |
| focus-visible | applicable | Keyboard-reachable action; visual treatment omitted |
| disabled | applicable | A primary action can be gated; the Disabled record holds the muted pairing |
| loading | not-applicable | Destination primary action on the live kdan.com surface; it commits no operation in place |
| error | not-applicable | Destination primary action; error red is reserved for error messaging and validation |
| success | not-applicable | Destination primary action on the live kdan.com surface; it commits no operation in place |

### Outline

- Role: secondary action paired alongside the solid primary
- Primitive type: `button` · Kind: interactive
- Background: `transparent`
- Text: `#002d37` / `#002D37`
- Border: `1.5px solid #002d37` / `1.5px solid #002D37`
- Radius: `4px`
- Height: `38px`
- Font: `16px / 500`
- Token-set use: `Secondary action paired with solid primary`
- §4 longer use: secondary action paired alongside the solid primary
- The 38px height is this control's geometry, shared with the solid primary so action rows align. The `4px` radius and `16px / 500` font are this control's. Reading those figures as this paired secondary's geometry rather than as a spacing step or the solid-primary record is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A secondary action can be gated; visual treatment omitted |
| loading | not-applicable | Paired secondary action; it commits no operation in place |
| error | not-applicable | Paired secondary action; error red is reserved for error messaging and validation |
| success | not-applicable | Paired secondary action; it commits no operation in place |

### Lime hero CTA

- Role: single loud, high-emphasis hero call-to-action
- Primitive type: `button` · Kind: interactive
- Background: `#caff00` / `#CAFF00`
- Text: `#000000`
- Border: none
- Radius: `4px`
- Height: `53px`
- Font: `22px / 500`
- Token-set use: `Single loud high-emphasis hero CTA`
- §4 longer use: single loud, high-emphasis hero call-to-action
- The 53px height is this hero CTA's geometry. It is not the solid or outline 38px height. The `22px / 500` font is this control's font; it is the hero-cta type-role, not a spacing step. `#000000` is this control's text; it is not a YAML color key. Reading 53px / 22px / `#000000` as this hero record rather than as the solid primary or a spacing step is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web hero CTA; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable CTA; visual treatment omitted |
| disabled | applicable | A hero CTA can be gated; visual treatment omitted |
| loading | not-applicable | Destination hero CTA on the live kdan.com surface; it commits no operation in place |
| error | not-applicable | Destination hero CTA; error red is reserved for error messaging and validation |
| success | not-applicable | Destination hero CTA on the live kdan.com surface; it commits no operation in place |

### Disabled

- Role: non-interactive disabled state
- Primitive type: `button` · Kind: interactive
- Background: `#afafaf` / `#AFAFAF`
- Text: `#4b4b4b` / `#4B4B4B`
- Border: none
- Radius: `4px`
- Token-set use: `Disabled state`
- YAML `disabled:` `non-interactive`
- §4 longer use: non-interactive disabled state
- This record is the muted pairing. `#AFAFAF` is `tokens.colors.disabled` and this control's background. `#4B4B4B` is `tokens.colors.muted` and this control's text. They are not transferred onto another component as that other component's default fields. Reading those hexes as this disabled record, rather than as a fourth radius or a type-role, is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | This record is the disabled treatment |
| hover | not-applicable | Non-interactive disabled record; hover is not a meaningful state of this record |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | YAML `disabled: non-interactive`; this record is that state |
| loading | not-applicable | Non-interactive disabled state; it commits no operation in place |
| error | not-applicable | Non-interactive disabled state; error red is reserved for error messaging and validation |
| success | not-applicable | Non-interactive disabled state; it commits no operation in place |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Kdan's layout reads as confident document software: a structural deep teal-black base anchors the page, with content set against the light neutral scale (gray100 `#FAFAFA` at the bright end). A steep type hierarchy — 56px/700 hero down to 16px body — establishes a clear declarative top and a calm reading body. Primary and outline actions share the same 38px height and 4px radius so action rows align cleanly, while the taller 53px lime hero CTA stands apart as the single loudest target. The crisp 4px corner radius is applied consistently across controls.

The source does not capture explicit breakpoints, fluid grids, or per-viewport measurements for Kdan, so specific responsive numbers are not asserted here. Qualitatively, the fixed control heights (38px standard, 53px hero CTA) and the consistent 4px radius are designed to hold their proportions across viewports, while the steep Clear Sans hierarchy (56px/700 hero, 16px body) gives a reliable anchor for scaling text down on smaller screens. Action rows that pair the solid primary and outline buttons share a common 38px height, which keeps them aligned whether stacked or placed side by side.

The 38px, 53px, 56px/700, 16px, and 4px figures are the source's recorded control and type measurements. Reading them as those live kdan.com observations rather than as a responsive grid specification, and leaving breakpoints, fluid grids, and per-viewport measurements unnamed rather than filling them, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Kdan's voice is bold, modern, and confident — the tone of a document-software company that is sure of its product and credible to developers. It speaks plainly and energetically, matching the high-contrast structural base and the single electric-lime exclamation point. Copy should feel capable and direct: productivity language that respects the signer's time and the engineer's intelligence, never cute or padded. Those sentences are the source's own voice paragraph. Reading them as this contract's public voice rather than as a separately published Kdan microcopy guide, and reproducing issued names byte-exact, are derived editorial implementation inferences from the verified surfaces; they are not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

Issued names, kept byte-exact: Kdan Mobile, kdanGreen, kdan-ui, kdan-ui-revamp, Clear Sans, SF Mono, Gray 1000, Gray 100.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Calling the list a set of named gaps rather than a domain inventory is a derived editorial implementation inference from the verified surfaces; it is not Kdan-authored or taken from a separately published UI specification, including the published kdan-ui-revamp token library.

- explicit breakpoints, fluid grids, and per-viewport measurements
- animation durations, easing curves, and transition timing
