# KakaoPay Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

KakaoPay is a Korean financial platform whose public main site presents everyday payment, money-management, and financial services under the line “마음 놓고 금융하다.” This contract's selector-backed tokens attach to one inspected surface from 2026-07-13: the Kakao corporate service page at `https://www.kakaocorp.com/page/service/service/KakaoPay`. Named first-party sources that frame the service, the graphic system, the font asset, or the API error contract — and that the source does not treat as that token surface — are `https://www.kakaopay.com/main`, `https://www.kakaopay.com/services/life/payment?t_ch=membership&t_src=homepage`, `https://partner.kakaopay.com/partner/online/introduction`, `https://story.kakaopay.com/`, `https://story.kakaopay.com/225-kakaopay-design/`, `https://www.kakaocorp.com/page/detail/11571`, and `https://developers.kakaopay.com/docs/payment/online/reference`. Every value stays attached to the surface that established it. The YAML `ds` record names the 2024 design article as KakaoPay design story and records that article as graphic-accessibility work, not a public component library. Reading the corporate service page as this contract's token surface, keeping values attached to the surface that established them, and treating the main, payment, partner, story, font-resource, and developer-docs URLs as named sources for framing, graphic direction, font license, or API error-code documentation rather than as that token surface, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

The current public expression frames finance as an everyday action rather than as a separate institutional task. KakaoPay's 2024 design article makes the visual rationale unusually explicit: graphics were rebuilt for accessibility and operational reuse, replacing black-line graphics with filled forms, requiring a minimum 3:1 contrast between graphic color and adjacent background, and separating graphics into icon, 2D, and 3D levels by information priority. It characterizes the desired expression as simple and clear, warm and soft in color, and rounded and soft in line. Those are editorial and graphic-system principles, not permission to infer a private payment-app component kit. The supplied capture covers the KakaoPay story and Kakao corporate service pages; its selector-backed controls come only from the latter. The slogan, the 2024 article's contrast and filled-form and icon/2D/3D facts, the official wording of the desired expression, and the capture-boundary sentence are the source's own. Calling that public expression everyday-finance framing, and keeping those graphic-system principles from authorizing a private payment-app component kit, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. KakaoPay presents payment, money management, and financial services as activities that can sit inside everyday life. Its official payment page focuses on immediate payment and biometric verification, while its partner page describes a four-step online-payment journey from a merchant checkout click to completion. Its design story adds a complementary rationale: accessibility and operational efficiency were explicit goals of a graphic-system redesign, and the work continues as the service evolves. That evidence supports the narrative of accessible, reusable graphic communication; it does not substantiate historical, market-share, or user-persona claims not present in these sources. The everyday-life positioning, the biometric-verification focus, the four-step merchant journey, the accessibility and operational-efficiency redesign goals, the continuing-evolution sentence, and that closing pair of sentences are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that payment-and-graphic narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, and classifying them as surface-or-control outcomes rather than fictional biographies, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification. Each names a surface the source records. They do not come from the source's persona section.

- Read the official main-site line “마음 놓고 금융하다” and the everyday-finance service framing on `https://www.kakaopay.com/main`.
- Read the official payment-service description of card- and cash-free payment, including biometric verification.
- Scan the Kakao corporate service directory chrome for KakaoPay at `https://www.kakaocorp.com/page/service/service/KakaoPay`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source states that no official persona research was reviewed and that the public sources identify broad audiences but do not support named, demographic, or behavioral personas. No name, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is the audience at a group level: people using KakaoPay for everyday payments, and merchants integrating online payments. Refusing to promote individual personas, and reading those source-named groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, are a derived editorial implementation inference from the verified surfaces — they are not KakaoPay-authored or a separately published UI specification.

- Everyday-finance product framing on official KakaoPay marketing and merchant surfaces
- Official graphic direction: clear filled forms, warm/soft color, rounded/soft lines
- A documented 3:1 minimum graphic-to-background contrast target
- Loaded KakaoSmall and KakaoBig on the captured corporate service page
- Corporate service controls, marketing content, developer documentation, and unobserved authenticated payment flows kept separate

### Principles

The official-article names and the sentences that attribute them to the 2024 design article are first-party graphic-system material. Every *UI implication* below, and the reading of those values as implementation principles for the captured public and corporate surfaces, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

1. **Graphic accessibility is a product concern.** The official article requires at least 3:1 contrast between a graphic color and adjacent background. *UI implication:* measure graphic contrast instead of assuming yellow or any other familiar brand cue will work in context.
2. **Make graphic information clear.** KakaoPay describes replacing black-line graphics with filled forms to reduce visual collision with text. *UI implication:* preserve a clear text/graphic distinction when using the documented graphic approach.
3. **Match visual detail to information priority.** The article differentiates icon, 2D, and 3D graphic levels. *UI implication:* do not mix tiers merely as decoration; choose the level for the information hierarchy.
4. **Support reuse without flattening context.** The redesign consolidated more than one thousand graphics for organization-wide use. *UI implication:* retain source and context metadata when reusing an observed asset or control.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

- Use the official graphic direction—clear filled forms, warm/soft color, and rounded/soft lines—when a task is specifically about KakaoPay graphics.
- Check graphic/background contrast against the official 3:1 minimum described by KakaoPay.
- Keep KakaoSmall/KakaoBig claims tied to the captured public/corporate surfaces and their official distribution context.
- Preserve the provenance of the four recorded corporate controls.

### Avoid

The source states these four as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

- Do not treat corporate directory chrome as a KakaoPay authenticated payment screen.
- Do not substitute a system font and call it KakaoSmall or KakaoBig.
- Do not invent payment states, semantic colors, responsive rules, or component variants from the marketing article.
- Do not promote KakaoDigitLatin or Helvetica Neue into the UI family without visible loaded use.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Role names below are the source's own labels. Pairing each hex to the token-set path named beside it, taking those role names from the source's own labels, keeping catalog identity `primary_color` `#ffeb00` off the reconciled token block, and attaching every role to the corporate-service YAML claim rather than relabeling an observed corporate value as a payment-app CTA, semantic status color, or brand-color replacement, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification. The hex values and recorded uses are the source's own. Surface attachments follow the YAML claim anchors.

- **Ink** (`#000000`): observed menu text and dark menu fill on the Kakao corporate service page. Token-set path `tokens.colors.ink`. Claim surface: corporate-service.
- **Text** (`#333333`): observed corporate-service control, card, and list text. Token-set path `tokens.colors.text`. Claim surface: corporate-service.
- **Muted** (`#666666`): observed secondary corporate-service text. Token-set path `tokens.colors.muted`. Claim surface: corporate-service.
- **Subtle** (`#888888`): observed secondary corporate-service text. Token-set path `tokens.colors.subtle`. Claim surface: corporate-service.
- **Canvas** (`#ffffff`): observed corporate-service menu background. Token-set path `tokens.colors.canvas`. Claim surface: corporate-service.
- **Surface** (`#f3f3f3`): observed corporate-service menu-hover and service-card background. Token-set path `tokens.colors.surface`. Claim surface: corporate-service.
- **Surface soft** (`#eeeeee`): observed corporate-service search and tag background. Token-set path `tokens.colors.surface-soft`. Claim surface: corporate-service.

The official KakaoPay design article establishes contrast and a newly made graphic palette, but the readable article does not publish hexadecimal values for a general KakaoPay product palette. The catalog's existing `primary_color` is not promoted into the reconciled token block.

### Spacing

Token-set path `tokens.spacing`, unitless steps as the YAML recorded them. Claim surface for all three steps: corporate-service.

| Step | Value | Token-set path | Claim surface |
|---|---:|---|---|
| compact | 4 | `tokens.spacing.compact` | corporate-service |
| control | 16 | `tokens.spacing.control` | corporate-service |
| card | 32 | `tokens.spacing.card` | corporate-service |

`tokens.spacing.compact: 4` is not the menu padding `4px` in `4px 16px 6px`. `tokens.spacing.control: 16` is not `tokens.rounded.card: 16`, not the 16px in that same menu padding, and not a type size. `tokens.spacing.card: 32` is the YAML card step; the corporate card's horizontal inner-card padding is the source's `32px` spelling of that same corporate-service observation. Keeping each number on its own key path, rather than treating a shared numeral as the same token, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded`, unitless steps as the YAML recorded them. Claim surface for all five steps: corporate-service.

| Step | Value | Token-set path |
|---|---:|---|
| none | 0 | `tokens.rounded.none` |
| card | 16 | `tokens.rounded.card` |
| search | 18 | `tokens.rounded.search` |
| action | 24 | `tokens.rounded.action` |
| pill | 999 | `tokens.rounded.pill` |

`tokens.rounded.card: 16` is not `tokens.spacing.control: 16`. `tokens.rounded.search: 18` is the YAML search step; the search action records `18` / `18px` on that control. `tokens.rounded.action: 24` is a YAML step with no matching component radius in the token set. `tokens.rounded.pill: 999` is a step; the menu action records `999` / `999px` on that control. The topic tag records `34` / `34px` on that control; that `34` is not a YAML rounded step. Keeping those local radii on their components, and keeping each YAML step on its own key path, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

### Elevation

The representative captured controls and card use `box-shadow: none`. Token-set path `tokens.shadow.none` with value `none`. No public evidence in this pass establishes a KakaoPay elevation scale, modal shadow, bottom sheet, or floating action treatment. Reading that stack as flat observed layering on the corporate-service controls and card only, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

### Motion

No motion durations, easing curves, reduced-motion behavior, or animated payment-state evidence was captured. Motion tokens are omitted. Treating that absence as an unnamed motion set rather than a default curve, and leaving reduced-motion behavior unnamed, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | **KakaoSmall** is computed on 156 elements and **KakaoBig** on 45 elements in the supplied three-route artifact. Both have a high-confidence loaded FontFaceSet match and Kakao CDN `@font-face` sources. This verifies their use on the captured story/corporate public surfaces, not in an authenticated KakaoPay app. |
| Official distributed brand asset and license | Kakao's official font resource describes Kakao Big as the display-oriented face and Kakao Small as the body/caption-oriented face. It says the pair is available under OFL for personal and commercial use. This parent-company distribution evidence explains the family and licensing boundary; it is not a statement that every Kakao service uses it. |
| Declared-only | **KakaoDigitLatin** has four declared CDN font-face sources in the artifact but no visible computed use, so it is not a UI token. |
| Declared/system fallback | Apple SD Gothic Neo, Malgun Gothic, and 맑은 고딕 occur after the loaded Kakao families in captured declarations. They are fallback context, not KakaoPay font substitutions. |
| Unresolved class | Helvetica Neue appears in a small number of computed observations without a matching loaded FontFace or source URL. It is omitted from tokens. |

Reading Kakao Big / Kakao Small as the official resource's display and body/caption roles, classing KakaoDigitLatin as declared-only rather than a UI token, classing Apple SD Gothic Neo / Malgun Gothic / 맑은 고딕 as fallback context rather than substitutions, and leaving Helvetica Neue omitted, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

### Family

- **sans:** `KakaoSmall` — Token-set path `tokens.typography.family.sans`. Claim surface: corporate-service.
- **display:** `KakaoBig` — Token-set path `tokens.typography.family.display`. Claim surface: corporate-service.

Do not replace KakaoSmall or KakaoBig with a system substitute, and do not present a fallback as KakaoSmall or KakaoBig. That fallback prohibition is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

### Type roles

YAML unitless line heights stay ratios (A1a). The parenthetical px figures are the source §3 spelling, not a replacement of the YAML ratio. Token-set `use` strings are kept verbatim. Surface attachments follow the YAML claim anchors. Keeping YAML line heights as unitless ratios, keeping the YAML singles and the §3 px spellings on separate readings, and attaching surfaces from the YAML claim anchors, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Token-set use | Claim surface |
|---|---|---:|---:|---|---|---|---|
| Corporate card title | KakaoBig | 26px | 700 | 1.38 (36px) | -0.6 | Observed corporate-service card title only. | corporate-service |
| Corporate public body | KakaoSmall | 14px | 400 | 1.5 (21px) | 0 | Observed corporate-service list and card copy only. | corporate-service |
| Corporate utility link | KakaoSmall | 12px | 400 | 1.5 (18px) | -0.2 | Observed corporate-service utility link only. | corporate-service |

The 26px card title is `surface-3::strong.tit_card` only. The 17px / 400 / KakaoBig menu font and the 13px / 400 / KakaoBig tag font are those controls' fonts; they are not YAML type-role rows. The 14px public-body size is not the search action's 14px / 400 / KakaoSmall font as a shared type-role row. Reading those sizes as the roles or controls named beside them, rather than as shared numerals across spacing or another component, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

### Assets

- Catalog identity pointer: `logo.type: favicon`, `logo.slug: https://t1.kakaocdn.net/kakaopay/icons/web/192-brand.png`.
- Official font resource: `https://www.kakaocorp.com/page/detail/11571`. Kakao Big / Kakao Small under OFL. That license describes the font asset, not a KakaoPay product-surface specification.

Reading the favicon URL as an identity pointer, and reading the OFL sentence as a font-asset license rather than a KakaoPay product-surface specification, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted:

The collector records no interaction snapshots (`interactionCount: 0`). Hover and pressed labels are preserved as labels rather than expanded into unobserved variants. No selector-backed KakaoPay payment-app button, input, receipt, account card, transaction row, toast, sheet, error, success, or mobile navigation component was captured.

The reviewed public developer reference documents API error responses and error codes, while the supplied UI artifact records no interaction snapshots. This supports neither a visual error-state specification nor payment success/loading/empty-state components. For implementation, use the endpoint’s documented error code/message contract and obtain a product-surface capture before defining visual state tokens.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. A generic hover or pressed collector label is not `focus-visible` treatment evidence; the observed hover/pressed labels stay recorded as those observed labels, and they are not a color assigned to the `focus-visible` row. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. `not-applicable` is used only where the control's role makes the state meaningless — a destination navigation item, a search trigger that commits no operation in place, or a destination topic link — and the reason given is always that semantic one. Where the source supplies no interaction evidence for a row at all, its kind and applicability map are omitted rather than decided. Preserving the source state contract here rather than delegating it to an unadopted catalog graph, the role-based decision procedure above, every interactive-kind verdict, every applicability verdict, the reason given for either, and the refusal to treat this as a complete state-coverage claim, are derived editorial implementation inferences from the verified surfaces; they are not KakaoPay-authored or a separately published UI specification. This is not a complete state-coverage claim.

### Menu action

- Role: corporate navigation chrome on the Kakao corporate service page
- Primitive type: `button` · Kind: interactive
- Background: `#ffffff`
- Text: `#000000`
- Radius: `999px`
- Padding: `4px 16px 6px`
- Font: `17px / 400 / KakaoBig`
- Token-set use: `surface-3::[data-omd-capture="1"] on the Kakao corporate service page, not a KakaoPay payment-app control.`
- Observed: default. Hover: `#f3f3f3` background (collector label). Pressed: collector label observed; no separate snapshot value captured.
- Claim surface: corporate-service
- The `999px` radius is this menu's geometry. It is not only `tokens.rounded.pill: 999`. The `4px` and `16px` in the padding are this control's padding. They are not `tokens.spacing.compact: 4` or `tokens.spacing.control: 16`. The 17px / 400 / KakaoBig font is this control's font, not a YAML type-role row. Reading those figures as this menu's geometry rather than as those YAML steps or type-role rows is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web navigation item; hover captured as collector label `#f3f3f3` |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A navigation item can be gated; visual treatment omitted |
| loading | not-applicable | Destination navigation item; it commits no operation in place |
| error | not-applicable | Destination navigation item; it commits no operation in place |
| success | not-applicable | Destination navigation item; it commits no operation in place |

### Search action

- Role: corporate search action on the Kakao corporate service page
- Primitive type: `button` · Kind: interactive
- Background: `#eeeeee`
- Text: `#333333`
- Radius: `18px`
- Padding: `0px`
- Font: `14px / 400 / KakaoSmall`
- Token-set use: `surface-3::[data-omd-capture="7"] on the Kakao corporate service page.`
- Observed: default. Hover: `#eeeeee` background (collector label). Pressed: collector label observed; no separate snapshot value captured.
- Claim surface: corporate-service
- Token-set path `tokens.components.corporate-search.bg` is `#eeeeee`. The hover collector label is also `#eeeeee`. Those are two readings of the same hex on this control, not a transfer from `tokens.colors.surface-soft`. The `18px` radius is this search action's geometry. It is not only `tokens.rounded.search: 18`. The 14px / 400 / KakaoSmall font is this control's font; it is not the public-body type-role row. Reading those figures as this search action's geometry rather than as `tokens.colors.surface-soft` or the public-body type-role row is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web search trigger; hover captured as collector label `#eeeeee` |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A search action can be gated; visual treatment omitted |
| loading | not-applicable | Search trigger; it commits no operation in place |
| error | not-applicable | Search trigger; it commits no operation in place |
| success | not-applicable | Search trigger; it commits no operation in place |

### Service card

- Role: corporate Kakao service-directory card
- Primitive type: `card`
- Background: `#f3f3f3`
- Text: `#333333`
- Radius: `16px`
- Padding: `0px`
- Font: `14px / 400 / KakaoSmall`
- Token-set use: `surface-3::div.item_card_new.item_normal_card on the Kakao corporate service page.`
- Claim surface: corporate-service
- The `16px` radius is this card's geometry. It is not `tokens.spacing.control: 16`. The 14px / 400 / KakaoSmall font is this card's font; it is not only the public-body type-role row. Reading those figures as this card's geometry rather than `tokens.spacing.control: 16`, and reading the 14px / 400 / KakaoSmall font as this card's font rather than as a shared public-body type-role row, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.
- Kind and applicability map omitted — the source supplies no interaction evidence for the card (C4).

### Topic tag

- Role: corporate-service topic tag
- Primitive type: `badge` · Kind: interactive
- Background: `#eeeeee`
- Text: `#000000`
- Radius: `34px`
- Padding: `0px 15px`
- Font: `13px / 400 / KakaoBig`
- Token-set use: `surface-3::[data-omd-capture="26"].link_tag on the Kakao corporate service page.`
- Observed: default only
- Claim surface: corporate-service
- The `34px` radius is this tag's geometry. It is not a YAML rounded step. The 13px / 400 / KakaoBig font is this control's font, not a YAML type-role row. Token-set path `tokens.components.corporate-tag.bg` is `#eeeeee`. That hex is this tag's background; it is not a transfer from `tokens.colors.surface-soft` or from the search action. Reading those figures as this tag's geometry rather than as a YAML rounded step, a type-role row, or another component's background is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web destination link; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable link; visual treatment omitted |
| disabled | applicable | A topic link can be gated; visual treatment omitted |
| loading | not-applicable | Destination topic link; it commits no operation in place |
| error | not-applicable | Destination topic link; it commits no operation in place |
| success | not-applicable | Destination topic link; it commits no operation in place |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The supplied artifact has a corporate service page, a KakaoPay story home, and a KakaoPay design article. The selector-backed corporate card uses 32px horizontal inner-card padding, while observed spacing values also include 4px and 16px in public controls. The official product and partner pages establish that payment is offered across PC, mobile web, and mobile app contexts, but they do not provide a public responsive component specification in the material reviewed here. Reading those spacing figures as the corporate-service observations the source names, rather than as a checkout or authenticated-layout specification, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

The 999px menu, 18px search action, 16px service card, 34px topic tag, 17px / 14px / 13px control fonts, and 32px inner-card padding are measurements on the Kakao corporate service page. Reading those heights and paddings as that page's measurements rather than as cross-viewport specifications is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Official public copy frames the service as “마음 놓고 금융하다” and explains payment with direct, everyday language such as paying without cards or cash. The partner surface similarly uses short, instructional statements for merchant onboarding. That supports a plain, reassuring public register; it does not establish a private in-app microcopy guide. Reading that register as this contract's public voice, rather than as a separately published KakaoPay microcopy guide, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

Published names and lines the source records, kept byte-exact: KakaoPay, 마음 놓고 금융하다, KakaoSmall, KakaoBig, Kakao Big, Kakao Small, KakaoDigitLatin, Apple SD Gothic Neo, Malgun Gothic, 맑은 고딕.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Calling this list a set of named gaps rather than a domain inventory, and treating the items as unnamed values rather than permissions to invent, is a derived editorial implementation inference from the verified surfaces; it is not KakaoPay-authored or a separately published UI specification.

- hexadecimal values for a general KakaoPay product palette, and catalog identity `primary_color` `#ffeb00` as a reconciled color token
- payment-app CTA, semantic status color, or brand-color replacement relabeled from an observed corporate value
- KakaoDigitLatin or Helvetica Neue as a UI family
- reusable duration, easing curve, reduced-motion behavior, and animated payment-state evidence
- a KakaoPay elevation scale, modal shadow, bottom sheet, or floating action treatment
- selector-backed KakaoPay payment-app button, input, receipt, account card, transaction row, toast, sheet, error, success, or mobile navigation component
- visual error-state specification and payment success/loading/empty-state components
- payment-app grid, safe-area rule, checkout layout, or responsive breakpoint
- authenticated product layout
