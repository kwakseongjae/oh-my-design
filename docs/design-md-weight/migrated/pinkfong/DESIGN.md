# The Pinkfong Company Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

The Pinkfong Company (더핑크퐁컴퍼니) is a global family-entertainment company whose official corporate pages describe a portfolio led by Pinkfong, Baby Shark, and Bebefinn. This contract covers the three public corporate routes the source inspected on 2026-07-13: corporate-home at `https://www.thepinkfongcompany.com/`, corporate-company at `https://www.thepinkfongcompany.com/company`, and corporate-business at `https://www.thepinkfongcompany.com/business`. The English company and business pages at `https://www.thepinkfongcompany.com/en/company` and `https://www.thepinkfongcompany.com/en/business`, the official identity page at `https://www.thepinkfongcompany.com/company`, and the Pinkfong Baby Shark Font release at `https://www.thepinkfongcompany.com/news/pr/207` are named brand-owned sources; they confirm mission, portfolio, CI/BI downloads, and the font release, and they do not by themselves supply the selector-backed interface tokens below. Treating those three inspected corporate routes as this contract's token surfaces, and keeping the English pages, identity page, and font release as named sources that do not by themselves supply selector-backed interface tokens, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

The current reference is intentionally narrower than that portfolio: its supplied collector evidence covers only the public corporate home, company, and business pages. Those pages use a mostly white, black, and gray corporate shell with Pinkfong pink (`#ff66af`) for a prominent action, rather than proving a universal consumer-product interface. Public corporate pages use white `#ffffff`, black `#000000`, muted gray `#5a5a5a`, and a measured Pinkfong-pink `#ff66af` action. `#ff66af` is a selector-backed corporate-action value, not a blanket claim for consumer, app, or franchise controls. Spoqa Han Sans Neo is loaded on the corporate capture; the generic system stack is separately observed on many public controls. Pinkfong Baby Shark Font is an official distributed brand asset and one loaded corporate-business display use; it is not promoted to the corporate UI family. Only corporate-page defaults and their exact selector/surface provenance are documented. Product flows, consumer-site UI, and generic family-site components are omitted. The company’s identity page supplies CI and BI downloads for The Pinkfong Company, Pinkfong, Baby Shark, and Bebefinn. It also links to an official Pinkfong Baby Shark Font release. That official asset expresses the character side of the brand, while the captured corporate shell uses a loaded Spoqa Han Sans Neo body family plus system-stack controls. The distinction is important: identity assets and one loaded display heading are not a general app type scale or a license to substitute a system font for the named family. YAML `ds` records the identity page as The Pinkfong Company Identity at `https://www.thepinkfongcompany.com/company`, `type: brand`, with the source description “Official corporate identity page with company and franchise BI downloads; not a public product-component specification.” The prior reference mixed a historical consumer capture into this corporate-only artifact. This update preserves official brand and font context but retains machine tokens and components only where the 2026-07-13 supplied evidence provides a current selector and surface. Reading those pages as a corporate shell rather than as proof of a universal consumer-product interface, treating the identity-asset versus captured-shell distinction as not a general app type scale and not a license to substitute a system font for the named family, keeping the measured pink action as a selector-backed corporate-action value rather than a blanket consumer, app, or franchise claim, keeping the official distributed display family off the corporate UI family, and retaining machine tokens and components only where the 2026-07-13 supplied evidence provides a current selector and surface, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. The Pinkfong Company describes itself as a global family-entertainment company working across music, stories, technology, video and audio content, live events, mobile apps, partnerships, and merchandise. Its official identity page groups the company CI with Pinkfong, Baby Shark, and Bebefinn BI assets, showing a portfolio structure rather than a single product surface. Public materials pair memorable music, stories, technology, character worlds, and experiences for children and families; the company page explicitly frames the work as content that connects people through joy. In 2025, the company released Pinkfong Baby Shark Font for Baby Shark's tenth anniversary. The release describes a 9-language family with 13,200 characters, fin-inspired triangular details, and a rhythmic visual idea based on the song; it offers the font through the identity page for everyday documents, fan art, and card news. The same official release describes its fin-inspired triangular form and rhythmic character, and says it is freely downloadable from the company identity page for documents, fan art, and card-news-style work. That is an official asset and brand-expression evolution, not evidence that every corporate or consumer control uses the font. Official history and the live corporate captures provide that narrative context; they do not by themselves supply interface tokens. Classifying that company-and-font narrative, including its closing sentence that the font release is not evidence that every corporate or consumer control uses the font, as brand context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not The Pinkfong Company-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

The source declares no task list of its own. Selecting these three as the product's primary tasks, each naming a control or surface the source records, is a derived editorial implementation inference from the verified surfaces; it is not The Pinkfong Company-authored or a separately published UI specification. They do not come from a persona section.

- Act on the public corporate CTA on corporate-home.
- Use the family-site pill repeated on corporate-home, corporate-company, and corporate-business.
- Act on the business outline action on corporate-company.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source states that official public materials identify **kids and families worldwide** as the audience for joyful content, and that these are public audience and stakeholder cues, not validated individual personas. No fictional personas are added. No name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. Dropping any individual persona rather than promoting one, carrying no name, age, city, motivation, or affiliation classification, and keeping **kids and families worldwide** in the source's wording, is a derived editorial implementation inference from the verified surfaces; it is not The Pinkfong Company-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings inside them, including the selector-backed-not-blanket reading of the measured pink action and the refusal to promote the official distributed display family to the corporate UI family, is a derived editorial implementation inference from the verified surfaces; it is not The Pinkfong Company-authored or a separately published UI specification.

- Public corporate pages use white `#ffffff`, black `#000000`, muted gray `#5a5a5a`, and a measured Pinkfong-pink `#ff66af` action
- `#ff66af` is a selector-backed corporate-action value, not a blanket claim for consumer, app, or franchise controls
- Spoqa Han Sans Neo is loaded on the corporate capture; the generic system stack is separately observed on many public controls
- Pinkfong Baby Shark Font is an official distributed brand asset and one loaded corporate-business display use; it is not promoted to the corporate UI family
- Only corporate-page defaults and their exact selector/surface provenance are documented. Product flows, consumer-site UI, and generic family-site components are omitted

### Principles

These 3 items are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification. The source states them in its own Principles section together with the UI implication it draws from each. The stems rest on official mission, identity-page CI/BI distribution, and the official font release; the *UI implication* attached to each is the source's reconstruction.

1. **Connect through joyful content.** The official mission explicitly centers joyful content and entertaining experiences. *UI implication:* corporate public copy should state the content or experience clearly before adding promotional flourish.
2. **Keep portfolio identities distinct.** The company page distributes separate CI/BI assets for the company, Pinkfong, Baby Shark, and Bebefinn. *UI implication:* do not collapse corporate identity assets into a single universal franchise UI palette.
3. **Treat type as an asset with a boundary.** The official font release gives purpose and permitted creative examples, while the corporate capture separately proves a loaded body family. *UI implication:* use a named font only with the evidence class and load/distribution condition that supports it.

### Application rules

The source states these five as its Do list, kept as written. The following three additional application rules are unique constraints from the source's Agent Prompt Guide, moved here rather than dropped. These rules, the reasons attached to them, and the decision to keep the Agent Prompt Guide's unique constraints beside the Do list, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

- Preserve the corporate/public source boundary when using the documented controls.
- Use `#ff66af` only where a Pinkfong corporate-action treatment is intended and evidenced.
- Load Spoqa Han Sans Neo before naming a corporate public-text treatment after it.
- Treat Pinkfong Baby Shark Font as a distinct display asset with its own official release and stated-use boundary.
- Keep the recorded selector and route with every component reuse claim.
- Load Spoqa Han Sans Neo only when the verified source can be loaded.
- Use Pinkfong Baby Shark Font as an official display asset only within its stated distribution boundary.
- Use the specific corporate CTA, family-site pill, or business outline action only with its recorded route and default-state limitation.

### Avoid

The source states these five as its Don't list, kept as written. The following additional prohibition is a unique constraint from the source's Agent Prompt Guide, moved here rather than dropped. These prohibitions, the reasons inside them, and the decision to keep the Agent Prompt Guide's unique constraint beside the Don't list, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

- Do not turn the public corporate CTA into a general consumer-product, checkout, or authenticated-app button.
- Do not infer interaction styling from collector state labels while `interactionCount` is zero.
- Do not call a system-stack heading a Pinkfong font.
- Do not promote the company’s BI downloads or font release into a public component library.
- Do not invent mobile, error, success, loading, or responsive variants from this desktop artifact.
- Do not generate a Pinkfong consumer site, child-facing app, purchase flow, status system, or generic character-card library from this reference.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Role names follow the source's own labels and token-set keys. Taking those role names from the source's own labels, pairing each hex to the token-set path named beside it, keeping `#08c7ff` inside the business-outline-action record rather than elevating it to a global color token, keeping family-site-pill `#8c8c8c` as that component's foreground rather than as `tokens.colors.muted`, and reading the identity-page BI downloads as asset availability rather than as a UI palette, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

- **Pink action** (`#ff66af`): observed on the public corporate-home primary CTA and on a business-page primary action. Token-set path `tokens.colors.primary`. Catalog `primary_color` is the same hex. `#ff66af` is a selector-backed corporate-action value, not a blanket claim for consumer, app, or franchise controls.
- **Canvas** (`#ffffff`): observed on the family-site pill, business-page outlined action, and mobile-menu dialog. Token-set path `tokens.colors.canvas`.
- **Corporate foreground** (`#000000`): recurrent public corporate text value and the mobile-menu dialog foreground. Token-set path `tokens.colors.foreground`.
- **Muted corporate text** (`#5a5a5a`): recurrent public corporate text value. Token-set path `tokens.colors.muted`.

The capture does not include Pinkfong consumer pages, an authenticated product, or an official color-specification document that maps these values to semantic states. #08c7ff occurs only as the selector-backed corporate-company outline-action foreground; it is retained inside that component record, not elevated to a global color token. The identity-page BI downloads establish asset availability, not a UI palette.

### Spacing

Token-set paths: `tokens.spacing.xs: 8`, `tokens.spacing.control: 20`, `tokens.spacing.action: 32`. YAML writes those steps unitless. Source §5 writes spacing clusters at 8px, 20px, and 32px. Both writings stay. The unitless YAML steps are not rewritten as a replacement `px`. `tokens.spacing.xs: 8` is a spacing step. It is not the shape step `tokens.rounded.compact: 8`. `tokens.spacing.action: 32` is a spacing step. It is not the shape step `tokens.rounded.pill: 32`, and it is not the corporate-primary-action radius `32` or the padding writing `32px 15px`. Keeping YAML `8` / `20` / `32` off prose `8px` / `20px` / `32px`, keeping `tokens.spacing.xs: 8` off `tokens.rounded.compact: 8`, keeping `tokens.spacing.action: 32` off `tokens.rounded.pill: 32` and off component radius and padding writings, and keeping measured control padding as control-local rather than inferring a generic card or page-spacing rule, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification. The step values themselves are the source's own.

### Shape

Token-set paths: `tokens.rounded.none: 0`, `tokens.rounded.compact: 8`, `tokens.rounded.pill: 32`, `tokens.rounded.emphasis: 40`. YAML writes those steps unitless. Harvested controls in source §4 record 32px radius on the corporate primary action, the family-site pill, and the business outline action. Both writings stay. YAML `0` / `8` / `32` / `40` stay on their own paths; they are not rewritten as a replacement `px`. `tokens.rounded.compact: 8` is a radius step. It is not the spacing step `tokens.spacing.xs: 8`. `tokens.rounded.pill: 32` is a radius step. It is not the spacing step `tokens.spacing.action: 32`. `tokens.rounded.emphasis: 40` is a radius step on the YAML scale, claimed on the corporate-business surface; it is not the harvested business-outline-action radius `32`. Keeping YAML `0` / `8` / `32` / `40` off prose `px`, keeping `compact: 8` off spacing `xs: 8`, keeping `pill: 32` off spacing `action: 32` and off the three harvested 32px radii, and keeping `emphasis: 40` off the harvested business-outline-action radius `32`, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

### Elevation

Most representative corporate controls and the dialog have `box-shadow: none`. Token-set path `tokens.shadow.none` is `none`. Two observed button treatments carry small shadows: the Pinkfong action uses `rgba(255, 5, 88, 0.06) 0px 2px 0px 0px` (token-set path `tokens.shadow.pink-lift`), while the family-site pill uses `rgba(0, 0, 0, 0.02) 0px 2px 0px 0px`. These are component-local observations, not an elevation scale. Keeping `tokens.shadow.pink-lift` on its token path and also on the corporate primary action, keeping the family-site-pill shadow as that component's local observation rather than as a second named shadow token, and keeping `tokens.shadow.none` off those two local shadows, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

### Motion

No duration, easing curve, reduced-motion rule, or captured animated state is available in the supplied artifact. Motion tokens are intentionally absent.

Naming the five evidence kinds below as the promotion gate, refusing a partial confirmation, and refusing a match against an official framework or vendor document as that gate, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification. An exact motion value may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official specification document — does not satisfy that condition.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, and the resolution in each cell, is a derived editorial implementation inference from the verified surfaces; it is not The Pinkfong Company-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | The official identity page and company/business pages describe the company, franchise BI downloads, and the font release. They do not publish a public product-component specification or a universal current typography token. |
| Live computed surface-use | The supplied artifact records **Spoqa Han Sans Neo** as `loaded` with high confidence on three corporate-route text elements and corroborates it with 15 Spoqa CDN source URLs. It is the verified corporate-public text family only. |
| Live computed display use | One corporate-business h1 computes to `BabyShark`, 48px/700/48px, with a loaded `pbs-light.otf` source on the company domain. The official release calls the distributed family **Pinkfong Baby Shark Font**; the naming difference is retained as a source boundary rather than silently normalized. |
| Official distributed asset | The company announced Pinkfong Baby Shark Font in 2025, describes its fin-inspired triangular form and rhythmic character, and says it is freely downloadable from the company identity page for documents, fan art, and card-news-style work. In 2025, the company released Pinkfong Baby Shark Font for Baby Shark's tenth anniversary. The release describes a 9-language family with 13,200 characters, fin-inspired triangular details, and a rhythmic visual idea based on the song; it offers the font through the identity page for everyday documents, fan art, and card news. This is a font-asset and stated-use fact, not a source for uncaptured UI metrics or components. |
| License boundary | The current official release describes free download and the listed creative uses but does not establish a broader webfont license in this reference. Separately, the loaded corporate Spoqa Han Sans Neo is distributed by Spoqa under the SIL Open Font License. |
| Declared/system families | `-apple-system`, `system-ui`, Segoe UI, Roboto, Helvetica Neue, Noto Sans, emoji families, and Arial appear in computed stacks. They remain system context; do not label a substituted system font as Spoqa Han Sans Neo or Pinkfong Baby Shark Font. |

### Family

- **Current visible corporate-public UI family:** `Spoqa Han Sans Neo`. Token-set path `tokens.typography.family.corporate-ui`. It is the verified corporate-public text family only. Load Spoqa Han Sans Neo before naming a corporate public-text treatment after it.
- **Official distributed display family:** `Pinkfong Baby Shark Font`. Token-set path `tokens.typography.family.brand-display`. One corporate-business h1 computes to loaded `BabyShark`; the official release calls the distributed family Pinkfong Baby Shark Font. The naming difference is retained as a source boundary rather than silently normalized. It is not promoted to the corporate UI family.
- **Observed control stack:** many public controls use a generic system stack. Do not call a system-stack heading a Pinkfong font. Do not label a substituted system font as Spoqa Han Sans Neo or Pinkfong Baby Shark Font.

Calling Spoqa Han Sans Neo the verified corporate-public text family only, keeping computed `BabyShark` unmerged from official Pinkfong Baby Shark Font, and refusing to present the system stack as either named family, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification. The family names, the loaded `pbs-light.otf` source, and the 15 Spoqa CDN source URLs are the source's own.

### Type roles

YAML writes numeric sizes and line heights without a `px` suffix. Source §3 writes the same roles with `px` computed line heights. Both writings stay. YAML lineHeight values stay as those numbers and are never converted to a replacement px. Pairing each YAML role to the token-set path named beside it, keeping YAML `use` strings verbatim in the Token-set use column, keeping the longer §3 source-boundary notes beside them, keeping YAML `16` / `1.5715` off prose `16px` / `25.144px`, keeping YAML `38` / `1.5715` off prose `38px` / `59.717px`, keeping YAML `48` / `1` off prose `48px`, and keeping corporate-body `16` off `tokens.spacing.xs` and off every other `16`, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

| Role | Family | Size | Weight | Line height | Token-set use | Source boundary |
|---|---|---|---:|---|---|---|
| Corporate body | Spoqa Han Sans Neo | YAML `16` / §3 `16px` | 400 | YAML `1.5715` / §3 `25.144px` | Observed corporate body text on the supplied public routes. | Loaded Spoqa Han Sans Neo text on supplied corporate routes |
| Corporate heading | computed system stack, not a Pinkfong font token | YAML `38` / §3 `38px` | 700 | YAML `1.5715` / §3 `59.717px` | Observed corporate-company h1 scale; its computed family is the system stack, not a Pinkfong font token. | Corporate-company h1; computed system stack, not a brand-font token |
| Brand display | computed `BabyShark` / official Pinkfong Baby Shark Font | YAML `48` / §3 `48px` | 700 | YAML `1` / §3 `48px` | One corporate-business h1 uses loaded BabyShark; official release calls the distributed family Pinkfong Baby Shark Font. | One corporate-business h1 computes to loaded `BabyShark`; official distribution is Pinkfong Baby Shark Font |

Token-set paths: `tokens.typography.corporate-body.size` · `tokens.typography.corporate-body.weight` · `tokens.typography.corporate-body.lineHeight` · `tokens.typography.corporate-body.use` · `tokens.typography.corporate-heading.size` · `tokens.typography.corporate-heading.weight` · `tokens.typography.corporate-heading.lineHeight` · `tokens.typography.corporate-heading.use` · `tokens.typography.brand-display.size` · `tokens.typography.brand-display.weight` · `tokens.typography.brand-display.lineHeight` · `tokens.typography.brand-display.use`.

### Assets

- Catalog favicon: `https://www.google.com/s2/favicons?domain=pinkfong.com&sz=128`. Frontmatter records `logo.type: favicon`. That URL is a third-party favicon-proxy pointer, not a Pinkfong-hosted brand file.
- Identity-page CI and BI downloads for The Pinkfong Company, Pinkfong, Baby Shark, and Bebefinn. The identity-page BI downloads establish asset availability, not a UI palette. Do not promote the company’s BI downloads or font release into a public component library.
- Official Pinkfong Baby Shark Font release: `https://www.thepinkfongcompany.com/news/pr/207`. Loaded display source on the company domain: `pbs-light.otf`.
- Spoqa Han Sans Neo source and license: `https://github.com/spoqa/spoqa-han-sans` (SIL Open Font License). This describes the font asset, not a Pinkfong brand asset.

Reading the Google s2 favicon URL as a catalog identity pointer rather than a Pinkfong-hosted brand file, and reading the CI/BI downloads as identity assets rather than as a UI palette or public component library, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The supplied artifact contains default corporate controls, a hidden mobile-menu dialog structure, one disabled menu-label sample, and collector-labelled focus/hover/pressed entries with `interactionCount: 0`. It does not provide opened-dialog, empty, loading, error, success, toast, form-validation, or product-state evidence. Those specifications are intentionally absent. YAML component `states` for the corporate primary action and the family-site pill: “Observed default only; interactionCount is 0.” YAML component `states` for the business outline action: “Collector labels focus, hover, and pressed on `surface-2::[data-omd-capture=\"23\"]`; interactionCount is 0, so no state value is specified.” Collector labels are not `focus-visible` treatment values. No public consumer-product button, form, card, badge, navigation state, toast, authenticated flow, or general component variant had the required selector plus surface provenance in this update.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted where this packet holds no value. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow each control's product role rather than its primitive kind. This is not a complete state-coverage claim.

The mobile-menu dialog has recorded structure and no opened-dialog interactive-kind evidence, so kind and a state-applicability map are omitted for it rather than assumed. A `Primitive type` line is attached only when the source YAML records that type on that component. YAML records `type: button` on `tokens.components.corporate-primary-action`, `tokens.components.family-site-pill`, and `tokens.components.business-outline-action`; `type: dialog` on `tokens.components.mobile-menu-dialog`.

The preceding applicability note, the collector-label versus `focus-visible` evidence-class split, every interactive-kind verdict, every applicability verdict, the reason given for either, the omit-kind decision for the mobile-menu dialog, the refusal to attach a YAML primitive type that the token set does not record, and the refusal to treat this as a complete state-coverage claim are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

### Corporate primary action

- Role: one public corporate CTA on corporate-home
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#ff66af`
- Text: `#ffffff`
- Radius: 32px
- Padding: 32px 15px
- Font: 24px / 400 / system stack
- Shadow: rgba(255, 5, 88, 0.06) 0px 2px 0px 0px
- Token-set path: `tokens.components.corporate-primary-action`
- Token-set field paths: `tokens.components.corporate-primary-action.type` · `tokens.components.corporate-primary-action.bg` · `tokens.components.corporate-primary-action.fg` · `tokens.components.corporate-primary-action.radius` · `tokens.components.corporate-primary-action.padding` · `tokens.components.corporate-primary-action.font` · `tokens.components.corporate-primary-action.states` · `tokens.components.corporate-primary-action.use`
- Token-set use: corporate-home `home::[data-omd-capture="26"]`; one public corporate CTA, 64px rendered height.
- Token-set states: Observed default only; interactionCount is 0.
- Use: `corporate-home` / `home::[data-omd-capture="26"]`; one public corporate CTA with 64px rendered height.
- Observed: default only; interactionCount is 0.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on corporate-home |
| hover | applicable | Pointer-web public corporate CTA; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Button control; visual treatment omitted |
| loading | not-applicable | Public corporate CTA; destination control, commits no in-place operation |
| error | not-applicable | Public corporate CTA; destination control, commits no in-place operation |
| success | not-applicable | Public corporate CTA; destination control, commits no in-place operation |

### Family-site pill

- Role: family-site pill repeated on the three corporate routes
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#ffffff`
- Text: `#8c8c8c`
- Radius: 32px
- Padding: 4px 20px
- Font: 14px / 400 / system stack
- Shadow: rgba(0, 0, 0, 0.02) 0px 2px 0px 0px
- Token-set path: `tokens.components.family-site-pill`
- Token-set field paths: `tokens.components.family-site-pill.type` · `tokens.components.family-site-pill.bg` · `tokens.components.family-site-pill.fg` · `tokens.components.family-site-pill.radius` · `tokens.components.family-site-pill.padding` · `tokens.components.family-site-pill.font` · `tokens.components.family-site-pill.states` · `tokens.components.family-site-pill.use`
- Token-set use: `home::[data-omd-capture="31"]`, repeated across the three corporate routes; 35px rendered height.
- Token-set states: Observed default only; interactionCount is 0.
- Use: `home::[data-omd-capture="31"]`, repeated on corporate-home, corporate-company, and corporate-business; 35px rendered height.
- Observed: default only; interactionCount is 0.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on corporate-home, corporate-company, and corporate-business |
| hover | applicable | Pointer-web family-site pill; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Button control; visual treatment omitted |
| loading | not-applicable | Family-site destination switcher; commits no in-place operation |
| error | not-applicable | Family-site destination switcher; commits no in-place operation |
| success | not-applicable | Family-site destination switcher; commits no in-place operation |

### Business outline action

- Role: one public corporate-company / business-page action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#ffffff`
- Text: `#08c7ff`
- Radius: 32px
- Padding: 30px 15px
- Font: 18px / 400 / system stack
- Token-set path: `tokens.components.business-outline-action`
- Token-set field paths: `tokens.components.business-outline-action.type` · `tokens.components.business-outline-action.bg` · `tokens.components.business-outline-action.fg` · `tokens.components.business-outline-action.radius` · `tokens.components.business-outline-action.padding` · `tokens.components.business-outline-action.font` · `tokens.components.business-outline-action.states` · `tokens.components.business-outline-action.use`
- Token-set use: corporate-company `surface-2::[data-omd-capture="23"]`; one public business-page action, 60px rendered height.
- Token-set states: Collector labels focus, hover, and pressed on `surface-2::[data-omd-capture="23"]`; interactionCount is 0, so no state value is specified.
- Use: One public corporate-company action with 60px rendered height.
- Observed: Collector labels focus, hover, and pressed on `corporate-company` / `surface-2::[data-omd-capture="23"]`; interactionCount is 0, so no state value is specified. Those collector labels are not `focus-visible` treatment values.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on corporate-company |
| hover | applicable | Pointer-web public business-page action; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Button control; visual treatment omitted |
| loading | not-applicable | Public business-page action; destination control, commits no in-place operation |
| error | not-applicable | Public business-page action; destination control, commits no in-place operation |
| success | not-applicable | Public business-page action; destination control, commits no in-place operation |

### Mobile menu dialog

- Role: hidden mobile-menu dialog structure captured on all three corporate routes
- Primitive type: `dialog`
- Kind and state-applicability map omitted: captured closed structure; no opened dialog interaction was captured
- Background: `#ffffff`
- Text: `#000000`
- Padding: 30px
- Font: 14px / 400 / system stack
- Shadow: `box-shadow: none` among representative corporate controls and the dialog
- Token-set path: `tokens.components.mobile-menu-dialog`
- Token-set field paths: `tokens.components.mobile-menu-dialog.type` · `tokens.components.mobile-menu-dialog.bg` · `tokens.components.mobile-menu-dialog.fg` · `tokens.components.mobile-menu-dialog.padding` · `tokens.components.mobile-menu-dialog.font` · `tokens.components.mobile-menu-dialog.use`
- Token-set use: `home::div.DefaultMenu_mobile-menu-modal__SEaJA`; hidden mobile-menu dialog structure captured on all three corporate routes.
- Use: `home::div.DefaultMenu_mobile-menu-modal__SEaJA`, a hidden dialog structure captured on all three corporate routes; no opened dialog interaction was captured.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The supplied evidence is a single desktop corporate capture. It records spacing clusters at 8px, 20px, and 32px, but does not establish a reusable product grid, consumer-content layout, or mobile breakpoint system. The measured controls above preserve their own padding; no generic card or page-spacing rule is inferred.

Only a 1440×900 collector viewport was supplied. The artifact includes a hidden mobile-menu dialog structure, but no open-dialog or mobile viewport observation. Breakpoints, touch-target rules, safe-area behavior, and responsive layout changes are intentionally absent.

Reading that packet as a desktop corporate capture rather than as a cross-viewport layout specification, keeping the 1440×900 figure as the collector viewport the source supplied rather than as a layout token, and keeping measured control padding as control-local rather than inferring a generic card or page-spacing rule, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The official company description ties vivid characters, memorable music, and easy-to-follow movement to family-entertainment content IP. Its English company page says the mission is to connect people through joyful content and entertaining experiences, while the business page presents content, partnership, merchandise, live events, and mobile apps as separate offerings. [Company](https://www.thepinkfongcompany.com/en/company) · [Business](https://www.thepinkfongcompany.com/en/business)

The following voice reading, the restatement of the Principles UI implication as the same implication rather than as a new microcopy sample, and keeping catalog identity name **더핑크퐁컴퍼니** beside The Pinkfong Company rather than as a replacement, are a derived editorial implementation inference from the verified surfaces; they are not The Pinkfong Company-authored or a separately published UI specification. This supports an optimistic, clear corporate voice that speaks about family enjoyment and content capability without treating all corporate copy as child-facing UI guidance. Corporate public copy should state the content or experience clearly before adding promotional flourish. No synthetic voice samples are promoted. Catalog identity name **더핑크퐁컴퍼니** stays beside The Pinkfong Company.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Listing them as unnamed values already named by the source, rather than as coverage of domains the source never named, is a derived editorial implementation inference from the verified surfaces; it is not The Pinkfong Company-authored or a separately published UI specification.

- opened-dialog, empty, loading, error, success, toast, form-validation, and product-state visual treatments
- duration, easing curve, reduced-motion rule, and captured animated state
- reusable product grid, consumer-content layout, and mobile breakpoint system
- breakpoints, touch-target rules, safe-area behavior, and responsive layout changes
- official color-specification document that maps captured values to semantic states
- public consumer-product button, form, card, badge, navigation state, toast, authenticated flow, and general component variant with selector plus surface provenance
