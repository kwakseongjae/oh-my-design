T2 마이그레이션을 시작하기 전에 세션 상태를 복원하고, 지정된 규격·규칙집·골든 샘플을 순서대로 읽겠습니다.지정 문서를 순서대로 읽고, 승인된 musinsa·notion 산출물과 규칙집을 대조한 뒤 Bilibili 원문을 옮기겠습니다.Core v2 §4.4와 musinsa·notion 개정본을 읽고, 이미 있는 `migrated/bilibili` 상태를 확인합니다.Bilibili 원문의 상태·폰트 경계와 게이트 조건을 더 대조한 뒤, notion 개정본의 컴포넌트 표를 기준으로 이관 초안을 잡겠습니다.원본에서 게이트가 세는 hex·px 값을 전부 뽑아, 손실·발명이 없도록 이관 본문을 맞추겠습니다.원문 값과 §4.4 역할 판정을 유지한 채로 세 산출물을 작성합니다.기계 게이트와 Portable Core 검사를 돌리고, 막히는 항목이 있으면 바로 고치겠습니다.게이트가 잡은 세 가지를 확인합니다. `focus-visible`과 hex가 같은 줄에 있고, scope 부정 패턴과 placeholder 셀도 의심됩니다.같은 웨이브의 apple·toss 상태 표를 보고, 검색 필드와 roll 버튼의 역할 판정이 어긋나지 않는지 맞춥니다.검색 필드는 웨이브의 C2 판정(쿼리/내비 컨트롤은 loading·error·success가 역할이 아님)에 맞추고, roll 버튼은 카드 갱신 대기만 applicable로 남깁니다.`web/references/bilibili/DESIGN.md`를 Core v2 Portable Core로 옮겼습니다. 규칙집 v2. 산출은 `docs/design-md-weight/migrated/bilibili/`입니다.

**지킨 것**
- MUST 7 claim, 검증값 손실 0 / 발명 0, 새 `[FILL IN]` 0
- `#FB7299`는 catalog metadata로만 provenance에 둠. 제품 색은 `#f1f2f3` / `#ffffff` / `#18191c` / `#9499a0` / `#61666d` / `#e3e5e7`
- `#61666d`와 `#e3e5e7`을 일반 ink/hairline으로 합치지 않음
- 검색 focused/pressed 색은 colour-aggregate 관측으로 남기고 `focus-visible` treatment로 올리지 않음
- Cover / stats / skeleton은 kind·applicability map 생략
- Home search loading/error/success, feed roll error/success는 역할 의미로 `not-applicable`. Feed roll loading은 카드 셋 대기로 applicable
- §14 Loading·Skeleton 값 보존. Empty/Error/Success/Disabled는 값만 생략
- HarmonyOS는 declared-only. `-apple-system`을 브랜드 페이스로 쓰지 않음
- §9 도구 프롬프트 삭제. §13은 원문의 stakeholder groups만 Audience로. sidecar 없음

**검증**
- `inspectDesignMd` `portable_core: true`, placeholders 0
- `migrate-reference.mjs --brand bilibili --gate-only` PASS

카탈로그 채택은 하지 않았습니다.

DONE migrated=1

## Revision 2026-08-23 (wave1 sol resubmit)

규칙집 v3. FAIL `docs/reviews/t2-1-wave1-2026-08-23-sol-sample.md`.

- B2/B2a: 세 UI implication 인접 본문에 공식 원칙 아님 · 공식 서사+관측 표면에서 파생한 편집적 해석 · 별도 product-system 규격 밖.
- E2b: §8/§14/§15 로그에 실질 제약 → portable / 원문 placeholder wrapper → provenance omission ledger. 값 미채움.

inspectDesignMd portable_core true, placeholders 0. --gate-only PASS.

DONE revised=1
