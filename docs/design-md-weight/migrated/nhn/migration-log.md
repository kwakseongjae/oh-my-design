# nhn migration log

Source: `web/references/nhn/DESIGN.md` (unmodified)
Sibling read (not the migration input): `web/references/nhn/.verification.md`
Destination: `docs/design-md-weight/migrated/nhn/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/nhn/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep -oF` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `str.count` / `grep -oF … | wc -l` per file, never a remembered count.

Source SHA-256 `dda8a946b1a92a65a5828eedbf0e53569c3c23bdef456ff54dacfcc577e1b4be` (`web/references/nhn/DESIGN.md`). Sibling SHA-256 `fd3e8fe6440566f0f42651536f68ea27e9bc1dfbf172dcd3c6c98302cf780500` (`web/references/nhn/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# NHN Design System` (`DESIGN.md` 1). Identity table `provenance.md` 7–23. YAML homepage `https://www.nhn.com/` is dual: DESIGN dest 8 (prefix matches include `/en-US/` Brand Resource and IR/services paths) + provenance dest 10 (E2a). Frontmatter `primary_color` `#212126` is dual: DESIGN dest 10 + provenance dest 8 (E2a). Exact `logo.type: favicon` DESIGN dest 1 at 159 / provenance dest 1 at 27. `s2/favicons` DESIGN dest 1 at 159 / provenance dest 1 at 16 (E2a). `display_name_kr` as a field name: DESIGN dest 0 / provenance dest 1 at 11. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: reconciled`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): identity rows `provenance.md` 18–23, freshness table 33–40. The source footer's **Verified:** 2026-07-13 is at `provenance.md` 42. The token note is at `provenance.md` 29. `tokens.source: reconciled` DESIGN dest 0 / provenance dest 3 at 29/79/198 (E2a: ledger only). `components_harvested` DESIGN dest 0 / provenance dest 2 at 23/197. No `ds.name` / `ds.url` / `ds.type` fields exist in the source YAML. |
| YAML `tokens.colors` (9 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 76–87. All nine roles kept with names, values, and token-set keys: primary `#212126`, foreground `#36363d`, muted `#57575b`, subtle `#62626a`, hint `#aaaaae`, surface `#f8f8f8`, canvas `#ffffff`, on-primary `#ffffff` (second key), hairline `#e5e7eb`. Source heading **Canvas / On Ink** restored at 82 beside the two keys (wave 37 keep-both). Characterizing phrases covered by the adjacent qualifier at 74 (B2/B2a). |
| YAML `tokens.typography.family.ui` (`Pretendard Variable`) / `display-kr` (`Main Pretendard Variable`) | 옮김 → Typography & Assets Family | Family `DESIGN.md` 136–139. `tokens.typography.family.ui` dest 2. `tokens.typography.family.display-kr` dest 2. |
| YAML `tokens.typography.body` / `nav` / `label` / `title` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 149–153. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.50` dest 3; `1.75` dest 3; `1.57` dest 3. Form-keeping decision carries an adjacent complete qualifier at 145 (B2/B2a), which also names YAML `use` / §3 evidence-boundary keep-both. All four YAML `use` strings restored verbatim in Token-set use (A1, A3, wave 37): `Observed corporate body/list text` / `Observed primary navigation action` / `Observed secondary action and label` / `Observed Korean heading`. Source §3 notes that are longer sit beside them: Repeated across all captured surfaces; Transparent action-label control; Services controls/labels; Captured `h3` role. Large Korean display 32px / 800 / 48px is a §3 row, not a YAML typography key (`DESIGN.md` 153). `tokens.typography.body.size` `16` is disambiguated from a spacing step at 145/155. Display `32` is disambiguated from `tokens.spacing.xl: 32` at 95/155. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (2 steps) / `tokens.shadow.none` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 91 (`xs: 4 · sm: 7 · md: 8 · base: 10 · lg: 22 · xl: 32 · xxl: 80`), with key-path forms `tokens.spacing.xs: 4` dest 2 / `sm: 7` dest 2 / `md: 8` dest 2 / `base: 10` dest 1 / `lg: 22` dest 2 / `xl: 32` dest 2 / `xxl: 80` dest 2. Shape at 99 (`none: 0 · pill: 50`). `tokens.rounded.none: 0` dest 1. `tokens.rounded.pill: 50` dest 3. `tokens.spacing.xs: 4` is disambiguated from pill padding `4px` at 95. `tokens.spacing.sm: 7` is disambiguated from `7.008px` at 95. `tokens.spacing.md: 8` is disambiguated from pill padding `8px` at 95. `tokens.spacing.xxl: 80` is disambiguated from list padding `80px` at 95. `tokens.shadow.none` at `DESIGN.md` 108. |
| YAML `tokens.components` (1 record) | 옮김 → Components & States | Previous control `DESIGN.md` 259–284. Verified primitive type preserved, not flattened to `Kind` (A1b): `Primitive type: \`button\`` dest 1 at 261 = YAML `previous-control` only. YAML `use` restored as `Token-set use:` with key path `tokens.components.previous-control.use` dest 1 at 272. YAML `states: "disabled opacity 0.4"` at 271 beside §14 `opacity: 0.4`. YAML font `16px / 400 Pretendard Variable` DESIGN dest 2 at 270/273 + provenance dest 2 at 180/236 beside §4 `16px / 400 / Pretendard Variable` DESIGN dest 4 at 178/200/265/273 + provenance dest 1 at 236. Both stay: Font: is the source §4 slash form; Token-set font is the YAML short form. No §4-only component borrowed a `Primitive type`. Primary list item, Secondary list item, Transparent action label, Secondary label action, and Services pill are labelled `not in the token set` (dest 7 at 168×2/173/195/217/239/288). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–38. Atmosphere readings (restrained and nearly monochrome; principal ink; corporate-web observations rather than a claim about separate customer products or documentation interfaces) carry an adjacent complete qualifier at 11 (B2/B2a). |
| §1 공식 히스토리·CI URL | 분리 → provenance; URL 옮김 → Experience Scope | 서사 출처. 본문은 토큰이 아니라고 밝힌 경계를 Experience에 유지 (`DESIGN.md` 9/13). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 76–87. Official CI achromatic sentence at 85. Two-key `#ffffff` unmerge at 82/87. |
| §3 Typography Rules — evidence classes, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 125–132; family 136–141; hierarchy table 149–153. Official-product-use and Font-evidence wrap carry adjacent complete qualifiers at 122 and 126 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | Primary list item 171, Secondary list item 193, Transparent action label 215, Secondary label action 237, Previous control 259, Services pill 286. Capture selectors kept on the role lines. Previous `Font:` is source §4 `16px / 400 / Pretendard Variable` DESIGN dest 4 at 178/200/265/273 (Primary, Secondary, Previous Font, keep-both qualifier); YAML short `16px / 400 Pretendard Variable` DESIGN dest 2 at 270/273. Footer **Verified** / Tier 1 / Tier 2 / Conflicts at legacy 204–207 are provenance (next row). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 33–40; Tier 1 list 63–68; Tier 2 72–73. Conflicts unresolved: none — `provenance.md` 44. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing | `DESIGN.md` 311: flat corporate information layout, `#f8f8f8` surface moments, white canvas, spacing clusters 4/7/8/10/22/32/80px as observations rather than a complete spacing scale, no product-app layout or documentation layout captured. The flat-layout reading, the observations-not-complete-scale reading, and the 1440×900-as-viewport reading carry an adjacent qualifier at 315 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 108: `box-shadow: none`, text hierarchy, white/neutral surfaces, `#e5e7eb` hairline, no elevation ramp inferred. The flat-treatment reading carries an adjacent qualifier at 108 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 52–56 (five Do rules), under the grouping qualifier at 50. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 62–66, under the qualifier at 60. The four prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 313: collector used only a 1440×900 viewport; responsive breakpoints, mobile navigation, and touch-target behaviour unresolved and intentionally omitted. `1440×900` is dual: DESIGN dest 2 at 313/315 + provenance dest 3 at 131/149/238 (E2a). `measures 1440px` is not written (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing restatement; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion. The §9-only sentence that this reference is for an NHN-like **corporate information surface** and is not a substitute for an NHN affiliate product, authenticated app, or documentation system lands on Avoid `DESIGN.md` 66 (A2, A3). The check is itemised at `provenance.md` 192. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 320: official slogan “Weaving New Play,” shift from simple connection to multidirectional weaving, explanatory and composed rather than product-marketing superlatives. Curly quotes restored as source bytes (A5). The qualifier at 320 covers the voice reading (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: Hangame Communication, Hangame online-game portal, modern global IT group working across multiple business areas, 2023 public introduction of “Weaving New Play”, 2024 CI, subsequent folded-paper identity, 27-degree fold, achromatic colour decision. Each source paragraph's last sentence is kept as one unit (wave-31 hogangnono). Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §12 Principles — 3 numbered | 옮김 → Experience principles | `DESIGN.md` 44–46 under the B2a form at 42: "These 3 items are a derived editorial implementation inference from the verified surfaces; they are not NHN-authored or a separately published UI specification." No separately published NHN UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 211–240 (30 data rows). |
| §13 Personas — unfinished `[FILL IN]` slot | 삭제 | The source records that NHN has not supplied first-party audience-segment or persona documentation and instructs not to fabricate named user personas from the corporate, services, or IR surfaces. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, motivation, or affiliation classification appears in either output (D2, D2a). Disposition at `provenance.md` 188 names field kinds only. Audience at `DESIGN.md` 28 carries no invented group. The three `primary-tasks` at 21–23 are source noun phrases (`the main site` / `a services listing` / `an investor-relations financial page`) plus the captured URLs; 19 says so and qualifies the step from surface to "primary task". `[FILL IN]` DESIGN dest 0. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Source state contract preserved at `DESIGN.md` 166 (A2; the catalog graph is still 0/440, so nothing is delegated): disabled previous-navigation control, transparent background, `#212126` text, `opacity: 0.4`; hover, focus, pressed, loading, error, empty, and success unresolved. The applicability rule is at 168, whose qualifier covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 6 interactive controls × 7 states. Previous control is YAML `type: button` but its role is a previous-navigation arrow, so loading/error/success close with a role reason (C2 v10) — `loading \| not-applicable` dest 6 at 188/210/232/254/281/304. `loading \| applicable` dest 0. Disabled captured on Previous control at 280. |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 112–114: no durations, easing curves, or interaction transitions captured; raw class names not sufficient evidence; leave motion unresolved; no motion token promoted. No cubic-bezier values exist in the source, so none were deleted. |
| §15 Motion — promotion gate | 옮김 → Foundations motion | B3 is held: the promotion condition at `DESIGN.md` 114 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The five-kind gate itself carries an adjacent complete B2a close on the same line. |
| Opening product / atmosphere sentence (legacy 106) | 옮김 → Experience Scope | `DESIGN.md` 9–11: Korean IT group; Hangame-era; games, payments and advertising, technology, commerce, and content; three public corporate surfaces. |

## Sibling handling (`web/references/nhn/.verification.md`)

The sibling exists — confirmed with `find web/references/nhn -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 119–148 and is **not** promoted into `DESIGN.md` as a new fact.
- Forms it carries that the visible source body does not: coverage score `66`; `17 component variants`; Playwright / `document.fonts`; class fragments `text-grayscale-2` / `mainLink` / `pr-80` / `py-7` / `opacity-40`; rgb() samples; sibling capture of action label `45` as `surface-2::`; `12` NHN-hosted Next font assets; Pretendard OFL URL; Poppins publisher repository; older hairline `#dbdbdb`; NHN Sans `three weights`; padding shorthand `0 80px 0 0` / `4 8 4 14`; sibling slogan spelling `Weaving New Play.`; `artifacts/reference-evidence/nhn.json`; `interactionCount` / `interactionKinds`. Recorded at `provenance.md` 137–147.
- Measured `DESIGN.md` 0 for those sibling-only strings: `66` as coverage score is ledger-only; `#dbdbdb` DESIGN dest 0; `opacity-40` DESIGN dest 0; `Weaving New Play.` with the sibling period DESIGN dest 0 (source curly forms without that period are in DESIGN); `0 80px 0 0` DESIGN dest 0 (source writes `0px 80px 0px 0px`).
- Hex values those RGB samples convert to (`#212126`, `#36363d`, `#57575b`, `#62626a`, `#f8f8f8`, `#e5e7eb`) are already in the source body and remain there.
- Viewport `1440×900` is in the source §8 body and is dual: DESIGN dest 2 at 313/315 + provenance dest 3 at 131/149/238 (E2a).

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All CJK runs in the source, any length | 0 distinct | 0 | 0 | Source body has no CJK runs. |
| Published Latin / English strings from the source body | 22 | 0 | 0 | See sub-needles below. Curly-quoted slogan forms restored as source bytes. |
| Sibling-only published fragments | 2 high (`0 80px 0 0`, `Weaving New Play.`) | 0 from DESIGN (intentionally) | 0 promoted | Kept as sibling corroboration at `provenance.md` 139–140. |

Sub-needle labels confirmed present individually in `DESIGN.md`: Weaving New Play; Hangame Communication; Hangame online-game portal; NHN Sans; Pretendard Variable; Main Pretendard Variable; Connection becomes weaving; Achromatic identity leaves room for variety; CI and typography are controlled brand assets; corporate information surface; Canvas / On Ink; Observed corporate body/list text; Observed primary navigation action; Observed secondary action and label; Observed Korean heading; Observed previous-navigation control on the services surface.

A5 분모: CJK hand sweep 0 extracted / 0 missing; published Latin hand sweep 22 extracted / 0 missing. `node test-v2/tools/latin-copy-audit.mjs --brand nhn` reported `scanned: 1`, `clean: 1`, `withLoss: 0`, `totalLost: 0`. First audit run lost 2 high sibling-only strings; both restored to provenance, not promoted.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand nhn --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 106, detail: "바늘 0개 — 이 브랜드에서 A5는 기계 검사되지 않았다. 발행 라틴 문자열을 손으로 전수 대조하라." }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 106.

## Deviations recorded

- `DESIGN.md` is 5,521 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: nine color keys, four type roles plus the §3 Large Korean display row, six declared component records, a seven-state applicability matrix on six interactive controls, canvas/on-primary unmerged, the full §11 founding-and-rebrand narrative including each paragraph's last sentence, YAML-font / §4-Font keep-both on Previous control, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Unfinished §13 persona slot omitted (D2, D2a). Audience invents no group.
- Sibling-only measurements (`66`, `#dbdbdb`, `opacity-40`, `0 80px 0 0`, sibling `Weaving New Play.`) not promoted (B1).
- No invented out-of-scope domain (`native application` / `mobile app` / `back-office` / `product application` / `storefront` DESIGN dest 0). No defensive `measures 1440px` negation (D1). Desktop keeps the source collector viewport `1440×900` at 313/315.
- `Primitive type` attached only to the YAML component that holds that type (`button` dest 1 at 261). Five §4-only controls are `not in the token set`.
- No separately published NHN UI specification is named in the source, so every derived-editorial close uses the toss-form `not NHN-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Measure: `derived editorial implementation inference` DESIGN = `not NHN-authored` DESIGN = 30. Provenance derived ledger 30 rows at 211–240 (E1 1:1). Spacing/Layout/Transparent-action/Previous-control-font keep-both readings are named on the adjacent qualifier, not as extra rows.
- Portable DESIGN SHA-256 `e451df4189853113674afe361cb72e6f619141dae0f4f40727bb4032ac33826a` after Previous-control YAML-font / §4-Font keep-both. Auditor SHA recorded in `audit-log.md`.

## Unique-phrase contrast (wave 43)

Extracted 83 source-unique expressions (years, proper names, slogan, YAML `use` strings, constraints, key-path forms). First-draft DESIGN missing 4 (`Canvas / On Ink`; `corporate information surface`; curly-quoted `“Weaving New Play”` / `“Weaving New Play,”`; `NHN’s` / `company’s` apostrophes). All four restored. Residual 0.

## Mandatory final passes

- **F1 B2a scan.** Re-read the finished `DESIGN.md` from the title through Named gaps. Every causal, interpretive, or judging sentence — Scope surface-boundary, atmosphere, and narrative classification, Primary-tasks selection, Audience slot-drop, Distinctive grouping, Principles and their UI implications, Application rules / Avoid reasons, Semantic-color key splits including canvas/on-primary, Spacing YAML-vs-§5 keep-both and unmerges, Shape unmerges, Elevation flat reading, Motion absence, Motion B3 five-kind gate, Font-evidence classes, Family fallback, Type-roles keep-both, Assets slug classification, Components how-to-read, per-component geometry readings including Transparent-action YAML-nav-use / §3 keep-both and Previous-control YAML-font / §4-Font keep-both, Layout flat-layout and observations reading, Content voice, Named-gaps catalog reading — has an adjacent complete qualifier: derived editorial implementation inference / not NHN-authored or a separately published UI specification. Count 30 = provenance inventory 30.
- **F2 E2 대조.** Each log row above was written only after `grep -o` / `str.count` confirmed the named values on the cited lines in `DESIGN.md` and/or `provenance.md`. Dual destinations list both. Compliance claims (B3 at 114; B2a form at 42; §14 body at 166) name the lines that actually hold that full text. Homepage dest 8/10, `#212126` dest 10/8, exact `tokens.source: reconciled` dest 0/3 at 29/79/198, `1.50`/`1.75`/`1.57` dest 3/3/3, freshness 33–40, Verified 42, Conflicts 44, `not in the token set` dest 7, `tokens.components.previous-control.use` dest 1 at 272, YAML font `16px / 400 Pretendard Variable` DESIGN dest 2 at 270/273 / provenance dest 2 at 180/236, §4 `16px / 400 / Pretendard Variable` DESIGN dest 4 at 178/200/265/273 / provenance dest 1 at 236, `1440×900` DESIGN dest 2 / provenance dest 3.
