# ABEMA migration log

Source: `web/references/abema/DESIGN.md`
Destination: `docs/design-md-weight/migrated/abema/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/abema/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: post-F3 re-run. `scripts/design-md-core.cjs` inspectDesignMd `portable_core: true`, placeholders 0. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/abema/DESIGN.md --check --require-portable-core --json` → exit 0, `structurally_valid: true`, `portable_core: true`, reasons [], SHA-256 `b8d958a518c4454217131bbd20e37e4868913e2b13f232140ea173627d75dc9d`. Foundations avoids the Core-checker negation pattern `do not … constraint` and does not weaken the complete B2a limiter. `node test-v2/tools/migrate-reference.mjs --brand abema --gate-only` → PASS, problems []. Prior stale `portable_core: true` vs `missing-actionable-foundations-or-known-constraints` is closed by this command/result.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage URL 옮김 → Experience `scope`; `primary_color` 옮김 → Foundations ABEMA Yellow; logo type/slug 경계 옮김 → Typography & Assets + Avoid | Portable file has no frontmatter. Name kept as H1 `ABEMA Design System`. Homepage `https://abema.tv` is dual-destination: Experience Scope + Primary tasks + provenance identity/surfaces/Tier 1 (E2a). Catalog `primary_color` `#ddaa00` is dual identity metadata + portable Foundations ABEMA Yellow (E2a). Catalog logo type `favicon` / Google s2 slug is dual: provenance Identity holds type/slug; portable Assets and Avoid state the identity-only / not-a-mark-file boundary as a catalog/identity evidence class (not a source Don’t); no first-party mark file is attached (E2a). |
| YAML `omd`, `verified`, `added`, token claims, `tokens.source` / `extracted` / `note`, `components_harvested` | 분리 → provenance; `tokens.source: live-extract`와 token note(outage / Wayback) 옮김 → Experience `scope` | 출처 원장·freshness·Proof. `tokens.source: live-extract` is metadata (A1c) and is restated in portable Scope as the extraction class (E2a dual). Token note (outage-day inspect; CSS recovered from brand bundle Wayback 2025-12-31) is dual: Scope + provenance (E2a). `components_harvested: true` is provenance-only (A1c). No `ds.type` on source; none invented. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. Outage-day live-measured claims only (`#000000` canvas, `rgba(23,23,23,.8)` chrome, `#ddaa00` links, 4px radius, CopyRight stack) also map to `abema-live`. `primary-hover` / `accent` / `accent-hover` remain `abema-css` only (E1). `#ddaa00` action yellow와 `#f0163a` LIVE/danger를 합치지 않음. `--color-primary-hover` `#dfb015`와 `--font-color-link-hover` `#c5c5c5`를 합치지 않음 (A4). Crimson hover `#f34461`와 legacy `#bb122e`를 합치지 않음. YAML `on-primary` `#212121`는 yellow-button label이지 Foreground `#e6e6e6`가 아님 (A4). YAML `rounded.full` 9999와 body circle 50%를 합치지 않음. YAML unitless `lineHeight` **1.5** (heading / body-lg / body / caption)는 비율로 보존 (A1a). YAML Title L/M/S / Micro는 size/weight only — body-table 1.3 / 1.5를 YAML에 역주입하지 않음. 검증된 primitive type은 컴포넌트별로 보존: button ×4 (Primary, Secondary, Dark, Danger), badge (LIVE), input, card (Floating Panel), tab (Panel Tab), toggle (Checkbox), toast (Notification Block). `Kind: interactive`로 뭉개지 않음 (A1b). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(`https://abema.tv`; outage-day shell; CSS bundle Wayback 2025-12-31; `https://times.abema.tv` named as brand-owned media dark-canvas inspect). No native-app / TIMES harvested-inventory negative claim. Evidence-domain (“exact values stay attached to named observations”) has adjacent complete B2a; the incomplete “not an ABEMA-published UI specification” phrase was removed so token facts are not recast as editorial (B2/B2a). “committed dark-surface / 361 vs 24 / `--radius` 278” 문단 인접에 derived editorial implementation inference / not ABEMA-authored or a separately published UI specification 한정 (B2/B2a). Key characteristics는 Distinctive traits. Distinctive의 `role-locked` / `deliberately light` 읽기는 인접 완전 B2a (B2/B2a). Distinctive radius bullet is the `--radius: 4px` token, not a “universal” application word. Homepage·CSS URL dual Experience + provenance; homepage URL은 Primary tasks에도 있음 (E2a). TIMES URL dual Experience Scope + Typography & Assets + provenance (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Hex 문자열만으로 역할 보존을 주장하지 않음. Named hex (`#ddaa00` `#dfb015` `#212121` `#f0163a` `#f34461` `#bb122e` `#16deb5` `#44e5c5` `#a873ff` `#af7eff` `#02d1d6` `#ffc400` `#000000` `#0b0b0b` `#171717` `#212121` `#373737` `#1c1c1c` `#e6e6e6` `#999999` `#767676` `#c5c5c5` `#333333` `#555555` `#ffffff` `#f5f5f5` `#eeeeee` `#fcf6e5`)는 해당 역할 슬롯에 유지. Pale/`lt-*` timetable은 light-theme exception. Semantic color retained editorial (`never white-on-yellow`; role-bound named-uses; Foreground `near-white, not pure white`; Link Hover `cool to`; Pale `deliberately light`; Pure White maximum-contrast)는 각 문장 인접 완전 B2a (B2/B2a). Accent heading is “both captured uses”; the “do not collapse them into error only” application was removed. Shape local-geometry (4px not every unlisted surface) has adjacent complete B2a; YAML `rounded.full` 9999 vs 50% remains an A4 non-merge note. Foundations heading no longer uses the Core-checker negation pattern `do not … constraint`. |
| §3 Typography Rules | 옮김 → Typography & Assets | 다섯 증거 등급, CopyRight 스택(YAML compact vs body `--font-family-sans-serif` with Emoji / BIZ UDGothic / Meiryo — both kept, not merged), Helvetica/Arial alphanumeric, Roboto Condensed digits. Unresolved claim: CopyRight license / loadable specimen. TIMES dark-canvas inspect is recorded here as a destination, not as a native-app/TIMES typography-absence claim (E2a). Hiragino/system fallback display boundary is a migration/runtime evidence class in Typography + Avoid, not an ABEMA source Don’t (E2). YAML unitless 1.5는 Type roles 비율 열 (A1a). Type-character 문단 인접에 derived editorial implementation inference / not ABEMA-authored or separately published UI specification 한정 (B2/B2a). |
| §3 / YAML logo Google favicon | 옮김 → Typography & Assets + Avoid; 분리 → provenance | Dual destination (E2a): portable Assets/Avoid는 identity-only / not a first-party mark. provenance Identity holds type/slug. |
| §4 Component Stylings | 옮김 → Components & States | Primary / Secondary / Dark / Danger (`Type: button` ×4), Text Field (`Type: input`), Floating Panel (`Type: card`, Kind omitted — container), LIVE Tag (`Type: badge`, Kind non-interactive, map omitted), Panel Tab (`Type: tab`), Checkbox (`Type: toggle`), Notification Block (`Type: toast`, Kind omitted). YAML 기하와 본문 기하를 같은 슬롯에 유지. Round Tab generic `Focus` `#e6e6e6` / `#212121`는 `focus-visible`로 승격하지 않음 (B1). |
| §4 Primary Dark / Title Card / Pre-Broadcast / NEW / Round Tab / Modal | 옮김 → Components & States | YAML `tokens.components` 밖이지만 §4 본문 고유 값 (A3). Primary Dark는 `com-a-Button--primary-dark` → `Type: button`, Kind interactive + 4-state map (default/hover/focus-visible/disabled); loading/error/success omitted; hover hex 없음 → treatment omitted. Round Tab는 `com-RoundTabItem` → `Type: tab`, Kind interactive + map including selection-meaning not-applicable loading/error/success; generic `Focus`는 additional observed. Title Card / Pre-Broadcast / NEW / Modal은 YAML `type` 없음 → type 발명 없음. Title Card·Modal Kind/map 생략 (C4). Pre-Broadcast·NEW는 `Kind: non-interactive`, map 생략. |
| §4 footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance; URL 옮김 → Experience `scope` | Dual destination (E2a): `https://abema.tv`는 portable Scope + Primary tasks + provenance surfaces/Tier 1. CSS bundle URL은 portable Scope + provenance. `https://times.abema.tv`는 portable Scope + Typography & Assets + provenance surfaces/Tier 1. getdesign.md / refero는 provenance Tier 2 only. Conflicts unresolved: none. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape; home rails는 Primary tasks에도 | 4px base, 4/8/12/16/32, 724/1024/1280 content, 640px mobile, 50% circles, 2/4/8/12–16 radius, YAML `rounded.full` 9999 vs 50% 비합침. Home vertical stack of horizontal carousels is also Primary tasks (browse home rails) (E2a). “Density over air / Black is the separator / edge gradients” 인접에 derived editorial implementation inference / not ABEMA-authored or a separately published UI specification 한정 (B2/B2a). Grid의 `structural exception` 읽기도 해당 불릿 인접 완전 B2a (B2/B2a). Foundations Shape local-geometry (4px not every unlisted surface) has adjacent complete B2a. Thumbnail-veil “lifts” reading has adjacent complete B2a. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | 5-level table + YAML `shadow.floating` / `strong` 값 보존. Elevation philosophy 인접에 derived editorial implementation inference / not ABEMA-authored or separately published UI specification 한정 (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. Governance 통제 문구에 넣지 않음. Principles 머리 한정이 capture-bound list까지 덮음 (B2/B2a). |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. Causal/judgement wording (`single attention signal`; `"broadcasting live"` meaning; `surface-color steps`; `broadcast-bold 700`)는 Avoid 머리 인접 B2a (B2/B2a). Google favicon identity-only is a catalog/identity evidence class in Avoid + Assets, not a source Don’t and not in the source-Don’ts list (E2). Hiragino/system fallback is not a source Don’t; it is recorded as a migration/runtime evidence class in Avoid + Typography (E2). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms; VOD width는 Primary tasks에도 | <640 / 724 / 1024 / 1280, `mb-` type scale 32→27 / 27→20, badges 20→16 / 10px, tab rows 48px, 44/52/56px buttons, 60px timetable arrows, 16:9 thumbnails, `hsla(0,0%,100%,.08)` veil. VOD 1280px / watch-page player is also Primary tasks (play on-demand video) (E2a). 원본에 없는 최소폭 값을 발명하지 않음. Collapsing-purpose (`hierarchy compresses one step rather than reflowing`)와 watch-page hierarchy (`the player remains the fixed anchor`)는 해당 문단 인접 완전 B2a (B2/B2a). Named-measurement application (44–60px heights / 724/1024/1280/640 widths are not a claim that every unlisted surface shares them) has its own adjacent complete B2a (B2/B2a). |
| §9 Agent Prompt Guide | 삭제; §9-only 조각 1건 옮김 → Components | 도구별 복붙 프롬프트. 색·CTA·LIVE badge·search 46px·floating shadow 수치는 이미 Foundations/Components에 있음. 고유 값 옮김 (A3): inactive Panel Tab label `#999999` → Panel Tab. 슬롯 없는 위임 없음. |
| §10 Voice & Tone | 옮김 → Content & Locales | 표 6행 + forbidden register. Verbatim 3건 (title / mute / SorryPage)은 first-party observations. 합성 보이스 샘플 없음. Voice characterization 인접에 derived editorial implementation inference / not ABEMA-authored or a separately published UI specification 한정; verbatim 샘플은 그 한정 밖 (B2/B2a). Forbidden-register has its own adjacent complete B2a after the verbatim interlude; the section header does not cover it (B2/B2a). Title / mute dual Content + provenance; SorryPage heading "ABEMAの表示に失敗しました" is Capture record + Content + provenance (E2a). 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance; free-linear 사실은 Primary tasks에도 | 2016 AbemaTV / CyberAgent–TV Asahi / 藤田晋 / 2020 rebrand / 2021 renewal / World Cup 2022는 서사 맥락이지 토큰 도메인이 아님. Free linear-streaming identity is also Primary tasks (watch free linear channels) (E2a). Source HTML “not independently re-verified this turn”. CADC 2022 「テレビの再発明」은 first-party concept: dual Scope + Principles + provenance narrative (E2a). cadc.cyberagent.co.jp URL은 provenance-only. highlights.jp는 provenance-only (E2a). 봉투 은유(“switched-on television / tuning lamp / on-air light”)는 인접 완전 B2a (B2/B2a). |
| §12 Principles | 옮김 → Experience principles | 다섯 원칙 + UI implication. 인접 본문에 derived editorial implementation inference / not ABEMA-authored or a separately published UI specification 한정을 둠. Item 5의 「テレビの再発明」 개념명은 first-party, UI implication은 아님 (B2/B2a). Capture-bound application과 Governance 일반 문구는 그 한정의 대체물이 아님. |
| §13 Personas | mixed: exclusion boundary 옮김 → Experience Audience; fictional names·ages·cities·biographies 삭제; sidecar 재수록 없음 | 원본이 fictional archetypes라고 명시. portable Audience는 “Source §13’s named fictional archetypes are not Audience and are not primary tasks” exclusion boundary를 유지. Observable-work application has adjacent complete B2a (B2/B2a). 이름·나이·도시·biography는 삭제. sidecar 재수록 없음 (D2). Independently verified Primary tasks 3건은 §13이 아니라 abema.tv linear-watch / home-rails / VOD-play (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability | 본문 보존: 10행 표 (Empty×2, Loading×2, Error×2, Maintenance/outage, Success action-saved, Skeleton, Disabled) (A2). 표 안 causal wording은 capture-record 인접 B2a (B2/B2a). SorryPage heading "ABEMAの表示に失敗しました" is also Content verbatim + provenance (E2a). 선언 컴포넌트는 §4.4를 닫되 미관측 시각값은 발명하지 않음. LIVE/Pre-Broadcast/NEW는 `Kind: non-interactive`, map 생략. Floating Panel / Title Card / Notification Block / Modal은 kind와 map을 생략 (C4). `not captured`를 `not-applicable` 사유로 쓰지 않음 (C1). Mixed/generic Primary, Secondary, Dark, Primary Dark, Danger, Text Field, and Checkbox omit loading·error·success rather than closing them from the global page/error/toast contract (C2). Panel Tab / Round Tab keep selection-meaning not-applicable rows. generic `Focus` (Round Tab)는 `focus-visible` treatment로 승격하지 않음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion | Duration 4개(0.1s fading / 0.1s ease-out buttons / 0.5s `--duration` / 0.3s tooltip delay)와 named easings (`linear` / `--easing: linear`; `ease-out`; `cubic-bezier(.33, 1, .68, 1)` search-suggest, source-labeled easeOutCubic)는 본문 보존. 해당 cubic-bezier는 brand-shipped CSS에 귀속되어 삭제 대상인 무출처 커브가 아님. 삭제된 무출처 cubic-bezier 없음 — omission ledger 없음. Easings 표의 character reading `fast in, soft settle`은 표 직후 인접 완전 B2a (B2/B2a). Motion-purpose (`must never feel like it's animating between channels`; `liveness comes from the content`)는 인접 완전 B2a (B2/B2a). `prefers-reduced-motion: reduce` fades-collapse-to-instant is the B3 reduced-motion fact. The application reading `nothing in the interface depends on animation to communicate state` has its own adjacent complete B2a (B2/B2a). Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 표 밖 curve/animation-name/transition-property를 승격한다는 게이트를 전문 명시. 단일 curve/duration 확인은 그 게이트가 아님 (B3, E2c). |
| HTML comment OmD v0.1 Sources | 분리 → provenance; 일부 개념·verbatim 옮김 → portable Scope / Content | freshness, CSS `:root` quote, CADC/highlights, fictional personas, editorial readings. 「テレビの再発明」 dual Scope + Principles + provenance (E2a). cadc.cyberagent.co.jp provenance-only. highlights.jp provenance-only. Title / mute dual Content + provenance; SorryPage heading also Capture record (E2a). 해석 한정은 Principles만이 아니라 Scope evidence-domain, Scope visual-character/envelope, Audience observable-work, Distinctive `role-locked`/`deliberately light`, Avoid source Don’ts, Semantic color (including Foreground near-white and Pure White maximum-contrast), Shape local-geometry, Elevation, Motion table character, Motion purpose, Motion reduced-motion application, type-character, Capture-record, Layout whitespace/`structural exception`/collapsing·fixed-anchor/thumbnail-veil/measurement-application, Assets thumbnail reading, Content voice(verbatim 제외), Content forbidden-register 인접에도 남김 (B2/B2a). |

### F1 / F2 (v7 mandatory final passes)

Superseded by the wave5 full resubmit scan below. Prior F1 omitted Pure White / Assets thumbnail / veil-lifts sites. Prior F2 recorded `portable_core: true` and TIMES Named-gaps destination that the then-current files did not match.

## Revision 2026-08-23 (wave5 full resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-wave5-2026-08-23-sol-full.md` §3.

| Item | Correction |
|---|---|
| Claim ledger E1 | `primary-hover` / `accent` / `accent-hover` are `abema-css` only. `abema-live` keeps outage-day black canvas, translucent chrome, yellow links, 4px radius, CopyRight stack. |
| B2a / F1 | Worker resubmit put adjacent complete B2a on Pure White maximum-contrast, Assets thumbnail first-party / no-invented-decoration, and thumbnail-veil “lifts”. Fresh F3 then qualified remaining third-class sites (Scope evidence-domain, Audience observable-work, Foreground near-white, Shape local-geometry, reduced-motion application, Layout measurement-application, Content forbidden-register) and reclassified the favicon Don’t. Provenance derived inventory synchronized to that body. |
| C2 | Mixed/generic Primary, Secondary, Dark, Primary Dark, Danger, Text Field, Checkbox omit loading/error/success. |
| D1 | Removed native-app / TIMES negative coverage. TIMES inspect remains Scope + Typography & Assets + provenance. |
| Fallback E2 | Hiragino/system fallback is a migration/runtime evidence class, not an ABEMA source Don’t. §5/§8/§11 task destinations recorded. |
| Core checker | Reworded Foundations role-bound heading so `do not … constraint` no longer trips `missing-actionable-foundations-or-known-constraints`. Complete B2a limiter kept. Exact command/result recorded after re-run. |

### F1 / F2 (resubmit, after F3)

Worker F1 omitted Scope evidence-domain, Audience observable-work, Foreground near-white, Shape local-geometry, reduced-motion application, Layout measurement-application, and Content forbidden-register, and treated the favicon identity-only line as a source Don’t. Worker F2 called SorryPage verbatim dual Content + provenance and Primary Dark a 7-state map. Those overclaims are withdrawn.

- **F1 B2a scan (full DESIGN.md reread after F3):** Adjacent complete B2a on: Scope evidence-domain (values stay attached to named observations); Scope visual-character; Scope TV-envelope; Audience observable-work; Distinctive `role-locked` / `deliberately light`; Principles (five items + capture-bound); Avoid causal source Don’ts; Semantic color `never white-on-yellow` / role-bound named-uses / Foreground `near-white, not pure white` / Link Hover `cool to` / Pale `deliberately light` / Pure White maximum-contrast; Shape local-geometry; Elevation philosophy; Motion table character `fast in, soft settle`; Motion purpose; Motion reduced-motion application; type-character; Capture-record §14 causal wording; Layout whitespace; Layout `structural exception`; Layout collapsing-purpose / player-as-fixed-anchor; Layout thumbnail-veil “lifts”; Layout measurement-application; Assets thumbnail first-party / no-invented-decoration; Content voice (except verbatim); Content forbidden-register (own limiter after the verbatim interlude). Hiragino fallback and catalog Google favicon identity-only are evidence-class boundaries (migration/runtime; catalog/identity), not brand Don’ts and not reconstruction-boundary exemptions. Class 1/2 leftovers: Scope coverage / `live-extract` / outage/Wayback recovery / TIMES named as media inspect / public facts with the source’s “not independently re-verified this turn” qualifier; Primary tasks; D2 Audience exclusion; token/YAML fields and A4 non-merge notes; B3 five-kind gate; Font evidence table; component anatomy and C2 omission sentences; Governance.
- **F2 E2 grep (value + field/role context after F3):** Homepage `https://abema.tv` grep-matched Scope, Primary tasks, provenance. CSS bundle URL grep-matched Scope and provenance. `https://times.abema.tv` grep-matched Scope, Typography & Assets, provenance — not Named gaps as a harvested-inventory absence. `live-extract` grep-matched Scope and provenance. Catalog `primary_color` grep-matched Foundations ABEMA Yellow and provenance identity. Claim ledger: `primary-hover` / `accent` / `accent-hover` grep-matched `abema-css` only; canvas / yellow links / 4px / CopyRight also `abema-live`. 「テレビの再発明」 grep-matched Scope, Principles, provenance. cadc.cyberagent.co.jp and highlights.jp provenance-only. Title / mute dual Content + provenance. SorryPage heading "ABEMAの表示に失敗しました" grep-matched Capture record + Content verbatim + provenance. Inactive Panel Tab `#999999` in Panel Tab. YAML unitless 1.5 in Type roles. `Type: button` ×5. Mixed controls grep-matched omission sentences, not global page/error/toast `not-applicable` rows. Primary Dark map is default/hover/focus-visible/disabled only. `native-app typography` / TIMES harvested inventory grep-matched no portable negative claim. Hiragino fallback grep-matched migration/runtime wording, not “source Don’t”. Favicon identity-only grep-matched Assets + Avoid evidence-class paragraphs, not the source-Don’ts list. B3 five-kind gate in Foundations Motion. `cubic-bezier(.33, 1, .68, 1)` in portable Motion. Fictional biographies absent. Core checker command/result in the Portable Core line after re-run. This F1/F2 list is the post-F3 inventory, not a claim that no further reading could exist.
