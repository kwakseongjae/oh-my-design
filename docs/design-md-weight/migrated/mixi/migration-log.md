# mixi migration log

Source: `web/references/mixi/DESIGN.md`
Sibling read (not the migration input): `web/references/mixi/.verification.md`
Destination: `docs/design-md-weight/migrated/mixi/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/mixi/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use ripgrep match lists per file, never a remembered count.

Source SHA-256 `4677baf9c71a58f878dff0751d221bf9eaabbe73548ea6867a32b33817a80357` (`web/references/mixi/DESIGN.md`). Sibling SHA-256 `799bbd10c373cd951b000b6d9922142068810adc776a4dbc21e2f8b551457764` (`web/references/mixi/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations ink; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# MIXI Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–21. YAML homepage `https://mixi.co.jp/` exact (no extra path) DESIGN dest 2 / provenance dest 4; substring `https://mixi.co.jp/` (includes `/about/`, `/en/`, `/about/conductguidelines/`) DESIGN dest 6 / provenance dest 16 (E2a). `#000000` is dual: DESIGN dest 9 / provenance dest 6 (E2a). Exact `s2/favicons` DESIGN dest 1 at 152 / provenance dest 1 at 16 (E2a). `primary_color` DESIGN dest 2 / provenance dest 2. Exact `logo.type: favicon` DESIGN dest 1 / provenance dest 1. |
| YAML `omd: "0.1"`, `verified`, `verification_v2`, `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): identity rows `provenance.md` 17–21, freshness table 37–43. The source footer's **Verified:** 2026-07-13 is at `provenance.md` 45. `reconciled` DESIGN dest 0 / provenance dest 2 at 19/27 — ledger metadata, no other portable slot. Exact `tokens.source: reconciled` DESIGN dest 0 / provenance dest 1 at 27. `components_harvested` DESIGN dest 0 / provenance dest 2 at 21/217. |
| YAML `tokens.note` | 옮김 → Experience Scope; 분리 → provenance | Full note transcribed at `provenance.md` 31. Capture-bound restatement at `DESIGN.md` 11. Dual (E2a). |
| YAML `tokens.colors` (4 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 82–85. All four roles kept with names, values, and token-set keys: canvas `#ffffff`, ink `#000000`, muted `#5c5c5c`, news-red `#e5004d`. News-red kept as home news-label background, not a global CTA/error/action (`DESIGN.md` 85). Official red/orange emotion-mark rationale kept separate (`DESIGN.md` 87). |
| YAML `tokens.typography.family.ui` | 옮김 → Typography & Assets Family | Family `DESIGN.md` 133. `Noto Sans JP` dest 17. Token-set path `tokens.typography.family.ui` dest 1 at 133. |
| YAML `tokens.typography.body` / `header-navigation` / `guideline-heading` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 144–146. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.8` dest 2; `1.15` dest 2; `1.5` dest 2. Source §3 px writings sit beside them: `27px` / `14.95px` / `52.5px`. All three YAML `use` strings restored verbatim in the Token-set use column (A1, A3, wave 37 keep-both): `Observed about and conduct-guideline body copy` / `Observed corporate-header global-navigation control` / `Observed conduct-guideline section heading`. `tokens.typography.body.size` `15`, `header-navigation.size` `13`, `guideline-heading.size` `35` at `DESIGN.md` 148. News-label `11px` stays on the component, not as a fourth type role (`DESIGN.md` 140/225). |
| YAML `tokens.rounded` (`control: 3`, `news-image: 5`) | 옮김 → Foundations shape | Unitless: `DESIGN.md` 95 (`control: 3` · `news-image: 5`). `tokens.rounded.control: 3` dest at 100/103. `tokens.rounded.news-image: 5` dest at 101/103/234. Header radius `0px` stays a component writing and is not `tokens.rounded.control: 3` (`DESIGN.md` 99/190). Trailing `5px` of news-label padding `4px 9px 5px` is not `tokens.rounded.news-image: 5` (`DESIGN.md` 225). |
| YAML `tokens.components` (2 records) | 옮김 → Components & States | `DESIGN.md` 167–225. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×1 (170 = YAML header-global-navigation), `Primitive type: \`badge\`` ×1 (205 = YAML news-category-label). Both YAML `use` strings restored verbatim as `Token-set use:` lines (187/223). YAML `font` shorthands `13px / 600 Noto Sans JP` and `11px / 600 Noto Sans JP` kept beside the §4 writings with extra slashes. Header YAML use writes `home::[data-omd-capture=3]` without quotes; §4 writes `home::[data-omd-capture="3"]`. Both writings stay (`DESIGN.md` 188–189). No §4-only component borrowed a `Primitive type`. |
| §1 Visual Theme & Atmosphere (legacy 89–95) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–11; Distinctive traits 38–44. The atmosphere readings (unusually deliberate; stark editorial black-and-white; restrained rather than a universal product system) and the route-local red news label ≠ universal action colour carry an adjacent complete qualifier at 11 (B2/B2a). Capture counts 3 surfaces / 30 component variants / 0 interaction expansions / coverage 71 / 337 visible uses at 11. |
| §1 / §11 연도·연혁·리브랜드 | 옮김 → Experience Scope 서사 (`DESIGN.md` 14, claim 밖 Experience 본문) | 1997 Find Job!; 2004 mixi social network; MONSTER STRIKE; family sharing; sports; live-event activity; mobile entertainment; events; 2022 MIXI, Inc.; “Third Founding”; We-Time Economy; heavy uppercase logo rationale (trust, stability, ubiquity, a leading presence, a large-scale communications presence); two red/orange rotated underscore forms; company’s documented red and orange emotion marks give excitement and warmth separate roles. Each source paragraph’s last sentence is kept as one unit: company narrative not one web component system; they do not fill in missing service UI facts; they do not establish unobserved application controls, signed-in flows, or service-level design tokens. Classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §2 Color & Surface Evidence | 옮김 → Foundations semantic color | `DESIGN.md` 82–87. Characterizing phrases (supporting corporate text only; not a global CTA/error/action; official red as excitement/adrenaline and orange as comfort/warmth without an exact CSS token; catalog `primary_color` beside ink, not a second colors token; same ink hex on header/news-label component paths) covered by the adjacent qualifier at 80 (B2/B2a). |
| §3 Typography Rules — evidence classes, family, scale | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 121–127; family 133–136; hierarchy table 144–146. Official-product-use / canonical-family / Family-fallback / Type-roles readings carry adjacent complete qualifiers at 129, 136, and 140 (B2/B2a). Source evidence-class label Unresolved is kept in the resolution cell; the class column reads `Unresolved in this capture` so a bare `Unresolved` cell is not a prescriptive placeholder. That keep-both is named in the `:129` qualifier (`Unresolved in this capture` DESIGN dest 2). |
| §3 MIXISANS / mixi-bold / mixi-medium / Noto Sans / swiper-icons | 옮김 → Typography & Assets Font evidence + Family | `mixi-bold` dest 3; `mixi-medium` dest 3; `MIXISANS` dest 9; `swiper-icons` dest 2; declared `Noto Sans` dest in 126/135/136. mixi2 dest 2 at 129. |
| §4 Component Stylings (legacy 128–152) | 옮김 → Components & States | Header `DESIGN.md` 167–200; News category label 202–225. §4 body values and YAML `use` / font / radius / padding forms are both kept. `interactionCount: 0` dest 2 at 44/163. News-card rows are links/rows rather than button semantics (`links/rows` DESIGN dest 3 at 163/225/225-qualifier). Footer **Verified** / Tier 1 / Tier 2 / Conflicts at legacy 155–158 are provenance (footer row). Exact `static default specimen, not a state model` DESIGN dest 1 at 179; the `:190` qualifier names the same reading without duplicating that exact phrase. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 155–158) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness table `provenance.md` 37–43; Verified paragraph 45; Tier 1 list 70–74; Tier 2 list 78–79. Homepage / about / conduct / en/about / history URLs are dual as listed in the identity row (E2a). Conflicts unresolved: none — `provenance.md` 47. |
| §5 Layout & Responsive Evidence | 옮김 → Layout & Platforms | `DESIGN.md` 230–234: 70px header, broad editorial content widths, 390px-wide home news-card rows, 22px category labels, 5px news-image rounding. No breakpoint system, mobile composition, grid scale, or touch-target policy — source wording kept. The recorded-measurements reading carries an adjacent qualifier at 234 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 107: no reusable box-shadow; transparent fields, black rules, image framing, and whitespace; do not create card, dialog, popover, or modal shadow values from static page structure. The recorded-treatment reading carries an adjacent qualifier at 107 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 59–62 (four Do rules), under the grouping qualifier at 57. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 68–71, under the qualifier at 66. The four prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 232: Only the supplied 1440×900 desktop surface captures are available. No mobile viewport, breakpoint, focus management, menu expansion, or responsive-state observation is recorded. `measures 1440px` is not written (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion; the §9-only sentences land as follows: square-cornered on Header radius `DESIGN.md` 174; exact surface and selector evidence requirement on Avoid `DESIGN.md` 72 (A2, A3). The check is itemised at `provenance.md` 182. |
| §10 Voice & Tone | 옮김 → Content & Locales | Adjectives and Do/Don't table `DESIGN.md` 239–245; illustrative samples 247. The qualifier at 239 covers the voice reading (B2/B2a, A5). The illustrative-sample reading carries its own adjacent complete qualifier at 249. Byte-exact: “Make a moment worth sharing.” “Leave room for a pleasant surprise.” “Build the connection, then let the joy travel.” |
| §11 Brand Narrative | 옮김 → Experience Scope 서사 | `DESIGN.md` 14. Combined with §1 연혁 as one unit including each paragraph's last sentence. See §1 / §11 row above. |
| §12 Principles — 4 numbered | 옮김 → Experience principles | `DESIGN.md` 50–53 under the B2a form at 48: "These 4 items are a derived editorial implementation inference from the verified surfaces; they are not MIXI-authored or a separately published UI specification." No separately published MIXI UI specification is named (the MIXI DESIGN activity report is recorded as culture context, not a public component specification at `DESIGN.md` 32), so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` Derived editorial inventory (25 data rows). |
| §13 Stakeholder Groups — 3 source-grounded groups | 옮김 → Experience Audience | The source's own header says *Source-grounded groups, not fictional personas.* That header and the three group wordings land at `DESIGN.md` 28–32. They are not rewritten as new segment labels. No named biography is present in the source, so none is re-hosted (D2, D2a). Disposition at `provenance.md` 178. The three `primary-tasks` at 21–23 come from surfaces and controls the source records; 19 says so and qualifies the step from surface to "primary task". Audience grep against source: People sharing communication… dest 1 / src 1; MIXI designers and collaborators dest 1 / src 1; Service audiences across MIXI’s portfolio dest 1 / src 1 (U+2019 apostrophe restored). |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source body preserved at `DESIGN.md` 159–163 (A2; the catalog graph is still 0/440, so nothing is delegated), including zero interaction records, omitted rather than synthesized, and `interactionCount: 0` removes hover/focus/pressed/disabled/error/dialog/toast/menu-state claims only. The applicability rule is at 165, whose qualifier at 165 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Header global navigation is interactive and declares the seven canonical states. loading/error/success close with a role reason (C2 v10) — destination navigation does not commit an in-place operation (`DESIGN.md` 198–200). News category label has YAML `type: badge` and no interactive-kind evidence, so kind and a state-applicability map are omitted rather than confirmed (C4) (`DESIGN.md` 165/225). `loading \| applicable` dest 0. This is not a complete state-coverage claim (`DESIGN.md` 165). |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 111: No reusable duration, easing, transition, or motion rule; static corporate elements and a brand narrative about emotion do not establish a motion contract; no motion token is promoted. No cubic-bezier values exist in the source, so none were deleted. |
| §15 Motion — promotion gate | 옮김 → Foundations motion | B3 is held: the promotion condition at `DESIGN.md` 113 names all five evidence kinds — computed transition properties, animation name, duration, easing, and reduced-motion behavior — and the per-component gate, in full text (E2c). The five-kind gate itself carries an adjacent complete B2a close on the same line. |

## Sibling handling (`web/references/mixi/.verification.md`)

The sibling exists — confirmed with `find web/references/mixi -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 138–151 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: Header hamburger `button`; header width `52px`; news-label width `98px`; news-red treatments beyond `#e5004d` background (white text, 2px white border, `3px 0px 4px`, 42px × 21px); news image wrapper 120px × 90px; breadcrumb type `11px/400/15.4px`; `MIXISANS-BOLD.woff` / `MIXISANS-MEDIUM.woff`; class names `c-header__globalNavi--itemInner` / `a.c-newsCard__inner` / `li.c-newsCard`; artifact timestamp; four component classes; Designer Relations; 3C as an acronym. Recorded at `provenance.md` 157–168.
- Measured `DESIGN.md` 0 for those sibling-only strings: `hamburger` 0 · `52px` 0 · `98px` 0 · `130px` 0 · `MIXISANS-BOLD` 0 · `Designer Relations` 0 · `15.4px` 0 · `2px white border` 0.
- `#000000` / `#ffffff` / `#5c5c5c` / `#e5004d` / 70px / `4px 9px 5px` / 22px / 3px / 13px/600/14.95px / 15px/400/27px / 35px/600/52.5px / 337 / coverage 71 / 1997 Find Job! / 2004 mixi / 2022 MIXI name change are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. MIXI's published Japanese is not the quoted surface copy in this source; a hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels, names, and slogans in the source body | 18 distinct | 0 | 0 | Find Job! / mixi / MONSTER STRIKE / MIXI, Inc. / “Third Founding” / We-Time Economy / MIXI Way / mixi2 / MIXISANS / mixi-bold / mixi-medium / Noto Sans JP / Noto Sans / swiper-icons / “Make a moment worth sharing.” / “Leave room for a pleasant surprise.” / “Build the connection, then let the joy travel.” / MIXI DESIGN. |
| YAML `use` strings | 5 distinct | 0 | 0 | All three type-role Token-set use lines and both component Token-set use lines restore the YAML `use` byte-exact. |
| `node test-v2/tools/latin-copy-audit.mjs --brand mixi --candidate docs/design-md-weight/migrated/mixi/DESIGN.md` | 0 lost / 1 brand scanned | 0 published | 0 | `scanned: 1`, `clean: 1`, `withLoss: 0`, `totalLost: 0`. |
| Sibling published strings | sibling-only selectors / hamburger / MIXISANS filenames | 0 | 0 | Ledger at `provenance.md` 157–168; not promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: Find Job!, MONSTER STRIKE, MIXI, Inc., Third Founding, We-Time Economy, MIXI Way, mixi2, MIXISANS, mixi-bold, mixi-medium, Noto Sans JP, swiper-icons, Make a moment worth sharing., Leave room for a pleasant surprise., Build the connection, then let the joy travel.

A5 분모: hand sweep of source published labels 18 extracted / 0 missing; YAML use 5 / 0; latin-copy-audit published-copy lost 0 / 1 scanned; gate `copy-loss` compared 0 / candidates 83.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand mixi --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 83, detail: "바늘 0개 — 이 브랜드에서 A5는 기계 검사되지 않았다. 발행 라틴 문자열을 손으로 전수 대조하라." }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 83.

## Deviations recorded

- `DESIGN.md` is 4,562 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: four color keys, three type roles, two declared component records, a source §14 body plus seven-state applicability on the header control, `tokens.rounded.control: 3` kept off header `0px` and off news-image `5`, the full §1/§11 founding-and-portfolio narrative including each paragraph's last sentence, YAML/§3 keep-both, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published MIXI UI specification is named in the source (MIXI DESIGN is culture context, not a public component specification), so every derived-editorial close uses the toss-form `not MIXI-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Measure: `derived editorial implementation inference` DESIGN = `not MIXI-authored` DESIGN = 25. Provenance derived ledger 25 rows at 188–212 (E1 1:1). `node scripts/check-limiter-ledger.mjs mixi` 본문 25 = 원장 25.
- F3-audit portable DESIGN SHA-256 `add7fdd329b0ac863e420e5e5c5e7d8aeb604cddd56cf1e36b8f9b11d49a8745`. Provenance SHA-256 `7ba306190ebe568f18a8264275383d5055263048cb50eaba9a38008e5fe07e59`.
- Scope claim body stops after the three-route capture bound (`DESIGN.md` 9–11). The §1/§11 narrative stays in Experience after `claim-end` (`DESIGN.md` 14). Moving it out of the claim body avoids a conformance false positive on the source sentence “without turning every public surface into a multicolour UI” (`without` + `surface` inside 32 characters) without rewriting that source sentence (E3).
- Font-evidence class column reads `Unresolved in this capture` with the source label Unresolved kept in the resolution cell, because a bare table cell `Unresolved` is classified as a prescriptive placeholder by `BARE_PLACEHOLDER`.

## Mandatory final passes

- **F1 B2a scan.** Re-read the finished `DESIGN.md` from the title through Named gaps. Every causal, interpretive, or judging sentence — Scope token-surface and atmosphere readings including route-local news-red ≠ universal action colour, founding-and-portfolio classification, Primary-tasks selection, Audience group reading, Distinctive grouping, Principles and their UI implications, Application rules / Avoid reasons, Semantic-color key splits including catalog `primary_color` beside ink and the same ink hex on header/news-label component paths, Spacing omission, Shape unmerges, Elevation recorded-treatment, Motion five-kind gate, Font-evidence canonical-family reading and Unresolved keep-both, Family fallback, Type-roles keep-both, Assets slug classification, Components how-to-read, per-component geometry readings including static default specimen rather than a state model and news-card rows as links/rows rather than button semantics, Layout recorded-measurements reading, Content voice / illustrative-sample readings, Named-gaps catalog reading — has an adjacent complete qualifier: derived editorial implementation inference / not MIXI-authored or a separately published UI specification. Count 25 = provenance inventory 25 (188–212).
- **F2 E2 대조.** Each log row above was written only after `grep -o <pattern> <file> | wc -l` confirmed the named values in `DESIGN.md` and/or `provenance.md` (file-by-file; never `grep -c`). Dual destinations list both. Compliance claims (B3 at 113; B2a form at 48; §14 body at 159–163) name the lines that actually hold that full text. Homepage exact dest 2/4, substring dest 6/16, `#000000` dest 9/6, `reconciled` dest 0/2, `1.8`/`1.15`/`1.5` dest 2/2/2, `primary_color` dest 2/2, `Unresolved in this capture` dest 2, `static default specimen, not a state model` dest 1, `links/rows` dest 3, freshness 37–43, Verified 45, Conflicts 47, Tier 1 70–74, Tier 2 78–79.

## Unique-phrase self-check (wave 43)

Extracted 59 source-unique expressions (years, proper names, quotes, §11 last sentences, value modifiers, §7/§15 constraint sentences). First pass missing 2 (`square-cornered` from §9; italic header `Source-grounded groups, not fictional personas.`). A3 pass then found 1 more (`static default specimen, not a state model` from §9). All three restored. Final `grep -oF` missing 0. Restored 3.
