T2 Codeit 마이그레이션을 규칙집 기준으로 진행합니다. 지정 문서부터 순서대로 읽고, 골든 샘플과 레거시 원문을 맞춰 옮기겠습니다.세션 복원 후 지정 문서부터 순서대로 읽습니다. 규칙집 v8과 골든 샘플을 기준으로 맞춥니다.규칙집과 무신사 골든 샘플, Codeit 원문을 이어서 읽습니다.원문·골든 형태는 확보했습니다. 최근 PASS 이관과 게이트가 실제로 검사하는 항목을 대조합니다.원문 토큰과 C2·B2a 패턴을 맞춘 뒤 Core 본문부터 작성합니다. `type: button|card|tab`과 unitless line-height는 그대로 보존합니다.본문을 처음부터 다시 읽어 B2a 한정을 확인하고, 줄 번호를 grep으로 잡아 provenance와 원장을 맞춥니다.F1에서 한정이 비어 있던 문장 네 곳을 고칩니다. 토큰 노트, Semantic 후단, 배지 kind, 레이아웃 범위입니다.provenance.md를 DESIGN 실측 줄 번호에 맞춰 작성합니다.게이트가 `focus-visible`과 hex가 같은 줄에 있는 문장을 잡았습니다. 해당 B2a 문장을 나눠 B1 오탐을 없앱니다.게이트는 통과했습니다. 줄이 밀렸으니 dest를 다시 grep으로 맞추고 Portable Core도 확인합니다.Codeit legacy `DESIGN.md`를 Core v2로 옮겼습니다. 규칙집 **v8**, musinsa 개정본 형태를 기준으로 했고, 제출 전에 F1(B2a)·F2(E2 grep)를 수행했습니다.

산출물:
- `docs/design-md-weight/migrated/codeit/DESIGN.md`
- `docs/design-md-weight/migrated/codeit/provenance.md`
- `docs/design-md-weight/migrated/codeit/migration-log.md`

보존·분리 요약:
- YAML unitless line-height `1.21` / `1.29` / `1.38` / `1.43` / `1.67` / `1.50`는 비율 그대로
- `type: button` ×5, `card` ×3, `tab`, `input`, `badge`는 컴포넌트별로 유지
- 무출처 cubic-bezier 3개는 승격하지 않고 Named gaps + provenance에만 남김. duration `120ms` / `220ms` / `320ms`와 B3 다섯 증거 게이트는 Foundations에 유지
- §13 가상 페르소나는 본문·provenance 모두에서 삭제. Primary tasks 4건은 캡처 CTA에서만
- Google favicon URL은 provenance-only. 카드는 C4로 kind/map 생략. Hero CTA는 L/E/S 생략, 칩·필·고스트·탭은 역할상 not-applicable

검증: `--gate-only` **PASS** / problems `[]`. Core `portable_core: true`. SHA-256 `3e7835a9a1284b6709d23c9dca3457357f3123f3ff9cfabbdecd2689489dac7e`. 카탈로그 채택은 아닙니다.

DONE migrated=1
