# Codeit Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Codeit (코드잇) is a Korean EdTech company operating an online IT-education platform. Catalog homepage identity is `https://www.codeit.kr`. Treating that catalog homepage as identity rather than a fifth captured surface, treating the four 2026-06-26 live-inspect routes as this contract’s coverage, and treating token extraction as `live-extract` for those four surfaces, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. This contract covers four first-party live-inspect surfaces from 2026-06-26: homepage `https://www.codeit.kr`, subscription `https://www.codeit.kr/subscription`, explore `https://www.codeit.kr/explore`, and sign-in `https://www.codeit.kr/signin`. Token extraction is `live-extract`.

Treating `https://tech.codeit.kr` as an official engineering blog and `https://about.codeit.com/ko/` as an official company intro for product-scope narrative rather than token-capture surfaces, treating `sprint.codeit.kr` as a source-named bootcamp operator rather than a captured token surface, and keeping color, type, and component values attached to the surface that established them, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. `https://tech.codeit.kr` (“Codeit blog”) is the official engineering blog and `https://about.codeit.com/ko/` (“코드잇 소개”) is the official company intro; both are brand-owned secondary surfaces confirmed live on 2026-06-26. They are official engineering-blog and official company-intro sources for product-scope narrative. Token-capture stays limited to the four live-inspect routes. Career-track bootcamps are named as operating via `sprint.codeit.kr`; that host stays outside this pass’s token-capture routes.

Treating the source token note as reconstruction coverage for these four surfaces, including primary-as-live-CTA-purple-across-home-subscription-explore, brighter-and-deep-violets-as-emphasis-not-CTA-fill, lavender-as-recommended-plan-card-fill, ink-as-warm-near-black-never-pure-black, and Pretendard-800-as-display-H1-with-SpoqaHanSansNeo-elsewhere, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Source token note: primary = live CTA purple (`#9933ff`) across home/subscription/explore; brighter `#8f00ff` + deep `#760dde` are emphasis violets; recommended-plan card fills lavender `#b363fd`. Text is warm near-black ink `#333236`, never pure black. Display H1 is Pretendard 800; all other headings + body are SpoqaHanSansNeo.

The following atmosphere readings — homepage-as-friendly-energetic-consumer-product-rather-than-a-dry-e-learning-portal, white-canvas-broken-up-by-cool-grey-and-pale-violet-bands-so-the-page-feels-open-and-modern, ink-deliberately-not-pure-black-so-long-Korean-copy-stays-readable-and-approachable, purple-as-the-single-saturated-action-color-that-trains-the-eye, two-font-split-as-bold-where-it-motivates-and-calm-where-it-teaches, and playful-violet-to-pink-plus-generous-rounded-geometry-as-what-distinguishes-Codeit-from-typical-fintech-minimal-Korean-sites — are a derived editorial implementation inference from the verified surfaces; they are not Codeit-authored or a separately published UI specification. The captured homepage canvas is `#ffffff`, broken up by `#f6f6f8` and `#f8ecff` section bands. Text sits in `#333236`. The live CTA purple is `#9933ff` on the nav membership chip, the hero “무료 체험 시작하기” button, and plan CTAs. The hero headline “5분마다 인생이 바뀐다” is Pretendard ExtraBold (800) at 68px with `-1.5px` tracking. Section titles, card heads, navigation, and body copy are SpoqaHanSansNeo. Beyond the core purple the source names brighter violet `#8f00ff`, deep violet `#760dde`, lavenders `#b363fd` and `#c47cfd`, pink `#ff52b7`, night `#080c14`, and plum `#3d1457`. Geometry on the captured surfaces includes 20px–24px cards, pill chips, and CTAs in a 6px–10px-to-22px range. The homepage is nearly shadowless; the subscription page lifts plan cards with `rgba(0,0,0,0.08) 0px 2px 14px` and the recommended lavender `#b363fd` card with `rgba(0,0,0,0.2) 0px 4px 24px`.

Treating the following first-party live-site product-scope facts as narrative rather than interface tokens, treating the five-minute cycle as the homepage promise rather than a published UI specification, treating the official engineering blog and official company intro as providing that narrative context without by themselves supplying interface tokens, and treating broader corporate facts beyond the inspected surfaces as omitted rather than reconstructed from general public knowledge, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. The live site frames learning around a “5분 학습 사이클” of short lessons, hands-on practice (실습), and quizzes, with the homepage promise “5분마다 인생이 바뀐다”. Product scope on those surfaces spans self-paced courses (the explore catalog across 입문/데이터/웹/디자인/프로그래밍/AI/비즈니스 tracks), a membership subscription, career-track bootcamps, corporate training (기업교육), government-subsidized lifelong-education vouchers (평생교육이용권), the “AI GURU” tutor, and gamified progress systems (페이스 메이커, 레벨업 시스템, 월별 챌린지). The official engineering blog and official company intro provide that narrative context; they do not by themselves supply interface tokens. Broader corporate facts beyond the inspected surfaces are omitted.

The following refuse-and-embrace reading — refusing-dry-text-heavy-legacy-LMS-chrome-and-fear-driven-학원-urgency, embracing-a-friendly-white-and-purple-consumer-aesthetic-and-a-single-trustworthy-action-color, and positioning-learning-as-approachable-habitual-and-even-fun-with-the-source-line-개발도-게임처럼 — is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Source §11 records that refuse/embrace contrast and the line “개발도 게임처럼.”
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=4 lang=en -->
### Primary tasks

Treating the following four captured-surface outcomes as Primary tasks — start a free trial from the captured homepage CTA (“무료 체험 시작하기”); start membership from the captured subscription plan CTA (“멤버십 시작하기”); search and scan courses on the captured explore catalog (“어떤 강의를 찾고 있나요?”, “모든 IT 강의 보러가기”); sign in with email on the captured sign-in surface (“이메일로 로그인하기”) — and not lifting source §13 fictional archetypes into this list, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

- Start a free trial from the captured homepage CTA (“무료 체험 시작하기”).
- Start membership from the captured subscription plan CTA (“멤버십 시작하기”).
- Search and scan courses on the captured explore catalog (“어떤 강의를 찾고 있나요?”, “모든 IT 강의 보러가기”).
- Sign in with email on the captured sign-in surface (“이메일로 로그인하기”).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, tying observable work only to the four primary tasks, not substituting those entries, and treating product-scope offerings as offerings rather than researched persona segments, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. No individual personas are promoted. Source §13 is labeled fictional archetypes informed by publicly observable user segments; names in that section are illustrative and do not refer to real people. Do not substitute those entries. Product scope names self-paced courses, membership, bootcamps, corporate training, and lifelong-education vouchers as offerings, not as researched persona segments.

### Distinctive traits

Treating `#9933ff` as catalog `primary_color` and the live CTA purple rather than as `#8f00ff` / `#760dde` / `#b363fd` / `#c47cfd` / `#ff52b7`, treating canvas `#ffffff` as unmerged from on-primary `#ffffff`, treating ink `#333236` as unmerged from night `#080c14` and plum `#3d1457`, treating Pretendard 800 as hero-only rather than the workhorse family, treating YAML `rounded` 6 / 8 / 12 / 20 / 24 / 9999 as unmerged from component 10px / 22px / 21px, treating YAML button type-role 16px / 500 as unmerged from the hero CTA 18px / 700, and treating hover, focus, pressed, disabled, loading, error, and success paints as not promoted from the 2026-06-26 default computed-style notes, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

- Catalog `primary_color` / YAML `primary` `#9933ff` as the live CTA purple on home, subscription, and explore
- Warm near-black ink `#333236` for text; canvas `#ffffff`; surface `#f6f6f8`; violet surface `#f8ecff`
- Two-font split: Pretendard ExtraBold (800) for the 68px hero; SpoqaHanSansNeo for headings, navigation, buttons, and body
- Tight negative tracking on display type (`-1.5px` at 68px, source `-1px` at 48px next to YAML `-1.0` / `-1.0px`, `-0.3px` on mid headings)
- Local rounded geometry: 20px course cards, 24px plan cards, 6px chips/badges, 10px hero CTA, 22px pill CTA, 21px search field; YAML `pill` 9999 is a YAML field, not those observed radii
- Mostly flat homepage; plan-card shadow `rgba(0,0,0,0.08) 0px 2px 14px`; recommended-card shadow `rgba(0,0,0,0.2) 0px 4px 24px`
- Emphasis violets `#8f00ff` and `#760dde`; lavenders `#b363fd` and `#c47cfd`; pink `#ff52b7`; dark bands `#080c14` and `#3d1457`
- The 2026-06-26 inspect notes record default computed styles on the four surfaces. Hover, focus, pressed, disabled, loading, error, and success paints are not promoted from those notes

### Principles

These 5 items — Small steps, real progress, with the product-thesis reading that the five-minute cycle is the product thesis and the UI implication to keep units short and self-contained and make “start now” the easiest possible action with one purple CTA; One action, one color, with the reading that purple (`#9933ff`) means “do this” and the UI implication to reserve saturated purple for the primary CTA so the next step is never ambiguous; Motivate, don’t intimidate, with the reading that coding is framed as approachable and game-like and the UI implication of warm ink text, rounded shapes, and encouraging copy with no fear-based urgency; Bold where it persuades, calm where it teaches, with the UI implication of Pretendard ExtraBold for the hero promise and quiet SpoqaHanSansNeo 400 for lesson and feature content; Flat and friendly, with the reading that color and shape carry hierarchy, not heavy elevation, and the UI implication to segment with color bands and rounded cards and reserve shadow for the one card that must stand out — are a derived editorial implementation inference from the verified surfaces; they are not Codeit-authored or a separately published UI specification.

1. **Small steps, real progress.** The five-minute cycle is the product thesis. *UI implication:* keep units short and self-contained; make “start now” the easiest possible action with one purple CTA.
2. **One action, one color.** Purple (`#9933ff`) means “do this.” *UI implication:* reserve the saturated purple for the primary CTA so the next step is never ambiguous.
3. **Motivate, don’t intimidate.** Coding is framed as approachable and game-like. *UI implication:* warm ink text, rounded shapes, encouraging copy; no fear-based urgency.
4. **Bold where it persuades, calm where it teaches.** *UI implication:* Pretendard ExtraBold for the hero promise; quiet SpoqaHanSansNeo 400 for lesson and feature content.
5. **Flat and friendly.** Color and shape carry hierarchy, not heavy elevation. *UI implication:* segment with color bands and rounded cards; reserve shadow for the one card that must stand out.

Treating the following as a capture-bound application of source §7 Do’s — reserve-`#9933ff`-for-the-primary-CTA, Pretendard-ExtraBold-800-for-the-hero-and-SpoqaHanSansNeo-for-everything-else, ink-`#333236`-instead-of-pure-black, color-bands-`#ffffff`-`#f6f6f8`-`#f8ecff`-not-heavy-borders, rounded-20px-24px-cards-pill-chips-6px-10px-CTAs, lift-only-the-recommended-plan-card, tight-negative-tracking-`-1.5px`-at-68px, and lavender-`#b363fd`-fill-to-mark-the-featured-plan — is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

- Reserve purple (`#9933ff`) for the primary CTA / action.
- Use Pretendard ExtraBold (800) for the hero headline and SpoqaHanSansNeo for everything else.
- Use warm near-black ink (`#333236`) for text instead of pure black.
- Separate sections with color bands (`#ffffff`, `#f6f6f8`, `#f8ecff`), not heavy borders.
- Keep geometry rounded — 20px–24px cards, pill chips, 6px–10px CTAs.
- Lift only the recommended plan card; keep the rest flat.
- Apply tight negative tracking on display type (`-1.5px` at 68px).
- Use the lavender (`#b363fd`) fill to mark the featured plan.

### Avoid

The following items copy source §7 Don’ts, including no-spread-purple-because-it-dilutes-the-single-action-signal, no-pure-black-body-text-use-ink-`#333236`, no-SpoqaHanSansNeo-hero-because-Pretendard-owns-the-68px-display-moment, no-drop-shadows-on-the-marketing-homepage-because-it-is-a-flat-color-banded-system, no-sharp-square-corners-on-cards-or-chips-because-everything-is-rounded, no-second-saturated-CTA-hue-because-purple-is-the-action-color, no-positive-letter-spacing-on-headlines-because-Codeit-tracks-tight, and no-overloading-accent-colors-onto-interactive-controls-because-they-are-decorative. They are a derived editorial implementation inference from the verified surfaces; they are not Codeit-authored or a separately published UI specification.

- Don’t spread purple across many elements — it dilutes the single-action signal.
- Don’t use pure black for body text — use ink `#333236`.
- Don’t set the hero in SpoqaHanSansNeo — Pretendard owns the 68px display moment.
- Don’t add drop shadows to the marketing homepage — it’s a flat, color-banded system.
- Don’t use sharp / square corners on cards or chips — everything is rounded.
- Don’t introduce a second saturated hue as a CTA color — purple is the action color.
- Don’t use positive letter-spacing on headlines — Codeit tracks tight.
- Don’t overload accent colors (`#8f00ff`, `#760dde`, `#c47cfd`, `#ff52b7`) onto interactive controls — they are decorative.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings — catalog-primary-`#9933ff`-as-live-CTA-purple-not-`#8f00ff`-not-`#760dde`-not-`#b363fd`, canvas-`#ffffff`-unmerged-from-on-primary-`#ffffff`, ink-`#333236`-unmerged-from-night-and-plum, surface-`#f6f6f8`-unmerged-from-violet-surface-`#f8ecff`-violet-tint-`#f3e1fd`-pink-surface-`#ffebf7`, and component-fields-staying-on-those-controls-rather-than-as-extra-general-inks — are a derived editorial implementation inference from the verified surfaces; they are not Codeit-authored or a separately published UI specification.

- **Codeit Purple / primary** (`#9933ff`): catalog `primary_color`. YAML `primary`. Live CTA purple on the nav “멤버십 안내” chip, the hero “무료 체험 시작하기”, and plan “멤버십 시작하기”.
- **Bright Violet** (`#8f00ff`): YAML `primary-bright`. Subscription-surface emphasis accents and price highlights. Not the primary CTA fill.
- **Deep Violet** (`#760dde`): YAML `primary-deep`. Source color role for active / pressed link text and emphasis labels on light backgrounds. Not a `focus-visible` treatment.
- **Lavender** (`#b363fd`): YAML `lavender`. Fill of the highlighted / recommended membership card.
- **Soft Lavender** (`#c47cfd`): YAML `lavender-soft`. Decorative tints, gradient stops, and secondary accents.
- **Pink** (`#ff52b7`): YAML `pink`. Sparing playful highlights and tags.
- **Ink** (`#333236`): YAML `ink`. Warm near-black for body text and most headings.
- **Night** (`#080c14`): YAML `night`. Darkest section background for immersive dark feature bands.
- **Plum** (`#3d1457`): YAML `plum`. Deep purple card / section background for premium dark-on-violet moments.
- **Canvas** (`#ffffff`): YAML `canvas`. Default page and card background.
- **Surface** (`#f6f6f8`): YAML `surface`. Cool grey for segmented sections, the search field, and neutral buttons.
- **Violet Surface** (`#f8ecff`): YAML `surface-violet`. Pale violet tint for soft branded section bands.
- **Violet Tint** (`#f3e1fd`): YAML `tint-violet`. Slightly deeper violet tint for cards sitting on violet sections.
- **Pink Surface** (`#ffebf7`): YAML `surface-pink`. Pale pink tint for warm highlight blocks.
- **On Primary** (`#ffffff`): YAML `on-primary`. Text and icons on the purple CTA. Same hex as canvas; the YAML jobs stay named separately.

Keeping those component fields on the controls rather than as extra general inks is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. YAML component fields such as button-primary `bg` `#9933ff` / `fg` `#ffffff` stay on those controls. They are not extra general inks.

### Spacing

Treating YAML `spacing` numbers as recorded without a required px suffix, treating body 4px / 8px / 12px / 16px / 20px / 24px / 32px / 48px as body-recorded observations rather than a converted YAML scale, and treating CTA padding 8px 24px / 10px 32px and card padding 24px 32px as component fields rather than a universal gutter, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. YAML `spacing`: xs 4, sm 8, md 12, base 16, lg 20, xl 24, xxl 32, section 48. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names a 4px base unit and the scale 4px, 8px, 12px, 16px, 20px, 24px, 32px, 48px. CTA padding clusters at 8px 24px (outline) and 10px 32px (primary); card padding runs 24px 32px.

### Shape

YAML `rounded`: sm 6, md 8, base 12, lg 20, xl 24, pill 9999.

The following local-geometry reading, including 6px-nav-chips-and-count-badges, source Medium range `8px–10px` with YAML `md` 8 and hero 10px remaining unmerged local fields, 12px-inner-containers, 20px-course-cards, 24px-plan-cards, 22px-pill-CTAs, 21px-search-field, and YAML-`pill`-9999-as-a-YAML-field-not-those-observed-radii, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

- Small (YAML `sm` 6): nav chips, count badges — observed 6px
- Medium (`8px–10px`): standard buttons, neutral actions. YAML `md` 8 is the YAML field (observed 8px); hero primary CTA is a separate 10px field
- Base (YAML `base` 12): inner containers
- Large (YAML `lg` 20): course cards — observed 20px
- XL (YAML `xl` 24): membership plan cards — observed 24px
- Pill (YAML `pill` 9999): YAML field. Observed pill CTA 22px and search field 21px are not that YAML number

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow (`none`) | Homepage hero, nav, most sections |
| Tint (Level 1) | `#f6f6f8` / `#f8ecff` background shift | Section separation without elevation |
| Card (Level 2) | `rgba(0,0,0,0.08) 0px 2px 14px` | Membership / content plan cards |
| Elevated (Level 3) | `rgba(0,0,0,0.2) 0px 4px 24px` | Recommended (lavender) plan card |

YAML `shadow.card` is `rgba(0,0,0,0.08) 0px 2px 14px`. YAML `shadow.elevated` is `rgba(0,0,0,0.2) 0px 4px 24px`. YAML `shadow.none` is `none`.

Treating Codeit as mostly flat, treating the marketing homepage as effectively shadowless with separation from color bands and rounded shapes, treating elevation as introduced on the subscription page only, and treating emphasis as carried by color far more than by shadow, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Source §6 records that shadow philosophy.

### Motion

Source §15 names three durations. Treating those durations as source-stated named tokens rather than per-component computed observations, and treating the easing role names and signature-motion sentences as source §15 rules rather than live transition captures, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, chip press, focus |
| `motion-standard` | 220ms | Card / section reveal, sheet, dropdown |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Easing role names and uses from source §15, without promoting an exact curve:

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, sheets, chips |
| `ease-exit` | Dismissals |
| `ease-standard` | Two-way transitions |

Treating the exact curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` as omitted unattributed unnamed values in Named gaps rather than as promoted motion tokens is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Exact curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are omitted as unattributed. They are unnamed values in Named gaps, not promoted motion tokens.

Source §15 motion rules: motion is friendly but restrained; purple CTAs and pill chips respond to press with a subtle scale / opacity shift; course and plan cards fade-in from below at `motion-standard / ease-enter`; gamified surfaces (level-up, challenges) may use a single celebratory pop on milestone completion, while routine navigation stays calm. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional. Treating those sentences as source-stated motion rules rather than computed per-component evidence is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

Do not promote an easing curve, animation name, transition property, or a duration as computed per-component evidence until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-inspect-Pretendard-hero-and-SpoqaHanSansNeo-elsewhere and declared-only-Apple-SD-Gothic-Neo-and-Noto-Sans-KR-fallbacks-not-promoted-as-the-brand-face, are a derived editorial implementation inference from the verified surfaces; they are not Codeit-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Homepage hero H1 “5분마다 인생이 바뀐다” computes as Pretendard 68px / 800 / `-1.5px` in ink `rgb(51,50,54)`. Section H2s 48px / 32px weight 700; card H3 28px / 700; body SpoqaHanSansNeo 16px `rgb(51,50,54)`. YAML `family.display` is Pretendard; YAML `family.text` is SpoqaHanSansNeo. |
| Declared-only fallbacks — not promoted | Body copy names `Apple SD Gothic Neo` and `Noto Sans KR` as fallbacks for SpoqaHanSansNeo. They remain fallback declarations rather than UI-family claims. |

### Family

- **Display:** `Pretendard` — hero H1 ExtraBold (800) only
- **Text:** `SpoqaHanSansNeo` (with `Apple SD Gothic Neo`, `Noto Sans KR` fallbacks) — section titles, card heads, navigation, buttons, and body. Bold (700) for headings, Regular (400) for body, Medium (500) for nav and buttons

Treating Pretendard as hero-only rather than a substitute for SpoqaHanSansNeo, treating `Apple SD Gothic Neo` and `Noto Sans KR` as fallbacks rather than the UI family, treating a system-face replacement of either observed family on these captured surfaces as forbidden, and treating setting the 68px hero in SpoqaHanSansNeo as forbidden, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Do not replace Pretendard or SpoqaHanSansNeo with a system face on these captured surfaces. Do not set the 68px hero in SpoqaHanSansNeo.

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1.21`, `1.29`, `1.38`, `1.43`, `1.67`, and `1.50`. Treating those ratios as scaling with font size rather than fixed px, treating 82px / 62px / 44px / 40px / 30px as size-local observations rather than replacements for the ratios, treating YAML tracking `-1.5` / `-1.0` / `-0.3` as unmerged from body-table `-1.5px` / `-1.0px` / `-0.3px` and from source `-1px` at 48px, treating body/button/nav `normal` tracking as a size-local observation rather than invented YAML tracking, and treating hero CTA 18px / 700 as a component field rather than a replacement for the YAML button type-role 16px / 500, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking (YAML / table) | YAML use |
|---|---|---:|---:|---:|---|---|---|
| Hero | Pretendard | 68px (4.25rem) | 800 | 1.21 | 82px | -1.5 / -1.5px | Hero H1, Pretendard ExtraBold (5분마다 인생이 바뀐다) |
| Display | SpoqaHanSansNeo | 48px (3.00rem) | 700 | 1.29 | 62px | -1.0 / -1.0px | Large section titles, SpoqaHanSansNeo Bold |
| Section | SpoqaHanSansNeo | 32px (2.00rem) | 700 | 1.38 | 44px | -0.3 / -0.3px | Section headings |
| Card Title | SpoqaHanSansNeo | 28px (1.75rem) | 700 | 1.43 | 40px | -0.3 / -0.3px | Card / feature heads (H3) |
| Subhead | SpoqaHanSansNeo | 18px (1.13rem) | 700 | 1.67 | 30px | -0.3 / -0.3px | Small headings, testimonial titles |
| Body | SpoqaHanSansNeo | 16px (1.00rem) | 400 | 1.50 | — | — / normal | Standard reading text, SpoqaHanSansNeo |
| Button | SpoqaHanSansNeo | 16px | 500 | 1.50 | — | — / normal | Standard CTA label |
| Nav | SpoqaHanSansNeo | 13px (0.81rem) | 500 | 1.50 | — | — / normal | Nav links and chips |

Treating 68 × 1.21 ≈ 82, 48 × 1.29 ≈ 62, 32 × 1.38 ≈ 44, 28 × 1.43 ≈ 40, and 18 × 1.67 ≈ 30 as showing that the body-table px figures are ratio products at the captured sizes, treating those px figures as not replacements for the YAML ratios, and treating hero CTA 18px / 700 SpoqaHanSansNeo as a component field rather than a replacement for this YAML button row, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. 68 × 1.21 ≈ 82, 48 × 1.29 ≈ 62, 32 × 1.38 ≈ 44, 28 × 1.43 ≈ 40, 18 × 1.67 ≈ 30. Do not replace the YAML ratios with those px figures. Hero CTA 18px / 700 SpoqaHanSansNeo is a component field; it is not a replacement for this YAML button row.

Source typography principles — Hero is Pretendard, the rest is Spoqa; bold display, regular body; tracking tightens with size (`-1.5px` at 68px, `-1px` at 48px, `-0.3px` on mid headings); hangul-comfortable body at 16px / 1.50 — are restated from source §3. Treating those four stems as source §3 principles rather than a separately published Codeit type specification is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Catalog logo metadata is a Google favicon lookup, not a captured first-party mark.

Treating feature illustrations and course thumbnails as keeping their rounded (20–24px) corners at all sizes, and not replacing them with invented brand-color decoration, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Feature illustrations and course thumbnails keep their rounded (20–24px) corners at all sizes.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, and treating YAML `components_harvested: true` as a harvest flag rather than complete state coverage, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

YAML `components_harvested` is true. Treating the 2026-06-26 inspect notes as default computed styles on the four surfaces, and treating hover, focus, pressed, and other interaction paints as omitted from those notes rather than as a complete interaction contract, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. The 2026-06-26 inspect notes record default computed styles on the four surfaces. Hover, focus, pressed, and other interaction paints for the harvested controls stay omitted from those notes.

Source §14 state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| Empty (no course results) | White canvas. Single Ink (`#333236`) line explaining no matching courses, with one purple CTA to adjust filters. No clutter. |
| Empty (no enrolled courses) | Muted ink line inviting the learner to explore the catalog, plus a purple “모든 강의” path. Encouraging, never scolding. |
| Loading (catalog fetch) | Skeleton course cards at final dimensions on white, 20px radius, flat pulse consistent with the mostly-shadowless system. |
| Loading (AI GURU answer) | Inline typing indicator within the assistant panel; prior context stays visible. |
| Error (request failed) | Inline message in Ink (`#333236`) with a plain-language explanation and a retry — no bare “오류가 발생했습니다”. |
| Error (form validation) | Field-level message below the input describing what’s valid, not just “필수”. |
| Success (enrollment / subscription) | Brief inline confirmation in a calm tone; next-step (“학습 시작하기”) linked immediately below. No celebratory emoji spam. |
| Skeleton | `#f6f6f8` blocks at final dimensions, rounded corners, flat pulse. |
| Disabled | Reduced-opacity surface and text; purple actions fade rather than turn grey to preserve the brand read. |

Treating YAML `primary-deep` `#760dde` as a color role for active / pressed link text, not `focus-visible` treatment evidence, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. YAML `primary-deep` `#760dde` is a color role for active / pressed link text, not `focus-visible` treatment evidence.

Treating those §14 rows as source-stated state copy rather than live interaction captures, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, treating YAML `type: card` as having no interactive-kind confirmation for a §4.4 map, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source never records `focus-visible`; that applicability stays, and the visual treatment is omitted. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. Membership Plan Card, Recommended Plan Card, and Course Card have YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. This is not a complete state-coverage claim.

### Hero Primary CTA

- Role: hero / page primary CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#9933ff`
- Text: `#ffffff`
- Radius: 10px
- Padding: 10px 32px
- Height: 48px
- Font: 18px SpoqaHanSansNeo weight 700
- YAML font: `18px / 700 SpoqaHanSansNeo`
- Use: Hero primary CTA — 무료 체험 시작하기 / 멤버십 시작하기
- Observed: default computed style on homepage and subscription
- Surface: homepage, subscription

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the hero / page primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary CTA can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this hero / page primary CTA because exact destination, request, and outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a hero / page primary CTA (“무료 체험 시작하기”, “멤버십 시작하기”); exact destination, request, and outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Membership Chip (Nav)

- Role: compact purple chip in the top nav
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#9933ff`
- Text: `#ffffff`
- Radius: 6px
- Padding: 6px 12px 5px
- Height: 32px
- Font: 13px SpoqaHanSansNeo weight 500
- Use: Nav membership chip — 멤버십 안내
- Observed: default computed style on homepage nav
- Surface: homepage
- Field note: Treating the following as a source §9-only local recipe on this chip — white header, 13px SpoqaHanSansNeo weight 500 links in `#333236`, a purple `#9933ff` membership chip (6px radius, 6px 12px padding) right-aligned — is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Top nav: white header, 13px SpoqaHanSansNeo weight 500 links in `#333236`, a purple `#9933ff` membership chip (6px radius, 6px 12px padding) right-aligned.

Treating this nav membership chip as a destination/info action, so loading, error, and success are not-applicable on the control itself, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the nav membership chip |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav chip can be unavailable; visual treatment omitted |
| loading | not-applicable | A “멤버십 안내” chip is a destination/info action; the control itself does not enter a loading state |
| error | not-applicable | Chip meaning is the destination action, not a request or validation failure on the control |
| success | not-applicable | Following the chip is not an action-outcome confirmation on the control |

### Pill CTA

- Role: rounded inline CTA inside feature sections
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#9933ff`
- Text: `#ffffff`
- Radius: 22px
- Padding: 8px 16px
- Height: 43px
- Font: 16px SpoqaHanSansNeo weight 500
- Use: Rounded inline CTA — 지원 기기
- Observed: default computed style in feature sections
- Surface: homepage feature sections

Treating this pill CTA as a destination/info action, so loading, error, and success are not-applicable on the control itself, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the rounded inline CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A pill CTA can be unavailable; visual treatment omitted |
| loading | not-applicable | A “지원 기기” pill is a destination/info action; the control itself does not enter a loading state |
| error | not-applicable | Pill meaning is the destination action, not a request or validation failure on the control |
| success | not-applicable | Following the pill is not an action-outcome confirmation on the control |

### Ghost / Outline

- Role: secondary outline CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Text: `#333236`
- Radius: 8px
- Padding: 8px 24px
- Height: 45px
- Border: 1px solid rgba(51,50,54,0.2)
- Font: 16px SpoqaHanSansNeo weight 500
- Use: Outline secondary CTA — 모든 IT 강의 보러가기 / 전체 후기 보기
- Observed: default computed style
- Surface: homepage

Treating this outline CTA as a destination action, so loading, error, and success are not-applicable on the control itself, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the outline secondary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An outline CTA can be unavailable; visual treatment omitted |
| loading | not-applicable | An “모든 IT 강의 보러가기” outline is a destination action; the control itself does not enter a loading state |
| error | not-applicable | Outline-action meaning is the destination action, not a request or validation failure on the control |
| success | not-applicable | Following the outline action is not an action-outcome confirmation on the control |

### Neutral (Surface)

- Role: full-width neutral action on grey surface
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#f6f6f8`
- Text: `#333236`
- Radius: 8px
- Padding: 14px 0px
- Height: 55px
- Font: 16px SpoqaHanSansNeo weight 400
- Use: Neutral full-width action — 이메일로 로그인하기
- Observed: default computed style on sign-in
- Surface: sign-in

Treating this email-login control as a sign-in submit so loading and error remain applicable on the control, and treating success-paint mapping as omitted because whether confirmation paints on this control or navigation follows is unresolved, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the email-login action on sign-in |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A login action can be unavailable; visual treatment omitted |
| loading | applicable | An email-login control can wait on a request; visual treatment omitted |
| error | applicable | A login / form control can fail; visual treatment omitted |

Treating success applicability as omitted on this email-login control because whether a success paint belongs on this control or navigation follows is unresolved, rather than closing that field from the §14 enrollment/subscription row, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Success applicability is omitted. Source names this control as “이메일로 로그인하기”; whether a success paint belongs on this control or navigation follows is unresolved, so that field stays omitted at this boundary rather than closed from the §14 enrollment/subscription row.

### Course Search

- Role: pill-shaped search field on the explore page
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#f6f6f8`
- Text: `#333236`
- Radius: 21px
- Padding: 8px 16px
- Height: 40px
- Font: 14px SpoqaHanSansNeo
- Use: Course search field — 어떤 강의를 찾고 있나요?
- Observed: default computed style on explore
- Surface: explore

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the explore search field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A search field can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted |

Treating loading and success applicability as omitted on this explore search field because catalog-fetch loading and result-outcome confirmation are named on page-level §14 rows rather than as paints on this input, and exact request/outcome mapping to the input is unresolved, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Loading and success applicability are omitted. Catalog-fetch loading and result-outcome confirmation are named on page-level §14 rows, not as paints on this field, and exact request/outcome mapping to the input is unresolved.

### Membership Plan Card

- Role: standard membership plan card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 24px
- Border: 2px solid rgba(51,50,54,0.1)
- Shadow: `rgba(0,0,0,0.08) 0px 2px 14px`
- Use: Standard membership plan card
- Observed: default computed style on subscription
- Surface: subscription

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Recommended Plan Card

- Role: featured / recommended plan card
- Type: card
- Anatomy: surface
- Background: `#b363fd`
- Text: `#ffffff`
- Radius: 24px
- Shadow: `rgba(0,0,0,0.2) 0px 4px 24px`
- Use: Recommended / featured plan card
- Observed: default computed style on subscription
- Surface: subscription

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Course Card

- Role: course card on the explore grid
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 20px
- Border: 1px solid rgba(51,50,54,0.15) (hairline outline)
- Shadow: none (flat; no shadow)
- Use: Course card on the explore grid (flat, hairline outline, no shadow)
- Observed: default computed style on explore
- Surface: explore

Treating flat, hairline outline, and no shadow as this Course Card’s local fields rather than generic Elevation, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Count Badge

- Role: cart / notification counter
- Kind: non-interactive
- Type: badge
- Anatomy: count label
- Background: `#9933ff`
- Text: `#ffffff`
- Radius: 6px
- Padding: 0px 6px
- Height: 28px
- Font: 15px SpoqaHanSansNeo weight 500
- Use: Cart / notification counter
- Observed: default computed style on explore
- Surface: explore

Treating this count badge as a status marker rather than a control, so kind is non-interactive and a state-applicability map is omitted, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Kind is non-interactive: a count badge is a status marker, not a control. A state-applicability map is omitted.

### Navigation tab

- Role: top nav / explore category tabs
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#ffffff`
- Text: `#333236`
- Font: 13px SpoqaHanSansNeo weight 500
- Inactive: `#333236` at ~80% opacity
- Active: solid `#333236` label
- Use: Top nav / explore category tabs — 부트캠프, 기업교육, 커뮤니티, 수강 후기; explore category tabs
- Observed: default computed style; inactive/active as named
- Surface: homepage nav, explore

Treating this top nav / explore category tab as a panel-select control, so loading, error, and success are not-applicable on the tab itself, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as top nav / explore category tabs |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive tab; visual treatment omitted |
| disabled | applicable | A tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A tab selects a panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab selection is not a validation or request-failure state on the tab |
| success | not-applicable | Selecting a tab is not an action-outcome confirmation on the tab |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Treating YAML spacing numbers and the body 4px–48px scale as captured-surface figures rather than a cross-viewport specification, treating CTA 8px 24px / 10px 32px and card 24px 32px as component fields rather than a global gutter, and treating color-banded full-width sections as source §5 layout on the four live-inspect surfaces rather than uninspected lesson-layout chrome, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. YAML spacing is xs 4, sm 8, md 12, base 16, lg 20, xl 24, xxl 32, section 48. Body layout names a 4px base and 4px, 8px, 12px, 16px, 20px, 24px, 32px, 48px. Centered single-column hero is anchored by the 68px Pretendard headline. Feature sections alternate full-width white (`#ffffff`), cool-grey (`#f6f6f8`), and pale-violet (`#f8ecff`) bands. Subscription uses a 2–3 column plan-card grid with one lifted, color-filled recommended card. Explore uses a responsive course-card grid with a pill search field and text category tabs.

Treating the following as a source §9-only local recipe for the top nav — white header, 13px SpoqaHanSansNeo weight 500 links in `#333236`, a purple `#9933ff` membership chip (6px radius, 6px 12px padding) right-aligned — is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Top nav: white header, 13px SpoqaHanSansNeo weight 500 links in `#333236`, a purple `#9933ff` membership chip (6px radius, 6px 12px padding) right-aligned.

The following whitespace-and-rhythm readings — open-and-friendly-generous-vertical-rhythm, color-banded-segmentation-not-heavy-borders, and rounded-rhythm-from-repeated-20px-24px-cards-and-full-pill-chips — are a derived editorial implementation inference from the verified surfaces; they are not Codeit-authored or a separately published UI specification. Source §5 records that whitespace philosophy.

Treating source §8 Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` as source-stated layout rules rather than a captured cross-viewport pass, treating 48px / 45px–55px / 40px as desktop-named control heights rather than proven touch-target tokens from a mobile capture, and treating hero-scale-down / 3-up-to-stacked / horizontal-scroll-tabs as source §8 collapsing strategy rather than observed interaction expansion, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero compresses, plan cards stack |
| Tablet | 640-1024px | 2-up plan / course cards, moderate padding |
| Desktop | 1024-1440px | Full layout, centered hero, multi-column grids |

Touch-target sentences from source §8: Primary CTA at 48px height; outline / neutral buttons at 45px–55px height; pill search field at 40px height with 8px 16px padding; nav chips tappable within the header. Collapsing strategy: 68px Pretendard headline scales down on mobile, weight 800 maintained; plan-card grid 3-up → 2-up → stacked single column; course grid responsive wrap; category tabs become a horizontal scroll; color-banded sections maintain full-width treatment. Feature illustrations and course thumbnails keep rounded (20–24px) corners at all sizes; dark feature bands (`#080c14`, `#3d1457`) maintain full-width treatment.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating Codeit’s voice as warm, motivating, and plain-spoken — an encouraging coach who turns learning to code into five-minute steps — treating that register as a derived reading of the live headlines, treating the table below as observed guidance from live copy including Hero-headlines-aspirational-concrete, Feature-copy-benefit-first-anti-boredom, Product-AI-helpful-direct, CTAs-low-pressure-invitations, and Progress-encouraging-habit-framed, and treating the forbidden-register sentences as source §10 constraints rather than Codeit-authored doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Codeit-authored or a separately published UI specification. Source §10 records that voice reading. Copy samples below are verbatim from the live homepage and subscription page (verified 2026-06-26).

| Context | Tone |
|---|---|
| Hero headlines | Aspirational, concrete. “5분마다 인생이 바뀐다.” Bold promise, not hype. |
| Feature copy | Benefit-first, anti-boredom. “20분 이상 영상만 보는 지루한 학습은 그만.” |
| Product / AI features | Helpful, direct. “1초 만에 당신을 도와줄 AI GURU.” |
| CTAs | Low-pressure invitations. “무료 체험 시작하기”, “멤버십 시작하기”, “모든 IT 강의 보러가기”. |
| Progress / gamification | Encouraging, habit-framed. “페이스 메이커”, “레벨업 시스템”, “월별 챌린지”. |

Voice samples (verbatim from live surfaces, verified 2026-06-26):

- “5분마다 인생이 바뀐다” — hero H1 and page title
- “20분 이상 영상만 보는 지루한 학습은 그만” — feature heading
- “전문가 20인의 노하우로 탄생한 가장 완벽한 5분” — section heading
- “1초 만에 당신을 도와줄 AI GURU” — AI feature heading
- “코드잇 멤버십 구독” — subscription page H1

Forbidden register from source §10: fear-based “you’re falling behind” urgency, undefined jargon left unexplained, hype-heavy superlatives, and any tone that makes a beginner feel judged.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- exact easing curves `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)`
- hover, focus, pressed, and `focus-visible` visual treatments on the harvested controls
- loading, error, and success visual treatments on Hero Primary CTA; success paint on Neutral email-login; loading and success paints on Course Search
- `sprint.codeit.kr`, `tech.codeit.kr`, and `about.codeit.com` as token surfaces
- founding chronology and other corporate facts beyond the inspected surfaces
- motion transition properties, animation names, and per-component computed duration / easing / reduced-motion behavior — promote those as computed evidence only after per-component capture of all five: transition properties, animation name, duration, easing, and reduced-motion behavior
