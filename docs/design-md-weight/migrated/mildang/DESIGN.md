# Milddang (I Hate Flying Bugs) Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Milddang (밀당) is the AI-driven 1:1 tutoring product of the Korean edu-tech company I Hate Flying Bugs (IHFB). Catalog identity is `name: Milddang (I Hate Flying Bugs)`, `display_name_kr: 밀당 (아이헤이트플라잉버그스)`. Catalog homepage identity is `https://www.ihateflyingbugs.com/`. This contract covers the two first-party surfaces the source inspected live on 2026-07-02: the corporate home at `https://www.ihateflyingbugs.com/` and the Mildang PT product page at `https://www.ihateflyingbugs.com/mildang-pt-en/`. The IHFB R&D team blog at `https://medium.com/mildang` is a named narrative source ("밀당PT와 스쿨PT를 개발하는 IHFB R&D팀"); it does not supply the computed interface tokens below. Every value stays attached to the surface that established it. Reading those two inspected routes as this contract's token surfaces, keeping the Medium blog as a named narrative source that does not supply computed interface tokens, and keeping every value attached to the surface that established it, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

The captured layer is a white canvas (`#ffffff`) with near-black text (`#333333` body, `#111111` headings and nav) rather than pure black, and a single typeface — **Pretendard** — used at every level. The organizing idea is **one product, one color**. IHFB runs two education lines and codes each with a saturated hue: the private-education flagship **Mildang PT** owns teal (`#00b29d`), while the public-education **School PT** line owns periwinkle blue (`#555dfa`). Across the corporate home these appear as large, evenly-sized product-entry cards, so the color itself becomes the wayfinding. A magenta accent (`#cc3366`) carries links and corporate emphasis; Newsroom entries fall back to a light grey card (`#efeff1`). On the Mildang PT product surface the teal returns as the accent throughout, backed by two tinted-teal wash surfaces (`#e5f7f5` and `#dff1f1`). Live inspection found `box-shadow: none` across nav, hero, product cards, headings, and segment cards on both surfaces. Geometry: 8px radius on the home entry cards, and 40px pills/rounded cards on the Mildang PT page. The hex values, the family name, `box-shadow: none`, and the 8px / 40px radii are the source's own. Readings of that layer as a calm editorial education brand rather than a loud cram-school ad, of the atmosphere as trustworthy and quietly optimistic, of product-as-color wayfinding, of restraint with depth as a deliberately flat, shadowless system, and of a clean mobile-native education-first aesthetic that feels credible and calm rather than promotional, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. Milddang (밀당) is the flagship product of **I Hate Flying Bugs (IHFB)**, a Korean edu-tech company whose mission — stated plainly on its home — is "High-Quality Education for Equal Opportunity." The company builds two education lines: **Mildang PT**, an AI-based 1:1 personalized tutoring service for private education, and **School PT**, its counterpart aimed at public education. The founding premise reframes tutoring quality — historically gated by geography and household income — as something that software-assisted personalization can distribute more equally. The product's core is **AI-based 1:1 personalized tutoring**: the site addresses each stakeholder in the learning loop directly — students, lecturers, parents, and "Ontact" (online-contact) teachers — signaling that the product is a coordinated system, not a single app. The IHFB R&D team documents this work publicly on its team blog ("밀당PT와 스쿨PT를 개발하는 IHFB R&D팀"), reinforcing an engineering-led, transparent posture. What Milddang refuses, visible in its design: the loud, urgency-driven chrome of the Korean private-tutoring market (no countdowns, no fear-selling, no grade-guarantee banners). What it embraces: a flat, shadowless, education-first interface; a calm single typeface (Pretendard); and a disciplined **one-product-one-color** palette (Mildang teal `#00b29d`, School blue `#555dfa`) that makes the two lines legible at a glance while a warm magenta (`#cc3366`) carries the corporate voice. The mission line, the two product lines, the founding-premise sentence, the stakeholder list including "Ontact" (online-contact) teachers, the blog line, and the refuses/embraces closing are the source's own narrative facts; they do not by themselves supply interface tokens. The source's closing note records that broader company facts beyond the live sites are general public knowledge, not quoted from a single verified statement that turn, and that interpretive claims such as "one product, one color" and "calm over urgency as a rejection of cram-market chrome" are editorial readings connecting observed design to the stated mission, not directly sourced Milddang statements. Classifying that narrative as context that does not by itself supply interface tokens, keeping the founding-premise sentence and each paragraph's last sentence as one unit with the paragraphs they close, and treating the refuses/embraces pairing, the engineering-led transparent posture, and "one product, one color" as those editorial readings, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, classifying them as surface-or-control outcomes rather than fictional biographies, and recording that they do not come from the source's persona section, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification. Each names a surface or control the source records.

- Enter Mildang PT, School PT, or Newsroom from the corporate-home product-entry cards.
- Read the Mildang PT product surface — Students, Lecturers, Parents, and Ontact Teachers segment cards, and the Teal pill CTA.
- Use corporate nav ("Who we are", "What we do", "Newsroom") and the KOR / ESP language toggle.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source's persona section states in its own header, and again in its closing note, that its archetypes are fictional and that the names are illustrative; those biographies are not carried here and are not re-hosted in the sidecar. What the source independently records on the captured Mildang PT page and in §11, in its own wording, is the audience at a group level: students, lecturers, parents, and "Ontact" (online-contact) teachers. Dropping the fictional biographies rather than promoting them, carrying no name, age, city, motivation, or affiliation classification, and reading those source-named groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

### Distinctive traits

The eight traits below are the source's own Key Characteristics. The values in them are recorded; the groupings and the readings inside them are a derived editorial implementation inference from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

- Single typeface — Pretendard — at every level; Bold (700) for display/headings, Regular (400) for body
- Product-as-color wayfinding: Mildang PT teal (`#00b29d`) vs School PT blue (`#555dfa`)
- Magenta (`#cc3366`) reserved for links and corporate accent
- Near-black text (`#111111` headings, `#333333` body) instead of pure black — warm and trustworthy
- Flat, shadowless depth (`box-shadow: none`); separation by tinted teal (`#e5f7f5`, `#dff1f1`) and grey (`#efeff1`) surfaces
- Soft geometry: 8px entry cards, 40px pills/rounded feature cards, 50% circular avatars
- Slate ladder for text hierarchy: `#333333` body → `#494c4f` secondary → `#999999` muted/inactive
- Dark corporate footer band (`#2f3233`) anchors the parent-company identity (I Hate Flying Bugs Inc.)

### Principles

These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Milddang-authored or a separately published UI specification. The source states them in its own Principles section together with the UI implication it draws from each. The mission line in item 1 is first-party copy the source attributes to the home; every *UI implication* below is the source's own editorial reading.

1. **Equal opportunity is the product.** The mission ("High-Quality Education for Equal Opportunity") is the design brief. *UI implication:* keep the interface approachable and non-intimidating; never gate basic understanding behind jargon or paywalled chrome.
2. **One product, one color.** Each education line owns a hue. *UI implication:* reserve teal (`#00b29d`) for Mildang PT and blue (`#555dfa`) for School PT; never blend them, so the color itself is the wayfinding.
3. **Calm over urgency.** The brand rejects cram-market pressure tactics. *UI implication:* no countdowns, no fear-selling banners; give the education message editorial room to breathe.
4. **Flat and honest.** Depth is communicated by color, not decoration. *UI implication:* `box-shadow: none`; separate with tinted-teal and grey surfaces and a single dark corporate band.
5. **Speak to every learner in the loop.** Students, lecturers, parents, and teachers each get an addressed segment. *UI implication:* structure surfaces around audiences, not features; use rounded tinted-teal cards to make each segment feel welcoming.

### Application rules

The source states these eight as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

- Use Pretendard for everything — display, nav, and body; drive hierarchy with weight (700 vs 400)
- Reserve teal (`#00b29d`) as the Mildang PT action/brand color — keep it the product's single hue
- Use periwinkle blue (`#555dfa`) only for the School PT (public-education) sibling line
- Use magenta (`#cc3366`) for links and corporate accent, not as a fill
- Use near-black `#111111` for headings/nav and `#333333` for body instead of pure black
- Separate sections with flat tinted-teal (`#e5f7f5` / `#dff1f1`) and grey (`#efeff1`) surfaces, not shadows
- Keep geometry soft — 8px entry cards, 40px pills, 50% circular avatars
- Let color carry the wayfinding — one product, one color

### Avoid

The source states these seven as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

- Use drop shadows for elevation — Milddang is a flat, shadowless system
- Mix the two product colors — teal is Mildang PT, blue is School PT; keep the lines distinct
- Spread teal across unrelated UI — it dilutes the product signal
- Use pure black (`#000000`) for body text — reserve near-black `#333333` / `#111111`
- Swap fonts for headlines — Pretendard owns every level; change weight, not typeface
- Use magenta (`#cc3366`) as a large fill — it is a link/accent hue only
- Add hard-sell urgency chrome (banners, countdowns) — the brand voice is calm and education-first

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Pairing each hex to the token-set path named beside it, taking those role names from the source's own labels, keeping `tokens.colors.canvas` and `tokens.colors.on-primary` as two keys that happen to share `#ffffff`, keeping `tokens.colors.ink-pure` `#000000` unmerged from `tokens.colors.ink` `#111111`, attaching every role to the surface the source recorded, and the warmth / not-a-fill / product-signature readings the source attaches to those roles, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

Primary

- **Mildang Teal** (`#00b29d`): Primary brand color for the Mildang PT (private-education) line — the product-entry card background and the accent color throughout the Mildang PT surface. The system's core "this is Mildang" hue. Token-set path `tokens.colors.primary`. Catalog identity `primary_color` is `#00b29d`.
- **Teal Tint** (`#e5f7f5`): Lightest tinted-teal wash surface used for emphasis labels and feature blocks on Mildang PT. Token-set path `tokens.colors.primary-tint`.
- **Teal Tint Alt** (`#dff1f1`): A slightly deeper tinted-teal surface for rounded persona/segment cards. Token-set path `tokens.colors.primary-tint-alt`.

Accent & Sibling Line

- **Corporate Magenta** (`#cc3366`): Link color and corporate emphasis accent across the home (appears ~14× in the text-color scan). Not a fill color — a text/accent hue. Token-set path `tokens.colors.accent-magenta`.
- **School PT Blue** (`#555dfa`): The periwinkle blue owned by the sibling public-education line (School PT); appears as its product-entry card background. Token-set path `tokens.colors.accent-blue`.

Text / Ink

- **Ink** (`#111111`): Primary heading and nav color — a near-black used instead of pure black for warmth. Token-set path `tokens.colors.ink`.
- **Ink Pure** (`#000000`): Occasional maximum-contrast heading text (appears heavily in the fg scan on white sections). Token-set path `tokens.colors.ink-pure`.
- **Body** (`#333333`): The dominant body-text color (×138 in the fg scan) — standard reading text. Token-set path `tokens.colors.body`.
- **Slate** (`#494c4f`): Secondary text and captions. Token-set path `tokens.colors.slate`.
- **Muted** (`#999999`): Tertiary / inactive text (e.g. the inactive KOR / ESP language toggles). Token-set path `tokens.colors.muted`.

Neutral & Surface

- **Canvas** (`#ffffff`): Page background, white sections, and text color on teal/blue/dark surfaces. Token-set path `tokens.colors.canvas`.
- **Surface Grey** (`#efeff1`): Light grey card/section surface for neutral entries (Newsroom) and alternating blocks. Token-set path `tokens.colors.surface`.
- **Dark** (`#2f3233`): The dark corporate footer band background. Token-set path `tokens.colors.dark`.
- **On-Primary** (`#ffffff`): White text and headings placed on the teal, blue, and dark surfaces. Token-set path `tokens.colors.on-primary`.

`tokens.colors.canvas` and `tokens.colors.on-primary` both write `#ffffff`. They stay two keys. `tokens.colors.ink-pure: #000000` is not `tokens.colors.ink: #111111`.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept as the source wrote them): `tokens.spacing.xs: 4` · `tokens.spacing.sm: 8` · `tokens.spacing.md: 12` · `tokens.spacing.base: 16` · `tokens.spacing.lg: 20` · `tokens.spacing.xl: 28` · `tokens.spacing.xxl: 48` · `tokens.spacing.section: 64`.

Source §5 restates the same range as Base unit: ~4px and the scale `4px, 8px, 12px, 16px, 20px, 28px, 48px, 64px`. Notable recorded geometry, kept on the surfaces that established it rather than merged into those steps: entry-card padding lands at `20px 16px` (measured); nav items use a tall `30px` vertical padding for an 80px header hit area; persona cards on Mildang PT use a 28px inset with a large image gutter.

`tokens.spacing.xs: 4` is a spacing step. `tokens.spacing.md: 12` is a spacing step. `tokens.spacing.xxl: 48` is a spacing step. `tokens.spacing.lg: 20` is not the `20px` in entry-card padding. `tokens.spacing.base: 16` is not the `16px` in that padding and is not the 16px type size. `tokens.spacing.xl: 28` is not the persona-card 28px inset. `tokens.spacing.sm: 8` is not `tokens.rounded.md: 8`. `tokens.spacing.section: 64` stays its own key. Keeping those unitless spacing steps on their own keys rather than rewriting them as a converted px sheet, and keeping those writings of `20px 16px` / `30px` / `80px` / `28px` on their own records, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path): `tokens.rounded.sm: 3` · `tokens.rounded.md: 8` · `tokens.rounded.lg: 40` · `tokens.rounded.full: 9999`.

Source §5 names the same four steps with their uses. YAML writes the full step as `9999`; §5 spells it `50% / 9999px`. Both writings stay.

- Small (`3` / `3px`): fine inner elements. Token-set key `tokens.rounded.sm`.
- Medium (`8` / `8px`): the home product-entry cards — the workhorse. Token-set key `tokens.rounded.md`. YAML entry-card records also write `radius: "8px"`.
- Large (`40` / `40px`): pills and rounded feature/persona cards on Mildang PT. Token-set key `tokens.rounded.lg`. YAML `cta-pill` / `badge-teal` / `card-tinted` also write `radius: "40px"`.
- Full (`9999` / `50%` / `9999px`): circular avatars and round icons. Token-set key `tokens.rounded.full`.

`tokens.rounded.sm: 3` is not a spacing step. `tokens.rounded.md: 8` is not `tokens.spacing.sm: 8`. `tokens.rounded.lg: 40` is not a spacing step and is not only the CTA radius written as a scale. `tokens.rounded.full: 9999` stays the unitless full step; `50%` stays the §5 circular-avatar spelling. Keeping `3`, `8`, `40`, and `9999` as four keys, and keeping `50%` beside `9999` rather than choosing one as a replacement, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Tint (Level 1) | Teal tint (`#e5f7f5` / `#dff1f1`) or grey (`#efeff1`) background shift | Card / section separation without elevation |
| Color (Level 2) | Saturated product fill (`#00b29d` / `#555dfa`) | Product-entry cards that must read as tappable |
| Dark (Level 3) | Dark band (`#2f3233`) | Corporate footer anchor |

YAML `tokens.shadow.none` is `"none"`. Live inspection found `box-shadow: none` across the nav, hero, product cards, headings, and Mildang PT segment cards on both surfaces. Depth and grouping are communicated entirely through flat color — tinted teal and grey wash surfaces, saturated product fills, and a single dark corporate band. The source's Shadow Philosophy records this as a deliberate modern-flat choice that keeps the education UI feeling clean, fast, and approachable; when emphasis is needed the system reaches for its product color (`#00b29d`), never elevation. The four-level table, the `none` token, and `box-shadow: none` on both surfaces are the source's own. Reading that shadow-free color as a deliberate modern-flat choice, and reading emphasis as a reach for product color rather than elevation, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

### Motion

The source attributes its token-level claims (§1–9) to a live inspection of computed color, type, geometry, and shadow on two surfaces. The motion contract below sits outside that attribution: the sibling verification file records no transition, animation, duration, or easing observation on either surface. The durations, easing roles, signature motions, and motion rules below are therefore a derived editorial implementation inference from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

Durations:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, card/pill press, focus |
| `motion-standard` | 220ms | Card / section reveal, sheet, dropdown |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Easing roles — three roles with declared uses. The specific curve values the source lists are not traceable to Milddang evidence, so the curves are omitted here and only the roles and their uses are kept. Treating those curves as untraceable and omitting them, rather than promoting them as Milddang motion tokens, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, sheets, segment blocks |
| `ease-exit` | Dismissals |
| `ease-standard` | Two-way transitions |

Motion rules, as the source states them:

- Motion is functional and quiet — consistent with the flat, calm, education-first aesthetic.
- Product-entry cards respond to press with a subtle scale/opacity shift; segment blocks fade-in from below at `motion-standard / ease-enter`.
- No bounce or spring — an education brand signals steadiness and focus, not playfulness.
- Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or specification document — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, and refusing a partial confirmation, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, treating Pretendard as the only promoted UI family, treating `sans-serif` as a fallback list rather than a brand face, and refusing to substitute a system font while calling it Pretendard, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | The corporate home and Mildang PT page use Pretendard for headlines, nav, and body alike. No separately published Milddang type specimen or exclusive distributed family is named in the source. |
| Live computed surface-use | YAML `tokens.typography.family.sans` writes `Pretendard`. Source §3: `Pretendard`, with `sans-serif` fallback — used for headlines, nav, and body alike. There is no display/body split; hierarchy comes entirely from size and weight. |
| Official distributed asset | No Milddang-exclusive distributed type family was verified. |
| Declared-only | None named in the source. |
| License | No font-license sentence is recorded in the source. |
| Outside these captures | Faces on surfaces other than the corporate home and Mildang PT page stay outside this contract. |

### Family

- **Sans (only family):** `Pretendard` — Token-set path `tokens.typography.family.sans`. Used for headlines, nav, and body alike.
- Fallback list recorded by the source: `sans-serif`.
- Do not substitute a system face for Pretendard and present it as the brand type. Do not introduce a second family for headlines. That fallback prohibition, and keeping Pretendard as the only promoted UI family, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

### Type roles

Token-set `use` strings are kept verbatim. The longer §3 spellings sit beside them; neither was chosen as a replacement. YAML `lineHeight` stays the unitless ratio the YAML recorded (`1.40`, `1.50`); parenthetical rem and px figures are the source §3 spelling. YAML `heading.tracking` is `-0.2`; the §3 table writes `-0.2px`. Display, nav, and body have no YAML tracking field; the §3 table records `normal` at those captured sizes. That `normal` is not invented YAML tracking. Keeping the YAML `use` strings verbatim, keeping the YAML singles and the §3 longer spellings on separate readings, and refusing to flatten a unitless ratio into a replacement px, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking (YAML / §3) | Token-set use | §3 notes |
|---|---|---:|---:|---:|---|---|---|
| Display / H1 | Pretendard | 32px (2.00rem) | 700 | 1.40 (44.8–48px) | — / normal | Hero / page H1 headline, Pretendard Bold | Hero + page headlines |
| Heading / H2·H3 | Pretendard | 20px (1.25rem) | 700 | 1.40 (28px) | -0.2 / -0.2px | Section / card H2·H3, Pretendard Bold | Section / card heads |
| Nav | Pretendard | 16px (1.00rem) | 600 | 1.50 | — / normal | Top-nav item, Pretendard SemiBold | Top-navigation items |
| Body | Pretendard | 16px (1.00rem) | 400 | 1.50 (24px) | — / normal | Standard reading text, Pretendard Regular | Standard reading text |

Token-set paths: `tokens.typography.display` · `tokens.typography.heading` · `tokens.typography.nav` · `tokens.typography.body`. YAML sizes are unitless: `tokens.typography.display.size: 32` · `tokens.typography.heading.size: 20` · `tokens.typography.nav.size: 16` · `tokens.typography.body.size: 16`. `tokens.typography.display.size: 32` is not a spacing step. `tokens.typography.heading.size: 20` is not `tokens.spacing.lg: 20`. `tokens.typography.nav.size: 16` and `tokens.typography.body.size: 16` are type sizes; they are not `tokens.spacing.base: 16`. `tokens.typography.heading.tracking: -0.2` is the YAML tracking field. `tokens.spacing.md: 12` is not a type size. `tokens.spacing.xs: 4` is not a radius. `tokens.spacing.xxl: 48` is not a type size.

### Typography principles

The four rules below are the source's own. Their reasoning — what a weight or a tracking value is *for* — is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

- **One typeface, weight-driven hierarchy**: Pretendard carries everything. Contrast comes from Bold (700) for display/headings vs Regular (400) for body — never a font swap.
- **Bold display, calm body**: Headlines run at 700; running text stays a quiet 400 at 16px with a comfortable 1.5 line-height for dense hangul legibility.
- **Slight negative tracking on section heads**: 20px headings carry `-0.2px` letter-spacing; display and body stay at normal tracking.
- **Near-black, not black, for text**: Headings/nav sit at `#111111` and body at `#333333`; pure black `#000000` is reserved for maximum-contrast moments.

### Assets

- Catalog identity records `logo.type: favicon` and `logo.slug: https://www.google.com/s2/favicons?domain=ihateflyingbugs.com&sz=128`. That is a third-party favicon proxy URL held as catalog identity metadata; it is not a first-party distributed Milddang brand asset, and it is not promoted to one here.
- Source-recorded document titles: "I Hate Flying Bugs Inc." (corporate home) / "Mildang PT - I Hate Flying Bugs" (Mildang PT page).
- Illustrations and product screenshots carry no shadow at any size, consistent with the flat system.

Classifying the Google s2 slug as an identity pointer rather than a hosted brand file, keeping those document titles as recorded strings rather than as a mark library, and reading "no shadow at any size" as consistent with the flat system, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| **Empty (no assigned lessons)** | White canvas. Single Ink (`#111111`) line at body size explaining nothing is assigned yet, with one teal (`#00b29d`) pill CTA to start. No illustration clutter. |
| **Empty (saved / history none yet)** | Muted (`#999999`) single line: nothing here yet, plus a calm path back. Honest, non-pressuring. |
| **Loading (content fetch)** | Skeleton blocks on a tinted-teal (`#e5f7f5`) or grey (`#efeff1`) surface at final dimensions, 8px radius. Flat pulse — no shadow shimmer, consistent with the shadowless system. |
| **Loading (session compute)** | Inline progress within the card; previous values stay visible. |
| **Error (fetch/session failed)** | Inline message in Ink (`#111111`) with a plain-language explanation and a retry. No bare "오류가 발생했습니다" — always states the next step. |
| **Error (form validation)** | Field-level message below the input; describes what is valid, not just "필수". |
| **Success (lesson / action complete)** | Brief inline confirmation in a calm tone with teal (`#00b29d`) accent; next-step detail linked below. No celebratory emoji. |
| **Skeleton** | Tinted-teal (`#dff1f1`) or grey (`#efeff1`) blocks at final dimensions, 8px / 40px radius, flat pulse. |
| **Disabled** | Muted (`#999999`) text on a reduced-opacity surface; teal actions fade rather than turn grey, to preserve the brand read. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. `not-applicable` is used only where the control's role makes the state meaningless — a destination product-entry card or a destination nav item that commits no operation in place — and the reason given is always that semantic one. A `Primitive type` line is attached only when the source YAML records that type on that component. Where the source supplies no interaction evidence for a container or marker, its kind and applicability map are omitted rather than decided. The source records no `focus-visible` capture and no generic Focus row; no `focus-visible` row carries a treatment. Preserving the source state contract here rather than delegating it to an unadopted catalog graph, the role-based decision procedure above, every interactive-kind verdict, every applicability verdict, the reason given for either, omitting kind and a map where interactive-kind is unconfirmed, and the refusal to treat this as a complete state-coverage claim, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification. This is not a complete state-coverage claim.

### Teal CTA Pill

- Role: Primary action pill on Mildang PT surfaces — teal is the single action color
- Primitive type: `button` · Kind: interactive
- Background: `#00b29d`
- Text: `#ffffff`
- Radius: 40px
- Font: 16px / 700 / Pretendard
- Token-set type: `tokens.components.cta-pill.type` `button` (YAML `type: button`)
- Token-set bg: `tokens.components.cta-pill.bg` `#00b29d`
- Token-set fg: `tokens.components.cta-pill.fg` `#ffffff`
- Token-set radius: `tokens.components.cta-pill.radius` `40px`
- Token-set font: `tokens.components.cta-pill.font` `16px / 700 Pretendard`
- Token-set use: `Teal pill CTA — teal is the single action color`
- Recorded use: Primary action pill on Mildang PT surfaces — teal is the single action color
- The `40px` radius is this control's radius; it is not `tokens.rounded.lg: 40` written as a spacing step. The `16px / 700` font is this control's font; it is not only the Body or Display type-role row. YAML font writes `16px / 700 Pretendard`; §4 writes `16px / 700 / Pretendard`. Both writings stay. Reading those figures as this control's geometry rather than as those YAML scale steps is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | Button control; the surface contract records muted `#999999` / teal fade; visual treatment omitted on this token-set record |
| loading | applicable | A primary action pill commits an action; in-progress treatment at this control is omitted |
| error | applicable | Same commit role; failure treatment at this control is omitted |
| success | applicable | Same commit role; the surface contract records a calm teal confirmation, not this control's success treatment |

### Mildang PT Entry Card

- Role: Private-education (Mildang PT) product-entry card on the corporate home grid
- Primitive type: `card` · Kind: interactive
- Background: `#00b29d`
- Text: `#ffffff`
- Radius: 8px
- Padding: 20px 16px
- Token-set type: `tokens.components.card-mildang.type` `card` (YAML `type: card`)
- Token-set bg: `tokens.components.card-mildang.bg` `#00b29d`
- Token-set fg: `tokens.components.card-mildang.fg` `#ffffff`
- Token-set radius: `tokens.components.card-mildang.radius` `8px`
- Token-set padding: `tokens.components.card-mildang.padding` `20px 16px`
- Token-set use: `Mildang PT entry card — teal, the private-education flagship line`
- Recorded use: Private-education (Mildang PT) product-entry card on the corporate home grid
- Source §6 Color (Level 2) records product-entry cards that must read as tappable. The `8px` radius is this card's radius; it is not `tokens.spacing.sm: 8`. The `20px 16px` padding is this card's padding; it is not `tokens.spacing.lg: 20` or `tokens.spacing.base: 16`. Reading those figures as this card's geometry, and treating this card as a destination tile that presents Mildang PT rather than as an in-place commit control, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web tappable card; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination card whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This product-entry card presents a destination; it does not commit an operation whose pending result this card would report. |
| error | not-applicable | Same role reason: choosing the Mildang PT destination is not an operation with an error result this card would report. |
| success | not-applicable | Same role reason: reaching that destination is not an operation this card reports as success. |

### School PT Entry Card

- Role: Public-education (School PT) product-entry card — sibling line, periwinkle blue
- Primitive type: `card` · Kind: interactive
- Background: `#555dfa`
- Text: `#ffffff`
- Radius: 8px
- Padding: 20px 16px
- Token-set type: `tokens.components.card-school.type` `card` (YAML `type: card`)
- Token-set bg: `tokens.components.card-school.bg` `#555dfa`
- Token-set fg: `tokens.components.card-school.fg` `#ffffff`
- Token-set radius: `tokens.components.card-school.radius` `8px`
- Token-set padding: `tokens.components.card-school.padding` `20px 16px`
- Token-set use: `School PT entry card — periwinkle blue, the public-education line`
- Recorded use: Public-education (School PT) product-entry card — sibling line, periwinkle blue
- The `8px` radius and `20px 16px` padding are this card's geometry; they are not those YAML spacing steps. Reading those figures as this card's geometry, and treating this card as a destination tile that presents School PT rather than as an in-place commit control, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web tappable card; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination card whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This product-entry card presents a destination; it does not commit an operation whose pending result this card would report. |
| error | not-applicable | Same role reason: choosing the School PT destination is not an operation with an error result this card would report. |
| success | not-applicable | Same role reason: reaching that destination is not an operation this card reports as success. |

### Neutral Entry Card

- Role: Newsroom / neutral entry card on the grey surface
- Primitive type: `card` · Kind: interactive
- Background: `#efeff1`
- Text: `#111111`
- Radius: 8px
- Padding: 20px 16px
- Token-set type: `tokens.components.card-neutral.type` `card` (YAML `type: card`)
- Token-set bg: `tokens.components.card-neutral.bg` `#efeff1`
- Token-set fg: `tokens.components.card-neutral.fg` `#111111`
- Token-set radius: `tokens.components.card-neutral.radius` `8px`
- Token-set padding: `tokens.components.card-neutral.padding` `20px 16px`
- Token-set use: `Newsroom / neutral entry card on grey surface`
- Recorded use: Newsroom / neutral entry card on the grey surface
- The `8px` radius and `20px 16px` padding are this card's geometry; they are not those YAML spacing steps. Reading those figures as this card's geometry, and treating this card as a destination tile that presents Newsroom rather than as an in-place commit control, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web tappable card; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination card whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This product-entry card presents a destination; it does not commit an operation whose pending result this card would report. |
| error | not-applicable | Same role reason: choosing the Newsroom destination is not an operation with an error result this card would report. |
| success | not-applicable | Same role reason: reaching that destination is not an operation this card reports as success. |

### Tinted-Teal Segment Card

- Role: Rounded persona/segment card (Students / Lecturers / Parents) on the Mildang PT page
- Primitive type: `card`
- Background: `#dff1f1`
- Text: `#333333`
- Radius: 40px
- Token-set type: `tokens.components.card-tinted.type` `card` (YAML `type: card`)
- Token-set bg: `tokens.components.card-tinted.bg` `#dff1f1`
- Token-set fg: `tokens.components.card-tinted.fg` `#333333`
- Token-set radius: `tokens.components.card-tinted.radius` `40px`
- Token-set use: `Rounded tinted-teal persona/segment card on the Mildang PT page`
- Recorded use: Rounded persona/segment card (Students / Lecturers / Parents) on the Mildang PT page
- Source §5 also names the same cards as Students, Lecturers, Parents, Ontact Teachers at ~240px tall, with a 28px inset and a large image gutter. YAML records no padding on this component. The `40px` radius is this card's radius; it is not `tokens.rounded.lg: 40` written as a spacing step. The ~240px height and 28px inset are this card's geometry; they are not `tokens.spacing.xl: 28`. The source supplies no interaction evidence for this card, so kind and a state-applicability map are both withheld. Reading those figures as this card's geometry, keeping the YAML use string, the §4 Students / Lecturers / Parents spelling, and the §5 Ontact Teachers spelling on their own records, and omitting kind and a map rather than inventing them, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

### Teal Emphasis Label

- Role: Tinted-teal emphasis label / tag on Mildang PT surfaces
- Primitive type: `badge`
- Background: `#e5f7f5`
- Text: `#00b29d`
- Radius: 40px
- Font: 16px / 700 / Pretendard
- Token-set type: `tokens.components.badge-teal.type` `badge` (YAML `type: badge`)
- Token-set bg: `tokens.components.badge-teal.bg` `#e5f7f5`
- Token-set fg: `tokens.components.badge-teal.fg` `#00b29d`
- Token-set radius: `tokens.components.badge-teal.radius` `40px`
- Token-set font: `tokens.components.badge-teal.font` `16px / 700 Pretendard`
- Token-set use: `Tinted-teal emphasis label on Mildang PT surfaces`
- Recorded use: Tinted-teal emphasis label / tag on Mildang PT surfaces
- The `40px` radius and `16px / 700` font are this label's geometry. The source supplies no interaction evidence for this badge, so kind and a state-applicability map are both withheld. Reading those figures as this label's geometry, and omitting kind and a map rather than inventing them, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

### Top Nav Item

- Role: Corporate nav ("Who we are", "What we do", "Newsroom"); the inactive language toggle (KOR / ESP) sits at `#999999`
- Primitive type: `tab` · Kind: interactive
- Text: `#111111`
- Font: 16px / 600 / Pretendard
- Padding: 30px 0px
- Active: magenta `#cc3366` link/accent
- Token-set type: `tokens.components.nav-link.type` `tab` (YAML `type: tab`)
- Token-set fg: `tokens.components.nav-link.fg` `#111111`
- Token-set font: `tokens.components.nav-link.font` `16px / 600 Pretendard`
- Token-set active: `tokens.components.nav-link.active` `magenta #cc3366 text`
- Token-set use: `Top-nav item; inactive language toggle sits at #999999`
- Recorded use: Corporate nav ("Who we are", "What we do", "Newsroom"); the inactive language toggle (KOR / ESP) sits at `#999999`
- YAML records no padding; §4 writes `Padding: 30px 0px`. Both writings stay — the padding is a §4 field, not a YAML key. The `30px` vertical padding is this nav item's padding; it is not a spacing step. The `16px / 600` font is this control's font. Reading those figures as this nav item's geometry, keeping the YAML use string beside the §4 nav labels and KOR / ESP spelling, and treating this control as a destination nav item rather than as an in-place commit control, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above; active is the recorded magenta `#cc3366` variant |
| hover | applicable | Pointer-web nav item; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A destination nav item whose availability can lapse; the inactive language toggle sits at `#999999`; visual treatment omitted on this token-set record beyond that recorded inactive color |
| loading | not-applicable | This control selects a destination; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | A nav item that only selects a destination does not report a failed request on itself |
| success | not-applicable | Same role reason: reaching a nav destination is not an operation this item reports as success |

### Dark Corporate Band

- Role: Dark footer band carrying the parent-company (I Hate Flying Bugs Inc.) info and links
- Primitive type: `listItem`
- Background: `#2f3233`
- Text: `#ffffff`
- Font: 16px / 400 / Pretendard
- Token-set type: `tokens.components.footer-band.type` `listItem` (YAML `type: listItem`)
- Token-set bg: `tokens.components.footer-band.bg` `#2f3233`
- Token-set fg: `tokens.components.footer-band.fg` `#ffffff`
- Token-set font: `tokens.components.footer-band.font` `16px / 400 Pretendard`
- Token-set use: `Dark corporate footer band (I Hate Flying Bugs Inc. info)`
- Recorded use: Dark footer band carrying the parent-company (I Hate Flying Bugs Inc.) info and links
- The `16px / 400` font is this band's font. The source supplies no interaction evidence for this band as a control, so kind and a state-applicability map are both withheld. Reading that font as this band's geometry, and omitting kind and a map rather than inventing them, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Corporate home: a full-width grey hero, then an evenly-sized product-entry card grid (Mildang PT / School PT / Newsroom), each card ~385px wide × 160px tall. Mildang PT page: centered editorial column with rounded persona/segment cards (Students, Lecturers, Parents, Ontact Teachers) at ~240px tall. Alternating white (`#ffffff`) and tinted-teal (`#e5f7f5` / `#dff1f1`) / grey (`#efeff1`) full-width bands. A dark corporate band (`#2f3233`) closes the page.

Source §9 also writes the hero as a full-width grey band, H1 in Pretendard 32px weight 700, line-height 1.4, color white on image (or `#111111` on white), body 16px Pretendard weight 400, `#333333`. The live-inspect comment records corporate hero H1 "High-Quality Education for Equal Opportunity" — Pretendard 32px / 700 / white on hero. Both the white-on-image / `#111111`-on-white pairing and the white-on-hero observation stay. Keeping those two writings together rather than choosing one as a replacement is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

Whitespace philosophy, as the source states it:

- **Editorial calm over density**: generous vertical rhythm between sections; the education message is given room to breathe rather than crammed.
- **Flat segmentation**: sections separate by background color (teal tint vs grey vs white), never by shadow or heavy borders.
- **Color as structure**: the product colors (teal, blue) do the wayfinding work that a busier site would hand to iconography or dividers.

### Breakpoints

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, product cards stack, hero headline compresses |
| Tablet | 640-1024px | 2-up product cards, moderate padding |
| Desktop | 1024-1440px | Full layout, 3-up product-entry grid, centered editorial column |

### Touch targets

- Product-entry cards at ~160px tall with 20px 16px padding — large, unambiguous tap targets
- Nav items with 30px vertical padding create an ~80px header band
- Rounded 40px pills give a generous, tappable hit area on Mildang PT

### Collapsing strategy

- Product-entry grid: 3-up → 2-up → single-column stacked
- Persona/segment cards: side-by-side → stacked full-width
- Tinted/white/grey alternating bands maintain full-width treatment
- Hero: 32px headline scales down on mobile, weight 700 maintained

### Image behavior

- Illustrations and product screenshots carry no shadow at any size, consistent with the flat system
- Circular avatars (50%) and rounded cards (40px / 8px) hold their radius across breakpoints

`tokens.spacing.lg: 20` is not the entry-card padding `20px`. `tokens.spacing.base: 16` is not the `16px` side of that padding. `~385px` / `160px` / `~240px` / `80px` / `30px` are recorded measurements of these two surfaces. Reading the three named ranges, the collapsing strategy, the touch-target figures, and the whitespace philosophy as recorded measurements and source-stated register of these two surfaces rather than as a specification invented on top of them, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Milddang's voice is **calm, credible, and mission-framed** — an education brand that treats teaching quality as a public good rather than a sales pitch. The corporate line "High-Quality Education for Equal Opportunity" sets the register: earnest, purposeful, and free of cram-market urgency. Copy speaks to students, parents, and teachers as partners in learning, and it decodes the product (AI-based 1:1 personalized tutoring) in plain terms rather than hyping the technology. Calling that register calm, credible, and mission-framed, and refusing to treat the mission line as a complete product-microcopy guide, are derived editorial implementation inferences from the verified surfaces; they are not Milddang-authored or a separately published UI specification.

| Context | Tone |
|---|---|
| Corporate mission | Purpose-driven, plain. "High-Quality Education for Equal Opportunity." Never superlative. |
| Product headlines | Descriptive and concrete. "High-Quality Personalized…", "AI-based 1:1 Personalized…". Explains, doesn't boast. |
| Segment sections | Audience-addressed, calm. "Students", "Lecturers", "Parents", "Ontact Teachers" — each spoken to directly. |
| Nav / wayfinding | Functional and human. "Who we are", "What we do", "Newsroom". |
| Trust / company | Steady and transparent — corporate identity (I Hate Flying Bugs Inc.) stated plainly in the footer. |

**Voice samples (verbatim from live surfaces):**

- "High-Quality Education for Equal Opportunity" — corporate hero headline (mission-framed). *(verified live 2026-07-02)*
- "High-Quality Personalized…" — Mildang PT product H1 (product promise). *(verified live 2026-07-02)*
- "AI-based 1:1 Personalized…" — Mildang PT feature heading (what the product is). *(verified live 2026-07-02)*

**Forbidden register**: cram-school fear-selling, countdown/urgency pressure, undefined AI buzzwords left unexplained, exclamation-heavy hype, grade-guarantee claims.

Nav labels, kept as published strings: "Who we are", "What we do", "Newsroom". Language-toggle labels, kept as published strings: KOR / ESP. Segment labels, kept as published strings: "Students", "Lecturers", "Parents", "Ontact Teachers". Error copy from the state contract: no bare "오류가 발생했습니다" — always states the next step; form validation describes what is valid, not just "필수".

Classifying those three lines as verbatim live-surface samples rather than as a synthetic voice pack, and keeping KOR / ESP as captured toggle labels rather than as a locale-profile claim, is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Reading the list as a catalog of unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Milddang-authored or a separately published UI specification.

- exact cubic-bezier values for `ease-enter` / `ease-exit` / `ease-standard` (unattributed; names kept)
- getdesign.md / refero records for mildang
