# iicombined migration log

Source: `web/references/iicombined/DESIGN.md`
Sibling read (not the migration input): `web/references/iicombined/.verification.md`
Destination: `docs/design-md-weight/migrated/iicombined/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/iicombined/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `6b3a7c05c812df0b36e6c8ecdb47276a56822a23de6b72ecc8606af9f7b8333d` (`web/references/iicombined/DESIGN.md`). Sibling SHA-256 `be0d5eeca680d6ab47ea6efdf0fbc5ddd9cb73c260edced8340859a0986f8f10` (`web/references/iicombined/.verification.md`). Worker-close portable DESIGN SHA-256 `a9db3b64a25e5c1b023baa59175c9512418725291f0f52b1fc9fd3a7fd9da7fb`. Provenance SHA-256 `f14d8fcc334c9ee0f164f37427c0ac43245397d52e9d3163775cf0c9fcf6bf16`. Auditor-close hashes are recorded in `audit-log.md` if F3 runs.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# IICOMBINED Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–23. Homepage `https://www.gentlemonster.com` as its own URL is dual: `DESIGN.md` dest 4 at 9/21/238 (9×2) + `provenance.md` 14/46/51/72 (E2a). `#111111` is dual: `DESIGN.md` dest 19 at 11/35/39/49/58/63/76/89/101/137/239/264/311/335/346/366/387/391 + `provenance.md` 15/61/63 (E2a). Favicon slug is dual: `DESIGN.md` 218 + `provenance.md` 17 (E2a). `아이아이컴바인드` is dual: `DESIGN.md` 9/13/457 + `provenance.md` 11. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 19–23, 33–36. The source footer's **Verified:** 2026-06-17 is at `provenance.md` 38. `live-extract` DESIGN 0 / provenance 1 (`provenance.md` 21). `components_harvested` DESIGN 0 / provenance 2 (`provenance.md` 23 / 211). These are ledger keys with no portable slot. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations; 분리 → provenance | Full note transcribed at `provenance.md` 63. The hex and family facts it names (`#111111`; `#1d1d1d`; Gentle Monster Serif; GentleSans 350 / 400; Pretendard) land in `DESIGN.md` 9–11/89–90/187–189. Dual (E2a). |
| YAML `tokens.colors` (12 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 89–110. All twelve roles kept with names, values, and token-set keys: primary, ink-alt, ink-pure, canvas, surface, surface-alt, on-dark, muted, muted-deep, muted-mid, navy-accent, alert. `#ffffff` as `canvas` is the page/tile role (`DESIGN.md` 91); `#ffffff` as `on-dark` stays a separate key (`DESIGN.md` 105). `#f3f4f6` is `tokens.colors.surface` (`DESIGN.md` 96). `#f2f4f5` is `tokens.colors.surface-alt` (`DESIGN.md` 97). |
| YAML `tokens.typography.family.display` / `family.sans` / `family.sans-alt` | 옮김 → Typography & Assets Family | Paths at `DESIGN.md` **187–189**. Live computed `DESIGN.md` **179**. Declared-only Times-class fallback at **181**. Family qualifier at **190** covers the three-key unmerge and the fallback prohibition. |
| YAML `tokens.typography.campaign-serif / section-heading / subhead / product-title / body / nav / meta / caption` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 196–203. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.17` dest 2 · `1.00` dest 5 · `1.57` dest 2 · `1.50` dest 5 · `1.38` dest 2 · `1.42` dest 2. Campaign-serif tracking `0` at 196/205. All eight YAML `use` strings restored verbatim in the Token-set use column (A1, A3). Paths `tokens.typography.campaign-serif` … `tokens.typography.caption` dest 1 each at 196–203. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) / `tokens.shadow.none` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing paths: `DESIGN.md` 114 (`tokens.spacing.xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 23` · `xl: 32` · `xxl: 48` · `section: 64`). Shape paths: `DESIGN.md` 118 (`tokens.rounded.sm: 8` · `md: 25` · `lg: 45` · `full: 9999`). `tokens.rounded.full: 9999` dest 3 (`DESIGN.md` 118/125/521). Visible `9999px` dest 8 (`DESIGN.md` 11/38/62/125/291/407/521). `tokens.rounded.lg: 45` dest 4 is not pill height `45px` (`DESIGN.md` 127/294). `tokens.spacing.sm: 8` is disambiguated from `tokens.rounded.sm: 8` at 114/127. `tokens.spacing.lg: 23` is disambiguated from `tokens.rounded.md: 25` at 114. `tokens.shadow.none` dest 1 at `DESIGN.md` 137. There is no `tokens.rounded.none` key — sharp 0px is a component writing (`DESIGN.md` 118/122/336/347). Tall `90px` header from §4/§5 dest 3 (`DESIGN.md` 318/405/419) + provenance (A3, E2a). |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | `DESIGN.md` 234–369. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×3 (237/263/286 = YAML outline + solid + pill), `tab` ×1 (310), `card` ×2 (333/344), `badge` ×1 (355), `listItem` ×1 (365). All eight YAML `use` strings restored verbatim as `Token-set use:` lines (247/270/295/316/337/348/360/369). YAML `font` shorthands kept as `token-set font record` lines: `16px / 400 GentleSans` dest 2 · `13px / 400 GentleSans` dest 1 · `10px / 400 Pretendard` dest 1 · `16px / 350 GentleSans` dest 1 · `13px / 350 GentleSans` dest 1. YAML `border` shorthands `1px solid #ffffff` dest 1 (241) and `1px solid #000000` dest 1 (290). YAML `active` `white #ffffff label when over dark hero imagery` dest 1 at 315. YAML `product-card.fg` `#111111` is the Product / Campaign Tile `Text` row at 335 (same hex as surface-card Text at 346 and footer Text at 366 — not ink or nav; the key path is the tile Text row). YAML `surface-card.fg` `#111111` at 346. No §4-only component borrowed a `Primitive type` — `not in the token set` dest 0 because every named component has a YAML row. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 35–42. The atmosphere readings (installation-grade gallery / receding UI / magazine-cover / whispered / gallery-grade) carry an adjacent complete qualifier at 11 (B2/B2a). The house/surface assignment in ¶1 is named in the qualifier at 9. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 89–110. Characterizing phrases covered by the adjacent qualifier at 85 (B2/B2a). Canvas / on-dark key-unmerge at 105 has its own adjacent complete qualifier. |
| §3 Typography Rules — family, hierarchy, principles | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 177–183; family 187–190; hierarchy table 196–203; type rules 209–214. The Official-product-use, Official-distributed-asset, Declared-only, License, Outside, Family fallback-plus-three-key-unmerge, Type-roles ratio, and Type-rules readings carry adjacent complete qualifiers at 178, 180, 181, 182, 183, 190, 205, and 214 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 234–369. §4 body values and YAML `use` / font / radius / height forms are both kept. Outlined CTA padding `0px 23px` at 243. Solid consent height 48px at 269. Tamburins pill height 45px at 294. Nav active white over dark hero at 314–315. Product tile Text `#111111` at 335. Overlay font `13px / 350 GentleSans` at 359. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 33–36; Tier 1 list 51–52; Tier 2 56–57. `https://www.gentlemonster.com` is dual: `DESIGN.md` dest 4 at 9/21/238 + `provenance.md` 14/46/51/72 (E2a). `https://www.tamburins.com` is dual: `DESIGN.md` 9/24/287 + `provenance.md` 47/52/84 (E2a). Conflicts unresolved: none — `provenance.md` 40. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 402–407 (full-bleed hero, 12px caption, tinted/white bands, 90px header, spacing 4/8/12/16/23/32/48/64, shape 8/25/45/9999) and 409 (imagery-over-chrome / flat-segmentation / gallery-rhythm). 36px CTA height at 246/419. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 131–137: three-level table, `tokens.shadow.none`. The gallery-grade / conventional-retail reading carries an adjacent qualifier at 137 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 58–65 (eight Do rules), under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 71–77, under the qualifier at 69. The seven prohibitions are the source's own list, including `#000000` as the forbidden large-text color at 76. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 411–432: Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` at 415–417, 36px / 45px / 90px targets at 419, collapsing strategy 421–426, image behavior 428–432. The Desktop row keeps the source body's `1024-1440px` range at 417. The system-level-not-cross-viewport reading carries an adjacent qualifier at 411 (B2/B2a). No "does not measure 1440px" sentence is added (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The deletion row is `provenance.md` 162; the itemised check is `provenance.md` 164. |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings `DESIGN.md` 455–478; tone table 439–445; forbidden register 480. The qualifier at 437 covers the voice reading and the tone-table contract (B2/B2a, A5). Voice-sample parentheticals have their own adjacent complete qualifier at 453. The forbidden-register close at 480 has its own adjacent complete qualifier. Byte-exact / gloss-beside close at 482. Source English glosses (`Buy`, `View campaign`, `a new hair-perfume collection`) sit beside the Korean lines and do not replace them. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: Hankook Kim (김한국), Gentle Monster eyewear launched 2011, Tamburins fragrance and body care, Nudake "make a new dream". Refuse/embrace pairing in the same paragraph, including saturated badge-heavy urgency, chromatic palettes that fight photography, decorative depth that would feel like a shop rather than a space, achromatic black-and-white, custom typography as identity, full-bleed imagery as the primary medium, and restraint that signals art over merchandising — the source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 213. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 48–52 under the B2a form at 46: "These five items are a derived editorial implementation inference from the verified surfaces; they are not IICOMBINED-authored or a separately published UI specification." IICOMBINED has no separately published first-party UI specification in the source, so the toss-form applies (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 172–206 (35 rows). |
| §13 Personas — 3 entries | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or motivation appears in either output (D2, D2a). Disposition at `provenance.md` 158. Audience at `DESIGN.md` 29 carries only the group-level description the source independently records (design-aware fashion buyers, fragrance enthusiasts, art-and-retail followers), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at `DESIGN.md` 387–395 (A2; the catalog graph is still 0/440, so nothing is delegated). The applicability rule is at 226–232, whose qualifier at 232 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Outlined CTA, Tamburins Outlined Pill, Top Nav Item, and Footer / Menu Link close loading/error/success with a role reason (C2 v10) — they commit no operation in place (`loading \| not-applicable` dest 4 · `success \| applicable` dest 1, that one being Solid Consent). Solid Consent opens loading/error/success because it commits a consent action. Overlay Label declares `kind: non-interactive` with a reason instead of a map (Core §4.4) — `Kind: non-interactive` dest 1. The two cards get no `kind` and no map because the source supplies no interaction evidence for the containers — `DESIGN.md` 339/350 (C4). |
| §15 Motion & Easing — durations, easing roles, motion rules, reduced-motion | 옮김 → Foundations motion | `DESIGN.md` 147–149 (150ms / 280ms / 500ms with uses), 155–157 (ease-enter / ease-exit / ease-standard roles and uses, curves omitted), 163–166 (cinematic but disciplined, campaign cross-fade at `motion-slow / ease-enter`, no spring or overshoot, `prefers-reduced-motion: reduce` collapses to instant and freezes ambient campaign motion). Source phrase `consumer-app` dest 1 at 165 — kept as the source's own motion sentence, not as a native-application scope claim. |
| §15 Motion & Easing — three curve values | 삭제 → provenance omission ledger | `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are named as omitted at `DESIGN.md` 151 and are not promoted as tokens. Durations stay. Ledger at `provenance.md` 159–161. B3 is held: the promotion condition at `DESIGN.md` 159 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 518. |
| Opening product / atmosphere sentence | 옮김 → Experience Scope | `DESIGN.md` 9–11: Korean creative house; two inspected surfaces; `#ffffff` canvas; reserved near-black ink; custom serif + whisper-weight sans. |

## Sibling handling (`web/references/iicombined/.verification.md`)

The sibling exists — confirmed with `find web/references/iicombined -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 71–90 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: overlay label `전체보기`; H3 `SCRUNCHIE SUMMER TAILS`; fallback face `Sandoll GothicNeo1 Md`; consent `letter-spacing: 0.13px`; computed family tokens `__gentleSansRegularKo` / `__gentleSansLightKo` / `__gentleMonsterSerif`; frequency counts; extra background `rgb(191,191,191)`; Tamburins weight 600 ×3; house-domain favicon fetch note. Recorded at `provenance.md` 94–103.
- Measured `DESIGN.md` 0 for those sibling-only strings: `전체보기` 0 · `SCRUNCHIE SUMMER TAILS` 0 · `Sandoll GothicNeo1 Md` 0 · `0.13px` 0.
- `구매하기` / `캠페인 보기` / `2026 Veggie Collection` / `모두 수락 - ACCEPT ALL COOKIES` / `토피 02` / `SUMMER TAILS` / `새로운 헤어 퍼퓸 컬렉션` are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. IICOMBINED is Korean-first on labels, so a hand sweep of published labels is mandatory even when the needle set is non-empty.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 22 distinct | 0 | 0 | 아이아이컴바인드 / 구매하기 / 캠페인 보기 / ACCEPT ALL COOKIES / 모두 수락 - ACCEPT ALL COOKIES / 2026 Veggie Collection / BOLD COLLECTION / SUMMER TAILS / 토피 02 / 베르 02 / 아덴 02 / 새로운 헤어 퍼퓸 컬렉션 / 선글라스 / 안경 / 베스트셀러 / 2026 컬렉션 / 선물 / Home \| 젠틀몬스터 공식 온라인 스토어 / TAMBURINS 탬버린즈 공식 온라인 스토어 / make a new dream / 김한국 / Hankook Kim. |
| `node test-v2/tools/latin-copy-audit.mjs --brand iicombined` | 0 lost / 1 scanned | 0 | 0 | `scanned: 1`, `clean: 1`, `withLoss: 0`. English glosses that sit beside Korean lines (`Buy`, `View campaign`, `a new hair-perfume collection`) are kept as glosses, not replacements (A5). YAML use `Primary CTA over imagery — 구매하기 / 캠페인 보기, transparent fill, hairline pill` is a token-set use string, not a rewritten CTA. |
| Sibling published strings | 2 distinct sibling-only | 0 | 0 | `전체보기` and `SCRUNCHIE SUMMER TAILS` survive in provenance; none are promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 아이아이컴바인드, 구매하기, 캠페인 보기, 2026 Veggie Collection, 새로운 헤어 퍼퓸 컬렉션, 선글라스, 2026 컬렉션, 2011, Hankook Kim, 김한국, make a new dream, fragrance and body care, shop rather than a space, art over merchandising.

A5 분모: hand sweep of source published labels 22 extracted / 0 missing; latin-copy-audit 0 lost / 1 scanned; gate `copy-loss` compared 10 / candidates 185. A5a was mandatory because `compared` 10 < `candidates` 185.

§13 identifiers were extracted in the hand sweep and dropped under D2a — they are not published copy and are not listed here as names.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand iicombined --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 10, candidates: 185 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `format: core-v2`, `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 10 < `candidates` 185.

## Deviations recorded

- `DESIGN.md` is 6,633 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: eight declared component records, a nine-row surface state contract, a seven-state applicability matrix on five interactive controls, `tokens.spacing.sm: 8` kept off `tokens.rounded.sm: 8`, `tokens.rounded.lg: 45` kept off pill height 45px, `tokens.colors.canvas` kept off `tokens.colors.on-dark`, the full §11 founding-and-installation narrative including its last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- IICOMBINED has no separately published first-party UI specification in the source, so every derived-editorial close uses the toss-form `not IICOMBINED-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Worker-close measure: 33 = 33. Auditor-close measure: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 35 = `not IICOMBINED-authored` 35 = `separately published UI specification` 35. Provenance derived ledger is 35 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

DESIGN.md was re-read from line 1 after the body was complete. Every causal, interpretive, or judging sentence — Scope ¶1 surface-assignment, Scope ¶2 atmosphere including expressive/near-silent split and chrome-reduces-to-hairline, Scope ¶3 narrative-not-token, Primary-tasks selection, Audience grouping, Distinctive-traits grouping, Principles, Application rules, Avoid, Semantic characterizations, canvas/on-dark unmerge, Spacing/Shape key unmerge including missing-`none`-as-component-writing, Elevation gallery-grade reading, Motion attribution including curve-omission, each Font evidence class, Family fallback and three-key unmerge, Type-roles ratios, Type rules, Assets favicon/photography, Components how-to-read, Top Nav 90px-not-a-YAML-field (318), State-record attribution, State non-attachment, Layout whitespace, Layout responsive, Content voice, Voice-sample parentheticals (453), Forbidden register, byte-exact close, Recorded unresolved — has an adjacent complete B2a form (derived editorial implementation inference / not IICOMBINED-authored or a separately published UI specification). Hex values, published Korean strings, YAML `use` strings, and the §11 year / Hankook Kim / 김한국 / 2011 / make a new dream / fragrance and body care / last-sentence facts are source records and were not qualified as if they were editorial.

## F2 E2 check (mandatory final pass)

Each log row's destination was grepped in the named file before the row was written. Dual destinations (homepage URL, `#111111`, favicon slug, `아이아이컴바인드`, token note hex facts, tamburins URL) name both files. Compliance claims used only where the full text is present: B3 five kinds at `DESIGN.md` 159 and 518; B2a complete form 35 = 35; C2 role-closed L/E/S as counted above; D2a omission ledger names field kinds, not identifiers. Auditor dest corrections are in `audit-log.md`.
