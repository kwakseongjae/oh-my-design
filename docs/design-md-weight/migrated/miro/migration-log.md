# Miro migration log

- Source: `web/references/miro/DESIGN.md` (legacy, omd 0.1) — 수정하지 않음
- Sibling: `web/references/miro/.verification.md` — **채택**(증거 등급). 상세는 provenance `Sibling handling`.
- Destination: `docs/design-md-weight/migrated/miro/DESIGN.md`
- Provenance: `docs/design-md-weight/migrated/miro/provenance.md`
- 규칙집: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
- 대응표: `docs/design-md-weight/2026-08-22-essence-verdict.md` / 제약: `docs/reviews/t1-2-essence-2026-08-23-rereview.md` 27–33행 / 의미 조건: `docs/reviews/t1-3-golden-2026-08-23-sol-review.md` §5
- 형태 모범: `docs/design-md-weight/golden-samples/musinsa/`
- 도메인: design-tools (US collaborative canvas). 토큰 표면은 공개 마케팅 홈 `https://miro.com/ko/` + 공개 가격 `https://miro.com/ko/pricing/`. YAML `ds.type: system` — 발행 1차 DS = Mirotone (`https://www.mirotone.xyz`). sibling 전용 측정(dialog `460px × 764px`, RGB 표기, `roobertPROLocal` 298 uses, 2025 newsroom URL)은 portable 토큰으로 올리지 않았다.

`provenance.md`의 `Claim ledger`가 모든 값의 원본 위치와 portable 목적지를 색인한다. 아래 표의 각 행은 **portable 목적지**와, 색인을 넘어 provenance가 값 자체를 보관하는 경우의 **추가 목적지**를 함께 적는다(E2a). 각 행은 산출 파일을 `grep`한 뒤에 썼다(F2).

Source SHA-256 `e1cf082fcd6c5d9860c1238be564030e3c12de7079f4468f243a80703e474827` (`web/references/miro/DESIGN.md`). Sibling SHA-256 `daf0bcda764c1c2a175a5945a78a67fb6b6e14e78f88e06513799048c954cb1b` (`web/references/miro/.verification.md`).

## legacy 섹션별 처리

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope (catalog URL + inspected `ko` routes); `primary_color` 옮김 → Scope + Foundations + yellow promo; `logo.type` / `logo.slug` 옮김 → Typography & Assets Assets | Portable 파일에 frontmatter 없음. H1 is `# Miro Design System`. Identity table `provenance.md` Identity. `https://miro.com` stays in the identity table; inspected home `https://miro.com/ko/` is dual: `DESIGN.md` Scope dest **2** + `provenance.md` Surfaces (E2a). `#fde050` is dual: `DESIGN.md` dest **7** + `provenance.md` dest **2** (E2a). `logo.type: simpleicons` / `logo.slug: miro` is dual: `DESIGN.md` dest **1** each + `provenance.md` Identity (E2a). |
| YAML `omd: "0.1"`, `verified`, `verification_v2`, `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true`, `ds.name` / `ds.url` / `ds.type: system` / `ds.description` / `ds.og_image` | 분리 → provenance; Mirotone 이름·경계·`ds.description` 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. Exact `tokens.source` reconciled DESIGN dest **0** / provenance dest **1**. `components_harvested` DESIGN dest **0** / provenance dest **2**. `ds.type: system` DESIGN dest **1** (Scope writes the ledger key) + provenance dest **2** (E2a). `ds.description` string DESIGN dest **1** / provenance dest **1** (E2a). `ds.og_image` DESIGN dest **0** / provenance dest **3** (ledger-only key at Identity table + dual-destination paragraph + Omission ledger). Cover URL `https://www.mirotone.xyz/cover.png` DESIGN dest **0** / provenance dest **2**. Conflicts unresolved: none — Freshness. |
| YAML `tokens.note` | 옮김 → Experience Scope · 분리 → provenance Token note | Verbatim dual dest (E2a): "Machine tokens are limited to supplied public marketing/pricing capture. Mirotone, Aura, and documentation establish context only; they do not supply unobserved live variants." DESIGN dest **1** / provenance dest **1**. |
| YAML `tokens.colors` (9 keys) | 옮김 → Foundations semantic color | All nine roles kept with names, values, and token-set keys: brand-yellow `#fde050` dest **7**, action-blue `#3859ff` dest **6**, action-blue-border `#7a90fe` dest **5**, ink `#1c1c1e` dest **10**, canvas `#ffffff` dest **5**, border-input `#e9eaef` dest **3**, border-control `#c7cad5` dest **3**, border-subtle `#e0e2e8` dest **2**, muted `#555a6a` dest **3**. Paths `tokens.colors.brand-yellow` dest **1** · `action-blue` dest **2** · `action-blue-border` dest **1** · `ink` dest **1** · `canvas` dest **1** · `border-input` dest **1** · `border-control` dest **1** · `border-subtle` dest **1** · `muted` dest **1**. Qualifier at Semantic color (B2/B2a). Aura named accents stay narrative. |
| YAML `tokens.typography.family` (`ui` / `display`) | 옮김 → Typography & Assets Family | `Noto Sans` DESIGN dest **20**. `Roobert PRO Medium` DESIGN dest **11**. Paths `tokens.typography.family.ui` dest **1** · `display` dest **1**. The no-substitution rule and `roobertPROLocal` alias reading carry an adjacent complete qualifier (B2/B2a). |
| YAML `tokens.typography.display-hero` / `display-section` / `body` / `action` | 옮김 → Typography & Assets Type roles | Token-set table keeps unitless `56` / `55.2` / `20` / `24` and is never converted to a replacement px (A1a). Observed `56px` / `55.2px` / `24px` / `20px` stay on the hierarchy table. YAML `use` strings restored verbatim: `Observed public home display headline` dest **1** · `Observed public home section heading` dest **1** · `Observed public pricing control and list text` dest **1** · `Observed public pricing action` dest **1**. Tracking `-1.68` dest **4** beside `-1.68px`; `-1.44` dest **4** beside `-1.44px`. Yellow promo `16px / 900 / Roobert PRO Medium` dest **2** is that control's font, not `tokens.typography.action` (`16` / `600`). Qualifier at Type roles and at Type roles after tables (display-hero `lineHeight` `56` not a replacement for `size` `56`). |
| YAML `tokens.spacing` (`xs: 4`, `sm: 8`, `md: 12`, `lg: 16`, `xl: 24`, `xxl: 32`) | 옮김 → Foundations spacing | Compact line `tokens.spacing.xs: 4` dest **1** · `sm: 8` dest **1** · `md: 12` dest **1** · `lg: 16` dest **2** · `xl: 24` dest **1** · `xxl: 32` dest **1**. Px cluster `4, 8, 12, 16, 24, and 32px` kept as the source's own writing. `tokens.spacing.lg` dest **2** is a spacing step — not action size `16` and not email padding `16px`. Qualifier at Spacing. |
| YAML `tokens.rounded` (`control: 8`, `segmented: 40`) | 옮김 → Foundations shape (+ Components) | `tokens.rounded.control` dest **2** / provenance dest **1** (E2a). `tokens.rounded.segmented` dest **1**. Segmented `40` stays off yellow promo height `40px`. Local `50%` dest **4** is not a YAML key. Qualifier at Shape. |
| YAML `tokens.components.pricing-period-toggle` | 옮김 → Components & States Pricing-period toggle | Verified primitive type preserved on this component only, not flattened to `Kind` (A1b): `Primitive type: \`toggle\`` dest **1** = source YAML `type: toggle`. `Token-set use: \`Public pricing billing-period selector\`` dest **2**. Path `tokens.components.pricing-period-toggle` dest **1** (`type: toggle`, `radius: 40`, `height: 34`, `padding: 6px 16px`, `states: checked, unchecked`, `use: Public pricing billing-period selector`). The other five §4 records are `not in the token set` dest **6**. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope paragraphs 1–2; Distinctive traits restates Yellow as a signal / Canvas before chrome / Workroom energy. `collaborative canvas for teams that want to move from an early idea to a shared outcome` dest **1**. RealtimeBoard dest **2**. 2011 dest **2**. Andrey Khusid dest **2**. `yellow should be the protagonist rather than paint every surface` dest **1**. `broad-language body face` dest **1**. Surface-boundary reading and atmosphere reading carry adjacent complete Mirotone-named qualifiers (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Observed public-surface roles kept with source role names. Aura Coral, Cyan, Lilac, Lime, Sunshine, Orange, Pink, Moss, and Teal dest **2** as narrative context, not machine tokens. Legacy `#5b76fe` dest **1** kept as the source's already-removed value. Role-slotting qualifier at Semantic color (B2/B2a). |
| §3 Typography Rules — evidence classes, hierarchy | 옮김 → Typography & Assets | Evidence classes keep `882 visible uses` dest **1** · `24 font-source URLs` dest **1** · `46 visible uses` dest **1** · three Miro-hosted WOFF2 dest **1**. Declared-only Fragment Mono / Inter Placeholder / M PLUS 1 / Nanum Pen Script / Vazirmatn dest **2** each in DESIGN.md. Displaay foundry URL `https://displaay.net/typeface/roobert` DESIGN dest **1** / provenance dest **2** (E2a). Licence URL `https://displaay.net/help/licenses` DESIGN dest **1** / provenance dest **2** (E2a). Qualifiers at Font evidence, Family, Type roles, and Type roles after tables. |
| §4 Component Stylings | 옮김 → Components & States | Six records: Yellow promo action, Blue pricing action, Outlined pricing action, Email input, Pricing-period toggle, Pricing dialog. Selectors dual dest with `provenance.md` Capture selectors (E2a): `data-omd-capture="0"` dest **1** / P dest **1** · `"76"` dest **1** / P dest **1** · `"136"` dest **2** / P dest **1** · `"137"` dest **1** / P dest **1** · `"82"` dest **1** / P dest **1** · `"86"` dest **2** / P dest **1** · `"85"` dest **2** / P dest **1** · `dialog-0-8` dest **1** / P dest **1**. Outlined background stays `transparent` (sibling white fill is ledger-only). Dialog `oklch(1 0 0)` DESIGN dest **2** / provenance dest **1** (E2a). Shadow `rgba(0, 0, 0, 0.1) 0px 10px 15px -3px` DESIGN dest **2**. `no global modal token or further state contract is inferred` dest **1**. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; 검사 URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. Home / pricing / About / Aura / identity / Mirotone-docs / Displaay URLs: token surfaces dual Scope + provenance; narrative URLs dual Scope ¶3 + provenance Sources (E2a). `getdesign.md` DESIGN dest **0** / provenance dest **1**. `refero.design` DESIGN dest **0** / provenance dest **1**. Conflicts unresolved: none — Freshness only. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | Spacing cluster. Public action and email input `8px`. Pricing-period option `40px`. Circular `50%` locally, not a general-purpose radius token dest **4**. Qualifier at Layout. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `No global elevation scale is established` dest **1**. `local two-part black shadow` dest **1**. `public buttons are largely flat` dest **2** (Elevation + Named gaps). Qualifier at Elevation. |
| §7 Do's | 옮김 → Experience application rules | Four items, matching the source Do list, under the grouping qualifier. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | Four source Don'ts under the qualifier. `Don’t turn the observed yellow promo button into Miro’s universal primary action` dest **1**. `Don’t apply Mirotone app-library components to Miro marketing/pricing tokens without direct live evidence` dest **1**. No invented domain (`native application` DESIGN dest **0** / provenance dest **0**). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `desktop-only (\`1440x900\`)` dest **3**. `mobile breakpoint, layout transition, or responsive component variant` dest **1**. Unresolved items are omitted as values, not denied as invented measurements (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` §9 deletion check. |
| §10 Voice & Tone | 옮김 → Content & Locales | Voice paragraph dest; context table dest; samples byte-exact: `empower teams to create the next big thing` dest **1** · `canvas-first` dest **3** · `from idea to outcome` dest **1** · `Everyone should be able to collaborate … without barriers` dest **1** (A5). `No invented error copy or prohibition list is asserted` dest **1**. Qualifier at Content (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | 2011 RealtimeBoard dest **2**. Andrey Khusid dest **2**. `visual workspace where teams work with AI across strategy, product design, and innovation delivery` dest **1**. `yellow had always been iconic` dest **1**. `moderation and supporting colors rather than flood every interface` dest **1**. Last sentence kept as one unit: `lively brand expression belongs to marketing and board context, while only the supplied live public controls become implementation tokens` dest **1**. Marked as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §12 Principles — 4 numbered | 옮김 → Experience principles | Four stems + UI implications under the B2a form. `The canvas keeps work shared.` dest **1**. `Yellow is an intentional identity signal.` dest **1**. `Multilingual clarity is a product concern.` dest **1**. `Accessible collaboration is part of the experience.` dest **1**. Miro has a published first-party design system (Mirotone), so the close names that specification (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` `## Derived editorial inventory` (**25** data rows). |
| §13 Personas | 옮김 → Experience Audience (group wordings only) | Source header states first-party stakeholder groups, not fictional individual personas. Group labels dest **1** each: Cross-functional innovation teams · Multilingual, hybrid collaborators · People with access needs. `broad language support for distributed teams` dest **1**. `keyboard, screen-reader, voice-control, color, and motion-sensitive collaboration` dest **1**. No name, age, or city existed to delete (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Motivations are not rewritten as Primary tasks. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Source body preserved: Checked / Unchecked / Dialog open / Disabled 36px circular home control / Reduced motion. `No public evidence in this pass establishes a loading, empty, success, error, hover, pressed, or focus treatment` dest **1**. Disabled circular dest **2**; retained in the capture record only, not a component. Graph not used (A2). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Five interactive controls declare the seven canonical states. Destination promo/pricing actions and the toggle close loading/error/success with a role reason (C2): `loading \| not-applicable` dest **5**. `loading \| applicable` dest **0**. Email field: `error \| applicable` dest **1** · `success \| applicable` dest **1**. `default` and `focus-visible` stay applicable. Generic Focus is not treated as a `focus-visible` treatment (B1). Non-observation is never used as a `not-applicable` reason (C1). Pricing dialog omits kind and map (C4). This is not a complete state-coverage claim (C3). Capture-record qualifier covers every Reason cell. |
| §15 Motion & Easing | 옮김 → Foundations motion | `no timing, easing, or transition values` dest **1**. `behavioral boundary, not motion tokens` dest **1**. `No duration or easing value is asserted` dest **1**. B3 is held: Foundations Motion names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). `transition properties` dest **2**. `animation name` dest **2**. `reduced-motion behavior` dest **3**. Governance Named gaps restates the five kinds and the gate as a motion-token promotion. The five-kind gate itself carries an adjacent complete B2a close on the same line. |
| §15 Motion & Easing — unattributed template curves | 해당 없음 (삭제할 무출처 커브 없음) | The source does not contain `cubic-bezier(0.4, 0.0, 1, 1)` or the other `spec/omd-v0.1.md` example-table curves. dest **0** / provenance dest **1** (omission-ledger mention that none were present; not a restored curve). |

## Sibling handling (`web/references/miro/.verification.md`)

The sibling exists — confirmed with `find web/references/miro -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: RGB spellings; dialog `460px × 764px`; `roobertPROLocal` 298 visible uses; Roobert PRO SemiBold 3/3; 2025 newsroom `AI Innovation Workspace`; Accessibility page URL `https://miro.com/accessibility/`; collector totals score 100 / 53 variants / six dialog interactions; sibling domain-boundary wording that home is not an authenticated board/product UI.
- Measured `DESIGN.md` 0 for those sibling-only strings: `460px` dest **0** · `AI Innovation Workspace` dest **0** · `rgb(253, 224, 80)` dest **0** · `authenticated` dest **0**. `460px` provenance dest **2**. `AI Innovation Workspace` provenance dest **1**.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Miro's published lines in the source are Latin, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published official / captured labels in the source body | 22 distinct | 0 | 0 | Miro / Mirotone / Aura / RealtimeBoard / Andrey Khusid / Noto Sans / Roobert PRO Medium / Displaay / Coral, Cyan, Lilac, Lime, Sunshine, Orange, Pink, Moss, and Teal / empower teams to create the next big thing / canvas-first / from idea to outcome / Everyone should be able to collaborate … without barriers / The canvas keeps work shared. / Yellow is an intentional identity signal. / Multilingual clarity is a product concern. / Accessible collaboration is part of the experience. / Cross-functional innovation teams / Multilingual, hybrid collaborators / People with access needs / Workroom energy / yellow should be the protagonist rather than paint every surface. |
| YAML `use` strings | 5 distinct | 0 | 0 | Observed public home display headline / Observed public home section heading / Observed public pricing control and list text / Observed public pricing action / Public pricing billing-period selector. |
| Sibling published strings | 0 brand-published extras | 0 | 0 | getdesign third-party gloss and collector totals are not Miro-published copy. They survive in provenance as sibling-only records; not promoted into `DESIGN.md`. |

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 22 + YAML use 5 extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand miro --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 128 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 128.

## Deviations recorded

- `DESIGN.md` is above the spec's 600–1,800-word SHOULD budget (`wc -w` ~5467). The budget yielded to A1: six declared component records with selectors and seven-state maps, dual token-set / observed type tables, the full §1/§11 founding-and-identity narrative including the last-sentence split, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Miro has a published first-party design system (Mirotone), so every derived-editorial close names that specification (rulebook v12 B2a 전제 주석). Measure `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = **25** = `not Miro-authored` **25** = `including the published Mirotone documentation` **25**. Provenance `## Derived editorial inventory` **25** data rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is a Miro-issued fact or a derived reading. Scope surface-boundary, atmosphere/Workroom energy, and narrative-as-context plus last-sentence split; primary-task selection; Audience group reading; trait grouping; Principles / Do / Don't headers; Semantic role slotting including unmerged yellow/blue/borders/canvas-vs-oklch and canvas-fill / Blue-action-text / Email-background as separate roles even where they share a hex; spacing unitless-vs-px / 16-unmerge / `xs: 4` off type size; shape key-path separation including 40-vs-40px-height and local 50%; elevation and motion-gate readings; font evidence including declared-only-not-UI-token / family no-substitution and roobertPROLocal-as-alias / type-role keep-both; Type roles after tables (display-hero `lineHeight` `56` not a replacement for `size` `56`); simpleicons-slug-as-pointer; Capture record ¶1 (YAML primitive type only on Pricing-period toggle); applicability procedure including YAML `type: toggle` only on Pricing-period toggle, dialog C4, and generic Focus not as `focus-visible` treatment; Pricing dialog local C4 restatement; layout 1440x900-capture; byte-exact / content-boundary reading; and the unresolved-list naming each carry an adjacent complete Mirotone-named qualifier. Source facts (hexes, YAML keys, published strings, 882/46 counts, selectors, 2011/RealtimeBoard/Andrey Khusid, 2023 identity sentences) were left unqualified. Inventory: **25** lines.

## F2 E2 rescan (mandatory final pass)

Every destination line above was checked with a Python substring scan against the three output files after the B2a body edits. Counts use per-file match lists, never a remembered count. Dual destinations named both files (identity name, `#fde050`, simpleicons slug, token note, `ds.type: system`, `ds.description`, Displaay foundry/licence URLs, `oklch(1 0 0)`, selectors, narrative URLs, B3, spacing/rounded paths). `reconciled` · `components_harvested` · `ds.og_image` (provenance dest **3**) · `getdesign.md` · `460px` · `AI Innovation Workspace` are provenance only (DESIGN dest 0). Compliance claims were written only after the portable body actually contained the full text: B3 five-kind gate dest **2**, primitive type toggle dest **1** = YAML count, lineHeight ratios kept unitless, YAML `use` 5/5, §14 five observed rows, no cubic-bezier promoted. `public buttons are largely flat` dest **2**.

## 값 보존 실측 (산출 3파일 대조)

| 검사 | 결과 |
|---|---|
| legacy 고유 hex 9종 + 제거된 `#5b76fe` | 9/9 이 산출 `DESIGN.md` 본문에 존재 — 손실 0. `#5b76fe` dest **1** as the source's already-removed value. |
| portable 본문의 hex 발명 | 0건. 본문의 모든 `#rrggbb`가 legacy 토큰 집합 또는 그 제거 기록의 부분집합. |
| unitless lineHeight (A1a) | YAML `56` / `55.2` / `20` / `24` kept beside px spellings. |
| primitive type (A1b) | toggle 1 — legacy YAML 실측과 동수. `Primitive type: \`button\`` dest **0**. Other §4 records `not in the token set` dest **6**. |
| `[FILL IN]` | 원본 0건, 산출 DESIGN.md 0건. |
| C1 (`not captured`/`not named`를 not-applicable 사유로) | 0건. `not-applicable` 행은 전부 역할 사유(destination promo/pricing action · toggle · value field). 머리의 "Absence of a capture is not a `not-applicable` reason" 1회는 금지 사유가 아님. |
| C2 | Yellow / Blue / Outlined 의 L/E/S는 destination 역할로 `not-applicable`. Toggle 의 L/E/S는 toggle 역할로 `not-applicable`. Email 의 error/success는 form field로 `applicable`, loading은 value-field 역할로 `not-applicable`. primitive 일괄 개방 아님. `loading \| applicable` dest **0** · `loading \| not-applicable` dest **5**. |
| C3 | "This is not a complete state-coverage claim." 를 Components Capture record에 명시. 완료 주장 0건. |
| C4 | Pricing dialog omits kind and a state-applicability map. Disabled 36px circular home control is capture-record only. |
| D2 / D2a | §13 식별자 본문 0 · provenance 0 · 이 로그 0. Audience는 원본 그룹 원문만. |
| D1 | `native-client` / `native application` / `mobile app` / `authenticated` / `back-office` DESIGN dest **0**. 원본이 세운 home / pricing / Aura / Mirotone / `1440x900` 만 유지. |
| A5 발행 문자열 전수 대조 (게이트 바늘 밖까지) | 추출 22 + YAML use 5 / 미생존 **0건**. 게이트 compared/candidates는 아래 Gate run. |

### A5a 손 스윕 목록 (추출 27 / 미생존 0)

발행 카피만. 설명문·점 경로·폰트 스택·제3자 매체명·declared-only 페이스 이름은 바늘이 아니다.

Legacy 본문 (22): `empower teams to create the next big thing` · `canvas-first` · `from idea to outcome` · `Everyone should be able to collaborate … without barriers` · `The canvas keeps work shared.` · `Yellow is an intentional identity signal.` · `Multilingual clarity is a product concern.` · `Accessible collaboration is part of the experience.` · `Cross-functional innovation teams` · `Multilingual, hybrid collaborators` · `People with access needs` · `Workroom energy` · `RealtimeBoard` · `Andrey Khusid` · `Mirotone` · `Aura` · `Noto Sans` · `Roobert PRO Medium` · `Coral, Cyan, Lilac, Lime, Sunshine, Orange, Pink, Moss, and Teal` · `yellow should be the protagonist rather than paint every surface` · `lively brand expression belongs to marketing and board context` · `These are first-party stakeholder groups, not fictional individual personas`.

YAML use (5): `Observed public home display headline` · `Observed public home section heading` · `Observed public pricing control and list text` · `Observed public pricing action` · `Public pricing billing-period selector`.

## 제출 전 자가 대조 (웨이브 43)

뽑은 고유 표현 112 + 후속 50. `grep -oF` 0이었던 뒤 복원: **6** (`Workroom energy` · `broad-language body face` · `These are first-party stakeholder groups, not fictional individual personas` · `tokens.spacing.xs` · About/Aura/identity/Mirotone-docs/Displaay URLs · `broad language support for distributed teams` / `keyboard, screen-reader, voice-control, color, and motion-sensitive collaboration`). ASCII `Don't` 오탐 4건은 원본 타이포그래픽 apostrophe와 일치해 복원 대상이 아님.

고유 표현 대조 결과: 뽑은 표현 162 / 0이었다가 복원한 수 6.
