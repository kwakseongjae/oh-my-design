# hp migration log

Source: `web/references/hp/DESIGN.md`
Sibling read (not the migration input): `web/references/hp/.verification.md`
Destination: `docs/design-md-weight/migrated/hp/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hp/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `b347ce61c6e5267f84040c8cd474605877510498c55b7a9a8baf0ad737ccc575` (`web/references/hp/DESIGN.md`). Sibling SHA-256 `d41bf2bfe9db7eebfc1b9392f82ce063561b5a75952c8e2ebd82f59a04525800` (`web/references/hp/.verification.md`). Worker-close portable DESIGN SHA-256 `86ce74e4656cf6554a8cac78317e45719f5715b865c95b101ff6c0e7d8e3a63c`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components | Portable file has no frontmatter. H1 is `# HP Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://www.hp.com` is dual: dest 2 / provenance 6 (E2a). `#0096D6` is dual: dest 20 / provenance 5. The simpleicons slug is provenance-only (Assets says so; `DESIGN.md` 249 + `provenance.md` Logo decision). The catalog-field reading on 249 carries its own adjacent complete qualifier (B2/B2a). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. `prose-derived` is dual: dest 4 / provenance 3. These are ledger keys; the portable body names `prose-derived` only where the evidence class of a value depends on it. |
| YAML `tokens.colors` (18 keys) | 옮김 → Foundations semantic color | `DESIGN.md` Semantic color (87–122). All eighteen roles kept with names and values: primary `#0096d6` dest 2 / provenance 1 · primary-hover `#0073a8` dest 1 / provenance 1 · primary-pressed `#005c87` dest 1 / provenance 1 · primary-light `#e6f4fb` dest 1 / provenance 1 · electric-blue `#0278ab` dest 2 / provenance 1 · ink `#212121` dest 15 / provenance 3 · canvas `#ffffff` dest 17 / provenance 5 · grey-50 `#f7f7f7` dest 4 / provenance 1 · grey-100 `#eeeeee` dest 3 / provenance 1 · border `#e0e0e0` dest 3 / provenance 1 · border-strong `#cccccc` dest 13 / provenance 1 · placeholder `#9e9e9e` dest 2 / provenance 1 · caption `#767676` dest 7 / provenance 1 · body `#595959` dest 4 / provenance 1 · emphasis `#404040` dest 1 / provenance 1 · success `#0c7d2f` dest 3 / provenance 1 · error `#d32f2f` dest 9 / provenance 1 · warning `#f5a623` dest 1 / provenance 1. Paths named `tokens.colors.<key>`. Prose uppercase forms (`#0096D6` dest 20) sit beside the YAML lowercase forms; they are not merged. Overlay `rgba(33,33,33,0.6)` dest 2 is a source-body value, not a YAML key. |
| YAML `tokens.typography.family` (`sans: "Forma DJR UI"`, `mono: "SF Mono"`) | 옮김 → Typography & Assets Family | `DESIGN.md` Family (206–213). Paths named `tokens.typography.family.sans` / `mono`. `Forma DJR UI` dest 32. `SF Mono` dest 2. The fallback prohibition on 213 carries an adjacent complete qualifier (B2/B2a). Declared-only fallback-members-are-not-the-brand-face reading is qualified on the Font evidence row at 202. Live computed "both statements are kept" is qualified on the Font evidence row at 200. |
| YAML `tokens.typography.display-hero / body / button / label` | 옮김 → Typography & Assets Type roles | `DESIGN.md` Type roles (215–235). Unitless line heights stay ratios and are never converted to px (A1a): `1.17` dest 1 · `1.22` dest 1 · `1.29` dest 1 · `1.36` dest 1 · `1.44` dest 1 · `1.50` dest 2 · `1.63` dest 1 · `1.57` dest 4 · `1.54` dest 1 · token-set `1.5` on the Body (token-set) row at 229. Token-set Body `16` / `400` / `1.5` / `Standard reading text, inputs` and hierarchy Body `14px` / `400` / `22px (1.57)` stay on separate rows. YAML `use` strings restored verbatim: `Marketing hero headlines` dest 1 · `Standard reading text, inputs` dest 2 · `Button label` dest 1 · `Input labels, captions` dest 1. Tracking `-0.5` kept on the Display Hero row. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Paths kept separate (easywallet-type check): Spacing slot `DESIGN.md` 128 `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 24` · `xl: 32` · `xxl: 48` · `section: 64`. Shape slot 136 `sm: 4` · `md: 4` · `lg: 8` · `full: 9999`. `base: 16` is the spacing path (128/130). `lg: 24` is the spacing path (128/130); it is not a radius step. `lg: 8` dest 3 is the radius path (136/139/631). `full: 9999` dest 2 is the radius path (136/631). `9999px` dest 6. `sm: 4` and `md: 4` stay as two keys. YAML search `radius: 8` and body Search `24px` and the pill step `9999px` stay on those records (432–456). The keep-both / choose-neither reading is qualified on 446 (B2/B2a). Layout restates both scales at 630–631. Spacing qualifier at 132. Shape qualifier at 144. `full: 9999` provenance dest 0 — this log does not invent a provenance second home (fitpet-type). |
| YAML `tokens.shadow.flat` | 옮김 → Foundations elevation | `tokens.shadow.flat` dest 2 / provenance 2 (E2a). Path named at `DESIGN.md` 156. Value `none`. The four shadow strings from §6 sit beside it; neither was chosen over the other. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | `DESIGN.md` 259–456. Verified primitive types preserved per YAML component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×5 (Primary / Secondary / Tertiary / Dark / Danger at 262/291/315/338/361), `Primitive type: \`input\`` ×3 (Default / Error / Search at 384/411/435). All eight YAML `use` strings restored as `Token-set use:` lines. YAML `font` shorthands `16px weight 600` dest ≥ 1 · `16px weight 400` dest 1. Cards, badges, tab, banner, modal, and switch have no YAML `type`; no primitive type was invented for them. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–15; Key Characteristics as Distinctive traits 34–44. Atmosphere readings carry an adjacent complete qualifier at 11 (B2/B2a), which names clean-confident / products-as-color / never-compete as well as the source's unmistakable / precise-approachable / established-current wording. Surface-boundary reading at 9. Narrative-context and heritage/thesis readings at 13. 2012 / 2024–2025 / Electric Blue / Forma DJR / David Jonathan Ross kept. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 87–124. The source's own group headings (Primary, Neutral / Ink, Semantic, Surface & Borders) are kept as the grouping. Role-naming readings carry the adjacent qualifier at 89 (B2/B2a). PMS 2925 C dest 1. Pantone 2132 dest 1. Color rules (HP Blue is interaction; black or white only) kept on 124. |
| §3 Typography Rules — family, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 195–204; family 206–213; hierarchy table 215–235. Official product-use, Live computed, Declared-only, License, and Outside-captures rows carry adjacent complete qualifiers (B2/B2a). Brand Central URL kept on the Official product-use row. |
| §3 Principles — sentence case, three weights, black or white, humanist grotesque | 옮김 → Typography & Assets Typography rules | `DESIGN.md` 237–244 under the qualifier at 239 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 259–601. §4 body values (44px / 36px / 52px / 1.5px blue border / Search 24px / card shadows / badge tints `#e6f4d9` / `#fdecea` / switch 9999px) and YAML `use` / `font` / padding byte forms are both kept where they differ. Search keep-both / choose-neither is qualified on 446. |
| Footer **Tier 1 sources** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. `https://www.hp.com` is dual: Scope + provenance Surfaces (E2a). The source §4 footer "verified via live DOM getComputedStyle" and the HTML comment "live DOM not directly inspected" are both ledgered; neither was chosen over the other. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 624–654 (1280px / 12-column / 24px gutters / 32px margins / 4→2→1 grids at 626–628; spacing/shape restatements 630–631). Hardware-breathe / banded sections / quiet density readings carry the qualifier at 633 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Five-level table `DESIGN.md` 148–154, including Sticky header `0 1px 4px rgba(0,0,0,0.08)`. The Shadow philosophy paragraph is carried as a qualified reading at 158 (B2/B2a). YAML `tokens.shadow.flat` sits on 156. Backdrop blur kept on 158. |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 59–69, under the grouping qualifier at 61. Not placed in Governance controlled copy. Seven items, matching the source Do list. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 71–81, under the qualifier at 73. Seven items, including the `#000` ink prohibition and the Electric Blue / working-UI-blue split. No invented domain (no `native application`, no `back-office`, no `measures 1440px`). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Breakpoint table `DESIGN.md` 637–642 with the "stated at system level rather than measured across viewports" note at 635; touch targets 644 (`44px` / `48px` / `40px`); collapsing strategy 646–651; image behavior 653. Exact string `measures 1440px` dest 0 / provenance 0. Desktop row keeps the source body's `1024–1280px`. Wide row keeps `>1280px` and the 1280px cap. |
| §9 Agent Prompt Guide — Quick Color Reference, Example Component Prompts, Iteration Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present — the palette hexes, the 4/8/9999 radii, the 16/48 sizes, the 400/600/700 weights, the published strings, and the 1280px / 12-col / 24px gutter layout (A2, A3). |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings `DESIGN.md` 656–690; register table 660–668; further recorded strings 670–685; forbidden register 687. The qualifier at 658 covers the characterization and the table. The closing line at 689 says a gloss never replaces the line (A5) and carries an adjacent complete qualifier for that byte-exact / gloss-beside rule (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: **1939 Palo Alto garage**; **Bill Hewlett** and **Dave Packard**; **audio oscillators**; **2015** split into **HP Inc.** and **Hewlett Packard Enterprise**; **HP Blue `#0096D6`** since **2012**; four-letter lowercase logo inside a circle designed by **Eight Inc.** as a "future" mark; **2024–2025** brand evolution; **Forma DJR** replacing **HP Simplified**; **Electric Blue** for campaign energy; **OMEN** sub-brand carries gaming energy separately. `grep -oF` dest: `1939` 1 · `Palo Alto` 1 · `Bill Hewlett` 1 · `Dave Packard` 1 · `audio oscillators` 1 · `2015` 2 · `HP Inc.` 4 · `Hewlett Packard Enterprise` 1 · `2012` 4 · `Eight Inc.` 2 · `HP Simplified` 6 · `OMEN` 1. Marked there as narrative context that supplies no interface tokens. The design-language paragraph at 15 carries an adjacent complete qualifier (B2/B2a) and keeps `restraint is the statement` dest 1 (HTML-comment wording) plus `HP's restraint is its statement` (body wording). No "native application is out of scope" sentence was written (D1). |
| §12 Principles — 8 numbered | 옮김 → Experience principles | `DESIGN.md` 46–57 under the B2a form at 48: "These eight items are a derived editorial implementation inference from the verified surfaces; they are not HP-authored or a separately published UI specification." HP has Brand Central typography guidelines and no published first-party UI design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). Brand Central is named as official type guidance, not denied. |
| §13 Personas — 3 entries | 삭제 | The source's own italic line labels them fictional archetypes informed by publicly described consumer-tech buyer segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, or city appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience at `DESIGN.md` 32 carries only the group-level description the source records (people who use HP personal systems and printing to get work done), under an adjacent qualifier. The five `primary-tasks` at 23–27 come from labels and surfaces the source records; 21 says so and qualifies the step from label to "primary task". |
| §14 States — 11 rows | 옮김 → Components & States State record + per-component applicability | Full eleven-row body preserved at `DESIGN.md` 605–619 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings `Your cart is empty`, `Shop laptops`, `No results match your filters`, `Enter a valid email address`, `Your order is confirmed`, `Out of stock`, `Notify me`, `#fdecea`, `#e6f4d9`, `rgba(0,150,214,0.3)`, and `1.02×`. The applicability rule is at 255–257, whose qualifier at 257 covers every Reason cell that follows. The non-attachment sentence at 621 carries its own adjacent complete qualifier. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive maps: Primary / Secondary / Dark / Danger open loading/error/success as commit-capable action buttons (`loading \| applicable` dest 4 · `error \| applicable` dest 7 · `success \| applicable` dest 4). Tertiary / Underline Tab / Switch close loading/error/success together as destination or toggle (`loading \| not-applicable` dest 6 · `error \| not-applicable` dest 3 · `success \| not-applicable` dest 6). Default / Error / Search inputs open error and close loading/success (fields). The three cards and the modal get no `kind` and no map because the source supplies no interactive-kind evidence — C4 dest 4. The four badges and the inline banner declare `Kind: non-interactive` dest 5 with a reason instead of a map (Core §4.4). |
| §15 Motion & Easing — durations, easing token names/roles, signature motions, reduced-motion | 옮김 → Foundations motion | `DESIGN.md` 164–172 (the five duration tokens with their uses: `motion-instant` 0ms, `motion-fast` 150ms, `motion-standard` 250ms, `motion-slow` 350ms, `motion-page` 400ms), 174–180 (three easing token names and roles), 184–189 (product card lift, mega-menu reveal, add-to-cart pulse, and the `prefers-reduced-motion: reduce` behavior). `0ms` dest 6 · `150ms` dest 2 · `250ms` dest 1 · `350ms` dest 1 · `400ms` dest 1. |
| §15 Motion & Easing — three `cubic-bezier` values | 삭제 → provenance omission ledger | No observation stands behind them: the source token-set is `prose-derived` and the HTML comment assigns token-level motion to conventional consumer-tech / e-commerce values. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to the example table at `spec/omd-v0.1.md`. The other two (`cubic-bezier(0.0, 0.0, 0.2, 1)`, `cubic-bezier(0.4, 0.0, 0.2, 1)`) are likewise unattributed. Ledger at `provenance.md` Omission ledger. Occurrence counts by `grep -oF cubic-bezier` → `DESIGN.md` 0, `provenance.md` 3 (three curves). B3 is held: the promotion condition at `DESIGN.md` 182 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus the partial-confirmation exclusion (E2c). Governance 725 restates the five kinds and the gate. |
| HTML comment OmD v0.1 Sources | 분리 → provenance | Live-inspect / not-inspected pair, Tier 1/2 URLs, narrative class, Brand Central type citation, and the interpretive-claim note are ledger material: `provenance.md` Evidence class + Raw samples. Frequency counts and sibling computed samples stay ledger-only. |

## Sibling handling (`web/references/hp/.verification.md`)

The sibling exists — confirmed with `find web/references/hp -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact.

- Its full record is transcribed at `provenance.md` Sibling verification + Raw samples and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not, or that conflict with the source: body `#191919`, heading `#000000` / `34px` / `400` / `forma-djr-micro`, primary button `#000000` / `0px` / `43px` / `500`, link `#3d3d3d`, root `#000000`. Recorded at `provenance.md` Sibling-only values. Neither side of a conflict was chosen as a new token.
- `grep -oF` dest 0 for `#191919` · `forma-djr-micro` · `#3d3d3d` · `43px`. Provenance holds those strings as sibling-only mentions.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Brand-issued strings in the source (labels, CTAs, slogans, microcopy, names) | 26 distinct | 0 | 0 | All 26 survive in `DESIGN.md` (hand list below). |
| Brand-issued strings in the sibling, any length | 0 sibling-only | 0 | 0 | Sibling records no measured copy. |
| `node test-v2/tools/latin-copy-audit.mjs --brand hp` after restore | 46 candidates | 1 reported | 0 | The remaining needle is `just worked for six years.` — source §13 persona biography, not published HP copy. Deleted with §13 (D2, D2a). `restraint is the statement` dest 1 after restore (HTML-comment wording). |

Sub-needle labels confirmed present individually in `DESIGN.md`: Add to cart, Buy now, Continue, Learn more, View details, Shop laptops, Your cart is empty, No results match your filters, Your order is confirmed, Enter a valid email address, Out of stock, Notify me, New, Featured, In stock, Let's get your printer connected, Keep Reinventing, Let's create, Power through your day, Remove from cart, Cancel order, Overview, Specs, Reviews, Oops, restraint is the statement.

A5 분모: latin-copy-audit 46 candidates / 1 lost = persona biography (not published copy); hand sweep 26 extracted / 0 missing / 0 published-copy missing. Sibling-only published 0.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hp --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 205 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 205.

## Deviations recorded

- `DESIGN.md` word count is recorded after the F1 block. The spec's 600–1,800-word SHOULD budget yielded to A1: eighteen color roles, a dual unitless/px type table, eight declared YAML component records plus §4 cards/badges/tab/banner/modal/switch, an eleven-row surface state contract, a per-control applicability matrix, §11 year/split/logo facts, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- HP has Brand Central typography guidelines and no published first-party UI design system in this packet, so every derived-editorial close uses the toss-form "not HP-authored or a separately published UI specification" (rulebook v12 B2a 전제 주석). Brand Central is named on the Official product-use row. Measure is in the F1 block: 33=33.
- There is no `Named gaps` section (`DESIGN.md` 0). Recorded unresolved names only the easing-curve omission, the token-set Body vs hierarchy Body pair, the search-radius trio, and the two source inspect statements — all established by the source.

## F1 — B2a scan (mandatory final pass)

The finished `DESIGN.md` was re-read from the title through Governance. Every sentence that states a cause, a reading, a role name this contract assigned, a kind/applicability verdict, or a voice characterization was asked: is this an HP-issued fact, or a reading derived from the observed surfaces? The latter received an adjacent complete qualifier — "derived editorial implementation inference from the verified surfaces; … not HP-authored or a separately published UI specification."

Worker-close measured dest 30 = inventory 30. F3 audit re-read the portable body independently and found three editorial sentences without an adjacent complete qualifier (Live computed both-statements, Assets catalog-field, Search keep-both) plus two existing qualifiers that did not name the readings in their own paragraph (Scope ¶2 remaining atmosphere readings; Scope ¶3 heritage/thesis). After those in-place edits: dest 33 = inventory 33. `derived editorial implementation inference` dest 33 = `not HP-authored or a separately published UI specification` dest 33 = provenance Derived editorial inventory 33 data rows. Provenance names the clause in the inventory preamble; those preamble sentences are not portable qualifications.

Source facts that were not qualified: the 1939 / 2012 / 2015 / 2024–2025 years, Bill Hewlett, Dave Packard, Palo Alto garage, audio oscillators, HP Inc. / Hewlett Packard Enterprise split, Eight Inc. circle logo, HP Simplified → Forma DJR, OMEN as a named sub-brand, Brand Central type rules as the source attributes them, the live hex and metric values, and the published strings.

## F2 — E2 대조 (mandatory final pass)

Each log row above was written only after `grep -oF` confirmed the named value in the named file. Dual destinations are claimed only where both files contain the string (`#0096d6`, `#0073a8`, `#005c87`, `#e6f4fb`, `#0278ab`, `#212121`, `#ffffff`, `#0096D6`, homepage URL, `1939`, `2015`, `2012`, `Eight Inc.`, `prose-derived`, `tokens.shadow.flat`). `full: 9999` and `1.17` are dest-only — this log does not invent a second home (fitpet-type). "B3 is held" is claimed because `DESIGN.md` 182 contains the five evidence kinds, the per-component gate, and the partial-confirmation exclusion in full text. "B2a 33=33" is claimed because both the portable count and the inventory row count are 33. §13 deletion is claimed as unidentified: this file and provenance name the section, the headcount, and the dropped field kinds, and do not restate the identifiers (name / age / city dest 0 / provenance 0 / this file 0 as use).

## YAML key-path self-check (wave extra)

Every `tokens.*` key path from the source frontmatter was checked against the portable body by path, not by finding the same number in another scale.

| Path | Check |
|---|---|
| tokens.colors.* (18 hex) | Each hex is a Semantic color role row; dest ≥ 1. Lowercase YAML forms and uppercase prose forms are both kept. |
| tokens.typography.family.{sans,mono} | Family section names both |
| tokens.typography.{display-hero,body,button,label} | Type-roles table; unitless LH `1.17` / `1.5` / `1.0` on those rows; hierarchy Body `14px` / `1.57` is a separate row |
| tokens.spacing.{xs,sm,md,base,lg,xl,xxl,section} | Spacing slot `xs: 4` … `section: 64`; `base: 16` is not a radius step; `lg: 24` is not a radius step |
| tokens.rounded.{sm,md,lg,full} | Shape slot `sm: 4` · `md: 4` · `lg: 8` · `full: 9999`; `lg: 8` on this path is not the spacing `sm: 8` |
| tokens.shadow.flat | Elevation `none` |
| tokens.components.* type | button 5 / input 3 |

`Primitive type: \`button\`` dest 5 = source YAML button records 5. `Primitive type: \`input\`` dest 3 = source YAML input records 3. No line-height was copied from one role onto another. `full: 9999` dest 2 is the rounded path (136/631), not a spacing coincidence.
