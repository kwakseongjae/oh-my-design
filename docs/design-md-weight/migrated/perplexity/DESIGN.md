# Perplexity Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Perplexity is an AI answer engine. This contract covers the first-party production site the source names: `https://www.perplexity.ai`. Values below stay attached to the surfaces that established them: the Ask composer and thread/answer reading column, Home / Discover / Spaces / Library navigation, marketing/landing moments (including the Display Hero), settings/sign-in/share/upgrade dialogs, and the first-class dark mode on the same site. Treating that production URL and those named surfaces as this contract's scope, and keeping values attached to the surface that established them, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

The source's own atmosphere record, kept as written. Perplexity's design is built around a single idea: get out of the way of the answer. Where most AI products lean on neon gradients, glassmorphism, and a cold sci-fi chrome, Perplexity went the opposite direction — a calm, editorial, almost Scandinavian aesthetic where the interface dissolves and the text takes the stage. Surfaces are warm off-white and bone-paper tones (`#FBFAF4`, `#FCFCF9`) rather than clinical pure white, paired with a deep near-black ink (`#091717`) that reads like printed type. The one expressive move is the signature **Peacock / True Turquoise teal** (`#20808D`) — a muted, slightly grayed teal that nods to terminal phosphor and to the Perplexity "answer cursor" without tipping into the saturated cyan that every other AI startup uses. Typographically the brand runs on **FK Grotesk** and **FK Display** (Florian Karsten's grotesque family); body copy frequently uses **FK Grotesk Neue** for legibility at small sizes. The result, in the source's words, is an interface that feels like a well-set magazine that happens to think. The hex values, type family names, "get out of the way of the answer," "Peacock / True Turquoise," "answer cursor," "terminal phosphor," and "well-set magazine that happens to think" are the source's own. Readings of that layer as quiet and literary, as an editorial low-glare reading surface, as print-like rather than `#000`, as confident but never loud, as subway-signage neutrality with quiet character, or as an "invisible brand" whose chrome recedes so the answer is the hero, are a derived editorial implementation inference from the verified surfaces; they are not Perplexity-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. Perplexity AI was founded in **2022** by Aravind Srinivas, Denis Yarats, Johnny Ho, and Andy Konwinski as an "answer engine" — a search product that responds in synthesized, cited prose instead of ten blue links. The thesis was that the future of search is a conversation that always shows its work. That single conviction — **answers with citations** — drives every design decision. When the brand identity needed to mature past a developer prototype, Perplexity worked with Philadelphia studio **Smith & Diction**, who framed the work as building "an invisible brand." and who described the brief as "creating an invisible brand": the product is a reading experience, so the chrome should feel like good paper and good ink, not a dashboard. The logic: Perplexity is a reading and thinking tool, so the most respectful design recedes. They reached for an editorial, Scandinavian-leaning system — warm paper grounds, a precise grotesque type family (**FK Grotesk / FK Display**), and a single muted teal (`#20808D`) chosen specifically because it was *not* the saturated cyan of the AI gold rush. The teal reads a little like terminal phosphor and a little like a peacock — technical and natural at once. What Perplexity refuses is instructive: no gradient meshes, no glassmorphic dashboards, no neon "AI" sheen, no mascot. In a category sprinting toward maximal sci-fi spectacle, Perplexity bet on the opposite — calm, literacy, and trust. The interface should feel like the best version of reading a really good article, where the answer is the design and the chrome is just good paper and good ink. The 2022 founding, the four named founders, the "answer engine" / ten-blue-links thesis, the **answers with citations** conviction and the sentence that it drives every design decision, Smith & Diction, Philadelphia, both "invisible brand" wordings, "reading and thinking tool," the teal-not-saturated-cyan choice, the refusal list, and that closing article / paper / ink sentence are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-identity narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a control or surface the source records, and not taking them from the source's persona section, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

- Ask in the Ask box (`Ask anything…`) with the Ask / submit primary CTA.
- Read a generated answer block with inline source references (`[1]`).
- Open a Cited source card, or select a Focus mode / filter selector (Web, Academic, Writing).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source labels its Personas section as fictional archetypes, not individual people; those biographies are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. Observable work follows the three primary tasks the Ask box, generated answer block, cited source card, and Focus mode / filter selector establish. Dropping those fictional archetypes rather than promoting them, carrying no demographic segment list, and reading audience only from those captured controls, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside it, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

- Peacock teal (`#20808D`) as the single brand accent — muted, terminal-adjacent, never neon
- Warm paper backgrounds (`#FBFAF4` / `#FCFCF9`), not pure white — an editorial, low-glare reading surface
- Deep teal-black ink (`#091717`) for type — reads like print, not `#000`
- FK Grotesk / FK Display / FK Grotesk Neue — neutral grotesque with quiet character
- "Invisible brand" philosophy: the answer is the hero, the chrome recedes
- First-class dark mode built on the same teal, on a near-black `#0D1117`-family canvas
- Generous radii (8–12px) and soft, low-contrast borders for a paper-soft feel

### Principles

These 8 items are a derived editorial implementation inference from the verified surfaces; they are not Perplexity-authored or a separately published UI specification. The source states them in its own Principles section.

1. **The answer is the hero.** Every pixel of chrome exists to deliver the user to the text faster and then disappear. If a UI element competes with the answer for attention, it's wrong.
2. **Always show your work.** No claim without a path to its source. Citations are a first-class, typographic part of the answer, not an afterthought panel.
3. **Calm over spectacle.** Warm paper, muted teal, quiet grays. The brand earns trust by *not* shouting in a category that shouts.
4. **One accent, used with discipline.** Teal `#20808D` means action or citation. It never decorates. Restraint makes the teal legible as a signal.
5. **Read like print.** Answer text gets a constrained measure and generous leading. The product is text; it should typeset like a magazine, not a form.
6. **Paper-flat depth.** Separation comes from warm borders and surface temperature, not drop shadows. Elevation is reserved for things that truly float.
7. **Dark mode is not an afterthought.** Many users read at night; the dark theme is designed in parallel with the same teal logic, not bolted on.
8. **Invisible brand.** The strongest identity here is the one you stop noticing — the brand succeeds when the user only remembers the answer.

### Application rules

The source states these eight as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Perplexity-authored or a separately published UI specification.

- Use Peacock teal (`#20808D`) as the single accent — buttons, links, focus rings, active states
- Background with warm paper tones (`#FBFAF4` / `#FCFCF9`), not pure white
- Use teal-tinted ink (`#091717`) for text, never `#000000`
- Set answer body in FK Grotesk Neue at ~1.6 line-height for a reading experience
- Constrain the answer column to a ~68ch measure
- Keep chrome quiet (`#5C6A6A` ink grays) so the answer is the hero
- Render inline citations as small teal `[1]` tokens in the type flow
- Provide a first-class dark mode on `#0D1117` with brightened teal `#34B4C4`

### Avoid

The source states these seven as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Perplexity-authored or a separately published UI specification.

- Don't use saturated neon cyan or AI-startup gradients — the teal is muted on purpose
- Don't put the answer in pure white boxes with heavy shadows — stay paper-flat
- Don't crowd the reading column with dense toolbars or rails
- Don't use bright multi-color accents for sources — keep the muted source palette
- Don't set body text below 15px or above 16px in the answer flow
- Don't use `#000000` text or `#FFFFFF` page backgrounds in light mode
- Don't let the brand teal decorate non-interactive elements — it signals action and citation

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Token-set keys follow the YAML paths. YAML writes lowercase hex; source §2 writes mixed-case hex; both writings stay. Pairing each hex to the token-set path named beside it, keeping YAML lowercase beside §2 mixed-case, keeping `tokens.colors.surface-white` `#ffffff` unmerged from `tokens.colors.on-primary` `#ffffff` and off the light-mode page background, keeping catalog `primary_color` `#20808D` as the same hue as `tokens.colors.primary` `#20808d` without collapsing the catalog field into the token-set key, keeping Info as a §2 role on that same teal rather than as a new YAML key, keeping `tokens.colors.canvas` `#fbfaf4` unmerged from `tokens.colors.surface` `#fcfcf9` and from `tokens.colors.surface-white` `#ffffff`, keeping `tokens.colors.ink` `#091717` unmerged from `tokens.colors.body` `#2e3a3a` / `tokens.colors.muted` `#5c6a6a` / `tokens.colors.placeholder` `#8a9494`, keeping `tokens.colors.hairline` `#e4e4dc` unmerged from `tokens.colors.hairline-soft` `#efefe9`, keeping dark-mode keys unmerged from light-mode keys, keeping `tokens.colors.teal-on-dark` `#34b4c4` unmerged from `tokens.colors.primary` `#20808d`, keeping Source Slate / Source Plum / Source Clay and Surface Raised `#1C2128` and Ink Inverse 500 `#9BA1A6` as body writings that are not YAML `tokens.colors` keys, and keeping YAML component fills such as `button-secondary.bg` `#fcfcf9` and `button-ghost.bg` `#efefe9` as component fields rather than as general Ink or Paper, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

**Primary**

- **True Turquoise / Peacock** (`#20808D` / YAML `#20808d`): `tokens.colors.primary`. Catalog identity `primary_color` is `#20808D`. The brand teal. Primary interactive accent — the submit/answer button, active links, focus rings, source highlights, the answer cursor. The single hue allowed to carry brand voice. Source §2 **Info** (`#20808D`): informational notes reuse the brand teal — info and brand are the same voice here. Info is that §2 role on this hue, not a separate YAML key.
- **Teal Hover** (`#1A6873` / YAML `#1a6873`): `tokens.colors.primary-hover`. Darkened Peacock for hover/pressed states on teal surfaces.
- **Teal Deep** (`#13343B` / YAML `#13343b`): `tokens.colors.primary-deep`. Inky teal for emphasis on light surfaces, selected-source rails, and quiet brand washes.
- **Teal Tint** (`#E5F2F2` / YAML `#e5f2f2`): `tokens.colors.primary-tint`. Pale teal wash for informational backgrounds, hovered list rows, citation chips on light surfaces.

**Ink & Paper (Neutrals — light mode)**

- **Offblack Ink** (`#091717`): `tokens.colors.ink`. Primary text. A teal-tinted near-black that reads like printed ink, never harsh `#000000`.
- **Paper** (`#FBFAF4` / YAML `#fbfaf4`): `tokens.colors.canvas`. Primary page background — warm bone-white, the editorial reading surface.
- **Paper Raised** (`#FCFCF9` / YAML `#fcfcf9`): `tokens.colors.surface`. Card and panel surface, fractionally lighter/cooler than the page. This is not `tokens.colors.canvas`.
- **Pure White** (`#FFFFFF` / YAML `#ffffff`): `tokens.colors.surface-white`. Reserved for input wells and elevated overlays that need to pop off Paper. This is not `tokens.colors.canvas` and is not a light-mode page background.
- **On-primary** (`#ffffff`): `tokens.colors.on-primary`. YAML on-teal label. Same hex as `tokens.colors.surface-white`; it stays a second key.
- **Ink 700** (`#2E3A3A` / YAML `#2e3a3a`): `tokens.colors.body`. Strong body text, sub-headings.
- **Ink 500** (`#5C6A6A` / YAML `#5c6a6a`): `tokens.colors.muted`. Secondary text, metadata, captions.
- **Ink 400** (`#8A9494` / YAML `#8a9494`): `tokens.colors.placeholder`. Placeholder text, disabled labels, timestamps.
- **Line 200** (`#E4E4DC` / YAML `#e4e4dc`): `tokens.colors.hairline`. Default borders and dividers on Paper — soft, warm, low-contrast.
- **Line 100** (`#EFEFE9` / YAML `#efefe9`): `tokens.colors.hairline-soft`. Hairline separators inside grouped lists.

**Dark Mode (the canonical reading mode for many users)**

- **Canvas** (`#0D1117` / YAML `#0d1117`): `tokens.colors.dark-canvas`. Primary dark page background — near-black with a cool cast.
- **Surface** (`#161B22` / YAML `#161b22`): `tokens.colors.dark-surface`. Cards, the composer well, source panels.
- **Surface Raised** (`#1C2128`): hovered rows, popovers, menus. Not a YAML `tokens.colors` key.
- **Ink Inverse** (`#F2F2ED` / YAML `#f2f2ed`): `tokens.colors.ink-inverse`. Primary text on dark — warm off-white, matching the Paper temperature.
- **Ink Inverse 500** (`#9BA1A6`): secondary text on dark. Not a YAML `tokens.colors` key.
- **Line Dark** (`#2A2F37` / YAML `#2a2f37`): `tokens.colors.dark-line`. Borders and dividers on dark surfaces.
- **Teal on Dark** (`#34B4C4` / YAML `#34b4c4`): `tokens.colors.teal-on-dark`. A brightened Peacock used as the accent on dark canvas so the teal keeps its contrast. This is not `tokens.colors.primary`.

**Semantic**

- **Success** (`#1F9D6B` / YAML `#1f9d6b`): `tokens.colors.success`. Confirmations, copied-to-clipboard, completed runs.
- **Error** (`#E0524A` / YAML `#e0524a`): `tokens.colors.error`. Failed queries, destructive actions, rate-limit notices.
- **Warning** (`#D9923A` / YAML `#d9923a`): `tokens.colors.warning`. Soft cautions, "this answer may be outdated" flags.

**Source / Citation Accents**

Citation chips and source pills cycle through a restrained set so users can distinguish references at a glance without a rainbow:

- **Source Teal** (`#20808D`) — same hue as `tokens.colors.primary`; it stays this citation-accent role rather than a second YAML key.
- **Source Slate** (`#4B5A66`), **Source Plum** (`#6E5A86`), **Source Clay** (`#A86A4B`). All muted, all sitting quietly under the text. These three are source §2 writings, not YAML `tokens.colors` keys.

### Spacing

YAML `tokens.spacing` steps, recorded without a px suffix: `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 24` · `xl: 32` · `xxl: 48` · `section: 64`. Source §5 writes a 4px base unit and common values 4, 8, 12, 16, 20, 24, 32, 40, 48, 64px. `20` and `40` are that §5 common-values list; they are not YAML `tokens.spacing` keys. `tokens.spacing.base: 16` is not `tokens.typography.answer-body.size` `16`, not `tokens.rounded.lg: 16`, and not composer padding `16px`. `tokens.spacing.md: 12` is not answer-card radius `12`. `tokens.spacing.sm: 8` is not ghost radius `8`. `tokens.spacing.lg: 24` is not answer-card padding `20px 24px` as a replacement. Reading-column vertical rhythm 24–32px between answer blocks is a §5 writing beside those steps. Keeping those YAML steps unitless beside the source's own px list, keeping `20` and `40` off the YAML map, and not treating a spacing step as a type size, radius, padding, or control height, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Shape

YAML `tokens.rounded` (unitless steps, kept on their own path): `sm: 6` · `md: 10` · `lg: 16` · `full: 9999`.

Source §5 named uses, kept beside those keys:

- Small (6px): chips, citation tokens, badges — YAML `tokens.rounded.sm`
- Standard (10px): buttons, inputs, source cards — YAML `tokens.rounded.md`
- Comfortable (12px): answer/feed cards — source §5 and `tokens.components.answer-card.radius` `12`; **not** a YAML `tokens.rounded` key
- Large (16px): composer well, modals — YAML `tokens.rounded.lg`
- Pill (9999px): focus-mode pills, toggles — YAML `tokens.rounded.full`

Ghost / Quiet radius 8px is a `tokens.components.button-ghost` field. It is not `tokens.rounded.sm: 6` and not a YAML rounded step. `tokens.rounded.lg: 16` is not `tokens.spacing.base: 16`. Keeping Distinctive-traits "Generous radii (8–12px)" as that source range rather than a replacement for the YAML map, keeping `6` / `10` / `16` / `9999` as four YAML keys, keeping `12` and `8` as component/§5 writings off that map, and reading those values as local harvested geometry rather than as a universal radius for every unlisted control, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Elevation

YAML `tokens.shadow`: `ambient: "rgba(9,23,23,0.06) 0px 1px 3px"` · `standard: "rgba(9,23,23,0.08) 0px 2px 8px"` · `elevated: "rgba(9,23,23,0.20) 0px 16px 48px"`.

Source §6 table, kept beside those keys:

| Level | Treatment | Use |
|-------|-----------|-----|
| Flat (0) | No shadow, warm border only | Answer cards, page surfaces — the editorial default |
| Subtle (1) | `0 1px 2px rgba(9,23,23,0.05)` | Source cards, inputs at rest |
| Standard (2) | `0 2px 8px rgba(9,23,23,0.08)` | Hovered source cards, popovers |
| Elevated (3) | `0 4px 16px rgba(9,23,23,0.12)` | Dropdowns, menus, toasts |
| Modal (4) | `0 16px 48px rgba(9,23,23,0.20)` | Dialogs, command palette |

YAML `tokens.shadow.ambient` is the composer rest shadow `0 1px 3px rgba(9,23,23,0.06)`; it is not §6 Subtle `0 1px 2px rgba(9,23,23,0.05)`. YAML `tokens.shadow.standard` matches §6 Standard (2). YAML `tokens.shadow.elevated` matches §6 Modal (4). §6 Elevated (3) `0 4px 16px rgba(9,23,23,0.12)` is a body writing, not a YAML shadow key. Source §4 toast shadow is `0 4px 16px rgba(9,23,23,0.18)`; that `0.18` writing stays beside §6 Elevated (3) `0.12` — neither replaces the other. Dialog backdrop is `rgba(9,23,23,0.40)`. Toggle thumb shadow is `0 1px 2px rgba(9,23,23,0.20)`.

Source shadow philosophy, kept as recorded: Perplexity is paper-flat by default. Most separation comes from warm, low-contrast borders and the temperature shift between Paper (`#FBFAF4`) and Paper Raised (`#FCFCF9`), not from drop shadows. Shadows are reserved for genuinely floating layers, and they're tinted with the ink color (`rgba(9,23,23,…)`) rather than pure black, keeping the whole surface warm. The brand's restraint is the point — depth is a tool, not decoration.

Blur effects the source records: source popovers and the command palette use a light backdrop blur (`blur(8px)`) over a translucent scrim; the sticky composer applies a subtle blur as content scrolls beneath it.

Keeping those YAML shadow strings unmerged from the §6 table, keeping toast `0.18` unmerged from Elevated (3) `0.12`, and reading the samples as paper-flat default with reserved floating layers rather than as a global shadow scale, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Motion

Durations the source records, kept as duration tokens. They are not easing curves.

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Toggle flips, focus-pill selection |
| `motion-fast` | 120ms | Hover, focus rings, button press |
| `motion-standard` | 220ms | Card hover-lift, popover open, tab switch |
| `motion-slow` | 360ms | Modal/dialog entrance, drawer slide |
| `motion-stream` | per-token | Answer streaming cadence — tokens appear as generated, the brand's signature motion |

Unsourced easing curve values (`ease-enter` `cubic-bezier(0.0, 0.0, 0.2, 1)`, `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)`, `ease-standard` `cubic-bezier(0.4, 0.0, 0.2, 1)`) are omitted at the curve-value boundary. All three match the legacy template example table and are not live-computed. Named easing roles the source records stay as role names without those curve values, with the Use column the source records:

| Token | Use |
|---|---|
| `ease-enter` | Things appearing — popovers, toasts, source cards |
| `ease-exit` | Dismissals, leaving elements |
| `ease-standard` | Two-way transitions — hover lifts, tab content |
| `ease-cursor` | The teal answer cursor blink — mechanical, terminal-like, intentional |

`ease-cursor` stays with the source's own curve writing `steps / linear`. That writing is not one of the three omitted template cubic-bezier values.

Signature motions the source names stay, with their duration tokens and recorded easing-role pairs; the omitted cubic-bezier values are not restored as promoted tokens.

1. **Answer streaming.** The defining motion. Generated text appears token-by-token in reading order, trailed by a teal block cursor (`#20808D`). Sources resolve and fade in slightly ahead of the text that cites them, so citations are "ready" by the time you read past them. Never reveal a full answer in one flash — the streaming *is* the brand telling you it's thinking and sourcing in real time.
2. **Source-card lift.** On hover, a source card raises with `motion-standard / ease-standard`, its border warming from `#E4E4DC` to teal `#20808D`. A quiet, confident invitation to click through.
3. **Citation popover.** Hovering an inline `[1]` opens a source preview with `motion-fast / ease-enter` and a light backdrop blur — fast, so it feels like the source was always there.
4. **Reduce motion.** Under `prefers-reduced-motion: reduce`, streaming collapses to a single fade-in of the completed answer, the cursor blink stops, and all `motion-*` tokens drop to `motion-instant`. The product stays fully usable — just static.

Omitting the three unsourced template cubic-bezier values, keeping the five duration rows as duration tokens rather than easing curves, keeping the four easing-role Use writings, keeping `ease-cursor` `steps / linear`, keeping the recorded signature pairings and reduced-motion rule, holding that those three cubic-bezier values matching the legacy template example are not live-computed evidence, holding the five-kind per-component promotion gate rather than treating a single named curve as sufficient, and holding that official documentation of a single curve or duration is not that gate, are derived editorial implementation inferences from the verified surfaces; they are not Perplexity-authored or a separately published UI specification.

Do not promote a motion duration, easing, animation name, transition, or reduced-motion behavior as a new component token until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, holding that this packet records no Perplexity-authored font licence or public type specimen assigning a general product role, treating FK Grotesk / FK Display / FK Grotesk Neue / Berkeley Mono as the families the source names without a FontFaceSet source URL in this packet, refusing to substitute Inter / Georgia / JetBrains Mono / a system stack while calling the result those families, reading YAML `tokens.typography.family.sans` `FK Grotesk` unmerged from §3 FK Display and FK Grotesk Neue, and reading `tokens.typography.family.mono` `Berkeley Mono` unmerged from the JetBrains Mono fallback, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | No Perplexity-authored font licence or public type specimen assigning a general product role was found in this packet. The families below are the names the source records. |
| Live computed surface-use | Source §4 footer records verification via live DOM getComputedStyle on `https://www.perplexity.ai`. YAML `tokens.source` is `prose-derived`. Family names in the portable body follow the source DESIGN.md (FK Grotesk / FK Display / FK Grotesk Neue / Berkeley Mono), not a sibling-only computed family. |
| FontFaceSet and source corroboration | This packet does not record matching FontFaceSet source URLs for FK Grotesk, FK Display, FK Grotesk Neue, or Berkeley Mono. |
| Official distributed asset | No Perplexity-hosted font-file URL is recorded. |
| Declared-only / fallback | `"Inter"`, `-apple-system`, `BlinkMacSystemFont`, `"Segoe UI"`, `Roboto`, `Georgia`, `"JetBrains Mono"`, `ui-monospace`, `SFMono-Regular`, `Menlo` appear only as CSS fallbacks in observed stacks. They are not substitutes for FK Grotesk, FK Display, FK Grotesk Neue, or Berkeley Mono. |
| License | No Perplexity font-licence URL is recorded. Do not invent one. |

### Family

- **Display / Wordmark:** `"FK Display", "FK Grotesk", Georgia, serif-fallback` — used for the logotype and the largest hero moments only. Not a YAML `tokens.typography.family` key.
- **Primary UI / Headings:** `"FK Grotesk", "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif` — YAML `tokens.typography.family.sans` is `FK Grotesk`.
- **Body / Answer text:** `"FK Grotesk Neue", "FK Grotesk", "Inter", sans-serif` — the Neue cut is optimized for small-size legibility, used for answer bodies and dense reading. Not a YAML family key.
- **Monospace:** `"Berkeley Mono", "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, monospace` — YAML `tokens.typography.family.mono` is `Berkeley Mono`. Code blocks, model names, API snippets.

Do not substitute a system font and call it FK Grotesk, FK Display, FK Grotesk Neue, or Berkeley Mono. Do not present Inter, Georgia, or JetBrains Mono as those families. Keeping YAML `family.sans` `FK Grotesk` unmerged from FK Display and FK Grotesk Neue, keeping YAML `family.mono` `Berkeley Mono` unmerged from JetBrains Mono, and reading computed-family metadata without a FontFaceSet source URL as family metadata rather than as a loadable specimen, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Type roles

YAML writes unitless `lineHeight` values `1.08` / `1.17` / `1.29` / `1.36` / `1.44` / `1.63` / `1.60` / `1.43` / `1.38` / `1.54`. Source §3 writes the same roles with a px line height beside the ratio (`52px (1.08)` and so on). Both writings stay. Unitless ratios stay ratios and are never converted to a replacement px (A1a). YAML `use` strings stay verbatim; longer §3 notes sit beside them. Pairing each YAML role to the token-set path named beside it, keeping YAML `use` verbatim in the Token-set use column, keeping the longer §3 Notes column beside them, keeping Display Hero's `"Where knowledge begins"` and Mono's `` `sonar-pro` `` as the longer §3 writings, keeping heading size `22` beside §3 `22px`, keeping `tokens.typography.answer-body.size` `16` off `tokens.spacing.base: 16`, keeping `tokens.typography.body.size` `15` off a spacing step, keeping `tokens.typography.label.size` `14` off caption `13`, and keeping `tokens.typography.caption.size` `13` unmerged from `tokens.typography.mono.size` `13`, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Token-set use | Notes |
|---|---|---:|---:|---|---|---|---|
| Display Hero | FK Display | 48 | 500 | 1.08 / 52px | -0.02 / -0.02em | Landing hero | Landing hero, "Where knowledge begins". YAML `tokens.typography.display-hero` |
| Display | FK Display | 36 | 500 | 1.17 / 42px | -0.02 / -0.02em | Marketing section headers | YAML `tokens.typography.display` |
| Heading Large | FK Grotesk | 28 | 600 | 1.29 / 36px | -0.01 / -0.01em | Thread title, page headers | YAML `tokens.typography.heading-lg` |
| Heading | FK Grotesk | 22 / 22px | 600 | 1.36 / 30px | -0.01 / -0.01em | Answer section headers, modal titles | YAML `tokens.typography.heading`. Size `22` / `22px` is the YAML step beside the §3 px writing; it is not a spacing step |
| Subtitle | FK Grotesk | 18 | 600 | 1.44 / 26px | normal | Source group labels, card headings | YAML `tokens.typography.subtitle` |
| Answer Body | FK Grotesk Neue | 16 | 400 | 1.63 / 26px | normal | Generated answer text, reading-first leading | The hero — generated answer text, generous leading for reading. YAML `tokens.typography.answer-body`. Size `16` is not `tokens.spacing.base: 16` |
| Body | FK Grotesk Neue | 15 | 400 | 1.60 / 24px | normal | UI descriptions, settings | YAML `tokens.typography.body` |
| Label | FK Grotesk | 14 | 500 | 1.43 / 20px | normal | Buttons, tabs, chips | YAML `tokens.typography.label` |
| Caption | FK Grotesk Neue | 13 | 400 | 1.38 / 18px | normal | Metadata, source domains, timestamps | YAML `tokens.typography.caption`. Size `13` is not mono `13` as a replacement |
| Mono | Berkeley Mono | 13 | 400 | 1.54 / 20px | normal | Code, model IDs | Code, model IDs (`sonar-pro`). YAML `tokens.typography.mono` |

Source typography principles, kept as type rules:

- **Reading-first leading**: Answer body uses ~1.6 line-height — well above typical UI. The product is text, so the text breathes like a magazine column, not a form. YAML answer-body `lineHeight` is `1.63`; ~1.6 is the source's principle wording beside that ratio.
- **Display is rare**: FK Display appears only in the wordmark and the largest marketing moments. The UI itself is almost entirely FK Grotesk / Neue. Restraint keeps the display weight special.
- **Negative tracking on big type**: Headings and display tighten slightly (-0.01 to -0.02em) for an editorial, set-by-hand feel. Body and small sizes stay at normal tracking for legibility.
- **Three weights**: 400 (body), 500 (labels/display), 600 (headings). No black, no thin — the grotesque carries authority at mid-weights.
- **Inline citations are typographic**: Source numbers `[1]` render as superscript teal tokens inside the answer flow, part of the type system rather than separate UI.

Treating those five as type-role rules from the source's typography section rather than as a separately published type specification is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Assets

- Catalog identity: `logo.type: simpleicons`, slug `perplexity`. That pointer is Simple Icons identity, not a captured first-party mark file.
- No first-party hosted font-file URL or font-licence URL is recorded in this packet.

Reading the Simple Icons slug as catalog identity metadata rather than as a Perplexity-hosted mark, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here in full while the catalog graph is not adopted. Preserving that table in full while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification:

| State | Treatment |
|---|---|
| **Empty (new thread)** | Centered composer on Paper, "Ask anything…" placeholder, a few suggested-prompt pills below in `#FCFCF9` with `#E4E4DC` borders. Inviting, never apologetic. |
| **Empty (library)** | Single line of `#5C6A6A` body (`Start a thread to see it here.`) — no illustration. |
| **Loading (generating answer)** | Teal `#20808D` shimmer cursor pulses where text will appear; source cards fade in first, then the answer streams token-by-token. Skeleton lines in `#EFEFE9`. |
| **Streaming** | Answer text writes in left-to-right; a teal block cursor (`#20808D`) trails the last token. Citations pop in as their sources resolve. |
| **Error (query failed)** | Inline notice card: `#E0524A` left border, `Couldn't complete that. Try again.` in `#091717`, a teal retry button. No mascot, no `Oops`. |
| **Error (source unreachable)** | Source card dims to 50% with a `#8A9494` "Unavailable" label; the answer continues with remaining sources. |
| **Success (copied/shared)** | Dark toast (`#091717` bg, `#F2F2ED` text) bottom-center, 3s, `Copied to clipboard`. |
| **Focus mode active** | Selected pill flips to `#E5F2F2` bg / `#20808D` border / `#13343B` text; others stay quiet white. |
| **Skeleton** | `#EFEFE9` (light) / `#1C2128` (dark) blocks at final dimensions, 1.2s shimmer with low-opacity highlight, rounded to component radius. |
| **Disabled** | Buttons drop to 40% opacity; inputs keep `#E4E4DC` border and `#8A9494` text so geometry stays stable. |
| **Hover (source card)** | Border shifts to `#20808D`, card lifts to `0 2px 8px rgba(9,23,23,0.08)`. |

These rows stay as recorded product-level treatments for the states they name; they are not a complete state-coverage claim for every control. Streaming, Focus mode active, and Hover (source card) are additional named observed states; they are not among the seven Core applicability rows and are not copied onto a `focus-visible` row. Composer and Text Field **Focus** (border `#20808D`, ring `0 0 0 3px rgba(32,128,141,0.12)`) is a captured Focus treatment, not a `focus-visible` treatment. The source records no `focus-visible` capture; no `focus-visible` row carries that ring.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. A `Primitive type` line is attached only when the source YAML records that type on that component. Discover / Feed Card is a §4 writing and is labelled `not in the token set`. Every interactive-kind verdict, every applicability verdict, and the reason given for either — including treating Composer and Text Field Focus as a captured Focus treatment rather than a `focus-visible` treatment, treating Streaming / Focus mode active / Hover (source card) / Pressed as additional named observed states rather than Core applicability rows, keeping the §4 footer production URL as an evidence boundary rather than as a token, keeping each YAML `use` string as a Token-set use row, keeping YAML font / padding / radius / bg beside the §4 writings, treating YAML `button-secondary.bg` `#fcfcf9` and §4 Secondary rest `transparent` as two rest writings rather than choosing one, treating YAML `button-ghost.bg` `#efefe9` and §4 Ghost rest `transparent` as two rest writings, treating Ask / Rewrite / Copy / Share as in-place commits, treating Home / Discover / Spaces / Library and Web / Academic / Writing as selectors, treating cited source cards as destination links, and treating citation chips as disclosure — is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification. This is not a complete state-coverage claim.

Source §4 footer, kept as evidence boundary rather than as a token: **Tier 1 sources:** `https://www.perplexity.ai` (live production site, verified via live DOM getComputedStyle).

### Primary (Submit / Ask)

- Role: The ask/submit action, primary CTA on marketing pages
- Token-set use: Ask / submit primary CTA
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled filled button
- Background: `#20808D`. YAML bg: `#20808d`
- Text: `#FFFFFF`. YAML fg: `#ffffff`
- Border: none
- Radius: 10px. YAML radius: `10`
- Padding: 0 18px (height 44px). YAML padding: `0 18px`
- Font: 14px / 500 / FK Grotesk. YAML font: `14px / 500`
- Hover: `#1A6873`
- Pressed: `#13343B`
- Disabled: `#20808D` at 40% opacity
- Observed: default plus named hover / pressed / disabled treatments

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the Ask / submit primary CTA |
| hover | applicable | Pointer-web button; recorded treatment `#1A6873` |
| focus-visible | applicable | Interactive control; visual treatment omitted. The captured composer/field Focus ring is not this row. |
| disabled | applicable | Button control; recorded treatment 40% opacity |
| loading | applicable | Ask / submit is an in-place query commit; visual treatment omitted (product-level generating-answer treatment lives in the capture record) |
| error | applicable | A failed query can be reported on this control; visual treatment omitted (query-failed notice is a product-level treatment) |
| success | applicable | A completed ask can be reported on this control; visual treatment omitted |

Hover `#1A6873` and Pressed `#13343B` are additional named observed states as well as, for hover, the Core hover row's recorded treatment. Pressed is not among the seven Core applicability rows.

### Secondary (Outline)

- Role: Secondary actions next to a primary (e.g. "Rewrite", "Copy")
- Token-set use: Secondary action, 1px #e4e4dc border
- Primitive type: `button` · Kind: interactive
- Anatomy: outlined button
- YAML rest background: `#fcfcf9`. Source §4 rest background: transparent. Both rest writings stay; neither replaces the other. Hover background `#FCFCF9` is the §4 hover fill, not a reason to drop the YAML rest fill.
- Text: `#091717`. YAML fg: `#091717`
- Border: 1px solid `#E4E4DC`
- Radius: 10px. YAML radius: `10`
- Padding: 0 18px (height 44px). YAML padding: `0 18px`
- Font: 14px / 500 / FK Grotesk. YAML font: `14px / 500`
- Hover: background `#FCFCF9`, border `#D6D6CC`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as Rewrite / Copy secondary action |
| hover | applicable | Pointer-web button; recorded treatment background `#FCFCF9`, border `#D6D6CC` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Button control; product-level 40% opacity |
| loading | applicable | Rewrite / Copy is an in-place commit; visual treatment omitted |
| error | applicable | A failed rewrite or copy can be reported on this control; visual treatment omitted |
| success | applicable | Copy is confirmed by the copied/shared toast; visual treatment on this button omitted |

### Ghost / Quiet

- Role: Toolbar actions, icon+label buttons in the thread (Share, Sources, More)
- Token-set use: Toolbar actions (Share, Sources, More)
- Primitive type: `button` · Kind: interactive
- Anatomy: quiet toolbar button
- YAML rest background: `#efefe9`. Source §4 rest background: transparent. Both rest writings stay; neither replaces the other. Hover background `#EFEFE9` is the §4 hover fill, not a reason to drop the YAML rest fill.
- Text: `#5C6A6A`. YAML fg: `#5c6a6a`
- Border: none
- Radius: 8px. YAML radius: `8`. This `8` is not `tokens.rounded.sm: 6`
- Padding: 0 12px (height 36px). YAML padding: `0 12px`
- Font: 14px / 500 / FK Grotesk. YAML font: `14px / 500`
- Hover: background `#EFEFE9`, text `#091717`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as Share / Sources / More toolbar actions |
| hover | applicable | Pointer-web button; recorded treatment background `#EFEFE9`, text `#091717` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Button control; product-level 40% opacity |
| loading | applicable | Share is an in-place commit among the named uses; visual treatment omitted |
| error | applicable | A failed share can be reported on this control; visual treatment omitted |
| success | applicable | Share is confirmed by the copied/shared toast; visual treatment on this button omitted |

### Pill (Focus mode / Filter)

- Role: Search-focus selectors (Web, Academic, Writing), source filters
- Token-set use: Focus mode / filter selector
- Token-set active: `#20808d` border, `#13343b` text, `#e5f2f2` bg
- Primitive type: `badge` · Kind: interactive
- Anatomy: selectable pill
- Background: `#FFFFFF`. YAML bg: `#ffffff`
- Text: `#2E3A3A`. YAML fg: `#2e3a3a`
- Border: 1px solid `#E4E4DC`
- Radius: 9999px. YAML radius: `9999`
- Padding: 6px 14px (height 34px). YAML padding: `6px 14px`
- Font: 13px / 500 / FK Grotesk. YAML font: `13px / 500`
- Active: border `#20808D`, text `#13343B`, background `#E5F2F2`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as Web / Academic / Writing focus-mode pills |
| hover | applicable | Pointer-web pill; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A filter pill can be unavailable; visual treatment omitted |
| loading | not-applicable | This pill selects a focus mode or filter; it does not commit an operation whose pending result this pill would report. |
| error | not-applicable | Same role reason: switching Web / Academic / Writing is not an operation with an error result this pill would report. |
| success | not-applicable | Same role reason: selecting a filter is not an action-outcome confirmation on this pill. |

Focus mode active (`#E5F2F2` / `#20808D` / `#13343B`) is an additional named observed state, not a Core applicability row.

### Composer (the Ask box)

The composer is the signature surface — a large rounded well where the user types.

- Role: The Ask box
- Token-set use: The Ask box, 1px #e4e4dc border, focus #20808d ring
- Primitive type: `input` · Kind: interactive
- Anatomy: value field with inner toolbar
- Background: `#FFFFFF` (light) / `#161B22` (dark). YAML bg: `#ffffff`
- Text: `#091717` / `#F2F2ED`. YAML fg: `#091717`
- Border: 1px solid `#E4E4DC` / `#2A2F37`
- Radius: 16px. YAML radius: `16`
- Padding: 16px 18px. YAML padding: `16px 18px`
- Font: 16px / 400 / FK Grotesk Neue. YAML font: `16px / 400`
- Placeholder: `#8A9494` ("Ask anything…")
- Focus: border `#20808D`, soft ring `0 0 0 3px rgba(32,128,141,0.12)`
- Shadow: `0 1px 3px rgba(9,23,23,0.06)` — YAML `tokens.shadow.ambient`
- Inner toolbar: focus-mode pills left, attach/voice/submit icons right; submit is the teal Primary button

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the Ask box |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted. The captured Focus ring is not this row. |
| disabled | applicable | Input control; product-level treatment keeps `#E4E4DC` border and `#8A9494` text |
| loading | not-applicable | This field presents the query; generating-answer / streaming treatments are product-level, not a pending result this input would report. |
| error | applicable | Form field; no validation treatment observed on this composer (query-failed notice is a product-level card) |
| success | not-applicable | Completing an ask is not a success result this input would report. |

Captured Focus (border `#20808D` + 3px ring) is an additional named observed state, not a `focus-visible` treatment.

### Text Field

- Role: Standard text field
- Token-set use: Standard text field, 1px #e4e4dc border
- Primitive type: `input` · Kind: interactive
- Anatomy: value field
- Background: `#FFFFFF`. YAML bg: `#ffffff`
- Text: `#091717`. YAML fg: `#091717`
- Border: 1px solid `#E4E4DC`
- Radius: 10px. YAML radius: `10`
- Padding: 10px 14px. YAML padding: `10px 14px`
- Font: 15px / 400 / FK Grotesk Neue. YAML font: `15px / 400`
- Placeholder: `#8A9494`
- Focus: border `#20808D`, ring `0 0 0 3px rgba(32,128,141,0.12)`
- Error: border `#E0524A`, ring `rgba(224,82,74,0.12)`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the standard text field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted. The captured Focus ring is not this row. |
| disabled | applicable | Input control; product-level treatment keeps `#E4E4DC` border and `#8A9494` text |
| loading | not-applicable | This field presents a value; it does not commit an operation whose pending result this field would report. |
| error | applicable | Form field; recorded treatment border `#E0524A`, ring `rgba(224,82,74,0.12)` |
| success | not-applicable | Completing a field value is not a success result this input would report. |

### Answer Card

- Role: The container for a generated answer block in a thread
- Token-set use: Generated answer block, 1px #efefe9 border, editorial flat
- Primitive type: `card`
- Kind: non-interactive — generated-answer container; the Ask / citation / source controls around it are the interactive components
- Background: `#FCFCF9`. YAML bg: `#fcfcf9`
- Border: 1px solid `#EFEFE9`
- Radius: 12px. YAML radius: `12`. This `12` is not a YAML `tokens.rounded` key
- Padding: 20px 24px. YAML padding: `20px 24px`
- Shadow: none (separation via warm border, editorial flat)
- No state-applicability map: the card itself is not an interactive control.

### Source Card

- Role: Horizontal scroll rail of cited sources above an answer
- Token-set use: Cited source card, 1px #e4e4dc border
- Primitive type: `card` · Kind: interactive
- Anatomy: favicon 16px + domain (13px `#5C6A6A`) + title (14px `#091717`), 2-line clamp
- Background: `#FFFFFF`. YAML bg: `#ffffff`
- Border: 1px solid `#E4E4DC`
- Radius: 10px. YAML radius: `10`
- Padding: 12px 14px. YAML padding: `12px 14px`
- Shadow: `0 1px 2px rgba(9,23,23,0.05)` — §6 Subtle; not YAML `tokens.shadow.ambient`
- Hover: border `#20808D`, lift to `0 2px 8px rgba(9,23,23,0.08)`
- On mobile: full-width tappable rows

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the cited source card |
| hover | applicable | Pointer-web destination; recorded treatment border `#20808D`, lift `0 2px 8px rgba(9,23,23,0.08)` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A source destination can be unavailable; product-level unreachable treatment dims to 50% with "Unavailable" |
| loading | not-applicable | This card is a cited-source destination; it does not commit an operation whose pending result this card would report. |
| error | not-applicable | Same role reason: opening a cited source is not an operation with an error result this card would report. Unreachable-source dimming is a product-level treatment. |
| success | not-applicable | Same role reason: following a citation is not an action-outcome confirmation on this card. |

### Discover / Feed Card

- Role: Discover-tab editorial story cards
- Primitive type: not in the token set
- Background: `#FCFCF9`
- Border: 1px solid `#EFEFE9`
- Radius: 12px
- Padding: 0 (image top, 16px text)
- Discover story images: 16:9, full-bleed card tops, lazy-loaded
- No YAML `tokens.components` key and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Citation Chips

- Role: Inline source references inside answer text; hover reveals a source popover
- Token-set use: Inline source reference
- Primitive type: `badge` · Kind: interactive
- Anatomy: inline chip or superscript `[1]`
- Background: `#E5F2F2` (light) / `rgba(52,180,196,0.16)` (dark). YAML bg: `#e5f2f2`
- Text: `#13343B` / `#34B4C4`. YAML fg: `#13343b`
- Border: none
- Radius: 6px. YAML radius: `6`
- Padding: 1px 6px. YAML padding: `1px 6px`
- Font: 11px / 600 / FK Grotesk (or superscript `[1]` inline). YAML font: `11px / 600`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as inline source references |
| hover | applicable | Pointer-web disclosure; recorded pairing `motion-fast / ease-enter` with a light backdrop blur |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A citation target can be unavailable; visual treatment omitted |
| loading | not-applicable | This chip discloses a source preview; it does not commit an operation whose pending result this chip would report. |
| error | not-applicable | Same role reason: opening a citation preview is not an operation with an error result this chip would report. |
| success | not-applicable | Same role reason: a citation preview is not an action-outcome confirmation on this chip. |

### Pro / Model Badge

- Role: "PRO", model labels (`Sonar`, `GPT-5`, `Claude`)
- Token-set use: PRO / model labels
- Primitive type: `badge`
- Kind: non-interactive — status/model label, not a commit control
- Background: `#13343B`. YAML bg: `#13343b`
- Text: `#FFFFFF`. YAML fg: `#ffffff`
- Border: none
- Radius: 6px. YAML radius: `6`
- Padding: 2px 8px. YAML padding: `2px 8px`
- Font: 11px / 600 / FK Grotesk. YAML font: `11px / 600`
- No state-applicability map: the badge itself is not an interactive control.

### Status Badge (Weak)

- Role: "New", "Beta", focus-mode labels
- Token-set use: New / Beta / focus-mode labels
- Primitive type: `badge`
- Kind: non-interactive — status label, not a commit control
- Background: `#E5F2F2`. YAML bg: `#e5f2f2`
- Text: `#20808D`. YAML fg: `#20808d`
- Border: none
- Radius: 6px. YAML radius: `6`
- Padding: 2px 8px. YAML padding: `2px 8px`
- Font: 11px / 600 / FK Grotesk. YAML font: `11px / 600`
- No state-applicability map: the badge itself is not an interactive control.

### Top Nav Tab

- Role: Home / Discover / Spaces / Library navigation
- Token-set use: Home / Discover / Spaces / Library
- Token-set active: `#091717` text, 2px bottom border `#20808d`
- Primitive type: `tab` · Kind: interactive
- Anatomy: text tab with bottom border when active
- Inactive: text `#5C6A6A`, transparent. YAML fg: `#5c6a6a`
- Active: text `#091717`, 2px bottom border `#20808D`
- Font: 14px / 500 / FK Grotesk. YAML font: `14px / 500`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as Home / Discover / Spaces / Library |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav tab can be unavailable; visual treatment omitted |
| loading | not-applicable | This tab selects Home / Discover / Spaces / Library; it does not commit an operation whose pending result this tab would report. |
| error | not-applicable | Same role reason: switching a nav tab is not an operation with an error result this tab would report. |
| success | not-applicable | Same role reason: panel selection is not an action-outcome confirmation on this tab. |

### Toast

- Role: "Copied", "Shared", transient confirmations — bottom-center, 3s dismiss
- Token-set use: Copied / Shared transient confirmations
- Primitive type: `toast`
- Kind: non-interactive — transient confirmation surface; auto-dismiss 3s
- Background: `#091717`. YAML bg: `#091717`
- Text: `#F2F2ED`. YAML fg: `#f2f2ed`
- Border: none
- Radius: 10px. YAML radius: `10`
- Padding: 12px 16px. YAML padding: `12px 16px`
- Shadow: `0 4px 16px rgba(9,23,23,0.18)` — this `0.18` is the §4 writing; it is not §6 Elevated (3) `0.12`
- Font: 14px / 400 / FK Grotesk Neue. YAML font: `14px / 400`
- No state-applicability map: the toast itself is not an interactive control.

### Centered Modal

- Role: Sign-in, settings, share, upgrade prompts
- Token-set use: Sign-in, settings, share, upgrade modal
- Primitive type: `dialog` · Kind: interactive
- Anatomy: centered modal on a scrim
- Background: `#FBFAF4`. YAML bg: `#fbfaf4`
- Text: `#091717`. YAML fg: `#091717`
- Border: none
- Radius: 16px. YAML radius: `16`
- Padding: 28px. YAML padding: `28px`
- Shadow: `0 16px 48px rgba(9,23,23,0.20)` — YAML `tokens.shadow.elevated`
- Backdrop: `rgba(9,23,23,0.40)`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as sign-in, settings, share, upgrade prompts |
| hover | applicable | Pointer-web dialog; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A dialog action can be unavailable; visual treatment omitted |
| loading | applicable | Sign-in and upgrade are in-place commits this dialog can report; visual treatment omitted |
| error | applicable | A failed sign-in or upgrade can be reported in this dialog; visual treatment omitted |
| success | applicable | A completed sign-in or upgrade can be reported in this dialog; visual treatment omitted |

### Switch

- Role: Settings (e.g. AI Profile, dark mode, auto-suggestions)
- Token-set use: Settings switch, #ffffff thumb
- Primitive type: `toggle` · Kind: interactive
- Anatomy: track + thumb
- On: `#20808D` track / off: `#D6D6CC` track. YAML bg: `#20808d`
- Thumb: `#FFFFFF` 18px circle, `0 1px 2px rgba(9,23,23,0.20)`
- Radius: 9999px. YAML radius: `9999`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the settings switch |
| hover | applicable | Pointer-web toggle; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A settings switch can be unavailable; visual treatment omitted |
| loading | not-applicable | This switch selects an on/off setting; it does not commit an operation whose pending result this switch would report. |
| error | not-applicable | Same role reason: flipping AI Profile / dark mode / auto-suggestions is not an operation with an error result this switch would report. |
| success | not-applicable | Same role reason: a settings flip is not an action-outcome confirmation on this switch. |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Source layout writings, kept as recorded. Reading those measurements as the source's layout notes rather than as a complete published grid, refusing to treat `tokens.spacing.base: 16` as answer-body `16`, and refusing to treat breakpoint names Mobile / Tablet / Desktop / Wide as live computed mobile captures, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

### Spacing and containers

- Base unit: 4px
- Common values: 4, 8, 12, 16, 20, 24, 32, 40, 48, 64px
- Reading column gets generous vertical rhythm (24–32px between answer blocks)
- Centered single reading column, max-width ~768px for answer/thread content (a magazine measure)
- Marketing pages: 1200px max container with 24px gutters
- Three-zone app shell: collapsible left rail (nav/library), centered content column, optional right rail (sources/related)
- Discover feed: responsive 1–3 column masonry of story cards

### Whitespace

- **The answer breathes**: Generated text gets a constrained measure (~68ch) and ~1.6 leading so it reads like an article, never a wall.
- **Quiet chrome**: Toolbars and nav sit in muted ink grays so they recede; only the teal and the answer text earn the user's eye.
- **Source rails are calm**: Citations live in a horizontal scroll above or beside the answer, never crowding the prose.

### Responsive behavior

The source records this breakpoint table. It is the source's responsive writing, not a live computed breakpoint capture.

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, left rail becomes a drawer, composer docks to bottom |
| Tablet | 640–1024px | Reading column + collapsible left rail, source rail moves above answer |
| Desktop | 1024–1440px | Three-zone shell, source rail can sit right |
| Wide | >1440px | Centered content caps at ~768px reading width, rails widen |

Touch targets the source records:

- Buttons: 44px primary, 36px ghost — comfortable thumb targets on mobile
- Composer submit: 40px circular/rounded target, always reachable
- Source cards: full-width tappable rows on mobile

Collapsing strategy the source records:

- Left navigation rail collapses to icon-only, then to a hamburger drawer on mobile
- Source rail: right-side on desktop → horizontal scroll strip above the answer on mobile
- Related questions: sidebar list on desktop → stacked chips below the answer on mobile
- Composer: inline on desktop, sticky-bottom with safe-area inset on mobile

Image behavior the source records:

- Source favicons: fixed 16px
- Discover story images: 16:9, full-bleed card tops, lazy-loaded
- Generated/answer images: responsive, max content-width, rounded 10px

<!-- design-md:section content-locales -->
## 6. Content & Locales

Perplexity speaks like a sharp, well-read research assistant: direct, neutral, citation-obsessed, and allergic to hype. The product's core promise is trust, so the copy never overclaims — it answers, then shows its sources. Sentences are clean and declarative. There's a quiet curiosity to the brand voice (the tagline lineage is literally "Where knowledge begins"), but it stays understated; Perplexity would rather be right than clever. Characterizing that voice as a sharp, well-read research assistant rather than as a separately published copy manual, requiring the quoted strings below byte-exact, and treating the table as the source's own context/tone record, is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

| Context | Tone |
|---|---|
| Composer placeholder | Open invitation: `Ask anything…` |
| CTAs | Plain verbs: `Ask`, `Search`, `Sources`, `Rewrite`, `Share` |
| Answers | Neutral, structured, citation-backed. Confident but hedged when evidence is thin (`Based on available sources…`). |
| Empty states | Curious and inviting, never apologetic (`Start a thread to see it here.`). |
| Errors | Plain and actionable (`Couldn't reach that source. Try again.`). No `Oops`, no cute mascots. |
| Marketing | Editorial, restrained, idea-led. Lets whitespace and the wordmark carry weight. |
| Upgrade prompts | Value-first, low-pressure (`Pro gives you more sources and stronger models.`). |

Further recorded strings, kept byte-exact: `Copy`; `More`; `Copied`; `Shared`; `Copied to clipboard`; `Couldn't complete that. Try again.`; `Unavailable`; `PRO`; `New`; `Beta`; `Home`; `Discover`; `Spaces`; `Library`; `Web`; `Academic`; `Writing`; `Sonar`; `GPT-5`; `Claude`; `sonar-pro`; `Where knowledge begins`.

**Forbidden phrases.** No `Oops`, no `magic`, no `revolutionary`, no exclamation-stacked hype, no emoji in answers. Never present a synthesized claim without a path to its source — unsourced confidence is the one thing the brand will not do.

Reproduce those quoted strings byte-exact rather than translating or re-casing them.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Listing them as unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Perplexity-authored or a separately published UI specification.

- unsourced easing curve values for `ease-enter`, `ease-exit`, and `ease-standard` — promote a motion value for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed
- `focus-visible` visual treatment (composer/text-field Focus ring is a different evidence class)
