# BMW Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

BMW makes premium automobiles, motorcycles, and mobility services. Catalog homepage identity is `https://www.bmw.com`. Treating the following three BMW USA URLs as the named live-inspect evidence domains of this reconstruction, not treating that catalog homepage URL as a captured page, and not turning those routes into a claim about an authenticated vehicle, account, dealer, or in-car interface, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. This contract covers three current public BMW USA product-marketing surfaces from the 2026-07-13 capture: the public home `https://www.bmwusa.com/`, the model catalog `https://www.bmwusa.com/vehicles/all-bmw-models.html`, and the build-your-own route `https://www.bmwusa.com/build-your-own.html#/`. It does not turn those routes into a claim about an authenticated vehicle, account, dealer, or in-car interface.

The following recorded-home-treatment readings, including small-number-of-blue-primary-actions, white-or-transparent-navigation-controls, light-high-line-height-BMW-Type-text, and surface-scoped-rather-than-a-fictional-global-palette, are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification. The recorded home treatment pairs a small number of blue primary actions with white or transparent navigation controls and light, high-line-height BMW Type text. The catalogue and configurator share the same family but do not resolve every color identically, so the values remain surface-scoped rather than being blended into a fictional global palette.

BMW’s official 2020 identity update describes a digitally oriented brand presence built around openness, clarity, visual restraint, and graphic flexibility. Treating the following public-history and current-evolution facts as narrative rather than interface tokens, including Neue Klasse carrying that 2020 direction into a reduced vehicle-design language, 7 March 1916 as the foundation date of today’s BMW AG, roots in Karl Rapp and Gustav Otto, the 1916 merger of the Flugmaschinenfabrik Gustav Otto into Bayerische Flugzeug-Werke AG, a company that now spans BMW, MINI, Rolls-Royce, and BMW Motorrad, the 2020 communication identity’s pared-down two-dimensional digitally focused presence, Neue Klasse electrification / digitalisation / circularity / first series-production iX3, and Neue Klasse as an update to the brand’s driving-pleasure proposition, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Its current Neue Klasse programme carries that direction into a new, reduced vehicle-design language. The 2020 communication identity moved toward a pared-down, two-dimensional, digitally focused presence centred on openness and clarity. The Neue Klasse programme positions electrification, digitalisation, circularity, and a reduced new design language as an update to the brand’s driving-pleasure proposition, with the first series-production iX3 as the beginning of that new model generation. Official history, the 2020 brand-design announcement, and Neue Klasse context inform this narrative, not the live web tokens.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three captured BMW USA routes below as the independently verified user outcomes, and not lifting tasks from source §13 stakeholder groups, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

- Use the BMW USA home primary CTA and paired outline CTA.
- Scan the all-BMW-models catalog.
- Start a vehicle on the BMW USA build-your-own route.
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, source §13 source-backed stakeholder groups are not primary tasks and not user-flow requirements, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. No individual personas are promoted. Source §13 names source-backed stakeholder groups, not fictional customers or demographic personas. Use those groups only; they are not user-flow requirements.

- **Prospective BMW customer:** the 2020 brand announcement says BMW puts the customer at the centre of the revised identity and invites customers into the world of BMW.
- **Official BMW Club representative:** the Club CI guideline addresses official clubs as brand ambassadors and sets mandatory rules for their communications; it is not a public-product UI persona.
- **BMW Group design collaborator:** BMW Group Design describes a global design organisation working across its automotive brands; this informs brand context, not a user-flow requirement.

### Distinctive traits

Treating `#1c69d4` as the observed home CTA fill and catalog `primary_color` rather than a generic semantic blue, treating 0px navigation chrome and 3px CTA corners as two measured geometries rather than a universal radius, treating home `#414141` and configurator `#262626` as unmerged foregrounds, treating on-dark navigation `#ffffff` as unmerged from primary-CTA on-fill `#ffffff`, treating `#f2f2f2` as the paired outline-action text and inset outline rather than a canvas, treating `#666666` as the build-your-own flyout label rather than a third foreground token, treating 0px navigation chrome as mostly-square-navigation-chrome rather than a universal square system, and treating hover, focus, pressed, modal, or responsive systems as not promoted from this capture, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

- A single observed blue home CTA fill, `#1c69d4`
- `bmwTypeNextWeb` as the loaded, computed public-web family
- Mostly square navigation chrome, with 3px CTA corners where measured
- Separate home and configurator foreground values rather than one assumed neutral scale
- No promoted hover, focus, pressed, modal, or responsive system from this capture

### Principles

The three numbered items’ source-backed stems cite BMW’s 2020 brand-design announcement (openness, clarity, restraint, graphic flexibility) and BMW Group Neue Klasse materials (driving pleasure into a digital future). The *UI implication* notes — hierarchy-and-calls-to-action-legible-before-decorative-treatment, reuse-verified-primitives-only-where-surface-provenance-matches / do-not-flatten-routes-into-one-speculative-kit, and foreground-product-imagery-and-digital-action-without-claiming-in-car-UI — and the capture-bound application list are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification.

1. **Communicate with openness and clarity.** *UI implication:* make hierarchy and calls to action legible before adding decorative treatment. Source: BMW’s 2020 brand-design announcement.
2. **Use restraint with flexibility across touchpoints.** *UI implication:* reuse verified primitives only where their surface provenance matches; do not flatten different routes into one speculative kit. Source: BMW’s 2020 brand-design announcement.
3. **Carry driving pleasure into a digital future.** *UI implication:* a BMW-inspired public page can foreground product imagery and digital action, but it must not claim in-car UI behavior without product evidence. Source: BMW Group Neue Klasse materials.

Treating the following as a capture-bound application of source §7 Do’s and the BMW USA capture, including home-primary-tied-to-observed-CTA, two-measured-geometries, `bmwTypeNextWeb`-metadata-attached-to-BMWUSA-web-evidence, lawful-fallback-only-when-the-proprietary-face-cannot-be-loaded, and home-versus-configurator-navigation-scoped-to-recorded-surfaces, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

- Keep the home primary action tied to the observed `#1c69d4` CTA rather than treating it as a generic semantic blue.
- Preserve the measured distinction between 0px navigation controls and 3px paired CTAs when recreating the captured home pattern.
- Keep `bmwTypeNextWeb` metadata attached to BMWUSA web evidence; use a lawful fallback only when the proprietary face cannot be loaded.
- Scope home and configurator navigation values to their recorded surfaces.

### Avoid

The following items copy source §7 Don’ts. Their extra causal readings — unobserved-interaction-not-claimed, declared-only-faces-not-UI-typography, BMW-hosted-files-not-a-public-downstream-licence, and consent-banner-or-chatbot-not-BMW-component-tokens — are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification.

- Don't claim unobserved hover, focus, pressed, disabled, error, or menu-open behavior.
- Don't promote declared-only Arial MT, `bmw_next_icons`, or SangBleuKingdom into BMW UI typography.
- Don't reuse BMW-hosted font files as though the capture supplied a public downstream licence.
- Don't turn consent-banner or chatbot records into BMW component tokens without source-specific evidence. The last item’s consent-banner-or-chatbot-not-BMW-component-tokens reading is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including home-CTA-fill-not-generic-semantic-blue, home-foreground-not-configurator-foreground, on-dark-navigation-not-primary-CTA-on-fill, outline-action-not-a-canvas, no-reusable-white-canvas-fill, `#666666`-flyout-label-not-a-YAML-color-role, no-semantic-success-warning-error-disabled-hover-or-focus-color-claimed, general-dark-surface-token-omitted-rather-than-inferred, system-wide-BMW-roundel-color-role-omitted-rather-than-inferred, and omitted-instead-of-inferred-from-marketing-imagery-or-logos, are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification.

- **Primary action** (`#1c69d4`): home `.cmp-button` background. Catalog `primary_color`. Not a generic semantic blue.
- **Home foreground** (`#414141`): home list and dialog chrome; it is not substituted for the configurator value.
- **Configurator foreground** (`#262626`): build-your-own navigation and local chrome. Not home `#414141`.
- **On-dark navigation** (`#ffffff`): home global-navigation control text. Same hex as primary-CTA on-fill; named jobs stay unmerged.
- **Outline action** (`#f2f2f2`): inset-outline and text color of the paired home CTA.

No semantic success, warning, error, disabled, hover, or focus color is claimed. The collector did not observe a reusable white canvas fill, a general dark-surface token, or a system-wide BMW roundel color role, so those values are omitted instead of inferred from marketing imagery or logos. `#666666` is the build-your-own flyout trigger’s label on that control, not a YAML color role.

### Spacing

YAML `spacing` is unitless: space-4 4, space-12 12, space-24 24, space-32 32. Treating those YAML numbers as unitless token numbers rather than a claimed BMW spacing scale, while keeping the §5 observed px roles as a separate observation, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

The following observed-px readings, including 4px-as-CTA-inset-padding, 12px-as-global-navigation-horizontal-padding, 24px-as-recurrent-control-or-list-line-height-or-spacing, 32px-as-home-list-margins, observed-values-rather-than-a-declared-BMW-spacing-scale, harvested-control-padding-staying-with-those-controls, and no-grid-container-width-or-breakpoint-from-these-repetitions, are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification. The supplied desktop capture repeatedly records 4px, 12px, 24px, and 32px values. They are exposed as observed values rather than a declared BMW spacing scale: 4px is the CTA inset padding, 12px is global-navigation horizontal padding, 24px is recurrent control/list line-height or spacing, and 32px occurs in home list margins. Harvested control padding stays with those controls, not as a universal padding token. No grid, container width, or breakpoint is established by these repetitions.

### Shape

YAML `rounded`: none 0, control 3.

The following local-geometry reading, including None-as-sampled-global-navigation and Control-as-sampled-paired-home-CTAs, two-measured-geometries-not-every-BMW-surface-square, and 3px-not-a-universal-corporate-radius, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

- None (0): sampled global-navigation controls
- Control (3): sampled paired home CTAs

Treating 0 and 3 as local harvested geometry rather than a universal radius for every unlisted control, and treating the two measured geometries as not a claim that every BMW surface is square or that 3px is a universal corporate radius, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. The evidence therefore supports two measured geometries, not a claim that every BMW surface is square or that 3px is a universal corporate radius. 0 and 3 remain local harvested geometry, not a universal radius for every unlisted control.

### Elevation

The following table Use assignments (observed-CTA-and-global-navigation-only), chatbot-shadow-not-an-elevation-token, and no-reusable-panel-card-or-modal-elevation-system readings are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification. A low-confidence chatbot record contains a shadow, but it is not used to define an elevation token or a general BMW component rule. No reusable panel, card, or modal elevation system was collected.

| Level | Treatment | Use |
|---|---|---|
| Flat | `box-shadow: none` | Observed BMW CTA and global-navigation controls |

### Motion

No BMW public-web duration, easing, or reduced-motion rule was captured or located in the official sources reviewed for this update. Treating that absence as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Motion tokens are intentionally omitted.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings of the source font evidence classes Live computed surface-use, Live computed but limited use, Official distributed brand asset, Declared-only, and Licence boundary — including `bmwTypeNextWeb` as the sole UI family token rather than a second generic family from limited `BMWTypeNext` use, Club CI as official brand-asset context rather than captured BMWUSA product UI, declared-only faces as not UI-family tokens, licence-boundary-browser-use-only, and Club-guideline-not-a-downstream-webfont-licence — are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use, FontFaceSet, and source corroboration | `bmwTypeNextWeb` is loaded with high confidence, appears on 288 captured elements across body, headings, navigation, buttons, dialogs, and list items, and is backed by 52 BMWUSA-hosted WOFF/WOFF2 source URLs. It is the sole UI family token. |
| Live computed but limited use | `BMWTypeNext` is also loaded with high confidence (five captured uses) from six BMWUSA-hosted BMWTypeNextLatin files. Its limited observed role is recorded here but it is not made a second generic UI-family token. |
| Official distributed brand asset | BMW’s official Club CI guideline specifies BMW TypeNext Bold and Regular for club identifiers. That establishes an official brand-asset context, not an assertion that the club guideline defines the captured BMWUSA product UI. |
| Declared-only | Arial MT, `bmw_next_icons`, and SangBleuKingdom have captured `@font-face` declarations but zero visible uses. They are not UI-family tokens. |
| Licence boundary | The collected BMWUSA WOFF/WOFF2 files corroborate browser use only. The public BMW Club guideline governs official-club brand materials and does not grant a downstream webfont licence; no public first-party reuse licence was located in this pass. |

The following font-use boundary, including system-stack-not-`bmwTypeNextWeb`, no-replacement-family, and `BMWTypeNext`-not-a-second-generic-UI-family, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Do not present a system stack as `bmwTypeNextWeb`. Do not replace `bmwTypeNextWeb` with a different claimed family. Do not present `BMWTypeNext` as a second generic UI-family token.

### Family

- **Current visible UI family:** `bmwTypeNextWeb` — YAML `family.ui`
- **Limited loaded companion:** `BMWTypeNext` — five captured uses; not a second generic UI-family token

### Type roles

Verified YAML line-height values are the unitless ratios `1.43`, `1.56`, `1.6`, and `1.5`. The following ratio-versus-size-local and omitted-tracking-not-invented readings, including ratios-scale-with-font-size-not-fixed-px, 50px-beside-Display-heading-1.43, 28px-beside-Body-copy-1.56, 24px-beside-Control-text-1.6, and 30px-beside-Icon-flyout-text-1.5, are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. 50px beside Display heading `1.43`, 28px beside Body copy `1.56`, 24px beside Control text `1.6`, and 30px beside Icon-flyout text `1.5` are size-local observations, not replacements for the ratios. YAML roles without a tracking field stay without one; none is invented.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Use (YAML) |
|---|---|---:|---:|---:|---|---|
| Display heading | bmwTypeNextWeb | 35px | 300 | 1.43 | 50px at this size | Observed home heading only. |
| Body copy | bmwTypeNextWeb | 18px | 300 | 1.56 | 28px at this size | Observed home body copy only. |
| Control text | bmwTypeNextWeb | 15px | 500 | 1.6 | 24px at this size | Observed home button and navigation control text only. |
| Icon-flyout text | bmwTypeNextWeb | 20px | 300 | 1.5 | 30px at this size | Observed home navigation icon-flyout text only. |

### Assets

Treating catalog logo metadata as Simple Icons identity (`bmw`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

No BMW product state treatment is asserted. The supplied collector reports zero observed states, zero interaction kinds, and zero interaction snapshots. Empty, loading, error, success, skeleton, and disabled treatments require a future selector-level public-surface capture.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 collector-zero rows. This is not a complete state-coverage claim.

Treating YAML `tokens.components` as empty (`components_harvested: false`) so values below stay body §4 only and Type is not invented, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. YAML `tokens.components` is empty (`components_harvested: false`). Values below are body §4 only. Type is not invented.

### Home primary CTA

- Role: home primary call-to-action
- Kind: interactive
- Anatomy: label
- Background: `#1c69d4`
- Text: `#ffffff`
- Radius: 3px
- Padding: 4px
- Height: 52px
- Font: 15px / 500 / bmwTypeNextWeb
- Use: home `.cmp-button`; 52px rendered height. No state snapshot was supplied.
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-generic-semantic-blue and on-fill-not-on-dark-navigation, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. `#1c69d4` is this control’s fill and catalog `primary_color`. It is not a generic semantic blue. `#ffffff` is this control’s on-fill label, not on-dark navigation text.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the home primary `.cmp-button` |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A call-to-action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the home primary CTA; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 collector-zero rows.

### Home outline CTA

- Role: paired home outline call-to-action
- Kind: interactive
- Anatomy: label
- Background: transparent
- Text: `#f2f2f2`
- Radius: 3px
- Padding: 4px
- Height: 52px
- Font: 15px / 500 / bmwTypeNextWeb
- Use: home `.cmp-button`; transparent fill with `#f2f2f2` 1px inset outline and 52px rendered height. No state snapshot was supplied.
- Observed: default only
- Field note: The following unmerged-field reading, including outline-pair-not-home-foreground and transparent-fill-not-a-canvas, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. `#f2f2f2` is this control’s text and 1px inset outline. It is not home foreground `#414141`. Transparent fill is this control’s field, not a reusable canvas token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the paired home outline `.cmp-button` |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A paired action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the paired outline action; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 collector-zero rows.

### Home flyout trigger

- Role: home global-navigation flyout trigger
- Kind: interactive
- Anatomy: label
- Text: `#ffffff`
- Radius: 0px
- Padding: 0px 12px
- Height: 84px
- Font: 15px / 500 / bmwTypeNextWeb
- Use: home `.cmp-globalnavigation__interaction--flyout`; 84px rendered height. No menu-open state was observed.
- Observed: default only
- Field note: The following unmerged-field reading, including on-dark-not-primary-CTA-on-fill, 0px-not-CTA-3px, and 84px-not-CTA-52px, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. `#ffffff` is this control’s label and YAML on-dark. Same hex as primary-CTA on-fill; jobs stay unmerged. 0px radius is this control, not CTA 3px. 84px height is this flyout, not CTA 52px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the home flyout trigger |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A navigation flyout can be unavailable; visual treatment omitted |
| loading | not-applicable | Flyout trigger opens a menu; it is not a request-in-flight control |
| error | not-applicable | Flyout trigger is not a form or request-outcome control |
| success | not-applicable | Flyout trigger is not a completion control |

Treating menu-open as a named unobserved state rather than a computed paint on this trigger is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Additional observed named state: menu-open, named as unobserved. That name is not copied here as a computed paint.

### Build-your-own flyout trigger

- Role: build-your-own global-navigation flyout trigger
- Kind: interactive
- Anatomy: label
- Text: `#666666`
- Radius: 0px
- Padding: 0px 12px
- Height: 84px
- Font: 15px / 500 / bmwTypeNextWeb
- Use: build-your-own `.cmp-globalnavigation__interaction--flyout`; 84px rendered height. This surface-specific value is not a home-navigation variant.
- Observed: default only
- Field note: The following unmerged-field reading, including this-label-not-home-foreground, not-configurator-foreground, not-on-dark, and surface-specific-not-a-home-navigation-variant, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. `#666666` is this control’s label. It is not home foreground `#414141`, not configurator foreground `#262626`, and not on-dark `#ffffff`. The Use line’s surface-specific value is not a home-navigation variant.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the build-your-own flyout trigger |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A navigation flyout can be unavailable; visual treatment omitted |
| loading | not-applicable | Flyout trigger opens a menu; it is not a request-in-flight control |
| error | not-applicable | Flyout trigger is not a form or request-outcome control |
| success | not-applicable | Flyout trigger is not a completion control |

Treating menu-open as a named unobserved state rather than a computed paint on this trigger is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Additional observed named state: menu-open, named as unobserved. That name is not copied here as a computed paint.

Treating inputs, cards, badges, dialogs, chatbot chrome, and menu panels as omitted surfaces without inventing kind or a state-applicability map, and treating OneTrust dialog and close-button records as third-party consent chrome rather than BMW product components, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Inputs, cards, badges, dialogs, chatbot chrome, menu panels, and state variants are omitted: the supplied artifact has no representative BMW selector/state evidence for them. OneTrust dialog and close-button records are third-party consent chrome, not BMW product components. Kind and a state-applicability map are not invented for those omitted surfaces.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing space-4 4, space-12 12, space-24 24, space-32 32. Harvested heights: 52px paired home CTAs, 84px flyout triggers. 3px radius on paired home CTAs; 0px on sampled global-navigation controls.

The following layout readings — two measured geometries rather than one square system, 4px / 12px / 24px / 32px as observed repetitions rather than a declared BMW spacing scale, and 52px / 84px as component heights rather than a layout token set — are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification.

Treating the supplied 1440×900 desktop collector run as not substantiating mobile breakpoints, navigation collapse, touch targets, or responsive spacing rules, treating no-responsive-token-or-rule as the application of that limit, and treating 52px and 84px as desktop-capture measurements rather than a cross-viewport specification, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification. Only a 1440×900 desktop collector run was supplied. It does not substantiate mobile breakpoints, navigation collapse, touch targets, or responsive spacing rules; no responsive token or rule is asserted.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The following voice readings — concise-confident-language-that-makes-the-next-action-legible, product-image-or-technical-detail-carries-the-rest, brand-communication-context-not-unobserved-product-error-copy, Brand-communication-openness-clarity-strength-of-character, Future-mobility-electric-digital-circular-driving-pleasure, and Controls-direct-labels-within-observed-surface-evidence — are a derived editorial implementation inference from the verified surfaces; they are not BMW-authored or a separately published UI specification. BMW’s official 2020 communication-design announcement describes the brand update as open, clear, customer-centred, and suited to a digital future. For source-backed BMW-inspired communication, use concise, confident language that makes the next action legible and lets a product image or technical detail carry the rest. This is brand-communication context, not evidence for unobserved product error copy.

| Context | Source-backed direction |
|---------|-------------------------|
| Brand communication | Openness, clarity, and strength of character from the 2020 brand-design announcement |
| Future mobility | Electric, digital, circular, and driving-pleasure framing from Neue Klasse materials |
| Controls | Use direct labels whose visual treatment stays within the observed surface-specific component evidence |

Not promoting synthetic voice samples, and treating the §14 collector-zero rows as the state contract rather than extra voice samples, is a derived editorial implementation inference from the verified surfaces; it is not BMW-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- semantic success, warning, error, disabled, hover, and focus colors
- reusable white canvas fill, general dark-surface token, and system-wide BMW roundel color role
- hover, pressed, disabled, loading, error, success, empty, skeleton, focus, and menu-open visual treatments
- Home primary CTA / Home outline CTA loading·error·success applicability (exact request/outcome unresolved)
- inputs, cards, badges, dialogs, chatbot chrome, and menu panels
- mobile breakpoints, navigation collapse, touch targets, and responsive spacing
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
- public downstream webfont licence
- product error copy
- authenticated vehicle, account, dealer, and in-car interface
