# hana migration log

Source: `web/references/hana/DESIGN.md`
Sibling read (not the migration input): `web/references/hana/.verification.md`
Destination: `docs/design-md-weight/migrated/hana/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hana/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `b742309347a359fc4ea46d9e99a77256541db8792b28ea1a658583774e650915` (`web/references/hana/DESIGN.md`). Sibling SHA-256 `f60cf2b25ff4533ac9c9e824da5fbb8b7d63ba08209e11ab8139c4286c5597d7` (`web/references/hana/.verification.md`). Worker-close portable DESIGN SHA-256 `d970eb4386252f988472c7d108266d8f915d77ef4e8541692c504c0a0ebcd161`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `display_name_kr` 옮김 → Scope + Content | Portable file has no frontmatter. H1 is `# Hana Bank Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://www.kebhana.com` is dual: `DESIGN.md` Scope + `provenance.md` Identity/Surfaces (E2a). `#00a39f` is dual: dest 13 / provenance 5. `하나은행` is dual: dest + provenance Identity. The favicon slug is provenance-only (Assets says so; `DESIGN.md` 224 + `provenance.md` Logo decision). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source token note is transcribed verbatim in Identity. The source footer's producer string `omd:add-reference CREATE — Tier 1 live inspect` is in Freshness. These are ledger keys with no portable slot. |
| YAML `tokens.colors` (14 keys) | 옮김 → Foundations semantic color | `DESIGN.md` Semantic color (84–113). All fourteen roles kept with names and values: primary `#00a39f`, primary-deep `#008485`, primary-group `#009178`, primary-light `#2dc396`, canvas `#ffffff`, surface `#f8f8f8`, surface-tint `#f2f9f9`, ink `#333333`, body `#555555`, muted `#666666`, muted-alt `#999999`, on-primary `#ffffff`, hairline `#dbdbdb`, dark `#333333`. Paths named `tokens.colors.<key>`. `#465e6f` is a source-body housing-finance color, not a YAML key; it is dual: dest 2 / provenance 3 (E2a). `#f2f9f9` dest 4 / provenance 0 — this log does not invent a provenance second home. |
| YAML `tokens.typography.family` (`primary: "NotoSans"`, `body: "NotoSans_Regular"`, `group: "Pretendard Variable"`) | 옮김 → Typography & Assets Family | `DESIGN.md` Family (192–198). Paths named `tokens.typography.family.primary` / `body` / `group`. The fallback prohibition on 198 carries an adjacent complete qualifier (B2/B2a). Declared-only fallback-members-are-not-the-brand-face reading is qualified on the Font evidence row at 188. |
| YAML `tokens.typography.heading / nav / body / footer / group-body / group-cta` | 옮김 → Typography & Assets Type roles | `DESIGN.md` Type roles (200–212). Unitless line heights stay ratios and are never converted to px (A1a): `1.56` dest 2 · `1.5` dest 4 · `1.4` dest 3. Group-body also keeps the source's own `19.6px` (1.4) spelling dest 2. All six YAML `use` strings restored verbatim in the Token-set use column (A1, A3). |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Paths kept separate (easywallet-type check): Spacing slot `DESIGN.md` 117 `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 20` · `xl: 24` · `xxl: 40` · `section: 48`. Shape slot 125 `sm: 6` · `md: 10` · `lg: 20` · `full: 9999`. The source body also spells this step `9999px`; both forms are kept. `base: 16` is the spacing path (117/119). `full: 9999` dest 3 is the radius path (125/132/454). `9999px` dest ≥ 1. `lg: 20` appears on both scales because the source wrote both; they stay on separate path lines. Layout restates both scales at 453–454. Component `27px` and badge `4px` stay on those components and are not merged into `tokens.rounded`. Spacing qualifier at 121 names the not-a-grid / `base: 16` ≠ radius readings. Shape qualifier at 132 names the sit-on-component reading. |
| YAML `tokens.shadow.card` | 옮김 → Foundations elevation | `0px 2px 8px rgba(0,0,0,0.08)` dest 1 / provenance 0 (fitpet-type: do not claim a provenance second home). Path named at `DESIGN.md` 143. The source body's "shadows were not observed" scan is kept beside it; neither value was chosen over the other. |
| YAML `tokens.components` (10 records) | 옮김 → Components & States | `DESIGN.md` 234–425. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×5 (Primary / Dark Secondary / Outline / Group White / Group Dark), `card` ×3 (Product / Featured / Surface), `badge` ×1, `tab` ×1. All ten YAML `use` strings restored as `Token-set use:` lines. YAML `font` shorthands `12px / 400 NotoSans` dest 4 · `14px / 400 NotoSans` dest 1 · `16px / 700 Pretendard` dest 4 · `18px / 400 NotoSans` dest 2. YAML `active` byte form `text #000000 on #ffffff, 70px height` dest 1. YAML padding `0px 24px` and `0px 48px 0px 24px` kept. YAML border `1px solid #dbdbdb` dest 1. Dark Secondary YAML radius `20px` and body radius `10px` are both kept (255–278). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–15; Key Characteristics as Distinctive traits 33–43. Atmosphere readings carry an adjacent complete qualifier at 11 (B2/B2a). Surface-boundary reading at 9. Housing-finance `#465e6f` and 推奨상품 kept. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 84–113. The source's own group headings (Primary Brand, Corporate & Group, Neutral & Surface, Error & Contrast) are kept as the grouping. Role-naming readings carry the adjacent qualifier at 86 (B2/B2a). |
| §3 Typography Rules — family, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 181–190; family 192–198; hierarchy table 200–212. Official product-use, Declared-only, License, and Outside-captures rows carry adjacent complete qualifiers (B2/B2a). Group Secondary Button 14px / 600 / 40px / 20px is from the source hierarchy table, not a YAML key. |
| §3 Principles — conservative sizing, NotoSans anchor, Pretendard newer layer | 옮김 → Typography & Assets Typography rules | `DESIGN.md` 214–220 under the qualifier at 216 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 234–425. §4 body values (48px / 54px / 234px / 245px / 0px 16px) and YAML `use` / `font` / `active` / padding byte forms are both kept where they differ. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 두 개 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. `https://www.kebhana.com` / `https://www.hanafn.com` are dual: Scope + provenance Surfaces (E2a). Conflicts unresolved: none — `provenance.md` Freshness. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 448–454 (multi-column banking layout 450–452, then spacing/shape restatements 453–454). Density / tint-separation / teal-anchor readings carry the qualifier at 456 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Four-level table `DESIGN.md` 134–141, including Teal Fill `#008485`. The Shadow philosophy paragraph is carried as a qualified reading at 145 (B2/B2a). YAML `tokens.shadow.card` sits on 143. |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 55–66, under the grouping qualifier at 57. Not placed in Governance controlled copy. Eight items, matching the source Do list. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 68–78, under the qualifier at 70. Seven items, including the `#000000` body-text prohibition and the `#009178` retail-surface prohibition. No invented domain (no `native application`, no `back-office`). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Breakpoint table `DESIGN.md` 462–466 with the "stated at system level rather than measured across viewports" note at 458; touch targets 468 (`70px height at 1440px` kept as the source wrote it); collapsing strategy 470–474. Exact string `measures 1440px` dest 0 / provenance 0. Desktop row keeps the source body's `1024px+`. |
| §9 Agent Prompt Guide — Quick Color Reference, Example Component Prompts, Iteration Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present — the palette hexes, the 6/10/20/27/9999 radii, the 12/18/24 sizes, the 400/700 weights, the published Korean strings, and the 234/245 card heights (A2, A3). |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings `DESIGN.md` 476–514; register table 480–486; voice samples 488–492; further recorded strings 494–510; forbidden register 512. The source's own English gloss of 자세히보기 is kept beside the Korean line at 484; the closing line at 514 says an English gloss never replaces the Korean line (A5) and carries an adjacent complete qualifier for that byte-exact / gloss-beside rule (B2/B2a). The qualifier at 478 covers the characterization and the table. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: **1971** Korea Investment Finance Corporation (**한국투자금융주식회사**); **1991** Hana Bank; **2015** merger with Korea Exchange Bank (KEB, **한국외환은행**); **주식회사 하나은행**; Hana Financial Group (**하나금융그룹**); Korea's second-largest financial holding company by total assets; **Hana1Q mobile banking platform**; **"Together, we grow"**; **하나** = one / together; Big Four. `grep -oF` dest: `1971` 1 · `1991` 2 · `2015` 2 · `한국투자금융주식회사` 1 · `한국외환은행` 1 · `주식회사 하나은행` 1 · `하나금융그룹` 2 · `Hana1Q` 2 · `Together, we grow` 3 · `second-largest` 1. Marked there as narrative context that supplies no interface tokens. The design-language paragraph at 15 carries an adjacent complete qualifier (B2/B2a). Hana1Q is kept in the source's own wording; no "mobile application is out of scope" sentence was written (D1). |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 45–53 under the B2a form at 47: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Hana Bank-authored or a separately published UI specification." No published first-party DS, so the toss-form close is used (rulebook v12 B2a 전제 주석). |
| §13 Personas — 3 entries | 삭제 | The source's own italic line labels them fictional archetypes informed by the publicly observable product catalog and Korean banking demographics, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, or city appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience at `DESIGN.md` 31 carries only the group-level description the source records (all age groups on the retail site; institutional / investor audiences on the group site), under an adjacent qualifier. The four `primary-tasks` at 23–26 come from labels and surfaces the source records; 21 says so and qualifies the step from label to "primary task". |
| §14 States — 10 rows | 옮김 → Components & States State record + per-component applicability | Full ten-row body preserved at `DESIGN.md` 428–443 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "페이지 요청 오류" and 이체하기, `#1, 128, 133`, `rgb(255,0,0)`, and `#c8d4d4`. The applicability rule is at 230–232, whose qualifier at 232 covers every Reason cell that follows. The non-attachment sentence at 445 carries its own adjacent complete qualifier. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 6 interactive components × 7 states = 42 rows: Primary Teal opens loading/error/success as a commit-capable action button (`loading \| applicable` dest 1); Dark Secondary / Outline / Group White / Group Dark / Main Banking Tab each close loading/error/success together as destination or destination-tab (`loading \| not-applicable` dest 5 · `error \| not-applicable` dest 5 · `success \| not-applicable` dest 5). The three cards get no `kind` and no map because the source supplies no interaction evidence — C4 at 366/379/391. The badge declares `Kind: non-interactive` with a reason instead of a map (Core §4.4). |
| §15 Motion & Easing — durations, easing token names/roles, motion rules, reduced-motion | 옮김 → Foundations motion | `DESIGN.md` 151–157 (the three duration tokens with their uses: `motion-fast` 100ms, `motion-standard` 200ms, `motion-slow` 300ms at 155–157), 159–165 (three easing token names and roles), 169–175 (institutional-conservative rule, hero carousel at `motion-slow`, `ease-enter` arrival, no spring / no bounce, and the `prefers-reduced-motion: reduce` behavior). `100ms` dest 1 · `200ms` dest 1 · `300ms` dest 1. |
| §15 Motion & Easing — three `cubic-bezier` values | 삭제 → provenance omission ledger | No observation stands behind them: the source records a Tier 1 live inspect of color, type, geometry, and shadow and supplies no transition, animation, or easing sample. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to the example table at `spec/omd-v0.1.md` line 267, the documented re-injection path. The other two (`cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.25, 0.1, 0.25, 1)`) are likewise unattributed. Ledger at `provenance.md` Omission ledger. Occurrence counts by `grep -oF cubic-bezier` → `DESIGN.md` 0, `provenance.md` 3 (three curves). B3 is held: the promotion condition at `DESIGN.md` 167 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus the partial-confirmation exclusion (E2c). Governance 550 restates the five kinds and the gate. |
| HTML comment OmD v0.1 Sources | 분리 → provenance | Live-inspect notes, Tier 1/2 URLs, narrative class, voice-sample attribution, and the unattributed §14/§15 partition are ledger material: `provenance.md` Evidence class + Raw samples. Frequency counts and `#008491` / `#009591` stay ledger-only. |

## Sibling handling (`web/references/hana/.verification.md`)

The sibling exists — confirmed with `find web/references/hana -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact.

- Its full record is transcribed at `provenance.md` Sibling verification + Raw samples and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not, or that conflict with the source: `#009591` / `#008491` (`rgb 0,149,145`), `#27b2a5`, `#00a88b`, body `line-height: 18px`, footer `#dddddd` / width `200px`, card widths `304px` / `258px`, group secondary `#f2f2f2` / `0px 40px 0px 20px`, segmented footer radii, frequency counts. Recorded at `provenance.md` Sibling-only values. Neither side of a conflict was chosen as a new token.
- Published strings that appear only in the sibling survive in provenance, not in the portable body: `내집마련 더블업` (D 0 / P 2), `PDF/MP3/Annual Report` (D 0 / P 3). `#465e6f` is not sibling-only (source body 1): D 2 / P 3.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Brand-issued Korean strings in the source (labels, CTAs, slogans, microcopy, names) | 31 distinct | 0 | 0 | All 31 survive in `DESIGN.md` (hand list below). |
| Brand-issued strings in the sibling, any length | 2 sibling-only | 0 | 0 | `내집마련 더블업` and `PDF/MP3/Annual Report` survive in provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand hana` after restore | 24 candidates | 0 reported | 0 | First run reported 2 lost (`1px solid #dbdbdb`, `No designs found`). The first is a YAML border byte form, restored as `Token-set border record:` (`DESIGN.md` Outline Footer Select). The second is a getdesign.md third-party string the source excludes; it is held in provenance Third-party strings, not as Hana copy. Re-run: `clean: 1`, `lost: 0`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 하나은행, 조회, 이체, 공과금, 외환, 금융상품, 고단위 플러스, 부자씨 적금, 하나의 정기예금, 급여하나 월복리적금, 급여하나 월복리 적금, 자세히보기, 페이지 요청 오류, 이체하기, 인재상 알아보기, 채용공고 바로가기, 브랜드사이트, 하나네트워크, 하나은행 SNS, 신용대출, 담보대출, 하나소비자세상, 하나 민트색, 한국투자금융주식회사, 한국외환은행, 주식회사 하나은행, 하나금융그룹, 推奨상품, Together, we grow, Hana1Q, ISMS.

A5 분모: latin-copy-audit 24/24 survived after restore; Korean hand sweep 31 extracted / 0 missing / 0 published-copy missing. Sibling-only published 2/2 in provenance.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hana --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 31, candidates: 186 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 31 < `candidates` 186.

## Deviations recorded

- `DESIGN.md` word count is recorded after the F3 audit in the F1 block below. The spec's 600–1,800-word SHOULD budget yielded to A1: fourteen color roles, six type roles with dual unitless/px forms, ten declared component records with dual `use`/`font`/`padding` forms, a ten-row surface state contract, a 42-row applicability matrix, retail-vs-group domain splits, §11 year/merger facts, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Hana Bank has no published first-party design system in this packet (getdesign.md/hana not found; refero no Hana match), so every derived-editorial close uses the toss-form "not Hana Bank-authored or a separately published UI specification" (rulebook v12 B2a 전제 주석). Measure is in the F1 block; worker-close 27 is superseded.
- There is no `Named gaps` section (`DESIGN.md` 0). Recorded unresolved names only the easing-curve omission, the Dark Secondary radius pair, and the two-surface split — all established by the source.

## F1 — B2a scan (mandatory final pass)

The finished `DESIGN.md` was re-read from the title through Governance. Every sentence that states a cause, a reading, a role name this contract assigned, a kind/applicability verdict, or a voice characterization was asked: is this a Hana Bank-issued fact, or a reading derived from the observed surfaces? The latter received an adjacent complete qualifier — "derived editorial implementation inference from the verified surfaces; … not Hana Bank-authored or a separately published UI specification."

Worker-close measured dest 27 = inventory 27. F3 audit added three adjacent complete qualifiers (Declared-only 188 · State-record close 445 · Content gloss 514) and expanded four existing ones (Semantic 86 · Spacing 121 · Shape 132 · Elevation 145). Post-audit measure: `derived editorial implementation inference` dest 30 = `not Hana Bank-authored or a separately published UI specification` dest 30 = provenance Derived editorial inventory 30 data rows. Provenance names the clause in the inventory preamble; those preamble sentences are not portable qualifications. `wc -w` after the audit is recorded next to the SHA in `audit-log.md`.

Source facts that were not qualified: the 1971 / 1991 / 2015 years, 한국투자금융주식회사, 한국외환은행, 주식회사 하나은행, Hana1Q, "Together, we grow", the live hex and metric values, and the published Korean strings.

## F2 — E2 대조 (mandatory final pass)

Each log row above was written only after `grep -oF` confirmed the named value in the named file. Dual destinations are claimed only where both files contain the string (`#00a39f`, `#008485`, `#009178`, `#2dc396`, `#555555`, `#666666`, `#dbdbdb`, `#465e6f`, `#222222`, `#292f35`, homepage URL, `하나은행`, `조회`, `고단위 플러스`, `인재상 알아보기`, `채용공고 바로가기`, `브랜드사이트`, `1971`). `#f2f9f9` and `0px 2px 8px rgba(0,0,0,0.08)` are dest-only — this log does not invent a second home (fitpet-type). "B3 is held" is claimed because `DESIGN.md` 167 contains the five evidence kinds, the per-component gate, and the partial-confirmation exclusion in full text. "B2a 30=30" is claimed because both the portable count and the inventory row count are 30 after the F3 audit (worker-close 27=27 is superseded). §13 deletion is claimed as unidentified: this file and provenance name the section, the headcount, and the dropped field kinds, and do not restate the identifiers (name / age / city dest 0 / provenance 0 / this file 0 as use).

## YAML key-path self-check (wave extra)

Every `tokens.*` key path from the source frontmatter was checked against the portable body by path, not by finding the same number in another scale.

| Path | Check |
|---|---|
| tokens.colors.* (14 hex) | Each hex is a Semantic color role row; dest ≥ 1. `#333333` is both `ink` and `dark`. |
| tokens.typography.family.{primary,body,group} | Family section names all three |
| tokens.typography.{heading,nav,body,footer,group-body,group-cta} | Type-roles table; unitless LH 1.56/1.5/1.4 on those rows only |
| tokens.spacing.{xs,sm,md,base,lg,xl,xxl,section} | Spacing slot `xs: 4` … `section: 48`; `base: 16` is not a radius step |
| tokens.rounded.{sm,md,lg,full} | Shape slot `sm: 6` … `full: 9999`; `lg: 20` on this path is not the spacing `lg: 20` |
| tokens.shadow.card | Elevation `0px 2px 8px rgba(0,0,0,0.08)` |
| tokens.components.* type | button 5 / card 3 / badge 1 / tab 1 |

`Primitive type: \`button\`` dest 5 = source YAML button records 5. No line-height was copied from one role onto another. `full: 9999` dest 3 is the rounded path (125/132/454), not a spacing coincidence.
