# Mailchimp Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Mailchimp is the marketing platform the source records on the public site at `https://mailchimp.com`. This contract covers the two brand-owned surfaces the source inspected for tokens: the homepage at `https://mailchimp.com/` and the official brand-assets page at `https://mailchimp.com/about/brand-assets/`. The catalog homepage field is `https://mailchimp.com`. Official About `https://mailchimp.com/about/` and the content style guide `https://styleguide.mailchimp.com/voice-and-tone/` are named sources for mission and voice; they do not supply the computed interface tokens below. Treating those two inspected URLs as this contract's token surfaces, keeping About and the content style guide as named narrative/voice sources that do not supply computed interface tokens, treating values as attached to the surface that established them, keeping catalog `primary_color` `#ffe01b` on the same hex as `tokens.colors.primary`, and treating the content style guide as a voice source rather than as a separately published UI specification, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

The source records a marketing homepage on a pure white canvas (`#ffffff`) anchored by a single hero color: Cavendish Yellow (`#ffe01b`), reserved almost exclusively for the primary call-to-action. Text and headings sit in Peppercorn (`#231e15`). Headlines run in **Means Web** at 48px on the hero with tight `-1px` tracking and weight 400. Body and UI text drop to **Graphik Web** at 16px (13px for nav and buttons). Shadows are almost entirely absent — separation comes from hairline borders (`1px solid #bcbab6`) and a faint ash-white surface (`#f5f5f5`). When elevation is used (a featured pricing card), the shadow is `rgba(35,30,21,0.15)`. Navigation items use a tight near-square 3px corner; every button is a full 26px pill outlined with a 1px peppercorn ring rendered as a box-shadow. Teal Ink (`#004e56`) is the link color. The hex values, the two families, the 48px / 16px / 13px writings, the 3px / 26px radii, the hairline, and the two shadow writings are the source's own. Readings of the page as outsider-art, warm, hand-made, editorial, tactile, or human, of yellow against peppercorn as the entire brand in two colors, of Means Web as a vintage printing press or literary magazine, of the serif-display-over-grotesque-body split as inverted from a typical SaaS playbook, and of the result as the antithesis of frictionless minimalism, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. Mailchimp was founded in **Atlanta in 2001** by **Ben Chestnut** and **Dan Kurzius** as a side project of their web-design agency, the Rocket Science Group ([Mailchimp — About](https://mailchimp.com/about/), [Wikipedia](https://en.wikipedia.org/wiki/Mailchimp)). It began as "an alternative to the oversized, expensive email software of the early 2000s" — built to give small-business owners the marketing tools their larger competitors took for granted. Wary of giving up control, Chestnut and Kurzius famously **bootstrapped the company from profits and took no outside investment** for two decades, an almost unheard-of path in SaaS ([Founder Stories — Dave Lu](https://www.davelu.com/p/founder-stories-ben-chestnut-of-mailchimp)). That underdog origin is the company's stated mission to this day: *"Today, we continue to empower the underdog and strive to be the business partner we wish we had way back in the beginning"* ([Mailchimp — About](https://mailchimp.com/about/)). The design system is a direct expression of that posture — warm, hand-made, and human, deliberately rejecting the cold polish of enterprise software because the brand sees itself on the side of the small operator, not the incumbent. In **September 2021, Intuit acquired Mailchimp for approximately $12 billion** in cash and stock ([Inside Philanthropy](https://www.insidephilanthropy.com/home/2021-9-22-after-a-big-acquisition-mailchimps-co-founders-are-the-latest-multi-billionaires-to-watch)). What Mailchimp refuses, visible in its design: the gradient-heavy, frictionless SaaS aesthetic and the hyperbolic over-promising of growth marketing. What it embraces: an editorial serif voice, a single voltage-yellow accent, Freddie the wordless winking mascot, and "outsider-art" illustration — a brand that is *"weird but not inappropriate, smart but not snobbish."* The 2001 / Atlanta / Ben Chestnut / Dan Kurzius / Rocket Science Group / bootstrap / Intuit / ~$12 billion facts, the quoted origin line, the quoted mission, the small-business-owner origin clause, the almost-unheard-of bootstrap path, the design-system-as-posture sentence, and the source's closing refuse/embrace sentences are the source's own. Classifying that origin-to-current-service narrative as context that does not by itself supply interface tokens, classifying Wikipedia / Dave Lu / Inside Philanthropy as narrative citations rather than token sources, and classifying the source's closing refuse-gradient-SaaS / embrace-editorial-serif reading as an editorial connection of observed chrome to positioning rather than as a published brand manifesto, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a captured homepage control the source records, and not taking them from the source's Personas section, is a derived editorial implementation inference from the verified surfaces; it is not Mailchimp-authored or a separately published UI specification.

- Start a free trial on `https://mailchimp.com` ("Start Free Trial").
- Log in ("Log In").
- Scan plan-selector cards and the trust badge on the homepage ("Risk-Free • No Credit Card Required").
<!-- design-md:claim-end -->

### Audience

No named individuals appear. The source labels its §13 figures as fictional archetypes informed by publicly observable user segments, not individual people, so those three archetypes are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records at a group level, outside that Personas section, is the Brand Narrative's small-business owner and the About-page underdog the mission names. Dropping those archetypes rather than promoting them, carrying no name, age, city, motivation, or affiliation classification, and reading those two source-named groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

- Cavendish Yellow (`#ffe01b`) as the single hero/CTA color — voltage-bright, used sparingly
- Peppercorn (`#231e15`) for all text instead of pure black — warm near-black with brown-red undertone
- Means Web editorial serif for headlines (48px / weight 400 / -1px tracking)
- Graphik Web grotesque for all body and UI text (16px body, 13px nav/buttons)
- Pill buttons (26px radius) outlined with a 1px peppercorn ring via box-shadow
- Near-square 3px nav corners — a deliberate counterpoint to the pill buttons
- Teal Ink (`#004e56`) reserved for links — the only cool accent in a warm palette
- Warm-tinted shadows (`rgba(35,30,21,0.15)`) on the rare elevated card — never neutral gray

### Principles

These 5 items — numbered stems the source states in its Principles section, which rest on Mailchimp-published mission and style-guide sentences the source attributes to the company, plus every *UI implication* below as the source's own editorial reading — are a derived editorial implementation inference from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

1. **Empower the underdog.** The company's stated mission is to be "the business partner we wish we had way back in the beginning." *UI implication:* keep the interface approachable and jargon-free — demystify marketing rather than gatekeeping it behind enterprise polish.
2. **Clarity above all.** The style guide values clarity over entertainment. *UI implication:* the single yellow CTA and one-loud-note layout make the next step unambiguous; copy is plain English.
3. **Weird but not inappropriate.** Personality is a feature, not a risk. *UI implication:* the editorial serif, hand-drawn Freddie, and outsider-art illustration give the product a point of view — but humor stays "the wry over the farcical."
4. **Warmth over polish.** The brand sides with the human, not the machine. *UI implication:* Peppercorn instead of black, warm-tinted shadows instead of gray, parchment surfaces — every neutral leans warm.
5. **One action, one color.** Cavendish Yellow means "do this." *UI implication:* reserve the saturated yellow exclusively for the primary CTA so the action is never ambiguous.

### Application rules

The source states these nine as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

- Use Means Web editorial serif for headlines at weight 400 — the vintage-press serif is the brand's voice
- Use Graphik Web grotesque for all body, nav, button, and badge text
- Reserve Cavendish Yellow (`#ffe01b`) for the single primary CTA — keep it the one "action" color
- Use Peppercorn (`#231e15`) for text and the 1px button outline instead of pure black
- Outline pill buttons with a 1px peppercorn ring rendered as a box-shadow
- Separate sections with `#f5f5f5` tint and `#bcbab6` hairlines, not shadows
- Use full pill geometry (26px) on buttons, near-square (3px) on nav items
- Tint the rare elevation shadow warm (`rgba(35,30,21,0.15)`), never neutral gray
- Reserve Teal Ink (`#004e56`) for links — the only cool accent

### Avoid

The source states these eight as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

- Set headlines in a geometric sans — Means Web serif owns display, that's the whole point
- Spread Cavendish Yellow across many elements — it dilutes the single-action signal
- Use pure black (`#000000`) for headings — reserve warm Peppercorn `#231e15`
- Reach for drop shadows for elevation — separate with hairlines and tint first
- Use neutral gray shadows — when elevation is needed, tint it warm with peppercorn
- Make buttons sharp-cornered — every button is a full 26px pill
- Use bold (700) weight on the serif headlines — weight 400 is the editorial register
- Add a second saturated accent — yellow is the only loud color, teal is reserved for links

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Role names follow the source's own labels and token-set keys. Taking those role names from the source's own token-set keys, pairing each hex to the token-set path named beside it, keeping catalog `primary_color` `#ffe01b` on the same hex as `tokens.colors.primary`, keeping `tokens.colors.ink` and `tokens.colors.on-primary` as two keys that happen to share `#231e15`, keeping `tokens.colors.ink` `#231e15` off `tokens.colors.ink-deep` `#241c15` and off `tokens.colors.ink-pure` `#000000`, attaching every role to the surface the source recorded, keeping Mailchimp's Cavendish Yellow / Peppercorn names rather than replacing them with refero's Voltage Yellow / Press Black labels, keeping sibling-only frequency-scan hexes off this token set, keeping `#ffffff` as Pure White canvas / page background / card surfaces / nav background off Secondary-on-Dark Text `#ffffff` and off Trust Badge Text `#ffffff`, keeping Peppercorn `#231e15` as ink and as On-Primary off Trust Badge Background `#231e15`, and keeping Pure Black `#000000` off Option Card Text `#000000` as two keys that share the hex, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

Primary

- **Cavendish Yellow** (`#ffe01b`): Mailchimp's hero color and the system's single primary-action background. The voltage-yellow pill CTA ("Start Free Trial") — the one color the eye is trained to treat as "the action." Confirmed verbatim on the brand-assets page: "Cavendish Yellow is Mailchimp's hero color. We use Peppercorn for accents." Token-set path `tokens.colors.primary`. Same hex as catalog `primary_color`.
- **Peppercorn** (`#231e15`): Primary text, heading, and CTA-label color, and the 1px outline ring on buttons. "We use Peppercorn for accents" (brand-assets page). Token-set path `tokens.colors.ink`.

Neutral & Surface

- **Pure White** (`#ffffff`): Page background, card surfaces, text on dark/photographic blocks. Token-set path `tokens.colors.canvas`.
- **Ash White** (`#f5f5f5`): Cool-neutral tinted surface for collapsed option cards and segmented blocks. Token-set path `tokens.colors.surface`.
- **Warm Parchment** (`#ebe1cd`): Warm cream surface for editorial/illustration panels. Token-set path `tokens.colors.surface-warm`.
- **Silver Rule** (`#bcbab6`): The hairline border color for option cards and dividers. Token-set path `tokens.colors.hairline`.
- **Deep Ink** (`#241c15`): Near-black peppercorn variant used as a full-bleed dark background (cookie/consent bar, dark sections). Token-set path `tokens.colors.ink-deep`. This is not `tokens.colors.ink`.
- **Pure Black** (`#000000`): Maximum-contrast body text inside dense cards (pricing tables). Token-set path `tokens.colors.ink-pure`. This is not `tokens.colors.ink`.
- **On-Primary** (`#231e15`): Text on the Cavendish Yellow CTA. Token-set path `tokens.colors.on-primary`. Same hex as `tokens.colors.ink`; it stays a second key.

Text Hierarchy

- **Peppercorn** (`#231e15`): Primary text, headings, nav, strong labels. Token-set path `tokens.colors.ink`.
- **Warm Charcoal** (`#403b3b`): Secondary copy and fine-print terms text. Token-set path `tokens.colors.charcoal`.
- **Graphite** (`#706d67`): Tertiary / muted captions and metadata. Token-set path `tokens.colors.graphite`.

Accent

- **Teal Ink** (`#004e56`): The link color (310 occurrences in the live scan) — a deep teal that is the only cool note in the palette. Token-set path `tokens.colors.teal`.
- **Slate Teal** (`#27455c`): Photographic-section overlay/background tint behind white headlines. Token-set path `tokens.colors.slate`.
- **Forest Green** (`#468254`): Occasional supporting accent on illustration/data blocks. Token-set path `tokens.colors.forest`.
- **Sand** (`#e7b75f`): Warm golden accent used in footer / illustration treatments. Token-set path `tokens.colors.sand`.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept as the source wrote them): `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 24` · `xl: 32` · `xxl: 48` · `section: 80`.

The source also writes those samples as 4px, 8px, 12px, 16px, 24px, 32px, 48px, 80px, with a 4px base unit. Notable, as the source states it: button padding lands at 12px 24px; section gaps run a generous 80px+. `tokens.spacing.xs: 4` is not a radius step. `tokens.spacing.sm: 8` is not `tokens.rounded.sm: 8` and is not nav padding `8px`. `tokens.spacing.md: 12` is not the `12px` in primary padding `12px 24px` and is not badge padding `4px 12px`. `tokens.spacing.base: 16` is not body 16px and is not the `16px` in featured-card padding `24px 16px`. `tokens.spacing.lg: 24` is not Heading Small 24px and is not the `24px` in `12px 24px`. `tokens.spacing.xl: 32` is not Heading 32px and is not `tokens.rounded.pill: 32`. `tokens.spacing.xxl: 48` is not Display Hero 48px. `tokens.spacing.section: 80` stays that unitless step. Treating those px samples as observed values rather than as an assertion of a complete mathematical scale, keeping those unitless spacing steps on their own keys rather than rewriting them as a grid, and keeping those writings of `4`, `8`, `12`, `16`, `24`, `32`, `48`, and `80` on their own records, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path): `xs: 3` · `sm: 8` · `md: 10` · `lg: 26` · `pill: 32`.

The source's named radius uses, kept on their own rows:

- Near-square (`3` / `3px`): nav items. Token-set key `tokens.rounded.xs`.
- Small (`8` / `8px`): badges, trust pills. Token-set key `tokens.rounded.sm`. This `8` is not `tokens.spacing.sm: 8`.
- Medium (`10` / `10px`): option / plan cards. Token-set key `tokens.rounded.md`.
- Large (`26` / `26px`): buttons (full pill at button height). Token-set key `tokens.rounded.lg`.
- Pill (`32` / `32px`): tags / large rounded chips. Token-set key `tokens.rounded.pill`. This `32` is not `tokens.spacing.xl: 32` and is not Heading 32px.

Keeping `3`, `8`, `10`, `26`, and `32` as five keys, and keeping those component heights off the rounded map, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Tint (Level 1) | `#f5f5f5` background shift | Card / section separation without elevation |
| Hairline (Level 2) | `1px solid #bcbab6` border | Option-card outlines, dividers |
| Ring (Level 2b) | `rgb(35,30,21) 0px 0px 0px 1px` box-shadow | The 1px peppercorn outline on pill buttons |
| Elevation (Level 3) | `rgba(35,30,21,0.15) 0px 0px 20px 3px` | The single featured pricing card — warm-tinted |

YAML writes `tokens.shadow.hairline` as `rgb(35, 30, 21) 0px 0px 0px 1px` and `tokens.shadow.elevation` as `rgba(35, 30, 21, 0.15) 0px 0px 20px 3px`. Source §6 writes the same two shadows without those extra spaces after the commas. Both writings stay. Live inspection found `box-shadow: none` across the hero, nav, headings, and most cards. The two shadows that do exist are both warm-tinted with peppercorn. The `none` reports, the five-level table, and those two shadow writings are the source's own. Reading those representative `box-shadow: none` reports as the only elevation record for the observed elements, rather than as a depth scale for every Mailchimp surface, is a derived editorial implementation inference from the verified surfaces; it is not Mailchimp-authored or a separately published UI specification.

### Motion

Durations, as the source states them:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 200ms | Card / section reveal, dropdown, sheet |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

The source also names three easing tokens (`ease-enter`, `ease-exit`, `ease-standard`) and assigns cubic-bezier values with no computed-sample attribution. Those unattributed curve values are omitted here. Duration names and millisecond values stay. Named easing roles the source records stay as role names without those curve values, with the Use writings the source records: `ease-enter` Arriving — cards, dropdowns, sheets; `ease-exit` Dismissals; `ease-standard` Two-way transitions. Signature motion, as the source states it: Motion is warm and human but restrained — consistent with the editorial, near-shadowless aesthetic. Buttons respond to press with a subtle scale/opacity shift; sections fade-in from below at `motion-standard / ease-enter`. The brand's playfulness lives in illustration and copy (Freddie's wink), not in bouncy UI — so motion avoids spring and overshoot, which would read as gimmicky against the editorial register. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional. An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation — one curve read off one element, or a match against an official framework or vendor document — keeping the three duration tokens, keeping the three easing-role Use writings, keeping the source duration/easing-role pair without promoting the omitted curve values, keeping the source's signature-motion and reduced-motion sentences, keeping the no-spring / no-overshoot stance, and omitting the three unattributed curves, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Official product-use | The official brand-assets page names Cavendish Yellow and Peppercorn; the content style guide is a voice source. Neither publishes a universal current typography token. |
| Live computed surface-use | The captured homepage computes headlines as Means Web and body/UI as Graphik Web. |
| FontFaceSet and source corroboration | Source §3 records the live families as Means Web and Graphik Web, with the fallback stacks named below. |
| Official distributed asset | No Mailchimp-exclusive distributed type family was verified. |
| Declared-only / fallback | Display fallback `Georgia, Times, "Times New Roman"`. Sans fallback `"Helvetica Neue", Helvetica, Arial, Verdana, sans-serif`. These are fallbacks, not the brand face. |
| Outside these captures | Typography beyond the inspected public homepage and brand-assets page stays outside this contract. |

Reading those six evidence-class rows as the source's own resolution table rather than as a published Mailchimp type specimen, keeping the official-product-use row from independently establishing a UI family, keeping YAML family names and §3 fallback stacks as two records rather than choosing a fallback as a replacement, and projecting typography beyond the two inspected public surfaces as Outside these captures, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

### Family

- **Display:** `Means Web`. Token-set path `tokens.typography.family.display`. Source §3 fallback: `Georgia, Times, "Times New Roman"`. High-contrast editorial serif used for all headlines (h1/h2) at weight 400.
- **Sans / Body:** `Graphik Web`. Token-set path `tokens.typography.family.sans`. Source §3 fallback: `"Helvetica Neue", Helvetica, Arial, Verdana, sans-serif`. Neutral grotesque carrying all body copy, nav, buttons, badges, and smaller headings (h3 at 24px).

Do not replace Means Web or Graphik Web with a different claimed family, and do not present a system or fallback stack as Means Web or Graphik Web. Keeping the two family names on display versus sans, keeping the §3 fallback stacks as fallbacks rather than as UI faces, and keeping h3 24px on Graphik Web rather than on Means Web, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

### Type roles

YAML writes unitless line height `1.0` on display-hero and heading-lg, `1.33` on heading-sm, and `1.35` on body. Source §3 writes `1.0 (48px)`, `1.0 (40px)`, `1.33 (32px)`, `1.35 (21.6px)`, and `normal` on Heading / Label / Caption. YAML `heading` has no `lineHeight`. YAML `heading-sm` weight is `400`; source §3 writes `400-500` for that role. YAML tracking is `-1.0`, `-0.5`, `-0.32`; source §3 writes `-1px`, `-0.5px`, `-0.32px`. Both writings stay. Unitless ratios stay ratios and are never converted to a replacement px (A1a). Pairing each YAML role to the token-set path named beside it, keeping YAML `use` strings verbatim in the Token-set use column, keeping the longer §3 role name beside them, keeping YAML size `48` beside §3 `48px` / `3.00rem` and YAML size `40` beside `40px` / `2.50rem` and YAML size `32` beside `32px` / `2.00rem` and YAML size `24` beside `24px` / `1.50rem` and YAML size `16` beside `16px` / `1.00rem` and YAML size `13` beside `13px` / `0.81rem` and YAML size `11` beside `11px` / `0.69rem`, keeping body `16` off `tokens.spacing.base: 16`, keeping Heading `32` off `tokens.spacing.xl: 32` and off `tokens.rounded.pill: 32`, and keeping Display Hero `48` off `tokens.spacing.xxl: 48`, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Token-set use | §3 notes |
|---|---|---:|---:|---|---|---|---|
| Display Hero | Means Web | 48 / 48px / 3.00rem | 400 | 1.0 / 1.0 (48px) | -1.0 / -1px | Hero headline, Means Web serif | Hero headline, editorial serif |
| Heading Large | Means Web | 40 / 40px / 2.50rem | 400 | 1.0 / 1.0 (40px) | -0.5 / -0.5px | Section titles, Means Web serif | Section titles |
| Heading | Means Web | 32 / 32px / 2.00rem | 400 | (YAML omits; §3 normal) | -0.5 / -0.5px | Sub-section heads, Means Web serif | Sub-section / FAQ heads |
| Heading Small | Graphik Web | 24 / 24px / 1.50rem | 400 / 400-500 | 1.33 / 1.33 (32px) | -0.32 / -0.32px | Card / feature heads, Graphik Web | Card / feature heads |
| Body | Graphik Web | 16 / 16px / 1.00rem | 400 | 1.35 / 1.35 (21.6px) | (YAML omits; §3 normal) | Standard reading text, Graphik Web | Standard reading text |
| Label | Graphik Web | 13 / 13px / 0.81rem | 500 | (YAML omits; §3 normal) | (YAML omits; §3 normal) | Nav links, button labels, badges, Graphik Web | Nav links, button labels, badges |
| Caption | Graphik Web | 11 / 11px / 0.69rem | 400 | (YAML omits; §3 normal) | (YAML omits; §3 normal) | Fine print, terms, Graphik Web | Fine print, terms |

Token-set paths: `tokens.typography.display-hero` · `tokens.typography.heading-lg` · `tokens.typography.heading` · `tokens.typography.heading-sm` · `tokens.typography.body` · `tokens.typography.label` · `tokens.typography.caption`.

Source §3 principles, kept as written: **Serif display, grotesque body**; **Light editorial weight** (headlines at weight 400, not bold); **Tight display tracking** (-1px at 48px, -0.5px at 40px; body stays at normal tracking); **13px functional UI** (nav, buttons, and badges at 13px / weight 500).

### Assets

- Catalog favicon: `https://www.google.com/s2/favicons?domain=mailchimp.com&sz=128`. Frontmatter records `logo.type: favicon`.
- Hand-drawn Freddie illustrations and editorial imagery, as the source records them in Image Behavior: they carry no shadow, consistent with the flat system.

Reading the Google s2 favicon URL as a catalog identity pointer rather than a Mailchimp-hosted brand file, and reading Freddie illustrations as first-party imagery the source names rather than as a substitute type role, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### How to read this section

The source state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| **Empty (no campaigns yet)** | White canvas. Single Peppercorn (`#231e15`) line at body size in plain English explaining nothing's been created yet, with one Cavendish-Yellow CTA to start. A Freddie illustration may appear — warm, not clinical. |
| **Empty (no audience/contacts)** | Warm Charcoal (`#403b3b`) single line: nothing imported yet, plus a clear path to add contacts. Honest, jargon-free. |
| **Loading (dashboard first paint)** | Skeleton blocks at final dimensions on `#f5f5f5` tinted surface, 10px radius. Flat pulse consistent with the near-shadowless system — no heavy shimmer. |
| **Loading (in-place refresh)** | Previous content stays visible; a subtle progress indicator rather than a blocking spinner. |
| **Error (action failed)** | Inline message in Peppercorn with a plain-language explanation and a concrete next step. No generic "Something went wrong" — states what happened and what to do. |
| **Error (form validation)** | Field-level message below the input describing what's valid, not just "Required". Plain English, never condescending. |
| **Success (campaign sent)** | Brief inline confirmation in a calm, plainspoken tone; the Freddie high-five appears for genuine milestones — celebratory but never over-the-top. |
| **Skeleton** | `#f5f5f5` blocks at final dimensions, 10px radius, flat pulse. |
| **Disabled** | Reduced-opacity surface and text together; the yellow CTA fades rather than turning gray, preserving the brand read. |

The following applicability note, every interactive-kind verdict, every applicability verdict, the reason given for either, treating the Start Free Trial button, the Log In button, and the Customize my experience button as commit controls so loading / error / success stay applicable, treating the nav item as a destination tab so loading / error / success close on that role, treating the plan-selector radio as a form field that keeps error applicable and closes loading / success, declaring the trust badge `Kind: non-interactive`, withholding kind and a map on the option card and the featured plan card because the source supplies no interaction evidence for those cards (C4), keeping each YAML `use` string as a Token-set use row beside Role, keeping YAML font / padding / radius / border / height / type / shadow byte forms beside the §4 writings, keeping 3px / 8px / 10px / 26px / 32px keep-apart from the spacing steps that share a number, treating the option-card and featured-plan-card observations as default-only, treating the source nav `hover/active` teal `#004e56` text as that nav's captured hover/active treatment rather than as `focus-visible` evidence, and the refusal to treat this as a complete state-coverage claim are a derived editorial implementation inference from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted where this packet holds no value for that same canonical state. Absence of an observation is not a `not-applicable` reason. Loading, error, and success follow each control's product role rather than its primitive kind. A `Primitive type` line is attached only when the source YAML records that type on that component. None of the §14 rows above is marked Verified live on the two inspected URLs; they are the source's stated treatments. This is not a complete state-coverage claim.

### Start Free Trial (Primary)

- Role: Hero / nav primary call-to-action — the single Cavendish-Yellow action color
- Token-set use: Hero CTA — Start Free Trial, full Cavendish-Yellow pill
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled primary button
- Background: `#ffe01b`. YAML bg: `#ffe01b`
- Text: `#231e15`. YAML fg: `#231e15`
- Radius: 26px. YAML radius: `26px`
- Padding: 12px 24px. YAML padding: `12px 24px`
- Height: 44px. YAML height: `44px`
- Font: 13px Graphik Web weight 500. YAML font: `13px / 500 Graphik Web`
- Shadow: `rgb(35, 30, 21) 0px 0px 0px 1px` (1px peppercorn ring). YAML shadow: `1px #231e15 ring`
- Observed: default on `https://mailchimp.com`
- The radius `26px` is this control's geometry and `tokens.rounded.lg`. The height `44px` is this control's geometry, not a spacing step. The `12px` / `24px` in the padding stay this control's padding, not `tokens.spacing.md: 12` or `tokens.spacing.lg: 24`. The font `13px` is this control's size, not a spacing step.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured homepage primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A trial CTA can be gated; visual treatment omitted. Source Disabled row: the yellow CTA fades rather than turning gray |
| loading | applicable | "Start Free Trial" commits a trial-signup action |
| error | applicable | Same commit role; the source records Error (action failed) in plain language |
| success | applicable | Same commit role; a completed trial start is a success this control can report |

### Log In (Outline)

- Role: Secondary action — transparent pill with peppercorn outline
- Token-set use: Secondary action — Log In, transparent pill with peppercorn outline
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled outline button
- Text: `#231e15`. YAML fg: `#231e15`
- Radius: 26px. YAML radius: `26px`
- Padding: 12px 24px. YAML padding: `12px 24px`
- Height: 44px. YAML height: `44px`
- Font: 13px Graphik Web weight 500. YAML font: `13px / 500 Graphik Web`
- Shadow: `rgb(35, 30, 21) 0px 0px 0px 1px` (1px peppercorn ring). YAML shadow: `1px #231e15 ring`
- Observed: default on the homepage
- The radius `26px` is this control's geometry and `tokens.rounded.lg`; it is not a second rounded key.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured homepage Log In control |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A log-in control can be gated; visual treatment omitted |
| loading | applicable | "Log In" commits an account action |
| error | applicable | Same commit role; a rejected log-in is an error this control can report |
| success | applicable | Same commit role; a completed log-in is a success this control can report |

### Secondary on Dark (Light Outline)

- Role: Secondary CTA on dark/photographic backgrounds ("Customize my experience")
- Token-set use: Secondary CTA on dark/photographic backgrounds — Customize my experience
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled light-outline button
- Text: `#ffffff`. YAML fg: `#ffffff`
- Radius: 26px. YAML radius: `26px`
- Padding: 6px 16px. YAML padding: `6px 16px`
- Height: 32px. YAML height: `32px`
- Font: 13px Graphik Web weight 500. YAML font: `13px / 500 Graphik Web`
- Shadow: `rgb(255, 255, 255) 0px 0px 0px 1px` (1px white ring). YAML shadow: `1px #ffffff ring`
- Observed: default on dark/photographic blocks
- The height `32px` is this control's geometry; it is not `tokens.spacing.xl: 32` and is not Heading 32px. The `16px` in the padding stays this control's padding, not `tokens.spacing.base: 16`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured dark-background secondary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A customize CTA can be gated; visual treatment omitted |
| loading | applicable | "Customize my experience" commits a customization action |
| error | applicable | Same commit role; a failed customization start is an error this control can report |
| success | applicable | Same commit role; completing the customization start is a success this control can report |

### Nav Link

- Role: Top horizontal nav ("Industries and Solutions", "Integrations", "Resources", "Pricing")
- Token-set use: Top nav item, near-square 3px corner
- Primitive type: `tab` · Kind: interactive
- Anatomy: labelled nav item
- Background: `#ffffff`
- Text: `#231e15`. YAML fg: `#231e15`
- Radius: 3px. YAML radius: `3px`
- Padding: 8px. YAML padding: `8px`
- Font: 13px Graphik Web weight 500. YAML font: `13px / 500 Graphik Web`
- YAML active: `teal #004e56 text on hover/active`
- Observed: default plus captured hover/active Teal Ink text on the homepage
- The radius `3px` is this control's geometry and `tokens.rounded.xs`. The padding `8px` is this control's padding; it is not `tokens.spacing.sm: 8` and is not `tokens.rounded.sm: 8`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured top nav item |
| hover | applicable | Pointer-web nav item; captured treatment is Teal Ink `#004e56` text on hover/active |
| focus-visible | applicable | Keyboard-reachable nav item; visual treatment omitted. The captured hover/active teal text is not this row |
| disabled | applicable | A nav item can be gated; visual treatment omitted |
| loading | not-applicable | A top nav item selects a destination; it commits no operation in place |
| error | not-applicable | A nav item does not report a failed request on itself |
| success | not-applicable | Reaching the destination is not an operation this nav item reports as success |

### Option Card

- Role: Plan-selector / option card with hairline border, no shadow
- Token-set use: Plan-selector / option card, hairline border, no shadow
- Primitive type: `card`
- Background: `#ffffff`. YAML bg: `#ffffff`
- Text: `#000000`. YAML fg: `#000000`
- Border: 1px solid `#bcbab6`. YAML border: `1px solid #bcbab6`
- Radius: 10px. YAML radius: `10px`
- Padding: 8px. YAML padding: `8px`
- Observed: default on the homepage plan selector
- The radius `10px` is this card's geometry and `tokens.rounded.md`. The padding `8px` is this card's padding; it is not `tokens.spacing.sm: 8`. Kind and applicability map omitted — the source supplies no interaction evidence for the card (C4). The radio option row sits inside this card.

### Featured Plan Card (Elevated)

- Role: Highlighted pricing plan — the one place elevation appears, tinted peppercorn not gray
- Token-set use: Featured pricing plan card, warm-tinted elevation
- Primitive type: `card`
- Background: `#ffffff`. YAML bg: `#ffffff`
- Text: `#231e15`. YAML fg: `#231e15`
- Radius: 10px. YAML radius: `10px`
- Padding: 24px 16px. YAML padding: `24px 16px`
- Shadow: `rgba(35,30,21,0.15) 0px 0px 20px 3px` (warm-tinted). YAML shadow: `rgba(35, 30, 21, 0.15) 0px 0px 20px 3px`
- Observed: default on the homepage
- The radius `10px` is this card's geometry and `tokens.rounded.md`. The `24px` / `16px` in the padding stay this card's padding, not `tokens.spacing.lg: 24` or `tokens.spacing.base: 16`. Kind and applicability map omitted — the source supplies no interaction evidence for the card (C4).

### Trust Badge (Peppercorn)

- Role: Risk-Free / No Credit Card Required reassurance pill
- Token-set use: Trust badge — Risk-Free / No Credit Card Required
- Primitive type: `badge`
- Kind: non-interactive — a trust label, not a commit control
- Background: `#231e15`. YAML bg: `#231e15`
- Text: `#ffffff`. YAML fg: `#ffffff`
- Radius: 8px. YAML radius: `8px`
- Padding: 4px 12px. YAML padding: `4px 12px`
- Font: 13px Graphik Web weight 500. YAML font: `13px / 500 Graphik Web`
- Observed: default on the homepage
- The radius `8px` is this badge's geometry and `tokens.rounded.sm`; it is not `tokens.spacing.sm: 8`. No state-applicability map: the badge itself is not an interactive control.

### Radio Option Row

- Role: Plan-selector radio option, the row sits inside the option card; collapsed state shifts to `#f5f5f5` background
- Token-set use: Plan-selector radio option row, sits inside option card
- Primitive type: `input` · Kind: interactive
- Anatomy: radio option row
- Border: 1px solid `#bcbab6`. YAML border: `1px solid #bcbab6`
- Radius: 10px. YAML radius: `10px`
- Observed: default on the homepage plan selector, including the collapsed `#f5f5f5` background the source records
- The radius `10px` is this row's geometry and `tokens.rounded.md`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured plan-selector radio option row |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable field; visual treatment omitted |
| disabled | applicable | A plan-selector row can be gated; visual treatment omitted |
| loading | not-applicable | The row accepts a plan choice; it does not commit an operation whose in-progress state it could report on itself |
| error | applicable | A form field can fail validation; the source records Error (form validation) below the input; visual treatment omitted |
| success | not-applicable | Completing the field is not a success result this input would report |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Hero: centered single column with the 48px Means Web serif headline as the anchor and a single yellow CTA. Feature sections: photographic full-bleed blocks (slate-teal `#27455c` tint) with white headlines, alternating with white editorial blocks. Pricing: card-based plan selector with hairline-bordered option cards and one warm-shadowed featured card. Max content width: ~1200px.

Whitespace, as the source states it: **Editorial breathing room** — generous vertical rhythm, large serif headlines given space to read. **Flat segmentation** — sections separate by background swap (`#ffffff` ↔ `#f5f5f5` ↔ photographic slate) and hairlines, rarely by shadow. **One loud note** — the Cavendish-Yellow CTA is the single high-saturation element on an otherwise warm-neutral page.

The source's Agent Prompt Guide is the only place that records the yellow `#ffe01b` CTA pill as right-aligned in the top nav, with a peppercorn 1px ring. That placement is kept here.

The source's Responsive Behavior table, kept as written:

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, hero serif compresses, plan cards stack |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024-1280px | Full layout, centered hero, multi-column feature bands |
| Large Desktop | >1280px | Centered ~1200px content with generous margins |

Touch targets, as the source states them: primary CTA at 44px height, full pill; nav links at 13px within a comfortably padded header; trust badges and plan-card radios spaced for touch.

Collapsing strategy, as the source states it: Hero 48px Means Web serif scales down on mobile, weight 400 maintained; photographic full-bleed blocks maintain full-width and reduce internal padding; plan cards collapse from side-by-side to stacked single column; white / ash / photographic sections maintain full-width treatment.

Image behavior, as the source states it: hand-drawn Freddie illustrations and editorial imagery carry no shadow; photographic feature blocks keep the slate-teal (`#27455c`) overlay tint behind white headlines; cards maintain 10px radius across breakpoints.

Reading the 640px / 1024px / 1280px / ~1200px figures under the source's own Responsive Behavior table rather than as a newly measured cross-viewport specification, reading the §9 right-aligned yellow nav CTA as a recorded placement rather than as a newly observed measurement, and keeping 44px / 32px / 13px on the controls that established them, are derived editorial implementation inferences from the verified surfaces; they are not Mailchimp-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Mailchimp's voice is **plainspoken, genuine, and wryly funny** — a small-business champion that strips away B2B jargon and talks to the underdog like a knowledgeable friend, not a vendor. The official content style guide states the brand value clarity above all: *"We're weird but not inappropriate, smart but not snobbish. We prefer the subtle over the noisy, the wry over the farcical."* Humor is used only when it's natural — Freddie the mascot winks and high-fives but never talks, and the copy follows the same rule: dry, straight-faced, a touch eccentric, but never forced. Characterizing that reading as a plainspoken, genuine, wry implementation context rather than as a complete product-microcopy guide beyond the quoted style-guide and homepage lines, requiring the quoted strings below byte-exact, treating the style guide as a named voice source rather than as a UI specification, and reading English as the captured homepage locale rather than as a complete locale profile, is a derived editorial implementation inference from the verified surfaces; it is not Mailchimp-authored or a separately published UI specification.

| Context | Tone |
|---|---|
| Hero headlines | Plainspoken, benefit-first. "Email & SMS marketing minus the learning curve." Confident, never hyperbolic. |
| Feature copy | Translator role — demystifies marketing jargon and actually educates, in plain English. |
| CTAs | Direct, low-pressure imperatives. "Start Free Trial", "Customize my experience". |
| Trust / reassurance | Concrete and calm. "Risk-Free • No Credit Card Required." States the promise plainly. |
| Humor | Dry and subtle. Freddie's wink, not a punchline — "the wry over the farcical". |

Voice samples (verbatim from live homepage 2026-06-17):

- "Email & SMS marketing minus the learning curve" — hero headline (benefit-first, plainspoken). *(verified live)*
- "Marketing that delivers results" — section heading (concrete promise, no hyperbole). *(verified live)*
- "Risk-Free • No Credit Card Required" — trust badge (plain reassurance). *(verified live)*

**Forbidden register** (from the official style guide): passive voice, slang and jargon, hyperbolic over-promises, "fluffy metaphors and cheap plays to emotion", forced humor, and anything condescending or exclusive.

Reproduce those quoted strings byte-exact rather than translating or re-casing them. The captured homepage copy is English.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. The list names only fields the source itself left unnamed or that this migration omitted at the smallest value boundary. Reading the list as a catalog of those unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Mailchimp-authored or a separately published UI specification.

- unattributed easing-curve values on `ease-enter`, `ease-exit`, and `ease-standard` — promote a motion value for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed
- hover visual treatments other than the captured nav Teal Ink `#004e56` text on hover/active
- button-press visual treatment
- getdesign.md / styles.refero.design records (the source names getdesign.md/mailchimp as no entry; refero corroborates live inspect under the labels Press Black / Voltage Yellow / Teal Ink)
