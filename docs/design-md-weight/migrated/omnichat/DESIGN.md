# Omnichat Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Omnichat is Taiwan and Hong Kong's leading omnichannel chat-commerce platform. Catalog homepage identity is `https://www.omnichat.ai`. This contract covers the zh-TW marketing surfaces the source inspected for tokens: the homepage at `https://www.omnichat.ai/tw/` and the pricing surface at `https://www.omnichat.ai/tw/pricing/`. The about page at `https://www.omnichat.ai/tw/about/` and the official blog at `https://blog.omnichat.ai/tw/` are named brand-owned sources; they confirm founding, mission, and case-study copy, and they do not supply the computed interface tokens below. Token extraction is `tokens.source: live-extract` (`tokens.extracted` 2026-06-10). Every value stays attached to the surface that established it. Reading those two inspected pages as this contract's token surfaces, treating the about page and the blog as named sources that do not supply interface tokens, and keeping values attached to the surface that established them, are derived editorial implementation inferences from the verified surfaces; they are not Omnichat-authored or a separately published UI specification.

The captured zh-TW marketing layer is a pure black (`#000000`) hero stage where the headline itself glows in an animated aurora gradient — lavender (`#ac9cff`) through violet (`#8e4dff`), mint (`#69dfb2`) and sky blue (`#379afd`) — clipped directly into the Outfit letterforms. Below that opening, the page settles onto a white (`#ffffff`) and soft-grey (`#f8f9fa`) canvas with periodic light-blue tint bands (`#ecf4ff`), so the dark bands and the bright commercial sections alternate. Latin display text — including the rounded "Omnichat" wordmark moments — is set in Outfit at weight 500, while Traditional Chinese UI copy runs in Noto Sans HK. Every heading uses positive letter-spacing (+1px to +1.4px) at medium 500 weight. Heading color is a hierarchy device — deep navy-blue (`#0202a6`) for trust-bearing section titles, sky blues (`#4e97ff`, `#75adfc`) for AI feature heads on dark, and neutral inks (`#2e2e2e`, `#3d3d3d`) for plain content. Header CTAs round three corners at 13px and square the bottom-right (`13px 13px 0px`); pricing and form CTAs round only opposing corners (`0px 24px`); pricing cards sit at 20px with feather-soft shadows. Combined with the chat-bubble logo mark and the blue monochrome action palette (`#006aff` primary, `#408fff` action), those recorded values sit on the inspected pages. Calling that opening cinematic, calling the surface a confident AI-era SaaS pitch deck, calling the dark bands an AI theatre against bright commercial sections, calling headlines open, geometric, and friendly rather than aggressive, calling the notched and ribbon corners a signature that means nothing is a plain rectangle and nothing is a default pill, and calling the whole surface "conversational, automated, commerce-grade", are derived editorial implementation inferences from the verified surfaces; they are not Omnichat-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. Omnichat was founded in **2017 by Alan Chan** in Hong Kong, beginning — as the company's own about page tells it — with a single rented coworking desk where Chan coded the first MVP of a website chat tool that quickly attracted hundreds of e-commerce businesses. The founding insight was that in Asia's mobile-first markets, commerce doesn't happen on web forms — it happens inside chat threads on LINE, WhatsApp, Facebook Messenger, Instagram and WeChat. Omnichat unified those channels into one conversational commerce platform, and Taiwan became its largest market: the zh-TW surface leads the brand, LINE 官方帳號 sits first in the platform menu, and Taiwanese retail names (信義房屋, à la sha, 博士助聽器) headline its case studies. The company states its mission plainly: "Omnichat 讓品牌與顧客保持緊密連結互動，以提升品牌的轉換率為使命" — keep brands and customers tightly connected, with raising conversion rate as the mission. Founder Alan Chan compresses it further: "Omnichat 的目標永遠只有一個，就是提高品牌的轉換率" (Omnichat has only ever had one goal: raising brands' conversion rates). Backed by a US$1.8M Pre-A round in 2022 and consecutive years of ~300% ARR growth, the platform now serves 5,000+ companies across Taiwan, Hong Kong, Malaysia, Singapore, Thailand and Indonesia as an official WhatsApp Business Solution Provider and authorized partner of Meta and LINE — partner logos the design treats as first-class trust assets. The 2025-era reinvention as an "Agentic AI customer experience platform" (Omni AI) put the black-stage aurora hero at the front of the brand. What Omnichat refuses: faceless automation. The company's stated belief — "Omnichat 相信有溫度的顧客體驗，是品牌成功的關鍵" (warm customer experience is the key to brand success) — explains the design's character: a friendly rounded wordmark, open positive-tracked headings, consultative CTAs that offer a human expert, and chat bubbles as the recurring visual motif. The aesthetic says AI infrastructure; the voice says there's a person on the other end of the conversation. The year 2017, Alan Chan, Hong Kong, the coworking-desk MVP, hundreds of e-commerce businesses, the chat-thread founding insight and the five named channels, Taiwan as largest market, LINE 官方帳號 first in the platform menu, 信義房屋 / à la sha / 博士助聽器, the two mission sentences, US$1.8M Pre-A in 2022, ~300% ARR growth, 5,000+ companies across Taiwan, Hong Kong, Malaysia, Singapore, Thailand and Indonesia, WhatsApp Business Solution Provider / Meta / LINE partner status, the 2025-era Agentic AI / Omni AI sentence, the refusal of faceless automation, the 有溫度的顧客體驗 belief, and that closing aesthetic-and-voice sentence are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-mission narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=4 lang=en -->
### Primary tasks

Selecting these four as the product's primary tasks is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification. Each names a label or surface the source records. They do not come from the source's persona section.

- Open 專家諮詢 or 免費試用 from the header.
- Switch pricing groups 線上對話商務 / OMO全渠道銷售 / Social CDP and use 聯絡專人 / 獲得報價 / 預約示範.
- Read case studies via 了解完整案例 and 更多成功案例.
- Submit the lead form 送出資料 (name, company, email) or use the hero field 你想從哪裡著手?
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source labels its named figures as fictional archetypes informed by publicly observable Omnichat customer segments (Taiwanese omnichannel retailers, e-commerce marketing teams, customer-service leads), not individual people, so those biographies are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is those publicly observable Omnichat customer segments: Taiwanese omnichannel retailers, e-commerce marketing teams, customer-service leads. The founding narrative also names hundreds of e-commerce businesses, and the platform now serves 5,000+ companies across Taiwan, Hong Kong, Malaysia, Singapore, Thailand and Indonesia. Reading those source-named groups as this product's audience, and dropping the archetype biographies rather than promoting them, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, is a derived editorial implementation inference from the verified surfaces — it is not Omnichat-authored or a separately published UI specification.

- Black hero stage with aurora-gradient text (`#ac9cff` → `#8e4dff` → `#69dfb2` → `#379afd`) clipped into the headline
- Outfit (Latin) + Noto Sans HK (zh-TW) pairing at medium weight 500 — never bold-heavy
- Positive letter-spacing (+1px / +1.4px) on all headings — open, geometric, friendly
- Asymmetric corner signatures: notched `13px 13px 0px` header CTAs, ribbon `0px 24px` action CTAs
- Blue-only action palette: `#006aff` primary chrome, `#408fff` action CTAs, `#0202a6` deep headings
- White/`#f8f9fa` canvas with `#ecf4ff` and `#d9e8ff` light-blue tint bands
- Feather shadows (`rgba(0,0,0,0.06) 3px 3px 15px`) on 20px-radius pricing cards
- Heading color as hierarchy: deep blue for trust claims, sky blue for AI features, ink for plain content

### Principles

These five items are a derived editorial implementation inference from the verified surfaces; they are not Omnichat-authored or a separately published UI specification. The source states them in its own Principles section together with the UI implication it draws from each.

1. **Conversion is the only scoreboard.** Every surface ties capability to 轉換率. *UI implication:* every section ends in a measurable next step (試用, 諮詢, 報價); decorative sections without a CTA are off-brand.
2. **Conversation is the interface.** Commerce happens in chat threads, so chat is the hero artifact. *UI implication:* show real LINE/WhatsApp-style bubbles and threads in product mockups; the logo's speech-bubble motif echoes through rounded, asymmetric shapes.
3. **Warmth over automation theatre.** "有溫度的顧客體驗" is the stated belief. *UI implication:* medium weights, open tracking, consultative CTA language ("專家諮詢" not "立即購買"), and human-team imagery beside AI features.
4. **Prove it with partners and numbers.** Official Meta/LINE/WhatsApp partnership and 5,000+ clients carry the trust load. *UI implication:* partner logos and quantified case-study metrics are first-class design elements, placed early and styled prominently.
5. **One drama per page.** The black aurora hero is the spectacle; everything after is calm white shelf. *UI implication:* reserve gradient and black bands for the opening statement and one AI showcase; keep pricing and content sections flat, tinted, and quiet. The aurora gradient appears exactly twice per page: hero headline clip + hero CTA.

### Application rules

The source states these eight as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Omnichat-authored or a separately published UI specification.

- Stage hero statements on pure black (`#000000`) with the aurora gradient (`#ac9cff` → `#8e4dff` → `#69dfb2` → `#379afd`) clipped into the headline text
- Set headings in Outfit weight 500 with positive +1px letter-spacing; pair zh-TW copy in Noto Sans HK
- Use the notched corner (`13px 13px 0px`) on header CTAs and the ribbon (`0px 24px`) on action CTAs — the asymmetric corners are the brand signature
- Keep the action palette blue-only: `#006aff` chrome, `#408fff` in-page CTAs, `#0202a6` deep headings
- Separate sections with background alternation (white / `#f8f9fa` / `#ecf4ff` / black), not rules or heavy shadows
- Use heading color (deep blue vs sky vs ink) to signal content register
- Float the `#006aff` badge above the featured pricing card and mark it with a 2px `#006aff` border
- Keep shadows at feather strength (`rgba(0,0,0,0.06)`)

### Avoid

The source states these eight as its Don't list; they are kept as its rules, reasons included. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Omnichat-authored or a separately published UI specification.

- Use bold (700+) display weights — the system persuades at weight 500
- Track headings tight or negative — Omnichat's letter-spacing is always open and positive
- Round buttons symmetrically into default pills or plain rectangles — every CTA carries an asymmetric corner
- Introduce warm accent colors on actions — the raspberry `#cc3366` lives only inside mega-menu dropdowns
- Stack heavy drop shadows on cards — elevation stays whisper-quiet
- Put the aurora gradient on body text or small UI — it belongs to hero-scale statements and the hero CTA only
- Use grey hairline dividers between sections — switch the band background instead
- Mix a second blue system into chrome — `#006aff` and `#408fff` have fixed, separate jobs

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Token-set keys keep the YAML hex. Calling `#006aff` the chrome-level "do this" blue, calling `#408fff` the lighter working blue, calling `#0202a6` trust-bearing, calling the aurora stops a hero signature, calling heading hue a type hierarchy, and calling `#cc3366` a hidden wayfinding accent inside an otherwise all-blue system, are derived editorial implementation inferences from the verified surfaces; they are not Omnichat-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

Primary

- **Omnichat Blue** (`#006aff`): Primary brand action color — header CTA fill and outline, featured pricing-card border, the 最受歡迎 badge. The chrome-level "do this" blue. Token-set path `tokens.colors.primary`.
- **Action Blue** (`#408fff`): Lighter working blue for in-page CTAs (聯絡專人, 送出資料, 獲得報價), the active pricing toggle, inline link CTAs (了解完整案例), and the rounded wordmark moments. Token-set path `tokens.colors.action`.
- **Deep Blue** (`#0202a6`): Near-navy section-heading color for trust statements ("深受全球 5,000+ 客戶信賴") and the dark solid button (更多成功案例). A close variant `#0403b7` appears on feature H3s. Token-set path `tokens.colors.deep`. `#0403b7` has no YAML color key.

Aurora Gradient (hero signature)

- **Gradient Lavender** (`#ac9cff`): First and last stop of the 270° hero text gradient. Token-set path `tokens.colors.gradient-lavender`.
- **Gradient Violet** (`#8e4dff`): Second stop — the purple depth of the aurora. Token-set path `tokens.colors.gradient-violet`.
- **Gradient Mint** (`#69dfb2`): Center stop — the green flash that ties to the logo's mint petals. Token-set path `tokens.colors.gradient-mint`.
- **Gradient Sky** (`#379afd`): Fourth stop — bridges the aurora back to brand blue. Token-set path `tokens.colors.gradient-sky`.
- **Gradient Base** (`#3337f6`): Indigo base color of the hero gradient CTA button (stops run `#3332f6` → `#379afd` → `#69dfb2`). Token-set path `tokens.colors.gradient-base`. `#3332f6` has no YAML color key.

Headings on Dark

- **Heading Sky** (`#4e97ff`): AI feature H3s ("Omni AI Message Flow：AI 行銷活動產生器"). Token-set path `tokens.colors.heading-sky`.
- **Heading Sky Soft** (`#75adfc`): Large section H2s on black bands. Token-set path `tokens.colors.heading-sky-soft`.
- **Hero Sub** (`#8ca8cf`): Muted blue-grey hero subtitle ("Omni AI by Omnichat"). Token-set path `tokens.colors.hero-sub`.

Neutral & Ink

- **Ink** (`#333333`): Document body color and nav links. Token-set path `tokens.colors.ink`.
- **Ink Strong** (`#2e2e2e`): Strong section H2s on white ("為何 Omnichat 是最好的選擇？"). Token-set path `tokens.colors.ink-strong`.
- **Ink Heading** (`#3d3d3d`): H3s, plan names, partner statements. Token-set path `tokens.colors.ink-heading`.
- **Muted** (`#666666`): Secondary copy and descriptions. Token-set path `tokens.colors.muted`.
- **Icon Grey** (`#69727d`): Footer social icons and tertiary glyphs. Token-set path `tokens.colors.icon-grey`.

Surface

- **Canvas** (`#ffffff`): Page background, cards, button text on blue. Token-set path `tokens.colors.canvas`.
- **Surface** (`#f8f9fa`): The dominant section-band grey (most frequent background on the homepage). Token-set path `tokens.colors.surface`.
- **Surface Alt** (`#f9f9f9`): Form-field fill. Token-set path `tokens.colors.surface-alt`.
- **Tint Blue** (`#ecf4ff`): Light-blue pricing hero band. Token-set path `tokens.colors.tint-blue`.
- **Tint Blue Deep** (`#d9e8ff`): Deeper blue tint for feature chips and translucent panels (`rgba(217,232,255,0.75)`). Token-set path `tokens.colors.tint-blue-deep`.
- **Hero Dark** (`#000000`): Full-bleed black hero and AI showcase bands. Token-set path `tokens.colors.hero-dark`.
- **Input Border** (`#c5c5c5`): Form-field hairline. Token-set path `tokens.colors.input-border`.

Accent (menu-only)

- **Menu Accent** (`#cc3366`): Raspberry-pink link color inside the mega-menu dropdowns (platform/product lists). It lives only in the opened navigation panels — never on buttons or body copy. Token-set path `tokens.colors.menu-accent`.

### Spacing

Token-set path `tokens.spacing`, unitless steps as the YAML recorded them:

| Step | Value | Token-set path |
|---|---:|---|
| xs | 3 | `tokens.spacing.xs` |
| sm | 8 | `tokens.spacing.sm` |
| md | 10 | `tokens.spacing.md` |
| base | 16 | `tokens.spacing.base` |
| lg | 20 | `tokens.spacing.lg` |
| xl | 27 | `tokens.spacing.xl` |
| xxl | 36 | `tokens.spacing.xxl` |

The source also names a base unit of ~4px with measured stops at 3px, 8px, 10px, 16px, 20px, 27px, 36px. Notable: pricing cards run asymmetric 36px 26px padding with a deep 130px bottom reserve for the anchored CTA; panels use 27px. `tokens.spacing.md: 10` is not `tokens.rounded.md: 10`. `tokens.spacing.lg: 20` is not `tokens.rounded.lg: 20`. `tokens.spacing.base: 16` is not the 16px body size and is not button font 16px. `tokens.spacing.sm: 8` is not the 8px input radius. `tokens.spacing.xl: 27` is the spacing step and the 27px panel padding sits on Tab Content Panel. `tokens.spacing.xxl: 36` is not a type size. The ~4px base unit is not a YAML spacing key. Keeping each number on its own key path, rather than treating a shared numeral as the same token, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded`, unitless steps as the YAML recorded them:

| Step | Value | Token-set path |
|---|---:|---|
| sm | 5 | `tokens.rounded.sm` |
| md | 10 | `tokens.rounded.md` |
| lg | 20 | `tokens.rounded.lg` |
| full | 99 | `tokens.rounded.full` |

Source §5 also writes the scale with uses:

- Small (5px): badges, dropdown bottoms
- Medium (8–10px): inputs, gradient/deep buttons
- Notch (`13px 13px 0px`): header CTAs, tab panels (inverted)
- Large (20px): pricing cards
- Ribbon (`0px 24px`): action CTAs — opposing corners only
- Full (99px): segmented pills

`tokens.rounded.md: 10` is the YAML medium step. It is not `tokens.spacing.md: 10`. `tokens.rounded.lg: 20` is the pricing-card step and is not `tokens.spacing.lg: 20`. `tokens.rounded.sm: 5` is the badge step. `tokens.rounded.full: 99` is the segmented-pill step. The 8px input radius, the notched `13px 13px 0px`, and the ribbon `0px 24px` sit on those components; they are not YAML rounded keys. Reading those notched and ribbon corners as the brand signature, and keeping those local radii off the YAML scale, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Bands, hero, nav, badges |
| Tint (Level 1) | `#f8f9fa` / `#ecf4ff` background shift | Section and chapter separation |
| Feather (Level 2) | `rgba(0,0,0,0.06) 3px 3px 15px` | Pricing cards — barely-there lift |
| Panel (Level 3) | `rgba(0,0,0,0.15) 0px 3px 7px` | Tab content panels, dropdown sheets |

Token-set path `tokens.shadow.card`: `rgba(0,0,0,0.06) 3px 3px 15px 0px`. Token-set path `tokens.shadow.panel`: `rgba(0,0,0,0.15) 0px 3px 7px 0px`. The YAML forms with trailing `0px` and the Level 2 / Level 3 spellings without that trailing zero are the same two shadows on two writings; neither replaces the other. Keeping those two writings side by side, rather than collapsing either, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

**Shadow Philosophy**, as the source states it: Omnichat keeps elevation nearly invisible. The two measured shadows are a feather-soft 6%-alpha card lift and a slightly firmer 15%-alpha panel drop — both tight and neutral. Drama is delegated entirely to the black bands and the aurora gradient; surfaces themselves stay calm and flat so the conversational UI mockups inside them (chat bubbles, LINE/WhatsApp threads) carry the visual interest. When the system wants emphasis it changes the background color of a whole band, never the z-axis of a single card. Reading that stack as whisper-quiet elevation with drama delegated to the black bands, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Motion

The source's HTML comment marks States and Motion as design-system extrapolations consistent with the observed live aesthetic (flat surfaces, feather shadows, single ambient gradient), editorial/illustrative rather than documented Omnichat product specs. The durations, easing roles, and motion rules below are therefore a derived editorial implementation inference from the verified surfaces; they are not Omnichat-authored or a separately published UI specification.

**Durations**:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 150ms | Hover, pill toggle, focus |
| `motion-standard` | 250ms | Card reveal, dropdown sheets, tab panels |
| `motion-slow` | 400ms | Band transitions, hero entrance |
| `motion-ambient` | 6–10s | Aurora gradient sweep on hero text and CTA |

**Easings** — token names and uses. The source lists `ease-enter` as arriving (dropdowns, panels, cards) and `ease-exit` as dismissals. Those two curve values are not traceable to Omnichat computed evidence, so the curves are omitted here and only the roles and their uses are kept. Omitting those two curve values and keeping only the roles and their uses is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

| Token | Use |
|---|---|
| `ease-enter` | Arriving — dropdowns, panels, cards |
| `ease-exit` | Dismissals |
| `ease-ambient` | `linear` — the looping aurora gradient |

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition.

Motion rules, as the source states them: The aurora gradient is the brand's only continuous motion — a slow linear 270° sweep across the hero headline and gradient CTA, ambient like the AI it represents. Everything else is quick and functional: dropdown mega-menus slide down with `motion-standard / ease-enter`, the pricing pill slides its active state, chat-bubble mockups may type in once on scroll-into-view. No bounce, no spring — the product sells reliability to retailers. Under `prefers-reduced-motion: reduce`, the aurora freezes to a static gradient and all transitions collapse to instant; nothing functional is lost.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Official product-use | The homepage, pricing, about, and blog pages state the product and the zh-TW storefront. They do not publish a universal current typography token or a separately issued type specimen. That "no published type token" reading is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification. |
| Live computed surface-use | Latin display and headings compute as Outfit; buttons, nav, and body-level zh-TW copy compute as Noto Sans HK. |
| Official distributed asset | No Omnichat-exclusive distributed type family was verified. That "no exclusive distributed family" reading is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification. |
| Declared-only | Body default is a system stack (`-apple-system, "Segoe UI", Roboto, "Noto Sans"...`) at 16px/24px. Those fallback members are declared. They are fallbacks, not a second brand face. Classing those fallback members as not the brand face is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification. |
| License | This record does not establish an Omnichat font-license notice for Outfit or Noto Sans HK. Outfit and Noto Sans HK are upstream faces, not Omnichat-owned brand assets; that classification is a derived editorial implementation inference from the verified surfaces, and it is not Omnichat-authored or a separately published UI specification. |
| Outside these captures | Typography on surfaces beyond the token-inspected homepage and pricing page, other than the about and blog copy the source already quotes, sits outside this contract. That outside-this-contract reading is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification. |

### Family

- **Display / Latin:** `Outfit` — geometric rounded sans for all headings and English display text. Token-set path `tokens.typography.family.display`.
- **Traditional Chinese UI:** `"Noto Sans HK"` — buttons, nav, body-level zh-TW copy. Token-set path `tokens.typography.family.zh`.
- **Body default:** system stack (`-apple-system, "Segoe UI", Roboto, "Noto Sans"...`) at 16px/24px.

A fallback member of a stack is never presented as the brand face. Do not replace Outfit or Noto Sans HK with a system substitute. That fallback prohibition is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Type roles

YAML token-set roles keep unitless line heights as ratios. Source §3 spells several of the same roles with px equivalents, size ranges, and tracking in `+px`. Both writings are kept; the longer §3 form is not collapsed into the YAML single.

| Role | Font | Size | Weight | Line height | Letter spacing | Token-set use |
|---|---|---|---:|---|---|---|
| Display Hero | Outfit | 52px | 500 | 1.23 (64px) | YAML `tracking: 1.4` / §3 +1.4px | Hero headline, aurora-gradient-clipped Outfit on black |
| Display XL | Outfit | 46px | 500 | 1.40 | YAML `tracking: 1` / §3 +1px | Major section headlines on dark bands |
| Section | Outfit | YAML 40 / §3 40–42px | 500 | 1.60 | YAML `tracking: 1` / §3 +1px | Section titles (deep blue or ink) |
| Feature | Outfit | 34px | 500 | 1.40 | YAML `tracking: 1` / §3 +1px | Feature / case-study headings |
| Card Head | Outfit | 26px | 500 | 1.30 | YAML `tracking: 1` / §3 +1px | Card and capability heads |
| Plan Head | Outfit | 24px | 500 | — | YAML `tracking: 1` / §3 +1px | Pricing plan names |
| Sub-head | Outfit | 22–24px | 500 | 1.40–1.60 | +0.6–1px | Partner line, hero eyebrow. No YAML typography key. Hero subtitle is 24px Outfit 500 in `#8ca8cf`. |
| Nav | Noto Sans HK | 17px | 400 | — | normal | Top navigation items |
| Body / Button | Noto Sans HK | 16px | YAML 400 / §3 400–500 | 1.50 (24px) | normal–0.6px | Body copy, buttons, badges. Body copy 400; buttons 500. |
| Menu Item | Noto Sans HK | 14px | 500 | — | normal | Mega-menu dropdown links |

Token-set paths: `tokens.typography.display-hero` · `tokens.typography.display-xl` · `tokens.typography.section` · `tokens.typography.feature` · `tokens.typography.card-head` · `tokens.typography.plan-head` · `tokens.typography.nav` · `tokens.typography.body` · `tokens.typography.menu-item`. Sub-head is a §3-only writing. Unitless line heights stay ratios: `1.23`, `1.40`, `1.60`, `1.30`, `1.50`. The parenthetical `64px` and `24px` are the source §3 spellings, not replacements of those ratios. Keeping the YAML singles and the §3 ranges on separate readings, and keeping YAML `tracking: 1.4` / `1` beside §3 `+1.4px` / `+1px`, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Typography rules

The source states these four as its typography principles. Reading the measured metrics as those principles is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

- **Medium, never bold**: Weight 500 is the display weight across every size. Omnichat persuades with color and gradient, not with heavy strokes.
- **Positive tracking as identity**: +1px to +1.4px letter-spacing on headings — open and airy where most tech brands track tight. Body returns to normal tracking.
- **Color is the type hierarchy**: The same 40px/500 heading switches between deep blue (`#0202a6`) for trust claims, ink (`#2e2e2e`) for questions, and sky (`#75adfc`) on dark — meaning is encoded in hue, not weight.
- **Two scripts, one voice**: Outfit's geometric rounds and Noto Sans HK's clean strokes are matched in optical weight so bilingual lines feel uniform.

### Assets

- Catalog favicon pointer the source frontmatter records: `https://www.omnichat.ai/wp-content/uploads/webiconlogo.png` (`logo.type: favicon`). That URL is dual: identity metadata and this Assets pointer.
- Product UI mockups (chat threads, dashboards) sit directly on band backgrounds without frames. Client logos render monochrome on white cards in case-study rows. Treating those mockups and logos as first-party imagery, and the instruction not to replace them with invented brand-color decoration, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### How to read this section

The source declares each token-set component with a primitive type (`button`, `tab`, `input`, `card`, `badge`) and a value set. Those types are preserved per component. A `Primitive type` line is attached only when the source YAML records that type on that component. §4-only components are labelled `not in the token set`. Applicability below is judged by each control's role, never by whether a visual treatment for that state happens to be recorded: where the source supplies no treatment for an applicable state, the value is omitted and the state stays applicable. `not-applicable` is used only where the control's role makes the state meaningless, and the reason given is always that semantic one. Where the source supplies no interaction evidence for a container at all, its kind and applicability map are omitted rather than decided. A generic `focus` observation is not `focus-visible` treatment evidence; `focus-visible` stays applicable on interactive controls and unobserved visual treatments are omitted. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow product role, not primitive kind. Every kind verdict, every applicability verdict, and the reason given for either, keeping YAML token-set writings beside longer §4 body writings rather than collapsing either, and reading a generic `focus` observation as not `focus-visible` treatment evidence, are derived editorial implementation inferences from the verified surfaces; they are not Omnichat-authored or a separately published UI specification. This is not a complete state-coverage claim.

The source's live inspection recorded computed default styling. Hero Gradient records a hover treatment. The state treatments in the State record below are stated at system level rather than measured per control. Stating those treatments at system level rather than as per-control measurements is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

### Header Primary (專家諮詢)

- Role: Header expert-consult CTA — signature notched bottom-right corner
- Primitive type: `button` · Kind: interactive
- Background: `#006aff`
- Text: `#ffffff`
- Border: 1px solid `#006aff`
- Radius: `13px 13px 0px`
- Padding: 3px
- Height: 32px
- Font: 16px / 500 / Noto Sans HK
- Token-set path: `tokens.components.button-primary`
- Token-set use: Header 專家諮詢 CTA — notched bottom-right corner
- Token-set font: `16px / 500`
- Token-set border: 1px solid #006aff

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An entry point whose availability can lapse; the system-level reduced-opacity blue applies and no opacity value is given |
| loading | not-applicable | This control takes the reader toward 專家諮詢; it does not commit an in-page operation whose in-progress state it could report. |
| error | not-applicable | This control takes the reader to a destination; it does not commit an operation whose outcome it could report. |
| success | not-applicable | Same role reason: reaching a consult destination is not an operation with a success result. |

### Header Outline (免費試用)

- Role: Free-trial secondary CTA, identical notched geometry next to the primary
- Primitive type: `button` · Kind: interactive
- Background: `#ffffff`
- Text: `#006aff`
- Border: 1px solid `#006aff`
- Radius: `13px 13px 0px`
- Padding: 3px
- Height: 32px
- Font: 16px / 500 / Noto Sans HK
- Token-set path: `tokens.components.button-outline`
- Token-set use: Header 免費試用 secondary CTA, same notched geometry
- Token-set font: `16px / 500`
- Token-set border: 1px solid #006aff

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An entry point whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control takes the reader toward 免費試用; it does not commit an in-page operation whose in-progress state it could report. |
| error | not-applicable | This control takes the reader to a destination; it does not commit an operation whose outcome it could report. |
| success | not-applicable | Same role reason: reaching a trial destination is not an operation with a success result. |

### Hero Gradient (專家諮詢)

- Role: Hero CTA on the black stage — the aurora gradient made clickable
- Primitive type: `button` · Kind: interactive
- Background: `#3337f6`
- Text: `#ffffff`
- Radius: 10px
- Padding: 10px
- Height: 44px
- Font: 16px / 500 / Noto Sans HK
- Hover: animated 270° gradient sweep (`#3332f6` → `#379afd` → `#69dfb2` → `#379afd` → `#3332f6`)
- Token-set path: `tokens.components.button-gradient`
- Token-set use: Hero 專家諮詢 CTA on black
- Token-set font: `16px / 500`
- Token-set states: animated 270deg gradient #3337f6 → #379afd → #69dfb2

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Recorded hover: animated 270° gradient sweep (`#3332f6` → `#379afd` → `#69dfb2` → `#379afd` → `#3332f6`). YAML states write the shorter `#3337f6` → `#379afd` → `#69dfb2`. Both writings stay. |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An entry point whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control takes the reader toward 專家諮詢 from the black stage; it does not commit an in-page operation whose in-progress state it could report. |
| error | not-applicable | Destination role; the destination, not this button, reports failure. |
| success | not-applicable | Same role reason: reaching a consult destination is not an operation this button reports as success. |

### Action Ribbon (聯絡專人 / 送出資料 / 獲得報價 / 預約示範)

- Role: All pricing and form CTAs — opposing corners rounded into a ribbon shape
- Primitive type: `button` · Kind: interactive
- Background: `#408fff`
- Text: `#ffffff`
- Border: 1px solid `#006aff`
- Radius: `0px 24px`
- Padding: `8px 20px 11px`
- Height: 45px
- Font: 16px / 500 / Noto Sans HK
- Compact 33px variant uses `2px 20px 5px` padding
- Lead-form submit variant uses padding `9px 80px 10px`
- Token-set path: `tokens.components.button-action`
- Token-set use: 聯絡專人 / 送出資料 ribbon CTA — opposing corners rounded
- Token-set font: `16px / 500`
- YAML records no border on this token; §4 records `1px solid #006aff`. Both writings stay.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An entry point whose availability can lapse; the system-level reduced-opacity blue applies and no opacity value is given |
| loading | applicable | 送出資料 commits the lead-form submit; visual treatment omitted |
| error | applicable | A submit control can report a failed request; visual treatment omitted |
| success | applicable | A submit control can report completion; visual treatment omitted |

### Deep Solid (更多成功案例)

- Role: Case-study section CTA — deep blue gravity without the gradient
- Primitive type: not in the token set · Kind: interactive
- Background: `#0202a6`
- Text: `#ffffff`
- Radius: 10px
- Padding: 10px
- Font: 16px / 500 / Outfit
- Use: 更多成功案例

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An entry point whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control takes the reader toward 更多成功案例; it does not commit an in-page operation whose in-progress state it could report. |
| error | not-applicable | Destination role; the destination, not this button, reports failure. |
| success | not-applicable | Same role reason: reaching case studies is not an operation this button reports as success. |

### Pricing Product Switcher

- Role: Segmented pill switching 線上對話商務 / OMO全渠道銷售 / Social CDP plan groups
- Primitive type: `tab` · Kind: interactive
- Background: `#ffffff`
- Text: `#408fff`
- Radius: 99px
- Padding: 6px
- Height: 36px
- Font: 16px / 500 / Noto Sans HK
- Active: `#408fff` background + `#ffffff` text
- Track: 99px-radius track on white with 1px blue outline
- Token-set path: `tokens.components.segment-toggle`
- Token-set use: Pricing product switcher pill (線上對話商務 / OMO / Social CDP)
- Token-set font: `16px / 500`
- Token-set active: bg `#408fff` + text `#ffffff`
- YAML use writes `OMO`; §4 writes `OMO全渠道銷售`. The longer form stays beside the YAML use.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A switcher option whose availability can lapse; visual treatment omitted |
| loading | not-applicable | A segmented pill selects a plan group; it commits no operation in place |
| error | not-applicable | A tab switcher does not report a failed request on itself |
| success | not-applicable | Selecting a plan group is not an operation this control reports as success |

### Lead-Form Field

- Role: Demo-request form fields (name, company, email)
- Primitive type: `input` · Kind: interactive
- Background: `#f9f9f9`
- Text: `#3d3d3d`
- Border: 1px solid #c5c5c5
- Token-set border: 1px solid #c5c5c5
- Radius: 8px
- Padding: `8px 16px`
- Height: 44px
- Font: 16px / 400
- Token-set path: `tokens.components.input-default`
- Token-set use: Lead-form text field
- Token-set font: `16px / 400`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A field whose availability can lapse; visual treatment omitted |
| loading | not-applicable | A text field does not commit an operation whose in-progress state it could report |
| error | applicable | Form field; the system-level field-level validation message applies and no color value is given for this input |
| success | not-applicable | Form-submitted success replaces the form body; it is not a success paint on this field |

### Pricing Plan Card

- Role: Plan cards (高效客服方案, 基本行銷商務…); featured plan swaps the invisible border for 2px solid `#006aff`
- Primitive type: `card`
- Background: `#ffffff`
- Border: 2px solid `#ffffff` (featured: 2px solid `#006aff`)
- Radius: 20px
- Padding: `36px 26px` with a 130px bottom reserve for the anchored CTA
- Width: 288px
- Shadow: `rgba(0,0,0,0.06) 3px 3px 15px`
- Token-set path: `tokens.components.card-pricing`
- Token-set use: Featured plan card; non-featured plans carry an invisible 2px #ffffff border
- Token-set border: `2px solid #006aff` (featured writing). YAML and §4 both stay: YAML names the featured border; §4 names the non-featured `2px solid #ffffff` and the swap.

The source supplies no interaction evidence for this card, so kind and a state-applicability map are both withheld.

### Tab Content Panel

- Role: Content panel hanging below active tabs — top corners squared into the tab bar
- Primitive type: not in the token set
- Background: `#ffffff`
- Radius: `0px 0px 13px 13px`
- Padding: 27px
- Shadow: `rgba(0,0,0,0.15) 0px 3px 7px`

The source supplies no interaction evidence for this panel, so kind and a state-applicability map are both withheld.

### 最受歡迎 (Most Popular)

- Role: Plan ribbon floating above the featured pricing card
- Primitive type: `badge`
- Kind: non-interactive — a plan ribbon, not a control
- Background: `#006aff`
- Text: `#ffffff`
- Radius: 5px
- Padding: `1px 32px`
- Height: 28px
- Font: 16px / 400
- Token-set path: `tokens.components.badge-popular`
- Token-set use: 最受歡迎 plan ribbon

### Navigation

- Role: Top nav (平台 / 產品 / 案例 / 價格 / 資源 / 關於 Omnichat); active section turns `#408fff`
- Primitive type: not in the token set · Kind: interactive
- Background: `#ffffff`
- Text: `#333333`
- Font: 17px / 400 / Noto Sans HK
- Height: 60px rows
- Dropdown items: `#cc3366` text, 14px / 500, radius `0px 0px 5px 5px`, 42px rows

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web nav; mega-menu hover dropdowns are recorded as a collapsing behavior, not as a fill token |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination item whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This item is a destination link; it does not commit an operation whose in-progress state it could report. |
| error | not-applicable | Destination link; the destination, not the item, reports failure. |
| success | not-applicable | Same role reason: reaching a nav destination is not an operation with a success result. |

### State record

The source's state contract, preserved with its values and copy. The source's own HTML comment marks States as design-system extrapolations consistent with the observed live aesthetic, editorial/illustrative rather than documented Omnichat product specs; the treatments below are therefore a derived editorial implementation inference from the verified surfaces, and they are not Omnichat-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no conversations yet)** | White canvas, single Ink (`#333333`) line explaining the channel isn't connected, with one Action Blue (`#408fff`) ribbon CTA to connect LINE/WhatsApp. Chat-bubble illustration kept minimal. |
| **Empty (no campaign results)** | Muted (`#666666`) single line on `#f8f9fa`; CTA offers a template, not a blank editor. |
| **Loading (dashboard)** | Skeleton blocks on `#f8f9fa` at final dimensions, 20px radius for cards; flat pulse, no shimmer drama — consistent with the feather-shadow system. |
| **Loading (AI generating)** | The one permitted spectacle: aurora-gradient progress treatment on the generating element, echoing the hero gradient. |
| **Error (channel disconnected)** | Inline banner with plain-language cause and a reconnect action; never just "發生錯誤". Deep tone, no red flooding. |
| **Error (form validation)** | Field-level message below the `#c5c5c5`-bordered input; states what a valid value looks like. |
| **Success (campaign sent)** | Calm inline confirmation with the sent count — a number, in brand fashion. No confetti. |
| **Success (form submitted)** | Confirmation replaces the form body; next step (專人將與您聯繫) stated explicitly. |
| **Skeleton** | `#f8f9fa` blocks, 20px radius on cards, 99px on pill placeholders; flat pulse. |
| **Disabled** | Buttons drop to reduced-opacity blue rather than grey, preserving the blue-only action language; text at `#69727d`. |

These rows describe empty, loading, error, success, skeleton, and disabled treatments the source wrote at system level. They are not attached as visual treatments to the marketing destination controls above except where a control's role already makes that state applicable. That non-attachment, except where a control's role already makes the state applicable, is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

- Full-bleed alternating bands: black hero → white/`#f8f9fa` content → black AI showcase → `#ecf4ff` pricing tint
- Centered single-column hero with a single inline question field ("你想從哪裡著手?")
- Pricing: 4-up equal-width plan cards (288px) under a centered 99px segmented pill
- Case studies: logo-led cards with 了解完整案例 link CTAs in repeating rows
- Spacing restated from `tokens.spacing`: 3 / 8 / 10 / 16 / 20 / 27 / 36
- Shape restated from `tokens.rounded`: badges 5 · medium 10 · pricing cards 20 · pills 99, with notched `13px 13px 0px` and ribbon `0px 24px` kept on those components

Whitespace philosophy, as the source states it:

- **Stage vs. shelf**: black bands are theatrical and sparse (one headline, one CTA); white bands are denser, shelf-like product sections. The contrast in density is intentional rhythm.
- **Tint as chapter break**: `#ecf4ff` and `#d9e8ff` bands mark commercial chapters (pricing, lead capture) without needing rules or borders.
- **Soft separation**: shadows are feather-light; most separation comes from background alternation.

Reading black bands as theatrical stage against white shelf, reading tint as chapter break, and reading density contrast as intentional rhythm, are derived editorial implementation inferences from the verified surfaces; they are not Omnichat-authored or a separately published UI specification.

Responsive behavior. The source's live inspection was a desktop computed-style pass; the breakpoints, collapsing strategy, image behavior, and recorded target sizes are stated by the source at system level rather than measured across viewports, so they are a derived editorial implementation inference from the verified surfaces and are not Omnichat-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <768px | Single column, hero text scales down, plan cards stack |
| Tablet | 768–1024px | 2-up plan cards, condensed nav |
| Desktop | 1024–1440px | Full 4-up pricing, mega-menu navigation |

Touch targets the source records:

- Action ribbon CTAs at 45px height with 20px side padding
- Segmented pill options at 36px inside a generously padded track
- Form fields at 44px height
- Nav rows at 60px with dropdown items at 42px

Collapsing strategy, as the source states it:

- Hero: 52px aurora headline compresses while keeping gradient clip and +letter-spacing
- Pricing: 4-up cards → stacked column, featured card keeps its `#006aff` border and badge
- Mega-menu: hover dropdowns → accordion drawer
- Black showcase bands keep full-bleed treatment with reduced internal padding

Image behavior: Product UI mockups (chat threads, dashboards) sit directly on band backgrounds without frames. Client logos render monochrome on white cards in case-study rows. Cards maintain 20px radius across breakpoints.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Omnichat's voice is **confident, conversion-obsessed, and warmly pragmatic** — a sales engineer who genuinely believes conversation is commerce. Every headline ties technology to a business outcome: AI is never demoed for its own sake, it "賦能客服、行銷、銷售轉換" (empowers service, marketing, and sales conversion). Numbers do the persuading — 5,000+ clients, 50% open rates, 轉換率 (conversion rate) appears in nearly every section — while the register stays polite Taiwanese business Mandarin with English product nouns (Omni AI, Social CDP, OMO) left untranslated as badges of category leadership. That characterization, that register reading, and the tone table below are a derived editorial implementation inference from the verified surfaces; they are not Omnichat-authored or a separately published UI specification. The zh-TW lines themselves are live surface copy.

| Context | Tone |
|---|---|
| Hero headlines | Platform-scale declarations. "AI 全通路顧客體驗平台" — category first, capability second. |
| Feature heads | Outcome verbs: 打造, 賦能, 轉換, 加速. Tech term + business result in one line. |
| CTAs | Consultative, low-friction: "免費試用", "專家諮詢", "聯絡專人", "預約示範" — talk to a human, not "Buy now". |
| Trust claims | Quantified and partner-anchored: "深受全球 5,000+ 客戶信賴", "LINE、WhatsApp、Meta 官方合作夥伴". |
| Case studies | Client name + metric + mechanism: "信義房屋…締造精準受眾推播 50% 超高開封率". |
| Pricing | Friendly imperative with one exclamation allowed: "選擇最適合您的方案，優化客戶服務，提升銷售業績！" |

**Voice samples (verbatim from live zh-TW surfaces):**

- "AI 全通路顧客體驗平台 全方位賦能客服、行銷、銷售轉換" — hero H1
- "全渠道 AI 對話商務平台 深受全球 5,000+ 客戶信賴" — homepage H2
- "為何 Omnichat 是最好的選擇？" — homepage H2
- "選擇最適合您的方案，優化客戶服務，提升銷售業績！" — pricing subtitle
- "Omnichat 讓品牌與顧客保持緊密連結互動，以提升品牌的轉換率為使命。" — about page mission

An English gloss never replaces a zh-TW line.

**Forbidden register**: vague AI mysticism without a metric, hard-sell urgency ("限時搶購"), untranslated jargon walls, and cold enterprise stiffness — the brand believes "有溫度的顧客體驗" (warm customer experience) and its copy must feel like a helpful consultant, not a vending machine. That forbidden-register reading is a derived editorial implementation inference from the verified surfaces; it is not Omnichat-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Recorded unresolved decisions

These are named values, not permissions to invent. The source records no conflict among its own Tier 1 values.

- **Exact easing curves for `ease-enter` and `ease-exit`.** Two easing roles and their uses are established; the curve values are omitted because they are not traceable to Omnichat computed evidence. Promote a curve for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed.
- **The disabled opacity value.** The system states that buttons drop to reduced-opacity blue rather than grey, with text at `#69727d`, without naming the opacity.
- **Plan Head, Nav, and Menu Item line-height.** The source hierarchy table leaves those three line-height cells unnamed.
- **About and blog as token sources.** `https://www.omnichat.ai/tw/about/` and `https://blog.omnichat.ai/tw/` are named brand-owned sources. They confirm founding, mission, and case-study copy. They do not contribute computed interface tokens.
