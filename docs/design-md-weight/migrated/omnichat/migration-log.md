# Omnichat migration log

Source: `web/references/omnichat/DESIGN.md`
Sibling read (not the migration input): `web/references/omnichat/.verification.md`
Destination: `docs/design-md-weight/migrated/omnichat/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/omnichat/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `ffaf8fe5312316c76366fb49ec0144b073ec020adba3292b158a48de01253996` (`web/references/omnichat/DESIGN.md`). Sibling SHA-256 `a0d9cda64110005516c941457e9b1e26d8c74983780018cdb4ce5f96c85226f2` (`web/references/omnichat/.verification.md`). Worker-close portable DESIGN SHA-256 `71ad9a2edf4f5fe5a5ddb2aa421c9004b7c72fd695f769b352befcb1e6cc2b25`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Assets | Portable file has no frontmatter. H1 is `# Omnichat Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://www.omnichat.ai` is dual: `DESIGN.md` Scope + `provenance.md` Identity (E2a). `#006aff` is dual: `DESIGN.md` (`grep -oF '#006aff'` dest 21) + `provenance.md` (8). Favicon `webiconlogo.png` is dual: `DESIGN.md` Assets 1 + `provenance.md` 2 (E2a). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source token note is transcribed verbatim in Identity. No `ds.name` / `ds.url` / `ds.type` in the source frontmatter — absence recorded, not filled (A1c). |
| YAML `tokens.colors` (24 keys) | 옮김 → Foundations semantic color | `DESIGN.md` Semantic color. All 24 YAML roles kept with names, values, and token-set paths: primary, action, deep, gradient-base, gradient-sky, gradient-mint, gradient-lavender, gradient-violet, heading-sky, heading-sky-soft, hero-sub, ink, ink-strong, ink-heading, muted, icon-grey, canvas, surface, surface-alt, tint-blue, tint-blue-deep, hero-dark, input-border, menu-accent. `grep -oF` dest: `#006aff` 21 · `#408fff` 12 · `#0202a6` 7 · `#3337f6` 4 · `#379afd` 11 · `#69dfb2` 9 · `#ac9cff` 4 · `#8e4dff` 4 · `#4e97ff` 2 · `#75adfc` 3 · `#8ca8cf` 2 · `#333333` 3 · `#2e2e2e` 3 · `#3d3d3d` 3 · `#666666` 2 · `#69727d` 3 · `#ffffff` 17 · `#f8f9fa` 9 · `#f9f9f9` 2 · `#ecf4ff` 7 · `#d9e8ff` 3 · `#000000` 3 · `#c5c5c5` 4 · `#cc3366` 4. Close variant `#0403b7` dest 2 (no YAML key). Gradient stop `#3332f6` dest 6 (no YAML key). `rgba(217,232,255,0.75)` dest 1. |
| YAML `tokens.typography.family` (`display: "Outfit"`, `zh: "Noto Sans HK"`) | 옮김 → Typography & Assets Family | `DESIGN.md` Family. Paths named `tokens.typography.family.display` dest 1 / `family.zh` dest 1. Outfit dest 20 · Noto Sans HK dest 18. Body default system stack kept as declared fallback, not as the brand face. The fallback prohibition carries an adjacent complete qualifier (B2/B2a). |
| YAML `tokens.typography.display-hero / display-xl / section / feature / card-head / plan-head / nav / body / menu-item` | 옮김 → Typography & Assets Type roles | `DESIGN.md` Type roles. Unitless line heights stay ratios (A1a): `1.23` dest 2 · `1.40` dest 4 · `1.60` dest 3 · `1.30` dest 2 · `1.50` dest 2. YAML `tracking: 1.4` dest 2 kept beside §3 `+1.4px`. All nine YAML `use` strings restored verbatim in the Token-set use column (A1). Full paths `tokens.typography.display-hero` … `menu-item` dest 1 each. Sub-head 22–24px is a §3-only writing. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Paths kept separate: `tokens.spacing.xs` dest 1 · `sm` 2 · `md` 3 · `base` 2 · `lg` 3 · `xl` 2 · `xxl` 2. `tokens.rounded.sm` dest 2 · `md` 3 · `lg` 3 · `full` 2. Spacing table: xs 3 · sm 8 · md 10 · base 16 · lg 20 · xl 27 · xxl 36. Shape table: sm 5 · md 10 · lg 20 · full 99. `tokens.spacing.md: 10` is not `tokens.rounded.md: 10`. `tokens.spacing.lg: 20` is not `tokens.rounded.lg: 20`. `tokens.spacing.base: 16` is not the 16px body size. Notch `13px 13px 0px` dest 8 and ribbon `0px 24px` dest 7 stay on components, off the YAML rounded map. ~4px base unit is not a YAML spacing key. |
| YAML `tokens.shadow.card` / `panel` | 옮김 → Foundations elevation | YAML `0px` forms dest 1 each: `rgba(0,0,0,0.06) 3px 3px 15px 0px` · `rgba(0,0,0,0.15) 0px 3px 7px 0px`. `tokens.shadow.card` dest 1 · `tokens.shadow.panel` dest 1. Level 2 / Level 3 spellings without trailing `0px` kept beside them. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×4 (Header Primary / Outline / Hero Gradient / Action Ribbon), `tab` ×1 (Pricing Product Switcher), `input` ×1 (Lead-Form Field), `card` ×1 (Pricing Plan Card), `badge` ×1 (最受歡迎). `not in the token set` dest 4 (Deep Solid, Tab Content Panel, Navigation — plus the how-to-read rule). All eight YAML `use` strings restored as `Token-set use:` lines, including `Featured plan card; non-featured plans carry an invisible 2px #ffffff border` dest 1. YAML `font` shorthands `16px / 500` dest 11 · `16px / 400` dest 3. YAML states byte form `animated 270deg gradient #3337f6 → #379afd → #69dfb2` dest 1. YAML borders `1px solid #006aff` dest 3 · `1px solid #c5c5c5` dest 2. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Key Characteristics as Distinctive traits. Atmosphere readings carry an adjacent complete qualifier at 11 (B2/B2a). Surface-boundary reading at 9. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Source group headings (Primary, Aurora Gradient, Headings on Dark, Neutral & Ink, Surface, Accent menu-only) kept as the grouping. Role-naming readings carry the adjacent qualifier at 86 (B2/B2a). |
| §3 Typography Rules — family, hierarchy | 옮김 → Typography & Assets | Evidence classes; family; hierarchy table with YAML singles beside §3 ranges (section 40–42px, body 400–500, Sub-head 22–24px). Official product-use and License rows carry adjacent complete qualifiers (B2/B2a). |
| §3 Principles — medium never bold, positive tracking, color as hierarchy, two scripts | 옮김 → Typography & Assets Typography rules | Under the qualifier at 250 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | §4 body values and YAML `use` / `font` / `states` / height / padding byte forms are both kept where they differ. Keep-both of YAML token-set writings and longer §4 body writings is named in the how-to-read qualifier at 267 (B2/B2a). Longer §4 Action Ribbon adds border `1px solid #006aff`, compact `2px 20px 5px` / 33px, and 獲得報價 / 預約示範. Longer Hero hover adds looping `#3332f6` stops beside the YAML states writing. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 네 개 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. `https://www.omnichat.ai/tw/` D 4 / P 11; `/pricing/` D 1 / P 4; `/about/` D 2 / P 3; `blog.omnichat.ai/tw/` D 2 / P 3 — dual (E2a). Conflicts unresolved: none — `provenance.md` Freshness. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | Full-bleed bands, hero field 你想從哪裡著手?, 4-up 288px cards, 130px bottom reserve, 27px panels. Stage vs. shelf / tint as chapter break readings carry the qualifier at 548 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Four-level table including Feather and Panel. YAML `0px` / Level 2–3 keep-both qualifier at 177; Shadow Philosophy paragraph is a qualified reading at 179 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | Eight items, matching the source Do list, under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | Eight items, under the qualifier at 69, including the `#cc3366` menu-only prohibition and the `#006aff` / `#408fff` separate-jobs rule. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Breakpoint table with Mobile `<768px` / Tablet `768–1024px` / Desktop `1024–1440px` kept as declared widths. Touch targets 45px / 36px / 44px / 60px / 42px. Collapsing strategy and image behavior kept. Stated at system level rather than measured across viewports — qualifier at 550. Exact string `measures 1440px` dest 0 / provenance 0 (D1). |
| §9 Agent Prompt Guide — Quick Color Reference, Example Component Prompts, Iteration Guide | 삭제; §9-only 값 세 건은 옮김 | Tool-facing copy-paste prompts. Three writings that lived only in §9 were moved before deletion (A3): segmented-switcher track `1px blue outline` dest 1; lead-form submit padding `9px 80px 10px` dest 1; aurora gradient appears `exactly twice per page` dest 1 (Principle 5). No receiving slot and no delegation (A2, A3). §9 deletion check is in `provenance.md` Omission ledger. |
| §10 Voice & Tone | 옮김 → Content & Locales | Characterization, tone table, five verbatim zh-TW samples, forbidden register. English gloss sits beside a zh-TW line and never replaces it (A5). Qualifier at 577 covers the characterization and the table (B2/B2a). Forbidden-register qualifier at 598. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: **2017 by Alan Chan** dest 1; coworking desk dest 1; Facebook Messenger dest 1; Instagram and WeChat dest 1; LINE 官方帳號 dest 2; 信義房屋 dest 3; à la sha dest 2; 博士助聽器 dest 2; mission sentence dest 2; founder quote dest 1; US$1.8M Pre-A dest 2; ~300% ARR dest 2; Malaysia, Singapore, Thailand and Indonesia dest 3; WhatsApp Business Solution Provider dest 2; 2025-era **Agentic AI customer experience platform** dest 1; faceless automation dest 2; 有溫度的顧客體驗 dest 4; closing **The aesthetic says AI infrastructure** dest 1. Marked there as narrative context that supplies no interface tokens. The classification carries an adjacent complete qualifier (B2/B2a). |
| §12 Principles — 5 numbered | 옮김 → Experience principles | Under the B2a form at 46: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Omnichat-authored or a separately published UI specification." No published first-party DS, so the toss-form close is used (rulebook v12 B2a 전제 주석). |
| §13 Personas — 3 entries | 삭제 | The source's own italic line labels them fictional archetypes informed by publicly observable Omnichat customer segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar as names, ages, cities, motivations, or affiliation classifications (D2, D2a). Audience at `DESIGN.md` 29 carries only the group-level wording the source header records (`Taiwanese omnichannel retailers` SRC 1 / DST 2; `e-commerce marketing teams` SRC 1 / DST 2; `customer-service leads` SRC 1 / DST 2), under an adjacent qualifier. The four `primary-tasks` come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". Each task item greps in the source at ≥1. |
| §14 States — 10 rows | 옮김 → Components & States State record + per-component applicability | Full ten-row body preserved at `DESIGN.md` State record (A2; the catalog graph is still 0/440, so nothing is delegated), including 發生錯誤 dest 1 and 專人將與您聯繫 dest 1. The source HTML comment marks States as editorial/illustrative; the qualifier at 515 covers that class. System-level rather than per-control: qualifier at 269. Non-attachment except where role already makes the state applicable: qualifier at 530. The applicability rule is at 267. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 7 interactive components × 7 states. Header Primary / Outline / Hero Gradient / Deep Solid / Navigation close loading/error/success as destination (C2 v10). Action Ribbon keeps loading/error/success applicable because 送出資料 commits the lead-form submit. Pricing Product Switcher (tab) closes loading/error/success as a plan-group selector with no in-place commit. Lead-Form Field keeps error applicable and closes loading/success on field role. Pricing Plan Card and Tab Content Panel get no `kind` and no map because the source supplies no interaction evidence (C4). 最受歡迎 declares `Kind: non-interactive` with a reason instead of a map (Core §4.4). Hero Gradient hover keeps the recorded 270° sweep; it is not turned into `not-applicable`. |
| §15 Motion & Easing — durations, easing token names/roles, motion rules, reduced-motion | 옮김 → Foundations motion | `motion-fast` dest 1 · `motion-standard` dest 2 · `motion-slow` dest 1 · `motion-ambient` dest 1. `ease-ambient` `linear` dest 1. `prefers-reduced-motion` dest 1. No bounce, no spring dest 1. The source HTML comment marks Motion as editorial/illustrative; the qualifier at 183 covers the section (B2/B2a). Unattributed-curve omission qualifier at 194. |
| §15 Motion & Easing — two `cubic-bezier` values | 삭제 → provenance omission ledger | No computed observation stands behind them. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to `spec/omd-v0.1.md`. `ease-enter` `cubic-bezier(0.2, 0.6, 0.25, 1)` is likewise unattributed. `grep -oF cubic-bezier` → `DESIGN.md` 0; `provenance.md` 2 (the two omitted curves). B3 is held: the promotion condition at `DESIGN.md` 202 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus the partial-confirmation exclusion (E2c). Recorded unresolved restates the five kinds. |
| HTML comment OmD v0.1 Sources | 분리 → provenance | Live-inspect notes, Tier 1/2 URLs, narrative class, voice-sample attribution, persona class, and the editorial/illustrative admission for §14/§15 are ledger material: `provenance.md` Narrative + Omission ledger + Proof notes. |

## Sibling handling (`web/references/omnichat/.verification.md`)

The sibling exists — confirmed with `find web/references/omnichat -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not, or that conflict with the source: header-outline computed `background: transparent` (source `#ffffff`); viewport `1440×900`; hero-band height `704px`; plan names 進階行銷商務 / 企業旗艦方案; label 聯絡專員; `document.title` home and pricing; frequency counts; LINE green `rgb(6,199,85)`. Recorded at `provenance.md` Sibling handling. Neither side of a conflict was chosen as a new token.
- Its structural classifications were likewise not promoted (B1): frequency-scan ranks and the viewport figure. The portable Desktop row keeps source `1024–1440px` as declared width. Exact string `measures 1440px` dest 0 / provenance 0.
- Published strings that appear only in the sibling survive in provenance, not in the portable body: `Omnichat Blog 2.0 TW`; `No designs found for 'omnichat'`; `document.title` two strings.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Brand-issued zh-TW strings in the source (labels, CTAs, slogans, microcopy, titles) | 40 distinct | 0 | 0 | All 40 survive in `DESIGN.md` (hand list below). |
| Brand-issued strings in the sibling, any length | 4 sibling-only | 0 | 0 | Blog 2.0 TW label, getdesign sentence, two `document.title`s survive in provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand omnichat` after restore | 45 candidates | 0 reported | 0 | First run reported 7 lost. Two were YAML byte forms (`animated 270deg gradient #3337f6 → #379afd → #69dfb2`, `1px solid #c5c5c5`) restored as Token-set lines. Five were sibling-only (Blog 2.0 TW, s2 query, rgb featured-border, logos.ts comment, getdesign sentence) transcribed in provenance. Re-run: `clean: 1`, `lost: 0`. |

Sub-needle labels the machine check could not see were confirmed present individually in `DESIGN.md`: 專家諮詢, 免費試用, 聯絡專人, 送出資料, 獲得報價, 預約示範, 了解完整案例, 更多成功案例, 最受歡迎, 線上對話商務, OMO全渠道銷售, Social CDP, 你想從哪裡著手?, 平台, 產品, 案例, 價格, 資源, 關於 Omnichat, LINE 官方帳號, 深受全球 5,000+ 客戶信賴, 為何 Omnichat 是最好的選擇？, AI 全通路顧客體驗平台, 全渠道 AI 對話商務平台, 選擇最適合您的方案，優化客戶服務，提升銷售業績！, Omnichat 讓品牌與顧客保持緊密連結互動，以提升品牌的轉換率為使命。, Omnichat 的目標永遠只有一個，就是提高品牌的轉換率, Omnichat 相信有溫度的顧客體驗，是品牌成功的關鍵, 有溫度的顧客體驗, 賦能客服、行銷、銷售轉換, 高效客服方案, 基本行銷商務, Omni AI Message Flow：AI 行銷活動產生器, Omni AI by Omnichat, 信義房屋, à la sha, 博士助聽器, 信義房屋…締造精準受眾推播 50% 超高開封率, LINE、WhatsApp、Meta 官方合作夥伴, 發生錯誤, 專人將與您聯繫, 限時搶購, 立即購買, 轉換率.

A5 분모: latin-copy-audit 45/45 survived after restore; zh-TW hand sweep 40 extracted / 0 missing / 0 published-copy missing. Sibling-only published 4/4 in provenance.

Audience/Primary-tasks source grep (wave 31·42): every Audience group string and every Primary-task label has source count ≥ 1. No persona motivation was rewritten as a task.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand omnichat --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 38, candidates: 228 }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. `latin-copy-audit --brand omnichat` after restore: `clean: 1`, `lost: 0`. These are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so none of them is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 38 < `candidates` 228.

## Deviations recorded

- `DESIGN.md` is 8,016 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: twenty-four color roles, ten type roles with dual unitless/px and YAML/§3 forms, eight declared component records plus three §4-only writings with dual `use`/`font`/`states` forms, a ten-row surface state contract, a per-component applicability matrix, zh-TW byte-exact copy, the §11 founding-through-closing-sentence narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Omnichat has no published first-party design system (getdesign.md 404; refero not listed), so every derived-editorial close uses the toss-form "not Omnichat-authored or a separately published UI specification" (rulebook v12 B2a 전제 주석). Measure `grep -oF 'not Omnichat-authored or a separately published UI specification' DESIGN.md | wc -l` = 33. Provenance `## Derived editorial inventory` table has 33 data rows (1:1).
- The source names four Tier 1 URLs but supplies computed evidence for only two of them. The about and blog URLs are therefore carried as named sources with no computed design value attached, in Scope and in Recorded unresolved.

## Mandatory final passes

F1 B2a scan: reread the portable body. Every causal, interpretive, or judging sentence (Scope atmosphere and narrative-not-token classification, Distinctive-traits grouping, Principles/Do/Don't, color role names, spacing/shape keep-apart, elevation YAML/Level keep-both at 177, elevation philosophy at 179, motion editorial class at 183, unattributed-curve omission at 194, font-evidence rows, family fallback prohibition, type-role keep-both, typography principles, Assets reading, kind/applicability verdicts and YAML/§4 keep-both and focus≠focus-visible at 267, State-record system-level at 269, State-record editorial class at 515, State-record non-attachment at 530, layout stage/shelf and responsive-system-level readings, Content characterization and forbidden register) has an adjacent complete qualifier of the form "derived editorial implementation inference from the verified surfaces; … not Omnichat-authored or a separately published UI specification". Count 33 / inventory 33.

F2 E2 check: each log row's destination was grepped in `DESIGN.md` and/or `provenance.md` before the row was written. Dual destinations (homepage, four Tier-1 URLs, `#006aff`, favicon) name both homes. "B3 유지" is written only because `DESIGN.md` 202 contains the five evidence kinds, the per-component gate, and the partial-confirmation exclusion in full text.

Unique-phrase self-check (wave 43): 67 source-unique expressions extracted (years, proper names, quotes, causal §11 sentences, value modifiers, §15/§5 constraints); `grep -oF` dest 0 remaining after restore = 0. Additional YAML-use/path sweep restored 9 items that the 67-list did not cover (1 card-pricing `use` string whose backticks had split the hex; 8 full `tokens.typography.*` paths that had been abbreviated after `display-hero`). Restored=9.
