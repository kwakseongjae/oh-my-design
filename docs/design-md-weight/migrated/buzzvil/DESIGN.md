# Buzzvil Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Buzzvil (버즈빌) is Korea's reward-advertising and adtech platform. Catalog homepage identity is `https://www.buzzvil.com`. YAML `display_name_kr` is 버즈빌.

Treating the following URL as the named live-inspect evidence domain of this reconstruction, treating the Tech blog URLs as narrative or listed Tier-1 hosts rather than extra token captures, treating stay-attached-to-the-establishing-page as an evidence-domain reading, and treating a homepage value as not a stand-in for HoneyScreen, BuzzScreen SDK, or SlideJoy surfaces named only in the public-history narrative, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. This contract covers the flagship homepage `https://www.buzzvil.com` from the 2026-06-26 live inspect. Source token-level claims in §1–9 are sourced from playwright `getComputedStyle` on that URL. The source HTML comment attaches those claims to that inspect. Values below stay attached to the page that established them. A homepage value is not a stand-in for HoneyScreen, BuzzScreen SDK, or SlideJoy surfaces named only in the public-history narrative.

Source token note: primary = live contact-CTA coral red (`#f44336`, also the brandmark color); hero/dark sections sit on ink navy (`#0e171f`) with a dark slate card surface (`#2a3f4d`); the cool light surface (`#f2f5f7`) carries secondary buttons. Flat — `box-shadow: none` across the flagship surface. Treating that note as a register split — `#f44336` is catalog `primary_color` and the harvested 문의하기 fill plus brandmark hue rather than a fill for every control; `#0e171f` is ink-navy hero/immersive fill and strong heading ink rather than Pure Black `#000000`; `#2a3f4d` is the nested dark-slate card rather than the navy band; `#f2f5f7` is the cool light band and secondary-button fill rather than canvas `#ffffff`; and `box-shadow: none` is the flagship flat record rather than a stacked-elevation system — is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

The following homepage-character, confident, AI-forward, rather-than-a-media-buying-middleman, dual-mood, bright-airy-info-bands, immersive-persuasive-navy, never-softened, crisp-declarative-weight, reserved-almost-entirely-for-문의하기, single-saturated-brand-accent, and exact-hue-of-the-Buzzvil-brandmark readings are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. The source reads the homepage as a confident, AI-forward product company rather than a media-buying middleman. The flagship surface (`www.buzzvil.com`) alternates two moods: bright, airy white (`#ffffff`) and cool-grey (`#f2f5f7`) bands for informational content, and immersive dark navy (`#0e171f`) sections — paired with a dark slate card surface (`#2a3f4d`) — for the persuasive, atmospheric moments. Text is set in pure black (`#000000`) and a deep near-black navy ink (`#0e171f`), never softened, which the source treats as giving the marketing copy a crisp, declarative weight. The single saturated brand accent is a coral red (`#f44336`) — the exact hue of the Buzzvil brandmark — reserved almost entirely for the "문의하기" (contact) call-to-action.

The following typographic-personality, unmistakably-Korean-premium, entirely-Pretendard, wide-weight-range, headlines-run-enormous-and-heavy, hierarchy-by-size-and-weight-not-a-second-typeface, and lead-subhead-sits-quiet readings are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. The source treats the typographic personality as unmistakably Korean-premium and entirely **Pretendard**, carried at a wide weight range. Headlines run enormous and heavy — the hero "모두가 사랑하는 방식의 광고" ("Advertising that everyone loves") lands at **78px / weight 800 (ExtraBold)** — while section heads step down through 56px, 48px, and 32px at weight 700, and the lead subhead sits quiet at 24px / 400. Body and UI text drop to 16px / 400, with navigation labels at 16px / 600 SemiBold. There is exactly one font family doing every job; hierarchy comes from size and weight, not from a second typeface.

The following distinguishing-from-heavier-enterprise-adtech-peers, flatness-and-generous-rounding, depth-by-background-color-and-hairlines-never-elevation, geometry-leans-soft, tidy-4px, calm-engineered-blue-grey-temperature, and white-CTA-text-as-on-primary readings are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. What the source treats as distinguishing Buzzvil from heavier enterprise adtech peers is its flatness and its generous rounding. Live inspection found `box-shadow: none` across the hero, nav, cards, and buttons — depth is communicated by background color (white vs cool-grey `#f2f5f7` vs navy `#0e171f`) and by thin `#dce3e8` hairlines, never by elevation. Geometry leans soft: feature and stat cards use a large **32px** radius, segmented "goal" filter pills go fully round (9999px), and the smaller header buttons sit at a tidy 4px. The cool-slate neutral ladder — body slate `#3e5463`, nav muted `#5b7282`, then `#7a909e`, `#9fb1bd`, and the faint `#c1ccd6` used for inactive controls — is recorded as giving text and UI a calm, engineered, blue-grey temperature. White CTA text (`#ffffff`) reads as `on-primary` against both the red and the navy.

Treating the following public-history facts as narrative rather than interface tokens, including named-for-the-buzz, lock-screen-idea-from-subway-platform-screen-doors, founding-insight-recorded-as-concrete, everyday-surface-people-glance-at-constantly, HoneyScreen-then-BuzzScreen-SDK, SlideJoy-2016, offices-across-Korea-US-Japan-Taiwan, mission-captured-as-the-homepage-line, rewards-rather-than-interrupts, current-chapter-interaction-AI-agents, and hyper-personalized-experiences, and treating those facts as widely documented public knowledge not all quoted from a single verified Buzzvil statement this turn, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Buzzvil was founded in **2012** by **John Gwanwoo Lee (이관우, CEO)** and co-founder **Robert Seo**, who named the company for the "buzz" they intended to create. The founding insight is recorded as concrete: Lee took the idea for lock-screen advertising from the platform screen doors at subway stations — an everyday surface people glance at constantly — and asked what a smartphone's first screen could become. From that came **HoneyScreen** (a reward lock-screen app) and then **BuzzScreen**, a B2B SDK that lets any partner app run a reward-advertising lock screen without shipping a separate app. The company grew into a cross-border reward-advertising and adtech platform, acquiring the US lock-screen company **SlideJoy** in 2016 and operating offices across Korea, the US, Japan, and Taiwan. Its mission — to let advertisers and publishers engage users on the first screen in a way that rewards rather than interrupts — is captured on the homepage as "모두가 사랑하는 방식의 광고" (advertising everyone loves). The current chapter reframes the platform around "인터랙션 AI 에이전트" — interaction AI agents that deliver hyper-personalized experiences.

The following refusal, embrace, heavy-shadow-stacked-chrome, interruption-aesthetics-of-legacy-adtech, echoed-in-its-own-design-system-writing, flat-fast-Pretendard-set, single-trustworthy-coral-red, large-soft-rounding, and copy-that-backs-claims-with-numbers readings are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. What the source treats Buzzvil as refusing, visible in its design: the heavy, shadow-stacked chrome and interruption aesthetics of legacy adtech. What it treats the design as embracing — echoed in its own design-system writing ("Simplicity is key… minimalism comes from this mindset") — is a flat, fast, Pretendard-set interface; a single trustworthy coral red; large soft rounding; and copy that backs claims with numbers.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three live-surface jobs below as reconstructed user outcomes from harvested homepage strings, not as independently verified destination routes for every named control, and keeping source §13 fictional archetypes out of this list, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Independently verified in this packet is the inspected homepage URL `https://www.buzzvil.com`; named CTA and filter labels are harvested strings/controls, not independently verified destination routes.

- Start contact from the harvested header "문의하기" CTA on `https://www.buzzvil.com`.
- Open the harvested header "광고센터 바로가기" action on `https://www.buzzvil.com`.
- Explore harvested "광고 상품 둘러보기" and goal filters ("전체", "브랜드 알리기") on `https://www.buzzvil.com`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 fictional archetypes were deleted and are not Audience and are not primary tasks; independently verified work is the three primary tasks. Restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

### Distinctive traits

Treating `#f44336` as catalog `primary_color` plus 문의하기 fill plus brandmark hue rather than a fill that also owns `#000000` ad-center or `#f2f5f7` secondary, treating `#0e171f` as navy band and heading ink rather than Pure Black, treating 4px / 8px / 16px / 32px / 9999px as local harvested geometry rather than one radius, treating Pretendard ExtraBold 800 through body 400 as size/weight hierarchy rather than a second family, treating near-flat depth as `box-shadow: none` plus color bands plus `#dce3e8` hairlines rather than a stacked-elevation system, and treating the cool-slate ladder as unmerged inks, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

- Catalog `primary_color` / Buzzvil coral red `#f44336` as the harvested 문의하기 fill; same hue as the brandmark
- Dual-mood layout: white `#ffffff` / cool-grey `#f2f5f7` info bands vs immersive navy `#0e171f` / slate `#2a3f4d`
- Pure black `#000000` and near-black navy `#0e171f` text; on-primary white `#ffffff` on red, navy, black, and slate
- Pretendard for every role — display ExtraBold 800 (78px) down to 16px body 400; nav 16px / 600
- Flat depth: `box-shadow: none`; separation by background color and `#dce3e8` hairlines
- Soft geometry: 32px feature/stat cards, 9999px filter pills, 8px hero secondary/ghost, 4px header buttons, 16px inner white cards / media tiles
- Cool blue-grey ladder `#3e5463` → `#5b7282` → `#7a909e` → `#9fb1bd` → `#c1ccd6`

### Principles

These five items, including each stem and each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. The source HTML comment marks interpretive claims such as “one action, one color” and “flat and fast as a rejection of legacy adtech chrome” as editorial readings connecting Buzzvil's observed design and stated design-team values to its positioning, not directly sourced Buzzvil statements. The quoted line “Simplicity is key. By keeping things simple, more people will understand what you want to express.” is verbatim from the Buzzvil design-system blog; packaging it as principle 3 with a UI implication is the editorial layer.

1. **Advertising people welcome.** The brand exists to make ads rewarding, not interruptive. *UI implication:* lead with user benefit and concrete value (reward, relevance), never dark-pattern urgency.
2. **One action, one color.** Coral red (`#f44336`) means "do this / talk to us." *UI implication:* reserve the saturated red for the primary CTA so the next step is unambiguous.
3. **Simplicity by design.** Buzzvil's own design team states "simplicity is key… minimalism comes from this mindset." *UI implication:* fewer typefaces, flat depth, color-band layout; remove before adding.
4. **Prove it with numbers.** Performance claims are shown as metrics (CTR, CVR, retention). *UI implication:* pair every benefit headline with a measurable stat in a card.
5. **Soft, modern, flat.** Large rounding and shadow-free surfaces over heavy elevation. *UI implication:* 32px cards, pills, color bands and hairlines instead of drop shadows.

Treating the following as a capture-bound application of source §7 Do’s and live inspect, including Pretendard-for-everything, coral-red-as-single-action, black-and-navy-for-text, crisp-and-declarative, white-grey-navy-full-width-bands, flat-color-bands-and-hairlines-never-shadows, 32px-cards-and-9999px-pills, cool-slate-ladder-for-text-hierarchy, and dark-slate-card-inside-navy, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

- Use Pretendard for everything — ExtraBold (800) for the hero, 700 for section heads, 600 for nav, 400 for body.
- Reserve coral red (`#f44336`) for the primary contact CTA — keep it the single "action" color.
- Use pure black (`#000000`) and near-black navy (`#0e171f`) for text — crisp and declarative.
- Alternate white, cool-grey (`#f2f5f7`), and dark navy (`#0e171f`) full-width bands for rhythm.
- Keep the system flat — separate with color bands and `#dce3e8` hairlines, never shadows.
- Use large 32px rounding on feature/stat cards and full-round (9999px) filter pills.
- Use the cool blue-grey neutral ladder (`#3e5463` → `#5b7282` → `#7a909e` → `#9fb1bd` → `#c1ccd6`) for text hierarchy.
- Use a dark slate card (`#2a3f4d`) for grouping inside navy sections.

### Avoid

The following items copy source §7 Don’ts. Their extra causal readings — coral-red-dilutes-the-single-action-signal, flat-shadow-free-system, Pretendard-owns-every-weight-and-role, display-is-ExtraBold-800, coral-red-is-the-only-saturated-accent, large-32px-rounding-is-the-brand-geometry, and faint-slate-c1ccd6-is-the-quiet-state — are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification.

- Do not spread coral red across many elements — it dilutes the single-action signal.
- Do not add drop shadows for elevation — Buzzvil is a flat, shadow-free system.
- Do not introduce a second display typeface — Pretendard owns every weight and role.
- Do not soften headlines to a light weight — display is ExtraBold (800).
- Do not mix in a second saturated accent hue — coral red is the only one.
- Do not use sharp corners on big cards — large 32px rounding is the brand geometry.
- Do not use grey for inactive-only controls when the faint slate (`#c1ccd6`) is the system's quiet state. Treating this last Don’t, including faint-slate-c1ccd6-is-the-quiet-state, as a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including coral-red-not-ad-center-black, ink-navy-not-pure-black, pure-black-jobs-share-hex-not-a-second-navy, canvas-white-not-surface-grey, surface-dark-not-navy-band, body-slate-not-muted, muted-not-muted-alt, faint-not-faint-alt, faint-alt-not-hairline, hairline-as-primary-separation-in-a-shadow-free-system, on-primary-white-shared-hex-not-a-second-canvas, and brandmark-hue-sharing-hex-with-문의하기-not-one-semantic-for-every-control, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. Catalog `primary_color` `#f44336` is the live 문의하기 fill (rgb(244,67,54)) and YAML `primary`; the source also names that hue as the brandmark color — those jobs share a hex and are not one semantic that also owns `#000000` ad-center fill. Ink Navy `#0e171f` (rgb(14,23,31)) is dark hero/immersive fill and strong heading text, YAML `ink`, not Pure Black `#000000`. Pure Black `#000000` (rgb(0,0,0)) is body/headline text, the 광고센터 CTA fill, and the language-toggle outline — those jobs share a hex and are not a second navy. Pure White `#ffffff` is page canvas, white feature cards, and `on-primary` text on red/navy/black; those jobs share a hex and are not a second canvas token. Surface Grey `#f2f5f7` (rgb(242,245,247)) is the cool light band and secondary-button fill, YAML `surface`, not canvas. Surface Dark `#2a3f4d` is nested slate inside navy, YAML `surface-dark`, not the navy band. Hairline `#dce3e8` is the thin divider in this shadow-free system. Body Slate `#3e5463` (rgb(62,84,99)) is not Muted Slate `#5b7282` (rgb(91,114,130)). Muted Alt `#7a909e` is not Faint `#9fb1bd`. Faint Alt `#c1ccd6` is inactive control text/border, not Hairline.

- **Buzzvil Coral Red / Primary** (`#f44336`): catalog `primary_color`. Contact-CTA background and the brandmark hue. YAML `primary`.
- **Ink Navy** (`#0e171f`): Dark hero/immersive section backgrounds and strong heading text. YAML `ink`.
- **Pure Black** (`#000000`): Body and headline text, the black header "광고센터" CTA background, and the language-toggle outline. YAML `ink-pure`.
- **Pure White** (`#ffffff`): Page background, white feature cards, and `on-primary` text on red/navy/black. YAML `canvas` / `on-primary`.
- **Surface Grey** (`#f2f5f7`): Cool light-grey surface for alternating content bands and the secondary explore button. YAML `surface`.
- **Surface Dark** (`#2a3f4d`): Dark slate card surface used inside the navy sections. YAML `surface-dark`.
- **Hairline** (`#dce3e8`): Thin borders and dividers — the primary separation device in this shadow-free system. YAML `hairline`.
- **Body Slate** (`#3e5463`): Secondary body text and the secondary-button label color. YAML `body`.
- **Muted Slate** (`#5b7282`): Top-nav link color, tertiary labels. YAML `muted`.
- **Muted Alt** (`#7a909e`): Alternate muted slate for captions and metadata. YAML `muted-alt`.
- **Faint Blue-Grey** (`#9fb1bd`): Low-emphasis labels and quiet supporting text. YAML `faint`.
- **Faint Alt** (`#c1ccd6`): Inactive control text and borders (e.g. unselected filter pills). YAML `faint-alt`.

### Spacing

YAML `spacing`: xs 4, sm 8, md 12, base 16, lg 24, xl 32, xxl 40, section 80. Body layout states a base unit of 4px and a scale 4px, 8px, 12px, 16px, 24px, 32px, 40px, 80px.

Treating that YAML scale and the body 4px-base as recorded spacing in this packet rather than a universal spacing token for every unlisted control, treating unitless YAML steps as not converted into a required px suffix, treating component padding as staying with the components, and treating card interiors 80px top / 40px side (measured 80px 40px 40px) as the Feature / Stat Card field rather than a second spacing token, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Header CTAs pad `12px 16px`. Hero secondary/ghost pad `16px 32px`. Filter pills pad `16px`. Feature/stat cards pad `80px 40px 40px`.

### Shape

YAML `rounded`: sm 4, md 8, lg 16, xl 32, full 9999 (`9999px`).

The following local-geometry reading, including 4px-not-8px, 8px-not-16px, 16px-not-32px, 32px-not-9999px, workhorse-rounding, and the Small/Medium/Large/XL/Full role labels, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. 4px / 8px / 16px / 32px / `9999px` remain local harvested geometry, not a universal radius for every unlisted control. YAML `rounded.sm` 4 is header buttons (contact, ad-center, language toggle). YAML `rounded.md` 8 is hero secondary/ghost buttons. YAML `rounded.lg` 16 is inner white cards / media tiles; no harvested inner-card component records that radius as a YAML `tokens.components` field. YAML `rounded.xl` 32 is feature and stat cards — the workhorse rounding. YAML `rounded.full` 9999 / `9999px` is goal filter pills and round chips. Do not merge 4px header buttons into 8px hero buttons, 16px inner tiles into 32px stat cards, or 32px into `9999px`.

### Elevation

Treating the table Use assignments (Page background, inline text, most surfaces; Section separation without elevation; Dividers and subtle card outlines; Grouping within immersive bands) as source-stated uses in this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Color band (Level 1) | Background shift (white / `#f2f5f7` / `#0e171f`) | Section separation without elevation |
| Hairline (Level 2) | `1px solid #dce3e8` border | Dividers and subtle card outlines |
| Tint card (Level 3) | Tinted surface inside dark sections (slate `#2a3f4d`) | Grouping within immersive bands |

YAML `tokens.shadow.none` is `none`. Live inspection found `box-shadow: none` across the hero, nav, headings, cards, and buttons.

The following near-shadowless and shadow-philosophy readings, including depth-from-background-color-and-hairlines-rather-than-elevation and emphasis-from-coral-red-or-dark-slate-never-elevation, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. The source reads Buzzvil as a near-shadowless system. Depth and grouping are communicated entirely through background color (white, cool-grey `#f2f5f7`, and dark navy `#0e171f`) and thin `#dce3e8` hairlines. When emphasis is needed, the system reaches for the coral red (`#f44336`) or a dark slate card (`#2a3f4d`), never elevation.

### Motion

Treating the duration table, easing names, easing-table Use labels Arriving / Dismissals / Two-way transitions, signature pill-press / card-fade-in-from-below, and reduced-motion line as source-stated rather than computed CSS, treating omitted `ease-exit` as matching the legacy spec-template example, treating `ease-enter` / `ease-exit` / `ease-standard` as unattributed cubic-bezier rather than computed CSS, and treating the "functional and quiet" / "signals reliability, not gimmickry" readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, pill press, focus |
| `motion-standard` | 200ms | Card/section reveal, filter switch |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, pills, panels |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including functional-and-quiet, matching-the-flat-fast-aesthetic, filter-pills-subtle-scale-opacity, stat-and-feature-cards-fade-in-from-below-at-motion-standard-ease-enter, no-bounce-or-spring reliability-not-gimmickry, and reduced-motion-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. Motion is described as functional and quiet, matching the flat, fast aesthetic. Filter pills respond to press with a subtle scale/opacity shift; stat and feature cards fade-in from below at `motion-standard / ease-enter`. No bounce or spring — an adtech platform signals reliability, not gimmickry. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional.

Treating unattributed cubic-bezier curves as omitted rather than promoted, while keeping source-stated names and uses, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Exact cubic-bezier curves are unattributed — `ease-exit` matches the legacy spec template — and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists. Source `motion-fast` names “Hover, pill press, focus” as duration use; that duration row is not `focus-visible` treatment evidence.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Pretendard-as-the-single-family-for-every-role and sans-serif-as-listed-fallback-not-the-brand-face, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Hero H1 "모두가 사랑하는 방식의 광고" in Pretendard 78px / weight 800 over dark navy. Hero subhead 24px / 400. Nav 16px / 600. Contact CTA 16px / 400. Secondary explore and filter-pill labels 20px / 400. YAML roles in the table below. |
| Declared fallback | `sans-serif` — fallback listed with Pretendard, not a substitute for Pretendard. |

The following font-use boundary, including Pretendard as the single family for headlines, navigation, buttons, and body, ExtraBold 800 at the hero, 700 for section heads, 600 for nav, 400 for body, and a fallback as not the brand face, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Do not present `sans-serif` as Pretendard. Do not introduce a second display typeface.

### Family

- **Sans (all roles):** `Pretendard` — YAML `family.sans`. The single family for headlines, navigation, buttons, and body, with `sans-serif` fallback.

### Type roles

Verified YAML line-height values are the unitless ratios `1.19`, `1.31`, and `1.50`. The following ratio-versus-size-local reading, including 1.19-(93px)-as-a-local-companion-not-a-YAML-replacement, 1.50-not-the-prose-1.5, and YAML-roles-without-lineHeight-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Body also writes `1.19 (93px)` for Display Hero; that px companion stays local to the table notes and is not a replacement for the YAML ratio. Body principle prose also writes "1.5 line-height"; that writing stays unmerged from YAML `1.50`. YAML `display` / `section` / `subsection` / `title` / `nav` / `button-lg` / `caption` have no `lineHeight` field; none is invented.

The following one-family-full-weight-range, headlines-run-large-and-heavy, SemiBold-for-navigation, hangul-first-body, weight-jump-is-the-primary-hierarchy-signal, declarative-brand-forward-scale, compresses-on-smaller-viewports-while-keeping-the-weight, notch-heavier-so-chrome-reads-as-interactive, and dense-Korean-marketing-copy type-rule readings are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. Pretendard does every job; the jump from ExtraBold 800 headlines to 400 body is the system's primary hierarchy signal. The hero is 78px / 800 — a declarative, brand-forward scale that compresses on smaller viewports while keeping the weight. Nav links sit at 16px / 600, a notch heavier than 400 body so the chrome reads as interactive. Body sits at 16px / 400 with YAML 1.50 line-height; source principle prose also writes 1.5 line-height for dense, legible Korean marketing copy.

| Role | Font | Size | Weight | Line height (YAML) | rem | Use (YAML) |
|---|---|---:|---:|---:|---:|---|
| Display Hero | Pretendard | 78px | 800 | 1.19 | 4.88rem | Hero headline, Pretendard ExtraBold |
| Display | Pretendard | 56px | 700 | — | 3.50rem | Large section headline |
| Section | Pretendard | 48px | 700 | — | 3.00rem | Section title |
| Sub-section | Pretendard | 32px | 700 | — | 2.00rem | Card / feature heading |
| Title | Pretendard | 24px | 700 | — | 1.50rem | Sub-heading |
| Lead | Pretendard | 24px | 400 | 1.31 | 1.50rem | Hero subhead / lead paragraph |
| Nav Link | Pretendard | 16px | 600 | — | 1.00rem | Top nav link, Pretendard SemiBold |
| Body | Pretendard | 16px | 400 | 1.50 | 1.00rem | Standard reading text |
| Button Large | Pretendard | 20px | 400 | — | 1.25rem | Large CTA / filter-pill label |
| Caption | Pretendard | 12px | 500 | — | 0.75rem | Small labels, metadata |

Display Hero also writes body companion `1.19 (93px)`. Treating Display Hero on the dark navy hero as white text in the source §9 local recipe, and not merging that ink job into a YAML type-role color, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

### Assets

Treating catalog logo metadata as a favicon file at the homepage host, not a Google favicon lookup, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

- Marketing-site favicon: `https://www.buzzvil.com/favicon.png`

Treating product screenshots and illustrations as carrying no shadow at any size, consistent with the flat system, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Source §8 records that image behavior.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Treating graph-not-adopted preservation of that contract, and treating the table below as that philosophy-layer contract rather than computed per-component CSS, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. The source state contract is preserved here while the catalog graph is not adopted. Hex values and geometry in the harvested components remain source-stated. The source HTML comment places sections 10–15 in the philosophy layer.

Treating these rows as empty/loading/error/success/skeleton/disabled treatments for a campaign/results/inquiry surface, and not copying them onto harvested marketing CTAs as computed paints, including table characterizations “No clutter”, “Calm and honest”, “Flat pulse, no shadow shimmer”, “consistent with the shadow-free system”, “previous values stay visible”, “No generic 오류가 발생했습니다 alone”, “not just 필수”, “No celebratory emoji”, “preserve brand read”, coral-red-CTA-to-start, muted-slate-path-back, and red-actions-fade-rather-than-turning-grey, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no campaign / results)** | White canvas. Single near-black navy (`#0e171f`) line explaining there's nothing yet, with one coral-red (`#f44336`) CTA to start. No clutter. |
| **Empty (saved list, none yet)** | Muted slate (`#5b7282`) single line: nothing saved yet, plus a path back. Calm and honest. |
| **Loading (results fetch)** | Skeleton blocks on `#f2f5f7` tinted surface at final card dimensions, 32px radius. Flat pulse, no shadow shimmer — consistent with the shadow-free system. |
| **Loading (in-place refresh)** | Subtle inline progress; previous values stay visible. |
| **Error (request failed)** | Inline message in `#0e171f` with a plain-language explanation and a retry. No generic "오류가 발생했습니다" alone. |
| **Error (form validation)** | Field-level message below the input describing what's valid, not just "필수". |
| **Success (inquiry submitted)** | Brief inline confirmation in a calm tone; next-step detail linked immediately below. No celebratory emoji. |
| **Skeleton** | `#f2f5f7` blocks at final dimensions, 32px radius, flat pulse. |
| **Disabled** | Faint blue-grey (`#c1ccd6`) text on a reduced-opacity surface; red actions fade rather than turn grey to preserve brand read. |

Characterizations in that table such as “No clutter”, “Calm and honest”, “Flat pulse, no shadow shimmer”, “consistent with the shadow-free system”, “previous values stay visible”, “No generic 오류가 발생했습니다 alone”, “not just 필수”, “No celebratory emoji”, “preserve brand read”, coral-red-CTA-to-start, muted-slate-path-back, and red-actions-fade-rather-than-turning-grey are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records no generic Focus capture with a colour. The `focus-visible` rows do not carry a colour. A later generic Focus observation would not be `focus-visible` evidence. Source `motion-fast` names “Hover, pill press, focus” as duration use; that duration row is not `focus-visible` treatment evidence. Source Top Nav “selected/hover item” is a captured variant on that control, not `focus-visible` treatment.

Feature / Stat Card and Dark Slate Card have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Contact CTA (Primary)

- Role: header "문의하기" contact call-to-action — the saturated coral-red action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#f44336`
- Text: `#ffffff`
- Radius: `4px`
- Padding: `12px 16px`
- Height: `44px`
- Font: `16px / 400` / Pretendard
- Use: YAML `Header 문의하기 contact CTA — coral-red primary action`
- Observed: default only
- YAML `tokens.components.button-contact`
- Field note: The following unmerged-field reading, including harvested-height-not-touch-target on `44px` versus §8 comfortably-tappable, 4px-not-8px, fill-jobs-not-one-semantic on `#f44336`, `#ffffff` label-not-a-second-canvas-token, and right-aligned-in-sticky-white-75px-nav-not-global-alignment, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. `#f44336` is this control’s fill and catalog `primary_color`. It is also named as the brandmark hue; those jobs share a hex and are not this button’s only meaning. `#ffffff` is this control’s label and on-primary; it is also canvas / card fill. Height `44px` is this action’s harvested height, not a touch-target policy. Radius `4px` is YAML `rounded.sm` / this control; it is not hero secondary 8px and not card 32px.
- Local recipe (source §9 sticky white top nav): sticky white top nav (75px). Pretendard 16px / 600 links in `#5b7282`, active `#0e171f`. Right-aligned: black `#000000` "광고센터 바로가기" CTA and coral-red `#f44336` "문의하기" CTA, white text, 4px radius, 12px 16px padding, 44px height. Treating that as one local relationship with Header / Navigation band, Top Nav Link, and Ad-Center CTA, not as global alignment/radius tokens, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header 문의하기 contact CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says red actions fade rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the header contact CTA and records §14 Success (inquiry submitted) as a philosophy-layer row; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Ad-Center CTA (Black)

- Role: header "광고센터 바로가기" — black high-contrast action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#000000`
- Text: `#ffffff`
- Radius: `4px`
- Padding: `12px 16px`
- Height: `44px`
- Font: `16px / 400` / Pretendard
- Use: YAML `Header 광고센터 바로가기 — black high-contrast action`
- Observed: default only
- YAML `tokens.components.button-adcenter`
- Field note: The following unmerged-field reading, including `#000000` fill-not-ink-navy, `#ffffff` label-not-a-second-canvas-token, 4px-not-8px, height-44px-not-hero-55px, and right-aligned-with-문의하기-in-the-75px-nav, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. `#000000` is this control’s fill and Pure Black; it is also body text and the language-toggle outline. Radius 4px is this control and YAML `rounded.sm`. Height `44px` is this action, not Secondary Explore 55px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header 광고센터 바로가기 action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A header action can be unavailable; visual treatment omitted |
| loading | not-applicable | The control selects a destination (바로가기); the control itself does not enter a loading state |
| error | not-applicable | Destination versus resting, not a request or validation failure on the control |
| success | not-applicable | Destination, not action-outcome confirmation |

### Language Toggle (Outline)

- Role: ENG / KOR language switch
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#000000`
- Border: `1px solid #000000`
- Radius: `4px`
- Padding: `12px 16px`
- Height: `44px`
- Font: `16px / 400` / Pretendard
- Use: YAML `ENG / KOR language toggle`
- Observed: default only
- YAML `tokens.components.button-outline`
- Field note: The following unmerged-field reading, including transparent-not-canvas, YAML-has-no-bg, `#000000` outline-not-ad-center-fill, body-height-44px-not-a-YAML-height-field, and 4px-not-8px, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. YAML has no `bg` and no `height`; body §4 writes Background transparent and Height 44px. `#000000` is this control’s text and border, not the ad-center fill job.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the ENG / KOR language toggle |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A language toggle can be unavailable; visual treatment omitted |
| loading | not-applicable | The control switches locale; the control itself does not enter a loading state |
| error | not-applicable | Locale versus resting, not a request or validation failure on the control |
| success | not-applicable | Locale switch, not action-outcome confirmation |

### Secondary Explore (Light)

- Role: "광고 상품 둘러보기" secondary explore button
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#f2f5f7`
- Text: `#3e5463`
- Border: `1px solid #f2f5f7`
- Radius: `8px`
- Padding: `16px 32px`
- Height: `55px`
- Font: `20px / 400` / Pretendard
- Use: YAML `광고 상품 둘러보기 secondary explore`
- Observed: default only
- YAML `tokens.components.button-secondary`
- Field note: The following unmerged-field reading, including `#f2f5f7` fill-not-canvas, `#3e5463` label-not-ink-navy, YAML-has-no-border versus body `1px solid #f2f5f7`, 8px-not-4px, 55px-not-44px, and §9-dark-hero-pair-with-ghost, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. YAML has no `border`; body §4 writes `1px solid #f2f5f7`. `#f2f5f7` is this control’s fill and Surface Grey. `#3e5463` is this control’s text and Body Slate. Radius 8px is YAML `rounded.md` / this control, not header 4px. Height `55px` is this action, not header 44px.
- Local recipe (source §9 dark hero): dark hero `#0e171f` background. Headline 78px Pretendard weight 800, white text. Subhead 24px / 400 in `#f2f5f7`. Two buttons at 8px radius, 16px 32px padding, 55px height: light `#f2f5f7` fill with `#3e5463` text, and a ghost with 1px `#f2f5f7` border and `#f2f5f7` text. Treating that as one local relationship with Ghost on Dark and the dark-hero parent, not as global type-role colors, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the 광고 상품 둘러보기 secondary explore |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary explore action can be unavailable; visual treatment omitted |
| loading | not-applicable | The control selects an explore destination (둘러보기); the control itself does not enter a loading state |
| error | not-applicable | Destination versus resting, not a request or validation failure on the control |
| success | not-applicable | Destination, not action-outcome confirmation |

### Ghost on Dark

- Role: "광고 문의하기" ghost CTA over the dark navy hero
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#f2f5f7`
- Border: `1px solid #f2f5f7`
- Radius: `8px`
- Padding: `16px 32px`
- Height: `55px`
- Font: `20px / 400` / Pretendard
- Use: YAML `광고 문의하기 ghost CTA over dark navy hero`
- Observed: default only
- YAML `tokens.components.button-ghost-dark`
- Field note: The following unmerged-field reading, including transparent-not-navy-fill, YAML-has-no-bg, `#f2f5f7` text-and-border-not-secondary-fill, 8px-not-4px, 55px-not-44px, and §9-dark-hero-pair-with-secondary, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. YAML has no `bg`; body §4 writes transparent. `#f2f5f7` is this control’s text and border, not Secondary Explore’s fill job as a second component.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the 광고 문의하기 ghost CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A ghost contact action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a ghost contact CTA over the dark navy hero and records §14 Success (inquiry submitted) as a philosophy-layer row; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Feature / Stat Card

- Role: large rounded feature/stat card, flat (no shadow)
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Text: `#0e171f`
- Radius: `32px`
- Padding: `80px 40px 40px`
- Shadow: none
- Use: YAML `Large rounded feature / stat card, flat (no shadow)`; body "39% 평균 클릭률", "x4 전환율", "82% 리텐션"
- Observed: default only
- YAML `tokens.components.card-feature`
- Field note: The following unmerged-field reading, including `#ffffff` fill-not-surface-grey, `#0e171f` fg-not-navy-band-as-this-card’s-background, 32px-not-16px, 80px-40px-40px-not-a-spacing-token, no-shadow-not-elevation, and §9-only number/label local tuple, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. `#ffffff` is this card’s fill and Canvas. `#0e171f` is YAML `fg` / Ink Navy as this card’s text, not this card’s fill. Radius 32px is YAML `rounded.xl` / this control, not inner-tile 16px. Padding `80px 40px 40px` is this card’s field. Source §9-only mixed anatomy is big number in `#0e171f` with label in `#3e5463` on the same card, three side by side in a band; that local tuple stays on this card and is not YAML Body as a page-wide card token.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Dark Slate Card

- Role: dark feature card nested in the navy hero/immersive sections
- Type: card
- Anatomy: surface
- Background: `#2a3f4d`
- Text: `#ffffff`
- Radius: `32px`
- Shadow: none
- Use: YAML `Dark slate feature card on the navy hero section`
- Observed: default only
- YAML `tokens.components.card-dark`
- Field note: The following unmerged-field reading, including `#2a3f4d` fill-not-navy-band, `#ffffff` text-not-a-second-canvas-token, 32px-not-4px, YAML-has-no-padding, and nested-inside-navy-not-a-page-canvas, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. `#2a3f4d` is this card’s fill and Surface Dark, not `#0e171f`. YAML has no `padding`; none is invented. Radius 32px is this control and YAML `rounded.xl`.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Header / Navigation band

- Role: header/navigation band (parent surface)
- Anatomy: surface
- Background: `#ffffff`
- Height: 75px header row
- Use: Sticky top nav parent surface with right-aligned black "광고센터 바로가기" + red "문의하기" CTAs
- Observed: default only
- Field note: The following parent-surface reading, including header-fill-not-nav-item-background, 75px-not-a-nav-link-height, sticky-white-not-navy-hero, and right-aligned-CTAs-as-one-local-relationship, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. `#ffffff` is this band’s fill and Canvas. Height 75px is this parent row, not Top Nav Link. YAML does not record a nav-band component; this header fill is the parent surface, not a Top Nav Link Background.
- Local recipe (source §9 sticky white top nav): sticky white top nav (75px). Pretendard 16px / 600 links in `#5b7282`, active `#0e171f`. Right-aligned: black `#000000` "광고센터 바로가기" CTA and coral-red `#f44336` "문의하기" CTA, white text, 4px radius, 12px 16px padding, 44px height. Treating that as one local relationship of this parent plus Top Nav Link plus the two header CTAs, not as global alignment/radius tokens, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

### Top Nav Link

- Role: top navigation item
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#5b7282`
- Font: `16px / 600` / Pretendard
- Active: `#0e171f` text on the selected/hover item
- Use: YAML `Top nav item (Products / Technologies / Company)`; body also writes Products / Technologies / Resources / Company / Career
- Observed: static default and static selected/hover appearances. Treating that selected/hover appearance as a captured variant rather than an observed click transition, and treating YAML Products / Technologies / Company as unmerged from body Resources / Career, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.
- YAML `tokens.components.nav-link`
- Field note: The following unmerged-field and captured-variant readings, including `#5b7282` item-text-not-header-fill, `#0e171f` active-not-navy-band-fill, 16px-600-not-Button-400, item-not-75px-parent, captured-variant-not-click-transition, and sticky-white-nav-right-aligned-CTAs-as-one-local-relationship, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. `#5b7282` is this item’s text and Muted Slate, not the parent fill. Weight 600 is this item and Nav Link; Contact CTA is 400. Active `#0e171f` is this component’s active field, not the navy hero fill. This item has no Background field and no Height field; the header band fill and 75px stay on the parent surface.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named as the top navigation item |
| hover | applicable | Pointer-web nav item; source writes selected/hover as one captured appearance `#0e171f`; that appearance is not copied here as a separate hover paint beyond the captured variant |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A top nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Nav meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | Nav meaning is destination, not action-outcome confirmation |

Additional observed named state: static selected/hover appearance (`#0e171f` text). Treating that appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

### Goal Filter Pill

- Role: goal segmented filter
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: transparent
- Text: `#c1ccd6`
- Border: `1px solid #c1ccd6`
- Radius: `9999px`
- Padding: `16px`
- Height: `55px`
- Font: `20px / 400` / Pretendard
- Active: `#000000` text on `#ffffff` fill, weight 700
- Use: YAML `Goal filter segmented pills (전체 / 브랜드 알리기)`
- Observed: static default and static active appearances. Treating that active appearance as a captured variant rather than an observed click transition, and treating body weight 700 on active as body-stated rather than a YAML `font` field, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.
- YAML `tokens.components.filter-chip`
- Field note: The following unmerged-field reading, including transparent-not-canvas, `#c1ccd6` inactive-not-hairline, 9999px-not-32px, 55px-not-44px, active-fill-not-문의하기-red, and body-weight-700-not-YAML-font-400, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. `#c1ccd6` is this pill’s inactive text and border and Faint Alt. Radius `9999px` is YAML `rounded.full` / this control, not card 32px. Active `#ffffff` fill with `#000000` text is this component’s active field, not Contact CTA coral red. YAML `font` is `20px / 400`; body writes active weight 700.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the goal filter pill |
| hover | applicable | Pointer-web filter; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A filter pill can be unavailable; visual treatment omitted |
| loading | not-applicable | A filter pill selects a goal segment; the pill itself does not enter a loading state |
| error | not-applicable | Filter meaning is selected versus resting, not a request or validation failure on the pill |
| success | not-applicable | Filter meaning is selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`#000000` text on `#ffffff` fill, weight 700). Treating that appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 24, xl 32, xxl 40, section 80. Body layout repeats a 4px base and a scale 4px, 8px, 12px, 16px, 24px, 32px, 40px, 80px. Header CTAs pad `12px 16px`; hero secondary/ghost pad `16px 32px`; filter pills pad `16px`; feature/stat cards pad `80px 40px 40px`.

The “airy over dense”, “color-band segmentation”, and “round rhythm” readings in this section, including despite-being-a-data-performance-product, generous-vertical-rhythm-and-large-card-padding, sections-separate-by-background-color-not-borders-or-shadows, and repeated-32px-cards-and-9999px-pills-as-a-soft-horizontal-cadence, are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification.

Recorded layout:

- Centered single-column hero anchored by the 78px Pretendard ExtraBold headline over a dark navy (`#0e171f`) backdrop. Treating the following as a source §9-only mixed pair on that dark `#0e171f` band, not as extra YAML component fields, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification. Headline 78px Pretendard weight 800, white text; subhead 24px / 400 in `#f2f5f7`; two buttons at 8px radius, 16px 32px padding, 55px height: light `#f2f5f7` fill with `#3e5463` text, and a ghost with 1px `#f2f5f7` border and `#f2f5f7` text.
- Goal-selector row of fully-round filter pills beneath the hero
- Content alternates full-width bands: white (`#ffffff`), cool-grey (`#f2f5f7`), and immersive navy (`#0e171f`)
- Stat/feature cards group at a 32px radius; nested dark cards use slate (`#2a3f4d`)
- Airy over dense: despite being a data/performance product, the marketing surface is generous with vertical rhythm and large card padding
- Color-band segmentation: sections separate by background color (white vs `#f2f5f7` vs navy), not by borders or shadows
- Round rhythm: the repeated 32px card radius and 9999px pills set a consistently soft horizontal cadence

Source breakpoint table. Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 78px / 75px / 55px / 44px figures as surface measurements in this packet rather than universal layout tokens, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating collapsing / image-behavior rows as source-stated rather than a cross-viewport specification, treating collapsing extras hero-headline-compresses-from-78px-weight-800-maintained / filter-pill-row-wraps-scrolls / feature-stat-cards-multi-column-to-stacked / white-grey-navy-bands-maintain-full-width, treating image-behavior extras product-screenshots-and-illustrations-carry-no-shadow-at-any-size / cards-keep-32px-feature-16px-inner-across-breakpoints rather than a new elevation rule, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero compresses from 78px, filter pills wrap/scroll |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024-1440px | Full layout, centered hero, multi-column card bands |

Treating the following collapsing, image-behavior, and touch-target rows as the source-stated records named in the limiter above the table, including comfortably-tappable and unmistakable-targets as purpose readings of the harvested heights rather than a complete target-size specification, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

Collapsing strategy recorded in the source: Hero 78px ExtraBold headline scales down on mobile, weight 800 maintained. Filter-pill row: horizontal wrap/scroll on narrow viewports. Feature/stat cards: multi-column → stacked single column, 32px radius retained. White / grey / navy bands maintain full-width treatment.

Image behavior: Product screenshots and illustrations carry no shadow at any size, consistent with the flat system. Cards keep their 32px (feature) / 16px (inner) radius across breakpoints.

Touch-target record: Header CTAs at 44px height with 12px 16px padding — comfortably tappable. Hero secondary/ghost buttons at 55px height, 16px 32px padding. Filter pills at 55px height, fully round for unmistakable targets.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-26)

The live strings below are source-stated. Treating the parenthetical characterizations (mission-framed / positioning) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

- "모두가 사랑하는 방식의 광고" — homepage hero headline (mission-framed). *(verified live 2026-06-26)*
- "인터랙션 AI 에이전트를 기반으로, 고객에게 필요한 초개인화 된 경험을" — hero subhead (positioning). *(verified live 2026-06-26)*
- Control and section strings from the harvested homepage: "문의하기"; "광고센터 바로가기"; "광고 상품 둘러보기"; "광고 문의하기"; "전체"; "브랜드 알리기"; "39% 평균 클릭률"; "x4 전환율"; "82% 리텐션"; document.title "버즈빌 | 인터랙션 AI agent로 시작하는 모두가 사랑하는 방식의 광고"

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

### First-party design-system writing

"Simplicity is key. By keeping things simple, more people will understand what you want to express." — Buzzvil design-system blog. *(verified live 2026-06-26)* Treating that line as a brand-authored quote from `https://tech.buzzvil.com/blog/design-system-at-buzzvil`, and treating “peer-to-peer engineering voice, reflective and principled” as citation-character rather than a complete product-microcopy guide, is a derived editorial implementation inference from the verified surfaces; it is not Buzzvil-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — including confident-benefit-first-and-friendly-technical, frames-advertising-not-as-interruption-but-as-something-users-genuinely-welcome, optimistic-human-centered-and-quietly-bold, performance-through-concrete-numbers-rather-than-hype-adjectives, product-story-leans-into-interaction-AI-agents, Hero-Optimistic / Product-Benefit-first / CTAs-Direct-and-low-pressure / Goal-selector-Plain-outcome-framed / Tech-blog-Peer-to-peer tone-table labels, and forbidden-register interruption-framed-ad-language / fear-based-or-dark-pattern-urgency / hollow-superlatives-without-a-metric / jargon-left-unexplained-to-non-marketers — are a derived editorial implementation inference from the verified surfaces; they are not Buzzvil-authored or a separately published UI specification. They are not the Observed strings above. The blog quote sits in First-party design-system writing, not in this derived voice as an Observed homepage string.

The source describes Buzzvil's voice as **confident, benefit-first, and friendly-technical** — an adtech company that frames advertising not as interruption but as something users genuinely welcome. The hero line "모두가 사랑하는 방식의 광고" sets the register: optimistic, human-centered, and quietly bold. Performance is communicated through concrete numbers (CTR, CVR, retention) rather than hype adjectives, and the product story leans into "인터랙션 AI 에이전트" (interaction AI agents) as the next chapter.

| Context | Tone |
|---|---|
| Hero headlines | Optimistic, human-centered, declarative. "모두가 사랑하는 방식의 광고." |
| Product / feature copy | Benefit-first, backed by a concrete metric. "39% 평균 클릭률", "x4 전환율". |
| CTAs | Direct and low-pressure. "문의하기", "광고센터 바로가기", "광고 상품 둘러보기". |
| Goal selector | Plain, outcome-framed. "브랜드 알리기", "전체". |
| Tech / design blog | Peer-to-peer engineering voice, reflective and principled (e.g. minimalism, technical debt in design). |

**Forbidden register** (source): interruption-framed ad language, fear-based or dark-pattern urgency, hollow superlatives without a metric, jargon left unexplained to non-marketers.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover visual treatments other than the Top Nav captured selected/hover appearance; press and `focus-visible` visual treatments
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Contact CTA and Ghost on Dark
- interactive kind and state-applicability map for Feature / Stat Card and Dark Slate Card
- YAML `lineHeight` on Display, Section, Sub-section, Title, Nav Link, Button Large, and Caption
- YAML `bg` on Language Toggle and Ghost on Dark; YAML `height` on Language Toggle; YAML `border` on Secondary Explore; YAML `padding` on Dark Slate Card
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
