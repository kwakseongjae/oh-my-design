# au Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

au is KDDI’s consumer communications brand: it began as a mobile-phone brand and, in 2012, expanded to consumer services alongside mobile service under the company’s 3M strategy. Catalog homepage identity is `https://www.au.com/`. Official visual identity = au Visual Identity (`https://www.kddi.com/brand/visual-identity/au/`, `ds.type: brand`). The YAML `ds.description` records official au visual-identity guidance for voice, design principles, colour, typography, graphics, photography, logo, and slogan use.

Treating the following three au.com URLs as the named product-surface evidence domains of this reconstruction, and treating official Visual Identity and brand-message documents as official-doc evidence rather than an extra au.com capture, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. This contract covers three supplied au.com product captures from the 2026-07-13 packet: the public telecom home `https://www.au.com/`; the public mobile catalog `https://www.au.com/mobile/`; and the public smartphone catalog `https://www.au.com/mobile/product/smartphone/`. Official Visual Identity and brand-message documents explain voice, principles, colour, typography, and slogan use.

Source token note: Machine tokens are limited to supplied au.com product-surface measurements and explicitly labelled official Visual Identity facts. The official brand font is not substituted for the observed loaded UI family. Treating that note as a register split — measured catalog values stay with the three au.com captures; official au Orange, Tazugane Gothic, SIMPLE, and voice characteristics stay labelled as Visual Identity facts; `noto-sans-cjk-jp` is the loaded UI family and is not replaced by Tazugane Gothic — is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

The following visual-character, tension, identity-mark, deliberately-practical / information-rich, and highly-visible-orange-action-lane readings are a derived editorial implementation inference from the verified surfaces; they are not au-authored or a separately published UI specification. The source reads the public product routes as a deliberately practical, information-rich telecom experience: white canvas, charcoal text, bright blue links, square controls, bordered cards, and a highly visible orange action lane. The distinction is read as the tension between the comparatively neutral service UI and the more expressive official brand system. Orange cursive lettering in open white space remains the identity mark in that reading, while the current brand message, 「おもしろいほうの未来へ。」, is the official line.

au’s current brand message is 「おもしろいほうの未来へ。」, introduced in 2019 in place of 「あたらしい自由。」. KDDI describes the shift as joining communications with life design so it can propose enjoyable change and exciting experience value in customers’ lives. au says it aims to be a companion helping everyone move toward the more interesting future.

Treating the following 2025 Visual Identity update as narrative and official guidance rather than a wholesale transcription of that brand book into product tokens, treating `#0066AA`, `#333333`, white, and hairlines as the colours that organise the observed catalog UI, retaining those domains separately, and keeping the canonical token set restricted to the three supplied au.com captures so the broader brand story explains direction, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. In March 2025 au refreshed its Visual Identity guidance, simplified its design and text expression, added slogan rules, and adjusted colour guidance alongside an au UI colour system. The 2025 revision seeks a consistent expression across a widening range of services and customer touchpoints. The public mobile routes captured here are product-surface evidence, not a wholesale transcription of that brand book: `#EB5505` is officially au Orange and appears on a measured mobile action, while `#0066AA`, `#333333`, white, and hairlines organise the observed catalog UI. This reference retains those domains separately. The canonical product tokens in this document stay restricted to the three supplied au.com captures; the broader brand story explains the direction.
<!-- design-md:claim-end -->

The following unmeasured-facts reading is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. The broader brand story explains the direction. Unmeasured facts stay unnamed.

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three captured public-route jobs below as the independently verified user outcomes, and keeping source §13 (which records no first-party persona segmentation) out of this list, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

- Use the captured au.com public home on `https://www.au.com/`.
- Scan mobile-catalog service cards, tabs, orange/outline actions, global navigation, and header search on `https://www.au.com/mobile/`. Source claim metadata binds navigation/search typography and `header-search.*` to the mobile surface / mobile-capture, not to home.
- Scan smartphone-catalog product labels on `https://www.au.com/mobile/product/smartphone/`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 records that no first-party persona segmentation was found in the sources consulted, that available first-party material addresses customers broadly and calls for consideration of diverse people, and that it does not authorize invented demographic or behavioral personas. The source placeholder for validated audience archetypes is omitted rather than filled. Restricting Audience so no individual personas are promoted, no invented demographic or behavioral persona is named, the source placeholder is omitted rather than filled, and observable work is tied only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

### Distinctive traits

- Catalog `primary_color` / official au Orange `#EB5505` (`#eb5505`; RGB 235, 85, 5) on a measured mobile action, a product-label text/border, and a selected-tab 2px underline
- White canvas `#FFFFFF` (`#ffffff`) with charcoal foreground `#333333` and bright catalog links `#0066AA` (`#0066aa`)
- Loaded UI family `noto-sans-cjk-jp` (1,437 observed uses); official brand-message face Tazugane Gothic, not substituted for that UI family
- Square controls at `0px`; mobile service cards at `5px` with `#E5E5E5` (`#e5e5e5`) hairlines
- Repeated 16px / 400 body and card text; 15px / 700 global navigation; 12px / 400 header search
- Official brand message 「おもしろいほうの未来へ。」
- Only the catalog tab has captured interaction provenance; hover, focus, pressed, disabled, error, modal, and toast visual treatments are not promoted from this packet

Treating `#EB5505` as official au Orange plus the measured action / label / selected-underline jobs rather than one interchangeable semantic, treating `#0066AA` as a catalog-link measurement rather than an au brand colour, treating `0px` controls and `5px` cards as distinct recorded surfaces rather than one radius scale, treating `noto-sans-cjk-jp` as the loaded UI family rather than Tazugane Gothic, treating the orange action lane against neutral catalog chrome as visual character rather than published UI doctrine, and treating hover, focus, pressed, disabled, error, modal, and toast visual treatments as not promoted from this packet, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

### Principles

The four numbered stems below are first-party au Visual Identity language from the official design-principles and tone-of-voice pages. The *UI implication* notes and the capture-bound application list are a derived editorial implementation inference from the verified surfaces; they are not au-authored or a separately published UI specification.

1. **Make SIMPLE the baseline.** au requires simple composition, sufficient whitespace, disciplined type, uncluttered photography, and a unified colour impression.
   *UI implication:* reduce surplus decoration before adding a new product-surface token.
2. **Use a service-appropriate secondary character.** CLEAN, ADVANCED, PLAYFUL, and FRIENDLY are additional values selected by the service’s intended impression.
   *UI implication:* do not flatten all four into a simultaneous visual recipe.
3. **Keep orange intentional.** Official guidance treats au Orange as a primary brand element whose use and proportion need care. Official V.I. says to use au Orange thoughtfully rather than indiscriminately.
   *UI implication:* retain the observed orange action/underline/label roles instead of assigning arbitrary semantic states.
4. **Express a considerate personality.** The V.I. makes future orientation, curiosity/playfulness, approachability, and respect for others its four voice characteristics.
   *UI implication:* concise service copy should remain positive and attentive rather than merely promotional.

Treating the following as a capture-bound application of source §7 Do’s and harvested geometry, with first-party SIMPLE language marked separately from the remaining token-role rules, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

- Use au Orange `#EB5505` as the recorded brand/action emphasis, leaving white space and neutral product chrome around it.
- Keep the observed product UI family as `noto-sans-cjk-jp`; use Tazugane Gothic only under its official brand-message boundary.
- Preserve the measured square controls, 5px cards, hairlines, and selected-tab orange underline on the documented public surfaces.
- Follow the official SIMPLE principle: reduce nonessential elements, retain generous whitespace, unify type, and avoid unnecessary colour variation.

### Avoid

The following items copy source §7 Don’ts and the unique §9 construction constraints, including catalog-link-not-brand-colour on `#0066AA`, Helvetica-or-brand-font-not-UI-family, unobserved-state-not-invented, catalog-chrome-not-KDDI-corporate-marketing-or-authenticated-app, and rounded-generic-CTA / fallback-font / unobserved-state / corporate-marketing-token. They are a derived editorial implementation inference from the verified surfaces; they are not au-authored or a separately published UI specification.

- Do not promote `#0066AA` from a catalog-link measurement into an au brand colour.
- Do not substitute Helvetica or another system family for the observed UI family, or use the official brand font as though it were a loaded product webfont.
- Do not invent hover, focus, pressed, disabled, error, modal, toast, or checkout states.
- Do not use product catalog chrome as evidence for KDDI corporate, marketing, or authenticated-app UI.
- Do not add a rounded generic CTA, a fallback font, an unobserved state, or a corporate-marketing token. Treating those unique §9 construction constraints as reconstruction limits rather than au-authored UI specification is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-link-not-brand-colour on `#0066AA`, action-underline-label-unmerged on `#EB5505`, canvas-on-primary-card-search-outline-label `#FFFFFF` not-a-second-canvas-token, `#333333` not-Link, `#777777` not-Foreground, and hairline `#E5E5E5` not-control-border `#D1D1D1`, are a derived editorial implementation inference from the verified surfaces; they are not au-authored or a separately published UI specification. Catalog `primary_color` YAML `#eb5505` / body `#EB5505` is official au Orange (RGB 235, 85, 5); the same hex is the mobile-catalog orange-action fill, the smartphone-label text/border, and the selected-tab 2px underline — those jobs share a hex and are not one semantic. YAML `#ffffff` / body `#FFFFFF` is canvas, on-primary (white action text), card surface, search surface, outline-action fill, and smartphone-label fill; those jobs share a hex and are not a second canvas token. `#333333` is repeated public text, navigation, search text, outline-action text, and unselected tab ink; it is not Link. YAML `#0066aa` / body `#0066AA` is repeated public catalog link and card-link colour; it is not promoted as a corporate brand colour. `#777777` is supporting text, not Foreground. YAML `#e5e5e5` / body `#E5E5E5` is the mobile card edge; it is not control-border YAML `#d1d1d1` / body `#D1D1D1`, which is the header-search border.

Treating the token note as that register split — only supplied au.com product-surface measurements and explicitly labelled official Visual Identity facts are tokens; the official brand font is not substituted for the observed loaded UI family — is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

- **au Orange / Primary** (`#EB5505` / YAML `#eb5505`): official brand colour (RGB 235, 85, 5). Measured as the fill of the supplied mobile-catalog primary action and as product-label text/border. YAML `primary`. Official V.I. says to use au Orange thoughtfully rather than indiscriminately.
- **Canvas / on-primary** (`#FFFFFF` / YAML `#ffffff`): observed mobile catalog card and search surface; observed white action text. YAML `canvas` / `on-primary`.
- **Foreground** (`#333333`): repeated public text, navigation, search text, controls, and unselected tab ink. YAML `foreground`.
- **Link** (`#0066AA` / YAML `#0066aa`): repeated public catalog link and card-link colour; it is not promoted as a corporate brand colour. Treating that catalog-link measurement as not a corporate brand colour is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. YAML `link`.
- **Muted** (`#777777`): repeated public supporting text. YAML `muted`.
- **Hairline** (`#E5E5E5` / YAML `#e5e5e5`): observed mobile card edge. YAML `hairline`.
- **Control border** (`#D1D1D1` / YAML `#d1d1d1`): observed header-search border. YAML `control-border`.

The public capture does not establish semantic success, error, warning, or disabled colour tokens, so those are absent.

### Spacing

YAML `spacing`: xs 10, sm 15, md 20, lg 25. Body layout records the observed aggregate 10, 15, 20, and 25px, preserved as a local frequency-backed set rather than an asserted global spacing scale. Treating that YAML scale and the 10, 15, 20, and 25px aggregate as recorded spacing in this packet rather than a universal spacing token for every unlisted control, and treating component padding as staying with the components, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. Orange/outline-action padding is `12.8px 40px 12.8px 15px`. Header-search padding is `0px 30px 0px 6px`. Tab padding is `0px 16px`. Service-card padding is `15px 25px 25px`. Smartphone-label padding is `3.3px 11px`.

### Shape

YAML `rounded`: square 0, card 5.

- Square controls (orange action, outline action, header search, tab, smartphone label): `0px`
- Mobile service card: `5px`

The following local-geometry reading, including no-rounded-generic-CTA, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `0px` control corners and `5px` card geometry are local recorded defaults, not a universal radius scale. Do not add a rounded generic CTA.

### Elevation

The representative public controls, cards, tab, and search input have `box-shadow: none`. YAML `shadow.flat` is `none`. Treating flat separation as coming from white surfaces, hairlines, and square-to-5px corners, rather than from a published elevation ladder, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

A single home modal trigger has a local shadow in the packet, but no opened dialog state or repeatable elevation system was observed, so it is not promoted to a shadow token.

The following not-a-general-ladder reading is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. The local modal-trigger shadow is that specimen’s field, not a page-wide elevation token and not a repeatable elevation system.

### Motion

No duration, easing curve, transition, carousel timing, or motion-reduction behavior is established by the supplied evidence. Motion tokens are absent rather than inferred from static markup.

Do not promote a motion duration, easing, animation name, transition, or reduced-motion behavior until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including only-UI-family-token on live computed `noto-sans-cjk-jp`, brand-message-not-substitute on Tazugane Gothic, Helvetica-not-an-au-type-asset-or-token, and no-declared-only-face-promoted, are a derived editorial implementation inference from the verified surfaces; they are not au-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product/brand guidance | au’s Visual Identity specifies Tazugane Gothic for brand messages and recommends Noto Sans JP for standard use. The brand face requires contact with brand management; the Noto Sans JP download is linked by the official guidance. |
| Live computed surface-use | The supplied three-route capture resolves `noto-sans-cjk-jp` as loaded/high confidence for 1,437 observed uses across body, navigation, cards, inputs, tabs, buttons, and labels. This is the only UI-family token. |
| Official distributed brand asset | Tazugane Gothic is a named official brand typeface with Light, Regular, and Bold samples in the V.I. guidance. It remains a brand-message asset, not a substitute for the captured web UI family. |
| System-resolved | Helvetica appears twice as an operating-system stack in the capture. It is not an au type asset or a token. |
| Declared-only / unresolved | No declared-only face is promoted by this packet. |

### Family

- **Current visible UI family:** `noto-sans-cjk-jp` — YAML `family.ui`
- **Official brand-message face:** Tazugane Gothic — YAML `family.brand`; Light / Regular / Bold distributed in V.I. guidance; no live product specimen claimed
- YAML body use: Repeated public au.com body and card text
- YAML navigation use: Public global-navigation item
- YAML search use: Public header text search input

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. Do not substitute Helvetica or another system family for the observed UI family. Do not use Tazugane Gothic as though it were a loaded product webfont. Do not present `noto-sans-cjk-jp` as Tazugane Gothic. Do not replace the observed UI family with a fallback font.

### Type roles

Verified YAML line-height values are the unitless ratios `1.4` (body, navigation) and `2.5` (search).

The following ratio-versus-size-local reading, including not converting those recorded values into a different unit and not promoting smartphone-label `11px / 400 / noto-sans-cjk-jp` as a fourth YAML type role, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. The px figures in the body table are size-local observations, not replacements for the ratios. Smartphone-label `11px / 400 / noto-sans-cjk-jp` stays with that component.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Provenance |
|---|---|---:|---:|---:|---|---|
| Public body/card text | `noto-sans-cjk-jp` | 16px | 400 | 1.4 | 22.4px | Repeated supplied product-surface measurement. YAML `body`. |
| Public global navigation | `noto-sans-cjk-jp` | 15px | 700 | 1.4 | 21px | Measured navigation item. YAML `navigation`. |
| Public header search | `noto-sans-cjk-jp` | 12px | 400 | 2.5 | 30px | Measured text input. YAML `search`. |
| Brand message | Tazugane Gothic | — | Light/Regular/Bold distributed | — | — | Official V.I. brand-asset guidance; no live product specimen claimed |

YAML `use`: body = Repeated public au.com body and card text. navigation = Public global-navigation item. search = Public header text search input.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification.

The supplied evidence records one tab-selection interaction and no stable empty, loading, error, success, skeleton, or disabled component states. Those state groups are therefore absent from the token graph and component specifications. An implementation may provide accessible states, but it must not label their visual values as observed au facts.

Only the tab has captured interaction provenance. Hover, focus, pressed, disabled, error, modal, toast, and other interactive state values are absent; their absence does not remove these measured default geometries.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no generic Focus capture with a colour; `focus-visible` rows carry no colour. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact destination/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The Mobile service card and Smartphone product label have default geometry and no state or interactive-kind evidence, so kind and a state-applicability map are omitted.

### Mobile orange action

- Role: mobile catalog orange action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#EB5505`
- Text: `#FFFFFF`
- Radius: `0px`
- Height: `44px`
- Padding: `12.8px 40px 12.8px 15px`
- Font: `16px / 700 / noto-sans-cjk-jp`
- Use: Mobile catalog action; `surface-2::[data-omd-capture="91"]`. YAML `tokens.components.mobile-orange-action`.
- Observed: default only; no button interaction state captured
- Field note: The following unmerged-field reading, including harvested-height-not-touch-target on `44px`, fill-jobs-not-this-button, and `#FFFFFF` label-not-a-second-canvas-token, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `#EB5505` is this control’s fill, catalog `primary_color`, and official au Orange. It is also smartphone-label text/border and the selected-tab underline; those jobs are not this button. `#FFFFFF` is this control’s label and on-primary; it is also canvas / card / search fill, not a second canvas token. Height `44px` is this action’s harvested height, not a touch-target policy.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the mobile catalog orange action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A catalog action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the mobile catalog orange action and records selector `surface-2::[data-omd-capture="91"]`; exact destination/request/outcome is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Mobile outline action

- Role: mobile catalog outline action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#FFFFFF`
- Text: `#333333`
- Border: `1px solid #333333`
- Radius: `0px`
- Height: `44px`
- Padding: `12.8px 40px 12.8px 15px`
- Font: `16px / 400 / noto-sans-cjk-jp`
- Use: Mobile catalog outline action; `surface-2::[data-omd-capture="92"]`. YAML `tokens.components.mobile-outline-action`.
- Observed: default only; no button interaction state captured
- Field note: The following unmerged-field reading, including harvested-height-not-touch-target on `44px`, not merging this outline action with the orange action, `#FFFFFF` shared-hex-not-a-second-canvas-token, and `#333333` not-Link, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `#FFFFFF` is this control’s fill; it is also canvas / on-primary / card / search fill. `#333333` is this control’s text and Foreground, not Link. Height `44px` is this action’s harvested height, not a touch-target policy and not a second orange-action token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the mobile catalog outline action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A catalog action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the mobile catalog outline action and records selector `surface-2::[data-omd-capture="92"]`; exact destination/request/outcome is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Header search

- Role: public header text search
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#FFFFFF`
- Text: `#333333`
- Border: `1px solid #D1D1D1`
- Radius: `0px`
- Height: `32px`
- Padding: `0px 30px 0px 6px`
- Font: `12px / 400 / noto-sans-cjk-jp`
- Use: Public header text search; `surface-2::[data-omd-capture="8"]`. YAML `tokens.components.header-search`.
- Observed: default only; no input interaction state captured
- Field note: The following unmerged-field reading, including search-ratio-versus-30px, height-32px-not-44px-actions, and control-border-not-hairline, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `#D1D1D1` is this control’s border and YAML `control-border`; it is not Hairline `#E5E5E5`. `#333333` is this input’s text and Foreground. YAML search lineHeight `2.5` is the type-role ratio; `30px` is the size-local body-table observation at 12px, not a replacement for the ratio. Height `32px` is this input, not the 44px actions.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public header search input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A search input can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the public header search input and records selector `surface-2::[data-omd-capture="8"]`; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Mobile catalog tab

- Role: mobile catalog tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#333333`
- Border (selected item as captured): `0px 0px 2px solid #EB5505`
- Radius: `0px`
- Padding: `0px 16px`
- Font: `16px / 400 / noto-sans-cjk-jp`
- Use: Mobile catalog tab; `surface-2::[data-omd-capture="81"]`. YAML `tokens.components.mobile-tab`.
- Observed: selected and unselected were observed; a tab-selection interaction was captured
- Field note: The following unmerged-field reading, including 2px-underline-only-on-selected-tab and not-a-second-primary-token, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `#333333` is this tab’s text and Foreground. `#EB5505` on the 2px selected underline is this tab’s selected-border field; it is also official au Orange and the orange-action fill, not a second primary token. Add a 2px orange underline only to an observed selected tab.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Unselected and selected appearances observed on the mobile catalog tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A catalog tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A catalog tab selects a grouping; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus unselected, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

Additional observed named state: selected item as captured (`0px 0px 2px solid #EB5505`). Source records that a tab-selection interaction was captured. That selected underline is the captured selected appearance.

### Mobile service card

- Role: mobile catalog service-card wrapper and contents
- Type: card
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Anatomy: surface
- Background: `#FFFFFF`
- Border: `1px solid #E5E5E5`
- Radius: `5px`
- Padding: `15px 25px 25px`
- Use: Mobile catalog card wrapper and contents; `surface-2::div.cmp-au-com-card__wrapper` and `.cmp-au-com-card__contents-section`. YAML `tokens.components.mobile-service-card`.
- Observed: default only
- Field note: The following unmerged-field reading, including canvas-fill, hairline-not-control-border, and 5px-not-0px-control-geometry, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `#FFFFFF` is this card’s fill and Canvas. `#E5E5E5` is this card’s edge and Hairline, not control-border `#D1D1D1`. `5px` is this card’s radius, not the `0px` control geometry.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Smartphone product label

- Role: smartphone catalog product label
- Type: badge
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Anatomy: label
- Background: `#FFFFFF`
- Text: `#EB5505`
- Border: `1px solid #EB5505`
- Radius: `0px`
- Padding: `3.3px 11px`
- Font: `11px / 400 / noto-sans-cjk-jp`
- Use: Smartphone catalog product label; `surface-3::span.product-card-item-label`. YAML `tokens.components.smartphone-label`.
- Observed: default only
- Field note: The following unmerged-field reading, including 11px-not-a-fourth-YAML-type-role, hex-jobs-not-a-type-role, and fill-not-a-second-canvas-token, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. `#EB5505` is this label’s text and border; it is also official au Orange and the orange-action fill, not a fourth YAML type role. `11px / 400` stays with this component. `#FFFFFF` is this label’s fill, not a second canvas token.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The supplied desktop capture is 1440×900 and records three public surfaces.

The following measurement-boundary, local-frequency, and not-generalized readings, including 5px-bordered-cards / product-card-shells, not-a-touch-target-policy, and not-cross-viewport-specifications, are a derived editorial implementation inference from the verified surfaces; they are not au-authored or a separately published UI specification. The mobile catalog uses repeated 5px bordered cards, including a card-content padding sample of `15px 25px 25px`; the smartphone route also records product-card shells and compact labels. The observed aggregate includes 10, 15, 20, and 25px spacing values, preserved as a local frequency-backed set rather than an asserted global spacing scale. The 44px orange/outline actions, 32px header search, and compact smartphone labels are desktop-capture measurements, not a touch-target policy and not cross-viewport specifications.

Treating the packet as desktop 1440×900 snapshots only, with no breakpoint, sticky behavior, or touch-target policy claimed, and treating responsive rules as intentionally absent rather than inferred, is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. No breakpoint, fixed container width, authenticated purchase journey, or responsive-grid rule is claimed. The packet records desktop 1440×900 snapshots only. It establishes repeated public components across three routes but no mobile viewport, breakpoint, sticky behavior, or touch-target policy. Responsive rules are intentionally absent.

<!-- design-md:section content-locales -->
## 6. Content & Locales

**Voice adjectives (official V.I.):** future-oriented · curious/playful · approachable · respectful

Official V.I. records a respect requirement: make respect for other people a non-negotiable baseline, and do not use discriminatory or hurtful language.

The English Do/Don’t implementation sentences in the table below are a derived editorial implementation inference from the source reconstruction; they are not au-authored or a separately published UI specification.

| Do | Don't |
|---|---|
| Speak positively about the next choice or possibility. | Treat the future as fixed or inaccessible. |
| Show curiosity and room for discovery without making the customer the punchline. | Use novelty or humour at another person’s expense. |
| Keep an approachable, calm register that can sit beside daily life. | Sound distant, over-formal, or needlessly technical. |
| Make respect for other people a non-negotiable baseline. | Use discriminatory or hurtful language. |

Source-derived sample direction, not reusable au copy: “Choose the path that opens a new possibility.” “Let’s make the next connection easier to explore.” “Start from what matters in your everyday life.”

The following no-synthetic-sample reading is a derived editorial implementation inference from the verified surfaces; it is not au-authored or a separately published UI specification. Those three lines remain labelled source-derived sample direction, not reusable au copy. No additional synthetic voice samples are promoted.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- semantic success, error, warning, and disabled colour tokens
- hover, focus, pressed, disabled, error, modal, toast, empty, loading, success, skeleton, and checkout visual treatments
- Mobile orange action, Mobile outline action, and Header search loading, error, and success applicability (exact destination/request/outcome unresolved; capture selectors remain known where recorded)
- interactive kind and state-applicability map for the Mobile service card and the Smartphone product label
- `focus-visible` visual treatments
- a universal spacing scale; a universal radius scale; opened dialog state; a repeatable elevation system
- a live product specimen of Tazugane Gothic; Helvetica or another system family as the UI family
- breakpoint, fixed container width, authenticated purchase journey, responsive-grid, mobile viewport, sticky behavior, and touch-target policy
- first-party persona segmentation; invented demographic or behavioral personas
- motion duration, easing, animation name, transition properties, carousel timing, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
