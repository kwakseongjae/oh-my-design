B2a·E2 감사만 수행합니다. 먼저 규칙과 AU 산출물·원문을 읽고, 문장 분류와 원장 대조부터 시작하겠습니다.규칙 B2·E2만 기준으로 두고, AU 본문·원장·원본과 최근 감사 로그 형식을 함께 읽겠습니다.문장 분류와 로그 줄번호 대조를 위해 원본·유사 감사 로그·핵심 문자열 grep을 이어서 확인합니다.로그가 가리키는 줄번호·이중 목적지·B3 전문을 값 단위로 grep해 원장 불일치를 확정합니다.분류가 끝났습니다. 세 번째 부류인데 한정이 없거나 인접하지 않은 문장부터 고치고, 이어서 원장 줄번호와 이중 목적지를 맞춥니다.줄번호는 유지됐습니다. provenance 파생 목록과 로그 목적지를 실제 본문에 맞춥니다.Core 검사가 Scope를 놓쳤습니다. 한정 문장이 탐지 휴리스틱을 깨뜨렸는지 확인합니다.Scope 한정에 `without`…`product`가 붙어 Core 검사가 막혔습니다. 같은 의미로 문장만 다시 씁니다.AU F3 감사는 B2a 한정과 원장 불일치만 고쳤습니다. 토큰 값, 컴포넌트 표, 상태 applicability, 구조는 그대로입니다.

**B2a.** 편집적 해석인데 인접 완전 한정이 없거나 읽기를 못 가리키던 문장에 `derived editorial implementation inference` / `not au-authored or a separately published UI specification`을 붙이거나 이름을 넓혔습니다. 대상은 Scope(practical/information-rich, organise/retain-domains), Audience(no-individual-personas), Distinctive(not-promoted-states), Avoid 마지막 불릿, Semantic 추가 비해석 합침, Foundations Link, Elevation 평면 분리, Font 증거 종류, 필드 노트, Layout `intentionally absent`입니다.

**E2.** migration-log 목적지를 grep으로 맞춰 고쳤습니다. Avoid 74를 `#EB5505` 목적지로 적은 것, `#0066AA` not-brand를 Scope 19·Distinctive 41에 둔 것, 탭/Layout/Elevation 줄번호, `"8"` Type-roles, F1/F2 준수 과장이 해당됩니다. provenance 파생 목록도 본문 실제에 맞췄습니다.

Post-F3 DESIGN SHA-256 `28a67dff8b79141ee7842ceb9d3d59982eac65e3ee637275b8a4956899af0e0a`. `--gate-only` PASS, `portable_core: true`. 기록은 `docs/design-md-weight/migrated/au/audit-log.md`입니다.

AUDIT_DONE fixes=43
