# goorm migration log

Source: `web/references/goorm/DESIGN.md`
Sibling read (not the migration input): `web/references/goorm/.verification.md`
Destination: `docs/design-md-weight/migrated/goorm/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/goorm/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as committed in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `1e82dac09da2d1c1dbc05c98b6c9b7b9c87b37b0896bcf18feb417a98ed9964f` (`web/references/goorm/DESIGN.md`). Sibling SHA-256 `67e8e128cada778778ac4cd97163fdd2edb63d7416d6d15a9ff4dc6094dc6aa4` (`web/references/goorm/.verification.md`). Worker-close portable DESIGN SHA-256 `f512434347fceeaeec62787249a7f601cd5b53e842658ddb0caf34adfa3ac8d2`. Post-F3 portable DESIGN SHA-256 `28ce80b0200b9cc8efffb404f060981a7ce3f737563df21a9ca2308b01fca3bb`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Typography & Assets Assets; `display_name_kr` 옮김 → Scope + Content | Portable file has no frontmatter; H1 is `# goorm Design System` (`DESIGN.md` 1). Identity table `provenance.md` 7–22. `https://goorm.co` is dual: `DESIGN.md` 9/21/28/79/91/174/192/194/200/214/323/334/480/481 + `provenance.md` 14/45/50 (E2a). `#2a72e5` is dual: `DESIGN.md` 11/35/49/59/91/146/226/240/249/361/362/522 + `provenance.md` 15/26/72/92/117/139 (E2a). The hex was removed from the input `focus-visible` reason cell so the row carries no treatment (B1); it remains on the input's own Focus record at 361. The favicon slug is dual: `DESIGN.md` 214 + `provenance.md` 16 (E2a). `구름` is dual: `DESIGN.md` 9/21/456/466/481 + `provenance.md` 11. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–22, 32–35, 137. The source token note is transcribed at `provenance.md` 26. The source footer's producer string `omd:add-reference CREATE — Tier 1 live inspect, 2 surfaces` is at `provenance.md` 37. These are ledger keys with no portable slot. |
| YAML `tokens.colors` (19 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 91–117. All nineteen roles kept with their names and values: primary, primary-active, link, ink, text-secondary, text-muted, text-tertiary, faint, canvas, surface, hairline, border-strong, success, danger, info-tint, success-tint, danger-tint, warning-tint, on-primary. `#393939` (Tertiary Slate) and `#ffd9c8` (Warning Tint) have no other component home and stay palette roles at 100 and 117. |
| YAML `tokens.typography.family` (`sans: "Pretendard Variable"`) | 옮김 → Typography & Assets Family | `DESIGN.md` 183. The source-body fallback stack Pretendard / Apple SD Gothic Neo / Noto Sans KR is at 175/183. |
| YAML `tokens.typography.display-hero / display-soft / section / nav / body / button / button-lg / caption` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 189–198. Unitless line heights stay ratios and are never converted to px (A1a): `1.2` at 191/192/202, `1.3` at 193/202, `1.4` at 194/198/202, `1.5` at 195/202, `1.0` at 196/197/202; also `provenance.md` 102. The source visible table's `~1.2` / `~1.3` are recorded as table spelling at 202, not as the token. All eight YAML `use` strings are restored verbatim on one line at `DESIGN.md` 200 (A1, A3). Display Soft tracking `-0.4px` at 192. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (5 steps) | 옮김 → Foundations spacing + shape; also Layout | Unitless steps and px forms both kept: `DESIGN.md` 121 (`xs 4 · sm 8 · md 12 · base 16 · lg 24 · xl 32 · xxl 48` plus the px scale), 125–131 (`sm 6 · md 8 · lg 12 · xl 16 · full 9999` plus the five named uses, including `50%` avatars), 430 (Layout restatement). `full: 9999` stays a step (`DESIGN.md` 125/131/430). |
| YAML `tokens.shadow.none` / `inset-hairline` / `inset-strong` | 옮김 → Foundations elevation | `DESIGN.md` 144. Inset Strong also in the elevation table at 142. `rgb(225, 225, 225) 0px 0px 0px 1px inset` and `rgb(198, 198, 198) 0px 0px 0px 1px inset` keep their `rgb()` byte forms (`provenance.md` 105). |
| YAML `tokens.components` (11 records) | 옮김 → Components & States | `DESIGN.md` 244–425. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×4 (247/272/297/324), `input` (349), `card` (378), `tab` (390), `badge` (414). All eleven YAML `use` strings restored verbatim as `Token-set use:` lines (257/282/309/334/362/384/397) plus the four badge uses in the variant table at 422–425. YAML `font` shorthands kept as `token-set font record` lines (255/280/307/332/359/396/417). YAML padding byte forms `0 16px` (256/281/308) and `0 24px` (333/360) kept beside the §4 `0px` spellings. YAML border byte forms `1px solid #c6c6c6` (302) and `1px solid #e1e1e1` (354/382). The tab `active` record is kept verbatim at 398. |
| §1 Visual Theme & Atmosphere (legacy 72–88) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Key Characteristics as Distinctive traits 34–41. The atmosphere readings ("calm, engineered software documentation", "engineered to disappear behind the work") carry an adjacent complete qualifier at 11 (B2/B2a). The surface-boundary reading carries one at 9. |
| §2 Color Palette & Roles (legacy 90–116) | 옮김 → Foundations semantic color | `DESIGN.md` 91–117. The source's own group headings (Primary & Interactive, Ink & Text Hierarchy, Neutral & Surface, Semantic & Tints) are kept as the grouping. The characterizing phrases the source attaches to roles are covered by the adjacent qualifier at 87 (B2/B2a). |
| §3 Typography Rules — family, hierarchy (legacy 118–134) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 171–179; family 183–185; hierarchy table 189–198. The License-row reading that treats Pretendard Variable as an upstream face, not a goorm-owned brand asset, carries an adjacent complete qualifier at 178 (B2/B2a). |
| §3 Principles — one family two registers, two display moods, hangul-first sizing, quiet UI weight (legacy 136–140) | 옮김 → Typography & Assets Type rules | The observable half of the scale is stated plainly at `DESIGN.md` 208; the interpretive half sits inside the qualified sentence at 210 (B2/B2a). |
| §4 Component Stylings (legacy 142–241) | 옮김 → Components & States | `DESIGN.md` 244–425. §4 body values (`0px 16px`, `0px 24px`, `1px solid` with backtick hex) and YAML `use` / padding / border byte forms are both kept where they differ. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 245–248) | 분리 → provenance; URL 다섯 개 옮김 → Experience Scope + Typography & Assets + Governance | Freshness `provenance.md` 28–37; Tier 1 list 50–54; Tier 2 57–59. `https://vapor-ui.goorm.io` is dual: `DESIGN.md` 9 + `provenance.md` 46/51. `https://tech.goorm.io` and `https://blog.goorm.io/design/` are dual: `DESIGN.md` 9/179/526 + `provenance.md` 53–54/136 (E2a). Conflicts unresolved: none — `provenance.md` 39. |
| §5 Layout Principles (legacy 250–273) | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 430 (centered column, sticky 40px nav, docs side-nav + TOC, alternating `#ffffff` / `#f7f7f7` bands, 12px cards, radius scale), 121 (spacing), 125–131 (radius). The "calm density" / flat-segmentation reading carries an adjacent complete qualifier at 432 (B2/B2a). |
| §6 Depth & Elevation (legacy 275–284) | 옮김 → Foundations elevation | Four-level table `DESIGN.md` 137–142, including Inset Strong `rgb(198, 198, 198) 0px 0px 0px 1px inset`. The Shadow Philosophy paragraph is carried as a qualified reading at 146 (B2/B2a). The text-field focus ring `#2a72e5` is deliberately not attached to any component `focus-visible` row: `focus-visible` occurs 0 times in the source, measured with `grep -o 'focus-visible' web/references/goorm/DESIGN.md \| wc -l` (B1); recorded at `provenance.md` 139. |
| §7 Do's (legacy 288–296) | 옮김 → Experience application rules | `DESIGN.md` 58–65, under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 298–306) | 옮김 → Experience avoid | `DESIGN.md` 71–78, under the qualifier at 69. One extra line at 79 restates this contract's scope boundary as a prohibition; 69 says so. |
| §8 Responsive Behavior (legacy 308–330) | 옮김 → Layout & Platforms; image behavior 옮김 → Typography & Assets Assets + Layout | Breakpoint table `DESIGN.md` 436–440 with the "declared behavior rather than an observation" note at 434; touch targets 442; collapsing strategy 444; image behavior 215/446. The source's Desktop row `1024-1440px` is kept as declared width, not rewritten as a measured band (`measures 1440px` occurs 0 times in `DESIGN.md`). |
| §9 Agent Prompt Guide — Quick Color Reference, Example Component Prompts, Iteration Guide (legacy 332–361) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present — the palette hexes, the 8px/12px/9999px radii, the 14px/16px/32px/48px sizes, the 400/500/600/800 weights, the 0×16px and 0×24px paddings, and the inset hairline / strong-border treatments (A2, A3). The check is itemised at `provenance.md` 117. |
| §10 Voice & Tone (legacy 365–382) | 옮김 → Content & Locales | Published strings `DESIGN.md` 454–466; register table 470–476; voice samples 478–482; forbidden register 484. The source's own English gloss of the hero is kept at 456; reading it as a reader aid that never replaces the Korean line carries its own adjacent complete qualifier on that same line (B2/B2a, A5). The qualifier at 468 covers the table and the forbidden-register rule (B2/B2a). |
| §11 Brand Narrative (legacy 384–390) | 옮김 → Experience Scope | `DESIGN.md` 13: founding **2013**, name = Korean for "cloud", goormIDE / goormEDU / goormEXP / goormDEVTH / goormLEVEL / Arkain / EXP, AX (AI Transformation), the "anyone can become a developer" belief, the TechBlog mission line, and the source's own class that founding-year and subscriber figures are widely reported public facts, not quoted from a single verified goorm statement. The source's own naming of Vapor UI as recently open-sourced as **Vapor UI**, a WCAG-compliant React component library with a Leonardo-driven color system, is restored on the same line as a source-stated fact (wave28 review). Marked there as narrative context that supplies no interface tokens; the refusal/embrace readings and "the design is the message: development should feel approachable" carry an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 140. |
| §12 Principles — 6 numbered (legacy 392–399) | 옮김 → Experience principles | `DESIGN.md` 47–52 under the adapted B2a form at 45: "These six items are a derived editorial implementation inference from the verified surfaces; they are not goorm-authored or taken from a separately published UI specification, including the published Vapor UI documentation." The published-DS close is required because Vapor UI is a first-party issued specification (rulebook v12 B2a 전제 주석). |
| §13 Personas — 3 entries (legacy 401–409) | 삭제 | The source's own italic line at legacy 403 labels them fictional archetypes informed by publicly observable segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or segment appears in either output (D2, D2a). Disposition at `provenance.md` 114. Audience at `DESIGN.md` 28 carries only the group-level description the two captured surfaces establish, under an adjacent qualifier. The three `primary-tasks` at 21–23 come from modules and labels the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows (legacy 411–423) | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at `DESIGN.md` 224–234 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "오류가 발생했습니다" and 필수. The applicability rule is at 238–242, whose qualifier at 242 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 6 interactive components × 7 states = 42 rows: 35 `applicable` and 7 `not-applicable`, the latter only with a role reason (C2 v10) — the marketing CTA 341–344 (destination inquiry; disabled + L/E/S closed together) and the docs tab 407–409 (Preview/Code selector; L/E/S closed together). The primary / secondary / outline buttons and the text field keep loading/error/success `applicable` with treatments omitted, citing the surface state contract. The Content Card gets no `kind` and no map because the source supplies no interaction evidence for it — `DESIGN.md` 385 (C4). The Semantic Badge declares `kind: non-interactive` with a reason instead of a map — 414 (Core §4.4). Grouping the four token-block badge records into one component is qualified at 418 (B2/B2a); each variant keeps its values at 422–425. |
| §15 Motion & Easing — durations, easing token names/roles, motion rules, signature motions, reduced-motion (legacy 425–443) | 옮김 → Foundations motion | `DESIGN.md` 154–158 (the three duration tokens with their uses: `motion-fast` 120ms, `motion-standard` 200ms, `motion-slow` 320ms), 160 (three easing token names and roles), 162 (functional/quiet rule, active-tab underline, fade-from-below, no bounce or spring, and the `prefers-reduced-motion: reduce` behavior). |
| §15 Motion & Easing — three `cubic-bezier` values (legacy 437–441) | 삭제 → provenance omission ledger | No observation stands behind them: the source records a Tier 1 live inspect of color, type, geometry, border, and shadow and supplies no transition, animation, or easing sample. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to the example table at `spec/omd-v0.1.md` line 262, the documented re-injection path. Ledger at `provenance.md` 111–113. Occurrence counts by `grep -o cubic-bezier <file> \| wc -l` → `DESIGN.md` 0, `provenance.md` 3. B3 is held: the promotion condition at `DESIGN.md` 160 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). |
| HTML comment OmD v0.1 Sources (legacy 445–470) | 분리 → provenance | Live-inspect notes, Tier 1/2 URLs, narrative class, voice-sample attribution, and the editorial-reading admission are ledger material: `provenance.md` 61–105, 134–142. The GitHub quote `open-source UI component library` / 34+ / `@vapor-ui/color-generator` / `@vapor-ui/css-generator` is at `provenance.md` 142 (no portable slot). |

## Sibling handling (`web/references/goorm/.verification.md`)

The sibling exists — confirmed with `find web/references/goorm -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact.

- Its full record is transcribed at `provenance.md` 67–88 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: the accent-button label `미션 완료`, Vapor docs H1 fragment `Kickstart your project...`, H2 `Instantly customize your theme`, version tag `1.3.0`, radius frequency `999px` ×4, every `rgb()` byte form, and the frequency counts. Recorded at `provenance.md` 90–98. The outline-button label `100개 추가` is not sibling-only: the source HTML comment records it (source 1); visible §4 does not. DESIGN.md **0** / provenance mention (disposition, not a portable use string).
- Its structural classifications were likewise not promoted (B1): the Button/Badge component-page URLs, the frequency-scan ranks, and the `999px` count. The portable body keeps source `full: 9999` (`DESIGN.md` 125) and does not promote `999px`.
- Published strings that appear only in the sibling survive in provenance, not in the portable body: `미션 완료` (D 0 / P 2), `Kickstart your project` (D 0 / P 2), `Instantly customize` (D 0 / P 2), `1.3.0` (D 0 / P 2). `100개 추가` is D 0 / P 2 / source 1 (HTML comment).

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All Korean runs in the source, any length | 32 distinct | 6 | 0 | The six are §13 fictional-persona identifiers. Deleted with the personas under D2 (D2a: not restated). |
| All Korean runs in the sibling, any length | 19 distinct | 0 | 0 | Sibling-only labels `미션` / `완료` / `추가` survive in provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand goorm` after restore | 35 candidates | 0 reported | 0 | First run reported 5 lost (`0 16px`, `0 24px`, `1px solid #c6c6c6`, `open-source UI component library`, `Start AX with goorm`). All five restored: YAML padding/border byte forms as token-set records (`DESIGN.md` 256/281/302/308/333/354/360/382), the source's own English gloss at 456, and the GitHub quote at `provenance.md` 142. Re-run: `clean: 1`, `lost: 0`. |

Sub-needle labels the machine check could not see were confirmed present individually in `DESIGN.md`: 제품, 솔루션, 리소스, 채용, 도입 문의하기, 더 알아보기, Docs 보러 가기, 취소, Public으로 변경, 45 포인트 획득, 크레딧 개수, 오류가 발생했습니다, 필수, 구름, Superpowers, for everyone, AX, 구름과 함께 시작해보세요, Start AX with goorm, We are creating an ecosystem centered on developer growth, Preview / Code, Save.

A5 분모: latin-copy-audit 35/35 survived after restore; Korean hand sweep 32 extracted / 6 missing / 0 published-copy missing (the 6 are D2 deletions).

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand goorm --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 18, candidates: 219 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 18 < `candidates` 219.

## Deviations recorded

- `DESIGN.md` is 6,689 words by `wc -w` after F3 (worker close was 6,602), above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: eleven declared component records with dual `use`/`font`/`padding` forms, a nine-row surface state contract, a 42-row applicability matrix, Vapor UI vs marketing domain splits, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Vapor UI is a published first-party design system, so every derived-editorial close names that specification instead of copying the toss example's "not a separately published UI specification" (rulebook v12 B2a 전제 주석). Worker-close measure `grep -o 'published Vapor UI documentation' DESIGN.md \| wc -l` = 24; post-F3 measure is in the F1 rescan.
- The source names five Tier 1 URLs but supplies computed evidence for only two of them. The GitHub / TechBlog / design-blog URLs are therefore carried as named sources with no computed design value attached, in Scope, in the Font-evidence table, and in Named gaps.

## Mandatory passes

- **F1 (B2a scan).** `DESIGN.md` was re-read from line 1 and every causal, interpretive, or judgement sentence was classified against its evidence class. Edits that followed:
  1. Scope ¶1 (9) — the first draft cited sibling Button/Badge component pages as part of the capture. That is a sibling classification, not a source-body fact (B1). The clause was removed. The evidence-domain boundary (goorm.co ↛ Vapor UI token; three named sources as narrative) now carries an adjacent complete qualifier.
  2. Scope ¶2 / ¶3 (11/13) — atmosphere readings and the refusal/embrace / "design is the message" line already carried the complete form, including the Vapor UI close.
  3. Avoid (69) — a draft said "eight avoidances" while the list had nine bullets. The sentence now splits the source Don't list from the scope-boundary line.
  4. Official distributed-asset row (176) — a draft asserted "No goorm-exclusive distributed type family was verified." That is an absence claim the source does not make. It now says only that the source records no goorm-distributed type file.
  5. Motion (150) — a draft cited the sibling verification file as a second witness that motion was unobserved. Sibling is not the migration input; the sentence now rests on the source alone. Sibling motion absence stays at `provenance.md` 88.
  6. Applicability (242) — "none of them is goorm-authored" was normalized to "they are not goorm-authored" so the three-piece form is byte-complete on every qualified line.
  7. Content hero gloss (456) — "Start AX with goorm" is the source's own English gloss, not a replacement. It carries its own adjacent complete qualifier.
  8. Text Field `focus-visible` reason — a first gate run blocked on `focus-visible-promotion` because the reason cell still named `#2a72e5`. The hex stays on the input's Focus record (`DESIGN.md` 361) and on the evidence-boundary sentence (240); the table row now states only that the generic focus observation is a different evidence class. Value unchanged; B1 preserved. Not an E3 dodge — the gate asked for the treatment to leave that row, which is the rule.
  Measured after the worker scan: `derived editorial implementation inference` 24 = `not goorm-authored` 24 = `published Vapor UI documentation` 24. F3 added two in-place complete qualifiers (Font evidence Official product-use; Layout breakpoints) and widened two existing subjects (Scope ¶3 narrative-not-token; applicability decision procedure). Post-F3: `derived editorial implementation inference` 26 = `not goorm-authored` 26 = `published Vapor UI documentation` 26. Provenance derived ledger 26 rows (E1 1:1). `wc -w` DESIGN.md = 6,689.
- **F2 (E2 collation).** Every row above was grepped against the three files before it was written; dual destinations are recorded on both sides (E2a); the §15 curve row records both the deletion and its provenance ledger location (E2b). The only compliance claim made in this log is B3, and it is made because `DESIGN.md` 160 carries the five evidence kinds and the per-component gate in full text (E2c). The §13 deletion row names field kinds (names, ages, cities) and does not restate identifiers (D2a). No sentence in provenance asserts that those identifiers are absent from a file that lists them (E2d).

## Revision 2026-08-28 (wave28 review)

Confirmed defect only: A1 unique-fact loss of the source §11:390 clause `a WCAG-compliant React component library`. Orchestrator reconfirmed the absence; existence is not re-litigated.

- Restore: Experience Scope ¶3 `DESIGN.md` 13 (same line that already carried founding 2013, the product ecosystem, the refusal/embrace readings, and "the design is the message"). Source wording restored as a source-stated fact — `The source records that goorm recently open-sourced as **Vapor UI**, a WCAG-compliant React component library with a Leonardo-driven color system.` — not as a derived reading. No new B2a qualifier, no new provenance derived row. B2a remains **26 = 26**.
- Same-paragraph WCAG / Leonardo / open-source already survived; only the stack proper noun was missing. The GitHub HTML-comment gloss `open-source UI component library` (no `React`) stays at `provenance.md` 142 and is a different string.
- Not touched: token values, component tables, state rows, existing B2a sentences, provenance 1:1, `web/references/goorm/**`.
- Line count: `DESIGN.md` still **526** (in-place edit). F2 pointers re-read against the current file and still land: qualifier lines 9 · 11 · 13 · 19 · 28 · 32 · 45 · 56 · 69 · 87 · 121 · 133 · 146 · 150 · 173 · 178 · 185 · 210 · 214 · 222 · 242 · 418 · 432 · 434 · 456 · 468; B3 condition 160; marketing CTA L/E/S 341–344; hero gloss 456; `구름` 466; Named-gaps URLs 526. Provenance pointers 7–22 / 26 / 28–37 / 39 / 50–54 / 57–59 / 61–105 / 111–114 / 117 / 134–142 / 146–175 re-read; no dest moved.
- Word count: `wc -w` 6,689 → **6,709**.
- `node test-v2/tools/migrate-reference.mjs --brand goorm --gate-only` → `verdict: PASS`, `problems: []`. `inspectDesignMd` → `portable_core: true`, `level: portable-core`, `reasons: []`.
- SHA-256 `b6972343b2318df1ba518f41832ba0e79bd5d94aa9e48eb3052855007d8676b4`.

실측 (`find`로 산출 4파일·원본·sibling 존재 확인 후 `grep -o <패턴> <파일> | wc -l`, 파일별. `no matches found` 아님):

| 패턴 | 원본 | sibling | DESIGN.md | provenance.md | migration-log.md | audit-log.md |
|---|---:|---:|---:|---:|---:|---:|
| `React component library` | 1 | 0 | **1** | 0 | 5 | 0 |
| `React` | 1 | 0 | **1** | 0 | 7 | 0 |
| `WCAG-compliant React component library` | 1 | 0 | **1** | 0 | 4 | 0 |
| `open-sourced as` | 1 | 0 | **1** | 0 | 3 | 0 |

Review-time portable-body counts were DESIGN **0**. After this revision the portable body is **1** (line 13). The log's extra hits are the §11 dest row plus this revision section (E2d: this file is in its own denominator). Provenance 0 is correct: the clause is a source fact, not a derived inference. audit-log 0 is correct: F3 did not open this fact.
