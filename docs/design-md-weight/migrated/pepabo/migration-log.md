# pepabo migration log

Source: `web/references/pepabo/DESIGN.md`
Destination: `docs/design-md-weight/migrated/pepabo/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/pepabo/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with ripgrep against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use ripgrep match lists per file, never a remembered count.

Source SHA-256 `0e337b46eabcd3559c25589273bfbf27444fe9d6f0ec8fd5173c1ed06829ce0d` (`web/references/pepabo/DESIGN.md`). Sibling SHA-256 `539eddd373d397ec93c1aca674c6819e6311d56ea69b0562edb6d58605175cbe` (`web/references/pepabo/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# GMO Pepabo (Inhouse) Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–19. YAML homepage `https://pepabo.com/` is dual: DESIGN dest 2 at 9/9 + provenance dest 3 at 13/69/81 (E2a). `#1f7ccc` is dual: DESIGN dest 14 at 11/11/36/60/92/92/132/247/259/262/367/369/378/577 + provenance dest 10 at 14/23/46/47/47/96/162/186/186/194 (E2a). Favicon URL is dual: DESIGN dest 1 at 237 + provenance dest 1 at 15 (E2a). Display `GMO Pepabo (Inhouse)` DESIGN dest 1 at 1 + provenance dest 2 at 1/10. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance; token note 옮김 → Foundations Semantic color | Verification metadata is a value and is kept, not dropped (A1c): identity rows `provenance.md` 17–19, freshness table 27–33. The source footer's **Verified:** 2026-06-17 is at `provenance.md` 83. Exact `tokens.source` DESIGN dest 0 / provenance dest 2 at 17/191. `components_harvested` DESIGN dest 0 / provenance dest 2 at 19/190. Token note (pepper base / Pepper Blue 600 / live links / brand mint / component bg `#000000` / text `#393c41`) at `provenance.md` 23 and `DESIGN.md` 132. These ledger keys have no other portable slot. |
| YAML `tokens.colors` (22 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 86–132. All twenty-two roles kept with names, values, and token-set keys: primary `#1f7ccc`, primary-strong `#0a62ad`, primary-bright `#3e93de`, link `#005bac`, ink `#393c41`, ink-strong `#1f2124`, body `#585c63`, muted `#767b85`, faint `#9297a1`, hairline `#dee0e3`, surface `#edeef0`, surface-soft `#f7f8fa`, canvas `#ffffff`, on-primary `#ffffff`, brand-mint `#30f4c5`, brand-mint-deep `#0e7365`, positive `#1dc487`, notice `#debf43`, negative `#cc1f24`, negative-strong `#b50b11`, attention `#db7d42`, ink-black `#000000`. `tokens.colors.canvas` and `tokens.colors.on-primary` stay two keys that share `#ffffff` (`DESIGN.md` 110/112/84). Four blues stay unmerged (`DESIGN.md` 92–95). Pepper Gray 800 / 900 / 700 / 600 / 500 / 300 / 200 / 100 steps sit on the Neutral table. YAML token note kept as the facts it names at 132. |
| YAML `tokens.typography.family` (sans / cjk / kerning / mono) | 옮김 → Typography & Assets Family + Font evidence | Family `DESIGN.md` 208–214. `tokens.typography.family.sans` dest 1 at 208. Live computed stack dest at 200/212. YakuHanJP as kerning layer dest at 201/210. Declared fallback `apple-system` / `"system-ui"` / `"Lucida Grande"` dest at 204/212. |
| YAML `tokens.typography.xxxl` … `xxs` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 222–231. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.25` dest 2 at 224/233; `1.29` dest 2; `1.33` dest 4 at 226/230/233/233; `1.14` dest 2 at 227/233; `1.50` dest 2; `1.43` dest 2; `1.45` dest 2 at 231/233. §3 px writings stay beside them: 40px / 36px / 32px / 24px / 24px / 20px / 16px / 16px. All eight YAML `use` strings restored verbatim in the Token-set use column (A1, A3, wave 37 keep-both). Live H1 21px/700 dest at 200/227. Densities 28/24/20px dest 2 at 220/228. `tokens.typography.m` size 16 is disambiguated from `tokens.spacing.base: 16` at 138/218. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (3 steps) / `tokens.shadow` (3 keys) | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 136 (`xxs: 4` · `xs: 8` · `sm: 12` · `base: 16` · `md: 24` · `lg: 32` · `xl: 48` · `xxl: 64`). Shape: `DESIGN.md` 142 (`sm: 4` · `pill: 20` · `full: 9999`). `tokens.spacing.xxs: 4` is disambiguated from `tokens.rounded.sm: 4` at 138. `tokens.spacing.lg: 32` is disambiguated from XXXL 32px, pill height 32px, and Textfield Small height 32px at 138. `tokens.shadow.flat` `none` dest 1 at 154. `tokens.shadow.level1` dest 1 at 155 with the full `-0.1px` string dest 3. `tokens.shadow.level2` dest 1 at 156. `get-elevation-box-shadow($level)` dest 2 at 155/158. |
| YAML `tokens.components` (11 records) | 옮김 → Components & States | `DESIGN.md` 269–473. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×4 at 272/295/318/339 (YAML button-primary / button-outline / button-pill / button-small), `input` ×2 at 360/385, `card` ×1 at 407, `tab` ×1 at 448, `badge` ×2 at 425/437, `avatar` ×1 at 470. All eleven YAML `use` strings restored verbatim as `Token-set use:` lines. Hairline Card is §4-only: `Primitive type: not in the token set` dest 3 at 249/417/421 (wave 31). YAML `font` shorthands restored. YAML `active: "text #393c41 weight 700"` dest on Navigation. Filled radius `4px 4px 0px 0px` dest at 388. Textfield Large 48px / 18px / 10px 24px dest at 370. Heavier outlined 2px border dest at 303. Medium button 32px / 14px dest at 280. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–42. Two-tier tokens, Flavor names (Color, Sizing, Icon, Typography, Elevation), Pepper ramps 50 to 900, `get-primitive-color($name: blue, $level: 600)` → `#1f7ccc`, mission line "人類のアウトプットを増やす", 4px workhorse / 20px pill / full-round. Characterizations carry an adjacent complete qualifier at 9/11 (B2/B2a). |
| §1 공식 히스토리·DS URL | 분리 → provenance; URL 옮김 → Experience Scope | Tier 1 list `provenance.md` 77–81. Elevation Flavor URL from the closing comment at `provenance.md` 71. Evidence-class table `provenance.md` 53–59. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 86–132. YAML keys and §2 Pepper names (Pepper Blue 600 / Strong / Bright, Pepper Gray 800–100, intentions, pepabo Flavor mint) are both kept. Intention / keep-both readings covered by the adjacent qualifier at 84 (B2/B2a). |
| §3 Typography Rules | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 193–204; family 208–214; type-role table 222–231. **Sass functions, not named roles**, **16px baseline**, **Japanese-first kerning**, and `typography-first is a stated principle` dest at 220. The Font-evidence wrap, Family, and Type-roles readings carry adjacent complete qualifiers at 195, 214, and 218 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 269–473. §4 body values and YAML `use` / font / radius / padding / height / shadow / active are both kept. Hairline Card is labelled `not in the token set`. Footer **Verified** / Tier 1 / Tier 2 / Conflicts at legacy 260–263 are provenance (next row). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 260–263) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness table `provenance.md` 27–33; Verified paragraph 83; Tier 1 list 77–81; Tier 2 87–88. Conflicts unresolved: none — `provenance.md` 35. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 477–516: 4px base; scale 4/8/12/16/24/32/48/64; documentation sidebar + centered prose; corporate full-bleed black hero then white sections with mint; calm neutrality / grid-locked rhythm / density options. Radius scale restated on Foundations Shape, not re-invented here. Whitespace readings carry an adjacent qualifier at 481 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 150–158: Flat `none`; level1 with `-0.1px`; level2 4–6px float; `get-elevation-box-shadow($level)`; achromatic `rgba(0,0,0,0.12)`. The gentle-lift / barely-there / achromatic-by-design readings carry an adjacent qualifier at 158 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 58–65 (eight Do rules), under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 71–78, under the qualifier at 69. The eight prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 489–516: Mobile `<768px`; Tablet `768-1024px`; Desktop `>1024px`; 40px medium / 24px and 32px pointer-led; App Bar ~40–48px; sidebar drawer; type stays on the 4px grid; full-bleed hero; Icon flavor inline SVG; avatars 9999px; cards 4px across breakpoints. Stated as the source's own contract rather than as a measured cross-viewport specification; that reading carries an adjacent qualifier at 516 (B2/B2a). `measures 1440px` is not written (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing recreate-the-control prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 186. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 519–539: collegial / pragmatic / quietly principled; "a convenient constraint" (便利な制約); "人類のアウトプットを増やす" / "Increase humanity's output"; tone table including tech.pepabo.com and minne/SUZURI flavor-layer voice; three verbatim samples; forbidden register. The qualifier at 521 covers naming that register (B2/B2a). Locale-claim omission at 539 carries its own adjacent complete qualifier (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: 2003 Fukuoka founding; Tokyo HQ; GMO Internet Group; minne / SUZURI / lolipop! / ヘテムル / ムームードメイン / カラーミーショップ; duplicated-foundation problem; Inhouse as the answer; 2021 Pepabo Tech Portal "ペパボのデザインシステムのドキュメントを公開します"; Flavors; stated motivation to free designers from foundational busywork; **closing unit kept**: What Inhouse refuses … What it embraces … "environmental honesty". Each source paragraph's last sentence is kept as one unit. The connecting reading carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 195. `2003` DESIGN dest 1 at 13. `2021` DESIGN dest 1 at 13. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 48–52 under the B2a form at 46: paraphrase of the published Inhouse About documentation plus UI implications. Published Inhouse documentation is named in the close (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 144–170 (27 data rows). |
| §13 Personas — 3 slots | 삭제 | Fictional archetypes. Not promoted as individuals, tasks, or Audience groups (D2, D2a). Affiliation classification and motivation are not re-hosted. Portable Audience at `DESIGN.md` 28 uses only the source §13 header wording: "Pepabo product designers and engineers building across multiple service brands" (src 1 / dest 1). Disposition is unidentified: source §13, 3 slots (name, age, and city were present). Disposition at `provenance.md` 180. The three `primary-tasks` at 21–23 come from surfaces the source records; 19 says so and qualifies the step from surface to "primary task". |
| §14 States | 옮김 → Components & States capture record + Surface state contract + per-component applicability | Full source body preserved at `DESIGN.md` 253–267 (A2; the catalog graph is still 0/440, so nothing is delegated): Empty list / Empty search / Loading fetch / Loading in-place / Error request / Error form validation (`必須`) / Success saved / Skeleton / Disabled. The applicability rule is at 249, whose qualifier at 249 covers every Reason cell that follows. The Focus-vs-focus-visible classification at 247 and on the Textfield Observed Focus line at 367 each carry an adjacent complete qualifier (B2/B2a). Non-observation is never used as a `not-applicable` reason (C1). Generic Focus `#1f7ccc` on Textfield is not promoted onto a `focus-visible` row (B1); dest at 247/367/369/577. |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary / Outline / Pill / Small buttons keep loading/error/success applicable because they can commit (保存) and the surface contract declares those treatments (`DESIGN.md` 284–286 / 307–309 / 330–332 / 351–353) (C2 v10). Textfields keep error applicable as a form field (`DESIGN.md` 378 / 400). Navigation (`type: tab`) closes loading/error/success with a role reason — a Top App Bar / sidebar nav item selects a documentation destination and commits no operation in place (`DESIGN.md` 464–466). Elevated Card and Hairline Card get no `kind` and no map because the source supplies no interaction evidence — `DESIGN.md` 412 / 421 (C4). Badges and Avatar are `Kind: non-interactive`. `Primitive type` is attached only when the YAML token set records that type. |
| §15 Motion & Easing | 옮김 → Foundations motion; unattributed curves 삭제 | Durations `motion-fast` 120ms / `motion-standard` 200ms / `motion-slow` 320ms dest at 166–170, qualified at 162 as editorial defaults (source closing note: public docs expose elevation tokens but not a published duration/easing scale). Easing *roles* and uses dest at 174–177. Exact cubic-bezier values omitted (`cubic-bezier` DESIGN dest 0); they were unattributed editorial defaults, including template-class `ease-exit`. Motion rules dest at 184–187 including reduced-motion and no bounce/spring. B3 is held: the promotion condition at `DESIGN.md` 180 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The durations/roles/rules reading carries an adjacent complete B2a close at 162. |
| HTML comment philosophy layer (legacy 454–486) | 분리 → provenance; 본문에 이미 있는 값은 유지 | Capture notes `provenance.md` 130–138: `get-implication-color`; unpromoted ramp endpoints `f5faff`/`002647`/`f5fffb`/`003d27`/`fffafa`/`540003`; corporate `#1f7acc`; ×640. Interpretive claims named in the comment ("neutral so brands can overwrite it", "elevation stays achromatic by design") are qualified in portable Elevation/Scope, not promoted as Inhouse-authored doctrine. |
| Closing-comment motion disclaimer | 옮김 → Foundations Motion B2a | `DESIGN.md` 162 restates the source's own classing of §15 as editorial defaults. |

## Sibling handling (`web/references/pepabo/.verification.md`)

The sibling exists — confirmed with `find web/references/pepabo -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` under Sibling verification file and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: H1 `Pepabo Design`; title `Inhouse - Pepabo Design`; About URL `https://design.pepabo.com/inhouse/about/`; `$blue-600` / `$informative-color`; `get-image-overlay-color($state)`; full Pepper ramp lists; sub-nav `0px 16px` / 48px; hero region 64px; `No designs found for 'pepabo'`; Tech Portal dated URL `https://tech.pepabo.com/2021/04/30/introduction-of-inhouse/`. Recorded at `provenance.md` Sibling section.
- Measured `DESIGN.md` 0 for those sibling-only strings: `Pepabo Design` dest 0; `Inhouse - Pepabo Design` dest 0; `get-image-overlay-color` dest 0; hero-region `font-size: 64px` dest 0 (the portable `64px` hits are `tokens.spacing.xxl: 64`, not that sibling hero metric); `No designs found for 'pepabo'` dest 0.
- Live H1 21px/700, `#1f7ccc`, 保存, elevation strings, and "人類のアウトプットを増やす" are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. A hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 12 distinct | 0 | 0 | 人類のアウトプットを増やす / Increase humanity's output / 便利な制約 / 保存 / 無料診断中 / 必須 / Inhouse / Foundation / Flavors / Components / get-primitive-color($name, $level) / get-semantic-color($intention, $level) / ペパボのデザインシステムのドキュメントを公開します / GMOペパボ株式会社 / 約物. |
| YAML `use` strings | 8 type + 11 component | 0 | 0 | All eight type-role Token-set use lines and all eleven component Token-set use lines restore the YAML `use` byte-exact. §4 Brand Mint Use line restored beside the YAML string (wave 37 keep-both). |
| Audience / primary-task source wording | 1 audience phrase + nav/API/mission strings | 0 | 0 | Audience grep `Pepabo product designers and engineers building across multiple service brands` src 1 / dest 1. Primary tasks name Foundation / Inhouse / Components, the token API, and "人類のアウトプットを増やす". |
| Sibling published strings | 2 issued titles + 1 getdesign 404 line | 0 in the three-file set after provenance transcription | 0 missing from the three files | H1 `Pepabo Design` and title `Inhouse - Pepabo Design` live in provenance Sibling section, not in `DESIGN.md` (sibling-only; not promoted as portable voice samples). |

Sub-needle labels confirmed present individually in `DESIGN.md`: 人類のアウトプットを増やす, 便利な制約, 保存, 無料診断中, 必須, YakuHanJP, Noto Sans JP, Open Sans, ペパボのデザインシステムのドキュメントを公開します, ヘテムル, ムームードメイン, カラーミーショップ.

A5 분모: hand sweep of source published labels 12 extracted / 0 missing; YAML use 19 / 0; gate `copy-loss` compared 11 / candidates 234 (A5a mandatory because compared < candidates). `node test-v2/tools/latin-copy-audit.mjs --brand pepabo --candidate docs/design-md-weight/migrated/pepabo/DESIGN.md` lost 12 / candidates 41. Published copy among those 12: YAML `1px solid #585c63` (restored byte-exact on Outlined / Secondary). The rest are not issued copy: pepper-ramp hex lists that exist only as abbreviated endpoints in the source closing comment (ledger at `provenance.md` 134); playwright `goto`; the comment's example interpretive string `neutral so brands can overwrite it` (qualified in portable Elevation; named in this log's HTML-comment row and in provenance capture notes); high-confidence `Inhouse - Pepabo Design` / `Pepabo Design` (source body 0; sibling raw samples; transcribed in provenance, not promoted into `DESIGN.md`); Tier 2 `No designs found for 'pepabo'` (sibling exact 404 string; source footer says "no Pepabo entry"; provenance Sibling section).

## Unique-phrase contrast (wave 43)

Picked 43 source-unique expressions across (a) §11/§1 causal and year/name strings, (b) value modifiers (Pepper Blue 600, `-0.1px`, 50 to 900, intention names), (c) §15/§7 constraint sentences. First pass 0-count: `text() mixin` (false zero — source writes it in backticks; dest has `` `text()` mixin ``), `Sass functions, not named roles`, `Japanese-first kerning`, `typography-first is a stated principle`, and the §4-only "無料診断中" Use line. All restored. `near-black, never pure black` was a case variant; dest already had `Near-black, never pure black`. Closing-comment-only capture (`get-implication-color`, ramp endpoints, `#1f7acc`) stays in provenance, not the portable body.

Unique-phrase contrast: picked 43 / restored 5 (including keep-both 無料診断中).

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand pepabo --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 11, candidates: 234, detail: "인용 문자열 234개 중 11개만 비교했다 — 나머지는 라틴이라 바늘이 되지 않는다. 라틴 전수 대조는 손으로 하라." }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 11 < `candidates` 234.

First gate run blocked on `focus-visible-promotion` because the Textfield `focus-visible` reason cell still named `#1f7ccc`. That hex is the source's generic Focus observation (B1), kept on the component's Observed Focus line at `DESIGN.md` 367 and in the capture record at 247; it was removed from the `focus-visible` row so the row carries no treatment. After that close: gate PASS / `problems []`. First Portable Core fail is recorded under Deviations.

## Deviations recorded

- `DESIGN.md` is ~7,523 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: twenty-two color keys, eight type roles, eleven declared component records plus Hairline Card, a seven-state applicability matrix on interactive controls, YAML/§2 keep-both, YAML unitless and §3 px line-height keep-both, the full §11 narrative including the refuses/embraces closing unit, the §14 state table, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Published Inhouse documentation exists, so every derived-editorial close uses the adapted form `not Pepabo-authored or taken from a separately published UI specification, including the published Inhouse documentation` (rulebook v12 B2a 전제 주석). Measure: `derived editorial implementation inference` DESIGN = 27 complete-form lines at 9/11/13/19/28/32/46/56/69/84/138/148/158/162/195/214/218/237/247/249/367/421/481/516/521/539/573; `including the published Inhouse documentation` DESIGN = 28 because Motion `:180` names that documentation inside the B3 partial-confirmation sentence, which is the five-kind gate rather than a 28th B2a row. Provenance derived ledger 27 rows at 144–170 (E1 1:1).
- First Portable Core `missing-actionable-foundations-or-known-constraints` was the source sentence `No bounce or spring in the foundation` matching the foundations self-negation window (`No` … `foundation`). Wording at `DESIGN.md` 186 now reads `No bounce or spring in the pepper base` plus `The source states this as a foundation motion rule`, keeping the source meaning on two sentences so the checker does not treat the claim body as denying foundations (E3: the document was not hex-distorted; the adjacent noun was split). After that close: `portable_core: true`.
