# ferrari migration log

Source: `web/references/ferrari/DESIGN.md` (unmodified)
Destination: `docs/design-md-weight/migrated/ferrari/DESIGN.md`
Ledger: `docs/design-md-weight/migrated/ferrari/provenance.md`
Rulebook version used: **v10** (`docs/design-md-weight/MIGRATION_RULEBOOK.md`)
Date: 2026-08-26

Every row below was grep-checked against the actual output files before it was written (F2). Counts were taken with `grep -o … | wc -l`, so they count occurrences rather than lines, and each count states what was counted. Where a value legitimately lands in two places, both destinations are named.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML `id`, `country`, `category`, `homepage` | 분리 → 원장 (`id` / `country` / `category`) **및** 옮김 → DESIGN.md (`homepage`) | Identity table for all four; the portable file carries no frontmatter. The `homepage` value also reaches the portable body — `grep -o "https://www.ferrari.com" DESIGN.md | wc -l` = 1 occurrence, in the Experience Scope sentence that names the host the three observed surfaces sit on; it is the only URL in the portable file. `id`, `country` and `category` stay in the ledger only. |
| YAML `name: Ferrari` | 분리 → 원장 **및** 옮김 → DESIGN.md | Identity table row; DESIGN.md H1 `# Ferrari Design System` plus the Scope, Principles and Content prose. `grep -o "Ferrari" DESIGN.md | wc -l` = 54 occurrences, counting the bare brand name together with the substring inside `FerrariSans`, `Ferrari-SansRegular`, `Ferrari-SansMedium`, `Ferrari-red` and the `not Ferrari-authored` qualifier clauses. |
| YAML `primary_color: "#da291c"` | 분리 → 원장 **및** 옮김 → DESIGN.md | Identity table + claim ledger (3 occurrences in the ledger file); DESIGN.md Scope, Distinctive traits, Avoid, Foundations semantic color and the Subscribe CTA component (5 occurrences). |
| YAML `logo` (`type: simpleicons`, `slug: ferrari`) | 분리 → 원장 | Identity table only (`simpleicons` 2×, 0× in DESIGN.md). It is a third-party icon-set entry rather than a Ferrari-distributed asset file, so it is not promoted into the portable Assets subsection; that reasoning is recorded beside the row. The portable Assets subsection states only that this contract carries no image, icon, or logo file. |
| YAML `verified`, `omd: "0.1"`, `verification_v2.schema` / `.checked` | 분리 → 원장 | Freshness table for `verified`, `schema` and `checked`; `omd: "0.1"` sits in the Identity table as `omd format (source)`. The `2026-07-13` string these fields share with `verification_v2.surfaces[].inspected` does appear once in the portable body; that dual destination is recorded on the surfaces row below rather than counted twice here. |
| YAML `verification_v2.surfaces` (home / car-range / formula1, kinds and inspected dates) | 분리 → 원장 **및** 옮김 → DESIGN.md | Surfaces table (ids, kinds, URLs, dates) and Freshness (aggregate inspected row). The three-surface **boundary**, the `en-EN` locale and the observation date stay in the portable Experience Scope and Content & Locales, because standalone reading needs them — `grep -o "2026-07-13" DESIGN.md | wc -l` = 1 occurrence, in "three public Ferrari web surfaces observed on 2026-07-13". Only the surface ids and kinds and the per-surface URLs moved out. |
| YAML `verification_v2.sources` (3 product-surface + brand-asset + 4 official-doc/license) | 분리 → 원장 | Sources table and the narrative/licence context list. The font-asset and Legal URLs are recorded there; the portable §3 keeps their **substance** (`Ferrari-SansRegular` / `Ferrari-SansMedium` files, no downstream web-font licence), grep-confirmed 2× and 2× in DESIGN.md. |
| YAML `verification_v2.claims` (all anchors and methods) | 분리 → 원장 | Claim ledger, one row per token path, anchors `home` / `range` / `formula1` preserved. |
| YAML `verification_v2.conflicts: []` | 분리 → 원장 | Freshness ("Conflicts unresolved: none") and Proof notes. |
| YAML `tokens.source: reconciled`, `tokens.extracted: 2026-07-13`, `components_harvested: true` | 분리 → 원장 | Identity table for all three; `tokens.extracted` also has its own Freshness row, and `components_harvested` is repeated in Proof notes (`components_harvested` 2× in the ledger, 0× in DESIGN.md). The word `reconciled` also reaches DESIGN.md once, inside the token-note substance carried into Scope. |
| YAML `tokens.note` | 분리 → 원장 **및** 옮김 → DESIGN.md | Quoted verbatim in the Identity section; its substance is restated in DESIGN.md Scope ("Three supplied public Ferrari surfaces were reconciled", the observed selector/surface boundary, and the no-authenticated-UI / no-responsive / no-semantic-status / no-unobserved-variant limit). |
| YAML `tokens.colors` (primary, canvas, foreground, on-primary) | 옮김 → Foundations | §2 Semantic color. `#da291c` 5×, `#ffffff` 9×, `#181818` 3× in DESIGN.md. `canvas` and `on-primary` are kept as one role line because the source assigns the same `#ffffff` to both. |
| §4 `#000000` cookie text and border value | 옮김 → Foundations **및** Components & States | §2 names it as the consent-utility ink scoped to that control — explicitly not a general Ferrari ink role (A4) — and §4 carries it as the Cookie-consent action's own Text and `1px solid #000000` border. 3 occurrences in DESIGN.md. |
| YAML `tokens.typography.family` (`ui: Body-Font`, `chrome: FerrariSans`) | 옮김 → Typography & Assets | §3 Family, both keys with their observed role. `Body-Font` 13×, `FerrariSans` 11× in DESIGN.md. |
| YAML `tokens.typography.nav` (12 / 400 / **1.27** / tracking 1) + `use` | 옮김 → Typography & Assets **및** Components & States | §3 Type roles row `Header item | Body-Font | 12px | 400 | 15.24px (1.27) | 1` with the `use` string `Observed public header item` in the Use column, and the Header navigation item component. The unitless ratio 1.27 sits beside the computed px so the scale semantics survive (2 occurrences of `1.27`). |
| YAML `tokens.typography.primary-action` (16 / 400 / **1.15**) + `use` | 옮김 → Typography & Assets **및** Components & States | §3 Type roles row `Red Subscribe CTA | Body-Font | 16px | 400 | 18.4px (1.15)` with `Observed Ferrari-red Subscribe CTA` in the Use column, and the Subscribe CTA component font line. `1.15` 2×. |
| YAML `tokens.typography.consent-action` (13.008 / 600 / **1.2** / tracking 0.13008) + `use` | 옮김 → Typography & Assets **및** Components & States | §3 Type roles row `Cookie Manage action | FerrariSans | 13.008px | 600 | 15.6096px (1.2) | 0.13008` with `Observed OneTrust cookie-control action` in the Use column, and the Cookie-consent action component. `0.13008` 2× in DESIGN.md, 1× in the ledger. |
| §3 computed line heights 15.24px / 18.4px / 15.6096px | 옮김 → Typography & Assets **및** Components & States | §3 Type roles table and the three component font lines — 2 occurrences each in DESIGN.md. |
| YAML `tokens.spacing` (nav-y 5, consent-y 12, primary-inset 21) | 옮김 → Foundations **및** Components & States | §2 Spacing names all three as control insets and states they are three control measurements rather than a general scale; §4 carries them as `5px 0px`, `12px 10px` and `21px` padding on their own components (each 2× in DESIGN.md). |
| YAML `tokens.rounded` (sharp 0, consent 2) | 옮김 → Foundations **및** Components & States | §2 Shape, both keys with the third-party-utility boundary on the 2px step; §4 repeats each as the component's Radius. The two 0px measurements stay as measurements; summarising them as a sharp public geometry carries an adjacent derived-editorial-implementation-inference qualification in §2 Shape, matching the one the Distinctive-traits list puts on the `Sharp geometry` label. |
| YAML `tokens.components.racing-media-card` (type card, bg transparent, fg `#ffffff`, radius 0px, shadow none) | 옮김 → Components & States | §4 "Formula 1 media card". The verified primitive `type: card` is kept per component rather than collapsed into an interactive label (A1b). The source's own word "Static" is the reason for `Kind: non-interactive`, and no state-applicability map is asserted for it. |
| YAML `tokens.components.racing-media-card.use` (role text + `surface-3::div.Card__wrapper…` selector) | 옮김 → Components & States **및** 분리 → 원장 | The role text `Static Formula 1 media-card wrapper` is the component's Declared use line in DESIGN.md; the selector `Card__wrapper__2HwxoSe5.Card__wrapper--visible__1LdTLCPj.` sits in the capture-selector table only (0× in DESIGN.md, 1× in the ledger). |
| §1 Visual Theme & Atmosphere (prose) | 옮김 → Experience Scope | Maranello, the official-history framing, the observed black/white/transparent chrome, the red Subscribe fill, the 2px consent exception, and the four-domain capture boundary. The source author's readings ("express that heritage as image-led …", "recedes", "deliberate, low-chrome") carry an adjacent derived-editorial-implementation-inference qualification. |
| §1 four key-characteristic bullets | 옮김 → Experience Distinctive traits | All four bullets with their values. The bullet **labels** (Image-led contrast, Measured accent, Sharp geometry) are named as the derived part in the qualifying sentence directly under the list. |
| §1 Centro Stile / design-writing sentence | 옮김 → Experience Scope | Kept with the source's own "context, not a substitute for a web token" boundary restated as "they assign no interface value on their own". |
| §2 Color Palette & Roles (3 role bullets) | 옮김 → Foundations Semantic color | See the `tokens.colors` row. |
| §2 `BtnCta__button__w7eTRXBJ` class | 분리 → 원장 | Capture-selector table (1×, 0× in DESIGN.md). The fact the class carries — that one CTA appears on both the car-range and Formula 1 surfaces — stays in the portable body as the Subscribe CTA's "Observed" line and its `default` state reason. |
| §2 "does not establish error, success, warning, link-hover, yellow heritage, or a general dark-surface role" | 옮김 → Foundations **및** Governance Named gaps | Carried verbatim in §2 and named as an unresolved group in Named gaps (`yellow heritage` 2× in DESIGN.md). |
| §3 Font evidence boundary table (5 evidence classes) | 옮김 → Typography & Assets | §3 carries all five rows verbatim, including the official SF-24 product-use row, the 746 / 32 visible-use counts, the WOFF/WOFF2 corroboration, the declared-only faces, and the `Title-Font` alias plus licence boundary. `Body-Font-Medium`, `LF Maranello`, `Noe Display`, `Open Sans` and `Title-Font` are each present in DESIGN.md. |
| §3 role table (3 rows) | 옮김 → Typography & Assets | See the three typography rows above. The table's `Provenance` column held `home::[data-omd-capture=…]` selectors; those moved to the capture-selector table (8 `data-omd-capture` occurrences there, 0 in DESIGN.md) and the column was replaced by the frontmatter `use` strings. |
| §4 Subscribe CTA / Header navigation item / Cookie-consent action | 옮김 → Components & States | Three component subsections with anatomy, values and observed-default notes preserved. Selectors split to the ledger (row above). |
| §4 "No generic card, input, carousel, menu, notification, tab, hover, pressed, disabled, or error variant is published here" | 옮김 → Components & States State record | Carried verbatim. It does not conflict with the named Formula 1 media card: the source's sentence bounds **generic** variants, while the media card is the one card the frontmatter measured. |
| §4 header focus-snapshot note | 옮김 → Components & States | Kept twice: in the State record ("a focus snapshot is not evidence of a transition, menu, dialog flow, or unlisted variant") and as the Header navigation item's `focus-visible` row reason. No colour or treatment is attached to any `focus-visible` row — the source records a focus snapshot, a different evidence class (B1). |
| §5 Layout Principles | 옮김 → Layout & Platforms | `1440×900`, full-width public surfaces, light-on-image header controls, and the "does not establish a global grid, maximum width, breakpoint, carousel behavior, or a reusable card layout" limit, with the same limit named in Named gaps. The photography-vs-application-shell reading is qualified adjacently. |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | `boxShadow: none` verbatim, the flat-observation scope, and the "no overlay, modal elevation, or dark-card token" boundary, which is also named in Named gaps. The attribution of depth to transparency, contrast and fills carries an adjacent qualification. |
| §7 Do (4 items) | 옮김 → Experience Principles "Applied rules" | All four kept as brand application rules rather than folded into Governance boilerplate. The section's derived-editorial qualifier is worded to cover the four numbered principles **and** this list. |
| §7 Don't (4 items) | 옮김 → Experience Avoid | All four carried, plus the §3 system-font-substitution prohibition and the §1 surface-scope prohibition. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms **및** Governance Named gaps | The `1440×900`-only bound and the "mobile breakpoints, navigation collapse, touch-target policy, image crops, and reduced-motion behavior were not measured" sentence carried verbatim; the same five items are named in Named gaps. Component heights are restated as desktop measurements. |
| §9 Agent Prompt Guide — the construction prompt itself | 삭제 | A tool-directed prompt wrapper with no receiving Core slot. Its design values (`#da291c` background, white 16px `Body-Font` text, 21px inset, 0px radius, 57px CTA; transparent background, white 12px `Body-Font` text, 5px vertical padding, 1px tracking) are restatements of §1–§4 and were each grep-confirmed in DESIGN.md before this row was written (`1px tracking` 3×, `57px` 4×, `21px` 2×, `5px 0px` 2×). |
| §9 out-of-scope list (configurator, checkout, dashboard, alert, component-state system) | 옮김 → Experience Scope | This boundary exists only in §9 and Scope has a slot for it (A3), so it moved rather than being dropped with the prompt: `dashboard` 1×, `component-state system` 1×, `checkout` 2×, `alert` 2×, `configurator` 3× in DESIGN.md. |
| §10 Voice & Tone prose | 옮김 → Content & Locales | The corporate-language list carried verbatim; the "much terser" comparison and the concise-discovery-language conclusion drawn from it carry one adjacent derived-editorial-implementation-inference qualification; the "do not fabricate customer-service, error, or transactional voice rules" prohibition is kept and the same three are named in Named gaps. |
| §10 first-party wording “The power of passion becomes the beauty of achievement.” | 옮김 → Content & Locales **및** 분리 → 원장 | Carried byte-for-byte with the source's curly quotation marks and the "— Ferrari Corporate, About us" attribution (1× in DESIGN.md); the About-us URL behind the source's Markdown link moved to the ledger, where `grep -o "corporate/about-us" provenance.md | wc -l` = 2 — once in the Sources table and once in the narrative-context list. |
| Control names `Subscribe` and `Manage Cookies` | 옮김 → Content & Locales **및** Components & States | Both are carried exactly as written, capitalisation included: `Subscribe` 17× and `Manage Cookies` 4× in DESIGN.md. Content & Locales adds the source's boundary that the OneTrust string is third-party chrome rather than Ferrari product copy. |
| §11 Brand Narrative (2 paras) | 옮김 → Experience Scope | 1947, the 125 S, the Maranello factory gates, the History framing, the Prancing Horse description list, Centro Stile 2010, and "design and engineering, form and content". The source's "adds a current evolution" framing and its "should be read alongside" instruction are carried as a derived editorial inference with the qualification adjacent. The three narrative URLs moved to the ledger. |
| §12 Principles (4 + UI implications) | 옮김 → Experience Principles | All four with their UI implications, under an adjacent qualifier that also states they are not Ferrari-authored or a separately published UI specification (`derived editorial implementation inference` 8× across DESIGN.md, counted with `grep -o … | wc -l`). |
| §13 Personas | 옮김 → Experience Audience | The clients stakeholder group carried with the source's own limit (no demographics, jobs-to-be-done, purchase behavior, accessibility needs, or task flows). No biography is invented and nothing is rehosted as research. Not promoted into the `primary-tasks` claim — those three come from the measured surfaces instead. |
| §13 `[FILL IN: validated stakeholder research before adding user archetypes.]` | 삭제 (본문) → 원장에 보관 | Removed from the portable body (0× there, and the portable file emits no placeholder). The exact string is retained as an omission record in the ledger (1×), and Named gaps names the unresolved stakeholder research so the gap is visible without a placeholder. |
| §14 States prose | 옮김 → Components & States State record | Body preserved: `interactionCount: 0`, the default baselines and one header focus snapshot, and "empty, loading, success, failure, disabled, form validation, and skeleton states were not observed. No state treatment is invented here." The same seven state names are listed in Named gaps. |
| §14 → per-component applicability | 옮김 → Components & States | Each declared interactive component closes the seven canonical states by control role. The Subscribe CTA keeps `loading` / `error` / `success` **applicable** with treatments omitted, because subscribing commits an operation. The Header navigation item and the `Manage Cookies` control mark those three `not-applicable` with role reasons — destination selection and opening the cookie-management surface commit no operation of their own — never with a missing-observation reason (C1/C2). The Formula 1 media card has no interaction evidence, so no kind-based applicability map is asserted (C4). The body states "Absence of an observation is never a `not-applicable` reason" and "This is not a complete state-coverage claim"; the ledger's Proof notes carry the equivalent pair in its own wording. |
| §15 Motion & Easing | 옮김 → Foundations Motion **및** Governance Named gaps | The "no duration, easing, autoplay, or reduced-motion rule was measured" statement and the "do not infer a Ferrari motion system from editorial photography or the existence of racing content" prohibition are carried verbatim, and duration / easing / autoplay are named in Named gaps. The promotion condition added beside them names all five evidence kinds — transition properties, animation name, duration, easing, and reduced-motion behavior — plus the per-component computed-observation gate (B3); that full sentence is present in §2 Motion, which is why this row claims it. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → 원장 | Freshness, the Tier 1 and Tier 2 source lists, and the conflicts line. No tier vocabulary appears in the portable body (`getdesign.md` and `refero` 0× in DESIGN.md, 2× each in the ledger). |
| Sibling `web/references/ferrari/.verification.md` | 채택 → 원장 | Adopted as ledger evidence under "Sibling verification notes — adopted": raw computed samples, the coverage counters (5 component types, 25 variants, coverage score 80, `interactionKinds: 0`), the surface-kind caveat, the font/licence table, the Tier 2 records, the conflict matrix, and the reverted-legacy-claims list. Its distinct values are **not** promoted into portable tokens — notably the `13px` / `19.5px` / `rgb(0, 0, 0)` body sample and the `letter-spacing: 1px` measurement, which the source DESIGN.md never promotes; the portable type-role table keeps the source's unitless `1` and `0.13008` tracking instead. The non-promotion and its reason are stated in that ledger section (E2). |

### 웨이브 24 개정 (2026-08-26) — E1

- 개정 1줄: `provenance.md`에 **Portable derived-editorial scope** 절을 신설해 본문의 파생 편집 해석 8건을 행 단위(덮는 해석 / 본문 줄번호 / legacy 출처 / 한정 배치)로 열거하고, `grep -o … | wc -l` 파일별 실측으로 8행 ↔ 본문 8회의 1:1을 기록했다 — 개정 전 원장의 `derived` 언급은 "Reverted legacy claims" 한 줄뿐이라 파생 범위 원장이 아니었다. 본문·값·표·applicability·구조는 불변.
- 실측: `grep -o 'derived editorial implementation inference from the verified surfaces' DESIGN.md | wc -l` = 8; `not Ferrari-authored` = 8; `separately published UI specification` = 8; `grep -n` 줄 = 11 / 13 / 35 / 39 / 84 / 88 / 218 / 227 (8줄). 세 조각이 모두 8이므로 부분 한정은 없다. `provenance.md`의 같은 문구는 0회(원장은 범위를 기록할 뿐 본문 한정을 재수록하지 않는다), `migration-log.md`·`audit-log.md`의 등장은 각각 2회이며 배치를 보고하는 mention이지 use가 아니다.
- 재검증: `node test-v2/tools/migrate-reference.mjs --brand ferrari --gate-only` → `verdict: PASS`, `problems: []`. 저장소 루트에서 `evaluatePortableCore(DESIGN.md)` → `portable_core: true`, `reasons: []`, `level: portable-core`. `shasum -a 256 DESIGN.md` = `a9281a6410a4ad63d9cbeb7e1f7eb82b947d0ee1fccd340e54b2e2054a08a7be` — 이번 개정은 `provenance.md`와 이 로그만 건드렸다.
- 이 두 출력은 명령 결과일 뿐 적합성 증거가 아니다. E1은 게이트가 검사하지 않는 조항이고, 이번 결함도 게이트 PASS 상태에서 의미 검토가 잡았다 (E2c).
