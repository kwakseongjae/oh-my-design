# ClickHouse Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating ClickHouse as a column-oriented analytics database built for real-time SQL reporting, treating catalog homepage identity as `https://clickhouse.com` rather than an extra captured host beyond the three named public URLs below, and treating “speed as something users should see as well as measure” as this reconstruction’s reading of official brand writing rather than a published UI token, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. ClickHouse is a column-oriented analytics database built for real-time SQL reporting. Catalog homepage identity is `https://clickhouse.com`.

Treating the following three URLs as the named evidence domains of this reconstruction, including this-contract-covers-three-current-first-party-public-surfaces-from-the-2026-07-13-packet, values-stay-attached-to-the-public-surface-that-established-them, public-home-not-a-stand-in-for-an-authenticated-console, public-calculator-not-a-stand-in-for-uninspected-console-UI, editorial-story-not-a-stand-in-for-marketing-or-calculator-chrome, and privacy-dialog-kept-separate-from-product-and-calculator-components, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. This contract covers three current first-party public surfaces from the 2026-07-13 packet: marketing home `https://clickhouse.com/` (YAML `home`, marketing), public pricing calculator `https://clickhouse.com/pricing` (YAML `pricing`, public-calculator), and an editorial customer story `https://clickhouse.com/blog/corsearch-replaces-mysql-with-clickhouse-for-content-and-brand-protection` (YAML `story`, editorial). Color, type, and component values below stay attached to the surface that established them. A public-home value is not a stand-in for an authenticated console. A public-calculator value is not a stand-in for uninspected console UI. An editorial-story value is not a stand-in for marketing or calculator chrome. Privacy-dialog observations stay separate from product and calculator components.

Treating official ClickHouse Design, brand guidelines, and Click UI as official-doc context rather than a substitute for unmeasured public CSS values, including YAML-`ds.type`-system / `ds.name`-ClickHouse-Design / `ds.url`-https://clickhouse.design, official-brand-foundations-plus-the-separate-Click-UI-design-system-and-component-library, and Click-UI-documented-variants-not-copied-unless-the-supplied-public-capture-exposed-their-visual-values, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. YAML `ds.type: system`; `ds.name: ClickHouse Design`; `ds.url: https://clickhouse.design`. Official brand guidelines live at `https://clickhouse.design/brand`. Official Click UI lives at `https://clickhouse.design/click-ui`. The catalog description is official brand foundations plus the separate Click UI design system and component library. Click UI documents broader Button, IconButton, SplitButton, Text Field, and Select APIs separately; those documented variants are not copied here unless the supplied public capture exposed their visual values.

Source token note: Public marketing, pricing-calculator, editorial, Click UI documentation, and declared font assets remain separate evidence domains. Values below are selector-backed public-surface observations only. Treating that note as a register split — catalog `primary_color` `#faff69` as public action fill and selected/expanded border rather than a claim that all product UI is neon-on-black; canvas `#131312` unmerged from control surface `#282828` and from overlay `#141414`; on-action `#151515` unmerged from strong/modal `#ffffff`; hairline `#414141` unmerged from accent-border `#4f5101` and from cookie-dialog `#393939`; cookie-consent green `#166534` excluded from the action palette; Inter as the repeated public UI family rather than Basier headings or Inconsolata’s one sample; Söhne as official marketing-only context rather than a live UI family; and Click UI as component-intent guidance rather than unmeasured public CSS — is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification.

The official brand guidelines describe a direct, technically confident character: yellow leads the brand while dark neutrals, hard rules, and compact controls keep the message legible. ClickHouse’s separate Click UI documentation confirms a themeable product system whose yellow is intentionally used more selectively in product UI. The following atmosphere readings — yellow-led-public-action-`#faff69`-on-`#151515`, dark-public-surfaces-`#131312`-canvas-and-`#282828`-calculator-menu, compact-Inter-controls-and-Basier-headings-on-the-captured-public-routes, 4px-6px-8px-and-10px-radii-each-selector-backed-none-universal, product-marketing-editorial-privacy-dialog-and-Click-UI-documentation-evidence-kept-separate, high-contrast-treatment-belongs-to-public-communication-and-the-calculator-not-proof-of-an-authenticated-console, and official-brand-writing-provides-that-character-context-it-does-not-by-itself-supply-interface-tokens — are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification. The supplied public surfaces pair live `#faff69` action yellow with a near-black field of `#131312` / `#282828`, Inter for the repeated UI rhythm, and Basier for larger headings. That high-contrast treatment belongs to public communication and the calculator, not proof of an authenticated console. Official brand writing provides that character context; it does not by itself supply interface tokens.

Treating the following public-history facts as narrative rather than interface tokens — 2009-experiment-to-generate-analytical-reports-in-real-time-from-non-aggregated-data, 2012-production-launch, 2016-Apache-2-release, 2021-incorporation-of-ClickHouse-Inc, distributed-company-serving-data-work-that-must-stay-fast-as-it-grows, official-direct-minimalism-clarity-and-purpose-before-decoration, built-for-engineers-and-trusted-by-leaders, 2026-open-source-history-as-modular-well-documented-work-and-an-open-contribution-culture-rather-than-performance-theater, that-engineering-origin-as-useful-tension-between-bold-yellow-led-public-identity-and-selective-product-yellow, and official-history-brand-guidelines-and-2026-open-source-account-provide-that-narrative-context-not-interface-tokens — is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. ClickHouse began in 2009 as an experiment to generate analytical reports in real time from non-aggregated data. The company’s official history records a 2012 production launch, the 2016 Apache 2 release, and the 2021 incorporation of ClickHouse, Inc.; its current story also frames ClickHouse as a distributed company serving data work that must stay fast as it grows. The official brand guidelines call this “direct minimalism”—clarity and purpose before decoration—and describe the company as built for engineers and trusted by leaders. The 2026 open-source history reinforces the operating principle behind the tone: modular, well-documented work and an open contribution culture rather than performance theater. Official history, official brand guidelines, and the 2026 open-source account provide that narrative context; they do not by themselves supply interface tokens.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three captured public URLs as independently verified in this packet, treating the named public-home / pricing-calculator / editorial-story outcomes as harvested from those URLs and from YAML / §4 Use strings rather than from §13 — including review-the-public-marketing-home, use-the-public-pricing-calculator, and read-the-captured-public-customer-story — not lifting §13 developer / data-team groups into this list, and treating named public-action / number-field / list-item uses as harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Independently verified in this packet are the three captured public URLs; named public-action, number-field, and list-item uses are harvested strings/controls, not independently verified destination routes.

- Review the public marketing home at `https://clickhouse.com/` (YAML `home`, marketing).
- Use the public pricing calculator at `https://clickhouse.com/pricing` (YAML `pricing`, public-calculator).
- Read the captured public customer story at `https://clickhouse.com/blog/corsearch-replaces-mysql-with-clickhouse-for-content-and-brand-protection` (YAML `story`, editorial).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 first-party material as identifying developers and data teams as the intended audience rather than verified biographical personas, treating those groups as not Audience biographies and not primary tasks, and treating this-is-an-audience-boundary-not-a-substitute-for-ClickHouse-user-research, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. No individual personas are promoted. The reviewed first-party material identifies developers and data teams as the intended audience, but it does not provide validated demographic or task-research personas suitable for named archetypes. Do not invent them. This is an audience boundary, not a substitute for ClickHouse user research.

### Distinctive traits

The following unmerged-role extras — treating catalog `primary_color` `#faff69` as public action fill and selected/expanded border rather than cookie-consent green `#166534` and rather than a neon-on-black product system, treating canvas `#131312` as unmerged from control surface `#282828` and from overlay `#141414` and from toggle `#1f1f1c`, treating on-action `#151515` as unmerged from strong/modal `#ffffff`, treating hairline `#414141` as unmerged from accent-border `#4f5101` and from cookie-dialog `#393939`, treating YAML display `36` / `1.25` as unmerged from body h2 `36px` / `45px` and from body h1 `48px` / `60px` and from the `96px` sample, treating YAML control `14` / `500` / `1.43` as unmerged from number-field `14px` / `400` and from control body `20px` or `17.5px`, treating YAML radii sm 4 / md 6 / lg 8 / dialog 10 / full 9999 as unmerged from list-item `0` and from select-menu `0px 0px 6px 6px`, treating home primary padding `10px 16px` as unmerged from pricing primary `12px 24px`, treating public-navigation focus/hover/pressed as additional named observed states rather than a global rule or a `focus-visible` treatment, treating compact-Inter-controls-and-Basier-headings-as-the-captured-public-route-pairing, treating 4px-6px-8px-and-10px-radii-as-each-having-selector-backed-uses-with-none-universal, and treating product-marketing-editorial-privacy-dialog-and-Click-UI-documentation-evidence-as-separate — are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification.

- Yellow-led public action treatment: `#faff69` on `#151515`
- Dark public surfaces: `#131312` canvas and `#282828` calculator/menu surfaces
- Compact Inter controls; Basier headings on the captured public routes
- 4px, 6px, 8px, and 10px radii each have selector-backed uses; none is universal
- Product, marketing, editorial, privacy-dialog, and Click UI documentation evidence are separate

### Principles

These 5 items, including each stem’s packaging in this reconstruction — Prove-speed-rather-than-decorate-it, Function-over-frills, Be-technical-not-generic, Keep-brand-and-product-roles-distinct, and Design-for-an-open-engineering-community — and each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification. The numbered stems quote official ClickHouse voice, brand-value, color, and open-source writing; they are not a separately published UI specification.

1. **Prove speed rather than decorate it.** The official voice guidance asks for numbers, benchmarks, and examples. *UI implication:* pair a performance claim with a concrete result or source.
2. **Function over frills.** ClickHouse’s brand values favor what works over what trends. *UI implication:* keep controls compact and purposeful; avoid decorative component states not established by the surface.
3. **Be technical, not generic.** The guidelines ask teams to speak to builders with precise language. *UI implication:* use direct action labels and explain technical consequences plainly.
4. **Keep brand and product roles distinct.** Official color guidance says yellow is primary for brand and selective for product. *UI implication:* do not blanket an application in yellow just because a public campaign uses it prominently.
5. **Design for an open engineering community.** The official open-source history emphasizes code, documentation, review, and contributors. *UI implication:* expose meaningful technical context and avoid claims the interface cannot substantiate.

The UI implications are this reference’s interpretation of official positioning, not published ClickHouse product principles. Treating those UI-implication tails as a derived editorial implementation inference rather than a separately published ClickHouse UI specification, including pair-a-performance-claim-with-a-concrete-result-or-source, keep-controls-compact-and-purposeful, use-direct-action-labels-and-explain-technical-consequences-plainly, do-not-blanket-an-application-in-yellow, and expose-meaningful-technical-context, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do’s, including yellow-as-a-decisive-public-action-or-attention-signal-while-keeping-it-selective-on-product-oriented-UI, write-direct-technically-specific-labels-and-support-performance-claims-with-a-number-benchmark-or-concrete-example, preserve-the-captured-selector-surface-boundary, use-Inter-for-the-observed-public-UI-patterns-and-reserve-Söhne-for-its-official-marketing-only-role, and treat-Click-UI-system-documentation-as-component-intent-guidance-not-a-substitute-for-unmeasured-public-CSS-values, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification.

- Use yellow as a decisive public action or attention signal, while keeping it selective on product-oriented UI.
- Write direct, technically specific labels and support performance claims with a number, benchmark, or concrete example.
- Preserve the captured selector/surface boundary when using primary actions, calculator fields, or expanded menus.
- Use Inter for the observed public UI patterns; reserve Söhne for its official marketing-only role.
- Treat Click UI’s system documentation as component-intent guidance, not a substitute for unmeasured public CSS values.

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including no-cookie-consent-green-or-its-hover-or-privacy-dialog-layout-in-the-ClickHouse-product-palette, no-Söhne-fontSohneBreit-fallback-face-or-system-stack-as-a-verified-live-product-family, no-yellow-public-marketing-treatment-as-a-claim-that-all-product-UI-is-neon-on-black, no-invented-calculator-error-loading-disabled-success-mobile-or-authenticated-console-states, no-copying-Click-UI-API-variants-as-if-observed-on-the-three-supplied-public-routes, pair-the-pricing-primary-action-only-with-the-selector-backed-`#282828`-number-field, and do-not-invent-product-console-status-error-or-mobile-behavior. They are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification.

- Do not promote cookie-consent green `#166534`, its hover state, or privacy-dialog layout into the ClickHouse product palette.
- Do not present Söhne, `fontSohneBreit`, a fallback face, or a system stack as a verified live product family.
- Do not convert the yellow public marketing treatment into a claim that all product UI is neon-on-black.
- Do not invent calculator error, loading, disabled, success, mobile, or authenticated-console states.
- Do not copy Click UI API variants as if they were observed on the three supplied public routes.
- Do not invent product-console status, error, or mobile behavior; pair a public pricing-calculator action only with the selector-backed `#282828` number field.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#faff69`-as-public-action-fill-and-selected-expanded-border-not-cookie-green-`#166534`-not-a-neon-on-black-product-system, canvas-`#131312`-not-control-surface-`#282828`-not-overlay-`#141414`-not-toggle-`#1f1f1c`, on-action-`#151515`-not-strong-modal-`#ffffff`, hairline-`#414141`-not-accent-border-`#4f5101`-not-cookie-dialog-`#393939`, overlay-as-cookie-dialog-surface-only-not-a-general-product-surface-token, official-yellow-primary-in-brand-and-selective-in-product-not-undocumented-scale-steps-as-hex, cookie-consent-green-intentionally-excluded, and YAML-component-fields-staying-on-those-controls — are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification. Catalog `primary_color` `#faff69` is YAML `primary` and the public action fill; it is also the selected/expanded border on home and the pricing calculator. It is not cookie-consent green `#166534` and not a neon-on-black product system. YAML `canvas` `#131312` is the observed public page background; do not generalize it to every ClickHouse product surface. YAML `surface` `#282828` is the public pricing input and expanded select-menu background. YAML `overlay` `#141414` is the observed cookie-dialog surface only; not promoted as a general product surface token. YAML `foreground` `#dfdfdf` is computed neutral text on repeated public controls and lists. YAML `foreground-strong` `#ffffff` is observed in public secondary actions and the cookie dialog. YAML `hairline` `#414141` is the pricing number-field border. YAML `accent-border` `#4f5101` is the public secondary-action border. YAML `on-primary` `#151515` is primary-action text. The official brand guidelines say yellow is primary in brand work and a more selective accent in product work; they name neutral dark-theme chrome and slate light-theme chrome, but this packet does not turn undocumented scale steps into hex tokens. Cookie-consent green (`#166534`) and its hover value were observed in a privacy dialog, so they are intentionally excluded from the ClickHouse action palette.

- **Action Yellow / catalog primary** (`#faff69`): YAML `primary`. Primary public action background and selected/expanded border on the home and pricing calculator.
- **Canvas** (`#131312`): YAML `canvas`. Observed public page background; do not generalize it to every ClickHouse product surface.
- **Control Surface** (`#282828`): YAML `surface`. Public pricing input and expanded select-menu background.
- **Overlay Surface** (`#141414`): YAML `overlay`. Observed cookie-dialog surface only; not a general product surface token.
- **Primary Public Text** (`#dfdfdf`): YAML `foreground`. Computed neutral text on repeated public controls and lists.
- **Strong/Modal Text** (`#ffffff`): YAML `foreground-strong`. Observed in public secondary actions and the cookie dialog.
- **Hairline** (`#414141`): YAML `hairline`. Pricing number-field border.
- **Accent Border** (`#4f5101`): YAML `accent-border`. Public secondary-action border.
- **On Action Yellow** (`#151515`): YAML `on-primary`. Primary-action text.

### Spacing

YAML `spacing`: xs 4, sm 6, md 8, control 10, lg 12, xl 16, xxl 24, section 32. Treating those YAML numbers as recorded without a required px suffix, including none-is-added-here, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Those YAML numbers are recorded without a px suffix; none is added here.

Treating those YAML numbers as recorded without a required px suffix, treating body 4 / 6 / 8 / 10 / 12 / 16 / 20 / 24 / 32px as body-recorded observations rather than a converted YAML scale, treating body `20px` as a layout observation rather than a YAML spacing step, treating those 1440×900 repeats as supporting a compact-public-control-rhythm-but-not-a-complete-grid-or-responsive-layout-specification, and treating harvested control padding as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. The supplied 1440×900 captures repeat 4, 6, 8, 10, 12, 16, 20, 24, and 32px spacing values. They support a compact public-control rhythm, but do not establish a complete grid or responsive layout specification. YAML has no `20` spacing step; `20px` is a body layout observation.

### Shape

YAML `rounded`: sm 4, md 6, lg 8, dialog 10, full 9999.

The following local-geometry reading, including sm-4-primary-secondary-number-field-select-trigger, md-6-as-YAML-not-select-menu-lower-corners, lg-8-public-navigation, dialog-10-cookie-dialog, full-9999-radio-like-toggle, list-item-0-not-sm-4, select-menu-0px-0px-6px-6px-not-YAML-md-6-as-a-second-paint, and none-is-universal, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification.

- Small (YAML `sm` 4): public primary action, secondary action, number field, and select trigger at 4px
- Medium (YAML `md` 6): YAML radius step; not the select-menu `0px 0px 6px 6px` local geometry as a second paint
- Large (YAML `lg` 8): public navigation item at 8px
- Dialog (YAML `dialog` 10): cookie dialog at 10px
- Full (YAML `full` 9999): radio-like toggle at `9999px`
- Public list item: YAML `radius` 0 / `0px` — not YAML `sm` 4
- Pricing select menu (expanded): `0px 0px 6px 6px` — body-only local geometry

Treating 4 / 6 / 8 / 10 / 9999 / 0 / `0px 0px 6px 6px` as local harvested geometry rather than a universal radius for every unlisted control, and treating 4px, 6px, 8px, and 10px radii as each having selector-backed uses with none universal, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification.

### Elevation

The following elevation readings, including most-observed-public-controls-are-flat, hierarchy-from-dark-surface-changes-thin-rules-and-yellow-action-border, expanded-pricing-select-as-the-recorded-exception, and cookie-dialog-and-public-menus-not-evidence-for-a-universal-application-overlay-scale, are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification. Most observed public controls report `box-shadow: none`; hierarchy comes from dark-surface changes, thin rules, and the yellow action/border. The expanded pricing select is the recorded exception: it includes `rgba(0,0,0,0.1) 0px 10px 15px -3px` and `rgba(0,0,0,0.1) 0px 4px 6px -4px`. The cookie dialog and public menus are not evidence for a universal application-overlay scale.

### Motion

The supplied evidence records interaction outcomes (public navigation focus/hover/pressed, pricing-menu expansion, toggle state labels, and dialogs) but no duration, easing, transition-property, or reduced-motion measurement. Treating that source-stated absence, including source-stated-absence, no-duration-easing-transition-property-or-reduced-motion-measurement, and do-not-infer-a-ClickHouse-motion-scale-from-static-public-states-or-Click-UI-documentation, as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. No motion token is promoted. Do not infer a ClickHouse motion scale from static public states or Click UI documentation.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including official-product-use-Inter-as-title-and-body-in-the-product, live-Inter-1215-Basier-66-Inconsolata-one-18px-400-sample, official-distributed-Söhne-as-marketing-only-not-a-product-UI-token, declared-only-fontSohne-fontSohneBreit-fallback-faces-and-basier-Fallback, system-ui-sans-serif-not-a-ClickHouse-family-substitute, and ui-monospace-one-unresolved-computed-occurrence-not-promoted, are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | ClickHouse Design says **Inter** can serve as both title and body copy in the product. |
| Live computed surface-use | **Inter** is loaded/high with 1,215 observed uses across the three public routes; **Basier** is loaded/high with 66 uses, including 36px/600 h2 and 20px/600 h3 samples; **Inconsolata** is loaded/medium with one 18px/400 sample. Each has a FontFaceSet/source-URL match in the supplied artifact. YAML `family.ui` `Inter`, YAML `family.display` `Basier`, YAML `family.mono` `Inconsolata`. |
| Official distributed brand asset | ClickHouse names **Söhne** as its display voice for marketing and calls it marketing-only; the Klim Type Foundry source identifies WOFF2 web formats and commercial availability. This is brand/font context, not a product-UI token. |
| Declared-only | `fontSohne`, `fontSohneBreit`, their fallback faces, and `basier Fallback` are declared in captured CSS but had no visible computed use in this packet. |
| System / unresolved | `ui-sans-serif` is an operating-system stack, not a ClickHouse-family substitute. `ui-monospace` had one unresolved computed occurrence and is not promoted. |

### Family

- **Current visible UI family:** `Inter` — YAML `family.ui`
- **Current visible display family:** `Basier` — YAML `family.display`
- **Current visible mono sample:** `Inconsolata` — YAML `family.mono`; one observed sample only
- **Official marketing-only (not live in this packet):** Söhne

The following font-use boundary, including do-not-render-a-system-fallback-as-Söhne-Basier-or-Inconsolata, Söhne-useful-official-marketing-font-context-excluded-from-tokens.typography.family-because-no-visible-computed-Söhne-use, Inter-not-Basier-not-Inconsolata, declared-only-fontSohne-fontSohneBreit-basier-Fallback-not-loaded-ClickHouse-faces, and ui-sans-serif-not-a-ClickHouse-family-substitute, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Do not render a system fallback as Söhne, Basier, or Inconsolata. Söhne remains useful official marketing-font context but is excluded from `tokens.typography.family` because the packet records no visible computed Söhne use. Do not present Inter as Basier or as Inconsolata. Declared-only fonts must not be presented as loaded ClickHouse faces.

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1.25`, `1.50`, and `1.43`. The following ratio-versus-size-local reading, including unitless-ratios-not-fixed-px, YAML-display-36-1.25-not-body-h2-45px-not-body-h1-48px-60px-not-96px-sample, YAML-heading-20-1.25-not-body-h3-25px, YAML-body-16-1.50-not-body-24px, YAML-control-14-500-1.43-not-body-20px-or-17.5px-not-number-field-14px-400, YAML-code-18-1.50-not-body-27px, public-h1-as-body-table-only, preserve-route-value-for-control-20px-or-17.5px, and yaml-has-no-lineHeight-for-h1-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. Public h1 is a body-table role; YAML has no `lineHeight` field for it, and none is invented as a ratio. Control body observations are `20px` or `17.5px`; preserve the route value.

| Role | Font | Size | Weight | Line height (YAML) | Body observation | Use (YAML) / Notes |
|---|---|---:|---:|---:|---|---|
| Public h1 | Basier | 48px (also one 96px sample) | 600 | — | 60px (96px sample: 96px) | public routes; not a complete display scale (body table only) |
| Public h2 | Basier | YAML `36` / body 36px | 600 | 1.25 | 45px | YAML use: `Basier h2 observed on public routes`. 18 observed samples |
| Public h3 | Basier | YAML `20` / body 20px | 600 | 1.25 | 25px | YAML use: `Basier h3 observed on public routes`. 20 observed samples |
| Body / list | Inter | YAML `16` / body 16px | 400 | 1.50 | 24px | YAML use: `Inter public-route body and list items` |
| Control | Inter | YAML `14` / body 14px | 500 | 1.43 | 20px or 17.5px | YAML use: `Inter primary-action controls`; preserve route value |
| Code-adjacent body | Inconsolata | YAML `18` / body 18px | 400 | 1.50 | 27px | YAML use: `Single observed Inconsolata body sample; not a general code-scale claim` |

### Assets

Treating catalog logo metadata as type `simpleicons`, slug `clickhouse`, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, including public-primary-action-default, public-navigation-item-focus-hover-pressed-on-home-`data-omd-capture="4"`-raw-style-retained-in-verification-notes-because-no-one-global-state-color-was-established, pricing-select-expanded-menu-open, pricing-menu-option-selected-no-unobserved-selected-fill-claimed, pricing-toggle-checked-unchecked-no-different-checked-visual-value-asserted, cookie-dialog-open-as-separate-consent-chrome, and loading-error-empty-disabled-success-skeleton-not-captured-on-these-public-routes-intentionally-unspecified, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted:

| State | Evidence boundary |
|---|---|
| Public primary action — default | Home/pricing actions: `#faff69`, `#151515`, 4px radius; exact padding varies by selector. |
| Public navigation item — focus/hover/pressed | Home `data-omd-capture="4"` recorded all three states; the raw style is retained in verification notes because no one global state color was established. |
| Pricing select — expanded/menu-open | Three captured menus: `#282828`, `#dfdfdf`, lower 6px corners, yellow lower/side border. |
| Pricing menu option — selected | Collector recorded selected targets within the expanded menu; no unobserved selected fill is claimed. |
| Pricing toggle — checked/unchecked | Both state labels were captured on one radio-like control; no different checked visual value is asserted. |
| Cookie dialog — open | Separate consent dialog: `#141414`, `#ffffff`, 1px `#393939`, 10px radius. |
| Loading, error, empty, disabled, success, skeleton | Not captured on these public routes; intentionally unspecified. |

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating recorded navigation focus/hover/pressed as additional named observed states rather than a `focus-visible` treatment or a global rule, treating expanded/menu-open, selected, checked/unchecked, and cookie-dialog open as additional named observed states rather than Core applicability rows, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. Public-navigation focus/hover/pressed is not `focus-visible` evidence and is not copied onto that Core row. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating Public List Item YAML `type: listItem` with no interactive-kind confirmation so kind and a state-applicability map are omitted, treating Pricing Select Menu and Cookie Dialog as having no YAML type and no interactive-kind confirmation so kind and a state-applicability map are omitted with Type uninvented, and treating primary-action-secondary-action-navigation-number-field-select-trigger-and-toggle YAML-type-as-absent-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Public List Item has YAML `type: listItem` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Pricing Select Menu and Cookie Dialog have no YAML `type` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted; Type is not invented. Primary Action, Secondary Action, Navigation Item, Number Field, Select Trigger, and Radio-like Toggle have no YAML `type`; none is invented.

### Public Primary Action

- Role: public action control on the home and pricing calculator
- Kind: interactive
- Anatomy: label
- Background: `#faff69`
- Text: `#151515`
- Border: `1px solid #faff69`
- Radius: `4px`
- Padding: `10px 16px` on `home::[data-omd-capture="55"]`; `12px 24px` on pricing `surface-2::[data-omd-capture="102"]`
- Font: `14px / 500 / Inter`
- Use: Public action controls captured on the home and pricing calculator.
- YAML control use: `Inter primary-action controls`
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-as-catalog-`primary_color`-not-cookie-green-`#166534`, on-fill-`#151515`-not-strong-modal-`#ffffff`, 1px-matching-yellow-border-not-hairline-`#414141`-not-accent-border-`#4f5101`, 4px-not-nav-8px-not-dialog-10px-not-toggle-9999px, home-`10px 16px`-not-pricing-`12px 24px`, and 14px-500-not-number-field-14px-400, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. `#faff69` is this control’s fill and catalog `primary_color`. `#151515` is this control’s on-fill label. Radius 4px is YAML `sm`; it is not nav 8px or dialog 10px. Home padding `10px 16px` and pricing padding `12px 24px` stay unmerged.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public primary action on home and the pricing calculator |
| hover | applicable | Pointer-web public action; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A public action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a public action on home and the pricing calculator; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Public Secondary Action

- Role: route-local secondary action on the pricing calculator
- Kind: interactive
- Anatomy: label
- Background: `oklab(0.1957 -0.0000019595 0.00000458956 / 0.00999999)`
- Text: `#ffffff`
- Border: `1px solid #4f5101`
- Radius: `4px`
- Padding: `12px 24px`
- Font: `14px / 500 / Inter`
- Use: `surface-2::[data-omd-capture="103"]`; route-local secondary action.
- Observed: default only
- Field note: The following unmerged-field reading, including oklab-fill-not-canvas-`#131312`-not-control-surface-`#282828`, on-fill-white-not-on-action-`#151515`-as-a-second-paint, 1px-`#4f5101`-not-hairline-`#414141`-not-yellow-`#faff69`, 4px-matching-primary-not-nav-8px, and 12px-24px-matching-pricing-primary-padding-not-home-10px-16px, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. The oklab fill is this control’s field. `#ffffff` is this control’s on-fill label (Strong/Modal Text), not On Action Yellow `#151515`. Border `1px solid #4f5101` is Accent Border, not hairline `#414141`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the route-local pricing secondary action |
| hover | applicable | Pointer-web public action; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a route-local secondary action; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Public Navigation Item

- Role: public navigation/menu item on the public home
- Kind: interactive
- Anatomy: label
- Radius: `8px`
- Padding: `10px 12px`
- Font: `14px / 500 / Inter`
- Use: `home::[data-omd-capture="1"]`; public navigation/menu item.
- Observed: default; collector recorded a public home focus/hover/pressed state on `data-omd-capture="4"`; raw state styling is retained in the verification record rather than promoted as a global rule
- Field note: The following unmerged-field reading, including 8px-not-primary-4px-not-dialog-10px, 10px-12px-not-primary-10px-16px-not-pricing-12px-24px, 14px-500-not-body-16px-400, capture-1-Use-unmerged-from-capture-4-state-record, and focus-hover-pressed-as-additional-named-observed-states-not-Core-rows-not-focus-visible, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Radius 8px is YAML `lg` / this nav item. Use pointer `home::[data-omd-capture="1"]` and §14 state pointer `data-omd-capture="4"` stay unmerged. Focus/hover/pressed are additional named observed states; they are not among the seven Core applicability rows and are not copied onto a `focus-visible` row.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public home navigation/menu item |
| hover | applicable | Pointer-web nav item; additional named observed state exists; no global hover hex is promoted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A public nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Nav meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | Nav meaning is destination, not action-outcome confirmation |

Treating public-home focus/hover/pressed as additional named observed states rather than Core applicability rows or a `focus-visible` treatment, and treating no-one-global-state-color-as-established, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Additional observed named states: focus, hover, and pressed on the public home chrome. No one global state color is promoted. Focus is not a `focus-visible` treatment.

### Pricing Number Field

- Role: public pricing-calculator number input
- Kind: interactive
- Anatomy: value field
- Background: `#282828`
- Text: `#dfdfdf`
- Border: `1px solid #414141`
- Radius: `4px`
- Padding: `0px 12px`
- Font: `14px / 400 / Inter`
- Use: `surface-2::[data-omd-capture="93"]`; public pricing-calculator number input.
- Observed: default only
- Field note: The following unmerged-field reading, including `#282828`-as-this-field-fill-not-canvas-`#131312`, `#dfdfdf`-as-this-field-text-not-on-action-`#151515`, 1px-`#414141`-not-accent-`#4f5101`-not-yellow-`#faff69`, 4px-not-select-menu-lower-6px, 0px-12px-not-select-trigger-0px-28px-0px-12px, and 14px-400-not-YAML-control-14px-500, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. `#282828` is this field’s fill (Control Surface). Font 14px / 400 is this field, not YAML control 14 / 500.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public pricing-calculator number input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A number field can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted |

Loading and success applicability are omitted. Source names this control as a public pricing-calculator number input; exact request/outcome mapping for loading and success is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

### Pricing Select Trigger

- Role: trigger for an observed open pricing select
- Kind: interactive
- Anatomy: trigger
- Background: `#282828`
- Text: `#dfdfdf`
- Border: `1px solid #414141`
- Radius: `4px`
- Padding: `0px 28px 0px 12px`
- Font: `14px / 400 / Inter`
- Use: `surface-2::[data-omd-capture="63"]`; trigger for an observed open pricing select.
- Observed: default as the expanded trigger
- Field note: The following unmerged-field reading, including `#282828`-not-canvas, 1px-`#414141`-not-expanded-menu-yellow-border, 4px-not-menu-0px-0px-6px-6px, 0px-28px-0px-12px-not-number-field-0px-12px, and 14px-400-not-YAML-control-500, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Padding `0px 28px 0px 12px` is this trigger. The expanded-menu yellow border stays on the Select Menu component.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the expanded pricing select trigger |
| hover | applicable | Pointer-web select trigger; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A select trigger can be unavailable; visual treatment omitted |
| error | applicable | Form control; visual treatment omitted |

Loading and success applicability are omitted. Source names this control as a trigger for an observed open pricing select; exact request/outcome mapping for loading and success is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

### Pricing Select Menu

- Role: expanded pricing select menu
- Anatomy: menu surface
- Background: `#282828`
- Text: `#dfdfdf`
- Border: `0px 1px 1px solid #faff69`
- Radius: `0px 0px 6px 6px`
- Padding: `4px 0px`
- Font: `14px / 400 / Inter`
- Use: `surface-2::[data-omd-interaction-capture="menu-0-0"]`; three pricing menus exposed the same expanded/menu-open treatment.
- Observed: expanded/menu-open; selected targets recorded within the expanded menu; no unobserved selected fill is claimed
- Field note: The following unmerged-field reading, including `#282828`-not-canvas, yellow-lower-side-border-not-primary-fill-as-a-second-paint, `0px 0px 6px 6px`-not-YAML-md-6-as-a-second-paint-not-trigger-4px, 4px-0px-not-trigger-padding, expanded-menu-open-as-additional-named-observed-state-not-a-Core-row, selected-as-additional-named-observed-state-not-a-Core-row, and recorded-exception-elevation-`rgba(0,0,0,0.1) 0px 10px 15px -3px`-and-`rgba(0,0,0,0.1) 0px 4px 6px -4px`-staying-on-this-expanded-select, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Border `0px 1px 1px solid #faff69` is this expanded menu. Radius `0px 0px 6px 6px` is this menu’s local geometry.

Treating this expanded pricing select menu as having no YAML `type` and no interactive-kind confirmation for a §4.4 map so kind and a state-applicability map are omitted rather than invented, and treating expanded/menu-open and selected as additional named observed states rather than Core applicability rows, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Type is not invented. Kind and a state-applicability map are omitted. Additional observed named states: expanded/menu-open (three captured menus); selected targets within the expanded menu, with no unobserved selected fill claimed.

### Radio-like Toggle

- Role: radio-like toggle on the public pricing calculator
- Kind: interactive
- Anatomy: control
- Background: `#1f1f1c`
- Border: `1px solid #414141`
- Radius: `9999px`
- Use: `surface-2::[data-omd-capture="96"]`; checked and unchecked states were recorded, but no different checked value is asserted.
- Observed: checked and unchecked labels; no different checked visual value is asserted
- Field note: The following unmerged-field reading, including `#1f1f1c`-not-canvas-`#131312`-not-control-surface-`#282828`-not-overlay-`#141414`, 1px-`#414141`-not-yellow-`#faff69`, 9999px-not-primary-4px-not-nav-8px, and checked-unchecked-as-additional-named-observed-states-not-Core-rows, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. `#1f1f1c` is this control’s fill. Radius `9999px` is YAML `full` / this toggle.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the pricing radio-like toggle |
| hover | applicable | Pointer-web toggle; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A toggle can be unavailable; visual treatment omitted |
| loading | not-applicable | A radio-like toggle selects an option; the toggle itself does not enter a loading state |
| error | not-applicable | Toggle meaning is checked versus unchecked, not a request or validation failure on the toggle |
| success | not-applicable | Toggle meaning is option selection, not action-outcome confirmation |

Treating checked and unchecked as additional named observed states rather than Core applicability rows, and treating no-different-checked-visual-value-as-asserted, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Additional observed named states: checked and unchecked labels. No different checked visual value is asserted.

### Cookie Dialog

- Role: public privacy / cookie-consent dialog
- Anatomy: dialog surface
- Background: `#141414`
- Text: `#ffffff`
- Border: `1px solid #393939`
- Radius: `10px`
- Use: `home::[data-omd-interaction-capture="dialog-0-0"]`; consent chrome, excluded from product/calculator components.
- Observed: open
- Field note: The following unmerged-field reading, including `#141414`-as-overlay-only-not-canvas-`#131312`-not-control-surface-`#282828`, `#ffffff`-as-this-dialog-text-not-on-action-`#151515`, 1px-`#393939`-not-hairline-`#414141`, 10px-YAML-dialog-not-primary-4px-not-nav-8px, consent-chrome-excluded-from-product-calculator-components, and open-as-additional-named-observed-state-not-a-Core-row, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. `#141414` is YAML `overlay` / this dialog. Radius 10px is YAML `dialog`.

Treating this cookie dialog as having no YAML `type` and no interactive-kind confirmation for a §4.4 map so kind and a state-applicability map are omitted rather than invented, and treating open as an additional named observed state rather than a Core applicability row, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Type is not invented. Kind and a state-applicability map are omitted. Additional observed named state: open (separate consent dialog).

### Public List Item

- Role: repeated public-route list item
- Type: listItem
- Anatomy: list item
- Text: `#dfdfdf`
- Radius: `0`
- Padding: `0px`
- Font: `16px / 400 Inter`
- Use: YAML `Repeated public-route list item; 155 observed occurrences`
- Observed: default; 155 observed occurrences
- Field note: The following unmerged-field reading, including `#dfdfdf`-as-this-item’s-renderable-foreground-not-extra-general-ink, radius-0-not-YAML-sm-4, padding-0px-not-control-padding, and 16px-400-not-control-14px-500, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. `#dfdfdf` is this component’s renderable foreground, not extra general ink. Radius 0 is this list item, not YAML `sm` 4.

Treating this list item as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML-spacing-xs-4-sm-6-md-8-control-10-lg-12-xl-16-xxl-24-section-32, body-4-6-8-10-12-16-20-24-32px, compact-public-control-rhythm-not-a-complete-grid-or-responsive-layout-specification, public-home-body-dark-`#131312`-canvas, calculator-fields-and-menus-on-`#282828`, editorial-content-keeps-its-own-article-layout, do-not-infer-authenticated-console-density-dashboard-grids-or-mobile-collapse-behavior, and 20px-as-a-body-observation-not-a-YAML-spacing-step — are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification. YAML spacing is xs 4, sm 6, md 8, control 10, lg 12, xl 16, xxl 24, section 32. The supplied 1440×900 captures repeat 4, 6, 8, 10, 12, 16, 20, 24, and 32px spacing values. They support a compact public-control rhythm, but do not establish a complete grid or responsive layout specification. The public home body measures a dark `#131312` canvas; the calculator puts fields and menus on `#282828`; editorial content keeps its own article layout. Do not infer authenticated-console density, dashboard grids, or mobile collapse behavior from these routes.

No viewport comparison is available in this packet. All three supplied surfaces were captured at 1440×900. Treating those 4 / 6 / 8 / 10 / 12 / 16 / 20 / 24 / 32px / 1440×900 figures as captured-surface measurements rather than a cross-viewport specification, including preserve-accessibility-and-responsive-implementation-requirements-but-do-not-claim-ClickHouse-specific-mobile-menu-behavior-breakpoints-touch-targets-or-grid-columns-without-another-direct-observation, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. Preserve accessibility and responsive implementation requirements, but do not claim ClickHouse-specific mobile menu behavior, breakpoints, touch targets, or grid columns without another direct observation. The harvested control sizes below are desktop-capture measurements, not cross-viewport specifications: home primary padding `10px 16px`, pricing primary and secondary padding `12px 24px`, number-field `0px 12px`, select-trigger `0px 28px 0px 12px`, nav `10px 12px`, cookie dialog 10px radius.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating ClickHouse’s official guidance as calling the voice raw, confident, exacting, technical, and direct, including stated-aim-to-empower-developers-and-data-teams-with-speed-performance-and-control, writing-guidance-get-to-the-point-avoid-generic-marketing-language-and-prove-claims-with-numbers-benchmarks-or-examples, use-active-short-sentences-and-practical-error-language, and useful-official-writing-guidance-not-a-complete-product-microcopy-guide, as a reconstruction reading of official writing rather than a separately published UI specification, is a derived editorial implementation inference from the verified surfaces; it is not ClickHouse-authored or a separately published UI specification. ClickHouse’s official guidance calls the voice raw, confident, exacting, technical, and direct. Its stated aim is to empower developers and data teams with speed, performance, and control; the writing guidance asks teams to get to the point, avoid generic marketing language, and prove claims with numbers, benchmarks, or examples. Use active, short sentences and practical error language.

The following voice-table directions — Action-use-a-direct-verb-and-name-the-outcome, Performance-claim-state-the-measured-result-then-link-the-benchmark-or-example, Technical-explanation-lead-with-the-practical-consequence-before-implementation-detail, and Error-or-warning-state-what-happened-and-what-to-try-next-do-not-blame-the-user-or-use-cutesy-copy — are a derived editorial implementation inference from the verified surfaces; they are not ClickHouse-authored or a separately published UI specification.

| Context | Treatment |
|---|---|
| Action | Use a direct verb and name the outcome. |
| Performance claim | State the measured result, then link the benchmark or example. |
| Technical explanation | Lead with the practical consequence before implementation detail. |
| Error or warning | State what happened and what to try next; do not blame the user or use cutesy copy. |

**Official wording samples**

- *“direct minimalism”* — official brand guidelines; clarity and purpose before decoration.
- *“raw, confident, exacting, technical, and direct”* — official voice and tone.
- *“built for engineers and trusted by leaders”* — official brand guidelines.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover, `focus-visible`, empty, loading, error, success, disabled, and skeleton visual treatments on the captured public routes
- Public Primary Action / Public Secondary Action loading·error·success applicability (exact request/outcome unresolved)
- Pricing Number Field / Pricing Select Trigger loading·success applicability (exact request/outcome unresolved)
- interactive kind and state-applicability map for Public List Item, Pricing Select Menu, and Cookie Dialog
- YAML `type` for Primary Action, Secondary Action, Navigation Item, Number Field, Select Trigger, Select Menu, Radio-like Toggle, and Cookie Dialog
- a complete grid or responsive layout specification; ClickHouse-specific mobile menu behavior, breakpoints, touch targets, or grid columns
- a universal application-overlay scale
- undocumented official color-scale steps as hex tokens
- cookie-consent green `#166534` and its hover value as an action-palette token
- Söhne / `fontSohne` / `fontSohneBreit` / `basier Fallback` / `ui-sans-serif` / `ui-monospace` as live UI families
- authenticated-console density, dashboard grids, and mobile collapse behavior; authenticated product-console typography and components
- Click UI Button, IconButton, SplitButton, Text Field, and Select API variants not exposed by the supplied public capture
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
