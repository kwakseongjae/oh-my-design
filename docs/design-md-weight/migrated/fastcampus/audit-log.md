# Fastcampus F3 audit log

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v10** — B2 · B2a and E1 · E2 · E2a–c only.
Date: 2026-08-26. Fresh session, separate from the migration worker.
Scope discipline: findings outside those clauses are reported, not repaired. No value, table, or structure was changed.

## Fixes

### B2a — portable body (3)

1. **§1 Scope, body line 15.** The *fast + campus* reading was asserted in the document's own voice ("The brand name frames the proposition as *fast + campus*, a compressed academic experience for adults who cannot take a semester off"), while the adjacent qualifier covered only the visual-evolution reading beside it. Folded both readings into one qualified sentence. Every word of the reading survives; only its evidence class changed.
2. **§3 type register, body line 203.** "the honorific `~님` reserved for instructor-facing surfaces" states a convention on surfaces the 2026-05-15 inspection did not cover — Scope names only the home storefront and `category_online_all` — and was unqualified. Added the complete adjacent qualifier for that clause alone. The straight-quote observation and the recorded carousel strings (`신청하세요`, `만나자`, `쓸 수 있는`) stay unqualified: they are observed copy, and qualifying them would demote evidence.
3. **§6 Register, body line 503.** Same shape: "formal `~합니다` kept for legal, refund, and receipt screens" is a convention about uninspected surfaces. Qualified that half; left the product-surface half, which restates the register of the strings recorded above it, unqualified.

All three already appeared under a qualifier elsewhere — 1 beside the visual-evolution reading, 2 and 3 inside Principle 5 — so the defect was the unqualified standalone restatement, which is exactly what B2a's adjacency requirement is for. Qualifier occurrences: 15 before the audit, 17 after (`grep -oF "derived editorial implementation inference" DESIGN.md | wc -l`).

No qualifier was removed. Nothing was demoted for being "the source's own sentence" — the test applied throughout is the publishing entity, not the authoring file.

### E1 — portable body (1)

4. **§3 Assets, body line 207.** The bullet read "Catalog logo pointer" and "recorded as the catalog's logo reference". That names *this project's* reference catalog, three words from the body's twelve other uses of "catalog", which all mean Fastcampus's course catalog. Now "Logo pointer" and "this contract's logo reference". The URL, the favicon-proxy grading, and the not-brand-artwork caveat are untouched; `grep -oF 's2/favicons?domain=fastcampus'` is still 1 in the body and 1 in provenance.

`node test-v2/tools/process-leak-check.mjs` reported fastcampus with zero hits both before and after — this term is not one the checker holds.

### E2 / E2a / E2c — migration log (7) and provenance (1)

5. **Provenance, Freshness note.** "This ledger records three that the migration found and did not resolve" undercounted. The three tabled conflicts are all source-versus-sibling. The migration also found four source-internal contradictions and recorded them elsewhere — mono family and the fixed h1 / h2 sizes in the Identity note, the rank-badge corner clip and the `커뮤니티` / `기업교육` nav roles in the portable body. The sentence now scopes its three and points at the other four. The three tabled conflicts were verified against the proof files and are accurate (see Verifications).
6. **Log row, YAML `verified` / `omd` / `tokens.source` / …** Disposition column said 분리 → provenance only, while the reason cell already admitted the body carries the 2026-05-15 date. E2a wants both destinations in the disposition. Added.
7. **Log row, §7 Motion.** The row claimed "the category-pill scroll-snap with no animated tap transition" and "the SSR full-page navigation" were 옮김 → §2 Motion. Neither is in the body. Occurrences over the portable body: `scroll-snap` 1 — and that one is §5 on course rails, a different element — `tap` 0, `SSR` 0, `full-page` 0. The Swiper auto-advance interval (`5s`) is absent on the same footing. Row rewritten to name what actually landed and to log the two observations as 미이관 with the measurement.
8. **Log row, §11 Brand Narrative.** Read as a clean 옮김 while two whole narrative paragraphs were dropped with no disposition anywhere: the founding-observation paragraph (fragmented offline market, weekend academies, public-institute lectures, book-and-DVD self-study, premium willingness-to-pay) and the bet paragraph (project structure, instructor accountability, cohort timing). Over both output files: `fragmented` 0, `premium` 0, `YouTube` 0, `accountability` 0, `cohort timing` 0, `offline market` 0. Recorded as a deletion.
9. **Log rule note, A5 — E2c.** The note claimed "every published non-Latin string in the source survives" and "No Korean label was replaced by an English paraphrase". Both are stronger than the body. See Out of scope for the three strings; the note now states the measured result, names the gate blind spot that produced the false confidence, and stops claiming survival.
10. **Log rule note, A1 — counting rule.** "76 distinct hexes, 75 in the body" is true only case-folded; unfolded it is 77 / 76. The note now says which, per the counting rule that a count states what it counted. The single absence (`#f8f9fa`) is the same either way.
11. **Log rule note, E2a.** Said "nine rows above name two destinations each" and then listed eight. The enumeration was also wrong at both ends: it omitted the §2 orange-ramp fifth step (provenance omission ledger + portable §7 Named gaps), the `verified` date, `tokens.spacing` (§2 + §5) and §9 Accessibility (§2 + §4 + §5 + §6), and it counted the sibling proof-file row, whose disposition is provenance only. Recounted to ten with the destinations named.
12. **Log, F1 pass.** Claimed 13 qualifiers against a body that had 15 before this audit and has 17 after. Count corrected, body line numbers cited, enumeration extended to the passages it had not listed.
13. **Log, F2 pass.** Re-measured after the audit's edits and rewritten to say what changed. The `--gate-only` PASS and `portable_core: true` lines were removed as conformance evidence: the A5 finding is a case of the gate passing a body that is missing published copy, so a PASS bounds nothing this log claims. The E1 leak-check result is kept, because that is the check the claim beside it actually rests on.

## Verifications performed, no change needed

- **The `#fa a93f` handling is correct and was left alone.** The legacy orange ramp's fifth step is written with a space inside the hex and is not renderable. The body elides that one step, states the elision in place, names it as a gap in Governance, and quotes the corrupted value verbatim in the omission ledger. Measured: `faa93f` in the body 0, `fa a93f` in provenance 1, `f9a93f` (the sibling `tokens.json` value) in the body 0 and provenance 1. Repairing the hex would have been an A1 invention; promoting the sibling value would have been a second one. Both were declined, and the E3 note explaining why is accurate.
- **The unitless H2 `26` was left alone**, on the same reasoning the worker gave. Noted below as an internal asymmetry, not touched.
- **The four `assets/_reference/*.json` proof files exist and were read.** All four are real: `.live-inspect-proof.json` (40 home raw samples, 2 category raw samples), `tokens.json` (103 `:root` custom properties), `fonts.json`, `structure.json`.
- **The three recorded conflicts are real and stated accurately**, against the source footer's "Conflicts unresolved: none":
  - *Second-route description.* The proof file's two `category_online_all` samples are a not-found page — `h3` `페이지를 찾을 수 없습니다.` in `rgb(37, 39, 41)`, `a` `홈으로 이동하기` on `rgb(252, 28, 73)` — byte-for-byte as the ledger quotes them. The source calls that route a "category-page GNB confirmation". The ledger's resolution is also sound: `structure.json` carries an identical header record for both routes, which is what the portable Scope claims and no more.
  - *Focus color.* `tokens.json` carries `--fds-semantic-border-focus: #37393b`; the source derives a `#fc1c49` focus outline from the primary token. Both real, neither promoted to a `focus-visible` treatment.
  - *GNB position.* `structure.json` records `"position": "static"` on both routes; the source says sticky from CSS-module class naming. The body carries the source's statement with its basis named and asserts no computed position.
- **`#f8f9fa` is correctly out of the body and in the omission ledger.** It is Inflearn's Mantine gray-0, cited in the legacy file only as the neutral Fastcampus is *not* — "a slightly cooler/grayer neutral than Inflearn's". Promoting a competitor's token into a Fastcampus contract would be the error; the E2b disposition (`분리 → provenance 생략 원장`, not a bare deletion) is the right one and is logged as such. It is the only hex in the legacy file absent from the body.
- **Sibling disposition is stated and the caution about it holds.** Provenance says "There is no `web/references/fastcampus/.verification.md` sibling" — subject is the reference directory, and that is true; the four files it does have are named and dispositioned in their own table.
- **Every migration-log count re-measured independently** with `grep -o … | wc -l` or `grep -c` over a named block: 패스트캠퍼스 1/1, favicon URL 1/1, `prose-derived` 0/4, `components_harvested` 0/2, `2026-06-09` 0/4, `9999` 1/1, `cubic-bezier` 0/2, 22/22 distinct px, 3/3 ms, 4→3+1 percent, 9→9 Key Characteristics, 12 neutral rows, 9 type rows, 10 tone rows, 5→5 Principles, 6→8 Avoid, 11→13 state bullets, 12→12 state-catalog rows, 13 `###` blocks in §4, 7 `type:` mentions (tab 2 / button 1 / card 1 / input 1 / badge 2), 7 state tables, 33 applicable, 16 not-applicable, `focus-visible` 0 in the legacy file. All matched except the four corrected above.

## Out of scope — reported, not repaired

- **A5, three published strings lost (the substantive one).** `비주얼 브랜딩`, `수강신청 완료`, and `Codex 기반 AI 인증시험` are the pink, green, and blue ramps' rail labels in the legacy file. Each is absent from all three migrated files — legacy 1 / body 0 / provenance 0 / log 0. `수강신청 완료` was replaced by the English paraphrase "enrolment-complete confirmation", which is the exact v9 failure mode; the similar `수강신청이 완료되었어요` elsewhere in the body is a different label and does not cover it. Restoring them is a value edit this audit may not make. **A-lane closure, 2026-08-26 (added by the A5 restoration worker; the finding above is left exactly as the audit wrote it).** All three are now in the portable body byte-for-byte — on their own ramp line in §2 and cross-listed in §6 Content & Locales — with the English gloss kept beside `수강신청 완료` in parentheses rather than in its place. Re-measured per file with `grep -oF … | wc -l`: legacy 1 / portable `DESIGN.md` 2 / `provenance.md` 0 for each. The `log 0` reading above is superseded: this file and `migration-log.md` now mention the strings while reporting them, and those mentions are not uses in a contract body.
- **E3, gate false negative behind that loss.** The A5 needle extractor only takes non-Latin runs from quoted spans. On the extended-hue-ramp lines the long backtick-quoted ramp mispairs with the following `"…"`, so the rail label after it is never made a needle. Result: 41 needles where a whole-file run scan finds 74, and a clean `0 missing` over a body that is missing three labels. Left in place and reported rather than worked around.
- **A1, motion observations dropped.** The category-pill line ("horizontal scroll-snap, no animated transitions on tap — instant active-state swap") and the page-transition line ("SSR / Next.js — full-page navigation, no client-side route animation") are in neither output file. The Swiper auto-advance interval `5s` is gone too; the body says "the Swiper default interval". The log rows now say so. **Partial A-lane closure, 2026-08-26 (added by the revision worker; the finding above is left exactly as the audit wrote it).** The `5s` is now in the portable body, inside the §2 Motion carousel rule where the legacy file put it, and its log disposition moved from 미이관 to 옮김 → §2 Motion on its owning row (§15 Motion rules). Per file, `grep -oE '[^0-9.]5s' … \| wc -l` (the class excludes the `1.5s` shimmer, which is 3 in the legacy file and 3 in the body): legacy 1 / portable `DESIGN.md` 1 / `provenance.md` 1, the last being the derived-editorial ledger's index entry rather than a second home for the value. The other two observations in this bullet stand as reported — still absent, still logged as 미이관 with their measurements — because the semantic review confirmed only the interval as a FAIL and a revision worker does not widen its own mandate.
- **A1, brand-narrative paragraphs dropped.** Recorded under fix 8. Business-thesis prose with no obvious Core v2 slot, but it left both output files with no disposition until now.
- **Provenance sibling-only list is incomplete.** `--fds-semantic-label-secondary = #747678` and `--theme-brightness = 1` are in `tokens.json`, absent from the legacy file, and missing from the "values that exist only in the siblings" list. A value list, so untouched.
- **Provenance freshness timestamp is imprecise.** "sibling proof files captured | 2026-05-15T07:09:53Z" holds for `fonts.json`, `structure.json`, and `tokens.json` (all `…07:09:53.194Z`); `.live-inspect-proof.json` is `2026-05-15T07:09:57.318Z`. A table value, so untouched.
- **H1 / H2 unit asymmetry.** The body writes the frontmatter's h1 as `34px` and the frontmatter's h2 as unitless `26`, though both are unitless in the frontmatter and both prose ranges are in px (`32-36px`, `24-28px`). The stated reason for the difference — that `26px` appears nowhere in the legacy file — is true, but `34px` appears there only as a competitor's hero size. Left as written on instruction; noted because the asymmetry is not self-explaining.
- **"The source" appears 55 times in the portable body.** It carries the evidence-class grading E1 requires in the body, but it names a document a standalone reader does not have. Not one of E1's held terms, and the corpus uses the same device widely (dji 106, appier 58). Left alone.

## Appended after the audit — not part of the audit record

**2026-08-26, revision worker.** Nothing above was rewritten; this section is the correction the audit could not make on itself.

The three B2a fixes in this log (§1 Scope body line 15, §3 register body line 203, §6 Register body line 503) landed in the portable body and nowhere else. `provenance.md`'s "Derived-editorial scope carried in the portable body" ledger still listed 12 rows for what was by then a 17-qualifier body, and its lead-in claimed to record "the full extent so the two files agree" — a completeness claim that the audit's own edits had falsified an hour earlier. That is not a defect in the audit's judgement: the fixes were correct and the count in the B2a section (15 before, 17 after) was measured and right. It is a handoff gap — this audit is scoped to B2 · B2a and E1 · E2, so a provenance ledger table was outside what it could touch, and no step afterwards carried the body change across.

The ledger is now one row per body occurrence, 17 rows carrying 17 body line numbers, with the three rows above attributed to this audit in place. Two further occurrences that predate the audit had also never been rowed — the yellow/orange accent-slot reading (body 128) and the course card's interactive kind (body 341) — and are now rowed too. Verified 1:1 rather than by total: `grep -noF 'derived editorial' DESIGN.md | cut -d: -f1` and the ledger's Body line column give the same 17 values in the same order. Per-file occurrences with `grep -oF 'derived editorial' … | wc -l`, taken after this section was written: `DESIGN.md` 17, `provenance.md` 3, `migration-log.md` 5, this file 3 — of which only the body's 17 are qualifiers standing in a contract; the rest, including the two this section itself adds, are mentions of the ledger or of the finding, not uses.

The E2c downgrade is recorded in `migration-log.md` under the F2 pass. The body edits in this revision — the `5s` restoration and the removal of `authenticated` / `app` from the last Avoid bullet — both sit inside existing lines, so every body line number cited anywhere in this log is still accurate. The body is 556 lines before and after.

## Status

`node test-v2/tools/process-leak-check.mjs` → fastcampus absent from the leaking list, zero hits.
`node migrate-reference.mjs --brand fastcampus --gate-only` → PASS, problems `[]` — recorded as status only. It read the same before this audit's 13 fixes and after, and it passed a body missing three published labels, so it is not offered as evidence of conformance.

AUDIT_DONE fastcampus fixes=13
