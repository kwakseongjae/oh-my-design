# Fastcampus migration log

Source: `web/references/fastcampus/DESIGN.md`
Destination: `docs/design-md-weight/migrated/fastcampus/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/fastcampus/provenance.md`
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v10**
Date: 2026-08-26

Source layout note: this legacy file is the 15-section format with two irregularities. Its Do's and Don'ts block is numbered `## 16` and sits between §4 and §5, and the **Verified** / Tier / conflicts footer sits mid-document immediately after it rather than at the end. Section numbering below refers to the section titles, not to the file order. There is no Agent Prompt Guide in this file; its `## 9` is Accessibility & Internationalization.

Every row below was checked by grepping the actual destination files before it was written (F2). Where a value landed in two places, both destinations are named.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`) | 분리 → provenance Identity | Portable file carries no frontmatter. Name kept as H1 `Fastcampus Design System`; 패스트캠퍼스 appears in the Scope claim as well, so this row lands in **both** provenance Identity and portable §1 Scope (`grep -oF 패스트캠퍼스`: DESIGN.md 1, provenance.md 1). |
| YAML `logo` (favicon proxy URL) | 분리 → provenance Identity **and** 옮김 → portable §3 Assets | Recorded in both places, in both as a third-party favicon proxy rather than a Fastcampus-published asset — as "this contract's logo reference" in the body, which the audit de-referenced from "the catalog's" on 2026-08-26 (E1: the body must not name this project's own catalog), and as a catalog pointer in provenance, where that vocabulary belongs (`grep -oF 's2/favicons?domain=fastcampus'`: DESIGN.md 1, provenance.md 1). |
| YAML `verified` / `omd` / `tokens.source` / `tokens.extracted` / `components_harvested` | 분리 → provenance Identity + Freshness **and** (`verified` only) 옮김 → portable §1 Scope | Ledger and freshness fields only (`grep -oF prose-derived`: DESIGN.md 0, provenance.md 4; `grep -oF components_harvested`: DESIGN.md 0, provenance.md 2; `grep -oF 2026-06-09`: DESIGN.md 0, provenance.md 4). The portable body carries the 2026-05-15 inspection date as an evidence boundary and nothing else. |
| YAML `tokens.colors` (21 entries) | 옮김 → §2 Semantic color | All 21 kept with their source role names. Counted with `grep -oE '#[0-9a-fA-F]{6}\b' … \| sort -u \| wc -l` over each whole file: legacy 76 distinct hexes, portable body 75. The single legacy hex absent from the body is `#f8f9fa`, a competitor's neutral, held in the provenance omission ledger (row below). |
| YAML `tokens.typography` (family, banner, h1, h2, body, gnb-compact, button, rank) | 옮김 → §3 Family + Type roles | 9 role rows plus a header (`grep -cE '^\| [A-Z]'` over the Type roles block = 10). Unitless ratios preserved as ratios: 1.2, 1.3, 1.35, 1.5, 1.4, 1.0 all present in the body. `h1` and `h2` are carried as the prose's inferred ranges with the frontmatter's fixed numbers named in Notes — the frontmatter's `26` has no `px` form anywhere in the legacy file, so writing `26px` would have invented a token; it is written unitless. |
| YAML `tokens.typography.family.mono` | 옮김 → §3 Family, as a conflict | The frontmatter sets `Pretendard Variable`; the prose calls the mono role an inferred system mono stack. Both statements are carried and neither is chosen, per the Core rule that conflicts are preserved as conflicts. |
| YAML `tokens.spacing` (7 steps) | 옮김 → §2 Spacing, restated by use in §5 | 4 / 8 / 12 / 16 / 24 / 32 / 48, plus the prose's 4-6px and 48-64px range endpoints. |
| YAML `tokens.rounded` (`sm` 4, `md` 4, `lg` 19, `full` 9999) | 옮김 → §2 Shape **and** 분리 → provenance claim ledger | 4px and 19px are in Shape with their uses. `full: 9999` is in **both** the portable Shape list and the provenance claim ledger, in both places recorded as a scale step no recorded element uses (`grep -oF 9999`: DESIGN.md 1, provenance.md 1). It is written without a `px` suffix because the legacy file has none. |
| YAML `tokens.components` (11 entries) | 옮김 → §4 Components & States | Each entry keeps its verified `type` on its Kind line — `tab` ×2, `button` (named as covering both the filled and the tinted entry), `card`, `input`, `badge` (named once on the Rank Badge and once as covering all four tag chips). `grep -oE 'source \`type: [a-z]+\`'` over the body: `tab` 2, `button` 1, `card` 1, `input` 1, `badge` 2 — 7 mentions covering the 11 entries, because the three CTA entries and the four tag entries are declared as variants inside one component each. |
| §1 Visual Theme & Atmosphere | 옮김 → §1 Scope + Distinctive traits | 9 Key Characteristics → 9 Distinctive traits (`grep -cE '^- '` over each block: legacy 9, body 9). The FDS namespace, the 103 `:root` custom properties, and the 10-shade ramp structure are in Scope. The commerce-red-not-warning-red reading and the "programmed market" register reading carry an adjacent derived-editorial qualifier. |
| §1 Inflearn / Classum / Toss comparisons | 삭제 (본문) → 분리 → provenance 생략 원장 | The body states Fastcampus's register directly instead of by contrast. `#f8f9fa`, Inflearn's 32px nav pills and 34px hero, `부담없이 시작하기`, `지식공유자님`, Toss's 12-16px radii, and Classum's institutional blue are recorded in the provenance omission ledger. Two of those numbers survive in the body on Fastcampus's own account instead: 34px as the H1 role's fixed size and 32px as a spacing step. |
| §2 Color Palette & Roles | 옮김 → §2 Semantic color **and** (the three ramp labels) 옮김 → §6 Content & Locales | Primary family, the 12-row neutral ladder (12 rows verified by `grep -cE '^\| .neutral'`), the static neutrals, the FDS semantic role tokens, and all six extended hue ramps. Each ramp keeps the source's own use note. Three published labels inside those notes — `비주얼 브랜딩` on pink, `수강신청 완료` on green, `Codex 기반 AI 인증시험` on blue — were dropped in the first pass, the green one replaced by an English paraphrase; all three were **restored byte-for-byte on 2026-08-26** in place on their ramp line and cross-listed in §6, so this row lands in **two** places in the body for them (`grep -oF … \| wc -l` over the portable body: `비주얼 브랜딩` 2, `수강신청 완료` 2, `Codex 기반 AI 인증시험` 2; provenance.md 0 for each). See the A5 note. |
| §2 Implementation hints | 옮김 → §2 Color rules | The `--c-primary-rgb` alpha-mixing note, the yellow/orange adjacent-slot reading (now carrying the adjacent derived-editorial qualifier), and the `--swiper-theme-color` `#007aff` non-adoption. The last also appears in §1 Avoid, so that value lands in **two** places in the body. |
| §2 orange ramp, fifth step | 분리 → provenance 생략 원장 + §7 Named gaps | The legacy value is written `#fa a93f`, which is not a renderable hex and which the hex scanner therefore never saw. It is elided from the portable ramp with the elision stated in place, named as a gap in Governance, and recorded verbatim in the omission ledger (`grep -oF 'fa a93f'`: provenance.md 1). The sibling `tokens.json` value `#f9a93f` is recorded beside it and **not** promoted (`grep -oF f9a93f`: DESIGN.md 0, provenance.md 1). |
| §3 Font Family + Hierarchy + Rules | 옮김 → §3 Font evidence + Family + Type roles | The full `--font-base` stack verbatim, the `html` root declaration, the type-role table, and the weight-cadence and two-line-banner rules. The binary-cadence and inverted-density readings carry an adjacent qualifier. |
| §3 Korean punctuation / honorific line | 옮김 → §3 | Straight quotes, `~님` for instructor-facing surfaces, and the carousel-copy examples `신청하세요`, `만나자`, `쓸 수 있는`. The carousel strings stay unqualified as recorded copy; the `~님` reservation names a convention on surfaces the inspection did not cover and carries an adjacent qualifier (audit 2026-08-26). |
| §4 Component Stylings (16 declarations + 1 state) | 옮김 → §4 per component | 16 legacy declarations → 11 portable declarations: the three Primary CTA entries fold into one component with two named variants, and the four tag chips fold into one component with four named variants; every value from all 16 is carried. The seventeenth bold block, `Disabled (Inferred from neutral ladder)`, is a state and went to the state contract instead. §4 now has 13 `###` blocks: 11 declarations plus Capture record and State contract. |
| §4 inference labels | 옮김 → §4 Capture record + per-component `Source class` lines | Every "(Inferred from …)" label survives as an explicit evidence class on the component it belongs to, and the Capture record names which components are live-inspected and which are derived. |
| §16 Do's (6) | 옮김 → §1 Principles / §2·§3 rules | Brand application rules stayed in Experience and Foundations; none was folded into the Governance control clauses. |
| §16 Don'ts (6) | 옮김 → §1 Avoid | All six kept. The Avoid lead-in qualifies all 8 items and states that six restate the source's list while the last two are evidence-boundary rules (`grep -cE '^- '` over the Avoid block = 8; over the legacy `### Don't` block = 6). |
| Footer **Verified** / Tier 1 / Tier 1 official DS negative / Tier 2 / Conflicts | 분리 → provenance Freshness + Surfaces and sources **and** 옮김 → portable §1 Scope | The Tier grading, the `getdesign.md` and `styles.refero.design` empties, the DNS/ECONNREFUSED/404 probe detail, and "Conflicts unresolved: none" are provenance-only. The 2026-05-15 date, the two inspected URLs, and the substance of the negative DS result (no public DS site, Figma kit, or token repository was reached) are **also** in the portable Scope as the evidence boundary, so this row lands in **both** files. |
| Footer "Inferred-but-not-live-captured tokens" list (7 items) | 옮김 → §7 Named gaps **and** 분리 → provenance Evidence classes | Carried verbatim as the source's own follow-up list in Governance, and graded again in the provenance evidence-class table. |
| Footer `tech.day1company.io` UPDATE instruction | 삭제 (본문) → 분리 → provenance 생략 원장 | A pipeline instruction, not a design fact. |
| §5 Layout & Spacing | 옮김 → §5 Layout & Platforms | The 7-step inferred spacing table's per-step uses, the full-bleed banner over a 1280-1440px container, scroll-snap below 1280px against a 4-5 column grid above, and the 96px / 40px / 327px above-the-fold stack. The commerce-dense "programmed market" reading carries an adjacent qualifier. |
| §6 Iconography & Imagery | 옮김 → §3 Assets | 24×24 stroke icons at 1.5-2px, 16:9 thumbnails with the `강사 얼굴` + `굵은 타이포` composite convention, gradient banner backdrops, and the text-only rank-badge overlay. |
| §7 Motion | 옮김 → §2 Motion (부분) + 미이관 2건 | In the body: the Swiper carousel transition, the pill hover fade, the card hover micro-scale, the "Motion tokens not exposed at `:root`" constraint, and the `prefers-reduced-motion: reduce` contract. **Not in the body and not previously logged** (audit 2026-08-26): the category-pill line ("horizontal scroll-snap, no animated transitions on tap (instant active-state swap)") and the page-transition line ("SSR / Next.js — full-page navigation, no client-side route animation"). Measured over the portable body with `grep -oiF … \| wc -l`, counting occurrences: `scroll-snap` 1 — and that one is §5 on course rails, a different element — `tap` 0, `SSR` 0, `full-page` 0. **Superseded for one of the three, 2026-08-26 (revision worker).** The Swiper auto-advance interval (`5s`) was reported absent here on the same footing; it has since been restored to the §2 Motion carousel rule, so its disposition is 옮김 → §2 Motion and it is logged on its owning row, §15 Motion rules. Measured after the restoration, per file with `grep -oE '[^0-9.]5s' … \| wc -l` (the class excludes the `1.5s` shimmer, which is 3 in the legacy file and 3 in the body): legacy `web/references/fastcampus/DESIGN.md` 1, portable `DESIGN.md` 1, `provenance.md` 1 — the provenance occurrence is the derived-editorial ledger's §2 Motion row naming the interval, an index entry, not a second home for the value. The other two observations above are still 미이관 at the measurements given; they are recorded observations, so that remains an A-lane repair, logged here so it stays visible. |
| §8 States (11 bullets) | 옮김 → §4 State contract | All 11 kept with their values and inference labels (`grep -cE '^- \*\*'` over the legacy §8 block = 11; over the portable "Interaction and status treatments" block = 13 — the two extra are the keyboard-outline and screen-reader lines moved from §9). The Focus bullet keeps its derivation from the primary semantic token and states why no `focus-visible` row carries a treatment. |
| §9 Accessibility & Internationalization | 옮김 → §2 Color rules (contrast), §4 State contract (keyboard + screen reader), §5 (density caveat), §6 (locale) | Split by meaning: the six contrast measurements to Foundations, the 2px Tab outline and the carousel `aria-label` / in-DOM rank text to the state contract, the 12px rank-label readability caveat and the 100% zoom check to Layout, and the Korean-primary / no-English-mirror / Pretendard-coverage facts to Content & Locales. |
| §10 Voice & Tone | 옮김 → §6 Content & Locales | The 9-row tone table kept row for row (`grep -cE '^\| [A-Z]'` over the table in each file = 10, i.e. 9 data rows plus the header), with every Korean example byte-for-byte. The register line and the forbidden-phrase list are kept with the source's own "illustrative, not from a published guide" labelling; the register line's legal / refund / receipt half names a convention on surfaces the inspection did not cover and carries an adjacent qualifier of its own (audit 2026-08-26). The voice characterization, the tone table, and the `~만나자` reading carry one adjacent qualifier. |
| §10 Voice samples (3, labelled OmD-original) | 삭제 → 분리 → provenance 생략 원장 | Synthetic tone-shape lines that the source states reproduce no verbatim Fastcampus copy, so no synthetic voice sample is promoted. Recorded once in the omission ledger so the deletion is visible, with all three lines quoted there and nowhere else. |
| §11 Brand Narrative | 옮김 → §1 Scope (부분); 증거 등급 → provenance; 삭제 2문단 | 2014 founding, Day1Company / 데이원컴퍼니 parentage, Colosso and RealClass, the catalog's subject range, and the *fast + campus* framing are in Scope — the last of those now stated as a reading under the adjacent qualifier rather than asserted (audit 2026-08-26). The source's own weaker attribution (public Korean tech-press coverage; founder / founding-month / headcount not exhaustively verified) is carried in the Scope text **and** recorded in the provenance evidence-class table, so this row lands in **both** files. The reading of the visual evolution as the signal of the positioning carries the same adjacent qualifier. **Deleted and not previously logged** (audit 2026-08-26): the founding-observation paragraph (Korean adult upskilling as a fragmented offline market — weekend classroom academies, public-institute lectures, book-and-DVD self-study — and professionals' willingness to pay premium prices for short-format, instructor-led, project-based courses) and the bet paragraph (project structure, instructor accountability, and cohort timing as the difference from a free video tutorial). Measured over both output files with `grep -oiF … \| wc -l`: `fragmented` 0, `premium` 0, `YouTube` 0, `accountability` 0, `cohort timing` 0, `offline market` 0. Market-history and business-thesis prose with no Core v2 slot; recorded as a deletion so the row is not read as a complete 옮김. |
| §12 Principles (5) | 옮김 → §1 Principles | All five kept with their UI implications (`grep -cE '^[0-9]+\. '` over each block: legacy 5, body 5), under the B2a lead-in qualifier. |
| §13 Personas (4) | 삭제 | The source labels them inferred from observable surface targeting rather than taken from a published persona document. Not promoted to Audience or primary tasks, and not rehosted in provenance — the omission ledger records the deletion without reproducing any age band, buying pattern, or segment description. The one surface fact underneath them, the `기업교육` nav label, is a live-recorded label and survives on its own. |
| §14 States Catalog (12 rows) | 옮김 → §4 State contract | Body preserved in full, 12 rows for 12 (`grep -cE '^\| \*\*'` over each table = 12 and 12), including `'<query>'에 대한 결과가 없어요.`, `다른 결제 수단으로 시도하기`, and `3일 21:14:02 남음`. Marked as the source's state language under a derived-editorial qualifier and explicitly not as per-component paint values. No graph delegation. |
| §15 Motion & Easing — durations (150ms / 200ms / 300ms) | 옮김 → §2 Motion | Kept as a table with the source's uses. Carries an adjacent qualifier because the source states outright that `--fds-motion-*` is not exposed at `:root` and that motion was not exhaustively captured. |
| §15 Motion & Easing — `easing-default` (`ease`) | 옮김 → §2 Motion | A plain CSS keyword with a stated role, not an unsourced curve; kept with its use. |
| §15 Motion & Easing — the two `cubic-bezier` values | 삭제 (본문) → 분리 → provenance 생략 원장 | Unsourced curves. The emphasized and decelerate role names and their uses stay in §2 Motion; the two literal values are held in the provenance omission ledger and named as a gap in §7 (`grep -oF cubic-bezier`: DESIGN.md 0, provenance.md 2). |
| §15 Motion rules + reduced-motion | 옮김 → §2 Motion | Carousel auto-advance **with its `5s` interval**, the card hover micro-scale as the only ambient motion, no parallax, no scroll-triggered hero animation, and the `prefers-reduced-motion: reduce` fallback are all in the body. The interval is the legacy line's `(Swiper default 5s)`; it was dropped in the first pass — the body read "the Swiper default interval" with no number — and restored on 2026-08-26. Measured per file with `grep -oE '[^0-9.]5s' … \| wc -l` (the class excludes the `1.5s` shimmer, which is 3 in the legacy file and 3 in the body): legacy 1, portable `DESIGN.md` 1, `provenance.md` 1 (a ledger index entry, not a second home). The legacy file writes it bare inside `(Swiper default 5s)` and the body writes it as ``5s``; the backticks are this file's uniform code formatting, not a change to the value. It sits inside the §2 Motion prose rather than the duration table, which is where the legacy file put it, and it falls under the qualifier already adjacent at body line 161 — the source labels the whole motion set as inference. |
| Sibling `assets/_reference/*.json` (4 files) | 읽음 → provenance Sibling evidence files; 승격 없음 | There is no `.verification.md` sibling for this reference. The four proof files the source's own footer cites were read and used only to grade evidence class. No sibling-only value is promoted to a portable token; the sibling-only list and the three conflicts they surface (second-route identity, focus color, GNB position) are recorded in provenance. |

## Rule notes

- **A5** — the gate's own needle extraction (quoted spans, contiguous non-Latin runs of 4+ characters) yields 41 needles: 36 present byte-for-byte in the portable body, 5 in provenance only, 0 missing. The 5 are `부담없이 시작하기` (a competitor's CTA, held in the comparison ledger) and the four runs belonging to the three deleted synthetic voice samples. Latin published strings were checked by hand in the same pass: `AI TECH`, `AI CREATIVE`, `React`, `Next.js`, `AI`, `Codex`, `Claude`, `LLM`, `RAG`, `AI Agent`, `Oops!`, and the `!!!` tail are all present in the body.
  **Corrected by the audit, 2026-08-26.** This note previously read "every published non-Latin string in the source survives … No Korean label was replaced by an English paraphrase", which is stronger than the body. That claim rested on the gate's 0-missing result, and the gate's needle extraction is blind on the extended-hue-ramp lines: each of those lines opens with a long backtick-quoted ramp, the quote pairing then runs past the `"…"` label that follows it, and the label is never turned into a needle. A whole-file scan of contiguous non-Latin runs (74 runs, not 41) finds three published rail labels absent from all three files — legacy 1 / body 0 / provenance 0 / log 0 for each, by `grep -oF … \| wc -l`:
  - `비주얼 브랜딩` — the pink ramp's rail label. **Restored 2026-08-26 (A5 repair).** The pink ramp line in §2 now reads "creative and design category accent, the `비주얼 브랜딩` rails", and the label is cross-listed in §6 Content & Locales. `grep -oF '비주얼 브랜딩' … | wc -l`: legacy DESIGN.md 1, portable DESIGN.md 2 (§2 line 121, §6 line 487), provenance.md 0, migration-log.md 5, audit-log.md 1 — the log and audit counts are mentions of the finding, not uses in a contract body.
  - `수강신청 완료` — the green ramp's toast label. It had been replaced by the English paraphrase "enrolment-complete confirmation", which is the A5 failure mode named in the v9 revision, not a relocation: the distinct string `수강신청이 완료되었어요` elsewhere in the body is a different label and never covered it. **Restored 2026-08-26 (A5 repair).** The green ramp line now reads "success state, the `수강신청 완료` toast (enrolment-complete confirmation), course-progress complete bar" — the Korean label carries the fact and the English gloss sits beside it in parentheses rather than in its place, per the §6 rule that a Korean label is never re-rendered as an English paraphrase. Cross-listed in §6, where it is stated explicitly not to be a variant spelling of `수강신청이 완료되었어요`. `grep -oF '수강신청 완료' … | wc -l`: legacy DESIGN.md 1, portable DESIGN.md 2 (§2 line 122, §6 line 487), provenance.md 0, migration-log.md 5, audit-log.md 3 — log and audit counts are mentions. The separate `수강신청이 완료되었어요` is unchanged where it already stood, at body lines 462 and 500, and is named once more at 487 in the sentence that keeps the two apart.
  - `Codex 기반 AI 인증시험` — the blue ramp's rail label. **Restored 2026-08-26 (A5 repair).** The blue ramp line now reads "developer and data category accent, the `Codex 기반 AI 인증시험` rails", and the label is cross-listed in §6. `grep -oF 'Codex 기반 AI 인증시험' … | wc -l`: legacy DESIGN.md 1, portable DESIGN.md 2 (§2 line 123, §6 line 487), provenance.md 0, migration-log.md 5, audit-log.md 1 — log and audit counts are mentions.
  Restoring them was a value edit the F3 audit could not make, so it was handed to the A-lane and done there on 2026-08-26. All three are now in the portable body byte-for-byte, in §2 beside the hue that paints each (body lines 121 pink, 122 green, 123 blue) and again in §6 as recorded copy (body line 487) — two destinations each, per E2a. Nothing else on those three ramp lines changed: the ten-step hex ladders, the neighbouring ramps, and the per-band reading with its adjacent qualifier are untouched, and the counts a restoration cannot alter were re-measured after the edit and are unchanged (distinct hexes in the body 75 case-folded, `derived editorial implementation inference` 17, body length 556 lines). The strings are not added to provenance.md: they are published labels carried in the contract, not omissions, so the omission ledger has no row to hold and their provenance count stays 0. The gate false negative behind the loss stands as an E3 report — it is a defect in the needle extractor, not something this repair closes.
- **A1** — token survival checked with `grep -oE … \| sort -u`: 22 distinct `px` values in the legacy file, 22 in the portable body; 3 `ms` values, 3 in the body; 4 `%` values, 3 in the body and the fourth (`90%`, a competitor's imperative) in provenance; 76 distinct hexes case-folded, 75 in the body and the 76th (`#f8f9fa`) in provenance — 77 / 76 if `#FFFFFF` and `#ffffff` are counted apart, same single absence either way (audit 2026-08-26 added the fold, which the note had not stated).
  **Added 2026-08-26 (revision worker).** The `ms` and `px` sweeps never covered the one duration the legacy file writes in seconds, `(Swiper default 5s)`, and the first pass dropped it. Restored to the §2 Motion carousel rule; per file, `grep -oE '[^0-9.]5s' … \| wc -l` (the class excludes the `1.5s` shimmer, which is 3 in the legacy file and 3 in the body) reads legacy 1, portable `DESIGN.md` 1, `provenance.md` 1 (a ledger index entry). The `1.5s` shimmer is a separate value and was never absent — legacy 3, body 3 by `grep -oF '1.5s' … \| wc -l`. The 3 / 3 `ms` count above is unaffected: `5s` is not an `ms` value, which is how a unit-scoped sweep missed it.
- **A1a** — the six unitless line-heights (1.2, 1.3, 1.35, 1.5, 1.4, 1.0) are in the §3 type-role table as ratios, not converted to px.
- **A1b** — the source `type` is named on every component's Kind line; the five distinct primitives (`tab`, `button`, `card`, `input`, `badge`) all survive.
- **A3** — the only summary blocks deleted are the competitor comparisons, the three synthetic voice samples, and the `tech.day1company.io` follow-up instruction. Each was checked value by value for anything unique to it; the two numbers that were unique to the comparison prose, 34px and 32px, both have Fastcampus-owned slots and were moved there rather than dropped.
- **B1** — the legacy file contains `focus-visible` zero times. No `focus-visible` row in the portable file carries a treatment value; the source's derived 2px `#fc1c49` focus outline is kept in the state contract as its own observed-state line with its derivation named.
- **B3** — §2 Motion states the promotion condition with all five evidence kinds (transition properties, animation name, duration, easing, reduced-motion behaviour) and the per-component-observation gate, and rejects a single-curve confirmation as sufficient. This claim is written because that full sentence is in the body.
- **C1 / C2** — every state row was decided by control role. 7 state tables × 7 rows = 49 rows (`grep -c '^| State | Applicability | Reason |'` = 7; `grep -o '| not-applicable |' | wc -l` = 16, `grep -oE '^\| [a-z-]+ \| applicable \|' | wc -l` = 33). The 16 `not-applicable` entries sit on the two category pills, the carousel arrow, the dot cluster, the course card (3 each — loading, error, success, all destination or paging roles that commit nothing) and the search field (success alone, because a query resolves into the results region). No entry anywhere uses an absence of observation as grounds, and no reason string names a primitive kind in place of a role.
- **C3** — §4 Capture record says state coverage is not claimed complete.
- **C4** — the rank badge, the enrolment-count pill, the four tag chips, and the GNB container get `non-interactive` with a role reason and no applicability map, because the source assigns them no control role. The course card is the one component whose interactive kind was inferred rather than declared, and the inference is stated in place with its ground (the source's own hover treatment for it) and an adjacent derived-editorial qualifier.
- **D1 / D1a** — the Named gaps block lists only domains the source itself establishes: the two easing roles it names, the orange ramp step it records, and the seven items of its own "Inferred-but-not-live-captured" footer list. No further domain is enumerated. The Scope boundary names only course-detail, cart, payment, and footer, each of which the source names in that same footer list.
  **Corrected 2026-08-26 (revision worker).** This note was true of Named gaps and false of the Avoid block, and the F2 pass did not catch the gap between them. The last Avoid bullet read "course-detail, cart, payment, authenticated, or app surfaces" — a five-domain negative claim where the source establishes three. Measured over the legacy file with `grep -oiF … \| wc -l`, per file: `course-detail` 3, `cart` 3, `payment` 3, `authenticated` 0, and `app` as a whole word (`grep -oiE '\bapps?\b'`) 0. An unestablished domain named in a coverage clause asserts that the domain exists but is unresolved, which is the D1a shape. `authenticated` and `app` were struck; the three the source names stand. Measured over the portable body after the edit: `authenticated` 0, `\bapps?\b` 0. The body is 556 lines before and after, so no line pointer in this log, in `provenance.md`, or in `audit-log.md` moved.
- **D2** — the four personas are deleted from both files; no age band, buying pattern, or segment description is rehosted.
- **E1** — the portable body carries no Tier grading, no sidecar or ledger pointer, and no migration vocabulary. `node test-v2/tools/process-leak-check.mjs` reports fastcampus with zero hits. One term the checker does not hold was found and removed by the audit on 2026-08-26: §3 Assets read "Catalog logo pointer" and "recorded as the catalog's logo reference", which names this project's own catalog, not Fastcampus's course catalog — the two senses sit three words apart in the same file. It now reads "Logo pointer" and "this contract's logo reference"; the URL and the not-brand-artwork caveat are unchanged. The body's other 12 uses of "catalog" all mean the course catalog and stand. Left in place and reported rather than changed: the body says "the source" 55 times, which grades evidence class as E1 requires but names a document a standalone reader does not have.
  **Corrected 2026-08-26 (revision worker), provenance "Derived-editorial scope carried in the portable body".** That ledger listed 12 rows against a body carrying 17 qualifiers, so it under-scoped the very thing it exists to index — a narrow ledger fails E1 the same way a wide one does. Five occurrences had no row: the *fast + campus* reading (body 15), the yellow/orange accent-slot reading (128), the `~님` reservation (203), the course card's interactive kind (341), and the `~합니다` register assignment (503). Three of those five are the F3 audit's own additions of 2026-08-26 (15, 203, 503), which the audit made in the body without the ledger following — the audit could not have made the ledger edit itself, since it is barred from touching provenance tables beyond its two clauses, but nothing carried the change across afterwards. The ledger is now one row per occurrence with the body line number on each row, and the two lists are identical: `grep -noF 'derived editorial' DESIGN.md \| cut -d: -f1` gives 13 15 32 36 50 60 116 128 134 161 201 203 341 432 474 489 503, and the ledger's Body line column gives the same 17 in the same order. Per-file occurrence counts with `grep -oF 'derived editorial' … \| wc -l`, taken after this revision's own writing: `DESIGN.md` 17, `provenance.md` 3, `migration-log.md` 5, `audit-log.md` 3. Only the body's 17 are qualifiers standing in a contract; the counts in this log, in `audit-log.md`, and two of the three in `provenance.md` are **mentions** of the ledger or of the finding, not uses, and folding the four files into one number is what would make a missing qualifier look present.
- **E2a** — ten rows above name two or more destinations. Seven land in both files: YAML identity, `logo`, YAML `verified`, `tokens.rounded.full`, the §2 orange ramp's fifth step (provenance omission ledger + portable §7 Named gaps), the footer Verified/Tier block, and the footer inferred-token list; §11 Brand Narrative also lands in both, for its attribution. Three name several destinations inside the portable body: `tokens.spacing` (§2 + §5), the `#007aff` non-adoption (§2 Color rules + §1 Avoid), and §9 Accessibility (§2 + §4 + §5 + §6). Corrected by the audit, 2026-08-26: the note said "nine" while listing eight, it omitted the orange-ramp, `verified`, `tokens.spacing` and §9 rows, and it counted the sibling proof-file row, whose disposition is provenance only.
- **E2b** — the values held in the provenance omission ledger rather than dropped outright (`#fa a93f`, the two curves, `#f8f9fa` and the rest of the comparison set, the three voice samples) are logged as `분리 → provenance 생략 원장`, not as bare deletions.
- **E3** — no notation was distorted to satisfy a checker. Two places where a gate and the source disagreed were resolved by writing the honest form: the H2 size is written unitless because the legacy file's only `26` is unitless, and the corrupted orange step is quoted verbatim in provenance rather than repaired in the body.

## F1 pass (B2a scan)

The whole body was re-read sentence by sentence and each causal, interpretive, or evaluative sentence was classified. 17 adjacent derived-editorial qualifiers are now in place — occurrences, `grep -oF "derived editorial implementation inference" DESIGN.md | wc -l` = 17, one per passage, on body lines 13, 15, 32, 36, 50, 60, 116, 128, 134, 161, 201, 203, 341, 432, 474, 489, 503 — each in the complete form: *derived editorial implementation inference from the verified surfaces; not Fastcampus-authored or a separately published … specification*. They cover the two Scope readings, the brand-name and visual-evolution readings, the Audience grouping, the Distinctive-traits usage readings, all 5 Principles, all 8 Avoid items, the per-band hue reading, the yellow/orange accent-slot reading, the spacing scale, the motion set, the type-role readings, the `~님` reservation, the course card's interactive kind, the state contract, the layout density reading, the voice reading including the tone table, and the `~합니다` register assignment.

The count read 13 until the audit of 2026-08-26, which was already wrong for the body as written (15) and gained two more from the audit's own fixes. The three passages the audit qualified were all conventions asserted about surfaces the 2026-05-15 inspection did not cover, or readings of the brand's own name: the *fast + campus* framing in Scope, `~님` as reserved for instructor-facing surfaces in §3, and `~합니다` as reserved for legal, refund, and receipt screens in §6. Each already appeared under a qualifier elsewhere — the first beside the visual-evolution reading, the other two inside Principle 5 — so the standalone restatements were the gap, which is the shape B2a describes: the qualifier has to sit next to the sentence, not somewhere else in the file.

The pass also rewrote eight sentences that carried an interpretation without saying so, in the direction of the documentary fact rather than adding a qualifier: the negative DS-search result no longer generalizes to "the only form in which the system is available"; the error-ramp rule now names both of the source's statements instead of only one; the two contrast sentences no longer assert "therefore" and "which is why"; the card seam, the split-surface arrow pair, the flat-depth reading, the art-direction description, and the rank-badge "does the work" line are now attributed to the source rather than asserted. In the other direction, no qualifier was attached to a documentary or measured fact: the token values, the 103 custom properties, the contrast measurements, the recorded copy, and the inspection dates stand unqualified.

## F2 pass (E2 cross-check)

Every table row above was written after grepping the destination files. The counts cited are `grep -o … | wc -l` or `grep -c` over a named block, and each states what was counted. Re-measured on 2026-08-26 after the audit's edits, and unchanged by them except where noted: 76 / 75 distinct hexes case-folded (whole file, `sort -u`; 77 / 76 unfolded), 22 / 22 distinct px, 3 / 3 ms, 4 / 3 + 1 percent values, 6 unitless line-height ratios, 9 Key Characteristics → 9 Distinctive traits, 5 → 5 Principles, 6 Don'ts → 8 Avoid items, 16 + 1 legacy component blocks → 11 declarations + 1 state, 12 → 12 neutral-ladder rows, 9 → 9 type-role rows, 9 → 9 tone-table rows, 11 → 13 state bullets, 12 → 12 state-catalog rows, 7 state tables / 33 applicable / 16 not-applicable, `#fa a93f` in provenance 1 and in the body 0, `f9a93f` in the body 0 and in provenance 1, `cubic-bezier` in the body 0 and in provenance 2.

Changed by the audit: the derived-editorial qualifier count is 17, not 13 (see the F1 pass); the copy-needle line is no longer cited as evidence of A5 conformance, because the 41-needle extraction is blind on the extended-hue-ramp lines and three published rail labels were in fact absent (see the A5 note); and three destination claims were corrected — the `verified` date's second destination, the §7 Motion row, and the §11 Brand Narrative row.

Changed by the A5 repair, 2026-08-26: the three absent labels are now in the portable body, so the §2 Color Palette & Roles row is a two-destination row and the A5 note records a restoration rather than a loss. Re-measured after the repair, per file and with `grep -oF … \| wc -l` — legacy `web/references/fastcampus/DESIGN.md` 1 / portable `DESIGN.md` 2 / `provenance.md` 0 for each of the three. The `migration-log.md` and `audit-log.md` counts are **mentions of the finding**, not uses in a contract body, and are not added to the body total; folding the four files into one count is what makes a still-missing string look restored. Counts a copy restoration cannot move were re-taken and are unchanged: 75 distinct hexes case-folded in the body, 17 qualifier occurrences on the same body lines as before (13, 15, 32, 36, 50, 60, 116, 128, 134, 161, 201, 203, 341, 432, 474, 489, 503), 556 body lines. The restored text sits inside existing lines, so no pointer in this log or in `audit-log.md` moved.

Changed by the revision of 2026-08-26 (semantic review, grok-4.6, five confirmed FAILs; rulebook v10). Values and tables were not touched, the F3 audit's 13 fixes and the A5 restoration's 3 stand, and the `#fa a93f` handling was left exactly as the audit verified it.

- **A1, the `5s` interval.** Restored to the §2 Motion carousel rule and logged on its owning row (§15 Motion rules), with the §7 Motion row's "absent on the same footing" reading marked superseded. The failure worth naming: the previous log recorded a disposition for a value that was in no output file, so the log and the body disagreed while both read as complete. Per file, `grep -oE '[^0-9.]5s' … \| wc -l` (the class excludes the `1.5s` shimmer, which is 3 in the legacy file and 3 in the body): legacy 1, portable `DESIGN.md` 1, `provenance.md` 1.
- **D1a, two invented domains.** `authenticated` and `app` struck from the last Avoid bullet; `course-detail`, `cart`, `payment` kept. Legacy occurrences, per file: 3 / 3 / 3 against 0 / 0. Recorded in the D1 rule note above.
- **E1, ledger under-scope.** The derived-editorial ledger went from 12 rows to 17, one per body occurrence, with body line numbers. Recorded in the E1 rule note above.
- **E2c, the completeness claim.** The ledger's lead-in read "This ledger records the full extent so the two files agree" while the ledger held 12 rows against 17 body occurrences — a claim of exhaustiveness made by a document that was not exhaustive, which is the same shape as the A5 note's "every published string survives" the audit had already corrected in this file. It now states the measurement, gives the per-file counts, and says what the measurement does **not** bound: that the two lists agree on 17 occurrences is not a warrant that every interpretive sentence in the body has been found. The F1 and F3 passes carry that judgement, and the F3 audit found three the worker had missed.
- **F3, sync after audit.** The audit's three body additions (lines 15, 203, 503) never reached the ledger. Now rowed and attributed to the audit in place. The audit record itself was not rewritten: the correction is appended under its own dated heading, alongside the A5 restoration worker's closure note that already followed that convention.

Counts a revision of this shape cannot move were re-taken and are unchanged: 556 body lines, 17 qualifier occurrences on the same body lines as before (13, 15, 32, 36, 50, 60, 116, 128, 134, 161, 201, 203, 341, 432, 474, 489, 503), 75 distinct hexes case-folded in the body, 22 distinct `px`, 3 `ms`, 12 state-catalog rows, 9 tone rows, 33 applicable / 16 not-applicable. Both edits to the body sit inside existing lines, so no pointer in this log, in `provenance.md`, or in `audit-log.md` moved.

Gates: `node test-v2/tools/process-leak-check.mjs` → fastcampus absent from the leaking list, zero hits (E1). The `--gate-only` and `portable_core` results are recorded in the run output but are not restated here as evidence of conformance: this same gate returned PASS over a body that was missing three published labels, so a PASS bounds nothing this log claims — including the A5 repair above, which was verified by per-file `grep -oF` and not by the gate.
