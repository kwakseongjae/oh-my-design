# Dell migration log

Rulebook version used: **v9** (`docs/design-md-weight/MIGRATION_RULEBOOK.md`, 2026-08-26 — includes A5). The dispatching prompt named v8; the rulebook file had already moved to v9 before this run started, and v9 is what was read and applied. Recorded here rather than silently claiming the version the prompt asked for.

Source: `web/references/dell/DESIGN.md` (legacy, omd 0.1) — not modified.
Destination: `docs/design-md-weight/migrated/dell/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/dell/provenance.md`
Date: 2026-08-26

Gate: `node migrate-reference.mjs --brand dell --gate-only` → PASS, problems `[]`.

Every row below was checked by grepping the actual output files before it was written; a value that landed in two files has both destinations recorded.

F3 note: a separate-session B2a·E2 audit re-checked these rows against the three files and corrected the ones that did not hold. See `audit-log.md` in this directory for the list.

## Disposition by legacy section

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML `id` `country` `category` `homepage` `primary_color` `logo` | 분리 → provenance (Identity) | Portable file carries no frontmatter. The `logo` record has **one** destination: `logo.type: simpleicons` / `slug: "dell"` is carried only in provenance Identity. `DESIGN.md` §3 Assets carries no part of that value — it carries a non-promotion disclosure ("The catalog logo record for this reference is a third-party icon-set entry and is held in provenance, not promoted here as a Dell-supplied asset file"), which is a statement about the record, not a second copy of it. |
| YAML `name` | 옮김 → H1 `# Dell Design System` **and** 분리 → provenance (Identity) | Two destinations. |
| YAML `verified` `added` `omd` | 분리 → provenance (Freshness, Identity) | freshness/원장. |
| YAML `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance (Identity + Source token ledger) **and** 옮김 → DESIGN.md §1 Scope | A1c: verification metadata is a value. `prose-derived` also appears in the portable Scope because a standalone reader needs the evidence class to read the palette correctly. Two destinations. |
| YAML `tokens.colors` (20 values) | 옮김 → §2 Semantic color **and** 분리 → provenance (Source token ledger, verbatim) | Two destinations. Every hex is in both files; the ledger keeps the byte-level YAML form. |
| YAML `tokens.typography` (family + 11 roles) | 옮김 → §3 Families / Type roles **and** 분리 → provenance ledger | A1a: unitless lineHeights `1.17 1.22 1.29 1.36 1.44 1.50 1.43 1.38 1.33` survive as ratios in the §3 table's `56px (1.17)` form, never converted to px alone. Two destinations. |
| YAML `tokens.spacing` (unitless 4…64) | 옮김 → §2 Spacing (px form) **and** 분리 → provenance ledger (unitless YAML form) | Two destinations. The source carries both forms; both are preserved. |
| YAML `tokens.rounded` | 옮김 → §2 Shape **and** 분리 → provenance ledger | `md: 8` and `lg: 8` are the same value under two names; §2 states 8px once and the provenance ledger note records the duplication. |
| YAML `tokens.shadow` (4 values) | 옮김 → §2 Elevation table **and** 분리 → provenance ledger | Two destinations. |
| YAML `tokens.components` (16 records) | 옮김 → §4 per component **and** 분리 → provenance ledger | A1b: every ledger `type:` — `button` `input` `card` `badge` `tab` `toast` `dialog` `toggle` — is carried as a per-component `Type (source)` line (9 such lines in §4; the five `button` records collapse into one Button with five variants). `Kind:` is a separate line and never replaces the type. |
| §1 Visual Theme & Atmosphere | 옮김 → §1 Scope, Distinctive traits | Scope, the "trustworthy, efficient, and calmly authoritative" and "commerce-grade clarity" characterisations, and the Key Characteristics list. The atmosphere sentences that only restate the palette are folded into the traits list rather than repeated. |
| §2 Color Palette & Roles | 옮김 → §2 Semantic color (Primary / Brand / Semantic / Neutral scale / Surface and borders) | All 20 distinct §2 hexes plus `rgba(14, 27, 44, 0.6)`; migrated §2 carries 21 because the A4 note also names the success-toast field `#0A5A0A`, which the legacy declares in §4. A4 note added: component-level fields (`#6B7480` label column, `#22262B` value column, `#0A5A0A` success-toast text) stay bound to their component and are not merged into general ink roles. |
| §3 Typography Rules | 옮김 → §3 Font evidence / Families / Type roles / Type rules | Includes the `-0.5px` / `-0.25px` / `0.2px` / `+0.5px` tracking values and the Price Display `tight` line height. |
| §4 Component Stylings | 옮김 → §4 Button, Text Input, Product Card, Configuration Card, Tabs, Toggle, Badge, Spec Table, Promo Card, Toast, Dialog | Configuration Card and Spec Table exist only in the §4 body, not in the token ledger, so they carry no `Type (source)` line. |
| §4 Input `Focus:` treatment (`#0076CE` border, `0 0 0 3px rgba(0,118,206,0.2)` ring) | 옮김 → §4 Text Input as a separate source-labelled row | B1: the source labels it `Focus`, never `focus-visible`. The `focus-visible` row stays applicable with its treatment unresolved, and carries no color value. |
| §4 `**Tier 1 sources:**` line | 분리 → provenance (Sources, Evidence-class conflict) **and** 옮김 → §1 Scope + §3 Font evidence | Two destinations. The live-DOM `getComputedStyle` sentence conflicts with the footer's "not text-extractable / reasoned" statement and with `tokens.source: prose-derived`; provenance records the conflict, and the portable body states the weaker class. No value was deleted because of the conflict. |
| §5 Layout Principles | 옮김 → §5 Grid and container, Density **and** §2 Spacing / Shape | Two destinations: the spacing system and the border-radius scale are foundation values, the grid/container/whitespace material is layout. |
| §6 Depth & Elevation | 옮김 → §2 Elevation (table + shadow philosophy + blur) | Five levels, the `rgba(17,20,26,…)` rule, the "this floats above the page" phrasing, and the masthead/mega-menu blur behaviour. |
| §7 Do's (7 items) | 옮김 → §2 Rules | Value-application rules belong to the `foundations` claim, which is declared `kind=rules-or-constraints`. Not placed in Governance, whose prose is controlled copy. |
| §7 Don'ts (7 items) | 옮김 → §1 Avoid | Carried as seven prohibitions plus one migration-scope prohibition (do not treat reasoned values as extracted Dell tokens), making eight. |
| §8 Responsive Behavior | 옮김 → §5 Breakpoints, Touch targets, Reflow and collapsing, Image behavior | Breakpoint table, `48px`/`40px`/`44px` targets, collapsing strategy, and image behaviour including the `24-32px` partner-logo height. §5 Platform boundary states the extent of that evidence and, since the 2026-08-26 revision, names no unrecorded platform profile of its own (see the revision summary, D1a). |
| §9 Agent Prompt Guide — Quick Color Reference | 삭제 | Restatement of §2 with no unique value. Verified mechanically: the token bag of §9 minus the rest of the document is empty, so §9 holds no hex, px, ms, or pct value that does not already exist elsewhere. A3 satisfied without an orphaned value. |
| §9 Example Component Prompts (5) | 삭제 | Copy-paste prompt wrappers. Same emptiness check applies; every value they quote is in §2/§3/§4/§5. |
| §9 Iteration Guide (7 items) | 삭제 | Tool-facing restatement. Each of the seven rules already exists as a §7 Do or Don't or a §3 principle, and all seven are present in the migrated §2 Rules, §1 Avoid, or §3 Type rules. Nothing is delegated to a skill or adapter. |
| §10 Voice & Tone | 옮김 → §6 Content & Locales | Voice characterisation, the 7-row context/tone table, and the forbidden-pattern list. A5: every quoted string — `"Add to Cart"`, `"Customize & Buy"`, `"Shop deals"`, `"Compare"`, `"XPS 13 Laptop"`, `"Alienware m18 R2 Gaming Laptop"`, `"16GB LPDDR5x 7467MT/s"`, `"Power your possible"`, `"Built to do more"`, `"Enter a valid ZIP code to check delivery"`, `"Oops"`, `"In Stock. Ships in 1-2 business days."`, `"$1,299.99 · Save $200"`, `"might be available"`, `"around $1,300"`, `"the best laptop ever"`, `"Whoops"`, `"16GB DDR5"`, `"fast, future-ready memory"` — is carried byte-for-byte, without paraphrase or case change. |
| §11 Brand Narrative | 옮김 → §1 Scope (narrative paragraph) and §1 Principles | 1984 / Michael Dell / University of Texas / PC's Limited origin, the Inspiron·XPS·Alienware·G series·Latitude·OptiPlex·Precision·Dell Technologies range, the roundel with the tilted "E", and the recorded refusals (opacity in pricing, decorative excess, friction in configuration, `"interested"` → `"ordered"`). The "order desk" reading is carried with its editorial qualifier adjacent. **Third destination:** the §11 scale sentence ("a student buying a `$500` laptop and an enterprise IT director provisioning thousands of workstations") lands in §1 Audience. The `$500` figure was dropped in the first pass — the F3 audit recorded the loss under "Out of scope, observed, not corrected" — and was restored in the 2026-08-26 revision (A1). `$400` and `$4,000` from §1 were never lost; they are in §1 Scope. |
| §12 Principles (8) | 옮김 → §1 Principles | All eight preserved, headed by the B2a qualifier. |
| §13 Personas (3 archetypes) | 삭제 | D2. The source itself labels them fictional archetypes. No demographic and no biography is promoted, and none is re-recorded in provenance. Verified: the token bag of §13 minus the rest of the document is empty, so no unique value is lost with them. The only product-surface string they contained is the button label `"Save Quote"`; it is kept in this log and in the provenance omission ledger rather than promoted from a fictional context — its two destinations are those two files, not `DESIGN.md`. The F3 audit correctly refused to let this row claim a clean §13 disposition while `DESIGN.md` §7 Named gaps still listed a `business-portal` surface, since `business portal` occurs in the source **only** inside the §13 Diane sentence ("Provisions … in bulk via Dell's business portal") — the same fictional sentence whose `"Save Quote"` this row already held out of the body. That flag is now discharged: the 2026-08-26 revision removed `business-portal` from the Named gaps bullet, so `business portal` occurs 0× in `DESIGN.md` (grep-verified) and is dispositioned exactly like `"Save Quote"` — recorded in this log, absent from the portable body. The claim this row makes is therefore true as written again. |
| §13 note "Personas below are fictional archetypes…" | 삭제 | The disclaimer goes with the material it disclaims. Its substance — no invented individuals — is stated in §1 Audience instead. |
| §14 States (12 rows) | 옮김 → §4 "Declared product-level state contract" (본문 보존) **and** §4 per-component applicability tables | A2. The 12-row table is preserved in full in the portable body, with every value including `1.2s shimmer`, `8% white highlight`, `4s auto-dismiss`, and the `$0` skeleton rule. Two destinations: the same contract is also cited as the reason text in the per-component tables. Nothing is deferred to a graph. |
| §15 Motion — durations (5 tokens) | 옮김 → §2 Motion | `0ms` `120ms` `240ms` `360ms` `300ms` retained. The approved verdict scopes deletion to unsourced curves; duration is explicitly out of scope. |
| §15 Motion — reduced motion | 옮김 → §2 Motion | `prefers-reduced-motion: reduce` collapse-to-instant rule retained verbatim in substance. |
| §15 Motion — signature motions (4 of 5) | 옮김 → §2 Motion | Cart flyout, live price recalc, card hover lift, mega-menu open retained with their duration tokens. The fifth numbered item was the reduced-motion rule and is carried under Reduced motion. |
| §15 Motion — 4 easing tokens and curves | 삭제 → recorded in provenance omission ledger | Unsourced curves. Three of the four — `cubic-bezier(0.0, 0.0, 0.2, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, `cubic-bezier(0.4, 0.0, 0.2, 1)` — are byte-identical to the template example rows at `spec/omd-v0.1.md:266`, `:267`, `:268`; `cubic-bezier(0.2, 0.0, 0, 1)` is not in that table, so template copying is not established for it. §2 Motion and the omission ledger both state three. The token names are removed with them because they carried nothing else and Core v2 forbids referencing an unresolved token. E2b: the omission ledger holding them is itself a disposition and is recorded as such. |
| Footer `OmD v0.1 Sources — Dell` comment | 분리 → provenance (Sources, Evidence-class conflict, Interpretive-claim disclosure) **and** 옮김 → §1 Scope evidence-class paragraph | Two destinations. The source's own "interpretive claims are editorial readings" disclosure is restated in the portable body adjacent to the affected sentences, per B2 — the qualifier does not live only in provenance. |

## Rule compliance, stated only where the text actually exists

| Clause | Where the full text is |
|---|---|
| A1a | §3 Type roles column `Line height` renders every unitless ratio, e.g. `56px (1.17)`, `20px (1.43)`; §3 Type rules keeps `1.38` as a ratio in prose. |
| A1b | Nine `Type (source):` lines in §4. |
| A1c | provenance Identity table (`tokens.source`, `tokens.extracted`, `components_harvested`, `omd`) plus the verbatim YAML ledger. |
| A3 | §9 deletion justified by an executed token-bag difference, not by assertion. |
| A4 | §2 closing paragraph names `#6B7480`, `#22262B`, `#0A5A0A` as component-bound fields. |
| A5 | §4 state contract, §4 Badge/Button/Toast use columns, and §6 carry every quoted Dell string unchanged; `"Save Quote"` is the one quoted product string held outside `DESIGN.md`, and this row is its record. |
| B1 | §4 Text Input carries a separate `Focus` row; §4 "How to read the state tables below" states the evidence-class separation; no `focus-visible` row in the document carries a color value. |
| B2 / B2a | 19 adjacent qualifiers in `DESIGN.md`, each using the full form "derived editorial implementation inference … not Dell-authored or a separately published UI specification", covering Scope boundary, brand-history reading, scope reading, Primary tasks, Audience restriction, Distinctive traits, Principles, Avoid, Foundations value classes, unmerged fields, elevation reading, font-evidence reading, ratio-versus-px reading, Assets, the whole of §4's role/kind/applicability judgments, Badge role, Spec Table role, Density, and Content voice/contract. The easing-removal paragraph in §2 Motion is **not** one of the 19: it is a migration disposition and carries its own differently-worded qualifier ("a judgment about this reference's evidence, not a Dell statement about Dell's own motion"). |
| B3 | §2 Motion, final paragraph: names all five evidence kinds — "transition properties, animation name, duration, easing, and reduced-motion behavior" — with the per-component computed-observation gate and the explicit rejection of a single documented curve or duration as sufficient. |
| C1 | No `not-applicable` row in the document uses non-observation as its reason. Missing evidence omits a treatment only. |
| C2 | Applicability is judged per role. Three `not-applicable` rows exist, all on Tabs (loading, error, success), each citing the source's own routing of those states to the panel, the inline field, the toast, and the page-blocking or confirmation page. |
| C3 | §4 ends its reading note with "This is not a complete state-coverage claim." |
| C4 | Promo Card, Toast, and Dialog declare no kind and no applicability map; each says why the source gives no basis for deciding one. |
| D1 | No negative claim in the document introduces vocabulary the source lacks; the gate's alien-negative-claim check is clean. |
| D1a | Since the 2026-08-26 revision, no noun-phrase gap list opens a domain the source does not establish. Every noun in §7 Named gaps and in §5 Platform boundary was grep-checked against `web/references/dell/DESIGN.md`: `account` ×3, `quote` ×4, `service tag` ×2 (+ `service-tag` ×1), `Alienware` ×3 — all with occurrences outside §13. The terms that had 0 source occurrences were removed: `authenticated`, `business portal`/`business-portal`, `iOS`, `Android`, `desktop app`, `zoom`, `overflow`, `reading order`. Verified 0× in `DESIGN.md` after the edit. One residual: the §5 H3 heading `Reflow and collapsing` uses `reflow`, which is 0× in the source (the source heading is `Collapsing Strategy`). It labels present collapsing content rather than opening an unresolved domain, and section structure was out of this revision's mandate — recorded here for the lane that owns headings. |
| D2 | §13 deleted; nothing from it in provenance. Clean for the portable body as of the 2026-08-26 revision: `business portal` — the only §13-exclusive product-surface phrase that had reached the body — is now handled identically to `"Save Quote"`, held in this log and absent from `DESIGN.md`. See the §13 row above. |
| E1 | Ledger, freshness, evidence conflict, and omission record are in provenance; the qualifiers needed to read the document standalone stay in the body. |
| E3 | No value notation was altered to dodge a check. The gate reported no false positive on this reference. |

## Revision — 2026-08-26, wave 20 (semantic review FAIL ×4 + one value loss)

Rulebook v9. Edits confined to two places in `DESIGN.md` — §1 Scope boundary and §7 Named gaps — plus the §5
Platform boundary paragraph they mirror, and one restored figure in §1 Audience. Token values, component tables,
state applicability, section structure, and `web/references/dell/DESIGN.md` were not touched, and no qualifier or
correction inserted by the F3 audit was reverted.

| # | Finding | What was actually wrong | Fix |
|---|---|---|---|
| 1 | **D1a** — `business-portal` opened as an unresolved domain | `business portal` occurs 1× in the source, at `web/references/dell/DESIGN.md:563`, inside the §13 Diane persona sentence. §13 is a deleted section, so the phrase establishes no domain that could be "present but unresolved". Grep-confirmed the three siblings do have non-§13 evidence and were kept: `quote` at `:164` (tabular figures for quote tables), `:247` (quote forms), `:311` (quote line items); `service tag(s)` at `:144` (monospace role) and `:264` (read-only service tag); `Alienware` at `:527` (product-name copy) and `:540` (§11 brand range). | `business-portal` deleted from the Named gaps bullet. `Authenticated` went with it in the same edit: `authenticated` is 0× in the source, which the F3 audit had already flagged under "Out of scope, observed, not corrected", and it is the identical defect. Both now 0× in `DESIGN.md`. |
| 2 | **D2** — self-contradiction on the same source sentence | This log already held `"Save Quote"` out of the portable body because it comes from a fictional persona, then let `business portal` — from the *same* Diane sentence — stand in the body. One sentence, two standards. | Same disposition as `"Save Quote"`: removed from the body, recorded in this log only. Not added to the provenance omission ledger, because it is not a value — it is a scope noun the migration should never have minted. The §13 row and the D2 compliance row above were rewritten to say so. |
| 3 | **D1a** — a native-app profile invented for a web-only sample | §5 Platform boundary and the §7 Named gaps platform bullet enumerated `iOS`, `Android`, `desktop app`, `zoom`, `reflow`, `overflow`, and `reading order`. All 0× in the source (grep). Enumerating them asserts those profiles exist for Dell and merely went unrecorded — the databricks/datadog `native-client` defect, and among the 104 migrated references this wording was dell's alone. | The enumeration is gone from both places. §5 Platform boundary now states only what the source records — web breakpoints, touch targets, collapsing behaviour — and says naming a further value would itself be outside the evidence. The Named gaps bullet points at §5 instead of listing profiles. The §5 H3 `Reflow and collapsing` still carries `reflow`; heading structure was out of mandate, and it is recorded in the D1a compliance row above rather than quietly edited. |
| 4 | **Reverse error** — §7 demoted surfaces that §4 documents | Named gaps called account, quote, and service-tag surfaces "outside the public commerce scope", while §4 carries the source's own values for exactly those contexts: `tab … use: "PDP sections, account dashboard"` (source `:71`, `:357`; migrated `:365`), `input … "search, account fields, quote forms"` (`:247`; migrated `:309`), the disabled input for a `read-only service tag` (`:264`; migrated `:311`), the spec table for `quote line items` (`:311`; migrated `:413`), `dialog … "Configuration confirm, cart review, sign-in"` (`:73`, `:388`; migrated `:440`), the monospace role for `spec codes, part numbers, service tags` (`:144`; migrated `:207`), and tabular figures for `quote tables` (`:164`; migrated `:232`). A derived scope reading had inverted established source facts into gaps. | **Both halves of the rulebook's choice, because the evidence supports both and neither alone is honest.** The Scope boundary now names those contexts as covered *to the extent of the values §3 and §4 record against them* — that is the accurate statement, since the source does attach real component values to them. The gap was then narrowed rather than deleted, to "further tokens … past the component values the source already records", because the source genuinely records no dedicated screen, route, or token set for an account dashboard or a quote flow. Deleting the bullet outright would have implied full coverage of those surfaces; keeping it as written kept a claim the same document contradicts. The B2a full-form qualifier on the Scope sentence was preserved and its subject clause widened to match; body qualifier count is unchanged at 19. |
| + | **A1 value loss** (below judgment threshold, flagged with the findings) | §11's scale sentence reached §1 Audience as "a student buying a laptop"; the source (`:540`) says "a student buying a `$500` laptop". The F3 audit had listed this under "Out of scope, observed, not corrected". | `$500` restored in §1 Audience. `$400` and `$4,000` were never lost (§1 Scope). The §11 row above now records Audience as a third destination. |

### Ledger re-verification (F2, full pass)

Every pointer in this log and in `provenance.md` was re-checked by grep after the edits, not carried forward on trust.

- **External line refs.** `spec/omd-v0.1.md:266` = `cubic-bezier(0.0, 0.0, 0.2, 1)`, `:267` = `cubic-bezier(0.4, 0.0, 1, 1)`, `:268` = `cubic-bezier(0.4, 0.0, 0.2, 1)` — all three confirmed byte-exact; `:269` is `ease-spring`, so `cubic-bezier(0.2, 0.0, 0, 1)` is still correctly recorded as *not* a template match. No line ref moved: the source and the spec were not edited.
- **Counts.** `Type (source):` lines = 9 (A1b row). Full-form B2a qualifiers = 19, all 19 carrying the complete evidence-class tail (13 read "it is not Dell-authored…", 5 "they are not…", 1 "none of them is…"); the easing-removal paragraph is still excluded and still carries its own wording. `not-applicable` rows = 3, all on Tabs. Distinct §2 hexes = 21 against 20 in the legacy §2, the extra being the A4-noted `#0A5A0A`. Every count in the compliance table above holds as written.
- **Values spot-checked present after the edits.** `1.2s`, `8% white`, `4s auto-dismiss`, the `$0` skeleton rule, `0ms`/`120ms`/`240ms`/`360ms`/`300ms`, `prefers-reduced-motion: reduce`, `24-32px`, `1280px`, `#0A5A0A`, `#6B7480`, `#22262B`, `rgba(14, 27, 44, 0.6)`, `PC's Limited`, `University of Texas`, `Michael Dell`. `simpleicons` remains 0× in `DESIGN.md` and `"Save Quote"` 0× in `DESIGN.md` / present in this log and the provenance ledger — the two single-destination claims the F3 audit corrected still hold.
- **Line numbers cited in this revision** are `DESIGN.md` line numbers *after* the edits; the four §4 pointers in finding 4 were re-read at their post-edit positions.

### Gate

`cd test-v2/tools && node migrate-reference.mjs --brand dell --gate-only` → **PASS**, problems `[]`, before and after the revision.

E3: no notation was altered to dodge a check, and the gate reported no false positive on this reference. Worth
recording for the rulebook lane rather than for this file: findings 1–4 were all gate-PASS defects. D1a's own text
predicts this — the mechanised D1 check triggers on negative *sentences*, and all four defects lived in noun-phrase
lists and in a scope reading that contradicted the document's own tables. Nothing here argues the gate is wrong;
it argues these four remain sol-lane work.

### D1a 잔여 처리 (2026-08-26, 오케스트레이터)

개정 워커가 자기 권한 밖으로 보고한 잔여를 처리했다: §5 소제목 `Reflow and collapsing`의
`reflow`는 원본에 **0회**이고(실측 `grep -ic reflow` → 0), 원본의 대응 소제목은
`### Collapsing Strategy`다. 원본이 세우지 않은 도메인 어휘가 소제목에 남으면 본문에서
지운 의미가 제목으로 되돌아온다 — 같은 D1a 결함이다.

처분: 소제목을 `### Collapsing strategy`로 정정. 값·표·applicability·절 구조는 불변이며
H3 하나의 문자열만 바뀌었다.
