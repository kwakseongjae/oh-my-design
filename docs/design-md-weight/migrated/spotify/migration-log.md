# Spotify migration log

Source: `web/references/spotify/DESIGN.md`
Destination: `docs/design-md-weight/migrated/spotify/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/spotify/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd, `portable_core: true`, placeholders 0)

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; `primary_color` 옮김 → Foundations newsroom/editorial Spotify Green | Portable file has no frontmatter. Name kept as H1 `Spotify Design System`. Homepage `https://www.spotify.com` is identity-only in provenance (E2a: not dual-destination). Catalog `primary_color` `#1ed760` is dual identity metadata + portable Foundations Spotify Green, and is not a Web Player control fill (E2a). Catalog logo type `simpleicons` / slug `spotify` is identity-only in provenance; it is not a portable Typography & Assets mark (E2a: not dual-destination). |
| YAML `omd`, `verified`, token claims, `tokens.source` / `extracted` | 분리 → provenance | 출처 원장·freshness·Proof. `tokens.source: reconciled` is metadata (A1c). No `ds.type` on source; none invented. |
| YAML `components_harvested: false` | 옮김 → Components & States capture record; 원장 분리 → provenance | Dual destination (E2a): portable capture record keeps `YAML components_harvested is false`; provenance keeps the same flag. |
| YAML `verification_v2` (surfaces/sources) | 분리 → provenance; 세 capture URL 옮김 → Experience Scope | Dual destination (E2a): `https://open.spotify.com/`, `https://open.spotify.com/search`, `https://newsroom.spotify.com/company-info/` are portable Scope capture coverage AND provenance surfaces/sources. Design-history URL `https://newsroom.spotify.com/2026-04-23/spotify-design-history/` and developer-guidelines URL `https://developer.spotify.com/documentation/design` are provenance (YAML sources + footer Tier 1). They are not a §1/§11 source-row. Claims/conflicts stay provenance. Surface objects keep `id`/`url`/`inspected` only; surface `kind` is omitted (A1). Source-row `kind` (`product-surface` / `official-doc`) is preserved on the sources table. |
| YAML `tokens.colors` / `typography` / `rounded` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. `#1ed760`를 player control fill과 합치지 않음. Search `#ffffff`와 Foreground, Search `#1f1f1f`와 Control dark는 원본이 같은 관측으로 묶은 필드로 유지. Outline `#7c7c7c`는 compact outlined action의 renderable border이며 일반 player ink가 아님 (A4). 검증된 primitive type은 컴포넌트별로 보존: Player Search Field `Type: input`, Outlined Compact Action `Type: button`, Circular Icon Control `Type: button`, Filled Newsroom CTA `Type: button`. Hollow Newsroom CTA has no source primitive type — no `Type: button` (A1/A4). `Kind: interactive`로 뭉개지 않음 (A1b). YAML에 unitless lineHeight 없음; 비율 발명 없음. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(Web Player home / `/search` / company-info newsroom; authenticated-player layout audit 아님), player vs newsroom 분리, 2026 design-history의 bright green·early dark-mode·Spotify Mix. 세 capture URL은 provenance와 dual (E2a). content-led / geometry-repeatedly-resolves 문단과 design-history 인과문 (`That account explains the recognizable expression`)은 인접 완전 B2a (B2/B2a). |
| YAML sources / footer Tier 1 공식 히스토리·developer URL | 분리 → provenance | 서사 출처. 본문은 기사 이름으로 인용하고 두 URL은 portable에 없음. §1과 §11 본문에는 literal URL이 없다. 해당 URL은 YAML `verification_v2.sources`와 footer Tier 1에 있다. 본문은 토큰이 아니라고 밝힌 경계를 Experience에 유지. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color + Named gaps | Player `#1f1f1f` `#ffffff` `#b3b3b3` `#7c7c7c`; newsroom `#1ed760`; opaque product-canvas / semantic-error / warning / shadow 생략 제약. Dual destination (E2a): Foundations Semantic color omission sentence + Governance Named gaps (opaque product-canvas, semantic-error, warning, shadow). |
| §3 Typography Rules | 옮김 → Typography & Assets + Named gaps | 여섯 증거 등급, SpotifyMixUI 665 / SpotifyMixUITitle 7 / Spotify Mix 106 / Spotify Mix Narrow 21, `encore.scdn.co`, declared-only CircularSp-* / Spotify Circular / Mix Wide / SpotifyMixUITitleVariable, partner stack Helvetica Neue / Helvetica / Arial. Dual destination (E2a): Typography Font evidence / Family + Governance Named gaps (public license grant; declared-only faces as UI families). 미확인 패밀리 대체 금지. YAML body 16/400 "Product-player body and input text", action 14/700 "Compact player action labels", title 24px/700. |
| §4 Component Stylings | 옮김 → Components & States | Player Search Field (`Type: input`), Outlined Compact Action (`Type: button`), Circular Icon Control (`Type: button`), Filled Newsroom CTA (`Type: button`). YAML 기하와 본문 기하를 같은 슬롯에 유지. Player hover/focus/pressed는 관측만; 미기록 값 추론 없음. Newsroom filled Focus `#25d564`/`#1ed760`, Pressed `#26d966`/`#1dcf5c` 보존. Capture selector는 provenance. |
| §4 Hollow newsroom CTA | 옮김 → Components & States; selector 분리 → provenance | Dual destination (E2a). YAML `tokens.components`에 없음. `#1ed760` text/border, 100px, 16px 33px 15px, 14px/800 Spotify Mix, Focus `#1dd35e`, 246px by 50px, `surface-3::[data-omd-capture="10"]`. Source body has no primitive type. Portable Hollow has Kind: interactive and no `Type`. Filled sibling `type: button` is not copied (A1/A4). |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance; 세 capture URL 옮김 → Experience Scope | Dual destination (E2a): footer Tier 1의 `https://open.spotify.com/` · `https://open.spotify.com/search` · `https://newsroom.spotify.com/company-info/`는 같은 dual Scope + provenance surfaces. Design-history와 developer-guidelines Tier 1은 provenance (same URLs as YAML sources). Tier 2 getdesign.md / refero는 numeric token 비승격 유지. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Experience Scope + Avoid + Named gaps | Dual/quad destination (E2a): authenticated-player layout-audit 부정과 sidebar/grid/player-bar/breakpoint/card-elevation 미추론은 Scope capture boundary + Avoid + Layout & Platforms + Governance Named gaps. compact top-bar search and 32px/48px controls는 Layout. Partner artwork 4px small/medium · 8px large는 partner 규칙이며 Web Player 토큰이 아님. Layout 문단 인접 완전 B2a (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation + Named gaps | Dual destination (E2a): Foundations Elevation (`box-shadow: none`; no reusable elevation token) + Governance Named gaps (reusable spacing scale or elevation token). Elevation 판단 문장 인접 완전 B2a (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. Governance 통제 문구에 넣지 않음. capture-bound는 Principles 머리의 derived 한정 범위에 포함. |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. newsroom green CTA를 player control로 쓰지 말 것, artwork 변형 금지, MixUI 시스템폰트 치환 금지. Avoid 머리 인접 완전 B2a가 source Don'ts와 추가 capture-bound 판단(layout/motion)을 덮음 (B2/B2a). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | No viewport capture. 유일한 잔류 규칙은 partner artwork 4px/8px. 원본에 없는 최소폭 값을 발명하지 않음. Cross-viewport 판단은 Layout 인접 B2a 범위 (B2/B2a). |
| §9 Agent Prompt Guide | 삭제 | 도구별 복붙 프롬프트. `#1f1f1f` 16px/400 SpotifyMixUI 500px 12px 96px 12px 48px search, outlined 14px/700 `#7c7c7c` 9999px 4px 16px 4px 36px, newsroom `#1ed760` 14px/800 Spotify Mix 100px 16px 33px 15px는 이미 Foundations / Components에 있음. 슬롯 없는 위임 없음 (A3: §9-only 고유값 없음). |
| §10 Voice & Tone | 옮김 → Content & Locales + Named gaps | Dual destination (E2a): Content 표의 product-copy 경계 + Governance Named gaps (live-player text corpus and a complete product-microcopy system). Partner like-state copy is dual Content + §14 capture record (E2a). Product-copy 경계 문장 인접 완전 B2a (B2/B2a). 합성 live-player voice 규칙 없음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; URL·서사 원장 분리 → provenance | 2008 출시, podcasts, 2022 audiobooks, mission "one note, one voice, one idea at a time", 2026 design-history(green, dark-mode, Mix, flex with culture). Design-history URL은 YAML sources + footer + provenance Narrative; portable Experience cites the article by name without that URL (E2a: 본문은 기사 이름만, URL은 provenance). |
| §12 Principles | 옮김 → Experience principles | 세 numbered stem + *UI implication* + capture-bound application 전체가 derived editorial implementation inference / not Spotify-authored or a separately published UI specification. Developer-guidance facts cited inside a stem remain source-stated; the numbered reconstruction is not first-party doctrine (B2/B2a, E1, E2c). Governance 일반 문구는 그 한정의 대체물이 아님. |
| §13 Personas | mixed: stakeholder groups 옮김 → Experience Audience; sidecar 재수록 없음 | 원본이 official company surface의 stakeholder groups이며 fictional personas가 아니라고 명시. 가상 biography 없음(원본도 없음). portable Audience는 Listeners / Artists and creators / Partner developers 세 그룹을 유지. sidecar 재수록 없음 (D2). Independently verified Primary tasks 3건은 §13이 아니라 캡처된 Web Player home / `/search` / company-info newsroom 표면 (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability; like strings 옮김 → Content | 본문 보존: hover/focus/pressed player snapshots; newsroom CTA focus/pressed; disabled/unchecked icon-control instances without a general disabled visual rule; partner restricted playback disabled-or-not-shown; like messages "Added to Liked Songs" / "Removed from Liked Songs"; no additional loading, error, toast, or motion state (A2). Like strings are dual-destination: Components capture record + Content & Locales (E2a). 선언 컴포넌트는 §4.4 표를 닫되 미관측 시각값은 발명하지 않음. `not captured`를 `not-applicable` 사유로 쓰지 않음 (C1). Player Search Field와 Filled/Hollow Newsroom CTA의 loading·error·success는 식별된 search-entry / editorial-destination 역할로 판정. Outlined Compact Action과 Circular Icon Control은 exact selector label/behavior가 없어 loading·error·success applicability를 이 경계에서 생략 (C2). generic `Focus`는 `focus-visible` treatment로 승격하지 않음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion + Avoid + Named gaps | Triple destination (E2a): Avoid (do not derive a motion duration/easing/token from static snapshots) + Foundations Motion + Governance Named gaps (five-kind gate). 무출처 커브 없음(삭제할 값 없음). duration / signature-motion도 원본에 없음. 원문 "Do not derive one from static hover/focus/pressed snapshots" 보존. Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). |

## Revision 2026-08-23 (wave4 full resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6 (F1·F2 의무 최종 패스). Source FAIL: `docs/reviews/t2-1-wave4-2026-08-23-sol-full.md` §1.

| Item | Correction |
|---|---|
| Hollow Type / surface kind A1/A4 | Hollow `Type: button` removed. Source §4 body has no primitive type; filled sibling type is not copied. Kind remains interactive from CTA role plus captured Focus. Provenance surfaces table no longer invents `product` / `official-doc` kind; source surfaces keep `id`/`url`/`inspected` only. |
| §12 stems B2/B2a/E1 | All three numbered stems, *UI implication* notes, and capture-bound application sit under one adjacent complete B2a. They are not classified as first-party language. Developer-guidance facts cited inside a stem remain source-stated. Scope visual-character and design-history causal sentences, Avoid, local-scale/elevation, Layout, and Content product-copy boundary have adjacent complete B2a. |
| Outlined/Circular C2 | Exact selector label/behavior is not captured. Loading/error/success applicability is omitted at that field boundary. Invented `local player action` meaning and Circular-success vs partner-like comparison are withdrawn. Like strings remain dual capture-record + Content. |
| Source-row destinations E2/E2a/E2c | Log now records dual/triple destinations including Governance Named gaps for §2/§3/§5/§6/§10/§15, Content for like strings, Avoid for §15, and YAML-sources/footer (not §1/§11) for design-history and developer URLs. Withdrawn: Hollow type counted as a verified `Type: button`; numbered stems as first-party; F1/F2 completion stronger than the body. |

### F1 / F2 (v6 mandatory final passes)

- **F1 B2a scan (full DESIGN.md reread):** Editorial/causal/judgement sentences have adjacent complete B2a (`derived editorial implementation inference` / `not Spotify-authored or a separately published UI specification`) on: Scope visual-character paragraph (content-led / geometry repeatedly resolves); Scope design-history causal sentence (`That account explains the recognizable expression`); Principles (three numbered stems + *UI implication* + capture-bound application); Avoid capture-bound judgements; Spacing local-scale reading; Shape local-scale reading; Elevation “any shadow treatment requires a separately observed surface”; Layout authenticated-audit / do-not-infer / not-cross-viewport readings; Content product-copy boundary. Remaining Scope sentences are surface coverage, 2008/mission/design-history source-stated facts, or named capture URLs. Distinctive traits are captured values and surface splits. Font evidence rows are evidence-class labels from the source. Player Search Field and Filled/Hollow Newsroom CTA state reasons are named roles. Outlined/Circular loading/error/success rows are omitted rather than judged. No unqualified catalog-principle, causal-history, local-scale, layout, or product-copy reading remains outside a B2a limiter.
- **F2 E2 grep (value + field/role context):** Hollow grep-matched no `Type: button` on that component; Filled/Outlined/Circular still grep-matched `Type: button`; Search grep-matched `Type: input`. Surfaces table grep-matched `id`/`url`/`inspected` without a surface `kind` column. Principles head grep-matched numbered stems inside the complete B2a limiter, not a first-party-language exemption. Like strings `"Added to Liked Songs"` / `"Removed from Liked Songs"` grep-matched Components capture record and Content. §5 authenticated-player / sidebar/grid/player-bar language grep-matched Scope, Avoid, Layout, and Named gaps. §15 “Do not derive one from static hover/focus/pressed snapshots” grep-matched Avoid, Foundations Motion, and Named gaps. §2 omitted color groups grep-matched Foundations and Named gaps. §3 declared-only faces and license boundary grep-matched Typography and Named gaps. §6 elevation gap grep-matched Foundations and Named gaps. §10 live-player corpus gap grep-matched Content and Named gaps. Design-history and developer-guidelines URLs grep-matched provenance sources/Tier 1/Narrative, not Scope body. Three capture URLs grep-matched Scope and provenance surfaces/sources/Tier 1. Homepage `https://www.spotify.com` and Simple Icons `spotify` grep-matched provenance only. `#1ed760` grep-matched Foundations Spotify Green and provenance identity. `components_harvested: false` grep-matched capture record and provenance. B3 five-kind gate grep-matched Foundations Motion. Outlined/Circular loading/error/success applicability grep-matched the omission sentence, not `not-applicable`. Compliance claims match the body.
