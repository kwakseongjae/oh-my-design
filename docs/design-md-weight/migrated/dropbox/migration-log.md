# Dropbox migration log

Ruleset: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v9** (2026-08-26)

Source: `web/references/dropbox/DESIGN.md` (unmodified)
Sibling proof: `web/references/dropbox/.verification.md`
Destination: `docs/design-md-weight/migrated/dropbox/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/dropbox/provenance.md`
Date: 2026-08-26
Worker: Opus 5 T2 migration worker
Portable Core: seven claim markers present exactly once each; no `[FILL IN]` in the portable body.
Gate: `node test-v2/tools/migrate-reference.mjs --brand dropbox --gate-only` → PASS, problems `[]`.
Core v2 check: `scripts/design-md-core.cjs` `inspectDesignMd` → `format: core-v2`, `cleanTop: true`, all seven core section ids present, zero failing conformance checks, zero `[FILL IN]` placeholders; `conformance.portable_core: true`, `conformance.reasons: []`.
Revision: wave 21 — six confirmed defects resolved; see `audit-log.md` post-audit addendum. Gate re-run after revision → PASS, problems `[]`.
DESIGN SHA-256: `c8a217b49221f5876ff762709e74984f68605231db8dba700e51363d498e54c3` (post wave-21 revision)

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`) | 분리 → provenance Identity; `name` → H1; `primary_color` 값 → Foundations + Components + Experience | Portable file carries no frontmatter. `Dropbox` remains the H1; `#0061fe` remains in its verified portable roles as well as in the provenance ledger. |
| YAML `logo` (`type: simpleicons`, `slug: dropbox`) | 분리 → provenance Identity + Logo decision | Catalog pointer is proof metadata. Portable Assets keeps only the authority boundary (official brand site owns logo/iconography/imagery); no logo geometry is asserted. |
| YAML `verified`, `added`, `omd` | 분리 → provenance Identity + Freshness | Format and freshness metadata; the portable top must carry no verification or generator metadata. |
| YAML `ds.name` / `ds.url` / `ds.type` / `ds.description` | exact fields 분리 → provenance Identity; guideline-domain proposition 옮김 → Experience Scope + Typography & Assets | Provenance holds the four exact fields including `ds.type: brand`. The portable Scope states the same guideline coverage (framework, voice and tone, logo, typography, iconography, color, imagery, motion) and the `dropbox.design` → `brand.dropbox.com` redirect without repeating the ledger row. |
| YAML `tokens.source`, `tokens.extracted`, `components_harvested` | 분리 → provenance Identity | Extraction and harvest metadata are proof records, not contract values. |
| YAML `tokens.note` | exact string 분리 → provenance Identity; its propositions 옮김 → Foundations Semantic color | The verbatim note stays in provenance. Its content — core trio confirmed on both the brand site and live, coconut-not-white text on blue, flat shadowless marketing system — is carried in the portable Semantic color and Elevation blocks. |
| YAML `tokens.colors` (13 values) | 옮김 → Foundations Semantic color (+ Experience traits, Components); exact record 분리 → provenance Token record | All thirteen values survive portable. `canvas` and `coconut` share `#f7f5f2` and `on-primary` repeats it; the roles are kept distinct rather than merged. |
| YAML `tokens.typography` (families + 7 roles) | 옮김 → Typography & Assets Family + Type roles (+ Experience, Components); exact record 분리 → provenance Token record | Unitless line heights `1.20` / `1.30` / `1.50` / `1.55` are preserved as ratios with the source's computed px in parentheses; no ratio was converted to a fixed px. |
| YAML `tokens.spacing` (8 steps) | 옮김 → Foundations Spacing; 이중 기록 → provenance Token record | Named scale `xs` 4 … `section` 72 preserved with names, alongside the measured paddings from section 5. |
| YAML `tokens.rounded` (4 steps) | 옮김 → Foundations Shape; 이중 기록 → provenance Token record | 8 / 12 / 16 / 100 preserved with the source's role names and the live 12px ×60 frequency. |
| YAML `tokens.shadow.none` | 옮김 → Foundations Elevation; 이중 기록 → provenance Token record | Recorded as the measured `box-shadow: none` fact, separate from the "shadowless by design" reading. |
| YAML `tokens.components` (7 records incl. `type:`) | 옮김 → Components & States; exact record 분리 → provenance Token record | Every primitive `type` survives per component: `button` ×3, `tab`, `card` ×2, `listItem`. No record was flattened to a generic interactive kind. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Palette, typography pairing, flat depth, geometry, graphite bands, accent wheel, and the eight Key Characteristics are preserved. The warm-editorial, reddish-undertone, coconut-on-blue, and two-font-register readings carry adjacent complete B2a qualification (`DESIGN.md:13`, `:15`, `:17`, `:49`). |
| §2 Color Palette & Roles | 옮김 → Foundations Semantic color | Core trio / Surfaces / Text hierarchy / Accents groups preserved, including the live `rgba(82, 74, 62, 0.82)` at 82% opacity and its `#716b61` composite. The twelve further accent names and the 20-step grey scale are carried by name; their values stay omitted portable and, for the three the sibling recorded, live in provenance only. |
| §3 Typography Rules | 옮김 → Typography & Assets (evidence classes, Family, Type roles, rules) | rem equivalents, computed line heights, `Sharp Grotesk 23`, `Atlas Grotesk Web`, DB Sharp Grotesk / Sharp Type commission, variable-weight note, "Type is what meaning looks like" (Max Phillips, Sharp Type), medium-never-bold, normal tracking. Interpretive rule readings qualified at `DESIGN.md:209`. |
| §4 Component Stylings | 옮김 → Components & States | Nine records: three buttons, the text button, the header nav item, the mega-menu card, the sand card, the graphite band, the footer link. The hero 71px / 24px variant, the coconut outline flip on graphite bands, and the self-colored 2px border rule are carried in the section preamble and the primary CTA record. |
| §4 footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance Sources + Freshness + Conflict matrix | Freshness, URLs, and the Tier 2 dispositions are proof ledger. The `styles.refero.design` confirmation and the `getdesign.md` non-listing are recorded there. |
| §5 Layout Principles | 옮김 → Foundations Spacing + Foundations Shape + Layout & Platforms | Base unit ~4px, 12/16/24 rhythm, ~72px section gaps, 72px sticky header, two-column hero, band alternation, mega-menu grid, pricing plan columns, and the 8/12/16/100 radius scale with the 60-occurrence figure. Editorial air / band cadence / flat segmentation readings qualified at `DESIGN.md:415`. |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | The four-level table is carried whole. The measured `box-shadow: none` fact is stated separately from the shadowless-by-design and 2px-border-weight readings, which carry adjacent qualification (`DESIGN.md:149`). |
| §7 Do's | 옮김 → Experience Principles (application rules) | Eight rules, including the quoted guideline fragment "to create harmony and focus". Not placed in Governance controlled prose. |
| §7 Don'ts | 옮김 → Experience Avoid | Eight prohibitions, including the 1px-border and 700-weight boundaries. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms + Typography & Assets (Assets) | Breakpoint table (<640px / 640–1024px / 1024–1440px), touch targets, and collapsing strategy → Layout & Platforms; of Image Behavior, the 12px-radius-across-breakpoints rule → Layout & Platforms collapsing strategy, while the flat no-shadow product-screenshot rule and the warm-accent illustration/photography rule → Typography & Assets Assets (`DESIGN.md:214`, `:215`). The 1440×900 desktop-capture boundary and the absence of a separate resize capture behind the collapsing rows are stated at `DESIGN.md:429`. |
| §9 Quick Color Reference | 삭제 | A restatement of §2 with no unique value; every entry (`#0061fe`, `#f7f5f2`, `#ffffff`, `#eee9e2`, `#1e1919`, `#1c1d21`, `#716b61`, `rgba(82,74,62,0.82)`, the four accents) is already carried in Foundations Semantic color. |
| §9 Example Component Prompts | 삭제 (프롬프트 포장) + 고유값 4종 옮김 | The prompt wrappers are tool-facing and are removed, not delegated. Their four unique facts moved to real slots: the "Try for free" plan CTA label → Experience Primary tasks and Content & Locales; white `#ffffff` pricing panels on `#f7f5f2` at 12px radius, flat → Layout & Platforms; the mega-menu one-line muted description in `rgba(82,74,62,0.82)` → the Mega-menu Product Card anatomy; and the mega-menu card title at 16px Atlas Grotesk → the Mega-menu Product Card record `Font` line (`DESIGN.md:346`), the only place that binding appears in the source. |
| §9 Iteration Guide | 삭제 | Seven numbered restatements of §7 Do's/Don'ts and §2/§3 values; no unique value. The canvas, single-blue, type-pairing, 2px-border, zero-shadow, radius, and accent-restraint rules all remain in Experience Principles / Avoid and Foundations. |
| §10 Voice & Tone | 옮김 → Content & Locales | Four pillars with verbatim guidance (quoted in Experience Official brand guidance and referenced in Content), the governing rule, the six-row tone table with its verbatim samples, the three cited voice samples with their verification dates, and the forbidden register. |
| §11 Brand Narrative | 옮김 → Experience Scope; 출처 URL·freshness 분리 → provenance | 2007, Drew Houston, Arash Ferdowsi, the USB-drive origin, Ashraf Alkarmi in 2024, the mission, the magic-folder-to-workspace evolution with Replay / Sign / DocSend / Dash / Reclaim.ai, "over 700 million registered users", the framework line and four pillars, the refuses/embraces reading, "warm, soulful, and relatable", and the Eames quote. Public-fact and narrative-not-tokens dispositions at `DESIGN.md:19`, `:21`. |
| §12 Principles | 옮김 → Experience Principles | Five numbered items with their verbatim pillar quotes and *UI implication* notes. The quoted halves are marked first-party; the implications and item 5 carry adjacent qualification at `DESIGN.md:63`. |
| §13 Personas | 삭제 (가상 아카이타입·전기) + 세그먼트 라벨만 옮김 → Experience Audience | The three named archetypes with their ages, cities, occupations, and narratives are deleted under D2 and are not re-hosted in provenance. Only the four group-level segment labels the source calls publicly observable survive, in Audience, marked as segment labels rather than verified audience research. No persona content became a primary task. |
| §14 States | 옮김 → Components & States "Source-stated state treatments" | The nine-row table is carried whole with its values and copy, including "It's that simple :)" and the "Required" contrast. The block states its evidence class in place: the table belongs to neither named evidence domain, no capture of a Dropbox product screen stands behind it, and it projects marketing-surface values onto product file-management situations (`DESIGN.md:396`). Per-component applicability is judged by control role and does not draw treatments from this table. No graph delegation; no coverage claim. |
| §15 Motion durations | 옮김 → Foundations Motion, class 부착 | 120ms / 240ms / 400ms are kept with the source's own "illustrative scale value" class in a dedicated column, not promoted as brand tokens. |
| §15 `ease-dropbox` curve | 옮김 → Foundations Motion | `cubic-bezier(0.65, 0, 0.45, 1)` is the officially documented curve from the "Create folder" motion example and is kept. The "symmetric, snappy in, soft out" characterization is qualified in place (`DESIGN.md:168`). |
| §15 `ease-exit` curve value | 삭제 (값 경계) + 토큰명·용도 옮김 → Foundations Motion | `cubic-bezier(0.4, 0.0, 1, 1)` is unattributed and matches the legacy spec-template example table; the value is removed at the smallest boundary while the token name and its "Dismissals" use survive portable. The deleted value and the reason are recorded in provenance Proof notes. |
| §15 Motion rules and reduced motion | 옮김 → Experience Official brand guidance + Foundations Motion | The four principle names and their verbatim quotes are carried as first-party language; the "Enable animation" toggle and the `prefers-reduced-motion: reduce` collapse-to-instant behavior are carried as established behavior. |
| HTML comment — source evidence ledger | 분리 → provenance Proof notes + Sources + Evidence-domain boundaries; 필요한 한정 옮김 → 본문 | Tier 1 URLs, fetch dates, and the §1–9 / §10–15 assignment live in provenance. The four qualifications a standalone reader needs — illustrative durations, the single documented curve, the public-fact status of the co-founder and USB origin, and the editorial status of "shadowless by design" and coconut-on-blue — are carried in the portable body next to the sentences they govern. |
| Sibling `.verification.md` | 분리·채택 → provenance Sibling verification record | Method, raw samples, official swatch values (including Pink / Rust / Gold, which the source `DESIGN.md` names without values), conflict matrix, logo decision, and brand-site record. The `#0061FF` Simple Icons fill and the three swatch-only hexes stay provenance-side and do not enter the portable body. |

## Required final passes

- **F1 (B2a scan).** The portable body was re-read end to end after drafting and again at the wave-21 revision. Thirty-four interpretive or causal passages now carry an adjacent complete qualification naming the evidence class — "derived editorial implementation inference from the verified surfaces; … not Dropbox-authored or a separately published UI specification" — at `DESIGN.md:11`, `:13`, `:15`, `:17`, `:19`, `:21`, `:27`, `:36`, `:49`, `:63`, `:71`, `:84`, `:95`, `:130`, `:134`, `:138`, `:149`, `:166`, `:168`, `:183`, `:189`, `:209`, `:213`, `:224`, `:226`, `:381`, `:389`, `:396`, `:415`, `:429`, `:434`, `:451`, `:453`, `:489` — measured by grep against the current file. The earlier record read thirty-three and was wrong twice: the migration-authored byte-for-byte reproduction rule in Content & Locales carried no qualification at all (now at `:453`), and the state-table block was counted as qualified while its opening sentence still claimed the two contract domains "account for every value elsewhere in this document", which the About-page facts at `:19`, the widely-documented public facts in the same paragraph, and the illustrative motion durations at `:153` each contradict (the sentence is now narrowed at `:396`). The Governance authority claim is not used as a substitute for any of them. First-party quoted language is grouped under Experience "Official brand guidance" and attributed there rather than qualified as derived.
- **F2 (E2 collation).** Every row above was checked with `grep -F` against the three output files before being written. Dual destinations are recorded on both sides (colors, typography, spacing, rounded, shadow, components, `ds.*`, `tokens.note`, `primary_color`). Compliance statements are written only where the portable body actually carries the text: the five-evidence motion promotion gate is present verbatim at `DESIGN.md:168`, and all nine §14 rows are present in full at `DESIGN.md:400–408` under the class statement at `:396`.
- **A5 (copy check).** Thirty-two brand-published strings were grepped byte-for-byte across the outputs: the four voice-pillar quotes and the governing rule, the four motion-principle names and their quotes, the framework line, the Eames quote, the typography maxim, the color statement, the four principle quotes, the three cited voice samples, the six tone-table samples, the CTA and nav labels ("Try Dropbox free", "Get started", "Try for free", "Buy now", "or buy now", "Learn more", "Products", "Solutions", "Enterprise", "Pricing"), the product names, "It's that simple :)", "Enable animation", and "to create harmony and focus". None was translated, re-cased, or paraphrased.
- **D1a (named-gap check).** Governance Named gaps lists only values the source itself names without settling: the twelve published accent names and the 20-step grey scale, the `ease-exit` curve behind a token the source declares, and the visual treatments for the hover/focus moments the source's own motion scale names and for the disabled presentation its state table describes. No domain absent from the source is enumerated. The accent bullet now also separates Pink, Rust, and Gold from the rest of that list: the guideline color page publishes swatch values for those three, so the gap is the source's silence about them, not an unresolved value — the values themselves stay provenance-side and are not promoted.
- **E1 (process leak).** No migration-internal vocabulary survives in the portable body: it names no catalog graph, no legacy spec template, no wave or rulebook clause, and no legacy section numbers. The `ease-exit` row states only that the token name and use are stated while the curve is unattributed; the spec-template match that justifies dropping the value is recorded in `provenance.md` Proof notes, where a reader who needs the migration reason can find it.
- **E3 (gate evasion).** No wording, hex, selector, state label, or line break was distorted to satisfy the gate. No gate false positive was observed on this reference.
