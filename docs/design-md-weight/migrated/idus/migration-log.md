# idus migration log

Source: `web/references/idus/DESIGN.md`
Sibling read (not the migration input): `web/references/idus/.verification.md`
Destination: `docs/design-md-weight/migrated/idus/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/idus/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `0f4b238ffb4ccec09cc2ed46761ad7cee99b9d5734609d476db40cd5808a32a1` (`web/references/idus/DESIGN.md`). Sibling SHA-256 `40e0975418f1c86d2ae4c8d35fe4d2bbca9aa8a4bd8ce30903ee505ae9da502b` (`web/references/idus/.verification.md`). Worker-close portable DESIGN SHA-256 `bade30e144a9e53a721ab7f7a870be6db40aae9f7fad97f4bad874322550efb6`. Auditor-close portable DESIGN SHA-256 `5b781ac69c47265bb38e00019119e2496f39d79a1cee0c8cd31fdec82ff7dc82`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# idus (Backpackr) Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–22. Homepage `https://www.idus.com` as its own URL (not only as a prefix of `/v2/product/`) is dual: `DESIGN.md` 9/21 + `provenance.md` 14/45/51 (E2a). `#ef7014` is dual: `DESIGN.md` 11/35/50/59/91/144/243/259/295/296/379/394/442/445 + `provenance.md` 15/62/64/76/78/79/80/81/106/142 (E2a). Favicon slug is dual: `DESIGN.md` 222 + `provenance.md` 17 (E2a). `아이디어스` is dual: `DESIGN.md` 9/13/510 + `provenance.md` 11/73/74/94. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 19–22, 32–35. The source footer's **Verified:** 2026-07-02 is at `provenance.md` 37. `live-extract` DESIGN 0 / provenance 1 (`provenance.md` 20). `components_harvested` DESIGN 0 / provenance 2 (`provenance.md` 22/189). These are ledger keys with no portable slot. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations; 분리 → provenance | Full note transcribed at `provenance.md` 64. The facts it names (carrot orange `#ef7014`; coral `#ff4b50`; gold `#ffaf00`; ink `#111111`; text ladder `#333333` → `#666666` → `#999999`; flat near-shadowless white canvas) land in `DESIGN.md` 11/35–43/91–113. Dual (E2a). |
| YAML `tokens.colors` (14 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 91–113. All fourteen roles kept with names, values, and token-set keys: primary, primary-tint, sale, sale-tint, rating, highlight, ink, text, text-muted, text-faint, border, border-strong, canvas, on-primary. `#ffffff` as `canvas` is the page-and-card role (`DESIGN.md` 111); `#ffffff` as `on-primary` stays a separate key (`DESIGN.md` 93/115). |
| YAML `tokens.typography.family.sans` / `family.kr` | 옮김 → Typography & Assets Family | `DESIGN.md` 194. Live computed `DESIGN.md` 186. Declared-only classing **188** (not Family). |
| YAML `tokens.typography.cta / section-tab / button-strong / body / caption / micro` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 202–207. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.4` dest 3 · `1.5` dest 5. All six YAML `use` strings restored verbatim in the Token-set use column (A1, A3). Paths `tokens.typography.cta` / `section-tab` / `button-strong` / `body` / `caption` / `micro` dest 1 each at `DESIGN.md` 209. |
| YAML `tokens.spacing` (6 steps) / `tokens.rounded` (4 steps) / `tokens.shadow.none` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 119 (`tokens.spacing.xs: 4` · `sm: 8` · `base: 14` · `md: 16` · `lg: 20` · `xl: 24`). Shape: `DESIGN.md` 123 (`sm 2` · `md 4` · `lg 12` · `full 100` plus the named uses; workhorse reading of Sharp 2px at 133). `tokens.rounded.full: 100` dest 3 (`DESIGN.md` 130/133/583). `tokens.spacing.base: 14` dest 3 (`DESIGN.md` 119/461) is disambiguated from the pill's 14px padding and from 14px type. `tokens.spacing.md: 16` is disambiguated from button `0 16px` at 119. `tokens.spacing.xs: 4` is disambiguated from `tokens.rounded.md: 4` at 119/133. `tokens.shadow.none` at `DESIGN.md` 144. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | `DESIGN.md` 238–434. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×3 (241/266/292 = YAML primary + secondary + outline), `badge` ×2 (377/392), `input` ×1 (342), `tab` ×1 (416), `card` ×1 (366). All eight YAML `use` strings restored verbatim as `Token-set use:` lines (250/276/302/349/369/386/399/423). YAML `font` shorthands kept as `token-set font record` lines: `18px / 700` dest 2 · `14px / 700` dest 2 · `14px / 400` dest 1 · `16px / 700` dest 1 · `16px / 400` dest 2 (`DESIGN.md` 11/420). Top Utility and Dark Caption Chip are §4-only and say `not in the token set` (327/411) — no borrowed `Primitive type`. |
| §1 Visual Theme & Atmosphere (legacy 60–77) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 35–43. The atmosphere readings (warm high-density commerce, orange as commitment, system-native typographic personality, hierarchy by weight and size, scanning not editorial pause) carry an adjacent complete qualifier at 11 (B2/B2a). The largest-marketplace reading in ¶1 is named in the qualifier at 9. |
| §2 Color Palette & Roles (legacy 79–101) | 옮김 → Foundations semantic color | `DESIGN.md` 91–113. Characterizing phrases covered by the adjacent qualifier at 87 (B2/B2a). Canvas / on-primary key-unmerge at 115 has its own adjacent complete qualifier. |
| §3 Typography Rules — family, hierarchy, principles (legacy 103–124) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 185–190; family 194–196; hierarchy table 202–207; type rules 213–216. The Official-product-use, Official-distributed-asset, Declared-only, License, Outside, Family fallback, Type-roles ratio, and Type-rules readings carry adjacent complete qualifiers at 185, 187, 188, 189, 190, 196, 209, and 218 (B2/B2a). |
| §4 Component Stylings (legacy 126–214) | 옮김 → Components & States | `DESIGN.md` 238–434. §4 body values and YAML `use` / font / radius / height forms are both kept. Top Utility (315–337) and Dark Caption Chip (402–411) have no YAML row. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 218–221) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 32–35; Tier 1 list 51–53; Tier 2 57–59. `https://www.idus.com` is dual: `DESIGN.md` 9/21 + `provenance.md` 14/45/51 (E2a). `https://www.idus.com/v2/product/` is dual: `DESIGN.md` 9/23/242/267 + `provenance.md` 46/52 (E2a). `https://github.com/backpackr` is dual: `DESIGN.md` 9 + `provenance.md` 47/53 (E2a). Conflicts unresolved: none — `provenance.md` 39. |
| §5 Layout Principles (legacy 223–245) | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 457–462 (dense grids, category tab row, sticky action rail, dark chips on full-bleed imagery, spacing 4/8/14/16/20/24, shape 2/4/12/100) and 464 (density with breathing hairlines / flat segmentation / action clarity). Image-behavior consistent-with-flat qualifier at 487. 48px action height at 119/247/273/474. `full: 100` dest 4 at 130/133/462/583. |
| §6 Depth & Elevation (legacy 247–256) | 옮김 → Foundations elevation | `DESIGN.md` 137–144: four-level table, `tokens.shadow.none`. The near-shadowless / dense-grid-fast-and-flat reading carries an adjacent qualifier at 144 (B2/B2a). |
| §7 Do's (legacy 260–268) | 옮김 → Experience application rules | `DESIGN.md` 59–66 (eight Do rules), under the grouping qualifier at 57. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 270–278) | 옮김 → Experience avoid | `DESIGN.md` 72–79, under the qualifier at 70. The eight prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior (legacy 280–303) | 옮김 → Layout & Platforms | `DESIGN.md` 466–487: Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` at 470–472, 48px / 33px / ~67px targets at 474, collapsing strategy 476–481, image behavior 483–487. The Desktop row keeps the source body's `1024-1440px` range at 472. The system-level-not-cross-viewport reading carries an adjacent qualifier at 466 (B2/B2a). |
| §9 Agent Prompt Guide (legacy 305–336) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 142. |
| §10 Voice & Tone (legacy 340–357) | 옮김 → Content & Locales | Published strings `DESIGN.md` 510–542; tone table 494–500; forbidden register 544. The qualifier at 492 covers the voice reading and the tone-table contract (B2/B2a, A5). Voice-sample parenthetical glosses carry their own adjacent complete qualifier at 506. The premise-to-register causal at 544 has its own adjacent complete qualifier. Byte-exact / gloss-beside close at 546. |
| §11 Brand Narrative (legacy 359–365) | 옮김 → Experience Scope | `DESIGN.md` 13: 2014, Backpackr, 백패커, 김동환, Kim Dong-hwan, ceramics / jewelry / candles / baked goods / leather craft, "idea + us / ideas", 클래스, 텀블벅 / Tumblbug, 작가, 작품, 작가홈, 팔로우, "follow and support people who make things by hand". Refuse/embrace pairing in the same paragraph, including glossy shadow-stacked chrome, hard-sell scarcity tactics, warm single accent, flat and dense but scannable grid, social proof as encouragement, and a maker-first vocabulary — the source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 191. |
| §12 Principles — 5 numbered (legacy 367–373) | 옮김 → Experience principles | `DESIGN.md` 49–53 under the B2a form at 47: "These five items are a derived editorial implementation inference from the verified surfaces; they are not idus-authored or a separately published UI specification." No separately published UI specification is named in the source, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 150–184 (35 rows). |
| §13 Personas — 3 entries (legacy 375–383) | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, motive, or occupation classification appears in either output (D2, D2a). Disposition at `provenance.md` 136. Audience at `DESIGN.md` 29 carries only the group-level description the source independently records (gift-shoppers seeking something personal, supporters of independent makers, hobbyists browsing handmade classes), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows (legacy 385–397) | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at `DESIGN.md` 442–450 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "아직 담은 작품이 없어요", "작품 둘러보기", "오류가 발생했습니다", and "필수" as the source wrote them. The applicability rule is at 232–236, whose qualifier at 236 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary Purchase CTA and Secondary Cart / Gift open loading/error/success with a role reason (C2 v10) — they commit purchase or cart/gift in place (`loading \| applicable` dest 2 · `error \| applicable` dest 3 including Search · `success \| applicable` dest 2). Brand Outline, Top Utility, and Category Tab close loading/error/success with a role reason — inquiry/follow, destination links, and destination tabs (`loading \| not-applicable` dest 4). Global Search opens error (form-validation row) and closes loading/success because the field does not commit the capture (`error \| applicable` dest 1 of the 3). Social-Proof Pill, Ranking Flag, and Dark Caption Chip declare `Kind: non-interactive` with a reason instead of a map (Core §4.4) — `Kind: non-interactive` dest 3 (`kind: non-interactive` DESIGN 0). Product Card gets no `kind` and no map because the source supplies no interaction evidence for the container — `DESIGN.md` 372 (C4). |
| §15 Motion & Easing — durations, easing roles, motion rules, reduced-motion (legacy 399–417) | 옮김 → Foundations motion | `DESIGN.md` 154–156 (120ms / 200ms / 320ms with uses), 162–164 (ease-enter / ease-exit / ease-standard roles and uses, curves omitted), 170–173 (functional and quiet, fade-in at `motion-standard / ease-enter`, no bounce or spring, `prefers-reduced-motion: reduce` collapses to instant and freezes the banner carousel). |
| §15 Motion & Easing — three curve values (legacy 413–415) | 삭제 → provenance omission ledger | `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are named as omitted at `DESIGN.md` 158 and are not promoted as tokens. Durations stay. Ledger at `provenance.md` 137–139. B3 is held: the promotion condition at `DESIGN.md` 166 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 582. |
| Opening product / atmosphere sentence (legacy 62) | 옮김 → Experience Scope | `DESIGN.md` 9–11: handmade-goods marketplace; two inspected surfaces; `#ffffff` canvas; reserved carrot orange; system stack. |

## Sibling handling (`web/references/idus/.verification.md`)

The sibling exists — confirmed with `find web/references/idus -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 73–90 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: product-page title `재구매 1위🥇 925실버 레이어드링 은반지 20종 | 아이디어스`; `ui-sans-serif`; body `16px` / `24px` and body `rgb(0, 0, 0)`; `팔로우` height `36px`; ranking-flag height `30px`; dark-chip height `26px` / chip `16px`; category-tab height `48px`; frequency counts; product UUID path; `https://medium.com/idus-tech`; `No designs found for 'idus'.`. Recorded at `provenance.md` 94–104.
- Measured `DESIGN.md` 0 for those sibling-only strings: `재구매 1위` 0 · `ui-sans-serif` 0 · `36px` 0 · `30px` as ranking-flag height 0 · `26px` 0 · `medium.com/idus-tech` 0.
- `구매하기` / `장바구니` / `선물하기` / `작품문의` / `팔로우` / `찾으시는 작가, 작품이 있나요?` / `최근 573건 더 많이 구매되었어요` / `로그인` / 48px CTA height / 100px pill are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. idus is Korean-first, so a hand sweep of published labels is mandatory even when the needle set is non-empty.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 32 distinct | 0 | 0 | 아이디어스 / 백패커 / 작가 / 작품 / 클래스 / 텀블벅 / 김동환 / 작가홈 / 팔로우 / 구매하기 / 장바구니 / 선물하기 / 작품문의 / 선물추천 / 할인 / 베스트 / 취향발견 / 실시간 / 최신작품 / 커뮤니티 / 찾으시는 작가, 작품이 있나요? / 최근 573건 더 많이 구매되었어요 / 최근 N건 더 많이 구매되었어요 / 로그인 / 회원가입 / 고객센터 / 관심 / 내 정보 / 아직 담은 작품이 없어요 / 작품 둘러보기 / 오류가 발생했습니다 / 필수 / 주문내역 / 지금 아니면 끝!. |
| `node test-v2/tools/latin-copy-audit.mjs --brand idus` | 4 lost / 26 scanned | 4 | 0 | Lost strings are sibling/tool text (`Browse 2,000+…`, `1px solid #ef7014`, `curl -sL`, `idus — 0 DESIGN.md files ... No designs found for 'idus'.`), not published idus product copy. `No designs found for 'idus'.` survives in provenance. English glosses that sit beside Korean lines (`makers`, `works`) are kept as glosses, not replacements (A5). YAML padding form `0 16px` is at `DESIGN.md` 246/272/298 beside visible `0px 16px`. |
| Sibling published strings | 1 distinct sibling-only product title | 0 | 0 | `재구매 1위🥇 925실버 레이어드링 은반지 20종 \| 아이디어스` survives in provenance; it is not promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 아이디어스, 백패커, 김동환, 2014, 텀블벅, 클래스, 작가홈, 구매하기, 찾으시는 작가, 작품이 있나요?, 최근 573건 더 많이 구매되었어요, hard-sell scarcity, maker-first vocabulary.

A5 분모: hand sweep of source published labels 32 extracted / 0 missing; latin-copy-audit 4 lost / 26 scanned (0 published copy among the lost); gate `copy-loss` compared 41 / candidates 210.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand idus --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 41, candidates: 210 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 41 < `candidates` 210.

## Deviations recorded

- `DESIGN.md` is 7,142 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: eight declared component records plus two §4-only records, a nine-row surface state contract, a seven-state applicability matrix on six interactive controls, `tokens.rounded.full: 100` kept on its own path, the full §11 founding-and-maker narrative including its last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published UI specification is named in the source, so every derived-editorial close uses the toss-form `not idus-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Worker-close measure: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 33. Auditor-close measure: 35 = `not idus-authored` 35 = `separately published UI specification` 35. Provenance derived ledger is 35 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

The portable body was re-read from the title through Governance. Every causal, interpretive, or judgment sentence — Scope ¶1 surface-selection and GitHub-not-token, Scope ¶2 atmosphere readings (including system-native typographic personality and hierarchy-by-weight-and-size), Scope ¶3 refuse/embrace as editorial, Content voice characterization, voice-sample parenthetical glosses, Docs-citation character of the GitHub org, Type-rules readings, Layout titles, Layout image-behavior consistent-with-flat, Shape workhorse, Motion unattribution — has an adjacent complete qualifier naming both `derived editorial implementation inference` and `not idus-authored or a separately published UI specification`. Count 35 = 35. No unqualified editorial sentence remains.

## F2 E2 contrast (mandatory final pass)

Each log row above was written only after `grep` confirmed the destination string in the named file. Dual destinations (homepage URL, `#ef7014`, favicon slug, `아이디어스`, product URL, GitHub URL, token note facts) list both files. Compliance claims: B3 is held only because `DESIGN.md` 166 and 582 name all five evidence kinds in full text. "B3 유지" is not claimed from memory. `live-extract` and `components_harvested` are claimed only as provenance-only because DESIGN dest is 0.
