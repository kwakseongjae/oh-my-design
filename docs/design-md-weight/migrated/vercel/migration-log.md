# Vercel migration log

Source: `web/references/vercel/DESIGN.md`
Destination: `docs/design-md-weight/migrated/vercel/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/vercel/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd, `portable_core: true`, placeholders 0)

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage URL 옮김 → Experience `scope`; `primary_color` 옮김 → Foundations | Portable file has no frontmatter. Name kept as H1 `Vercel Design System`. Homepage `https://vercel.com` / `https://vercel.com/` is dual-destination: Experience Scope + provenance identity/surfaces/Tier 1 (E2a). Catalog `primary_color` `#171717` is dual identity metadata + portable Foundations Primary / Foreground (E2a). Catalog logo type `simpleicons` / slug `vercel` is identity-only in provenance; it is not a portable Typography & Assets mark (E2a: not dual-destination). |
| YAML `omd`, `verified`, token claims, `tokens.source` / `extracted`, `components_harvested` | 분리 → provenance | 출처 원장·freshness·Proof. `tokens.source: reconciled` and `components_harvested: true` are metadata (A1c). |
| YAML `ds.name` / `ds.url` / `ds.type` / `ds.description` | `ds.type` 분리 → provenance only; `ds.name`/`ds.url`/`ds.description` 옮김 → Experience Scope + provenance | YAML `ds.type: system` is provenance metadata (A1c); not in portable Scope. `ds.name` Geist and `ds.url` `https://vercel.com/geist` are dual Experience Scope + provenance identity (E2a). `ds.description` evidence-domain split (marketing / component examples / authenticated dashboard) is dual Experience Scope + provenance (E2a). |
| YAML `tokens.note` | 옮김 → Experience Scope; 원장 분리 → provenance | Dual destination (E2a): portable Scope keeps the fresh public-home + Geist introduction capture, baseline-only mode, and explicit state absence rather than inferred hover; provenance keeps the source note. |
| YAML `verification_v2` (surfaces/sources) | 분리 → provenance; home / geist / font URL 옮김 → Experience Scope; font URL 옮김 → Typography & Assets | Dual/triple destination (E2a): `https://vercel.com/` and `https://vercel.com/geist/introduction` are portable Scope capture coverage AND provenance surfaces/sources/Tier 1. `https://vercel.com/geist` is portable Scope named Geist documentation URL AND provenance `ds.url`. `https://vercel.com/font` is portable Typography & Assets OFL distribution AND provenance sources/Tier 1 AND Scope license-surface sentence. Claims/conflicts stay provenance. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. `#171717` Primary/Foreground와 catalog `primary_color`를 다른 액센트로 합치지 않음. Header-link / radio `#4d4d4d`와 Body, secondary/icon/input `#171717`와 Primary, input adjacent ring / example-card grid `#ebebeb`와 Border/control-ring은 원본이 같은 관측으로 묶은 필드로 유지하되 일반 Ink로 합치지 않음 (A4). YAML unitless `lineHeight` 1 / 1.1 / 1.33 / 1.5 / 1.43은 비율로 보존하고 본문 표의 `64px` / `56px` / `33px` / `32px` / `24px` / `20px` 관측과 합치지 않음 (A1a). 검증된 primitive type은 컴포넌트별로 보존: Header link `Type: button`, Geist secondary action `Type: button`, Geist icon button `Type: button`, Geist compact input `Type: input`, Geist radio `Type: toggle`, Geist component-example card `Type: card`. `Kind: interactive`로 뭉개지 않음 (A1b). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(public home / Geist introduction; authenticated dashboard 제외), GeistSans 428 / Geist Mono 65, 6px/36px/32px Geist 예제 기하. “turns infrastructure into a visual grid” / “deliberately quiet” / “more typographically expansive” 문단 인접에 derived editorial implementation inference / not Vercel-authored or separately published UI specification 한정 (B2/B2a). Distinctive traits information/elevation/strict-boundary 판단도 인접 완전 B2a (B2/B2a). Home / Geist URLs는 provenance와 dual (E2a). |
| §1 공식 Geist 문서 URL | 분리 → provenance; 옮김 → Experience `scope` | Dual destination (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color + Named gaps | `#171717` `#ffffff` `#4d4d4d` `#666666` `#ebebeb` `#fafafa` 및 Ship red / Preview pink / Develop blue / console syntax / link blue / badge blue 생략 제약. Dual destination (E2a): Foundations Semantic color omission sentence + Governance Named gaps. |
| §3 Typography Rules | 옮김 → Typography & Assets + Named gaps | 다섯 증거 등급, GeistSans 428, Geist Mono 65, docs `Geist` alias FontFace 부재 및 공식 family 재조정, declared-only Geist Pixel·GeistSans Fallback. Authenticated dashboard-only overrides and product-specific font loading keep evidence class `Unresolved claim` (captured but uncorroborated). Dual destination (E2a): Typography Font evidence + Governance Named gaps (declared-only faces; unresolved dashboard font overrides). YAML unitless line-height와 본문 표 size-local px는 별 값으로 유지 (A1a). `mono.weight` YAML 400과 본문 표 400–600은 합치지 않음. 시스템 fallback을 Geist로 표시하지 않음. |
| §3 Geist / Geist Mono OFL URL | 분리 → provenance; 본문에 증거 등급으로도 유지 | `https://vercel.com/font` is triple: Typography & Assets official-distributed row + Experience Scope license-surface + provenance sources/Tier 1 (E2a). 본문은 OFL 배포이지 dashboard 토큰이 아님을 유지. |
| §4 Component Stylings | 옮김 → Components & States + Named gaps | Header link (`Type: button`), Geist secondary action (`Type: button`), Geist icon button (`Type: button`), Geist compact input (`Type: input`), Geist radio (`Type: toggle`), Geist component-example card (`Type: card`). YAML 기하와 본문 기하를 같은 슬롯에 유지. Capture selector는 원본에 없음. Compact-input focus boundary is dual Components field note + Governance Named gaps (`focus-visible` / focus treatments) (E2a). |
| §4 Geist component-example card | 옮김 → Components & States | default 기하만. interactive-kind 근거 없음 → kind와 applicability map 생략 (C4). shared `#ebebeb` grid boundary 유지 (A4). Grid-versus-independent-shadow 판단 인접 완전 B2a (B2/B2a). |
| §4 omitted Login CTA / dashboard / command-menu / badge / toast / authenticated input | 옮김 → Components & States capture record + Avoid + Named gaps | Dual/triple destination (E2a): capture-record omission + Avoid (do not invent dashboard/deployment/login/command-menu states) + Governance Named gaps. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Experience capture-bound application | hairline grids, compressed headings vs diagrams/code/imagery, 32–36px / 4–6px controls, Geist Mono for genuine technical content, marketing / Geist / authenticated 분리. Layout 문단 인접에 derived editorial implementation inference / not Vercel-authored or separately published UI specification 한정 (B2/B2a). Geist Mono 규칙은 Experience capture-bound에도 있음 (E2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation + Named gaps | Dual destination (E2a): Foundations Elevation + Governance Named gaps (earlier multi-layer card shadows). primarily-planar / grid-vs-shadow 판단 인접 완전 B2a (B2/B2a). 무출처 shadow 값 발명 없음. |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. Governance 통제 문구에 넣지 않음. Capture-bound는 Principles 머리 B2a 한정 범위 안 (B2/B2a). |
| §7 Don'ts | 옮김 → Experience avoid + Named gaps | Dual destination (E2a): Avoid + Governance Named gaps (workflow accents/badge colors; layered elevation; Geist examples as dashboard tokens). Avoid 머리 인접 완전 B2a (B2/B2a). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms + Named gaps | Dual destination (E2a): Layout & Platforms + Governance Named gaps (authenticated dashboard breakpoints, mobile project navigation, deployment-log overflow). `maintaining the neutral hierarchy` / `compact and composable` 판단 인접 완전 B2a (B2/B2a). 원본에 없는 최소폭 값을 발명하지 않음. |
| §9 Agent Prompt Guide | 삭제; dashboard/deployment 금지는 Avoid + Named gaps에 이미 있음 | 도구별 복붙 프롬프트. `#171717` headings, `#4d4d4d` body, `#ebebeb` grid, Geist compressed tracking, Geist Mono technical, flat 6px ring-bordered controls는 이미 Foundations / Components / Typography에 있음. Unverified dashboard/deployment prohibition is dual Avoid + Governance Named gaps (E2a). 슬롯 없는 위임 없음 (A3: §9-only 고유값 없음). |
| §10 Voice & Tone | 옮김 → Content & Locales + Named gaps | Dual destination (E2a): Content public-language block + Governance Named gaps (complete product-copy guide). 합성 에러/엠프티 카피 없음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). 문단 인접에 derived editorial implementation inference / not Vercel-authored or separately published UI specification 한정 (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 원장 분리 → provenance | ZEIT → Next.js / frontend cloud → AI-native, triangle / monochrome, source→deployment→inspectable environment. 문단 인접에 derived editorial implementation inference / not Vercel-authored or separately published UI specification 한정 (B2/B2a, E1). 연표 URL은 원본에 없음. Geist / home URLs는 provenance (E2a: Experience + provenance). |
| §12 Principles | 옮김 → Experience principles | 네 원칙. 인접 본문에 derived editorial implementation inference / not Vercel-authored or separately published UI specification 한정. Capture-bound application과 Governance 일반 문구는 그 한정의 대체물이 아님 (B2/B2a, E1, E2c). |
| §13 Personas | mixed: task contexts 옮김 → Experience `primary-tasks` + Audience; unspecified fields 옮김 → Audience + Named gaps; sidecar 재수록 없음 | 원본이 first-party task contexts only이며 project-specific names / company size / stack / deployment volume / budget / success metrics는 unspecified. Dual/triple destination (E2a): Primary tasks + Audience + Governance Named gaps for those unspecified project-brief fields. 가상 biography 없음(원본도 없음). Independently verified Primary tasks 3건은 그 세 contexts (`count=3`). sidecar 재수록 없음 (D2). |
| §14 States | 옮김 → Components & States capture record + per-component applicability + Named gaps | Dual destination (E2a): capture record + Governance Named gaps for absent hover/pressed/focus/loading/success/error/deployment/authenticated-dashboard treatments. 본문 보존: Geist radio unchecked, compact icon action disabled in the baseline, otherwise default-only (A2). `not captured`를 `not-applicable` 사유로 쓰지 않음 (C1). Header-link navigation and radio selection have identified roles; their loading·error·success follow those roles. Geist secondary action / icon button / compact input have no exact action or validation identity — loading·error·success applicability is omitted at that field boundary, not closed from specimen/primitive names (C2). Geist example card는 kind/map 생략 (C4). generic `focus`는 `focus-visible` treatment로 승격하지 않음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion | 무출처 커브 없음(삭제할 값 없음). duration / signature-motion도 원본에 없음. Foundations Motion은 원문 “No reusable duration or easing curve is promoted. Baseline DOM capture cannot establish a universal motion system, and the current reference does not infer one.”를 보존하고, B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps가 같은 다섯 종류 게이트를 말함. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance; Tier 1 home / geist / font URL 옮김 → Experience Scope; font URL 옮김 → Typography & Assets | Dual/triple destination (E2a): footer Tier 1 `https://vercel.com/` · `https://vercel.com/geist/introduction` · `https://vercel.com/font`는 같은 dual/triple Scope / Typography / provenance surfaces. Footer `Verified: 2026-07-12 (omd:migrate)` keeps the `(omd:migrate)` mark in provenance Freshness (A1c). Tier 2 getdesign.md directory-only / Refero no reliable record는 numeric token 비승격 유지. |

## Revision 2026-08-23 (wave4 full resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6 (F1·F2 의무 최종 패스). Source FAIL: `docs/reviews/t2-1-wave4-2026-08-23-sol-full.md` §3.

| Item | Correction |
|---|---|
| Unresolved claim / footer A1/A1c | Font evidence class restored as `Unresolved claim` (captured but uncorroborated). Footer `(omd:migrate)` preserved in provenance Freshness and this log. |
| Distinctive/Avoid/Elevation/card/responsive B2a | Adjacent complete B2a on Distinctive trait readings, Avoid Don'ts, Elevation planar/grid-vs-shadow, example-card grid-versus-shadow, and responsive hierarchy/composable judgements. Source Key Characteristics/Don'ts are not a Vercel-authored UI specification. |
| Geist specimens C2 | Secondary action, icon button, and compact input omit loading/error/success applicability. No request or validation meaning is invented from specimen/primitive names. Header-link navigation and radio selection keep identified-role maps. |
| Source-row destinations E2/E2a/E2c | Log now records Governance Named gaps alongside Foundations/Typography/Components/Avoid/Layout/Content/Audience for §2/§3/§4/§6/§7/§8/§9/§10/§13/§14. F1/F2 rewritten after grep. |

### F1 / F2 (v6 mandatory final passes)

- **F1 B2a scan (full DESIGN.md reread):** Editorial/causal/judgement sentences have adjacent complete B2a (`derived editorial implementation inference` / `not Vercel-authored or a separately published UI specification`) on: Scope visual-character paragraph; Scope platform-narrative paragraph; Distinctive traits information/elevation/strict-boundary readings; Principles (four items + capture-bound application); Avoid Don'ts; Elevation primarily-planar / grid-vs-shadow reading; example-card grid-versus-independent-shadow field note; Layout imperative bullets; Layout responsive `maintaining the neutral hierarchy` / `compact and composable` reading; Content public-language block. Remaining Scope sentences are current public-home product story, named surfaces/URLs, GeistSans/Geist Mono load counts, official Geist example geometry, `ds.description` evidence-domain split, and capture-boundary (`tokens.note`). Font evidence `Unresolved claim` is an evidence class, not a brand causal reading. Header-link and radio state reasons are identified roles. Geist secondary/icon/input loading/error/success rows are omitted rather than judged. No unqualified brand causal reading remains outside those B2a envelopes.
- **F2 E2 grep (value + field/role context):** Font evidence grep-matched `Unresolved claim` for dashboard-only overrides; `Outside this capture` is not the class for that row. `(omd:migrate)` grep-matched provenance Freshness. Distinctive traits / Avoid / Elevation / card field note / responsive paragraph grep-matched adjacent complete B2a. Secondary action / icon button / compact input grep-matched the omission sentence for loading/error/success, not `can wait on its request` or validation-failure applicable. Ship red / Preview pink omission grep-matched Foundations and Named gaps. Declared Geist Pixel faces grep-matched Typography and Named gaps. Compact-input focus boundary grep-matched Components and Named gaps. Multi-layer card shadows grep-matched Foundations and Named gaps. Don'ts grep-matched Avoid and Named gaps. Authenticated dashboard breakpoints grep-matched Layout and Named gaps. Dashboard/deployment prohibition grep-matched Avoid, capture record, and Named gaps. Product-copy boundary grep-matched Content and Named gaps. Unspecified project-brief fields grep-matched Audience and Named gaps. Absent §14 treatments grep-matched capture record and Named gaps. `#171717` is Foundations Primary/Foreground and provenance `primary_color` (dual). Simple Icons `vercel` greps to provenance only. `ds.type: system` greps to provenance only. Home / Geist / font URLs grep to their dual/triple portable + provenance destinations. Unitless `1.1` / `1.33` / `1.5` / `1.43` grep to Type roles. `Type: button` ×3, `Type: input`, `Type: toggle`, `Type: card` grep per component. B3 five-kind gate greps to Foundations Motion and Named gaps. Compliance claims match the body.
