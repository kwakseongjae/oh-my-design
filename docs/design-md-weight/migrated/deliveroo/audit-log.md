# Deliveroo — separate-session audit (F3)

Auditor: fresh session, no access to the migration session's reasoning.
Scope: B2 / B2a and E1 / E2 / E2a–c only (`MIGRATION_RULEBOOK.md` v9).
Out of scope and untouched: token values, component tables, state applicability, document structure.
Date: 2026-08-26

## B2a — adjacent qualifier added to editorial / causal statements

| # | Location | Defect | Fix |
|---|---|---|---|
| 1 | `DESIGN.md` §3 Typography & Assets → **Font evidence** (now L191, before the table) | The whole evidence-class table carried **no** qualifier, although classifying the type record into "official product-use / custom face / type-attribution record / live computed surface-use / fallback / license" is an editorial grouping, and the "Live computed surface-use = None. The geo-block above prevented…" cell is a causal judgment. The parallel Foundations "Colour evidence classes" table already had one, so the omission was inconsistent within the same document. | Added the adjacent qualifier: "Grouping the type record by evidence class below, and the boundary each class draws, are derived editorial implementation inferences from the recorded sources; they are not Deliveroo-authored or a separately published UI specification. The classes are distinct and one does not vouch for another." |
| 2 | `DESIGN.md` §3 → Family, third bullet (L206) | The no-substitution rule said only "it is not a Deliveroo instruction" — B2a requires the evidence class to be carried to the end ("derived editorial implementation inference … not <Brand>-authored **or a separately published UI specification**"). Adjacent but incomplete = FAIL under the apple §1.2 precedent. | Rewritten to "This restriction is a derived editorial implementation inference applying the unknown-absence rule in Governance; it is not Deliveroo-authored or a separately published UI specification." |
| 3 | `DESIGN.md` §4 Components & States → Evidence boundary (L245) | The section qualifier was enumerated too narrowly ("applicability and interaction-kind judgments"), so two editorial judgments inside the section fell outside it: the search input's decision that the recorded generic `Focus` treatment is a different evidence class and is held back from the `focus-visible` row (B1), and the Promo card's decision to omit the interaction kind and applicability map entirely. | Enumeration widened to "applicability, interaction-kind, and evidence-class judgments … including which recorded treatment is held back from a state row and which component is given no interaction kind at all … from each control's product role and from the capture boundary". No applicability value, reason cell, or table row was changed. |
| 4 | `DESIGN.md` §5 Layout & Platforms, closing paragraph (L464) | "No live viewport measurement backs them, so they describe intended behaviour rather than verified responsive proof" is an inference drawn from the capture boundary, stated as bare fact. The structurally identical motion promotion gate in Foundations already carried the "derived editorial implementation inference from the evidence boundary" qualifier; this one did not. | Reworded so the inference is qualified in place: "…reading them as intended behaviour rather than as verified responsive proof is a derived editorial implementation inference from the capture boundary, and it is not Deliveroo-authored or a separately published UI specification." The evidence claim itself is unchanged. |

## E2 / E2a / E2c — ledger corrected to match the actual files

| # | Location | Defect | Fix |
|---|---|---|---|
| 5 | `migration-log.md` row **§15 easing curve values (4)** (L39) | The row named `ease-exit` and **`ease-standard`** as the legacy template's implementation-default family. Grep against `spec/omd-v0.1.md` L266–269 shows the template examples are `ease-enter` `cubic-bezier(0.0, 0.0, 0.2, 1)`, `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)`, `ease-standard` `cubic-bezier(0.4, 0.0, 0.2, 1)`, `ease-spring` `cubic-bezier(0.34, 1.56, 0.64, 1)`. The byte-identical pair is `ease-exit` + **`ease-spring`**; deliveroo's `ease-standard` `cubic-bezier(0.25, 0.1, 0.25, 1)` differs from the template. `DESIGN.md` (Motion, easing roles) and `provenance.md` (omission ledger L73–76) both had it right — only the log row was wrong, so the log was fixed, not the body. | Row now names `ease-exit` + `ease-spring` with their exact curves as byte-identical to `spec/omd-v0.1.md`, and records `ease-enter` + `ease-standard` as differing and omitted as unattributed rather than as re-injected. |
| 6 | `migration-log.md` row **§11 Brand Narrative** (L34) | E2a violation: the destination cell listed only Experience Scope (plus the Rooute dual destination), but the three §11 refusals actually landed as three bullets in Experience **Avoid** — a second destination the row never named. The log's own F2 note admits the refusals were moved to Avoid, so the row was left contradicting the pass that fixed it. | Destination cell now records the dual destination (Experience Avoid three bullets + Scope pointer sentence); the reason cell states which half went where. |
| 7 | `migration-log.md` row **§5 Layout Principles** (L27) | Two ledger errors in one row. (a) §5's *Spacing System* (base unit 8px, the eight-step scale, and the "restaurant cards 16px padding / section headers 24–32px" note) had **no** destination recorded at all, although it is split across Foundations Spacing and Layout Grid and container. (b) "radius scale 이중 목적지 → Foundations Shape + Layout" overstated: grep of the Layout section shows only the `9999px` value survives there, inside Whitespace "Pill rhythm" — the full 0 / 4 / 8 / 16 / 9999 scale exists only in Foundations Shape. | Destination cell now records the Spacing System dual destination explicitly and narrows the radius claim to the `9999px` step; the reason cell states that this is not a whole-scale dual destination. |
| 8 | `migration-log.md` row **§14 States** (L37) | E2c: the row claimed "All ten rows retained **verbatim**". The values and every quoted string are indeed preserved, but the connective prose was normalized (source `+` written as `plus`, "with proactive message" → "with a proactive message", "(full card width)" → "at full card width"), so the compliance claim was stronger than the body. | Claim softened to what the body actually delivers: exact values and byte-identical quoted strings, with connective prose normalized — "complete rather than character-for-character verbatim". No state row was edited. |
| 9 | `migration-log.md` F2 paragraph (L45) | The dual-destination list did not match the corrected rows, and the paragraph described the §15 curve claim as fixed while the §15 row still named `ease-standard`. | List updated (§5 spacing note, `9999px` pill value, §11 refusals) and a sentence added recording that the §15 row itself was corrected in this separate-session audit. |

## E1 — provenance derived scope widened to match the body

| # | Location | Defect | Fix |
|---|---|---|---|
| 10 | `provenance.md` Claim ledger (new row after the "Editorial interpretation" row, L106) | The ledger's only editorial-interpretation entry was the source comment's own three readings ("food-first" philosophy, pill-native system, contrast-first PDS 2.0). The portable body actually marks ~19 passages as derived editorial implementation inference **of this migration** — a much wider derived scope than provenance recorded. Read alone, provenance understated how much of the document is migration-authored interpretation. | Added a claim-ledger row enumerating the portable document's own B2a qualifications and classing them as "Derived editorial implementation inference of this migration", noting they are stated adjacent in the body (B2/B2a) rather than held only in the sidecar (E1). |

## E3 — gate false positives

None found in this audit. No value, hex spelling, curve string, URL, or state label was altered to change gate behaviour; the only body edits are added or completed qualification sentences. The `explicitlyNegatesClaim('scope')` false positive the migration session disclosed (log L47) did not recur — the Scope evidence-boundary sentence passes as written.

## Not changed (observations only, outside B2a / E2 scope)

- `migration-log.md` L3 records `Ruleset: MIGRATION_RULEBOOK.md v8 (2026-08-25)`. The rulebook header is now v9 (2026-08-26, adds A5), and its 개정 이력 table has no v9 row. The v8 record is consistent with the seven sibling logs migrated in the same wave, so it was left as the honest record of the version actually used. A5 was not part of this audit's mandate.

AUDIT_DONE deliveroo fixes=10
