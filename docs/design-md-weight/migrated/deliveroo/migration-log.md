# Deliveroo migration log

Ruleset: `MIGRATION_RULEBOOK.md` v8 (2026-08-25)

Source: `web/references/deliveroo/DESIGN.md`
Destination: `docs/design-md-weight/migrated/deliveroo/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/deliveroo/provenance.md`
Date: 2026-08-26
Worker: Opus 5 T2 migration
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd: `portable_core: true`, reasons `[]`, placeholders 0)
Gate: `test-v2/tools/migrate-reference.mjs --brand deliveroo --gate-only` — PASS, problems `[]`

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance Identity; name 이중 목적지 → H1 `# Deliveroo Design System`; `primary_color` 이중 목적지 → Foundations semantic colour + provenance claim ledger | Portable file carries no frontmatter. The logo record is a SimpleIcons pointer, so Typography & Assets states the pointer without claiming a distributed asset or licence. |
| YAML `verified`, `omd`, `tokens.source`, `tokens.extracted`, `components_harvested` | 분리 → provenance Identity + Freshness | Process and freshness metadata stay outside the portable top matter. |
| YAML `tokens.note` | 분리 → provenance (verbatim); its four facts 이중 목적지 → Experience Scope (`#00CCBC` confirmation, Seaweed naming, Stratos) and Foundations (`#121212` dark canvas) | The note is an attribution sentence, so the ledger keeps it verbatim while the portable body carries the facts under their evidence classes. |
| YAML `tokens.colors` (15 keys) | 옮김 → Foundations semantic colour + Foundations colour evidence classes; attribution 이중 목적지 → provenance claim ledger | All 15 values retained. Grouped by evidence class rather than merged into one authority, per the source ledger's own three-way split. |
| YAML `tokens.typography` | 옮김 → Typography & Assets Family + Type roles | Family split, all eight roles, sizes, weights, unitless line-height ratios (`1.10`, `1.15`, `1.20`, `1.50`, `1.00`), tracking, and use notes retained as ratios rather than converted to px. |
| YAML `tokens.spacing` / `rounded` / `shadow` | 옮김 → Foundations Spacing / Shape / Elevation; container geometry 이중 목적지 → Layout & Platforms | Named steps (`xs`…`section`) kept alongside the px scale; both shadow strings kept exactly. |
| YAML `tokens.components` (10 records) | 옮김 → Components & States | Every record's primitive `type` is preserved per component (button ×3, input, card ×2, badge ×2, tab, toggle) instead of being flattened into `Kind: interactive`. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits; PDS 2.0 / Seaweed / AA detail 이중 목적지 → Scope + Foundations evidence classes; Stratos 6° detail 이중 목적지 → Scope + Typography & Assets | The eight Key Characteristics survive as Distinctive traits under an adjacent B2a qualifier because selecting them is an editorial judgment. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic colour | All five groups, every role name, value, and use sentence retained, including the AA notes on `#767676` and the Seaweed equivalence. |
| §3 Typography Rules | 옮김 → Typography & Assets (Font evidence, Family, Type roles, Typography rules) | The `-0.5px` form of the hero tracking is retained in the note under the table so both the px form and the unitless token form survive. The four §3 Principles carry an adjacent B2a qualifier. |
| §4 Component Stylings | 옮김 → Components & States | All 11 body components (the 10 YAML records plus Category Card) retained with anatomy, values, and use. The search input's `Focus: 1.5px solid #00CCBC` stays a source-recorded generic focus observation and is deliberately kept out of the `focus-visible` row (B1). |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts unresolved | 분리 → provenance Capture boundary, Sources, Conflicts; the geo-block boundary 이중 목적지 → Experience Scope | `#00CDBC` and `#00c1b2` exist only in the provenance Conflicts block; the portable body carries `#00CCBC` plus a one-line pointer to the resolved conflict. |
| §5 Layout Principles | 옮김 → Layout & Platforms (Grid and container, Whitespace); Spacing System 이중 목적지 → Foundations Spacing (base unit 8px and the eight-step scale) + Layout & Platforms Grid and container (the 16px card padding / 24–32px header margin note); radius scale → Foundations Shape, with only its `9999px` full step 이중 목적지 → Layout Whitespace "Pill rhythm" | The three Whitespace Philosophy readings are editorial, so they sit under an adjacent B2a qualifier. The full 0 / 4 / 8 / 16 / 9999 scale exists only in Foundations Shape; Layout carries the pill value alone, so this is not a whole-scale dual destination. |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | Four levels retained. Level 3 keeps its qualitative description; no numeric shadow or scrim value is invented for it, and the gap is named in Governance. The Shadow Philosophy paragraph carries an adjacent B2a qualifier. |
| §7 Do's | 옮김 → Experience derived implementation principles | Six of the seven Do rules become their own principle bullets; the seventh ("Lead with food photography") is merged into the §12 "Food is the hero" principle because the two state the same rule with the same authority. The `#f5f5f5` segmentation rule is kept in both Experience and Foundations. |
| §7 Don'ts | 옮김 → Experience Avoid | All seven retained under an adjacent B2a qualifier. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms (Breakpoints, Touch targets, Collapsing strategy); the tappable-card fact 이중 목적지 → Components & States Restaurant card kind evidence | A closing sentence records that these are source-recorded rules rather than verified responsive proof. |
| §9 Agent Prompt Guide — prompt wrapper, example prompts, iteration guide | 삭제 (+ §9-only values 옮김) | Tool-facing packaging has no receiving Core slot. Before deleting, the four value groups that existed **only** in §9 were moved into Components & States labelled *legacy prompt-block guidance* (restaurant-name type and rating colour; magnifier icon colour; deal-card headline/subtext/child CTA; full-width primary button on mobile). The moved list is also recorded in the provenance omission ledger. The quick colour reference, the "25–35 min" string, the pill-badge recipe, and the eight iteration rules restate values already carried elsewhere and are deleted as repetition. |
| §10 Voice & Tone | 옮김 → Content & Locales | The voice paragraph, all seven tone rows, the forbidden register, and the three voice samples are retained; the samples keep their source-declared classes (verified / public fact / illustrative) in a copy-evidence table. |
| §11 Brand Narrative | 옮김 → Experience Scope; the three refusals 이중 목적지 → Experience Avoid (three bullets) + Scope pointer sentence; Rooute motif 이중 목적지 → Scope + Typography & Assets Assets | Founding, founders, the bicycle year, the 2016 DesignStudio rebrand, 400 assets, ten markets, the 2024 PDS 2.0 overhaul and its attribution, and the refuses/embraces contrast are retained — the embraces sentence sits in Scope, the three refusals sit in Experience Avoid, and Scope carries a one-line pointer to them. The "growing up" and "logistics utility → premium-yet-friendly" readings are labelled editorial interpretations in place. |
| §12 Principles | 옮김 → Experience derived implementation principles | All five principles and their UI implications retained under an adjacent B2a qualifier covering the whole list. |
| §13 Personas | 삭제 | The source itself discloses them as fictional archetypes with illustrative names. D2: not promoted to primary tasks or audience, and **not** re-hosted in provenance — no name, age, city, or segment profile appears in any output file. Experience Audience keeps only group-level scope. |
| §14 States | 옮김 → Components & States "Source state contract" + per-component applicability | All ten rows retained with their exact values (`#b3e8e5`, `rgba(255,255,255,0.6)`, 1.4s shimmer, 3s toast, 18px/16px copy sizes) and every quoted string byte-identical; connective prose was normalized (`+` written as `plus`), so the rows are complete rather than character-for-character verbatim. The out-of-zone treatment is also attached to Restaurant card as its source-recorded disabled treatment. Applicability is judged by control role, never by record completeness. |
| §15 Motion & Easing — durations, easing roles, signature motions, reduced motion | 옮김 → Foundations Motion | Five duration tokens, four easing role names with their uses, three signature motions (including the 40ms stagger), and the reduced-motion contract retained, all under an adjacent B2a qualifier noting that the source ledger attributes nothing to motion. The B3 promotion rule is present in full: it names transition properties, animation name, duration, easing, and reduced-motion behavior, and gates promotion on component-specific computed observation. |
| §15 easing curve values (4) | 삭제 → exact values 보관 → provenance omission ledger | Unattributed curves; `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` and `ease-spring` `cubic-bezier(0.34, 1.56, 0.64, 1)` are byte-identical to the `ease-exit` and `ease-spring` examples in the legacy authoring template `spec/omd-v0.1.md`, which that file labels non-brand implementation defaults. `ease-enter` `cubic-bezier(0.2, 0.6, 0.25, 1)` and `ease-standard` `cubic-bezier(0.25, 0.1, 0.25, 1)` differ from the template examples and are omitted as unattributed rather than as re-injected. Role names and uses stay in Foundations; the four exact strings are stored in the provenance omission ledger, and the gap is named in Governance. |
| Philosophy-layer HTML comment (source list, persona disclosure, interpretive-claim disclosure, public-record note) | 분리 → provenance Sources / Capture boundary / Claim ledger; the interpretive and geo-block boundaries 이중 목적지 → Experience Scope, Foundations Motion, Content & Locales | The comment's own evidence-class disclosures are what the portable B2a qualifiers restate; keeping them only in the sidecar would have moved the authority qualification out of the standalone document. |

## Required final passes

- **F1 (B2a scan).** The portable document was re-read end to end after drafting. Adjacent qualifiers were added to every interpretive or causal passage rather than to Principles alone: Scope (surface-scope, evidence-class, and causal statements; the "growing up" and rebrand-positioning readings labelled in place), Primary tasks (task formulation), Audience (persona-retention decision stated in place), Distinctive traits (selection judgment), Derived implementation principles, Avoid, Foundations colour evidence classes, Elevation shadow reading, the whole Motion contract, the motion promotion rule, Typography rules, Components & States evidence boundary, the Source state contract table, Layout Whitespace, Content & Locales voice characterization, copy-evidence classes and locale register, Typography & Assets asset authority, the easing-role re-injection reading, and Governance. The Family section's no-substitution rule is marked as an application of the Governance unknown-absence policy rather than a Deliveroo instruction.
- **F2 (E2 cross-check).** Every row above was grep-verified across the three output files before being written. Dual destinations are recorded in both places (`primary_color`, `tokens.note` facts, Seaweed/Stratos/`#121212`, geo-block boundary, the §5 spacing note, the `9999px` pill value, the §11 refusals, Rooute, the tappable-card fact, and the §9-only moved values). The §9 row states both the deletion and the move because both happened; the §15 curve row states both the deletion and the provenance retention (E2b). The B3 compliance claim is made only because the five evidence kinds and the component-specific gate are literally present in Foundations → Motion. Two rows failed their own grep on the first attempt and were fixed rather than left standing: the §11 row claimed the refuses/embraces contrast was retained when the portable body carried neither, so the three refusals were added to Avoid and the embraces sentence to Scope; and the §15 row's template-curve claim over-generalized, so the portable body and the provenance ledger now name only `ease-exit` and `ease-spring` as byte-identical to the legacy template examples, with `ease-enter` and `ease-standard` recorded as differing from them. The §15 curve row itself was left naming `ease-standard` and was corrected in the separate-session audit; see `audit-log.md`.
- **E3.** The migration gate produced no false positive, and no value, hex spelling, curve string, URL, or state label was distorted to change gate behaviour. `#00CDBC` and `#00c1b2` are written exactly as the source writes them.
- **E3 disclosure — separate tool.** The Portable Core inspector (`scripts/design-md-core-conformance.cjs`, `explicitlyNegatesClaim('scope')`) first read the Scope evidence-boundary sentence "…so no computed product-surface observation backs any value in this document" as a scope negation, because its heuristic matches `no` within 32 characters of `product`. The sentence states the evidence class, not the absence of a product surface. It was reworded to "…so every value in this document rests on those recorded sources rather than on a computed reading of a live Deliveroo interface", which carries the same boundary; no value, class, or qualification changed. Recorded here rather than left silent because the trigger is a heuristic reading an honest boundary sentence.

## 규칙집 버전 기록 (2026-08-26, 오케스트레이터)

이 이관은 **v8** 규칙집으로 수행됐다(위 기록 그대로가 사실이다). 작업 중 규칙집이
**v9**로 올라가며 A5(브랜드 발행 문자열 바이트 보존)와 게이트 `copy-loss` 검사가
신설됐다. 소급 재검증 결과 이 산출물은 A5 위반 **0건**이며, 정착 빌드
(`migrate-reference.mjs` SHA `3170bb7e…`, 자체테스트 10/10)에서 게이트 PASS다.
v8 표기를 v9로 고쳐 쓰지 않는다 — 어느 규칙집으로 만들었는지와 어느 규칙집으로
검증했는지는 다른 사실이고, 원장은 둘 다 보여야 한다.
