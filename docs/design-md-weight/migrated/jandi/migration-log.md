# jandi migration log

Source: `web/references/jandi/DESIGN.md`
Sibling read (not the migration input): `web/references/jandi/.verification.md`
Destination: `docs/design-md-weight/migrated/jandi/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/jandi/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `5b303d98458f1420149e0b11c6fe284064da41790e0da1dd4c55038bca15485d` (`web/references/jandi/DESIGN.md`). Sibling SHA-256 `cd0732870746536a1ed380d1c555e2571bcc5110409eb37b1c7a1965739a1dd2` (`web/references/jandi/.verification.md`). Worker-close portable DESIGN SHA-256 `1629ad8ca28246d18ee9f40eac1bc520638e045542ebab89d8f7ee40f35cafeb`. Auditor SHA-256 `140445153fbccc43faa84d3fac7dbced4c934995bcf6c4bfccb2c8869ca28064`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope (inspected landing URL); `primary_color` 옮김 → Foundations Primary; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# JANDI Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–16. YAML `homepage` `https://www.jandi.com` is `provenance.md` 13 (identity field). The inspected landing URL `https://www.jandi.com/landing/kr` is dual: `DESIGN.md` 9/21 + `provenance.md` 23/45/46/63 (E2a). `#00c473` is dual: `DESIGN.md` 34/45/55/76/170/172/179 + `provenance.md` 14 (E2a). Favicon slug is dual: `DESIGN.md` 149 + `provenance.md` 16 (E2a). |
| YAML `omd: "0.1"`, `verified`, `verification_v2`, `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–21, 31–35. The source footer's **Verified:** 2026-07-13 producer string is at `provenance.md` 37; the freshness `verified` cell is 31. 39 is Conflicts. `reconciled` DESIGN 0 / provenance 2 (`provenance.md` 19/83). `components_harvested` DESIGN 0 / provenance 2 (`provenance.md` 21/164). These are ledger keys with no portable slot. No `ds.type` field exists in the source; that absence is recorded at `provenance.md` 23, not filled. |
| YAML `tokens.colors` (7 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 76–82. All seven roles kept with names, values, and token-set keys: primary `#00c473`, canvas `#ffffff`, on-dark `#ffffff`, ink `#000000`, ink-muted `#333333`, muted `#a2a2a2`, action-ink `#041911`. Canvas and on-dark stay two keys (`tokens.colors.canvas` dest 2 at 74/77 · `tokens.colors.on-dark` dest 2 at 74/78) and are not folded (A4). Pairing qualifier at 74. |
| YAML `tokens.typography.family.ui` (`Noto Sans`) | 옮김 → Typography & Assets Family | Family `DESIGN.md` 129 (`tokens.typography.family.ui`). Live computed class also at 119. `Noto Sans` dest 19. |
| YAML `tokens.typography.hero` / `section` / `nav-action` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 139–141. Unitless line heights stay unitless and are never converted to a replacement px (A1a): `1.43` dest 5 at 139/140/141/143/145. The §2 px spellings `56px/700/80px` · `42px/700/60px` · `56px/700/66px` sit on `DESIGN.md` 122/143/272. All three YAML `use` strings restored verbatim in the Token-set use column (A1, A3): `Public landing headline` dest 2 · `Public landing feature heading` dest 2 · `Public global navigation action` dest 2. Landing-action `15px / 500` is disambiguated from nav-action `14` at 145/199 (A4). |
| YAML `tokens.spacing` (4 steps) / `tokens.rounded` (3 steps) | 옮김 → Foundations spacing + shape; also Layout | Unitless spacing: `DESIGN.md` 86 (`nav-action-y: 7` · `nav-action-x: 14` · `landing-action-y: 12` · `landing-action-x: 30`). Shape: `DESIGN.md` 92 (`action: 6` · `floating-nav: 10` · `security-card: 16`). `nav-action-x: 14` dest 4 at 86/88×2/177. `landing-action-x: 30` dest 4 at 86/88×2/201. `tokens.rounded.action: 6` dest 2 at 178/202 (`tokens.rounded.action` dest 3 at 94/178/202; `action: 6` dest 3 at 92/178/202). `tokens.rounded.floating-nav: 10` dest 1 at 224 (`tokens.rounded.floating-nav` dest 2 at 95/224; `floating-nav: 10` dest 2 at 92/224). `tokens.rounded.security-card: 16` dest 2 at 98/251 (`security-card: 16` dest 3 at 92/98/251; 247 is padding). `tokens.spacing.nav-action-x: 14` is disambiguated from `tokens.typography.nav-action.size` `14` at 88. No shadow key in the token set. |
| YAML `tokens.components.security-environment-card` | 옮김 → Components & States Security environment card | `DESIGN.md` 237–251. Verified primitive type preserved, not flattened to `Kind` (A1b): `Primitive type: \`card\`` dest 2 = YAML `type: card` 2. Token-set type/bg/radius/padding/use rows at 246–250. YAML `use` restored verbatim at 250. 251 is Token-set shape. 253 is the AI heading. |
| YAML `tokens.components.ai-environment-card` | 옮김 → Components & States AI environment card | `DESIGN.md` 255–267. `Primitive type: \`card\`` at 256. Token-set type/bg/radius/padding/use rows at 263–267. YAML `use` restored verbatim at 267. 261 is Font. Radius stays on `tokens.components.ai-environment-card.radius` and is not invented as `tokens.rounded.ai-card` (98). The three §4-only controls (global navigation action, landing action, feature floating navigation) are `not in the token set` dest 3 (169/194/218). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–39. 2015 introduction, six marketing records / five URLs, white field / black Korean type / green conversion action, June 2026 Project 2.0, authenticated-product boundary. Atmosphere readings carry adjacent complete qualifiers at 9 and 11 (B2/B2a). |
| §2 Layout & Grid | 옮김 → Layout & Platforms | `DESIGN.md` 272–274. 56px/700/80px hero, 42px/700/60px feature headings, feature-route 56px/700/66px, 40px or 32px secondary, 7px 14px / 12px 30px action padding, no page container maximum / breakpoint / logged-in application shell / universal grid. Desktop-sample qualifier at 274 (B2/B2a). |
| §3 Color & Typography | 옮김 → Foundations semantic color + Typography & Assets | Color `DESIGN.md` 76–82. Evidence classes 117–123; family 129–131; type roles 139–141. 676 uses dest 2 (36/119). Seven JANDI-CDN OTF URLs dest 3 (36/119/131). SIL Open Font License dest 3 (120/125/296). `icomoon` dest 6 · `swiper-icons` dest 6. Official product-use / live computed / licence / declared-only / measured hierarchy / documentation-chrome readings carry adjacent complete qualifiers at 125, 133, 145, and 153 (B2/B2a). |
| §3 Noto licence URL / company / Project 2.0 / support-centre URLs | 옮김 → Experience Scope; 분리 → provenance | Company URL dual: `DESIGN.md` 9 + `provenance.md` 56/68 (E2a). Project 2.0 URL dual: `DESIGN.md` 9 + `provenance.md` 57/69 (E2a). Support-centre URL dual: `DESIGN.md` 9 + `provenance.md` 58/70 (E2a). Noto licence URL is provenance-only: `DESIGN.md` dest **0** / `provenance.md` 59/71. Scope 9 names “Noto’s licence documentation” without the URL (fitpet-type 2nd destination withdrawn). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 166–267. Five declared controls. Capture selectors stay in the Use lines (`DESIGN.md` 176/200/223/245/262) and are also in `provenance.md` 155–159 (E2a). 261 is Font. |
| §5 Iconography | 옮김 → Typography & Assets Assets | `DESIGN.md` 151. Declared-only `icomoon` / `swiper-icons`; no icon token promoted. |
| §6 Imagery & Illustration | 옮김 → Typography & Assets Assets | `DESIGN.md` 150. No image ratio, crop, overlay, or reusable screenshot frame. Do not derive an illustration system. |
| §7 Motion | 옮김 → Foundations motion | `DESIGN.md` 104–108. No duration, easing, transition, hover result. Motion is intentionally undocumented. |
| §8 Accessibility | 옮김 → Components contrast pairs + Capture record | Global-nav contrast `#ffffff` with `#00c473` at `DESIGN.md` 179. Landing-action contrast `#041911` with `#ffffff` at 203. Focus-indicator sentence at 160, under the capture-record qualifier. `Noto Sans` backing is in Family 131. |
| §9 Content & Voice | 옮김 → Content & Locales | `DESIGN.md` 279. Practical collaboration infrastructure; concise, explanatory, operational; no invented authenticated-product microcopy. This source has no Agent Prompt Guide; §9 is voice, not a tool-command section, so it is mapped rather than deleted. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 281–287. Voice adjectives and the Do / Don’t table kept byte-exact (`State the work problem before describing a capability.` dest 1). The “correspondingly concise…” reading carries an adjacent complete qualifier at 300 (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: Toss Lab official history dates the JANDI launch to 2015; collaboration platform for business communication; current direction AI-assisted and project-based work; June 2026 JANDI Project 2.0; project-management experience integrated with messaging; redesigned around visibility for both individual contributors and team managers; closing sentence `This reference keeps that product evolution separate from the measured public-marketing styles` dest **1** at 13 (the same line also paraphrases the sentence; exact dest is 1, not 2). `2015` dest 3 (9/13×2). `Project 2.0` dest 8. Marked there as narrative context that supplies no interface tokens; founding-and-Project-2.0 reading carries an adjacent complete qualifier in the same paragraph (B2/B2a). |
| §12 Principles — 4 numbered | 옮김 → Experience principles | `DESIGN.md` 45–48 under the B2a form at 43: "These four items are a derived editorial implementation inference from the verified surfaces; they are not JANDI-authored or a separately published UI specification." JANDI has no published first-party UI specification in the source, so the toss-form close is used (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 175–197 (23 rows). |
| §13 Personas — 3 stakeholder groups | 옮김 → Experience audience | The source invented no named or demographic personas and says so. The three first-party groups (individual contributors, team managers, teams) and the Project 2.0 distinction are retained in Audience at `DESIGN.md` 28 in the source’s own wording. Their announced-experience descriptions stay in Audience and are not promoted to `primary-tasks` (19 says the three tasks do not come from the persona section). No name, age, city, or invented affiliation is re-hosted in the sidecar (D2, D2a). Disposition at `provenance.md` 126. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Body preserved at `DESIGN.md` 160 (A2; the catalog graph is still 0/440, so nothing is delegated), including zero interaction events / kinds / observed states. The applicability rule is at 162–164, whose qualifier at 164 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. The three actions are destination / feature-navigation roles, so loading/error/success close with a role reason (C2 v10): `loading \| not-applicable` dest 3 · `error \| not-applicable` dest 3 · `success \| not-applicable` dest 3 · `loading \| applicable` dest 0. Hover and disabled stay applicable; visual treatments omitted. The two cards are `Kind: non-interactive` and carry no state map (C4). |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 104–108. No duration, curve, or reduced-motion was observed. No unattributed cubic-bezier is present, so nothing is deleted. B3 is held: the promotion condition at `DESIGN.md` 108 names all five evidence kinds — transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 340. |
| §16 Do's | 옮김 → Experience application rules | `DESIGN.md` 54–57 (four Do rules), under the grouping qualifier at 52. Not placed in Governance controlled copy. |
| §16 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 63–66, under the qualifier at 61. The four prohibitions are the source’s own list, including `authenticated product-app system` (source wording, dest 1) and the removed `/ko/pricing` / pricing-card / input / universal shadow claims. No invented domain is added. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** / **Pipeline** / **Catalog position** | 분리 → provenance; URL 옮김 → Experience Scope + Assets | Freshness table `provenance.md` 31–35; producer string **37**. Tier 1 list 63–71; Tier 2 75–77. Conflicts unresolved: none — `provenance.md` 39. Pipeline / catalog-position producer strings have no portable slot. |

## Sibling handling (`web/references/jandi/.verification.md`)

The sibling exists — confirmed with `find web/references/jandi -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 89–100 and is **not** promoted into `DESIGN.md`. 102 starts the sibling-only list.
- Values it carries that the visible source body does not: Toss Lab 2014 founding; company-page principles about ownership / sharing work records / teamwork; card `box-shadow` `rgba(0, 18, 47, 0.08)` / `rgba(0, 18, 47, 0.16)`; `home::h1` white; floating-nav `rgba(0, 0, 0, 0)` and class `floatingNavButton`; coverage score 71; 27 fingerprints; CDN path `https://cdn.jandi.com/landing/_next/static/media/`; seven `NotoSans-*` filenames. Recorded at `provenance.md` 102–110. 112 is Byte-form notes.
- Measured `DESIGN.md` 0 for those sibling-only strings: `2014` 0 · `rgba(0, 18, 47` 0 · `cdn.jandi.com` 0 · `rgb(0, 196, 115)` 0 · `floatingNavButton` 0 · `coverage score` 0. Exact `NotoSans-Thin` and `score 71` are dest 0 in sibling as well (sibling writes `NotoSans-{Thin,…}` and `coverage score \`71\``).
- RGB spellings are sibling records of hex values the source already names; they stay in provenance raw samples.
- Capture selectors `home::[data-omd-capture="10"]` / `"19"` and the security/AI list-item selectors are already in the source §4 Use lines (`DESIGN.md` 176/200/223/245/262) and are corroboration, not a sibling-only promotion.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. JANDI’s issued strings are mostly Latin brand names plus `잔디`, so a hand sweep of published names is mandatory even when the needle set is small.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published names in the source body | 12 distinct | 0 | 0 | JANDI / 잔디 / Toss Lab / Project 2.0 / Noto Sans / SIL Open Font License / icomoon / swiper-icons / `Collaboration_icon1__cFiWm` / `Member_icon1__MIU61` / `Security_securityEnvironmentList__3CRP0` / `JandiAi_aiEnvironmentList__2ng2t`. |
| YAML `use` strings | 2 | 0 | 0 | Both restored verbatim as Token-set use. |
| `node test-v2/tools/latin-copy-audit.mjs --brand jandi` | 1 lost / 26 scanned | 1 | 0 | Lost sample is `site:styles.refero.design JANDI` from the sibling/source Tier 2 search line. That is a tool query, not issued product copy (A5a: 카피에 대한 서술·UI 메타는 바늘이 아니다). |
| Sibling published strings | 0 distinct sibling-only issued labels | 0 | 0 | Sibling raw samples are measurements and class names, not issued CTAs. 2014 founding is sibling narrative, not source-body copy. |

Sub-needle names confirmed present individually in `DESIGN.md`: JANDI, 잔디, Toss Lab, Project 2.0, Noto Sans, SIL Open Font License, icomoon, swiper-icons, 2015.

A5 분모: hand sweep of source published names 12 extracted / 0 missing; latin-copy-audit 1 lost / 26 scanned (lost = catalog search query, not published copy); gate `copy-loss` compared 0 / candidates 99.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand jandi --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 99 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 99.

Worker-close portable DESIGN SHA-256 `1629ad8ca28246d18ee9f40eac1bc520638e045542ebab89d8f7ee40f35cafeb`. Auditor SHA-256 `140445153fbccc43faa84d3fac7dbced4c934995bcf6c4bfccb2c8869ca28064`.

## Deviations recorded

- `DESIGN.md` is 4,484 words by `wc -w` (342 lines), above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: five declared component records, a seven-state applicability matrix on three interactive controls, two non-interactive cards with token-set field rows, the full §11 narrative including the last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- JANDI has no published first-party UI specification in the source, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Auditor measure: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 23 = `not JANDI-authored` 23 = `separately published UI specification` 23. Provenance derived ledger 23 rows (E1 1:1). Worker-close was also 23=23; five existing qualifiers were expanded in place (Scope ¶2 wordings-as-source · Primary tasks surface-or-control · Semantic role names · Applicability not-complete-coverage · Unresolved named-values-not-permissions).
- F1 B2a scan was re-run by the auditor. Every causal or interpretive sentence in Scope, Principles, Content, Docs-domain classification, and state-role verdicts was checked for evidence class; derived readings carry an adjacent complete qualifier that names the judgment.
- F2: each log destination was re-grepped in the written files. Dual destinations name both files. The Noto licence URL is not a DESIGN 2nd destination. Compliance claims (B3 held; `not in the token set` dest 3; C2 pair-closed) are made only where the body text exists.

## Mandatory passes

- F1 B2a scan: done on the finished `DESIGN.md` before submission.
- F2 E2 contrast: done; destinations above were grepped, not recalled.
