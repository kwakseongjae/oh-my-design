# FLO migration log

Source: `web/references/flo/DESIGN.md`
Destination: `docs/design-md-weight/migrated/flo/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/flo/provenance.md`
Date: 2026-08-26
Worker: opus5 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v10**
Gate: `node test-v2/tools/migrate-reference.mjs --brand flo --gate-only` — result recorded at the end of this file, with the reason it is not conformance evidence.

## A5 — brand-published strings

The catalog `name` is the Latin `FLO`, so the H1 is `# FLO Design System` and the Latin form is the one the body uses in running prose, exactly as the source does. The Korean form `플로` is carried byte-for-byte at first mention in Scope (`FLO (플로)`) and again in Content & Locales → Terminology, beside the Latin form rather than replaced by it.

Every Korean run the source publishes is present in the portable body byte-for-byte, including punctuation. Counts are occurrences, measured per string with `grep -oF '<string>' DESIGN.md | wc -l` — not `grep -c`, which would report matching lines.

| String | Body count | Lines | Evidence class as the source states it |
|---|---:|---|---|
| `플로` | 2 | 9, 478 | Korean company name |
| `가볍게, 나답게 FLO.` | 1 | 459 | "the brand tagline" — FLO's own line |
| `지금 음악 찾기` | 2 | 234, 463 | state-contract empty-state CTA |
| `내 구독 보기` | 2 | 240, 464 | state-contract post-purchase secondary action |
| `내가 원하는 음악이 물 흐르듯 끊임없이 흘러나온다.` | 1 | 470 | marked *(Illustrative)* in the source |
| `1억 곡 중에서 딱 나의 취향만.` | 1 | 471 | marked *(Illustrative)* in the source |
| `오늘 기분에 맞는 음악, FLO가 먼저 알아요.` | 1 | 472 | marked *(Illustrative)* in the source |

Seven Korean runs exist in the source and seven are in the portable body — `grep -n '[가-힣]' web/references/flo/DESIGN.md` returns 7 lines (60, 280, 281, 282, 283, 321, 327), and every string on them is above. **Nothing Korean is dropped.** The three illustrative lines are kept under the source's own marker rather than deleted, following the approved handling of illustrative Korean voice samples; the marker travels with each line so no reader can mistake them for captured copy. Their English glosses sit beside the Korean and never replace it. The trailing period the source writes on the tagline (`가볍게, 나답게 FLO.`) is preserved; the sibling's marker-free form (`가볍게, 나답게 FLO`) is recorded in `provenance.md` and is not substituted.

Latin-side check (the gate's `copy-loss` sees non-Latin runs only, so this was done by hand). Every quoted Latin string in the source was extracted and searched across the three outputs. The classes:

- **Product and company names** — `FLO`, `RE;CORD`, `FLO Studio`, `FLO Shop`, `Dreamus Company`, `SK Telecom`. All verbatim in the body, `RE;CORD` with its semicolon intact. Portable lines for each of the three product names: 11, 17, 111, 179, 478, 528.
- **Rule identifiers the product itself publishes** — `btn_bg_blue_s` (245), `btn_bg_error_b` (256, as the source's own `btn_bg_error_b base`), `btn-round` (269), `btn-buy` (291, 303), `header-multi-track-search-button` (315), `comp_inp_txt` and `comp_inp_pw` (both 339). All byte-exact; underscores and hyphens are not normalised to each other.
- **Type family names** — `Pretendard Variable`, `Pretendard`, and the whole declared stack including `-apple-system`, `BlinkMacSystemFont`, `Roboto`, `Segoe UI`, `Helvetica`, `sans-serif`. Carried in the source's own quoted form from §9, not the sibling's unquoted form.
- **Forbidden-register examples** — `"best ever"`, `"revolutionary"`, and `"music lovers"` from §10's Don't column. All three verbatim in the Content & Locales voice table.
- **Token `use:` descriptions** — these do not survive as quoted strings because they are field descriptions rather than published copy; each one's content lands on its Role, Use, or component line. Audited row by row in the disposition table below.
- **§9 prompt fragments** — audited in the §9 row below; two carry §9-only values that were moved (A3), the rest restate values that land elsewhere.

No brand-published Latin label was lost.

### Multi-destination published strings (E2a)

| String | Portable locations |
|---|---|
| `플로` | Scope ¶1 (9), Terminology (478) |
| `지금 음악 찾기` | Source state contract row (234), Brand-published lines (463) |
| `내 구독 보기` | Source state contract row (240), Brand-published lines (464) |
| `RE;CORD`, `FLO Studio`, `FLO Shop` | Scope ¶2 (11), Scope ¶5 (17), Evidence-domain boundary (111), Font evidence "Outside this reading" (179), Terminology (478), Named gaps (528) |
| `btn-buy` | Medium Pill Button heading (291) and its qualification sentence (303) |
| `가볍게, 나답게 FLO.` | Brand-published lines (459) only — single destination |

## Section-by-section disposition

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity `id` / `country` / `category` | 분리 → provenance Identity | Portable file has no frontmatter. These three are ledger-only. |
| YAML `name` `FLO` | 분리 → provenance Identity; 옮김 → portable H1 + running prose | Dual (E2a). `# FLO Design System`. |
| YAML `homepage` `https://www.music-flo.com` | 옮김 → Experience Scope; 분리 → provenance Identity / Surfaces / Sources | Dual (E2a). Grep-verified: 1 occurrence in the portable body. |
| YAML `primary_color` `#3f3fff` | 옮김 → Experience (traits, principles, application, avoid), Foundations Semantic color, Motion, five component records, three state-contract rows; 분리 → provenance Identity | Multi-destination (E2a), grep-verified at **23** occurrences in the portable body. |
| YAML `logo` (`type: favicon`, Google s2 favicon slug at `sz=256`) | 옮김 → Typography & Assets Assets; 분리 → provenance Identity | Dual (E2a). The same URL appears in both; the portable line records it as a third-party favicon proxy rather than a captured first-party mark. No Named-gaps row was invented for a first-party logo-file absence. |
| YAML `verified` `2026-06-03`, `omd: "0.1"` | 분리 → provenance Identity / Freshness | `omd: "0.1"` is ledger-only. The date is dual (E2a): the ledger + portable Scope and Capture record. |
| YAML `tokens.source: prose-derived`, `tokens.extracted: 2026-06-09`, `components_harvested: true` | 분리 → provenance Identity + Freshness + Proof notes; 사실만 옮김 → portable prose | Ledger fields kept as values (A1c). Grep-verified: `prose-derived` and `components_harvested` each reach 0 occurrences in the portable body, while the facts they state are carried as prose — Scope states the derivation and the six-day gap, and Capture record states the seven/nine component counts. |
| YAML `tokens.colors` (15 keys) | 옮김 → Foundations Semantic color | All fifteen hexes preserved at field granularity, each as its own named role. `primary-hover` `#2f2fae` keeps a separate bullet from `primary-pressed` `#1a1a86` rather than being merged into one press value (A4), and `heading` `#181818` keeps a separate bullet from `body` `#333333`. |
| YAML `tokens.typography.family` (`sans` and `mono`, both `Pretendard Variable`) | 옮김 → Typography & Assets Family | **Both keys preserved as keys** rather than collapsed because the values coincide. The portable Family list carries a Sans bullet and a Mono bullet and states that the token record assigns the same family to both, so the mono role is not silently resolved into a separate monospace face. |
| YAML `tokens.typography` 8 roles + §3 size list | 옮김 → Typography & Assets Type roles | Eight rows with size, weight, line height, and the YAML `use:` string as the Use column. Unitless line heights stay ratios (A1a): `1.20` appears 10× in the body — eight rows, the Distinctive traits line, and the sentence stating the ratio is kept as a ratio. Sizes are given in the token record's unitless form, and §3's px restatements (18px, 16px, 15px, 14px, 13px, 12px) and its two display ranges are stated beneath the table so both forms of every size survive. |
| §3 "Input text: 15px / 400 with `#181818`; search bar 28px / 700" | 옮김 → Typography & Assets Type roles (the two extra roles) | Neither role is in the eight-row token scale; both are preserved as roles recorded outside it. The search-bar role has no component record in the source, which is why Named gaps names that absence rather than inventing a component. |
| §3 "Line height: 1.2 default; labels and button text use explicit line-height matching the component height" | 옮김 → Typography & Assets Type roles | Both halves preserved, including the `1.2` default form beside the `1.20` token form. |
| YAML `tokens.spacing` (7 keys) | 옮김 → Foundations Spacing | Each key named with its value in the source's unitless form: `xs` 5, `sm` 9, `md` 14, `base` 15, `lg` 25, `xl` 50, `section` 60. No unit was added, because adding one would state a precision the source does not. §5's `15px` / `25–50px` rhythm sentence is carried beside it. |
| YAML `tokens.rounded` (4 keys) | 옮김 → Foundations Shape | Each key named: `sm` 5, `md` 8, `lg` 16, `full` 9999, again unitless as recorded, with the component radii listed separately in the units §4 uses. Dual (E2a): Foundations → Shape and the component records. |
| YAML `tokens.shadow` (floating, modal) | 옮김 → Foundations Elevation | Both longhand values preserved verbatim beside §6's shorthand forms, so neither notation is lost. |
| YAML `tokens.components` (7 records) | 옮김 → Components & States | Verified `type:` values preserved per component as an explicit `Type:` line — button ×3, input ×1, card ×2, tab ×1 — rather than flattened into `Kind: interactive` (A1b). Every bg / fg / radius / padding / font / use / active field lands on its component. The `use:` strings are decomposed onto the fields they describe: `button-primary`'s "36px height" becomes the Height field, `input-text`'s "focus `#181818`, error `#ff4d78`, valid `#3f3fff`" becomes the three named appearances, `tab-nav`'s "inactive on `#f5f5f5`" becomes the inactive background. |
| §1 header line — "FLO (플로) is South Korea's music streaming platform operated by Dreamus Company, offering 120 million tracks with deep personalization and AI-driven discovery." | 옮김 → Experience Scope ¶1 | Every fact preserved. The catalog-size, personalization, and discovery claims are recorded as claims the material carries rather than as findings, which is the product-claim separation this reference needs. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope ¶4, under an adjacent complete qualification | The whole reading — "clean, luminous, and quietly energetic", "punches through an overwhelmingly white canvas", "a single, unwavering brand hue", "almost entirely achromatic", "barely-there light grey", "keeps the music and cover art front and center", "a deliberate secondary signal language", "a clean audio studio: cool-toned, confident, and built for extended listening sessions without visual fatigue" — is kept as the source wrote it under the fixed qualification (B2, B2a). Its factual halves (the roles `#3f3fff` anchors, the achromatic values, `#ff4d78` reserved for error) also stand unqualified in Distinctive traits and Foundations. |
| §2 Color Palette & Roles (15 roles) | 옮김 → Foundations Semantic color | All fifteen roles with their names, values, and uses. The Body Text / heading naming disagreement is stated on the `#181818` bullet rather than resolved. |
| §4 component-scoped colours `#f4f5f8` and `#6d6d6d` | 옮김 → Components & States + a note in Foundations Semantic color | Neither is in the YAML colour block. They are kept on the components that carry them and named in Foundations as component-scoped rather than promoted to a general Surface or Text role (A4). |
| §3 Typography Rules — typeface stack | 옮김 → Typography & Assets Family + Font evidence | The full arrow-form stack and the §9 CSS declaration form both preserved. The portable body states that the chain after Pretendard is a fallback chain and must not be presented as the brand face. |
| §4 Component Stylings (9 components incl. variants) | 옮김 → Components & States | All nine declared: Primary Blue CTA with its Large variant, Small Pill, Medium Pill, Ghost Outline Chip, Text Input with its Password variant, Voucher Card, Active Voucher Card, Album Thumbnail, Main Navigation Tab. Every value lands: 36px / 0 15px / 5px / 14px-400, 62px / 0 92px / 18px-400, `#2f2fae`, `#1a1a86`, 28px / 0 12px / 15px / 12px-500, 32px / 0 17px / 22px / 14px-400, `0.5px solid #3f3fff` / 16px / 9px 15px, 58px / `1px solid #ebebeb` / 15px-400 / `#181818` / `#c4c4c4`, 6px / 175px, `#f4f5f8` / 8px / 50px 60px, `#525cfd` / 8px, `#6d6d6d` / `#f5f5f5` / `#3f3fff` / 2px. |
| §4 Text Input **Focus** / **Error** / **Valid / OK** / **Placeholder** | 옮김 → Components & States as named appearances, not as `focus-visible` treatment | B1. The source never writes `focus-visible` (`grep -c 'focus-visible' web/references/flo/DESIGN.md` = 0), so the `Focus` border `1px solid #181818` is kept as its own named appearance on the component and the `focus-visible` row carries no value. Error and Valid / OK feed the `error` and `success` applicability reasons because the source records them as appearances on the field itself. |
| §4 mid-file footer **Verified** / **Tier 1 sources** / **Tier 2 sources** / **Conflicts unresolved** | 분리 → provenance Freshness / Tier 1 / Tier 2 | Quoted verbatim in the ledger. The three source URLs and the `467 KB` figure stay there; only the 2026-06-03 date is dual (E2a). |
| §5 Layout Principles (6 bullets) | 옮김 → Layout & Platforms Recorded layout | All six verbatim in content: 1070px / 960px, 96px / 80px, the 15px and 25–50px rhythm, the 175×175px grid with 14px top padding and 15px bottom margin, the pinned player bar and full-screen overlay, and the ≤955px mobile collapse. |
| §6 Depth & Elevation (5 levels) | 옮김 → Foundations Elevation | All five rows — Level 0, Level 1, Level 2, overlay scrim, dark scrim — with their values and uses. The "album cover hover" use stays on Level 1 and is cross-referenced from the Album Thumbnail record instead of being converted into a state. |
| §7 Do's (6 items) | 옮김 → Experience Capture-bound application, under an adjacent complete qualification | All six kept as application rules. They are the source author's prescriptions read off the captured surfaces rather than published doctrine (B2, B2a). |
| §7 Don'ts (5 items) | 옮김 → Experience Avoid, under an adjacent complete qualification | All five boundary prohibitions with their rationales, same reason as the Do's. |
| §8 Responsive Behavior (4 bullets) | 옮김 → Layout & Platforms Recorded breakpoint bands | The three bands with every value, plus the fixed-px / `0.9375rem` = 15px rule. The read method was a source-file fetch with no rendering, so the band names, the collapsing behavior, and the grid reflow carry an adjacent complete qualification and the eleven numeric values are named as the recorded parts (B2a). |
| §9 Agent Prompt Guide (9 bullets) | 삭제, 두 값만 옮김 → Foundations Shape + Components & States (A3) | Tool-facing packaging addressed to a generating agent. Every value it restates lands elsewhere — grep-verified in the portable body: `#3f3fff`, `#ffffff`, 5px, 36px, 62px, the font stack, `#333333`, 14px / 400, `#181818`, 16 and 18 with weight 600, the four input rules, `#f4f5f8`, 8px, `0 4px 20px 0 rgba(0,0,0,0.10)`, and the error-only rule for `#ff4d78`. Two facts existed **only** in §9 and were moved rather than deleted: (a) `border-radius:20px` for pill shapes — `grep -c 'border-radius:20px' web/references/flo/DESIGN.md` = 1, at line 259 — now in Foundations → Shape and Named gaps as a preserved disagreement with §4's 15px / 22px / 16px; (b) the highlighted card `background:#3f3fff` with the same 8px radius — `grep -n 'background:#3f3fff' …` = 1, at line 264 — now on the Active Voucher Card record and in Named gaps as a preserved disagreement with `#525cfd`. Neither is resolved in favour of one side. |
| §10 Voice & Tone — adjectives + 4-row Do/Don't table | 옮김 → Content & Locales Voice, under an adjacent complete qualification | "Friendly, direct, unforced" and all four rows verbatim, including the Latin examples `"best ever"`, `"revolutionary"`, and `"music lovers"` (B2a, A5). |
| §10 voice sample — `가볍게, 나답게 FLO.` | 옮김 → Content & Locales Brand-published lines | Carried byte-exact as the brand's own line, on the source's own words ("the brand tagline"), **unqualified** — it is a published string, not a reading. Only the source's gloss beside it ("effortlessness and personal identity in six syllables") carries the qualification (B2a). |
| §10 voice samples ×3 marked *(Illustrative)* | 옮김 → Content & Locales Illustrative samples | Kept byte-exact with the marker travelling with each line and each gloss beside rather than replacing the Korean (A5). Not promoted to brand copy and not deleted. |
| §11 Brand Narrative | 옮김 → Experience Scope ¶5, under an adjacent complete qualification | The December 2018 launch, SK Telecom, the 2021 spin-out into Dreamus Company, RE;CORD / FLO Studio / FLO Shop with their descriptions, the 2026 date on FLO Shop, and the 120 million figure. The qualification states that the material attaches no source marker to the account and that the interpretive closings — the name-as-promise reading, the deliberate-counterpoint reading, "algorithmic empathy", "expresses the same philosophy in pixels" — are editorial (B2, B2a). Nothing from §11 was deleted. |
| §12 Principles (5 numbered items with UI implications) | 옮김 → Experience Principles, under an adjacent complete qualification | All five kept with their UI implications, using the approved wording. An added clause states they assert nothing about how FLO actually recommends, ranks, licenses, or sequences music. The `0.3s` / `0.25s` values inside item 5 are preserved and match §15. |
| §13 Personas (4, each marked *Illustrative*) | 삭제; 경계 문장만 옮김 → Experience Audience | The source itself marks all four illustrative. Portable Audience keeps that boundary; the four names, ages, cities, commutes, listening habits, and genre preferences are deleted and are not re-hosted in `provenance.md` (D2). Grep-verified in the portable body: `Minjeong` 0, `Junho` 0, `Hyunsook` 0, `Taeyun` 0, `Seoul` 0, `Busan` 0, `Incheon` 0, `Daejeon` 0, `dark mode` 0, `J-POP` 0, `K-pop` 0, `crate-digger` 0, `subway` 0, `daily mix` 0, `artist pages` 0, `SKT` 0. In `provenance.md` and in this file each of those strings occurs only inside the deletion record that names it. Product references that exist only inside the persona text go with it; the dark-surface fact survives independently through §7's Do. Primary tasks (`count=5`) come from §1, §3, §4, §5, and §14 surface evidence, not from §13. |
| §14 States (10 rows) | 옮김 → Components & States Source state contract + per-component applicability | 본문 보존: the full ten-row table survives verbatim, including `지금 음악 찾기`, `내 구독 보기`, `#ebebeb`, `#f5f5f5`, `#ff4d78`, `#3f3fff`, `175×175px`, 6px, 16px/600, 18px/600, 12px, `opacity: 0.2`, `rgba(59,59,59,0.3)`, and `hsla(0,0%,94%,0.3)` (A2, A5). The qualification carves out the values that restate the component records and notes that the two disabled rows are in CSS declaration form, so recorded values are not demoted (B2a). Rows feed applicability where they name a control: Disabled (button) → the `disabled` reason on the three blue-base buttons; Disabled (input) → the `disabled` reason on the text input; Error (network) → the `error` reasons on the CTA and the ghost chip; Error (input validation) → the input's `error` reason; Success (purchase complete) → the CTA and medium pill `success` reasons. `not captured` is never used as a `not-applicable` ground (C1); every `not-applicable` cell gives a role reason (C2); coverage is stated as not complete (C3); the two cards and the album thumbnail declare no map (C4). No graph delegation. |
| §15 Motion & Easing | 옮김 → Foundations Motion | The five duration steps with their uses, the two named easings with their declarations, and all four motion rules stay in the portable body. **Nothing was deleted:** the source has no `cubic-bezier` string at all (`grep -o 'cubic-bezier' web/references/flo/DESIGN.md | wc -l` = 0), so the unattributed-curve rule finds nothing to remove here; its easings are the CSS keywords `ease-in` and `linear`, each attached to a recorded declaration. The step names, the duration-to-use assignments, and the four rules carry an adjacent complete qualification, and the four CSS declarations quoted inside them are named as the recorded form (B2a). Reduced motion is absent from the source, so Foundations → Motion states the B3 promotion gate in full — transition properties, animation name, duration, easing, and reduced-motion behavior, observed per component, with the explicit clause that a single named curve or duration is not that gate. |
| Sibling `web/references/flo/.verification.md` (2,847 bytes) | 분리 → provenance Canonical proof; 값은 승격하지 않음 (E2) | **Adopted** as ledger evidence, not as portable authority. It supplies the read method, the byte sizes, eleven raw samples, the country determination with HQ, and the Korean regional-source statement. Thirteen of its values are sibling-only — the byte sizes, the SPA-shell characterization, five CSS selectors, `padding:10px 80px 0`, `box-shadow:-100vw 0 0 100vw`, the `.9375rem` radius form on `btn-round`, the jsDelivr Pretendard `<link>` and `v1.3.9`, the App Store "blue mascot color" line, and "HQ Seoul, Gangnam-gu". **None reaches the portable body**; each returns 0 from a literal grep of `DESIGN.md`, listed with its grep in `provenance.md` → Sibling-only values. Its App Store tagline line corroborates the source's own tagline rather than adding one, and the portable body keeps the source's trailing period. A live read of published files and the source `DESIGN.md` reconstruction are separate evidence domains, so adoption stops at the ledger. |
| Internal disagreements found during migration | 옮김 → Typography & Assets, Foundations Shape, Active Voucher Card, Governance Named gaps; 분리 → provenance Freshness | Five value-level disagreements the source footer's "Conflicts unresolved: none" does not record — the two display sizes, the Body Primary weight, the pill radius, and the highlighted card background. Both sides of each are preserved, neither is selected, and each is named in Named gaps. A sixth, milder one (the `#181818` role name) is stated in place on its Foundations bullet. |

## Named gaps check (D1a)

Every noun enumerated in Governance → Named gaps names a domain the source itself establishes, grep-verified against `web/references/flo/DESIGN.md`:

| Named gap | Source basis |
|---|---|
| reduced-motion behavior, and transition-property / animation-name evidence | §15 establishes a full duration scale, two named easings, and four transition declarations; it states no reduced-motion rule anywhere |
| the display type sizes | `tokens.typography.display-lg.size: 34` and `display-sm.size: 24` against §3 "Display Large: 30–38px" and "Display Small: 22–28px" |
| the Body Primary weight | `tokens.typography.body-primary.weight: 400` against §3 "Primary body: 15px / 400–500" |
| the pill radius | §4 records 15px, 22px, and 16px; §9 line 259 records `border-radius:20px` for pill shapes |
| the highlighted card background | §4 and the token record give `#525cfd`; §9 line 264 gives `background:#3f3fff` at the same 8px radius |
| a `focus-visible` appearance | §4 records a **Focus** border for the text input and §15 records "focus ring appear" at 150ms; the term `focus-visible` occurs 0 times |
| hover for controls other than the primary CTA and the tab | §4 records **Hover / Pressed** on the primary button and **Hover Tab** on the navigation tab, and §6 names "album cover hover"; no other control has one |
| the placement behind `#7286ff` | §2 "Blue Light: `#7286ff` — lighter tint used on selected items, soft accents" — role named, no component or element carries it |
| the component record behind the 28px / 700 search bar | §3 "Input text: 15px / 400 with `#181818`; search bar 28px / 700" — a role with no component record in §4 or the token block |
| the component record behind the checked control | §1 "checked inputs", §2 "checked-state labels" with `#3f3fff`, §15 "checked state icon swap" — the control is named three times and never given a record |
| values for the FLO mobile app, RE;CORD, FLO Studio, FLO Shop | §11 names all three products; the footer names the App Store KR listing as a source; no measured value is attached to any of them |

No domain the source leaves unmentioned is listed. In particular no authenticated-account, help-center, native-client, campaign, parity, minimum-width, touch-target, reading-order, or overflow domain is enumerated. Two of those were drafted and then removed for exactly this reason: an earlier draft listed "minimum supported width, 200% reflow behavior, touch-target sizing, reading order, and overflow policy" as a Named gap, and the source establishes none of them (the `200%` string is absent from the source, which is also why the gate flagged it as an invented token). It was cut rather than reworded.

## C2 — state applicability by role

Every `not-applicable` cell in the portable body gives a semantic role reason, and no cell cites absence of observation. Six controls declare a map; three components declare none — nine in all, matching the nine records. Measured: `grep -c '^| State | Applicability | Reason |' DESIGN.md` = 6 tables, `grep -c '^- Type:' DESIGN.md` = 9 components.

| Component | loading | error | success | Basis |
|---|---|---|---|---|
| Primary Blue CTA `btn_bg_blue_s` | applicable | applicable | applicable | Commits the single primary action on a screen; §14 records an inline failure label and a retry path, and a purchase-complete confirmation. The confirmation renders on a separate screen, so the `success` treatment is omitted while applicability stands |
| Small Pill `btn-round` | applicable | applicable | applicable | The source's own word for it is "action button"; an action started here runs, can fail, and can confirm on the control |
| Medium Pill `btn-buy` | applicable | applicable | applicable | A buy action runs to completion; §14's purchase-complete row is its recorded outcome, placed on a separate screen |
| Ghost Outline Chip | applicable | applicable | **not-applicable** | The rule name describes a search control. A search runs and can fail in place, but it resolves into results elsewhere on the screen, so the control carries no action-outcome confirmation |
| Text Input `comp_inp_txt` / `comp_inp_pw` | applicable | applicable | applicable | Validation resolves *on this field* — the source records both an Error border and a Valid / OK border on it — so the resolution it displays can also be pending on it, and both outcome states have recorded treatments |
| Main Navigation Tab `tab-nav` | **not-applicable** | **not-applicable** | **not-applicable** | Tab meaning is selected versus resting; it selects a section and runs no operation in place |
| Voucher Card / Active Voucher Card / Album Thumbnail | — | — | — | No map declared: two card surfaces and a grid cell with no interactive-kind evidence (C4) |

`default`, `hover`, `focus-visible`, and `disabled` are `applicable` on all six declared maps. Treatments are present only where the source records one — the CTA's `#2f2fae` hover, the tab's `#3f3fff` hover, the §14 disabled treatments for the blue-base buttons and the input — and omitted otherwise. No `focus-visible` row carries a colour value, and the source records no `focus-visible` observation.

The one deliberate C2 judgment worth naming for review: the Album Thumbnail is left without a `Kind` and without a map, even though §6 names an "album cover hover" as the Level 1 elevation trigger. That line is the only interactive-adjacent evidence and it is attached to the elevation level, not to the component; rather than decide the question from it, the record omits both fields and states where the hover line sits (C4, following the approved handling of grid items). It is recorded here so the judgment is visible rather than silent.

No playback-condition state was introduced. `playing`, `paused`, and `buffering` are absent from the source and absent from the portable body; the Capture record states in one sentence that the seven canonical states are the only ones any component declares.

## E1 process-leak check

`node test-v2/tools/process-leak-check.mjs` → flo absent from `detail`, 0 hits (confirmed by calling `findProcessLeaks` directly on the portable body, which returns `[]`). The body carries no clause id, wave number, ledger pointer, Tier grading, or sidecar reference; the only specification reference is `Core §4.4`, which is the approved idiom. Grep-verified at 0 hits in `DESIGN.md`: `Tier`, `provenance`, `prose-derived`, `components_harvested`. The only URLs in the body are FLO's own homepage, FLO's CDN domain, and the catalog favicon slug — all brand facts, not verification-record addresses; the three source URLs with their capture parentheticals live in `provenance.md`.

## Final passes

**Pass 1 (B2a).** The body was re-read from the top and every causal, interpretive, or evaluative sentence was classified as brand-published fact, observation, or editorial reading. **Nine items failed that classification on the first read and were corrected before submission:**

| # | Location | Was | Now |
|---|---|---|---|
| 1 | Distinctive traits | "One interactive hue: `#3f3fff` carries…", "A rose-pink `#ff4d78` reserved for… and used nowhere else", "An almost entirely achromatic surround", "Two shadow steps and two scrims **rather than an elevation ladder**" | Rewritten as records of what the material states — "`#3f3fff` is recorded against…", "`#ff4d78` is recorded only against…", "The surround is achromatic in the values themselves", "Depth is two shadow steps and two scrims". The evaluative halves stay where they are qualified, in Scope ¶4. |
| 2 | Scope ¶2 and Foundations → Evidence-domain boundary | "together with one line taken from FLO's Korean App Store listing"; "One line comes from it: the app tagline" | The tagline attribution came from the sibling, not from the source, which calls the line "the brand tagline" without attributing it. Both sentences now say only that the material names the listing among its sources and attaches no measured value to it. |
| 3 | Typography → Font evidence, "Loaded face" row | "The homepage markup loads Pretendard from the family's own public distribution rather than from a FLO-hosted path." | Row deleted. The `<link>` it paraphrased exists only in the sibling; the source records the stack and never records where the face loads from. The table now makes no loaded-face claim, and the ownership row states that no license text or ownership statement is recorded. |
| 4 | Typography → Font evidence, "Declared surface-use" row | The sibling's unquoted `font-family:Pretendard Variable,Pretendard,…` byte string | Replaced with the source's own quoted form from §9. |
| 5 | Components → Capture record | "the hover, pressed, focus, error, valid, and placeholder appearances **that the CSS itself declares by rule name**" | The source gives rule names for the base components, not for those appearances. Now: "Several carry the rule identifier the product itself uses. Alongside the resting appearances, the material records hover, pressed, focus, error, valid, and placeholder appearances against specific components." |
| 6 | Components → Capture record | "Playback conditions such as playing, paused, and buffering are not among the seven canonical states…" | Enumerating three state names the source never uses would introduce the vocabulary the sentence exists to exclude. Now: "The seven canonical states are the only ones any component declares here. The reviewed material records no additional playback-condition state for any control, and none is introduced." |
| 7 | Components → Source state contract | "Reading **the ten rows** as the system's state behavior is a derived editorial…" | Over-broad: two of the values restate the component records and the disabled rows are in CSS declaration form. Now scoped to "Beyond those restatements", so recorded values are not demoted by the qualification. |
| 8 | Typography → Assets | "Album artwork is first-party catalog content **and the dominant visual element on every screen**" | The second half is §12 Principle 3, which is qualified where it sits. Reduced to the factual half. |
| 9 | Content & Locales → Locale, and Illustrative samples | "…so a locale whose script needs different metrics is not covered by this contract"; "the illustrative line is not a published count and **there is nothing to reconcile between them**" | Both conclusions were mine rather than the material's. Replaced with the document facts: what the material establishes about locale, and that it marks the line illustrative. |

Two further wordings were tightened without changing evidence class: the primary CTA's `loading` reason now cites the material's own "standard blue call to action" and single-primary-action rule instead of asserting the role flatly, and the input's `success` reason now says "the reviewed material's own label" rather than "the source's own label". No token value, component-table value, or state applicability verdict was changed by any of the eleven; all are sentence-level.

After the fixes, twelve readings remain and each carries an adjacent complete qualification that closes the evidence class — `grep -o 'derived editorial' DESIGN.md | wc -l` = 12 and `grep -o 'not FLO-authored' DESIGN.md | wc -l` = 12, on the same 12 lines (15, 17, 48, 58, 69, 163, 230, 303, 327, 435, 453, 459). The 1:1 map of occurrence to portable location is the *Portable derived-editorial scope* table in `provenance.md`. The opposite error was checked too: recorded values were not demoted by attaching a qualification to them. Each qualification paragraph names its recorded parts separately — the CSS declarations under Motion, the rule name and six values under the Medium Pill, the eleven numeric values under Layout, and the tagline itself under Brand-published lines, which is carried unqualified because it is a published string. The colour roles, the type metrics, the component fields, the elevation levels, the spacing and radius scales, and all seven published Korean strings carry no qualification, because they are recorded values rather than readings.

**Pass 2 (E2 cross-check).** Every row above was written after grepping the three output files, not from memory. Dual and multi-destination values name every destination. Counts are `grep -o … | wc -l` (or `grep -oF` for strings with regex metacharacters such as `.` and `;`) with the counted unit stated — occurrences, not lines. Compliance claims are made only where the body carries the text: the B3 promotion gate is claimed because Foundations → Motion states all five evidence kinds plus the per-component gate and the "a single named curve or duration is not that gate" clause verbatim; the C3 statement is claimed because the Capture record says "State coverage is not complete here."

| Claim | Re-measured |
|---|---|
| 7 Korean runs in the source, 7 in the portable body | `grep -n '[가-힣]' web/references/flo/DESIGN.md` = 7 lines (60, 280–283, 321, 327); all seven strings present, occurrence counts as in the A5 table |
| `#3f3fff` occurrences with the destination list | 23 |
| `플로` / `지금 음악 찾기` / `내 구독 보기` | 2 / 2 / 2, at lines 9+478, 234+463, 240+464 |
| `1.20` in the body | 10 — eight type rows, Distinctive traits, and the ratio sentence |
| `Type:` lines / state tables in the body | 9 / 6 |
| `cubic-bezier` in the source / in the body | 0 / 0 — nothing to delete, nothing deleted |
| `focus-visible` in the source | 0; `focus-visible` table rows in the body carrying a hex: 0 |
| `verification_v2`, `ds.type`, HTML comments in the source | 0 each |
| `Conflicts` in the source | 1 — the footer's "Conflicts unresolved: none" |
| thirteen sibling-only values in the portable body | 0 each, each with its own literal grep in `provenance.md` |
| four persona names, four cities, and six persona-only phrases in the body | 0 each; in `provenance.md` and this file they occur only inside the deletion records |
| `border-radius:20px` / `background:#3f3fff` in the source | 1 each, both in §9 — the two A3 moves are real |
| D1 trigger phrases in the body (`not captured`, `were not`, `없었`, `않았다`, `미기록`) | 0 — no negative coverage sentence exists to introduce source-absent vocabulary |
| `not-applicable` cells citing non-observation | 0 — every one gives a role reason |
| coverage sentences in the body | 1, "State coverage is not complete here" — negated, as required |
| `Tier`, `provenance`, `prose-derived`, `components_harvested` in the body | 0 each |
| portable body line count (for the line references above and in `provenance.md`) | 528 |

## Gate

`node test-v2/tools/migrate-reference.mjs --brand flo --gate-only` → **PASS**, `problems: []`.

Portable Core: `inspectDesignMd(...).conformance` from `scripts/design-md-core.cjs` → `portable_core: true`, `reasons: []`, all seven claims present exactly once, `claim_locale: en`. Run separately from the gate, because the gate does not compare canonical claim bytes.

Three gate blocks were hit and fixed during the run, recorded here rather than silently corrected: `px:34px` and `px:24px` (the token record states those two sizes unitless, so writing them with a unit invented a token — the Type roles table now uses the unitless form and states §3's px restatements separately); `pct:200%` (from the drafted Named-gaps row described in the D1a section, which was cut, not reworded); and one `alien-negative-claim` where an editorial sentence used the phrase "not captured" (rewritten as a document fact). No gate finding was worked around by changing notation (E3).

**The gate is not evidence of conformance for any of this.** D1a, C2, B2a, and E2 are sentence-level judgments the gate does not evaluate; the gate returns the same PASS whether the qualifications are adjacent and complete or absent.
