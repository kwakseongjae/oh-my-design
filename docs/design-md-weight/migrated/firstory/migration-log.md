# Firstory migration log

Rulebook version used: **v10** (`docs/design-md-weight/MIGRATION_RULEBOOK.md`, 2026-08-26).

Source: `web/references/firstory/DESIGN.md`
Sibling evidence file: `web/references/firstory/.verification.md` — **adopted**, recorded in the ledger's own "Sibling verification file — adopted" section.
Destination: `docs/design-md-weight/migrated/firstory/DESIGN.md`
Ledger: `docs/design-md-weight/migrated/firstory/provenance.md`
Date: 2026-08-26

Every row below was checked with `grep -o … | wc -l` against the actual output files before it was written; counts are stated where a row claims a specific destination. `doc` = the migrated `DESIGN.md`, `led` = the ledger file.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo.type`/`slug`) | 분리 → 원장 | Portable file carries no frontmatter. `slug: Firstory` led=2, `type: github` led=2, `saas` led=1, doc=0 for all three. Name survives as H1 `# Firstory Design System`. |
| YAML `omd`, `added`, `verified`, `tokens.source`, `tokens.extracted`, `components_harvested` | 분리 → 원장 | Freshness and format metadata. `live-extract` led=2 doc=0; `2026-06-10` led=13 doc=0; `components_harvested` led=2 doc=0. |
| YAML `tokens.note` | 분리 → 원장 | Quoted verbatim in the ledger's Identity block. Its substance (pink CTA on warm off-white, Poppins over Open Sans, flat oklch/ring system) is restated in Foundations and Typography as observation. |
| YAML `tokens.colors` (13 keys) | 옮김 → doc §2 Semantic color | All thirteen hexes present in doc: `#fb355e` 12, `#fafafa` 6, `#0d131c` 10, `#242a34` 2, `#4f5661` 5, `#fbfaf9` 7, `#ffffff` 6, `#edf0f6` 5, `#ffe6e7` 10, `#962339` 6, `#f6efe5` 2, `#e0e3e8` 2, `#00c950` 4. **Dual destination:** all thirteen also appear in the ledger's raw-sample list. |
| YAML `tokens.typography.family` + 8 role tokens | 옮김 → doc §3 Family + Type roles | Poppins / Open Sans named; every YAML `use` string folded into the table's Notes column. Unitless line-heights preserved as ratios (A1a): `1.00` 4, `1.11` 1, `1.20` 1, `1.56` 1, `1.50` 2, `1.43` 2, `1.40` 2 — all doc, led=0. |
| §3 table rows absent from YAML (Step Title 20px/600/1.40, FAQ Item 16px/500/1.50, Eyebrow Label 14px/600/1.43) | 옮김 → doc §3 Type roles | Merged into the same table so the three §3-only roles are not lost. `1.25rem` doc=1, `1.00rem` doc=2, `0.88rem` doc=2. |
| §3 rem column (9 values) | 옮김 → doc §3 Type roles | `4.50rem` `3.00rem` `2.25rem` `1.88rem` `1.25rem` `1.13rem` `1.00rem` `0.88rem` `0.75rem` all doc≥1, led=0. |
| YAML `tokens.spacing` (7 keys) + §5 scale | 옮김 → doc §2 Spacing | Named scale carried key by key; §5's extra 20px recorded as "Also repeated in the inspected layout". |
| YAML `tokens.rounded` (4 keys) + §5 Border Radius Scale | 옮김 → doc §2 Shape | 10px / 12px / 16px / 9999px with their per-role assignments. `9999px` doc=4, led=1. |
| YAML `tokens.shadow.none` | 옮김 → doc §2 Elevation | Carried as "The shadow token is literally `none`" plus the four-level table. |
| YAML `tokens.components` (10 entries, incl. `type:`) | 옮김 → doc §4 | Verified primitive types preserved per component (A1b): `Type: button` doc=4, `Type: tab` doc=1, `Type: card` doc=3, `Type: badge` doc=3. **Dual destination:** the same measurements appear in the ledger's raw-sample list (`0px 32px` doc=1/led=1, `8px 20px` doc=1/led=2, `2px 8px` doc=1/led=1, `4px 16px` doc=1/led=1). |
| §1 Visual Theme & Atmosphere — measured half | 옮김 → doc §1 Distinctive traits, §2, §3 | Families, weights, hexes, radii, tracking. |
| §1 Visual Theme & Atmosphere — atmosphere reading | 옮김 → doc §1 Scope (qualified) | Carried under an adjacent derived-editorial qualification (B2/B2a), together with §11's "what Firstory refuses" paragraph. |
| §1 Key Characteristics (9 bullets) + bilingual parity | 옮김 → doc §1 Distinctive traits | Ten bullets; character adjectives qualified in the sentence that follows the list. |
| §2 Color Palette & Roles | 옮김 → doc §2 Semantic color | Role names, hexes, and placements. Character readings ("reads creative and energetic", "accessible companion") qualified adjacently; no contrast ratio is asserted. |
| §3 Font Family + Hierarchy + Principles | 옮김 → doc §3 | Metrics as fact; "Poppins persuades, Open Sans informs", "solid 1.0 line-height", "prices are display type" moved into one adjacent qualified paragraph. |
| §3 `Poppins Fallback` / `Open Sans Fallback` | 옮김 → doc §3 Font evidence (Declared-only row) | doc=1 each, led=0. Recorded as fallback names, not the brand face. |
| §4 Component Stylings (Buttons, Tabs, Cards, Badges, Navigation, FAQ Accordion, Footer) | 옮김 → doc §4 | Fourteen entries (six declared interactive controls, five presentational card/badge entries, the announcement pill, and the two regions). §4-only values that have no YAML token are carried: FAQ trigger `20px 24px` / `64px` (doc=1 / doc=5), Pricing Plan Card `24px 0px` (doc=2), footer heading and link colors, nav composition string (doc=1). |
| §4 verbatim UI labels | 옮김 → doc §4 + §6 | Byte-preserved (A5): "Get started today" doc=5, "Get Started for Free" doc=1, 「立即開始」, "14-Day Free Trial" doc=2, 「免費試用 14 天」, "Start Free" doc=1, 「免費開始」, "Save 22%" doc=1, "Recommend" doc=5, "First Wave in Asia-Pacific! …" doc=1, "How do I get started with Firstory?" doc=2, "Privacy Policy" doc=1, "Features"/"Pricing"/"Blog", "Product"/"Resources"/"Legal", "AI Studio"/"Data Analytics", "Distribution", "Monthly"/"Yearly". **Dual destination** for every label the adopted sibling file also samples — eighteen of those listed, not three: "Get started today" led=1, 「立即開始」 led=1, "14-Day Free Trial" led=1, 「免費試用 14 天」 led=1, "Start Free" led=1, 「免費開始」 led=1, "Save 22%" led=1, "Recommend" led=1, "First Wave in Asia-Pacific! …" led=1, "How do I get started with Firstory?" led=1, "Privacy Policy" led=1, the nav triple `"Features"/"Pricing"/"Blog"` led=1, "Product" led=1, `"AI Studio` led=1, "Monthly" led=1, "Yearly" led=1, "Simple, Transparent Pricing" led=1. doc-only among the listed labels: "Get Started for Free", "Resources", "Legal", "Data Analytics", "Distribution" (led=0 each). |
| §4 footer **Verified** line, source-tier lines, **Conflicts unresolved** | 분리 → 원장 | Freshness and source ledger; `2026-06-10` led=13, doc=0. Conflict matrix reproduced in the ledger. |
| §5 Layout Principles (grid, container, whitespace) | 옮김 → doc §5 | Composition bullets carried; `193px` doc=2/led=1. Whitespace philosophy carried inside the section's qualified paragraph. |
| §6 Depth & Elevation table + Shadow Philosophy | 옮김 → doc §2 Elevation | Four levels verbatim; the transparent-ring-placeholder observation kept as fact, the "reaches for the pink, never for depth" reading qualified adjacently. |
| §7 Do's (8) | 옮김 → doc §1 Principles (application rules) | Kept as brand application rules, not folded into Governance controlled copy. |
| §7 Don'ts (8) | 옮김 → doc §1 Avoid | All eight carried. |
| §8 Responsive Behavior (breakpoints, touch targets, collapsing, image behavior) | 옮김 → doc §5 + §3 Assets | Breakpoint table (`640px`, `640-1024px`, `1024-1440px` — doc=1 each) and collapsing behavior carried under a qualification that they are the source's reading rather than per-width measurement; image behavior in §3 Assets. |
| §9 Quick Color Reference / Example Component Prompts / Iteration Guide | 삭제 (고유값은 이동) | Tool-facing prompt packaging deleted. Its §9-only values were moved first (A3): alias names Near-White / Warm Canvas / Pure White / Deep Berry into §2 (doc=1 each, led=0); solution-card body copy `#4f5661` at `16px / 400 / Open Sans` into the Solution Card entry (doc=2); right-aligned pink nav CTA into the Navigation bar entry; announcement pill above the hero headline into §5. Nothing else in §9 was unique. |
| §10 Voice & Tone — verbatim samples | 옮김 → doc §6 | Byte-preserved with English beside, never replacing (A5): 「用 Podcast 開啟你的媒體事業」 doc=2/led=1, 「準備好開始你的 Podcast 之旅了嗎？」, 「從核心功能到強大的進階特色」, 「受到全球創作者的信賴」, 「在任何地方嵌入播放器」, 「3 步輕鬆開始 — 註冊帳號, 上傳你的 Podcast, 發佈與成長」, 「透過訂閱打造穩定收入」, 「Firstory 要多少錢？」, 「AI 工作室」, 「會員訂閱」, 「節目推廣」, 「廣告主專區」 doc=3, and the zh page title. **Dual destination** for the eight the sibling file also samples, each led=1: 「用 Podcast 開啟你的媒體事業」, 「準備好開始你的 Podcast 之旅了嗎？」, 「從核心功能到強大的進階特色」, 「受到全球創作者的信賴」, 「立即開始」, 「免費開始」, 「免費試用 14 天」, and the zh page title 「Firstory：Podcast 訂閱與創作者變現的 AI 工具」. doc-only (led=0): 「在任何地方嵌入播放器」, 「3 步輕鬆開始 — 註冊帳號, 上傳你的 Podcast, 發佈與成長」, 「透過訂閱打造穩定收入」, 「Firstory 要多少錢？」, 「AI 工作室」, 「會員訂閱」, 「節目推廣」, 「廣告主專區」. |
| §10 Context/Tone table + voice characterization + forbidden register | 옮김 → doc §6 | Table carried with its verbatim examples. The Tone column's own characterizations, the voice characterization, and the forbidden-register list all sit under the one adjacent derived-editorial qualification that follows the table. |
| §11 Brand Narrative — founding facts | 옮김 → doc §1 Scope | 2018 Taipei, 于子軒 / 翁子皓 / 劉德政 (doc=1 each), "Firstory Studio" (doc=1), 2019 pivot, October 2020 KKBOX seed (doc=1/led=1). Attributed to Taiwanese technology press rather than hedged, since they are documentary facts. |
| §11 product-stack paragraph | 옮김 → doc §1 Scope | Distribution, analytics, cross-promotion, Flink smart-link (doc=1), donations, member subscriptions, advertiser marketplace, AI tooling. |
| §11 "what Firstory refuses" paragraph | 옮김 → doc §1 Scope (qualified) | Merged into the qualified atmosphere paragraph. |
| §11 INSIDE / Meet / Business Next article titles | 분리 → 원장 | Discovery-only narrative sources: 「從聲音交友 180 度變身大平台的創業物語」 led=1 doc=0, 「一畢業就創辦 Firstory」 led=1 doc=0. Byte-preserved in the ledger (A5) and explicitly not cited for design claims. |
| §12 Principles (5, with UI implications) | 옮김 → doc §1 Principles | All five with their UI implications, under the fixed B2a qualification form. |
| §13 Personas — three fictional entries (names, ages, cities, biographies) | 삭제 | D2: fictional archetypes are neither promoted nor re-recorded as evidence. Demographics dropped from the ledger too; only a disposition row remains there. The source's own group-level segments survive in doc §1 Audience. |
| §14 States (9 rows) | 옮김 → doc §4 Derived state stances | Body preserved rather than delegated. All nine rows carried with their values (`#edf0f6` skeleton blocks, "flat pulse" doc=1, "Reduced-opacity pink" doc=1, 「上傳你的 Podcast」 doc=3), under one derived-editorial qualification, and deliberately **not** attached to any component's applicability map. No playback state (playing / paused / buffering) was created: the source records "Loading (audio processing)" as an episode-row progress stance and nothing more. |
| §15 Motion durations (`motion-fast` 120ms, `motion-standard` 200ms, `motion-slow` 300ms) | 옮김 → doc §2 Motion (qualified) | Token-name counts in doc: `motion-fast` 1, `motion-standard` 2 (the duration table row plus the billing-toggle motion rule that cites it), `motion-slow` 1; led=0 for all three. Carried at the source's own stated derived authority and no higher. |
| §15 Easing curves (3) | 삭제 → 원장 omission ledger | Unattributed; `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, `cubic-bezier(0.25, 0.1, 0.25, 1)` each led=1 doc=0. The second is the worked example in the legacy authoring format, so it is boilerplate rather than an observation of this brand. |
| §15 Motion rules + reduced-motion behavior | 옮김 → doc §2 Motion (qualified) | Rules carried without the curve references; `prefers-reduced-motion` doc=1. The B3 promotion gate is written out in full in the body — all five evidence kinds (transition properties, animation name, duration, easing, reduced-motion behavior) plus the per-component computed-observation condition and the "a single documented curve is not that gate" clause. Verified present before this row was written. |
| Trailing HTML source comment (sections 10–15 evidence note) | 분리 → 원장 | Source and authority record. Its four substantive limits — voice samples verbatim, personas fictional, §14/§15 illustrative, interpretive claims editorial — are re-expressed in the body as adjacent qualifications rather than as a comment. |

## Verification

- `node test-v2/tools/migrate-reference.mjs --brand firstory --gate-only` → `PASS`, `problems: []`.
- `evaluatePortableCore` (`scripts/design-md-core.cjs`) → `level: portable-core`, `portable_core: true`, all thirteen checks pass, `reasons: []`, seven claim declarations ×1 each, `claim_locale: en`.
- `node test-v2/tools/process-leak-check.mjs` → firstory absent from the leaking list (0 hits).

## Final passes

- **Pass 1 (B2a).** Whole body re-read sentence by sentence. Six places were carrying derived interpretation without an adjacent qualification and were fixed: the Distinctive-traits character adjectives, the Semantic-color character readings (including the unmeasured `#962339`/`#ffe6e7` contrast pairing), the typographic "Poppins persuades" reading, the Layout breakpoint/whitespace reading, the role readings that close `loading`/`error`/`success`, and the Content voice characterization. Three assertions were also narrowed to what the source actually establishes: the Primary CTA's role no longer claims a sign-up destination, the scope no longer says the creator dashboard is a navigation name, and the font-evidence table no longer raises a Firstory-exclusive type family as a question. Documentary facts — the founding, the pivot, the KKBOX round, every measured value — were left unqualified rather than downgraded.
- **Pass 2 (E2).** Every row above was grep-checked against the output files, with counts taken by `grep -o … | wc -l` and the counted string named in the row. Dual destinations are recorded on both sides for the color tokens, the component measurements, the sampled UI labels, and the voice samples the sibling evidence file also carries. The one compliance claim made here (B3 kept in full) was checked against the body text before this log asserted it. **Corrected by the F3 audit** (see `audit-log.md`): as originally written this paragraph was stronger than the rows underneath it. Three rows had not actually been measured as stated — `motion-standard` was recorded doc=1 when the body carries it twice, the §4 label row named three dual destinations when eighteen of its listed labels are in the ledger, and the §10 voice row said "the six" when the sibling file samples eight. Those three rows now carry the measured values.

## F3 audit re-check (E2 재대조 after the audit's own body edits)

A separate audit session edited five places in the portable body (B2/B2a). Every destination claim above was re-measured afterwards; none moved.

- Thirteen color tokens: `#fb355e` 12, `#fafafa` 6, `#0d131c` 10, `#242a34` 2, `#4f5661` 5, `#fbfaf9` 7, `#ffffff` 6, `#edf0f6` 5, `#ffe6e7` 10, `#962339` 6, `#f6efe5` 2, `#e0e3e8` 2, `#00c950` 4 — unchanged from the rows above.
- Primitive types: `Type: button` doc=4, `Type: tab` doc=1, `Type: card` doc=3, `Type: badge` doc=3 — unchanged.
- Geometry and §4-only values: `9999px` doc=4, `193px` doc=2, `64px` doc=5, `24px 0px` doc=2, `20px 24px` doc=1, `16px / 400 / Open Sans` doc=2 — unchanged.
- Motion: `motion-fast` doc=1, `motion-standard` doc=2, `motion-slow` doc=1, `prefers-reduced-motion` doc=1; the three easing curves stay doc=0 / led=1.
- §9-only aliases: Near-White / Warm Canvas / Pure White / Deep Berry doc=1 each, led=0 — unchanged.
- §14 stance values: "flat pulse" doc=1, "Reduced-opacity pink" doc=1, 「上傳你的 Podcast」 doc=3 — unchanged, and still outside every component applicability map.
- Traditional-Chinese needles: 「你」 doc=7 (source=7); 「您」 appears once and only in the §6 sentence that records its absence from the sampled strings (source=0), so the register contrast survives rather than being normalised away.
- Derived-editorial qualification blocks: 13 in the body, each pairing `derived editorial implementation inference` with `not Firstory-authored or a separately published UI specification` (13/13). The ledger's Proof notes now enumerate all thirteen.
- One row edited by the audit that touches a destination claim: the §10 Context/Tone row, because the qualification after that table now names the Tone column explicitly.

Re-verified after the audit edits: `migrate-reference.mjs --brand firstory --gate-only` → `PASS`, `problems: []`; `evaluatePortableCore` → `level: portable-core`, `portable_core: true`, `reasons: []`, `claim_locale: en`; `process-leak-check.mjs` → firstory absent (0 hits).

