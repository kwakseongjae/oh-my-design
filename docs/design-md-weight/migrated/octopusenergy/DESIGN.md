# Octopus Energy Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Octopus Energy is the UK's most awarded energy supplier. This contract covers the two first-party marketing surfaces the source inspected for tokens on 2026-06-23: the homepage at `https://octopus.energy/` and the tariffs page at `https://octopus.energy/tariffs/`. Every value stays attached to the surface or control that established it. Reading those two inspected pages as this contract's token surfaces, and keeping every value attached to the surface or control that established it, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

The captured marketing layer is a deep cosmic indigo (`#100030`) canvas saturated with electric magenta (`#f050f8`) and cyan (`#60f0f8`). Headlines sit at 36px weight 500 on a 1.5 line-height, colored in the signature cyan (`#60f0f8`). Body copy uses the near-white `#f0ffff` (azure-tinted white) rather than pure white. The custom typeface `Chromatophore` carries all text at a single weight family (400 for body, 500 for headings and buttons), with the homepage hero postcode field as the recorded exception (white `#ffffff` background, `#000000` text, 18px Arial weight 600). There are effectively no drop shadows — live inspection confirmed `box-shadow: none` across buttons, cards, nav, and hero. Depth is handled by background-color contrast between `#100030` and `#180048`. The hex values, sizes, weights, family name, Arial exception, and `box-shadow: none` in this paragraph are recorded. Readings of that layer as a rave-poster-turned-utility-brand, as a screen that glows at you from the dark like a neon sign in a midnight sky, as fully intentional, as positioning clean energy as something exciting, modern, and even joyful rather than the dull obligation of switching suppliers, as a humanist sans-serif with just enough warmth to counterbalance the electric palette, as never feeling cold or corporate, as a cool counterpoint to the hot magenta CTA, as a disciplined three-color hierarchy (deep indigo for canvas, magenta for action, cyan for headlines), as dark-mode-native before dark mode was fashionable, and as a flat, glow-based system where brightness and color temperature create depth, are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. Octopus Energy was founded in **2015** by **Greg Jackson (CEO)** as a vertically-integrated energy retailer and technology company, built on the conviction that the UK energy market was broken — overpriced, opaque, and hostile to innovation. The company's proprietary Kraken platform (a cloud-based energy management system) powers Octopus's ability to offer smart tariffs, agile pricing, and direct-to-bill renewable energy at a scale that incumbents cannot match. The brand's defining move was refusing the norms of utility-brand communication: instead of institutional imagery and corporate language, Octopus built a brand around an octopus mascot called **Constantine**, a playful illustration character whose many arms hold products (energy, heat pumps, solar, EV charging) like a friendly multi-tasker. The deep indigo and magenta palette — far from any competitor's safe blues and greens — signals that this is a technology company wearing a utility's licence, not the other way around. By 2023-2024, Octopus Energy had become the UK's largest domestic energy supplier by customer numbers, with the Which? Recommended Provider status held for nine consecutive years. The Kraken platform has been licensed internationally (to E.ON, EDF, Origin Energy in Australia, and others), making the company's technology as significant a revenue stream as its retail energy business. What Octopus refuses: greenwashing without substance, the incumbent utility's "cheapest deal" race to the bottom, and the assumption that sustainability has to be dull. What it embraces: radical transparency on pricing, a brand that feels like a tech company, and the position that clean energy is a source of joy and pride for households — not just an obligation. The source's own evidence note classifies the 2015 founding, CEO Greg Jackson, the Kraken platform, Which? Recommended Provider for nine consecutive years, and UK's largest domestic energy supplier as widely documented public facts. The year, founder, vertically-integrated framing, broken-market conviction, Kraken capabilities, Constantine and the many-arms product list, the 2023-2024 customer-number claim, the nine-year Which? status, the E.ON / EDF / Origin Energy licences, the technology-as-revenue-stream sentence, and the refuse/embrace pairing through that closing obligation sentence are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-Kraken narrative as context that does not by itself supply interface tokens, and treating "a technology company wearing a utility's licence" as the source's own editorial reading connecting observed design to positioning, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=4 lang=en -->
### Primary tasks

Selecting these four as the product's primary tasks is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification. Each names a surface or control the source records. They do not come from the source's persona section.

- Get a quote from the homepage hero postcode attached-CTA (`#hero-quote-form-field-postcode` paired with inline `Get a quote`).
- Get a quote on `https://octopus.energy/tariffs/`.
- Scan product category cards (Energy / Heat pumps / Solar & batteries / Electric vehicles).
- Open `Log in` from the sticky nav.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source labels its named figures as fictional archetypes informed by publicly observable Octopus Energy user segments (UK households switching energy suppliers, early-adopter EV and solar homeowners, sustainability-conscious renters), not individual people, so those biographies are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is those publicly observable segments: UK households switching energy suppliers, early-adopter EV and solar homeowners, sustainability-conscious renters. Reading those source-named groups as this product's audience, and dropping the source's archetype biographies rather than promoting them, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

- Chromatophore custom font at weight 500 for all interactive elements — tactile, brand-exclusive
- Deep indigo (`#100030`) as the page canvas — dark-mode-native before dark mode was fashionable
- Signature magenta (`#f050f8`) reserved strictly for primary CTAs and borders
- Cyan (`#60f0f8`) for all major headings — cool complement to the hot CTA
- Azure-tinted near-white (`#f0ffff`) for body text — gentler than pure white on dark backgrounds
- Zero shadows — depth via color temperature and background tint variation (`#180048`)
- 12px border-radius on all interactive elements — rounded but not pill
- Cookie-banner button system doubles as the primary component pattern

### Principles

These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification. The source states them in its own Principles section. Every *UI implication* below is that same derived class, including "the brand earns affection through character, not just price."

1. **Technology over incumbency.** Octopus's Kraken platform is the product; the energy licence is the delivery mechanism. *UI implication:* the interface feels like a tech product — custom font, dark canvas, electric palette — not a utility bill.
2. **Warmth in the unexpected place.** A grid-connected household energy company built a beloved mascot. *UI implication:* Constantine appears in hero illustrations; the brand earns affection through character, not just price.
3. **Transparency as differentiation.** In a market famous for opaque pricing, Octopus publishes its tariff rates prominently and compares them to the price cap on its own homepage. *UI implication:* pricing copy is upfront, direct, and data-backed.
4. **Clean energy as cause and commerce.** The brand never separates "cheap" from "green" — they are the same offer. *UI implication:* sustainability language is woven into product descriptions, not siloed in a CSR section.
5. **One action, one color.** Magenta (`#f050f8`) means "do this now." *UI implication:* the CTA button stands as the only saturated warm hue on the deep indigo canvas — it cannot be missed.

### Application rules

The source states these eight as its Do list, kept as written. The four geometry exceptions after the list are the source's own iteration constraints. These rules, the reasons attached to them, and the decision to keep the iteration constraints beside the Do list, are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification.

- Use Chromatophore at weight 500 for all headings and buttons
- Use magenta (`#f050f8`) exclusively for primary CTAs and their borders
- Use cyan (`#60f0f8`) for H1 and H2 headings on the dark canvas
- Keep the page canvas dark (`#100030`) — the brand's identity lives in the contrast
- Use `#f0ffff` azure-white for body text rather than pure `#ffffff`
- Apply 12px radius to all interactive elements — consistent and rounded
- Use `#180048` for card and elevated surfaces instead of shadows
- Keep typography at weight 500 for interactive elements, 400 for body

Source iteration constraints that sit only in that guide, kept as written:

- Canvas is always dark (`#100030`) — the hero postcode input is an exception (white `#ffffff` bg, `#000000` text, Arial 600) for readability inside the attached-CTA component
- Standalone buttons use 12px full radius; attached-CTA pairs split the radius (input: left-round 12px 0 0 12px, button: right-round 0 12px 12px 0); full-width tariffs input uses 0px radius
- Body text is `#f0ffff` (not `#ffffff`) — the azure tint is intentional; `#ffffff` only appears as the hero postcode input background
- Font is Chromatophore weight 500 for headings/buttons, weight 400 for body; the hero postcode input uses Arial weight 600 (system font, not Chromatophore)

### Avoid

The source states these eight as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification.

- Use drop shadows — Octopus is a flat, glow-based system
- Use magenta for decorative or text elements — it signals action only
- Use a light canvas (`#ffffff`) for any page section — the brand lives in darkness
- Use pure white text (`#ffffff`) — the brand uses azure-tinted `#f0ffff`
- Use font weight 700 or bold — Chromatophore 500 is the maximum
- Use rounded pills on buttons — 12px is the brand's radius cap (pills are for badges only)
- Mix cyan into interactive elements — cyan is for headings, magenta is for action
- Use a secondary typeface — Chromatophore is the sole font across all surfaces

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Token-set keys follow the YAML paths. Pairing each hex to the token-set path named beside it, keeping `tokens.colors.primary` unmerged from `tokens.colors.primary-border` even though both are `#f050f8`, keeping `tokens.colors.canvas` unmerged from `tokens.colors.on-primary` even though both are `#100030`, keeping the sticky-nav `rgba(16, 0, 48, 0.9)` on the Canvas Alt role as the source wrote it rather than rewriting it as a second canvas hex, and treating characterizations such as deep-water bioluminescent signature, night-sky base, never decorative, and azure-white rather than pure white as the source's own role prose around live hexes, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

#### Primary Action

- **Octopus Magenta** (`#f050f8`): The single primary action color — CTA button backgrounds, borders on outlined buttons. The brand's most saturated and eye-catching hue, inspired by the deep-water bioluminescent signature. Used only for action; never decorative. Token-set path `tokens.colors.primary`. `tokens.colors.primary-border` is the same hex on a second key.
- **Deep Indigo** (`#100030`): Page canvas and primary background. The "night sky" base that makes the magenta and cyan pop. Also used as foreground text color on magenta buttons to create readable contrast. Token-set path `tokens.colors.canvas`. `tokens.colors.on-primary` is the same hex on a second key (text on magenta).

#### Surface

- **Canvas Alt** (`#180048`): Slightly lighter indigo for elevated card surfaces, sticky nav background (`rgba(16, 0, 48, 0.9)`), and product feature cards. Creates hierarchy without shadow. Token-set path `tokens.colors.canvas-alt`. The sticky-nav `rgba(16, 0, 48, 0.9)` stays the source's own writing on this role; it is not rewritten as a second canvas hex.
- **Brand Accent Blue** (`#5840ff`): A vivid blue-violet, seen in secondary branded areas — appears on feature illustration backgrounds and accent surfaces. Token-set path `tokens.colors.brand-accent`.

#### Text & Heading

- **Cyan** (`#60f0f8`): H1 and H2 heading text color across the site. An electric aqua-teal that reads as energetic and modern against the dark indigo canvas. Token-set path `tokens.colors.cyan`.
- **Foreground** (`#f0ffff`): Primary body text, nav links, and secondary elements. Azure-white rather than pure white — it has warmth. Token-set path `tokens.colors.foreground`.
- **Muted** (`#a49fc8`): Tertiary text and fine-print items on the dark canvas. Token-set path `tokens.colors.muted`.
- **On-primary** (`#100030`): Text color used on magenta buttons — the deep indigo as foreground ensures readable contrast. Token-set path `tokens.colors.on-primary`. Same hex as Canvas; the keys stay unmerged.

### Spacing

Token-set path `tokens.spacing`, unitless steps as the YAML recorded them.

| Step | Value | Token-set path |
|---|---:|---|
| xs | 4 | `tokens.spacing.xs` |
| sm | 8 | `tokens.spacing.sm` |
| md | 12 | `tokens.spacing.md` |
| base | 16 | `tokens.spacing.base` |
| lg | 20 | `tokens.spacing.lg` |
| xl | 24 | `tokens.spacing.xl` |
| xxl | 48 | `tokens.spacing.xxl` |
| section | 64 | `tokens.spacing.section` |

The source also writes a base unit of `16px` and a scale observed as 4px, 8px, 12px, 16px, 20px, 24px, 48px, 64px. Postcode input inner padding: 16px; button padding: 12–24px. `tokens.spacing.md: 12` is not `tokens.rounded.lg: 12` and is not the 12px half of button padding `12px 16px`. `tokens.spacing.base: 16` is not the postcode field's `16px` padding and is not a type size. `tokens.spacing.lg: 20` is not the 20px half of `16px 20px`. `tokens.spacing.xl: 24` is not the 24px half of `12–24px`. `tokens.spacing.xxl: 48` is not a type line-height. `tokens.spacing.section: 64` is not a layout max-width. Keeping each number on its own key path, rather than treating a shared numeral as the same token, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded`, unitless steps as the YAML recorded them.

| Step | Value | Token-set path |
|---|---:|---|
| sm | 4 | `tokens.rounded.sm` |
| md | 8 | `tokens.rounded.md` |
| lg | 12 | `tokens.rounded.lg` |
| full | 9999 | `tokens.rounded.full` |

The source's prose radius scale is Standard (`12px`) for all buttons, inputs, and nav items — consistent and rounded; Card (`12px`) for product feature cards matching the button system; Full pill (`9999px`) for trust indicator badges only. `tokens.rounded.lg: 12` is not `tokens.spacing.md: 12`. `tokens.rounded.full: 9999` is a YAML step for those pills; it is not a button radius. `tokens.rounded.sm: 4` and `tokens.rounded.md: 8` are YAML steps; the source's prose radius scale does not name a use for them. Attached-CTA split radii (`12px 0px 0px 12px` / `0px 12px 12px 0px`) and the tariffs input `0px` live on those components; they are not YAML rounded steps. Keeping those local radii on their components, keeping `full: 9999` on its own key path, and leaving `sm: 4` / `md: 8` without an invented use, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | `#100030` — base canvas | Page background |
| Raised (Level 1) | `#180048` tint | Product cards, nav background |
| Translucent (Sticky) | `rgba(16, 0, 48, 0.9)` + implied blur | Sticky top nav on scroll |

**Shadow Philosophy**, as the source states it: Octopus Energy is a shadow-free system. Live inspection confirmed `box-shadow: none` across all major elements — buttons, cards, nav, and hero. Depth is handled entirely by background-color contrast between the two indigo tints (`#100030` vs `#180048`), creating a low-key depth that preserves the neon-on-dark aesthetic. Shadows would add a material realism that conflicts with the brand's otherworldly, bioluminescent feel. Token-set path `tokens.shadow.none: "none"`. Reading that stack as tint-not-shadow elevation, and treating "implied blur" and "otherworldly, bioluminescent feel" as the source's own elevation prose, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

### Motion

The source attributes its token-level claims to a live inspection of computed color, type, radius, and shadow on `https://octopus.energy/` and `https://octopus.energy/tariffs/`. The motion contract below sits outside that attribution: the source's own evidence note assigns a source to its voice samples, its brand narrative, and its personas, and assigns none to the easing curves. The durations, easing roles, and motion rules below are therefore a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification.

Durations:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 100ms | Hover states, button press, focus ring |
| `motion-standard` | 200ms | Card transitions, nav dropdown, input focus |
| `motion-slow` | 300ms | Page-level reveals, hero illustration entrance |

Easing roles — three roles with declared uses. The specific curve values the source lists (`cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, `cubic-bezier(0.25, 0.1, 0.25, 1)`) are not traceable to Octopus Energy-computed samples, so the curves are omitted here and only the roles and their uses are kept:

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, nav panels |
| `ease-exit` | Dismissals, nav close |
| `ease-standard` | Two-way transitions |

Motion rules, as the source states them: Motion supports the brand's energy and warmth without becoming frivolous. The Constantine mascot illustration may entrance with a gentle float-in on hero load. Button hover shows a subtle brightness shift on the magenta (`#f050f8` → slightly lighter); outlined buttons fill on hover. No spring or bounce — the brand is playful but its energy infrastructure must feel dependable. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; Constantine does not animate on load.

The three exact cubic-bezier curves carry no attribution in this record and stay omitted rather than promoted. `ease-exit`'s omitted value matches the legacy authoring template. Do not promote an easing curve, an animation name, a CSS transition property, or a duration beyond the table above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists. The five-kind promotion gate in this paragraph is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, and the resolution in each cell, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | The source's token note records that the brand owns Chromatophore, a custom typeface named after octopus chromatophores (pigment cells). This capture does not include a separately issued type specimen. |
| Live computed surface-use | Both inspected surfaces compute visible text as `Chromatophore, helvetica, arial, sans-serif`. The homepage hero postcode field computes as Arial weight 600. |
| Official distributed asset | The record establishes the family in use and carries no Octopus Energy-distributed font file URL. |
| Declared-only | No monospace/code variant observed on marketing surfaces. |
| License | No license or distribution statement accompanies Chromatophore in this record. |
| Outside these captures | Surfaces other than the homepage and the tariffs page contributed no type value here. |

### Family

- **Primary:** `Chromatophore` — Octopus Energy's custom typeface, named after octopus chromatophores (pigment cells). Fallback: `helvetica, arial, sans-serif`. Token-set path `tokens.typography.family.sans`: `Chromatophore, helvetica, arial, sans-serif`.
- The homepage hero postcode field uses Arial weight 600 (system font, not Chromatophore), as the source records on that control.
- Do not present a fallback member of the stack as the brand face. Do not replace Chromatophore with a system substitute on surfaces that compute Chromatophore.

Calling Chromatophore the sole marketing face, treating the Arial 600 hero field as a recorded control exception rather than as a second brand family, and refusing to present a fallback member as the brand face, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

### Type roles

Line heights stay in the form the source verified them: YAML unitless `1.5` and `1.11` beside the source table's own `1.5 (54px)` / `1.5 (42px)` / `1.5 (27px)` / `1.5 (18px)` writings where both exist. YAML `use` strings sit beside the §3 Notes writings; neither replaces the other. YAML `body-sm` (`16` / `400` / `1.5`, use: Nav links, captions — foreground white) is not the §3 Body / Nav row (`16px` / `500` / `1.11`, Notes: Nav links, button labels) and is not YAML `button` (`16` / `500` / `1.11`, use: Button label — Chromatophore medium). Keeping those YAML unitless ratios beside the source's own px equivalents, keeping `body-sm` unmerged from Body / Nav and from `button`, and keeping each YAML `use` string beside the hierarchy-table note without replacing either, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Token-set path | Token-set use | §3 notes |
|---|---|---:|---:|---|---|---|---|
| Display Hero (H1) | Chromatophore | 36px | 500 | 1.5 (54px) | `tokens.typography.display-hero` | H1 hero headline — cyan on deep indigo | Cyan `#60f0f8` on indigo canvas |
| Section Heading (H2) | Chromatophore | 36px | 500 | 1.5 (54px) | `tokens.typography.section` | H2 section headings — cyan | Cyan, same scale as H1 |
| Sub-heading (H3) | Chromatophore | 28px | 500 | 1.5 (42px) | `tokens.typography.subsection` | H3 feature subheadings — foreground white | `#f0ffff` foreground or `#100030` on light card |
| Body Large | Chromatophore | 18px | 400 | 1.5 (27px) | `tokens.typography.body` | Body paragraphs — foreground white | `#f0ffff` foreground white |
| Body-sm | Chromatophore | 16px | 400 | 1.5 | `tokens.typography.body-sm` | Nav links, captions — foreground white | YAML-only row; not the §3 Body / Nav 500 / 1.11 row |
| Body / Nav / Button | Chromatophore | 16px | 500 | 1.11 | `tokens.typography.button` | Button label — Chromatophore medium | Nav links, button labels |
| Caption | Chromatophore | 12px | 400 | 1.5 (18px) | `tokens.typography.caption` | Fine-print trust indicators | Trust indicators, fine print |

Type rules the source states:

- **Single typeface, all surfaces**: Chromatophore carries 100% of the typographic load. No secondary or mono companion visible on the marketing surface.
- **Medium weight as the default**: Weight 500 is used for all interactive and heading elements; weight 400 for body paragraphs only.
- **H1 = H2 in size**: The 36px heading scale does not differentiate H1 from H2 by size — context and color do the hierarchy work (cyan for main, white for sub).
- **Line height 1.5 throughout**: A single, generous line-height ratio applies across all sizes — simplified and consistent.

The four rule titles and the hierarchy / default-weight / H1=H2 / generous-ratio readings are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification. The sizes, weights, and ratios are recorded. YAML `button` lineHeight `1.11` remains on that key; it is not rewritten into `1.5`.

### Assets

- Catalog identity points at `logo.type: favicon`, slug `https://www.google.com/s2/favicons?domain=octopus.energy&sz=128`. That pointer is a third-party favicon-service URL, not an Octopus Energy-hosted brand file.
- The mascot Constantine the Octopus appears in hero illustrations; the source records that character as filling hero space that other brands give to photography — character, not whitespace.
- Product category cards name Energy / Heat pumps / Solar & batteries / Electric vehicles.

Reading the favicon-service URL as an identity pointer rather than a hosted brand file, reading Constantine as first-party illustration content rather than as a published illustration specification, and taking the source's reading that the mascot fills hero space that other brands give to photography — character, not whitespace — as illustration content rather than as a published art-direction specification, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### How applicability is decided here

The source declares each token-set component with a primitive type (`button`, `input`, `card`, `tab`, `badge`, `listItem`) and a value set; those types are preserved per component. Applicability below is judged by each control's role, never by whether a visual treatment for that state happens to be recorded: where the source supplies no treatment for an applicable state, the value is omitted and the state stays applicable. `not-applicable` is used only where the control's role makes the state meaningless — a destination control that commits no operation in place, a nav item that only selects a destination, or a display element with no action at all — and the reason given is always that semantic one. Where the source supplies no interaction evidence for a container at all, its kind and applicability map are omitted rather than decided. A `Primitive type` line is attached only when the source YAML records that type on that component.

The source records no `focus-visible` treatment. Generic focus is not invented. `focus-visible` stays applicable on interactive controls; the visual treatment is omitted. Motion-rule hover ("slightly lighter" magenta; outlined buttons fill) is a qualitative observation without a computed hex; it is not attached as a treatment value on the `hover` rows.

The role-based decision procedure above, every interactive-kind verdict, every applicability verdict, and the reason given for either is a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification. This is not a complete state-coverage claim.

### State record

The source's state contract, preserved with its values and copy. The source's own evidence note assigns a source to its voice samples, its brand narrative, and its personas, and assigns none to this state section; the treatments below are therefore a derived editorial implementation inference from the verified surfaces rather than measured per-control observations, and they are not Octopus Energy-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (postcode, no input)** | Input field with placeholder text in muted `#a49fc8`; magenta CTA is present and enabled — the empty state is the default CTA state. |
| **Loading (postcode quote fetch)** | Inline progress on the CTA; the deep indigo canvas stays visible. No full-page spinner — the quote funnel is fast. |
| **Error (invalid postcode)** | Inline field-level message below the input. Plain-language: "Please enter a valid UK postcode." Magenta border on the input field. |
| **Success (quote returned)** | Redirect to the quote flow with tariff cards on dark canvas. No celebratory toast — the product is the feedback. |
| **Empty (account, no usage data)** | Friendly message with a Constantine illustration reference; single CTA to set up smart meter. Calm and on-brand. |
| **Loading (account dashboard)** | Skeleton placeholder rows on `#180048` surface; flat pulse consistent with shadow-free system. |
| **Error (payment failed)** | Inline banner with plain-language explanation; "Update payment details" as a magenta CTA. No jargon. |
| **Success (payment taken)** | Confirmation email rather than in-app toast; app view shows updated balance. Understated. |
| **Disabled** | Reduced opacity on magenta elements; `#a49fc8` muted text for inactive fields. Brand magenta fades, does not switch to grey. |
| **Skeleton** | `#180048` blocks at final dimensions, flat colour pulse — no shimmer or shadow. |

These rows describe empty, loading, error, success, disabled, and skeleton treatments the source wrote at system level. They are not attached as visual treatments to destination controls below. Quote CTAs that commit a fetch cite the matching postcode loading / success rows because those controls commit Get a quote. Postcode fields cite the matching empty / error rows because those fields accept the postcode. That non-attachment reading, and those two attachments, are derived editorial implementation inferences from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification.

### Primary (Standard)

- Role: standard primary action (cookie accept "That's cool", navigation CTAs)
- Primitive type: `button` · Kind: interactive
- Domain: homepage cookie banner and standard actions
- Background: `#f050f8`
- Text: `#100030`
- Radius: 12px
- Padding: 12px 16px
- Font: 16px Chromatophore weight 500. Token-set font: `16px / 500 Chromatophore`
- Border: 2px solid `#f050f8`
- Token-set use: Primary CTA — cookie accept, standard action
- Published label: `That's cool`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; qualitative brightness shift named in motion rules without a computed hex, so no treatment value is carried |
| focus-visible | applicable | Keyboard-reachable action control; visual treatment omitted |
| disabled | applicable | The surface contract fades magenta rather than switching it to grey; visual treatment omitted on this record |
| loading | applicable | Cookie accept commits a consent step; visual treatment omitted |
| error | applicable | A committed consent step can fail; visual treatment omitted |
| success | applicable | A committed consent step can complete; visual treatment omitted |

### Primary (Large / Tariff CTA)

- Role: large hero CTA — "Get a quote" on tariffs page
- Primitive type: `button` · Kind: interactive
- Domain: `https://octopus.energy/tariffs/`
- Background: `#f050f8`
- Text: `#f0ffff`
- Radius: 12px
- Padding: 16px 20px
- Height: 56px
- Font: 18px Chromatophore weight 500. Token-set font: `18px / 500 Chromatophore`
- Border: 2px solid `#f050f8`
- Token-set use: Large primary CTA — Get a quote on tariffs page
- Published label: `Get a quote`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; qualitative brightness shift named in motion rules without a computed hex, so no treatment value is carried |
| focus-visible | applicable | Keyboard-reachable action control; visual treatment omitted |
| disabled | applicable | A quote CTA can be gated; visual treatment omitted |
| loading | applicable | This control commits Get a quote; the surface contract records inline progress on the CTA |
| error | applicable | A quote fetch can fail; visual treatment omitted on this button (invalid postcode is reported on the field) |
| success | applicable | A quote fetch can complete; the surface contract records a redirect to the quote flow with tariff cards on dark canvas |

### Outlined / Secondary

- Role: secondary option alongside primary (cookie "Fine tune")
- Primitive type: `button` · Kind: interactive
- Domain: homepage cookie banner
- Background: transparent
- Text: `#f050f8`
- Radius: 12px
- Padding: 12px 16px
- Font: 16px Chromatophore weight 500. Token-set font: `16px / 500 Chromatophore`
- Border: 2px solid `#f050f8`
- Token-set use: Secondary outlined button — Fine tune / cookie secondary
- Published label: `Fine tune`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; motion rules say outlined buttons fill on hover, without a computed fill hex, so no treatment value is carried |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A secondary option whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control opens Fine tune; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | This control sends the reader to cookie settings; the destination, not the button, reports whether that request failed |
| success | not-applicable | Same role reason: reaching Fine tune is not an operation with a success result on this button |

### Ghost / Nav

- Role: navigation link-buttons — "Energy", "Heat pumps", "Log in"
- Primitive type: `button` · Kind: interactive
- Domain: sticky global nav
- Background: transparent
- Text: `#f0ffff`
- Radius: 12px
- Padding: 12px 16px
- Font: 16px Chromatophore weight 500. Token-set font: `16px / 500 Chromatophore`
- Border: 2px solid transparent
- Token-set use: Nav ghost link-buttons — Energy, Heat pumps, Log in
- Published labels: `Energy`, `Heat pumps`, `Log in`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web destination control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable nav control; visual treatment omitted |
| disabled | applicable | A destination control whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control opens Energy, Heat pumps, or Log in; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | Destination action; the destination, not this button, reports failure |
| success | not-applicable | Same role reason: reaching those destinations is not an operation this button reports as success |

### Hero Homepage Postcode Input

- Role: postcode entry in the homepage hero "Get a quote" attached-CTA form
- Primitive type: `input` · Kind: interactive
- Domain: homepage hero (`#hero-quote-form-field-postcode`)
- Background: `#ffffff` (white)
- Text: `#000000` (black)
- Font: 18px Arial weight 600 (system font, not Chromatophore). Token-set font: `18px / 600 Arial`
- Radius: 12px 0px 0px 12px (left-rounded; the attached CTA button is on the right)
- Height: 60px
- Padding: 16px
- Token-set use: Hero homepage postcode field — white bg / black text / Arial 600, left-rounded, attached to magenta Get-a-quote button on right

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web field; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable form field; visual treatment omitted |
| disabled | applicable | A field whose availability can lapse; visual treatment omitted |
| loading | not-applicable | The field accepts a postcode; the quote fetch is committed by the attached Get a quote button, not by the field |
| error | applicable | Form field; the surface contract records "Please enter a valid UK postcode." with a magenta border on the input field |
| success | not-applicable | Completion is not confirmed on the field; success is a redirect from the attached CTA |

### Hero "Get a Quote" Attached CTA Button

- Role: inline submit button attached to the hero postcode field
- Primitive type: `button` · Kind: interactive
- Domain: homepage hero, paired with `#hero-quote-form-field-postcode`
- Background: `#f050f8`
- Text: `#f0ffff`
- Radius: 0px 12px 12px 0px (right-rounded; completes the pill formed with the input)
- Padding: 16px 20px
- Font: 18px Chromatophore weight 400. Token-set font: `18px / 400 Chromatophore`
- Border: 2px solid `#f050f8`
- Token-set use: Hero attached-CTA button — magenta, right-rounded, paired with hero postcode input
- Published label: `Get a quote`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; qualitative brightness shift named in motion rules without a computed hex, so no treatment value is carried |
| focus-visible | applicable | Keyboard-reachable submit control; visual treatment omitted |
| disabled | applicable | A submit control can be gated; visual treatment omitted |
| loading | applicable | This control commits Get a quote; the surface contract records inline progress on the CTA |
| error | applicable | A quote fetch can fail; invalid postcode is reported on the paired field |
| success | applicable | A quote fetch can complete; the surface contract records a redirect to the quote flow |

### Tariffs Page Postcode Input

- Role: full-width postcode entry on the /tariffs/ page, separate from the large magenta "Get a quote" button below it
- Primitive type: `input` · Kind: interactive
- Domain: `https://octopus.energy/tariffs/` (`#postcode`)
- Background: transparent (`rgba(0,0,0,0)`)
- Text: `#f0ffff`
- Font: 18px Chromatophore weight 400. Token-set font: `18px / 400 Chromatophore`
- Radius: 0px (no rounding — full-width standalone field)
- Height: 60px
- Padding: 16px
- Border: 2px solid transparent
- Token-set use: Tariffs page full-width postcode input — transparent / #f0ffff text / Chromatophore 400 / no radius

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web field; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable form field; visual treatment omitted |
| disabled | applicable | A field whose availability can lapse; visual treatment omitted |
| loading | not-applicable | The field accepts a postcode; the quote fetch is committed by the large Get a quote button below it, not by the field |
| error | applicable | Form field; the surface contract records "Please enter a valid UK postcode." with a magenta border on the input field |
| success | not-applicable | Completion is not confirmed on the field; success is a redirect from the large CTA |

### Product Feature Card

- Role: product category cards (Energy / Heat pumps / Solar & batteries / Electric vehicles) sitting on the deeper `#100030` canvas
- Primitive type: `card`
- Domain: homepage
- Background: `#180048`
- Text: `#100030`
- Radius: 12px
- Token-set use: Product feature card (Energy/Heat pumps/Solar) on deep indigo
- Product name, recorded only in the source's agent-prompt example and kept here (A3): 16px Chromatophore weight 500, `#100030`. Keeping that agent-prompt-only product-name metric on this card rather than dropping it with the prompt guide is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

The source supplies no interaction evidence for this card, so kind and a state-applicability map are both withheld.

### Trust Indicator Pill

- Role: award and rating trust pills
- Primitive type: `badge`
- Kind: non-interactive — a trust marker, not a commit control
- Background: `#180048`
- Text: `#f0ffff`
- Radius: 9999px
- Font: 12px Chromatophore weight 400. Token-set font: `12px / 400 Chromatophore`
- Padding: 4px 12px
- Token-set use: Trust indicator pill — star ratings, awards
- Published labels: `Which? Recommended Provider`, `Rated 4.8 stars`

Padding `4px 12px` is recorded only in the source's agent-prompt example and is kept on this component rather than dropped. Keeping that agent-prompt-only padding on this pill rather than dropping it with the prompt guide is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

### Top Nav item

- Role: top nav item
- Primitive type: `tab` · Kind: interactive
- Domain: sticky global nav
- Text: `#f0ffff`
- Font: 16px Chromatophore weight 500. Token-set font: `16px / 500 Chromatophore`
- Active: text `#f0ffff` on `#180048` background
- Token-set use: Top nav item
- Token-set active: text #f0ffff on #180048 background

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable item; visual treatment omitted |
| disabled | applicable | A destination item whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This item is a destination tab; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | Destination tab; the destination, not the item, reports failure |
| success | not-applicable | Same role reason: reaching a nav destination is not an operation with a success result |

### Inline body link

- Role: inline body links and sub-nav anchor text
- Primitive type: `listItem` · Kind: interactive
- Text: `#60f0f8`
- Font: 18px Chromatophore weight 500. Token-set font: `18px / 500 Chromatophore`
- Token-set use: Inline body links and sub-nav anchor text

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable link; visual treatment omitted |
| disabled | applicable | A destination link whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a destination link; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | Destination link; the destination, not the link, reports failure |
| success | not-applicable | Same role reason: following the link is not an operation this control reports as success |

### Sticky top nav chrome

- Role: sticky global nav with product category links + "Log in" ghost button
- Primitive type: not in the token set
- Background: `rgba(16, 0, 48, 0.9)` (sticky, slightly translucent)
- Text: `#f0ffff` for links, `#60f0f8` for logo wordmark
- Font: 16px Chromatophore weight 500
- Radius: 12px on nav items
- Height: ~60px
- Domain: both inspected pages

The source describes this bar as chrome around the nav items already declared above, so kind and a state-applicability map are both withheld.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

- Centered single-column hero with the 36px cyan headline anchoring the top
- 4-column product cards below (Energy / Heat pumps / Solar / EV) on desktop
- Full-width dark sections; no container border — the indigo bleeds edge to edge
- Max content width: approximately 1200px centered
- Spacing restated from `tokens.spacing`: 4 / 8 / 12 / 16 / 20 / 24 / 48 / 64
- Shape restated from `tokens.rounded`: sm 4 · md 8 · lg 12 · `full: 9999`

Whitespace philosophy, as the source states it:

- **Dark-canvas breathing room**: generous vertical padding between sections keeps the dark canvas from feeling claustrophobic
- **Color as separator**: different tints of indigo (`#100030` vs `#180048`) create section breaks without any visible borders or shadows
- **Illustration as content**: the Constantine octopus mascot fills hero space that other brands give to photography — character, not whitespace

Reading that marketing surface as dark-canvas breathing room, as color-as-separator rather than border or shadow, and as illustration-as-content, is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

Responsive behavior. The source's live inspection was a computed-style pass on the homepage and tariffs page; the breakpoints, collapsing strategy, and the source's reading of the recorded target sizes are stated by the source at system level rather than measured across viewports, so they are a derived editorial implementation inference from the verified surfaces and are not Octopus Energy-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero headline compresses, product cards stack |
| Tablet | 640-1024px | 2-column product cards, moderate padding |
| Desktop | 1024px+ | Full 4-column product grid, centered 1200px container |

Touch targets the source records:

- Nav buttons at 46px height with 12px 16px padding — well within touch target guidelines
- Large CTA buttons at 56px height on tariff pages — generous for mobile
- Postcode input at 60px height for easy tap

Collapsing strategy, as the source states it:

- Hero headline stays at weight 500, scales down on mobile
- 4-column product cards collapse to 2-up then single column
- Sticky nav compresses to hamburger toggle at mobile
- Dark background sections maintain full-width indigo treatment

`tokens.spacing.base: 16` is not the postcode `16px` padding. `tokens.spacing.md: 12` is not nav padding `12px 16px`. Nav bar height `~60px` is not nav-button height `46px` and is not postcode height `60px`. Large CTA `56px` is not a spacing step. Approximately `1200px` is a max content width, not a breakpoint. Keeping those measurements on the jobs the source assigned them is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The source characterizes Octopus Energy's voice as **warm, direct, and cheeky** — the energy brand that doesn't talk like an energy brand. Where competitors use utility-grade corporate prose ("Switching is simple — just provide your account details"), Octopus uses the friendly confidence of a trusted friend who happens to know a lot about renewable energy. The homepage headline "Join the UK's most popular energy supplier" isn't a claim — it's a casual invitation. The brand's playfulness is genuine rather than performed; it flows from a founding mission (make clean energy affordable and accessible) rather than a marketing brief. That characterization, that "casual invitation" reading, and that genuine-rather-than-performed reading are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification. The published lines themselves are live surface copy.

| Context | Tone |
|---|---|
| Hero headlines | Confident and welcoming. "Join the UK's most popular energy supplier." |
| Tariff descriptions | Plain-language, benefit-first. "Most homes could save with our fixed and flexible tariffs." |
| CTAs | Action-forward, friendly. "Get a quote", "That's cool", "change my tariff." |
| Trust signals | Factual and proud. "Rated 4.8 stars for customer service from 815,906 reviews." |
| Support / help | Warm and pragmatic. Explains without condescension. |
| Sustainability | Earnest without preaching. States the mission, lets the numbers speak. |

**Voice samples (verbatim from live homepage):**

- "Join the UK's most popular energy supplier" — H1 hero headline, welcoming not boastful.
- "Everything you need for a cheaper, greener home & business" — H2 section heading, benefit-led.
- "Great value energy for your home or business" — H2, plain and accessible.
- "Our standard prices have always been cheaper than the price cap" — body copy, confident claim with data behind it.

Parenthetical captions on those four samples (welcoming not boastful / benefit-led / plain and accessible / confident claim with data behind it) are a derived editorial implementation inference from the verified surfaces; they are not Octopus Energy-authored or a separately published UI specification.

Further published strings the source records, kept byte-exact:

- Join the UK's most popular energy supplier
- Everything you need for a cheaper, greener home & business
- Great value energy for your home or business
- Our standard prices have always been cheaper than the price cap
- Most homes could save with our fixed and flexible tariffs
- Get a quote
- That's cool
- Fine tune
- change my tariff
- Energy
- Heat pumps
- Log in
- Which? Recommended Provider
- Rated 4.8 stars
- Rated 4.8 stars for customer service from 815,906 reviews
- Please enter a valid UK postcode
- Update payment details
- Octopus Energy: The UK's most awarded energy supplier
- All our tariffs | Octopus Energy

**Forbidden register**: corporate energy jargon, fear-of-missing-out pricing urgency, vague greenwashing ("we care about the planet"), defensive contract language foregrounded in user flows, and excessive exclamation points. That exclusion list is the source's own Don't-register; treating it as this contract's forbidden register is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

The inspected surfaces and the invalid-postcode string are UK English and a UK postcode. That is recorded copy and a recorded market, not a complete locale profile. Reading those strings as recorded copy and a recorded market rather than as a complete locale profile is a derived editorial implementation inference from the verified surfaces; it is not Octopus Energy-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- the exact `ease-enter`, `ease-exit`, and `ease-standard` curves; the token names and uses are recorded, the curves carry no attribution
- hover, button-press, and focus-visible visual treatments as computed values; motion rules name a qualitative magenta brightness shift and an outlined fill without a hex
- the interactive kind of the Product Feature Card
- computed per-component values behind the empty, loading, error, success, skeleton, and disabled treatments described above
