# Adobe Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Adobe is a US design-tools company whose public product story spans Creative Cloud, Document Cloud, Experience Cloud, Adobe Express, and Firefly. Treating that lineup as the product-story span of this reconstruction rather than as interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. Catalog homepage identity is `https://www.adobe.com`. Official design system = Spectrum (`https://spectrum.adobe.com`, `ds.type: system`). Spectrum 2 (`https://s2.spectrum.adobe.com/`) is the current generation — blue-900 `#3b63fb` accent, 16px pill geometry, Adobe Clean Spectrum VF.

Treating the following URLs as the named evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. This contract covers six pages across five evidence-domain groups from the 2026-06-11 packet: the marketing homepage `https://www.adobe.com/`; the Creative Cloud plans/commerce surface `https://www.adobe.com/creativecloud/plans.html`; Spectrum docs `https://spectrum.adobe.com/page/button/` and `https://spectrum.adobe.com/page/principles/` as one docs group; the Spectrum 2 announcement site `https://s2.spectrum.adobe.com/`; and the about page `https://www.adobe.com/about-adobe.html`. Official token package `@adobe/spectrum-tokens` v14.13.0 is a named token source for Spectrum 2 color/type/layout JSON, not a captured page.

Source token note: primary = Adobe brand red (`#eb1000`, logo + marketing). Product/commerce interactive accent = Spectrum 2 blue-900 (`#3b63fb`, live Buy-now CTA + official `@adobe/spectrum-tokens` v14). Legacy Spectrum 1 accent `#0265dc` still renders on spectrum.adobe.com docs. Treating that note as a register split — `#eb1000` is catalog `primary_color` and identity, `#3b63fb` is the harvested Buy-now fill and commerce accent rather than a second identity red, `#0265dc` is Spectrum 1 docs rather than Spectrum 2 commerce, and Spectrum names the current generation Spectrum 2 — is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

The following evidence-domain sentence is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. Color, type, and component values below stay attached to the surface that established them. A homepage marketing value is not a proxy for Spectrum docs. A Spectrum 2 commerce value is not a proxy for Spectrum 1 docs chrome.

The next paragraphs’ shout-whisper, two-register, mid-migration, friendlier-geometry, unobtrusive-infrastructure, heaviest-hero-comparison, monochrome-frame, logo-as-signature, contrast-strategy-binary, and near-shadowless / whisper-of-elevation readings are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. The source reads Adobe’s web presence as letting the work shout while the chrome whispers. The homepage is described as an almost monochrome frame — pure white (`#ffffff`) canvas, black (`#000000`) text, white-on-dark navigation — wrapped around enormous, saturated creative imagery and video. The single loudest brand element in that reading is the logo itself: the Adobe red A (`#eb1000`) sits in the corner like a signature, while the page’s working accent colors stay neutral. Headlines land at 80px in Adobe Clean Display Black (weight 900) over full-bleed artwork, declaring “Create at the highest level.” The contrast strategy is read as binary: maximum-weight type, minimum-color UI.

Underneath the marketing layer runs Spectrum, now in its second generation. Spectrum 2 (“Rational. Human. Focused. Collaborative.”) shows up live on commerce surfaces: the Creative Cloud pricing page renders its Buy-now CTAs in Spectrum 2’s blue-900 accent (`#3b63fb`) as fully rounded 16px pills, with links in blue-1000 (`#274dea`) and a neutral gray ladder (`#131313` ink, `#292929` strong labels, `#686868` muted) pulled from the official `@adobe/spectrum-tokens` palette. The older Spectrum 1 accent blue (`#0265dc`) still renders on the spectrum.adobe.com documentation site. The source treats that leftover as a visible artifact of a design system mid-migration, and as a reminder that Adobe runs marketing chrome (red + black + white) and product chrome (Spectrum blue) as two coordinated but distinct registers.

The geometry is read as friendlier than expected from a 40-year-old enterprise: pills everywhere (75px and 999px radii on marketing CTAs, 16px full-round on Spectrum 2 buttons), 16px-radius cards on soft gray tiles (`#f3f3f3`), and translucent glass quick-link cards floating over hero video. Shadows are nearly absent — separation comes from background tint shifts and hairlines (`#dadada`), with only a whisper of elevation (`rgba(0,0,0,0.06)`) on floating helpers. The total effect is read as confident, editorial, and deliberately unobtrusive: infrastructure for other people’s creativity. Treating “Weight 900 at 80px as the marketing voice — the heaviest hero typography of any major design-tools brand” as an editorial comparison, not a first-party ranking, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

Treating the following public-history and about-page facts as narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. Adobe was founded in **December 1982** by **John Warnock** and **Charles Geschke**, two Xerox PARC scientists who left to commercialize PostScript — the page-description language that made the desktop-publishing revolution physically printable — and named the company after Adobe Creek behind Warnock’s home in Los Altos. Those founding details are widely documented public facts used in the source without same-turn verification. The stated mission on about-adobe.html: “Adobe empowers everyone, everywhere to imagine, create, and bring any digital experience to life” — compressed on that page to “Empowering everyone to create.” The company organizes its world into three clouds (Creative, Document, Experience) plus Adobe Express. Scale numbers on that page: 29 billion+ Firefly images generated, 700 million+ Adobe Stock assets, 50 million+ Behance members. The company states it trains Firefly “on data that allows us to offer customers a solution designed to be commercially safe” (about-adobe.html, live 2026-06-11).

The following through-line / refuses / embraces readings are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. The source reads a four-decade through-line: Adobe builds the substrate other people create on (PostScript begat PDF; Illustrator and Photoshop begat Creative Cloud; the current era adds Firefly generative AI). Spectrum 2 frames the redesign in human terms: “more modern, more friendly, more accessible, and more enjoyable to use,” and “The biggest change in Spectrum is... all of the little things.” What the source treats Adobe as refusing: decoration for its own sake (shadowless UI, monochrome chrome), and using its own brand color where the user’s content should dominate. What it treats Adobe as embracing: typographic confidence, open tokens (`@adobe/spectrum-tokens` published on npm), and a system designed to “belong to everyone.”
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

- Start a Creative Cloud plan (“Buy now”) or a free trial on the captured Adobe marketing and commerce surfaces.
- Switch Individuals / Business / Students & teachers on the Creative Cloud plans page.
- Scan homepage product-category tiles and hero CTAs on `https://www.adobe.com/`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable Adobe user segments (creative professionals, freelance designers, students on Express, enterprise product teams building on Spectrum), not individual people. Restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

### Distinctive traits

- Brand red `#eb1000` as catalog `primary_color` and logo fill; not used for buttons or links
- Spectrum 2 accent `#3b63fb` as live Buy-now CTA fill; link blue `#274dea`; legacy Spectrum 1 `#0265dc` on spectrum.adobe.com docs
- Neutral ladder `#131313` / `#292929` / `#2c2c2c` / `#686868` on commerce; heading `#000000` on marketing
- Canvas `#ffffff`, surface `#f8f8f8`, tile `#f3f3f3`, hairline `#dadada`
- Promo yellow `#f5c700`; success `#05834e`; error `#d73220` (distinct from brand red)
- Adobe Clean family: Display Black 900 at 80px / 48px / 24px; UI 14px / 700; commerce body 18px; Spectrum VF / Serif / Source Code Pro / Adobe Clean Han
- Pill geometry: 75px / 999px marketing CTAs; 16px Spectrum 2 rounds; 8px glass cards; 4px folder-tab tops
- Near-shadowless: tint shifts and `#dadada` hairlines; `rgba(0,0,0,0.06) 0px 4px 12px` reserved for the floating assistant
- Official Spectrum tokens published as `@adobe/spectrum-tokens` v14.13.0

Treating `#eb1000` as identity rather than interaction, treating `#3b63fb` as Spectrum 2 commerce accent rather than brand red, treating `#0265dc` as Spectrum 1 docs rather than Spectrum 2, treating heading `#000000` as unmerged from commerce ink `#131313`, treating error `#d73220` as unmerged from brand red, treating 75px and 999px as distinct marketing-pill radii rather than one step, treating 15px Buy-now type as unmerged from 14px YAML `typography.button`, and treating near-shadowless chrome with `rgba(0,0,0,0.06) 0px 4px 12px` reserved for the floating assistant as a surface-pattern rather than a universal elevation token, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

### Principles

The four numbered Spectrum items below are first-party Spectrum principle language from `https://spectrum.adobe.com/page/principles/` (Rational, Human, Focused) and `https://s2.spectrum.adobe.com/` (Collaborative: “The future is built collectively… it belongs to everyone.”). The *UI implication* notes, item 5, and the capture-bound application list are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. Item 5 is an interpretive claim connecting observed design to stated principles; the source HTML comment marks “brand recedes, work leads” as editorial.

1. **Rational.** “Spectrum is based on real-world situations. Every component, pattern, and principle is informed by research and thoughtful testing.” (spectrum.adobe.com/page/principles, live 2026-06-11).
   *UI implication:* no speculative variants — every component spec ships with a use case, and values come from tokens, not eyeballing.
2. **Human.** “Spectrum places customer needs first. It's deeply committed to a high standard of accessibility, honesty, and respect for user attention.”
   *UI implication:* WCAG-compliant contrast on every pairing, visible focus states, a dedicated mobile type scale (14→17px) rather than shrunken desktop UI, and a reduce-motion toggle (present on s2.spectrum.adobe.com itself).
3. **Focused.** “Spectrum strives to deliver what's needed, when it's needed. No unnecessary decoration or irrelevant content.”
   *UI implication:* shadowless surfaces, monochrome chrome, one accent color per register; imagery carries the emotion so the UI doesn't have to.
4. **Collaborative (Spectrum 2).** “The future is built collectively… it belongs to everyone.”
   *UI implication:* design with tokens, not hardcodes — the system is consumed by hundreds of Adobe products and published openly; an interface styled with `@adobe/spectrum-tokens` values stays upgrade-compatible.
5. **Brand recedes, work leads.** Adobe red appears exactly once per page — the logo.
   *UI implication:* never use `#eb1000` for interactive elements; let user content and product imagery be the loudest pixels on screen.

Treating the following as a capture-bound application of source §7 Do’s, live inspect, and Spectrum 2 tokens, with first-party Spectrum names marked separately from the remaining token-role rules, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

- First-party (Spectrum docs / `@adobe/spectrum-tokens`): official design system Spectrum; Spectrum 2 current generation; families Adobe Clean Spectrum VF, Adobe Clean Serif, Source Code Pro.
- Use Adobe Clean for the captured marketing/UI surfaces; reach for Display Black (900) on headlines.
- Reserve Adobe red (`#eb1000`) for the brand mark and identity moments only.
- Use Spectrum 2 blue (`#3b63fb`) for primary interactive accents in product/commerce UI.
- Use white pill CTAs (75px+ radius, 14px/700) over dark imagery.
- Separate sections with tint shifts (`#f3f3f3`, `#f8f8f8`) and `#dadada` hairlines, not shadows.
- Let full-bleed imagery and video carry the color; keep UI chrome black/white/gray.
- Use the official Spectrum gray ladder (`#131313`, `#292929`, `#686868`) for text hierarchy.
- Round Spectrum 2 components fully (16px at standard heights).

### Avoid

The following items copy source §7 Don’ts. They are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification.

- Do not use Adobe red as a button or link color — interaction is Spectrum blue, identity is red.
- Do not mix Spectrum 1 (`#0265dc`, 4px corners) and Spectrum 2 (`#3b63fb`, 16px pills) values in one surface.
- Do not add drop shadows to cards or buttons — the system separates with tint and translucency.
- Do not set headlines below weight 900 on marketing surfaces — Display Black is the voice.
- Do not introduce a second typeface — Adobe Clean covers display, body, serif, code-adjacent and CJK.
- Do not use error red (`#d73220`) and brand red (`#eb1000`) interchangeably — they are different tokens with different jobs.
- Do not crowd the hero with copy — one headline, one line of support text, one or two pill CTAs.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including identity-not-interaction on Adobe Red and Spectrum-1-reproduction-only on `#0265dc`, are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. Catalog `primary_color` `#eb1000` is the live logo fill and marketing badge color (15 background occurrences on the live homepage; `.cls-1{fill: #eb1000}` in the live SVG); it is not Buy-now `#3b63fb` and not error `#d73220`. Accent Blue is the Spectrum 2 commerce CTA fill, not a second identity red. Link Blue `#274dea` is commerce hyperlinks (“See terms.”), not the Buy-now fill. Legacy Spectrum 1 Blue `#0265dc` is spectrum.adobe.com docs, not Spectrum 2 commerce. Heading `#000000` is marketing heading/body and inverse-pill text; Ink `#131313` is commerce primary text and active tab labels; they are not merged. Strong Label `#292929` is outline-button text; Body Slate `#2c2c2c` is commerce body copy; they are not Ink. Surface `#f8f8f8` is active-tab fill; Surface Tile `#f3f3f3` is homepage category-tile fill; they are not Canvas. `#ffffff` is canvas, nav text on dark headers, CTA pill fills, and text on accent blue (YAML `on-primary`); those jobs share a hex and are not a second canvas token. Promo `#f5c700` is the Best-value flag, not a Spectrum 2 CTA.

**Brand**

- **Adobe Red** (`#eb1000`): The brand mark. Logo fill and recurring marketing badge color. YAML `primary`. Identity, not interaction: the source records it is not used for buttons or links.
- **Black** (`#000000`): Primary heading and body text on marketing surfaces; also the text color on white pill CTAs. YAML `heading`.
- **Pure White** (`#ffffff`): Page canvas, nav text on dark headers, CTA pill fills, text on accent blue. YAML `canvas` / `on-primary`.

**Spectrum 2 Accent (product/commerce interactive)**

- **Accent Blue / blue-900** (`#3b63fb`): The Spectrum 2 accent. Live “Buy now” CTA background on the Creative Cloud plans page (35 occurrences) and the official `blue-900` light value — rgb(59, 99, 251) — in `@adobe/spectrum-tokens` v14. YAML `accent`.
- **Link Blue / blue-1000** (`#274dea`): Hyperlinks and text buttons on commerce surfaces (“See terms.”), matching official `blue-1000` rgb(39, 77, 234). YAML `accent-link`.
- **Legacy Spectrum 1 Blue** (`#0265dc`): The previous-generation accent (blue-800/900 in Spectrum 1), still rendering on spectrum.adobe.com component docs. Use only when reproducing classic Spectrum 1 product UI. YAML `accent-legacy-s1`.

**Neutral Ladder (Spectrum gray, light theme)**

- **Ink / gray-900** (`#131313`): Primary text on commerce surfaces, active tab labels. YAML `ink`.
- **Strong Label / gray-800** (`#292929`): Secondary button text, strong UI labels. YAML `gray-800`.
- **Body Slate** (`#2c2c2c`): Commerce body copy (live computed body color on plans page). YAML `body`.
- **Muted / inactive** (`#686868`): Inactive tab labels, tertiary text. YAML `muted`.
- **Surface / gray-50** (`#f8f8f8`): Active tab fill, subtle panel backgrounds. YAML `surface`.
- **Surface Tile** (`#f3f3f3`): Homepage product-category tile background. YAML `surface-tile`.
- **Hairline / gray-300** (`#dadada`): 2px outline-button borders, dividers — matches official `gray-300` rgb(218, 218, 218). YAML `hairline`.

**Semantic & Promo**

- **Promo Yellow / yellow-400** (`#f5c700`): “Best value” promotional flags on pricing cards — matches official `yellow-400` rgb(245, 199, 0). YAML `promo-yellow`.
- **Success / green-900** (`#05834e`): Positive semantic color from the official Spectrum 2 palette (rgb(5, 131, 78)). YAML `success`.
- **Error / red-900** (`#d73220`): Negative semantic color from the official Spectrum 2 palette (rgb(215, 50, 32)) — deliberately distinct from brand red `#eb1000`. YAML `error`.

### Spacing

YAML `spacing`: xs 4, sm 8, md 12, base 16, lg 24, xl 32, section 64. Body layout states a 4px/8px rhythm and the same observed scale: 4px, 8px, 12px, 16px, 24px, 32px, 64px. Treating that YAML scale and 4px/8px rhythm as recorded spacing in this packet rather than a universal spacing token for every unlisted control is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. Glass quick-link internal padding is `12px 12px 16px`. Audience-tab padding is `24px 32px 18px`. Inverse/Ghost pill padding is `0px 24px` (YAML inverse `0 24px`). Outline / header Sign-in padding is `6px 16px 8px`. Buy-now padding is `3px 16px 4px`. Nav has no recorded padding in YAML.

### Shape

YAML `rounded`: sm 4, md 8, lg 16, full 999.

Observed radii in the body:

- Small (4px): folder-tab tops (`4px 4px 0px 0px`), legacy Spectrum 1 components
- Medium (8px): glass quick-link cards
- Large (16px): tiles, Spectrum 2 buttons (full-round at component height), modals
- Pill (75px / 999px): every marketing CTA and header button
- App switcher: 5px radius, 32px square
- Floating assistant: 24px radius, 48px height

The following local-geometry reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. 4px / 5px / 8px / 16px / 24px / 75px / 999px remain local harvested geometry, not a universal radius for every unlisted control. 75px inverse/ghost pills and 999px header Sign-in are distinct marketing-pill radii, not one YAML `full` step applied everywhere. 16px Spectrum 2 rounds are not 75px marketing pills.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Nearly everything: heroes, tiles, tabs, buttons |
| Tint (Level 1) | `#f8f8f8` / `#f3f3f3` background shift | Section and panel separation |
| Hairline (Level 2) | 2px solid `#dadada`; inset `rgba(255,255,255,0.11)` ring on glass | Outline buttons; glass-card edge |
| Float (Level 3) | `rgba(0,0,0,0.06) 0px 4px 12px` | Floating assistant button, sticky helpers |

YAML `tokens.shadow.subtle` is `rgba(0,0,0,0.06) 0px 4px 12px`. YAML `tokens.shadow.inset-hairline` is `rgba(255,255,255,0.11) 0px 0px 0px 1px inset`. Live inspection found `box-shadow: none` on virtually every button, card, and headline.

The following divider-not-elevation and shadow-philosophy readings are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. The source reads Adobe’s marketing system as effectively shadowless. Depth is communicated by photographic imagery itself and by translucency: the glass quick-link dock (`rgba(0, 0, 0, 0.44)` over video) is read as a floating layer without a drop shadow. The one genuine elevation — a soft `rgba(0,0,0,0.06)` blur on the floating “Ask a question” pill — is reserved for UI that must visibly sit above scrolling content. The source says that discipline mirrors Spectrum’s “Focused” principle: no unnecessary decoration. That mirroring is this derived reading, not a Spectrum-authored elevation token.

### Motion

Source-stated duration roles. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and marks motion token values as illustrative defaults consistent with observed behavior, with Spectrum’s internal motion token values not publicly documented on the inspected pages. Treating the duration table, easing names, content-not-chrome / no-spring readings, and reduced-motion line as source-stated rather than computed CSS, and treating the “tools for professionals signal steadiness” reading as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, focus ring, pill press |
| `motion-standard` | 200ms | Tab switch, card reveal, dropdown |
| `motion-slow` | 320ms | Hero media crossfade, section transitions |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving panels, cards |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including treating the live “Pause” control as an accessibility commitment, are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. Motion at Adobe is described as carried by content, not chrome — heroes autoplay video (with an explicit “Pause” control in the live DOM, an accessibility commitment), while UI transitions stay quick and unobtrusive. Carousels page with simple horizontal slides; tabs swap panels with a fade, never a bounce. The Spectrum 2 site ships a first-class “Reduce motion” toggle (observed live), and `prefers-reduced-motion: reduce` collapses transitions to instant and halts autoplaying media. No spring or overshoot anywhere: tools for professionals are read as signalling steadiness.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including the fallback-not-product-face assignment, are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | `@adobe/spectrum-tokens` names `Adobe Clean Spectrum VF` as official `sans-serif-font-family`, `Adobe Clean Serif` for docs display, and `Source Code Pro` as official `code-font-family`. Spectrum 2 is the current published generation. |
| Live computed surface-use | Marketing/UI stack `"Adobe Clean", adobe-clean, "Trebuchet MS", sans-serif`. Display Black (900) at 80px / 48px / 24px on the homepage. Commerce H1 36px / 700. Commerce body 18px / 400. Homepage body 16px / 400. Buttons and global nav 14px / 700. |
| Official distributed asset | `@adobe/spectrum-tokens` v14.13.0 on npm/GitHub publishes color-palette / typography / layout JSON. Adobe Clean is the live computed family on the captured marketing/UI surfaces. |
| Declared-only / fallback | `Trebuchet MS` and `sans-serif` sit in the live computed stack as fallbacks. They are fallbacks, not the brand face. |
| CJK extension | `adobe-clean-han-japanese` / “Adobe Clean Han” on international docs surfaces. That is docs-surface evidence, not a homepage marketing face. |
| Unresolved claim | Spectrum docs page titles use `adobe-clean-serif` at 58px / 800. That is a docs-display role, not a marketing Display Hero substitute. |

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. Do not present Trebuchet MS or a system fallback as Adobe Clean. Do not replace Adobe Clean with Trebuchet MS or another face as if it were Adobe Clean. Do not present Adobe Clean Serif as the marketing Display Black face. Do not present Source Code Pro as the marketing UI family.

The following type-rule readings (one family total coverage; weight 900 as marketing voice; 700 for interactive; commerce reads bigger; docs go serif) are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. Adobe Clean is described as spanning 900-weight display to 14px UI labels, plus Serif, Han, and variable (Spectrum VF) cuts, with no second typeface appearing. Hero and section headlines always use Display Black. Buttons, nav links, and eyebrows sit at bold 14-16px — interaction is read as signaled by weight before color. Body text steps up from 16px (marketing) to 18px/27 (pricing) where comprehension of terms matters. Spectrum documentation uses adobe-clean-serif at 58px/800 for page titles — an editorial register reserved for the design system’s own voice.

### Family

- **Marketing/UI:** `"Adobe Clean", adobe-clean, "Trebuchet MS", sans-serif` (live computed stack) — YAML `family.sans`
- **Display:** `Adobe Clean Display Black` for 900-weight headlines — YAML `family.display`
- **Spectrum 2 token family:** `Adobe Clean Spectrum VF` — YAML `family.spectrum`
- **Docs display:** `Adobe Clean Serif` / `adobe-clean-serif` — YAML `family.serif`
- **Code:** `Source Code Pro` — YAML `family.code`
- **CJK:** `adobe-clean-han-japanese` / “Adobe Clean Han” on international docs surfaces

### Type roles

Verified YAML line-height values are the unitless ratios `1.5` and `1.25`. The following ratio-versus-size-local, no-lineHeight-promoted, and Buy-now-15px-unmerged-from-YAML-button-14px readings are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures (`27px` at 18px, `20px` at 16px) are size-local observations, not replacements for the ratios. YAML roles without a lineHeight stay without one. YAML `typography.button` 14px / 700 is the button/nav role; Buy-now harvested font 15px / 700 is that control’s field and is not this row.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Use (YAML / body) |
|---|---|---:|---:|---:|---|---|
| Display Hero | Adobe Clean Display Black | 80px | 900 | — | — | Homepage hero headlines, Adobe Clean Display Black on dark imagery (“Create at the highest level.”) |
| Display Section | Adobe Clean Display Black | 48px | 900 | — | — | Section headlines and customer-quote pull text |
| Page Title | Adobe Clean | 36px | 700 | — | — | Commerce page H1 (plans & pricing) |
| Card Heading | Adobe Clean Display Black | 24px | 900 | — | — | Feature/product card heads, Adobe Clean Display Black (“Firefly”) |
| Tab Label | Adobe Clean | 18px | 600 | — | — | Audience tabs on pricing |
| Body Large | Adobe Clean | 18px | 400 | 1.5 | 27px at this size | Commerce body copy |
| Body | Adobe Clean | 16px | 400 | 1.25 | 20px at this size | Homepage body and card copy |
| Section Label | Adobe Clean | 16px | 700 | — | — | Eyebrow labels (“Adobe News”) |
| Button / Nav | Adobe Clean | 14px | 700 | — | — | Buttons and global nav labels |
| Docs Display | adobe-clean-serif | 58px | 800 | — | — | Spectrum docs page titles |

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. The live homepage SVG fill `.cls-1{fill: #eb1000}` is identity evidence for Adobe Red.

Treating verified product and hero imagery/video as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Treating the following table as the source state contract preserved here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated.

| State | Treatment |
|---|---|
| **Empty (no files/projects)** | White canvas, one Ink (`#131313`) sentence naming the next action, one accent (`#3b63fb`) pill CTA. The tone mirrors hero copy: what you can make, not what is missing. |
| **Empty (search, no results)** | Muted (`#686868`) single line with the query echoed back and a path to broaden; no illustration noise. |
| **Loading (page/panel)** | Skeleton blocks in `#f3f3f3`/`#f8f8f8` at final dimensions, 16px radius, flat pulse — no shimmer gradients on a shadowless system. |
| **Loading (in-place action)** | Button label swaps to progress indicator inside the same pill; geometry never changes. |
| **Error (form/field)** | Field-level message in error red (`#d73220` — the Spectrum token, never brand `#eb1000`), 2px border on the offending input, text stating what would be valid. |
| **Error (system/page)** | Inline panel on `#f8f8f8` with a plain-language sentence and one retry action; no stack traces, no “Something went wrong” alone. |
| **Success (purchase/save)** | Inline confirmation with success green (`#05834e`) icon + sentence-case past-tense copy; next step linked immediately (“Open in Photoshop”). |
| **Skeleton** | Tint blocks at final size; text skeletons match the heavy display proportions so 900-weight headlines don't reflow on arrival. |
| **Disabled** | Label and surface fade together toward the gray ladder (`#686868` text); accent pills lose saturation but stay blue — never turn gray entirely, preserving the action's identity. |

Characterizations in that table such as “The tone mirrors hero copy”, “no illustration noise”, “no shimmer gradients on a shadowless system”, “geometry never changes”, “never brand `#eb1000`”, “no stack traces”, “no Something went wrong alone”, “sentence-case past-tense copy”, and “preserving the action's identity” are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source never records `focus-visible` or a generic Focus capture. The `focus-visible` row does not carry a colour. A later generic Focus observation would not be `focus-visible` evidence.

Category Tile and Promo Flag have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Glass Quick-Link Card keeps Kind: interactive from source §4 quick-link and §5 entry-point dock evidence. App switcher is a separate grid-trigger control.

### Spectrum 2 Accent (Buy now)

- Role: primary purchase CTA on commerce surfaces (Creative Cloud plans)
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#3b63fb`
- Text: `#ffffff`
- Border: 2px solid `#3b63fb`
- Radius: 16px
- Padding: 3px 16px 4px
- Height: 30px
- Font: 15px / 700 / Adobe Clean
- Use: Buy now — Spectrum 2 accent CTA on commerce surfaces
- Observed: default only
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. `#3b63fb` is this control’s fill and Accent Blue. 15px is this control’s harvested font, not YAML `typography.button` 14px. 16px radius at 30px height is Spectrum 2 full-round, not a 75px marketing pill.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the Spectrum 2 Buy-now CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A purchase CTA can be unavailable; visual treatment omitted. Source §14 Disabled says accent pills lose saturation but stay blue; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as Buy now on commerce surfaces; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Inverse Pill (Free trial)

- Role: hero CTAs on dark imagery
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#000000`
- Border: 1px solid `#ffffff`
- Radius: 75px
- Padding: 0px 24px (YAML `0 24px`)
- Height: 40px
- Font: 14px / 700 / Adobe Clean
- Use: Free trial pill on dark hero imagery (“Free trial”, “Create with Firefly”); header “Sign in” uses the same white pill at 999px radius with 6px 16px 8px padding
- Observed: default only
- Field note: The following unmerged-variant reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. 75px is this control’s harvested radius. Header Sign-in 999px is a recorded variant radius on the same white-pill recipe, not a second component and not YAML `rounded.full` applied to the hero pill.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the inverse free-trial pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A free-trial CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a hero free-trial / Create-with-Firefly pill; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Ghost Pill (on dark)

- Role: secondary CTA over imagery
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#ffffff`
- Border: 1px solid `#ffffff`
- Radius: 75px
- Padding: 0px 24px
- Height: 40px
- Font: 14px / 700 / Adobe Clean
- Use: Secondary CTA over imagery (“See all products”)
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named ghost pill on dark imagery |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary imagery CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as “See all products”; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Outline Secondary (Sign in, commerce)

- Role: secondary action on light commerce chrome
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#292929`
- Border: 2px solid `#dadada`
- Radius: 16px
- Padding: 6px 16px 8px
- Height: 32px
- Font: 14px / 700 / Adobe Clean
- Use: Sign in — secondary action on light chrome
- Observed: default only
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. `#292929` is this control’s label and Strong Label, not Ink `#131313` and not marketing Black `#000000`. Transparent fill is this control’s field, not Canvas.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the commerce Sign-in outline |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A Sign-in action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as Sign in on light commerce chrome; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Audience Tab (Pricing)

- Role: Individuals / Business / Students & teachers switcher on the plans page
- Kind: interactive
- Type: tab
- Anatomy: label
- Active: text `#131313` + background `#f8f8f8`
- Inactive: text `#686868` on transparent
- YAML `disabled`: `#686868` label
- Radius: 4px 4px 0px 0px
- Padding: 24px 32px 18px
- Font: 18px / 600 / Adobe Clean
- Use: Individuals / Business audience switcher on pricing — folder-tab metaphor, active tab fuses with the panel below
- Observed: static active and static inactive appearances. The active appearance is a captured variant, not an observed click transition.
- Field note: Treating “folder-tab metaphor” / “active tab fuses with the panel below”, and treating the static active appearance as a captured variant rather than an observed click transition, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. `#131313` is this tab’s active label and Ink. `#f8f8f8` is this tab’s active fill and Surface. `#686868` is this tab’s inactive/YAML-disabled label and Muted. YAML `disabled` is that field’s recorded appearance, not a complete disabled treatment.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Static inactive appearance captured on pricing audience tabs |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An audience tab can be unavailable; visual treatment omitted. YAML records `#686868` as a disabled/inactive label; that field is not copied here as a complete disabled paint |
| loading | not-applicable | An audience tab selects a grouping (Individuals / Business / Students & teachers); the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`#131313` text on `#f8f8f8`). That appearance is a captured variant, not an observed click transition.

### Category Tile

- Role: product category tile on homepage
- Type: card
- Anatomy: surface
- Background: `#f3f3f3`
- Text: `#000000`
- Radius: 16px
- Heading: Adobe Clean Display Black, 24px, 900, `#000000`
- Body: 16px / 400
- Text link: bold 14px / 700
- Shadow: none
- Border: none
- Use: Product category tile on homepage (“Creativity and design”, “Everything you need to make anything.”)
- Observed: default only
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. `#f3f3f3` is this tile’s fill and Surface Tile, not Surface `#f8f8f8` and not Canvas. The heading / body / text-link / no-shadow / no-border tuple is this tile’s field-role binding from source §9, not a substitute for the Type-role table.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Glass Quick-Link Card

- Role: translucent quick-link cards floating over the hero video
- Kind: interactive
- Type: card
- Anatomy: surface
- Background: `rgba(0, 0, 0, 0.44)`
- Text: `#ffffff`
- Radius: 8px
- Padding: 12px 12px 16px
- Font: 16px (source §9 construction prompt only; weight not stated)
- Shadow: `rgba(255, 255, 255, 0.11) 0px 0px 0px 1px inset`
- Use: Translucent quick-link cards floating over the hero video (Creativity and design / Content creation / PDF and document essentials); the inset white hairline replaces a border. Source §5 names the row a glass dock of entry points.
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 plus the §9-only 16px text size. Type: card is from source §4 Cards & Containers.
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. The inset ring is YAML `shadow.inset-hairline` and this card’s edge. `rgba(0, 0, 0, 0.44)` is this card’s fill, not Canvas and not a drop shadow.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the hero quick-link / entry-point dock |
| hover | applicable | Pointer-web entry point; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A quick-link can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a quick-link entry point; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Promo Flag (Best value)

- Role: yellow promotional flags on pricing cards
- Type: badge
- Anatomy: label
- Background: `#f5c700`
- Use: Yellow promotional flags on pricing cards (official Spectrum `yellow-400`)
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4. Type: badge is from source §4 Badges.
- Field note: The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. `#f5c700` is this flag’s fill and Promo Yellow, not a Spectrum 2 CTA.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Global Nav Link

- Role: global nav item on dark header
- Kind: interactive
- Type: listItem
- Anatomy: label
- Text: `#ffffff`
- Font: 14px / 700 / Adobe Clean
- Use: Global nav item on dark header
- Observed: default only
- Field note: Dark translucent header over hero imagery; commerce pages flip to light chrome: same 14px / 700 labels in `#292929`/`#131313` on white.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the dark-header global nav item |
| hover | applicable | Pointer-web nav item; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a global nav item; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### App switcher

- Role: grid trigger in the global nav
- Kind: interactive
- Radius: 5px
- Size: 32px square
- Use: “App switcher” grid trigger
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 Navigation. Type is not invented.
- Field note: The 5px radius / 32px square pair is this trigger’s geometry. It is not Global Nav Link and not the 32px Outline Sign-in height.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named as the nav grid trigger |
| hover | applicable | Pointer-web trigger; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A grid trigger can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as an app-switcher grid trigger; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Floating Assistant (“Ask a question”)

- Role: floating assistant button
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `rgba(250,250,250,0.85)`
- Radius: 24px
- Height: 48px
- Shadow: `rgba(0,0,0,0.06) 0px 4px 12px`
- Use: Floating assistant button (“Ask a question”) — one of the only shadows on the page
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4. YAML `tokens.shadow.subtle` is this shadow string.
- Field note: Treating “one of the only shadows on the page”, and treating this control’s fill `rgba(250,250,250,0.85)` as unmerged from Canvas `#ffffff`, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. This control’s fill is `rgba(250,250,250,0.85)`, not Canvas `#ffffff`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named floating assistant on the captured homepage |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An assistant button can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as “Ask a question”; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 24, xl 32, section 64. Body layout repeats 4px, 8px, 12px, 16px, 24px, 32px, 64px with a 4px/8px rhythm. Glass cards pad `12px 12px 16px`. Audience tabs pad `24px 32px 18px`. Marketing pills are 40px high with 24px horizontal padding. Buy-now is 30px high. Outline Sign-in is 32px high. Floating assistant is 48px high. App switcher is 32px square.

The “imagery is the layout”, “two densities”, “separation by tint, not line”, and folder-tab top-loading readings in this section are derived editorial implementation inferences from the verified surfaces; they are not Adobe-authored or a separately published UI specification.

Recorded layout:

- Full-bleed hero blocks: edge-to-edge video/imagery with headline + CTA overlaid bottom-left
- Homepage alternates full-bleed dark sections with white sections holding 16px-radius tile grids
- Pricing: centered single column, H1 at 36px, tabbed plan matrix beneath
- Quick-link card row floats over the hero — a glass dock of entry points (Creativity and design / Content creation / PDF and document essentials)
- Imagery is the layout: sections are sized by their artwork, not by text. Copy occupies a disciplined corner of each full-bleed canvas.
- Two densities: marketing surfaces are vast and cinematic; commerce surfaces (plans matrix) are dense, tabular, and 18px-readable.
- Separation by tint, not line: white → `#f3f3f3` → `#f8f8f8` shifts segment content; hairlines appear only inside components (outline buttons, dividers).
- Card internal padding `12px 12px 16px` (quick-links); tab padding `24px 32px 18px` — generous top-loading for the folder-tab metaphor

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <600px | Single column; hero type compresses; Spectrum mobile type scale (fonts step up ~1.25×: 14→17px, 16→19px per official tokens) |
| Tablet | 600-1024px | 2-up tile grids, condensed nav |
| Desktop | 1024px+ | Full-bleed heroes, multi-column tile and pricing grids |

Collapsing strategy recorded in the source: hero 80px Display Black steps down; weight 900 is maintained at all sizes; quick-link glass dock horizontal row → stacked/scrollable cards; pricing tabs horizontal scroll with edge arrows (“Scroll tabs to left” affordance is in the live DOM); plan cards multi-column matrix → stacked cards with persistent Buy-now pills. Image behavior: full-bleed video/imagery crops rather than letterboxes; copy stays anchored to its corner; tiles keep 16px radius at all sizes; glass cards maintain translucency over moving video on every breakpoint.

Touch-target record: marketing pills at 40px height with 24px horizontal padding; tabs carry oversized 24px top padding — comfortably tappable; Spectrum tokens define a dedicated mobile scale (larger type, larger hit areas) rather than scaling desktop down.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 30px / 32px / 40px / 48px / 16px figures as surface measurements in this packet rather than universal layout tokens, treating the collapsing-strategy and image-behavior rows (crops rather than letterboxes; copy stays anchored; tiles keep 16px; glass translucency over video) as recorded packet application rather than a complete responsive specification, and treating the touch-target record (including tabs spaced for touch) as a purpose reading of those measurements rather than a complete target-size specification, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-11)

The live strings below are source-stated. Treating the live-inspect attributions, the Focused quote’s first-party-principle-language-also-in-Experience marking, the HTML-comment-additional-H2 marking, and the harvested-control-string grouping as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

- “Create at the highest level.” — homepage hero H2 *(live inspect 2026-06-11)*
- “All the best models, all in one place.” — homepage Firefly hero H2 *(live inspect 2026-06-11)*
- “Everything you need to make anything.” — homepage section H2 *(live inspect 2026-06-11)*
- “Adobe empowers everyone, everywhere to imagine, create, and bring any digital experience to life” — about-adobe.html mission *(live 2026-06-11)*
- “Empowering everyone to create.” — about-adobe.html headline *(live 2026-06-11)*
- Control strings from harvested components and §4: “Buy now”; “Free trial”; “Create with Firefly”; “See all products”; “See terms.”; “Sign in”; “Ask a question”; audience tabs Individuals / Business / Students & teachers; “Best value”; “Adobe News”; “Plans and pricing…”; “Firefly”; “Creativity and design”
- Spectrum Focused quote (first-party principle language, also in Experience): “No unnecessary decoration or irrelevant content.”
- HTML-comment additional homepage H2: “Get work done. Faster.”

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list are a derived editorial implementation inference from the verified surfaces; they are not Adobe-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Adobe’s marketing voice as short, declarative, and aspirational — headline sentences of four to seven words that put the user’s creative output (not the software) in the subject position. It avoids feature-speak in heroes entirely, pushing specifics down into card copy. The register is read as confident without being technical, warm without being cute; punctuation is full sentences with periods, even in CTAs’ supporting text. Product names (Photoshop, Firefly, Express, Acrobat) do the concrete work while the surrounding language stays about possibility.

| Context | Tone |
|---|---|
| Hero headlines | Imperative or superlative-adjacent declarations. “Create at the highest level.” Period always included. |
| Product cards | Name + plain capability. “Firefly — Create and enhance images, videos…” |
| CTAs | Two-word imperatives: “Free trial”, “Buy now”, “Create with Firefly”, “See all products”. |
| Pricing/legal | Sober, complete sentences; “See terms.” links everywhere a price appears. |
| Spectrum docs | Peer-to-peer, principle-led, plain. “No unnecessary decoration or irrelevant content.” |
| About/corporate | Mission-forward, inclusive. “Empowering everyone to create.” |

**Forbidden register** (source): jargon-led heroes (“industry-leading AI-powered solutions” as a headline), exclamation marks, hedging (“we think you might like”), and tech-spec language on marketing surfaces.

Not promoting synthetic voice samples beyond the quoted first-party lines is a derived editorial implementation inference from the verified surfaces; it is not Adobe-authored or a separately published UI specification. No synthetic voice samples are promoted.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- loading, error, and success visual treatments on the harvested buttons, ghost pill, nav link, floating assistant, glass quick-link, and app switcher, and the omitted loading/error/success applicability fields on Spectrum 2 Accent, Inverse Pill, Ghost Pill, Outline Secondary, Global Nav Link, Glass Quick-Link Card, App switcher, and Floating Assistant
- interactive kind and state-applicability map for Category Tile and Promo Flag
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
