# Finda migration log

Source: `web/references/finda/DESIGN.md`
Destination: `docs/design-md-weight/migrated/finda/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/finda/provenance.md`
Date: 2026-08-26
Worker: opus5 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v10**
Gate: `node test-v2/tools/migrate-reference.mjs --brand finda --gate-only` — result recorded at the end of this file, with the reason it is not conformance evidence.

## A5 — brand-published strings

The catalog `name` is the Latin `Finda`, so the H1 is `# Finda Design System` and the Latin form is the one the body uses in running prose, exactly as the source does. The Korean company name `핀다` is carried byte-for-byte into Experience → Scope at first mention (`Finda (핀다)`), beside the Latin form rather than replaced by it.

Every Korean run the source publishes is present in the portable body byte-for-byte. Counts are occurrences, measured per string with `grep -oF '<string>' DESIGN.md | wc -l` — not `grep -c`, which would report matching lines. Substring overlaps are noted where they inflate a count.

| String | Body count | Note |
|---|---:|---|
| `핀다` | 7 | 3 standalone + 4 inside `핀다소개` |
| `금융 선택의 기준을 바꾸다` | 3 | |
| `대출 비교부터 신청까지` | 3 | |
| `비교부터 신청까지` | 4 | 1 standalone + 3 inside `대출 비교부터 신청까지` |
| `1분만에 국내 최다 금융사 대출 비교` | 4 | 3 standalone + 1 inside the full document title |
| `대출비교플랫폼` | 1 | inside the full document title |
| `대출 비교` | 8 | 5 standalone + 3 inside `대출 비교부터 신청까지` |
| `대출이자` | 6 | |
| `연봉 실수령` | 7 | |
| `DSR` | 8 | |
| `전월세 비교` | 6 | |
| `앱 다운받기` | 8 | |
| `더 보러가기` | 5 | |
| `악성 앱 차단` | 2 | was 3 until the 2026-08-26 revision below removed the Scope ¶3 occurrence |
| `핀다소개` | 4 | |
| `회사소개` | 4 | |
| `언론보도` | 4 | |
| `서비스` | 4 | |
| `이용약관` | 3 | |
| `개인정보처리방침` | 3 | |
| `제휴 금융기관` | 3 | |
| `오류가 발생했습니다` | 2 | state contract row + Content & Locales |
| `필수` | 2 | state contract row + Content & Locales |
| `이혜민` | 1 | Scope, beside `(Lee Hye-min, CEO)` |

The one English rendering the source supplies alongside a published line — "We change the standard of financial choice" for `금융 선택의 기준을 바꾸다` — is placed **beside** the Korean and labelled as a reading aid. No published string is replaced by a translation, and no Korean label is normalised to an English paraphrase.

One Korean run in the source is **not** carried: `대출 갈아타기`, which appears only inside the third fictional persona's sentence and is deleted with that persona. It is not a published label; the source never records it on the captured page. Recorded as a deletion here and in `provenance.md` → Deletions.

Latin-side check (the gate's `copy-loss` sees non-Latin runs only, so this was done by hand). Every quoted Latin string in the source was extracted and searched across the three outputs. The strings that do not survive verbatim are the token-record `use:` descriptions, the §9 prompt fragments, and five evaluative phrases; each was checked individually — the `use:` content lands in a Role, Use, or component line, the §9 fragments are audited row by row below, and the five evaluative phrases are recorded as deletions with a grep count. The one quoted Latin phrase the source itself supplies as brand meaning, `"the action"`, is carried verbatim in Scope and in Capture-bound application. No brand-published Latin label was lost.

### Multi-destination published strings (E2a)

The section rows below name each legacy section's disposition, which is the section's own destination. A published string that lands in more than one portable location needs every location recorded, so they are measured here with `grep -n` and given as line numbers in `DESIGN.md`.

| String | Portable locations |
|---|---|
| `금융 선택의 기준을 바꾸다` | Scope ¶3 (13), Brand-published lines (420), Voice reading table (449) |
| `대출 비교부터 신청까지` | Scope ¶3 (13), Evidence-domain boundary (110), Brand-published lines (421) |
| `1분만에 국내 최다 금융사 대출 비교` | Scope ¶3 (13), Evidence-domain boundary (110), Brand-published lines (422), and inside the full document title (424) |
| `대출이자`, `전월세 비교` | Scope ¶3 (13), Primary tasks (24), Evidence-domain boundary (110), Dark Tool Chip Labels (263), label list (430), Voice reading table (450) |
| `연봉 실수령`, `DSR` | the same six locations, plus Principles item 2 (54) |
| `앱 다운받기` | Scope ¶4 (15), Primary tasks (23), Evidence-domain boundary (112), App-Download CTA heading (229) and its Label field (239), label list (431), CTA tone row (451), Named gaps (506) |
| `더 보러가기` | Primary tasks (25), More Link Pill heading (278) and its Label field (289), label list (431), CTA tone row (451) |
| `악성 앱 차단` | label list (434), Voice reading table (453) — both in Content & Locales, both carrying the source's tone-table classification. Scope ¶3 (13) was a third location until the 2026-08-26 revision below. |
| `핀다소개`, `회사소개`, `언론보도`, `서비스` | Primary tasks (26), Audience (34), Top Navigation Link Labels (310), label list (432) |
| `이용약관`, `개인정보처리방침`, `제휴 금융기관` | Primary tasks (27), Footer Link Labels (332), label list (433) |
| `오류가 발생했습니다`, `필수` | state contract rows (223 / 224), Content & Locales (437) |
| `핀다` (standalone) | Scope ¶1 (9 — twice: `Finda (핀다)` and "The Korean name 핀다"), the full document title (424). The other four occurrences are inside the nav label `핀다소개` and are counted on that row. |

Two cross-section moves are inside that table and are named here so the disposition is not read off the section rows alone:

- `전월세 비교` reaches the portable **Dark Tool Chip** `Labels` field from §10, not from §4: §4's chip `use` names only `대출이자`, `연봉 실수령`, `DSR`, while §10's tone table names the fourth. The union of the two source sections is the four labels the component record carries; nothing came from the sibling, which names six.
- `오류가 발생했습니다` and `필수` reach **Content & Locales** from §14, where the source states them, and the portable body says so at line 437. Since the 2026-08-26 revision below they are two of **three** published strings in this contract that the source does not place on the captured page; the third is `악성 앱 차단`, which the source records only in its §10 tone table. The portable body separates all three from the captured-page labels — the list intro at 428 announces the split, the string sits at 434 with its tone-table classification, and 437 covers the two state-contract strings.

## Section-by-section disposition

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity `id` / `name` / `country` / `category` | 분리 → provenance Identity | Portable file has no frontmatter. `name` `Finda` is dual (E2a): the ledger + the H1 and the portable prose. `핀다` is dual: the ledger + Scope and Content & Locales. |
| YAML `homepage` `https://finda.co.kr` | 옮김 → Experience Scope + Foundations Evidence-domain boundary; 분리 → provenance Identity / Surfaces / Sources | Dual-destination (E2a). Scope names the inspected route as the full URL; the boundary section names the site. |
| YAML `primary_color` `#4e2eed` | 옮김 → Experience Distinctive traits, Principles item 3, Capture-bound application, Foundations Semantic color, App-Download CTA, Top Navigation Link (active line + named-state note); 분리 → provenance Identity | Multi-destination, grep-verified at 7 hex occurrences in the portable body, one per location: lines 40, 55, 65, 91, 234, 309, 323 (E2a). Experience → Avoid carries the colour by role, not as the hex, so it is not a destination for the value. |
| YAML `logo` (`type: favicon`, Google s2 favicon slug) | 옮김 → Typography & Assets Assets; 분리 → provenance Identity | Dual (E2a). The same URL appears in both; the portable line records it as a third-party favicon proxy rather than a captured first-party mark, on the sibling's own terms. No Named-gaps row was invented for a first-party logo-file absence. |
| YAML `verified` / `added` `2026-06-09`, `omd: "0.1"` | 분리 → provenance Identity / Freshness | `omd: "0.1"` is ledger-only. The date `2026-06-09` is dual (E2a): the ledger + portable Scope, Evidence-domain boundary, Font evidence, Capture record, and Brand-published lines, all of which date the inspection. |
| YAML `tokens.source: live-extract`, `tokens.extracted: 2026-06-09`, `components_harvested: true` | 분리 → provenance Identity + Freshness + Proof notes | Ledger fields kept as values (A1c). Grep-verified: `live-extract` reaches provenance only, 0 hits in the portable body. `components_harvested` is dual (E2a): Identity + Proof notes; the portable Capture record states the same fact as prose ("harvested the eight components below"). |
| YAML `tokens.note` | 옮김 → Foundations Semantic color; 원문 분리 → provenance Identity | Dual (E2a). The note's four operative facts — the CTA violet as primary, the secondary violet on tinted surfaces, near-black navy headings, and the dark chip background — are the Primary / Violet Alt / Ink Navy / Dark Chip bullets. The ledger quotes the note in full. |
| YAML `tokens.colors` (14 keys) | 옮김 → Foundations Semantic color | All fourteen hexes preserved at field granularity, each as its own named role. `on-primary` `#ffffff` keeps a separate bullet from `canvas` `#ffffff` rather than being merged (A4), and `ink-pure` `#000000` keeps a separate bullet from `ink` `#010a26`, as do `muted` `#65798e` and `muted-alt` `#737a94`. |
| YAML `tokens.typography.family` (display / body) | 옮김 → Typography & Assets Family | Two keys, two bullets: `SUIT` (with `SUIT Fallback`) and `Pretendard` (with `Pretendard Fallback`), the fallback names taken from §3. |
| YAML `tokens.typography` 7 roles + §3 Hierarchy table | 옮김 → Typography & Assets Type roles | Seven rows: display-hero 64/800/1.31/-0.96, section 34/800/1.41/-0.51, subsection 28/800/1.43/-0.42, nav 14/400/1.50, button 14/400/1.50, button-sm 12/400/1.50, body 14/400/1.50. Unitless ratios stay ratios (A1a) — `1.31`, `1.41`, `1.43`, `1.50` all present — with the source's px equivalents (84px, 48px, 40px, 21px, 18px) kept beside them, and the rem equivalents (4.00rem, 2.13rem, 1.75rem, 0.88rem, 0.75rem) kept in the size column. The Notes column is §3's own Notes column carried across; each YAML `use:` string lands split the way the source itself splits it — the role half in Notes (`Hero headline, ExtraBold`, `Section titles`, `Card / feature heads`, `Top navigation items`, `Dark pill chip labels`, `App-download CTA label`, `Standard reading text`) and the family half in the Font column (`SUIT` ×6, `Pretendard`). No fact inside a `use:` string is dropped: where the source writes `ExtraBold` inside one, that row's Weight column carries 800. |
| YAML `tokens.spacing` (8 keys) | 옮김 → Foundations Spacing + Layout & Platforms | Each key named with its value: `xs` 4px, `sm` 8px, `md` 12px, `base` 16px, `lg` 20px, `xl` 29px, `xxl` 48px, `section` 64px. Layout repeats the scale as the page rhythm. Dual (E2a). |
| YAML `tokens.rounded` (4 keys) | 옮김 → Foundations Shape + Layout & Platforms Radius scale | Each key named: `sm` 8px, `md` 16px, `lg` 60px, `full` 9999px, with the CTA's 100px recorded alongside `full` because §5 writes the full step as "100px / 9999px". Dual (E2a): Foundations Shape and the Layout radius list carry the same four steps. |
| YAML `tokens.shadow.none` | 옮김 → Foundations Elevation | `none` is the Flat (Level 0) treatment, stated with the live `box-shadow: none` observation across hero, nav, headings, and chips. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | Verified `type:` values preserved per component as an explicit `Type:` line — button ×3, tab ×1, card ×2, badge ×1, listItem ×1 — rather than flattened into `Kind: interactive` (A1b). Every bg / fg / radius / padding / height / font / use / active field lands on its component. |
| §1 Visual Theme & Atmosphere — narrative and scope | 옮김 → Experience Scope | The Korean loan-comparison platform, finda.co.kr, and the one-route scope boundary. The source's evaluative framing of the company — "Korea's design-forward loan-comparison fintech" — is not carried; the factual half is. Grep-verified: `design-forward` 0 hits in the portable body. |
| §1 — "calm, editorial financial product rather than a hard-sell lending site", "airy, breathable zones", "premium, trustworthy weight without harshness", "the eye is trained to treat that one color as 'the action'", "the core tension of the system", "restraint with depth", "flat, modern, mobile-first", "financial tooling that doesn't look intimidating" | 옮김 → Experience Scope ¶5, under an adjacent complete qualification | These are the source author's evaluative readings. The adjacent sentence reads "a derived editorial implementation inference from the verified surface; it is not Finda-authored or a separately published UI specification" (B2, B2a). |
| §1 Key Characteristics (8 bullets) | 옮김 → Experience Distinctive traits | Token facts kept as observations. The three evaluative halves — "bold, declarative Korean-premium voice", "quiet, dense, hangul-optimized" with the de-facto-Korean-product-font claim, and "warm, trustworthy" — are kept as the source wrote them and carry an adjacent complete qualification below the list, with the families, sizes, weights, tracking, and colour roles named as the measured parts (B2a). |
| §2 Color Palette & Roles (Primary / Neutral & Surface / Text Hierarchy) | 옮김 → Foundations Semantic color | All three groups, all fourteen roles. §2's "Pure Black … Occasional maximum-contrast heading text (e.g. hero H1, feature H3)" is preserved as its own role and feeds the heading-colour conflict row below. Two role descriptions were **reduced to their factual half** rather than carried as unqualified readings (B2a): Ink Navy's "carries warmth and financial-grade trust" — the same reading is carried and qualified in Experience → Distinctive traits at line 47 as "warm, trustworthy" — and Hairline's "the primary separation device given the shadow-free system", whose operative half is stated in Foundations → Elevation ("Depth and grouping come from flat tinted surfaces and thin hairlines instead"). No value or role name was lost. |
| §3 Font Family | 옮김 → Typography & Assets Font evidence + Family | Five evidence classes kept distinct: official product-use, live computed surface-use, declared fallback, official distributed asset, ownership boundary. Neither family is asserted as Finda-owned or Finda-licensed, because the source establishes no such thing and records no license text. |
| §3 Principles (4 items) | 옮김 → Experience Capture-bound application + Typography & Assets + Content & Locales Locale | "Bold display, light body" and "Two fonts, two jobs" land in Typography → Family under an adjacent complete qualification; "Tight tracking scales with size" lands in Type roles with the three tracking values named as measured and the compression reading qualified; "Hangul-first sizing" lands in Content & Locales → Locale with the 14px role named as measured and the legibility reading qualified (B2a). |
| §4 Component Stylings (8 components) | 옮김 → Components & States | All eight: App-Download CTA 앱 다운받기, Dark Tool Chip, More Link Pill 더 보러가기, Top Navigation Link, Footer Link, Tinted Surface Card, White Feature Card, Violet Highlight Pill. Token record and §4 body are one-to-one — eight and eight. Nav header height 56px, header background `#ffffff`, and the active violet land on the Top Navigation Link; footer link colour and font land on the Footer Link. |
| §4 Dark Tool Chip / More Link Pill foreground | 옮김 → Components & States as recorded, with the evidence split stated | Both records pair `bg: #15161b` with `fg: #010a26`, identically in the token record and the §4 body. The values are preserved as written. The portable Dark Tool Chip record states that the source's observation note for the chips carries background, radius, and padding and no text colour, so the `#010a26` comes from the component record rather than from that note. Also recorded in provenance → Source-side gaps inherited. |
| §4 footer **Verified** / **Tier 1 sources** | 분리 → provenance Freshness / Tier 1 / Sources | The footer string is quoted verbatim in provenance. `blog.finda.co.kr` is multi-destination (E2a): the ledger's Surfaces/Sources/Tier 1 rows + **two** portable locations — Experience → Scope (line 15), which places it outside the contract's scope, and Governance → Named gaps (line 507), which names it as a source with no measured value attached. Measured: `grep -oF 'blog.finda.co.kr' DESIGN.md \| wc -l` = 2, at those two lines. |
| §5 Layout Principles (spacing, grid, whitespace, radius) | 옮김 → Layout & Platforms + Foundations Spacing / Shape | Base unit and scale, the centred single-column hero anchored by the 64px headline, the horizontal chip row, the alternating white/tinted full-width bands, and the 16px card grouping. The four radius steps land in both Foundations → Shape and the Layout radius list. The whitespace account carries an adjacent complete qualification; the 29px chip padding, the 60px and 16px radii, and the two band colours are named as the measured parts (B2a). |
| §6 Depth & Elevation (3 levels + philosophy) | 옮김 → Foundations Elevation | All three levels — Flat / Tint `#f5f6fa` / Hairline `1px solid #dedede`. The live `box-shadow: none` observation is kept as observation. The "deliberate modern-flat choice" paragraph, including the legacy "card stack" contrast, is kept under an adjacent complete qualification (B2a). |
| §7 Do's (8 items) | 옮김 → Experience Capture-bound application | Kept as application rules under an adjacent complete qualification on the list head, since they are the source author's prescriptions read off the captured surface rather than published doctrine (B2, B2a). |
| §7 Don'ts (8 items) | 옮김 → Experience Avoid | All eight boundary prohibitions, under an adjacent complete qualification on the list head, for the same reason as the Do's. |
| §8 Responsive Behavior (breakpoints, touch targets, collapsing, image behavior) | 옮김 → Layout & Platforms | The full breakpoint table (640px, 1024px, 1366px), all three touch-target heights (48px, 33px, 56px), all four collapsing rules, and both image-behavior rules. The inspection covers one desktop route and carries no breakpoint measurement, so the table, the collapsing strategy, the image behavior, and the tap-comfort reading carry an adjacent complete qualification, and the three heights are named as desktop measurements rather than accessibility-target rules (B2a). |
| §9 Quick Color Reference (10 lines) | 삭제 | Ten colour restatements, each already a named Foundations role. Grep-verified in the portable body: `#4e2eed` `#5d4cf2` `#ffffff` `#f5f6fa` `#010a26` `#3a415a` `#65798e` `#a9b0c9` `#15161b` `#dedede` all present in Foundations → Semantic color. Nothing unique to §9 in this block. |
| §9 Example Component Prompts (4) + Iteration Guide (7) | 삭제, 세 값만 옮김 → Components & States + Typography & Assets (A3) | Tool-facing packaging. Every value they restate lands elsewhere, grep-verified: 64px/800/1.31/-0.96px, chip `#15161b` + `#010a26` + 60px + 14px 29px, CTA `#4e2eed` + 100px + 12px, card `#ffffff` + `1px solid #dedede` + 16px + 28px/800/-0.42px, tinted band `#f5f6fa`, nav 56px + 14px + `#010a26` + active `#4e2eed` — all present in Foundations, Type roles, or the component records. Three facts existed **only** in §9 and were therefore moved rather than deleted: (a) the app-download CTA is **right-aligned** in the header — `grep -oF 'right-aligned' web/references/finda/DESIGN.md \| wc -l` = 1, at §9 line 293 — now on the App-Download CTA record; (b) the feature card's **body text `#3a415a`**, scoped to the card only at §9 line 291, now a `Body text inside the card` field on the White Feature Card (A4: it is that component's renderable field, not the general Body Slate role, which is also kept); (c) the **34px section-title colour `#010a26`**, given only at §9 line 292 since the §3 Hierarchy row carries no colour, now in the Type roles Notes column. The Iteration Guide's seven steps restate §7 Do's and add nothing unique. |
| §10 Voice & Tone — three verbatim voice samples | 옮김 → Content & Locales Brand-published lines | `금융 선택의 기준을 바꾸다`, `대출 비교부터 신청까지`, `1분만에 국내 최다 금융사 대출 비교` byte-exact, under the "marked verbatim from the live homepage, 2026-06-09" heading the source's own *(verified live 2026-06-09)* markers establish (A5). The source's role notes beside them — mission-framed, end-to-end promise, speed + scope claim — are its editorial reading and carry an adjacent complete qualification after the list, with the published strings and their live markers named as the measured parts (B2a). |
| §10 Voice & Tone — characterisation + 5-row context table | 옮김 → Content & Locales Voice reading, under an adjacent complete qualification | The "clear, reassuring, and empowering" characterisation and all five context rows are the source author's editorial reading (B2, B2a). Every Korean label inside the table is carried byte-exact. |
| §10 Forbidden register | 옮김 → Content & Locales Forbidden register | All four prohibitions. Explicitly labelled an authoring rule for this style, not a Finda-published policy, and stated to assert nothing about the company's products, lending practice, marketing conduct, or compliance position — the financial-domain separation this reference requires. |
| §11 Brand Narrative | 옮김 → Experience Scope | The 2015 founding, 이혜민 (Lee Hye-min, CEO), and the loan-comparison positioning, carried with the source's own evidence-class statement that the founding details are general public knowledge rather than a quotation from a verified Finda statement. The founding account and the user's-advocate reading carry an adjacent complete qualification and an explicit clause that they are not a finding about any named institution or about the Korean lending market (B2a). **삭제:** the superlative "Korea's leading 대출 비교 (loan-comparison) platform" and the English restatement "compare offers from the country's largest set of financial institutions in about a minute", which paraphrases Finda's own published page-title claim without its marker — the published Korean line is carried instead. Also deleted: the evaluative contrast "no institutional navy-and-gold" and "the dark-pattern urgency of predatory lending marketing", whose operative halves survive in Foundations → Elevation and Content & Locales → Forbidden register. Grep-verified in the portable body: `leading` 0, `largest set` 0, `about a minute` 0, `navy-and-gold` 0, `dark-pattern` 0, `predatory` 0. |
| §12 Principles (5 numbered items with UI implications) | 옮김 → Experience Principles, under an adjacent complete qualification | All five kept with their UI implications, using the approved wording (B2a). Item 1 carries an additional clause stating it is not a claim about how Finda actually ranks, sources, or discloses loan offers, and asserts nothing about lending practice or regulatory position. |
| §13 Personas | 삭제; 경계 문장만 옮김 → Experience Audience | The source itself marks the three archetypes as fictional. Portable Audience keeps that boundary; the three names, ages, cities, and biographies are deleted and are not re-hosted in provenance (D2). The segment labels quoted inside the source's own persona disclaimer are not re-hosted as an audience finding either. Grep-verified in the portable body: `김지원` 0, `박서준` 0, `이수진` 0, `서울` 0, `경기` 0, `부산` 0, `대출 갈아타기` 0. In `provenance.md` and in this log each string occurs only inside the deletion record that names it (1, 1, 1, 1, 1, 1, and 3 occurrences respectively in this file), never as a persona fact. Primary tasks (`count=5`) come from §1/§4/§10 surface evidence, not from §13. |
| §14 States (9 rows) | 옮김 → Components & States Source state contract + per-component applicability | 본문 보존: the full nine-row table survives verbatim, including `오류가 발생했습니다`, `필수`, `#010a26`, `#65798e`, `#f5f6fa`, `#a9b0c9`, and the 16px skeleton radius (A2, A5). No row carries a verification marker, so all nine carry an adjacent complete derived-editorial qualification plus a clause separating their financial scenarios from any claim about Finda's actual behaviour (B2a). Rows feed applicability where they name a control: Loading (calculator compute) → the Dark Tool Chip `loading` reason, which is why that row is `applicable` with a named treatment; Disabled → the `disabled` reason on all five declared maps, with the violet-fade clause cited on the App-Download CTA. `not captured` is never used as a `not-applicable` ground (C1); every `not-applicable` cell gives a role reason (C2); coverage is stated as not complete (C3); the two cards and the badge, which carry no interactive-kind evidence, declare no applicability map at all (C4). No graph delegation. |
| §15 Motion & Easing | 옮김 → Foundations Motion; 무출처 커브 3개만 삭제 → provenance 원장 (E2b) | The duration table (120ms / 200ms / 320ms) with its uses, the three easing token names with their uses, the motion rules, and the reduced-motion contract stay in the portable body. Only the three unattributed `cubic-bezier` strings are removed; the source's observation record contains no motion measurement. Grep-verified: `grep -oE 'cubic-bezier\([0-9. ,]+\)' DESIGN.md \| wc -l` = 0 in the portable body, 3 in the provenance omission ledger. Foundations → Motion states the promotion gate in full — transition properties, animation name, duration, easing, and reduced-motion behavior, observed per component, with the explicit note that a single named curve or duration is not that gate (B3). |
| HTML comment "OmD v0.1 Sources — Philosophy Layer (sections 10–15)" | mixed: 관측 원장 분리 → provenance; 한정 문장 옮김 → portable | The seven computed observations and the page-title line go to the provenance observation ledger. The comment's four evidence-class statements are dual (E2a): quoted in provenance and carried in the portable body — the verbatim voice-sample status in Content & Locales → Brand-published lines, the founding evidence class in Scope ¶1, the fictional-persona flag in Audience, and the editorial status of the interpretive claims in Principles and Foundations → Elevation. |
| Sibling `web/references/finda/.verification.md` (3,050 bytes) | 분리 → provenance Canonical proof; 값은 승격하지 않음 (E2) | **Adopted** as ledger evidence, not as portable authority. It supplies the full inspection method, the inspection date, the inspected URLs, 12 raw computed samples, a third brand-owned source the source `DESIGN.md` never names (the Korean App Store listing), and the Korean regional-source statement including the exclusion of the Google favicon from that requirement — which is why the portable Assets line records the catalog logo as a third-party proxy. Eight of its samples carry sibling-only values: the hero H1 computed `rgb(0, 0, 0)`, the section H2 computed `rgb(21, 22, 27)`, the chip's measured `14.48px 29px` padding, two extra chip labels (`내 집 대출한도`, `연말정산`), the text colour `rgb(68, 72, 87)`, both frequency scans, and the App Store URL. None reaches the portable body; each returns 0 from a literal grep of `DESIGN.md`. Live-DOM measurement and the source `DESIGN.md` reconstruction are separate evidence domains, so adoption stops at the ledger. |
| Heading-colour disagreement inside the source | 옮김 → Typography & Assets Type roles + Governance Named gaps; 분리 → provenance Freshness | Found during migration and not recorded by the source footer, which has no Conflicts line (`grep -o 'Conflicts' web/references/finda/DESIGN.md \| wc -l` = 0). §2 assigns `#000000` to maximum-contrast hero H1 and feature H3; the trailing comment and the §9 restatements give `#010a26` for the same two headings. Both values are recorded, neither is chosen. The sibling's H1 sample agrees with the `#000000` side; that agreement is recorded in provenance and is not used to close the conflict. |

## Named gaps check (D1a)

Every noun enumerated in Governance → Named gaps names a domain the source itself establishes, grep-verified against `web/references/finda/DESIGN.md`:

| Named gap | Source basis |
|---|---|
| exact cubic-bezier curves for the three easing roles | §15 Easings table names all three roles and carries the three curves |
| transition properties behind the recorded durations | §15 "all transitions collapse to instant"; "Pill chips respond to press with a subtle scale/opacity shift" |
| hover appearance of any control | §15 duration table, `motion-fast` use "Hover, chip press, focus" |
| heading text color | §2 "Pure Black (`#000000`) … e.g. hero H1, feature H3" versus the trailing comment's "Hero H1 … color rgb(1,10,38)" and §9's two `#010a26` restatements |
| error color behind the warm error tone | §14 Error (form validation): "Field-level message below the input in a warm error tone" — the tone is named, the value is not |
| placement behind `#737a94` | §2 Text Hierarchy: "Muted Alt (`#737a94`): Alternate muted slate for fine print" — role named, no captured element |
| placement behind `#f6f6f6` | §2 Neutral & Surface: "Surface Alt (`#f6f6f6`): A warmer secondary grey surface for alternating blocks" — role named, no captured element |
| an observed appearance for any of the nine state rows | §14 carries all nine as treatments; the trailing comment's observation list carries default appearances only |
| measured evidence behind the three breakpoints and the collapsing strategy | §8 Breakpoints table and Collapsing Strategy exist; the inspection covers one desktop route |
| values for the Finda app and the in-app application flow | §4 "Header app-download call-to-action"; §11 "apply directly in-app"; §14 "Success (application submitted)" |
| any value on `blog.finda.co.kr` | Source footer "**Tier 1 sources:** https://finda.co.kr, https://blog.finda.co.kr"; the trailing comment attributes token claims to the finda.co.kr inspection only |

No domain the source leaves unmentioned is listed, and no gap is written as a negative sentence about an unestablished domain. In particular, no authenticated-account, help-center, native-client, campaign, or parity domain is enumerated, because the source establishes none of them.

## C2 — state applicability by role

Every `not-applicable` cell in the portable body gives a semantic role reason, and no cell cites absence of observation. Five controls declare a map; three components declare none — eight in all, matching the eight harvested components. Measured: `grep -c '^| State | Applicability | Reason |' DESIGN.md` = 5 tables, `grep -c '^- Type:' DESIGN.md` = 8 components.

| Component | loading | error | success | Basis |
|---|---|---|---|---|
| App-Download CTA 앱 다운받기 | not-applicable | not-applicable | not-applicable | Hands off to the app download; no operation commits on the control |
| Dark Tool Chip | **applicable** | **applicable** | not-applicable | §14 puts calculator compute *inside* this control ("Inline progress within the tool chip; previous values stay visible"), so an operation does run in place and can fail in place; the outcome is a displayed value, not a confirmation |
| More Link Pill 더 보러가기 | not-applicable | not-applicable | not-applicable | Opens more of the same content; a destination role |
| Top Navigation Link | not-applicable | not-applicable | not-applicable | Tab meaning is selected versus resting |
| Footer Link | not-applicable | not-applicable | not-applicable | Link meaning is the destination |
| Tinted Surface Card / White Feature Card / Violet Highlight Pill | — | — | — | No map declared: containers and a heading badge with no interactive-kind evidence (C4) |

`default`, `hover`, `focus-visible`, and `disabled` are `applicable` on all five declared maps, with visual treatments omitted except where §14 supplies one. The `disabled` reasons cite the §14 Disabled row rather than a capture; no `focus-visible` row carries a colour value, and the source records no `focus-visible` observation (`grep -o 'focus-visible' web/references/finda/DESIGN.md | wc -l` = 0).

## E1 process-leak check

`node test-v2/tools/process-leak-check.mjs` → finda absent from `detail`, 0 hits. The body carries no clause id, wave number, ledger pointer, Tier grading, or sidecar reference; the only specification reference is `Core §4.4`, which is the approved idiom. Grep-verified at 0 hits in `DESIGN.md`: `Tier 1`, `Tier 2`, `provenance`, `live-extract`, and the three omitted curve strings.

## Final passes

**Pass 1 (B2a).** The body was re-read from the top and every causal, interpretive, or evaluative sentence was classified as brand-published fact, observation, or editorial reading. **Eight sentences failed that classification on the first read and were corrected before submission:**

| # | Location | Was | Now |
|---|---|---|---|
| 1 | Scope ¶3 | "악성 앱 차단 appears as a trust-and-security heading" | "…appears as a section heading" — **superseded by the 2026-08-26 revision below.** Pass 1 traded one classification the source does not make for another: "section heading" is the *sibling's* structural class. The revision removed the string from Scope ¶3 entirely; the trust/security classification is the source's own tone-table label and now carries the string in Content & Locales, in the label list (434) and in Voice reading (453). |
| 2 | Primary tasks | "Follow a 더 보러가기 more-link out of a content band into a fuller section" | "Follow a 더 보러가기 more-link pill chip to more of the same content" — the source's own noun, without invented page geography. |
| 3 | Primary tasks | "Reach a policy or partner page from the footer" | "Reach a footer destination" — the source records footer navigation, not page kinds. |
| 4 | Audience | "its hero leads with calculators and tools" | "a row of calculator and tool chips sits beneath its hero", which is what §5 records ("beneath the hero"). |
| 5 | Foundations → Semantic color | Ink Navy "…recorded as carrying warmth and financial-grade trust" | Reduced to the factual half. The reading is carried and qualified in Distinctive traits (line 47) as "warm, trustworthy". |
| 6 | Foundations → Semantic color | Hairline "…recorded as the primary separation device given the shadow-free system" | Reduced to the factual half. The operative half is stated in Foundations → Elevation. |
| 7 | Capture record | "no interaction event, no hover transition, and no focus or pressed treatment" | "no interaction event and no hover, press, or focus treatment" — the source's own three words (§15 `motion-fast` use "Hover, chip press, focus"); `pressed` is a term the source never writes. |
| 8 | Source state contract | "are the author's UI scenarios" | "are editorial scenarios written into this contract" — reads standalone without pointing at an off-document author. |

No token value, component-table value, or state applicability verdict was changed by any of the eight; all eight are sentence-level. Line numbering is unchanged, so the qualification map in `provenance.md` and the line references in this log still hold, and both were re-measured after the edits.

After the fixes, seventeen readings remain and each carries an adjacent complete qualification that closes the evidence class — `grep -o 'derived editorial' DESIGN.md | wc -l` = 17 and `grep -o 'not Finda-authored' DESIGN.md | wc -l` = 17, on 17 distinct lines. The 1:1 map of occurrence to portable location is the *Portable derived-editorial scope* table in `provenance.md`. The opposite error was checked too: measured values were not demoted by attaching a qualification to them. Each qualification paragraph names its measured parts separately — the families and metrics under Distinctive traits, the 29px padding under Spacing, the five tracking values under Type roles, the published strings and their live markers under Brand-published lines, the 14px role under Locale, the three heights under Touch targets. The colour roles, the type metrics, the component fields, the elevation levels, the spacing and radius scales, and the published Korean strings carry no qualification, because they are recorded values rather than readings.

**Pass 2 (E2 cross-check).** Every row above was written after grepping the three output files, not from memory. Dual and multi-destination values name both destinations. Counts are `grep -o … | wc -l` with the counted unit stated — occurrences, not lines — and substring overlaps are declared in the A5 table. Compliance claims are made only where the body carries the text: the B3 promotion gate is claimed because Foundations → Motion states all five evidence kinds plus the per-component gate and the "a single named curve or duration is not that gate" clause verbatim; the C3 statement is claimed because the Capture record says "State coverage is not complete here."

| Claim | Re-measured |
|---|---|
| 24 Korean runs, occurrence counts in the portable body | all 24 match the A5 table |
| `#4e2eed` 7 occurrences with the per-location breakdown | 7, at lines 40 / 55 / 65 / 91 / 234 / 309 / 323 |
| easing curve values in the portable body / in the provenance ledger | 0 / 3; the bare word `cubic-bezier` is 4 in the body — three omission labels and the Named-gaps line |
| `live-extract`, `Tier 1`, `Tier 2`, `provenance` in the portable body | 0 each |
| `design-forward`, `leading`, `largest set`, `about a minute`, `navy-and-gold`, `dark-pattern`, `predatory` in the portable body | 0 each |
| three persona names, three cities, `대출 갈아타기` | 0 in the portable body; in `provenance.md` and this log they occur only inside the deletion records that name them |
| `verification_v2`, `ds.type`, `Conflicts`, `focus-visible` in the source | 0 each |
| eight sibling-only values in the portable body | 0 each |
| `right-aligned` in the source | 1, at §9 line 293 — the A3 move is real |
| `derived editorial` / `not Finda-authored` in the body, after the eight pass-1 fixes | 17 / 17, on 17 distinct lines: 11, 17, 47, 51, 61, 74, 118, 137, 159, 181, 195, 215, 390, 411, 426, 443, 461 |
| D1 trigger phrases in the body (`not captured`, `were not`, `없었`, `않았다`, `미기록`) | 0 — no negative coverage sentence exists to introduce source-absent vocabulary |
| `not-applicable` cells citing non-observation | 0 — every one gives a role reason |
| coverage sentences in the body | 1, "State coverage is not complete here" (line 211) — negated, as required |
| `focus-visible` table rows carrying a hex | 0 |

**Pass 3 (F2 re-cross-check after the separate-session audit).** The audit edited three sentences in the portable body — Scope ¶3 (13) and Evidence-domain boundary (110) dropped the role note "promise", which the body itself classifies as derived editorial at line 426, and Distinctive traits (47) corrected where the three readings sit. Every destination list above was re-measured against the edited body afterwards, and none moved: the file is still 507 lines, the qualification phrases are still 17 / 17 on the same 17 lines, all 24 Korean occurrence counts in the A5 table still hold (`대출 비교부터 신청까지` 3, `1분만에 국내 최다 금융사 대출 비교` 4, `비교부터 신청까지` 4, `대출 비교` 8), `#4e2eed` is still 7 at the same seven lines, and `promise` now stands at 1 occurrence — the qualified one at line 426. The audit's own additions to this log are the *Multi-destination published strings* table, the corrected control count in the C2 section, and the two-location record for `blog.finda.co.kr`.

## Revision 2026-08-26 (wave24 — B1, sibling structural class)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v10**, clause **B1**. Trigger: one confirmed FAIL from the semantic review (grok-4.6). The separate-session auditor reported the same thing independently as out-of-scope item 1 in `audit-log.md`, and the orchestrator re-measured it before this revision.

**What was wrong.** The body stated `- Section heading 악성 앱 차단.` at line 434 and "…악성 앱 차단 appears as a section heading" inside Scope ¶3 at line 13. The string is the source's; the **classification is not**. The source records `악성 앱 차단` exactly once — `grep -oF '악성 앱 차단' web/references/finda/DESIGN.md | wc -l` = 1 — in its §10 tone table, as the *Trust / security copy* example, and attaches no element and no page position to it. The only record of it as a heading is the sibling's raw sample, `grep -oF '악성 앱 차단' web/references/finda/.verification.md | wc -l` = 1, which reads `h3 "악성 앱 차단": font-size: 28px; font-weight: 800; line-height: 40px; letter-spacing: -0.42px`. A structural observation made in the live-DOM domain had therefore been carried into the reconstruction of the source `DESIGN.md` as a fact about that document. That is B1 in its **class** form rather than its value form, which is why every sibling-only *value* grep still returned 0 and the gate still returned PASS. Pass 1 fix #1 above did not introduce the string but did pick the wrong repair: it replaced "trust-and-security heading" with "section heading", trading a classification the source does not make for another one.

**Treatment chosen: (a).** The string keeps the classification the source itself records, and the structural observation moves to the ledger with its evidence class named. (b) — dropping the string from the body and keeping only the provenance record — was available and would also have closed the FAIL, but it would have demoted a source-recorded value into a ledger-only one for the sake of a defect that lives entirely in the surrounding description. (a) fixes the description and leaves the value where the source put it.

| # | File | Line | Was | Now |
|---|---|---:|---|---|
| 1 | `DESIGN.md` | 13 | "A row of dark pill chips opens calculators and tools — 대출이자, 연봉 실수령, DSR, 전월세 비교 — and 악성 앱 차단 appears as a section heading." | "…— 대출이자, 연봉 실수령, DSR, 전월세 비교." The paragraph's subject is what the homepage publishes, and the source establishes no page position for this string, so the string leaves the paragraph rather than acquiring a hedge inside it. Nothing is lost: it is carried twice below, in the section the source's §10 maps to. |
| 2 | `DESIGN.md` | 428 | "Labels recorded on the captured page, as control, navigation, section, and footer names rather than as marked voice samples:" | "Labels recorded on the captured page as control, navigation, section, and footer names, then two strings the reviewed material records without a page position attached to them:" — the list already ended with one such exception (the narrative-quoted phrases at 435); the intro now says so, so the reclassified bullet does not sit under a claim that contradicts it. |
| 3 | `DESIGN.md` | 434 | "- Section heading 악성 앱 차단." | "- 악성 앱 차단, recorded once as the trust-and-security example in the tone table." The source's own classification, with no element and no position asserted. |
| 4 | `provenance.md` | Sibling-only values | — | New entry recording the sibling's binding of the string to an `h3` and its four measurements, stating that the four numbers corroborate the source's own Sub-section type role (28px / 800 / `1.43` = 40px / -0.42px) rather than adding to it, that what the sibling alone establishes is the heading binding, and that the binding stays in the ledger because promoting it would change the evidence class of the observation (B1). |
| 5 | `provenance.md` | after the sibling-only grep line | — | The grep line covers values; this entry is a class, so a separate measurement was added: the string occurs 2 in the portable body, 1 in the source, 1 in the sibling, and neither portable occurrence attaches an element or a position. |

**Ledger rows re-synced with this revision:** the A5 count row for `악성 앱 차단` (3 → 2, with the reason), its *Multi-destination published strings* row (Scope ¶3 destination removed; 434 and 453 kept, both described as carrying the source's tone-table classification), the cross-section note under that table (`오류가 발생했습니다` / `필수` are now two of **three** strings the source does not place on the captured page, and the body's three separating locations 428 / 434 / 437 are named), and Pass 1 fix #1, marked superseded with the reason.

**Re-measured after the edits (F2).** The body is still **507 lines**, so every line reference in `provenance.md` and in this log still resolves; each was re-checked against the file rather than assumed. All 24 A5 occurrence counts match the table, the one changed count included. `derived editorial` = 17 and `not Finda-authored` = 17, on the same 17 lines (11, 17, 47, 51, 61, 74, 118, 137, 159, 181, 195, 215, 390, 411, 426, 443, 461); the unnumbered closure at 457 still reads by anaphora. `#4e2eed` = 7 at 40 / 55 / 65 / 91 / 234 / 309 / 323. `promise` = 1, at 426. `[FILL IN]` = 0. The remaining "section heading" occurrences in the body (13, 110, 421) all describe `대출 비교부터 신청까지`, which the source's own §10 voice-sample list classifies that way.

**Not changed.** No token value, no component-table value, no state applicability verdict, no section structure, and neither source file (`web/references/finda/DESIGN.md` SHA-256 `85f5d95abb02b7b185d08d0e8fe3334b4ff6af5e84178f6c7beb272983b3b2b5`, sibling `c608646af8684cd2fde69b23b4dfba9882302f66945df71405a2856c0430ee5e`, both unmodified). None of the separate-session auditor's sixteen fixes was reverted. The heading-colour contradiction stays unresolved and unqualified — `#000000` at 78 / 94 / 197 / 500 against `#010a26`, neither side selected — because that two source statements disagree is a document fact. The three financial-domain boundary points stand untouched: Foundations → Evidence-domain boundary (108–112), the extra clause on Principles item 1 (51), and the editorial-scenarios clause on the §14 state contract (215).

**E3.** No gate false positive was encountered in this revision, and no notation was reshaped to avoid one.

Post-revision `DESIGN.md` SHA-256: `3e808aa516d1ea6663986557cb3865a401780aadc531220cae578ebc34ded26b`.

## Gate

`node test-v2/tools/migrate-reference.mjs --brand finda --gate-only` → **PASS**, `problems: []`.

Portable Core: `inspectDesignMd(...).conformance` from `scripts/design-md-core.cjs` → `portable_core: true`, `reasons: []`. Run separately from the gate, because the gate does not compare canonical claim bytes.

**The gate is not evidence of conformance for any of this.** D1a, C2, B2a, and E2 are sentence-level judgments the gate does not evaluate; the gate returns the same PASS whether the qualifications are adjacent and complete or absent. The 2026-08-26 revision above is a further case: its B1 defect was a **classification**, not a value, and the gate's B1 check is bound to table rows carrying values, so the gate returned the same PASS with the defect present and with it corrected. Both results below were re-run after the revision.
