# Mistral AI Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Mistral AI builds frontier models, developer tools, applications, and compute for people and organizations that need control over how they use AI. Catalog homepage identity is `https://mistral.ai`. Catalog `primary_color` is `#000000`. This contract covers two current first-party public surfaces inspected on 2026-07-13: public marketing home `https://mistral.ai/` (marketing) and public commercial pricing `https://mistral.ai/pricing/` (product-pricing). The captured home and pricing pages are not a product-app audit: they are separate public marketing and commercial-pricing surfaces. Official brand guidance at `https://mistral.ai/brand/` supplies a distinct asset layer. Color, type, and component values below stay attached to the surface that established them. Reading those two URLs as this contract's token surfaces, keeping catalog homepage identity as catalog identity rather than as an extra inspected URL, keeping the captured pages as public marketing and commercial-pricing rather than as a product-app audit, and attaching every value to the surface that established it, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

The official about page frames the company as a European, open and responsible alternative born in 2023, while its public site currently expresses that position with restrained, almost editorial web chrome: black text, pale near-white or warm-gray surfaces, fine `#e4e3de` rules, and a mix of proprietary-feeling display lettering with Inter for practical reading. Official brand guidance supplies a distinct asset layer—the pixel-cat emblem, a preferred gradient logo, monochrome variants, and colorful model illustrations—but its artwork is not promoted into the live UI palette without a computed source. The hex values, the family names, the pixel-cat emblem, the preferred gradient logo, the monochrome variants, and the colorful model illustrations are the source's record. Characterizing that captured chrome as restrained, almost editorial, and proprietary-feeling, and reading the artwork/UI split as an asset/UI boundary rather than as a missing color scale, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

Brand narrative recorded by the source, kept as narrative context. Mistral’s official account traces the company to April 2023, when its co-founders set out to build a European AI company around openness, transparency, cost efficiency, responsibility, and user control. Its present offer extends beyond models to developer tools, applications, and compute, with enterprise and government work positioned around ownership and production reliability. The public brand system is correspondingly more than a logo: the official guidance describes a pixel-cat emblem, controlled gradient and monochrome logo variants, and colorful small-scale model illustrations. Those assets explain the brand’s visual identity, while the token layer above remains limited to the current captured public pages. Official About, Brand, and Careers writing provide that narrative context; they do not by themselves supply interface tokens. The April 2023 founding, the co-founders’ European-company brief (openness, transparency, cost efficiency, responsibility, and user control), the present offer of models plus developer tools, applications, and compute, the enterprise and government positioning around ownership and production reliability, the pixel-cat / gradient / monochrome / small-scale illustration system, and that closing sentence are the source's own. Classifying that narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a surface or control the source records, and recording that they do not come from the source's stakeholder-group section, is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

- Read public-marketing headings, list rows, and body copy on `https://mistral.ai/`.
- Compare `Pricing-plan shell` treatments on `https://mistral.ai/pricing/`.
- Use `Header controls and compact links` on the captured public surfaces.
<!-- design-md:claim-end -->

### Audience

No named individuals appear. The source labels its groups as evidence-based groups, not fictional profiles, and records three groups at that level, kept as written: Enterprise and government teams — organizations in high-stakes industries that need tailored systems, control, ownership, and production reliability; Developers and product builders — people using Mistral models, developer tools, applications, and compute to build or customize AI systems; Research, product, and engineering collaborators — internal teams and candidates working under the stated expectations of audacity, rigor, customer centricity, speed, and low-ego ownership. Reading those source-named groups as this product's audience, and keeping the source's own "not fictional profiles" label rather than inventing biographies, is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

### Distinctive traits

The three traits below rest on the source's own labeled bullets. The values in them are recorded; the groupings and the readings inside them are a derived editorial implementation inference from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

- **Structured neutrality:** current captured chrome is led by black, soft off-whites, and fine neutral dividers.
- **Type as contrast:** `ALTMistral` supplies high-visibility headings and controls; Inter carries repeated reading and transactional copy.
- **Asset/UI boundary:** the official gradient logo and model artwork establish identity, but their colors are not claimed as current generic UI tokens.

### Principles

These 4 items are a derived editorial implementation inference from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance. The source states them in its own Principles section together with the UI implication it draws from each.

1. **Put control with the user.** *UI implication:* explain ownership, deployment, and capability boundaries concretely rather than hiding them in generic AI claims.
2. **Be rigorous and direct.** *UI implication:* favor structured labels, specific actions, and readable information hierarchy over ornamental copy.
3. **Make complex systems usable.** *UI implication:* keep commercial choices and quantities legible with clear rows, dividers, and restrained controls.
4. **Keep brand art distinct from interface evidence.** *UI implication:* use supplied gradient logos or illustrations only under official guidance; do not synthesize an orange semantic UI system from artwork.

### Application rules

The source states these as its Do list, kept as written, together with unique constraints from its Agent Prompt Guide that already have Experience / Foundations / Components slots. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

- Use the captured public-web neutrals and one-pixel `#e4e3de` dividers only in the documented public-web scope.
- Pair ALTMistral, Inter, and Space Mono only when the target has a legitimate right to load those exact families.
- Preserve selector and surface boundaries when borrowing a row, pricing-plan, tab-shell, or quantity-input treatment.
- Treat the official gradient logo and colorful model art as brand assets rather than unmeasured UI color tokens.
- For a Mistral-like public commercial page, begin with a quiet `#fbfbf8` or `#f5f4ef` surface, black content, thin `#e4e3de` dividers, Inter for repeated reading, and ALTMistral only where it can be legally loaded. Use square list and pricing-plan shells plus the observed 6px segmented container; do not recreate Mistral’s product, documentation, or brand artwork from this limited public-web reference.

### Avoid

The source states these as its Don't list, kept as written, together with the unique Agent Prompt Guide prohibition on recreating product, documentation, or brand artwork from this limited public-web reference, and with the independent-analysis exclusion. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

- Do not restore the earlier orange/golden palette, Arial, all-0px rule, or warm-shadow system without fresh Tier 1 evidence.
- Do not treat Source Sans 3, documentation chrome, or a system fallback as a Mistral UI family.
- Do not invent active tabs, hover colors, focus rings, menus, dialogs, errors, breakpoints, or additional price-plan variants.
- Do not use the captured font URLs as a downstream redistribution licence.
- Do not recreate Mistral’s product, documentation, or brand artwork from this limited public-web reference.
- Do not promote the independent getdesign.md analysis’s purple, DM Serif, JetBrains Mono, 8px-button, and 12px-card claims.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Role names follow the source's own token-set keys. Catalog `primary_color` `#000000` is YAML `tokens.colors.foreground`. Taking those role names from the source's own token-set keys, pairing each hex to the token-set path named beside it, keeping YAML `tokens.components.pricing-plan.fg` `#000000` off the §4 plan-shell text `oklch(0.21 0.006 285.885)`, keeping quantity-input border `1px solid oklch(0.21 0.006 285.885)` off YAML `tokens.colors.border` `#e4e3de`, keeping `tokens.colors.surface-brand-primary` `#fbfbf8` off `tokens.colors.surface-brand-secondary` `#f5f4ef`, treating official gradient / model-illustration colors as an identity source rather than as an inferred semantic or interaction palette, and keeping the source's statement that no success, warning, error, dark-mode, focus, or hover color token is established, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance. The hex values, the oklch writings, and the recorded uses are the source's own.

- **Foreground** (`#000000`): YAML `tokens.colors.foreground`. Repeated text and border color on the captured home and pricing records. Catalog `primary_color`.
- **Divider** (`#e4e3de`): YAML `tokens.colors.border`. Repeated one-pixel navigation, list-row, pricing-plan, and segmented-control border.
- **Brand-secondary surface** (`#f5f4ef`): YAML `tokens.colors.surface-brand-secondary`. Observed on pricing plan and segmented-shell treatments.
- **Brand-primary surface** (`#fbfbf8`): YAML `tokens.colors.surface-brand-primary`. Observed on a pricing-plan shell and an expanded header-control treatment.

The official brand page documents gradient, black, and white logo variants plus colorful model illustrations, but it does not publish the numeric artwork colors in the inspected text. Those brand assets remain an identity source, not an inferred semantic or interaction palette. No success, warning, error, dark-mode, focus, or hover color token is established by this capture.

YAML `tokens.components.pricing-plan.fg` `#000000` stays on that YAML field. Source §4 writes the primary-surface and secondary-surface plan-shell text, and the quantity-input text, as `oklch(0.21 0.006 285.885)`. Those two writings stay unmerged. Quantity-input bottom border is `1px solid oklch(0.21 0.006 285.885)`, not YAML `tokens.colors.border`.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept as the source wrote them): `tokens.spacing.xs: 2` · `tokens.spacing.sm: 4` · `tokens.spacing.md: 8` · `tokens.spacing.lg: 16` · `tokens.spacing.xl: 24`.

The supplied evidence recurs at 2px, 4px, 8px, 16px, and 24px. The highest-frequency values are 16px for rows and card/control spacing, 8px for gaps, and 4px for compact labels. This is an observed public-web rhythm, not a released spacing scale or responsive specification. `tokens.spacing.lg: 16` is not `tokens.typography.body.size` `16`, is not the `16px` in component font shorthands, and is not the `16px` in header-control padding `0px 16px`. `tokens.spacing.sm: 4` is not `tokens.rounded.sm: 4` and is not the disabled-compact padding `4px`. `tokens.spacing.md: 8` is not `tokens.rounded.lg: 8`. Keeping those unitless spacing steps on their own keys rather than rewriting them as a grid, keeping the px cluster as the source's observed rhythm rather than as a released specification, and keeping those writings of `2`, `4`, `8`, `16`, and `24` on their own records, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path): `sharp: 0` · `sm: 4` · `md: 6` · `lg: 8`.

- Sharp (`0` / `0px`): YAML `tokens.rounded.sharp`. Public header control, expandable/list row, pricing-plan shells, and quantity input record `0px`.
- sm (`4`): YAML `tokens.rounded.sm`. Kept as its own key; it is not `tokens.spacing.sm: 4` and is not the disabled-compact padding `4px`.
- md (`6` / `6px`): YAML `tokens.rounded.md`. Pricing navigation container radius `6px`; YAML disabled-compact-control radius `6px`.
- lg (`8`): YAML `tokens.rounded.lg`. Kept as its own key; it is not `tokens.spacing.md: 8`.

Keeping `0`, `4`, `6`, and `8` as four keys, keeping harvested `0px` / `6px` on the components that record them, and reading those steps as local harvested geometry rather than as a universal radius scale, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

### Elevation

The representative pricing plans, rows, tabs, and inputs in this bundle report `box-shadow: none`. No global shadow scale, modal elevation, overlay treatment, or animation layer is claimed. The `box-shadow: none` string and that no-global-scale sentence are the source's own. Reading those representatives as a flat treatment for the observed elements only, rather than as a depth scale for every Mistral surface, is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

### Motion

No computed transition duration, easing token, motion rule, or reduced-motion behavior is promoted from this evidence. Class names that include transition utilities are not a measured motion specification. The source names no duration token and no easing role. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation, and keeping the source's "no computed transition duration, easing token, motion rule, or reduced-motion behavior" sentence, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition. Until then the unresolved motion fields stay absent rather than carrying a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

Reading those evidence-class rows as the source's own resolution table rather than as a published Mistral type specimen, keeping Source Sans 3 declared-only, keeping documentation chrome outside this reference, and keeping the licence boundary from independently authorizing reuse, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

| Evidence class | Resolution |
|---|---|
| Live computed public-web use | `Inter` is loaded with high confidence, has 632 visible uses, and has a matching Mistral-hosted variable-font source. It is observed in body, list, card, input, tab, and button roles. YAML `tokens.typography.family.ui` `Inter`. |
| Live computed public-web use | `ALTMistral` is loaded with high confidence, has 560 visible uses and matching Mistral-hosted Regular, Medium, Semibold, and italic WOFF/WOFF2 sources. It appears in public-marketing display, badges, body, and header-control roles. YAML `tokens.typography.family.display` `ALTMistral`. |
| Live computed public-web use | `Space Mono` is loaded with high confidence, has 38 visible uses, and has a matching Mistral-hosted source. The capture confines it to small body/card-label contexts. YAML `tokens.typography.family.mono` `Space Mono`. |
| Declared-only | `Source Sans 3` has seven `@font-face` source URLs through the consent-provider domain but zero visible captured uses. It is not a typography token. |
| Documentation chrome | `https://docs.mistral.ai/` was consulted only as an official documentation domain. No supplied computed-style or FontFaceSet record supports promoting its chrome or a docs family into this reference. |
| Licence boundary | Mistral’s brand guidance says its name, trademarks, logos, icons, and identifying assets are its property; it does not publish a downstream reuse licence for the captured font files. Keep font metadata, but do not substitute another font and do not treat these URLs as reusable project assets. |

### Family

- **Current visible UI family:** `Inter` — YAML `tokens.typography.family.ui`
- **Current visible display family:** `ALTMistral` — YAML `tokens.typography.family.display`
- **Current visible mono family:** `Space Mono` — YAML `tokens.typography.family.mono`
- **Declared-only:** `Source Sans 3` — seven `@font-face` source URLs through the consent-provider domain; zero visible captured uses
- Loaded sources recorded in the packet: `https://mistral.ai/fonts/inter/Inter-VariableFont.ttf`, `https://mistral.ai/fonts/alt-mistral/ALTMistral-Regular.woff2`, `https://mistral.ai/fonts/space-mono/SpaceMono-Regular.ttf`

Do not replace unavailable or unobserved brand type with Inter, ALTMistral, or Space Mono. Do not present Source Sans 3, documentation chrome, or a system fallback as a Mistral UI family. Do not treat these URLs as a downstream redistribution licence. Reading Inter as the repeated reading family, ALTMistral as the display/control family, and Space Mono as the small-label family, rather than as one interchangeable stack, is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

### Type roles

YAML writes numeric sizes without a `px` suffix and unitless line-heights (`1.5`, `1.07`, `1.45`) where it records them. Source §3 writes the same roles with `px` line-heights and, for some rows, `normal`. Both writings stay. Unitless ratios stay ratios and are never converted to a replacement px. Pairing each YAML role to the token-set path named beside it, keeping YAML `use` strings verbatim, keeping the longer §3 hierarchy rows beside them, keeping the §3-only Marketing heading `44px` / `52px` / `-0.44px` and Compact link/label `14px` / `20px` / `0.28px` rows, keeping YAML `16` / `56` / `11` off spacing and radius steps, and keeping the two YAML size-16 type roles on their own paths rather than collapsing them, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

| Role | Family | Size | Weight | Line height | Tracking | Token-set use | Notes |
|---|---|---|---:|---|---|---|---|
| Large marketing heading | ALTMistral | YAML `56` / §3 `56px` | 500 | YAML `1.07` / §3 `60px` | YAML `-0.56` / §3 `-0.56px` | Observed large public-marketing headings | captured home body elements. Path `tokens.typography.display` |
| Marketing heading | ALTMistral | `44px` | 500 | `52px` | `-0.44px` | — | captured home body elements. Not a YAML type-role |
| Control label | ALTMistral | YAML `16` / §3 `16px` | 500 | YAML `1.5` / §3 `24px` | YAML `0.16` / §3 `0.16px` | Header controls and compact links | `home::[data-omd-capture="55"]`. Path `tokens.typography.control` |
| Compact link/label | ALTMistral | `14px` | 500 | `20px` | `0.28px` | — | `home::[data-omd-capture="126"]`. Not a YAML type-role |
| Body, list, and card copy | Inter | YAML `16` / §3 `16px` | 400 | YAML `1.5` / §3 `24px` | normal | Public-site body, list, card, and product-pricing copy | captured home and pricing elements. Path `tokens.typography.body` |
| Small label | Space Mono | YAML `11` / §3 `11px` | 400 | YAML `1.45` / §3 `16px` | normal | Small uppercase label on the public marketing surface | `home::p.text-eyebrow-small`. Path `tokens.typography.eyebrow` |

Token-set paths: `tokens.typography.display.size` · `tokens.typography.display.weight` · `tokens.typography.display.lineHeight` · `tokens.typography.display.tracking` · `tokens.typography.display.use` · `tokens.typography.control.size` · `tokens.typography.control.weight` · `tokens.typography.control.lineHeight` · `tokens.typography.control.tracking` · `tokens.typography.control.use` · `tokens.typography.body.size` · `tokens.typography.body.weight` · `tokens.typography.body.lineHeight` · `tokens.typography.body.use` · `tokens.typography.eyebrow.size` · `tokens.typography.eyebrow.weight` · `tokens.typography.eyebrow.lineHeight` · `tokens.typography.eyebrow.use`. `tokens.typography.body.size` `16` is not `tokens.typography.control.size` `16` and is not `tokens.spacing.lg: 16`. `tokens.typography.display.size` `56` is not a spacing step. `tokens.typography.eyebrow.size` `11` is not a spacing step. `tokens.typography.display.lineHeight` `1.07` is not a replacement for §3 `60px`. `tokens.typography.control.lineHeight` `1.5` is not a replacement for §3 `24px`. `tokens.typography.body.lineHeight` `1.5` is the YAML body ratio, not a replacement for §3 `24px`. `tokens.typography.eyebrow.lineHeight` `1.45` is not a replacement for §3 `16px`. YAML `tokens.typography.body.weight` `400` and `tokens.typography.control.weight` `500` and `tokens.typography.display.weight` `500` and `tokens.typography.eyebrow.weight` `400` stay on those keys. YAML `tokens.typography.control.tracking` `0.16` stays beside §3 `0.16px`. YAML `tokens.typography.display.tracking` `-0.56` stays beside §3 `-0.56px`. YAML `use` strings stay in the Token-set use column.

### Assets

- Catalog logo: type `simpleicons`, slug `mistralai`. That is catalog identity metadata, not a captured first-party mark file.
- Loaded font files recorded above are delivery evidence for Inter, ALTMistral, and Space Mono. They are not a licence grant for reuse.
- Official brand guidance documents the pixel-cat emblem, a preferred gradient logo, monochrome variants, and colorful model illustrations as brand assets. Their artwork colors are not claimed as current generic UI tokens.

Treating the simpleicons slug as catalog identity rather than as a Mistral-hosted mark file, treating the three font URLs as delivery evidence rather than as a reuse licence, and treating pixel-cat / gradient / monochrome / model-illustration artwork as brand assets rather than as UI color tokens, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted: The collector records `interactionCount: 0` and an empty `interactions[]`; class names mentioning hover do not establish a hover value. The one disabled row is a captured disabled DOM state, not a complete state or behavior specification. The supplied capture records one disabled compact control at `pricing::[data-omd-capture="75"]`, with `rgba(7, 7, 11, 0.1)` background, `oklch(0.552 0.016 285.938)` text, 6px radius, and 4px padding. It does not supply empty, loading, success, error, validation, toast, dialog, or skeleton states. Those state groups are intentionally omitted rather than inferred.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `focus-visible` treatment and no generic Focus capture; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. `Primitive type` is attached only when the source YAML records that type. Public header control, Expandable/list row, Secondary-surface plan shell, Pricing navigation container, and Pricing number input are not in the token set. Primary-surface plan shell, Secondary-surface plan shell, and Pricing navigation container omit kind and a state-applicability map because the source supplies container/shell observations, not interactive-kind evidence. This is not a complete state-coverage claim. Every kind verdict, every applicability verdict, the Primitive-type attachment rule, the kind-omission on the three shells, generic Focus not being `focus-visible` evidence, the disabled compact control's loading/error/success fields omitted at the unresolved-request boundary rather than closed from the captured state rows, and keeping the source state contract here while the catalog graph is not adopted, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

### Public header control

- Role: 48px-high public header control on home and pricing
- Kind: interactive
- Primitive type: not in the token set
- Anatomy: label
- Background: transparent
- Text: `#000000`
- Border: `1px solid #e4e3de` on the right edge
- Radius: `0px`
- Padding: `0px 16px`
- Font: `16px / 500 / ALTMistral`
- Use: `home::[data-omd-capture="55"]`; 48px-high public header control on home and pricing
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the 48px-high public header control on home and pricing |
| hover | applicable | Pointer-web header control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A header control can be unavailable; visual treatment omitted |
| loading | not-applicable | Header controls and compact links do not perform a request-commit |
| error | not-applicable | Header controls and compact links do not report a request or validation failure |
| success | not-applicable | Header controls and compact links do not confirm an action outcome |

### Expandable/list row

- Role: full-width expandable/list row on the captured public surfaces
- Kind: interactive
- Primitive type: not in the token set
- Anatomy: row
- Background: transparent
- Text: `#000000`
- Border: `1px solid #e4e3de` on the bottom edge
- Radius: `0px`
- Padding: `16px`
- Font: `16px / 400 / Inter`
- Use: `home::[data-omd-capture="1"]`; full-width row observed across the captured public surfaces
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the full-width expandable/list row |
| hover | applicable | Pointer-web expandable row; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An expandable row can be unavailable; visual treatment omitted |
| loading | not-applicable | An expandable/list row discloses content; the row itself does not enter a loading state |
| error | not-applicable | Row meaning is expanded versus resting, not a request or validation failure on the row |
| success | not-applicable | Row meaning is disclosure, not action-outcome confirmation |

### Primary-surface plan shell

- Role: public pricing-plan shell
- Primitive type: `card`
- Anatomy: surface
- Background: `#fbfbf8`
- YAML `fg`: `#000000`
- §4 text: `oklch(0.21 0.006 285.885)`
- Border: `1px solid #e4e3de`
- Radius: `0px`
- Font: `16px / 400 / Inter`
- Token-set path: `tokens.components.pricing-plan.type` · `tokens.components.pricing-plan.bg` · `tokens.components.pricing-plan.fg` · `tokens.components.pricing-plan.border` · `tokens.components.pricing-plan.radius` · `tokens.components.pricing-plan.font` · `tokens.components.pricing-plan.use`
- Use: YAML `Pricing-plan shell`; `pricing::mistral-block-card-plan`
- Observed: default only
- Kind and a state-applicability map are omitted. The source records a pricing-plan shell, not interactive-kind evidence.

### Secondary-surface plan shell

- Role: alternate captured public pricing-plan shell
- Primitive type: not in the token set
- Anatomy: surface
- Background: `#f5f4ef`
- Text: `oklch(0.21 0.006 285.885)`
- Border: `1px solid #e4e3de`
- Radius: `0px`
- Font: `16px / 400 / Inter`
- Use: `pricing::mistral-block-card-plan`; alternate captured public pricing-plan shell
- Observed: default only
- Kind and a state-applicability map are omitted. The source records an alternate pricing-plan shell, not interactive-kind evidence.

### Pricing navigation container

- Role: pricing navigation container
- Primitive type: not in the token set
- Anatomy: surface
- Background: `#f5f4ef`
- Text: `#000000`
- Border: `1px solid #e4e3de`
- Radius: `6px`
- Padding: `6px`
- Font: `16px / 400 / Inter`
- Use: `pricing::mistral-atom-navigation-tabs`; a container observation, not a child-tab active/inactive claim
- Observed: default only
- Kind and a state-applicability map are omitted. The source records a container observation, not a child-tab active/inactive claim and not interactive-kind evidence.

### Pricing number input

- Role: number input in the public pricing surface
- Kind: interactive
- Primitive type: not in the token set
- Anatomy: value field
- Background: transparent
- Text: `oklch(0.21 0.006 285.885)`
- Border: `1px solid oklch(0.21 0.006 285.885)` on the bottom edge
- Radius: `0px`
- Font: `16px / 400 / Inter`
- Use: `pricing::[data-omd-capture="74"]`; number input in the public pricing surface
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the pricing number input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A quantity field can be unavailable; visual treatment omitted |
| loading | not-applicable | A quantity number input does not itself enter a loading state |
| error | applicable | Quantity field; visual treatment omitted |
| success | not-applicable | A quantity number input does not confirm an action outcome |

### Disabled compact control

- Role: disabled compact pricing control
- Kind: interactive
- Primitive type: `button`
- Anatomy: control
- Background: `rgba(7, 7, 11, 0.1)`
- Text: `oklch(0.552 0.016 285.938)`
- Radius: `6px`
- Padding: `4px`
- Font: `16px / 400 / Inter`
- YAML `states`: `disabled DOM state observed`
- Token-set path: `tokens.components.disabled-compact-control.type` · `tokens.components.disabled-compact-control.bg` · `tokens.components.disabled-compact-control.fg` · `tokens.components.disabled-compact-control.radius` · `tokens.components.disabled-compact-control.padding` · `tokens.components.disabled-compact-control.font` · `tokens.components.disabled-compact-control.states` · `tokens.components.disabled-compact-control.use`
- Use: YAML `Disabled compact pricing control`; `pricing::[data-omd-capture="75"]`; disabled DOM control on the public pricing surface
- Observed: disabled DOM state; no interaction transition was recorded. The listed visual values are that disabled treatment. No enabled rest-state treatment is recorded.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | A compact pricing control has a rest state; visual treatment omitted |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Captured disabled DOM state; treatment recorded above |

Loading, error, and success applicability are omitted. Source names this a disabled compact pricing control; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML `tokens.spacing.xs: 2` / `tokens.spacing.sm: 4` / `tokens.spacing.md: 8` / `tokens.spacing.lg: 16` / `tokens.spacing.xl: 24` kept unitless beside the observed 2px / 4px / 8px / 16px / 24px cluster, highest-frequency 16px for rows and card/control spacing / 8px for gaps / 4px for compact labels as an observed public-web rhythm rather than a released spacing scale, 48px-high header control, `0px 16px` header padding, and the segmented container as local captured geometry rather than a cross-viewport specification, horizontally scrollable pricing navigation shell and responsive utility classes in markup as source-recorded facts that do not establish breakpoints, and only the supplied desktop public-surface evidence in scope — are a derived editorial implementation inference from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

The supplied evidence recurs at 2px, 4px, 8px, 16px, and 24px. The highest-frequency values are 16px for rows and card/control spacing, 8px for gaps, and 4px for compact labels. This is an observed public-web rhythm, not a released spacing scale or responsive specification. The capture does not establish a sitewide max width, grid, or authenticated-product layout.

Only the supplied desktop public-surface evidence is in scope. It exposes a horizontally scrollable pricing navigation shell and responsive utility classes in markup, but it does not establish breakpoints, mobile navigation behavior, touch-target policy, reflow rules, or an authenticated-product responsive contract. The 48px-high public header control, `0px 16px` header padding, and 6px segmented container are desktop-capture measurements, not a cross-viewport specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Official language is direct, technically ambitious, and oriented toward access and control: Mistral says it exists to put frontier AI in everyone’s hands, while its careers page asks for structured, direct communication that values content over tone. This supports product-led, specific public language; it is not evidence for a comprehensive error-copy or conversational-assistant style guide. Characterizing that official writing as direct, technically ambitious, and oriented toward access and control, reading it as support for product-led, specific public language, and keeping it from becoming a comprehensive error-copy or conversational-assistant style guide, are derived editorial implementation inferences from the verified surfaces; they are not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

| Context | First-party direction |
|---|---|
| Mission | Frame AI as open, controllable, and capable of solving hard problems. |
| Enterprise and public-sector work | Describe the concrete stakes: control, ownership, reliability, and tailored systems. |
| Internal culture | Prefer rigor, directness, ownership, speed, and low-ego collaboration. |

**Voice samples**

- “Putting frontier AI in everyone's hands.” <!-- official about page, 2026-07-13 -->
- “Mission-critical AI for enterprises and governments.” <!-- official about page, 2026-07-13 -->
- “Build the future of frontier AI.” <!-- official careers page, 2026-07-13 -->

The three quoted lines are official wording from the about page and the careers page, kept byte-exact. Treating them as official wording rather than as synthetic microcopy, and naming the public marketing home and the public commercial-pricing route as this packet's named surfaces, is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

This packet names the public marketing home and the public commercial-pricing route. Official About, Brand, and Careers samples above are official wording.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Listing them as unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Mistral-authored or taken from a separately published UI specification, including the official brand-asset guidance.

- success, warning, error, dark-mode, focus, and hover color tokens
- Source Sans 3 as a typography token
- documentation chrome or a docs family
- hover, focus-visible, empty, loading, success, error, validation, toast, dialog, and skeleton visual treatments (except the one captured disabled compact-control treatment)
- Disabled compact control loading · error · success applicability (exact request/outcome unresolved)
- a sitewide max width, grid, or authenticated-product layout
- a global shadow scale, modal elevation, overlay treatment, or animation layer
- breakpoints, mobile navigation behavior, touch-target policy, reflow rules, or an authenticated-product responsive contract
- a comprehensive error-copy or conversational-assistant style guide
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
- the earlier orange/golden palette, Arial, all-0px rule, and warm-shadow system
- getdesign.md purple, DM Serif, JetBrains Mono, 8px-button, and 12px-card claims
- active tabs, additional price-plan variants, menus, dialogs, and focus rings
