# Nexon Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Nexon's website is the digital storefront of one of Korea's largest game publishers. This contract covers the two first-party public surfaces the source inspected for tokens on 2026-06-09: the Korean home page at `https://www.nexon.com` and the main portal surface at `https://www.nexon.com/Main/Index`. Token-set note: primary = live Nexon brand green (`#00de5a`, `rgb(0,222,90)`) measured on the home-page CTA with black label; chrome built on near-black `#17191d` text over white. Every color, type, geometry, elevation, and component value below stays attached to the surface that established it. Reading those two inspected routes as this contract's token surfaces, keeping values attached to the surface that established them, and keeping the token-set note as a measurement bound rather than as tokens for surfaces the source did not inspect, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

The following characterization of the interface is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification. The source records a deliberate split personality: an editorial, almost corporate calm in the chrome, broken by sudden bursts of high-voltage brand green. The page sits on a clean white canvas (`#ffffff`) with near-black ink (`#17191d`) for headings and navigation, but the moment a primary call-to-action appears, the system detonates its signature `#00de5a` — an electric, arcade-bright green that reads as "press start." This is the green of a power-up, of a glowing console button, and it is used with restraint precisely so that each appearance lands like a hit. The typographic backbone is **NEXON Gothic Bold**, the company's proprietary typeface, which carries the brand voice on every CTA and emphasis element. It falls back to `malgun gothic` for body copy. The result is a hierarchy where the custom face does the shouting and the system face does the reading. Black text (`#000000`) sits directly on the green CTA — a high-contrast, almost industrial pairing that refuses the soft white-on-color convention most consumer sites default to. The black-on-green combination is the single most identifiable Nexon design signal. Geometry is sharp and confident. Buttons and nav items use zero or minimal corner radius, echoing the rectangular, pixel-grid heritage of game UI rather than the soft pill shapes of modern SaaS. Shadows are light and functional — elevation exists to separate cards from the canvas, not to create atmosphere. The overall impression is of a company that takes its games' energy seriously but frames it inside a disciplined, grown-up corporate shell.

Brand narrative recorded by the source, kept as narrative context. Nexon was founded in **1994** in Seoul by **Jake Kim (Kim Jung-ju)** and is widely credited with pioneering the **massively multiplayer online (MMO) graphical game** and the **free-to-play, item-based business model** that reshaped global game monetization. Its 1996 title *Nexus: The Kingdom of the Winds* (바람의나라) is recognized as one of the world's first commercially successful graphical MMORPGs. Over three decades Nexon grew into one of the largest game publishers in Asia, operating franchises such as *MapleStory*, *Dungeon&Fighter*, *KartRider*, *FIFA Online*, and *The Finals*, and is publicly listed (Nexon Co., Ltd. on the Tokyo Stock Exchange). The brand identity is built around a single, confident gesture: the electric green "go" signal. In a category crowded with dark, fantasy-heavy game branding, Nexon's bright `#00de5a` on white reads as energetic, accessible, and modern — a publisher's mark rather than a single game's skin. The proprietary **NEXON Gothic** typeface (released as a free public font family) reinforces a corporate identity that is consistent across dozens of game brands while remaining recognizably Nexon. What the brand refuses: a single dominant game aesthetic imposed on the corporate shell. The white canvas and disciplined gray-and-green system act as a neutral frame so that each game's own art can shine through, while the green CTA and NEXON Gothic stamp keep the publisher's identity present on every screen. The years 1994 / 1996, Jake Kim (Kim Jung-ju), the MMO graphical-game and free-to-play item-based model, *Nexus: The Kingdom of the Winds* (바람의나라), the franchise names, Nexon Co., Ltd. on the Tokyo Stock Exchange, the electric green "go" signal, NEXON Gothic as a free public font family, and that closing refuse-and-neutral-frame sentence are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-portfolio narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, and classifying them as surface-or-control outcomes rather than fictional biographies, is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification. Each names a surface or control the source records. They do not come from the source's persona section.

- Scan multi-column game grids and event tiles on the nexon.com portal.
- Use the primary green CTA — "다운로드", "게임 시작", "지금 플레이".
- Read the centered content column for corporate/IR pages.
<!-- design-md:claim-end -->

### Audience

No named or demographic personas are invented. The source labels its named figures as fictional archetypes informed by publicly observable Nexon audience segments, so those biographies are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is the audience at a group level: Korean and global players, returning veterans, prospective partners/investors. Dropping the fictional archetypes rather than promoting them, carrying no name, age, city, motivation, or affiliation classification, and reading those source-named groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Distinctive traits

The list restates values the source records. The groupings and the readings inside them are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

- Signature arcade-green `#00de5a` reserved for primary CTAs and brand accents — used sparingly for maximum impact
- Black label (`#000000`) directly on green — the defining high-contrast Nexon pairing
- Proprietary **NEXON Gothic Bold** for CTAs and emphasis; `malgun gothic` fallback for body
- Near-black ink (`#17191d`) instead of pure black for headings and navigation
- Sharp, low-radius geometry (0-4px) echoing game-UI heritage
- Restrained, functional shadows — elevation, not atmosphere
- Muted gray scale (`#737881`, `#4a4e57`, `#919191`, `#9fa1a7`) for hierarchy and utility text

### Principles

These 6 items are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

1. **Green is the spark, not the field.** The brand green works because it is scarce. Used as the CTA and accent only, it stays a power-up; used everywhere, it would become wallpaper.
2. **Black on green, by design.** The high-contrast label pairing is intentional and industrial — it signals confidence and refuses the soft default of white-on-color.
3. **A neutral frame for vivid games.** The white-and-gray chrome is deliberately calm so that diverse game art can carry the color. The publisher's restraint is what lets the products be loud.
4. **The typeface is the constant.** NEXON Gothic Bold appears across every game brand and every CTA — one consistent voice unifying a varied portfolio.
5. **Sharp geometry signals games.** The low-radius, rectangular forms tie the corporate site back to the pixel-grid heritage of the medium.
6. **Energy without hype.** Player copy is immediate and direct; corporate copy is measured. Both avoid stacked superlatives.

### Application rules

The source states these as its Do lists (§8 and the closing §16), kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification. Where the two lists write the same rule in different words, both writings stay.

- Reserve Nexon Green (`#00de5a`) for primary CTAs, active states, and brand accents — scarcity is what makes it land
- Use Nexon Green (`#00de5a`) as the scarce, high-impact CTA and accent color
- Pair black (`#000000`) text directly on the green — the high-contrast combo is the brand signal
- Pair black (`#000000`) on the green; near-black ink (`#17191d`) for headings
- Use NEXON Gothic Bold for CTAs and emphasis; `malgun gothic` for body
- Use near-black ink (`#17191d`) for headings and nav instead of pure black
- Keep geometry sharp (0-4px radius) — it echoes the game-UI heritage
- Keep corners sharp (0-4px) and shadows neutral/functional
- Keep shadows neutral and functional — elevation, not atmosphere
- Use the gray scale (`#737881`, `#4a4e57`, `#919191`, `#9fa1a7`) for hierarchy
- Step text hierarchy through the gray scale (`#737881`, `#4a4e57`, `#919191`, `#9fa1a7`)
- Let the white-and-gray frame stay calm so game art and the green CTA carry the energy
- Green (`#00de5a`) is precious — one or two appearances per viewport maximum (§9 Iteration Guide, unique there)

### Avoid

The source states these as its Don't lists (§8 and the closing §16). These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification. Where the two lists write the same prohibition in different words, both writings stay.

- Don't overuse the green — a page full of `#00de5a` kills its power-up impact
- Don't flood the layout with green — scarcity is the whole point
- Don't put white text on the green CTA — black is the brand-correct label
- Don't use white text on the green CTA
- Don't use large pill radii on buttons — Nexon's geometry is sharp and rectangular
- Don't use pill/large radii or colored decorative shadows
- Don't add colored or heavy decorative shadows — elevation is functional only
- Don't replace NEXON Gothic Bold on CTAs with a generic bold — the proprietary face is the stamp
- Don't swap NEXON Gothic Bold for a generic bold on CTAs
- Don't use pure black (`#000000`) for body headings — `#17191d` reads cleaner
- Don't use pure black (`#000000`) for body headings — prefer `#17191d`

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Token-set keys keep the YAML lowercase hex. Calling `#00de5a` the signature arcade green, calling the black-on-green pairing the brand's most identifiable signal, calling `#17191d` a near-black that reads cleaner than pure black on white, keeping `tokens.colors.on-primary` and `tokens.colors.pure-black` as two keys that happen to share `#000000`, and keeping `tokens.colors.disabled` `#9fa1a7` off `tokens.components.footer-label` foreground, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

Primary

- **Nexon Green** (`#00de5a`): The signature brand color. Primary CTA backgrounds, brand accents, active-state underlines, event badges. An electric arcade green measured live as `rgb(0,222,90)` on the home-page CTA. Token-set path `tokens.colors.primary`.
- **On-Primary Black** (`#000000`): Label color that sits directly on Nexon Green. The black-on-green pairing is the brand's most identifiable signal. Token-set path `tokens.colors.on-primary`.
- **Pure White** (`#ffffff`): Page background, card surfaces, nav background. Token-set path `tokens.colors.canvas`.

Ink & Text

- **Ink** (`#17191d`): Primary heading, navigation, and link text. A near-black that reads cleaner than pure black on white. Token-set path `tokens.colors.ink`.
- **Near-Black Link** (`#080410`): Deepest link/text tone for high-emphasis inline links. Token-set path `tokens.colors.near-black`.
- **Body Gray** (`#737881`): Default body and secondary text color, measured on `body`. Token-set path `tokens.colors.body`.
- **Label Gray** (`#4a4e57`): Stronger secondary labels and sub-headings. Token-set path `tokens.colors.label`.
- **Muted Gray** (`#919191`): Utility labels and placeholder-adjacent text, often with -0.3px tracking. Token-set path `tokens.colors.muted`.
- **Disabled Gray** (`#9fa1a7`): Inactive CTA fills and disabled control text. Token-set path `tokens.colors.disabled`.

Pure Black

- **Pure Black** (`#000000`): CTA label on green, maximum-contrast utility. Token-set path `tokens.colors.pure-black`.

`tokens.colors.on-primary` and `tokens.colors.pure-black` both write `#000000`. They stay two keys. `tokens.colors.disabled` `#9fa1a7` is not the footer-label foreground as a second colors key; that foreground stays on `tokens.components.footer-label`.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept as the source wrote them): `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 20` · `xl: 24` · `xxl: 36` · `section: 48`.

Source §5 restates the same range as a scale: 4px, 8px, 12px, 16px, 20px, 24px, 36px, 48px, with base unit 8px (with dense 4px micro-steps). Both writings stay. Notable: Dense spacing reflects the Korean game-portal convention of fitting many entry points (games, events, community) above the fold.

`tokens.spacing.xs: 4` is not `tokens.rounded.md: 4` and is not a component radius. `tokens.spacing.sm: 8` is not `tokens.rounded.lg: 8` and is not badge padding `2px 8px`. `tokens.spacing.md: 12` is not Primary CTA padding `12px 24px` and is not a type size. `tokens.spacing.base: 16` is not nav-link padding `0px 16px`, not card padding `16px`, and not Nav Link `16px`. `tokens.spacing.lg: 20` is not Ghost padding `10px 20px`. `tokens.spacing.xl: 24` is not the `24px` in Primary CTA padding. `tokens.spacing.xxl: 36` is not a type size. `tokens.spacing.section: 48` is not a type size. Keeping those unitless spacing steps on their own keys rather than rewriting them as a grid, keeping those writings of `4`, `8`, `12`, `16`, `20`, `24`, `36`, and `48` on their own records, and reading dense spacing as the Korean game-portal convention of fitting many entry points above the fold, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path): `sm: 0` · `md: 4` · `lg: 8` · `full: 9999`.

The source's named radius uses, kept on their own rows:

- Sharp (`0` / `0px`): Buttons, nav items, GNB — the game-UI default. Token-set key `tokens.rounded.sm`. YAML `button-primary` / `button-disabled` / `button-ghost` / `nav-link` / `menu-item` / `footer-label` also record `radius: "0px"`.
- Small (`4` / `4px`): Cards, badges, inputs. Token-set key `tokens.rounded.md`.
- Large (`8` / `8px`): Larger feature containers. Token-set key `tokens.rounded.lg`.
- Full (`9999`): token-set key `tokens.rounded.full: 9999`. No use is attached to `full` here.

`tokens.rounded.sm: 0`, `tokens.rounded.md: 4`, `tokens.rounded.lg: 8`, and `tokens.rounded.full: 9999` stay four keys. `tokens.rounded.full: 9999` stays the unitless full step. It is not a type size, and it is not rewritten as a component radius the source did not attach. YAML `0` stays off prose `0px`. Reading 0–4px corners as sharp geometry echoing game-UI heritage rather than as a universal radius scale, and reading `full: 9999` as a recorded step rather than as a pill on buttons, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | `none` | Page background, nav, inline text |
| Ambient (Level 1) | `rgba(0,0,0,0.08) 0px 2px 8px` | Cards, hover lift |
| Elevated (Level 2) | `rgba(0,0,0,0.16) 0px 8px 24px` | Dropdowns, modals, featured tiles |

Token-set path `tokens.shadow.flat` `"none"` · `tokens.shadow.ambient` `"rgba(0,0,0,0.08) 0px 2px 8px"` · `tokens.shadow.elevated` `"rgba(0,0,0,0.16) 0px 8px 24px"`.

**Shadow Philosophy**, as the source states it: Nexon's elevation is purely functional. Shadows are neutral black at low alpha — they exist to lift a card off the white canvas, not to add brand atmosphere. The brand energy comes entirely from color (`#00de5a`) and typography (NEXON Gothic Bold), never from decorative depth. This keeps the chrome calm so the game content and the green CTA carry all the visual voltage. Reading that elevation as purely functional rather than atmospheric, and reading brand energy as coming from color and typography rather than from decorative depth, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Motion

The source records durations, easing *roles*, signature motions, and a reduced-motion rule in its Philosophy Layer (sections 10–15). Token-level claims in sections 1–9 are sourced from a live Playwright getComputedStyle inspection; the source does not attach computed transition properties, animation names, or easing curves to that inspection. Durations, role names, uses, signature motions, and the reduced-motion rule are therefore kept as source-stated values. The two cubic-bezier curve values are omitted at the smallest value boundary as unsourced curves. Classifying that philosophy-layer attribution, keeping the named duration and role records, omitting the unsourced curves, and holding the five-kind per-component promotion gate, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

Durations, as the source table states them:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, focus, button press |
| `motion-standard` | 240ms | Dropdown, menu, card hover lift |
| `motion-slow` | 360ms | Hero banner carousel transitions |

Easing roles, as the source names them. Curve values are omitted:

| Token | Use |
|---|---|
| `ease-standard` | Menus, hover, two-way transitions |
| `ease-enter` | Dropdowns, banner arrivals |

Signature motions, as the source states them:

1. **CTA hover.** The green CTA brightens slightly on hover using `motion-fast / ease-standard` — a quick, responsive "ready" signal, no scale bounce.
2. **Hero banner carousel.** The featured-game banner crossfades or slides at `motion-slow`, giving each game a moment to register.
3. **Reduce motion.** Under `prefers-reduced-motion: reduce`, carousel auto-advance pauses and transitions collapse to near-instant; the site stays fully functional.

`motion-fast` 120ms is this duration token. It is not a spacing step. `motion-standard` 240ms is not `tokens.spacing.xl: 24`. `motion-slow` 360ms is not `tokens.spacing.xxl: 36`. The source's "focus" in the `motion-fast` use column is a duration role; it is not a `focus-visible` visual treatment and is not promoted into any component's `focus-visible` row. Keeping those duration values off matching spacing steps, and treating the `motion-fast` "focus" use as a duration role rather than as a `focus-visible` treatment, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

Promotion gate. Do not promote any duration, easing curve, transition property, animation name, or reduced-motion behavior as a computed component token until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve, or a match against a framework or vendor document, is not that gate. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation, and refusing a match against an official framework or vendor document as that gate, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | Live home-page CTA computes `"NEXON Gothic Bold"` 14px weight 700. The source does not publish a separate universal current typography specification. |
| Live computed surface-use | Primary CTA: font `"NEXON Gothic Bold"` 14px weight 700. Body font fallback: `"malgun gothic"`, `"sans-serif"`. |
| Official distributed asset | Source §11 records that **NEXON Gothic** was released as a free public font family. That is a narrative/distribution fact; it does not by itself supply a license string. |
| Declared-only | The source records no declared-but-unused face. |
| License | The source records no font license. None is supplied here. |
| Fallback | `malgun gothic`, then `sans-serif` — the running-text face for body copy and most navigation. Not a substitute for NEXON Gothic Bold. |
| Outside these captures | Type beyond the two inspected nexon.com routes stays outside this contract. |

### Family

- **Primary / sans:** `NEXON Gothic Bold`. Token-set path `tokens.typography.family.sans`. The proprietary brand face, used on CTAs, emphasis, and bold captions.
- **Fallback / Body:** `malgun gothic`, then `sans-serif`. Token-set path `tokens.typography.family.fallback`. The running-text face for body copy and most navigation.
- Do not replace NEXON Gothic Bold on CTAs with a generic bold. Do not present `malgun gothic` as NEXON Gothic Bold. Reading the two-face split as a deliberate brand stamp, and refusing the fallback as a substitute for the proprietary face, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Type roles

YAML writes numeric sizes without a `px` suffix and unitless `lineHeight: 1.20`. Source §3 writes `16px` / `14px` / `12px` and line height `1.20`. Both writings stay. YAML `use` strings are restored verbatim; §3 Notes that are longer sit beside them. Keeping each role on the token-set path the source names, keeping YAML `use` and §3 notes as two writings rather than choosing one as a replacement, and keeping `1.20` as a unitless ratio rather than rewriting it as a replacement px, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Token-set use | Notes |
|---|---|---|---:|---|---|---|---|
| Nav Link | NEXON Gothic Bold / malgun | YAML `16` / §3 `16px` | 400 | 1.20 | normal | Primary GNB navigation link, ink color | Primary GNB items, `#17191d` |
| Menu Link | malgun gothic | YAML `14` / §3 `14px` | 400 | 1.20 | normal | Secondary nav / sub-menu link | Sub-nav, dropdown rows |
| CTA | NEXON Gothic Bold | YAML `14` / §3 `14px` | 700 | 1.20 | normal | Primary green CTA label, NEXON Gothic Bold | Green CTA label, black text |
| Body | malgun gothic | YAML `12` / §3 `12px` | 400 | 1.20 | normal | Standard reading text, footer copy | Standard reading text |
| Label | malgun gothic | YAML `12` / §3 `12px` | 400 | 1.20 | YAML `-0.3` / §3 `-0.3px` | Muted utility labels | Muted utility labels |
| Caption Bold | NEXON Gothic Bold | YAML `12` / §3 `12px` | 700 | 1.20 | normal | Bold footer / emphasis caption | Bold footer labels, `#9fa1a7` |

Token-set paths: `tokens.typography.nav-link.size` / `weight` / `lineHeight` / `use`; `tokens.typography.menu-link.*`; `tokens.typography.cta.*`; `tokens.typography.body.*`; `tokens.typography.label.*` (plus `tracking`); `tokens.typography.caption.*`.

YAML `tokens.typography.nav-link.size` `16` is not `tokens.spacing.base: 16`. YAML `tokens.typography.body.size` `12` is not `tokens.spacing.md: 12`. Keeping those YAML sizes off those spacing steps is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

### Typography rules

The source states these four as its typography principles. These rules, and the readings inside them, are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

- **Two-face system**: NEXON Gothic Bold for shouting (CTAs, emphasis), `malgun gothic` for reading (body, most nav). The custom face is a deliberate brand stamp.
- **Weight as emphasis**: 700 is reserved for CTAs and key labels; 400 carries everything else. The contrast between the two is the primary typographic signal.
- **Tight line-height**: Compact ~1.2 line-heights keep dense Korean-web layouts legible without wasted vertical space.
- **Small base size**: 12px body reflects the information-dense Korean portal convention; emphasis is created by weight and color, not large type.

### Assets

- Catalog `logo` is recorded as a favicon slug: `https://www.google.com/s2/favicons?domain=www.nexon.com&sz=128`. Frontmatter records `logo.type: favicon`. That URL is a third-party favicon-proxy pointer, not a Nexon-hosted brand file.
- **NEXON Gothic** released as a free public font family (source §11). Distribution fact, not a license string.
- Game promo imagery and event tiles are first-party catalog content on the white canvas; do not replace them with invented brand-color decoration.

The catalog-boundary reading of the Google s2 favicon slug, classifying NEXON Gothic's free-public release as a distribution fact rather than a license string, and classifying game promo imagery as first-party catalog content rather than as a substitute decoration, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here in full:

| State | Treatment |
|---|---|
| **Empty (no events)** | White canvas, single line in `#737881` body gray: "예정된 이벤트가 없습니다." No illustration. |
| **Loading** | Neutral skeleton blocks in light gray at final dimensions, subtle shimmer. |
| **Error** | Inline message in muted gray with a clear retry text action. Plain, factual wording. |
| **Success (action done)** | Brief inline confirmation, no emoji. The green accent marks completion sparingly. |
| **Disabled (CTA)** | Green fill switches to disabled gray (`#9fa1a7`) with white text — clearly inert, brand color withheld. |
| **Active (nav)** | Nexon Green (`#00de5a`) bottom-border underline on the current GNB item. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted where this packet holds no value for that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow each control's product role rather than its primitive kind. A `Primitive type` line is attached only when the source YAML records that type on that component. Kind and a state-applicability map are omitted where the source supplies no interactive-kind evidence. The source's "focus" in the `motion-fast` use column is a duration role, a different evidence kind from `focus-visible`; no `focus-visible` row carries a treatment. This is not a complete state-coverage claim. The applicability note above, every interactive-kind verdict, every applicability verdict, the reason given for either, attaching a YAML primitive type only when the token set records it, omitting kind and map where interactive-kind evidence is absent, the `focus` / `focus-visible` evidence-kind distinction, and the refusal to treat this as a complete state-coverage claim are a derived editorial implementation inference from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Primary Green CTA

- Role: Primary action — "다운로드", "게임 시작", "지금 플레이"
- Primitive type: `button` · YAML `type: button` · Token-set path `tokens.components.button-primary.type` · Kind: interactive
- Anatomy: label
- Background: `#00de5a`
- Text: `#000000`
- Padding: `12px 24px`
- Radius: `0px` (sharp)
- Font: `14px / 700` · 14px NEXON Gothic Bold, weight 700
- Token-set use: `Primary green CTA, black label, NEXON Gothic Bold, sharp corners`
- Observed hover (signature motion): The green CTA brightens slightly on hover using `motion-fast / ease-standard` — a quick, responsive "ready" signal, no scale bounce. No brighter hex is recorded.
- The `12px` / `24px` in the padding are this button's padding. They are not `tokens.spacing.md: 12` or `tokens.spacing.xl: 24`. The `14px / 700` font is this control's font; it is not only the CTA type-role row. Reading those figures as this button's geometry rather than as those YAML spacing steps or a shared type-role row is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; signature motion records a slight brighten; no brighter hex is promoted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted. The `motion-fast` "focus" duration role is a different evidence kind |
| disabled | applicable | Button control; disabled appearance is the `button-disabled` token-set record below |
| loading | applicable | "다운로드" / "게임 시작" / "지금 플레이" commit an operation; the surface contract records skeleton blocks, not this control's in-progress treatment |
| error | applicable | The same commit can fail; the surface contract records an inline muted-gray retry message, not this control's error treatment |
| success | applicable | Same commit role; the surface contract records a brief inline confirmation with a spare green accent, not this control's success treatment |

### Disabled CTA

- Role: Inactive / unavailable action
- Primitive type: `button` · YAML `type: button` · Token-set path `tokens.components.button-disabled.type`
- Kind: omitted. This token-set record is the disabled appearance of the primary CTA, not a second control family, so it declares no second Core §4.4 state-applicability map.
- Background: `#9fa1a7`
- Text: `#ffffff`
- Padding: `12px 24px`
- Radius: `0px`
- Font: `14px / 400`
- Token-set use: `Inactive / disabled CTA, muted gray fill`
- Omitting kind and a second map on this appearance record, rather than inventing a second control family, is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

### Ghost / Text Action

- Role: Secondary text-only actions
- Primitive type: `button` · YAML `type: button` · Token-set path `tokens.components.button-ghost.type` · Kind: interactive
- Anatomy: label
- Background: `#ffffff`
- Text: `#17191d`
- Padding: `10px 20px`
- Radius: `0px`
- Font: `14px / 400`
- Token-set use: `Secondary text action, no fill, ink label`
- The `20px` in the padding is this button's padding. It is not `tokens.spacing.lg: 20`. The surface contract's error row names a "clear retry text action"; that wording is kept on the capture record and is not rewritten as a proof that this ghost is the only retry control. Reading those figures as this button's geometry rather than as that YAML spacing step, and keeping the retry wording on the capture record rather than rewriting it as a proof that this ghost is the only retry control, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | Button control; visual treatment omitted |
| loading | applicable | A secondary text action can commit an operation (the surface contract names a retry text action); visual treatment omitted |
| error | applicable | The same commit can fail; visual treatment omitted |
| success | applicable | Same commit role; visual treatment omitted |

### GNB Nav Link

- Role: GNB top navigation item
- Primitive type: `tab` · YAML `type: tab` · Token-set path `tokens.components.nav-link.type` · Kind: interactive
- Anatomy: label
- Text: `#17191d`
- Radius: `0px`
- Padding: `0px 16px`
- Font: `16px / 400`
- Active: YAML `Nx bottom border #00de5a` · §4 Active item: Nexon Green (`#00de5a`) bottom border underline. Both writings stay.
- Token-set use: `GNB top navigation item`
- Clean horizontal GNB on white (`#ffffff`). Links: 16px, `#17191d` ink, NEXON Gothic Bold / malgun fallback.
- The `16px` in the padding and font are this control's geometry. They are not `tokens.spacing.base: 16` and not only the Nav Link type-role row. Reading those figures as this tab's geometry rather than as that YAML spacing step, and keeping YAML `Nx bottom border #00de5a` beside the §4 underline wording rather than choosing one as a replacement, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above; active is the recorded variant |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A destination tab whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control selects a destination; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | A tab that only selects a destination does not report a failed request on itself |
| success | not-applicable | Same role reason: selecting a destination is not an operation this tab reports as success |

### Menu Item

- Role: Dropdown / sub-nav list row
- Primitive type: `listItem` · YAML `type: listItem` · Token-set path `tokens.components.menu-item.type` · Kind: interactive
- Anatomy: label
- Background: `#ffffff`
- Text: `#17191d`
- Radius: `0px`
- Padding: `8px 16px`
- Font: `14px / 400`
- Token-set use: `Dropdown / sub-nav list row`
- Sub-menu rows: 14px `malgun gothic`, `#17191d` on white.
- The `8px` / `16px` in the padding are this row's padding. They are not `tokens.spacing.sm: 8` or `tokens.spacing.base: 16`. Reading those figures as this list row's geometry rather than as those YAML spacing steps is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web list row; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A destination row whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a destination row; it does not commit an operation whose in-progress state it could report |
| error | not-applicable | A destination row does not report a failed request on itself |
| success | not-applicable | Same role reason: reaching the destination is not an operation this row reports as success |

### Game Promo Card

- Role: Game promo card on white canvas, ambient shadow
- Primitive type: `card` · YAML `type: card` · Token-set path `tokens.components.card.type`
- Kind: omitted. The source records this as a container on the white canvas and gives it no control role or interactive-kind evidence, so it declares no Core §4.4 state-applicability map and it is not recast as a control.
- Background: `#ffffff`
- Text: `#17191d`
- Radius: `4px`
- Padding: `16px`
- Shadow (ambient): `rgba(0,0,0,0.08) 0px 2px 8px`
- Shadow (elevated): `rgba(0,0,0,0.16) 0px 8px 24px` — source §4 Cards & Containers also records this elevated treatment for cards, event tiles on white canvas
- Token-set use: `Game promo card on white canvas, ambient shadow`
- §9 example prompt (unique there): Title 16px `#17191d`, body 12px `#737881`. Green `#00de5a` NEW badge with black text, 4px radius.
- The `16px` padding is this card's padding. It is not `tokens.spacing.base: 16`. The `4px` radius is this card's radius. It is not `tokens.spacing.xs: 4`. Omitting kind and a map rather than inventing them, and reading those figures as this card's geometry rather than as those YAML spacing steps, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### NEW / Event Badge

- Role: NEW / event markers
- Primitive type: `badge` · YAML `type: badge` · Token-set path `tokens.components.badge.type`
- Kind: non-interactive. Status marker, not a control; no state-applicability map.
- Background: `#00de5a`
- Text: `#000000`
- Padding: `2px 8px`
- Radius: `4px`
- Font: `12px / 700` · 12px NEXON Gothic Bold, weight 700
- Token-set use: `NEW / event badge, brand green fill`
- Treating this badge as a status marker rather than a control is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

### Footer Label

- Role: Bold footer label, muted gray
- Primitive type: `badge` · YAML `type: badge` · Token-set path `tokens.components.footer-label.type`
- Kind: non-interactive. Label, not a control; no state-applicability map.
- Text: `#9fa1a7`
- Radius: `0px`
- Font: `12px / 700`
- Token-set use: `Bold footer label, muted gray`
- `#9fa1a7` here is this label's foreground. It is not `tokens.colors.disabled` as a second writing of the same key. Treating this footer label as a status/label marker rather than a control, and keeping `#9fa1a7` on this component rather than merging it into the disabled color key, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

### Search / Login Field

- Role: Search / login field, muted `#919191` placeholder
- Primitive type: `input` · YAML `type: input` · Token-set path `tokens.components.input-text.type` · Kind: interactive
- Anatomy: value field
- Background: `#ffffff`
- Text: `#17191d`
- Radius: `4px`
- Padding: `8px 12px`
- Font: `14px / 400`
- Placeholder: `#919191` muted gray
- Token-set use: `Search / login field, muted #919191 placeholder`
- The `8px` / `12px` in the padding are this field's padding. They are not `tokens.spacing.sm: 8` or `tokens.spacing.md: 12`. YAML names both search and login on this one record; both uses stay on this field rather than being split into a second component the token set does not record. Reading those figures as this input's geometry rather than as those YAML spacing steps, and keeping search and login on this one record, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable field; visual treatment omitted |
| disabled | applicable | Input control; visual treatment omitted |
| loading | applicable | YAML names login on this field; login is an operation that can be pending on the control that starts it. Visual treatment omitted |
| error | applicable | Form field; visual treatment omitted |
| success | applicable | A field that also names login can confirm that outcome; visual treatment omitted |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

### Spacing system

- Base unit: 8px (with dense 4px micro-steps)
- Scale: 4px, 8px, 12px, 16px, 20px, 24px, 36px, 48px
- Notable: Dense spacing reflects the Korean game-portal convention of fitting many entry points (games, events, community) above the fold.

### Grid & container

- Full-width hero banner for the featured game, edge-to-edge
- Below the hero: multi-column game grids and event tiles
- Centered content column for corporate/IR pages
- White canvas throughout, with cards as the primary content unit

### Whitespace

- **Functional density**: Nexon packs many games and entry points into the layout; whitespace separates groups, not individual items.
- **Green as the spotlight**: In a dense gray-and-white field, the green CTA is the deliberate focal point that pulls the eye.

### Breakpoints

As the source table states them:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <768px | Single column, hamburger GNB, full-width CTAs |
| Tablet | 768-1024px | 2-column game grids |
| Desktop | >1024px | Full multi-column portal layout |

### Touch targets

- CTAs use comfortable 12px vertical padding
- Nav items spaced for tap on mobile GNB
- Green CTA scales to full-width on mobile for thumb reach

### Collapsing strategy

- GNB: horizontal links collapse to hamburger
- Game grids: multi-column reduce to 2-column then single
- Hero banner: maintains full-width, reduces internal padding
- Body type holds at 12-14px; emphasis stays weight-driven

`tokens.spacing.md: 12` is not the CTA `12px` vertical padding as a second key. Reading functional density, green as the spotlight, and the breakpoint / collapsing rows as source-stated layout rules of the inspected nexon.com portal rather than as a specification invented on top of them, is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Nexon's voice is that of an established game publisher speaking to two audiences at once: players, with energy and immediacy, and the market and partners, with measured corporate confidence. On player-facing surfaces the register is direct and inviting — "지금 플레이", "다운로드", short imperative verbs that map to the green "press start" CTA. On corporate, IR, and careers surfaces the same brand restraint applies, but the tone shifts to the steady, factual register of a major listed company. Calling that register a two-audience split, and refusing to treat those lines as a complete product-microcopy guide, are derived editorial implementation inferences from the verified surfaces; they are not Nexon-authored or a separately published UI specification.

| Context | Tone |
|---|---|
| Game CTAs | Direct imperatives — "지금 플레이", "사전등록", "다운로드". |
| Game/event copy | Energetic, benefit-forward, but not hype-stacked. |
| Corporate / IR | Measured, factual, the register of a publicly listed company. |
| Careers / About | Confident, mission-oriented, talent-focused. |

Empty-state copy, kept as a published string: "예정된 이벤트가 없습니다."

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. The list names only fields the source itself left unnamed or that this migration omitted at the smallest value boundary. Reading the list as a catalog of those unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Nexon-authored or a separately published UI specification.

- cubic-bezier curve values for `ease-standard` and `ease-enter`
- a use for YAML `tokens.rounded.full: 9999`
- `focus-visible` visual treatment
- a brighter hex for CTA hover
