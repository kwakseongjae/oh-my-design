# Genie Music migration log

Source: `web/references/genie/DESIGN.md`
Sibling read (not the migration input): `web/references/genie/.verification.md`
Destination: `docs/design-md-weight/migrated/genie/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/genie/provenance.md`
Date: 2026-08-26
Worker: opus5 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v11**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as committed in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Genie Music Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–21. `https://www.genie.co.kr` is dual: `DESIGN.md` 9/21 + `provenance.md` 13/44/48/62/73/104 (E2a). `#fa4065` is dual: `DESIGN.md` 11/35/49/61/72/112/160/210/248/249/263/342/343/381/497 — 16 occurrences on 15 lines, line 11 carries it twice, and the occurrence on 249 is the leading 7 bytes of `#fa406580` — plus `provenance.md` 14/69/87/100/122/132, 9 occurrences on 6 lines (E2a). The favicon slug is dual: `DESIGN.md` 229 + `provenance.md` 15 (E2a). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 16–21, 31–33, 120. The source footer's producer string `omd-add-reference — Tier 1 live inspect` is at `provenance.md` 36. These are ledger keys with no portable slot. |
| YAML `tokens.colors` (15 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 112–138. All fifteen roles kept with their names and values: primary, primary-hover, accent-blue, heading, body, muted, label, secondary, canvas, surface, hairline, border, icon-gray, on-primary, chip-dark. `#a6afb6` (Icon Gray) has no other home in the document and stays a palette role at 133. |
| YAML `tokens.typography.family` (`sans: "dotum"`, `fallback: "sans-serif"`) | 옮김 → Typography & Assets Family | `DESIGN.md` 203. The fuller stack `dotum, 돋움, sans-serif` exists only in the sibling and stays at `provenance.md` 63/77 (B1). |
| YAML `tokens.typography.gnb / heading / body / body-bold / chip / caption` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 209–215. Unitless line heights stay ratios and are never converted to px (A1a): `1.30` at 210, `1.71` at 211, `1.50` at 212/213, `2.08` at 214; also `provenance.md` 85. `caption` has no `lineHeight` key and keeps `normal` at 215. All six YAML `use` strings are restored verbatim on one line at `DESIGN.md` 217, alongside the §3 table's own Notes column (A1, A3). |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Unitless steps and px forms both kept: `DESIGN.md` 142 (`xs 1 · sm 6 · md 8 · base 12 · lg 18 · xl 24 · xxl 32` plus the px scale), 146–151 (`sm 4 · md 8 · lg 13 · full 9999` plus the four named uses), 414 (Layout restatement). `full: 9999` stays a step and is not given a px suffix. |
| YAML `tokens.shadow.ambient` / `panel` | 옮김 → Foundations elevation | `DESIGN.md` 158–159. Also on the Card / Panel component at 399 and inside the signature-motion sentence at 183 (E2a). |
| YAML `tokens.components` (9 records) | 옮김 → Components & States | `DESIGN.md` 258–409. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Type: button` ×2 (260/280), `Type: badge` ×2 (299/406), `Type: tab` ×2 (318/338), `Type: input` (357), `Type: listItem` (376), `Type: card` (397). All nine YAML `use` strings restored verbatim as `Token-set use:` lines (266/285/304/323/342/362/382/401/409). The `font` shorthand byte forms are kept as `token-set font record` lines (264/284/303/322/341/361/379/408). The two `active` records are kept verbatim: `active text #0096ff bold` at 323 and `#fa4065 with bottom border` at 342. |
| §1 Visual Theme & Atmosphere (legacy 61–76) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Key Characteristics as Distinctive traits 35–41. The atmosphere readings ("a working jukebox interface", the gray-scaffolding rationale, `#fa4065` as "this is the thing you press") carry an adjacent complete qualifier at 11; the trait list carries one at 33 (B2/B2a). |
| §2 Color Palette & Roles (legacy 78–105) | 옮김 → Foundations semantic color | `DESIGN.md` 112–138. The source's own group headings (Primary, Text Neutrals, Interactive, Surface & Borders, Chips, On-color) are kept as the grouping. The characterizing phrases the source attaches to three roles are covered by the adjacent qualifier at 108 (B2/B2a). |
| §3 Typography Rules — family, hierarchy (legacy 107–122) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 192–199; family 203–204; hierarchy table 209–215. The source's "no custom web font is loaded / platform `dotum`/Gothic rendering" statement is at 196. |
| §3 Principles — 12px workhorse, weight lever, two sizes plus nav, normal tracking (legacy 124–128) | 옮김 → Typography & Assets Type rules | The observable half of the scale is stated plainly at `DESIGN.md` 223; the interpretive half sits inside the qualified sentence at 225 (B2/B2a). |
| §4 Component Stylings (legacy 130–190) | 옮김 → Components & States | `DESIGN.md` 258–409. §4 body values and YAML `use` strings are both kept where they differ — for example the ghost button's `Background: transparent` and `Border: 1px solid #d2d2d2` (283) alongside the token-set record (285). |
| Footer **Verified** / **Tier 1 sources** block (legacy 192–195) | 분리 → provenance; 두 URL 옮김 → Experience Scope + Typography & Assets + Governance | Freshness `provenance.md` 27–36; Tier 1 list 46–49. `https://company.genie.co.kr` is dual: `DESIGN.md` 9/199/501 + `provenance.md` 49/73/119 (E2a). |
| §5 Layout Principles (legacy 197–218) | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 414 (spacing scale, centered fixed-width column, multi-column chart blocks, header/main/footer zones, square thumbnails, radius scale), 142 (spacing), 146–151 (radius). The Korean-portal reading and the density-first whitespace policy carry an adjacent complete qualifier at 416 (B2/B2a). |
| §6 Depth & Elevation (legacy 220–229) | 옮김 → Foundations elevation | Four-level table `DESIGN.md` 155–160, including the Ring (Accessibility) row verbatim. The Ring row's scope reading is qualified at 162, and the Shadow Philosophy paragraph is carried as a qualified reading at 164 (B2/B2a). The Ring row is deliberately not attached to any component `focus-visible` row: `focus-visible` occurs 0 times in the source, measured with `grep -o 'focus-visible' web/references/genie/DESIGN.md | wc -l` (B1); recorded at `provenance.md` 122. |
| §7 Do's (legacy 233–240) | 옮김 → Experience application rules | `DESIGN.md` 61–67, under the grouping qualifier at 57. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 242–249) | 옮김 → Experience avoid | `DESIGN.md` 84–90, under the qualifier at 80. |
| §8 Responsive Behavior (legacy 251–276) | 옮김 → Layout & Platforms; image behavior 옮김 → Typography & Assets Assets | Breakpoint table `DESIGN.md` 420–425 with the "declared behavior rather than an observation" note at 418; touch targets 427; collapsing strategy 429. The artwork/placeholder behavior is at 230, where the "first-party catalog content" reading is qualified (B2/B2a). The 25px chip height and 12px horizontal padding are also the source's own evidence that the chip is a tap target, which is why the chip is declared interactive at `DESIGN.md` 300 (A3). |
| §9 Agent Prompt Guide — Quick Color Reference, Example Component Prompts, Iteration Guide (legacy 278–308) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present — the twelve hexes, the 4px/8px/13px radii, the 11px/12px/18px sizes, the 400/700 weights, the 8px 18px and 0px 12px and 0px 8px paddings, the `rgba(0,0,0,0.12) 0px 4px 12px` panel shadow, and the `1px #eef1f4` / `1px #d2d2d2` borders (A2, A3). The check is itemised at `provenance.md` 100. |
| §10 Voice & Tone (legacy 311–324) | 옮김 → Content & Locales | Published strings `DESIGN.md` 436–446; register table 450–457; forbidden register 459. The qualifier precedes the derived material at 448, so the table and the forbidden-register rule are inside its scope (B2/B2a). The source's own English gloss of the tagline is kept and marked as a reader aid that never replaces the label at 438 (A5). |
| §11 Brand Narrative (legacy 326–332) | 옮김 → Experience Scope | `DESIGN.md` 13: 지니뮤직 (Genie Music Corporation), the KT (Korea Telecom) affiliation, the Melon/FLO market placement, the tagline, the genie/lamp metaphor, and the embraces/avoids pair. Marked there as narrative context that supplies no interface tokens; the Korean-web-portal heritage reading carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 123. |
| §12 Principles — 7 numbered (legacy 334–342) | 옮김 → Experience principles | `DESIGN.md` 47–53 under the fixed B2a form at 45: "These seven items are a derived editorial implementation inference from the verified surface; they are not Genie Music-authored or a separately published UI specification." |
| §13 Personas — 4 entries (legacy 344–354) | 삭제 | The source's own italic line at legacy 346 labels them fictional archetypes informed by publicly observable segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or segment appears in either output (D2). Disposition at `provenance.md` 97. Audience at `DESIGN.md` 29 carries only the group-level description the source's own §12 states, under an adjacent qualifier. The four `primary-tasks` at 21–24 come from modules and labels the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 10 rows (legacy 356–369) | 옮김 → Components & States surface state contract + per-component applicability | Full ten-row body preserved at `DESIGN.md` 241–250 (A2; the catalog graph is still 0/440, so nothing is delegated), including the `#fa406580` disabled value and the published strings "검색 결과가 없습니다" and "오류가 발생했습니다". The `Active` and `Selected` rows are kept as their own observations rather than folded into a canonical state — noted at 252 and carried on the components at 324, 343, 383. The applicability rule is at 256, whose qualifier covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 7 interactive components × 7 states = 49 rows: 40 `applicable` and 9 `not-applicable`, the latter only with a role reason (C2 v10) — the search keyword chip 312–314, the chart scope tab 332–334, and the global nav item 351–353 each hand off or navigate and commit no operation of their own. The primary play button 270–276, the ghost row button 289–295, the header search input 366–372, and the track row 387–393 keep loading/error/success `applicable` with treatments omitted, three of them citing the surface state contract. The Card / Panel gets no `kind` and no map because the source supplies no interaction evidence for it — `DESIGN.md` 402 (C4). The Rank Numeral Badge declares `kind: non-interactive` with a reason instead of a map — 407 (Core §4.4). |
| §15 Motion & Easing — durations, easing token names/roles, forbidden rule, signature motions, reduced-motion (legacy 371–397) | 옮김 → Foundations motion | `DESIGN.md` 172–177 (the four duration tokens with their uses), 179 (three easing token names and roles), 181 (forbidden bounce/overshoot and the `prefers-reduced-motion: reduce` behavior), 183 (three signature motions). |
| §15 Motion & Easing — three `cubic-bezier` values (legacy 386–388) | 삭제 → provenance omission ledger | No observation stands behind them: the source records one Tier 1 live inspect of one homepage and supplies no transition, animation, or easing sample. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to the example table at `spec/omd-v0.1.md` line 262, the documented re-injection path. Ledger at `provenance.md` 94–96. Occurrence counts by `grep -o cubic-bezier <file> | wc -l` → `DESIGN.md` 0, `provenance.md` 3, `migration-log.md` 3 (all three on this row: the section label, the `ease-exit` value, and the counting command itself). B3 is held: the promotion condition at `DESIGN.md` 179 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). |
| §16 Do's and Don'ts (Brand Philosophy) (legacy 399–415) | 옮김 → Experience application rules + avoid | Kept as its own labelled group rather than merged into §7, because the two lists overlap without being identical and a migrator does not pick the more plausible of two authorities: `DESIGN.md` 71–76 (Do) and 94–99 (Don't). One extra line at 100 restates this contract's scope boundary as a prohibition; 80 qualifies it separately. |

## Sibling handling (`web/references/genie/.verification.md`)

The sibling exists — confirmed with `find web/references/genie -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact.

- Its full record is transcribed at `provenance.md` 61–73 and is **not** promoted into `DESIGN.md`.
- Values it carries that the migration source does not: the font stack `dotum, 돋움, sans-serif`, the body line-height 18px, the utility-menu line-height 24px and padding `0px 18px 0px 5px`, every `rgb()` byte form, and the frequency counts (×6138, ×712, ×429, ×365, ×10, ×4). Recorded at `provenance.md` 77–79.
- Its structural classifications were likewise not promoted (B1): the `.gnb a` selector name, the `<title>` element identification, the `body`/`h1` element naming, and — the one that matters most here — the sibling's classification of the search-keyword chip as a `link`. The portable body declares that chip interactive on the source's own §8 touch-target line (25px height, 12px horizontal padding) at `DESIGN.md` 300, not on the sibling's element classification. Recorded at `provenance.md` 57.
- Two published strings appear only in the sibling: `로그인/회원가입` (the source writes the same labels as `로그인·회원가입`) and `지니(genie) - 음악, 노래, 비디오` (the Apple App Store KR listing title). Both are preserved byte-for-byte at `provenance.md` 71/73/80 (A5).

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Measured at audit time: `coverage.copy-loss compared 7 / candidates 144` — **4.9%**. `compared < candidates`, so the A5a hand sweep is mandatory and was run. The six machine needles were 지니뮤직, 로그인·회원가입, 그리고 설레임, 검색 결과가 없습니다, 실시간 차트, 오류가 발생했습니다; every published label joined by `/` or `,` (종합, 국내, 국외, 듣기, 담기, 다운, 검색, 차트, 최신음악, 라디오, 매거진, 인기검색어, 최근검색어) is under the four-character run threshold or split by a separator, so none of them was a needle.

| Sweep | Extracted | Missing from all three outputs | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All Korean runs in the source, any length | 14 distinct | 0 | 0 | — |
| All Korean runs in the sibling, any length | 9 distinct | 0 | 0 | — |
| All backtick / straight-double / curly-double / 「」/（）/() delimited strings in the source, 2–80 chars | 145 | 5 | 0 | See the disposition list below. |
| All the same in the sibling | 42 | 2 | 0 | `Tier 1 / brand-owned, KR` and `live brand surface; tagline "지니 : 음악, 그리고 설레임" confirmed in DOM \`<title>\`` are the sibling's own classification and description text, not brand-published strings. The tagline inside the second one survives at `DESIGN.md` 13/438 and `provenance.md` 72/73/80. |
| `node test-v2/tools/latin-copy-audit.mjs --brand genie` | 24 candidates | 3 reported | 0 | Candidate output, not a verdict. Two are §13 persona prose (`redesign`, `fast and to the point`), deleted with the personas under D2. The third is `this is the thing you press.` — the source's own editorial sentence, whose label survives at `DESIGN.md` 11 without the sentence-final period. |

Disposition of the five source quotations that do not survive verbatim:

1. `this is the thing you press.` — survives at `DESIGN.md` 11 without the trailing period; the period is sentence punctuation outside the quoted label.
2. `strengthens to #27282d on hover` — a §9 prompt fragment. The fact survives at `DESIGN.md` 379 ("title strengthens to `#27282d` on hover/selection") and in the token-set use string at 382.
3. `#fa4065, 4px radius, 8px 18px padding, white 12px/700 text, hover #d62952` — a §9 prompt fragment. Every value in it is on the Primary Listen / Play Button at `DESIGN.md` 260–266.
4. `redesign` — §13 persona prose, deleted under D2.
5. `fast and to the point` — §13 persona prose, deleted under D2.

Sub-needle labels the machine check could not see were confirmed present individually in `DESIGN.md`: 종합, 국내, 국외, 듣기, 담기, 다운, 검색, 차트, 최신음악, 라디오, 매거진, 인기검색어, 최근검색어, 최근검색어 전체삭제, 설레임, 돋움, 지니뮤직, 실시간 차트, 로그인·회원가입, 검색 결과가 없습니다, 오류가 발생했습니다, 지니 : 음악, 그리고 설레임.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand genie --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 7, candidates: 144 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, `sourceValidation.valid: true`, and 0 dropped segments. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are.

Token accounting, measured rather than asserted: the source carries 36 distinct hex/px/ms/pct tokens. All 36 survive in the portable `DESIGN.md` itself, 0 are lost across the three outputs, and 0 tokens appear in the portable body that the source does not carry.

## Deviations recorded

- `DESIGN.md` is 6,161 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: nine declared components with dual `use`/`font` records, a ten-row surface state contract, a 49-row applicability matrix, two separate Do lists and two separate Don't lists that may not be merged, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- The source names two Tier 1 URLs but supplies computed evidence for only one of them. The corporate URL is therefore carried as a named source with no design value attached, in Scope, in the Font-evidence table, and in Named gaps, rather than being treated as a second captured surface.
- The source's `#fa406580` disabled value is an 8-digit form the token scanner does not see. It is carried anyway, at `DESIGN.md` 249 and `provenance.md` 87.

## Mandatory passes

- **F1 (B2a scan).** `DESIGN.md` was re-read from line 1 and every causal, interpretive, or judgement sentence was classified against its evidence class. Eight edits followed. Line numbers are post-edit positions.
  1. Semantic color (108) — the three characterizing phrases the source attaches to roles ("the one warm note in a gray system", "a near-black with a faint warm cast", "the one cool interactive signal") were sitting unqualified beside the values. A qualifier now precedes the role list.
  2. Elevation Ring row (162) — the sentence classifying the Ring row as surface-level rather than per-control was my judgment stated as fact. It is now qualified, and the values in the row are untouched.
  3. Motion (168) — the qualifier was moved from the end of the subsection to the top so that the duration table, the easing roles, the forbidden-motion rule, and the signature motions all fall inside its scope rather than trailing it.
  4. Motion promotion condition (179) — a sentence was added recording that the condition is this document's, not a Genie Music rule, so the B3 text is not read as a brand-published requirement.
  5. Font evidence, distributed-asset row (196) — the causal "so no Genie-distributed type asset is claimed" was split from the source's statement, so the inference is not presented as part of what the source says.
  6. Type rules (223) — "there is no size above 18px" was bounded to "in this scale"; unbounded it was a claim about every Genie surface.
  7. Assets (230) — "album artwork is first-party catalog content" was a reading presented as a fact; the observable half now stands plainly and the reading is qualified.
  8. Layout (418) — a draft sentence asserted the inspect was a desktop-web capture. The source states no viewport. The sentence now says only that the source records no measurement at these widths, which is what makes the breakpoint table declared behavior.
- **F2 (E2 collation).** Every row above was grepped against the three files before it was written; dual destinations are recorded on both sides (E2a); the §15 curve row records both the deletion and its provenance ledger location (E2b). The only compliance claim made in this log is B3, and it is made because `DESIGN.md` 179 carries the five evidence kinds and the per-component gate in full text (E2c).

DONE migrated=1
