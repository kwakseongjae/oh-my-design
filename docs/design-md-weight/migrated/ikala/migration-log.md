# ikala migration log

Source: `web/references/ikala/DESIGN.md`
Sibling read (not the migration input): `web/references/ikala/.verification.md`
Destination: `docs/design-md-weight/migrated/ikala/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/ikala/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use Python `str.count` / `grep -oF` per file, never `grep -c`, which counts lines.

Source SHA-256 `0e727b1028b5752d81df85c6065ddf9b43934f82cd3ef451b673721dc517952e` (`web/references/ikala/DESIGN.md`). Sibling SHA-256 `cd18e5132dd577346c2e5821ec81316a0b8f45153cb1bb9696b5e33d4b290ba9` (`web/references/ikala/.verification.md`). Worker-close portable DESIGN SHA-256 `b7c8dd31836dbb24ae78000f3cae952d10685395bc3b267a368f7e3d946fe831`. Auditor DESIGN SHA-256 `bc7e06196b6999bc1675ae6c5eddf830812e67dfb82e3b5f7bd45e14371044f4`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# iKala Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–21. Homepage `https://ikala.ai` as its own URL (not only as a prefix of `/tw/blog`) is dual: `DESIGN.md` 9/154/250 + `provenance.md` 13/44/49/72 (E2a). `https://ikala.tw` is dual: `DESIGN.md` 9/154 + `provenance.md` 45/50/56 (E2a). `#061232` is dual: `DESIGN.md` dest 8 + `provenance.md` dest 9 (E2a). Favicon slug is dual: `DESIGN.md` 230 + `provenance.md` 16 (E2a). `logo.type` is dual: `DESIGN.md` 230 + `provenance.md` 15 (E2a). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 19–21, 32–35. The source footer's **Verified:** 2026-06-08 is at `provenance.md` 37. `live-extract` DESIGN 0 / provenance 1 (`provenance.md` 19). `components_harvested` DESIGN 0 / provenance 2 (`provenance.md` 21/176). These are ledger keys with no portable slot. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations; 분리 → provenance | Full note transcribed at `provenance.md` 64. The facts it names (deep navy CTA fill `#061232` live on `Get in Touch`; brand-blue accent `#3a82dd` for links/nav; family `Noto Sans TC`, Taiwan-localized) land in `DESIGN.md` 11/37/92/98/199. Line 9 is Scope URLs only and does not carry those note facts. Dual (E2a). |
| YAML `tokens.colors` (14 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 92–114. All fourteen roles kept with names, values, and token-set keys: primary `#061232`, primary-deep `#031234`, accent-blue `#3a82dd`, accent-blue-strong `#4770df`, accent-blue-deep `#2168c2`, link `#2563eb`, canvas `#ffffff`, heading `#333333`, body `#525151`, muted `#6b7280`, faint `#999999`, on-primary `#ffffff`, hairline `#edf1f7`, ink `#000000`. `#ffffff` as `canvas` is the page-background / card-surface / hero-text role (`DESIGN.md` 94); `#ffffff` as `on-primary` stays a separate key (`DESIGN.md` 114). |
| YAML `tokens.typography.family.sans` / `family.fallback` | 옮김 → Typography & Assets Family | Family `DESIGN.md` **199**. Live computed `DESIGN.md` **191**. Declared-only fallback classing **193** (not Family). |
| YAML `tokens.typography.display-hero / section / subheading / body-lg / body / button / caption` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 207–214. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.14` dest 2 · `1.40` dest 3 · `1.00` dest 5 · `1.55` dest 3. All seven YAML `use` strings restored verbatim in the Token-set use column (A1, A3). Caption has no YAML `lineHeight`; the source table writes `normal` (`DESIGN.md` 214) and no ratio is invented. Nav Active is a source-table row, not a YAML typography key (`DESIGN.md` 213). YAML tracking stays on its keys at `DESIGN.md` 216: `display-hero` `-3.49`, `section` `-1.50`, `subheading` `-1.00`. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) / `tokens.shadow` (3 strings) | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 124 (`xs 4 · sm 8 · md 12 · base 16 · lg 24 · xl 30 · xxl 48 · section 80`). Shape: `DESIGN.md` 128 (`sm 4 · md 8 · lg 30 · full 9999` plus the named uses). `tokens.rounded.full: 9999` dest 3 (`DESIGN.md` 135/137/497). Line 396 is the shorter `full: 9999` restatement, not that key path. The unitless step `full 9999` is at `DESIGN.md` 128. `tokens.spacing.xs: 4` is disambiguated from `tokens.rounded.sm: 4` at 124/137. `tokens.spacing.sm: 8` is disambiguated from `tokens.rounded.md: 8` at 124/137. `tokens.spacing.base: 16` is disambiguated from type 16 at 124. `tokens.spacing.lg: 24` is disambiguated from the 24px type size at 124. `tokens.spacing.xl: 30` is disambiguated from `tokens.rounded.lg: 30` and from the 30px half of `20px 30px` at 124. Shadows at `DESIGN.md` 148: ambient / standard / elevated. |
| YAML `tokens.components` (6 records) | 옮김 → Components & States | `DESIGN.md` 246–371. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×2 (249/274 = YAML button-primary + button-ghost), `badge` ×2 (297/365 = link-inline + section-title), `card` ×1 (318), `tab` ×1 (341). All six YAML `use` strings restored verbatim as `Token-set use:` lines (258/281/302/324/349/371). YAML `font` shorthands kept as `token-set font record` lines: `18px / 500` dest 1 · `20px / 500` dest 1 · `18px / 400` dest 1 · `18px / 700` dest 1 · `36px / 600` dest 1. The §4-only chips/pills block is `not in the token set` (`DESIGN.md` 331). No §4-only component borrowed a `Primitive type` from another YAML record. |
| §1 Visual Theme & Atmosphere (legacy 60–76) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 36–43. The atmosphere readings (measured-and-trustworthy, trust-through-clarity, market-honest Noto, top-down hierarchy, enterprise-grade-without-cold) carry an adjacent complete qualifier at 11 (B2/B2a). The Taiwan-headquartered B2B reading in ¶1 is named in the qualifier at 9. |
| §2 Color Palette & Roles (legacy 78–104) | 옮김 → Foundations semantic color | `DESIGN.md` 92–120. Characterizing phrases covered by the adjacent qualifier at 88 (B2/B2a). Canvas / on-primary key-unmerge at 114 has its own adjacent complete qualifier. Shadow color names at 118–120. |
| §3 Typography Rules — family, hierarchy, principles (legacy 106–130) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 188–195; family 199–201; hierarchy table 207–214; type rules 218–226. The Official-product-use, Official-distributed-asset, Declared-only, License, Outside, Family (single-family + fallback), Type-roles ratio, and Type-rules readings carry adjacent complete qualifiers at 190, 192, 193, 194, 195, 201, 216, and 226 (B2/B2a). |
| §4 Component Stylings (legacy 132–188) | 옮김 → Components & States | `DESIGN.md` 246–371. §4 body values and YAML `use` / font / radius / height forms are both kept. Chips/pills (legacy 166–170) are §4-only and carry `not in the token set` at 331. |
| Footer **Verified** / **Tier 1 sources** / **Method** / **`.verification.md`** (legacy 192–195) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 32–35; Tier 1 list 49–50. `https://ikala.ai` is dual: `DESIGN.md` 9/154/250 + `provenance.md` 13/44/49/72 (E2a). `https://ikala.tw` is dual: `DESIGN.md` 9/154 + `provenance.md` 45/50/56 (E2a). Conflicts unresolved: none — `provenance.md` 39. |
| §5 Layout Principles (legacy 197–219) | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 391–396 (centered column, full-bleed hero, industry grids, navy bands, spacing 4/8/12/16/24/30/48/80, shape 4/8/30/9999) and 398 (generous-and-calm / visual-descent / predictable-eye-path). `58px` dest 2 at 255/409. `20px 30px` dest 2 at 124/253. |
| §6 Depth & Elevation (legacy 221–234) | 옮김 → Foundations elevation | `DESIGN.md` 141–150: four-level table, three token-set shadow strings. The clean-and-contemporary / lifted-but-never-heavy reading carries an adjacent qualifier at 150 (B2/B2a). |
| §7 Do's and Don'ts overview (legacy 236–242) + §16 Do/Don't (legacy 380–400) | 옮김 → Experience application rules + Avoid | `DESIGN.md` 60–67 (eight Do rules) under the grouping qualifier at 58; `DESIGN.md` 73–80 (eight Don't rules) under the qualifier at 71. Not placed in Governance controlled copy. No invented domain is added. |
| §8 Responsive Behavior (legacy 244–267) | 옮김 → Layout & Platforms | `DESIGN.md` 400–418: Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` / Large Desktop `>1440px` at 404–407, `58px` targets at 409, collapsing strategy 413–416, image behavior at 418. The Desktop row keeps the source body's `1024-1440px` range at 406. The system-level-not-cross-viewport reading carries an adjacent qualifier at 400 (B2/B2a). The image-rule restatement carries an adjacent qualifier at 418. |
| §9 Agent Prompt Guide (legacy 269–296) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 129. |
| §10 Voice & Tone (legacy 300–312) | 옮김 → Content & Locales | Published strings `DESIGN.md` 433–456; tone table 425–431; forbidden register 458. The qualifier at 423 covers the voice reading and the tone-table contract (B2/B2a, A5). The premise-to-register causal at 458 has its own adjacent complete qualifier. Byte-exact / gloss-beside close at 460. |
| §11 Brand Narrative (legacy 314–320) | 옮김 → Experience Scope | `DESIGN.md` 13: `2015`, `Sega Cheng`, *"Data → Intelligence → Impact"*, `AI cloud / AI transformation`, manufacturing / retail / finance, `iKala Commerce / KOL Radar`, Greater China and Southeast Asia, `Google Cloud partner`, `black-box novelty`. The source paragraph's last sentence is kept as one unit: the bilingual Taiwan-rooted identity is not incidental; the brand bet is that the most trusted AI partner for an APAC enterprise is `unmistakably local` in language and `unmistakably credible` in presentation. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 178. |
| §12 Principles — 6 numbered (legacy 322–329) | 옮김 → Experience principles | `DESIGN.md` 49–54 under the B2a form at 47: "These six items are a derived editorial implementation inference from the verified surfaces; they are not iKala-authored or a separately published UI specification." No separately published iKala UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 137–171 (35 rows). |
| §13 Personas — 4 entries (legacy 331–341) | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, motivation, or occupation classification appears in either output (D2, D2a). Disposition at `provenance.md` 123. Audience at `DESIGN.md` 30 carries only the group-level description the source independently records (APAC enterprise AI buyers, retail/manufacturing digital-transformation leads, brand marketers using influencer intelligence), under an adjacent qualifier. The five `primary-tasks` at 21–25 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 6 rows (legacy 343–352) | 옮김 → Components & States surface state contract + per-component applicability | Full six-row body preserved at `DESIGN.md` 377–384 (A2; the catalog graph is still 0/440, so nothing is delegated). The applicability rule is at 240–244, whose qualifier at 244 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary (Navy), Ghost / Light, Text / Inline Link, and Navigation close loading/error/success with a role reason (C2 v10) — they commit no operation in place (`loading \| not-applicable` dest 4 · `loading \| applicable` dest 0). Feature / Industry Card gets no `kind` and no map because the hover glow is a recorded visual, not evidence that the card is a commit control — `DESIGN.md` 326 (C4), with its own adjacent qualifier. Badges / Chips / Pills and Section Titles declare `Kind: non-interactive` (`DESIGN.md` 332/366 dest 2). `kind: non-interactive` DESIGN 0. |
| §15 Motion & Easing — durations, easing roles, signature motions, reduced-motion (legacy 354–378) | 옮김 → Foundations motion | `DESIGN.md` 160–163 (0ms / 150ms / 250ms / 400ms with uses), 169–171 (ease-standard / ease-enter / ease-exit roles and uses, curves omitted), 175–180 (card hover-lift, link color shift, section reveal, `prefers-reduced-motion: reduce` collapses to `motion-instant`). |
| §15 Motion & Easing — three curve values (legacy 367–371) | 삭제 → provenance omission ledger | `cubic-bezier(0.25, 0.1, 0.25, 1)`, `cubic-bezier(0.2, 0.6, 0.25, 1)`, and `cubic-bezier(0.4, 0.0, 1, 1)` are named as omitted at `DESIGN.md` 165 and are not promoted as tokens. Durations stay. Ledger at `provenance.md` 124–126. B3 is held: the promotion condition at `DESIGN.md` 173 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 496. |
| Opening product / atmosphere sentence (legacy 62) | 옮김 → Experience Scope | `DESIGN.md` 9–11: Taiwan-headquartered B2B AI and martech; two inspected surfaces; `#ffffff` canvas; deep navy `#061232`; `Noto Sans TC`. |

## Sibling handling (`web/references/ikala/.verification.md`)

The sibling exists — confirmed with `find web/references/ikala -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 72–83 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: hero h1 sample `Home`; computed line-height px `95.67px` / `50.4px`; frequency counts; playwright `--disable-http2` and the 1500-element sweep; document.title prefix `iKala - Data → Intelligence → Impact`; `https://www.kolradar.com`; `https://ikala.ai/tw/blog/`; rgb() forms. Recorded at `provenance.md` 87–93.
- Measured `DESIGN.md` 0 for those sibling-only strings: `95.67px` 0 · `50.4px` 0 · `kolradar.com` 0 · `Home` 0.
- `Get in Touch` / `Try it Now` / `Manufacturing` / `Total AI Transformation Solutions and Services` / 58px / `20px 30px` / `Noto Sans TC` are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. iKala's published UI copy is English-forward (`Get in Touch`, `Data → Intelligence → Impact`), so a hand sweep of published labels is mandatory even when the needle set is empty or thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 16 distinct | 0 | 0 | Get in Touch / Contact / Try it Now / Data → Intelligence → Impact / Total AI Transformation Solutions and Services / Manufacturing / Retail / Finance / iKala / iKala Commerce / KOL Radar / learn more / Noto Sans TC / Noto Sans / Sega Cheng / Google Cloud. |
| `node test-v2/tools/latin-copy-audit.mjs --brand ikala --candidate docs/design-md-weight/migrated/ikala/DESIGN.md` | 0 lost | 0 | 0 | Tool reported `scanned: 1`, `clean: 1`, `totalLost: 0`. |
| Sibling published strings | 2 distinct sibling-only | 0 | 0 | Hero sample `Home` and document.title prefix `iKala - Data → Intelligence → Impact` survive in provenance; neither is promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: Get in Touch, Contact, Try it Now, Data → Intelligence → Impact, Total AI Transformation Solutions and Services, Manufacturing, Retail, Finance, iKala Commerce, KOL Radar, Sega Cheng, 2015, unmistakably local, unmistakably credible, black-box novelty.

A5 분모: hand sweep of source published labels 16 extracted / 0 missing; latin-copy-audit 0 lost; gate `copy-loss` compared 0 / candidates 155.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand ikala --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 155 }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 155.

## Deviations recorded

- `DESIGN.md` is 6,666 words by `wc -w` / Python split, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: six declared component records plus one §4-only chip block, a six-row surface state contract, a seven-state applicability matrix on four interactive controls, `tokens.rounded.full: 9999` kept off matching spacing keys, the full §11 founding-and-portfolio narrative including its last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published iKala UI specification is named in the source, so every derived-editorial close uses the toss-form `not iKala-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Worker-close measure: `derived editorial implementation inference` DESIGN 35 = `not iKala-authored` 35 = `separately published UI specification` 35. Provenance derived ledger is 35 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or judgment sentence — Scope so/because sentences, Content voice readings, Docs-citation characterizations, Font evidence classing, C4 withholding, Layout image restatement — has an adjacent complete qualifier naming derived editorial implementation inference and `not iKala-authored or a separately published UI specification`. Principles use the toss-form header. No 1차 published UI specification is named, so the 전제 주석 does not require a Pajamas/Polaris-style close. Inventory 35 = body 35.

## F2 E2 contrast (mandatory final pass)

Each log row was written only after `grep` / Python `str.count` confirmed the destination. Dual destinations name both files. Compliance claims (`B3` held, `B2a` 35=35, `loading \| applicable` dest 0) match the body: B3 full text at `DESIGN.md` 173 and 496; `loading \| applicable` DESIGN 0; `Primitive type: \`button\`` dest 2 = YAML `type: button` 2; persona identifiers DESIGN 0 / provenance 0.
