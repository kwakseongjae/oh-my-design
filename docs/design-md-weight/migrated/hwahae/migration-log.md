# hwahae migration log

Source: `web/references/hwahae/DESIGN.md`
Sibling read (not the migration input): `web/references/hwahae/.verification.md`
Destination: `docs/design-md-weight/migrated/hwahae/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hwahae/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `acac6595192838a29097845951d88b1a51ada788916fb923f673b04451f44c74` (`web/references/hwahae/DESIGN.md`). Sibling SHA-256 `6ed0ecd0f4b51e4836d36c853182ac5cba7113cad6931fca975d6830cc8f4c57` (`web/references/hwahae/.verification.md`). Worker-close portable DESIGN SHA-256 `80ac7377f8ba71089829c24c8365befc08337913d557d4f8dfb163e0fb9dbe3b`. Auditor-close portable DESIGN SHA-256 `10d6e6906076f080d58b35554e600a957698cbe0742e056508373aa8ec68eb4c`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Hwahae Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–22. Homepage `https://www.hwahae.co.kr` as its own URL (not a prefix of the blog host) is dual: `DESIGN.md` 9/21/249 + `provenance.md` 14/45/52 (E2a). `#00d5ce` is dual: `DESIGN.md` 11/35/52/61/92/148/229/250 + `provenance.md` 15/63/65/94/102/152 (E2a). Favicon slug is dual: `DESIGN.md` 228 + `provenance.md` 17 (E2a). `화해` is dual: `DESIGN.md` 9/13/490 + `provenance.md` 11. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 19–22, 32–35. The source footer's **Verified:** 2026-06-26 is at `provenance.md` 37. `live-extract` DESIGN 0 / provenance 1 (`provenance.md` 20). `components_harvested` DESIGN 0 / provenance 2 (`provenance.md` 22/197). These are ledger keys with no portable slot. |
| YAML `tokens.note` | 옮김 → Experience Scope + Foundations; 분리 → provenance | Full note transcribed at `provenance.md` 65. The facts it names (turquoise flower mark `#00d5ce` / live blog `#22d3d6` / darker UI teal `#00a5aa`; amber `#ffaa3c`; Pretendard Variable on `#f7f7f7`; Spoqa Han Sans on the blog) land in `DESIGN.md` 11/35–37/92–99. Dual (E2a). |
| YAML `tokens.colors` (19 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 92–119. All nineteen roles kept with names, values, and token-set keys: brand, brand-bright, brand-deep, tint, rating, info, alert, ink, ink-soft, ink-blog, body, muted, faint, placeholder, divider, hairline, canvas, surface, on-brand. `#ffffff` as `canvas` is the card/cleanest-background role (`DESIGN.md` 115); `#f7f7f7` as `surface` is the page background (`DESIGN.md` 116); `#ffffff` as `on-brand` stays a separate key (`DESIGN.md` 119). |
| YAML `tokens.typography.family.product` / `family.blog` | 옮김 → Typography & Assets Family | Product family `DESIGN.md` **198**. Blog family `DESIGN.md` **199**. Live computed `DESIGN.md` **190**. Declared-only fallback classing **192** (not Family). |
| YAML `tokens.typography.blog-display / section / nav / card-title / body / label / caption` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 207–213. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.44` dest 2 · `1.33` dest 2 · `1.5` dest 6. All seven YAML `use` strings restored verbatim in the Token-set use column (A1, A3). YAML `tokens.typography.blog-display.tracking: -1.0` stays unitless at `DESIGN.md` 215 beside visible `-1.0px`. YAML `tokens.typography.section.tracking: -0.2` stays unitless beside `-0.2px`. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (5 steps) / `tokens.shadow` (3 keys) | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 123 (`xs 4 · sm 8 · md 12 · base 16 · lg 20 · xl 24 · xxl 32 · section 48`). Shape: `DESIGN.md` 127 (`xs 4 · sm 8 · md 16 · lg 20 · full 99999` plus the named uses). `tokens.rounded.full: 99999` dest 4 (`DESIGN.md` 135/136/138/552). The unitless step `full 99999` is at `DESIGN.md` 127. Body-named circle `50%` dest 5 is a different key (`DESIGN.md` 11/136/138/447/552). `tokens.spacing.base: 16` is disambiguated from `tokens.rounded.md: 16` at 123/138. `tokens.spacing.lg: 20` is disambiguated from `tokens.rounded.lg: 20` at 123/138. `tokens.shadow.card` / `hairline-ring` / `none` at `DESIGN.md` 148. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | `DESIGN.md` 245–408. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×2 (248/270 = YAML primary + outline), `tab` ×1 (400), `card` ×2 (341/354), `input` ×1 (319), `badge` ×2 (365/377). All eight YAML `use` strings restored verbatim as `Token-set use:` lines (255/280/326/346/358/372/384/408). YAML `font` shorthands kept as `token-set font record` lines: `16px / 600` dest 1 · `12px / 400` dest 1 · `15px / 400` dest 1 · `12px / 600` dest 2. Language / Icon Button and Image Count Pill are §4-only and say `not in the token set` (304/395) — no borrowed `Primitive type`. |
| §1 Visual Theme & Atmosphere (legacy 68–85) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 35–43. The atmosphere readings (imagery-carries-color, beauty index, encyclopedic weight, amber as score/trust) carry an adjacent complete qualifier at 11 (B2/B2a). The dominant-platform reading in ¶1 is named in the qualifier at 9. |
| §2 Color Palette & Roles (legacy 87–114) | 옮김 → Foundations semantic color | `DESIGN.md` 92–119. Characterizing phrases covered by the adjacent qualifier at 88 (B2/B2a). Canvas / on-brand key-unmerge at 119 has its own adjacent complete qualifier. |
| §3 Typography Rules — family, hierarchy, principles (legacy 116–138) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 189–194; family 198–201; hierarchy table 207–213; type rules 218–224. The Official-product-use, Official-distributed-asset, Declared-only, License, Outside, Family (two-surfaces-never-mix + fallback), Type-roles ratio, and Type-rules readings carry adjacent complete qualifiers at 189, 191, 192, 193, 194, 201, 215, and 224 (B2/B2a). |
| §4 Component Stylings (legacy 140–223) | 옮김 → Components & States | `DESIGN.md` 245–419. §4 body values and YAML `use` / font / radius / height forms are both kept. Language / Icon Button (293–314) and Image Count Pill (386–395) have no YAML row. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 227–230) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 32–35; Tier 1 list 51–54; Tier 2 57–59. `https://www.hwahae.co.kr` is dual: `DESIGN.md` 9/21/249 + `provenance.md` 14/45/52 (E2a). `https://blog.hwahae.co.kr/` is dual: `DESIGN.md` 9 + `provenance.md` 46/53 (E2a). `https://blog.hwahae.co.kr/all/tech/13236` is dual: `DESIGN.md` 9 + `provenance.md` 47/54 (E2a). Conflicts unresolved: none — `provenance.md` 39. |
| §5 Layout Principles (legacy 232–255) | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 442–447 (centered content-first, 18px titles, white cards on `#f7f7f7`, 16px cards, spacing 4/8/12/16/20/24/32/48, shape 4/8/16/20/99999) and 449 (imagery-carries-color / dense-but-breathable / flat tint-and-hairline). 44px utility height at 302/459. Circle `50%` dest 5 at 11/136/138/447/552. |
| §6 Depth & Elevation (legacy 257–265) | 옮김 → Foundations elevation | `DESIGN.md` 142–148: three-level table, `tokens.shadow.card` / `hairline-ring` / `none`. The near-flat / imagery-not-chrome reading carries an adjacent qualifier at 148 (B2/B2a). |
| §7 Do's (legacy 269–277) | 옮김 → Experience application rules | `DESIGN.md` 60–67 (eight Do rules), under the grouping qualifier at 58. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 279–287) | 옮김 → Experience avoid | `DESIGN.md` 73–80, under the qualifier at 71. The eight prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior (legacy 289–311) | 옮김 → Layout & Platforms | `DESIGN.md` 451–467: Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` at 455–457, 44px / 24px targets at 459, collapsing strategy 461–465, image-count pill at 467. The Desktop row keeps the source body's `1024-1440px` range at 457. The system-level-not-cross-viewport reading carries an adjacent qualifier at 451 (B2/B2a). |
| §9 Agent Prompt Guide (legacy 313–339) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 152. |
| §10 Voice & Tone (legacy 343–360) | 옮김 → Content & Locales | Published strings `DESIGN.md` 482–511; tone table 474–480; forbidden register 513. The qualifier at 472 covers the voice reading and the tone-table contract (B2/B2a, A5). The premise-to-register causal at 513 has its own adjacent complete qualifier. Byte-exact / gloss-beside close at 515. |
| §11 Brand Narrative (legacy 362–368) | 옮김 → Experience Scope | `DESIGN.md` 13: 2013, 화장품 정보 비대칭, information asymmetry, ingredients legible, 샘플체험, January 2023, `hds-`, Hwahae Design System, Foundation (Color, Typography, Grid, Radius, Spacing), Components, Templates, Figma, Storybook, TestApp, "experiment fast, validate". Refuse/embrace pairing in the same paragraph, including glossy color-saturated chrome, dark-pattern urgency, quiet neutral canvas, single trustworthy turquoise, amber rating stars as the honest trust signal, and a real documented design system — the source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 199. |
| §12 Principles — 6 numbered (legacy 370–377) | 옮김 → Experience principles | `DESIGN.md` 49–54 under the B2a form at 47: "These six items are a derived editorial implementation inference from the verified surfaces; they are not Hwahae-authored or taken from a separately published UI specification, including the Hwahae Design System (`hds-`) construction documented on the official tech blog." Hwahae's product-design team publicly documented HDS construction, so the close names that published construction (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 160–192 (33 rows). |
| §13 Personas — 3 entries (legacy 379–387) | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, or occupation appears in either output (D2, D2a). Disposition at `provenance.md` 146. Audience at `DESIGN.md` 29 carries only the group-level description the source independently records (Korean beauty shoppers comparing ingredients and reviews), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows (legacy 389–401) | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at `DESIGN.md` 427–435 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "오류가 발생했습니다" and "필수" as the source's forbidden bare forms. The applicability rule is at 239–243, whose qualifier at 243 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary Brand Turquoise, Secondary Chip Outline, Language / Icon Button, and Top Nav close loading/error/success with a role reason (C2 v10) — they commit no operation in place (`loading \| not-applicable` dest 5 · `loading \| applicable` dest 0 · `success \| applicable` dest 0). Header Search opens error (form-validation row) and closes loading/success because the field does not commit the capture (`error \| applicable` dest 1). Rating Star, Info Chip, and Image Count Pill declare `kind: non-interactive` with a reason instead of a map (Core §4.4) — `Kind: non-interactive` dest 3. The two cards get no `kind` and no map because the source supplies no interaction evidence for the containers — `DESIGN.md` 349/360 (C4). |
| §15 Motion & Easing — durations, easing roles, motion rules, reduced-motion (legacy 403–421) | 옮김 → Foundations motion | `DESIGN.md` 158–160 (120ms / 200ms / 320ms with uses), 166–168 (ease-enter / ease-exit / ease-standard roles and uses, curves omitted), 174–177 (functional and quiet, fade-in at `motion-standard / ease-enter`, no bounce or spring, `prefers-reduced-motion: reduce` collapses to instant). |
| §15 Motion & Easing — three curve values (legacy 417–419) | 삭제 → provenance omission ledger | `cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.4, 0.0, 1, 1)`, and `cubic-bezier(0.25, 0.1, 0.25, 1)` are named as omitted at `DESIGN.md` 162 and are not promoted as tokens. Durations stay. Ledger at `provenance.md` 147–149. B3 is held: the promotion condition at `DESIGN.md` 170 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 551. |
| Opening product / atmosphere sentence (legacy 70) | 옮김 → Experience Scope | `DESIGN.md` 9–11: cosmetics-review and ingredient-analysis platform; two inspected surfaces; `#f7f7f7` canvas; reserved Hwahae Turquoise; Pretendard Variable / Spoqa Han Sans. |

## Sibling handling (`web/references/hwahae/.verification.md`)

The sibling exists — confirmed with `find web/references/hwahae -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 73–94 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: card-link height `332px`; H3 `카테고리 전체`; segmented radius `0px 0px 16px 16px`; frequency counts; OG cluster `#00d6cf` / `#00dad4` / `#00d7d0`; sibling title hyphen form; blog `document.title` `화해 블로그 | 기술 블로그`; `No designs found for hwahae`; sibling example class names `hds-absolute hds-left-12`. Recorded at `provenance.md` 98–106.
- Measured `DESIGN.md` 0 for those sibling-only strings: `332px` 0 · `카테고리 전체` 0 · `0px 0px 16px 16px` 0 · `#00d6cf` 0 · `#00dad4` 0 · `#00d7d0` 0 · `화해 블로그` 0.
- `급상승 랭킹` / `로그인` / `한국어` / `1/10` / `추천 아티클` / `Tech` / 24px chip height / 44px lang-button height are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Hwahae is Korean-first, so a hand sweep of published labels is mandatory even when the needle set is non-empty.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 22 distinct | 0 | 0 | 화해 / 화장품을 해석하다 / 화해 고객들이 직접 선택한 / 급상승 랭킹 / 내 피부에 꼭 맞는 제품 랭킹 / 나이대별 추천 / 화해 앱에서 더 편리하게 / 검색 페이지로 이동 / 사용 가능한 진짜 디자인 시스템을 만드는 여정 / 화장품 정보는 화해 — 화장품 성분과 정보, 리뷰 확인하고 구매 하세요 / 로그인 / 홈 / 랭킹 / 어워드 / 한국어 / 1/10 / Tech / 추천 아티클 / 오류가 발생했습니다 / 필수 / 샘플체험 / 화장품 정보 비대칭. |
| `node test-v2/tools/latin-copy-audit.mjs --brand hwahae` | 0 lost / 17 scanned | 0 | 0 | `scanned: 1`, `clean: 1`, `withLoss: 0`, `totalLost: 0`. English glosses that sit beside Korean lines (`decoding cosmetics`) are kept as glosses, not replacements (A5). YAML padding form `0 8px` is at `DESIGN.md` 276 beside visible `0px 8px`. |
| Sibling published strings | 2 distinct sibling-only | 0 | 0 | `카테고리 전체` and `화해 블로그 \| 기술 블로그` survive in provenance; none are promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 화해, 급상승 랭킹, 로그인, 홈, 랭킹, 어워드, 화해 고객들이 직접 선택한, 사용 가능한 진짜 디자인 시스템을 만드는 여정, 화장품 정보 비대칭, 2013, January 2023, 샘플체험, dark-pattern urgency, honest trust signal.

A5 분모: hand sweep of source published labels 22 extracted / 0 missing; latin-copy-audit 0 lost / 17 scanned; gate `copy-loss` compared 17 / candidates 204.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hwahae --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 17, candidates: 204 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 17 < `candidates` 204.

## Deviations recorded

- `DESIGN.md` is 7,264 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: eight declared component records plus two §4-only records, a nine-row surface state contract, a seven-state applicability matrix on five interactive controls, `tokens.rounded.full: 99999` kept off the 50% circle and off matching spacing keys, the full §11 founding-and-system narrative including its last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Hwahae's product-design team publicly documented the Hwahae Design System (`hds-`) construction, so every derived-editorial close names that published construction rather than using the toss-form that would deny a separately published specification (rulebook v12 B2a 전제 주석). Worker-close measure: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 30 = `not Hwahae-authored` 30 = `separately published UI specification` 30. Provenance derived ledger was 30 rows. Auditor-close: DESIGN 33 = `not Hwahae-authored` 33 = `separately published UI specification` 33. Provenance derived ledger 33 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

The portable body was re-read from the title down. Every sentence that states a cause, a reading, or a judgment — Scope dominant-platform + two-surface contract coverage + surface-split + DS-post-not-tokens, atmosphere characterizations including imagery-carries-color, narrative-not-token classification, primary-task selection, audience grouping, distinctive-trait grouping, the six principles, Do/Don't reasons, semantic-color characterizations, canvas/on-brand key-unmerge, spacing/shape key-path splits, elevation philosophy, motion evidence class, font evidence-class readings including Official distributed asset, family two-surfaces-never-mix + fallback, type-role ratio keep, type-rule titles, favicon-pointer reading, applicability procedure, state-record editorial class, state-record non-attachment, layout density reading, breakpoint system-level reading, voice characterization, Forbidden-register premise-to-register causal, Recorded-unresolved named-values framing, and the byte-exact / gloss-beside rule — has an adjacent complete qualifier. Principles 47 uses the HDS-adapted close in full. No "does not say" closer was written. No invented domain (`native application` / `mobile app` / `back-office` / `product application` / `measures 1440px`) appears in `DESIGN.md` (each dest 0).

## F2 E2 rescan (mandatory final pass)

Each destination in the table above was grepped after the files were written. Dual destinations are both named. B3 is claimed only because `DESIGN.md` 170 contains the five evidence kinds plus the per-component gate and the partial-confirmation exclusion in full text; 551 restates the five kinds and the gate, not the partial-confirmation sentence (E2c). §13 deletion is unidentified in the omission ledger (`provenance.md` 146). Sibling-only strings are mentioned as disposition, not used as portable facts. Auditor-close B2a 33=33.
