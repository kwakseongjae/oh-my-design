# 3o3 Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

3o3 (삼쩜삼) is a Korean mass-market tax-refund product operated by 자비스앤빌런즈 (Jobis & Villains). Catalog homepage identity is `https://3o3.co.kr`. This contract covers four named first-party evidence domains from the 2026-06-10 packet: the acquisition funnel at `https://www.3o3.co.kr`, the app login at `https://app.3o3.co.kr`, the official brand resource center at `https://brand.3o3.co.kr` (`/brand-logo` and `/color-typeface`), and the operator corporate site at `https://jobisnvillains.com`. Treating the official blog `https://blog.3o3.co.kr` as a named Tier-1 source whose color, type, and component values stay unattached in this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Color, type, and component values below come from the four named domains above.

Source token note: primary = SZS Blue (`#0c64e6`) sampled from official brand-center swatch images; the funnel landing runs its own lighter CTA pair (`#fbbd41` yellow / `#3e79ff` blue); app/corporate ink = `#141618` on Pretendard. Treating that note as a register split — SZS Blue is the brand-center primary and is not the funnel CTA fill, and the funnel pair is not the identity blue — is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Minor webp quantization on the sampled brand-center swatches is possible.

The following evidence-domain sentence is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. A funnel CTA value is not a proxy for the app login, the brand-center identity tokens, or the corporate recruiting chrome. Exact values below stay attached to the surface that established them.

The next paragraphs’ temperature, persuasion, and “DNA” readings are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. The source reads three coordinated registers. The funnel is described as a conversion machine: tall 61px CTAs in `#fbbd41` and `#3e79ff` at a tight 7px radius, on alternating white and dark-slate (`#283240`) bands, with a Unbounce-built Noto Sans KR exception. The app login is described as quieter and rounder: an 18px-radius Kakao login in `#fae100`, 10px info chips, and 12px compliance footnotes in `#a4acb4`. The corporate/brand layer uses Poppins for English headings and Pretendard for Korean, with 36px/700 headlines. The registers are read as differing in temperature while sharing flat, shadowless surfaces, honest typography, and blue as a spine. The official brand identity on the brand resource center anchors brand moments to **SZS Blue** (`#0c64e6`), with deep navy (`#023266`) and amber (`#fea800`) as the supporting official palette, and a white canvas (`#ffffff`) with cool grey surfaces (`#f5f6f8`) and near-black ink (`#141618`) on Pretendard. The source’s “fiscal credibility” / “money coming back” / “maximum trust, minimum friction” characterizations of those hexes are part of the same derived reading.

The following distinctive-among-fintechs and Toss-comparison reading is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. The source contrasts 삼쩜삼’s direct-response energy with Toss’s premium minimalism, and treats the live promise “숨은 환급금 간편하게 찾아보세요” plus on-page 12px disclosure lists as persuasion-forward and legally scrupulous — a friendly money-finding game and a licensed tax intermediary at once.

Treating the following public-history facts as narrative rather than interface tokens, including the 3.3% name origin as widely documented public knowledge rather than a UI token, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Public history recorded in the source (operator corporate site and the app statutory footer): 자비스앤빌런즈 founded August 2015, CEO 김범섭 named on the live footer. First product 자비스 (Jarvis), an AI bookkeeping service for small businesses; 삼쩜삼 launched May 2020. The source states that the 3.3% name origin (the withholding-tax rate) is widely documented public knowledge. The following product-origin / problem thesis is a derived editorial implementation inference from the source Brand Narrative; it is not 3o3-authored or a separately published UI specification. The source records that gig workers, freelancers, and platform laborers had overpaid taxes sitting unclaimed because the refund process assumed an accountant, and that 삼쩜삼 turned that recovery into a consumer app. The app disclosure line counts **7,208,747** cumulative filing customers between 2020.05.01 and 2026.04.01. The corporate site reports the platform passing 24 million cumulative users with over 2조원 in refunds processed as of mid-2025, Korean 예비유니콘 designation in 2023, and a mission frame of “더 많은 부”, “더 많은 시간”, and “더 많은 권리”. Homepage title: “삼쩜삼 - 생활 밀착 환급·혜택 플랫폼”.

The following refusal and expansion-arc reading is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. The source treats 삼쩜삼 as refusing the intimidating chrome of tax authorities and legacy 세무 software, and the dark-pattern vagueness of “free check” services that hide their fee, and as embracing a treasure-hunt verbal frame on a disclosed legal base, one mandated brand blue, and an expansion from tax refunds toward the everyday hidden-money platform the homepage title claims.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

- Find hidden refund amount (`내 환급금 조회하기` on the funnel; `내 숨은 환급액을 찾아보세요` as the app title).
- Apply for a refund (`지금 환급 신청하기`; trust-framed `안전하게 환급 신청하기`).
- Continue into the app with a Kakao account (`카카오 계정으로 계속하기`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable segments (freelancers and gig workers reclaiming withheld tax, small-business owners filing 종합소득세), not individual people. Restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

### Distinctive traits

- Official SZS Blue `#0c64e6` as catalog `primary_color` and brand-center identity color, not the funnel CTA fill
- Funnel CTA pair `#fbbd41` / `#3e79ff` at 7px / 61px, with a dark eligibility CTA `#2f3943` and dark band `#283240`
- App Kakao login `#fae100` at 18px / 56px, info chip `#f0f1f2` at 10px / 33px, footnotes `#a4acb4` 12px
- Near-black ink `#141618` on Pretendard for app/corporate product text; Poppins for English brand-center headings
- Funnel-only Noto Sans KR on Unbounce landing CTAs — not the brand typeface
- Flat, shadowless surfaces: measured `box-shadow: none`; separation by tint (`#f5f6f8`, `#fafbfc`, `#f3f6ff`) and the dark band, not elevation
- 12px / 500 muted disclosure lists as a harvested caption role

Treating SZS Blue as catalog `primary_color` and brand-center identity color rather than the funnel CTA fill, treating Noto Sans KR as funnel-only rather than the brand typeface, treating surfaces as separated by tint and the dark band rather than elevation, treating the three registers as coordinated but not interchangeable, and treating 12px footnotes as a first-class harvested role rather than leftover type, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

### Principles

These five items, including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. Item 4 restates the brand-center SZS Blue mandate, which is quoted as first-party under Capture-bound and Foundations; the implication sentence remains derived.

1. **Reframe the chore as a find.** Tax filing is repositioned as discovering money that is already yours. *UI implication:* lead with the outcome (“내 숨은 환급액”) and put process steps behind a single tall CTA; never open with forms.
2. **Every claim carries its basis.** Average-refund figures cite exact cohorts (7,208,747 filers, dated range). *UI implication:* bold number + adjacent basis chip or 12px footnote is an inseparable pair — never render one without the other.
3. **Safety is copywriting, not just engineering.** “안전하게” lives inside the button label. *UI implication:* trust-sensitive actions get the blue CTA and reassurance microcopy at the point of click, not on a separate security page.
4. **One governed blue.** SZS Blue is mandated by the brand center with explicit Do-Nots. *UI implication:* brand surfaces use `#0c64e6` untouched; funnel experiments may vary CTA colors but never the logo or identity blue.
5. **Flat means honest.** No shadows, no layered chrome, no hidden depth. *UI implication:* separate with tint and dark bands; if a section needs emphasis, change its background, not its elevation.

Capture-bound application (source §7 Do’s, brand-center quotes, and harvested geometry). First-party brand-center sentences are marked. The remaining bullets are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification.

- First-party (brand.3o3.co.kr): “SZS Blue는 삼쩜삼을 대표하는 색상입니다. 일관된 표현을 위해 브랜드 색상은 반드시 준수하여 사용합니다.”
- First-party (brand.3o3.co.kr): “영문 사용 시 Poppins, 국문 사용 시 Pretendard를 사용합니다.”
- First-party (brand.3o3.co.kr Logo Do Nots): do not recolor or restyle the logo (size, shape, color, effect).
- Use `#0c64e6` for brand-identity moments; do not substitute funnel `#3e79ff` or link `#006efa` as SZS Blue.
- Keep funnel CTAs at 61px height and 7px radius on Noto Sans KR 18px/500.
- Render the Kakao login in Kakao yellow `#fae100` verbatim (source: Kakao-mandated social login; not a 3o3 palette swatch).
- Keep measured surfaces flat (`box-shadow: none`); separate with `#fafbfc`, `#f3f6ff`, and `#283240`.
- Give compliance footnotes their own 12px/500 `#a4acb4` block.
- Pair each bold money claim with its calculation-basis chip or footnote.
- Use amber `#fea800` for refund-optimism accents within official brand graphics, not as a funnel CTA fill.

### Avoid

First-party brand-center Logo Do Nots:

- Do not recolor or restyle the logo — size, shape, color, and effect changes are forbidden there.

The following items copy source §7 Don’ts. They are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification.

- Do not add drop shadows or elevation stacks — every measured surface is shadowless.
- Do not use tight negative letter-spacing — measured letter-spacing is `normal`.
- Do not swap the typeface split (Poppins for Korean, or Pretendard-only English lockups).
- Do not hide or shrink statutory disclosures to make the pitch cleaner.
- Do not use the funnel’s Noto Sans KR on product or brand surfaces — it is a funnel-only legacy.
- Do not spread Kakao yellow (`#fae100`) beyond the Kakao login action.
- Do not make refund claims without the basis line (“누적 신고 고객 평균” framing).

The SZS Blue / funnel-blue split below is from source §9 Iteration Guide, not from §7 Don’ts. Restating it here is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

- Do not conflate SZS Blue `#0c64e6` with funnel conversion blue `#3e79ff`.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. SZS Blue is catalog `primary_color` and the official representative brand color, not a funnel CTA fill. Official navy, amber, sky tint, and link blue are not funnel CTA, dark-section, ink, Kakao, or funnel-tint tokens. Funnel CTA and band hexes stay unmerged from each other and from the official swatches. Chip Grey is not a general disabled or skeleton token for harvested buttons. Corporate Ghost on-image fill is this control’s field, not Canvas.

Official brand palette (brand.3o3.co.kr swatches; minor webp quantization possible):

- **SZS Blue / Primary** (`#0c64e6`): catalog `primary_color` and the official representative brand color. Logo, brand moments, key graphics. Not a funnel CTA fill.
- **Deep Navy** (`#023266`): official secondary dark — brand graphics and dark logo lockups. Not Ink `#141618` and not Dark Section `#283240`.
- **Amber** (`#fea800`): official warm accent in brand graphics. Not CTA Yellow `#fbbd41` and not Kakao `#fae100`.
- **Sky Tint** (`#f3f9ff`): official light blue-tinted background from the brand palette. Not Funnel Tint Blue `#f3f6ff`.
- **Link Blue** (`#006efa`): hyperlink/action blue on brand-center chrome. Not SZS Blue and not Funnel CTA Blue.

Funnel CTAs and bands (`www.3o3.co.kr`):

- **CTA Yellow** (`#fbbd41`): landing highest-frequency conversion button. YAML `cta-yellow`.
- **CTA Blue** (`#3e79ff`): trust-framed CTA. YAML `cta-blue`. A deeper sibling `#355aff` appears on one band; it is not merged with `#3e79ff`.
- **CTA Dark** (`#2f3943`): dark-slate eligibility CTA. YAML `cta-dark`. Not Dark Section.
- **Dark Section** (`#283240`): full-width dark-slate band backgrounds. Not CTA Dark.
- **Tint Blue** (`#f3f6ff`): light-blue info band on the funnel. YAML `tint-blue`. Not Sky Tint.

Product and corporate neutrals:

- **Ink** (`#141618`): primary text on app, corporate site, and brand center. YAML `ink`.
- **Pure Black** (`#000000`): label color on yellow funnel CTAs and default landing body text. YAML `ink-pure`. Not Ink.
- **Body Sub** (`#606a76`): secondary descriptive text on corporate/brand surfaces. YAML `body-sub`.
- **Lead slate** (`#3a4047`): mission-statement lead variant. Not Body Sub.
- **Muted** (`#a4acb4`): compliance footnotes, disclosure lists. YAML `muted`. Sidebar inactive `#b9c0c6` is adjacent; it is not Muted.
- **Muted Alt** (`#788391`): info-chip label text on the app login. YAML `muted-alt`. Not Muted.
- **Canvas** (`#ffffff`): page background. YAML `canvas`.
- **Surface** (`#f5f6f8`): app background grey. YAML `surface`.
- **Surface Soft** (`#fafbfc`): soft grey section background on brand center and landing root. YAML `surface-soft`.
- **Chip Grey** (`#f0f1f2`): info-chip background. YAML `chip`. Not a general disabled or skeleton token for harvested buttons.

Third-party action:

- **Kakao Yellow** (`#fae100`): Kakao-mandated social-login button. YAML `kakao`. Not Amber and not CTA Yellow.

Corporate Ghost on-image uses `rgba(255, 255, 255, 0.25)` over hero photography with `#ffffff` label text. That fill is this control’s field, not Canvas.

### Spacing

YAML scale: xs 4, sm 8, md 12, base 16, lg 24, xl 32, xxl 48, section 80. Body layout states a 4px base unit and the same 4px, 8px, 12px, 16px, 24px, 32px, 48px, 80px section rhythm. Funnel CTAs are full-width-of-column blocks at 61px height. Corporate Ghost pads 16px 48px.

### Shape

YAML `rounded`: sm 7, md 10, lg 16, xl 18, full 9999.

Observed radii in the body: 7px (funnel CTAs), 10px (app chips), 16px (corporate Ghost), 18px (Kakao login). YAML `full` 9999 is the recorded full token; it is not merged with the 18px login radius, and no observed use of 9999 is named in the body.

The following register-geometry reading is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. The source reads the radius as growing as the surface moves from “sell” (funnel 7px) to “use” (app 18px). 7px / 10px / 16px / 18px remain local harvested geometry, not a universal radius for every unlisted control.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | `box-shadow: none` | Every measured surface — funnel, app, corporate |
| Tint (Level 1) | `#f5f6f8` / `#fafbfc` / `#f3f6ff` background shift | Section and card separation |
| Dark band (Level 2) | `#283240` full-width slab | High-contrast segment breaks on the funnel |

YAML `tokens.shadow.none` is `none`. Live inspection returned `box-shadow: none` on every button, chip, band, and card measured.

The following “flat means honest” / “no hidden anything” reading is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. The source treats tint shifts and the dark slab as the depth language of a conversion page that should read fast and an app chrome that should stay lightweight.

### Motion

Source-stated duration roles. The source HTML comment classifies §15 motion tokens as illustrative extrapolations consistent with the measured flat system, except the brand-center menu value. That classification, and the signature-count-up / “tax intermediary signals steadiness” readings, are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Button press, chip expand/collapse, focus |
| `motion-standard` | 200ms | Screen-to-screen step transitions, accordion, sheet |
| `motion-slow` | 300ms | Sidebar/menu slide (brand center uses `transform 0.3s ease-in-out`), result reveal |

Measured live (brand-center menu CSS): `transform 0.3s ease-in-out`. `ease-standard` is the CSS keyword `ease-in-out` on that two-way menu transition.

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-standard` | `ease-in-out` (measured on brand-center menu) | Two-way transitions |
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving sheets, step advances |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |

The following motion-rule readings (functional and quiet, signature count-up as payoff, funnel pages as static, no spring/bounce) are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. They sit under the source HTML comment that classifies motion tokens as illustrative extrapolations except the measured brand-center menu. Motion is described as functional and quiet. The named signature moment is the refund-amount reveal — a short `motion-slow / ease-enter` count-up. Funnel pages are described as effectively static (image bands + anchor scrolling). No spring, no bounce. Under `prefers-reduced-motion: reduce`, the count-up renders instantly and all transitions collapse; filing remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Official product-use | Brand resource center: “Poppins와 Pretendard는 삼쩜삼을 대표하는 타입페이스입니다. 영문 사용 시 Poppins, 국문 사용 시 Pretendard를 사용합니다.” That split is first-party. |
| Live computed surface-use | App login and corporate site: Pretendard (Pretendard JP Variable named on the corporate site). Brand-center English headings: Poppins. Funnel landing: Noto Sans KR. |
| Official distributed asset | App icon/symbol downloads are named as official zips from `cdn.3o3.co.kr`. Not promoting a 3o3-exclusive distributed type family from that zip evidence is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. |
| Declared-only / funnel exception | Noto Sans KR is the Unbounce funnel face. It is a funnel-only legacy, not the brand typeface. |

The following evidence-class application readings are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. Noto Sans KR is a funnel-only exception, not the brand typeface. Do not present Noto Sans KR or a system stack as Pretendard or Poppins.

### Family

- **Korean (product and corporate):** Pretendard (Pretendard JP Variable on the corporate site)
- **English (brand/display):** Poppins
- **Funnel exception:** Noto Sans KR
- YAML `family.en-display`: `Poppins`; `family.kr`: `Pretendard`; `family.funnel`: `Noto Sans KR`

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Do not replace Pretendard or Poppins with Noto Sans KR. Do not present Noto Sans KR as the brand face.

The following type-rule readings (700 never heavier; normal tracking versus tight fintech tracking; compliance type as a real tier) are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. Measured letter-spacing is `normal`. Display and headings sit at weight 700; emphasis inside body uses 700 spans. The 12px/500 muted footnote is a harvested caption role on funnel and app.

### Type roles

Verified YAML line-height values are the unitless ratios `1.50`, `1.00`, `1.40`, `1.22`, and `1.60`. They scale with font size and are not fixed px. Px figures in the legacy body table are size-local observations at those captured sizes, not replacements for the ratios. YAML Subsection `1.50` and body Subsection `1.00–1.50` are both kept. The following unmerged-ink reading is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. YAML Body color is not a single ink: the body table paints Body at `#606a76` while Display/Heading EN use `#141618`, and Lead uses `#3a4047`.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Color (body table) | Use (YAML) |
|---|---|---:|---:|---:|---|---|---|
| Display | Pretendard | 36px | 700 | 1.50 | 54px at this size | `#141618` | Corporate hero / section headlines, Pretendard |
| Heading EN | Poppins | 32px | 700 | 1.00 | 1.00 | `#141618` | Brand-center page titles, Poppins |
| Lead | Pretendard | 28px | 500 | 1.40 | 39px at this size | `#3a4047` | Mission-statement lead line |
| Subsection | Poppins/Pretendard | 24px | 700 | 1.50 | 1.00–1.50 | `#141618` | Service card titles, EN subsection heads |
| Body Large | Pretendard | 20px | 500 | 1.40 | 28px at this size | `#606a76` | Sub-lead, supporting descriptions |
| Funnel CTA (YAML `button`) | Noto Sans KR | 18px | 500 | 1.22 | 22px at this size | `#000000` / `#ffffff` | Funnel CTA label, Noto Sans KR |
| Body | Pretendard | 16px | 500 | 1.60 | 25.6px at this size | `#606a76` | Standard reading text, Pretendard |
| App CTA (YAML `button-app`) | Pretendard | 16px | 700 | 1.50 | 24px at this size | `#141618` | App CTA label (Kakao login) |
| Chip | Pretendard | 12px | 700 | 1.50 | 18px at this size | `#788391` | Info chip label |
| Caption | Pretendard | 12px | 500 | 1.50 | 18px at this size | `#a4acb4` | Compliance footnotes, disclosures |

The following dark-band type-role reading is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Dark-band copy on the funnel is recorded as white 20px/400 (“숨은 환급금 간편하게 찾아보세요”). That 20px/400 is not Body Large 20px/500.

### Assets

Treating catalog logo metadata as a Google favicon lookup (`https://www.google.com/s2/favicons?domain=app.3o3.co.kr&sz=256`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

App icon/symbol downloads are named as official zips from `cdn.3o3.co.kr`. Funnel persuasion blocks ship as optimized webp bands; brand swatches and graphics are full-bleed webp. First-party Logo Do Nots remain: do not recolor or restyle the logo. Treating verified product and brand imagery as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted. The source HTML comment classifies empty/loading/error/success treatments as illustrative extrapolations consistent with the measured flat/shadowless system and the compliance-forward voice; they are not live computed paints of the harvested funnel CTAs, Kakao login, chip, cards, or footnote list. That classification, and treating `#f0f1f2` in the Skeleton/Disabled rows as not the info-chip field and not a harvested-button fill, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated. `#f0f1f2` in the Skeleton/Disabled rows is not the info-chip field, and is not copied onto harvested buttons.

| State | Treatment |
|---|---|
| **Empty (no refund found)** | White canvas, Ink (`#141618`) headline stating the result plainly, Body Sub (`#606a76`) explanation of why (already filed, no withheld tax), one CTA to check another year. No fake-positive framing. |
| **Empty (no linked income data)** | Muted (`#a4acb4`) single line explaining what’s missing plus the chip-style basis explainer; CTA to connect 홈택스 auth. |
| **Loading (refund calculation)** | Flat skeleton blocks on `#f5f6f8` at final dimensions; progress copy in the treasure-hunt voice (“숨은 환급액을 찾는 중”) — calculation steps shown, never a bare spinner. |
| **Loading (document submission)** | Inline progress with the previous screen’s summary still visible; blue accent progress, no modal lock. |
| **Error (홈택스/NTS connection failed)** | Inline banner: plain-Korean cause + retry; names the external dependency honestly (“국세청 연결이 지연되고 있어요” framing). Never a bare error code. |
| **Error (identity verification failed)** | Field-level message stating what mismatched and what to fix; offers the alternate verification path. Calm, never accusatory. |
| **Error (filing rejected)** | Dedicated state with the rejection reason verbatim plus plain-language next steps; support entry point attached. |
| **Success (refund filed)** | Confirmation screen with the filed amount, expected deposit window, and basis line; calm congratulation, no confetti excess — the money is the reward. |
| **Success (deposit completed)** | Push + in-app record with exact amount and date; archived in history. |
| **Skeleton** | `#f0f1f2` blocks at final dimensions, flat pulse, no shimmer gradient — consistent with the shadowless system. |
| **Disabled** | Surface drops to `#f0f1f2` with Muted (`#a4acb4`) label; CTAs never disappear, they explain (“대상이 아니에요”) via chip or footnote. |

Characterizations in that table such as “No fake-positive framing”, “never a bare spinner”, “Calm, never accusatory”, “no confetti excess — the money is the reward”, and “consistent with the shadowless system” are part of the same derived editorial implementation inference; they are not 3o3-authored or a separately published UI specification.

The login surface offers no traditional text input — sign-in is Kakao-only — so input specs are intentionally absent (source §4). All measured surfaces returned `box-shadow: none`.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the illustrative §14 rows. This is not a complete state-coverage claim.

Tinted Info Band, Soft Section, Dark Band, and Compliance Footnote have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. The Info Chip is harvested as `type: badge` with an expandable-basis use; expandable disclosure is not evidence that the chip surface itself has a closed seven-state map, so kind and a map are omitted.

The following 48px-floor sentence is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification. Source §9 Iteration Guide says CTAs are tall (61px funnel / 56px app) and “never compress below 48px”. Harvested heights remain 61px and 56px; 48px is that iteration-guide floor, not a harvested control height.

### Funnel CTA — Yellow

- Role: funnel primary conversion
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#fbbd41`
- Text: `#000000`
- Radius: 7px
- Height: 61px
- Font: 18px / 500 / Noto Sans KR
- Use: Funnel primary CTA — 내 환급금 조회하기 (also “지금 환급 신청하기”, “지금 찾으러 가기”)
- Observed: default only
- Field note: `#000000` is this control’s label, not Ink `#141618`. `#fbbd41` is not Amber `#fea800` and not Kakao `#fae100`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the funnel primary conversion button |
| hover | applicable | Pointer-web button on www.3o3.co.kr; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A conversion CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a landing conversion button with several labels; exact selector/request/outcome mapping to refund lookup versus navigation is unresolved, so those three fields stay omitted at this boundary rather than closed from the illustrative §14 rows.

### Funnel CTA — Blue

- Role: funnel trust-framed conversion
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#3e79ff`
- Text: `#ffffff`
- Radius: 7px
- Height: 61px
- Font: 18px / 500 / Noto Sans KR
- Use: Funnel trust-framed CTA — 안전하게 환급 신청하기
- Observed: default only
- Field note: `#3e79ff` is not SZS Blue `#0c64e6`, not Link Blue `#006efa`, and not the sibling `#355aff` band.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the trust-framed funnel CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A conversion CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a trust-framed conversion button; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Funnel CTA — Dark

- Role: funnel eligibility check
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#2f3943`
- Text: `#ffffff`
- Radius: 7px
- Height: 61px
- Font: 18px / 500 / Noto Sans KR
- Use: Funnel eligibility CTA — 대상자 여부 확인하기
- Observed: default only
- Field note: `#2f3943` is this control’s fill, not Dark Section `#283240`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the eligibility CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An eligibility CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as an eligibility check; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Kakao Login

- Role: app login — the app’s single sign-in action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#fae100`
- Text: `#141618`
- Radius: 18px
- Height: 56px
- Padding: 0 16px
- Font: 16px / 700 / Pretendard
- Use: App login — 카카오 계정으로 계속하기
- Observed: default only
- Field note: `#fae100` is Kakao-mandated login chrome, not Amber and not CTA Yellow.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the app’s single Kakao sign-in action |
| hover | applicable | Pointer-web button on app.3o3.co.kr; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A login action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source records default geometry only. Illustrative §14 홈택스 / identity-verification / filing rows are not this button’s loading, error, or success paints, and exact Kakao OAuth request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Corporate Ghost (On-Image)

- Role: recruiting CTA over hero photography on jobisnvillains.com
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `rgba(255, 255, 255, 0.25)`
- Text: `#ffffff`
- Radius: 16px
- Padding: 16px 48px
- Font: 16px / 700 / Pretendard
- Use: Recruiting CTA — 채용 중인 직무 보러 가기
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named recruiting CTA on the corporate hero |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A recruiting CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as an on-image recruiting CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Info Chip

- Role: expandable basis-of-calculation chip on the app login
- Type: badge
- Anatomy: label
- Background: `#f0f1f2`
- Text: `#788391`
- Radius: 10px
- Height: 33px
- Padding: 0 12px
- Font: 12px / 700 / Pretendard
- Use: Info chip — 예상 환급액을 계산하는 기준
- Observed: default only
- Field note: `#f0f1f2` is this chip’s background. Treating that fill as not a harvested-button disabled fill and not the illustrative §14 skeleton token is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

No interactive-kind confirmation is given for a §4.4 map. “Expandable” in Use is not a closed seven-state contract. Kind and a state-applicability map are omitted.

### Tinted Info Band

- Role: light-blue info band on the funnel landing
- Type: card
- Anatomy: surface
- Background: `#f3f6ff`
- Text: `#141618`
- Use: Tinted info band on funnel landing

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Soft Section

- Role: soft grey full-width section
- Type: card
- Anatomy: surface
- Background: `#fafbfc`
- Text: `#141618`
- Use: Soft grey canvas section (brand center, landing)

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Dark Band

- Role: full-width dark slate band on the funnel
- Anatomy: surface
- Background: `#283240`
- Text: `#ffffff`
- Use: Full-width dark slate band (white 20px/400 copy — “숨은 환급금 간편하게 찾아보세요”)
- YAML `tokens.components` does not record this band; values are body §4 only.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted. Type is not invented.

### Compliance Footnote

- Role: statutory disclosure list under the fold
- Type: listItem
- Anatomy: text row
- Text: `#a4acb4`
- Font: 12px / 500 / Pretendard
- Use: Compliance footnote list under the fold — average-refund basis, 세무대리 scope, 사업자 정보 (“(주) 자비스앤빌런즈 | 대표자 : 김범섭”)
- App footer also names 사업자등록번호 : 158-86-00171 and the cohort line “2020.05.01~2026.04.01 기간의 누적 신고 고객 7,208,747명”

No interactive-kind evidence is given. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 24, xl 32, xxl 48, section 80. Body layout repeats 4px, 8px, 12px, 16px, 24px, 32px, 48px, 80px. Funnel CTAs are full-width-of-column 61px blocks. Corporate Ghost pads 16px 48px. App Kakao login is 56px, full width. Info chip is 33px with 12px side padding. Corporate sticky header is 71px. Corporate container is max-width ~1440px centered.

The “conversion rhythm over gallery calm”, “whitespace as a pacing device”, “flat segmentation”, “disclosure gets room”, and “geometry tracks register” readings in this section are derived editorial implementation inferences from the verified surfaces; they are not 3o3-authored or a separately published UI specification.

Recorded grids:

- Funnel: single-column, centered, long-scroll page assembled from full-width image/text bands; CTA repeated after every persuasion block. Funnel bands are pre-built per breakpoint (desktop/mobile image pairs) rather than reflowed.
- Corporate: max-width ~1440px centered container with sticky 71px header. Corporate hero: 36px/700 headlines hold weight on mobile, scale size down.
- Brand center: sticky left sidebar (section nav) + single content column with full-bleed swatch/graphic images. Sidebar nav collapses into a top sticky bar with a disclosure arrow.
- App: mobile-frame single column centered on desktop, content capped at app width.

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <768px | Single column; funnel serves a parallel mobile variant of each band; app is mobile-native |
| Tablet | 768-1440px | Corporate grid widens, brand-center sidebar appears |
| Desktop | ≥1440px | 1440px max container; app login renders as a centered mobile frame |

Treating that table as a recorded span of named widths, not a complete cross-viewport specification of every unlisted control, and treating the 61px, 56px, 33px, 71px, and ~1440px figures as surface measurements in this packet rather than universal layout tokens, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

Touch-target record: funnel CTAs 61px height; app Kakao login 56px height, full width; info chip 33px with 12px side padding. Source §9 Iteration Guide’s “never compress below 48px” floor sits under the Capture-record limiter; 48px is not a harvested control height.

Image behavior: funnel persuasion blocks as optimized webp bands swapped per breakpoint; brand swatches and graphics as full-bleed webp; no shadow at any size. App icon/symbol downloads from `cdn.3o3.co.kr`.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-10)

- “삼쩜삼 - 생활 밀착 환급·혜택 플랫폼” — homepage title
- “세금 환급은 물론, 모든 숨은돈 삼쩜삼에서 찾기” — homepage meta description
- “내 숨은 환급액을 찾아보세요” — app title
- “쩜 쉬운 세금 관리의 시작” — jobisnvillains.com headline
- “SZS Blue는 삼쩜삼을 대표하는 색상입니다. 일관된 표현을 위해 브랜드 색상은 반드시 준수하여 사용합니다.” — brand resource center
- Funnel/app control strings: “숨은 환급금 간편하게 찾아보세요”; “내 환급금 조회하기”; “지금 환급 신청하기”; “지금 찾으러 가기”; “안전하게 환급 신청하기”; “대상자 여부 확인하기”; “카카오 계정으로 계속하기”; “예상 환급액을 계산하는 기준”; “채용 중인 직무 보러 가기”

Treating the §14 empty/loading/error/success strings as part of the illustrative state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not 3o3-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list are a derived editorial implementation inference from the verified surfaces; they are not 3o3-authored or a separately published UI specification. They are not the Observed strings above.

The source describes 삼쩜삼’s voice as **friendly, money-smart, and reassuring** — tax (세금) in the register of a helpful friend who found money in a coat pocket. The core verbal move is read as reframing: tax filing becomes “숨은 환급금 찾기”. Claims are read as bold but always anchored.

| Context | Tone |
|---|---|
| Funnel headlines | Promise-led, possessive framing: it’s *your* hidden money. “숨은 환급금 간편하게 찾아보세요.” |
| CTAs | Imperative + immediate: “지금 찾으러 가기”, “내 환급금 조회하기”. The word 지금 (now) recurs. |
| Trust-sensitive CTAs | Safety-prefixed: “안전하게 환급 신청하기”. |
| App chrome | Calm and procedural: “카카오 계정으로 계속하기”, “예상 환급액을 계산하는 기준”. |
| Compliance copy | Precise statutory Korean, never minimized: scope of 세무대리, average-refund basis with exact cohort counts. |
| Corporate / brand | Mission-cast wordplay: “쩜 쉬운 세금 관리의 시작” — the brand’s own ‘쩜’ folded into “참 쉬운”. |

**Forbidden register** (source): fear-based tax-penalty scaremongering; unanchored refund-amount hype (every figure needs its basis line); bureaucratic 국세청-style officialese in user-facing flows; minimizing or hiding statutory disclosures.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Funnel CTA Yellow / Blue / Dark, Kakao Login, and Corporate Ghost
- input anatomy (source: Kakao-only login; specs intentionally absent)
- YAML `rounded.full` 9999 observed use
- motion animation names, transition properties, and any duration beyond the three source tokens plus the measured `transform 0.3s ease-in-out` — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
