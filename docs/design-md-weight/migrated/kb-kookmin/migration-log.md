# kb-kookmin migration log

Source: `web/references/kb-kookmin/DESIGN.md`
Sibling read (not the migration input): `web/references/kb-kookmin/.verification.md`
Destination: `docs/design-md-weight/migrated/kb-kookmin/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/kb-kookmin/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use ripgrep match lists per file, never a remembered count.

Source SHA-256 `7a4cf60aeaf0abcd8b58ebaad4976d803ddb234a914968feb2e9954d7ce21506` (`web/references/kb-kookmin/DESIGN.md`). Sibling SHA-256 `8745a41da7dba6bf2bb12d3fcff91d8ba4bd9d6ee1fe5d800f55fd5085efc727` (`web/references/kb-kookmin/.verification.md`). Worker-close portable DESIGN SHA-256 `74261c08fc8a93e66b8f1a7a3bf82a55d6595b9efe45cecd316ae33f818282e6`. Auditor SHA `2de860350e9fae4583405cf7ef2bf335da456d84dece2324b141e0d87f6b9bc7`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# KB국민은행 Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–21. Homepage is dual (E2a): YAML `https://www.kbstar.com/` at `DESIGN.md` dest 2 at 9/21 + `provenance.md` dest 5 at 13/47/49/55/66. Catalog `primary_color` `#ffcc00` is dual: `DESIGN.md` dest 6 at 11/35/60/78/84/88 + `provenance.md` dest 4 at 14/23/118/184 (it also matches `tokens.colors.header-accent`). Favicon slug URL is dual: `DESIGN.md` dest 1 at 151 + `provenance.md` dest 1 at 16 (E2a). `logo.slug` as a field name is `DESIGN.md` dest 1 at 151 + `provenance.md` dest 2 at 16/25. |
| YAML `omd: "0.1"`, `verified`, `verification_v2`, `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–21, 33–37. `omd` format is at 17; `tokens.source \| reconciled` at 19; `verification_v2.checked` at 34. The source footer's **Verified:** 2026-07-13 is at `provenance.md` 39. `components_harvested` DESIGN dest 0 / provenance dest 3 (`provenance.md` 21/144/207). These are ledger keys with no portable slot. No `ds.*` fields in the source (`provenance.md` 27). |
| YAML `tokens.note` | 옮김 → Experience Scope; 분리 → provenance | Full note transcribed at `provenance.md` 79–83. The facts it names (selector-backed public product-web values only; group CI / font / history / value materials as brand-context; no fallback family or unobserved interaction) land at `DESIGN.md` 9. Dual (E2a). |
| YAML `tokens.colors` (7 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 80–86. All seven roles kept with names, values, token-set keys, and claim surfaces: canvas `#ffffff` (home; §2 also records the same hex on online-banking backgrounds), foreground `#333333` (online-banking), foreground-strong `#000000` (home), muted `#5a5a5a` (home), header-accent `#ffcc00` (home), link `#0c4ad1` (home), hairline `#dddddd` (home). Surface attachments follow YAML claim anchors. `#222222` stays on the outline list item (`DESIGN.md` 185/193), not promoted to a color token (A4). |
| YAML `tokens.typography.body` / `section-heading` / `online-selected-item` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 143–145. YAML line heights stay the YAML px spelling `21px` / `26px` / `14px` and are never converted to a replacement unitless ratio (A1a does not apply as a conversion). All three YAML `use` strings restored verbatim in the Token-set use column (A1, A3). Surface attachments follow YAML claim anchors: body / section-heading = home; online-selected-item = online-banking. The 14px body size is not the selected-item 14px size; body `21px` is not selected `14px` (`DESIGN.md` 147). No `tokens.typography.family` path is promoted (`DESIGN.md` 135). |
| YAML `tokens.spacing` (4 steps) | 옮김 → Foundations spacing | Unitless: `DESIGN.md` 96–99 (`inline-link-left 9` · `inline-link-right 8` · `outline-link-inline 10` · `toggle-inline 12`). Claim surface for all four: home. `tokens.spacing.inline-link-left: 9` / `inline-link-right: 8` are disambiguated from inline padding `0px 8px 0px 9px` at `DESIGN.md` 101/220. `tokens.spacing.outline-link-inline: 10` is disambiguated from outline padding `0px 10px` at `DESIGN.md` 101/195. `tokens.spacing.toggle-inline: 12` dest 2 at `DESIGN.md` 99/101; no toggle component is promoted. |
| YAML `tokens.rounded.square` / `tokens.shadow.none` | 옮김 → Foundations shape + elevation | `tokens.rounded.square: 0` at `DESIGN.md` 105. `tokens.shadow.none`: `none` at `DESIGN.md` 109 with `box-shadow: none`. Both claim surface: home. Local public-web observation, not a universal radius or elevation system. |
| YAML `tokens.components` (3 records) | 옮김 → Components & States | `DESIGN.md` 181–246. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`listItem\`` ×3 (183/210/235 = YAML home-outline + home-inline + online-selected). All three YAML `use` strings restored verbatim as `Token-set use:` lines (191/216/242). YAML `states` string kept on the selected item (`DESIGN.md` 241). No §4-only component borrowed a `Primitive type`. `not in the token set` DESIGN dest 0. |
| §1 Visual Theme & Atmosphere (legacy 114–123) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–37. The atmosphere reading (practical visual language / trust / legibility / institutional continuity kept distinct from unobserved native-app or authenticated banking flows) carries the adjacent complete qualifier at 11 (B2/B2a). Key characteristics restated at 34–37 under the qualifier at 32. |
| §1 공식 히스토리·CI·font·values·annual-report URL | 분리 → provenance; URL·정체 경계 옮김 → Scope | Identity/source URLs at `provenance.md` 47–73. The source fact that group CI / font / history / value materials are brand-context evidence stays in `DESIGN.md` 9. |
| §2 Color Palette & Roles (legacy 125–139) | 옮김 → Foundations semantic color | `DESIGN.md` 80–88. Brand-asset boundary (CI guide / group CI / Star-b / image-based colour; only numeric yellow is computed `#ffcc00`) at 88. Characterizing phrases covered by the adjacent qualifier at 78 (B2/B2a). |
| §3 Typography Rules — evidence classes, family, hierarchy (legacy 141–161) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 123–129 (source header `Unresolved` rewritten as `Unresolved class` so the table cell is not a bare placeholder word; the resolution text is unchanged). Family 135; hierarchy table 143–145. Official-brand-asset / Declared-only / Unresolved-class readings carry the adjacent complete qualifier at 131 (B2/B2a). |
| §4 Component Stylings (legacy 163–196) | 옮김 → Components & States | `DESIGN.md` 181–246. §4 body values and YAML `use` / radius / padding forms are both kept. Unique §4 values land: outline text `#222222` or `#000000` at 185/193; §4 quoted selectors `home::[data-omd-capture="14"]` / `"15"` at 192 and `"67"` at 217 (A3, A5); `interactionCount: 0` / `interactionKinds: 0` at 176; low-confidence carousel/listbox and icon-button detections remain raw verification evidence at 176; no menu / dialog / toast / validation / authenticated-product / native-app variant inferred at 176. Surface attachments follow YAML claim anchors (home / online-banking). |
| §5 Layout Principles (legacy 204–206) | 옮김 → Layout & Platforms | `DESIGN.md` 251: captured at `1440×900`; dense public-web lists, 14px text roles, small square controls; no grid / breakpoint / responsive rule / authenticated banking layout / native mobile layout; third snapshot repeats the public-home URL. Capture-measurement reading at the same line (B2/B2a). `1440×900` is the source's own capture figure, not an invented closer. |
| §6 Depth & Elevation (legacy 208–210) | 옮김 → Foundations elevation | `DESIGN.md` 109: `box-shadow: none` and 0px radius on the measured links and online-banking item; local public-web observation, not a universal KB elevation or card system. Flat-observation reading carries the adjacent qualifier on the same line (B2/B2a). |
| §7 Do's (legacy 214–219) | 옮김 → Experience application rules | `DESIGN.md` 58–61 (four Do rules), under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 221–226) | 옮김 → Experience avoid | `DESIGN.md` 67–70, under the qualifier at 65. The four prohibitions are the source's own list, including Star-b / Malgun Gothic / Roboto / rounded cards / filled primary buttons / authenticated/native surfaces. No invented domain is added. |
| §8 Accessibility & Content (legacy 228–230) | 옮김 → Layout & Platforms + Content & Locales | Compact 13–14px public text and square utility geometry; no keyboard / focus / contrast-ratio / error-message / screen-reader behaviour at `DESIGN.md` 251. "Do not claim WCAG conformance or a KB accessibility contract" at `DESIGN.md` 274. Preserve meaningful link text at 251. This legacy heading is Accessibility & Content, not Responsive Behavior. |
| §9 Reference Scope & Evidence (legacy 232–234) | 분리 → provenance; 범위 사실 옮김 → Experience Scope | Three snapshots and first-party group materials as separate context land at `DESIGN.md` 9. Tier 2 getdesign / Refero failures are provenance-only: `getdesign.md` DESIGN dest 0 / P dest 1 at 77; `refero` DESIGN dest 0 / P dest 1 at 77 (fitpet — the portable body does not carry those catalogue names). `_research.md` is named at `provenance.md` 87 as a ledger pointer, not published copy. This legacy heading is Reference Scope & Evidence, not an Agent Prompt Guide — no tool-command deletion was applied to it. |
| §10 Voice & Tone (legacy 236–250) | 옮김 → Content & Locales | `DESIGN.md` 257–274. Do/Don't table at 260–264. Illustrative Korean lines at 266–268 with the source's own "Illustrative, not extracted product copy" label. Published names at 276: KB국민은행, KB Kookmin Bank, KB Financial Group, KB금융체, Star-b, “finance that changes the world.” Qualifiers at 257 and 270 (B2/B2a, A5). Byte-exact names and samples. |
| §11 Brand Narrative (legacy 252–258) | 옮김 → Experience Scope | `DESIGN.md` 13: Kookmin Bank and Housing & Commercial Bank merger; people and businesses; “finance that changes the world”; most trusted lifetime financial partner; 2024 annual report (customer-centred digital consultation, core-banking modernisation, planned redesign of branch-terminal screens); and the closing sentence — it does not establish completed 2025 UI values or authorise a broader product design system. The source paragraph's last sentence is kept as one unit (`completed 2025 UI values` DESIGN dest 2 at 13/318). Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 211. |
| §12 Principles — 5 numbered (legacy 260–271) | 옮김 → Experience principles | `DESIGN.md` 43–52 under the B2a form at 41: "These five items are a derived editorial implementation inference from the verified surfaces; they are not KB국민은행-authored or a separately published UI specification." No published UI specification is in the source, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 171–202 (28 data rows at 175–202). |
| §13 Personas — 3 stakeholder categories (legacy 273–287) | 삭제 (무식별); 원본 공개 그룹만 옮김 → Experience Audience | The source's own header says stakeholder categories evidenced by first-party group materials, not synthetic personal profiles, and leaves task / device / accessibility need / journey unspecified. No name, age, city, motivation, or affiliation classification is invented or re-hosted (D2, D2a). Source-named publicly observable groups — customers broadly; people and businesses — land at `DESIGN.md` 28. Branch-terminal redesign remains evolution context only at the same line. Disposition at `provenance.md` 141 (unidentified; 3 headings named only as a count). Selecting three captured-surface tasks as primary tasks is qualified at `DESIGN.md` 19; those tasks do not come from the persona section. |
| §14 States (legacy 289–304) | 옮김 → Components & States capture record + per-component applicability | Full source body preserved at `DESIGN.md` 159–174: Empty / Loading / Error — validation / Error — service/system / Success / Skeleton / Disabled / Focus / Hover / Pressed, each with the source's captured-evidence-boundary sentence (A2; the catalog graph is still 0/440, so nothing is delegated). The applicability rule is at 178, whose qualifier covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). A generic `Focus` capture is not `focus-visible` treatment (B1) — `DESIGN.md` 178. |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive destination links declare the seven canonical states. Outline and inline list items close loading/error/success with a role reason (C2 v10) — destination list-item link commits no operation in place (`loading \| not-applicable` dest 2 at 203/228). `loading \| applicable` DESIGN dest 0. Online-banking selected list item gets no `kind` and no map because the source supplies no interaction evidence for the row — `DESIGN.md` 246 (C4). This is not a complete state-coverage claim (`DESIGN.md` 178). |
| §15 Motion & Easing (legacy 306–308) | 옮김 → Foundations motion | `DESIGN.md` 113: "The supplied bundle records zero interaction kinds and zero interaction records. It therefore establishes no duration, easing curve, transition, animation, carousel movement, or reduced-motion rule." No curve was present to delete. B3 is held: the promotion condition at `DESIGN.md` 115 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 198–202; placed after §4 in the source) | 분리 → provenance | Freshness `provenance.md` 33–43; Tier 1 list 65–73; Tier 2 75–77. Conflicts unresolved: none — `provenance.md` 43. |

## Sibling handling (`web/references/kb-kookmin/.verification.md`)

The sibling exists — confirmed with `find web/references/kb-kookmin -type f`. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 91–101 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: coverage score `80`; 5 detected component types; 33 component variants; 352 / 67 / 1 font-use counts; Times (one occurrence); 21 high-confidence background occurrences; 420 radius observations; `home::[data-omd-capture="59"]` 14px square box; `surface-2::[data-omd-capture="14"]` low-confidence detector; `artifacts/reference-evidence/kb-kookmin.json`; sibling narrative gloss “straight forms for trust and curved forms for friendliness”. Recorded at `provenance.md` 105–114.
- Measured `DESIGN.md` 0 for those sibling-only strings: `80` as a coverage score is not a portable token; `352` dest 0 · `Times` dest 0 · `420` dest 0 · `straight forms for trust` dest 0.
- `#ffffff` / `#333333` / `#ffcc00` / `#0c4ad1` / `#dddddd` / 맑은 고딕 / Roboto / KB금융체 / `1440×900` / `interactionCount: 0` are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. KB국민은행 is Korean-forward in names and three illustrative lines, so a hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published names and identity strings in the source body | 12 distinct | 0 | 0 | KB국민은행 / KB Kookmin Bank / KB Financial Group / KB금융체 / Star-b / “finance that changes the world” / most trusted lifetime financial partner / 맑은 고딕 / Malgun Gothic / Roboto / “확인할 내용을 먼저 보여드립니다.” / “필요한 금융 정보를 차분하게 안내합니다.” / “변경 사항과 다음 단계를 함께 확인하세요.” |
| `node test-v2/tools/latin-copy-audit.mjs --brand kb-kookmin --candidate docs/design-md-weight/migrated/kb-kookmin/DESIGN.md` | 3 lost / 1 brand scanned | 0 published | 0 | `_research.md` is a source §9 ledger filename (landed at `provenance.md` 87). `", captured:"` is YAML metadata, not published copy (same class as intuit). `home::[data-omd-capture="15"]` is a §4 selector restored at `DESIGN.md` 192. |
| Sibling published strings | 0 distinct sibling-only CTAs | 0 | 0 | Sibling records measurements and a conflict matrix, not additional published CTAs. The “straight forms for trust and curved forms for friendliness” gloss is sibling-only narrative and stays in provenance. |

Sub-needle labels confirmed present individually in `DESIGN.md`: KB국민은행, KB Kookmin Bank, KB Financial Group, KB금융체, Star-b, finance that changes the world, 맑은 고딕, Malgun Gothic, Roboto, 확인할 내용을 먼저 보여드립니다, 필요한 금융 정보를 차분하게 안내합니다, 변경 사항과 다음 단계를 함께 확인하세요.

A5 분모: hand sweep of source published labels 12 extracted / 0 missing; latin-copy-audit 3 lost / 1 scanned (0 published); gate `copy-loss` compared 4 / candidates 116.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand kb-kookmin --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 4, candidates: 116, detail: "인용 문자열 116개 중 4개만 비교했다 — 나머지는 라틴이라 바늘이 되지 않는다. 라틴 전수 대조는 손으로 하라." }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 4 < `candidates` 116.

## Deviations recorded

- `DESIGN.md` is 4,644 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: three declared component records, a source §14 body plus seven-state applicability on two interactive destination links, surface-attached token-set paths, the full §11 narrative including its last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No `ds.*` field is in the source, so every derived-editorial close uses the toss-form `not KB국민은행-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Measure: `derived editorial implementation inference` DESIGN = `not KB국민은행-authored` DESIGN = 28. Provenance derived ledger 28 rows (E1 1:1).
- Font-evidence table header `Unresolved` was rewritten as `Unresolved class` (`DESIGN.md` 129) so a table cell is not a bare Core placeholder word. The resolution text is the source's own. Recorded rather than left as a Portable Core fail.
- Scope claim first failed Portable Core on `explicitlyNegatesClaim` because two source sentences paired `does not` with `product` (`universal product fill`; `broader product design system`) without a source/capture subject the checker accepts. The bytes were kept and the subject was attributed to the source (`the source packet`; `The source's closing sentence is that it does not establish…`) rather than rewriting the claim away (E3). After that attribution, `evaluatePortableCore` reports `portable_core: true`.

## F1 B2a rescan (mandatory final pass)

The portable body was re-read from H1 through Named gaps after the last prose edit. Every causal, interpretive, or judging sentence was asked whether it is a KB국민은행-issued fact or a derived reading.

Complete-form closes (`derived editorial implementation inference` + `not KB국민은행-authored or a separately published UI specification`) sit adjacent to:

1. Scope ¶1 — two-surface contract; duplicate home; values stay attached; group materials as brand-context
2. Scope ¶2 — practical visual language / trust / legibility / institutional continuity
3. Scope narrative — §11 as narrative-not-tokens, including the last sentence on 2025 UI values
4. Primary tasks — selecting the three captured-surface tasks; not from the persona section
5. Audience — refusing individual personas; reading those groups as audience
6. Distinctive traits — restatement classification; groupings
7. Principles — five items
8. Application rules — four Do rules
9. Avoid — four Don'ts
10. Semantic color — pairing; role names from source labels; `#ffcc00` as local accent; surfaces from YAML anchors
11. Spacing — key-path keep-separate
12. Shape — local observation, not a universal radius scale
13. Elevation — local flat observation
14. Motion — zero-interaction as constraint, not a motion token; reduced-motion unnamed; five-kind per-component promotion gate
15. Font evidence — neither stack as a KB UI token; KB금융체 separate; `@font-face` declared-only
16. Family — omitting `tokens.typography.family`; fallback prohibition
17. Type roles — YAML px line heights; 13px off the 14px body; YAML claim anchors
18. Type roles sizes — 14/14/21/14/20 as named roles, not shared numerals
19. Assets — Google favicon pointer
20. Capture record — source-contract keep; role-based applicability; not-complete-coverage
21. Outline list item — 28px / `0px 10px` as this item
22. Inline list item — 13px / 24px / padding as this item
23. Selected list item — 14px line height / 28px as this item; C4 omit; not promoted as a tab
24. Layout capture — `1440×900` as capture measurement; 13–14px not an accessibility contract
25. Layout measurements — named heights as surface measurements
26. Content & Locales — voice register
27. Voice samples — illustrative, not extracted; Do/Don't as source rules
28. Named gaps — named values, not a domain inventory; not permissions to invent

Count: DESIGN 28 = provenance inventory 28. No Scope/Avoid sentence invents a domain the source did not name (D1). No "does not say that anything measures …" closer was added (over-defense). `native application` / `mobile app` / `back-office` / `product application` / `measures 1440px` DESIGN dest 0. Source-established `native-app` / `native mobile layout` / `authenticated/native surfaces` / `1440×900` are kept as the source wrote them.

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep -oF` against `DESIGN.md` and `provenance.md`. Dual destinations named in this log were confirmed in both files before the row was closed: homepage DESIGN dest 2 / P dest 5, `#ffcc00` DESIGN dest 6 / P dest 4, favicon slug URL DESIGN dest 1 / P dest 1, `logo.slug` field name P dest 2 at 16/25, `tokens.note` facts at 79–83, `tokens.spacing.inline-link-left` dest 3 at 96/101/220 / `outline-link-inline` dest 3 at 98/101/195 / `toggle-inline` dest 2 at 99/101, `tokens.rounded.square`, `tokens.shadow.none`, published names. Tier 2 getdesign / Refero are provenance-only (DESIGN dest 0). Compliance claims used only when the portable body actually holds the cited text: B3 five-kind gate at `DESIGN.md` 115; B2a complete form 28 = 28; C4 omit at 246; C2 role reasons at 203/228. Persona deletion is unidentified (`provenance.md` 141) and does not re-host a name, age, city, or motivation (D2a, E2d).
