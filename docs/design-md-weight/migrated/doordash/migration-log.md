# DoorDash migration log

Ruleset: `MIGRATION_RULEBOOK.md` **v9** (2026-08-26) — including A5 (brand-published strings move as bytes) and D1a (a named-gap noun list is a D1 claim too).

Source: `web/references/doordash/DESIGN.md` (unmodified)
Destination: `docs/design-md-weight/migrated/doordash/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/doordash/provenance.md`
Date: 2026-08-26
Worker: Opus 5 T2 migration
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd — `level: portable-core`, `portable_core: true`, `reasons: []`, all seven claim declarations present exactly once, `claim_locale: en`)
Gate: `test-v2/tools/migrate-reference.mjs --brand doordash --gate-only` — PASS, problems `[]`

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance Identity; `name` 이중 목적지 → H1 `# DoorDash Design System`; `primary_color` 이중 목적지 → Foundations semantic color (`#eb1700`) + provenance Identity and claim ledger | The portable file carries no frontmatter. The logo record is a SimpleIcons pointer, so Typography & Assets states the pointer and claims no distributed asset or license. |
| YAML `verified`, `omd`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance Identity + Freshness | Process and freshness metadata (A1c: the ledger fields are carried, not dropped). |
| YAML `tokens.note` | 분리 → provenance (verbatim); its three facts 이중 목적지 → Experience Scope (`#eb1700` = `rgb(235, 23, 0)` live CTA red; the `DD Norms` / `DD-TTNorms` / `TT-Norms` family), Foundations semantic color (`#191919` = `rgb(25, 25, 25)` used instead of pure black), and Typography & Assets Font evidence | The note is an attribution sentence, so the ledger keeps it verbatim while the portable body carries its facts under their evidence class. |
| YAML `tokens.colors` (9 keys) | 옮김 → Foundations semantic color, each with its token name (`primary`, `on-primary`, `canvas`, `ink`, `ink-pure`, `muted`, `surface`, `hairline`, `error`) | All nine values kept. `error: #eb1700` is recorded as its own role rather than merged into `primary`, because the source's token record maps error onto the same red deliberately (A4: a verified field binding is not flattened). |
| YAML `tokens.typography` | 옮김 → Typography & Assets Family + Type roles | Both families, all seven roles, sizes, weights, the `-0.5` hero tracking, and the unitless line-height ratios `1.0`, `1.2`, `1.4`, `1.15` are kept as ratios rather than converted to px (A1a). The `use` strings survive in the Notes column. |
| YAML `tokens.spacing` / `rounded` | 옮김 → Foundations Spacing / Shape | Named steps (`xs` 4 … `section` 64) kept alongside the px scale; the radius record's `sm` 4px and `lg` 16px steps are kept without a component claim and the gap is named in Governance. |
| YAML `tokens.shadow` (`soft`, `ring`) | 옮김 → Foundations Elevation; both strings 이중 목적지 → Components & States (the `ring` inset on Primary Red CTA and Secondary White Pill, and both strings on Ghost Soft Pill) | Byte-exact in both places, in the spacing form each source section uses. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | Every record's primitive `type` is preserved per component — button ×3, input, card, badge, tab, toggle — instead of being flattened into `Kind: interactive` (A1b). `button-primary.states: "hover darker red"` and `nav-link.active: "#191919 underline accent"` are kept on their components as source-recorded treatments; `active` is not a canonical Core state, so it stays outside the seven-state table. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | The seven Key Characteristics survive verbatim as Distinctive traits under an adjacent B2a qualifier, because selecting them as distinctive is an editorial judgment. The three interpretive readings in the prose ("the only color with real weight", "engineered speed", the intentional sharp-corner contrast) are labelled in place as the source's own readings. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | All three groups, every role name, value and use sentence retained, including `rgb(235, 23, 0)` live-measured and the near-black rationale. |
| §3 Typography Rules | 옮김 → Typography & Assets (Font evidence, Family, Type roles, Typography rules) | The fallback stack is kept and explicitly marked a platform fallback rather than the brand face. The four §3 Principles carry an adjacent B2a qualifier. |
| §4 Component Stylings | 옮김 → Components & States | All ten body records retained: three buttons, the address input, content card, tinted surface section, promo badge, about-site top nav item, top nav CTA, and the carousel dot. Anatomy, geometry, type and use strings kept. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts unresolved | 분리 → provenance Freshness, Capture boundary, Sources, and the Tier 2 line; the capture boundary 이중 목적지 → Experience Scope + Components & States evidence boundary; the Tier 2 lookup result 이중 목적지 → Foundations "Evidence class of the color record" + Governance Named gaps | The two-surface boundary must survive in the standalone document (E1), so Scope states it while the URL-level ledger stays in provenance. The lookup result is an evidence-class boundary, so it is stated in the body as "secondary-source lookups" — the Tier taxonomy itself is ledger vocabulary and stays in provenance (E1). |
| §5 Layout Principles | 옮김 → Layout & Platforms (Grid and container, Whitespace); Spacing System → Foundations Spacing (base unit 8px, the eight-step scale, and the alternating-band note); Border Radius Scale → Foundations Shape (merged into the Use column) | Single destination for each: the radius scale is not repeated in Layout, and the spacing scale is not repeated there either. The three Whitespace Philosophy readings are editorial and sit under an adjacent B2a qualifier. |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | All three levels retained. The Shadow Philosophy paragraph carries an adjacent B2a qualifier, and the zero-alpha `ring` note is qualified separately because reading it as a resting-state slot is also derived. |
| §7 Do's | 옮김 → Experience derived implementation principles | All seven Do rules become bullets in the same qualified list as the §12 principles. |
| §7 Don'ts | 옮김 → Experience Avoid | All seven retained verbatim under an adjacent B2a qualifier. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms (Breakpoints, Touch targets, Collapsing strategy); the full-width-on-mobile CTA rule 이중 목적지 → Components & States Primary Red CTA (pointer) | A closing sentence records that these are source-recorded design rules with no viewport-level measurement behind them. |
| §9 Agent Prompt Guide — quick color reference, three example prompts, six iteration rules | 삭제 (+ §9-only values 옮김) | Tool-facing packaging has no receiving Core slot; it is deleted, not delegated. Before deleting, the values that existed **only** in §9 were moved (A3): the white 48px nav header with a right-aligned red CTA → Components & States About-site Top Nav CTA and Layout & Platforms Grid and container; one red pill CTA per feature section, right-aligned → Components & States Primary Red CTA and Layout & Platforms Grid and container. Both are labelled *legacy prompt-block guidance*. The third §9-only value — the hero headline's red background image — is carried in Typography & Assets → Type roles as a separate evidence-class sentence below the table, naming §9 and the §14 promo row as its origin; only the `#ffffff` in the Hero Banner notes is the element-level reading (B1). The moved list is also recorded in the provenance omission ledger. The quick color reference and the six iteration rules restate values already carried by Foundations, Typography & Assets and Experience, and are deleted as repetition. |
| §10 Voice & Tone | 옮김 → Content & Locales | The voice paragraph, all seven tone rows, the five voice samples with their source-declared "verified live 2026-06-22" class, and the forbidden register are retained. Every quoted string moves as bytes (A5), including "$0 DELIVERY FEE ON FIRST ORDER", "Attract new customers" and "0% commission". |
| §11 Brand Narrative | 옮김 → Experience Scope; the three refusals 이중 목적지 → Experience Avoid + a Scope pointer sentence | Founding year, all four founders, Stanford, the Palo Alto origin, the local-commerce insight, the mission quote, DashPass, the category expansion and the "Door to More" positioning are retained. The platform-ambition reading of "Door to More" is labelled as the source's own interpretation. |
| §12 Principles | 옮김 → Experience derived implementation principles | All five principles and their *UI implication* clauses retained under the adjacent B2a qualifier covering the whole list. |
| §13 Personas | 삭제 | The source itself discloses them as fictional archetypes that do not refer to real people. D2: not promoted to primary tasks or audience, and **not** re-hosted in provenance — no name, age, city or demographic-segment profile appears in any output file. Experience Audience keeps group-level scope only, grounded in the "Become a Dasher" CTA and the source's own "Merchant / driver surfaces" voice row rather than in the persona section. |
| §14 States | 옮김 → Components & States "Source state contract" (11 rows) + per-component applicability | The section body is preserved while the catalog graph is 0/440 (A2). Every value and quoted string is kept byte-exact, including `rgba(235,23,0,0.4)`, the `#767676` disabled label, "We couldn't find that address. Check the spelling or try a nearby address.", "$0 delivery fees on your next order." and "$0 DELIVERY FEE". The disabled recipe is 이중 목적지 → the Primary Red CTA `disabled` row. Applicability is judged by control role; no row uses non-observation as a `not-applicable` reason (C1), and no state is made applicable by primitive kind alone (C2). |
| §15 Motion & Easing — durations, easing roles, motion rules, reduced motion | 옮김 → Foundations Motion | Four duration tokens with their uses, three easing role names with their uses, the motion-rules paragraph and the `prefers-reduced-motion: reduce` contract are retained under an adjacent B2a qualifier noting the source attributes nothing to motion. |
| §15 easing curve values (3) | 삭제 → exact values 보관 → provenance omission ledger | Unattributed curves. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to the `ease-exit` example in the legacy authoring template `spec/omd-v0.1.md`, which that file labels a non-brand implementation default; the other two differ from the template examples and are omitted as unattributed. Role names and uses stay in Foundations, the three exact strings are stored in the provenance omission ledger (E2b), and the gap is named in Governance. |
| Philosophy-layer HTML comment (Tier 1 element list, public-record note, persona disclosure, interpretive-claim disclosure) | 분리 → provenance Sources / Capture boundary / Claim ledger; the about-site H1 weight 700 reading 옮김 → Typography & Assets Type roles note; the evidence-class boundaries 이중 목적지 → Experience Scope, Foundations Motion, Components & States, Content & Locales | The comment's own evidence-class disclosures are what the portable B2a qualifiers restate; keeping them only in the sidecar would move the authority qualification out of the standalone document (E1). The comment's H1 weight 700 exists nowhere else in the source, so it is carried rather than dropped (A3). |

## Required final passes

- **F1 (B2a scan).** The portable document was re-read end to end after drafting. Adjacent qualifiers were added to every interpretive or causal passage, not to Principles alone: Scope (surface-scope boundary, evidence-class split, and the three in-prose readings), Primary tasks (task formulation), Audience (grouping and the persona decision), Distinctive traits (selection judgment), Derived implementation principles, Avoid, the Foundations color-class boundary, the Elevation shadow reading, the zero-alpha `ring` reading, the whole Motion contract and its promotion gate, Font evidence, the Family no-substitution rule (marked as an application of the Governance unknown-absence policy), Typography rules, Assets, the Components & States evidence boundary, the Source state contract, Layout Whitespace and the responsive-proof sentence, the Content & Locales voice characterization and copy-class boundary, and Governance. Four sentences were rewritten during this pass rather than qualified: "deliberately narrow" lost the intent attribution; the error-token line now states that the state contract names no value instead of asserting the two are the same value; the Tier 2 sentence now states the lookup result instead of inferring from it; and the locale sentence now states the record instead of listing domains it does not cover.
  **F3 (separate-session audit, 2026-08-26).** The audit added four further adjacent qualifiers this pass had missed — the Foundations semantic-color character readings, the `Family` display-face character reading ("On-demand boldness"), the component `Use:`-line character and intent readings (notably the sharp input corners read as an *intentional* contrast), and Layout → Grid and container, whose figures the Scope sentence already promised would carry their source-recorded boundary "where it appears" — and rewrote three portable passages that exposed migration-internal vocabulary (E1). See `audit-log.md`.
  **Wave-21 semantic review revision (2026-08-26).** A separate semantic review found four confirmed FAILs, fixed here without touching token values, component-table values, state applicability judgments, section structure or the canonical source, and without reverting any of the ten F3 audit fixes. (i) Foundations → Shape: the §5 causal reading "the search box anchors the UI" had been carried into the table's `Use` column with no qualifier anywhere in the Shape passage — the nearest one covers only the color-class boundary — so an adjacent complete-form qualifier was added, matching the standard the F3 audit already applied to the §4 `Use:` readings. (ii) Typography & Assets → Type roles: "Condensed black for maximum impact" is an effect judgment and the table sat between two qualifiers that each covered only their own neighbour (Family above, Typography rules below), so the table got its own adjacent complete-form qualifier. (iii) Same row, B1: "observed in `#ffffff` on the red hero background" merged an element-level reading with an unobserved background; the observation now stops at `#ffffff` and the background is a separate sentence naming §9 and the §14 promo row, which also restores the source's "red **or image**" option that the merged phrasing had collapsed to "red". (iv) `provenance.md` §9-only move table: the row claimed the Tier 1 record corroborated the whole reading, but that record holds `color #ffffff` and no background — the row now separates the corroborated color from the §9/§14-derived background, and the body label it had wrongly excused is restored.
- **F2 (E2 cross-check).** Every row above was grep-verified across the three output files before being written. Dual destinations are recorded in both places (`name`, `primary_color`, the `tokens.note` facts, both shadow strings, the capture boundary, the §11 refusals, the §14 disabled recipe, the §8 full-width rule, and the two §9-only guidance values). The §9 row states both the deletion and the move because both happened; the §15 curve row states both the deletion and the provenance retention (E2b). Two rows failed their own grep on the first attempt and were corrected rather than left standing: the Primary Red CTA carried "full-width on mobile" under the *legacy prompt-block guidance* label when that rule comes from §8 Collapsing strategy, so the label was split and the provenance §9-only table lost that row; and the §5 row initially claimed the radius scale had two destinations, when the Layout duplicate was removed and Foundations Shape is its only destination. The B3 compliance claim below is made only because the five evidence kinds and the component-specific gate are literally present in Foundations → Motion.
- **B3.** The motion promotion rule is present in full in the portable body: it names transition properties, animation name, duration, easing, and reduced-motion behavior, and gates promotion on component-specific computed observation, stating that a single confirmed curve does not satisfy it.
- **A5 (brand-published strings).** All 92 distinct quoted strings in the source (143 occurrences) were diffed against the three output files. Every published label, CTA, headline, placeholder, nav item, state message and promotional line survives byte-exact: "Find restaurants", "Get DashPass", "Sign In", "Sign Up", "Shop Groceries", "Become a Dasher", "Sign in for saved address", "Use current location", "Enter delivery address", "Get Started", "Products", "Company", "Impact", "News", "Blog", "Everything you crave, delivered.", "DashPass is delivery for less", "As a delivery driver, make money and work on your schedule.", "Make money and work on your schedule.", "Your Door to More", "$0 DELIVERY FEE ON FIRST ORDER", "$0 DELIVERY FEE", "$0 delivery fees on your next order.", "We couldn't find that address. Check the spelling or try a nearby address.", "something went wrong.", "Attract new customers", "0% commission", "to grow and empower local economies", and the forbidden-register samples. Thirteen source quotations appear nowhere in the three output files as literal strings. Twelve are frontmatter `use:` annotations — six `typography` role descriptions and six component descriptions — whose content is carried in the corresponding type-role or component entry; they are the source's own field descriptions, not strings DoorDash published. The thirteenth is not a `use:` annotation at all: `button-ghost-sm`'s combined `shadow:` field, which concatenates two shadow strings in the frontmatter's compressed spacing; both are carried, in the spaced form each source section uses. Two further `use:` strings, `"Address / delivery location search field"` and `"Content card, feature section"`, are literal only where this log quotes them; their content likewise sits on the address-input and content-card records. The four §9 example prompts are the remaining non-literal quotations and were deleted with that section (row above) after their §9-only values were moved. *(Re-measured 2026-08-26: this row previously read "All 87 quoted strings" and "The seventeen quotations … are all frontmatter `use:` annotations" — the diff base is 92 distinct strings, the non-literal set is thirteen once the four deleted prompts are counted where they belong, and one member of it is a `shadow:` field, not a `use:` annotation.)* One further quotation, `"shop anything"`, existed only inside the §13 persona biographies and left with them under D2 — the source gives it no live-verification marking, so it is not a published string A5 protects.
- **D1a.** The Named gaps list was checked noun by noun against the source. Every domain it names is one the source itself establishes: the hover direction without a value (`states: "hover darker red"`), the three easing roles (§15), the canonical state treatments this document declares applicable, the ordering / order-tracking / payment / DashPass screens (§14) and the merchant and driver surfaces (§10 voice table), viewport measurement behind §8, the unassigned `rounded` steps, and the two Tier 2 lookups (footer). No domain absent from the source is listed.
- **E3.** No value, hex spelling, curve string, URL, quoted label or state name was distorted to change gate behaviour. The gate produced no false positive on this reference.
