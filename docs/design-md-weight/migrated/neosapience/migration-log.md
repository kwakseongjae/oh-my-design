# neosapience migration log

Source: `web/references/neosapience/DESIGN.md`
Sibling read (not the migration input): `web/references/neosapience/.verification.md`
Destination: `docs/design-md-weight/migrated/neosapience/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/neosapience/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with ripgrep / a Python substring scan against the three output files before it was written (F2). Counts use per-file match lists, never a remembered count.

Source SHA-256 `baf194abef3fb8435154a4e9af2735c73ac0ca61c223db989a309ccf380fc198` (`web/references/neosapience/DESIGN.md`). Sibling SHA-256 `5787466f84aea51ec1ed6a1f31c15d0bd477d82a859cf4293e2fd5a6f3f23eb1` (`web/references/neosapience/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `name` 옮김 → H1; `homepage` 옮김 → Experience Scope; catalog `primary_color` 옮김 → Scope keep-same + Semantic color; `logo` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# Neosapience Design System`. Identity table `provenance.md`. `display_name_kr` 네오사피엔스 DESIGN dest (substring 네오사피엔스) 8 / provenance dest 4 (E2a). Exact `display_name_kr` DESIGN dest 0 / provenance dest 1. Homepage `https://neosapience.com` DESIGN dest 2 / provenance dest 4 (E2a). Catalog `primary_color` DESIGN dest 4 / provenance dest 2 (E2a). Favicon URL DESIGN dest (favicon) 3 / provenance dest 4 (E2a). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept (A1c). Exact `tokens.source: live-extract` DESIGN dest 0 / provenance dest 2. `components_harvested` DESIGN dest 0 / provenance dest 2. Token note verbatim in provenance Identity. No `ds.name` / `ds.url` / `ds.type`; absence recorded in provenance, not filled. |
| YAML `tokens.colors` (17 keys) | 옮김 → Foundations semantic color | All 17 roles kept with names, values, and token-set paths: primary `#fe7e43`, product-cta `#f97316`, amber `#f7b500`, ink `#09162d`, ink-product `#262626`, body `#111827`, nav `#1f2937`, body-product `#404040`, muted `#4b5563`, muted-alt `#6b7280`, canvas `#ffffff`, surface `#f4f4f4`, surface-alt `#f9fafb`, peach `#ffe7d4`, tab-active `#ffc98f`, tab-border `#e5e5e5`, ink-pure `#000000`. `tokens.colors.primary` DESIGN dest 4. `tokens.colors.product-cta` dest 3. Two oranges stay two keys. Qualifier at `DESIGN.md` `:85` (B2/B2a) also names canvas role off Typecast CTA text and off emotion-chip / corporate-nav background as component-field attachments rather than extra colors keys. |
| YAML `tokens.typography.family` (4 keys) + hierarchy (8 roles) | 옮김 → Typography & Assets Family + Type roles | Family paths `corporate` / `product-display` / `product-ui` / `product-fallback`. Unitless line-height stays a ratio (A1a): `1.06`, `1.2`, `1.25`, `1.55`, `1.5`, `1.0`. YAML `use` strings restored verbatim beside longer §3 rem / Surface / Notes (wave 37 keep-both): `Typecast product hero, Plus Jakarta Sans` / `Typecast section heads, Plus Jakarta Sans` / `Corporate H1 / section heads, Pretendard` / `Corporate hero lead paragraph, Pretendard` / `Corporate nav links, Pretendard` / `Body copy, Pretendard` / `Typecast primary CTA label, Roboto` / `Chip labels, metadata`. Caption YAML size 14 beside §3 `14–16px`. Qualifier at `:195` also names the inline demo CTA's 16px / 700 Roboto as a component-local writing off the Button type-role. |
| YAML `tokens.spacing` (10 steps) / `tokens.rounded` (7 steps) / `tokens.shadow` (2) | 옮김 → Foundations spacing + shape + elevation | Spacing keys `tokens.spacing.xs: 4` through `tokens.spacing.section: 64` (`DESIGN.md` `:115`). Shape keys include `tokens.rounded.full: 9999` dest 2 (`DESIGN.md` `:123` / `:135`). `tokens.shadow.none` dest 1; `tokens.shadow.product-soft` dest 1; `rgba(0,0,0,0.06) 0px 4px 16px` DESIGN dest 3; `box-shadow: none` DESIGN dest 1 / provenance dest 1 (E2a). 32–40px hero padding is a §5 notable, not a spacing key (`32–40px` DESIGN dest 2 at `:117` / `:119`). |
| YAML `tokens.components` (8 keys) | 옮김 → Components & States | Each keeps YAML `type` as Primitive type (A1b): `button` ×2, `tab` ×2, `badge` ×1, `card` ×2, `listItem` ×1. Longer §4 writings kept beside YAML `use` (Smart Emotion; Happy · Paige; Our tech / Research; Kid / TikTok; 4px 20px 4px 16px; padding 16px / radius 0px on research row). `corporate-card` and `research-item` omit kind and applicability map (C4). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` `:9`–`:13`; Distinctive traits `:33`. Original sentences kept: "two deliberately different surfaces", "the split is the whole story", "What unifies the two systems is a shared warm-orange spine and a hangul-first typographic discipline." Atmosphere readings carry adjacent complete qualifiers at `:11` (B2/B2a). Primary-tasks qualifier at `:19` names the four outcomes (corporate mission-and-publication reading, Typecast hero/demo invitation, Typecast feature-and-emotion switching, corporate top-nav destinations). |
| Footer **Verified** / **Tier 1** / **Tier 2** / **Conflicts** (legacy `:218`–`:221`) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness and Tier lists in provenance. Conflicts unresolved: none. Tier 2 exact strings "No designs found for 'neosapience'" / "No designs found for 'typecast'" / `?q=typecast` provenance dest 1 each; DESIGN dest 0 (sibling/footer ledger, not portable tokens). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Role names from the source (Neosapience Orange, Typecast CTA Orange, Ink Navy, …) sit beside YAML keys. Characterizing / keep-both / two-key readings and canvas-vs-CTA-text / chip-nav-background attachments covered by the qualifier at `:85`. |
| §3 Typography Rules | 옮김 → Typography & Assets | Family, type-role table, and the four source typography principles. Qualifiers at `:184` (including trailing fallback/system-face and do-not-mix-display-into-corporate) and `:195` (including inline demo CTA off Button type-role). |
| §4 Component Stylings | 옮김 → Components & States | Eight controls. YAML + §4 keep-both. Capture-record qualifier at `:218` also names component-local height/padding/radius/font kept off spacing, type, and rounded keys. Footer moved to provenance (previous row). |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | Base unit 4px; scale; ~1050px; 32–40px notable; whitespace philosophy under qualifier at `:418`. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Flat / Tint / Soft table; shadow philosophy under qualifier at `:147`. |
| §7 Do's | 옮김 → Experience application rules | Eight Do rules at `:55`–`:63`, under the grouping qualifier at `:55`. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | Eight Don'ts at `:68`–`:76`, under the qualifier at `:68`. No invented domain is added (D1). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Mobile `<640px` / Tablet `640–1024px` / Desktop `1024–1440px`; touch 60px / 44px / 40px; collapsing strategy; image behavior. `1024–1440px` is the source's own breakpoint writing; `measures 1440px` is not written (D1). Qualifier at `:452` covers purpose clauses only. |
| §9 Agent Prompt Guide | 삭제 | Tool-facing construction prompts and a restated color list; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at provenance Omission ledger. |
| §10 Voice & Tone | 옮김 → Content & Locales | Two-register table; four verbatim voice samples; forbidden register. Qualifiers at `:457` and `:477` (`:477` also names adding no further locale behavior beyond the recorded samples, KO mirror title, and hangul-first body size). Byte-exact: "We invent the future of creativity with AI" DESIGN dest 4; "The world's most expressive AI voice" dest 4; "What are you making? Let's bring it to life" dest 3; "TRY FOR FREE" dest 11; "Try me" dest 10; "an AI startup at the forefront" dest 1. |
| §11 Brand Narrative | 옮김 → Experience Scope ¶3 | `DESIGN.md` `:13`: 2017 founding; 김태수 (Taesu Kim, CEO); dated list stretching back to 2018; Diff-TTS, EdiTTS, MLP Singer, face reenactment; Typecast; Typecast SSFM ("controllable speech/video synthesis"); cross-speaker emotion transfer; millions of creators; refuses/embraces closing through "picking an emotion and pressing \"Try me.\"" Each source paragraph including the last sentence is kept as one unit. HTML-comment "not directly quoted from a verified company statement in this turn" bound is adjacent in the same paragraph and in provenance. Classification qualifier at `:13` (B2/B2a). |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` `:45`–`:51` under the B2a form at `:45`: "These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Neosapience-authored or a separately published UI specification." No separately published Neosapience UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` `## Derived editorial inventory` (24 data rows). |
| §13 Personas | 삭제 (field boundary) | Source labels three fictional archetypes. No name, age, city, biography, motivation, or affiliation classification is promoted or re-hosted (D2, D2a). Audience at `DESIGN.md` `:29` reads only source-named groups: "investors, researchers, and recruits" (src 2); "millions of creators" (src 1); "creators using AI voice, ML researchers, and recruits" (src 1). Primary tasks at `:19`–`:24` come from surfaces and verbatim labels; `:19` says so. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source body preserved at `DESIGN.md` Empty / Loading / Error / Success / Disabled table (A2; catalog graph still 0/440, so nothing is delegated). Philosophy-layer bound is in the capture-record qualifier at `:218`. Placeholder wrappers were not present. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Feature Tab, Emotion Preset Chip, Use-case Card, and Corporate Nav Link close loading/error/success with a role reason (C2 v10) — they commit no operation in place. Typecast Primary CTA closes loading/error/success because the public hero CTA hands off. Typecast Inline CTA keeps loading and error applicable because that control commits voice synthesis; success is not-applicable because playback is not a completion outcome of that control. `corporate-card` and `research-item` omit kind and map (C4). |
| §15 Motion & Easing | 옮김 → Foundations motion; unattributed curves 삭제 | Durations kept: `motion-fast` 120ms, `motion-standard` 200ms, `motion-slow` 320ms. Easing *names and uses* kept; cubic-bezier *values* omitted. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` DESIGN dest 0 / provenance dest 1 (omission ledger). `ease-enter` `cubic-bezier(0.2, 0.6, 0.25, 1)` DESIGN dest 0 / provenance dest 1. Signature motion, no-bounce, and `prefers-reduced-motion: reduce` kept. B3 is held: the promotion condition at `DESIGN.md` `:169` names all five evidence kinds — transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus "A single confirmed curve does not satisfy that condition" (E2c). Qualifiers at `:151` and `:167`. |
| HTML comment (philosophy layer / inspect notes) | 분리 → provenance; inspect rgb/hex 옮김 → Typography live inspect; founding bound 옮김 → Scope ¶3 | Live inspect notes transcribed in provenance. Founding bound also in portable Scope because standalone interpretation needs the evidence class (E1). |

## Sibling handling (`web/references/neosapience/.verification.md`)

The sibling exists — confirmed with `find web/references/neosapience -type f`. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its record is transcribed at provenance `## Sibling` and is **not** promoted into `DESIGN.md`.
- Sibling-only strings (DESIGN dest 0): "About Neosapience"; "회사 소개"; "미션"; "AI Voice Generator & Text-to-Speech \| Voiceover Tool"; "Dec 6, 2023 DRAFT: Dense Retrieval…"; nav height 40px; body `line-height: 24px`; `padding: 32px 0px 40px`; `rgba(254,126,67,0.1)`; logo 657 bytes.
- Hex, family, CTA geometry, KO title, and H1 "네오사피엔스 소개" that the sibling repeats are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. A hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 22 distinct | 0 | 0 | 네오사피엔스 / TRY FOR FREE / Try me / "We invent the future of creativity with AI" / "The world's most expressive AI voice" / "What are you making? Let's bring it to life" / "an AI startup at the forefront" / Happy · Paige / Sad · Nia / Angry · Riley / Whisper · Chad / Text-to-Speech / Smart Emotion / Voice Cloning / About / Mission / Our tech / Research / Careers / Kid / TikTok / 네오사피엔스 소개 / KO title. |
| YAML `use` strings | 16 distinct | 0 | 0 | Eight type-role uses + eight component uses restored byte-exact. |
| `node test-v2/tools/latin-copy-audit.mjs --brand neosapience --candidate docs/design-md-weight/migrated/neosapience/DESIGN.md` | 0 lost / 55 candidates | 0 published | 0 | First pass lost 6 medium (CSS/YAML fragments `#e5e5e5 border`, `1px solid #000000`, `2px solid #ffc98f`, plus Tier-2/URL). CSS/YAML restored in DESIGN; Tier-2 strings live in provenance. Re-run: 0 lost. |
| Sibling published strings | 5 sibling-only issued labels | 0 | 0 | "About Neosapience"; "회사 소개" / "미션"; product `document.title`; research-row sample title — transcribed in provenance, not promoted. |

A5 분모: hand sweep of source published labels 22 extracted / 0 missing; YAML use 16 / 0; latin-copy-audit 0 lost / 55 candidates (after restore); gate `copy-loss` compared 3 / candidates 193.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand neosapience --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 3, candidates: 193, detail: "인용 문자열 193개 중 3개만 비교했다 — 나머지는 라틴이라 바늘이 되지 않는다. 라틴 전수 대조는 손으로 하라." }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 3 < `candidates` 193.

## Deviations recorded

- `DESIGN.md` is 7,153 words by Python `split()`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: seventeen color keys, eight YAML type roles, eight declared components with seven-state applicability on six interactive controls, YAML unitless and §3 rem keep-both, the full §11 founding-and-research narrative including the closing refuses/embraces sentence, §14 body, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published Neosapience UI specification is named in the source, so every derived-editorial close uses the toss-form `not Neosapience-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Measure (`grep -o` per file): `derived editorial implementation inference` DESIGN = `not Neosapience-authored` DESIGN = `separately published UI specification` DESIGN = 24. Provenance derived ledger 24 rows under `## Derived editorial inventory` with header `| Location in DESIGN.md | Qualified reading |` (E1 1:1). `node scripts/check-limiter-ledger.mjs neosapience` 본문 24 = 원장 24 (153–176).
- F1 B2a scan (F3 remasure): adjacent complete qualifiers at DESIGN.md 9, 11, 13, 19, 29, 33, 45, 55, 68, 85, 119, 135, 147, 151, 167, 184, 195, 210, 218, 418, 452, 457, 477, 513. Under-named readings folded into those existing closes (jobs at `:19`; canvas-vs-CTA-text at `:85`; 32–40px notable at `:119`; fallback/system-face at `:184`; inline demo CTA off Button at `:195`; component-local geometry at `:218`; no further locale at `:477`). A D1 over-defense ("not a native-application claim") on the breakpoint table was removed before worker submit. "Does not say that anything measures 1440px" was never written; `1024–1440px` is the source's own breakpoint.
- F2 E2 (F3 remasure, `grep -o` per file, never `grep -c`): Exact `tokens.source: live-extract` DESIGN dest 0 / provenance dest 2 (log had dest 3; Identity table uses `tokens.source | live-extract`, not the exact colon form). Dual destinations: homepage `https://neosapience.com` DESIGN 2 / provenance 4; `primary_color` DESIGN 4 / provenance 2; `favicon` DESIGN 3 / provenance 4; 네오사피엔스 DESIGN 8 / provenance 4; `box-shadow: none` DESIGN 1 / provenance 1; omitted `cubic-bezier(0.4, 0.0, 1, 1)` / `cubic-bezier(0.2, 0.6, 0.25, 1)` DESIGN 0 / provenance 1; Tier-2 "No designs found for 'neosapience'" / "No designs found for 'typecast'" / `?q=typecast` DESIGN 0 / provenance 1. `32–40px` DESIGN dest 2. `TRY FOR FREE` DESIGN dest 11. `Try me` DESIGN dest 10. `tokens.colors.primary` DESIGN dest 4. `tokens.colors.product-cta` DESIGN dest 3. `tokens.rounded.full: 9999` DESIGN dest 2. B3 five-kind 전문 at `:169` dest 1.
- Unique-phrase contrast (wave 43): extracted 55 source-unique expressions (years, proper nouns, quotes, §11 causal sentences, value modifiers, §15/§7 constraints); first DESIGN draft had 2 at 0 (`the split is the whole story`, `What unifies the two systems`) and both were restored; final 0 remaining. Restored=2 / extracted=55.

F3-audit portable DESIGN SHA-256 `4de20821573e031d8264aa507be64466e7e25451bf52f2dff0078049da1daa03`. Worker-close SHA `daec9d41526a7fa2c70f78320fae3e3cc2181d1401b6e6982f9821cf2172005c` is the pre-audit body.
