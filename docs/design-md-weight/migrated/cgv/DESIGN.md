# CGV Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

CGV is Korea’s multiplex cinema brand and a consumer-facing service for finding films, choosing a theatre, and moving toward a viewing experience. Catalog homepage identity is `https://www.cgv.co.kr/`.

Treating the following three 2026-07-13 capture records as the named live-inspect evidence domains of this reconstruction, treating catalog homepage `https://www.cgv.co.kr/` as identity rather than a captured page, treating captured host `https://cgv.co.kr/` as unmerged from that `www` identity, treating those records as not a claim about an authenticated booking flow, seat selection, payment, or theatre-operation layout, treating this-contract-covers-three-supplied-public-captures-of-that-captured-host as the reconstruction coverage sentence, and treating the source token note’s interaction-count-is-zero-so-only-default-component-values-are-promoted causal as a reconstruction reading rather than a CGV-authored promotion rule, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. This contract covers three supplied public CGV captures of `https://cgv.co.kr/` from 2026-07-13 (YAML ids `home`, `surface-2`, `surface-3`; kinds marketing-product and public-home-variant). Token note: three supplied public CGV captures (coverage 81). Pretendard is loaded/high with 1,217 visible uses; interaction count is zero, so only default component values are promoted. Token extraction is `live-extract`.

The following atmosphere readings — restrained public-web palette rather than a historical CGV logo or corporate colour system, crisp and utility-led rather than theatrical by default, white `#ffffff` canvas with near-black `#121212` information hierarchy, `#f4f4f4` / `#454545` rounded category treatment, `#d9d9d9` outline actions, compact 10px / 14px / 15px / 22px text roles, role-specific 4px / 16px / 20px / 25px geometries rather than a universal radius, no red primary action invented from brand familiarity, square-list-rows-coexist-with-4px-format-badges-16px-media-corners-20px-outline-actions-and-25px-category-chips, and restrained-public-web-palette-describes-the-supplied-current-capture-only — are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification. The captured interface is crisp and utility-led rather than theatrical by default. Square list rows coexist with 4px format badges, 16px media corners, 20px outline actions, and 25px category chips. That restrained public-web palette describes the supplied current capture only.

Treating the following public-history and current-evolution facts as narrative rather than interface tokens, including Gangbyeon 1998 as Korea’s first multiplex, the CJ Village / CJ Golden Village lineage into the CJ CGV corporate identity, the Cultureplex proposition beyond a conventional screening venue, and the 2024 “Deep Dive Space” rebranding campaign around immersive experience, special formats, CGV-only content, and expanded space use, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. CGV introduced the multiplex format in Korea with Gangbyeon in 1998 and has evolved its stated Cultureplex proposition beyond a conventional screening venue. Its 2024 sustainability report describes a rebranding campaign around “Deep Dive Space,” positioning the business around immersive experience, special formats, CGV-only content, and expanded space use. Official history and that report inform this narrative, not the live web tokens.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML public-component use strings below as the independently verified user outcomes, not lifting source §13 moviegoer / Cultureplex-visitor / special-format groups into this list, and treating those YAML use strings as harvested-home-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

- Select a public main category on the captured CGV home (YAML use: `Public main-category tab`).
- Scan movie-card screen-format markers on that home (YAML use: `Movie-card screen-format marker`).
- Use a rounded public movie-card action on that home (YAML use: `Rounded public movie-card action`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, source §13 stakeholder groups are not this list’s primary jobs and not fictional biographies, tying observable work only to the three primary tasks, and treating the unique-relationship implications keep-essential-selection-information-clear / consistent-with-stated-Cultureplex-direction / format-claims-accurate-and-specific as source-stated group jobs rather than individual personas, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. No individual personas are promoted. Source §13 names stakeholder groups stated or directly implied by first-party CGV materials, not fictional personas or synthetic research. Use those groups only:

- **Moviegoers:** people choosing a film, a theatre, and a viewing format; the public experience should keep essential selection information clear.
- **Cultureplex visitors:** audiences using a cinema venue as a broader cultural and leisure destination, consistent with CGV’s stated Cultureplex direction.
- **Special-format audiences and partners:** people engaging with differentiated formats such as 4DX or ScreenX; format claims should remain accurate and specific.

### Distinctive traits

Treating `#121212` as catalog `primary_color` and the captured foreground rather than a red primary, treating `#fc5555` as a low-frequency signal rather than that primary or an error colour, treating canvas `#ffffff` as unmerged from outline-action fill `#ffffff`, treating `#f4f4f4` / `#454545` as the category-chip pair rather than canvas or foreground, treating `#d9d9d9` as the outline-action border rather than a fill, treating Pretendard as the loaded UI family rather than declared-only Roboto or swiper-icons, treating 25px / 20px / 16px / 4px / 0px as role-specific geometries rather than a universal radius, treating 10px menu-row type as unmerged from 14px body, and treating hover, focus, pressed, disabled, or error paints as not promoted from this capture, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

- White `#ffffff` public canvas with near-black `#121212` information hierarchy (catalog `primary_color`)
- Loaded Pretendard across the captured public surface (1,217 visible uses)
- `#f4f4f4` / `#454545` rounded category treatment and `#d9d9d9` outline actions
- Compact 10px, 14px, 15px, and 22px observed text roles
- Static evidence for buttons, badges, and a navigation row; no interaction state values

### Principles

These four items, including each stem and each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification.

1. **Evolve beyond a screening transaction.** CGV’s official mission frames the brand as more than a film-viewing environment. *UI implication:* connect practical selection steps to confirmed format and venue context.
2. **Make differentiated experiences legible.** Special formats and content are part of the stated direction. *UI implication:* present verified format information as clear, compact metadata.
3. **Design for cultural use, not only attendance.** The Cultureplex proposition treats the venue as a broader cultural space. *UI implication:* avoid reducing supporting discovery content to decoration when it informs a real visit.
4. **Keep practical decisions clear.** A cinema service still needs reliable choices around title, time, location, and format. *UI implication:* preserve strong text hierarchy and explicit actions.

Treating the following as a capture-bound application of source §7 Do’s, including `#121212`-on-`#ffffff` contrast hierarchy, rounded-category-and-outline-action-only-in-observed-public-roles, and screen-format-metadata-compact-and-subordinate, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

- Preserve a clear contrast hierarchy with `#121212` content on `#ffffff` surfaces.
- Use the verified rounded category and outline-action patterns only in their observed public roles.
- Keep screen-format metadata compact and subordinate to movie information.

### Avoid

The following items copy source §7 Don’ts and source §9 / §5 bounds, including `#fc5555`-not-a-universal-primary-or-error-colour, no-assumed-hover-focus-pressed-disabled-or-error-values, Roboto-or-swiper-icons-or-a-system-fallback-not-Pretendard, screen-format-badge-colour-not-the-only-format-signal, and home-capture-not-generalized-into-authenticated-seat-selection-payment-or-theatre-operation. They are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification.

- Do not treat the low-frequency `#fc5555` observation as a universal CGV primary or error colour.
- Do not add hover, focus, pressed, disabled, or error component values from assumptions.
- Do not substitute Roboto, swiper-icons, or a system fallback as though it were the loaded Pretendard UI family.
- Do not rely on screen-format badge colour alone to convey movie-format information.
- Do not generalize the supplied home capture into authenticated seat selection, payment, or theatre-operation layouts. The last item’s home-capture-not-generalized-into-authenticated-seat-selection-payment-or-theatre-operation reading is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-primary-as-foreground-`#121212`-not-a-red-primary, canvas-white-not-a-second-paint-from-outline-action-fill, subtle-control-`#f4f4f4`-not-canvas, secondary-text-`#454545`-not-foreground, line-`#d9d9d9`-as-outline-border-not-a-fill, and signal-`#fc5555`-not-primary-and-not-error-success-warning-hover-pressed-or-focus, are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification.

- **Foreground** (`#121212`): catalog `primary_color`. Dominant captured text, border, and dark surface value.
- **Canvas** (`#ffffff`): repeated page and outlined-action surface. Same hex as the Outline Action fill; named jobs stay unmerged.
- **Subtle control** (`#f4f4f4`): observed category-chip background and border.
- **Secondary text** (`#454545`): category-chip foreground.
- **Line** (`#d9d9d9`): observed rounded outline-action border.
- **Signal** (`#fc5555`): a low-frequency public text/border observation; it is preserved as a signal value, not promoted to a primary or state palette.

No verified current error, success, warning, hover, pressed, or focus palette was supplied. Those groups are omitted.

### Spacing

YAML `spacing` is unitless: dense 10, sm 8, base 16, lg 24. Treating those YAML numbers as unitless token numbers rather than a claimed universal px scale, while keeping the §5 observed 8/10/16/24px recurring set and 10px metadata as a separate observation, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

Body layout repeats the source 8/10/16/24px set. Treating harvested control padding as staying with those controls rather than as a universal padding token, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. Harvested control padding stays with those controls: category chip `0px 16px`, outline action `9px 24px`, screen-format badge `0px 5px`.

### Shape

YAML `rounded`: badge 4, media 16, action 20, chip 25.

The following local-geometry reading, including Badge-4px, Media-16px, Outline-action-20px, Category-chip-25px, Menu-row-0px, and not-a-universal-radius, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

- Badge (YAML `badge` 4): screen-format marker `4px`
- Media (YAML `media` 16): 16px media corners
- Action (YAML `action` 20): outline action `20px`
- Chip (YAML `chip` 25): category chip `25px`
- Menu row: `0px`

Treating 4 / 16 / 20 / 25 / 0 as role-specific harvested geometry rather than a universal radius for every unlisted control, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. This reference keeps those role-specific geometries separate instead of inventing a universal radius.

### Elevation

YAML `shadow.flat`: `none`.

| Level | Treatment | Use |
|---|---|---|
| Flat | `none` | YAML `shadow.flat` |



### Motion

No duration, easing, transition, or animated-state value is present in the supplied evidence. No motion token is prescribed for CGV in this reference. Treating no-motion-token-prescribed-for-CGV-in-this-reference as source-stated-absence rather than a captured motion system, and treating that absence as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings of Official product-use, Live computed surface-use, Official distributed brand asset, Declared-only, Outside these captures, and Licence — including official-product-use-no-CGV-authored-type-spec, live-computed-pretendard-1,217-uses, nine CDN webfont sources as FontFace corroboration rather than a CGV-owned face, official-distributed-no-CGV-exclusive-face, declared-only-Roboto-and-swiper-icons-not-UI-family-tokens, outside-these-captures-native-app-authenticated-booking-and-special-format-campaign, Pretendard as the UI family rather than Roboto or swiper-icons, and OFL 1.1 as the font-project licence rather than a CGV brand asset — are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | No official CGV product typography specification was supplied or used for token promotion. |
| Live computed surface-use | `pretendard` is loaded/high with 1,217 visible uses across headings, body, buttons, cards, dialogs, inputs, list items, and badges in the supplied capture. |
| FontFaceSet and source corroboration | The supplied font evidence links nine CGV CDN Pretendard webfont sources and backs the computed family with a loaded FontFace. |
| Official distributed brand asset | No CGV-exclusive distributed font asset was verified. |
| Declared-only | `Roboto` and `swiper-icons` appear in declarations or sources with zero visible text usage; neither is a UI family token. |
| Outside these captures | Native-app, authenticated booking-flow, and special-format campaign typography are not resolved by this capture. |
| Licence | Pretendard’s own distribution is under the SIL Open Font License 1.1; that licence describes the font project, not a CGV-owned font asset. |

### Family

- **Current public UI family:** `Pretendard` followed by the observed system fallbacks. YAML `family.sans`.

The following font-use boundary, including Pretendard-as-the-loaded-UI-family, observed-system-fallbacks-not-named-as-a-substitute, and Roboto-or-swiper-icons-not-UI-family-tokens, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. Do not name those fallbacks as a substitute family. Do not present a system stack as Pretendard. Do not substitute Roboto, swiper-icons, or a system fallback as though it were the loaded Pretendard UI family.

### Type roles

Verified YAML line-height values are the unitless ratios `1.4` (display, body) and `1` (label). The following ratio-versus-size-local readings, including ratios-scale-with-font-size-not-fixed-px, 30.8px-beside-Prominent-heading-1.4, 19.6px-beside-Public-body-1.4, 15px-beside-Category-label-1, 19px-beside-Screen-marker-as-body-table-only, YAML-tracking-`-0.4`/`-0.2`-unmerged-from-`-0.4px`/`-0.2px`, and Screen-marker-11px-700-not-a-YAML-typography-role, are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. 30.8px beside Prominent heading `1.4`, 19.6px beside Public body `1.4`, and 15px beside Category label `1` are size-local observations, not replacements for the ratios. YAML tracking is unitless `-0.4` / `-0.2`; the body table writes `-0.4px` / `-0.2px`. Screen marker 11px / 700 / 19px / `-0.2px` is the body-table observation for the movie-card format badge; it is not a YAML `tokens.typography` role.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking (YAML / table) | Use (YAML) |
|---|---|---:|---:|---:|---|---|---|
| Prominent heading | Pretendard | 22px | 700 | 1.4 | 30.8px at this size | -0.4 / -0.4px | Observed prominent body-role heading |
| Public body | Pretendard | 14px | 400 | 1.4 | 19.6px at this size | -0.2 / -0.2px | Observed public-page body and input copy |
| Category label | Pretendard | 15px | 400 | 1 | 15px at this size | -0.2 / -0.2px | Observed category-tab label |
| Screen marker | Pretendard | 11px | 700 | — | 19px at this size | — / -0.2px | Movie-card format badge |

### Assets

Treating catalog logo metadata as a Google favicon lookup (`https://www.google.com/s2/favicons?domain=cgv.co.kr&sz=128`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

Movie artwork and screen-format labels carry much of the public discovery context; the verified badge sits as a compact overlay. `swiper-icons` is declared-only and is not promoted as a CGV icon token. No CGV brand-icon grid, illustration style, or imagery treatment beyond these supplied public observations is specified here. Treating movie-artwork-and-screen-format-labels-carry-much-of-the-public-discovery-context as a reconstruction reading, treating the verified badge as a compact overlay rather than a brand-icon system, treating movie artwork as captured/recorded surface imagery; ownership unclaimed rather than invented brand-color decoration, treating swiper-icons as declared-only rather than an icon library, and treating no-CGV-brand-icon-grid-illustration-style-or-imagery-treatment-beyond-these-supplied-public-observations as unclaimed rather than a negative doctrine, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, treating the following content requirements as not token values and as philosophy-layer / §14-as-source-stated-contract-not-computed-CSS, and treating them as must-be-designed-only-with-separately-verified-visual-evidence, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

No visual state system was captured. The following content requirements are not token values and must be designed only with separately verified visual evidence.

| Category | Requirement |
|---|---|
| Empty | Explain when no film, theatre, or schedule is available. |
| Loading | Preserve context while programme or booking data is pending. |
| Error — availability | State when a requested session is no longer available. |
| Error — network | State when current information could not be retrieved. |
| Success | Confirm the completed user action with the relevant context. |
| Skeleton | Reserve structure without asserting unmeasured geometry. |
| Disabled | Explain why a booking or selection action cannot proceed. |

Treating only the selector-backed static variants below as canonical tokens, treating zero-interaction-observations as the reason no interactive states are inferred, and treating accessible-focus-treatment-and-form-validation-styling-require-separate-verified-evidence as a reconstruction bound rather than a captured focus token, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. The capture contains 383 component variants over three supplied public surfaces. Only the selector-backed static variants below are canonical tokens; 0 interaction observations means no interactive states are inferred. The supplied capture has no focus or error-state observation; accessible focus treatment and form validation styling require separate verified evidence.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

YAML `tokens.components` records Category Chip and Outline Action as `type: button`, Screen Format Badge as `type: badge`, and Menu Row as `type: listItem`. Treating those YAML primitives as preserved per component rather than inverted (button not rewritten as tab, listItem not recast as button, badge not given an invented interactive kind), is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

### Category Chip

- Role: public main-category tab
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#f4f4f4`
- Text: `#454545`
- Border: `1px solid #f4f4f4`
- Radius: 25px
- Padding: 0px 16px
- Height: 40px
- Font: 15px / 400 / Pretendard
- Use: YAML `Public main-category tab`
- Observed: default only at `home::[data-omd-capture="36"]`; no hover, focus, pressed, disabled, or error state observed
- Field note: The following unmerged-field reading, including this-fill-not-canvas-`#ffffff`, this-foreground-not-`#121212`, 25px-not-outline-20px-not-badge-4px, 40px-matching-menu-row-height-not-badge-20px, 15px-400-matching-Category-label-not-outline-14px-500, and YAML-type-button-not-inverted-to-tab, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. `#f4f4f4` is this control’s fill and border. `#454545` is this control’s label. Radius 25px is this chip; it is not outline 20px and not badge 4px. Height 40px is this control. Font 15px / 400 matches Category label; it is not Outline Action 14px / 500. YAML `type` is `button`; Use names a tab; both records are kept and type is not inverted to tab.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public main-category tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A category tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A category tab selects a programme grouping; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the control |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

### Outline Action

- Role: rounded public movie-card action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#121212`
- Border: `1px solid #d9d9d9`
- Radius: 20px
- Padding: 9px 24px
- Font: 14px / 500 / Pretendard
- Use: YAML `Rounded public movie-card action`
- Observed: default only at `home::[data-omd-capture="128"]`; no hover, focus, pressed, disabled, or error state observed
- Field note: The following unmerged-field reading, including this-fill-as-canvas-hex-not-chip-`#f4f4f4`, this-foreground-as-catalog-primary-not-chip-`#454545`, `#d9d9d9`-border-not-a-fill, 20px-not-chip-25px, 9px-24px-not-chip-0px-16px, and 14px-500-not-Public-body-14px-400, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. `#ffffff` is this control’s fill, same hex as canvas; jobs stay unmerged. `#121212` is this control’s label. `#d9d9d9` is this control’s border. Radius 20px is this action; it is not chip 25px. Padding `9px 24px` is this action. Font 14px / 500 is this control; it is not Public body 14px / 400.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the rounded public movie-card action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A movie-card action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a rounded public movie-card action; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Screen Format Badge

- Role: movie-card screen-format marker
- Type: badge
- Anatomy: overlay label
- Background: `rgba(0, 0, 0, 0.55)`
- Text: `#121212`
- Radius: 4px
- Padding: 0px 5px
- Height: 20px
- Font: 11px / 700 / Pretendard
- Use: YAML `Movie-card screen-format marker`
- Field note: The following unmerged-field reading, including rgba-overlay-not-a-solid-canvas-or-chip-fill, this-foreground-as-catalog-primary-on-the-overlay, 4px-not-chip-25px-not-outline-20px, 20px-height-not-chip-40px, 11px-700-not-a-YAML-typography-role, and YAML-type-badge-with-no-interactive-kind-confirmation, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. `rgba(0, 0, 0, 0.55)` is this marker’s fill. `#121212` is this marker’s label. Radius 4px is this badge. Height 20px is this marker. Font 11px / 700 is the Screen marker body-table row, not a YAML `tokens.typography` role. YAML `type` is `badge`.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Menu Row

- Role: observed public navigation/menu row
- Kind: interactive
- Type: listItem
- Anatomy: text row
- Background: transparent
- Text: `#121212`
- Radius: 0px
- Height: 40px
- Font: 10px / 400 / Pretendard
- Use: YAML `Observed public navigation/menu row`
- Field note: The following unmerged-field reading, including transparent-not-chip-`#f4f4f4`, this-foreground-as-catalog-primary, 0px-not-chip-25px, 40px-matching-chip-height-not-badge-20px, 10px-400-not-Public-body-14px-400, YAML-type-listItem-not-recast-as-button, and kind-interactive-from-navigation-row-Use-not-a-rewrite-of-YAML-type, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. Fill is transparent. `#121212` is this row’s label. Radius 0px is this row. Height 40px matches Category Chip; jobs stay unmerged. Font 10px / 400 is this row; it is not Public body 14px / 400. YAML `type` is `listItem`; it is not recast as a button. Kind `interactive` is the navigation/menu-row Use, not a rewrite of that YAML type.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public navigation/menu row |
| hover | applicable | Pointer-web navigation row; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A navigation row can be unavailable; visual treatment omitted |
| loading | not-applicable | A navigation/menu row selects a destination; the row itself does not enter a loading state |
| error | not-applicable | Navigation-row meaning is destination versus resting, not a request or validation failure on the row |
| success | not-applicable | Navigation-row meaning is destination selection, not action-outcome confirmation |

Treating headings, body, buttons, cards, dialogs, inputs, list items, and badges in the font-evidence live-use list as not additional canonical components, and treating kind and applicability maps as not invented for those unnamed variants, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification. Headings, body, buttons, cards, dialogs, inputs, list items, and badges appear in the font-evidence live-use list. They are not additional canonical components. Kind and applicability maps are not invented for those unnamed variants.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is dense 10, sm 8, base 16, lg 24. Body layout repeats the source 8/10/16/24px observed spacing set, plus 10px metadata. Harvested heights: 40px category chip, 40px menu row, 20px screen-format badge.

The following layout readings — white-public-surfaces-with-near-black-hierarchy, dense-10px-metadata, 40px-category-and-menu-controls, role-specific-25px-20px-16px-4px-rounding, three-capture-surfaces-without-a-responsive-breakpoint-system, 40px-and-20px-as-desktop-capture-measurements-not-cross-viewport, home-capture-not-generalized-into-authenticated-seat-selection-payment-or-theatre-operation, preserve-observed-component-values-only-where-they-remain-appropriate, and breakpoint-navigation-collapse-and-seat-map-behavior-unresolved — are a derived editorial implementation inference from the verified surfaces; they are not CGV-authored or a separately published UI specification.

Recorded layout:

- Use white public surfaces and a near-black information hierarchy where the captured CGV pattern is relevant.
- Keep dense content roles compact: 10px metadata, 8/10/16/24px recurring spacing, and 40px category/menu controls where specifically observed.
- Treat rounded controls as role-specific: 25px category chip, 20px outline action, 16px media, and 4px badge.
- The supplied evidence confirms public components across three capture surfaces but does not establish a responsive breakpoint system.
- Preserve the observed component values only where they remain appropriate; breakpoint, navigation collapse, and seat-map behavior are unresolved.

Treating 40px / 20px as surface measurements in this packet rather than a cross-viewport specification, and treating no-focus-or-error-state-observation as a reason for an implementation to supply its own accessible focus rather than inferring one from the recorded radii, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The corporate material frames CGV around immersive, differentiated cultural experience. Treating corporate-material-frames-CGV-around-immersive-differentiated-cultural-experience as a voice-context reading rather than a UI-token claim, and treating the reconstruction that public UI copy should be direct, experience-oriented, and concise rather than overly cinematic as a voice reading of that corporate framing rather than a CGV-authored microcopy specification, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

The following voice-table labels — Clear, Immersive, Considerate — and their Use / Avoid pairings are source-stated in this reconstruction. Treating those labels as reconstruction voice qualities rather than a complete product-microcopy guide, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

| Voice quality | Use | Avoid |
|---|---|---|
| Clear | Name the film, theatre, format, and next action plainly. | Vague promotional instructions. |
| Immersive | Describe a confirmed format or space experience. | Promising an unverified sensory outcome. |
| Considerate | Give timing and booking information in compact language. | Hiding practical constraints behind campaign language. |

- “영화와 상영 시간을 선택하세요.” *(illustrative; no claim of current CGV copy)*
- “특별관 정보를 확인하세요.” *(illustrative; no claim of current CGV copy)*
- “예매 내용을 다시 확인하세요.” *(illustrative; no claim of current CGV copy)*

Treating those three Korean lines as illustrative reconstruction samples rather than captured CGV microcopy, and treating the §14 empty/loading/error/success/skeleton/disabled rows as the state-content contract rather than extra voice samples, is a derived editorial implementation inference from the verified surfaces; it is not CGV-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- error, success, warning, hover, pressed, and focus palette
- hover, focus, pressed, disabled, loading, error, success, empty, skeleton, dialog, and form-error visual treatments
- Outline Action loading·error·success applicability (exact request/outcome unresolved)
- Screen Format Badge kind and state-applicability map (no interactive-kind confirmation)
- responsive breakpoint system, navigation collapse, and seat-map behavior
- authenticated seat selection, payment, and theatre-operation layouts
- native-app, authenticated booking-flow, and special-format campaign typography
- CGV-exclusive distributed font asset; official CGV product typography specification
- CGV brand-icon grid, illustration style, and imagery treatment beyond the supplied public observations
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
