# Brandi Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Brandi is an official Korean women's fashion shopping service. The public site title calls it “여성 패션 쇼핑앱 브랜디.” Catalog homepage identity is `https://www.brandi.co.kr`.

Treating the following three URLs as the named live-capture evidence domains of this reconstruction, treating those three current first-party public routes as this contract’s coverage, treating values as attached to the route and selector that established them, and treating home-route `#ff365d` as route-local promo evidence rather than a purchase-link fill, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. This contract covers three current first-party public routes from the 2026-07-13 capture: home `https://www.brandi.co.kr/`, product `https://www.brandi.co.kr/products/106329458`, and product `https://www.brandi.co.kr/products/125381184`. Values below stay attached to the route and selector that established them. Home-route `#ff365d` stays with the active promotional slider; it is not a purchase-link fill.

The following atmosphere and action-ranking readings — predominantly white commerce surfaces with `#202429` text, the clearest action treatment not being the legacy pink claim, direct-purchase `#1e1e1e` with white text versus the adjacent partner `#00c73c` path, and home-slider `#ff365d` therefore recorded as route-local promo evidence rather than a universal Brandi action token — are a derived editorial implementation inference from the verified surfaces; they are not Brandi-authored or a separately published UI specification. Those public commerce surfaces are predominantly white with `#202429` text. The direct purchase link is `#1e1e1e` with white text, while an adjacent green `#00c73c` link is a partner purchase path. On the home route, `#ff365d` appears on an active promotional slider item.

Treating the two captured item pages’ concentration around choosing an option and proceeding to purchase as the product-evidence focus of this reconstruction, rather than a published Brandi journey map, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. The two captured item pages concentrate the available product evidence around choosing an option and proceeding to purchase.

Treating desktop-only as the evidence-domain bound of this reconstruction, and treating an app, marketing, documentation, or account-area system as not established beyond these public routes, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. The capture is desktop-only and does not establish an app, marketing, documentation, or account-area system beyond these public routes.

Treating the following public-identity and legal-context facts as narrative rather than interface tokens, and treating them as not authority for a broader origin story, market position, mission, or rebrand narrative, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. The site footer identifies Newnex as the hosting operator and describes its payment/intermediation boundary. These are product and legal-context facts, not authority for a broader origin story, market position, mission, or rebrand narrative.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three captured product-detail controls below as the independently verified user outcomes, and not lifting tasks from source §13 (which records no first-party stakeholder groups), is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

- Choose a product option on a captured product-detail page.
- Proceed to purchase via the captured direct purchase link (`.btn-buy`).
- Follow the adjacent partner purchase path on the same product pages (`.btn-n-buy`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, recording that source §13 has no first-party stakeholder groups or research and includes no synthetic personas, and tying independently verified work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. No individual personas are promoted. Source §13 records no first-party, source-backed stakeholder groups or research, and includes no synthetic personas.

### Distinctive traits

Treating `#1e1e1e` as catalog `primary_color` and the direct-purchase fill rather than partner `#00c73c` or promo `#ff365d`, treating white commerce canvas `#ffffff` with `#202429` ink as spanning all three captured routes, treating canvas `#ffffff` as unmerged from on-action `#ffffff`, treating `#ff365d` as not a general CTA rule, treating `#e6e6e6` as unmerged from disabled-option `#e1e1e1`, treating `#ebeef2` / `#808893` as the badge pair rather than purchase fills, treating 6px as local harvested geometry on the recorded controls rather than a universal radius, treating listbox `0px 0px 6px 6px` as unmerged from 6px all-around, treating Noto Sans KR as the sole UI-family token rather than Spoqa Han Sans, treating Spoqa Han Sans as live surface use rather than an additional UI-family token, treating Pretendard as declared-only rather than a substitute family, and treating Arial as system-only rather than a Brandi font claim, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

- White commerce canvas `#ffffff` with `#202429` ink across all three captured routes
- Product-detail purchase pair: direct `#1e1e1e` (catalog `primary_color`) and a separate green partner action `#00c73c`
- On-action text `#ffffff` as a named field, unmerged from canvas `#ffffff`
- Active home promotional slider evidence `#ff365d`, not a general CTA rule
- 6px rounding on the recorded product actions, option control, listbox, and badge; listbox corners are `0px 0px 6px 6px`
- Noto Sans KR as the sole `tokens.typography.family.ui` family; Spoqa Han Sans as live surface use, not an additional UI-family token
- Pretendard declared-only; Arial system-only

### Principles

Treating official Brandi product or design principles as not presented by source §12, treating the observed commerce constraints as not official principles, and treating these capture-bound application items from source §7 and §16 Do’s — keep-direct-purchase-link-black-`#1e1e1e`-with-white-text, keep-green-`#00c73c`-purchase-link-scoped-to-the-observed-adjacent-partner-action, use-measured-6px-corners-and-13px-Spoqa-Han-Sans-option-text-only-for-the-recorded-product-option-controls, preserve-the-observed-option-listbox-and-disabled-border-as-distinct-states, treat-Noto-Sans-KR-and-Spoqa-Han-Sans-as-live-font-evidence-and-keep-Pretendard-declared-only, reuse-only-values-tied-to-a-captured-Brandi-route-and-selector, and keep-font-component-and-interaction-provenance-separate-by-surface-and-state — as reconstruction rules is a derived editorial implementation inference from the verified surfaces; they are not Brandi-authored or a separately published UI specification. Source §12 does not present official Brandi product or design principles. The observed commerce constraints are not official principles.

- Keep the direct product purchase link black `#1e1e1e` with white text when reproducing the captured product-page state.
- Keep the green `#00c73c` purchase link scoped to the observed adjacent partner action.
- Use the measured 6px corners and 13px Spoqa Han Sans option text only for the recorded product-option controls.
- Preserve the observed option listbox and disabled border as distinct states with their route/selector provenance.
- Treat Noto Sans KR and Spoqa Han Sans as live font evidence; keep Pretendard declared-only.
- Reuse only values tied to a captured Brandi route and selector.
- Keep font, component, and interaction provenance separate by surface and state.

### Avoid

The following items copy source §7 and §16 Don’ts — do-not-restore-legacy-pink-`#ff204b`, do-not-turn-home-slider-`#ff365d`-into-a-universal-product-action-token, do-not-infer-hover-focus-pressed-error-checkout-responsive-or-app-native-variants-from-the-two-recorded-menu-expansions, do-not-use-the-green-partner-action-as-Brandi-owned-palette-evidence, do-not-substitute-a-system-font-or-declared-only-Pretendard, do-not-promote-declared-only-assets-or-old-token-values, and do-not-invent-checkout-account-marketing-documentation-or-mobile-components — they are a derived editorial implementation inference from the verified surfaces; they are not Brandi-authored or a separately published UI specification.

- Do not restore the legacy pink `#ff204b` CTA, sale badge, wish-toggle, or card system from this evidence set.
- Do not turn the home slider’s `#ff365d` into a universal product action token.
- Do not infer hover, focus, pressed, error, checkout, responsive, or app-native variants from the two recorded menu expansions.
- Do not use the green partner action as evidence of a Brandi-owned brand palette.
- Do not substitute a system font or declared-only Pretendard for the captured loaded families.
- Do not promote declared-only assets or old token values into the current product system.
- Treating invention of checkout, account, marketing, documentation, or mobile components from these three public routes as forbidden is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Do not invent checkout, account, marketing, documentation, or mobile components from these three public routes.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-primary-as-direct-purchase-fill, partner-green-as-route-local-not-universal, on-action-unmerged-from-canvas, promo-active-as-home-slider-only, default-option-border-unmerged-from-disabled-option-border, badge-pair-unmerged-from-purchase-fills, canvas-as-observed-page-control-background-on-home-and-product, and ink-as-product-option-menu-and-general-text, are a derived editorial implementation inference from the verified surfaces; they are not Brandi-authored or a separately published UI specification. Catalog `primary_color` `#1e1e1e` is YAML `action-direct` and the `.btn-buy` fill; it is not partner `#00c73c` and not promo `#ff365d`. Canvas `#ffffff` is YAML `canvas`; YAML `action-on` also uses `#ffffff` as text on both purchase links and stays unmerged as a named field. Partner `#00c73c` is YAML `action-partner`, a route-local partner action, not a universal Brandi color. Promo `#ff365d` is YAML `promo-active`, observed only on active home slider list items. Option border `#e6e6e6` is the default selector border; disabled option border `#e1e1e1` is the disabled selector border; they stay unmerged. Badge surface `#ebeef2` and badge text `#808893` are the product-detail badge pair.

- **Canvas** (`#ffffff`): YAML `canvas`. Observed page/control background on the home and product routes.
- **Ink** (`#202429`): YAML `ink`. Observed product-option, menu, and general text color.
- **Direct purchase** (`#1e1e1e`): YAML `action-direct`. Background of `.btn-buy` on both captured product pages. Catalog `primary_color`.
- **Partner purchase** (`#00c73c`): YAML `action-partner`. Background of the adjacent `.btn-n-buy`; this is a route-local partner action, not a universal Brandi color.
- **On action** (`#ffffff`): YAML `action-on`. Observed text on both recorded purchase links. Same hex as Canvas; named field stays unmerged.
- **Promo active** (`#ff365d`): YAML `promo-active`. Observed only on active home slider list items.
- **Option border** (`#e6e6e6`): YAML `option-border`. Default product-option selector border.
- **Disabled option border** (`#e1e1e1`): YAML `option-border-disabled`. Observed disabled selector border.
- **Badge surface** (`#ebeef2`): YAML `badge-surface`. Product-detail badge fill.
- **Badge text** (`#808893`): YAML `badge-text`. Product-detail badge text.

Treating the absence of live `#ff204b` in the 2026-07-13 capture as a reason not to retain it as a current token is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. No `#ff204b` live use was recorded by the supplied 2026-07-13 capture, so it is not retained as a current token.

### Spacing

YAML `spacing` is unitless: action-x 4, action-y 18, option-item 16, badge-x 8, badge-y-start 2, badge-y-end 3. Treating those YAML numbers as unitless token numbers rather than a claimed universal px scale, while keeping the recorded control padding as component fields, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

Harvested control padding stays with those controls: product purchase `18px 4px`; option-listbox item padding 16px on the representative option wrapper; product-detail badge `2px 8px 3px`. Treating harvested control padding as staying with those controls, not as a universal padding token, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

### Shape

YAML `rounded`: product-action 6, option 6, option-menu 6, badge 6.

Observed radii in the body. The following local-geometry reading, including 6px as recorded-control corners rather than a universal radius for every unlisted control, listbox `0px 0px 6px 6px` as unmerged from 6px all-around, and YAML `rounded` 6 recorded without a px suffix while body observations use `6px`, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

- Recorded product actions, option trigger, and badge: `6px`
- Product option listbox: `0px 0px 6px 6px`

Treating 6px corners as local harvested geometry on the recorded product actions, option control, listbox, and badge rather than a universal radius for every unlisted control, and treating YAML `rounded` 6 as recorded without a px suffix while body observations use `6px`, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. 6px corners remain local harvested geometry on the recorded product actions, option control, listbox, and badge, not a universal radius for every unlisted control. YAML `rounded` 6 is recorded without a px suffix; the body observations use `6px`.

### Elevation

The following elevation readings, including `box-shadow: none` as a flat treatment for these observed elements only rather than a universal elevation token for unlisted surfaces, and no shadow scale or image overlay system established by this capture, are a derived editorial implementation inference from the verified surfaces; they are not Brandi-authored or a separately published UI specification. The representative purchase links, select trigger, listbox, and badge all have `box-shadow: none`. No shadow scale or image overlay system is established by this capture.

### Motion

No motion duration, easing curve, transition, or reduced-motion behavior was captured. None is specified. Treating that absence as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Noto-as-sole-UI-family-token, Spoqa-as-live-surface-use-not-additional-UI-family, Pretendard-declared-only, Arial-system-only, OFL-licence-context-not-Brandi-owned, and do-not-rename-Spoqa-to-a-newer-upstream-alias, are a derived editorial implementation inference from the verified surfaces; they are not Brandi-authored or a separately published UI specification.

| Family | Captured use | FontFaceSet / source corroboration | Resolution |
|---|---|---|---|
| `Noto Sans KR` | 257 visible uses across home and both product pages, including headings, controls, badges, and purchase links | `loaded`, high confidence, with 124 `fonts.gstatic.com` source URLs | **Verified live webfont.** It is the sole `tokens.typography.family.ui` family. The Noto documentation describes the collection’s OFL use boundary; that licence context does not make it a Brandi-owned font. |
| `Spoqa Han Sans` | 124 visible uses across body, menu, button, badge, and list-item roles | `loaded`, high confidence, backed by the captured FontFaceSet; the artifact lists no exact source URL for this family | **Verified live surface use**, preserved as route-local typographic evidence rather than an additional UI-family token. The Spoqa project distributes its family under SIL OFL and documents webfont use; do not rename the exact captured family to a newer upstream alias. |
| `Pretendard` | No visible use | 18 CDN `@font-face` sources but no computed use | **Declared-only.** It is not a Brandi UI token and is not substituted into examples. |
| `Arial` | Seven utility-button uses | System classification; no webfont source | **System-only.** Not a Brandi font claim. |

### Family

The following font-use boundary, including do-not-substitute-Pretendard-or-Arial and Spoqa-not-promoted-as-a-second-UI-family-token, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

- **Current visible UI family:** `Noto Sans KR`
- **Live surface use, not an additional UI-family token:** `Spoqa Han Sans`
- **Declared-only:** `Pretendard`
- **System-only:** `Arial`

Treating substitution of a system font or declared-only Pretendard for the captured loaded families as forbidden, and treating Spoqa Han Sans as not a second `tokens.typography.family.ui` family, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Do not substitute a system font or declared-only Pretendard for the captured loaded families. Do not present Spoqa Han Sans as a second `tokens.typography.family.ui` family.

### Type roles

Treating the unitless YAML ratio `1.0` as the verified line-height value that scales with font size rather than a fixed px, and treating body-table `normal` as a size-local observation rather than a replacement for that ratio, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Verified line-height values are the unitless YAML ratio `1.0` on product-action, option, and badge. They scale with font size and are not fixed px. The legacy body table recorded line height as `normal` at those captured sizes.

| Role | Font | Size | Weight | Line height | Size-local observation | YAML use |
|---|---|---:|---:|---:|---|---|
| Product purchase link | Noto Sans KR | 17px | 500 | 1.0 | normal | Observed direct and partner purchase links on both captured product pages. |
| Product option / expanded menu | Spoqa Han Sans | 13px | 400 | 1.0 | normal | Observed product-option select and expanded listbox. |
| Product badge | Noto Sans KR | 13px | 700 | 1.0 | normal | Observed product-detail badge. |

The following unmerged-role reading, including purchase-17px-500-Noto unmerged from option-13px-400-Spoqa and from badge-13px-700-Noto, and option-and-badge sharing 13px without merging, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Purchase 17px / 500 / Noto Sans KR is unmerged from option 13px / 400 / Spoqa Han Sans and from badge 13px / 700 / Noto Sans KR. Option and badge share 13px and are not merged.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

Treating Noto Sans KR and Spoqa Han Sans as loaded live faces rather than Brandi-exclusive distributed families, treating Pretendard as remaining declared-only, and treating the Noto documentation and the Spoqa project as licence/distribution context rather than Brandi-owned font assets, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Noto Sans KR and Spoqa Han Sans are loaded live faces, not Brandi-exclusive distributed families. Pretendard remains declared-only. The Noto documentation and the Spoqa project are licence/distribution context, not Brandi-owned font assets.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

Only the product-option expanded/listbox state and the disabled option border were observed. Empty, loading, error, success, cart, wish, sold-out, and validation treatments are not specified. For the product-option select, no hover, focus, pressed, validation, or selected-option styling is specified.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating a generic Focus observation as a different evidence type from `focus-visible`, treating loading/error/success as following product role rather than primitive kind, and omitting those three fields where request/outcome mapping is unresolved rather than closing them from the words “purchase link” or “select”, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `focus-visible` treatment and says focus styling is not specified; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. Where exact request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from “purchase link” or “select” as a primitive. This is not a complete state-coverage claim.

Treating Product option listbox as an expanded interaction appearance without a separate interactive-kind confirmation, and treating Product detail badge as `type: badge` without a §4.4 map, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Product option listbox is recorded as an expanded interaction appearance. Product detail badge is harvested as `type: badge` with no interactive-kind confirmation. Kind and a state-applicability map are omitted for those two.

### Direct purchase

- Role: direct product purchase link on both captured product pages
- Kind: interactive
- Anatomy: label
- Background: `#1e1e1e`
- Text: `#ffffff`
- Radius: `6px`
- Padding: `18px 4px`
- Font: `17px / 500 / Noto Sans KR`
- Use: `surface-2::[data-omd-capture="17"]` / `.btn-buy`, also observed on surface-3; direct product purchase link.
- Observed: default only
- Field note: YAML does not record a primitive `type` for this control; none is invented. The following unmerged-field reading, including yaml-primitive-type-not-invented, this-fill-as-catalog-primary-not-partner-and-not-promo, and on-action-unmerged-from-canvas, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. `#1e1e1e` is this control’s fill and catalog `primary_color`. It is not partner `#00c73c` and not promo `#ff365d`. `#ffffff` is this control’s on-action label, not a second canvas token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on both product pages as `.btn-buy` |
| hover | applicable | Pointer-web purchase link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A purchase link can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted at this boundary rather than closed from the “link” noun is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a direct product purchase link; exact request/outcome mapping on the control itself is unresolved, and source §14 does not specify loading, error, or success treatments, so those three fields stay omitted at this boundary rather than closed from the “link” noun.

### Partner purchase

- Role: adjacent partner purchase link on both captured product pages
- Kind: interactive
- Anatomy: label
- Background: `#00c73c`
- Text: `#ffffff`
- Radius: `6px`
- Padding: `18px 4px`
- Font: `17px / 500 / Noto Sans KR`
- Use: `surface-2::[data-omd-capture="18"]` / `.btn-n-buy`, also observed on surface-3; adjacent green partner purchase link.
- Observed: default only
- Field note: YAML does not record a primitive `type` for this control; none is invented. The following unmerged-field reading, including yaml-primitive-type-not-invented and this-fill-as-route-local-partner-not-universal-and-not-direct-purchase, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. `#00c73c` is this control’s fill and YAML `action-partner`. It is not a universal Brandi color, not direct purchase `#1e1e1e`, and not promo `#ff365d`. `#ffffff` is this control’s on-action label, not a second canvas token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on both product pages as `.btn-n-buy` |
| hover | applicable | Pointer-web purchase link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A partner purchase link can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted at this boundary rather than closed from the “link” noun is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as an adjacent partner purchase link; exact request/outcome mapping on the control itself is unresolved, and source §14 does not specify loading, error, or success treatments, so those three fields stay omitted at this boundary rather than closed from the “link” noun.

### Product option select

- Role: product-option selector on both captured product pages
- Kind: interactive
- Anatomy: trigger
- Background: `#ffffff`
- Text: `#202429`
- Border: `1px solid #e6e6e6`
- Radius: `6px`
- Font: `13px / 400 / Spoqa Han Sans`
- Use: Product-option selector. Expanded trigger at `surface-2::[data-omd-capture="15"]` opened the recorded listbox on both product routes. Disabled trigger at `surface-2::[data-omd-capture="16"]`.
- Observed: default, expanded (listbox opened), and disabled
- Field note: YAML does not record a primitive `type` for this control; none is invented. The following unmerged-field reading, including yaml-primitive-type-not-invented, default-border-unmerged-from-disabled-border, option-13px-400-Spoqa-unmerged-from-purchase-17px-500-Noto, and canvas-fill-as-this-control-background, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. `#e6e6e6` is this trigger’s default border, not disabled `#e1e1e1`. Font 13px / 400 / Spoqa Han Sans is this control’s field, not purchase 17px / 500 / Noto Sans KR. `#ffffff` here is the trigger fill, not on-action text.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on both product pages |
| hover | applicable | Pointer-web option selector; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Captured: white background, `#202429` text, `1px solid #e1e1e1` border |

Treating loading, error, and success applicability as omitted at this boundary rather than closed from the “select” noun is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a product-option selector; source §14 does not specify loading, error, success, or validation treatments, so those three fields stay omitted at this boundary rather than closed from primitive kind.

Additional observed named state: expanded listbox after the option-trigger interaction. Treating that appearance as a named expanded variant, not an observed hover/focus/pressed paint and not this table’s error row, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification.

### Product option listbox

- Role: product-option listbox on product pages only
- Anatomy: expanded menu surface
- Background: `#ffffff`
- Text: `#202429`
- Border: `0px 1px 1px #e6e6e6`
- Radius: `0px 0px 6px 6px`
- Padding: `0px 0px 1px`
- Font: `13px / 400 / Spoqa Han Sans`
- Expanded: `surface-2::[data-omd-interaction-capture="menu-0-0"]` / `.ui-menu` appeared after the option-trigger interaction; a 16px-padded option wrapper was observed inside it.
- Use: Product-option listbox on product pages only.
- Field note: YAML has no primitive `type` for this surface. The source records an expanded interaction appearance (`.ui-menu`) and no interactive-kind confirmation, so no §4.4 map is declared. The following unmerged-field reading, including yaml-has-no-primitive-type, no-interactive-kind-confirmation, no-§4.4-map, listbox-radius-unmerged-from-6px-all-around, listbox-border-unmerged-from-1px-solid-trigger-border, and 16px-wrapper-padding-as-this-surface-field, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. Radius `0px 0px 6px 6px` is this listbox’s field, not 6px on every corner. Border `0px 1px 1px #e6e6e6` is this listbox’s field, not the trigger’s `1px solid #e6e6e6`. The 16px-padded option wrapper stays with this expanded surface.

### Product detail badge

- Role: observed product-detail metadata badge
- Type: badge
- Anatomy: label
- Background: `#ebeef2`
- Text: `#808893`
- Radius: `6px`
- Padding: `2px 8px 3px`
- Font: `13px / 700 / Noto Sans KR`
- Use: `surface-2::span` / `.badge`, also observed on surface-3. YAML use: Observed product-detail metadata badge.
- Observed: default only
- Field note: The source records default badge geometry and no interactive-kind confirmation, so no §4.4 map is declared. The following unmerged-field reading, including no-interactive-kind-confirmation, no-§4.4-map, badge-pair-unmerged-from-purchase-fills, badge-13px-700-Noto-unmerged-from-option-13px-400-Spoqa, and badge-padding-unmerged-from-purchase-18px-4px, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. `#ebeef2` / `#808893` are this badge’s pair, not purchase fills. Font 13px / 700 / Noto Sans KR is this badge’s field, not option 13px / 400 / Spoqa Han Sans and not purchase 17px / 500. Padding `2px 8px 3px` is this badge’s field, not purchase `18px 4px`.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is unitless action-x 4, action-y 18, option-item 16, badge-x 8, badge-y-start 2, badge-y-end 3. The supplied evidence is a 1440×900 desktop capture.

Treating the “supports only” bound as a reconstruction limit on these harvested paddings, not as a published layout system, and treating product-grid, page-container, sticky-bar, mobile-layout, and checkout-funnel rules as not retained without a representative captured selector, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. It supports only the recorded product-action spacing (`18px 4px`), option-listbox item padding (16px on the representative option wrapper), and compact badge padding (`2px 8px 3px`). No product grid, page container, sticky bar, mobile layout, or checkout funnel rule is retained without a representative captured selector.

Treating the 1440×900 figure and the recorded control paddings as desktop-capture measurements, not a cross-viewport specification, treating no mobile viewport as supplied, and treating breakpoints, touch-target requirements, sticky behavior, product-grid columns, and mobile navigation rules as not established by the public evidence, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. No mobile viewport was supplied. The public evidence does not establish breakpoints, touch-target requirements, sticky behavior, product-grid columns, or mobile navigation rules.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating the capture’s service name and commerce labels as insufficient for a voice guide, treating that insufficiency as a reason not to promote synthetic voice samples, and treating “여성 패션 쇼핑앱 브랜디” as a public site-title string rather than a voice doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. The supplied capture establishes the service name and commerce labels only; it does not provide a first-party editorial voice guide or enough verified copy to derive one.

Treating the public site title and the Newnex footer strings as product and legal-context strings rather than a microcopy system is a derived editorial implementation inference from the verified surfaces; it is not Brandi-authored or a separately published UI specification. The public site title is “여성 패션 쇼핑앱 브랜디.” The site footer identifies Newnex as the hosting operator and describes its payment/intermediation boundary. Those are product and legal-context strings, not a microcopy system.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- official Brandi product or design principles
- first-party history, mission, or current-evolution source
- first-party editorial voice principles or source-backed microcopy
- first-party, source-backed stakeholder groups or research
- `#ff204b` as a current token (no live use in the 2026-07-13 capture)
- Pretendard as a Brandi UI-family token; Arial as a Brandi font claim
- hover, focus, pressed, validation, and selected-option visual treatments on the product-option select
- empty, loading, error, success, cart, wish, sold-out, and validation visual treatments
- Direct purchase, Partner purchase, and Product option select loading·error·success applicability (exact request/outcome unresolved; source §14 does not specify those treatments)
- interactive kind and state-applicability map for Product option listbox and Product detail badge
- `focus-visible` visual treatments; a generic Focus observation is a different evidence type
- YAML primitive `type` for Direct purchase, Partner purchase, and Product option select
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
- breakpoints, touch-target requirements, sticky behavior, product-grid columns, mobile navigation, page container, and checkout funnel
- app, marketing, documentation, and account-area system beyond the three public routes
