# grip migration log

Source: `web/references/grip/DESIGN.md`
Sibling read (not the migration input): `web/references/grip/.verification.md`
Destination: `docs/design-md-weight/migrated/grip/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/grip/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `b6f8dfb5d1618da4c2a8b8092b324bfb741a071938b0fae4d1c626720ae8521f` (`web/references/grip/DESIGN.md`). Sibling SHA-256 `c451c3749ac90c0daa890f919c1a1b1790c49635a0df184b608ffe34aac13c3f` (`web/references/grip/.verification.md`). Worker-close portable DESIGN SHA-256 `f93494be2f9d08e45dc3b545aa7107712abe7682cb447301b45fae3ab1286830` (superseded). Post-F3 portable DESIGN SHA-256 `05ee758bbeb26790574a7aa44f7a0bb35c956e9458bd84473a1da3eb6ca27f2d`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Grip Design System` (`DESIGN.md` 1). Identity table `provenance.md` 7–20. `https://www.grip.show` is dual: `DESIGN.md` 9/21 + `provenance.md` 13/42/47 (E2a). `#eb2b51` is dual: `DESIGN.md` 11/22/34/58/86/243/263/286/382 + `provenance.md` 14 (E2a). The favicon slug is dual: `DESIGN.md` 227 + `provenance.md` 15 (E2a). |
| YAML `omd: "0.1"`, `verified`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–20, 30–32, 139. The source footer's **Verified:** 2026-06-03 is at `provenance.md` 34. These are ledger keys with no portable slot. |
| YAML `tokens.colors` (16 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 86–113. All sixteen roles kept with names, values, and token-set keys: primary, hot-pink, purple, canvas, surface-1, surface-2, surface-3, surface-light, surface-subtle, text-primary, text-body, text-muted, text-subdued, border, border-subtle, error. `#222327` (Surface Dark-2) and `#404149` (Surface Dark-3) have no other component home and stay palette roles at 94 and 95. |
| YAML `tokens.typography.family` (`sans: "Pretendard"`, `mono: "Pretendard"`) | 옮김 → Typography & Assets Family | `DESIGN.md` 197. Fallback Pretendard Fallback (Arial-based) at 187/196. |
| YAML `tokens.typography.discount / confirm / button / body / caption / badge` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 204–209. Unitless line heights stay ratios and are never converted to px (A1a): `1.35` on Discount only at 204/211/221; `1.4` on confirm/button/body/caption/badge at 205–209/211/221. All six YAML `use` strings restored verbatim at `DESIGN.md` 217 and in the table Use column (A1, A3). Visible-section ranges (400–500, 11–12px) stay ranges at 213, not extra token keys. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Unitless steps kept: `DESIGN.md` 125 (`xs 4 · sm 8 · md 12 · base 16 · lg 24 · xl 32 · section 56`). Visible px strings that already exist in the source (4px, 8px, 12px, 16px, 56px) stay; `lg: 24` and `xl: 32` stay unitless because the source never suffixes those two steps. Shape at 129–136 (`sm 4 · md 6 · lg 8 · full 9999` plus the four named uses). `full: 9999` stays a step (`DESIGN.md` 40/129/136). `tokens.spacing.section: 56` is disambiguated from the 56px page header at 125/215/422. `tokens.rounded.lg: 8` is disambiguated from `tokens.spacing.sm: 8` at 136. |
| YAML `tokens.components` (7 records) | 옮김 → Components & States | `DESIGN.md` 258–413. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×5 (261/284/307/330/354), `card` ×2 (378/391), `toast` ×1 (403). All seven YAML `use` strings restored verbatim as `Token-set use:` lines (269/292/315/339/362/385/412). YAML `font` shorthands kept as `token-set font record` lines (268/291/314/338/361/384/411). YAML nudge bg byte form `rgba(47,23,253,0.05)` at 406 beside the §4 `rgba(47, 23, 253, 0.05)` at 405. |
| §1 Visual Theme & Atmosphere (legacy 58–60) | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–42. The atmosphere readings (cinema-mode, buy-now urgency, kinetically distinctive, premium-yet-playful, structured-not-clinical) carry an adjacent complete qualifier at 11 (B2/B2a). The two live-ring records stay unmerged at 11/37/117–119. |
| §2 Color Palette & Roles (legacy 64–81) | 옮김 → Foundations semantic color | `DESIGN.md` 86–121. Characterizing phrases covered by the adjacent qualifier at 82 (B2/B2a). Product `conic-gradient(from -68deg, #ff2b51, #ffae8e)`, theme/corp `#fe0189→#ff583c`, and creator `conic-gradient(from -68deg, #1ec7be, #1dc3ff)` stay three records. |
| §3 Typography Rules — family, hierarchy (legacy 85–94) | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 183–191; family 195–198; hierarchy table 204–209. The License-row reading that treats Pretendard as an upstream face carries an adjacent complete qualifier at 190 (B2/B2a). |
| §4 Component Stylings (legacy 98–160) | 옮김 → Components & States | `DESIGN.md` 258–413. §4 body values and YAML `use` / font / radius / height forms are both kept where they differ (Active Coupon `#ffffff` + `#fff5f8`; nudge 500–600 vs YAML `14px / 500`; Confirm YAML radius 8px). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 163–166) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 28–34; Tier 1 list 47–52; Tier 2 56–57. `https://gripcorp.co` is dual: `DESIGN.md` 9 + `provenance.md` 43/51 (E2a). Conflicts unresolved: none — `provenance.md` 36. |
| §5 Layout Principles (legacy 170) | 옮김 → Layout & Platforms | `DESIGN.md` 418: 420px column, 56px header, 52px + safe-area nav, 100dvh live video, 109px thumbnail grids, Indiana Scroll. |
| §6 Depth & Elevation (legacy 174) | 옮김 → Foundations elevation | `DESIGN.md` 142: contrast layering, `rgba(0,0,0,0.1)` scrim, `z-index:-1` live ring, `outline: 1px solid rgba(0,0,0,0.05)`. The flat-system reading carries an adjacent qualifier at 144 (B2/B2a). |
| §7 Do's (legacy 178–184) | 옮김 → Experience application rules | `DESIGN.md` 58–63, under the grouping qualifier at 56. Not placed in Governance controlled copy. |
| §7 Don'ts (legacy 186–191) | 옮김 → Experience avoid | `DESIGN.md` 69–73, under the qualifier at 67. One extra line at 74 restates this contract's scope boundary as a prohibition; 67 says so. |
| §8 Responsive Behavior (legacy 195) | 옮김 → Layout & Platforms | `DESIGN.md` 420: 420px centered, fluid ≤460px, `env(safe-area-inset-bottom)`, video `min-width:360px` / `min-height:568px`, no tablet breakpoint. The fidelity-constraint reading carries an adjacent qualifier at 424 (B2/B2a). |
| §9 Agent Prompt Guide (legacy 199–208) | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 118. `0.81s` from §9 is kept next to 810ms at `DESIGN.md` 155. |
| §10 Voice & Tone (legacy 212–224) | 옮김 → Content & Locales | Published strings `DESIGN.md` 431–438; tone adjectives 440; register table 442–447. The qualifier at 449 covers the voice reading and the register-table contract (B2/B2a, A5). |
| §11 Brand Narrative (legacy 228–232) | 옮김 → Experience Scope | `DESIGN.md` 13: July 2018, Kim Han-na, Jam Live, Snow, six co-founders from Naver and Kakao, anyone-with-a-smartphone belief, GripCompany / 주식회사 그립컴퍼니, Pangyo / Seongnam, December 2021 Kakao ~50% ₩180B, personal media commerce, Grippers, rolling dice / quiz games / flash-sale countdowns, 2024 10 million downloads, 1,300 live shows daily, US via gripcorp.co, Japan in planning, Grip Cloud. Marked there as narrative context that supplies no interface tokens; the refusal that the two captured surfaces are not a proxy for Grip Cloud or Japan carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 142. |
| §12 Principles — 5 numbered (legacy 236–244) | 옮김 → Experience principles | `DESIGN.md` 48–52 under the B2a form at 46: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Grip-authored or a separately published UI specification." Grip has no published first-party design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 150–171 (22 rows). |
| §13 Personas — 4 entries (legacy 248–258) | 삭제 | The source's own headers label them illustrative. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or segment appears in either output (D2, D2a). Disposition at `provenance.md` 115. Audience at `DESIGN.md` 28 carries only the group-level description the captured surfaces and the published term Gripper establish, under an adjacent qualifier. The three `primary-tasks` at 21–23 come from modules and labels the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 8 rows (legacy 262–269) | 옮김 → Components & States surface state contract + per-component applicability | Full eight-row body preserved at `DESIGN.md` 239–246 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "담긴 상품이 없어요", "품절", and "사용 완료". The applicability rule is at 252–256, whose qualifier at 256 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 5 interactive buttons × 7 states = 35 rows: 31 `applicable` and 4 `not-applicable`, the latter only with a role reason (C2 v10) — Shorts / Discovery CTA 370–373 (destination to shorts; disabled + L/E/S closed together). Primary / Confirm / Dark Secondary / Outline keep loading/error/success `applicable` with treatments omitted, because those four commit in place. The two coupon cards get no `kind` and no map because the source supplies no interaction evidence for the containers — `DESIGN.md` 386/398 (C4). The Urgency Nudge declares `kind: non-interactive` with a reason instead of a map — 413 (Core §4.4). |
| §15 Motion & Easing — durations, brand curves, named ease-out, motion rules (legacy 273–289) | 옮김 → Foundations motion | `DESIGN.md` 152–155 (80ms / 150ms / 200–300ms / 810ms with uses), 159–162 (live-avatar `cubic-bezier(0.167, 0.166, 1, 1)` and `cubic-bezier(0.167, 0.167, 0.833, 0.833)`, `ease-out`, `linear`), 168–171 (ripple-sm, `translateY(100%)`, asymmetric drawer, no spring). |
| §15 Motion & Easing — Standard `cubic-bezier(0.4, 0, 0.2, 1)` (legacy 275, 280) | 삭제 → provenance omission ledger | The source labels the curve Material-derived / Tailwind default. Duration 150ms stays. Ledger at `provenance.md` 114. The omitted value is named as omitted at `DESIGN.md` 164 and 487, not promoted as a token. B3 is held: the promotion condition at `DESIGN.md` 175 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). |
| Opening one-line product sentence (legacy 56) | 옮김 → Experience Scope | `DESIGN.md` 9: "Korea's first live commerce platform — a mobile-first video shopping app where sellers and buyers connect in real time through livestreams, chat, and exclusive in-broadcast deals." |

## Sibling handling (`web/references/grip/.verification.md`)

The sibling exists — confirmed with `find web/references/grip -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 65–79 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: token names `primary-100` / `primary-default` / `base-100` / `secondary-default`; cart-button width `210px`; live-ring extra coordinates; chat-booster accents `#df005d` / `#8314c8` / `#1254b1`; corp propose-button `28px` / `136px` × `44px`; slogan gradient angle `133.59deg`; cart-item CSS URL; 사업자번호 `239-87-01063`; HQ street address; meta description `영상으로 만나는 패션&라이프 쇼핑`; `https://business.grip.show`; corp footer `GripCompany Co., Ltd., Republic of Korea`. Recorded at `provenance.md` 83–94.
- Measured `DESIGN.md` 0 for those sibling-only strings: `#df005d` 0 · `#8314c8` 0 · `#1254b1` 0 · `239-87-01063` 0 · `영상으로 만나는` 0 · `business.grip.show` 0 · `136px` 0.
- og:title `1,000만이 선택한 영상 쇼핑, 그립` is already in the source §10 (`DESIGN.md` 431) and is corroboration, not a sibling-only promotion.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All Korean runs in the source, any length | 11 distinct | 0 | 0 | All eleven survive in `DESIGN.md` (several also in provenance). |
| All Korean runs in the sibling, any length | 6 distinct | 0 | 0 | Sibling-only `영상으로 만나는 패션&라이프 쇼핑`, HQ address, 사업자번호, 비즈니스센터 survive in provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand grip` | 32 candidates / 9 reported lost | 9 reported | 0 published-copy losses | 4 high-confidence hits are §13 fictional-persona identifiers (D2 / D2a: not restated). 1 high-confidence hit is sibling footer `GripCompany Co., Ltd., Republic of Korea`, transcribed at `provenance.md` 94. 3 medium hits are §9 prompt fragments (`background #0e1011` / `#323232` / `#eb2b51`) whose hex values are already Foundations / component roles. 1 medium hit is the Tier 2 page title `grip — 0 DESIGN.md files \| getdesign.md`, transcribed at `provenance.md` 98. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 지금 바로, 한정 특가, 지금 방송 중, 담긴 상품이 없어요, 품절, 사용 완료, 1,000만이 선택한 영상 쇼핑, 그립., 지금 라이브 중!, 그리퍼가 직접, 주식회사 그립컴퍼니, Gripper, Grippers, Grip Cloud.

A5 분모: latin-copy-audit 32 candidates / 0 published-copy missing after disposition; Korean hand sweep 11 extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand grip --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 8, candidates: 152 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 8 < `candidates` 152.

## Deviations recorded

- `DESIGN.md` is 5,518 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: seven declared component records with dual `use`/`font` forms, an eight-row surface state contract, a 35-row applicability matrix, two unmerged live-ring records, the full §11 founding narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Grip has no published first-party design system, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Measure `grep -o 'derived editorial implementation inference' DESIGN.md \| wc -l` = 22 = `not Grip-authored` 22 = `separately published UI specification` 22. Provenance derived ledger 22 rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

The finished `DESIGN.md` was re-read from line 1. Every causal, interpretive, or judgment sentence — including Scope so/therefore boundaries, Content voice readings, and Docs-citation characterizations — was asked whether it is a Grip-issued fact or an observation-derived reading. F3 added adjacent complete-form closes at Spacing (`DESIGN.md` 125) and Family (`DESIGN.md` 198), and folded narrative-not-token / rounded-vs-spacing / range-vs-key / header-vs-`section: 56` into the existing closes at 13 / 138 / 223 / 424. The twenty-two complete-form qualifications listed in `provenance.md` 150–171 cover those readings. Source-stated narrative (July 2018, Kim Han-na, Jam Live, Snow, Kakao stake, 10 million / 1,300, Grip Cloud) and published copy were left unqualified.

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep` located the value in the named file and section. Dual destinations (homepage, `#eb2b51`, favicon slug, `https://gripcorp.co`) name both files. F3 corrected provenance line pointers that had landed on blanks or on `webapp-resource.grip.show` substring hits. The B3 claim is backed by the full five-kind sentence at `DESIGN.md` 175, not by a weaker paraphrase. The B2a 22=22 claim is backed by `grep -o` on the finished body and by the 22-row provenance inventory. The §13 deletion row does not restate names, ages, or cities (D2a). The omission ledger does not assert absence of an item in the same sentence that lists it (E2d).
