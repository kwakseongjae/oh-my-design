# Asleep Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Asleep (에이슬립) is a KAIST-born sleep-tech AI company. Treating that product-origin / sleep-tech-definition sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Catalog homepage identity is `https://www.asleep.ai/`. Treating the following two URLs as the named live-inspect evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. This contract covers two current first-party surfaces from the 2026-07-02 live inspect: the marketing homepage `https://www.asleep.ai/` and the ReadMe-hosted brand-guideline docs at `https://docs.asleep.ai/docs/brand-guideline`.

Source token note: primary = live marketing CTA blue (`#2a75fc`) on a near-black `#121212` canvas; the docs surface (docs.asleep.ai, ReadMe-powered) flips to light mode with its own accent blue `#3a61f2`. Marketing chrome is sharp-cornered (`0px` radius) and shadowless. Treating that note as a register split — `#2a75fc` is catalog `primary_color` and the harvested marketing primary-CTA fill, `#3a61f2` is the docs-surface accent rather than a second marketing primary, and `#118cfd` is the docs info tint rather than either accent — is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

The following evidence-domain sentence is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. The source HTML comment attaches token-level claims in §1–9 to playwright `getComputedStyle` on those two URLs. `https://www.asleep.ai/company` is recorded as the quotation source for founding, mission, and executive quotes, not as a token sheet for marketing or docs chrome.

The following visual-character, laboratory-at-night, Korean-modern typographic (including de-facto-product-typeface, calm-declarative-authority, generous-line-height, and hierarchy-from-size-not-flourish), sharp-and-shadowless (including reserved-almost-entirely, nothing-is-a-pill, and deliberate-counterpoint), and two-surface / marketing-pitch versus docs-workbench readings are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. The source reads the marketing site as a dark, cinematic, research-grade surface where a single electric blue does all the persuading. The hero canvas is a near-black `#121212`, text is pure white (`#ffffff`), and the one saturated accent — an electric blue `#2a75fc` — is reserved almost entirely for the primary call-to-action. The effect is read as a page that feels like a laboratory at night: quiet, precise, technically confident, letting sleep-data visualizations glow against the dark rather than competing with decorative color.

The typographic voice is read as Korean-modern: everything runs in **Pretendard Variable**, described as the de-facto Korean product typeface, with headlines at weight 600 and large sizes (52px on the hero, 40px on section titles) projecting a calm, declarative authority ("수면으로의 영역 확장, 어렵지 않습니다" / "Expanding into sleep is not difficult"). Body and UI text drop to a quiet 14px / weight 400 with a generous 20px line-height. There is no second display font and no weight above 600 — the hierarchy is read as coming from size and the dark/light contrast, not from typographic flourish.

The most distinctive geometric choice is read as what is *absent*: the marketing chrome is **sharp-cornered and shadowless**. Live inspection found `border-radius: 0px` on the primary CTA, the outlined button, and the feature cards, and `box-shadow: none` across the hero, nav, and cards. Nothing is a pill, nothing floats on elevation — separation is read as coming from the dark canvas, thin outlines, and the blue accent alone. The developer documentation (docs.asleep.ai, a ReadMe-hosted surface) is the deliberate counterpoint: it flips to light mode with a slightly different accent blue `#3a61f2`, softer 6–8px rounded controls, muted slate navigation (`#444e57`), and an informational tint blue `#118cfd`. Two surfaces, one company — the marketing site is read as the confident dark pitch; the docs are read as the calm, legible workbench.

Treating the following public-history and product-line facts as narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Asleep was founded in **2020** as a spin-out of a **KAIST** research lab, led by CEO **이동헌 (Lee Dong-heon)** — a KAIST AI graduate — with CTO **홍준기 (Hong Jun-ki)** and a founding group of KAIST electrical-engineering researchers. The company describes itself as "잠이 부족한 세상을 기술로 혁신하고 있는 슬립테크 스타트업" (a sleep-tech startup innovating a sleep-deprived world through technology). As the CEO put it: "호흡음이 실제로 가장 정확할 거라는 근거는 없었지만... 확신을 가지고 진행시켰습니다" ("there was no initial evidence that breathing sounds would be the most accurate signal — but we proceeded with conviction"). That bet — measuring sleep stages from breathing sound alone, contactless, with no wearable — became **AsleepTrack**, the API and SDK the company now licenses to partners.

Treating the founding-pain, clinic-barrier, belief-statement, and measurement-layer-not-consumer-device facts as narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. The founding pain point was access: a proper polysomnography sleep study historically cost around a million won and required a clinic. The CEO frames the mission around removing that barrier — "누구나 필요하다면 비용걱정 없이 검사를 받았으면 합니다" ("anyone who needs it should be able to get tested without worrying about cost"). Asleep's belief statement: "잘 자는 세상은 잠 자체를 잘 아는 것에서부터 시작한다" ("a world that sleeps well begins with understanding sleep itself"). Rather than sell a consumer device, Asleep chose to be the measurement layer other products build on — smart home, health/fitness, wellness, and finance partners integrate the sleep model instead of building their own.

The following refusal, embrace, and measurement-layer readings are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. What the source treats Asleep as refusing, visible in its design: the fear-driven, over-decorated aesthetic of much health marketing, and the intimidating chrome of clinical software. What it treats the brand as embracing: a dark, quiet, evidence-first surface where the data visualization is the hero and a single blue CTA points to the one next step; numbers instead of adjectives; and a documentation experience precise enough for a developer to integrate a medically-certified model in an afternoon. As the CTO frames the posture: "혁신을 주도하는 스타트업은 바람을 직접 일으킬 줄도 알아야 합니다" ("a startup that leads innovation has to know how to create the wind itself").
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three live CTA / feature-link strings below as the independently verified user outcomes, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

- Inquire about API adoption (`API 도입 문의하기` on the captured marketing surface).
- Open API Docs (`API Docs ↗`).
- Open Dashboard from a feature-card link (`Dashboard ↗`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable Asleep customer segments (developers and product teams integrating sleep measurement, smart-home and wellness partners), not individual people. Treating that no-individual-personas-promoted restriction, restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

### Distinctive traits

- Near-black marketing canvas `#121212` with pure-white (`#ffffff`) text
- Catalog `primary_color` / electric CTA blue `#2a75fc` reserved for the marketing primary CTA
- Docs accent blue `#3a61f2`; docs info tint `#118cfd`
- Pretendard Variable everywhere — hierarchy by size/weight, no second display font, no weight above 600
- Hero at 52px / weight 600 / YAML line-height `1.31` (68px at this size); sections at 40px / 600 / `1.30`
- Body at 14px / 400 / YAML line-height `1.43` (20px at this size)
- Sharp `0px` corners on marketing buttons and feature cards; docs controls at 6px / 8px
- Shadowless marketing chrome — live inspection `box-shadow: none` across hero, nav, and cards; docs subtle `rgba(0, 0, 0, 0.1) 0px 1px 2px`
- Light-mode ReadMe docs versus dark marketing canvas
- Heading grey `#222222` and body grey `#333333` on light sections; pure `#000000` for docs-control text
- Muted slate `#444e57` for docs sidebar rows
- YAML `rounded.full` 9999 / body `9999px` / `100%` for circular icons and avatars
- Feature cards ~450px tall on the dark canvas

Treating `#2a75fc` as the single marketing action hue rather than a second identity color and as catalog `primary_color` plus primary-CTA fill, treating `#3a61f2` as the docs-surface accent rather than a second marketing primary, treating `#118cfd` as the docs info tint rather than either accent, treating `#ffffff` on-dark text as unmerged from light-docs canvas `#ffffff` as a shared hex with two jobs, treating heading `#222222` as unmerged from body `#333333` and from docs-control `#000000`, treating `0px` marketing corners as unmerged from docs 6px / 8px, treating YAML `full` 9999 / `9999px` / `100%` as reserved for circular icons and avatars rather than a button or card radius, and treating laboratory-at-night / two-surface / sharp-and-shadowless / hierarchy-by-size as distinctive character rather than published UI doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

### Principles

These six items, including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification.

1. **Evidence over adjectives.** Asleep sells accuracy, so it shows accuracy. *UI implication:* pair every capability claim with the number behind it (accuracy, training instances, patents, certification) rather than superlatives.
2. **The data is the hero; the chrome recedes.** *UI implication:* keep the canvas dark and quiet so sleep visualizations glow; use one blue accent and no decorative color.
3. **One action, one color.** Electric blue (`#2a75fc`) means "do this next." *UI implication:* reserve the saturated blue for the single primary CTA so the next step is never ambiguous.
4. **Engineered, not soft.** A precision measurement API should look precise. *UI implication:* sharp `0px` corners and a shadowless flat surface on the marketing site; save the softer rounding for the docs.
5. **Be the layer, not the app.** Asleep succeeds when partners integrate easily. *UI implication:* the developer documentation is a first-class design surface — legible, exact, and fast to navigate.
6. **Access is the mission.** Sleep measurement should not require a clinic. *UI implication:* copy and flows emphasize ease of adoption ("어렵지 않습니다") over technical gatekeeping.

Treating the following as a capture-bound application of source §7 Do’s and live inspect is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

- Use the near-black `#121212` canvas with white (`#ffffff`) text for hero and dark sections
- Reserve electric blue (`#2a75fc`) for the primary CTA — keep it the single "action" color on the marketing surface
- Use sharp `0px` corners on marketing buttons and cards — the square edge is the signature
- Keep the marketing surface shadowless — separate with the dark canvas, outlines, and color
- Set every headline in Pretendard Variable at weight 600, sized 40–52px
- Use body text at 14px / weight 400 with a 20px line-height for dense hangul legibility
- On the docs surface, switch to the docs accent blue (`#3a61f2`), light mode, and 6–8px rounding
- Use muted slate (`#444e57`) for documentation navigation rows

### Avoid

The following items copy source §7 Don’ts. They are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification.

- Use drop shadows for elevation on the marketing surface — it is a flat, shadowless system
- Round the marketing CTA or cards — corners stay at `0px`, never pills
- Spread the electric blue (`#2a75fc`) across many elements — it dilutes the single-action signal
- Mix in a second saturated accent hue on the marketing site — blue is the only one
- Use a heavy display weight (700/800) — headlines top out at 600
- Use pure black (`#000000`) for marketing headings — reserve near-black `#222222` / dark-grey `#333333`
- Carry the marketing dark canvas into the docs — documentation is deliberately light-mode

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including the extra list characterizations (single action color on the marketing surface; docs accent as a close but distinct sibling; info tint used on callout edges; heading grey as warm-not-pure-black; muted slate as the quiet neutral of the documentation surface), are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. Catalog `primary_color` `#2a75fc` is the marketing CTA fill and YAML `primary`; it is not Docs Accent `#3a61f2` and not Info Blue `#118cfd`. Docs Accent is the ReadMe-hosted docs links/active/highlight field (YAML `accent-docs`), not a second marketing primary and not the harvested marketing CTA fill. Info Blue is the docs callout-border / tint field (YAML `info`), not an action fill. Ink Black Canvas `#121212` is the marketing hero/dark-section background and the Feature Card fill — those jobs share a hex and are not a second canvas token. Pure White `#ffffff` is text and outlines on the dark canvas (YAML `on-dark`) and the background of the light docs surface (YAML `canvas`) — those jobs share a hex and are not a second white token. Heading Grey `#222222` is near-black heading color on light sections (YAML `ink`); Body Grey `#333333` is default body/paragraph text (YAML `body`); Pure Black `#000000` is maximum-contrast text for docs controls (YAML `black`) — they stay unmerged. Muted Slate `#444e57` is docs sidebar navigation text (YAML `muted`), not marketing body ink. Primary CTA `fg` `#ffffff` and Outlined `fg` `#ffffff` stay on those controls. They are not extra general inks.

- **Asleep Blue / Primary** (`#2a75fc`): catalog `primary_color`. YAML `primary`. Primary brand color and CTA background on "API 도입 문의하기" — the marketing system's single "action" color.
- **Docs Accent Blue** (`#3a61f2`): YAML `accent-docs`. The accent blue of the ReadMe-hosted docs surface — links, active states, and interactive highlights inside `docs.asleep.ai`. A close but distinct sibling of the marketing blue.
- **Info Blue** (`#118cfd`): YAML `info`. Informational highlight/tint blue used in the docs (callout borders, tinted note backgrounds at low alpha).
- **Ink Black Canvas** (`#121212`): YAML `canvas-dark`. The near-black background of the marketing hero and dark sections.
- **Pure White** (`#ffffff`): YAML `on-dark` and `canvas`. Text and outlines on the dark canvas, and the background of the light docs surface.
- **Heading Grey** (`#222222`): YAML `ink`. Near-black heading color on light sections — warm, not pure black.
- **Body Grey** (`#333333`): YAML `body`. Default body/paragraph text color (document default, 14px).
- **Pure Black** (`#000000`): YAML `black`. Maximum-contrast text for docs controls (search field, high-emphasis labels).
- **Muted Slate** (`#444e57`): YAML `muted`. Docs sidebar navigation text and secondary/muted labels — the quiet neutral of the documentation surface.

### Spacing

YAML `spacing` is unitless: xs 4, sm 8, base 10, md 12, lg 20, xl 32, xxl 48, section 64. Treating those YAML numbers as unitless token numbers rather than a claimed px scale, while keeping the §5 observed px scale as a separate observation, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

Observed scale in the body: 4px, 8px, 10px, 12px, 20px, 32px, 48px, 64px. Base grid stated in the source: ~4px, with control padding landing at 8px vertical / 10–12px horizontal. Notable: buttons use a tight 8px 12px padding; nav items use 0 10px; feature-card headings use 0 20px. Treating that ~4px base / tight 8px 12px button padding / 0 10px nav / 0 20px feature-card heading split as recorded application rather than a universal spacing token for every unlisted control is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

### Shape

YAML `rounded`: none 0, sm 6, md 8, lg 20, full 9999 (`9999px` / `100%` in the body).

The following local-geometry reading, including the None / Small / Medium / Large / Full role labels, calling `0px` the marketing signature rather than a universal radius, calling 6px / 8px docs-only rather than marketing-button geometry, calling 20px occasional rounded media/containers rather than a card radius, and calling `9999px` / `100%` reserved for circular icons and avatars rather than a button or card radius, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

Observed radii in the body:

- None (`0px`): marketing buttons, feature cards — the sharp-cornered signature
- Small (6px): docs search field, docs callouts
- Medium (8px): docs sidebar navigation rows
- Large (20px): occasional rounded media/containers
- Full (`9999px` / `100%`): circular icons and avatars

`0px` / 6px / 8px / 20px / `9999px` remain local harvested geometry, not a universal radius for every unlisted control.

### Elevation

YAML `tokens.shadow.none` is `none`. YAML `tokens.shadow.subtle` is `rgba(0, 0, 0, 0.1) 0px 1px 2px`. Live inspection found `box-shadow: none` across the hero, nav, headings, and feature cards.

The following tone-not-elevation and shadow-philosophy readings, including the table Use assignments (marketing default flat; section separation without elevation; docs-only faint lift), are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Marketing hero, nav, feature cards — the default |
| Tone (Level 1) | Dark `#121212` vs lighter band | Section separation without elevation |
| Subtle (Level 2) | `rgba(0, 0, 0, 0.1) 0px 1px 2px` | Docs controls (search, dropdowns) — a faint lift only |

Asleep's marketing surface is a near-shadowless system. Depth is communicated by the dark canvas, thin white outlines, and the single blue accent — never by drop shadows or floating card stacks. The only place elevation appears is the docs surface, where ReadMe's chrome adds a barely-there `rgba(0,0,0,0.1)` lift on interactive controls. The source reads this flat, sharp-cornered treatment as engineered and technical — appropriate for a company selling a precision measurement API. That mirroring is this derived reading, not an Asleep-authored elevation token.

### Motion

Source-stated duration roles. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list. Treating the duration table and easing names as source-stated rather than computed CSS, and treating the ease-exit omitted-curve note as a spec-template match rather than a computed curve, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, focus, button press |
| `motion-standard` | 200ms | Card/section reveal, dropdown, docs sidebar expand |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — sections, cards, panels |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including functional-and-restrained, consistent-with-the-flat-evidence-first-aesthetic, signature fade-in / CTA opacity-scale, no-bounce, signals-steadiness-not-playfulness, and the-product-remains-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. Motion is described as functional and restrained — consistent with the flat, evidence-first aesthetic. Sections and data cards fade in from slightly below at `motion-standard / ease-enter`; the primary blue CTA responds to press with a subtle opacity/scale shift. No bounce or spring — a precision measurement product is read as signalling steadiness, not playfulness. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant and the product remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including the fallback-not-product-face assignment, are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Captured marketing homepage and docs brand-guideline: Pretendard Variable. Hero H1 "수면으로의 영역 확장, 어렵지 않습니다" 52px / 600 / white. Section titles 40px / 600. Nav 18px / 400 white on a 65px header. Body 14px / 400 / `#333333`. Docs sidebar 14px / 450 / `#444e57`. |
| Declared fallback | Body records `sans-serif` beside `Pretendard Variable`. YAML `family.sans` is `Pretendard Variable`. `sans-serif` is the recorded fallback, not a second product face. |

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Do not replace Pretendard Variable with a different claimed family. Do not present the `sans-serif` fallback as a second brand face. Do not present harvested-control `Pretendard` as a second family from YAML `Pretendard Variable`.

The following type-rule readings (one typeface, size-driven hierarchy; restrained weight topping at 600; hangul-first body at 14px / 20px including information-rich-API-content-compact; docs nudge to 450 including subtle-legibility-bump) are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. Pretendard Variable carries everything. Contrast comes from size (52 → 40 → 18 → 14) and from the dark/light surface split, not from mixing fonts. Headlines top out at weight 600 — confident but never heavy. There is no 700/800 display weight anywhere on the marketing surface. Body sits at 14px with a 20px line-height — generous for dense hangul legibility while keeping information-rich API content compact. The documentation sidebar uses an intermediate weight (450) — a subtle legibility bump for long navigation lists, distinct from the marketing 400.

### Family

- **Current visible UI family:** `Pretendard Variable` (with `sans-serif` fallback) — YAML `family.sans`

### Type roles

Verified YAML line-height values are the unitless ratios `1.31`, `1.30`, `1.20`, and `1.43`. The following ratio-versus-size-local reading, including treating YAML-omitted tracking as not invented, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. The body table’s `68px` beside Display Hero `1.31` is a size-local observation at 52px, not a replacement for the ratio. The body table’s `20px` beside Body `1.43` is a size-local observation at 14px, not a replacement for the ratio. YAML records no tracking field on these roles — none is invented.

YAML font names on harvested controls say `Pretendard`; the type-role table uses `Pretendard Variable`. Treating those strings as unmerged, and not converting one into the other, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. YAML `display-hero` use says "Pretendard SemiBold on dark"; SemiBold is that use string, not a second family.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Use (YAML) |
|---|---|---:|---:|---:|---|---|
| Display Hero | Pretendard Variable | 52px (3.25rem) | 600 | 1.31 | 68px at this size | Hero headline, Pretendard SemiBold on dark |
| Section Heading | Pretendard Variable | 40px (2.50rem) | 600 | 1.30 | (none extra) | Section titles |
| Nav Link | Pretendard Variable | 18px (1.13rem) | 400 | 1.30 | (none extra) | Top nav items on dark header |
| Button | Pretendard Variable | 16px (1.00rem) | 400 | 1.20 | (none extra) | CTA button label |
| Body | Pretendard Variable | 14px (0.88rem) | 400 | 1.43 | 20px at this size | Body text, 20px line-height |
| Docs Nav | Pretendard Variable | 14px (0.88rem) | 450 | 1.43 | (none extra) | Docs sidebar navigation row |

Docs Search harvested font is 13px / 400; Info Tint harvested font is 13px / 500. Treating those two 13px strings as component fields, not extra type-role rows, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

The following imagery-boundary reading, including no-shadow-at-any-size and consistent-with-the-flat-system, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Product/data visualizations sit on the dark canvas with no shadow at any size, consistent with the flat system. Do not replace verified product and data visualizations with invented brand-color decoration.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated.

The following classification of the source §14 table as philosophy-layer implementation guidance (sections 10–15), not live-inspect marketing-chrome paints, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. The source HTML comment attaches live inspect to token-level claims in §1–9. These rows describe empty/loading/error/success/skeleton/disabled treatments for a sleep-measurement product surface. They are not copied onto harvested marketing CTAs as computed paints.

| State | Treatment |
|---|---|
| **Empty (no sleep sessions yet)** | Dark `#121212` canvas. Single white (`#ffffff`) line explaining no measurements yet, with one blue `#2a75fc` CTA to start a first session. No decorative illustration. |
| **Empty (dashboard, no data)** | Muted line on the surface tone; a clear path to create an API key or run a sample session. Honest and calm. |
| **Loading (measurement / analysis)** | Flat progress indicator consistent with the shadowless system; previous values stay visible during in-place refresh. |
| **Loading (page / docs)** | Skeleton blocks at final dimensions, no shadow shimmer — a flat pulse matching the flat surface. |
| **Error (API call failed)** | Inline message stating the error type and a plain next step; on the docs surface, the error code is shown verbatim with a link to the Error Codes reference. No generic "문제가 발생했습니다" alone. |
| **Error (form validation)** | Field-level message below the input describing what is valid, not just "필수". |
| **Success (session complete / key created)** | Brief inline confirmation in a calm tone; the resulting data or key is surfaced immediately below. No celebratory emoji. |
| **Skeleton** | Flat blocks at final dimensions on the surface, sharp corners on the marketing side and 6px on the docs side. |
| **Disabled** | Reduced-opacity control; the blue action fades rather than turning grey, preserving the single-action read. |

Characterizations in that table such as “No decorative illustration”, “Honest and calm”, “consistent with the shadowless system”, “no shadow shimmer”, “No generic 문제가 발생했습니다”, “not just 필수”, “No celebratory emoji”, and “preserving the single-action read” are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records no generic Focus capture. The `focus-visible` row does not carry a colour. A later generic Focus observation would not be `focus-visible` evidence.

Feature Card (Marketing) is classified under source Cards & Containers (`Type: card`) and has no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Docs Callout has YAML `type: card` and no interactive-kind confirmation; kind and a map are omitted. Info Tint is classified under source Badges (`Type: badge`) with no YAML `tokens.components` type and no interactive-kind confirmation; kind and a map are omitted.

### Primary CTA

- Role: marketing primary call-to-action on the dark canvas
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#2a75fc`
- Text: `#ffffff`
- Radius: `0px`
- Padding: `8px 12px`
- Height: 38px
- Font: YAML `16px / 400 Pretendard`; body 16px / 400 / Pretendard Variable
- Use: YAML `Marketing primary CTA (API 도입 문의하기) — sharp-edged electric blue`
- Observed: default only
- Field note: The following unmerged-field reading, including treating `#2a75fc` as this control’s fill and catalog `primary_color` rather than Docs Accent `#3a61f2`, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Height 38px is this primary control, not Outlined 45px and not the 65px header.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the marketing primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says the blue action fades rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as a marketing API-inquiry CTA; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Outlined (Secondary)

- Role: marketing secondary CTA outlined on the dark hero
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `transparent`
- Text: `#ffffff`
- Border: 1px solid `#ffffff`
- Radius: `0px`
- Padding: `8px 12px`
- Height: 45px
- Font: YAML `16px / 400 Pretendard`; body 16px / 400 / Pretendard Variable
- Use: YAML `Marketing secondary CTA (API Docs) outlined on dark hero`; body Secondary action ("API Docs ↗")
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Height 45px is this outlined control, not Primary CTA 38px. `#ffffff` border/text is this control’s field, not the light-docs canvas. Source §9-only `Background: transparent` is this outlined control’s renderable field, not a general canvas or color token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the marketing outlined CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a marketing docs CTA; exact selector/destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Docs Search

- Role: documentation search field (⌘K) on the light docs surface
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#000000`
- Radius: 6px
- Font: YAML `13px / 400 Pretendard`; body 13px / 400 / Pretendard Variable
- Use: YAML `Docs search field (⌘K) on the light docs surface`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. `#000000` is this field’s text and Pure Black, not Heading Grey `#222222` and not Body Grey `#333333`. 13px / 400 is this search field, not Info Tint 13px / 500 and not YAML Button 16px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the docs search field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source records default geometry plus `#000000` text. Source §14 names generic form-validation error and does not attach that state to this documentation-search role. Exact validation/error/loading/success mapping for this field is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 generic form-validation row or from primitive `Form field` class. They are not closed as `not-applicable`.

### Feature Card (Marketing)

- Role: product/feature entry card on the dark canvas
- Type: card
- Anatomy: surface
- Background: `#121212`
- Text: `#ffffff`
- Radius: `0px`
- Height: ~450px (body §5 / §9)
- Use: Product/feature entry card on the dark canvas (Dashboard, Asleep Docs, Sleep Report Design Guide) — sharp corners, shadowless
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 / §5 / §9. Type `card` is the source Cards & Containers classification, not a YAML `tokens.components` type.
- Field note: The following unmerged-role and mixed-anatomy readings are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. `#121212` is this card’s fill and Ink Black Canvas. YAML-absent mixed anatomy from source §9 is a Pretendard 600 heading with a small `Dashboard ↗` link at the bottom; that mixed heading/link is not the same as painting the whole card copy as YAML Button 16px / 400. Height ~450px is this card, not a spacing token.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Docs Callout

- Role: documentation info callout / content card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Border: 1px solid `#118cfd`
- Radius: 6px
- Use: YAML `Docs info callout / content card with a blue accent edge`
- Observed: default only
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. `#118cfd` is this callout’s border and Info Blue, not Primary `#2a75fc` and not Docs Accent `#3a61f2`. `#ffffff` fill is this card and the light-docs canvas, not on-dark text.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Info Tint

- Role: docs informational tint pill / note highlight
- Type: badge
- Anatomy: label
- Background: `rgba(17,140,253,0.1)`
- Text: `#118cfd`
- Radius: 6px
- Font: 13px / 500 / Pretendard Variable
- Use: Docs informational tint pill / note highlight
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type `badge` is the source Badges classification, not a YAML `tokens.components` type.
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. `#118cfd` and `rgba(17,140,253,0.1)` are this pill’s fields, not a page-wide info wash and not Primary `#2a75fc`. 13px / 500 is this pill, not Docs Search 13px / 400.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Top Nav

- Role: top navigation item on the dark header
- Kind: interactive
- Type: tab
- Anatomy: label
- Text (inactive): `#ffffff`
- Text (active): `#2a75fc`
- Font: YAML `18px / 400 Pretendard`; body 18px / 400 / Pretendard Variable
- Height: 65px header
- Padding: 0 10px (body §5)
- Use: YAML `Top nav item on dark #121212 header`; body "Products", "Research", "Company", "FAQ"
- Observed: static inactive and static active appearances. Treating the active appearance as a captured variant, not an observed click transition, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. Active text `#2a75fc` is this nav item’s active field; it shares a hex with Primary CTA fill and is not that fill. `#ffffff` inactive label is this control’s field and on-dark text. Height 65px is the header that holds these items, not Primary CTA 38px. Padding 0 10px is this nav item (body §5), not button `8px 12px`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Static inactive appearance captured on the dark header |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination grouping; the item itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab meaning is navigation selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`#2a75fc` text). Treating that appearance as a captured variant, not an observed click transition, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

### Docs Nav Item

- Role: docs sidebar navigation row
- Kind: interactive
- Type: listItem
- Anatomy: label
- Text: `#444e57`
- Radius: 8px
- Font: YAML `14px / 450 Pretendard`; body 14px / 450 / Pretendard Variable
- Active: bg `rgba(255,255,255,0.1)`
- Use: YAML `Docs sidebar navigation row`
- Observed: default, plus named active wash. Treating the active appearance as a captured variant, not an observed click transition, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.
- Field note: The following unmerged-field reading is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification. `#444e57` is this row’s label and Muted Slate, not Body Grey `#333333`. Weight 450 is this docs-nav field, not marketing 400. Active `rgba(255,255,255,0.1)` is this row’s named wash, not a page-wide white overlay. Type `listItem` is the YAML primitive.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the docs sidebar navigation row |
| hover | applicable | Pointer-web nav row; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A sidebar row can be unavailable; visual treatment omitted |
| loading | not-applicable | A sidebar row selects a docs grouping; the row itself does not enter a loading state |
| error | not-applicable | Nav-row meaning is selected versus resting, not a request or validation failure on the row |
| success | not-applicable | Nav-row meaning is destination selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`rgba(255,255,255,0.1)` background). Treating that appearance as a captured variant, not an observed click transition, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, base 10, md 12, lg 20, xl 32, xxl 48, section 64. Body layout repeats 4px, 8px, 10px, 12px, 20px, 32px, 48px, 64px with a ~4px base unit. Buttons use 8px 12px padding. Nav items use 0 10px. Feature-card headings use 0 20px. Primary CTA is 38px high; outlined secondary is 45px high. Nav items sit within a 65px header. Feature cards are ~450px tall. Docs search uses 6px radius; docs sidebar rows use 8px radius. Treating that YAML-versus-body-px restatement and those padding / height / radius figures as recorded measurements in this packet rather than a universal layout scale is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

The “dark breathing room”, “flat segmentation”, “docs density”, centered-hero, equal-height feature-row, and ReadMe three-zone readings in this section are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification.

Recorded layout:

- Dark full-width hero with a centered 52px Pretendard headline as the anchor
- Product/feature entries arranged as a row of large equal-height cards (~450px tall) on the dark canvas
- Sections alternate between the dark `#121212` canvas and lighter content bands
- Docs surface uses a fixed left navigation sidebar + wide content column (classic ReadMe three-zone layout)
- Dark breathing room: the marketing surface leans on generous negative space filled by the dark canvas, so the single blue CTA and white headline carry the eye
- Flat segmentation: sections separate by surface tone and thin outlines, not by shadow or heavy borders
- Docs density: the documentation flips the priority — dense, scannable navigation and compact rows for information-rich API reference

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero headline compresses, nav collapses to a menu |
| Tablet | 640–1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024–1440px | Full layout, centered hero, multi-card feature row; docs sidebar visible |

Collapsing strategy recorded in the source: hero 52px Pretendard headline scales down on mobile, weight 600 maintained; feature card row multi-column → stacked single column; dark/light sections maintain full-width treatment; docs fixed sidebar collapses to a toggle menu on narrow viewports. Image behavior: product/data visualizations sit on the dark canvas with no shadow at any size, consistent with the flat system; marketing cards keep sharp `0px` corners across breakpoints.

Touch-target record: Primary CTA at 38px height; secondary outlined button at 45px; nav items sit within a tall 65px header for generous hit area; docs search and sidebar rows sized for pointer + touch on the documentation surface.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the collapsing strategy and image-behavior lines (visualizations with no shadow at any size, consistent with the flat system; `0px` corners across breakpoints) as recorded application rather than a complete layout specification, treating the 38px / 45px / 65px / 52px / ~450px figures as surface measurements in this packet rather than universal layout tokens, and treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-07-02)

The live strings below are source-stated. Treating the live-inspect attributions, harvested-control-string grouping, and company-page quotation restatement as citation-character of those live strings, not as derived voice, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

- "수면으로의 영역 확장, 어렵지 않습니다" — hero headline, homepage. *(verified live 2026-07-02)*
- "세계에서 가장 정확한 수면 AI 모델을 사용하여" — product section, homepage. *(verified live 2026-07-02)*
- "서비스에 Asleep 브랜드를 활용하는 방법" — brand guideline heading, docs.asleep.ai. *(verified live 2026-07-02)*
- Control strings from harvested components: "API 도입 문의하기"; "API Docs ↗"; "Dashboard ↗"; nav "Products", "Research", "Company", "FAQ"
- Company / mission strings restated from the company-page quotation source: "잠이 부족한 세상을 기술로 혁신하고 있는 슬립테크 스타트업"; "잘 자는 세상은 잠 자체를 잘 아는 것에서부터 시작한다."; CEO "호흡음이 실제로 가장 정확할 거라는 근거는 없었지만... 확신을 가지고 진행시켰습니다"; CEO "누구나 필요하다면 비용걱정 없이 검사를 받았으면 합니다"; CTO "혁신을 주도하는 스타트업은 바람을 직접 일으킬 줄도 알아야 합니다"

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Asleep-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list are a derived editorial implementation inference from the verified surfaces; they are not Asleep-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Asleep's voice as **calm, credible, and quietly ambitious** — a research team that turned a lab breakthrough into an API and talks about it with evidence, not hype. The hero line is read as setting the register: reassuring and enabling, framed around the partner's ease rather than Asleep's cleverness. Claims are backed with numbers ("세계에서 가장 정확한 수면 AI 모델", 2,201,145 training instances, 74 patent applications, 22 papers, KFDA Class-2 certification) rather than adjectives. The tone is read as treating the reader — usually a developer or product team integrating sleep measurement — as a capable peer.

| Context | Tone |
|---|---|
| Hero headline | Enabling, partner-focused. "수면으로의 영역 확장, 어렵지 않습니다." Calm, not hype. |
| Product / API descriptions | Precise and evidence-led. States the capability and the number behind it. |
| CTAs | Direct and low-pressure. "API 도입 문의하기", "API Docs ↗", "Dashboard ↗". |
| Docs / brand guideline | Instructional and exact. "서비스에 Asleep 브랜드를 활용하는 방법" — clear rules, no fluff. |
| Company / mission | Mission-framed and humble about the science. "잘 자는 세상은 잠 자체를 잘 아는 것에서부터 시작한다." |

**Forbidden register** (source): fear-based health scare marketing, undefined medical jargon left unexplained, exclamation-heavy hype, and unquantified superlatives ("revolutionary", "magical") — accuracy claims must carry a number.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- loading, error, and success visual treatments on the harvested marketing controls, and the omitted loading/error/success applicability fields on Primary CTA, Outlined (Secondary), and Docs Search
- YAML `tokens.components` type for Feature Card (Marketing) and Info Tint (body heading classification is preserved as Type `card` / Type `badge`)
- interactive kind and state-applicability map for Feature Card (Marketing), Docs Callout, and Info Tint
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
