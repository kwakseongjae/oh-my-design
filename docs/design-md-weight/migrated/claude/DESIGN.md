# Claude (Anthropic) Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating Claude as Anthropic’s public AI product and treating catalog homepage `https://claude.com` as identity rather than an extra captured host beyond the three named URLs below is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Claude is Anthropic’s public AI product. The official product overview describes it as a conversational tool for bringing together documents, tools, and web knowledge. Anthropic describes its broader work as building reliable, interpretable, and steerable AI systems. Catalog homepage identity is `https://claude.com`.

Treating the following three URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, Anthropic-marketing-home-not-a-stand-in-for-Claude-overview-tokens, Claude-overview-not-a-stand-in-for-public-pricing, public-pricing-not-a-stand-in-for-overview-or-marketing, and no-equivalence-with-authenticated-chat-Help-Center-documentation-chrome-or-internal-product-UI, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. This contract covers three current first-party public surfaces from the 2026-07-13 packet: Anthropic marketing home `https://www.anthropic.com/` (marketing), Claude public product overview `https://claude.com/product/overview` (product-marketing), and Claude public pricing `https://claude.com/pricing` (product-pricing). Color, type, and component values below stay attached to the surface that established them. Anthropic’s marketing home is not a stand-in for Claude overview tokens. The Claude overview is not a stand-in for public pricing. Public pricing is not a stand-in for overview or marketing. This reconstruction does not assert equivalence with authenticated chat, Help Center/documentation chrome, or internal product UI.

Source token note: Selector-backed observations from Anthropic marketing, Claude product overview, and public pricing are retained as separate surface domains. No authenticated chat UI, documentation chrome, or declared-only font asset is represented as a token. Treating that note as a register split — `#c96442` is catalog `primary_color` and a low-frequency overview accent rather than a universal action fill; `#f5f4ed` parchment canvas is unmerged from white surface `#ffffff`, hairline cream `#f0eee6`, and warm sand `#e8e6dc`; `#141413` near-black is unmerged from muted `#5e5d59`, pressed-tab `#5d5c58`, appearance-tile `#4d4c48`, and dark surface `#30302e`; Anthropic Sans is the public UI/body family rather than Anthropic Serif display; Anthropic Mono and JetBrains Mono stay declared-only; and authenticated-chat, documentation-chrome, and internal-product values stay unpromoted — is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

The following atmosphere readings — that-practical-role-is-expressed-through-a-restrained-warm-neutral-system-rather-than-a-generic-blue-software-palette, parchment-canvas-with-near-black-text-and-white-cards, serif-display-as-editorial-cadence, sans-as-public-UI-body-tab-and-control-rhythm, and terracotta-as-a-low-frequency-observed-accent-not-every-Claude-action — are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification. On the supplied public surfaces, that practical role is expressed through a restrained warm-neutral system rather than a generic blue software palette. The Claude overview and pricing pages repeatedly use `#f5f4ed` canvas areas, near-black `#141413` text, white cards, and a mix of loaded Anthropic Sans and Anthropic Serif. Serif display text gives the public product pages an editorial cadence; Sans carries repeated navigation, body, tab, and control text. The capture also records `#c96442` on the overview, but it is a low-frequency observed accent, not evidence that every Claude action is terracotta.

Treating the following public-history facts as narrative rather than interface tokens — Anthropic-as-an-AI-safety-and-research-company, company-page-translation-of-research-into-practical-tools-such-as-Claude-for-businesses-nonprofits-civil-society-groups-their-clients-and-people-around-the-world, public-overview-Claude-as-a-conversational-thinking-partner-that-can-connect-documents-tools-and-web-knowledge, January-2026-Constitution-as-a-current-public-articulation-of-intended-character-and-role, Constitution-as-part-of-the-training-process-naming-broad-safety-ethics-compliance-with-Anthropic-guidelines-and-genuine-helpfulness, Constitution-as-narrative-and-product-intent-not-evidence-for-color-typography-or-component-tokens, and official-company-writing-the-public-overview-and-the-Constitution-provide-narrative-context-not-interface-tokens — is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Anthropic describes itself as an AI safety and research company building reliable, interpretable, and steerable systems. Its company page says it translates that research into practical tools such as Claude for businesses, nonprofits, civil-society groups, their clients, and people around the world. The public Claude overview, separately, presents Claude as a conversational thinking partner that can connect documents, tools, and web knowledge. The January 2026 Constitution adds a current public articulation of Claude’s intended character and role. Anthropic says the document expresses its intentions for Claude’s values and behavior and is part of the training process; it identifies broad safety, ethics, compliance with Anthropic guidelines, and genuine helpfulness as key concerns. Official company writing, the public overview, and the Constitution provide that narrative context; they do not by themselves supply interface tokens. Treating those company and product statements as explaining a product/category relationship, and keeping marketing pages distinct from an authenticated-product specification, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Those statements explain the product/category relationship. Marketing pages stay distinct from an authenticated-product specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings below as Primary tasks, and not lifting source §13 stakeholder archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

- Switch the selected tab on the public Claude overview (YAML use: `Selected tab on the public Claude overview`).
- Scan public Claude overview model cards (YAML use: `Public Claude overview model card`).
- Compare public Claude pricing cards (YAML use: `Public Claude pricing card`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 entries as evidence-bounded stakeholder groups rather than verified biographical personas, treating those entries as not Audience biographies and not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No individual personas are promoted. Official material discusses users at a group level. Use stakeholder groups only:

- People using Claude as a conversational tool with documents, tools, and web knowledge, as the official overview presents that product role.
- Teams and organizations — businesses, nonprofits, civil-society groups, and their clients — that Anthropic’s company page names as recipients of practical tools such as Claude.
- People affected by AI systems. Anthropic’s company page explicitly broadens “users” beyond direct customers and names customers, policymakers, employees, and people affected by the technology.

### Distinctive traits

The following unmerged-role extras — treating `#c96442` as catalog `primary_color` and a low-frequency overview accent rather than a universal action fill, treating `#f5f4ed` canvas as unmerged from `#ffffff` / `#f0eee6` / `#e8e6dc`, treating `#141413` as unmerged from `#5e5d59` / `#5d5c58` / `#4d4c48` / `#30302e`, treating Anthropic Sans as unmerged from Anthropic Serif, treating 12px tabs as unmerged from 24px pricing cards and 32px model cards and 16px appearance tiles, and treating featured-pricing blue-tinted shadow as unmerged from flat white cards — are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification.

- Warm public canvas `#f5f4ed` with near-black `#141413` text
- White public cards bounded by quiet `#f0eee6` hairline
- Loaded Anthropic Serif for editorial display and loaded Anthropic Sans for public UI/body rhythm
- Rounded public tabs (12px), pricing cards (24px), and model cards (32px), each tied to a separate selector
- A limited observed `#c96442` accent, without a universal-action claim

### Principles

The four numbered items’ source-backed stems cite Anthropic’s company page (reliable, interpretable, and steerable systems; AI safety as a science; a broad definition of users) and the company values / Constitution (helpfulness beside honesty and safety). The *UI implication* notes — make-the-scope-and-consequence-of-a-public-action-understandable-rather-than-relying-on-visual-emphasis-alone, distinguish-an-observed-interface-state-from-an-inference-and-retain-its-evidence-boundary, pair-useful-next-steps-with-clear-limits-where-the-product-context-requires-them, and avoid-assuming-a-single-expert-audience-or-treating-one-public-marketing-surface-as-the-whole-product — are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification. The capture-bound application list is limited at the grouping below, not here.

1. **Build systems people can rely on.** Anthropic’s company page describes reliable, interpretable, and steerable systems. *UI implication:* make the scope and consequence of a public action understandable rather than relying on visual emphasis alone.
2. **Treat safety as systematic work.** Anthropic says it treats AI safety as a science and feeds product learning back into research. *UI implication:* distinguish an observed interface state from an inference, and retain its evidence boundary.
3. **Be helpful, honest, and harmless.** The company values and Constitution both place helpfulness beside honesty and safety. *UI implication:* pair useful next steps with clear limits where the product context requires them.
4. **Serve a broad set of affected people.** Anthropic’s company page names customers, policymakers, employees, and people affected by the technology in its broad definition of users. *UI implication:* avoid assuming a single expert audience or treating one public marketing surface as the whole product.

Treating the following as a capture-bound application of source §7 Do’s and the unique source §9 constraints, including selector-backed-public-values-only-for-these-particular-public-card-and-tab-patterns, Anthropic-Sans-and-Anthropic-Serif-kept-distinct-when-browser-loadable-sources-are-available, selected-tab-and-featured-pricing-as-route-local-variants, parchment-canvas-near-black-text-white-card-with-route-specific-24px-or-32px-radius-and-loaded-Anthropic-Sans-Serif, and public-web-observations-not-a-complete-implementation-kit-for-authenticated-chat, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

- Use only the selector-backed public values above when recreating these particular public card and tab patterns.
- Keep Anthropic Sans and Anthropic Serif distinct when their browser-loadable sources are available.
- Preserve the selected-tab and featured-pricing treatments as route-local variants.
- Use the public Claude overview pattern only where the evidence fits: a `#f5f4ed` canvas, near-black text, a white card with the route-specific 24px or 32px radius, and loaded Anthropic Sans/Serif.
- Treat the values as public-web observations, not as a complete implementation kit for the authenticated chat product.

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including Arial-Georgia-Inter-or-a-system-stack-not-labeled-as-Anthropic-Sans-Serif-or-Mono, featured-pricing-card-blue-shadow-not-generalized-to-ordinary-cards-or-product-surfaces, and no-invented-chat-composer-error-toast-menu-disabled-or-responsive-variants. They are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification.

- Do not use Arial, Georgia, Inter, or a system stack while labeling the result Anthropic Sans, Anthropic Serif, or Anthropic Mono.
- Do not generalize the featured pricing-card blue shadow to ordinary cards or product surfaces.
- Do not invent chat composer, error, toast, menu, disabled, or responsive variants that this packet did not observe.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#c96442`-as-low-frequency-overview-accent-not-a-universal-action-fill, parchment-`#f5f4ed`-not-white-`#ffffff`-not-hairline-`#f0eee6`-not-warm-sand-`#e8e6dc`, parchment-canvas-not-claimed-as-an-authenticated-chat-canvas, near-black-`#141413`-not-muted-`#5e5d59`-not-pressed-tab-`#5d5c58`-not-appearance-tile-`#4d4c48`-not-dark-surface-`#30302e`, featured-pricing-blue-tinted-border-and-shadow-not-a-palette-role, YAML-component-fields-staying-on-those-controls, and authenticated-chat-or-documentation-chrome-colors-unnamed-in-this-pass — are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification. Catalog `primary_color` `#c96442` is YAML `primary` and a low-frequency overview background observation. Treat it as an observed public accent, not a complete action scale. YAML `canvas` `#f5f4ed` is the observed overview and pricing background value; it is not claimed as an authenticated-chat canvas. YAML `surface` `#ffffff` is selected public tabs and recorded model/pricing cards. YAML `foreground` `#141413` is repeated text, border, and dark-surface value across the three supplied public routes. YAML `muted` `#5e5d59` is repeated public overview/pricing text and border value. YAML `surface-dark` `#30302e` is observed on the public product/pricing surfaces. YAML `hairline` `#f0eee6` is a recorded border and background value, including the public card borders below. YAML `warm-sand` `#e8e6dc` is a recorded public-pricing background value and the overview appearance-toggle ring treatment. YAML component fields such as overview-tab `fg` `#141413` stay on those controls. They are not extra general inks. The capture includes a blue-tinted featured pricing-card variant; that single route-local card is not promoted to the general palette. Authenticated-chat and documentation-chrome colors remain unnamed in this pass.

- **Terracotta accent** (`#c96442`): catalog `primary_color`. YAML `primary`. A low-frequency overview background observation. Observed public accent, not a complete action scale.
- **Parchment canvas** (`#f5f4ed`): YAML `canvas`. Observed overview and pricing background value; not an authenticated-chat canvas.
- **White surface** (`#ffffff`): YAML `surface`. Selected public tabs and recorded model/pricing cards.
- **Near black** (`#141413`): YAML `foreground`. Repeated text, border, and dark-surface value across all three supplied public routes.
- **Muted warm gray** (`#5e5d59`): YAML `muted`. Repeated public overview/pricing text and border value.
- **Dark surface** (`#30302e`): YAML `surface-dark`. Observed on the public product/pricing surfaces.
- **Hairline cream** (`#f0eee6`): YAML `hairline`. Recorded border and background value, including the public card borders below.
- **Warm sand** (`#e8e6dc`): YAML `warm-sand`. Recorded public-pricing background value and the overview appearance-toggle ring treatment.

### Spacing

YAML `spacing`: xs 4, sm 8, md 12, lg 16, xl 24, xxl 32, 3xl 48.

Treating those YAML numbers as recorded without a required px suffix, and treating the body 4px / 8px / 12px / 16px / 24px / 32px / 48px cluster as body-recorded observations rather than a converted YAML scale, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names the same cluster as an observed public-style inventory, not a declared global spacing scale.

### Shape

YAML `rounded`: sm 6, md 8, lg 12, card 24, media 32.

The following local-geometry reading, including Small-6, Medium-8, Tab-12, Pricing-card-24, Model-card-32, and Appearance-tile-16-as-body-local-not-YAML, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. The universal-radius refusal is limited after the list, not here.

- Small (YAML `sm` 6)
- Medium (YAML `md` 8)
- Tab (YAML `lg` 12): public overview tab at 12px
- Pricing card (YAML `card` 24): public pricing card at 24px
- Model card (YAML `media` 32): public overview model card at 32px
- Appearance tile (body 16px): one overview appearance control; YAML `rounded` has no 16 step, and none is invented there

Treating 6 / 8 / 12 / 16 / 24 / 32 as local harvested geometry rather than a universal radius for every unlisted control, and treating the 12px tab, 24px pricing card, and 32px model card as selector-backed component use rather than one interchangeable radius, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

### Elevation

The following elevation readings, including standard-public-model-and-pricing-cards-as-flat-white-surfaces-with-`#f0eee6`-borders-and-no-computed-shadow, one-featured-pricing-card-alone-using-a-cool-translucent-border-and-`rgba(98, 158, 218, 0.16) 0px 4px 20px 0px`, overview-appearance-control-using-a-one-pixel-warm-ring, and these-as-selector-specific-observations-not-a-general-elevation-scale, are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification. The standard public model and pricing card samples are flat white surfaces with `#f0eee6` borders and no computed shadow. One featured pricing card alone uses a cool translucent border and `rgba(98, 158, 218, 0.16) 0px 4px 20px 0px` shadow; the overview appearance control uses a one-pixel warm ring. These are selector-specific observations, not a general elevation scale.

### Motion

No duration, easing, reduced-motion, streaming, or page-transition value was captured. The only interaction evidence is tab selection/pressed state and tab-panel expansion; it establishes neither an animation curve nor a motion scale. Treating that source-stated absence, including tab-selection-pressed-and-tab-panel-expansion-prove-state-change-not-a-universal-animation-specification, as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No motion token is promoted.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including no-first-party-typography-guide-or-font-license-assigning-a-general-product-role, live-font-evidence-below-is-the-basis-for-UI-family-tokens, live-computed-Anthropic-Sans-1493-and-Anthropic-Serif-65-with-FontFaceSet-source-URL-corroboration, browser-served-WOFF2-as-delivery-on-captured-public-routes-not-a-license-grant-for-reuse, declared-only-anthropicMono-and-JetBrains-Mono-zero-visible-computed-usage, Arial-and-Georgia-as-CSS-fallbacks-not-substitutes, and authenticated-chat-or-Help-Center-typography-unnamed, are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | No first-party typography guide or font license assigning a general product role was found in the reviewed official context sources. The live-font evidence below is the basis for UI-family tokens. |
| Live computed surface-use | **Anthropic Sans** is loaded/high with 1,493 computed uses across the supplied routes; **Anthropic Serif** is loaded/high with 65. Both have computed-family usage plus FontFaceSet/source-URL corroboration in the supplied artifact. YAML `family.ui` `Anthropic Sans` and YAML `family.display` `Anthropic Serif` name those live families. |
| Official distributed brand asset | No separate official font-distribution or license page was located in this pass. The browser-served WOFF2 URLs establish delivery on the captured public routes, not a license grant for reuse. |
| Declared-only | `anthropicMono` and JetBrains Mono have captured `@font-face` source URLs but zero visible computed usage. They remain declared-only and are excluded from `tokens.typography.family`. |
| System / unresolved | Arial and Georgia appear only as CSS fallbacks in observed stacks. They are not substitutes for, or tokens representing, Anthropic Sans or Anthropic Serif. |
| Captured-scope unnamed | Authenticated-chat and Help Center/documentation-chrome typography remain unnamed in this pass. |

Treating Anthropic Mono as useful as a declared asset record but not as a claimed mono scale, because no visible use was captured, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Anthropic Mono is useful as a declared asset record, but no visible use was captured, so this reference does not claim a mono scale.

### Family

- **Current visible UI family:** `Anthropic Sans` — YAML `family.ui`
- **Current visible display family:** `Anthropic Serif` — YAML `family.display`
- **Declared-only:** `anthropicMono`, JetBrains Mono — captured `@font-face` sources; zero visible computed usage
- **CSS fallbacks only:** Arial, Georgia — not substitutes for the named Anthropic faces

The following font-use boundary, including do-not-render-a-system-fallback-as-a-named-Anthropic-typeface, Arial-Georgia-Inter-or-a-system-stack-not-labeled-as-Anthropic-Sans-Serif-or-Mono, Anthropic-Sans-not-Anthropic-Serif, and declared-only-anthropicMono-and-JetBrains-Mono-not-loaded-UI-faces, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Do not render a system fallback as a named Anthropic typeface. Do not present Arial, Georgia, Inter, or a system stack as Anthropic Sans, Anthropic Serif, or Anthropic Mono. Do not present Anthropic Sans as Anthropic Serif. Declared-only fonts must not be presented as loaded Anthropic faces.

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1.10`, `1.20`, and `1.60`. The following ratio-versus-size-local reading, including unitless-ratios-not-fixed-px, public-h1-64-not-h2-52-not-h3-36, body-20-not-control-15, public-tab-20-as-body-table-not-a-YAML-type-role, public-h3-as-body-table-only, and yaml-has-no-lineHeight-for-h3-or-tab-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. Public h3 and Public tab are body-table roles; YAML has no `lineHeight` field for them, and none is invented.

| Role | Font | Size | Weight | Line height (YAML) | Body observation | Use (YAML) / Notes |
|---|---|---:|---:|---:|---|---|
| Public h1 | Anthropic Serif | 64px | 500 | 1.10 | 70.4px at this size | Observed Claude public-route h1; two public-route h1 occurrences |
| Public h2 | Anthropic Serif | 52px | 500 | 1.20 | 62.4px at this size | Observed Claude public-route h2; five public-route h2 occurrences |
| Public h3 | Anthropic Serif | 36px | 500 | — | 46.8px | four public-route h3 occurrences (body table only) |
| Repeated body/list | Anthropic Sans | 20px | 400 | 1.60 | 32px at this size | Repeated public-route body and list text |
| Public control | Anthropic Sans | 15px | 400 | 1.60 | 24px at this size | Observed public-route button and input text; preserve selector-specific geometry |
| Public tab | Anthropic Sans | 20px | 400 | — | 32px | overview and pricing tab controls (body table only; YAML has no separate tab type-role) |

Treating the trailing type-table restatement, including system-fallback-not-a-named-Anthropic-typeface and Anthropic-Mono-declared-only-not-a-mono-scale, as the same font-use boundary already limited at Family, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Do not render a system fallback as a named Anthropic typeface. Anthropic Mono is useful as a declared asset record, but no visible use was captured, so this reference does not claim a mono scale.

### Assets

Treating catalog logo metadata as Simple Icons identity (`anthropic`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

Browser-served WOFF2 files on the captured public routes are delivery evidence for Anthropic Sans and Anthropic Serif. They are not a license grant for reuse. Treating that delivery-versus-license split, including no-standalone-first-party-font-license-or-type-specimen-in-this-packet, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, including because-the-supplied-capture-records-only-selected-tab-selected-and-pressed-tab-behavior-plus-tab-panel-expansion-and-does-not-provide-selector-backed-empty-loading-error-success-skeleton-or-disabled-treatments, intentionally-left-unspecified-rather-than-populated-with-plausible-Claude-like-patterns, and no-generic-modal-toast-menu-error-disabled-or-hover-system-is-inferred-from-the-supplied-artifact, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted: The supplied capture records only selected/tab-selected and pressed tab behavior, plus tab-panel expansion on the public overview and pricing pages. It does not provide selector-backed empty, loading, error, success, skeleton, or disabled treatments. Those state categories are intentionally left unspecified rather than populated with plausible Claude-like patterns. Only tab selection, tab pressed, and tab-panel expansion were observed as interactions. No generic modal, toast, menu, error, disabled, or hover system is inferred from the supplied artifact. YAML overview-tab `states`: selected, hover, and pressed observed on the public overview tabs. No selected/disabled variant was captured for the appearance toggle.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating a later generic Focus observation as not `focus-visible` evidence, treating selected-hover-and-pressed as additional named observed states rather than Core applicability rows or `focus-visible` treatments, treating Overview Tab loading-error-success as role-based not-applicable rather than closed from a missing capture, and treating Model Card, Pricing Card, and Appearance Toggle Tile as having no interactive-kind confirmation, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `focus-visible` treatment and no generic Focus capture; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Overview Tab closes those three fields as not-applicable by meaning (panel selection). This is not a complete state-coverage claim.

Treating selected / tab-selected, YAML-observed hover (no hex), and pressed as additional named observed states rather than Core applicability rows or a `focus-visible` treatment is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Selected / tab-selected, YAML-observed hover (no hex), and pressed are additional named observed states; they are not among the seven Core applicability rows and are not copied onto a `focus-visible` row.

Treating Model Card and Pricing Card as YAML `type: card` with no interactive-kind confirmation, treating Appearance Toggle Tile as having no YAML `type` and no interactive-kind confirmation, and omitting kind and a state-applicability map rather than inventing them, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Model Card and Pricing Card have YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Appearance Toggle Tile has no YAML `type` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. None is invented.

### Overview Tab

- Role: selected tab on the public Claude overview
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#ffffff`
- Text: `#141413`
- Radius: 12px
- Padding: `8px 16px 8px 12px`
- Font: `20px/400 Anthropic Sans`
- Use: YAML `Selected tab on the public Claude overview`
- YAML `states`: selected, hover, and pressed observed on the public overview tabs
- Observed: selected (harvested default fields); hover observed (no hex); pressed sample below
- Field note: The following unmerged-field reading, including this-fill-as-white-surface-not-parchment-canvas, this-label-as-near-black-not-muted-`#5e5d59`-not-pressed-`#5d5c58`, 12px-not-pricing-24px-not-model-32px, 20px-400-as-this-tab-field-not-a-second-body-role, and selected-hover-pressed-as-additional-named-observed-states-not-Core-rows, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. `#ffffff` is this tab’s selected fill (White surface). `#141413` is this tab’s selected label (Near black). Radius 12px is this tab (YAML `lg`); it is not pricing-card 24px or model-card 32px. Font 20px / 400 is this control’s field. Selected / tab-selected, YAML-observed hover, and pressed are additional named observed states, not Core applicability rows and not `focus-visible` treatments.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the selected tab on the public Claude overview |
| hover | applicable | Pointer-web tab; YAML records hover observed; visual treatment omitted (no hex) |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An overview tab can be unavailable; visual treatment omitted |
| loading | not-applicable | An overview tab selects a panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is panel selection, not action-outcome confirmation |

Treating the following pressed-sample reading, including this-as-the-observed-pressed-state-not-a-universal-hover-or-focus-rule and `#5d5c58`-not-muted-`#5e5d59`, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. Additional observed named state: pressed — Unselected Overview Tab pressed sample: Text `#5d5c58`; Radius 12px; Padding `8px 16px 8px 12px`; Font `20px / 400 / Anthropic Sans`; Pressed background `rgba(255, 255, 255, 0.004)`. This is the observed pressed state, not a universal hover or focus rule. `#5d5c58` is this pressed-sample text; it is not Muted `#5e5d59`.

### Model Card — overview

- Role: public Claude overview model card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Text: `#141413`
- Border: `1px solid #f0eee6`
- Radius: 32px
- Padding: 48px
- Font: `20px / 400 / Anthropic Sans`
- Use: YAML `Public Claude overview model card`
- Observed: default only; four observed overview cards
- Field note: The following unmerged-field reading, including white-surface-not-parchment-canvas, hairline-border-not-featured-blue-tinted-border, 32px-not-pricing-24px-not-tab-12px, 48px-padding-as-this-card, and no-computed-shadow-not-featured-shadow, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. `#ffffff` is this card’s fill (White surface). `1px solid #f0eee6` is this card’s hairline. Radius 32px is this model card (YAML `media`); it is not pricing-card 24px or tab 12px. Padding 48px is this captured variant. No computed shadow.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Pricing Card — standard

- Role: public Claude pricing card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Text: `#141413`
- Border: `1px solid #f0eee6`
- Radius: 24px
- Padding: 32px
- Font: `20px / 400 / Anthropic Sans`
- Use: YAML `Public Claude pricing card`
- Observed: default only; two observed public pricing cards
- Field note: The following unmerged-field reading, including white-surface-not-parchment-canvas, hairline-not-featured-`color(srgb 0.415686 0.607843 0.8 / 0.2)`, 24px-not-model-32px-not-tab-12px, 32px-padding-not-model-48px, and no-computed-shadow-not-featured-`rgba(98, 158, 218, 0.16) 0px 4px 20px 0px`, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. `#ffffff` is this card’s fill. `1px solid #f0eee6` is this card’s hairline. Radius 24px is this pricing card (YAML `card`); it is not model-card 32px. Padding 32px is this captured variant. No computed shadow.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Pricing Card — featured

- Role: featured public pricing-card variant
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Border: `1px solid color(srgb 0.415686 0.607843 0.8 / 0.2)`
- Radius: 24px
- Padding: 32px
- Shadow: `rgba(98, 158, 218, 0.16) 0px 4px 20px 0px`
- Use: one featured public pricing-card variant only
- Observed: default only
- Field note: The following unmerged-field reading, including this-blue-tinted-border-not-hairline-`#f0eee6`, this-shadow-not-a-general-elevation-token, 24px-matching-standard-pricing-not-model-32px, and one-route-local-variant-not-promoted-to-the-general-palette, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. The cool translucent border and blue-tinted shadow are this featured variant’s fields. They are not Hairline `#f0eee6` and not a reusable elevation scale. Radius 24px matches the standard pricing card; it is not model-card 32px.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Appearance Toggle Tile

- Role: overview appearance control
- Anatomy: tile
- Background: `#e8e6dc` (computed as `color(srgb 0.909804 0.901961 0.862745)`)
- Text: `#4d4c48`
- Radius: 16px
- Shadow: `#e8e6dc 0px 0px 0px 0px, #d1cfc5 0px 0px 0px 1px`
- Font: `16px / 400 / Anthropic Sans`
- Use: one overview control. No selected/disabled variant was captured.
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-as-warm-sand-not-parchment-canvas-not-white-surface, `#4d4c48`-not-muted-`#5e5d59`-not-near-black-`#141413`, 16px-not-YAML-rounded-step, `#d1cfc5`-one-pixel-ring-not-hairline-`#f0eee6`, yaml-has-no-type-none-invented, and 16px-400-as-this-control-not-tab-20px, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. `#e8e6dc` is this tile’s fill (Warm sand). `#4d4c48` is this tile’s label; it is not Muted `#5e5d59`. Radius 16px is this captured control; YAML `rounded` has no 16 step. `#d1cfc5` is this one-pixel warm ring; it is not Hairline `#f0eee6`. YAML has no `type` for this control; none is invented.

Treating this tile as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML-spacing-xs-4-sm-8-md-12-lg-16-xl-24-xxl-32-3xl-48, observed-public-style-inventory-not-a-declared-global-spacing-scale, recorded-public-radii-6-8-12-16-24-32, 12px-tab-versus-24px-pricing-card-versus-32px-model-card, preserve-12px-tab-24px-pricing-card-32px-model-card-as-route-local-geometry-rather-than-one-interchangeable-radius, no-mobile-viewport-container-width-or-responsive-breakpoint-measurement, and do-not-derive-breakpoints-or-a-universal-grid-from-this-packet — are a derived editorial implementation inference from the verified surfaces; they are not Anthropic-authored or a separately published UI specification. YAML spacing is xs 4, sm 8, md 12, lg 16, xl 24, xxl 32, 3xl 48. The artifact clusters 4px, 8px, 12px, 16px, 24px, 32px, and 48px spacing values. This is an observed public-style inventory, not a declared global spacing scale. Recorded public radii include 6px, 8px, 12px, 16px, 24px, and 32px. The 12px tab, 24px pricing card, and 32px model card have selector-backed component use above.

- The supplied capture has no mobile viewport, container-width, or responsive-breakpoint measurement. Do not derive breakpoints or a universal grid from this packet.
- Preserve the 12px tab, 24px pricing card, and 32px model card as route-local geometry rather than one interchangeable radius.
- No breakpoint, collapse, or touch-target token claims are made.

Treating those 4px / 8px / 12px / 16px / 24px / 32px / 48px figures and the 12px / 24px / 32px radii as captured-surface measurements rather than a cross-viewport specification, including no-responsive-breakpoint-or-mobile-surface-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. No responsive breakpoint or mobile-surface evidence was supplied. The reference intentionally has no breakpoint, collapse, or touch-target token claims.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Official Anthropic sources frame the company as an AI safety and research company building systems people can rely on, and describe Claude’s intended behavior as helpful, honest, thoughtful, and caring. Treating the following as a source-informed public-content voice rather than a universal copywriting ban list or an app-wide tone specification, including official-writing-characterization, direct-careful-about-uncertainty-and-oriented-to-practical-help, explain-the-user-outcome-and-the-relevant-capability-plainly, name-the-tradeoff-and-intent-directly, prefer-specific-accountable-language-about-reliable-interpretable-and-steerable-systems, distinguish-an-intention-or-policy-from-an-observed-product-behavior, and tone-table-context-labels-Product-explanation-Safety-or-policy-Company-communication, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. This supports a source-informed public-content voice that is direct, careful about uncertainty, and oriented to practical help; it does not prove a universal copywriting ban list or an app-wide tone specification.

| Context | Source-informed guidance |
|---|---|
| Product explanation | Explain the user outcome and the relevant capability plainly, as the official Claude overview does for conversation, documents, tools, and web knowledge. |
| Safety or policy | Name the tradeoff and intent directly; the Constitution explains both broad safety and genuine helpfulness rather than reducing the topic to a slogan. |
| Company communication | Prefer specific, accountable language about reliable, interpretable, and steerable systems. |

Treating the following Do / Don't items as a copy of source §10, including state-what-the-product-can-help-a-person-or-organization-do, keep-safety-and-helpfulness-in-the-same-explanation, distinguish-an-intention-or-policy-from-an-observed-product-behavior, Constitution-intended-values-not-a-guarantee-about-every-response, and public-product-page-tone-not-an-undocumented-rule-for-support-legal-or-signed-in-surfaces, is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification.

### Do

- State what the product can help a person or organization do.
- Keep safety and helpfulness in the same explanation when both materially affect the outcome.
- Distinguish an intention or policy from an observed product behavior.

### Don't

- Don't present the Constitution’s intended values as a guarantee about every response.
- Don't turn the public product-page tone into an undocumented rule for support, legal, or signed-in surfaces.

The following captured-host reading — Anthropic-marketing-Claude-public-overview-and-public-pricing as the named hosts in this packet rather than a complete copywriting ban list or an app-wide tone specification, and unobserved-signed-in-support-or-legal-copy remaining unnamed — is a derived editorial implementation inference from the verified surfaces; it is not Anthropic-authored or a separately published UI specification. This packet names Anthropic marketing, Claude public overview, and public pricing. Unobserved signed-in, support, or legal copy rules remain unnamed in this pass.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover visual treatment (YAML records overview-tab hover observed; no hex)
- focus-visible, empty, loading, error, success, skeleton, and disabled visual treatments
- Overview Tab loading·error·success applicability is closed as not-applicable by tab meaning
- interactive kind and state-applicability map for Model Card, Pricing Card — standard, Pricing Card — featured, and Appearance Toggle Tile
- YAML `type` for Appearance Toggle Tile
- authenticated-chat, Help Center/documentation-chrome, and internal-product colors, type, and components
- featured pricing-card blue-tinted border/shadow as a general elevation or palette role
- Anthropic-exclusive redistributable brand font; a standalone font-license or type specimen
- a mono type scale (Anthropic Mono and JetBrains Mono remain declared-only)
- responsive breakpoints, collapse, touch-target tokens, and a universal grid
- chat composer, error, toast, menu, and generic hover system
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
