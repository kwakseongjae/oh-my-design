# moin migration log

Source: `web/references/moin/DESIGN.md`
Sibling read (not the migration input): `web/references/moin/.verification.md`
Destination: `docs/design-md-weight/migrated/moin/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/moin/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `e8550ef46f515a6e1ba38bc40e0bfdb99d6113f094900a7ba765826f28fcf7f0` (`web/references/moin/DESIGN.md`). Sibling SHA-256 `bff3069ccd9720bbfab2a4a54814302a0ba7fdfa7277541b6c1f71a45390ee71` (`web/references/moin/.verification.md`). Worker-close portable DESIGN SHA-256 `9dd72a6f4cd8fe87b85c071dc7e2b0ef6f75b72720d04cb7ab68e0fbf565d6b5`. Worker-close provenance SHA-256 `b58c182373811cb07c1d82644a9d8d6ba9f841bcda42ccc7b48ca20cd191926b`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `display_name_kr` 옮김 → Scope + Content | Portable file has no frontmatter. H1 is `# Moin Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://www.themoin.com/ko` is dual: `DESIGN.md` Scope + `provenance.md` Identity/Surfaces (E2a). `#0082ff` is dual: `DESIGN.md` + `provenance.md` (`grep -oF '#0082ff'` dest 21 / provenance 8). `모인` is dual: dest 3 / provenance 4. Favicon slug is dual: Assets classification `DESIGN.md` + identity `provenance.md` (`s2/favicons?domain=themoin.com` dest 1 / provenance 1). |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance; `live-extract` 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. `live-extract` is dual: `DESIGN.md` Scope dest 1 / `provenance.md` dest 2 (E2a). Token note transcribed verbatim in Identity. `components_harvested` dest 0 / provenance 2 (ledger-only; dual is provenance Token/Proof, not the portable body). Source footer producer string `omd:add-reference CREATE — Tier 1 live inspect` is in Freshness. |
| YAML `tokens.colors` (16 keys) | 옮김 → Foundations semantic color | `DESIGN.md` Semantic color. All sixteen roles kept with names and values: primary, primary-alt, ink, body, dark, muted, muted-slate, grey, faint, canvas, surface, surface-cool, hairline, border, border-cool, on-primary. `grep -oF` dest: `#0082ff` 21 · `#007bff` 8 · `#1a1b22` 14 · `#333333` 12 · `#242424` 4 · `#6b6c74` 3 · `#818daa` 2 · `#555555` 2 · `#a9abb4` 4 · `#ffffff` 10 · `#f7f7f8` 9 · `#f3f5f8` 7 · `#efefef` 10 · `#e0e0e0` 5 · `#e9ecef` 2. `canvas` and `on-primary` share `#ffffff` as two paths. Section H2 "Why so many users choose MOIN" keeps `#333333`; it is not Ink Navy. |
| YAML `tokens.typography.family` (`sans: "Spoqa Han Sans"`, `fallback: "Spoqa Han Sans JP"`) | 옮김 → Typography & Assets Family | `DESIGN.md` Family. Paths named `tokens.typography.family.sans` dest 1 / `fallback` dest 1. `Spoqa Han Sans` dest 33. Fallback prohibition carries an adjacent complete qualifier (B2/B2a). |
| YAML `tokens.typography.display-hero / section-lg / section / eyebrow / body / button` | 옮김 → Typography & Assets Type roles | Unitless line heights stay ratios (A1a): `1.2` dest 3 · `1.5` dest 3. §3 px/rem spellings sit beside YAML integers (`2.50rem` dest 1 · `2.00rem` dest 1 · `1.50rem` dest 1 · `1.00rem` dest 3). YAML `use` strings restored; longer §3 / live-inspect writings sit beside them (wave 37): section-lg `"Why so many users choose MOIN"`; eyebrow `Easy to start`; button `400-700` (700 on dark/nav, 400 on exchange CTA). Two 32px uses stay unmerged: section title `#333333` vs amount `#242424` / `#0082ff`. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (5 steps) | 옮김 → Foundations spacing + shape; also Layout | Paths kept separate: `tokens.spacing` dest 15 · `tokens.rounded` dest 11. Spacing steps: `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 24` · `xl: 48` · `section: 64`. Shape steps: `sm: 6` · `md: 8` · `lg: 16` · `xl: 20` · `full: 9999`. `full: 9999` dest 2; `9999` dest 5; §5 `9999px / 50%` sits beside it. `xl: 20` kept as "larger rounded containers". `md: 12` is spacing, not a radius. `base: 16` is not `rounded.lg: 16`. `xl: 48` is not carousel height 48px. |
| YAML `tokens.shadow.card` / `elevated` | 옮김 → Foundations elevation | Longer §6 form `rgba(0, 0, 0, 0.05) 0px 4px 15px 0px` dest 3; YAML short `rgba(0,0,0,0.05) 0px 4px 15px` dest 2. Elevated `rgba(36, 36, 36, 0.3) 0px 10px 20px 0px` dest 2. Five-level table kept. `box-shadow: none` dest 1. |
| YAML `tokens.components` (8 records) | 옮김 → Components & States | Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×4 (Exchange / Nav Send / Dark / Carousel), `input` ×1, `card` ×2, `badge` ×1. All eight YAML `use` strings restored as `Token-set use:` lines. YAML font shorthands `16px / 400 Spoqa Han Sans` dest 1 · `16px / 700 Spoqa Han Sans` dest 3 · `32px / 700 Spoqa Han Sans` dest 1. YAML `states` byte form `active value turns #0082ff` dest 1. YAML border byte forms restored: `1px solid rgba(0,50,100,0.1)` dest 1 · `1px solid #007bff` dest 1 · `1px solid #e0e0e0` dest 1 · `1px solid #efefef` dest 1. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope atmosphere paragraph; Key Characteristics as Distinctive traits. Atmosphere readings carry an adjacent complete qualifier (B2/B2a). Surface-boundary reading at Scope ¶1. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Source group headings (Primary, Ink & Text, Surface & Neutral) kept. Role-naming readings carry the adjacent qualifier. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 두 개 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. `https://www.themoin.com/ko` / `https://themoin.github.io/` are dual: Scope + provenance Surfaces (E2a). Conflicts unresolved: none — `provenance.md` Freshness. |
| §3 Typography Rules — family, hierarchy | 옮김 → Typography & Assets | Evidence classes; family; hierarchy table. Official product-use and License rows carry adjacent complete qualifiers (B2/B2a). |
| §3 Principles — one family, compact scale, bold/plain, number is loudest | 옮김 → Typography & Assets Typography rules | Under the qualifier on that heading. |
| §4 Component Stylings | 옮김 → Components & States | Eight token-set anatomies plus §4 Navigation (not in the token set). §4 body values and YAML `use` / `font` / `states` / `border` / padding byte forms are both kept where they differ. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | Centered hero, exchange widget as primary interactive object, alternating bands, carousel, whitespace names, spacing/shape restatements. Breathing-room / numbers-get-room readings carry the qualifier. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Five-level table including Elevated `rgba(36, 36, 36, 0.3)`. Shadow-philosophy paragraph is a qualified reading. |
| §7 Do's | 옮김 → Experience application rules | Brand rules. Not placed in Governance controlled copy. Eight items, matching the source Do list. |
| §7 Don'ts | 옮김 → Experience avoid | Brand prohibitions. Eight items, including the `#000000` body-text prohibition and `sharp 0px corners`. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms; image behavior 옮김 → Typography & Assets Assets + Layout | Breakpoint table with the "stated at system level rather than measured across viewports" qualifier; touch targets; collapsing strategy; image behavior. Source Desktop row `1024-1440px` kept as declared width. Exact string `measures 1440px` dest 0 / provenance 0. |
| §9 Agent Prompt Guide — Quick Color Reference, Example Component Prompts, Iteration Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present — the sixteen palette hexes, the 6/8/16/20/9999 radii, the 40/32/24/16 sizes, the 700/400 weights, the 8px 16px and 14px 48px paddings, and the published English strings (A2, A3). Provenance §9 deletion check. |
| §10 Voice & Tone | 옮김 → Content & Locales | Register, context table, three verbatim samples, forbidden register. Hero line kept byte-exact. `best rates` dest 1. Qualifier covers the characterization and the table (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope ¶3 + 분리 → provenance Proof notes | **Moin Inc.** dest 3 / provenance 1. `licensed small-sum overseas-remittance` dest 2. `Foreign Exchange Transactions Act` dest 2 / provenance 2. `early independent players` dest 1. `"Complex international remittances, now made simple with MOIN!"` dest 6. The shorter §11 writing `"Complex international remittances, now made simple"` greps dest 9 because it is also a prefix of the longer line. `about five minutes on their phone` dest 1. `sender's advocate` dest 1. Closing refuse-and-embrace paragraph: `no shadow-stacked cards` dest 1 · `institutional navy-and-gold ornamentation` dest 1 · `dark-pattern urgency of aggressive financial marketing` dest 1 · `the real number — the amount and the fee` dest 1. Dual: `DESIGN.md` Scope and provenance Claim ledger / Proof notes (E2a). Philosophy-layer public-knowledge bound kept in Scope, not only in the sidecar (B2). |
| §12 Principles — 5 numbered | 옮김 → Experience principles | Under the B2a form: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Moin-authored or a separately published UI specification." No published first-party DS, so the toss-form close is used (rulebook v12 B2a 전제 주석). |
| §13 Personas — 3 entries | 삭제 | The source's own italic line labels them fictional archetypes informed by publicly observable segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, or city appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience keeps only source-named groups in source wording: migrant workers dest 3 · students dest 3 · anyone sending money home dest 2 · international students dest 1 · Koreans sending money abroad dest 1. The three `primary-tasks` come from labels and surfaces the source records; the claim says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows | 옮김 → Components & States State record + per-component applicability | Full nine-row body preserved (A2; the catalog graph is still 0/440, so nothing is delegated), including Empty (no transfer history) / Empty (saved recipients) / Loading (rate fetch) / Loading (transfer submitting) / Error (rate/transfer failed) / Error (form validation) / Success (transfer submitted) / Skeleton / Disabled. Applicability rule is in How to read this section, whose qualifier covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 5 interactive components × 7 states = 35 rows. `not-applicable` only with a role reason (C2 v10): Nav Send / Dark Secondary / Carousel close loading/error/success as destination or arrow; Currency Amount closes success as a value field. Exchange CTA keeps loading/error/success applicable (core product action that commits a transfer). `loading \| applicable` dest 2 · `loading \| not-applicable` dest 3. Two cards omit kind + map (C4). Azure Eyebrow declares `Kind: non-interactive` dest 1. Top Navigation is not in the token set dest 1 and omits kind + map (C4). `Kind and applicability map omitted` dest 3. |
| §15 Motion & Easing — durations, easing token names/roles, motion rules, reduced-motion | 옮김 → Foundations motion | Three duration tokens with their uses: `motion-fast` dest 2 · 120ms dest 1, `motion-standard` dest 2 · 200ms dest 1, `motion-slow` dest 1 · 320ms dest 1. Three easing token names and roles. Functional/quiet rule, amount color flip at `motion-fast`, carousel at `motion-standard / ease-enter`, opacity/scale on press, no bounce or spring, and `prefers-reduced-motion: reduce` dest 1. |
| §15 Motion & Easing — three `cubic-bezier` values | 삭제 → provenance omission ledger | No observation stands behind them: the source records a Tier 1 live inspect of color, type, geometry, and shadow and supplies no transition, animation, or easing sample. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is byte-identical to the example table at `spec/omd-v0.1.md` line 267, the documented re-injection path. The other two (`cubic-bezier(0.2, 0.6, 0.25, 1)`, `cubic-bezier(0.25, 0.1, 0.25, 1)`) are likewise unattributed. Ledger at `provenance.md` Omission ledger. Occurrence counts by `grep -oF cubic-bezier` → `DESIGN.md` 4 (three omitted values named + the omd-v0.1 citation), `provenance.md` 2. B3 is held: the promotion condition at Foundations Motion names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus the partial-confirmation exclusion (`A partial confirmation` dest 1) (E2c). Governance restates the five kinds and the gate; it does not repeat the partial-confirmation sentence (`computed transition properties` dest 2). |
| HTML comment OmD v0.1 Sources | 분리 → provenance + 옮김 → Scope / Principles 한정 | Live-inspect notes, Tier 1/2 URLs, narrative class, voice-sample attribution, and the editorial-reading admission are ledger material: `provenance.md` Sibling handling + Proof notes. Founding public-knowledge bound and interpretive-claim class stay in portable Scope and Principles, not only in the sidecar (B2). Personas comment is disposition, not a rehost of names. |

## Sibling handling (`web/references/moin/.verification.md`)

The sibling exists — confirmed with `find web/references/moin -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: amount-field height `47px` (D 0 / P 1), radius frequency `12px` ×2, text frequency `rgb(0,0,0)` ×69, blog link colors, viewport `1440×900`. Recorded at `provenance.md` Sibling-only values. Neither side of a missing height was filled as a new token.
- Published strings that appear only in the sibling survive in provenance, not in the portable body: `Previous review` / `Next review` (D 0 / P 1), `MOIN: Global Money Transfer` (D 0 / P 1), blog nav `소개` / `포스트` / `채용`, sibling footer comma form `© 2026, ALL RIGHTS RESERVED MOIN Inc.`. `MOIN 모인 기술 블로그` is not sibling-only: the source HTML comment names it, and provenance holds it (D 0 / P 2).
- The source HTML comment's no-comma footer `"© 2026 ALL RIGHTS RESERVED MOIN Inc."` is the source writing; the sibling comma form stays in provenance. Dual writings that the source already names (hero H1 split lines; section H2 `#333333`) stay in `DESIGN.md`.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Brand-issued strings in the source (labels, CTAs, slogans, microcopy, names) | 14 distinct | 0 | 0 | All 14 survive in `DESIGN.md`: 모인, Moin, Moin Inc., MOIN, "Send money", "More reviews", "Speed", "Easy to start", "Lower fees", "Why so many users choose MOIN", "Complex international remittances, now made simple with MOIN!", "Complex international remittances, now made simple", "Fast, direct remittances you can trust", "Done in five minutes, right in the app". |
| Brand-issued strings in the sibling, any length | 5 sibling-only groups | 0 | 0 | Carousel "Previous review"/"Next review", document.title, blog title, blog nav, sibling footer comma form survive in provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand moin` after restore | 40 candidates | 1 reported | 0 published-copy | First run reported 5 lost: four YAML border byte forms (`1px solid #007bff`, `1px solid #e0e0e0`, `1px solid #efefef`, `1px solid rgba(0,50,100,0.1)`) — CSS values, not published copy — restored as `YAML \`…\`` lines. Remaining lost: `great rates`. That string is quoted only inside the source persona biography (`rather than "great rates"`). It is not issued UI copy. It is omitted with §13 (D2) and is not restated here as use (D2a). `best rates` from §10 dest 1. Re-run: `lost: 1` (`great rates` only). |

A5 분모: latin-copy-audit 40 candidates / 1 reported lost / 0 published-copy lost. Korean/English hand sweep 14 extracted / 0 missing / 0 published-copy missing. Sibling-only published 5/5 in provenance.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand moin --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 175 }]`. `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 175.

## Deviations recorded

- `DESIGN.md` is 7,885 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: sixteen color roles, six type roles with dual unitless/px forms, eight declared component records with dual `use`/`font`/`states`/`border` forms, a nine-row surface state contract, a 35-row applicability matrix, homepage-vs-blog domain splits, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Moin has no published first-party design system (getdesign.md no data; refero no Moin fintech match), so every derived-editorial close uses the toss-form "not Moin-authored or a separately published UI specification" (rulebook v12 B2a 전제 주석). Measure `grep -o 'not Moin-authored or a separately published UI specification' DESIGN.md | wc -l` = 38. `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 38. Provenance `## Derived editorial inventory` data rows = 38 (1:1). F3 audit added six complete forms (Semantic color 32px unmerge; Tinted Surface Card / Cool Feature Block / Azure Eyebrow geometry; Top Navigation identity; State-record non-attachment) and folded Focus≠`focus-visible` into the how-to-read qualifier.
- There is no `Named gaps` section (`Named gaps` dest 0). Unresolved values the source itself named sit under Recorded unresolved decisions: exact easing curves, disabled opacity, skeleton pulse, engineering-blog tokens.

## Wave 43 unique-phrase contrast

Phrases pulled from source §1/§11 narrative, value modifiers, and §5/§7/§10/§14/§15 constraints: 63. First-pass zeros: 0. YAML border byte forms restored after latin-copy-audit: 4. `live-extract` restored to Scope as dual A1c. Restored-from-zero in the unique-phrase set: 0.

## Mandatory passes

- **F1 B2a scan:** DESIGN.md re-read end to end. Causal/interpretive sentences (Scope surface-boundary, atmosphere readings, founding-and-licensing classification and refuse/embrace, primary-task selection, audience grouping, distinctive-trait grouping, principles, Do/Don't, color role naming, path-keeping, Semantic color 32px unmerge, elevation/mobile-native reading, unattributed motion, spring stance, font-evidence classifications, fallback prohibition, type-role attachment, typography principles, favicon identity pointer, kind/applicability verdicts including Focus≠`focus-visible`, Tinted Surface Card / Cool Feature Block / Azure Eyebrow geometry, Top Navigation identity, unattributed state record and its non-attachment to destination/carousel controls, layout philosophy, responsive system-level reading, voice characterization) carry an adjacent complete qualifier. Path-keeping that sits in its own paragraph is counted as a B2a row when an adjacent complete form is present.
- **F2 E2 contrast:** each log row was written after `grep -oF` against the destination files, then remeasured after the F3 body edits (`grep -o … | wc -l` per file, never `grep -c`). Dual destinations named where a value lives in both DESIGN.md and provenance.md. "B3 유지" is claimed only because Foundations Motion carries the five evidence kinds, the per-component gate, and the partial-confirmation exclusion in full text.
