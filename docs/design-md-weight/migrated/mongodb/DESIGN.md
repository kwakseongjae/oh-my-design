# MongoDB Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

MongoDB is a developer data platform built around a flexible document model and services that let teams build and scale modern applications. Catalog homepage is `https://www.mongodb.com`. Catalog `primary_color` is `#00ed64`. YAML `ds.name` is LeafyGreen; `ds.url` is `https://www.mongodb.design`; `ds.type` is `system`; YAML describes it as MongoDB's open-source design system and React component library. This contract covers the three first-party public surfaces in the 2026-07-13 collector bundle: public marketing home `https://www.mongodb.com/` (YAML `home`, marketing), the LeafyGreen design system `https://www.mongodb.design/` (YAML `surface-2`, design-system), and public documentation chrome `https://www.mongodb.com/ko-kr/docs/` (YAML `surface-3`, documentation). Official typography guidance at `https://www.mongodb.design/foundations/typography` is named official-doc context for type roles; official company pages at `https://www.mongodb.com/company` and `https://www.mongodb.com/company/our-story` are named narrative sources. They do not by themselves supply the computed interface tokens below. Every color, type, geometry, and component value stays attached to the surface that established it. Home is marketing, `mongodb.design` is the LeafyGreen design system, and `/docs/` is documentation chrome. No authenticated product screen was captured. This reference keeps those three public domains separate rather than treating the public website as an authenticated Atlas UI. Reading those three inspected pages as this contract's token surfaces, keeping values attached to the surface that established them, and treating LeafyGreen typography guidance plus the company and our-story pages as named sources that do not automatically supply computed interface tokens, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

Across the captured public marketing, LeafyGreen design-system, and documentation surfaces, that technical purpose is expressed through a navy-and-spring-green signal system, strong rectangular actions, and a deliberately mixed typographic voice: Value Serif for display authority, Euclid Circular A for UI clarity, and Source Code Pro for code-like labels. `#00ed64` appears as a navy-text primary action on the public home and documentation headers. `#001e2b` anchors dark marketing sections and inverse actions; LeafyGreen’s captured design-system page uses a separate `#112733` dark canvas. Value Serif leads headings; Euclid and Source Code Pro support product reading and code/value notation. The hex values, the three families, and the three-domain split are recorded. Calling that expression a navy-and-spring-green signal system, calling the actions strong rectangular actions, and calling the type mix a deliberately mixed typographic voice, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation. The type-by-role assignment itself is LeafyGreen’s official product-use statement and is recorded in Typography & Assets.

Brand narrative recorded by the source, kept as narrative context. Its origin story is equally developer-centred: the founders’ experience operating DoubleClick at high scale led them to establish 10gen in 2007, launch MongoDB in 2009, and rename the company after the product in 2013. Today Atlas joins data services into a unified developer data platform. MongoDB traces its start to Dwight Merriman, Eliot Horowitz, and Kevin Ryan’s experience at DoubleClick, where relational databases struggled at more than 400,000 ads per second. MongoDB’s official history says they founded the company, then called 10gen, in 2007 to make a more scalable and flexible approach to data. The team launched MongoDB in 2009 with a document model intended to manage large, unstructured data and scale for modern applications. The company renamed itself MongoDB in 2013 as the product gained traction. Its current account presents Atlas as the unified developer data platform that carries forward that developer-first direction across cloud providers and on-premises workloads. The origin in the founders’ experience operating DoubleClick at high scale, the 10gen founding in 2007, the 2009 launch, the 2013 rename after the product, and the Atlas account are the source’s own narrative facts; they do not by themselves supply interface tokens. Classifying that DoubleClick-to-Atlas narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation. Each names a control or surface the source records. They do not come from the source's persona section.

- Use the public home and documentation header primary action (“Get Started”).
- Read the LeafyGreen design system at `https://www.mongodb.design/`.
- Use compact public documentation controls on `https://www.mongodb.com/ko-kr/docs/`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source's persona section states that these are stakeholder groups named by MongoDB's official company and story materials, not fictional user profiles. Those group labels, kept as the source wrote them, are Developers and builders; Innovators and application teams; and Organizations operating across environments. MongoDB describes developers as the product’s North Star and the audience that needs a flexible way to manage and interact with data. The company mission addresses teams creating, transforming, and disrupting industries with modern applications. Atlas is described as consolidating workloads through a unified experience for on-premises or preferred-cloud deployment. Keeping those official group labels as Audience, and not converting the North Star / innovator / Atlas sentences into primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

### Distinctive traits

- **High-signal green action:** `#00ed64` appears as a navy-text primary action on the public home and documentation headers.
- **Dark technical field:** `#001e2b` anchors dark marketing sections and inverse actions; LeafyGreen’s captured design-system page uses a separate `#112733` dark canvas.
- **Editorial plus operational typography:** Value Serif leads headings; Euclid and Source Code Pro support product reading and code/value notation.
- **Domain boundary:** home is marketing, `mongodb.design` is the LeafyGreen design system, and `/docs/` is documentation chrome. No authenticated product screen was captured.

The four labels and the grouping that treats `#001e2b` and `#112733` as two observed dark canvases rather than one collapsed navy are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation. Each bullet names the values it rests on.

### Principles

These 4 items are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation. The numbered stems and UI implications below are those editorial readings, kept as the source wrote them. The type-by-role assignment inside item 3 restates LeafyGreen’s official typography guidance and is recorded in Typography & Assets.

1. **Build for developer agility.** *UI implication:* explain the concrete action or data outcome before introducing platform breadth.
2. **Make flexibility legible.** *UI implication:* distinguish a public marketing message, design-system rule, and documentation affordance instead of flattening them into one generic UI.
3. **Use type by role.** *UI implication:* Value Serif belongs in display hierarchy, Euclid in product reading/UI, and Source Code Pro in code/value contexts.
4. **Keep high-signal actions disciplined.** *UI implication:* reserve Spring Green primary actions for the observed navy-text pairing; Forest Green remains announcement chrome in this evidence set.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

- Use Spring Green (`#00ed64`) with MongoDB Navy (`#001e2b`) for the observed public primary-action pairing.
- Keep Value Serif for the captured display hierarchy, Euclid for UI/body roles, and Source Code Pro for code/value labels.
- Preserve the source-domain distinction between marketing, LeafyGreen, and documentation observations.
- Carry selector and surface provenance forward when using the component examples.

### Avoid

The source states these four as its Don't list. The fifth item is the source’s Agent Prompt Guide constraint that LeafyGreen or documentation work choose only the separately documented controls rather than importing marketing card geometry by default. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

- Do not substitute a system font for Euclid Circular A, MongoDB Value Serif, or Source Code Pro.
- Do not reclassify Forest Green (`#00684a`) announcement chrome as the canonical CTA fill.
- Do not infer interaction states, modal behavior, form errors, toasts, or component variants from this baseline-only bundle.
- Do not generalize public-site styles into an authenticated Atlas product interface.
- For LeafyGreen or documentation work, choose only the separately documented controls rather than importing marketing card geometry by default.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Role names below are the source's own labels. Pairing each hex to the token-set path named beside it, keeping `#00ed64` on the public primary-action fill and `#00684a` on announcement chrome rather than collapsing them, keeping `#001e2b` unmerged from LeafyGreen inverse canvas `#112733` and from ink `#000000`, keeping the captured white, gray, and local marketing-card surfaces as local observations rather than a complete application semantic palette, keeping `#3d4f58` muted-text and utility-control-background uses on those records, keeping `#00684a` announcement-strip and icon-control-text uses on those records, and keeping `#e7eeec` unmerged from `#e8edeb`, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation. The hex values and recorded uses are the source's own.

- **Spring Green** (`#00ed64`): public home and documentation header primary-action background. Token-set path `tokens.colors.primary`. Catalog `primary_color` is the same hex.
- **MongoDB Navy** (`#001e2b`): primary-action text and border; dark marketing-section foreground/background use. Token-set path `tokens.colors.navy`.
- **Forest Green chrome** (`#00684a`): home announcement-strip background; it is not promoted to the canonical CTA fill. Token-set path `tokens.colors.forest-chrome`.
- **Link Blue** (`#006cfa`): observed home text-link color. Token-set path `tokens.colors.link`.
- **Documentation canvas** (`#f9fbfa`): compact public documentation control background. Token-set path `tokens.colors.docs-canvas`.
- **Documentation border** (`#889397`): observed one-pixel compact-control border. Token-set path `tokens.colors.docs-border`.
- **Documentation muted text** (`#3d4f58`): documentation utility/control text. Token-set path `tokens.colors.docs-muted`.
- **LeafyGreen inverse canvas** (`#112733`): captured design-system search-field background. Token-set path `tokens.colors.docs-inverse`.

The captured white, gray, and local marketing-card surfaces remain local observations. They do not establish a complete application semantic palette.

- **Canvas** (`#ffffff`): YAML `tokens.colors.canvas`. Also the public marketing feature-panel background and the documentation icon-control background. Those component uses stay on those records.
- **Ink** (`#000000`): YAML `tokens.colors.ink`. Also the public marketing feature-panel text (`fg`). It is not MongoDB Navy `#001e2b`.
- **Feature border** (`#e7eeec`): YAML `tokens.colors.feature-border`. Public marketing feature-panel border. It is not the documentation icon-control border `#e8edeb`.

`#3d4f58` is documentation muted text and, separately, the LeafyGreen utility-control background. `#00684a` is announcement-strip background and, separately, documentation icon-control text. Those component fields stay on those records. They are not merged into one role.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept on their own path): `primary-action-y: 15` · `primary-action-x: 24` · `docs-side-y: 16` · `docs-side-x: 24`.

Measured spacing from source §5, kept on those records:

- Public primary action: `15px 24px` at `home::[data-omd-capture="11"]`.
- Larger public primary actions: `16px 32px` at home CTA captures.
- Documentation side-nav items: `16px 24px` on the captured public docs surface.
- LeafyGreen navigation list item: `8px 16px` on the design-system surface.

The evidence does not establish a full cross-product spacing scale. Use the recorded values only in their source-domain contexts. `primary-action-y: 15` is not the larger public primary-action `16px`. `primary-action-x: 24` is not the larger public primary-action `32px`. `docs-side-y: 16` is a documentation side-nav step. It is not `tokens.typography.primary-action.size` `16`, not `tokens.typography.primary-action.lineHeight` `16`, and not the larger public primary-action `16px`. `docs-side-x: 24` is a documentation side-nav step. It is not `tokens.spacing.primary-action-x` `24`. Keeping those writings on their own records rather than choosing one 16 or one 24 as a replacement, and reading the four measurements as source-domain values rather than a complete spacing scale, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path): `primary-action: 4` · `docs-control: 6` · `docs-icon: 9999`.

Shape boundary from source §5, kept on those records:

- Public primary action: 4px. Token-set key `tokens.rounded.primary-action`.
- Documentation compact control: 6px. Token-set key `tokens.rounded.docs-control`.
- Public marketing feature panel: 40px. YAML `tokens.components.marketing-feature-panel.radius` is `"40px"`. Not a `tokens.rounded` key.
- Documentation icon control: 100% circular. Token-set key `tokens.rounded.docs-icon` is `9999`. Both writings stay. Neither was chosen as a replacement.
- LeafyGreen utility control: 50px pill. Not a `tokens.rounded` key.

Keeping `4`, `6`, `40px`, `100%` / `9999`, and `50px` on those records rather than as a universal radius scale is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

### Elevation

Two shadows are directly observed: the public marketing feature panel uses `rgba(0, 0, 0, 0.1) 0px 2px 4px 0px`; the documentation icon control uses `rgba(0, 30, 43, 0.1) 0px 3px 4px 0px`. YAML `tokens.components.marketing-feature-panel.shadow` is `"rgba(0, 0, 0, 0.1) 0px 2px 4px 0px"`. No general elevation ladder is promoted from those local instances. Reading those two shadows as local instances rather than a depth system is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

### Motion

No duration, easing curve, or reduced-motion behavior was captured. No motion token is asserted.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or specification document, including the published LeafyGreen documentation — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation, and refusing a match against an official framework or vendor document as that gate, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Official product-use | LeafyGreen’s typography guidance says MongoDB Value Serif is used in the logo and primarily for H1/H2, Euclid Circular A is the frequently used product body font, and Source Code Pro displays code snippets and other values. |
| Live computed surface-use | The supplied capture reports all three families as loaded with high confidence: Euclid Circular A (287 visible uses), MongoDB Value Serif (5), and Source Code Pro (3). Euclid and Value Serif have MongoDB-hosted font sources; Source Code Pro is loaded from Google-hosted files. |
| Official licence context | Adobe licenses Source Code Pro under SIL Open Font License 1.1. That licence applies to Source Code Pro, not to MongoDB’s proprietary or brand-hosted font files. |
| Loaded but not promoted | Akzidenz-Grotesk Std has loaded visible use in the bundle but has no captured source URL or matching LeafyGreen product-role statement. It is retained as a live observation, not a machine UI-family token. |
| Declared-only / system / unresolved | icon fonts, Noto CJK fallback declarations, and Times/Arial system or unresolved fallbacks are not promoted. No substitute is rendered as a MongoDB font. |

Reading the LeafyGreen typography-guidance line as official product-use rather than proof that every MongoDB surface loads every role in the same CSS, reading the OFL 1.1 line as a Source Code Pro licence boundary rather than a MongoDB brand asset, reading Akzidenz-Grotesk Std as a live observation rather than a UI-family token, and reading icon fonts, Noto CJK, and Times/Arial as not promoted, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation. The observation inside each row stands on its own.

### Family

- **UI:** `Euclid Circular A`. Token-set path `tokens.typography.family.ui`.
- **Display:** `MongoDB Value Serif`. Token-set path `tokens.typography.family.display`.
- **Mono:** `Source Code Pro`. Token-set path `tokens.typography.family.mono`.

Do not substitute a system font for Euclid Circular A, MongoDB Value Serif, or Source Code Pro. Do not replace unavailable or unobserved brand type with a system stack, and do not render a substitute as a MongoDB font. The no-substitution rule, and the reading that the three families are canonical here because official LeafyGreen roles and loaded visible use agree, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

### Type roles

Token-set roles keep the source's recorded numbers. The measured public hierarchy table keeps the source's px spellings and the longer use / surface-boundary strings. YAML `lineHeight` `72` / `16` / `20` stay unitless on those keys; the table writes `72px` / `16px` / `20px`. YAML `tracking` `0.16` stays beside table `0.16px`. Neither writing was chosen as a replacement. Public section heading, public code/value label, and LeafyGreen page title are in the §3 table and are not YAML typography keys. Keeping the token-set numbers on their own rows, the px hierarchy on its own table, `tokens.typography.primary-action.size` `16` as a type key rather than `tokens.spacing.docs-side-y` `16` or the larger public primary-action `16px`, and the public code/value label ranges rather than a single sibling sample, is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

Token-set path `tokens.typography.hero`: size `64`, weight `400`, lineHeight `72`, use `Public home marketing H1`.
Token-set path `tokens.typography.primary-action`: size `16`, weight `500`, lineHeight `16`, tracking `0.16`, use `Public home and documentation primary action`.
Token-set path `tokens.typography.docs-control`: size `13`, weight `500`, lineHeight `20`, use `Small documentation control`.

| Role | Font | Size | Weight | Line height | Tracking | Surface boundary |
|---|---|---:|---:|---:|---:|---|
| Public home hero | MongoDB Value Serif | 64px | 400 | 72px | normal | home marketing H1 |
| Public primary action | Euclid Circular A | 16px | 500 | 16px | 0.16px | home and documentation header |
| Public section heading | Euclid Circular A | 36px | 500 | 48px | normal | home marketing |
| Public code/value label | Source Code Pro | 14px | 400–500 | 16–24px | 1–2px | home marketing |
| LeafyGreen page title | MongoDB Value Serif | 48px | 400 | 64px | normal | design-system page |
| Documentation compact control | Euclid Circular A | 13px | 500 | 20px | normal | public docs |

The public code/value label keeps the source table’s ranges `400–500` / `16–24px` / `1–2px`. A sibling sample of one home Source Code Pro label is not a replacement for those ranges.

### Assets

The catalog identity records `logo.type: simpleicons` and `logo.slug: mongodb`. That is a third-party icon-set rendering, not a MongoDB-distributed logo asset. Classifying the simpleicons slug as an identity pointer rather than a hosted brand file is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

All variants below are baseline observations from the supplied 2026-07-13 collector bundle. The bundle records `interactionCount: 0`; therefore it does not establish hover, pressed, focus, menu, modal, toast, form-error, or transition variants.

No empty, loading, success, error, disabled, skeleton, or interactive transition treatment was captured. These states are intentionally omitted rather than reconstructed from public baseline screenshots.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted. Absence of a capture is not a `not-applicable` reason. `not-applicable` is used only where the control's role makes the state meaningless — a destination header action, a role-button feature panel, or documentation / LeafyGreen chrome that does not report commit progress — and the reason given is always that semantic one. Generic `Focus` capture is not treated as a `focus-visible` treatment. This is not a complete state-coverage claim.

The role-based decision procedure above, every interactive-kind verdict, every applicability verdict and the reason given for either, and attaching YAML `type: card` only to Public marketing feature panel, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

The source token-set declares one component: `marketing-feature-panel` with `type: card`. That primitive type is attached only to Public marketing feature panel. The other §4 records are not in the token set.

### Public primary action

- Role: public marketing and documentation header action
- Primitive type: not in the token set · Kind: interactive
- Domain: home marketing and documentation header (`surface-3`)
- Background: `#00ed64`
- Text: `#001e2b`
- Border: `1px solid #001e2b`
- Radius: `4px`
- Padding: `15px 24px`
- Font: `16px / 500 / Euclid Circular A`
- Use: public marketing and documentation header action; `home::[data-omd-capture="11"]`, also observed on `surface-3`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web header action; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable action; visual treatment omitted |
| disabled | applicable | A destination action whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a public header destination action; it does not commit an in-page operation whose in-progress state it could report |
| error | not-applicable | Destination action; the destination, not this control, reports failure |
| success | not-applicable | Same role reason: reaching the destination is not an operation this control reports as success |

### Public marketing feature panel

- Role: home role-button feature panel
- Primitive type: `card` · Kind: interactive
- Domain: home marketing
- Background: `#ffffff`
- Text: `#000000`
- Border: `1px solid #e7eeec`
- Radius: `40px`
- Padding: `40px 48px`
- Shadow: `rgba(0, 0, 0, 0.1) 0px 2px 4px 0px`
- Token-set path: `tokens.components.marketing-feature-panel` (`type: card`, `bg: #ffffff`, `fg: #000000`, `border: 1px solid #e7eeec`, `radius: 40px`, `padding: 40px 48px`, `shadow: rgba(0, 0, 0, 0.1) 0px 2px 4px 0px`, `use: Observed public-home feature panel`)
- Use: home role-button feature panel; `home::[data-omd-capture="15"]`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web role-button panel; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable role-button; visual treatment omitted |
| disabled | applicable | A destination panel whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a home role-button feature panel; it does not commit an in-page operation whose in-progress state it could report |
| error | not-applicable | Destination panel; the destination, not this panel, reports failure |
| success | not-applicable | Same role reason: remaining on the panel is not an operation this control reports as success |

### Documentation compact control

- Role: compact public documentation control
- Primitive type: not in the token set · Kind: interactive
- Domain: public docs (`surface-3`)
- Background: `#f9fbfa`
- Text: `#001e2b`
- Border: `1px solid #889397`
- Radius: `6px`
- Font: `13px / 500 / Euclid Circular A`
- Use: compact public documentation control; `surface-3::[data-omd-capture="21"]`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web documentation chrome; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A chrome control whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is compact documentation chrome; it does not commit an in-page operation whose in-progress state it could report |
| error | not-applicable | Documentation chrome; it does not report a commit failure on this control |
| success | not-applicable | Same role reason: using this chrome control is not an operation it reports as success |

### Documentation icon control

- Role: public documentation icon control
- Primitive type: not in the token set · Kind: interactive
- Domain: public docs (`surface-3`)
- Background: `#ffffff`
- Text: `#00684a`
- Border: `1px solid #e8edeb`
- Radius: `100%`
- Padding: `1px 6px`
- Shadow: `rgba(0, 30, 43, 0.1) 0px 3px 4px 0px`
- Use: public documentation icon control; `surface-3::[data-omd-capture="22"]`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web icon control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable icon control; visual treatment omitted |
| disabled | applicable | An icon control whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a documentation icon trigger; it does not commit an in-page operation whose in-progress state it could report |
| error | not-applicable | Icon chrome; it does not report a commit failure on this control |
| success | not-applicable | Same role reason: using this icon control is not an operation it reports as success |

### LeafyGreen utility control

- Role: LeafyGreen design-system utility control
- Primitive type: not in the token set · Kind: interactive
- Domain: LeafyGreen design-system page (`surface-2`)
- Background: `#3d4f58`
- Text: `#ffffff`
- Border: `1px solid #889397`
- Radius: `50px`
- Font: `13px / 500 / Euclid Circular A`
- Use: LeafyGreen design-system utility control; `surface-2::[data-omd-capture="48"]`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web utility control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable utility control; visual treatment omitted |
| disabled | applicable | A utility control whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is LeafyGreen design-system utility chrome; it does not commit an in-page operation whose in-progress state it could report |
| error | not-applicable | Utility chrome; it does not report a commit failure on this control |
| success | not-applicable | Same role reason: using this utility control is not an operation it reports as success |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The supplied evidence is a 1440×900 capture. It establishes no responsive breakpoint, mobile layout, touch-target, or collapse behavior claim.

Measured spacing, used only in their source-domain contexts:

- Public primary action: `15px 24px` at `home::[data-omd-capture="11"]`.
- Larger public primary actions: `16px 32px` at home CTA captures.
- Documentation side-nav items: `16px 24px` on the captured public docs surface.
- LeafyGreen navigation list item: `8px 16px` on the design-system surface.

The 64px public home hero, 16px public primary action, 13px documentation compact control, 4px primary-action radius, 6px docs-control radius, 40px feature-panel radius, 100% icon control, and 50px LeafyGreen utility pill are capture measurements, not cross-viewport specifications. Reading those figures as capture measurements rather than cross-viewport specifications is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

<!-- design-md:section content-locales -->
## 6. Content & Locales

MongoDB’s first-party company story is developer-centred, practical, and ambitious: make a flexible data platform that helps developers build and scale. Its public writing pairs direct action language with concrete technical outcomes rather than generic database superlatives. The mission, founder-story, and CTA rows below, and the three voice samples, are the source's own. Calling that first-party story developer-centred, practical, and ambitious, calling the public writing a pairing of direct action language with concrete technical outcomes, and asking that the published strings be reproduced byte-exact, are a derived editorial implementation inference from the verified surfaces; they are not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

| Context | Observed or official direction |
|---|---|
| Product mission | Empower innovators by unleashing the power of software and data. |
| Founder story | Frame scale, flexibility, and developer productivity as the problem to solve. |
| CTA | Use concise actions such as “Get Started” when supported by the captured public header. |

**Voice samples**
- “Get Started” — captured public primary action.
- “Our journey” — official company-history framing.
- “The needs of developers have always served as the company’s North Star.” — official story page.

Reproduce those strings byte-exact rather than translating or re-casing them.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These are unnamed values, not permissions to invent. Naming the list from the source's own unresolved fields, rather than adding surfaces the source did not name, is a derived editorial implementation inference from the verified surfaces; it is not MongoDB-authored or taken from a separately published UI specification, including the published LeafyGreen documentation.

- **Hover, pressed, focus, menu, modal, toast, form-error, and transition variants.** The bundle records `interactionCount: 0`. They are not `not-applicable`; applicability follows control meaning.
- **Empty, loading, success, error, disabled, skeleton, and interactive transition treatments.** Source §14 leaves those visual treatments unspecified. They are intentionally omitted rather than reconstructed from public baseline screenshots.
- **Authenticated Atlas product interface.** No authenticated product screen was captured. Public-site styles are not a substitute.
- **Responsive breakpoint, mobile layout, touch-target, and collapse behavior.** The supplied evidence is a 1440×900 capture and establishes no such claim.
- **A complete application semantic palette.** The captured white, gray, and local marketing-card surfaces remain local observations.
- **A full cross-product spacing scale.** Use the recorded values only in their source-domain contexts.
- **A general elevation ladder.** Two local shadows only.
- **Motion duration, easing curve, and reduced-motion behavior.** No motion token is asserted. Promote a curve only after a component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed.
- **Akzidenz-Grotesk Std as a machine UI-family token.** Loaded visible use; no captured source URL or matching LeafyGreen product-role statement.
- **Icon fonts, Noto CJK fallback declarations, and Times/Arial as MongoDB fonts.** Not promoted. No substitute is rendered as a MongoDB font.
