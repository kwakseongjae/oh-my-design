# paypay migration log

Source: `web/references/paypay/DESIGN.md`
Sibling read (not the migration input): `web/references/paypay/.verification.md`
Destination: `docs/design-md-weight/migrated/paypay/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/paypay/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use ripgrep match lists per file, never a remembered count.

Source SHA-256 `54cb4080b80be786821a1bbbe49222161489af25a3467df86aa266d41c051424` (`web/references/paypay/DESIGN.md`). Sibling SHA-256 `10becbf280673e524599c655cf4fc8bec3075a893a5f4d613fb40bd5290aa915` (`web/references/paypay/.verification.md`). Worker-close portable DESIGN SHA-256 `8b289fc0f7fc95522b5a0421aea5af9ec8395622015f8e08e8e0e3cfa76af55a`. F3-audit portable DESIGN SHA-256 `7761a30e9c3f1c2c60ff9487dfc2235fb65dae2181a941690c39a84d32b6ca9d`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope + Components Tier 1; `primary_color` 옮김 → Scope + Foundations; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# PayPay Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–22. YAML homepage `https://paypay.ne.jp` is dual: DESIGN dest **3** at `DESIGN.md` 9 (×2) / 717 + provenance dest **7** at `provenance.md` 13/40/47/51/55/73/80 (E2a). `provenance.md` 24 names the dual without repeating the URL. `#FF0033` is dual: `DESIGN.md` dest many (Scope / Foundations / components) + `provenance.md` 14 (E2a). Favicon slug is dual: DESIGN dest **1** at `DESIGN.md` 253 + provenance dest **1** at `provenance.md` 16 (E2a). `provenance.md` 24 names the slug as dual without repeating the URL. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance; `prose-derived` 옮김 → Experience Scope + Motion + Typography Font evidence | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 18–22, 31–35. `prose-derived` is dual: DESIGN dest **3** at `DESIGN.md` 9/167/208 + provenance dest **6** at `provenance.md` 20/64/102/171/178 (E2a; line 64 carries two occurrences). `components_harvested` DESIGN 0 / provenance dest **2** at `provenance.md` 22/172. These ledger keys have no other portable slot. |
| YAML `tokens.colors` (19 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 88–126. All nineteen roles kept with names, values, and token-set keys: primary `#ff0033` / `#FF0033`, primary-pressed `#e0002e` / `#E0002E`, primary-deep `#cc0029` / `#CC0029`, primary-tint `#ffebef` / `#FFEBEF`, primary-disabled `#ffb3c1`, canvas `#ffffff`, ink `#222222`, success `#00b900` / `#00B900`, error `#e0002e` / `#E0002E`, warning `#ff8800` / `#FF8800`, info `#0088ff` / `#0088FF`, point-gold `#ffb200` / `#FFB200`, gray-50 `#f5f5f5`, gray-100 `#eeeeee`, gray-200 `#e0e0e0`, gray-300 `#cccccc`, gray-400 `#999999`, gray-500 `#767676`, gray-700 `#555555`. Pressed and error share `#e0002e` and stay two roles (`DESIGN.md` 86/91/106/887). Ink and Gray 900 share `#222222`; Gray 900 is not a YAML key (`DESIGN.md` 86/99/120/888). |
| YAML `tokens.typography.family.sans` / `family.mono` | 옮김 → Typography & Assets Family | Family `Noto Sans JP` `DESIGN.md` **216**. Mono `SF Mono` **217**. §3 full stack including `Hiragino Kaku Gothic ProN` / `Yu Gothic` / `YuGothic` / `Meiryo` at **218**. Mono stack `Menlo, Consolas` at **219**. |
| YAML `tokens.typography.display-hero` / `balance` / `display-lg` / `heading-lg` / `heading` / `subtitle` / `body-lg` / `body` / `body-sm` / `caption` / `amount-hero` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 229–239. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.38` / `1.29` / `1.42` / `1.4` / `1.44` / `1.5` / `1.63` / `1.57` / `1.54`. Amount Hero has no YAML `lineHeight`; the cell records YAML none beside §3 `tight` (`DESIGN.md` 239/889). All eleven YAML `use` strings restored verbatim in the Token-set use column (A1). §3 px spellings stay beside the YAML sizes (`44px` / `36px` / `34px` / `28px` / `26px` / `24px` / `22px` / `20px` / `18px`). §3 Amount Hero `36px+` stays beside YAML `36` (`DESIGN.md` 241). |
| YAML `tokens.spacing` (3 steps) / `tokens.rounded` (5 steps) / `tokens.shadow.card` / `toast` / `dialog` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless spacing: `DESIGN.md` 130 (`sm 8 · base 16 · lg 24`). Shape: `DESIGN.md` 134 (`sm 8 · md 12 · lg 16 · xl 24 · full 9999`). `tokens.rounded.full: 9999` dest 2 at `DESIGN.md` 144/886 (`tokens.rounded.full` dest 3 at 142/144/886). `full: 9999` restated in Layout at 743. `tokens.spacing.sm: 8` is disambiguated from `tokens.rounded.sm: 8` at 130. `tokens.spacing.base: 16` is disambiguated from `tokens.rounded.lg: 16` at 130/144. `tokens.spacing.lg: 24` is disambiguated from `tokens.rounded.xl: 24` and from padding `0 24px` at 130. YAML shadows at `DESIGN.md` 151 (`tokens.shadow.card`) / 154 (`toast`) / 155 (`dialog`). §6-only Level 2 `0px 2px 8px rgba(0,0,0,0.12)` and Level 2r `0px 2px 8px rgba(255,0,51,0.20)` stay on their own rows (152–153). Extra local shadows (bottom-sheet, toggle thumb, segmented thumb) at 157. |
| YAML `tokens.components` (20 records) | 옮김 → Components & States | `DESIGN.md` 276–703. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×4, `input` ×2, `card` ×3, `listItem` ×1, `badge` ×4, `tab` ×2, `toast` ×1, `dialog` ×2, `toggle` ×1. All twenty YAML `use` strings restored verbatim as `Token-set use:` lines. Campaign / Promo YAML use `Point campaigns, あげる/もらえる offers` DESIGN dest **1** at Token-set use 506; §4 longer Use suffix `bright, image-forward` DESIGN dest **1** at Role 500 (keep-both; restored in Revision 2026-09-02). §4-only Amount (Hero), Error (input), PIN/OTP, Weak / Gray badge, and Payment-Complete Screen are labeled `not in the token set` (`DESIGN.md` dest 7). No §4-only component borrowed a `Primitive type`. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 35–41. Original sentence `That red is the brand; remove it and nothing is left` is at `DESIGN.md` 11. The atmosphere readings carry an adjacent complete qualifier at 11 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 88–126. Characterizing phrases and keep-boths covered by the adjacent qualifier at 86 (B2/B2a). White Wordmark / no third logo color at 100 and 254. Overlay Scrim `rgba(0,0,0,0.5)` at 126. |
| §3 Typography Rules — family, scale, principles | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 207–212; family 216–221; hierarchy table 229–239; type principles 245–249. Qualifiers at 207/208/209/210/211/212/221/225/243 (B2/B2a). The live-computed row's adjacent complete qualifier is at 208 (not provenance-only). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 276–716. Reconstruction note (no public DS; representative rather than spec-exact) at 270. §4 body values and YAML `use` / font / radius / padding forms are both kept; the longer §4 form is expanded (wave 37). Campaign / Promo Role 500 carries the source §4 suffix `bright, image-forward` beside YAML Token-set use 506 (restored in Revision 2026-09-02; YAML short dest **1** unchanged). Unique §4-only values: Amount Hero `¥` prefix 24px / 700; PIN 4-digit randomized keypad; OTP 6 boxes 48px × 56px; Weak / Gray badge; Payment-Complete Screen full recipe; drag handle 36px × 4px; toggle thumb 22px; button size scale 36/44/52; segmented track/thumb 10/8. |
| §4 Tier 1 URL | 옮김 → Experience Scope + Components close; 분리 → provenance | `https://paypay.ne.jp` dual: DESIGN dest **3** at `DESIGN.md` 9 (×2) / 717 + provenance dest **7** at `provenance.md` 13/40/47/51/55/73/80 (E2a). Same URL as the YAML homepage row; both destinations named. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 743–751: 375px baseline, single-column, ~960px marketing, 4-per-row icon grid, 8px base (4px tight), common 4/8/12/16/20/24/32/40, 16px screen margin. Whitespace philosophy at 747–749 under the qualifier at 751. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 148–163: six-level table (150–155), red-tinted lift, blur/scrim. Extra local shadows (bottom-sheet, toggle thumb, segmented thumb) at 157. Qualifier at 163. |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 60–66 (seven Do rules), under the grouping qualifier at 58. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 72–78, under the qualifier at 70. The seven prohibitions are the source's own list. No invented domain is added (D1). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 755–778: Mobile <600 / Tablet 600–960 / Desktop >960, touch targets 52 / 60–64 / 48 / 56–64, collapsing strategy, image behavior. The system-level-not-cross-viewport reading carries an adjacent qualifier at 761 (B2/B2a). |
| §9 Agent Prompt Guide | 삭제; §9-only 값 옮김 → Balance Card + Compact (List Item) + Bottom Tab / Layout | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion. Three §9-only writings moved: PayPay残高 13px / 400 / white 80% opacity → `DESIGN.md` 492; transaction-row 36px circle merchant icon + 48px min-height anatomy → `DESIGN.md` 519; center pay button `60px` → `DESIGN.md` 598/766 (kept beside §8 `60–64px`, not merged; also named at 604/761). The check is itemised at `provenance.md` 123. |
| §10 Voice & Tone | 옮김 → Content & Locales | Tone table `DESIGN.md` 786–793; forbidden moves 795; published strings 799–847. Qualifiers at 782 / 795 / 849 (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: October 2018, SoftBank, Yahoo! Japan, Paytm, `100億円あげちゃうキャンペーン`, ¥10-billion giveaway, convenience stores / restaurants / tiny neighborhood shops, make paying feel like winning, Toss (Korea) / Stripe, reward and delight, LY Corporation, LINE × Yahoo Japan, PayPayポイント / PayPay銀行 / PayPayカード / PayPay証券, in-store QR tap, austere navy seriousness, muted minimalism of Western fintech, closer to retail than to banking, by deliberate design. Each source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 175. |
| §12 Principles — 8 numbered | 옮김 → Experience principles | `DESIGN.md` 47–54 under the B2a form at 45: "These 8 items are a derived editorial implementation inference from the verified surfaces; they are not PayPay-authored or a separately published UI specification." No separately published PayPay UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 129–167 (39 data rows). HTML-comment interpretive quote `paying should feel like winning` is at `DESIGN.md` 48 beside the §12 stem. |
| §13 Personas — 3 entries | 삭제 | The source's own header labels them fictional archetypes informed by publicly described Japanese mobile-payment user segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, motivation, or affiliation classification appears in either output (D2, D2a). Disposition at `provenance.md` 116. Audience at `DESIGN.md` 29 carries only the group-level description the source independently records (`publicly described Japanese mobile-payment user segments`), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels and surfaces the source records (支払う / チャージ / 送る / PayPay残高); 19 says so and qualifies the step from label to "primary task". |
| §14 States — 12 rows | 옮김 → Components & States surface state contract + per-component applicability | Full twelve-row body preserved at `DESIGN.md` 725–736 (A2; the catalog graph is still 0/440, so nothing is delegated), including `まだ取引履歴がありません`, `条件に合う結果がありません`, `¥--`, `残高が不足しています`, `閉じる`, `#FFB3C1`, 1.2s shimmer, 300ms fade, ~3s auto-dismiss, no double-submit, never a fake number, never skeleton bars. The applicability rule is at 266–272, whose qualifier at 272 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary money action opens loading (spinner recorded) and closes error/success with a role reason — those results live on the field/toast/screen / Payment-Complete Screen (`DESIGN.md` 304–306) (C2 v10). Secondary / Neutral / Text Button / Bottom Tab / Segmented / Toggle close loading/error/success with a role reason. Inputs open error. Cards, dialogs, and the Payment-Complete Screen get no `kind` and no map where the source supplies no interaction evidence for the container (`DESIGN.md` 480/496/508/521/669/683/715) (C4). Badges and Toast declare `Kind: non-interactive`. |
| §15 Motion & Easing — durations, easing roles, signature motions, reduced-motion | 옮김 → Foundations motion | `DESIGN.md` 171–176 (0 / 150 / 250 / 400 / 700 / 300ms with uses), 181–184 (ease-enter / exit / standard / bounce roles and uses, curves omitted), 193–197 (five signature motions including y+40px sheet, 0.94 press-scale, never cross-fade money, static red success screen under reduced motion). |
| §15 Motion & Easing — four curve values | 삭제 → provenance omission ledger | The four value strings are DESIGN dest **0**. Omission is named at `DESIGN.md` 167 (`cubic-bezier` dest **1** in that file); 184 and 885 name the omission without repeating the bezier strings. Values live in the omission ledger at `provenance.md` 117–120 (dest 1 each). Durations stay. B3 is held: the promotion condition at `DESIGN.md` 189 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The same five kinds are restated at `DESIGN.md` 885. |
| HTML comment (sources, reconstruction bound, interpretive-claim note, persona label) | 분리 → provenance; 재건 한계·해석 한정은 본문에 유지 | WebFetch 2026-06-06 and Wikipedia pointer at `provenance.md` 55–56. Reconstruction bound (no public DS; representative geometry) stays in portable Scope 9 and Components 270 (standalone authority). Interpretive-claim example `paying should feel like winning` at `DESIGN.md` 48. Persona fictional-archetype label used only as the deletion reason, not as a re-host (`provenance.md` 116). |
| Opening product / atmosphere sentence | 옮김 → Experience Scope | `DESIGN.md` 9–11: Japan's dominant QR-code mobile wallet; `#FF0033`; Hiragino / Noto Sans / Yu Gothic; 375px; 「あたり」. |

## Sibling handling (`web/references/paypay/.verification.md`)

The sibling exists — confirmed with `find web/references/paypay -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 72–83 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: body text `#242323`; root background `#000000`; heading `55px`; primary-button / link fill `#3895ff`; primary-button radius `6px`; primary-button height `58px`; method string `playwright getComputedStyle`. Recorded at `provenance.md` 86–92.
- Measured `DESIGN.md` 0 for those sibling-only strings: `#242323` 0 · `#3895ff` 0 · `55px` 0 · `playwright getComputedStyle` 0. (`#000000` DESIGN 0; the portable Don't uses source `#000`. Standalone sibling-radius `6px` DESIGN dest **0** — `grep -oF 6px` only hits inside `16px`/`26px`/`36px`/`56px`, not a button-radius claim. `58px` DESIGN 0.)
- `#ffffff` / Noto Sans JP / Hiragino Kaku Gothic ProN / 16px / 700 are already in the source body and are corroboration.
- Conflict kept unresolved in provenance: source-body CTA red `#FF0033` vs sibling live-DOM button fill `#3895ff`. The portable body does not promote the sibling measurement (D1: no new negative claim about the sibling fill is written in DESIGN.md).

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. A hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 38 distinct | 0 | 0 | ペイペイ / 支払う / チャージ / チャージする / 送る / 閉じる / 戻る / もっと見る / 詳細 / キャンセル / 後で / NEW / 限定 / 完了 / コピーしました / あたり / あげる / もらえる / お支払いが完了しました / 残高が不足しています。チャージしてください / スマホひとつで かんたんお支払い / スマホひとつで かんたんに お支払いはPayPayで / 登録は無料！最短1分 / いますぐPayPayアプリをダウンロード / 今すぐダウンロード / 100ポイントもらえる！ / おトク / まだ取引履歴がありません / 条件に合う結果がありません / 100億円あげちゃうキャンペーン / PayPayポイント / PayPay銀行 / PayPayカード / PayPay証券 / PayPay残高 / 通知 / 自動チャージ / +◯◯ポイント. |
| `node test-v2/tools/latin-copy-audit.mjs --brand paypay --candidate docs/design-md-weight/migrated/paypay/DESIGN.md` | 2 lost / 1 brand scanned | 1 remaining after restore | 0 | Restored `paying should feel like winning` (source HTML-comment interpretive quote) at `DESIGN.md` 48 beside the §12 stem. Remaining lost=`this works.` (persona quoted shorthand; not published product copy; deleted with §13 under D2, not restored). |
| Sibling published strings | 0 distinct sibling-only product copy | 0 | 0 | Sibling is a live-DOM measurement note; no additional published product labels. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 支払う, チャージ, 送る, 閉じる, 100億円あげちゃうキャンペーン, お支払いが完了しました, PayPay残高, October 2018, SoftBank, LY Corporation.

A5 분모: hand sweep of source published labels 38 extracted / 0 missing; latin-copy-audit 2 lost / 1 scanned (0 published remaining); gate `copy-loss` compared 30 / candidates 285.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand paypay --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 30, candidates: 285, detail: "인용 문자열 285개 중 30개만 비교했다 — 나머지는 라틴이라 바늘이 되지 않는다. 라틴 전수 대조는 손으로 하라." }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `format: core-v2`, `structurally_valid: true`, `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 30 < `candidates` 285. First gate run blocked on `token-loss: px:60px` — source §9 writes the center pay button as `60px` and §8 writes `60–64px`; the portable body had kept only the en-dash band. Restored `60px` at `DESIGN.md` 598/766 beside `60–64px` without converting either. Then `verdict: PASS`.

## Deviations recorded

- `DESIGN.md` is 9,946 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: twenty declared component records plus five §4-only surfaces, a twelve-row surface state contract, a seven-state applicability matrix on the interactive controls, `tokens.rounded.full: 9999` kept off matching spacing keys and off the 10px badge, YAML/§4 keep-boths (primary radius 24 vs 12 inline, balance fill vs gradient, tab inactive pair, segmented track/thumb, bottom-sheet top-only radius), the full §11 founding-and-campaign narrative including each paragraph's last sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published PayPay UI specification is named in the source, so every derived-editorial close uses the toss-form `not PayPay-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). F3-audit measure: `derived editorial implementation inference` DESIGN dest **39** = `not PayPay-authored` DESIGN dest **39**. Provenance derived ledger **39** rows at `provenance.md` 129–167 (E1 1:1). Confirmed with `node scripts/check-limiter-ledger.mjs paypay` → `1:1 OK`. Worker-close had been 38=38; the live-computed row at `DESIGN.md` 208 was provenance-only until F3.

## F1 B2a rescan (mandatory final pass)

Re-read `DESIGN.md` from the title through Recorded unresolved. Every causal, interpretive, or classificatory sentence was asked whether it is a brand-issued fact or a derived reading. Derived readings already carried an adjacent complete qualifier, or received one at 9 / 11 / 13 / 19 / 29 / 33 / 45 / 58 / 70 / 86 / 130 / 144 / 163 / 167 / 207 / 208 / 209 / 210 / 211 / 212 / 221 / 225 / 243 / 259 / 272 / 274 / 296 / 496 / 604 / 630 / 683 / 715 / 738 / 751 / 761 / 782 / 795 / 849 / 883 (39). No qualifier was weakened to "derived from verified surfaces" without the authored-or-specification close. F3 added the live-computed close at 208 (B2: that reading had lived only in `provenance.md` 64).

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep` confirmed the cited line in `DESIGN.md` and/or `provenance.md`. Dual destinations name both files. The B3 claim is made only because `DESIGN.md` 189 and 885 contain the five evidence kinds in full text. The "B2a 39=39" claim is made only because both the portable closes and the provenance inventory are 39 rows (`check-limiter-ledger.mjs` 1:1 OK, range 129–167). F3 re-grepped every destination in this log after the 208 close.

## Unique-phrase restore (wave 43 self-check)

Pulled 138 source-unique expressions (years, proper names, quoted copy, §11 causal last-sentences, value modifiers, §15/§5/§7 constraints, §9-only `60px`). `grep -oF` against `DESIGN.md`: 3 were 0 on first pass — (1) `That red is the brand; remove it and nothing is left` (source §1; restored at `DESIGN.md` 11); (2) standalone `60px` from the §9 center-pay prompt (restored at `DESIGN.md` 598/766 beside §8 `60–64px`); (3) `auto-charge` / `components_harvested` were not restored into the portable body (`auto-charge` is the English gloss inside a deleted persona; `自動チャージ` already lives on the Toggle use; `components_harvested` is ledger metadata in provenance). Result: extracted 138 / restored 2.

## Revision 2026-09-02 (wave45 review)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**. Trigger: semantic review FAIL 1. One defect only. Token values, component-table structure, state applicability, B2a complete qualifiers, and the derived-inventory 1:1 were not opened. Source `web/references/paypay/DESIGN.md` was not modified. Sibling `web/references/paypay/.verification.md` exists (`find` + `test -f`). Provenance inventory 39 rows unchanged. In-place Role restore: DESIGN.md line count 890 unchanged.

Counts below are `grep -oF -e '<needle>' <file> | wc -l`, file by file. `grep -c` was not used.

**1. A1 · item 11 — Campaign / Promo §4 Use 복원.** Source §4 `:289` writes Campaign / Promo Use as `Point campaigns, あげる/もらえる offers — bright, image-forward`. YAML `:65` is the shorter `Point campaigns, あげる/もらえる offers`. Portable Campaign / Promo kept only the YAML string as Role (`DESIGN.md` 500) and Token-set use (506). Other YAML controls that differ in length already keep the longer §4 writing in Role or Use (Balance Card Role `wallet balance hero on home`; Compact Role `transaction history rows, settings list rows`; Bottom Tab Use `home, points, QR-pay (center), promotions, account`). Restored the source §4 suffix on Role at `DESIGN.md` 500: `point campaigns, あげる/もらえる offers — bright, image-forward`. YAML Token-set use kept verbatim. No new sentence class, no new B2a qualifier, no new provenance inventory row — source-value restore into the existing Role slot, not a derived reading.

실측 (`find` 후 `grep -oF -e` | `wc -l`, 파일별):

| needle | orig | sib | dest after | provenance after |
|---|---|---|---|---|
| `bright, image-forward` | 1 | 0 | **1** | 0 |
| `image-forward` | 1 | 0 | **1** | 0 |
| `Point campaigns, あげる/もらえる offers` | 2 | 0 | **1** | 0 |
| `point campaigns, あげる/もらえる offers` | 0 | 0 | **1** | 0 |

`bright, image-forward` dest **1** = Campaign / Promo Role `:500`. orig **1** = source §4 `:289`. YAML `:65` does not contain `image-forward`. Token-set use dest **1** unchanged (`Point campaigns, あげる/もらえる offers` at 506). Role dest **1** is the lowercase Role line, now a prefix of the restored longer writing. provenance **0** — the claim ledger already names `tokens.components.promo-card` as a class and does not need a second body fact. This revision section and the updated YAML-components / §4 dest rows name the same needles; those mentions are this file's denominator (E2d).

`wc -w` DESIGN.md **9,946** (was 9,943). B2a `derived editorial implementation inference` dest **39** = provenance inventory **39** data rows (129–167). Current portable DESIGN SHA-256 `ce427b53e4926ee84bc4efb842b13343171314669364a0a989ed65489389c2b9`. Provenance SHA-256 unchanged `2b419a4e4013da43986375b57d788e10f39a02c14ddfed9925f7789d87e00964`. Auditor SHA `7761a30e9c3f1c2c60ff9487dfc2235fb65dae2181a941690c39a84d32b6ca9d` remains the F3 snapshot.

**안 건드린 것.** 토큰 값 · 컴포넌트 상태 표 · B2a 완전형 39=원장 39 · 원본 `web/references/paypay/**` · sibling 승격 없음.
