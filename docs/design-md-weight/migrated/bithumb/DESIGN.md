# Bithumb Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Bithumb is a Korean crypto-asset exchange. Catalog homepage identity is `https://www.bithumb.com`.

Treating the following four URLs as the named evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. This contract covers the live primary site named at `https://www.bithumb.com`. The source footer also names `https://apidocs.bithumb.com` (official API docs), `https://www.bithumbcorp.com` (corporate site), and `https://github.com/bithumb` (official GitHub org) as brand-owned Tier 1 sources.

Source token note: primary `#1C2028` is the measured dominant structural near-black (matches catalog `primary_color`); bronze `#543E35` is the rebrand CTA accent, not the structural primary. Trading red/blue follow Korea convention (red=up, blue=down). Treating that note as a register split — `#1C2028` is catalog `primary_color` and the structural near-black of signup and active chips, `#543E35` is the filled exchange-CTA accent rather than a second structural primary, and `#E15241` / `#4880EE` are directional trading signal rather than structure — is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

The following atmosphere readings — money taken seriously; dark, premium, data-dense trading surface; trust through density rather than decoration; packed grids, tight controls, and micro-labels that never waste a pixel; restrained structural palette that lets trading colors do the emotional work; proprietary typeface as a money-grade voice; less a consumer app than a professional terminal; composed, serious, engineered for people moving real value; bronze against near-black as the whole mood; warmth held in tension with a structural, almost industrial calm — are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification. The source describes a dark, data-dense trading surface on a near-black structural base of `#1C2028`, warmed in chosen places by a bronze/brown accent (`#543E35`) that it ties to a recent rebrand. The source calls Bithumb Korea's No.1 crypto-asset exchange. Trading red (`#E15241`) is price up and trading blue (`#4880EE`) is price down, following the Korea-market convention rather than the Western one. The typeface named in the packet is proprietary **Bithumb Trading Sans**.

The following brand-narrative readings — an institution maturing into a premium, professional identity; warmth introduced into an austere, money-grade environment; the proprietary face making the brand speak in its own voice; Korea-convention trading colors rooting the brand in its home market; the throughline of seriousness about money expressed as density, restraint, and a single warm accent — are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification. The source pairs a near-black structural base with the bronze accent `#543E35` and names Bithumb Trading Sans as a brand-owned face.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings below as Primary tasks, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

- Sign up via the structural top-chrome action (YAML use: `Signup — structural top-chrome action`).
- Take the primary exchange call-to-action (YAML use: `Primary exchange CTA — single warmest action`).
- Select a filter in the chip row (YAML use: `Selected filter chip` / `Unselected filter chip`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes, not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

### Distinctive traits

The following unmerged-role extras — treating `#1C2028` as structure rather than bronze, treating `#543E35` as the scarce CTA accent rather than a second primary, treating `#E15241` / `#4880EE` as directional trading signal rather than structure, treating outline `#4F3327` / `#B6ABA1` as unmerged from the bronze fill, treating `#F8F9FA` as bronze-CTA text rather than on-primary `#FFFFFF`, treating `#707882` and `#93989E` as unmerged muted roles, treating YAML `family.sans` and `family.mono` as the same name rather than a second harvested face, treating 32px / 36px / 56px as component heights rather than extra tokens, treating 4px and 8px as local harvested geometry rather than one radius for every unlisted control, treating YAML `rounded.full` 9999 as an unused YAML step rather than a harvested pill, and treating “tight type scale” and “flat depth” as source characterizations of the recorded size/weight and shadow.flat values rather than extra tokens — are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification.

- Structural near-black `#1C2028` as catalog `primary_color`, YAML `primary` / `canvas`, signup fill, and active-chip fill
- Bronze `#543E35` as YAML `brand` and the filled exchange-CTA accent, kept as the single warmest action
- Outline CTA text `#4F3327` and outline border `#B6ABA1`, unmerged from the bronze fill
- Trading red `#E15241` (price up) and trading blue `#4880EE` (price down), Korea convention
- Off-white `#F8F9FA` on the bronze CTA; pure white `#FFFFFF` on the near-black signup and active chips
- Muted `#707882` on inactive chips; muted-2 `#93989E` as secondary/tertiary muted text
- Proprietary **Bithumb Trading Sans**; YAML `family.sans` and `family.mono` both name this face
- Tight type scale: body 14px / 400, controls 13px / 500, micro-labels 12px / 400, active chip 13px / 600, exchange CTA 18px / 500
- 4px radius on buttons and CTAs; 8px on filter chips; YAML `rounded.full` 9999 has no harvested control
- Heights: 32px signup, 36px active chip, 56px exchange CTAs
- Flat depth: YAML `shadow.flat` none; 1px `#B6ABA1` outline; 1px `rgba(28,32,40,0.1)` inactive-chip border

### Principles

These six items (Density with discipline, One warm accent, Color is signal, Owned voice, Structural depth, Money-grade seriousness) are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification.

1. **Density with discipline** — pack information tightly, but keep the scale and palette restrained so it never reads as cluttered.
2. **One warm accent** — let bronze (`#543E35`) carry the primary action; scarcity is what gives it power.
3. **Color is signal** — reserve red/blue strictly for directional price meaning; structure stays in near-black and gray.
4. **Owned voice** — the proprietary Bithumb Trading Sans is non-negotiable to the identity.
5. **Structural depth** — separate with contrast and 1px borders, not shadows.
6. **Money-grade seriousness** — every choice should reinforce trust and gravity, not delight.

Treating the following as a capture-bound application of source §7 Do's, including structural-anchor-`#1C2028`, bronze-for-the-single-most-important-CTA, korea-convention-red-up-blue-down-never-swapped, tight-type-scale-weight-not-size, and 4px-buttons-8px-chips, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

- Use `#1C2028` as the structural anchor for primary surfaces and active states.
- Reserve `#543E35` bronze for the single most important call-to-action.
- Keep red (`#E15241`) strictly for price-up and blue (`#4880EE`) strictly for price-down — Korea convention, never swapped.
- Hold the type scale tight (14 / 13 / 12px) and use weight (500–600), not size, for emphasis.
- Use 4px radius on buttons/CTAs and 8px on filter chips, consistently.

### Avoid

The following items copy source §7 Don'ts, including western-green-up-red-down, generic-font-not-Bithumb-Trading-Sans, bronze-scarcity-of-`#543E35`, no-heavy-drop-shadows / structural-depth, and money-grade-seriousness-depends-on-density. They are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification.

- Do not apply the Western convention (green-up / red-down) — Bithumb's market reads red as up.
- Do not substitute a generic font; the proprietary Bithumb Trading Sans is part of the brand signature.
- Do not dilute the bronze accent by spreading `#543E35` across many surfaces — its power is its scarcity.
- Do not introduce heavy drop shadows; depth here is structural (color + 1px borders).
- Do not loosen the data density into airy spacing — the money-grade seriousness depends on it. Treating that money-grade-seriousness-depends-on-density characterization as a reconstruction reading rather than Bithumb-authored spacing doctrine is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including structural-dark-UI-base, single-warmest-gesture-in-the-chrome, outline-text-not-bronze-fill, hairline-not-inactive-chip-border, muted-not-muted-2, directional-trading-signal-not-structure, on-primary-not-a-second-canvas, and cta-surface-text-not-canvas-and-not-on-primary, are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification. Catalog `primary_color` `#1C2028` is YAML `primary` and YAML `canvas`; those two keys share the hex and are not split into two paints, but canvas is the YAML canvas key and primary is the structural near-black used as signup and active-chip fill. YAML `brand` `#543E35` is the filled exchange-CTA accent, not structural primary. YAML `on-primary` and YAML `foreground` share `#FFFFFF`; on-primary is the filled near-black label field, not a second canvas token. YAML `cta-surface-text` and YAML `surface-text` share `#F8F9FA`; that hex is text on the bronze CTA, not canvas and not on-primary. YAML `cta-text` `#4F3327` is outline-CTA text, not the bronze fill. YAML `hairline` `#B6ABA1` is the outline-CTA 1px border, not the inactive-chip border `rgba(28,32,40,0.1)`. YAML `muted` `#707882` is inactive-chip text; YAML `muted-2` `#93989E` is secondary/tertiary muted text; they are not merged. Price-up `#E15241` and price-down `#4880EE` are directional trading signal, not structure and not bronze.

- **Structural near-black / Primary** (`#1C2028`): catalog `primary_color`. YAML `primary`, `canvas`. Signup button surface, active filter chips, and the structural dark UI base.
- **Warm bronze / Brand** (`#543E35`): YAML `brand`. Rebrand accent; filled exchange CTA — the single warmest gesture in the chrome.
- **Brown text / CTA text** (`#4F3327`): YAML `cta-text`. Text color on outline CTAs.
- **Neutral border / Hairline** (`#B6ABA1`): YAML `hairline`. 1px border on outline CTAs.
- **Trading red (price up)** (`#E15241`): YAML `price-up`. Korea convention: rising prices. Directional, not structural.
- **Trading blue (price down)** (`#4880EE`): YAML `price-down`. Korea convention: falling prices.
- **Muted gray** (`#707882`): YAML `muted`. Inactive chip text and secondary labels.
- **Muted gray 2** (`#93989E`): YAML `muted-2`. Secondary/tertiary muted text.
- **Off-white surface text** (`#F8F9FA`): YAML `cta-surface-text`, `surface-text`. Text on the bronze exchange CTA.
- **Pure white / On-primary** (`#FFFFFF`): YAML `on-primary`, `foreground`. Text on the structural near-black signup button and active chips.

### Spacing

YAML scale: xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 48, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Harvested control heights in the body are 32px (signup), 36px (active filter chip), and 56px (exchange CTAs). Treating those YAML numbers as recorded without a required px suffix, and treating those heights as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

### Shape

YAML `rounded`: sm 4, md 8, full 9999.

The following local-geometry reading, including Buttons-and-CTAs 4px, Filter-chips 8px, and YAML-full-9999-not-a-harvested-pill / not-converted-to-a-px-token, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

- Buttons and CTAs: 4px (YAML `rounded.sm`)
- Filter chips: 8px (YAML `rounded.md`)
- YAML `rounded.full` 9999: recorded as a YAML step. No harvested control in this packet uses it.

4px button/CTA corners and 8px filter-chip corners are local harvested geometry, not a universal radius for every unlisted control. YAML `full` 9999 is not converted to a px token and is not treated as a harvested pill.

### Elevation

YAML `tokens.shadow.flat`: `none — depth is structural (color layering + 1px borders), no drop shadows captured`.
YAML `tokens.shadow.hairline`: `1px solid #B6ABA1`.
YAML `tokens.shadow.chip-inactive`: `1px solid rgba(28,32,40,0.1)`.

The following elevation reading, including structural-and-flat-rather-than-shadow-heavy, contrast-and-1px-borders-not-drop-shadows, and filled-surfaces-foreground-through-contrast-and-warmth, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. The source treats the aesthetic as structural and flat rather than shadow-heavy: depth comes from color layering against the near-black `#1C2028` base and from borders, not from elevation. The outline CTA uses a 1px solid `#B6ABA1` border, and inactive chips use a 1px solid `rgba(28,32,40,0.1)` border. Filled surfaces (the bronze `#543E35` CTA, the structural `#1C2028` signup button and active chips) establish foreground through contrast and warmth against the dark ground. No specific shadow blur, spread, or elevation token was captured in the inspection, so elevation stays qualitative: contrast and 1px borders, not drop shadows.

### Motion

No motion durations, easing curves, or transition tokens were captured in the live inspection. Treating that absence as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

The following motion-character reading — minimal, fast, and functional; restrained transition appropriate to a professional trading terminal; responsiveness and clarity over expressive animation; immediate and composed state changes; qualitative stance not a duration/curve/animation-name/reduced-motion token — is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. The source says that, in keeping with the brand's serious, money-grade, data-dense character, motion should be treated qualitatively: minimal, fast, and functional — the kind of restrained transition appropriate to a professional trading terminal, where responsiveness and clarity matter more than expressive animation. State changes (chip active/inactive, CTA press) should feel immediate and composed. That qualitative stance is not a duration, curve, animation name, or reduced-motion token. Until real timing values are measured, do not invent durations or curves.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including official-product-use of proprietary brand-owned Bithumb Trading Sans as rebrand signature, prose-derived extraction class, and YAML-mono-same-name-not-a-second-harvested-face, are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | The source calls **Bithumb Trading Sans** proprietary and brand-owned, and a core part of the rebrand's signature. |
| Live computed surface-use | Token extraction is `prose-derived`. |
| Declared-only | YAML `family.sans` and `family.mono` both name `Bithumb Trading Sans`. Mono is the same YAML name, not a second harvested face. |

### Family

- **Current UI family:** `Bithumb Trading Sans`
- **YAML `family.sans`:** `Bithumb Trading Sans`
- **YAML `family.mono`:** `Bithumb Trading Sans` (same name as sans; not a second identity)

The following font-use boundary, including do-not-substitute-a-generic-font and YAML-mono-not-a-distinct-monospace-specimen, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. Do not substitute a generic font for Bithumb Trading Sans. YAML `family.mono` is not promoted as a distinct monospace specimen.

### Type roles

YAML records no `lineHeight`. YAML typography `use` strings are kept. Promoting no line-height value from that absence is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

The following type-rule reading (hold the scale narrow; let weight and color — not size — create hierarchy in a money-grade, data-first interface) is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. YAML records weight 400 on body and micro-label as well as 500 on controls/CTA and 600 on the active chip. Body §3's "weights observed in components run from 500 up to 600" sentence is not a license to drop the YAML 400 roles.

| Role | Font | Size | Weight | YAML use |
|---|---|---:|---:|---|
| Body | Bithumb Trading Sans | 14px | 400 | Workhorse readable content in a packed layout |
| Control | Bithumb Trading Sans | 13px | 500 | Buttons, chips, interactive labels |
| Micro-label | Bithumb Trading Sans | 12px | 400 | Dense data annotations coexisting with numbers |
| Chip-active | Bithumb Trading Sans | 13px | 600 | Active filter chip label |
| CTA | Bithumb Trading Sans | 18px | 500 | Primary exchange CTA label |

The following unmerged-role reading is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. Control 13px / 500 and chip-active 13px / 600 share size and are not merged. CTA 18px / 500 is the filled exchange-CTA label; outline CTA has no YAML font field, and none is invented.

### Assets

Treating catalog logo metadata as a Google favicon lookup (`https://www.google.com/s2/favicons?domain=bithumb.com&sz=128`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

Characterizations in the following table such as “high-contrast, structural, clearly selected”, “recessive and quiet against the dark ground”, “the warmest, most prominent state”, and “paired but subordinate to the filled bronze” are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Active (filter chip)** | background `#1C2028`, text `#FFFFFF`, radius 8px, height 36px, font 13px/600 — high-contrast, structural, clearly selected. |
| **Inactive (filter chip)** | background transparent, text `#707882` (muted gray), border 1px solid `rgba(28,32,40,0.1)`, radius 8px — recessive and quiet against the dark ground. |
| **Primary action (exchange CTA)** | background `#543E35`, text `#F8F9FA`, radius 4px, height 56px, font 18px/500 — the warmest, most prominent state. |
| **Secondary action (outline CTA)** | background transparent, text `#4F3327`, border 1px solid `#B6ABA1`, radius 4px, height 56px — paired but subordinate to the filled bronze. |
| **Price up** | trading red `#E15241` (Korea convention). |
| **Price down** | trading blue `#4880EE` (Korea convention). |

Hover, pressed, focus, and disabled states were not captured in the live inspection. The source parenthetical that they be treated qualitatively (likely subtle contrast shifts against `#1C2028`) rather than inventing values is a reconstruction invitation, not a published visual token. Treating that parenthetical as not a token, and omitting those visual treatments rather than synthesizing contrast-shift paints, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. Where exact label/destination/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 variant-as-state rows. This is not a complete state-coverage claim.

Active and Inactive filter chips are harvested as `type: badge` with default/selected appearances and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Signup (structural)

- Role: account signup / primary structural action in the top chrome
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#1C2028`
- Text: `#FFFFFF`
- Border: none
- Radius: 4px
- Height: 32px
- Font: 13px / 500
- Use: Signup — structural top-chrome action
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-bronze and on-primary-not-`#F8F9FA`, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. `#1C2028` is this control’s fill and catalog `primary_color`. It is not bronze `#543E35`. `#FFFFFF` is this control’s on-fill label (on-primary), not `#F8F9FA`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the structural top-chrome signup action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A signup action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as account signup / primary structural action in the top chrome; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Exchange CTA (filled)

- Role: the primary exchange call-to-action — the single warmest, most prominent action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#543E35`
- Text: `#F8F9FA`
- Border: none
- Radius: 4px
- Height: 56px
- Font: 18px / 500
- Use: Primary exchange CTA — single warmest action
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-structural-primary and on-fill-not-on-primary, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. `#543E35` is this control’s fill and YAML `brand`. It is not structural primary `#1C2028`. `#F8F9FA` is this control’s on-fill label, not on-primary `#FFFFFF`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the filled bronze exchange CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A call-to-action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the primary exchange call-to-action; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 Primary action row.

### Exchange CTA (outline)

- Role: secondary action paired alongside the filled bronze CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#4F3327`
- Border: 1px solid `#B6ABA1`
- Radius: 4px
- Height: 56px
- Use: Secondary action paired with filled bronze CTA
- Observed: default only
- YAML `cta-outline` records bg/fg/border/radius/height/use; no font field. None is invented.
- Field note: The following unmerged-field reading, including cta-text-not-bronze-fill, hairline-not-inactive-chip-border, and no-font-field-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. `#4F3327` is this control’s label (cta-text), not bronze fill `#543E35`. `#B6ABA1` is this control’s 1px border (hairline), not the inactive-chip border `rgba(28,32,40,0.1)`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the outline exchange CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the secondary action paired with the filled bronze CTA; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 Secondary action row.

### Filter Chip (active)

- Role: the currently selected filter in a chip row
- Type: badge
- Anatomy: label
- Background: `#1C2028`
- Text: `#FFFFFF`
- Border: none
- Radius: 8px
- Height: 36px
- Font: 13px / 600
- Use: Selected filter chip
- Observed: default / selected appearance as the §14 Active row
- Field note: The following unmerged-field reading, including this-fill-not-bronze and 13px-600-not-Control-13px-500, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. `#1C2028` is this badge’s fill and structural near-black. It is not bronze `#543E35`. Font 13px / 600 is this badge’s field, not Control 13px / 500.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Filter Chip (inactive)

- Role: unselected filter options in the same chip row
- Type: badge
- Anatomy: label
- Background: transparent
- Text: `#707882`
- Border: 1px solid `rgba(28,32,40,0.1)`
- Radius: 8px
- Use: Unselected filter chip
- Observed: default / unselected appearance as the §14 Inactive row
- YAML `chip-inactive` records bg/fg/border/radius/use; no height or font field. None is invented.
- Field note: The following unmerged-field reading, including muted-not-muted-2, inactive-border-not-hairline, and no-height-or-font-field-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. `#707882` is this badge’s label (muted), not muted-2 `#93989E`. `rgba(28,32,40,0.1)` is this badge’s border, not hairline `#B6ABA1`.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 48, section 64. Harvested heights: 32px signup, 36px active chip, 56px exchange CTAs. 4px radius on buttons/CTAs; 8px on filter chips.

The following layout readings — density done deliberately; the near-black base framing content rather than competing with it; two radius scales signaling two layout intents (4px crisp terminal-like edges on buttons/CTAs, 8px slightly softer tappable chip row); control heights tiered to importance; restraint in surface and richness in data; structure receding so numbers and directional trading colors lead — are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification.

No discrete breakpoint pixel values or responsive grid tokens were captured in the live inspection, so responsive behavior is described qualitatively. The following breakpoint and touch-purpose readings — 32px / 36px / 56px translating cleanly to touch targets; 56px CTAs comfortably finger-sized; 36px chips reading as a scrollable control row; dense 13px controls and 12px micro-labels implying stacked, scrollable panels on narrower viewports rather than shrinking type below a legibility floor; chip row horizontally scrollable on small screens; bronze CTA full-width and prominent where space contracts; source-stated 32px / 36px / 56px as component heights not a cross-viewport specification; preserve height tiers and radius scales rather than inventing new ones — are a derived editorial implementation inference from the verified surfaces; they are not Bithumb-authored or a separately published UI specification. Until real breakpoints are measured, preserve the height tiers and radius scales rather than inventing new ones. The 32px, 36px, and 56px figures are source-stated component heights, not a cross-viewport specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The following voice reading — serious, dense, and money-grade; professional trading floor not a playful consumer app; precision and economy; real numbers and real risk; direct unembellished microcopy; clarity of state over personality; confident and composed; Korea's No.1 without saying so loudly — is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification. The source describes Bithumb's voice as serious, dense, and money-grade — the register of a professional trading floor, not a playful consumer app. It communicates with precision and economy, trusting the user to handle real numbers and real risk. Microcopy should be direct and unembellished, prioritizing clarity of state (price up / price down, active / inactive) over personality. The tone is read as confident and composed: it conveys that this is Korea's No.1 exchange without saying so loudly, letting the density and discipline of the interface make the claim.

Not promoting synthetic voice samples, and treating the §14 variant-as-state rows as the state contract rather than extra voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Bithumb-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- Hover, pressed, focus, and disabled states were not captured in the live inspection; the qualitative contrast shifts against `#1C2028` are not a token
- loading, error, and success visual treatments, and Signup / Exchange CTA (filled) / Exchange CTA (outline) loading·error·success applicability (exact request/outcome unresolved)
- interactive kind and state-applicability map for Filter Chip (active) and Filter Chip (inactive)
- `focus-visible` visual treatments; a generic Focus observation is a different evidence type
- discrete breakpoint pixel values and responsive grid tokens (source: not captured)
- YAML `lineHeight` (none recorded)
- YAML `rounded.full` 9999 harvested onto a control (none recorded)
- outline-CTA font; inactive-chip height and font (YAML fields absent)
- shadow blur, spread, or elevation token (source: not captured)
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
