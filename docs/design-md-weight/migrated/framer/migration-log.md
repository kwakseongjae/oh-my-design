# framer migration log

Source: `web/references/framer/DESIGN.md` (unmodified)
Destination: `docs/design-md-weight/migrated/framer/DESIGN.md`
Ledger: `docs/design-md-weight/migrated/framer/provenance.md`
Rulebook version used: **v10** (`docs/design-md-weight/MIGRATION_RULEBOOK.md`)
Date: 2026-08-26

Every row below was grep-checked against the actual output files before it was written (F2). Counts come from `grep -o … | wc -l`, so they count occurrences rather than lines, and each count names the file it was taken in. Where a value legitimately lands in two places, both destinations are named. Where a value is only *mentioned* (named inside a boundary, prohibition or omission sentence) rather than *used* as a renderable token, the row says so.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML `id`, `country`, `category` | 분리 → 원장 | Identity table only. The portable file carries no frontmatter. `framer` as an id string, `US` and `design-tools` are 0× in DESIGN.md. |
| YAML `name: Framer` | 분리 → 원장 **및** 옮김 → DESIGN.md | Identity table row; DESIGN.md H1 `# Framer Design System` plus the Scope, Assets, Principles and Content prose. `grep -o "Framer" DESIGN.md \| wc -l` = 39 occurrences, counting the bare brand name together with the substring inside `Framer Blue` (4), `Framer Deep Blue` (2), `Framer-hosted` (5) and the `not Framer-authored` qualifier clauses (7). |
| YAML `homepage: "https://www.framer.com"` | 분리 → 원장 **및** 옮김 → DESIGN.md | Identity table; also the Experience Scope sentence that names the host the three observed surfaces sit on — `grep -o "https://www.framer.com" DESIGN.md \| wc -l` = 1, and it is the only URL in the portable file. |
| YAML `primary_color: "#0055ff"` | 분리 → 원장 **및** 옮김(언급) → DESIGN.md | Identity table (`#0055ff` 2× in provenance.md). In DESIGN.md the value appears only in its published form `#0055FF` (2×: the Distinctive-traits brand-asset bullet and the Foundations sentence "…remains an official brand-asset color from the brand guidelines, but it was not a measured token in the supplied live samples"). It is **mentioned as a brand-asset colour, never used as a token** — this is the field the source itself excluded from the live token set, and the Foundations paragraph states the separate-evidence-domain rule beside it. The uppercase/lowercase split is preserved as the source has it (A5): lowercase for the catalog field, uppercase for the published mark colour. |
| YAML `logo` (`type: simpleicons`, `slug: framer`) | 분리 → 원장 | Identity table only (`simpleicons` 2× in provenance.md, 0× in DESIGN.md). It is a third-party icon-set entry, not a Framer-distributed mark file, so it is not promoted into the portable Assets subsection; that reasoning is recorded beside the identity row. The portable Assets subsection states instead that the contract carries no first-party Framer mark, image, or icon file. |
| YAML `ds` (`name: Framer Brand Guidelines`, `url`, `type: brand`, `description: Official mark, color, and trademark-use guidance.`, `og_image`) | 분리 → 원장 **및** 옮김(내용) → DESIGN.md | All five fields are in the Identity table verbatim, including `ds.type: brand` (A1c) and the og_image URL (`Framer Brand Guidelines` 1×, `og_image` 1× in provenance.md; both 0× in DESIGN.md). Their **substance** — that Framer publishes official guidance covering the mark, colour and trademark use, and that the mark palette is published there — is in the portable Typography & Assets subsection and Distinctive traits. The URL stays in the ledger and in its narrative-context row. |
| YAML `verified`, `omd: "0.1"`, `verification_v2.schema: 2`, `.checked` | 분리 → 원장 | Freshness table (`verified`, `schema`, `checked`) and Identity (`omd format (source)`). The shared `2026-07-13` date also reaches the portable Scope once; that dual destination is recorded on the surfaces row below rather than counted twice here. |
| YAML `verification_v2.surfaces` (home / surface-2 / surface-3, kinds, inspected) | 분리 → 원장 **및** 옮김(경계) → DESIGN.md | Surfaces table carries ids, kinds, URLs and dates. The three-surface **boundary**, the domain names (marketing / community Marketplace / pricing) and the capture date stay in the portable Experience Scope, because standalone reading needs them: `grep -o "2026-07-13" DESIGN.md \| wc -l` = 1, in "the supplied 2026-07-13 evidence". Only the surface ids, kinds and per-surface URLs moved out (`surface-2` 0× in DESIGN.md, 15× in provenance.md). |
| YAML `verification_v2.sources` (3 product-surface + brand-asset + license) | 분리 → 원장 | Sources table and the narrative/brand/licence context list. The portable §3 keeps their substance — the two Framer-hosted Inter Variable WOFF2 sources exist, Inter's OFL family/licence context, Input's licence requirement, JetBrains Mono OFL-1.1 — while the URLs stay in the ledger (`github.com/rsms/inter` 0× in DESIGN.md, 3× in provenance.md). |
| YAML `verification_v2.claims` (32 paths, anchors, methods) | 분리 → 원장 | Claim ledger, one row per token path, anchors `home` / `mkt` preserved. The single `method: interaction-capture` path (`marketplace-filter.states`) is called out in its own sentence there, because it is the evidence behind the portable filter trigger's `expanded and menu-open` observation line. |
| YAML `verification_v2.conflicts: []` | 분리 → 원장 | Freshness ("Conflicts unresolved: none") and Proof notes. |
| YAML `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true` | 분리 → 원장 | Identity table for all three; `tokens.extracted` also has its own Freshness row and `components_harvested` is repeated in Proof notes (`components_harvested` 2× in provenance.md, 0× in DESIGN.md). |
| YAML `tokens.colors` (canvas, foreground, muted, marketplace-surface, accent) | 옮김 → Foundations | §2 Semantic color, one role line per key with the source's own role wording. In DESIGN.md: `#000000` 3×, `#ffffff` 8×, `#999999` 1×, `#242424` 6×, `#0099ff` 5×. |
| §4 `#333333`, `#666666`, `#dee6ff` | 옮김 → Components & States **및** 언급 → Foundations | Each stays its own component's renderable field (A4): the menu-panel border (`#333333` 2×), the disabled-tab text (`#666666` 4×), the form-error text (`#dee6ff` 4×). Foundations names all three once in a sentence that says they stay with their components and are not general semantic roles — that is a **mention**, not a promotion to a role line, and it matches source §2's "other isolated colors … are not raised into a system palette". |
| YAML `tokens.typography.family` (`ui: Inter`, `display: GT Walsheim Medium`) | 옮김 → Typography & Assets | §3 Family, both keys with their observed role, plus the no-substitution rule. `Inter` 26× and `GT Walsheim Medium` 7× in DESIGN.md. |
| YAML `tokens.typography.public-body` (14 / 400 / 14 / -0.01) + `use` | 옮김 → Typography & Assets | §3 hierarchy row `Public home body \| Inter Variable \| 14px \| 400 \| 14px \| -0.01px`, with the source `use` string `Observed public-home body text` in the boundary column (1× in DESIGN.md). The source's YAML carries no unitless line-height ratio for this or any other role — `lineHeight: 14` is the px measurement — so no ratio was derived (A1a: the form of the value is preserved as the source states it). |
| YAML `tokens.spacing` (`compact-x: 10`, `menu-inset: 5`) | 옮김 → Foundations **및** Layout & Platforms | §2 Spacing names both as control measurements and says they are not a universal scale; §5 repeats the source's own `0px 10px` padding and 5px inset sentences. `0px 10px` 6× in DESIGN.md; `5px` 8× in DESIGN.md, counted with a preceding-digit guard so the run inside `15px` is not counted. |
| YAML `tokens.rounded` (`compact: 8`, `menu: 13`, `category-card: 20`) | 옮김 → Foundations **및** Components & States | §2 Shape lists all three with the "not a site-wide full-pill rule" bound from §12; §4 repeats each as its components' Radius. `8px` 12×, `13px` 8×, `20px` 5× in DESIGN.md, each counted with a preceding-digit guard. |
| YAML `tokens.shadow.marketplace-menu` | 옮김 → Foundations **및** Components & States | §2 Elevation carries the compact YAML form `rgba(0,0,0,0.08) 0px 3px 6px 0px` (1×) and §4 carries the spaced §4 form `rgba(0, 0, 0, 0.08) 0px 3px 6px 0px` (1×) on the menu panel. Both notations are the source's own and both are preserved as written. |
| YAML `tokens.components.marketplace-filter` (`type: button`, bg, fg, radius, padding, font, `states`, `use`) | 옮김 → Components & States | The "Marketplace filter trigger" subsection. The verified primitive is kept per component as `Primitive: button` rather than collapsed into an interactive label (A1b) — `button` 2× in DESIGN.md. The `states` string is carried verbatim as the Observed line: `expanded and menu-open captured on the Marketplace filter` (1×). The `use` string `Marketplace filter trigger on the public community surface` is the Role line and also the §3 hierarchy boundary cell (2× in DESIGN.md). |
| YAML `tokens.components.marketplace-category` (`type: card`, bg, fg, radius, `use`) | 옮김 → Components & States | The "Marketplace category card" subsection. `Primitive: card` is preserved per component (A1b) — `card` 19× in DESIGN.md, counting the component name, the category-card role text and the Shape row. The `use` string `Marketplace category card on the public community surface` is its Role line (1×). |
| §1 Visual Theme & Atmosphere (prose) | 옮김 → Experience Scope | The careers-page mission, the AI-assisted-website-creation product framing, the three captured public domains, and the "does not turn public marketing, community browsing chrome, or pricing into a claim about the authenticated editor, generated sites, or documentation UI" boundary. The source author's reading ("the clearest recurring visual expression is an achromatic black-and-white public canvas …") is restated with an adjacent derived-editorial-implementation-inference qualification; the values it reads are listed as observations first (B2/B2a). |
| §1 four key-characteristic bullets | 옮김 → Experience Distinctive traits | All four carried with their values and the source's own labels, including the published `#0099FF` / `#0055FF` mark palette bullet. A fifth bullet states the observed display/UI family split from §3; it adds no value that is not already measured there. |
| §2 Observed public-web roles (6 bullets) | 옮김 → Foundations Semantic color | See the `tokens.colors` row; the sixth bullet (Framer Blue) keeps the source's "official brand guidance also lists the same color" clause. |
| §2 `#0055FF` paragraph and "other isolated colors" sentence | 옮김 → Foundations | Both carried; the separate-evidence-domain rule ("the published brand palette and the live public-web capture are separate evidence domains: an observation on one does not populate a token in the other") is added beside them as the explicit form of the boundary the two source sentences already draw. |
| §3 Evidence classes (6 bullets) | 옮김 → Typography & Assets | All six carried, including the 512 / 392 / 11 visible-use counts, the two Framer-hosted WOFF2 sources as a fact, the FontFaceSet-without-URL note for GT Walsheim Medium, the Input and JetBrains Mono licence context, the declared-only list, and the unobserved-domain boundary. The source's resolution of the `inter, "inter Fallback"` computed stack to the canonical family name is an evidence-handling decision, so it carries an adjacent complete qualification (B2a). |
| §3 Measured public hierarchy (5 rows) | 옮김 → Typography & Assets | All five rows with family, size, weight, line height and tracking. `-0.01px`, `-2.16px`, `-1.76px`, `48.4px`, `18.2px`, `54px`, `44px` all present in DESIGN.md. The table's `Evidence boundary` column held capture selectors; those moved to the ledger's capture-selector table (`data-omd-capture` 0× in DESIGN.md, 10× in provenance.md) and the column now carries the role/use wording. |
| §4 intro paragraph | 옮김 → Components & States Capture record | Carried verbatim, including the three-interaction-expansion fact and the "does not capture an authenticated editor, documentation chrome, hover, pressed, focus, modal, or product-toast contract beyond the exact rows called out below" bound. |
| §4 Marketplace action — Filter trigger, menu-open capture | 옮김 → Components & States | Values, border, radius, padding and font preserved; `aria-haspopup="menu"` kept as the anatomy note. Selector moved to the ledger. |
| §4 Marketplace action — Accent action | 옮김 → Components & States | Its own subsection with `#0099ff` background and `12px / 600 / Inter`, separated from the filter trigger so that each control's applicability is judged on its own role (C2). Selector moved to the ledger. |
| §4 Marketplace menu — Open panel | 옮김 → Components & States | All six values plus the shadow. Declared `Kind: non-interactive` with the reason that the record measures the expanded panel surface and the control role belongs to the options inside it, so no applicability map is asserted. |
| §4 Marketplace menu — Unchecked menu option | 옮김 → Components & States | Values, `role="menuitemcheckbox"` and `aria-checked="false"` preserved as the role line, with the source's "captured only in the expanded Marketplace menu" bound. |
| §4 Marketplace category card — Default | 옮김 → Components & States | See the `tokens.components.marketplace-category` row. |
| §4 Marketplace tab — Disabled | 옮김 → Components & States | `#666666`, 8px, `0px 10px`, `12px / 500 / Inter`, and the "disabled, coming-soon" role. |
| §4 Home form field — Error capture | 옮김 → Components & States | `#dee6ff`, `14px / 500 / Inter`, and the source's "records the error state but does not supply a reusable field shell, label, or transition contract" sentence, carried verbatim. |
| §5 Layout Principles (4 bullets) | 옮김 → Layout & Platforms **및** Foundations Spacing | All four bullets carried verbatim in §5, including the `1440×900` desktop-only bound, the 15px card-local padding and the 5px / 10px / 14px / 15px / 40px aggregate note. The two named spacing tokens also have Foundations role lines (row above); the aggregate values are **mentioned** there only through the Named-gaps entry, not turned into a scale. |
| §6 Depth & Elevation (3 bullets) | 옮김 → Foundations Elevation | All three carried: the menu panel as the only selector-backed elevated panel, the three home utility controls with `rgba(0,0,0,0.2) 0px 2px 6px 0px` whose semantic role is not identified, and the "no general blue glow, card-shadow hierarchy, or authenticated-product elevation rule" bound. The selector `data-omd-capture="31"` through `"33"` moved to the ledger; the portable text says "three individual home buttons". |
| §7 Do (4 items) | 옮김 → Experience Principles "Applied rules" | All four kept as brand application rules rather than folded into Governance boilerplate. The fourth is reworded from "route-level component provenance" to "route-level component evidence" so the portable body carries the meaning without ledger vocabulary; the meaning — provenance at route/component level rather than a universal semantic-state guarantee — is unchanged. |
| §7 Don't (4 items) | 옮김 → Experience Avoid | All four carried verbatim. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms **및** Governance Named gaps | The paragraph is carried verbatim, and breakpoint / mobile navigation / touch target / image crop / responsive state transition are named as unresolved in Named gaps. |
| §9 Agent Prompt Guide — the three prompt strings | 삭제 | A tool-directed construction prompt with no receiving Core slot. Its design values (`#000000` canvas, `#ffffff` foreground, Inter UI text, `#0099ff` accent; `#242424` with white `12px / 500` Inter text, `8px` radius, `0px 10px` padding, the `13px` menu panel; GT Walsheim Medium at `54px`/500 and `44px`/500) are restatements of §1–§4 and were each grep-confirmed in DESIGN.md before this row was written. The deletion is recorded in the ledger's omission section as well. |
| §9 "public-web direction, not an app-kit specification" and "not an unbounded display scale" | 옮김 → Experience Scope / Typography & Assets | These two clauses exist only in §9 and there are slots for both (A3), so they moved rather than being dropped with the wrapper: `app-kit` 1× and `unbounded display scale` 1× in DESIGN.md. |
| §10 Voice & Tone prose | 옮김 → Content & Locales | The careers-page and product-blog mission wording carried. The source's own inference ("That supports a direct, maker-facing public voice") is restated with an adjacent complete qualification, and the "does not establish editor error copy or a complete product-content style guide" bound is kept. |
| §10 published strings “Make the web more creative” / “Sign up.” | 옮김 → Content & Locales **및** Experience | Both carried byte-for-byte with the source's curly quotation marks, in both the punctuated and unpunctuated forms the source uses (A5): `grep -o "Make the web more creative" DESIGN.md \| wc -l` = 3 (Scope reference, the §6 lead sentence, the §6 table row) and `grep -o "Sign up." DESIGN.md \| wc -l` = 3 (primary task, table row, the sentence naming both as Framer's own published strings). |
| §10 direction table (3 rows) | 옮김 → Content & Locales | Carried as a table. The "Supported direction" column is an editorial reading of the two published strings and the Marketplace surface, so the qualification directly under the table names the table itself (B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope | The web-design-and-publishing framing, the careers/blog statements, and the design / collaboration / CMS / hosting / performance / SEO / conversion / publishing navigation span. The source's "ties the company to the future of the web" reading and its "the visually distinctive expression in this packet comes from the current public surfaces" reading both sit inside one adjacent complete qualification, and the "not a claim that every Framer product surface uses the same components or palette" bound is kept. |
| §12 Principles (4 + UI implications) | 옮김 → Experience Principles | All four with their UI implications, under a head qualifier that also covers the applied rules below them (`derived editorial implementation inference` 7× in DESIGN.md, across all seven qualified sites). |
| §13 Personas (3 archetypes) | 옮김 → Experience Audience | The three archetypes are carried as audience groups with their one-line public-positioning basis and no biography, demographic, or private-behavior claim. The source's own qualification ("derived from Framer's public positioning, not synthetic test users or claims about private product behavior") is kept and completed with the not-Framer-authored clause (B2a/D2). They are **not** promoted into the `primary-tasks` claim: those three tasks come from the measured public surfaces — the Marketplace listing, the pricing page and the captured `Sign up.` action. |
| §14 States table (5 rows) | 옮김 → Components & States Capture record | The whole table is preserved as a table, including the toast-viewport row (present element, no message / lifecycle / style contract) and the "Empty/loading/success/product failure — Not captured" row. No toast component is declared anywhere. |
| §14 → per-component applicability | 옮김 → Components & States | Each declared interactive component closes the seven canonical states by control role. `loading` / `error` / `success` stay **applicable** on the accent action (it is recorded as an action that runs an operation) and on the home form field (it commits an entry, and its `error` is the observed treatment). They are `not-applicable` with role reasons on the filter trigger (opens a menu), the `menuitemcheckbox` option (toggles its own value in place) and the coming-soon tab (selects which panel is shown) — never for a missing observation (C1/C2 v10). The category card and the menu panel are the two components with no interactive-kind evidence for a control role: the card fixes no kind and no map (C4), the panel declares non-interactive with the reason. The body states "Absence of an observation is never a `not-applicable` reason" and "This is not a complete state-coverage claim" (C3). |
| §15 Motion & Easing | 옮김 → Foundations Motion **및** Governance Named gaps | The "supplies no measured duration, easing, transition, or reduced-motion behavior" sentence and the "Framer's historical association with motion does not license a motion-token claim" prohibition are carried verbatim, and duration / easing / transition / reduced-motion behavior are named in Named gaps. The promotion condition added beside them names all five evidence kinds — transition properties, animation name, duration, easing, and reduced-motion behavior — plus the per-component computed-observation gate and the explicit statement that one documented curve or duration does not satisfy it (B3). That full sentence is present in §2 Motion, which is why this row claims it (E2c). The source carries no easing curve value, so there was no unsourced curve to remove. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → 원장 | Freshness, the Tier 1 and Tier 2 lists, and the conflicts line. No tier vocabulary appears in the portable body (`getdesign.md` and `refero` 0× in DESIGN.md, 2× and 3× in provenance.md). |
| Sibling `web/references/framer/.verification.md` | 채택 → 원장 | Adopted as ledger evidence under "Sibling files": the bundle timestamp and raw-collector path, the coverage counters (`componentTypes: 10`, `componentVariants: 39`, `observedStates: 5`, `interactionKinds: 2`, `interactionCount: 3`, score 100), the ten raw computed samples, the toast resolution, the font-evidence detail (78 zero-use declarations, the two Inter Variable WOFF2 URLs, Lazzer / Manrope / Mona Sans / Open Runde, the `sans-serif` system-stack note), the Grilli Type licence source, the dictionary product-vocabulary source, the Tier 2 records, the conflict matrix and the reverted-legacy-claims list. Its distinct values are **not** promoted into portable tokens — specifically the `home::body` `12px / 400` sample, the category-card `18px` line height, the form-error `0px` radius and `19.6px` line height, and the toast-viewport `15px` padding and `12px / 400` type — and that non-promotion with its reason is stated in that ledger section (E2). |
| Siblings `README.md`, `preview.html`, `preview-dark.html` | 미채택 → 원장에 사유 기록 | **Not adopted**, with the reason recorded rather than left silent (E2): the README is upstream packaging metadata for a nine-section document shape this migration replaces, and the two preview catalogs are built from the pre-reconciliation snapshot — `--color-near-black: #090909`, `--color-blue-glow`, frosted surfaces, `'Azeret Mono'` as the mono role, a `--radius-xs: 1px` / `--radius-sm: 5px` scale — which is exactly the value set the 2026-07-13 reconciliation removed at the field boundary. Adopting them would reinstate rolled-back claims, so nothing from any of the three reaches the portable body or the ledger's token rows. |

## Mandatory final passes

| Pass | Result |
|---|---|
| **F1 — B2a scan** | Re-read the whole portable body sentence by sentence and classified each as brand-published fact, observation, or editorial interpretation. Seven sentences were the third kind and each carries an adjacent complete qualification naming both halves of the evidence class ("a derived editorial implementation inference from the verified surfaces; it is not Framer-authored or a separately published UI specification"): the §1 achromatic-canvas reading (line 11), the §1 future-of-the-web / distinctive-expression reading (13), the Audience archetypes (26), the Distinctive-traits labels and editorial-display reading (36), the Principles head (40), the §3 canonical-family-name resolution (111), and the §6 maker-facing-voice reading together with the direction table (287). Two of them were added by this pass: the Distinctive-traits label sentence, and the reword of the third primary task, which had borrowed the source’s derived "direct account/start action" phrasing and now names only the published “Sign up.” label. Documented facts were **not** demoted by qualification: the careers-page and blog mission wording, the published mark palette, the observed computed values, the visible-use counts, and the licence statements are stated as facts with their source class named, without a derived qualifier. |
| **F2 — E2 collation** | Every row above was grep-confirmed before it was written, with counts taken per file. Dual destinations named on 13 rows of the 47 legacy rows. Spot checks: `#0055FF` 2× in DESIGN.md — both mentions, no token row; `#0099ff` 5× in DESIGN.md — token use in Scope, Distinctive traits, Foundations, the applied rule and the accent action, while the published `#0099FF` form appears once in the mark-palette bullet; `data-omd-capture` 0× in DESIGN.md and 10× in provenance.md; `omd-apply` and `npx omd` 0× in DESIGN.md; `[FILL IN` 0× in the source and 0× in DESIGN.md; its one occurrence in provenance.md is the omission-ledger sentence recording that the source has none, and the concrete colon-form placeholder that the gate traces back to the source is 0× in every file; `Tier` 0× in DESIGN.md; `provenance` 0× in DESIGN.md (the §7 "route-level component provenance" phrasing was reworded to "route-level component evidence"); `button` 2× and `card` 19× in DESIGN.md, so both verified primitive types survive; `expanded and menu-open captured on the Marketplace filter` 1× in DESIGN.md, so the only `interaction-capture` claim is carried as written. Compliance claims are only asserted where the body actually carries the full text (E2c) — the B3 five-kind sentence was re-read in §2 Motion before the §15 row claimed it. |
| **E1 — process-leak self-check** | `node test-v2/tools/process-leak-check.mjs` — framer is not in the leaking list. |
| **Portable-core checker false positive — reported** | The first `--require-portable-core` run failed `actionable_foundations_or_known_constraints`. The cause was one Foundations sentence, "These are the supported compact controls, not a site-wide full-pill rule.", matching the self-denial window `(no|not).{0,32}(foundation|constraint|rule)` while its evidence-boundary guard did not fire. The claim body was never short of actionable values — the six colour roles, two spacing values, three radius steps and the elevation values were all present. The sentence was not reworded around the checker's vocabulary (E3): it now names the evidence it always rested on — "These three radius steps are what the capture measured on the observed Marketplace controls; they are the supported compact controls, not a site-wide full-pill rule." The source's own wording, "the supported compact controls are 8px-rounded Marketplace controls, not a site-wide full-pill rule", stays byte-intact in Experience Principles item 4. The checker window is worth narrowing the same way `t2-1-conformance-fp-2026-08-26` narrowed it for dell / dmm / drnow / easywallet: a scope bound of the form "X, not a site-wide Y" is not a claim that the document has no foundations. |
| **Machine gates** | `node test-v2/tools/migrate-reference.mjs --brand framer --gate-only` → PASS, problems []. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/framer/DESIGN.md --check --require-portable-core --json` → exit 0, `portable_core: true`, `conformance.level: portable-core`, `dropped_segments: 0`, SHA-256 `41118f371dc670a823b6414e0f90abbb98b4828bcb22169ca9d4b2fd95a0e256`. All three results are from the final bytes, after the F1 rewords and the Foundations evidence-naming fix. |
