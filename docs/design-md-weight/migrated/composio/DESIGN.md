# Composio Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating Composio as a US backend-devops product that supplies pre-built integrations LLM agents can call, treating catalog homepage identity as `https://composio.dev` rather than a third captured host, treating this-contract-covers-the-two-public-hosts-named-in-the-source-footer, and treating values-stay-attached-to-the-evidence-domain-that-established-them, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Composio is a US backend-devops product that supplies pre-built integrations LLM agents can call. Catalog homepage identity is `https://composio.dev`. This contract covers two current first-party public hosts named in the source footer: `composio.dev` homepage and `composio.dev/pricing`. Color, type, and component values stay attached to the evidence domain that established them.

Token extraction is `tokens.source: prose-derived`. Catalog `primary_color` is `#6366f1`. YAML `tokens.colors.primary` / `brand` is Composio Cobalt `#0007cd`. Treating that pair as unmerged, and treating the source token note as reconstruction coverage rather than a published Composio token sheet, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Source token note: `primary_color` field (`#6366f1`) is an indigo approximation; the live DS brand is Composio Cobalt `#0007cd`. Dark-canvas system: many roles use white-opacity (rgba) borders/text — only stated 6-digit hexes promoted to colors; opacity variants live in shadow/note context.

The source footer records a later live-DOM overlay on those two hosts (playwright getComputedStyle): UPPERCASE CTAs `#fff` / `#000` / 0px radius / 6×8 padding / 33px / 14px·400. YAML `button-primary` records radius 4, padding `8px 24px`, fill `#ffffff`, fg `#0f0f0f`. Source §4 still writes the white-fill primary as “subtly rounded (likely 4px)”. Treating those three records as unmerged evidence domains rather than choosing the live 0px as the only portable radius, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

The following atmosphere readings — nocturnal-command-center, Void-Black-`#0f0f0f`-canvas, barely-visible-white-opacity-containment, abcDiatype-with-JetBrains-Mono, heading-line-height-0.87, Electric-Cyan-`#00ffff`-and-Cobalt-`#0007cd`-reserved-for-interactive-moments-and-accent-glows, and hard-offset-shadows-`4px 4px`-on-select-elements — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification. Source §1 records a nocturnal command-center read: Void Black `#0f0f0f` canvas, barely-visible white-opacity containment, abcDiatype with JetBrains Mono, heading line-height 0.87, Electric Cyan `#00ffff` and Composio Cobalt `#0007cd` reserved for interactive moments and accent glows, and hard-offset shadows `4px 4px` on select elements.

Treating the following public-history facts as narrative rather than interface tokens — 2023-San-Francisco-founding, Soham-Ganatra-CEO-and-Karan-Vaidya-CTO, both-IIT-Bombay-graduates, Physics-Olympiad-camp-then-college-roommates, pre-built-integrations-200+-tools-and-APIs, 100K+-developers-and-200+-companies-including-Glean-April-OpenNote-and-Altera, ~$29M-funding, $4M-seed-2024-led-by-Together-Fund-with-Elevation-Capital-and-angels-Gokul-Rajaram-Sohum-Mazumdar-Rubrik-Dharmesh-Shah-HubSpot, and $25M-Series-A-Nov-2024-led-by-Lightspeed-Venture-Partners — treating they-do-not-by-themselves-supply-interface-tokens, and treating the source-stated-refusal-list-consumer-AI-hype-magic-wand-metaphors-abstraction-that-hides-the-actual-tool-call as narrative-not-interface-tokens, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Source §11 records those facts from named third-party pages (Tracxn, Entrackr, Lightspeed Venture Partners). Funding total ~$29M: $4M seed (2024) led by Together Fund with Elevation Capital + angels Gokul Rajaram, Sohum Mazumdar (Rubrik), Dharmesh Shah (HubSpot), then $25M Series A (Nov 2024) led by Lightspeed Venture Partners. They do not by themselves supply interface tokens. What the source treats Composio as refusing: consumer-AI hype framing, magic-wand metaphors, abstraction that hides the actual tool call.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the two footer-named public hosts as independently verified in this packet, treating the named jobs as review-the-public-homepage / start-from-the-homepage-UPPERCASE-primary-CTA / review-pricing, treating the named homepage UPPERCASE CTAs and the pricing host as harvested from §10 / the footer live-DOM line / the philosophy line rather than from §13, and treating YAML `use` strings as harvested-control-labels-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Independently verified in this packet are the two named public hosts; named CTA strings are harvested labels, not independently verified destination routes.

- Review the public homepage at `https://composio.dev` (source footer live DOM; §10 samples *“GET STARTED FOR FREE”* / *“ADD TO MY AGENT”*).
- Start from the homepage UPPERCASE primary CTA (*“GET STARTED FOR FREE”* / footer *“GET STARTED”*).
- Review pricing at `composio.dev/pricing` (source footer Tier 1 philosophy host).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no named or fictional personas are promoted, treating source §13 as fictional archetypes informed by named user segments rather than verified biographies, treating those named people as not Audience and not primary tasks, treating do-not-substitute-those-entries as the reconstruction boundary, tying observable work only to the three primary tasks, and treating AI-engineers / agent-platform-builders / indie-developers as source-stated segment labels rather than researched persona segments, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. No individual personas are promoted. Source §13 labels its entries fictional archetypes informed by Composio user segments (AI engineers, agent platform builders, indie developers shipping agent products), not individual people. Do not substitute those entries.

### Distinctive traits

The following unmerged-role extras — treating catalog `primary_color` `#6366f1` as the YAML identity approximation rather than Cobalt `#0007cd` and rather than Electric Cyan `#00ffff` / Signal Blue `#0089ff` / Ocean Blue `#0096ff`, treating canvas `#0f0f0f` as unmerged from YAML button fg `#0f0f0f` and from live header-CTA fill `#000000`, treating surface `#000000` as unmerged from that live header fill, treating on-primary `#ffffff` as unmerged from foreground `#ffffff` and from YAML white-fill button bg `#ffffff`, treating YAML button radius 4 / padding `8px 24px` / body 16px as unmerged from footer live 0px / 6×8 / 33px / 14px·400, treating YAML `rounded.lg` 37 as unmerged from card 4px / inline 2px / full 9999 / live CTA 0px, treating Signal Blue `#0089ff` as a named Focus color role rather than a `focus-visible` treatment, and treating source-stated-likely-hover-paints-as-not-promoted — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification.

- Catalog `primary_color` `#6366f1` as an indigo approximation; YAML brand/primary Composio Cobalt `#0007cd`
- Void Black canvas `#0f0f0f` with Pure Black card interiors `#000000` and Charcoal hairline `#2c2c2c`
- Dual-font split: abcDiatype for content; JetBrains Mono for technical/code
- Ultra-tight heading line-heights (YAML `0.87`–`1.00`)
- Electric Cyan `#00ffff` at `rgba(0,255,255,0.12)` glows; Signal Blue `#0089ff`; Ocean Blue `#0096ff`
- Hard-offset brutalist shadow `rgba(0,0,0,0.15) 4px 4px 0px 0px` on select elements
- YAML white-fill primary (`#ffffff` / `#0f0f0f` / radius 4 / `8px 24px`) unmerged from footer live UPPERCASE CTAs (`#fff` / `#000` / 0px / 6×8 / 33px / 14px·400)
- Source-stated hover paints are written as “likely”; they are not promoted

### Principles

These 5 items — Terminal aesthetic is the register, with the UI implication don’t-soften-with-sentence-case-marketing and the extra the-brand-feels-like-a-CLI; Integration count is the headline, with the UI implication hero-modules-show-200+-tools-or-live-integration-grid; Code-first documentation, with the UI implication every-concept-page-leads-with-a-copy-paste-code-example; Electric Cyan glows are decorative depth, with the UI implication radial-gradient-cyan-glows-behind-cards-never-solid-cyan-fills-on-chrome; Two fonts, strict roles, with the UI implication never-mix-pick-the-surface-and-commit — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification.

1. **Terminal aesthetic is the register.** UPPERCASE CTAs + JetBrains Mono for technical content. *UI implication:* don’t soften with sentence-case marketing; the brand feels like a CLI.
2. **Integration count is the headline.** *UI implication:* hero modules show “200+ tools” or live integration grid, not vague claims.
3. **Code-first documentation.** *UI implication:* every concept page leads with a copy-paste code example.
4. **Electric Cyan glows are decorative depth.** *UI implication:* radial-gradient cyan glows behind cards, never solid cyan fills on chrome.
5. **Two fonts, strict roles.** abcDiatype for marketing, JetBrains Mono for technical. *UI implication:* never mix; pick the surface and commit.

The UI implications are this reference’s interpretation of source §12, not a separately published Composio UI specification. Treating those UI-implication tails as a derived editorial implementation inference rather than a separately published Composio UI specification is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do’s, including Void-Black-`#0f0f0f`-as-primary-page-background, heading-line-heights-0.87-1.0, white-opacity-borders-4-12%, Electric-Cyan-`#00ffff`-for-high-signal-moments-only, abcDiatype-paired-with-JetBrains-Mono, hard-offset-`4px 4px`-on-select-elements, button-text-dark-`oklch(0.145 0 0)`-even-on-the-darkest-backgrounds, opacity-based-borders-for-depth, uppercase-plus-letter-spacing-only-for-tiny-overline-labels-12px-or-smaller, never-pure-white-for-main-surfaces, compressed-authoritative-text-blocks, white-opacity-borders-more-important-than-shadows, Electric-Cyan-for-CTAs-glows-interactive-accents, pair-fonts-to-reinforce-developer-tool-identity, hard-offset-for-brutalist-personality, buttons-carry-their-own-surface, and opacity-borders-for-subtle-depth-without-shadows, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

- Use Void Black (`#0f0f0f`) as the primary page background — never pure white for main surfaces.
- Keep heading line-heights ultra-tight (0.87–1.0) for compressed, authoritative text blocks.
- Use white-opacity borders (4-12%) for containment — they’re more important than shadows here.
- Reserve Electric Cyan (`#00ffff`) for high-signal moments only — CTAs, glows, interactive accents.
- Pair abcDiatype with JetBrains Mono to reinforce the developer-tool identity.
- Use the hard-offset shadow (`4px 4px`) intentionally on select elements for brutalist personality.
- Keep button text dark (`oklch(0.145 0 0)`) even on the darkest backgrounds — buttons carry their own surface.
- Layer opacity-based borders to create subtle depth without shadows.
- Use uppercase + letter-spacing only for tiny overline labels (12px or smaller).

### Avoid

The following items copy source §7 Don’ts: no-bright-backgrounds-or-light-surfaces-as-primary-containers, no-heavy-shadows-everywhere, no-Composio-Cobalt-`#0007cd`-as-a-text-color, no-heading-line-heights-beyond-1.2, no-bold-700-for-body-or-heading-text, no-warm-colors, no-border-radius-larger-than-4px-on-content-cards, no-Electric-Cyan-at-full-opacity-on-large-surfaces, no-decorative-serif-or-handwritten-fonts, and no-skipping-JetBrains-Mono-for-technical-content, including the causal tails depth-comes-from-border-opacity-not-box-shadow, Cobalt-too-dark-on-dark-and-too-saturated-on-light, compressed-feel-is-core-to-the-identity, 400-500-is-the-weight-ceiling, palette-is-strictly-cool, near-square-corner-precision-is-intentional, Electric-Cyan-accent-at-12%-max-for-backgrounds, entire-identity-is-geometric-sans-plus-monospace, and JetBrains-Mono-is-a-credibility-signal-not-decoration. They are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification.

- Don’t use bright backgrounds or light surfaces as primary containers.
- Don’t apply heavy shadows everywhere — depth comes from border opacity, not box-shadow.
- Don’t use Composio Cobalt (`#0007cd`) as a text color — it’s too dark on dark and too saturated on light.
- Don’t increase heading line-heights beyond 1.2 — the compressed feel is core to the identity.
- Don’t use bold (700) weight for body or heading text — 400-500 is the ceiling.
- Don’t mix warm colors — the palette is strictly cool (blue, cyan, white, black).
- Don’t use border-radius larger than 4px on content cards — the precision of near-square corners is intentional.
- Don’t place Electric Cyan at full opacity on large surfaces — it’s an accent, used at 12% max for backgrounds.
- Don’t use decorative serif or handwritten fonts — the entire identity is geometric sans + monospace.
- Don’t skip the monospace font for technical content — JetBrains Mono is not decorative, it’s a credibility signal.

Treating the last reconstruction-boundary Avoid as not-presenting-fallback-faces-as-abcDiatype-or-JetBrains-Mono, and as not-merging-catalog-`#6366f1`-into-Cobalt-`#0007cd`, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.
- Do not present a fallback face as abcDiatype or as JetBrains Mono.
- Do not merge catalog `primary_color` `#6366f1` into Composio Cobalt `#0007cd`.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Treating YAML roles and catalog `primary_color` as listed first, including same-hex-two-named-jobs-stay-unmerged, body-§2-pairs-stay-beside-the-YAML-hex-not-averaged, and Signal-Blue-`#0089ff`-as-named-Focus-not-a-focus-visible-treatment, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML roles and catalog `primary_color` first. Same hex used for two named jobs stays unmerged. Body §2 pairs that add a second form stay beside the YAML hex; they are not averaged.

- **Catalog primary_color** (`#6366f1`): YAML identity field. Indigo approximation. Not YAML `primary` / `brand`.
- **Composio Cobalt / Primary / Brand** (`#0007cd`): YAML `primary` / `brand`. Core brand color, used sparingly for high-priority interactive elements and brand moments. It anchors the identity with quiet intensity. Source: do not use as a text color.
- **Electric Cyan / Accent cyan** (`#00ffff`): YAML `accent-cyan`. Attention-grabbing accent. Low-opacity use `rgba(0,255,255,0.12)` for glowing button backgrounds and card highlights. At full saturation, it serves as the energetic counterpoint to the dark canvas.
- **Signal Blue / Accent signal** (`#0089ff` / `rgb(0,137,255)`): YAML `accent-signal`. Select button borders and interactive focus states. Bridging Cobalt and Cyan. Named Focus in source §2; not a `focus-visible` treatment.
- **Ocean Blue / Accent ocean** (`#0096ff` / `rgb(0,150,255)`): YAML `accent-ocean`. Accent border color on CTA buttons, slightly warmer than Signal Blue.
- **Void Black / Canvas** (`#0f0f0f`): YAML `canvas`. Primary page background — not pure black, but a hair warmer, reducing eye strain on dark displays. Same hex appears as YAML white-fill button `fg`; named jobs stay unmerged.
- **Pure Black / Surface** (`#000000`): YAML `surface`. Card interiors and deep-nested containers, creating a subtle depth distinction from the page background. Same hex appears as the footer live header-CTA fill; named jobs stay unmerged.
- **Charcoal / Hairline** (`#2c2c2c` / `rgb(44,44,44)`): YAML `hairline`. Secondary button borders and divider lines on dark surfaces.
- **Pure White / Foreground** (`#ffffff`): YAML `foreground`. Primary heading and high-emphasis text on dark surfaces. Same hex as YAML `on-primary` and YAML white-fill button `bg`; named jobs stay unmerged.
- **On-primary** (`#ffffff`): YAML `on-primary`. Not merged into Foreground or into the white-fill button paint.
- **Muted Smoke** (`#444444`): YAML `muted`. De-emphasized body text, metadata, and tertiary content.
- **Light Border** (`#e0e0e0` / `rgb(224,224,224)`): YAML `light-border`. Reserved for light-surface contexts (rare on this site).
- **Ghost White** (`rgba(255,255,255,0.6)`): Secondary body text and link labels. Source also writes the range `rgba(255,255,255,0.5-0.6)` for semi-transparent white secondary text.
- **Whisper White** (`rgba(255,255,255,0.5)`): Tertiary button text and placeholder content.
- **Phantom White** (`rgba(255,255,255,0.2)`): Subtle button backgrounds and deeply receded UI chrome.
- **Border Mist 12** (`rgba(255,255,255,0.12)`): Highest-opacity border treatment — prominent card edges and content separators.
- **Border Mist 10** (`rgba(255,255,255,0.10)`): Standard container borders on dark surfaces.
- **Border Mist 08** (`rgba(255,255,255,0.08)`): Subtle section dividers and secondary card edges.
- **Border Mist 06** (`rgba(255,255,255,0.06)`): Near-invisible containment borders for background groupings.
- **Border Mist 04** (`rgba(255,255,255,0.04)`): The faintest border — atmospheric separation only.

Treating Cyan Glow / Blue-to-Black Fade / White Fog as source-stated gradient names rather than extra hexes, including bioluminescent-halos-behind-cards-and-feature-sections, hero-backgrounds-and-section-transitions, and atmospheric-horizon-line-effect-near-the-footer, and treating YAML component fields as staying on those controls, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Gradient system (source-stated names, no extra hex): Cyan Glow — radial gradients using `#00ffff` at very low opacity, creating bioluminescent halos behind cards and feature sections; Blue-to-Black Fade — linear gradients from Composio Cobalt (`#0007cd`) fading into Void Black (`#0f0f0f`), used in hero backgrounds and section transitions; White Fog — bottom-of-page gradient from dark to a diffused white/gray, creating an atmospheric “horizon line” effect near the footer.

Treating YAML white-fill button `fg` `#0f0f0f` / body button text `oklch(0.145 0 0)` / footer live CTA `#000` as component-fields-not-extra-general-inks, and treating Signal Blue `#0089ff` as a named Focus color role not copied onto a `focus-visible` row, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML white-fill button `fg` `#0f0f0f`, body button text `oklch(0.145 0 0)`, and footer live CTA `#000` stay on those controls. They are not extra general inks. Signal Blue `#0089ff` as “interactive focus states” stays a named Focus color role; it is not copied onto a `focus-visible` row.

### Spacing

Treating YAML spacing numbers as recorded without a required px suffix, treating body 1px / 2px / 4px / 6px / 8px / 10px / 12px / 14px / 16px / 18px / 20px / 24px / 30px / 32px / 40px as body-recorded observations rather than a converted YAML scale, treating `1px` / `2px` / `6px` / `10px` / `12px` / `14px` / `18px` / `20px` / `30px` as body-only-not-YAML-spacing-keys, treating YAML `section` 80 as unmerged from body `80-120px`, and treating estimated-80-120px-and-card-internal-24-32px as source-stated observations not a global gutter, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML `spacing` (numbers without a required px suffix; none is added here): xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 40, section 80.

Body §5: base unit 8px. Scale: 1px, 2px, 4px, 6px, 8px, 10px, 12px, 14px, 16px, 18px, 20px, 24px, 30px, 32px, 40px. Component padding: typically 10px (buttons) to 24px (CTA buttons horizontal). Section padding: generous vertical spacing (estimated `80-120px` between major sections). Card internal padding: approximately 24-32px.

### Shape

YAML `rounded`: sm 2, md 4, lg 37, full 9999.

Treating Body §5 Nearly-squared-2px / Subtly-rounded-4px / Pill-shaped-37px / Full-round-9999px+ as a local layout scale rather than averaging it with YAML or with live CTA 0px or with §4 “likely 4px”, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Body §5 layout scale (not averaged with YAML, with live CTA 0px, or with §4 likely-4px):

- Nearly squared (2px): Inline code spans, small tags, pre blocks — the sharpest treatment, conveying technical precision
- Subtly rounded (4px): Content cards, images, standard containers — the workhorse radius
- Pill-shaped (37px): Select buttons and badges — creates a softer, more approachable feel for key CTAs
- Full round (9999px+): Circular elements, avatar-like containers, decorative dots

Treating YAML `full` 9999 and body `9999px+` as both kept, and treating YAML white-fill radius 4 / cyan/ghost radius 4 / live footer CTA 0px / §4 “likely 4px” as unmerged local geometries rather than a universal radius, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML `full` 9999 and body `9999px+` are both kept. YAML white-fill radius 4, cyan/ghost radius 4, live footer CTA 0px, and §4 “likely 4px” stay unmerged local geometries. They are not a universal radius.

### Elevation

YAML `shadow`:

| Token | Value |
|---|---|
| brutalist | `rgba(0,0,0,0.15) 4px 4px 0px 0px` |
| floating | `rgba(0,0,0,0.5) 0px 8px 32px` |
| glow-cyan | `rgba(0,255,255,0.12) radial halo` |

Treating the body brutalist row as keeping no `0px 0px` suffix and as not rewritten into the YAML `rgba(0,0,0,0.15) 4px 4px 0px 0px` form, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Body §6:

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow, no border | Page background, inline text |
| Contained (Level 1) | Border Mist 04-08, no shadow | Background groupings, subtle sections |
| Card (Level 2) | Border Mist 10-12, no shadow | Standard content cards, code blocks |
| Brutalist (Level 3) | Hard offset shadow (`4px 4px`, 15% black) | Select interactive cards, distinctive feature highlights |
| Floating (Level 4) | Soft diffuse shadow (`0px 8px 32px`, 50% black) | Modals, overlays, deeply elevated content |

The following elevation readings — shadows-used-sparingly, hard-offset-brutalist-shadow-is-the-signature, soft-diffuse-reserved-for-truly-floating-elements, most-depth-through-border-opacity-gradations-rather-than-shadows, decorative-depth-Cyan-Glow-Halos-creating-a-screen-glow-effect, Blue-Black-Gradient-Washes-adding-subtle-color-temperature-shifts, and White-Fog-Horizon-creating-an-atmospheric-dawn-effect-before-the-footer — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification. Source §6 reads shadows as used sparingly: the hard-offset brutalist shadow is the signature; the soft diffuse shadow is reserved for truly floating elements; most depth is communicated through border opacity gradations rather than shadows. Decorative depth named in source: Cyan Glow Halos — radial gradient halos using Electric Cyan at low opacity behind feature cards and images, creating a “screen glow” effect as if the UI elements are emitting light; Blue-Black Gradient Washes — linear gradients from Composio Cobalt to Void Black used as section backgrounds, adding subtle color temperature shifts; White Fog Horizon — a gradient from dark to diffused white/gray at the bottom of the page, creating an atmospheric “dawn” effect before the footer.

### Motion

Treating source token extraction as `prose-derived`, treating duration names/values / easing-role prose / signature-motion prose / reduced-motion behavior as source-stated reconstruction rather than computed evidence, and treating the reconstruction-vs-omission split as the portable Motion contract, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Source token extraction is `prose-derived`. Duration names/values, easing-role prose, signature-motion prose, and reduced-motion behavior remain as source-stated reconstruction. No cubic-bezier string exists in the source.

Source-stated duration roles:

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Toggle |
| `motion-fast` | 100ms | Hover (source: faster than typical for terminal feel) |
| `motion-glow-pulse` | 1500ms | Cyan glow heartbeat behind hero elements |
| `motion-standard` | 250ms | Modal, panel |

Source-stated easing: “terminal-precise.” No curve is recorded. Treating “terminal-precise” as a source-stated easing role rather than an invented cubic-bezier is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

Treating the following signature motions as source-stated-not-computed-evidence, including glow-pulse-never-on-input-fields, hover-faster-than-typical-as-a-causal-tail-not-a-computed-curve, and reduced-motion-glow-pulse-becomes-static, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Signature motions (source-stated):

1. **Glow pulse.** `motion-glow-pulse` 1500ms cyan glow heartbeat behind hero elements. Glow pulses never animate on input fields.
2. **Hover.** `motion-fast` 100ms. Source writes hover as faster than typical for terminal feel; that causal tail is not a computed curve.
3. **Reduce motion.** Under `prefers-reduced-motion: reduce`, glow pulse is removed (becomes static).

Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including abcDiatype-and-JetBrains-Mono-as-named-families-in-YAML-and-§3, live-DOM-footer-as-playwright-getComputedStyle-on-UPPERCASE-CTAs-at-14px·400-abcDiatype, fallback-stacks-as-declared-only-not-the-brand-face, and do-not-present-the-system-stack-as-abcDiatype-or-JetBrains-Mono, are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Source footer: composio.dev live DOM via playwright — UPPERCASE CTAs at 14px·400. Sibling Proof records abcDiatype on those CTAs. |
| Named families | YAML/§3: `abcDiatype`; `JetBrains Mono`. |
| Declared-only | YAML/§3 fallback stacks: `abcDiatype Fallback, ui-sans-serif, system-ui, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol, Noto Color Emoji`; `JetBrains Mono Fallback, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, Liberation Mono, Courier New`; system monospace `Menlo`, `monospace` for smallest inline code. |

### Family

- **Primary / YAML `family.sans`:** `abcDiatype`
- **Primary stack (§3 body):** `abcDiatype`, with fallbacks: `abcDiatype Fallback, ui-sans-serif, system-ui, Apple Color Emoji, Segoe UI Emoji, Segoe UI Symbol, Noto Color Emoji`
- **Monospace / YAML `family.mono`:** `JetBrains Mono`
- **Monospace stack (§3 body):** `JetBrains Mono`, with fallbacks: `JetBrains Mono Fallback, ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, Liberation Mono, Courier New`
- **System Monospace (fallback):** `Menlo`, `monospace` for smallest inline code

Treating YAML family names and the §3 stacks as both kept, including do-not-collapse-fallback-faces and do-not-present-a-fallback-face-as-abcDiatype-or-JetBrains-Mono, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML family names and the §3 stacks are both kept. Do not collapse the fallback faces out of the stacks, and do not present a fallback face as abcDiatype or as JetBrains Mono.

The following type-character readings — compression-creates-authority-making-large-text-feel-dense-and-commanding-rather-than-airy-and-decorative, dual-personality-abcDiatype-geometric-precise-friendly-versus-JetBrains-Mono-credible-functional-familiar-to-developers, weight-restraint-400-with-500-for-small-labels, negative-letter-spacing-on-code-for-dense-compact-code-blocks-that-feel-like-a-real-IDE, and uppercase-is-earned — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification. Source §3 principles: **Compression creates authority** — heading line-heights are drastically tight (0.87-1.0), making large text feel dense and commanding rather than airy and decorative. **Dual personality** — abcDiatype carries the marketing voice (geometric, precise, friendly); JetBrains Mono carries the technical voice (credible, functional, familiar to developers). **Weight restraint** — almost everything is weight 400; weight 500 is reserved for small labels, badges, and select card titles; weight 700 appears only in microscopic system-monospace contexts. **Negative letter-spacing on code** — JetBrains Mono uses negative letter-spacing (`-0.28px` to `-0.98px`) for dense, compact code blocks that feel like a real IDE. **Uppercase is earned** — `uppercase` + `letter-spacing` is reserved exclusively for tiny overline labels and technical tags — never for headings.

### Type roles

Treating unitless YAML lineHeight ratios as preserved rather than rewritten as the body-table px figures, treating body px and rem conversions as Notes-column observations, treating YAML tracking numbers and body px tracking as both kept, treating body-only Micro / Code Caption / Code Overline / Code Micro / Code Nano as not YAML keys, and treating Body/Button 16px·400 as unmerged from footer live 14px·400, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Verified line-height values include the unitless YAML ratios `0.87`, `1.00`, `1.20`, `1.50`, and `1.63`. They scale with font size and are not rewritten as fixed px. Body-only ratios `1.43` and `1.33` stay on Code Overline / Code Micro / Code Nano. YAML tracking `0.3` / `-0.32` / `-0.28` and body `0.3px` / `-0.32px` / `-0.28px` / `0.7px` / `0.55px` / `0.45-0.5px` are both kept.

| Role | Font | Size | Weight | Line height | Tracking | Use (YAML) / Notes |
|---|---|---:|---:|---:|---:|---|
| Display / Hero | abcDiatype | 64px (4rem) | 400 | 0.87 | YAML omitted / body normal | Massive compressed hero headings |
| Section Heading | abcDiatype | 48px (3rem) | 400 | 1.00 | YAML omitted / body normal | Major feature section titles |
| Sub-heading Large | abcDiatype | 40px (2.5rem) | 400 | 1.00 | YAML omitted / body normal | Secondary section markers |
| Sub-heading | abcDiatype | 28px (1.75rem) | 400 | 1.20 | YAML omitted / body normal | Card titles, feature names |
| Card Title | abcDiatype | 24px (1.5rem) | 500 | 1.20 | YAML omitted / body normal | Medium-emphasis card headings |
| Feature Label | abcDiatype | 20px (1.25rem) | 500 | 1.20 | YAML omitted / body normal | Smaller card titles, labels |
| Body Large | abcDiatype | 18px (1.125rem) | 400 | 1.20 | YAML omitted / body normal | Intro paragraphs |
| Body / Button | abcDiatype | 16px (1rem) | 400 | 1.50 | YAML omitted / body normal | Body text, nav links, buttons. Footer live CTAs are 14px·400; unmerged |
| Body Small | abcDiatype | 15px (0.94rem) | 400 | 1.63 | YAML omitted / body normal | Longer-form body text |
| Caption | abcDiatype | 14px (0.875rem) | 400 | 1.63 | YAML omitted / body normal | Descriptions, metadata |
| Label | abcDiatype | 13px (0.81rem) | 500 | 1.50 | YAML omitted / body normal | UI labels, badges |
| Tag / Overline | abcDiatype | 12px (0.75rem) | 500 | 1.00 | YAML `0.3` / body `0.3px` | Uppercase overline labels |
| Micro | abcDiatype | 12px (0.75rem) | 400 | 1.00 | body `0.3px` | Smallest sans-serif text. Body-only; not a YAML key |
| Code Body | JetBrains Mono | 16px (1rem) | 400 | 1.50 | YAML `-0.32` / body `-0.32px` | Inline code, terminal output |
| Code Small | JetBrains Mono | 14px (0.875rem) | 400 | 1.50 | YAML `-0.28` / body `-0.28px` | Code snippets, technical labels |
| Code Caption | JetBrains Mono | 12px (0.75rem) | 400 | 1.50 | body `-0.28px` | Small code references. Body-only |
| Code Overline | JetBrains Mono | 14px (0.875rem) | 400 | 1.43 | body `0.7px` | Uppercase technical labels. Body-only |
| Code Micro | JetBrains Mono | 11px (0.69rem) | 400 | 1.33 | body `0.55px` | Tiny uppercase code tags. Body-only |
| Code Nano | JetBrains Mono | 9-10px | 400 | 1.33 | body `0.45-0.5px` | Smallest monospace text. Body-only |

### Assets

Treating catalog logo metadata as a GitHub slug lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Catalog logo metadata is GitHub type, slug `composiohq`. It is not a captured first-party mark file.

Treating dark-themed product screenshots and UI mockups as sitting within bordered containers matching the card system, including product-screenshots-scale-proportionally-within-their-containers, dark-themed-images-maintain-contrast-on-the-dark-background-at-all-sizes, gradient-glow-effects-scale-with-container-size, blue/cyan-gradient-glows-behind-or-beneath-feature-images, no-visible-border-radius-on-images-beyond-container-rounding-4px, full-bleed-within-their-card-containers, and no-visible-art-direction-changes-between-breakpoints-same-crops-proportional-scaling, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Product screenshots scale proportionally within their containers. Dark-themed images maintain contrast on the dark background at all sizes. Gradient glow effects scale with container size. Dark-themed product screenshots and UI mockups sit within bordered containers matching the card system. Blue/cyan gradient glows sit behind or beneath feature images. No visible border-radius on images beyond container rounding (4px). Images are full-bleed within their card containers. No visible art direction changes between breakpoints — same crops, proportional scaling.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, and treating YAML `components_harvested: true` as a harvest flag rather than complete state coverage, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

Treating YAML `components_harvested` as true, treating token extraction as `prose-derived`, treating the source footer as a later playwright getComputedStyle overlay on UPPERCASE CTAs, and treating source §4 “likely” hover paints as not promoted, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML `components_harvested` is true. Token extraction is `prose-derived`. The source footer later records playwright getComputedStyle on UPPERCASE CTAs. Source §4 writes several hover paints as “likely”; those likely paints are not promoted.

Source §14 state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| Empty (no agents) | UPPERCASE CTA "CREATE FIRST AGENT" + integration grid preview |
| Empty (no integrations) | "BROWSE 200+ INTEGRATIONS" with searchable grid |
| Loading (integration installing) | OAuth flow with provider-specific UI; progress visible |
| Loading (agent running) | Per-step trace with tool calls visible |
| Error (tool failed) | Full stack trace + provider error code + retry button |
| Error (auth) | "RECONNECT [Tool]" with OAuth re-trigger |
| Success (integration installed) | Cyan glow pulse on integration card; toast "INSTALLED" |
| Success (agent run) | Trace expanded showing all tool calls + final output |
| Skeleton (integration grid) | Dark cards with subtle cyan border |
| Disabled (rate limit) | 0.5 opacity + "UPGRADE" CTA |
| Loading (model inference) | Per-token streaming visible alongside tool-call trace |

Treating Signal Blue `#0089ff` as a color role for interactive focus states, not `focus-visible` treatment evidence, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. YAML `accent-signal` `#0089ff` is a color role for select button borders and interactive focus states, not `focus-visible` treatment evidence.

Treating those §14 rows as source-stated state copy rather than live interaction captures on home/pricing, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, treating YAML `type: card` as having no interactive-kind confirmation for a §4.4 map, treating Phantom Button as a source-named button primitive even without a YAML row, treating Navigation as a container whose links and white-fill CTA are judged as children, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source never records `focus-visible`; that applicability stays, and the visual treatment is omitted. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. Card and Code Block have YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Phantom Button keeps `Type: button` from the source §4 primitive name. Navigation is a container; kind and a state map are omitted on the bar. Ghost Charcoal and Phantom omit loading/error/success because exact request/destination/outcome is unresolved. This is not a complete state-coverage claim.

Treating the source footer live-DOM overlay as an evidence-domain note rather than one aggregated renderable control, and treating adopted Proof CTA tuples as staying on the provenance Proof ledger control-separated rather than bound onto one portable Footer component, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. The source footer records a later live-DOM overlay on composio.dev home + pricing: UPPERCASE CTAs `#fff` / `#000` / 0px radius / 6×8 padding / 33px / 14px·400. That overlay is an evidence-domain note, not one aggregated renderable control. Adopted Proof names separate controls; those Proof tuples stay on the provenance Proof ledger and are not bound onto one portable Footer component.

Treating source §4 Inputs & Forms as naming no explicit input token data, and treating the “likely follow” pattern as not a declared input component, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Source §4 Inputs & Forms: no explicit input token data extracted. No input component is declared from the “likely follow” pattern.

### YAML White-fill Primary Button

- Role: white-fill primary CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#0f0f0f`
- Radius: 4
- Padding: `8px 24px`
- Font: YAML body/button `16px / 400 / abcDiatype`
- Border: none
- YAML use: `White fill, near-black text, no border`
- Source-stated / prose-derived: YAML default; source §4 also writes text as `oklch(0.145 0 0)` and radius as “likely 4px”; hover written as “likely”
- Field note: The following unmerged-field reading, including YAML-`#ffffff`-fill-not-on-primary-job-not-foreground-job, YAML-fg-`#0f0f0f`-not-canvas-job-not-`oklch(0.145 0 0)`-not-footer-`#000`, YAML-radius-4-not-live-0px-not-card-4px-as-a-second-paint, YAML-padding-`8px 24px`-not-footer-6×8, YAML-16px-400-not-footer-14px·400, and likely-hover-not-promoted, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. `#ffffff` is this control’s fill. `#0f0f0f` is YAML `fg`. Radius 4 is the YAML field. Padding `8px 24px` is the YAML field. Footer live UPPERCASE CTAs remain a separate evidence domain (0px / 6×8 / 33px / 14px·400 / `#fff` / `#000`).

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML harvested white-fill primary |
| hover | applicable | Pointer-web button; source “likely” paint not promoted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary CTA can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this white-fill primary because exact destination, request, and outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a white-fill primary CTA; exact destination, request, and outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Treating the source §9 matching-padding Signal-Blue secondary as this white-fill primary’s local relation, and as not-the-Cyan-hero-pairing and not-a-prompt-wrapper, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Source §9-only local recipe on this white-fill primary: place it next to a secondary button with transparent background, Signal Blue border, and matching padding (`8px` vertical, `24px` horizontal). That matching-padding Signal-Blue pair is this control’s local relation; it is not the Cyan hero pairing and is not a prompt wrapper.

### Cyan Accent CTA

- Role: electric-cyan accent CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `rgba(0,255,255,0.12)`
- Text: `oklch(0.145 0 0)`
- Border: `1px solid rgb(0,150,255)`
- Radius: 4
- Padding: `8px 24px`
- YAML use: `Electric cyan 12% bg, 1px ocean blue #0096ff border, glow-from-within`
- Source-stated / prose-derived: YAML + §4 default; hover not recorded beyond “likely” on other buttons
- Field note: The following unmerged-field reading, including `rgba(0,255,255,0.12)`-as-this-control-fill-not-a-second-Electric-Cyan-paint, `oklch(0.145 0 0)`-not-YAML-white-fill-fg-`#0f0f0f`, 1px-`rgb(0,150,255)`-not-Signal-Blue-`rgb(0,137,255)`, radius-4-not-live-0px, glow-from-within-as-YAML-use-not-a-computed-animation-name, and white-CTA-plus-cyan-accented-secondary-below-64px-0.87-hero-as-§9-local-recipe-not-a-prompt-wrapper-and-not-the-matching-padding-Signal-Blue-pair, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Fill `rgba(0,255,255,0.12)` is this control. Border `1px solid rgb(0,150,255)` is Ocean Blue on this control. Radius 4 is YAML `button-cyan`. Source §9 also pairs a white CTA with a cyan-accented secondary below a 64px / 0.87 hero; that hero pairing is a local recipe, not a prompt wrapper, and it does not replace the white-primary + transparent Signal-Blue secondary matching-padding relation.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML harvested cyan accent CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A cyan accent CTA can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this cyan accent CTA because exact destination, request, and outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as an electric-cyan accent CTA; exact destination, request, and outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Ghost / Outline Button

- Role: transparent outline button
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `oklch(0.145 0 0)`
- Radius: 4
- Padding: `10px`
- YAML use: `Transparent fill, 1px signal blue #0089ff or charcoal #2c2c2c border`
- Source-stated / prose-derived: YAML default; two source-stated border variants
- Field note: The following unmerged-field reading, including Signal-Blue-border-`1px solid rgb(0,137,255)`-unmerged-from-Charcoal-border-`1px solid rgb(44,44,44)`, padding-`10px`-not-white-fill-`8px 24px`, radius-4-not-live-0px, and `#0089ff`-as-this-control-border-not-a-`focus-visible`-treatment, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Padding `10px` is YAML `button-ghost`. Signal Blue `1px solid rgb(0,137,255)` and Charcoal `1px solid rgb(44,44,44)` are both source-stated variants; they are not averaged. Source §4 Charcoal ghost is for secondary/tertiary actions on dark surfaces; exact destination, request, and outcome are unresolved.

Treating loading, error, and success applicability as omitted on this ghost / outline button because exact request, destination, and outcome mapping is unresolved, rather than inventing a secondary destination control and closing those fields as not-applicable, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML harvested ghost / outline button |
| hover | applicable | Pointer-web button; source “likely fill or border color shift” not promoted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An outline button can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names the Charcoal variant as for secondary/tertiary actions on dark surfaces; exact request/destination/outcome mapping is unresolved, so those three fields stay omitted at this boundary.

### Phantom Button

- Role: deeply de-emphasized action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `rgba(255,255,255,0.2)`
- Text: `rgba(255,255,255,0.5)`
- Border: no visible border
- Source-stated / prose-derived: source §4 Phantom Button; YAML has no phantom row
- Field note: Treating Type as button from the source §4 primitive name Phantom Button even though YAML has no phantom row, treating Phantom-White-fill-as-this-control-not-a-second-Foreground, and treating Whisper-White-text-as-this-control-fg, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Fill `rgba(255,255,255,0.2)` and text `rgba(255,255,255,0.5)` are this control. Source §4 uses Phantom Button for deeply de-emphasized actions; exact request, destination, and outcome are unresolved.

Treating loading, error, and success applicability as omitted on this Phantom Button because exact request, destination, and outcome mapping is unresolved, rather than closing those fields as not-applicable from de-emphasis alone, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Source §4 Phantom Button |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A de-emphasized action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as used for deeply de-emphasized actions; exact request/destination/outcome mapping is unresolved, so those three fields stay omitted at this boundary.

### Card

- Role: content card / container
- Type: card
- Anatomy: surface
- Background: `#000000` or transparent
- Border: white at very low opacity, Border Mist 04 `rgba(255,255,255,0.04)` to Border Mist 12 `rgba(255,255,255,0.12)` depending on prominence
- Radius: 4px for content cards; 2px for inline elements
- YAML use: `Pure black surface, white-opacity 4-12% border, optional brutalist shadow`
- Source-stated / prose-derived: YAML + §4 default; hover written as “likely”
- Field note: The following unmerged-field reading, including `#000000`-as-this-card-fill-not-live-header-CTA-fill, 4px-not-YAML-lg-37-not-live-CTA-0px, 2px-as-inline-not-card-4px, brutalist-`rgba(0,0,0,0.15) 4px 4px 0px 0px`-optional-on-this-card-not-floating-`rgba(0,0,0,0.5) 0px 8px 32px`, likely-hover-not-promoted, and §9-only-near-black-`#000000`-10%-white-border-4px-hard-offset-24px-500-Ghost-White-60%-16px-as-local-recipe-not-a-prompt-wrapper, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. `#000000` is this card’s fill (YAML `bg`). Radius 4px is YAML `card` / content-card geometry. Source §9-only local recipe: near-black `#000000`, barely visible white border at 10% opacity, 4px corners, hard-offset shadow (4px right, 4px down, 15% black), title 24px weight 500, Ghost White (60% opacity) description at 16px.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Code Block

- Role: code snippet / terminal preview
- Type: card
- Anatomy: surface + monospace content
- Background: `#000000`
- Radius: 4
- Font: JetBrains Mono
- YAML use: `JetBrains Mono, white-opacity 10% border, syntax-highlighted`
- Source-stated / prose-derived: YAML + §4 default
- Field note: The following unmerged-field reading, including `#000000`-as-this-block-fill-not-a-second-Surface-paint, Border-Mist-10-`rgba(255,255,255,0.10)`-as-this-block-border, 4px-not-inline-2px, JetBrains-Mono-14px-`-0.28px`-as-§9-local-recipe-unmerged-from-YAML-code-16px-`-0.32`, and white-and-cyan-syntax-text-as-§9-local-recipe, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. `#000000` is YAML `code-block` `bg`. Source §9-only local recipe: JetBrains Mono at 14px with `-0.28px` letter-spacing on a black background, Border Mist 10, 4px radius, syntax-highlighted content with white and cyan text.

Treating this code block as having YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Kind and a state-applicability map are omitted.

### Navigation

- Role: sticky top nav container on dark/black background
- Anatomy: wordmark + links + white-fill CTA
- Background: dark/black
- Nav-link text: `#ffffff` at standard body size (16px, abcDiatype)
- Bottom border: Border Mist 06-08
- Source-stated / prose-derived: source §4 default; mobile written as hamburger collapse
- Field note: Treating this sticky top nav as a container rather than one destination item, treating Type as not invented on the bar because YAML has no nav row, treating 16px-white-abcDiatype-links-as-child-links-not-footer-14px·400-CTAs, treating the-nav-CTA-as-White-Fill-Primary-style-not-a-second-YAML-button, and treating §9-only-4-5-nav-links-plus-Border-Mist-06-bottom-border-plus-wordmark-on-the-left-and-white-fill-CTA-on-the-right as a local recipe, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Kind and a state-applicability map are omitted on the container. Logo is a white SVG Composio wordmark on the left. Source §9-only local recipe: Void Black nav, wordmark in white on the left, 4-5 nav links in white abcDiatype at 16px, white-fill CTA on the right, Border Mist 06 bottom border.

#### Navigation links (child)

- Role: destination nav links inside the sticky top nav
- Kind: interactive
- Anatomy: label
- Text: `#ffffff` at 16px abcDiatype
- Source-stated / prose-derived: source §4 nav links

Treating these nav links as destination/select children, so loading, error, and success are not-applicable on the link itself, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Source §4 nav links |
| hover | applicable | Pointer-web nav link; visual treatment omitted |
| focus-visible | applicable | Interactive link; visual treatment omitted |
| disabled | applicable | A nav link can be unavailable; visual treatment omitted |
| loading | not-applicable | A public nav link selects a destination; the link itself does not enter a loading state |
| error | not-applicable | Nav-link selection is not a validation or request-failure state on the link |
| success | not-applicable | Following a nav link is not an action-outcome confirmation on the link |

#### Navigation white-fill CTA (child)

- Role: white-fill primary CTA inside the sticky top nav
- Kind: interactive
- Type: button
- Source-stated / prose-derived: source §4 “CTA button in the nav: White Fill Primary style” — same YAML white-fill primary, not a second paint
Treating loading, error, and success applicability on this nav white-fill CTA as following the YAML White-fill Primary Button omission because exact destination, request, and outcome mapping is unresolved, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.
- Loading, error, and success applicability follow the YAML White-fill Primary Button (omitted; exact destination/request/outcome unresolved)

### Distinctive surfaces (no YAML type; no kind/map)

Treating Stats/Metrics Display, Integration/Partner Logos Grid, and “COMPOSIO” Brand Display as source-stated distinctive surfaces without YAML `type` and without interactive-kind evidence, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

- **Stats/Metrics Display.** Large monospace numbers (JetBrains Mono) — "10k+" style. Tight layout with subtle label text beneath.
- **Integration/Partner Logos Grid.** Grid layout of tool logos on dark surface, contained within a bordered card. Demonstrates ecosystem breadth.
- **“COMPOSIO” Brand Display.** Oversized brand typography — likely the largest text on the page. Used as a section divider/brand statement. Stark white on black.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML-spacing-xs-4-sm-8-md-16-base-16-lg-24-xl-32-xxl-40-section-80-without-a-required-px-suffix, body-8px-base-and-1px-through-40px-scale, max-container-approximately-1200px-centered, single-column-or-2-3-column-feature-cards, hero-centered-single-column, and asymmetric-feature-sections-mixing-text-blocks-with-product-screenshots — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification. YAML spacing is xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 40, section 80. Body layout names an 8px base unit and the 1px–40px scale above. Max container width: approximately 1200px, centered. Content sections use single-column or 2-3 column grids for feature cards. Hero: centered single-column. Feature sections: asymmetric layouts mixing text blocks with product screenshots.

Whitespace philosophy (source-stated): large vertical gaps create distinct “chapters”; cards and text blocks are internally compact (tight line-heights, minimal internal padding); border opacity differences and subtle background shifts delineate content zones. Treating that philosophy as source §5 rather than a separately published layout specification, including large-vertical-gaps-as-chapters, cards-and-text-blocks-internally-compact, and border-opacity-and-background-shifts-delineate-zones, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification.

Source §8 breakpoints (source-stated; not a captured cross-viewport pass):

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <768px | Single column, hamburger nav, full-width cards, reduced section padding, hero text scales down to ~28-40px |
| Tablet | 768-1024px | 2-column grid for cards, condensed nav, slightly reduced hero text |
| Desktop | 1024-1440px | Full multi-column layout, expanded nav with all links visible, large hero typography (64px) |
| Large Desktop | >1440px | Max-width container centered, generous horizontal margins |

Touch targets: minimum `44x44px` for all interactive elements. Buttons use comfortable padding (`8px 24px` minimum). Nav links spaced with sufficient gap for thumb navigation.

Collapsing strategy (source-stated): navigation full horizontal on desktop collapses to hamburger on mobile; feature grids 3-column → 2-column → single-column; hero text 64px → 40px → 28px; section padding reduces proportionally; cards stack vertically on mobile with full-width treatment; code blocks horizontal scroll on smaller viewports rather than wrapping.

Treating those 768px / 1024px / 1440px / 1200px / `44x44px` / 64px → 40px → 28px figures as source-stated layout copy rather than a captured cross-viewport specification, including comfortable-padding-`8px 24px`-minimum, sufficient-gap-for-thumb-navigation, hamburger-nav-on-mobile, feature-grids-3-to-2-to-single-column, cards-stack-full-width, code-blocks-horizontal-scroll-rather-than-wrapping, section-padding-reduces-proportionally, and not-a-second-live-mobile-capture-from-this-prose-derived-packet, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. The harvested control sizes above are source-stated measurements, not a second live mobile capture.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating Composio’s voice as terminal-confident and technically precise, including marketing-copy-that-reads-like-a-developer-changelog, capability-statements-no-hype, hero-CTAs-that-use-uppercase, brand-register-that-borrows-terminal-vocabulary, uppercase-would-feel-aggressive-on-most-brands-but-reads-as-command-line-aesthetic-here, and signal-this-is-for-developers-building-agent-infrastructure, is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. Source §10 records the voice as **terminal-confident and technically precise.** Marketing copy reads like a developer changelog — capability statements, no hype. Hero CTAs use uppercase ("GET STARTED", "ADD TO MY AGENT", "TRY IT OUT") which on most brands would feel aggressive but on Composio reads as "command line aesthetic" — the brand register intentionally borrows terminal vocabulary to signal "this is for developers building agent infrastructure."

The following voice-table directions — CTA-UPPERCASE-imperative, Marketing-capability-plus-integration-list, Documentation-code-block-first, Error-stack-trace-acceptable — are a derived editorial implementation inference from the verified surfaces; they are not Composio-authored or a separately published UI specification.

| Context | Tone |
|---|---|
| CTA | UPPERCASE imperative. "GET STARTED", "ADD TO MY AGENT", "TRY IT OUT" |
| Marketing | Capability + integration list. "X tools, Y agents, Z protocols" |
| Documentation | Code-block-first; minimal prose between examples |
| Error | Stack-trace-acceptable. Technical users want the actual error |

**Voice samples**

- Marketing CTA: *"GET STARTED FOR FREE"* / *"ADD TO MY AGENT"* — source §10, verified composio.dev homepage 2026-05

Treating the following forbidden-register items as source §10 register — consumer-AI-hype-magic-and-AI-revolution, apology-theatre, and excessive-emoji — is a derived editorial implementation inference from the verified surfaces; it is not Composio-authored or a separately published UI specification. **Forbidden phrases.** Consumer-AI hype ("magic", "AI revolution"). Apology theatre. Excessive emoji.

Source-stated samples above are *"GET STARTED FOR FREE"* / *"ADD TO MY AGENT"* from source §10.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- YAML white-fill radius 4 / padding `8px 24px` / 16px·400 versus footer live 0px / 6×8 / 33px / 14px·400 (unmerged; not resolved here)
- catalog `primary_color` `#6366f1` versus YAML Cobalt `#0007cd` (unmerged)
- hover visual treatments written as “likely”; `focus-visible` visual treatments (named Focus `#0089ff` is not that evidence)
- YAML White-fill Primary / Cyan Accent CTA / Ghost / Phantom loading·error·success applicability (exact request/destination/outcome unresolved)
- interactive kind and state-applicability map for Card, Code Block, and the Navigation container
- YAML `type` for Navigation container, Stats/Metrics, Integration grid, and “COMPOSIO” Brand Display
- explicit input tokens (source: no explicit input token data extracted)
- cubic-bezier easing curves
- motion duration, easing, animation name, transition properties, and reduced-motion behavior beyond the source-stated table — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
