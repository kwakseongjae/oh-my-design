# Appier Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Appier is a Taiwan-founded, Tokyo-listed enterprise AI company. Its software helps businesses turn AI into ROI through marketing, personalization, and data clouds. Catalog homepage identity is `https://www.appier.com`. The following coverage assignment is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. This contract covers that named first-party marketing reconstruction as recorded in the source.

Exact values below are limited to the source’s stated reconstruction. Token extraction is `prose-derived`. Live computed-style inspection of appier.com was not completed this pass: the inspection browser session redirected unreliably; WebFetch confirmed a blue-dominant identity (blue nav/CTAs/UI, “white logo on blue background” per a brand-asset search) but not exact hexes. Catalog `primary_color` `#1D2EFF` is the creation-brief-provided value. Hexes other than that primary, and the secondary/semantic palette, are recorded in the source as well-grounded B2B approximations pending live re-inspection. The following matching and reconstruction-authority readings are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. That brief-provided primary is recorded as matching Appier’s known electric-/Klein-adjacent blue identity, and this reconstruction is not treated as an Appier-authored specification.

The following atmosphere readings (predominantly-white high-contrast canvas; clean enterprise futurism / Klein-adjacent; consumer-warm-vs-pure-charged; living-animated three-cloud systems; card-based grid-disciplined IA) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. The recorded interface is a predominantly white, high-contrast canvas anchored by `#1D2EFF`. The source names the atmosphere **clean enterprise futurism** and reads that blue as close to International Klein Blue — saturated, intelligent, and unmistakably “AI.” Where consumer brands warm their blues toward friendliness, the reconstruction keeps this blue pure and charged. Typography is recorded as a clean, modern sans (system or near-system stack) with bold-headline / regular-body hierarchy, English as the primary global voice and Traditional Chinese for the TW market. Visual storytelling leans on animated product demos and gradient motion: the three clouds (Ad / Personalization / Data) as living, animated systems, with subtle blue gradient animation. Layout is card-based and grid-disciplined: case-study cards, solution tiles, tabbed industry results.

Public facts recorded in the source from `https://www.appier.com/en/about` and appier.com via WebFetch 2026-05-19, plus press via WebSearch 2026-05-19 (metrics not independently audited; not independently audited): founded **2012** with the vision **“Making AI easy by making software intelligent.”** Customer roster recorded as global enterprises including **L’Oréal, BMW, and Nexon**. Credentials recorded: Gartner, ISO 27001, TAG Anti-Fraud. Listing publicly on the Tokyo Stock Exchange.

The following founders-thesis, three-cloud product-story-span, prominent-deep-tech-success-story, and not-interface-token readings are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. The founders are recorded as a team with deep AI research roots; the thesis is that artificial intelligence’s value would come from being operationalized into everyday business software so ordinary marketing and growth teams could capture ROI without building their own ML stacks. Appier grew from an AI-for-advertising startup in Taipei into a multi-product enterprise platform spanning **Ad Cloud, Personalization Cloud, and Data Cloud**, each now infused with what Appier calls **agentic AI**, and became one of Taiwan’s most prominent deep-tech success stories. Those facts are not interface tokens.

The following product-expression readings (design-language-as-thesis; charged-blue-without-robot-clichés; organized-explainable-complexity; one-place-futuristic; not-we-have-AI-but-turn-AI-into-ROI; metaphors-do-not-derive-tokens) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. The source’s narrative reads the design language as the surface expression of that “make AI easy” thesis: charged electric blue as intelligent technology without robot clichés; crisp white enterprise canvas and disciplined card/grid IA as organized and explainable complexity; measurable stats and third-party credentials as proof. The blue→cyan gradient motion behind product demos is recorded as the one place the brand allows itself to look futuristic, visualizing “intelligence in motion.” The recurring promise is not “we have AI” but “we turn AI into ROI.” Those metaphors do not derive tokens.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

- Book a demo or contact Appier from the marketing site (`Book a Demo`, `Contact Us`).
- Scan case-study stats and third-party credentials (Gartner, ISO 27001, TAG Anti-Fraud).
- Review Ad Cloud, Personalization Cloud, and Data Cloud solutions.
<!-- design-md:claim-end -->

### Audience

The following audience-application readings (no-invented-personas-promoted; observable-work-follows-three-tasks; §13-not-Audience) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. No invented demographic personas are promoted. Observable work follows the three primary tasks: people booking a demo or contacting Appier; people scanning case-study stats and credentials; people reviewing the three clouds. Source §13’s named fictional archetypes are not Audience and are not primary tasks.

### Distinctive traits

The following list restates source-stated reconstruction traits. Ranking those traits as what “distinguishes” Appier from a generic SaaS template, and the trait characterizations (charged/Klein-adjacent, clean enterprise futurism, sophisticated/credible/not playful, intelligence in motion, results-forward, one-blue rationing, conservative-to-moderate radius, logo-social-proof-rail-as-homepage-anchor, bold-headline-regular-body), are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

- Electric Klein-adjacent blue (`#1D2EFF`) as the brand + action color
- Clean white, high-contrast enterprise canvas
- Bold-headline / regular-body hierarchy in a modern sans (EN primary, TC for TW)
- Animated product demos + subtle blue gradient motion — “intelligence in motion”
- Card- and grid-based IA — case-study cards, solution tiles, tabbed results
- Results-forward: measurable stats and third-party credentials as primary trust signals
- Logo social-proof rail (enterprise brands) as a homepage anchor
- Blue rationed to action/intelligence; white + dark neutral for everything else
- Sophisticated, confident, futurist register — `Empowering Businesses to Turn AI into ROI`
- Conservative-to-moderate radius (`8px` workhorse)

### Principles

These six items and the capture-bound application list are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

1. **AI into ROI, always.** Appier reframes AI from capability to outcome. *UI implication:* Lead with the business result (a stat, a P&L benefit), then explain the mechanism. Never headline with model cleverness alone.
2. **Charged blue is intelligence.** The electric blue is the brand's single voice of "active AI." *UI implication:* Apply `#1D2EFF` to CTAs, links, stat highlights, and gradient accents; keep the base crisp neutral so the blue always reads as the intelligent/active element. Resist competing accents.
3. **Proof over promise.** Credibility comes from numbers and credentials, not adjectives. *UI implication:* Surface case-study stats (large, blue) and third-party badges (Gartner/ISO) as first-class components. Every claim should be backed by a real figure.
4. **Organized complexity.** AI is complex; the design makes it explainable. *UI implication:* Use disciplined card/grid IA, tabbed industry results, and clear three-cloud structure. Whitespace and order convey "this is under control."
5. **Futurist, but accountable.** The brand looks forward without losing rigor. *UI implication:* Reserve the blue→cyan gradient and demo motion for the "intelligence in motion" narrative; keep the rest enterprise-crisp. Visionary visuals must still clarify, not just dazzle.
6. **Global EN, native local.** English leads; TC/JP are authored natively. *UI implication:* Route copy through locale bundles; never machine-translate; never Simplified on TW surfaces.

Capture-bound application (source Do’s; values are the recorded reconstruction):

- Reserve electric blue (`#1D2EFF`) for brand + action + intelligence — CTAs, links, stat highlights, gradient accents.
- Lead with measurable results (case-study stats) and third-party credentials (Gartner, ISO).
- Use the blue→cyan gradient (`#1D2EFF` → `#00D4FF`) for “intelligence in motion” moments (hero, product demos).
- Keep a crisp white/dark-ink neutral base so the blue always reads as the active element.
- Use `8–12px` radius (buttons 8px, cards 12px).
- Use big bold headlines and big bold stats; B2B clarity over decoration.
- Author EN primary with native TC/JP per market; never machine-translate.

### Avoid

The following Don’ts, in full — pastel dilution, vague AI hype, scattered gradients, competing accents that muddy the single-blue brand signal, playful pill geometry or heavy shadows, and machine-translate / Simplified-Chinese on TW — are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

- Do not dilute the blue toward a friendly/pastel tone.
- Do not rely on vague AI hype.
- Do not scatter gradients everywhere.
- Do not introduce competing accent colors that muddy the single-blue brand signal.
- Do not use playful pill geometry or heavy shadows.
- Do not machine-translate; do not use Simplified-Chinese characters on TW surfaces.

The following catalog-favicon boundary is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. The catalog Google favicon identity-only boundary is a catalog/identity evidence class, not a source Don’t: the Google s2 URL is not a first-party mark file. It is not an Appier-authored Avoid rule.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Values other than brief-provided primary `#1D2EFF` are source-stated reconstructions. The source labels hexes other than the primary, and the secondary/semantic palette, as well-grounded approximations pending live re-inspection. They are not live computed-style harvests. The following extra role characterizations in this list (very-light-blue-wash; subtle-page-tint; deep-near-navy; secondary-tech-accent; stat-emphasize) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

Primary / action:

- **Appier Blue** (`#1D2EFF`): YAML `primary`. Catalog `primary_color`. Brand + primary action color in the reconstruction — CTAs (`Book a Demo`, `Contact Us`), links, key accents, the logo. The “brand + action / electric, Klein-adjacent” role reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.
- **Blue Pressed** (`#1626D9`): YAML `primary-hover`. Darker press/hover state. YAML Primary button use also names hover `#1626D9`. Both the §2 press/hover wording and the YAML hover are kept.
- **Blue Tint** (`#EBEDFF`): YAML `primary-tint`. Very light blue wash for highlight surfaces, selected states, soft section backgrounds. YAML Secondary button hover bg.
- **On-primary** (`#FFFFFF`): YAML `on-primary`. Text on Appier Blue fills. Same hex as canvas; the roles are not merged into one ink for every string. That unmerged-role reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.

Surface:

- **Pure White** (`#FFFFFF`): YAML `canvas`. Primary content + card surface.
- **Surface Soft** (`#F5F6FA`): YAML `surface-soft`. Grouped sections, subtle page tint.
- **Ink / Dark Section** (`#0A0E27` / `#101130`): Deep near-navy for dark hero/feature sections. YAML `ink` / `foreground` is `#101130` only. `#0A0E27` is body-only. The two writings are kept. They are not averaged.

The following unmerged-role readings are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. YAML `ink` `#101130` is not merged with body-only `#0A0E27`. YAML `foreground` `#101130` is not merged with body-only `#1A1A2E`. On-primary white is not Canvas. Cyan Accent is not Appier Blue. Success / Warning / Error are not Appier Blue.

Neutral / text:

- **Text Primary** (`#101130` / `#1A1A2E`): Headings, primary copy. YAML `foreground` / `ink` is `#101130`. `#1A1A2E` is body-only. Both are kept. The “deep ink with a blue cast” reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.
- **Text Secondary** (`#4A4E69`): YAML `body`. Body, descriptions.
- **Text Muted** (`#8A8FA3`): YAML `muted`. Metadata, captions, helper text.
- **Text Disabled** (`#C4C7D4`): YAML `disabled`. Disabled labels, placeholders.

Borders & dividers:

- **Border Light** (`#E6E8F0`): YAML `hairline`. Row dividers, soft separators. YAML Solution card border.
- **Border Mid** (`#D1D5E3`): YAML `border`. Component borders (inputs, outlined buttons, cards).

Accent / gradient:

- **Cyan Accent** (`#00D4FF`): YAML `accent`. Secondary tech accent in gradient animations and data visualizations.
- **Brand Gradient**: Blue→cyan (`#1D2EFF` → `#00D4FF`) for “intelligence in motion” hero/demo motion and key graphics. Not a YAML `tokens.colors` key as a single hex.
- **Stat Highlight**: Appier Blue (`#1D2EFF`) used to emphasize case-study numbers (e.g., `41%`).

Semantic:

- **Success** (`#16A34A`): YAML `success`. Success states, positive deltas in dashboards.
- **Warning** (`#F59E0B`): YAML `warning`. Attention, neutral alerts.
- **Error / Danger** (`#DC2626`): YAML `error`. Errors, destructive actions. Also the Input error border.

### Spacing

YAML scale: xs 4, sm 8, md 12, base 16, lg 24, xl 32, section 64, section-lg 96. Both writings are kept. YAML unitless numbers are not rewritten as a `px` token. The following generous-section-rhythm, comfortable-card-internals, and unlisted-route-boundary readings are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. Source body: 8px-based spacing scale; generous section rhythm (64–96px); comfortable card internals (24px). These are source-stated reconstruction values, not a claim that every unlisted route shares them.

### Shape

The following local-geometry reading (8px-workhorse; 12px cards; 4–6px chips; 9999-not-circle-percentage) is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. 8px button corners, 12px card corners, and 4–6px credential-chip corners are local geometry in this reconstruction, not a universal radius for every unlisted surface. YAML `rounded.full` 9999 is kept; it is not merged with a circle percentage the source does not record.

- YAML `rounded.sm`: 6 — also the YAML Credential chip radius; body chip radius is `4–6px`. Both writings are kept.
- YAML `rounded.md`: 8 — buttons; body `8px` workhorse
- YAML `rounded.lg`: 12 — cards
- YAML `rounded.full`: 9999 (unitless in YAML; not written here as a `px` token)
- Body also names `8–12px` radius (buttons 8px, cards 12px)

### Elevation

The following elevation readings (clean-with-soft-elevation; buttons-flat-blue-is-the-weight; table Use assignments of soft-cool-cast-lift, header-on-scroll, modal, glow-behind-demos, and solution-or-shadow-separated) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. Appier leans clean with soft elevation and luminous gradient accents. Buttons are flat; blue color is the weight.

- **Card shadow**: `0 4px 16px rgba(16,17,48,0.08)` — YAML `tokens.shadow.card`. Source also names this as soft, cool-cast lift, and as the Solution/case-study card-lift treatment over `motion-standard`.
- **Sticky header**: `0 1px 4px rgba(16,17,48,0.06)` on scroll — YAML `tokens.shadow.header`
- **Modal / dialog**: `0 12px 40px rgba(16,17,48,0.2)` — YAML `tokens.shadow.modal`
- **Glow accents**: blue→cyan (`#1D2EFF` → `#00D4FF`) gradient glows behind product demos and hero graphics. No separate glow hex is recorded.
- Solution / Cloud Card body also records `1px solid #E6E8F0` **or** shadow-separated. Both writings are kept.

Z-index, source-stated order: sticky nav above content; mega-menu above nav; modals above chrome; toasts highest.

### Motion

The following purposeful-motion and spring-stance readings (futurist-restrained; loops-rather-than-bouncing; platform-native interactive feedback) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. Appier motion is recorded as purposeful and futurist-restrained — gradient/demo motion suggests intelligence in motion; UI chrome stays crisp and quick. Spring/overshoot is avoided on UI chrome. The brand's motion energy lives in ambient gradient and product-demo animation (“intelligence in motion”), which loops smoothly rather than bouncing. Interactive feedback (hover, press, tab) is quick and platform-native.

Source-stated duration roles:

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Toggle flips, reduce-motion fallback |
| `motion-fast` | 150ms | Hover/press, tab switch, link transitions |
| `motion-standard` | 250ms | Dropdowns, mega-menu, card lifts, tooltip fades |
| `motion-slow` | 400ms | Modal open, section reveals |
| `motion-ambient` | 2000ms+ | Looping blue→cyan gradient / product-demo animation |

Signature motions (source-stated). Characterizations such as “the visual signature of active intelligence,” “explanatory motion,” and “no scale jump,” and reading easing token names as source-stated names rather than computed curves, are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

1. **Gradient glow (ambient).** A slow blue→cyan (`#1D2EFF` → `#00D4FF`) gradient drifts behind hero/demo graphics over `motion-ambient / ease-ambient`, looping.
2. **Product-demo animation.** The three clouds animate to show how data flows / personalization adapts — explanatory motion, looping, with a static fallback on mobile/reduce-motion.
3. **Card lift.** Solution/case-study cards lift via shadow fade-in (`0 4px 16px rgba(16,17,48,0.08)`) over `motion-standard`. No scale jump.
4. **Tab switch (industry results).** Active indicator slides under the new tab over `motion-fast / ease-standard`; content cross-fades.

Source-stated easing token names and uses (uncomputed; cubic-bezier curves omitted). Characterizations such as “Default two-way transitions” and “looping” in this table are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

| Token | Curve | Use |
|---|---|---|
| `ease-standard` | omitted (unattributed cubic-bezier; matches the legacy spec template) | Default two-way transitions; also named on the industry-results tab switch |
| `ease-enter` | omitted (unattributed cubic-bezier; matches the legacy spec template) | Things appearing — modals, menus, cards |
| `ease-exit` | omitted (unattributed cubic-bezier; matches the legacy spec template) | Dismissals |
| `ease-ambient` | `ease-in-out` (looping; source-stated named easing, not a cubic-bezier) | Gradient glow / demo loops |

**Reduce motion.** Under `prefers-reduced-motion: reduce`, ambient gradient and demo animations freeze to a static gradient still; card lifts and tab transitions go instant; all `motion-*` collapse to `motion-instant`. The following reduced-motion application reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. The explanatory content remains (as a static diagram); only the motion is removed.

Exact cubic-bezier curves are unattributed — `ease-standard`, `ease-enter`, and `ease-exit` match the legacy spec template — and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings (live-inspect-not-a-FontFace-harvest; inferred-not-internal-spec; geometric-sans-not-a-family) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Live computed-style inspection of appier.com was not completed this pass. Token extraction is `prose-derived`. WebFetch (2026-05-19) confirmed a blue-dominant identity and named copy, not a FontFace harvest. |
| Inferred locale stacks | Source labels the stacks “inferred — modern B2B sans.” They are not a sourced internal Appier specification. Body also records “or a geometric brand sans” as an unresolved alternative, not a family. |
| Declared-only | Default (EN) / Traditional Chinese / Japanese system stacks below. |

### Family

- **Default (EN):** YAML `sans` `-apple-system, Segoe UI, Roboto, Helvetica, Arial, sans-serif`. Body table quotes Segoe UI as `-apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif`. Both writings are kept.
- **Traditional Chinese:** YAML `tc` `PingFang TC, Microsoft JhengHei, sans-serif`. Body: `… "PingFang TC", "Microsoft JhengHei", sans-serif`
- **Japanese:** YAML `jp` `Hiragino Kaku Gothic Pro, Meiryo, sans-serif`. Body: `… "Hiragino Kaku Gothic Pro", "Meiryo", sans-serif`

The following fallback-display boundary is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Do not present PingFang, Hiragino, Segoe UI, Roboto, Helvetica, Arial, or a system fallback as an Appier-exclusive brand face. An inferred stack keeps its metadata; it is not live-verified product-use. Do not promote “a geometric brand sans” as the family.

The following type-character readings (B2B-clarity-over-decoration; stats-are-hero-material; weight-plus-blue-accent-hierarchy) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. Big bold hero headlines stating the value proposition; B2B clarity over decoration. Stats are hero material — case-study numbers render large and bold, often in Appier Blue. Weight + the blue accent drive hierarchy; neutral dark for copy, blue for action/emphasis. English primary, with native TC/JP per market — authored, not machine-translated.

### Type roles

The following type-role readings (three-writings-not-averaged; ratio-versus-size-local; no-lineHeight-promoted) are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. YAML records exact sizes. The body table records inferred ranges. §9 construction prompts record additional sizes. All three writings are kept. They are not averaged. YAML `lineHeight` is recorded only on body as unitless `1.5`. That ratio is preserved as a ratio and is not converted to a fixed px. No other YAML `lineHeight` is recorded; none is invented.

| Role | Font | Size | Weight | Line height | Use |
|---|---|---|---:|---|---|
| Caption / meta | inferred locale stack | YAML 13; body `12–13px` | 400 | omitted (unrecorded) | Caption, metadata. YAML `use`: Caption, metadata |
| Body | inferred locale stack | YAML 16; body `15–16px`; §9 prompt description `15px` / 400 `#4A4E69`; §9 case-study outcome `16px` / 400 `#4A4E69` | 400 | YAML `1.5` | Body, descriptions. 16, the 15–16px range, and 15px are kept as separate recorded figures |
| Body large / lead | inferred locale stack | YAML 18; body `18px` | 400 | omitted (unrecorded) | Body large / lead |
| Card heading | inferred locale stack | YAML 22 / 600; body `20–24px`; §9 prompt title `20px` / 600 `#101130` | 600 | omitted (unrecorded) | Card titles, subheads. 22, the 20–24px range, and 20px are kept as separate recorded figures |
| Section heading | inferred locale stack | YAML 32 / 700; body `28–36px` | 700 | omitted (unrecorded) | Section headings |
| Hero headline | inferred locale stack | YAML 48 / 700; body `40–56px`; §9 dark-hero headline `48px` / 700 white | 700 | omitted (unrecorded) | Hero headlines, value proposition |
| Stat figure | inferred locale stack | YAML 48 / 700; body `36–64px`; §9 case-study stat `48px` / 700 `#1D2EFF` | 700 | omitted (unrecorded) | Case-study stat figures, in Appier Blue |
| Nav links | inferred locale stack | body `15–16px` / 500 | 500 | omitted (unrecorded) | Navigation links. Not a YAML typography key |
| Form-error helper | inferred locale stack | §14 `12px` / `#DC2626` | omitted (unrecorded) | omitted (unrecorded) | Helper text below an error field. Not a YAML typography key |

Weights recorded: **700** hero headlines, section headings, case-study stats, CTA emphasis; **600** subheads, button labels, card titles; **400** body, descriptions, metadata. Nav links also record **500**.

### Assets

The following identity-not-captured and imagery-replacement readings are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. Catalog logo metadata is a Google s2 identity record in the source ledger; it is not a portable first-party mark here. Product-demo animations, customer logos on the social-proof rail, and case-study imagery are first-party marketing content in the reconstruction; do not replace them with invented brand-color decoration.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The following graph-not-adopted preservation, and characterizations in that table such as onboarding-oriented empty copy, blameless errors, “never terminal No data,” “a fake metric is worse than none,” and “no celebratory animation in an enterprise tool,” are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. The source state contract is preserved here while the catalog graph is not adopted.

| State | Treatment |
|---|---|
| Empty (dashboard, no data connected) | One `#8A8FA3` line + primary CTA (`#1D2EFF`) to connect a data source / launch first campaign. Onboarding-oriented. |
| Empty (no results in a report) | Clear caption explaining the zero-result in the user's filter terms + adjust-filter action. Never terminal "No data". |
| Loading (dashboard / report) | Skeleton cards at `#F5F6FA`, 12px radius; chart areas as gray blocks. Existing data stays during refresh; no full-page spinner. |
| Loading (inline — submit demo form) | CTA holds width, in-button spinner; prevents double-submit. |
| Error (form field) | Border `1px solid #DC2626`, helper text below in `#DC2626` 12px, field-specific and blameless. |
| Error (data / API failure) | Inline banner with `#DC2626` accent, plain technical-but-clear sentence + retry. Never a bare error code. |
| Success (demo requested) | Confirmation screen/section — what happens next (a specialist will reach out), not a fleeting toast. |
| Success (campaign launched) | Clear confirmation + link to monitoring view; no celebratory animation in an enterprise tool. |
| Skeleton | `#F5F6FA` blocks at exact dimensions; numeric placeholders render as `—`, never `0` (a fake metric is worse than none). |
| Disabled (button) | Faded fill, `#C4C7D4` text, geometry preserved. |

The following reconstruction-pixel reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Radii, paddings, and heights are representative reconstruction pixels; exact internal design-token names may differ. Live computed-style inspection was not completed.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. A generic `Focus` capture is not `focus-visible` treatment evidence; named focus colors stay as additional observed states, and the `focus-visible` visual treatment remains omitted. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. Where exact selector/label/request/outcome is unresolved, those three applicability fields are omitted at this boundary rather than closed. This is not a complete state-coverage claim.

Solution / Cloud Card, Case-Study Card, and Dark Feature Card record default geometry and no state or interactive-kind evidence, so kind and a state-applicability map are omitted. Credential / Award Chip likewise records type, use, and geometry only; kind and a state-applicability map are omitted. Navigation composition records header layout only; kind and a state-applicability map are omitted.

### Primary (Demo / Contact)

- Role: "Book a Demo", "Contact Us", primary conversion CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#1D2EFF`
- Text: `#FFFFFF`
- Border: none
- Radius: 8px (YAML radius 8)
- Padding: `12px 24px`
- Font: `16px` / `600`
- Hover: bg `#1626D9`
- Use: "Book a Demo", "Contact Us", primary conversion CTA, flat. YAML use: primary CTA, flat; hover `#1626D9`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named primary conversion CTA |
| hover | applicable | Pointer-web button; source also records hover bg `#1626D9` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Documented any-button disabled: faded fill, `#C4C7D4` text, geometry preserved |

Loading, error, and success applicability are omitted. Source names this control for "Book a Demo", "Contact Us", and primary conversion CTA together; exact selector/label/request/outcome is unresolved, so those three fields stay omitted at this boundary rather than closed as a destination or as the submitting demo-form action. §14 Loading (inline — submit demo form) and Success (demo requested) remain rows on the capture-record table, not this mixed control’s closed fields.

Additional observed named state: Hover `#1626D9`.

### Secondary (Outlined)

- Role: Secondary actions, "Learn more"
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#FFFFFF`
- Text: `#1D2EFF`
- Border: `1px solid #1D2EFF`
- Radius: 8px
- Padding: `12px 24px`
- Font: `16px` / `600`
- Hover: bg `#EBEDFF`
- Use: Secondary actions, "Learn more". YAML use: 1px `#1D2EFF` border; hover `#EBEDFF` bg
- Field note: the 1px `#1D2EFF` border is this control’s renderable field, not Border Mid `#D1D5E3`. That unmerged-field reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named secondary / Learn more action |
| hover | applicable | Pointer-web button; source also records hover bg `#EBEDFF` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Documented any-button disabled: faded fill, `#C4C7D4` text, geometry preserved |

Loading, error, and success applicability are omitted. Source names this control for secondary actions and "Learn more" together; exact selector/label/request/outcome is unresolved, so those three fields stay omitted at this boundary rather than closed as a destination link.

Additional observed named state: Hover `#EBEDFF`.

### Ghost / Nav

- Role: Navigation links, tertiary actions
- Kind: interactive
- Anatomy: label
- Background: transparent
- Text: `#101130`
- Hover: text `#1D2EFF`
- Use: Navigation links, tertiary actions
- Field note: no YAML `type` is recorded for this control; none is invented.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named navigation / tertiary action |
| hover | applicable | Pointer-web control; source also records hover text `#1D2EFF` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav or tertiary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control for navigation links and tertiary actions together; exact selector/label/request/outcome is unresolved, so those three fields stay omitted at this boundary rather than closed as destination navigation.

Additional observed named state: Hover text `#1D2EFF`.

### On-dark CTA

- Role: CTAs on the deep-ink hero/feature sections
- Kind: interactive
- Anatomy: label
- Background: `#1D2EFF` (or white). Both recordings are kept.
- Text: `#FFFFFF` (or `#101130`). Both recordings are kept.
- Radius: 8px
- Use: CTAs on the deep-ink hero/feature sections. §9 dark-hero prompt also names a white CTA + blue secondary together.
- Field note: no YAML `type` is recorded for this control; none is invented.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named on-dark CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Documented any-button disabled: faded fill, `#C4C7D4` text, geometry preserved |

Loading, error, and success applicability are omitted. Source names this control only as CTAs on deep-ink sections, with two unmerged fills; exact selector/label/request/outcome is unresolved, so those three fields stay omitted at this boundary rather than closed as a submitting action.

### Default Input

- Role: Contact/demo-request forms
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#FFFFFF`
- Text: `#101130`
- Border: `1px solid #D1D5E3`
- Radius: 8px
- Padding: `12px 14px`
- Font: `16px` / `400`
- Focus: border `#1D2EFF`, subtle blue focus ring
- Error: border `#DC2626`
- Use: Contact/demo-request forms. YAML use: 1px `#D1D5E3` border; focus `#1D2EFF`; error `#DC2626`
- Field note: the 1px `#D1D5E3` border is this control’s renderable field and Border Mid; it is not Hairline `#E6E8F0`. That unmerged-field reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named contact/demo-request field |
| hover | applicable | Pointer-web field; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | A field can be unavailable; visual treatment omitted |
| error | applicable | Source Input error border `#DC2626`; §14 Error (form field): border `1px solid #DC2626`, helper text below in `#DC2626` 12px |

Loading and success applicability are omitted. Source records loading of the demo request on the CTA (in-button spinner) and success as a confirmation screen/section, not as states of this field, so those two fields stay omitted at this boundary rather than closed.

Additional observed named state: generic `Focus` — border `#1D2EFF`, subtle blue focus ring. This is not `focus-visible` evidence.

### Solution / Cloud Card

- Role: Ad Cloud / Personalization Cloud / Data Cloud tiles
- Type: card
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Background: `#FFFFFF`
- Border: `1px solid #E6E8F0` or shadow-separated. Both recordings are kept.
- Radius: 12px
- Padding: `24px`
- Use: Ad Cloud / Personalization Cloud / Data Cloud tiles — icon (wand), title, description, blue "Learn more". YAML use: 1px `#E6E8F0` border, blue Learn more link. §9 prompt also records a wand icon in `#1D2EFF`, title `20px`/600 `#101130`, description `15px`/400 `#4A4E69`, "Learn more" link in `#1D2EFF`, and hover shadow `0 4px 16px rgba(16,17,48,0.08)`. Those prompt figures are kept beside YAML 22 / 16; they are not averaged.

### Case-Study Card

- Role: Customer results — logo, big blue stat, one-line outcome
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind` confirmation and no §4.4 state-applicability map are declared. No YAML `type` is recorded; none is invented.
- Background: `#FFFFFF` (or image-led). Both recordings are kept.
- Radius: 12px
- Padding: `24px`
- Use: Customer results — logo, big blue stat (`41% conversion boost`), one-line outcome. §9 prompt: customer logo top, big stat `41%` in `48px`/700 `#1D2EFF`, one-line outcome in `16px`/400 `#4A4E69`.

### Dark Feature Card

- Role: On-dark storytelling where the blue glows
- Type: card
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Background: `#101130` deep ink
- Text: `#FFFFFF`
- Accent: blue→cyan gradient line/graphic
- Radius: 12px
- Use: On-dark storytelling where the blue glows. YAML use: deep-ink bg, blue to cyan gradient accent. §9 dark-hero prompt: white headline `48px`/700, blue→cyan (`#1D2EFF` → `#00D4FF`) gradient glow behind an animated product demo, white CTA + blue secondary.

### Credential / Award Chip

- Role: Gartner / ISO 27001 / TAG Anti-Fraud trust badges
- Type: badge
- Kind: omitted. The source records type, use, and geometry only and no interactive-kind evidence, so no `Kind` confirmation and no §4.4 state-applicability map are declared.
- Background: `#F5F6FA` or white. Both recordings are kept.
- Text: `#101130`
- Border: `1px solid #E6E8F0`
- Radius: YAML 6; body `4–6px`; §9 prompt `6px`. All three writings are kept.
- Use: Gartner / ISO 27001 / TAG Anti-Fraud trust badges. YAML use: 1px `#E6E8F0` border, 4-6px radius. §9 prompt also records a small `#8A8FA3` caption `Trusted & certified`.

### Industry Tab

- Role: Tabbed industry-results switcher
- Kind: interactive
- Anatomy: tab labels
- Active: `#1D2EFF` text + underline/indicator
- Inactive: `#8A8FA3`
- Use: Tabbed industry-results switcher. Signature motion: active indicator slides under the new tab over `motion-fast / ease-standard`; content cross-fades.
- Field note: no YAML `type` is recorded for this control; none is invented.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Inactive tab is the resting item |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A tab selects an industry-results pane; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected vs inactive, not a request or validation failure on the tab |
| success | not-applicable | Opening a tab is selection, not an action-outcome confirmation on the tab |

Additional observed named states: active `#1D2EFF` text + underline/indicator; inactive `#8A8FA3`. Treating that active appearance as a captured variant, not an observed click transition, is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.

### Navigation

- Role: White sticky header composition — logo left, product/solutions/resources nav, blue "Book a Demo" CTA right
- Kind: omitted. The source records header composition only and no interactive-kind evidence for the header as a whole, so no `Kind` confirmation and no §4.4 state-applicability map are declared.
- Nav links: `15–16px` / `500`, blue hover/active
- Mega-menu for the three clouds + solutions
- Sticky header elevation: `0 1px 4px rgba(16,17,48,0.06)` on scroll
- Use: White sticky header. The following unmerged-role reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Primary "Book a Demo" CTA is the Primary button, not a header fill.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following density reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Appier is **medium density** — more breathing room than a commerce surface, befitting a considered B2B purchase, but information-rich (stats, credentials, solution explainers). Whitespace conveys credibility and focus.

The following structure reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Structure: centered max-width (~1200–1280px) marketing layout; alternating white / soft / deep-ink sections to pace the narrative; logo social-proof rail near the top; case-study + solution grids below; credential strip near the footer.

The following recorded-layout spacing restatement (8px-base; generous-section-rhythm; comfortable-card-internals) is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Spacing: 8px-based scale. Generous section rhythm (64–96px); comfortable card internals (24px).

The following collapsing-strategy reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification.

| Width | Behavior |
|---|---|
| Desktop `>1200px` | Full nav + mega-menu, multi-column solution/case-study grids, centered container |
| Laptop `1024–1200px` | 2–3 column grids, condensed nav |
| Tablet `768–1024px` | 2 column → single column; nav collapses; logo rail wraps |
| Mobile `<768px` | Single column, hamburger nav, full-width blue CTA, stacked cards, tabs become a scrollable row |

The following touch, mobile-performance, and media readings are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification. Touch & mobile: full-width primary CTA on mobile; industry-results tabs become horizontally scrollable; animated demos degrade to static graphics on mobile for performance; generous tap targets (44px+).

The following image-behavior reading is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Media: product-demo animations (GIF/video) with static fallback; logo rail and graphics use SVG; responsive scaling.

The following unlisted-surface boundary is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. The 8px / 12px radii, 12px 24px button padding, 12px 14px input padding, 24px card padding, 44px+ tap targets, and 768px / 1024px / 1200px / 1280px measurements are source measurements, not a claim that every unlisted surface shares them.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Observed (appier.com via WebFetch 2026-05-19; dual-destination with provenance):

- `Empowering Businesses to Turn AI into ROI` — homepage tagline / page title
- `Making AI easy by making software intelligent` — vision statement
- `Autonomous, Adaptive, and Agentic` — product-capability framing
- `Book a Demo` — primary conversion CTA

The remaining voice examples, the copy-pattern table, the voice characterization, the forbidden-phrase list, and the EN/TC/JP authored-copy rule are a derived editorial implementation inference from the source reconstruction; they are not Appier-authored or a separately published UI specification, and they are not the same evidence class as those WebFetch strings. Appier speaks like a confident enterprise-AI advisor — precise, outcome-oriented, and credible, never hypey or jargon-drunk. The register is **results-forward and authoritative**: it leads with what AI does for the business ("turn AI into ROI") and backs every claim with a number or a credential. The brand is fluent in the language of CMOs and growth teams — ROI, ROAS, conversion, retention — and frames its own technology in terms of the customer's outcome, not the model's cleverness. English is the primary global voice; Traditional Chinese and Japanese serve the home and listing markets, authored natively. The tagline and the vision capture the tone — ambitious but grounded, futuristic but accountable.

| Context | Tone |
|---|---|
| Hero / positioning | Confident, outcome-led. `Empowering Businesses to Turn AI into ROI.` Ambition tied to a measurable promise. |
| CTAs | Direct, low-friction. `Book a Demo`, `Contact Us`, `Get Started`. No exclamation. |
| Product / solution copy | Benefit-then-mechanism. What it achieves for the business, then how the AI does it. |
| Case studies | Number-led. `41% conversion boost` — the stat is the headline; narrative supports it. |
| Credentials | Plain and factual. Gartner / ISO / awards stated, not embellished. |
| Empty states (product) | Clear next step toward value (connect a data source / launch a campaign). |
| Errors | Blameless, technical-but-clear, actionable. |
| Corporate / About | First-person-plural, plain, mission-forward. `Appier's vision is…`. |

Treating `41% conversion boost` as an illustrative stat-led format, not a named-customer Observed string, is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. Illustrative, not verified as a named-customer live figure this pass: `41% conversion boost` — representative of Appier's stat-led case-study format observed via WebFetch; specific figure not pinned to a named customer here.

Treating `Contact Us` and `Get Started` as source CTA inventory, not WebFetch-marked Observed strings, is a derived editorial implementation inference from the source reconstruction; it is not Appier-authored or a separately published UI specification. `Contact Us` and `Get Started` are source CTA inventory examples; only `Book a Demo` is the WebFetch-marked Observed string above.

Forbidden phrases (source list; the list itself sits under the Content B2a above): empty AI hype (`revolutionary AI magic`, `the future is here`) without a metric, `Oops! Something went wrong` without a reason, buzzword stacking (`synergistic agentic blockchain AI`), unsubstantiated superlatives, approximate or unsourced stats (every number must be real), emoji in enterprise chrome, Simplified-Chinese characters on TW surfaces.

Use English as the primary global voice with native Traditional Chinese and Japanese per market; never machine-translate; never Simplified on TW surfaces.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- live computed-style production hexes beyond brief-provided primary `#1D2EFF` (secondary/semantic palette remains source-stated approximations pending live re-inspection)
- exact cubic-bezier easing curves (omitted as unattributed; `ease-standard` / `ease-enter` / `ease-exit` match the legacy spec template)
- animation names and CSS transition properties until per-component computed capture of all five motion evidence kinds exists; a single named duration is not that gate
- `focus-visible` visual treatments (generic `Focus` on the default input is a different observation)
- live computed family (live computed-style inspection of appier.com was not completed this pass)
- YAML `lineHeight` on roles other than body `1.5` (unrecorded; not invented)
- a geometric brand sans as a family (body records it as an unresolved alternative)
