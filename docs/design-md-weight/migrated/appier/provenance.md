# Appier provenance

Not part of the portable `DESIGN.md`. Source ledger, freshness, and proof for the T2 migration. Canonical source remains `web/references/appier/DESIGN.md` until catalog adoption.

## Identity

| Field | Value |
|---|---|
| id | appier |
| name | Appier |
| country | TW |
| category | ai |
| homepage | https://www.appier.com |
| primary_color | `#1D2EFF` |
| logo | favicon `https://www.google.com/s2/favicons?domain=appier.com&sz=128` |
| omd format (source) | 0.1 |
| tokens.source | prose-derived |
| tokens.extracted | 2026-06-08 |

Token note from source: primary = electric Klein-adjacent blue (`#1D2EFF`, brief-provided + brand); secondary palette is grounded B2B approximation pending live re-inspection (per §2 note). Dual-destination with portable Experience Scope and Foundations Semantic color (E2a). Catalog `primary_color` `#1D2EFF` is restated wherever that hex appears in the portable body, not only Distinctive / Principles / Foundations Appier Blue / Components fills / Named gaps (E2a): also Scope matching/atmosphere, Semantic Brand Gradient / Stat Highlight, Elevation glow, Motion gradient-glow, Type-role Stat figure, Capture-record Empty dashboard CTA, Secondary text/border/field-note, Ghost hover, On-dark fill, Default Input Focus, Solution wand, Case-Study stat, Dark Feature gradient, Industry Tab active. The Google s2 favicon URL is catalog identity-only in this ledger; it is not a portable Typography & Assets mark (E2a: URL not dual-destination). The portable Assets identity-not-captured sentence sits under adjacent complete B2a. Named gaps has no first-party-mark-file sentence (D1). The capture-method / not-a-portable-mark boundary is Assets + Avoid catalog-favicon, not a Named-gap absence claim. Homepage `https://www.appier.com` is dual-destination: Experience Scope + this identity/surfaces/sources/Tier 1/Proof ledger (E2a). The URL `https://www.appier.com/en` is this Sources/Tier 1 ledger only; it is not a portable Scope URL (E2a).

No `ds.name` / `ds.url` / `ds.type` / `ds.description` fields exist on the source. None are invented here.

## Freshness

| Event | Date |
|---|---|
| verified | 2026-05-19 |
| tokens.extracted | 2026-06-08 |
| WebFetch tagline / vision / framing / CTA | 2026-05-19 |
| about-page WebFetch | 2026-05-19 |
| WebSearch press | 2026-05-19 |

Conflicts unresolved: production hexes beyond primary not live-verified this pass (browser unreliable) — flagged for UPDATE. Portable Foundations restates those hexes as source-stated approximations, not as a computed-style harvest.

Preserved value pairs (both sides stay in portable Foundations / Typography / Components; neither is chosen):

- YAML ink/foreground `#101130` vs body-only dark-section `#0A0E27`
- YAML foreground `#101130` vs body-only text-primary `#1A1A2E`
- YAML caption 13 vs body `12–13px`
- YAML body 16 / `lineHeight` 1.5 vs body `15–16px` vs §9 description `15px`
- YAML card-heading 22 vs body `20–24px` vs §9 title `20px`
- YAML section 32 vs body `28–36px`
- YAML hero 48 vs body `40–56px` vs §9 dark-hero 48px
- YAML stat 48 vs body `36–64px` vs §9 `48px` / 700 `#1D2EFF`
- YAML Credential radius 6 vs body `4–6px` vs §9 `6px`
- Solution / Cloud Card border `1px solid #E6E8F0` vs shadow-separated
- Case-Study Card background `#FFFFFF` vs image-led
- Credential chip background `#F5F6FA` vs white
- On-dark CTA fill `#1D2EFF` vs white; text `#FFFFFF` vs `#101130`
- YAML `sans` unquoted `Segoe UI` vs body quoted `"Segoe UI"`
- body “or a geometric brand sans” as an unresolved alternative, not a family
- Empty copy: §14 dashboard/report treatments vs §10 empty-state direction (connect a data source / launch a campaign)
- `41% conversion boost` as illustrative stat-led format, not a named-customer figure

## Surfaces

| id | kind | url | inspected |
|---|---|---|---|
| home | marketing (attempted) | https://www.appier.com | 2026-05-19 — live inspect NOT completed (browser redirect / injected interstitials) |
| about | narrative | https://www.appier.com/en/about | 2026-05-19 — WebFetch (founding/vision/clouds/TSE; not a computed-style harvest) |

The source names the appier.com marketing reconstruction. It does not list separate inspectable product-app or native-app URLs. Those names stay as portable Scope; they are not given invented routes here. `https://www.appier.com` is the catalog homepage and the attempted live-inspect URL. It is not used as a computed-style claim surface.

## Sources

The source has no `verification_v2.sources` table. The URLs below are copied from the footer and HTML comment, not invented as an in-app harvest.

| id | kind | url | captured |
|---|---|---|---|
| appier-home-attempted | product-surface (attempted) | https://www.appier.com | 2026-05-19 (inspect not completed) |
| appier-en | marketing WebFetch | https://www.appier.com/en | 2026-05-19 |
| appier-about | narrative | https://www.appier.com/en/about | 2026-05-19 |

### Tier 1 (partial)

- https://www.appier.com — live inspect NOT completed (browser redirect). Dual-destination with portable Experience Scope as the named homepage URL (E2a). Not a computed-style product-token surface.
- https://www.appier.com/en — WebFetch 2026-05-19: blue-dominant identity (blue nav/CTAs/UI, “white logo on blue background” per brand-asset search), tagline `Empowering Businesses to Turn AI into ROI`, vision `Making AI easy by making software intelligent`, `Autonomous, Adaptive, and Agentic`, three clouds (Ad / Personalization / Data), L’Oréal / BMW / Nexon social proof, case-study stats, Gartner / ISO credentials, card/tabbed IA, gradient demo animation. The URL string is this Sources/Tier 1 ledger only (not a portable Scope URL). Observed strings from that fetch are dual portable Content + this ledger (E2a).
- Catalog `primary_color` `#1D2EFF` is the creation-brief-provided value and is recorded as matching Appier’s known electric-/Klein-adjacent blue identity; it is not a live computed hex from this pass. Matching/reconstruction-authority sit under Scope adjacent complete B2a. Hex destinations follow the Identity token-note list above (E2a).

### Tier 2 (not checked this pass)

- https://styles.refero.design — not checked this pass (browser session unreliable)
- https://getdesign.md — not checked this pass (browser session unreliable)
- brandfetch / logokit aggregators — returned 403

### Tier 2 (philosophy / founders) — not interface tokens

- https://www.appier.com/en/about (WebFetch 2026-05-19) — founded 2012; vision “Making AI easy by making software intelligent”; SaaS AI for business decisions; three clouds; agentic AI; Tokyo Stock Exchange listing; enterprise customers. Dual-destination: portable Experience Scope narrative + this ledger (E2a). Founders-thesis / three-cloud product-story-span / prominent-deep-tech-success-story / not-interface-token have Scope adjacent complete B2a.
- WebSearch 2026-05-19 (PRNewswire/Sharecast press) — agentic-AI product-line framing, ROI-driven positioning. Metrics not independently audited.

### Style ref (not a token source)

- `stripe` (engineering/enterprise tone). Provenance-only. Not a portable Avoid or Foundations rule.

## Claim ledger

The source has no `verification_v2.claims` object and no per-claim surface mapping. None is invented here. Token extraction remains `prose-derived` (2026-06-08). `components_harvested: true`. The string `prose-derived` is dual portable Scope + Typography Font evidence + this ledger (not Foundations). The approximation bound (brief-provided primary; other hexes pending live re-inspection) is dual portable Scope + Foundations Semantic color + Named gaps + this ledger (E2a). Homepage `https://www.appier.com` is not assigned as the computed-style surface of in-app product tokens. Surface id `appier-live` is not used as a claim surface.

## Capture selectors

The source does not record `data-omd-capture` selectors. None are invented here.

## Omitted unattributed easing curves (E2b)

Portable Foundations keeps token names `ease-standard` / `ease-enter` / `ease-exit` / `ease-ambient` and their uses. `ease-ambient` keeps source-stated `ease-in-out` (looping). These cubic-bezier strings are omitted from the portable body as unattributed (all three match `spec/omd-v0.1.md` template examples) and are stored here as the omission ledger:

- `ease-standard` `cubic-bezier(0.4, 0, 0.2, 1)` — matches legacy spec template `ease-standard` `cubic-bezier(0.4, 0.0, 0.2, 1)`
- `ease-enter` `cubic-bezier(0, 0, 0.2, 1)` — matches legacy spec template `ease-enter` `cubic-bezier(0.0, 0.0, 0.2, 1)`
- `ease-exit` `cubic-bezier(0.4, 0, 1, 1)` — matches legacy spec template `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)`

Duration tokens (`0ms` / `150ms` / `250ms` / `400ms` / `2000ms+`) and signature-motion prose remain in portable Motion.

## Proof notes

- tokens.source: `prose-derived`; not `reconciled`; not an official published Appier UI specification. String destinations: this ledger + portable Scope + Typography Font evidence (E2a). Not Foundations.
- components_harvested: true
- Catalog logo Google s2 URL is identity-only. Not dual-destination with portable Assets (E2a). Portable Assets identity-not-captured + imagery-replacement sit under adjacent complete B2a. Named gaps has no first-party-mark-file sentence (D1)
- Homepage `https://www.appier.com` is dual-destination: Experience Scope + this identity/surfaces/sources/Tier 1/Proof ledger (E2a). Attempted live inspect, not a computed-style product-token surface
- `https://www.appier.com/en` is this Sources/Tier 1 ledger only. It is not a portable Scope URL (E2a)
- `https://www.appier.com/en/about` is dual-destination: portable Scope founding/vision facts + this Sources/Tier 2 ledger (E2a)
- Source token note (brief-provided primary; other hexes approximations) is dual portable Scope + Foundations Semantic color + Named gaps + this ledger (E2a)
- Observed WebFetch 2026-05-19 strings: `Empowering Businesses to Turn AI into ROI` is portable Content Observed + Content derived Hero row + Distinctive register bullet + this ledger (E2a). `Making AI easy by making software intelligent` is portable Content Observed + Scope public-facts vision + this ledger (E2a). `Autonomous, Adaptive, and Agentic` is portable Content Observed + this ledger only (E2a). `Book a Demo` is portable Content Observed + Content derived CTAs row + Primary tasks + Appier Blue + Primary CTA Role/Use/C2 + Navigation Role/Use + this ledger (E2a). Content inventory-not-Observed B2a marks `Contact Us` / `Get Started` as not the WebFetch Observed class.
- Remaining voice examples, copy-pattern table, voice characterization, forbidden-phrase list, and EN/TC/JP authored-copy rule sit under Content adjacent complete B2a and are not labeled Observed. Illustrative `41% conversion boost` has its own adjacent complete B2a (not a named-customer Observed string).
- Founding 2012 / vision / Taipei advertising-startup origin / three clouds / agentic AI / TSE listing / L’Oréal / BMW / Nexon / Gartner / ISO 27001 / TAG Anti-Fraud are dual portable Scope narrative + this ledger (E2a); metrics not independently audited. Founders-thesis / three-cloud product-story-span / prominent-deep-tech-success-story / not-interface-token have Scope adjacent complete B2a.
- Style ref `stripe` is provenance-only
- Source has no per-claim surface mapping; none invented
- Solution / Cloud Card, Case-Study Card, and Dark Feature Card omit kind and map (C4). Credential / Award Chip omits kind and map (C4). Navigation composition omits kind and map (C4). Kind is not declared `non-interactive`. Case-Study Card has no YAML `type`; none invented.
- Adjacent complete B2a in the portable body after F3: Scope coverage assignment; Scope matching / reconstruction-authority; Scope atmosphere extra names (predominantly-white high-contrast; consumer-warm-vs-pure-charged; living-animated three-cloud systems; card-based grid-disciplined IA); Scope founders-thesis / three-cloud product-story-span / prominent-deep-tech-success-story / not-interface-token; Scope product-expression extra names; Audience no-invented-personas-promoted / observable-work-follows-three-tasks / §13-not-Audience; Distinctive ranking + trait characterizations including logo-social-proof-rail-as-homepage-anchor / bold-headline-regular-body; numbered Principles (six items + capture-bound application); Avoid Don’ts in full (pastel / AI hype / scattered gradients / competing accents / pill-shadow / machine-translate-TW); Avoid catalog-favicon; Semantic extra role characterizations (very-light-blue-wash / subtle-page-tint / deep-near-navy / secondary-tech-accent / stat-emphasize); Semantic “brand + action / Klein-adjacent”; Semantic On-primary unmerged; Semantic unmerged-role; Text Primary “blue cast”; Spacing generous-section-rhythm / comfortable-card-internals / unlisted-route; Shape local-geometry including 8px-workhorse / 9999-not-circle-percentage (limiter precedes the YAML list); Elevation philosophy + table Use assignments; Motion purposeful/spring extra names (futurist-restrained / loops-rather-than-bouncing / platform-native); Signature characterizations / easing names not-computed; Reduce-motion “content remains”; easing-table “Default two-way” / “looping”; reconstruction-pixel; Font evidence-class application including live-inspect-not-a-FontFace-harvest / inferred-not-internal-spec / geometric-sans-not-a-family; Family fallback-display; type-character extra names; Type-role three-writings-not-averaged / ratio-versus-size-local / no-lineHeight-promoted; Assets identity-not-captured + imagery-replacement; Capture-record graph-not-adopted + onboarding/blameless/never-terminal/fake-metric/no-celebratory characterizations; Secondary and Default Input unmerged-field notes; Industry Tab captured-variant-not-click-transition; Navigation “not a header fill”; Layout density / “credibility and focus”; Layout structure / pacing; Layout recorded-layout spacing restatement; Layout collapsing-strategy; Layout touch / mobile-performance; Layout image-behavior / media; Layout unlisted-surface; Content voice + derived table + forbidden-phrase list + authored-copy rule; Content illustrative-41%-not-named-customer; Content Contact-Us/Get-Started inventory-not-Observed. Reconstruction-boundary exemption not used. Governance Authority is not a substitute.
- Source §13 personas are fictional archetypes informed by publicly described Appier customer segments (enterprise marketing/growth teams across APAC and global brands), not individual people. Portable Audience keeps the exclusion boundary only. Names, ages, titles, and biographies are not copied here (D2). Primary tasks come from Book-a-Demo / credentials / three-clouds evidence, not §13
- Generic `Focus` (default input border `#1D2EFF`, subtle blue focus ring) is not `focus-visible` treatment
- Uncaptured hover/disabled/loading visual treatments are omitted. They are not `not-applicable`; applicability follows control meaning. Input error stays applicable from source Input + §14 form-field row. Primary / Secondary / Ghost / On-dark loading/error/success are omitted at the field boundary, not closed as N/A. Industry Tab loading/error/success are not-applicable by tab role. State coverage is not claimed complete
- In-app component pixels (radii, paddings) are representative reconstruction values; exact internal design-token names may differ. That qualifier stays in portable Components
- No `[FILL IN]` placeholders exist in the source; none are emitted
