# Coinbase Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Coinbase. Catalog homepage identity is `https://www.coinbase.com`. Treating that catalog homepage as identity rather than a fourth captured surface is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

Treating the following hosts as the named evidence domains of this reconstruction, including this-contract-covers-two-current-first-party-evidence-domains-from-the-2026-06-08-packet, values-stay-attached-to-the-surface-or-spec-that-established-them, live-coinbase.com-not-a-stand-in-for-CDS-documented-semantics, CDS-documented-semantics-not-a-stand-in-for-live-computed-geometry, CDS-open-sourced-as-source-stated-§8, and product-family-names-Wallet-non-custodial-Prime-institutional-Base-not-inspected-interface-tokens-in-this-packet, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. This contract covers two current first-party evidence domains from the 2026-06-08 packet: live public web at `coinbase.com` across `/`, `/explore`, and `/about` (playwright `getComputedStyle`, 2026-06), and Coinbase Design System (CDS, internally Cedar), which source §8 records as open-sourced, at `cds.coinbase.com` and `github.com/coinbase/cds`. Color, type, and component values below stay attached to the domain that established them. A live `coinbase.com` measurement is not a stand-in for a CDS-documented semantic. A CDS-documented semantic is not a stand-in for live-computed geometry. Source §11 names Coinbase Wallet (non-custodial), Coinbase Prime (institutional), and Base as a product-family extension; those names are narrative context in this packet, not inspected interface tokens.

Source §8 states that Coinbase open-sourced its design system — Coinbase Design System (CDS), internally Cedar — at `cds.coinbase.com` and `github.com/coinbase/cds`, and that the patterns combine CDS-documented semantics with values measured live from `coinbase.com`. Color roles map to CDS semantic tokens: `fgPrimary`/`bgPrimary` = Blue70 (`#0052ff`), `fgMuted`/`line` = Gray60 (the `#5b616e` muted role at 20% opacity), `fgInverse`/`bg` = Gray0/white, foreground text = `#0a0b0d`. Treating that mapping as source-stated CDS-to-hex correspondence rather than a second palette, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

The following atmosphere readings — clean-trustworthy-crypto-platform, financial-reliability-through-a-blue-and-white-binary-palette, Coinbase-Blue-as-the-singular-brand-accent, professional-financial-grade-interface, proprietary-four-font-system, 56px-radius-pill-CTAs-with-hover-to-`#578bfa`, alternating-white-and-near-black-feature-sections, 1.00-line-height-on-display-headings, cool-gray-secondary-surface-with-blue-tint, and `text-transform: lowercase` on some button labels — are a derived editorial implementation inference from the verified surfaces; they are not Coinbase-authored or a separately published UI specification. Source §1 describes the public website as a clean, trustworthy crypto platform and records Coinbase Blue (`#0052ff`) as the singular brand accent against white and near-black surfaces. The proprietary font family named here is CoinbaseDisplay, CoinbaseSans, CoinbaseText, and CoinbaseIcons. Source §1 also records 56px-radius pill CTAs with hover to `#578bfa`, dark sections at `#0a0b0d` and `#282b31`, cool gray `#eef0f3`, display line-height 1.00, and `text-transform: lowercase` on some button labels.

Treating the following public-history facts as narrative rather than interface tokens — June-2012-San-Francisco-founding, Brian-Armstrong-CEO-and-Fred-Ehrsam-former-Goldman-Sachs-FX-trader, Armstrong-Hacker-News-co-founder-post-for-Y-Combinator, Ehrsam-responded-after-seeing-the-post-on-Reddit, HN-post-itself-went-viral, Y-Combinator-S12-with-$150K, mission-string-to-increase-economic-freedom-in-the-world, NASDAQ-direct-listing-14-April-2021-ticker-COIN, IPO-peak-approached-$100B-market-cap, 2022-crypto-winter-and-FTX-collapse-Nov-2022, most-regulated-US-crypto-exchange, regulatory-caution-into-positioning-advantage, and product-family-Wallet-non-custodial-Prime-institutional-Base-L2-2023 — is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Source §11 records those facts from named public-history pages (Wikipedia / Frederick.ai / 99bitcoins) plus “Brian Armstrong public talks” and “SEC public filings” in the footer philosophy line. They do not by themselves supply interface tokens. Source §11: founded June 2012 in San Francisco by Brian Armstrong (CEO) and Fred Ehrsam (former Goldman Sachs FX trader). Armstrong posted on Hacker News looking for a co-founder for Y Combinator; Ehrsam responded after seeing the post on Reddit — the HN post itself went viral. They entered Y Combinator S12 with $150K. Mission: *"to increase economic freedom in the world."* NASDAQ direct listing April 14, 2021 under ticker COIN, briefly approached $100B market cap at IPO peak. Survived the 2022 crypto winter and the FTX collapse (Nov 2022) as the most-regulated US crypto exchange — turning regulatory caution into positioning advantage. Product family extended: Coinbase Wallet (non-custodial), Coinbase Prime (institutional), Base (L2 blockchain, launched 2023). Treating the origin-story sequence, the regulatory-caution-into-positioning-advantage causal tail, and the Wallet/Prime product-role qualifiers as source-stated public-history rather than interface tokens, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

The following refusal list is source-stated in §11, not a separately published UI specification; treating token-shilling / unregistered-securities-listings / aggressive-trader-bait-UI / casino-style-gambling-framing as reconstruction-scope refusals rather than live-captured chrome is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. What the source treats Coinbase as refusing: token shilling, unregistered securities listings (regulatory caution), aggressive trader-bait UI, casino-style gambling framing.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the following three source-stated top-nav outcomes as Primary tasks — Sign-up-from-the-top-nav, Search-from-the-top-nav, Scan-header-category-chips-on-the-public-header — not lifting tasks from source §13 fictional archetypes, and not binding those tasks to exact live component ids, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

- Sign up from the top-nav on `coinbase.com` (source §8 top-nav: Sign up CTAs).
- Search from the top-nav (source §8 top-nav: search).
- Scan header category chips on the public header (source §8 top-nav: category chips; live hosts `/`, `/explore`, `/about`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes, not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

### Distinctive traits

The following unmerged-role extras — treating catalog `primary_color` `#0052ff` as YAML primary/brand and CDS Blue70 rather than Hover Blue `#578bfa` and rather than Link Blue `#0667d0`, treating canvas `#ffffff` as unmerged from on-primary `#ffffff`, treating foreground `#0a0b0d` as unmerged from dark-section `#0a0b0d`, treating muted `#5b616e` as unmerged from the same hex at 20% opacity used as line, treating surface `#eef0f3` as unmerged from YAML primary fill `#0052ff` and from §4 Primary Pill backgrounds `#eef0f3` or `#282b31`, treating YAML pill 56 as unmerged from YAML full `100000` and from live `100000px` full-pill CTAs, treating YAML button `16px / 600 / 1.20` plus `0.16px` tracking as unmerged from nav-tab-chip `14px / 400`, and treating four named Coinbase families as unmerged from any system substitute — are a derived editorial implementation inference from the verified surfaces; they are not Coinbase-authored or a separately published UI specification.

- Coinbase Blue `#0052ff` as catalog `primary_color`, YAML `primary`/`brand`, and CDS `Blue70` / `fgPrimary`/`bgPrimary`
- Four named proprietary families: CoinbaseDisplay, CoinbaseSans, CoinbaseText, CoinbaseIcons
- Pill geometry conflict preserved: YAML/§8 full-pill `100000px` versus §1/§4/§7 `56px` CTA radius (YAML `rounded.pill` 56 / `rounded.full` 100000)
- Near-black `#0a0b0d` dark sections + white `#ffffff` light sections; cool gray `#eef0f3`; dark card `#282b31`
- Display line-height 1.00; button tracking `0.16px`
- `text-transform: lowercase` on some button labels
- Hover Blue `#578bfa`; Link Blue `#0667d0`; muted `#5b616e` also as 20% opacity line `rgba(91,97,110,0.2)`
- Live viewport 1440px; CDS breakpoint table xs 400 through 4xl 1600

### Principles

These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Coinbase-authored or a separately published UI specification.

1. **Compliance is a feature, not a tax.** *UI implication:* KYC/security flows have first-class UX investment, not last-mile.
2. **Show the receipt, always.** *UI implication:* every transaction includes asset / amount / fee / USD value / network status.
3. **Education sits next to action.** *UI implication:* Earn pages, Learn pages have nav prominence equal to Trade.
4. **Round buttons, not aggressive corners.** *UI implication:* fully-pill (`56px` radius) on primary actions reads "approachable, retail-grade".
5. **Coinbase Blue `#0052ff` is the only accent.** *UI implication:* never multi-color brand chrome; let market data charts carry color.

The UI implications are this reference’s interpretation of the reconstruction principles, not a separately published Coinbase UI specification. Treating those UI-implication tails as a derived editorial implementation inference rather than a separately published Coinbase UI specification, including KYC-security-flows-first-class, receipt-asset-amount-fee-USD-network, Earn-Learn-nav-equal-to-Trade, fully-pill-56px-approachable-retail-grade, and never-multi-color-brand-chrome, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do’s, including Coinbase-Blue-`#0052ff`-for-primary-interactive-elements, 56px-radius-for-all-CTA-buttons, CoinbaseDisplay-for-hero-headings-only, and alternate-dark-`#0a0b0d`-and-white-sections, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

- Use Coinbase Blue (`#0052ff`) for primary interactive elements
- Apply 56px radius for all CTA buttons
- Use CoinbaseDisplay for hero headings only
- Alternate dark (`#0a0b0d`) and white sections

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including blue-not-decorative-functional-only, no-sharp-corners-on-CTAs-56px-minimum, and the two §9 local recipes (white-hero-CoinbaseDisplay-80px-line-height-1.00-pill-CTA-`#eef0f3`-56px-hover-`#578bfa`; dark-section-`#0a0b0d`-CoinbaseDisplay-64px-white-blue-accent-link-`#0052ff`). They are a derived editorial implementation inference from the verified surfaces; they are not Coinbase-authored or a separately published UI specification.

- Do not use the blue decoratively — it's functional only
- Do not use sharp corners on CTAs — 56px minimum

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#0052ff`-as-primary-brand-Blue70-not-Hover-`#578bfa`-not-Link-`#0667d0`, canvas-`#ffffff`-not-on-primary-`#ffffff`-as-a-second-token, foreground-`#0a0b0d`-not-dark-section-as-a-second-token, muted-`#5b616e`-not-the-20%-line-as-a-second-hex, surface-`#eef0f3`-not-YAML-primary-fill, surface-dark-`#282b31`-not-dark-section-`#0a0b0d`, Light-Surface-`rgba(247,247,247,0.88)`-not-canvas-not-`#eef0f3`, and YAML-component-fields-staying-on-those-controls — are a derived editorial implementation inference from the verified surfaces; they are not Coinbase-authored or a separately published UI specification.

- **Coinbase Blue / catalog primary** (`#0052ff`): YAML `primary` / `brand`. CDS `Blue70` / `fgPrimary`/`bgPrimary`. Primary brand, links, CTA borders in §2; YAML `button-primary` fill.
- **Hover Blue** (`#578bfa`): YAML `primary-hover`. Button hover background.
- **Link Blue** (`#0667d0`): YAML `link`. Secondary link color; nav category-chip text.
- **Pure White / canvas** (`#ffffff`): YAML `canvas`. Primary light surface. Same hex as on-primary; named jobs stay unmerged.
- **On-primary** (`#ffffff`): YAML `on-primary`. White label on `#0052ff` fill.
- **Near Black / foreground** (`#0a0b0d`): YAML `foreground`. Text. Same hex as YAML `dark-section`; named jobs stay unmerged.
- **Dark section** (`#0a0b0d`): YAML `dark-section`. Dark feature-section background; YAML `dark-section` component fill.
- **Muted** (`#5b616e`): YAML `muted`. CDS `Gray60` / `fgMuted`. Also the 20% opacity line `rgba(91,97,110,0.2)` — that line is the muted role at 20% opacity, not a second hex.
- **Cool Gray Surface** (`#eef0f3`): YAML `surface`. Secondary button background; YAML `button-secondary` fill; SegmentedControl track.
- **Dark Card** (`#282b31`): YAML `surface-dark`. Dark button/card backgrounds; YAML `button-inverse` fill.
- **Light Surface** (`rgba(247,247,247,0.88)`): body §2 only; not YAML. Not canvas `#ffffff` and not `#eef0f3`.

### Spacing

YAML `spacing`: xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 48, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Treating those YAML numbers as recorded without a required px suffix, and adding no px suffix here, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

Source §5 live scale (Base: 8px): 1px, 3px, 4px, 5px, 6px, 8px, 10px, 12px, 15px, 16px, 20px, 24px, 25px, 32px, 48px.

Treating YAML numbers as recorded without a required px suffix, treating the §5 px scale as body-recorded observations rather than a converted YAML scale, treating no-site-wide-spacing-token-as-merged-from-those-two-lists, and treating yaml-section-64-as-YAML-only-not-in-the-§5-list, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. YAML `section` 64 is YAML-only. The §5 list has no 64 step.

### Shape

The following local-geometry reading, including YAML-pill-56-unmerged-from-YAML-full-100000, live-`100000px`-full-pill-CTAs, compact-nav-chips-`16px`, card-radius-`8px–40px`, and not-a-universal-radius-chosen-from-the-conflict, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. YAML `rounded`: sm 4, md 8, lg 16, xl 40, pill 56, full 100000.

Source §5 live ranges:

- Small (4px–8px): Article links, small cards
- Standard (12px–16px): Cards, menus
- Large (24px–32px): Feature containers
- XL (40px): Large buttons/containers
- Pill (56px): Primary CTAs
- Full (100000px): Maximum pill

The following local-geometry reading, including YAML-pill-56-unmerged-from-YAML-full-100000, live-`100000px`-full-pill-CTAs, compact-nav-chips-`16px`, card-radius-`8px–40px`, and not-a-universal-radius-chosen-from-the-conflict, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. YAML 56 and YAML 100000 stay unmerged from each other and from live `56px` / `100000px` / `16px` chip geometry.

### Elevation

YAML `shadow.soft`: `minimal — depth from dark/light section contrast, not box-shadow`. Source §6: Minimal shadow system — depth from color contrast between dark/light sections. Cards: `shadow: none — depth from section contrast`. Treating no-reusable-elevation-ladder-or-numeric-shadow-token-is-established is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. No numeric shadow token is promoted.

Treating modal-scrim-overlay-plus-FocusTrap as modal chrome rather than a reusable elevation token is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Modal records a scrim overlay + FocusTrap; that overlay is modal chrome, not a reusable elevation token.

### Motion

Source §15 durations and signature motion, preserved:

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Toggle |
| `motion-fast` | 150ms | Hover |
| `motion-standard` | 250ms | Modal, panel |
| `motion-pulse` | continuous | Live price-update micro-flash on cells |

Live price updates flash green/red cell background briefly on tick; that flash is disabled under `prefers-reduced-motion: reduce`. Source also records `no bounce`. Sparkline live price ticks flash green/red on the cell (disabled under `prefers-reduced-motion`). Toast auto-dismiss is 5s base + content/action adjustments, and persists on hover.

Source easing language `standard cubic-bezier` is omitted here; no exact cubic-bezier value was supplied. Treating that omitted-standard-cubic-bezier-language as captured-absence-not-a-promoted-easing-token, and not as a complete motion token sheet, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Do not promote an easing curve, animation name, or transition property until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

Treating the following evidence-class application, including named-proprietary-family-in-this-reconstruction and YAML-and-body-§3-metrics-as-source-stated-not-live-computed-type-size-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Named in this reconstruction | Display `CoinbaseDisplay`, UI/Sans `CoinbaseSans`, Body `CoinbaseText`, Icons `CoinbaseIcons`. |
| Source-stated metrics | YAML `tokens.typography` and body §3 hierarchy (sizes, weights, unitless line-heights, Button tracking `+0.16px`). These are source-stated named-family metrics, not a live-computed type-size evidence chain. |

Treating do-not-replace-these-named-families-with-a-system-substitute, and treating an-unavailable-face-keeps-its-name-and-loses-only-a-live-specimen, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Do not replace these named families with a system substitute. An unavailable face keeps its name and loses only a live specimen.

### Family

- **Display:** `CoinbaseDisplay` — hero headlines
- **UI / Sans:** `CoinbaseSans` — buttons, headings, nav
- **Body:** `CoinbaseText` — reading text
- **Icons:** `CoinbaseIcons` — icon font

Treating CoinbaseDisplay-for-hero-headings-only as the source §7 Do rather than a license to load Display on UI chrome, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

### Type roles

Verified line-height values are the unitless YAML ratios `1.00`, `1.11`, `1.13`, `1.33`, `1.50`, `1.25`, `1.56`, `1.20`, and `1.23`. They scale with font size and are not fixed px. Treating YAML sizes as recorded without a required px suffix, treating body §3 px writings as size-local observations rather than converted YAML numbers, and treating those two writings as unmerged source forms, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Provenance |
|---|---|---:|---:|---:|---|---|
| Display Hero | CoinbaseDisplay | 80px | 400 | 1.00 | | YAML `display-hero` / body Display Hero. Use: Hero headlines, maximum impact |
| Display Secondary | CoinbaseDisplay | 64px | 400 | 1.00 | | YAML `display-secondary`. Use: Sub-hero headlines |
| Display Third | CoinbaseDisplay | 52px | 400 | 1.00 | | YAML `display-third`. Use: Third-tier display |
| Section Heading | CoinbaseSans | 36px | 400 | 1.11 | | YAML `section-heading`. Use: Feature section titles |
| Card Title | CoinbaseSans | 32px | 400 | 1.13 | | YAML `card-title`. Use: Card headings |
| Feature Title | CoinbaseSans | 18px | 600 | 1.33 | | YAML `feature-title`. Use: Feature emphasis |
| Body Bold | CoinbaseSans | 16px | 700 | 1.50 | | YAML `body-bold`. Use: Strong body |
| Body Semibold | CoinbaseSans | 16px | 600 | 1.25 | | YAML `body-semibold`. Use: Buttons, nav |
| Body | CoinbaseText | 18px | 400 | 1.56 | | YAML `body`. Use: Standard reading |
| Body Small | CoinbaseText | 16px | 400 | 1.50 | | YAML `body-small`. Use: Secondary reading |
| Button | CoinbaseSans | 16px | 600 | 1.20 | +0.16px | YAML `button`. Use: Button labels |
| Caption | CoinbaseSans | 14px | YAML 600; body 600–700 | 1.50 | | YAML `caption` weight 600 is not merged with body table 600–700. Use: Metadata |
| Small | CoinbaseSans | 13px | 600 | 1.23 | | YAML `small`. Use: Tags |

Treating YAML `body-semibold` 16px / 600 / 1.25 (Buttons, nav) as unmerged from YAML `button` 16px / 600 / 1.20 / +0.16px tracking, and treating YAML `caption` weight 600 as unmerged from body table 600–700, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

### Assets

Catalog logo: type `simpleicons`, slug `coinbase`. Treating that Simple Icons slug as a catalog identity-boundary record rather than a captured first-party Coinbase mark, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

Source §8 records that Coinbase open-sourced its design system — Coinbase Design System (CDS), internally Cedar — at `cds.coinbase.com` and `github.com/coinbase/cds`. It is a cross-platform React / React Native library of 100+ components.

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

Source §14, preserved:

| State | Treatment |
|---|---|
| **Empty (no portfolio)** | "Make your first purchase" CTA + recurring-buy promotion |
| **Empty (no transactions)** | "Your transaction history will appear here." Plain, no upsell |
| **Loading (price feed)** | Per-cell shimmer; chart shows last cached + stale-indicator |
| **Loading (KYC verification)** | Persistent badge, allows navigation while pending |
| **Error (verification)** | Specific reason + remediation path, never blocking-without-recourse |
| **Error (transaction)** | Receipt-style failure + retry + customer support link |
| **Success (purchase)** | Full receipt: amount, fee, USD value, network confirmation tracker |
| **Success (deposit)** | Confirmation + when funds available, network reliance disclosed |
| **Skeleton (asset list)** | Light gray rows at exact dimensions |
| **Disabled (insufficient funds)** | 0.5 opacity + "Add funds" inline link |
| **Loading (long action: send/withdraw)** | Multi-step progress (Submitted → Confirming → Confirmed) with explicit timing |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. YAML `button-primary` records `focus: 2px solid black outline`; that generic Focus observation is not `focus-visible` treatment evidence and is not copied onto a `focus-visible` row as a colour. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Card, data-table, tooltip, progress, sparkline, and dark-section have YAML `type: card` (or tooltip as `type: card`) without an interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. List Cell has YAML `type: listItem` with media/title/subtitle anatomy and is the mobile substitute for Table; no interactive-kind confirmation is given, so kind and a state-applicability map are omitted.

### Primary Button

- Role: High-emphasis primary CTA, one per screen
- Kind: interactive
- Type: button
- Anatomy: label; slots `startIcon`, `endIcon`
- Background: `#0052ff`
- Text: `#ffffff`
- Radius: `100000px`
- Border: `1px solid #0052ff`
- Font: `16px / 600` CoinbaseSans; tracking `+0.16px`
- Hover: bg `#578bfa`
- Sizes: Compact / Default / Block (fills width)
- Use: YAML `High-emphasis primary CTA, one per screen`. CDS `Button` `variant="primary"`
- YAML `tokens.components.button-primary`
- Field note: The following unmerged-field reading, including this-fill-not-§4-Primary-Pill-`#eef0f3`-or-`#282b31` and this-radius-`100000px`-not-56px, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. `#0052ff` is this control’s fill and catalog `primary_color`. Radius `100000px` is this YAML full-pill field; it is not §4/§7 `56px`. Generic Focus `2px solid black` outline is a named observed state on this control, not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS primary CTA |
| hover | applicable | Pointer-web button; captured treatment bg `#578bfa` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted |
| loading | applicable | Source: loading hides the label and shows an indeterminate ProgressCircle |

Error and success applicability are omitted. Source names this control as the high-emphasis primary CTA (one per screen); purchase/deposit receipts in §14 are product outcomes, not paints on this button.

Additional observed named state: generic Focus `2px solid black` outline, not `focus-visible` evidence.

### Secondary Button

- Role: Medium-emphasis equal-weight actions
- Kind: interactive
- Type: button
- Background: `#eef0f3`
- Text: `#0a0b0d`
- Radius: `100000px`
- Border: `1px solid #eef0f3`
- Font: `16px / 600`
- Use: YAML `Medium-emphasis equal-weight actions`. CDS `variant="secondary"`
- YAML `tokens.components.button-secondary`
- Field note: The following unmerged-field reading, including this-fill-not-YAML-primary-`#0052ff` and same-hex-as-YAML-surface-and-as-one-§4-Primary-Pill-background-named-jobs-unmerged, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. `#eef0f3` is this control’s fill, not YAML primary `#0052ff`. Same hex as YAML `surface` and as one §4 Primary Pill background; named jobs stay unmerged.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML secondary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A medium-emphasis action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source does not map those three outcomes onto this equal-weight action.

### Tertiary / Inverse / Negative Buttons

**Tertiary** — YAML `button-tertiary`. Type: button. Kind: interactive. Background `#eef0f3`, text `#0a0b0d`, radius `100000px`. States: transparent-until-interaction. Use: Low-emphasis action.

**Inverse** — YAML `button-inverse`. Type: button. Kind: interactive. Background `#282b31`, text `#ffffff`, radius `100000px`. Use: High contrast on dark sections.

**Negative** — YAML `button-negative`. Type: button. Kind: interactive. Text `#ffffff`, radius `100000px`. Background is unnamed in YAML; omitted. Use: Destructive only, used sparingly.

Each of tertiary / inverse / negative:

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML variant |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | The action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted for all three. Negative is a destructive variant, not an error state of another button.

### Transparent Button

- Role: Transparent modifier on any variant
- Kind: interactive
- Type: button
- Background: `transparent`
- Text: `#0a0b0d`
- Radius: `100000px`
- States: container visible only on hover/press · sizes Compact/Default/Block · `startIcon`+`endIcon` slots
- Use: YAML `Transparent modifier on any variant`
- YAML `tokens.components.button-transparent`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML transparent modifier |
| hover | applicable | Pointer-web button; container becomes visible on hover/press |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | The action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source maps visibility to hover/press, not to request outcomes.

### Blue Bordered Button

- Role: Secondary CTA pairing
- Kind: interactive
- Type: button
- Background: `transparent`
- Text: `#0052ff`
- Radius: `100000px`
- Border: `1px solid #0052ff`
- Font: `16px / 600`
- Use: YAML `Secondary CTA pairing`
- YAML `tokens.components.button-blue-bordered`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML blue-bordered CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A pairing CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted.

### Icon Button

- Role: Icon-only action, 56px round hit-target
- Kind: interactive
- Type: button
- Background: `transparent`
- Radius: `100000px`
- Height: `56px`
- Use: YAML `Icon-only action, 56px round hit-target`. CDS `IconButton`. Measured `56px` round hit-target on the homepage utility row
- YAML `tokens.components.icon-button`
- Field note: Treating height-`56px`-as-this-hit-target-not-YAML-`rounded.pill`-56-as-a-radius-token, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Height `56px` is this hit-target; it is not YAML `rounded.pill` 56 as a radius token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/live homepage utility IconButton |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An icon-only action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted.

### Chip

- Role: Pill selectable token
- Kind: interactive
- Type: badge
- Background: `#eef0f3`
- Text: `#0a0b0d`
- Radius: `100000px`
- Selected (named): bg `#0052ff`
- Use: YAML `Pill selectable token`
- YAML `tokens.components.chip`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML chip |
| hover | applicable | Pointer-web selectable token; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A selectable token can be unavailable; visual treatment omitted |
| loading | not-applicable | A chip selects a token; the chip itself does not enter a loading state |
| error | not-applicable | Chip meaning is selected versus resting, not a request or validation failure on the chip |
| success | not-applicable | Chip meaning is selection, not action-outcome confirmation |

Additional observed named state: selected bg `#0052ff`. Treating that appearance as a captured variant, not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

### Nav Tab Chip

- Role: Header category chip
- Kind: interactive
- Type: tab
- Text: `#0667d0`
- Radius: `16px`
- Padding: `4px 16px`
- Font: `14px / 400`
- Use: YAML `Header category chip`. Live: `16px` radius, `4px 16px` padding, `14px` CoinbaseSans; link text in Link Blue (`#0667d0`)
- YAML `tokens.components.nav-tab-chip`
- Field note: The following unmerged-field reading, including radius-`16px`-not-pill-`56px`-not-full-`100000px` and font-`14px / 400`-not-YAML-button-`16px / 600`, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Radius `16px` is this chip; it is not pill `56px` and not full `100000px`. Font `14px / 400` is not YAML button `16px / 600`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/live header category chip |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav chip can be unavailable; visual treatment omitted |
| loading | not-applicable | A category chip selects a destination; the chip itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the chip |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

### Text Input

- Role: Bordered text field
- Kind: interactive
- Type: input
- Background: `#ffffff`
- Text: `#0a0b0d`
- Border: `1px solid rgba(91,97,110,0.2)`
- Radius: `8px`
- Height: `56px`
- Padding: `16px`
- Font: `16px / 400`
- Label: `outside` (above) or `inside` (floats when unfocused)
- Use: YAML `Bordered text field`. CDS `TextInput`
- YAML `tokens.components.text-input`
- Field note: The following unmerged-field reading, including height-`56px`-not-IconButton-height-as-a-second-token, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Height `56px` is this field; it is not IconButton height as a second token. Read-only takes a secondary background and remains focusable; disabled inputs are visually distinct. `variant="negative"` auto-sets `aria-invalid="true"` and expects `"Error: …"` helper text; `variant="positive"` for validated values.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS TextInput |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | Source: disabled inputs are visually distinct; treatment omitted as a computed paint |
| error | applicable | Form field; `variant="negative"` + `aria-invalid` + `"Error: …"` helper. Treatment omitted as a computed paint beyond that contract |
| success | applicable | Form field; `variant="positive"` for validated values. Treatment omitted as a computed paint |

Loading applicability is omitted. Source does not map a loading variant onto TextInput.

### Search Input

- Role: Inline search field
- Kind: interactive
- Type: input
- Background: `transparent`
- Text: `#0a0b0d`
- Border: `none`
- Font: `16px / 400`
- States: borderless inline · leading search glyph
- Use: YAML `Inline search field`. CDS `SearchInput`
- YAML `tokens.components.search-input`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS SearchInput |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | A search field can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as borderless inline search chrome, not a validated form field.

### Switch

- Role: On/off toggle
- Kind: interactive
- Type: toggle
- Checked (named): `controlColor` resolves to `#0052ff`; optional thumb elevation/shadow
- Use: YAML `On/off toggle`. CDS `Switch`
- YAML `tokens.components.switch`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS Switch |
| hover | applicable | Pointer-web toggle; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A toggle can be unavailable; visual treatment omitted |
| loading | not-applicable | A switch is on/off; the control itself does not enter a loading state |
| error | not-applicable | Toggle meaning is checked versus unchecked, not a request or validation failure on the switch |
| success | not-applicable | Toggle meaning is on/off, not action-outcome confirmation |

Additional observed named state: checked fill `#0052ff`. Treating that appearance as a captured variant, not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

### Checkbox / Radio

- Role: Checkbox and Radio selection
- Kind: interactive
- Type: toggle
- Selected (named): fill `#0052ff`
- States: Cell + Group wrappers
- Use: YAML `Checkbox and Radio selection`
- YAML `tokens.components.checkbox-radio`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML Checkbox/Radio |
| hover | applicable | Pointer-web selection control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A choice can be unavailable; visual treatment omitted |
| loading | not-applicable | A checkbox/radio selects an option; the control itself does not enter a loading state |
| error | not-applicable | Selection meaning is selected versus resting, not a request failure on the control |
| success | not-applicable | Selection meaning is choice, not action-outcome confirmation |

### Segmented Control

- Role: Time-range and view switches
- Kind: interactive
- Type: tab
- Background: `#eef0f3`
- Active (named): active segment `#0052ff`
- Radius: `100000px`
- Use: YAML `Time-range and view switches`. CDS `SegmentedControl`
- YAML `tokens.components.segmented-control`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML SegmentedControl |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A segment can be unavailable; visual treatment omitted |
| loading | not-applicable | A segmented control selects a view; the control itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the segment |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

### Card

- Role: Content container family
- Type: card
- Background: `#ffffff`
- Border: `1px solid rgba(91,97,110,0.2)`
- Radius: `8px–40px`
- Shadow: none — depth from section contrast
- States: ContentCard Header/Body/Footer · DataCard/MediaCard/NudgeCard/UpsellCard
- Use: YAML `Content container family`
- YAML `tokens.components.card`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Data Table

- Role: Tabular data, desktop only
- Type: card
- Border: `1px solid rgba(91,97,110,0.2)`
- States: desktop-only (Lists View on mobile) · variants default/graph/ruled · required header row · sortable · row/column spans · sticky header · `TableCellFallback` skeleton
- Use: YAML `Tabular data, desktop only`. CDS `Table`
- YAML `tokens.components.data-table`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted. `TableCellFallback` skeleton rows remain a named table behavior, not a closed Core row.

### List Cell

- Role: Mobile substitute for Table, asset rows
- Type: listItem
- Text: `#0a0b0d`
- States: leading CellMedia + title/subtitle
- Use: YAML `Mobile substitute for Table, asset rows`. `ListCell` / `ContentCell`
- YAML `tokens.components.list-cell`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Modal

- Role: Header/Body/Footer modal
- Kind: interactive
- Type: dialog
- Background: `#ffffff`
- Shadow: scrim overlay + FocusTrap
- States: `visible` + `onRequestClose` · `restoreFocusOnUnmount=false` for chains · FullscreenModal/Tray/Alert/FullscreenAlert siblings
- Use: YAML `Header/Body/Footer modal`. CDS `Modal`
- YAML `tokens.components.modal`
- Field note: Treating `Tray`-bottom-sheet-replaces-`Modal`-on-small-screens as source-stated layout substitution rather than a second modal fill, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. `Tray` (bottom sheet) replaces `Modal` on small screens (source Responsive Behavior). That replacement is layout behavior, not a second modal fill.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS Modal |
| hover | applicable | Pointer-web dialog chrome; visual treatment omitted |
| focus-visible | applicable | Interactive control; FocusTrap is documented; `focus-visible` paint omitted |
| disabled | applicable | Dialog actions can be unavailable; visual treatment omitted |
| loading | not-applicable | The dialog is a container; loading belongs to inner content, not the modal chrome |
| error | not-applicable | Request failure is not a validation state of the dialog chrome |
| success | not-applicable | Completion is not a success confirmation painted on the dialog chrome |

### Toast

- Role: Transient status message
- Kind: interactive
- Type: toast
- States: bottom-anchored · auto-dismiss 5s base + close button · `bgPositive`/`bgNegative`/`bgWarning` · `role=alert` · persists on hover
- Use: YAML `Transient status message`. CDS `Toast`. `bottomOffset` clears bottom nav
- YAML `tokens.components.toast`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS Toast |
| hover | applicable | Pointer-web; source: persists on hover |
| focus-visible | applicable | Interactive control (close button); visual treatment omitted |
| disabled | applicable | A toast close action can be unavailable; visual treatment omitted |
| loading | not-applicable | A toast reports status; the toast itself does not enter a loading state |
| error | applicable | `bgNegative` is the negative/status variant |
| success | applicable | `bgPositive` is the positive/status variant |

`bgWarning` is an additional named variant, not a Core canonical row.

### Tooltip

- Role: Contextual hints and first-run tours
- Type: card
- States: Tooltip + PopoverPanel + Coachmark
- Use: YAML `Contextual hints and first-run tours`
- YAML `tokens.components.tooltip`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Banner

- Role: Feedback and status messaging
- Kind: interactive
- Type: card
- Border: global avoids custom radius — flush with status bar
- States: styles global/inline/contextual · variants informational/warning/error/promotional · `startIcon`+`title`+`children`+`primaryAction`+`secondaryAction`+`showDismiss`
- Use: YAML `Feedback and status messaging`. CDS `Banner`
- YAML `tokens.components.banner`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/CDS Banner |
| hover | applicable | Pointer-web (actions/dismiss); visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Banner actions can be unavailable; visual treatment omitted |
| loading | not-applicable | A banner reports status; the banner itself does not enter a loading state |
| error | applicable | Source variant `error` |

Success applicability is omitted.

### Progress

- Role: Determinate/indeterminate progress
- Type: card
- Foreground: `#0052ff`
- States: ProgressCircle determinate 0–100% with % overlay / indeterminate `fgMuted` arc · stroke thin 2px / normal 4px / semiheavy 8px / heavy 12px · ProgressBar (fixed and floating label variants) + Spinner
- Use: YAML `Determinate/indeterminate progress`
- YAML `tokens.components.progress`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted. Primary Button loading uses an indeterminate ProgressCircle; that usage stays on the button.

### Sparkline

- Role: Inline price/chart visualization
- Type: card
- Foreground: `#0052ff`
- States: Sparkline/LineChart/AreaChart/BarChart/PercentageBarChart with `XAxis`/`YAxis`/`Scrubber` · live price ticks flash green/red (disabled under reduced-motion)
- Use: YAML `Inline price/chart visualization`
- YAML `tokens.components.sparkline`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted. The green/red flash is the Motion `motion-pulse` signature, not a Core canonical row.

### Dark Section

- Role: Dark feature section
- Type: card
- Background: `#0a0b0d`
- Text: `#ffffff`
- Active (named): accent links `#0052ff`
- Use: YAML `Dark feature section`
- YAML `tokens.components.dark-section`

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### §4 Primary Pill recipe (body-only; conflicts with YAML primary)

Source §4 “Primary Pill (56px radius)” is not merged with YAML `button-primary`. Treating that unmerged-recipe reading, including `#eef0f3`-or-`#282b31`-fill-not-YAML-`#0052ff`, 56px-not-`100000px`, matching-`1px solid`-border, hover-`#578bfa`, and Focus-`2px solid black`-outline, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

- Background: `#eef0f3` or `#282b31`
- Radius: 56px
- Border: `1px solid` matching background
- Hover: `#578bfa`
- Focus: `2px solid black` outline
- Full Pill (`100000px` radius) is recorded in the same §4 block as a separate maximum-pill shape

### CDS-documented siblings without YAML `tokens.components` rows

Treating Type-not-invented-for-CDS-documented-siblings-without-YAML-`tokens.components`-rows, including top-nav / Tabs / Stepper / Pagination, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. Type is not invented for these. Source §8 names them:

- **top-nav** — sticky `header` containing brand wordmark, category chips, search, and Sign in / Sign up CTAs
- **tabs** — CDS `Tabs` (current; `TabNavigation` is deprecated). Primary and secondary variants, active tab tracked by `value`, underline `TabIndicator`, full W3C tab keyboard pattern with arrow-key wrap-around and overflow arrows
- **stepper / pagination** — `Stepper` for multi-step flows (mirrors Submitted → Confirming → Confirmed); `Pagination` for paged tables

### Local recipes from source §9 (wrappers deleted; unique tuples kept)

Treating the following parent-child tuples as local recipes rather than extra global tokens, including white-hero-canvas-`#ffffff`-CoinbaseDisplay-80px-line-height-1.00-pill-CTA-`#eef0f3`-56px-hover-`#578bfa` and dark-section-`#0a0b0d`-CoinbaseDisplay-64px-white-blue-accent-link-`#0052ff`, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

**White hero**

- Canvas `#ffffff`
- CoinbaseDisplay 80px, line-height 1.00
- Pill CTA `#eef0f3`, 56px radius
- Hover `#578bfa`

**Dark section**

- Background `#0a0b0d`
- CoinbaseDisplay 64px white text
- Blue accent link `#0052ff`

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Treating YAML-spacing-numbers-and-the-§5-px-scale as remaining-the-spacing-record, and treating card-radius-nav-chip-input-IconButton-pill-geometry as stay-component-local, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification. YAML spacing numbers (xs 4 through section 64) and the §5 px scale (1px through 48px, Base 8px) remain the spacing record. Card radius `8px–40px`, nav chip `16px` / `4px 16px`, input `56px` height / `16px` padding, IconButton `56px`, and pill `56px` versus `100000px` stay component-local.

Source CDS breakpoint table (column unit px):

| Breakpoint | px | Notes |
|---|---|---|
| xs | 400 | Smallest phone |
| sm | 576 | Phone landscape |
| md | 640 | Large phone / small tablet |
| lg | 768 | Tablet |
| xl | 896 | Tablet landscape |
| xxl | 1280 | Desktop |
| 3xl | 1440 | Wide desktop |
| 4xl | 1600 | Max content width |

Measured live viewport 1440px. `Table` is desktop-only and swaps to `ListCell` / Lists View below tablet; `Tray` (bottom sheet) replaces `Modal` on small screens.

Treating that table as a recorded span of named CDS widths, treating 1440px as the measured live viewport rather than a cross-viewport specification of every unlisted control, and treating Table→ListCell and Modal→Tray as source-stated substitutions rather than a complete responsive specification, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed

The live string below is source-stated. Treating the parenthetical characterization (homepage-tagline-2026-05) as citation-character of that live string is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

- Tagline: *"Buy and Sell Bitcoin, Ethereum, and more with trust"* <!-- verified: coinbase.com homepage 2026-05 -->

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Coinbase-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — compliance-aware-but-friendly, fintech-through-SEC-scrutiny, legal-precision-and-consumer-warmth, avoids-hyped-crypto-bro-language, plainspoken-trust-tagline, regulatory-clarity-over-speculative-excitement, verb-first-CTAs, never-Trade-now, marketing-trust-emphasized, error-verification-specific, success-receipt-detail, and risk-warnings-required-and-prominent — are a derived editorial implementation inference from the verified surfaces; they are not Coinbase-authored or a separately published UI specification. They are not the Observed tagline above.

Source §10 describes Coinbase’s voice as **compliance-aware-but-friendly** — a fintech that's been through SEC scrutiny and writes microcopy with both legal precision and consumer warmth. Marketing copy avoids hyped crypto-bro language ("moon", "lambo") and stays plainspoken. Product UI emphasizes regulatory clarity (KYC required, rates explicit, fees disclosed) over speculative excitement.

| Context | Tone |
|---|---|
| CTA | Verb-first. "Sign up", "Buy", "Sell". Never "Trade now!!" |
| Marketing | Trust-emphasized. Plain language about regulation/security |
| Error (verification) | Specific. "Verification incomplete. Upload a government-issued ID to continue." |
| Success (transaction) | Receipt-detail. Asset, amount, fee, USD equivalent, network confirmation status |
| Risk warnings | Required and prominent — never minimized in fine print |

**Forbidden phrases** (source). "To the moon", "diamond hands". Hyperbolic ROI claims. Hidden fees. "Risk-free" framing on volatile assets.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `standard cubic-bezier` easing curve (source language omitted; no exact cubic-bezier value supplied)
- `focus-visible` visual treatments (generic Focus `2px solid black` outline is a different observation)
- loading, error, and success visual treatments on harvested buttons other than Primary Button loading (label hidden + ProgressCircle); omitted loading/error/success applicability on Secondary / Tertiary / Inverse / Negative / Transparent / Blue Bordered / Icon Button / Search Input
- interactive kind and state-applicability map for Card, Data Table, List Cell, Tooltip, Progress, Sparkline, and Dark Section
- Type on top-nav, Tabs, Stepper, and Pagination (no YAML `tokens.components` row)
- YAML `button-negative` background
- motion animation names, transition properties, and any easing beyond source `no bounce` — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
- Coinbase Wallet, Coinbase Prime, and Base as inspected interface tokens
