# CJ ONSTYLE Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

CJ ONSTYLE (CJ온스타일). Catalog homepage identity is `https://www.cjonstyle.com`. Treating that product-origin sentence as catalog identity rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

Treating the following URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, homepage-value-not-a-proxy-for-every-PDP-module, PDP-buy-CTA-not-a-homepage-chrome-fill, medium-tech-blog-as-country-identity-not-a-token-capture, and redirect-host-as-the-same-captured-homepage-rather-than-a-second-token-set, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. This contract covers the 2026-07-02 live inspect of the CJ ONSTYLE homepage requested at `https://www.cjonstyle.com` and resolved to `display.cjonstyle.com/p/homeTab/main`, and the product-detail surface at `https://display.cjonstyle.com/p/item/2086524438`. Source footer also names `https://medium.com/cj-onstyle` as a Tier 1 host. Color, type, and component values below stay attached to the surface that established them. A homepage chrome value is not a proxy for every PDP module. The PDP `바로구매` fill is not a homepage GNB fill. `https://medium.com/cj-onstyle` is a country-identity / engineering-blog source in the source packet; it is not an extra token capture in this reconstruction.

Source token note: `primary = live buy-CTA purple (#640faf, rgb 100,15,175) — CJ ONSTYLE signature violet, 52 bg + 143 fg occurrences; sale/price accent magenta (#ec0040); near-flat (box-shadow none). Body font legacy Nanum Barun Gothic; newer promo/PDP modules use Pretendard.` Treating that note as a register split — `#640faf` is catalog `primary_color` and the harvested PDP `바로구매` fill rather than sale magenta `#ec0040` and rather than promo red `#d53225` or promo orange `#f26d00`; `#ec0040` is the price/sale accent rather than a second buy-CTA fill; `#ffffff` is canvas rather than a second paint from on-primary `#ffffff`; `#000000` is default ink rather than nav ink `#111111` or body `#2a2a2a`; and `box-shadow: none` is near-flat rather than a shadow ladder — is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

The following atmosphere readings — dense-information-rich-conversion-first-retail-grid, not-an-airy-editorial-site, white-canvas-segmented-by-cool-grey-bands, unadorned-black-and-near-black-for-scan-ability, signature-violet-reserved-almost-exclusively-for-the-primary-buy-action, shopper-eye-trained-to-read-that-hue-as-commit-to-purchase, functional-legacy-Korean-rather-than-boutique, quiet-Nanum-Barun-Gothic-for-the-shell-and-punchy-Pretendard-for-the-pitch, near-total-absence-of-elevation, sharp-geometry, and fast-flat-high-density-commerce-surface-engineered-for-throughput — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification. The source describes CJ ONSTYLE as Korea's flagship TV-home-shopping-and-commerce brand. The canvas is pure white (`#ffffff`) segmented by cool-grey bands (`#f5f5f5`, `#f0f0f0`). Type sits in unadorned black (`#000000`) and near-black navy-grey (`#111111`, `#2a2a2a`). The single saturated brand color is deep violet (`#640faf`, rgb 100,15,175). Body and navigation run in **Nanum Barun Gothic** (나눔바른고딕) at a dense 12–15px, weight 400; the wordmark and section heads step up to weight 700. Newer promotional and product-detail modules layer in **Pretendard**: banner headlines at 26px/700 and discount badges at 12px/700. Live inspection returned `box-shadow: none` across the nav, GNB, buy CTA, and product cards. Geometry is recorded as 0px corners on nav, inputs, and the main buttons, a dominant 2px micro-radius on sale/benefit overlay pills, and a 4px radius on the purchase buttons. The pricing layer adds magenta (`#ec0040`), with promotional module reds (`#d53225`) and oranges (`#f26d00`).

Treating the following public-history facts as narrative rather than interface tokens, including CJ-Group-commerce-arm, born-from-CJ오쇼핑-CJ홈쇼핑, relaunched-under-CJ-ONSTYLE-in-2021, converged-live-commerce-plus-mobile-shopping, unified-TV-broadcast-mobile-app-and-web-storefront-as-a-single-style-and-living-destination, and ON-plus-STYLE-as-a-name-reading, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. CJ ONSTYLE is the commerce arm of Korea's CJ Group, born from the country's pioneering TV home-shopping business (CJ오쇼핑 / CJ홈쇼핑) and relaunched under the **CJ ONSTYLE** brand in **2021** as a converged "live commerce + mobile shopping" platform. The rebrand unified CJ's TV broadcast, mobile app, and web storefront into a single style-and-living destination. Public-history narrative provides that context; it does not by itself supply interface tokens.

Treating the name itself as fusing ON (always-on, on-air, online) with STYLE, and treating that name as signaling the shift from a channel you watch to a shopping surface you live in, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. The name itself fuses "ON" (always-on, on-air, online) with "STYLE," signaling the shift from a channel you watch to a shopping surface you live in.

Treating the product DNA as retail-television, treating a host's job as making the value legible in seconds and moving the viewer to purchase before the segment ends, and treating that urgency as baked into the digital surface — the offer leads, the benefit badges stack, and the violet buy button is never more than a glance away — is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. The product DNA is retail-television: a host's job is to make the value legible in seconds and move the viewer to purchase before the segment ends. That urgency is baked into the digital surface — the offer leads, the benefit badges stack, and the violet buy button is never more than a glance away.

Treating CJ ONSTYLE as a curated style-and-home destination rather than a bargain-bin marketplace, and treating that positioning as why the chrome stays clean and black-on-white while the promotional layer carries the color and noise, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. CJ ONSTYLE positions itself as a curated style-and-home destination rather than a bargain-bin marketplace, which is why the chrome stays clean and black-on-white while the promotional layer carries the color and noise.

The following refusal, embrace, heavy-decorative-card-stacking, cold-minimalism-of-design-boutique-commerce, density-that-respects-a-catalog-of-thousands-of-SKUs, single-trustworthy-violet-for-the-commit-action, and magenta-price-accent-that-reads-instantly-as-here-is-the-deal readings are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification. What the source treats the design as refusing: the heavy, decorative card-stacking of legacy portals, and the cold minimalism of design-boutique commerce. What it treats the design as embracing: density that respects a catalog of thousands of SKUs, a single trustworthy violet for the commit action, and a magenta price accent.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings / live labels below as Primary tasks, and not lifting tasks from source §13 fictional archetypes, and treating independently-verified-in-this-packet as the inspected homepage and PDP URLs rather than the named CTA and tile labels, which remain harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Independently verified in this packet are the inspected homepage and PDP URLs; named CTA and nav labels are harvested strings/controls, not independently verified destination routes.

- Act on a harvested PDP primary buy CTA (YAML use: `PDP 바로구매 primary buy CTA — the single saturated action color`).
- Search from the harvested header search field (YAML use: `Header search field, underline style`).
- Scan harvested product grid cards on home / listing (YAML use: `Product grid card on home / listing, near-flat`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. No individual personas are promoted. Observable work follows the three primary tasks.

### Distinctive traits

The following unmerged-role extras — treating `#640faf` as catalog `primary_color` and the harvested PDP `바로구매` fill rather than sale `#ec0040`, treating `#ec0040` as the price/sale accent rather than a second buy fill, treating `#d53225` / `#f26d00` as unmerged promo accents, treating `#000000` / `#111111` / `#2a2a2a` as unmerged inks, treating canvas `#ffffff` as unmerged from on-primary `#ffffff`, treating `#f5f5f5` as unmerged from `#f0f0f0`, treating `#e5e5e5` as unmerged from `#b2b2b2`, treating `#26292a` as utility chrome rather than ink, treating 0px nav/inputs as unmerged from 2px badges/cards, 4px buy buttons, 11px chips, 18px carousel prev/next, YAML `rounded.full` 9999, and carousel pause 50%, treating 60px buy height as unmerged from 40px inquiry, 46px search, 34px utility bar, and 36px carousel, treating Nanum Barun Gothic as the dense shell rather than Pretendard, and treating 700/400 as the recorded hierarchy engine — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

- Catalog `primary_color` / CJ ONSTYLE Violet `#640faf` as the harvested PDP `바로구매` fill and active-state text
- Sale magenta `#ec0040` as the price/sale accent (118 text occurrences in the live scan)
- Nanum Barun Gothic for the dense shell (nav/body 12–15px); Pretendard for loud promo/PDP copy
- Black (`#000000`) and near-black (`#111111`, `#2a2a2a`) text on white for tile scan-ability
- Near-flat depth: `box-shadow: none`; separation via `#f5f5f5`/`#f0f0f0` tints and `#e5e5e5` hairlines
- Sharp geometry — 0px nav/inputs, 2px sale badges and product cards, 4px buy buttons; 11px/18px and 50% only where named
- Dark utility chrome bar (`#26292a`) and grey utility links (`#767676`, `#929292`, `#666666`)

### Principles

These five items (The offer leads, One color means "buy.", Density is a feature, not a flaw, Flat and fast, Quiet shell, loud pitch), including each stem and each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

1. **The offer leads.** Home shopping sells the value first. *UI implication:* surface discount rate, benefit badges, and price before secondary detail; let the magenta (`#ec0040`) price accent do the pulling.
2. **One color means "buy."** Violet (`#640faf`) is the commit signal. *UI implication:* reserve the saturated violet for the primary buy CTA and active states — never dilute it across decorative chrome.
3. **Density is a feature, not a flaw.** A catalog of thousands must be scannable. *UI implication:* pack tiles tightly, keep type dense (12px body), and prefer tint/hairline separation over space-hungry cards.
4. **Flat and fast.** Elevation slows a high-traffic grid. *UI implication:* no shadows; separate with `#f5f5f5`/`#f0f0f0` tint and `#e5e5e5` hairlines; keep the page quick to paint and scan.
5. **Quiet shell, loud pitch.** *UI implication:* Nanum Barun Gothic black-on-white for the navigation and product chrome; Pretendard bold plus the violet/magenta palette for the promotional layer.

Treating the following as a capture-bound application of source §7 Do's, including violet-`#640faf`-for-the-primary-buy-action-and-active-states, magenta-`#ec0040`-for-discount-rates-and-price, Nanum-Barun-Gothic-12–15px-weight-400-for-body-and-nav, Pretendard-weight-700-for-loud-promo-and-badges, tint-and-hairline-separation, sharp-geometry-0px-2px-4px, black-and-near-black-product-titles, and sale-badges-on-a-`rgba(0,0,0,0.2)`-scrim, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- Reserve violet (`#640faf`) for the primary buy action and active states — keep it the single "commit" color.
- Use magenta (`#ec0040`) for discount rates and price emphasis.
- Set body and nav in Nanum Barun Gothic at a dense 12–15px, weight 400.
- Use Pretendard weight 700 for loud promo headlines and sale/benefit badges.
- Separate sections with flat tint (`#f5f5f5`/`#f0f0f0`) and `#e5e5e5` hairlines, not shadows.
- Keep geometry sharp — 0px on nav/inputs, 2px on badges, 4px on buy buttons.
- Use black (`#000000`) / near-black (`#111111`) for product titles for maximum tile scan-ability.
- Overlay sale badges on a `rgba(0,0,0,0.2)` scrim so white text stays legible on imagery.

### Avoid

The following items copy source §7 Don'ts, including violet-as-the-single-buy-action-signal, no-drop-shadows, no-Pretendard-on-the-shell, no-large-pill-radii-on-buttons-or-cards, no-generous-whitespace-at-the-expense-of-product-density, no-second-saturated-action-color, and magenta-`#ec0040`-not-for-navigation-or-non-price-UI. They are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

- Don't spread violet across many elements — it dilutes the single buy-action signal.
- Don't use drop shadows for elevation — CJ ONSTYLE is a near-flat system.
- Don't set the shell in Pretendard — Nanum Barun Gothic owns the dense body/nav.
- Don't use large pill radii on buttons or cards — buttons are 4px, cards 2px; pills only appear on carousel controls.
- Don't add generous whitespace at the expense of product density — this is a throughput-first catalog.
- Don't introduce a second saturated action color — violet is the only "commit" hue.
- Don't use magenta (`#ec0040`) for navigation or non-price UI — it reads exclusively as "deal/price." Treating that last-bullet characterization as a reconstruction reading of source §7 rather than CJ ONSTYLE-authored color doctrine is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including primary-CTA-fill-not-sale-magenta, sale-magenta-not-a-second-CTA, promo-red-not-promo-orange, canvas-white-not-on-primary-as-a-second-paint, ink-black-not-nav-ink-not-body-grey, muted-not-utility-not-faint, surface-grey-not-surface-alt, hairline-not-border-strong, and chrome-dark-not-ink — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification. Catalog `primary_color` `#640faf` is YAML `primary` and the harvested PDP buy-CTA fill; it is not sale `#ec0040`. YAML `sale` `#ec0040` is the price/sale accent. YAML `accent-red` `#d53225` and `accent-orange` `#f26d00` are unmerged promo accents. YAML `canvas` `#ffffff` is page background and card surfaces. YAML `on-primary` `#ffffff` is text on violet buy buttons and dark chrome; it is the same hex as canvas, not a second paint. YAML `ink` `#000000`, `ink-nav` `#111111`, and `body` `#2a2a2a` are unmerged inks. YAML `muted` `#666666`, `muted-alt` `#767676`, and `faint` `#929292` are unmerged greys. YAML `surface` `#f5f5f5` and `surface-alt` `#f0f0f0` are unmerged tints. YAML `hairline` `#e5e5e5` and `border-strong` `#b2b2b2` are unmerged borders. YAML `header-dark` `#26292a` is utility chrome. YAML component fields such as buy-primary `fg` `#ffffff` and gnb-tab `fg` `#111111` stay on those controls.

- **CJ ONSTYLE Violet / Primary** (`#640faf`): catalog `primary_color`. YAML `primary`. Harvested PDP `바로구매` / `상품문의` fill, active-state text, and brand accents. 52 background and 143 text occurrences in the live scan.
- **On Primary** (`#ffffff`): YAML `on-primary`. White text on the violet buy buttons and dark chrome. Same hex as canvas; jobs stay unmerged.
- **Sale Magenta** (`#ec0040`): YAML `sale`. Discount rates and price emphasis. 118 text occurrences. Not a buy-CTA fill.
- **Promo Red** (`#d53225`): YAML `accent-red`. Secondary promotional red for campaign badges and urgency modules.
- **Promo Orange** (`#f26d00`): YAML `accent-orange`. Tertiary promotional orange for time-limited / benefit highlights.
- **Ink Black** (`#000000`): YAML `ink`. Default body text and product titles. Dominant foreground (2424 occurrences).
- **Nav Ink** (`#111111`): YAML `ink-nav`. Global-nav and menu text, input text.
- **Body Grey** (`#2a2a2a`): YAML `body`. Dark secondary body copy and descriptions.
- **Muted Grey** (`#666666`): YAML `muted`. Tertiary text, product meta, captions.
- **Utility Grey** (`#767676`): YAML `muted-alt`. Utility links (로그인/마이존/장바구니), inactive tab labels.
- **Faint Grey** (`#929292`): YAML `faint`. Lowest-emphasis labels, fine print, placeholders.
- **Pure White** (`#ffffff`): YAML `canvas`. Page background, card surfaces.
- **Surface Grey** (`#f5f5f5`): YAML `surface`. Cool-grey tinted surface for content bands.
- **Surface Alt** (`#f0f0f0`): YAML `surface-alt`. Secondary grey surface for alternating blocks.
- **Hairline** (`#e5e5e5`): YAML `hairline`. Thin borders, tab underlines, and dividers.
- **Border Strong** (`#b2b2b2`): YAML `border-strong`. Heavier outline for the 찜 wishlist toggle and carousel controls.
- **Chrome Dark** (`#26292a`): YAML `header-dark`. Near-black background for the top utility/skip-link chrome bar.

### Spacing

YAML scale: xs 2, sm 4, base 8, md 12, lg 20, xl 26, xxl 48. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names ~8px as a base unit and lists 2px, 4px, 8px, 12px, 20px, 26px, 48px. Treating those YAML numbers as recorded without a required px suffix, treating body 2px / 4px / 8px / 12px / 20px / 26px / 48px as body-recorded steps rather than a rewrite of the YAML keys, treating YAML xxl 48 as a YAML step rather than adding a px suffix to that number, and treating harvested control heights 60px / 40px / 46px / 34px / 36px and paddings `0px 8px` / `0px 20px 0px 26px` as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

The following ~8px-base / dense-at-the-small-end / spacing-tuned-per-module-rather-than-from-a-strict-global-token-set / legacy-retail-grid readings are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification. Body layout states a base unit of ~8px, dense at the small end (2px, 4px, 8px). Notable body paddings: sale-badge `0px 8px`; the utility bar uses asymmetric `0px 20px 0px 26px`.

### Shape

YAML `rounded`: xs 2, sm 4, md 11, lg 18, full 9999.

The following local-geometry reading, including 0px-on-nav-and-inputs, 2px-on-sale-badges-and-product-cards, 4px-on-buy-inquiry-wishlist, 11px-on-pill-chips, 18px-on-carousel-prev-next, 50%-on-carousel-pause, no-pill-shapes-on-buttons-or-cards, and YAML-full-9999-as-a-YAML-step-not-merged-with-50%, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- Nav / inputs: 0px (body; not a YAML `rounded` key)
- Micro (YAML `rounded.xs` 2): sale/benefit badges, product cards — the dominant rounding (×225 in scan)
- Small (YAML `rounded.sm` 4): buy / inquiry / wishlist buttons
- Chip (YAML `rounded.md` 11): pill chips and small controls
- Control (YAML `rounded.lg` 18): carousel prev/next controls
- Carousel pause: 50%
- YAML `rounded.full` 9999: recorded as a YAML step. Source body writes `9999px / 50%` for circular carousel pause / round controls; 9999 is not converted into a harvested pill radius and no `px` suffix is added to the YAML number. 9999 and 50% stay unmerged.

Treating 0px / 2px / 4px / 11px / 18px / 50% as local harvested geometry on the named controls rather than a universal radius for every unlisted control, and treating YAML `full` 9999 as a YAML step rather than merged with 50% or with 0px, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. 2px on badges and cards is local harvested geometry, not a universal radius for every unlisted control. YAML `full` 9999 is recorded as a YAML step; it is not merged with carousel pause 50% and not merged with input 0px.

### Elevation

The following elevation-table Use readings, including page-background-product-tiles-nav-buttons, card-section-separation-without-elevation, card-outlines-tab-underlines-dividers, sale-badges-plus-text-legibility-over-product-photos, plus the extra philosophy readings named here rather than as sentences after the table — near-shadowless / live-inspection-`box-shadow: none` / flatness-characteristic-of-high-density-Korean-retail / printable-flat / free-of-the-heavy-card-stack-look — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, product tiles, nav, buttons |
| Tint (Level 1) | `#f5f5f5` / `#f0f0f0` background shift | Card/section separation without elevation |
| Hairline (Level 2) | `1px solid #e5e5e5` border | Card outlines, tab underlines, dividers |
| Scrim (Level 3) | `rgba(0,0,0,0.2)` overlay on imagery | Sale badges + text legibility over product photos |

YAML `tokens.shadow.none`: `none`.

Treating live-inspection-`box-shadow: none` across header, GNB, buy CTA, and product cards as the recorded near-flat contract for those observed elements, treating tint-and-hairline as grouping rather than a shadow ladder, and treating the `rgba(0,0,0,0.2)` scrim as imagery-legibility rather than elevation, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Live inspection found `box-shadow: none` across the header, GNB, buy CTA, and product cards. Depth and grouping are communicated through flat tinted surfaces (`#f5f5f5`, `#f0f0f0`) and thin `#e5e5e5` hairlines, with a translucent black scrim (`rgba(0,0,0,0.2)`) used only to keep white badge text legible over product imagery.

### Motion

Source-stated duration roles. Treating the duration table, easing names, signature motions, and reduced-motion line as source-stated rather than computed CSS, and treating Arriving / Dismissals / Two-way / spec-template-ease-exit-match / no-bounce-no-spring / commerce-grid-signals-reliability-and-speed-not-playfulness / hero-carousel-auto-advances-on-a-slow-cadence / tap-feedback-subtle-opacity-scale / storefront-remains-fully-functional readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, tap feedback, focus |
| `motion-standard` | 200ms | Tile/section reveal, dropdown, tab switch |
| `motion-slow` | 400ms | Hero banner carousel auto-advance transition |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — dropdowns, sheets, tiles |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions, carousel slide |

Signature motions (source-stated):

Treating no-bounce-or-spring on the commerce grid as a reliability-and-speed reading, treating the hero-banner-carousel-auto-advance as source-stated rather than computed CSS, and treating the following signature-motion readings — tap-feedback-as-a-subtle-opacity-scale-shift, carousel-stops-auto-advancing, and the-storefront-remains-fully-functional — as editorial rather than computed CSS, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

Source forbidden: nothing bounces or springs. The hero banner carousel auto-advances on a slow cadence with a horizontal slide. Tap feedback on buttons and tiles is a subtle opacity/scale shift.

1. **Hero banner carousel.** Auto-advance at `motion-slow` with a horizontal slide.
2. **Tap feedback.** Subtle opacity/scale shift at `motion-fast`.
3. **Reduce motion.** Under `prefers-reduced-motion: reduce`, the carousel stops auto-advancing and all transitions collapse to instant; the storefront remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-computed-Nanum-Barun-Gothic-plus-Pretendard, YAML-family-keys-naming-those-live-computed-families, fallback-stack-not-the-brand-face, two-fonts-two-jobs, and outside-the-captured-homepage-and-PDP, are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Captured homepage and PDP compute Nanum Barun Gothic (나눔바른고딕) as the document default for nav, body, product meta, and the wordmark, and Pretendard for newer promotional banners, sale/benefit badges, and the header search input. YAML `family.base` `Nanum Barun Gothic` and YAML `family.alt` `Pretendard` name those live computed families. Wordmark / H1 24px / 700; promo headline 26px / 700; body 12px / 400 / line-height 1.5 (18px). |
| Outside these captures | Surfaces other than the captured homepage URL and the captured PDP URL. |

### Family

- **Current visible UI base (verbatim from source):** `Nanum Barun Gothic` (나눔바른고딕, with `Malgun Gothic` / Arial fallbacks) — YAML `family.base`
- **Current visible UI alt:** `Pretendard` (with `Malgun Gothic` fallback) — YAML `family.alt`

Treating Nanum Barun Gothic as the dense shell and Pretendard as the loud promo layer in that pairing, rather than promoting either fallback as the brand face, treating the two-fonts-two-jobs split as a reconstruction of source §3 rather than a separately published font specification, and treating do-not-replace-unavailable-or-unobserved-brand-type-on-a-surface-that-did-not-establish-it as a reconstruction boundary, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. They do not swap roles. Do not replace unavailable or unobserved brand type with this pairing on a surface that did not establish it.

### Type roles

Treating YAML unitless `lineHeight` 1.4 (nav) and 1.5 (body) as ratios rather than only as a local px companion, treating body-table px companion 18px as local rather than a rewrite of 1.5, treating roles without a YAML lineHeight as omitted rather than filled, and treating tracking as omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

The following type-rule extras — two-fonts-two-jobs, weight-as-the-only-headline-signal, little-display-scale-typography-beyond-the-24–26px-heads, dense-sizing-for-throughput, and sale-copy-shouts-product-copy-whispers — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification. Headlines commit to weight 700; body, nav, and buttons sit at weight 400. Nav uses a 1.4 ratio. Body uses a 1.5 ratio (18px at 12px). Roles without a YAML lineHeight keep that field omitted.

| Role | Font | Size | Weight | Line height | Tracking | Provenance |
|---|---|---:|---:|---:|---:|---|
| Wordmark / H1 | Nanum Barun Gothic | 24px | 700 | omitted (no YAML lineHeight) | omitted | YAML `wordmark` use `CJ ONSTYLE logotype / page H1, Nanum Barun Gothic`; body Notes: CJ ONSTYLE logotype / page heading |
| Promo Headline | Pretendard | 26px | 700 | omitted (no YAML lineHeight) | omitted | YAML `promo-headline` use `Banner promo headline, Pretendard, white on imagery`; body Notes: Banner promo copy, white on imagery |
| Buy CTA | Nanum Barun Gothic | 20px | 400 | omitted (no YAML lineHeight) | omitted | YAML `cta` use `PDP 바로구매 buy CTA label`; body Notes: PDP 바로구매 button label |
| Search | Pretendard | 18px | 400 | omitted (no YAML lineHeight) | omitted | YAML `search` use `Header search input, Pretendard`; body Notes: Header search input |
| Nav | Nanum Barun Gothic | 15px | 400 | 1.4 | omitted | YAML `nav` use `Global nav menu items, Nanum Barun Gothic`; body Notes: Global nav menu items |
| Sub-menu | Nanum Barun Gothic | 14px | 400 | omitted (no YAML lineHeight) | omitted | YAML `submenu` use `Sub-menu / PDP secondary links`; body Notes: Sub-menu / PDP secondary links |
| Badge | Pretendard | 12px | 700 | omitted (no YAML lineHeight) | omitted | YAML `badge` use `Sale / benefit overlay pills, Pretendard`; body Notes: Sale / benefit overlay pills |
| Body | Nanum Barun Gothic | 12px | 400 | 1.5 (18px) | omitted | YAML `body` use `Default body / product meta, Nanum Barun Gothic`; body Notes: Default body / product meta |
| Utility | Nanum Barun Gothic | 12px | 400 | omitted (no YAML lineHeight) | omitted | YAML `util` use `Utility links (로그인/마이존), muted grey`; body Notes: Utility links (로그인/마이존) |

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting a portable mark-file URL, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

Treating captured homepage product photography, banner imagery, and PDP merchandise photography as recorded surface imagery rather than an ownership class, and treating imagery-not-invented-decoration as a reconstruction of source §7 / §12 rather than a separately published asset specification, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Do not replace those captured images with invented brand-color decoration. Ownership of the files is not claimed here.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, treating hex-and-geometry in the harvested components as remaining source-stated, treating source-HTML-comment sections 10–15 as a philosophy-layer, and treating the §14 rows below as that source-stated contract rather than computed CSS from the 2026-07-02 live inspect, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated. Source HTML comment marks sections 10–15 as a philosophy layer; the §14 rows below are that source-stated contract, not computed CSS from the 2026-07-02 live inspect.

Characterizations in the following table such as “No decorative illustration”, “Calm, functional”, “Flat pulse — no shadow shimmer”, “button stays `#640faf` and disables to prevent double-submit”, “plain-language cause”, “not just 필수”, “Restrained tone, no confetti”, “the violet is removed so buy is never implied when unavailable”, and “violet actions fade rather than switch hue” are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no search results)** | White canvas. Single black (`#000000`) line explaining no matching products, with a path back to categories. No decorative illustration. |
| **Empty (empty cart)** | Muted grey (`#666666`) line: nothing in the cart yet, plus a violet CTA back to shopping. Calm, functional. |
| **Loading (grid fetch)** | Skeleton tiles on `#f5f5f5` at final card dimensions, 2px radius. Flat pulse — no shadow shimmer, consistent with the shadowless system. |
| **Loading (buy action)** | Inline spinner within the violet buy button; button stays `#640faf` and disables to prevent double-submit. |
| **Error (payment/checkout failed)** | Inline message in near-black (`#111111`) with a plain-language cause and a retry path. States what to do next, not just a generic failure. |
| **Error (form validation)** | Field-level message below the input; describes what is valid (e.g. address, card), not just "필수". |
| **Success (order placed)** | Confirmation screen with order summary; next-step detail (delivery, tracking) linked immediately. Restrained tone, no confetti. |
| **Skeleton** | `#f5f5f5` blocks at final tile dimensions, 2px radius, flat pulse. |
| **Sold out / unavailable** | Buy CTA switches to a disabled grey state; the violet is removed so "buy" is never implied when unavailable. |
| **Disabled** | Faint grey (`#929292`) text on reduced-opacity surface; violet actions fade rather than switch hue, preserving the brand read. |

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating a generic Focus observation as not `focus-visible` evidence, treating loading/error/success as following product role rather than primitive kind, and treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Visual treatment for that keyboard state remains omitted. The source records no named Focus colour on inputs or buttons.

Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating Product Card and Sale / Benefit Pill as lacking interactive-kind confirmation for a §4.4 map, treating YAML `type: card` and `type: badge` as those YAML primitives rather than invented kinds, treating Utility / Skip-link Bar and Carousel Control YAML-row-absent Type as not invented, and treating Carousel Kind `interactive` as the body Use as prev/next/pause controls rather than an invented YAML type, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Product Card and Sale / Benefit Pill have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. YAML `tokens.components` records Product Card as `type: card` and Sale / Benefit Pill as `type: badge`. Utility / Skip-link Bar and Carousel Control have no YAML `tokens.components` row; Type is not invented from YAML absence. Carousel Control takes Kind `interactive` from body Use as prev/next/pause controls.

### Buy Primary (바로구매)

- Role: product-detail primary buy CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#640faf`
- Text: `#ffffff`
- Border: `1px solid #640faf`
- Radius: 4px
- Height: 60px
- Font: 20px / 400 / Nanum Barun Gothic
- Use: YAML `PDP 바로구매 primary buy CTA — the single saturated action color`; body §4 Use: Product-detail primary buy CTA — the system's single saturated action color
- Observed: default only
- Field note: The following unmerged-field reading, including Role-as-the-harvested-PDP-primary-buy-action, this-fill-not-sale-`#ec0040`, on-fill-white-not-ink-black, 4px-not-YAML-full-9999-not-2px-badge, 60px-not-40px-inquiry-not-46px-search, 20px-400-Nanum-not-headline-700, and no-named-hover-or-Focus-hex, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#640faf` is this control’s fill and catalog `primary_color`. It is not sale `#ec0040`. `#ffffff` is this control’s on-fill label, not ink `#000000`. Radius 4px is this control; it is not YAML `rounded.full` 9999 and not badge 2px. Height 60px is this CTA; it is not inquiry 40px and not search 46px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the PDP primary buy CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary buy action can be unavailable; visual treatment omitted. Source §14 Sold out / Disabled rows are not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the PDP `바로구매` primary buy CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows. Source §14 Loading (buy action) names an inline spinner inside this button; that row is not copied here as a computed paint.

### Inquiry (상품문의)

- Role: PDP secondary purple action (product inquiry)
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#640faf`
- Text: `#ffffff`
- Radius: 4px
- Height: 40px
- Font: 14px / 400
- Use: YAML `PDP 상품문의 secondary purple action`
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-matching-buy-primary-not-sale-magenta, 40px-not-60px-buy, 14px-400-without-an-invented-family, and YAML-border-absent-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#640faf` is this control’s fill, same hex as Buy Primary, not sale `#ec0040`. Height 40px is this inquiry; it is not Buy Primary 60px. Font 14px / 400 is this YAML field; no family is named, and none is invented. YAML records no border for this control; none is invented.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the PDP inquiry action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the PDP `상품문의` secondary purple action; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Wishlist (찜)

- Role: PDP wishlist toggle
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#111111`
- Border: `1px solid #b2b2b2`
- Radius: 4px
- Height: 60px
- Font: 20px / 400
- Use: YAML `PDP 찜 wishlist toggle, outlined neutral`
- Observed: default only
- Field note: The following unmerged-field reading, including white-fill-not-violet-`#640faf`, nav-ink-not-on-primary-white, border-strong-not-hairline, 4px-matching-buy-not-2px-card, 60px-matching-buy-height-as-this-outlined-counterpart, and YAML-family-absent-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#ffffff` is this control’s fill, not violet `#640faf`. `#111111` is this control’s label (nav ink), not on-primary `#ffffff`. Border `#b2b2b2` is border-strong, not hairline `#e5e5e5`. Height 60px matches Buy Primary height as this outlined counterpart; fill and border stay unmerged from that CTA.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the PDP wishlist toggle |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A wishlist toggle can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the PDP `찜` wishlist toggle; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Header Search

- Role: header search field
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#111111`
- Border: `1px solid #111111`
- Radius: 0px
- Height: 46px
- Font: 18px / 400 / Pretendard
- Use: YAML `Header search field, underline style`
- Observed: default only
- Field note: The following unmerged-field reading, including canvas-white-not-surface-grey, nav-ink-not-ink-black, 0px-not-4px-button, 46px-not-60px-buy, Pretendard-18px-400-not-Nanum-body-12px, and underline-style-as-source-Use-not-a-second-border-token, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#ffffff` is this field’s fill (canvas), not surface `#f5f5f5`. `#111111` is this field’s value color (nav ink), not ink `#000000`. Radius 0px is this search field; it is not button 4px. Height 46px is this search; it is not Buy Primary 60px. Font 18px / 400 / Pretendard is this control’s field, not Body 12px / 400 / Nanum Barun Gothic.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header search field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | A search field can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the header search field; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 Empty (no search results) or Error (form validation) rows.

### Global Nav (GNB)

- Role: top navigation
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#111111`
- Font: 15px / 400 / Nanum Barun Gothic
- Active: violet `#640faf` text on active item
- Use: YAML `Global nav items (홈/혜택/TV쇼핑)`; body also names 카테고리
- Observed: default (“홈”, “혜택”, “TV쇼핑”, “카테고리”); named active text `#640faf`
- Field note: The following unmerged-field reading, including nav-ink-not-ink-black, 15px-400-Nanum-not-body-12px, named-active-violet-not-hover, and captured-labels-not-a-second-nav-token, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.
Resting `#111111` is this item’s label (nav ink), not ink `#000000`. Font 15px / 400 is this nav item, not Body 12px / 400. YAML `active` “text `#640faf`” is a named active observation on this control, not hover.
A named active observation is not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the global nav item |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

Additional observed named state: active text `#640faf`. Treating that appearance as a named active observation, not hover, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.
A named active observation is not `focus-visible` evidence.

### PDP Section Tab

- Role: product-detail section tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#767676`
- Active: `#111111` text + 1px bottom border `#e5e5e5`
- Use: YAML `PDP section tabs (상세설명/리뷰/Q&A)`
- Observed: default (“상세설명”, “리뷰”, “Q&A”); named active `#111111` + 1px bottom `#e5e5e5`
- Field note: The following unmerged-field reading, including utility-grey-not-nav-ink, named-active-near-black-plus-hairline-underline-not-violet-GNB-active, and YAML-font-absent-not-invented-as-submenu-14px, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.
Resting `#767676` is this tab’s label (utility grey), not nav ink `#111111`. YAML `active` `#111111` + 1px bottom `#e5e5e5` is a named active observation on this control, not GNB active `#640faf`. YAML records no font for this control; Sub-menu 14px is not copied onto this tab.
A named active observation is not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the PDP section tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A section tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A section tab selects a panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

Additional observed named state: active `#111111` text + 1px bottom border `#e5e5e5`. Treating that appearance as a named active observation, not hover, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.
A named active observation is not `focus-visible` evidence.

### Product Card

- Role: product grid tile on home / listing
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Border: `1px solid #e5e5e5`
- Radius: 2px
- Use: YAML `Product grid card on home / listing, near-flat`
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-surface-grey, hairline-not-border-strong, 2px-not-4px-button, and near-flat-no-shadow-not-a-shadow-ladder, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#ffffff` is this card’s fill (canvas), not surface `#f5f5f5`. Border `#e5e5e5` is hairline, not border-strong `#b2b2b2`. Radius 2px is this card; it is not button 4px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Sale / Benefit Pill

- Role: discount/benefit overlay on product imagery
- Type: badge
- Anatomy: label on scrim
- Text: `#ffffff`
- Radius: 2px
- Padding: `0px 8px`
- Font: 12px / 700 / Pretendard
- Use: YAML `Discount/benefit overlay pill on product imagery, rgba(0,0,0,0.2) scrim bg`
- Observed: default (“적립5%”, “카드 5%”, “~50%할인”); sitting on a `rgba(0,0,0,0.2)` scrim with a `rgba(255,255,255,0.3)` hairline
- Field note: The following unmerged-field reading, including on-scrim-white-not-canvas-as-a-second-paint, 2px-matching-product-card-not-4px-button, 12px-700-Pretendard-not-body-12px-400-Nanum, YAML-fill-absent-scrim-not-invented-as-a-solid, and not-a-buy-CTA, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#ffffff` is this badge’s label on the scrim. Radius 2px is this badge; it is not button 4px. Font 12px / 700 / Pretendard is this badge, not Body 12px / 400 / Nanum Barun Gothic. YAML records no solid fill; the `rgba(0,0,0,0.2)` scrim stays the Use field.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Utility / Skip-link Bar

- Role: top accessibility skip-link / utility chrome bar
- Kind: interactive
- Anatomy: label on chrome
- Background: `#26292a`
- Text: `#ffffff`
- Padding: `0px 20px 0px 26px`
- Height: 34px
- Use: Top accessibility skip-link / utility chrome bar
- Observed: default
- YAML `tokens.components` does not record this control; values are body §4 only. Treating YAML-row-absent Type as not invented is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Type is not invented from YAML absence.
- Field note: The following unmerged-field reading, including chrome-dark-not-ink, on-chrome-white-not-nav-ink, 34px-not-60px-buy, asymmetric-`0px 20px 0px 26px`-not-sale-badge-`0px 8px`, and YAML-row-absent-Type-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#26292a` is this bar’s fill (header-dark), not ink `#000000`. `#ffffff` is this bar’s label. Height 34px is this bar; it is not Buy Primary 60px. Padding `0px 20px 0px 26px` is this bar, not sale-badge `0px 8px`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the skip-link / utility chrome bar |
| hover | applicable | Pointer-web skip-link; visual treatment omitted |
| focus-visible | applicable | Interactive skip-link; visual treatment omitted |
| disabled | applicable | A skip-link can be unavailable; visual treatment omitted |
| loading | not-applicable | Skip-link meaning is destination chrome; the bar itself does not enter a request-in-flight state |
| error | not-applicable | Skip-link meaning is destination chrome, not a request or validation failure on the bar |
| success | not-applicable | Skip-link meaning is destination chrome, not action-outcome confirmation |

### Carousel Control

Body §4 records a combined prev/next/pause chrome row. Proof records separate local fields for prev/next corners and a pause-only border. Those local fields stay unmerged with each other and with YAML `rounded.lg` 18 / `rounded.full` 9999 / Border Strong `#b2b2b2`.

- Role: hero banner carousel prev/next/pause controls (body §4 combined row)
- Kind: interactive
- Anatomy: control
- Background: `#ffffff`
- Body §4 combined-row border: `1px solid #b2b2b2` (the combined chrome row; not the pause-only border)
- Prev radius (Proof local): `18px 0 0 18px`
- Next radius (Proof local): `0 18px 18px 0`
- Pause radius: 50%
- Pause-only border (Proof local): `1px solid rgb(209, 209, 209)`; not `#b2b2b2`
- Height: 36px
- Use: Hero banner carousel prev/next/pause controls
- Observed: default
- YAML `tokens.components` does not record this control; values are body §4 plus Proof-local corner and pause-border fields. Treating YAML-row-absent Type as not invented, treating Kind `interactive` as the body Use as prev/next/pause controls rather than an invented YAML type, and treating prev/next directional corners and the pause-only `rgb(209, 209, 209)` border as unmerged local fields rather than one scalar 18px/50% control with a shared `#b2b2b2` border, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Type is not invented from YAML absence. Kind `interactive` is the body Use as prev/next/pause controls, not an invention of a YAML type.
- Field note: The following unmerged-field reading, including white-fill-not-violet, body-§4-combined-row-`#b2b2b2`-not-pause-only-rgb(209,209,209), prev-`18px 0 0 18px`-and-next-`0 18px 18px 0`-not-scalar-18px, 50%-pause-not-YAML-full-9999, 36px-not-34px-utility-bar, and YAML-row-absent-Type-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. `#ffffff` is this control’s fill. Body §4 combined-row border `#b2b2b2` is not the pause-only Proof border `rgb(209, 209, 209)`. Prev/next directional corners stay unmerged from each other and from YAML `rounded.lg` 18. Pause 50% is not merged with YAML `rounded.full` 9999 and not merged with button 4px. Height 36px is this control; it is not utility bar 34px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the carousel prev/next/pause control |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A carousel control can be unavailable; visual treatment omitted |
| loading | not-applicable | Carousel chrome advances or pauses slides; the control itself does not enter a request-in-flight state |
| error | not-applicable | Carousel-control meaning is slide chrome, not a request or validation failure on the control |
| success | not-applicable | Carousel-control meaning is slide chrome, not action-outcome confirmation |

### Product-detail buy bar (local recipe)

Treating the following as a source §9 local composition — a full-width `바로구매` button (`#640faf` fill, white text, `1px solid #640faf`, 4px, 60px, 20px Nanum Barun Gothic) beside a square 찜 wishlist button (white background, `#111111` text, `1px solid #b2b2b2`, 4px) — and not as a rewrite of YAML Buy Primary or Wishlist, and not as a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- Role: product-detail-buy-bar parent surface
- Anatomy: surface
- Local children (this composition only): full-width `바로구매` (`#640faf`, white text, `1px solid #640faf`, 4px radius, 60px height, 20px Nanum Barun Gothic); beside it a square 찜 (white background, `#111111` text, `1px solid #b2b2b2`, 4px radius)
- Use: source §9 product-detail buy bar
- Kind, Type, and a state-applicability map are omitted. Harvested Buy Primary and Wishlist stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-buy-bar-not-a-second-primary-token, 바로구매-plus-찜-as-this-bar, and not-a-rewrite-of-YAML-Buy-or-Wishlist, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. The `바로구매` and 찜 stay children of this buy-bar parent.

### Product card with sale overlay (local recipe)

Treating the following as a source §9 local composition — white `#ffffff` parent, `1px solid #e5e5e5`, 2px radius, no shadow, title in black `#000000` 12px Nanum Barun Gothic, price in black, and a sale badge overlaying the image (white text, 12px/700 Pretendard, 2px radius, `0px 8px` padding, on a `rgba(0,0,0,0.2)` scrim) — and not as a rewrite of YAML Product Card / Badge / Body, and not as a global pairing token, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- Role: product-card-with-sale-overlay parent
- Anatomy: surface
- Background: `#ffffff`
- Border: `1px solid #e5e5e5`
- Radius: 2px
- Local children (this composition only): title `#000000` 12px Nanum Barun Gothic; price in black; sale badge overlay (white text, 12px/700 Pretendard, 2px, `0px 8px`, `rgba(0,0,0,0.2)` scrim)
- Use: source §9 product card with sale overlay
- Kind, Type, and a state-applicability map are omitted. Harvested Product Card and Sale / Benefit Pill stay in their own slots.
- Field note: The following unmerged-composition reading, including 12px-Nanum-title-plus-Pretendard-badge-as-this-card and not-a-global-pairing-token, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Title 12px / 400 `#000000` and the sale badge stay children of this parent.

### Global header assembly (local recipe)

Treating the following as a source §9 local composition — white header with a dark `#26292a` utility bar above, nav items 15px Nanum Barun Gothic `#111111` with violet `#640faf` on the active item, and a header search input (Pretendard 18px, `#111111` text, underline style) — and not as a rewrite of YAML GNB / Header Search / Utility Bar, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- Role: global-header-assembly parent
- Anatomy: surface
- Local children (this composition only): dark `#26292a` utility bar; nav items 15px Nanum Barun Gothic `#111111`, violet `#640faf` when active; header search Pretendard 18px `#111111` underline style
- Use: source §9 global nav assembly
- Kind, Type, and a state-applicability map are omitted. Harvested Global Nav, Header Search, and Utility / Skip-link Bar stay in their own slots.
- Field note: The following unmerged-composition reading, including utility-bar-plus-GNB-plus-search-as-this-header and not-promoted-as-global-tokens, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

### Promo banner headline (local recipe)

Treating the following as a source §9 local composition — Pretendard 26px weight 700, white text over imagery, with a magenta `#ec0040` discount rate emphasized — and not as a rewrite of YAML Promo Headline or Sale Magenta, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- Role: promo-banner-headline parent
- Anatomy: surface
- Local children (this composition only): Pretendard 26px / 700 white text over imagery; magenta `#ec0040` discount rate
- Use: source §9 promo banner headline
- Kind, Type, and a state-applicability map are omitted. YAML Promo Headline and Sale Magenta stay in their own slots.
- Field note: The following unmerged-composition reading, including 26px-700-white-plus-magenta-rate-as-this-banner and not-a-second-type-role, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 2, sm 4, base 8, md 12, lg 20, xl 26, xxl 48. Body layout repeats 2px, 4px, 8px, 12px, 20px, 26px, 48px. Harvested heights: 60px buy / wishlist, 40px inquiry, 46px search, 34px utility bar, 36px carousel.

The following layout readings — density-over-air, flat-segmentation, signal-by-color-not-space, fixed-width-centered-content-column-with-a-persistent-header, home-as-a-stack-of-full-width-promotional-banners-over-multi-column-product-tile-grids, PDP-as-a-two-column-layout-media-left-buy-panel-right, sticky-prominent-violet-buy-CTA, and sections-alternating-white-and-tinted-grey-bands — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification.

Recorded layout:

- Fixed-width centered content column with a persistent header: utility chrome bar (`#26292a`), logo + search + GNB, then a dense product grid
- Home is a stack of full-width promotional banners over multi-column product tile grids
- PDP is a two-column layout (media left, buy panel right) with a sticky/prominent violet buy CTA
- Sections alternate white (`#ffffff`) and tinted grey (`#f5f5f5`, `#f0f0f0`) bands

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 60px / 40px / 46px / 34px / 36px figures as surface measurements in this packet rather than a cross-viewport specification, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating product-imagery-and-banners-carry-no-shadow-at-any-size-consistent-with-the-flat-system / sale-badges-overlay-imagery-on-a-translucent-scrim-to-preserve-legibility / cards-keep-2px-radius-across-breakpoints as source-stated image behavior rather than a complete image specification, and treating product-grid-multi-column-to-two-up-to-single / GNB-horizontal-to-hamburger-drawer / PDP-two-column-to-stacked-with-violet-buy-CTA-pinned-to-a-sticky-bottom-bar / promotional-banners-full-width-crop-imagery-scale-the-26px-headline-down as recorded collapsing rather than a complete responsive specification, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <768px | Single/two-up product grid, hamburger nav, sticky bottom buy bar on PDP |
| Tablet | 768-1024px | Moderate multi-column tile grid, condensed GNB |
| Desktop | 1024-1440px | Full fixed-width grid, full GNB, two-column PDP |

Treating the following collapsing strategy as recorded collapsing rather than a complete responsive specification is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Collapsing strategy recorded in the source: product grid multi-column → two-up → single column on narrow viewports; GNB horizontal menu → hamburger / drawer; PDP two-column media+buy panel → stacked, with the violet buy CTA pinned to a sticky bottom bar; promotional banners maintain full-width, crop imagery, scale the 26px headline down.

Treating the following image behavior, including carry-no-shadow-at-any-size-consistent-with-the-flat-system, sale-badges-overlay-imagery-on-a-translucent-scrim-to-preserve-legibility, and cards-keep-2px-radius-across-breakpoints, as source-stated image behavior rather than a complete image specification is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Image behavior: product imagery and banners carry no shadow at any size, consistent with the flat system; sale badges overlay imagery on a translucent scrim to preserve legibility; cards keep 2px radius across breakpoints.

Treating the following touch-target record as a purpose reading of those measurements rather than a complete target-size specification is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification. Touch-target record: buy CTA at 60px height, full-width on PDP; wishlist (찜) at 60px square-ish outlined button; nav items spaced within the header for touch; utility links compact at 12px.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-07-02)

The live strings below are source-stated. Treating the em-dash role labels (PDP-primary-buy-CTA / promo-banner-headline / benefit-overlay-badges / buy-path-CTAs / utility-nav / GNB / PDP-section-tabs / PDP-wishlist) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

- "바로구매" — PDP primary buy CTA. *(verified live 2026-07-02)*
- "빕스바우처 ~56%" — promo banner headline. *(verified live 2026-07-02)*
- "적립5% / 카드 5% / 네이버포인트" — benefit overlay badges. *(verified live 2026-07-02)*
- "장바구니", "상품문의" — buy-path CTAs. *(source body)*
- "로그인", "마이존", "카테고리" — utility / nav. *(source body)*
- "홈", "혜택", "TV쇼핑" — GNB. *(source body)*
- "상세설명", "리뷰", "Q&A" — PDP section tabs. *(source body)*
- "찜" — PDP wishlist. *(source body)*

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — brisk-benefit-forward-deal-driven / trusted-home-shopping-host / offer-leads / value-seeker / where-a-fintech-might-reassure-CJ-ONSTYLE-motivates / High-energy-offer-first / Terse-factual-perks / Direct-imperatives / Descriptive-spec-forward / Neutral-and-functional / vague-lifestyle-poetry-with-no-offer / undefined-jargon / hesitant-hedging-on-price / low-contrast-quiet-luxury-copy — are a derived editorial implementation inference from the verified surfaces; they are not CJ ONSTYLE-authored or a separately published UI specification. They are not the Observed strings above.

The source describes CJ ONSTYLE's voice as brisk, benefit-forward, and deal-driven — the register of a trusted home-shopping host translating a live pitch into a scannable screen. Copy leads with the offer ("~50%할인", "5%카드", "적립5%") and the concrete product benefit, in plain, high-energy Korean.

| Context | Tone |
|---|---|
| Promo headlines | High-energy, offer-first. "빕스바우처 ~56%". Numbers and rates lead. |
| Benefit badges | Terse, factual perks. "적립5%", "카드 5%", "네이버포인트". |
| Buy CTAs | Direct imperatives. "바로구매", "장바구니", "상품문의". |
| Product titles | Descriptive, spec-forward, plain black text. |
| Utility / nav | Neutral and functional. "로그인", "마이존", "카테고리". |

**Forbidden register** (source): vague lifestyle poetry with no offer, undefined jargon, hesitant hedging on price, and low-contrast "quiet luxury" copy that hides the deal. The deal is the message. Treating that last-sentence characterization as a reconstruction reading of source §10 rather than CJ ONSTYLE-authored voice doctrine is a derived editorial implementation inference from the verified surfaces; it is not CJ ONSTYLE-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exceptions remain Global Nav YAML `active` text `#640faf` and PDP Section Tab YAML `active` `#111111` + 1px bottom `#e5e5e5`
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Buy Primary, Inquiry, Wishlist, and Header Search
- interactive kind and state-applicability map for Product Card and Sale / Benefit Pill
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
