# Chunghwa Telecom Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Chunghwa Telecom (中華電信). Catalog homepage identity is `https://www.cht.com.tw`. Treating that product-origin sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

Treating the following URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, homepage-value-not-a-proxy-for-every-named-service-chrome, and personal-services-as-session-gated-partial-rather-than-a-second-token-set, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. This contract covers the 2026-06-22 live inspect of the Chunghwa Telecom homepage at `https://www.cht.com.tw/` and the session-gated personal-services surface at `https://www.cht.com.tw/home/cht/personal/` (partial; the inspect also recorded the “網頁過期囉” session-expired page). Color, type, and component values below stay attached to those captured surfaces. A homepage value is not a proxy for every named offering in the narrative (5G mobile, HiNet broadband, MOD, enterprise ICT) as separate captured chrome.

Source token note: `primary = live Chunghwa Blue (#209cff = rgb(32,156,255)); accent orange (#ff874d = rgb(255,135,77)); canvas near-white (#fafafa); body text near-black (#333333).` Treating that note as a register split — `#209cff` is catalog `primary_color` and the harvested primary-CTA fill rather than accent `#ff874d` and rather than hover `#0083ec`; `#ff874d` is promotional accent rather than a second primary CTA fill; `#fafafa` is canvas rather than surface `#ffffff`; `#333333` is ink rather than muted `#666666` or dark-mid `#4c4c4c`; `#eaeaea` is hairline rather than input border `#cbcbcb`; YAML search-input `fg` `#686868` is a component field rather than muted ink — is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

The following atmosphere readings — bold-accessible-sky-blue, approachable-and-utilitarian, dual-tone-accent-system, classic-telecom-retail-convention-blue-trust-orange-deal-alert, restraint-on-a-content-heavy-portal, clean-institutional-feel, never-pure-black, Chunghwa-Blue-as-brand-color-link-color-and-primary-interactive-accent, and tinted-blue-surfaces-for-hierarchy-without-heavy-shadows — are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification. The source describes a near-white canvas (`#fafafa`) punctuated by white content panels (`#ffffff`) and Chunghwa Blue (`#209cff`) as brand color, link color, and primary interactive accent. Warm orange (`#ff874d`) is recorded for promotional heat. Body text sits at 20px. Navigation labels at 16px toggle between weight 700 (active) and weight 400 (inactive). Border radius in the source centers on 20px as the dominant measured corner (~46% of measured radii). Tinted blue surfaces (`#e4f2ff`, `#f0f8ff`) are recorded for hierarchy without heavy shadows.

Treating the following public-history facts as narrative rather than interface tokens, including 1996-spin-out-of-the-Directorate-General-of-Telecommunications, full-privatization-by-2005, TWSE-2412, NYSE-CHT, successor-to-national-telephone-infrastructure, widest-fixed-line-network, largest-HiNet-fiber-subscriber-base, one-of-Taiwan's-lead-5G-operators, and product-span-across-5G-HiNet-MOD-enterprise-ICT-and-smart-city, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. Chunghwa Telecom was spun out of the Directorate General of Telecommunications of Taiwan in 1996 and fully privatized by 2005, becoming a listed company on the Taiwan Stock Exchange (TWSE: 2412) and the New York Stock Exchange (NYSE: CHT). As the successor to Taiwan's national telephone infrastructure, it holds the widest fixed-line network, the largest fiber broadband subscriber base (HiNet), and — following 5G spectrum auctions — one of Taiwan's lead 5G operators. The product surface spans mobile (5G/4G), fixed broadband (HiNet 光世代), cable TV and OTT (MOD), enterprise ICT solutions, and smart city infrastructure. Public-history narrative provides that context; it does not by itself supply interface tokens.

Treating the mission evolution from connecting Taiwan's phones to connecting Taiwan's digital life as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. The brand's mission has evolved from connecting Taiwan's phones to connecting Taiwan's digital life.

Treating Chunghwa Telecom's posture as the backbone of Taiwan's digital transformation rather than a consumer-products challenger, and treating that posture as showing in design that is institutional, comprehensive, and confidence-projecting rather than disruptive or playful, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. CHT positions itself as the backbone of Taiwan's digital transformation rather than a consumer-products challenger — a posture that shows in its design: institutional, comprehensive, and confidence-projecting rather than disruptive or playful.

The following refusal, embrace, excessive-decoration, trend-chasing-color-palettes, marketing-language-that-obscures-service-terms, clear-blue-identity, dense-information-architecture, and dual-language-Chinese-English-navigation readings, including stable-and-trustworthy-blue-for-a-decades-old-national-audience, dense-IA-that-respects-users'-time, and dual-language-nav-that-reflects-Taiwan's-bilingual-professional-class, are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification. What the source treats the brand as refusing: excessive decoration, trend-chasing color palettes, or marketing language that obscures service terms. What it treats the design as embracing: a clear blue identity that reads as stable and trustworthy to a national audience that has used CHT services for decades, dense information architecture that respects users' time, and dual-language (Chinese/English) navigation that reflects Taiwan's bilingual professional class.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings / live homepage labels below as Primary tasks, and not lifting tasks from source §13 fictional archetypes, and treating independently-verified-in-this-packet as the inspected homepage URL rather than the named CTA and field labels, which remain harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. Independently verified in this packet is the inspected homepage URL; named CTA and field labels are harvested strings/controls, not independently verified destination routes.

- Act on a harvested homepage primary CTA (YAML use: `Primary CTA, accept/consent, back-to-home actions`).
- Open a harvested find-more section CTA (YAML use: `Find-more section CTA (找更多)`).
- Search the captured homepage (YAML use: `Site search field`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. No individual personas are promoted. Observable work follows the three primary tasks.

### Distinctive traits

The following unmerged-role extras — treating `#209cff` as catalog `primary_color` and primary-CTA fill rather than accent `#ff874d` or hover `#0083ec`, treating `#ff874d` as promotional accent rather than a second CTA fill, treating `#fafafa` as canvas rather than surface `#ffffff`, treating on-primary `#ffffff` as the same hex as surface rather than a second paint, treating `#333333` / `#666666` / `#4c4c4c` / search `#686868` as unmerged inks, treating `#eaeaea` as hairline rather than input `#cbcbcb`, treating YAML `rounded.full` 100 as a YAML step rather than harvested `100px` or live `30px` on “回首頁”, treating 50px YAML primary height as unmerged from live “我接受” 54px, treating 16px / 400 YAML primary-button font as unmerged from YAML `cta-label` 16px / 700, and treating PingFang TC plus the recorded fallback stack as a system stack rather than a custom web font — are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

- Catalog `primary_color` / Chunghwa Blue `#209cff` as the harvested primary-CTA fill (“我接受”, “回首頁”)
- Primary hover `#0083ec` as a named deeper-blue on primary buttons, not a second primary fill
- Warm orange `#ff874d` as promotional badges and urgency, not a primary interactive fill
- Near-white canvas `#fafafa` with surface white `#ffffff`; ink `#333333` (1084 foreground occurrences in the live scan)
- PingFang TC system stack; hero 32px / 700 / 1.40; body 20px / 400 / 1.60; nav 16px toggling 700 / 400
- YAML radius steps sm 4 / md 8 / lg 16 / card 20 / full 100; harvested primary CTA `100px`; live “回首頁” `30px`; dominant measured 20px (~46%)
- Primary CTA YAML 50px / `16px 35px` / 16px / 400; live “我接受” 54px / `16px` / `100px`; Find More 40px / `7px 58px` / 8px / 16px / 700
- Blue tints `#e4f2ff` and `#f0f8ff` for secondary actions and content-zone hierarchy

### Principles

These five items (Clarity before persuasion, Blue as trust, orange as attention, Inclusive legibility, Comprehensive before minimal, Institutional credibility) are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

1. **Clarity before persuasion.** Taiwanese users come to CHT to confirm a service, compare a plan, or initiate a subscription — not to be convinced. Treating the UI implication “lead with product names and specs; reserve persuasion for secondary copy” as part of that same derived reading is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.
2. **Blue as trust, orange as attention.** The two-tone accent system maps directly to user need states. Treating the UI implication “blue (`#209cff`) for every action that continues a journey; orange (`#ff874d`) only for time-sensitive promotions” as part of that same derived reading is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.
3. **Inclusive legibility.** CHT's user base spans teenagers to seniors, urban to rural. Treating the UI implication “20px body text, high-contrast `#333333` on `#fafafa`, CJK-optimized font stack, generous touch targets” as part of that same derived reading is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.
4. **Comprehensive before minimal.** Unlike SaaS brands that hide complexity, CHT surfaces all products because users arrive with specific intent. Treating the UI implication “tabbed navigation covers every service segment; sub-categories are explicit, not hidden under hamburger menus” as part of that same derived reading is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.
5. **Institutional credibility.** As Taiwan's largest carrier, CHT's design signals stability over novelty. Treating the UI implication “measured radius scale (4px–20px for most elements, 100px pill for primary CTA only), flat system fonts, no experimental motion” as part of that same derived reading is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do's, including Chunghwa-Blue-`#209cff`-for-all-primary-interactive-elements, PingFang-TC-with-the-full-CJK-fallback-stack, orange-`#ff874d`-only-for-promotional-urgency, 100px-radius-pill-buttons-for-primary-CTAs, weight-700-for-active-nav-and-weight-400-for-inactive, near-black-`#333333`-never-pure-black-for-body-copy, and blue-tint-surfaces-`#e4f2ff`-rather-than-heavy-shadows, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

- Use Chunghwa Blue (`#209cff`) for all primary interactive elements and brand-owned actions.
- Use PingFang TC with the full CJK fallback stack to ensure cross-platform Traditional Chinese legibility.
- Apply the orange accent (`#ff874d`) only for promotional urgency — price highlights, limited-time offers.
- Use 100px-radius pill buttons for primary CTAs to distinguish them from secondary actions.
- Maintain weight 700 for active nav states and weight 400 for inactive states.
- Use near-black (`#333333`) for text — never pure black for body copy.
- Separate content sections with blue tint surfaces (`#e4f2ff`) rather than heavy shadows.

### Avoid

The following items copy source §7 Don'ts, including orange-as-accent-only, no-heavy-drop-shadows, no-mix-of-PingFang-TC-and-Western-display-fonts, no-rounded-corners-larger-than-20px-on-service-cards-or-modals, inactive-nav-always-`#666666`, no-pure-black-`#000000`-for-body-text, and no-overloading-the-blue-accent. They are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

- Don't use orange (`#ff874d`) as a primary brand color — it is an accent-only promotional signal.
- Don't apply drop shadows heavily — CHT uses flat tint-based depth, not elevation.
- Don't mix Traditional Chinese (PingFang TC) and Western display fonts — system stack is consistent.
- Don't use rounded corners larger than 20px on service cards or modals (20px is the documented maximum for cards; reserve 100px exclusively for pill CTAs).
- Don't set inactive nav items with colored text — inactive is always `#666666` muted grey.
- Don't use pure black (`#000000`) for body text — the warm near-black `#333333` is the system standard.
- Don't overload the blue accent — it should signal primary action, not decoration. Treating that last-bullet characterization as a reconstruction reading of source §7 rather than Chunghwa Telecom-authored color doctrine is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including primary-CTA-fill-not-accent, hover-not-a-second-primary, accent-not-a-second-CTA, canvas-near-white-not-surface-white, on-primary-white-as-the-same-hex-as-surface-rather-than-a-second-paint, ink-not-muted-not-dark-mid-not-search-fg, hairline-not-input-border, tint-not-a-second-blue-fill, surface-tint-not-the-same-as-primary-tint, orange-never-an-interactive-primary, and named-hover-pressed-not-keyboard-focus-evidence — are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification. Catalog `primary_color` `#209cff` is YAML `primary` and the harvested CTA fill; it is not accent `#ff874d` and not hover `#0083ec`. YAML `accent` `#ff874d` is the promotional orange. YAML `canvas` `#fafafa` is page background; YAML `surface` `#ffffff` is cards and panels. YAML `on-primary` `#ffffff` is text and icons on primary blue or orange fills; it is the same hex as surface, not a second paint. YAML `ink` `#333333`, `muted` `#666666`, and `dark-mid` `#4c4c4c` are unmerged inks. YAML search-input `fg` `#686868` is a component field. YAML `hairline` `#eaeaea` and `border-input` `#cbcbcb` are unmerged borders. YAML `primary-tint` `#e4f2ff` and `primary-surface` `#f0f8ff` are unmerged tints. YAML component fields such as primary CTA `fg` `#ffffff` and nav-tab `fg` `#333333` stay on those controls.

- **Chunghwa Blue / Primary** (`#209cff`): catalog `primary_color`. YAML `primary`. Harvested primary CTA fill, link text, and brand-owned surfaces. Equivalent to `rgb(32, 156, 255)` measured live on the homepage.
- **Primary Hover** (`#0083ec`): YAML `primary-hover`. Slightly deeper blue for hover/pressed states on primary buttons — measured live (`rgb(0, 131, 236)`) on the CTA button. Named hover/pressed on that control; not a second primary fill and not `focus-visible`.
- **Primary Tint** (`#e4f2ff`): YAML `primary-tint`. Light blue tint for secondary button backgrounds (“找更多” CTA) and hover surfaces. Measured `rgb(228, 242, 255)` live.
- **Primary Surface** (`#f0f8ff`): YAML `primary-surface`. Lighter blue surface tint for content zone backgrounds and card accents. Measured `rgb(240, 248, 255)` live.
- **CHT Orange / Accent** (`#ff874d`): YAML `accent`. Warm orange for promotional badges, price highlights, and urgency indicators. Measured `rgb(255, 135, 77)` live as a frequent background color. Never an interactive primary.
- **Near-White Canvas** (`#fafafa`): YAML `canvas`. Page background.
- **Surface White** (`#ffffff`): YAML `surface`. Card and panel backgrounds.
- **On-Primary White** (`#ffffff`): YAML `on-primary`. Text and icons on primary blue or orange backgrounds. Same hex as surface; not a second paint.
- **Ink** (`#333333`): YAML `ink`. Primary text, headings, nav active, all body copy. Measured `rgb(51, 51, 51)` as the dominant foreground (1084 occurrences).
- **Muted** (`#666666`): YAML `muted`. Secondary/inactive nav links, captions. Measured `rgb(102, 102, 102)` live.
- **Dark Mid** (`#4c4c4c`): YAML `dark-mid`. Mid-range text for certain sub-headings. Measured `rgb(76, 76, 76)` live.
- **Hairline** (`#eaeaea`): YAML `hairline`. Thin border and divider color for card outlines and section separators. Measured `rgb(234, 234, 234)` live.
- **Input Border** (`#cbcbcb`): YAML `border-input`. Form field border color. Measured `rgb(203, 203, 203)` live on the search input. Not merged with hairline `#eaeaea`.

### Spacing

YAML scale: xs 4, sm 8, md 12, base 16, lg 20, xl 32, xxl 48, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names 4px as a base unit and lists 4px, 8px, 12px, 16px, 20px, 32px, 48px, 64px. Treating those YAML numbers as recorded without a required px suffix, treating body 4px / 8px / 12px / 16px / 20px / 32px / 48px / 64px as body-recorded steps rather than a rewrite of the YAML keys, treating YAML section 64 as a YAML step rather than adding a px suffix to that number, and treating harvested control heights 50px / 54px / 40px / 36px / 34px / 38px and paddings `16px 35px` / `7px 58px` / `0px 10px` as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

The following 4px-base / information-dense-portal / 12–20px-internal-padding-on-tabbed-panels readings are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification. Body layout states a base unit of 4px. Source §5 notes sections group related services (行動上網通話, 光世代寬頻, MOD) into tabbed panels with 12–20px internal padding.

### Shape

YAML `rounded`: sm 4, md 8, lg 16, card 20, full 100.

The following local-geometry reading, including 4px-on-form-inputs-small-badges-and-search-fields, 8px-on-secondary-CTAs-and-tinted-surface-cards, 16px-on-service-and-product-listing-cards, 20px-as-dominant-modal-and-primary-card-default, 100px-on-primary-CTA, YAML-full-100-as-a-YAML-step-not-converted-into-harvested-100px, live-回首頁-30px-unmerged, secondary-observed-50%-circle-and-42px, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

- Minimal (4px): form inputs, small badges, search fields (YAML `rounded.sm`)
- Standard (8px): secondary CTAs (“找更多”), tinted surface cards (YAML `rounded.md`)
- Card (16px): service and product listing cards (YAML `rounded.lg`)
- Modal/Card Default (20px): YAML `rounded.card`. Source body records this as the dominant radius — ~46% of all measured corner radii (×66 live); used on modals and primary card containers
- Full pill (100px): primary CTA buttons (YAML component field `radius: 100px`). YAML `rounded.full` 100 is recorded as a YAML step; 100 is not converted into a harvested pill by adding a `px` suffix at the YAML-scale layer
- Secondary observed: 50% / circle (×11, avatar/icon crops), 42px (×4, select promotional containers)
- Live “回首頁” back-to-home CTA: `30px` radius in the source HTML comment / inspect. That 30px is not merged with YAML `100px` or with YAML `full` 100

Treating 4px / 8px / 16px / 20px / 100px corners as local harvested geometry on the named controls rather than a universal radius for every unlisted control, treating YAML `full` 100 as a YAML step rather than merged with harvested `100px` or with live `30px`, and treating 50% and 42px as secondary observed values rather than extra YAML scale keys, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

### Elevation

The following elevation-table Use readings, including canvas-inline-text-most-surfaces, service-cards-content-containers, featured-sections-secondary-CTAs, plus the largely-flat-portal-style / primary-depth-as-background-color-difference / white-cards-on-near-white-canvas-rather-than-box-shadows / fast-loading-and-accessible / YAML-shadow-card-not-merged-with-no-shadow readings that follow the table, are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Canvas, inline text, most surfaces |
| Card (Level 1) | White bg `#ffffff` on `#fafafa` canvas | Service cards, content containers |
| Tint (Level 2) | `#e4f2ff` or `#f0f8ff` background | Featured sections, secondary CTAs |

YAML `tokens.shadow.card`: `0 2px 8px rgba(0,0,0,0.08)`. YAML `tokens.shadow.none`: `none`.

Treating YAML `shadow.card` as a YAML token rather than a harvested paint on White Service Card, and treating source §6 “largely flat, portal-style / primary depth mechanism is background color difference” as not a deletion of that YAML token, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. YAML `shadow.card` `0 2px 8px rgba(0,0,0,0.08)` is recorded as a YAML step. It is not merged with YAML `none` and not copied onto the White Service Card as a computed paint. Source §6 states CHT operates a largely flat, portal-style design whose primary depth mechanism is background color difference — white cards (`#ffffff`) on a near-white canvas (`#fafafa`) — rather than box shadows.

### Motion

Source-stated duration roles. Treating the duration table, easing names, signature motions, and reduced-motion line as source-stated rather than computed CSS, and treating Arriving / Dismissals / Two-way / spec-template-ease-exit-match / utilitarian-portal / functional-and-imperceptible-rather-than-expressive / no-bounce-no-spring-no-playful-physics / calm-predictable-credibility / all-functionality-remains-intact readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 100ms | Hover states on nav links, badge pulse |
| `motion-standard` | 200ms | Panel transitions, card reveal, carousel slide |
| `motion-slow` | 300ms | Page-level transitions, sheet or dropdown open |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Panels arriving — cards, dropdowns |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals, closing panels |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions, carousel |

Signature motions (source-stated):

Treating no-bounce-or-spring-or-playful-physics on core chrome as a portal-discipline reading, treating hero-carousels-auto-advance-at-a-slow-non-disorienting-rate as source-stated cadence prose rather than the `motion-slow` duration token and rather than computed CSS, treating carousel-slide as `motion-standard` rather than `motion-slow`, treating `ease-standard` as carousel two-way easing rather than the auto-advance cadence, and treating the following signature-motion readings — utilitarian-portal, functional-and-imperceptible-rather-than-expressive, sub-nav-panels-slide-in-at-motion-standard-ease-enter, and all-functionality-remains-intact — as editorial rather than computed CSS, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

Source forbidden: no bounce, spring, or playful physics. Hero carousels auto-advance at a slow, non-disorienting rate. That cadence prose is not the `motion-slow` token. Carousel slide uses `motion-standard`. Carousel two-way easing uses `ease-standard`. Sub-nav panels slide in at `motion-standard / ease-enter`.

1. **Hero carousel.** Slide transition uses `motion-standard`. Auto-advance cadence is the source-stated slow, non-disorienting rate; that cadence is not the `motion-slow` duration token. Two-way/carousel easing uses `ease-standard`.
2. **Sub-nav panel.** Slide in at `motion-standard / ease-enter`.
3. **Reduce motion.** Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; all functionality remains intact.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-computed-PingFang-TC-plus-fallback-stack, YAML-family-keys-naming-those-live-computed-families, fallback-stack-not-the-brand-face, no-dedicated-brand-typeface-or-custom-web-font, and outside-the-captured-homepage-and-session-gated-personal-surface, are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Captured homepage computes `"PingFang TC", "SF Pro TC", "SF Pro Text", "Microsoft JhengHei", "Helvetica Neue", "Noto Sans CJK TC", Helvetica, Arial, sans-serif`. YAML `family.display` `PingFang TC` and YAML `family.body` `PingFang TC, SF Pro TC, Microsoft JhengHei, Helvetica Neue, Noto Sans CJK TC, Helvetica, Arial, sans-serif` name those live computed families. Body 20px / weight not restated here / line-height 32px at that size. |
| Outside these captures | Surfaces other than the captured homepage URL and the session-gated personal-services inspect. |

### Family

- **Current visible UI stack (verbatim from live DOM / source HTML comment):** `"PingFang TC", "SF Pro TC", "SF Pro Text", "Microsoft JhengHei", "Helvetica Neue", "Noto Sans CJK TC", Helvetica, Arial, sans-serif`
- **YAML `family.display`:** `PingFang TC`
- **YAML `family.body`:** `PingFang TC, SF Pro TC, Microsoft JhengHei, Helvetica Neue, Noto Sans CJK TC, Helvetica, Arial, sans-serif`

Treating PingFang TC as the recorded Traditional Chinese face in that stack rather than promoting a fallback as the brand face, treating YAML `family.body` as omitting `SF Pro Text` while the live stack includes it — those two records stay unmerged — treating same-system-stack-for-all-contexts and no-dedicated-brand-typeface-or-custom-web-font as source-§3 reconstruction rather than a separately published font specification, and treating do-not-replace-unavailable-or-unobserved-brand-type-on-a-surface-that-did-not-establish-it as a reconstruction boundary rather than a separately published font specification, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. YAML `family.body` does not list `SF Pro Text`; the live computed stack and source §3 body do. Both records are kept. Do not replace unavailable or unobserved brand type with this stack on a surface that did not establish it. Source §3 states CHT uses the same system font stack for all contexts — no dedicated brand typeface or custom web font.

### Type roles

Verified YAML line-height values are the unitless ratios `1.40`, `1.42`, `1.38`, `1.60`, `1.44`, and `1.50`. The body table also records Small / Label at `1.30`, which is not a YAML `lineHeight` field. Treating YAML unitless ratios as ratios rather than only as local px companions, treating the live 32px body companion as local rather than a rewrite of 1.60, treating YAML `cta-label` 16px / 700 / 1.50 as unmerged from YAML `button-primary` font `16px / 400`, and treating body-table “Body / Secondary Nav” 16px / 400 / 1.38 as unmerged from YAML `body` 20px / 400 / 1.60, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

The following type-rule extras — weight-as-selection-signal, CJK-first-sizing, no-display-font-separation, generous-line-height-1.60-at-body, and same-system-stack-for-all-contexts — are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification. Nav tabs toggle between 700 (active) and 400 (inactive). Source §3 records 20px as the body default. CHT uses the same system font stack for all contexts. Line height 1.60 at body size is the YAML `body` ratio.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Use (YAML) | Notes (body table) |
|---|---|---:|---:|---:|---|---|---|
| Hero Headline (YAML `hero`) | PingFang TC | 32px | 700 | 1.40 | (no extra px in the body table) | Hero section headlines | Service category hero titles |
| Section Heading (YAML `section`) | PingFang TC | 24px | 700 | 1.42 | (no extra px in the body table) | Section headers, service category titles | Major page sections |
| Sub-nav Header (YAML `sub-nav`) | PingFang TC | 18px | 700 | 1.44 | (no extra px in the body table) | Sub-navigation section headers | Second-level nav ("行動上網通話") |
| Primary Nav Tab (YAML `nav-primary`) | PingFang TC | 16px | 700 | 1.38 | (no extra px in the body table) | Primary navigation tabs (個人家庭 active) | Active nav (個人家庭) — bold signals selection |
| Secondary Nav (YAML `nav-secondary`) | PingFang TC | 16px | 400 | 1.38 | Body / Secondary Nav 16px / 400 / 1.38 | Secondary nav links, inactive states | Inactive tabs, descriptions, general text |
| Body Large (YAML `body`) | PingFang TC | 20px | 400 | 1.60 | 32px at this size (live computed) | Body text, descriptions | Default body text (document-level) |
| CTA label (YAML `cta-label`) | PingFang TC | 16px | 700 | 1.50 | (no extra px in the body table) | Button labels, find-more CTAs | Unmerged from YAML `button-primary` font `16px / 400` |
| Small / Label | PingFang TC | 13px | 400 | (no YAML `lineHeight`) | 1.30 | (no YAML role) | Form labels, search input text |

### Assets

- Homepage favicon: `https://web-eshop.cdn.hinet.net/eShop%20Images/Consumer/Faicon/Faicon_logo_128x128.png`. Treating that URL as catalog logo identity rather than a captured first-party wordmark file, and rather than a substitute for every named product-line chrome, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.
Treating captured homepage promotional banners and service imagery as recorded surface imagery rather than an ownership class, and treating imagery-not-invented-decoration as a reconstruction of source §7 / §12 rather than a separately published asset specification, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. Do not replace those captured images with invented brand-color decoration. Ownership of the files is not claimed here.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, treating hex-and-geometry in the harvested components as remaining source-stated, treating source-HTML-comment sections 10–15 as a philosophy-layer, and treating the §14 rows below as that source-stated contract rather than computed CSS from the 2026-06-22 live inspect, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated. Source HTML comment marks sections 10–15 as a philosophy layer; the §14 rows below are that source-stated contract, not computed CSS from the 2026-06-22 live inspect.

Characterizations in the following table such as “No illustration”, “session-based portal uses server-side rendering”, “Calm and directive, not alarming”, “describes what format is expected”, “No heavy celebratory treatment”, “minimal pulse”, “Matches the flat system aesthetic”, and “blue actions fade — preserves brand read while signaling unavailability” are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (search, no results)** | Ink (`#333333`) message explaining no matches, with a suggestion to refine or call 123 customer service. Blue CTA to return to home. |
| **Empty (service unavailable in region)** | Plain-language Chinese copy with contact reference. No illustration. |
| **Loading (page / panel)** | Session-based portal uses server-side rendering; expired sessions show "網頁過期囉" page — white canvas with a single blue pill CTA ("回首頁"). |
| **Loading (search results)** | Skeleton card placeholders on `#fafafa` at standard 16px-radius card dimensions. |
| **Error (session expired)** | Dedicated error page: title "網頁過期囉", near-white canvas, single `#209cff` pill button "回首頁". Calm and directive, not alarming. |
| **Error (form validation)** | Field-level inline message in `#333333` below the input; describes what format is expected. |
| **Success (application submitted)** | Plain confirmation copy in `#333333` with next-step summary. No heavy celebratory treatment. |
| **Skeleton** | `#eaeaea` blocks at final card dimensions, 16px radius, minimal pulse. Matches the flat system aesthetic. |
| **Disabled** | Muted (`#666666`) text on `#eaeaea` background; blue actions fade — preserves brand read while signaling unavailability. |

Treating named Primary Hover as a different evidence type from `focus-visible`, treating a later generic Focus observation as not `focus-visible` evidence, and treating omitted loading/error/success fields as unresolved mapping rather than as `not-applicable` from non-observation, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence.

Named Primary Hover `#0083ec` is a different evidence type from keyboard-focus treatment. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating YAML-row-absent Type as not invented for Blue Tint Card, and treating White Service Card / Blue Tint Card / Orange Promo Badge / Blue Feature Badge as lacking interactive-kind confirmation for a §4.4 map, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. White Service Card, Blue Tint Card, Orange Promo Badge, and Blue Feature Badge have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. YAML `tokens.components` records White Service Card as `type: card` and the two badges as `type: badge`. Blue Tint Card has no YAML `tokens.components` row; Type is not invented from YAML absence.

### Primary CTA

- Role: primary accept/consent and back-to-home CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#209cff`
- Text: `#ffffff`
- Radius: `100px`
- Padding: `16px 35px`
- Height: 50px
- Font: 16px / 400 / PingFang TC
- Hover (named): `#0083ec` (deeper blue)
- Use: YAML `Primary CTA, accept/consent, back-to-home actions`
- Observed: default; named hover `#0083ec`; live “我接受” and live “回首頁” as unmerged inspect records
- Field note: The following unmerged-field reading, including Role-as-the-harvested-primary-action, this-fill-not-accent-`#ff874d`, on-fill-white-not-ink, YAML-100px-not-YAML-full-100-not-live-回首頁-30px, YAML-50px-not-live-我接受-54px, YAML-padding-16px-35px-not-live-我接受-16px-not-live-回首頁-0px-35px, 16px-400-not-cta-label-16px-700, and named-hover-hex-not-keyboard-focus-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#209cff` is this control’s fill and catalog `primary_color`. It is not accent `#ff874d` and not hover `#0083ec` as a fill. `#ffffff` is this control’s on-fill label, not ink `#333333`. YAML radius `100px` is this control’s field; it is not YAML `rounded.full` 100 and not live “回首頁” `30px`. YAML height 50px is this control’s field; live “我接受” is 54px with padding `16px` and radius `100px`. Live “回首頁” is 50px with padding `0px 35px` and radius `30px`. Font 16px / 400 is this YAML component field; it is not YAML `cta-label` 16px / 700 / 1.50. Named hover `#0083ec` is not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the primary CTA |
| hover | applicable | Pointer-web button. Named treatment `#0083ec` is recorded on this control |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says blue actions fade; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this YAML row as accept/consent and back-to-home (“我接受”, “回首頁”); exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows. Source §14 Error (session expired) names a page with a “回首頁” pill; that page row is not copied here as this button’s error state. Treating that session-expired page row as not this button’s error-state computed paint is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

Additional observed named state: Primary Hover `#0083ec`. Treating that appearance as a named hover on this control, not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

### Find More (Secondary Tint)

- Role: section-level discovery CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#e4f2ff`
- Text: `#209cff`
- Radius: 8px
- Padding: `7px 58px`
- Height: 40px
- Font: 16px / 700 / PingFang TC
- Use: YAML `Find-more section CTA (找更多)`
- Observed: default (“找更多 >”)
- Field note: The following unmerged-field reading, including tint-fill-not-primary-`#209cff`-fill, this-foreground-not-ink, 8px-not-primary-100px, 40px-not-50px, and 16px-700-matching-cta-label-not-button-primary-16px-400, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#e4f2ff` is this control’s fill (YAML `primary-tint`), not primary `#209cff` and not `#f0f8ff`. `#209cff` here is this control’s foreground, not a second pill fill. Radius 8px is this control; it is not primary `100px` and not card 16px. Height 40px is this CTA; it is not YAML primary 50px. Font 16px / 700 matches YAML `cta-label`; it is not YAML `button-primary` `16px / 400`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the find-more section CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A discovery CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the section-level “找更多 >” discovery CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Primary Tab (Active)

- Role: active top-level nav tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#333333`
- Font: 16px / 700 / PingFang TC
- Height: 34px
- Use: YAML `Primary top nav active tab (個人家庭)`
- Observed: default / named active (“個人家庭”)
- Field note: The following unmerged-field reading, including active-ink-not-muted, 700-not-inactive-400, captured-variant-not-a-second-nav-token, and named-active-ink-weight-not-keyboard-focus-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#333333` is this tab’s active label, not muted `#666666`. Weight 700 is the active signal; inactive is 400. Named active ink/weight is a captured variant on this item, not a second nav token and not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the active top nav tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav tab selects a destination or segment; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

Additional observed named state: static active appearance (weight 700, `#333333`). Treating that appearance as a captured variant, not an observed click transition, and treating named active ink as not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

### Secondary Nav Links (Inactive)

- Role: inactive top-level nav tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#666666`
- Font: 16px / 400 / PingFang TC
- Use: YAML `Inactive top nav links (企業服務, 國際服務)`
- Observed: default (“企業服務”, “國際服務”, “科技研發”, “關於我們”)
- Field note: The following unmerged-field reading, including muted-not-ink-except-on-the-active-tab and 400-not-active-700, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. Resting `#666666` is this link’s label (muted), not ink `#333333`. Weight 400 is inactive; active is 700.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the inactive top nav links |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

### Search Input

- Role: site-wide search field
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#686868`
- Border (YAML): `1px solid #cbcbcb`
- Border (body §4): `1px solid #eaeaea`
- Radius: 4px
- Padding: `0px 10px`
- Height: 36px
- Font: 13px / 400 / PingFang TC
- Use: YAML `Site search field`
- Observed: default only
- Field note: The following unmerged-field reading, including YAML-border-`#cbcbcb`-not-body-`#eaeaea`, search-fg-`#686868`-not-muted-`#666666`, 4px-not-card-16px, 13px-400-not-body-20px, and 13px-400-as-body-table-Small-Label, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. YAML `border` / `border-input` `#cbcbcb` is this field’s YAML border. Body §4 records `1px solid #eaeaea` on this same named control. Both records are kept; they are not merged. `#686868` is this field’s value color, not muted `#666666`. Radius 4px is this input; it is not card 16px. Font 13px / 400 is this control’s field and the body-table Small / Label size; it is not Body Large 20px / 400 / 1.60.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the site search field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted. Source §14 names a field-level message in `#333333` below the input; that row is not copied here as a computed paint |

Loading and success applicability are omitted. Source records default geometry plus YAML/body border records. Exact loading/success mapping for this field is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

### White Service Card

- Role: service category / product listing card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Text: `#333333`
- Radius: 16px
- Use: YAML `Service category cards, product cards`
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-canvas, 16px-not-20px-modal-default-not-8px-tint, and no-shadow-as-body-§4-not-a-deletion-of-YAML-shadow-card, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#ffffff` is this card’s fill, not canvas `#fafafa`. Radius 16px is this card (YAML `rounded.lg` / YAML component `radius: 16px`); it is not YAML `card` 20px and not tint 8px. Body §4 does not assign a box-shadow to this card; YAML `shadow.card` stays in Foundations Elevation and is not copied here.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Blue Tint Card

- Role: featured service highlight / promotional content zone
- Anatomy: surface
- Background: `#e4f2ff`
- Text: `#209cff`
- Radius: 8px
- Use: Featured service highlights, promotional content zones
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented from YAML absence.
- Field note: The following unmerged-field reading, including tint-fill-not-primary-surface-`#f0f8ff`, this-foreground-not-a-second-CTA-fill, 8px-matching-Find-More-not-card-16px, and YAML-row-absent-Type-not-invented, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#e4f2ff` is this card’s fill; it is not `#f0f8ff`. `#209cff` here is this card’s foreground, not a pill fill. Radius 8px matches Find More; it is not White Service Card 16px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Orange Promo Badge

- Role: promotional tag / hot-deal indicator
- Type: badge
- Anatomy: label
- Background: `#ff874d`
- Text: `#ffffff`
- Radius: 4px
- Use: YAML `Promotional accent tags, hot-deal indicators`
- Observed: default only
- Field note: The following unmerged-field reading, including orange-fill-not-primary-`#209cff`, on-fill-white-not-ink, and 4px-matching-search-not-card-16px, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#ff874d` is this badge’s fill, not primary `#209cff`. `#ffffff` is this badge’s on-fill label. Radius 4px is this badge; it is not card 16px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Blue Feature Badge

- Role: feature highlight / primary status tag
- Type: badge
- Anatomy: label
- Background: `#209cff`
- Text: `#ffffff`
- Radius: 4px
- Use: YAML `Feature highlight or primary status tags`
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-not-orange-`#ff874d`, on-fill-white-not-ink, and 4px-matching-orange-badge-not-a-pill, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#209cff` is this badge’s fill; it is not orange `#ff874d` and not a second primary CTA. Radius 4px is this badge; it is not primary `100px`.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### White Service Card (local recipe)

Treating the following as a source §9 local composition — white `#ffffff` parent on `#fafafa` canvas + 16px radius + no shadow + title 18px / 700 / `#333333` + secondary 16px / 400 / `#666666` — and not as a rewrite of YAML White Service Card or of Type-role Sub-nav Header, and not as a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

- Role: service-card parent surface
- Anatomy: surface
- Background: `#ffffff` on canvas `#fafafa`
- Radius: 16px
- Shadow: none (this composition; YAML `shadow.card` stays in Foundations)
- Local children (this composition only): title in `#333333`, 18px PingFang TC weight 700; secondary text `#666666`, 16px weight 400
- Use: source §9 service card — white background on `#fafafa`, 16px radius, no shadow, 18px/700 title, 16px/400 secondary
- Kind, Type, and a state-applicability map are omitted. Harvested White Service Card stays in its own slot. Title 18px / 700 is this recipe’s child; it is not a rewrite of YAML `sub-nav` 18px / 700 / 1.44.
- Field note: The following unmerged-composition reading, including parent-white-not-a-second-canvas-token, 18px-700-title-plus-16px-400-secondary-as-this-card, not-a-rewrite-of-YAML-White-Service-Card, and not-a-rewrite-of-YAML-sub-nav-18px-700, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#ffffff` is this parent’s fill. The 18px/700 title and 16px/400 secondary stay children of this card; they are not promoted as extra global type roles.

### Navigation Bar (local recipe)

Treating the following as a source §9 local composition — white `#ffffff` parent + five tabs at 16px PingFang TC + active weight 700 / `#333333` + inactive weight 400 / `#666666` — and not as a rewrite of YAML Primary Tab / Secondary Nav Links, and not as a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

- Role: top navigation-bar parent
- Anatomy: surface
- Background: `#ffffff`
- Local children (this composition only): five tabs at 16px PingFang TC; active = weight 700, color `#333333`; inactive = weight 400, color `#666666`
- Use: source §9 navigation bar — white background, 5 tabs at 16px PingFang TC, active 700/`#333333`, inactive 400/`#666666`
- Kind, Type, and a state-applicability map are omitted. Harvested Primary Tab and Secondary Nav Links stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-white-not-a-second-canvas, five-tabs-as-this-bar, and not-a-rewrite-of-YAML-nav-tab-or-nav-link, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. `#ffffff` is this parent’s fill. The five tabs stay children of this bar.

### Promo Badge on Service Card (local recipe)

Treating the following as a source §9 local composition — Orange Promo Badge (`#ff874d` / white / 4px / 13px PingFang TC) placed in the upper corner of a service card — and not as a global pairing token or a rewrite of YAML Orange Promo Badge / White Service Card, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

- Role: promotional-badge-on-card parent
- Anatomy: surface
- Local children (this composition only): `#ff874d` background, white text, 4px radius, 13px PingFang TC, placed in the upper corner of a service card
- Use: source §9 promotional badge on a service card
- Kind, Type, and a state-applicability map are omitted. Harvested Orange Promo Badge and White Service Card stay in their own slots.
- Field note: The following unmerged-composition reading, including 13px-badge-plus-upper-corner-placement-as-this-recipe, not-a-global-pairing-token, and not-a-rewrite-of-Type-role-Small-Label, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification. The 13px label stays a child of this recipe; it is not a rewrite of Type-role Small / Label.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 20, xl 32, xxl 48, section 64. Body layout repeats 4px, 8px, 12px, 16px, 20px, 32px, 48px, 64px. Harvested heights: 50px YAML primary CTA, 54px live “我接受”, 40px Find More, 36px search, 34px nav tab, 38px sub-nav (touch record).

The following layout readings — utility-over-luxury, flat-tint-segmentation, card-separation, information-dense-portal, whitespace-functional-not-editorial, centered-max-width-at-1440px, full-width-top-nav-with-5-primary-tabs, sub-navigation-row-horizontal-scrolling, hero-full-width-carousel, and 4–6-column-card-grids — are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification.

Recorded layout:

- Centered max-width layout at 1440px viewport
- Top navigation bar: full-width with 5 primary tabs (個人家庭, 企業服務, 國際服務, 科技研發, 關於我們)
- Sub-navigation row: horizontal scrolling links for service sub-categories
- Hero section: full-width carousel with promotional banners
- Content panels: 4–6 column card grids for service listings

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 50px / 54px / 40px / 36px / 34px / 38px figures as surface measurements in this packet rather than a cross-viewport specification, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, and treating hamburger-on-mobile / sub-category-wrap-or-horizontal-scroll / service-cards-multi-column-to-single-column / hero-banners-full-width-at-all-breakpoints as recorded collapsing rather than a complete responsive specification, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, sub-nav horizontally scrollable, hero compresses |
| Tablet | 640–1024px | 2-col card grid, condensed nav |
| Desktop | 1024–1440px | Full 4–6 col card grid, full horizontal nav |

Collapsing strategy recorded in the source: top navigation collapses to hamburger on mobile; sub-category links wrap or scroll horizontally; service cards stack from multi-column to single-column; hero banners maintain full-width treatment at all breakpoints.

Touch-target record: primary pill CTA 50px height × full-width on mobile; nav tabs 34px height; sub-nav links 38px height with 8px right padding.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-22)

The live strings below are source-stated. Treating the parenthetical characterizations (exhaustive-service-listing / low-friction-direct / two-characters-full-concept / segment-first-then-product) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

- "5G行動上網通話、HiNet光世代寬頻上網/市話、MOD影視娛樂及智慧生活應用" — page title (exhaustive service listing). *(verified live 2026-06-22)*
- "找更多 >" — section-level discovery CTA (low-friction, direct). *(verified live 2026-06-22)*
- "個人家庭" — primary nav tab for the consumer segment (two characters = full concept). *(verified live 2026-06-22)*
- "新申請", "續約門號" — CTA labels named in source §10. *(source body)*
- "企業服務", "國際服務", "科技研發", "關於我們" — inactive nav tabs. *(source body)*
- "行動上網通話" — sub-nav section header. *(source body)*
- "我接受", "回首頁" — primary CTA labels. *(source body / inspect)*

Treating the §14 empty/loading/error/success strings (“網頁過期囉”, “回首頁”, 123 customer service) as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Chunghwa Telecom-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — reliable-inclusive-and-service-oriented / authority-of-Taiwan-dominant-telecom / approachability-of-a-national-public-utility / declarative-and-benefit-forward / dense-with-product-specifics / Categorical-and-precise / Product-forward / Direct-and-action-named / Urgency-without-panic / Institutional-clarity / vague-aspirational-slogans / English-dominant-copy / aggressive-fear-based-urgency-without-stated-terms — are a derived editorial implementation inference from the verified surfaces; they are not Chunghwa Telecom-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Chunghwa Telecom's voice as reliable, inclusive, and service-oriented — the brand speaks with the authority of Taiwan's dominant telecom operator and the approachability of a national public utility. Headlines are declarative and benefit-forward ("5G行動上網通話", "HiNet光世代寬頻"), naming the service directly without marketing abstraction. Copy is dense with product specifics — plan names, speeds, prices.

| Context | Tone |
|---|---|
| Navigation tabs | Categorical and precise. "個人家庭", "企業服務", "國際服務" — segment first, then product. |
| Hero banners | Product-forward. Names the plan, technology, and benefit in one tight phrase. |
| CTAs | Direct and action-named. "新申請", "續約門號", "找更多" — no soft language. |
| Promotional copy | Urgency without panic. Price and deal terms stated plainly. |
| Error / system copy | Institutional clarity. "網頁過期囉" (page expired) with a single recovery action. |

**Forbidden register** (source): vague aspirational slogans that omit product specifics, English-dominant copy that marginalizes Mandarin speakers, aggressive fear-based urgency without stated terms.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exception remains Primary CTA Hover `#0083ec`
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Primary CTA and Find More; omitted loading/success on Search Input
- interactive kind and state-applicability map for White Service Card, Blue Tint Card, Orange Promo Badge, and Blue Feature Badge
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
