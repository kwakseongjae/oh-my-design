T2 마이그레이션을 시작합니다. 먼저 규칙집·대응표·골든 샘플과 104번 레거시 본문을 순서대로 읽고, 그다음 Core v2로 옮기겠습니다.규칙집을 확인했습니다. 이어서 대응표, T1 제약, 골든 샘플, 그리고 104번 레거시 본문을 읽겠습니다.골든 샘플과 104번 레거시 본문을 읽고, 최근 통과한 이관본 형태도 대조하겠습니다.최근 통과 이관본과 게이트가 104와 비슷한 컴포넌트(탭·카드·다이얼로그)를 어떻게 처리했는지 확인하겠습니다.규칙과 골든·통과본 형태를 맞춰 본문을 작성합니다. 작성 직후 B2a 재독과 E2 grep 대조를 하겠습니다.B2a 스캔에서 범위 한정, Elevation 판단, 타입 비율 설명, Content 인용 성격이 비한정으로 남아 있어 인접 한정을 붙입니다.게이트가 `32px` 발명을 잡았습니다. 원문은 `32–36px`만 있고, 로그가 `32px`을 따로 써서 차단된 거라 그 표기를 고칩니다.`web/references/104/DESIGN.md`를 Core v2로 옮겨 `docs/design-md-weight/migrated/104/`에 세 파일을 썼습니다. 규칙집 **v7**.

보존한 값: YAML 비율 `1.43` / `2.14` / `1.4`, `type: button|tab|card|dialog` 컴포넌트별 유지, `tokens.source: live-extract`, Search Action `2px transparent`, Canvas/On Action `#ffffff` 분리, §14 `observedStates/interactionKinds/interactionCount` 0.

의미 경계: Search Action·Outline Action의 loading/error/success는 역할이 닫히지 않아 생략(C2). Company Card·Login Dialog는 kind/map 생략(C4). 미관측을 `not-applicable`로 바꾸지 않음. Principles 1–5와 Scope/Content 해석 문장에는 인접 B2a 한정.

검증: `inspectDesignMd` `portable_core: true`, placeholders 0. `--gate-only` PASS, problems 0. `--require-portable-core` exit 0. F3 감사와 sol 의미 레인은 대기. 카탈로그 채택은 아님.

DONE migrated=1
