# 誠品 migration log

Source: `web/references/eslite/DESIGN.md`
Destination: `docs/design-md-weight/migrated/eslite/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/eslite/provenance.md`
Date: 2026-08-26
Worker: opus5 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v9**
Gate: `node test-v2/tools/migrate-reference.mjs --brand eslite --gate-only` — result recorded at the end of this file.

Source name `誠品` is carried byte-for-byte into the H1 (`# 誠品 Design System`) and into every portable sentence that names the company, and the Latin `Eslite` never replaces it (A5). `Eslite` survives where the source itself uses the Latin form (`Eslite Online`, `Eslite-inspired`, `Eslite brand font`), and appears once more as an added first-mention gloss — `誠品 (Eslite)` in Scope — which sits **beside** the 繁體 name, not in place of it. Where source prose used `Eslite` / `Eslite’s` as the bare company name (§10, §11, §12, §13), the portable body writes `誠品` / `誠品’s` or drops the possessive: substitution runs only toward the published 繁體 name, never away from it. Every multi-character 繁體 run in the source — `誠品`, `蘋方體`, and the three §10 voice samples — is present in the portable body byte-for-byte.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity `id` / `name` / `country` / `category` | 분리 → provenance Identity | Portable file has no frontmatter. `name` `誠品` also 옮김 → H1 and portable body, byte-exact (A5, dual destination E2a). |
| YAML `homepage` `https://www.eslite.com/` | 분리 → provenance Identity + Surfaces + Sources + Tier 1 | The portable Scope names the Eslite Online homepage as a captured route without repeating the bare URL. `https://www.eslite.com/brand` and `https://www.eslite.com/brand/1098` are dual: portable Primary tasks + provenance Surfaces/Sources/Tier 1 (E2a). |
| YAML `primary_color` `#917e57` | 옮김 → Foundations Search Accent + Experience Distinctive traits + Experience Scope reading + Experience Capture-bound application; 분리 → provenance Identity | Five destinations (E2a), grep-checked: portable Scope reading, Distinctive traits, Capture-bound application, Foundations Search Accent, plus the provenance identity ledger. The portable body labels it "also the catalog identity color" rather than asserting a universal role, matching the source boundary. |
| YAML `logo` (`type: favicon`, Google favicon-service slug) | 분리 → provenance Identity; 경계 문장 옮김 → Typography & Assets | Dual destination (E2a). The slug value lives only in the ledger; the portable body keeps the boundary (favicon-service URL on a third-party host, identity metadata rather than a captured first-party 誠品 mark). |
| YAML `omd: "0.1"`, `verified`, `verification_v2` (`schema`, `checked`, `surfaces`, `sources`, `conflicts`, 37 `claims`) | 분리 → provenance Identity / Freshness / Surfaces / Sources / Claim ledger | 출처 원장·freshness·Proof (E1). Per-claim `method` values (`supplied-selector-backed-computed-style`, `supplied-selector-provenance`, `supplied-static-state-samples-without-interaction`, `supplied-selector-provenance-and-classification`) are preserved as verification metadata (A1c). |
| YAML `tokens.source: reconciled`, `tokens.extracted: 2026-07-13`, `components_harvested: true` | 분리 → provenance Identity (세 필드 전부) + Freshness (`tokens.extracted`) + Proof notes (`components_harvested`) | Ledger fields, kept as values rather than dropped (A1c). Per-field destinations, grep-checked: `reconciled` → Identity only; `2026-07-13` → Identity **and** Freshness (E2a); `components_harvested` → Identity **and** Proof notes (E2a). None of the three reaches the portable body. |
| YAML `tokens.note` | 옮김 → Foundations Evidence-domain boundary; 원문 분리 → provenance Identity | Dual destination (E2a). Portable keeps the operative rule "Only supplied selector-backed public commerce values are tokens here"; the ledger quotes the note in full, including the corporate/editorial/anniversary/unresolved-font clause. |
| YAML `tokens.colors` (accent, canvas, foreground, muted, hairline) | 옮김 → Foundations Semantic color | All five hexes preserved at field granularity: `#917e57` `#ffffff` `#212529` `#999999` `#cccccc`. `muted` stays the search-field text field and is not merged into a general metadata Ink (A4). |
| YAML `tokens.typography.body` / `compact-control` (size, weight, lineHeight, use) | 옮김 → Typography & Assets Type roles; `use` 원문 분리 → provenance | Dual destination (E2a). 16px/400/24px and 14px/500/20px preserved. Line heights are px strings in the source and stay px — no unitless ratio exists to convert (A1a n/a). The `compact-control.use` limit ("no complete control type scale is claimed") is carried into the portable body under the Type roles table. |
| YAML `tokens.spacing` (search-y 6, search-x 12, search-leading 16) | 옮김 → Foundations Spacing + Layout & Platforms + Components padding + Experience Capture-bound application | 6px / 12px / 16px. The literal `6px 12px 6px 16px` string is dual (E2a): the component anatomy line and the retained §9 geometry bullet under Capture-bound application; Foundations Spacing and Layout carry the same measurements in prose. Named as search-field measurements, not a spacing scale — the source establishes no scale. |
| YAML `tokens.rounded` (square 0, search-left 8) | 옮김 → Foundations Shape + Components radius + Experience Capture-bound application + Experience Avoid | 0px square card geometry; 8px left-only search rounding. The literal `8px 0px 0px 8px` string is triple (E2a): Foundations Shape, the component radius line, and the retained §9 geometry bullet under Capture-bound application. Not generalised into a radius scale; the "do not spread the 8px radius" rule sits in Avoid. |
| YAML `tokens.components.public-search-input` (type, bg, fg, border, radius, padding, height, font, states, use) | 옮김 → Components & States Public Search Text Input; `states` 원문 + selector 분리 → provenance | Verified `type: input` preserved as `Type: input`, not flattened into `Kind: interactive` alone (A1b). `states` is dual: portable `Observed` line + provenance claim ledger and quoted string (E2a). Selector `home::[data-omd-capture="11"]` is dual: portable `Use` line + provenance Capture selectors (E2a). `height: 40px` is triple (E2a): the component anatomy line, Layout & Platforms, and the retained §9 geometry bullet under Capture-bound application. |
| YAML `tokens.components.expo-product-card` (type, bg, fg, border, radius, padding, height, font, use) | 옮김 → Components & States Expo Product Card Shell; selector + class list 분리 → provenance (also kept in the portable `Use` line) | Verified `type: card` preserved as `Type: card` (A1b). Kind and the §4.4 state-applicability map are omitted, because the source classifies it as a card shell despite its anchor implementation and records no interaction evidence (C4). Zero-width `rgba(0, 0, 0, 0.176)` border kept verbatim. Selector `surface-3::[data-omd-capture="91"]` and the class string are dual (E2a). |
| §1 Visual Theme & Atmosphere — history, ecosystem, "Live your Dream" chapter, scope boundary | 옮김 → Experience `scope` claim | Product/surface scope: three captured public routes; not member, checkout, native-app, or physical-store systems. |
| §1 — "gives that cultural proposition a restrained interface expression", "keep dense retail choice legible rather than theatrical", "feels editorial and calm" | 옮김 → Experience `scope` claim, under an adjacent complete qualification | These are the source author's evaluative readings, i.e. derived, not brand-published. Adjacent text reads "derived editorial implementation inference from the verified surfaces; it is not 誠品-authored or a separately published UI specification" (B2/B2a). |
| §1 Key characteristics (4 bullets) | 옮김 → Experience Distinctive traits | Token facts and the 3-surface / zero-interaction record. The evaluative halves ("carry the baseline information hierarchy", "component geometry remains useful") were restated as observations so the list carries no unqualified interpretation (B2a). |
| §2 Selector-backed public commerce colors | 옮김 → Foundations Semantic color | Five roles with their observed contexts. |
| §2 Evidence-domain boundary (incl. browser-default blue links) | 옮김 → Foundations Evidence-domain boundary | Public commerce vs corporate/editorial/anniversary domains kept separate; blue-link observations kept as unpromoted. Also recorded in provenance Proof notes (E2a). |
| §3 Typography evidence classes (5 rows) | 옮김 → Typography & Assets Font evidence | Official product-use / live computed surface-use / official distributed brand asset / declared-only / unresolved preserved as distinct evidence domains. The last row's label is written `Unresolved claim` rather than the source's bare `Unresolved`: a lone `Unresolved` cell reads as an unfilled placeholder to the Core conformance checker, and every migrated body in this catalog that carries such a row qualifies the label — 17 as `Unresolved claim`, 8 with another class noun (`Unresolved mapping`, `Unresolved evidence class`, `Unresolved boundary`, …) — while none leaves it bare. The checker behaviour is a false positive on a legitimate evidence-class label and is reported as such in `audit-log.md` (E3); no value notation was altered to obtain it. Meaning is unchanged; the row still names the evidence class. `Noto Sans TC`, PingFang TC, `蘋方體`, Microsoft JhengHei, `NotoSerifCJKtc`, `swiper-icons` byte-exact, 繁體 included (A5). |
| §3 Captured hierarchy (3 rows) + fallback warning | 옮김 → Typography & Assets Type roles + Family | Three roles with sizes, weights, line heights, and evidence boundaries. The "do not render a system fallback as an Eslite-owned typeface" rule stays in the portable body. |
| §4 Search Text Input + Product Card Shell | 옮김 → Components & States | Anatomy, all measured values, and use strings. |
| §4 Scope boundary paragraph (`interactionKinds: 0`, `interactionCount: 0`, "not recast as a generic button", other anchors as raw evidence) | 옮김 → Components & States Capture record + Expo Product Card Shell Kind reason | Also 분리 → provenance Proof notes (E2a). |
| §4 footer **Verified** / Tier 1 / Tier 2 / Conflicts unresolved | 분리 → provenance Freshness / Tier 1 / Tier 2 / Conflicts | freshness·원장 (E1). The two Tier 2 retrieval failures are recorded with their attempted date and the note that no values were used. |
| §5 Layout Principles | 옮김 → Layout & Platforms (+ Foundations Spacing/Shape) | Search strip, dense lists, repeatable card shells; 40px height, 6px/12px/16px padding, 0px radius, 270px card height; explicit "not a universal grid, breakpoint, mobile, or checkout-layout specification". |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | `box-shadow: none` on both promoted components; zero-width `rgba(0, 0, 0, 0.176)` is not evidence for a visible stroke; no popover/modal/menu/elevated-shadow published. |
| §7 Do's (5 items) | 옮김 → Experience Capture-bound application (4 of 5) + Content & Locales Locale (1 of 5) | The Traditional-Chinese preservation rule landed in Content & Locales because it is a locale rule; the other four are capture-bound application. Both landing sites carry an adjacent complete derived-editorial qualification, since these are the source author's prescriptions rather than published doctrine (B2/B2a). |
| §7 Don'ts (5 items) | 옮김 → Experience Avoid | Boundary prohibitions (blue links, unlicensed font naming, 8px radius spread, uncaptured interaction behavior, corporate imagery as product tokens). No qualification added: each is an evidence-boundary limit, which E1 keeps in the body as-is. |
| §8 Responsive & Interaction Notes | 옮김 → Layout & Platforms | Three desktop public-surface records, no interaction capture; measured components are not accessibility-target or breakpoint rules; static focus/pressed samples do not establish a transition contract; responsive layouts, menu expansion, cart, checkout, member states absent. |
| §9 Implementation Notes | mixed: 4문장 중 3문장 옮김 → Experience Capture-bound application (2 bullets); 1문장(`Use the gold #917e57 only where the captured search-control context calls for it`)은 §7 Do #2와 중복이라 그 bullet에 합침 | Grep-verified that every value §9 restates already lands elsewhere: white field → Foundations Canvas `#ffffff`; 16px/400 → Type roles; narrow gray edges → Foundations Hairline `#cccccc`; square cards → Foundations Shape 0px; `#917e57` search-only → Capture-bound application + Foundations; 40px / `6px 12px 6px 16px` / `8px 0px 0px 8px` → Components + Layout. Correcting an earlier reading of this row: the opening sentence was **not** deleted as a mere value restatement. It is retained as the Capture-bound application bullet "Begin an Eslite-inspired public catalog surface with a white field, compact 16px / 400 reading text, narrow gray edges, and content-led square cards", alongside the two §9-only sentences (reuse-the-geometry-exactly, and "treat official cultural language as editorial direction, not authorization to create a new checkout, membership, or interaction system") — A3. Only the gold-only sentence has no bullet of its own, because §7 Do #2 already states the same rule; nothing from §9 was dropped. |
| §10 Voice & Tone — three official strings | 옮김 → Content & Locales Brand-published lines | `Live your Dream`, `每個精彩可期的相遇，每間店可愛有趣的小故事`, `讓人文、藝術與創意融入於你我的生活` are byte-exact. English glosses are placed **beside** the 繁體 strings and labelled as reading aids, never as replacements (A5). |
| §10 Voice & Tone — characterisation + Do/Don't table | 옮김 → Content & Locales Voice reading, under an adjacent complete qualification | The "reflective, welcoming, invites without urgency" characterisation and all four Do/Don't rows are the source author's editorial reading of official language (B2/B2a). |
| §11 Brand Narrative | mixed: 대부분 옮김 → Experience `scope` claim; 편집적 총론 2문장 삭제; URL 원장 분리 → provenance | Ecosystem breadth, corporate pages for ideals/founder/operating scope/development history, the Japanbridge cultural-bridge account, the expo platform description, and the 35th-anniversary "Live your Dream" evolution with its "narrative and voice context, not a token" boundary. The four inline narrative URLs move to the provenance Sources/Tier 1 ledger. 삭제 2문장 + 사유: "Eslite’s current cultural-commerce identity joins reading with a broader everyday-life proposition." and "The cultural premise is active beyond a storefront." are the source author’s thesis restatements, not brand-published or measured facts; each only re-states the ecosystem breadth and the beyond-the-storefront cultural claim that the retained §11 sentences already carry with their own attributions, so they were dropped rather than re-hosted under a B2a qualifier. Grep-verified absent from the portable body (`cultural-commerce`, `beyond a storefront` → 0 hits). No verified value, URL, or 繁體 string is lost with them. |
| §12 Principles (4 numbered items with UI implications) | 옮김 → Experience Principles, under an adjacent complete qualification | All four kept with their UI implications. The qualification uses the approved wording (B2a). |
| §13 Personas | 옮김 → Experience Audience | Three public participant groups with their source attributions, and the source's own "no named customer persona, journey, spending pattern, or conversion behavior is asserted" line. No demographic is invented, and no group is re-hosted in provenance as a persona (D2). |
| §14 States | 옮김 → Components & States Capture record + per-component applicability | 본문 보존: the full §14 sentence (no empty/loading/error/success/skeleton/disabled/validation/transition state published; `interactionKinds: 0`, `interactionCount: 0`; only default geometry and retained static input samples). The declared interactive input closes the Core §4.4 seven-state map by control meaning; non-observation omits the visual treatment only and is never used as a `not-applicable` reason (C1). The card declares no map at all (C4). Coverage is explicitly not claimed complete (C3). No graph delegation. |
| §14 static focus/pressed samples | 옮김 → Components & States Capture record (separate observed-sample note) + Components `Observed` line | Kept as observations of static samples and explicitly not promoted into a `focus-visible` treatment; no focus-visible row carries a value (B1). |
| §15 Motion & Easing | 옮김 → Foundations Motion | "No first-party motion duration, easing curve, reduced-motion policy, or event transition was present in the supplied packet. No motion token is published for 誠品 in this reference." Source carries no easing curve, so there is no unsourced curve to remove, and no promotion condition to preserve — B3's five-evidence-kind clause is **not** claimed here, because the source never states a promotion gate and the portable body therefore does not contain one (E2c). |

## Named gaps check (D1a)

Every noun enumerated in Governance → Named gaps is a domain the source itself names as absent or unresolved, grep-verified against `web/references/eslite/DESIGN.md`:

| Named gap group | Source line |
|---|---|
| member, checkout, native-app, physical-store systems | §1 "not a claim about member, checkout, native-app, or physical-store systems" |
| hover, focus transition, pressed transition, disabled, dialog, toast, tab, validation, authenticated component states | §4 Scope boundary |
| empty, loading, error, success, skeleton state treatments | §14 |
| responsive layouts, menu expansion, cart behavior; universal grid, breakpoint, mobile, checkout-layout specification | §8 and §5 |
| named type family for the captured public commerce routes; first-party distributed Eslite type asset or license | §3 evidence classes |
| popover, modal, menu, elevated-shadow treatments | §6 |
| motion duration, easing curve, reduced-motion policy, event transition | §15 |

No domain the source leaves unmentioned is listed.

## Gate

`node test-v2/tools/migrate-reference.mjs --brand eslite --gate-only` → **PASS**, `problems: []`.

Portable Core: `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, all seven claim declarations present exactly once, `cleanTop: true`, `projectionLocale: en`.
