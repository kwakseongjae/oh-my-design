# hogangnono migration log

Source: `web/references/hogangnono/DESIGN.md`
Sibling read (not the migration input): `web/references/hogangnono/.verification.md`
Destination: `docs/design-md-weight/migrated/hogangnono/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hogangnono/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `1eda52cba59ca6cc719363df8e3e6f871e5db1a6bd720c4e219b20b85d57f292` (`web/references/hogangnono/DESIGN.md`). Sibling SHA-256 `1536f44471a5928c7f3d62429254e5eb151d677018f9c53420f648026fe13e17` (`web/references/hogangnono/.verification.md`). Worker-close portable DESIGN SHA-256 `0e9c01a4e134e99219974ae17b177b505f789b760dba9d088fe76ccc0dc22ee7`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / favicon slug 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# Hogangnono Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://hogangnono.com` is dual: `DESIGN.md` 9 + `provenance.md` Identity/Surfaces (E2a). `#584de4` is dual: first portable line `DESIGN.md` 11 (Scope / Foundations / components; `grep -oF` dest **19**) + `provenance.md` Identity (E2a). Line 20 is a blank after Primary tasks and does not contain the hex. The Google s2 favicon slug is dual: `DESIGN.md` 240 + `provenance.md` Identity (E2a). |
| YAML `omd: "0.1"`, `verified`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source footer's **Verified:** 2026-06-03 is in Freshness. These are ledger keys with no portable slot. `ds.type` is absent from the source (`grep -oF 'ds.type'` dest 0 / provenance 0) and is not invented. |
| YAML `tokens.colors` (16 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 79–106. All sixteen roles kept with names, values, and token-set keys: primary `#584de4`, primary-nav `#4d55b2`, primary-tint `#f3f4fc`, primary-light `#eeedfc`, price-accent `#4337de`, heading `#333333`, body `#4f4f4f`, caption `#6e6e6e`, muted `#b3b3b3`, border `#e5e5e5`, surface-chip `#f3f3f3`, canvas `#f9f9f9`, white `#ffffff`, info `#3e8ce8`, success `#3dab6a`, danger `#ee3a3a` (danger row is 106). YAML lowercase and source §2 mixed case stay on the same rows (`#4F4F4F` dest 2 · `#6E6E6E` 2 · `#B3B3B3` 2 · `#E5E5E5` 2 · `#F3F3F3` 3 · `#F9F9F9` 1 · `#3E8CE8` 1 · `#3DAB6A` 3 · `#EE3A3A` 3). Theme-color vs `--primary` conflict is dual: `DESIGN.md` 86/536 + `provenance.md` Freshness (E2a). |
| YAML `tokens.typography.family` (`sans: Pretendard`, `fallback: Apple SD Gothic Neo, NanumGothic, sans-serif`) | 옮김 → Typography & Assets Family | `DESIGN.md` 209–214. Paths named `tokens.typography.family.sans` / `fallback`. The fallback prohibition on 214 carries an adjacent complete qualifier (B2/B2a). Declared-only fallback-members-are-not-the-brand-face reading is qualified on 205. |
| YAML `tokens.typography.modal-heading / section / list-title / price / body / caption` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 216–236. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.33` dest 3 · `1.3` dest 7 (`1.33` is a substring of the `1.3` needle; the body ratio sits on the Type roles row at 221; `1.3 s` shimmer is the source §14 spelling). Source §3 `line-height 40px` stays beside `1.33` at 219/227. All six YAML `use` strings restored verbatim in the Token-set use column (A1, A3): `Large modal heading` dest 3 · `Section headings` 2 · `List item titles` 2 · `Apartment listing price display` 1 · `Base body text` 3 · `Caption / badge` 3. §3 ranges (18–22px / 15–17px / 14–16px / 11–13px) stay beside the YAML singles at 232–236; they are not collapsed. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) | 옮김 → Foundations spacing + shape; also Layout | Paths kept separate (easywallet-type check): Spacing slot `DESIGN.md` 108–121 `xs: 4` · `sm: 8` · `md: 12` · `base: 16` · `lg: 24` · `xl: 32` · `xxl: 48` · `section: 64`. Shape slot 125–136 `sm: 4` · `md: 6` · `lg: 12` · `full: 9999`. `full: 9999` (colon form) dest 1 at 136; bare `9999` dest 3 (134/136/464). `tokens.spacing.md: 12` dest **2** (123/136) is not `tokens.rounded.lg: 12` dest **3** (123/136/325). `tokens.spacing.xs: 4` dest **3** (123/136/390) is not `tokens.rounded.sm: 4` dest **3** (123/136/390). `tokens.spacing.base: 16` dest **1** (123) is not the 16px body. `tokens.spacing.xxl: 48` dest **1** (123) is not the 48 px filter-chip strip. YAML `tokens.components.card` radius 6 is not `tokens.rounded.md: 6` (`DESIGN.md` 411). Layout restates both scales at **464** (463 is the blank after the spacing-unit bullet). Component `3px` / `8px` / `10px` / `5px` stay on those components. |
| YAML `tokens.shadow.button` | 옮김 → Foundations elevation + Primary Fill | `rgba(89,99,217,0.3) 0px 4px 5px 0px` dest 1 at `DESIGN.md` 151. Source §4 / §6 spelling `0 4px 5px 0 rgba(89, 99, 217, 0.3)` dest 2. Path `tokens.shadow.button` dest 2 (151/266). Dual with the Level 3 row; neither writing replaces the other. |
| YAML `tokens.components` (7 records) | 옮김 → Components & States | `DESIGN.md` 254–437. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×2 (Primary / Tint) = source YAML `type: button` 3 (`button-primary` / `button-tint` / `button-disabled` as the Primary disabled treatment, not a second component). Outline is §4-only and has no YAML type key — `Primitive type: not in the token set`, same as Filter Chip / Search Input / Modal Input / Map Control (`not in the token set` dest **5**). `badge` ×3 (Info Chip / Badge Up / Badge Down), `card` ×1. `Disabled primary state` dest 1. All seven YAML `use` strings restored as `Token-set use:` lines. YAML `font: "17px"` kept as `Token-set font record: 17px` on Primary and Tint. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 31–40. Atmosphere readings (data-clarity, civic data tool, pragmatic/modern, map-and-price dominant) carry an adjacent complete qualifier at 11 (B2/B2a). Surface-boundary reading at 9. Opening product line "Korea's #1 apartment real-estate transparency platform — map-first, data-honest, free of paid-listing distortion." kept at 9 (A5). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 79–106. Role-naming readings carry the adjacent qualifier at 81 (B2/B2a). Extra body-only hexes kept: `#cecfdc` dest 1 · `#CECFDC` 1 · `#cecece` 1 · `#eef0f3` 1 · `#dadada` 4 · `#f0f0f0` 3 · `#3a3a3a` 1 · `#f2f2f2` 2. |
| §3 Typography Rules — family, hierarchy, ranges, tracking 0 | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 198–207; family 209–214; hierarchy table 216–236. Official product-use (202), Official distributed (204), Declared-only (205), License (206), Outside these captures (207), and Type-roles range-plus-40px/1.33 (229) readings carry adjacent complete qualifiers (B2/B2a). Button 17px rows sit in the §3 range list at 235–236 and on the two button components. |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 254–437. §4 body values (50px / 45px / 38px / 32px / 3px / 8px / 0.5px / `#cecfdc` / `#CECFDC` / `#eef0f3` / focus `2px solid #584de4`) and YAML `use` / `font` / radius forms are both kept where they differ. Outline, Filter Chip, Search Input, Modal Input, and Map Control have no YAML token-set row and stay as §4 records. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. `https://hogangnono.com` / desktop.css / reset.desktop.css / App Store URL are dual: Scope 9 + provenance Surfaces (E2a). Conflicts unresolved: theme-color vs `--primary` — `DESIGN.md` 86/536 + `provenance.md` Freshness. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | `DESIGN.md` 453–464 (354–375 px dest 3 · 510 dest 2 · 48 px strip / header · 35 px · 10 px · 12 px / 20 px card padding · 8 px / 5 px gaps). Map-first reading carries the qualifier at 455; the YAML-scale-not-local-radii restatement carries its own qualifier at 464 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Six-row table `DESIGN.md` 138–153, including Level 1 list-panel stack, Level 2 FAB `4px 2px 12px 0px rgba(0,0,0,0.24)`, Level 3 indigo shadow, Level 4 `#000` / 0.3 scrim / `z-index: 15`, tooltip `#3a3a3a` / 5 px / `z-index: 20`. YAML `tokens.shadow.button` sits on 151. The map-first-depth reading carries an adjacent qualifier at 140 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 52–62, under the grouping qualifier at 54. Not placed in Governance controlled copy. Seven items, matching the source Do list (last item, the `#f3f4fc` tint rule, is 62). |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 64–73, under the qualifier at 66. Six items, including the `#4d55b2` new-interactive prohibition and the `#4337de` non-price prohibition. No invented domain (no `native application`, no `back-office`, no `product application`). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 466: fixed 354–375 px split, mobile app (iOS/Android) bottom-sheet, horizontal chip scroll, FAB absolute/fixed, `(-webkit-min-device-pixel-ratio: 2)`, 510 px modal with pixel-perfect negative margins. `mobile app` dest 1 = source 1. Exact string `measures 1440px` dest 0 / provenance 0. The source-written-not-cross-viewport reading carries an adjacent qualifier at 468 (B2/B2a). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` §9 deletion check. |
| §10 Voice & Tone | 옮김 → Content & Locales | Three adjectives and the Do/Don't table `DESIGN.md` 471–482; published UI strings 484–492; illustrative samples 494–498 under the source's own "illustrative" label. The qualifier at 473 covers the characterization and the table. The illustrative-sample reading is qualified at 494. The byte-exact / gloss-beside rule is qualified at 500 (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: **2015** dest 1; **Shim Sang-min** dest 1; **Ministry of Land** dest 1; team of three / first version in weeks; **호갱** dest 7; **3D sunlight** dest 1; **school-district** dest 1; **reconstruction auction** dest 1; **resident reviews** dest 1; **Zigbang** dest 1; **2018** dest 1; **2 million** dest 1; **"advancing the real estate industry through IT by providing honest information"** dest 1; "what did this apartment actually sell for, and when?"; **Every design decision — the prominent price display, the map-first layout, the refusal to accept payment for listings — flows from that original commitment to be the side of the user.** dest 1 (restored in `Revision 2026-08-28 (wave31 review)`). Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` Proof notes. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 42–50 under the B2a form at 44: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Hogangnono-authored or a separately published UI specification." Hogangnono has no published first-party design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). `"확인하기"` / `"비교하기"` / `"문의하기"` dest 2 each. Portable derived-editorial inventory: `provenance.md` Portable derived-editorial scope (**31** rows). |
| §13 Personas — 4 entries | 삭제 | The source's own header labels them illustrative archetypes, not real users. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, or city appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience at `DESIGN.md` 27–29 carries only the group-level description the source records (buyers and renters seeking official transaction data), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States — 8 rows | 옮김 → Components & States Surface state contract + per-component applicability | Full eight-row body preserved at `DESIGN.md` 439–451 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "조건에 맞는 매물이 없어요", "데이터를 불러오지 못했어요 — 다시 시도해 주세요", "공식 거래 기록이 없어요", "문의가 전송되었어요", `#F3F3F3` skeleton, 1.3 s shimmer, `#EE3A3A` banner, `#3DAB6A` toast, `#E5E5E5` map bubbles, `#f0f0f0` / `#dadada` disabled. The applicability rule is at 246–252, whose qualifier at 252 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary Fill opens loading/error/success as a commit-capable action (`loading \| applicable` dest 1). Tint / Outline / Filter Chip / Search / Modal Input / Map Control close loading as destination, toggle, field, or map control (`loading \| not-applicable` dest 6). Search and Modal Input open error as fields (`error \| applicable` dest 3 including Primary) and close success (`success \| not-applicable` dest 6). The card gets no `kind` and no map because the source supplies no interaction evidence for the container — `DESIGN.md` 412 (C4). The three badges declare `Kind: non-interactive` dest 3 with a reason instead of a map (Core §4.4). |
| §15 Motion & Easing — durations, CSS easing, named keyframes, Photoswipe curve, motion rules | 옮김 → Foundations motion | `DESIGN.md` 155–191: Micro 200 ms / Standard 300 ms / Expand 350 ms / Map pan 350 ms–2 s; `ease` / `transform 0.35s` / `transform 2–3s` linear / `opacity 0.2s linear` / `opacity 0.3s`; `cubic-bezier(0.4, 0, 0.22, 1)` dest 1 attributed to Photoswipe dest 5; `fadeIn` dest 2 · `bottomBoundIn` dest 2 · `topBoundIn` dest 2; rules (no width/height reflow, instant price numbers, named keyframe uses). Motion-contract reading at 157; Photoswipe-not-a-token-set-step reading at 176 (B2/B2a). No `prefers-reduced-motion` sentence was written — the source does not state one, and the absence is omitted rather than denied (D1). |
| §15 Motion & Easing — unattributed template curves | 해당 없음 (삭제할 무출처 커브 없음) | The source does not contain `cubic-bezier(0.4, 0.0, 1, 1)` or the other `spec/omd-v0.1.md` example-table curves. `grep -oF 'cubic-bezier(0.4, 0.0, 1, 1)'` dest 0 / provenance 0. The one gallery curve is attributed and kept. B3 is held: the promotion condition at `DESIGN.md` 184 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text, plus the partial-confirmation exclusion (E2c). Governance 538 restates the five kinds and the gate. |
| Opening product / atmosphere sentence (legacy 58) | 옮김 → Experience Scope | `DESIGN.md` 9–11: 호갱노노; Korea's #1 apartment real-estate transparency platform; hogangnono.com + desktop.css + reset + App Store; indigo-violet `#584de4`; Pretendard; 0.3 s transitions. |

## Sibling handling (`web/references/hogangnono/.verification.md`)

The sibling exists — confirmed with `find web/references/hogangnono -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: CSS line 429 / occurrence count 66; `--gray900`…`--gray100` variable names; class selectors `.btn-basic.fill` / `.css-c4krkq` / `.scene-recommend-apt-list`; `line-height: 48px`; `line-height: 49px`; `#FFF`; `#0000003d`; `Apple SD 산돌고딕 Neo`; `나눔고딕`; `ng`; founded-Seoul wording; `(주)호갱노노`; `1.09 MB`; `No designs found for 'hogangnono'`. Recorded at `provenance.md` sibling-only list.
- Measured `DESIGN.md` 0 for those sibling-only strings: `line-height: 48px` 0 · `line-height: 49px` 0 · `#0000003d` 0 · `Apple SD 산돌고딕 Neo` 0 · `나눔고딕` 0 · `(주)호갱노노` 0 · `1.09 MB` 0 · `css-c4krkq` 0.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Hogangnono is Korean-and-English, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published Korean labels in the source body | 9 distinct | 0 | 0 | 호갱노노 / 호갱 / 조건에 맞는 매물이 없어요 / 데이터를 불러오지 못했어요 — 다시 시도해 주세요 / 공식 거래 기록이 없어요 / 문의가 전송되었어요 / 확인하기 / 비교하기 / 문의하기. Illustrative samples kept under the source's own label: 이 단지의 최근 실거래가를 확인했어요… / 호갱 되지 마세요… / 원하시는 조건을 설정하면…. |
| Published English / YAML `use` strings | 16 distinct | 0 | 0 | Korea's #1 apartment real-estate transparency platform — map-first, data-honest, free of paid-listing distortion. / advancing the real estate industry through IT by providing honest information / no more being a fool / Primary fill action, 50px tall / Secondary tint button / Disabled primary state / Info chip / subtle fill / Positive up-trend price indicator / Negative down-trend price indicator / Card surface with gentle elevation / Large modal heading / Section headings / List item titles / Apartment listing price display / Base body text / Caption / badge. |
| Sibling published strings | 1 sibling-only brand string | 0 | 0 | `(주)호갱노노` survives in provenance; not promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 호갱노노, 호갱, 조건에 맞는 매물이 없어요, 데이터를 불러오지 못했어요, 공식 거래 기록이 없어요, 문의가 전송되었어요, 확인하기, 비교하기, 문의하기, 2015, Shim Sang-min, Zigbang, 2018, 2 million, Ministry of Land, 3D sunlight, school-district, reconstruction auction, resident reviews, Every design decision, refusal to accept payment for listings, side of the user.

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 9 Korean + 16 English/use extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hogangnono --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 9, candidates: 146 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 9 < `candidates` 146.

## Deviations recorded

- `DESIGN.md` is above the spec's 600–1,800-word SHOULD budget (`wc -w` 6,268). The budget yielded to A1: seven declared component records with dual `use`/`font` forms, an eight-row surface state contract, a seven-state applicability matrix on seven interactive controls, `tokens.rounded.full: 9999` kept off local 3px/8px/10px radii, the full §11 founding narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Hogangnono has no published first-party design system, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Worker-close measure was 27. Auditor measure `grep -o 'derived editorial implementation inference' DESIGN.md \| wc -l` = **31** = `not Hogangnono-authored` 31 = `separately published UI specification` 31. Provenance derived ledger **31** rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is a Hogangnono-issued fact or a derived reading. Scope atmosphere, narrative-as-context, primary-task selection, audience grouping, trait grouping, Principles / Do / Don't headers, Semantic role names, spacing/shape key-path separation, elevation and motion contract readings, Photoswipe-not-token-set-step (176), font evidence Official product-use / Official distributed / Declared-only / License / Outside-captures, fallback prohibition, type-role range-plus-40px/1.33, favicon-as-pointer, applicability procedure, surface-state system-level reading, layout map-first, YAML-scale restatement (464), and responsive readings, voice characterization, illustrative-sample classification, and the byte-exact rule each carry an adjacent complete toss-form qualifier. Source facts (years, hexes, YAML keys, published strings, recorded durations and keyframes) were left unqualified. Auditor inventory: 31 lines.

## F2 E2 rescan (mandatory final pass)

Each log row was written only after `grep -oF` against `DESIGN.md` and `provenance.md`. Dual destinations named above were confirmed in both files. B3 is claimed only because `DESIGN.md` 184 names all five evidence kinds, the per-component gate, and the partial-confirmation exclusion in full text (E2c). Governance 538 restates the five kinds and the gate and does not repeat the partial-confirmation sentence. No "B3 유지" claim is made beyond that. Sibling-only strings are claimed dest 0 after measurement. Persona identifiers are claimed dest 0 / provenance 0 as use, not as a self-denominator absence sentence that lists the names (D2a, E2d). B2a 31=31.

## Revision 2026-08-28 (wave31 review)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**. Trigger: semantic review FAIL 2, re-confirmed by the orchestrator. Two defects only. Token values, component-table structure, other state rows, B2a complete qualifiers, and the derived-inventory 1:1 were not opened. Source `web/references/hogangnono/DESIGN.md` and sibling `.verification.md` were not modified.

`find` confirmed the six files before any count: dest `DESIGN.md` / `provenance.md` / `migration-log.md` / `audit-log.md`, source `DESIGN.md`, sibling `.verification.md`. Counts below are `grep -oF '<needle>' <file> | wc -l`, file by file. A file that exists and prints no match is 0 for that file.

**1. A1 — restore the source §11 closing sentence.** Source `web/references/hogangnono/DESIGN.md:292` `Every design decision — the prominent price display, the map-first layout, the refusal to accept payment for listings — flows from that original commitment to be the side of the user.` Scope ¶3 already had the preceding sponsored-content / 실거래가 sentence and stopped there. The missing sentence is a source fact, so it is restored in the same paragraph at `DESIGN.md` 13, in the source's own wording, immediately after "what did this apartment actually sell for, and when?". No new B2a qualifier and no new provenance inventory row: the existing Scope ¶3 classification already covers the founding-and-acquisition narrative as context that does not supply tokens. The claim ledger and Proof notes name the restored sentence as source §11 narrative.

**2. A1 invention · A4 — remove Outline's transplanted `Primitive type`.** Source YAML `:46–48` `type: button` is three times only: `button-primary` / `button-tint` / `button-disabled`. Outline is §4-only and has no type key. The portable Outline row at `DESIGN.md` 303 had `Primitive type: \`button\`` — disabled's type moved onto a component that is not in the token set. Filter Chip / Search Input / Modal Input / Map Control already said `not in the token set`. Outline is now the same: `Primitive type: not in the token set` at 303. Primary Fill 257 and Tint 281 keep `Primitive type: \`button\``. The YAML components log row is corrected from ×3 (Primary / Tint / Outline) to ×2 (Primary / Tint).

실측 (`find` 후 `grep -oF` | `wc -l`, 파일별):

| needle | orig | sib | dest after | provenance after |
|---|---:|---:|---:|---:|
| `refusal to accept payment for listings` | 1 | 0 | **1** | 2 |
| `side of the user` | 1 | 0 | **1** | 2 |
| `Every design decision` | 1 | 0 | **1** | 1 |
| `Primitive type: \`button\`` | 0 | 0 | **2** | 0 |
| `type: button` | 3 | 0 | 0 | 0 |
| `Primitive type: not in the token set` | 0 | 0 | **5** | 0 |

`type: button` dest 0 is expected: the portable body writes `Primitive type: \`button\``, not the YAML key form. dest **2** = Primary 257 + Tint 281. dest **5** = Outline 303 + Filter 321 + Search 340 + Modal 361 + FAB 383. provenance `refusal` / `side of the user` 2 are the claim-ledger row and the Proof-notes sentence, not a second body fact. This revision section and the updated §11 / YAML-components rows mention the same needles; those mentions are this file's denominator (E2d).

Line count of `DESIGN.md` stays 538 (in-paragraph restore + same-line type correction). F2 pointers re-checked against the current file: 1 · 9 · 11 · 13 · 19 · 21–24 · 29 · 44 · 52–62 · 79–106 · 123 · 136 · 176 · 184 · 229 · 240 · 252 · 257 · 281 · 303 · 325 · 411 · 412 · 464 · 536 · 538. B2a `derived editorial implementation inference` dest **31** = provenance inventory **31** data rows. gate `PASS` / `problems []`. `inspectDesignMd` `portable_core: true` / `level: portable-core` / `reasons: []`. SHA-256 `2a5136d75485e0f87c60d9d7230bf5ae96c826a38ec940a0a1ce561cdbca9334`.
