T2 Airbridge 마이그레이션을 시작합니다. 지정된 규격·규칙집·골든 샘플을 먼저 읽고, 그다음 legacy 본문을 Core v2로 옮기겠습니다.규격과 규칙집을 읽었습니다. 이제 대응표, T1 제약, 골든 샘플, 그리고 Airbridge 원문을 이어서 읽겠습니다.이미 `migrated/airbridge/`가 있습니다. 골든 샘플, 원문, 기존 산출물, 그리고 최근 통과본의 형태를 대조하겠습니다.원문을 전부 확보했습니다. 최근 통과본의 상태 표·모션·페르소나 처리를 대조해 형태를 맞추겠습니다.게이트 검사와 최근 통과본의 파비콘·입력 처리, 그리고 원문의 고유 값 위치를 확인합니다.원문 값과 통과본 형태를 맞춰 DESIGN.md를 작성합니다. 이어서 provenance와, grep 대조 뒤 migration-log를 쓰겠습니다.본문을 처음부터 다시 읽어 B2a를 점검하고, 동시에 provenance에 넣을 원장 값을 원문에서 대조합니다.B2a 한정을 보강한 뒤 provenance 원장을 작성합니다.E2 대조를 위해 원문 값과 산출물의 실제 위치를 grep으로 확인합니다.게이트가 로그 파일을 요구하므로, grep으로 확인한 목적지를 기준으로 migration-log를 작성합니다.Airbridge legacy `DESIGN.md`를 Core v2로 옮겼습니다. 산출물은 `docs/design-md-weight/migrated/airbridge/`입니다.

규칙집 **v7**을 적용했습니다. 검증된 값(unitless line-height `1.0`/`1.33`/`1.54`/`1.5`/`1.43`, primitive type button×3+tab×2+card×2+listItem, YAML `use` 8필드)은 보존하고, 무출처 cubic-bezier 3개(그중 `ease-exit`는 템플릿과 일치)는 provenance omission ledger에만 두었습니다. §13 가상 persona는 본문·sidecar 모두에서 빼었고, 마케팅 CTA의 loading/error/success는 역할이 닫히지 않아 생략했습니다. B3 다섯 증거 종류 게이트는 Foundations Motion에 전문으로 남겼습니다.

F1(B2a 전편 재독)과 F2(grep 대조)를 수행했습니다. `--gate-only` PASS, Core `portable_core: true`, SHA-256 `bdd9b320180af18108339fcf0295a4e3b9f8068dc0b5fd98d688613a7f065858`. F3 신선 세션 감사와 sol 의미 레인은 대기입니다. 카탈로그 채택은 아닙니다.

DONE migrated=1
