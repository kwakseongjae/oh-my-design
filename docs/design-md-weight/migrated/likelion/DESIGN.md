# LikeLion Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

LikeLion (멋쟁이사자처럼) is a Korean programming-education brand whose public home surface currently groups course discovery, K-Digital Training bootcamps, AI education, and business education. This contract covers the first-party public course-marketing/catalog capture inspected on 2026-07-13 at `https://likelion.net/`. The public design-system URL `https://designsystem.likelion.net/` is retained as official documentation under the published name LikeLion Design System. The supplied collector run reached its Storybook documentation chrome at `https://designsystem.likelion.net/?path=/docs/intro-introduction--docs` rather than component stories. Homepage measurements describe a public course-marketing/catalog surface. Documentation chrome establishes only that the public documentation route exists; its controls, colors, and loaded font observation do not authorize product or marketing component tokens. Treating `https://likelion.net/` as this contract's token surface, treating the published LikeLion Design System route as official documentation whose captured chrome is not a product-token source, and refusing to fill a generic component library from that chrome, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

The supplied desktop homepage translates that welcoming, action-oriented positioning into a restrained field of `#222222` copy and `#e5e5e5` hairlines, with `#ff6000` concentrated in the search/attention treatment and `#fcf4ee` warming a large promotional tile. Orange `#ff6000` is an observed attention/search color on the captured homepage, not a universal semantic palette. Homepage foreground is `#222222`, with `#a3a3a3` and `#737373` as observed lower-emphasis text. An observed warm `#fcf4ee` promo tile supplies the strongest surfaced color block. The verified geometry is local: a 16px promo tile and a 9999px navigation-account pill. The observed marketing computed face is unresolved; a loaded Pretendard observation on documentation chrome is not promoted to a homepage UI-family token. Readings of that captured layer as a restrained field, of `#ff6000` as concentrated rather than as a filled orange CTA system, and of the documentation-chrome Pretendard load as not a homepage UI-family token, are a derived editorial implementation inference from the verified surfaces; they are not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

Brand narrative recorded by the source, kept separate from the interface evidence above. LikeLion's official history page traces the brand to 2013 and the “HACK YOUR LIFE!” idea of turning an individual’s idea into something they can make; it describes an online/offline programming-education brand that began around the value of realizing one’s own idea, then a move into a commercial corporation and additional education initiatives. The current public home surface shows the present offer as a mix of courses, bootcamps, AI education, and business education. The current business page frames learning around practical problems, mentoring, and shared experience, and describes AX learning as problem-led, mentored, and collaborative. The orange-and-warm-neutral homepage is a separately observed public course-marketing expression of that educational role. It should not be read as proof of a single interface across the learner product, business program, or documentation site. Treating that 2013 origin, the “HACK YOUR LIFE!” through-line, the commercial-corporation move, the current course mix, and AX learning as official educational-context facts that do not by themselves supply interface tokens, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a captured homepage surface or control, and not taking them from the source's persona section, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

- Discover public courses, K-Digital Training bootcamps, AI education, and business education on `https://likelion.net/`.
- Search courses with the captured course-search input.
- Use the captured 로그인/회원가입 control.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source's own §13 header states that first-party sources in this pass establish learner and organisation-facing education contexts, but do not supply enough persona research to support invented demographic archetypes, so those unfinished persona slots are dropped rather than promoted, and no demographic segment list is carried into this document or its sidecar. Dropping those unfinished persona slots rather than promoting them, carrying no demographic segment list, and refusing to treat the dropped slots as this product's audience, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

### Distinctive traits

The list restates measured values from the source. Classifying the list as that restatement, and the groupings inside it, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

- Orange `#ff6000` is an observed attention/search color on the captured homepage, not a universal semantic palette
- Homepage foreground `#222222`, with `#a3a3a3` and `#737373` as observed lower-emphasis text
- Warm `#fcf4ee` promo tile as the strongest surfaced color block
- Local geometry: 16px promo tile and 9999px navigation-account pill
- Marketing computed face unresolved; loaded Pretendard on documentation chrome is not a homepage UI-family token

### Principles

These 4 items are a derived editorial implementation inference from the verified surfaces; they are not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation. The numbered stems rest on official history and business-education material recorded by the source. Every *UI implication* below is the source's own editorial reading, not taken from the published LikeLion Design System documentation.

1. **Make ideas buildable.** The official history frames programming as a means to realize an individual idea. *UI implication:* describe a learning path in concrete terms; do not manufacture product features or outcomes.
2. **Start from practical problems.** The business surface says its AI learning begins with members’ work problems. *UI implication:* lead educational copy with the task or problem being addressed.
3. **Learn through repetition and sharing.** The published process names repeated problem solving and experience sharing. *UI implication:* where learning progress is evidenced, make the next activity and shared context legible.
4. **Keep domains honest.** Homepage, business education, and documentation serve different contexts. *UI implication:* do not carry a token or component from one domain into another without direct evidence.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

- Keep the observed homepage scope separate from documentation chrome and official narrative sources.
- Use `#ff6000` only for the observed attention/search treatment unless another component has its own selector-backed evidence.
- Preserve the warm promo tile’s `#fcf4ee`, 16px radius, and 40px padding only in its captured homepage context.
- Treat `Pretendard Variable` on the homepage as unresolved until exact loaded-face/source corroboration exists.

### Avoid

The source states these four as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

- Do not turn the public Storybook’s documentation controls into LikeLion product components.
- Do not recreate historical ActionButton, chip, tab, tag, toast, dialog, hover, disabled, or error variants without current selector and state provenance.
- Do not label Gotham, HeirofLight, Noto Sans Mono, Nunito Sans, or slick as a live UI family.
- Do not derive a semantic palette, breakpoint, or motion system from this two-surface desktop bundle.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Token-set keys follow the YAML paths. Pairing each hex to the token-set path named beside it, keeping `tokens.colors.primary` `#ff6000` as the observed attention/search color rather than as a filled orange CTA, keeping `tokens.colors.promo` `#fcf4ee` unmerged from any canvas claim, keeping `tokens.colors.hairline` `#e5e5e5` unmerged from `tokens.colors.nav-border` `#d4d4d4`, keeping `tokens.colors.muted` `#a3a3a3` unmerged from `tokens.colors.muted-secondary` `#737373`, and not marketing documentation-chrome values `#6b7583`, `#4e5967`, `#f3f4f6`, and `#e5e7ea` as LikeLion product tokens, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

- **Attention / search** (`#ff6000`): `tokens.colors.primary`. Observed as the course-search input foreground at `home::[data-omd-capture="7"]`. Catalog identity `primary_color` is `#ff6000`.
- **Primary foreground** (`#222222`): `tokens.colors.foreground`. Repeated homepage text color, including the warm promo tile.
- **Muted text** (`#a3a3a3`): `tokens.colors.muted`. Repeated lower-emphasis homepage text.
- **Secondary muted text** (`#737373`): `tokens.colors.muted-secondary`. Observed on the homepage search-input border sample and other low-emphasis text.
- **Hairline** (`#e5e5e5`): `tokens.colors.hairline`. Repeated homepage border color in the collector output.
- **Warm promo surface** (`#fcf4ee`): `tokens.colors.promo`. `home::[data-omd-capture="13"]` background.
- **Account-pill border** (`#d4d4d4`): `tokens.colors.nav-border`. Observed on the navigation account control.

The supplied homepage evidence does not establish a filled orange CTA, orange hover color, semantic success/error palette, a neutral scale, or a general canvas token. Documentation-chrome values such as `#6b7583`, `#4e5967`, `#f3f4f6`, and `#e5e7ea` are not marketed as LikeLion product tokens here.

### Spacing

YAML `tokens.spacing` is empty (`{}`). The source records no spacing scale. Local captured geometry that is not a spacing scale: the course-search input rendered 731px wide; the promo tile rendered 310px high with 40px padding; the account pill rendered 43px high with padding `10px 16px`. Treating that empty YAML object as no spacing scale, and keeping 40px / `10px 16px` as those components' padding rather than as steps of a missing scale, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

### Shape

- YAML `tokens.rounded.promo: 16` — promotional tile corners (16px)
- Account-pill radius `9999px` is the component field `tokens.components.account-pill.radius`, not a YAML `tokens.rounded` key

Reading 16 and 9999px as local harvested geometry for those observed controls, not a universal radius for every unlisted LikeLion surface, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

### Elevation

YAML `tokens.shadow.none: "none"`. Representative homepage and documentation-chrome samples report `box-shadow: none`. The observed promo tile is distinguished by `#fcf4ee`, type, and a 16px radius rather than an elevation value. No global shadow scale or overlay treatment is established. Reading those samples as a flat treatment for the observed elements only, and not as a global shadow scale, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

### Motion

No motion, transition, or user-flow state was supplied for the homepage. Do not promote a motion duration, easing, animation name, transition, or reduced-motion behavior until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists. Treating that measured absence as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior, and requiring a per-component computed observation of all five kinds before any promotion, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, treating homepage `Pretendard Variable` as an unresolved marketing computed face rather than a UI-family token, treating loaded Pretendard on documentation chrome as documentation-chrome observation only, treating declared-only faces as omitted, and refusing to substitute a system font while calling it Pretendard, are derived editorial implementation inferences from the verified surfaces; they are not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | No LikeLion-authored font licence or official announcement establishing a distinct product font was found in the requested source search. The published LikeLion Design System route is official documentation; this capture did not take a universal current typography token from it. Classifying official product-use as unresolved on that search, and refusing to take a universal current typography token from the published documentation route, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation. |
| Live computed surface-use — unresolved | The homepage computes `Pretendard Variable` on 193 visible captured elements. The collector found no matching loaded FontFaceSet record or source URL for that exact family, so it is not a `tokens.typography.family` token. |
| Documentation-chrome computed use — loaded | The Storybook documentation route computes `pretendard` / `Pretendard` on 34 visible elements; it has loaded FontFaceSet corroboration and 22 source URLs, including LikeLion-hosted static Pretendard files and the Pretendard CDN. This verifies the documentation-chrome observation only, not course-product use. |
| Official distributed brand asset | LikeLion-hosted Pretendard files are recorded on documentation chrome. This proves delivery on that captured documentation route, not a homepage UI-family token and not ownership of a proprietary LikeLion typeface. Reading those hosted files as proof of delivery on the captured documentation route rather than as a homepage UI-family token or as ownership of a proprietary LikeLion typeface, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation. |
| Declared-only | Gotham, HeirofLight, Noto Sans Mono, Nunito Sans, and slick have declared font-face assets with zero visible captured use. They are not family tokens or specimens. |
| License | The upstream Pretendard project publishes its files under SIL Open Font License 1.1. That licence describes Pretendard’s upstream distribution; it does not establish that the declared Gotham, HeirofLight, or Noto Sans Mono files are reusable assets. |
| System fallback | The homepage’s fallback list is a fallback list, not evidence for a substitute branded family. |

### Family

- **Homepage UI family:** unresolved. YAML assigns no `tokens.typography.family` token. The computed marketing face is `Pretendard Variable`.
- **Documentation-chrome visible family:** `pretendard` / `Pretendard` — loaded on the Storybook documentation route only.
- Do not substitute a system font and call it Pretendard. The exact marketing computed family remains unresolved until matching FontFaceSet/source evidence is captured on that surface. Reading computed visible use without a matching FontFaceSet as the reason no homepage UI-family token is assigned, and refusing to extend the documentation-chrome Pretendard load to the homepage, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

### Type roles

Line heights stay in the form the source verified them: YAML unitless `1.5` and `1.2` beside the source's own 24px / 48px / 24px writings where both exist; course-card title 30px stays 30px and is not converted into a ratio. Keeping those YAML unitless ratios beside the source's own px equivalents, keeping the course-card title as a §3 hierarchy row that is not a YAML typography key, and keeping each YAML `use` string beside the hierarchy-table note without replacing either, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

| Role | Size | Weight | Line height | Notes |
|---|---:|---:|---:|---|
| Default body | 16px | 400 | 1.5 (24px) | `home::body`. YAML use: Observed home body copy; the computed Pretendard Variable face is unresolved, so no UI-family token is assigned. |
| Course-section heading | 32px | 700 | 1.5 (48px) | Homepage `h3` samples. YAML use: Observed home course-section heading. |
| Course-search input | 20px | 600 | 1.2 (24px) | `home::[data-omd-capture="7"]`. YAML use: Observed home course-search input. |
| Course-card title | 20px | 600 | 30px | Captured homepage card text. Not a YAML `tokens.typography` key. |

### Assets

Catalog identity points at `logo.type: favicon`, slug `https://likelion.net/img/favicon.png`. That pointer is a first-party favicon file on the captured homepage host. Treating it as identity metadata for the captured homepage, not as a mark taken from the published LikeLion Design System documentation, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

All component observations below are scoped to the supplied `home` desktop capture. The bundle records one documentation form-error interaction and no homepage interaction flow; focus/pressed rows on the course search are pseudo-state samples, not a general product-state contract. Documentation-chrome controls are excluded.

No course-product empty, loading, success, disabled, or error state was captured. The only recorded interaction is a form-error sample on documentation chrome, which is not promoted to the course surface.

| Category | Evidence status |
|---|---|
| Empty | No captured empty-state treatment on the public course surface. |
| Loading | No captured loading treatment on the public course surface. |
| Error | No captured error treatment on the public course surface. Documentation-chrome form-error is not promoted here. |
| Success | No captured success treatment on the public course surface. |
| Skeleton | No captured skeleton treatment on the public course surface. |
| Disabled | No captured disabled treatment on the public course surface. |

No ActionButton, TextField, chip, tab, tag, toast, dialog, card, or responsive variant is specified: the supplied design-system route is documentation chrome rather than a component-story capture, and the homepage does not provide selector-backed observations for those variants.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover applies wherever a pointer control exists. Loading, error, and success follow the control’s product role, not its primitive kind. Where a state applies by role and no treatment was observed, the state stays applicable and only its visual treatment is omitted. Absence of a capture is not a `not-applicable` reason. The source records no `focus-visible` capture; no `focus-visible` row carries a treatment. The generic observed-state name `focus` on the course-search input (`#2563eb` border-color while border width remains `0px`) is a captured pseudo-state sample, not a `focus-visible` treatment. Every interactive-kind verdict, every applicability verdict, and the reason given for either — including keeping each YAML `use` string as a Token-set use row beside Role, keeping YAML font, padding, radius, border, and states byte forms beside the §4 writings, treating the promo tile as a clickable destination tile, treating the account pill as a 로그인/회원가입 destination, and treating course-search as a query field — is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation. This is not a complete state-coverage claim.

### Promotional tile

- Role: clickable warm promotional tile on the captured homepage
- Token-set use: home::[data-omd-capture="13"] clickable warm promotional tile; 310px rendered height.
- Token-set states: default observed only
- Primitive type: `button` · Kind: interactive
- Anatomy: labelled promotional surface
- Background: `#fcf4ee`
- Text: `#222222`
- Radius: 16px. YAML radius: `16px`
- Padding: 40px. YAML padding: `40px`
- Height: 310px
- Font: 16px / 400 / unresolved computed stack. YAML font: `16px / 400 / unresolved computed stack`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the homepage promotional tile |
| hover | applicable | Pointer-web clickable tile; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination tile can be unavailable; visual treatment omitted |
| loading | not-applicable | This clickable promotional tile presents a destination; it does not commit an operation whose pending result this tile would report. |
| error | not-applicable | Same role reason: choosing a promotional destination is not an operation with an error result this tile would report. |
| success | not-applicable | Same role reason: choosing a promotional destination is not an operation with a success result. |

### Navigation account control

- Role: 로그인/회원가입 control
- Token-set use: home::[data-omd-capture="6"] 로그인/회원가입 control; 43px rendered height; low-confidence collector classification.
- Token-set states: default observed only
- Primitive type: `button` · Kind: interactive
- Anatomy: outlined pill
- Text: `#222222`
- Border: `1px solid #d4d4d4`. YAML border: `1px solid #d4d4d4`
- Radius: 9999px. YAML radius: `9999px`
- Padding: 10px 16px. YAML padding: `10px 16px`
- Height: 43px
- Font: 16px / 400 / unresolved computed stack. YAML font: `16px / 400 / unresolved computed stack`
- Observed: default only. The collector’s component classification is low confidence, so no further variant is inferred.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the 로그인/회원가입 control |
| hover | applicable | Pointer-web outlined pill; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A destination pill can be unavailable; visual treatment omitted |
| loading | not-applicable | This 로그인/회원가입 control presents login/sign-up as a destination; it does not commit an operation whose pending result this pill would report. |
| error | not-applicable | Same role reason: choosing that destination is not an operation with an error result this pill would report. |
| success | not-applicable | Same role reason: choosing that destination is not an operation with a success result. |

### Course search

- Role: course-search input
- Token-set use: home::[data-omd-capture="7"] course-search input; no complete interaction or result state was captured.
- Token-set states: default plus focus and pressed pseudo-state samples only
- Primitive type: `input` · Kind: interactive
- Anatomy: value field
- Text: `#ff6000`
- Font: 20px / 600 / unresolved computed stack. YAML font: `20px / 600 / unresolved computed stack`
- Observed Focus (pseudo-state sample, not a `focus-visible` treatment): border-color `#2563eb` in the captured pseudo-state sample while border width remains `0px`
- Observed Pressed: captured pseudo-state sample only; no separate color value
- Observed: default plus those two pseudo-state samples. No result list, validation, disabled, or complete focus-flow state is specified.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the homepage course-search input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted. The captured generic-focus sample is not this row. |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| loading | not-applicable | This course-search input presents a query field; it does not commit an operation whose pending result this input would report. |
| error | applicable | Form field; no validation treatment observed |
| success | not-applicable | Completing a query is not a success result this input would report; no result-list state is specified. |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The supplied home surface is a 1440×900 desktop capture. Its reliable layout observations are local: the course-search input rendered 731px wide, the promo tile rendered 310px high with 40px padding, and the account pill rendered 43px high. No public mobile viewport, course-grid rule, carousel behavior, sticky header behavior, or breakpoint was captured, so none is specified. Reading those measurements as local captured geometry rather than as a complete grid declaration, and refusing to average the promo tile and account-pill geometries, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

### Responsive behavior

No mobile viewport or responsive transition was captured. Touch-target, grid, menu-collapse, carousel-scroll, and breakpoint rules require a public responsive observation before they can be specified. Reading 1440×900 as the capture viewport rather than a cross-viewport specification is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

<!-- design-md:section content-locales -->
## 6. Content & Locales

LikeLion’s official materials are practical, encouraging, and learning-oriented. The history page’s “HACK YOUR LIFE!” framing pairs self-directed making with programming education; the current business page describes learning through repeated problem solving, experience sharing, and a shift from AI as a tool to AI as a collaborator. These are brand and educational-context sources, not homepage microcopy rules. Characterizing those official materials as practical, encouraging, and learning-oriented implementation context rather than as a homepage microcopy guide, requiring the quoted strings below byte-exact, and treating English beside a Korean label as a reading aid rather than a replacement, is a derived editorial implementation inference from the verified surfaces; it is not LikeLion-authored or taken from a separately published UI specification, including the published LikeLion Design System documentation.

| Do | Don't |
|---|---|
| State the learner’s practical next step plainly. | Attribute unobserved error, enrollment, or support copy to the product. |
| Connect technical learning to a problem the learner or team can work on. | Convert corporate-training language into a visual token. |
| Keep the distinction between public course, business, and documentation surfaces. | Invent urgency, guarantees, or personal outcomes. |

Source-grounded samples.

- “HACK YOUR LIFE!” — official history-page slogan.
- “문제 해결 반복 및 경험 공유” — official business-education process label.
- “AI = 동료” — official business-education process label.

Reproduce those three strings byte-exact rather than translating or re-casing them.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- filled orange CTA, orange hover color, semantic success/error palette, a neutral scale, and a general canvas token
- hover, pressed, disabled, loading, error, success, empty, skeleton, and form-error visual treatments on the course surface
- ActionButton, TextField, chip, tab, tag, toast, dialog, card, and responsive variants
- public mobile viewport, course-grid rule, carousel behavior, sticky header behavior, and breakpoint
- touch-target, grid, menu-collapse, carousel-scroll, and responsive-transition rules
- duration, easing curve, animation name, transition, and reduced-motion behavior — promote a motion value for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed
- a matching FontFaceSet/source record for homepage `Pretendard Variable`
- Gotham, HeirofLight, Noto Sans Mono, Nunito Sans, and slick as live UI families
- homepage microcopy, error copy, enrollment copy, and support copy
