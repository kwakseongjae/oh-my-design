T2 마이그레이션을 시작하기 전에 규칙집·골든 샘플·레거시 본문을 순서대로 읽고, 세션 상태도 복원하겠습니다.세션은 복원됐고, 지정된 규격·규칙집·골든 샘플을 순서대로 읽겠습니다.골든 샘플과 42dot 원문, 최근 통과 이관본을 이어서 대조합니다.이미 있는 42dot 이관본과 승인된 musinsa 골든, 최근 통과 산출물을 대조합니다.원문의 검증 주석·충돌값과 최근 통과 이관본의 형식을 확인합니다.게이트가 hex·px·rem·ms·% 손실을 기계 차단하므로, 원문의 토큰 가방을 먼저 추출합니다.토큰 가방과 C2·B2a·B3 제약을 맞춰 본문을 작성합니다. 먼저 `DESIGN.md`를 쓰고, 이어서 provenance와 로그를 grep으로 대조합니다.본문을 처음부터 다시 읽어 B2a 누락을 찾고, 이어서 provenance를 작성합니다.패스 2: 로그에 적을 값마다 실제 파일 위치를 grep으로 확인합니다.42dot legacy `DESIGN.md`를 Core v2로 옮겼습니다. 산출물은 `docs/design-md-weight/migrated/42dot/`의 `DESIGN.md`, `provenance.md`, `migration-log.md`입니다. 규칙집 **v7**.

기계 검증: `--gate-only` **PASS** (problems 0), `inspectDesignMd` / `migrate-design-md-core.cjs --require-portable-core` **exit 0**, `portable_core: true`. F3 감사는 부모 세션. 카탈로그 채택은 아닙니다.

의미 쪽만 짧게 적으면 다음과 같습니다.

- **범위**는 홈(`https://42dot.ai/`)과 블로그(`https://42dot.ai/blog`) 두 표면. 2019 설립·2022 현대차그룹 인수는 공개 사실로 남기고 토큰으로 올리지 않았습니다.
- **§13** 가상 페르소나(정민호, Sarah Kim, 이수현, David Park)는 삭제했고 provenance에도 재수록하지 않았습니다. Primary tasks 3개는 히어로 읽기 / 카드 스캔 / 태그 필터입니다.
- **무출처 커브 3개**만 뺐습니다. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)`는 레거시 템플릿과 같고, 문자열은 provenance omission ledger에 있습니다. 120ms / 240ms / 400ms와 `ease-*` 이름·용도는 본문에 남겼고, B3 다섯 종류 게이트도 Foundations에 전문으로 있습니다.
- **C4**: Active Tag Pill, Blog Card, Nav Dropdown은 kind/map을 생략했습니다. Tag Filter(탭 선택), Nav Item, Carousel(prev/next)은 역할로 loading/error/success를 닫았습니다.
- **A3**: §9에만 있던 tag-filter `transparent` 배경을 Inactive Tag Filter로 옮겼습니다. YAML `1px solid #737d8c`와 라이브 `rgba(115,125,140,0.3)` 충돌은 양쪽 다 남겼습니다.

DONE migrated=1
