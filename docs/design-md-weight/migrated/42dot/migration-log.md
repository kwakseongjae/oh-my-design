# 42dot migration log

Source: `web/references/42dot/DESIGN.md`
Destination: `docs/design-md-weight/migrated/42dot/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/42dot/provenance.md`
Date: 2026-08-24
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: worker inspectDesignMd pass (`portable_core: true`, placeholders 0) is pre-F3. `--gate-only` PASS / problems 0 was a pre-F3 worker result. F3 body edits (this audit session) mean this log does not re-assert that result or a SHA as the current file (E2c).

### Revision 2026-08-24 (F3 B2a·E2 audit)

Fresh-session audit attached remaining adjacent complete B2a and aligned destinations with grep. See `audit-log.md`. Token values, component tables, state applicability, and section structure were not changed.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, display_name_kr, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage URL 옮김 → Experience `scope`; `display_name_kr` 포티투닷 옮김 → Experience Scope; favicon `https://42dot.ai/icon.png` 옮김 → Typography & Assets; `primary_color` 옮김 → Distinctive + Foundations + Scope + Principles + capture-bound hex | Portable file has no frontmatter. Name kept as H1 `42dot Design System`. Catalog homepage `https://42dot.ai/` is dual-destination: Experience Scope + provenance identity/surfaces/sources/Tier 1 (E2a). Captured blog `https://42dot.ai/blog` is a distinct URL, also dual Experience Scope + provenance identity/surfaces/sources/Tier 1. Catalog `primary_color` `#786efa` is provenance identity + Distinctive periwinkle hex + Foundations Periwinkle Violet + Scope accent-signal hex + Principles “One accent, one meaning” + capture-bound reserve-violet (E2a). Active Tag Pill `Background: #786efa` is the tag-active field, not a general Ink (A4). Favicon `https://42dot.ai/icon.png` is dual-destination: provenance identity AND portable Typography & Assets (E2a). `display_name_kr` 포티투닷 is dual Scope + provenance identity (E2a). |
| YAML `omd`, `verified`, `added`, token claims, `tokens.source` / `extracted` / `note`, `components_harvested` | 분리 → provenance; `tokens.source` 옮김 → Font evidence; `tokens.note` 옮김 → Distinctive / Foundations; homepage / blog URL 옮김 → Experience `scope` | 출처 원장·freshness·Proof. `tokens.source: live-extract` is dual-destination: provenance identity/Proof AND portable Font evidence live-computed row (E2a). `components_harvested: true` is provenance metadata only (A1c). Token note (monochrome + `#786efa` reserved for active tags; `#32353f` / `#282b32` chrome; `#737d8c` slate; AstaSans / Noto Sans KR) destinations: provenance identity + portable Scope accent-signal + Distinctive + Foundations Semantic color + Typography Family (AstaSans live / Noto declared-only) (E2a). Footer `Verified: 2026-07-02 (omd:add-reference CREATE — Tier 1 live inspect, 2 surfaces)` keeps that mark in provenance Freshness (A1c). |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. `#786efa` 액센트와 tag-active `bg`를 일반 Ink로 합치지 않음. `#ffffff` canvas와 on-dark를 한 잉크로 뭉개지 않음 (A4). YAML `tokens.shadow.dropdown` `rgba(0,0,0,0.2) 0px 4px 10px` is Foundations Elevation + Nav Dropdown Panel Shadow + Scope geometry mention + provenance claim ledger / Proof notes (E2a). YAML unitless `lineHeight` 1.45 / 1.60 / 1.50 / 1.40은 비율로 보존하고 본문 표의 고정 px 관측(55.68px / 56.83px / 41.76px / 38.40px)과 합치지 않음 (A1a). Nav `0.89rem`과 Body `0.88rem`을 합치지 않음. tag-active / tag-filter의 검증된 `Type: badge`, blog-card / nav-dropdown의 `Type: card`, nav-item의 `Type: tab`, carousel-button의 `Type: button`은 `Kind: interactive`로 뭉개지 않고 컴포넌트별로 보존 (A1b). |
| YAML vs HTML inspect rounding (22px/22.4px, 6px/5.6px, 11px/11.2px, 36px/35.52px, 26px/25.6px, 29px/28.8px, 9999px/50%) | 옮김 → Type roles / Font evidence / Shape / Components + provenance Conflicts | Dual destination (E2a): portable body keeps both sides; provenance Conflicts ledger records the pairs. Neither side is chosen. `11.2px` / `35.52px` / `25.6px` / `28.8px` also Font evidence live-computed. Carousel `50%` radius is not Nav Item `50%` white. |
| Inactive tag-filter border YAML/§4 `1px solid #737d8c` vs HTML inspect `1px solid rgba(115,125,140,0.3)` | 옮김 → Components Inactive Tag Filter + provenance Conflicts | Dual destination (E2a). Both values kept. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 두 public 표면, `#ffffff`/`#fbfbfb` 캔버스, `#786efa` 액센트, AstaSans, 0px 카드, 22px/9999px 예외. Adjacent complete B2a on: visual-character / engineering-lab; accent-signal / violet-does-a-lot-of-work; geometry-affordance / roundness-as-interactive (B2/B2a). Distinctive trait readings have their own adjacent complete B2a. Coverage URLs stay class 1. Homepage hero / blog-card / tag-filter evidence also Primary tasks (E2a). `precision-over-decoration` is not a portable sentence. Homepage `#786efa` ×16 and blog `#786efa` ×16 are both preserved. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `#786efa` `#000000` `#737d8c` `#32353f` `#282b32` `#ffffff` `#fbfbfb` `#f6f6f9`. Semantic-role / character readings and unpromoted-role / transparent-not-canvas readings have adjacent complete B2a (B2/B2a). Transparent background destinations (E2a, A3): Foundations unpromoted-role + Inactive Tag Filter + provenance identity/Proof (claim ledger `tokens.components.tag-filter` + Proof notes). |
| §3 Typography Rules | 옮김 → Typography & Assets + Avoid | 증거 등급, AstaSans live-extract, Noto Sans KR declared-only, 역할 메트릭. Family/fallback is Typography + Avoid “Do not present a system fallback as AstaSans” (E2a). AstaSans live use and Noto declared-only are separate evidence classes. No redistribution-right or third-party reproduction negative was kept. `tokens.source: live-extract` is Font evidence live-computed AND provenance identity/Proof (E2a). YAML unitless line-height와 본문 표 computed px는 별 값으로 유지 (A1a). `11.2px` / `35.52px` / `25.6px` / `28.8px` also Type roles / Components where listed + provenance Conflicts. Evidence-class application, Family font-use boundary, hierarchy-signal readings have adjacent complete B2a (B2/B2a). Assets favicon identity-not-wordmark and first-party imagery not-invented-decoration share one adjacent complete B2a. |
| §4 Component Stylings | 옮김 → Components & States + Primary tasks | Active Tag Pill, Inactive Tag Filter, Blog / Research Card, Nav Dropdown Panel, Nav Item, Carousel Control. 검증된 primitive type 보존. Inactive Tag Filter `Background: transparent` 유지 (A3). Homepage card/tag and blog filter evidence also Primary tasks (E2a).
| §4 Active Tag Pill / Blog / Research Card / Nav Dropdown Panel | 옮김 → Components & States + Named gaps | default 기하. interactive-kind 근거 없음 → kind와 applicability map 생략 (C4). Dual destination (E2a): Components kind-omitted entries + Governance Named gaps (`Active Tag Pill interactive kind`; `Blog / Research Card interactive kind`; `Nav Dropdown Panel interactive kind`). |
| §5 Layout Principles | 옮김 → Layout & Platforms, Foundations spacing/shape + Primary tasks | full-bleed hero then light bands, horizontal card grid, Tags/Posts split, mega-dropdown full-width, 6/12/16/24/48/64, 0/22/9999. Layout-purpose / cinematic-then-calm reading has adjacent complete B2a (B2/B2a). Layout-application (touch-target purpose / collapsing as packet rows / image-behavior legibility) has adjacent complete B2a. Source-stated breakpoint/measurement rows kept; no new desktop/mobile/cross-viewport verdict. Spacing local-scale / ~8px and Shape local-geometry have adjacent complete B2a. Hero/card/filter layout also Primary tasks (E2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation + Components Nav Dropdown Panel + Scope geometry | 4-level table, `box-shadow: none` on hero/headings/cards/tags, dropdown `rgba(0,0,0,0.2) 0px 4px 10px`. Destinations (E2a): Foundations Elevation + Nav Dropdown Panel Shadow + Scope geometry mention + provenance claim ledger (`tokens.shadow.dropdown`) / Proof notes. Elevation philosophy reading has adjacent complete B2a (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. 인접 본문에 별도 complete B2a (`derived editorial implementation inference` / `not 42dot-authored or a separately published UI specification`). Principles 머리의 “These five items” 한정은 번호 원칙만 덮으며 capture-bound의 대체물이 아님. Governance 통제 문구에 넣지 않음. |
| §7 Don'ts | 옮김 → Experience avoid | Dual destination (E2a): Avoid. Avoid 머리 인접 완전 B2a (B2/B2a). Bounce/spring Don't is Avoid AND Foundations Motion “No bounce” (E2a; source rows are §7 Don'ts and §15). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | Mobile `<640px` / Tablet 640-1024px / Desktop 1024-1440px, 44-48px-tall header, ~24px tags, ~45px carousel, collapsing strategy, image crop. Source-stated breakpoint/touch-target/collapsing/image-behavior rows kept. Touch-target purpose / collapsing packet-rows / image-behavior legibility have adjacent complete B2a (B2/B2a). No new desktop getComputedStyle or mobile/cross-viewport coverage verdict. |
| §9 Agent Prompt Guide | 삭제; 표에만 있던 transparent 옮김 → Components Inactive Tag Filter + Foundations unpromoted-role + provenance identity/Proof | 도구별 명령·복붙 프롬프트. 색·카드·nav 수치는 이미 Foundations/Components에 있음. 고유 값만 옮김 (A3): tag-filter `transparent background` (YAML에 `bg` 없음; §9 프롬프트와 HTML inspect에만 있음). Destinations (E2a): Inactive Tag Filter + Foundations unpromoted-role + provenance identity/Proof (claim ledger `tokens.components.tag-filter` + Proof notes). 슬롯 없는 위임 없음. |
| §10 Voice & Tone | 옮김 → Content & Locales | 표 5행 + verbatim 3줄 + forbidden register. 합성 보이스 샘플 없음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). Voice / tone-table / application, Forbidden-register list, and no-synthetic-voice readings have adjacent complete B2a (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience `scope` | 2019 Song Chang-hyun, 2022 Hyundai Motor Group, AKit / SDV / 42dot LLM / TAP!, Douglas Adams "42". 공개 사실 문장(verbatim taglines / widely documented public facts, not directly quoted)은 Scope 본문에 유지. 그 문장을 토큰이 아니라고 적용하는 판단은 인접 완전 B2a. 연표식 원장은 provenance Proof notes. Design-refusal reading has adjacent complete B2a (B2/B2a). |
| §12 Principles | 옮김 → Experience principles | 다섯 원칙과 UI implication. 인접 본문에 derived editorial implementation inference / not 42dot-authored or a separately published UI specification 한정을 둠. 그 한정은 번호 다섯 항목만 덮는다 (B2/B2a). Capture-bound application은 별도 인접 완전 B2a. Governance 일반 문구는 그 한정의 대체물이 아님 (E1, E2c). |
| §13 Personas | mixed: exclusion boundary 옮김 → Experience Audience; fictional names·ages·cities·biographies 삭제; sidecar 재수록 없음 | 원본이 fictional archetypes라고 명시. portable Audience는 “Source §13’s named fictional archetypes are not Audience and are not primary tasks” exclusion boundary를 유지. 정민호 / Sarah Kim / 이수현 / David Park 이름·나이·도시·biography는 삭제. sidecar 재수록 없음 (D2). Independently verified Primary tasks 3건은 §13이 아니라 homepage hero / blog-card scan / blog tag filter (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability + Named gaps | Form-validation color (warm error tone, no hex) destinations (E2a): capture record + Governance Named gaps + provenance Proof notes. Hover/pressed/focus-visible treatments: capture record + Named gaps. 본문 보존: Empty×2, Loading×2, Error×2, Success, Skeleton, Disabled (A2). 선언 컴포넌트는 §4.4 7상태 표를 닫되 미관측 시각값은 발명하지 않음. Active Tag Pill / Blog Card / Nav Dropdown는 kind/map 생략이라 C2 표를 닫지 않음 (C4). `not captured`를 `not-applicable` 사유로 쓰지 않음 (C1). Inactive Tag Filter (blog tag filter) and Carousel Control (prev/next paging) keep identified-role loading·error·success maps (C2). Nav Item omits loading·error·success: exact destination versus dropdown-trigger unresolved. generic `Focus`는 `focus-visible` treatment로 승격하지 않음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). Capture-record characterizations have adjacent complete B2a. |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브는 provenance omission ledger; 생략 사실은 Named gaps에도 | Duration 3개(120ms / 240ms / 400ms)와 signature motion 5개(dropdown, carousel, hero cross-fade, no bounce, reduce motion)는 본문 보존. No-bounce is Foundations Motion + Avoid (E2a). Token names `ease-enter` / `ease-exit` / `ease-standard`와 use는 source-stated/uncomputed로 본문 보존. 무출처 cubic-bezier 3개만 삭제 — `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)`은 legacy 규격 템플릿 `ease-exit`와 동일. 삭제한 커브 문자열은 provenance omission ledger에 보관 (E2b). Dual destination (E2a): Foundations Motion + Governance Named gaps (cubic-bezier; animation name / transition properties / five-kind gate) + omission ledger. Character/purpose 읽기는 인접 완전 B2a. Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 curve/animation-name/transition-property를 승격한다는 게이트를 전문 명시. 단일 curve/duration 확인은 그 게이트가 아님 (B3, E2c). Named gaps는 그 게이트를 짧게 가리키며 B3 전문의 대체물이 아님. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance; Tier 1 public surface URLs 옮김 → Experience `scope` | freshness·원장. Footer `Verified: 2026-07-02 (omd:add-reference CREATE — Tier 1 live inspect, 2 surfaces)` keeps that mark in provenance Freshness (A1c). Homepage / blog URLs are dual Experience Scope + provenance identity/surfaces/sources/Tier 1 (E2a). |
| HTML comment OmD v0.1 Sources | 분리 → provenance; live computed 수치 옮김 → Type roles / Components / Conflicts; 해석 한정은 portable 본문 인접에도 | freshness, playwright getComputedStyle, rgb notes, fictional personas, editorial readings. 해석 한정은 portable Principles와 Principles 밖 retained editorial block 인접에도 남김 (B2/B2a). |

### F1 / F2 (v7 mandatory final passes)

- **F1 B2a scan (full DESIGN.md reread):** Adjacent complete B2a (`derived editorial implementation inference` / `not 42dot-authored or a separately published UI specification`) on: Scope visual-character / engineering-lab; Scope accent-signal / violet-does-a-lot-of-work; Scope geometry-affordance / roundness-as-interactive; Scope design-refusal; Scope public-fact-not-token application; Audience task-context application; Distinctive trait readings; five numbered Principles (`These five items` only); capture-bound application list (separate limiter); Avoid Don'ts; Semantic-role and character readings; Semantic unpromoted-role / transparent-not-canvas; Spacing local-scale / ~8px; Shape local-geometry; Elevation philosophy; Motion character / no-bounce-steadiness; Font evidence-class application readings; Family font-use boundary; Typography hierarchy-signal / imagery-leads; Assets favicon identity-not-wordmark and first-party imagery not-invented-decoration (one limiter); Capture-record state-contract characterizations; Layout-purpose / cinematic-then-calm; Layout-application (touch-target purpose / collapsing as packet rows / image-behavior legibility); Content voice / tone-table / application; Content Forbidden-register list; Content no-synthetic-voice. Left unqualified as brand-published, observed-technical, public-fact, or Core procedure, not as a reconstruction-boundary exemption: coverage/URLs; Primary tasks; token measurements and component anatomy; Type roles A1a rounding notes; B3 five-kind Motion gate; Core C1/C2/C3 capture-record policy and per-control C2 maps / B1 notes; C4 kind/map omission; Governance controlled copy; Named gaps inventory; founding 2019 / Song / 2022 Hyundai public-fact sentences (source-stated “widely documented public facts, not directly quoted”) except the not-token application. This log does not claim that no unqualified sentence remains.
- **F2 E2 grep (value + field/role context):** `포티투닷` grep-matched Experience Scope AND provenance identity. Favicon `https://42dot.ai/icon.png` grep-matched portable Assets AND provenance identity. Catalog `primary_color` `#786efa` → provenance identity + Distinctive + Foundations Periwinkle Violet + Scope accent-signal + Principles “One accent, one meaning” + capture-bound reserve-violet; Active Tag Pill Background is the tag-active field (A4). `tokens.source` `live-extract` → Font evidence live-computed AND provenance identity/Proof. `(omd:add-reference CREATE — Tier 1 live inspect, 2 surfaces)` grep-matched provenance Freshness. Homepage / blog URLs grep-matched Experience Scope AND provenance identity/surfaces/sources/Tier 1. `Background: transparent` grep-matched Inactive Tag Filter AND Foundations unpromoted-role AND provenance. Border `rgba(115,125,140,0.3)` grep-matched Inactive Tag Filter AND provenance Conflicts. `22.4px` grep-matched Shape + Active Tag Pill + provenance Conflicts. `5.6px` grep-matched Active Tag Pill + provenance Conflicts. `11.2px` / `35.52px` / `25.6px` / `28.8px` grep-matched Font evidence live-computed + Type roles (+ Active Tag Pill for `11.2px`) + provenance Conflicts. Carousel `50%` radius grep-matched Carousel Control + provenance Conflicts; Nav Item `50%` white is a different field. `rgba(0,0,0,0.2) 0px 4px 10px` grep-matched Foundations Elevation + Nav Dropdown Panel Shadow + Scope geometry mention + provenance claim ledger / Proof notes. Homepage `#786efa` ×16 and blog `#786efa` ×16 both preserved. Nav Item loading/error/success omitted. Unitless `1.45` / `1.60` / `1.50` / `1.40` grep to Type roles. `0.89rem` / `0.88rem` / `0.70rem` / `2.40rem` / `2.22rem` / `1.80rem` / `1.60rem` grep to Type roles. `Type: badge` ×2, `Type: card` ×2, `Type: tab`, `Type: button` grep per component. Kind omitted on Active Tag Pill / Blog / Research Card / Nav Dropdown Panel grep-matched Components AND Named gaps. AstaSans live-computed and Noto declared-only are split; no loadable-reproduction Unresolved claim remains. cubic-bezier strings grep-matched provenance omission ledger only, not portable Foundations tables. `120ms` / `240ms` / `400ms` grep-matched Foundations Motion. B3 five-kind list (`transition properties, animation name, duration, easing, and reduced-motion behavior`) greps to Foundations Motion; Named gaps points at that gate in short and is not the B3 전문. `Empty (blog filter` / `오류가 발생했습니다` / `warm error tone` grep to capture record; form-validation color also Named gaps + provenance Proof notes. `정민호` / `Sarah Kim` / `이수현` / `David Park` grep-matched neither portable body nor provenance (D2). §9 construction prompts grep-matched no portable body. This log does not re-assert a post-write Core SHA.

## Revision 2026-08-24 (wave6 sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-wave6-2026-08-24-sol-full.md` §5.

| Item | Correction |
|---|---|
| 1. Surface observations + family evidence | Homepage `#786efa` ×16 and blog `#786efa` ×16 both preserved. Claim ledger splits primary home/blog. AstaSans live computed and Noto Sans KR declared-only are separate; family.sans / kr are not one home live-inspect mapping. |
| 2. D1 font/license | Redistribution-right / third-party reproduction / “officially known but unavailable” negatives removed. |
| 3. Viewport verdicts | Source-stated breakpoint/measurement rows kept. desktop getComputedStyle / newly measured mobile pass / cross-viewport coverage language removed. |
| 4. Nav Item C2 | loading/error/success omitted. Exact destination versus dropdown-trigger unresolved. Not closed as destination-select. |
| 5. Source-row destinations | §3 family/fallback → Typography + Avoid. §15 no-bounce → Motion + Avoid. §1/§4/§5 hero·card·filter evidence also Primary tasks. YAML token note → Scope/Distinctive/Foundations/Typography + provenance. Shadow → Elevation/Nav Panel/Scope + provenance claim/proof. transparent and warm-error-tone include provenance. **[SUPERSEDED 2026-08-24 wave6 ledger sync — this summary did not rewrite original §6 / §2 / §9 / §14 source rows. Original rows now carry the provenance destinations. See Revision wave6 ledger sync.]** |
| 6. E2c | D1/C2/F2 compliance claims lowered to actual body/disposition. **[SUPERSEDED 2026-08-24 wave6 ledger sync — F2 below treated this summary as closing those source-row destinations. See Revision wave6 ledger sync.]** |
| 7. Machine gates | `node test-v2/tools/migrate-reference.mjs --brand 42dot --gate-only` → PASS, problems []. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/42dot/DESIGN.md --check --require-portable-core --json` → exit 0, `portable_core: true`, SHA-256 `a043a85e11dbc8b1e950853ae4fc1deccd2da573fcea89ddc9bbd33af548a9c6`. New F3 not required. |

### F1 / F2 (v7 mandatory final passes; this resubmit)

F1: Layout inspect-notes / cross-viewport verdicts removed from the derived inventory. Layout-application B2a remains without those invented coverage claims.

F2: blog `#786efa` ×16 present; Noto declared-only not mapped as home live-inspect; Nav Item omission sentence present; no-bounce dual Motion+Avoid; shadow also provenance; no loadable-reproduction Named gap. Compliance is not claimed stronger than the body (E2c). **[SUPERSEDED 2026-08-24 wave6 ledger sync — “shadow also provenance” / F2-complete relative to original §6 / §2 / §9 / §14 source rows is stronger than those rows then recorded. See Revision wave6 ledger sync.]**

## Revision 2026-08-24 (wave6 ledger sync)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source remainder: `docs/reviews/t2-1-wave6-2026-08-24-sol-recheck.md` 42dot conditions 1–3 (prior 3, 5, 6). DESIGN.md not edited.

| Item | Correction |
|---|---|
| 1. Audit viewport verdicts | `audit-log.md` classification “Layout capture-local-versus-cross-viewport height reading”, F3 fix 3 “newly measured mobile pass” / height-class limiter, and F3 fix 20 collapsing-strategy-vs-inspect limiter as a current site are superseded. Source-stated breakpoint/measurement rows kept. Current Layout-application B2a does not state a desktop getComputedStyle, newly measured mobile pass, or cross-viewport coverage verdict. No new F3. |
| 2. §6 shadow source row | Original §6 row destinations now: Foundations Elevation + Nav Dropdown Panel Shadow + Scope geometry mention + provenance claim ledger (`tokens.shadow.dropdown`) / Proof notes. Matches `provenance.md` claim ledger and Proof notes. |
| 3. §2 / §9 transparent source rows | Original §2 and §9 rows now record transparent destinations: Foundations unpromoted-role + Inactive Tag Filter + provenance identity/Proof (claim ledger `tokens.components.tag-filter` + Proof notes). Matches `provenance.md` claim ledger and Proof notes. |
| 4. §14 warm-error-tone source row | Original §14 row now records form-validation color (warm error tone, no hex) destinations: capture record + Governance Named gaps + provenance Proof notes. Hover/pressed/focus-visible stay capture record + Named gaps. |
| 5. F2 / E2c | Wave6 sol-resubmit summary table and its F2 paragraph are superseded as a substitute for those original source rows. F2 for this revision is the three greps below only. |

### F1 / F2 (v7; this ledger sync)

F1: unchanged from wave6 sol resubmit. Layout-application B2a remains without inspect-notes / newly-measured-mobile-pass / cross-viewport coverage verdicts. Audit-log records of those verdicts as current sites are superseded.

F2 (this revision; value + field/role context; not a claim that every source-row destination is closed):

- §6 `rgba(0,0,0,0.2) 0px 4px 10px` → Foundations Elevation + Nav Dropdown Panel Shadow + Scope geometry mention + provenance claim ledger / Proof notes
- §2 / §9 `Background: transparent` → Inactive Tag Filter + Foundations unpromoted-role + provenance identity/Proof
- §14 `warm error tone` / form-validation color → capture record + Named gaps + provenance Proof notes

This log does not claim F2 completeness beyond those three greps, and does not re-assert D1/C2/F2 compliance as closed (E2c).
