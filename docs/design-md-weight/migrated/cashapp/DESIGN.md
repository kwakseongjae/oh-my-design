# Cash App Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Cash App. Catalog homepage identity is `https://cash.app`.

Treating the following three URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, marketing-homepage-value-not-a-proxy-for-the-brand-portal, cash.app/card-green-CTA-not-a-stand-in-for-every-other-harvested-control, brand-portal-documentation-not-a-proxy-for-live-computed-geometry, and typography-documentation-named-in-the-source-HTML-comment-not-a-fourth-live-inspect-host-in-this-packet, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. This contract covers three named first-party evidence domains from the 2026-06-17 packet: the marketing homepage at `https://cash.app`, the product surface at `https://cash.app/card`, and the official brand portal Color page at `https://design.cash.app/color` (typography documentation at `https://design.cash.app/typography` is named in the source HTML comment). Color, type, and component values below stay attached to the surface that established them. A marketing-homepage value is not a proxy for the black brand-portal canvas. A `cash.app/card` green CTA is not a stand-in for every other harvested control. Brand-portal documentation is not a proxy for live-computed geometry.

Source token note: primary = Cash Green `#00e013` (documented hero color, PMS 802 C; live green CTA bg on cash.app/card). Black `#000000` + White `#ffffff` are the supporting balance pair. Citron `#d8ff14` is a sparing high-impact accent (never a full background fill). Live product surfaces use 999px pill buttons in CashSans; brand portal (`design.cash.app`) is a black canvas with white type. Treating that note as a register split — `#00e013` is catalog `primary_color`, documented portal Cash Green, and the harvested green Sign-up fill rather than a second identity green; `#000000` as on-primary / portal canvas / Log-in fill rather than a second black; `#ffffff` as marketing canvas / on-dark / Sign-up-on-dark fill rather than a second white; `#d8ff14` as accent-only rather than a full background fill; 999px as button geometry rather than card 20px, badge 9999px, input 2px, or avatar 50% — is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

The following atmosphere readings — irreverent-expressive-consumer-fintech, radioactive-single-hero-color, so-saturated-it-reads-almost-like-a-glitch, most-finance-brands-hedge-toward-trust-blue, high-contrast-almost-street-culture-confidence, green-detonates-on-the-primary-call-to-action, acid-yellow-green, warmth-and-approachability-from-rounded-punctuation, typography-becomes-its-own-distinct-graphic-object, flat-pill-forward-anti-corporate-geometry, engineered-bold-culturally-fluent, financial-tooling-styled-like-a-confident-consumer-product-not-a-bank, dual-canvas-portal-black-versus-marketing-white, green-sits-on-the-primary-call-to-action, those-quoted-clauses-are-portal-documented, uniformly-999px-pill, and share-the-silhouette-at-46px — are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification. The brand portal (`design.cash.app`) opens on a pure black canvas (`#000000`) with white (`#ffffff`) type, while the marketing site (`cash.app`) flips to a white canvas with near-black text and lets Cash Green (`#00e013`) sit on the primary call-to-action. Black and white are framed as the supporting pair that "provide balance to Cash Green." Citron (`#d8ff14`) is permitted only "sparingly as a high-impact accent" and "never as a full background fill." Those quoted clauses are portal-documented. The typographic identity is **Cash Sans**, a customized cut of Klim Type Foundry's Söhne. The portal documents rounded punctuation as the primary feature that distinguishes Cash Sans from Söhne, plus tuned `2` / `5` / `7` / `£` glyphs. On the live product surfaces Cash Sans runs with a `Helvetica Neue` fallback. Default working weights are Regular (400) and Medium (500). The brand portal showcases oversized section index labels (86px+). Interactive chrome is uniformly **999px pill** — green Sign-up, black Log-in, and white hero button share the silhouette at 46px height. Live inspection found `box-shadow: none` across nav, hero, and buttons.

Treating the following public-history facts as narrative rather than interface tokens — email-and-text-frictionless-Square-Cash-origin, P2P-to-platform-Card-direct-deposit-commission-free-stocks-bitcoin-tax-filing, redefining-money-as-branchless-banking-alternative, 2024-bespoke-immersive-Index-Studio-infinite-canvas-portal, and digital-first-push-boundaries-defy-conventions — is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Cash App launched in 2013 by Block, Inc. (then Square, Inc.; Jack Dorsey co-founder) as "Square Cash" — a frictionless way to send money over email and text. It grew from a peer-to-peer transfer tool into a full consumer financial platform: the Cash App Card debit card, direct deposit, commission-free stock investing, bitcoin buying/selling, and tax filing. The brand's framing of itself as "redefining money" reflects that arc: from a single feature into a banking alternative for a generation that never wanted a branch. The 2024 brand refresh is a bespoke, immersive brand portal built with Index Studio on an infinite canvas — a digital-first identity centered on Cash Green as a hero color, Cash Sans as the voice, and an explicit invitation to "push boundaries" and "defy conventions" (Awwwards SOTD). Public-history narrative from those widely documented facts, the live brand portal, and named press coverage (Creative Bloq, Awwwards) provides that context; they do not by themselves supply interface tokens.

The following refusal, embrace, and dual-register readings — institutional-trust-blue, heavy-card-stack-elevation, white-glove-formality, flat-bold-pill-forward, one-omnipresent-green, black-and-white-as-structural-balance, money-treated-plainly-and-confidently-rather-than-reverently — are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification. What the source treats Cash App’s design as refusing: institutional trust-blue, heavy card-stack elevation, and white-glove formality. What it treats the brand as embracing: a flat, bold, pill-forward interface; one omnipresent green; black-and-white as structural balance; and a tone that treats money plainly and confidently rather than reverently.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings / live hosts below as Primary tasks, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Sign up from the Cash Green primary CTA on `https://cash.app` and `https://cash.app/card` (YAML use: `Primary CTA (Sign up) — Cash Green hero action`).
- Log in from the dark pill on light marketing surfaces (YAML use: `Log in pill on light surfaces`).
- Scan documented Color, Typography, and Voice & Tone on the brand portal (YAML use: `Brand-portal section nav on black canvas`; live hosts `https://design.cash.app/color` and `https://design.cash.app/typography`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes, not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

### Distinctive traits

The following unmerged-role extras — treating `#00e013` as catalog `primary_color` plus documented portal Cash Green plus harvested Sign-up fill rather than a second identity green, treating `#d8ff14` as accent-only rather than a full background fill, treating `#000000` on-green as on-primary rather than white-on-green, treating `#ffffff` marketing canvas as unmerged from on-dark `#ffffff` as a role, treating `#27455c` slate as a section band rather than a second black, treating `#3860be` as editorial blue rather than Cash Green, treating `#f8f8f8` / `#f6f6f6` as unmerged surfaces, treating `#737373` / `#555555` / `#999999` / `#858585` as unmerged greys, treating 999px pills as unmerged from 20px cards / 9999px chips / 2px inputs / 50% avatars, treating marketing-nav 16px/600 as unmerged from portal-nav 16px/400, treating Sign-up 14px/500 as unmerged from Log-in 14px/400 and from dark-hero Sign-up 12px/500, treating Cash Sans as unmerged from `Helvetica Neue`, and treating `box-shadow: none` as captured flatness rather than an extra elevation token — are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

- Cash Green `#00e013` as catalog `primary_color`, documented PMS 802 C, and the harvested “Sign up” / “Get started” pill fill
- Black `#000000` + White `#ffffff` as the supporting balance pair; black text on the green CTA
- Citron `#d8ff14` as a sparing high-impact accent — never a full background fill
- Dual canvas: black brand portal (`design.cash.app`) vs white marketing site (`cash.app`)
- Cash Sans (customized Söhne) with rounded punctuation; Regular (400) / Medium (500) defaults; `Helvetica Neue` fallback on live product surfaces
- 999px pill geometry on green, black, and white buttons at 46px height
- Flat depth: live `box-shadow: none` across nav, hero, and buttons; separation via raw color blocking (white / slate `#27455c` / black)
- Slate `#27455c` full-bleed marketing bands; Blue Accent `#3860be` for occasional editorial fills and links
- Surface `#f8f8f8` vs surface-alt `#f6f6f6`; muted ladder `#737373` / `#555555` / `#999999`; portal-grey `#858585`

### Principles

These six items (One hero color, used with intent; Black and white carry the structure; Flat, never elevated; Rigor by default, expression on purpose; Warmth through detail; Reject the bank aesthetic), including each stem and each *UI implication* (reserve-the-saturated-green-for-the-primary-action-never-wallpaper, build-layouts-in-black-white-blocks-let-green-mark-the-one-thing-to-do, no-drop-shadows-separate-with-raw-fills, keep-product-UI-plain-and-legible-reserve-typographic-experimentation-for-brand-surfaces, small-humanizing-details-the-geometry-is-the-personality, avoid-trust-blue-gold-and-institutional-chrome), are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

1. **One hero color, used with intent.** Cash Green (`#00e013`) is the single brand color. *UI implication:* reserve the saturated green for the primary action and key brand moments; never let it become wallpaper — "floods of green... shouldn't be the only way green shows up."
2. **Black and white carry the structure.** The supporting pair gives green room to stand out. *UI implication:* build layouts in black/white blocks and let green mark the one thing to do.
3. **Flat, never elevated.** Depth is communicated by color blocking, not shadow. *UI implication:* no drop shadows; separate sections with raw fills (white / `#27455c` slate / black).
4. **Rigor by default, expression on purpose.** Cash Sans Regular/Medium for clarity; pushed to extremes only on dedicated brand canvases. *UI implication:* keep product UI plain and legible; reserve typographic experimentation for brand surfaces.
5. **Warmth through detail.** Rounded punctuation makes a neo-grotesque approachable. *UI implication:* small humanizing details (rounded glyphs, full-pill buttons) carry the friendly tone — the geometry is the personality.
6. **Reject the bank aesthetic.** *UI implication:* avoid trust-blue, gold, and institutional chrome; lead with green, black, white, and confident plain language.

Treating the following as a capture-bound application of source §7 Do's, including Cash-Green-`#00e013`-as-the-single-hero, omnipresent-but-strategic, black-`#000000`-text-on-the-green-CTA, black-and-white-as-the-supporting-balance-pair, Cash-Sans-or-Söhne-with-rounded-punctuation, 999px-pills-at-46px, raw-color-blocking-white-slate-black, Citron-`#d8ff14`-sparing-only, and oversized-type-on-dedicated-brand-canvases, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Treat Cash Green (`#00e013`) as the single hero color — make it omnipresent but strategic
- Put black (`#000000`) text on the green CTA — never white-on-green
- Use black and white as the supporting balance pair so green stands out where it matters
- Use Cash Sans (or Söhne with rounded punctuation) for all type, Regular/Medium by default
- Keep all buttons as 999px pills at a consistent 46px height
- Separate sections with raw color blocking (white / `#27455c` slate / black), not shadows
- Reserve Citron (`#d8ff14`) for sparing, high-impact accents only
- Push typography to expressive, oversized extremes on dedicated brand canvases

### Avoid

The following items copy source §7 Don'ts, including Citron-`#d8ff14`-not-as-a-full-background-fill, no-white-text-on-the-green-button, black-on-green-for-contrast, no-drop-shadows, the-system-is-flat-use-color-blocks, no-diluting-green-across-many-elements, keep-it-the-single-hero-action, no-sharp-square-corners-on-buttons, every-button-is-a-full-pill, no-second-typeface-for-emphasis, push-Cash-Sans-instead, no-trust-blue-institutional-palettes, rejects-legacy-bank-aesthetics, and no-floods-of-green-as-the-only-way-green-shows-up. They are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

- Do not use Citron (`#d8ff14`) as a full background fill — it is an accent only
- Do not put white text on the green button — Cash App uses black-on-green for contrast
- Do not add drop shadows for elevation — the system is flat; use color blocks instead
- Do not dilute the green by spreading it across many elements — keep it the single hero action
- Do not use sharp/square corners on buttons — every button is a full pill
- Do not swap to a different typeface for emphasis — push Cash Sans instead
- Do not use trust-blue institutional palettes — Cash App rejects legacy-bank aesthetics
- Do not flood every surface with green — "floods of green can be powerful, but that shouldn't be the only way green shows up." Treating that last quoted clause as portal-adjacent source copy, and treating the surrounding Don't as reconstruction rather than a separately published UI specification, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including Cash-Green-not-Citron-not-Blue-Accent, on-primary-black-not-a-second-paint-from-ink, on-dark-white-not-a-second-paint-from-canvas, slate-not-portal-canvas, surface-not-surface-alt, muted-not-muted-strong-not-muted-soft-not-portal-grey, muted-soft-`#999999`-as-Default-Input-border-not-text, Cash-Green-Tint-not-canvas, and component-fields-such-as-green-CTA-fg-`#000000`-staying-on-that-control — are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification. Catalog `primary_color` `#00e013` is YAML `primary` and the Sign-up fill; it is not Citron `#d8ff14` and not Blue Accent `#3860be`. YAML `on-primary` `#000000` shares a hex with YAML `ink` `#000000` and is this reconstruction’s on-green label role, not a second black paint. YAML `on-dark` `#ffffff` shares a hex with YAML `canvas` `#ffffff` and is this reconstruction’s on-black/on-green-portal-white role, not a second white paint. YAML component fields such as button-green `fg` `#000000` stay on those controls. They are not extra general inks.

- **Cash Green / Primary** (`#00e013`): catalog `primary_color`. YAML `primary`. Documented hero color (CMYK 65/0/100/0, RGB 0/224/19, PMS 802 C). Primary CTA background ("Sign up") with black text. Portal-documented as "omnipresent across all brand touchpoints" and "used strategically such that it stands out as the hero color." Live computed green on `cash.app/card` is `rgb(0,224,19)`, matching `#00E013`.
- **Cash Green Tint** (`#f9fffa`): YAML `primary-tint`. Faint near-white green-tinted surface used as a subtle background wash on green-themed sections.
- **Black / Ink** (`#000000`): YAML `ink`. Primary text on white, brand-portal canvas, Log-in button background, and the "structure and contrast" half of the supporting pair (CMYK 60/40/40/100, PMS Black C).
- **White / Canvas** (`#ffffff`): YAML `canvas`. Marketing-site canvas, text on black/green, Sign-up button background on dark heroes (CMYK 0/0/0/0, PMS Bright White C).
- **On-primary** (`#000000`): YAML `on-primary`. Black text on the green CTA. Same hex as Ink; this is the on-green label role.
- **On-dark** (`#ffffff`): YAML `on-dark`. White text on black/green dark surfaces. Same hex as Canvas; this is the on-dark label role.
- **Citron** (`#d8ff14`): YAML `citron`. High-impact acid yellow-green accent (CMYK 11/0/91/0, RGB 216/255/20, PMS 388 C). Portal-documented: used "sparingly... to introduce depth and energy" and "should never be used as a full background fill."
- **Slate** (`#27455c`): YAML `slate`. Full-bleed section background band on the marketing surface, a darker neutral block between white and black zones. Not portal-canvas black.
- **Blue Accent** (`#3860be`): YAML `blue-accent`. Muted editorial blue used for occasional accent fills and links within content blocks. Not Cash Green.
- **Surface** (`#f8f8f8`): YAML `surface`. Light off-white surface for feature cards and segmented blocks.
- **Surface Alt** (`#f6f6f6`): YAML `surface-alt`. A second near-white surface for alternating content bands. Not Surface.
- **Muted** (`#737373`): YAML `muted`. Secondary body text and captions.
- **Muted Strong** (`#555555`): YAML `muted-strong`. Stronger secondary text and labels.
- **Muted Soft** (`#999999`): YAML `muted-soft`. Tertiary text, metadata, fine print. Not the Default Input border field, which uses this same hex as a 1px border on that control.
- **Portal Grey** (`#858585`): YAML `portal-grey`. Dimmed grey of inactive oversized section labels on the black brand portal.

### Spacing

Treating those YAML numbers as recorded without a required px suffix (none is added here), treating body px writings as body-recorded steps rather than a converted YAML scale, and treating harvested control heights 46px and the 276px hero media zone as component/layout fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. YAML scale: xs 4, sm 8, md 12, base 16, lg 20, xl 31, xxl 48, section 64. Those YAML numbers are recorded without a px suffix. Body layout names a scale of 4px, 8px, 12px, 16px, 20px, 31px, 48px, 64px.

The following ~4px-base / 0px-vertical-20px-horizontal-pill-padding / fixed-46px-height readings are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification. Body layout states a base unit of ~4px. Pill buttons land on a 0px vertical / 20px horizontal padding pattern at a fixed 46px height.

### Shape

YAML `rounded`: sm 2, md 20, lg 999, full 9999.

The following local-geometry reading, including Sharp-2px-inputs, Medium-20px-feature-cards, container-workhorse, Pill-999px-all-buttons, Full-9999px-accent-badges, and avatar-50%-unmerged-from-9999, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Sharp (2px): inputs, fine structural elements (YAML `rounded.sm`)
- Medium (20px): feature cards, content tiles — the container workhorse (YAML `rounded.md`)
- Pill (999px): all buttons (YAML `rounded.lg`)
- Full (`9999px`): accent badges (YAML `rounded.full`)
- Circle (`50%`): circular avatars/controls (body Account Control; YAML avatar `radius: 50%`)

Treating 2px inputs, 20px cards, 999px pills, `9999px` chips, and `50%` avatars as local harvested geometry rather than a universal radius for every unlisted control, and treating YAML `lg` 999 as a YAML step and as `999px` on the three harvested pills rather than merged with YAML `full` 9999 or with avatar `50%`, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. 2px inputs, 20px cards, 999px pills, `9999px` chips, and `50%` avatars are local harvested geometry, not a universal radius for every unlisted control. YAML `lg` 999 is recorded as a YAML step and as `999px` on the harvested buttons; it is not merged with YAML `full` 9999 or with avatar `50%`.

### Elevation

The following elevation-table Use readings, including page-background-inline-text-most-surfaces, section-separation-without-elevation, and interactive-affordance-via-shape-plus-color-not-shadow, plus the near-shadowless-flat / grouping-by-raw-color-blocking / pill-geometry / emphasis-via-Cash-Green-or-Citron-never-drop-shadow / deliberate-modern-flat-anti-corporate / fast-confident-culturally-current / avoiding-legacy-banking-card-stack-elevation readings that follow the table, are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Color block (Level 1) | Raw fill shift (white ↔ `#27455c` slate ↔ `#000000` black) | Section separation without elevation |
| Pill (Level 2) | 999px capsule fill (green/black/white) | Interactive affordance via shape + color, not shadow |

YAML `tokens.shadow.none`: `none`.

Live inspection found `box-shadow: none` across the nav, hero, and all buttons on both `cash.app` and `cash.app/card`. Depth and grouping are communicated entirely through bold color blocking — alternating black, white, and slate (`#27455c`) bands — and through the pill geometry of interactive elements. This is a deliberate modern-flat, anti-corporate choice: it keeps the brand feeling fast, confident, and culturally current, avoiding the heavy "card stack" elevation of legacy banking UIs. When emphasis is needed, the system reaches for Cash Green (`#00e013`) or, sparingly, Citron (`#d8ff14`) — never a drop shadow.

### Motion

Source-stated duration roles. Treating the duration table, easing names, signature motions, and reduced-motion line as source-stated rather than computed CSS, and treating Arriving / Dismissals / Two-way / spec-template-ease-exit-match / functional-and-quick / green-never-bounces-gratuitously / confidence-reads-as-steadiness / “the product remains fully functional” readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, pill press, focus |
| `motion-standard` | 220ms | Card/section reveal, sheet, dropdown |
| `motion-slow` | 360ms | Page-level transitions, expressive brand reveals |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — sheets, cards, pills |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

Signature motions (source-stated):

Treating Motion on product surfaces as functional-and-quick, treating pill-press as a subtle-scale-opacity-shift, treating content-fades-in-from-below at `motion-standard / ease-enter`, treating brand-portal Motion as the documented expressive register on an infinite canvas, treating playful-character-driven-transitions-on-an-infinite-canvas, treating no-gratuitous-bounce on the green in product UI as a signal of steadiness, and treating “the product remains fully functional”, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

On product surfaces motion is functional and quick — pill buttons respond to press with a subtle scale/opacity shift, content fades in from below at `motion-standard / ease-enter`. The brand portal is where motion gets expressive (the documented "Motion" section), with playful, character-driven transitions on an infinite canvas. The green never bounces gratuitously in product UI — confidence reads as steadiness. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant and the product remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including portal-Cash-Sans-as-official-product-use-documentation, live-computed-Cash-Sans-with-Helvetica-Neue-fallback, YAML-family-keys-naming-those-families, fallback-stack-not-the-brand-face, and outside-the-three-named-URLs, are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | `https://design.cash.app/typography` documents Cash Sans as a customized version of Söhne by Klim Type Foundry; rounded punctuation is the primary distinguishing feature; Regular and Medium are the default for typesetting. The same portal page has a Secondary typeface section for Display / Novelty / Bespoke usage; no family name is recorded here. |
| Live computed surface-use | Live product surfaces compute Cash Sans with a `Helvetica Neue` fallback (then `helvetica`, `sans-serif`). YAML `family.display` / `family.body` `Cash Sans` and YAML `family.fallback` `Helvetica Neue` name those live computed families. YAML/body control size is 14px. A sidecar live-computed Green CTA font-size, recorded in provenance Proof, is not this 14px. |
| Outside these captures | Surfaces other than the three named URLs. |

### Family

- **Current visible UI family:** `Cash Sans` (customized Söhne; YAML `family.display` / `family.body`)
- **Fallback:** `Helvetica Neue`, then `helvetica`, `sans-serif` (live computed stack; YAML `family.fallback` `Helvetica Neue`)
- **Default weights:** Regular (400) and Medium (500) — portal-documented as "our default for typesetting" where "clear communication and brand recognition take priority over typographic expression."

The following font-use boundary, including do-not-present-Helvetica-Neue-as-Cash-Sans, do-not-replace-Cash-Sans-with-a-different-family-for-emphasis, and rounded-punctuation-as-the-signature-not-a-second-face, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Do not present `Helvetica Neue`, `helvetica`, or `sans-serif` as Cash Sans. Do not replace Cash Sans with a different family for emphasis. Do not present a system stack as Cash Sans.

The following type-rule readings (restrained-default-expressive-ceiling, rounded-punctuation-is-the-signature, one-typeface-two-registers, black-on-green-never-white-on-green) are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification. Regular (400) and Medium (500) are the everyday working weights; the portal demonstrates the same typeface pushed to oversized, experimental extremes. Rounded punctuation is the single feature that distinguishes Cash Sans from stock Söhne. Cash Sans handles both the plainspoken product UI and the experimental brand canvas. Button text on the green CTA is black (`#000000`).

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1.0`, `1.1`, `1.2`, and `1.5`. The following ratio-versus-size-local reading, including unitless-ratios-not-fixed-px, px-and-rem-companions-local, YAML-use-strings-kept, Nav-16px-600-unmerged-from-Body-16px-400-and-from-portal-nav-16px-400, button-14px-500-unmerged-from-Log-in-14px-400, and button-sm-12px-500-compact-header-pill, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px / rem figures are size-local observations, not replacements for the ratios.

| Role | Font | Size | Weight | Line height (YAML) | Tracking | Use (YAML) / Notes |
|---|---|---:|---:|---:|---|---|
| Portal Index | Cash Sans | 86px (5.40rem) | 400 | 1.0 | | Brand-portal oversized section index labels, Cash Sans |
| Section Heading | Cash Sans | 40px (2.50rem) | 500 | 1.1 | | Marketing section headlines, Cash Sans Medium |
| Sub-section | Cash Sans | 24px (1.50rem) | 500 | 1.2 | | Feature card heads, Cash Sans Medium |
| Nav / Active | Cash Sans | 16px (1.00rem) | 600 | 1.0 | | Portal nav / active labels, Cash Sans Semibold |
| Body | Cash Sans | 16px (1.00rem) | 400 | 1.5 | | Standard reading text, Cash Sans Regular |
| Button | Cash Sans | 14px (0.88rem) | 500 | 1.0 | | Pill button label, Cash Sans Medium |
| Button Small | Cash Sans | 12px (0.75rem) | 500 | 1.0 | | Compact header pill label, Cash Sans Medium |

YAML `nav` 16px / 600 / 1.0 is not merged with YAML `body` 16px / 400 / 1.5. YAML `button` 14px / 500 is not merged with Log-in body 14px / 400. YAML `button-sm` 12px / 500 is the compact header pill.

### Assets

Catalog logo: type `favicon`, slug `https://www.google.com/s2/favicons?domain=cash.app&sz=128`. Treating that Google s2 URL as a catalog identity-boundary record rather than a captured first-party Cash App mark, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

Product screenshots and illustrations carry no shadow at any size, consistent with the flat system. Treating that no-shadow imagery reading as source-stated image behavior rather than a complete image specification, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated.

Characterizations in the following table such as “No clutter”, “calm, plain path back to the action”, “No shadow shimmer — a flat pulse consistent with the shadowless system”, “label stays legible; no spinner-on-green that kills the contrast”, “never a bare Something went wrong”, “describing what's valid, not just Required”, “Plain, no celebratory emoji pile-up”, and “green actions fade rather than turn grey to preserve the brand read” are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no activity)** | White canvas. Single black (`#000000`) line in Cash Sans explaining nothing's happened yet, with one Cash Green (`#00e013`) CTA to take the first action. No clutter. |
| **Empty (no saved items)** | Muted (`#737373`) single line; calm, plain path back to the action. |
| **Loading (data fetch)** | Flat skeleton blocks at final dimensions on `#f8f8f8` surface, 20px radius. No shadow shimmer — a flat pulse consistent with the shadowless system. |
| **Loading (action in progress)** | Inline progress within the pill button; label stays legible; no spinner-on-green that kills the contrast. |
| **Error (transaction failed)** | Inline message in black with a plain-language explanation and a retry. States what to do next — never a bare "Something went wrong". |
| **Error (form validation)** | Field-level message below the input describing what's valid, not just "Required". |
| **Success (sent / completed)** | Brief inline confirmation; Cash Green accent on the confirmation mark with black text. Plain, no celebratory emoji pile-up. |
| **Skeleton** | `#f8f8f8` blocks at final dimensions, 20px radius, flat pulse. |
| **Disabled** | Muted Soft (`#999999`) text on reduced-opacity surface; green actions fade rather than turn grey to preserve the brand read. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `Focus` capture and no `focus-visible` treatment; `focus-visible` visual treatment remains omitted. Source §15 names `focus` only as a `motion-fast` duration use (Hover, pill press, focus); that duration role is not `focus-visible` evidence and is not copied onto a `focus-visible` row as a colour. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Light Feature Card, Dark Tile, and Citron Accent Chip have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Default Input has no YAML `tokens.components` row; Type is not invented from the Inputs heading.

### Sign Up (Primary Green)

- Role: Primary CTA — the Cash Green hero action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#00e013`
- Text: `#000000`
- Radius: 999px
- Padding: YAML `0 20px`; body `0px 20px`
- Height: 46px
- Font: 14px / 500 / Cash Sans
- Use: YAML `Primary CTA (Sign up) — Cash Green hero action`. Body examples: "Sign up", "Get started"
- Observed: default only
- YAML `tokens.components.button-green`
- Field note: The following unmerged-field reading, including Role-as-the-Cash-Green-hero-action, this-fill-not-Citron, on-fill-black-not-white-on-green, 999px-not-20px-not-9999px-not-50%, and 14px-500-not-Log-in-14px-400-not-Button-Small-12px-500, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#00e013` is this control’s fill and catalog `primary_color`. `#000000` is this control’s on-fill label (on-primary). Radius 999px is this pill; it is not card 20px, badge `9999px`, or avatar `50%`. Font 14px / 500 is this control’s field, not Log-in 14px / 400.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the Cash Green primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says green actions fade rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the marketing primary CTA “Sign up” / “Get started”; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Log In (Dark Pill)

- Role: Secondary header action on light surfaces
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#000000`
- Text: `#ffffff`
- Radius: 999px
- Padding: YAML `0 20px`; body `0px 20px`
- Height: 46px
- Font: 14px / 400 / Cash Sans
- Use: YAML `Log in pill on light surfaces`. Body example: "Log in"
- Observed: default only
- YAML `tokens.components.button-dark`
- Field note: The following unmerged-field reading, including this-fill-not-green-CTA, on-fill-white-not-on-primary-black, 14px-400-not-Sign-up-14px-500, and same-999px-46px-geometry-as-the-green-pill-not-a-second-radius, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#000000` is this control’s fill. `#ffffff` is this control’s on-fill label. Font 14px / 400 is this control’s field, not Sign-up 14px / 500.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the Log-in pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A header action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the header “Log in” pill on light surfaces; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Sign Up on Dark (Light Pill)

- Role: Header sign-up pill when the hero canvas is black
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#000000`
- Radius: 999px
- Padding: YAML `0 20px`; body `0px 20px`
- Height: 46px
- Font: 12px / 500 / Cash Sans
- Use: YAML `Sign up pill on dark hero`
- Observed: default only
- YAML `tokens.components.button-light`
- Field note: The following unmerged-field reading, including this-fill-not-marketing-canvas-as-a-second-token, 12px-500-not-Sign-up-14px-500, and same-999px-46px-geometry, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#ffffff` is this control’s fill. Font 12px / 500 is YAML `button-sm` / this control’s field, not Sign-up 14px / 500.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the dark-hero Sign-up pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A header action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the header sign-up pill on a black hero; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Default Input

- Role: Form text input on white surfaces (sharp-cornered, neutral hairline)
- Kind: interactive
- Anatomy: value field
- Background: `#ffffff`
- Text: `#000000`
- Border: `1px solid #999999`
- Radius: 2px
- Use: Form text input on white surfaces (sharp-cornered, neutral hairline)
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented from the Inputs heading.
- Field note: The following unmerged-field reading, including 2px-not-999px, `#999999`-as-this-control-border-not-muted-soft-text, and input-fill-white-not-a-second-canvas-token, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Radius 2px is this input (YAML `rounded.sm`). `#999999` here is this field’s 1px border, not muted-soft text. `#ffffff` is this field’s fill.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the default form text input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted. Source §14 names a field-level message describing what's valid, not just "Required"; that row is not copied here as a computed paint |

Loading and success applicability are omitted. Source records default geometry. Exact loading/success mapping for this field is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

### Light Feature Card

- Role: Light feature card on the marketing surface (flat, no shadow)
- Type: card
- Anatomy: surface
- Background: `#f8f8f8`
- Text: `#000000`
- Radius: 20px
- Use: YAML `Light feature card on marketing surface (flat, no shadow)`
- Observed: default; `box-shadow: none`
- YAML `tokens.components.card-light`
- Field note: The following unmerged-field reading, including this-fill-not-surface-alt, 20px-not-999px, and card-ink-not-muted-`#737373`, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#f8f8f8` is this card’s fill (YAML `surface`), not surface-alt `#f6f6f6`. Radius 20px is this card; it is not pill 999px. YAML `fg` `#000000` is this card’s ink; body copy `#737373` stays on the §9 Feature Card local recipe, not as this YAML field.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Dark Tile

- Role: Dark feature card / brand-portal content tile
- Type: card
- Anatomy: surface
- Background: `#000000`
- Text: `#ffffff`
- Radius: 20px
- Use: YAML `Dark feature card / brand-portal tile`
- Observed: default
- YAML `tokens.components.card-dark`
- Field note: The following unmerged-field reading, including this-fill-not-slate-`#27455c` and 20px-not-pill-999px, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#000000` is this tile’s fill, not slate `#27455c`. Radius 20px is this tile; it is not pill 999px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Citron Accent Chip

- Role: High-impact accent pill — used sparingly, never as a full background fill
- Type: badge
- Anatomy: label
- Background: `#d8ff14`
- Text: `#000000`
- Radius: 9999px
- Font: 12px / 500 / Cash Sans
- Use: YAML `High-impact accent chip (sparing, never full-bg)`
- Observed: default
- YAML `tokens.components.badge-citron`
- Field note: The following unmerged-field reading, including this-fill-not-Cash-Green, 9999px-not-999px-not-50%, and 12px-500-matching-Button-Small-not-a-second-family, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#d8ff14` is this chip’s fill, not Cash Green `#00e013`. Radius `9999px` is this chip (YAML `rounded.full`); it is not button 999px and not avatar `50%`.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Marketing Nav

- Role: Top horizontal marketing nav
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#ffffff`
- Text: `#000000`
- Font: 16px / 600 / Cash Sans
- Active (named): Cash Green `#00e013` fill/underline on hover
- Use: YAML `Top nav item`
- Observed: default plus named active/hover
- YAML `tokens.components.nav-tab`
- Field note: The following unmerged-field reading, including 16px-600-not-Body-16px-400-not-portal-nav-16px-400, and captured-hover-variant-not-a-second-nav-token, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Font 16px / 600 is this item’s field (YAML `nav`). Named Cash Green `#00e013` fill/underline on hover is a captured variant on this item.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top marketing nav item |
| hover | applicable | Pointer-web tab/link; visual treatment omitted from this row. Named `#00e013` fill/underline on hover is recorded as a captured variant below, not copied onto `focus-visible` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

Additional observed named state: Cash Green `#00e013` fill/underline on hover. Treating that appearance as a captured variant, not an observed click transition, and treating named hover/active green as not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

### Brand-Portal Nav

- Role: Section nav on the black brand-portal canvas (Foundations / Expressions / Resources)
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#000000`
- Text: `#ffffff` (active) / `rgba(255,255,255,0.7)` (inactive)
- Font: YAML `16px / 400 Cash Sans`; body `16px Cash Sans`
- Use: YAML `Brand-portal section nav on black canvas`
- Observed: default plus named active/inactive
- YAML `tokens.components.portal-nav-tab`
- Field note: The following unmerged-field reading, including YAML-400-not-marketing-nav-600, active-opaque-white-not-Cash-Green, and inactive-rgba-0.7-not-portal-grey-`#858585`, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. YAML font 16px / 400 is this item’s field; it is not marketing-nav 16px / 600. Active `#ffffff` opaque is this item’s selected label, not Cash Green. Inactive `rgba(255,255,255,0.7)` is this item’s resting label, not portal-grey `#858585` (oversized section-index dim).

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the brand-portal section nav |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A section nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

Additional observed named state: active text `#ffffff` opaque versus inactive `rgba(255,255,255,0.7)`. Treating that appearance as a captured variant, not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

### Account Control

- Role: Circular account/profile control in the header
- Kind: interactive
- Type: avatar
- Anatomy: control
- Background: `#000000`
- Text: `#ffffff`
- Radius: 50% (circle)
- Height: 46px
- Use: YAML `Circular account / profile control in header`
- Observed: default only
- YAML `tokens.components.avatar-round`
- Field note: The following unmerged-field reading, including 50%-not-9999px-not-999px and 46px-height-matching-pills-not-a-second-radius, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Radius `50%` is this control (YAML avatar); it is not badge `9999px` and not pill 999px. Height 46px is this control’s body field, matching pill height; it is not a radius.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header account/profile control |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A profile control can be unavailable; visual treatment omitted |
| loading | not-applicable | A header account/profile control opens account chrome; the control itself does not enter a loading state |
| error | not-applicable | Account-control meaning is profile chrome, not a request or validation failure on the control |
| success | not-applicable | Account-control meaning is profile chrome, not action-outcome confirmation |

### White Hero (local recipe)

Treating the following as a source §9 local composition — white `#ffffff` parent + Cash Sans Medium 40px black headline + green Sign-up pill + black Log-in pill at the same 999px geometry — and not as a rewrite of YAML Sign Up / Log In or a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Role: white-hero parent surface
- Anatomy: surface
- Background: `#ffffff`
- Local children (this composition only): headline Cash Sans Medium 40px, black `#000000`; Primary CTA Cash Green `#00e013` background, black `#000000` text, 999px pill, 0 20px padding, 46px height, Cash Sans 14px weight 500 — "Sign up"; Secondary "Log in" pill: black `#000000` bg, white text, same 999px geometry
- Use: source §9 white hero — white background, 40px/500 `#000000` headline, green Sign-up + black Log-in pair
- Kind, Type, and a state-applicability map are omitted. Harvested Sign Up (Primary Green) and Log In (Dark Pill) stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-white-not-a-second-canvas-token, 40px-headline-plus-two-pills-as-this-hero, and not-a-rewrite-of-YAML-Sign-Up-or-Log-In, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#ffffff` is this parent’s fill. The two pills stay children of this hero; they are not promoted as a global hero token.

### Feature Card (local recipe)

Treating the following as a source §9 local composition — `#f8f8f8` parent + 20px radius + no shadow + title Cash Sans Medium 24px black `#000000` + body Cash Sans 16px weight 400 `#737373` — and not as a rewrite of YAML Light Feature Card or a global card-copy token, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Role: feature-card parent
- Anatomy: surface
- Background: `#f8f8f8`
- Radius: 20px
- Shadow: none
- Local children (this composition only): title Cash Sans Medium 24px black `#000000`; body Cash Sans 16px weight 400, `#737373`
- Use: source §9 feature card — `#f8f8f8`, 20px, no shadow, 24px title, 16px/400 `#737373` body
- Kind, Type, and a state-applicability map are omitted. Harvested Light Feature Card stays in its own slot.
- Field note: The following unmerged-composition reading, including `#737373`-as-this-card-body-not-YAML-card-fg-`#000000` and 24px-title-plus-16px-muted-body-as-this-card, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#737373` stays this composition’s body colour, not YAML `card-light` `fg` `#000000`.

### Dark Section (local recipe)

Treating the following as a source §9 local composition — black `#000000` full-bleed parent + white `#ffffff` Cash Sans type + one Citron `#d8ff14` accent chip (9999px, black text) used sparingly — and not as a global dark-band token or a rewrite of YAML Dark Tile / Citron Accent Chip, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Role: full-bleed dark-section parent
- Anatomy: surface
- Background: `#000000` (full-bleed)
- Local children (this composition only): white `#ffffff` Cash Sans type; one Citron `#d8ff14` accent chip, 9999px, black text — never as the full background
- Use: source §9 dark section — black full-bleed, white type, one Citron chip
- Kind, Type, and a state-applicability map are omitted. Harvested Dark Tile and Citron Accent Chip stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-black-not-slate and one-Citron-chip-not-a-full-background, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. `#000000` is this band’s parent fill, not slate `#27455c`. The Citron chip stays a child of this section; it is not promoted as a full-bleed fill.

### Marketing Top Nav (local recipe)

Treating the following as a source §9 local composition — white header + Cash Sans 16px weight 600 black links + Cash Green `#00e013` on active + green Sign-up pill right-aligned + black Log-in pill beside it, both 999px — and not as a rewrite of YAML Marketing Nav / Sign Up / Log In or a global header token, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- Role: marketing-header parent
- Anatomy: surface
- Background: white header
- Local children (this composition only): Cash Sans 16px weight 600 black links, Cash Green `#00e013` on active; Green "Sign up" pill right-aligned; black "Log in" pill beside it; both 999px
- Use: source §9 top nav — white header, 16px/600 links, right-aligned Sign-up + Log-in pair
- Kind, Type, and a state-applicability map are omitted. Harvested Marketing Nav, Sign Up (Primary Green), and Log In (Dark Pill) stay in their own slots.
- Field note: The following unmerged-composition reading, including right-aligned-Sign-up-plus-Log-in-beside-it-as-this-header and not-a-global-header-token, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Right-aligned Sign-up with Log-in beside it stays on this parent; it is not promoted as a global header token.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 20, xl 31, xxl 48, section 64. Body layout repeats 4px, 8px, 12px, 16px, 20px, 31px, 48px, 64px. Harvested heights: 46px pills and account control. Hero media/illustration zone: 276px. Portal index labels: 86px.

The following layout readings — color-blocking-over-elevation, single-action-focus, expressive-vs-functional-zones, white-canvas-with-full-bleed-alternating-section-bands, large-headline-over-276px-media-zone, green-CTA-as-the-focal-action, black-canvas-with-oversized-vertical-index, feature-content-in-20px-radius-cards — are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification.

Recorded layout:

- Marketing site: white canvas with full-bleed alternating section bands (white / slate `#27455c` / black)
- Hero: large headline anchored over a tall (276px) media/illustration zone, with the green CTA as the focal action
- Brand portal: black canvas with an oversized vertical index of section labels (Logo, Color, Typography, Motion, Iconography, Voice & Tone) at 86px
- Feature content groups into 20px-radius cards on light or dark fills

Treating the following whitespace philosophy as reconstruction rather than a separately published layout specification, including color-blocking-over-elevation, single-action-focus so the green CTA is the one saturated element on a given surface, and expressive-vs-functional-zones (marketing/product surfaces stay rigorous and plain; the brand portal is where typography and color are pushed to expressive extremes), is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification. Whitespace philosophy (source): color blocking over elevation; single-action focus so the green CTA is the one saturated element on a given surface; expressive vs functional zones — marketing/product surfaces stay rigorous and plain; the brand portal is where typography and color are pushed to expressive extremes.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 46px / 276px / 86px figures as surface measurements in this packet rather than a cross-viewport specification, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating green-CTA-full-pill / nav-items-spaced-for-touch as that purpose reading, treating no-shadow screenshots / 20px-radius-across-breakpoints / Cash-Green-and-Citron-full-saturation-across-sizes / consistent-with-the-flat-system / RGB-as-documented-truest-depiction as source-stated image behavior rather than a complete image specification, and treating headline-compresses / green-CTA-may-go-full-width-pill / feature-bands-multi-column-to-stacked / full-width-alternating-bands / portal-index-reflows-vertically as recorded collapsing rather than a complete responsive specification, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero headline compresses, full-width pill CTAs |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024-1440px | Full layout, multi-column feature bands, oversized portal index |

Collapsing strategy recorded in the source: hero large headline scales down on mobile; green CTA may go full-width pill; feature bands: multi-column → stacked single column; alternating black/white/slate sections maintain full-width treatment; brand-portal oversized index reflows vertically on narrow viewports.

Image behavior: product screenshots and illustrations carry no shadow at any size, consistent with the flat system; cards maintain 20px radius across breakpoints; Cash Green and Citron retain full saturation across sizes (RGB is the documented "truest depiction").

Touch-target record: pill buttons at 46px height with 20px horizontal padding; green CTA full-pill; nav items spaced for touch within the header.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-17)

The live strings below are source-stated. Treating the parenthetical characterizations (homepage-title-meta / brand-portal-Color-page / hero-feature-label) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

- "Send, Receive, Invest, & Manage Your Money with Cash App" — homepage title meta. *(verified live 2026-06-17, cash.app)*
- "Our brand color palette is centered around Cash Green, a bold distillation of how we redefine money." — brand portal Color page. *(verified live 2026-06-17, design.cash.app/color)*
- "Bank* on your terms" — hero feature label. *(verified live 2026-06-17, cash.app)*

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Cash App-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — direct-irreverent-culturally-fluent, plainspoken-about-money, rejects-banking-stiffness-and-startup-hype, rounded-punctuation-aligned-with-tone-of-voice, short-confident-unpretentious, bare-imperatives, short-declarative-benefit-first, plain-and-functional, confident-expressive-a-little-defiant, plain-and-unembellished, trust-washing-bank-formality, fear-based-finance-pitches, exclamation-heavy-hype, jargon-left-undefined, diluting-Cash-Green-from-hero-color-to-wallpaper — are a derived editorial implementation inference from the verified surfaces; they are not Cash App-authored or a separately published UI specification. They are not the Observed strings above. The brand portal frames its own register under "Voice & Tone." The design-language clause that rounded punctuation is "to provide warmth and approachability that aligns with our tone of voice" is source-quoted from that portal framing.

The source describes Cash App's voice as direct, irreverent, and culturally fluent — plainspoken about money in a way that rejects both banking stiffness and startup hype. Copy is short, confident, and unpretentious; CTAs are bare imperatives ("Sign up", "Log in", "Order a Cash App Card", "Send money for free").

| Context | Tone |
|---|---|
| Hero headlines | Short, declarative, benefit-first. "Bank* on your terms", "Send money for free". |
| Feature labels | Plain and functional. "Save for your goals", "Buy and sell bitcoin easily". |
| CTAs | Bare imperatives. "Sign up", "Order a Cash App Card", "File your taxes for free". |
| Brand / portal copy | Confident, expressive, a little defiant. "A bold distillation of how we redefine money." |
| Legal / compliance | Plain and unembellished. "Prepaid debit cards issued by Sutton Bank, Member FDIC." |

**Forbidden register** (source): trust-washing bank formality, fear-based finance pitches, exclamation-heavy hype, jargon left undefined, and any treatment that dilutes Cash Green from hero color to wallpaper.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exception remains the Marketing Nav `#00e013` fill/underline on hover, recorded as a captured variant
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Sign Up (Primary Green), Log In (Dark Pill), and Sign Up on Dark (Light Pill); omitted loading/success on Default Input
- interactive kind and state-applicability map for Light Feature Card, Dark Tile, and Citron Accent Chip
- Type on Default Input (YAML `tokens.components` has no row)
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
