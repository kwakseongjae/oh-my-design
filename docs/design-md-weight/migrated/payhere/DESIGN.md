# Payhere Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Payhere (페이히어) is a Korean fintech whose homepage positions it as Korea's No.1 mobile-POS company. This contract covers the first-party marketing homepage the source inspected for tokens on 2026-07-02 at `https://payhere.in/`. YAML `tokens.source` is `live-extract`. Named first-party sources that frame long-form typography or brand-narrative quotes — and that the source does not treat as that homepage token surface — are `https://tech.payhere.in/` and `https://tech.payhere.in/post/design-payhere-welcome-kit/`. Catalog `primary_color` `#008cff` is the same azure as `tokens.colors.primary` `#008cff`; they stay two writings, not a second azure. Every value stays attached to the surface that established it. Reading `https://payhere.in/` as this contract's token surface, keeping the tech blog and the welcome-kit post as named sources for Pretendard and for quoted brand lines rather than as that homepage token surface, keeping catalog `primary_color` beside `tokens.colors.primary` rather than as a second azure, and keeping every value attached to the surface that established it, are derived editorial implementation inferences from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

The homepage reads like a bright, energetic tech-retail product rather than a staid financial service. The canvas is pure white (`#ffffff`) broken by soft cool-grey cards (`#f5f8fa`) and a saturated azure blue (`#008cff`) that functions as the system's single "action" color — the fill on primary product tabs and the ring on the active selector. Body text sits in flat black (`#000000`) at a compact 16px in Noto Sans KR, while headings step up to a deep, warm navy (`#101a2e`) and card labels to a slightly softer navy (`#1c2638`). The typographic personality is bold and declarative. Hero headlines run in **Noto Sans KR Bold (weight 700)** at 44px ("모바일 포스 1위 / 매장의 새로운 미래, 페이히어" — "No.1 mobile POS / the store's new future"), section headings at 32px/700, and product-category card labels at a punchy 26px/700 in navy (`#1c2638`). Inactive headings dim all the way to a faint blue-grey (`#c1cad2`), so the eye is guided by weight and contrast rather than color. Below the display tier, everything drops to a quiet 16px/400 body — dense, hangul-optimized, and uniform across nav, buttons, and copy. What distinguishes Payhere from its fintech peers is its flat, pill-heavy, near-shadowless geometry. Separation comes from tinted grey surfaces (`#f5f8fa`, and a warmer `#f4f8f9`) and thin `#919ba5` hairlines rather than elevation; the only "shadow" in the interactive chrome is a 1px inset azure ring on the active tab. Feature filters are full pills at a 30px radius, product cards round at 12–16px, and a promo-banner blue (`#0077fe`) plus decorative coral (`#ff5b46`) and green (`#08d07e`) accents punctuate the scroll. The rebranded logo mark itself is a blue-to-purple spectrum — bright blue (`#1d99ff`), indigo (`#163bd8`), and purple (`#a164f9`). The hex values, the hero line, the 44px / 32px / 26px / 16px sizes, the 30px pills, the 12–16px cards, the 5px compact tabs, and the logo spectrum are the source's own. The characterizations built on them — bright energetic tech-retail rather than a staid financial service; a single "action" color; bold and declarative; the eye guided by weight and contrast; quiet dense hangul-optimized body; flat pill-heavy near-shadowless geometry; modern optimistic identity — are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. Payhere (페이히어) is a Korean fintech that set out to modernize the point of sale for small merchants — replacing bulky, expensive legacy POS hardware with an affordable mobile-first system spanning card terminals, table-order, kiosk, and an all-in-one terminal ("페이히어 터미널"). Its positioning is explicit on the homepage: **"모바일 포스 1위"** (No.1 mobile POS) and **"매장의 새로운 미래"** (the store's new future). The company frames its mission as a journey to the future. In its own words on the official tech blog, *"페이히어는 매장의 새로운 미래를 만듭니다. (We Make The Future)"* and the internal brand line *"Journey to the Future"*. A 2022 rebranding gave the company a clearer identity and a new logo — the blue-to-purple mark (bright blue `#1d99ff`, indigo `#163bd8`, purple `#a164f9`) observed live in the site header — and its welcome-kit design was built on three words: *"Simple (이해하기 쉬운), Familiar (친숙한), Long Awaited (기대되는)"* (whose initials W, M, F became character motifs). What Payhere refuses, visible in its design: the heavy, intimidating chrome of legacy retail-banking hardware and the density of enterprise POS dashboards. What it embraces: a flat, fast, mobile-native interface; a single trustworthy azure action color; bold Noto Sans KR headlines that speak plainly about price and capability; and copy that names store operations in everyday Korean rather than payments jargon. The year 2022, the all-in-one terminal name, the two homepage positioning lines, the welcome-kit quotes, the W, M, F motifs, and that closing refusal-and-embrace paragraph are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-rebrand narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a surface or control the source records, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. They do not come from the source's Personas section.

- Scan the homepage product tabs and product-category cards ("카드 단말기", "테이블 오더", "키오스크", "인터넷 패키지") on `https://payhere.in/`.
- Filter homepage features with the captured pills ("NFC", "리뷰 마케팅", "비대면 결제", "선불권", "단독 결제").
- Read the official tech-blog welcome-kit post at `https://tech.payhere.in/post/design-payhere-welcome-kit/`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source labels its three entries as fictional archetypes informed by publicly observable Payhere user segments, not individual people, so those biographies are dropped rather than promoted, and no name, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is the audience at a group level: Korean small-business owners, café/restaurant operators, retail merchants. Dropping the illustrative biographies rather than promoting them, carrying no name, motivation, or affiliation classification, and reading those source-named groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, are a derived editorial implementation inference from the verified surfaces — they are not Payhere-authored or a separately published UI specification.

- Noto Sans KR Bold (weight 700) for all display headlines — bold, declarative merchant-tech voice
- Noto Sans KR weight 400 at 16px for body/UI/nav — quiet, dense, hangul-optimized
- Single saturated azure (`#008cff`) reserved for the primary action (filled tab, active ring)
- Promo/active-filter blue (`#0077fe`) as the highlight companion to the primary azure
- Near-black (`#000000`) body text with deep-navy (`#101a2e`) and card-navy (`#1c2638`) headings
- Flat depth: essentially no drop shadows; tinted `#f5f8fa` / `#f4f8f9` surfaces + `#919ba5` hairlines separate
- Pill-and-card geometry — 30px filter pills, 12–16px cards, 5px compact tabs
- Faint blue-grey (`#c1cad2`) for inactive/de-emphasized headings; slate `#5f6976` for secondary body
- Rebranded logo spectrum: bright blue `#1d99ff` → indigo `#163bd8` → purple `#a164f9`

### Principles

Principle 2's stem — *"Simple, Familiar, Long Awaited"* — is quoted from Payhere's own welcome-kit design concept. The other numbered stems, and every *UI implication* below, are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification. The source's own HTML comment names "one action, one color" and "flat and fast as a rejection of legacy POS chrome" as editorial readings connecting observed design to positioning, not directly sourced Payhere statements.

1. **The store comes first.** Every product decision is framed around a merchant running a shop, not a financial abstraction. *UI implication:* label features as store operations (테이블 오더, 카드 단말기) and lead with concrete price/rank, not fintech terminology.
2. **Simple, familiar, long-awaited.** Payhere's own design concept — *"Simple, Familiar, Long Awaited"* — guides its brand work. *UI implication:* keep layouts uncluttered, use conventional patterns owners already recognize, and make each surface feel like a welcome upgrade.
3. **One action, one color.** Azure (`#008cff`) means "do this." *UI implication:* reserve the saturated azure for the primary tab/CTA and the active ring so the next step is never ambiguous; promo blue (`#0077fe`) handles secondary highlights.
4. **Flat and fast.** Mobile-native clarity beats decorative depth. *UI implication:* no drop shadows; separate with tint (`#f5f8fa`) and `#919ba5` hairlines; the only elevation cue is a 1px azure inset ring.
5. **Bold where it persuades, quiet where it informs.** *UI implication:* Noto Sans KR Bold (700) for headlines that sell the "new future"; weight 400 for the dense operational text that runs the store.

### Application rules

The source states these eight as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

- Use Noto Sans KR Bold (weight 700) for all display headlines — it's the brand's declarative voice
- Use Noto Sans KR weight 400 at 16px for body, nav, and filter-pill labels
- Reserve azure (`#008cff`) for the primary action — the filled tab and the active ring
- Use the promo blue (`#0077fe`) for active filter pills and promo banners
- Use deep navy (`#101a2e`) and card navy (`#1c2638`) for headings; black (`#000000`) for body
- Separate sections with flat tinted surfaces (`#f5f8fa` / `#f4f8f9`) and `#919ba5` hairlines, not shadows
- Dim inactive headings to faint blue-grey (`#c1cad2`) instead of changing hue
- Use pill geometry (30px) for filter chips and 8–16px radii for cards

### Avoid

The source states these seven as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

- Add drop shadows for elevation — Payhere is a flat, near-shadowless system
- Spread azure (`#008cff`) across many elements — it dilutes the single-action signal
- Set headlines in a light weight — display is always Bold (700)
- Use sharp square corners on interactive chrome — tabs are 5px, pills 30px, cards 8–16px
- Introduce a competing saturated hue for actions — blue owns interaction; coral/green are decorative only
- Use faint grey (`#c1cad2`) for active content — it reads as disabled/inactive
- Mix Pretendard into the marketing homepage — that face belongs to the tech blog

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Role names below are the source's own labels. Pairing each hex to the token-set path named beside it, taking those role names from the source's own labels, keeping catalog `primary_color` `#008cff` beside `tokens.colors.primary` `#008cff`, keeping `tokens.colors.canvas` `#ffffff` unmerged from `tokens.colors.on-primary` `#ffffff`, keeping `tokens.colors.surface` `#f5f8fa` unmerged from `tokens.colors.surface-alt` `#f4f8f9`, keeping `tokens.colors.primary` `#008cff` unmerged from `tokens.colors.primary-strong` `#0077fe` and from the three logo-mark keys, keeping the hero H2 live `rgb(0,0,0)` on black rather than on the ink navy key, and attaching every role to the homepage live-extract rather than relabeling a logo fill as an action color, are derived editorial implementation inferences from the verified surfaces; they are not Payhere-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

- **Payhere Azure** (`#008cff`): Primary interactive color. The fill on the filled primary product tab (white text) and the 1px inset ring on the active outline tab — the system's single "action" color. Token-set path `tokens.colors.primary`. Catalog identity `primary_color` is the same hex on a second writing.
- **Promo Blue** (`#0077fe`): The highlight companion — the top promo-banner background and the active-state fill on feature filter pills (e.g. "NFC", "리뷰 마케팅"). Token-set path `tokens.colors.primary-strong`.
- **Brand Blue** (`#1d99ff`): The brightest blue in the rebranded logo mark. Token-set path `tokens.colors.brand-blue`.
- **Brand Indigo** (`#163bd8`): The deep-blue element of the logo mark. Token-set path `tokens.colors.brand-indigo`.
- **Brand Purple** (`#a164f9`): The purple accent in the logo mark, giving the identity its blue-to-purple spectrum. Token-set path `tokens.colors.brand-purple`.
- **Deep Navy** (`#101a2e`): Deep-navy heading color — warm, trustworthy, used instead of pure black on many headings. Token-set path `tokens.colors.ink`. Live inspect of the hero H2 "모바일 포스 1위 / 매장의 새로운 미래, 페이히어" records `color rgb(0,0,0)`; that hero observation stays on black, not on this navy key.
- **Card Navy** (`#1c2638`): Slightly softer navy for product-category card labels. Token-set path `tokens.colors.ink-card`.
- **Pure Black** (`#000000`): The document default body text color in Noto Sans KR. Token-set path `tokens.colors.ink-pure`.
- **Body Slate** (`#5f6976`): Secondary body copy, descriptions, supporting text. Token-set path `tokens.colors.body`.
- **Muted Grey** (`#919ba5`): Hairlines, card borders, and muted/tertiary text. Token-set path `tokens.colors.muted`.
- **Faint Blue-Grey** (`#c1cad2`): Inactive/de-emphasized headings and lowest-emphasis labels. Token-set path `tokens.colors.faint`. Inactive H3 "영수증 인쇄" computes 32px / 700 / `#c1cad2`.
- **Pure White** (`#ffffff`): Page background, white cards, and text on azure/dark (`on-primary`). Token-set path `tokens.colors.canvas`. `tokens.colors.on-primary` is the same hex on a second key.
- **Surface Grey** (`#f5f8fa`): Cool-grey tinted surface for product-selector cards and segmented sections. Token-set path `tokens.colors.surface`.
- **Surface Alt** (`#f4f8f9`): A near-identical warmer grey for alternating blocks. Token-set path `tokens.colors.surface-alt`.
- **Accent Coral** (`#ff5b46`): A warm coral-red used sparingly for decorative highlights and section punctuation. Token-set path `tokens.colors.accent-coral`.
- **Accent Green** (`#08d07e`): A vivid mint-green used for positive/decorative accents. Token-set path `tokens.colors.accent-green`.

YAML `tokens.note` records the same split in producer language: primary = live interactive azure (`#008cff` — filled primary CTA + active tab ring); promo/active-filter blue (`#0077fe`); logo mark spans bright blue `#1d99ff`, indigo `#163bd8`, purple `#a164f9`. Near-black body text (`#000000`); navy card labels (`#1c2638`) and deep-navy headings (`#101a2e`).

### Spacing

Token-set path `tokens.spacing`, unitless steps as the YAML recorded them. Source §5 also writes a base unit of 4px and the scale 4px, 8px, 12px, 16px, 20px, 24px, 48px.

| Step | Value | Token-set path |
|---|---:|---|
| xs | 4 | `tokens.spacing.xs` |
| sm | 8 | `tokens.spacing.sm` |
| md | 12 | `tokens.spacing.md` |
| base | 16 | `tokens.spacing.base` |
| lg | 20 | `tokens.spacing.lg` |
| xl | 24 | `tokens.spacing.xl` |
| xxl | 48 | `tokens.spacing.xxl` |

`tokens.spacing.sm: 8` is not the device-option card radius `8px`. `tokens.spacing.md: 12` is not `tokens.rounded.md: 12` and is not the filter-pill padding `12px`. `tokens.spacing.base: 16` is not `tokens.rounded.lg: 16`, not the 16px type roles, and not the tab padding `16px`. `tokens.spacing.lg: 20` is not the product-card padding `20px`. `tokens.spacing.xl: 24` is not `tokens.rounded.xl: 24` and is not the filter-pill padding `24px`. `tokens.spacing.xxl: 48` is the YAML xxl step. Keeping each number on its own key path, rather than treating a shared numeral as the same token, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded`, unitless steps as the YAML recorded them. Source §5 also writes the prose radius scale Compact (5px), Standard (8px), Comfortable (12px), Large (16px), Extra (24px), Pill (30px), Full (9999px).

| Step | Value | Token-set path |
|---|---:|---|
| sm | 5 | `tokens.rounded.sm` |
| md | 12 | `tokens.rounded.md` |
| lg | 16 | `tokens.rounded.lg` |
| xl | 24 | `tokens.rounded.xl` |
| pill | 30 | `tokens.rounded.pill` |
| full | 9999 | `tokens.rounded.full` |

`tokens.rounded.sm: 5` is the YAML compact-tab step; header tabs record `5px` on those controls. `tokens.rounded.md: 12` is not `tokens.spacing.md: 12`. `tokens.rounded.lg: 16` is not `tokens.spacing.base: 16`. `tokens.rounded.xl: 24` is not `tokens.spacing.xl: 24`. `tokens.rounded.pill: 30` is the YAML pill step; filter pills record `30px` on those controls. `tokens.rounded.full: 9999` is a YAML step for circular badges and avatars. The device-option card records `8px`; that `8px` is this control's geometry and the source's Standard (8px) prose step. It is not a YAML rounded key. Keeping those local radii on their components, and keeping each YAML step on its own key path, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Tint (Level 1) | `#f5f8fa` / `#f4f8f9` background shift | Card/section separation without elevation |
| Hairline (Level 2) | `1px solid #919ba5` border | White device-card outlines, dividers |
| Ring (Focus/Active) | `#008cff 0px 0px 0px 1px inset` | Active outline tab / selected state |

YAML `tokens.shadow.none` is `none`. YAML `tokens.shadow.ring` is `#008cff 0px 0px 0px 1px inset`. Live inspection found `box-shadow: none` across the hero, section headings, and product cards; the only elevation cue in the interactive chrome is a 1px inset azure (`#008cff`) ring on the active tab.

The following elevation reading, including near-shadowless, tint-and-hairline grouping, and "when emphasis is needed, the system reaches for color — the azure `#008cff` fill or the promo blue `#0077fe` — never a drop shadow", is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. Payhere is a near-shadowless, flat system. Depth and grouping come from flat tinted surfaces (`#f5f8fa`, `#f4f8f9`) and thin `#919ba5` hairlines. This keeps the merchant UI feeling clean, fast, and mobile-native.

### Motion

Source-stated duration roles. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list. Treating §15 as philosophy-layer rather than live-inspect, treating the duration table, easing names, no-bounce / merchant-tooling-reliability readings, and reduced-motion line as source-stated rather than computed CSS, and treating the omitted `ease-exit` curve as matching the legacy spec-template `ease-exit` example, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, tab/pill press, focus ring |
| `motion-standard` | 200ms | Card/section reveal, sheet, dropdown |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, pills, sheets |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including functional-and-quiet, no-bounce / no-spring, merchant-tooling-reliability, and reduced-motion-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification. Motion is functional and quiet — consistent with the flat, fast aesthetic. Tabs and filter pills respond to press with a subtle scale/opacity shift and the active azure inset ring settling in; product cards fade-in from below at `motion-standard / ease-enter`. No bounce or spring — merchant tooling signals reliability, not playfulness. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant; the product remains fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, an animation name, a CSS transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition. Holding that five-kind per-component gate, and omitting the three unattributed cubic-bezier values rather than promoting them, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, and the resolution in each cell, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | This record's evidence is live inspection of the homepage and the official tech blog; it carries no Payhere-published type specification. |
| Live computed surface-use | Homepage hero H2 "모바일 포스 1위 / 매장의 새로운 미래, 페이히어" computes as Noto Sans KR 44px / weight 700 / `color rgb(0,0,0)`. Section H3 "POS 일체형" computes 32px / 700. Inactive H3 "영수증 인쇄" computes 32px / 700 / `#c1cad2`. Body, nav, buttons, and filter pills sit at 16px Noto Sans KR. Tech-blog article H1 computes as Pretendard 48px / 700. |
| Official distributed asset | The record establishes the families in use and carries no Payhere-distributed font file. |
| Declared fallback | `Noto Sans KR Fallback` is named beside the homepage primary family. That face is fallback context, not the brand face. |
| License | No license or distribution statement accompanies the families in this record. Pretendard's upstream project is a font asset, not a Payhere brand asset. |
| Outside these captures | Surfaces other than `https://payhere.in/` and `https://tech.payhere.in/` contributed no type value here. |

### Family

- **Primary:** `Noto Sans KR` (with `Noto Sans KR Fallback`) — YAML `tokens.typography.family.primary`. The document default across the marketing homepage; carries headlines, nav, buttons, and body.
- **Blog / Companion:** `Pretendard` — YAML `tokens.typography.family.blog`. Used on the official Payhere Tech Blog (`tech.payhere.in`) for long-form article typography.

Calling Noto Sans KR the document default and confining Pretendard to the long-form tech blog, and refusing to present `Noto Sans KR Fallback` as the brand face, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. The family names and the homepage/blog split are the source's own.

Do not substitute a system font or another family for Noto Sans KR and present it as the homepage face. Do not mix Pretendard into the marketing homepage. Do not replace an unavailable or unobserved brand type with Pretendard. Refusing a system-font or Pretendard substitute for the homepage face, and refusing to mix Pretendard into the marketing homepage, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

### Type roles

YAML unitless line heights stay ratios (A1a). The parenthetical rem figures and the `~` spellings are the source §3 writing, not a replacement of the YAML ratio. Token-set `use` strings are kept verbatim. Keeping YAML line heights as unitless ratios, keeping the YAML `use` strings and the §3 notes as two writings, and keeping body `16` as a type size rather than `tokens.spacing.base: 16`, are derived editorial implementation inferences from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height (YAML) | Token-set path | Token-set use | §3 notes |
|---|---|---:|---:|---|---|---|---|
| Display Hero | Noto Sans KR | 44px (2.75rem) | 700 | 1.40 | `tokens.typography.display-hero` | Hero headline, Noto Sans KR Bold | ~1.40. Hero headline ("매장의 새로운 미래, 페이히어") |
| Section Heading | Noto Sans KR | 32px (2.00rem) | 700 | 1.50 | `tokens.typography.section` | Section / feature headings (POS 일체형) | ~1.50. Feature/section titles ("POS 일체형") |
| Card Title | Noto Sans KR | 26px (1.63rem) | 700 | 1.30 | `tokens.typography.card-title` | Product-category card labels (카드 단말기) | ~1.30. Product-category card labels |
| Button / Active Tab | Noto Sans KR | 16px (1.00rem) | 700 | 1.00 | `tokens.typography.button` | Filled primary CTA / active tab label | 1.00. Filled primary CTA, active tab label |
| Body / Nav | Noto Sans KR | 16px (1.00rem) | 400 | 1.50 | `tokens.typography.body` | Body / UI / nav text, Noto Sans KR | 1.50. Standard reading, nav links, filter pills |
| Blog Hero | Pretendard | 48px (3.00rem) | 700 | 1.30 | `tokens.typography.blog-hero` | Tech-blog article H1, Pretendard | ~1.30. Tech-blog article H1 |

The following type-hierarchy readings are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

- **Bold display, quiet body**: weight 700 carries every headline; weight 400 carries every paragraph and UI label. The weight jump is the primary hierarchy signal.
- **Contrast, not color, for state**: inactive headings dim to faint blue-grey (`#c1cad2`) rather than shifting hue — active items stay full-strength navy or azure.
- **Hangul-first uniformity**: body, nav, and buttons all sit at a single 16px in Noto Sans KR, keeping information-dense merchant screens legible and consistent.
- **One display family**: Noto Sans KR owns the homepage; Pretendard is confined to the long-form tech blog and never mixes into the marketing chrome.

### Assets

- Catalog identity pointer: `logo.type: favicon`, `logo.slug: https://www.google.com/s2/favicons?domain=payhere.in&sz=128`. That is an identity pointer, not a Payhere-hosted brand file.
- Header logo SVG fills recorded on the homepage: `#1d99ff` (bright blue), `#163bd8` (indigo), `#a164f9` (purple) — the rebranded blue-to-purple mark.
- `document.title`: "페이히어｜매장의 새로운 미래".

Reading the Google s2 favicon slug as an identity pointer rather than as a Payhere-hosted brand file, and reading the three SVG fills as the header logo mark rather than as action colors, are derived editorial implementation inferences from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The live inspection recorded default computed styles on the homepage. Hover, tab/pill press, and focus ring are named in this record's motion rules (`motion-fast` 120ms), but no computed value accompanies them, so those visual treatments are omitted here. Omitting those visual treatments for want of a computed value, rather than inventing a hover or focus-visible treatment, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

The source state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| **Empty (no sales/transactions yet)** | White canvas. Single deep-navy (`#101a2e`) line at 16px explaining nothing has been recorded yet, with one azure (`#008cff`) CTA to get started. No illustration clutter. |
| **Empty (no saved items)** | Muted slate (`#5f6976`) single line: nothing here yet, plus a path back. Honest, calm. |
| **Loading (data fetch)** | Skeleton cards on `#f5f8fa` tinted surface at final 12px-radius dimensions. Flat pulse, no shadow shimmer — consistent with the shadowless system. |
| **Loading (in-place refresh)** | Subtle azure (`#008cff`) progress cue; previous values stay visible during refresh. |
| **Error (request failed)** | Inline message in deep navy with a plain-language explanation and a retry. No bare "오류가 발생했습니다" — states what to do next. |
| **Error (form validation)** | Field-level message below the input describing what's valid, not just "필수". |
| **Success (action complete)** | Brief inline confirmation in a calm tone; next-step detail linked immediately below. No celebratory emoji. |
| **Skeleton** | `#f5f8fa` blocks at final dimensions, 12px radius, flat pulse. |
| **Disabled** | Faint blue-grey (`#c1cad2`) text on reduced-opacity surface; azure actions fade rather than turn grey to preserve brand read. |

The nine state treatments above are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification. They compose values established elsewhere in this contract, and no computed per-component observation accompanies them.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control's product role, not its primitive kind. `not-applicable` is used only where the control's role makes the state meaningless — a product tab, a destination login link, or a filter pill that commits no operation in place — and the reason given is always that semantic one. Where the source supplies no interactive-kind evidence for a row at all, its kind and applicability map are omitted rather than decided. YAML `type` is recorded as `Primitive type` only on components that carry that key in the token set. §4-only rows are labelled `not in the token set`. Preserving the source state contract here rather than delegating it to an unadopted catalog graph, the role-based decision procedure above, every interactive-kind verdict, every applicability verdict, the reason given for either, and the refusal to treat this as a complete state-coverage claim, are derived editorial implementation inferences from the verified surfaces; they are not Payhere-authored or a separately published UI specification. This is not a complete state-coverage claim.

### Filled Primary Tab

- Role: filled primary product tab in the header
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Label: "카드 단말기"
- Background: `#008cff`
- Text: `#ffffff`
- Radius: 5px
- Padding: 0px 16px (YAML `0 16px`)
- Height: 40px
- Font: 16px / 700. 16px Noto Sans KR weight 700
- Token-set path: `tokens.components.tab-filled`
- Token-set use: `Filled primary product tab (카드 단말기)`
- Use: Filled primary product tab in the header ("카드 단말기") — the system's single filled action
- Observed: default only
- The `5px` radius is this tab's geometry. It is not only `tokens.rounded.sm: 5`. The `16px` in the padding is this control's padding. It is not `tokens.spacing.base: 16`. The 16px / 700 font is this control's font, not only the Button type-role row. Reading those figures as this tab's geometry rather than as those YAML steps or type-role rows is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the homepage header |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A product tab can be gated; visual treatment omitted |
| loading | not-applicable | Product tab; it selects a category and commits no operation in place |
| error | not-applicable | Product tab; it reports no request or validation failure |
| success | not-applicable | Selecting a product category is not an action-outcome confirmation on the tab |

### Outline / Active Tab

- Role: selected/active outline product tab
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Label: "테이블 오더"
- Background: `#ffffff`
- Text: `#008cff`
- Radius: 5px
- Padding: 0px 16px (YAML `0 16px`)
- Height: 40px
- Font: 16px / 700. 16px Noto Sans KR weight 700
- Active: 1px `#008cff` inset ring (box-shadow inset). YAML `states`: `1px #008cff inset ring`
- Token-set path: `tokens.components.tab-outline`
- Token-set use: `Selected/active outline tab (테이블 오더)`
- Use: Selected/active outline tab ("테이블 오더")
- Observed: default / selected
- The inset ring is this tab's selected treatment and `tokens.shadow.ring`. The `5px` radius is this tab's geometry. Reading those figures as this tab's geometry is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the homepage header as the selected outline tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A product tab can be gated; visual treatment omitted |
| loading | not-applicable | Product tab; it selects a category and commits no operation in place |
| error | not-applicable | Product tab; it reports no request or validation failure |
| success | not-applicable | Selecting a product category is not an action-outcome confirmation on the tab |

### Login Link Button

- Role: header login link
- not in the token set
- Kind: interactive
- Anatomy: label
- Label: "로그인"
- Background: `#ffffff`
- Radius: 5px
- Padding: 0px 12px
- Height: 40px
- Font: 16px Noto Sans KR weight 400
- Use: Header login link ("로그인")
- Observed: default only
- Text color is unnamed on this control. The `5px` radius is this control's geometry. Reading the unnamed text color as omitted rather than filled from another ink key is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the homepage header |
| hover | applicable | Pointer-web destination link; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A login entry can be unavailable; visual treatment omitted |
| loading | not-applicable | Destination login link; the control itself commits no operation in place |
| error | not-applicable | Destination login link; it reports no request or validation failure |
| success | not-applicable | Reaching login is navigation, not an action-outcome confirmation on the link |

### Active Filter Pill

- Role: selected feature filter pill
- Primitive type: `badge` · Kind: interactive
- Anatomy: label
- Labels: "NFC", "리뷰 마케팅"
- Background: `#0077fe`
- Text: `#000000`
- Radius: 30px
- Padding: 12px 24px
- Height: 56px
- Font: 16px / 400. 16px Noto Sans KR weight 400
- Token-set path: `tokens.components.filter-pill`
- Token-set use: `Active feature filter pill (NFC, 리뷰 마케팅)`
- Use: Selected feature filter pill ("NFC", "리뷰 마케팅")
- Observed: default / selected
- The `30px` radius is this pill's geometry. It is not only `tokens.rounded.pill: 30`. The `12px` and `24px` in the padding are this control's padding. They are not `tokens.spacing.md: 12` or `tokens.spacing.xl: 24`. The 16px / 400 font is this control's font, not only the Body type-role row. Reading those figures as this pill's geometry rather than as those YAML steps or type-role rows is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the homepage as the selected filter |
| hover | applicable | Pointer-web filter; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A filter can be unavailable; visual treatment omitted |
| loading | not-applicable | Feature filter; it toggles a selection and commits no operation in place |
| error | not-applicable | Feature filter; it reports no request or validation failure |
| success | not-applicable | Toggling a filter is not an action-outcome confirmation on the pill |

### Default Filter Pill

- Role: unselected feature filter pill
- not in the token set
- Kind: interactive
- Anatomy: label
- Labels: "비대면 결제", "선불권", "단독 결제"
- Background: `#ffffff`
- Text: `#000000`
- Radius: 30px
- Padding: 12px 24px
- Height: 56px
- Font: 16px Noto Sans KR weight 400
- Use: Unselected feature filter pill ("비대면 결제", "선불권", "단독 결제")
- Observed: default only
- The `30px` radius is this pill's geometry. Reading those figures as this pill's geometry rather than as `tokens.rounded.pill: 30` applied to every unlisted control is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the homepage as the unselected filter |
| hover | applicable | Pointer-web filter; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A filter can be unavailable; visual treatment omitted |
| loading | not-applicable | Feature filter; it toggles a selection and commits no operation in place |
| error | not-applicable | Feature filter; it reports no request or validation failure |
| success | not-applicable | Toggling a filter is not an action-outcome confirmation on the pill |

### Product-Category Card

- Role: product-category selector card
- Primitive type: `card`
- Kind: omitted. The record establishes geometry and a selector use string, and no button or link type, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Labels: "카드 단말기", "테이블 오더", "키오스크", "인터넷 패키지"
- Background: `#f5f8fa`
- Text: `#1c2638`
- Radius: 12px
- Padding: 20px 0px (YAML `20px 0`)
- Height: 84px
- Font: 26px / 700. 26px Noto Sans KR weight 700
- Token-set path: `tokens.components.product-card`
- Token-set use: `Product-category selector card (카드 단말기 / 테이블 오더 / 키오스크)`
- Use: Product-category selector card ("카드 단말기", "테이블 오더", "키오스크", "인터넷 패키지")
- The `12px` radius is this card's geometry. It is not only `tokens.rounded.md: 12`. The `20px` in the padding is this control's padding. It is not `tokens.spacing.lg: 20`. The 84px height is the §4 writing; YAML does not record a height on this key. Reading those figures as this card's geometry, and keeping YAML `use` beside the longer §4 label list, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

### Device-Option Card

- Role: device-option card
- Primitive type: `card`
- Kind: omitted. The record establishes geometry and no interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Labels: "갤럭시", "아이폰·아이패드", "윈도우"
- Background: `#ffffff`
- Text: `#000000`
- Border: 1px solid `#919ba5`
- Radius: 8px
- Height: 72px
- Token-set path: `tokens.components.device-card`
- Token-set use: `Device-option card (갤럭시 / 아이폰·아이패드 / 윈도우)`
- Use: Device-option card ("갤럭시", "아이폰·아이패드", "윈도우")
- The `8px` radius is this card's geometry and the source's Standard (8px) prose step. It is not a YAML rounded key. The 72px height is this card's geometry. Reading those figures as this card's geometry is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

### Product-nav tab

- Role: top product-nav tab active state
- Primitive type: `tab` · Kind: interactive
- Anatomy: label
- Background: `#ffffff`
- Text: `#000000`
- Font: 16px / 700. 16px Noto Sans KR
- Active: text `#008cff`. Source §4 also writes azure `#008cff` text / ring on the selected product tab
- Token-set path: `tokens.components.section-tab`
- Token-set use: `Product-nav tab active state`
- Use: Top product-nav tabs ("카드 단말기" / "테이블 오더")
- Observed: default; active text `#008cff`
- YAML records `fg` / `font` / `active` on this key and does not record height, padding, or radius here. Those measurements stay on Filled Primary Tab and Outline / Active Tab. Keeping this row's active-text writing beside those two button records, rather than copying their height onto this key, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the homepage header |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A nav tab can be unavailable; visual treatment omitted |
| loading | not-applicable | Product-nav tab; it selects a destination area and commits no operation in place |
| error | not-applicable | Active versus inactive is the tab's whole meaning; it reports no request or validation failure |
| success | not-applicable | Reaching a product area is navigation, not an action-outcome confirmation on the tab |

Additional observed state: active — text `#008cff` / ring on the selected product tab.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

### Spacing and container

The recorded spacing scale runs 4px, 8px, 12px, 16px, 20px, 24px, 48px on a 4px base unit. Filter pills use a generous 12px 24px padding at 56px height for a comfortable tap target; product cards use 20px vertical padding. Centered marketing layout with a 44px hero headline as the anchor. Product-category cards arranged in a horizontal row of tinted `#f5f8fa` tiles (12px radius). Feature filter pills wrap in a horizontal pill row beneath section headings. Device-option cards form a 3-up grid of white tiles with `#919ba5` hairline borders (8px radius). Alternating white (`#ffffff`) and tinted-grey (`#f5f8fa` / `#f4f8f9`) full-width bands.

The following whitespace reading — "Breathing room over density", "Flat segmentation", "Pill rhythm", and calling the 12px 24px / 56px pill padding comfortable — is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

- **Breathing room over density**: despite being merchant tooling, the marketing surface is airy with generous vertical rhythm between sections.
- **Flat segmentation**: sections separate by background tint (`#f5f8fa` vs `#ffffff`) and hairlines, not by shadow.
- **Pill rhythm**: the repeated 30px-radius filter pill creates a consistent horizontal cadence across feature lists.

### Border radius (prose scale)

Source §5 writes this prose scale beside the YAML rounded keys. Keeping the Standard (8px) row on device-option cards rather than inventing a YAML rounded key for `8`, and keeping Compact / Comfortable / Large / Extra / Pill / Full as the source's names for the YAML steps, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

- Compact (5px): header tabs, small buttons
- Standard (8px): device-option cards, inner elements
- Comfortable (12px): product-category cards — the workhorse
- Large (16px): larger content cards
- Extra (24px): feature panels, rounded section corners
- Pill (30px): feature filter pills
- Full (9999px): circular badges and avatars

### Responsive behavior

The breakpoint table and the collapsing rules below are the source's own §8 writing. Treating them as source-stated layout rules for the captured marketing homepage rather than as a newly measured cross-viewport specification is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, hero headline compresses, product cards stack, pills wrap |
| Tablet | 640-1024px | Moderate padding, 2-up product/device cards |
| Desktop | 1024-1440px | Full layout, centered hero, multi-column card rows |

- Hero: 44px Noto Sans KR headline scales down on mobile, weight 700 maintained
- Product-category card row: multi-column → stacked single column
- Filter-pill row: horizontal wrap/scroll on narrow viewports
- Tinted/white alternating bands maintain full-width treatment

### Touch targets

- Filter pills at 56px height with 12px 24px padding — comfortably tappable
- Header tabs at 40px height, device cards at 72px — generous merchant-friendly targets
- Product-category cards at 84px height for confident tapping

Calling those heights comfortable, generous, merchant-friendly, or confident is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. The heights themselves are recorded component measurements.

### Image behavior

- Product/device shots carry no shadow at any size, consistent with the flat system
- Cards maintain 12–16px radius across breakpoints

Reading the no-shadow imagery rule as consistent with the flat system, and keeping 12–16px radius across breakpoints, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. The geometry values themselves are recorded component measurements.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Voice samples

These strings are verbatim live copy from the captured surfaces. The parenthetical labels — rank + mission, all-in-one capability, brand statement — are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification.

- "모바일 포스 1위 매장의 새로운 미래, 페이히어" — hero headline (rank + mission). *(verified live 2026-07-02, payhere.in)*
- "인쇄·스캔·키오스크 다 되는 올인원 단말기, 페이히어 터미널" — section headline (all-in-one capability). *(verified live 2026-07-02, payhere.in)*
- "페이히어는 매장의 새로운 미래를 만듭니다. (We Make The Future)" — brand statement. *(verified 2026-07-02, tech.payhere.in welcome-kit post)*

### Voice and tone

The voice reading below, including the tone table, is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification. Payhere's voice is **confident, plainspoken, and merchant-first** — a partner that helps small-business owners run their store, framed as "매장의 새로운 미래" ("the store's new future"). The register is benefit-led and concrete: it leads with price, rank, and capability ("모바일 포스 1위" — "No.1 mobile POS"), then names features in plain Korean nouns. It talks to a café or restaurant owner, not a payments engineer.

| Context | Tone |
|---|---|
| Hero headlines | Declarative, mission-framed. "매장의 새로운 미래, 페이히어." Confident, rank-forward. |
| Product / feature labels | Plain functional nouns. "카드 단말기", "테이블 오더", "키오스크", "인터넷 패키지". |
| CTAs | Direct and value-led. "인터넷+CCTV+단말기 다 해도 3만 원대", "업계 최저가 도전". |
| Feature descriptions | Capability-first, jargon decoded into store operations (인쇄·스캔·키오스크). |
| Internal / brand copy | Warm and welcoming — "빛나는 여정을 함께해요" (welcome-kit), a journey framing. |

### Forbidden register

The exclusions below are a derived editorial implementation inference from the verified surfaces; they are not Payhere-authored or a separately published UI specification. Intimidating financial jargon left unexplained, cold institutional banking tone, fear-based urgency. Payhere keeps it merchant-friendly and concrete.

### Locale

Payhere is a Korean company. Its name is written 페이히어 in Korean, and the captured homepage is hangul-first. Hangul-first sizing at 16px body is recorded under Typography. Treating that hangul-first sizing as recorded under Typography rather than as a complete locale profile is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Reading the list as a catalog of unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Payhere-authored or a separately published UI specification.

- the exact `ease-enter`, `ease-exit`, and `ease-standard` curves; the token names and uses are recorded, the curves carry no attribution
- hover, tab/pill-press, and focus-ring visual treatments, which the motion rules name (`motion-fast` 120ms) without any accompanying computed value
- the interactive kind of the Product-Category Card and the Device-Option Card
- computed per-component values behind the empty, loading, error, success, skeleton, and disabled treatments described above
- Login Link Button text color
