# Cohere Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating Cohere as an enterprise AI company that builds foundation models and end-to-end AI products for business use, and treating catalog homepage `https://cohere.com` as identity rather than a fourth captured host beyond the three named Korean-route URLs below, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Cohere is an enterprise AI company that builds foundation models and end-to-end AI products for business use. Catalog homepage identity is `https://cohere.com`.

Treating the following three URLs as the named evidence domains of this reconstruction, including this-contract-covers-three-current-first-party-public-Korean-routes-from-the-2026-07-13-packet, values-stay-attached-to-the-surface-that-established-them, public-Korean-home-not-a-stand-in-for-public-pricing, public-pricing-not-a-stand-in-for-public-products, public-products-not-a-stand-in-for-home-or-pricing, and signed-in-application-UI-and-documentation-chrome-stay-outside-this-reconstruction, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. This contract covers three current first-party public Korean routes from the 2026-07-13 packet: public marketing home `https://cohere.com/ko` (marketing), public pricing `https://cohere.com/ko/pricing` (product-pricing), and public products `https://cohere.com/ko/products` (product-marketing). Color, type, and component values below stay attached to the surface that established them. The public Korean home is not a stand-in for public pricing. Public pricing is not a stand-in for public products. Public products are not a stand-in for home or pricing. Signed-in application UI and documentation chrome stay outside this reconstruction.

Source token note: Only selector-backed observations from Cohere's public Korean marketing, pricing, and products routes are tokens. The capture contains no documentation chrome or authenticated product UI; declared-only font assets remain outside typography tokens. Treating that note as a register split — `#17171c` is catalog `primary_color` and the observed dark pill background rather than a universal action fill and rather than Foreground `#000000` or Foreground soft `#212121`; `#ffffff` canvas is unmerged from Inverse text `#fafafa` and from dark-CTA on-fill `#ffffff` as a component field; Unica77 Cohere Web is the repeated public UI family rather than CohereText display or CohereMono labels; declared-only CohereColor / CohereHeadline / icon / variable / KaTeX faces stay unpromoted; and signed-in product UI and documentation chrome stay unpromoted — is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification.

The following atmosphere readings — business-first-posture-as-a-quiet-high-contrast-interface, white-canvases-black-and-charcoal-text-occasional-inverse-panels-and-rounded-pill-actions, CohereText-only-for-a-small-display-tier, loaded-Unica77-Cohere-Web-as-the-repeated-navigation-body-card-input-tab-and-action-rhythm, restrained-rather-than-decorative, and large-editorial-headlines-introduce-practical-enterprise-product-and-pricing-content — are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification. On the three supplied public Korean routes, that business-first posture appears as a quiet, high-contrast interface: white canvases, black and charcoal text, occasional inverse panels, and rounded pill actions. The capture records CohereText only for a small display tier, while the loaded Unica77 Cohere Web family carries the repeated navigation, body, card, input, tab, and action rhythm. The result is restrained rather than decorative: large editorial headlines introduce practical enterprise product and pricing content.

Treating this reconstruction as a public-surface reference, including signed-in-product-UI-and-documentation-chrome-stay-outside-the-supplied-capture and retain-only-the-selectors-routes-and-states-in-the-supplied-capture, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. This is a public-surface reference. Signed-in product UI and documentation chrome stay outside the supplied capture. The components and tokens below retain only the selectors, routes, and states in the supplied capture.

Treating the following public-history facts as narrative rather than interface tokens — 2019-Toronto-founding-to-scale-intelligence-in-service-of-humanity, named-co-founders-Aidan-Gomez-Nick-Frosst-and-Ivan-Zhang, current-enterprise-focus-on-judgment-execution-and-extending-what-teams-can-achieve, 2025-North-as-a-turnkey-workplace-platform-using-agentic-capabilities-and-retrieval-with-enterprise-data, private-secure-customizable-deployment-across-a-customer’s-infrastructure, About-page-framing-around-improving-human-wellbeing-by-helping-organizations-scale-innovation-productivity-and-progress, and official-About-Careers-and-homepage-writing-as-narrative-context-not-interface-tokens — is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Cohere says it was founded in Toronto in 2019 to scale intelligence in service of humanity. Its official About page identifies co-founders Aidan Gomez, Nick Frosst, and Ivan Zhang, and describes a current enterprise focus on helping teams improve judgment, accelerate execution, and extend what they can achieve. The same source places the 2025 launch of North in that evolution: a turnkey workplace platform using agentic capabilities and retrieval with enterprise data. The company’s current public positioning centers on private, secure, customizable deployment across a customer’s infrastructure. Official About, Careers, and homepage writing provide that narrative context; they do not by themselves supply interface tokens. Treating those statements as explaining a product/category relationship, keeping public marketing, public pricing, and public product marketing distinct from signed-in product UI, and treating the sober public presentation as not an all-purpose visual color story, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Those statements explain the product/category relationship. Public marketing, public pricing, and public product marketing stay distinct from signed-in product UI. That narrative explains the sober public presentation more reliably than the older snapshot’s unsupported claims about an all-purpose visual color story.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML / §4 Use strings below as Primary tasks — Use-a-public-dark-marketing-CTA-on-home-pricing-and-products, Switch-the-selected-public-pricing-tab, and Scan-the-home-marketing-card-container — and not lifting source §13 stakeholder groups, and treating independently-verified-in-this-packet as the three captured public Korean URLs rather than the harvested CTA / tab / card uses, which remain harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Independently verified in this packet are the three captured public Korean URLs; named CTA, tab, and media-card uses are harvested strings/controls, not independently verified destination routes.

- Use a public dark marketing CTA on home, pricing, and products (Use: Home, pricing, and products public CTA; evidence `home::[data-omd-capture="53"]`).
- Switch the selected public pricing tab (YAML use: `Observed selected public pricing tab`).
- Scan the home marketing-card container (YAML use: `Observed home marketing-card container; no fill, border, or state is promoted`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 entries as official public stakeholder groups rather than verified biographical personas, treating those entries as not Audience biographies and not primary tasks, tying observable work only to the three primary tasks, and not fabricating named personas or behavioral metrics from that broad audience evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. No individual personas are promoted. Official public sources identify enterprises, teams, workforces, and customers as the intended stakeholder groups, including organizations that need secure, private, and customizable AI deployment. They do not provide validated individual user personas. This reference intentionally does not fabricate named personas or behavioral metrics from that broad audience evidence.

### Distinctive traits

The following unmerged-role extras — treating `#17171c` as catalog `primary_color` and the dark pill fill rather than Foreground `#000000` or Foreground soft `#212121`, treating `#ffffff` canvas as unmerged from Inverse text `#fafafa` and from dark-CTA on-fill `#ffffff`, treating CohereText display as unmerged from Unica77 Cohere Web UI and from CohereMono labels, treating 9999px pills as unmerged from 22px home media-card and from 0px pricing tab and from 8px route-local dialog and from 4px compact controls, without-a-claim-of-a-universal-card-recipe, and promoting no captured hover-pressed-focus-menu-dialog-toast-or-error state token — are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification.

- White public canvas with black, charcoal, and soft-gray text
- CohereText at large display sizes; loaded Unica77 Cohere Web throughout the observed public UI
- Dark and inverse white 9999px pill actions, backed by multiple public routes
- A 22px rounded home marketing-card container, without a claim of a universal card recipe
- No hover, pressed, focus, menu, dialog, toast, or error state token: the capture recorded no interactions

### Principles

The four numbered items’ source-backed stems cite the official About page (scale intelligence in service of humanity; helping organizations scale innovation, productivity, and progress), public positioning (private, secure, customizable deployment; control over data and infrastructure), and Careers values (Momentum, Openness, and Autonomy). The *UI implication* notes — connect-public-product-messaging-to-human-and-organizational-outcomes-rather-than-novelty-alone, make-security-deployment-and-customization-legible-in-public-information-architecture, make-a-next-action-visible-without-relying-on-unobserved-interaction-effects, and prefer-direct-explanatory-content-and-clear-choice-boundaries-over-opaque-system-behavior — are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification. The capture-bound application list is limited at the grouping below, not here.

1. **Scale intelligence to serve people.** Official About and founding language frames the work around scaling intelligence in service of humanity. *UI implication:* connect public product messaging to human and organizational outcomes rather than novelty alone.
2. **Keep control with the enterprise.** Public positioning centers on private, secure, customizable deployment and control over data and infrastructure. *UI implication:* make security, deployment, and customization legible in public information architecture.
3. **Support momentum.** Careers names Momentum as a company value. *UI implication:* make a next action visible without relying on unobserved interaction effects.
4. **Make room for autonomy and openness.** Careers names Autonomy and Openness as company values. *UI implication:* prefer direct explanatory content and clear choice boundaries over opaque system behavior.

Treating the following as a capture-bound application of source §7 Do’s and the unique source §9 constraints, including loaded-CohereText-Unica77-CohereMono-only-when-the-deployment-can-legally-load-the-named-family, dark-or-inverse-9999px-pills-with-12px-vertical-padding, distinction-between-public-marketing-public-pricing-public-product-marketing-and-route-local-floating-dialog-chrome, 22px-radius-only-for-the-observed-home-media-card-container, public-dark-CTA-`#17171c`-background-`#ffffff`-text-9999px-`12px 24px`-16px-400-Unica77-only-when-the-licensed-font-is-available, inverse-counterpart-only-for-a-dark-public-section, CohereText-72px-400-72px-for-an-observed-public-h1-not-authenticated-product, and pricing-tab-route-local-with-selected-as-the-only-captured-state, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification.

- Use the loaded CohereText / Unica77 / CohereMono hierarchy only when the deployment can legally load the named family.
- Keep the captured public action geometry specific: dark or inverse 9999px pills, with 12px vertical padding.
- Preserve the distinction between public marketing, public pricing, public product marketing, and route-local floating-dialog chrome.
- Use the 22px radius only for the observed home media-card container unless another selector proves a broader rule.
- Build a public marketing CTA with `#17171c` background, `#ffffff` text, 9999px radius, `12px 24px` padding, and 16px / 400 Unica77 Cohere Web only when the licensed font is available.
- Build the inverse counterpart only for a dark public section: `#ffffff` background, `#17171c` text, and the same observed pill geometry.
- Use CohereText 72px / 400 / 72px for an observed public h1 treatment, not as a claim about the authenticated product.
- Keep a pricing tab route-local: `#212121` text, 0px radius, 4px vertical padding; selected is the only captured state.

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including Inter-Arial-or-system-UI-not-named-as-a-Cohere-typeface, declared-only-CohereHeadline-CohereColor-icon-variable-or-KaTeX-not-visible-UI-tokens, no-invented-hover-pressed-focus-error-dialog-toast-or-accordion-expanded-variants-from-class-names, and floating-dialog-8px-shadow-not-generalized-into-product-cards-or-primary-actions. They are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification.

- Do not substitute Inter, Arial, or system UI while naming the result as a Cohere typeface.
- Do not promote the declared-only CohereHeadline, CohereColor, icon, variable, or KaTeX assets into visible UI tokens.
- Do not invent hover, pressed, focus, error, dialog, toast, or accordion-expanded variants from class names.
- Do not generalize the floating dialog’s 8px/shadow treatment into product cards or primary actions.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#17171c`-as-dark-pill-background-not-Foreground-`#000000`-not-Foreground-soft-`#212121`-not-a-complete-action-scale, canvas-`#ffffff`-as-page-and-inverse-action-surface-not-Inverse-text-`#fafafa`-not-dark-CTA-on-fill-as-a-second-paint, YAML-pricing-tab-`fg`-`#212121`-staying-on-that-control, YAML-component-fields-staying-on-those-controls, no-selector-backed-blue-purple-green-gradient-focus-ring-or-error-color-role, and omitted-rather-than-generalized-from-class-names-asset-names-or-an-adjacent-surface — are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification. Catalog `primary_color` `#17171c` is YAML `primary` and the observed dark pill background on public home, pricing, and products actions. Treat it as that observed public action fill, not a complete action scale and not Foreground `#000000`. YAML `canvas` `#ffffff` is the observed page and inverse-action surface across all captured routes. YAML `foreground` `#000000` is the repeated public text value. YAML `foreground-soft` `#212121` is the repeated public button, tab, and body value. YAML pricing-tab `fg` `#212121` stays on that control; it is not a second general ink. YAML `muted` `#75758a` is the repeated public secondary-text value. YAML `inverse` `#fafafa` is observed text on dark public areas; it is not dark-CTA on-fill `#ffffff`. YAML `border` `#e5e7eb` is the repeated computed border value across the three routes. YAML `border-dark` `#525260` is the observed one-pixel border on the home and products routes. The artifact does not provide a selector-backed blue, purple, green, gradient, focus-ring, or error-color role for these routes. Those older claims are omitted rather than generalized from class names, asset names, or an adjacent surface.

- **Dark action** (`#17171c`): catalog `primary_color`. YAML `primary`. Observed dark pill background on public home, pricing, and products actions.
- **Canvas** (`#ffffff`): YAML `canvas`. Observed page and inverse-action surface across all captured routes.
- **Foreground** (`#000000`): YAML `foreground`. Repeated public text value.
- **Foreground soft** (`#212121`): YAML `foreground-soft`. Repeated public button, tab, and body value.
- **Muted** (`#75758a`): YAML `muted`. Repeated public secondary-text value.
- **Inverse text** (`#fafafa`): YAML `inverse`. Observed text on dark public areas.
- **Light border** (`#e5e7eb`): YAML `border`. Repeated computed border value across the three routes.
- **Dark-section boundary** (`#525260`): YAML `border-dark`. Observed one-pixel border on the home and products routes.

### Spacing

YAML `spacing`: xs 6, sm 8, md 12, lg 16, xl 24, xxl 36, xxxl 40.

Treating those YAML numbers as recorded without a required px suffix, treating the body 6px / 8px / 12px / 16px / 24px / 36px / 40px cluster as observed clusters rather than a documented Cohere spacing specification, and treating route-specific card and section geometry as local until a selector-backed measurement establishes it, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Those YAML numbers are recorded without a px suffix; none is added here. The capture shows 6px, 8px, 12px, 16px, 24px, 36px, and 40px spacing values. These are observed clusters rather than a documented Cohere spacing specification. Public actions use 12px vertical padding; the captured navigation has 16px vertical and 40px horizontal padding at the 1440px viewport. Treat route-specific card and section geometry as local until a selector-backed measurement establishes it.

### Shape

YAML `rounded`: control 4, dialog 8, media 22, pill 9999.

The following local-geometry reading, including Compact-control-4, a-disabled-4px-radius-icon-control-is-a-route-local-observation-and-is-not-promoted-as-an-action-variant, Route-local-dialog-8, Home-media-card-22, Pill-9999, Pricing-tab-0-as-component-local-not-a-YAML-rounded-step, and yaml-rounded-has-no-0-step-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. The universal-radius refusal is limited after the list, not here.

- Compact control (YAML `control` 4): compact controls can be 4px; a disabled 4px-radius icon control is a route-local observation and is not promoted as an action variant
- Route-local dialog (YAML `dialog` 8): a captured route-local dialog is 8px
- Home media card (YAML `media` 22): the home media-card container is 22px rounded
- Pill (YAML `pill` 9999): dark and inverse public actions
- Pricing tab (body 0px): YAML pricing-tab `radius` 0; YAML `rounded` has no 0 step, and none is invented there

Treating 4 / 8 / 22 / 9999 / 0px as local harvested geometry rather than a claim that every Cohere card, image, dialog, or form uses one radius, and treating the 22px home media-card, 8px route-local dialog, 4px compact control, 0px pricing tab, and 9999px pills as selector-backed component use rather than one interchangeable radius, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. The home media-card container is 22px rounded, while a captured route-local dialog is 8px and compact controls can be 4px. Do not flatten that into a claim that every Cohere card, image, dialog, or form uses one radius.

### Elevation

The following elevation readings, including most-observed-public-controls-as-shadow-free, one-route-local-floating-control-and-dialog-with-`0px 2px 8px rgba(0, 0, 0, 0.15)`, and that-shadow-as-route-local-chrome-not-a-general-Cohere-elevation-token, are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification. Most observed public controls are shadow-free. The capture also records a route-local floating control and dialog with a `0px 2px 8px rgba(0, 0, 0, 0.15)` shadow. It is route-local chrome, not a general Cohere elevation token.

### Motion

The supplied public capture contains transition-related class names on actions and a floating control, but no measured transition duration, easing value, reduced-motion behavior, or before/after interaction observation. Treating that source-stated absence, including transition-related-class-names-are-not-measured-motion-tokens and no-motion-token-is-asserted, as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. No motion token is asserted.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including no-first-party-typography-guide-assigning-a-universal-product-role, live-computed-Unica77-Cohere-Web-1262-CohereText-five-heading-uses-CohereMono-29-label-body-uses-with-FontFaceSet-and-browser-served-source-corroboration, Cohere-served-WOFF-WOFF2-OTF-as-delivery-not-a-reuse-license, Lineto-LL-Unica77-as-authorized-digital-Haas-Unica-with-separate-licensing-terms, declared-only-CohereColor-CohereHeadline-CohereIconDefault-CohereIconOutline-CohereVariable-and-KaTeX, and Inter-Arial-and-system-families-as-fallback-stacks-only, are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification.

| Evidence class | Family and boundary |
|---|---|
| Official product-use | Cohere’s official public pages establish the company/product context, but this research pass found no first-party typography guide that assigns a universal product role to a named Cohere family. |
| Live computed surface-use | Unica77 Cohere Web is loaded/high with 1,262 uses across body, buttons, cards, dialogs, inputs, lists, tabs, and toggles. CohereText is loaded/high with five heading uses, and CohereMono is loaded/high with 29 label/body uses. Each has computed-family use plus FontFaceSet and browser-served source corroboration in the supplied artifact. YAML `family.ui` `Unica77 Cohere Web`, YAML `family.display` `CohereText`, and YAML `family.mono` `CohereMono` name those live families. |
| Official distributed brand asset | The capture records Cohere-served WOFF/WOFF2/OTF assets. For Unica77, Lineto identifies LL Unica77 as an authorized digital Haas Unica version and publishes separate licensing terms; that does not grant reuse of Cohere’s served custom files. |
| Declared-only | CohereColor, CohereHeadline, CohereIconDefault, CohereIconOutline, and CohereVariable have declared source assets but no visible computed use. They remain declared-only. KaTeX faces are also declared-only technical assets, not Cohere brand typography. |
| System / unresolved | Inter, Arial, and system families occur in fallback stacks only. They are not substituted for or promoted as Cohere typefaces. |

### Family

- **Current visible UI family:** `Unica77 Cohere Web` — YAML `family.ui`
- **Current visible display family:** `CohereText` — YAML `family.display`
- **Current visible mono family:** `CohereMono` — YAML `family.mono`
- **Declared-only:** CohereColor, CohereHeadline, CohereIconDefault, CohereIconOutline, CohereVariable, KaTeX — declared source assets; no visible computed use
- **CSS fallbacks only:** Inter, Arial, and system families — not substitutes for the named Cohere faces

The following font-use boundary, including do-not-render-a-system-fallback-as-a-named-Cohere-typeface, Inter-Arial-or-system-UI-not-labeled-as-Unica77-CohereText-or-CohereMono, CohereText-not-Unica77-not-CohereMono, and declared-only-faces-not-loaded-UI-faces, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Do not render a system fallback as a named Cohere typeface. Do not present Inter, Arial, or system UI as Unica77 Cohere Web, CohereText, or CohereMono. Do not present CohereText as Unica77 Cohere Web. Declared-only fonts must not be presented as loaded Cohere faces.

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1`, `1.2`, `1.5`, and `1.4`. The following ratio-versus-size-local reading, including unitless-ratios-not-fixed-px, public-h1-72-not-public-h2-60-not-public-h3-48, public-heading-32-as-body-table-only, secondary-body-14-Unica77-unmerged-from-CohereMono-label-14, yaml-has-no-lineHeight-for-public-h2-or-public-heading-32-or-secondary-body-none-invented, and yaml-tracking-display-minus-1.44-heading-minus-0.48-label-0.28-unmerged, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. Public h2, Public heading 32px, and Secondary body are body-table roles; YAML has no `lineHeight` field for them, and none is invented.

| Role | Font | Size | Weight | Line height (YAML) | Body observation | Tracking | Use (YAML) / Notes |
|---|---|---:|---:|---:|---|---:|---|
| Public h1 | CohereText | 72px | 400 | 1 | 72px at this size | YAML `-1.44` | YAML use: `Observed public h1 on the supplied routes`; two observed headings |
| Public h2 | CohereText | 60px | 400 | — | 60px | — | two observed headings (body table only; YAML has no separate h2 type-role) |
| Public h3 | Unica77 Cohere Web | 48px | 400 | 1.2 | 57.6px at this size | YAML `-0.48` | YAML heading use: `Observed public h3 on the supplied routes`; ten observed headings |
| Public heading | Unica77 Cohere Web | 32px | 400 | — | 38.4px | — | observed h2/h4 (body table only; YAML has no 32 type-role) |
| Repeated body/action | Unica77 Cohere Web | 16px | 400 | 1.5 | 24px at this size | — | YAML body use: `Repeated public navigation, body, and action text` |
| Secondary body | Unica77 Cohere Web | 14px | 400 | — | 19.6px | — | repeated public body text (body table only; not CohereMono label) |
| Technical label | CohereMono | 14px | 400 | 1.4 | 19.6px at this size | YAML `0.28` / body `0.28px` | YAML label use: `Observed CohereMono label text` |

Treating the trailing type-table restatement, including system-fallback-not-a-named-Cohere-typeface and declared-only-faces-not-a-visible-UI-scale, as the same font-use boundary already limited at Family, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Do not render a system fallback as a named Cohere typeface. Declared-only faces are useful as asset records, but no visible use was captured for them, so this reference does not claim those families as loaded UI faces.

### Assets

Treating catalog logo metadata as type `github`, slug `cohere-ai`, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification.

Treating browser-served WOFF/WOFF2/OTF files as delivery evidence rather than a license grant for reuse, including Lineto-terms-as-the-third-party-license-boundary-for-Unica77-not-authorization-to-reuse-Cohere-served-files and no-standalone-first-party-CohereText-or-CohereMono-font-license-or-type-specimen-in-this-packet, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Browser-served WOFF/WOFF2/OTF files on the captured public routes are delivery evidence for Unica77 Cohere Web, CohereText, and CohereMono. They are not a license grant for reuse.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, including because-no-loading-empty-error-success-skeleton-or-validation-state-was-captured-on-the-three-supplied-public-routes, the-only-recorded-stateful-component-evidence-is-a-selected-pricing-tab-and-one-disabled-4px-radius-icon-control, do-not-extrapolate-those-observations-into-a-general-application-state-system, no-component-hover-pressed-focus-expanded-dialog-open-toast-validation-or-generic-disabled-treatment, a-disabled-4px-icon-control-is-a-route-local-observation-and-is-not-promoted-as-an-action-variant, collector-recorded-no-interactions, and class-names-mentioning-transitions-or-focus-are-not-treated-as-observed-states, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted: No loading, empty, error, success, skeleton, or validation state was captured on the three supplied public routes. The only recorded stateful component evidence is a selected pricing tab and one disabled 4px-radius icon control. Do not extrapolate those observations into a general application-state system. The supplied capture records no component hover, pressed, focus, expanded, dialog-open, toast, validation, or generic disabled treatment. A disabled 4px icon control is a route-local observation and is not promoted as an action variant. The collector recorded no interactions. Class names mentioning transitions or focus are not treated as observed states.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating a later generic Focus observation as not `focus-visible` evidence, treating selected as an additional named observed state rather than a Core applicability row or a `focus-visible` treatment, treating Dark solid / Inverse solid / Outlined pricing action loading-error-success as omitted fields rather than closed from a missing capture, treating Selected tab loading-error-success as role-based not-applicable rather than closed from a missing capture, treating Home marketing media as YAML `type: card` with canonical Home-only-interactive-media-card-container as interactive-kind evidence for a §4.4 map, and treating Home marketing media loading-error-success as omitted fields rather than closed from a missing capture, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `focus-visible` treatment and no generic Focus capture; `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. Selected tab closes those three fields as not-applicable by meaning (panel selection). Home marketing media restores Kind: interactive and a role-based map from that canonical interactive-kind evidence; its loading, error, and success fields stay omitted. This is not a complete state-coverage claim.

Treating selected / `aria-selected=true` as an additional named observed state rather than a Core applicability row or a `focus-visible` treatment is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Selected is an additional named observed state; it is not among the seven Core applicability rows and is not copied onto a `focus-visible` row.

Treating Home marketing media as YAML `type: card` with canonical Home-only-interactive-media-card-container as interactive-kind evidence, restoring Kind: interactive and a role-based §4.4 map, omitting that container’s loading-error-success fields because exact request/outcome mapping is unresolved rather than closing them from a missing capture, treating Dark solid / Inverse solid / Outlined pricing action YAML `type` as absent and not invented, and restoring Proof-confirmed Type: anchor on those three public actions rather than inventing a YAML type, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Home marketing media has YAML `type: card`. Canonical source names it a Home-only interactive media-card container; that is interactive-kind evidence. Kind: interactive and a role-based applicability map are restored. Loading, error, and success fields stay omitted at this boundary. Dark solid, Inverse solid, and Outlined pricing action have no YAML `type`; none is invented as YAML. The adopted Proof sidecar confirms each as an anchor; Type: anchor is that Proof-confirmed primitive (A1b), not a YAML type.

### Dark solid

- Role: public dark marketing CTA on home, pricing, and products
- Kind: interactive
- Type: anchor
- Anatomy: label
- Background: `#17171c`
- Text: `#ffffff`
- Radius: 9999px
- Padding: `12px 24px`
- Font: `16px / 400 / Unica77 Cohere Web`
- Use: Home, pricing, and products public CTA; home selector `home::[data-omd-capture="53"]`
- Observed: default only. Proof sidecar: an anchor.
- Field note: The following unmerged-field reading, including this-fill-as-catalog-`primary_color`-not-Foreground-`#000000`, this-on-fill-`#ffffff`-not-Inverse-text-`#fafafa`-not-Canvas-as-a-second-paint, 9999px-not-media-22px-not-tab-0px, `12px 24px`-not-outlined-`12px 16px`, and 16px-400-as-this-control-not-a-second-body-role, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. `#17171c` is this control’s fill (Dark action). `#ffffff` is this control’s on-fill label; it is not Inverse text `#fafafa`. Radius 9999px is this pill (YAML `pill`); it is not media-card 22px or tab 0px. Padding `12px 24px` is this captured variant.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the public dark CTA on home, pricing, and products |
| hover | applicable | Pointer-web public CTA; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A public CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a public CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Inverse solid

- Role: inverse public CTA on home and products
- Kind: interactive
- Type: anchor
- Anatomy: label
- Background: `#ffffff`
- Text: `#17171c`
- Radius: 9999px
- Padding: `12px 24px`
- Font: `16px / 400 / Unica77 Cohere Web`
- Use: Inverse public CTA on home and products; home selector `home::[data-omd-capture="58"]`
- Observed: default only. Proof sidecar: an anchor.
- Field note: The following unmerged-field reading, including this-fill-as-Canvas-`#ffffff`-as-inverse-action-surface-not-Inverse-text-`#fafafa`, this-label-as-Dark-action-`#17171c`-not-Foreground-`#000000`, 9999px-matching-dark-solid-not-media-22px, and inverse-counterpart-only-for-a-dark-public-section, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. `#ffffff` is this control’s fill (Canvas as inverse-action surface). `#17171c` is this control’s label (Dark action). Radius 9999px matches Dark solid; it is not media-card 22px. This inverse counterpart is for a dark public section.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the inverse public CTA on home and products |
| hover | applicable | Pointer-web public CTA; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A public CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as an inverse public CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Outlined pricing action

- Role: pricing-only secondary action
- Kind: interactive
- Type: anchor
- Anatomy: label
- Text: `#17171c`
- Border: `1px solid #17171c`
- Radius: 9999px
- Padding: `12px 16px`
- Font: `16px / 400 / Unica77 Cohere Web`
- Use: Pricing-only secondary action; selector `surface-2::[data-omd-capture="59"]`
- Observed: default only. Proof sidecar: an anchor.
- Field note: The following unmerged-field reading, including this-label-and-border-as-Dark-action-`#17171c`-not-Foreground-`#000000`, `12px 16px`-not-dark-solid-`12px 24px`, 9999px-matching-pills-not-tab-0px, no-fill-promoted-in-source-§4, and pricing-route-not-universal-button-guidance, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. `#17171c` is this control’s text and border. Padding `12px 16px` is this captured variant; it is not Dark solid `12px 24px`. Source §4 does not promote a fill for this action. This is a pricing-route component, not universal button guidance.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the pricing-only secondary action |
| hover | applicable | Pointer-web public action; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A pricing secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a pricing-only secondary action; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Selected tab

- Role: observed selected public pricing tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#212121`
- Radius: 0px
- Padding: `4px 0px`
- Font: `16px / 400 / Unica77 Cohere Web`
- Selected: `aria-selected=true` observed
- Use: YAML `Observed selected public pricing tab`; selector `surface-2::[data-omd-capture="56"]`
- YAML `states`: selected only
- Observed: selected (harvested default fields)
- Field note: The following unmerged-field reading, including this-label-as-Foreground-soft-`#212121`-not-Foreground-`#000000`-not-Dark-action-`#17171c`, 0px-not-pill-9999px-not-media-22px, `4px 0px`-not-CTA-`12px 24px`, 16px-400-as-this-tab-field, and selected-as-an-additional-named-observed-state-not-a-Core-row, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. `#212121` is this tab’s selected label (Foreground soft / YAML pricing-tab `fg`). Radius 0px is this tab (YAML pricing-tab `radius` 0); it is not pill 9999px. Padding `4px 0px` is this captured variant. Selected is an additional named observed state, not a Core applicability row and not a `focus-visible` treatment.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the selected public pricing tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A pricing tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A pricing tab selects a panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is panel selection, not action-outcome confirmation |

Treating selected / `aria-selected=true` as the observed selected state rather than a universal hover or focus rule is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Additional observed named state: selected — `aria-selected=true` observed. YAML records selected only. This is the observed selected state, not a universal hover or focus rule.

### Home marketing media

- Role: home-only marketing-card container
- Kind: interactive
- Type: card
- Anatomy: surface
- Radius: 22px
- Use: YAML `Observed home marketing-card container; no fill, border, or state is promoted`. Home-only interactive media-card container; selector `home::div.group/card`
- Observed: default only; no fill, border, or state is promoted
- Field note: The following unmerged-field reading, including 22px-as-this-home-container-not-pill-9999px-not-dialog-8px-not-control-4px, no-fill-border-or-state-promoted, and home-only-not-a-universal-card-recipe, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Radius 22px is this home media-card container (YAML `media` 22 / YAML marketing-media-card `radius` 22). No fill, border, or state is promoted. This is a home-only container, not a universal card recipe.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the home-only interactive media-card container |
| hover | applicable | Pointer-web interactive media-card; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An interactive media-card can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this a home-only interactive media-card container; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML-spacing-xs-6-sm-8-md-12-lg-16-xl-24-xxl-36-xxxl-40, observed-clusters-6px-8px-12px-16px-24px-36px-40px-not-a-documented-Cohere-spacing-specification, public-actions-12px-vertical-padding, captured-navigation-16px-vertical-and-40px-horizontal-at-the-1440px-viewport, 22px-home-media-card-versus-8px-route-local-dialog-versus-4px-compact-versus-0px-pricing-tab-versus-9999px-pills, treat-route-specific-card-and-section-geometry-as-local, supplied-capture-at-1440x900-desktop-computed-values-only, navigation-class-indicates-responsive-padding-rules-and-home-media-card-includes-responsive-dimensions-but-no-smaller-viewport-render, and mobile-breakpoints-collapsed-navigation-and-touch-behavior-remain-unclaimed — are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification. YAML spacing is xs 6, sm 8, md 12, lg 16, xl 24, xxl 36, xxxl 40. The capture shows 6px, 8px, 12px, 16px, 24px, 36px, and 40px spacing values. These are observed clusters rather than a documented Cohere spacing specification. Public actions use 12px vertical padding; the captured navigation has 16px vertical and 40px horizontal padding at the 1440px viewport. Treat route-specific card and section geometry as local until a selector-backed measurement establishes it. The following bullet readings — do-not-flatten-22px-8px-4px-into-one-radius, supplied-capture-establishes-desktop-computed-values-only, and mobile-breakpoints-collapsed-navigation-and-touch-behavior-remain-unclaimed — are a derived editorial implementation inference from the verified surfaces; they are not Cohere-authored or a separately published UI specification.

- The home media-card container is 22px rounded, while a captured route-local dialog is 8px and compact controls can be 4px. Do not flatten that into a claim that every Cohere card, image, dialog, or form uses one radius.
- The supplied capture is at 1440x900. It establishes desktop computed values only.
- The navigation class indicates responsive padding rules and the home media card includes responsive dimensions, but no smaller-viewport render or interaction was supplied; mobile breakpoints, collapsed navigation, and touch behavior therefore remain unclaimed.

Treating those 6px / 8px / 12px / 16px / 24px / 36px / 40px figures, the 1440px / 1440x900 viewport, and the 12px / 22px / 8px / 4px / 0px / 9999px radii as captured-surface measurements rather than a cross-viewport specification, including no-smaller-viewport-render-or-interaction-was-supplied, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. No smaller-viewport render or interaction was supplied. Mobile breakpoints, collapsed navigation, and touch behavior remain unclaimed.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating the following as a source-informed public-content voice rather than a universal copywriting ban list or an app-wide tone specification, including official-writing-characterization, practical-enterprise-oriented-and-human-purposeful, About-page-foundation-models-and-AI-solutions-that-turn-everyday-effort-into-impact, public-homepage-leads-with-control-over-data-and-infrastructure, Careers-names-Momentum-Openness-and-Autonomy, these-are-official-messaging-and-culture-evidence-not-a-claim-about-every-product-microcopy-string, tone-table-context-labels-Enterprise-proposition-Product-framing-Culture, Enterprise-proposition-emphasize-control-security-flexibility-and-customization, Product-framing-connect-an-AI-capability-to-a-work-outcome-or-organization-context, and Culture-use-purposeful-direct-language-consistent-with-Momentum-Openness-and-Autonomy, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. Official Cohere sources frame the public voice as practical, enterprise-oriented, and human-purposeful. The About page describes foundation models and AI solutions that turn everyday effort into impact, while the public homepage leads with control over data and infrastructure. Careers names Momentum, Openness, and Autonomy as company values. These are official messaging and culture evidence, not a claim about every product microcopy string.

| Context | Evidence-backed direction |
|---|---|
| Enterprise proposition | Emphasize control, security, flexibility, and customization. |
| Product framing | Connect an AI capability to a work outcome or organization context. |
| Culture | Use purposeful, direct language consistent with Momentum, Openness, and Autonomy. |

Treating the following public samples as official wording rather than synthetic microcopy, including Own-your-AI-as-official-homepage, Where-enterprise-AI-meets-real-world-purpose-as-official-About, and Join-the-mission-to-scale-intelligence-as-official-Careers, is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification.

**Public samples**

- “Own your AI” (official homepage).
- “Where enterprise AI meets real-world purpose” (official About page).
- “Join the mission to scale intelligence” (official Careers page).

The following captured-host reading — public-Korean-marketing-pricing-and-products as the named hosts in this packet rather than a complete locale profile, official-About-Careers-and-homepage-samples-as-English-official-wording, and signed-in-application-UI-and-documentation-chrome-stay-outside-the-supplied-capture — is a derived editorial implementation inference from the verified surfaces; it is not Cohere-authored or a separately published UI specification. This packet names the three public Korean marketing, pricing, and products routes. Official About, Careers, and homepage samples above are official wording. Signed-in application UI and documentation chrome stay outside the supplied capture.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- blue, purple, green, gradient, focus-ring, and error-color roles
- hover, pressed, focus, expanded, dialog-open, toast, validation, and generic disabled visual treatments
- Dark solid / Inverse solid / Outlined pricing action / Home marketing media loading·error·success applicability (exact request/outcome unresolved)
- Selected tab loading·error·success applicability is closed as not-applicable by tab meaning
- signed-in product UI and documentation chrome colors, type, and components
- a general Cohere elevation token (the floating dialog’s `0px 2px 8px rgba(0, 0, 0, 0.15)` stays route-local)
- a documented Cohere spacing specification or a universal card/image/dialog/form radius
- mobile breakpoints, collapsed navigation, and touch behavior
- a standalone first-party CohereText/CohereMono font license or public type specimen was not found through this discovery pass
- CohereColor, CohereHeadline, CohereIconDefault, CohereIconOutline, CohereVariable, and KaTeX as loaded UI faces
- every product microcopy string beyond the official About / homepage / Careers samples
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
