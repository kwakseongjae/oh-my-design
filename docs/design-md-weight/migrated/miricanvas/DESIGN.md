# MiriCanvas Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

MiriCanvas (미리캔버스) is MIRIDIH's browser-based design service: its public template and pricing surfaces position design creation as something users can begin from ready-made material instead of specialist software. This contract covers the three first-party public product surfaces the source inspected for tokens on 2026-07-13: home at `https://www.miricanvas.com/ko`, templates at `https://www.miricanvas.com/ko/templates`, and pricing at `https://www.miricanvas.com/ko/pricing`. Catalog identity also records homepage `https://www.miricanvas.com` and `primary_color: "#21afbf"`; that catalog color is the same hex as `tokens.colors.primary` and is not a second YAML colors key. The operator page at `https://www.miridih.com/ko/home`, the editor-control article at `https://help.miricanvas.com/hc/ko/articles/4413862566937`, and the license article at `https://help.miricanvas.com/hc/ko/articles/56483608870681` are named sources for operator context, documented editor capability, and content-license boundary; they do not supply the computed interface tokens below. YAML token note, kept as written: Public MiriCanvas surfaces only. Tokens are limited to computed observations corroborated by the supplied FontFaceSet evidence; no authenticated editor or help-center chrome is promoted. Reading those three inspected public URLs as this contract's token surfaces, keeping the operator and Help Center URLs as named context sources that do not supply computed interface tokens, treating values as attached to the surface that established them, and keeping catalog `primary_color` `#21afbf` on the same hex as `tokens.colors.primary` rather than as a second colors key, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

The visible system is deliberately spare: a white canvas (`#ffffff`), dark ink (`#16181d`), and an aqua-teal action color (`#21afbf`) recur across the home, template, and pricing routes. This gives the public experience a clear action hierarchy without turning all of those surfaces into brand decoration. The collector confirms a loaded, site-hosted `Pretendard Variable` face throughout styled public UI. A 64px/700 homepage heading with 96px line-height sits above smaller 28px, 24px, and 14px steps; the contrast is soft geometry rather than an assertion about a private editor system. The evidence packet covers public surfaces only. It does not establish authenticated-editor styling, marketing-only feature-page styling, help-center chrome, or a complete responsive system. The hex values, the three-route recurrence, the loaded face, the type steps, and those evidence-packet bounds are the source's own. The characterizations built on them — a deliberately spare visible system; a clear action hierarchy without turning all of those surfaces into brand decoration; contrast as soft geometry rather than an assertion about a private editor system — are a derived editorial implementation inference from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. MiriCanvas is operated by MIRIDIH. The operator's official corporate page says it aims to change inconvenient moments in design and enable people to communicate more easily through design; it lists integrity, customer focus, data-driven work, and high standards as operating values. The same page describes current growth beyond the domestic market, providing a bounded account of the brand's present direction. MIRIDIH frames its broader purpose as reducing inconvenient moments in design and enabling easier communication through design; its corporate site also describes a move from domestic success toward global markets. Those company statements provide context for the inclusive product positioning, but they are not UI-token evidence. The public MiriCanvas ecosystem shows two verified contexts rather than one undifferentiated surface. Template and pricing routes serve public discovery and plan comparison, while the business and Help Center materials describe organization-level governance, approval, and content use. The company’s terms state that rights in service-provided content remain with the original author/content provider or company as applicable and that use follows the license agreement. That is a license boundary, not a claim that every template, font, or customer output has the same rights. The operator relationship, the mission and values list, the domestic-to-global direction, the two verified contexts, the license-boundary closing sentence, and the source's own "not UI-token evidence" bound are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that operator-and-license narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not MiriCanvas-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, each naming a captured public surface or a label the source records, and not taking them from the source's Personas section, is a derived editorial implementation inference from the verified surfaces; it is not MiriCanvas-authored or a separately published UI specification.

- Begin design creation from ready-made material on `https://www.miricanvas.com/ko`.
- Public discovery on the template route at `https://www.miricanvas.com/ko/templates`.
- Plan comparison on the pricing route at `https://www.miricanvas.com/ko/pricing`.
<!-- design-md:claim-end -->

### Audience

No named individuals appear. The source's §13 lists documentation- and business-material stakeholder groups and states they are not fictional individuals or demographic personas; those groups are omitted from this public-surface audience rather than promoted, and no name, age, city, motivation, or affiliation classification is carried. The captured public routes address users who can begin from ready-made material instead of specialist software. Dropping those documentation- and business-material stakeholder groups rather than promoting them, carrying no name, age, city, motivation, or affiliation classification, and reading the captured public copy's users-from-ready-made-material address as this contract's audience bound, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, are a derived editorial implementation inference from the verified surfaces — they are not MiriCanvas-authored or a separately published UI specification.

- Aqua teal (`#21afbf`) on an observed 40px public CTA; a clear action hierarchy without turning every surface into brand decoration
- White (`#ffffff`), Ink (`#16181d`), and Ink Soft (`#23242a`) as recurring public-surface neutrals
- `Pretendard Variable` is loaded and used by visible styled UI; fallback family names are not promoted
- 8px, 10px, 12px, and 16px are observed radii, with 8px the strongest public-button cluster
- An expanded navigation menu is the only captured interaction state; it is white, 16px-rounded, and softly elevated

### Principles

These 4 items are a derived editorial implementation inference from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification. The source states them in its own Principles section together with the UI implication it draws from each.

1. **Ease communication through design.** MIRIDIH's stated mission supports clear, approachable public entry points. *UI implication:* retain the public routes' plain action hierarchy, without asserting unobserved editor flows.
2. **Customer focus and high standards.** These are MIRIDIH's stated values. *UI implication:* treat a visual token as canonical only when its source domain and computed evidence agree.
3. **Governable team creation.** Official help documentation allows owners and managers to constrain colors, fonts, templates, and elements for members. *UI implication:* brand-governance controls are product capabilities, not a license to promote documentation chrome into the public token system.
4. **Licensed content has boundaries.** The service terms describe content rights and license-based use. *UI implication:* do not characterize templates or font assets as universally “copyright safe” without a specific applicable license.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

- Use `#21afbf` with white text for the observed public filled-action pattern.
- Use the loaded `Pretendard Variable` family for a reconstruction of these public surfaces.
- Retain the observed 8px / 14px 500 / 40px geometry where the standard public CTA or navigation item is intended.
- Treat the 16px rounded, shadowed menu only as an expanded navigation state.

### Avoid

The source states these four as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

- Substitute an unresolved or declared-only fallback family as if it were loaded MiriCanvas UI typography.
- Add hover, disabled, focus, error, modal, toast, or editor-state specifications from this public packet.
- Generalize the menu's shadow to all cards or buttons.
- Treat Help Center styling or a feature/marketing page as evidence for the authenticated product editor.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Each role below pairs a name with the value and the use the source records. Role names follow the source's own labels and token-set keys. Taking those role names from the source's own token-set keys, pairing each hex to the token-set path named beside it, keeping `tokens.colors.canvas` `#ffffff` off `tokens.colors.on-primary` `#ffffff`, keeping menu-open panel background `#ffffff` on the canvas key rather than as a third YAML colors key, keeping `tokens.colors.primary` `#21afbf` off `tokens.colors.primary-deep` `#1c95a2`, keeping menu-open text `#000000` as a §4 field that is not a YAML `tokens.colors` key, attaching every role to the public routes the source recorded rather than relabeling a home value as a house palette for every MiriCanvas surface, and keeping the source's own "No semantic error, success, disabled, hover, focus, or editor color token is asserted from this packet" bound, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

- **Primary teal** (`#21afbf`): observed background of a high-confidence public CTA on home, templates, and pricing. Token-set path `tokens.colors.primary`. Catalog identity `primary_color` is the same hex.
- **Teal text** (`#1c95a2`): observed on a public text-action button; keep it distinct from the filled primary action. Token-set path `tokens.colors.primary-deep`.
- **Canvas** (`#ffffff`): observed page and menu surface. Token-set path `tokens.colors.canvas`. Same hex as `tokens.colors.on-primary`; it stays a second key.
- **On-primary** (`#ffffff`): white CTA text. Token-set path `tokens.colors.on-primary`. Same hex as `tokens.colors.canvas`; it stays a second key.
- **Ink** (`#16181d`): observed primary text on the three public routes. Token-set path `tokens.colors.ink`.
- **Ink Soft** (`#23242a`): observed on high-confidence public navigation buttons. Token-set path `tokens.colors.ink-soft`.
- **Muted** (`#70798f`): repeated lower-emphasis public text observation. Token-set path `tokens.colors.muted`.

No semantic error, success, disabled, hover, focus, or editor color token is asserted from this packet. Menu-open text `#000000` is recorded on the expanded navigation panel; it is not a YAML `tokens.colors` key.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept as the source wrote them): `xs: 4` · `sm: 6` · `md: 8` · `lg: 12` · `xl: 16` · `xxl: 20` · `hero: 24`.

`tokens.spacing.md: 8` is not `tokens.rounded.sm: 8` and is not the `8px` in filled-primary and navigation-item padding `8px 16px`. `tokens.spacing.lg: 12` is not `tokens.rounded.lg: 12` and is not the switcher padding `12px`. `tokens.spacing.xl: 16` is not `tokens.rounded.xl: 16` and is not the menu-open padding `16px`. `tokens.spacing.hero: 24` is not `tokens.typography.section.size` `24`. `tokens.spacing.xxl: 20` is not the ui line-height writing `20px`. Observed §5 values `14px` and `56px` are not YAML spacing keys; `14px` is also `tokens.typography.ui.size`. Keeping those unitless spacing steps on their own keys rather than rewriting them as a grid, keeping `tokens.spacing.md: 8` off `tokens.rounded.sm: 8` and off padding `8px 16px`, keeping `tokens.spacing.lg: 12` off `tokens.rounded.lg: 12` and off switcher padding `12px`, keeping `tokens.spacing.xl: 16` off `tokens.rounded.xl: 16` and off menu-open padding `16px`, keeping `tokens.spacing.hero: 24` off `tokens.typography.section.size` `24`, keeping `tokens.spacing.xxl: 20` off the ui line-height writing `20px`, keeping observed §5 `14px` and `56px` off YAML spacing keys, and keeping `14px` as also `tokens.typography.ui.size`, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path): `sm: 8` · `md: 10` · `lg: 12` · `xl: 16`.

The source's named radius uses, kept on their own rows:

- Small (`8` / `8px`): common on navigation and the filled CTA; 8px is the strongest public-button cluster. Token-set key `tokens.rounded.sm`.
- Medium (`10` / `10px`): observed; occurs less often. Token-set key `tokens.rounded.md`.
- Large (`12` / `12px`): observed; occurs less often. Token-set key `tokens.rounded.lg`.
- Extra-large (`16` / `16px`): expanded navigation menu. Token-set key `tokens.rounded.xl`. This `16` is not a spacing step.

Do not turn the capture frequency into a claim about an exhaustive design scale. Keeping `8`, `10`, `12`, and `16` as four keys, keeping Extra-large `16` off a spacing step, keeping 8px as the strongest public-button cluster the source recorded rather than as a universal radius, and keeping that frequency sentence from becoming an exhaustive-scale claim, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

### Elevation

The high-confidence filled CTA and navigation records have `box-shadow: none`. One expanded navigation menu state has `rgba(11, 60, 65, 0.2) 0px 6px 20px 0px`. Token-set path `tokens.shadow.menu-open`. No card, modal, editor, hover-lift, or general elevation policy is established from the new packet. The `none` writing, the menu-open shadow, and that no-general-elevation sentence are the source's own. Reading those representative `box-shadow: none` reports and the one menu-open shadow as the only elevation records, rather than as a depth scale for every MiriCanvas surface, and refusing to generalize the menu's shadow to all cards or buttons, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

### Motion

No duration, easing curve, transition, or reduced-motion rule was captured in the supplied evidence. No motion token is asserted. The source names no duration token and no easing role. An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation, and keeping the source's "No duration, easing curve, transition, or reduced-motion rule was captured in the supplied evidence. No motion token is asserted" sentences, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

| Evidence class | Resolution |
|---|---|
| Live computed surface-use + loaded source corroboration | `Pretendard Variable` is the computed family for styled public UI and is backed by a matching loaded `FontFace` record (215 observed uses; 92 MiriCanvas-hosted subset source URLs). It is the only family promoted to `tokens.typography.family`. Token-set path `tokens.typography.family.sans`. |
| Declared-only fallbacks | `Figtree`, `IBM Plex Sans JP`, `Pretendard JP Variable`, and `Pretendard Std Variable` occur later in the computed stack of styled elements, but the supplied `FontFaceSet` evidence does not match them as loaded faces. They remain declared-only and are not UI-family tokens. |
| Unresolved capture artifact | `Times` appears in some captured unstyled DOM fragments and in the expanded menu record without a matching loaded face or source URL. It is not treated as a MiriCanvas type choice. |
| Official distributed font asset | Pretendard's upstream project documents the variable family and its SIL Open Font License 1.1. This establishes the upstream font's distribution/license boundary; it does not establish a MiriCanvas-owned font asset. |
| Official product-use announcement | none located in the first-party sources reviewed for this update. |

Reading those five evidence-class rows as the source's own resolution table rather than as a published MiriCanvas type specimen, keeping the official-licence row from independently establishing product use, keeping the Official product-use announcement row as none located, and keeping `Times` as an unresolved capture artifact rather than as a type choice, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

### Family

- **Current visible UI family:** `Pretendard Variable`
- **Token-set path:** `tokens.typography.family.sans`
- Do not replace unavailable or unobserved brand type with Pretendard Variable. It is canonical here only because computed visible use and loaded FontFace/source evidence agree. Do not reuse `Figtree`, `IBM Plex Sans JP`, `Pretendard JP Variable`, `Pretendard Std Variable`, or `Times` entries or substitute them for Pretendard Variable.

Keeping Pretendard Variable as the sole UI-family token on the three captured public routes, treating it as canonical here only because computed visible use and loaded FontFace/source evidence agree, refusing to replace an unavailable or unobserved brand type with it, and refusing declared-only and unresolved entries as substitutes, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

### Type roles

YAML writes unitless line heights (`1.50`, `1.20`, `1.29`, `1.33`, `1.43`). Source §3 writes the same roles with px line heights (`96px`, `48px`, `36px`, `32px`, `20px`). Both writings stay. Unitless ratios stay ratios and are never converted to a replacement px (A1a). Pairing each YAML role to the token-set path named beside it, keeping both the YAML unitless writings and the §3 px writings, keeping those unitless ratios as ratios and never converting them to a replacement px, keeping YAML `use` strings verbatim in the Token-set use column, keeping the longer §3 Public-surface provenance column beside them, keeping `tokens.typography.section.size` `24` off `tokens.spacing.hero: 24`, and keeping `tokens.typography.ui.size` `14` off the §5 observed `14px` spacing writing, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

| Role | Family | Size | Weight | Line height | Token-set use | Public-surface provenance |
|---|---|---:|---:|---|---|---|
| Homepage display (`display-hero`) | Pretendard Variable | 64 | 700 | 1.50 / 96px | Public homepage h1 | `home`, one h1 observation |
| Pricing display (`page-title`) | Pretendard Variable | 40 | 700 | 1.20 / 48px | Public pricing h1 | `surface-3`, h1 observation |
| Heading (`subsection`) | Pretendard Variable | 28 | 600 | 1.29 / 36px | Public-surface heading | public heading observations |
| Heading (`section`) | Pretendard Variable | 24 | 600 | 1.33 / 32px | Public-surface heading | public heading observations |
| Navigation / standard button (`ui`) | Pretendard Variable | 14 | 500 | 1.43 / 20px | Observed navigation and standard buttons | high-confidence button records |

Token-set paths: `tokens.typography.display-hero` · `tokens.typography.page-title` · `tokens.typography.subsection` · `tokens.typography.section` · `tokens.typography.ui`. `tokens.typography.section.size` `24` is not `tokens.spacing.hero: 24`. `tokens.typography.ui.size` `14` is not a YAML spacing key.

### Assets

- Catalog favicon: `https://www.google.com/s2/favicons?domain=miricanvas.com&sz=128`. Frontmatter records `logo.type: favicon`.
- Upstream Pretendard licence: SIL Open Font License 1.1. This describes the font asset, not a MiriCanvas brand asset.
- Declared-only `Figtree`, `IBM Plex Sans JP`, `Pretendard JP Variable`, and `Pretendard Std Variable` stay declared-only. `Times` stays an unresolved capture artifact.

Reading the Google s2 favicon URL as a catalog identity pointer rather than a MiriCanvas-hosted brand file, and reading the Pretendard licence as an upstream font-asset boundary rather than a MiriCanvas brand asset, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here in full. No public runtime empty, loading, error, success, validation, disabled, skeleton, or reduced-motion state was captured. This reference deliberately does not prescribe them.

Only the following public-surface variants have sufficient selector and computed-style provenance. No hover, pressed, focused, disabled, error, dialog, tab, toast, or authenticated-editor variant is inferred.

Medium- and low-confidence captures (including a 56px white button, a 48px white button, and a 46px input) remain raw observations in `.verification.md`; they are not promoted as canonical component variants because their visible purpose and/or confidence is insufficient. Leaving those medium- and low-confidence captures as raw observations rather than canonical component variants is a derived editorial implementation inference from the verified surfaces; it is not MiriCanvas-authored or a separately published UI specification.

The following applicability note, every interactive-kind verdict, every applicability verdict, the reason given for either, the menu-open panel's non-interactive reading, the refusal to attach a YAML primitive type that the token set does not record, labelling every §4 component `not in the token set`, the refusal to claim hover, pressed, focused, disabled, error, dialog, tab, toast, or authenticated-editor variants from this public packet, and the refusal to treat this as a complete state-coverage claim are a derived editorial implementation inference from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. A generic `focus` observation in the source is not `focus-visible` treatment evidence; the source records that no focus state was captured, and that is not a color assigned to the `focus-visible` row. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted where this packet holds no value for that same canonical state. Absence of an observation is not a `not-applicable` reason. Loading, error, and success follow each control's product role rather than its primitive kind. This is not a complete state-coverage claim.

A `Primitive type` line is attached only when the source YAML records that type on that component. The YAML token set records `tokens.components: {}`. Every component below is a §4 writing and is labelled `not in the token set`. No YAML primitive type is invented.

### Filled primary

- Role: high-confidence public CTA on `home`, `surface-2`, and `surface-3`
- Primitive type: not in the token set
- Kind: interactive
- Anatomy: label
- Background: `#21afbf`
- Text: `#ffffff`
- Radius: 8px
- Padding: 8px 16px
- Font: 14px / 500 / Pretendard Variable
- Height: 40px
- Use: High-confidence CTA record on `home`, `surface-2`, and `surface-3`; representative selector `home::[data-omd-capture="21"]`.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on `home`, `surface-2`, and `surface-3` |
| hover | applicable | Pointer-web CTA; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A public CTA can be made unavailable; visual treatment omitted |
| loading | not-applicable | A public filled CTA on the home, template, and pricing routes hands off to its destination; progress belongs to that destination, not to this control |
| error | not-applicable | The same destination-hand-off role; the control carries no input of its own to validate |
| success | not-applicable | The same destination-hand-off role; the control presents no completion outcome of its own |

### Navigation item

- Role: high-confidence public navigation record on all three surfaces
- Primitive type: not in the token set
- Kind: interactive
- Anatomy: label
- Background: transparent
- Text: `#23242a`
- Radius: 8px
- Padding: 8px 16px
- Font: 14px / 500 / Pretendard Variable
- Height: 40px
- Use: High-confidence public navigation record on all three surfaces; representative selector `home::[data-omd-capture="12"]`.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on all three public surfaces |
| hover | applicable | Pointer-web navigation item; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A navigation item can be made unavailable; visual treatment omitted |
| loading | not-applicable | A public navigation item selects a destination; it commits no operation of its own that could be pending |
| error | not-applicable | The same destination-selection role; a destination that fails to load reports that on the destination, not on this item |
| success | not-applicable | The same destination-selection role; the shown destination is the outcome itself, so the item confirms no separate completion |

### Navigation switcher item

- Role: high-confidence public navigation record on all three surfaces
- Primitive type: not in the token set
- Kind: interactive
- Anatomy: label
- Background: transparent
- Text: `#23242a`
- Radius: 8px
- Padding: 12px
- Font: 14px / 500 / Pretendard Variable
- Height: 44px
- Use: High-confidence public navigation record on all three surfaces; representative selector `home::[data-omd-capture="1"]`.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on all three public surfaces |
| hover | applicable | Pointer-web navigation switcher; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A navigation switcher item can be made unavailable; visual treatment omitted |
| loading | not-applicable | A public navigation switcher item selects among destinations; it commits no operation of its own that could be pending |
| error | not-applicable | The same destination-selection role; a destination that fails to load reports that on the destination, not on this item |
| success | not-applicable | The same destination-selection role; the shown destination is the outcome itself, so the item confirms no separate completion |

### Menu trigger

- Role: high-confidence trigger with `aria-haspopup="menu"`
- Primitive type: not in the token set
- Kind: interactive
- Anatomy: label with a menu affordance (`aria-haspopup="menu"`)
- Background: transparent
- Text: `#23242a`
- Radius: 8px
- Padding: 8px 12px 8px 16px
- Font: 14px / 500 / Pretendard Variable
- Height: 40px
- Use: High-confidence trigger with `aria-haspopup="menu"`; representative selector `home::[data-omd-capture="9"]`.
- Observed: default; `expanded` / `menu-open` captured on the panel below

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured closed-trigger treatment |
| hover | applicable | Pointer-web trigger; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A disclosure trigger can be made unavailable; visual treatment omitted |
| loading | not-applicable | The control’s role is to open a menu (`aria-haspopup="menu"`); opening a menu commits no operation of its own that could be pending |
| error | not-applicable | The same disclosure role; disclosure carries no validation outcome |
| success | not-applicable | The same disclosure role; the menu opening is the outcome itself, so the trigger confirms no separate completion |

### Menu open

- Role: expanded navigation menu, observed only in `expanded` / `menu-open` state on all three surfaces
- Primitive type: not in the token set
- Background: `#ffffff`
- Text: `#000000`
- Radius: 16px
- Padding: 16px
- Shadow: `rgba(11, 60, 65, 0.2) 0px 6px 20px 0px`
- Height: 321px
- Use: Observed only in `expanded` / `menu-open` state on all three surfaces; representative selector `home::[data-omd-interaction-capture="menu-0-0"]`.
- Kind: non-interactive — the record measures the expanded panel surface itself, and the control role belongs to the trigger that opened it, so no state-applicability map is asserted for the panel.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The public packet establishes component-level spacing, not a full grid. Observed spacing values are 4px, 6px, 8px, 12px, 14px, 16px, 20px, 24px, and 56px; the strongest component pairs are 8px 16px, 12px, and 14px 24px. The page-level grid, card-rail behavior, and authenticated-editor layout are unresolved.

Observed public geometry is a small radius ladder: 8px is common on navigation and the filled CTA; 10px, 12px, and 16px occur less often. Do not turn the capture frequency into a claim about an exhaustive design scale.

No viewport comparison or responsive-state capture was supplied. Breakpoints, navigation collapse behavior, touch-target policy, and mobile editor behavior are absent from this reference rather than inferred from desktop public routes.

The 40px filled primary and navigation item, the 44px navigation switcher item, and the 321px menu-open panel are desktop-capture measurements, not cross-viewport specifications. Reading those repeats as component-level spacing rather than as a full grid, keeping `14px` and `56px` as §5 observed writings rather than as YAML spacing keys, reading the listed measurements as desktop-capture rather than as cross-viewport specifications, and keeping breakpoints, navigation collapse behavior, touch-target policy, and mobile editor behavior absent rather than inferred from desktop public routes, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The public product copy is direct, inclusive, and task-oriented. The template route promises that complex design can be completed with a template in three minutes; the business route frames the service as an all-in-one answer for creation, management, and made-to-order printing. This is source context for voice, not evidence of an editor microcopy system. Summarizing that language as direct, inclusive, and task-oriented, classifying the official expressions below as public-route and corporate context rather than as a complete product-microcopy guide, keeping the source's own "not evidence of an editor microcopy system" bound, and keeping the source's avoid-inventing editor error messages, onboarding wording, or persona-specific voice rules bound, are derived editorial implementation inferences from the verified surfaces; they are not MiriCanvas-authored or a separately published UI specification.

| Context | Observed or official expression |
|---------|--------------------------------|
| Homepage | “세상의 모든 디자인은 미리캔버스로 완성” (supplied public-surface capture) |
| Templates | “복잡한 디자인, 템플릿으로 3분이면 끝!” (official template page) |
| Business | “기업의 디자인 고민, 미리캔버스로 한 번에 해결합니다” (official business page) |
| Corporate | MIRIDIH describes making communication through design easier (official corporate page) |

Avoid inventing editor error messages, onboarding wording, or persona-specific voice rules: these were not available in public evidence.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Listing them as a catalog of values the source left unnamed, rather than as coverage of domains the source never named, is a derived editorial implementation inference from the verified surfaces; it is not MiriCanvas-authored or a separately published UI specification.

- semantic error, success, disabled, hover, focus, and editor color tokens
- hover, pressed, focused, disabled, error, dialog, tab, toast, and authenticated-editor visual treatments
- page-level grid, card-rail behavior, and authenticated-editor layout
- breakpoints, navigation collapse behavior, touch-target policy, and mobile editor behavior
- duration, easing curve, transition, and reduced-motion rules
- card, modal, editor, hover-lift, and general elevation policy
- editor error messages, onboarding wording, and persona-specific voice rules
- official product-use type announcement
- 56px white button, 48px white button, and 46px input as canonical component variants
