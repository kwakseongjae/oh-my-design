# microsoft migration log

Source: `web/references/microsoft/DESIGN.md`
Sibling read (not the migration input): `web/references/microsoft/.verification.md`
Destination: `docs/design-md-weight/migrated/microsoft/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/microsoft/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep -oF --` / a Python substring scan against the three output files before it was written (F2). Counts use per-file match lists, never a remembered count.

Source SHA-256 `d83097718b7483b23112d96cf1289930a687d88750dc4b1fbb2f7ccc73121d3f` (`web/references/microsoft/DESIGN.md`). Sibling SHA-256 `99f1e95d7d47e97204920d05a2bafbe886215537e73152f19ea83700a70c2514` (`web/references/microsoft/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope (catalog URL + inspected `ko-kr` route); `primary_color` 옮김 → Scope + Foundations + home CTAs; `logo.type` / `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# Microsoft Design System`. Identity table `provenance.md` Identity. `https://www.microsoft.com` stays in the identity table; inspected home `https://www.microsoft.com/ko-kr` is dual: `DESIGN.md` Scope / Primary tasks + `provenance.md` Surfaces (E2a). `#0078d4` is dual: `DESIGN.md` dest **9** + `provenance.md` dest **4** (E2a). `logo.type: github` / `logo.slug: microsoft` is dual: `DESIGN.md` dest **1** each + `provenance.md` Identity (E2a). |
| YAML `omd: "0.1"`, `verified`, `added`, `verification_v2`, `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true`, `ds.name` / `ds.url` / `ds.type: system` / `ds.description` | 분리 → provenance; Fluent 2 이름·경계·`ds.description` 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source footer's **Verified:** 2026-07-13 is in Freshness. Exact `tokens.source` reconciled DESIGN dest **0** / provenance dest **1**. `components_harvested` DESIGN dest **0** / provenance dest **2**. `ds.type: system` DESIGN dest **1** (Scope writes the ledger key) + provenance dest **2** (E2a). `ds.description` string DESIGN dest **1** / provenance dest **1** (E2a). Conflicts unresolved: none — Freshness. YAML source ids `segoue-font` / `segoue-license` DESIGN dest **0** / provenance dest **2** each (ledger-only; A1c). |
| YAML `tokens.note` | 옮김 → Experience Scope · 분리 → provenance Token note | Verbatim dual dest (E2a): "Marketing tokens below are observed only on microsoft.com home and Microsoft 365 public marketing. Fluent documentation is a separate source domain; its product-system tokens are not inferred from marketing." DESIGN dest **1** / provenance dest **1**. |
| YAML `tokens.colors` (10 keys) | 옮김 → Foundations semantic color | `DESIGN.md` Semantic color. All ten roles kept with names, values, and token-set keys: primary `#0078d4` dest **9**, primary-dark `#091f2c` dest **8**, ink `#0e1726` dest **3**, body `#17253d` dest **3**, nav-ink `#262626` dest **4**, muted `#616161` dest **3**, link `#0067b8` dest **2**, canvas `#ffffff` dest **10**, card `#fefefe` dest **3**, footer `#f2f2f2` dest **3**. Paths `tokens.colors.primary` dest **2** · `primary-dark` dest **1** · `ink` dest **1** · `body` dest **1** · `nav-ink` dest **1** · `muted` dest **1** · `link` dest **1** · `canvas` dest **1** · `card` dest **1** · `footer` dest **1**. `#06161f` dest **5** and `#051118` dest **3** stay on those component records, not color-token keys. Qualifier at Semantic color (B2/B2a). |
| YAML `tokens.typography.family` (`ui` / `display` / `chrome`) | 옮김 → Typography & Assets Family | `Segoe UI Variable Text` DESIGN dest **16**. `Segoe UI Variable Display` DESIGN dest **9**. `Segoe UI` as chrome family. Paths `tokens.typography.family.ui` dest **1** · `display` dest **1** · `chrome` dest **1**. The no-substitution rule and canonicity-because-loaded reading carry an adjacent complete qualifier (B2/B2a). |
| YAML `tokens.typography.display` / `section` / `card-title` / `body` / `cta` / `nav` | 옮김 → Typography & Assets Type roles | Token-set table keeps unitless `1.17` DESIGN dest **3** · `1.25` dest **3** · `1.4` dest **6** · `1.5` dest **3** · `1.47` dest **3** and is never converted to a replacement px (A1a). Observed `48px / 56px` dest **1** · `32px / 40px` dest **1** · `20px / 28px` dest **1** · `16px / 24px` dest **1** · `15px / 22px` dest **1** stay on the hierarchy table. `13px / normal` dest **2** (hierarchy table + Layout). `11px / 16px` dest **3** (Type roles keep-both sentence + hierarchy table + Layout). Layout restates the two chrome rows as separate desktop-capture measurements. YAML `use` strings restored verbatim: `Observed home and Microsoft 365 public-marketing headings` dest **1** · `Observed marketing section headings` dest **1** · `Observed home card headings` dest **1** · `Observed marketing body copy` dest **1** · `Observed marketing action labels` dest **1** · `Observed home global navigation` dest **1**. Tracking `-1.2` dest **4** beside `-1.2px`; `-0.8` dest **4**; `-0.5` dest **4**; `-0.48` dest **5**; `-0.3` dest **4**. YAML Display off observed `Segoe UI Variable Text / Display` dest **2**. Qualifier at Type roles. |
| YAML `tokens.spacing` (`xs: 4`, `sm: 8`, `md: 12`, `base: 16`, `lg: 24`, `xl: 32`, `xxl: 48`) | 옮김 → Foundations spacing | Compact line `xs: 4` dest **1** · `sm: 8` dest **1** · `md: 12` dest **1** · `base: 16` dest **1** · `lg: 24` dest **1** · `xl: 32` dest **1** · `xxl: 48` dest **1**. Px cluster `4px, 8px, 12px, 16px, 24px, 32px, and 48px` kept as the source's own writing. `tokens.spacing.base` dest **1** is a spacing step — not body size `16`. Qualifier at Spacing. |
| YAML `tokens.rounded` (`sm: 2`, `md: 4`, `lg: 8`, `xl: 24`, `pill: 200`) | 옮김 → Foundations shape | `pill: 200` DESIGN dest **3** / provenance dest **2** (E2a). `tokens.rounded.pill` dest **1**. CTA `8px` stays on `lg: 8`. Chrome `0px` stays on header/footer records; no YAML sharp key invented. Qualifier at Shape. |
| YAML `tokens.components.m365-tab-active` | 옮김 → Components & States Active pill tab | Verified primitive type preserved on this component only, not flattened to `Kind` (A1b): `Primitive type: \`tab\`` dest **1** = source YAML `type: tab`. `Token-set use: \`Observed active Microsoft 365 pill tab\`` dest **1**. Path `tokens.components.m365-tab-active` dest **1** (`type: tab`, `bg: #06161f`, `fg: #ffffff`, `radius: 200px`, `padding: 8px 24px`, `height: 40px`, `font: 16px / 600 Segoe UI Variable Text`, `active: true`, `use: Observed active Microsoft 365 pill tab`). `active: true` dest **1**. The other seven §4 records are `not in the token set` dest **8**. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope paragraphs 1–2; Distinctive traits restates Key Characteristics. Generous white space / product imagery / blue calls to action / familiar Segoe typographic voice; coherent rather than experimental; July 2026 capture; Microsoft-hosted Segoe variable webfonts; Communication Blue; mission sentence; clear hierarchy and deliberately familiar controls; dark blue-black headings; 8px blue action controls; 200px active pill tabs; authenticated product, Windows app, or documentation surface refusal; Fluent 2 is a separate official system domain; its component guidance is not used to fill missing marketing variants. Surface-boundary reading and atmosphere reading carry adjacent complete Fluent-named qualifiers (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Observed public-marketing list kept with source role names. Fluent 2 boundary sentence kept: design-system values are useful for Fluent implementations, but they are not assigned to the marketing token set unless they occur in the supplied marketing capture. Role-slotting qualifier at Semantic color (B2/B2a). |
| §3 Typography Rules — evidence classes, hierarchy, Fluent context | 옮김 → Typography & Assets | Evidence classes keep `797 visible uses` dest **1** · `242 visible uses` dest **1** · `58 visible uses` dest **1** · `only two captured text uses` dest **2**. FAQ: `Windows fonts cannot be self-hosted from a Windows installation` dest **1**; Monotype dest **1**; Segoe UI Variable not available for licensing outside Microsoft products or non-Windows platforms dest **1**. Declared-only FabricMDL2Icons / MWF-FLUENT-ICONS / Leelawadee UI Web / SegoeUI Fallback dest **2** each in DESIGN.md. Cascadia Code dest **4** (Avoid + Font evidence + Font-evidence qualifier + Named gaps). Fluent ramp `10px/14px` dest **2** · `68px/92px` dest **2** kept off marketing roles. Qualifiers at Font evidence, Family, Type roles. |
| §4 Component Stylings | 옮김 → Components & States | Eight records: Standard CTA, Hero CTA, Dark CTA, Compact dark CTA, Outline CTA, Active pill tab, Header link, Footer link. Selectors dual dest with `provenance.md` Capture selectors (E2a): `data-omd-capture="14"` dest **1** / P dest **1** · `data-omd-capture="17"` dest **1** / P dest **1** · `tab-2-10` dest **1** / P dest **1** · `tab-2-25` dest **1** / P dest **1** · `tab-2-53` dest **1** / P dest **1** · `tab-2-0` dest **1** / P dest **1** · `.uhf-nav-item.uhf-nav-link` dest **1** / P dest **1** · `.uhf-footer-link` dest **1** / P dest **1**. Padding `12px 17px` dest **1** · `8px 16px` dest **1** · `8px 24px` dest **2** · `4px 12px` dest **2** · `12px 16px` dest **1** · `0px 8px` dest **1**. Heights 40 / 48 / 52 / 36 / 54 kept. Outline CTA has no invented background (sibling `transparent background` is ledger-only). `No inactive-state rule is asserted` dest **2**. `no hover, disabled, validation, or menu variant is inferred from a class name or general Fluent guidance` dest **1**. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; 검사 URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. Home / Microsoft 365 / Fluent 2 URLs are dual: Scope + provenance Surfaces (E2a). About URL `https://www.microsoft.com/en-us/about` DESIGN dest **0** / provenance dest **1** (footer-only; About facts are in Scope without that URL). Font-list and font-FAQ URLs DESIGN dest **0** / provenance (facts in Typography). `getdesign` DESIGN dest **0** / provenance dest **1**. `styles.refero.design` DESIGN dest **0** / provenance dest **3**. Conflicts unresolved: none — Freshness only. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `large image-led bands` dest **1**. `dense product-marketing modules` dest **1**. Spacing cluster. `does not establish a universal layout scale for authenticated apps` dest **2**. `Do not use Fluent documentation geometry to fill gaps in the two captured marketing surfaces` dest **1**. Qualifier at Layout. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `mostly flat public-marketing actions and containers` dest **1**. `does not provide a stable, cross-surface shadow recipe` dest **1**. `no shadow token is exported` dest **1**. `Fluent elevation guidance belongs to the Fluent design-system domain` dest **2**. Qualifier at Elevation. |
| §7 Do's | 옮김 → Experience application rules | Four items, matching the source Do list, under the grouping qualifier. Not placed in Governance controlled copy. `Scope \`#0078d4\` 8px CTAs to the captured home-marketing surface` dest **1**. `Preserve the 13px Segoe UI global chrome and 11px footer treatment` dest **1**. |
| §7 Don'ts | 옮김 → Experience avoid | Four source Don'ts under the qualifier. `Substitute a system font and label it Segoe UI Variable` dest **1**. `Promote Cascadia Code or icon fonts from zero-use declarations into UI typography` dest **1**. `Invent inactive, disabled, hover, error, or responsive variants not recorded by the collector` dest **1**. No invented domain (no Azure portal, no Store checkout in `DESIGN.md`; those sibling-only names stay in provenance Sibling handling as mention). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `desktop-only (1440×900)` dest **1**. `40px, 48px, and 52px` dest **1**. `mobile breakpoints, navigation collapse behavior, or touch-target rules` dest **1**. Unresolved items are omitted as values, not denied as invented measurements (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` §9 deletion check. Home `#0078d4` / 8px, Microsoft 365 `#091f2c` / 8px, active pill `#06161f` / 200px, heading/body inks, and chrome/footer colors are in Foundations / Components. |
| §10 Voice & Tone | 옮김 → Content & Locales | Voice paragraph dest; context table dest; samples byte-exact: `We empower the world` dest **1** · `Your everyday AI companion` dest **1** · `What will you do with Copilot?` dest **1** (A5). `empowering, inclusive, and practical` dest **1**. `companion language rather than technical showmanship` dest **1**. `narrative and content boundary, not an observed rule for every product team` dest **1**. Qualifier at Content (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `to empower every person and every organization on the planet to achieve more` dest **1** (Scope ¶2). `empowering every person and every organization on the planet to achieve more` dest **1** (Scope ¶3). Not one string dest 2. `expanding opportunity, earning trust, protecting fundamental rights, and advancing sustainability` dest **2**. `complementary—not interchangeable` dest **3**. Marked as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` Proof notes. |
| §12 Principles — 4 numbered | 옮김 → Experience principles | Four stems + UI implications under the B2a form: Microsoft-authored mission/About (1–2) and Fluent 2 documentation (3–4); UI implications and grouping are derived. `safe, secure, and responsible digital world` dest **1**. `native platform defaults` dest **1**. `global and alias token layers` dest **1**. Microsoft has a published first-party design system (Fluent 2), so the close names that specification (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` `## Derived editorial inventory` (**23** data rows). |
| §13 Personas | 삭제 | The source recorded no validated research defining product-specific personas and used a `[FILL IN]` placeholder. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or affiliation appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience restates the source's own “does not provide validated research defining product-specific personas” sentence. The three `primary-tasks` come from labels and surfaces the source records; the Primary-tasks qualifier says so. |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Source body preserved: `Fluent documentation form-error interaction` dest **2**; `Microsoft 365 dialog/tab interactions` dest **2**; `does not provide reusable error, success, loading, empty, disabled, or responsive state specifications` dest **1**. `[FILL IN]` wrapper omitted (A1) and stored in provenance Omission ledger (E2b). Graph not used (A2). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Eight interactive controls declare the seven canonical states. Destination CTAs, destination chrome links, and the tab close loading/error/success with a role reason (C2): `loading \| not-applicable` dest **8**. `loading \| applicable` dest **0**. `default` and `focus-visible` stay applicable. Generic Focus is not treated as a `focus-visible` treatment (B1). Non-observation is never used as a `not-applicable` reason (C1). This is not a complete state-coverage claim (C3). Capture-record qualifier covers every Reason cell. |
| §15 Motion & Easing | 옮김 → Foundations motion | `motion can be tokenized` dest **1**. `does not provide a source-backed Microsoft-wide duration or easing table` dest **1**. `[FILL IN]` wrapper omitted (A1) and stored in provenance Omission ledger (E2b). B3 is held: Foundations Motion names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). `Promote a motion token` dest **2**. `transition properties` dest **2**. `animation name` dest **2**. `reduced-motion` dest **2**. Governance Named gaps restates the five kinds and the gate as a motion-token promotion. The five-kind gate itself carries an adjacent complete B2a close on the same line. |
| §15 Motion & Easing — unattributed template curves | 해당 없음 (삭제할 무출처 커브 없음) | The source does not contain `cubic-bezier(0.4, 0.0, 1, 1)` or the other `spec/omd-v0.1.md` example-table curves. dest **0** / provenance dest **0**. |

## Sibling handling (`web/references/microsoft/.verification.md`)

The sibling exists — confirmed with `find web/references/microsoft -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: RGB spellings; `home::h1` / `home::h2`; tab line-height `16px / 600 / 24px`; outline-CTA `transparent background`; Fluent docs menu toggle `#fae9ec` / `#1b1a19` / `8px 14px` / `14px / 600`; collector totals `score: 100` / `componentVariants: 117` / `interactionCount: 5`; WOFF2 path under `echo/etc.clientlibs`; `c.s-microsoft.com` Segoe UI prefix; `five Microsoft-hosted Segoe Sans source families`; sibling domain-boundary names Store checkout and Azure portal; Refero gloss that one page denies a display face / 2px-radius / 0px-card. Recorded at `provenance.md` sibling-only list (provenance wording is `denies a display face`, not the sibling exact `no display face`).
- Measured `DESIGN.md` 0 for those sibling-only strings: `rgb(0, 120, 212)` dest **0** · `home::h1` dest **0** · `#fae9ec` dest **0** · `#1b1a19` dest **0** · `Azure portal` dest **0** · `Store checkout` dest **0** · `transparent background` dest **0** · `16px / 600 / 24px` dest **0** · `no display face` dest **0**. `denies a display face` DESIGN dest **0** / provenance dest **1**. `no display face` provenance dest **0** (mention in this log is not provenance use).

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Microsoft's published lines in the source are Latin, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published official / captured labels in the source body | 21 distinct | 0 | 0 | Microsoft / Fluent 2 / Communication Blue / Segoe UI / Segoe UI Variable Text / Segoe UI Variable Display / Segoe UI Variable Small / SegoeUI / Cascadia Code / FabricMDL2Icons / MWF-FLUENT-ICONS / Leelawadee UI Web / SegoeUI Fallback / Monotype / Copilot / Microsoft 365 / Windows / We empower the world / Your everyday AI companion / What will you do with Copilot? / empower every person and every organization on the planet to achieve more. |
| YAML `use` strings | 7 distinct | 0 | 0 | Observed home and Microsoft 365 public-marketing headings / Observed marketing section headings / Observed home card headings / Observed marketing body copy / Observed marketing action labels / Observed home global navigation / Observed active Microsoft 365 pill tab. |
| Sibling published strings | 0 brand-published extras | 0 | 0 | Refero glosses and collector totals are not Microsoft-published copy. They survive in provenance as sibling-only records (`denies a display face`); not promoted into `DESIGN.md`. |

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 21 + YAML use 7 extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand microsoft --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 138 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 138.

## Deviations recorded

- `DESIGN.md` is above the spec's 600–1,800-word SHOULD budget (`wc -w` 6169). The budget yielded to A1: eight declared component records with selectors and seven-state maps, dual token-set / observed type tables, the full §1/§11 mission-and-About narrative including complementary—not interchangeable, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Microsoft has a published first-party design system (Fluent 2), so every derived-editorial close names that specification (rulebook v12 B2a 전제 주석). Measure `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = **23** = `not Microsoft-authored` **23** = `including the published Fluent 2 documentation` **23**. Provenance `## Derived editorial inventory` **23** data rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is a Microsoft-issued fact or a derived reading. Scope surface-boundary, atmosphere/coherent-rather-than-experimental, and narrative-as-context; primary-task selection; Audience restriction; trait grouping; Principles / Do / Don't headers; Semantic role slotting including unmerged blues/inks/whites; spacing unitless-vs-px / not-universal-scale; shape key-path separation; elevation and motion-gate readings; font evidence including declared-only-not-UI-token / family no-substitution and canonicity-because-loaded / type-role keep-both; body-`16`-off-spacing-`16`; github-slug-as-pointer; applicability procedure including YAML `type: tab` only on Active pill tab and generic Focus not as `focus-visible` treatment; layout 1440×900-capture and Fluent-geometry-not-filling-gaps; byte-exact / content-boundary reading; and the unresolved-list naming each carry an adjacent complete Fluent-named qualifier. Source facts (hexes, YAML keys, published strings, 797/242/58 counts, selectors, FAQ sentences, mission sentence) were left unqualified. Inventory: **23** lines. Audience restates the source's own no-research sentence and qualifies only the restriction step. Content restates the source §10 paragraph; the official-guidance-not-complete-microcopy reading is qualified there.

## F2 E2 rescan (mandatory final pass)

Each log row was written only after a substring scan against `DESIGN.md` and `provenance.md`, then remeasured after the F3 body/ledger edits (wave 40) and again after the independent-review un-fusion / motion-gate wording (this pass). Dual destinations named above were confirmed in both files, including Hero CTA `data-omd-capture="14"`. B3 is claimed only because Foundations Motion names all five evidence kinds and the per-component gate in full text (E2c). Governance Named gaps restates the five kinds and the gate. No "B3 유지" claim is made beyond that. Sibling-only strings are claimed dest 0 after measurement. Persona identifiers are claimed dest 0 / provenance 0 as use, not as a self-denominator absence sentence that lists names (D2a, E2d).

## Unique-expression contrast (제출 전 자가 대조)

뽑은 고유 표현 152 (연도·고유명사·인용구 + §1/§11 서사 연결 + 값 수식어 + §15/§5/§7/§8 제약). First `grep -oF` pass: 148 present, **4 zero** (straight-apostrophe rewrite of source U+2019 possessives). Restored 4 to source bytes: `collector’s tab interaction`; `Fluent’s official semantic tokens`; `product’s capability`; `user’s task and next action plainly`. After restore: 152/152 in `DESIGN.md`. Restored count: 4. Additional keep-both restore (not in the 4): source §1 clause `while the documentation site is evidence for the Fluent design system rather than a proxy for every Microsoft product UI` and the em-dash surface split `two public marketing surfaces—Microsoft home and Microsoft 365—from Fluent 2 documentation chrome` (an earlier paraphrase had been `not a proxy for every Microsoft product UI` without `evidence`).

Persona given names, cities, motivations, and affiliation classifications: unidentified deletion (D2a). Not re-listed here.
