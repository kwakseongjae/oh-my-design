# Cathay Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Cathay United Bank (國泰世華銀行) is the banking arm of Cathay Financial Holding (國泰金控). Catalog homepage identity is `https://www.cathaybk.com.tw`. Treating that product-origin sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

Treating the following URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, homepage-value-not-a-proxy-for-group-company-or-CUBE-App-chrome, cathayholdings-and-App-Store-as-country-identity-not-token-captures, and redirect-host-as-the-same-captured-homepage-rather-than-a-second-token-set, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. This contract covers the 2026-06-08 live inspect of the 國泰世華銀行 homepage requested at `https://www.cathaybk.com.tw` and resolved to `https://www.cathay-cube.com.tw/cathaybk`. Source footer also names `https://www.cathay-cube.com.tw` as a Tier 1 host for that same inspect. Color, type, and component values below stay attached to that captured homepage. A homepage value is not a proxy for 國泰人壽, 國泰產險, 國泰綜合證券, or 國泰投信 surfaces named in the footer and narrative, and is not a proxy for CUBE App chrome. `https://www.cathayholdings.com` and the Apple App Store TW CUBE App listing are country-identity sources in the source packet; they are not extra token captures in this reconstruction.

Source token note: primary = deep brand green `#00512a` (live primary CTA fill); accent = bright Cathay green `#26a862` (most-used non-neutral, icon + emphasis). Treating that note as a register split — `#00512a` is catalog `primary_color` and the harvested primary-CTA fill rather than a wash and rather than accent `#26a862`; `#26a862` is the bright accent for icons, links, and emphasis rather than a second primary CTA fill; `#fafafa` is canvas rather than surface `#ffffff`; `#333333` is heading ink rather than body `#555555` or muted `#888888`; and `#00283d` is footer / dark-surface grounding rather than heading ink — is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

The following atmosphere readings — calm-dependable-institutional-weight-with-surprising-lightness, never-heavy-or-corporate-cold, warmth-from-a-single-confident-brand-green, trustworthy-and-grounded-never-stuffy, steady-partner-for-everyday-financial-life-not-a-marble-columned-vault, utilitarian-friendly-rather-than-aspirational-luxury, green-as-punctuation-not-a-wash, no-decorative-gradients, no-pill-shaped-buttons, no-playful-overshoot, and customer's-task-above-the-brand's-ego — are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification. The source describes an almost-white canvas (`#fafafa`) carrying near-black `#333333` text. Two greens do the recorded work: deep `#00512a` on primary actions and brighter `#26a862` on icons, links, and emphasis. Headlines run at weight 700 (36px hero, 28px sections). Line-height is recorded at 1.50. Interactive surfaces in the harvested set settle on a 5px corner. Quick-action tiles — 換匯, 提款, 轉帳, 視訊 — sit below the hero in the source layout. Neutrals (`#333333`, `#ffffff`, `#555555`) are recorded as carrying the vast majority of color applications; `#00512a` and `#26a862` together account for only a few dozen of the hundreds of applications in that live scan. A faint mint tint (`#f3fff7`) backs green-themed sections. Deep teal-navy (`#00283d`) appears in the footer and on dark surfaces.

Treating the following public-history facts as narrative rather than interface tokens, including 2003-merger-of-Cathay-Commercial-Bank-and-United-World-Chinese-Commercial-Bank, group-footer-companies-as-live-DOM-links, CUBE-as-contemporary-digital-identity, and redirect-into-cathay-cube-as-a-strategic-bet-sentence, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. The bank in its current form is the product of the 2003 merger of Cathay Commercial Bank and United World Chinese Commercial Bank (世華聯合商業銀行). The broader Cathay group — life insurance (國泰人壽), property insurance (國泰產險), securities (國泰綜合證券), investment trust (國泰投信), and more — is surfaced in the bank's own footer. The contemporary digital identity in the source centers on the CUBE brand: the CUBE App and CUBE credit card (CUBE信用卡). Taglines "每次都是更好的體驗" and "用科技讓金融生活更安全簡單" are observed verbatim from the live homepage DOM. Public-history narrative and those live strings provide that context; they do not by themselves supply interface tokens.

The following refusal, embrace, cold-marble-and-gold-clichés, hype-driven-disruptor-fintech, single-trustworthy-brand-green-used-with-discipline, airy-off-white-surfaces, bold-but-friendly-Traditional-Chinese-type, and task-first-layout readings are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification. What the source treats Cathay's design as refusing: the cold marble-and-gold visual clichés of legacy banking, and the hype-driven aesthetics of disruptor fintech. What it treats the design as embracing: a single trustworthy brand green used with discipline, airy off-white surfaces, bold-but-friendly Traditional Chinese type, and a task-first layout that treats the customer's daily needs as the product.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings / live homepage labels below as Primary tasks, and not lifting tasks from source §13 fictional archetypes, and treating independently-verified-in-this-packet as the inspected homepage URL rather than the named CTA and tile labels, which remain harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Independently verified in this packet is the inspected homepage URL; named CTA and tile labels are harvested strings/controls, not independently verified destination routes.

- Act on a harvested homepage primary CTA (YAML use: `Primary CTA (把握好機匯, 我有興趣)`).
- Enter an everyday banking task from the harvested quick-action strip ("換匯", "提款", "轉帳", "視訊").
- Open a harvested glass-tile service (YAML use: `Quick-entry glass tiles over hero imagery (開戶, 匯率查詢)`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. No individual personas are promoted. Observable work follows the three primary tasks.

### Distinctive traits

The following unmerged-role extras — treating `#00512a` as catalog `primary_color` and primary-CTA fill rather than accent `#26a862`, treating `#26a862` as bright accent rather than a second CTA fill, treating `#fafafa` as canvas rather than surface `#ffffff`, treating on-primary `#ffffff` as the same hex as surface rather than a second paint, treating `#333333` / `#555555` / `#888888` as unmerged inks, treating `#00283d` as footer grounding rather than heading ink, treating 5px harvested corners as local rather than YAML `rounded.full` 9999 or link 0px, treating 50px primary-CTA height as unmerged from 40px 登入 height and from ≈187px glass tiles, treating Roboto Flex + Noto Sans TC as a live stack rather than one family, and treating 700/400 as the recorded hierarchy engine rather than a medium weight — are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

- Catalog `primary_color` / Cathay Deep Green `#00512a` as the harvested primary-CTA fill ("把握好機匯", "我有興趣")
- Bright Cathay green `#26a862` as the most-used non-neutral accent (37 applications in the live scan)
- Off-white canvas `#fafafa` with heading ink `#333333`; surface white `#ffffff` on cards and tiles
- Roboto Flex + Noto Sans TC stack; headlines 36px / 28px at weight 700; body 16px at weight 400; line-height 1.50
- Uniform 5px radius on harvested buttons, cards, tiles, and inputs; 0px on plain text links and full-width footer rows
- Primary CTA 50px / `12px 20px` / 16px / 400; utility 登入 40px tall; glass tiles ≈187px with `40px 15px`
- Deep teal-navy `#00283d` footer band with white 14px links
- Mint tint `#f3fff7` behind green-themed section bands; warn orange `#e87c07` as warning only

### Principles

These seven items (Trust is the product, Green with discipline, Service before marketing, Readable in Traditional Chinese first, One radius, one rhythm, Warm, not cold, Honest depth) are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

1. **Trust is the product.** Every color, weight, and corner radius serves the perception of a safe, dependable bank. Design that gambles trust for novelty is off-brand.
2. **Green with discipline.** The brand green earns its impact precisely because it is rare — punctuation, never wallpaper. Neutrals carry the page so green can mean something.
3. **Service before marketing.** The most common banking tasks sit above the fold in dense quick-action tiles; promotion gets the lower, airier real estate.
4. **Readable in Traditional Chinese first.** 1.5 line-height and the Noto Sans TC pairing are non-negotiable — Han legibility outranks Latin-centric tightness.
5. **One radius, one rhythm.** The uniform 5px corner across every surface is a quiet consistency that reads as institutional reliability.
6. **Warm, not cold.** Off-white over stark white, bold-but-friendly headlines, optimistic bright green — the bank chooses approachability over austerity.
7. **Honest depth.** Shadows stay neutral and subtle. Elevation signals function, never spectacle.

Treating the following as a capture-bound application of source §7 Do's, including deep-green-`#00512a`-for-every-primary-CTA-fill-and-outline-border, bright-green-`#26a862`-for-icons-links-and-emphasis, canvas-`#fafafa`-with-`#333333`-text, weight-700-headlines-and-weight-400-elsewhere, uniform-5px-radius, 1.5-line-height, neutral-untinted-soft-black-shadows, and footer-grounding-in-`#00283d`, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

- Use deep green `#00512a` for every primary CTA fill and outline border.
- Reserve bright green `#26a862` for icons, links, and emphasis accents.
- Keep the canvas off-white (`#fafafa`) with `#333333` near-black text.
- Use weight 700 for headlines, weight 400 for everything else.
- Apply the uniform 5px radius to buttons, cards, tiles, and inputs.
- Maintain 1.5 line-height for comfortable Traditional Chinese reading.
- Use neutral (untinted) soft black shadows for elevation.
- Ground the footer / dark surfaces with deep teal-navy `#00283d`.
- Write warm, service-first Traditional Chinese copy — verbs and reassurance over hype. Treating that copy rule as a capture-bound restatement of source §16 Do rather than a separately published voice specification is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

### Avoid

The following items copy source §7 Don'ts and unique source §16 Don'ts, including green-as-punctuation, no-pill-or-large-radii, no-medium-headline-weight, no-tinted-shadows, no-CJK-letter-spacing, orange-`#e87c07`-warning-only, no-replacing-`#fafafa`-with-stark-pure-white, no-bold-body-nav-or-button-labels, no-drop-below-1.5-line-height, and no-trading-calm-trustworthy-tone-for-disruptor-excitement. They are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

- Don't wash large areas in green — green is punctuation, neutrals dominate.
- Don't use pill shapes or large radii — the gentle 5px is the brand.
- Don't introduce a medium weight between 400 and 700 on headlines.
- Don't tint shadows with brand color — Cathay shadows stay neutral black.
- Don't add letter-spacing to CJK text — it breaks Han rhythm.
- Don't use orange `#e87c07` as an interactive/primary color — warning only.
- Don't replace off-white `#fafafa` with stark pure white across full pages.
- Don't bold body, nav, or button labels — they stay weight 400.
- Don't tighten CJK letter-spacing or drop below 1.5 line-height.
- Don't trade the bank's calm, trustworthy tone for disruptor excitement. Treating that last-bullet characterization as a reconstruction reading of source §16 rather than Cathay-authored tone doctrine is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including primary-CTA-fill-not-accent, accent-not-a-second-CTA, canvas-off-white-not-surface-white, on-primary-white-as-the-same-hex-as-surface-rather-than-a-second-paint, heading-ink-not-body-not-muted, teal-navy-not-heading-ink, hairline-not-border-mute, mint-not-a-second-green-fill, warn-orange-never-an-interactive-primary, stable-mature-trustworthy, barely-there-green-white, near-black-with-no-color-cast, and optimistic-counterpart — are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification. Catalog `primary_color` `#00512a` is YAML `primary` and the harvested CTA fill; it is not accent `#26a862`. YAML `accent` `#26a862` is the bright green. YAML `canvas` `#fafafa` is page background; YAML `surface` `#ffffff` is cards, tiles, dropdowns, and content containers. YAML `on-primary` `#ffffff` is text and icons on deep-green fills; it is the same hex as surface, not a second paint. YAML `heading` `#333333`, `body` `#555555`, and `muted` `#888888` are unmerged inks. YAML `ink-deep` `#00283d` is footer / dark-surface grounding. YAML `hairline` `#eeeeee` and `border-mute` `#bebebe` are unmerged borders. YAML `accent-tint` `#f3fff7` is mint section bands. YAML `warn` `#e87c07` is warning / attention, never an interactive primary. YAML component fields such as primary CTA `fg` `#ffffff` and nav-link `fg` `#333333` stay on those controls.

- **Cathay Deep Green / Primary** (`#00512a`): catalog `primary_color`. YAML `primary`. Harvested primary CTA fill and outline-button border ("把握好機匯", "我有興趣", "更多新鮮事").
- **Cathay Bright Green / Accent** (`#26a862`): YAML `accent`. Icons, inline emphasis, link affordances. Most-used non-neutral in the live scan (37 applications).
- **Mint Tint** (`#f3fff7`): YAML `accent-tint`. Green-themed section bands and soft highlight zones.
- **Off-White Canvas** (`#fafafa`): YAML `canvas`. Page background.
- **Surface White** (`#ffffff`): YAML `surface`. Card and tile surfaces, dropdown panels, content containers.
- **On-Primary White** (`#ffffff`): YAML `on-primary`. Text and icons on deep-green fills. Same hex as surface; not a second paint.
- **Heading Ink** (`#333333`): YAML `heading`. Primary text and headings. Dominant color in the live scan (592 applications).
- **Body Slate** (`#555555`): YAML `body`. Secondary text, sub-navigation links, descriptive copy.
- **Muted Gray** (`#888888`): YAML `muted`. Tertiary text, timestamps, disabled-adjacent labels, input placeholder.
- **Deep Teal-Navy** (`#00283d`): YAML `ink-deep`. Footer background and dark-surface grounding.
- **Hairline** (`#eeeeee`): YAML `hairline`. Default border and divider for cards, list rows, and inputs.
- **Border Mute** (`#bebebe`): YAML `border-mute`. Stronger neutral border for emphasized containers and form outlines.
- **Warn Orange** (`#e87c07`): YAML `warn`. Sparingly used warning / attention accent. Never an interactive primary.

### Spacing

YAML scale: xs 5, sm 11, md 15, base 20, lg 27, xl 40, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names ~5px as a base unit and lists 5px, 11px, 15px, 20px, 27px, 40px. Treating those YAML numbers as recorded without a required px suffix, treating body 5px / 11px / 15px / 20px / 27px / 40px as body-recorded steps rather than a rewrite of the YAML keys, treating YAML section 64 as a YAML step rather than adding a px suffix to that number, and treating harvested control heights 50px / 40px / ≈187px and paddings `12px 20px` / `21-27px` / `40px 15px` as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

The following ~5px-base / generous-internal-breathing-room-on-tappable-surfaces readings are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification. Body layout states a base unit of ~5px, with the live DOM favoring multiples and near-multiples: 5px, 11px, 15px, 20px, 27px, 40px. Notable body paddings: button `12px 20px`, card `21-27px`, glass-tile `40px 15px`.

### Shape

YAML `rounded`: sm 5, md 5, lg 5, full 9999.

The following local-geometry reading, including 5px-on-buttons-cards-tiles-and-inputs, 0px-on-plain-text-links-and-full-width-footer-rows, no-pill-shapes, no-large-12px-plus-rounding, and YAML-full-9999-as-a-YAML-step-not-a-harvested-pill, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

- Small / Medium / Large (5px): buttons, cards, tiles, and inputs (YAML `rounded.sm` / `md` / `lg`)
- Plain text links and full-width footer rows: 0px
- YAML `rounded.full` 9999: recorded as a YAML step. Source body states no pill shapes and no large 12px+ rounding; 9999 is not converted into a harvested pill radius and no `px` suffix is added.

Treating 5px corners as local harvested geometry on the named controls rather than a universal radius for every unlisted control, and treating YAML `full` 9999 as a YAML step rather than merged with 5px or with 0px, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. 5px on buttons, cards, tiles, and inputs is local harvested geometry, not a universal radius for every unlisted control. YAML `full` 9999 is recorded as a YAML step; it is not merged with 5px and not merged with link 0px.

### Elevation

The following elevation-table Use readings, including subtle-card-lift-hover-hints, content-and-product-cards, dropdowns-modals-floating-menus, plus the honest-and-neutral / no-color-tint / in-deliberate-contrast-to-fintechs-that-brand-their-shadows / subtle-trustworthy-lift-rather-than-dramatic-float / green-reserved-for-brand-intent-rather-than-shadow / glass-frosted-layered-feel-without-heavy-shadow / footer-grounding-contrast / mint-figure-ground-without-elevation / overlay-shadow-and-scrim-as-two-source-stated-uses-of-the-same-value / not-merged-with-0.5-or-ambient-0.08-or-standard-0.12 readings that follow the table, are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, footer |
| Ambient (Level 1) | `rgba(0,0,0,0.08) 0px 2px 8px` | Subtle card lift, hover hints |
| Standard (Level 2) | `rgba(0,0,0,0.12) 0px 4px 16px` | Content cards, product cards |
| Overlay (Level 3) | `rgba(0,0,0,0.22) 0px 8px 24px` | Dropdowns, modals, floating menus |

YAML `tokens.shadow.ambient`: `rgba(0,0,0,0.08) 0px 2px 8px`. YAML `tokens.shadow.standard`: `rgba(0,0,0,0.12) 0px 4px 16px`. YAML `tokens.shadow.overlay`: `rgba(0,0,0,0.22) 0px 8px 24px`.

Treating overlay-shadow-and-scrim as two source-stated uses of the same `rgba(0,0,0,0.22)` value, not-merged-with-`rgba(0,0,0,0.5)`-or-ambient-0.08-or-standard-0.12, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. The live DOM also shows semi-transparent black overlays (`rgba(0,0,0,0.5)`, `rgba(0,0,0,0.22)`) used for scrims and floating layers. `rgba(0,0,0,0.22)` as overlay shadow and as scrim are two source-stated uses of the same value; they are not merged with `rgba(0,0,0,0.5)` and not merged with ambient `0.08` or standard `0.12`. Decorative depth recorded in the source: glass tiles (`rgba(255,255,255,0.5)`) over hero imagery; dark footer band (`#00283d`); mint tint (`#f3fff7`) sections.

### Motion

Source-stated duration roles. Treating the duration table, easing names, signature motions, and reduced-motion line as source-stated rather than computed CSS, and treating Arriving / Dismissals / Two-way / spec-template-ease-exit-match / no-bounce-no-spring-no-playful-elastic / bank-motion-steady-and-predictable / tile-rises-it-does-not-light-up / subtle-tactile-trustworthy / function-is-never-sacrificed-for-delight readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 150ms | Hover, focus, button press states |
| `motion-standard` | 250ms | Dropdowns, tile hover lift, card reveals |
| `motion-slow` | 400ms | Page-level transitions, hero / carousel reveals |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Most two-way transitions |
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — dropdowns, menus |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |

Signature motions (source-stated):

Treating no-bounce-or-spring-or-playful-elastic on core chrome as a bank-discipline reading, treating carousels-and-hero-reveals-at-the-slow-end as source-stated rather than computed CSS, and treating the following signature-motion readings — tile-rises-it-does-not-light-up, subtle-tactile-trustworthy, and function-is-never-sacrificed-for-delight — as editorial rather than computed CSS, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

Source forbidden: no bounce, no spring overshoot, no playful elastic motion. Carousels and hero reveals on the homepage move at the slow end (≈400ms).

1. **Quick-action tile hover.** Gentle lift with the standard shadow (`rgba(0,0,0,0.12) 0px 4px 16px`) at `motion-standard / ease-standard`. No color flash — the tile rises, it does not light up.
2. **Primary CTA press.** Brief darkening of the `#00512a` fill at `motion-fast`. Subtle, tactile, trustworthy.
3. **Reduce motion.** Under `prefers-reduced-motion: reduce`, all transitions collapse to near-instant. Carousels become static. Function is never sacrificed for delight.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-computed-Roboto-Flex-plus-Noto-Sans-TC, YAML-family-keys-naming-those-live-computed-families, fallback-stack-not-the-brand-face, pairing-as-deliberate-CJK-Latin-evenness, and outside-the-captured-homepage, are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Captured homepage computes `"Roboto Flex", "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", "Heiti TC", sans-serif`. YAML `family.sans` `Roboto Flex`, YAML `family.cjk` `Noto Sans TC`, and YAML `family.fallback` `PingFang TC, Microsoft JhengHei, Heiti TC, sans-serif` name those live computed families. Hero h1 36px / 700 / line-height 54px; section h2 28px / 700 / 42px. |
| Outside these captures | Surfaces other than the captured homepage URL. |

### Family

- **Current visible UI stack (verbatim from live DOM):** `"Roboto Flex", "Noto Sans TC", "PingFang TC", "Microsoft JhengHei", "Heiti TC", sans-serif`
- **YAML `family.sans`:** `Roboto Flex`
- **YAML `family.cjk`:** `Noto Sans TC`
- **YAML `family.fallback`:** `PingFang TC, Microsoft JhengHei, Heiti TC, sans-serif`

Treating Roboto Flex as the Latin/numeral face and Noto Sans TC as the Traditional Chinese face in that stack, rather than promoting either fallback as the brand face, and treating do-not-replace-unavailable-or-unobserved-brand-type-on-a-surface-that-did-not-establish-it as a reconstruction boundary rather than a separately published font specification, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Do not replace unavailable or unobserved brand type with this stack on a surface that did not establish it.

### Type roles

Treating YAML unitless `lineHeight` 1.50 as a ratio rather than only as a local px companion, treating body-table px companions 54px / 42px / 27px / 24px / 21px as local rather than a rewrite of 1.50, treating rem companions 2.25rem / 1.75rem / 1.13rem / 1.00rem / 0.88rem as local size companions, and treating tracking `0` / `normal` as unmerged from a letter-spacing trick, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

The following type-rule extras — bold-for-hierarchy-regular-for-everything-else, no-in-between-medium-weight-on-the-marketing-surface, consistent-1.5-line-height-as-a-CJK-readability-decision, no-letter-spacing-tricks, Han-characters-already-monospaced-by-design, and size-not-weight-separates-body-tiers — are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification. Headlines commit to weight 700; body, nav, and buttons sit at weight 400. Every text role uses a 1.50 ratio. Tracking is `normal` / `0`. Subheadings step up to 18px while staying weight 400.

| Role | Font | Size | Weight | Line height | Tracking | Provenance |
|---|---|---:|---:|---:|---:|---|
| Hero Headline | Roboto Flex + Noto Sans TC stack | 36px (2.25rem) | 700 | 1.50 (54px) | 0 / normal | YAML `hero` use `Homepage hero headline`; body Notes: Homepage hero — "每次都是更好的體驗" |
| Section Title | Roboto Flex + Noto Sans TC stack | 28px (1.75rem) | 700 | 1.50 (42px) | 0 / normal | YAML `section` use `Feature / section titles`; body Notes: Feature / activity section heads |
| Subheading / Lead | Roboto Flex + Noto Sans TC stack | 18px (1.13rem) | 400 | 1.50 (27px) | 0 / normal | YAML `subheading` use `Quick-action labels, lead text`; body Notes: Quick-action labels, intro copy |
| Body | Roboto Flex + Noto Sans TC stack | 16px (1.00rem) | 400 | 1.50 (24px) | 0 / normal | YAML `body` use `Standard reading text, nav, button label`; body Notes: Standard text, nav, button label |
| Caption / Footer | Roboto Flex + Noto Sans TC stack | 14px (0.88rem) | 400 | 1.50 (21px) | 0 / normal | YAML `caption` use `Footer links, metadata, fine print`; body Notes: Footer links, metadata, fine print |

### Assets

- Homepage apple-touch icon: `https://www.cathaybk.com.tw/apple-touch-icon.png`. Treating that URL as catalog logo identity rather than a captured first-party wordmark file, and rather than a substitute for CUBE App chrome, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.
Treating captured homepage product photography, hero imagery, and CUBE card imagery as recorded surface imagery rather than an ownership class, and treating imagery-not-invented-decoration as a reconstruction of source §7 / §12 rather than a separately published asset specification, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Do not replace those captured images with invented brand-color decoration. Ownership of the files is not claimed here.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, treating hex-and-geometry in the harvested components as remaining source-stated, treating source-HTML-comment sections 10–16 as a philosophy-layer, and treating the §14 rows below as that source-stated contract rather than computed CSS from the 2026-06-08 live inspect, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated. Source HTML comment marks sections 10–16 as a philosophy layer; the §14 rows below are that source-stated contract, not computed CSS from the 2026-06-08 live inspect.

Characterizations in the following table such as “No illustration clutter”, “No green spinner — loading stays neutral”, “Plain-language guidance on what to fix”, “No vague something went wrong”, “Calm, past-tense, no exclamation”, “desaturates toward gray while keeping brand read”, and “for keyboard accessibility” are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no transactions)** | Off-white canvas, single line in `#555555` at 16px: a plain statement that nothing has happened yet, plus one deep-green `#00512a` CTA to begin. No illustration clutter. |
| **Loading** | Neutral skeleton blocks in `#eeeeee` at final dimensions, soft shimmer. No green spinner — loading stays neutral. |
| **Error (form validation)** | Field-level message below the input, warn orange `#e87c07` accent + `#bebebe` emphasized border. Plain-language guidance on what to fix. |
| **Error (service failure)** | Inline notice in regulator-aware tone. States what happened and the next step. No vague "something went wrong". |
| **Success (action complete)** | Brief confirmation using bright green `#26a862` accent + `#f3fff7` mint background. Calm, past-tense, no exclamation. |
| **Disabled** | Reduced opacity on the green action so it desaturates toward gray while keeping brand read. |
| **Focus** | Deep green `#00512a` border / ring on inputs and buttons for keyboard accessibility. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. `focus-visible` visual treatment remains omitted. A generic Focus observation is not `focus-visible` evidence.

Source records a named `Focus` treatment (`#00512a` border / ring on inputs and buttons) and YAML input `focus: border #00512a`. Those named Focus records are a different evidence type from `focus-visible`.

Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Standard Card has no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. YAML `tokens.components` records Standard Card and Glass Tile as `type: card`. Quiet Nav Action and Utility Sign-in have no YAML `tokens.components` row; body §4 and Proof record them as links / `(a)` anchors, so Type `link` is that source primitive, not an invention from YAML absence. Glass Tile keeps YAML `type: card` and takes Kind `interactive` from Proof `(a)` on “開戶” plus body Use as quick-entry.

### Primary CTA (Deep Green)

- Role: homepage primary call-to-action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#00512a`
- Text: `#ffffff`
- Border: `1px solid #00512a`
- Radius: 5px
- Padding: `12px 20px`
- Height: 50px
- Font: 16px / 400
- Use: YAML `Primary CTA (把握好機匯, 我有興趣)`; body §4 Use on this same control also names "設定手機提款", "更多新鮮事"
- Observed: default; YAML `active: fill darkens briefly`
- Field note: The following unmerged-field reading, including Role-as-the-harvested-homepage-primary-action, this-fill-not-accent-`#26a862`, on-fill-white-not-heading-ink, 5px-not-YAML-full-9999, 50px-not-40px-登入-not-≈187px-glass, 16px-400-not-headline-700, and YAML-active-named-press-not-hover, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#00512a` is this control’s fill and catalog `primary_color`. It is not accent `#26a862`. `#ffffff` is this control’s on-fill label, not heading ink `#333333`. Radius 5px is this control; it is not YAML `rounded.full` 9999. Height 50px is this CTA; it is not utility 登入 40px and not glass ≈187px. YAML `active` “fill darkens briefly” is a named press observation on this control, not hover and not `focus-visible`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the homepage primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says the green action reduces opacity; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the homepage primary CTA “把握好機匯”, “我有興趣”, “設定手機提款”, “更多新鮮事”; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: YAML `active` fill darkens briefly (also source §15 Primary CTA press at `motion-fast`). Treating that appearance as a named press observation, not hover and not `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

### Outline / Secondary

- Role: secondary action beside a primary CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#00512a`
- Border: `1px solid #00512a`
- Radius: 5px
- Font: 16px / 400
- Use: YAML `Secondary action beside primary CTA`
- Observed: default only
- Field note: The following unmerged-field reading, including transparent-fill-not-primary-`#00512a`-fill, this-foreground-not-accent-`#26a862`, and 5px-matching-primary-not-a-pill, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Fill is transparent. `#00512a` is this control’s text and border, not a fill and not accent `#26a862`. Radius 5px is this control; it is not YAML `rounded.full` 9999.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the outline / secondary action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a secondary action beside a primary CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Top Nav Link

- Role: top horizontal nav item
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: transparent
- Text: `#333333`
- Font: 16px / 400
- Use: YAML `Top nav links (個人金融, 企業金融), no underline`
- Observed: default (“個人金融”, “企業金融”, “私人銀行”); no underline
- Field note: The following unmerged-field reading, including heading-ink-not-body-slate, 16px-400-not-subheading-18px, and captured-labels-not-a-second-nav-token, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Resting `#333333` is this link’s label (heading ink), not body slate `#555555`. Font 16px / 400 is this nav item, not Subheading 18px / 400.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top nav item |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the item |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

### Quiet Nav Action

- Role: inline service shortcuts (換匯 / 提款 / 轉帳 / 視訊)
- Kind: interactive
- Type: link
- Anatomy: label
- Background: transparent
- Text: `#555555`
- Radius: 0
- Font: 16-18px / 400
- Use: Header utility links and inline service shortcuts (換匯 / 提款 / 轉帳 / 視訊)
- Observed: default (“換匯”, “提款”, “轉帳”, “視訊”). Proof records “換匯” as an `(a)` anchor.
- YAML `tokens.components` does not record this control; values are body §4 only. Type `link` is the source primitive (body Use: utility links; Proof: `(a)`), not an invention from YAML absence.
- Field note: The following unmerged-field reading, including body-slate-not-heading-ink, 16-18px-not-nav-16px-only, radius-0-not-5px, and not-a-rewrite-of-YAML-subheading-18px, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#555555` is this control’s label, not heading ink `#333333`. Font 16-18px is this body field; YAML subheading 18px / 400 remains the type-role for quick-action labels and is not merged into a second family. Radius 0 is this shortcut; it is not control 5px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as header utility / service-shortcut links |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Interactive link; visual treatment omitted |
| disabled | applicable | A shortcut can be unavailable; visual treatment omitted |
| loading | not-applicable | Destination/selection shortcut; the control itself does not enter a request-in-flight state |
| error | not-applicable | Navigation-shortcut meaning, not a request or validation failure on the control |
| success | not-applicable | Navigation-shortcut meaning, not action-outcome confirmation |

### Utility Sign-in (登入)

- Role: header utility sign-in link
- Kind: interactive
- Type: link
- Anatomy: label
- Background: transparent
- Text: `#555555` (or `#333333` in main nav, per source Quiet Nav Action)
- Radius: 0
- Font: 16px / 400
- Height: 40px
- Use: Header utility link (登入 / sign-in)
- Observed: default (“登入”)
- YAML `tokens.components` does not record this control; values are body §4 Navigation only. Type `link` is the source primitive (body: Utility link), not an invention from YAML absence.
- Field note: The following unmerged-field reading, including 40px-not-primary-CTA-50px, 16px-not-subheading-18px, and transparent-not-deep-green-fill, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Height 40px is this utility; it is not Primary CTA 50px and not glass ≈187px. Font 16px is this utility, not Subheading 18px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header utility sign-in link |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Interactive link; visual treatment omitted |
| disabled | applicable | A sign-in link can be unavailable; visual treatment omitted |
| loading | not-applicable | Header utility-link meaning is destination chrome, not a request-in-flight on this control |
| error | not-applicable | Utility-link meaning is destination chrome, not a request or validation failure on this control |
| success | not-applicable | Utility-link meaning is destination chrome, not action-outcome confirmation |

### Standard Card

- Role: content / product card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 5px
- Padding: YAML `27px`; body also records `21-27px`
- Shadow: `rgba(0,0,0,0.12) 0px 4px 16px`
- Use: YAML `Content/product cards (CUBE信用卡)`
- Observed: default (“CUBE信用卡”, “CUBE好友大募集”)
- Field note: The following unmerged-field reading, including this-fill-not-canvas-`#fafafa`, YAML-27px-not-rewritten-as-only-21-27px, 5px-not-0px, and standard-shadow-not-ambient-not-overlay, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#ffffff` is this card’s fill (surface), not canvas `#fafafa`. YAML padding `27px` and body `21-27px` are both kept; neither is chosen. Radius 5px is this card; it is not link 0px. Shadow is YAML `standard`, not ambient `0.08` and not overlay `0.22`.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Glass Tile

- Role: quick-entry glass tile over hero / feature imagery
- Kind: interactive
- Type: card
- Anatomy: surface + label
- Background: `rgba(255,255,255,0.5)`
- Radius: 5px
- Padding: `40px 15px`
- Height: ≈187px (body §8 touch-target record)
- Use: YAML `Quick-entry glass tiles over hero imagery (開戶, 匯率查詢)`; body also names "挑選信用卡", "線上申辦", "預約服務", "活動專區"
- Observed: default (“開戶”, “挑選信用卡”, “線上申辦”, “匯率查詢”, “預約服務”, “活動專區”). Proof records “開戶” as an `(a)` anchor.
- Field note: The following unmerged-field reading, including glass-rgba-not-solid-surface-white, 40px-15px-not-card-27px, ≈187px-not-CTA-50px, and 5px-matching-cards-not-a-pill, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `rgba(255,255,255,0.5)` is this tile’s fill, not solid `#ffffff`. Padding `40px 15px` is this tile, not Standard Card YAML 27px. Height ≈187px is the body §8 record for these tiles; it is not Primary CTA 50px. YAML `type: card` stays; Kind `interactive` is the Proof `(a)` / quick-entry record, not a rewrite of that YAML type.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as quick-entry tiles over hero/feature imagery |
| hover | applicable | Pointer-web quick-entry; visual treatment omitted |
| focus-visible | applicable | Interactive quick-entry; visual treatment omitted |
| disabled | applicable | A quick-entry can be unavailable; visual treatment omitted |
| loading | not-applicable | Quick-entry meaning is destination selection, not a request-in-flight on the tile |
| error | not-applicable | Quick-entry meaning is destination selection, not a request or validation failure on the tile |
| success | not-applicable | Quick-entry meaning is destination selection, not action-outcome confirmation |

### Form Input

- Role: form text input
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#333333`
- Border: `1px solid #eeeeee` (or `#bebebe` for emphasis)
- Radius: 5px
- Placeholder: `#888888`
- Use: YAML `Form input, placeholder #888888`
- Observed: default; YAML `focus: border #00512a`; body Focus: deep green `#00512a` border / ring
- Field note: The following unmerged-field reading, including heading-ink-not-muted-placeholder, hairline-not-border-mute-except-on-emphasis, 5px-matching-buttons-not-0px, and named-Focus-observation, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#333333` is this field’s value color, not placeholder `#888888`. Default border `#eeeeee` is hairline; `#bebebe` is the emphasized outline. YAML `focus` / body Focus `#00512a` is a named Focus observation.
- Named Focus is a different evidence type from `focus-visible`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the form input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted. Source §14 names a field-level message with `#e87c07` accent and `#bebebe` emphasized border; that row is not copied here as a computed paint |

Loading and success applicability are omitted. Source records default geometry plus a named Focus treatment. Exact loading/success mapping for this field is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: Focus (`#00512a` border / ring). Treating that appearance as named Focus, not `focus-visible`, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

### Footer Link

- Role: footer group-company and policy link on the dark band
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `#00283d`
- Text: `#ffffff`
- Font: 14px / 400
- Use: YAML `Footer group-company + policy links on dark band`
- Observed: default — group-company links ("國泰金控", "國泰人壽", "國泰產險", "國泰綜合證券"); policy links ("隱私權保護政策", "顧客資料保密措施")
- Field note: The following unmerged-field reading, including footer-band-`#00283d`-not-heading-ink, on-band-white-not-sub-nav-`#555555`, and 14px-not-body-16px, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#00283d` is this band’s fill (ink-deep), not heading ink `#333333`. `#ffffff` is this link’s label on that band. Source also records sub-nav and footer groups at `#555555` / 14px; that `#555555` record is not merged with this on-band `#ffffff`. Font 14px / 400 is Caption / Footer, not Body 16px / 400.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the dark-band footer link |
| hover | applicable | Pointer-web tab/link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A footer link can be unavailable; visual treatment omitted |
| loading | not-applicable | A footer link selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Footer-link meaning is destination choice, not a request or validation failure on the item |
| success | not-applicable | Footer-link meaning is destination choice, not action-outcome confirmation |

### Off-white Hero (local recipe)

Treating the following as a source §9 local composition — off-white `#fafafa` parent + 36px / 700 / line-height 1.5 `#333333` headline + horizontal strip of glass tiles (`rgba(255,255,255,0.5)`, 5px, `40px 15px`) + one primary CTA (`#00512a` fill, white text, 5px, 50px, `12px 20px`, weight 400) — and not as a rewrite of YAML Glass Tile or Primary CTA, and not as a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

- Role: off-white-hero parent surface
- Anatomy: surface
- Background: `#fafafa`
- Local children (this composition only): headline 36px Noto Sans TC weight 700, line-height 1.5, color `#333333`; below it a horizontal strip of quick-action glass tiles (`rgba(255,255,255,0.5)`, 5px radius, `40px 15px` padding); one primary CTA (`#00512a` fill, white text, 5px radius, 50px height, `12px 20px` padding, weight 400)
- Use: source §9 hero — off-white `#fafafa`, 36px/700/1.5 `#333333` headline, glass-tile strip, one primary CTA
- Kind, Type, and a state-applicability map are omitted. Harvested Glass Tile and Primary CTA stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-`#fafafa`-not-a-second-canvas-token, prompt-stated-Noto-Sans-TC-headline-not-a-rewrite-of-the-live-stack, glass-strip-plus-one-CTA-as-this-hero, and not-a-rewrite-of-YAML-Glass-or-Primary-CTA, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#fafafa` is this parent’s fill. Source §9 states the headline family as Noto Sans TC; YAML/live stack remains Roboto Flex + Noto Sans TC. Those two family records are kept; neither is chosen as a global rewrite. The glass-tile strip and the one primary CTA stay children of this hero.

### Product Card (local recipe)

Treating the following as a source §9 local composition — white `#ffffff` parent + title 18px / 400 / `#333333` + body 16px / 400 / `#555555` + bottom deep-green CTA — and not as a rewrite of YAML Standard Card / Subheading / Body / Primary CTA, and not as a global pairing token, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

- Role: product-card parent
- Anatomy: surface
- Background: `#ffffff`
- Radius: 5px
- Padding: 27px
- Shadow: `rgba(0,0,0,0.12) 0px 4px 16px`
- Local children (this composition only): title 18px weight 400 `#333333`; body 16px weight 400 `#555555`; bottom CTA deep green `#00512a`, white text, 5px radius
- Use: source §9 product card — white surface, 5px, 27px padding, standard shadow, 18px/400 title, 16px/400 body, deep-green CTA
- Kind, Type, and a state-applicability map are omitted. Harvested Standard Card and Primary CTA stay in their own slots.
- Field note: The following unmerged-composition reading, including 18px-400-title-plus-16px-400-body-plus-CTA-as-this-card and not-a-global-pairing-token, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. Title 18px / 400 `#333333` and body 16px / 400 `#555555` stay children of this product-card parent; they are not promoted as extra global type roles.

### Mint Highlight Band (local recipe)

Treating the following as a source §9 local composition — `#f3fff7` mint parent + `#26a862` icons and emphasis + `#333333` body text + 5px-radius cards inside — and not as a global mint-band token or a rewrite of YAML Mint Tint / Accent / Heading / Standard Card, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

- Role: green-themed highlight-band parent
- Anatomy: surface
- Background: `#f3fff7`
- Local children (this composition only): `#26a862` bright-green icons and emphasis; `#333333` body text; 5px-radius cards inside
- Use: source §9 green-themed highlight band — mint background, bright-green icons and emphasis, heading-ink body, 5px-radius cards
- Kind, Type, and a state-applicability map are omitted. YAML Mint Tint, Accent, Heading Ink, and Standard Card stay in their own slots.
- Field note: The following unmerged-composition reading, including parent-mint-not-a-second-canvas, bright-green-icons-plus-heading-ink-plus-5px-cards-as-this-band, and not-promoted-as-global-tokens, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification. `#f3fff7` is this band’s parent fill. The `#26a862` icons, `#333333` body, and 5px-radius cards stay children of this parent.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 5, sm 11, md 15, base 20, lg 27, xl 40, section 64. Body layout repeats 5px, 11px, 15px, 20px, 27px, 40px. Harvested heights: 50px primary CTA, 40px 登入, ≈187px glass tiles.

The following layout readings — service-density-up-top-marketing-breathing-room-below, card-driven-rhythm, whitespace-as-the-gap-between-cards, CJK-comfortable-leading, centered-max-width-content-column-with-full-bleed-hero-and-feature-bands, quick-action-row-as-task-first-IA, product-activity-sections-as-multi-column-card-grids, and footer-as-wide-multi-column-link-directory — are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification.

Recorded layout:

- Centered max-width content column with full-bleed hero and feature bands
- Quick-action row: a horizontal strip of equal-width tiles (換匯 / 提款 / 轉帳 / 視訊) directly below the hero
- Product/activity sections: multi-column card grids that collapse to single column on mobile
- Footer: wide multi-column link directory grouping the broader Cathay Financial group on a dark `#00283d` band

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 50px / 40px / ≈187px / 36px-headline figures as surface measurements in this packet rather than a cross-viewport specification, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating glass-scrim-at-all-sizes / hero-imagery-crops-to-maintain-headline-contrast / card-images-keep-5px as source-stated image behavior rather than a complete image specification, and treating headline-scale-down-weight-700-maintained / quick-action-horizontal-to-2-up-or-3-up / card-grids-multi-column-to-stacked / top-nav-to-hamburger-drawer / footer-directory-to-stacked-accordion as recorded collapsing rather than a complete responsive specification, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <768px | Single column, stacked cards, quick-actions wrap to grid |
| Tablet | 768-1024px | 2-column card grids, condensed nav |
| Desktop | 1024-1440px | Full multi-column layout, horizontal quick-action row |
| Large | >1440px | Centered content with wide margins |

Collapsing strategy recorded in the source: hero 36px headline scales down on mobile, weight 700 maintained; quick-action strip: horizontal row → 2-up or 3-up grid of tiles; product/activity card grids: multi-column → single column stacked; top nav: full horizontal → hamburger drawer; footer directory: wide multi-column → stacked accordion groups.

Image behavior: glass tiles over imagery keep `rgba(255,255,255,0.5)` legibility scrim at all sizes; hero imagery crops to maintain headline contrast on narrow viewports; card images keep the consistent 5px radius.

Touch-target record: buttons at 50px height; quick-action tiles tall (≈187px) with `40px 15px` padding; footer links at 14px with row spacing.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-08)

The live strings below are source-stated. Treating the parenthetical characterizations (progress-as-care / safety-and-simplicity / friendly-invitation / single-concrete-verb / group-company / policy) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

- "每次都是更好的體驗" — homepage hero headline. *(verified live)*
- "用科技讓金融生活更安全簡單" — section headline. *(verified live)*
- "把握好機匯", "我有興趣", "更多新鮮事", "設定手機提款" — primary / invitation CTAs. *(verified live / source body)*
- "換匯", "提款", "轉帳", "視訊" — quick-action labels. *(verified live / source body)*
- "開戶", "挑選信用卡", "線上申辦", "匯率查詢", "預約服務", "活動專區" — glass-tile labels. *(source body)*
- "個人金融", "企業金融", "私人銀行" — top nav. *(source body)*
- "登入" — header utility. *(source body)*
- "CUBE信用卡", "CUBE好友大募集" — product cards. *(source body)*
- "國泰金控", "國泰人壽", "國泰產險", "國泰綜合證券" — footer group-company links. *(source body)*
- "隱私權保護政策", "顧客資料保密措施" — footer policy links. *(source body)*

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Cathay-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — warm-reassuring-service-led / knowledgeable-teller / progress-framed-as-care-not-disruption / plain-action-first-Traditional-Chinese / Warm-optimistic-human / Single-concrete-verb / Friendly-invitation-not-command / Benefit-first-reassurance-led / Formal-regulator-aware-precise / Institutional-exact / Hype-superlatives-革命性-revolutionary / pressure-tactics / never-gambles-safety-and-trust-for-excitement — are a derived editorial implementation inference from the verified surfaces; they are not Cathay-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Cathay United Bank's voice as warm, reassuring, and service-led — the tone of a knowledgeable teller who genuinely wants everyday banking to be easier. The homepage tagline "每次都是更好的體驗" and "用科技讓金融生活更安全簡單" set the register in the source: progress framed as care, not disruption. Copy speaks directly to common life tasks in plain, action-first Traditional Chinese.

| Context | Tone |
|---|---|
| Hero headlines | Warm, optimistic, human. "每次都是更好的體驗." Progress as care. |
| Quick-action labels | Single concrete verb. "換匯", "提款", "轉帳", "視訊". No fluff. |
| CTAs | Friendly invitation, not command. "我有興趣", "更多新鮮事", "想轉就轉". |
| Product descriptions | Benefit-first, reassurance-led. Safety and simplicity foregrounded. |
| Notices / fine print | Formal, regulator-aware, precise. Banking compliance register. |
| Footer / legal | Institutional, exact. Group-company and policy links stated plainly. |

**Forbidden register** (source): hype superlatives ("革命性" / "revolutionary"), pressure tactics, anything that undercuts the bank's core promise of safety and trust. Cathay sells reassurance; the voice never gambles it for excitement.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exceptions remain Primary CTA YAML `active` fill-darkens-briefly and Form Input named Focus `#00512a` border / ring
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Primary CTA and Outline / Secondary; omitted loading/success on Form Input
- interactive kind and state-applicability map for Standard Card
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
