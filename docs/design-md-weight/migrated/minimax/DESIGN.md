# MiniMax Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

MiniMax is a general-AI company whose own current account centers a family of proprietary multimodal models and AI-native products for people, enterprises, and developers. Catalog homepage identity is `https://www.minimaxi.com`. Catalog `primary_color` is `#000000`. This contract covers the four first-party public surfaces the source inspected on 2026-07-13: public marketing home at `https://www.minimaxi.com/`, the M3 model-launch page at `https://www.minimaxi.com/models/text/m3`, the public audio tool at `https://www.minimaxi.com/audio`, and careers marketing at `https://www.minimaxi.com/careers`. The source YAML token note names the current public marketing, model-launch, audio-tool, and careers surfaces separately. The source records no authenticated product or documentation-chrome token. Official company context at `https://minimaxi.com/about` and official model documentation at `https://platform.minimaxi.com/docs/guides/models-intro` are named narrative sources; they do not supply computed interface tokens. Treating those four inspected public URLs as this contract's token surfaces, keeping the about page and platform model documentation as named narrative sources that do not supply computed interface tokens, and keeping those four surfaces named separately rather than blended into one fictional system, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

The public web translates that breadth into a mostly white, text-led presentation: near-black type, restrained grey surfaces, large model-specific hero moments, and a few route-local action colors. The stable visual thread across the captured home, M3 launch, audio tool, and careers pages is not a universal card style or color scale; it is dense information given room to breathe through simple backgrounds, rounded actions, and clear hierarchy. The current product family is changing quickly—MiniMax’s official model documentation lists text, video, speech, image, and music offerings—so this reference preserves each observed surface domain rather than blending a launch page, an audio tool, careers marketing, and unobserved documentation chrome into one fictional system. The hex values, the four named routes, the model-family list, and the instruction not to blend those domains are the source's own. Readings of that captured layer as a mostly white, text-led presentation, of the stable visual thread as not a universal card style or color scale, and of dense information given room to breathe through simple backgrounds, rounded actions, and clear hierarchy, are a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. MiniMax describes itself as a general-AI company founded in early 2022, pursuing AGI through the mission “Intelligence with Everyone.” Its official company profile connects proprietary multimodal models to AI-native products and an enterprise/developer open platform; the current official model documentation separates that portfolio into text, video, speech, image, and music families. The public visual system reflects this multi-product posture through a shared neutral web base with deliberately local launch, tool, and careers treatments rather than one uniformly styled application. The official careers page adds a culture boundary: it presents technology, product, content, and aesthetics as intersecting disciplines, and frames curiosity and exploration as valued qualities. That supports a narrative of technical work that still attends to expression and usability; it does not justify invented user stories, company chronology beyond the official profile, or app-interface claims absent from the capture. The early-2022 founding, the AGI pursuit, the mission string, the model-family list, each paragraph’s last sentence, and the careers-page culture boundary are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-portfolio narrative as context that does not by itself supply interface tokens, keeping each source paragraph’s last sentence as one unit with the paragraph it closes, and reading the public visual system as reflecting that multi-product posture through a shared neutral web base with deliberately local launch, tool, and careers treatments, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=4 lang=en -->
### Primary tasks

Selecting these four as the product's primary tasks, each naming a captured public-home, M3-launch, audio-tool, or careers-marketing surface or control, and recording that they do not come from the source's audience-boundary list, is a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

- Use the Home marketing header action at `home::[data-omd-capture="16"]`.
- Read the M3 model-launch hero heading at `surface-2::h1`.
- Use the Public audio-tool generate action at `surface-3::[data-omd-capture="12"]`.
- Use the Careers-marketing primary action at `surface-4::[data-omd-capture="18"]`.
<!-- design-md:claim-end -->

### Audience

No fictional personas are asserted. Official materials identify several audiences and contexts: individual users, enterprises, developers using the open platform, and prospective team members across technical, product, content, and aesthetic disciplines. Those are audience boundaries only; they do not replace MiniMax user research or validate behavioral assumptions. No name, age, city, motivation, or affiliation classification is carried beyond those source wordings. Dropping any later invented individual rather than promoting one, carrying no name, age, city, or affiliation classification beyond those source wordings, and reading those source-named groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's §1 bullets. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside it, is a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

- **Neutral public base:** `#ffffff` canvas, `#18181b` ink, and `#e5e7eb` borders recur on home, M3, and careers surfaces.
- **Surface-local action geometry:** 32px on the home header, 8px on M3 paired actions, 100px on the audio tool, and full pills on careers are separate observations.
- **Model-family expression:** M3 uses a 78px Outfit hero while the careers page uses a 60px MiSans headline; neither becomes a universal app heading rule.
- **Bounded source domains:** the raw bundle contains no authenticated product session or documentation chrome. Those domains are intentionally not represented by tokens.

### Principles

These 4 items are a derived editorial implementation inference from the verified surfaces; they are not MiniMax-authored or a separately published UI specification. The numbered stems rest on official about, model-documentation, and careers material recorded by the source. The UI implications are this reference's constrained interpretations of official positioning, not published MiniMax component rules.

1. **Intelligence with everyone.** MiniMax publicly frames its mission around broad participation in intelligent systems. *Reference UI implication:* explain what a capability enables before escalating technical detail.
2. **Multimodal, product-specific clarity.** Official materials distinguish text, video, speech, image, and music models. *Reference UI implication:* name the model and modality instead of using a vague universal “AI” label.
3. **No shortcuts.** The official about page lists this as a company value. *Reference UI implication:* avoid presenting unmeasured shortcuts, states, or design-system rules as facts.
4. **Technology and taste intersect.** The careers page places technology, product, content, and aesthetics together. *Reference UI implication:* use visual emphasis to clarify a technical proposition, not to hide its boundaries.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

- Keep the public-web neutral base clear: white canvas, near-black text, and low-contrast borders.
- Use a surface-local action treatment only in the context where it was observed.
- Use MiSans only where its loaded asset and target-project licence permit it; otherwise leave the family unresolved rather than substituting a generic font as MiSans.
- Treat current model names and product areas as evolving content, not permanent navigation or component taxonomy.

### Avoid

The source states these four as its Don't list. The fifth restates a brand constraint unique to the source’s Agent Prompt Guide. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

- Do not combine the audio purple action with M3 or careers actions into a single global primary color.
- Do not turn CSS hover utilities or the presence of `transition-*` classes into measured state or motion specifications.
- Do not promote DM Sans, Inter, Roboto, or declared icon fonts to live MiniMax UI families.
- Do not infer app, console, or documentation-chrome design rules from these public routes.
- Do not copy an M3 launch button into the audio tool or careers surface merely because all are MiniMax-owned pages. For application work, leave product, documentation, and error-state decisions open until there is direct evidence.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

YAML token note, kept as the facts it names: Current public marketing, model-launch, audio-tool, and careers surfaces are named separately; no authenticated product or documentation-chrome token is inferred.

Each role below pairs a name with the value and the use the source records. Token-set keys follow the YAML paths. Keeping the YAML token note as the facts it names rather than as a published MiniMax token specification, pairing each hex to the token-set path named beside it, keeping `tokens.colors.canvas` `#ffffff` unmerged from `tokens.colors.action-on-dark` `#ffffff` and from m3-light-action background `#ffffff`, keeping `tokens.colors.ink` `#18181b` unmerged from `tokens.colors.action-dark` `#181e25` and from m3-dark-action background `#000000` and from m3-light-action text `#222222`, keeping home-light-action text `#181e25` unmerged from `tokens.colors.action-dark` `#181e25`, keeping `tokens.colors.surface` `#f5f5f5` on the home light header action rather than as a global card fill, and keeping `tokens.colors.audio-accent` `#7659fa` as an audio-tool-local value rather than a global MiniMax brand-primary claim, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

#### Repeated public-web roles

- **Canvas** (`#ffffff`): `tokens.colors.canvas`; body background observed on all four captured routes.
- **Ink** (`#18181b`): `tokens.colors.ink`; repeated home, M3, and careers text.
- **Secondary text** (`#45515e`): `tokens.colors.secondary-text`; home supporting copy.
- **Muted** (`#86909c`): `tokens.colors.muted`; repeated home and M3 supporting text.
- **Surface** (`#f5f5f5`): `tokens.colors.surface`; home light header action.
- **Border** (`#e5e7eb`): `tokens.colors.border`; repeated computed border color on home, M3, and careers elements.
- **Dark action** (`#181e25`) with **on-dark** (`#ffffff`): `tokens.colors.action-dark` / `tokens.colors.action-on-dark`; careers primary action; the dark value is also observed repeatedly on the home/M3 public family.

#### Audio-tool-local role

- **Audio accent** (`#7659fa`) with **audio on-accent** (`#f8f8f8`): `tokens.colors.audio-accent` / `tokens.colors.audio-on-accent`; the observed `surface-3` generate action. It is an audio-tool-local value, not a global MiniMax brand-primary claim.

Catalog identity `primary_color` is `#000000`. That identity field is not a YAML color-role key; the `#000000` fill recorded on the M3 launch paired dark action stays on that control. Keeping catalog identity `primary_color` `#000000` unmerged from that M3 launch paired dark-action fill `#000000` is a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

### Spacing

Token-set paths, unitless steps as the YAML recorded them. Source §4 and §5 write the same steps with `px` on the matching controls. YAML unitless values stay beside those `px` writings; neither was chosen as a replacement. Keeping those YAML unitless spacing steps beside the matching §4 / §5 `px` writings, and choosing neither writing as a replacement, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

| Step | Value | Token-set path | Surface bound |
|---|---:|---|---|
| home-action-x | 28 | `tokens.spacing.home-action-x` | home header action horizontal padding `0px 28px` |
| m3-action-x | 12 | `tokens.spacing.m3-action-x` | M3 paired-action horizontal padding `0px 12px` |
| audio-action-x | 20 | `tokens.spacing.audio-action-x` | audio generate horizontal padding `0px 20px` |
| careers-action-x | 24 | `tokens.spacing.careers-action-x` | careers primary horizontal padding `12px 24px` |
| careers-action-y | 12 | `tokens.spacing.careers-action-y` | careers primary vertical padding `12px 24px` |

`tokens.spacing.m3-action-x: 12` is not `tokens.spacing.careers-action-y: 12`. Careers outline padding `12px 32px` is that control’s padding; `32` is not a YAML spacing key, and it is not `tokens.rounded.home-action: 32`. The capture establishes local action padding—28px horizontal on the home action, 12px on M3 paired actions, 20px on the audio action, and 24px/32px on careers actions—not a universal spacing scale. Keeping YAML `12` on two named keys rather than merging them, keeping outline `32px` off `tokens.rounded.home-action: 32`, and reading those five keys as local action padding rather than as a universal spacing scale, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded`, unitless steps as the YAML recorded them.

| Step | Value | Token-set path | Surface bound |
|---|---:|---|---|
| home-action | 32 | `tokens.rounded.home-action` | home header action `32px` |
| m3-action | 8 | `tokens.rounded.m3-action` | M3 paired actions `8px` |
| audio-action | 100 | `tokens.rounded.audio-action` | audio generate `100px` |
| careers-action | 9999 | `tokens.rounded.careers-action` | careers primary and outline full pills `9999px` |

Source §1 writes the same four observations as 32px on the home header, 8px on M3 paired actions, 100px on the audio tool, and full pills on careers. YAML unitless `32` / `8` / `100` / `9999` stay beside those `px` writings; neither was chosen as a replacement. `tokens.rounded.home-action: 32` is not careers-outline padding `32px`. Keeping the four YAML steps as four keys, keeping the longer §1 / §4 px writings beside them, and reading those keys as surface-local action geometry rather than as a universal radius scale, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

### Elevation

The representative actions in the component records report `boxShadow: none`. No reusable elevation ladder, card shadow, or hover-lift rule is established. The old purple glow and broad product-card shadow claims were removed because this capture does not corroborate them. Naming that unresolved bound, and refusing to carry forward the old purple glow and broad product-card shadow claims, are a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

### Motion

No motion token, duration, easing curve, or reduced-motion behavior is measured. Several observed classes include transition utilities, but the zero-interaction capture does not establish what changes, when it changes, or how it behaves under user preferences. No MiniMax motion token is specified.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or specification document — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation, refusing a match against an official framework or specification document as that gate, and refusing to turn `transition-*` class names into a measured motion specification, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into the source's own classes. The sorting, the live MiSans/Outfit surface-use rows, JetBrains Mono as a specialist observation rather than a general UI or code token, declared-only faces as not UI-family tokens, `-apple-system` as a system fallback observation rather than a MiniMax typeface, and the licence row as font-context facts rather than an assertion that MiniMax distributes either family as a reusable brand kit, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Live computed public-web use | `MiSans` is the dominant visible first family: the collector reports 421 visible uses across body, button, card, heading, list, and dialog roles. It is backed by loaded FontFaceSet entries and 43 MiniMax-hosted font-source URLs. It is the canonical UI-family token for these public routes. |
| Live computed display use | `Outfit` is visibly used and loaded (9 uses, including M3 hero headings), backed by a MiniMax/Hailuo-hosted font source and a CDN asset. It is retained as a public display-family token only. |
| Live computed specialist use | `JetBrains Mono` is loaded for two heading observations. It is not promoted to a general UI or code token because the supplied capture does not establish a reusable product role. |
| Declared-only assets | `DM Sans`, Inter, Plus Jakarta Sans, Roboto, and Font Awesome faces occur in declarations or system stacks without observed visible use. They are not UI tokens and are not substituted at runtime. |
| System fallback | `-apple-system` appears in the audio surface’s input stack. It is a system fallback observation, not a MiniMax typeface. |
| Font context and licence boundary | Xiaomi identifies MiSans as a variable multilingual family and publishes its own licence agreement; MiniMax’s current hosted webfont use does not grant a downstream project permission to reuse MiniMax’s delivery assets. Outfit’s upstream project publishes it under SIL OFL 1.1. These are font-context facts, not an assertion that MiniMax distributes either family as a reusable brand kit. |

Use large display type only when the observed font is actually available.

### Family

- **Current visible UI family:** `MiSans`. Token-set path `tokens.typography.family.ui`.
- **Current visible display family:** `Outfit`. Token-set path `tokens.typography.family.display`.
- Both are canonical here only because computed visible use and loaded FontFace/source evidence agree on the supplied public routes. Neither is promoted to an authenticated-product or documentation-chrome family. Do not replace unavailable or unobserved brand type with a generic font as MiSans. Do not promote JetBrains Mono, DM Sans, Inter, Plus Jakarta Sans, Roboto, Font Awesome, or `-apple-system` to a UI-family token. Keeping UI versus display on those two families, refusing a generic-font substitute for MiSans, keeping Outfit as a public display-family token only, and keeping JetBrains Mono unpromoted as a general UI or code token, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

### Type roles

YAML writes numeric sizes without a `px` suffix on `public-body`, `m3-display`, and `careers-display`. Source §3 writes the same roles with `px`. Both writings stay. YAML `lineHeight` values are already the px strings `"24px"`, `"85.8px"`, and `"60px"`; they stay as those strings and are never converted to a replacement unitless ratio. Token-set `use` strings are kept verbatim. The longer §3 spellings sit beside them; neither was chosen as a replacement. Pairing each YAML role to the token-set path named beside it, keeping YAML `use` strings verbatim in the Token-set use column, keeping the longer §3 metrics and surface boundaries beside them, keeping YAML `16` off prose `16px` and off careers-outline font `16px`, keeping YAML `78` off prose `78px`, keeping YAML `60` off prose `60px` and off careers-display line-height `"60px"`, and keeping M3 display metrics on the M3 launch page rather than as a universal app heading rule, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

| Role | Family | Size | Weight | Line height | Token-set use | Surface boundary |
|---|---|---|---:|---|---|---|
| Public body | MiSans | YAML `16` / §3 `16px` | 400 | YAML `"24px"` / §3 `24px` | Repeated public home body and navigation context | repeated home body/navigation context |
| M3 display | Outfit | YAML `78` / §3 `78px` | 600 | YAML `"85.8px"` / §3 `85.8px` | M3 model-launch hero heading | `surface-2::h1` on the M3 launch page |
| Careers display | MiSans | YAML `60` / §3 `60px` | 700 | YAML `"60px"` / §3 `60px` | Careers-marketing headline | `surface-4::h1` on careers marketing |

Token-set paths: `tokens.typography.public-body` · `tokens.typography.m3-display` · `tokens.typography.careers-display`. YAML field paths: `tokens.typography.public-body.size` `16` · `tokens.typography.public-body.weight` `400` · `tokens.typography.public-body.lineHeight` `"24px"` · `tokens.typography.m3-display.size` `78` · `tokens.typography.m3-display.weight` `600` · `tokens.typography.m3-display.lineHeight` `"85.8px"` · `tokens.typography.careers-display.size` `60` · `tokens.typography.careers-display.weight` `700` · `tokens.typography.careers-display.lineHeight` `"60px"`. `tokens.typography.m3-display` stays on the M3 launch page. `tokens.typography.careers-display` stays on careers marketing. `tokens.typography.public-body` stays on repeated home body/navigation context. Home-header-action font `16px / 400 / MiSans` and careers-outline font `16px / 500 / MiSans` stay on those controls; they are not extra YAML type-role keys. Keeping those two control fonts on those controls rather than as extra YAML type-role keys is a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

### Assets

- Catalog identity records `logo.type: simpleicons` and `logo.slug: minimax`. That is a catalog identity pointer, not a MiniMax-hosted brand file, and it is not promoted to one here.
- Loaded MiniMax-hosted font asset: `https://filecdn.minimax.chat/public/MiSans-Regular.woff2`. MiniMax’s current hosted webfont use does not grant a downstream project permission to reuse MiniMax’s delivery assets.
- Xiaomi identifies MiSans as a variable multilingual family and publishes its own licence agreement. Outfit’s upstream project publishes it under SIL OFL 1.1. These are font-context facts, not an assertion that MiniMax distributes either family as a reusable brand kit.

Classifying the simpleicons slug as a catalog identity pointer rather than a hosted brand file, and reading the Xiaomi and Outfit licence facts as font-context rather than as a MiniMax brand kit, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

All variants are selector-backed observations from the supplied public bundle. `coverage.interactionCount` and `coverage.observedStates` are both zero, so every entry below is a default-state observation only. Class names may contain hover utilities, but those declarations are not promoted as measured hover, focus, pressed, disabled, or motion variants.

No authenticated application state, empty state, loading state, error state, success state, disabled control, or accessibility-state contract was captured. The audio page is a public tool surface, but the supplied bundle records zero interaction events and zero observed states. The default component observations in this section must not be expanded into an application-state specification.

YAML `tokens.components` records six keys, each with `type: button`. YAML `components_harvested: true`. YAML `states` on each of those six keys is `default only; no interaction event or pseudo-state captured`. Source §4 writes the same bound as `Default only; no interaction event or pseudo-state captured.` Both writings stay. A `Primitive type` line is attached only when the source YAML records that type on that component; all six records below carry `type: button`. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow each control's product role rather than its primitive kind. This is not a complete state-coverage claim. Preserving the source state contract here rather than delegating it to an unadopted catalog graph, the role-based decision procedure above, every interactive-kind verdict, every applicability verdict, the reason given for either, attaching YAML `type: button` only on the six token-set keys that record it, keeping YAML `states` beside the §4 capitalized writing, and the refusal to treat this as a complete state-coverage claim, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

### Home header action

**Light action — `home` marketing surface**

- Role: Home marketing header action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#f5f5f5`
- Text: `#181e25`
- Radius: YAML `tokens.rounded.home-action: 32` · §4 `32px`
- Padding: YAML `tokens.spacing.home-action-x: 28` · §4 `0px 28px`
- Font: `16px / 400 / MiSans`
- Token-set use: Home marketing header action, selector home::[data-omd-capture=16]
- Use: Home marketing header action at `home::[data-omd-capture="16"]`.
- States: Default only; no interaction event or pseudo-state captured.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the home marketing surface |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A header action can be gated; visual treatment omitted |
| loading | not-applicable | A home marketing header action does not itself present in-place progress |
| error | not-applicable | The control carries no input of its own to validate |
| success | not-applicable | The control presents no completion outcome of its own |

### M3 launch actions

**Dark paired action — `m3-launch` product-launch surface**

- Role: M3 launch paired dark action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#000000`
- Text: `#ffffff`
- Radius: YAML `tokens.rounded.m3-action: 8` · §4 `8px`
- Padding: YAML `tokens.spacing.m3-action-x: 12` · §4 `0px 12px`
- Font: `14px / 400 / MiSans`
- Token-set use: M3 launch paired dark action, selector surface-2::[data-omd-capture=20]
- Use: M3 launch paired action at `surface-2::[data-omd-capture="20"]`.
- States: Default only; no interaction event or pseudo-state captured.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the M3 launch surface |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A launch action can be gated; visual treatment omitted |
| loading | not-applicable | An M3 launch paired action does not itself present in-place progress |
| error | not-applicable | The control carries no input of its own to validate |
| success | not-applicable | The control presents no completion outcome of its own |

**Light paired action — `m3-launch` product-launch surface**

- Role: M3 launch paired light action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#ffffff`
- Text: `#222222`
- Radius: YAML `tokens.rounded.m3-action: 8` · §4 `8px`
- Padding: YAML `tokens.spacing.m3-action-x: 12` · §4 `0px 12px`
- Font: `14px / 400 / MiSans`
- Token-set use: M3 launch paired light action, selector surface-2::[data-omd-capture=21]
- Use: M3 launch paired action at `surface-2::[data-omd-capture="21"]`.
- States: Default only; no interaction event or pseudo-state captured.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the M3 launch surface |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A launch action can be gated; visual treatment omitted |
| loading | not-applicable | An M3 launch paired action does not itself present in-place progress |
| error | not-applicable | The control carries no input of its own to validate |
| success | not-applicable | The control presents no completion outcome of its own |

### Audio-tool action

**Generate — `audio-tool` product-tool surface**

- Role: Public audio-tool generate action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#7659fa`
- Text: `#f8f8f8`
- Radius: YAML `tokens.rounded.audio-action: 100` · §4 `100px`
- Padding: YAML `tokens.spacing.audio-action-x: 20` · §4 `0px 20px`
- Font: `14px / 500 / MiSans`
- Token-set use: Public audio-tool generate action, selector surface-3::[data-omd-capture=12]
- Use: Public audio-tool generate action at `surface-3::[data-omd-capture="12"]`.
- States: Default only; no interaction event or pseudo-state captured.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public audio-tool surface |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A generate action can be gated; visual treatment omitted |
| loading | applicable | A public audio-tool generate action can present in-place progress; visual treatment omitted |
| error | applicable | A generate action can fail; visual treatment omitted |
| success | applicable | A generate action can complete; visual treatment omitted |

### Careers actions

**Primary — `careers` marketing surface**

- Role: Careers-marketing primary action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Background: `#181e25`
- Text: `#ffffff`
- Radius: YAML `tokens.rounded.careers-action: 9999` · §4 `9999px`
- Padding: YAML `tokens.spacing.careers-action-y: 12` / `tokens.spacing.careers-action-x: 24` · §4 `12px 24px`
- Font: `14px / 500 / MiSans`
- Token-set use: Careers-marketing primary action, selector surface-4::[data-omd-capture=18]
- Use: Careers primary action at `surface-4::[data-omd-capture="18"]`.
- States: Default only; no interaction event or pseudo-state captured.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the careers marketing surface |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A careers action can be gated; visual treatment omitted |
| loading | not-applicable | A careers-marketing primary action does not itself present in-place progress |
| error | not-applicable | The control carries no input of its own to validate |
| success | not-applicable | The control presents no completion outcome of its own |

**Outline — `careers` marketing surface**

- Role: Careers-marketing outline action
- Primitive type: `button` · Kind: interactive
- Anatomy: label
- Text: `#18181b`
- Border: `1px solid #18181b`
- Radius: YAML `tokens.rounded.careers-action: 9999` · §4 `9999px`
- Padding: §4 `12px 32px` (not a YAML spacing key; `32` is not `tokens.rounded.home-action: 32`)
- Font: `16px / 500 / MiSans`
- Token-set use: Careers-marketing outline action, selector surface-4::[data-omd-capture=26]
- Use: Careers outline action at `surface-4::[data-omd-capture="26"]`.
- States: Default only; no interaction event or pseudo-state captured.
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the careers marketing surface |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable button; visual treatment omitted |
| disabled | applicable | A careers action can be gated; visual treatment omitted |
| loading | not-applicable | A careers-marketing outline action does not itself present in-place progress |
| error | not-applicable | The control carries no input of its own to validate |
| success | not-applicable | The control presents no completion outcome of its own |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The capture is a 1440×900 desktop bundle. It establishes local action padding—28px horizontal on the home action, 12px on M3 paired actions, 20px on the audio action, and 24px/32px on careers actions—not a universal spacing scale.

White body surfaces recur across all four routes, but the routes serve different jobs: company/model marketing, an M3 launch, an audio tool, and recruitment.

The bundle does not establish responsive breakpoints, authenticated-app navigation, or documentation layout conventions.

Only one 1440×900 desktop capture was supplied. No breakpoint, mobile navigation, reflow, touch target, or reduced-motion behavior is verified here.

The 32px home-header radius, 8px M3 paired-action radius, 100px audio-action radius, 9999px careers-action radius, and the action paddings above are desktop-capture measurements, not cross-viewport specifications. Treating the 1440×900 figure as the collector's capture size rather than as a breakpoint system, treating those paddings as local action padding rather than as a universal spacing scale, and treating the four routes as serving different jobs rather than as one application layout, are derived editorial implementation inferences from the verified surfaces; they are not MiniMax-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Official MiniMax materials combine a mission-led company voice with concrete model capability and product-family labels. The about page expresses the mission as “Intelligence with Everyone”; the M3 launch and platform documentation name capabilities such as coding, agentic work, multimodality, and long context. This supports a direct, technical register without establishing a complete product microcopy system. The mission string, the named capabilities, the three official wording samples, and the source’s own “does not establish a complete product microcopy system” limit are the source's own. Calling the supported register direct and technical is the source’s own “supports” reading; classifying that reading as source-grounded service framing rather than as a complete product-microcopy guide is a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

| Context | Evidence-backed direction |
|---|---|
| Company positioning | State the mission and the intended human or productivity outcome plainly. |
| Model launch | Name the model and the specific capability before making a broad claim. |
| Developer documentation | Prefer a model name, modality, and operating constraint over metaphor. |

**Official wording samples**
- *“Intelligence with Everyone”* — MiniMax about page and M3 launch.
- *“Coding & Agentic”* and *“1M”* context — M3 launch page.
- *“MiniMax-M3”*, *“MiniMax Hailuo 2.3”*, and *“Speech-2.8”* — official platform model documentation.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Recorded unresolved decisions

These are named values the source already opened, not permissions to invent, and not a list of domains the source never established. That framing is a derived editorial implementation inference from the verified surfaces; it is not MiniMax-authored or a separately published UI specification.

- authenticated product session; documentation chrome; app, console, or documentation-chrome design rules
- hover, focus, pressed, disabled, empty, loading, error, success, and accessibility-state visual treatments, except the default-state observations recorded above
- an application-state specification expanded from those default observations
- responsive breakpoints, authenticated-app navigation, documentation layout conventions, mobile navigation, reflow, touch target
- a universal spacing scale; a universal app heading rule; a universal card style or color scale
- a reusable elevation ladder, card shadow, or hover-lift rule; the old purple glow and broad product-card shadow claims
- a motion token, duration, easing curve, or reduced-motion behavior; measured state or motion specifications from CSS hover utilities or `transition-*` classes
- JetBrains Mono as a general UI or code token; DM Sans, Inter, Plus Jakarta Sans, Roboto, Font Awesome, or `-apple-system` as live MiniMax UI families
- a complete product microcopy system
- a single global primary color combining the audio purple action with M3 or careers actions
