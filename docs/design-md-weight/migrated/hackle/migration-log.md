# hackle migration log

Source: `web/references/hackle/DESIGN.md`
Sibling read (not the migration input): `web/references/hackle/.verification.md`
Destination: `docs/design-md-weight/migrated/hackle/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hackle/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `197b1d2963991812cdb5c3a52697f364aafca9adc12051d1c309098b8e0f8456` (`web/references/hackle/DESIGN.md`). Sibling SHA-256 `81076337cb3566df35e08317a32a84ccb85eb568b1ecaac033fd8006cad6b994` (`web/references/hackle/.verification.md`). Worker-close portable DESIGN SHA-256 `9557604083eeed4332a5dd906ab2bc389d4719fe11963fda63ccd60dbb8715d3`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Hackle Design System` (`DESIGN.md` 1). Identity table `provenance.md` 7–22. `https://hackle.io` is dual: `DESIGN.md` 9 (`hackle.io/ko/`, `/en/`, `/ko/pricing/`) + `provenance.md` 14/45–47 (E2a). `#0065ff` is dual: `DESIGN.md` 11/92/241 and Semantic/component rows + `provenance.md` 15 (E2a). The favicon slug is dual: `DESIGN.md` 227 + `provenance.md` 16 (E2a). `핵클` is dual: `DESIGN.md` 9/13/570 + `provenance.md` 11. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–22, 30–35, 26, 141. The source footer's **Verified:** 2026-06-26 is at `provenance.md` 37. These are ledger keys with no portable slot. |
| YAML `tokens.colors` (15 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 92–115. All fifteen roles kept with names, values, and token-set keys: primary, primary-alt, docs-blue, accent-blue, tint-blue, navy-deep, heading, ink, body, muted, hairline, canvas, surface, surface-alt, white. Material ladder `rgba(0,0,0,0.87)` / `0.6` / `0.3` sits beside muted at 107, not as a substitute hex. |
| YAML `tokens.typography.family` (`display: "Montserrat"`, `body: "Pretendard"`, `docs: "Inter"`) | 옮김 → Typography & Assets Family | `DESIGN.md` 194–196. Fallbacks Montserrat Fallback / Pretendard Fallback / Inter Fallback / `ui-sans-serif, system-ui` at 186/194–196. |
| YAML `tokens.typography.display-hero` / `section` / `subsection` / `body` / `button-lg` / `button` / `docs` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 204–210. Unitless line heights stay ratios and are never converted to a single px form (A1a): `1.0` on display-hero / button-lg / button at 204/208/209/212; `1.2` on section / subsection at 205–206/212; `1.5` on body / docs at 207/210/212. Body also keeps the source's `24px` spelling at 207/212. All seven YAML `use` strings restored verbatim at `DESIGN.md` 214 and in the table Use column (A1, A3). |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (5 steps) / `tokens.shadow.none` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless steps kept: `DESIGN.md` 119 (`xs 6 · sm 10 · md 12 · base 16 · lg 20 · xl 24 · xxl 32 · section 64`). Shape at 123–129 (`xs 4 · sm 5 · md 8 · lg 12 · full 9999` plus the five named uses). `full: 9999` as an exact string is `DESIGN.md` **1** (`tokens.rounded.full: 9999` at 131). Bare `full 9999` is at 123/525. The source §5 spelling `9999px` is at 129. `tokens.spacing.md: 12` is disambiguated from `tokens.rounded.lg: 12` at 119/131. `tokens.spacing.xl: 24` is disambiguated from radius at 119. `tokens.spacing.base: 16` is disambiguated from the 16px body size at 119. `tokens.shadow.none: none` at 144 beside `box-shadow: none` at 11/144. |
| YAML `tokens.components` (12 records) | 옮김 → Components & States | `DESIGN.md` 239–499. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×6 (242/268/292/317/341/365), `input` ×1 (391), `tab` ×2 (417/438), `card` ×3 (458/469/480 — two YAML + Marketing Feature Card from §4 body), `badge` ×1 (490). All twelve YAML `use` strings restored verbatim as `Token-set use:` lines (251/277/302/326/350/375/401/423/443/463/474/497). YAML `font` shorthands kept as `token-set font record` lines (250/276/301/325/349/374/400). YAML hover `hover #0065ff @ 90% opacity` at 252. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–15; Distinctive traits 35–42. The atmosphere readings (engineered-but-friendly, single-blue persuasion, calmer docs) carry an adjacent complete qualifier at 11 (B2/B2a). Material UI foundation, hero `#0065ff` 8px, chips 5px, Montserrat/Pretendard 700, `box-shadow: none`, docs Inter + `#0c408d` kept as recorded values. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 92–115. Characterizing phrases covered by the adjacent qualifier at 88 (B2/B2a). |
| §3 Typography Rules — family, hierarchy, principles | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 182–190; family 194–198; hierarchy table 204–210; type rules 220–223. The License-row reading that treats the three faces as upstream carries an adjacent complete qualifier at 189 (B2/B2a). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 239–499. §4 body values and YAML `use` / font / radius / height forms are both kept. Reject ("거부") variant of consent is recorded on the Accept component at 376 and in the State record at 512. Marketing Feature Card (body-only, not a YAML key) at 477–485. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 28–39; Tier 1 list 51–55; Tier 2 59–61. `https://docs.hackle.io/` is dual: `DESIGN.md` 9 + `provenance.md` 48/54 (E2a). `https://github.com/hackle-io` is dual: `DESIGN.md` **9/645** + `provenance.md` 49/55 (E2a). Line 13 is the bare host `github.com/hackle-io`, not the `https://` form. Conflicts unresolved: none — `provenance.md` 39. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 518–525: 46px headline hero, logo wall quote, team/dashboard section quote, 8px cards on `#fafafa` / `#f7f7f7`, spacing 6/10/12/16/20/24/32/64, shape 4/5/8/12/9999. Whitespace three bullets at 531–533 (intro at 529). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 137–144: four-level table + `box-shadow: none`. The flat-system reading carries an adjacent qualifier at 144 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 61–68, under the grouping qualifier at 59. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 74–80, under the qualifier at 72. No extra invented-domain prohibition was added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 537–553: Mobile `<640px`, Tablet `640-1024px`, Desktop `1024-1440px`, touch 53/39/44/~72, collapsing strategy, image 8px. The Desktop row keeps the source body's `1024-1440px` range at 541/553. The system-level reading of those breakpoints carries an adjacent qualifier at 535 (B2/B2a). No defensive "does not measure 1440px" sentence (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 122. |
| §10 Voice & Tone | 옮김 → Content & Locales | Voice reading `DESIGN.md` 558; tone table 560–566; verbatim samples 568–573; further published strings 575–601; forbidden register 603. The qualifier at 558 covers the voice reading and the register-table contract (B2/B2a, A5). The peer-to-peer docs-voice characterization is qualified at 603. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: Seoul-based, 올인원 AI 그로스 플랫폼, A/B testing / feature flags / CRM 간편발송 / Simple Send / product/data analysis, consolidation pitch, `github.com/hackle-io` SDKs, `docs.hackle.io`, 개발자 문서, "웹, 앱, 서버 상관없이 5분이면 사용 가능", "팀 별로 필요한 모든 것을 하나의 대시보드에서". Marked there as narrative context that supplies no interface tokens; that classing carries an adjacent complete qualifier in the same paragraph (B2/B2a). Refuse/embrace paragraph at 15 with its own qualifier. Also noted as narrative-not-token-source at `provenance.md` 144. No founding year or founder name was added — the source itself omits those. |
| §12 Principles — 6 numbered | 옮김 → Experience principles | `DESIGN.md` 50–55 under the B2a form at 48: "These six items are a derived editorial implementation inference from the verified surfaces; they are not Hackle-authored or a separately published UI specification." Hackle has no published first-party design system, so the toss-form close is used (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 152–181 (30 rows). |
| §13 Personas — 3 entries | 삭제 | The source's own header labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or segment appears in either output (D2, D2a). Disposition at `provenance.md` 119. Audience at `DESIGN.md` 31 carries only the group-level description the source records (Korean growth teams, product managers, and engineers adopting experimentation tooling), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels the source records; 26 says so and qualifies the step from label to "primary task". |
| §14 States — 8 rows | 옮김 → Components & States State record + per-component applicability | Full eight-row body preserved at `DESIGN.md` 506–513 (A2; the catalog graph is still 0/440, so nothing is delegated), including the published strings "오류가 발생했습니다", 필수, "수락", "거부". The applicability rule is at 235, whose qualifier covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 9 interactive controls × 7 states = 63 rows. Docs Consent Accept opens loading/error/success because it commits consent (`DESIGN.md` 384–386). Docs Search opens error and closes loading/success (410–412). Hero / MUI Filled / Outline / Dark Chip / White Chip / Top Nav / Docs Nav close loading/error/success together as destination or destination-tab (`loading \| not-applicable` dest 8 · `loading \| applicable` dest 1). The three cards get no `kind` and no map because the source supplies no interaction evidence — C4 at 464/475/485. The badge declares `Kind: non-interactive` at 490 with a reason at 498 instead of a map (Core §4.4). |
| §15 Motion & Easing — durations, motion rules, reduced-motion (legacy 425–441) | 옮김 → Foundations motion | `DESIGN.md` 152–154 (120ms / 200ms / 320ms with uses), 166–172 (90% hover dim, opacity/scale press, fade-in at `motion-standard / ease-enter`, no bounce or spring, `prefers-reduced-motion: reduce`). |
| §15 Motion & Easing — three cubic-bezier values | 삭제 → provenance omission ledger | `ease-enter` `cubic-bezier(0.2, 0.6, 0.25, 1)`, `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` (exact `spec/omd-v0.1.md` template), `ease-standard` `cubic-bezier(0.25, 0.1, 0.25, 1)`. Role names and uses stay at `DESIGN.md` 160–162. Durations stay. Ledger at `provenance.md` 114–118. The omitted values are named as omitted at `DESIGN.md` 156/641, not promoted as tokens. B3 is held: the promotion condition at `DESIGN.md` 164 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). |
| Opening product sentence + Key Characteristics | 옮김 → Experience Scope + Distinctive traits | `DESIGN.md` 9 and 35–42. |

## Sibling handling (`web/references/hackle/.verification.md`)

The sibling exists — confirmed with `find web/references/hackle -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 70–90 and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: header CTA height `38px` and header radius `8px`; docs body `font-size: 16px`; copy-button label `복사` and `8px 0px 0px 8px`; sidebar `핵클 서비스` / `핵클 사용 가이드`; titles `핵클(Hackle) | 올인원 AI 그로스 플랫폼`, `핵클 사용 가이드 | Hackle`, `핵클 가격 안내 | …`; hover `oklab(...)`; frequency-scan `#f5f5f5` / `#f9fafb` / `#2d2d2d` / `#e2eeff`; favicon `1523` bytes; GitHub avatar `2962B`. Recorded at `provenance.md` 91–100.
- Measured `DESIGN.md` 0 for those sibling-only strings: `38px` 0 · `복사` 0 · `핵클 사용 가이드` 0 · `핵클 가격 안내` 0 · `oklab` 0 · `226,238,255` 0 · `245,245,245` 0.
- EN page title `Hackle | All-in-One Business Optimization Solution` is already in the source §10 (`DESIGN.md` 573) and is corroboration, not a sibling-only promotion.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All Korean runs in the source, any length ≥2 | 66 distinct | 5 | 0 | The five are §13 fictional-persona identifiers (D2 / D2a: not restated). All other Korean published copy survives in `DESIGN.md`. |
| All Korean runs in the sibling, any length ≥2 | 53 distinct | 0 | 0 | Sibling-only `복사`, `핵클 서비스`, `핵클 사용 가이드`, title strings survive in provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand hackle` | 36 candidates / 7 reported lost | 7 reported | 0 published-copy losses | 2 medium hits are CSS fragments (`1px solid #0065ff` / `1px solid #0c408d`) already at `DESIGN.md` 296/369. 1 is the sibling ellipsis variant of the KO hero line, whose full string is at 570. 2 are source-comment interpretive labels (`calm docs, bold marketing` / `one platform one dashboard`) already as Principle titles at 50/55. 1 is YAML `hover #0065ff @ 90% opacity` at 252. 1 is sibling method `curl -sL`, transcribed only as sibling method context at `provenance.md` 88. |

Sub-needle labels confirmed present individually in `DESIGN.md`: 데모 둘러보기, 상담 신청하기, 문의하기, 카드 등록하고 바로 사용하기, 간편발송 바로가기, 가이드북 다운받기, 템플릿으로 바로 만들기, 수락, 거부, 찾으시는 기능이나 키워드를 검색해보세요, 검색…, 서비스 소개, 가이드, 가격 안내, 블로그, 로그인, AI와 데이터로 이끄는 성장, 앞서가는 기업들은 이미 핵클의 고객사입니다., 웹, 앱, 서버 상관없이 5분이면 사용 가능, 올인원 AI 그로스 플랫폼, 팀 별로 필요한 모든 것을 하나의 대시보드에서, 개발자 문서, 이탈 사용자 찾아보기, 오류가 발생했습니다, 핵클, Explore Demo, Request a Demo, Go to Simple Send, View Guides, All-in-One Business Optimization Solution, Simple Send.

A5 분모: latin-copy-audit 36 candidates / 0 published-copy missing after disposition; Korean hand sweep 66 extracted / 5 missing = 5 §13 identifiers (D2a) / 0 published-copy missing. Gate `copy-loss` compared 32 / candidates 210.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hackle --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 32, candidates: 210 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 32 < `candidates` 210.

## Deviations recorded

- `DESIGN.md` is 7,198 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: twelve declared component records with dual `use`/`font` forms, an eight-row surface state contract, a 63-row applicability matrix, the full §11 Seoul-based / SDK / 개발자 문서 narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Hackle has no published first-party design system, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Measure `grep -o 'derived editorial implementation inference' DESIGN.md \| wc -l` = 30 = `not Hackle-authored` 30 = `separately published UI specification` 30. Provenance derived ledger 30 rows (E1 1:1). Worker-close was 26=26; F3 added four complete-form qualifications and four inventory rows.

## F1 B2a rescan (mandatory final pass)

The finished `DESIGN.md` was re-read from line 1. Every causal, interpretive, or judgment sentence — including Scope so/therefore boundaries, Content voice readings, Docs-citation characterizations, Semantic role characterizations, Spacing/Shape key-path judgments, Elevation and Motion readings, Font evidence classing, Family fallback ban, Type-rule readings, Assets favicon classing, applicability verdicts, Layout calm/tint readings, and Forbidden-register gloss — was asked whether it is a Hackle-issued fact or an observation-derived reading. The thirty complete-form qualifications listed in `provenance.md` 152–181 cover those readings. Worker-close named twenty-six; F3 added Motion omitted-curves, Font Outside-captures, Assets image-behavior, and Layout image-behavior, and extended Scope ¶2 and State record. Source-stated narrative (Seoul-based, `github.com/hackle-io`, 개발자 문서, 간편발송, quoted product lines) and published copy were left unqualified.

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep` located the value in the named file and section. Dual destinations (homepage hosts, `#0065ff`, favicon slug, `https://docs.hackle.io/`, `https://github.com/hackle-io`) name both files. The B3 claim is backed by the full five-kind sentence at `DESIGN.md` 164, not by a weaker paraphrase. The B2a 30=30 claim is backed by `grep -o` on the finished body and by the 30-row provenance inventory. The §13 deletion row does not restate names, ages, or cities (D2a). The omission ledger does not assert absence of an item in the same sentence that lists it (E2d).
