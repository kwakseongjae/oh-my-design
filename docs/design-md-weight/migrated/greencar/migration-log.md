# Greencar migration log

Source: `web/references/greencar/DESIGN.md`
Sibling read (not the migration input): `web/references/greencar/.verification.md` — `find web/references/greencar -type f` returned both files. A dotfile is invisible to `ls` and to a `*` glob; `find` is the measure.
Destination: `docs/design-md-weight/migrated/greencar/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/greencar/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2 wave 29
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination below was checked with `grep -o` against the three output files before it was written (F2). Counts use `grep -o … | wc -l` per file, never `grep -c`. Line numbers are from the files as written in this directory (DESIGN 469 lines).

Source SHA-256: `20bad9d8330d4bec9c42b2dd5c5444581b048bc4112e520dd7282f9c30ebefb2`
Destination SHA-256: `4f0058f6364b3e4ca47bec810beabf9297026af0eed54db7b203e167864d41d3`

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations; `logo.slug` 옮김 → Typography & Assets Assets; `그린카` 옮김 → Scope + Type roles + Content | Portable file has no frontmatter; H1 is `# Greencar Design System`. Identity table `provenance.md` Identity. `https://www.greencar.co.kr` is dual: DESIGN Scope + provenance homepage / Surfaces (E2a). `#00c88c` is dual: DESIGN Scope / Semantic color / Application rules + provenance `primary_color` and Tier 1 (E2a). Favicon slug is dual: DESIGN Assets + provenance logo row (E2a). `그린카` is dual: DESIGN Scope / Wordmark / Content + provenance `display_name_kr` (E2a). |
| YAML `omd: "0.1"`, `verified: "2026-06-26"`, `added: "2026-06-26"`, `tokens.source: live-extract`, `tokens.extracted: "2026-06-26"`, `tokens.note`, `components_harvested: true` | 분리 → provenance; 검사일 옮김 → Experience Scope | Verification metadata is a value and is kept (A1c): provenance Identity + Freshness. `live-extract` DESIGN 0 / provenance 2. `components_harvested` DESIGN 0 / provenance 2. Producer string `Verified:` DESIGN 0 / provenance 1. Calendar date `2026-06-26` is dual: DESIGN Scope (`2026-06-26` 1) + provenance Freshness (`2026-06-26` 13) (E2a). Token note facts (identity/action, Outfit/Pretendard, single card shadow) are already in DESIGN Scope / Foundations; the note string itself is provenance-only. |
| YAML `tokens.colors` (13 keys) | 옮김 → Foundations semantic color | DESIGN Semantic color. All thirteen roles kept with names, values, and token-set keys: `primary` `#00c88c` (DESIGN 9 / provenance 5), `primary-bright` `#00dc9a` (DESIGN 5 / provenance 3), `ink` `#222222` (DESIGN 16 / provenance 5), `ink-pure` `#000000` (DESIGN 9 / provenance 1), `active` `#171717` (DESIGN 5 / provenance 4), `teal` `#133b55` (DESIGN 8 / provenance 2), `nav-muted` `#777777` (DESIGN 6 / provenance 3), `body` `#303030` (DESIGN 3 / provenance 1), `muted` `#5e5e5e` (DESIGN 4 / provenance 1), `faint` `#b4b4b4` (DESIGN 4 / provenance 1), `hairline` `#dddddd` (DESIGN 10 / provenance 3), `surface` `#f6f6f6` (DESIGN 8 / provenance 2), `canvas` `#ffffff` (DESIGN 8 / provenance 3). Live `rgb()` corroborations stay with the roles. |
| YAML `tokens.typography.family` (`display: "Outfit"`, `body: "Pretendard"`) | 옮김 → Typography & Assets Family | DESIGN Family. Live stack `Outfit, Pretendard, sans-serif` is DESIGN Font evidence + Family. Token-set keys `family.display` / `family.body` are on the Family lines. |
| YAML `tokens.typography.hero-en / section / logo / nav / button / body` | 옮김 → Typography & Assets Type roles | DESIGN Type roles table. Unitless line heights stay ratios (A1a): `1.50` DESIGN 6 — one per role, matching the six YAML keys. The source's rem/px companions (`4.00rem` / `96px`, `2.25rem` / `54px`, `2.00rem` / `48px`, `1.13rem`, `1.00rem` / `24px`) stay as the source wrote them, not as a replacement. All six YAML `use` strings restored in the Token-set use column (each DESIGN 1), including `English hero tagline 'Create a Better Life', Outfit, teal #133b55` and `H1 wordmark 그린카`. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (6 steps) | 옮김 → Foundations spacing + shape; also Layout | Unitless steps and px forms both kept: DESIGN Spacing `xs 4 · sm 8 · md 12 · base 16 · lg 20 · xl 32 · xxl 48` (each step DESIGN 1), Shape `sm: 6` / `md: 8` / `base: 12` / `lg: 16` / `xl: 20` / `full: 9999` (`full: 9999` DESIGN 1 / provenance 3; bare `9999` DESIGN 2 / provenance 4). `full: 9999` stays a step and is not given a px suffix. Spacing `base: 16` and Shape `lg: 16` occupy different slots (easywallet trap). |
| YAML `tokens.shadow.card` / `shadow.none` | 옮김 → Foundations elevation | DESIGN Elevation: `rgba(0,0,0,0.08) 0px 4px 8px` (DESIGN 6 / provenance 4) and `shadow.none` = `none` (DESIGN 1). `box-shadow: none` DESIGN 1. |
| YAML `tokens.components` (7 records) | 옮김 → Components & States | DESIGN Solid Dark / Outline Dark / Outline Invert / News Card / Brand Panel / Top Nav Item / Green Accent Link. Verified primitive types preserved, not flattened (A1b): `Primitive type: \`button\`` 3, `Primitive type: \`card\`` 2, `Primitive type: \`tab\`` 1, `Primitive type: \`listItem\`` 1. All seven YAML `use` strings restored as `Token-set use:` lines (each DESIGN 1). YAML border shorthands restored as contiguous strings: `1px solid #222222` DESIGN 2, `1px solid #ffffff` DESIGN 2, `1px solid #dddddd` DESIGN 3. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope DESIGN 7–17; Key Characteristics as Distinctive traits. Atmosphere readings carry adjacent complete qualifiers at Scope ¶2–¶4 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | DESIGN Semantic color. Role names Greencar Green / Bright Green / Chrome Ink / Pure Black / Active Ink / Teal Navy / Body / Muted / Nav Muted / Faint / Pure White / Surface Grey / Hairline kept. Characterizing phrases sit under the Semantic color qualifier (B2/B2a). |
| §3 Typography Rules — stack, hierarchy, principles | 옮김 → Typography & Assets | Evidence classes DESIGN Font evidence; family; token-set table; conventions as Type rules under an adjacent qualifier. |
| §4 Component Stylings | 옮김 → Components & States | DESIGN Solid Dark through Green Accent Link. §4 body values and YAML `use` strings are both kept. Published labels `채용 바로가기` DESIGN 5 / provenance 2, `더 많은 뉴스 보기` DESIGN 5 / provenance 1, `더 알아보기` DESIGN 5 / provenance 2, `회사소개` DESIGN 3 / provenance 1, `인재채용` DESIGN 3 / provenance 1, `개인정보 처리방침` DESIGN 3 / provenance 1, `위치기반 서비스 이용약관` DESIGN 3 / provenance 1 (E2a). |
| Footer **Verified** / **Tier 1** / **Tier 2** / Conflicts | 분리 → provenance; 두 URL 옮김 → Experience Scope; 검사일 옮김 → Experience Scope | Freshness + Surfaces + Proof notes in provenance. Both URLs are dual: DESIGN Scope + provenance Surfaces / Tier 1 (E2a). Conflicts unresolved: none — provenance Freshness. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing / shape | DESIGN Layout: centered hero, full-bleed green bands, 12px-radius card row, `#f6f6f6` tinted bands, 4/8/12/16/20/32/48 scale. Brand-band-as-section-device reading carries an adjacent complete qualifier (B2/B2a). Shape steps from this section land in Foundations Shape, not in Spacing. |
| §6 Depth & Elevation | 옮김 → Foundations elevation | DESIGN Elevation four-level table + `shadow.card` / `shadow.none`. Philosophy-as-binding-rule reading is qualified (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | DESIGN Application rules, under the grouping qualifier. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | DESIGN Avoid, under the qualifier. One extra line restates this contract's scope boundary; the same qualifier covers it. |
| §8 Responsive Behavior — breakpoints, touch, collapsing, images | 옮김 → Layout & Platforms | DESIGN Layout breakpoint table under the adjacent complete qualifier that reads the table as declared behavior rather than as an observation. `<640px` DESIGN 1, `640-1024px` DESIGN 1, `1024-1440px` DESIGN 1. Touch 52px / 14px 20px, hamburger toggle, full-bleed green persist, card shadow on all viewports. Sibling viewport `1440×900` is dual: DESIGN Layout method note + provenance Sibling handling (E2a); it is not a new breakpoint token. |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every verified value §9 names was checked against the portable body before deletion (A2, A3). The check is itemised at provenance Omission ledger. |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings DESIGN Content; register table; forbidden register. Tagline `Create a Better Life` is dual: DESIGN Primary tasks + Type roles + Content + provenance Claim ledger (E2a). Section heading `그린카는 고객 중심의 모빌리티 풀 라인업을 열어갑니다.` DESIGN 3 / provenance 1 (E2a). Recruit `더 나은 모빌리티 경험을 함께 만들어 갈 여러분을 기다립니다.` DESIGN 2 / provenance 1 (E2a). Meta `이동을 넘어 사람과 공간을 연결하는 그린카.` DESIGN 2 / provenance 1 (E2a). Title `그린카(Greencar) 공식사이트` DESIGN 2 / provenance 1 (E2a). The voice reading carries an adjacent complete qualifier (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope | DESIGN Scope ¶5: pioneer car-sharing, established **2011**, **2015** Lotte Rental (`롯데렌탈`) subsidiary, `프리미엄 카셰어링 서비스`, `롯데렌탈과 함께`, `이동을 넘어 사람과 공간을 연결하는`, `사용자 중심의 새로운 이동 경험`, G car (round-trip and one-way, G car zone, G car Pass, G car M, G car Biz), Movus, Clingwash, `고객 중심의 모빌리티 풀 라인업`. `2011` DESIGN 1 / provenance 1; `2015` DESIGN 1 / provenance 1 (E2a). Refuse/embrace pair is qualified (B2/B2a). Official history is narrative context, not a token source. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | DESIGN Principles under the fixed B2a form: "These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Greencar-authored or a separately published UI specification." |
| §13 Personas — 3 entries | 삭제 | The source's own italic line labels them fictional archetypes informed by publicly observable user segments, not individual people. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: §13 three entries (names, ages, cities, biographies) dropped and not restated in either output (D2 / D2a). Disposition at provenance Omission ledger. Audience carries only the group-level set the source's own header names (Korean urban car-sharing users, weekend renters, corporate mobility customers), under an adjacent qualifier. Identifier strings from that section measure DESIGN 0 / provenance 0; this log row does not restate them. |
| §14 States — 9 rows | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at DESIGN Surface state contract (A2; the catalog graph is still 0/440, so nothing is delegated), including the generic `오류가 발생했습니다` avoidance (DESIGN 2) and the bare `필수` avoidance. The applicability rule is at How applicability is decided here, whose qualifier covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 5 interactive components × 7 states. `loading \| applicable` DESIGN 0. `loading \| not-applicable` DESIGN 5 (Solid Dark, Outline Dark, Outline Invert, Top Nav Item, Green Accent Link) — destination roles; role reasons, not capture-absence (C2 v10). News / Line-up Card and Green Brand Panel get no `kind` and no map because the source supplies no interaction evidence (C4). Nav active `#171717` is kept as its own observation rather than folded into a canonical state. State coverage is not claimed complete (C3). |
| §15 Motion & Easing — durations, easing names/roles, fade-in / press rules, reduced-motion | 옮김 → Foundations motion | DESIGN Motion: three duration tokens with their uses (`motion-fast` 120ms DESIGN 1, `motion-standard` 200ms DESIGN 2 — table row plus the fade-in rule, `motion-slow` 320ms DESIGN 1), three easing token names and roles, no-bounce rule, fade-in from below, press opacity/scale, `prefers-reduced-motion: reduce` (`prefers-reduced-motion` DESIGN 1). Adjacent complete qualifier on the subsection (B2/B2a). |
| §15 Motion & Easing — three `cubic-bezier` values | 삭제 → provenance omission ledger | No observation stands behind them. `ease-exit` `cubic-bezier(0.4, 0.0, 1, 1)` is the documented re-injection path (`spec/omd-v0.1.md`). Occurrence counts by `grep -o cubic-bezier` → DESIGN 0, provenance 4 (three values + the template comparison). B3 is held: the promotion condition at DESIGN Motion names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). |

## Sibling handling

Sibling present. Confirmed with `find web/references/greencar -type f` = `DESIGN.md` + `.verification.md`. Recorded at provenance Sibling handling. Date, method, and the two URLs agree with the source footer, so the sibling corroborates and does not widen the token set.

Sibling-only measurements kept in provenance and not promoted to the portable body (B1): green-band padding `220px` DESIGN 0 / provenance 1; nav padding `6px 0px` DESIGN 0 / provenance 1; nav active `font-weight: 500` DESIGN 0 / provenance 1 (source keeps weight 600 + active color `#171717`; the two records are not merged); radius frequency `50%` DESIGN 0 / provenance 2 and `100px` DESIGN 0 / provenance 2; logo `1012` bytes DESIGN 0 / provenance 1; Medium tech-blog URL DESIGN 0 / provenance 1.

Sibling-recorded published copy adopted into DESIGN Content and provenance (A5 / A5a; dual): `그린카의 이동은` DESIGN 1 / provenance 2; `롯데렌터카 G car` DESIGN 2 / provenance 2. Neither is merged into the source's `롯데렌탈` spelling or the corporate title.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published CTAs / taglines / nav / policy / meta / product names in the source | 26 distinct | 0 | 0 | `Create a Better Life`, `그린카는 고객 중심의 모빌리티 풀 라인업을 열어갑니다.`, `더 나은 모빌리티 경험을 함께 만들어 갈 여러분을 기다립니다.`, `이동을 넘어 사람과 공간을 연결하는 그린카.`, `그린카(Greencar) 공식사이트`, `채용 바로가기`, `더 많은 뉴스 보기`, `더 알아보기`, `회사소개`, `서비스`, `인재채용`, `개인정보 처리방침`, `위치기반 서비스 이용약관`, `프리미엄 카셰어링 서비스`, `롯데렌탈과 함께`, `사용자 중심의 새로운 이동 경험`, `고객 중심의 모빌리티 풀 라인업`, `오류가 발생했습니다`, G car / G car zone / G car Pass / G car M / G car Biz / Movus / Clingwash, `2011`, `2015`, `롯데렌탈` all survive in DESIGN. |
| Sibling-measured published copy not in the source body | 2 | 0 | 0 | `그린카의 이동은`, `롯데렌터카 G car` survive in DESIGN Content + provenance. |
| `node test-v2/tools/latin-copy-audit.mjs --brand greencar` | 15 candidates | 0 after restoring YAML border shorthands `1px solid #222222` and `1px solid #ffffff` as contiguous strings | 0 | Candidate output, not a verdict. First run reported those two CSS shorthands as lost because the hex sat in its own backticks; they are token values, not published copy, and are now contiguous in DESIGN. |

Survival is measured against `DESIGN.md` and `provenance.md`. This log does not restate the deleted persona entries' names, ages, or cities (D2a).

## F1 / F2

F1 B2a scan: DESIGN was re-read in full after the body was drafted. Every causal / interpretive / judgment sentence — Scope atmosphere, type-personality, restraint, refuse/embrace, Content voice reading, sibling-title classification, applicability Reason cells, breakpoint declared-vs-observed, published-string glosses — received an adjacent complete qualifier. Measured `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = **27**; the same 27 lines carry `not Greencar-authored` and `separately published UI specification`. Provenance derived ledger has **27** data rows (E1 1:1). F3 audit 2026-08-28 raised the body from 25 to 27 (breakpoints + published-string gloss) and folded four existing qualifiers (Scope ¶1 surface-attachment / sibling title, Shape action workhorse, Family substitution prohibition, Surface-state prose-vs-inspection).

F2 E2 contrast: each log row above was written after `grep -o` on the actual files. Dual destinations are listed only where both files contain the string. Compliance claims ("B3 is held") point at the DESIGN Motion sentence that names all five evidence kinds in full text.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand greencar --gate-only` → `verdict: PASS`, `problems: []`.
`inspectDesignMd(...).conformance` → `portable_core: true`, `level: portable-core`, `reasons: []`.
`coverage.copy-loss compared 25 / candidates 170`. A5a hand sweep is in the table above. Not evidence of semantic adequacy (E2c).
