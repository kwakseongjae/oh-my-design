# Barogo Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Barogo (바로고) is a Korean last-mile delivery-dispatch platform. Catalog homepage identity is `https://www.barogo.com/`.

Treating the following two URLs as the named first-party evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. This contract covers two brand-owned surfaces from the 2026-07-02 packet: the public homepage `https://www.barogo.com/` and the company page `https://www.barogo.com/aboutUs`.

Source token note: primary = live hero-CTA orange (`#fa5014`, rgb(250,80,20)); text is true black (`#000000`) softening to `#111111`; near-shadowless flat surfaces with a `#2d3ce6` electric-blue used only inside the aboutUs brand-identity block. Single family Pretendard, with Termina reserved for Latin/numeral display accents. Treating that note as a register split — `#fa5014` is catalog `primary_color` and the harvested primary-CTA fill plus outline-button ink, `#000000` / `#111111` stay text inks rather than navy, `#2d3ce6` stays inside the “BAROGO Brand Identity” block rather than general UI color, Pretendard is the document default, and Termina is numeral/Latin display only — is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

The following visual-character, operator-plainness, high-energy-plainness, matter-of-factness, and eye-trained-to-orange-as-act-now readings — including confident operator-that-moves-physical-goods, hot-delivery-orange applied-with-discipline, orange-owns-the-primary-CTA-and-outline-ink-and-little-else, and high-contrast-read-mirroring-무엇이든-어디서나 — are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. The source reads the website as carrying the confident, high-energy plainness of an operator that moves physical goods for a living. The canvas is pure white (`#ffffff`) with occasional cool-grey resting surfaces (`#f6f6f6`, `#f9f9f9`), and text is set in true black (`#000000`) softening to a near-black (`#111111`) — no navy, no warm greys. The single saturated brand color is a hot delivery orange (`#fa5014`), applied with discipline: it owns the primary call-to-action, the outline-button ink, and little else. The source says the eye is trained to read orange as "act now — get moving," and it connects that direct high-contrast read to the brand line "무엇이든 어디서나" (anything, anywhere).

The following Korean-utilitarian, almost-no-ornament, and size-and-weight-not-color-or-decoration hierarchy readings — including Pretendard-carries-the-entire-site, Termina-as-only-exception, and quiet-weight-400-for-body-and-UI — are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. The source treats the typographic voice as Korean-utilitarian rather than editorial. A single family, **Pretendard**, carries essentially the entire site (measured across 434–474 nodes per page) — bold weight 700 for every headline from the 42px homepage hero up to the 54px mission line on the company page, and a quiet weight 400 at 16px for body and UI. The only exception recorded is **Termina**, a geometric Latin display face reserved for oversized numerals such as the ghosted `2016` founding-year marker (150px, black at 5% alpha / `#000000` at 0.05 alpha) on the about page.

The following near-flat, sharp-cornered, functional-and-fast, and structure-by-tint-hairline-footer readings — including barely-there ~3px corners, restrained elevation, and identity-blue as a deliberate secondary-brand accent never as general UI color — are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. Buttons round at a barely-there ~3px (measured 3.008px). Cards step up to 12px and 16px, and larger rounded surfaces reach 20px. Elevation stays restrained: a light `rgba(0,0,0,0.1) 0px 2px 20px` card shadow and a slightly firmer `rgba(0,0,0,0.2) 0px 2px 10px` under the primary CTA. Structure is communicated by tint (`#f6f6f6`), hairlines (`#dcdcdc`), and the black footer (`#1a1a1a`) rather than by heavy stacked cards. An electric blue (`#2d3ce6`) appears once, inside the "BAROGO Brand Identity" block, as a deliberate secondary-brand accent — never as general UI color.

Treating the following public-history and about-page facts as narrative rather than interface tokens, including founding-year-from-ghost-numeral, scale-as-public-fact rather than a quoted first-party claim, structural-gap / shared-rails / software-coordination as source §11 relations, and live-surface labels as site-visible names, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. The 2016 founding year is taken from the oversized ghost numeral on the aboutUs page. The mission line "세상에 활력을 더하는 초연결 생태계를 만듭니다." is verbatim from that page. Broader scale and positioning claims (including “one of Korea's largest”) are general public knowledge about the company, not quoted from a single verified statement in the source turn. Source §11 records a structural gap: individual stores had no reliable, technology-driven way to get orders to customers, and the delivery-agency market was fragmented and manual. The premise, captured in that mission line, was to become the shared logistics rails connecting merchants, riders, and delivery demand. The product coordinates riders and store operators through software; the site-visible entry points are "스토어프로그램", "라이더 지원", and "허브 창업", plus the "숫자로 보는 바로고" section.

The following refusal, embrace, and operator-aesthetic readings are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. The source treats the design as refusing the decorative softness of consumer-lifestyle branding — no navy-and-pastel gentleness, no heavy card-stacking, no pill-rounded playfulness — and as embracing one hot orange for action, true-black high-contrast text, a single workhorse typeface (Pretendard), sharp functional corners, and a flat, fast surface.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three live-surface jobs below as the independently verified user outcomes, and keeping source §13 fictional archetypes out of this list, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

- Start an inquiry with "바로 문의하기" on the public homepage.
- Open merchant, rider, and hub-franchise entry points named on the site ("배달대행 문의", "라이더 지원", "허브 창업", "스토어프로그램").
- Read company-scale figures in "숫자로 보는 바로고" on aboutUs.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable Barogo user segments (Korean store owners using delivery dispatch, delivery riders, hub franchise operators), not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and observable work is tied only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

### Distinctive traits

- Catalog `primary_color` / delivery orange `#fa5014` reserved for the primary CTA fill and outline-button ink
- True black `#000000` and near-black `#111111` text on white `#ffffff`
- Cool-grey resting surfaces `#f6f6f6` / `#f9f9f9` and hairline `#dcdcdc`
- Black footer `#1a1a1a` with white `#ffffff` links
- Electric blue `#2d3ce6` only inside the aboutUs "BAROGO Brand Identity" block
- Pretendard for headlines (42px / 54px weight 700) and body/UI (16px weight 400); Termina for the ghosted 150px `2016` (black at 5% alpha / `#000000` at 0.05 alpha)
- Sharp ~3px button corners (measured 3.008px); cards at 12px / 16px; rounded surfaces at 20px; YAML `full` 9999 / `9999px`
- Restrained elevation: card `rgba(0,0,0,0.1) 0px 2px 20px`, CTA `rgba(0,0,0,0.2) 0px 2px 10px`

Treating `#fa5014` as the single action hue rather than a second identity color and as catalog `primary_color` plus primary-CTA fill plus outline ink, treating `#2d3ce6` as a brand-identity-block accent rather than general UI color, treating `#000000` / `#111111` as text inks rather than navy, treating 3px / measured 3.008px as button geometry rather than a pill, treating Termina as numeral/Latin display rather than hangul body, treating cool-grey surfaces and hairlines as structure rather than stacked cards, treating the black footer as the closing band, and treating restrained elevation as not a heavy card stack, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

### Principles

These five items, including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification.

1. **Anything, anywhere — reliably.** The brand promise is universal reach with operational dependability. *UI implication:* keep the primary action ("바로 문의하기") always obvious in orange; never bury the path to getting started.
2. **One action, one color.** Orange (`#fa5014`) means "do this." *UI implication:* reserve the saturated orange for the primary CTA and outline ink so the next step is never ambiguous.
3. **Operator, not marketer.** Copy and layout favor plain capability over hype. *UI implication:* lead with concrete figures ("숫자로 보는 바로고") and direct labels; avoid superlatives and decorative flourish.
4. **Fast and flat.** A logistics product should feel quick and unfussy. *UI implication:* near-shadowless surfaces, tint-and-hairline separation, sharp ~3px corners — nothing that reads as slow or heavy.
5. **Respect the store owner.** The merchant ("사장님") is the customer whose craft Barogo protects. *UI implication:* merchant-facing copy stays supportive ("사장님의 정성 그대로"); tools present as approachable, not gatekeeping.

Treating the following as a capture-bound application of source §7 Do’s and harvested geometry is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

- Reserve Barogo orange (`#fa5014`) for the primary CTA and outline-button ink — keep it the single "action" color.
- Set every headline in Pretendard weight 700; use weight 400 at 16px for body and UI.
- Use true black (`#000000`) / near-black (`#111111`) for text on white — direct and high-contrast.
- Keep button corners sharp at ~3px; step cards up to 12px/16px and rounded surfaces to 20px.
- Separate sections with flat tint (`#f6f6f6` / `#f9f9f9`) and `#dcdcdc` hairlines, not heavy shadows.
- Keep elevation light — `rgba(0,0,0,0.1) 0px 2px 20px` on cards, firmer `rgba(0,0,0,0.2)` only on the CTA.
- Use the black footer (`#1a1a1a`) with white (`#ffffff`) links for the closing band.
- Reserve Termina and the electric blue (`#2d3ce6`) for display numerals and the brand-identity block respectively.

### Avoid

The following items copy source §7 Don’ts, including orange-dilutes-the-single-action-signal, headlines-always-Pretendard-700, navy-or-soft-grey-not-primary-text, 3px-not-pill, tint-and-hairlines-not-heavy-shadows, Termina-not-hangul-body, electric-blue-not-general-UI, and no-second-saturated-accent-in-product-chrome. They are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification.

- Do not spread orange across many elements — it dilutes the single-action signal.
- Do not set headlines in a light weight — display is always Pretendard 700.
- Do not use navy or soft grey for primary text — Barogo text is true black on white.
- Do not round buttons into full pills — the ~3px sharp corner is the functional read.
- Do not lean on heavy stacked drop shadows for structure — use tint and hairlines instead.
- Do not use Termina for hangul body text — it is a Latin/numeral display accent only.
- Do not promote the electric blue (`#2d3ce6`) into general UI color — it belongs to the brand-identity block.
- Do not mix in a second saturated accent alongside orange in product chrome.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including single-saturated-action-hue, near-black-on-secondary-headings, cool-grey-resting-for-segmented-blocks, slightly-lighter-alternating-bands, footer-rendered-alongside-pure-black, and hairline-not-a-card-fill, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. Catalog `primary_color` `#fa5014` is the live hero-CTA orange (rgb(250,80,20)) and the harvested primary-CTA fill; the same hex is outline-button ink and border — those jobs share a hex and are not one semantic. Ink `#000000` is heading/body text and top-nav ink; it is also the ghost numeral’s `#000000` at 0.05 alpha, not a second black token and not footer `#1a1a1a`. Ink Soft `#111111` is a near-black companion, not Ink. Body Grey `#666666` is secondary copy; it is also badge text, not Ink. Muted Grey `#999999` is tertiary text. Pure White `#ffffff` is page canvas, card fill, on-primary / on-dark text, and footer-link color; those jobs share a hex and are not a second canvas token. Surface Grey `#f6f6f6` is not Surface Alt `#f9f9f9`. Hairline `#dcdcdc` is not a card fill. Dark `#1a1a1a` is the footer band, not Ink. Brand-Identity Blue `#2d3ce6` is the aboutUs identity-block accent, not general UI chrome and not Primary.

- **Barogo Orange / Primary** (`#fa5014`): catalog `primary_color`. The brand's single saturated color and primary CTA background (rgb(250,80,20)). Also the ink and border color of outline buttons. YAML `primary`.
- **Ink Black** (`#000000`): Primary heading and body text color — true black. YAML `ink`.
- **Ink Soft** (`#111111`): A near-black used on secondary headings and dense text. YAML `ink-soft`.
- **Body Grey** (`#666666`): Secondary body copy and descriptions on content-heavy pages. YAML `body`.
- **Muted Grey** (`#999999`): Tertiary text, captions, and metadata. YAML `muted`.
- **Pure White** (`#ffffff`): Page background, card surfaces, text on orange/dark. Also the `on-primary` text color. YAML `canvas` / `on-primary`.
- **Surface Grey** (`#f6f6f6`): Cool-grey resting surface for segmented content blocks. YAML `surface`.
- **Surface Alt** (`#f9f9f9`): A slightly lighter secondary grey surface for alternating bands on the company page. YAML `surface-alt`.
- **Hairline** (`#dcdcdc`): Thin borders, dividers, and card outlines. YAML `hairline`.
- **Dark** (`#1a1a1a`): Near-black footer background (rendered alongside pure `#000000`), carrying white links. YAML `dark`.
- **Brand-Identity Blue** (`#2d3ce6`): An electric blue used only inside the "BAROGO Brand Identity" block on the about page. YAML `accent-blue`.

### Spacing

YAML `spacing`: xs 8, sm 12, md 16, base 20, lg 30, xl 40, xxl 50. Body layout states a ~8px base rhythm, scaling 8 / 12 / 16 / 20 / 30 / 40 / 50.

Treating that YAML scale and the ~8px / 8 / 12 / 16 / 20 / 30 / 40 / 50 rhythm as recorded spacing in this packet rather than a universal spacing token for every unlisted control, treating unitless YAML steps as not converted into a required px suffix, and treating component padding as staying with the components, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. Primary CTA pads `0px 40px` (YAML `0 40px`). Outline pads `0px 30px` (YAML `0 30px`). Feature cards pad `20px`. Stat cards pad `40px 32px`. Footer band pads 50px vertically. Nav links pad 10px vertically within the top header.

### Shape

YAML `rounded`: sm 3, md 12, lg 16, xl 20, full 9999 (`9999px`).

The following local-geometry reading, including 3px-not-3.008px, 3px-not-pill, sharp-functional-button-read, and the Small/Medium/Large/X-Large/Full role labels, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. 3px / measured 3.008px / 12px / 16px / 20px / `9999px` remain local harvested geometry, not a universal radius for every unlisted control. YAML `rounded.sm` 3 and measured 3.008px share a role and are not merged into one number. Do not round buttons into full pills.

Observed radii in the body: Small 3px (buttons — sharp, functional; measured 3.008px on the live primary CTA); Medium 12px (feature/info cards); Large 16px (stat cards); X-Large 20px (rounded surfaces and pill-style chips); Full `9999px` (fully rounded elements when used).

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, headings, inline text, most surfaces |
| Tint (Level 1) | `#f6f6f6` / `#f9f9f9` background shift | Section/card separation without elevation |
| Hairline (Level 2) | `1px solid #dcdcdc` border | Card and divider outlines |
| Card (Level 3) | `rgba(0,0,0,0.1) 0px 2px 20px` | Feature and stat cards |
| CTA (Level 4) | `rgba(0,0,0,0.2) 0px 2px 10px` | Primary orange call-to-action button |

YAML `tokens.shadow.cta` is `rgba(0,0,0,0.2) 0px 2px 10px 0px`. YAML `tokens.shadow.card` is `rgba(0,0,0,0.1) 0px 2px 20px 0px`. Body CTA / card shadows are the same strings without the trailing `0px`. YAML `button-primary.shadow` is `rgba(0,0,0,0.2) 0px 2px 10px`. Treating the table Use assignments (page background, headings, inline text, most surfaces; section/card separation without elevation; card and divider outlines; Feature and stat cards; Primary orange call-to-action button) as source-stated uses in this reconstruction, and treating those trailing-`0px` and no-trailing-`0px` writings as unmerged records of the same harvested shadows, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

The following near-flat and shadow-philosophy readings, including light-and-diffuse-when-used, CTA-reads-as-pressable, fast-and-modern-rather-than-heavy, and emphasis-from-orange-and-sharp-geometry-not-stacked-shadows, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. The source reads Barogo as a near-flat system. Most of the page carries `box-shadow: none`; depth is communicated through flat tinted surfaces (`#f6f6f6`, `#f9f9f9`) and `#dcdcdc` hairlines. When elevation is used it is light and diffuse — a soft `rgba(0,0,0,0.1) 0px 2px 20px` lift on cards, and a slightly firmer, tighter `rgba(0,0,0,0.2) 0px 2px 10px` under the primary CTA so the single action button reads as pressable. The source reads that restraint as keeping the logistics UI feeling fast and modern rather than heavy — emphasis comes from the orange (`#fa5014`) and the sharp geometry, not from stacked drop shadows.

### Motion

Source-stated duration roles. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list. Treating the duration table, easing names, signature carousel/press, and reduced-motion line as source-stated rather than computed CSS, treating omitted `ease-exit` as matching the legacy spec-template example, treating `ease-enter` / `ease-exit` / `ease-standard` as the nintendo/workday/barogo unattributed set rather than computed CSS, and treating the "functional and steady" / "signals reliability, not playfulness" readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 220ms | Card/section reveal, dropdown, slide (hero carousel step) |
| `motion-slow` | 320ms | Page-level transitions, hero reveal |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, sheets, carousel slides |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including functional-and-steady, matching-fast-flat-logistics-operator, signature carousel-press, no-bounce-or-spring reliability-not-playfulness, and reduced-motion-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. Motion is described as functional and steady — matching the fast, flat aesthetic of a logistics operator. The homepage hero carousel advances with a `motion-standard / ease-enter` slide (the "Previous slide" / "Next slide" controls); buttons respond to press with a subtle opacity/scale shift. No bounce or spring — a delivery-infrastructure product is read as signalling reliability, not playfulness. Under `prefers-reduced-motion: reduce`, carousel auto-advance pauses and all transitions collapse to instant; the site remains fully functional.

Treating unattributed cubic-bezier curves as omitted rather than promoted, while keeping source-stated names and uses, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. Exact cubic-bezier curves are unattributed — `ease-standard`, `ease-enter`, and `ease-exit` match the nintendo/workday/barogo unattributed set, and `ease-exit` matches the legacy spec template — and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Termina-as-numeral-ornament-only and Pretendard-as-document-default, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Homepage: Pretendard ×434 visible uses; aboutUs continues the same family. Headlines Pretendard 700 (homepage hero 42px; aboutUs mission 54px). Body/UI Pretendard 16px / 400. |
| Display accent | Termina (`termina, sans-serif`) on the ghosted 150px `2016` founding-year marker, `#000000` at 0.05 alpha / black at 5% alpha. Never used for hangul body text. |

The following font-use boundary, including Pretendard as the document default carrying the measured site rather than Termina, Termina as geometric-Latin numeral/display only, and a system fallback as not Pretendard, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. Do not replace Pretendard with Termina on hangul body or UI. Do not present Termina as the document default. Do not present a system fallback as Pretendard.

### Family

- **Body / UI / Headlines:** `Pretendard` — YAML `family.body`. The document default, carrying essentially the entire site (measured across 434–474 nodes per page). Weight 700 for display, weight 400–500 for text and nav.
- **Display Accent:** `Termina` (`termina, sans-serif`) — YAML `family.display`. A geometric Latin face reserved for oversized numeral/Latin display accents.

### Type roles

Verified YAML line-height values are the unitless ratios `1.33`, `1.43`, and `1.0`. The following ratio-versus-size-local reading is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. YAML roles without a `lineHeight` field stay without one; none is invented.

The following one-family size-and-weight, bold-display-light-body, Termina-as-numeral-ornament-only, and high-contrast-black-text type-rule readings, including not-the-softened-navy-common-to-fintech-peers, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. Pretendard does nearly all the work; hierarchy comes from size (16px → 42px → 54px) and weight (400 → 700), not from mixing typefaces or colors. Every headline is weight 700; body and UI sit at weight 400. Termina appears solely on oversized numerals and Latin wordmarks (the ghosted `2016`), never on functional hangul text. Text is `#000000`/`#111111` on white — direct and legible, not the softened navy common to fintech peers.

| Role | Font | Size | Weight | Line height (YAML) | rem | Use (YAML) |
|---|---|---:|---:|---:|---:|---|
| Mission Hero | Pretendard | 54px | 700 | 1.33 | 3.38rem | About-page mission headline, Pretendard Bold |
| Hero / Section | Pretendard | 42px | 700 | 1.43 | 2.63rem | Homepage hero + section titles, Pretendard Bold |
| Stat Numeral | Termina | 150px | 700 | — | 9.38rem | Ghosted background year numerals (Termina), color #000000 at 0.05 alpha |
| Sub-section | Pretendard | 36px | 700 | 1.0 | 2.25rem | Sub-section heads / stat block labels |
| Nav | Pretendard | 20px | 500 | — | 1.25rem | Top nav item, Pretendard (700 when active) |
| Button | Pretendard | 20px | 700 | — | 1.25rem | Primary CTA label, Pretendard Bold |
| Button Small | Pretendard | 18px | 700 | — | 1.13rem | Outline button label, Pretendard Bold |
| Body | Pretendard | 16px | 400 | 1.0 | 1.00rem | Standard reading / UI text, Pretendard |
| Caption | Pretendard | 12px | 500 | — | 0.75rem | Footer links, Pretendard |

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

Treating verified product and company imagery as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted. Hex values and geometry in the harvested components remain source-stated. The source HTML comment places sections 10–15 in the philosophy layer; treating graph-not-adopted preservation of that contract, and treating the table below as that philosophy-layer contract rather than computed per-component CSS, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no orders / no results)** | White canvas. Single Ink Black (`#000000`) line at body size explaining there is nothing yet, with one orange (`#fa5014`) CTA to take the next operational step. No decorative illustration. |
| **Empty (saved / list none yet)** | Muted Grey (`#999999`) single line: nothing here yet, plus a path back to the primary flow. Calm and plain. |
| **Loading (dashboard / results fetch)** | Skeleton cards on `#f6f6f6` tinted surface at final 12px/16px dimensions. Light flat pulse consistent with the near-shadowless system — no heavy shimmer. |
| **Loading (action submit)** | Inline progress on the pressed control; the orange CTA stays visible with a reduced-opacity state rather than disappearing. |
| **Error (request failed)** | Inline message in Ink Black with a plain-language explanation and a retry. States what to do next — never a bare "오류가 발생했습니다". |
| **Error (form validation)** | Field-level message below the input in a direct tone; describes what is valid, not just "필수". |
| **Success (inquiry / application submitted)** | Brief inline confirmation in a plain, operational tone; next-step detail linked immediately below. No celebratory emoji. |
| **Skeleton** | `#f6f6f6` blocks at final dimensions, 12px/16px radius, flat pulse. |
| **Disabled** | Muted Grey (`#999999`) text on reduced-opacity surface; the orange action fades rather than turning grey to preserve the brand read. |

Characterizations in that table such as “No decorative illustration”, “Calm and plain”, “no heavy shimmer”, “never a bare 오류가 발생했습니다”, “not just 필수”, “No celebratory emoji”, “preserve the brand read”, next-operational-step, path-back-to-the-primary-flow, light-flat-pulse-consistent-with-near-shadowless, orange-CTA-stays-visible-at-reduced-opacity-rather-than-disappearing, plain-language-what-to-do-next, direct-tone-describes-what-is-valid, plain-operational-tone-with-next-step-linked, and orange-fades-rather-than-turning-grey are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records no generic Focus capture with a colour. The `focus-visible` rows do not carry a colour. A later generic Focus observation would not be `focus-visible` evidence. Source `motion-fast` names “Hover, button press, focus” as duration use; that duration row is not `focus-visible` treatment evidence.

Feature Card, Stat Card, and Badges have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Primary CTA

- Role: primary call-to-action — "바로 문의하기"
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#fa5014`
- Text: `#ffffff`
- Radius: 3px
- Padding: 0px 40px
- Height: 72px
- Font: 20px / 700 / Pretendard
- Shadow: `rgba(0,0,0,0.2) 0px 2px 10px`
- Use: Primary CTA — 바로 문의하기
- Observed: default only
- YAML `tokens.components.button-primary`
- Field note: The following unmerged-field reading, including harvested-height-not-touch-target on `72px`, 3px-not-measured-3.008px, fill-jobs-not-one-semantic on `#fa5014`, and `#ffffff` label-not-a-second-canvas-token, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#fa5014` is this control’s fill and catalog `primary_color`. It is also outline-button ink/border; those jobs are not this button. `#ffffff` is this control’s label and on-primary; it is also canvas / card fill / footer-link color. Height `72px` is this action’s harvested height, not a touch-target policy. Radius 3px is YAML `rounded.sm` / this control; measured 3.008px is the live inspect of this CTA and is not merged into 3px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says the orange action fades rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the primary inquiry CTA ("바로 문의하기") and records §14 Loading (action submit) / Success (inquiry / application submitted) as philosophy-layer rows; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Outline / Secondary

- Role: secondary outline action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#fa5014`
- Border: 1px solid `#fa5014`
- Radius: 3px
- Padding: 0px 30px
- Height: 64px
- Font: 18px / 700 / Pretendard
- Use: Secondary outline action — 스토어프로그램 / 사용 설명서 / 자세히 보기; body also records "BI / GUIDE"
- Observed: default only
- YAML `tokens.components.button-outline`
- Field note: The following unmerged-field reading, including harvested-height-not-touch-target on `64px`, not merging this outline with the primary CTA, and `#fa5014` ink-not-fill, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#fa5014` is this control’s text and border, not this control’s fill. Height `64px` is this action’s harvested height, not the 72px primary CTA and not a touch-target policy. Radius 3px is this control, not 12px/16px cards.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the outline / secondary action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as secondary actions ("스토어프로그램", "사용 설명서", "자세히 보기", "BI / GUIDE"); exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Feature Card

- Role: feature/info card grid on aboutUs
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 12px
- Padding: 20px
- Shadow: `rgba(0,0,0,0.1) 0px 2px 20px`
- Use: Feature/info card grid on aboutUs
- Title: Pretendard 700 / `#000000`
- Body: 16px / 400 / `#666666`
- Observed: default white fill; body also records the same card geometry rendered in `#fa5014`, `#000000`, `#2d3ce6`, and `#dcdcdc` fills
- YAML `tokens.components.card-feature`
- Field note: The following unmerged-field reading, including additional-fills-not-page-wide-primary, not-Ink-as-card-fill, not-identity-blue-as-general-chrome, not-Hairline-as-fill, 12px-not-16px-not-3px, and §9-only title/body local tuple, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#ffffff` is this card’s default fill and Canvas. The `#fa5014` / `#000000` / `#2d3ce6` / `#dcdcdc` fills are additional observed fills of this geometry, not Primary as a page-wide card token, not Ink as a card fill, not Brand-Identity Blue as general UI chrome, and not Hairline as a fill. Radius 12px is this card, not 16px stat cards and not 3px buttons. Source §9-only mixed anatomy is title Pretendard 700 / `#000000` with body 16px / 400 / `#666666`; that local tuple stays on this card and is not YAML Body 16px / 400 as a page-wide token and not Mission/Hero 700 as a card title role.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Stat Card

- Role: "숫자로 보는 바로고" statistic card
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 16px
- Padding: 40px 32px
- Shadow: `rgba(0,0,0,0.1) 0px 2px 20px`
- Use: '숫자로 보는 바로고' statistic card
- Observed: default only
- YAML `tokens.components.card-stat`
- Field note: The following unmerged-field reading, including canvas-fill, 16px-not-12px, and padding-40px-32px-not-20px, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#ffffff` is this card’s fill and Canvas. Radius 16px is this card, not 12px feature cards. Padding `40px 32px` is this card, not feature-card `20px`.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Badges

- Role: small rounded category/status pills
- Anatomy: label
- Background: `#f6f6f6`
- Text: `#666666`
- Radius: 20px
- Font: 12px / 500 / Pretendard
- Use: Small rounded category/status pills reusing the 20px surface radius
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.
- Field note: The following unmerged-field reading, including fill-not-canvas, text-not-Ink, and 20px-not-3px-not-full-9999, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#f6f6f6` is this pill’s fill and Surface Grey, not Canvas. `#666666` is this pill’s text and Body Grey, not Ink. Radius 20px is this pill and YAML `rounded.xl`; it is not button 3px and not a YAML `full` 9999 / `9999px` writing of this pill.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Header / Navigation band

- Role: header/navigation band (parent surface)
- Anatomy: surface
- Background: `#ffffff`
- Use: White header behind top nav items
- Observed: default only
- Field note: The following parent-surface reading, including header-fill-not-nav-item-background, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#ffffff` is this band’s fill and Canvas. YAML nav-link records fg/font/active/use only; this header fill is the parent surface, not a Top Nav item Background.

### Top Nav

- Role: top horizontal navigation item
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#000000`
- Font: 20px / 500 / Pretendard
- Active: weight 700, `#000000`
- Use: Top navigation item — "배달대행 문의", "라이더 지원", "허브 창업", "회사 소개", "채용"
- Observed: static default and static active appearances. The active appearance is a captured variant, not an observed click transition.
- YAML `tokens.components.nav-link`
- Field note: The following unmerged-field and captured-variant readings, including weight-700-active-not-a-second-type-role, YAML-use-versus-five-body-labels, and item-not-header-fill, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. `#000000` is this item’s text and Ink. Weight 700 on the active item is this component’s active field, not a second type role. YAML `use` says Top navigation item; the five labels are body §4. This item has no Background field; the header band fill `#ffffff` stays on the parent surface.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top navigation item |
| hover | applicable | Pointer-web nav item; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A top nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Nav meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | Nav meaning is destination, not action-outcome confirmation |

Additional observed named state: static active appearance (weight 700, `#000000`). Treating that appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

### Footer band

- Role: footer band (parent surface)
- Anatomy: surface
- Background: `#1a1a1a`
- Use: Near-black dark footer behind footer links
- Observed: default only
- Field note: The following parent-surface reading, including footer-fill-not-link-background, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#1a1a1a` is this band’s fill and Dark, not Ink `#000000`. YAML footer-link records fg/font/use only; this footer fill is the parent surface, not a Footer Link Background.

### Footer Link

- Role: footer navigation link on the near-black dark band
- Kind: interactive
- Type: listItem
- Anatomy: label
- Text: `#ffffff`
- Font: 12px / 500 / Pretendard
- Use: Footer navigation link on #1a1a1a dark footer
- Observed: default only
- YAML `tokens.components.footer-link`
- Field note: The following unmerged-field reading, including on-dark-not-a-second-canvas-token, 12px-500-not-Body-16-400, and item-not-footer-fill, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification. `#ffffff` is this link’s text and on-dark, not a second canvas token. 12px / 500 is Caption / this control, not Body 16px / 400. This item has no Background field; the footer band fill `#1a1a1a` stays on the parent surface.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the footer navigation link |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A footer link can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a footer navigation link; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 8, sm 12, md 16, base 20, lg 30, xl 40, xxl 50. Body layout repeats a ~8px base rhythm, scaling 8 / 12 / 16 / 20 / 30 / 40 / 50. Buttons pad horizontally at 30px (outline) and 40px (primary CTA); cards pad at 20px, stat cards at 40px 32px; footer band pads 50px vertically. Nav links pad 10px vertically within the top header.

The “operational clarity over decoration”, “flat segmentation”, and “sharp, functional geometry” readings in this section, including logistics-dense-product-feeling-calm-and-scannable and generous-vertical-rhythm, are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification.

Recorded layout:

- Centered single-column white Hero: 42px / 700 / `#000000` Pretendard H1, the orange primary CTA, and beside it the outline CTA pair
- Feature/info cards arranged in multi-column grids (12px radius), grouping services and store-program entry points
- Alternating full-width bands: white (`#ffffff`) content over cool-grey (`#f6f6f6` / `#f9f9f9`) resting sections
- About page adds oversized ghost numerals (Termina) as background typographic texture behind stat blocks
- Operational clarity over decoration: generous vertical rhythm between sections keeps a logistics-dense product feeling calm and scannable
- Flat segmentation: sections separate by background tint (`#f6f6f6` vs `#ffffff`) and `#dcdcdc` hairlines rather than by heavy elevation
- Sharp, functional geometry: the ~3px button corner keeps interactive chrome reading as fast and utilitarian

Source breakpoint table:

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, hero headline compresses, card grids stack |
| Tablet | 640–1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024–1440px | Full layout, centered hero, multi-column feature/stat grids |

Collapsing strategy recorded in the source: Hero 42px (home) / 54px (about) headline scales down on mobile, weight 700 maintained; feature/stat card grids multi-column → 2-up → single stacked column; alternating white / tinted bands maintain full-width treatment; internal padding reduces; oversized Termina ghost numerals scale down or clip gracefully as background texture.

Image behavior: App screenshots and illustrations sit on flat surfaces with light or no shadow, consistent with the near-flat system. Cards maintain their 12px/16px radius across breakpoints.

Touch-target record: Primary CTA at 72px height with 40px horizontal padding; outline buttons at 64px height with 30px horizontal padding; nav links padded 10px vertically within the top header.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 72px / 64px / 10px / 42px / 54px figures as surface measurements in this packet rather than universal layout tokens, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating collapsing / image-behavior rows as source-stated rather than a cross-viewport specification, and treating image-behavior as consistent-with-the-near-flat-system rather than a new elevation rule, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-07-02)

The live strings below are source-stated. Treating the parenthetical characterizations (capability-first / mission-framed / merchant-supportive) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

- "무엇이든 어디서나" — brand tagline / homepage title (capability-first). *(verified live 2026-07-02)*
- "무엇이든 어디서나 배달" — homepage hero H1. *(verified live 2026-07-02)*
- "세상에 활력을 더하는 초연결 생태계를 만듭니다." — aboutUs mission headline (mission-framed). *(verified live 2026-07-02)*
- "성공하는 매장의 노하우" — homepage section heading (merchant-supportive). *(verified live 2026-07-02)*
- Control and section strings from harvested surfaces: "바로 문의하기"; "자세히 보기"; "사장님의 정성 그대로"; "스토어프로그램"; "사용 설명서"; "BI / GUIDE"; "배달대행 문의"; "라이더 지원"; "허브 창업"; "회사 소개"; "채용"; "숫자로 보는 바로고"; "바로고가 걸어온 길"; ghost numeral "2016"

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Barogo-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — including direct-operational-can-do, logistics-partner-that-measures-whether-the-delivery-arrives, two-audiences-merchants-and-riders, practical-and-reassuring, infrastructure-that-lets-a-small-business-focus-on-its-craft, and operator-not-marketer — are a derived editorial implementation inference from the verified surfaces; they are not Barogo-authored or a separately published UI specification. They are not the Observed strings above.

The source describes Barogo's voice as **direct, operational, and can-do** — the register of a logistics partner that measures itself by whether the delivery arrives. The tagline "무엇이든 어디서나" and the hero "무엇이든 어디서나 배달" set the tone in that reading: plain, capability-first Korean, no hype. Copy is read as addressing two audiences at once — merchants ("사장님") and riders — and as staying practical and reassuring for both, framing Barogo as the infrastructure that lets a small business focus on its craft ("사장님의 정성 그대로").

| Context | Tone |
|---|---|
| Hero headlines | Plain, capability-first. "무엇이든 어디서나 배달." Confident, never superlative. |
| Merchant-facing copy | Supportive, respectful of the store owner. "사장님의 정성 그대로", "성공하는 매장의 노하우". |
| CTAs | Direct, low-friction. "바로 문의하기", "자세히 보기", "바로레터 구독하기". |
| Company / mission | Aspirational but grounded. "세상에 활력을 더하는 초연결 생태계를 만듭니다." |
| Data / trust copy | Concrete and numeric. "숫자로 보는 바로고" — lets the figures speak. |

**Forbidden register** (source): hype superlatives, fear-based urgency, jargon that hides the operational reality, exclamation-heavy sales copy. The source closing line that Barogo speaks like an operator, not a marketer, is part of this same derived reading.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, press, and `focus-visible` visual treatments
- loading, error, and success visual treatments on the harvested buttons and footer link, and the omitted loading/error/success applicability fields on Primary CTA, Outline / Secondary, and Footer Link
- interactive kind and state-applicability map for Feature Card, Stat Card, and Badges
- YAML `lineHeight` on Stat Numeral, Nav, Button, Button Small, and Caption
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
