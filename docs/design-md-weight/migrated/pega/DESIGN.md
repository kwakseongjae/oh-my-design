# Pega UX Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Pega is an enterprise AI decisioning and workflow-automation company whose public UX system is built for people completing consequential, guided work. Catalog homepage identity is `https://design.pega.com/`. YAML `ds.name` is Pega UX Design System, `ds.url` is `https://design.pega.com/`, `ds.type` is system. The YAML `ds.description` is kept as written: Pega's public, prescribed system for enterprise application workflows. This contract covers the supplied public Pega UX Design System surfaces the source inspected on 2026-07-13: the public system home at `https://design.pega.com/` (`home` and `surface-2`) and the public components route at `https://design.pega.com/components/` (`surface-3`). The official design-resources page at `https://design.pega.com/resources/design-resources/` is a named source for design assets and Roboto Flex confirmation. Official getting-started material, forms guidance, and Pega’s 2021 brand account are named in the source as system-purpose, forms, and corporate-evolution context; they do not supply the computed interface tokens below. Every value stays attached to the surface that established it. YAML token note, kept as written: Only the supplied public Pega UX Design System surfaces are tokenized; corporate and authenticated-product UI are outside this evidence bundle. Current web geometry is limited to Pega’s public design-system documentation, not Pega corporate marketing or an authenticated enterprise application. Reading those inspected public UX System URLs as this contract’s token surfaces, keeping design-resources, getting-started material, forms guidance, and the 2021 brand account as named sources that do not supply computed interface tokens, keeping every value attached to the surface that established it, and keeping that token note as the facts it names, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

In the supplied Pega UX System surfaces, a deep blue header, crisp white canvas, Roboto Flex, compact utility controls, and a single large pill-shaped action create a calm but operationally direct document experience. The system’s recognizable expression is deliberately prescribed rather than a loose sticker sheet: it directs teams toward pre-built workflows, familiar patterns, and reduced interface choice so that attention can stay on business logic, accuracy, and progress. Catalog `primary_color` is `#1a3a5c`. The captured layer pairs header `#1a3a5c` with white `#ffffff` utility text, a dark `#03102e` `link-as-button` action at 59px height and 32px radius, body text `#050505`, and a public-home link sample of accent `#0b6dd3` on accent-weak `#e8f3fb`. The hex values, Roboto Flex, the `link-as-button` class, the 59px / 32px action, and the public-home accent pair are recorded. Characterizations built on them — a calm but operationally direct document experience; deliberately prescribed rather than a loose sticker sheet — are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

Brand narrative recorded by the source, kept as narrative context. Pega says founder and CEO Alan Trefler began developing chess-playing AI agents more than four decades ago, applying the underlying ideas to technology and business. Its current corporate story positions the company as an enterprise-transformation platform for governed, scalable, adaptable processes and decisions. That long horizon helps explain the UX system’s emphasis on prescribed patterns: the public getting-started guidance explicitly redirects effort away from repeated button-placement choices and toward business logic, workflows, and productivity. The current design-system site describes Constellation as the default design system name and direction while saying the underlying system remains the same. This is a product-language evolution, not evidence that all historical Pega public surfaces share the measured values in this reference. Pega’s 2021 brand account separately describes a move away from a rigid, four-color/four-shape visual system toward a more modern, flexible, narrative illustration approach; that corporate brand history is not used as a token source for the Pega UX System pages. The founder/CEO, chess-playing AI agents, more than four decades, enterprise-transformation platform wording, getting-started redirect away from repeated button-placement choices, Constellation as the default name and direction with the underlying system remaining the same, product-language evolution, 2021 four-color/four-shape account, and that closing not-a-token-source sentence are the source’s own recordings of first-party pages; they do not by themselves supply interface tokens. Classifying that founding-and-Constellation-and-2021 narrative as context that does not by itself supply interface tokens, and the “long horizon helps explain” causal link from that history to prescribed patterns, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product’s primary tasks, each naming a captured public UX System surface or control the source records, and not taking them from the source’s stakeholder-group section, is a derived editorial implementation inference from the verified surfaces; it is not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

- Read the public Pega UX Design System home at `https://design.pega.com/`, including its header search and dark `link-as-button` action.
- Scan public design-system menu rows on that home.
- Read the public components route at `https://design.pega.com/components/`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source states that this reference does not invent named personas. Pega’s official UX-system pages identify people who design, author, configure, and complete guided enterprise work: application designers and authors use the documented patterns, while end users create and manage Cases and complete Assignments. Pega’s public corporate context also names large organizations modernizing workflows and customer experiences. These are stakeholder groups, not substitutes for first-party user research or verified application roles. No name, age, city, occupation, motivation, or affiliation classification is carried into this document or its sidecar. Keeping those source-named groups in the source’s own wording, and not lifting Cases or Assignments into Primary tasks, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

### Distinctive traits

The list restates values the source records. The groupings and the readings inside them are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

- Deep header blue `#1a3a5c` paired with white `#ffffff` utility text
- A dark `#03102e` link styled as a 59px, 32px-radius primary action in the captured public system home, with explicit `link-as-button` class
- Roboto Flex loaded on the supplied surfaces and named in Pega’s official UX System ’25 design resources; 184 visible observations
- Compact documentation chrome (version-control padding 6px / 12px; search 12px horizontal) beside that large forward action (16px / 32px at 59px)
- Public-home accent pair `#0b6dd3` / `#e8f3fb` kept on that link sample, not on the dark `link-as-button`
- Zero interaction actions and zero observed states; no hover, focus, pressed, disabled, error, or selection styling is asserted

### Principles

These four items are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation. The numbered stems rest on official public guidance the source attributes to first-party pages (forms guidance, getting-started, corporate position). Every *UI implication* below is the source’s own editorial reading. The source’s own closing sentence already says the UI implications are this reference’s application of official public guidance, not a claim of unpublished Pega product rules.

1. **Guide work toward an accurate outcome.** The system’s forms guidance makes speed, readability, and reduced overwhelm explicit. *UI implication:* show the next required task and its context before adding optional controls.
2. **Prescribe the repeatable decisions.** Pega’s getting-started page emphasizes pre-built workflows and consistent patterns. *UI implication:* make ordinary workflow choices predictable instead of styling each page as a separate concept.
3. **Keep essential input explicit.** Pega recommends labels and helper text instead of placeholders. *UI implication:* expose what a field means and what is required without relying on faint temporary copy.
4. **Build for continuous change.** Pega’s corporate position ties enterprise transformation to adaptable, governed systems. *UI implication:* use clear information hierarchy that can accommodate evolving case data without obscuring the current action.

### Application rules

The source states these four as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

- Use the documented system as a guide for task-oriented enterprise flows, where the next meaningful action is clear.
- Preserve the measured dark blue `#03102e` action, white `#ffffff` text, and 32px action radius only in a comparable public-system context.
- Use Roboto Flex only when it is actually available and appropriately licensed for the target project.
- Keep labels, helper content, and form structure explicit; Pega’s forms guidance favors labels and helper text over placeholders.

The source’s Agent Prompt Guide also records these unique constraints, kept as written: a **Pega UX System public documentation or guided-enterprise-workflow direction**; a deep-blue utility header, white canvas, Roboto Flex when truly available, low-shadow separation, clear labels, and one highly legible dark rounded next-step action; Let the workflow and business information lead. Keeping those Agent Prompt Guide constraints on this page rather than as a tool prompt is a derived editorial implementation inference from the verified surfaces; it is not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

### Avoid

The source states these four as its Don’t list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

- Do not transplant public documentation chrome into an authenticated Pega application without product-surface evidence.
- Do not substitute a system font and label it Roboto Flex.
- Do not invent hover, focus, pressed, disabled, error, or motion values from this zero-interaction capture.
- Do not promote DM Sans, JetBrains Mono, or Source Serif 4 from declared CSS into a Pega UI-family claim.

The same Agent Prompt Guide records these unique prohibitions, kept as written: Do not present this public documentation geometry as verified Pega product UI, and leave all interactive states and motion unspecified unless new product-surface evidence records them. Keeping those prohibitions here rather than as a tool prompt is a derived editorial implementation inference from the verified surfaces; it is not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

### Public Pega UX System surface roles

Pairing each hex to the token-set path named beside it, keeping `tokens.colors.canvas` `#ffffff` off `tokens.components.dark-link-action.fg` `#ffffff` and off header utility text `#ffffff`, keeping `tokens.colors.action` `#03102e` off `tokens.colors.header` `#1a3a5c`, keeping `tokens.colors.accent` `#0b6dd3` and `tokens.colors.accent-weak` `#e8f3fb` on the public-home link sample rather than on the dark `link-as-button`, and reading the observed palette as a documentation-surface record rather than as Pega corporate campaign colors or authenticated-product semantic colors, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation. The hex values and recorded uses are the source’s own.

- **Header** (`#1a3a5c`): measured background of the public design-system masthead. Token-set path `tokens.colors.header`. Catalog `primary_color` is this value.
- **Action** (`#03102e`): measured fill of the explicit `link-as-button` action on the public home surface. Token-set path `tokens.colors.action`.
- **Canvas** (`#ffffff`): measured page background and text-on-dark pairing. Token-set path `tokens.colors.canvas`.
- **Foreground** (`#050505`): measured dark menu and body text. Token-set path `tokens.colors.foreground`.
- **Accent** (`#0b6dd3`): measured blue foreground in a public-home link sample. Token-set path `tokens.colors.accent`.
- **Accent Weak** (`#e8f3fb`): measured pale-blue background in that same public-home link sample. Token-set path `tokens.colors.accent-weak`.

The observed palette is a documentation-surface record. It does not establish Pega corporate campaign colors or authenticated-product semantic colors.

### Spacing

YAML `tokens.spacing` keys, kept as separate steps (`version-button-x: 12` is not `search-input-x: 12`; `dark-action-y: 16` is not `tokens.typography.body.size` `16` and is not the dark-action font `16px`; `dark-action-x: 32` is not `tokens.rounded.dark-action: 32` and is not header-search height `32px`):

- `tokens.spacing.version-button-y`: 6
- `tokens.spacing.version-button-x`: 12
- `tokens.spacing.search-input-x`: 12
- `tokens.spacing.dark-action-y`: 16
- `tokens.spacing.dark-action-x`: 32

`version-button-y` / `version-button-x` match the captured version control’s 6px vertical and 12px horizontal padding / `6px 12px`. `search-input-x: 12` matches the search input’s 12px horizontal padding / `0px 12px`. `dark-action-y` / `dark-action-x` match the observed dark action’s 16px vertical and 32px horizontal padding at 59px height / `16px 32px`. Keeping those five keys unmerged, and keeping those writings of `12`, `16`, and `32` on their own records, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

### Shape

YAML `tokens.rounded` keys, kept as separate steps (`version-button: 2` is not a declared component; `dark-action: 32` is not `tokens.spacing.dark-action-x: 32` and is not header-search height `32px`; `search-input: 0` is the search-input radius step):

- `tokens.rounded.version-button`: 2
- `tokens.rounded.dark-action`: 32
- `tokens.rounded.search-input`: 0

The 32px dark-action radius belongs to the public-home `link-as-button`. The 0px search-input radius belongs to the header search. The version-button radius `2` is a YAML rounded key; the source token set does not declare a version-button component. Reading those three keys as that split, not as a universal radius scale, is a derived editorial implementation inference from the verified surfaces; it is not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

### Elevation

The retained dark action, header controls, menu rows, and captured article containers report no box shadow. No Pega elevation scale is established. In this narrow evidence bundle, hierarchy comes from the dark header, contrast, padding, and the action’s rounded silhouette rather than a reusable shadow ladder. The no-box-shadow observations and the “no Pega elevation scale” sentence are the source’s own. Reading hierarchy as coming from that header-contrast-padding-silhouette cluster rather than as a reusable shadow ladder is a derived editorial implementation inference from the verified surfaces; it is not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

### Motion

The evidence packet records `interactionCount: 0`, no interaction kinds, and no transition or easing measurements. No duration, easing, reduced-motion, or motion-behavior token is established. Promote a motion token only after a per-component computed observation of transition properties, animation name, duration, easing, and reduced-motion behavior. A partial confirmation — one curve read off one element, or a match against an official framework or specification document, including the published Pega UX Design System documentation — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, refusing a partial confirmation, and keeping the source’s `interactionCount: 0` / no-duration-easing-reduced-motion-or-motion-behavior sentence, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record’s font evidence into classes. The sorting, treating loaded Roboto Flex as the UI family on the supplied surfaces, treating the UX System ’25 design-kit naming as official-asset confirmation rather than a licence grant, treating DM Sans, JetBrains Mono, and Source Serif 4 as declared-only, treating `-apple-system` and Arial as runtime fallbacks rather than Pega brand families, and reading SIL Open Font License 1.1 as typeface context separate from Pega’s own application assets, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use and live surface-use | Roboto Flex is the loaded, high-confidence family across 184 visible observations in the supplied Pega UX System pages, including body text, headings, buttons, inputs, cards, and menu rows. The Pega UX System ’25 design-kit resource also names “Roboto Flex” as its font family. Token-set path `tokens.typography.family.ui`. |
| Official distributed design asset | Pega’s design-resources page links its Figma kit, icon library, and Roboto Flex family. This confirms the family as a Pega UX System asset; it does not transfer a Pega project asset licence to another project. |
| Licence boundary | The Roboto Flex project publishes its fonts under the SIL Open Font License 1.1. That licence is typeface context, separate from Pega’s own application assets and from this reference’s measured web usage. |
| Declared-only and unresolved | DM Sans, JetBrains Mono, and Source Serif 4 were declared in the captured CSS but had zero visible uses. They are not promoted to a UI token. System stacks such as `-apple-system` and Arial were also observed, but are treated as runtime fallbacks rather than Pega brand families. |

### Family

- **Current visible UI family:** `Roboto Flex`. Token-set path `tokens.typography.family.ui`.
- Do not replace unavailable or unobserved brand type with Roboto Flex. It is canonical here only because computed visible use, loaded FontFace evidence, and Pega’s official UX System ’25 design-kit naming agree. Do not substitute a system font and label it Roboto Flex.

Keeping Roboto Flex as the UI-family token on the supplied public UX System surfaces, treating it as canonical here only because computed visible use, loaded FontFace evidence, and the official design-kit naming agree, and refusing those substitutes, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

### Type roles

YAML writes heading size `38.4` and body size `16` without a unit; source §3 writes `38.4px / 500 / 48px` and `16px / 400 / 24.64px`. Both writings stay. YAML line heights stay the px strings `48px` and `24.64px` and are never converted to a replacement unitless ratio (A1a). The heading is a component-page (`surface-3`) measurement; the body is a public-home measurement. Pairing each role to the token-set path named beside it, keeping those two surfaces unmerged, keeping `tokens.typography.body.size` `16` off `tokens.spacing.dark-action-y: 16` and off the dark-action font `16px`, and keeping header-search font `14px / 400 / Roboto Flex` on that component rather than as a YAML typography role, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

| Role | Font | Size | Weight | Line height | Provenance |
|---|---|---:|---:|---|---|
| Component-page heading (`tokens.typography.heading`) | Roboto Flex | 38.4 / 38.4px | 500 | 48px | measured public documentation values retained as reusable reference points; `surface-3` / `https://design.pega.com/components/` |
| Body (`tokens.typography.body`) | Roboto Flex | 16 / 16px | 400 | 24.64px | measured public documentation values retained as reusable reference points; public home |

Header-search font `14px / 400 / Roboto Flex` stays on that component. It is not a YAML typography role.

### Assets

- Catalog favicon: `https://www.google.com/s2/favicons?domain=pega.com&sz=128`. Frontmatter records `logo.type: favicon`.
- Pega’s design-resources page links its Figma kit, icon library, and Roboto Flex family. This confirms those as Pega UX System assets; it does not transfer a Pega project asset licence to another project.
- Roboto Flex upstream licence: SIL Open Font License 1.1 at `https://github.com/googlefonts/roboto-flex`. That licence is typeface context, separate from Pega’s own application assets.

Reading the Google s2 favicon URL as a catalog identity pointer rather than as a Pega-hosted brand file, and reading the design-resources links and the OFL as asset-association and typeface-licence context rather than as a Pega project licence grant, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here in full: No authenticated-product empty, loading, success, error, or disabled state was supplied. The packet records zero interaction actions and zero observed states, so no component-state contract is inferred. Pega’s public documentation discusses forms, accessibility, and workflow patterns, but that narrative does not create measured state tokens for this reference. The menu item is retained as `listItem`: the supplied evidence records a row/menuitem, not button semantics. No hover, focus, pressed, disabled, error, or selection styling is asserted because the bundle records zero interaction actions and zero observed states.

The following applicability note, every interactive-kind verdict, every applicability verdict, the reason given for either, attaching a YAML `Primitive type` only when the token set records that type on that component, refusing a §4-only component outside the three token-set records, and the refusal to treat this as a complete state-coverage claim are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted where this packet holds no value. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow each control’s product role rather than its primitive kind. This is not a complete state-coverage claim.

A `Primitive type` line is attached only when the source YAML records that type on that component. The three token-set records are `dark-link-action`, `header-search`, and `menu-row`. There is no §4-only component outside that set. The YAML rounded key `version-button` is not a declared component.

### Public dark link action

- Role: public design-system link with explicit `link-as-button` class
- Primitive type: `button` · YAML `tokens.components.dark-link-action.type: button` · Kind: interactive
- Background: `#03102e`
- Text: `#ffffff`
- Border: `1px solid #ffffff`
- Radius: `32px` / `tokens.rounded.dark-action` 32
- Height: `59px`
- Padding: `16px 32px` / `tokens.spacing.dark-action-y` 16 · `tokens.spacing.dark-action-x` 32
- Font: `16px / 700 / Roboto Flex`
- YAML fields on this component: `tokens.components.dark-link-action.type`, `tokens.components.dark-link-action.bg`, `tokens.components.dark-link-action.fg`, `tokens.components.dark-link-action.border`, `tokens.components.dark-link-action.radius`, `tokens.components.dark-link-action.height`, `tokens.components.dark-link-action.padding`, `tokens.components.dark-link-action.font`, `tokens.components.dark-link-action.states`, `tokens.components.dark-link-action.use`
- Token-set use: public design-system link with explicit link-as-button class
- Use: public design-system link with explicit `link-as-button` class
- YAML states, kept as written: default observed only; interaction capture reported 0
- Observed: default observed only; interaction capture reported 0

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured default on the public system home |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable link; visual treatment omitted |
| disabled | applicable | A destination link whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a public design-system destination link with an explicit `link-as-button` class; it commits no operation in place |
| error | not-applicable | A destination link does not report a failed request on itself |
| success | not-applicable | Reaching the destination is not an operation this link reports as success |

### Header search input

- Role: public design-system search input
- Primitive type: `input` · YAML `tokens.components.header-search.type: input` · Kind: interactive
- Background: `rgba(255, 255, 255, 0.14)`
- Text: `#ffffff`
- Radius: `0px` / `tokens.rounded.search-input` 0
- Height: `32px`
- Padding: `0px 12px` / `tokens.spacing.search-input-x` 12
- Font: `14px / 400 / Roboto Flex`
- YAML fields on this component: `tokens.components.header-search.type`, `tokens.components.header-search.bg`, `tokens.components.header-search.fg`, `tokens.components.header-search.radius`, `tokens.components.header-search.height`, `tokens.components.header-search.padding`, `tokens.components.header-search.font`, `tokens.components.header-search.states`, `tokens.components.header-search.use`
- Token-set use: public design-system search input
- Use: public design-system search input
- YAML states, kept as written: default observed only; interaction capture reported 0
- Observed: default observed only; interaction capture reported 0

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured default on the public system home |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable field; visual treatment omitted |
| disabled | applicable | An input whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a public design-system search field; it does not commit an operation whose in-progress state it could report |
| error | applicable | Form field; visual treatment omitted |
| success | not-applicable | A value in this field is not an operation this control reports as success |

### Menu row

- Role: public design-system menu row
- Primitive type: `listItem` · YAML `tokens.components.menu-row.type: listItem` · Kind: interactive
- Text: `#050505`
- Height: `42px`
- Font: `16px / 400 / Roboto Flex`
- YAML fields on this component: `tokens.components.menu-row.type`, `tokens.components.menu-row.fg`, `tokens.components.menu-row.height`, `tokens.components.menu-row.font`, `tokens.components.menu-row.use`
- Token-set use: public design-system menu row
- Use: public design-system menu row
- The supplied evidence records a row/menuitem, not button semantics.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured default on the public system home |
| hover | applicable | Pointer-web menuitem; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable menuitem; visual treatment omitted |
| disabled | applicable | A menuitem whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control is a menu row / menuitem; it commits no operation in place |
| error | not-applicable | A menu row does not report a failed request on itself |
| success | not-applicable | Selecting the row is not an operation this control reports as success |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

- **Prescribed workflow first:** Pega’s official getting-started material frames the system as a way to remove trivial UI decisions so teams can focus on business logic, productivity, and ROI.
- **Compact documentation chrome:** the captured version control uses 6px vertical and 12px horizontal padding / `6px 12px`; the search input uses 12px horizontal padding.
- **Large forward action:** the observed dark action uses 16px vertical and 32px horizontal padding at 59px height.
- **Scope boundary:** these dimensions describe the supplied public design-system pages at 1440×900; they do not prove a product application grid, responsive breakpoint, or authenticated workflow layout.

Only 1440×900 surfaces were supplied. Pega’s official material says the system addresses multi-device support and responsive behavior, but this packet contains no measured breakpoint, reflow, touch-target, or mobile-navigation value. Those fields are therefore absent.

Reading “Prescribed workflow first,” “Compact documentation chrome,” and “Large forward action” as the source’s own layout sentences rather than as a published product grid, and treating 1440×900 as the supplied capture size rather than as a breakpoint system, are derived editorial implementation inferences from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Pega’s official system language is practical and outcome-oriented: it describes guided workflows, familiar interaction patterns, accessibility, localization, and the fastest path to an accurate outcome. The corporate voice adds “Build for Change” to a long-running AI and automation story. Treat this as public documentation and positioning guidance, not as a complete error-message or transactional-copy specification.

| Context | Direction grounded in official material |
| --- | --- |
| Workflow guidance | Name the task and the outcome plainly. |
| Form help | Explain what is expected with labels and short helper text. |
| Product framing | Connect consistency to speed, accuracy, and adaptability. |

**Official wording samples**

- *“Patterns and components for building enterprise applications.”* — Pega UX Design System home.
- *“The fastest path to an accurate outcome.”* — Pega forms guidance.
- *“Build for Change®.”* — Pega corporate positioning.

Reproduce those strings byte-exact rather than translating or re-casing them. Naming the official system language practical and outcome-oriented, treating the table and the three wording samples as public documentation and positioning guidance rather than as a complete error-message or transactional-copy specification, are a derived editorial implementation inference from the verified surfaces; they are not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Treating the list as unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not Pega-authored or taken from a separately published UI specification, including the published Pega UX Design System documentation.

- hover, focus, pressed, disabled, error, and selection visual treatments (zero interaction actions and zero observed states)
- authenticated-product empty, loading, success, error, or disabled visual treatments
- duration, easing, reduced-motion, and motion-behavior tokens
- measured breakpoint, reflow, touch-target, or mobile-navigation value
- a product application grid, responsive breakpoint, or authenticated workflow layout
- Pega corporate campaign colors and authenticated-product semantic colors
- a Pega elevation scale
- complete error-message or transactional-copy specification
- DM Sans, JetBrains Mono, or Source Serif 4 as a Pega UI-family claim
