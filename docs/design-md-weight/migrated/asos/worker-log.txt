ASOS T2 마이그레이션을 시작합니다. 지정된 규격·규칙집·골든 샘플을 순서대로 읽고, 그다음에 legacy 원문을 옮기겠습니다.규격은 확인했습니다. 승인된 대응표·골든 샘플·ASOS 원문을 이어서 읽겠습니다.골든 샘플과 ASOS 원문, 기존 이관 산출물을 읽겠습니다.ASOS 원문은 확보했습니다. 최근 PASS 이관 형태와 모션 템플릿·게이트 요구를 대조하겠습니다.게이트가 원본의 hex·px·ms를 모두 요구하므로, 원문 토큰을 전부 뽑아 빠짐없이 옮기겠습니다.원문 토큰 목록은 확보했습니다. Core v2 `DESIGN.md`를 골든 샘플 형태와 airbridge/adobe B2a 패턴으로 작성합니다.본문을 처음부터 다시 읽어 B2a 한정이 빠진 해석 문장을 찾고, 그와 맞춰 provenance를 쓰겠습니다.게이트가 같은 줄의 `focus-visible`과 hex를 승격으로 잡습니다. 해당 줄을 끊겠습니다.provenance.md를 작성합니다. 허위 준수 주장 없이, 이중 목적지는 둘 다 적습니다.Portable Core가 Scope claim을 거부했습니다. 검사기가 무엇을 요구하는지 확인합니다.ASOS legacy `DESIGN.md`를 Core v2로 옮겼습니다. 산출물은 `docs/design-md-weight/migrated/asos/`입니다.

보존한 값과 경계를 요약하면 다음과 같습니다.

- YAML unitless line-height `1.25` / `1.0`은 비율 그대로 두었고, 본문의 “16px on 16px”와 합치지 않았습니다.
- 컴포넌트 primitive는 button×4, input, card×3, badge, tab×2로 유지했습니다. Size Selector는 YAML type이 없어 만들지 않았습니다.
- Google favicon URL은 provenance에만 두고, portable Assets에는 URL 없는 identity 경계만 남겼습니다.
- §13 가상 persona는 삭제했고 sidecar에도 다시 싣지 않았습니다. Primary tasks 3건은 homepage/PDP 컨트롤에서만 가져왔습니다.
- 무출처 cubic-bezier 3개(그중 `ease-exit`는 규격 템플릿과 동일)는 provenance omission ledger로 분리했습니다. duration 이름과 `prefers-reduced-motion`은 Foundations에 남겼고, B3 다섯 증거 종류 전문은 Motion에만 있습니다.
- Add to Bag / Hero CTA 둘 / Retry는 loading·error·success를 역할 매핑이 닫히지 않아 생략했습니다. Product Grid Card / Surface Section / Sale Badge / Lime Promo Banner는 kind/map을 생략했습니다.
- §9-only 제품명 14px / 400은 Product Grid Card에 옮겼습니다.

기계 검사: `--gate-only` PASS, Portable Core `true`. SHA-256 `22f194c6e900f724cd97a97cd77386febe016fbe7137670ef8425ae1cd4dcd7c`. F1·F2는 수행했습니다. F3와 sol 의미 레인은 아직입니다. 카탈로그 채택이 아닙니다.

DONE migrated=1
