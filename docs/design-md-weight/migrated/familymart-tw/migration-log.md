# 全家便利商店 migration log

Source: `web/references/familymart-tw/DESIGN.md`
Destination: `docs/design-md-weight/migrated/familymart-tw/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/familymart-tw/provenance.md`
Date: 2026-08-26
Worker: opus5 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v9**
Gate: `node test-v2/tools/migrate-reference.mjs --brand familymart-tw --gate-only` — result recorded at the end of this file.

## A5 — brand-published strings

Every multi-character 繁體 run in the source survives byte-for-byte in the portable body: `全家便利商店` (10 occurrences on 9 lines — the Scope opening line names the company twice — incl. the H1 `# 全家便利商店 Design System`), `全家就是你家` (2 occurrences on 2 lines), `微軟正黑體` (2 occurrences on 2 lines, inside the computed family stack). The counting basis is **occurrences, not matching lines**: a substring count over the final file returns 10 for `全家便利商店` where `grep -c` returns 9. Both numbers are correct for their own method and neither indicates a lost string.

The source name `全家便利商店` is carried into the H1 and into every portable sentence that names the company; the Latin `Taiwan FamilyMart` never replaces it. `FamilyMart` survives only where the source itself writes the Latin form (`FamilyMart's corporate material`, `a FamilyMart typeface`, `a FamilyMart UI typeface`, `generic FamilyMart-looking UI`, `first-party distributed FamilyMart type asset`), and one added first-mention gloss `全家便利商店 (Taiwan FamilyMart)` sits **beside** the 繁體 name in Scope. The franchise brand idea `全家就是你家` keeps its 繁體 bytes in Content & Locales with an English reading aid placed beside it and labelled as an aid, plus the explicit rule not to substitute the English rendering. The source publishes no other quoted UI label, CTA, or microcopy string — §10 records that the observed public-route voice samples are "not tokenized or generalized as a copy system", so there is no label inventory to move.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity `id` / `name` / `country` / `category` | 분리 → provenance Identity | Portable file has no frontmatter. `name` `全家便利商店` is dual (E2a): the ledger **and** the portable H1 + body, byte-exact (A5). |
| YAML `homepage` `https://www.family.com.tw/Marketing/` | 분리 → provenance Identity | The portable body names the three captured route URLs instead; the bare marketing root is not repeated there. Single destination. |
| YAML `primary_color` `#00b347` | 옮김 → Foundations Brand green + Experience Scope (qualified reading) + Experience Distinctive traits + Components Current-section navigation row background + Components retained `dropbtn-more__btn` text; 분리 → provenance Identity | Grep-verified: 5 hits in the portable body, 5 in the ledger (E2a). The portable body keeps the source limit "not generalized into an unmeasured primary-button system". |
| YAML `logo` (`type: favicon`, Google favicon-service slug) | 분리 → provenance Identity; 경계 문장 옮김 → Typography & Assets Assets | Dual (E2a). The slug value lives only in the ledger; the portable body keeps the boundary — third-party favicon-service URL keyed to `family.com.tw`, identity metadata rather than a captured first-party 全家便利商店 mark. |
| YAML `omd: "0.1"`, `verified: 2026-07-13`, `verification_v2` (`schema`, `checked`, 3 `surfaces`, 6 `sources`, `conflicts: []`, 32 `claims`) | 분리 → provenance Identity / Freshness / Surfaces / Sources / Claim ledger | 출처 원장·freshness·Proof (E1). The single YAML anchor (`surface_id: home`, `source_id: familymart-home-live`, `method: computed-style`, `captured: 2026-07-13`) is preserved as verification metadata for all 32 claims (A1c). The three surface URLs are dual: portable Primary tasks + ledger (E2a). |
| YAML `tokens.source: live-extract`, `tokens.extracted: 2026-07-13`, `components_harvested: true` | 분리 → provenance Identity (세 필드 전부) + Freshness (`tokens.extracted`) + Proof notes (`tokens.source`, `components_harvested`) | Ledger fields kept as values rather than dropped (A1c). Grep-verified: `components_harvested` appears **4 times on 3 ledger lines** (the dual-destination bullet names it twice) and **0×** in the portable body — only its operative meaning ("One component token is published, and that is not a claim that every public component was observed") is in the body, which is the §9 sentence's substance without the field name. |
| YAML `tokens.colors` (6 roles) | 옮김 → Foundations Semantic color | All six hexes at field granularity: `#00b347` `#ffffff` `#212529` `#7a7a7a` `#f2f2f2` `#68b5ac`. `card-title` stays a content-card title color and is not merged into a general accent (A4). |
| YAML `tokens.typography.body` / `navigation` / `card-title` (size, weight, lineHeight, use) | 옮김 → Typography & Assets Type roles; `use` 문자열 원문 분리 → provenance Claim ledger | Dual (E2a). The three `use` strings also survive inside the portable Evidence boundary column ("Observed public-route body and text navigation", "Observed main navigation list-item label", "Observed content-card title"). Unitless line heights `1.5` / `1.5` / `1.2` are preserved **as ratios** in the portable table (`21px (1.5)`, `21px (1.5)`, `19.2px (1.2)`) beside the pixel figures the source records (A1a); grep-verified `1.5` ×3 and `1.2` ×2 in the body. |
| YAML `tokens.spacing` (xs 3, sm 5, md 10, lg 15) | 옮김 → Foundations Spacing + Layout & Platforms; 원문 분리 → provenance Claim ledger | The four role names and their unitless values are carried in Foundations (`xs` 3, `sm` 5, `md` 10, `lg` 15) together with the source's own `3/5/10/15px` notation, and repeated in Layout (E2a). Named as a compact public-route rhythm, not a scale — the source states the packet does not establish a universal grid. |
| YAML `tokens.rounded` (square 0, compact 3) | 옮김 → Foundations Shape + Components radius + Experience Capture-bound application + Layout & Platforms | 0px and 3px. The "sharp-to-compact 0px/3px corner distinction" rule from §7 keeps the pair readable as a distinction, not a scale. Four destinations (E2a) — Layout & Platforms restates the pair as "the zero- and three-pixel corner pair". |
| YAML `tokens.shadow.flat: "none"` | 옮김 → Foundations Elevation | `box-shadow: none` on the supplied component and element samples; the flat token is the only elevation token. |
| YAML `tokens.components.current-section-nav-row` (type, bg, fg, radius, height, font, use) | 옮김 → Components & States Current-section navigation row + Layout & Platforms (`height` 51px); token path + selector 분리 → provenance | Verified `type: listItem` preserved as `Type: listItem`, not flattened into `Kind: interactive` alone (A1b). Selector `home::li.main-menu__menu--active` is dual: portable `Use` line + ledger Capture selectors (E2a). The full token path `tokens.components.current-section-nav-row` lives in the ledger; the portable body names the token as `current-section-nav-row`. The measured `height` 51px is dual (E2a): the component `Height` line and Layout & Platforms ("The current-section navigation row measures 51px high"). |
| *(no legacy field)* — portable `Kind: interactive` on the current-section navigation row | 신규 파생 → Components & States Current-section navigation row | Not a source value, so it needs its own disposition row: the legacy token carries `type: listItem` and no kind field. C4 omission of the kind **and** the applicability map was considered and rejected — the interactive-kind basis taken here is the element’s own role (a main-navigation row that selects a section), not the primitive name, and the source calls it a navigation row without retyping it as a button or tab. Measured precedent inside this migrated set: 23 bodies pair `Kind: interactive` with `Type: listItem`, `bahamut` among them (`Sidenav Active Link` — `Kind: interactive` / `Type: listItem`). Checked and **not** carried as precedent: `catchtable` contains no `listItem` component at all (its `Kind: interactive` entries are `input` and `button`), so it is not cited here. The verified `listItem` primitive is preserved beside the kind, never replaced by it (A1b). |
| §1 Visual Theme & Atmosphere — 1988 first Taipei shop, nationwide network, digital-transformation / OMO framing, scope boundary | 옮김 → Experience `scope` claim | Product/surface scope: three captured public routes; not a checkout, membership account, native app, corporate website, or unobserved mobile breakpoint. The "corporate material does not turn corporate language into UI-token evidence" boundary is kept verbatim in the body. |
| §1 — "that practical 'always nearby' role appears as a white, information-dense utility surface …", "The page does not turn green into a dominant canvas …", "That restrained treatment lets store information … carry the page's changing content while the shell remains familiar." | 옮김 → Experience `scope` claim, under an adjacent complete qualification | These are the source author's evaluative readings of the capture, i.e. derived, not brand-published. The adjacent text reads "derived editorial implementation inference from the verified surfaces; it is not 全家便利商店-authored or a separately published UI specification" (B2/B2a). |
| §1 Key characteristics (5 bullets) | 옮김 → Experience Distinctive traits | Token facts kept whole. Two evaluative halves were restated as observations rather than dropped. "`#7a7a7a` text navigation and `#f2f2f2` utility controls keep the shell low contrast" became "`#7a7a7a` is the measured text-navigation value and `#f2f2f2` the measured utility-control fill"; the low-contrast reading itself survives inside the qualified Scope paragraph ("quiet gray navigation"). "The card-title accent is teal `#68b5ac`, separating content emphasis from the navigation green" became "… measured on `card__title`; the navigation green is a separate measured role"; the emphasis reading survives inside the same qualified paragraph ("teal content-card headings"). No reading is lost and none stands unqualified (B2a). |
| §1 narrative URLs (Company information, Brand vision) | 분리 → provenance Sources / Tier 1 / Narrative documents | 서사 출처 (E1). The body keeps the boundary sentence that these documents supply business and OMO context, not UI tokens. |
| §2 Observed public-route palette (6 roles) | 옮김 → Foundations Semantic color | Each role keeps its observed context, including the `navbar-item--text` and `dropbtn-more__btn` selectors. |
| §2 Other measured siblings (`#007bff` `#444444` `#737373` `#8c8c8c` `#28a745` `#36ad1b`) | 옮김 → Foundations Evidence-domain boundary + Governance Named gaps; 분리 → provenance Proof notes | Three destinations (E2a). All six hexes appear **twice** in the portable body — Foundations Evidence-domain boundary and Governance Named gaps, where they are the unresolved-role group — and once in the ledger, with the source's reason ("kept in the raw proof … role is not resolved at a smaller boundary") intact. |
| §3 Evidence classes (4 paragraphs) | 옮김 → Typography & Assets Font evidence (4 rows) | Live computed surface-use / official product-use / official distributed brand asset / declared-only-and-loaded icon assets preserved as distinct evidence domains. 652 observations, the unresolved-and-low-confidence classification, the missing FontFace / known-system mapping, `Font Awesome 5 Free` / `Font Awesome 5 Brands` / `IAGlyphs` / `Material Icons`, and "no `tokens.typography.family` is emitted" all survive. `微軟正黑體` byte-exact (A5). |
| §3 Observed public-route hierarchy (3 rows) | 옮김 → Typography & Assets Type roles | Sizes, weights, line heights, and the per-row evidence boundaries (`main-menu__menu` row only, `card__title` only). |
| §4 List item — Current-section navigation row | 옮김 → Components & States Current-section navigation row | Anatomy and every measured value; `interactionCount: 0`; the static-snapshot field note, including "not as a button or inferred tab". |
| §4 "Button and input measurements retained outside tokens" | 옮김 → Components & States Measurements retained outside tokens + Layout & Platforms (radius / padding); 분리 → provenance Proof notes | Three destinations (E2a). `dropbtn-more__btn` `#f2f2f2` fill / `#00b347` text / 3px radius / 5px padding / 12px/400 and the 14px input sample are the only place those five values exist (A3) — they are not deleted with the section. The radius and padding are restated once more in Layout & Platforms ("the retained compact utility button carries a 3px radius with 5px padding"). |
| §4 footer **Verified** / Tier 1 (5 URLs) / Tier 2 (2 attempts) / Conflicts unresolved | 분리 → provenance Freshness / Tier 1 / Tier 2 / Conflicts | freshness·원장 (E1). The ledger also records that the franchise URL is a registered source cited inline in §11 but absent from the source's own footer list, and does not add it to that list. |
| §5 Layout Principles (4 bullets) | mixed: b1 옮김 → Foundations Spacing + Layout & Platforms; b2·b3 옮김 → Experience Capture-bound application; b4 옮김 → Layout & Platforms | Counted in the body: `universal grid` ×2 and `1440×900` ×1. b2 (green accent local) and b3 (teal for card titles only) are the source author's prescriptions and sit inside the qualified Capture-bound application group (B2a); b1 and b4 are evidence boundaries and stay unqualified. |
| §6 Depth & Elevation | 옮김 → Foundations Elevation | `box-shadow: none`; the explicit refusal to claim that dialogs, menus, maps, or other unobserved layers are shadowless is kept. |
| §7 Do's (4 items) | 옮김 → Experience Capture-bound application | Derived prescriptions, grouped under one adjacent complete qualification (B2/B2a). |
| §7 Don'ts (4 items) | 옮김 → Experience Avoid | Evidence-boundary prohibitions, kept unqualified as-is (E1). |
| §8 Accessibility Notes (3 bullets) | mixed: b1 옮김 → Experience Capture-bound application; b2·b3 옮김 → Components & States Accessibility boundary | Grep-verified: `class name as proof of keyboard focus` ×1 and `contrast-audit` ×2 in the body. b1 is a prescription and sits in the qualified group; b2 and b3 are boundaries. The unverified keyboard / screen-reader / contrast-audit / error / responsive tests are also enumerated in Named gaps. |
| §9 Implementation Notes — sole-component-token rationale + `components_harvested` meaning | 옮김 → Components & States Capture record | A3: this rationale exists nowhere else in the source. "high-confidence collector `listItem` with a named class, source surface, selector, and default geometry" and "not a claim that every public component was observed" are both in the body (grep-verified). |
| §9 Implementation Notes — "Use the machine-readable token block as the canonical source for the measured public-route values." | 삭제 | Sole deletion. It points the consumer at the YAML frontmatter block, which the portable file does not have. Grep-verified that every value it points at has a destination anyway: colors → Foundations Semantic color, type metrics → Type roles, spacing → Foundations Spacing + Layout, rounded → Foundations Shape, shadow → Foundations Elevation, component fields → Current-section navigation row. `machine-readable` returns 0 hits in `DESIGN.md` and 0 in `provenance.md`; it survives only in this log, quoting the deleted sentence. No value, URL, or 繁體 string is lost with it. |
| §10 Voice & Tone — first-party vision framing | 옮김 → Content & Locales Voice reading | Customer needs, convenience, shared growth, cross-industry convenience-life platform. The Brand vision URL moves to the ledger. |
| §10 Voice & Tone — "supports a practical, nearby, and action-oriented register" + Do/Don't table (3 rows) | 옮김 → Content & Locales Voice reading, under an adjacent complete qualification | The register reading and all three rows are the source author's editorial inference from official language (B2/B2a). The source's own "not a claim about an unpublished editorial style guide" limit is kept beside it. |
| §10 Observed public-route voice samples | 옮김 → Content & Locales Locale | Utility navigation labels, convenience-information route, store-map route; "their exact strings are not tokenized or generalized as a copy system" preserved verbatim. |
| §11 Brand Narrative (3 paragraphs) | 옮김 → Experience `scope` claim; URL 원장 분리 → provenance | 1988 first Taipei store, growth into a network embedded in daily routines, the employees/franchisees/customers/suppliers/cross-industry-partners list, the 2021–2026 digital-transformation label, the customer-needs-led OMO data-driven platform, the ESG direction, `全家就是你家` (also in Content & Locales, E2a), community retail / supply-chain capability / fresh food / varied store formats, and the "does not attribute an unverified executive quotation" limit. Three narrative URLs → ledger Sources / Tier 1 / Narrative documents. Nothing from §11 was dropped. |
| §12 Principles (4 numbered items with UI implications) | 옮김 → Experience Principles, under an adjacent complete qualification | All four kept with their UI implications, using the approved qualification wording (B2a). |
| §13 Personas (4 archetypes) | 옮김 → Experience Audience | Kept with the source's own "archetypes … not research-validated personas or demographic claims" qualification plus an adjacent complete derived-editorial qualification. Not promoted into the `primary-tasks` claim, and **not** re-hosted in the ledger — the ledger states only that they stay in the portable body (D2). No demographic is invented. The brand-published stakeholder list from §11 is what Audience opens with. |
| §14 States (11-row table + framing sentence) | 옮김 → Components & States Capture record (table preserved verbatim) + per-component applicability | 본문 보존 (A2): all 11 rows with their Observed status and Boundary text, plus "the supplied evidence establishes no branded state system" and the not-silently-replaced-with-generic-FamilyMart-looking-UI reason. The declared interactive component closes the Core §4.4 seven-state map by control meaning; non-observation is never a `not-applicable` reason (C1). The three `not-applicable` rows carry role reasons — a current-section marker carries route context, not pending / validation / action-outcome meaning — rather than a blanket "interactive control" grant (C2). No `focus-visible` row carries a treatment value; the source records no `focus-visible` observation (B1). Coverage is explicitly not complete (C3). No graph delegation. |
| §15 Motion & Easing | 옮김 → Foundations Motion | Verbatim: no duration, easing, transition, loading, carousel, or menu-motion value; `interactionCount: 0`; omitted rather than filled with a generic convenience-retail default. The source carries no easing curve, so there is no unsourced curve to remove and no promotion gate to preserve — B3's five-evidence-kind clause is **not** claimed here, because the source states no promotion condition and the portable body therefore contains none (E2c). |
| Legacy H1 "# Design System Inspiration of 全家便利商店" | 삭제 → replaced by the Core H1 | `# 全家便利商店 Design System`. The 繁體 name survives byte-exact (A5). |
| Sibling `web/references/familymart-tw/.verification.md` (not a legacy §, but a source-tree artifact that was previously unrecorded) | 채택 → provenance **Sibling verification document — adopted** + provenance **Tier 2** (dates) | Wave-23 fix. The sibling was neither adopted nor declined in the ledger — silence. It is now adopted at ledger level: method (supplied deterministic collector bundle, no rerun, no MCP), dates (document 2026-07-13; Tier 2 attempts 2026-07-14), sources (7 raw computed-style samples; the same five Tier 1 URLs), and the artifact chain (`packet.md`, `familymart-tw.json` captured `2026-07-13T15:02:43.748Z`). No sibling value is promoted to a portable body token — the three sibling-only facts (`card__text` → `#737373` role, the `NewsContent_en.aspx?ID=2939` narrative URL, the `surface-2`/`surface-3` labels) are recorded with their non-promotion reasons. `DESIGN.md` unchanged by this row. |

## Named gaps check (D1a)

Every noun enumerated in Governance → Named gaps is a domain the source itself names as absent, unobserved, or unresolved. Grep-verified against `web/references/familymart-tw/DESIGN.md`:

| Named gap group | Source line |
|---|---|
| checkout, membership account, native app, corporate website, unobserved mobile breakpoint; authenticated purchase and account behavior | §1 "not a checkout, membership account, native app, corporate website, or unobserved mobile breakpoint"; §7 Don't "Do not infer authenticated purchase, account, or native-app behavior" |
| documented universal CTA fill, unmeasured primary-button system, hover recipe, universal active state | §1 Key characteristics; §2 Brand green; §7 Don't |
| empty, loading, form-error, service-error, success, skeleton, disabled, hover, focus, pressed treatments | §14 table (11 rows) |
| dialog, menu, map, and other unobserved layer elevation | §6 "does not claim that dialogs, menus, maps, or other unobserved layers are shadowless" |
| keyboard, screen-reader, contrast-audit, error, responsive tests | §8 "contains no keyboard, screen-reader, contrast-audit, error, or responsive test" |
| mobile columns, touch targets, carousel behavior, breakpoints | §5 "Do not derive mobile columns, touch targets, carousel behavior, or breakpoints" |
| product or app typeface; first-party distributed FamilyMart type asset | §3 "Official product-use" and "Official distributed brand asset" |
| duration, easing, transition, loading, carousel, menu-motion values | §15 |
| resolved roles of `#007bff` `#444444` `#737373` `#8c8c8c` `#28a745` `#36ad1b` | §2 "their role is not resolved at a smaller boundary" |
| published editorial style guide behind the observed copy | §10 "not a claim about an unpublished editorial style guide" |

No domain the source leaves unmentioned is listed. In particular no storefront, cart, order-tracking, loyalty, payment, or delivery domain is enumerated, because the source never establishes that any such surface exists in this capture.

## Final passes

**F1 (B2a scan).** Every causal, interpretive, or evaluative sentence in the portable body carries an adjacent complete qualification: Scope's interface reading, Audience's four archetypes, Principles (4 items), Capture-bound application (7 items), and Content & Locales' register reading plus its 3 Do/Don't rows. The clause "derived editorial implementation inference from the verified surfaces; it is not 全家便利商店-authored or a separately published UI specification" occurs 5× in the body, once per interpretive group (counted over the final file). Sentences left unqualified are of two kinds only — reports of first-party statements with their attribution intact (§11 narrative, §10 vision framing) and evidence-boundary limits (Avoid, Evidence-domain boundary, Elevation, Motion, Accessibility boundary, Layout).

The scan changed six sentences after the first draft, and this log describes the post-scan file:

| Before | After | Why |
|---|---|---|
| Distinctive traits: "`#7a7a7a` text navigation and `#f2f2f2` utility controls are the measured shell values." | "`#7a7a7a` is the measured text-navigation value and `#f2f2f2` the measured utility-control fill." | "shell" is the source author's framing, not a measured fact. The framing survives in the qualified Scope paragraph. |
| Distinctive traits: "The card-title accent is teal `#68b5ac`, separating content emphasis from the navigation green." | "… measured on `card__title`; the navigation green is a separate measured role." | "separating content emphasis" is an evaluative reading; the two measured roles are the fact. |
| Foundations Ink: "use as the observed functional baseline" | "the observed functional baseline" | Dropped an imperative the source author added on top of the measurement. |
| Type roles: "The line heights are unitless ratios in the source (1.5 and 1.2) …" | "The supplied bundle records the line heights as unitless ratios (1.5 and 1.2) …" | Attributes the ratio form to the evidence bundle and matches the vocabulary the rest of the body uses. |
| Assets: "Store information, service categories, and promotional imagery are the changing content of the captured routes; the supplied evidence attaches no product-asset rule to them." | "The captured routes carry promotional imagery, but the supplied evidence attaches no asset, license, or usage rule to it, and none is created here." | The "changing content" reading is interpretive and belongs only in the qualified Scope paragraph, where it still appears. |
| Layout: "not a grid, gutter, or platform specification" | "rather than a breakpoint specification" | "gutter" and "platform" name domains the source never establishes; "breakpoints" is its own word (D1). The universal-grid limit is already stated verbatim in the preceding sentence and in Foundations Spacing. |

**F2 (E2 contrast).** Every row above was written after a grep against the three output files, not from memory; dual and multiple destinations are recorded on both sides. Note the counting basis, because it is where the two disputed numbers came from: `grep -c -F` returns matching **lines**, `grep -o … | wc -l` and a substring count return **occurrences**. Token-level sweep: all 12 source hexes, all 10 source px values (`14px` `16px` `21px` `19.2px` `0px` `51px` `3px` `5px` `12px` `15px`), both unitless ratios (`1.5`, `1.2`), the `listItem` primitive, all five source class names, `652`, and `1440×900` were located by key in the outputs. Compliance is asserted only where the body actually carries the text — B3 is explicitly **not** claimed (see §15 row).

**F2 re-contrast (F3 audit, 2026-08-26, separate session).** Rows re-grepped against the three output files after the audit pass. The portable `DESIGN.md` was **not** edited by the audit, so no destination moved; five rows were rewritten because their destination lists were narrower than the files, and one row was added for a field that had none. Re-measured destination counts: `tokens.rounded` → four (Foundations Shape, Components radius, Experience Capture-bound application, Layout & Platforms); `tokens.components.current-section-nav-row.height` 51px → dual (Components, Layout & Platforms); §4 button/input geometry → three (Components, Layout & Platforms, ledger Proof notes); the six sibling hexes → three (Foundations Evidence-domain boundary, Governance Named gaps, ledger Proof notes). Re-measured counts: `全家便利商店` 10 occurrences / 9 lines, `全家就是你家` 2 / 2, `微軟正黑體` 2 / 2, `components_harvested` 4 occurrences / 3 ledger lines, 0 in the body, 12 source hexes present in body and ledger, `1.5` ×3 and `1.2` ×2 in the body, `machine-readable` 0 / 0, `universal grid` ×2, `1440×900` ×1, `contrast-audit` ×2. The two counts the audit was asked to settle differed by counting method only, not by value loss (E2c).

## Wave 23 revision (2026-08-26)

Two ledger defects were fixed; `DESIGN.md` was **not** edited (values, component table, state applicability, and section structure are out of scope, and the F3 auditor's 13 fixes were left standing).

| # | Clause | Defect | Fix |
|---|---|---|---|
| E1 | E1 | The provenance derived record was narrower than the body: it described the derivation as the **Audience archetypes**, while the body carries **five** derived groups (`DESIGN.md:15` Scope reading, `:30` Audience, `:49` Principles, `:58` Capture-bound application, `:220` Content voice). A too-narrow scope record forces a reviewer to re-derive it by reading the whole body. | New provenance section **Derived interpretation ledger** — one row per group with size, portable line, legacy origin, and qualification placement — plus a 1:1 count table. The persona bullet in Proof notes now points at it instead of standing as the whole record. |
| E2 | E1/E2 | The sibling `web/references/familymart-tw/.verification.md` exists but the ledger neither adopted it nor recorded a decision not to — silence. | **Adopted.** New provenance section records method, dates, sources, and the artifact chain; the Tier 2 block gains the sibling-supplied `2026-07-14` attempt date the source footer omits. Adoption stops at the ledger: three sibling-only facts are listed with explicit non-promotion reasons. New disposition row above. |

Two further review observations were recorded, not corrected — provenance **Observations recorded, not corrected**: the `DESIGN.md:167` applicability summary that names only `default` and `focus-visible` while the table at `DESIGN.md:188`–`196` also marks `hover` and `disabled` applicable (corpus boilerplate, measured at **97 of 115** migrated bodies; correcting it is a state-applicability edit), and the `全家就是你家` quotation-mark change (source `“…”` U+201C/U+201D → body `「…」` U+300C/U+300D) whose brand string is the identical 18 bytes in both files — an A5 note, not an A5 violation.

### Full pointer re-verification

Every ledger pointer and count claim in this log and in `provenance.md` was re-measured against the files, not carried forward. **Counting method: `grep -o … | wc -l` for occurrences and `grep -n` for line positions. `grep -c` was deliberately not used** — it returns matching *lines*, and on this brand that difference already produced a false discrepancy (`全家便利商店`: 10 occurrences on 9 lines, because the Scope opening line names the company twice). Every number below says what was counted.

| Pointer / claim | What was counted | Measured | Log claim | Status |
|---|---|---|---|---|
| `全家便利商店` in body | occurrences / matching lines | 10 / 9 | 10 on 9 lines | holds |
| `全家就是你家` in body | occurrences / lines | 2 / 2 | 2 / 2 | holds |
| `微軟正黑體` in body | occurrences / lines | 2 / 2 | 2 / 2 | holds |
| `components_harvested` | occurrences in ledger / ledger lines / body occurrences | 4 / 3 / 0 | 4 on 3 lines, 0 in body | holds |
| `#00b347` | occurrences in body / in ledger | 5 / 5 | 5 / 5 | holds |
| six sibling hexes (`#007bff` `#444444` `#737373` `#8c8c8c` `#28a745` `#36ad1b`) | occurrences per hex, body / ledger | 2 / 1 each | twice in body, once in ledger | holds |
| all 12 source hexes | presence in body and ledger | present in both | present | holds |
| all 10 source px values (`14px` `16px` `21px` `19.2px` `0px` `51px` `3px` `5px` `12px` `15px`) | presence in body | all present | all located | holds |
| `1.5` / `1.2` in body | occurrences | 3 / 2 | ×3 / ×2 | holds |
| `machine-readable` | occurrences in body / ledger | 0 / 0 | 0 / 0 | holds |
| `universal grid` / `1440×900` / `contrast-audit` in body | occurrences | 2 / 1 / 2 | ×2 / ×1 / ×2 | holds |
| `class name as proof of keyboard focus` in body | occurrences | 1 | ×1 | holds |
| `listItem` primitive | occurrences in body / ledger | 3 / 3 | preserved | holds |
| five source class names | occurrences in body | `navbar-item--text` 1, `dropbtn-more__btn` 2, `main-menu__menu` 4, `card__title` 2 | present | holds |
| four class names reach **four** portable sections | section of each body hit | Experience (21, 45), Foundations (84, 85), Typography & Assets (134, 135), Components & States (160, 185, 200) | four sections | holds |
| `652` | occurrences in body | 1 | present | holds |
| derived qualification clause | occurrences in body / lines | 5 / 5 at lines 15, 30, 49, 58, 220 | 5×, once per group | holds |
| audit-log line pointers into `DESIGN.md` (15, 30, 43, 45, 49, 58, 65, 71, 83, 127, 137, 142, 167, 205, 220) | line content at each pointer | every pointer still lands on the sentence it names | — | holds, no drift |
| `DESIGN.md` SHA-256 | `shasum -a 256` | `6c66603accbe76dec9fd8c3e0c1823b71284d6b9330163cde7e047f0ffdc2f36` | same value recorded under Gate | holds, body unedited |

No line number was stale: because `DESIGN.md` has been byte-identical since the F1 edits, every `DESIGN.md:N` pointer in `audit-log.md` and in the new provenance sections resolves to the sentence it names. The pointers that moved are inside `provenance.md` itself, which this revision rewrote; nothing in the repository addresses `provenance.md` by line number, and the new sections are addressed by heading.

## Gate

`node test-v2/tools/migrate-reference.mjs --brand familymart-tw --gate-only` → **PASS**, `problems: []`.

Portable Core: `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/familymart-tw/DESIGN.md --check --require-portable-core --json` → exit 0, `status: pass`, `level: portable-core`, `portable_core: true`, `dropped_segments: 0`, `projection_roundtrip_equal: true`.

Current `DESIGN.md` SHA-256 `6c66603accbe76dec9fd8c3e0c1823b71284d6b9330163cde7e047f0ffdc2f36`. All three results above were re-run against this file after the F1 edits listed under Final passes; no earlier draft result is re-asserted (E2c). A separate-session F3 audit (B2a·E1·E2 only) was run on 2026-08-26; it changed this log and `provenance.md` and left `DESIGN.md` byte-identical, so the SHA above still describes the audited file. Its findings are recorded in `audit-log.md`.

`node test-v2/tools/process-leak-check.mjs` → familymart-tw not listed among leaking bodies (E1).

These record command outputs; they are not a catalog-adoption claim (E2c).

### Wave 23 re-run (2026-08-26, after the two ledger fixes)

- `cd test-v2/tools && node migrate-reference.mjs --brand familymart-tw --gate-only` → `verdict: PASS`, `problems: []`.
- `node -e '… inspectDesignMd(…).conformance'` from the repo root → `portable_core: true`, `reasons: []`.
- `shasum -a 256 DESIGN.md` → `6c66603accbe76dec9fd8c3e0c1823b71284d6b9330163cde7e047f0ffdc2f36`, unchanged: the revision touched `provenance.md` and this log only.

These are command outputs. They are **not** cited as evidence of semantic conformance — the gate is mechanical and both wave-23 defects (a derived-scope record narrower than the body, and ledger silence about a present sibling) sat under a green gate. They are also not a catalog-adoption claim (E2c).
