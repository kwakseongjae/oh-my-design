# AmazingTalker Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

AmazingTalker is a Taiwan-founded online language-learning marketplace that connects learners with professional tutors for one-on-one lessons. Treating that product-origin / marketplace-definition sentence as public-history narrative rather than an interface token is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. Catalog homepage identity is `https://www.amazingtalker.com`. Treating the following two URLs as the named evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. This contract covers two named first-party evidence domains from the 2026-06-08 packet: the global English home at `https://en.amazingtalker.com/` (`https://www.amazingtalker.com` issues a 302 to that host) and the Traditional-Chinese home at `https://tw.amazingtalker.com`.

Source token note: primary = live teal CTA fill `#02cab9` (rgb 2,202,185); coral `#ff5f5f` is the secondary energy accent; body text `#484848` across both en. and tw. surfaces. Treating that note as a register split — `#02cab9` is catalog `primary_color` and the harvested hero/section CTA fill, `#ff5f5f` is the secondary accent rather than a second primary, and `#484848` is the repeated body ink rather than heading `#363636` or pure `#000000` — is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

The following evidence-domain sentence is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. Color, type, and component values below stay attached to the surface that established them. An en. value is not a proxy for every language-locale subdomain. A tw. confirmation of the same teal CTA is not a proxy for every Traditional-Chinese inner page. `https://tw.amazingtalker.com/blog` and `https://www.amazingtalker.com/about` are named in the verification packet as brand-owned country sources; they are not token-capture surfaces in this pass.

The next paragraphs’ marketplace-character, one-teal-discipline, dual-script, friendly-weight / typographic-foundation, conversion-first-hero, coral-as-warmth, and two-hue-restraint readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. The source reads AmazingTalker as a Taiwanese language-learning marketplace whose design speaks friendly, approachable confidence rather than institutional gravity. The page opens on a clean white canvas (`#ffffff`) with a single brand signal: a bright aquatic teal (`#02cab9`) that owns every primary call-to-action. The source contrasts that teal with the corporate blue of legacy ed-tech and the playful primary-color chaos of consumer apps, and treats it as one saturated, optimistic teal carried with discipline across en. and tw. surfaces. The atmosphere is read as warm, human, and conversion-focused: large hero imagery of real tutors, generous white CTAs, and a layout that always knows what it wants you to click next.

The typographic foundation is `Roboto` at a friendly weight 600 for the 50px hero headline ("Languages and learning made fun"). On CJK section titles the stack falls back to `Helvetica Neue`, `PingFang`, and `Microsoft JhengHei`. The source treats that dual-script awareness as core to a marketplace serving Taiwan, Korea, Japan, and the broader Asian language market.

The following stripe-apple-analogy / reassuring, teal-competence / coral-warmth / slate-not-pure-black readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. Where the source analogizes Stripe whispering and Apple subtracting, it reads AmazingTalker as reassuring. The secondary accent — a warm coral (`#ff5f5f`) — appears for badges, highlights, and moments of energy. The result is read as a palette that feels like a conversation between calm competence (teal) and human warmth (coral), set on abundant white, with body copy in a soft near-black slate (`#484848`) that never bites the way pure black would.

Treating the following public-history facts as narrative rather than interface tokens is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. The source records that AmazingTalker was born in Taipei and expanded from its Traditional-Chinese home market (`tw.amazingtalker.com`) across Asia and globally (`en.amazingtalker.com` plus dozens of language-locale subdomains), serving learners of English, Spanish, Korean, Japanese, French, Chinese, and more.

The following product-premise / vetted-human-tutor / visual-language-warmth readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. The source records a design-forward premise: language learning should be personal, flexible, and genuinely enjoyable — a far cry from rigid classroom curricula or impersonal app gamification. The marketplace model puts real, vetted human tutors at the center, and the brand's visual language reflects that human warmth.

The following refusal, embrace, and design-forward-startup readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. What the source treats AmazingTalker as rejecting: the cold, institutional aesthetic of legacy education platforms and the over-gamified noise of mass-market language apps. What it treats the brand as embracing: one confident teal that signals trust and calm, a coral accent that keeps the experience warm and human, abundant white space, and real tutor imagery. The design-forward startup ethos is read in the restraint — a tightly held two-hue palette, disciplined typography, and a relentless focus on guiding the learner to their next lesson.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

- Find tutors via the hero primary CTA (`English tutors`, `Match me with tutors`).
- Start picking a teacher on the Traditional-Chinese home (`開始挑選教師吧！`).
- Open Apply To Teach from the header utility.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable user segments (adult language learners, working professionals upskilling, parents arranging lessons for children, and tutors), not individual people. Restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

### Distinctive traits

- Brand teal `#02cab9` as catalog `primary_color` and the harvested hero/section CTA fill
- Teal deep `#02b3a4` as the source-stated pressed / hover-darkened CTA variant
- Coral `#ff5f5f` as the warm secondary accent; coral soft `#ffe5e5` / `rgba(255,95,95,0.1)` as the tinted fill
- White canvas `#ffffff` with surface band `#f3f5f7` and surface-alt `#fbfbfb`
- Body `#484848` as the repeated reading color; heading `#363636`; muted `#767676`; slate `#909399`
- `Roboto` for Latin; `"Helvetica Neue", Helvetica, Arial, "PingFang HK", PingFang-SC-Regular, PingFang, "Hiragino Sans GB", STHeiti, "Microsoft JhengHei", sans-serif` for CJK
- Two harvested teal-button geometries: 5px-radius hero (20px/400) and 3px-radius section CTA (24px/300, `8px 18px`, height ~54px)
- Conservative radii 3px / 5px / 8px plus YAML `full` 9999 (`9999px`)
- Neutral soft shadows `rgba(0,0,0,0.06) 0px 2px 8px` / `rgba(0,0,0,0.10) 0px 4px 16px` / `rgba(0,0,0,0.12) 0px 8px 28px`

Treating `#02cab9` as the single interactive hue rather than a second identity color, treating `#ff5f5f` as a warm secondary / warmth/urgency rather than a second CTA fill, treating `#484848` as warmer-than-black body ink, treating 3px–5px buttons and 8px cards as conservative/approachable geometry, treating the listed radii as conservative, treating the listed shadows as neutral-soft, and treating white / `#f3f5f7` bands as conversion-first rhythm, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

### Principles

These six items are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification.

1. **Human warmth over institutional gravity.** The marketplace is built on real tutors and real connection; the design must feel approachable, never clinical. Soft slate text, warm coral accents, and friendly Roboto all serve this.
2. **One brand color, held with discipline.** Teal `#02cab9` does all the interactive work. The restraint is the brand — a single confident hue reads as more trustworthy than a rainbow.
3. **Dual-script as a first-class concern.** Serving Taiwan, Korea, Japan, and the global market means Latin and CJK must both render beautifully. The font stack is a deliberate design decision.
4. **Conversion is a kindness, not a trick.** Every layout funnels toward the next teal CTA — but the goal is to help a hesitant learner actually start, which the brand frames as a genuine service.
5. **Fun is functional.** "Made fun" is not marketing fluff; it is a design constraint. If a surface feels like a chore, it has failed the brand.
6. **Restraint signals quality.** Two hues, neutral shadows, conservative radii, no custom-font theatrics — the discipline is what makes a design-forward Taiwanese startup read as premium and reliable.

Treating the following as a capture-bound application of source §7 Do’s (and the matching §16 Do’s), live inspect, and the token note, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

- Use teal `#02cab9` for every primary CTA — it is the single brand signal.
- Use coral `#ff5f5f` for warmth, urgency, and highlight moments only.
- Use `#484848` for body text — never pure black.
- Keep button radius at 3px–5px and card radius at 8px.
- Use Roboto for Latin and the PingFang/Helvetica stack for CJK.
- Alternate white and `#f3f5f7` bands to chunk long pages.
- Make every section funnel toward the next teal CTA.
- Frame copy as encouraging and learner-first — "made fun" is a constraint.

### Avoid

The following items copy source §7 / §16 Don’ts. They are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification.

- Don’t introduce a third brand hue competing with teal and coral.
- Don’t use pure black (`#000000`) for body copy — `#484848` is warmer.
- Don’t use large pill radii on primary buttons — stay sharp (3–5px).
- Don’t tint shadows — AmazingTalker uses neutral soft shadows.
- Don’t drop the CJK fallback stack — Traditional-Chinese must render crisply.
- Don’t bury the teal CTA — every section funnels toward it.
- Don’t make coral a second CTA color — it is accent only.
- Don’t write cold institutional ed-tech copy — keep it human and inviting.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. Catalog `primary_color` `#02cab9` is the live teal CTA fill and the harvested hero/section button fill; it is not Teal Deep `#02b3a4` and not Coral `#ff5f5f`. Teal Deep is the source-stated pressed / hover-darkened variant, not a second primary. Coral is the warm energy accent; Coral Soft `#ffe5e5` is the tinted fill (`rgba(255,95,95,0.1)` observed live), not the solid coral badge fill. Heading `#363636` is the YAML heading token (live scan ×193); live H2 "Popular Courses" and "4 reasons to learn on AmazingTalker" compute `#484848`. Body `#484848` is the dominant reading color (live scan ×907) and the Course Card price-label field in the body; live inspect of the price string "₩14,580/lesson" is `#ffffff`. Those two price colors are not merged. Muted `#767676` is secondary text; Slate `#909399` is icon/tertiary gray and the input placeholder. Canvas `#ffffff` is page background and on-primary text; Surface `#f3f5f7` is the alternating band; Surface Alt `#fbfbfb` is the near-white card-separation surface. Hairline `#dcdfe6` is the standard border; Hairline Soft `#ebebeb` is the lightest divider. YAML component fields such as hero CTA `fg` `#ffffff` and Course Card `fg` `#484848` stay on those controls. They are not extra general inks. Live scan `rgb(0,0,0)` ×566 and `rgba(0,0,0,0.87)` ×101 remain unmerged from Body `#484848`.

- **Brand Teal / Primary** (`#02cab9`): catalog `primary_color`. Primary CTA backgrounds ("English tutors", "Match me with tutors", "開始挑選教師吧！"), active nav state, link highlights. YAML `primary`. Live rgb(2, 202, 185).
- **Teal Deep** (`#02b3a4`): Pressed / hover-darkened variant of the primary teal for interactive feedback on CTAs. YAML `primary-deep`.
- **Pure White / Canvas / On Primary** (`#ffffff`): Page background, card surfaces, and the text color sitting on every teal button. YAML `canvas` and `on-primary` share this hex.
- **Coral** (`#ff5f5f`): The warm energy accent (rgb 255,95,95). Badges, urgency tags, highlight numbers, decorative emphasis. YAML `coral`.
- **Coral Soft** (`#ffe5e5`): Tinted coral surface (source: derived from coral at low alpha; `rgba(255,95,95,0.1)` observed live) for highlight-card backgrounds and badge fills. YAML `coral-soft`.
- **Heading** (`#363636`): Darkest text tone (rgb 54,54,54). Strong section titles, emphasis labels. YAML `heading`.
- **Body** (`#484848`): The dominant text color (rgb 72,72,72). Standard reading copy, nav. YAML `body`. Course Card body price label uses this hex at weight 300; that field is not the live inspect `#ffffff` on "₩14,580/lesson".
- **Muted** (`#767676`): Secondary text (rgb 118,118,118). Descriptions, metadata, helper copy. YAML `muted`.
- **Slate** (`#909399`): Icon and tertiary-detail gray (rgb 144,147,153). Inactive icons, subtle markers, input placeholder. YAML `slate`.
- **Surface** (`#f3f5f7`): Light cool-gray section background (rgb 243,245,247) for alternating bands and grouped content. YAML `surface`.
- **Surface Alt** (`#fbfbfb`): Near-white surface for subtle card separation. YAML `surface-alt`.
- **Hairline** (`#dcdfe6`): Standard border / divider color (rgb 220,223,230) for cards and inputs. YAML `hairline`.
- **Hairline Soft** (`#ebebeb`): Lightest divider for low-emphasis separation. YAML `hairline-soft`.

YAML Teal Tint Tag `rgba(2,202,185,0.2)` is a body-only category-marker wash. It is not a YAML color token and is not merged into Brand Teal.

### Spacing

YAML scale: xs 4, sm 8, md 12, base 16, lg 18, xl 24, xxl 36, section 64. Scale named in the body: 4px, 8px, 12px, 16px, 18px, 24px, 36px, 64px.

The following ~8px-base / frequent-18px / generous-64px+ readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. Body layout states a ~8px base unit with frequent 18px (`lg`) rhythm observed in CTA padding (`8px 18px`). Section vertical rhythm: generous 64px+ bands separating content groups.

### Shape

YAML `rounded`: sm 3, md 5, lg 8, full 9999 (`9999px`).

The following local-geometry reading, including the Sharp / Standard / Comfortable / Pill role labels and calling 3px–5px buttons crisp/modern and 8px cards friendly but restrained, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. 3px / 5px / 8px / `9999px` remain local harvested geometry, not a universal radius for every unlisted control. Do not use large pill radii on primary buttons.

- Sharp (3px): Section CTAs, tags
- Standard (5px): Hero buttons, inputs
- Comfortable (8px): Cards, containers
- Pill (`9999px`): Avatars, occasional filter chips only

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, hero overlay text |
| Ambient (Level 1) | `rgba(0,0,0,0.06) 0px 2px 8px` | Subtle card lift, hover hints. YAML `shadow.ambient` |
| Standard (Level 2) | `rgba(0,0,0,0.10) 0px 4px 16px` | Course cards, content panels. YAML `shadow.standard` |
| Elevated (Level 3) | `rgba(0,0,0,0.12) 0px 8px 28px` | Dropdown mega-menus, popovers, modals. YAML `shadow.elevated` |
| Ring (Accessibility) | `2px solid #02cab9` outline | Keyboard focus ring |

YAML `card-course.use` says "ambient shadow, image top + price label". Body Course Card shadow is `rgba(0,0,0,0.10) 0px 4px 16px` (labeled "standard ambient"). Both the YAML use string and the standard token value are kept; they are not collapsed into one shadow.

The following shadow-philosophy and decorative-depth readings are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. AmazingTalker uses honest, neutral soft shadows — no chromatic tinting. Depth is read as gentle and functional: cards lift just enough to read as tappable, dropdowns sit clearly above content, and nothing floats dramatically. This restraint is read as keeping visual energy concentrated in the teal CTAs and coral accents rather than in elevation theatrics. The shadows are warm-neutral black at low alpha. Hero imagery is read as providing natural depth; overlay text sits flat in white. Light-gray surface bands (`#f3f5f7`) are read as creating depth through value contrast rather than shadow. Coral and teal accents are read as providing chromatic energy without relying on elevation.

Named Ring `2px solid #02cab9` is a generic focus/ring appearance. It is not `focus-visible` treatment evidence.

### Motion

Source-stated duration roles. Treating the duration table, easing names, signature motions, and reduced-motion line as source-stated rather than computed CSS, and treating the "signaling tappability without bounce" / "calm, never abrupt" / "immediate, reassuring tactile feedback" / "the marketplace stays fully functional" readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Selection ticks, focus rings |
| `motion-fast` | 150ms | Hover, button press, card lift |
| `motion-standard` | 240ms | Dropdown mega-menu, modal, card expand |
| `motion-slow` | 360ms | Page-level transitions, hero reveals |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — dropdowns, cards, panels |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

Signature motions (source-stated):

1. **Card hover lift.** Course cards lift gently on hover using `motion-fast / ease-standard` with a small shadow increase — signaling tappability without bounce.
2. **Mega-menu reveal.** Tutor-category dropdowns open with `motion-standard / ease-enter`, fading and sliding down a few pixels — calm, never abrupt.
3. **CTA feedback.** Teal CTAs darken toward `#02b3a4` on press with `motion-fast` — immediate, reassuring tactile feedback.
4. **Reduce motion.** Under `prefers-reduced-motion: reduce`, all `motion-*` tokens collapse to `motion-instant`. Hover lifts become instantaneous; the marketplace stays fully functional.

Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including the no-custom-webfont / dual-script-intentional assignment, are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | en. body `font-family: Roboto, sans-serif`. Hero H1 "Languages and learning made fun" 50px / 600 / line-height 65px / `#ffffff` / Roboto. H2 "4 reasons to learn on AmazingTalker" 31px / 500 / 46.5px / `#484848` / Roboto. H2 "Popular Courses" 30px / 500 / 45px / `#484848` / `"Helvetica Neue", ... PingFang ... "Microsoft JhengHei"`. Nav active 16px / 400 / 24px / `#02cab9` / Roboto. Price "₩14,580/lesson" 18px / 300 / 27px. tw. section CTA 24px / 300. |
| FontFaceSet and source corroboration | Source states no custom webfont; Roboto plus the Helvetica/PingFang/Microsoft JhengHei stack. |
| Declared-only | YAML `family.cjk` short string `Helvetica Neue, PingFang, Microsoft JhengHei` versus the body long stack. Both kept. |
| Outside these captures | Other language-locale subdomains beyond en. and tw. home. |

### Family

- **Latin (YAML `family.sans`):** `Roboto`
- **Latin body stack:** `Roboto, sans-serif`
- **CJK (YAML `family.cjk`):** `Helvetica Neue, PingFang, Microsoft JhengHei`
- **CJK body stack:** `"Helvetica Neue", Helvetica, Arial, "PingFang HK", PingFang-SC-Regular, PingFang, "Hiragino Sans GB", STHeiti, "Microsoft JhengHei", sans-serif`

The following font-use boundary is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. Do not present Helvetica Neue, PingFang, Microsoft JhengHei, Arial, Hiragino Sans GB, or STHeiti as Roboto. Do not replace Roboto on Latin UI with the CJK stack. Do not present a system stack as a bespoke AmazingTalker face. The source states there is no custom webfont.

The following type-rule readings (weight as hierarchy not drama; Roboto for trust and legibility; CJK-aware fallback as intentional; generous 1.5 line-height; no letter-spacing tricks) are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. Weight 600 only at the hero, 500 for section titles, 400 for body and CTAs, 300 for price/intro lines. Tracking stays `normal` everywhere in the body; YAML tracking is `0` on the roles that record it. Caption YAML has no tracking field; none is invented.

### Type roles

Verified YAML line-height values are the unitless ratios `1.30` and `1.50`. The following ratio-versus-size-local reading is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. YAML `subhead` `1.30` covers "Hero subtitle, card category heads"; the body table splits Hero Subtitle `1.30` (31.2px) from Category Head (H3) `1.60` (38.4px). Both are kept; they are not collapsed. YAML tracking `0` is the same pair written `normal` in the body.

The following unmerged-ink reading is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. YAML Heading color is `#363636`; live H2 samples paint `#484848`. Hero H1 paints `#ffffff`. YAML `body-lg` use is "Price labels, intro lines" with no color field; Course Card body paints the price label `#484848` weight 300; live inspect paints "₩14,580/lesson" `#ffffff`. Those color assignments stay unmerged.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking | Use (YAML) |
|---|---|---:|---:|---:|---|---|---|
| Hero H1 (YAML `display-hero`) | Roboto | 50px | 600 | 1.30 | 65px at this size | 0 | Hero H1 over imagery, white text, friendly authority |
| Section Title (YAML `section`) | Roboto | 31px | 500 | 1.50 | 46.5px at this size | 0 | Feature section titles, e.g. 4 reasons to learn |
| Section Title CJK (YAML `section-cjk`) | Helvetica/PingFang | 30px | 500 | 1.50 | 45px at this size | 0 | CJK section titles on Helvetica/PingFang stack |
| Hero Subtitle (YAML `subhead`) | Roboto | 24px | 400 | 1.30 | 31.2px at this size | 0 | Hero subtitle, card category heads |
| Category Head (H3) | Roboto | 24px | 400 | (body 1.60; not a YAML lineHeight) | 38.4px at this size | normal | Course-card category headings ("English tutors") |
| Body Large (YAML `body-lg`) | Roboto | 18px | 300 | 1.50 | 27px at this size | 0 | Price labels, intro lines |
| Body (YAML `body`) | Roboto | 16px | 400 | 1.50 | 24px at this size | 0 | Standard reading text, nav links |
| Primary CTA (YAML `button`) | Roboto | 20px | 400 | 1.50 | 30px at this size | 0 | Primary hero CTA label |
| Utility Button (YAML `button-sm`) | Roboto | 14px | 400 | 1.50 | 21px at this size | 0 | Header utility buttons, Sign In |
| Caption (YAML `caption`) | Roboto | 14px | 400 | 1.50 | 1.50 (no extra px) | (YAML field absent) | Small labels, helper text |

### Assets

Treating catalog logo metadata as a Google favicon lookup (`https://www.google.com/s2/favicons?domain=amazingtalker.com&sz=128`), not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

Treating verified tutor/course imagery as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification. Hex values and geometry in the harvested components remain source-stated.

| State | Treatment |
|---|---|
| **Empty (no search results)** | White canvas. Friendly single line in `#484848` at 18px Roboto: "No tutors match these filters yet." One teal CTA: "Adjust filters". Encouraging, never a dead end. |
| **Empty (no bookings yet)** | Slate (`#767676`) line at 16px: "You haven't booked a lesson yet." Teal CTA "Find tutors". Warm, opportunity-framed. |
| **Loading (tutor list)** | Skeleton course cards at final dimensions in `#f3f5f7`. Gentle shimmer. No spinner-only screens. |
| **Loading (in-place filter)** | Subtle teal `#02cab9` progress indicator; previous results stay visible while refining. |
| **Error (search failed)** | Inline message in `#484848` with a coral `#ff5f5f` accent marker + plain-language explanation + "Try again" link. No generic "Something went wrong". |
| **Error (form validation)** | Field-level, coral `#ff5f5f` border + 14px helper text describing exactly what is needed. |
| **Success (lesson booked)** | Brief confirmation with teal `#02cab9` accent: "Lesson booked!" — the warmth and exclamation fit the brand. Booking detail linked immediately below. |
| **Disabled** | Reduced opacity on surface and text together. Teal actions fade to a lower-alpha teal — never switch to gray, to preserve brand read. |
| **Highlight / Promo** | Coral `#ff5f5f` badge or `#ffe5e5` tinted card for time-sensitive offers and popular markers. |

Characterizations in that table such as “Encouraging, never a dead end”, “Warm, opportunity-framed”, “No spinner-only screens”, “No generic Something went wrong”, “the warmth and exclamation fit the brand”, and “never switch to gray, to preserve brand read” are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records a generic Input Focus (`#02cab9` border / ring) and an Elevation Ring (`2px solid #02cab9`). Those appearances are generic Focus/Ring, not `focus-visible` treatment evidence. The `focus-visible` row does not carry a colour.

Course Card keeps Kind: interactive from source §8 full tap targets and §15 card hover lift signaling tappability. Coral Soft Badge has no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Teal Tint Tag and Coral Accent Button have no YAML `type`; none is invented. Teal Tint Tag also has no interactive-kind confirmation, so kind and a map are omitted.

### Primary Hero CTA

- Role: hero primary action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#02cab9`
- Text: `#ffffff`
- Radius: 5px
- Font: YAML `Roboto 20px/400`; body 20px Roboto weight 400
- Use: YAML `hero primary CTA`; body "English tutors", "Match me with tutors"
- Observed: default (live "English tutors" fill `#02cab9`, text `#ffffff`, radius 5px, 20px / 400 / Roboto)
- Field note: `#02cab9` is this control’s fill and catalog `primary_color`. It is not Teal Deep `#02b3a4` and not Coral `#ff5f5f`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the hero primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says teal actions fade to a lower-alpha teal rather than turning gray; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as a hero primary CTA with two labels ("English tutors", "Match me with tutors"); exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Primary Section CTA

- Role: section conversion CTA
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#02cab9`
- Text: `#ffffff`
- Padding: 8px 18px
- Radius: 3px
- Height: ~54px
- Font: YAML `24px/300`; body 24px Roboto weight 300
- Use: YAML `section CTA`; body observed live on tw. as "開始挑選教師吧！"
- Hover (named): darken toward `#02b3a4`
- Observed: default (tw. "開始挑選教師吧！" fill `#02cab9`, text `#ffffff`, radius 3px, padding 8px 18px, 24px / 300, height ~54px)
- Field note: YAML font string is `24px/300` without a family; body records Roboto. Both kept. `#02cab9` is this control’s fill, not a second hero token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the section CTA |
| hover | applicable | Named darken toward `#02b3a4` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A section CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a section conversion CTA; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: hover darken toward `#02b3a4`. That appearance is a named hover, not `focus-visible` evidence.

### Header Utility (Ghost)

- Role: low-emphasis header actions
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#484848` (or `rgba(0,0,0,0.87)` in nav context)
- Border: none
- Font: 14px Roboto weight 400
- Use: YAML `header utility actions (Sign In, Apply To Teach), no border`
- Observed: default only
- Field note: `#484848` and `rgba(0,0,0,0.87)` are both source-stated on this control; they are not merged into Body as a single ink.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header utility |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A header utility can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as Sign In / Apply To Teach together, without a selector, label, or request/outcome behavior for each. Those mixed identities cannot share one map, so the three fields stay omitted at this boundary rather than closed.

### Nav Link

- Role: header nav link
- Kind: interactive
- Type: tab
- Anatomy: label
- Font: YAML `Roboto 16px/400`
- Text (resting): `rgba(0,0,0,0.87)`
- Active/hover (named): teal `#02cab9`
- Use: YAML `nav link`; YAML `active`: `teal #02cab9 on active/hover`
- Observed: live nav active link `color: #02cab9`, 16px / 400 / line-height 24px / Roboto
- Dropdown mega-menus for tutor categories (English / Spanish / Korean / Japanese / French / Chinese Tutors) are named on the header; they are not a harvested component.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the nav link |
| hover | applicable | Named teal `#02cab9` on hover |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav link can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav link selects a destination or mega-menu grouping; the link itself does not enter a loading state |
| error | not-applicable | Tab/link meaning is selected versus resting, not a request or validation failure on the link |
| success | not-applicable | Tab/link meaning is selection, not action-outcome confirmation |

Additional observed named state: static active appearance (`#02cab9`). That appearance is a captured variant, not an observed click transition. Named hover/active teal is not `focus-visible` evidence.

### Course Card

- Role: tutor/course listing card
- Kind: interactive
- Type: card
- Anatomy: image top + category head + price label
- Background: `#ffffff`
- Text: `#484848`
- Radius: 8px
- Shadow: body `rgba(0,0,0,0.10) 0px 4px 16px` (standard); YAML use `ambient shadow, image top + price label`
- Border: optional `1px solid #dcdfe6` hairline
- Category head: 24px Roboto 400 `#363636`
- Price label: 18px weight 300 `#484848` (e.g. "₩14,580/lesson")
- Structure: tutor/course image top, category head, price label
- Use: YAML `ambient shadow, image top + price label`
- Observed: default only
- Source §8 records course cards as full tap targets. Source §15 records card hover lift as signaling tappability.
- Field note: YAML use "ambient shadow" and body standard shadow `rgba(0,0,0,0.10) 0px 4px 16px` are both kept. Course Card `fg` `#484848` is this control’s price-label field. Category head `#363636` is this tile’s heading tuple, not that price field. Live inspect of "₩14,580/lesson" `#ffffff` is not merged into this field. The 24px Roboto 400 `#363636` category-head tuple is this card’s field-role binding from source §9, not a substitute for the Type-role table.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured as the tutor/course listing card |
| hover | applicable | Named card hover lift signaling tappability; extra visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A listing card can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a full tap target listing card; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Coral Soft Badge

- Role: badges, highlights, urgency tags, soft tint background
- Type: badge
- Anatomy: label
- Background: `#ffe5e5`
- Text: `#ff5f5f`
- Font: 14px Roboto 400
- Use: YAML `badges, highlights, urgency tags, soft tint background`
- Observed: default only
- Field note: `#ffe5e5` / `rgba(255,95,95,0.1)` is this badge’s fill. It is not the solid coral button/badge fill `#ff5f5f`. `#ff5f5f` here is this badge’s foreground. The 14px / 400 pair is this badge’s field-role binding from source §9, not a substitute for Caption.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Coral Accent Button

- Role: urgency tags, promotional highlights, energy moments
- Anatomy: label
- Background: `#ff5f5f`
- Text: `#ffffff`
- Radius: 3px–5px
- Font: 14px Roboto 400
- Use: Urgency tags, promotional highlights, energy moments
- YAML `tokens.components` does not record this control; values are body §4 plus the §9-only 14px / 400 pair. Type is not invented.
- Field note: `#ff5f5f` is this control’s fill. It is not Coral Soft `#ffe5e5` and not Brand Teal `#02cab9`. The 14px / 400 pair is this urgency badge’s field-role binding from source §9, not a substitute for Caption.

Kind is omitted. The source lists this under Buttons and also as a Coral Badge (`#ff5f5f` background, white text, ~3px radius). Interactive-kind confirmation for a single §4.4 map is not given, so no map is declared.

### Teal Tint Tag

- Role: on-brand category markers
- Anatomy: label
- Background: `rgba(2,202,185,0.2)`
- Text: teal `#02cab9`
- Use: on-brand category markers
- YAML `tokens.components` does not record this control. Type is not invented.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Form Input

- Role: inputs and forms
- Kind: interactive
- Anatomy: value field
- Border: `1px solid #dcdfe6`
- Radius: 3px–5px
- Text: `#484848`
- Placeholder: `#909399`
- Focus (named): teal `#02cab9` border / ring
- Use: Inputs & Forms
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.
- Field note: Named Focus `#02cab9` is generic Focus, not `focus-visible` treatment.
- `#484848` is this field’s value color; `#909399` is this field’s placeholder, not Body.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named form field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive field; visual treatment omitted |
| disabled | applicable | An input can be unavailable; visual treatment omitted |
| error | applicable | Form field; visual treatment omitted. Source §14 names a field-level coral `#ff5f5f` border + 14px helper; that row is not copied here as a computed paint |

Loading and success applicability are omitted. Source records default geometry plus named Focus `#02cab9`. Exact loading/success mapping for this field is unresolved, so those two fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: Focus `#02cab9` border / ring. That appearance is a captured generic Focus, not an observed `focus-visible` treatment.

### Surface Band

- Role: alternating section bands ("Popular Courses", "4 reasons to learn", "Who can learn with AmazingTalker?")
- Background: `#f3f5f7` or `#fbfbfb`
- Not a YAML `tokens.components` entry. Kind and a state-applicability map are omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 18, xl 24, xxl 36, section 64. Body layout repeats 4px, 8px, 12px, 16px, 18px, 24px, 36px, 64px. Section CTA height ~54px. Hero H1 is 50px. Course cards use 8px radius. Hero overlay text sits on full-bleed imagery.

The “generous and inviting”, “conversion-anchored”, “band rhythm”, “whitespace always funnels toward the next teal CTA”, and “empty space is never decorative — it is directional” readings, the ~8px-base / frequent-18px CTA-padding restatement, and the recorded-layout “visual rhythm” / “Clean horizontal header” characterizations in this section are derived editorial implementation inferences from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. Body layout also states a ~8px base unit and frequent 18px CTA padding (`8px 18px`).

Recorded layout:

- Centered max-width content column with full-bleed hero imagery
- Hero: large background image + white overlay headline/subtitle + teal CTA, left or center aligned
- Course grids: multi-column responsive card grids (English / Spanish / Korean / Japanese tutors)
- Alternating white and `#f3f5f7` section bands for visual rhythm
- Mega-menu navigation for the large tutor-category taxonomy
- Clean horizontal header on white; brand logotype left-aligned; utility actions right ("Sign In", "Apply To Teach")
- Locale awareness: header adapts between en. and tw. subdomains

Source breakpoint table:

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, hero text scales down, stacked cards, hamburger nav |
| Tablet | 640–1024px | 2-column card grids, condensed mega-menu |
| Desktop | 1024–1280px | Full multi-column course grids, full mega-menu nav |
| Large Desktop | >1280px | Centered content with generous side margins |

Collapsing strategy recorded in the source: hero 50px H1 → ~32px on mobile, weight 600 maintained; navigation: horizontal links + mega-menu → hamburger drawer; course grids: 4-column → 2-column → single-column stacked; section CTA: full-width teal button on mobile; surface bands maintain full-width treatment, reduce internal padding.

Image behavior: hero imagery uses responsive cropping, keeping the tutor's face and CTA visible; course-card thumbnails maintain 8px radius across breakpoints; locale-specific imagery may swap between en. and tw. surfaces.

Touch-target record: primary CTAs are large (hero CTA ~54px tall section variant); nav links at 16px with adequate spacing; course cards are full tap targets.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 50px / 32px / 54px / 8px / 16px figures as surface measurements in this packet rather than universal layout tokens, treating the touch-target record (including cards as full tap targets and nav links spaced for touch) as a purpose reading of those measurements rather than a complete target-size specification, and treating hero responsive cropping / tutor-face-and-CTA-visible / locale-specific imagery swap as source-stated image behavior rather than a complete image specification, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-08)

The live strings below are source-stated. Treating the parenthetical characterizations (hero tagline / tutor-finding CTA / Traditional-Chinese conversion CTA / header utilities / section titles / price sample) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

- "Languages and learning made fun" — hero H1. *(verified live 2026-06-08)*
- "English tutors" / "Match me with tutors" — hero primary CTA labels. *(verified live 2026-06-08)*
- "開始挑選教師吧！" — tw. section CTA. *(verified live 2026-06-08)*
- "Sign In" / "Apply To Teach" — header utilities.
- "Popular Courses" / "4 reasons to learn on AmazingTalker" / "Who can learn with AmazingTalker?" — section titles.
- "₩14,580/lesson" — price sample. *(verified live 2026-06-08)*

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not AmazingTalker-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list are a derived editorial implementation inference from the verified surfaces; they are not AmazingTalker-authored or a separately published UI specification. They are not the Observed strings above.

The source describes AmazingTalker's voice as encouraging, plain-spoken, and learner-first — the tone of a friendly guide who genuinely wants you to start learning today. The tagline "Languages and learning made fun" is read as setting the register: warm, optimistic, zero jargon. CTAs are read as direct and inviting ("Find tutors", "Match me with tutors", "Apply To Teach"), and on Traditional-Chinese surfaces they are read as carrying the same warmth ("開始挑選教師吧！" — "Let's start picking your teacher!", complete with an inviting exclamation that the brand allows because the register is genuinely consumer-friendly).

| Context | Tone |
|---|---|
| Hero headlines | Warm, aspirational, accessible. "Languages and learning made fun." |
| CTAs | Direct, inviting imperatives. "Find tutors", "Match me with tutors". |
| Course / tutor descriptions | Concrete, benefit-led, learner-focused. Price and value upfront. |
| Tutor-facing (Apply To Teach) | Encouraging, opportunity-framed. "Apply To Teach". |
| Traditional-Chinese (tw.) | Same warmth, locally natural phrasing, friendly exclamation allowed. |
| Reviews / social proof | Authentic, specific, learner-voiced. |

**Forbidden register** (source): Cold institutional ed-tech jargon ("pedagogical outcomes", "learning solutions"). Hype superlatives stacked on capabilities. Anything that makes language learning feel like an obligation rather than an opportunity. The brand's whole premise is that learning should feel human and fun — the copy never betrays that.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover, pressed, and `focus-visible` visual treatments
- named exceptions remain the Section CTA hover-darken toward `#02b3a4` and the named Nav Link teal `#02cab9` active/hover
- loading, error, and success visual treatments on the harvested buttons and Course Card, and the omitted loading/error/success applicability fields on Primary Hero CTA, Primary Section CTA, Header Utility (Ghost), and Course Card; omitted loading/success on Form Input
- interactive kind and state-applicability map for Coral Soft Badge, Coral Accent Button, Teal Tint Tag, and Surface Band
- motion animation names, transition properties, and any duration beyond the four source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
