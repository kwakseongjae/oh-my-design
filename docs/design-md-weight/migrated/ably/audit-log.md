# Ably F3 audit — B2a / E2 only (post wave5 sol resubmit)

Auditor: grok-4.6 (fresh session, not the migration worker). Rulebook v7 B2·B2a, E1·E2·E2a–c. Token values, component tables, state applicability, and structure were not changed. Team primary/soft/editorial pill/Seller entry loading·error·success omissions, removal of `navigates Team content`, deletion of the Content copy-rule negative claim, `verification_v2.surfaces` dual recording, and `components_harvested` provenance-only recording were left as the worker set them.

Sentence class used: brand-published fact / observed technical / editorial interpretation or causal judgement. Only the third class without an adjacent complete qualifier (`derived editorial implementation inference` / `not ABLY-authored or a separately published UI specification`) was edited in the body.

## B2a — qualifier attached

| Location | What was attached |
|---|---|
| `DESIGN.md` Compact app-entry field note | Complete B2a on `does not carry native purchase meaning`. Story-card field note already had an adjacent complete qualifier for its elevation judgement; this meaning reading did not. State-table role reasons stay unqualified. |

Already-qualified and left as-is: Scope evidence-domain sentences; Scope product-story synthesis; Scope cross-surface-signal; Scope ecosystem/interchangeability; Distinctive narrative pairing and evidence-domain boundary; Principles numbered list plus capture-bound application; Avoid Don'ts; Spacing/Shape local-scale; Elevation not-a-native-commerce-sheet-token (Foundations and story-card); yellow-campaign canonicality; Typography surface-ownership Don't and reading; Assets product-imagery; Layout not-a-cross-viewport; Content voice/copy reading.

Left unqualified (class 1 or 2, or reconstruction/A4/C2/B3 boundary): official product-story and mission attribution; three-surface measurements and capture inventory; yellow campaign “captured as a local promotion”; YAML/live token fields; A4 field-identity notes; B3 five-kind gate; Compact app-entry loading/error/success role reasons; Team/Seller omission sentences.

## E1 — provenance derived range

| Location | What was fixed |
|---|---|
| `provenance.md` Narrative / derived range | Range omitted the compact app-entry field-note meaning reading after it was present (now qualified) in the portable body. Added that site. Outside-range paragraph unchanged. |

## E2 / E2a / E2c — ledger corrected (body not rewritten for these)

| Location | What was fixed |
|---|---|
| `migration-log.md` YAML tokens | Control sizes `62px x 28px` / `160px x 56px` / `312px x 48px` / `105px x 48px` / `114px x 49px` are Components Size **and** Layout, not Components-only. Logged both (E2a). |
| `migration-log.md` §4 Component Stylings | Same size dual; recorded adjacent complete B2a on the compact app-entry field note. |
| `migration-log.md` §5 Layout Principles | Surface-local sizes were logged as Layout only. Grep: Layout + Components Size (E2a). |
| `migration-log.md` F1 | Added the compact app-entry field-note site. Distinguished it from unqualified state-table role reasons. |
| `migration-log.md` F2 | `#4e4e4e` is Foundations A4 note + editorial-pill Text/field note + provenance, not the pill field alone. Control-size dual added. Dropped `Compliance claims match the body after this revision` (E2c). |

B3 five-kind gate remains in Foundations Motion; that compliance claim was kept.

AUDIT_DONE fixes=7
