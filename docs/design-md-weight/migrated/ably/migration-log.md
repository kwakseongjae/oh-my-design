# Ably migration log

Source: `web/references/ably/DESIGN.md`
Destination: `docs/design-md-weight/migrated/ably/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/ably/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd, `portable_core: true`, placeholders 0)

Revision 2026-08-23 (F3 B2a/E2 audit): adjacent complete B2a attached to leftover editorial/causal sentences; log destinations aligned to grep (dual Named gaps, display_name provenance-only); provenance derived range matched to the portable body. Token values, component tables, state applicability, and structure were not changed.

Revision 2026-08-23 (F3 post-resubmit): compact app-entry field-note B2a; YAML control sizes dual Components + Layout; F1/F2/provenance derived range aligned to grep. Token values, component tables, state applicability, and structure were not changed.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, display_name_kr, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage 옮김 → Experience Scope; `primary_color` 옮김 → Foundations Current ABLY coral; logo 경계 문장 옮김 → Typography & Assets | Portable file has no frontmatter. Name kept as H1 `Ably Design System`. YAML `display_name_kr` `Ably (에이블리)` is provenance identity only; Scope uses the body form `ABLY (에이블리)`, not the YAML spelling. Homepage `https://m.a-bly.com` is dual Scope + provenance identity/surfaces/Tier 1 (E2a). Catalog `primary_color` `#ff5160` is dual identity + portable Foundations Current ABLY coral (E2a). Catalog logo type `favicon` / Google s2 `https://www.google.com/s2/favicons?domain=a-bly.com&sz=128` is dual: provenance identity + portable Typography & Assets identity-only boundary (`Catalog logo metadata is a Google s2 favicon proxy`, not a captured first-party mark) (E2a). |
| YAML `ds` (name/url/type/description) | 분리 → provenance; `ds.description` 옮김 → Experience Scope; `ds.url` 옮김 → Experience Scope | 출처 원장. YAML `ds.type: brand` 보존 (A1c). YAML `ds.name` ABLY Team 보존. `ds.description` (`ABLY's official mission, company, product-evolution, and culture surface; consumer app and Seller Square remain separate UI evidence domains.`) is dual Scope + provenance (E2a). `ds.url` `https://ably.team/` is dual Scope ABLY Team URL + provenance identity/surfaces/Tier 1 (E2a). |
| YAML `omd`, `verified`, `verification_v2`, token claims, `tokens.source` / `extracted` | mixed: 원장 분리 → provenance; `verification_v2.surfaces` URL 옮김 → Experience Scope | Dual destination (E2a) for YAML `verification_v2.surfaces` consumer/team/seller URLs (`https://m.a-bly.com/`, `https://ably.team/`, `https://square.a-bly.com/`): portable Scope + provenance Surfaces/Sources/Tier 1. Footer Tier 1 repeats those URLs; that footer row does not replace this source-row. `verification_v2.sources` product-story URL is provenance-only. `tokens.source: reconciled` preserved (A1c). `verified` footer `2026-07-12 (omd:migrate)` is provenance freshness only, not portable (E2). |
| YAML `tokens.components_harvested: true` | 분리 → provenance Proof notes | Provenance-only (E2). Not a portable Scope sentence. |
| YAML `tokens.note` | 옮김 → Experience Scope; 원장 분리 → provenance | Dual destination (E2a): portable Scope keeps `Each evidence domain keeps its own font and component roles; native-app commerce patterns are not inferred from brand or seller surfaces`; provenance keeps the source note. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States; control sizes 옮김 → Layout | 검증된 값만 최소 필드 단위. YAML unitless `lineHeight` 1.25 / 1.33 / 1.27 / 1.4 / 1.5 / 1.6는 비율로 보존하고 본문 표의 20px / 16px / 14px / 64px / 56px / 32px / 24px / 25.6px와 합치지 않음 (A1a). 검증된 primitive type은 컴포넌트별로 보존: button×5 (consumer-open-app / corporate-primary / corporate-soft-action / corporate-pill / seller-primary) + card (corporate-card). `Kind: interactive`로 뭉개지 않음 (A1b). Editorial-pill `fg` `#4e4e4e`는 그 컨트롤 필드이며 일반 Foreground `#1f1f1f`가 아님 (A4). Soft-action `bg` `#fff2ea`는 그 컨트롤 accent surface이며 일반 Canvas `#ffffff`가 아님 (A4). Seller-primary `font` `14px / 400`은 그 엔트리 액션이며 Seller body 16px / 400 / 1.6이 아님 (A4). YAML `consumer-open-app.states` `default baseline captured; no hover or pressed value promoted`는 Compact app-entry Observed에 유지. Capture selector는 원본에 없음 — 발명하지 않음. Spacing `not a universal spacing scale`와 Shape `not a universal radius scale` 판단은 Foundations 각 절 인접 B2a (B2/B2a). YAML component sizes `62px x 28px` / `160px x 56px` / `312px x 48px` / `105px x 48px` / `114px x 49px`는 Components Size + Layout (E2a). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(consumer mobile web / ABLY Team / Seller Square). Official product story와 company mission 문장은 한정 없이. Product-story synthesis (`two connected sides of that next-commerce direction`)는 그 문장 인접 완전 B2a (B2/B2a). 세 표면 관측(Pretendard alias, 11–16px, 28px app-entry, 40–48px Team, Noto Sans Korean)과 capture inventory(Seller Square pill / Team recruiting CTA / compact app-entry; native shopping 부재)는 한정 없이. Evidence-domain sentences (`remain separate UI evidence domains` / `native-app commerce patterns are not inferred`) 인접 B2a. Cross-surface-signal reading (`The stable cross-surface signal is…`) 인접 B2a. Ecosystem/interchangeability 문단 인접 B2a. Key characteristics 관측 4항은 Distinctive; narrative pairing과 strict evidence-domain boundary 3항은 Distinctive 인접 B2a (B2/B2a). `https://m.a-bly.com/` · `https://ably.team/` · `https://square.a-bly.com/`는 provenance와 dual (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color; 생략 제약 옮김 → Governance Named gaps | Dual (E2a): verified hex roles `#ff5160` `#ffffff` `#1f1f1f` `#777777` `#757575` `#5b5b5b` `#dddddd` `#e5e7eb` `#fff2ea`는 Foundations. Older hot-deal / discount / shipping mint / success / error / link / tab / native surface 생략은 Foundations + Named gaps. Yellow Seller Square campaign action capture(local promotion)는 Foundations 관측. Canonicality 판단(`not a canonical ABLY semantic color`)은 인접 완전 B2a 아래 Foundations + Named gaps (E2a, B2/B2a). |
| §3 Typography Rules | 옮김 → Typography & Assets; Unresolved native/campaign type 옮김 → Governance Named gaps | Dual (E2a): 다섯 증거 등급과 `__Pretendard_a4ae19` 85 / Pretendard 117 / Noto Sans Korean 119, YAML unitless line-height는 Typography (A1a). Unresolved native iOS/Android consumer typography and campaign-specific type는 Font evidence + Named gaps. Surface-ownership Don't (`Do not render Noto…`)와 `Surface ownership matters more than visual similarity`는 같은 인접 B2a 아래 (B2/B2a). 시스템/대체 스택을 브랜드 페이스로 표시하지 않음. |
| §3 Do not render Noto / Pretendard swap | 옮김 → Typography Don't + Experience avoid | Dual (E2a): Typography surface-ownership Don't (인접 B2a) + Avoid `Do not show Noto Sans Korean as the consumer product font` / `Do not render Pretendard as Seller Square truth`. 원문 금지. |
| §4 Component Stylings | 옮김 → Components & States; control sizes 옮김 → Layout | Compact app-entry (`Type: button`), Team primary (`Type: button`), Team soft (`Type: button`), editorial pill (`Type: button`), story card (`Type: card`, kind/map 생략 C4), Seller primary (`Type: button`). YAML 기하와 본문 기하를 같은 슬롯에 유지. `Kind: interactive`로 button/card 구분을 뭉개지 않음 (A1b). YAML/본문 control sizes (`62px x 28px`, `160px x 56px`, `312px x 48px`, `105px x 48px`, `114px x 49px`)는 Components Size + Layout (E2a). Compact app-entry field note (`does not carry native purchase meaning`) 인접 완전 B2a (B2/B2a). |
| §4 native consumer inventory 부재 | 옮김 → Components & States capture record; Experience avoid; Governance Named gaps | Triple (E2a), overlapping not identical: capture record (`Native consumer product cards, price stacks, shipping/deal badges, filters, checkout CTAs, bottom tabs, bottom sheets, dialogs, and transactional states`); Avoid (`Do not fabricate native shopping cards, price stacks, checkout actions, bottom navigation, badges, or sheets`); Named gaps (`native shopping cards, price stacks, shipping/deal badges, filters, checkout CTAs, bottom tabs, bottom sheets, and dialogs`). coverage 완료가 아님 (C3). |
| §5 Layout Principles | mixed: 관측 옮김 → Layout & Platforms; 적용 규칙 옮김 → Experience capture-bound; product-imagery 문장 옮김 → Typography & Assets | Dual/triple (E2a): compact mobile-web / Team editorial reflow / Seller desktop hierarchy는 Layout. Surface-local control sizes (`62px x 28px`, `160px x 56px`, `312px x 48px`, `105px x 48px`, `114px x 49px`)는 Layout + Components Size (E2a). Layout `not a cross-viewport specification` 판단은 인접 B2a (B2/B2a). `Keep consumer mobile web compact…` / `Share #ff5160 as identity, not as proof of identical control geometry` 등은 capture-bound (Principles B2a 범위). `Let product imagery and taste categories carry consumer variety; do not fabricate their layout from memory`는 capture-bound + Assets (Assets 문장 인접 B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation; story-card field note | Dual (E2a): Consumer/Seller promoted controls flat and Team `0 4px 48px rgba(0,0,0,0.08)` on 12px story cards는 Foundations Elevation + story-card Shadow. `not a native commerce sheet token` 판단은 Foundations Elevation과 story-card field note 양쪽, 각각 인접 B2a (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. `#FF5160` 표기는 원문 Do 유지. Numbered editorial Principles와 함께 B2a 범위. Governance 통제 문구에 넣지 않음. |
| §7 Don'ts | 옮김 → Experience avoid; 생략 항목 옮김 → Governance Named gaps | Dual (E2a): source Don'ts는 Avoid (인접 B2a: source-stated prohibitions and retained capture-bound judgements는 derived editorial, not ABLY-authored doctrine). Older `#FA2E5F` as current primary는 Avoid + Named gaps. `#FA2E5F` 표기는 원문 Don't 유지. 미관측을 `not-applicable`로 바꾸는 근거가 아님 (C1). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms; unresolved 옮김 → Governance Named gaps | Dual (E2a): Consumer explicitly mobile web; Team reflows editorial stories; Seller desktop-oriented hierarchy는 Layout. Native-app safe areas, bottom navigation, checkout keyboards, product-grid breakpoints unresolved는 Layout + Named gaps. Layout `not a cross-viewport specification` 판단은 인접 B2a. 원본에 없는 최소폭 발명 없음. |
| §9 Agent Prompt Guide | 삭제 | 도구별 복붙 프롬프트. `#ff5160` / white / `#1f1f1f` / Pretendard consumer-Team / Noto Sans Korean Seller / domain-specific geometry / native shopping omit는 이미 Foundations/Typography/Components/Avoid에 있음. §9-only 고유 근거값 없음 (A3). 슬롯 없는 위임 없음. |
| §10 Voice & Tone | 옮김 → Content & Locales (인접 B2a) | 전체 voice/copy 독해 인접 완전 B2a (`derived editorial implementation inference` / `not ABLY-authored or a separately published UI specification`) (B2/B2a, E1). 원문 Avoid invented discounts/conversion/urgency/seller-growth numbers 유지. 합성 보이스 샘플 없음. §14 interaction-state absence를 세 surface copy-rule 부재로 확장하지 않음 — 그 부정 claim은 삭제 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance | Official product story / mission / three-surface 관측은 Scope. Visual-system-mirrors / shared-color-signals-one-company 독해는 Scope 인접 B2a (B2/B2a). Product-story URL `https://ably.team/news/ZS8_IREAACIAr50Y`는 provenance-only. |
| §12 Principles | 옮김 → Experience principles | 네 항목 전체 editorial reconstruction. 인접 본문에 derived editorial implementation inference / not ABLY-authored or a separately published UI specification. Capture-bound application은 같은 한정 범위. Governance 일반 문구는 그 한정의 대체물이 아님 (B2/B2a, E1). |
| §13 Personas | mixed: 세 first-party task context 옮김 → Experience Audience; unnamed project fields 옮김 → Governance Named gaps; 가상 biography 없음·sidecar 재수록 없음 | Dual (E2a): 원본 first-party task contexts는 Audience (`without project-specific names, ages…` — 본문에 `unspecified` 단어는 없음). 같은 필드 목록은 Named gaps. provenance에 가상 세그먼트로 재호스팅하지 않음 (D2). Independently verified Primary tasks 3건은 캡처된 compact mobile-web app-entry, ABLY Team mission/culture/platform reading, Seller Square primary entry (`count=3`). §13의 purchasing은 Audience stakeholder context로만 남김 — native purchase UI가 이 캡처에 없음. |
| §14 States | mixed: full-body 옮김 → Capture record; absent-treatment 목록 옮김 → Named gaps | Full-body preservation (A2) is Capture record only: baseline-only; no reusable consumer hover, focus, pressed, loading, empty, cart, payment, order-success, or error state; those states remain absent; default-only boundaries explicit in machine components. Named gaps keeps the absent-treatment **list** (hover, focus, pressed, loading, empty, cart, payment, order-success, and error visual treatments) — not the full baseline-only sentence (E2). `not captured` / `not named`를 `not-applicable` 사유로 쓰지 않음 (C1). Compact app-entry loading·error·success는 source-stated app-entry/handoff 역할로 not-applicable. Team primary / Team soft / editorial pill / Seller primary는 exact selector/label/behavior 부재로 세 field 생략 — `navigates Team content` 등 unsupported behavior 없음 (C2). Story card는 kind/map 생략 (C4). Named generic focus는 `focus-visible` treatment로 승격하지 않음; focus-visible 행에 hex 없음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion | 무출처 커브 없음(삭제할 값 없음). 원문 `No reusable duration or easing curve is promoted` / native app motion 부재 유지. Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps가 같은 다섯 종류 게이트를 말함. Placeholder wrapper 없음 — omission ledger에 `[FILL IN]` 보관 없음 (E2b). |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts | 분리 → provenance; Tier 1 세 표면 URL 옮김 → Experience Scope | Dual destination (E2a) for `https://m.a-bly.com/` · `https://ably.team/` · `https://square.a-bly.com/`: portable Scope + provenance surfaces/sources/Tier 1. Product-story URL과 Tier 2 getdesign.md/Refero와 `2026-07-12 (omd:migrate)`는 provenance-only. Conflicts: none. |

### F1 / F2 (v7 mandatory final passes)

Superseded by the wave5 sol resubmit scan below. The prior F1 list omitted product-story synthesis and yellow-campaign canonicality; the prior F2 treated the §14 full sentence as dual with Named gaps and omitted `verification_v2.surfaces` / `components_harvested` source-rows.

## Revision 2026-08-23 (wave5 sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-wave5-2026-08-23-sol-sample.md` §2.

| Item | Correction |
|---|---|
| Scope synthesis + yellow canonicality B2/B2a | Adjacent complete B2a on “two connected sides of that next-commerce direction” and on “not a canonical ABLY semantic color”. Provenance derived range lists both. |
| Team/Seller C2 / invented behavior A1 | Team primary, Team soft, editorial pill, and Seller entry omit loading/error/success. `navigates Team content` removed. Compact app-entry keeps source-stated handoff not-applicable. |
| Content D1 | Deleted the negative claim that consumer/Team/Seller Square CTA/error/empty/cart/payment/order-success copy rules are unobserved. |
| `verification_v2.surfaces` E2/E2a | Recorded as Scope + provenance dual. Footer URL repeat is not a substitute. |
| `components_harvested: true` E2 | Provenance Proof notes only; logged as that disposition. |
| §14 full-body vs list E2 | Capture record keeps the full baseline-only sentence. Named gaps keeps the absent-treatment list only. |

### F1 / F2 (resubmit)

- **F1 B2a scan (full DESIGN.md reread after F3 post-resubmit):** Adjacent complete B2a on: Scope evidence-domain sentences; Scope product-story synthesis; Scope cross-surface-signal; Scope ecosystem/interchangeability; Distinctive narrative pairing and evidence-domain boundary; Principles numbered list plus capture-bound application; Avoid Don'ts; Spacing local-scale; Shape local-scale; Elevation not-a-native-commerce-sheet-token; yellow-campaign canonicality; Typography surface-ownership Don't and reading; Assets product-imagery; Compact app-entry field note (native purchase meaning); story-card field note; Layout not-a-cross-viewport; Content voice/copy reading. Official product-story and mission sentences before the synthesis stay unqualified. Yellow campaign “captured as a local promotion” stays observational. Live measurements, YAML fields, A4 notes, B3 gate, Compact app-entry state-table role reasons, and omission sentences (unresolved exact behavior) stay unqualified.
- **F2 E2 grep (value + field/role context):** YAML `verification_v2.surfaces` URLs grep-matched Scope and provenance Surfaces/Sources/Tier 1 (dual). `components_harvested: true` grep-matched provenance Proof notes, not Scope. Full baseline-only sentence grep-matched Capture record only. Named gaps grep-matched the absent-treatment list (`hover, focus, pressed, loading, empty, cart, payment, order-success, and error visual treatments`), not that full sentence. `copy rules are unobserved` grep-matched no portable hit. `navigates Team content` grep-matched no portable hit. Team primary/soft/pill and Seller entry grep-matched omission sentences, not `not-applicable` loading/error/success rows. Compact app-entry grep-matched those three `not-applicable` rows. `#ff5160` grep-matched Foundations Current ABLY coral and provenance identity. Homepage / Team / Seller Square URLs grep-matched Scope and provenance. Google s2 favicon grep-matched Assets and provenance. Token note grep-matched Scope and provenance. `ds.type` `brand` grep-matched provenance only. `(omd:migrate)` grep-matched provenance freshness only. Product-story URL grep-matched provenance only. Unitless `1.25` / `1.33` / `1.27` / `1.4` / `1.5` / `1.6` grep-matched Type roles. `Type: button` ×5; `Type: card` with `Kind: omitted`. `#4e4e4e` grep-matched Foundations A4 note + editorial-pill Text/field note + provenance (not Foreground `#1f1f1f`). YAML control sizes `62px x 28px` / `160px x 56px` / `312px x 48px` / `105px x 48px` / `114px x 49px` grep-matched Components Size + Layout (E2a). B3 five-kind gate grep-matched Foundations Motion. YAML `display_name_kr` grep-matched provenance only. Fictional names/ages absent.
