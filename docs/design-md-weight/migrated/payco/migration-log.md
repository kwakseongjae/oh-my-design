# PAYCO migration log

Source: `web/references/payco/DESIGN.md`
Sibling read (not the migration input): `web/references/payco/.verification.md`
Destination: `docs/design-md-weight/migrated/payco/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/payco/provenance.md`
Date: 2026-09-02
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with ripgrep against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use ripgrep match lists per file, never a remembered count.

Source SHA-256 `1a8e3de65b355c3fcd65133ad795d36bd992c0ca7df454287699dc85bae27b92` (`web/references/payco/DESIGN.md`). Sibling SHA-256 `81c6a10d888b2614210882ddfc48e45ca3f805763a47181fd35b7c41722a8c4d` (`web/references/payco/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# PAYCO Design System` (`DESIGN.md` 1). Identity table `provenance.md` 9–21. YAML homepage `https://www.payco.com` is dual: DESIGN dest 3 at 9/21 (line 9 occ 2 because the CSS bundle URL shares the prefix) + provenance dest 7 at 13/46/47/55/56/60/61 (E2a). CSS bundle URL `https://www.payco.com/share/css/common.css?1778564615926` is dual: DESIGN dest 1 at 9 + provenance dest 3 at 47/56/61 (E2a). `#FF2233` is dual: DESIGN dest 14 at 11/34/49/55/67/78/80/198/211/213/276/316 (line 78 occ 3) + provenance dest 5 at 14/23/40/135 (line 40 occ 2) (E2a). Favicon URL is dual: DESIGN dest 1 at 185 + provenance dest 1 at 16 (E2a). Display `PAYCO` DESIGN dest many + provenance dest 1 at 11. |
| YAML `omd: "0.1"`, `verified`, `tokens.source: prose-derived`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance; `prose-derived` 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): identity rows `provenance.md` 18–21, freshness table 33–36. The source footer's **Verified:** 2026-06-03 is at `provenance.md` 38. Exact `prose-derived` is dual: DESIGN dest 2 at 9 + provenance dest 4 at 19/49/132/141 (E2a). `components_harvested` DESIGN dest 0 / provenance dest 2 at 21/131. `tokens.extracted` / `2026-06-09` DESIGN dest 0 / provenance dest 2 at 20/34. These ledger keys have no other portable slot. |
| YAML `tokens.colors` (14 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 80–93. All fourteen roles kept with names, values, and token-set keys: primary `#FF2233`, primary-legacy `#ff1414`, heading `#2d2d2d`, body `#2a303a`, muted `#666666`, placeholder `#999999`, secondary `#565960`, surface `#f4f6fa`, divider `#ededed`, border `#d4d4d4`, disabled-bg `#dadada`, disabled-text `#aaacae`, canvas `#ffffff`, on-primary `#ffffff`. `tokens.colors.canvas` and `tokens.colors.on-primary` stay two keys that share `#ffffff` (`DESIGN.md` 78/92/93). `tokens.colors.primary` stays off `tokens.colors.primary-legacy` (`DESIGN.md` 78/80/81). `tokens.colors.heading` `#2d2d2d` stays off `tokens.colors.body` `#2a303a` (`DESIGN.md` 82/83). YAML `#666666` / `#999999` sit beside body `#666` / `#999`. `#f4f6fa` dest 1 at 87. `#ededed` dest 1 at 88. |
| YAML `tokens.typography.family.sans` / `mono` | 옮김 → Typography & Assets Family + Font evidence | Family `DESIGN.md` 159–162. `tokens.typography.family.sans` dest 3 at 153/155/159. `NotoSans` DESIGN dest 17. `tokens.typography.family.mono` dest 3 at 153/155/162. `monospace` dest 3 at 153/155/162. `'Pretendard Variable'` is a §3 family, not a YAML family key (`DESIGN.md` 151/160). |
| YAML `tokens.typography.hero / section-sub / nav / subtext / button-lg / body / button-md / input` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 172–179. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.56` dest 1 at 173; `1.33` dest 1 at 175; `1.27` dest 2 at 177. Tracking `-0.56` dest 1 at 172; `-1` as YAML tracking beside §3 −1px at 177. All eight YAML `use` strings restored verbatim in the Token-set use column (A1, wave 37 keep-both): `Hero / section titles` dest 3; `Section subtitles` dest 2; `Navigation links` dest 2; `Hero subtext` dest 2; `Large button label` dest 2; `Standard body text` dest 2; `Medium button label` dest 2; `Input value text` dest 2. Longer §3 selector/color/px spellings sit beside them: `.kv_heading` / `.main_title` / `.kv_text` / `.sub_title` / `.bn_big *` / `.bn_l *`. `tokens.typography.body.size` `16` is disambiguated from spacing and from input height at 168/181. `tokens.typography.nav.size` `24` stays off `tokens.typography.subtext.size` `24` at 181. |
| YAML `tokens.spacing` (7 steps) / `tokens.rounded` (4 steps) / `tokens.shadow.panel` | 옮김 → Foundations spacing + shape + elevation | Unitless spacing: `DESIGN.md` 105–111 (`xs: 10` · `sm: 20` · `md: 25` · `base: 32` · `lg: 48` · `xl: 115` · `section: 159`). Shape: `DESIGN.md` 121–124 (`sm: 8` · `md: 20` · `lg: 100` · `full: 9999`). `tokens.rounded.full` dest 2 at 124/126; exact `tokens.rounded.full: 9999` dest 1 at 126. `tokens.spacing.sm: 20` is disambiguated from input padding-left 20px at 113. `tokens.spacing.md: 25` is disambiguated from service-tab 25px at 113. `tokens.spacing.base: 32` is disambiguated from input height 32px and from hero-subtext 32px at 113. `tokens.spacing.lg: 48` is disambiguated from `.bn_big` height 48px at 113. `tokens.spacing.xl: 115` / `section: 159` are disambiguated from section paddings at 113. `tokens.rounded.sm: 8` is disambiguated from `button-cta-modern.radius: 8px` at 126. YAML `tokens.shadow.panel` `0 1px 0 rgba(0,0,0,0.1)` dest 2 at 38/130; §6 `0 1px 0 rgba(0,0,0,.1)` dest at 130. |
| YAML `tokens.components` (6 records) | 옮김 → Components & States | `DESIGN.md` 207–328. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` on button-primary / button-secondary / button-ghost / button-cta-modern, and on the disabled *recipe*; `input` on input-standard. All six YAML `use` strings restored verbatim as `Token-set use:` lines (217/240/260/282/306/325). YAML `font` shorthands: `18px / 700` dest 6; `14px / 400` dest 1; `12px / 400` dest 1. YAML padding `0 0` dest at 215/219; `16px 10px` dest 2; `0 0 0 20px` dest 3. YAML `button-ghost.fg` `#2d2d2d` sits beside §4 `#191a1c`. YAML `button-disabled` is a disabled treatment recipe, not a fifth map (`DESIGN.md` 319–328). No §4-only component borrowed a `Primitive type`. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 34–39. Source clauses restored: `keeping legibility sharp on white surfaces`; `so the brand red always reads as a call to action`; `nothing extraneous competes with the moment of payment or redemption`. Atmosphere readings carry an adjacent complete qualifier at 11 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 80–97. YAML keys and §2 labels both kept. Characterizing / keep-both / two-key readings covered by the adjacent qualifier at 78 (B2/B2a). Nav-link `#191919` off YAML color keys, and ghost-button §4 `#191a1c` beside YAML `button-ghost.fg` `#2d2d2d`, carry an adjacent complete qualifier at 97 (B2/B2a). `#191919` is dual: DESIGN dest 3 at 97/174 + provenance dest 1 at 151 (E2a). `#191a1c` is dual: DESIGN dest 3 at 97/257 + provenance dest 2 at 135/151 (E2a). Conflict sentence (`#FF2233` vs `#ff1414`) is dual: DESIGN dest at 78 + provenance dest at 40 (E2a). |
| §3 Typography Rules — families, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 151–153; family 159–164; type-role table 172–179. NotoSans KR weights 100–700; `'Pretendard Variable'` 400/500/700; legacy `'Apple SD Gothic Neo', NanumGothic, ng, dotum, Helvetica, sans-serif`. Font-evidence, Family, and Type-roles readings carry adjacent complete qualifiers at 155, 164, 168, and 181 (B2/B2a). The after-table size unmerges (`nav` 24 off `subtext` 24; `button-md` 13 off error body 13px; `input` 12 off inline-error 12px) are named at 181. |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 207–328. §4 body values (borders `#FF2233` / `#4a4f56` / `#bfbfbf` / `#d2d2d2`; Ghost text `#191a1c`; halt_apply 51px / `16px 10px` / 8px) sit beside YAML fields. Footer **Verified** / Tier 1 / Tier 2 / Conflicts at legacy 142–145 are provenance (next row). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** (legacy 142–145) | 분리 → provenance; URL 옮김 → Experience Scope | Freshness table `provenance.md` 33–36; Verified paragraph 38; Tier 1 list 60–61; Tier 2 65–66. Homepage / CSS URLs are dual as listed in the identity row (E2a). Conflicts unresolved restated at `provenance.md` 40 and `DESIGN.md` 78. `398 KB` is dual: DESIGN dest 1 at 9 + provenance dest 2 at 61/114 (E2a). |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 333–343: 1026px `.wrap`; 1600px hero min-width; `.snb_header_wrap` 184px; 159px / 115px section padding; 25px tab spacing; footer 760px / 180px. Covered by the Layout qualifier at 345 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 130: flat surfaces; `.ly_panel_cont` shadow; `.dimmed` `#000` / opacity `.7`; z-index skip 120 / sticky nav 99 / date picker 100 / modal 100+; GNB 4px solid `#ff1414`. The flat-observed-layering reading carries an adjacent qualifier at 130 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 55–60 (six Do rules), under the grouping qualifier at 53. Includes `bn_big` sizing 198×48px. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 66–70, under the qualifier at 64. The five prohibitions are the source's own list. No invented domain is added. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 339–343: max-width 1280px / 1100px; min-width 1600px; `transform: translate(-50%, -50%)`; `fp_nav` absolute-to-fixed at 69px; footer and sidebar widths do not adapt below 1100px; mobile experience served by native apps. Covered by the Layout qualifier at 345. The native-apps clause is the source's own layout sentence, not a new out-of-scope domain (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing conventions prompt; no receiving slot and no delegation. Unique values §9 names were checked against the portable body before deletion (A2, A3): `#f4f4f4` info panels at `DESIGN.md` 78/95; 198×48px dest 3 at 22/59/219; 8px modern CTAs / 20px–100px pill badges/tags / flat 0 at 126; `width 0.5s` at 134; `#666` / `#999` at 84–85; `#fff` / `#f4f6fa` in Semantic color; disabled `#dadada` / `#d2d2d2` / `#aaacae` on the recipe; input 32px / padding-left 20px / 12px already on Standard Input; NotoSans / Pretendard / Apple SD Gothic Neo already in Family. The check is itemised at `provenance.md` 177. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 349–364: adjectives Practical, reassuring, familiar; Do/Don't table; three quoted samples byte-exact including "결제가 필요한 모든 순간, PAYCO 하세요." / "실속있는 포인트, 편리한 결제, 간편한 금융." / "일상의 빈틈을 채우다" and the source's own English glosses. The qualifier at 364 covers reading the table as register guidance rather than a complete microcopy guide (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: 2015 launch; NHN / NHN Entertainment / Hangame / NAVER's early gaming arm; single mobile wallet; mass consumer rather than power users; bank accounts without a new card; OG “실속있는 포인트, 편리한 결제, 간편한 금융, 안전한 전자문서함”; 페이코 라이프 (PAYCO Life); employment (office meal vouchers, campus IDs), commerce, and government-facing document exchange; NHN PAYCO subsidiary; contrast with legacy Korean fintech clutter; closing tagline sentence “일상의 빈틈을 채우다” / always available, never intrusive. Each source paragraph's last sentence is kept as one unit. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 134. `2015` DESIGN dest 2 at 13. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 45–49 under the B2a form at 43: "These 5 items are a derived editorial implementation inference from the verified surfaces; they are not PAYCO-authored or a separately published UI specification." Includes `never depend on hover states for critical information` and `concrete numbers rather than percentages`. No separately published PAYCO UI specification is named, so the close uses the toss-form (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` Derived editorial inventory (26 data rows at 141–166). |
| §13 Personas — 4 illustrative archetypes | 삭제 | The source labels them illustrative archetypes synthesised from stated service areas. Biographies are dropped rather than promoted. No name, age, city, motivation, or affiliation classification is re-hosted (D2, D2a). Portable Audience uses only the source wording mass consumer rather than power users (`DESIGN.md` 28; SRC `mass consumer` 1 / `power users` 1). Disposition at `provenance.md` 176. The three `primary-tasks` at 21–23 come from surfaces/controls the source records; 19 says so and qualifies the step from surface to "primary task". |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source body preserved at `DESIGN.md` 194–201 (A2; the catalog graph is still 0/440, so nothing is delegated). The preservation itself carries an adjacent complete qualifier at 192 (B2/B2a): Empty `#999` / false urgency; Loading spinner 22×22px `.sp_load`; Loading image 30×30px `.img_loading`; Error page-level 24px `#565960` / 13px `#666` / `.error .desc a`; Error inline `#FF2233` `.caution` / `.confirm` 12px; Success implicit via forward navigation; Skeleton none found, spinners serve; Disabled `#dadada` / `#d2d2d2` / `#aaacae` / cursor:default. The applicability rule is at 203, whose qualifier at 203 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Primary Brand Button, Dark Secondary, Ghost, and Modern CTA Link close loading/error/success with a role reason (C2 v10) — public page-level CTA / secondary pairing / destination link (`DESIGN.md` 227–229 / 248–250 / 268–270 / 291–293). Standard Input keeps error applicable as the inline caution field and closes loading/success (`DESIGN.md` 315–317). YAML `button-disabled` gets no map of its own (`DESIGN.md` 319–328) (recipe, not a fifth component). `error | applicable` dest 1 at 316. |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 134–142: `transition: width 0.5s`; easing unspecified (browser default ease); `transform: translate(-50%, -50%)` / `translateY(-50%)` for layout, not animation; `fp_nav` scroll with no explicit scroll-animation timing; duration scale 0.5s; no cubic-bezier custom curves found in the CSS bundle. No unattributed curve to delete. B3 is held: the promotion condition at `DESIGN.md` 142 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). The measured-record reading carries an adjacent complete B2a close at 140. |

## Sibling handling (`web/references/payco/.verification.md`)

The sibling exists — confirmed with `find web/references/payco -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling file and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: homepage HTML 19,911 bytes; CSS 398,170 bytes; `'나눔고딕'` / `'돋움'`; GNB wrap height 110px; `.inp` `line-height: 32px`; `.halt_apply .bottom_area .btn_link` selector; Pretendard CDN `payco-cdn.cdn.toastoven.net/.../Pretendard-Bold.eot`; getdesign.md 12,568 bytes. Recorded at `provenance.md` 114–124.
- `#FF2233` / `--brand-color` / `.bn_big` 198×48px / `.inp` 32px / 20px / `#d4d4d4` / Pretendard Variable / 52px hero / disabled `#dadada` are already in the source body and are corroboration.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. A hand sweep of published labels is mandatory even when the needle set is thin.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published labels and slogans in the source body | 9 distinct | 0 | 0 | 결제가 필요한 모든 순간, PAYCO 하세요. / 실속있는 포인트, 편리한 결제, 간편한 금융. / 실속있는 포인트, 편리한 결제, 간편한 금융, 안전한 전자문서함 / 일상의 빈틈을 채우다 / 페이코 라이프 / PAYCO Life / Fill the gaps in your daily life / Every moment you need to pay, PAYCO it. / Practical points, convenient payment, simple finance. |
| YAML `use` strings | 8+6 distinct | 0 | 0 | All eight type-role Token-set use lines and all six component Token-set use lines restore the YAML `use` byte-exact. |
| Class / custom-property names the source treats as issued selectors | 12 distinct | 0 | 0 | `--brand-color` / `.kv_heading` / `.kv_text` / `.main_title` / `.sub_title` / `.bn_big` / `.bn_l` / `bn_bk` / `bn_gy` / `bn_disabled` / `halt_apply` / `(inp)`. |
| Sibling published strings | 2 sibling-only hangul stack names | 0 from the three-file set | 0 dropped without a ledger | `'나눔고딕'` / `'돋움'` stay in provenance Sibling file (ledger only; source writes NanumGothic / dotum). |

Sub-needle labels confirmed present individually in `DESIGN.md`: 페이코 라이프, 일상의 빈틈을 채우다, 결제가 필요한 모든 순간, 안전한 전자문서함, `--brand-color`, NotoSans, Pretendard Variable, mass consumer, power users.

A5 분모: hand sweep of source published labels 9 extracted / 0 missing; YAML use 14 / 0; class names 12 / 0; latin-copy-audit 0 lost / 1 scanned; gate `copy-loss` compared 5 / candidates 128. A5a was mandatory because `compared` 5 < `candidates` 128.

## Unique-phrase contrast (wave 43)

Expressions pulled from source §1–§15 / YAML (years, proper nouns, quotes, causal clauses, value qualifiers, constraint sentences): 96. Zero-in-output then restored: 3 (`bn_disabled`; `keeping legibility sharp on white surfaces`; `nothing extraneous competes with the moment of payment or redemption`). After restore, `grep -oF` dest ≥ 1 for each of those 96.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand payco --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 5, candidates: 128, detail: "인용 문자열 128개 중 5개만 비교했다 — 나머지는 라틴이라 바늘이 되지 않는다. 라틴 전수 대조는 손으로 하라." }]`. Separately, `scripts/design-md-core.cjs` `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 5 < `candidates` 128.

## Deviations recorded

- `DESIGN.md` is 5,650 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: fourteen color keys, eight type roles, six declared component records, a seven-state applicability matrix on five interactive controls, YAML/§2 hex keep-both, YAML unitless and §3 px line-height keep-both, the full §11 founding-and-mission narrative including each paragraph's last sentence, §14 state body, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- No separately published PAYCO UI specification is named in the source, so every derived-editorial close uses the toss-form `not PAYCO-authored or a separately published UI specification` (rulebook v12 B2a 전제 주석). Measure: `derived editorial implementation inference` DESIGN = `not PAYCO-authored` DESIGN = 26. Provenance derived ledger 26 rows at 141–166 (E1 1:1).
- F1 B2a scan: every causal / interpretive / judgment sentence in the portable body was re-read after drafting. Atmosphere, principle UI implications, applicability verdicts, path-unmerge readings, narrative-as-context classifications, after-list color keep-both (`:97`), after-table type-role unmerges (`:181`), and §14 preservation-while-graph-absent (`:192`) carry an adjacent complete qualifier. Source-authored §10 sample annotations were moved out of the derived class and marked as the source's own. Portable complete forms: 26.
- F2 E2: each log row's destination was grepped in DESIGN.md and/or provenance.md before writing. Dual destinations are both named. "B3 유지" is claimed only because `DESIGN.md` 142 contains the five evidence kinds and the per-component gate in full text.
