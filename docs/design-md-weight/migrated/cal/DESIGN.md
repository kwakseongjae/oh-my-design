# Cal.com Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating this reconstruction as covering the public marketing site on `https://cal.com` (built on Framer) plus the product-UI patterns the source names — booking pages, calendar widgets, event types, and booking confirmation — is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. This contract covers Cal.com as named on `https://cal.com`. Token extraction is prose-derived. The reconstruction is of the public marketing site (built on Framer) and of product-UI patterns the source names — booking pages, calendar widgets, event types, and booking confirmation.

Source token note: primary = prose Charcoal `#242424` (signature near-black CTA/heading); `primary_color` frontmatter `#111827` is a catalog approximation. Brand is intentionally grayscale — no accent color. Treating `#111827` as catalog approximation rather than Charcoal, treating the grayscale assignment as catalog commentary on this reconstruction, and treating “signature near-black CTA/heading” / “no accent color” as that same catalog commentary rather than a separately published palette doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

Live DOM capture on cal.com (playwright) listed Black `#000000` Primary 12px / 8×16 / 39px / 13.92px·500 and Cream `#f3f2ed` Secondary. Treating marketing-site prose tokens, that live-DOM note, product-UI screenshot color, and third-party history as remaining separate evidence domains, and treating the live-DOM Black/Cream pair as not a replacement for Charcoal `#242424` or for Cal Sans 64px / 48px, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. Marketing-site prose tokens, that live-DOM note, product-UI screenshot color, and third-party history remain separate evidence domains. The live-DOM Black/Cream pair is not a replacement for Charcoal `#242424` or for Cal Sans 64px / 48px.

The following atmosphere readings — grayscale-world-boldness-from-black-on-white-rather-than-hue, Uber-minimal-aesthetic-as-inspiration, color-as-foreign-substance / rare-blue-link / green-trust-badge-in-a-black-and-white-photograph, Cal-Sans-as-visual-centerpiece, Cal-Sans-speaks-Inter-explains as an editorial pairing rather than a published type specification, 11-shadow-definitions / ring-borders / diffused-shadows / inset-highlights, and Framer-plus-2px-to-9999px-pill — are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification. The source presents the marketing site as a grayscale world where boldness comes from black text on white space rather than from hue, names Uber’s minimal aesthetic as the inspiration, and reads color as a foreign substance — a rare blue link or a green trust badge in an otherwise black-and-white photograph. Cal Sans, a custom geometric display face designed by Mark Davis, is named as the visual centerpiece: letters “intentionally spaced to be extremely close” at large sizes, 64px and 48px headings at weight 600 with line-height 1.10. Body switches to Inter, quoted as “rock-solid.” The source’s division “Cal Sans speaks, Inter explains” is retained here as that editorial pairing, not as a published type specification. Elevation is described as 11 shadow definitions using ring borders (`0px 0px 0px 1px`), diffused shadows, and inset highlights; the site is built on Framer with a radius scale from 2px to 9999px (pill).

Cal.com was founded **January 1, 2021** (initially as **Calendso**) by **Peer Richelsen** and **Bailey Pumfleet** — the latter recorded as **18 years old at founding** — as the open-source alternative to Calendly. First version launched **April 30, 2021**, and the source records a rise from **#1 Product of the Day → Week → Month on Product Hunt**. **AGPL-licensed + hosted SaaS.** Funding recorded in the source: **Seed $7.4M** (OSS Capital lead + Chad Hurley/YouTube co-founder + angels), **Series A $25M (May 2022)** (Seven Seven Six lead + Obvious Ventures), **$32M total**. Cal.ai + Cal.et AI-driven scheduling shipped post-Series A. Treating those founding facts, ages, Product Hunt rank, license, and funding figures as third-party-corroborated public-history narrative rather than interface tokens, and treating the source’s founding observation (scheduling tools as essential infrastructure while the dominant player was closed-source) as narrative rather than an interface token, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. Those founding facts, ages, Product Hunt rank, license, and funding figures are third-party-corroborated in the source (GetLatka, a brief-history page, Product Hunt Stories, Startup Intros). The original tagline recorded in the source is “the open source Calendly alternative.”

The following refusal readings — lock-in pricing, opaque feature gating, lifestyle marketing imagery — are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification. What the source says Cal.com refuses: lock-in pricing, opaque feature gating, lifestyle marketing imagery.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=1 lang=en -->
### Primary tasks

Treating the verified homepage CTA below as the independently verified Primary task, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Get started from the cal.com homepage (verified homepage CTA 2026-05).
<!-- design-md:claim-end -->

Source §14 empty-state strings stay on the Capture record as state-contract copy. They are not independently verified Primary tasks.

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 named-person entries as fictional archetypes rather than verified biographies, treating those entries as not Audience and not primary tasks, and tying observable work only to the independently verified homepage CTA, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. No individual personas are promoted. Observable work follows the independently verified homepage CTA.

### Distinctive traits

Treating catalog `#111827` as not Charcoal, treating Charcoal as three YAML names (`primary` / `brand` / `foreground`) rather than one ink for every string, treating 7px / 29px / 100px / 1000px as body-only radius names rather than the YAML scale, treating the trailing `0px` on YAML `ring-soft` as unmerged from Level 2, treating Cal Sans display plus Inter body plus Roboto Mono / Matter as the recorded pairing rather than a single family, treating product screenshots plus grayscale trust logos as the visual content rather than illustration, and treating Framer as the recorded build platform rather than a token source, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Charcoal `#242424` as YAML `primary` / `brand` / `foreground`; catalog `primary_color` `#111827` is not that charcoal
- White canvas `#ffffff`; Mid Gray `#898989`; Midnight `#111111`; Light Gray `#f5f5f5`
- Cal Sans display + Inter body + Roboto Mono + Matter Regular (product UI); Cal Sans UI Light/Medium variants
- YAML radius 2 / 4 / 6 / 8 / 12 / 16 / 9999; body also names 7px, 29px, 100px, 1000px
- Multi-layer ring+soft shadows; YAML `ring-soft` ends `0px 4px 8px 0px`, Level 2 body omits the trailing `0px`
- Product screenshots as primary visual content; trust logos grayscale
- Built on Framer

### Principles

These five items (Open by default, Calendar over UI, Embed-first thinking, Monochrome confidence, Section spacing is the visual rhythm), including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification.

1. **Open by default.** Source, roadmap, pricing — all public. *UI implication:* "View source" / "Roadmap" links in footer.
2. **Calendar over UI.** *UI implication:* booking page = mostly calendar widget, minimal nav.
3. **Embed-first thinking.** *UI implication:* design tokens are CSS variables (overridable by host).
4. **Monochrome confidence.** No accent color at all. *UI implication:* don't introduce a brand accent; calendar widget provides the only color.
5. **Section spacing is the visual rhythm.** *UI implication:* never cramp; whitespace is the design.

Capture-bound application: treating this list as a capture-bound application of source §7 Do’s and harvested geometry, including the Cal Sans 24px+ heading rule kept beside the 16px / 12px Cal Sans table rows, Cal Sans headings not treated as the Inter / Matter body face, YAML `subheading` tracking `0.2` as the 20px row rather than a replacement for card-title tracking `0`, Link Blue / Default Link / Focus Ring / screenshot color staying in named roles, multi-layered shadow stack rather than a CSS `border` on harvested cards, `#ffffff` backgrounds in this reconstruction, Inter UI labels at 16px / 600 beside Cal Sans UI Light 300-weight body-intro rows, product screenshots as visual content with no illustrations, and section spacing 80px–96px recorded beside the Don’ts floor of 48px, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Use Cal Sans for headings in the harvested table; do not treat it as the Inter / Matter body face.
- Apply positive letter-spacing (`+0.2px`) when using Cal Sans below 24px in the Do’s; YAML `subheading` tracking `0.2` is that 20px row, not a replacement for card-title tracking `0`.
- Keep the grayscale palette on the marketing reconstruction; Link Blue `#0099ff`, Default Link `#0000ee`, Focus Ring `#3b82f6`, and screenshot color stay in their named roles.
- Use the multi-layered shadow stack for card elevation — ring shadow + diffused shadow + contact shadow — rather than a CSS `border` on those harvested cards.
- Keep backgrounds `#ffffff` in this reconstruction.
- Use Inter for harvested UI labels at 16px / 600; Cal Sans UI Light remains the 300-weight body-intro rows.
- Let product screenshots be the visual content — no illustrations, no decorative graphics in this reconstruction.
- Apply section spacing 80px–96px as recorded; Don’ts also say not to reduce section spacing below 48px.

### Avoid

The following Don’ts include source-stated prohibitions and retained editorial judgements (Cal Sans not for extended reading, Caption Label 12px / Card Title 16px retained beside that Don’t, catalog `#111827` not substituted for Charcoal, live-DOM Cream not substituted for canvas, ring-shadow containment, Cal Sans UI Light negative tracking stays on those light rows, no heavy dark shadows / 5% diffused / 70% contact, typography-plus-product-UI-only, display-600 versus UI-Light-300, premium whitespace / mobile ~48px collapsing still recorded, forbidden-voice last bullet). Those judgements are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification.

- Do not use Cal Sans for body text or text below 16px as a reading face — the Don’t is retained beside Caption Label Cal Sans 12px / 600 and Card Title Cal Sans 16px / 600; those table rows are not deleted.
- Do not add brand colors on the marketing reconstruction — color is reserved for links and UI states in this packet. Do not substitute catalog `#111827` for Charcoal `#242424`. Do not substitute live-DOM Cream `#f3f2ed` for canvas `#ffffff`.
- Do not use CSS borders when the harvested card uses ring-shadow containment.
- Do not apply negative letter-spacing to Cal Sans at small sizes — Do’s ask for `+0.2px` below 24px. Cal Sans UI Light tracking `-0.2px` to `-0.28px` stays on those light rows.
- Do not create heavy, dark shadows — recorded diffused shadow is 5% opacity with sharp contact edges at 70% opacity.
- Do not use illustrations, abstract graphics, or decorative elements — the recorded visual language is typography + product UI.
- Do not mix Cal Sans weights as a universal scale — harvested Cal Sans display rows are weight 600; Cal Sans UI Light rows are weight 300.
- Do not reduce section spacing below 48px in the Don’ts; mobile collapsing still records ~48px.
- Do not use forbidden voice like “Revolutionary scheduling”, “the future of calendars”, or emoji in chrome.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

YAML roles unless marked as body-only.

The following characterizations — warmer-than-pure-black, only-blue-in-the-system, signaling-openness, deliberately-colorless, Uber-mirror, content-carries-color — and the unmerged-role applications (Primary / Brand / Foreground as three names on `#242424`; canvas / on-primary as two names on `#ffffff`; Midnight two YAML names not Charcoal; skeleton `#f5f5f5` as not a general canvas; `#111827` as catalog approximation rather than Charcoal and not a CTA fill; live-DOM `#000000` as not a general CTA fill; Dark Primary alternate `#1e1f23` as not Charcoal; live-DOM Cream as not canvas and not surface-alt; Border Gray as ring-shadow rather than a CSS `border`) are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification.

- **Charcoal / Primary** (`#242424`): YAML `primary`. Heading and button text; signature near-black CTA/heading. Same hex as YAML `brand` and `foreground`; the three role names are not merged into one ink for every string.
- **Brand** (`#242424`): YAML `brand`. Same hex as Primary; not a second charcoal.
- **Foreground** (`#242424`): YAML `foreground`. Same hex as Primary; not a second charcoal.
- **Catalog primary_color** (`#111827`): frontmatter approximation. Not Charcoal and not a CTA fill.
- **Midnight / Primary Hover / Deep Text** (`#111111`): YAML `primary-hover` and `deep-text`. Deepest text/overlay; body also uses it at 50% opacity for subtle overlays. Same hex, two YAML names; not Charcoal.
- **White / Canvas / On-primary** (`#ffffff`): YAML `canvas` and `on-primary`. Page background, card surfaces, and text on Charcoal fills. Same hex; the roles are not merged into one ink for every string.
- **Mid Gray / Muted** (`#898989`): YAML `muted`. Secondary text, descriptions, muted labels.
- **Light Gray / Surface-alt** (`#f5f5f5`): YAML `surface-alt`. Subtle section differentiation. Also the §14 skeleton / calendar-loading block color; that skeleton field is not a general canvas.
- **Link Blue** (`#0099ff`): YAML `link`. In-text links with underline decoration.
- **Default Link** (`#0000ee`): YAML `link-default`. Browser-default link color on some elements — unmodified.
- **Pure Black** (`#000000`): YAML `black`. Certain link text elements. Also the live-DOM Primary note; that live-DOM use is not YAML `black` as a general CTA fill.
- **Focus Ring** (`#3b82f6` at 50% opacity): YAML `focus-ring`. Keyboard focus indicator — accessibility-only in the source. This is not a `focus-visible` treatment on harvested components.
- **Dark Primary alternate** (`#1e1f23`): body-only Dark Primary background alternative. Not Charcoal `#242424`.
- **Live-DOM Cream** (`#f3f2ed`): footer Secondary. Not canvas `#ffffff` and not surface-alt `#f5f5f5`.
- **Border Gray** (approx `rgba(34, 42, 53, 0.08–0.10)`): body-only. Shadow-based borders using ring shadows instead of CSS borders.

Source §2 also records: Cal.com is deliberately colorless for brand elements — “a grayscale brand to emphasise on boldness and professionalism”; product UI screenshots show color (blues, greens in the scheduling interface), but the marketing site itself stays monochrome; no gradients on the marketing site.

### Spacing

YAML scale: xs 4, sm 8, md 12, base 16, lg 20, xl 24, xxl 28, section 80, section-lg 96.

Body layout states an 8px base unit and scale 1px, 2px, 3px, 4px, 6px, 8px, 12px, 16px, 20px, 24px, 28px, 80px, 96px. Treating both scales as kept, treating 1px / 2px / 3px / 6px as not the YAML `xs` 4 value, and treating the 28px-to-80px jump as a source-stated section-level spacing tier rather than a missing YAML step, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. Both are kept; 1px / 2px / 3px / 6px are not the YAML `xs` 4 value. Section padding 80px–96px vertical. Card padding 12px–24px internal. Component gaps 4px–8px. The 28px-to-80px jump is source-stated. The source calls the 28px-to-80px jump a deliberate gap emphasizing the section-level spacing tier.

### Shape

YAML `rounded`: xs 2, sm 4, base 6, md 8, lg 12, xl 16, full 9999.

Body layout also names 2px, 4px, 6px–7px, 8px, 12px, 16px, 29px, 100px, 1000px, and 9999px. Treating YAML `full` 9999 as unmerged from body 100px / 1000px, and treating 6px Compact-button corners, 8px Dark-Primary corners, and 9999px pill corners as local geometry rather than a universal radius for every surface, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. YAML `full` 9999 and body 100px / 1000px are not merged. 6px Compact-button corners, 8px Dark-Primary corners, and 9999px pill corners are local geometry, not a universal radius for every surface.

### Elevation

- **Level 0 (Flat):** no shadow — page canvas, basic text containers
- **Level 1 (Inset):** YAML `inset` / `rgba(0,0,0,0.16) 0px 1px 1.9px 0px inset` — pressed/recessed elements, input wells
- **Level 2 (Ring + Soft, YAML `ring-soft`):** `rgba(19,19,22,0.7) 0px 1px 5px -4px, rgba(34,42,53,0.08) 0px 0px 0px 1px, rgba(34,42,53,0.05) 0px 4px 8px 0px` — Ghost button and YAML card. Trailing `0px` is part of this string.
- **Level 2 body (Ring + Soft):** `rgba(19,19,22,0.7) 0px 1px 5px -4px, rgba(34,42,53,0.08) 0px 0px 0px 1px, rgba(34,42,53,0.05) 0px 4px 8px` — cards, containers. No trailing `0px`; not merged with YAML `ring-soft`.
- **Level 3 (Ring + Soft Alt):** `rgba(36,36,36,0.7) 0px 1px 5px -4px, rgba(36,36,36,0.05) 0px 4px 8px` — alt card elevation without ring border
- **Level 4 (Inset Highlight):** YAML `inset-highlight` `rgba(255,255,255,0.15) 0px 2px 0px inset` or body `rgb(255,255,255) 0px 2px 0px inset` — button inner highlight. Both strings kept.
- **Level 5 (Soft Only):** YAML `soft` / `rgba(34,42,53,0.05) 0px 4px 8px` — subtle ambient shadow

The following elevation readings — shadow-first rather than border-first, surfaces that feel like physical cards sitting on a table, unnamed-members-stay-unnamed, no-gradients-or-glow, listed-depth-from-the-shadow-compositing-rows, YAML `ring-soft` trailing `0px` unmerged from Level 2 body, and inset-highlight `rgba` and `rgb` both kept — are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification. The source names 11 shadow definitions; only the rows above are listed. The unnamed members stay unnamed. Ring borders `0px 0px 0px 1px`. Diffused soft shadows `0px 4px 8px` at 5% opacity. Sharp contact shadows `0px 1px 5px -4px` at 70% opacity. The source reads this as a shadow-first approach rather than border-first, and as surfaces that feel like physical cards sitting on a table. No gradients or glow effects. All listed depth comes from the shadow compositing rows above.

### Motion

Source-stated duration roles:

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Slot select |
| `motion-fast` | 150ms | Hover |
| `motion-standard` | 250ms | Modal, panel |

Source-stated easing: “Standard cubic-bezier; no bounce.” Treating exact cubic-bezier values as unattributed and omitted, while retaining the “Standard cubic-bezier; no bounce” name, the three duration tokens, and `prefers-reduced-motion: reduce` (calendar grid fade-in), is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. Exact cubic-bezier values are unattributed and omitted. `prefers-reduced-motion: reduce` removes calendar grid fade-in.

Do not promote an easing curve, animation name, transition property, or a duration beyond the table above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application — live-DOM measurements as not a Cal Sans 64px family proof, source-stated family names and Google Fonts / GitHub distribution as recorded rather than a loaded-stack rewrite, Inter Placeholder as a declared fallback rather than Inter, and a system fallback stack as not presentable as Cal Sans — is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Footer live DOM on cal.com: Black `#000000` Primary 12px / 8×16 / 39px / 13.92px·500. That note does not name a FontFace family. It is not Cal Sans 64px / 600. |
| Source reconstruction | The source names Cal Sans (display; custom geometric sans-serif by Mark Davis), Inter (body), Cal Sans UI Variable Light / Cal Sans UI Medium, Roboto Mono, and Matter Regular / Matter SemiBold / Matter Medium. |
| Official distributed asset | Cal Sans is source-stated as open-source, available on Google Fonts and GitHub. |
| Declared-only | Inter fallback `Inter Placeholder`. |
| Character variants | Cal Sans is source-stated to have 6 character variants (Cc, j, t, u, 0, 1). |

### Family

- **Display (YAML `sans`):** `Cal Sans`
- **Body (YAML `body`):** `Inter`, declared fallback `Inter Placeholder`
- **Mono (YAML `mono`):** `Roboto Mono`
- **UI Light:** `Cal Sans UI Variable Light` (weight 300)
- **UI Medium:** `Cal Sans UI Medium` (weight 500)
- **Tertiary:** `Matter Regular` / `Matter SemiBold` / `Matter Medium`
- Do not replace Cal Sans with Inter or with a system stack. Do not present `Inter Placeholder` as Inter.

The do-not-replace / do-not-present-placeholder-as-Inter applications are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification.

### Type roles

Verified line-height values are the unitless YAML ratios `1.10`, `1.30`, `1.20`, `1.50`, `1.40`, `1.00`, and `1.14`. Treating those unitless YAML ratios as scaling with font size rather than fixed px, treating Caption Light 1.40–1.50 as an additional observation rather than a replacement for YAML `1.40`, and treating YAML tracking `0.2` / `-0.2` as unmerged from body-table `+0.2px` / `-0.2px` / `-0.2 to -0.28px`, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. They scale with font size and are not fixed px. The legacy body table also recorded 1.40–1.50 on Caption Light. That range is an additional observation, not a replacement for YAML `1.40`. YAML tracking `0.2` (subheading) and `-0.2` (body-light / body-light-std / caption-light) are kept beside body-table `+0.2px` / `-0.2px` / `-0.2 to -0.28px`. YAML `use` strings are restored.

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking | Use |
|---|---|---:|---:|---:|---|---|---|
| Display Hero | Cal Sans | 64px | 600 | 1.10 | 0px | YAML 0 | Cal Sans hero, maximum impact, tight default spacing |
| Section Heading | Cal Sans | 48px | 600 | 1.10 | 0px | YAML 0 | Cal Sans large section titles |
| Feature Heading | Cal Sans | 24px | 600 | 1.30 | 0px | YAML 0 | Cal Sans feature block headlines |
| Sub-heading | Cal Sans | 20px | 600 | 1.20 | +0.2px | YAML 0.2 | Cal Sans sub-head, positive spacing at smaller size |
| Sub-heading Alt | Cal Sans | 20px | 600 | 1.50 | 0px | YAML 0 | Cal Sans relaxed line-height variant |
| Card Title | Cal Sans | 16px | 600 | 1.10 | 0px | YAML 0 | Cal Sans smallest usage, card titles |
| Caption Label | Cal Sans | 12px | 600 | 1.50 | 0px | YAML 0 | Cal Sans small labels |
| Body Light | Cal Sans UI Light | 18px | 300 | 1.30 | -0.2px | YAML -0.2 | Cal Sans UI Light body intro text |
| Body Light Standard | Cal Sans UI Light | 16px | 300 | 1.50 | -0.2px | YAML -0.2 | Cal Sans UI Light body text |
| Caption Light | Cal Sans UI Light | 14px | 300 | 1.40 | 1.40–1.50 / -0.2 to -0.28px | YAML -0.2 | Cal Sans UI Light captions and descriptions |
| UI Label | Inter | 16px | 600 | 1.00 | 0px | YAML 0 | Inter UI buttons and nav labels |
| Caption Inter | Inter | 14px | 500 | 1.14 | 0px | YAML 0 | Inter small UI text |
| Micro | Inter | 12px | 500 | 1.00 | 0px | YAML 0 | Inter smallest text |
| Code | Roboto Mono | 14px | 600 | 1.00 | 0px | YAML 0 | Roboto Mono code snippets, technical text |
| Body Matter | Matter Regular | 14px | 400 | 1.14 | 0px | YAML 0 | Matter Regular alternate body text (product UI) |

The following type-rule readings — Cal Sans at large / Inter at small; tight by default, space when small; weight 300 body variant; weight 600 dominance; negative tracking on light text; Do’s 24px+ beside Card Title 16px and Caption Label 12px; live-DOM 12px / 13.92px·500 / 39px as not a sixteenth type role; YAML nav use remaining “Sticky top nav, Cal Sans links, dark primary CTA” rather than the §9 sticky-nav Inter 14px / 500 local recipe — are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification. Source principles: Cal Sans at large, Inter at small; tight by default, space when small; weight 300 body variant; weight 600 dominance; negative tracking on light text. Do’s say Cal Sans exclusively for headings 24px+; the table still has Card Title 16px and Caption Label 12px. Live-DOM 12px / 13.92px·500 / 39px is not a sixteenth type role. YAML nav use remains “Sticky top nav, Cal Sans links, dark primary CTA.” Source §9 local recipes (white hero 64px Cal Sans; scheduling card shadow stack + 12px; sticky nav Inter 14px / 500; trust bar 16px gap; feature section 48px + Inter 16px / 300) sit in Components as local recipes, not as harvested type rows and not as omitted prompt-only figures.

### Assets

Treating catalog logo metadata as Simple Icons identity (`caldotcom`) rather than a captured product mark file is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. Catalog logo metadata is Simple Icons identity (`caldotcom`).

Product screenshots: large scheduling UI screenshots — the product is the primary visual. Trust logos: grayscale company logos in a horizontal trust bar. Aspect ratios: wide landscape for product UI screenshots. Source: no decorative imagery — no illustrations, photos, or abstract graphics.

The “product is the primary visual / no decorative imagery” sentence is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Treating the source state contract as preserved here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted:

| State | Treatment |
|---|---|
| **Empty (no bookings)** | "Your bookings will appear here." Single line, centered, no illustration |
| **Empty (no event types)** | CTA "Create your first event type" + 3 starter templates |
| **Loading (calendar fetching)** | Skeleton calendar grid `#f5f5f5` blocks |
| **Loading (booking submitting)** | CTA inline spinner, button label "Booking..." |
| **Error (no available times)** | "No available times in next 30 days." |
| **Error (timezone)** | Banner top of page. "Times shown in your timezone (Asia/Seoul)" |
| **Success (booked)** | Full-page confirmation with calendar invite preview |
| **Success (rescheduled)** | Same pattern, copy adjusts |
| **Skeleton** | Vertical rows of `#f5f5f5` rectangles, exact dimensions |
| **Disabled (slot)** | 0.3 opacity, cursor-not-allowed, tooltip "Already booked" |
| **Loading (calendar sync)** | Inline chip in nav header |

Those rows describe named product patterns (bookings, event types, calendar fetching, booking submit, timezone banner, confirmation, slot availability, calendar sync). Treating those named product patterns as not visual treatments of the harvested marketing buttons, inputs, cards, badges, or top nav, treating `#f5f5f5` as staying on the calendar-loading / skeleton rows, treating 0.3 opacity as staying on the Disabled (slot) row, treating “Booking...” as staying on booking submit, and treating live-DOM Black/Cream notes as named observations rather than harvested components, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. They are not visual treatments of the harvested marketing buttons, inputs, cards, badges, or top nav. `#f5f5f5` stays on the calendar-loading / skeleton rows; 0.3 opacity stays on the Disabled (slot) row; “Booking...” stays on booking submit. Live-DOM notes (Black `#000000` Primary 12px / 8×16 / 39px / 13.92px·500; Cream `#f3f2ed`) remain named observations, not harvested components.

Default Badge is marked in the source as inferred from the §1–§2 baseline (no explicit DS variant). It is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. That complete limiter is restated on that component.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records `--framer-focus-outline` on the input token and a YAML focus-ring color at 50% as a keyboard focus indicator; neither is a `focus-visible` treatment on harvested components. `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed. This is not a complete state-coverage claim.

### Dark Primary Button

- Role: signature CTA — maximally dark on white
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#242424` (or `#1e1f23`)
- Text: `#ffffff`
- Radius: 8px
- Padding: 12px 20px
- Font: 16px / 600 / Inter (YAML `16px / 600`)
- Hover: opacity 0.7
- Use: Signature CTA — maximally dark on white
- Observed: default; hover opacity 0.7
- Field note: Treating `#1e1f23` as this control’s alternate fill rather than Charcoal `#242424` as a single value is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. `#1e1f23` is this control’s alternate fill, not Charcoal `#242424` as a single value.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the signature marketing CTA |
| hover | applicable | Pointer-web button; harvested treatment opacity 0.7 |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A marketing CTA can be unavailable; visual treatment omitted (slot 0.3 opacity is not this button) |

Loading, error, and success applicability are omitted. Source names this control as a signature CTA on the marketing site; “Booking...” / full-page confirmation belong to the §14 booking-submit / success rows. Exact selector/label/request/outcome mapping to those product rows is unresolved, so those three fields stay omitted at this boundary rather than closed.

### White / Ghost Button

- Role: secondary CTA with shadow-ring border (no CSS border)
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#242424`
- Radius: 8px
- Padding: 12px 20px
- Shadow: `rgba(19,19,22,0.7) 0px 1px 5px -4px, rgba(34,42,53,0.08) 0px 0px 0px 1px, rgba(34,42,53,0.05) 0px 4px 8px 0px`
- Font: 16px / 600 / Inter (YAML `16px / 600`)
- Use: Secondary CTA with shadow-ring border (no CSS border)
- Observed: default only
- Field note: Treating the ring-shadow as this control’s containment rather than a CSS `border` and rather than Level 2 body (no trailing `0px`) is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. The ring-shadow is this control’s containment, not a CSS `border` and not Level 2 body (no trailing `0px`).

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the secondary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control only as a secondary CTA; exact selector/label/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Pill Button

- Role: rounded pill-shaped actions
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#242424`
- Text: `#ffffff`
- Radius: 9999px
- Padding: 8px 16px
- Use: Rounded pill-shaped actions and badges (YAML: “Rounded pill-shaped actions”)
- Observed: default only
- Field note: 9999px is this control’s local geometry, not the 8px Dark Primary radius. That local-geometry reading is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as a pill-shaped action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A pill action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control only as rounded pill-shaped actions; exact mapping to booking submit is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Compact Button

- Role: utility actions within product UI
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#242424`
- Text: `#ffffff`
- Radius: 6px
- Padding: 4px 8px
- Font: 14px / 600 / Inter
- Use: Utility actions within product UI
- Observed: default only
- Field note: 6px radius and 14px Inter are this control’s local fields, not Dark Primary 8px / 16px. That local-geometry reading is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

This Compact Button is body §4 only (not YAML `tokens.components`).

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as a product-UI utility action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A utility action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control only as utility actions within product UI; exact selector/label/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Inset Highlight Button

- Role: subtle inner-top highlight creating a 3D pressed effect
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#242424`
- Text: `#ffffff`
- Radius: 8px
- Shadow: `rgba(255, 255, 255, 0.15) 0px 2px 0px inset`
- Use: Subtle inner-top highlight creating a 3D pressed effect
- Observed: default only

This Inset Highlight Button is body §4 only (not YAML `tokens.components`). The Role/Use “3D pressed effect” reading is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the inset-highlight button |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | This button can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control only by its inset highlight; exact mapping to booking submit is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Select / Dropdown

- Role: form select / dropdown (YAML `input` use: “Text input / select”)
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#000000`
- Border: 1px solid `rgb(118,118,118)` (body also writes `rgb(118, 118, 118)`)
- Radius: 8px
- Padding: 8px 12px
- Focus: `--framer-focus-outline`
- Use: Form select / dropdown
- Observed: default; named focus `--framer-focus-outline`
- Field note: Treating `#000000` as this control’s renderable text rather than Charcoal `#242424` is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. `#000000` is this control’s renderable text, not Charcoal `#242424`.
- Field note: Treating `--framer-focus-outline` as this token’s named focus rather than a `focus-visible` treatment is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. `--framer-focus-outline` is this token’s named focus, not a `focus-visible` treatment.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Recorded as the form select / dropdown |
| hover | applicable | Pointer-web select; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted (`--framer-focus-outline` stays on the Focus field) |
| disabled | applicable | A select can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source records this select as a marketing-site form control; exact mapping to timezone / no-available-times errors is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Text Input

- Role: standard text input (YAML `input`; marketing site prioritizes CTAs over complex forms)
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#ffffff`
- Text: `#242424`
- Border: 1px solid `rgb(118,118,118)` (body also writes `rgb(118, 118, 118)`)
- Radius: 8px
- Padding: 8px 12px
- Use: Standard text input (marketing site prioritizes CTAs over complex forms)
- Observed: default only
- Field note: Treating `#242424` as this control’s renderable text and Charcoal rather than Select `#000000` is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. `#242424` is this control’s renderable text and Charcoal; it is not Select `#000000`.

The “marketing site prioritizes CTAs over complex forms” Use clause is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the standard text input |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A text field can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source records this input as a standard text field; exact selector/label/request/outcome mapping to form validation or booking submit is unresolved, so those three fields stay omitted at this boundary rather than closed.

### Shadow Card

- Role: standard card; ring shadow as border
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 8px
- Padding: 16px 24px
- Shadow: `rgba(19,19,22,0.7) 0px 1px 5px -4px, rgba(34,42,53,0.08) 0px 0px 0px 1px, rgba(34,42,53,0.05) 0px 4px 8px 0px`
- Use: Standard card; ring shadow as border

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Large Container

- Role: larger containers
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 12px
- Padding: 24px
- Use: Larger containers

This Large Container is body §4 only (not YAML `tokens.components`). No interactive-kind evidence is given. Kind and a state-applicability map are omitted. Treating the standard-card shadow as not copied here, and treating the §9 scheduling-card white fill + exact shadow stack + 12px radius as a local recipe rather than this harvested padding/radius pair, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. The standard-card shadow is not copied here. The §9 scheduling-card tuple sits on its own local-recipe slot.

### Prominent Section

- Role: prominent feature sections
- Type: card
- Anatomy: surface
- Background: `#ffffff`
- Radius: 16px
- Padding: 32px
- Use: Prominent feature sections

This Prominent Section is body §4 only (not YAML `tokens.components`). No interactive-kind evidence is given. Kind and a state-applicability map are omitted.

### Pill Badge

- Role: pill-shaped badges and tags
- Type: badge
- Anatomy: label
- Background: `#242424`
- Text: `#ffffff`
- Radius: 9999px
- Padding: 4px 10px
- Font: 12px / 600 / Cal Sans (YAML `12px / 600`)
- Use: Pill-shaped badges and tags

No interactive-kind evidence is given. Kind and a state-applicability map are omitted.

### Default Badge

- Role: default badge (source: inferred from §1–§2 baseline; no explicit DS variant)
- Type: badge
- Evidence class: this Default Badge is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.
- Anatomy: label
- Background: `#f5f5f5`
- Text: `#242424`
- Radius: 8px
- Padding: 4px 8px
- Font: 12px / 600 / Cal Sans (YAML `12px / 600`)
- Use: Default badge. Inferred from §1-§2 baseline (no explicit DS variant in source).
- Field note: Treating `#f5f5f5` as this control’s renderable background and Surface-alt rather than the §14 skeleton field as a general ink is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. `#f5f5f5` is this control’s renderable background and Surface-alt; it is not the §14 skeleton field as a general ink.

No interactive-kind evidence is given. Kind and a state-applicability map are omitted.

### Top Nav

- Role: sticky top nav, Cal Sans links, dark primary CTA
- Kind: interactive
- Type: tab
- Anatomy: links + dark primary CTA
- Background: white/transparent
- Nav text: `#111111` (Midnight) for primary links, `#000000` for emphasis
- CTA: Dark Primary in the nav
- Mobile: collapses to hamburger with simplified navigation
- Sticky: fixed on scroll
- Use: Sticky top nav, Cal Sans links, dark primary CTA
- Observed: default only
- Field note: Treating YAML type as `tab`, and treating the §9 sticky-nav Inter 14px / 500 local recipe as not this harvested Cal Sans-links row, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. YAML type is `tab`. YAML use remains “Sticky top nav, Cal Sans links, dark primary CTA.” The §9 Inter 14px / 500 sticky-nav tuple sits on its own local-recipe slot.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as sticky top nav |
| hover | applicable | Pointer-web nav links; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | Nav destinations can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as sticky top nav; calendar-sync chip in the nav header is a §14 row, not a harvested nav-link treatment. Exact mapping is unresolved, so those three fields stay omitted at this boundary rather than closed.

### White Hero (local recipe)

Treating the following as a source §9 local composition — white parent + 64px Cal Sans 600 / line-height 1.10 / `#242424` + centered layout + dark CTA `#242424` / 8px / white text — and not as a rewrite of YAML Display Hero or Dark Primary, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Role: white-hero parent surface
- Anatomy: surface
- Background: `#ffffff`
- Local children (this composition only): heading Cal Sans 64px weight 600, line-height 1.10, `#242424`; dark CTA `#242424` background, white text, 8px radius
- Use: source §9 white hero — white background, 64px Cal Sans heading, centered layout, dark CTA
- Kind, Type, and a state-applicability map are omitted. Harvested Display Hero type-role and Dark Primary stay in their own slots.

### Scheduling Card (local recipe)

Treating the following as a source §9 local composition — white parent + exact shadow stack `0px 1px 5px -4px rgba(19,19,22,0.7), 0px 0px 0px 1px rgba(34,42,53,0.08), 0px 4px 8px rgba(34,42,53,0.05)` + 12px radius — and not as a rewrite of harvested Large Container padding/radius, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Role: scheduling-card parent
- Anatomy: surface
- Background: `#ffffff`
- Radius: 12px
- Shadow: `0px 1px 5px -4px rgba(19,19,22,0.7), 0px 0px 0px 1px rgba(34,42,53,0.08), 0px 4px 8px rgba(34,42,53,0.05)`
- Use: source §9 scheduling card — white background, exact shadow stack, 12px radius
- Kind, Type, and a state-applicability map are omitted. Harvested Large Container stays in its own slot.

### Sticky Nav (local recipe)

Treating the following as a source §9 local composition — white sticky parent + Inter 14px / 500 / `#111111` links + dark CTA `#242424` — and not as a rewrite of harvested Top Nav YAML Cal Sans links, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Role: sticky-nav parent
- Anatomy: surface
- Background: `#ffffff`
- Position: sticky
- Local children (this composition only): Inter links 14px weight 500, `#111111`; dark CTA `#242424`
- Use: source §9 sticky nav — white background, Inter 14px / 500 links, dark CTA, sticky
- Kind, Type, and a state-applicability map are omitted. Harvested Top Nav stays in its own slot with YAML Cal Sans links.

### Trust Bar (local recipe)

Treating the following as a source §9 local composition — white parent + grayscale logos + horizontal centering + 16px gap — and not as a page-wide token, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Role: trust-bar parent
- Anatomy: surface
- Background: `#ffffff`
- Local children (this composition only): grayscale company logos, horizontally centered, 16px gap
- Use: source §9 trust bar — white background, grayscale logos, 16px gap
- Kind, Type, and a state-applicability map are omitted.

### Feature Section (local recipe)

Treating the following as a source §9 local composition — 48px Cal Sans 600 / `#242424` heading + Inter 16px / 300 / `#898989` / line-height 1.50 body + screenshot 12px radius with the card shadow — and not as a rewrite of YAML Section Heading or UI Label, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

- Role: feature-section parent
- Anatomy: surface
- Local children (this composition only): heading Cal Sans 48px weight 600, `#242424`; body Inter 16px weight 300, `#898989`, line-height 1.50; product screenshot 12px radius with the scheduling-card shadow stack
- Use: source §9 feature section — 48px Cal Sans heading, Inter 16px / 300 body, screenshot 12px / card shadow
- Kind, Type, and a state-applicability map are omitted. Harvested Section Heading and UI Label stay in their own slots.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The “not a page gutter”, “not a cross-viewport specification”, “Framer-generated span rather than a named product grid”, “not a universal content grid”, “centered headlines with generous margins”, “lavish / premium whitespace”, “~1200px as a recorded max-width rather than a named product grid”, “full-width hero / centered text blocks / 2-3 column feature grids”, and “product screenshots flanked by description text” readings in this section are derived editorial implementation inferences from the verified surfaces; they are not Cal.com-authored or a separately published UI specification. YAML spacing is xs 4, sm 8, md 12, base 16, lg 20, xl 24, xxl 28, section 80, section-lg 96. Body layout states 8px base and 1px, 2px, 3px, 4px, 6px, 8px, 12px, 16px, 20px, 24px, 28px, 80px, 96px. Card padding 12px–24px. Component gaps 4px–8px. Section padding 80px–96px. Max width ~1200px content container, centered. Column patterns: full-width hero, centered text blocks, 2-3 column feature grids. Feature showcase: product screenshots flanked by description text. 8px button corners are not a page gutter. Centered headlines: Cal Sans headings centered with generous margins above and below, as recorded in source whitespace philosophy.

Treating the breakpoint list as the source’s recorded span rather than a cross-viewport specification of a universal content grid, treating collapsing (hamburger nav, hero 64px → ~36px, feature grids multi-column → 2-column → stacked, screenshots scale, section 80px–96px → ~48px), treating image behavior (trust logos reflow, no art-direction changes, 7px–12px image radius), treating touch-purpose (10px+ vertical padding, full-width mobile CTAs, pill 9999px), and treating the listed component paddings/radii as component measurements rather than a universal content grid, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

Responsive record:

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, hero text ~36px, stacked features, hamburger nav |
| Tablet Small | 640px–768px | 2-column begins for some elements |
| Tablet | 768px–810px | Layout adjustments, fuller grid |
| Tablet Large | 810px–1024px | Multi-column feature grids |
| Desktop | 1024px–1199px | Full layout, expanded navigation |
| Large Desktop | >1199px | Max-width container, centered content |

Breakpoints 98px, 640px, 768px, 810px, 1024px, 1199px are source-stated as Framer-generated. That list is the source’s recorded span, not a cross-viewport specification of a universal content grid.

Touch targets: buttons 8px radius with comfortable padding (10px+ vertical); nav links dark text with adequate spacing; mobile CTAs full-width dark buttons; pill badges 9999px radius.

Collapsing: full horizontal nav → hamburger on mobile; hero 64px Cal Sans display → ~36px on mobile; feature grids multi-column → 2-column → single stacked column; product screenshots scale within containers, maintaining aspect ratios; section spacing reduces from 80px–96px to ~48px on mobile.

Image behavior: product screenshots scale responsively; trust logos reflow to multi-row grid on mobile; no art direction changes — same compositions at all sizes; images use 7px–12px border-radius.

The 12px 20px Dark Primary padding, 8px 12px input padding, 16px 24px card padding, 24px large-container padding, 32px prominent-section padding, and 8px / 12px / 16px / 6px / 9999px radii are component measurements in this reconstruction, not a universal content grid.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (cal.com homepage 2026-05)

- Marketing CTA: *"Get started"*

Treating the §14 empty-state strings as part of the state contract, not extra voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. Recorded strings: "Your bookings will appear here."; "Create your first event type"; "No available times in next 30 days."; "Times shown in your timezone (Asia/Seoul)"; "Booking...".

The following forbidden-phrase list is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification. It is not one of the Observed homepage strings above.

**Forbidden phrases** (source): “Revolutionary scheduling”, “the future of calendars”, emoji in chrome.

### Derived editorial copy direction

The following voice reading and copy-pattern table — engineer-direct / developer-warm, documentation-that-happens-to-be-on-a-marketing-page, capability-statements-no-hype, open-source-first / original tagline, “we built this because the proprietary one was bad” engineer credibility, Verb-first / Quiet / Specific-and-actionable / Imperative-code-block-heavy / Capability-list-monochrome-typography labels, and derived strings “Book a demo” / “Sign in with Google” / “Calendar permissions revoked. Reconnect Google Calendar.” — are a derived editorial implementation inference from the verified surfaces; they are not Cal.com-authored or a separately published UI specification. They are not the Observed homepage string above.

The source describes Cal.com’s voice as **engineer-direct and developer-warm.** Marketing copy is read as documentation that happens to be on a marketing page — capability statements, no hype. Open-source-first positioning shapes the register: “the open source Calendly alternative” is the original tagline, and the voice is read as still carrying “we built this because the proprietary one was bad” engineer credibility.

| Context | Tone |
|---|---|
| CTA | Verb-first. "Get started", "Book a demo", "Sign in with Google" |
| Empty (no bookings) | Quiet. "Your bookings will appear here." |
| Error | Specific + actionable. "Calendar permissions revoked. Reconnect Google Calendar." |
| Documentation | Imperative, code-block heavy |
| Marketing | Capability-list, monochrome typography |

Treating “Documentation” as a copy-register row rather than a captured docs-surface token set is a derived editorial implementation inference from the verified surfaces; it is not Cal.com-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- exact cubic-bezier curves for “Standard cubic-bezier”
- the unnamed members of the source’s 11 shadow definitions
- hover (except Dark Primary opacity 0.7), pressed, disabled, loading, error, success, and `focus-visible` visual treatments on harvested marketing controls
- loading, error, and success applicability fields omitted on Dark Primary, Ghost, Pill, Compact, Inset Highlight, Select, Text Input, and Top Nav
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
- product-UI color tokens for the blues/greens named only as screenshot content
