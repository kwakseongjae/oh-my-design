# GitHub migration log

Source: `web/references/github/DESIGN.md`
Destination: `docs/design-md-weight/migrated/github/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/github/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd, `portable_core: true`, placeholders 0)

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage 옮김 → Experience Scope; `primary_color` 옮김 → Foundations GitHub Blue; logo 경계 문장 옮김 → Typography & Assets | Portable file has no frontmatter. Name kept as H1 `GitHub Design System`. Homepage `https://github.com` is dual Scope + provenance identity/surfaces/Tier 1 (E2a). Catalog `primary_color` `#0969da` is dual identity + portable Foundations GitHub Blue / Accent fg (E2a). Catalog logo type `simpleicons` / slug `github` is dual: provenance identity + portable Typography & Assets boundary sentence (`Catalog logo metadata is Simple Icons identity (github)`, not a captured first-party mark) (E2a). |
| YAML `omd`, `verified`, `added`, token claims, `tokens.source` / `extracted`, `components_harvested` | 분리 → provenance | 출처 원장·freshness·Proof. `tokens.source: design-system` preserved (A1c). No `ds.type` on source; none invented. |
| YAML `tokens.note` | 옮김 → Experience Scope; 원장 분리 → provenance | Dual destination (E2a): portable Scope keeps Primer + live github.com grounding (`dark-default canvas #0d1117`, green primary `#1f883d`, 6px radius, 32px control height, `primary` = `fgColor-accent` `#0969da`); provenance keeps the source note. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. YAML unitless `lineHeight` 1.25 / 1.5 / 1.4 / 1.45는 비율로 보존 (A1a). 검증된 primitive type은 컴포넌트별로 보존: button×5 (primary/default/danger/invisible/icon-button) + input×3 (text-input/search/select) + toggle×3 + tab×4 + listItem×2 (action-list/tree-view) + badge (token interactive) + card/dialog/avatar/badge 표시 컴포넌트 `Type` 유지. `Kind: interactive`로 뭉개지 않음 (A1b). `button-sizes`는 스케일로 Primary Button에 유지. Primary `#187733` / Default `#eef1f4` `#e6eaef` / disabled `#94d3a2`는 해당 컨트롤 필드이며 일반 Ink가 아님 (A4). YAML Label `Border: 1px solid #ddf4ff` / `Height: 20px`는 body low-alpha / ~20px와 분리 보존. YAML Action List `Padding: 16px`는 body ~16px와 분리. YAML Text Input `Height: 32px`는 body ~32px와 분리. YAML Overlay shadow `0 3px 6px rgba(140,149,159,0.15)`는 body medium/large와 분리. YAML rounded small 3px와 layout Small 2px, Label `9999px`와 body `2em`, Counter `12px / 400`과 body `12px / 500`, Default Button YAML `0 16px` / 600과 live `5px 16px` / 500, Toggle on `#1f883d` 또는 `#0969da`는 양쪽 유지·비평균. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(Primer product UI on github.com; marketing homepage/Enterprise는 별도 증거 도메인). Token/theme facts는 한정 없이. Atmosphere·diff-inheritance·Octocat-rationing·2022–2024 migration 문단 인접에 derived editorial implementation inference / not GitHub-authored or separately published UI specification 한정 (B2/B2a). Key characteristics는 Distinctive traits. `https://github.com`와 `primer.style`는 provenance와 dual (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Light/dark functional tokens, sponsors, tab-active `#fd8c73`, accent fg/emphasis 동일 hex의 역할 비합침. |
| §3 Typography Rules | 옮김 → Typography & Assets | 다섯 증거 등급. YAML UI 스택과 §3 emoji 포함 스택 둘 다 유지. Mona Sans / Hubot Sans는 marketing display. YAML unitless line-height 보존 (A1a). Type-character 문단 인접 B2a (B2/B2a). 시스템 스택을 Mona Sans로 표시하지 않음. |
| §4 Component Stylings | 옮김 → Components & States | Buttons / inputs / box / labels / underline-nav / flash / dialog / toggle. Search Input(`Type: input`)은 YAML 밖 §4 고유값. Capture selector 없음 — 발명하지 않음. |
| §4 footer **Verified** / Tier 1 / Surface split | 분리 → provenance; Tier 1 URL 옮김 → Experience Scope | Dual destination (E2a): `primer.style`와 `https://github.com`는 portable Scope + provenance surfaces/sources/Tier 1. Surface split (product UI vs marketing Mona/Hubot)은 Experience/Typography에도 유지 (E1). |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | 8px base, containers 768/1012/1280, 12-column repo grid, radius scale including 2px vs YAML 3px. Whitespace 철학 문단 인접 B2a (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Flat/small/medium/large/xlarge + focus ring (elevation이 아님). Border-first 독해 인접 B2a (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | Primer/github.com 제품 규칙. Governance 통제 문구에 넣지 않음. Numbered editorial Principles와 분리. |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. 미관측을 `not-applicable`로 바꾸는 근거가 아님 (C1). |
| §8 Component Patterns | 옮김 → Components & States | YAML 컴포넌트 해부(Label exact border/height, Action List exact padding, Banner leading status icon + optional title/description, Inline Message leading icon) + §8-only 패턴(button-group/counter-button, nav-list, autocomplete, data-table, popover). Overlay 192px / 960px와 YAML Overlay shadow 유지. Box / Label / Counter / State Label / Branch Name / Avatar / Avatar Stack / Action Menu / Dialog / Overlay / Tooltip / Banner / Flash / Inline Message / Spinner / Progress Bar / Skeleton / Blankslate / Timeline은 kind/map 생략 또는 non-interactive (C4). Blankslate “rationed Octocat” 판단은 인접 B2a (B2/B2a). |
| Responsive Behavior (unnumbered H2) | 옮김 → Layout & Platforms | Primer breakpoints 320/544/768/1012/1280/1400, collapsing, touch 28/32/40, images 16:9 / max-width 100%. 원본에 없는 최소폭 발명 없음. |
| §9 Agent Prompt Guide | 삭제; §9-only 고유값 옮김 → Components additional patterns | 도구별 복붙 프롬프트 삭제. Color reference는 이미 Foundations에 있음. Diff line (added `#dafbe1` / removed `#ffebe9` / mono 12px / +/− gutter / line numbers `#59636e`)은 §9 예시에만 있는 고유 근거값이라 Components additional patterns로 옮김 (A3). 슬롯 없는 위임 없음. |
| §10 Voice & Tone | mixed: 관측 두 homepage string 옮김 → Content + provenance; derived 표/금지/독해 옮김 → Content (인접 B2a) | Observed (live github.com WebFetch 2026-06-06): `"Change is constant. GitHub keeps you ahead"` / `"The future of building happens together"` — dual portable Content + provenance, including the freshness date (E2a). Derived editorial copy-pattern table (including `Show, don't just tell`; marketing `Mona Sans, big and confident`; empty-repo `famous … zero fluff`), forbidden-copy list, and voice/Octocat reading sit under adjacent complete B2a and are not labeled Observed (B2/B2a, E1). 합성 보이스 샘플 없음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance | 2008 창립, Git 2005, Microsoft 2018 $7.5B, 100M+ developers, Octocat Simon Oxley, Primer open-source는 공개 사실. Git-data-model 인과·Octocat rationing·token migration 독해는 Scope 인접 B2a (B2/B2a). 연표 URL은 원본에 없음 — 발명하지 않음. |
| §12 Principles | 옮김 → Experience principles | 여덟 항목 전체가 원본 HTML 주석이 명시한 editorial readings. 인접 본문에 derived editorial implementation inference / not GitHub-authored or a separately published UI specification. Capture-bound application은 Primer/github.com 제품 규칙이며 그 한정 밖. Governance 일반 문구는 그 한정의 대체물이 아님 (B2/B2a, E1). |
| §13 Personas | mixed: 가상 biography 삭제·sidecar 재수록 없음; Audience는 배제 경계만 | 원본이 fictional archetypes라고 명시. Names/ages/cities/employers/biographies는 portable Audience에도 provenance에도 없음 (D2). Independently verified Primary tasks 3건은 §13이 아니라 Primer/github.com Create-Commit-Merge, diff/issue review, Code/Issues/PR/Actions navigation (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability | 본문 보존: Empty / Empty repo / Loading first-paint / Loading action-in-button / Error inline / Error flash / Success flash / checks passing / checks failing / merged / Disabled / Focus (keyboard) / Hover (row) (A2). 선언 컴포넌트는 §4.4 7상태 표를 닫되 미관측 시각값은 발명하지 않음. `not captured` / `not specified`를 `not-applicable` 사유로 쓰지 않음 (C1). loading·error·success는 역할 의미로 판정: Primary Button loading applicable (spinner replaces label); Select error applicable (native-backed form field; treatment omitted); Icon Button success applicable on copy-to-clipboard use (captured checkmark ~1.5s); 그 외 버튼/탭/토글 다수는 역할상 not-applicable (C2). Named `Focus (keyboard)`는 `focus-visible` treatment로 승격하지 않음; focus-visible 행에 hex 없음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브 분리 → provenance omission ledger | Duration (`instant` 0ms / `fast` 80–120ms / `standard` 160–200ms / `slow` 300ms), easing 이름/용도 (`ease-out` / `ease-in-out` / `ease-in`), signature motions 5, `prefers-reduced-motion` 유지. Merge-celebration “single licensed moment of whimsy” 판단은 인접 완전 B2a (B2/B2a). Exact cubic-bezier `cubic-bezier(0.33, 1, 0.68, 1)` / `cubic-bezier(0.65, 0, 0.35, 1)` / `cubic-bezier(0.32, 0, 0.67, 0)`는 무출처(원본 HTML 주석의 Primer 목록에 없음)라 portable에서 생략하고 provenance omission ledger에 보관 (E2b). Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 표 밖 값을 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps가 같은 다섯 종류 게이트를 말함. |
| HTML comment (OmD v0.1 Sources) | mixed: Primer 목록·narrative·경고 분리 → provenance; WebFetch 2026-06-06 freshness + 두 homepage voice string 옮김 → Content + provenance | Primer token 목록, narrative facts, fictional-persona 경고, interpretive-claim 경고는 provenance. Live homepage voice strings and the WebFetch `2026-06-06` freshness date are dual-destination: portable Content & Locales + provenance freshness/narrative (E2a). Interpretive 한정은 portable 본문에 남김 (E1). |

## Revision 2026-08-23 (wave4 sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6 (F1·F2 의무 최종 패스). Source FAIL: `docs/reviews/t2-1-wave4-2026-08-23-sol-sample.md` §2.

| Item | Correction |
|---|---|
| YAML Label/Action List/feedback A1/A4 | Label YAML exact `Border: 1px solid #ddf4ff` and `Height: 20px` restored beside body low-alpha / ~20px. Action List exact `Padding: 16px` beside ~16px. Text Input exact `Height: 32px` beside ~32px. Overlay YAML shadow restored. Inline Message leading icon and Banner leading status icon + optional title/description restored. |
| Content E1/B2a | Two WebFetch homepage strings are Observed. Copy-pattern table (`Show, don't just tell`; `Mona Sans, big and confident`; empty-repo `famous … zero fluff`), forbidden list, and voice reading sit under adjacent complete B2a and are not labeled Observed. |
| Motion/Blankslate B2a | Merge-celebration whimsy judgement and Blankslate rationed-Octocat judgement have adjacent complete B2a. |
| Select C1 / Copy Icon Button C2 | Select error is applicable as a native-backed form field (treatment omitted). Icon Button success is applicable for copy-to-clipboard use, with captured checkmark ~1.5s connected to that role. |
| YAML/§8/§10/HTML-comment log E2/E2a/E2c | Withdrawn: “최소 필드 단위” without exact Label/Action List fields; §10 table-as-Observed; HTML comment provenance-only for WebFetch date. WebFetch `2026-06-06` is dual Content + provenance. Compliance claims match the body. |

### F1 / F2 (v6 mandatory final passes)

- **F1 B2a scan (full DESIGN.md reread):** Editorial/causal/judgement sentences have adjacent complete B2a (`derived editorial implementation inference` / `not GitHub-authored or a separately published UI specification`) on: Scope atmosphere + diff-inheritance paragraph; Scope Git-data-model / Octocat / 2022–2024 migration paragraph; Principles numbered list (eight items); Elevation border-first reading; Typography type-character reading; Assets mascot-use reading; Layout whitespace reading; Content copy-pattern table + voice + Octocat-personality reading; Motion merge-celebration whimsy reading; Blankslate rationed-Octocat reading. Capture-bound application is labeled Primer/github.com product rules, not the numbered editorial list. Remaining Scope sentences are product/surface coverage, Primer/live token facts, or widely documented public founding facts. Avoid copies source Don'ts. Color “warm near-black” is source §2 wording. Observed homepage strings are the two WebFetch quotes only.
- **F2 E2 grep (value + field/role context):** Label grep-matched `1px solid #ddf4ff` as YAML exact Border and `Height: 20px` as YAML exact, separate from body low-alpha / ~20px. Action List grep-matched `Padding: 16px` (YAML exact) and ~16px (body). Banner grep-matched leading status icon and optional title/description. Inline Message grep-matched leading icon. Select error grep-matched `applicable` with form-field reason, not `not specified`. Icon Button success grep-matched copy-use checkmark ~1.5s. Content Observed block grep-matched the two homepage strings and `2026-06-06`; derived table grep-matched `Show, don't just tell` / `Mona Sans, big and confident` / `zero fluff` under the B2a limiter, not under Observed. Homepage `https://github.com` is in Scope and provenance identity/surfaces/Tier 1. `primer.style` is in Scope and provenance surfaces/sources/Tier 1. `#0969da` catalog primary is in Foundations and provenance identity. Token note is in Scope and provenance. Simple Icons `github` is in Assets and provenance identity. WebFetch date is in Content and provenance freshness/narrative. Cubic-bezier values are in provenance omission ledger only. B3 five-kind gate grep-matched Foundations Motion. Primary tasks grep-matched Create/commit/merge, diff/issue review, Code/Issues/PR/Actions — not §13. Maya/Daniel/Priya absent from DESIGN.md and provenance. Compliance claims match the body.
