# 591 migration log

Source: `web/references/591/DESIGN.md`
Destination: `docs/design-md-weight/migrated/591/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/591/provenance.md`
Date: 2026-08-24
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: worker pre-F3 SHA-256 `0da9651dab…89240b` withdrawn after F3 body edits (E2c). Post-F3 `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/591/DESIGN.md --check --require-portable-core --json` → exit 0, `portable_core: true`, SHA-256 `05899c6caf772de65e823896e2f9f4c597ed8f27c0ea66a728962dc835cb7d9d`. `node test-v2/tools/migrate-reference.mjs --brand 591 --gate-only` → PASS, problems []. These are checks of this file set; they are not a sol meaning-lane verdict (E2c).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Distinctive + Foundations 591 Orange + Search CTAs / Active Tab; logo URL 옮김 → Typography & Assets + provenance | Portable file has no frontmatter. Name kept as H1 `591 Design System`. Homepage `https://www.591.com.tw/` grep (E2a): provenance Identity/Surfaces/Sources/Tier 1 + portable Scope. Rent `https://rent.591.com.tw/` grep (E2a): provenance Surfaces/Sources/Tier 1 + portable Scope (Listing Search CTA/Input use the host form `rent.591.com.tw`). Catalog `primary_color` `#ff8000` grep (E2a): provenance Identity + portable Scope token-note/visual-character/public-history headlines, Distinctive, Principles item 2, capture-bound, Foundations 591 Orange, Semantic listing-card ink not-badge-tag, Homepage/Listing Search CTA Background, Active Property-type Tab Background, Nav Item active text, Type roles Hero/Nav Tab/Search Button, Capture-record selected filter, Property Feature Tag text. Avoid names the orange accent without this hex. Google s2 favicon `https://www.google.com/s2/favicons?domain=591.com.tw&sz=128` is dual provenance Identity + portable Typography & Assets identity-boundary (E2a). Named gaps does not hold a first-party-mark sentence. Not a captured first-party mark. **[SUPERSEDED 2026-08-24 wave7 ledger sync — prior clause claimed Named gaps holds a URL-free first-party-mark sentence. That Named-gaps row is absent (`DESIGN.md` Named gaps 626–642). See Revision wave7 ledger sync.]** |
| YAML `omd`, `verified`, token claims, `tokens.source` / `extracted` / `note`, `components_harvested` | mixed: `omd` / `verified` / `extracted` / `components_harvested` / `tokens.source` 분리 → provenance; `tokens.note` 옮김 → Experience Scope + provenance | 출처 원장·freshness·Proof. YAML `tokens.source: live-extract` and `components_harvested: true` are provenance-only (A1c). Source YAML has no `ds` / `ds.type` field; none was invented. `tokens.note` grep (E2a): provenance Token note + portable Scope quoted note with register-split B2a. YAML `verified` / `extracted` 2026-06-22 are provenance freshness; the same date is also portable Scope / Content Observed / component Observed lines (E2a). |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States, Distinctive, Named gaps | 검증된 값만 최소 필드 단위. YAML unitless `lineHeight` `1.31` / `1.5`는 비율로 보존하고 홈페이지 inspect `21px`와 합치지 않음 (A1a). YAML typography `use` 문자열은 Type roles Use 열에 복원 (A1). 검증된 primitive type은 컴포넌트별로 보존: `Type: button` ×3 (YAML search ×2 + body Secondary Text Button) / `Type: tab` ×3 / `Type: input` ×3 (YAML ×2 + rent Listing Search Input) / `Type: card` ×2 / `Type: badge` ×2. `Kind: interactive`로 뭉개지 않음 (A1b). YAML `input-search` fg `#333333`과 홈페이지 live `#999999`, YAML `dark-overlay` `#262626`과 §6 `rgba(0,0,0,0.8)`, YAML `rounded.full` 9999과 body `50%`, canvas/on-primary 공유 `#ffffff`, listing-card `#000000`과 Body `#4a4a4a`는 비합침 (A4). YAML 9999 grep (E2a): Distinctive, Avoid, Shape, Named gaps, provenance unmerged list. |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(homepage + rent listing). Token-note register-split, visual-character, public-history-as-narrative, market-position/redesign-refusal는 각 인접 완전 B2a (B2/B2a). Key characteristics는 Distinctive. Distinctive의 orange-not-decoration / stack-not-owned-face / price-red-unmerged / 9999-unmerged-from-50% / grey-ladder-unmerged는 목록 다음 인접 완전 B2a. Atmosphere 반복 중 범위에 기여하는 요약만. |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color, Distinctive | 16 hex 전부. Pagination bullets는 §2 Primary 문장에만 있는 사용처라 Foundations 591 Orange에 유지 (A3). Unmerged-role readings는 Semantic color 머리 인접 완전 B2a (inactive-tab `#dddddd` not Muted Light · listing-card ink not badge-tag/badge-price 포함). `rgb(22,158,113)` / `rgb(240,24,0)` / `rgb(80,120,179)`는 토큰 승격 없이 Semantic unmerged-role + Named gaps + provenance frequency ledger (E2a). |
| §3 Typography Rules | 옮김 → Typography & Assets | 증거 등급 5-class, YAML/homepage/rent 세 family 문자열 비합침, unitless `1.31`/`1.5`. Font evidence 적용 읽기·Family font-use boundary·§3 CJK-first/weight/red-for-value/size-restraint·Type-roles ratio-versus-px·Type-roles Nav Item vs hero-tab / listing Search CTA vs homepage Search CTA unmerged pairs는 각 인접 완전 B2a. YAML `use` 복원. Hero/Search Button/Price/Caption line-height는 YAML ratio 없어 생략 (Named gaps). |
| §4 Component Stylings | 옮김 → Components & States; 셀렉터 없음 | YAML 11개 + body Secondary Text Button + rent Listing Search Input + Featured Listing. Capture selector 없음 — 발명하지 않음; live inspect 서술은 provenance Capture record. Homepage Search Input `#333333`/`#999999` unmerged-field는 Avoid + Semantic unmerged-role + component field-note + provenance (E2a). Listing Search Input is not homepage YAML `input-search`: adjacent complete B2a on the field-note. Listing Row Card §9 `16px bold` vs Body Large 400: adjacent complete B2a. Active tab static-variant와 Nav Item active-not-filled-hero는 각 인접 완전 B2a. Featured Listing은 YAML type 없음 — Type 발명 없음, kind/map 생략 (C4). |
| §4 footer **Verified** / Tier 1 / Tier 2 / Conflicts | mixed: freshness·Tier 2·Conflicts 분리 → provenance; Tier 1 URL은 Scope + provenance | Footer Verified 2026-06-22 / Tier 2 not-found / Conflicts none는 provenance-only freshness·원장. Footer Tier 1 `https://www.591.com.tw/`와 `https://rent.591.com.tw/`는 dual Scope + provenance Tier 1 (E2a), not provenance-only. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | Scale 4, 8, 12, 16, 20, 24, 32, 48px (24/32는 unitless로 유지, px 단위 발명 없음). ~1200px, 264–300px, 30–44px. harvested-rhythm / density / flat-segmentation / tab-height는 Layout 인접 완전 B2a. YAML 9999 vs `50%`는 Shape + Distinctive + Avoid + Named gaps (E2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | Flat `none` + hairline `#e6e6e6` + tint `#f5f5f5`/`#f2f8ff` + overlay `rgba(0,0,0,0.8)`. YAML `#262626`는 별도 Dark Overlay (YAML) 필드. shadow-philosophy 인접 완전 B2a. |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. 인접 B2a. Governance 통제 문구에 넣지 않음. Dual-role `#e60012` reminder는 capture-bound 다음 인접 완전 B2a. |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. 인접 B2a. Unmerge Don'ts (`#999999`/`#333333`, `#262626`/`rgba(0,0,0,0.8)`, 9999/`50%`)도 같은 Avoid 머리 아래. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms + Named gaps | Mobile `<768px` / Tablet `768–1024px` / Desktop `1024px+` 기록 표. breakpoint table as recorded span not a captured Mobile/Tablet/Desktop inspect, collapsing list as source-stated not a captured Mobile pass, desktop-measurement / mouse-not-touch-first는 각 인접 완전 B2a. 원본에 없는 최소폭 발명 없음. Touch 55px/44px/30px/27px는 Layout + 해당 컴포넌트 높이 (E2a). |
| §9 Agent Prompt Guide | mixed: 도구 프롬프트 삭제; 고유 값 옮김 → Listing Row Card + Layout + Type roles Filter Label | 도구별 복붙 프롬프트·Example Component Prompts·Iteration Guide 번호 목록 삭제. Color reference의 검증된 hex는 이미 Foundations에 있음. Unique §9 listing-title “property title in `#333333` 16px bold”는 portable Listing Row Card + 그 행의 adjacent complete B2a (unmerged from Body Large 400) (E2a). Unique §9 Filter Label “Labels in `#666666` 14px”는 portable Type roles Filter Label, unmerged from Body Large 16px/`#333333` and Filter Range Input 13px (A3/A4, E2a). Unique attached search-bar composition은 portable Layout (E2a). 슬롯 없는 위임 없음. |
| §10 Voice & Tone | mixed: 관측 live string 옮김 → Content + provenance + Scope; derived 표/금지는 Content (인접 B2a) | Evidence classes stay split. Source-named live homepage voice samples: title + two H3s (Content + Scope + provenance). Additional live homepage chrome: 「搜尋」 homepage CTA + 租屋/中古屋/新建案 tabs. Live rent: 「搜尋」 listing CTA + 「社區找房」. Source tone table: extra category labels + “台灣第一房屋網路平台”. §14 implementation guidance: 「我的詢問」 (not live homepage). Title grep (E2a): Scope + Content live-homepage voice-sample + provenance — not Type roles Hero, not Primary tasks. Two H3s grep (E2a): Scope + Type roles Hero + Content live-homepage voice-sample + provenance. 「搜尋」 grep (E2a): Primary tasks + Type roles Search Button + Homepage/Listing Search CTA Use + Content live homepage/live rent + tone-table Search CTA + provenance. Derived voice / tone-table / forbidden-register는 인접 완전 B2a이며 Observed live-homepage voice-sample로 표시하지 않음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope` + Audience; 서사 원장 분리 → provenance | 2001 / Addcn / 五九一 / classified-board → marketplace evolution / product roof / 實價登錄 as mandated government price-transparency showing actual transaction prices rather than asking prices / data-trust moat는 Scope public-history 인접 완전 B2a 아래 narrative, not interface tokens. Dual Scope + provenance Narrative (E2a). Stakeholder groups (renters / buyers / landlords-agents)는 Audience. Market-position / redesign-refusal는 Scope 별도 인접 완전 B2a. 별도 official-doc URL은 원본에 없음 — 발명하지 않음. |
| §12 Principles | 옮김 → Experience principles | 다섯 항목 전체(stems + *UI implication*)는 catalog editorial reconstruction. 머리 인접 완전 B2a: derived editorial implementation inference / not 591-authored or a separately published UI specification (B2/B2a). Not classified as a first-party quote. |
| §13 Personas | mixed: 가상 biography 삭제·sidecar 재수록 없음; Audience는 배제 경계만 | 원본이 fictional archetypes라고 명시. Names/ages/cities/occupations/biographies는 portable Audience에도 provenance에도 없음 (D2). Audience exclusion + observable-work-follows-three-tasks는 인접 완전 B2a. Independently verified Primary tasks 3건은 §13이 아니라 live 「搜尋」 / listing-row scan / 租屋·中古屋·新建案 tabs (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability + Named gaps | 본문 표 10행 보존: Empty ×2 / Loading ×2 / Error ×2 / Success / Skeleton / Disabled / Active filter (A2). graph-not-adopted preservation + state-contract characterizations 인접 완전 B2a. `#e60012` validation은 Capture record + Foundations Price Red second named use + dual-role reminder (E2a, A4). 선언 컴포넌트는 §4.4를 닫되 미관측 시각값은 발명하지 않음. `not captured` / `not named`를 `not-applicable` 사유로 쓰지 않음 (C1). Homepage/Listing Search CTA loading·error는 검색 요청 역할로 applicable, 시각값 생략; success는 inquiry-sent가 다른 역할이라 생략. Secondary Text Button loading·error·success는 「社區找房」 캡처와 map-view/「地圖找房」 미해상으로 생략. Homepage/Listing Search Input과 Filter Range Input error·success는 §14 generic form-validation이 exact field를 지정하지 않아 생략. Property-type tabs와 Nav Item loading·error·success는 selection 의미로 `not-applicable` (C2). Card/Featured/Tag/Price는 kind/map 생략 (C4). Named Focus 없음; focus-visible 행에 hex 없음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브 값 분리 → provenance omission ledger; 커브 이름 생략 사실은 Named gaps에도 | Duration (`motion-fast` 100ms / `motion-standard` 200ms / `motion-slow` 300ms), easing 이름 (`ease-enter` / `ease-exit` / `ease-linear`), CSS keyword `linear`, “no spring / no bounce”, `prefers-reduced-motion: reduce` 유지. Exact cubic-bezier `cubic-bezier(0.2, 0.6, 0.25, 1)` / `cubic-bezier(0.4, 0.0, 1, 1)` 값은 무출처라 portable에서 생략하고 provenance omission ledger에 보관 (E2b). 커브가 생략됐다는 사실은 portable Motion 표 + Named gaps에도 있다 (E2a). `ease-exit` 커브는 legacy spec-template 예시와 일치. minimal/functional / no-spring / 25-year character 독해는 Motion 인접 완전 B2a. Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 표 밖 값을 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps restates “all five” without being the B3 full-text location. |
| Footer **Verified** / HTML comment | mixed: freshness 분리 → provenance; live-inspect 수치는 portable에도 | Dual (E2a): live title is Scope + Content Observed + provenance Capture record, not Type roles Hero. Two H3s are Scope + Type roles Hero + Content Observed + provenance. Computed stacks/hex/px는 Type roles / Components + provenance Capture record. HTML comment의 editorial “conventions over design innovation” 문장은 provenance Proof notes에만 있다. portable 본문은 그 구를 인용하지 않는다. Principle 4의 “legible convention > design innovation”은 Principles 머리 인접 완전 B2a 아래 catalog reconstruction이다 (E1). Personas illustrative 안내는 sidecar 재수록 없이 삭제 기록만 (D2). |

### F1 / F2 (v7 mandatory final passes)

- **F1 B2a scan (full DESIGN.md reread from line 1, restated after F3):** Adjacent complete B2a on: Scope token-note register-split; Scope visual-character; Scope public-history / 五九一 / 實價登錄 as narrative not tokens; Scope market-position / redesign-refusal; Audience exclusion / stakeholder-group / task-context; Distinctive orange-not-decoration / stack-not-owned-face / price-red-unmerged / 9999-unmerged-from-50% / grey-ladder-unmerged; numbered Principles including UI implications; dual-role `#e60012` reminder; capture-bound list; Avoid list; Semantic color unmerged-role readings (inactive-tab `#dddddd` not Muted Light; listing-card ink not badge-tag/badge-price); Spacing unitless-scale; Shape local-geometry / 9999 vs `50%`; Elevation shadow-philosophy; Motion minimal/functional / no-spring / 25-year character; Font evidence evidence-class application; Family font-use boundary; Family §3 CJK-first / weight / red-for-value / size-restraint; Type-roles ratio-versus-px; Type-roles Nav Item vs hero-tab / listing Search CTA vs homepage Search CTA unmerged pairs; Assets Google-favicon identity-only; Assets listing photography not invented decoration; Capture-record graph-not-adopted preservation + state-contract characterizations; Homepage Search Input field-note YAML fg vs live placeholder; Listing Search Input field-note not homepage YAML `input-search`; Listing Row Card §9 `16px bold` vs Body Large 400; Layout harvested-rhythm not universal grid; Layout density / flat-segmentation / tab-height; Layout breakpoint table as recorded span not a captured Mobile/Tablet/Desktop inspect; Layout collapsing as source-stated not a captured Mobile pass; Layout desktop-measurement / mouse-not-touch-first; Active Property-type Tab static-variant; Nav Item active-not-filled-hero; Content voice / tone-table / forbidden-register; Content no synthetic samples beyond observed strings and the source tone table. Left without B2a (verified hex/geometry, YAML `use` strings, live Observed strings, C2 role sentences, C4 omit-kind sentences, B3 five-kind gate, Governance controlled copy, Named gaps inventory). Reconstruction-boundary exemption not used. No “no unqualified sentence remains” claim. Worker F1 completeness is not re-asserted (E2c).
- **F2 E2 grep (value + field/role context, restated after F3):** `#ff8000` grep-matched Scope token-note/visual-character/public-history headlines, Distinctive, Principles item 2, capture-bound, Foundations 591 Orange, Semantic listing-card not-badge-tag, Search CTA Backgrounds, Active Tab Background, Nav Item active, Type roles Hero/Nav Tab/Search Button, Capture-record selected filter, Feature Tag; Avoid does not carry this hex. `#e60012` grep-matched Scope token-note/visual-character, Primary tasks, Distinctive, Principles item 3, capture-bound, dual-role reminder, Avoid, Foundations Price Red (price + §14 validation as second named use), Semantic unmerged-role, Type roles Price, Capture-record form validation, Price Display. `#999999` vs YAML `input-search` `#333333` grep-matched Avoid, Semantic unmerged-role, Homepage Search Input field-note, provenance unmerged list. `#262626` vs `rgba(0,0,0,0.8)` grep-matched Avoid, Semantic Dark Overlay (YAML) vs Dark Overlay (body §6), Elevation. `9999` grep-matched Distinctive, Avoid, Shape, Named gaps, provenance. `50%` grep-matched Distinctive, Avoid, Shape, Named gaps, provenance. `rgb(22,158,113)` / `rgb(240,24,0)` / `rgb(80,120,179)` grep-matched Semantic unmerged-role + Named gaps + provenance frequency ledger. Google favicon URL grep-matched Assets + provenance identity only. Named gaps has no first-party-mark sentence. **[SUPERSEDED 2026-08-24 wave7 ledger sync — prior “Named gaps is URL-free” treated a deleted Named-gaps row as a current destination. See Revision wave7 ledger sync.]** Homepage `https://www.591.com.tw/` grep-matched Scope + provenance identity/surfaces/Tier 1. Rent `https://rent.591.com.tw/` grep-matched Scope + provenance surfaces/Tier 1. `tokens.source` / `components_harvested` grep-matched provenance only, not portable DESIGN.md. YAML `1.31` / `1.5` grep-matched Type roles + provenance. `21px` grep-matched Type roles size-local + provenance homepage inspect. `100ms` / `200ms` / `300ms` grep-matched Motion table + provenance curve-omission paragraph. Exact cubic-bezier values grep-matched provenance omission ledger, not portable numeric promotion (portable says “omitted”). B3 five-kind gate grep-matched Foundations Motion (`transition properties, animation name, duration, easing, and reduced-motion behavior` + per component + “Official documentation of a single curve or duration is not that gate”). Named gaps restates “all five” and is not claimed as the B3 full-text location. `16px bold` grep-matched Listing Row Card (adjacent complete B2a on unmerged Body Large 400) + provenance §9 unique anatomy. Title string grep-matched Scope + Content Observed + provenance, not Type roles Hero. Two H3s grep-matched Scope + Type roles Hero + Content Observed + provenance. 「搜尋」 grep-matched Primary tasks + Type roles Search Button + Homepage/Listing Search CTA Use + Content Observed + tone-table + provenance. “conventions over design innovation” grep-matched provenance Proof notes only, not portable DESIGN.md. `Type: button` ×3, `Type: tab` ×3, `Type: card` ×2, `Type: input` ×3, `Type: badge` ×2 grep-matched. Search CTA / Secondary / search-input loading·error·success are not a single `not-applicable` set. **[SUPERSEDED 2026-08-24 wave7 ledger sync — this F2 treated those L/E/S fields as `not-applicable` rows. Current body: Homepage/Listing Search CTA loading·error applicable; success omitted; Secondary L/E/S omitted; search-input loading not-applicable, error·success omitted. See Revision wave7 ledger sync.]** Card/Featured/Tag/Price grep-matched omit-kind sentences. Persona names `林宜蓉` / `王建明` / `陳美如` absent from DESIGN.md and provenance. `omd-apply` / `npx omd` absent from portable body. YAML xl 24 / xxl 32 remain unitless token numbers and are not given a px unit in the output. No “B3 유지” claim beyond the Foundations Motion full text actually present. Worker F2 completeness is not re-asserted (E2c).

## Revision 2026-08-24 (wave7 sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-wave7-2026-08-24-sol-full.md` §1. New F3 not required.

| Item | Correction |
|---|---|
| 1. §11 relations | Restored in Scope under public-history B2a: classified-board → marketplace evolution; 實價登錄 as mandated government price-transparency; actual transaction prices versus asking prices; data-trust moat. Dual Scope + provenance Narrative. |
| 2. Filter Label | Restored as Type roles Filter Label `#666666` / 14px. Unmerged from Body Large 16px/`#333333` (YAML `body-lg` still uses the string “filter labels”) and Filter Range Input 13px. §9 mixed disposition updated. |
| 3. Content evidence classes | Split: source-named live homepage voice samples (title + two H3s); additional live homepage chrome; live rent (`「搜尋」` listing CTA, `「社區找房」`); source tone table; §14 implementation guidance `「我的詢問」` (not live homepage). |
| 4. C2 | Homepage/Listing Search CTA loading·error applicable (search-request role); success omitted. Secondary Text Button is `「社區找房」` only; L/E/S omitted (map-view / `「地圖找房」` unresolved). Search-input and Filter Range Input error·success omitted (generic form-validation names no exact field). Audit-log C2 restatement superseded. **[SUPERSEDED 2026-08-24 wave7 ledger sync — this summary did not rewrite the original F2 C2 `not-applicable` sentence. See Revision wave7 ledger sync.]** |
| 5. D1 | Official-product-use / exclusive-distributed / declared-only webfont negatives and Named-gaps first-party-logo / 591-owned-webfont rows deleted. Live computed system stack kept. **[SUPERSEDED 2026-08-24 wave7 ledger sync — this summary did not rewrite the original YAML-identity Named-gaps first-party-mark destination. See Revision wave7 ledger sync.]** |
| 6. Machine gates | `node test-v2/tools/migrate-reference.mjs --brand 591 --gate-only` → PASS, problems []. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/591/DESIGN.md --check --require-portable-core --json` → exit 0, `portable_core: true`, SHA-256 `866a65df67ec20d40760d95a77ddadd9b739001c6fe6110e5cbdb30313c6e73c`. New F3 not required. Prior Post-F3 SHA `05899c6caf…cb7d9d` superseded (E2c). |

### F1 / F2 (v7 mandatory final passes; this resubmit)

F1: public-history now includes board→marketplace / mandated-price-transparency / actual-versus-asking; Filter Label unmerged-pair; Content evidence-class split; Distinctive stack reading is observed-CJK-stack-as-system-stack, not a 591-owned-webfont negative.

F2: Filter Label `#666666` / 14px grep-matched Type roles + unmerged-pair B2a. `classified property listing board` / `actually transacted` grep-matched Scope. `「我的詢問」` grep-matched capture record + Content §14 class, not live-homepage voice-sample list. Search CTA loading·error grep-matched applicable rows. Official-type-announcement / 591-exclusive-distributed / 591-owned-webfont negatives absent from DESIGN.md Named gaps. Compliance is not claimed stronger than the body (E2c). **[SUPERSEDED 2026-08-24 wave7 ledger sync — this resubmit F2 did not rewrite the original F2 C2 `not-applicable` sentence or the YAML-identity Named-gaps first-party-mark destination. See Revision wave7 ledger sync.]**

## Revision 2026-08-24 (wave7 ledger sync)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source remainder: `docs/reviews/t2-1-wave7-2026-08-24-sol-recheck.md` 591 condition 5. DESIGN.md not edited. New F3 not required.

| Item | Correction |
|---|---|
| 1. Named-gaps first-party-mark destination | Original YAML-identity row and original F2 no longer list Named gaps as a first-party-mark destination. Current: Google favicon URL → provenance Identity + portable Assets identity-boundary (URL string present in both). Named gaps 626–642 has no first-party-mark / first-party-logo / 591-owned-webfont row. |
| 2. Original F2 C2 | Original F2 “Search CTA / Secondary / search-input L/E/S as `not-applicable` rows” is superseded. Current body matches the wave7 sol-resubmit C2 map: Homepage/Listing Search CTA loading·error applicable; success omitted; Secondary L/E/S omitted; search-input loading not-applicable; search-input and Filter Range Input error·success omitted. |
| 3. F2 / E2c | Wave7 sol-resubmit summary table items 4–5 and its F2 paragraph are superseded as a substitute for those original source-row / F2 sentences. F2 for this revision is the greps below only. |

### F1 / F2 (v7; this ledger sync)

F1: unchanged from wave7 sol resubmit.

F2 (this revision; value + field/role context; not a claim that every source-row destination is closed):

- Google favicon URL `s2/favicons?domain=591.com.tw` → provenance Identity + portable Assets. Named gaps: no first-party-mark sentence
- Homepage/Listing Search CTA `loading` / `error` → applicable rows. Success omitted (not a `not-applicable` row)
- Secondary Text Button loading·error·success → omitted (not `not-applicable` rows)
- Homepage/Listing Search Input loading → `not-applicable`; error·success omitted

This log does not claim F2 completeness beyond those greps, and does not re-assert D1/C2/F2 compliance as closed (E2c).
