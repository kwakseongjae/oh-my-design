# EasyWallet migration log

Rulebook: **`docs/design-md-weight/MIGRATION_RULEBOOK.md` v9**
Source: `web/references/easywallet/DESIGN.md` (unmodified)
Destination: `docs/design-md-weight/migrated/easywallet/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/easywallet/provenance.md`
Date: 2026-08-26

Every row below was checked by grep against the three files before it was written (F2). Rows with two destinations name both (E2a). Rows re-grepped and corrected by the separate-session audit are listed in `audit-log.md`.

**F2 re-verification, value-placement revision (2026-08-26).** After the audit's six out-of-scope findings were repaired, every pointer in this table was re-grepped against the three files — 93 load-bearing needles across destinations, values, byte-exact strings, deletions, and the two carried conflicts — with zero misses. The table carries no line-number pointers; the audit had already replaced the one that existed (the B3 paragraph) with a positional reference, which still resolves: the paragraph sits under the **Motion — easings** table and above `## 3. Typography & Assets`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `verified`, `omd`) | 분리 → provenance | Portable file carries no frontmatter. Name survives as the H1 `EasyWallet Design System`. |
| YAML `logo` (`type: favicon`, Google favicon-service slug) | 분리 → provenance | Third-party favicon-service URL, not a first-party brand asset, so it is not named as a reusable asset in Typography & Assets. |
| YAML `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata kept as ledger fields (A1c), not dropped. |
| YAML `tokens.note` (primary/magenta/yellow/teal/body-text roles) | 분리 → provenance (verbatim) **+ 옮김 → Foundations semantic color** | The note's role assignments are the same values Foundations states as roles; the note text itself stays in the ledger. |
| YAML `tokens.colors` (14 values) | 옮김 → Foundations semantic color | Every value including `#006ba8`, `#413b3b`, `#bfbfbf`, `#595153`. `on-primary` kept as its own role rather than folded into Canvas (A4). `#000000` added as **Ink** from its two observed uses. |
| YAML `tokens.typography` (family + 10 roles) | 옮김 → Typography & Assets type roles | Unitless line heights kept as ratios — 1.0, 1.2, 1.3, 1.4, 1.5 (A1a). `letterSpacing` kept as `0.17em` / `0.03em` / `0.1em`. |
| YAML `tokens.spacing` | 옮김 → Foundations spacing **+ Layout & Platforms spacing and grid** | Same 4/8/12/16/24/32/48/64 scale is stated in both places, as in the source. |
| YAML `tokens.rounded` | 옮김 → Foundations shape **+ 분리 → provenance claim ledger** | All four values — 4 / 8 / 16 / 9999 — plus the 0px EasyCard nav-link case from §5. `lg: 16` arrives as **Large (16px)** in the Shape list and in the ledger's `tokens.rounded.*` row. §5's prose Border Radius Scale (0 / 4 / 8 / 9999) is a subset of the token record, not a narrowing rule over it; the earlier revision followed §5 and dropped the value, which the separate-session audit flagged as an A1 loss. No use is named for 16 because the source names none — the value lands, the use stays absent. |
| YAML `tokens.shadow` (`card`, `nav`) | 옮김 → Foundations elevation | Rendered as the §6 three-level table with both shadow strings intact. |
| YAML `tokens.components` (12 records, incl. `type:`) | 옮김 → Components & States | Primitive types preserved per component as `Control type: button / card / tab / badge / input` (A1b). Surface attribution follows the source: the corporate-site label stays on the buttons and on `#333333` prose, which §2 and §4 attribute to that site, and was removed from **Default Text Input** — the source records `use: "Standard form input"` with no surface, and the HTML comment scopes the corporate observation to the CSS color system, `btn` classes, and font family only, so labelling the input would manufacture an attribution across the two evidence domains Scope separates (B1). The four `type: button` accent buttons are declared as one **Action Button** with a four-row variant table that keeps each variant's own bg / fg / border / hover; no value is merged into another role (A4). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience scope claim + distinctive traits | Two-surface scope, scene system, hero, accent-palette jobs, type stack. |
| §1 "Taiwan's leading mobile payment app" | 삭제 | Unsourced market-position superlative; no supporting evidence in either inspection or in the cited company history. Recorded in the provenance deletion ledger. |
| §1 causal readings (BEEP! = NFC beep, teal = transit heritage) | 옮김 → Experience scope, with adjacent qualification | Kept as a derived editorial implementation inference, explicitly not EasyCard-/EasyWallet-authored (B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Primary / Brand accents / Neutral-and-surface groupings preserved with their use descriptions. |
| §3 Font Family + Hierarchy | 옮김 → Typography & Assets font evidence + type roles | Noto Sans TC, PingFang TC, 微軟正黑體 / Microsoft JhengHei, Arial fallback chain byte-exact (A5). |
| §3 caption tracking `0.17em` vs YAML `0.1em` | 옮김 → Typography & Assets (both values) **+ 분리 → provenance carried conflicts** | Conflict preserved as a conflict; neither value chosen. |
| §3 Principles (3 rules) | 옮김 → Typography & Assets type principles, with adjacent qualification | Editorial reading of the hierarchy, marked as such (B2a). |
| §4 Component Stylings | 옮김 → Components & States | All 12 declarations with anatomy and values; hover fills `#006ba8`, `#f59205`, `#d5007d` kept in the variant table. |
| §4 nav text `#ffffff` vs YAML `nav-item.fg: #000000` | 옮김 → Components & States (both values) **+ 분리 → provenance carried conflicts** | Conflict preserved; neither value chosen. |
| Footer **Verified** / Tier 1 / Tier 2 / Conflicts unresolved | 분리 → provenance | Freshness and source ledger (E1). getdesign.md and refero "not found" results kept as Tier 2 non-results. |
| §5 Layout Principles | 옮김 → Layout & Platforms **+ 옮김 → Foundations shape** | 100vh scenes s-1…s-6, fixed 58px nav with the 2px `#f7b146` border, centered containers, and right-side dot navigation went to Layout & Platforms; §5's Border Radius Scale (0 / 4 / 8 / 9999) went to Foundations shape, not to Layout (E2a). |
| §5 Whitespace Philosophy ("perceived luxury", "the scroll experience IS the design") | 옮김 → Layout & Platforms, with adjacent qualification | Rewritten as an explicitly derived editorial reading rather than an authored doctrine (B2a). |
| §6 Depth & Elevation table | 옮김 → Foundations elevation | Three levels verbatim. |
| §6 Shadow philosophy | 옮김 → Foundations elevation, with adjacent qualification | The "scene shifts replace elevation" reading is marked derived (B2a). |
| §7 Do's | 옮김 → Experience principles + Foundations color roles **+ Typography type principles + Layout & Platforms** | Brand application rules; not placed in Governance controlled copy. The seven items split four ways: the four color-role items and the teal-heritage item into Foundations color roles and Principle 1, the weight-700 display item and the 0.03–0.17em CJK letter-spacing item into Typography type principles, and the full-viewport scene item into Layout & Platforms (E2a). |
| §7 Don'ts | 옮김 → Experience avoid, with adjacent qualification | Six editorial prohibitions marked as derived. Two more derived items arrive from the §9 Iteration Guide (see that row) and the evidence boundary closes the list, so the section reads eight derived items plus a ninth boundary item. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms breakpoints / touch targets / collapsing | 768px, 1200px, 1440px, ~75px, 17vw, 51px, 15–16px preserved. |
| §8 circle `17vw` | 옮김 → Components & States (Feature Selector Circle sizing) **+ Layout & Platforms touch targets** | Present in both places. |
| §9 Quick Color Reference | 삭제 | Restatement of §2 values that are already in Foundations semantic color; no unique evidence value (A3 checked value-by-value). |
| §9 Example Component Prompts — unique component values | 옮김 → Components & States | Feature-circle inactive treatment (white fill, `#e4007f` border) and active drop-shadow; download-CTA two-column iOS/Google Play layout with vertical rule and center alignment; nav app-logo placement. These existed only in §9, and Components had a slot for them (A3). |
| §9 Example Component Prompts — "five equal circles" | 삭제 | Illustrative count inside a tool-facing prompt that disagrees with the component's own 四大優勢 four-advantage naming. Deleted rather than carried as a third conflict: unlike the nav-foreground and caption-tracking disagreements — the same field recorded twice at the same evidence class, with nothing ranking either record — this one sets brand-published copy against a number inside a prompt wrapper whose whole class was deleted, so it is decidable. The provenance **Carried conflicts** section states that policy and the deletion ledger records the outcome. |
| §9 prompt wrappers and Iteration Guide | 삭제 | Tool-facing command/prompt packaging with no Core slot; deleted rather than delegated. Five of the seven Iteration Guide constraints were already carried by §2/§7 into Foundations, Experience, Typography, and Layout. The remaining two — magenta “not for text” and yellow “always secondary” — exist only in §9, so the §9-only evidence is now **옮김 → Experience avoid** as two further items under the same derived qualification (A3). The Avoid preamble count moves from six to eight derived items, the evidence boundary from seventh to ninth. Only the prompt/command packaging around them is deleted. |
| §10 Voice & Tone table + forbidden register | 옮김 → Content & Locales, with adjacent qualification | Tone characterizations marked as a derived editorial reading; the register constraint likewise. |
| §10 verbatim voice samples (3 verified) | 옮김 → Content & Locales verbatim samples **+ 분리 → provenance claim ledger** | 「一卡一付 無現生活更進一步！」, 「智慧升級，放心悠遊！」, 「悠遊付=最聰明的電子錢包」 kept byte-exact with the fullwidth punctuation; the live-verification date sits in the ledger (A5, E2a). |
| §10 CTA and feature-name strings | 옮김 → Content & Locales tone table + terminology | 立即下載, DOWNLOAD NOW, 嗶乘車, 掃碼付款, 自動加值 byte-exact, English gloss placed beside the Chinese rather than replacing it (A5). |
| §11 Brand Narrative | 옮김 → Experience scope | 2002 EasyCard launch, MRT/buses/YouBike/convenience stores, physical-to-digital continuation, and the published lines 從實體到數位 / 最佳拍檔 / 無現生活 悠遊無限 (also restated in Content & Locales terminology). |
| §11 interpretive framing ("continuity not disruption", "BEEP! bridges the NFC generation", inherited trust) | 옮김 → Experience scope, with adjacent qualification | Named explicitly as four readings above the evidence line (B2a). |
| §12 Principles (5, with UI implications) | 옮김 → Experience principles, with adjacent qualification | All five names and UI implications carried. Two source specifics were softened by an earlier revision and are restored here: “20+ years of EasyCard trust” (principle 1) and “the **MRT** gate” (principle 5), which had become “the EasyCard relationship” and “the transit gate”. Not byte-identical prose otherwise, and the log does not claim verbatim: “not away from it” reads “rather than away from it”, “never fully abandon the card-management metaphor” reads “keep the card-management metaphor”, “brand-heritage touchpoint” reads “heritage touchpoint”, “primary nav” reads “primary navigation”, “no fintech acronyms unexplained” reads “no unexplained fintech acronyms”, the straight quotes around 「BEEP!」/嗶乘車/掃碼付款 use this document's 「」 convention, and principle 5's opening clause “EasyWallet is a transit-adjacent product” is dropped as a characterization the Scope already carries at length. Qualification uses the approved form: derived editorial implementation inference, not EasyCard-/EasyWallet-authored, not a separately published UI specification (B2a). |
| §13 Personas (3 fictional archetypes) | 삭제 | The source labels them fictional archetypes. Not promoted to Audience or to `primary-tasks`, and **not** rehosted in provenance — the demographic segments are not re-listed there either (D2). Audience states stakeholder groups only. |
| §14 States (8 rows) | 옮김 → Components & States product state treatments | All eight rows carried with their values intact; 「感應失敗，請再試一次」 and `#b3d8ee` byte-exact (A2, A5). Not byte-identical prose, and the log no longer says verbatim: three rows take an inserted article (“explaining there are no records”, “with a plain-language”, “always provides a clear reason”) and the quoted Chinese uses this document's 「」 convention in place of the source's straight quotes. Adjacent qualification distinguishes the one corroborated row (the `#66ecd2` launch overlay, also in Foundations color and distinctive traits) from the seven in-app rows, which are marked derived and are not promoted to tokens for the declared web components. |
| §14 → per-component applicability | 옮김 → Components & States state tables | Five interactive components declare all seven canonical states; `default` and `focus-visible` are applicable everywhere. Missing observation omits the treatment value only and is never a `not-applicable` reason (C1). The six `not-applicable` rows — `loading`, `error`, and `success` on Nav Menu Item, and the same three on Feature Selector Circle — carry role reasons, not absence reasons (C2). (An earlier wording said “the three … rows on Nav Menu Item and on Feature Selector Circle”, which reads as three in total; it is three each.) Four card/badge declarations use `kind: non-interactive` with a reason instead of a map. No graph delegation. |
| §15 durations (100ms / 150ms / 500ms / 750ms) | 옮김 → Foundations motion | All four kept with their uses. |
| §15 easings | 옮김 → Foundations motion | `cubic-bezier(0.39, 0.575, 0.565, 1)` kept for both `ease-enter` and `ease-exit` with its EasyCard-CSS attribution, plus `0.5s ease`. Not an unattributed curve, so the T1-3 curve deletion does not apply. |
| §15 motion rules + reduced-motion | 옮김 → Foundations motion, with adjacent qualification | All three §15 motion sentences are preserved: the two-register split, the teal loading overlay fading in and out on a slow opacity transition (now a **Loading overlay** paragraph between the derived-motion qualification and **Reduced motion**), and the `prefers-reduced-motion: reduce` behavior. The overlay fade sits under the same derived qualification as the other two, which now names it explicitly. The adjacent paragraph names the five evidence kinds required before promotion — transition properties, animation name, duration, easing, reduced-motion behavior — under a per-component computed-observation gate, and states that a curve alone is not sufficient (B3; verified present directly under the **Motion — easings** table). |
| HTML comment source block (Tier 1 method, key observations, verification dates) | 분리 → provenance raw observations + claim ledger | Playwright `getComputedStyle` method note and all nine key observations preserved — the eight `rgb()` colors with their hex equivalents, plus the body-font stack byte-exact. The 0.5s scene-transition row in the same table comes from §1/§5, not from the comment. |
| HTML comment "interpretive claims are editorial connections" | 옮김 → the qualifications throughout the portable body | Turned from a hidden note into adjacent qualifications in Experience, Foundations, Typography, Layout, Content, and the product state table, so a standalone reader sees the evidence class without the sidecar (B2, E1). |

## Notes on withheld material

| Item | Why it is absent |
|---|---|
| `authenticated-account` and `offline signage` as out-of-scope or unresolved domains | Drafted, then removed. Neither term appears anywhere in the source, so naming it — even as a gap — would assert a domain the source never establishes (D1a). `app` and `promotional campaign` are established by the source and are kept. |
| Font license | The source records none. Omitted rather than sourced from elsewhere. |
| `focus-visible` treatment value | The source never records a focus observation. Applicability is declared by control meaning; no treatment value is emitted (B1). |

## Named gaps in the portable body

Three, all grounded in domains the source itself establishes: focus-visible visual treatment for the declared controls; computed screen values behind the product-level state and motion descriptions; a distributable logo file behind the app logo mark.
