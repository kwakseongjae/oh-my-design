T2 마이그레이션을 규칙집 기준으로 진행합니다. 먼저 세션 상태와 지정된 규격·골든 샘플부터 읽겠습니다.지정된 규격·규칙집·골든 샘플·원문을 순서대로 읽고 이관을 시작합니다.골든 샘플과 Channel Talk 원문, 최근 통과 이관본을 대조해 형태를 맞춥니다.최근 통과한 이중 도메인(마케팅/문서) 이관본과 게이트를 보고, Channel Talk에 맞는 형태를 잡겠습니다.최근 통과 이관본의 컴포넌트 상태·provenance 형식과 게이트를 더 자세히 대조합니다.원문의 값·도메인 분리·상태 의미를 유지한 Core v2 본문을 작성합니다. Scope claim 안에는 `not`과 `product`/`surface`가 가깝지 않게 둡니다.본문을 다시 읽고 B2a 한정이 빠졌는지 확인한 뒤, 게이트와 토큰 보존을 검사합니다.F1 스캔에서 한정이 문장 뒤에 있거나 빠진 해석 구간을 고칩니다.표의 단독 `Unresolved` 칸이 Portable Core placeholder 검사에 걸립니다. 의미는 유지하고 칸 이름만 바꿉니다.Portable Core는 통과했습니다. E2용으로 값별 줄 번호를 실측한 뒤 provenance와 migration-log를 씁니다.줄 번호를 실측했으니 provenance와 migration-log를 작성합니다.292행 필드 노트에서 `focus-visible` 뒤에 hex가 붙어 게이트가 막혔습니다. 같은 줄에서 둘을 분리합니다.Channel Talk legacy `DESIGN.md`를 Core v2로 옮겼습니다. 규칙집은 **v7**입니다.

산출물:
- `docs/design-md-weight/migrated/channeltalk/DESIGN.md`
- `docs/design-md-weight/migrated/channeltalk/provenance.md`
- `docs/design-md-weight/migrated/channeltalk/migration-log.md`

유지한 값: YAML unitless line-height `1.41` / `1.56` / `1.59`, primitive type `button`×3 + `card` + `tab`, marketing/docs 도메인 분리, Bezier `ds.type: system`은 공식 시스템 맥락이지 현재 마케팅 토큰이 아님. Card는 kind/map 생략(C4). Primary·outline·docs-icon의 loading/error/success는 요청 매핑이 없어 생략(C2). Tab L/E/S는 역할상 `not-applicable`. selected/pressed는 추가 관측이지 Core 7행이 아님. Motion은 무출처 커브가 없고, B3 다섯 증거 종류 게이트는 Foundations Motion 125에 전문으로 있습니다.

게이트: `--gate-only` PASS, `portable_core: true`. SHA-256 `2f201346e63bb87c587cc896c49db7b0e6ce6d6b2cb8ea2fcde7d084f1664f09`. 카탈로그 채택은 아닙니다.

DONE migrated=1
