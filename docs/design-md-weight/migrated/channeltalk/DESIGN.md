# Channel Talk Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Treating Channel Talk as a customer-service platform that joins live chat, team inbox, calls, marketing, workflows, and AI assistance around one ongoing customer relationship, and treating catalog homepage `https://channel.io` as identity rather than an extra captured host beyond the five named URLs below, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Channel Talk is a customer-service platform that joins live chat, team inbox, calls, marketing, workflows, and AI assistance around one ongoing customer relationship. Catalog homepage identity is `https://channel.io`.

Treating the following five URLs as the named evidence domains of this reconstruction, including values-stay-attached-to-the-surface-that-established-them, KR-or-US-marketing-value-not-a-stand-in-for-English-documentation, documentation-scale-not-a-stand-in-for-current-marketing, Bezier-repository-context-not-a-stand-in-for-uninspected-app-UI, and official-rebrand-writing-as-narrative-rather-than-a-token-sheet, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. This contract covers five current first-party surfaces from the 2026-07-12 packet: KR marketing `https://channel.io/kr` (marketing-product), US marketing `https://channel.io/us` (marketing-product), product updates `https://docs.channel.io/updates/en/articles/Notice-Channel-Talk-Major-Updates--b3d45997` (product-doc), help `https://docs.channel.io/help/en/articles/94f34984` (product-doc), and official rebrand writing `https://channel.io/kr/blog/articles/rebranding-channeltalk-3aff8113` (official-history). Color, type, and component values below stay attached to the surface that established them. A KR or US marketing value is not a stand-in for English documentation. A documentation scale is not a stand-in for current marketing. Bezier repository context is not a stand-in for uninspected app UI.

Source token note: Five current first-party surfaces. Marketing and product-doc domains are intentionally separate. Legacy universal Cobalt, BildV5, generic form, synthetic state, and inferred motion claims are not promoted. Treating that note as a register split — `#242428` is catalog `primary_color` and the harvested charcoal conversion fill rather than foreground `#000000` and rather than dark editorial surface `#3a3530`; `#ffffff` canvas is unmerged from on-primary `#ffffff`; `#f7f6f3` is the warm marketing surface rather than canvas; Pretendard is the marketing family rather than the Inter documentation alias; Bezier is official product-system context rather than a current marketing token sheet; and Cobalt `#329BE7`, BildV5, generic form, synthetic state, and inferred motion stay unpromoted — is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

The following atmosphere readings — warmer-editorial-system, black-type-with-cream-`#f7f6f3`, dark-charcoal-`#242428`-conversion-controls, generous-photography, rounded-35px-information-cards, conversational-and-human-despite-operational-depth, warmth-as-intentional-customer-first-expression, and official-rebrand-writing-as-narrative-context-not-interface-tokens — are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification. Current Korean and US pages pair black type with cream `#f7f6f3`, dark charcoal `#242428` conversion controls, generous photography, and rounded 35px information cards. Official rebrand writing explains that this warmth is intentional: Channel Talk wanted a clearer, more authentic expression of its customer-first culture rather than a generic software identity. Official rebrand writing provides that narrative context; it does not by itself supply interface tokens.

The following marketing-versus-documentation register readings — Pretendard-and-18px-reading-copy-and-44px-sectional-headings-and-full-pill-actions-on-marketing, Inter-alias-and-tighter-17px-27px-reading-scale-and-compact-6–8px-controls-on-English-documentation, two-domains-stay-separate-rather-than-one-fictional-stack, Bezier-as-official-system-history-rather-than-a-substitute-for-uninspected-app-UI, and Bezier-color-becomes-current-marketing-token-only-when-inspected-surface-confirms-it — are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification. Marketing loads Pretendard and uses 18px reading copy, 44px sectional headings, and full-pill actions. English documentation loads an Inter alias and uses a tighter 17px/27px reading scale with compact 6–8px controls. Marketing and documentation stay separate evidence domains. Bezier remains official evidence that Channel maintains a real product design system. A Bezier color becomes a current marketing token only when the inspected surface confirms it. YAML `ds.type: system`; `ds.name: Bezier`; `ds.url: https://github.com/channel-io/bezier-react`.

Treating the following public-history facts as narrative rather than interface tokens — conversation-as-a-durable-operating-capability-rather-than-a-support-widget, official-rebrand-as-a-clearer-expression-of-identity-and-culture, official-product-updates-combining-messaging-operations-with-AI-assisted-service, Bezier-as-a-separate-but-complementary-signal-of-reusable-product-primitives, conversation-as-the-place-where-support-sales-and-long-term-customer-understanding-meet, and reconstruction-does-not-use-Bezier-repository-to-fabricate-current-private-app-values — is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Channel Talk presents customer communication as a durable operating capability rather than a support widget. Official rebrand material describes the visual change as a way to express the company's identity and culture more honestly, while official product updates show the platform continuing to combine messaging operations with AI-assisted service. The public Bezier repository is a separate but complementary signal: Channel has invested in reusable product primitives. This reconstruction does not use that repository to fabricate current private-app values. Across these sources, the company consistently frames conversation as the place where support, sales, and long-term customer understanding meet. Official rebrand writing, official product updates, and the Bezier repository provide that context; they do not by themselves supply interface tokens.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three YAML component-use strings below as Primary tasks, and not lifting §13 support-lead / frontline-agent / growth-or-commerce-operator groups, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

- Sign up or convert on current KR/US marketing (YAML use: `Primary signup and conversion action on current KR/US marketing`).
- Switch an interactive marketing category tab (YAML use: `Interactive category switcher on current marketing`).
- Use a compact icon action in product documentation (YAML use: `Compact icon action in product documentation`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 entries as public-material task contexts rather than verified biographical personas, treating those entries as not Audience biographies and not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. No individual personas are promoted. Public product material supports task contexts, not verified biographical personas:

- A support lead reviewing conversations and adopting current AI-assisted operations.
- A frontline agent using product documentation to configure or troubleshoot a customer channel.
- A growth or commerce operator evaluating whether Channel Talk fits an ongoing customer relationship workflow.

Treating project-specific names, ages, company sizes, locations, and quantitative goals as intentionally unspecified in this public reference, and requiring them to come from the product brief rather than this reconstruction, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Project-specific names, ages, company sizes, locations, and quantitative goals are intentionally unspecified and must come from the product brief rather than this public reference.

### Distinctive traits

The following unmerged-role extras — treating `#242428` as catalog `primary_color` and conversion fill rather than foreground `#000000` or dark editorial `#3a3530`, treating `#ffffff` canvas as unmerged from on-primary `#ffffff`, treating `#f7f6f3` as warm marketing surface rather than canvas, treating `#716f6d` as secondary copy and inactive tab labels rather than foreground, treating `#e4e4e5` as hairline rather than a fill, treating Pretendard as the marketing family rather than the Inter documentation alias, treating 35px cards as unmerged from full-pill YAML `full` 9999 and from control 6 / utility 8, treating 18px marketing body as unmerged from 17px documentation body, treating hover / focus / Inbox / form / motion as unpromoted rather than inferred, and treating public Bezier implementation as product-system context not substituted for uninspected app UI — are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification.

- Warm cream `#f7f6f3` marketing surfaces with black type and `#242428` primary actions
- Pretendard on current KR/US marketing; loaded Inter alias on English product documentation
- 35px editorial cards and full-pill conversion controls
- Selected marketing tabs captured through six safe interaction expansions
- Public Bezier implementation retained as product-system context, not substituted for uninspected app UI

### Principles

These four items are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification.

1. **The customer is the answer.** Start from a real conversation or task rather than feature spectacle.
2. **Warmth supports operational clarity.** Editorial cream, photography, and rounded cards humanize a complex service platform.
3. **Evidence domains stay separate.** Marketing, documentation, Bezier, and private product surfaces cannot silently overwrite one another.
4. **AI should remove routine work.** Describe assistance in terms of what people can focus on next, without inventing outcomes.

Treating the following as a capture-bound application of source §7 Do’s, including current-surface-local-evidence-and-marketing-versus-documentation-roles, warm-cream-charcoal-actions-and-editorial-card-geometry-together-on-marketing, Bezier-as-official-system-history-and-implementation-context, Inter-documentation-scale-only-for-documentation-like-surfaces, and reuse-strengthening-continuity-rather-than-flattening-every-domain-into-one-marketing-style, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

- Use current surface-local evidence and label marketing versus documentation roles.
- Keep warm cream, charcoal actions, and editorial card geometry together on marketing surfaces.
- Preserve Bezier as official system history and implementation context.
- Use the Inter documentation scale only for documentation-like surfaces.
- Strengthen continuity between people and tools rather than flattening every captured domain into one marketing style.

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including Cobalt-not-the-universal-current-primary, no-system-font-BildV5-or-Noto-declaration-as-the-loaded-surface-family, no-Inbox-components-or-semantic-states-from-generic-SaaS-conventions, no-Cobalt-product-controls-inbox-states-inputs-or-dialogs-unless-a-current-product-source-is-supplied, and no-transfer-of-marketing-full-pill-geometry-to-uninspected-product-controls. They are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification.

- Do not call Cobalt the universal current primary without current product evidence.
- Do not substitute a system font, BildV5, or Noto declaration for the loaded surface family.
- Do not generate Inbox components or semantic states from generic SaaS conventions.
- Do not add Cobalt product controls, inbox states, inputs, or dialogs unless a current product source is supplied.
- Do not transfer marketing's full-pill geometry to uninspected product controls.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#242428`-not-foreground-`#000000`-not-dark-editorial-`#3a3530`, canvas-white-not-on-primary-white-as-a-second-paint, warm-surface-`#f7f6f3`-not-canvas, secondary-`#716f6d`-not-foreground, hairline-`#e4e4e5`-not-a-fill, on-primary-white-as-the-filled-action-label, Cobalt-`#329BE7`-not-a-current-primary, YAML-component-fields-staying-on-those-controls, and authenticated-Inbox-status-colors-unnamed-in-this-pass — are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification. Catalog `primary_color` `#242428` is YAML `primary` and the harvested conversion fill; it is not Foreground `#000000` and not Dark editorial surface `#3a3530`. YAML `canvas` `#ffffff` is the page and neutral content canvas. YAML `on-primary` `#ffffff` is text on the filled conversion action. Same hex; named jobs stay unmerged. YAML `surface` `#f7f6f3` is the warm card and section background, not canvas. YAML `secondary` `#716f6d` is marketing supporting copy and inactive tab labels. YAML `hairline` `#e4e4e5` is the current 1px marketing-card border, not a fill. YAML component fields such as marketing-primary `fg` `#ffffff` stay on those controls. They are not extra general inks. Bezier's historical Cobalt `#329BE7` is not promoted as a universal current primary: it was not the conversion color on the five captured public surfaces. Authenticated Inbox status colors remain unnamed in this pass.

- **Primary / conversion** (`#242428`): catalog `primary_color`. YAML `primary`. Repeated filled signup and conversion controls.
- **Canvas** (`#ffffff`): YAML `canvas`. Page and neutral content canvas.
- **Warm surface** (`#f7f6f3`): YAML `surface`. Repeated card and section background.
- **Foreground** (`#000000`): YAML `foreground`. Dominant marketing and documentation text.
- **Secondary** (`#716f6d`): YAML `secondary`. Marketing supporting copy and inactive tab labels.
- **Dark editorial surface** (`#3a3530`): YAML `dark-surface`. Repeated customer-case media field.
- **Hairline** (`#e4e4e5`): YAML `hairline`. Current 1px marketing-card border.
- **On-primary** (`#ffffff`): YAML `on-primary`. Text on the filled conversion action. Same hex as canvas; jobs stay unmerged.

### Spacing

YAML `spacing`: xs 4, sm 6, md 10, lg 20, xl 30. Those YAML numbers are recorded without a px suffix; none is added here. Body layout names generous 30–45px internal spacing on marketing and a 10px internal gap in the captured card variant.

Treating those YAML numbers as recorded without a required px suffix, treating body 30–45px and the 10px card-internal gap as body-recorded observations rather than a converted YAML scale, and treating harvested control padding as component fields rather than replacements for the YAML spacing scale, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

### Shape

YAML `rounded`: control 6, utility 8, card 35, full 9999.

The following local-geometry reading, including Control-6px, Utility-8px, Editorial-card-35px, Full-pill-9999, and not-a-universal-radius-for-uninspected-product-controls, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

- Control (YAML `control` 6): compact documentation icon action at 6px; documentation controls remain compact at 6–8px
- Utility (YAML `utility` 8): documentation compact utility geometry
- Editorial card (YAML `card` 35): current KR/US marketing information card at 35px
- Full (YAML `full` 9999): marketing primary, outline, and category-tab hit areas at `9999px`

Treating 6 / 8 / 35 / 9999 as local harvested geometry rather than a universal radius for every unlisted control, and treating marketing full-pill geometry as not transferable to uninspected product controls, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

### Elevation

YAML `tokens.shadow.flat`: `none`.

The following elevation readings, including current-promoted-surfaces-are-flat, marketing-cards-use-background-border-radius-and-photography, no-reusable-shadow-token, and documentation-overlays-and-authenticated-product-elevation-unnamed-in-this-pass, are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification. Current promoted surfaces are flat. Marketing cards use background, border, radius, and photography for separation; no reusable shadow token was established. Documentation overlays and authenticated product elevation remain unnamed in this pass.

### Motion

No reusable current duration or easing token was established by this capture. The six tab expansions prove state change, not a universal animation specification; motion values remain absent. Treating that source-stated absence, including six-tab-expansions-prove-state-change-not-a-universal-animation-specification, as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. No motion token is promoted.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Bezier-as-official-product-use-not-proof-for-every-current-private-app-surface, Pretendard-live-marketing-484, Inter-aliases-live-docs-581, no-Channel-exclusive-redistributable-brand-font, declared-only-NotoSansKR-Poppins-fallback-and-unused-locale-declarations, NotoSansJP-as-one-visible-body-use-not-the-universal-family, BildV5-omitted-because-no-declaration-or-visible-use, authenticated-app-overrides-and-unobserved-locale-behavior-unnamed, and source-Unresolved-class-name-not-a-fillable-token, are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | Bezier is an official product design-system implementation, but the inspected repository context is not treated as proof for every current private app surface. |
| Live surface-use | Pretendard loaded/high with 484 visible uses on marketing; Inter aliases loaded/high with 581 combined visible uses on product docs. YAML `family.ui` / `family.marketing` `Pretendard` and YAML `family.docs` `Inter` name those live families. |
| Official distributed asset | No Channel-exclusive redistributable brand font is promoted. |
| Declared-only | NotoSansKR, Poppins, fallback faces, and unused locale declarations remain metadata only. |
| Captured-scope unnamed | Authenticated app overrides and unobserved locale behavior remain unnamed in this pass. Source evidence-class label is Unresolved; that class name is not a fillable token. |

Treating `NotoSansJP` as one visible body use rather than the universal family, and treating BildV5 as omitted because the fresh capture showed no declaration or visible use, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. `NotoSansJP` was loaded for one visible body use and remains a surface-local observation, not the universal family. No BildV5 declaration or visible use appeared in the fresh capture, so it is omitted.

### Family

- **Current visible marketing UI family:** `Pretendard` — YAML `family.ui` / `family.marketing`
- **Current visible documentation family:** `Inter` (loaded alias) — YAML `family.docs`
- **Surface-local live companion:** `NotoSansJP` — one visible body use; not the universal family

The following font-use boundary, including system-or-declared-only-faces-not-Pretendard, Inter-alias-not-a-second-marketing-family, NotoSansJP-not-the-universal-family, NotoSansKR-declared-only-not-NotoSansJP, BildV5-not-a-loaded-face, declared-only-fonts-not-loaded-Channel-Talk-faces, do-not-replace-Pretendard-on-current-KR-US-marketing-with-the-Inter-documentation-alias, and do-not-present-Inter-as-Pretendard, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Do not present a system stack, BildV5, or a Noto declaration as Pretendard. Do not replace Pretendard on current KR/US marketing with the Inter documentation alias. Do not present Inter as Pretendard. Do not present `NotoSansKR` as `NotoSansJP`. Declared-only fonts must not be presented as loaded Channel Talk faces.

### Type roles

Verified YAML `lineHeight` values are the unitless ratios `1.41`, `1.56`, and `1.59`. The following ratio-versus-size-local reading, including unitless-ratios-not-fixed-px, marketing-heading-44-not-card-heading-25-not-article-heading-24, marketing-body-18-not-docs-body-17, marketing-tab-16-not-marketing-body-18, card-heading-and-article-heading-as-body-table-only, and yaml-has-no-lineHeight-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Those px figures are size-local observations, not replacements for the ratios. Card heading and Article heading are body-table roles; YAML has no `lineHeight` field for them, and none is invented.

| Role | Font | Size | Weight | Line height (YAML) | Body observation | Tracking | Use (YAML) / Notes |
|---|---|---:|---:|---:|---|---|---|
| Section heading | Pretendard | 44px | 600 | 1.41 | 62px at this size | YAML `-0.88` / body `-0.88px` | Large current marketing headings |
| Card heading | Pretendard | 25px | 600 | — | 32px | `-0.5px` | Marketing card heading (body table only) |
| Body / CTA | Pretendard | 18px | 400 | 1.56 | 28px at this size | YAML `-0.18` / body `-0.18px` | Current marketing body and action copy |
| Category tab | Pretendard | 16px | 600 | 1.56 | 25px at this size | YAML `-0.16` / body `-0.16px` | Interactive marketing category tabs |
| Article heading | Inter | 24px | 700 | — | 34px | `-0.5px` | English documentation article heading (body table only) |
| Article body | Inter | 17px | 400 | 1.59 | 27px at this size | YAML `-0.1` / body `-0.1px` | English product documentation body |

### Assets

Treating catalog logo metadata as type `github`, slug `channel-io`, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

Treating generous photography on current KR/US marketing as captured/recorded marketing-surface imagery; ownership unclaimed, not replacing it with invented brand-color decoration, and treating Bezier `og_image` as repository context in the source ledger rather than a current marketing token, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, including because-inspectable-evidence-did-not-establish-inputs-dialogs-toasts-authenticated-inbox-rows-or-error-success-patterns-at-the-required-boundary, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. The source state contract, preserved here while the catalog graph is not adopted: Only the current marketing tab selected state and a compact documentation control's pressed state were safely observed. No canonical empty, loading, error, success, disabled, or authenticated Inbox state is promoted. YAML marketing-primary and marketing-outline `states`: default captured; no hover or focus token promoted. Inputs, dialogs, toasts, authenticated inbox rows, and error/success patterns are not promoted because current inspectable evidence did not establish them at the required boundary.

Treating Core §4.4 applicability as judged by control meaning rather than capture completeness, treating a later generic Focus observation as not `focus-visible` evidence, treating selected-and-pressed as additional named observed states rather than Core applicability rows or `focus-visible` treatments, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, and treating Marketing information card as having no interactive-kind confirmation, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no `focus-visible` treatment; YAML says no hover or focus token promoted. `focus-visible` visual treatment remains omitted. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Selected (marketing tab) and pressed (documentation icon action) are additional named observed states; they are not among the seven Core applicability rows and are not copied onto a `focus-visible` row.

Marketing information card has YAML `type: card` and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Marketing primary action

- Role: primary signup and conversion action on current KR/US marketing
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#242428`
- Text: `#ffffff`
- Radius: `9999px`
- Padding: `10px 22px`
- Font: YAML `18px / 400`; body Pretendard 18px/400/28px
- Use: YAML `Primary signup and conversion action on current KR/US marketing`
- YAML `states`: default captured; no hover or focus token promoted
- Observed: default only
- Field note: The following unmerged-field reading, including this-fill-as-catalog-`primary_color`-not-foreground-`#000000`, on-fill-white-not-canvas-as-a-second-paint, 9999px-not-card-35px-not-control-6px, 18px-400-not-tab-16px-600, and YAML-states-not-merged-into-Use, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. `#242428` is this control’s fill and catalog `primary_color`. `#ffffff` is this control’s on-fill label (on-primary). Radius `9999px` is this pill; it is not card 35px or docs 6px. Font 18px / 400 is this control’s field, not Category tab 16px / 600.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the current KR/US marketing primary conversion action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary signup action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the primary signup and conversion action on current KR/US marketing; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Marketing outline action

- Role: secondary marketing conversion action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `transparent`
- Text: `#000000`
- Border: `1px solid #242428`
- Radius: `9999px`
- Padding: `10px 22px`
- Font: YAML `18px / 400`; body same full-pill geometry and type as the primary action
- Use: YAML `Secondary marketing conversion action`
- YAML `states`: default captured; no hover or focus token promoted
- Observed: default only
- Field note: The following unmerged-field reading, including transparent-not-a-second-canvas, black-label-not-on-primary-white, 1px-`#242428`-border-not-hairline-`#e4e4e5`, 9999px-matching-primary-not-card-35px, and 18px-400-not-tab-16px-600, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Transparent fill is this control’s field, not canvas. `#000000` is this control’s label (Foreground), not on-primary `#ffffff`. Border `1px solid #242428` is this outline, not hairline `#e4e4e5`. Radius `9999px` matches the primary pill; it is not card 35px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the secondary marketing conversion action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary conversion action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the secondary marketing conversion action; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Marketing information card

- Role: current KR/US marketing information card
- Type: card
- Anatomy: surface
- Background: `#f7f6f3`
- Border: `1px solid #e4e4e5`
- Radius: `35px`
- Padding: `30px 35px`, with 10px internal gap in the captured variant
- Use: YAML `Current KR/US marketing information card`
- Observed: default only
- Field note: The following unmerged-field reading, including warm-surface-not-canvas, hairline-border-not-outline-`#242428`, 35px-not-pill-9999px, and 30px-35px-padding-plus-10px-gap-as-this-card, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. `#f7f6f3` is this card’s fill (Warm surface). `1px solid #e4e4e5` is this card’s hairline. Radius 35px is this editorial card; it is not YAML `full` 9999. Padding `30px 35px` and the 10px internal gap are this captured variant.

Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Marketing category tab

- Role: interactive category switcher on current marketing
- Kind: interactive
- Type: tab
- Anatomy: label
- Background: `transparent`
- Text: `#716f6d`
- Radius: `9999px`
- Padding: `6px 20px`
- Font: YAML `16px / 600`; body Pretendard 16px/600/25px
- Use: YAML `Interactive category switcher on current marketing`
- YAML `states`: selected and tab-selected observed in six safe expansions
- Observed: default; selected / tab-selected in six safe tab expansions across KR/US pages
- Field note: The following unmerged-field reading, including transparent-not-canvas, secondary-`#716f6d`-not-foreground, 9999px-hit-area-not-card-35px, 6px-20px-not-primary-10px-22px, 16px-600-not-body-18px-400, and selected-tab-selected-as-additional-named-observed-state-not-Core-row, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Transparent fill is this tab’s field. `#716f6d` is this tab’s resting label (Secondary). Radius `9999px` is this hit area; it is not card 35px. Font 16px / 600 is this control’s field, not Body / CTA 18px / 400. Selected / tab-selected is an additional named observed state, not a Core applicability row and not a `focus-visible` treatment.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the interactive marketing category tab |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A category tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A category tab selects a category; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus resting, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is category selection, not action-outcome confirmation |

Additional observed named state: selected / tab-selected (six safe expansions across KR/US pages).

### Documentation icon action

- Role: compact icon action in product documentation
- Kind: interactive
- Type: button
- Anatomy: icon control
- Background: `transparent`
- Text: `rgba(0,0,0,0.85)`
- Radius: `6px`
- Font: loaded Inter alias (YAML has no font field; none is invented)
- Use: YAML `Compact icon action in product documentation`
- YAML `states`: pressed state observed on current documentation controls
- Observed: default; pressed on current documentation controls
- Field note: The following unmerged-field reading, including transparent-not-canvas, `rgba(0,0,0,0.85)`-as-this-control-fg-not-foreground-`#000000`-as-a-second-paint, 6px-not-utility-8px-not-card-35px-not-pill-9999px, Inter-alias-not-Pretendard, and yaml-has-no-font-field-none-invented, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Transparent fill is this control’s field. `rgba(0,0,0,0.85)` is this control’s foreground field; it is not merged into Foreground `#000000`. Radius 6px is this compact documentation control (YAML `control`); it is not utility 8px, card 35px, or pill `9999px`.
- Treating pressed as an additional named observed state rather than a Core applicability row is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Pressed is an additional named observed state, not a Core applicability row.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the compact documentation icon action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A documentation icon action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a compact icon action in product documentation; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: pressed (current documentation controls).

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — YAML-spacing-xs-4-sm-6-md-10-lg-20-xl-30, wide-centered-sections, strong-vertical-breaks, 30–45px-internal-spacing, 35px-editorial-cards-versus-6–8px-documentation-controls, marketing-versus-documentation-density-stay-separate, full-pill-and-rounded-editorial-cards-retained-while-horizontal-section-padding-and-tab-padding-reduce-on-narrower-layouts, private-product-breakpoints-and-mobile-native-behavior-unnamed-in-this-pass, and do-not-transfer-marketing-full-pill-geometry-to-uninspected-product-controls — are a derived editorial implementation inference from the verified surfaces; they are not Channel Talk-authored or a separately published UI specification. YAML spacing is xs 4, sm 6, md 10, lg 20, xl 30. Body layout names wide centered marketing sections with strong vertical breaks and generous 30–45px internal spacing. Editorial cards use large 35px corners; documentation controls remain compact at 6–8px.

- Marketing uses wide centered sections with strong vertical breaks and generous 30–45px internal spacing.
- Editorial cards use large 35px corners; documentation controls remain compact at 6–8px.
- Do not transfer marketing's full-pill geometry to uninspected product controls.
- Preserve the separation between marketing composition and documentation density.

The public marketing system retains full-pill controls and rounded editorial cards while reducing horizontal section padding and tab padding on narrower layouts. Exact private-product breakpoints and mobile-native behavior are unnamed in this pass and should not be inferred from the marketing site. Treating those 30–45px / 35px / 6–8px / full-pill-on-narrower-layouts figures as captured-surface measurements rather than a cross-viewport specification, including should-not-be-inferred-from-the-marketing-site, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating official Channel Talk writing as centering customers, conversations, and practical operational improvement, including direct-and-optimistic-rather-than-ceremonial, explain-what-a-team-can-do-why-it-improves-a-customer-relationship-and-where-AI-removes-routine-work, product-updates-naming-the-affected-workflow-the-person-who-benefits-and-the-next-available-action, support-guidance-calm-and-procedural, brand-stories-warmer-and-more-reflective, and avoid-unsupported-performance-numbers-excessive-futurism-and-generic-all-in-one-revolution-language, as a reconstruction reading of official writing rather than a separately published voice specification, is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. Official Channel Talk writing centers customers, conversations, and practical operational improvement. The tone is direct and optimistic rather than ceremonial: explain what a team can do, why it improves a customer relationship, and where AI removes routine work. Product updates should name the affected workflow, the person who benefits, and the next available action. Support guidance should stay calm and procedural, while brand stories may be warmer and more reflective. Avoid unsupported performance numbers, excessive futurism, and generic “all-in-one revolution” language.

The following captured-locale reading — KR-and-US-marketing-pages-and-English-product-documentation-as-the-named-hosts-in-this-packet-rather-than-a-complete-locale-profile, unobserved-locale-behavior-unnamed, and no-synthetic-voice-samples — is a derived editorial implementation inference from the verified surfaces; it is not Channel Talk-authored or a separately published UI specification. This packet names KR marketing, US marketing, and English product documentation. Unobserved locale behavior remains unnamed in this pass. No synthetic voice samples are promoted.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover, focus, empty, loading, error, success, disabled, and authenticated Inbox visual treatments
- Marketing primary action / Marketing outline action / Documentation icon action loading·error·success applicability (exact request/outcome unresolved)
- interactive kind and state-applicability map for Marketing information card
- Authenticated Inbox status colors
- documentation overlays and authenticated product elevation
- exact private-product breakpoints and mobile-native behavior
- authenticated app overrides and unobserved locale behavior
- inputs, dialogs, toasts, and authenticated inbox rows
- Channel-exclusive redistributable brand font; BildV5 (no declaration or visible use)
- Cobalt `#329BE7` as a universal current primary
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
