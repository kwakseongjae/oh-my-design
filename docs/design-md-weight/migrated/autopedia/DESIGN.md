# Dr.Cha (Autopedia) Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Dr.Cha (Autopedia) is the catalog identity for Autopedia (오토피디아; 닥터차(오토피디아)), a Korean mobility-technology company behind the 닥터차(Dr.Cha) automotive-aftermarket platform. Catalog homepage identity is `https://company.autopedia.co.kr/`. YAML `display_name_kr` is 닥터차(오토피디아).

Treating the following three company URLs as the named live-inspect evidence domains of this reconstruction, and treating Medium as a named company-blog source rather than an extra token surface, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. This contract covers three supplied company surfaces from the 2026-07-02 live inspect: the corporate homepage `https://company.autopedia.co.kr/`; the CI guide `https://company.autopedia.co.kr/about/ci-guide`; and the Dr.Cha product page `https://company.autopedia.co.kr/business/doctor-cha`. Token extraction is `live-extract`. Token-level claims in source §1–9 are sourced from playwright `getComputedStyle` across those three surfaces.

Source token note: primary = live brand violet `#7A00FF` (rgb 122, 0, 255), used as the active-nav accent and the full-bleed brand-statement band; near-shadowless high-contrast black/white system on Pretendard; corners are mostly sharp (`0px`), with `8px` only on the CI asset-download buttons. Treating that note as a register split — `#7A00FF` is catalog `primary_color` and the active-nav text / brand-band fill rather than a general fill for every control; `#000000` / `#ffffff` stay the high-contrast pair; Pretendard is the live UI family; `0px` is the default corner and `8px` is the CI-download exception — is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

The following atmosphere readings (stark high-contrast corporate identity; engineering-forward rather than consumer-cute; violet as the only saturated hue; Korean-corporate-modern Pretendard; refusal of decoration; genuinely shadowless; flat editorial almost-architectural; mobility-technology that wants to look precise and industrial, not playful; separation-via-flat-color-blocking; thin-hairlines-never-elevation) are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. The source reads Autopedia as presenting a stark, high-contrast corporate identity. The canvas is pure white (`#ffffff`) and the type is pure black (`#000000`). Against that black-and-white foundation a single vivid violet (`#7A00FF`) colors the active navigation item and fills a full-bleed brand-statement band. The typographic voice is recorded as Pretendard throughout: display headlines at 34px weight 600 with `-1.36px` tracking; navigation at 16px weight 700; body at 16px weight 400. Live inspection found `box-shadow: none` across the nav, hero, headings, buttons, and cards. Separation comes from flat color blocking (white sections, black image sections with a `rgba(0, 0, 0, 0.6)` scrim, and the violet band) and thin `#E5E7EB` hairlines. Geometry is mostly sharp: corners sit at `0px` by default; the only rounding in the system is the modest `8px` on the CI asset-download buttons.

The CI page records company registry facts: represented by CEO 김병근 (Kim Byung-geun); corporate registration 236-86-01261. The homepage brand-statement band uses the line “오토피디아는 기술로써 모빌리티의 진화를 이끌어 냅니다” (“Autopedia drives the evolution of mobility through technology”). The four business lines shown on the site, and the roles the site assigns them, are: **닥터차 (Dr.Cha)**, the owner-facing automotive-aftermarket management platform; **퀀텀모빌리티 (Quantum Mobility)**, specialist repair for imported brands (BMW, Audi, Benz, Tesla); **부품 유통 (Parts Distribution)**, a supply network delivering imported-car parts to repair shops; and an **AI 기술 플랫폼 (AI platform)** for vehicle-problem diagnosis, prediction, and consultation. The unifying frame on the Dr.Cha page is “모빌리티 자산” (mobility assets): “닥터차는 고객의 '모빌리티 자산'을 관리하는 최적의 서비스입니다.” Treating that four-line roster as one aftermarket ecosystem — owners, imported-car repair, parts supply to repair shops, and vehicle-problem consultation — is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

The following founding-observation and gap-closing readings, including everyday-car-use versus unchanged-driving-and-management, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. Source §11 records the homepage bands stating that while cars have woven themselves into daily life (“집에 갈 때, 쇼핑을 할 때, 여행을 갈 때 — 자동차는 이미 우리의 일상 깊숙이”), the way people drive and manage those cars has barely changed (“차를 운전하고 관리하는 방식은 달라지지 않았습니다”), and treats Autopedia’s premise as closing that gap with technology.

The following refuses-cluttered-auto-repair-chrome, refuses-consumer-cute-softness, and embraces-stark-black-and-white / single-violet / bold-Pretendard-mission / flat-architectural-precision readings are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. Source §11 reads what Autopedia refuses as the cluttered, promotion-heavy chrome of legacy auto-repair marketing (no shadow-stacked coupons, no alarmist red banners) and the softness of consumer-cute apps, and what it embraces as a stark black-and-white technology aesthetic, a single decisive violet, bold Pretendard headlines that speak in mission terms, and a flat, architectural layout.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three captured company-surface jobs below as the independently verified user outcomes, and not lifting tasks from source §13 fictional archetypes, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

- Scan Autopedia company identity, top nav (회사소개 · 사업소개 · 소식 · 블로그↗), the violet brand-statement band, and the four business-line feature cards on `https://company.autopedia.co.kr/`.
- Download CI / brand assets (JPG / PNG / 브랜드 가이드 다운 받기) on `https://company.autopedia.co.kr/about/ci-guide`.
- Read Dr.Cha “모빌리티 자산” product framing and use the in-section homepage-link CTA (홈페이지 바로가기) on `https://company.autopedia.co.kr/business/doctor-cha`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable Autopedia / Dr.Cha user and partner segments, not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

### Distinctive traits

- Catalog `primary_color` / Autopedia Violet `#7A00FF` (rgb 122, 0, 255) as active-nav text and the full-bleed brand-statement band
- Pure white canvas `#ffffff` with pure black `#000000` text; muted grey `#4B5563`; hairline `#E5E7EB`
- Pretendard throughout, with `Pretendard Fallback`; 34px / 600 display, 16px / 700 nav, 16px / 400 body
- Tight negative tracking that scales with size (`-1.36px` at 34px, `-1.04px` at 26px, `-0.88px` at 22px)
- `box-shadow: none` on nav, hero, headings, buttons, and cards
- Sharp `0px` corners by default; `8px` only on CI asset-download buttons; YAML `rounded.full` 9999 (`9999px`) reserved for incidental circular elements (avatars/icons)
- Dark photographic sections overlaid with `rgba(0, 0, 0, 0.6)` and white outline CTAs
- Brand-band statement “오토피디아는 기술로써 모빌리티의 진화를 이끌어 냅니다.”

Treating `#7A00FF` as active-nav text and brand-band fill rather than one interchangeable semantic, treating `#ffffff` canvas as unmerged from on-primary / outline-light text / solid-white fill, treating `#000000` ink as unmerged from dark photographic section fill, treating `0px` as the default rather than a universal radius for the CI-download `8px`, treating YAML `rounded.full` 9999 as reserved for incidental circular elements rather than a harvested control radius, treating weight 700 on nav as heavier than display 600, treating tight-negative-tracking-that-scales-with-size as a recorded metric grouping rather than published type doctrine, and treating engineering-forward / architectural / industrial-not-playful as distinctive character rather than published UI doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

### Principles

These five items, including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. The source HTML comment marks interpretive claims such as “technology over transaction”, “the car as a managed asset”, “one accent, decisive”, and “flat and precise as engineering seriousness” as editorial readings connecting Autopedia’s observed design and copy to its positioning, not directly sourced Autopedia statements.

1. **Technology over transaction.** Autopedia frames itself as driving “the evolution of mobility,” not selling repairs. *UI implication:* lead with mission bands and capability, keep pricing/promotion out of the hero; the violet band carries vision, not a discount.
2. **The car is a managed asset.** The “모빌리티 자산” framing treats a vehicle as something owned and tended over time. *UI implication:* favor management/overview surfaces (status, history, prediction) over one-off transactional flows.
3. **One accent, decisive.** Violet (`#7A00FF`) means “Autopedia.” *UI implication:* reserve the saturated violet for the active state and the brand band; never dilute it across ordinary UI.
4. **Flat and precise.** Engineering seriousness reads as clarity, not decoration. *UI implication:* no shadows; separate with color blocks and `#E5E7EB` hairlines; keep corners sharp.
5. **High contrast, no hedging.** Pure black on pure white. *UI implication:* commit to `#000000`/`#ffffff` for maximum legibility; use the grey `#4B5563` only for genuine secondary text.

Treating the following as a capture-bound application of source §7 Do’s and harvested geometry, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

- Use Pretendard for everything — 34px/600 display, 16px/700 nav, 16px/400 body.
- Reserve violet (`#7A00FF`) as the single brand accent — active nav and the full-bleed statement band.
- Set primary text in pure black (`#000000`) on pure white (`#ffffff`).
- Keep the system shadowless — separate with color blocks and `#E5E7EB` hairlines.
- Use sharp `0px` corners by default; reserve `8px` only for CI/brand-asset buttons.
- Overlay photographic sections with a `rgba(0, 0, 0, 0.6)` scrim and use white outline CTAs on top.
- Set navigation heavier (weight 700) than display copy (weight 600).
- Apply tight negative tracking on headlines (`-1.36px` at 34px, scaling down with size).

### Avoid

The following items copy source §7 Don’ts, including no-drop-shadows-for-elevation, violet-must-stay-the-single-Autopedia-signal, no-softened-near-black-headings, 8px-only-rounding-exception, Pretendard-and-one-violet-do-all-the-work, display-600-nav-700, white-on-violet-or-dark, and Autopedia-tracks-tight. They are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

- Do not add drop shadows for elevation.
- Do not spread violet across many elements — it must stay the single “Autopedia” signal.
- Do not use softened near-black or tinted grey for headings — primary text is pure black `#000000`.
- Do not round corners freely — geometry is sharp (`0px`); `8px` is the only exception.
- Do not introduce a second accent color or a decorative display face — Pretendard and one violet do all the work.
- Do not set body headlines at a light weight — display is always SemiBold (600), nav is Bold (700).
- Do not put dark text on the violet band or the dark image sections — use white (`#ffffff`).
- Do not use positive letter-spacing at display sizes — Autopedia tracks tight. Treating that Autopedia-tracks-tight characterization as a reconstruction reading rather than Autopedia-authored type doctrine is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including active-nav-and-brand-band-unmerged on `#7A00FF`, canvas-on-primary-outline-light-solid-white `#ffffff` not-a-second-canvas-token, `#000000` ink-not-dark-section-fill-as-one-job, `#4B5563` not-Foreground, hairline `#E5E7EB` not-control-border / not-1px-black / not-3px-white, and scrim-not-hex-not-Hairline, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

- **Autopedia Violet / Primary** (`#7A00FF`): YAML `primary`. Catalog `primary_color`. rgb 122, 0, 255. Active navigation item text and the full-bleed brand-statement band fill. Those jobs share a hex and are not one semantic.
- **Pure Black / Ink** (`#000000`): YAML `ink`. Primary text, headings, nav labels, outline-dark control text/border, feature-card text, footer-link text, and the background of dark photographic sections. Those jobs share a hex and are not one fill token.
- **Pure White / Canvas / on-primary** (`#ffffff`): YAML `canvas` / `on-primary`. Page background, card surfaces, text and outline color on black or violet backgrounds, outline-light CTA text/border, and solid-white CTA fill. Those jobs share a hex and are not a second canvas token.
- **Muted Grey** (`#4B5563`): YAML `muted`. Secondary text, sub-labels, captions, and fine print — rgb 75, 85, 99. Not Foreground.
- **Hairline** (`#E5E7EB`): YAML `hairline`. Thin card outlines and dividers — rgb 229, 231, 235. Not the 1px `#000000` outline-dark border and not the 3px `#ffffff` outline-light border.
- **Scrim**: `rgba(0, 0, 0, 0.6)` black overlay over hero/section photography so white headlines and outline CTAs stay legible. Not a hex token and not Hairline.

### Spacing

YAML `spacing`: xs 4, sm 8, md 12, base 16, lg 24, xl 36, xxl 48, section 96. Body layout records a base unit of ~`4px`, doubling through 8 / 12 / 16 / 24 / 36 / 48, and a partnership-block vertical measure of `96px`. Treating that YAML scale and the ~`4px` doubling as recorded spacing in this packet rather than a universal spacing token for every unlisted control, and treating component padding as staying with the components, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. CI-download padding is `18px 0px`. Hero outline-light padding is `8px 24px`. In-section solid-white padding is `12px 36px`.

### Shape

YAML `rounded`: sharp 0, md 8, full 9999.

The following local-geometry reading, including 0px-as-the-default, 8px-only-on-CI-download as the single exception, and 9999-not-a-harvested-control-radius / reserved-only-for-incidental-circular-elements, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

- Sharp (`0px`): the default — nav, hero CTAs, feature cards, brand band
- Medium (`8px`): the single exception — CI asset-download buttons
- Full (9999 / `9999px`): reserved only for incidental circular elements (avatars/icons)

`0px` corners and `8px` CI-download geometry are local recorded defaults, not a universal radius scale. YAML `rounded.full` 9999 is kept; it is not merged with a harvested control radius.

### Elevation

The following table Use assignments (everything-no-shadow, color-block-as-primary-separation, scrim-for-legibility, hairline-on-cards) are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Everything — page, cards, buttons, bands |
| Color block (Level 1) | White / black / `#7A00FF` full-width section fill | Primary separation device |
| Scrim (Level 2) | `rgba(0, 0, 0, 0.6)` over photography | Keeps white headlines/CTAs legible on image sections |
| Hairline (Level 3) | 1px solid `#E5E7EB` | Feature-card outlines and dividers |

YAML `shadow.none` is `none`. Live inspection returned `box-shadow: none` on the nav, hero, headings, buttons, and cards. The following strictly-shadowless / industrial-modern / engineered-and-confident-rather-than-soft / contrast-not-elevation readings are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. Source §6 records Autopedia as a strictly shadowless system. Depth is communicated through flat, full-width color blocking, the dark scrim, and thin hairlines. When emphasis is needed the system reaches for contrast (black-on-white, white-on-violet), never for elevation.

The following not-a-general-ladder reading is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. The Level 0–3 table is a source reconstruction of separation devices, not a published elevation token ladder.

### Motion

Treating the following as source-stated duration roles from philosophy-layer §15 rather than a computed-style harvest of transition properties, including the table Use assignments (hover-nav-active-and-press, card-section-reveal, page-level-and-brand-band-reveal), is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, nav active-state change, button press |
| `motion-standard` | 220ms | Card/section reveal, band transition, dropdown |
| `motion-slow` | 360ms | Page-level transitions, brand-band reveal |

The following restrained-and-purposeful, quiet-fade-up, no-bounce-or-spring, precision-and-steadiness-not-playfulness, and consistent-with-flat-high-contrast-engineering-forward readings are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. Source §15 records motion as restrained and purposeful — consistent with the flat, high-contrast, engineering-forward aesthetic. Full-width bands (white / black / violet) reveal with a quiet fade-up at `motion-standard / ease-enter` as they scroll into view; the active nav item transitions to violet (`#7A00FF`) at `motion-fast`. No bounce or spring.

Source-stated easing token names and uses (uncomputed; cubic-bezier curves omitted). Characterizations such as “Arriving — sections, cards, bands”, “Dismissals”, and “Two-way transitions” in this table are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier) | Arriving — sections, cards, bands |
| `ease-exit` | omitted (unattributed cubic-bezier; matches the legacy spec template) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier) | Two-way transitions |

**Reduce motion.** The following reduced-motion application readings, including instant-collapse and stays-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant and the interface stays fully functional. Instant collapse is the source-stated reduced-motion behavior; it is not a computed `prefers-reduced-motion` capture on a named component.

Exact cubic-bezier curves are unattributed — `ease-exit` matches the legacy spec template; `ease-enter` and `ease-standard` are also unattributed — and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including only-Pretendard-token on live computed Pretendard and Pretendard-Fallback-not-a-second-identity, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Source §1–9 records Pretendard as the document default and the only typeface across the three inspected company surfaces. |
| Declared-only | `Pretendard Fallback` is named beside Pretendard. It is not promoted as a second identity face. |

### Family

- **Current visible UI family:** Pretendard (with `Pretendard Fallback`) — YAML `family.body`
- **Loaded source boundary:** Pretendard is the live-extract UI family on the three company surfaces. No substitute family is recorded.

The following font-use boundary, including no-decorative-display-face, Pretendard-Fallback-not-a-second-identity, and no-system-face-replacement, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. Do not introduce a decorative display face. Do not present `Pretendard Fallback` as a second brand identity. Do not replace Pretendard with a system face.

### Type roles

Verified YAML line-height values are the unitless ratios `1.44` (display), `1.54` (section), `1.55` (subsection), `1.50` (nav, body), and `1.43` (caption).

The following ratio-versus-size-local reading, including hangul-first-sizing as a characterization rather than a published type doctrine, stay-open-display-line-heights, and not converting those recorded values into a different unit, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. The px figures in the body table are size-local observations, not replacements for the ratios. Source §3 also names hangul-first sizing: body at 16px with a 24px line-height; display line-heights stay open (1.44–1.55).

| Role | Font | Size | Weight | Line height (YAML) | Body-table observation | Tracking | Provenance |
|---|---|---:|---:|---:|---|---|---|
| Display | Pretendard | 34px (`2.13rem`) | 600 | 1.44 | 49px | `-1.36px` | Brand-statement headlines, on white or dark. YAML `display`. |
| Section | Pretendard | 26px (`1.63rem`) | 600 | 1.54 | 40px | `-1.04px` | Business/feature titles (퀀텀모빌리티, 부품 유통). YAML `section`. |
| Sub-section | Pretendard | 22px (`1.38rem`) | 600 | 1.55 | 34px | `-0.88px` | Sub-headings, partnership prompts. YAML `subsection`. |
| Nav | Pretendard | 16px (`1.00rem`) | 700 | 1.50 | 24px | normal | Top navigation, CI button labels. YAML `nav`. |
| Body | Pretendard | 16px (`1.00rem`) | 400 | 1.50 | 24px | normal | Standard reading text. YAML `body`. |
| Caption | Pretendard | 14px (`0.88rem`) | 400 | 1.43 | 20px | normal | Footer links, fine print. YAML `caption`. |

YAML `use`: display = Display / brand-statement headlines. section = Business/feature section titles. subsection = Sub-headings, partnership prompts. nav = Top nav items, CI button labels. body = Standard reading text, Pretendard. caption = Footer links, fine print.

One typeface, weight-driven hierarchy: Pretendard carries everything; the jump from 400 (body) to 600 (display) to 700 (nav) is the primary hierarchy signal, not a font swap. Treating that weight-driven-hierarchy sentence, including nav-heavier-than-display, as a reconstruction reading rather than Autopedia type doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

The following imagery-replacement reading is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. Photographic sections on the inspected company pages carry the `rgba(0, 0, 0, 0.6)` scrim; do not replace those photographs with invented brand-color decoration.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted, and treating source §14 product-app rows as philosophy-layer guidance rather than computed paints on the harvested company-site controls, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

The three inspected company surfaces record default component geometry. No hover, disabled, loading, error, or success visual treatment is promoted from the live extract onto those harvested controls. Source §14 records product-app empty / loading / error / success / skeleton / disabled treatments in the philosophy layer; those rows are preserved below.

The following table characterizations (no-illustration-clutter, honest-and-plain, no-shadow-shimmer, previous-content-stays-visible, no-bare-error-string, states-what-to-do-next, calm-technical-tone, no-celebratory-emoji, violet-actions-fade-rather-than-turn-a-different-color) are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| Empty (no vehicles/records yet) | White canvas. Single pure-black (`#000000`) line at body size explaining nothing is registered yet, with one CTA to add a vehicle. No illustration clutter. |
| Empty (no parts results) | Muted Grey (`#4B5563`) single line stating no matching parts, plus a path to adjust the search. Honest and plain. |
| Loading (diagnosis/compute) | Flat pulse blocks on white at final dimensions, sharp 0px corners. No shadow shimmer — consistent with the shadowless system. |
| Loading (list fetch) | Hairline-bordered (`#E5E7EB`) placeholder cards; previous content stays visible during in-place refresh. |
| Error (diagnosis failed) | Inline message in pure black with a plain-language explanation and a retry. No bare “오류가 발생했습니다” — states what to do next. |
| Error (form validation) | Field-level message below the input describing what is valid, not just “필수”. |
| Success (inquiry submitted) | Brief inline confirmation in a calm, technical tone; next-step / contact detail linked immediately below. No celebratory emoji. |
| Skeleton | White blocks at final dimensions, sharp corners, flat pulse — no elevation. |
| Disabled | Muted Grey (`#4B5563`) text on reduced-opacity surface; violet actions fade rather than turn a different color, preserving the single-accent read. |

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The source records no generic Focus capture with a colour; `focus-visible` rows carry no colour. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact destination/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Feature Card and Brand Band have YAML card types and no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted.

### Top Nav Link

- Role: top horizontal navigation item (회사소개, 사업소개, 소식, 블로그↗)
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#000000`
- Font: 16px / 700 Pretendard
- Height: 24px per link
- Active: violet `#7A00FF` text on the active item
- Use: YAML `Top navigation item (회사소개·사업소개·소식)`. YAML `tokens.components.nav-link`.
- Observed: default and active text colour; no nav interaction expansion captured
- Field note: The following unmerged-field reading, including active-text-not-brand-band-fill, not-a-second-primary-token, and height-24px-not-CI-download-63px, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. `#000000` is this item’s default text and Ink. `#7A00FF` is this item’s active text; it is also the brand-band fill and catalog `primary_color`, not a second primary token. Height `24px` is this link, not CI-download `63px`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top nav item |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A nav tab selects a company section; the tab itself does not enter a loading state |
| error | not-applicable | Tab meaning is selected versus unselected, not a request or validation failure on the tab |
| success | not-applicable | Tab meaning is selection, not action-outcome confirmation |

Additional observed named state: active item as captured (`#7A00FF` text). Treating that active appearance as a captured variant, not an observed click transition, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

### CI Download (Outline Dark)

- Role: brand-asset download button on the CI guide
- Kind: interactive
- Type: button
- Anatomy: label
- Text: `#000000`
- Border: 1px solid `#000000`
- Radius: 8px
- Padding: 18px 0px
- Height: 63px
- Font: 16px / 700 Pretendard
- Background: transparent
- Use: YAML `CI / brand-asset download button — transparent bg, black hairline outline`. Body: JPG/PNG/브랜드 가이드 다운 받기.
- Observed: default only
- Field note: The following unmerged-field reading, including 8px-not-0px-default, 1px-black-not-hairline, 3px-white-hero-not-this-border, harvested-height-not-touch-target on `63px`, and `#000000` text-not-a-dark-section-fill, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. `8px` is this control’s radius, not the `0px` default. `1px solid #000000` is this control’s border, not Hairline `#E5E7EB` and not the 3px white hero outline. Height `63px` is this button’s harvested height, not a touch-target policy.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the CI asset-download button |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A download action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the CI / brand-asset download button (JPG/PNG/브랜드 가이드 다운 받기); exact download-request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Hero CTA (Outline Light)

- Role: 문의하기 CTA on a dark image scrim in the hero / partnership sections
- Kind: interactive
- Type: button
- Anatomy: label
- Text: `#ffffff`
- Border: 3px solid `#ffffff`
- Radius: 0px
- Padding: 8px 24px
- Height: 47px
- Font: 16px / 400 Pretendard
- Background: transparent
- Use: YAML `Hero / partnership CTA (문의하기) on dark image scrim`
- Observed: default only
- Field note: The following unmerged-field reading, including 3px-white-not-1px-black, 0px-not-CI-8px, harvested-height-not-touch-target on `47px`, height-not-63-not-49, weight-400-not-nav-700, `#ffffff` label-not-a-second-canvas-token, and transparent-background-not-a-canvas-token, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. `Background: transparent` is this outline CTA’s field from source §9; it is not Canvas `#ffffff` and not a general color token. `3px solid #ffffff` is this control’s border, not the CI-download 1px `#000000` and not Hairline `#E5E7EB`. `#ffffff` is this control’s text and border; it is also Canvas / on-primary / solid-white fill. Height `47px` is this button’s harvested height, not CI-download `63px` and not solid-white `49px`. Weight 400 is this CTA, not nav/CI 700.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the 문의하기 hero / partnership CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An inquiry CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the 문의하기 CTA on a dark image scrim; exact submit-selector/destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 inquiry-submitted row.

### In-Section Link (Solid White)

- Role: 홈페이지 바로가기 CTA on the dark / violet product bands (Dr.Cha page)
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#000000`
- Radius: 0px
- Padding: 12px 36px
- Height: 49px
- Font: 16px / 600 Pretendard
- Use: YAML `In-section homepage-link CTA (홈페이지 바로가기) on dark / violet band`
- Observed: default only
- Field note: The following unmerged-field reading, including fill-not-a-second-canvas-token, 600-not-nav-700, harvested-height-not-touch-target on `49px`, height-not-47-not-63, and not merging this solid CTA with the outline-light 문의하기 control, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. `#ffffff` is this control’s fill; it is also Canvas / on-primary / outline-light text. `#000000` is this control’s label and Ink. Height `49px` is this button, not outline-light `47px` and not CI-download `63px`. Weight 600 is this CTA and display, not nav 700.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the 홈페이지 바로가기 in-section CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A homepage-link CTA can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as the in-section homepage-link CTA (홈페이지 바로가기); it is a navigation CTA, not the inquiry-submit role named in §14, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Feature Card

- Role: business/feature card (닥터차 · 퀀텀모빌리티 · 부품유통 · AI 기술 플랫폼)
- Type: card
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Anatomy: surface
- Background: `#ffffff`
- Text: `#000000`
- Border: 1px solid `#E5E7EB`
- Radius: 0px
- Use: YAML `Business/feature card (닥터차·퀀텀모빌리티·부품유통·AI) with hairline, no shadow`
- Observed: default only
- Field note: The following unmerged-field reading, including YAML-fg-versus-§9-body-muted, hairline-not-control-border, 0px-not-CI-8px, and tappable-layout-sentence-not-kind-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. YAML `fg` is `#000000`. Source §9 prompt also sets card body copy as 16px / 400 `#4B5563`. Both writings are kept. `#E5E7EB` is this card’s edge and Hairline, not the 1px `#000000` CI-download border. Layout §5 names these as large tappable feature cards in a row; that layout sentence is not interactive-kind evidence for a §4.4 map.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Brand Band

- Role: full-bleed violet brand-statement section
- Type: card
- Kind: omitted. The source records default geometry and no state or interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.
- Anatomy: surface
- Background: `#7A00FF`
- Text: `#ffffff`
- Radius: 0px
- Use: YAML `Full-bleed violet brand-statement section`. Body statement: “오토피디아는 기술로써 모빌리티의 진화를 이끌어 냅니다.”
- Observed: default only
- Field note: The following unmerged-field reading, including band-fill-not-active-nav-text, not-a-second-primary-token, and `#ffffff` on-primary-not-a-second-canvas-token, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. `#7A00FF` is this band’s fill; it is also active-nav text and catalog `primary_color`, not a second primary token. `#ffffff` is this band’s text and on-primary.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Footer Link

- Role: footer navigation link (제휴문의 · 사업소개 · 블로그)
- Kind: interactive
- Type: listItem
- Anatomy: label
- Text: `#000000`
- Font: 14px / 400 Pretendard
- Use: YAML `Footer navigation link (제휴문의·사업소개·블로그)`
- Observed: default only
- Field note: The following unmerged-field reading, including caption-size-not-nav-16px and `#000000` not-muted, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification. `#000000` is this link’s text and Ink, not Muted `#4B5563`. `14px / 400` is this link and Caption, not nav `16px / 700`.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the footer navigation link |
| hover | applicable | Pointer-web list item; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A footer link can be unavailable; visual treatment omitted |
| loading | not-applicable | A footer list item navigates; the item itself does not enter a loading state |
| error | not-applicable | List-item meaning is navigation, not a request or validation failure on the item |
| success | not-applicable | Opening a footer link is navigation, not action-outcome confirmation on the item |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following measurement-boundary, local-frequency, and not-generalized readings, including blocked-not-layered, generous-vertical-rhythm, sharp-edges-architectural, harvested-height-not-touch-target, not-cross-viewport-specifications, full-width-bands-stack, tappable-feature-cards, and centered-single-column, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification.

YAML spacing is xs 4, sm 8, md 12, base 16, lg 24, xl 36, xxl 48, section 96. Body layout repeats a ~`4px` base unit doubling through 8 / 12 / 16 / 24 / 36 / 48, with section-level padding recorded as generous (the partnership block measured `96px` vertical). Full-width horizontal bands stack vertically — white content sections alternate with black photographic sections and the violet brand band. Business offerings (닥터차 / 퀀텀모빌리티 / 부품유통 / AI) are presented as large tappable feature cards in a row. Display headlines use a centered single-column measure; text is set left- or center-aligned within each band. Content separates by full-width color blocking (white / black / violet), not by elevation or nested cards. The 63px CI download, 49px solid-white CTA, and 47px hero outline CTA are harvested heights, not a touch-target policy.

Source §8 collapsing notes, including the breakpoint table and the collapsing-bullet changes (scale-down-weight-600-maintained, cards-stack, full-bleed-reduce-padding), are source-stated reconstruction values, not a captured viewport series on those widths. Treating that collapsing-strategy table and those collapsing-bullet changes as source-stated rather than independently captured at each breakpoint, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column, display headline compresses, feature cards stack |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards |
| Desktop | 1024-1440px | Full-width bands, feature cards in a row, centered display measure |

- Display headlines: 34px scale down on mobile, weight 600 maintained
- Feature-card row: multi-column → stacked single column
- Full-width color bands (white / black / violet): maintain full-bleed treatment, reduce internal padding
- Dark photographic sections keep the `rgba(0, 0, 0, 0.6)` scrim at all sizes

The following image-behavior readings, including scrim-at-every-breakpoint and sharp-cornered-shadowless-across-sizes, are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification. Hero/section photography carries the dark scrim at every breakpoint so white text stays legible. Cards and images stay sharp-cornered and shadowless across sizes.

<!-- design-md:section content-locales -->
## 6. Content & Locales

Observed (company surfaces via live inspect 2026-07-02; dual-destination with provenance):

- “오토피디아는 기술로써 모빌리티의 진화를 이끌어 냅니다.” — full-bleed violet brand band
- “닥터차는 고객의 '모빌리티 자산'을 관리하는 최적의 서비스입니다.” — Dr.Cha product page lead
- “오토피디아는 첨단 AI 기술로 운전자들에게 새로운 차원의 경험을 제공합니다.” — AI section headline

The remaining voice examples, the copy-pattern table, the voice characterization, and the forbidden-register list — including confident-mission-framed-technology-first, rather-than-simply-car-repair, plain-functional-offering, rises-to-vision-language, Declarative-visionary, Ambitious-not-hype, Plain-categorical, Benefit-framed, Direct-low-pressure, and Formal-precise — are a derived editorial implementation inference from the verified surfaces; they are not Autopedia-authored or a separately published UI specification, and they are not the same evidence class as those live-inspect strings. Source §10 records Autopedia’s voice as confident, mission-framed, and technology-first — a mobility company that speaks about “the evolution of mobility” and “mobility assets” rather than simply “car repair.” Product copy is plain and functional at the offering level (닥터차, 퀀텀모빌리티, 부품 유통, AI 기술 플랫폼) but rises to vision language in the brand sections.

| Context | Tone |
|---|---|
| Brand-statement bands | Declarative, visionary. “오토피디아는 기술로써 모빌리티의 진화를 이끌어 냅니다.” Ambitious, not hype. |
| Business/offering titles | Plain, categorical. “퀀텀모빌리티”, “부품 유통”, “AI 기술 플랫폼”. |
| Product framing (Dr.Cha) | Benefit-framed. “닥터차는 고객의 '모빌리티 자산'을 관리하는 최적의 서비스입니다.” |
| CTAs | Direct, low-pressure. “문의하기”, “홈페이지 바로가기”, “제휴문의”. |
| Corporate / CI | Formal and precise. “오토피디아의 CI를 소개합니다.” |

Treating `문의하기`, `홈페이지 바로가기`, `제휴문의`, and “오토피디아의 CI를 소개합니다.” as source CTA / corporate inventory, not as extra Observed strings beyond the three live-inspect quotes above, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

Forbidden register recorded in source §10: discount-driven urgency, fear-based repair-scare messaging, undefined automotive jargon left unexplained, exclamation-heavy hype. Treating that forbidden-register list as a reconstruction reading rather than Autopedia-authored microcopy doctrine, is a derived editorial implementation inference from the verified surfaces; it is not Autopedia-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover, disabled, loading, error, success, empty, skeleton, and form-validation visual treatments on the harvested company-site controls
- CI Download, Hero CTA, and In-Section Link loading, error, and success applicability (exact download-request/outcome, inquiry-submit mapping, and navigation-request/outcome unresolved)
- interactive kind and state-applicability map for the Feature Card and the Brand Band
- `focus-visible` visual treatments
- a universal spacing scale; a universal radius scale; a published elevation token ladder
- independently captured viewport series at 640px / 1024px / 1440px
- invented demographic or behavioral personas
- easing curves (`ease-enter`, `ease-exit`, `ease-standard`) — unattributed cubic-bezier values omitted; promote a curve, animation name, or transition property only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
