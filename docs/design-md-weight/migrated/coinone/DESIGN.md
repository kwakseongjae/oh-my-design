# Coinone Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Coinone is a Korean virtual-asset exchange. Catalog homepage identity is `https://coinone.co.kr`. Treating that catalog homepage as identity rather than a fourth captured surface is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

Treating the following hosts as the named evidence domains of this reconstruction, including this-contract-covers-two-current-first-party-product-surfaces-and-a-corporate-brand-guideline-from-the-2026-07-13-packet, values-stay-attached-to-the-surface-or-guideline-that-established-them, public-home-not-a-stand-in-for-the-public-trading-route, public-trading-not-a-stand-in-for-the-public-home, corporate-guideline-not-a-source-of-exchange-control-CSS, public-captures-not-a-stand-in-for-logged-in-balance-order-entry-or-mobile-navigation, yaml-ds-type-brand, yaml-ds-name-Coinone-Brand-Guideline, www-versus-non-www-brand-hosts-stay-unmerged, corporate-brand-page-as-a-third-distinct-source-domain, and Roboto-led-documentation-chrome-stays-documentation-chrome-rather-than-an-exchange-UI-family, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. This contract covers two current first-party product surfaces from the 2026-07-13 packet plus corporate identity evidence: public exchange home `https://coinone.co.kr/` (YAML `exchange-home` / `home-capture`), public BTC/KRW trading `https://coinone.co.kr/exchange/trade/btc/krw` (YAML `exchange-trading` / `trading-capture`), and the official brand guideline named at YAML `ds.url` `https://www.coinonecorp.com/company/brand` (footer Tier 1 uses that same www host; YAML `verification_v2` surface `brand-guideline` and source `brand-capture` record `https://coinonecorp.com/company/brand` without www — those two host writings stay unmerged). YAML `ds.type: brand`; `ds.name: Coinone Brand Guideline`. The corporate brand page is a third, distinct source domain: it defines the signature, clear space, and Coinone Blue palette, but its Roboto-led documentation chrome stays documentation chrome rather than an exchange UI family. Color, type, and component values below stay attached to the domain that established them. A public-home value is not a stand-in for the public trading route. A public-trading value is not a stand-in for the public home. The corporate guideline is brand evidence rather than exchange-control CSS. The public captures do not stand in for a logged-in balance view, an order-entry flow, or mobile navigation.

Source token note: selector-backed product values are separated between the current exchange home and the public trading route. The corporate guideline is brand evidence rather than exchange-control CSS. Treating that note as a register split — official Coinone Blue `#006BD6` as catalog `primary_color` and YAML `brand` rather than observed home primary `#0B59D5`, home `pretendardCoinone` rather than trading `Spoqa Han Sans`, and guideline Roboto/Arial chrome rather than either product family — is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

The following atmosphere readings — formal-blue-corporate-identity, white-compact-current-home-in-loaded-`pretendardCoinone`, denser-public-trading-route-in-separately-loaded-`Spoqa Han Sans`, utilitarian-product-language-on-the-captured-routes, corporate-guideline-as-a-third-distinct-source-domain, and this-reference-preserves-those-boundaries-rather-than-forcing-them-into-one-inferred-UI-system — are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification. Founded in 2014, the exchange presents a formal blue identity in its corporate guideline, then uses a more utilitarian product language in the public routes captured here. The current exchange home is white, compact, and set in the loaded `pretendardCoinone` webfont; its public trading route is denser and uses the separately loaded `Spoqa Han Sans`. This reference preserves those boundaries rather than forcing them into one inferred UI system.

Treating the following official-company facts as narrative rather than interface tokens — 2014-establishment, Bitcoin-exchange-launch-that-October, slogan-Bringing-Blockchain-into-the-World, values-trust-innovation-and-expertise, blockchain-enabled-connection-and-movement-of-value, Coinone-Exchange-as-a-Korean-professional-virtual-asset-exchange, asset-safety-anti-money-laundering-transparent-listing-policy-and-a-user-centered-trading-environment, horizontal-signature-as-the-default, protected-clear-space, blue-as-the-main-color-representing-the-future, public-brand-guideline-gives-that-service-context-a-consistent-visual-identity, and official-history-mission-business-and-brand-guideline-writing-do-not-by-themselves-supply-interface-tokens — is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Coinone’s official history records the company’s establishment in 2014 and the launch of its Bitcoin exchange that October. Its mission page describes the company as believing in the possibilities created by free connection and movement of value through blockchain, under the slogan “Bringing Blockchain into the World.” The company names trust, innovation, and expertise as its values. The official business page describes Coinone Exchange as a Korean professional virtual-asset exchange and presents asset safety, anti-money-laundering systems, transparent listing policy, and a user-centered trading environment as core operating themes. The public brand guideline gives that service context a consistent visual identity: a horizontal signature as the default, protected clear space, and blue as the main color representing the future. Official history, mission, business, and brand-guideline writing provide that narrative context; they do not by themselves supply interface tokens.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the two captured product URLs as independently verified in this packet, treating the three YAML / §4 Use strings below as Primary tasks rather than lifting source §13 stakeholder groups, and treating those named Sign-in-on-the-public-exchange-home / Use-a-compact-product-control-on-the-public-exchange-home / Select-a-chart-tab-on-the-public-BTC-KRW-trading-route uses as harvested-strings-controls-not-independently-verified-destination-routes, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Independently verified in this packet are the two captured product URLs; named sign-in, compact-control, and chart-tab uses are harvested strings/controls, not independently verified destination routes.

- Sign in on the public exchange home (YAML use: `Home sign-in control, selector home::[data-omd-capture=14]`).
- Use a compact product control on the public exchange home (YAML use: `Home compact product control, selector home::[data-omd-capture=59]`).
- Select a chart tab on the public BTC/KRW trading route (YAML use: `Trading chart tab, selector surface-2::[data-omd-capture=49]`).
<!-- design-md:claim-end -->

### Audience

Restricting Audience so no individual personas are promoted, treating source §13 first-party stakeholder groups as an audience boundary rather than verified biographical personas, treating first-party-sources-identify-stakeholder-groups-rather-than-publishing-user-research-personas, treating Exchange members / prospective-or-returning-users-completing-customer-verification / customers-seeking-security-or-investor-protection-guidance as source-grounded-service-audiences-not-synthetic-behavioral-profiles and not Audience biographies and not primary tasks, and treating this-is-an-audience-boundary-not-a-substitute-for-Coinone-user-research, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. No individual personas are promoted. The first-party sources identify stakeholder groups rather than publishing user-research personas. Source-grounded service audiences, not synthetic behavioral profiles: exchange members using Coinone’s exchange and asset-management services; prospective or returning users completing customer verification (the official support flow identifies customer verification and real-name account checks as service requirements); customers seeking security or investor-protection guidance (the support center publishes phishing, fraud, account-protection, and investor-protection information). This is an audience boundary, not a substitute for Coinone user research.

### Distinctive traits

The following unmerged-role extras — treating catalog `primary_color` `#006BD6` as official Coinone Blue / YAML `brand` rather than observed home primary `#0B59D5` and rather than official point `#0090FF`, treating official `#194386` / `#062554` as supporting identity swatches rather than product-control paints, treating canvas `#FFFFFF` as unmerged from compact-control fill `#FFFFFF`, treating product foreground `#17181B` as unmerged from compact-control `#040505` and from chart-tab `#18191C` and from sign-in `#79818F` and from side-tab `#9E9E9E`, treating compact-control border `#DDE4EB` as unmerged from sign-in border `#AEB3BB`, treating YAML `family.home` `pretendardCoinone` as unmerged from YAML `family.trading` `Spoqa Han Sans`, treating YAML home-control `13` / `500` / `1.38` as unmerged from sign-in `12px` / `400` and from chart-tab `13px` / `400` and from side-tab `14px` / `400`, treating YAML rounded login `3` / control `6` / badge `26` as local observations rather than a universal radius scale, treating home 24px / 32px heights as unmerged from trading 37px / 40px heights, and treating collector `interactionCount: 0` plus unpromoted static hover/focus/pressed snapshots — are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification.

- Official Coinone Blue `#006BD6` as catalog `primary_color` and YAML `brand`, kept distinct from observed home primary `#0B59D5`
- White compact public home in loaded `pretendardCoinone` (408 observed uses); denser public trading route in loaded `Spoqa Han Sans` (314 observed uses)
- YAML home-control `13` / `500` / `1.38`; sign-in `12px` / `400`; chart tab `13px` / `400`; side tab `14px` / `400`
- Local radii 3px (sign-in), 6px (compact control), 26px (home badge); local heights 24px / 32px (home) and 37px / 40px (trading)
- Collector `interactionCount: 0`; static hover, focus, and pressed selector snapshots are not promoted as reusable states

### Principles

These 4 items — Make-trust-legible, Separate-identity-from-surface-evidence, Support-expert-use-without-inventing-density-rules, Keep-product-and-support-boundaries-clear — are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification.

1. **Make trust legible.** *UI implication:* keep verification, safety, and status language explicit instead of implying a guarantee.
2. **Separate identity from surface evidence.** *UI implication:* use the official palette as brand context while retaining the captured home and trading controls as route-local facts.
3. **Support expert use without inventing density rules.** *UI implication:* preserve observed trading-tab typography, but do not infer an entire order-entry system from a public route.
4. **Keep product and support boundaries clear.** *UI implication:* do not turn support documentation or corporate copy into an observed exchange state.

The UI implications are this reference’s interpretation of the reconstruction principles, not a separately published Coinone UI specification. Treating those UI-implication tails as a derived editorial implementation inference rather than a separately published Coinone UI specification, including keep-verification-safety-and-status-language-explicit, official-palette-as-brand-context-with-route-local-captured-controls, preserve-observed-trading-tab-typography-without-inferring-order-entry, and do-not-turn-support-or-corporate-copy-into-an-observed-exchange-state, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do’s, including keep-the-official-Coinone-Blue-palette-and-current-product-blue-observation-distinct, preserve-the-home-trading-typography-boundary, and treat-the-corporate-brand-guideline-as-identity-evidence-not-exchange-control-CSS, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

- Keep the official Coinone Blue palette and current product-blue observation distinct in implementation notes.
- Preserve the home/trading typography boundary when recreating one of the captured routes.
- Treat the corporate brand guideline as identity evidence, not as a source of exchange-control CSS.

### Avoid

The following items copy source §7 Don’ts, including no-declared-only-or-system-family-as-the-observed-family, no-invented-hover-error-empty-modal-responsive-or-logged-in-variants-from-static-capture-samples, and no-Roboto-documentation-chrome-as-exchange-UI-typography. They are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification.

- Do not substitute a declared-only or system family for `pretendardCoinone` or `Spoqa Han Sans` while presenting it as the observed family.
- Do not invent hover, error, empty, modal, responsive, or logged-in component variants from static capture samples.
- Do not generalize the corporate guideline’s Roboto documentation chrome into exchange UI typography.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#006BD6`-as-official-Coinone-Blue-not-observed-home-primary-`#0B59D5`-not-point-`#0090FF`, source-stated-distinct-from-but-compatible-with-not-collapsed, supporting-`#194386`-and-`#062554`-as-identity-swatches-not-product-control-paints, canvas-`#FFFFFF`-not-compact-control-fill-as-a-second-token, product-foreground-`#17181B`-not-compact-`#040505`-not-chart-tab-`#18191C`-not-sign-in-`#79818F`-not-side-tab-`#9E9E9E`, and compact-border-`#DDE4EB`-not-sign-in-border-`#AEB3BB`, are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification. Catalog `primary_color` `#006BD6` is YAML `brand` and the official Coinone Blue main color. Source records observed home primary `#0B59D5` as distinct from, but compatible with, that identity swatch; the two values are recorded by source domain rather than collapsed. YAML `point` `#0090FF` is the official point color. YAML `brand-deep` `#194386` and YAML `brand-navy` `#062554` are official supporting blue and navy colors. YAML `canvas` `#FFFFFF` is the observed public product canvas; the same hex is the compact-control background, and those named jobs stay unmerged. YAML `foreground` `#17181B` is observed product foreground across the home and trading route; it is not compact-control text `#040505`, not chart-tab text `#18191C`, not sign-in text `#79818F`, and not side-tab text `#9E9E9E`. YAML `control-border` `#DDE4EB` is the home compact-control border; sign-in uses `1px solid #AEB3BB`. YAML `login-text` `#79818F` is the sign-in label field.

- **Coinone Blue / catalog primary** (`#006BD6`): YAML `brand`. Official Coinone Blue main color.
- **Point** (`#0090FF`): YAML `point`. Official point color.
- **Brand deep** (`#194386`): YAML `brand-deep`. Official supporting blue.
- **Brand navy** (`#062554`): YAML `brand-navy`. Official supporting navy.
- **Canvas** (`#FFFFFF`): YAML `canvas`. Observed public product canvas. Same hex as compact-control fill; named jobs stay unmerged.
- **Product foreground** (`#17181B`): YAML `foreground`. Observed product foreground across the home and trading route.
- **Product primary (home)** (`#0B59D5`): YAML `product-primary`. Observed current-home primary blue. Distinct from official `#006BD6`; recorded by source domain rather than collapsed.
- **Control border** (`#DDE4EB`): YAML `control-border`. Home compact-control border.
- **Login text** (`#79818F`): YAML `login-text`. Home sign-in control text.

### Spacing

YAML `spacing`: xs 4, sm 8, md 12, lg 16. Those YAML numbers are recorded without a px suffix; none is added here. Treating those YAML numbers as recorded without a required px suffix, treating body 4px / 8px in home controls, 12px compact-control horizontal padding, and 16px chart-tab horizontal padding as body-recorded observations rather than a converted YAML scale, treating observed-component-spacing-intentionally-kept-small-and-local, and treating no-reusable-page-grid-as-asserted-from-those-local-values, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Observed component spacing is intentionally kept small and local: 4px and 8px occur in home controls, 12px in the compact-control horizontal padding, and 16px in the chart-tab horizontal padding.

### Shape

YAML `rounded`: login 3, control 6, badge 26.

The following local-geometry reading, including sign-in 3px, compact-control 6px, home-badge 26px, and not-a-universal-radius-scale, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

- Sign-in control: 3px (YAML `rounded.login`)
- Compact control: 6px (YAML `rounded.control`)
- Home badge: 26px (YAML `rounded.badge`)

These are observations, not a universal radius scale. Treating that closer as not-a-universal-radius-scale is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

### Elevation

The selector-backed home and trading controls above have `box-shadow: none`. Treating no-reusable-shadow-or-elevation-ladder-is-established is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. The capture does not substantiate a reusable shadow or elevation ladder.

### Motion

The supplied evidence does not measure durations, easing curves, animated price changes, or transition behavior. Treating that source-stated absence as a reason not to promote a motion duration, easing curve, animation name, transition property, or reduced-motion behavior is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. No motion token or recommendation is claimed.

Do not promote an easing curve, animation name, transition property, or a duration until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation value remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including live-home-`pretendardCoinone`-408-with-three-Coinone-CDN-WOFF2-source-URLs, live-trading-`Spoqa Han Sans`-314-with-Coinone-hosted-font-assets, documentation-chrome-Roboto-and-Arial-as-system-families-not-product-UI-tokens, loaded-icon-asset-`coinone_glyph_ui`-not-a-text-family, declared-only-`coinone_glyph_coin`-Glyphicons-Halflings-Noto-Sans-KR-`pretendardCoinone Fallback`-and-slick, and licence-boundary-live-loading-not-redistribution-terms, are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live home computed use | `pretendardCoinone` is loaded/high confidence with 408 observed uses across home body, controls, cards, headings, inputs, and badges. Three Coinone CDN WOFF2 source URLs corroborate the computed family. It is the home UI family in the machine tokens. YAML `family.home`. |
| Live trading computed use | `Spoqa Han Sans` is loaded/high confidence with 314 observed uses across the BTC/KRW public trading route, including controls, rows, tabs, and text. Its loaded source list includes Coinone-hosted font assets. It is a separate trading-route family, not a fallback for the home. YAML `family.trading`. |
| Documentation chrome | Roboto and Arial occur on the corporate brand page; the bundle classifies them as system families. They are not product UI tokens. |
| Loaded icon asset | `coinone_glyph_ui` is loaded for one observed icon-font use. It is an interface asset, not a text family token. |
| Declared-only assets | `coinone_glyph_coin`, Glyphicons Halflings, Noto Sans KR, `pretendardCoinone Fallback`, and slick have zero visible uses in the supplied capture. They remain declared-only and are not substituted or promoted. |
| Licence boundary | No first-party public font-licence statement was found for `pretendardCoinone` or the Coinone-hosted Spoqa files in the requested searches. The capture proves live loading and use; it does not establish redistribution terms. |

### Family

- **Current home UI family:** `pretendardCoinone` — YAML `family.home`
- **Current trading UI family:** `Spoqa Han Sans` — YAML `family.trading`

The following font-use boundary, including do-not-substitute-a-declared-only-or-system-family, do-not-replace-unavailable-or-unobserved-brand-type-outside-the-route-that-established-them, Roboto-and-Arial-not-product-UI-tokens, `coinone_glyph_ui`-not-a-text-family, and trading-family-not-a-fallback-for-the-home, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Do not replace unavailable or unobserved brand type with `pretendardCoinone` or `Spoqa Han Sans` outside the route that established them. Do not present Roboto or Arial as either product family. Do not present `coinone_glyph_ui` as a text family.

### Type roles

Treating YAML size `13` as recorded without a required px suffix, treating body `13px` / `12px` / `14px` writings as size-local observations rather than converted YAML numbers, treating YAML `1.38` as preserved as that ratio and not rewritten as a px line-height, treating yaml-1.38-scales-with-font-size-and-is-not-a-fixed-px, treating yaml-trading-tab-records-no-lineHeight-none-invented, and treating sign-in `12px` / `400` as unmerged from home-control `13` / `500` / `1.38` and from chart-tab `13px` / `400` and from side-tab `14px` / `400`, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Verified line-height is the unitless YAML ratio `1.38` on `home-control`. It scales with font size and is not a fixed px. YAML `trading-tab` records no `lineHeight`; none is invented.

| Role | Font | Size | Weight | Line height | Provenance |
|---|---|---:|---:|---:|---|
| Home compact control | pretendardCoinone | YAML `13` / body `13px` | 500 | 1.38 | YAML `home-control`. Use: Home compact control, selector `home::[data-omd-capture=59]` |
| Home sign-in | pretendardCoinone | 12px | 400 | | Body/YAML component font. Not home-control `13` / `500` / `1.38`. Use: Home sign-in control, selector `home::[data-omd-capture=14]` |
| Trading chart tab | Spoqa Han Sans | 13px | 400 | | YAML `trading-tab`. Not home-control `13` / `500`. Use: Trading chart tab, selector `surface-2::[data-omd-capture=49]` |
| Trading side tab | Spoqa Han Sans | 14px | 400 | | Body/YAML component font. Use: Trading side tab, selector `surface-2::[data-omd-capture=156]` |

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

Treating `coinone_glyph_ui` as a loaded icon-font asset for one observed use rather than a named icon set, image treatment, illustration style, or media-card specification, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. The public exchange home has a loaded `coinone_glyph_ui` asset for one observed icon-font use. The supplied bundle does not establish a named icon set, image treatment, illustration style, or media-card specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

Source §14, preserved: No empty, loading, success, validation-error, network-error, skeleton, disabled, toast, or responsive state was captured with interaction provenance. These states are intentionally not specified for Coinone in this reference.

The supplied bundle reports `interactionCount: 0` and no interaction records. Its static hover, focus, and pressed selector snapshots are not promoted as reusable states. No disabled, menu, dialog, validation, toast, responsive, or logged-in variants are claimed.

Legacy generic CTA, filter-chip, store-button, icon-button, trading-state, system-font, shadow, motion, and fallback-family claims were removed or narrowed where the 2026 supplied capture does not provide selector-backed product evidence.

Treating those §14 rows as source-stated state copy rather than live interaction captures, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. The capture includes static focus/pressed snapshots but no interaction record, so it does not establish a reusable keyboard `focus-visible` treatment. A later generic Focus observation would not be `focus-visible` evidence. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating Pressed and static hover/focus selector snapshots as additional named observed samples rather than Core applicability rows or `focus-visible` evidence is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Pressed and static hover/focus selector snapshots are additional named observed samples; they are not among the seven Core applicability rows and are not copied onto a `focus-visible` row.

### Home sign-in control

- Role: public-home sign-in control
- Kind: interactive
- Type: button
- Anatomy: label
- Text: `#79818F`
- Border: `1px solid #AEB3BB`
- Radius: `3px`
- Padding: `0px 8px`
- Height: `24px`
- Font: `12px / 400 pretendardCoinone`
- Use: Public-home sign-in control; `home::[data-omd-capture="14"]`
- Observed: default only; static pseudo-state samples are not promoted
- Field note: The following unmerged-field reading, including this-label-not-product-foreground-`#17181B`, this-border-not-`#DDE4EB`, this-radius-3px-not-6px-or-26px, this-font-12px-400-not-home-control-13-500-1.38, and this-static-focus-and-pressed-selector-snapshots-are-named-observed-samples-not-focus-visible-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. `#79818F` is this control’s label and YAML `login-text`. Radius `3px` is YAML `rounded.login`. The sign-in control records `#79818F` text with a `#AEB3BB` border. Static focus and pressed selector snapshots on this control are named observed samples, not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public exchange home |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A sign-in action can be unavailable; visual treatment omitted |

Treating omitted-L-E-S-because-exact-request-outcome-mapping-is-unresolved-for-the-public-home-sign-in-control is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as the public-home sign-in control; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Home compact control

- Role: public-home compact product control
- Kind: interactive
- Type: button
- Anatomy: control
- Background: `#FFFFFF`
- Text: `#040505`
- Border: `1px solid #DDE4EB`
- Radius: `6px`
- Padding: `6px 12px`
- Height: `32px`
- Font: `13px / 500 pretendardCoinone`
- Use: Public-home compact product control; `home::[data-omd-capture="59"]`
- Observed: default only; no interaction record
- Field note: The following unmerged-field reading, including this-fill-not-a-second-canvas-token, this-label-not-`#17181B`, this-border-not-`#AEB3BB`, this-radius-6px-not-3px-or-26px, and this-font-13px-500-not-sign-in-12px-400, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. `#FFFFFF` is this control’s fill; it shares a hex with YAML `canvas` and those jobs stay unmerged. `#040505` is this control’s label, not product foreground `#17181B`. `#DDE4EB` is this control’s border and YAML `control-border`. The home compact control records `#040505` text on white with a `#DDE4EB` border.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public exchange home |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A compact product control can be unavailable; visual treatment omitted |

Treating omitted-L-E-S-because-exact-request-outcome-mapping-is-unresolved-for-the-public-home-compact-product-control is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a public-home compact product control; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Trading chart tab

- Role: public BTC/KRW trading chart tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#18191C`
- Padding: `0px 16px`
- Height: `37px`
- Font: `13px / 400 Spoqa Han Sans`
- Use: Public BTC/KRW trading chart tab; `surface-2::[data-omd-capture="49"]`
- Observed: default only; no interaction record
- Field note: The following unmerged-field reading, including this-label-not-`#17181B`, this-13px-400-not-home-control-13-500-1.38, this-height-37px-not-40px, and this-family-Spoqa-not-`pretendardCoinone`, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. `#18191C` is this tab’s label, not product foreground `#17181B`. Font `13px / 400 Spoqa Han Sans` is this tab’s field, not YAML home-control `13` / `500` / `1.38`.

Treating this chart tab as a panel-select control, so loading, error, and success are not-applicable on the tab itself, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public BTC/KRW trading route |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive tab; visual treatment omitted |
| disabled | applicable | A tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A chart tab selects a visible chart view; the tab itself does not enter a loading state |
| error | not-applicable | Tab selection is not a validation or request-failure state on the tab |
| success | not-applicable | Selecting a chart tab is not an action-outcome confirmation on the tab |

### Trading side tab

- Role: public BTC/KRW trading side tab
- Kind: interactive
- Type: tab
- Anatomy: label
- Text: `#9E9E9E`
- Height: `40px`
- Font: `14px / 400 Spoqa Han Sans`
- Use: Public BTC/KRW trading side tab; `surface-2::[data-omd-capture="156"]`
- Observed: default only; no interaction record
- Field note: The following unmerged-field reading, including this-label-not-`#79818F`-and-not-`#18191C`, this-14px-400-not-chart-tab-13px-400, and this-height-40px-not-37px, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. `#9E9E9E` is this tab’s label, not sign-in `#79818F` and not chart-tab `#18191C`. Height `40px` is this tab’s field, not the chart tab’s `37px`.

Treating this side tab as a panel-select control, so loading, error, and success are not-applicable on the tab itself, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Captured on the public BTC/KRW trading route |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Interactive tab; visual treatment omitted |
| disabled | applicable | A tab can be unavailable; visual treatment omitted |
| loading | not-applicable | A side tab selects a visible trading panel; the tab itself does not enter a loading state |
| error | not-applicable | Tab selection is not a validation or request-failure state on the tab |
| success | not-applicable | Selecting a side tab is not an action-outcome confirmation on the tab |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

Treating YAML spacing xs 4 / sm 8 / md 12 / lg 16 as recorded without a required px suffix, treating observed-component-spacing-kept-small-and-local as 4px and 8px in home controls / 12px compact-control horizontal padding / 16px chart-tab horizontal padding, and treating home-capture-heights-24px-and-32px and trading-heights-37px-and-40px as body-recorded observations rather than a converted YAML scale, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. YAML spacing xs 4, sm 8, md 12, lg 16 without a required px suffix. Observed component spacing is kept small and local: 4px and 8px in home controls, 12px compact-control horizontal padding, 16px chart-tab horizontal padding. Home capture heights 24px and 32px; trading heights 37px and 40px.

The following layout readings — public-home-and-public-BTC-KRW-trading-as-separate-product-sub-surfaces, compact-24px-and-32px-home-controls-versus-37px-chart-tab-and-40px-side-tab, no-reusable-page-grid-breakpoint-logged-in-balance-view-order-entry-flow-or-mobile-navigation-behavior, and corporate-brand-guideline-not-used-to-populate-exchange-layout-tokens — are a derived editorial implementation inference from the verified surfaces; they are not Coinone-authored or a separately published UI specification. The public home and public BTC/KRW trading route are both product surfaces, but their captured controls use different loaded families and density. They are recorded as separate product sub-surfaces. The home capture contains compact 24px and 32px controls; the trading route contains a 37px chart tab and a 40px side tab. The supplied capture does not establish a reusable page grid, breakpoint, logged-in balance view, order-entry flow, or mobile navigation behavior. The corporate brand guideline provides identity rules and its own documentation chrome. It is not used to populate exchange layout tokens.

The 24px sign-in control, 32px compact control, 37px chart tab, and 40px side tab are desktop-capture measurements, not cross-viewport specifications. Treating those heights as captured-surface-not-cross-viewport, and treating no-accessibility-conformance-score / no-screen-reader-behavior / no-validation-behavior / no-mobile-target-rule-as-claimed-from-these-public-captures, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. No accessibility conformance score, screen-reader behavior, validation behavior, or mobile target rule is claimed from these public captures.

<!-- design-md:section content-locales -->
## 6. Content & Locales

The following voice reading — clear, trust-oriented, and technically grounded; concrete service language; compliance-or-risk-boundaries-explicit; product-and-support-materials-pair-task-specific-guidance-with-customer-verification-and-investor-protection-information; no-promotional-trading-promises; no-unobserved-product-microcopy — is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification. Coinone’s official mission emphasizes blockchain-enabled connection and names trust, innovation, and expertise as values. Its product and support materials pair task-specific guidance with customer-verification and investor-protection information. Use concrete service language and make compliance or risk boundaries explicit; do not invent promotional trading promises or unobserved product microcopy.

**Voice adjectives:** clear · trust-oriented · technically grounded

Treating the following tone-table directions as reconstruction guidance from official mission and support materials rather than a captured product-microcopy system, including Explain-the-task-requirement-or-protection-boundary-directly, Add-urgency-or-return-promises-not-supported-by-official-material, Separate-exchange-action-copy-from-compliance-support-information, Turn-the-company-mission-into-a-UI-slogan-without-route-specific-evidence, Use-precise-calm-Korean-financial-service-language, and Treat-illustrative-copy-as-a-captured-product-string, is a derived editorial implementation inference from the verified surfaces; it is not Coinone-authored or a separately published UI specification.

| Do | Don't |
|---|---|
| Explain the task, requirement, or protection boundary directly. | Add urgency or return promises not supported by official material. |
| Separate exchange action copy from compliance/support information. | Turn the company mission into a UI slogan without route-specific evidence. |
| Use precise, calm Korean financial-service language. | Treat illustrative copy as a captured product string. |

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- hover, pressed, focus, disabled, loading, error, success, empty, skeleton, menu, dialog, toast, validation, network-error, and logged-in visual treatments
- Home sign-in control and Home compact control loading·error·success applicability (exact request/outcome unresolved)
- reusable keyboard `focus-visible` treatment; static focus/pressed snapshots are a different evidence type
- reusable page grid, breakpoint, logged-in balance view, order-entry flow, and mobile navigation
- reusable shadow or elevation ladder
- motion duration, easing, animation name, transition properties, and reduced-motion behavior — promote only after per-component computed capture of all five kinds; official documentation of a single curve or duration is not that gate
- YAML `trading-tab` `lineHeight` (none recorded)
- named icon set, image treatment, illustration style, and media-card specification
- first-party public font-licence / redistribution terms for `pretendardCoinone` and Coinone-hosted Spoqa files
- accessibility conformance score, screen-reader behavior, validation behavior, and mobile target rule
