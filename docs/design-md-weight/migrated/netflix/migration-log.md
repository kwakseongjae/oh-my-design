# Netflix migration log

Source: `web/references/netflix/DESIGN.md`
Destination: `docs/design-md-weight/migrated/netflix/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/netflix/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6
Portable Core: pass (`scripts/design-md-core.cjs` inspectDesignMd, `portable_core: true`, placeholders 0)

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage URL 옮김 → Experience `scope`; `primary_color` 옮김 → Foundations Netflix Red; logo type/slug 경계 옮김 → Typography & Assets + Avoid | Portable file has no frontmatter. Name kept as H1 `Netflix Design System`. Homepage `https://www.netflix.com` is dual-destination: Experience Scope + provenance identity/surfaces (E2a). Catalog `primary_color` `#E50914` is dual identity metadata + portable Foundations Netflix Red (E2a); it is the logo/marketing-CTA/progress accent, not the Play fill `#FFFFFF`. Catalog logo type `simpleicons` / slug `netflix` is dual: provenance Identity holds type/slug; portable Assets and Avoid state the identity-only / not-a-mark-file boundary; no mark file is attached (E2a). |
| YAML `omd`, `verified`, `added`, token claims, `tokens.source` / `extracted`, `components_harvested` | 분리 → provenance | 출처 원장·freshness·Proof. `tokens.source: prose-derived` is metadata (A1c) and is also restated in portable Scope as the extraction class. No `ds.type` on source; none invented. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. `#E50914`와 Play fill `#FFFFFF`를 합치지 않음. `#2A9D3C` success와 `#46D369` match를 합치지 않음. YAML `error` `#E87C03`와 §2 form-error `#E50914` 충돌은 양쪽 보존 (평균하지 않음). Grey 800 `#2F2F2F` “secondary button fill”과 harvested More Info `rgba(109,109,110,0.7)` (grey600 @70%)를 합치지 않음 (A4). Grey 500 `#808080` scale roles (Caption/metadata, `"secondary"` CTA label, inactive nav links)는 Auth Field border field와 harvested More Info text `#FFFFFF`와 합치지 않음 (A4). YAML `muted`와 `body`는 같은 `#B3B3B3`로 본문에 Body / Muted로 명시. YAML unitless `lineHeight` 1.1 / 1.15 / 1.2 / 1.25 / 1.3 / 1.4 / 1.5는 비율로 보존 (A1a). 검증된 primitive type은 컴포넌트별로 보존: button ×4, input ×2, card ×2, badge ×3, tab, toast, dialog, toggle. `Kind: interactive`로 뭉개지 않음 (A1b). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits | 제품/표면 범위(browse / player / title-detail / marketing-acquisition; homepage `https://www.netflix.com`). “dark cinematic frame / theater reading” 문단 인접에 derived editorial implementation inference / not Netflix-authored or separately published UI specification 한정 (B2/B2a). Key characteristics는 Distinctive traits. Netflix Sans Dalton Maag 2018 이력은 Distinctive traits에서 third-party-corroborated로 한정하고 Typography Font evidence로 연결 (E1). Homepage dual Experience + provenance (E2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | Hex 문자열만으로 역할 보존을 주장하지 않음. Grey 500 `#808080` scale roles는 Caption/metadata text, `"secondary"` CTA label, inactive nav links. Auth Field `Border: 1px solid #808080`는 YAML `input-auth` field. Harvested More Info text는 `#FFFFFF`. 세 결합을 합치지 않음 (A4). 나머지 named hex (`#E50914` `#B20710` `#000000` `#141414` `#FFFFFF` `#E87C03` `#2A9D3C` `#46D369` `#181818` `#2F2F2F` `#404040` `#6D6D6E` `#B3B3B3` `#E5E5E5` `#333333`)는 해당 역할 슬롯에 유지. Pantone 1795 C / RGB 229·9·20 유지. Hero gradient `0%`/`100%` 유지. Overlay scrims 유지. Form-error 충돌은 위 행. Canvas “slightly lifted…so layered surfaces are distinguishable”는 인접 B2a (B2/B2a). |
| §3 Typography Rules | 옮김 → Typography & Assets | 다섯 증거 등급, Netflix Sans 스택, Courier New diagnostic-only, Gotham/Roboto legacy declared-only. Dalton Maag 2018 이력은 `Third-party-corroborated brand history` 증거 종류. Official product-use 행에 두지 않음 (E1). YAML unitless line-height는 Type roles 비율 열 (A1a). Helvetica/system fallback을 브랜드 페이스로 표시하지 않음. Type-character 문단 인접에 derived editorial implementation inference / not Netflix-authored or separately published UI specification 한정 (B2/B2a). |
| §3 / HTML `https://brand.netflix.com/en/assets/logos/` | 옮김 → Typography & Assets; 분리 → provenance | Dual destination (E2a): portable Assets는 official logos page이지 type specimen이 아님을 명시. provenance Sources에 같은 URL. |
| §4 Component Stylings | 옮김 → Components & States | Play (`Type: button`), Marketing CTA (`Type: button`), More Info (`Type: button`), Circle (`Type: button`), Auth Field (`Type: input`), Search Field (`Type: input`), Resting Title Card (`Type: card`), Expanded Preview (`Type: card`, Kind omitted — hover presentation of the resting tile; source Use `Hover/focus` is additional observed generic `Focus` on the tile and the preview, not `focus-visible` treatment), Badge ×3 (`Type: badge`, Kind non-interactive, map omitted), Top Nav (`Type: tab`; §9-only composition Red “N” left / links center-left / search+avatar right), Account Banner (`Type: toast`, Kind omitted), Modal (`Type: dialog`, Kind omitted), Toggle (`Type: toggle`). YAML 기하와 본문 기하를 같은 슬롯에 유지. Auth generic `Focus`와 tile generic `Focus`는 `focus-visible`로 승격하지 않음 (B1). |
| §4 Profile Gate Tile / Continue-watching Progress | 옮김 → Components & States | YAML `tokens.components` 밖이지만 §4 본문 고유 값 (A3). Profile Gate는 YAML `type` 없음 → type 발명 없음, Kind interactive + 7-state map. Progress는 non-interactive indicator → kind/map 생략 (C4). |
| §4 footer **Tier 1** netflix.com live DOM | 분리 → provenance; URL 옮김 → Experience `scope` | Dual destination (E2a): `https://www.netflix.com`은 portable Scope + provenance surfaces/Tier 1. |
| §5 Layout Principles | 옮김 → Layout & Platforms + Foundations spacing/shape | 4px base, 4/8/12/16/24/32/48/60, 4vw / 3vw, 56vh billboard, 850px modal, radius scale 2/4/6/9999. “Artwork gets vertical air / cinema frame” 인접에 derived editorial implementation inference / not Netflix-authored or separately published UI specification 한정 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | 4-level table + scrim, YAML `shadow.hover` / `floating` / `modal` 값 보존. Elevation philosophy 인접에 derived editorial implementation inference / not Netflix-authored or separately published UI specification 한정 (B2/B2a). |
| §7 Do's | 옮김 → Experience principles (capture-bound application) | 브랜드 적용 규칙. Governance 통제 문구에 넣지 않음. Principles 머리 한정이 capture-bound list까지 덮음 (B2/B2a). |
| §7 Don'ts | 옮김 → Experience avoid | 브랜드 금지. Causal/judgement wording (`grey hierarchy is what makes white legible`; `the black frame is the design`; `each category needs breathing room`)는 Avoid 머리 인접 B2a (B2/B2a). Simple Icons / Helvetica fallback 금지는 원본 Don'ts에 없는 토큰 경계이며 Avoid에 추가된 것은 원본 §3 fallback 금지와 catalog logo identity-only의 적용이지 원본 밖 도메인 coverage 문구가 아님. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | 500 / 950 / 1400 breakpoints, 40vh mobile hero, 44px/48px touch, 84px–200px profile gate, 16:9 / 2:3 crops, `#181818` lazy-load placeholder. 원본에 없는 최소폭 값을 발명하지 않음. |
| §9 Agent Prompt Guide | 삭제; §9-only 조각 2건 옮김 → Components | 도구별 복붙 프롬프트. 색·Play·CTA·nav-color·progress 수치는 이미 Foundations/Components에 있음. 고유 값 옮김 (A3): (1) “white circular play button” in the revealed info panel → Expanded Preview; (2) top-nav composition — Red “N” mark left, links center/left, search icon + profile avatar right → Top Nav Link. 슬롯 없는 위임 없음. |
| §10 Voice & Tone | 옮김 → Content & Locales | 표 7행 + forbidden moves + 30+ languages. 합성 보이스 샘플 없음. 원본에 없는 도메인 coverage 문구 신설 없음 (D1). Voice characterization 인접에 derived editorial implementation inference / not Netflix-authored or separately published UI specification 한정 (B2/B2a). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance | 1997 Hastings/Randolph, DVD-by-mail, 2007 streaming, 2013 House of Cards, 190+ countries, ad-supported/gaming/live-event 확장은 서사 맥락이지 토큰 도메인이 아님. `#E50914` envelope 인과 독해는 인접 완전 B2a 아래 brand-story context, not a token derivation (B2/B2a). 해석(“theater you carry”)은 Scope B2a 문단으로만. |
| §12 Principles | 옮김 → Experience principles | 여덟 원칙. 인접 본문에 derived editorial implementation inference / not Netflix-authored or a separately published UI specification 한정을 둠 (B2/B2a). Capture-bound application과 Governance 일반 문구는 그 한정의 대체물이 아님. |
| §13 Personas | mixed: exclusion boundary 옮김 → Experience Audience; fictional names·ages·cities·biographies 삭제; sidecar 재수록 없음 | 원본이 fictional archetypes라고 명시. portable Audience는 “Source §13’s named fictional archetypes are not Audience and are not primary tasks” exclusion boundary를 유지. 이름·나이·도시·biography는 삭제. sidecar 재수록 없음 (D2). Independently verified Primary tasks 3건은 §13이 아니라 catalog browse / Play-or-resume / marketing-auth (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability | 본문 보존: 11행 표 (Empty×2, Loading×2, Hover tile, Error×2, Success added-to-list, Disabled, Buffering, Progress resume) (A2). 선언 컴포넌트는 §4.4 7상태 표를 닫되 미관측 시각값은 발명하지 않음. Badge ×3는 `Kind: non-interactive`, map만 생략. Expanded Preview / Progress / Account Banner / Dialog는 kind와 map을 생략 (C4). `not captured`를 `not-applicable` 사유로 쓰지 않음 (C1). Play/CTA/More Info/Search/Tile/Nav/Profile/Toggle의 loading·error·success와 Circle success(add-to-list captured) / Auth error는 역할 의미로 판정 (C2). generic `Focus` (Auth border; tile Hover/focus activation)는 `focus-visible` treatment로 승격하지 않음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브만 삭제 | Duration 5개(0ms / 150ms / 300ms / 400ms / 600ms)와 signature motion 5개(tile hover-expand with source-stated `ease-out`/`ease-in`, row paging with `ease-scroll`, nav fade with `ease-standard`, billboard cross-fade, reduce motion)는 본문 보존. Row-paging `peek to promise more`와 billboard `so the artwork lands first`는 인접 완전 B2a (B2/B2a). Token names `ease-out` / `ease-in` / `ease-standard` / `ease-scroll`와 use는 source-stated/uncomputed로 본문 보존. 무출처 cubic-bezier 4개만 삭제 — `ease-out` `cubic-bezier(0.0, 0.0, 0.2, 1)`은 legacy 규격 템플릿 `ease-enter`와 동일; `ease-in` `cubic-bezier(0.4, 0.0, 1, 1)`은 `ease-exit`와 동일; `ease-standard` `cubic-bezier(0.4, 0.0, 0.2, 1)`은 템플릿 `ease-standard`와 동일; `ease-scroll` `cubic-bezier(0.25, 0.1, 0.25, 1)`은 무출처 CSS default-ease class. 삭제한 커브 문자열은 provenance omission ledger에 보관 (E2b). Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 curve/animation-name/transition-property를 승격한다는 게이트를 전문 명시. 단일 curve/duration 확인은 그 게이트가 아님 (B3, E2c). |
| HTML comment OmD v0.1 Sources | 분리 → provenance; 일부 URL 옮김 → portable Font evidence / Assets | freshness, WebSearch corroboration, prose-derived, fictional personas, editorial readings. `https://www.designyourway.net/blog/netflix-logo/`는 dual Typography Font evidence (third-party-corroborated) + provenance Web-search (E2a). `https://www.brandcolorcode.com/netflix`는 provenance-only (E2a: not dual-destination). 해석 한정은 portable Principles와 Principles 밖 retained editorial block 인접에도 남김 (B2/B2a). |

## Revision 2026-08-23 (wave4 sol resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v6 (F1·F2 의무 최종 패스). Source FAIL: `docs/reviews/t2-1-wave4-2026-08-23-sol-sample.md` §1.

| Item | Correction |
|---|---|
| Grey 500 A1/A4 | Scale roles restored: Caption/metadata text, `"secondary"` CTA label, inactive nav links. Auth Field border `1px solid #808080` stays a YAML field. Harvested More Info text stays `#FFFFFF`. The three bindings are not merged. |
| Expanded Preview Focus A1/B1 | Source Use `Hover/focus on a row tile` restored as additional observed generic `Focus` on the resting tile and the preview. `focus-visible` treatment is not promoted. |
| §9 nav composition A3 | Top-nav composition (Red “N” left; links center/left; search icon + profile avatar right) moved to Top Nav Link. White circular play remains on Expanded Preview. Log no longer says §9-only unique piece was 1건. |
| Motion/Avoid/Scope/Canvas B2a | Adjacent complete B2a on row-paging `peek to promise more`, billboard `so the artwork lands first`, Avoid causal Don’ts, DVD-envelope reading, and canvas “slightly lifted…distinguishable”. |
| §2/§4/§9/§14 log E2/E2c | Withdrawn: “전 hex 역할 보존”; “§9-only 1건”; badges kind/map both omitted. Log now records field/role context, two §9-only pieces, and badges `Kind: non-interactive` with map omitted. |

### F1 / F2 (v6 mandatory final passes)

- **F1 B2a scan (full DESIGN.md reread):** Editorial/causal/judgement sentences have adjacent complete B2a (`derived editorial implementation inference` / `not Netflix-authored or a separately published UI specification`) on: Scope visual-character paragraph; Scope DVD-envelope reading; Principles (eight items + capture-bound application); Avoid causal Don’ts; Elevation philosophy; Layout whitespace; type-character; Content voice; Motion row-paging purpose and billboard causal delay; Canvas “slightly lifted…so layered surfaces are distinguishable”. Distinctive traits Dalton Maag 2018 line is classified third-party-corroborated, not official product-use. Remaining Scope sentences are surface coverage, `prose-derived` extraction class, or widely documented public history. No unqualified theater/voice/elevation/motion-purpose reading remains outside a B2a limiter.
- **F2 E2 grep (value + field/role context):** Grey 500 `#808080` grep-matched Caption/metadata, `"secondary"` CTA label, inactive nav links in Foundations; Auth Field `Border: 1px solid #808080` is the YAML field; More Info `Text: #FFFFFF` is not that scale role. Top Nav composition grep-matched Red “N” / links center-left / search+avatar. Expanded Preview Use grep-matched `Hover/focus`. Motion B2a grep-matched `peek to promise more` and `so the artwork lands first`. Homepage `https://www.netflix.com` is in Scope and provenance surfaces/Tier 1; dual. `https://brand.netflix.com/en/assets/logos/` is in Assets and provenance Sources; dual. `designyourway.net` is in Font evidence and provenance; dual. `brandcolorcode.com` is provenance-only. `simpleicons` type/slug is in provenance Identity and the portable Assets/Avoid identity-only boundary; dual. Unitless line-heights 1.1–1.5 are in Type roles. B3 five-kind gate plus “Official documentation of a single curve or duration is not that gate” is in Foundations Motion. Four cubic-bezier strings are in provenance omission ledger and absent as promoted values. Badge ×3 grep-matched `Kind: non-interactive`. Fictional names/ages/cities/biographies are absent from DESIGN.md and from provenance beyond the exclusion sentence. Compliance claims match the body.
