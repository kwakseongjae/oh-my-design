# Beusable Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Beusable (뷰저블) is a CX-data and UX-heatmap SaaS built by the Korean analytics company 4Grit (포그리트). Catalog homepage identity is `https://www.beusable.net/`.

Treating the following two URLs as the named first-party live-inspect surfaces of this reconstruction, and treating the blog and company-English URLs as narrative or listed Tier-1 hosts rather than extra token captures, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. This contract covers two brand-owned surfaces from the 2026-07-02 packet: the public homepage `https://www.beusable.net/` and the UX Heatmap feature page `https://www.beusable.net/features/heatmaps`.

Source token note: primary = live Get Started CTA rose (`#ec0047`); brighter `#ff1553` on hover/emphasis. Cyan (`#00b7ff`) is the highlight/link accent; teal (`#13a5bf`/`#16b5d2`) + heatmap yellow (`#f6e136`) are the data-viz hues. Dark hero band `#181818` over white content; pure-black (`#000000`) body text. Treating that note as a register split — `#ec0047` is catalog `primary_color` and the harvested Get Started fill plus filter-tag outline plus nav active ink, `#ff1553` stays hover/emphasis rather than a second identity rose, `#00b7ff` stays highlight/link/back-control rather than an action fill, teal and heatmap yellow stay data-viz rather than chrome, `#181818` stays the dark hero band rather than Ink, and `#000000` stays body ink rather than the hero band — is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

The following visual-character, cinematic-hero, confident, data-forward, dark-stage-frames-visualizations, light-body-keeps-dense-analytics-copy-legible, crisp-high-contrast-white-product-sections, and signalling-via-rose-and-cyan readings are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. The source reads the marketing site as pairing a cinematic dark hero with crisp, high-contrast white product sections. The homepage opens on a near-black band (`#181818`) where a saturated rose CTA (`#ec0047`) and an electric cyan highlight (`#00b7ff`) do the signalling, then drops into bright white (`#ffffff`) content zones where text sits in pure black (`#000000`) and a graphite heading grey (`#2f2f2f`).

The following two-voice, globalized-product-marketing-cadence, bold-Latin-display-for-impact, Pretendard-for-dense-hangul-readability, and quiet-body-and-navigation readings are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. The source treats the typographic personality as a deliberate two-voice split. English display headlines run in the custom **NewRubrik** face at large sizes — 65px / weight 700 with tight `-1.5px` tracking on the feature hero H1 ("UX Heatmap") — while Korean headlines and all UI text run in **Pretendard Variable** (with Noto Sans KR and AppleSDGothicNeo fallbacks), including 55px / 700 Korean section headlines at `-1px` tracking. Body and navigation drop to a quiet 14–16px / 400–500.

The following pill-everything, leans-hard-into-the-pill, flat-depth, band-contrast-over-elevation, color-coded-data-hue, effectively-full-radii, tamer-inner-21px, and one-recurring-elevation-floating-back readings are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. Geometry leans into the pill. Primary CTAs and hero buttons carry effectively-full radii (700px–999px on the dark hero, a tamer 21px on inner feature pages), while dropdown menu rows and filter tags stay tight at 4–5px. Depth is mostly flat — most surfaces show `box-shadow: none`, separated instead by the dark/light band contrast, `#ebebeb` hairlines, and soft cyan-tinted cards (`#e0f6fa`). The one recurring elevation is a floating "back" control that uses a soft `rgba(0,0,0,0.05) 0px 9px 11px -5px` shadow. Color-coded data hues — teal (`#13a5bf`, `#16b5d2`) and a heatmap-warm yellow (`#f6e136`) — surface where the product shows its heatmap and journey-map visualizations.

Treating the following public-history, parent-company, homepage-reach-figure, product-surface-span, current-AI-adjacency, founding-idea-as-visual, hesitate-scroll-click-drop-off-as-product-relationship, and 숫자-대신-히트맵-as-quoted-framing facts as narrative rather than interface tokens, including 9,000-companies as the brand’s own homepage claim quoted as stated rather than independently audited, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Beusable is the CX-data analytics product of 4Grit. Where the source contrasts legacy web analytics as tables of numbers, Beusable's founding idea is recorded as visual: heatmaps, journey maps, funnels, and session analysis that let a team see where users hesitate, scroll, click, and drop off. The product's own framing — "숫자 대신 히트맵" — is quoted from that narrative. The company states broad reach on its homepage — "9,000개 기업이 발견한 것" — as its own claim, quoted as stated, not independently audited. Source §11 names a product surface spanning UX Heatmaps, RealTime Heatmap, Path Plot, Reporting & User Analytics, A/B Testing, Funnels, and Session Report, and records a current-site move into AI with "대화형 BI", "생성형 GEO", and "맞춤형 AI" sitting alongside the heatmap features. Company/parent facts are drawn from the brand's own site and company page `https://www.4grit.com/company-english/`.

The following refusal, embrace, cold-table-heavy-chrome, heatmap-warm-data-feel-legible-and-human, intimidation-that-keeps-non-analysts-away, cinematic-dark-stage-center-frame, single-decisive-rose-action-color, and curiosity-led-copy readings are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. The source treats the design as refusing the cold, table-heavy chrome of traditional analytics suites and the intimidation that keeps non-analysts away, and as embracing a cinematic dark stage that puts visualizations center-frame, a single decisive rose action color, curiosity-led copy that asks "왜인지 알아보기", and heatmap-warm data hues.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the three live-surface jobs below as reconstructed user outcomes from harvested homepage/heatmaps strings, not as independently verified destination routes for every named feature, and keeping source §13 fictional archetypes out of this list, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Independently verified in this packet is the inspected UX Heatmap feature-page URL; named feature labels and the Get Started CTA are harvested strings/controls, not independently verified destination routes.

- Start from the homepage Get Started conversion CTA.
- Open named feature destinations on the site ("UX 히트맵", "CX 저니맵", "대화형 BI", "생성형 GEO", "맞춤형 AI") as harvested top-nav labels.
- Read the UX Heatmap feature page ("UX Heatmap") at the inspected URL `https://www.beusable.net/features/heatmaps`.
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 is labelled fictional archetypes; names and biographies are not Audience. Restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Persona-derived segment labels are not reused as Audience values.

### Distinctive traits

Treating `#ec0047` as catalog `primary_color` plus Get Started fill plus filter-tag outline plus nav active rather than one semantic that also owns hover `#ff1553` or cyan `#00b7ff`, treating `#181818` as the hero band rather than Ink `#000000`, treating 9999px / 700px–999px / 21px / 20px / 5px / 4px as local harvested geometry rather than one radius, treating two-voice type and NewRubrik as English display rather than hangul UI, treating near-flat depth as `box-shadow: none` plus one floating-back shadow rather than a stacked-elevation system, and treating teal/yellow as data-viz rather than chrome, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

- Catalog `primary_color` / Beusable rose `#ec0047` as Get Started fill, filter-tag outline, and nav active ink; hover/emphasis `#ff1553`
- Electric cyan `#00b7ff` as highlight/link/back-control, not the action fill
- Dark hero band `#181818` over white `#ffffff` content; Ink `#000000` body text; heading ladder `#2f2f2f` → `#222222`
- Two-voice type: NewRubrik English display (65px / 700 / tracking `-1.5` / `-1.5px`); Pretendard Variable Korean headlines and all UI (55px / 700 / tracking `-1.0` / `-1px`)
- Pill CTAs: YAML `full` 9999 / `9999px`, body 700px–999px on the dark hero, 21px on inner feature pages; menu/tag 4px / 5px
- Near-flat depth: `box-shadow: none` on most surfaces; floating-back shadow `rgba(0,0,0,0.05) 0px 9px 11px -5px`
- Data-viz teal `#13a5bf` / `#16b5d2` and heatmap yellow `#f6e136`

### Principles

These five items, including each stem and each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. Source HTML comment records interpretive claims such as "see it, don't tabulate it" and "stage the product" as editorial readings connecting observed design to positioning, not directly sourced Beusable statements.

1. **See it, don't tabulate it.** Behavior is shown as heatmaps and journeys, not rows of numbers. *UI implication:* lead with visualization surfaces; keep raw tables secondary and on demand.
2. **One decisive action.** The rose (`#ec0047`) means "do this" and nothing else does. *UI implication:* reserve the saturated rose exclusively for the primary CTA; use cyan for highlights so the next step is never ambiguous.
3. **Curiosity over intimidation.** Copy asks "왜인지 알아보기" rather than presenting a wall of metrics. *UI implication:* frame entry points as questions and discoveries, decode analytics jargon into plain benefit.
4. **Stage the product.** The dark hero band exists to frame the visualizations. *UI implication:* use `#181818` as a cinematic stage for product imagery; keep reading content on white.
5. **Flat and fast.** Clarity beats decorative depth. *UI implication:* separate with band contrast and `#ebebeb` hairlines; reserve shadow for the one floating control.

Treating the following as a capture-bound application of source §7 Do’s and harvested geometry, including rose-as-single-primary-action, cyan-highlights-not-rose, NewRubrik-English-700-tight-tracking, Pretendard-Korean-and-all-UI, dark-hero-frames-visualizations, full-pill-hero-and-tight-menu-tag, hairlines-and-cyan-tint-not-shadows, and teal-yellow-data-viz-only, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

- Use the rose (`#ec0047`) as the single primary action color; brighten to `#ff1553` on hover.
- Keep cyan (`#00b7ff`) for highlights and links, distinct from the action rose.
- Set English display headlines in NewRubrik at weight 700 with tight negative tracking.
- Set Korean headlines and all UI text in Pretendard Variable.
- Frame product visualizations against the dark `#181818` hero band.
- Use full-pill radii on hero CTAs and tight 4–5px radii on menu rows and tags.
- Separate white sections with `#ebebeb` hairlines and cyan-tinted (`#e0f6fa`) cards, not shadows.
- Reserve teal (`#13a5bf`/`#16b5d2`) and heatmap yellow (`#f6e136`) for data visualization.

### Avoid

The following items copy source §7 Don’ts, including rose-dilutes-the-single-action-signal, cyan-means-highlight-not-do-this, NewRubrik-owns-English-display-only, heavy-drop-shadows-not-elevation, pure-black-not-content-section-background, no-additional-saturated-accent-beyond-rose-cyan-and-data-teals-yellow, display-always-700, and hero-actions-are-full-pills. They are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification.

- Do not spread the rose across many elements — it dilutes the single-action signal.
- Do not use cyan (`#00b7ff`) as an action color — it means "highlight," not "do this".
- Do not set Korean headlines in NewRubrik — it owns English display only.
- Do not add heavy drop shadows for elevation — the system separates with band contrast and hairlines.
- Do not use pure black (`#000000`) backgrounds for content sections — reserve `#181818` for the hero band.
- Do not mix additional saturated accent hues beyond rose, cyan, and the data teals/yellow.
- Do not set display headlines in a light weight — display is always 700.
- Do not use sharp square corners on CTAs — hero actions are full pills. Treating this last Don’t, including hero-actions-are-full-pills, as a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including single-do-this-color, highlight-never-reads-as-action, hover-not-a-second-identity-rose, hero-band-not-Ink, heading-ladder-not-one-ink, muted-not-muted-alt, faint-not-faint-alt, hairline-not-hairline-alt, teal-not-teal-alt, cinematic-dark-hero-band, near-shadowless-system, filter-tag-and-nav-active-share-hex-not-one-semantic, Heatmap-Yellow-not-chrome, Ink-not-content-section-background, Body-Grey-not-Ink, Border-Strong-not-Hairline, Pure-White-shared-hex-not-second-canvas, and Cyan-Tint-not-Cyan-Accent, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. Catalog `primary_color` `#ec0047` is the live Get Started fill (rgb(236,0,71)) and YAML `primary`; the same hex is filter-tag text/outline and nav active ink — those jobs share a hex and are not one semantic that also owns hover `#ff1553`. Rose Bright `#ff1553` (rgb(255,21,83)) is hover/emphasis, YAML `primary-hover`, not Primary. Cyan Accent `#00b7ff` (rgb(0,183,255)) is highlight/link/back-control text, YAML `accent`, not an action fill. Teal `#13a5bf` (rgb(19,165,191)) is not Teal Alt `#16b5d2` (rgb(22,181,210)). Heatmap Yellow `#f6e136` (rgb(246,225,54)) is a warm-zone marker, not chrome. Ink Black `#000000` is body text on white, not Surface Dark `#181818` (rgb(24,24,24)) and not a content-section background. Heading Graphite `#2f2f2f` (rgb(47,47,47)) is not Heading Strong `#222222` (rgb(34,34,34)). Body Grey `#444444` is not Ink. Border Strong `#666666` is the Sign In outline, not Hairline. Muted `#777777` is not Muted Alt `#999999`. Faint `#b1b1b1` is not Faint Alt `#cccccc`. Pure White `#ffffff` is page canvas, cards, nav on inner pages, and text on rose/dark; those jobs share a hex and are not a second canvas token. Cyan Tint `#e0f6fa` is not Cyan Accent. Hairline `#ebebeb` is not Hairline Alt `#d9d9d9`.

- **Beusable Rose / Primary** (`#ec0047`): catalog `primary_color`. Get Started CTA background, inline action buttons, active nav state, and filter-tag outline. YAML `primary`.
- **Rose Bright** (`#ff1553`): Hover and emphasis on the primary action. YAML `primary-hover`.
- **Cyan Accent** (`#00b7ff`): Highlight/link color — hero highlight text, inline links, and the floating back control. YAML `accent`.
- **Teal** (`#13a5bf`): Secondary data-visualization hue for charts and journey-map surfaces. YAML `teal`.
- **Teal Alt** (`#16b5d2`): A lighter teal companion for gradient and data accents. YAML `teal-alt`.
- **Heatmap Yellow** (`#f6e136`): Warm heatmap-zone marker. YAML `data-yellow`.
- **Ink Black** (`#000000`): Dominant body-text color across white sections. YAML `ink`.
- **Heading Graphite** (`#2f2f2f`): Display H1 heading color on light surfaces. YAML `heading`.
- **Heading Strong** (`#222222`): Strong Korean section-headline and secondary-button text color. YAML `heading-strong`.
- **Body Grey** (`#444444`): Nav links and standard body copy on white. YAML `body`.
- **Border Strong** (`#666666`): 1px outline color for the Sign In / secondary button on white. YAML `border-strong`.
- **Muted** (`#777777`): Tertiary text, captions, metadata. YAML `muted`.
- **Muted Alt** (`#999999`): Lower-emphasis labels and helper text. YAML `muted-alt`.
- **Faint** (`#b1b1b1`): Faint labels, disabled-adjacent text. YAML `faint`.
- **Faint Alt** (`#cccccc`): Lowest-emphasis text, placeholder-level greys. YAML `faint-alt`.
- **Pure White** (`#ffffff`): Page background, cards, nav, and text on rose/dark. YAML `canvas`.
- **Surface Dark** (`#181818`): Cinematic dark hero band and dark feature strips. YAML `surface-dark`.
- **Cyan Tint** (`#e0f6fa`): Soft cyan-tinted card/section surface for grouped feature blocks. YAML `tint-cyan`.
- **Hairline** (`#ebebeb`): Primary thin border/divider given the near-shadowless system. YAML `hairline`.
- **Hairline Alt** (`#d9d9d9`): Slightly stronger divider for secondary separation. YAML `hairline-alt`.

### Spacing

YAML `spacing`: xs 4, sm 8, md 12, base 16, lg 18, xl 28, xxl 32. Body layout states a base unit of 8px and a scale 4px, 6px, 8px, 10px, 11px, 12px, 14px, 16px, 18px, 28px, 32px.

Treating that YAML scale and the body 8px-base / extra 6px / 10px / 11px / 14px steps as recorded spacing in this packet rather than a universal spacing token for every unlisted control, treating unitless YAML steps as not converted into a required px suffix, treating 6px / 10px / 11px / 14px as body-stated extras rather than invented YAML keys, treating component padding as staying with the components, and treating inner nav/menu rows as tight-inner-nav-6-10px rather than a second spacing token, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Hero pill CTAs carry 28–32px horizontal padding. Get Started pads `8px 16px`. Sign In pads `10px 18px`. Hero glass pads YAML `0 28px` / body `0px 28px`. Inner nav/menu rows stay tight at 6–10px. Filter tags pad `4px 10px`. Floating back pads `9px 15px`.

### Shape

YAML `rounded`: sm 4, md 5, lg 21, full 9999 (`9999px`).

The following local-geometry reading, including 4px-not-5px, 20px-not-21px, 21px-not-9999px, 700px–999px-not-9999px, and the Small/Tag/Inner-CTA/Full role labels, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. 4px / 5px / 20px / 21px / 700px–999px / `9999px` remain local harvested geometry, not a universal radius for every unlisted control. YAML `rounded.sm` 4 is dropdown menu rows. YAML `rounded.md` 5 is filter/section tags. YAML `rounded.lg` 21 is inner-page CTA geometry; Sign In is 21px; Get Started on inner feature pages is also recorded as 21px — those are not one component. Card and floating-back radii are 20px, not 21px. YAML `rounded.full` 9999 / `9999px` is Get Started on hero, hero glass pills, and data markers; body also writes 700px–999px on the dark hero. Do not merge 20px cards into 21px inner CTAs, or 700px–999px into `9999px`.

Observed radii in the body: Small 4px (dropdown menu rows); Tag 5px (filter/section tags); Inner CTA 20–21px (buttons and floating controls on feature pages); Full 700–999px / `9999px` (hero CTAs, glass pills, data markers).

### Elevation

Treating the table Use assignments (Most surfaces — hero, nav, headings, cards; Primary section separation; White card outlines, dividers; Floating back control on feature pages) as source-stated uses in this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Most surfaces — hero, nav, headings, cards |
| Band (Level 1) | Dark `#181818` vs white `#ffffff` contrast | Primary section separation |
| Hairline (Level 2) | 1px solid `#ebebeb` / `#d9d9d9` | White card outlines, dividers |
| Floating (Level 3) | `rgba(0,0,0,0.05) 0px 9px 11px -5px` | Floating back control on feature pages |

YAML `tokens.shadow.none` is `none`. YAML `tokens.shadow.floating` is `rgba(0,0,0,0.05) 0px 9px 11px -5px`.

The following near-flat and shadow-philosophy readings, including depth-from-band-contrast-and-hairlines-rather-than-elevation, single-recurring-shadow-reserved-for-the-floating-back, emphasis-from-rose-or-cyan-not-a-heavier-shadow, and soft-wide-negative-spread-lift, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. The source reads Beusable as a near-flat system. Live inspection found `box-shadow: none` across the hero, nav, headings, and most cards; depth comes from the dark/light band contrast and thin `#ebebeb` hairlines rather than elevation. The single recurring shadow is a soft, wide, negative-spread lift (`rgba(0,0,0,0.05) 0px 9px 11px -5px`) reserved for the floating back control. When the product needs emphasis it reaches for color — the rose `#ec0047` action or the cyan `#00b7ff` highlight — not a heavier shadow.

### Motion

Treating the duration table, easing names, easing-table Use labels Arriving / Dismissals / Two-way transitions, signature heatmap-paint-in / pill-press / rose-hover / floating-back, and reduced-motion line as source-stated rather than computed CSS, treating omitted `ease-exit` as matching the legacy spec-template example, treating `ease-enter` / `ease-exit` / `ease-standard` as the nintendo/workday/barogo unattributed set rather than computed CSS, and treating the "functional and data-forward" / "signals steadiness and precision" readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 220ms | Card/section reveal, dropdown, sheet |
| `motion-slow` | 340ms | Page-level transitions, hero reveal, heatmap paint-in |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, dropdowns, floating back control |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including functional-and-data-forward, heatmap-paint-in-as-data-arriving-not-decoration, pill-CTA-scale-opacity, rose-hover-shift, floating-back-eases-in-with-soft-shadow, no-bounce-or-spring steadiness-and-precision, and reduced-motion-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. Motion is described as functional and data-forward. Heatmap and journey visualizations paint in progressively at `motion-slow / ease-enter` so the reveal reads as data arriving, not decoration. Pill CTAs respond to press with a subtle scale/opacity shift and hover shifts rose `#ec0047` → `#ff1553`. The floating back control eases in with its soft shadow. No bounce or spring — an analytics product signals steadiness and precision. Under `prefers-reduced-motion: reduce`, all transitions collapse to instant and visualizations render in their final state immediately; the product stays fully functional.

Treating unattributed cubic-bezier curves as omitted rather than promoted, while keeping source-stated names and uses, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Exact cubic-bezier curves are unattributed — `ease-standard`, `ease-enter`, and `ease-exit` match the nintendo/workday/barogo unattributed set, and `ease-exit` matches the legacy spec template — and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including NewRubrik-as-English-display-only, Pretendard-Variable-as-Korean-and-all-UI, and fallbacks-listed-with-Pretendard-not-English-display-face-not-a-substitute, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | Feature hero H1 "UX Heatmap" in NewRubrik 65px / 700 / tracking `-1.5` / `-1.5px`. Korean section headlines Pretendard Variable 55px / 700 / tracking `-1.0` / `-1px`. Nav, button, body, and caption roles in Pretendard Variable as the YAML table. |
| Declared fallbacks | `Noto Sans KR`, `AppleSDGothicNeo` — fallbacks listed with Pretendard Variable, not the English display face and not a substitute for NewRubrik or Pretendard Variable. |

The following font-use boundary, including NewRubrik as English display rather than hangul headlines or functional UI, Pretendard Variable as Korean headlines and every functional UI string, and a fallback as not the brand face, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Do not replace NewRubrik with Pretendard Variable on English display. Do not set Korean headlines in NewRubrik. Do not present Noto Sans KR or AppleSDGothicNeo as NewRubrik or as Pretendard Variable.

### Family

- **Display (English):** `NewRubrik` — YAML `family.display`. Custom Latin display face used for English hero headlines (e.g. "UX Heatmap") at weight 700 with tight negative tracking.
- **Body & Korean:** `Pretendard Variable` — YAML `family.body`. Korean headlines and all UI/body text, with `Noto Sans KR` and `AppleSDGothicNeo` fallbacks.

### Type roles

Verified YAML line-height values are the unitless ratios `1.00`, `1.31`, `1.40`, and `1.50`. The following ratio-versus-size-local reading, including 1.00-(65px)-and-1.31-(72px)-as-local-companions-not-YAML-replacements and YAML-tracking-versus-body-px-unmerged, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px. Body also writes `1.00 (65px)` and `1.31 (72px)`; those px companions stay local to the table notes and are not a replacement for the YAML ratios. YAML tracking `-1.5` / `-1.0` and body `-1.5px` / `-1.0px` / `-1px` stay unmerged writings.

The following two-voices-two-scripts, tight-display-tracking, heavy-display-light-body, hangul-first-body-sizing, never-swap-roles, weight-jump-is-the-primary-hierarchy-signal, and dense-legible-Korean-analytics-copy type-rule readings are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. NewRubrik owns English display impact; Pretendard Variable owns Korean headlines and every functional UI string. They never swap roles. Headlines compress (`-1.5px` on the 65px English H1, `-1px` on the 55px Korean section heading); body stays at normal tracking. Weight 700 for headlines and CTAs, 400–500 for reading and navigation — the weight jump is the primary hierarchy signal. Body sits at 14–16px with 1.50 line-height for dense, legible Korean analytics copy.

| Role | Font | Size | Weight | Line height (YAML) | rem | Tracking | Use (YAML) |
|---|---|---:|---:|---:|---:|---:|---|
| Display Hero (EN) | NewRubrik | 65px | 700 | 1.00 | 4.06rem | YAML `-1.5`; body `-1.5px` | English display H1, NewRubrik |
| Section Heading (KR) | Pretendard Variable | 55px | 700 | 1.31 | 3.44rem | YAML `-1.0`; body `-1.0px` / `-1px` | Korean section headline, Pretendard |
| Nav Link | Pretendard Variable | 14px | 500 | 1.40 | 0.88rem | body `normal` | Top nav links, Pretendard |
| Button | Pretendard Variable | 14px | 700 | 1.00 | 0.88rem | body `normal` | CTA / Get Started label |
| Body | Pretendard Variable | 16px | 400 | 1.50 | 1.00rem | body `normal` | Standard reading text |
| Caption / Menu | Pretendard Variable | 14px | 400 | 1.40 | 0.88rem | body `normal` | Sub-nav / dropdown menu labels |

YAML `nav` / `button` / `body` / `caption` have no `tracking` field; none is invented. Body table writes `normal` for those four roles. Treating Display Hero on light surfaces as Heading Graphite `#2f2f2f` and the source §9 dark-hero English H1 as `#ffffff`, and not merging those ink jobs, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

### Assets

Treating catalog logo metadata as a first-party favicon file on Beusable’s CDN, not a Google favicon lookup, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

- Marketing-site favicon: `https://dream-cdn.beusable.net/home/images/favicon.ico`

Treating verified heatmap and journey-map visualizations as not replaceable with invented brand-color decoration is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Treating graph-not-adopted preservation of that contract, and treating the table below as that philosophy-layer contract rather than computed per-component CSS, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. The source state contract is preserved here while the catalog graph is not adopted. Hex values and geometry in the harvested components remain source-stated. The source HTML comment places sections 10–15 in the philosophy layer.

Treating these rows as empty/loading/error/success/skeleton/disabled treatments for an analytics-product surface (sessions, heatmap render, dashboard fetch, tracking install), and not copying them onto harvested marketing CTAs as computed paints, including table characterizations “No clutter”, “consistent with the near-shadowless system”, “rather than a full-screen block”, “never a bare 오류가 발생했습니다”, “not just 필수”, “No celebratory emoji”, “preserve brand read”, filter-summary-visible-to-adjust-scope, cyan-progress-accent-where-a-determinate-bar-applies, previous-values-stay-visible, plain-language-what-to-do-next, calm-tone-with-next-step-linked, and rose-fades-rather-than-turning-grey, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (no data collected yet)** | White canvas. Single Ink Black (`#000000`) line explaining no sessions are collected yet, plus one rose (`#ec0047`) CTA to install/start tracking. No clutter. |
| **Empty (report, zero results)** | Muted (`#777777`) single line stating nothing matched the filter, with the filter summary visible above to adjust scope. |
| **Loading (heatmap render)** | Skeleton blocks at final dimensions on white with a flat pulse — consistent with the near-shadowless system. Cyan (`#00b7ff`) progress accent where a determinate bar applies. |
| **Loading (dashboard fetch)** | Previous values stay visible; a subtle cyan progress indicator rather than a full-screen block. |
| **Error (data fetch failed)** | Inline message in Ink Black with a plain-language explanation and a retry — states what to do next, never a bare "오류가 발생했습니다". |
| **Error (form validation)** | Field-level message below the input describing what is valid, not just "필수". |
| **Success (tracking installed / report saved)** | Brief inline confirmation in a calm tone with next-step detail linked below. No celebratory emoji. |
| **Skeleton** | Neutral blocks at final dimensions, 20px radius on cards, flat pulse. |
| **Disabled** | Faint (`#b1b1b1`) / Faint Alt (`#cccccc`) text on reduced-opacity surface; rose actions fade rather than turn grey to preserve brand read. |

Characterizations in that table such as “No clutter”, “consistent with the near-shadowless system”, “rather than a full-screen block”, “never a bare 오류가 발생했습니다”, “not just 필수”, “No celebratory emoji”, “preserve brand read”, filter-summary-visible-to-adjust-scope, cyan-progress-accent-where-a-determinate-bar-applies, previous-values-stay-visible, plain-language-what-to-do-next, calm-tone-with-next-step-linked, and rose-fades-rather-than-turning-grey are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records no generic Focus capture with a colour. The `focus-visible` rows do not carry a colour. A later generic Focus observation would not be `focus-visible` evidence. Source `motion-fast` names “Hover, button press, focus” as duration use; that duration row is not `focus-visible` treatment evidence.

Get Started YAML `states: hover #ff1553` is a captured hover on that control. That captured hover is not `focus-visible` treatment.

Cyan-Tinted Card, White Feature Card, Filter Tag, and Heatmap Data Marker have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. White Feature Card has no YAML type; Type is not invented.

### Get Started (Primary)

- Role: primary conversion CTA in header and hero — the single primary action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ec0047`
- Text: `#ffffff`
- Radius: YAML `9999px` (full pill on hero); body also records 21px on inner feature pages
- Padding: `8px 16px`
- Height: YAML / body `38px`; §8 also records 36–38px
- Font: YAML `14px / 700 Pretendard Variable`; body 14px Pretendard Variable weight 700
- Use: YAML `Primary Get Started CTA, full pill`
- Observed: default, plus named hover `#ff1553`
- YAML `tokens.components.cta-primary`
- Field note: The following unmerged-field reading, including harvested-height-not-touch-target on `38px` versus §8 `36–38px`, 9999px-not-21px, fill-jobs-not-one-semantic on `#ec0047`, `#ffffff` label-not-a-second-canvas-token, Role-as-the-single-primary-action, and right-aligned-21px-in-white-light-page-nav-not-global-alignment, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#ec0047` is this control’s fill and catalog `primary_color`. It is also filter-tag outline and nav active ink; those jobs are not this button. `#ffffff` is this control’s label and on-primary; it is also canvas / card fill / inner-nav band. Height `38px` is this action’s harvested height, not a touch-target policy and not the 36–38px span as a second token. Radius `9999px` is YAML `full` / this control on hero; 21px on inner feature pages is this same named CTA’s inner-page writing, not Sign In 21px and not card 20px.
- Local recipe (source §9 light-page nav): on the white header, this inner-page Get Started is the right-aligned `#ec0047` pill, white text, 21px radius, with Pretendard 14px weight 500 `#444444` links and active rose `#ec0047`. Treating that as one local relationship with Top Nav, not as global alignment/radius tokens, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the primary Get Started CTA |
| hover | applicable | Pointer-web button; captured treatment `#ff1553` |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says rose actions fade rather than turning grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as the primary conversion CTA and records §14 Loading (action submit is not a named Get Started row) / Success (tracking installed / report saved) as philosophy-layer rows; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

Additional observed named state: hover `#ff1553`. That appearance is a captured hover, not an observed `focus-visible` treatment.

### Sign In (Outline, Light)

- Role: secondary action on white surfaces
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#222222`
- Border: 1px solid `#666666`
- Radius: `21px`
- Padding: `10px 18px`
- Height: `38px`
- Font: YAML `14px / 700 Pretendard Variable`; body 14px Pretendard Variable weight 700
- Use: YAML `Sign In / secondary action on white surface`
- Observed: default only
- YAML `tokens.components.button-outline-light`
- Field note: The following unmerged-field reading, including transparent-not-canvas, `#222222` not-Ink, 21px-not-9999px, not merging this outline with Get Started, and height-38px-not-Hero-Glass-60px, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. Background `transparent` is this control’s field (body §4; YAML has no `bg`). `#222222` is this control’s text and Heading Strong, not Ink. Radius 21px is YAML `rounded.lg` / this control; it is not Get Started hero `9999px` and not the inner-page Get Started 21px writing of a different component. Height `38px` is this action, not Hero Glass 60px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the Sign In / secondary action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A secondary action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as Sign In / secondary action on white; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Hero Glass Pill

- Role: translucent hero CTA on the dark band
- Kind: interactive
- Type: button
- Anatomy: label
- Text: `#00b7ff`
- Border: 1px solid rgba(255,255,255,0.24)
- Radius: `9999px`
- Padding: YAML `0 28px`; body `0px 28px`
- Height: YAML / body `60px`; §8 also records 56–60px
- Font: YAML `16px / 400 Pretendard Variable`; body 16px Pretendard Variable weight 400
- Use: YAML `Hero glass CTA on dark band, translucent bg + white hairline border`; body "9,000개 기업이 발견한 것"
- Observed: default only
- YAML `tokens.components.hero-glass-pill`
- Field note: The following unmerged-field reading, including translucent-bg-without-an-invented-fill, `#00b7ff` text-not-action-fill, 60px-not-56–60px as a second token, YAML `0 28px` versus body `0px 28px`, and 9999px-not-21px, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. No fill hex is recorded; YAML use says translucent bg + white hairline border. `#00b7ff` is this control’s text and Cyan Accent, not Get Started fill. Height `60px` is this action’s harvested height, not the 56–60px span as a second token and not Get Started 38px. Radius `9999px` is this control and YAML `full`; it is not Sign In 21px.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the hero glass CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A hero glass action can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a hero glass CTA ("9,000개 기업이 발견한 것"); exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Floating Back

- Role: floating "뒤로" back control on feature pages
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#ffffff`
- Text: `#00b7ff`
- Radius: `20px`
- Padding: `9px 15px`
- Shadow: `rgba(0,0,0,0.05) 0px 9px 11px -5px`
- Font: YAML `16px / 400 Pretendard Variable`; body 16px Pretendard Variable weight 400
- Use: YAML `Floating back button on feature pages`
- Observed: default only
- YAML `tokens.components.back-fab`
- Field note: The following unmerged-field reading, including `#ffffff` fill-not-a-second-canvas-token, `#00b7ff` text-not-Get-Started-fill, 20px-not-21px, and shadow-not-copied-onto-cards, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#ffffff` is this control’s fill and Canvas. `#00b7ff` is this control’s text and Cyan Accent. Radius 20px is this control and the card radius; it is not YAML `rounded.lg` 21 and not `9999px`. The floating shadow is this control’s field and YAML `shadow.floating`, not a card elevation.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the floating back control |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A back control can be unavailable; visual treatment omitted |

Loading, error, and success applicability are omitted. Source names this control as a floating "뒤로" back control; exact destination/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Filter Tag

- Role: feature filter / section tag
- Type: badge
- Anatomy: label
- Background: transparent
- Text: `#ec0047`
- Border: 1px solid `#ec0047`
- Radius: `5px`
- Padding: `4px 10px`
- Font: YAML `14px / 500 Pretendard Variable`; body 14px Pretendard Variable weight 500
- Use: YAML `Feature filter / section tag`
- Observed: default only
- YAML `tokens.components.filter-tag`
- Field note: The following unmerged-field reading, including transparent-not-canvas, `#ec0047` outline-not-Get-Started-fill, and 5px-not-4px, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#ec0047` is this tag’s text and border and catalog `primary_color`; it is not this tag’s fill. Radius 5px is YAML `rounded.md` / this control, not menu-row 4px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Heatmap Data Marker

- Role: warm-zone marker in heatmap visualizations
- Type: badge
- Anatomy: marker
- Background: `#f6e136`
- Text: `#000000`
- Radius: `9999px`
- Use: YAML `Heatmap warm-zone data marker`
- Observed: default only
- YAML `tokens.components.data-marker`
- Field note: The following unmerged-field reading, including `#f6e136` marker-not-chrome, `#000000` marker-text-not-hero-band, and 9999px-not-card-20px, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#f6e136` is this marker’s fill and Heatmap Yellow, not a page-wide accent. `#000000` is this marker’s text and Ink, not Surface Dark. Radius `9999px` is this marker and YAML `full`; it is not card 20px.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted.

### Cyan-Tinted Card

- Role: soft cyan-tinted feature/grouping card
- Type: card
- Anatomy: surface
- Background: `#e0f6fa`
- Text: `#222222`
- Radius: `20px`
- Use: YAML `Soft cyan-tinted feature card`
- Observed: default only
- YAML `tokens.components.tinted-card`
- Field note: The following unmerged-field reading, including `#e0f6fa` tint-not-Cyan-Accent, `#222222` not-Ink, 20px-not-21px, and §9-only heading/body local tuple, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#e0f6fa` is this card’s fill and Cyan Tint, not `#00b7ff`. `#222222` is YAML `fg` / Heading Strong. Radius 20px is this card, not YAML `rounded.lg` 21. Source §9-only mixed anatomy is heading `#222222` with body `#444444` on the same card; that local tuple stays on this card and is not YAML Body 16px / 400 as a page-wide card token.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### White Feature Card

- Role: white feature card separated by hairline, no shadow
- Anatomy: surface
- Background: `#ffffff`
- Border: 1px solid `#ebebeb`
- Radius: `20px`
- Use: White feature card separated by hairline, no shadow
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.
- Field note: The following unmerged-field reading, including `#ffffff` fill-not-Cyan-Tint, `#ebebeb` hairline-not-elevation, 20px-not-21px, and §9-only title/body local tuple, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#ffffff` is this card’s fill and Canvas. Border `1px solid #ebebeb` is this card and Hairline, not a shadow. Radius 20px is this card, not Sign In 21px. Source §9-only mixed anatomy is title Pretendard Variable weight 700 / `#222222` with body 16px weight 400 / `#000000`; that local tuple stays on this card.

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted.

### Header / Navigation band

- Role: header/navigation band (parent surface)
- Anatomy: surface
- Background: `#181818` (dark home) / `#ffffff` (inner pages)
- Use: Top horizontal nav parent surface
- Observed: default only
- Field note: The following parent-surface reading, including header-fill-not-nav-item-background and dark-home-not-inner-white as two writings of the parent, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#181818` is this band’s dark-home fill and Surface Dark. `#ffffff` is this band’s inner-page fill and Canvas. YAML does not record a nav component; this header fill is the parent surface, not a Top Nav item Background.

### Top Nav

- Role: top horizontal navigation item
- Kind: interactive
- Anatomy: label
- Text: `#ffffff` (dark) / `#444444` (light)
- Font: 14px Pretendard Variable weight 500
- Active: rose `#ec0047` text
- Use: Top horizontal nav — "UX 히트맵", "CX 저니맵", "대화형 BI", "생성형 GEO", "맞춤형 AI"
- Observed: static default and static active appearances. Treating that active appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.
- Field note: The following unmerged-field and captured-variant readings, including dark-text-not-light-text, `#ec0047` active-not-Get-Started-fill, 14px-500-not-Button-700, item-not-header-fill, captured-variant-not-click-transition, and light-page-nav-right-aligned-Get-Started-as-one-local-relationship, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. `#ffffff` / `#444444` are this item’s text on dark/light parents, not the parent fills. Weight 500 is this item and Nav Link; Button is 700. Active `#ec0047` is this component’s active field, not Get Started fill. This item has no Background field; the header band fills stay on the parent surface.
- Local recipe (source §9 light-page nav): white header, Pretendard 14px weight 500 links in `#444444`, active item rose `#ec0047`, Get Started CTA right-aligned as a `#ec0047` pill, white text, 21px radius. Treating that as one local relationship of this light-page nav plus the inner-page Get Started, not as global alignment/radius tokens and not as a second nav type, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Named as the top navigation item |
| hover | applicable | Pointer-web nav item; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A top nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Nav meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | Nav meaning is destination, not action-outcome confirmation |

Additional observed named state: static active appearance (rose `#ec0047` text). Treating that appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

### Feature Dropdown Row

- Role: feature dropdown menu row
- Kind: interactive
- Type: listItem
- Anatomy: label
- Background: `#ffffff`
- Text: `#444444`
- Radius: `4px`
- Padding: `6px 10px`
- Font: YAML `14px / 400 Pretendard Variable`; body 14px Pretendard Variable weight 400
- Active: `#ec0047`
- Use: YAML `Feature dropdown menu row`; body Features / Pricing / UX Heatmaps…
- Observed: static default and static active appearances. Treating that active appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.
- YAML `tokens.components.nav-menu-item`
- Field note: The following unmerged-field reading, including `#ffffff` row-fill-not-header-band, `#444444` not-Ink, 4px-not-5px, active-not-Get-Started-fill, and captured-variant-not-click-transition, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. `#ffffff` is this row’s fill and Canvas. `#444444` is this row’s text and Body Grey. Radius 4px is YAML `rounded.sm` / this control, not filter-tag 5px. Active `#ec0047` is this component’s active field, not Get Started fill.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the feature dropdown menu row |
| hover | applicable | Pointer-web menu row; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A menu row can be unavailable; visual treatment omitted |
| loading | not-applicable | A dropdown row selects a destination; the row itself does not enter a loading state |
| error | not-applicable | Menu-row meaning is destination versus resting, not a request or validation failure on the row |
| success | not-applicable | Menu-row meaning is destination, not action-outcome confirmation |

Additional observed named state: static active appearance (`#ec0047`). Treating that appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 18, xl 28, xxl 32. Body layout repeats an 8px base and a scale 4px, 6px, 8px, 10px, 11px, 12px, 14px, 16px, 18px, 28px, 32px. Hero pill CTAs pad horizontally at 28–32px; Get Started pads `8px 16px`; Sign In pads `10px 18px`; hero glass pads YAML `0 28px` / body `0px 28px`; inner nav/menu rows stay at 6–10px.

The “band contrast over borders”, “flat separation”, and “pill rhythm” readings in this section, including dark-hero-versus-white-as-the-primary-structural-divide, hairlines-and-cyan-tinted-cards-without-elevation, and repeated-full-radius-CTAs-as-a-friendly-cadence, are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification.

Recorded layout:

- Centered single-column dark hero anchored by the CTA and highlight text. Treating the following as a source §9-only mixed pair on that dark `#181818` band, not as extra YAML component fields, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification. English H1 in NewRubrik 65px weight 700, line-height 1.00, letter-spacing `-1.5px`, color `#ffffff`; a glass CTA pill (translucent dark background, `#00b7ff` text, 1px solid rgba(255,255,255,0.24) border, full radius, YAML `0 28px` / body `0px 28px` padding, 60px height); a solid rose CTA (`#ec0047` background, white text, full pill, `8px 16px`, 14px Pretendard weight 700 — hover `#ff1553`).
- White product sections alternate as full-width bands beneath the hero
- Feature pages use a left sub-nav of feature rows plus a large display H1
- Cards group related visualizations at a 20px radius
- Band contrast over borders: the dark hero (`#181818`) versus white (`#ffffff`) sections is the primary structural divide, not heavy rules
- Flat separation: within white zones, `#ebebeb` and `#d9d9d9` hairlines and cyan-tinted (`#e0f6fa`) cards separate content without elevation
- Pill rhythm: repeated full-radius CTAs create a consistent, friendly cadence across conversion points

Source breakpoint table. Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 65px / 55px / 60px / 38px / 56–60px / 36–38px figures as surface measurements in this packet rather than universal layout tokens, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating collapsing / image-behavior rows as source-stated rather than a cross-viewport specification, treating collapsing extras hero-headline-compresses-weight-700-maintained / sub-nav-wraps-scrolls / product-bands-stack / full-width-reduce-padding, treating image-behavior extras heatmap-on-dark-or-white-cards / data-markers-keep-yellow-at-all-sizes / cards-maintain-20px-radius-no-shadow rather than a new elevation rule, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, hero headline compresses, nav collapses to menu |
| Tablet | 640-1024px | Moderate padding, 2-up feature cards, sub-nav wraps |
| Desktop | 1024-1440px | Full layout, centered dark hero, multi-column product bands |

Collapsing strategy recorded in the source: Hero 65px English / 55px Korean display compresses on mobile; weight 700 maintained. Feature sub-nav: left rail of feature rows wraps/scrolls on narrow viewports. Product bands: multi-column → stacked single column. Dark hero and white sections keep full-width treatment, reduce internal padding.

Image behavior: Heatmap and journey-map visualizations sit on the dark hero band or white cards at 20px radius. Data markers keep the heatmap yellow (`#f6e136`) at all sizes. Cards maintain 20px radius across breakpoints, no shadow.

Touch-target record: Hero glass pills at 56–60px height with 28–32px horizontal padding; Get Started CTA at 36–38px height, full pill; nav links and dropdown rows spaced for touch within the header.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-07-02)

The live strings below are source-stated. Treating the parenthetical characterizations (homepage title/positioning / social-proof-as-discovery / see-intent-needs-journey-failure-at-a-glance) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

- "가장 쉬운 CX데이터 분석" — homepage title/positioning (easiest CX-data analysis). *(verified live 2026-07-02)*
- "9,000개 기업이 발견한 것" — hero glass CTA (social proof as discovery). *(verified live 2026-07-02)*
- "사용자의 의도와 요구, 과정 그리고 실패를 한눈에 확인할 수 있습니다." — UX Heatmap feature H2 (see intent, needs, journey, failure at a glance). *(verified live 2026-07-02)*
- Control and section strings from harvested surfaces: "UX 히트맵"; "CX 저니맵"; "대화형 BI"; "생성형 GEO"; "맞춤형 AI"; "UX Heatmap"; "뒤로"; document.title "뷰저블 - 가장 쉬운 CX데이터 분석｜Beusable"; document.title "UX Heatmap｜Beusable"

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Beusable-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — including plain-curiosity-driven-outcome-focused, turns-web-app-analytics-from-an-intimidating-numbers-game-into-a-see-why-invitation, social-proof-stated-as-discovery-not-a-boast, conversational-question-led-CTAs, heatmap-over-raw-numbers, interrogate-user-behavior-rather-than-a-dashboard-to-admire, 왜인지-알아보기-and-직접-물어보기-as-derived-voice-not-Observed, Plain-benefit / curiosity-framed / not-bragging / Functional-product-named tone-table labels, and forbidden-register intimidating-statistics-speak / hype-superlatives / fear-based-conversion-pressure / jargon-left-undecoded — are a derived editorial implementation inference from the verified surfaces; they are not Beusable-authored or a separately published UI specification. They are not the Observed strings above. "왜인지 알아보기" and "직접 물어보기" sit in this derived voice only.

The source describes Beusable's voice as **plain, curiosity-driven, and outcome-focused** — it turns web/app analytics from an intimidating numbers game into a "see why" invitation. The homepage frames the product as "가장 쉬운 CX데이터 분석", and social proof is stated as a discovery, not a boast: "9,000개 기업이 발견한 것". CTAs are conversational and question-led ("왜인지 알아보기", "직접 물어보기"), positioning the product as a way to interrogate user behavior rather than a dashboard to admire.

| Context | Tone |
|---|---|
| Hero headline | Plain-benefit, curiosity-framed. "가장 쉬운 CX데이터 분석." |
| Social proof | Stated as discovery, not bragging. "9,000개 기업이 발견한 것." |
| Feature nav | Functional, product-named. "UX 히트맵", "CX 저니맵", "대화형 BI". |
| CTAs | Conversational, question-led. "왜인지 알아보기", "직접 물어보기". |
| Feature copy | Explains the analytics concept in user language, heatmap over raw numbers. |

**Forbidden register** (source): intimidating statistics-speak with no plain-language framing, hype superlatives, fear-based conversion pressure, jargon left undecoded.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- `ease-enter` / `ease-exit` / `ease-standard` cubic-bezier curves
- hover visual treatments other than Get Started `#ff1553`; press and `focus-visible` visual treatments
- loading, error, and success visual treatments on the harvested buttons, and the omitted loading/error/success applicability fields on Get Started, Sign In, Hero Glass Pill, and Floating Back
- interactive kind and state-applicability map for Filter Tag, Heatmap Data Marker, Cyan-Tinted Card, and White Feature Card
- YAML `tracking` on Nav Link, Button, Body, and Caption / Menu
- an exact fill value for the hero glass translucent background
- motion animation names, transition properties, and any duration beyond the three source tokens — promote only after per-component computed capture of all five kinds; a single named duration is not that gate
