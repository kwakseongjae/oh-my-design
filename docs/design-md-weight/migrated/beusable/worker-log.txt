T2 마이그레이션을 시작합니다. 규칙집·골든 샘플·레거시 원문을 순서대로 읽고 이어서 옮기겠습니다.필수 문서와 골든 샘플부터 읽겠습니다.골든 샘플과 레거시 beusable 원문을 이어서 읽겠습니다.골든 샘플 원장과 최근 통과본의 형태를 맞춰 보겠습니다.원문 값과 골든/통과본 형태를 맞춰 Core v2 `DESIGN.md`를 작성합니다.본문 초안을 처음부터 다시 읽어 B2a 한정이 빠지지 않았는지 확인합니다.원장 `provenance.md`를 작성한 뒤, grep으로 목적지를 대조해 `migration-log.md`를 쓰겠습니다.게이트와 Portable Core 검사를 돌리고, 허위 목적지·가상 페르소나 잔존을 다시 grep합니다.Beusable legacy `DESIGN.md`를 Core v2로 옮겼습니다. 규칙집 v7을 기준으로 작성했고, 본문 작성 뒤 B2a 전편 재독(F1)과 migration-log 행별 grep 대조(F2)를 했습니다.

산출물:
- `docs/design-md-weight/migrated/beusable/DESIGN.md`
- `docs/design-md-weight/migrated/beusable/provenance.md`
- `docs/design-md-weight/migrated/beusable/migration-log.md`

보존·처리 요지:
- YAML unitless lineHeight `1.00` / `1.31` / `1.40` / `1.50`를 비율로 유지
- 컴포넌트 primitive type을 컴포넌트별로 유지 (`button`×4, `badge`×2, `card`, `listItem`)
- Get Started 캡처 hover `#ff1553`는 hover로만 두고 `focus-visible`에 올리지 않음
- 카드·뱃지는 kind/map 생략(C4); CTA 4개는 loading/error/success 생략(C2); 내비/드롭다운은 목적지 선택 역할로 `not-applicable`
- 무출처 cubic-bezier 3개는 provenance omission ledger에만 두고, duration·easing 이름·reduced-motion·B3 다섯 증거 종류는 Foundations에 유지
- §13 가상 페르소나는 삭제, sidecar 재수록 없음

검사: `--gate-only` PASS, Core `portable_core: true`. DESIGN SHA-256 `2d08271c37cd20f0371bcc0f74e4dda83096e43bad417c564e287bbb96c1293e`. F3 감사와 sol 의미 레인은 다음 세션. 카탈로그 채택 아님.

DONE migrated=1
