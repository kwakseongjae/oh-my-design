T2 마이그레이션을 시작하기 위해 규격, 규칙집, 골든 샘플, 그리고 classting legacy 원문을 순서대로 읽겠습니다.규격과 규칙집을 읽었습니다. 다음으로 승인된 musinsa 골든 샘플과 classting 원문을 읽습니다.classting 산출물 폴더가 이미 있습니다. 원문·골든 샘플·기존 이관본을 대조해 누락과 형태를 확인하겠습니다.기존 이관본이 있습니다. 검증 사이드카·게이트·최근 통과 표본을 대조해 유지할 값과 고쳐야 할 점을 확인합니다.기존 초안에서 Proof 사이드카를 본문으로 올린 점과 `prose-derived`/`independently verified` 승격을 고친 뒤, 규칙집 v7에 맞춰 세 파일을 다시 쓰겠습니다.본문에서 Proof 사이드카 샘플과 YAML 추출 클래스 승격을 빼고, YAML use 문자열로 Primary tasks를 닫겠습니다.본문을 처음부터 다시 읽고 B2a를 스캔한 뒤, 값마다 grep으로 줄 번호를 확인합니다.줄 번호를 확인했습니다. provenance와 migration-log를 그 번호에 맞춰 작성합니다.게이트가 Proof 사이드카의 CT Corp. 로고 스톱 컬러를 발명 토큰으로 잡았습니다. provenance에서 그 hex를 빼고 다시 검사합니다.Classting legacy `DESIGN.md`를 Core v2 Portable Core로 옮겼습니다. 규칙집 **v7**.

산출물:
- `docs/design-md-weight/migrated/classting/DESIGN.md`
- `docs/design-md-weight/migrated/classting/provenance.md`
- `docs/design-md-weight/migrated/classting/migration-log.md`

보존·분리 요지:
- YAML unitless lineHeight `1.5` / `1.0` / `1.29` / `1.21` 비율 유지. `display-md` lineHeight는 원문에 없어 발명하지 않음.
- `type: button`×4 + `card`×2 + `badge` 컴포넌트별 유지. Orange tag는 YAML type이 없어 발명하지 않음.
- `#00C896` / `#17A27E` / `#EFFFFA`, `#ED872D` / `#ED872C` / `#ED8936`, `#FAFAFB` / `#FFFFFF`는 합치지 않음.
- `tokens.source: prose-derived`와 Google favicon URL은 provenance만. Proof 사이드카 샘플도 provenance만.
- §13 가상 persona는 삭제하고 sidecar에도 다시 싣지 않음. Primary tasks는 YAML use 문자열 3개.
- §14 8행 본문 유지. 네 CTA의 loading/error/success는 역할 매핑이 안 닫혀 생략(C2). Card/badge는 kind/map 생략(C4).
- 무출처 cubic-bezier 없음. B3 다섯 증거 종류 게이트는 Foundations Motion에 전문 유지.

검사: `--gate-only` PASS / problems `[]`. Core `portable_core: true`. SHA-256 `caf36542429f89f6de162b7dd90c3223d1e6e2e393bbb877d359e5a76aa06d4d`. 카탈로그 채택은 아님. F3 감사와 sol 의미 레인은 다음 단계입니다.

DONE migrated=1
