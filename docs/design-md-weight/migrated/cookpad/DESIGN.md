# Cookpad Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Cookpad (クックパッド) is Japan’s recipe-sharing platform. Catalog homepage identity is `https://cookpad.com`. Treating that catalog homepage as identity rather than a second captured host is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Treating the following host as the named evidence domain of this reconstruction, including this-contract-covers-the-cookpad-com-jp-live-inspect-from-the-2026-05-19-packet, values-stay-attached-to-the-surface-or-note-that-established-them, and live-computed-notes-not-a-stand-in-for-inferred-§4-controls, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. This contract covers the `cookpad.com/jp` live inspect in the 2026-05-19 packet (Playwright computed styles). Color, type, and component values below stay attached to the surface or source note that established them. A live-computed note is not a stand-in for an inferred §4 control.

The following atmosphere readings — warm-off-white-`#F8F6F2`-a-soft-cream-never-pure-white, recipe-cards-as-white-tiles-on-that-cream, Cookpad-Orange-`#FF9933`-on-the-primary-action-and-brand-mark, near-black-`#0F0F0F`-text, charcoal-`#4A4A4A`-chrome, rounded-`8px`-corners, photo-forward-recipe-images, Noto-Sans-backed-by-system-ui-apple-system-Segoe-UI-Roboto-Helvetica-Arial, and a-16px-body — are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification. Source §1 describes the working surface as warm off-white `#F8F6F2` (a soft cream), never pure white, with recipe cards as white tiles on that cream and Cookpad Orange `#FF9933` on the primary action and brand mark. Source §1 also records near-black `#0F0F0F` text, charcoal `#4A4A4A` chrome, rounded `8px` corners, photo-forward recipe images, Noto Sans backed by `system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial`, and a 16px body.

Treating the following public-history facts as narrative rather than interface tokens — founded-in-Japan-in-1997-by-Akimitsu-Sano-佐野陽光, Sano-an-engineer-trained-in-neural-computing-at-Keio-University, selling-produce-for-local-farmers-on-the-side, decline-in-home-cooking-eroding-people’s-connection-to-food, believed-technology-could-make-everyday-cooking-fun-again, launched-1998-as-Kitchen@coin, renamed-Cookpad-in-1999, dropped-a-paid-subscription-model-and-made-it-free, grew-into-a-vast-community-of-user-submitted-recipes-long-before-social-networks-existed, a-million-users-in-Japan-by-2003, more-than-half-the-Japanese-population-uses-it-monthly-`>50%`, and mission-string-make-everyday-cooking-fun — is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Source §11 records those facts from `medium.com/cookpadteam` founding story plus Crunchbase (Akimitsu Sano), WebSearch 2026-05-19, including the source HTML-comment form `>50%` of Japan uses it monthly. Akimitsu Sano (佐野陽光) is an engineer trained in neural computing at Keio University who had been selling produce for local farmers on the side. Sano saw a decline in home cooking eroding people’s connection to food, and believed technology could make everyday cooking *fun* again. After Sano dropped a paid-subscription model and made it free, the service grew into a vast community of user-submitted recipes long before social networks existed. They do not by themselves supply interface tokens.

Treating the following source-stated §11 refusal list as reconstruction-scope refusals rather than live-captured chrome — moody-dark-aesthetics-of-premium-food-blogs / celebrity-chef-gloss-of-TV-cooking-brands / cold-utility-of-a-pure-white-app — is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. What the source treats Cookpad as refusing: the moody dark aesthetics of premium food blogs, the celebrity-chef gloss of TV cooking brands, and the cold utility of a pure-white app.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Treating the following three YAML `use` strings as Primary tasks — Central-recipe-search, Ingredient-category-navigation, Main-action-search-post-recipe-sign-up — not lifting tasks from source §13 fictional archetypes, and not binding those tasks to exact live component ids, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

- Search for a recipe from the central search field (YAML `search-input` use: `Central recipe search; focus ring in #FF9933`).
- Browse ingredient/category navigation (YAML `category-tile` use: `Ingredient/category navigation — large tappable icon + label`).
- Post a recipe or sign up from the main action (YAML `button-primary` use: `Main action — search, post recipe, sign up`).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes, not individual people. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, tying observable work only to the three primary tasks, and treating group-level-Japanese-home-cooks-recipe-contributors-つくれぽ-community as official-material groups rather than researched persona segments, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Official material discusses Japanese home cooks, recipe contributors, and つくれぽ (made-it report) community at a group level.

### Distinctive traits

The following unmerged-role extras — treating catalog `primary_color` `#FF9933` as YAML primary/brand and live `rgb(255, 153, 51)`, treating canvas `#F8F6F2` as never-pure-white and unmerged from card/surface `#FFFFFF` and from on-primary `#FFFFFF`, treating foreground `#0F0F0F` as unmerged from charcoal `#4A4A4A`, treating YAML radius 8 / body `8px` as unmerged from YAML `full` 9999, treating YAML body 16 / 400 / 1.5 as unmerged from primary button 16 / 600, treating Noto Sans as unmerged from the system fallback chain, and treating photo-forward-recipe-cards / category-tiles-~64px-with-16px-padding as distinctive surface facts rather than a spacing token — are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

- Cookpad Orange `#FF9933` as catalog `primary_color`, YAML `primary`/`brand`, live `rgb(255, 153, 51)`
- Warm cream canvas `#F8F6F2` (`rgb(248, 246, 242)`), never pure white as the page background
- White recipe tiles `#FFFFFF` floating on that cream; on-primary `#FFFFFF` is a named job, not a second canvas
- Near-black `#0F0F0F`; charcoal `#4A4A4A` for chrome/secondary
- YAML radius 8 / body `8px` on buttons, cards, and category tiles; YAML `full` 9999 is a separate step
- Noto Sans–led stack; YAML body 16 / 400 / 1.5; primary button 16 / 600
- Photo-forward recipe cards; category tiles ~`64px` with `16px` padding

### Principles

These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

1. **Make everyday cooking fun.** The founding mission and the lens for every decision. *UI implication:* Warmth over polish, encouragement over instruction, approachable over elite. The cream background, friendly orange, and soft radius all serve this.
2. **Food is the hero.** Recipes exist to be cooked, and appetizing photos drive that. *UI implication:* Recipe cards are photo-forward; chrome stays out of the way; the warm canvas makes images pop. Don't clutter a card.
3. **Never make the cook feel inadequate.** Cookpad's user is a home cook, not a chef. *UI implication:* Plain helpful copy, no jargon, blameless errors, low-pressure CTAs. The voice roots for the user.
4. **Community first.** The platform is built on user recipes and made-it reports (つくれぽ). *UI implication:* Surface and celebrate contributors; make posting and reporting easy and warm; treat the community as the product, not an add-on.
5. **Warm, not clinical.** The whole interface should feel like a home kitchen. *UI implication:* Warm off-white `#F8F6F2` instead of pure white; warm-neutral accents; soft corners; gentle (never dramatic) depth.

Treating those UI-implication tails as a derived editorial implementation inference rather than a separately published Cookpad UI specification, including warmth-over-polish-encouragement-over-instruction-approachable-over-elite, the-cream-background-friendly-orange-and-soft-radius-all-serve-this, recipe-cards-photo-forward-chrome-out-of-the-way, the-warm-canvas-makes-images-pop, Don’t-clutter-a-card, plain-helpful-copy-no-jargon-blameless-errors, the-voice-roots-for-the-user, surface-and-celebrate-contributors, make-posting-and-reporting-easy-and-warm, treat-the-community-as-the-product-not-an-add-on, cream-`#F8F6F2`-instead-of-pure-white, and warm-neutral-accents-soft-corners-gentle-never-dramatic-depth, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Treating the following as a capture-bound application of source §7 Do’s, including cream-`#F8F6F2`-not-pure-white, orange-`#FF9933`-for-primary-action-and-brand-mark-only, food-photography-dominating-recipe-cards, soft-`8px`-radius, near-black-`#0F0F0F`-text, warm-plain-encouraging-copy, large-tappable-category-tiles-64px, and Noto-Sans-plus-system-fallback, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

- Put the interface on the warm cream `#F8F6F2`, not pure white.
- Reserve Cookpad Orange (`#FF9933`) for the primary action and brand mark.
- Let food photography dominate the recipe cards.
- Use the soft `8px` radius on the recorded controls.
- Use near-black `#0F0F0F` text for readability on cream.
- Keep copy warm, plain, and encouraging.
- Make category tiles large and tappable (64px).
- Lead with Noto Sans + system fallback.

### Avoid

The following items copy source §7 Don’ts and unique source §9 constraints, including no-clinical-pure-white-canvas, the-warmth-is-the-brand, orange-is-a-pop-not-a-wash, no-chrome-that-competes-with-the-photo, no-sharp-corners-or-huge-pill-radii, 8px-is-the-approachable-signature, no-low-contrast-gray-for-primary-recipe-text, no-chef-elite-or-fussy-tone, everyday-cooking-is-the-point, no-tiny-dense-navigation-rows, cooks-browse-with-thumbs, no-heavy-brand-webfont, and recipes-must-load-fast-on-every-phone. They are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

- Do not use a clinical pure-white canvas — the warmth is the brand.
- Do not flood the UI with orange — it's a pop, not a wash.
- Do not clutter cards with chrome that competes with the photo.
- Do not use sharp corners or huge pill radii — `8px` is the approachable signature.
- Do not use low-contrast gray for primary recipe text.
- Do not adopt a chef-elite or fussy tone — everyday cooking is the point.
- Do not shrink navigation into tiny dense rows — cooks browse with thumbs.
- Do not load a heavy brand webfont — recipes must load fast on every phone.

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including catalog-`#FF9933`-as-primary-brand-not-brief-`#FF7F33`, canvas-`#F8F6F2`-not-card-`#FFFFFF`-not-on-primary-`#FFFFFF`, cream-tint-`#FEF9EE`-not-pale-yellow-`#FAF5D7`-not-warm-yellow-`#E9B83F`, warm-gray-`#ECEBE9`-not-charcoal-`#4A4A4A`, foreground-`#0F0F0F`-not-charcoal, YAML-body-and-muted-as-named-jobs-on-the-same-hex, scrim-`rgba(0,0,0,0.2)`-as-photo-overlay-not-a-canvas, and YAML-component-fields-staying-on-those-controls — are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification. Catalog `primary_color` `#FF9933` is YAML `primary` / `brand` and the live inspect `rgb(255, 153, 51)`. Brief-supplied `#FF7F33` is a conflict note, not a token. YAML `canvas` `#F8F6F2` is the page background. YAML `surface` `#FFFFFF` is card white. YAML `on-primary` `#FFFFFF` is text on the orange primary button.

- **Cookpad Orange / catalog primary** (`#FF9933`): YAML `primary` / `brand`. Live `rgb(255, 153, 51)`. Primary buttons, brand mark, key accents.
- **On-primary** (`#FFFFFF`): YAML `on-primary`. White label on `#FF9933` fill. Same hex as card white; named jobs stay unmerged.
- **Warm Off-White / Cream canvas** (`#F8F6F2`): YAML `canvas`. Live `rgb(248, 246, 242)`. Signature page background — soft cream, never pure white.
- **Card White / surface** (`#FFFFFF`): YAML `surface`. Recipe cards and content tiles float on the cream.
- **Cream Tint** (`#FEF9EE`): YAML `cream-tint`. Live `rgb(254, 249, 238)`. Subtle warm fill for highlighted/promoted regions; YAML `promoted-card` fill.
- **Warm Gray Surface** (`#ECEBE9`): YAML `warm-gray`. Live `rgb(236, 235, 233)`. Dividers, subtle fills; YAML `button-disabled` fill.
- **Near-Black / foreground** (`#0F0F0F`): YAML `foreground`. Live `rgb(15, 15, 15)`. Primary text — recipe titles, body, on the cream and on white cards.
- **Charcoal** (`#4A4A4A`): YAML `body` / `muted`. Live `rgb(74, 74, 74)`. Chrome, secondary text, icon default, input text. YAML `body` and YAML `muted` are named jobs on this hex, not a second paint.
- **Warm Yellow** (`#E9B83F`): YAML `accent-yellow`. Live `rgb(233, 184, 63)`. Secondary warm accent (ratings/highlights), observed in the render.
- **Pale Yellow Tint** (`#FAF5D7`): YAML `pale-yellow`. Live `rgb(250, 245, 215)`. Light highlight fill; YAML `rating-badge` fill.
- **Scrim** (`rgba(0,0,0,0.2)`): Image/overlay darkening on photo tiles. Not a canvas token.

### Spacing

YAML `spacing`: xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 48, section 64. Those YAML numbers are recorded without a px suffix; none is added here. Treating those YAML numbers as recorded without a required px suffix, and adding no px suffix here, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Source §4 / §5 recorded px: `8px 24px` primary padding, `16px` category padding, `~12px 16px` search padding, YAML search `12px 16px`. Treating YAML numbers as recorded without a required px suffix, treating those px writings as body-recorded observations rather than a converted YAML scale, and treating no-site-wide-spacing-token-as-merged-from-those-two-lists, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

### Shape

YAML `rounded`: sm 8, md 8, lg 8, full 9999.

The following local-geometry reading, including YAML-8-unmerged-from-YAML-full-9999, body-`8px`-on-buttons-cards-category-tiles, badge-`8px`-or-`full`, 8px-is-the-approachable-signature, and not-a-universal-pill-chosen-from-the-Don’t, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. YAML 8 and YAML 9999 stay unmerged from each other and from body `8px` / `full`. Source §7 Don’ts say not to use sharp corners or huge pill radii — `8px` is the approachable signature. YAML `rating-badge` still records radius 8; body §4 also allows `full` on that badge.

### Elevation

YAML `shadow.soft`: `none — depth via warm cream background separating white cards`. Source §6: Cookpad reads soft and mostly flat. Depth comes from the warm cream background separating white cards, plus the food photography itself. Shadows, where present, are gentle. Recipe cards: minimal shadow; cream-vs-white contrast + `8px` radius separates them. Photo tiles: a subtle `rgba(0,0,0,0.2)` scrim over images supports white overlay text. Dropdowns / modals: light shadow + scrim. Treating no-reusable-numeric-shadow-token-is-promoted, treating depth-from-warm-cream-separating-white-cards and food-photography-itself-as-depth as source-stated register rather than a numeric ladder, and treating shadows-where-present-are-gentle / recipe-cards-minimal-shadow-cream-vs-white-contrast / photo-tiles-rgba-scrim / dropdowns-modals-light-shadow-plus-scrim as source-stated register rather than a token, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. No numeric shadow token is promoted.

### Motion

Source §15 durations and signature motion, preserved. Treating the duration table and easing names as source-stated rather than computed CSS, and treating duration-values-illustrative as the source HTML-comment class, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | Toggle commits, selection |
| `motion-fast` | 150ms | Button hover/press, save-heart tap |
| `motion-standard` | 250ms | Card reveal, dropdown, image fade-in |
| `motion-modal` | 300ms | Modal/dialog enter-exit |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-standard` example) | The default |
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-enter` example) | Things arriving (cards, modals) |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-warm` | omitted (unattributed cubic-bezier; source-stated name only; reserved) | A soft overshoot for the save/つくれぽ "heart" tap only |

**Spring stance.** A single soft overshoot is permitted on the save / made-it-report interaction (the emotional, community moment), matching the warm brand register. Everywhere else, motion stays calm and standard — no bouncy chrome, no kinetic flourish on navigation. Treating that spring-stance paragraph as a derived editorial implementation inference from the verified surfaces, not a separately published Cookpad motion specification, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Treating the following signature-motion characterizations — appetizing-reveal-no-slide, the-one-warm-overshoot-fitting-an-emotional-community-moment, tappable-and-immediate, and Calm — as a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

**Signature motions** (source-stated):

1. **Recipe card image fade-in.** Food photos fade in `opacity 0→1` over `motion-standard / ease-standard` as they load — appetizing reveal, no slide.
2. **Save-recipe heart.** On tap, the heart icon scales `1.0 → 1.15 → 1.0` over `motion-standard / ease-warm` and fills orange — the one warm overshoot, fitting an emotional community moment.
3. **Category tile press.** Subtle background tint + `motion-fast / ease-standard` on press; tappable and immediate.
4. **Modal enter.** Scrim fades in; dialog appears with opacity + slight translate over `motion-modal / ease-enter`. Calm.

**Reduce motion.** Under `prefers-reduced-motion: reduce`, all `motion-*` collapse to `motion-instant`; the heart overshoot flattens to a simple fill; image fade-ins become immediate. Treating warmth-never-costs-accessibility as source-stated register rather than a computed reduced-motion implementation, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Treating exact cubic-bezier curves as unattributed and omitted rather than promoted, and treating duration-values-illustrative as the source HTML-comment class rather than promoted motion tokens, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Exact cubic-bezier curves are unattributed and remain omitted rather than promoted. The source HTML comment records motion tokens in §15 as inferred, with duration values illustrative. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

Treating the following evidence-class application, including Noto-Sans-as-YAML-family-sans, live-inspect-stack-noto-sans-system-ui-apple-system-Segoe-UI-Roboto-Helvetica-arial-sans-serif, no-bespoke-brand-webfont-as-source-stated, and system-fallback-not-the-brand-face, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live inspect stack | `cookpad.com/jp` notes record `noto-sans, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, arial, sans-serif`. YAML `family.sans` `Noto Sans`. |
| Source-stated metrics | YAML `tokens.typography` section/title/button/body (sizes, weights, unitless line-heights). Body §3 size table is a size-local observation, not a replacement for those YAML ratios. |
| Source-stated webfont boundary | Source §3: Cookpad leads with Noto Sans, then falls back to each platform's system font. No bespoke brand webfont. |

### Family

- **Current named UI family:** `Noto Sans` — YAML `family.sans`
- **Recorded stack:** `noto-sans, system-ui, -apple-system, "Segoe UI", Roboto, Helvetica, arial, sans-serif`

The following font-use boundary, including do-not-present-system-ui-or-Arial-as-Noto-Sans, always-carry-the-Noto-Sans-plus-system-fallback-chain, and never-hardcode-a-Latin-only-font, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Do not present `system-ui`, `-apple-system`, `Segoe UI`, `Roboto`, `Helvetica`, or `arial` as Noto Sans. Always carry the Noto Sans + system fallback chain; never hardcode a Latin-only font.

### Type roles

Verified line-height values are the unitless YAML ratios `1.3`, `1.4`, and `1.5`. They scale with font size and are not fixed px. Treating those unitless-YAML-ratios-as-scaling-with-font-size-and-not-fixed-px, treating YAML sizes as recorded without a required px suffix, treating body §3 px writings as size-local observations rather than converted YAML numbers, treating YAML `section` 22 / 700 / 1.3 as unmerged from body table `20–24px` / 600–700, treating YAML `title` weight 600 as unmerged from body table 600–700, and treating YAML `button` 16 / 600 / 1.4 as unmerged from YAML `body` 16 / 400 / 1.5, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Tracking | Provenance |
|---|---|---:|---:|---:|---|---|
| Section headings | Noto Sans | YAML 22 / body `20–24px` | YAML 700 / body 600–700 | 1.3 | | YAML `section` use: `Section headings` |
| Recipe card title | Noto Sans | 16 | YAML 600 / body 600–700 | 1.4 | | YAML `title` use: `Recipe card title` |
| Primary button label | Noto Sans | 16 | 600 | 1.4 | | YAML `button` use: `Primary button label` |
| Body / recipe meta / category label | Noto Sans | 16 | 400 | 1.5 | | YAML `body` use: `Body / recipe meta / category label` |

Treating YAML `section` 22 / 700 / 1.3 as unmerged from body table `20–24px` / 600–700, YAML `title` weight 600 as unmerged from body table 600–700, and YAML `button` 16 / 600 / 1.4 as unmerged from YAML `body` 16 / 400 / 1.5, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. YAML `section` 22 / 700 / 1.3 is not merged with body table `20–24px` / 600–700. YAML `title` weight 600 is not merged with body table 600–700. YAML `button` 16 / 600 / 1.4 is not merged with YAML `body` 16 / 400 / 1.5.

**Conventions** (source §3): Treating 16px-is-the-comfortable-body-default, readable-for-long-recipe-steps-and-ingredient-lists, weight-600-carries-buttons-and-titles, 700-for-stronger-headings, and body-weight-400-for-ingredient-lists-and-step-text as a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification. 16px is the comfortable body default — readable for long recipe steps and ingredient lists. Weight 600 (semibold) carries buttons and titles; 700 for stronger headings. Body weight 400 for ingredient lists and step text.

### Assets

Catalog logo: type `favicon`. Treating that Google favicon lookup as a catalog identity-boundary record rather than a captured first-party Cookpad mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Treating recipe-card food photography as the source-stated primary content of those cards rather than as a complete image specification, including photo-forward-recipe-images-are-the-primary-content, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Source §1 and §9 record photo-forward recipe images as the primary content of recipe cards.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

Preserving the source state contract here while the catalog graph is not adopted is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Source §14, preserved:

| State | Treatment |
|---|---|
| **Empty (no search results)** | Warm cream canvas, one encouraging line (`#0F0F0F`) suggesting a broader search or a popular category, plus the search field. Never "no results, sorry." |
| **Empty (no saved recipes)** | Friendly one-liner inviting the user to save a recipe; an orange primary to browse. Warm, not blaming. |
| **Loading (recipe grid)** | Skeleton cards in warm-gray `#ECEBE9` at final card dimensions; gentle shimmer; no layout shift when photos load. |
| **Loading (inline)** | In-button spinner; button keeps `8px` radius and orange fill; label swaps to loading. |
| **Error (field)** | Gentle border swap + one calm helper line; cause + fix, friendly tone. |
| **Error (page/network)** | Soft notice on cream; one sentence + retry. No alarm, no apology-flood. |
| **Success (recipe saved/posted)** | Warm confirmation (`保存しました` / つくれぽ thanks); a touch of celebration for community actions. |
| **Disabled** | Warm-gray `#ECEBE9` fill, muted text. Palette swap is the signal. |
| **Skeleton** | Warm-gray blocks at exact final size, especially for the photo region; respects reduced-motion. |
| **Empty (new contributor)** | Encouraging invitation to post a first recipe; celebrate the act of sharing, never gate it behind intimidation. |

Characterizations such as Never-"no-results,-sorry", Warm-not-blaming, no-layout-shift, No-alarm-no-apology-flood, a-touch-of-celebration, and Palette-swap-is-the-signal are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

The source HTML comment records: VERIFIED live computed styles for `#FF9933` primary, `#F8F6F2` cream bg, `#0F0F0F` text, `#4A4A4A` chrome, `8px` radius, primary button spec, category tile spec, font stack. INFERRED: secondary/outline button, search input, badge variants in §4; warm-tint usage in notices/highlights; motion tokens in §15 (duration values illustrative). Treating that verified-versus-inferred split as the source’s own evidence class rather than a second token sheet is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

Treating named Focus ring `#FF9933` as not `focus-visible` evidence, treating unresolved request/outcome mapping as omitted-L-E-S-fields-rather-than-closed-from-§14-rows, and treating this as not a complete state-coverage claim, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. YAML `search-input` records `focus ring in #FF9933`; that named-source-state Focus (source HTML comment: INFERRED search input) is not `focus-visible` treatment evidence and is not copied onto a `focus-visible` row as a colour. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

Treating Recipe card and Promoted card YAML `type: card` with no interactive-kind confirmation so kind and a state-applicability map are omitted, and treating Rating badge YAML `type: badge` as Kind non-interactive so a state-applicability map is omitted, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Recipe card and promoted card have YAML `type: card` without an interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Rating badge has YAML `type: badge`; Kind is non-interactive (ratings / つくれぽ counts / promoted labels), so a state-applicability map is omitted.

### Primary Button

- Role: The main action — search, post a recipe, sign up
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#FF9933`
- Text: `#FFFFFF`
- Radius: YAML 8 / body `8px`
- Padding: `8px 24px`
- Height: ~`48px`
- Font: YAML `16/600` / body `16px` / `600`
- Use: YAML `Main action — search, post recipe, sign up`
- YAML `tokens.components.button-primary`
- Field note: The following unmerged-field reading, including this-fill-as-catalog-`primary_color`-not-brief-`#FF7F33`, on-fill-white-not-canvas-`#F8F6F2`, and radius-8-not-YAML-full-9999, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. `#FF9933` is this control’s fill and catalog `primary_color`. `#FFFFFF` is this control’s on-fill label. Radius 8 / `8px` is this YAML/body field; it is not YAML `full` 9999.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/live primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | YAML `button-disabled` records unavailable action; §14 Disabled `#ECEBE9` fill, muted text |
| loading | applicable | Source §14 Loading (inline): in-button spinner; button keeps `8px` radius and orange fill; label swaps to loading |

Treating error and success applicability as omitted on this main-action button because field/page errors and `保存しました` / つくれぽ thanks in §14 are product outcomes, not paints on this button, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Error and success applicability are omitted. Source names this control as the main action (search, post a recipe, sign up); field/page errors and `保存しました` / つくれぽ thanks in §14 are product outcomes, not paints on this button.

Treating YAML `button-disabled` as the disabled treatment recipe for this control rather than a fifth interactive component with its own map, including Type-button-preserved-on-that-recipe, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. YAML `button-disabled` (Type: button; bg `#ECEBE9`; fg `#4A4A4A`; use `Unavailable action`) is the disabled treatment recipe for this control, not a fifth interactive component with its own map. The source HTML comment records that disabled recipe as inferred.

### Secondary / Outline Button

- Role: Lower-emphasis action beside a primary
- Kind: interactive
- Type: button
- Anatomy: label
- Background: `#FFFFFF`
- Text: `#FF9933`
- Border: `1px solid #FF9933`
- Radius: YAML 8 / body `8px`
- Use: YAML `Outline, 1px #FF9933 border — lower-emphasis beside primary`
- YAML `tokens.components.button-secondary`
- Field note: The following inferred-control reading, including source-HTML-comment-INFERRED-secondary-outline-button, this-fill-`#FFFFFF`-not-canvas-`#F8F6F2`, and this-fg-`#FF9933`-as-this-control-not-a-second-primary-fill, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. The source HTML comment records this control as inferred from palette. `#FFFFFF` is this control’s fill. `#FF9933` is this control’s label and border.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML outline beside primary |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | An outline action can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this lower-emphasis outline action because exact request/outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a lower-emphasis action beside a primary; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Text / Quiet Button

- Role: Tertiary/navigation action
- Kind: interactive
- Type: button
- Anatomy: label
- Background: transparent
- Text: `#4A4A4A`
- Use: YAML `Transparent tertiary / navigation action`
- YAML `tokens.components.button-text`
- Field note: The following unmerged-field reading, including this-fg-`#4A4A4A`-as-this-control-not-a-second-foreground-`#0F0F0F`, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. `#4A4A4A` is this control’s label.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML tertiary / navigation action |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A tertiary action can be unavailable; visual treatment omitted |

Treating loading, error, and success applicability as omitted on this tertiary/navigation action because exact request/outcome mapping is unresolved, rather than closing those fields from the §14 rows, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Loading, error, and success applicability are omitted. Source names this control as a tertiary/navigation action; exact request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Category Tile

- Role: Ingredient/category navigation (野菜 / お肉 / 魚介 / たまご …)
- Kind: interactive
- Type: button
- Anatomy: large tappable icon + label
- Background: transparent
- Text: `#0F0F0F`
- Radius: YAML 8 / body `8px`
- Padding: `16px`
- Height: ~`64px`
- Font: YAML `16/400` / body `16px` / `400`
- Use: YAML `Ingredient/category navigation — large tappable icon + label`
- YAML `tokens.components.category-tile`
- Field note: The following unmerged-field reading, including this-height-~64px-not-primary-~48px, this-padding-16px-not-primary-8px-24px, and this-weight-400-not-primary-600, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Live inspect notes record category tiles `8px` radius, `16px` padding, ~`64px`.

Treating loading, error, and success as not-applicable on this ingredient/category-navigation tile because the tile does not submit a recipe or run an inline save, is not a form field, and つくれぽ / save confirmation is not a paint on the tile, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML/live category navigation |
| hover | applicable | Pointer-web tile; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A category tile can be unavailable; visual treatment omitted |
| loading | not-applicable | Ingredient/category navigation does not submit a recipe or run an inline save on the tile |
| error | not-applicable | The tile is not a form field; field/page errors sit on search and notices |
| success | not-applicable | つくれぽ / save confirmation is not a paint on a category tile |

Treating Category tile press in §15 as an additional named-source-state (source HTML comment: motion duration values illustrative) rather than a Core applicability row, an observed interaction capture, or `focus-visible` evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Additional named-source-state: Category tile press in §15 (subtle background tint + `motion-fast / ease-standard`; duration values illustrative). That press is not a Core applicability row, is not a live-observed interaction capture, and is not `focus-visible` evidence.

### Recipe Card

- Role: Recipe tile — large food photo on top, title + meta below, floating on the cream page
- Type: card
- Anatomy: food photo top; title strip below (16px 600 `#0F0F0F`) + meta row in `#4A4A4A`
- Background: `#FFFFFF`
- Text: `#0F0F0F`
- Meta: `#4A4A4A`
- Radius: YAML 8 / body `8px`
- Padding: `0` (image-led; title strip below)
- Use: YAML `Food photo top, title + meta below, floating on cream page`
- YAML `tokens.components.recipe-card`
- Field note: The following unmerged-field reading, including this-fill-`#FFFFFF`-not-canvas-`#F8F6F2`-not-promoted-`#FEF9EE`, meta-row-`#4A4A4A`-not-title-`#0F0F0F`, and C4-omit-kind-and-map, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. `#FFFFFF` is this card’s fill. Meta `#4A4A4A` is this card’s meta row, not the title `#0F0F0F`. Kind and a state-applicability map are omitted (YAML `type: card`; no interactive-kind confirmation).

### Promoted / Highlight Card

- Role: Featured/promoted recipe region
- Type: card
- Background: `#FEF9EE`
- Radius: YAML 8 / body `8px`
- Use: YAML `Featured / promoted recipe region`
- YAML `tokens.components.promoted-card`
- Field note: The following inferred-control reading, including source-HTML-comment-INFERRED-from-cream-tints, this-fill-`#FEF9EE`-not-card-white-`#FFFFFF`-not-pale-yellow-`#FAF5D7`, and C4-omit-kind-and-map, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. The source records this control as inferred from cream tints. Kind and a state-applicability map are omitted (YAML `type: card`; no interactive-kind confirmation).

### Search Field

- Role: The central recipe search — the front door of the product
- Kind: interactive
- Type: input
- Anatomy: value field
- Background: `#FFFFFF`
- Text: `#4A4A4A` (placeholder), `#0F0F0F` (typed)
- Radius: YAML 8 / body `8px`
- Padding: YAML `12px 16px` / body `~12px 16px`
- Use: YAML `Central recipe search; focus ring in #FF9933`
- YAML `tokens.components.search-input`
- Field note: The following inferred-control reading, including source-HTML-comment-INFERRED-search-input, YAML-`12px 16px`-unmerged-from-body-`~12px 16px`, this-fill-`#FFFFFF`-not-canvas-`#F8F6F2`, and named-Focus-ring-`#FF9933`-not-focus-visible-evidence, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. The source HTML comment records this control as inferred from chrome. Named Focus `border/ring in #FF9933` is an additional INFERRED named-source-state (source HTML comment: INFERRED search input), not `focus-visible` evidence.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | YAML central recipe search |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A search field can be unavailable; visual treatment omitted |
| error | applicable | Form field; §14 Error (field) is a field treatment; visual treatment omitted beyond that row |

Treating loading and success applicability as omitted on this central recipe search because grid loading and save confirmation in §14 are not paints on this field, and exact request/outcome mapping to the input is unresolved, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Loading and success applicability are omitted. Source names this control as the central recipe search; grid loading and save confirmation in §14 are not paints on this field.

Treating Focus border/ring in `#FF9933` as an additional INFERRED named-source-state rather than a Core `focus-visible` row or a live-observed interaction capture, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Additional INFERRED named-source-state: Focus border/ring in `#FF9933` (source HTML comment: INFERRED search input). That Focus is not a Core `focus-visible` row and is not copied onto that row as a colour.

### Rating / Highlight Badge

- Role: Ratings, つくれぽ counts, promoted labels
- Kind: non-interactive
- Type: badge
- Background: `#FAF5D7` (pale yellow) or `#E9B83F`
- Text: `#4A4A4A` / `#0F0F0F`
- Radius: YAML 8 / body `8px` or `full`
- Use: YAML `Ratings, つくれぽ counts, promoted labels`
- YAML `tokens.components.rating-badge`
- Field note: The following inferred-control reading, including source-HTML-comment-INFERRED-badge-variants, `#FAF5D7`-unmerged-from-`#E9B83F`, YAML-8-unmerged-from-body-`full`, and Kind-non-interactive-so-no-state-map, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. The source HTML comment records badge variants as inferred. Kind is non-interactive because the YAML use is ratings / counts / labels, not a control. A state-applicability map is omitted.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

The following layout readings — medium-density-photo-forward, packs-efficiently, each-card-gives-its-food-photo-room-to-look-appetizing, cream-`#F8F6F2`-separation-without-heavy-borders-or-shadows, top-search-bar-as-the-hero, recipe-discovery-is-the-core-loop, category-tiles-~64px-8px-radius-16px-padding, recipe-cards-in-a-responsive-grid-on-the-cream-base, generous-padding-inside-category-tiles-comfortable-tap-targets, and YAML-spacing-without-a-px-suffix — are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

Cookpad is medium density, photo-forward. The grid of recipe cards packs efficiently, but each card gives its food photo room to look appetizing. The warm `#F8F6F2` background provides separation between white cards without heavy borders or shadows.

- Top search bar is the hero — recipe discovery is the core loop
- Category tiles (large `64px`, `8px` radius, `16px` padding) form a tappable navigation grid
- Recipe cards in a responsive grid float on the cream base
- Generous padding inside category tiles makes them comfortable tap targets

YAML spacing is xs 4, sm 8, md 16, base 16, lg 24, xl 32, xxl 48, section 64, recorded without a px suffix.

| Width | Behavior |
|---|---|
| Desktop | Wide recipe grid (multi-column) on the cream base; top search + category grid prominent |
| Tablet | Recipe grid reflows to fewer columns; category tiles wrap |
| Mobile | Single/two-column recipe feed; search bar pinned; category tiles in a scrollable grid; mobile-first cooking context |

Treating that Desktop/Tablet/Mobile table as source-stated §8 rather than a measured breakpoint token sheet, including Category-tiles-~64px-and-primary-buttons-~48px-clear-touch-minimums, Recipe-photos-crop-responsively, titles-stay-readable-at-16px, core-usage-is-mobile-cooking-with-a-phone-propped-in-the-kitchen, and touch-targets-are-generous-by-design, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

- Category tiles (~64px) and primary buttons (~48px) clear touch minimums comfortably
- Recipe photos crop responsively; titles stay readable at 16px
- Cookpad's core usage is mobile (cooking with a phone propped in the kitchen) — touch targets are generous by design

<!-- design-md:section content-locales -->
## 6. Content & Locales

Treating Cookpad's voice as warm-encouraging-and-refreshingly-unpretentious, including mission-make-everyday-cooking-fun, copy-embodies-the-mission, tired-busy-not-a-professional-chef, never-makes-them-feel-inadequate, friendly-polite-Japanese-です・ます調, homey-peer-to-peer-warmth, closer-to-a-helpful-friend-sharing-a-recipe-than-an-authoritative-cookbook, and community-つくれぽ-as-central, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Source §10: Cookpad's voice is warm, encouraging, and refreshingly unpretentious — the brand's mission is literally to *make everyday cooking fun*, and the copy embodies that. It speaks to the home cook who is tired, busy, and not a professional chef, and it never makes them feel inadequate. The register is friendly polite Japanese (です・ます調) with a homey, peer-to-peer warmth — closer to a helpful friend sharing a recipe than an authoritative cookbook. Community is central: Cookpad is built on user-submitted recipes and "つくれぽ" (made-it reports), so the voice celebrates the contributors and the act of cooking-for-others.

The following voice-table directions — Buttons-short-friendly-JP-verb, Recipe-meta-plain-and-helpful, Empty-states-encouraging, Community-warm-and-celebratory, Errors-gentle-and-blameless, Success-friendly-confirmation, and Onboarding-welcoming-low-pressure — are a derived editorial implementation inference from the verified surfaces; they are not Cookpad-authored or a separately published UI specification.

| Context | Tone |
|---|---|
| Buttons | Short friendly JP verb — `検索`, `レシピを投稿`, `保存`. Inviting, no pressure. |
| Recipe meta | Plain and helpful — ingredients, steps, time. Practical, never fussy. |
| Empty states | Encouraging — invite the user to search or post; never imply failure. |
| Community (つくれぽ) | Warm and celebratory — thank and highlight the people who cooked and shared. |
| Errors | Gentle and blameless; one calm sentence + the fix. |
| Success | Friendly confirmation (`保存しました`) with a touch of warmth. |
| Onboarding | Welcoming, low-pressure — cooking is supposed to be fun. |

**Forbidden patterns.** Chef-elite condescension, fussy/technical jargon that intimidates home cooks, hype superlatives (`究極の`, `プロ級`), pressure/FOMO copy, exclamation-mark-button shouting, and anything that makes a tired parent feel they're cooking wrong. Cookpad roots for the cook. Treating that forbidden-pattern list as source-stated §10 rather than a separately published microcopy specification, including intimidates-home-cooks, anything-that-makes-a-tired-parent-feel-theyre-cooking-wrong, and Cookpad-roots-for-the-cook, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification.

**Voice samples.**

- `レシピを投稿` — post-a-recipe action; community-celebrating. Source marker: illustrative: standard Cookpad-register JP action label; not quoted verbatim from a specific live screen.
- `検索` — the central recipe-search action. Source marker: illustrative.
- "Make everyday cooking fun" — Cookpad's stated mission. Source marker: verified: widely-published Cookpad mission statement, WebSearch 2026-05-19 (`medium.com/cookpadteam`).

Japanese です・ます調 register and the recorded samples stay as source §10 evidence. Treating japanese-です・ます調-register-and-the-recorded-samples-as-source-§10-evidence, and treating illustrative samples as not-verbatim-live-strings-except-the-published-mission, is a derived editorial implementation inference from the verified surfaces; it is not Cookpad-authored or a separately published UI specification. Illustrative samples are not promoted as verbatim live strings except the published mission.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- brief-supplied `#FF7F33` as a current token (conflict; live-verified `#FF9933` is used)
- exact cubic-bezier values for `ease-standard` / `ease-enter` / `ease-exit` / `ease-warm` (unattributed; names kept)
- `focus-visible` visual treatments (named Focus ring `#FF9933` on search is not that evidence)
- hover visual treatments on Primary / Secondary / Text / Category / Search
- Primary error·success applicability (exact paint-on-button mapping unresolved)
- Secondary / Text loading·error·success applicability (exact request/outcome unresolved)
- Search loading·success applicability (exact request/outcome unresolved)
- interactive kind and state-applicability map for Recipe card and Promoted card
- numeric shadow token; reusable elevation ladder
- site-wide spacing scale as a converted px sheet from YAML numbers without a px suffix
- motion promotion beyond the duration table, easing names, signature motions, and reduced-motion line — promote only after per-component computed capture of all five kinds: transition properties, animation name, duration, easing, and reduced-motion behavior; official documentation of a single curve or duration is not that gate
