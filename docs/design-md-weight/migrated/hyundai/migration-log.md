# hyundai migration log

Source: `web/references/hyundai/DESIGN.md`
Sibling read (not the migration input): `web/references/hyundai/.verification.md`
Destination: `docs/design-md-weight/migrated/hyundai/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/hyundai/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `2dbbd9f31c46779a696b6f3586e20100f43264274df4846d03b848c29c9e3a11` (`web/references/hyundai/DESIGN.md`). Sibling SHA-256 `15e5d2fa1aefa49000224fa4d44636db06e0055b3b4b0674786a05177b067857` (`web/references/hyundai/.verification.md`). Worker-close portable DESIGN SHA-256 `ab5f394d6ed827bdb84a02ac7b8e9887843a01c81c1d78fec54d05c5c2a70965`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `display_name_kr`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# Hyundai Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://www.hyundai.com/kr/ko/e` is dual: `DESIGN.md` 9/21/22/23 + `provenance.md` Identity/Surfaces (E2a). `#002c5f` is dual: first portable line `DESIGN.md` 11 (Scope / Foundations / vehicle action; `grep -oF` dest **5** at 11/32/75/175) + `provenance.md` Identity (E2a). `logo.type: simpleicons` / `logo.slug: hyundai` is dual: `DESIGN.md` 155 + `provenance.md` Identity (E2a). `display_name_kr` 현대자동차 stays in the identity table only. |
| YAML `omd: "0.1"`, `verified`, `verification_v2`, `tokens.source: reconciled`, `tokens.extracted`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source footer's **Verified:** 2026-07-13 is in Freshness. These are ledger keys with no portable slot. `ds.type` is absent from the source and is not invented. |
| YAML `tokens.note` | 옮김 → Typography Family; 분리 → provenance Token note | Dual: `DESIGN.md` 131 + `provenance.md` Token note (E2a). Facts named: three-surface tokenization; FontFaceSet-backed `HyundaiSansTextKR` / `HyundaiSansHeadKR`; regional `HyundaiSansHeadKRR` / `HyundaiSansTextKRR`; Arial system chrome; `element-icons` declared-only. |
| YAML `tokens.colors` (8 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 75–82. All eight roles kept with names, values, and token-set keys: primary `#002c5f` dest **5**, accent-teal `#007fa8` dest **4** (11/36/76/237), accent-cyan `#00aad2` dest **4** (11/36/77/301), ink `#000000` dest **6**, on-primary `#ffffff` dest **5**, muted `#999999` dest **4**, utility `#444444` dest **3**, footer `#1c1b1b` dest **4**. Paths `tokens.colors.primary` dest 1 at 75 · `accent-teal` 76 · `accent-cyan` 77 · `ink` 78 · `on-primary` 79 · `muted` 80 · `utility` 81 · `footer` 82. `#676767` dest **4** (37/73/84/280) stays the Family Site border, not a color-token key. |
| YAML `tokens.typography.family` (`display: HyundaiSansHeadKR`, `body: HyundaiSansTextKR`) | 옮김 → Typography & Assets Family | `DESIGN.md` 126–127. Paths named `tokens.typography.family.display` / `family.body`. `HyundaiSansHeadKR` dest **14** · `HyundaiSansTextKR` dest **15** · `HyundaiSansHeadKRR` dest **6** · `HyundaiSansTextKRR` dest **4**. The no-substitution rule at 131, and the canonicity-because-FontFaceSet reading on the same line, carry an adjacent complete qualifier (B2/B2a). |
| YAML `tokens.typography.display-h2` / `body` / `action` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 137–149. Unitless line heights stay ratios and are never converted to a replacement px (A1a): `1.32` dest **3** (135/139/385) · `1.15` dest **5** (135/140/141/179/386). Observed `58px` dest **3** · `18.4px` dest **4** · `30px` dest **1** stay on the hierarchy table. YAML `use` strings restored verbatim: `Observed h2 on the KR product home surface` dest **1** at 139 · `Observed product-surface text` dest **1** at 140 · `Observed navy vehicle action` dest **2** at 141/179. `tokens.typography.body.size` `16` and `tokens.typography.action.size` `16` stay two type keys — dest **2** at `DESIGN.md` 135/151. Tracking `-0.4` dest **7**. Qualifier at 135. |
| YAML `tokens.spacing` (empty `{}`) | 옮김 → Foundations spacing as empty-map constraint | `DESIGN.md` 88. `tokens.spacing` dest **3** (88/151/381). No scale is invented. Component paddings stay on those records: `10px 0px` dest **2** (88/219) · `0px 4px` dest **2** (88/258) · `0px 13px` dest **2** (88/282). |
| YAML `tokens.rounded` (`none: 0`, `pager: 6`) | 옮김 → Foundations shape | `DESIGN.md` 92–98. Paths kept separate: `tokens.rounded.none` dest 1 at 94 · `tokens.rounded.pager` dest 1 at 95. `none: 0` dest **2** (92/98) · `pager: 6` dest **3** (92/98). Chatbot `100%` dest **5** stays on that control (96/98/302/322). `tokens.rounded.full` dest **0**. `full: 9999` dest **0**. Bare `9999` dest **4** is only the substring inside `#999999` (11/37/80/279). |
| YAML `tokens.shadow.chatbot` | 옮김 → Foundations elevation + Circular chatbot | `rgba(0,0,0,0.15) 0px 0px 20px 0px` dest **2** at `DESIGN.md` 102/303. Path `tokens.shadow.chatbot` dest 1 at 102. Repeated §4 components keep `box-shadow: none` at 102. |
| YAML `tokens.components.selected-carousel-indicator` | 옮김 → Components & States Selected indicator shell | `DESIGN.md` 251–272. Verified primitive type preserved on this component only, not flattened to `Kind` (A1b): `Primitive type: \`tab\`` dest **1** at 254 = source YAML `type: tab` 1. `Token-set use: \`Selected shell observed on the home surface\`` dest **1** at 260. Path `tokens.components.selected-carousel-indicator` dest 1 at 261 (`fg: #000000`, `radius: 0px`, `active: true`). The other six §4 records are `not in the token set` dest **7** (168 + six component lines 173/195/215/236/277/300). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 30–40. 1967 founding, mobility company, Ulsan-to-IONIQ shift, restrained interface, Sensuous Sportiness since 2018, structure / proportion / styling / technology, not a universal Hyundai design system, not a zero-radius system (6px pager / circular chatbot), hex list. Surface-boundary reading at 9; keep-on-surface reading at 11 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 75–84. Role-naming and teal/cyan-not-broader-accent readings carry the adjacent qualifier at 73 (B2/B2a). |
| §3 Typography Rules — evidence classes, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 112–122; family 124–131; token-set table 137–141; observed hierarchy 143–149. Official product-use / Official distributed / Declared-only readings carry the adjacent qualifier at 122. FontFaceSet counts `84` / `43` / `35` dest **1** at 117. `287` dest **2** (9 newsroom URL `0000000287` + 117 FontFaceSet count). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 170–315. Selectors, classes, occurrence counts, and “no state captured” notes kept on each record. Capture selectors are dual: body Use lines + `provenance.md` Sibling handling raw samples (E2a). Chatbot low-confidence / not-a-general-FAB sentence kept at 305. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. The three KR product URLs and the three official-doc URLs are dual: Scope 9 + provenance Surfaces (E2a). Conflicts unresolved: none. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 320–322. Three desktop routes; no breakpoint or grid; catalogue and IONIQ 6 share the navy action; no universal card / spacing / layout grid. Desktop-capture / no-universal-card reading at 322 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 102. Chatbot-only shadow reading carries the adjacent qualifier at 102 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 50–56, under the grouping qualifier at 52. Not placed in Governance controlled copy. Three items, matching the source Do list. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 58–65, under the qualifier at 60. Three source Don'ts plus the §9 card / input / error-state / motion constraint. No invented domain (no `native application`, no `mobile app`, no `back-office`, no `product application`). Exact string `measures 1440px` dest 0 / provenance 0. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 320. “No responsive viewport comparison was supplied. Breakpoints, touch-target rules, and collapse behavior remain unresolved.” Unresolved items are omitted as values, not denied as invented measurements (D1). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` §9 deletion check. Navy `#002c5f` / white / 0px / 16px/500 HyundaiSansTextKR is in Navy filled vehicle action. The card / input / error-state / motion prohibition is in Avoid 65. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 327–342. “Progress for Humanity” dest **3** (46/327/331). Sensuous Sportiness dest **7**. “no verbatim voice samples or prescriptive copy rules” kept at 327. Byte-exact / gloss-beside rule at 342 (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13 (and overlapping facts at 11): **1967** dest 4 (9 URL + 11 + 13); **1968** dest **1**; **Pony** dest **3**; **1976** dest **1**; **Ulsan** dest **4**; **mobility solution provider** dest **2**; **2024** dest **2**; **IONIQ** dest **14** (includes IONIQ 6 URLs); **2018** dest **3**; **Sensuous Sportiness** dest **7**; structure, proportion, and styling; **Seon** dest **4**; **Hyundai Sans UI** dest **7**; **ccNC** dest **4**; closing sentence “the Seon design system and Hyundai Sans UI are an in-vehicle ccNC context, not proof of the KR public-web component rules above” at 13. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` Proof notes. |
| §12 Principles — 3 numbered | 옮김 → Experience principles | `DESIGN.md` 42–48 under the B2a form at 44: "These three items are a derived editorial implementation inference from the verified surfaces; they are not Hyundai-authored or a separately published UI specification." Hyundai has no published first-party web UI specification, so the toss-form close is used (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` Portable derived-editorial scope (**21** rows). |
| §13 Personas | 삭제 | The source recorded no first-party audience segmentation and used a `[FILL IN]` placeholder. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or affiliation appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience at `DESIGN.md` 26–28 restates the source's own “no first-party audience segmentation” sentence and does not invent a shopper group. The three `primary-tasks` at 21–23 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Full source body preserved at `DESIGN.md` 162 (A2; the catalog graph is still 0/440, so nothing is delegated): `interactionCount: 0` dest **4** (38/162/378); only the carousel indicator shell carries observed `selected`; empty / loading / error / success / skeleton / disabled / hover / focus / pressed treatments unresolved. The applicability rule is at 164–166, whose qualifier at 166 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. All seven destination / trigger / pager / tab controls close loading/error/success (`loading \| not-applicable` dest **7** at 188/208/229/247/270/293/313; `loading \| applicable` dest **0**). `default` and `focus-visible` stay applicable. Generic Focus is not treated as a `focus-visible` treatment (B1). This is not a complete state-coverage claim (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 106. “No duration, easing, animation, or reduced-motion behavior was captured. Motion tokens and rules are unresolved. No motion token is promoted.” The source lists no curve values. B3 is held: the promotion condition at `DESIGN.md` 106 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). Governance 382 restates the five kinds and the gate. |
| §15 Motion & Easing — unattributed template curves | 해당 없음 (삭제할 무출처 커브 없음) | The source does not contain `cubic-bezier(0.4, 0.0, 1, 1)` or the other `spec/omd-v0.1.md` example-table curves. `grep -oF 'cubic-bezier(0.4, 0.0, 1, 1)'` dest 0 / provenance 0. |

## Sibling handling (`web/references/hyundai/.verification.md`)

The sibling exists — confirmed with `find web/references/hyundai -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: pager `width: 12px` / `height: 12px`; fallback face `"Magul Gothic"`; `aria-selected: true`; RGB spellings; collector totals 85 coverage / six component types / 54 variants; sustainability URL. Recorded at `provenance.md` sibling-only list.
- Measured `DESIGN.md` 0 for those sibling-only strings: `Magul Gothic` 0 · `width: 12px` 0 · `aria-selected` 0 · `12px` 0.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. Hyundai's published lines in the source are Latin, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published official / captured labels in the source body | 10 distinct | 0 | 0 | Progress for Humanity / Sensuous Sportiness / Family Site / Hyundai Sans / Hyundai Sans UI / Seon / IONIQ / Pony / Hyundai Motor Company / mobility solution provider. |
| YAML `use` strings | 4 distinct | 0 | 0 | Observed h2 on the KR product home surface / Observed product-surface text / Observed navy vehicle action / Selected shell observed on the home surface. |
| Sibling published strings | 0 brand-published extras | 0 | 0 | `Magul Gothic` is a fallback face in a computed stack, not brand-published copy. It survives in provenance as a sibling-only record; not promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: Progress for Humanity, Sensuous Sportiness, Family Site, Hyundai Sans UI, Seon, IONIQ, Pony, Hyundai Motor Company, mobility solution provider, 1967, 1968, 1976, Ulsan, 2024, 2018, ccNC, not proof of the KR public-web component rules.

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 10 + YAML use 4 extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand hyundai --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 92 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 92.

## Deviations recorded

- `DESIGN.md` is above the spec's 600–1,800-word SHOULD budget (`wc -w` 4842). The budget yielded to A1: seven declared component records with selectors and seven-state maps, dual token-set / observed type tables, the full §11 founding-and-design-identity narrative including its closing ccNC sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Hyundai has no published first-party web UI specification, so every derived-editorial close uses the toss-form example (rulebook v12 B2a 전제 주석). Worker-close measure `grep -o 'derived editorial implementation inference' DESIGN.md \| wc -l` = **21** = `not Hyundai-authored` 21 = `separately published UI specification` 21. Auditor re-measure after F3 expansions (same 21 lines; Family / Assets / Layout namings extended, no new occurrence): **21** = **21**. Provenance derived ledger **21** rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is a Hyundai-issued fact or a derived reading. Scope surface-boundary, keep-on-surface, and narrative-as-context; primary-task selection; trait grouping; Principles / Do / Don't headers; Semantic role slotting including `#676767`; empty-spacing and shape key-path separation; elevation and motion-gate readings; font evidence / family no-substitution and canonicity-because-FontFaceSet / type-role keep-both; simpleicons-as-pointer and photography-not-invented-decoration; applicability procedure; layout desktop-capture and no-universal-card reading; byte-exact rule; and the unresolved-list naming each carry an adjacent complete toss-form qualifier. Source facts (years, hexes, YAML keys, published strings, FontFaceSet counts, selectors) were left unqualified. Inventory: 21 lines (F3 expanded Family `:131`, Assets `:155`, Layout `:322` namings; no new occurrence). Audience restates the source's own no-segmentation sentence and does not invent a shopper group, so no extra qualifier was added there.

## F2 E2 rescan (mandatory final pass)

Each log row was written only after `grep -oF` against `DESIGN.md` and `provenance.md`. Dual destinations named above were confirmed in both files. B3 is claimed only because `DESIGN.md` 106 names all five evidence kinds and the per-component gate in full text (E2c). Governance 382 restates the five kinds and the gate. No "B3 유지" claim is made beyond that. Sibling-only strings are claimed dest 0 after measurement. Persona identifiers are claimed dest 0 / provenance 0 as use, not as a self-denominator absence sentence that lists names (D2a, E2d). F3 dest corrections: `tokens.typography.body.size` / `action.size` dest **2** at 135/151 (was 151 only); `287` dest **2** (9 URL + 117 count). B2a 21=21. Worker-close SHA `ab5f394d6ed827bdb84a02ac7b8e9887843a01c81c1d78fec54d05c5c2a70965`. Auditor SHA `f715c0a7fd15032c47d6b0707b0bd7556aa65c31cd3720257ff8e709e7a062b9`.
