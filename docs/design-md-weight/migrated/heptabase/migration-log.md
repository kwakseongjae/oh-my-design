# heptabase migration log

Source: `web/references/heptabase/DESIGN.md`
Sibling read (not the migration input): `web/references/heptabase/.verification.md`
Destination: `docs/design-md-weight/migrated/heptabase/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/heptabase/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -o … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `ff048fe0f7b3aabc004c1bee95bcd3a870701d8aba816bc6d362c10806ccb7e2` (`web/references/heptabase/DESIGN.md`). Sibling SHA-256 `51ac68ce8019189360f43c4620f25918b00114b868304e8b2fc7346dd3640a12` (`web/references/heptabase/.verification.md`).

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope; `primary_color` 옮김 → Scope + Foundations + components; `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter; H1 is `# Heptabase Design System` (`DESIGN.md` 1). Identity table `provenance.md` 7–21. `https://heptabase.com` is dual: `DESIGN.md` 9 + `provenance.md` 13/44/50/54 (E2a). Line 224 is the favicon-service URL that contains the same host as a path segment, not a homepage dest. Frontmatter `primary_color` `#2e2e2e` is dual: `DESIGN.md` 11/36/61/92 + `provenance.md` 14/25 (E2a). The favicon slug URL is dual: `DESIGN.md` 224 + `provenance.md` 15 (E2a). Line 23 discusses the slug without the URL bytes. |
| YAML `omd: "0.1"`, `verified`, `added`, `tokens.source: live-extract`, `tokens.extracted`, `tokens.note`, `components_harvested: true` | 분리 → provenance | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` 17–21, 31–36. The source footer's **Verified:** 2026-06-17 is at `provenance.md` 36. The token note is at `provenance.md` 25. No `ds.name` / `ds.url` / `ds.type` fields exist in the source YAML. |
| YAML `tokens.colors` (12 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 92–113. All twelve roles kept with names, values, and token-set keys: ink `#2e2e2e`, ink-pure `#000000`, canvas `#f7f7f7`, surface `#ffffff`, surface-tint `#fcfcfc`, surface-warm `#f0f0ea`, accent-blue `#207dff`, accent-green `#75c33a`, muted `#6a6972`, muted-warm `#777169`, hairline `#e5e7eb`, on-primary `#ffffff` (surface and on-primary share `#ffffff` at 94). Translucent fills `rgba(252,252,252,0.5)` / `rgba(0,0,0,0.08)` / `rgba(0,0,0,0.14)` stay in the same roles. |
| YAML `tokens.typography.family` (`display: Instrument Sans`, `body: Inter`, `cjk: system CJK (PingFang TC / Noto Sans TC)`) | 옮김 → Typography & Assets Family | `DESIGN.md` 190–192. All three paths named. Load names `__instrumentSans` / `__Inter` at 182/190–191. Heading at 188. |
| YAML `tokens.typography.display-hero` / `section` / `card-title` / `nav` / `body` / `button` / `pill` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 200–207. Unitless line heights stay ratios and are never converted to a single px form (A1a): `1.30` at 200/201/209; `1.50` at 202/204/205/209; `1.00` at 206/207/209. The form-keeping decision carries an adjacent complete qualifier at 209 (B2/B2a). Source-spelled `62.4px` / `46.8px` stay beside `1.30`. Tracking `-1.584px` / `-0.54px` / `-0.36px` at 200/201/203. All seven YAML `use` strings restored verbatim in the table and restated at 211 (A1, A3). The Logotype row (18px / 600 / `-0.36px`) is from the source hierarchy table and has no token-set key — `DESIGN.md` 203/210. |
| YAML `tokens.spacing` (8 steps) / `tokens.rounded` (4 steps) / `tokens.shadow.none` | 옮김 → Foundations spacing + shape + elevation; also Layout | Unitless steps kept: `DESIGN.md` 117 (`xs 4 · sm 8 · md 12 · base 16 · lg 24 · xl 32 · xxl 48 · section 64`), with key-path forms `tokens.spacing.xs: 4` / `sm: 8` / `md: 12` / `base: 16` / `lg: 24` / `xl: 32` / `xxl: 48` / `section: 64` on the same line. Shape at 121–128 (`sm 6 · md 8 · lg 12 · pill 9999`). The same two scales are restated at Layout `DESIGN.md` 441–442 (`pill 9999` without the colon). `tokens.rounded.pill: 9999` (colon form) stays a step (`DESIGN.md` 128). `tokens.spacing.sm: 8` is disambiguated from `tokens.rounded.md: 8` at 117/128. `tokens.spacing.md: 12` is disambiguated from `tokens.rounded.lg: 12` at 117/128. `tokens.spacing.base: 16` is disambiguated from the 16px type size at 117. `tokens.spacing.lg: 24` is disambiguated from the 24px card-title at 117. `tokens.shadow.none: none` at `DESIGN.md` 140. The 10px radius on Hero / Ghost CTAs is named as a component measurement, not a rounded step — `DESIGN.md` 128/267/292. That classing is named in the Shape qualifier at 130. |
| YAML `tokens.components` (9 records) | 옮김 → Components & States | `DESIGN.md` 236–408. Verified primitive types preserved per component, not flattened to `Kind` (A1b): `Primitive type: \`button\`` ×3 (239/263/287), `tab` ×2 (312/334), `toggle` ×1 (357), `card` ×3 (380/391/402). All nine YAML `use` strings restored verbatim as `Token-set use:` lines (248/272/297/318/341/365/385/396/407). YAML `font` shorthands kept as `Token-set font record:` lines (247/271/296). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–15; Distinctive traits 35–42. The atmosphere readings (paper-like, printed-on-paper, tidy desk, quiet board) carry an adjacent complete qualifier at 11 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 92–113. Characterizing phrases covered by the adjacent qualifier at 88 (B2/B2a). |
| §3 Typography Rules — family, hierarchy, principles | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 180–186; family 190–194; hierarchy table 200–207; type rules 215–220. The License-row reading that treats Instrument Sans / Inter as upstream faces carries an adjacent complete qualifier at 185 (B2/B2a). The unitless-ratio form-keeping at Type roles carries an adjacent complete qualifier at 209. |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 236–414. Header pill, Hero/Pricing rect, Ghost, Top Nav, Sub-nav pill, Billing toggle, Pricing card, White product card, Warm feature tile, Accent treatment. The accent-as-information reading at 414 carries an adjacent complete qualifier (B2/B2a). |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness `provenance.md` 29–38; Tier 1 list 50–52; Tier 2 59–61. `https://heptabase.com/pricing` is dual: `DESIGN.md` 9 + `provenance.md` 45/51/54 (E2a). `https://wiki.heptabase.com` is dual: `DESIGN.md` 9/556 + `provenance.md` 46/52/54 (E2a). Conflicts unresolved: none — `provenance.md` 38. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 437–450. Canvas, 3-column pricing, warm tiles, spacing and shape restated from token-set paths. The generous-canvas / tint-not-elevate / type-not-color readings carry an adjacent qualifier at 450 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 134–140. Flat / tint / hairline table and `box-shadow: none`. The flat/tint-and-hairline reading carries an adjacent qualifier at 140 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 60–67, under the grouping qualifier at 58. Not placed in Governance controlled copy. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 73–80, under the qualifier at 71. The list names only surfaces and treatments the source already named. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 454–473: Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px`, 48px / 36px targets, collapsing strategy, shadowless image behavior. The Desktop row keeps the source body's `1024-1440px` range. The system-level-not-cross-viewport reading carries an adjacent qualifier at 452; image-behavior at 473 (B2/B2a). |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompt and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` 123. |
| §10 Voice & Tone | 옮김 → Content & Locales | Published strings `DESIGN.md` 489–513; tone table 480–486. The qualifier at 478 covers the voice reading (B2/B2a, A5). Forbidden register at 514 is the source's own list. |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13: Alan Chan, visual note-taking and knowledge-management, infinite whiteboard of cards, vision "create a world where anyone can effectively establish a deep understanding of anything", Taipei-based team, `country: TW` by operating base, Delaware-incorporated, Y Combinator W22, US-registered legal entity. Marked there as narrative context that supplies no interface tokens; that classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` 146. |
| §12 Principles — 5 numbered | 옮김 → Experience principles | `DESIGN.md` 50–54 under the B2a form at 48: "These five items are a derived editorial implementation inference from the verified surfaces; they are not Heptabase-authored or a separately published UI specification." Heptabase publishes no first-party design-system documentation in the source, so the toss-form close is used (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` 154–185 (32 rows). |
| §13 Personas — 3 entries | 삭제 | Fictional archetypes. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, or biography appears in either output (D2, D2a). Disposition at `provenance.md` 119. Audience at `DESIGN.md` 31 carries only the group-level description the source's own disclaimer establishes (university researchers, lifelong learners, knowledge workers building personal knowledge bases), under an adjacent qualifier. The four `primary-tasks` at 21–24 come from labels the source records; 26 says so and qualifies the step from label to "primary task". |
| §14 States — 9 rows | 옮김 → Components & States surface state contract + per-component applicability | Full nine-row body preserved at `DESIGN.md` 420–430 (A2; the catalog graph is still 0/440, so nothing is delegated). The qualifier at 418 names that system-level remainder. The applicability rule is at 230–234, whose qualifier at 232 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | 6 interactive controls × 7 states = 42 rows: Header / Hero / Ghost CTAs, Top Nav, Sub-nav pill, Billing toggle. Destination and toggle roles close loading / error / success with a role reason (C2 v10) — `loading \| not-applicable` ×6 at 256/280/305/327/350/373; `loading \| applicable` DESIGN 0. Cards declare no `kind` and no map because the source supplies no interaction evidence for them — `DESIGN.md` 386/397/408 (C4). |
| §15 Motion & Easing | 옮김 → Foundations motion | Durations kept at `DESIGN.md` 148–150 (`motion-fast` 120ms / `motion-standard` 200ms / `motion-slow` 320ms). Three easing roles and uses kept at 154–158; the three cubic-bezier values are omitted as untraceable to Heptabase evidence (T1-3 제약 5). Motion rules including no-bounce, infinite-canvas panning/zooming, and `prefers-reduced-motion: reduce` at 162–168. B3 is held at `DESIGN.md` 160 in full text: five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — the per-component gate, and the partial-confirmation exclusion. `DESIGN.md` 552 names the five kinds and the per-component gate; it does not repeat the partial-confirmation sentence (E2c). |

## Sibling handling (`web/references/heptabase/.verification.md`)

The sibling exists — confirmed with `find web/references/heptabase -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` 69–88 and is **not** promoted into `DESIGN.md` as a new fact.
- Forms it carries that the visible source body does not: viewport `1440×900`; card measurements `420×676` / `644×500` / `316×152` / `420×717`; radius-frequency `50%`; `rgba(0,0,0,0.72)`; inactive sub-nav padding `7px 18px 7px 22px`; `100 AI credits/month`; `AI TutorNew`; document.title `Pricing | Heptabase`; method string `playwright getComputedStyle`. Recorded at `provenance.md` 92–102.
- Measured `DESIGN.md` 0 for those sibling-only strings: `1440×900` 0 · `420×` 0 · `50%` 0 · `0.72` 0 · `100 AI credits` 0 · `AI TutorNew` 0 · `Pricing | Heptabase` 0 · `playwright` 0.
- Hex values those RGB samples convert to (`#f7f7f7`, `#2e2e2e`, `#207dff`, `#75c33a`) are already in the source body and remain there.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. `compared < candidates` is expected, so the A5a hand sweep is mandatory and was run.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| All CJK runs in the source, any length | 1 distinct (繁體中文) | 0 | 0 | Survives in `DESIGN.md` 31/67/79/510/516 (also in provenance 121). |
| All CJK runs in the sibling, any length | 0 | 0 | 0 | Sibling has no CJK. |
| Published Latin / English strings from the source body | 18 | 0 | 0 | See sub-needles below. |
| Sibling-only published fragments | 3 (`100 AI credits/month`, `AI TutorNew`, `Pricing | Heptabase`) | 0 from DESIGN (intentionally) | 0 promoted | Kept as sibling corroboration at `provenance.md` 98–100. |

Sub-needle labels confirmed present individually in `DESIGN.md`: Master anything you learn, Do your best research and thinking, Ask AI to explain any sources you bring, For anyone building a lifelong knowledge base, Trusted by customers from the world's leading universities, create a world where anyone can effectively establish a deep understanding of anything, Get started, Get started on mobile, AI chat, AI tutor, Save 25%, Home, AI Tutor, Wiki, Download, Gallery, Pricing, Monthly, Yearly, 繁體中文, Heptabase, Hero headline, Instrument Sans Medium, Section titles, Instrument Sans, Pricing tier / card titles, Inter SemiBold, Nav links, Inter, Standard reading text, Inter, Header CTA label, Inter Medium, Segmented sub-nav pill, Inter.

A5 분모: CJK hand sweep 1 extracted / 0 missing; published Latin hand sweep 18 extracted / 0 missing. `node test-v2/tools/latin-copy-audit.mjs --brand heptabase` reported `candidates: 45`, `lost: 6` (`high: 1`, `medium: 5`). Disposition of those six, none treated as unrestored published copy:

- `Pricing | Heptabase` (high) — sibling `document.title` only. Kept at `provenance.md` 88/100. Not promoted into the portable body (B1).
- `) and social proof (` (medium) — source HTML-comment prose fragment, not a label.
- `No designs found for 'heptabase'.` (medium) — getdesign.md 404 string, already at `provenance.md` 59. Third-party, not Heptabase-issued copy.
- `the website is a demonstration of the product's thesis` (medium) — source editorial reading, already in Scope ¶4 at `DESIGN.md` 15 with the fuller §11 wording and a complete qualifier.
- `this is an AI feature.` (medium) — §13 persona biography. Dropped (D2, D2a). Not restated.
- `this is special` (medium) — source §2 editorial gloss of Accent Blue. Restored on the role line at `DESIGN.md` 105, under the Semantic color qualifier at 88.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand heptabase --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 170 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 170.

## Deviations recorded

- `DESIGN.md` is 6,780 words by `wc -w`, above the spec's 600–1,800-word SHOULD budget. The budget yielded to A1: nine declared component records with dual `use`/`font` forms, a nine-row surface state contract, a 42-row applicability matrix, the full §11 founding-and-incorporation narrative, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- Heptabase publishes no first-party design-system documentation in the source, so every derived-editorial close uses the toss-form (rulebook v12 B2a 전제 주석). Worker-close measure is 29=29. Auditor-close measure is 32=32.

## F1 B2a rescan (mandatory final pass)

The finished `DESIGN.md` was re-read from line 1. Every causal, interpretive, or judgment sentence — including Scope so/therefore boundaries, Content voice readings, and Wiki-citation characterizations — was asked whether it is a Heptabase-issued fact or an observation-derived reading. Source-stated narrative (Alan Chan, Taipei, Delaware, Y Combinator W22, the vision sentence, published copy) was left unqualified. Atmosphere readings, task selection, audience grouping, principle/Do/Don't rationales, role characterizations, key-path disambiguation, curve omission, font-license/fallback classing, favicon-service classing, applicability reasons, layout readings, and the voice register were given an adjacent complete-form qualifier. Complete-form count: `grep -o 'derived editorial implementation inference' DESIGN.md | wc -l` = 32 = `not Heptabase-authored` 32 = `separately published UI specification` 32. Provenance derived ledger 32 rows (E1 1:1). Auditor added three complete-form lines (Type roles 209, Accent Treatment 414, Recorded unresolved 550) and expanded Scope ¶3 / Audience / Shape so those adjacent hedges name the readings they already sat next to.

## F2 E2 check (mandatory final pass)

Each log row above was written only after `grep` located the value in the named file and section. Dual destinations (homepage, `#2e2e2e`, favicon slug, pricing URL, wiki URL, spacing/shape at Foundations 117/121–128 and Layout 441–442) name both files. The B3 claim is backed by the full five-kind + gate + partial-confirmation sentence at `DESIGN.md` 160; 552 names the five kinds and the gate only. The B2a 32=32 claim is backed by `grep -o` on the finished body and by the 32-row provenance inventory at `provenance.md` 154–185. The §13 deletion row does not restate names, ages, or cities (D2a). The omission ledger does not assert absence of an item in the same sentence that lists it (E2d). Worker-close portable DESIGN SHA-256 `6237d717709a6e21b73f1ef1cc4401bf15904629ea709b188872395748d299e7`. Auditor-close SHA is recorded in `audit-log.md`.
