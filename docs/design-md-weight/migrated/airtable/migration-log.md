# Airtable migration log

Source: `web/references/airtable/DESIGN.md`
Destination: `docs/design-md-weight/migrated/airtable/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/airtable/provenance.md`
Date: 2026-08-23
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7
Portable Core: post-F3 re-run. `scripts/design-md-core.cjs` inspectDesignMd `portable_core: true`, placeholders 0. `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/airtable/DESIGN.md --check --require-portable-core --json` → exit 0, `portable_core: true`, reasons [], SHA-256 `c306d03e3b3f8a8283585b356df026fbee91e629fabeb1aad85efd285035fe7a`. `node test-v2/tools/migrate-reference.mjs --brand airtable --gate-only` → PASS, problems [].

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (id, name, country, category, homepage, primary_color, logo) | 분리 → provenance; homepage 옮김 → Experience Scope; `primary_color` 옮김 → Foundations Brand-amber + Scope/Avoid/capture-bound; logo 경계 문장 옮김 → Typography & Assets | Portable file has no frontmatter. Name kept as H1 `Airtable Design System`. Homepage `https://www.airtable.com` is dual Scope + provenance identity/surfaces/Tier 1 (E2a). Catalog `primary_color` `#fcb400` is identity + portable Foundations Brand-amber + Scope/Avoid/capture-bound catalog-value mentions, not a CTA fill (E2a). Catalog logo type `simpleicons` / slug `airtable` is dual: provenance identity + portable Typography & Assets boundary sentence (`Catalog logo metadata is Simple Icons identity (airtable)`, not a captured first-party mark) (E2a). |
| YAML `omd`, `verified`, token claims, `tokens.source` / `extracted` / `note`, `components_harvested` | mixed: `omd` / `verified` / `extracted` / `components_harvested` / `tokens.source` 분리 → provenance; `tokens.note` 옮김 → Experience Scope + provenance | `tokens.source: prose-derived` is provenance-only (A1c). Token note (`primary = Airtable Blue (#1b61c9); brand-amber (#fcb400) from primary_color frontmatter. Color belongs to user data, chrome stays neutral.`) is dual Scope + provenance; the “not a CTA fill” assignment and color-belongs-to-data sentence have adjacent catalog-commentary / derived editorial implementation inference / not Airtable-authored or a separately published UI specification limiter in Scope (E2a, B2/B2a). YAML `verified` 2026-05-15 is provenance freshness only. |
| YAML `ds.name` / `ds.url` / `ds.type` / `ds.description` / `ds.og_image` | mixed: 전부 분리 → provenance; `ds.name` / `ds.description` / `ds.type: brand` / `ds.url` 옮김 → Experience Scope + Font evidence | `ds.type: brand` preserved (A1c). Destinations: provenance identity + portable Scope + Font evidence Official product-use. Packet treats Trademark Guidelines as named brand-usage guidance; interface-token evidence from that document is unresolved (no independent “they do not publish interface tokens” claim) (E2a). `ds.og_image` is provenance-only. |
| YAML `tokens.colors` / `typography` / `spacing` / `rounded` / `shadow` / `components` | 옮김 → Foundations, Typography & Assets, Components & States | 검증된 값만 최소 필드 단위. YAML unitless `lineHeight` 1.15 / 1.50 / 1.25 / 1.20 / 1.40 / 1.35 / 1.30 / 1.28는 비율로 보존 (A1a). YAML weights 450 (sub-heading, caption) 보존. YAML typography `use` 열 개 field (Hero headlines / Bold display, Haas Groot Disp / Section headings / Sub-headings / Card titles / Feature text / Standard reading text / Emphasized body / Button labels / Captions, small labels) restored on Type roles (A1). 검증된 primitive type은 컴포넌트별로 보존: button×2 (button-primary / button-white) + body Cookie Consent `Type: button` + `Type: input` + `Type: card`×2 + `Type: badge`. `Kind: interactive`로 뭉개지 않음 (A1b). `#1b61c9`와 catalog `#fcb400`, `#ffffff` on-primary와 canvas, `#f8fafc` surface와 badge bg, White Button `1px #ffffff` border와 Hairline `#e0e2e6`, YAML `rounded.full` 9999와 body `50%`는 비합침 (A4). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience `scope` claim, distinctive traits; letter-spacing 범위는 Typography에도 | 제품/표면 범위(airtable.com marketing chrome + named product patterns). Atmosphere “sophisticated simplicity” + Swiss-precision 문단 인접에 derived editorial implementation inference / not Airtable-authored or a separately published UI specification 한정 (B2/B2a). Source atmosphere phrase is retained, not deleted. Scope evidence-domain (“marketing chrome, named product patterns, trademark guidance, and third-party history remain separate”) has adjacent complete B2a. Key characteristics는 Distinctive traits. Distinctive의 cookie-consent 2px local 독해는 인접 완전 B2a. Body-text letter-spacing `0.08px`–`0.28px`는 Distinctive + Typography “§1 also states” 이중 (E2a). `https://www.airtable.com`는 provenance와 dual (E2a). No docs/native/unpublished negative coverage claim. Dropped “Token/theme facts는 한정 없이” (E2c). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color; shadow는 역할별로 갈림 | Deep Navy / Airtable Blue / White / Spotlight / Success / Weak Text / Secondary Active / Dark Gray / Mid Blue / Border / Light Surface는 semantic color. Spotlight·Weak Text·Secondary Active는 body-only. Soft ambient는 Foundations Elevation + Avoid only. Standard composite는 Elevation + Primary Blue Button + Standard Card. Soft is not a component field (E2a). Estimated `#6b7280`은 §9 값이며 Weak Text와 비합침. |
| §3 Typography Rules | 옮김 → Typography & Assets | 증거 등급. Haas / Haas Groot Disp. YAML unitless line-height 보존 (A1a). YAML `use` 열 개 field restored. Body-table Sub-heading tracking `normal` restored (was `—`). YAML vs body 범위는 양쪽 유지·비평균. 시스템 fallback을 Haas로 표시하지 않음. Live computed FontFace는 이 패킷에 없음 — Unresolved claim. Trademark Official product-use: interface-token evidence unresolved; no “they do not publish a universal current typography token” independent claim. |
| §4 Component Stylings | 옮김 → Components & States; Cookie 2px는 Experience/Foundations/Layout에도 | Primary Blue / White / Cookie Consent `Type: button`; Default Input `Type: input` (inferred, adjacent complete B2a); Standard + Large Section `Type: card` (kind/map 생략, C4); Default Badge `Type: badge` (inferred, adjacent complete B2a; kind/map 생략, C4). Capture selector 없음 — 발명하지 않음. Cookie 2px는 YAML 밖 §4 고유값이며 Cookie Consent 컴포넌트 + Distinctive + Avoid + Shape + Layout에 있다 (E2a). Cookie local-geometry field note and “deliberately sharp” Use share one adjacent complete B2a; Avoid 머리도 deliberately-sharp를 이름을 들어 덮음. Primary/White/Input loading·error·success omitted (C2). |
| §5 Layout | 옮김 → Layout & Platforms + Foundations spacing/shape | YAML scale + body 1–48px (8px base) + radius 2/12/16/24/32 + 50% circles. 9999 vs 50% 비합침. Shape local-geometry 독해와 Layout not-a-gutter / not-cross-viewport / not-universal-grid 독해는 각 절 인접 완전 B2a (B2/B2a). |
| §6 Depth | 옮김 → Foundations elevation | Blue-tinted multi-layer + Soft ambient 문자열 보존. Brand-blue 인과 독해(“so elevation ties back to the brand color”)의 원천은 §6이 아니라 §7 Do이며, 그 독해는 Elevation 본문에 인접 완전 B2a로 둠. |
| §7 Do's | 옮김 → Experience principles (capture-bound application); 인과 독해는 Foundations elevation에도 | Token-role 규칙. Numbered editorial Principles와 분리. Capture-bound 머리에 derived editorial implementation inference / not Airtable-authored or a separately published UI specification (B2/B2a). Do's “so elevation ties back to the brand color” 인과는 Foundations Elevation에도 인접 완전 B2a (E2a: capture-bound + Elevation). Governance 통제 문구에 넣지 않음. |
| §7 Don'ts | 옮김 → Experience avoid; motion 금지는 Foundations motion에도; forbidden voice는 Content에도 | 브랜드 금지. Swiss-precision / spotlight-depth / color-belongs / deliberately-sharp 2px 독해는 Avoid 머리 인접 완전 B2a (B2/B2a). Bouncy-spring / 150–400ms / `prefers-reduced-motion` 금지는 Avoid + Foundations Motion 이중 (E2a). Forbidden voice는 Avoid + Content 이중 (E2a). 미관측을 `not-applicable`로 바꾸는 근거가 아님 (C1). |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `425–1664px` (23 breakpoints) 기록 범위. “not a list of 23 named values / not a cross-viewport specification” 독해는 Layout 인접 완전 B2a 아래 (B2/B2a). 원본에 없는 최소폭 발명 없음. |
| §9 Agent Prompt Guide | mixed: 도구 프롬프트 삭제; 추정 hex·prompt-only 수치 분리 → provenance omission + portable Named gaps / Typography “not promoted” / Components not-promoted | 도구별 복붙 프롬프트·Iteration Guide 삭제. Color reference의 검증된 hex는 이미 Foundations에 있음. 추정 CTA hover `#0f4ba0` (~10% darker)와 muted `~#6b7280`는 portable Named gaps + Foundations “not promoted” + Components Capture record / Primary Blue hover reason + provenance omission ledger (E2a). Prompt-only `12px 20px` / Haas 14px 500 / Haas Groot Disp 18px 600는 Typography “not promoted” + provenance omission (E2a). Nav-header construction prompt는 portable에서 삭제하고 provenance omission ledger에 보관 (수확 컴포넌트 아님). 슬롯 없는 위임 없음. |
| §10 Voice & Tone | mixed: 관측 세 homepage string 옮김 → Content + provenance; derived 표/독해 옮김 → Content (인접 B2a); 금지는 Content + Avoid | Observed (homepage 2026-05): `"Build enterprise-ready AI workflows, apps & agents"` / `"Sign up"` / `"Talk to sales"` — Content + provenance, including the 2026-05 date. Hero line은 Scope + Primary tasks에도, `"Sign up"`는 Scope/Capture live-DOM notes에도 있다 (E2a). Derived editorial copy-pattern table (`Create base` / empty-state extra words / error sample / lower-case headings / onboarding) and voice reading sit under adjacent complete B2a and are not labeled Observed (B2/B2a, E1). Forbidden phrases는 Observed 제목 아래 남아 있으나 인접 완전 B2a로 homepage 관측과 분리되고, Avoid에도 있다 (E2a). Treating §14 empty-state strings as state-contract not extra voice samples has adjacent complete B2a in Content; the strings themselves remain capture record + Primary tasks + Content (E2a). 원본에 없는 도메인 coverage 문구 신설 없음 (D1). |
| §11 Brand Narrative | 옮김 → Experience `scope`; 서사 원장 분리 → provenance; 현재 라인은 Content + Primary tasks에도 | Duke connections, Etacts (YC W2010, age 20, Gmail, Salesforce acquisition 2010-12-21), Ofstad Android/Google Maps, Nicholas Stack Overflow 3+ years, ease-of-use vs structural-rigor hybrid thesis, pre-seed Birch/Bebo + Reeves/Gusto, 2012 founding, 2014 beta, March 2015 public launch, funding figures는 third-party-corroborated (Wikipedia / Antler / Taskade / Tracxn / Golden) and restated in portable Scope. 현재 라인 `"Build enterprise-ready AI workflows, apps & agents"`는 Scope + Content Observed + Primary tasks + provenance (E2a). 2015–2020 → 2024+ evolution과 Notion/no-code refusal 독해는 Scope 인접 완전 B2a (B2/B2a). 연표 URL은 provenance. |
| §12 Principles | 옮김 → Experience principles | 다섯 항목 전체 editorial readings. 인접 본문에 derived editorial implementation inference / not Airtable-authored or a separately published UI specification. Capture-bound는 token-role 규칙이며 같은 완전 B2a를 별도 머리에 둠. Governance 일반 문구는 그 한정의 대체물이 아님 (B2/B2a, E1). |
| §13 Personas | mixed: 가상 biography 삭제·sidecar 재수록 없음; Audience는 배제 경계만 | 원본이 fictional archetypes라고 명시. Names/ages/cities/employers/biographies는 portable Audience에도 provenance에도 없음 (D2). Audience exclusion + observable-work-follows-three-tasks application have adjacent complete B2a (B2/B2a). Independently verified Primary tasks 3건은 §13이 아니라 empty-base / add-record, sync-reconnect, homepage 2026-05 AI-workflows line (`count=3`). |
| §14 States | 옮김 → Components & States capture record + per-component applicability; empty-state 문자열은 Primary tasks + Content에도 | 본문 보존: Empty (no bases) / Empty (no records) / Loading (base opening) / Loading (sync) / Error (sync failed) / Error (formula `#ef4444`) / Success (saved) / Success (workflow run) / Skeleton (`#e5e7eb`) / Disabled 0.5 opacity / Loading (AI agent) (A2). `#e5e7eb` is also Semantic color Skeleton value + Capture record field note (E2a). "Create your first base" / "+ Add a record"는 capture record + Primary tasks + Content 삼중; Content의 state-contract-not-extra-voice-samples 독해는 인접 완전 B2a (E2a, B2/B2a). Capture-record named-product-pattern vs harvested-component split has adjacent complete B2a (B2/B2a). 선언 컴포넌트는 §4.4를 닫되 미관측 시각값은 발명하지 않음. `not captured` / `not named`를 `not-applicable` 사유로 쓰지 않음 (C1). Primary Blue / White / Default Input loading·error·success omitted (exact mapping unresolved) (C2). Cookie Consent remains a one-shot banner accept with not-applicable rows from that role. Card/Badge는 interactive-kind 근거 없어 kind/map 생략 (C4). Named Focus 없음; focus-visible 행에 hex 없음 (B1). graph 위임 없음. State coverage 완료 주장 없음 (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion; 무출처 커브 값 분리 → provenance omission ledger; 커브 이름 생략 사실은 Named gaps에도 | Duration (`motion-instant` 0ms / `motion-fast` 150ms / `motion-standard` 250ms / `motion-slow` 400ms), easing 이름 (`ease-enter` / `ease-exit`), “No bouncy springs”, `prefers-reduced-motion: reduce` (spotlight fade-in) 유지. Exact cubic-bezier `cubic-bezier(0.2,0.6,0.25,1)` / `cubic-bezier(0.4,0,1,1)` 값은 무출처라 portable에서 생략하고 provenance omission ledger에 보관 (E2b). 커브가 생략됐다는 사실은 portable Motion 표 + Named gaps에도 있다 (E2a). `ease-exit` 커브는 legacy spec-template 예시와 일치. Foundations Motion은 B3 다섯 증거 종류(transition properties · animation name · duration · easing · reduced-motion behavior)를 컴포넌트별 computed 관측한 뒤에만 표 밖 값을 승격한다는 게이트를 전문 명시. “공식 출처로 검증될 때까지” 약화 문구는 쓰지 않음 (B3, E2c). Named gaps가 같은 다섯 종류 게이트를 말함. |
| Footer **Verified** / Tier 1 / Tier 2 / Philosophy / Style ref / Conflicts | 분리 → provenance; live-DOM 세 노트와 homepage URL은 portable에도 | Dual (E2a): `https://www.airtable.com`는 Scope + provenance; playwright notes (round 50% icon buttons; ghost 12px; Sign up CTA)는 Scope/Capture record + provenance Tier 1. Footer verified 2026-05-08 (B1 loop)는 provenance freshness (YAML 2026-05-15와 둘 다 유지). Style ref `notion`은 provenance-only. Conflicts unresolved: none. |

### F1 / F2 (v7 mandatory final passes)

Superseded by the wave5 full resubmit scan below. Prior F1 omitted Input/Badge inference and claimed “sophisticated simplicity” was not retained. Prior F2 omitted ds.name/description portable hits, primary_color Avoid/capture-bound, soft-vs-standard shadow split, `#e5e7eb` Semantic/Capture destinations, and homepage-hero Primary tasks.

## Revision 2026-08-23 (wave5 full resubmit)

Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7. Source FAIL: `docs/reviews/t2-1-wave5-2026-08-23-sol-full.md` §2.

| Item | Correction |
|---|---|
| Typography A1 | Restored ten YAML `use` fields and Sub-heading tracking `normal`. |
| Live Proof E1 | Removed per-claim `airtable-live` mapping. Live DOM notes stay marketing-surface only; tokens remain prose-derived. |
| Input/Badge B2a | Adjacent complete B2a on Default Input and Default Badge; provenance derived inventory updated. |
| C2 | Primary Blue / White / Default Input loading·error·success omitted. |
| D1 | Removed docs/native/unpublished negative coverage and the independent Trademark no-token conclusion. Interface-token evidence from Trademark Guidelines is unresolved. |
| Narrative A1 | Restored Duke / Etacts / Ofstad / Nicholas / hybrid-thesis / Bebo-Gusto values. “sophisticated simplicity” retained under Scope B2a; §1 row records that retention. |
| Source-row E2/E2a | ds, `primary_color`, shadows, `#e5e7eb`, homepage hero / Primary tasks destinations rewritten. |

### F1 / F2 (resubmit)

- **F1 B2a scan (full DESIGN.md reread):** Adjacent complete B2a on: Scope “sophisticated simplicity” / Swiss-precision / Haas letter-spacing; token-note color-belongs-to-data and “not a CTA fill”; Scope evidence-domain (marketing / named product patterns / trademark / third-party history remain separate); 2015–2020 → 2024+ evolution; Notion/no-code refusal; Audience exclusion / observable-work application; Distinctive local cookie-consent 2px; numbered Principles; capture-bound token-role list; Avoid Swiss-precision / spotlight-depth / color-belongs / deliberately-sharp 2px; Elevation brand-blue / spotlight-vs-gray; Shape local-geometry; Layout not-a-gutter / not-cross-viewport / not-universal-grid; Cookie Consent local-geometry field note and “deliberately sharp” Use; Capture-record named-product-pattern vs harvested-component split; Content empty-state strings as state-contract not extra voice samples; Content forbidden-phrase list; Content voice + copy-pattern table; Default Input inference; Default Badge inference. Founding/narrative facts are labeled third-party-corroborated. Trademark sentences state unresolved interface-token evidence; they do not claim the Guidelines publish no tokens. No docs/native/unpublished coverage sentence remains. This F1 does not claim that live-DOM notes “no longer draw” a harvested-component conclusion: the Capture-record split is retained under adjacent complete B2a. Left without B2a (verified values, A4 field-identity, C2 omission sentences, Cookie not-applicable role reasons, B3 gate, Governance boilerplate) — not a reconstruction-boundary exemption for third-class judgements.
- **F2 E2 grep (value + field/role context):** `#1b61c9` grep-matched Scope token-note/atmosphere, Distinctive, capture-bound, Avoid, Foundations Primary, Primary Blue Button, Cookie Consent Button — not a `#fcb400` CTA fill. `#fcb400` grep-matched Foundations Brand-amber + provenance identity + Scope/Avoid/capture-bound, not a CTA fill. `ds.name` / Trademark Guidelines / `ds.type: brand` grep-matched Scope, Font evidence, and provenance identity. YAML `use` strings (Hero headlines, Emphasized body, Button labels, Captions, small labels) grep-matched Type roles. Sub-heading Tracking `normal` grep-matched. Soft ambient `rgba(15,48,106,0.05)` grep-matched Elevation and Avoid, not Primary Blue/Standard Card fields. Standard composite grep-matched Elevation + Primary Blue + Standard Card. `#e5e7eb` grep-matched skeleton row + Semantic color + Capture record field note. Homepage hero grep-matched Scope, Primary tasks, Content Observed, provenance. `airtable-live` claim ledger grep-matched no per-token live Proof table. Input/Badge grep-matched adjacent complete B2a. Primary/White/Input grep-matched omission sentences, not destination `not-applicable` rows. Duke / Etacts / YC W2010 / Bebo / Gusto grep-matched Scope and provenance narrative. “sophisticated simplicity” grep-matched Scope. `docs, native, and unpublished` grep-matched no portable sentence. B3 five-kind gate grep-matched Foundations Motion. Persona names absent from DESIGN.md and provenance. New B2a sites grep-matched: Scope evidence-domain; Audience Restricting; Capture-record “not visual treatments”; Cookie local-geometry field note sharing the deliberately-sharp limiter; Content empty-state state-contract routing. Dropped “Capture-record live-DOM notes no longer draw a not harvested components causal conclusion” (E2c).
