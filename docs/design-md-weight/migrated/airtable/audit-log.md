# Airtable F3 audit (wave5 full resubmit)

Auditor: grok-4.6 (fresh session, not the T2 worker).
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7 — B2 / B2a / E1 / E2 / E2a–c only.
Date: 2026-08-23

Sentence class used: brand-published fact / observed technical / editorial interpretation or causal judgement. Only the third class without an adjacent complete qualifier (`derived editorial implementation inference` / `not Airtable-authored or a separately published UI specification`) was edited in the body. There is no reconstruction-boundary exemption. Token values, component tables, state applicability, and structure were not changed.

Worker resubmit decisions left in place: YAML typography `use` fields and Sub-heading tracking `normal`; no per-claim `airtable-live` Proof map; Input/Badge adjacent complete B2a; Primary/White/Input loading·error·success omitted; no docs/native/unpublished negative coverage; no independent Trademark no-token claim; narrative values restored; “sophisticated simplicity” retained with B2a.

## Classification (DESIGN.md)

Already adjacent to a complete B2a phrase and left as-is (except the incomplete token-note limiter, which was completed):
- Scope “sophisticated simplicity” / Swiss-precision / Haas letter-spacing
- Scope 2015–2020 → 2024+ evolution
- Scope Notion/no-code refusal
- Distinctive local cookie-consent 2px
- numbered Principles (five items)
- capture-bound token-role list
- Avoid Swiss-precision / spotlight-depth / color-belongs / deliberately-sharp 2px
- Shape local-geometry
- Elevation brand-blue / spotlight-vs-gray
- Layout not-a-gutter / not-cross-viewport / not-universal-grid
- Default Input inference; Default Badge inference
- Content forbidden-phrase list; voice + copy-pattern table

Left unqualified (verified values, A4 field-identity, C2 omission sentences, Cookie not-applicable role reasons, B3 gate, Governance boilerplate — not a reconstruction-boundary exemption for third-class judgements):
- Scope coverage inventory, Trademark unresolved-evidence sentences, live-DOM named observations
- Primary tasks, Distinctive hex/radius/family facts
- Semantic color / spacing / shape YAML↔body non-merge notes
- Font evidence unresolved / fallback stacks; Type role YAML `use` and tracking; Simple Icons identity-only
- Component anatomy, C2 omission sentences, C4 kind/map omission
- Layout recorded measurements, Observed homepage strings, Named gaps

Third class found without adjacent complete limiter:
- Scope token-note “not a CTA fill” (limiter named only color-belongs-to-data)
- Scope evidence-domain: marketing chrome / named product patterns / trademark / third-party history remain separate
- Audience exclusion / observable-work-follows-three-tasks application
- Capture-record named-product-pattern vs harvested-component split
- Cookie Consent local-geometry field note (deliberately-sharp limiter did not name it)
- Content empty-state strings as state-contract not extra voice samples

## Fixes

1. `DESIGN.md` Scope token-note — extended the adjacent complete B2a so the “not a CTA fill” assignment and the color-belongs-to-data sentence share `derived editorial implementation inference` / `not Airtable-authored or a separately published UI specification`.
2. `DESIGN.md` Scope live-DOM paragraph — attached **The following evidence-domain sentence is a derived editorial implementation inference from the verified surfaces; it is not Airtable-authored or a separately published UI specification.** before the separate-domains sentence.
3. `DESIGN.md` Audience — attached **Restricting Audience so those fictional archetypes are not Audience and are not primary tasks, and tying observable work only to the three primary tasks, is a derived editorial implementation inference from the verified surfaces; it is not Airtable-authored or a separately published UI specification.**
4. `DESIGN.md` Capture record — attached the same complete evidence-domain limiter before “They are not visual treatments of the harvested marketing buttons, input, cards, or badge.”
5. `DESIGN.md` Cookie Consent — extended the adjacent complete B2a to name the local-geometry field note and the “deliberately sharp” reading together.
6. `DESIGN.md` Content — attached complete B2a on treating §14 empty-state strings as state-contract, not extra voice samples. Recorded strings kept.
7. `provenance.md` Identity token-note — limiter wording completed to the same evidence class as the body, including “not a CTA fill”.
8. `provenance.md` Proof notes derived range — was narrower than the body (omitted Scope evidence-domain, “not a CTA fill”, Audience application, Capture-record split, Cookie field-note, Content empty-state routing). Restated to the actual B2a sites.
9. `migration-log.md` YAML `tokens.note` — recorded the completed token-note limiter (not a CTA fill + color-belongs).
10. `migration-log.md` §1 — recorded Scope evidence-domain B2a; dropped “Token/theme facts는 한정 없이” (E2c).
11. `migration-log.md` §4 — Cookie local-geometry field note now shares the deliberately-sharp adjacent complete B2a.
12. `migration-log.md` §7 Do's — limiter quote completed with **a** separately published UI specification (body already had it).
13. `migration-log.md` §10 — recorded Content empty-state routing B2a; strings remain capture record + Primary tasks + Content (E2a).
14. `migration-log.md` §13 — recorded Audience exclusion / observable-work adjacent complete B2a.
15. `migration-log.md` §14 — recorded Capture-record split B2a and the Content empty-state routing destination.
16. `migration-log.md` F1 — listed the newly qualified sites; withdrew “Capture-record live-DOM notes no longer draw a not harvested components causal conclusion” (E2c). No reconstruction-boundary exemption.
17. `migration-log.md` F2 — `#1b61c9` destinations widened to actual grep hits (Scope / Distinctive / capture-bound / Avoid / Foundations Primary / Primary Blue / Cookie). New B2a sites recorded.

No token values, component tables, state applicability, or section structure were changed. C2 omissions were not reverted. Per-claim `airtable-live` mapping was not reintroduced. docs/native/unpublished negatives were not restored.

AUDIT_DONE fixes=17
