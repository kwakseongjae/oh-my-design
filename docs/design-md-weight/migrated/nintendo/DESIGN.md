# Nintendo Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Nintendo Co., Ltd. (任天堂) is a Kyoto, Japan-headquartered video-game and consumer-hardware company. Catalog homepage identity is `https://www.nintendo.com/us/`. Treating the following two URLs as the named first-party evidence domains of this reconstruction is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. This contract covers two brand-owned web surfaces from the 2026-06-17 packet: the US store `https://www.nintendo.com/us/` and the Japanese site `https://www.nintendo.com/jp/`.

Source token note: primary = Nintendo red (`#e60012`), the brand's near-immutable signature, used identically on US (`nintendo.com/us`) and JP (`nintendo.com/jp`) surfaces — links, primary CTAs, logo. US text ink is `#484848` with Geologica Variable; JP text ink is `#3c3c3c` with the YakuHan/Hiragino CJK stack. Translucent overlay/shadow values appear in prose/components only. Treating that note as a register split — `#e60012` is catalog `primary_color` and the harvested primary-CTA fill plus Play-pill fill plus inline-link ink plus wordmark, US `#484848` stays with Geologica Variable rather than JP `#3c3c3c`, JP `#3c3c3c` stays with the YakuHan/Hiragino CJK stack rather than Geologica, and translucent overlay/shadow values stay in prose/components rather than in `tokens.colors` — is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

The following visual-character, sacred-red, eye-trained-that-red-means-action-red-means-Nintendo, orderly-optimistic-family-friendly, and engineered-trustworthy-welcoming readings — including near-shadowless light-canvas, a single saturated red doing almost all the emotional work, never-pure-black for body copy, regional type that is not a philosophical split, and playful-where-it-persuades precise-everywhere-else — are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. The source reads Nintendo's web presence as a masterclass in disciplined brand craft: a near-shadowless, light-canvas system where a single saturated red does almost all the emotional work. The page opens on pure white (`#ffffff`) with soft cool-grey surfaces (`#f8f8f8`, `#efefef`) segmenting content, and text rendered in a warm dark grey (`#484848` on the US site, `#3c3c3c` on the Japanese site) — never pure black for body copy. Against that quiet neutral ground, the signature **Nintendo red (`#e60012`)** is reserved with surgical restraint: it is the color of every primary call-to-action, every inline navigational link, and the wordmark itself. The brand has kept this red essentially unchanged for decades, and the web treats it as sacred — the eye is trained that "red means action, red means Nintendo."

The following regional-type, humanist-warmth, deliberately-legible, generously-line-spaced, and split-is-regional-not-philosophical readings — including Geologica Variable on the US store at a friendly 16px body and 28px feature headlines, YakuHan handling tightened half-width kana punctuation and Hiragino carrying the kanji, and 32px on 16px Japanese reading rhythm — are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. The typographic personality differs by region but shares the same calm confidence. The US store runs **Geologica Variable** — a humanist sans with subtle warmth — at a friendly 16px body and 28px feature headlines, both at weight 600 for headings and 400 for prose. The Japanese site runs the authentic CJK stack **`YakuHanJPs, Roboto, "Hiragino Kaku Gothic ProN", "ヒラギノ角ゴ ProN W3", Arial, nc3Jp, sans-serif`**, with YakuHan handling tightened half-width kana punctuation and Hiragino carrying the kanji — a deliberately legible, generously line-spaced (32px on 16px) reading rhythm that respects dense Japanese text. The split is regional, not philosophical: both surfaces feel orderly, optimistic, and family-friendly rather than hard-sell.

The following near-shadowless, whisper-of-lift, flat-tint-and-hairline, gently-rounded, JP-tighter-2-3px-plus-50-percent-circles, and playful-where-it-persuades readings are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. What distinguishes Nintendo from louder consumer-tech peers is its restraint with depth and geometry. Shadows are barely-there — a whisper of `rgba(0,0,0,0.07) 0px 2px 8px` lifts media cards, and a slightly firmer `rgba(72,72,72,0.15) 0px 4px 16px` floats featured tiles; separation otherwise comes from flat tint and a thin `#e0e0e0` hairline. Geometry is gently rounded: 12px media cards, 8px primary buttons, fully-pill (48px) inline Play affordances, and 20px utility pills in the header. On the Japanese site the radii tighten to 2-3px with circular (50%) icon chips, a subtly more compact, information-dense register. The result is an interface that feels engineered, trustworthy, and welcoming all at once — playful where it persuades, precise everywhere else.

Treating the following public-history facts as narrative rather than interface tokens, including Kyoto-headquartered, founded-1889-as-a-maker-of-hanafuda-playing-cards, transforming-across-the-late-20th-century, and the wordmark-and-red-essentially-unchanged-for-decades, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. Nintendo Co., Ltd. (任天堂) is a **Kyoto, Japan**-headquartered video-game and consumer-hardware company, originally founded in **1889** as a maker of *hanafuda* playing cards before transforming, across the late 20th century, into one of the most recognizable entertainment brands in the world. Its design identity is defined less by trend-chasing than by an unusual constancy: the **Nintendo red (`#e60012`)** and the wordmark have remained essentially unchanged for decades, and the brand treats that consistency as an asset rather than a constraint. The source HTML comment records these as widely documented public facts; specific claims beyond the homepage are general public knowledge, not directly quoted from a verified Nintendo statement in this turn. Those years, the hanafuda origin, the late-20th-century transformation, and the constancy of the red and wordmark do not by themselves supply interface tokens.

The following calm-digital-product, light-canvas-single-sacred-accent, imagery-forward-storytelling, product-is-fun-chrome-is-composed, and trust-to-a-famously-broad-audience readings are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. That discipline carries into the digital product. Where many consumer-tech sites chase maximal contrast, heavy gradients, and aggressive conversion chrome, Nintendo's web surfaces stay deliberately calm: a light canvas, a single sacred accent color, gently rounded geometry, and imagery-forward storytelling. The product is fun; the chrome around it is composed. This restraint signals trust to a famously broad audience — Nintendo sells to children, parents, and adults who grew up with the brand simultaneously, and the design has to feel safe, legible, and joyful for all of them at once.

The following refusal, embrace, and craft-in-service-of-play readings are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. What Nintendo refuses, visible in its design: the cold, high-contrast aesthetic of enterprise tech; dark-pattern urgency in commerce; and the dilution of its red into a decorative palette. What it embraces: a near-immutable brand red used with restraint, region-tuned typography (Geologica in the West, the YakuHan + Hiragino CJK stack in Japan), flat-and-light depth, and a warm, family-first voice — craft in service of play.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=4 lang=en -->
### Primary tasks

Treating the four live-surface jobs below as the independently verified user outcomes, each naming a control or label the source records, and keeping source §13 fictional archetypes out of this list, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

- Act on US-store primary CTAs the source names: "Log in", "Learn more", "Start shopping", "See all news articles", "Pre-order now!".
- Search from the header field labelled "Search games, hardware, news, etc".
- Open top-nav destinations the source names: "Explore", "Shop", "Support" on the US store; "本体・グッズ", "ゲームソフト", "トピックス", "サポート" on the Japanese site.
- Use header utility pills the source names: "Search", "Wish List", "Cart".
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. Source §13 names fictional archetypes informed by publicly observable Nintendo audience segments (families, lifelong fans, new players), not individual people. Those biographies are dropped rather than promoted, and no name, age, city, motive, or affiliation classification is carried into this document or its sidecar. Restricting Audience so no individual personas are promoted, those fictional archetypes are not Audience and are not primary tasks, and the groups the source independently records outside the persona biographies remain the source's own strings — "kids, parents, lifelong fans" from Voice, "children, parents, and adults who grew up with the brand" from Brand Narrative, and the §13-header list "families, lifelong fans, new players" — is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Distinctive traits

- Single signature **Nintendo red (`#e60012`)** reserved for primary CTAs, inline links, and the wordmark — the system's one "action" color, identical across US and JP
- Warm dark-grey ink instead of black — `#484848` (US, Geologica) and `#3c3c3c` (JP, CJK stack)
- **Geologica Variable** on the US store; authentic **YakuHan + Hiragino Kaku Gothic ProN** CJK stack on the Japanese site
- Light canvas with cool-grey tint surfaces (`#f8f8f8`, `#efefef`, `#f2f2f2` JP) and a single `#e0e0e0` hairline
- Near-shadowless depth — `rgba(0,0,0,0.07) 0px 2px 8px` soft lift, `rgba(72,72,72,0.15) 0px 4px 16px` for featured cards
- Gently rounded geometry: 12px cards, 8px primary buttons, 48px Play pills, 20px header pills (US); 2-3px + 50% circles (JP)
- Restrained secondary accents — eShop green (`#468254`), occasional blue (`#4b5cce`) and navy (`#27455c`) for service/section theming, never competing with red
- Muted-grey neutral ladder (`#727272` → `#c8c8c8`) for secondary and disabled text

Treating `#e60012` as the single action hue rather than a decorative palette and as catalog `primary_color` plus primary-CTA fill plus Play-pill fill plus inline-link ink plus wordmark, treating `#484848` as US ink with Geologica rather than JP `#3c3c3c`, treating `#f8f8f8` / `#efefef` as US surfaces rather than JP `#f2f2f2`, treating 20px header pills as not YAML `rounded.pill` 48, treating 2-3px + 50% circles as JP rather than US 8px/12px/48px, and treating eShop green / accent blue / accent navy as subordinate service/section theming rather than a second action color, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Principles

These five items, including each *UI implication*, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. The source HTML comment records interpretive claims (e.g., "one sacred red", "calm chrome, playful content", "constancy is the brand") as editorial readings connecting Nintendo's observed design to its positioning, not directly sourced Nintendo statements.

1. **One sacred red.** Nintendo red (`#e60012`) is the single action color and brand signature. *UI implication:* reserve it for primary CTAs, inline links, and the wordmark — never let a competing saturated hue dilute it.
2. **Constancy is the brand.** The wordmark and red have barely changed in decades. *UI implication:* favor durable, classic choices over trend-driven styling; the 8-12px radius and warm grey ink should age gracefully.
3. **Calm chrome, playful content.** The product is fun; the interface around it is composed. *UI implication:* keep elevation whisper-soft and layouts orderly so imagery and play carry the energy.
4. **Region-tuned legibility.** Each script gets a font and rhythm tuned for it. *UI implication:* Geologica for Western surfaces; YakuHan + Hiragino with generous 2.0 line-height for Japanese.
5. **Welcoming to everyone.** Kids, parents, and lifelong fans share the same surface. *UI implication:* clear hierarchy, plain copy, friendly geometry, and high legibility over cleverness.

Treating the following as a capture-bound application of source §7 Do’s and harvested geometry is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

- Reserve Nintendo red (`#e60012`) for primary CTAs, inline links, and the wordmark — keep it the single "action" color
- Use warm dark grey for text (`#484848` US, `#3c3c3c` JP) instead of pure black
- Use Geologica Variable on Western/US surfaces and the YakuHan + Hiragino CJK stack on Japanese surfaces
- Separate sections with flat `#f8f8f8`/`#f2f2f2` tint and `#e0e0e0` hairlines rather than heavy shadows
- Keep elevation whisper-soft — `rgba(0,0,0,0.07) 0px 2px 8px` for standard cards
- Use 12px radius for media cards, 8px for primary buttons, 48px pills for inline Play affordances
- Let large key-art imagery carry visual energy so the UI chrome stays calm
- Use weight 600 for headings and 400 for body

### Avoid

The following items copy source §7 Don’ts, including red-dilutes-the-single-action-signal, never-pure-black-for-body, Geologica-stays-Western-CJK-stays-Japanese, flat-and-light-not-heavy-drop-shadows, eShop-green-blue-navy-stay-subordinate, home-headlines-top-out-around-28px, geometry-is-gently-rounded, and Japanese-body-line-height-not-below-2.0. They are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification.

- Spread red across many elements — it dilutes the single-action signal
- Use pure black (`#000000`) for body text — reserve warm grey
- Swap the regional fonts — Geologica stays Western, the CJK stack stays Japanese
- Reach for heavy drop shadows or hard elevation — the system is flat and light
- Introduce a competing saturated accent — eShop green, blue, and navy stay subordinate to red
- Set oversized display headlines — the home surface tops out around 28px and leans on imagery
- Use sharp square corners on cards or pills — geometry is gently rounded
- Tighten Japanese body line-height below its generous 2.0 rhythm

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

The following unmerged-role readings, including single-saturated-action-hue, US-ink-not-JP-ink, US-muted-not-JP-muted, US-surface-not-surface-alt-not-JP-surface, canvas-jobs-not-one-semantic, hairline-not-a-card-fill, and eShop-green-blue-navy-subordinate-to-red, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. Catalog `primary_color` `#e60012` is Nintendo Red (rgb 230,0,18) and the harvested primary-CTA fill; the same hex is Play-pill fill, inline-link ink, wordmark, and Red Status Pill fill — those jobs share a hex and are not one semantic. Ink `#484848` is US body/heading/nav/label with Geologica; it is not JP Ink `#3c3c3c`. Ink JP `#3c3c3c` is Japanese-site primary text with the CJK stack, a slightly deeper warm grey. Muted `#727272` is US secondary text, captions, metadata; it is not Muted JP `#8c8c8c`. Disabled `#c8c8c8` is disabled controls, placeholder, lowest-emphasis labels and carousel chrome; it is not Muted. Pure White `#ffffff` is page background, card surfaces, and text on red/dark — YAML `canvas` and `on-primary` share a hex and are not a second canvas token. Surface `#f8f8f8` is US header utility pills and segmented blocks; it is not Surface Alt `#efefef` and not Surface JP `#f2f2f2`. Hairline `#e0e0e0` is thin borders, dividers, and card outlines — the primary separation device in the near-shadowless system; it is not a card fill. eShop Green `#468254`, Accent Blue `#4b5cce`, and Accent Navy `#27455c` stay subordinate to red.

#### Primary

- **Nintendo Red** (`#e60012`): The brand's near-immutable signature. Primary CTA background (Log in, Learn more, Start shopping), the Play pill, all inline navigational/trending links, and the wordmark. The single saturated "action" hue, confirmed identical on both `nintendo.com/us` (rgb 230,0,18) and `nintendo.com/jp`. YAML `tokens.colors.primary`. Catalog `primary_color`.

#### Text / Ink

- **Ink Grey (US)** (`#484848`): Primary body, heading, nav, and label color on the US store — a warm dark grey, never pure black. YAML `tokens.colors.ink`.
- **Ink Grey (JP)** (`#3c3c3c`): Primary text color on the Japanese site — a slightly deeper warm grey paired with the CJK stack. YAML `tokens.colors.ink-jp`.
- **Muted Grey** (`#727272`): Secondary text, captions, metadata on the US site. YAML `tokens.colors.muted`.
- **Muted Grey (JP)** (`#8c8c8c`): Secondary/tertiary text on the Japanese site. YAML `tokens.colors.muted-jp`.
- **Disabled Grey** (`#c8c8c8`): Disabled controls, placeholder, lowest-emphasis labels and carousel chrome. YAML `tokens.colors.disabled`.

#### Neutral & Surface

- **Pure White** (`#ffffff`): Page background, card surfaces, and text on red/dark. YAML `tokens.colors.canvas`. Also **On-Primary White** (`#ffffff`): Text/iconography on the red CTA and dark backgrounds. YAML `tokens.colors.on-primary`.
- **Surface Grey** (`#f8f8f8`): Cool-grey tint for header utility pills and segmented blocks (US). YAML `tokens.colors.surface`.
- **Surface Alt** (`#efefef`): Slightly firmer grey for inset fields and alternating bands (US). YAML `tokens.colors.surface-alt`.
- **Surface Grey (JP)** (`#f2f2f2`): The Japanese site's primary tinted surface for chips and panels. YAML `tokens.colors.surface-jp`.
- **Hairline** (`#e0e0e0`): Thin borders, dividers, and card outlines — the primary separation device in the near-shadowless system. YAML `tokens.colors.hairline`.

#### Secondary Accents (service / section theming)

- **eShop Green** (`#468254`): Commerce/store accent used sparingly on shop surfaces and availability states. YAML `tokens.colors.eshop-green`.
- **Accent Blue** (`#4b5cce`): Occasional section/service theming (online services), kept subordinate to red. YAML `tokens.colors.accent-blue`.
- **Accent Navy** (`#27455c`): Deep navy band/section background for immersive editorial moments. YAML `tokens.colors.accent-navy`.

### Spacing

YAML `tokens.spacing`: xs 4, sm 8, md 12, base 16, lg 24, xl 32, xxl 48. Body layout states a base unit of 8px, scaling 4px, 8px, 12px, 16px, 24px, 32px, 48px.

Treating that YAML scale and the 8px / 4 / 8 / 12 / 16 / 24 / 32 / 48 rhythm as recorded spacing in this packet rather than a universal spacing token for every unlisted control, treating unitless YAML steps as not converted into a required px suffix, treating component padding as staying with the components, and keeping `tokens.spacing.md` 12 unmerged from 12px card radius, `tokens.spacing.base` 16 unmerged from 16px body/Play-pill type, `tokens.spacing.lg` 24 unmerged from 24px CTA padding, `tokens.spacing.xl` 32 unmerged from 32px Play height, `tokens.spacing.xxl` 48 unmerged from 48px CTA height and 48px Play radius, and `tokens.spacing.sm` 8 unmerged from 8px primary-button radius, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

Notable: Primary CTAs use 24px horizontal padding with zero vertical padding (height-driven at 48px); utility pills use a compact 8px/12px.

| Step | Value | Token-set path |
|---|---:|---|
| xs | 4 | `tokens.spacing.xs` |
| sm | 8 | `tokens.spacing.sm` |
| md | 12 | `tokens.spacing.md` |
| base | 16 | `tokens.spacing.base` |
| lg | 24 | `tokens.spacing.lg` |
| xl | 32 | `tokens.spacing.xl` |
| xxl | 48 | `tokens.spacing.xxl` |

### Shape

YAML `tokens.rounded`: xs 2, sm 4, md 8, lg 12, pill 48, full 9999.

| Step | Value | Token-set path |
|---|---:|---|
| xs | 2 | `tokens.rounded.xs` |
| sm | 4 | `tokens.rounded.sm` |
| md | 8 | `tokens.rounded.md` |
| lg | 12 | `tokens.rounded.lg` |
| pill | 48 | `tokens.rounded.pill` |
| full | 9999 | `tokens.rounded.full` |

The following local-geometry reading, including 20px-utility-not-YAML-pill-48, 2-3px-JP-not-merged-into-xs-2, 50-percent-JP-circles-not-YAML-full-9999, 8px-not-12px-not-48px, and the Extra-small/Small/Medium/Large/Pill/Full role labels, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. 2px / 2-3px / 4px / 8px / 12px / 20px / 48px / `9999px` / 50% remain local harvested geometry, not a universal radius for every unlisted control. YAML `tokens.rounded.xs` 2 and JP 2-3px share a neighbourhood and are not merged into one number. YAML `tokens.rounded.pill` 48 is Play affordance and status pills; header utility pills are 20px and are not that key. YAML `tokens.rounded.full` 9999 is the Full step beside 48px; JP circular icon chips are 50% and are not that key.

Observed radii in the body:

- Extra-small (2px): JP chips, fine inset elements
- Small (4px): small containers, inline chips
- Medium (8px): primary buttons, carousel-arrow chips
- Large (12px): media/feature cards — the workhorse
- Pill (20px): header utility pills
- Full (48px / 9999px): Play affordance and status pills; 50% circular icon chips on JP

### Elevation

| Level | Treatment | Use |
|-------|-----------|-----|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Tint (Level 1) | `#f8f8f8` / `#f2f2f2` background shift | Card/section separation without elevation |
| Hairline (Level 2) | `1px solid #e0e0e0` border | Card outlines, dividers |
| Soft (Level 3) | `rgba(0,0,0,0.07) 0px 2px 8px` | Standard media tiles, subtle lift |
| Card (Level 4) | `rgba(72,72,72,0.15) 0px 4px 16px` | Featured / promoted tiles |

YAML `tokens.shadow.soft` is `rgba(0,0,0,0.07) 0px 2px 8px 0px`. YAML `tokens.shadow.card` is `rgba(72,72,72,0.15) 0px 4px 16px 0px`. Body Soft / Card shadows and `tokens.components.card-media.shadow` are the same strings without the trailing `0px`. Treating the table Use assignments (Page background, inline text, most surfaces; Card/section separation without elevation; Card outlines, dividers; Standard media tiles, subtle lift; Featured / promoted tiles) as source-stated uses in this reconstruction, and treating those trailing-`0px` and no-trailing-`0px` writings as unmerged records of the same harvested shadows, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

The following near-shadowless and shadow-philosophy readings, including whisper-soft-on-media-cards, featured-tiles-reaching-only-brand-warm-grey-shadow-rather-than-pure-black, Japanese-site-flatter-still, and emphasis-reached-for-with-the-red-accent-never-with-heavy-drop-shadows, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. **Shadow Philosophy**: Nintendo runs a near-shadowless, light system. Live inspection found the strongest standard elevation to be a whisper-soft `rgba(0,0,0,0.07) 0px 2px 8px` on media cards, with featured tiles reaching only `rgba(72,72,72,0.15) 0px 4px 16px` — itself a brand-warm grey shadow rather than pure black. Most separation is done flat, through `#f8f8f8` tint and `#e0e0e0` hairlines. The Japanese site is flatter still (tighter 2-3px radii, minimal shadow). This restraint keeps the interface feeling clean, fast, and approachable — emphasis is reached for with the red accent (`#e60012`), never with heavy drop shadows.

### Motion

Source-stated duration roles. The source HTML comment attaches live inspect to token-level claims in §1–9; §15 sits in the philosophy layer (sections 10–15) and is not in the live-inspect list. Treating the duration table, easing names, signature carousel/card-lift/bounce, and reduced-motion line as source-stated rather than computed CSS, treating omitted `ease-exit` as matching the legacy spec-template example, treating `ease-enter` / `ease-exit` / `ease-standard` as the nintendo/workday/barogo unattributed set rather than computed CSS, and treating the "friendly but composed" / "calm, image-forward aesthetic" readings as editorial, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 220ms | Card/section reveal, carousel slide, dropdown |
| `motion-slow` | 360ms | Page-level transitions, hero/banner reveals |

Source-stated easing names (uncomputed cubic-bezier omitted):

| Token | Curve | Use |
|---|---|---|
| `ease-enter` | omitted (unattributed cubic-bezier; source-stated name only) | Arriving — cards, sheets, carousel slides |
| `ease-exit` | omitted (unattributed cubic-bezier; source-stated name only; matches the legacy spec-template `ease-exit` example) | Dismissals |
| `ease-standard` | omitted (unattributed cubic-bezier; source-stated name only) | Two-way transitions |

The following motion-rule readings, including friendly-but-composed, matching-calm-image-forward, signature carousel-slide and card-lift, gentle-tasteful-bounce-acceptable-on-playful-interactive-moments, core-navigation-and-commerce-stay-steady-and-predictable, and reduced-motion-fully-functional, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. Motion is friendly but composed — consistent with the calm, image-forward aesthetic. Carousels slide at `motion-standard / ease-enter`; cards lift subtly on hover with the soft shadow deepening slightly. A gentle, tasteful bounce is acceptable on playful interactive moments (it's a games brand), but core navigation and commerce flows stay steady and predictable. Under `prefers-reduced-motion: reduce`, carousels and reveals collapse to instant; the experience remains fully functional.

Treating unattributed cubic-bezier curves as omitted rather than promoted, while keeping source-stated names and uses, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. Exact cubic-bezier curves are unattributed — `ease-standard`, `ease-enter`, and `ease-exit` match the nintendo/workday/barogo unattributed set, and `ease-exit` matches the legacy spec template — and remain omitted rather than promoted. Do not promote an easing curve, animation name, transition property, or a duration beyond the tables above until a later pass has recorded computed evidence of all five kinds per component: transition properties, animation name, duration, easing, and reduced-motion behavior. Official documentation of a single curve or duration is not that gate. Any exact animation curve remains a local extension until that per-component computed observation exists.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings, including Geologica-as-US-store-family, YakuHan-plus-Hiragino-as-JP-stack, nc3Jp-as-Nintendo-bundled-web-font-fallback, and system-ui-mono-not-a-brand-face, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Live computed surface-use | US store: **Geologica Variable** for headings, nav, body, and button labels. Japanese site: authentic CJK stack with YakuHan tightening half-width kana punctuation, Roboto covering Latin, Hiragino Kaku Gothic ProN rendering kanji/kana. |
| Official product-use | Unresolved in this packet. The live US and JP stacks below are surface-use, not a Nintendo-authored type specimen in the supplied sources. |
| Official distributed asset | `nc3Jp` is Nintendo's bundled web font fallback on the Japanese stack. Geologica Variable is the live US-store family in this capture. Hiragino Kaku Gothic ProN / `ヒラギノ角ゴ ProN W3` are named in the JP stack. |
| Declared fallback | US fallback `-apple-system, "system-ui", sans-serif` is a fallback stack, not Geologica. YAML `tokens.typography.family.mono` is `system-ui`; it is not a brand face. JP stack also names `Arial` and `sans-serif` as later fallbacks. |

The following font-use boundary, including Geologica-owns-the-US-store, YakuHan-plus-Hiragino-owns-the-Japanese-site, the-two-never-swap, a-system-fallback-as-not-Geologica, and `system-ui` mono as not a brand face, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. Do not replace Geologica Variable with the CJK stack on Western/US surfaces. Do not replace the CJK stack with Geologica on Japanese surfaces. Do not present `-apple-system`, `"system-ui"`, `Arial`, or YAML `family.mono` `system-ui` as the brand face.

### Family

- **US (sans):** `Geologica Variable` — YAML `tokens.typography.family.sans`. Used for all US-store headings, nav, body, and button labels. Fallback `-apple-system, "system-ui", sans-serif`.
- **JP (CJK):** `YakuHanJPs, Roboto, "Hiragino Kaku Gothic ProN", "ヒラギノ角ゴ ProN W3", Arial, nc3Jp, sans-serif` — the longer §3 writing. YAML `tokens.typography.family.cjk` is the shorter `YakuHanJPs, Roboto, Hiragino Kaku Gothic ProN`. Both writings stay; the longer stack is the complete form. YakuHan tightens half-width kana punctuation, Roboto covers Latin, Hiragino Kaku Gothic ProN renders kanji/kana; `nc3Jp` is Nintendo's bundled web font fallback.
- **Mono:** `system-ui` — YAML `tokens.typography.family.mono`. Not a brand face.

### Type roles

Verified YAML line-height values are the unitless ratios `1.35`, `1.40`, `2.00`, and `1.00`. YAML token-set roles: `tokens.typography.display`, `tokens.typography.subheading`, `tokens.typography.label`, `tokens.typography.body`, `tokens.typography.body-jp`, `tokens.typography.button`, `tokens.typography.nav`, `tokens.typography.caption`. The following ratio-versus-size-local reading, including keeping YAML `use` beside the longer §3 notes, keeping Nav Item (JP) 14px / 700 as a §3-only row rather than inventing a YAML key, and keeping rem / computed-px writings beside the unitless ratios, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. The unitless ratios scale with font size and are not fixed px.

| Role | Font | Size | Weight | Line height (YAML) | rem / px note | Use |
|---|---|---:|---:|---:|---|---|
| Feature Headline (H2) | Geologica | 28px | 600 | 1.35 | 1.75rem; 37.8px | YAML `display` use: "Section/feature headline (H2), Geologica". §3: Section/feature titles, US |
| Panel Heading (H2) | Geologica | 21px | 600 | 1.40 | 1.31rem; 29.4px | YAML `subheading` use: "Card / panel head (H2)". §3: Card / login panel heads |
| Section Label | Geologica | 16px | 600 | 1.40 | 1.00rem; 22.4px | YAML `label` use: "Section label, nav, eyebrow". §3: Eyebrow labels ("Trending topics"), nav groups |
| Body | Geologica | 16px | 400 | 1.40 | 1.00rem; 22.4px | YAML `body` use: "Standard reading text, US" |
| Body (JP) | CJK stack | 16px | 400 | 2.00 | 1.00rem; 32px | YAML `body-jp` use: "Standard reading text, JP (32px line-height)" |
| Primary Button | Geologica | 18px | 600 | 1.00 | 1.13rem | YAML `button` use: "Primary CTA label" |
| Nav Item | Geologica | 14px | 600 | 1.00 | 0.88rem | YAML `nav` use: "Top nav menu item". §3: Top nav menu triggers |
| Nav Item (JP) | CJK stack | 14px | 700 | 1.00 | — | §3 only (not a YAML typography key): Japanese top nav (heavier weight) |
| Caption / Input | Geologica | 14px | 400 | 1.40 | 0.88rem | YAML `caption` use: "Search input, small labels". §3: Search field, small labels |

The following type-rule readings, including weight-600-carries-headings-400-carries-prose, regional-font-discipline, generous-Japanese-line-height, and no-display-heroics-headlines-top-out-at-28px, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification.

- **Weight 600 carries headings; 400 carries prose.** Across US headings (28px, 21px, 16px labels) the heading weight is a consistent 600 semibold — confident but never heavy. Body sits at 400.
- **Regional font discipline.** Geologica owns the US store; the YakuHan + Hiragino CJK stack owns the Japanese site. The two never swap — each is tuned for its script's legibility.
- **Generous Japanese line-height.** JP body runs at 32px line-height on 16px text (2.0) versus 1.4 on the US site — denser scripts get more vertical air for readability.
- **No display heroics.** Headlines top out at 28px on the home surface; Nintendo leans on imagery and the red accent for impact rather than oversized type.

### Assets

Treating catalog logo metadata as a Google favicon lookup, not a captured first-party mark, and not promoting it as a portable mark file, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

Treating verified key-art banners, game/box-art, and media-tile imagery as not replaceable with invented brand-color decoration, and treating the wordmark as Nintendo red (`#e60012`) identity context rather than a harvested SVG file, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification. Let large key-art imagery carry visual energy so the UI chrome stays calm.

<!-- design-md:section components-states -->
## 4. Components & States

### Capture record

The source state contract, preserved here while the catalog graph is not adopted. Hex values and geometry in the harvested components remain source-stated. The source HTML comment places sections 10–15 in the philosophy layer; treating graph-not-adopted preservation of that contract, and treating the table below as that philosophy-layer contract rather than computed per-component CSS, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

| State | Treatment |
|---|---|
| **Empty (wish list / cart, none yet)** | White canvas. Single Ink Grey (`#484848`) line explaining nothing is saved yet, with one red CTA to browse games. No clutter, friendly tone. |
| **Empty (search, no results)** | Muted Grey (`#727272`) single line stating no matches, with suggestions or a path to browse. Calm, never a dead-end error. |
| **Loading (storefront fetch)** | Skeleton media tiles at final 12px-radius dimensions on `#f8f8f8` tint, flat soft pulse consistent with the near-shadowless system. |
| **Loading (in-place refresh)** | Previous content stays visible; a subtle red `#e60012` progress affordance signals activity without blocking the view. |
| **Error (page/network failure)** | Friendly Ink Grey message with plain-language explanation and a retry. No raw error codes; states what to do next. |
| **Error (form validation)** | Field-level message below the input describing what's valid, in a warm tone — not just "Required". |
| **Success (purchase / pre-order placed)** | Brief calm confirmation with order/next-step detail linked immediately below. Enthusiastic but not gimmicky. |
| **Skeleton** | `#f8f8f8` blocks at final dimensions, 12px radius, flat soft pulse — no shadow shimmer. |
| **Disabled** | Disabled Grey (`#c8c8c8`) text on reduced-opacity surface; red actions fade rather than turn a foreign grey, preserving brand read. |

Characterizations in that table such as “No clutter, friendly tone”, “Calm, never a dead-end error”, “flat soft pulse consistent with the near-shadowless system”, “without blocking the view”, “No raw error codes”, “not just Required”, “Enthusiastic but not gimmicky”, “no shadow shimmer”, and “red actions fade rather than turn a foreign grey, preserving brand read” are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification.

Declared interactive components still declare Core §4.4 applicability by control meaning, not by capture completeness. `default` and `focus-visible` apply. Pointer-web hover, and other canonical states that are meaningful for the control, remain applicable; their visual treatments are omitted unless captured as that same canonical state. Absence of a capture is not a `not-applicable` reason. Loading, error, and success follow the control’s product role, not its primitive kind. Where exact selector/label/request/outcome mapping is unresolved, those three applicability fields are omitted at this boundary rather than closed from the §14 rows. This is not a complete state-coverage claim.

The source records no generic Focus capture with a colour. The `focus-visible` rows do not carry a colour. A later generic Focus observation would not be `focus-visible` evidence. Source `motion-fast` names “Hover, button press, focus” as duration use; that duration row is not `focus-visible` treatment evidence.

Media Tile and Featured Card have no interactive-kind confirmation for a §4.4 map, so kind and a state-applicability map are omitted. Red Status Pill is a status marker: `kind: non-interactive` and a reason, not a state-applicability map. Primitive type is recorded only where YAML `tokens.components.*` names `type`. Featured Card and Red Status Pill are not in the token set.

Every interactive-kind verdict, every applicability verdict, the reason given for either, every unmerged-field reading on the controls below (including YAML `use` beside longer §4 Use, YAML padding `0 24px` beside `0px 24px`, YAML font `18px / 600 Geologica` beside `18px Geologica weight 600`, 20px utility radius as not YAML `rounded.pill` 48, 48px US nav row as not 72px JP header, `#dadada` as carousel chrome rather than a color token, and `rgba(0,0,0,0.3)` as this arrow’s scrim rather than a YAML color), and treating Featured Card / Red Status Pill as not in the token set, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Primary CTA

- Role: primary call-to-action — "Log in", "Learn more", "Start shopping", "See all news articles"
- Kind: interactive
- Primitive type: `button`
- Anatomy: label
- Background: `#e60012`
- Text: `#ffffff`
- Radius: 8px
- Padding: YAML `0 24px`; §4 `0px 24px`
- Height: 48px
- Font: YAML `18px / 600 Geologica`; §4 `18px Geologica weight 600`
- Use: YAML `Primary CTA — Log in, Learn more, Start shopping`. §4 also names "See all news articles"
- Observed: default only
- YAML `tokens.components.button-primary`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the primary CTA |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A primary action can be unavailable; visual treatment omitted. Source §14 Disabled says red actions fade rather than turn a foreign grey; that row is not copied here as a computed paint |

Loading, error, and success applicability are omitted. Source names this control as a mixed primary CTA ("Log in", "Learn more", "Start shopping", "See all news articles") and records §14 Loading (in-place refresh) / Success (purchase / pre-order placed) as philosophy-layer rows; exact selector/request/outcome mapping is unresolved, so those three fields stay omitted at this boundary rather than closed from the §14 rows.

### Play Pill

- Role: inline "Play" affordance on media cards — fully pill-shaped
- Kind: interactive
- Primitive type: `button`
- Anatomy: label
- Background: `#e60012`
- Text: `#ffffff`
- Radius: 48px
- Padding: YAML `0 16px`; §4 `0px 16px`
- Height: 32px
- Font: YAML `16px / 600 Geologica`; §4 `16px Geologica weight 600`
- Use: YAML `Inline Play pill on media cards`. §4: Inline "Play" affordance on media cards — fully pill-shaped
- Observed: default only
- YAML `tokens.components.button-play`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the inline Play pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A Play affordance can be unavailable; visual treatment omitted |
| loading | not-applicable | The pill is an inline Play affordance on a media card; it does not commit a store operation on itself |
| error | not-applicable | The pill does not report a request or validation failure on itself |
| success | not-applicable | The pill does not complete an operation-outcome on itself |

### Utility Pill

- Role: header utility actions — "Search", "Wish List", "Cart"
- Kind: interactive
- Primitive type: `button`
- Anatomy: label
- Background: `#f8f8f8`
- Text: `#484848`
- Radius: 20px
- Padding: `8px 12px`
- Height: 32px
- Font: YAML `14px / 600 Geologica`; §4 `14px Geologica weight 600`
- Use: YAML `Header utility pills — Search, Wish List, Cart`
- Observed: default only
- YAML `tokens.components.button-utility`
- Field note: Radius 20px is this control; it is not YAML `rounded.pill` 48 and not YAML `rounded.full` 9999.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header utility pill |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A utility action can be unavailable; visual treatment omitted |
| loading | not-applicable | Search, Wish List, and Cart are header destinations / openers; the pill itself does not enter a loading state |
| error | not-applicable | The pill’s meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | The pill’s meaning is destination, not action-outcome confirmation |

### Red Inline Link

- Role: trending-topic and inline navigational links ("Nintendo Direct", "Games on sale") — red text on transparent, no background
- Kind: interactive
- Primitive type: `button`
- Anatomy: label
- Text: `#e60012`
- Font: YAML `16px / 600 Geologica`; §4 `16px Geologica weight 600`
- Use: YAML `Trending-topic / inline navigational link, red text on transparent`
- Observed: default only
- YAML `tokens.components.link-red`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the red inline link |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A navigational link can be unavailable; visual treatment omitted |
| loading | not-applicable | A destination link does not enter a loading state on itself |
| error | not-applicable | Nav meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | Nav meaning is destination, not action-outcome confirmation |

### Carousel Arrow

- Role: hero/product carousel previous & next controls — dark scrim chip over imagery
- Kind: interactive
- Primitive type: `button`
- Anatomy: control
- Background: `rgba(0,0,0,0.3)`
- Text: `#dadada`
- Radius: 8px
- Padding: 16px
- Use: YAML `Hero carousel prev/next, rgba(0,0,0,0.3) scrim bg`
- Observed: default only
- YAML `tokens.components.carousel-arrow`
- Field note: `#dadada` is this control’s text and is named with Disabled Grey `#c8c8c8` as carousel chrome in §2; it is not a YAML `tokens.colors.*` key. `rgba(0,0,0,0.3)` is this arrow’s scrim and is not a YAML color token.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the carousel prev/next control |
| hover | applicable | Pointer-web arrow; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A carousel arrow can be unavailable at the end of a set; visual treatment omitted |
| loading | not-applicable | An arrow advances a carousel; the arrow itself does not enter a loading state |
| error | not-applicable | The arrow commits no operation that fails on itself |
| success | not-applicable | The arrow commits no operation-outcome on itself |

### Header Search

- Role: header search field ("Search games, hardware, news, etc") — bottom-border affordance, no fill
- Kind: interactive
- Primitive type: `input`
- Anatomy: value field
- Background: `#ffffff`
- Text: `#484848`
- Radius: 0px
- Font: YAML `14px / 400 Geologica`; §4 `14px Geologica weight 400`
- Use: YAML `Header search field, bottom-border affordance`
- Observed: default only
- YAML `tokens.components.input-search`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the header search field |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A search field can be unavailable; visual treatment omitted |
| loading | not-applicable | The field holds a query; it does not commit a transaction |
| error | applicable | Form field; visual treatment omitted. Source §14 Error (form validation) is philosophy-layer and is not copied here as a computed paint |
| success | not-applicable | The field does not complete an operation on itself |

### Header / Navigation band

- Role: header/navigation band (parent surface)
- Anatomy: surface
- Background: `#ffffff`
- Height: 48px nav row (US) / 72px header (JP)
- Use: White header behind top nav items
- Observed: default only
- Field note: `#ffffff` is this band’s fill and Canvas. YAML nav-item records fg/font/active/use only; this header fill is the parent surface, not a Nav Item Background. 48px is the US nav row; 72px is the JP header. They are not merged.

### Nav Item

- Role: top horizontal nav trigger ("Explore", "Shop", "Support" on US; "本体・グッズ", "ゲームソフト", "トピックス", "サポート" on JP)
- Kind: interactive
- Primitive type: `tab`
- Anatomy: label
- Text: `#484848`
- Font: YAML `14px / 600 Geologica`; §4 `14px Geologica weight 600`. Japanese top nav is §3 `14px` / `700` / CJK stack
- Active: red `#e60012` text/indicator on active item. YAML `active: "text #e60012"`
- Use: YAML `Top nav menu trigger (Explore, Shop, Support)`
- Observed: static default and static active appearances. The active appearance is a captured variant, not an observed click transition.
- YAML `tokens.components.nav-item`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Harvested as the top nav menu trigger |
| hover | applicable | Pointer-web nav item; visual treatment omitted |
| focus-visible | applicable | Interactive control; visual treatment omitted |
| disabled | applicable | A nav item can be unavailable; visual treatment omitted |
| loading | not-applicable | A top nav item selects a destination; the item itself does not enter a loading state |
| error | not-applicable | Nav meaning is destination versus resting, not a request or validation failure on the item |
| success | not-applicable | Nav meaning is destination, not action-outcome confirmation |

Additional observed named state: static active appearance (text `#e60012`). Treating that appearance as a captured variant rather than an observed click transition is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Media Tile

- Role: game / topic / news media tile — the workhorse content card
- Primitive type: `card`
- Anatomy: surface
- Background: `#ffffff`
- Radius: 12px
- Shadow: YAML `rgba(0,0,0,0.07) 0px 2px 8px` (`tokens.components.card-media.shadow`); Foundations Soft also writes `rgba(0,0,0,0.07) 0px 2px 8px`; YAML `tokens.shadow.soft` adds trailing `0px`
- Use: YAML `Game/topic media tile`
- Observed: default only
- YAML `tokens.components.card-media`

No interactive-kind evidence is given for this surface. Kind and a state-applicability map are omitted. Treating this surface as having no interactive-kind evidence for a §4.4 map, so that kind and a state-applicability map stay omitted, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Featured Card

- Role: elevated featured/promoted tile — firmer lift than the standard media tile
- Primitive type: not in the token set
- Anatomy: surface
- Background: `#ffffff`
- Radius: 12px
- Shadow: `rgba(72,72,72,0.15) 0px 4px 16px`
- Use: Elevated featured/promoted tile — firmer lift than the standard media tile
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.

No interactive-kind confirmation is given for a §4.4 map. Kind and a state-applicability map are omitted. Treating this tile as having no interactive-kind confirmation for a §4.4 map, so that kind and a state-applicability map stay omitted, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Red Status Pill

- Role: emphasis / status pill (e.g. "New", availability) — the red action color in compact form
- Primitive type: not in the token set
- Kind: non-interactive — a status marker, not a control
- Anatomy: label
- Background: `#e60012`
- Text: `#ffffff`
- Radius: 48px
- Font: 14px Geologica weight 600
- Use: Emphasis / status pill (e.g. "New", availability) — the red action color in compact form
- Observed: default only
- YAML `tokens.components` does not record this control; values are body §4 only. Type is not invented.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

YAML spacing is xs 4, sm 8, md 12, base 16, lg 24, xl 32, xxl 48. Body layout repeats an 8px base unit, scaling 4px, 8px, 12px, 16px, 24px, 32px, 48px. Primary CTAs pad horizontally at 24px with zero vertical padding (height-driven at 48px); utility pills pad at 8px/12px.

The “calm, breathable rhythm”, “flat segmentation”, and “imagery does the heavy lifting” readings in this section, including content-and-commerce-dense-product-feeling-airy and chrome-staying-quiet-and-orderly, are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification.

Recorded layout:

- Centered, generous-margin content column with full-width hero/carousel banners at the top
- Trending-topics and feature sections arranged as horizontal media-tile rows / carousels
- Feature bands alternate white (`#ffffff`) with subtle grey tint, separated by `#e0e0e0` hairlines
- Media tiles use 12px radius and group games, news, and topics
- **Calm, breathable rhythm**: despite being a content- and commerce-dense product, the home surface is airy with generous vertical spacing between sections.
- **Flat segmentation**: sections separate by background tint and hairline, rarely by heavy elevation.
- **Imagery does the heavy lifting**: large key-art banners carry visual energy so the chrome can stay quiet and orderly.

Source breakpoint table:

| Name | Width | Key Changes |
|------|-------|-------------|
| Mobile | <640px | Single column, condensed nav (hamburger), carousels become swipe rows |
| Tablet | 640-1024px | 2-up media tiles, moderate padding |
| Desktop | 1024-1440px | Full layout, multi-column feature rows, centered content with margins |

Collapsing strategy recorded in the source: Hero/carousel banners maintain full-width treatment, reduce internal padding on mobile; Feature media-tile rows: multi-column → 2-up → single-column swipe; Top nav: horizontal menu → hamburger toggle; Tinted/white alternating bands maintain full-width treatment.

Image behavior: Key-art banners and media tiles maintain 12px radius across breakpoints. Card shadows stay whisper-soft at all sizes, consistent with the flat system. Game/box-art imagery scales to fit tile aspect ratios without added borders.

Touch-target record: Primary CTAs at 48px height for a comfortable, unmistakable tap target; Header utility pills at 32px height with 8px/12px padding; Nav items spaced within the 48px (US) / 72px (JP) header; Carousel arrows padded at 16px for easy reach over imagery.

Treating that table as a recorded span of named widths, not a complete specification of every unlisted control, treating the 48px / 32px / 72px / 16px figures as surface measurements in this packet rather than universal layout tokens, treating the touch-target record as a purpose reading of those measurements rather than a complete target-size specification, treating collapsing / image-behavior rows as source-stated rather than a cross-viewport specification, and treating image-behavior as consistent-with-the-flat-system rather than a new elevation rule, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Observed (live surfaces, 2026-06-17)

The live strings below are source-stated. Treating the parenthetical characterizations (experiential invitation / inclusive, family-first / plain, orderly navigation) as citation-character of those live strings is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

- "Experience The World's Game like never before" — feature headline (experiential invitation). *(verified live)*
- "Get up and move with friends and family" — feature headline (inclusive, family-first). *(verified live)*
- "Trending topics" — section label (plain, orderly navigation). *(verified live)*
- Control and section strings from harvested surfaces: "Log in"; "Learn more"; "Start shopping"; "See all news articles"; "Play"; "Search"; "Wish List"; "Cart"; "Nintendo Direct"; "Games on sale"; "Explore"; "Shop"; "Support"; "本体・グッズ"; "ゲームソフト"; "トピックス"; "サポート"; "Search games, hardware, news, etc"; "New"; "Online services"; "Pre-order now!"; "Available now!"; "Pre-order now! Blast off on a high…"

Treating the §14 empty/loading/error/success strings as part of the state contract, not extra Observed voice samples, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

### Derived editorial voice

The following voice reading, context table, and forbidden-register list — including warm-inviting-and-family-friendly, playful-where-it-persuades-plain-and-orderly-where-it-informs, welcoming-and-benefit-first, gentle-imperatives, rare-exclamation-as-genuine-enthusiasm-rather-than-urgency-marketing, and register-stays-clear-optimistic-and-never-condescending-or-jargon-heavy — are a derived editorial implementation inference from the verified surfaces; they are not Nintendo-authored or a separately published UI specification. They are not the Observed strings above.

Nintendo's voice is **warm, inviting, and family-friendly** — playful where it persuades, plain and orderly where it informs. Copy is welcoming and benefit-first ("Experience The World's Game like never before", "Get up and move with friends and family") without slipping into hype or pressure. Calls-to-action are gentle imperatives — "Learn more", "Start shopping", "Pre-order now!" — and the rare exclamation reads as genuine enthusiasm rather than urgency-marketing. The brand speaks to a broad audience (kids, parents, lifelong fans) at once, so the register stays clear, optimistic, and never condescending or jargon-heavy.

| Context | Tone |
|---|---|
| Hero / feature headlines | Inviting, experiential. "Experience The World's Game like never before." Enthusiastic, never hard-sell. |
| CTAs | Gentle imperatives. "Learn more", "Start shopping", "See all news articles". |
| Trending topics / links | Plain and direct. "Nintendo Direct", "Games on sale", "Online services". |
| Product / availability | Benefit-first, friendly. "Pre-order now! Blast off on a high…", "Available now!". |
| Support / account | Calm, reassuring, helpful. Plain instructions over marketing language. |

**Forbidden register**: fear-based or urgency-manipulation marketing, dense industry jargon left unexplained, edgy/exclusionary humor, condescension toward younger players, exclamation-stacked hype.

US and JP are the two captured locales in this packet. US copy and Geologica / `#484848` / line-height 1.40 sit on `nintendo.com/us`. JP copy, the CJK stack, `#3c3c3c`, line-height 2.00 (32px on 16px), nav weight 700, and nav labels "本体・グッズ", "ゲームソフト", "トピックス", "サポート" sit on `nintendo.com/jp`. Treating that regional pairing as locale behavior recorded in this packet rather than as a complete localization specification for every unlisted string, is a derived editorial implementation inference from the verified surfaces; it is not Nintendo-authored or a separately published UI specification.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- exact cubic-bezier values for `ease-enter`, `ease-exit`, and `ease-standard` (omitted as unattributed; source-stated names and uses remain in Foundations Motion)
- loading, error, and success applicability on Primary CTA (omitted at the mixed-role boundary rather than closed from the §14 rows)
