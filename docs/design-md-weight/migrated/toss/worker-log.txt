Toss 레거시 DESIGN.md를 Core v2로 옮기겠습니다. 먼저 세션 상태를 복원하고, 지정된 규격·규칙집·골든 샘플을 순서대로 읽겠습니다.세션은 T2 웨이브 산출을 `docs/design-md-weight/migrated/`에만 두는 상태입니다. 지정된 규격과 규칙집을 순서대로 읽겠습니다.규칙집 v2와 의미 보존 조건을 확인했습니다. 개정된 musinsa 골든 샘플과 Toss 원본, 최근 통과한 Notion 이관본을 대조하겠습니다.이미 `migrated/toss/`가 있습니다. 골든 샘플·게이트·기존 산출을 함께 읽어 형태와 검증 조건을 맞추겠습니다.게이트 검증 로직과 Notion 통과본의 상태 표를 읽어, Toss 컴포넌트에 같은 판정 방식을 적용하겠습니다.규칙과 골든 샘플 형태를 반영해 Toss Core v2 세 파일을 작성합니다. 값 손실·상태 의미 반전·§9 고유값 삭제를 피하겠습니다.작성한 산출을 Portable Core 검사와 마이그레이션 게이트로 검증합니다.Apple 웨이브 1이 마케팅 CTA의 loading/error/success를 역할 기준으로 어떻게 닫았는지 대조합니다. 같은 판정을 Toss에 맞추겠습니다.Toss 레거시 DESIGN.md를 Core v2 Portable Core로 옮겼습니다. 산출은 `docs/design-md-weight/migrated/toss/`에만 있고, `web/references/toss/`는 건드리지 않았습니다.

검증: `inspectDesignMd` `portable_core: true`, placeholders 0, `migrate-reference.mjs --brand toss --gate-only` **PASS**, hex/px 손실 0·발명 0. 규칙집 **v2**.

의미 처리 요지:

| 규칙 | Toss에서의 처리 |
|---|---|
| A2·§14 | 4행 상태 표를 Components 본문에 보존. graph 위임 없음 |
| A3·§9 | 도구 프롬프트 삭제. 수치는 이미 Components에 있음. “미수록 컴포넌트는 extension”만 Experience로 이동 |
| A4 | 카탈로그 `#0064ff` ≠ UI primary `#3182f6`. 마케팅 weak pair ≠ 제품 Primary |
| B1 | Text Field generic `focus`, Button keyboard focus를 `focus-visible` 색값으로 올리지 않음 |
| B2 | §12 “derived… not quoted corporate doctrine” 한정을 portable 본문에 유지 |
| B3 | 모션 비승격 + official component source 검증 전 local extension 조건을 Foundations에 유지 |
| C1 | `hover not captured`는 증거 사실로 남기고, hover는 `applicable` (treatment 생략) |
| C2 | TDS Button error/success, Text Field loading/success, Agreement·두 marketing CTA loading/error/success는 역할 의미로 `not-applicable` |
| C4 | Badge는 `non-interactive`, applicability map 생략 |
| D1 | storefront 등 원본 밖 부정 claim 없음 |
| D2 | §13 first-party product contexts만 primary-tasks로 흡수. 가상 persona sidecar 없음 |

DONE migrated=1

## Revision 2026-08-23 (wave1 sol resubmit)

규칙집 v3. FAIL `docs/reviews/t2-1-wave1-2026-08-23-sol-sample.md`.

- B3: Foundations Motion에 transition properties · animation name · duration · easing · reduced-motion behavior와 컴포넌트별 computed 관측 게이트 전문. 약화 문구 폐기.
- E2c: migration-log §15를 실제 본문과 일치.
- E2a: catalog favicon identity-only. portable Assets에서 제거. provenance만 유지.

inspectDesignMd portable_core true, placeholders 0. --gate-only PASS.

DONE revised=1
