# Lablup Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

Lablup (래블업) is the Korean AI-infrastructure company behind Backend.AI. This contract covers two first-party web surfaces inspected live: the corporate homepage at `https://www.lablup.com`, and the Backend.AI documentation at `https://docs.backend.ai/`. The official GitHub organization at `https://github.com/lablup` is recorded as the open-source Backend.AI and logo-mark source; it does not contribute interface tokens. Treating those two inspected pages as this contract's token surfaces, and keeping the GitHub organization as a logo-and-ownership source rather than a third token surface, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

The homepage states the brand line "Make AI infrastructure accessible" plainly. The defining recorded move is a deep, near-black teal (`#002926`) used as the brand canvas: the body background, the immersive hero, the footer, and the primary pill CTA. Headlines and body text are pure black (`#000000`) on light bands and pure white (`#ffffff`) on dark bands. A single saturated emerald (`#28ab6c`) is reserved for interactive accents — active states, link emphasis, focus borders, and the consent "Accept All" action. Calling that canvas the defining recorded move, calling it a forest-dark green-blue, and calling the palette engineered, trustworthy, serious infrastructure rather than a consumer toy, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification. The hex values and the reserved uses are recorded observations.

Display headlines run in **Google Sans** at heavy weights — 80px / weight 700 on the hero ("Making AI possible where it wasn't"), 52px / 700 for major section titles, 36px / 600 for section heads — while body text is 16px / 400 with `PyeojinGothic` and `Pretendard` as the Korean-optimized fallback stack. The documentation surface uses `Pretendard, Poppins` and introduces the product's signature link accent, a bright cyan (`#03b5e5`). The marketing brand is teal-and-emerald; the product/docs brand layers in cyan. Those two stacks and that hue split are the source's own. Calling the typographic personality geometric and quietly bold is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

Marketing CTAs are full pills (`999px`) — a deep-teal pill, a black pill, a white-on-dark pill, and ghost outline pills with `rgba(255,255,255,0.3)` borders — while utility chrome (cookie consent, docs) drops to a sharp 4px / 2px scale. Content cards use 24px and 18px radius in three temperatures: light grey (`#f3f3f3`), near-black (`#1a1a1a`), and brand teal (`#002926`). Live inspection found `box-shadow: none` across the hero, nav, cards, and CTAs. Separation comes from flat surface tints (`#fafafa`, `#f3f3f3`) and thin `#e5e5e5` hairlines. The neutral text ladder runs `#000000` → `#606060` → `#929292`, with mint `#badba3` and dimmed grey `#383838` on dark sections. Calling the geometry split and the near-shadowless treatment the distinguishing move, and calling the result a flat, modern, infrastructure-grade aesthetic, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

Lablup Inc. (래블업) was founded in **2015** in Seoul by **Jeongkyu Shin (신정규, CEO)**, **Joongi Kim (CTO)**, and **Jonghyun Park** — a team the source describes as largely current and former lab researchers frustrated by the repetitive technical hurdles of running computation in research environments. Their product is **Backend.AI**, an open-source platform that turns GPU and accelerator complexity into operational simplicity, scaling from a desktop AI workload up to frontier-model training and large-scale inference. The stated mission on the homepage is to **make AI accessible and scalable** — the same "Make AI infrastructure accessible" line. A decade on (the company marked its 10-year anniversary in 2025), Lablup positions Backend.AI as "The Operating System for AI Infrastructure," with a product family that spans **Backend.AI FastTrack** (MLOps pipelines), **Backend.AI:GO / AI:GO** (personal and desktop AI), and **Sovereign AI** infrastructure for national and consortium-scale deployments. CEO Jeongkyu Shin — a POSTECH physics Ph.D and Google Developer Expert in AI and Cloud — frames the work as untangling infrastructure so researchers and companies can focus on the AI itself. The brand keeps a Korean headquarters in Gangnam, Seoul and a US office in San Jose, and in 2026 was recognized with a Presidential Commendation at Korea's ICT Merit Awards. Founder names, founding year, the 10-year anniversary, and executive background are widely documented public facts in the source's own closing note, not quotations from a single verified Lablup statement in that turn. HQ Gangnam, the San Jose office, and the 2026 commendation are stated on the live homepage. None of that narrative supplies interface tokens. Reading the lab-researcher origin as the reason Backend.AI exists, and reading "untangling so researchers can focus on the AI" as a design implication, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

The source also reads what the design refuses and what it embraces: it refuses "the heavy, intimidating chrome and stock-photo gloss of legacy enterprise IT, and the hype-driven aesthetics of consumer AI"; it embraces "a flat, fast, infrastructure-grade interface; a serious deep-teal canvas with a single living-green accent; plainspoken engineering copy; and open source as a first-class commitment (Backend.AI lives in the open on the official GitHub org)." That refuses/embraces pairing is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

Two evidence domains stay separate. A marketing measurement (999px pill, `#002926` canvas, Google Sans 80px / 700) is not a docs token. A docs measurement (`Pretendard, Poppins`, cyan `#03b5e5`) is not a marketing value. The source's own token note makes that split. Keeping those two domains unmerged — a marketing measurement is not a docs token, a docs measurement is not a marketing value — is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=3 lang=en -->
### Primary tasks

Selecting these three as the product's primary tasks, and classifying them as surface-or-control outcomes rather than fictional biographies, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification. Each names a surface or label the source records. They do not come from the source's persona section.

- Act on the homepage CTAs "Contact Us", "About Lablup", and "View all partners".
- Scan the Product / News / Careers shortcut cards and the news carousel.
- Read Backend.AI documentation at `https://docs.backend.ai/` (TOC and inline links).
<!-- design-md:claim-end -->

### Audience

No named or demographic personas are invented. The source labels its named figures as fictional archetypes informed by publicly observable Lablup/Backend.AI user segments, not individual people, so those biographies are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records at a group level is: ML platform engineers, research-lab infra leads, enterprise AI teams. Dropping the fictional biographies rather than promoting them, carrying no name, age, city, motivation, or affiliation classification, and reading those groups as this product's audience, are derived editorial implementation inferences from the verified surfaces; they are not Lablup-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; classifying the list as that restatement, and the groupings and the readings inside them, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

- Deep near-black teal (`#002926`) as the signature brand canvas — body, hero, footer, primary pill
- Single saturated emerald (`#28ab6c`) reserved for interactive accents and links
- Backend.AI product cyan (`#03b5e5`) as the docs/product secondary accent
- Google Sans display at heavy weight (80px / 700 hero) over Pretendard body (16px / 400)
- Pill-everything marketing geometry (`999px`) vs sharp 4px/2px utility chrome
- Three-temperature cards — light `#f3f3f3`, near-black `#1a1a1a`, teal `#002926` — at 24px/18px radius
- Flat depth: no shadows; `#fafafa`/`#f3f3f3` tints + `#e5e5e5` hairlines do the separating
- Cool neutral text ladder `#000000` → `#606060` → `#929292`, with mint `#badba3` on dark

### Principles

These 5 items are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification. The source states them in its own Principles section together with the UI implication it draws from each. The source's closing note names "one action, one green" and "untangle, don't decorate" as editorial readings connecting observed design to stated positioning.

1. **Access over abstraction-for-its-own-sake.** The mission is to make AI infrastructure reachable. *UI implication:* lead with concrete capability and a clear next step (Contact / Explore); never gate basic understanding behind a sales wall.
2. **Untangle, don't decorate.** Complexity is the enemy; software is the solution. *UI implication:* flat surfaces, no shadow theater — separation by color temperature and hairlines, so the interface itself feels untangled.
3. **One action, one green.** Emerald (`#28ab6c`) means "do this / this is live." *UI implication:* reserve the saturated accent for interactive states and the primary action so the next step is unambiguous.
4. **Serious where it matters, calm everywhere else.** Deep teal signals infrastructural weight. *UI implication:* one loud typographic moment (the 80px hero), then a calm, functional register throughout.
5. **Built together, in the open.** Ecosystem partners and open source are core. *UI implication:* surface partners and code links as first-class content, with collaborative, humble copy.

### Application rules

The source states these eight as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification.

- Use deep teal (`#002926`) as the signature brand canvas for hero, footer, and immersive bands
- Reserve emerald (`#28ab6c`) for interactive accents, links, and active states — keep it the single "action" green
- Set marketing CTAs as full pills (`999px`) — teal, black, white-inverse, or ghost outline
- Use Google Sans at heavy weight (700) for the hero, stepping down to 600 for section heads
- Separate sections with flat surface temperature and `#e5e5e5` hairlines, not shadows
- Use the three card temperatures (light `#f3f3f3`, dark `#1a1a1a`, teal `#002926`) at 24px radius
- Keep the Backend.AI cyan (`#03b5e5`) scoped to product/docs surfaces
- Use white (`#ffffff`) text and the white inverse pill on the deep-teal dark bands

### Avoid

The source states these eight as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification.

- Use drop shadows for elevation — Lablup is a flat, shadow-free system
- Spread emerald across many elements — it dilutes the single-action signal
- Mix the docs cyan (`#03b5e5`) into the marketing brand — teal-and-emerald owns marketing
- Use sharp/square corners on marketing CTAs — they are always full pills
- Set the hero in a light weight — display is heavy Google Sans (700)
- Add a third saturated accent — the palette is teal + emerald (+ scoped product cyan)
- Use pure black bands where a deep-teal (`#002926`) band is the brand-correct dark
- Drop pill radius to a rounded rectangle on primary marketing CTAs

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Role names below are the source's own labels. Pairing each hex to that role, keeping marketing teal-and-emerald off the docs cyan, keeping `#000000` as both Ink Black text and the black-pill fill rather than merging those jobs, and reading emerald as the system's single live/action green and as the brighter facet of the isometric logo mark, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification. The hex values and recorded uses are the source's own.

**Primary & brand**

- **Lablup Emerald** (`#28ab6c`): The saturated interactive accent — active language toggle, link emphasis, focus borders, and the consent "Accept All" action. Token-set path `tokens.colors.primary`. The source also calls it the system's single "live/action" green and the brighter facet of the isometric logo mark; that character reading is covered by the qualification above.
- **Deep Teal** (`#002926`): The signature brand canvas. Body background, immersive hero, footer, and the primary pill CTA. Token-set path `tokens.colors.teal`.
- **Backend.AI Cyan** (`#03b5e5`): The product/documentation accent on `docs.backend.ai` — TOC links, inline links, and code references. Token-set path `tokens.colors.cyan`. The one secondary hue, scoped to the product surface.

**Neutral & surface**

- **Pure White** (`#ffffff`): Light-band background, white pill CTA, and text on teal/dark surfaces. Token-set path `tokens.colors.canvas`.
- **Off-White** (`#fafafa`): Subtle alternating section surface for light bands. Token-set path `tokens.colors.surface`.
- **Surface Grey** (`#f3f3f3`): Light feature/shortcut card and blog card background. Token-set path `tokens.colors.surface-grey`.
- **Hairline** (`#e5e5e5`): Thin card outlines and dividers — the primary separation device in a shadow-free system. Token-set path `tokens.colors.hairline`.
- **Dark Card** (`#1a1a1a`): Near-black background for the dark feature/news card. Token-set path `tokens.colors.dark-card`.
- **Pure Black** (`#000000`): Primary text on light bands and the black pill CTA. Token-set path `tokens.colors.ink`.

**Text hierarchy**

- **Ink Black** (`#000000`): Primary headings and body text on light surfaces. Same hex as `tokens.colors.ink`; the text role is not merged with the black-pill fill job.
- **Muted Grey** (`#606060`): Secondary text, captions, supporting copy. Token-set path `tokens.colors.muted`.
- **Faint Grey** (`#929292`): Tertiary text, metadata, lowest-emphasis labels. Token-set path `tokens.colors.faint`.
- **Dark Grey** (`#383838`): Inactive utility labels (e.g. unselected language option). Token-set path `tokens.colors.dark-grey`.
- **Mint** (`#badba3`): Soft light-green secondary text used on the deep-teal dark sections. Token-set path `tokens.colors.mint`.

### Spacing

Token-set keys from the source, kept as separate steps (`md: 14` is not `base: 16`). Keeping those steps unmerged is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

- `tokens.spacing.xs`: 4
- `tokens.spacing.sm`: 8
- `tokens.spacing.md`: 14
- `tokens.spacing.base`: 16
- `tokens.spacing.lg`: 24
- `tokens.spacing.xl`: 32
- `tokens.spacing.xxl`: 40
- `tokens.spacing.section`: 64

The source's spacing scale, as §5 writes it in px: 4px, 8px, 14px, 16px, 24px, 32px, 40px, 64px.

The source states a base unit of ~8px with 4px micro-steps, and notes that pill CTAs use generous asymmetric horizontal padding (24-40px) and that cards pad at 20-32px. Calling the ~8px figure a named base rather than converting the 14px step into it, and calling the pill padding generous, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

### Shape

Token-set keys from the source, kept as separate steps (`sm: 4` is not `xs: 2`; `lg: 18` is not `xl: 24`; `pill: 999` is not the carousel's `50%`). Keeping those radius steps unmerged from each other and from the carousel's `50%` is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

- Micro (`tokens.rounded.xs`): 2 — fine utility elements
- Small (`tokens.rounded.sm`): 4 — consent/utility buttons
- Medium (`tokens.rounded.md`): 8 — inner utility containers
- Large (`tokens.rounded.lg`): 18 — blog link cards
- XL (`tokens.rounded.xl`): 24 — feature/shortcut cards. The source calls this step the workhorse.
- Pill (`tokens.rounded.pill`): 999 — all marketing CTAs; 50% for circular carousel controls

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Tint (Level 1) | `#fafafa` / `#f3f3f3` surface shift | Card and section separation without elevation |
| Hairline (Level 2) | `1px solid #e5e5e5` border | Blog/link card outlines, dividers |
| Band (Level 3) | Deep-teal `#002926` full-width band | Immersive contrast section (hero, CTA, news) |

The token-set shadow value is `tokens.shadow.none`: `none`. Live inspection found `box-shadow: none` across the hero, nav, cards, and CTAs.

The following elevation reading is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification. Lablup is a near-shadowless system. Depth and grouping are communicated through flat surface temperature (light `#f3f3f3` vs near-black `#1a1a1a` vs brand `#002926`) and thin `#e5e5e5` hairlines, not elevation. When emphasis is needed the system reaches for color — the emerald `#28ab6c` accent or a full deep-teal band — rather than a drop shadow.

### Motion

The source attributes its token-level claims to a live inspection of computed color, type, geometry, border, and shadow on the two surfaces. The motion contract below sits outside that attribution: the sibling verification file records no transition, animation, duration, or easing observation on either surface. The durations, easing roles, and motion rules below are therefore a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification.

Durations:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, button press, focus |
| `motion-standard` | 220ms | Card/section reveal, carousel slide, dropdown |
| `motion-slow` | 360ms | Page-level transitions, hero word swap |

Easing roles — three roles with declared uses. The specific curve values the source lists are not traceable to Lablup evidence, so the curves are omitted here and only the roles and their uses are kept:

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, sheets, carousel pages |
| `ease-exit` | Dismissals |
| `ease-standard` | Two-way transitions |

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or specification document — does not satisfy that condition.

Motion rules, as the source states them:

- Motion is functional and steady — consistent with the flat, infrastructure-grade aesthetic.
- The hero's animated word swap ("possible / where it wasn't") cycles slowly; the news carousel pages with `motion-standard / ease-enter`; pills respond to press with a subtle opacity/scale shift.
- No bounce or spring — an infrastructure product signals reliability, not playfulness.
- Under `prefers-reduced-motion: reduce`, the hero word animation freezes, the carousel becomes instant paging, and all transitions collapse to instant; the product remains fully functional.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The rows below sort this record's font evidence into classes. The sorting, treating `PyeojinGothic` / `Pretendard` as Hangul-first fallback rather than the display face, and refusing to substitute a system face for Google Sans, Pretendard, or Poppins, are derived editorial implementation inferences from the verified surfaces; they are not Lablup-authored or a separately published UI specification. The observation inside each row stands on its own.

| Evidence class | Resolution |
|---|---|
| Official product-use | This record's evidence is live inspection of two web surfaces; it carries no Lablup-published type specification. |
| Live computed surface-use | Marketing computes visible display, nav, and button text as `Google Sans`. Marketing body is named `Pretendard`. Docs compute `Pretendard, Poppins`. |
| Declared fallback stack | `PyeojinGothic` and `Pretendard` sit behind Google Sans on the marketing surface. That stack is a Hangul-first fallback, not the display face, and must not be presented as one. |
| Official distributed asset | This capture reached no Lablup-distributed font file and no license statement for Google Sans, Pretendard, or Poppins; none is asserted here. |
| Declared-only | YAML `tokens.typography.family.docs` names `Poppins` as the docs family key; the live docs stack the source records is `Pretendard, Poppins`. Both writings are kept; neither is selected over the other. |

### Family

- **Current visible display family (marketing):** `Google Sans`, with declared fallbacks `PyeojinGothic`, `Pretendard`. Token-set path `tokens.typography.family.display`.
- **Current visible body family (marketing):** `Pretendard`. Token-set path `tokens.typography.family.body`.
- **Current visible docs reading stack:** `Pretendard, Poppins`. YAML `tokens.typography.family.docs` records `Poppins` as the docs key.
- Do not substitute a system face for Google Sans, Pretendard, or Poppins and present the substitute as the brand type. The fallback stack is a fallback.

### Type roles

YAML token-set metrics keep their unitless line-height ratios. The hierarchy table keeps the notes the source recorded beside them. Those two forms are not converted into each other. YAML `tokens.typography.button` records weight 700; the hierarchy table records Button as 500-700. Both writings stay. Keeping the YAML unitless ratios beside the table notes without converting them, and keeping YAML button weight 700 beside the table's 500-700, are derived editorial implementation inferences from the verified surfaces; they are not Lablup-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Notes |
|---|---|---:|---:|---:|---|
| Display Hero | Google Sans | 80px (5.00rem) | 700 | 1.05 | Animated hero headline. YAML use: "Hero headline, Google Sans Bold". Table note: ~1.05 (tight). |
| Display Large | Google Sans | 52px (3.25rem) | 700 | normal | Major section titles. YAML use: "Major section titles (Better together / Need a hand)". |
| Section Heading | Google Sans | 36px (2.25rem) | 600 | normal | Section heads (Latest news / Trusted by). YAML use: "Section headings (Latest news / Trusted by)". |
| Sub-section | Google Sans | 34px (2.13rem) | 600 | normal | News card headline / feature heads |
| Button | Google Sans | 19px (1.19rem) | 500-700 | normal | Pill CTA label. YAML weight for this role is 700. |
| Body | Pretendard | 16px (1.00rem) | 400 | 1.50 | Standard reading text. YAML `lineHeight` is `1.5`. |
| Caption / Utility | Google Sans | 13px (0.81rem) | 500 | normal | Consent + utility button label. YAML use: "Consent / utility button label". |

The four typography rules below are the source's own. Their reasoning — what a weight jump or a Hangul fallback is *for* — is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

- **Heavy display, calm body**: Google Sans at weight 600-700 carries every headline; Pretendard 400 at 16px carries every paragraph. The weight jump is the system's primary hierarchy signal.
- **Scale with drama**: the hero leaps to 80px, far above the 36-52px section heads — a deliberate single moment of typographic confidence per page.
- **Hangul-first fallback**: `PyeojinGothic` / `Pretendard` ensure dense Korean legibility behind the Latin-first Google Sans display face.
- **Two stacks, two jobs**: Google Sans is the marketing/branding voice; Pretendard/Poppins is the functional reading and documentation voice. They never swap roles.

### Assets

- **Brand mark.** The source records an isometric logo mark whose brighter facet is the emerald `#28ab6c`. Catalog identity points at `logo.type: github`, `slug: lablup` — the official GitHub organization, which the source names as the logo-mark source. The catalog pointer, byte size, and fetch notes stay in the provenance sidecar; they are not a Lablup-distributed asset file asserted here. Reading the isometric mark's brighter facet as that emerald, and refusing to assert the catalog pointer as a Lablup-distributed asset file, are derived editorial implementation inferences from the verified surfaces; they are not Lablup-authored or a separately published UI specification.
- Partner logos and product imagery carry no shadow at any size.
- Cards maintain 18-24px radius across breakpoints.

<!-- design-md:section components-states -->
## 4. Components & States

### How to read this section

The source declares each token-set component with a primitive type (`button`, `card`, `listItem`) and a value set; those types are preserved per component. Applicability below is judged by each control's role in this product, never by whether a visual treatment for that state happens to be recorded: where the source supplies no treatment for an applicable state, the value is omitted and the state stays applicable. `not-applicable` is used only where the control's role makes the state meaningless — a destination CTA that commits no operation in place, a carousel arrow, a documentation link — and the reason given is always that semantic one.

One evidence boundary matters here. The source records a keyboard **Focus** treatment: emerald (`#28ab6c`) 1px outline/border on the focused control. That is a generic Focus observation, which is a different evidence type from a `focus-visible` treatment; the observation is kept on the capture record and no `focus-visible` row in this section carries a treatment value.

Every interactive-kind verdict, every applicability verdict, the reason given for either, the refusal to treat generic Focus as a `focus-visible` treatment, and keeping each token-set `use` string beside the longer §4 Role writing, are derived editorial implementation inferences from the verified surfaces; they are not Lablup-authored or a separately published UI specification. This is not a complete state-coverage claim.

The Light Shortcut Card, Dark Feature Card, Teal Feature Card, and Blog Link Card have recorded geometry and no interactive-kind evidence, so kind and a state-applicability map are omitted for them rather than assumed.

### State treatments

The ten state treatments below are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification. They compose values established elsewhere in this contract, and no computed per-component observation accompanies them.

| State | Treatment |
|---|---|
| **Empty (no resources / clusters yet)** | Light canvas. Single Ink Black (`#000000`) line explaining nothing is provisioned yet, with one teal pill CTA to add a node or start. No illustration clutter. |
| **Empty (no news / list results)** | Muted Grey (`#606060`) single line stating there is nothing to show, plus a path back. Honest and calm. |
| **Loading (dashboard / list fetch)** | Skeleton blocks on `#f3f3f3` surface at final card dimensions, 18-24px radius. Flat pulse — no shadow shimmer, consistent with the shadowless system. |
| **Loading (in-place refresh)** | Subtle emerald (`#28ab6c`) progress indicator; previous content stays visible with prior values. |
| **Error (action failed)** | Inline message in Ink Black with a plain-language explanation and a retry. States what to do next — never a bare "오류가 발생했습니다". |
| **Error (form validation)** | Field-level message below the input describing what is valid, not just "required". |
| **Success (action completed)** | Brief inline confirmation in a calm tone; next-step detail linked immediately below. No celebratory emoji. |
| **Skeleton** | `#f3f3f3` blocks at final dimensions, 18-24px radius, flat pulse. |
| **Disabled** | Faint Grey (`#929292`) text on a reduced-opacity surface; emerald actions fade rather than turn grey, preserving the brand read. |
| **Focus (keyboard)** | Emerald (`#28ab6c`) 1px outline/border on the focused control, mirroring the live accent-border treatment. Recorded here as the generic Focus observation it is, and not carried into any `focus-visible` row. |

### Primary Teal Pill

- Role: primary brand CTA on light bands — "View all partners", "About Lablup" family
- Primitive type: `button` · Kind: interactive
- Domain: Marketing
- Background: `#002926`
- Text: `#ffffff`
- Radius: 999px
- Padding: 16px 36px
- Font: 19px / 700 / Google Sans
- Height: 51px
- Token-set path: `tokens.components.button-primary`
- Token-set use: `Primary deep-teal pill CTA (View all partners)`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried, see the evidence boundary above |
| disabled | not-applicable | A marketing CTA that leads to a public partners or about destination has no precondition to gate — the meaning of a disabled state is absent from the role, not merely unrecorded |
| loading | not-applicable | The CTA sends the reader to a destination; it commits no operation in place, so there is no in-progress state on the control itself |
| error | not-applicable | The CTA commits no operation in place, so a failure of that operation cannot be reported on it |
| success | not-applicable | The CTA commits no operation in place, so completion cannot be confirmed on it |

### Black Pill

- Role: secondary high-contrast CTA on light bands — "About Lablup"
- Primitive type: `button` · Kind: interactive
- Domain: Marketing
- Background: `#000000`
- Text: `#ffffff`
- Radius: 999px
- Padding: 14px 24px
- Font: 19px / 500 / Google Sans
- Height: 47px
- Token-set path: `tokens.components.button-dark`
- Token-set use: `Black pill CTA (About Lablup)`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | not-applicable | Same role as the teal pill — a public about destination with no precondition to gate |
| loading | not-applicable | Leads to a destination and commits no operation in place |
| error | not-applicable | Commits no operation in place, so a failure cannot be reported on it |
| success | not-applicable | Commits no operation in place, so completion cannot be confirmed on it |

### White Inverse Pill

- Role: primary CTA on the deep-teal dark sections — "Contact Us"
- Primitive type: `button` · Kind: interactive
- Domain: Marketing
- Background: `#ffffff`
- Text: `#002926`
- Radius: 999px
- Padding: 18px 40px
- Font: 19px / 700 / Google Sans
- Height: 55px
- Token-set path: `tokens.components.button-inverse`
- Token-set use: `White pill CTA on dark sections (Contact Us)`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | not-applicable | A public contact destination with no precondition to gate |
| loading | not-applicable | Leads to a destination and commits no operation in place |
| error | not-applicable | Commits no operation in place, so a failure cannot be reported on it |
| success | not-applicable | Commits no operation in place, so completion cannot be confirmed on it |

### Ghost Outline Pill

- Role: secondary action on dark sections — "View All"
- Primitive type: `button` · Kind: interactive
- Domain: Marketing
- Background: transparent
- Text: `#ffffff`
- Border: 1px solid `rgba(255,255,255,0.3)`
- Radius: 999px
- Padding: 14px 34px
- Font: 19px / 600 / Google Sans
- Height: 49px
- Token-set path: `tokens.components.button-outline`
- Token-set use: `Ghost outline pill on dark (View All)`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | not-applicable | A public "View All" destination with no precondition to gate |
| loading | not-applicable | Leads to a destination and commits no operation in place |
| error | not-applicable | Commits no operation in place, so a failure cannot be reported on it |
| success | not-applicable | Commits no operation in place, so completion cannot be confirmed on it |

### Emerald Accent Button

- Role: accent utility action (consent "Accept All")
- Primitive type: `button` · Kind: interactive
- Domain: Marketing utility chrome
- Background: `#28ab6c`
- Text: `#ffffff`
- Border: 1px solid `#28ab6c`
- Radius: 4px
- Padding: 7px 16px
- Font: 13px / 500 / Google Sans
- Token-set path: `tokens.components.button-accent`
- Token-set use: `Emerald accent action (consent Accept All)`
- Observed: default only
- Disabled (from the state contract, not a per-control capture): emerald actions fade rather than turn grey; Faint Grey (`#929292`) text on a reduced-opacity surface

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable action control; no treatment carried |
| disabled | applicable | The control commits a consent choice, so it can be gated; the state contract records that emerald actions fade rather than turn grey |
| loading | applicable | The control commits a consent write, so an in-progress state is meaningful; visual treatment omitted |
| error | applicable | The committed write can fail and report on this control; visual treatment omitted |
| success | applicable | The committed write can confirm on this control; visual treatment omitted |

### Emerald Outline Button

- Role: secondary utility action (consent "Customize")
- Primitive type: the token block declares no type for this variant; the source lists it among the buttons · Kind: interactive
- Domain: Marketing utility chrome
- Background: transparent
- Text: `#28ab6c`
- Border: 1px solid `#28ab6c`
- Radius: 4px
- Padding: 7px 16px
- Font: 13px / 500 / Google Sans
- Not in the token set
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | applicable | A consent-customize entry can be gated; treatment omitted |
| loading | not-applicable | The control opens a customize panel; it is a dialog trigger and commits no write of its own, so there is no in-progress state on the control |
| error | not-applicable | Opening the panel is not an operation whose failure is reported on this control |
| success | not-applicable | Opening the panel is not an action-outcome confirmation on this control |

### Light Shortcut Card

- Role: feature/shortcut card in the Product/News/Careers grid
- Primitive type: `card`
- Domain: Marketing
- Background: `#f3f3f3`
- Text: `#000000`
- Radius: 24px
- Padding: 20px 24px
- Token-set path: `tokens.components.card-light`
- Token-set use: `Light feature shortcut card (Product / News / Careers grid)`
- Kind: omitted. The record establishes geometry and no interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.

### Dark Feature Card

- Role: near-black feature card (News "Latest from Lablup")
- Primitive type: `card`
- Domain: Marketing
- Background: `#1a1a1a`
- Text: `#ffffff`
- Radius: 24px
- Padding: 20px 24px
- Token-set path: `tokens.components.card-dark`
- Token-set use: `Near-black dark feature card`
- Kind: omitted. The record establishes geometry and no interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.

### Teal Feature Card

- Role: brand-teal feature card (Careers "Join our team")
- Primitive type: the token block declares no type for this variant; the source lists it among the cards
- Domain: Marketing
- Background: `#002926`
- Text: `#ffffff`
- Radius: 24px
- Not in the token set
- Kind: omitted. The record establishes geometry and no interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.

### Blog Link Card

- Role: blog/tech-blog link card with hairline outline (no shadow)
- Primitive type: `card`
- Domain: Marketing
- Background: `#f3f3f3`
- Text: `#000000` (title). Body text `#606060` in Pretendard 16px is recorded only in the source's §9 example prompt and is kept here rather than dropped (A3).
- Border: 1px solid `#e5e5e5`
- Radius: 18px
- Padding: 32px
- Title: Google Sans 34px weight 600
- Token-set path: `tokens.components.card-blog` (YAML records no `fg`)
- Token-set use: `Blog link card with hairline outline (no shadow)`
- Kind: omitted. The record establishes geometry and no interactive-kind evidence, so no `Kind: interactive` confirmation and no §4.4 state-applicability map are declared.

### Carousel Circle Button

- Role: prev/next circular control on the dark hero carousel
- Primitive type: `button` · Kind: interactive
- Domain: Marketing
- Background: transparent
- Text: `#ffffff`
- Border: 1px solid `rgba(255,255,255,0.3)`
- Radius: 50%
- Padding: 1px 6px
- Height: 52px
- Token-set path: `tokens.components.nav-circle`
- Token-set use: `Carousel prev/next circle on dark hero`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web control; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | applicable | A carousel arrow can be gated at either end of the page set; visual treatment omitted |
| loading | not-applicable | The control pages the carousel; it is an arrow, not an operation that pends on the control |
| error | not-applicable | Paging the carousel reports no request or validation failure on the arrow |
| success | not-applicable | Arriving at the next page is navigation, not an action-outcome confirmation on the arrow |

### Top Nav Link

- Role: top horizontal nav — "Backend.AI", "About us", "Media center", "Stories", "Careers"
- Primitive type: the token block declares no type for this variant; the source lists it under Navigation
- Kind: interactive
- Domain: Marketing
- Text: `#000000`
- Font: 16px / 400 / Google Sans
- Active: emerald `#28ab6c` text on active item
- Not in the token set
- Observed: default; additional observed state: active text `#28ab6c`

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web navigation item; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | not-applicable | A public destination in the top nav has no precondition to gate |
| loading | not-applicable | The item moves the reader to another area; the item itself commits no operation that pends |
| error | not-applicable | Active versus inactive is the item's recorded extra state; it reports no request or validation failure |
| success | not-applicable | Reaching a destination area is navigation, not an action-outcome confirmation on the item |

Additional observed state: active — text `#28ab6c`.

### Docs Link (Backend.AI)

- Role: Backend.AI documentation TOC entries and inline links on docs.backend.ai
- Primitive type: `listItem` · Kind: interactive
- Domain: Product / docs
- Text: `#03b5e5`
- Font: 18px / 400 / Pretendard
- Token-set path: `tokens.components.doc-link`
- Token-set use: `Backend.AI docs TOC / inline link accent`
- Observed: default only

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web link; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; no treatment carried |
| disabled | not-applicable | A public documentation destination has no precondition to gate |
| loading | not-applicable | A destination link sends the reader to another page; the link itself commits no operation that pends |
| error | not-applicable | A destination link reports no request or validation failure of its own |
| success | not-applicable | Arriving at the destination is navigation, not an action-outcome confirmation on the link |

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

### Grid and container

- Centered single-column hero with the animated 80px Google Sans headline as the anchor
- A three-up shortcut grid (Product / News / Careers) using the light/dark/teal card temperatures
- Full-width alternating bands: light (`#ffffff` / `#fafafa`) sections and immersive deep-teal (`#002926`) sections
- A horizontally paged news carousel with circular prev/next controls on the dark band

### Whitespace

The three readings below name a purpose for each measurement. They are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification. The measurements themselves — 80px hero, 24px/18px cards, alternating band colors — are recorded values.

- **Breathing room over density**: despite being an infrastructure product, the marketing surface is airy with generous vertical rhythm between bands.
- **Flat segmentation**: sections separate by background temperature (teal vs white vs grey) and hairlines, never by shadow.
- **One loud moment**: the 80px hero is the single typographic crescendo; everything else stays calm and functional.

### Responsive behavior

The breakpoint table and the collapsing rules below are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification. The inspections that produced the values in this contract were taken at one desktop viewport.

| Name | Width | Key changes |
|---|---|---|
| Mobile | <640px | Single column, hero headline compresses from 80px, shortcut cards stack |
| Tablet | 640-1024px | 2-up shortcut/feature cards, moderate padding |
| Desktop | 1024-1440px | Full layout, three-up shortcut grid, centered hero |

### Touch targets

These heights are the recorded control measurements, not a separate cross-viewport specification. Calling the 47-55px pills comfortably tappable, and calling the nav links spaced for touch, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification.

- Pill CTAs run 47-55px tall with 24-40px horizontal padding
- Circular carousel controls at 52px diameter
- Nav links spaced for touch within the header

### Collapsing strategy

- Hero: 80px Google Sans headline scales down on mobile, weight 700 maintained
- Shortcut grid: three-up → two-up → stacked single column
- Alternating teal/white bands maintain full-width treatment, reduce internal padding
- News carousel: paged horizontal scroll with circular controls on all sizes

### Image behavior

- Partner logos and product imagery carry no shadow at any size
- Cards maintain 18-24px radius across breakpoints

Calling that no-shadow rule consistent with the flat system is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification. The no-shadow observation itself is recorded.

<!-- design-md:section content-locales -->
## 6. Content & Locales

### Voice samples

These four strings are verbatim live copy from the homepage.

- "Lablup — Make AI infrastructure accessible" — brand line
- "We untangle complex AI infrastructure with software, for a tomorrow where AI reaches everyone" — Vision
- "AI infrastructure is never a solo effort. We build alongside the hardware, storage, cloud, and service partners..." — partners section
- "The Operating System for AI Infrastructure" — Backend.AI positioning

The `*(verified live 2026-06-26)*` markers that sat beside each sample in the source are freshness metadata and live in the provenance sidecar; the sample strings themselves stay here verbatim.

### Voice and tone

The voice reading below, including the tone table, is a derived editorial implementation inference from the verified surfaces; it is not Lablup-authored or a separately published UI specification. The register is **plain, technical, and quietly mission-driven** — the register of engineers who would rather state a capability than sell it. The brand line "Make AI infrastructure accessible" and the vision "We untangle complex AI infrastructure with software, for a tomorrow where AI reaches everyone" set the tone: ambitious in scope, concrete in mechanism, zero exclamation marks. Product names are descriptive ("The Operating System for AI Infrastructure"), and copy treats the reader as a peer who runs real clusters, not a lead to be captured.

| Context | Tone |
|---|---|
| Hero / brand line | Declarative, mission-framed. "Making AI possible where it wasn't." Confident, not hype. |
| Product positioning | Concrete capability statements. "The Operating System for AI Infrastructure." |
| Vision / about | Plainspoken purpose. "for a tomorrow where AI reaches everyone." |
| Partner / ecosystem | Collaborative, humble. "AI infrastructure is never a solo effort." |
| CTAs | Direct, low-pressure. "Contact Us", "About Lablup", "View All". |
| Press / news | Factual, dated, engineer-to-engineer. Specific hardware and event names. |

### Forbidden register

The exclusions below are a derived editorial implementation inference from the verified surfaces; they are not Lablup-authored or a separately published UI specification. Superlative AI hype ("revolutionary", "game-changing"), fear-based urgency, undefined jargon left unexplained, exclamation-heavy marketing, and consumer-app cuteness on an infrastructure product.

### Locale

Lablup is a Korean company and its name is written `래블업` in Korean. The CEO name is written **Jeongkyu Shin (신정규, CEO)** with the Hangul alongside rather than replacing it. All copy recorded from the two captured surfaces is English, except the error-contract prohibition "오류가 발생했습니다".

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent:

- the exact `ease-enter`, `ease-exit`, and `ease-standard` curves; the token names and uses are recorded, the curves carry no attribution
- hover, button-press, and focus-ring visual treatments, which the motion rules name without any accompanying value
- `focus-visible` visual treatment; the recorded emerald 1px outline is a generic Focus observation
- the interactive kind of the Light Shortcut Card, Dark Feature Card, Teal Feature Card, and Blog Link Card
- the primitive type of the Emerald Outline Button, Teal Feature Card, and Top Nav Link, which the token block does not declare
- computed per-component values behind the empty, loading, error, success, skeleton, and disabled treatments described above
- YAML `tokens.typography.family.docs: Poppins` versus the live docs stack `Pretendard, Poppins`; both writings are kept and neither is selected
