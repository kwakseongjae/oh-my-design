# NCSOFT Design System

<!-- design-md:section experience -->
## 1. Experience

<!-- design-md:claim scope kind=product-surface lang=en -->
### Scope

NCSOFT (엔씨소프트) — the Korean game maker behind Lineage, AION, Blade & Soul, and Throne and Liberty, and in 2026 rebranded simply to **NC** — runs two distinct but coordinated brand surfaces. This contract covers those two first-party surfaces the source inspected for tokens on 2026-06-17: the corporate/game portal at `nc.com` (where `ncsoft.com` now redirects) and the brand-media surface at `about.ncsoft.com` (NC PLAY, "엔씨 공식 브랜드 미디어" / "NC's official brand media"). The official CI renewal article at `https://about.ncsoft.com/en/news/article/nc-ci-renewal-project-en` is a named brand source for the Pentagram identity story; it does not supply the computed interface tokens below. Catalog homepage identity is `https://about.ncsoft.com/`. Every value stays attached to the surface that established it. The live `nc.com` token layer is a structured DTCG system shipped as CSS variables (`--core_primary_normal` and the core / neutral / point / background families), dual light+dark themes; it is a live-extract record, not a separately published UI specification document. Reading those two inspected URLs as this contract's token surfaces, keeping the CI article as a named narrative source that does not supply computed tokens, keeping every value attached to the surface that established it, and reading the live DTCG CSS variables as a live-extract record rather than as a separately published UI specification, are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

The contrast between the two surfaces is, in the source's wording, the system. `nc.com` is a bright, premium media catalog: a pure white (`#ffffff`) and cool-grey (`#f2f2f3`) canvas, near-black ink (`#0f1011`), 16px-radius game cards in a dense grid, and Pretendard as the workhorse type. Its hero is a cinematic full-bleed carousel with 48px/700 Pretendard headlines reversed white over game key-art, and small white circular control buttons (`rgba(0,0,0,0.12)` hairline border) that keep the chrome out of the way of the art. The single saturated interactive color is **NC Purple** (`#7234e0`) — surfaced as `--core_primary_normal` in the live token system, and reinforced by a CDN literally namespaced `purple` (`assets.playnccdn.com/purple/...`). `about.ncsoft.com` is the opposite mood: a confident, monochrome editorial magazine. Its hero runs dark — a `#333333` stage with `#000000` panels and crisp white type — while article bodies flip to light, near-black ink (`#1e1e1e`) on white with thin `#ebebeb` / `#efefef` rules. Here the type is **Helvetica Now** for Latin and **NotoSans-kr** for Hangul, with **Helvetica Now Display Black** driving the oversized 40px editorial display lines (ALL / EDGE / INTERACTIVE). It is shadowless and grid-disciplined. The hex values, family names, sizes, radii, CSS variable names, and the `purple` CDN path in this paragraph are recorded. Calling `nc.com` a bright premium media catalog and NC PLAY a confident monochrome editorial magazine, reading the contrast between them as the system, reading the NC PLAY surface as a design annual rather than a game site, and reading the chrome as kept out of the way of the art, are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

Brand narrative recorded by the source, kept as narrative context. NCSOFT was founded in **1997** by **김택진 (Kim Taek-jin, CEO)** and became one of Korea's defining game companies on the strength of **Lineage** (1998), the genre-shaping MMORPG, later joined by **AION**, **Blade & Soul**, and **Throne and Liberty**. Over two decades NCSOFT grew from a single landmark title into a global games and technology company with serious in-house R&D and AI investment — a posture the NC PLAY brand media foregrounds with sections devoted to game art, sound, and "GAME AI." In **2020**, NCSOFT undertook a corporate-identity renewal with **Pentagram**. As the company's own account puts it, "the reason that game IPs with different roots, such as Lineage, AION, and Blade & Soul were able to be grouped together under the NCSOFT brand was because they share the same core values—namely a commitment to quality and a passion for creating joy." Pentagram cut the letterforms' edges at **45-degree angles** ("NC's sincerity in creating masterpieces based on cutting-edge technology"), set the letters bold with **no space between them** ("a new world that is connected"), and defined **NC BLUE** plus an **NC BLUE Tint** — a cyan-rich blue built by "filling in cyan at various percentages for sharper, clearer visibility." Source §11 writes the same Tint purpose as "sharper and clearer visibility." That heritage blue lives in the digital system as the point-cobalt family (`#1d4b99` normal, `#0e356a` strong). In **2026** the company formalized the shorthand it had been growing into, **rebranding from NCSOFT to simply NC** — `ncsoft.com` now redirects to `nc.com`, and the corporate voice ("Connected Through Joy") leans fully into the connection theme the CI renewal established. The years 1997 / 1998 / 2020 / 2026, 김택진 (Kim Taek-jin, CEO), Lineage, AION, Blade & Soul, Throne and Liberty, the Pentagram renewal and the quoted CI sentences, the 45-degree / no-space / NC BLUE Tint facts, the NC rebrand, and the `ncsoft.com` → `nc.com` redirect are the source's own narrative facts; they do not by themselves supply interface tokens. Classifying that founding-and-rebrand narrative as context that does not by itself supply interface tokens is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

What NC's design refuses, visible across both surfaces: the heavy, gimmicky chrome of typical game-portal marketing (no shadow-stacked cards, no flashing urgency) and the cold anonymity of pure-corporate sites. What it embraces: a structured digital token system (NC Purple primary, full neutral and point-accent ramps), an editorial brand-media voice that treats game-making as craft worth documenting, and a Pentagram-grade discipline that makes a games company read like a design house. The net atmosphere, in the source's wording: an engineering-grade games company that presents its products like a luxury catalog and its brand story like an editorial. That refuses/embraces pairing and that net-atmosphere sentence are the source's own closing unit of the brand narrative; reading them as a current-surface design instruction is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:claim primary-tasks kind=user-outcomes count=4 lang=en -->
### Primary tasks

Selecting these four as the product's primary tasks, and classifying them as surface-or-control outcomes rather than fictional biographies, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification. Each names a surface or control the source records. They do not come from the source's persona section.

- Scan the nc.com game-card grid grouped by genre (MMORPG, etc.) and follow portal action links (`바로가기`, `사전예약`).
- Search the portal with the recorded search / form field.
- Read NC PLAY editorial sections named The Game Art, Sound, Behind The Story, Our Way, AI.
- Use NC PLAY top nav (PLAY active / NEWS).
<!-- design-md:claim-end -->

### Audience

No individual personas are promoted. The source labels its named figures as fictional archetypes informed by publicly observable NC audiences (MMORPG players, global console gamers, design/brand-curious readers of NC PLAY), not individual people, so those biographies are dropped rather than promoted, and no name, age, city, motivation, or affiliation classification is carried into this document or its sidecar. What the source independently records, in its own wording, is the audience at a group level: MMORPG players, global console gamers, design/brand-curious readers of NC PLAY. Dropping the fictional biographies rather than promoting them, carrying no name, age, city, motivation, or affiliation classification, and reading those source-named groups as this product's audience, are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

### Distinctive traits

The list restates the source's Key Characteristics. The values are recorded; the groupings and the readings inside them are a derived editorial implementation inference from the verified surfaces — they are not NCSOFT-authored or a separately published UI specification.

- Two surfaces, one system: bright Pretendard portal (`nc.com`) + monochrome Helvetica Now editorial (`about.ncsoft.com`)
- NC Purple (`#7234e0`) as the single saturated digital primary — `--core_primary_normal`, on a `purple`-named CDN
- Structured DTCG token system on nc.com: core/neutral/point/background families as CSS variables, dual light+dark themes
- NC BLUE (point cobalt `#1d4b99` / `#0e356a`) as the Pentagram CI heritage mark — print-origin, used as a digital accent
- Helvetica Now Display Black for oversized editorial display (40px) on NC PLAY
- Near-black ink (`#0f1011` portal / `#1e1e1e` editorial) instead of pure black for text
- Shadowless: separation by tint (`#f2f2f3`), hairlines (`rgba(0,0,0,0.12)`, `#ebebeb`), and dark/light banding
- Card-radius scale 12px (NC PLAY tiles) → 16px (portal game cards); 6px buttons; full-round avatars/controls
- Point accent palette (red `#f1415e`, green `#21ab79`, magenta `#fa38ec`, light-blue `#38aefa`) for status/category

### Principles

These 6 items are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification. The source states them in its own Principles section together with the UI implication it draws from each. "Connected through joy" and the CI letterform facts are first-party brand lines the source attributes to NC; every *UI implication* below is the source's own editorial reading. The source's own closing note names readings of this kind — "heritage and product, distinct", "craft worth documenting", "flat, fast, modern as a rejection of gimmicky game-portal chrome" — as editorial readings connecting NC's observed design and stated values to its system, not directly sourced NC statements.

1. **Connected through joy.** NC's stated mission is connection via play. *UI implication:* favor warm, inviting framing and surfaces that showcase shared experiences (game art, communities) over transactional pressure.
2. **Craft is worth documenting.** The NC PLAY brand media exists to show the art, sound, and R&D behind the games. *UI implication:* give editorial content real typographic weight (Display Black, generous columns); don't bury craft beneath store chrome.
3. **One action, one color.** NC Purple (`#7234e0`) means "do this." *UI implication:* reserve the saturated purple for the primary CTA so the next step is unambiguous.
4. **Heritage and product, distinct.** NC BLUE is the Pentagram CI heritage mark; NC Purple is the live digital action color. *UI implication:* use blue for identity/trust accents and purple for interaction — never swap them.
5. **Flat, fast, modern.** Mobile-native clarity over decorative depth. *UI implication:* no shadows; separate with banding, tint, and hairlines; keep key-art and content first.
6. **Connected letterforms, connected system.** The CI's no-space, 45°-cut logo encodes "a connected world." *UI implication:* keep the system coherent across surfaces — shared tokens, consistent radius and ink, two type voices that each own a surface.

### Application rules

The source states these nine as its Do list, kept as written. These rules, and the reasons attached to them, are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

- Use NC Purple (`#7234e0`) as the single saturated interactive/CTA color
- Use Pretendard for the product portal (headlines 700, body 400, links 500)
- Use Helvetica Now + NotoSans-kr for the editorial NC PLAY surface
- Reserve Helvetica Now Display Black for oversized (40px) editorial display lines only
- Use near-black ink (`#0f1011` portal / `#1e1e1e` editorial) for text instead of pure black
- Separate with dark/light banding and `#f2f2f3` tint + hairlines, not shadows
- Keep button radius at 6px and game-card radius at 16px
- Use point accents (`#f1415e`, `#21ab79`, `#38aefa`) only for status/category, not chrome
- Treat NC BLUE (`#1d4b99`) as the CI heritage / trust accent, distinct from the purple primary

### Avoid

The source states these eight as its Don't list. These prohibitions, and the reasons inside them, are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

- Spread NC Purple across many elements — it dilutes the single-action signal
- Use drop shadows for elevation — NC is a flat, shadow-free system
- Mix Pretendard and Helvetica Now within the same surface
- Use Display Black for body or UI text — it is display-only
- Use pure black (`#000000`) for body text — reserve near-black ink
- Use NC BLUE and NC Purple interchangeably — blue is heritage/print, purple is the live digital action color
- Crowd hero chrome over the game key-art — controls stay minimal
- Use pill-shaped buttons — portal CTAs are a calm 6px, only controls/avatars are full-round

<!-- design-md:section foundations -->
## 2. Foundations

<!-- design-md:claim foundations kind=rules-or-constraints lang=en -->
### Semantic color

Role names below are the source's own labels. Pairing each hex to the token-set path named beside it, taking those role names from the source's own labels, keeping `tokens.colors.canvas` and `tokens.colors.on-primary` as two keys that share `#ffffff`, keeping §2 **Pure Black** (`#000000`) as a §2 role with no YAML colors key, keeping NC Purple and NC BLUE on their own families rather than swapping them, and attaching every role to the surface the source recorded, are derived editorial implementation inferences from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification. The hex values, CSS variable names, and recorded uses are the source's own.

Primary (NC Purple)

- **NC Purple** (`#7234e0`): Primary interactive color and CTA background. `--core_primary_normal` on the live `nc.com` portal (light theme). The system's single "action" hue. Token-set path `tokens.colors.primary`. Catalog identity `primary_color` is the same hex.
- **Purple Strong** (`#482486`): `--core_primary_strong`. Pressed / strong-emphasis purple for active CTA states and deep accents. Token-set path `tokens.colors.primary-strong`.
- **Purple Subtle** (`#e8d6ff`): `--core_primary_subtle`. Tinted purple surface for selected chips, soft buttons, and highlight backgrounds. Token-set path `tokens.colors.primary-subtle`.
- **Purple Faint** (`#f6eeff`): `--core_primary_faint`. Lightest purple wash for hover surfaces and tonal blocks. Token-set path `tokens.colors.primary-faint`.
- **Purple Dark-theme** (`#8243f2`): `--core_primary_normal` resolved in the dark theme — a slightly brighter purple for the same role on dark backgrounds. Token-set path `tokens.colors.primary-dark`.

NC BLUE (CI heritage)

- **NC Blue** (`#1d4b99`): The Pentagram CI heritage blue, surfaced digitally as `--point_cobalt_normal`. Cyan-rich corporate blue used for trust/identity accents. Token-set path `tokens.colors.nc-blue`.
- **NC Blue Strong** (`#0e356a`): `--point_cobalt_strong`. Deep cobalt for dark-surface accents and pressed states. Token-set path `tokens.colors.nc-blue-strong`.
- **NC Blue Subtle** (`#d3e2fc`): `--point_cobalt_subtle`. NC BLUE Tint — the light, clear cyan-blue wash described in the CI renewal. Token-set path `tokens.colors.nc-blue-subtle`.
- **Light Blue** (`#38aefa`): `--point_light_blue_normal`. Bright sky-blue point accent for links and informational highlights. Token-set path `tokens.colors.light-blue`.

Neutral Scale (portal gray ramp)

- **Ink** (`#0f1011`): `--background_base_1` (dark) / primary near-black ink. Headlines, strong body, nav text on light. Token-set path `tokens.colors.ink`.
- **Ink Soft** (`#1e1e1e`): `--neutral_gray_010`-adjacent; the editorial ink on NC PLAY article bodies. Token-set path `tokens.colors.ink-soft`.
- **Gray 015** (`#252628`): `--neutral_gray_015`. Dark container surface, strong secondary text on light. Token-set path `tokens.colors.gray-015`.
- **Gray 025** (`#3d3d43`): `--neutral_gray_025`. Secondary body copy. Token-set path `tokens.colors.gray-025`.
- **Gray 040** (`#62626a`): `--neutral_gray_040`. Tertiary text, captions. Token-set path `tokens.colors.gray-040`.
- **Gray 055** (`#888890`): `--neutral_gray_055`. Muted labels, metadata. Token-set path `tokens.colors.gray-055`.
- **Gray 065** (`#a3a3a9`): `--neutral_gray_065`. Disabled / lowest-emphasis text. Token-set path `tokens.colors.gray-065`.
- **Gray 075** (`#bdbdc1`): `--neutral_gray_075`. Borders, faint dividers on light. Token-set path `tokens.colors.gray-075`.

Surface & Editorial Neutrals

- **Pure White** (`#ffffff`): `--neutral_white`. Page background, card surfaces, reversed text on dark/purple. Token-set path `tokens.colors.canvas`. Token-set path `tokens.colors.on-primary` is the same hex on a second key (text on purple).
- **Surface** (`#f2f2f3`): `--background_base_2` (light) / `--core_neutral_normal`. Cool-grey segmenting surface. Token-set path `tokens.colors.surface`.
- **Surface Alt** (`#f7f7f8`): `--neutral_gray_097`. Warmer secondary grey for alternating blocks. Token-set path `tokens.colors.surface-alt`.
- **Hero Dark** (`#333333`): NC PLAY dark hero/stage background. Token-set path `tokens.colors.hero-dark`.
- **Editorial Ink** (`#a9a9a9`): NC PLAY muted nav / inactive labels on dark. Token-set path `tokens.colors.editorial-ink`.
- **Editorial Line** (`#ebebeb`): Thin editorial divider rule. Token-set path `tokens.colors.editorial-line`.
- **Editorial Faint** (`#efefef`): NC PLAY light editorial article block surface. Token-set path `tokens.colors.editorial-faint`.
- **Pure Black** (`#000000`): NC PLAY dark hero panels and maximum-contrast moments. A §2 role. It is not a YAML `tokens.colors` key.

Point Accents (status / category)

- **Point Red** (`#f1415e`): `--point_red_normal`. Discount / urgent / error point color. Token-set path `tokens.colors.point-red`.
- **Point Green** (`#21ab79`): `--point_green_normal`. Success / positive status. Token-set path `tokens.colors.point-green`.
- **Point Magenta** (`#fa38ec`): `--point_magenta_normal`. Vivid category / highlight accent. Token-set path `tokens.colors.point-magenta`.
- **Point Lavender** (`#6768f6`): `--point_lavender_normal`. Secondary indigo-lavender accent companion to NC Purple. Token-set path `tokens.colors.point-lavender`.

`tokens.colors.canvas` and `tokens.colors.on-primary` both write `#ffffff`. They stay two keys. NC BLUE (Pentagram print CI, point-cobalt `#1d4b99`) vs live digital primary NC Purple (`#7234e0`) is documented in the source as an intentional print-vs-digital split, not a conflict; `primary_color` follows the live interactive token.

### Spacing

Token-set path `tokens.spacing` (unitless steps, kept as the YAML recorded them):

| Step | Value | Token-set path |
|---|---:|---|
| xs | 4 | `tokens.spacing.xs` |
| sm | 8 | `tokens.spacing.sm` |
| md | 12 | `tokens.spacing.md` |
| base | 16 | `tokens.spacing.base` |
| lg | 24 | `tokens.spacing.lg` |
| xl | 32 | `tokens.spacing.xl` |
| xxl | 48 | `tokens.spacing.xxl` |
| section | 64 | `tokens.spacing.section` |

The source restates a base unit of 8px and the same scale as 4px, 8px, 12px, 16px, 24px, 32px, 48px, 64px. Notable: Portal action links sit at a generous 44px tap height; the editorial NC PLAY surface uses large 32–60px vertical padding under section heads (FEATURED 32px, CATEGORIES 60px) for magazine-style breathing room. `tokens.spacing.base: 16` is not `tokens.rounded.xl: 16`, not the 16px type roles, and not the `0 16px` button padding written as a second key. `tokens.spacing.xl: 32` is not the FEATURED `32px` padding written as a second key. `tokens.spacing.xxl: 48` is not the 48px hero type size. `tokens.spacing.md: 12` is not `tokens.rounded.lg: 12`. CATEGORIES `60px` is a §5 writing; it is not a YAML spacing key. The 44px tap height is a control height, not a spacing step. Keeping each number on its own key path, rather than treating a shared numeral as the same token, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

### Shape

Token-set path `tokens.rounded` (unitless steps, kept on their own path):

| Step | Value | Token-set path |
|---|---:|---|
| sm | 6 | `tokens.rounded.sm` |
| md | 10 | `tokens.rounded.md` |
| lg | 12 | `tokens.rounded.lg` |
| xl | 16 | `tokens.rounded.xl` |
| full | 9999 | `tokens.rounded.full` |

The source's named radius uses, kept on their own rows:

- Small (4px): editorial article blocks on NC PLAY. YAML `tokens.components.card-editorial.radius: "4px"`. It is not a YAML `tokens.rounded` step.
- Button (6px): portal CTAs, inputs, point badges. Token-set key `tokens.rounded.sm`. YAML `button-primary` / `badge-point` also record `radius: "6px"`.
- Medium (10px): scroll/utility elements. Token-set key `tokens.rounded.md`.
- Large (12px): NC PLAY dark media tiles. Token-set key `tokens.rounded.lg`. YAML `card-play` records `radius: "12px"`.
- XL (16px): portal game/media cards — the workhorse. Token-set key `tokens.rounded.xl`. YAML `card-game` records `radius: "16px"`.
- Full (9999px / 50%): circular controls and avatars. Token-set key `tokens.rounded.full: 9999`. YAML `button-icon` and `avatar` record `radius: "9999px"`.

`tokens.rounded.sm: 6`, `md: 10`, `lg: 12`, `xl: 16`, and `full: 9999` stay five keys. The editorial 4px radius stays on the card-editorial component. `tokens.rounded.xl: 16` is not `tokens.spacing.base: 16`. Keeping those five keys, and keeping 4px on the editorial card rather than inventing a sixth rounded step, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

### Elevation

| Level | Treatment | Use |
|---|---|---|
| Flat (Level 0) | No shadow | Page background, inline text, most surfaces |
| Banding (Level 1) | dark `#333333` / light `#ffffff` background shift | NC PLAY hero-vs-article separation |
| Tint (Level 2) | `#f2f2f3` / `#f7f7f8` background shift | Portal section separation |
| Hairline (Level 3) | `1px solid rgba(0,0,0,0.12)` / `#ebebeb` | Circular control outline, editorial divider rules |

**Shadow Philosophy**: Both NC surfaces are essentially shadowless. Token-set path `tokens.shadow.none` with value `none`. Live inspection found `box-shadow: none` across the NC PLAY hero, nav, headings, and tiles, and the portal relies on flat cards with hairline borders rather than elevation. Depth is communicated through dark/light banding (NC PLAY), cool-grey tint (`#f2f2f3`, portal), and thin hairlines. When emphasis is needed the system reaches for color — NC Purple (`#7234e0`) or a point accent (`#f1415e`) — never a drop shadow. This keeps both the catalog and the editorial feeling clean, fast, and modern. Reading that shadowless banding / tint / hairline stack as the elevation contract rather than filling a drop-shadow ladder, and reading the color-for-emphasis rule as the source's own, are derived editorial implementation inferences from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

### Motion

The source attributes its token-level claims (§1–9) to a live inspection and the live CSS token system. The motion contract below sits outside that attribution: the source's own evidence note assigns a source to its voice samples, its brand narrative, and its personas, and assigns none to this motion section. The durations, easing roles, signature motions, and motion rules below are therefore a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

Durations, as the source table states them:

| Token | Value | Use |
|---|---|---|
| `motion-fast` | 120ms | Hover, card lift-hint, focus |
| `motion-standard` | 240ms | Carousel slide, card/section reveal, dropdown |
| `motion-slow` | 360ms | Hero crossfade, dark/light band transitions |

Easing roles — three roles with declared uses. The specific curve values the source lists are not traceable to NCSOFT evidence, so the curves are omitted here and only the roles and their uses are kept. Treating those curves as untraceable and omitting them, rather than promoting them as NCSOFT motion tokens, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

| Token | Use |
|---|---|
| `ease-enter` | Arriving — cards, sheets, carousel slides in |
| `ease-exit` | Dismissals |
| `ease-standard` | Two-way transitions |

Motion rules, as the source states them: Motion is cinematic-but-restrained — the hero carousel crossfades game key-art at `motion-slow / ease-standard`, and portal cards reveal from below at `motion-standard / ease-enter`. The NC PLAY editorial surface transitions between dark and light bands as a slow ambient crossfade rather than a slide. No bounce or spring on UI chrome — a premium games brand signals craft and steadiness, not toy-like playfulness. Under `prefers-reduced-motion: reduce`, carousels stop auto-advancing and all transitions collapse to instant; both surfaces remain fully functional.

An exact curve may be promoted for a component only after that component's own computed transition properties, animation name, duration, easing, and reduced-motion behavior have been observed. A partial confirmation — one curve read off one element, or a match against an official framework or vendor document — does not satisfy that condition. Naming those five evidence kinds as the promotion gate, and refusing a partial confirmation, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.
<!-- design-md:claim-end -->

<!-- design-md:section typography-assets -->
## 3. Typography & Assets

### Font evidence

The following evidence-class application readings are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

| Evidence class | Resolution |
|---|---|
| Official product-use | The CI renewal article records the Pentagram letterform story and NC BLUE. It does not publish a universal current typography token. |
| Live computed surface-use | Portal (`nc.com`) computes `Pretendard` (with `Pretendard JP` and a full Noto Sans CJK fallback stack). Media (`about.ncsoft.com`) computes `Helvetica Now` for Latin with `NotoSans-kr` (and `NotoSans-jp` / `NotoSans-tc`) for CJK. Display on NC PLAY computes `Helvetica-Now-Display-Black`. |
| Official distributed asset | No NCSOFT-exclusive distributed type family was verified. Pretendard, Helvetica Now, and Noto Sans CJK are recorded as the live faces, not as NC-owned font files. |
| Declared / YAML family | YAML `tokens.typography.family.portal` writes `Pretendard`. YAML `tokens.typography.family.media` writes `Helvetica Now`. YAML `tokens.typography.family.display` writes `Helvetica-Now-Display-Black`. YAML `tokens.typography.family.kr` writes `NotoSans-kr`. All four keys stay. |
| License | This record does not establish an NCSOFT font-license notice for Pretendard, Helvetica Now, or Noto Sans CJK. |
| Outside these captures | Typography beyond the two inspected brand surfaces stays outside this contract. |

### Family

- **Portal (nc.com):** `Pretendard` — Token-set path `tokens.typography.family.portal`. The document default for all portal headlines, body, links, and UI, with `Pretendard JP` and a full Noto Sans CJK fallback stack.
- **Media (about.ncsoft.com):** `Helvetica Now` — Token-set path `tokens.typography.family.media`. Latin text, with `NotoSans-kr` (Token-set path `tokens.typography.family.kr`) and `NotoSans-jp` / `NotoSans-tc` for CJK.
- **Display (about.ncsoft.com):** `Helvetica-Now-Display-Black` — Token-set path `tokens.typography.family.display`. A heavy display cut reserved for oversized editorial lines.

Do not replace Pretendard, Helvetica Now, Helvetica-Now-Display-Black, or NotoSans-kr with a system substitute, and do not present the CJK fallback stack as those faces. They never mix within a surface. That fallback prohibition, the two-voice-by-surface split, and keeping the four YAML family keys beside the §3 Pretendard JP / NotoSans-jp / NotoSans-tc writings, are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

### Type roles

YAML unitless line heights stay ratios (A1a). `1.2`, `1.45`, `1.53`, `1.36`, `1.5`, and `1.7` are not rewritten as a fixed px. Token-set `use` strings are kept verbatim; where source §3 notes are longer, both writings are kept. Keeping YAML line heights as unitless ratios, keeping the YAML singles and the §3 px / rem / notes on separate readings, attaching surfaces from the source, keeping hero `48` off `tokens.spacing.xxl: 48`, and keeping body `16` as a type size rather than `tokens.spacing.base: 16`, are derived editorial implementation inferences from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

| Role | Font | Size | Weight | Line height | Token-set use | §3 notes |
|---|---|---:|---:|---:|---|---|
| Hero Headline | Pretendard | 48 | 700 | 1.2 | nc.com hero card headline, Pretendard Bold | 48px (3.00rem); nc.com carousel headline, reversed white over key-art |
| Editorial Display | Helvetica Now Display Black | 40 | 400 | 1.45 | NC PLAY editorial display, Helvetica Now Display Black | 40px (2.50rem); 400 (black face); NC PLAY oversized display (ALL / EDGE / INTERACTIVE) |
| Section Heading | Helvetica Now / Pretendard | 34 | 700 | 1.53 | Section titles, Helvetica Now / Pretendard | 34px (2.13rem); Section / featured-story titles |
| Sub-section | Pretendard / Helvetica Now | 28 | 700 | 1.36 | Card / feature heads | 28px (1.75rem); Card / feature heads (MMORPG, GAME AI) |
| Feature Head | Helvetica Now | 26 | 700 | 1.5 | FEATURED / LATEST list heads, NC PLAY | 26px (1.63rem); FEATURED / LATEST / CATEGORIES list heads |
| Nav (NC PLAY) | Helvetica Now | 20 | 700 | 1.7 | NC PLAY primary nav (PLAY / NEWS) | 20px (1.25rem); Primary editorial nav (PLAY active / NEWS) |
| Body | Pretendard / Helvetica Now | 16 | 400 | 1.5 | Standard reading text, body default | 16px (1.00rem); Standard reading text |
| Link | Pretendard | 16 | 500 | 1.5 | Portal links (바로가기), Pretendard Medium | 16px (1.00rem); Portal action links (바로가기, 사전예약) |
| Editorial Nav Item | Helvetica Now | 16 | 400 | 1.5 | Editorial nav items, Helvetica Now | 16px (1.00rem); Secondary editorial nav items |

Token-set paths: `tokens.typography.display-hero.size` / `weight` / `lineHeight` / `use`; `tokens.typography.display-black.*`; `tokens.typography.section.*`; `tokens.typography.subsection.*`; `tokens.typography.feature-head.*`; `tokens.typography.nav.*`; `tokens.typography.body.*`; `tokens.typography.link.*`; `tokens.typography.label.*`. YAML `label` is the Editorial Nav Item row.

### Type rules

The source states these five as its typography principles. Reading the scale as those five rules is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

- **Two type voices, by surface**: Pretendard runs the bright product portal; Helvetica Now + NotoSans-kr runs the editorial brand media. They never mix within a surface.
- **Display Black is a special occasion**: `Helvetica-Now-Display-Black` is reserved for oversized NC PLAY display moments (40px), never for body or UI.
- **Bold (700) carries headlines**: Both portal hero (48px Pretendard 700) and editorial headings (34px / 700) lean on weight 700 for hierarchy; body drops to 400.
- **Medium (500) for actions**: Portal links and CTAs sit at Pretendard 500 — a half-step up from body to mark interactivity without shouting.
- **Hangul-first fallback**: The portal stack threads Pretendard JP and the full Noto Sans CJK family so Korean, Japanese, and Traditional Chinese render consistently across NC's global storefronts.

### Assets

The catalog identity records `logo.type: favicon` and `logo.slug: https://www.google.com/s2/favicons?domain=about.ncsoft.com&sz=128`. That is a third-party favicon proxy URL held as catalog identity metadata; it is not a first-party distributed NCSOFT brand asset, and it is not promoted to one here. Game key-art and brand imagery on the two inspected surfaces are first-party content; they are not replaced with invented brand-color decoration. Source §8 records that game key-art and brand imagery carry no shadow at any size, consistent with the flat system. Classifying the Google s2 slug as an identity pointer rather than a hosted brand file, and not replacing first-party game imagery with invented brand-color decoration, are derived editorial implementation inferences from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

<!-- design-md:section components-states -->
## 4. Components & States

### How to read this section

The source state contract is preserved here while the catalog graph is not adopted. The source declares YAML components with a primitive type (`button`, `tab`, `card`, `badge`, `avatar`) and a value set; those types are preserved per component, and only on the component that holds the matching token-set key. The portal search / form field is a §4-only record, not in the token set, so it carries no `Primitive type` line. Applicability below is judged by each control's role in this product, never by whether a visual treatment for that state happens to be recorded: where the source supplies no treatment for an applicable state, the value is omitted and the state stays applicable. `not-applicable` is used only where the control's role makes the state meaningless — a carousel control or a destination tab that commits no operation in place — and the reason given is always that semantic one.

One evidence boundary matters here. The source records a `Focus: 1px solid #7234e0` on the portal search / form field. That is a generic focus observation, which is a different evidence type from a `focus-visible` treatment; the observation is kept on the input's own record and no `focus-visible` row in this section carries a treatment value.

Every interactive-kind verdict, every applicability verdict, and the reason given for either — treating the recorded search ring as a generic focus observation, a different evidence type from a `focus-visible` treatment; attaching `Primitive type` only when the source YAML records that type on that component; and omitting kind and a state-applicability map where the source supplies no interactive-kind evidence — is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification. This is not a complete state-coverage claim.

### State record

The source's state contract, preserved with its values and copy:

| State | Treatment |
|---|---|
| **Empty (no results / library)** | White canvas. Single near-black ink (`#0f1011`) line at body size explaining nothing matches, with one NC Purple CTA to adjust or browse. No clutter illustration. |
| **Empty (wishlist / saved, none yet)** | Muted Gray (`#888890`) single line: nothing saved yet, plus a path back to the catalog. Calm, honest. |
| **Loading (catalog fetch)** | Skeleton cards on `#f2f2f3` tinted surface at final 16px-radius card dimensions. Flat pulse — no shadow shimmer, consistent with the shadowless system. |
| **Loading (in-place section refresh)** | Subtle NC Purple progress affordance; previous game cards stay visible during refresh. |
| **Error (load failed)** | Inline message in near-black ink with a plain-language explanation and a retry. Point-red (`#f1415e`) accent on the icon/border. No bare "오류가 발생했습니다". |
| **Error (form validation)** | Field-level message below the input describing what is valid; point-red border, never just "필수". |
| **Success (pre-order / purchase placed)** | Brief inline confirmation in calm tone with point-green (`#21ab79`) accent; next-step detail linked immediately below. No celebratory spam. |
| **Skeleton** | `#f2f2f3` blocks at final dimensions, 16px radius (portal) / 12px (NC PLAY tiles), flat pulse. |
| **Disabled** | Faint Gray (`#a3a3a9`) text on reduced-opacity surface; NC Purple actions fade rather than turn grey to preserve brand read. |

### Primary (NC Purple CTA)

- Role: Portal primary action / CTA — NC Purple token
- Primitive type: `button` · Kind: interactive
- Domain: nc.com portal
- Background: `#7234e0`
- Text: `#ffffff`
- Radius: 6px
- Padding: 0 16px
- Height: 44px
- Font: 16px / 500 / Pretendard
- Active: `#482486` (strong)
- Token-set type: `tokens.components.button-primary.type` `button`
- Token-set bg: `tokens.components.button-primary.bg` `#7234e0`
- Token-set fg: `tokens.components.button-primary.fg` `#ffffff`
- Token-set radius: `tokens.components.button-primary.radius` `6px`
- Token-set padding: `tokens.components.button-primary.padding` `0 16px`
- Token-set height: `tokens.components.button-primary.height` `44px`
- Token-set font: `tokens.components.button-primary.font` `16px / 500 Pretendard`
- Token-set states: `tokens.components.button-primary.states` `strong #482486 · subtle bg #e8d6ff`
- Token-set use: `Portal primary action / CTA — NC Purple token`
- Recorded use: Portal primary action — the `--core_primary_normal` token applied to CTAs

**Soft Purple** (same YAML component; not a second token-set key): Background `#e8d6ff`; Text `#482486`; Radius 6px; Font 16px / 500 / Pretendard; Use: Secondary / tinted action on light surfaces (`--core_primary_subtle`).

The 44px height is this control's geometry. The `6px` radius is this control's radius; it is not `tokens.spacing` written as a radius. The `0 16px` padding is this control's padding; it is not `tokens.spacing.base: 16`. The 16px / 500 font is this control's font; it is not only the Link type-role row. Reading those figures as this button's geometry rather than as those YAML spacing or type-role keys is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web CTA; visual treatment omitted beyond Purple Faint as a system hover wash |
| focus-visible | applicable | Keyboard-reachable action control; visual treatment omitted |
| disabled | applicable | A primary action can be gated; the state contract records NC Purple actions fading rather than turning grey |
| loading | applicable | The control commits a portal action (바로가기 / 사전예약) in place, so it can report in-progress; treatment omitted |
| error | applicable | The control commits a portal action, so a failed request can be reported on it; treatment omitted |
| success | applicable | Same commit role (사전예약 / purchase placed lives on the surface contract); treatment omitted beyond the system success row |

### Circular Carousel Control

- Role: White circular prev/next control on the nc.com hero carousel
- Primitive type: `button` · Kind: interactive
- Domain: nc.com hero
- Background: `#ffffff`
- Text: `#000000`
- Radius: 9999px
- Height: 40px
- Border: 1px solid `rgba(0,0,0,0.12)`
- Font: 16px / 400 / Pretendard
- Token-set type: `tokens.components.button-icon.type` `button`
- Token-set bg: `tokens.components.button-icon.bg` `#ffffff`
- Token-set fg: `tokens.components.button-icon.fg` `#000000`
- Token-set radius: `tokens.components.button-icon.radius` `9999px`
- Token-set height: `tokens.components.button-icon.height` `40px`
- Token-set border: `tokens.components.button-icon.border` `1px solid rgba(0,0,0,0.12)`
- Token-set font: `tokens.components.button-icon.font` `16px / 400 Pretendard`
- Token-set use: `White circular carousel control on nc.com hero`
- The 40px height is this control's diameter. The `9999px` radius is this control's radius; it is not only `tokens.rounded.full: 9999` written as a second key. Reading those figures as this carousel control's geometry rather than as the rounded scale alone is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web button; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A carousel control can be gated at the ends of the reel; visual treatment omitted |
| loading | not-applicable | The carousel prev/next advances a slide; it commits no operation in place |
| error | not-applicable | The carousel prev/next commits no operation in place |
| success | not-applicable | The carousel prev/next commits no operation in place |

### Portal search / form field

- Role: Portal search / form field
- Kind: interactive
- Domain: nc.com portal
- Background: `#ffffff`
- Text: `#0f1011`
- Border: 1px solid `#bdbdc1`
- Radius: 6px
- Font: 16px / 400 / Pretendard
- Observed Focus: 1px solid `#7234e0` (generic focus observation; not a `focus-visible` treatment)
- not in the token set
- The 6px radius is this field's radius. The `#bdbdc1` border is this field's border and also `tokens.colors.gray-075`. Reading those figures as this field's geometry, and keeping the recorded Focus ring as a generic focus observation rather than as a `focus-visible` treatment, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above |
| hover | applicable | Pointer-web input; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable field; visual treatment omitted (the recorded ring is generic Focus) |
| disabled | applicable | Input control; visual treatment omitted |
| loading | not-applicable | This control is a value field; it does not commit an operation whose in-progress state it could report |
| error | applicable | Form field; the surface contract records a point-red border and a field-level message, never just "필수" |
| success | not-applicable | A search value field does not report a completed operation on itself |

### NC PLAY top nav

- Role: NC PLAY top nav — PLAY active white, NEWS muted grey
- Primitive type: `tab` · Kind: interactive
- Domain: about.ncsoft.com
- Text (active): `#ffffff`
- Text (inactive): `#a9a9a9`
- Font: 20px / 700 / Helvetica Now
- Token-set type: `tokens.components.nav-tab.type` `tab`
- Token-set fg: `tokens.components.nav-tab.fg` `#ffffff`
- Token-set font: `tokens.components.nav-tab.font` `20px / 700 Helvetica Now`
- Token-set active: `tokens.components.nav-tab.active` `text #ffffff`
- Token-set use: `NC PLAY top nav (PLAY active white / NEWS muted #a9a9a9)`
- YAML `fg` / `active` are `#ffffff`. §4 writes inactive `#a9a9a9`. Both writings stay.

| State | Applicability | Reason |
|---|---|---|
| default | applicable | Declared treatment above; active is the recorded variant |
| hover | applicable | Pointer-web tab; visual treatment omitted |
| focus-visible | applicable | Keyboard-reachable control; visual treatment omitted |
| disabled | applicable | A destination tab whose availability can lapse; visual treatment omitted |
| loading | not-applicable | This control selects a destination (PLAY / NEWS); it does not commit an operation whose in-progress state it could report |
| error | not-applicable | A tab that only selects a destination does not report a failed request on itself |
| success | not-applicable | Same role reason: selecting a destination is not an operation this tab reports as success |

### Portal Game Card

- Role: Game / media card in the nc.com portal grid (no shadow)
- Primitive type: `card`
- Domain: nc.com portal
- Background: `#ffffff`
- Text: `#0f1011`
- Radius: 16px
- Token-set type: `tokens.components.card-game.type` `card`
- Token-set bg: `tokens.components.card-game.bg` `#ffffff`
- Token-set fg: `tokens.components.card-game.fg` `#0f1011`
- Token-set radius: `tokens.components.card-game.radius` `16px`
- Token-set use: `Game / media card on nc.com portal grid`
- No kind and no applicability map: the source supplies no interaction evidence for this container, so neither is decided here. Treating this card as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

### NC PLAY Dark Tile

- Role: Dark media tile on the NC PLAY hero stage
- Primitive type: `card`
- Domain: about.ncsoft.com
- Background: `#333333`
- Text: `#ffffff`
- Radius: 12px
- Token-set type: `tokens.components.card-play.type` `card`
- Token-set bg: `tokens.components.card-play.bg` `#333333`
- Token-set fg: `tokens.components.card-play.fg` `#ffffff`
- Token-set radius: `tokens.components.card-play.radius` `12px`
- Token-set use: `NC PLAY dark media tile`
- No kind and no applicability map: the source supplies no interaction evidence for this container, so neither is decided here. Treating this tile as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

### Editorial Article Block

- Role: Light editorial article container on NC PLAY
- Primitive type: `card`
- Domain: about.ncsoft.com
- Background: `#efefef`
- Text: `#1e1e1e`
- Radius: 4px
- Token-set type: `tokens.components.card-editorial.type` `card`
- Token-set bg: `tokens.components.card-editorial.bg` `#efefef`
- Token-set fg: `tokens.components.card-editorial.fg` `#1e1e1e`
- Token-set radius: `tokens.components.card-editorial.radius` `4px`
- Token-set use: `NC PLAY light editorial article block`
- The 4px radius is this block's radius. It is not a YAML `tokens.rounded` step. No kind and no applicability map: the source supplies no interaction evidence for this container, so neither is decided here. Reading the 4px radius as this block's geometry rather than as a sixth rounded step, and treating this block as having no interactive-kind evidence for a §4.4 map so kind and a state-applicability map are omitted rather than invented, are derived editorial implementation inferences from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification.

### Point Badge

- Role: Discount / urgent point badge (red token; green `#21ab79` for positive status)
- Primitive type: `badge` · Kind: non-interactive
- Domain: nc.com portal
- Background: `#f1415e`
- Text: `#ffffff`
- Radius: 6px
- Font: 16px / 500 / Pretendard
- Token-set type: `tokens.components.badge-point.type` `badge`
- Token-set bg: `tokens.components.badge-point.bg` `#f1415e`
- Token-set fg: `tokens.components.badge-point.fg` `#ffffff`
- Token-set radius: `tokens.components.badge-point.radius` `6px`
- Token-set font: `tokens.components.badge-point.font` `16px / 500 Pretendard`
- Token-set use: `Discount / status point badge (red token)`
- Kind reason: a badge displays a discount / status. The source names no action, control, or state on the badge itself, so it declares no state-applicability map. Treating this point badge as a status marker rather than a control is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

### Avatar

- Role: Circular brand / social avatar on the portal
- Primitive type: `avatar`
- Domain: nc.com portal
- Background: `#ffffff`
- Radius: 9999px (full circle)
- Token-set type: `tokens.components.avatar.type` `avatar`
- Token-set bg: `tokens.components.avatar.bg` `#ffffff`
- Token-set radius: `tokens.components.avatar.radius` `9999px`
- Token-set use: `Circular brand / social avatar on portal`
- No kind and no applicability map: the source supplies no interaction evidence for this mark, so neither is decided here. Treating this mark as having no interactive-kind evidence for a §4.4 map, so kind and a state-applicability map are omitted rather than invented, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

<!-- design-md:section layout-platforms -->
## 5. Layout & Platforms

### Grid and container

- nc.com: full-bleed cinematic hero carousel anchored by a 48px Pretendard headline, followed by a dense game-card grid (16px radius cards) grouped by genre (MMORPG, etc.).
- about.ncsoft.com: editorial column grid — a dark hero stage banding into light article blocks; FEATURED / LATEST / CATEGORIES sections stack vertically with oversized Display Black display lines.
- Both surfaces center content with generous margins and never crowd the key art.

### Whitespace

- **Catalog density, editorial calm**: the product portal packs game cards tightly while the brand-media surface is airy and column-led — density follows intent.
- **Banded contrast**: NC PLAY alternates a dark `#333333` / `#000000` hero with light `#ffffff` / `#efefef` article bands; the portal alternates white and `#f2f2f3` tinted sections.
- **Key-art first**: hero chrome (controls, headlines) is minimal so cover art carries the page.

The Grid contrast that both surfaces never crowd the key art, and the three Whitespace readings above, name a purpose for each measurement. They are a derived editorial implementation inference from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification. The measurements themselves — the 16px-radius cards, the Display Black lines, the band colors — are recorded values.

### Responsive behavior

Treating source §8 Mobile `<640px` / Tablet `640-1024px` / Desktop `1024-1440px` as source-stated layout rules rather than a captured cross-viewport pass, treating ~44px / 40px / 32–36px as source-named control sizes rather than proven touch-target tokens from a mobile capture, and treating nav collapses / grid collapse / Display Black scaling as source §8 collapsing strategy rather than observed interaction expansion, is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification. The breakpoint table, the touch-target sizes, the collapsing rules, and the image-behavior bullets are the source's own responsive contract, stated as intended behavior at each width.

| Name | Width | Key Changes |
|---|---|---|
| Mobile | <640px | Single column; hero headline compresses; game grid → 1–2 up; nav collapses |
| Tablet | 640-1024px | 2–3-up game cards; moderate padding; editorial columns narrow |
| Desktop | 1024-1440px | Full carousel hero, multi-column game grid, full editorial layout |

Touch targets:

- Portal action links / CTAs at ~44px height — comfortably tappable
- Circular carousel controls at 40px diameter
- Editorial nav items at 32–36px row height within the NC PLAY header

Collapsing strategy:

- Hero: 48px Pretendard headline scales down on mobile, weight 700 maintained
- Game-card grid: multi-column → 2-up → single column stacked
- NC PLAY: dark hero stage and light article bands maintain full-width banding; Display Black scales down
- Genre groupings (MMORPG, etc.) reflow vertically on narrow viewports

Image behavior:

- Game key-art and brand imagery carry no shadow at any size, consistent with the flat system
- Portal cards maintain 16px radius across breakpoints; NC PLAY tiles maintain 12px
- Hero key-art remains full-bleed; headline reverses white and stays legible over art

<!-- design-md:section content-locales -->
## 6. Content & Locales

NC's voice is **confident, craft-proud, and connection-minded** — a games company that takes both its technology and its brand storytelling seriously. The corporate tagline "Welcome to a New world Connected Through Joy" and the portal description "NC connects the world through play" set the register: ambitious in scope, warm in framing, never gimmicky. The brand-media surface (NC PLAY, "엔씨 공식 브랜드 미디어" / "NC's official brand media") treats readers as people interested in craft — game art, sound, R&D, AI — not just players to be monetized. Product copy on the portal is direct and functional ("바로가기", "사전예약", "자세히 보기"). Calling that register confident / craft-proud / connection-minded, and refusing to treat the mission line as a complete product-microcopy guide, are derived editorial implementation inferences from the verified surfaces; they are not NCSOFT-authored or a separately published UI specification. The tagline, the portal labels, and the NC PLAY section names are recorded strings.

| Context | Tone |
|---|---|
| Corporate / mission | Ambitious, warm. "Welcome to a New world Connected Through Joy." Connection-framed, not hype. |
| Portal product copy | Direct, functional. "바로가기", "사전예약", "여름 정기 세일". |
| NC PLAY editorial | Craft-proud, curatorial. Sections named The Game Art, Sound, Behind The Story, Our Way, AI. |
| Brand / CI storytelling | Reflective, design-literate. "Renewal is not creating something new, but realigning existing values." |
| News / press | Plain, informative. Korean press-release register. |

**Voice samples (verbatim from live brand-owned surfaces):**

- "NC - Welcome to a New world Connected Through Joy" — nc.com page title (mission-framed). The source also records the title line as "Welcome to a New world Connected Through Joy".
- "NC PLAY (엔씨 플레이)" — about.ncsoft.com document.title.
- "엔씨 공식 브랜드 미디어" — about.ncsoft.com meta description.
- "From MMORPGs like Lineage, AION, Blade & Soul, and THRONE AND LIBERTY ... NC connects the world through play." — nc.com meta description.
- "Renewal is not creating something new, but realigning existing values and sharing a common brand mission in order to take the brand to the next level." — CI renewal article.

**Forbidden register**: pay-to-win urgency, fear-of-missing-out manipulation in brand copy, hollow superlatives ("revolutionary", "world's best"), and any tone that treats players as marks rather than an audience for craft.

The captured product surfaces carry Korean and English copy. Hangul, Japanese, and Traditional Chinese rendering is the portal type-stack rule recorded in Typography.

<!-- design-md:section governance -->
## 7. Governance

<!-- design-md:claim authority kind=evidence-backed-reconstruction lang=en -->
### Authority

This document is an evidence-backed reconstruction, not authority for an unrelated target project.
<!-- design-md:claim-end -->

<!-- design-md:claim application-priority order=prompt-fact,repository-fact,system-contract,reference-inspiration lang=en -->
### Application priority

1. Direct user instructions for the requested scope.
2. Repository facts.
3. This system contract.
4. Reference inspiration.
<!-- design-md:claim-end -->

<!-- design-md:claim unknowns policy=absent-at-smallest-unresolved-boundary lang=en -->
### Unknowns

Omit only the smallest unresolved value or group. Do not replace it with a plausible default.
<!-- design-md:claim-end -->

<!-- design-md:claim changes policy=review-record-validate-before-adoption lang=en -->
### Changes

Record, review, and validate changes before adoption.
<!-- design-md:claim-end -->

### Named gaps

These decisions are unnamed values, not permissions to invent. Reading the list as a catalog of unnamed values rather than as coverage of domains the source never named is a derived editorial implementation inference from the verified surfaces; it is not NCSOFT-authored or a separately published UI specification.

- the exact easing curves behind `ease-enter`, `ease-exit`, and `ease-standard`, and the per-component motion evidence that the Foundations promotion condition requires before any curve is promoted
- the hover and pressed visual treatments of every declared control
- the `focus-visible` treatment of all controls; the recorded search ring is a generic focus observation
- getdesign.md / refero records for NCSOFT / NC
