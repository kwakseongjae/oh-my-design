# ibm migration log

Source: `web/references/ibm/DESIGN.md`
Sibling read (not the migration input): `web/references/ibm/.verification.md`
Destination: `docs/design-md-weight/migrated/ibm/DESIGN.md`
Provenance: `docs/design-md-weight/migrated/ibm/provenance.md`
Date: 2026-08-28
Worker: grok-4.6 T2
Rulebook: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v12**
Not a catalog-adoption claim. Gate output is recorded below as a run result, not as evidence of semantic adequacy (E2c).

Every destination line below was checked with `grep` against the three output files before it was written (F2). Line numbers are from the files as written in this directory. Counts use `grep -oF … | wc -l` per file, never `grep -c`, which counts lines.

Source SHA-256 `7e5bcc935bad04b57fe1a4e1c0a57570b6870f508543ae73a087bc7445620869` (`web/references/ibm/DESIGN.md`). Sibling SHA-256 `aedd2d3ca551d236e0fdc2453ff794a9b96c75a5d157668d83d0370232f696c5` (`web/references/ibm/.verification.md`). Worker-close portable DESIGN SHA-256 `e2e46b0e7e89438aa2429e8ca6f817684d9d435f01343aa7e9c89daee8e5069d`. Auditor portable DESIGN SHA-256 `45ddf4460ef4382127222a3c087ed87d873a36cbdc0f51cd86a5ff1b5afd7fcb`.

| Legacy | Disposition | Destination / reason |
|---|---|---|
| YAML identity (`id`, `name`, `country`, `category`, `homepage`, `primary_color`, `logo`) | 분리 → provenance; `homepage` 옮김 → Experience Scope (inspected `kr-ko` route); `primary_color` 옮김 → Scope + Foundations + components; `logo.type` / `logo.slug` 옮김 → Typography & Assets Assets | Portable file has no frontmatter. H1 is `# IBM Design System` (`DESIGN.md` 1). Identity table `provenance.md` Identity. `https://www.ibm.com` stays in the identity table; the inspected marketing route `https://www.ibm.com/kr-ko` is dual: `DESIGN.md` 9/21 + `provenance.md` Surfaces (E2a). `#0f62fe` is dual: first portable line `DESIGN.md` 11 (Scope / Foundations / primary action; `grep -oF` dest **8** at 11/33/38/55/75/77/176/184) + `provenance.md` Identity (E2a). `logo.type: github` / `logo.slug: IBM` is dual: `DESIGN.md` 156 + `provenance.md` Identity (E2a). |
| YAML `omd: "0.1"`, `verified`, `verification_v2`, `tokens.source: live-extract`, `tokens.extracted`, `components_harvested: true`, `ds.name` / `ds.url` / `ds.type: system` / `ds.description` / `ds.og_image` | 분리 → provenance; Carbon 이름·경계 옮김 → Experience Scope | Verification metadata is a value and is kept, not dropped (A1c): `provenance.md` Identity + Freshness. The source footer's **Verified:** 2026-07-13 is in Freshness. `ds.type: system` dest in `DESIGN.md` **0** as a ledger key; the fact it names (Carbon is IBM's official open-source design system; documentation is distinct from captured IBM.com surfaces) is in `DESIGN.md` 9 + `provenance.md` Identity / Token note (E2a). |
| YAML `tokens.colors` (7 keys) | 옮김 → Foundations semantic color | `DESIGN.md` 77–83. All seven roles kept with names, values, and token-set keys: primary `#0f62fe` dest **8**, link `#0062fe` dest **6** (11/33/38/55/75/78), canvas `#ffffff` dest **5**, foreground `#161616` dest **7**, muted `#525252` dest **2** (11/81), layer `#f4f4f4` dest **7**, border `#c6c6c6` dest **4**. Paths `tokens.colors.primary` dest 1 at 77 · `link` 78 · `canvas` 79 · `foreground` 80 · `muted` 81 · `layer` 82 · `border` 83. Route-local `#e8e8e8` dest **3** · `#095bf4` dest **2** · `#0b5df8` dest **2** · `#0c56e7` dest **2** · `#0953e5` dest **2** stay on those component records, not color-token keys. |
| YAML `tokens.typography.family.sans` (`IBM Plex Sans KR`) | 옮김 → Typography & Assets Family | `DESIGN.md` 128. Path named `tokens.typography.family.sans`. `IBM Plex Sans KR` dest **20**. The no-substitution rule at 132, and the canonicity-because-loaded/high reading on the same line, carry an adjacent complete qualifier (B2/B2a). |
| YAML `tokens.typography.display` / `body` / `action` / `label` | 옮김 → Typography & Assets Type roles | `DESIGN.md` 140–152. Recorded line heights stay in source form and are never converted to a replacement ratio (A1a): `62.7669` dest **6** · `18.0001` dest **5** · `53.6469` dest **5**. Observed `62.7669px` / `24px` / `18.0001px` stay on the hierarchy table. YAML `use` strings restored verbatim: `Marketing leadspace at the captured 1440px viewport` dest **1** at 140 · `Observed public-product body text` dest **1** at 141 · `Observed primary action` dest **2** at 142/183 · `Observed marketing badge` dest **1** at 143. `tokens.typography.body.size` `16` dest **3** at 91/136/152. Tracking `0.16` dest **5** · `0.32` dest **3**. Qualifier at 136. |
| YAML `tokens.spacing` (`xs: 8`, `sm: 12`, `md: 16`, `lg: 24`, `xl: 32`, `section: 64`) | 옮김 → Foundations spacing | `DESIGN.md` 89. Paths kept on that compact line. `tokens.spacing` dest **4** (89/91/91/152). `md: 16` dest **2** (89/91) is a spacing step — not body size `16`, not label line-height `16`, not tab padding `0px 16px`. Qualifier at 91 names rhythm-candidates-not-grid, `48`-as-height-not-spacing-key, and the four writings of `16`. |
| YAML `tokens.rounded` (`sharp: 0`, `control: 4`) | 옮김 → Foundations shape | `DESIGN.md` 95–100. Paths kept separate: `tokens.rounded.sharp` dest **2** at 97/100 · `tokens.rounded.control` dest 1 at 98. `sharp: 0` dest **2** (95/100) · `control: 4` dest **3** (95/100). Accordion `0px` stays on that control. `tokens.rounded.full` dest **0**. `full: 9999` dest **0**. |
| YAML `tokens.shadow.flat` (`none`) | 옮김 → Foundations elevation | `box-shadow: none` dest **1** at `DESIGN.md` 104. Path `tokens.shadow.flat` dest 1 at 104. |
| YAML `tokens.components.primary-action` | 옮김 → Components & States Public product primary action | `DESIGN.md` 171–198. Verified primitive type preserved on this component only, not flattened to `Kind` (A1b): `Primitive type: \`button\`` dest **1** at 174 = source YAML `type: button` 1. `Token-set use: \`Observed primary action\`` dest **1** at 183. Path `tokens.components.primary-action` dest 1 at 184 (`type: button`, `bg: #0f62fe`, `fg: #ffffff`, `radius: 4`, `padding: 14px 63px 14px 15px`, `height: 48`, `use: Public product CTA`). `Public product CTA` dest **2** at 184/185. |
| YAML `tokens.components.product-tabs` | 옮김 → Components & States Public product tabs | `DESIGN.md` 200–228. `Primitive type: \`tab\`` dest **1** at 203 = source YAML `type: tab` 1. `Token-set use: \`Confluent public-product tablist\`` dest **1** at 212. Path `tokens.components.product-tabs` dest 1 at 213. `Confluent public-product tablist` dest **3** at 23/212/213. Accordion and newsletter field are `not in the token set` dest **3** (169 + 232 + 258). |
| §1 Visual Theme & Atmosphere | 옮김 → Experience Scope + Distinctive traits | Scope `DESIGN.md` 9–13; Distinctive traits 30–38. White / Gray 10 / Gray 100, IBM Blue `#0f62fe` and `#0062fe`, IBM Plex Sans KR, 14px / 400 / 18.0001px / 0.16px, mixed 0px / 4px corners, July 2026 capture, authenticated-IBM-application / every-Carbon-theme boundary, Carbon catalog-not-automatically-a-claim. Surface-boundary reading at 9; keep-on-surface reading at 11 (B2/B2a). |
| §2 Color Palette & Roles | 옮김 → Foundations semantic color | `DESIGN.md` 75–85. Role-naming and `#0f62fe`/`#0062fe`-not-collapsed readings carry the adjacent qualifier at 75 (B2/B2a). |
| §3 Typography Rules — evidence classes, hierarchy | 옮김 → Typography & Assets | Evidence classes `DESIGN.md` 112–124; family 126–132; token-set table 140–143; observed hierarchy 145–150. Official product-use / Official distributed / Declared-only / Helvetica-`ibm_icons` readings carry the adjacent qualifier at 124. FontFaceSet-style counts `485` dest **1** at 119 · `25 elements` dest **1** at 119 · `25 visible` dest **1** at 129 (the 25-element `IBM Plex Sans` count is dest **2** at 119/129). |
| §4 Component Stylings | 옮김 → Components & States | `DESIGN.md` 171–279. Selectors, classes, route-local hover/pressed, and “no expanded panel” notes kept on each record. Capture selectors `4` / `5` / `10` / `23` are dual: body Use lines + `provenance.md` Sibling handling raw samples (E2a). `surface-3::[data-omd-capture="16"]` is `DESIGN.md` 185 only — provenance Sibling handling dest **0** for that exact selector (not dual). Newsletter Helvetica stack kept off the IBM Plex product token at 268. |
| Footer **Verified** / **Tier 1 sources** / **Tier 2** / **Resolution note** / **Conflicts** | 분리 → provenance; URL 옮김 → Experience Scope | Freshness + Surfaces + Sources in `provenance.md`. The three Korean public URLs and the Carbon / typeface / history URLs are dual: Scope 9 + provenance Surfaces (E2a). Developer typography and About URLs are Scope 9 only (`https://www.ibm.com/brand/experience-guides/developer/brand/typography/` and `https://www.ibm.com/about` exact dest provenance **0**). Resolution-note strings `rolled back` / `earlier universal` / `fresh evidence` dest **0** in `DESIGN.md` and `provenance.md` (fitpet형 2차 목적지 아님). Avoid 63 is the source Don't (“Do not reintroduce a universal 0px-radius primary-button rule”), not the footer Resolution note. Conflicts unresolved: none — Freshness only. |
| §5 Layout Principles | 옮김 → Layout & Platforms | `DESIGN.md` 283–289. Captured 1440px rhythm 8/12/16/24/32/48/64; Carbon 2x Grid official but not measured; 48px action height; do not infer an authenticated-product layout. Desktop-capture / 2x-Grid-not-live-token reading at 289 (B2/B2a). |
| §6 Depth & Elevation | 옮김 → Foundations elevation | `DESIGN.md` 104. Flat-treatment reading carries the adjacent qualifier at 104 (B2/B2a). |
| §7 Do's | 옮김 → Experience application rules | `DESIGN.md` 49–57, under the grouping qualifier at 51. Not placed in Governance controlled copy. Five items, matching the source Do list. |
| §7 Don'ts | 옮김 → Experience avoid | `DESIGN.md` 59–67, under the qualifier at 61. Five source Don'ts. No invented domain (no `native application`, no `mobile app`, no `back-office`, no `product application`). Exact string `measures 1440px` dest 0 / provenance 0. Source-established `1440px` dest **3** and `1440×900` dest **3** are capture-viewport facts, not a denial. |
| §8 Responsive Behavior | 옮김 → Layout & Platforms | `DESIGN.md` 287. “No breakpoint comparison was collected in this packet. … measured only at the captured 1440×900 viewport.” Unresolved items are omitted as values, not denied as invented measurements (D1). Source “IBM-specific mobile geometry” is kept as the source's own wording at 287/361; it is not expanded into a `native application` or `mobile app` domain. |
| §9 Agent Prompt Guide | 삭제 | Tool-facing copy-paste prompts and restatements; no receiving slot and no delegation. Every value §9 names was checked against the portable body before deletion and each was already present (A2, A3). The check is itemised at `provenance.md` §9 deletion check. CTA `#0f62fe` / white / 4px / 48px / 14px/400 IBM Plex Sans KR is in Public product primary action. Body 16px/400/24px `#161616` is in Type roles. Tabs and accordion values are on those records. |
| §10 Voice & Tone | 옮김 → Content & Locales | `DESIGN.md` 294–322. Source voice paragraph at 294. Context table at 297–300. Byte-exact / gloss-beside rule at 322 (B2/B2a, A5). |
| §11 Brand Narrative | 옮김 → Experience Scope | `DESIGN.md` 13 (and overlapping facts at 9/11): **1911** dest **2**; **1924** dest **2**; **Computing-Tabulating-Recording** dest **5**; **quantum** dest **2**; **sustainability** dest **2**; **OFL** dest **3**; IBM Plex as corporate typeface; Carbon public component guidance; closing sentence “This reference keeps those first-party narrative facts separate from the limited set of computed values collected on Korean public routes” at 13. Marked there as narrative context that supplies no interface tokens; the classification carries an adjacent complete qualifier in the same paragraph (B2/B2a). Also noted as narrative-not-token-source at `provenance.md` Proof notes. |
| §12 Principles — 4 numbered | 옮김 → Experience principles | `DESIGN.md` 40–47 under the B2a form at 42: "These four items are a derived editorial implementation inference from the verified surfaces; they are not IBM-authored or taken from a separately published UI specification, including the published Carbon documentation." IBM has a published first-party design system (Carbon), so the close names that specification (rulebook v12 B2a 전제 주석). Portable derived-editorial inventory: `provenance.md` Portable derived-editorial scope (**21** rows). |
| §13 Personas | 삭제 | The source recorded no first-party audience research suitable for named personas and used `[FILL IN]` placeholders. Not promoted to Audience or to `primary-tasks`, and not re-hosted in the sidecar: no name, age, city, occupation, or affiliation appears in either output (D2, D2a). Identifier strings dest 0 / provenance 0 / this log 0 as use. Audience at `DESIGN.md` 26–28 restates the source's own “no first-party audience research suitable for named personas” sentence and does not invent a shopper or buyer group. The three `primary-tasks` at 21–23 come from labels and surfaces the source records; 19 says so and qualifies the step from label to "primary task". |
| §14 States | 옮김 → Components & States capture record + per-component applicability | Source §14 evidence boundaries are summarized at `DESIGN.md` 163 (not the source table verbatim). Per-state values sit on the component records 171–279. Applicability procedure 164–167; qualifier at 167 covers every Reason cell that follows. Non-observation is never used as a `not-applicable` reason (C1). |
| §14 → per-component applicability (Core §4.4) | 옮김 → Components & States | Interactive controls declare the seven canonical states. Destination CTA, tab, and accordion close loading/error/success (`loading \| not-applicable` dest **4** at 196/225/251/276; `loading \| applicable` dest **0**). Newsletter field opens `error` only. `default` and `focus-visible` stay applicable. Generic Focus is not treated as a `focus-visible` treatment (B1). This is not a complete state-coverage claim (C3). |
| §15 Motion & Easing | 옮김 → Foundations motion | `DESIGN.md` 108. “The supplied evidence records hover, pressed, focus, and tab-selected results but does not measure durations, curves, or reduced-motion behavior. Do not infer a motion scale from Carbon documentation or from static state values. … No motion token is promoted.” The source lists no curve values. B3 is held: the promotion condition at `DESIGN.md` 108 names all five evidence kinds — computed transition properties, animation name, duration, easing, reduced-motion behavior — and the per-component gate, in full text (E2c). Governance 363 restates the five kinds and the gate. |
| §15 Motion & Easing — unattributed template curves | 해당 없음 (삭제할 무출처 커브 없음) | The source does not contain `cubic-bezier(0.4, 0.0, 1, 1)` or the other `spec/omd-v0.1.md` example-table curves. `grep -oF 'cubic-bezier(0.4, 0.0, 1, 1)'` dest 0 / provenance 0. |

## Sibling handling (`web/references/ibm/.verification.md`)

The sibling exists — confirmed with `find web/references/ibm -type f`, since a dotfile is invisible to `ls` and to a `*` glob. It is a separate canonical file, not the migration input, so nothing in it was used to establish a portable body fact that the source body does not already record.

- Its full record is transcribed at `provenance.md` Sibling handling and is **not** promoted into `DESIGN.md`.
- Values it carries that the visible source body does not: RGB spellings; `home::#leadspaceSimpleTitle`; Marketo class tokens `mktoField` / `mktoTextField`; Helvetica **7 uses**; collector totals 6 types / 64 variants / coverage 100; artifact timestamp; getdesign “structured blue palette”; Refero URL; sibling abbreviation `C-T-R`. Recorded at `provenance.md` sibling-only list.
- Measured `DESIGN.md` 0 for those sibling-only strings: `rgb(15, 98, 254)` 0 · `leadspaceSimpleTitle` 0 · `mktoField` 0 · `structured blue` 0 · `C-T-R` 0 · `coverage 100` 0.

## A5 / A5a verification

The gate's `copy-loss` needles come only from contiguous non-Latin runs of four characters or more inside quotations. IBM's published lines in the source are Latin, so `compared < candidates` is expected and the A5a hand sweep is mandatory.

| Sweep | Extracted | Missing from `DESIGN.md` + `provenance.md` | Published copy among the missing | Handling |
|---|---:|---:|---:|---|
| Published official / captured labels in the source body | 17 distinct | 0 | 0 | IBM / IBM Plex / IBM Plex Sans KR / IBM Plex Sans / IBM Plex Mono / IBM Plex Serif / IBM Plex Sans Arabic / IBM Plex Sans Hebrew / IBM Plex Sans JP / Carbon / Computing-Tabulating-Recording / Computing-Tabulating-Recording Company / Gray 10 / Gray 100 / IBM Blue / Open Font License / Plex Mono Light. |
| YAML `use` strings | 6 distinct | 0 | 0 | Marketing leadspace at the captured 1440px viewport / Observed public-product body text / Observed primary action / Observed marketing badge / Public product CTA / Confluent public-product tablist. |
| Sibling published strings | 0 brand-published extras | 0 | 0 | getdesign “structured blue palette” is a Tier 2 directory gloss, not IBM-published copy. It survives in provenance as a sibling-only record; not promoted into `DESIGN.md`. |

Sub-needle labels confirmed present individually in `DESIGN.md`: IBM Plex Sans KR, Carbon, Computing-Tabulating-Recording, Gray 10, Gray 100, IBM Blue, Open Font License, Plex Mono Light, 1911, 1924, OFL, quantum, sustainability, keeps those first-party narrative facts separate.

A5 분모: gate `copy-loss` compared/candidates recorded below; hand sweep of source published labels 17 + YAML use 6 extracted / 0 missing.

## Gate run

`node test-v2/tools/migrate-reference.mjs --brand ibm --gate-only` → `verdict: PASS`, `problems: []`, `coverage: [{ check: "copy-loss", compared: 0, candidates: 124 }]`. Separately, `scripts/design-md-core.cjs` `inspectDesignMd` / `evaluatePortableCore` reports `level: portable-core`, `portable_core: true`, `reasons: []`, and 0 `[FILL IN]`. `latin-copy-audit.mjs --brand ibm` reports `totalLost: 0`. Both are run results only. The gate has historically passed A5 losses, narrow ledgers, false log destinations, and B1 promotions, so neither is cited here as evidence that the migration is correct; the sweeps above and the two mandatory passes are. A5a was mandatory because `compared` 0 < `candidates` 124.

## Deviations recorded

- `DESIGN.md` is above the spec's 600–1,800-word SHOULD budget (`wc -w` 5351 after auditor qualifier expansions; worker-close was 5305). The budget yielded to A1: four declared component records with selectors and seven-state maps, dual token-set / observed type tables, the full §11 founding-and-typeface-and-About narrative including its closing Korean-routes sentence, and the B2a qualifications cannot be compressed without dropping verified values or dropping the qualifications. Recorded rather than silently accepted.
- IBM has a published first-party design system (Carbon), so every derived-editorial close names that specification (rulebook v12 B2a 전제 주석). Worker-close measure `grep -o 'derived editorial implementation inference' DESIGN.md \| wc -l` = **21** = `not IBM-authored` 21 = `including the published Carbon documentation` 21. Provenance derived ledger **21** rows (E1 1:1).

## F1 B2a rescan (mandatory final pass)

Re-read the portable body from H1 through Governance. Every causal, interpretive, or classificatory sentence was asked whether it is an IBM-issued fact or a derived reading. Scope surface-boundary, keep-on-surface, and narrative-as-context; primary-task selection; trait grouping; Principles / Do / Don't headers; Semantic role slotting including `#0f62fe` / `#0062fe`; spacing rhythm-candidates / `48`-as-height / four-`16`s; shape key-path separation; elevation and motion-gate readings; font evidence including declared-only-not-UI-token / family no-substitution and canonicity-because-loaded/high / type-role keep-both; github-slug-as-pointer and `ibm_icons`-off-UI-family; applicability procedure; layout 1440×900-capture and 2x-Grid-not-live-token reading; byte-exact rule; and the unresolved-list naming each carry an adjacent complete Carbon-named qualifier. Source facts (years, hexes, YAML keys, published strings, 485/25 counts, selectors) were left unqualified. Inventory: **21** lines (auditor expanded `:91` and `:124` in place; count unchanged). Audience restates the source's own no-research sentence and does not invent a buyer group, so no extra qualifier was added there. Content `:294` restates the source §10 paragraph; the official-guidance-not-complete-microcopy reading is qualified at 322.

## F2 E2 rescan (mandatory final pass)

Each log row was written only after `grep -oF` against `DESIGN.md` and `provenance.md`. Dual destinations named above were confirmed in both files. B3 is claimed only because `DESIGN.md` 108 names all five evidence kinds and the per-component gate in full text (E2c). Governance 363 restates the five kinds and the gate. No "B3 유지" claim is made beyond that. Sibling-only strings are claimed dest 0 after measurement. Persona identifiers are claimed dest 0 / provenance 0 as use, not as a self-denominator absence sentence that lists names (D2a, E2d).
