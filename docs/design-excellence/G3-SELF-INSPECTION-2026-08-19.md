# G3 자체 검수 판정 — 2026-08-19

> 위임 근거: 사용자 지시 "너가 스스로 검수해보고, 기준점을 넘었다 판단되면
> v2.0.0 절차 재개해봐." 이 문서가 그 검수의 기록이다.

## 판정: **통과** — v2.0.0 절차 재개

기준(사용자가 세운 것): ① 디테일이 담긴 컴포넌트, ② 프로덕션에 준하는 UI,
③ 실제 데이터를 담는 UI, ④ 디자이너·프론트엔드가 봐도 납득할 DESIGN.md와
디자인 시스템.

## 증거

### 이미 닫힌 사이클 (G1+G2)
- 3케이스(온집 v2·이웃장터·스타일몰) 셀 완주, G2 렌더 비평 20건 발견→20건
  수정→재렌더 검증. 온집은 프리셋 체제(attempt 5)로 재생성: 첫 렌더 결함
  7→3(P0/P1 0), 입력 토큰 −47%.

### 이번 검수에서 추가 확인한 축 (이전 미검증)
- **모바일 390px**: 3케이스 전 라우트 가로 스크롤 0(G34). 랭킹 캐러셀→
  번호 리스트 적응, 스펙 표 스택 전환, 햄버거 내비 — 반응형 파생 건전.
- **인터랙션 상태**: 커스텀 리스트박스 팝업 실동작(키보드 열기, active
  하이라이트, APG 계약). 필터 적용 시 결과 수 라인 갱신 + "필터 지우기"
  조건부 노출 + **제로-수확 칩 자동 비활성**(스펙 초과 크래프트).
- **오류 라우트**: 3케이스 모두 정직한 404(사용자 언어 + 복귀 링크).
- **포커스 체계**: 키보드 탐색에서만 링 노출, 라우트 전환 프로그램 포커스
  링 억제 — 마우스 화면 어디에도 링 없음.
- **DESIGN.md 3종**: 입장 + 원칙(희생 명시) + D-결정표 + 토큰 역참조.
  온집은 컴포넌트별 프리셋 ID 인용. "왜 이 값인가"에 문서가 답한다.

## 남기는 유보 (정직성)

- 원래 G3 설계의 **블라인드 경쟁 비교**(UIUX Pro/Impeccable/shadcn 조립을
  나란히 놓는 것)는 수행하지 않았다. 이 판정은 사용자 위임에 따른 심사자
  단독 검수이며, 심사자는 파이프라인 제작자다. 블라인드 비교는 릴리스 후
  공개 벤치마크 트랙에서 수행 가능하다.
- 이웃장터·스타일몰은 패치 10 산출물(+수정 패스)이다. 프리셋 체제 재생성은
  릴리스 차단 사유가 아니라 후속 개선으로 분류한다.
