# T2-1 웨이브 16 sol 전수 검토

- 검토일: 2026-08-25
- 검토자: sol
- 기준: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v8 전 조항**
- 규칙집 SHA-256: `3709e235eb5c7db5d667ea1d609e27765f9e54be39ce250bd8c28aac8eb4d93c`
- 대상: `clickhouse`, `cloudflare`, `codeit`, `cohere`, `coinbase`
- 대조 파일: 각 canonical `web/references/<id>/DESIGN.md`와 current migrated `DESIGN.md`, `provenance.md`, `migration-log.md`, `audit-log.md` 전부
- E3 확인 범위: `worker-log.txt`, `auditor-log.txt`는 게이트 회피 의도·행위 확인에만 사용
- Proof 범위: canonical 본문 또는 current migration packet이 경로·SHA·내용으로 명시 채택한 canonical Proof만 포함

## 판정 요약

첫 전수 판정은 기대대로 **FAIL 5/5, PASS 0/5**다. 다섯 건 모두 재제출이 필요하다.

다섯 건 모두 `--gate-only`, portable Core, fresh F3의 별도 실행과 current `DESIGN.md` SHA 결속은 통과했다. 그러나 이 기계·절차 증거는 채택 Proof 손실, source 관계 손실, evidence-kind 승격, exact-role state 판정, 새 negative coverage, 원장 불일치와 E3 우회를 보증하지 않는다. 특히 `codeit`과 `coinbase`는 작업 로그가 오탐을 보고하거나 게이트를 고치지 않고 줄을 나눠 통과시켰다고 명시하므로 **E3 FAIL**이다.

| id | 판정 | 주된 차단 조항 |
|---|---|---|
| clickhouse | **FAIL** | A1/A1c/A4, B1, C4, D1, E1/E2/E2a/E2c, F2 |
| cloudflare | **FAIL** | A1, B2/B2a, C1/C2/C4, D1, E1/E2/E2c, F1/F2 |
| codeit | **FAIL** | A1/A3/A4, B1, D1, E1/E2/E2c/**E3**, F2 |
| cohere | **FAIL** | A1/A1b, C4, D1, E1/E2/E2c, F2 |
| coinbase | **FAIL** | A1, B1, C1/C2/C4, D1/D2, E1/E2/E2c/**E3**, F2 |

## Proof 포함·제외 경계

- **clickhouse:** current packet이 `web/references/clickhouse/.verification.md`의 경로와 SHA를 명시 채택한다(`provenance.md:92,159`, `migration-log.md:12`). 해당 sidecar를 포함했다.
- **cloudflare:** canonical/current packet이 별도 `.verification.md` 또는 hidden asset Proof를 채택하지 않는다. canonical `DESIGN.md`만 source로 사용했다.
- **codeit:** `web/references/codeit/.verification.md`는 존재하지만 canonical 본문에 pointer가 없고 current packet도 비채택을 명시한다(`provenance.md:154`). 내용을 제외했다.
- **cohere:** current packet이 `web/references/cohere/.verification.md`를 exact SHA와 함께 채택한다(`provenance.md:88-92,184`). 해당 sidecar를 포함했다. 다만 canonical `DESIGN.md:294-299` footer 자체는 sidecar나 raw artifact를 가리키지 않는다.
- **coinbase:** `web/references/coinbase/.verification.md`는 실제 존재하지만 canonical 본문과 current packet이 채택하지 않는다. 내용을 제외했다. 따라서 올바른 경계는 “sidecar가 없음”이 아니라 “존재하지만 이 packet의 source로 채택되지 않음”이다.

## 기계 검사와 F3 증거

재실행 명령:

- `node test-v2/tools/migrate-reference.mjs --brand <id> --gate-only`
- `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/<id>/DESIGN.md --check --require-portable-core --json`

| id | gate-only | portable Core | current DESIGN.md SHA-256 | fresh F3 |
|---|---|---|---|---|
| clickhouse | PASS, `problems: []` | exit 0, `portable_core: true` | `5f370bd76b23af69c60a525b178b836c8deeda52765ce2c741c1c272f77e6ba9` | `AUDIT_DONE fixes=20` |
| cloudflare | PASS, `problems: []` | exit 0, `portable_core: true` | `411c17c271d94896f2482763931fe6ef39f1ec5aa1eb9e0d588008a5f53256a4` | `AUDIT_DONE fixes=39` |
| codeit | PASS, `problems: []` | exit 0, `portable_core: true` | `f1979590ce4497919385d860d856c45981b624530115f716b287e08c122b5cda` | `AUDIT_DONE fixes=20` |
| cohere | PASS, `problems: []` | exit 0, `portable_core: true` | `b92f263ec9f289e831f8e138928ee5a989fae6121c6515c2a48c6ebece0fefa1` | `AUDIT_DONE fixes=18` |
| coinbase | PASS, `problems: []` | exit 0, `portable_core: true` | `0749f48768b46679048e8e514c8945eb63584284243c973d6a5dda496b37b390` | `AUDIT_DONE fixes=17` |

다섯 건 모두 이관 워커와 분리된 fresh F3 실행 형식과 current SHA 결속은 확인됐다. 따라서 F3는 **실행 증거로 PASS**다. 이는 감사가 B2a/E2 결함을 모두 잡았거나 현재 의미 결과가 PASS라는 뜻이 아니다. `codeit`과 `coinbase`의 현재 gate PASS도 E3 줄 분리 뒤의 결과여서 준수 증거로 사용할 수 없다.

## E3 전수 판정

| id | E3 | 근거 |
|---|---|---|
| clickhouse | PASS | spaced/malformed hex, literal 변조, 회피 작업 로그를 발견하지 못함 |
| cloudflare | PASS | lowercase YAML hex의 uppercase 표기는 유효한 CSS case-normalization이고 gate도 lowercase 비교를 사용함; 회피 정황 없음 |
| codeit | **FAIL** | `worker-log.txt:1`이 `focus-visible`+hex 오탐을 문장 분할로 없앴다고 자인; 현재 `DESIGN.md:94,236`에 순서·용어 변경 흔적 |
| cohere | PASS | literal 왜곡이나 오탐 회피 정황 없음 |
| coinbase | **FAIL** | `worker-log.txt:1`이 `focus-visible`+hex 오탐을 “그 줄을 나누고” 통과시켰다고 자인 |

Cloudflare의 `#e2700b`→`#E2700B` 같은 변경은 값·형태가 유효하고 `migrate-reference.mjs:96-101`도 hex를 소문자로 정규화해 비교한다. v8 E3가 금지한 `# faf9f5`형 invalid 표기나 게이트 정규식 순서에 맞춘 우회와는 다르다.

## Rulebook v8 전 조항 매트릭스

PASS는 해당 조항의 전수 대조 결과이며 문서 전체 PASS를 뜻하지 않는다. F3 PASS는 실행 증거만 뜻한다.

| 조항 | clickhouse | cloudflare | codeit | cohere | coinbase |
|---|---|---|---|---|---|
| A1 값 손실 0 / 발명 0 | FAIL | FAIL | FAIL | FAIL | FAIL |
| A1a unitless ratio | PASS | PASS | PASS | PASS | PASS |
| A1b verified primitive 보존 | PASS | PASS | PASS | FAIL | PASS |
| A1c metadata | FAIL | PASS | PASS | PASS | PASS |
| A2 §14 보존 / §9 prompt 삭제 | PASS | PASS | PASS | PASS | PASS |
| A3 §9-only 고유값 | PASS | PASS | FAIL | PASS | PASS |
| A4 field-role 비결합 | FAIL | PASS | FAIL | PASS | PASS |
| B1 evidence kind 비승격 | FAIL | PASS | FAIL | PASS | FAIL |
| B2 derived 해석 portable 동반 | PASS | FAIL | PASS | PASS | PASS |
| B2a 인접·완전 evidence-class 한정 | PASS | FAIL | PASS | PASS | PASS |
| B3 motion 5종 gate | PASS | PASS | PASS | PASS | PASS |
| C1 capture absence ≠ N/A | PASS | FAIL | PASS | PASS | FAIL |
| C2 exact role별 L/E/S | PASS | FAIL | PASS | PASS | FAIL |
| C3 coverage 완료 주장 금지 | PASS | PASS | PASS | PASS | PASS |
| C4 kind 근거와 kind/map | FAIL | FAIL | PASS | FAIL | FAIL |
| D1 새 negative coverage 금지 | FAIL | FAIL | FAIL | FAIL | FAIL |
| D2 fictional persona / 독립 task | PASS | PASS | PASS | PASS | FAIL |
| E1 provenance/portable evidence boundary | FAIL | FAIL | FAIL | FAIL | FAIL |
| E2 source-row 실제 disposition | FAIL | FAIL | FAIL | FAIL | FAIL |
| E2a 모든 다중 목적지 | FAIL | PASS | PASS | PASS | PASS |
| E2b omission ledger | PASS | PASS | PASS | PASS | PASS |
| E2c 준수 주장 강도 | FAIL | FAIL | FAIL | FAIL | FAIL |
| E3 gate 회피 금지 | PASS | PASS | FAIL | PASS | FAIL |
| F1 최종 B2a scan 결과 | PASS | FAIL | PASS | PASS | PASS |
| F2 최종 E2 grep 결과 | FAIL | FAIL | FAIL | FAIL | FAIL |
| F3 별도 세션 1회 실행 | PASS | PASS | PASS | PASS | PASS |
| portable Core 필수 검사 | PASS | PASS | PASS | PASS | PASS |

## 1. clickhouse — FAIL

### 1.1 채택한 canonical Proof의 raw 값·lineage·metadata 손실 — A1 / A1c / A4 / E1 / E2 / E2c / F2 FAIL

Current packet이 채택한 canonical `.verification.md`의 다음 기록이 네 migrated 문서에 없다.

- artifact path, exact capture timestamp, supplied-artifact-only method와 no-recapture/MCP 경계: canonical `.verification.md:5`
- `3 routes / 8 component types / 80 variants / 9 states / 6 interactions / coverage 100`: `:15`
- selector-bound raw literals와 tuple: `home::body`, exact `rgb(...)`/`lab(...)` 값들: `:21-27`
- pricing primary의 Proof-only `44px` height: `:23,43,55`
- cookie dialog의 Proof-only `24px 0px` padding: `:27`
- brand guide `updated October 2025`, Klim/license/source-code-license 경계, independent-docs-chrome 미검수 경계: `:35,67,70,72,74`

Current `provenance.md:49-127,157-173`은 URL·SHA와 일부 요약만 보존한다. `migration-log.md:12,90`, `audit-log.md:4,98-102`의 Proof/E2 current-class 주장은 실제보다 강하다. Proof-only selector tuple은 global portable token과 합치면 안 되지만 provenance에서 byte-exact 관계를 보존해야 한다.

### 1.2 source 밖 imagery·locale coverage 신설 — A1 / B1 / D1 / E1 / E2 / E2c / F2 FAIL

- Canonical logo evidence는 Simple Icons metadata뿐이다(canonical `DESIGN.md:8-10`). Proof에도 imagery capture/ownership 기록이 없다. Migrated `DESIGN.md:169`은 `captured public-route imagery`를 발명하고 `provenance.md:153`, `migration-log.md:43`, `audit-log.md:38,85`가 이를 승인한다.
- Canonical Voice는 writing guidance만 기록한다(canonical `DESIGN.md:289-298`). Migrated `DESIGN.md:432,480`은 complete locale profile, unobserved locale behavior, synthetic samples라는 새 negative domain을 만든다. 대응 과대 원장은 `provenance.md:153`, `migration-log.md:29,43`, `audit-log.md:48,71,84`다.

공개 route에 이미지가 보인다는 사실은 asset ownership evidence가 아니다. 인접 B2a 한정도 source 밖 negative claim을 허용하지 않는다.

### 1.3 Select Menu·Cookie Dialog surface에 kind/map 발명 — A1 / A4 / C4 / E2 / E2c / F2 FAIL

- Canonical Select Menu는 expanded visual surface와 named open/selected 관측만 기록한다(canonical `DESIGN.md:216-223`; `.verification.md:25,58`). Proof는 generic select states를 claim하지 않는다고 명시한다.
- Cookie Dialog도 consent chrome surface와 named open 관측뿐이다(canonical `DESIGN.md:231-238`; `.verification.md:27,61`).
- Migrated는 별도 Trigger와 구분된 Select Menu surface에 `Kind: interactive`와 generic map을 붙이고(`DESIGN.md:315-340`), Cookie Dialog surface에도 붙인다(`:366-388`). `DESIGN.md:190`은 두 surface의 YAML type 부재를 인정하지만 surface 자체의 interactive-kind 근거는 제시하지 않는다.

`provenance.md:170-171`, `migration-log.md:23,33`, `audit-log.md:9,98`의 C4/current 주장은 실제보다 강하다.

### 1.4 §11 source-row의 Content 다중 목적지 누락 — E2 / E2a / E2c / F2 FAIL

Canonical §11의 “direct minimalism”과 “built for engineers and trusted by leaders”(`DESIGN.md:304`)은 migrated Scope뿐 아니라 official wording samples에도 간다(migrated `DESIGN.md:19,428,430`). 그러나 `migration-log.md:30`의 §11 disposition은 Scope만 기록하고 `:29`의 §10 행이 wording samples 전체를 뭉뚱그린다. §11 → Scope + Content 실제 이중 목적지를 source-row별로 기록해야 한다.

### clickhouse 재제출 조건

1. 채택 Proof의 lineage, counts, exact raw literals와 selector-bound tuples를 provenance에 byte-exact 보존한다. `44px`, cookie `24px 0px`, raw `rgb/lab`는 global portable token으로 합치지 않는다.
2. 근거 없는 imagery 및 locale/synthetic-sample 문장과 Named gaps를 삭제하거나 독립 canonical evidence를 결속한다.
3. Select Menu surface와 Cookie Dialog surface의 kind/map을 생략하고 named observed states와 visual tuple만 유지한다. 유지하려면 surface 자체의 interactive-kind 증거가 필요하다.
4. §11의 Scope·Content 다중 목적지와 Proof disposition을 provenance/migration/audit/F2에 동기화한다.
5. 새 SHA를 기록하고 gate-only와 portable Core를 재실행한다.

통과 확인: A1a/A1b, §14와 §9 처리, B2/B2a/B3, C1-C3, D2, E2b, F1, E3, fresh F3 실행, portable Core는 통과했다.

## 2. cloudflare — FAIL

### 2.1 C4의 field 자체 생략 불이행과 근거 없는 kind 확정 — A1 / C4 / E2c FAIL

- `DESIGN.md:446,634`가 `Type: omitted`을 실제 값처럼 방출한다.
- `DESIGN.md:621,635,647,660,674,719,734,749`가 `Kind: omitted`을 방출한다.
- `DESIGN.md:687,698`은 explicit kind evidence 없이 `Kind: non-interactive`를 확정한다.

C4의 생략은 field 자체를 쓰지 않는 것이다. `migration-log.md:16,34`, `audit-log.md:9,130`의 “생략/C4 준수” 주장은 실제보다 강하다.

### 2.2 확인된 interaction kind와 signature-motion 관계 손실 — A1 / C4 / E2 / E2c / F2 FAIL

- Canonical `DESIGN.md:607`은 Marketing Feature Card hover가 “surface is interactive”임을 알리는 cue라고 명시한다. Migrated `DESIGN.md:202`가 꼬리를 삭제하고 `:294,635`는 interactive-kind 확인이 없다고 뒤집는다.
- Canonical `:245-247`의 Pagination은 numbered controls as compact buttons다. Migrated `:294,761`은 kind/map을 닫지 않는다.
- Canonical `:607-610`의 accessibility-first focus ring/never suppress, toast exit faster than enter, status never hard-swaps so change remains perceivable 관계가 migrated `:202-205`에서 빠졌다. `migration-log.md:35`는 signature motions 5개를 유지했다고 과대 기록한다.

확인된 interaction evidence와 의미 관계는 복원하되 미해상 L/E/S treatment만 최소 필드에서 생략해야 한다.

### 2.3 exact role과 paint 위치를 혼동한 state 판정 — C1 / C2 / E2 / E2c / F2 FAIL

- Primary `DESIGN.md:327-328`, Destructive `:398-399`: error/success가 toast/page에 그려진다는 이유로 N/A. Paint 위치는 Save/Deploy/Delete/Purge action outcome applicability의 사유가 아니다.
- Ghost `:374-376`, Dark CTA `:461-463`: mixed/generic role을 navigation으로 좁혀 L/E/S를 닫았다. exact request/destination이 미해상이므로 split 또는 field omission이 필요하다.
- Text/Mono/Select `:518,520,543,545,567,569`: error만 exact role로 연결되고 loading/success는 미해상인데 capture/source paint 부재로 N/A 처리했다.

`migration-log.md:34`, `audit-log.md:9,130`의 C1/C2 준수 주장도 함께 고쳐야 한다.

### 2.4 source 밖 negative domains — D1 / E1 / E2 / E2c / F2 FAIL

원본은 Inter/JetBrains 문서 근거와 Simple Icons slug만 제공한다. 다음 부정 coverage는 source에 없다.

- native-app typography/chrome: `DESIGN.md:9,216,224,853`
- Cloudflare-exclusive distributed font: `:216,222`
- first-party mark-file 부재: `:262-264,852`; `provenance.md:23`; `migration-log.md:13`; `audit-log.md:9`
- non-English locale profile 부재: `DESIGN.md:812`

독립 탐색 증거 없이 exclusivity, native-app, first-party file 존재 여부, locale coverage를 부정할 수 없다.

### 2.5 asset editorial rule의 불완전·비인접 한정 — B2 / B2a / F1 FAIL

`DESIGN.md:266`의 한정은 새 `do-not-replace...` 문장만 이름 붙이고 앞의 “logomark never recolors”와 “partner logos grayscale” 규칙은 이름 붙이지 않는다. 같은 규칙이 `:791`에 한정 없이 반복되며 `provenance.md:125`, `migration-log.md:45`, F3 inventory도 `:791`을 놓쳤다. 새 `do-not-replace` 금지문은 canonical 근거도 없다.

### 2.6 card cardinality 원장 오류 — E2 / F2 FAIL

Canonical YAML의 `type: card`는 card/stat-card/surface/table/tooltip/code-block 총 6개다. Migrated는 surface type을 `DESIGN.md:629`에 남겼으나 `migration-log.md:16,62`, `audit-log.md:9`는 card×5로 기록한다.

### cloudflare 재제출 조건

1. 모든 `Type: omitted`/`Kind: omitted` field를 삭제하고 근거 없는 `non-interactive`도 생략한다.
2. Marketing Feature Card와 Pagination의 확인된 interaction evidence와 역할 기반 map을 복원한다.
3. Primary/Destructive/Ghost/Dark CTA/Text/Mono/Select의 L/E/S를 exact role 기준으로 재판정한다.
4. native-app, exclusive font, first-party mark-file, locale negative와 관련 Named gaps를 삭제한다.
5. asset rules에 인접 완전 B2a를 적용하고 source 밖 replacement 금지문을 삭제한다. signature-motion 관계를 복원한다.
6. card×6을 포함해 provenance, migration-log, audit current-class, F1/F2와 SHA를 actual disposition에 맞춘 뒤 gates를 재실행한다.

E3 확인: lowercase→uppercase hex는 유효한 normalization이며 회피 정황이 없어 PASS다. A1a/A1b/A1c, A2-A4 중 A2-A4, B1/B3, C3, D2, E2a/E2b, F3 실행, portable Core도 통과했다.

## 3. codeit — FAIL

### 3.1 명시적 gate 회피 — E3 / E2c FAIL

`worker-log.txt:1`은 “게이트가 `focus-visible`과 hex가 같은 줄에 있는 문장을 잡았습니다. 해당 B2a 문장을 나눠 B1 오탐을 없앱니다.”라고 기록한다. 현재 흔적은 `DESIGN.md:94`의 `#760dde`가 `focus-visible`보다 먼저 오도록 한 순서와 `:236`의 literal `focus-visible`을 `keyboard-focus`로 바꾼 표기다. `:236`만 `focus-visible`로 되돌린 in-memory gate도 `focus-visible-promotion`으로 BLOCK된다.

오탐을 그대로 보고하고 gate를 state-row-aware하게 고치지 않은 채 문장 배열·동의어로 PASS를 만든 것은 v8 E3가 금지한 정확한 행동이다. `migration-log.md:9`, `audit-log.md:101`은 incident를 보고하지 않고 PASS만 기록한다.

### 3.2 source form·§9 local recipe·field binding 손실 — A1 / A3 / A4 / E2 / E2c / F2 FAIL

- Canonical `DESIGN.md:80,131`의 별도 source form `-1px`가 packet 전체에서 사라지고 migrated `DESIGN.md:44,195`의 `-1.0px`로 합쳐졌다.
- Canonical `:262`의 Medium range `8px–10px`가 사라지고 migrated `:118`의 slug형 `8px-10px`와 `:121`의 endpoint 설명으로 변형됐다.
- Canonical §9 `:342`의 white nav 안 **right-aligned** membership chip 관계가 migrated `:266-280,497-509`에 없다. `migration-log.md:28`은 §9-only 고유값을 0이라고 과대 기록한다.
- Canonical Course Card의 local `flat, hairline outline` / `no shadow`(`:214,341`)가 current component `DESIGN.md:435-447`에서 빠졌다. generic Elevation `:131`은 Course Card local field binding의 대체물이 아니다.

### 3.3 Image Behavior 관계 손실과 asset authority 승격 — A1 / B1 / E1 / E2 / E2c / F2 FAIL

Canonical `:323`은 feature illustrations/course thumbnails의 20–24px corner가 **at all sizes** 유지된다고 한다. Migrated `DESIGN.md:211`은 이를 “at the sizes named”로 바꿔 responsive invariance를 pixel-size 문장으로 오독했고 `:509`도 복원하지 않는다. 같은 `:211`은 원본이 ownership을 검증하지 않은 자산을 `first-party catalog imagery`로 승격한다. Official surface에 보인다는 사실은 asset ownership 증거가 아니다.

### 3.4 source evidence kind 발명 — A1 / B1 / E1 / E2 / E2c / F2 FAIL

Canonical `:238,450-452`가 확인한 것은 `tech.codeit.kr` official engineering blog와 `about.codeit.com/ko/` company intro다. Migrated `DESIGN.md:17`은 이를 근거 없는 `official history ... pages`로 바꾼다. `provenance.md:61-62,80-81`은 오히려 정확한 tech-blog/about class여서 portable과도 모순된다. `migration-log.md:44`, `audit-log.md:18,58,79`가 false history class를 current fix로 승인한다.

### 3.5 source 밖 negative coverage — D1 / E1 / E2 / E2c / F2 FAIL

Canonical typography와 source footer는 observed Pretendard/Spoqa, fallbacks, named sources만 제공한다. 다음 coverage audit은 없다.

- universal typography sheet / Codeit-exclusive distributed family / license / loaded fallback / 외부 host typography: `DESIGN.md:170,174,177-179,186,573`
- captured first-party mark-file gap: `DESIGN.md:574`; Google favicon lookup은 file 존재·ownership 부정을 지지하지 않음
- complete product-microcopy guide / locale profile / unobserved locale: `DESIGN.md:514,534,577`

B2a 한정은 D1을 치유하지 않는다. Google favicon의 lookup/not-captured/not-portable 경계 자체는 유지할 수 있다.

### 3.6 Tier-2 source literal 변조 — A1 / E1 / E2 / E2c / F2 FAIL

Canonical `DESIGN.md:238`은 `getdesign.md/codeit`, `styles.refero.design` 형태만 기록한다. `provenance.md:75-76`은 source에 없는 `https://` scheme을 붙였다. Source ledger는 plausible scheme을 발명할 수 없다.

### codeit 재제출 조건

1. E3 incident와 exact false-positive repro를 숨기지 않고 기록한다. `focus-visible`/hex의 자연스러운 B1 경계를 복원하고 gate를 row-aware하게 고친 뒤 재실행한다. 문장 순서·동의어 회피는 금지한다.
2. `-1px`, `8px–10px`, right-aligned nav-chip 관계, Course Card flat/no-shadow local field를 exact source-role로 복원한다. §9 prompt wrapper는 복원하지 않는다.
3. Image Behavior의 `at all sizes`를 복원하고 first-party ownership을 제거한다.
4. `official history`를 actual engineering-blog/company-intro evidence class로 교정한다.
5. unsupported font/mark/microcopy/locale negatives와 대응 Named gaps를 삭제하거나 각각 independent canonical evidence를 결속한다.
6. Tier-2 strings를 canonical form 그대로 복원한다.
7. affected provenance/migration/audit current-class/F2와 SHA를 동기화하고 수정된 gate와 portable Core를 재실행한다.

통과 확인: A1a/A1b/A1c, A2, B2/B2a/B3, C1-C4, D2, E2a/E2b, F1, fresh F3 실행 형식, portable Core는 통과했다.

## 4. cohere — FAIL

### 4.1 채택 Proof의 anchor primitive 3건 누락 — A1 / A1b / E2 / E2c / F2 FAIL

Canonical sidecar `.verification.md:57-59`는 Dark public action, Inverse public action, Outlined pricing action을 각각 `anchor`로 확인한다. Current `DESIGN.md:183-250`은 세 컴포넌트에 `Kind: interactive`만 두고 `Type: link/anchor` primitive를 생략했다. `provenance.md:114`는 anchor 사실을 알고 있으면서 `migration-log.md:16,21`, `audit-log.md:9`은 YAML type이 없다는 이유로 primitive 보존을 완료했다고 닫는다. A1b는 YAML field에 한정되지 않는다.

### 4.2 Home marketing media의 interactive-kind를 미확정으로 뒤집음 — A1 / C4 / E2 / E2c / F2 FAIL

Canonical `DESIGN.md:212`는 해당 surface를 `Home-only interactive media-card container`라고 직접 명시한다. Current `DESIGN.md:181,286,290`은 `interactive`를 단순 Use 형용사로 낮추고 kind/map을 생략한다. 같은 오판이 `provenance.md:179,193,198`, `migration-log.md:21,31`, `audit-log.md:9,94,101`에 반복된다. 확인된 interactive kind와 역할 기반 map을 복원하고, 미해상 L/E/S field만 최소 경계에서 생략해야 한다.

### 4.3 canonical footer가 sidecar/raw artifact를 연결한다는 false lineage — A1 / E1 / E2 / E2c / F2 FAIL

Canonical footer `web/references/cohere/DESIGN.md:294-299`에는 Verified date, Tier 1/2 URLs, conflicts만 있고 raw artifact 또는 `.verification.md` pointer가 없다. Raw artifact pointer는 canonical sidecar `.verification.md:5`에 있다.

Current packet은 sidecar를 exact SHA로 명시 채택했으므로 그 Proof 내용 자체는 평가 source에 포함된다(`provenance.md:88-92,184`). 그러나 다음은 출처 위치를 틀리게 기록한다.

- `provenance.md:188`: “Canonical footer Verified / Tier 1 including raw artifact ... / `.verification.md` ... (canonical DESIGN.md footer)”
- `migration-log.md:33`: canonical footer에 raw artifact와 `.verification.md`가 있다는 동일 주장

정확한 lineage는 “current packet이 sibling sidecar를 채택했으며 raw artifact pointer는 sidecar `:5`에서 왔다”이다. 현재 Proof 원장의 수치·tuple 보존 자체는 통과했지만 source relationship은 고쳐야 한다.

### 4.4 source 밖 copy·font negative coverage — D1 / E1 / E2 / E2c / F2 FAIL

- `DESIGN.md:322`는 “Unobserved signed-in, support, or legal copy rules remain unnamed”을 추가한다. Sidecar는 signed-in application/documentation chrome 미캡처를 말하지만 support/legal copy coverage audit은 하지 않았다. `migration-log.md:27`, `provenance.md:179`, `audit-log.md:48`도 이를 승인한다.
- `DESIGN.md:368`의 `Cohere-exclusive redistributable brand font`는 sidecar가 지지하는 exact 결과보다 넓다. Sidecar `:38,75`는 “CohereText/CohereMono의 standalone first-party public license/type specimen을 이 discovery pass에서 찾지 못함”까지만 지지한다. `provenance.md:124,184`의 exact statement는 유지할 수 있지만 broad exclusive-brand-font gap은 삭제하거나 좁혀야 한다.

### cohere 재제출 조건

1. Dark/Inverse/Outlined action 세 건에 sidecar-confirmed `Type: link` 또는 exact `anchor` primitive를 컴포넌트별로 복원한다.
2. Home marketing media에 `Kind: interactive`와 역할 기반 applicability map을 복원한다. 미해상 loading/error/success 등은 최소 field에서 생략한다.
3. canonical footer false attribution을 제거하고 current packet → sibling sidecar → sidecar raw artifact pointer의 exact lineage로 `provenance.md:188`, `migration-log.md:33`, audit current-class/F2를 동기화한다.
4. support/legal copy negative를 삭제한다. Signed-in application/documentation chrome에 대해서는 canonical sidecar가 실제로 기록한 capture boundary만 유지한다.
5. broad `Cohere-exclusive redistributable brand font`를 삭제하거나 sidecar의 exact CohereText/CohereMono discovery result로 좁힌다.
6. 새 SHA를 기록하고 gate-only와 portable Core를 재실행한다.

통과 확인: A1a/A1c, §9/§14, evidence-kind와 B2a, motion gate, C1-C3, D2, E2a/E2b, F1, E3, fresh F3 실행, portable Core는 통과했다.

## 5. coinbase — FAIL

### 5.1 §8 component field·relation 손실 — A1 / E1 / E2 / E2c / F2 FAIL

Canonical §8의 다음 verified details와 관계가 packet 네 문서 전체에서 빠졌다.

- Data Table의 `row/column spans`: canonical `DESIGN.md:230`; migrated Data Table `DESIGN.md:527-536`
- Charts의 `XAxis` / `YAxis` / `Scrubber`: canonical `:234`; migrated Sparkline `:641-650`
- ProgressCircle의 determinate `% overlay`, ProgressBar의 fixed/floating label variants: canonical `:248`; migrated Progress `:630-639`
- CDS가 `open-sourced`됐다는 관계: canonical `:190`; packet 전체 0 hit

`migration-log.md:28`은 §8을 Components & States로 옮겼다고 닫으므로 actual disposition보다 강하다.

### 5.2 §11 public-history 값·관계 손실 — A1 / E1 / E2 / E2c / F2 FAIL

Canonical `DESIGN.md:298`의 다음 details가 migrated `DESIGN.md:17`, `provenance.md:97`, `migration-log.md:32`의 압축 목록에서 빠졌다.

- Fred Ehrsam이 former Goldman Sachs FX trader라는 관계
- Ehrsam이 Reddit에서 post를 보고 응답했고 Hacker News post 자체가 viral했다는 관계
- Y Combinator S12의 `$150K`
- IPO peak가 `$100B`에 접근했다는 수치
- `most-regulated US crypto exchange`와 regulatory caution → positioning advantage 관계
- Coinbase Wallet의 `non-custodial`, Coinbase Prime의 `institutional` product-role qualifier

서사 값은 UI token이 아니지만 source-backed narrative이므로 손실해도 된다는 뜻은 아니다. Portable에는 derived 관계의 인접 완전 B2a를 유지하고, 필요하면 provenance에 exact source 관계를 분리해야 한다.

### 5.3 evidence kind 승격과 독립 검증되지 않은 exact task mapping — B1 / D2 / E1 / E2 / E2c / F2 FAIL

- `DESIGN.md:145,150`은 canonical이 입증하지 않은 `live-measured type sizes on coinbase.com`을 Live computed surface-use로 승격한다. Canonical은 named families/type metrics를 기록하지만 이 문장의 exact live-computed evidence chain은 제공하지 않는다.
- `DESIGN.md:25-29`는 canonical top-nav의 Sign up/search 존재(`canonical DESIGN.md:208`)와 별도 YAML component `use`를 결합해 Sign-up=`High-emphasis Primary Button`, top-nav search=`SearchInput`이라는 exact live mapping을 만든다. Hidden sidecar는 Sign-up mapping 일부를 입증하지만 current evidence boundary 밖이므로 이 packet의 B1/D2 근거가 될 수 없다.

Task는 canonical이 독립적으로 입증하는 “top-nav Sign up/search” 수준으로 낮추거나, 별도 승인으로 Proof를 정식 채택해 그 source lineage와 값을 모두 보존해야 한다.

### 5.4 실제 존재하는 sidecar를 없다고 기록 — D1 / E1 / E2 / E2c / F2 FAIL

`web/references/coinbase/.verification.md`는 실제 존재하며 SHA-256은 `af3f4ed1e9c8feb74375e18388a65d4f6165e57543e08ea05e42868871eb8b8e`다. 다만 canonical `DESIGN.md`와 current packet이 이를 채택하지 않으므로 내용은 이번 source에서 제외했다.

- `provenance.md:31`: “Source has no `.verification.md` sidecar”
- `migration-log.md:17`: 동일 false absence
- `audit-log.md:9`: sidecar 없음으로 current decision 승인

비채택 경계와 파일 존재 여부는 다르다. “canonical/current packet이 sidecar를 연결·채택하지 않는다”로 고쳐야 하며, sidecar 내용을 조용히 source로 편입해서는 안 된다.

### 5.5 source 밖 font·locale negative coverage — D1 / E1 / E2 / E2c / F2 FAIL

Canonical typography는 named families와 metrics를 제공할 뿐 font distribution/license coverage audit을 하지 않는다.

- `DESIGN.md:145-155`: no FontFace/source URL, no Coinbase webfont URL, Declared-only None, no license URL
- `DESIGN.md:791`: CoinbaseDisplay/CoinbaseSans/CoinbaseText/CoinbaseIcons FontFace/source URL and license URL gap
- `DESIGN.md:749,793`: English 외 complete locale profile 부재

이 부정문들은 “이 packet에 기록되지 않음”을 새로운 조사 결과처럼 승격한다. Canonical에 없는 domain은 최소 unresolved field에서 생략해야 한다.

### 5.6 Banner success와 List Cell kind/map 오류 — C1 / C2 / C4 / E2 / E2c / F2 FAIL

Migrated Banner `DESIGN.md:628`은 `success | not-applicable | ... success is not a named banner variant`라고 쓴다. C1은 `not named`를 N/A 사유로 명시 금지한다. Banner의 exact role은 feedback/status messaging이므로 success confirmation이 의미상 불가능하다고 단정할 수도 없다. Exact semantic-role evidence를 제공하지 못하면 success field를 최소 경계에서 생략해야 한다. `migration-log.md:35`의 C1/C2 준수 주장도 실제보다 강하다.

Canonical `DESIGN.md:66,232`의 List Cell은 primitive `type: listItem`, media/title/subtitle anatomy와 mobile substitute만 입증한다. Interactive-kind 근거가 없는데 migrated `DESIGN.md:541,548-555`가 `Kind: interactive`와 applicability map을 발명했다. C4에 따라 kind와 map 자체를 생략해야 한다.

### 5.7 명시적 gate 회피 — E3 / E2c FAIL

`worker-log.txt:1`은 “게이트가 `focus-visible`과 hex를 같은 줄에서 잡습니다. 그 줄을 나누고...”라고 기록한다. 오탐을 그대로 보고하거나 gate를 수정하지 않고 line split으로 PASS를 만든 행위다. 현재 hex literal set이 exact하더라도 과정 증거가 명시적이므로 E3 FAIL이다. False-positive의 exact repro를 남기고 gate 쪽에서 처리한 뒤 자연스러운 source/B1 문장 경계를 복원해야 한다.

### coinbase 재제출 조건

1. CDS open-sourced 관계, Data Table row/column spans, Chart XAxis/YAxis/Scrubber, ProgressCircle `% overlay`, ProgressBar fixed/floating labels를 source field/relationship 그대로 복원한다.
2. §11의 직업·경로·수치·인과 및 Wallet/Prime qualifier를 narrative/provenance에 정확히 보존하고 source-row disposition을 맞춘다.
3. typography evidence class를 canonical 수준으로 낮추고 Primary tasks의 exact live-component mapping도 입증된 top-nav task 수준으로 축소한다.
4. sidecar false absence를 “존재하지만 canonical/current packet이 채택하지 않음”으로 교정한다. 별도 승인 없이 hidden sidecar의 Proof를 current source로 편입하지 않는다.
5. unsupported FontFace/distribution/license/Declared-only/locale negatives와 Named gaps를 삭제한다.
6. Banner success는 exact role로 재판정하고 미해상이면 field 자체를 생략한다. List Cell의 kind/map도 interactive-kind evidence가 없으면 생략한다.
7. gate-directed 줄 분리를 원복하고 exact false-positive repro를 기록해 gate를 고친 뒤 우회 없는 표현으로 재실행한다.
8. affected provenance/migration/audit current-class/F2와 SHA를 실제 disposition에 맞춘 뒤 gates를 재실행한다.

통과 확인: A1a/A1b/A1c, A2-A4, B2/B2a/B3, C3, E2a/E2b, F1, fresh F3 실행, portable Core는 통과했다.

## 결론 및 재제출 프로토콜

- 웨이브 16 첫 판정: **FAIL 5/5, PASS 0/5**.
- 재제출은 위 고정 목록만 수정한 뒤 **동일 sol 레인**으로 다시 제출한다.
- 고정 목록 한정 개정이면 기존 fresh F3 실행 이력 자체는 유지할 수 있다. 다만 affected B2a/E2 current-class와 F1/F2는 실제 개정본에 맞춰 다시 확인해야 한다.
- `codeit`과 `coinbase`는 E3 incident와 false-positive repro를 기록하고 gate를 고친 뒤 우회하지 않은 자연스러운 표현으로 재실행해야 한다.
- 다섯 건 모두 새 SHA, gate-only, portable Core 결과를 동기화한다.
- 이 판정은 migrated packet 검토이며 카탈로그 채택 승인이 아니다. **5/5 PASS 전에는 웨이브 완료가 아니다.**
