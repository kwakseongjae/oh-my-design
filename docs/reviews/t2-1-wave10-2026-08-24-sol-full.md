# T2-1 웨이브 10 sol 전수 검토

- 검토일: 2026-08-24
- 검토자: sol
- 기준: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7 전 조항
- 대상: `autopedia`, `aws-cloudscape`, `bahamut`, `banksalad`, `barogo`
- 대조 범위: 각 canonical `web/references/<id>/DESIGN.md`와 migrated `DESIGN.md`, `provenance.md`, `migration-log.md`, `audit-log.md` 전부

## 판정 요약

첫 전수 판정은 **FAIL 5/5, PASS 0/5**다. `--gate-only`는 5건 모두 통과했지만 의미 보존·evidence authority·source-row disposition 결함이 남았다. AWS Cloudscape와 Banksalad는 portable Core 필수 검사도 실패한다.

| id | 판정 | 주된 차단 조항 |
|---|---|---|
| autopedia | **FAIL** | A1/A3/A4, C2, D1, E1/E2/E2a/E2c, F1/F2 |
| aws-cloudscape | **FAIL** | A1, B2/B2a, D1, E1/E2/E2a/E2c, F1/F2, Core |
| bahamut | **FAIL** | A1/A2, B2/B2a, D1, E1/E2/E2c, F1/F2 |
| banksalad | **FAIL** | A1/A4, B2/B2a, D1, E1/E2/E2c, F1/F2, Core |
| barogo | **FAIL** | A1/A3/A4, B1, D1/D2, E1/E2/E2c, F2 |

## 기계 검사와 F3 증거

재실행 명령:

- `node test-v2/tools/migrate-reference.mjs --brand <id> --gate-only`
- `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/<id>/DESIGN.md --check --require-portable-core --json`

| id | gate-only | portable Core | current DESIGN.md SHA-256 | 기존 fresh F3 |
|---|---|---|---|---|
| autopedia | PASS, problems `[]` | exit 0, `portable_core: true` | `157f4ea8f769f4e77580ea599a0a2cbafa3418f679a790ac45de5b1c32419993` | `AUDIT_DONE fixes=29` |
| aws-cloudscape | PASS, problems `[]` | **exit 1**, `missing-product-surface-scope`, `contains-prescriptive-placeholder` | `80a655126a17d382c18a2d0216390c7be0f04c2fd90ca0b16d38fe84496cb58d` | `AUDIT_DONE fixes=23` |
| bahamut | PASS, problems `[]` | exit 0, `portable_core: true` | `7d8f981bff17cd82534a6a5e5fb5d62ad1970c1dbdcff5449f78bc1ffa5bbf9b` | `AUDIT_DONE fixes=33` |
| banksalad | PASS, problems `[]` | **exit 1**, `contains-prescriptive-placeholder` | `7ec306d2d0e77b6dde4a1dfdec90c5f5bf54f56d61334a8de3d07d4b5e8cd47f` | `AUDIT_DONE fixes=24` |
| barogo | PASS, problems `[]` | exit 0, `portable_core: true` | `c5e7151ab57e537d54d5bf5f8eb8bcfa204a043090baa09d1d18cfb14516489e` | `AUDIT_DONE fixes=33` |

다섯 건 모두 이관 워커와 분리된 F3가 이미 한 번 실행됐고 audit의 post-F3 SHA가 현재 파일과 일치한다. 따라서 F3는 **절차 실행 증거로 PASS**다. 이는 의미 결함이 없다는 뜻이 아니다. 프로토콜상 이번 목록 개정에 새 F3를 요구하지 않는다.

## Rulebook v7 전 조항 매트릭스

PASS는 해당 조항의 전수 대조 결과이며 문서 전체 PASS를 뜻하지 않는다.

| 조항 | autopedia | aws-cloudscape | bahamut | banksalad | barogo |
|---|---|---|---|---|---|
| A1 값 손실 0 / 발명 0 | FAIL | FAIL | FAIL | FAIL | FAIL |
| A1a unitless ratio | PASS | PASS | PASS | PASS | PASS |
| A1b primitive type | PASS | PASS | PASS | PASS | PASS |
| A1c metadata | PASS | PASS | PASS | PASS | PASS |
| A2 §14 보존 / §9 prompt 삭제 | PASS | PASS | FAIL | PASS | PASS |
| A3 §9-only 고유값 | FAIL | PASS | PASS | PASS | FAIL |
| A4 field-role 비결합 | FAIL | PASS | PASS | FAIL | FAIL |
| B1 관측 evidence kind 비승격 | PASS | PASS | PASS | PASS | FAIL |
| B2/B2a 인접 완전 한정 | PASS | FAIL | FAIL | FAIL | PASS |
| B3 motion 5종 gate | PASS | PASS | PASS | PASS | PASS |
| C1 capture absence ≠ N/A | PASS | PASS | PASS | PASS | PASS |
| C2 exact role별 L/E/S | FAIL | PASS | PASS | PASS | PASS |
| C3 coverage 완료 주장 금지 | PASS | PASS | PASS | PASS | PASS |
| C4 kind 근거와 kind/map | PASS | PASS | PASS | PASS | PASS |
| D1 새 negative coverage 금지 | FAIL | FAIL | FAIL | FAIL | FAIL |
| D2 fictional persona 금지 | PASS | PASS | PASS | PASS | FAIL |
| E1 provenance/portable evidence boundary | FAIL | FAIL | FAIL | FAIL | FAIL |
| E2 source-row 실제 disposition | FAIL | FAIL | FAIL | FAIL | FAIL |
| E2a 모든 다중 목적지 | FAIL | FAIL | PASS | PASS | PASS |
| E2b omission ledger | PASS | PASS | PASS | PASS | PASS |
| E2c 준수 주장 강도 | FAIL | FAIL | FAIL | FAIL | FAIL |
| F1 최종 B2a scan 결과 | FAIL | FAIL | FAIL | FAIL | PASS |
| F2 최종 E2 grep 결과 | FAIL | FAIL | FAIL | FAIL | FAIL |
| F3 별도 세션 1회 실행 | PASS | PASS | PASS | PASS | PASS |
| portable Core 필수 검사 | PASS | FAIL | PASS | FAIL | PASS |

## 1. autopedia — FAIL

### 1.1 §9-only Hero `transparent` field 손실 — A1 / A3 / A4 / E2 / E2c / F2 FAIL

원본 `web/references/autopedia/DESIGN.md:261`은 Hero outline CTA에 `transparent background`를 명시한다. 이 field는 원본 YAML과 §4에는 없고 §9에만 있다. 이관본 `DESIGN.md:286-300`은 Text/Border/Radius/Padding/Height/Font를 보존했지만 Background를 생략했다. Outline이라는 이름이나 dark scrim으로 `transparent`를 추론할 수 없다.

`provenance.md:127-129`와 `migration-log.md:30`은 §9-only 고유값이 Feature Card body `16px / 400 / #4B5563`뿐이라고 잘못 닫는다. 이는 v7 A3의 Notion `transparent` 선례와 같은 최소-field 손실이다.

### 1.2 네 사업선의 검증된 역할·관계 손실 — A1 / E2 / E2c / F2 FAIL

원본 `web/references/autopedia/DESIGN.md:299-303`은 네 사업선을 이름뿐 아니라 역할로 설명한다.

- Dr.Cha: owner-facing automotive-aftermarket management platform
- Quantum Mobility: BMW/Audi/Benz/Tesla 등 수입차 전문 정비
- Parts Distribution: 정비소에 수입차 부품을 공급하는 네트워크
- AI platform: 차량 문제의 diagnosis, prediction, consultation

이관본 `DESIGN.md:17`은 이름과 Dr.Cha의 “모빌리티 자산” 문장만 남긴다. `Tesla`, `repair shops`, `supply network`, `vehicle-problem`, `consultation` 관계는 migrated 네 파일 전체에서 소실됐다. `provenance.md:94`와 `migration-log.md:19,32`는 §11의 고유 서사와 사업선을 옮겼다고 과대 기록한다.

### 1.3 Hero CTA를 submit control로 바꾼 상태 발명 — C2 / E2 / E2c / F2 FAIL

원본 `web/references/autopedia/DESIGN.md:128-135`은 dark-image 위 `문의하기 CTA`로만 식별한다. 원본 `:333`의 `Success (inquiry submitted)`는 §14 form outcome이며, Hero CTA 자체가 submit selector라는 결합 근거는 없다.

이관본 `DESIGN.md:308-310`은 “inquiry submit can…”을 이유로 loading/error/success를 모두 applicable로 만든다. `provenance.md:146`, `migration-log.md:35`, `audit-log.md:116`도 이를 C2 준수로 닫는다. exact selector/destination/request/outcome이 없으면 세 field만 생략해야 한다.

### 1.4 원본 밖 color/font negative domain — D1 / E1 / E2c FAIL

원본은 관측 palette와 Pretendard metrics를 제공하지만 다음 별도 audit를 하지 않는다.

- `DESIGN.md:100,469`: semantic success/error/warning/disabled color token 부재
- `DESIGN.md:167,169-173,474`: universal official type token, FontFace/hosted inventory, distributed family, license statement 부재

B2a 한정은 새 negative coverage를 허용하지 않는다. `migration-log.md:20-21`과 audit/derived inventory도 이 신설 domain을 정상 이관처럼 기록한다. 원본이 확인한 Pretendard, `Pretendard Fallback`, type metrics는 유지 대상이다.

### 1.5 fictional archetype에서 persona-segmentation 부재로 확장 — D1 / E1 / E2 / E2c / F1 / F2 FAIL

원본 `web/references/autopedia/DESIGN.md:315-321,375-377`은 세 persona가 공개 관측 segment에서 만든 fictional archetype이며 이름은 illustrative라고만 밝힌다. first-party persona segmentation domain을 별도로 조사하거나 그 부재를 확인하지 않는다.

이관본 Named gaps `DESIGN.md:477`은 `first-party persona segmentation` 부재를 새로 주장한다. D2에 따른 fictional-persona exclusion은 유지하되, 이 broad negative는 삭제하거나 독립 evidence로 입증해야 한다. `provenance.md`, `migration-log.md`, `audit-log.md`의 F1/F2 준수 주장도 이 현재 본문과 맞지 않는다.

### 1.6 F2 literal destination 원장 불완전 — E2 / E2a / E2c / F2 FAIL

`migration-log.md:49`의 exact-grep 목록은 source palette literal의 실제 목적지를 모두 기록하지 않는다. 예를 들어 `#ffffff`은 Scope/Principles/Do/Hairline comparison/Hero border까지 존재하지만 로그는 일부만 적고, `#000000`, `#E5E7EB`, `#4B5563`도 실제 section/field 목적지가 빠졌다. 값이 문서에 보존됐다는 사실과 E2a/F2 목적지 원장이 완전하다는 주장은 별개다.

### autopedia 재제출 조건

1. Hero CTA에만 `Background: transparent`를 복원하고 일반 Canvas/color token으로 승격하지 않는다.
2. 네 사업선의 역할과 ecosystem 관계를 Scope/서사에 복원하고 필요한 편집 관계에 인접 완전 B2a를 둔다.
3. Hero CTA의 loading/error/success는 exact submit 근거를 추가하거나 세 field를 최소 경계에서 생략한다.
4. unsupported color/font/persona-segmentation negative를 삭제하거나 각 독립 audit evidence를 추가한다.
5. provenance/migration/audit와 값별 F2 목적지를 실제 파일에 맞춰 supersede하고 두 기계 검사를 다시 통과시킨다.

통과 확인: unitless `1.44/1.54/1.55/1.50/1.43`, primitive `tab`·button×3·card×2·`listItem`, metadata, §14 9행, Feature Card의 §9-only body tuple, B1, B3, C1/C3/C4, D2, curve omission ledger, F3는 통과했다.

## 2. aws-cloudscape — FAIL

### 2.1 portable Core 필수 검사 실패

`DESIGN.md:11`의 `does not treat ... surface`가 marked Scope 전체를 부정 claim으로 판정시켜 `missing-product-surface-scope`를 만든다. `DESIGN.md:133`의 독립 표 셀 `Unresolved`는 `contains-prescriptive-placeholder`를 만든다. `migration-log.md:10`과 `audit-log.md:93`도 현재 `portable_core: false`를 정확히 인정한다.

원본 `Times` low-confidence evidence를 삭제하면 안 된다. positive scope claim을 유지하도록 범위 문장을 재구성하고, evidence class label만 `Captured but uncorroborated` 같은 비-sentinel 표현으로 바꿔 사실을 보존해야 한다.

### 2.2 evidence-bounded 역할의 고유 작업 손실 — A1 / E2 / E2c / F2 FAIL

원본 `web/references/aws-cloudscape/DESIGN.md:218-221`은 가상 persona가 아닌 evidence-bounded 역할을 기록한다.

- Cloud-product designer: wireframe에서 prototype으로 이동하며 accessible/consistent application structure 유지
- React developer: component APIs와 testing/responsiveness/accessibility guidance 사용

이관본 `DESIGN.md:34`은 그룹명과 일부 도구만 남기고 위 작업·관계를 잃었다. `provenance.md:148`과 `migration-log.md:33`은 두 역할이 Audience에 보존됐다고 과대 기록한다.

### 2.3 derived voice를 first-party로 분류 — B2 / B2a / E1 / E2 / E2c / F1 / F2 FAIL

`DESIGN.md:215`의 “public materials use direct, instructional language”는 label 관측에서 도출한 voice characterization이지 별도 발행 doctrine이 아니다. `DESIGN.md:217`의 qualifier는 application note/table/samples만 대상으로 하므로 215행에 인접하지 않는다.

`provenance.md:149`, `migration-log.md:30,47`, `audit-log.md:47`은 215행을 first-party로 남겨 F1을 과대 완료한다. first-party 목적·문구와 derived characterization을 분리해야 한다.

### 2.4 Google favicon에서 first-party mark 부재로 확장 — D1 / E1 / E2 / E2c / F2 FAIL

원본 `web/references/aws-cloudscape/DESIGN.md:8-10`은 Google s2 favicon metadata만 제공한다. 이관본 `DESIGN.md:155`는 “No first-party mark file is attached here”, `:268`은 `a first-party Cloudscape mark file` Named gap을 신설한다. `provenance.md:25,141`과 `migration-log.md:15,48`도 이를 logo disposition으로 승인한다.

Google favicon을 portable first-party mark로 승격하지 않는 경계는 유지할 수 있지만, first-party mark domain 자체의 부재를 주장할 수는 없다.

### 2.5 F2 palette 목적지 오기록 — E2 / E2a / E2c / F2 FAIL

`migration-log.md:48`은 `#0f141a / #ffffff / #8c8c94 / #424650` 네 값을 같은 목적지 집합으로 묶는다. 실제 목적지는 서로 다르다.

- `#0f141a`: `DESIGN.md:38,59,62,87,166,187`
- `#ffffff`: `:38,59,62,86`
- `#8c8c94`: `:38,89`
- `#424650`: `:88`

### 2.6 §9 prompt disposition 오기록 — E2 / E2c / F2 FAIL

원본 `web/references/aws-cloudscape/DESIGN.md:172-174`의 static-card state·surface-transfer·unmeasured-geometry constraints는 이미 §7 Don’t에 존재한다. §9 `:189`는 이를 boundary-aware tool prompt로 반복할 뿐이다. 이관본 Avoid `DESIGN.md:73-75`가 보존하는 근거는 §7이며 A2에 따른 §9 prompt 삭제 자체는 통과한다.

그러나 `migration-log.md:29`는 이 반복 constraint를 “§9-only 고유 제약”으로 분류해 Avoid로 옮겼다고 기록한다. 실제 disposition은 §7 값 보존 + §9 prompt 삭제이므로 해당 row와 F2/E2c 준수 주장을 고쳐야 한다.

### aws-cloudscape 재제출 조건

1. Scope 의미와 `Times` evidence를 보존하면서 Core 두 sentinel을 제거해 `portable_core: true`를 만든다.
2. 두 evidence-bounded 역할의 고유 작업·관계를 Audience에 복원한다.
3. `direct, instructional` characterization에 인접 완전 B2a를 붙이고 F1 inventory를 고친다.
4. broad first-party-mark gap을 삭제하거나 정확한 독립 evidence를 추가한다.
5. palette literal별 실제 목적지를 분리한다.
6. §9 row를 prompt 삭제와 §7 constraint 보존으로 정정하고 provenance/migration/audit/F2를 동기화한 뒤 두 기계 검사를 통과시킨다.

통과 확인: px line-height `48/24/20`, `Type: card`, `ds.type: system`, metadata, component geometry, §14 전체, B1, B3, C1–C4, D2, source `[FILL IN]` provenance omission, F3는 통과했다.

## 3. bahamut — FAIL

### 3.1 §9 도구 명령을 Avoid 규칙으로 재호스팅 — A2 / E2 / E2c / F2 FAIL

원본 `web/references/bahamut/DESIGN.md:302-307`은 Agent Prompt Guide의 numbered Iteration Guide다. 이관본 `DESIGN.md:78,86-88`은 `one badge per item maximum`과 `text hierarchy lives in size + weight`를 “unique §9 construction constraints”라 부르며 Avoid 명령으로 유지한다. `provenance.md:128`과 `migration-log.md:28,30`도 A3 이동이라고 기록한다.

이 둘은 component-local 근거값이 아니라 도구용 construction command다. A2에 따라 삭제해야 하며, B2a를 붙여도 prompt가 verified brand rule이 되지 않는다. 반면 Content Card의 title/metadata/badge tuple과 nav band의 `#ffffff`/44px처럼 §9에만 있는 renderable value는 A3에 따라 계속 보존해야 한다.

### 3.2 원본 밖 font coverage negative — D1 / E1 / E2 / E2c / F2 FAIL

원본 `web/references/bahamut/DESIGN.md:98-100,113`은 system stack과 no-custom-font 사실만 기록한다. 이관본은 다음 별도 domain 결론을 추가한다.

- `DESIGN.md:172,176`: Bahamut-authored universal current typography token 부재
- `:178`: Bahamut-exclusive distributed family 부재
- `:180`: system faces가 Bahamut brand asset이 아니라는 license/asset 결론

`provenance.md:180`, `migration-log.md:21`, `audit-log.md:88`도 이를 정상 evidence class로 승인한다.

### 3.3 imagery authority 발명과 불완전 한정 — A1 / B2 / B2a / D1 / E1 / E2 / E2c / F1 / F2 FAIL

`DESIGN.md:207`은 “Product photography, game thumbnails, and community imagery on the two inspected URLs are first-party catalog content”라고 분류한다. 원본에는 game-entry narrative와 loading-thumbnail state가 있을 뿐 product-photography/community-imagery inventory나 first-party asset classification은 없다.

같은 행의 qualifier는 “imagery-not-invented-decoration” reconstruction limit만 한정하며, 앞의 first-party 사실 분류를 끝까지 한정하지 않는다. `provenance.md:185`와 `audit-log.md:38`도 이를 승인한다.

### 3.4 §11 고유 관계·상태 손실 — A1 / E2 / E2c / F2 FAIL

원본 `web/references/bahamut/DESIGN.md:332`의 nascent Taiwanese internet 시점과 `grew organically` 관계, `:334`의 動畫瘋 = “Taiwan's largest legal anime streaming service”가 이관본 `DESIGN.md:23` 및 `provenance.md:84-87`에서 소실됐다. `migration-log.md:19,32`는 §11의 고유 proposition을 옮겼다고 닫는다.

### 3.5 first-party persona segmentation 부재 발명 — D1 / E1 / E2 / E2c / F1 / F2 FAIL

이관본 Named gaps `DESIGN.md:514`는 `first-party persona segmentation` 부재를 신설한다. 원본 `web/references/bahamut/DESIGN.md:348-354`은 fictional archetype이라고만 밝히며 first-party segmentation domain을 조사하지 않는다. D2 exclusion은 이미 Audience에 보존됐으므로 이 broad negative는 필요하지 않다.

### bahamut 재제출 조건

1. §9 prompt-only 두 command를 portable Avoid에서 삭제하고 §9 disposition을 prompt-delete와 renderable-value move로 정확히 나눈다.
2. live system stack/no-custom-font만 남기고 unsupported official/distributed/license negatives를 삭제하거나 독립 evidence를 추가한다.
3. invented imagery classes/first-party classification을 삭제하거나 exact capture proof를 추가하고 필요한 B2a를 완결한다.
4. §11의 nascent-internet/organic-growth/動畫瘋 legal-largest 관계를 정확한 authority로 복원한다.
5. first-party persona-segmentation Named gap을 삭제하고 모든 ledger/F1/F2를 실제 목적지와 맞춘 뒤 두 기계 검사를 통과시킨다.

통과 확인: A1a/A1b/A1c, §14, Content Card와 nav band의 §9-only renderable values, A4, B1/B3, C1–C4, D2 biography 삭제, E2a/E2b, F3, portable Core는 통과했다.

## 4. banksalad — FAIL

### 4.1 portable Core 필수 검사 실패

`DESIGN.md:145`의 독립 Evidence class 셀 `Unresolved`가 Core의 bare-placeholder sentinel과 충돌해 exit 1 / `portable_core: false`다. `migration-log.md:46`과 `audit-log.md:101,105`는 이 결과를 정직하게 기록하지만 대상 산출물은 여전히 Core 수용 조건을 충족하지 못한다.

원본 `web/references/banksalad/DESIGN.md:184`의 native-app-only typography evidence boundary를 삭제하면 안 된다. `Captured scope unresolved`처럼 sentinel이 아닌 evidence label로 재작성해 사실을 보존해야 한다.

### 4.2 Principles stems의 authority 승격 — B2 / B2a / E1 / E2 / E2c / F1 / F2 FAIL

원본 §12 `web/references/banksalad/DESIGN.md:656-660`의 다섯 stem은 BPL 자료를 바탕으로 만든 editorial paraphrase다. exact first-party 문구는 `:650`의 “Communication cost…” quote뿐이다.

이관본 `DESIGN.md:51-62`은 UI implication과 뒤 application list만 derived로 한정하고 stems를 first-party로 분류한다. `provenance.md:196-197`, `migration-log.md:33,44,46`, `audit-log.md:99-100`도 같은 authority 승격을 반복한다. B2a는 다섯 완전 항목의 stem과 implication 모두를 포괄해야 한다.

### 4.3 task·route·role 결합 — A1 / A4 / B2 / B2a / E1 / E2 / E2c / F1 / F2 FAIL

원본 `web/references/banksalad/DESIGN.md:667`의 observable job은 interest-rate-reduction-request availability 확인이다. `:639`의 “내 대출금리 조회”는 별도로 `/contents`에서 검증된 문자열이다.

이관본 `DESIGN.md:28`은 둘을 하나의 task/label 관계로 결합하면서 `Content:298`에는 실제 `/contents` route를 유지한다. `provenance.md:188,195`와 `migration-log.md:21,31,34,46`도 이 결합을 승인한다. exact label→reduction-request action evidence가 없으면 job과 observed string을 분리해야 한다.

### 4.4 원본 밖 negative domain — D1 / E1 / E2 / E2c / F1 / F2 FAIL

- `DESIGN.md:78,343`: 원본에 없는 `sale` styling/color coverage
- `DESIGN.md:348`: 별도 audit가 없는 `mobile navigation` gap
- `DESIGN.md:125`: 원본 `:701`이 property/duration/easing만 기록했는데 `No animation name was recorded`라고 absence를 추가

이들은 smallest unresolved field를 생략하는 대신 새 domain 부재를 사실로 만든다. `migration-log.md:28-29,36`과 F1/F2 inventory도 실제 원본 경계를 닫지 못한다.

### 4.5 §5/§8 전체를 stale로 wholesale demotion — A1 / A4 / E1 / E2 / E2c / F2 FAIL

원본 legacy `<details>`는 `web/references/banksalad/DESIGN.md:469`에서 닫힌다. footer `:478`이 명시적으로 되돌린 것은 `#04c584`, `#10df99`, BM JUA, 2px default radius, uncaptured input/chart/modal/state claims뿐이다. 그 뒤 main-body §5 `:480-508`과 §8 `:550-576`의 나머지 값·관계는 source semantics상 현재 body다.

이관본 `DESIGN.md:287`, `provenance.md:158-170`, `migration-log.md:25,29`는 두 packet 전체를 reverted/stale로 묶는다. 그 결과 full 4px spacing scale, 12px table-row padding, grid/container와 grouping-gap 역할, breakpoint별 collapse, 24–40px square logo framing, illustration constraint 등이 누락되거나 권위가 바뀐다. footer가 취소한 최소 field만 demote해야 한다.

### banksalad 재제출 조건

1. native-app typography 경계를 보존하면서 bare `Unresolved` sentinel을 제거해 Core를 통과시킨다.
2. 다섯 Principles 전체를 인접 완전 B2a 아래 두고 exact quote만 first-party로 분리한다.
3. interest-rate-reduction job과 `/contents` label을 분리하거나 정확한 label/action evidence를 추가한다.
4. `sale`, `mobile navigation`, animation-name absence를 삭제하거나 exact capture proof를 추가한다.
5. footer가 정확히 취소한 최소 field만 demote하고 나머지 §5/§8 값·역할을 portable current slot에 복원한다.
6. provenance/migration/audit/F1/F2/SHA를 실제 산출물과 동기화하고 두 기계 검사를 통과시킨다.

통과 확인: A1a/A1b/A1c, A2/A3, B1/B3, C1–C4, D2, E2a/E2b, F3는 통과했다.

## 5. barogo — FAIL

### 5.1 §9-only Hero composition·Feature Card tuple 손실 — A1 / A3 / A4 / E2 / E2c / F2 FAIL

원본 `web/references/barogo/DESIGN.md:289`은 white Hero의 42px/700/#000 H1, primary CTA, 그 옆 outline CTA pair를 기록한다. `:290`은 Feature Card 내부 title `Pretendard 700 / #000000`과 body `16px / 400 / #666666` 결합을 기록한다.

이관본 `DESIGN.md:230-298`은 버튼과 Feature Card surface를 평탄화하고, Layout `:390`은 Hero에 primary CTA만 둔다. title/body local tuple도 없다. `provenance.md:109-111`과 `migration-log.md:30`은 §9-only unique renderable field가 없다고 잘못 닫는다.

### 5.2 parent surface background를 item/link field로 병합 — A4 / E2 / E2c / F2 FAIL

원본 YAML nav-link는 fg/font/active/use만 있고, body Navigation `web/references/barogo/DESIGN.md:172-177`의 `#ffffff`는 header/navigation band 값이다. YAML footer-link도 fg/font/use만 있고, body Footer `:179-183`의 `#1a1a1a`는 footer band 값이다.

이관본 `DESIGN.md:331-344`은 Role=item/Anatomy=label인 Top Nav에 `Background: #ffffff`를 넣고, `:358-370`은 Footer Link에 `Background: #1a1a1a`를 넣는다. `:344`와 `:370`도 각각 “this header’s fill”, “this band’s fill”이라고 parent role임을 인정한다.

### 5.3 §11 고유 인과·제품 관계 손실 — A1 / E2 / E2c / F2 FAIL

원본 `web/references/barogo/DESIGN.md:326-330`의 다음 관계가 current `DESIGN.md:21-23`과 `provenance.md:71-76`에서 소실됐다.

- 개별 점포의 reliable technology-driven delivery 부재와 fragmented/manual agency market라는 structural gap
- shared logistics rails로 merchants, riders, delivery demand를 연결한다는 premise
- software로 riders와 store operators를 coordinate한다는 제품 관계

`migration-log.md:19,32`는 §11 unique narrative를 옮겼다고 과대 기록한다.

### 5.4 derived `바로레터 구독하기`를 harvested Observed로 승격 — B1 / E1 / E2 / E2c / F2 FAIL

`바로레터 구독하기`의 원본 유일 위치는 derived Voice/Tone table `web/references/barogo/DESIGN.md:313`이다. live-verbatim sample은 `:317-320`의 세 줄과 footer capture comment 범위다.

이관본 `DESIGN.md:425`는 이 문자열을 “from harvested surfaces”로 승격하고, `:439`에는 올바른 derived table도 중복 보존한다. `provenance.md:76`과 `migration-log.md:31`은 421–425 전체를 live dual destination으로 과대 기록한다.

### 5.5 원본 밖 product/font negative coverage — D1 / E1 / E2 / E2c / F2 FAIL

- `DESIGN.md:21`: Store Manager/rider-app/hub-franchise UI가 “not captured”라는 별도 product-domain 부재
- `DESIGN.md:167`: separately published Barogo type specification 부재
- `DESIGN.md:170`: 빈 Declared-only/unresolved row의 “No additional family is promoted”

원본은 두 public surface와 site label, live Pretendard/Termina facts만 제공한다. B2a는 위 negative domain을 치유하지 않는다. Google favicon을 first-party mark로 승격하지 않는 `DESIGN.md:199` 경계는 broad mark-file gap이 아니므로 이번 차단점이 아니다.

### 5.6 fictional persona를 provenance에 재수록 — D2 / E2 / E2c / F2 FAIL

`provenance.md:134`는 원본 fictional persona 이름 김상현·이도윤·박지은을 literal로 다시 쓰면서 “not copied here”라고 주장한다. `migration-log.md:34`도 같은 이름을 다시 쓰고 “sidecar에 없음 — names appear ... provenance 134”라고 자기모순을 만든다. Portable `DESIGN.md:38`의 generic exclusion-only 경계 자체는 통과다.

### 5.7 F2가 실제 disposition 완전성을 유보 — E2 / E2c / F2 FAIL

위 결함 때문에 `migration-log.md:30-34`는 실제 source-row disposition과 다르다. 더구나 `:45`는 “F2 completeness beyond those greps”를 주장하지 않는다고 명시한다. v7 F2는 모든 log row를 실제 grep한 뒤 기록하는 의무이며 선택적 완전성 유보로 대체할 수 없다.

비차단 보조 증거: `_tokencheck.cjs`는 `LOST []`, `INVENTED []`, `persona names true`를 출력한 뒤 52행 literal `EOF` 때문에 exit 1이다. 공식 gate로 credit하지 않았고 별도 v7 결함 수를 늘리지 않았지만 D2를 독립 확인한다.

### barogo 재제출 조건

1. Hero primary+outline pair와 Feature Card title/body tuple을 receiving component/layout에 local 관계로 복원한다.
2. `#ffffff` header와 `#1a1a1a` footer fill을 parent surface/context로 분리하고 item/link Background에서 제거한다.
3. §11 structural gap, shared-rails, software-coordination 관계를 복원하고 편집 부분에 인접 완전 B2a를 둔다.
4. `바로레터 구독하기`를 Observed에서 제거하거나 독립 live evidence를 추가하고 derived table은 유지한다.
5. unsupported product/font negatives를 삭제하거나 exact independent evidence를 추가한다.
6. migrated sidecars에서 fictional persona literal을 0 hit로 만들고 generic deletion disposition만 남긴다.
7. 모든 source row와 destination을 다시 grep해 provenance/migration/audit/F2/SHA를 동기화하고 두 기계 검사를 통과시킨다.

통과 확인: A1a/A1b/A1c, §14, B2/B2a 형식, B3, C1–C4, E2a/E2b, F1, F3, portable Core는 통과했다.

## 결론과 재제출 프로토콜

웨이브 10 첫 전수 판정은 **FAIL 5/5**다. 다섯 건 모두 위 최소 경계 결함을 수정하고 같은 sol에게 목록-only로 재제출해야 한다. 재제출은 새 기준을 추가하지 않고 이 문서의 건별 조건만 확인한다.

새 F3는 요구하지 않는다. 다만 수정된 최종 SHA에서 DESIGN/provenance/migration/audit의 line pointer, disposition, F1/F2 기록과 기계 결과가 서로 일치해야 한다. **5/5 PASS 전 웨이브 10 완료 및 카탈로그 채택 금지.**
