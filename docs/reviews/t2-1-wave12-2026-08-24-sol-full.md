# T2-1 웨이브 12 sol 전수 검토

- 검토일: 2026-08-24
- 검토자: sol
- 기준: `docs/design-md-weight/MIGRATION_RULEBOOK.md` v7 전 조항
- 대상: `bmw`, `brandi`, `bunjang`, `buzzvil`, `cafe24`
- 대조 범위: 각 canonical `web/references/<id>/DESIGN.md`, 그 본문이 직접 가리키는 proof sidecar, migrated `DESIGN.md`, `provenance.md`, `migration-log.md`, `audit-log.md` 전부

## 판정 요약

첫 전수 판정은 **FAIL 5/5, PASS 0/5**다. 다섯 건 모두 `--gate-only`, portable Core, 기존 fresh F3의 실행·SHA 결속은 통과했다. 그러나 canonical 값·관계·메타데이터 보존, evidence kind, §9 local binding, C4 field omission, fictional-persona 배제, proof/portable 경계와 source-row disposition에서 차단 결함이 남았다.

| id | 판정 | 주된 차단 조항 |
|---|---|---|
| bmw | **FAIL** | A1/A1c, B1, E1/E2/E2a/E2c, F2 |
| brandi | **FAIL** | A1, C4, E2/E2a/E2c, F2 |
| bunjang | **FAIL** | A1, B1, D1, E1/E2/E2a/E2c, F2 |
| buzzvil | **FAIL** | D2, E1/E2/E2c, F2 |
| cafe24 | **FAIL** | A1/A1b/A3/A4, B1/B2, C4, D1/D2, E1/E2/E2a/E2c, F2 |

## 기계 검사와 F3 증거

재실행 명령:

- `node test-v2/tools/migrate-reference.mjs --brand <id> --gate-only`
- `node scripts/migrate-design-md-core.cjs --input docs/design-md-weight/migrated/<id>/DESIGN.md --check --require-portable-core --json`

| id | gate-only | portable Core | current DESIGN.md SHA-256 | 기존 fresh F3 |
|---|---|---|---|---|
| bmw | PASS, problems `[]` | exit 0, `portable_core: true` | `4e9797e243afba2d3bb550bac4656268af58afe2ac13b52a71958351198279e2` | `AUDIT_DONE fixes=18` |
| brandi | PASS, problems `[]` | exit 0, `portable_core: true` | `f0d0ff42822760644aa7f4c2587ce7cd2e6185be6d68e0cbd911a7fa952d0d4c` | `AUDIT_DONE fixes=24` |
| bunjang | PASS, problems `[]` | exit 0, `portable_core: true` | `fecb6b1792e18e985d65ea905da9ee11a74a243e5d137f03209375697b75bcb3` | `AUDIT_DONE fixes=26` |
| buzzvil | PASS, problems `[]` | exit 0, `portable_core: true` | `e7ee118cdc4f1c0ebac1abf6efe906fe2641f3b0474cfeca586ede39bbd1cf5c` | `AUDIT_DONE fixes=16` |
| cafe24 | PASS, problems `[]` | exit 0, `portable_core: true` | `fde9399b9e7a63abe8d6553cfba583c398ec68d8f95aed8e1659760649d4ded0` | `AUDIT_DONE fixes=23` |

다섯 건 모두 이관 워커와 분리된 F3가 이미 실행됐고 audit의 post-F3 SHA가 현재 파일과 일치한다. 따라서 F3는 **별도 세션 1회 실행 증거로 PASS**다. 이는 B2a/E2 결과까지 PASS라는 뜻이 아니다. 이번 고정 목록 개정만으로 새 F3를 요구하지 않는다.

## Rulebook v7 전 조항 매트릭스

PASS는 해당 조항의 전수 대조 결과이며 문서 전체 PASS를 뜻하지 않는다.

| 조항 | bmw | brandi | bunjang | buzzvil | cafe24 |
|---|---|---|---|---|---|
| A1 값 손실 0 / 발명 0 | FAIL | FAIL | FAIL | PASS | FAIL |
| A1a unitless ratio | PASS | PASS | PASS | PASS | PASS |
| A1b primitive type | PASS | PASS | PASS | PASS | FAIL |
| A1c metadata | FAIL | PASS | PASS | PASS | PASS |
| A2 §14 보존 / §9 prompt 삭제 | PASS | PASS | PASS | PASS | PASS |
| A3 §9-only 고유값 | PASS | PASS | PASS | PASS | FAIL |
| A4 field-role 비결합 | PASS | PASS | PASS | PASS | FAIL |
| B1 관측 evidence kind 비승격 | FAIL | PASS | FAIL | PASS | FAIL |
| B2 derived 해석 portable 동반 | PASS | PASS | PASS | PASS | FAIL |
| B2a 인접·완전 evidence-class 한정 | PASS | PASS | PASS | PASS | PASS |
| B3 motion 5종 gate | PASS | PASS | PASS | PASS | PASS |
| C1 capture absence ≠ N/A | PASS | PASS | PASS | PASS | PASS |
| C2 exact role별 L/E/S | PASS | PASS | PASS | PASS | PASS |
| C3 coverage 완료 주장 금지 | PASS | PASS | PASS | PASS | PASS |
| C4 kind 근거와 kind/map | PASS | FAIL | PASS | PASS | FAIL |
| D1 새 negative coverage 금지 | PASS | PASS | FAIL | PASS | FAIL |
| D2 fictional persona / 독립 task | PASS | PASS | PASS | FAIL | FAIL |
| E1 provenance/portable evidence boundary | FAIL | PASS | FAIL | FAIL | FAIL |
| E2 source-row 실제 disposition | FAIL | FAIL | FAIL | FAIL | FAIL |
| E2a 모든 다중 목적지 | FAIL | FAIL | FAIL | PASS | FAIL |
| E2b omission ledger | PASS | PASS | PASS | PASS | PASS |
| E2c 준수 주장 강도 | FAIL | FAIL | FAIL | FAIL | FAIL |
| F1 최종 B2a scan 결과 | PASS | PASS | PASS | PASS | PASS |
| F2 최종 E2 grep 결과 | FAIL | FAIL | FAIL | FAIL | FAIL |
| F3 별도 세션 1회 실행 | PASS | PASS | PASS | PASS | PASS |
| portable Core 필수 검사 | PASS | PASS | PASS | PASS | PASS |

## 1. bmw — FAIL

### 1.1 `verification_v2.schema: 2` 손실 — A1 / A1c / E2 / E2c / F2 FAIL

원본 `web/references/bmw/DESIGN.md:13-14`의 검증 메타데이터 `verification_v2.schema: 2`가 migrated packet에 없다. `docs/design-md-weight/migrated/bmw/provenance.md:34`는 `checked`만, `:85-104`는 claim ledger만 보존한다. 그런데 `migration-log.md:14`는 `verification_v2`를 provenance로 분리했다고 닫는다.

### 1.2 §13 source-backed 관계 손실 — A1 / E2 / E2c / F2 FAIL

원본 `web/references/bmw/DESIGN.md:234-238`은 세 stakeholder group의 label만이 아니라 서로 다른 source-backed 관계를 기록한다.

- prospective customer: revised identity가 고객을 중심에 두고 BMW 세계로 초대한다.
- official BMW Club representative: official clubs는 brand ambassador이며 mandatory communication rules의 대상이다.
- BMW Group design collaborator: BMW Group Design은 자동차 브랜드 전반에서 일하는 global design organisation이다.

이관본 `DESIGN.md:28`은 label과 출처 괄호만 남긴다. `provenance.md:125-127`과 `migration-log.md:30`은 누락을 반영하지 않는다. 원본은 이들을 fictional persona가 아닌 source-backed stakeholder facts로 명시하므로 D2는 삭제 근거가 아니다.

### 1.3 일반 brand announcement를 font `Official product-use`로 승격 — A1 / B1 / E1 / E2 / E2c / F2 FAIL

원본 `web/references/bmw/DESIGN.md:84`는 2020 identity announcement와 Neue Klasse 자료가 narrative를 설명할 뿐 live web token 근거가 아니라고 명시한다. 실제 font evidence class는 `:108-114`의 live computed use, limited live use, official Club CI asset, declared-only faces, licence boundary다.

이관본 `DESIGN.md:119-126`, 특히 `:121`은 일반 identity announcement를 Font evidence의 `Official product-use` 행으로 신설했다. `provenance.md:74-83`은 여전히 이를 narrative-not-interface-token으로 분류하고, `migration-log.md:17,19`도 서로 다른 disposition을 적는다. 인접 B2a는 unrelated narrative를 font evidence로 바꾸지 못한다.

### 1.4 Named gaps 다중 목적지 누락 — E2 / E2a / E2c / F2 FAIL

대표 누락은 다음과 같다.

- 원본 `:104` → `DESIGN.md:78`과 `:321-322`; `migration-log.md:18`은 Named gaps를 누락한다.
- 원본 `:160` → `DESIGN.md:263`과 `:325`; 로그 `:20`은 `:325`를 누락한다.
- 원본 `:200` → `DESIGN.md:272`과 `:326`; 로그 `:25`는 `:326`을 누락한다.
- 원본 `:242` → `DESIGN.md:157`과 `:323`; 로그 `:31`은 `:323`을 누락한다.
- 원본 `:114`, `:212`, `:84`의 경계도 각각 Named gaps `:328`, `:329`, `:330`에 재기록됐지만 source-row 로그 `:19`, `:27`, `:16`에 없다.

따라서 post-F3 F2 기록 `migration-log.md:69-96`과 `audit-log.md:94-99`는 실제 disposition보다 강하다.

### bmw 재제출 조건

1. provenance에 `verification_v2.schema: 2`를 복원한다.
2. §13 세 그룹의 source-backed 고유 관계를 portable 본문에 복원한다.
3. 근거 없는 Font `Official product-use` 행을 삭제하거나 실제 font-linked official evidence로 교체한다.
4. 위 Named gaps를 포함해 source-row별 모든 실제 목적지를 다시 기록하고 F2/audit current claims를 동기화한다.
5. 두 기계 검사와 새 SHA를 기록한다.

통과 확인: A1a/A1b, §14, §9 prompt 처리, field-role 분리, formal B2/B2a, B3, C1-C4, D1/D2, E2b, F1, fresh F3, portable Core는 통과했다.

## 2. brandi — FAIL

### 2.1 C4의 “kind 자체 생략” 불이행 — A1 / C4 / E2c FAIL

이관본 `docs/design-md-weight/migrated/brandi/DESIGN.md:175`와 `migration-log.md:18,35,54`는 Product option listbox와 Product detail badge의 kind/map을 생략했다고 주장한다. 그러나 실제 본문은 listbox에 `Type: omitted`, `Kind: omitted`을 방출하고(`DESIGN.md:251-252`), badge에도 `Kind: omitted`을 방출한다(`:268`). C4는 미확정 kind field 자체를 생략하도록 요구한다. Badge의 검증된 `Type: badge`(`:267`)만 유지 대상이다.

### 2.2 F2 다중 목적지 누락 — E2 / E2a / E2c / F2 FAIL

- `migration-log.md:14`는 색상 목록을 “Dual hex hits”라고 부르지만 실제 provenance 목적지 `provenance.md:39`와 일부 literal의 `:132,149`를 누락한다.
- 원본 §14 `web/references/brandi/DESIGN.md:255-257`은 이관본 `DESIGN.md:171`뿐 아니라 Named gaps `:336-337`에도 재기록됐으나 로그 `:35`는 이를 누락한다.
- 원본 responsive 경계 `:229-231`은 `DESIGN.md:286`과 Named gaps `:342`에 있는데 로그 `:29`는 `:342`를 누락한다.
- voice/persona placeholder는 각각 Named gaps `:331`, `:332`에도 가지만 로그 `:31,34`는 그 목적지를 적지 않는다.
- Font 및 C4 경계도 Named gaps `:334,338`에 재기록됐지만 해당 source-row 로그 `:22,18`에 없다.

따라서 `audit-log.md:104-105`의 current-three-file grep/F2 완료 주장은 실제 원장보다 강하다.

### brandi 재제출 조건

1. listbox의 `Type: omitted`/`Kind: omitted`과 badge의 `Kind: omitted` 필드를 삭제한다. Badge `Type: badge`와 두 컴포넌트의 kind/map 미확정 설명은 prose로만 유지할 수 있다.
2. provenance 색상 hit와 위 Named gaps를 포함해 source-row별 모든 실제 목적지를 migration log에 추가한다.
3. audit의 F2 완료 주장과 SHA를 현재 파일에 맞게 갱신하고 두 기계 검사를 재실행한다.

통과 확인: unitless `1.0`, primitive badge, metadata, §14 본문, §9 값 처리, B1/B2/B2a/B3, C1-C3, D1/D2, E1/E2b, F1, fresh F3, portable Core는 통과했다.

## 3. bunjang — FAIL

### 3.1 button family cardinality/class pattern 손실 — A1 / E2 / E2c / F2 FAIL

원본 `web/references/bunjang/DESIGN.md:213`은 `three variants × four sizes`와 `_variant-{normal|primary|…} × _size-{XS|M|XL} × _full` 관계를 관측값으로 기록한다. 이관본 `docs/design-md-weight/migrated/bunjang/DESIGN.md:219-270`은 Primary/Secondary의 개별 값은 남기지만 관측 count와 class-family 관계를 잃었다. migrated packet 전체에도 해당 관계가 없다. `migration-log.md:27`의 “§8 Components 옮김” disposition은 실제보다 강하다.

### 3.2 font evidence kind 승격과 원본 밖 negative coverage — A1 / B1 / D1 / E1 / E2 / E2c / F2 FAIL

원본 `web/references/bunjang/DESIGN.md:134`이 확정하는 것은 Pretendard Variable의 live use, `sans-serif` fallback, Bunjang CDN self-host, 211/211 sampled nodes다.

이관본 `DESIGN.md:160-168,494`은 이를 `Official distributed asset`으로 승격하고, `Not a Bunjang-exclusive type family`를 새로 단정하며, exact `.woff2` URL의 부재/미해상을 새 coverage claim과 Named gap으로 만든다. `provenance.md:153`, `migration-log.md:22,47`, `audit-log.md:32,70,97`은 이를 current fact로 승인한다.

### 3.3 provenance 분리의 양방향 불일치 — A1 / E1 / E2 / E2a / E2c / F2 FAIL

원본 proof는 `web/references/bunjang/DESIGN.md:340`의 `suppress_origin=True to bypass --remote-allow-origins block`과 `:347-358`의 public-DS lookup host/status ledger를 포함한다.

- `provenance.md:58`은 `suppress_origin=True`만 남기고 `--remote-allow-origins` 원인 detail을 잃었다.
- 반대로 portable `DESIGN.md:17`은 lookup host와 DNS/HTTP 결과를 그대로 재수록하며, `provenance.md:72-82`에도 같은 lookup ledger가 있다.
- `provenance.md:82`는 lookup URL이 ledger에 남는다고 하나 `migration-log.md:20`은 조회 URL을 provenance로 분리하고 portable에는 결론만 restate했다고 기록한다.

### bunjang 재제출 조건

1. Components/Capture record에 3 variants/4 sizes가 관측됐다는 cardinality와 알려진 normal·primary, XS·M·XL, `_full` 관계를 복원한다. 미상 이름은 발명하지 않는다.
2. font table은 live computed use·fallback·CDN self-host만 유지한다. `Official distributed asset`, 비독점 단정, exact `.woff2` 부재/Named gap은 삭제하거나 각각 독립 증거를 추가한다.
3. `--remote-allow-origins` proof detail은 provenance에 복원하고, portable Scope에서는 개별 lookup host/status를 제거해 standalone 결론·authority boundary만 남긴다.
4. provenance/migration/audit current-class와 E2/E2a map, SHA를 동기화하고 두 기계 검사를 재실행한다.

통과 확인: A1a/A1b/A1c, §14 부재 처리, §9 값, field-role 분리, formal B2/B2a, B3, C1-C4, D2, E2b, F1, fresh F3, portable Core는 통과했다.

## 4. buzzvil — FAIL

### 4.1 fictional-persona 입력 segment의 portable/sidecar 재수록 — D2 / E1 / E2 / E2c / F2 FAIL

원본 `web/references/buzzvil/DESIGN.md:381-387,450-452`은 performance marketers, app publishers, adtech partners를 fictional archetype의 입력으로 명시하고, 세 biography도 fictional로 한정한다.

이 세 segment label이 다음에 재수록됐다.

- `docs/design-md-weight/migrated/buzzvil/DESIGN.md:38`
- `provenance.md:145`
- `migration-log.md:33`
- `audit-log.md:22,84`

반면 `provenance.md:123-125`는 persona-derived label을 re-host하지 않았다고 하고, `migration-log.md:60-61`은 given-name 및 persona-derived label 재수록이 0건이라고 닫는다. “not promoted”라는 부정 문맥도 D2가 허용하는 generic deletion disposition이 아니다.

### buzzvil 재제출 조건

1. 세 segment literal을 portable, provenance, migration-log, audit-log에서 전부 제거한다.
2. “fictional archetype material deleted; not re-hosted”라는 generic disposition만 남긴다.
3. F2/current-class 기록과 SHA를 갱신하고 두 기계 검사를 재실행한다.

통과 확인: A1-A4, B1-B3, C1-C4, D1, E2a/E2b, F1, fresh F3, portable Core는 통과했다.

## 5. cafe24 — FAIL

### 5.1 canonical proof sidecar의 false absence — A1 / D1 / E1 / E2 / E2c / F2 FAIL

canonical `web/references/cafe24/.verification.md:3`에는 `## Proof — Tier 1 live inspect` sidecar가 실제 존재한다. derived mirror `design-md/cafe24/.verification.md`와 SHA-256도 동일하다: `5812cc95e16da7f3a4afd1acf9948e0313a2beef422ede165186cc1a7125d606`.

그러나 migrated `provenance.md:62,71-73`과 `migration-log.md:33`은 derived mirror만 proof source로 가리키고, 특히 `provenance.md:73`은 `web/references/cafe24/.verification.md`의 부재를 단정한다. 이는 실제 canonical proof 경계와 반대이며 새 negative claim이다.

### 5.2 Circular Icon Button의 verified primitive/kind를 미확정으로 뒤집음 — A1 / A1b / C4 / E2 / E2c FAIL

원본 `web/references/cafe24/DESIGN.md:201-205`은 component heading과 Use 모두 **Circular Icon Button / round icon utility button**으로 primitive를 명시한다. 이관본 `DESIGN.md:251,433-444,539-548`은 YAML에 type이 없다는 이유로 interactive-kind/type/map 근거가 없다고 뒤집는다. A1b의 primitive type 근거는 YAML에만 한정되지 않는다. 같은 과대 주장이 `provenance.md:121,165,169`, `migration-log.md:15,20,31`, `audit-log.md:9,41`에 있다.

### 5.3 증거 없는 Persona Card task와 C4 자가모순 — A1 / C4 / D2 / E2 / E2a / E2c FAIL

이관본 `DESIGN.md:25-29`은 `Open a hero audience-segment card`를 Primary task로 쓴다. 원본 `web/references/cafe24/DESIGN.md:61,194-200,250`은 audience-segment **entry card**의 default anatomy/labels/배치만 기록하며 exact tap/open/destination proof는 없다. 같은 이관본 `DESIGN.md:251,360-373`도 Persona Card의 interactive-kind가 미확정이라고 명시한다. 미확정 kind와 `Open` user outcome을 동시에 유지할 수 없다. 실제 task 원천인 YAML/§4의 Primary-task 목적지는 `migration-log.md:15,20`에 없고 §13 행 `:30`에 우회 설명돼 E2/E2a map도 맞지 않는다.

### 5.4 §9-only local composition binding 손실 — A1 / A3 / A4 / E2 / E2c / F2 FAIL

원본 `web/references/cafe24/DESIGN.md:342-345`의 prompt wrapper 자체는 삭제 대상이지만 그 안의 검증된 local relationship은 삭제 대상이 아니다. 네 recipe가 값별로 흩어졌다.

- white hero parent + persona-card row + one primary pill CTA
- `:343`의 full-width dark feature band `#1a1d22` + 40px/700/-0.4px white headline + single lime emphasis + no shadow
- `:344`의 white-card parent 안 login input (`#ffffff`, `#d6dae1`, 4px, 14px 12px, 48px, `#1b1e26`) + submit (`#3971ff`, white, 4px, 56px, 16px/700, `로그인`)
- step-section parent 안 numbered badge + category-tag pairing

이관본 `DESIGN.md:139-150,210-220,278-302,375-400,449-474`에는 개별 값이 Foundations/Type/Components/Layout에 흩어져 있으나 parent-child/local pairing이 없다. `provenance.md:140-142`와 `migration-log.md:26`의 “verified values already live elsewhere / prompt-only omitted” disposition은 A3/A4와 맞지 않는다.

### 5.5 Typography body-table의 role-local Notes 손실 — A1 / A4 / E2c / F2 FAIL

원본 `web/references/cafe24/DESIGN.md:130-140`, 특히 `:134-137`은 role마다 `Recommendation headlines`, `Feature card heads`, `Standard reading text`라는 body-table Notes를 갖는다. 이관본 Type roles `DESIGN.md:210-220`은 YAML Use와 px 관측을 남기지만 이 role-local Notes를 삭제한다. `provenance.md:117,163`과 `migration-log.md:15,19,54`의 typography/use 보존 주장은 실제보다 강하다.

### 5.6 검증된/편집적 관계 삭제 — A1 / B2 / E2 / E2c / F2 FAIL

원본의 다음 관계가 portable에서 사라졌다.

- `web/references/cafe24/DESIGN.md:381`: Naver 지분 취득과 commerce partnership 심화의 관계
- `:385`: lime-on-dark가 commerce를 energetic, not bureaucratic으로 보이게 한다는 관계
- `:440`: motion의 functional-and-friendly 성격과 no-bounce가 dependability, not gimmickry를 신호한다는 관계

이관본 `DESIGN.md:17,19,154,170-177`은 사실·동작 일부만 남긴다. 특히 `audit-log.md:31,89,102`, `provenance.md:154`, `migration-log.md:78`은 마지막 motion 문구가 portable에 없다는 이유로 limiter에서도 제거했다고 기록한다. B2의 올바른 처리는 삭제가 아니라 portable 본문 복원과 인접 완전 B2a다.

### 5.7 font evidence class 오분류와 `Official history` 승격 — A1 / A4 / B1 / E1 / E2 / E2c / F2 FAIL

원본 font 근거는 `web/references/cafe24/DESIGN.md:124-126,445-453`의 marketing Pretendard와 developers-docs Noto Sans KR live surface-use다. merchant philosophy는 `:383`의 narrative다. 이관본 `DESIGN.md:187-192`은 다음처럼 class를 바꾼다.

- `:189`의 Newsroom merchant-centric philosophy는 font evidence가 아닌데 `Official product-use` font row에 들어간다.
- `:191`은 같은 표 `:190`에서 live computed로 확인한 Pretendard/Noto Sans KR를 `Declared-only`라고 다시 분류한다.
- Scope `:17`의 `Official history`는 원본이 든 KED Global/Wikipedia/Newsroom evidence class보다 강한 표현이다.

인접 B2a와 “philosophy, not a type spec” 설명은 잘못된 table field/evidence class를 교정하지 못한다. `migration-log.md:19,28`과 `audit-log.md:32,90`도 이 current class를 승인한다.

### 5.8 App Blue를 input fill처럼 결합 — A1 / A4 / E2 / E2c / F2 FAIL

구조화된 원본은 `#3971ff`를 submit fill로만 둔다(`web/references/cafe24/DESIGN.md:18,58,161-168,452-453`). Input은 white background, `#1b1e26` text, `#d6dae1` border다(`:62,182-190,452-453`). 이관본 `DESIGN.md:15,41`은 `#3971ff`를 “4px submit buttons and inputs”에 붙여 fill 역할을 합친다. 같은 이관본 `:92-105,278-302,375-400`에 있는 올바른 분리와도 충돌한다.

### cafe24 재제출 조건

1. proof source를 canonical `web/references/cafe24/.verification.md`로 교정하고 false-absence 문장을 삭제한다. mirror를 적을 경우 canonical/derived 관계를 정확히 쓴다.
2. Circular Icon Button의 verified `Type: button`/interactive kind를 복원하고 역할 기준 applicability map을 작성하되 미관측 visual treatment는 발명하지 않는다.
3. Persona Card task는 삭제·비상호작용 결과로 축소하거나 exact tap/open/destination proof를 추가한 뒤 kind/map과 일치시킨다.
4. §9의 네 recipe를 receiving component/local-recipe 형태로 복원한다. 특히 dark-band와 white-card login pair의 parent-child tuple을 전역 token으로 승격하지 않는다.
5. Typography body-table Notes와 §11/§15 관계를 복원하고, 편집적 관계에는 완전 B2a를 인접시킨다.
6. Font의 무관한 `Official product-use`/`Declared-only` class와 `Official history` 표현을 삭제하거나 각각 해당 evidence class를 독립 입증한다.
7. `#3971ff`는 submit fill, 4px은 submit/input 공통 geometry로 정확히 분리한다.
8. affected provenance/migration/audit current-class와 source-row 다중 목적지, F2, SHA를 동기화하고 두 기계 검사를 재실행한다.

통과 확인: unitless line-height, §14 9행, B3 five-kind gate, C1-C3, curve omission ledger(E2b), formal F3 실행, portable Core는 통과했다.

## 결론과 재제출 프로토콜

웨이브 12는 **전체 FAIL**이다. 다섯 건 모두 같은 sol 검토자에게 위 고정 조건만 반영한 목록-only 재제출 대상이며 새 기준은 추가하지 않는다. 기존 fresh F3 실행 증거는 유지할 수 있고, 개정 후에는 최소한 다음이 current file/SHA와 일치해야 한다.

1. `DESIGN.md`의 값·관계·local binding·evidence class·kind omission
2. `provenance.md`의 metadata·proof·authority·omission 경계
3. `migration-log.md`의 source-row disposition과 모든 다중 목적지
4. `audit-log.md`의 superseded/current-class 구분
5. `--gate-only` PASS 및 portable Core `portable_core: true`

5/5가 동일 재심에서 PASS하기 전 웨이브 완료 및 카탈로그 채택으로 간주하지 않는다.
