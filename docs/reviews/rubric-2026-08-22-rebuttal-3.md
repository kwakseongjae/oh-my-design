# 루브릭 반박 3차 — grok-4.6

대상: `test-v2/90-comparison/RUBRIC.md` (2항 개정본)
대조: `docs/reviews/rubric-2026-08-22-rebuttal-2.md` 남은 2항
일시: 2026-08-22

**판정: FREEZE** — 남은 2항은 표가 아니라 본문에 있다. 새 채점·게이트 규칙은 없다.

---

## 1. 2항 실제 반영 (본문 기준)

표는 `## 개정 이력` 3차 대조표(406–411행)다. 아래는 본문만 본다.

| # | 2차 지시 | 본문 | 판정 |
|---|---|---|---|
| 1a | N1–N5 재분류·한 문장 단일 버킷 삭제 | §3 64–67행. 다섯 라벨과 “한 번만 싣고”가 없다. 본문에 N1–N5 식별자는 이력 표에만 남는다 | 본문 반영 |
| 1b | 제목 계층만 중립화, 원문 순서 보존 | §3 65–66행. “제목 계층만 중립화하고 원문의 절·문장 순서를 보존” | 본문 반영 |
| 1c | 허용 변환을 한 줄로 | §3 66행. “허용 변환은 표지 삭제와 경로 삭제뿐이다” | 본문 반영 |
| 1d | Core v2 SHA를 채점·전사 패킷에 첨부하지 않음 | §4.4 233–235행. 용어 참고본이며 “채점·전사 패킷에는 첨부하지 않는다” | 본문 반영 |
| 1e | 파일럿 앵커 = 본실험과 같은 패킷 | §3 75–77행 | 본문 반영 |
| 2a | 추측 단위 = 레인 산출물 1회. “축별” 삭제 또는 축 패킷 별도 추측 | §3 79–83행. “각 레인의 각 익명 산출물마다 … 한 번만”. `B_raw`는 “레인별로”만 보고. 이전 “레인별·축별”은 이 절에서 삭제됨. 87행 “축별”은 평가자 일치도이며 추측 분모가 아니다 | 본문 반영 |
| 2b | `COMPROMISED`는 `형식 단서` 부분집합의 우연 초과에만 | §3 83–85행 | 본문 반영 |
| 2c | §8 조건 6의 합친 `B_raw` PASS 킬 제거. 측정·보고는 남김 | §8 358–366행. 조건 1–5만. 조건 6 없음. `B_raw`/`B_adj`/Wilson은 82–83행에 보고로 남음 | 본문 반영 |

`COMPROMISED`가 단독 우승만 막고 PASS는 막지 않는 것(§3 85행, §8에 조건 6 없음)은 2차가 요청한 누수 게이트와 측정 분리와 같다.

## 2. 범위 초과

지시하지 않은 새 채점 축·임계·PASS 조건은 없다.

§9 375행에서 동결 체크의 `Core v2 SHA` 기록 항목을 뺀 것은 패킷 미첨부(1d)와 같은 줄이다. 새 규칙이 아니다. SHA 값 자체는 §4.4에 남아 있다.

## 3. 동결 절차

배너를 `FROZEN`으로 바꾸고 본문 규칙을 더 고치지 않는다. §9 첫 체크(반박·수정 완료)를 채운 뒤, 코드북 9값 등 나머지 실행 게이트가 채워진 그 커밋에서 `git rev-parse HEAD`를 배너와 `docs/CURRENT_STATE.md`에 `RUBRIC_FROZEN_SHA=<SHA>`로 기록한 다음에만 T3-3을 연다.

VERDICT: FREEZE
