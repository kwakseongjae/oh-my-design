# T2-1 소급 전수 검토 2/2 — sol 의미 레인

- 대상: `docs/design-md-weight/migrated/{airbnb,slack,line}/`
- 원본: `web/references/{airbnb,slack,line}/DESIGN.md`
- 기준: `docs/design-md-weight/MIGRATION_RULEBOOK.md` **v7 전 조항**
- 일시: 2026-08-23
- 성격: 웨이브 3 표본에서 빠져 sol 검토 기록이 없던 3건의 **첫 전수 판정**이다. 목록-only 재확인이 아니다.
- 범위: 원본 값·형태·역할 결합, §9/§14 고유값, evidence class, state applicability, portable/provenance 분리, source-row별 migration disposition, F1–F3 절차를 처음부터 대조했다.

## 기계 검증과 F3 인벤토리

다음 두 검사를 현재 파일에서 직접 재실행했다.

```text
node test-v2/tools/migrate-reference.mjs --brand <id> --gate-only
node scripts/migrate-design-md-core.cjs \
  --input docs/design-md-weight/migrated/<id>/DESIGN.md \
  --check --require-portable-core --json
```

| 대상 | gate | Core | DESIGN SHA-256 |
|---|---|---|---|
| Airbnb | PASS, problems 0 | exit 0, `portable_core: true` | `a73cf275f06716ee614bfbd91ae4f016832741bd671338cca53c68b3336bb745` |
| Slack | PASS, problems 0 | exit 0, `portable_core: true` | `c16cfef8f41424508ff84a2d83dd54dfd4fa52f139489704477a862df7c5ddf9` |
| LINE | PASS, problems 0 | exit 0, `portable_core: true` | `cff4f2376db6eb30b0baecbefb69b4c9528a9bc1c6fee503e17d1f7d0a6ee6d7` |

세 대상 폴더에는 `DESIGN.md`, `provenance.md`, `migration-log.md`, `worker-log.txt`만 있다. 세 migration log 모두 규칙집 **v5**를 기록하고, F1·F2 수행 기록과 별도 신선 세션 F3의 `audit-log.md`가 없다. 따라서 기계 PASS는 v7 의미·절차 PASS를 대신하지 못한다.

## 판정 요약

| 대상 | 판정 | 차단 조항 |
|---|---|---|
| Airbnb | **FAIL** | A1/A1c, B2/B2a, C2, E1, E2/E2a/E2c, F1/F2/F3 |
| Slack | **FAIL** | A1/A3, B2/B2a, C2/C4, D1, E1, E2/E2a/E2c, F1/F2/F3 |
| LINE | **FAIL** | A1/A1c/A3, B2/B2a, C2, D1, E1, E2/E2a/E2c, F1/F2/F3 |

**소급 전수 결과: PASS 0/3, FAIL 3/3.** 이 세 건은 아직 양 레인 통과 집합에 추가할 수 없다.

## v7 전 조항 대조표

| 조항 | Airbnb | Slack | LINE |
|---|---|---|---|
| A1 | **FAIL** — evidence class·footer·generic behavior | **FAIL** — live Proof·§9 conflict·narrative | **FAIL** — verification method·component depth·Popup anatomy |
| A1a | PASS — unitless 5개 유지 | PASS — unitless 8개 유지 | PASS — unitless `1.334` 유지 |
| A1b | PASS — button/tab/input/listItem | PASS — primitive types 개별 유지 | PASS — button/input/dialog/tab/badge |
| A1c | **FAIL** — `(omd:migrate)` 손실 | PASS — source metadata 원장화 | **FAIL** — `in-app visual verification` 손실 |
| A2 | PASS — §14 본문 보존 | PASS — §14 12행 보존 | PASS — §14 10행+collector 한정 보존 |
| A3 | PASS — §9 고유값 수용 | **FAIL** — Success/Presence conflict 손실 | **FAIL** — Popup `optional area` 손실 |
| A4 | PASS | PASS | PASS |
| B1 | PASS | PASS | PASS |
| B2/B2a | **FAIL** — Principles 밖 다수 미한정 | **FAIL** — 일부 한정 밖 판단 잔존 | **FAIL** — UI implication 밖 판단 잔존 |
| B3 | PASS — 5종+per-component gate | PASS — 5종+per-component gate | PASS — 5종+per-component gate |
| C1 | PASS | PASS | PASS |
| C2 | **FAIL** — generic action outcome 단정 | **FAIL** — mixed/generic role outcome 단정 | **FAIL** — generic Box Button outcome 단정 |
| C3 | PASS | PASS | PASS |
| C4 | PASS | **FAIL** — 근거 없는 `non-interactive` kind | PASS |
| D1 | PASS | **FAIL** — 새 font/license coverage | **FAIL** — 새 locale coverage |
| D2 | PASS | PASS | PASS |
| E1 | **FAIL** — standalone 한정 불완전 | **FAIL** — live/derived class 과승격 | **FAIL** — standalone 한정 불완전 |
| E2/E2a/E2c | **FAIL** — logo/ds/Named gaps/준수 주장 | **FAIL** — §9/§11/Kind/준수 주장 | **FAIL** — footer/logo/§9/준수 주장 |
| E2b | 적용 대상 없음 — source placeholder 0 | 적용 대상 없음 — source placeholder 0 | PASS — §15 wrapper를 omission ledger에 보존 |
| F1/F2/F3 | **FAIL / FAIL / FAIL** | **FAIL / FAIL / FAIL** | **FAIL / FAIL / FAIL** |

## 1. Airbnb — FAIL

### 1.1 Evidence class와 검증 metadata가 바뀌거나 사라짐 — A1/A1c/E1/E2 FAIL

원본 Font evidence는 `Unresolved`를 독립 evidence class로 둔다(`web/references/airbnb/DESIGN.md` 183–189행). 이관본은 이를 `Outside this capture`로 바꾼다(`docs/design-md-weight/migrated/airbnb/DESIGN.md` 104–110행). Migration log는 `Unresolved`가 placeholder 금지에 걸려 바꿨다고 설명하지만(19행), 금지 대상은 `[FILL IN]`이고 unresolved claim은 증거 종류다. 의미 동치가 아니다.

원본 footer의 `Verified: 2026-07-12 (omd:migrate)`(298행)도 provenance에는 날짜만 남고(`provenance.md` 28–34행) `(omd:migrate)`가 사라졌다. 그런데 migration log 35행은 footer `Verified`를 provenance로 분리했다고 기록한다.

### 1.2 Principles 한정이 전편의 편집 판단을 덮지 않음 — B2/B2a/E1/F1 FAIL

완전한 qualifier는 이관본 38행 한 곳뿐이고 문언상 `These four items`, 즉 번호 원칙 네 개만 덮는다. 다음 retained block은 그 범위 밖이다.

- Scope의 evidence-domain·인과·visual-signature 판단: 9–13행
- Audience와 Distinctive traits: 26, 30–34행
- Capture-bound application과 Avoid: 45–57행
- Semantic/local-scale/elevation 판단: 65–73, 77, 86–92행
- Font/asset application 경계: 116, 132행
- Layout의 목적·cross-viewport 해석: 288–292행
- Voice/application 규칙: 297–299행

원본에 문장이 있었다는 사실만으로 Airbnb-authored doctrine이 되지 않는다. Governance의 일반 authority 문구도 인접한 evidence-class 구분을 대신하지 못한다. 현재 provenance에는 이 실제 derived 범위가 없고, v5 worker/log에도 F1 전편 재독 기록이 없다.

### 1.3 Generic action의 정확한 행동을 발명해 상태를 닫음 — A1/C2/E2c FAIL

원본이 확인한 것은 Hosting action의 style/use(원본 YAML 142행)와 circular/disabled circular control의 style·captured states(144–145행)다. Exact label, destination, request, validation, outcome behavior는 없다.

이관본은 Hosting action을 navigation으로 단정해 loading/error/success를 모두 `not-applicable`로 만들고(167–169행), 두 circular control도 `local marketplace action`으로 단정해 같은 세 field를 닫는다(216–218, 240–242행). Provenance 120행과 migration log 33행은 이를 C2 준수로 확정하므로 준수 주장도 실제 근거보다 강하다. 역할이 식별된 Category tab과 Search shell은 이 결함의 직접 대상이 아니다.

### 1.4 실제 다중 목적지를 migration log가 닫지 못함 — E2/E2a/F2 FAIL

- Simple Icons `airbnb`는 provenance 15행과 portable Assets 132행에 모두 있지만, migration log 13행과 provenance 20행은 provenance-only / Typography & Assets 비승격이라고 기록한다.
- `ds` 이름·설명은 portable Scope/Assets 9, 132행과 provenance 22행에 쓰였지만, log 14행은 URL 외 `ds`를 provenance-only로 적는다.
- §3 font gaps는 Typography 104–116행과 Named gaps 335, 346–349행에, §4 omitted components는 Components 141행과 Named gaps 338행에, §6 cookie shadow는 Foundations 92행과 Named gaps 343행에 각각 간다. 각 source row는 두 번째 목적지를 누락한다.
- §8 responsive gaps, §14 absent states, §15 motion gap, YAML `rounded.card` 12도 각각 본문과 Named gaps에 동시에 존재하지만 log 15, 27, 33–34행은 Named gaps 목적지를 기록하지 않는다.

이는 source-row별 grep 대조가 완료되지 않았다는 직접 증거다.

### Airbnb에서 확인된 통과 항목

Unitless line-height 다섯 값, primitive types, §14 본문, §9-only 값, field-role 결합, generic focus/`focus-visible` 분리, B3 전문, C1/C3/C4, 가상 persona 비승격은 현재 파일에서 유지된다.

### Airbnb 재제출 조건

1. `Unresolved claim` evidence class와 footer `(omd:migrate)` metadata를 복원한다.
2. Portable 전체를 F1 재스캔해 위 편집·인과 판단을 제거하거나 각각 인접 완전 B2a 아래 두고, 실제 derived 범위를 provenance와 log에 맞춘다.
3. Hosting action과 두 circular control의 exact behavior evidence를 추가하거나 loading/error/success 세 field만 최소 경계에서 생략한다.
4. Logo, `ds`, footer, 모든 Named gaps를 포함해 source-row별 실제 목적지를 migration log에 다시 기록하고 F2 대조 기록을 남긴다.
5. 위 값·상태·장부 수정 뒤 별도 신선 세션 F3를 실행하고 `audit-log.md`에 기록한다. 두 기계 검사를 재통과한 뒤 동일 sol 의미 레인에 재제출한다.

## 2. Slack — FAIL

### 2.1 Prose-derived/representative 값을 live product Proof로 승격 — A1/E1 FAIL

원본은 `tokens.source: prose-derived`다(`web/references/slack/DESIGN.md` 15–16행). Direct WebFetch가 확인한 것은 slack.com 마케팅의 aubergine chrome, conversational/AI-forward voice, green CTA뿐이다(614–617행). In-app component pixels는 representative observation이며 exact internal token name이 아닐 수 있다고 명시한다(631–634행).

그런데 provenance는 같은 URL을 Surfaces에서 marketing으로 기록하면서(39행) Sources에서는 `product-surface`로 바꾸고(47행), 모든 typography/token과 sidebar/composer/dialog/toggle까지 `slack-live`에 claim별 매핑한다(68–85행). Exact per-claim live evidence가 없는 값을 live product Proof로 올린 것이다.

### 2.2 §9-only conflict와 §11 narrative 값이 손실됨 — A1/A3/E2/E2a FAIL

원본의 정규 Success는 `#2BAC76`이지만(32, 122행), §9 Quick reference에는 별도로 `Success / Presence: #2EB67D`가 있다(496행). 이관본은 Success `#2BAC76`과 Online `#2EB67D`를 확정 분리하고(104–105행), provenance Conflicts 33행에도 원 ambiguity를 남기지 않았다. Migration log 32행의 `§9 고유 값만 옮김` 주장과도 맞지 않는다.

원본 §11의 Glitch 실패 연도 2012, fastest-growing/1년여 만의 $1B valuation은 산출물 전체에서 사라졌다(원본 536–540행; portable Scope 13행; provenance 64행). Founder Stewart Butterfield/ex-Flickr와 2019 redesign은 provenance 64행에만 있는데 log 34행은 Scope에 요약했다고 기록한다. 반대로 Tiny Speck/2014/Salesforce/$27.7B는 portable Scope 13행과 provenance 64행의 실제 dual destination인데 source row들은 두 목적지를 함께 기록하지 않는다.

### 2.3 일부 한정만으로 전편의 derived 판단을 닫음 — B2/B2a/E1/F1 FAIL

완전한 qualifier는 Scope의 다음 문단(11행), Principles 여덟 항(39행), Elevation(136행), type-character(205행), whitespace(654행), voice(680행)에만 인접한다. 다음 판단은 그 범위 밖이다.

- Distinctive traits: 30–35행
- Capture-bound application과 Avoid: 50–69행
- Motion의 character·purpose 읽기: 162–168행
- 새 component applicability 총론과 후속 state maps: 252행 및 274–460, 552–649행

Provenance 99행은 retained interpretive sites를 실제보다 좁게 기록한다. Migration log 39행의 B2a 준수 주장도 현재 body보다 강하다.

### 2.4 Mixed/generic 역할로 outcome state와 Kind를 확정 — A1/C2/C4/E2c FAIL

- Marketing CTA 세 종을 destination으로 단정해 loading/error/success를 전부 비적용 처리한다(280–282, 305–307, 330–332행).
- `Send`와 `Create channel`을 합친 Product Primary에 message/toast 전역 상태를 연결해 세 field를 닫는다(334–358행).
- Danger의 navigation/toast, Text Field의 toast/sent-message, Toggle의 save-failure toast 연결은 원본의 exact component behavior가 아니다(406–408, 432–434, 645–647행).
- Unread, Mention, Status, Presence, Toast에는 interactive-kind 근거가 없는데 `Kind: non-interactive`를 확정한다(502, 513, 523, 534, 592행). C4는 이런 경우 kind와 map 자체를 생략하도록 요구한다.

Migration log 25, 37행의 C4/C2 준수 주장은 따라서 성립하지 않는다.

### 2.5 원본 밖 font/license coverage 부정 claim — D1 FAIL

원본은 font family/stack과 corroboration만 기록한다(143–146, 623–624행). 이관본은 `No Slack-exclusive public redistribution right`, `Lato license terms are not stated`, loadable Larsseit/Circular의 third-party reproduction이 outside capture라는 새 coverage claim을 만든다(193–194, 731행). 독립 evidence가 없다면 이 미해상 field는 문장으로 부정하지 말고 생략해야 한다.

### 2.6 F2/F3 절차와 disposition 결과가 없음 — E2/E2a/E2c/F2/F3 FAIL

위 §9/§11 외에도 §15는 실제로 Foundations Motion, Governance Named gaps 728–729행, provenance omission ledger 101행에 가지만 log 38행은 Named gaps 목적지를 누락한다. HTML comment의 history/voice/type evidence도 portable과 provenance 양쪽에서 쓰이는데 log 39행은 provenance-only다.

Migration log 8행과 worker log는 v5 작업만 기록한다. F1/F2 수행 기록과 `audit-log.md`가 없으므로 신선 세션 F3도 실행되지 않았다.

### Slack에서 확인된 통과 항목

Unitless line-height 여덟 값, primitive types, freshness metadata, §14 12행, toast shadow·banner border·backdrop·marketing-green의 명시된 값 충돌, generic focus/`focus-visible` 분리, B3 전문, C1/C3, 가상 persona 비승격은 보존됐다. Source placeholder는 없다.

### Slack 재제출 조건

1. Claim ledger를 marketing live, representative in-app observation, prose-derived evidence로 다시 분리하고 근거 없는 `slack-live` per-claim mapping을 제거한다.
2. §9 Success/Presence ambiguity와 누락 narrative 값을 정확한 evidence class로 복원한다.
3. Portable 전체를 F1 재스캔해 모든 derived 판단을 제거하거나 인접 완전 B2a 아래 두고 provenance derived inventory를 동기화한다.
4. Mixed/generic role은 exact behavior evidence로 분리한다. 근거가 없으면 loading/error/success를 최소 field 경계에서 생략하고, 근거 없는 `Kind: non-interactive`도 생략한다.
5. 새 font/license coverage claim을 제거하거나 독립 evidence를 추가한다.
6. §9, §11, §15, HTML comment를 포함해 source-row별 모든 실제 목적지와 준수 문구를 migration log에 맞추고 F2 기록을 남긴다.
7. 별도 신선 세션 F3를 실행하고 `audit-log.md`에 기록한 뒤 두 기계 검사를 재통과해 동일 sol 의미 레인에 재제출한다.

## 3. LINE — FAIL

### 3.1 Verification method와 공식 component depth 손실 — A1/A1c/E2/F2 FAIL

원본 footer는 `Verified: 2026-07-11 (eight-surface collector plus in-app visual verification)`를 명시한다(`web/references/line/DESIGN.md` 360행). Provenance는 날짜(35행)와 collector 수치(43행)만 두고 `in-app visual verification` method를 잃었다. Migration log 18행은 footer를 provenance로 분리했다고 기록하므로 실제 disposition과 다르다.

원본은 공식 component system이 sheets, navigation, lists, cards, feedback까지 포함하고 각각 anatomy, usage, state behavior가 정의됐다고 기록한다(210행). Portable은 이를 `outside the harvested token set`과 Named gap으로만 바꾼다(221, 456행). 토큰 승격 없이 공식 범위·정의 존재라는 narrative fact는 보존해야 한다.

### 3.2 §9에만 있던 Popup anatomy 손실 — A1/A3/E2 FAIL

원본 Popup construction prompt만 `optional area`를 명시한다(432행). Portable Popup 350–358행에는 이 anatomy가 없고, migration log 29행은 §9 고유값을 password visibility toggle과 70px type boundary뿐이라고 기록한다. 받을 Components 슬롯이 있으므로 wrapper를 삭제해도 이 field는 보존해야 한다.

### 3.3 UI implication 한정 밖에 retained editorial block이 남음 — B2/B2a/E1/F1 FAIL

이관본 38행의 완전한 한정은 문언상 `UI implication notes`만 덮는다. 다음은 별도 block이다.

- Scope의 identity/`direct, approachable tone` 및 surface-application 판단: 9, 13행
- Audience의 observable-work/research prescription: 26행
- Capture-bound application: 53–62행
- Hierarchy/elevation synthesis: 120–122행

원본에 이미 있었다는 이유로 LINE-authored doctrine이 되지 않으며 Governance 422행은 인접 한정을 대신하지 못한다. Migration log §1/§6/§7/§12 행(17, 25–27, 33행)과 provenance derived inventory도 최종 body 범위에 맞아야 한다.

### 3.4 Generic Box Button outcome applicability 과단정 — C2/E2c FAIL

원본 역할은 Primary=`most important general action`, Destructive=`delete/replace/remove`, Outline=`secondary/turned-off`이고 exact label/destination/request/outcome은 없다(원본 274–300행; YAML 192–194행). Official state list가 loading은 지지하지만 error/success가 이름 붙지 않았다는 사실은 `not-applicable` 근거가 아니다.

이관본은 세 Box Button의 error/success를 모두 `not-applicable`로 확정한다(242–243, 269–270, 294–295행). 특히 delete/replace/remove의 failure/outcome 가능성을 배제할 evidence가 없다. Text/password/code를 합친 Text Input의 loading/success 344, 346행도 같은 기준으로 다시 감사해야 한다. Migration log 36행의 C2 준수 주장은 현재 근거보다 강하다.

### 3.5 원본 밖 locale coverage 부정 claim — D1/E2c FAIL

Portable은 `Locale-specific number, date, address, and legal rules ... are omitted`라고 쓴다(414행). 원본 §8/§10은 multilingual label length와 Voice만 기록하고 number/date/address/legal coverage는 말하지 않는다(407–445행). 가장 작은 미해상 경계는 침묵이어야 한다. Migration log 30행의 `원본에 없는 도메인 coverage 신설 없음` 주장도 실제보다 강하다.

### 3.6 Simple Icons의 실제 이중 목적지를 부정 — E2/E2a/F2 FAIL

원본 logo는 `simpleicons/line`이고(8–10행), provenance Identity에도 존재한다(15행). Portable Assets도 `Catalog logo metadata ... (line)`를 직접 보존한다(194행). 그런데 provenance 25, 155행과 migration log 13행은 `not promoted into Typography & Assets` / provenance-only라고 기록한다. 실제 disposition은 provenance + portable Assets boundary다.

### 3.7 v7 절차 미수행 — F1/F2/F3 FAIL

Migration log 8행과 worker log는 규칙집 v5 이관만 기록한다. F1/F2 수행 기록이 없고 실제 B2a/E2 결함도 남았다. `audit-log.md`가 없어 별도 신선 세션 F3도 실행되지 않았다.

### LINE에서 확인된 통과 항목

Unitless `1.334`, primitive types, component-local field roles, §14 10행과 collector 한정, generic focus/`focus-visible` 분리, B3 전문, C1/C3/C4, 가상 persona 비승격, §15 placeholder wrapper의 provenance omission ledger는 보존됐다.

### LINE 재제출 조건

1. `in-app visual verification` method, 공식 확장 component 범위와 정의 존재, Popup `optional area` anatomy를 복원한다.
2. Portable 전체를 F1 재스캔해 모든 retained editorial block을 제거하거나 인접 완전 B2a 아래 두고 provenance/log를 동기화한다.
3. 세 Box Button의 error/success는 exact behavior evidence를 추가하거나 최소 field 경계에서 생략한다. Text Input도 같은 기준으로 재감사한다.
4. 새 locale coverage 부정 claim을 삭제하거나 독립 evidence를 추가한다.
5. Simple Icons를 포함한 source-row별 실제 목적지와 준수 문구를 migration log에 맞추고 F2 기록을 남긴다.
6. 별도 신선 세션 F3를 실행하고 `audit-log.md`에 기록한 뒤 두 기계 검사를 재통과해 동일 sol 의미 레인에 재제출한다.

## 소급 원장 판정

**최종 판정: Airbnb FAIL / Slack FAIL / LINE FAIL.** 세 기계 gate와 Core checker의 PASS는 의미 보존과 v7 절차 PASS를 대신하지 않는다.

이 문서는 공백 3건에 대한 첫 sol 판정만 닫는다. 세 개정본이 위 목록을 해소하고 동일 sol 목록-only 재확인에서 PASS하기 전에는 이 3건을 양 레인 통과 집합에 더하지 않는다. 재제출에는 이관당 아직 한 번도 실행되지 않은 F3를 한 번 수행해야 하며, 그 뒤의 목록-only 재확인에 다시 새 F3를 요구하지 않는다. 카탈로그 채택은 별도 최종 승인 사항이다.
