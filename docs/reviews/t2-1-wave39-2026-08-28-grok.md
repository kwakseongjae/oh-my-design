# T2-1 웨이브 39 판정 — kkbox · kkday · kmong · krafton · kraken

- 일자: 2026-08-28
- 모델: `cursor-grok-4.6-high-fast` (검토는 `-f` 없이 read-only)
- 규칙집: v12 + `docs/design-md-weight/prompts/` (worker-addendum · f3-conditions · review-header)

## 판정

| 브랜드 | 이관 | 게이트 | F3 fixes | 본문 완전형 = 원장 | 검토 |
|---|---|---|---:|---|---|
| kkbox | OK | PASS | 27 | 27 = 27 (prov 135–161) | **PASS** |
| kkday | OK | PASS | 19 | 45 = 45 (prov 134–178) | **PASS** |
| kmong | OK | PASS | 22 | 23 = 23 (prov 176–198) | **PASS** |
| krafton | OK | PASS | 27 | 30 = 30 (prov 163–192) | **PASS** |
| kraken | OK | PASS | 24 | 40 = 40 (prov 143–182) | **PASS** |

- F3 총 119건. 검토 5/5 PASS, 개정 0건.
- 5개 전부 `inspectDesignMd(md).conformance` → `portable_core: true`, `level: portable-core`.

## 값 계열 확인 요지

- **A1 키 경로** — 5개 모두 YAML 키가 각 슬롯에 행으로 착지. easywallet형 오판(스케일 값과 CSS 값 혼동) 없음.
  - kkbox `spacing.sm: 16` ≠ padding `16px`, `rounded.sm/md/lg: 30` ≠ CTA `30px`
  - kkday `spacing.md: 12` ≠ card body `12px`, `rounded.md: 8`과 `lg: 8`은 두 키
  - kmong `spacing.xxs: 2` dest 1 (dotted 경로 dest 0 — 다른 스케일의 `2` 아님)
  - krafton `spacing.xxl: 140` ≠ hero 140, `spacing.md: 40` ≠ container `40px` ≠ button `15px`
  - kraken 토큰 귀속이 home / `/prices` 두 표면으로 분리 유지
- **A1b Primitive type** — 4개 브랜드에서 YAML `type:` 레코드 수와 1:1 (krafton button/input/tab/listItem 1:1:1:1, kkday button 3 / badge 3 등).
- **C2 상태 쌍** — loading/error/success 개방·폐쇄가 컴포넌트별로 일관. 혼합 개방(loading만 열고 error 닫기) 0건.
  - kkbox Download Button 쌍닫힘, kkday·kraken·krafton Primary만 개방, kmong Search는 `error | applicable` 1건만
- **D2a 페르소나 파생물** — 5개 전부 식별자·동기 문자열 DESIGN dest 0. Audience는 원본 그룹명 원문만.
- **D1 발명 도메인** — `native application` / `mobile app` / `back-office` / `product application` 5개 전부 dest 0.
- **B1 sibling 융합** — `measures 1440px` / `14px radius with 16px padding` 5개 전부 dest 0. sibling 전용 문자열은 원장 mention만.
- **항목 11 (짧은 쪽 절단)** — YAML `use`와 §3/§4 서술을 긴 쪽으로 병기. 절단 0건.

## 이번 웨이브에서 나온 새 관찰

- kkday: 원본 §15 곡선 세 값이 `:160`에 인용된 채 역할만 남는 T2 관례가 값 소실로 오독될 여지가 있음 — 검토자가 관례로 인정하고 PASS. 규칙집 문면화 후보.
- kmong: 원본이 모션 규칙을 명시적으로 갖지 않는 브랜드. `intentionally omitted rather than synthesized`가 본문에 남아 합성 금지를 스스로 진술 — 부재를 부재로 옮긴 모범 사례.
- krafton: `#ffffff` inner가 canvas가 아니라 Body Background로 귀속. 같은 hex의 표면 구분이 필요한 사례.

## 운영 사고 (작업 자체와 무관, 도구 결함)

- kmong F3는 20:33 완료했으나 22분간 진행이 멈춤. 원인은 대기 루프의 **자기매칭**: `pgrep -f 'cursor-agent/versions'` 패턴이 대기 셸 자신의 명령줄에 들어 있어 자신을 잡았고, PID 재사용으로 두 번째 대기 루프는 자기 PID를 기다림.
- 프로세스 2기를 "다른 프로젝트 것"으로 오판했으나 둘 다 자체 대기 셸이었음. 정정 후 메모리 기록.
- 규칙: 완료 신호는 프로세스가 아니라 산출물(`audit-log.md`의 `AUDIT_DONE`)로 판단. 맨 PID 대기 금지.
