# 최종 승인 — grok-4.6

대상: `docs/OMD_3TRACK_PLAN.md` 개정본
일시: 2026-08-22 · 경위: 기안(Opus 5) → 독립검토 A(sol, BLOCK)·B(grok, PROCEED_WITH_CHANGES) → 개정 → 최종 심사

**판정: APPROVE_WITH_CONDITIONS** — 조건 C1~C7.

열림: T3-0 측정기 코드 · 기존 PNG 입고 · T1-1
닫힘: T3-1 이후, T1-2 이후, T2 전부

---

## 1. 반영 검증 — 빠졌거나, 반영이라고 적혀 있으나 아닌 것

**반영된 것(대조용):** 영상 조건부(계획 81–84행), 단계표에서 루브릭→파일럿 순서와 파일럿 분모 제외(75–76행), 레인 A/B(114–130행), 스냅샷 공유·해석 주입 금지 정책(132–136행), book/상태 매트릭스를 능력 커버리지로 분리(142–151행), 식별력+천장(153–155행), DESIGN.md 채점 Core v2 SHA(157–158행), T1-4가 v3를 전제하지 않음(183–190행), T2 순서 dual reader→writer→canary→rollback(207–212행).

**반영되지 않은 것 / 거짓 반영:**

1. **실행 포인터가 옛 번호다.** 단계표의 T3-2는 루브릭 동결이다. 「다음 행동」은 여전히 `T3-1 → T3-2 무신사 1건 왕복`(297–298행)이고, 병행 다이어그램은 `캡처 → 유도규칙 → 무신사 왕복`(291행)이다. 검토 A/B가 요구한 순서는 표에만 있다.
2. **T3-1 템플릿이 T3-0·T3-6을 뒤집는다.** T3-0 #3·#4는 `luminanceGradient` 개명과 “figure-ground 실패 ≠ 엣지투엣지”다. T3-6은 그 해석을 팩에 넣지 말라고 한다. README §3도 같은 커밋에서 완화됐다. 그런데 T3-1은 `조명: …lightDirections`와 `대신 "엣지투엣지 크롭"이 근거가 된다`(105–112행)를 남긴다. `analysis.mjs:155`도 아직 `edge-to-edge composition`을 reason으로 쓴다.
3. **원본 보존은 계획이 선언하고 README가 부정한다.** 계획 225–233행은 README §6을 틀렸다고 적는다. README 131행은 `재현이 필요하면 캡처를 다시 돌리면 된다`를 그대로 둔다. git-밖 저장소 경로·책임자는 없고, 무신사 `evidence.json`에는 locale·로그인 키가 없다. PNG는 디스크의 gitignore 경로에만 있다.
4. **모집단은 목적 문장만 9개다.** T2는 여전히 `T3의 10개`(220–221행), README §9는 `나머지 9개`(161행), 다음 행동은 `9개를 캡처`(298행). 캔바를 T3에서 뺀 선택을 T2 문장이 되돌린다.
5. **게이트가 이름만 생긴 항목:** 반복 횟수·arm 무작위화·재시도, 4변형 중 어느 장, hallmark 질문 허용, `verify.json` vs 인간 충돌 시 승자, Playwright–Aside 동등성, T1 수용 기준, 시맨틱 아트디렉션 계층, `collectChrome`의 면적 최대 불투명 버튼(`capture-media-evidence.mjs:91–101`). T3-7·T3-4·T2-5는 “나중에 고정/명시”이지 조건이 아니다.
6. **§0 경쟁 조건**(13행)은 여전히 `핸디캡 없음. 최대 강도` 한 줄이다. T3-6이 배타라고 한 가설을 확정 표가 다시 한 실험으로 건다.

## 2. 개정이 만든 새 구멍

- 단계표 / 다음 행동 / 다이어그램이 한 문서에서 서로 다른 실험을 지시한다. 승인 직후 에이전트는 다음 행동을 따른다.
- T3-0 #4가 같은 개정이 이미 고친 README §3을 미수정 결함으로 인용한다. 실행자가 README만 보면 도구의 reason 문자열을 건너뛴다.
- 「이미 끝난 것」(64–66행)은 광고를 브랜드 색으로 오인하던 결함을 제거했다고 한다. T3-0 #1과 무신사 `primaryButton = #5ccca8`(라벨 빈 문자열)이 그 반례다.
- T2 표가 깨졌다(200–206행, 헤더 두 번 + 편집 메모).
- T1-5는 T1-4 분기와 상관없이 `새 규격으로` 왕복한다(184행).
- 무신사 재캡처는 표(37행)에만 있고 단계 번호가 없다. 지금 T3-3을 열면 `/recommend?gf=A` JSON으로 `category/001` 파일럿을 하게 된다.

## 3. 착수 가능 단위

**지금 연다**

| 단위 | 이유 |
|---|---|
| T3-0 측정기 코드 | 결함 5건이 실측 파일에 묶여 있다. 비교가 아니다. |
| 기존 musinsa/coupang PNG 입고 | 로컬 `capture/`가 gitignore 상태로만 있다. |
| T1-1 440 무게 실측 | T3와 기술적 의존이 없고 v3를 전제하지 않는다. |

T3-0 완료의 최소 검증: 기존 무신사 PNG를 **재분석**(재캡처 아님)했을 때 `subjectCenterX` 키가 사라지고, `primaryButton`이 `#5ccca8`이 아니거나 필드가 생략된다.

**지금은 닫는다** — 계획 「다음 행동」 그대로의 T3-1→왕복, T3-3 파일럿, T3-4 이후, T1-2 이후, T2 전부. T3-2 루브릭 **초안**은 T3-0과 병행 가능하나, 아래 C6 필드 없이 **동결하지 않는다.**

## 4. 조건

**C1. 실행 포인터 정합** (T3-1 전)  
다이어그램·다음 행동·캔바 문장·T2 표를 단계표와 같은 번호·분모로 맞춘다. 검증: `T3-2`가 왕복으로 검색되지 않고, `T3의 10개`가 없다.

**C2. T3-1이 T3-0·T3-6과 모순하지 않는다** (T3-1 전)  
112행의 엣지투엣지 대체 근거를 지우고, `lightDirections`를 조명 슬롯에 쓰지 않는다. 검증: 계획 T3-1과 `RULES.md`에 그 두 패턴이 없다.

**C3. README 동기화** (새 캡처 전)  
§6 재캡처 문장 삭제, §9를 잔여 7+무신사 재캡처로, §5 주 버튼을 유효 크로메로 제시하지 않음. 검증: `다시 캡처하면 된다`, `나머지 9개`가 README에 없다.

**C4. 저장소 경로 한 줄 + 기존 PNG SHA 입고** (새 캡처 전)  
검증: manifest SHA = 로컬 파일 SHA.

**C5. 무신사 재캡처를 T3-0 이후·T3-3 이전 단계로 번호 부여** (T3-3 전)  
검증: 표 URL과 새 `source.finalUrl`이 같은 표면. 현 `/recommend?gf=A`로 T3-3 금지.

**C6. 루브릭 동결 필수 필드를 계획에 박는다** (T3-2 동결 전)  
축 가중치, 평가자 수·일치도, 동률/결측/abandon, 통과 임계, 브랜드당 반복, 4변형 선택, verify vs 인간 승자, 레인 A 턴·시간·재시도, hallmark 질문 허용, 시맨틱 아트디렉션 포함 또는 **명시적 제외**. 검증: `90-comparison/RUBRIC.md`에 빈칸 없이 있고 반박을 통과한 뒤에만 T3-3.

**C7. 쿠팡 유지/탈락 한 줄 + 채널 동등성 허용치** (T3-4 전)  
검증: 쿠팡이 비교 분모에 있는지 T3-4 착수 전에 결정되어 있다.

`docs/OMD_3TRACK_PLAN.md`의 **다음 행동은 이 승인의 실행 지시가 아니다.**

VERDICT: APPROVE_WITH_CONDITIONS
