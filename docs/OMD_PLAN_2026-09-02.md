# 진단·잔여 작업·향후 계획 — 2026-09-02 (Fable 5.1 전환 시점)

> 정본. 이 문서가 다음 2주의 실행 순서를 정한다. 실행 중 변경은 여기를 먼저 고치고
> CURRENT_STATE.md에 반영한다. 작성: Fable 5.1 오케스트레이터. 근거는 전부 실측(경로·시각 병기).

## 0. 현재 상태 진단 (09:30 기준)

| 축 | 상태 | 근거 |
|---|---|---|
| T2 마이그레이션 | 원장 **229/440 (52%)**. 웨이브 46 진행 중(러너 첫 실전). 50 완주 시 253 | `migrated/DONE.txt`, `WAVES-36-50.md` |
| **카탈로그 채택 경로** | **없다.** 229본은 `docs/design-md-weight/migrated/`에만 있고 `web/references/`는 legacy 그대로(`CORE_V2_CATALOG_WRITE_BLOCKED`). `omd design-md compile/adopt`는 **사용자 프로젝트** 채택(체크포인트 영수증) 전용이고 카탈로그 교체가 아니다. 3트랙 계획의 T2-2(writer 게이트)·T2-4(롤백 실동작)가 미구현 | `OMD_3TRACK_PLAN.md` T2 표, `.claude/skills/omd-migrate` |
| T3 생산 | **51/108** → 잔여 57칸을 체인 2기로 09:31 가동(칸당 실측 $0.90, 26칸 $23.4) | `03-runs/lane-a`, `scratchpad/t3prod/chain{A,B}.log` |
| T3 전사 | 23/108 → 검증기 개정(09:55, §2)으로 apple/omd rep-2·3·4 차단·재전사, baemin/omd/rep-2 재전사, coupang 12 진행 — 유효 20 | `90-comparison/transcripts`, `docs/reviews/t3-transcribe-verify-reseal-2026-09-02.md` |
| T3 채점 준비 | **도구 완비**(캡처·verify·패킷·검증·세션 러너·집계기, §2). 전사 검증기 결함 2건(결정 ID `D-P2-1`·`ui-ux-pro-max` 탐지 누락, 펜스 안 선언 삭제 불허) 발견·수정·재봉인 진행(grok 판정 대기) | §2, `test-v2/tools/` |
| grok build | 402(잔액 소진, 08:12~09:23) → 09:23 복구 확인 | 프로브 `total_cost_usd 0.0099` |
| 콘텐츠·캠페인 | stripe 3-arm 완료·compare.html·render-integrity PASS. `/design.md` 커밋됨, **푸시 승인 대기**. 캠페인 사람 승인 대기 | `docs/handover/x-vercel-designmd-campaign.md` |
| 이슈 큐 | #75–#84 10건 미처리(하네스 5·T2 2·T3 1·core-v2 1·tooling 1) | `gh issue list` |
| 리포지토리 | main 대비 **15커밋 미푸시**, 미커밋: 웨이브 46 산출·전사본·러너·이 문서 | `git status` |
| 학습 | pending preferences 35건 → `/omd-learn` 검토 대기 | 세션 훅 |

**한 줄 진단.** 일주일의 마이그레이션 산출(229본)이 아직 제품에 닿지 않는다. 속도는 오늘
러너로 3배 이상 오르지만, 속도보다 먼저 결정할 것은 「전부 옮긴 뒤 채택」인지 「채택 경로를
먼저 열고 옮기며 소비자 결함을 되먹임」인지다(§4 결정 1).

## 1. 웨이브가 2시간(실제 4시간 25분) 걸린 이유 — 실측과 조치

### 1.1 실측 (웨이브 45, 2026-09-02, scratchpad mtime)

| 층 | 시작→끝 | 브랜드당 | 실행 형태 |
|---|---|---|---|
| 워커 5 | 03:10→04:28 | **≈15분** | 5기 **직렬** |
| F3 5 | 04:28→05:38 | **≈14분** | 직렬 |
| E1 정정 1 | 05:40→05:50 | 10분 | 단발 |
| 검토 5 | 05:50→06:52 | **≈12분** | 직렬 |
| 개정 5 | 06:53→07:34 | **≈8분** | 직렬 (5/5 FAIL 1–2) |
| 합계 | 03:10→07:35 = **4h25m** | LLM ≈49분/브랜드 | 층 사이 손 조립 |

비교: 웨이브 38→39는 5기 병렬로 **56분** 간격(08-28 20:12→21:08). 웨이브 44는 2기 병렬로
2.5시간. 즉 느려진 원인은 호출 수가 아니라 **마라톤 리그가 층 단위 직렬**(슬롯 A를 T3와
나누려고)이었던 것 + 층 사이를 오케스트레이터가 손으로 잇는 대기.

**불필요한 호출은 없었다.** 브랜드당 최소 4호출(워커·F3·검토·개정)은 3층 분리 설계의 결과이고,
층 분리의 근거(같은 저자 맹점, 웨이브 4·33)는 여전히 유효하다. 다만 두 가지 낭비가 있었다:

1. **프롬프트 조립 드리프트.** README 레시피는 `worker-addendum.md` 전문 부착인데 실제
   웨이브 44–46 워커 프롬프트(4.6KB)에는 마지막 절만 들어갔다 — 웨이브 27~42에서 태어난
   예방 조항 8개가 빠진 채 돌았다. 웨이브 45가 5/5 FAIL(개정 층 100% 가동)한 원인 후보 1순위.
2. **오케스트레이터 사고 3건**(동일 브랜드 검토·개정 동시 투입, 전경 `&` 정지, 고아 체인) —
   손 조립의 구조적 비용.

### 1.2 오늘 적용한 조치

- **`scripts/wave-run.mjs`** — 브랜드 단위 파이프라인 러너. 워커→gate1→F3→gate2(게이트·1:1·
  use·적합성, 불일치면 자동 정정 1회)→검토→개정→gate3→재실측표. 동시성은 `agent-budget.sh`
  예산, 한 브랜드에 두 호출이 겹칠 수 없는 구조. 402/인증 실패 2회 연속이면 자동 중단·같은
  명령으로 재개. 호출별 초·USD·턴을 `docs/design-md-weight/metrics/wave-<N>.jsonl`에 누적.
- **프롬프트 정본화** — `prompts/{diet,fix-template,mechfix-template}.md` 신설, `worker-addendum.md`에
  「제출 전 자가 대조」절, `f3-conditions.md`에 웨이브 39·40 조건 편입(실제 프롬프트에만 있던
  것). 러너가 이 파일만으로 조립하므로 드리프트가 재발할 자리가 없다.
- 웨이브 46을 러너로 실전 가동(09:30, 3-wide). **기대치: 4h25m → 1.3~1.7h(3-wide) / ≈1h(5-wide).**
  실측은 metrics 파일로 웨이브 46 마감 판정문에 기재.

### 1.3 남은 레버 (실험 순서, 각각 측정 규칙 포함)

| # | 레버 | 기대 | 측정 규칙 | 위험 |
|---|---|---|---|---|
| L1 | 5-wide (T3 완주 후) | 웨이브 ≈1h | metrics 벽시계 | jetsam — budget이 막는다 |
| L2 | `--effort high` F3·검토 | 호출 30~40% 단축 | 웨이브 1회 A/B: 검토 FAIL 수·개정 건수가 직전 3웨이브 평균 이하일 때만 채택 | 검출 저하 |
| L3 | 웨이브 10브랜드 | 마감 오버헤드(판정문·커밋 ≈20분) 반감 | — | 판정문 길이 |
| L4 | 웨이브 45·46 FAIL형을 워커 예방 조항으로 이동 | 개정 층 가동률 100%→<50% | 다음 3웨이브 FAIL율 | 프롬프트 비대 |

### 1.4 T2 잔여 전망

웨이브 50 이후 잔여 **187브랜드 ≈ 37웨이브**. 러너 1.5h/웨이브면 ≈56시간 연속(2.5일),
비용은 웨이브 46 metrics로 확정(T3 arm 셀 $0.9 기준 브랜드당 $2~4 추정 → $400~750).
**이 전망은 §4 결정 1에 종속된다.**

## 2. T3 잔여 작업 (재편 모델: 생산 grok / 전사 opus5 / 채점 grok+sonnet5 / 검수 Fable)

| Phase | 상태 | 남은 것 | 소요(추정) |
|---|---|---|---|
| 2 생산 | 51/108 → 체인 A(29)·B(28) 가동 | figma 9 · karrot·musinsa·naver·wanted 48 | 2-wide ≈7~8h, ≈$51 |
| 3 전사 | 27/108 | coupang 12 → 생산 완료 브랜드 순으로 5×12. 청크 경계마다 Claude 80% 게이트 | opus 에이전트 1기당 12문서 ≈1.5h |
| **4 준비** | **도구 완비 (09:50)** — 입력 대기 | `capture-cells.mjs`(§4.1 첫 캡처 1440×1000·390×844, 51칸 완료·바이트 결정론 확인) · `verify-cells.mjs`(칸별 verify.json) · `build-packets.mjs`(세션 패킷: 익명 복사·키 봉인·fail-close, `--check`로 준비 상태) · `validate-responses.mjs` · `run-scoring-session.sh`(grok) — sonnet5는 오케스트레이터가 같은 packet.md를 Agent(model: sonnet)로. **grok 헤드리스 이미지 입력 스모크 PASS**(축 1 성립). 청크 1 결손: baemin/omd/rep-2 전사 1 + coupang 전사 12(에이전트 가동) | 남은 것은 전사뿐 |
| 4 채점 | — | 4세션(grok A/B·sonnet A/B) × 3청크. 레인 B 부재 → 실제 2세션 × 3청크 | 청크당 ≈$1~2 + Claude |
| 5 집계 | **`aggregate-lane-a.mjs` 사전 작성·커밋** (결과를 보기 전에 규칙 고정: 2인 합의·severity 낮은 쪽·§4.5 재정규화·bootstrap 10k·Krippendorff α 2관측자) | 빈 상태 스모크 PASS. 운영 고정 4건은 파일 헤더에 | 기계 |
| 6 검수·성장 루프 | — | Fable 소견서 → `omd:issue` 등록(미흡점·차용 후보) | 반나절 |
| 7 공개 게이트 | — | 사용자 선별 1건 → X (캠페인 문서 연장) | 사람 |

렌더 무결성: 신규 칸은 브랜드 완주마다 `render-integrity.mjs` 전수. 이미 잡힌 marquee형 이탈
2건(baemin/hallmark/rep-1, baemin/uiuxpromax/rep-2)은 봉인 칸이라 수정하지 않고 채점 입력으로.

## 3. 하네스 고도화 계획 (Fable 5.1 시대)

근거: 코드베이스 인벤토리(에이전트 harness-inventory, 09-02)와 외부 리서치(landscape-research, 09-02,
URL·날짜 병기본은 세션 기록). 아래 판단은 둘을 대조한 결과이고, 수치는 모두 외부 발표치다(자체 실측 아님).

### 3.1 자리 매김 — 무엇이 이미 다르고, 무엇이 빠졌나

**유지할 차별점.** (a) 출처가 붙은 440개 기업 DESIGN.md 카탈로그 + Core v2 계약(경쟁자는 카탈로그가
없거나(hallmark·ui-ux-pro-max·frontend-design) 유료 스크린 DB(Refero·Mobbin) 또는 규격만(Google
DESIGN.md)). (b) 봉인 벤치(T3)와 5층 이관 파이프라인 — Vercel도 eval 하네스는 비공개고 n=6이다.
(c) 3채널(Claude·Codex·OpenCode) 스킬 배포 + 원격 MCP.

**빠진 것 — 경쟁 10종에 공통이고 omd에 없는 것 (리서치 목록 ↔ 인벤토리 대조):**

| # | 기능 | 누가 갖고 있나 | omd 현재 | 판정 |
|---|---|---|---|---|
| G1 | **이름 붙은 안티패턴 목록 + 렌더 DOM 결정론 게이트** | Vercel 27종 · Hallmark 57 gates · frontend-design 3 | `omd-slop-audit` rule ID(코드 기반) + `render-integrity` 7종(9/1 신설) | 확장 필요 |
| G2 | **결정론 lint / diff / export** (대비율·orphan token·broken ref, DTCG·Tailwind 내보내기) | Google DESIGN.md CLI 11규칙 | Core v2 conformance(7앵커·claim 바이트)만 | 신설 |
| G3 | **스크린샷 인더루프 자평 ≤N회** | frontend-design · AutoDesign(VLM critic) | autopilot iteration 3 + critic, 배선 미정식(#83) · unattended 없음(#82) | 정식화 |
| G4 | **브랜드별 공개 URL + 동반 CSS + SKILL.md 형 frontmatter** | Vercel `/design.md` + `vercel-brand.css` | `/design.md` 1개 준비(푸시 대기) · brand.css 분리 #79 | 확장 |
| G5 | **평가 원장**(런마다 prompt·model·DESIGN.md SHA·스크린샷·판정) + fix-routing(판단→산문 / 메커니즘→CSS / 기계→체크) | Vercel | T2에 층 이동 원칙은 있음 · content-runs에 trace.md만 | 도입 |
| G6 | URL/스크린샷 → DESIGN.md 추출 | Hallmark `study`, 다수 도구(commodity) | `omd-reference-capture`·`omd-add-reference`(내부) · brownfield #30–38 | 온보딩용만 |
| G7 | 실화면 리서치 MCP | Refero·Mobbin(유료) | 없음 | 낮음 |
| G8 | 스타일·팔레트·폰트 검색 인덱스 | ui-ux-pro-max BM25 | 카탈로그가 대체 | 낮음 |
| G9 | 모션 스펙 export | Figma Motion→MCP | `omd:feel` 113규칙(감사) | 중 |
| G10 | **네이티브 이미지 자산 생성 티어** | ui-ux-pro-max premium · OpenDesign · Codex `$imagegen` | `omd-codex-image`(Codex 채널만 생성, Claude는 소싱) | §3.2 |

**인벤토리가 드러낸 내부 부채 (경쟁과 무관하게 먼저 갚을 것):**

- `render-integrity.mjs`가 **어느 스킬에도 배선돼 있지 않다** — T3 실파손을 잡은 결정론 게이트가 문서에만 산다. autopilot `EXTERNAL_VERIFY`가 자리.
- autopilot이 자기 비평가(slop-audit·designer-review·final-qa·a11y-auditor)를 **한 번도 호출하지 않는다**; 루프 상한이 셋(게이트당 2 / iteration 3 / 자평 1)으로 갈리고 공용 개정 원장이 없다.
- 무인(unattended) 모드 부재 — CI·배치·예약 실행 불가(#82).
- 멀티페이지 규칙은 autopilot 산문(`SKILL.md:418-451`)뿐 — 페이지 간 스타일 diff·링크 해소 검사 없음(#84).
- 스킬 트리 3중화(`skills/`·`.claude/skills/`·`.agents/skills/`)에 파일별 드리프트, `omd-issue`는 한 트리에만; `.claude/skills/skill-rules.json`은 v0.2.0으로 24개 중 5개만 등재.
- Core v2 쓰기 차단 게이트 5종(`.claude` 전용)에 만료 조건 없음; `AGENTS.md`가 존재하지 않는 `omd harness` 헬퍼를 문서화.
- 원격 MCP(`web/app/api/mcp`)는 **410 retired**인데 `packages/mcp`는 도구 3종(list_references·get_design_md·search_by_vibe)이 살아 있다 — 호스팅 `search_by_vibe` 없음.
- 이미지 생성: Claude Code 채널은 소싱만, grok `image_gen` 경로(`generate-assets.mjs`)는 스킬로 승격된 적 없음 → F1.

### 3.2 압도적 경험을 위한 추가 기능 — 미디어 생성과 시연

**F1 `omd:media` — 채널 감지형 브랜드 자산 생성.** 실행 환경에서 쓸 수 있는 생성 채널을 **감지**하고
DESIGN.md에서 뽑은 프롬프트 팩(히어로·일러스트·아이콘 세트·OG 이미지)을 그 채널로 만든다. 채널이
없으면 프롬프트 팩 + 수동 큐(기존 `omd-codex-image`의 3경로를 일반화).

| 채널 | 감지 | 비용(외부 발표) | 특기 |
|---|---|---|---|
| Codex `$imagegen` (gpt-image-2) | `codex` CLI + ChatGPT 로그인 | ≈$0.03–0.08/장 | API 키 불필요 — 가장 싼 헤드리스 경로 |
| Gemini CLI `nanobanana` 확장 | `gemini` CLI | $0.067/장 (NB2) | 참조 이미지 편집 |
| grok build CLI `image_gen` / `image_to_video` 도구 | `grok` CLI 로그인 | 생산 셀 실측 $0.5–1.8/셀(이미지 포함) | T3 arm이 실제로 쓴 채널(`test-v2/tools/generate-assets.mjs`) — 이미지+6초 720p 영상. 외부 리서치의 「CLI 미노출」은 5월 정보로 낡았다 |
| xAI API `grok-imagine-image-2.0` | `XAI_API_KEY` | $0.04/장 | 헤드리스 API 경로(CLI 없이) |
| Recraft V4.1 | `RECRAFT_API_KEY` | SVG $0.088 · style 생성 $0.044 | **유일한 네이티브 SVG** + `style_id`(브랜드 참조 1–10장) → 아이콘·일러스트 세트 일관성 |
| 없음 | — | $0 | `omd-asset-fetch` 무료 소싱 + 프롬프트 팩 |

산출: `assets/generated/<set>/` + 출처·라이선스·비용 원장(기존 `## Used Assets` 표 확장). 레퍼런스별
Recraft `style_id`는 카탈로그 부가 데이터로 봉인 가능(브랜드 캡처에서 생성, 재사용).

**F2 `omd:showcase` — 시연 영상 한 명령.** Playwright 스텝 스크롤(뷰포트 1920×1080@2x, `scrollTop=ease(frame)`
프레임 고정 캡처) → PNG 시퀀스 → ffmpeg H.264/GIF. 결정론·$0·~150줄. 옵션: Remotion 타이틀 카드·캡션을
DESIGN.md 토큰으로(온브랜드 인트로/아웃트로). 사용자가 언급한 **scroll-world는 시연 도구가 아니라**
생성형 3D 디오라마 랜딩 스킬(GPT-Image-2 + Seedance, 사이트당 ≈$27)이다 — 차용할 것은
「스크롤 = 플레이헤드」 스크럽 엔진 아이디어뿐이며, 히어로 옵션으로 둔다. 영상 생성 모델(Veo 3.1 Lite
$0.05/s · Grok Imagine video $0.05/s)은 히어로 B-roll 옵션. **Sora API는 9/24 종료 — 제외.**
캠페인 영상(stripe 3-arm, 미국 브랜드 편)이 첫 소비자다.

**F3 슬롭 게이트 v2.** 공통 안티패턴 20종(Vercel 「generated-design reflexes」 27종과 Hallmark 게이트에서
증거 있는 것만)을 `render-integrity`의 DOM 결정론 검사로, 레퍼런스별 금지 패턴은 DESIGN.md
Core v2 anti-pattern 절로. slop rule 이름을 생성 프롬프트에 예방 주입(#80).

**F4 `omd lint | diff | export`.** Core v2 결정론 린터(대비 WCAG AA, orphan token, broken ref, 절 순서,
unknown key), 버전 diff, DTCG·Tailwind·CSS 변수 export. #37(src/core 통합)과 같이.

**F5 브랜드별 공개 URL.** `oh-my-design.kr/reference/<id>/design.md` + 동반 `brand.css`(#79) +
SKILL.md 형 frontmatter → `npx skills add` 호환. `/design.md` 관례 트윗의 연장선.

**F6 루프 정식화.** 평가 원장(`loop-trace.json`: prompt·model·DESIGN.md SHA·스크린샷·판정·autoSelected)
+ unattended 모드(#82) + render-integrity→slop-audit→designer-review 배선(#83) + fix-routing 규칙.
하네스 고도화 테스트 세트 v1(T-B 멀티페이지·T-D 모션)이 측정 도구.

**F7 멀티페이지 저니.** 공유 brand.css 참조·페이지 간 내비 실동작·토큰 드리프트 검사(#84). 영상 가치 최고.

### 3.3 우선순위·크기·순서

| 순서 | 기능 | 크기 | 선행 | 수용 기준(측정) | 실행자 |
|---|---|---|---|---|---|
| 0 | 위생: skill-rules.json 24종 등재 · 스킬 트리 드리프트 검사(check-mirror-drift 확장) · AGENTS.md 헬퍼 문구 정정 · 쓰기 게이트 만료 조건 · render-integrity를 autopilot EXTERNAL_VERIFY에 배선 | S (반일) | 없음 | 드리프트 검사 0건 · autopilot 런에서 render-integrity 결과 파일 생성 | opus |
| 1 | F2 showcase | S (1일) | 없음 | 명령 1회 → 60초 내 mp4/gif, stripe 3-arm 영상 산출 | grok 워커 + Fable 검수 |
| 2 | F6 루프 정식화 (#82·#83) | M (2일) | 없음 | T-B·T-D loop-trace에서 회차별 결함 수 감소 | opus 구현 · Fable 판정 |
| 3 | F3 슬롭 게이트 v2 (+#78·#80) | M (2일) | F6 | T3 산출 108칸에 소급 실행 → 결함 검출률·오탐 보고 | grok + opus |
| 4 | F1 media (채널 감지 + 프롬프트 팩 → Codex/Gemini/xAI → Recraft style_id) | M–L (3–4일) | 없음 | 레퍼런스 3개 자산 세트, 세트당 <$1, 원장 완비 | opus + 사용자 키 |
| 5 | F4 lint/diff/export (+#37) | M (2–3일) | 없음 | 카탈로그 440 린트 리포트, DTCG export 라운드트립 | opus |
| 6 | F5 브랜드별 공개 URL + brand.css (#79) | S–M | 배포 승인 | `curl /reference/toss/design.md` 200 · `npx skills add` | web |
| 7 | F7 멀티페이지 (#84) | M | F6 | T-B 3면 render-integrity PASS + 드리프트 0 | grok + opus |

착수 시점: **웨이브 50 + T3 생산 완료 뒤**(grok 슬롯이 비는 시점). F2는 캠페인 촬영과 직결되므로
사용자 승인 즉시 먼저 뽑을 수 있다(하루).

### 3.4 Fable 5.1 배치

- 현재 에이전트 모델 핀: opus = master·orchestrator·critic·ux-researcher·designer-review·final-qa·slop-auditor·
  ux-engineer·ux-writer·humanizer·locale-adapter·kr-writer, sonnet = ui-junior·microcopy·asset-curator·codex-image·
  persona-tester, haiku = a11y-auditor. Claude Code Agent 모델 열거에 `fable`이 있으므로
  **판단 밀도가 높은 3자리(omd-master · omd-critic · omd-final-qa)를 `fable`로 올리고 opus를
  폴백**으로 둔다. 효과는 하네스 고도화 테스트 세트(T-B·T-D, loop-trace)로 잰다 — 체감이 아니라 수치로.
- 벌크는 그대로: 이관·T3 생산 = grok build(비용 $0.35–1.0/호출), 전사 = opus, 채점 = grok+sonnet,
  최종 검수·판정문 = Fable.

### 3.5 하지 않을 것 (리서치 「Hype / skip」 반영)

Sora API(9/24 종료) · Midjourney 비공식 API(ToS 위반) · scroll-world를 시연 도구로 오해 · 범용
스타일/팔레트 카탈로그·URL→DESIGN.md 생성기 경쟁(commodity — 출처 카탈로그가 차별점) ·
Vercel의 「57% 감소」를 벤치 수치로 인용(n=6, 자체 경고).

## 3.6 실행 계약

우로보로스 인터뷰(5라운드, 모호도 0.19) 시드 `docs/OMD_SPRINT_SEED_2026-09-02.md` — 수용 기준 6·컷라인·종료 조건·D0–D7 일정. 사용자 확정: 이관 완주 우선(마라톤), 슬롭 게이트 둘 다, Fable 배치, 부채 선행, 문답형 setup.

## 4. 결정 대기 (사용자)

1. ~~채택 경로 우선 vs 전량 이관 우선~~ → **사용자 결정(09-02 11:30): 이관 완주 우선, 마라톤 무인.** (원 권고: 웨이브 50에서 이관을 **일시 정지**하고 T2-2
   (writer 게이트 해제 조건)·T2-3(대표 canary 채택)·T2-4(롤백 실동작)를 먼저 연다. 근거: 채택
   시 드러날 소비자 결함(빌더 프리뷰·MCP·llms-full)은 규칙집 개정을 부르고, 그 개정은 이미
   옮긴 253본의 재작업으로 돌아온다. 187본을 더 옮긴 뒤 알면 재작업이 440본이 된다.
2. ~~푸시 승인~~ → **승인(09-02 11:50)**: 브랜치 푸시 완료, main 머지·배포는 §5.
   최종 목표 추가(사용자): **2.0.1 릴리즈 + X·Threads 임팩트 콘텐츠**(시드 AC 7·8).
3. ~~L2 effort 실험 허가~~ → 마라톤에서 F3·검토만 high 적용 중, 발견율 규칙으로 자동 판정.
4. ~~§3의 우선순위 확정~~ → 2주 세트 확인(09-02 11:50). grok: SuperGrok Heavy 구독(잔액 충분). T3 생산 51칸 마라톤과 병행 재가동.

## 5. 실행 순서 (승인 없이 진행 중인 것 / 대기)

- **진행 중(자율)**: 웨이브 46 러너 → 47~50 순차 · T3 체인 A/B(57칸) · 전사(coupang 이후 생산
  완료 순) · Phase 4 준비 도구(캡처·verify·패킷·러너·이미지 스모크)를 생산과 병행 구축.
- **웨이브 50 + T3 생산·전사 완료 시점**: 중간 정리 보고(수치는 metrics·verify에서), 프로덕션
  싱크 준비(푸시 목록·배포 확인 절차·캠페인 상태).
- **대기**: §4 결정 1~4.
