# 1.9.814 OmD Reliability@3 task lock

Three fresh repair tasks are frozen for the only arm that passed the current
frontier-skill Stage A qualification. Each raw starter scores 75/85. Task
contract, state journey, design grounding, and evidence honesty are green;
responsive and accessibility are the only red critical gates.

This expansion follows the published metric rather than the recent diagnostic
shorthand: each of the three tasks must pass three independent trials. The
denominator is therefore nine cells, not three unrelated one-shot cells.

Browser-harness attached to the isolated local `omd1813` Chrome connection and
loaded all three exact starter routes. Every route exposed one h1, one main,
three review views, and the expected 4/5/6 records. The observed raw layout
overflowed by 113/191/197px at the attached browser width; the deterministic
four-viewport evaluator independently sealed the exact 75/85 baseline and
source-contract evidence.

No execution provider or benchmark model saw these task workspaces before the
lock. Provider calls: 0. Cursor calls: 0.
