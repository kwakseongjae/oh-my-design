# Reflow integrity v2 holdout — findings

Status: **ACCEPTED; provider calls 0**.

## Locked change

`omd-reflow-integrity-v2-experimental` uses the exact same detached current
OmD source, commit, install adapter, and installed skill as
`omd-portable-jade`. Its only difference is one activation paragraph.

The revision treats one-line integrity as an acceptance outcome rather than a
first CSS tactic. It requires a full-width reading row or sufficient `minmax`
space before reducing existing spacing, rejects `nowrap` when it creates
overflow or clipping, protects generated mobile labels, and preserves selected
target emphasis and decision-context containment.

## New unseen task

`release-artifact-promotion-v0.1` was contract-locked before generation. It is
an approval interaction family in a new platform-release domain with four
atomic artifact filenames, two manifest disclosures, filter and dialog state,
three narrow/reflow geometry viewports, and four-view decision hierarchy.

The untouched starter scored 79/85. Function, state journey, accessibility,
DESIGN grounding, evidence honesty, horizontal overflow, clipping, and control
overlap are green. The intended responsive gate is red: atomic filenames,
short owner labels, manifest controls, and the selected decision boundary
fragment at mobile, 320px, and 200% reflow. The 320px screenshot confirms the
narrow label-column and two-column decision boundary as visible causes.

## Decision

Use this task for a fresh paired 3-trial current-control versus activation-only
v2 comparison. Do not alter the evaluator, task contract, starter, or canonical
`skills/omd-apply/SKILL.md` after provider generation begins.
