# Field-sample helper-issued source fallback diagnostic — 1.9.696

The exact clean `5faf4830` OmD skill was prepared and executed once with Luna/high. The run was valid, made one product revision, requested no user intervention, performed no browser recovery retry, opened the helper-issued source fallback, and completed one successful static closure. Proof compliance therefore transferred as intended.

The UI did not resolve. It scored 81/85 and failed the responsive gate at 320px and the 200% surrogate. The compound target and concise evidence remained ordinary cells inside the decision grid. Neither received a distinct named, keyboard-reachable relationship carrier, so the concise evidence wrapped and page-level horizontal overflow remained.

The failure exposes a narrower contract gap than 1.9.693. The helper now controls the fallback lifecycle, but accepted a model-authored static manifest that did not encode the skill's target/evidence carrier requirements. In other words, the verifier proved the manifest it was given but did not prove that the manifest represented the normative fallback rule.

The 1.9.697 repair moves that rule into the shipped helper. When target or concise evidence rows exist, `source-fallback-open` now requires each row to declare `comparison-scroll`, bind to its own distinct pre-edit carrier, and provide a complete named scroll contract. The helper stamps the relationship contract and `static-close` directly checks exact carrier count, accessible name, `tabindex=0`, carrier overflow, row nowrap, visible focus, and exclusion of target/evidence peers, state, and action. A missing pre-edit snapshot now fails with a bounded contract error instead of a `TypeError`.

This diagnostic remains excluded from comparative rankings and does not change the 2.0 readiness snapshot.
