# Field-sample helper-issued source fallback diagnostic — 1.9.696

This single exact-current OmD cell tests whether the shipped helper contract added in commit `5faf4830` turns a pre-edit browser infrastructure failure into an auditable source-backed closure: `source-fallback-open` before the product revision, exactly one bounded product revision, then a successful `static-close`.

It reuses the exposed public task `field-sample-custody-review-v0.1`. It is diagnostic only and cannot enter Skill Lift ranking, establish a comparative claim, or advance a 2.0 frontier gate by itself.

Acceptance requires all of the following:

- no user handoff and no browser recovery retry;
- exactly one product revision;
- helper-issued source fallback opening before that revision;
- successful post-edit static closure with proof compliance;
- actual distinct named target/evidence relationship carriers when those roles exist;
- objective resolution at every required viewport, including the 320px and 200% surrogate checks.

Any missing helper lifecycle evidence, failed closure, invented relationship role, wrapping concise evidence, or document overflow is retained as a valid diagnostic failure rather than repaired after execution.
