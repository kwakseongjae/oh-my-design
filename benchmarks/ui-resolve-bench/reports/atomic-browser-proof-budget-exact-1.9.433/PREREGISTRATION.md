# Exact pin — atomic browser proof budget 1.9.433

This provider-free step pins the accepted 1.9.432 candidate before any new transfer task sees it. The next experiment must install only this exact detached commit and skill tree. Promotion is forbidden.
