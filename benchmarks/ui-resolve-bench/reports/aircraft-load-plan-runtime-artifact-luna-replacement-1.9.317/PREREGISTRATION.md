# Aircraft runtime-artifact transfer clean-baseline replacement — 1.9.317

This second infrastructure replacement preserves the 1.9.315 scientific contract exactly and changes only prepared-workspace integrity. Exact control `3a414a0a…`, candidate `8f8cec6e…`, task, prompt, starter, DESIGN.md, Luna/high, 900-second timeout, C/N–N/C–C/N order, 120-second pacing, no-retry policy, shared hook-safe host policy `7d21ea23…`, and all promotion gates remain fixed. Preparation code is pinned to `06086c37…`.

Every cell must start from a committed, detached, clean local Git baseline in addition to the previous equality requirements. The rejected `/private/tmp/u19315` preparation contributed zero provider calls and no spend. The frozen 1.9.311 control's 2,932,219 tokens remain charged to Tokens-to-Target attempt 4; its score remains excluded.

Calls remain one cell at a time with `--max-new-cells 1`. Provider calls at preregistration: 0.
