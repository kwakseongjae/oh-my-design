# Grid r1 control — 1.9.451

The exact 1.9.409 control produced an objectively resolved UI: 85/85, all four responsive conditions green, no serious/critical contrast issue, complete state journey, and preserved evidence. Runtime identity matched Luna/high through the isolated execution home. The cell completed in 374.437 seconds with 724,940 observed provider tokens.

The proof contract failed. The trace is analyzable, but the model performed four static closures, one browser recovery probe, and one verification after browser readiness. This is a valid system proof failure rather than infrastructure failure, and no retry is allowed.

The paired candidate remains eligible. It must preserve the 85/85 objective result while satisfying the exact one-static, one-browser, actual-zoom, helper-terminal proof contract.
