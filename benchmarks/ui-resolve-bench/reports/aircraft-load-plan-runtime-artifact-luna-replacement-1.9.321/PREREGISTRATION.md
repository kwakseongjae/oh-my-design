# Aircraft runtime-artifact proof-order replacement — 1.9.321

This third infrastructure replacement preserves the scientific contract from 1.9.315 and 1.9.317. Exact control `3a414a0a…`, candidate `8f8cec6e…`, aircraft task, prompt, starter, DESIGN.md, Luna/high, 900-second timeout, C/N–N/C–C/N order, 120-second pacing, no retry, attempt 4, and every promotion gate remain fixed.

The only infrastructure delta is the shared proof-order host policy pinned from committed source `e5f1e307…`. It is byte-identical across both arms and executes inventory → edit → static → browser → measured artifact → Stop/delivery. Preparation keeps the committed, detached, clean workspace baseline introduced in 1.9.316.

The known retained invalid spend remains 2,932,219 tokens. The 1.9.317 timeout is retained as provider usage unavailable and is not assigned an inferred token count. Both invalid scores are excluded. Fresh roots are `/private/tmp/u19321-vendors` and `/private/tmp/u19321`; neither previous root may be resumed. Provider calls at preregistration: 0.
