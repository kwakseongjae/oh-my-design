# Aircraft runtime-artifact proof-order replacement — findings

Status: **locked; fresh preparation not started**

The experiment retains the same attempt, task, two skill sources, Luna/high runtime contract, balanced order, and gates. The sole shared infrastructure change is exact host policy `e5f1e307…`, applied equally to both arms. Provider calls: **0**.

Next: prepare detached clean vendors and six committed/detached/clean Git workspaces, then attest equality before allowing one control cell with `--max-new-cells 1`.
