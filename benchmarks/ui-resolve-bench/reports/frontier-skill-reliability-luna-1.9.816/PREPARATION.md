# Provider-zero preparation

- Matrix: 9/9 cells prepared; execution artifacts absent.
- Normalization: `multi-task-repeated-reliability`; every required predicate passed.
- Tasks: exact first-occurrence order, lock hashes, repeated trial sets `[1,2,3]`, and within-task bytes passed.
- Skill: exact current OmD source commit and installed skill tree passed; publishable 9/9.
- Runtime: immutable Codex catalog snapshot, exact Luna profile, exact `high` effort, Cursor forbidden.
- Browser: controller-owned local `omd1816` connection; model may attach but may not launch another browser.
- Provider/model/Cursor calls at preparation: 0/0/0.

The next allowed action is one invocation with `--max-new-cells 1`. Completed cells are never replayed.
