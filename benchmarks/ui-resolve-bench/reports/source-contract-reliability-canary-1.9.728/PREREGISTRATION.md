# Workspace-bound candidate-preflight Reliability@3 — 1.9.728

This experiment preserves the exact conservation and wind-tunnel task bytes that remained unexposed when 1.9.725 froze. It adds the fresh provider-zero archaeology task and excludes the already exposed benthic task. No prior model output is replayed.

The controller resolves relative artifact product paths inside the prepared cell workspace and rejects workspace escapes. Every other lifecycle rule remains unchanged: complete candidate bytes are previewed with the sealed static evaluator, a passed receipt binds candidate/source/inventory hashes, those exact bytes become the single product edit, and static closure is consumed once.

The fixed order is conservation frame loan, wind-tunnel model handoff, then archaeology tray release. Each cell uses Codex with `gpt-5.6-luna`, high effort, a 720-second timeout, no retry, and no replacement. Execution advances one new cell per checkpoint.

A cell passes only at 85/85 with resolved UI, one product revision, one successful and zero failed static closures, zero user handoffs, proof compliance, unchanged sealed inventory, a passed candidate receipt, and exact candidate-to-final byte equality. Reliability@3 requires 3/3.

Any lifecycle hard-stop freezes the remaining cells. The experiment is diagnostic only and cannot by itself enter ranking, establish provider superiority, or promote the 2.0 release gate. Provider execution is forbidden until this plan and a provider-zero preparation/admission checkpoint are committed.
