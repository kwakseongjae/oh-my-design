# Field-sample relationship verifier diagnostic — 1.9.698

This single exact-current OmD cell tests the clean `3ec07131` helper contract after the 1.9.696 lifecycle-pass/UI-fail diagnostic. It asks whether a pre-edit browser infrastructure failure now results in either a structurally valid target/evidence relationship-carrier repair or an honest static-closure failure; the previous weak 81/85 implementation must no longer receive proof credit.

It reuses the exposed public task `field-sample-custody-review-v0.1`. It is diagnostic only and cannot enter Skill Lift ranking, establish a comparative claim, or advance a 2.0 frontier gate by itself.

Acceptance requires no user handoff, no browser recovery retry, exactly one product revision, helper-stamped relationship contracts for every existing target/evidence row, successful static closure, proof compliance, and objective resolution at all required viewports. A verifier rejection is considered correct fail-close behavior but not UI transfer success.
