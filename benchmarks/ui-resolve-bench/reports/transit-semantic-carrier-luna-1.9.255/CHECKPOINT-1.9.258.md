# Transit semantic-carrier transfer — checkpoint 1.9.258

Pair 1 completed valid:

| arm | score | UI-Resolved | proof | host policy | wall | tokens |
| --- | ---: | --- | --- | --- | ---: | ---: |
| close-latch | 75/85 | false | pass | pass | 192,082ms | 638,236 |
| semantic-carrier | 73/85 | false | pass | fail | 201,457ms | 509,555 |

Candidate delta: **-2**. The candidate preserved all protected hooks and evidence and removed page-level horizontal overflow, but atomic service/stop and decision metadata still exceeded the one-line budget at scored narrow viewports. Exact contrast remained red, and keyboard traversal failed. It opened a browser session but never navigated, so installed host policy correctly failed `delivery-incomplete` and `browser-attempt-missing`.

The preregistered promotion is already impossible because paired objective losses allowed is zero and candidate proof/host policy must pass 3/3. The remaining two pairs remain diagnostic for transfer reliability and failure recurrence; they cannot restore promotion eligibility.

Next locked cell: `luna-transit-r2-carrier`. Same root retry is forbidden.
