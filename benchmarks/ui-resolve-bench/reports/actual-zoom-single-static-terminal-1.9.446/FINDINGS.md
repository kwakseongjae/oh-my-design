# Actual zoom and single-static terminal repair — 1.9.446

This provider-free repair closes the two remaining defects observed in the frozen 1.9.445 pharmacy pair.

The reflow artifact now locks three immutable measurement conditions: 390px at zoom 1, 320px at zoom 1, and a 640px viewport at actual document zoom 2. A measured finalize must report the observed computed document zoom for every condition; a 640px-only surrogate can no longer close the 200% proof.

The skill now defines the first acceptance shell command after the final product edit as the entire static closure. Syntax, cardinality, supplied facts, forbidden text hacks, changed tokens, and contrast must be consolidated in that one command; a second command is explicitly a duplicate even when it checks a different subset.

The deterministic helper now emits `OMD_DELIVERY_READY` or `OMD_DELIVERY_UNRESOLVED` itself after finalize, so the one browser process cannot omit the terminal marker or require a post-browser artifact read.

Focused skill, artifact, proof-policy, and runtime-policy validation passed 62/62; the exact candidate pin test also passed. TypeScript and diff checks passed. The broad benchmark file passed 91 tests and retained two known external-vendor Git-root fixture failures unrelated to this patch. Provider calls: 0. Quality promotion: false pending exact pin and fresh transfer.
