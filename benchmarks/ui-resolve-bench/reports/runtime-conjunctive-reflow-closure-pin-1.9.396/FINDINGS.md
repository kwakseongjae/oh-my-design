# Runtime conjunctive reflow closure — exact pin

- The repaired runtime is pinned to commit `effd1d93d415f625cc9fb6222b9a2e89aca6f110`.
- Resolved delivery now requires every registered carrier and row to be resolved.
- Unresolved accounting requires one real browser infrastructure attempt and remains explicitly unresolved with `quality_pass: false`.
- The host validator independently enforces the same contract.
- No provider call was made. This pin is not a quality promotion; it is the only eligible candidate source for the next fresh holdout.
