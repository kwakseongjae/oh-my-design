# 1.9.744 — Luna council gate full product comparison

The Grok 1.9.743 root is frozen after a provider-limit response and will not be
resumed. This replacement experiment uses a new root and the previously
authorized Codex/Luna/high fallback.

The task and two arms are unchanged: current `omd:apply` without a council
packet versus the same skill with a provider-sealed, zero-dispatch maintenance
ledger. The task has one prior Grok prompt exposure with no model output and no
product change; it has zero prior Luna exposure.

Runtime/model is Codex / `gpt-5.6-luna`, effort high, timeout 720 seconds,
concurrency one, fixed order off then on, 30-second inter-cell pacing, and zero
retry or replacement. Objective quality, exact-route proof, evidence honesty,
wall time, and reported token usage remain separate axes. One task is Internal
Preview evidence only and cannot promote the 2.0 gate by itself.
