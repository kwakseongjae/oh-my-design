# Reflow v13 final-selector closure — 1.9.174

## Decision

Create a bounded v13 candidate. The delta addresses the repeated v12 transfer residual without adding task strings, selectors, content thresholds, CSS recipes, tokens, evaluator weights, or new inventory categories.

## Evidence

On the unseen audio-stem transfer, v12 retained every dynamic-state and relational-control manifest row in all three trials and beat the previous arm 3/3. Two candidate trials nevertheless left the same manifest state row naturally wrapping at 200% while claiming a source-derived pass. Both had already repaired topology and assigned the declared 12/17 type role.

The remaining defect is therefore the gap between one-line authority in the manifest and the final controlling selector, not another missed scope or type-role category.

## Delta

After width recovery, structural reflow, type-role assignment, and semantic-boundary preservation, every manifest row with one-line authority must now terminate in exactly one of three states:

1. measured one-line output with zero overflow and clipping at 390px, 320px, and 200%;
2. an explicit selector-level no-wrap decision backed by existing measured longest-string and available-width evidence at the same views;
3. honest `unresolved`.

Natural-wrap optimism, parent `width:100%`, a smaller declared font, the initial dynamic string alone, and prose budget claims are not selector-level closure. Dynamic rows must cover the longest render/template/state-map value. Copy, identifiers, and facts may not be shortened to satisfy the contract.

New zero-defect counters are `implicit_one_line_selector`, `one_line_authority_without_realization`, and `source_claim_without_selector_decision`.

## Verification

- focused canonical skill contract: `7/7` pass
- TypeScript lint: pass
- full unit suite: `348` pass, `1` skip, `2` pre-existing vendor-fixture infrastructure failures
- the two failures are non-Git `/tmp/omd-ui-skills-bench/vendors/{taste-skill,ui-ux-pro-max}` fixtures and do not execute the v13 skill delta

## Next gate

Commit and pin exact v13. Use the seen audio task only for a provider-free diagnostic, then contract-lock a new unseen non-approval task with a dynamic one-line state and a different layout topology. Promotion requires UI-Resolved 3/3, Reliability@3 100%, and zero paired loss against the exact previous canonical arm.
