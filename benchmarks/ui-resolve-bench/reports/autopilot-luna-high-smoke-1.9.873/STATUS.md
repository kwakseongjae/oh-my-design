# Autopilot Luna/high smoke 1.9.873

Diagnostic only. The exact three-cell block completed with three valid terminal
records, seven model calls, no user-question batch, and no retry, replacement,
fallback, or Cursor call. All three project-owned DESIGN.md proofs passed.

The sealed result is **1/3 UI-Resolved**. Provider-free structure-neutral
reevaluation keeps the corrected result at **1/3**: landing passes, while
cold-chain and locale each retain objective product defects. The fresh 3/3
gate, 12-task competitor comparison, and public one-shot claim remain blocked.

- Neighborhood library improved `50→70→100`. The one prompt now produces a
  project-owned design system and a working reservation-start journey across
  desktop, mobile, 320px, and 200%-reflow with no serious Axe violation.
- Cold-chain stayed sealed at `20→20`, then scores `60` under the corrected
  evaluator. Its default `Urgent only` native select contains an `All
  exceptions` baseline and the selected option is visibly/programmatically
  active. The polite status linked by `aria-describedby` is also valid error
  feedback. Real failures remain: 40px task controls and a closed detail whose
  `aria-hidden` subtree still contains focusable controls.
- Clinic locales stayed sealed at `50→50`, then scores `60` diagnostically.
  The product explicitly says it does not provide medical advice and correctly
  localizes the core checklist into KO/EN/JA/ZH-CN/ZH-TW. The real blocker is
  progress semantics: visible copy moves from `1 of 4` to `4 of 4`, while the
  progressbar continues to expose `aria-valuenow=0` and `aria-valuemax=0`.

Repair evidence was also a controller defect. Cold-chain round one expanded to
510,601 bytes because the same Axe nodes and four-viewport evidence were copied
repeatedly. The new evidence contract deduplicates actionable violations,
excludes the raw viewport tree from supporting evidence, and emits direct
filter, owner-error, honesty, and progress observations. The corrected
cold-chain failure payload is 2,007 bytes, so the next repair model sees the
actual targets instead of a half-megabyte trace.

The sealed records and products remain immutable. 1.9.873 is complete and
non-resumable; provider-free scores are diagnostic artifacts and never replace
the authority records.
