# Reflow v4 portable candidate — findings

Status: **PREPARED; no matrix provider call yet**.

The decision-context reflow candidate is pinned as
`omd-portable-reflow-v4-candidate` from detached clean commit
`c8416c435d6cfd17868731eab580decc865d66c8`. Its Cursor-adapted installed
tree is publishable and hashes to
`8ebc7e764772be6259e73f0362089ecd1865dc207a720c6a2158716659c361fa`.

The activation is identical to the exact previous canonical variant. The
candidate differs by its installed skill tree only. Rejected v2 and v3 sources
remain separately pinned and are not silently replaced.

Next, prepare the exact previous canonical versus v4 matrix against the locked
webhook destination holdout. The diagnostic preparation made no provider call
and is not part of the comparison denominator.
