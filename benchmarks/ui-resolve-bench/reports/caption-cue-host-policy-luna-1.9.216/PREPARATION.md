# Caption cue host-policy preparation — 1.9.217

Fresh root `/private/tmp/u19216` is prepared with six Git-backed cells and zero provider calls.

All cells have identical task prompt, runtime prompt, untouched product tree, detached skill, activation delta, source commit, runtime, model, effort, and timeout hashes. Controller cells have no installed hooks. Policy cells have the production-rendered Codex proof policy, valid installation attestations, and the locked native hook-trust boundary. No other paired difference was observed.

The next permitted action is one invocation that creates only `luna-caption-r1-controller`. Every continuation must preserve `max-new-cells=1`. A preparation, identity, policy-state, attribution, timeout, or infrastructure failure freezes `/private/tmp/u19216`; it cannot be repaired or retried in place.
