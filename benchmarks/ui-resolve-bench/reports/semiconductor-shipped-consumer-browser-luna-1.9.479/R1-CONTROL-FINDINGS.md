# Semiconductor r1 control — 1.9.481

The exact 1.9.474 control reached 83/85. It removed every narrow and actual-200% overflow, kept protected text on one line, cleared contrast, and preserved behavior and evidence. The only objective failure is the desktop decision action falling below the preregistered 8px spatial separation, so the UI is not resolved. Runtime identity matched Luna/high through the isolated execution home. The cell completed in 384.069 seconds with 838,924 observed provider tokens.

The command-shape trace is clean at edit 1, static 1, browser 1, recovery 0, duplicate 0, and post-ready 0. The experiment-level proof still fails: the model-authored browser command recorded `infrastructure-error`, `attached_existing: false`, and an open schema closure instead of measurements from the exact named consumer browser. The generic command-count gate reporting pass is not sufficient for the stricter preregistered attachment and schema gates.

The paired candidate must use its shipped runner rather than authoring browser measurement code. The current execution sandbox exposes the named browser socket while intentionally withholding raw CDP endpoint variables; the candidate result will therefore also test whether the shipped runner honors the real controller boundary. No retry or environment substitution is allowed inside this experiment.
