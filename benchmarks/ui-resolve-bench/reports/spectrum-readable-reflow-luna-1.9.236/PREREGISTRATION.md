# Spectrum readable-reflow replacement — 1.9.236

This is the clean replacement for frozen experiment 1.9.233. Task, exact skill commits, activation, Codex/Luna/high runtime, timeout, shared installed proof policy, balanced order, 120-second pacing, and promotion gates are unchanged. It uses fresh vendor worktrees, fresh six-cell root `/private/tmp/u19236`, and canonical checkpoint-bound execution only.

Every invocation must use `--max-new-cells 1`. Unknown CLI options now fail before preflight. No result or workspace from `/private/tmp/u19233` may enter this comparison. Provider calls at preregistration: 0.
