# Rail-signal compound-atomic range oracle — 1.9.417

Attempt 19 reuses the rail-signal possession handback holdout only after freezing an infrastructure-invalid control exposure from attempt 18. The control has seen the task once; the candidate has not seen it. It compares exact historical control `3a414a0a…` with exact 1.9.410 compound-atomic range-oracle candidate `1480d7cc…` under Luna/high in a fresh balanced 2×3 matrix: C/N, N/C, C/N. The installed host is exact repaired source `060f1d31…`, pinned at 1.9.416. Timeout is 900 seconds, concurrency is one, fixed inter-cell pacing is 120 seconds, and primary retries are forbidden.

The candidate must resolve all three trials, preserve all supplied signal/certificate relationships and all three relationship carriers, keep `SIG-AX14-204 + CERT-IR-77421` semantically exact while allowing line breaks only between its atomic children, introduce no paired objective loss, pass the installed host/proof gates three times, and stay within 1.1× control mean wall time and provider tokens. Proof must measure non-whitespace character-by-character `Range` line tops; element rectangles cannot satisfy the oracle. The live hook must intercept Codex `exec_command`/`cmd` and deny a second static closure before execution. All admission, checkpoint-resume, timeout-accounting, isolated runtime, named browser, bootstrap boundary/order, shared socket, completion-loop, observed-attempt, compound-atomic, character-range, and Codex-shell-interception pins are fixed before preparation. Earlier roots and outputs remain frozen. Provider calls at preregistration: 0; prior observed Tokens-to-Target minimum: 31,625,601 plus six usage-unavailable cells.
