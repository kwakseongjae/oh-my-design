# Meteorite source-contract 0.2 task lock — 1.9.718

`meteorite-section-loan-v0.1` is a fresh fictional repair task with provider calls 0 and model exposures 0. The provider-free raw baseline is 75/85: task contract, design grounding, state journey, and evidence honesty pass; responsive and accessibility are the two failed critical gates.

The normalized baseline evidence is hash-locked to the task and records the full evaluator score hash. Source-contract 0.2 admission compares the exact false critical-gate set to `critical_gate_debt_coverage`; missing accessibility or responsive coverage now rejects preparation before a model can see the workspace. A byte change to the evidence file also rejects preparation.

The sealed packet covers all four debts: specimen register reflow, return-strip reflow, decision reflow, and the 4.47:1 muted canvas text. It includes exact parent containment `.decision > div { min-width: 0; }`, separate target/evidence carriers, and exact ink-role declarations for header guidance and footer. The packet remained byte-stable across the read-only `source-packet` command.

The schema 0.2 prepared smoke used the current local skill without calling a provider. Focused contract, admission, helper, and inventory tests pass 71/71; TypeScript passes. This task is locked but not preregistered for execution. Provider execution remains forbidden until the exact task commit is recorded in a new one-cell plan with timeout, effort, no-retry policy, and diagnostic-only claim boundary.
