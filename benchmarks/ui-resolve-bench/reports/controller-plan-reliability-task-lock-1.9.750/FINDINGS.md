# Controller-plan Reliability task lock — 1.9.750

The failed 1.9.749 root remains frozen. Geology is excluded after Luna exposure. Herbarium and photographic negative return remain provider/model-zero; architectural drawing return is the fresh third task.

All three tasks begin at 75/85 with only responsive and accessibility red. The new task was evaluated locally without provider or model exposure. Cursor execution is forbidden; the experiment uses the Codex runtime and `gpt-5.6-luna` only.

The next plan must require controller-owned pre-edit browser measurement before any provider call. Execution remains forbidden until this task lock and the exact matrix are committed and a fresh isolated named-browser root passes provider-zero admission.
