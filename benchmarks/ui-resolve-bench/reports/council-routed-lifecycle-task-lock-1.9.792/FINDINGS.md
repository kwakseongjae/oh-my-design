# Council routed lifecycle task lock — 1.9.792

The three 1.9.791 tasks are now immutable experimental inputs at source commit `4f1fe866`. The lock covers prompt, starter HTML, DESIGN.md, task contract, baseline evidence, source contract, council oracle, deterministic council helpers, lifecycle adapter, and installed OmD apply skill.

No model has seen these task bytes. Execution remains forbidden until a provider-zero controller proves the exact transition contract and a separate run matrix preregisters model, effort, order, timeout, pacing, retry, browser, answer-receipt, product-write, and stop conditions.

The blocker is intentionally not an incomplete UI trial: its successful outcome is a byte-identical product plus an exact external-evidence handoff. The interview and advisory-ready cases become UI trials only after their respective checkpoints are satisfied.
