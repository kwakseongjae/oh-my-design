# Book signature host-policy final replacement — 1.9.210

This is the fresh-root replacement for execution-invalid 1.9.207 and 1.9.208. It changes only the experiment identity, product patch, output root, and the Codex runner boundary proven in 1.9.209. It does not reuse any earlier completed cell.

The task, exact detached close-latch skill, Codex/Luna high runtime, balanced 2×3 order, 900-second timeout, 120-second pacing, no-retry policy, and all quality/host-policy gates are unchanged. Both arms are Git roots; the only paired arm delta is the explicit project proof-policy installation.

Both arms load the same Codex user configuration because Codex 0.144.1 suppresses project-hook discovery under `--ignore-user-config`. Only the installed arm receives the automation-only native hook-trust bypass. This is part of the locked host execution contract and was validated against the production installer in `reports/codex-runner-policy-smoke-1.9.209/`.

Acceptance remains: installed UI-Resolved 3/3, paired quality loss 0, valid installed state 3/3, and zero unblocked browser recovery, duplicate static closure, or verification-after-ready. Denied attempts are reported separately. Any new infrastructure, attribution, timeout, or preparation failure freezes this replacement.
