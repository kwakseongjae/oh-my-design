# Certificate-rotation reflow v6 Grok validation — preregistration

Status: **LOCKED before preparation and benchmark provider execution**.

- Task: exact unseen `certificate-rotation-plan-v0.1` `0.1.0`
- Arms: exact previous canonical `omd-portable-jade` versus exact v6
  `omd-portable-reflow-v6-candidate`
- Provider scope: Cursor runtime / `cursor-grok-4.5-high` / High, applied to
  both arms
- Matrix: 2 arms × 3 trials with balanced first-arm order
- Timeout: 900 seconds; globally serial; fixed 120-second inter-cell pacing
- At most one new cell per invocation; no retry, fallback, repair, replacement,
  resume after failure, or model substitution inside this root
- Attribution: Internal registered-display-name result, not merged with Luna or
  prior tasks

The repository-free capacity probe completed before this lock and is diagnostic
only. It is excluded from the benchmark denominator and does not guarantee
long-form capacity.

The release gate is candidate UI-Resolved 3/3 and Reliability@3 100% with no
function, accessibility, evidence, hierarchy, injected-break, atomic-context,
compact-label, undeclared-type-shrink, single-text-scroll, generated-label, or
residual-width regression. A provider, attribution, or controller failure
freezes this root.
