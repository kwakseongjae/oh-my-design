# Consumer browser fit contract — 1.9.467

Status: **provider-free repair implemented; exact pin and unseen transfer remain pending**.

The 1.9.466 satellite candidate passed its self-authored proof but overflowed the objective consumer browser by 10 CSS px at 320px and 20px at actual 200% reflow. It had launched an independent Playwright Chromium inside the browser-harness command, reduced a protected decision type role to 13px, and accepted a zero-reserve fit. The UI remained 81/85, so the remaining four provider cells were frozen.

Schema 0.3 closes those three escape paths. The pre-edit artifact now locks the exact existing-CDP consumer connection contract and each protected row's computed font size, line height, weight, and 8 CSS px fit reserve. Finalization compares `BU_NAME` and `BU_CDP_URL` to the recorded attachment, rejects every launched-browser fallback, verifies document and body scroll/client widths in all three conditions, and rejects typography shrink or borderline fits. The proof-policy classifier also blocks `chromium|firefox|webkit.launch()` inside a browser-harness command as forbidden browser recovery.

The canonical and experimental skills share this contract; the experimental copy still differs only by its decision-context hierarchy paragraph. The 8px reserve is a measurement safety margin for engine rounding, not a new visual design token. No task selector, task fact, provider-specific answer, or benchmark-specific styling was added.

Validation:

- reflow, apply, proof trace/policy/runtime focused suite: 84/84;
- installed-skill distribution plus focused contracts: 85/85;
- TypeScript and diff checks: green;
- broad suite first pass: 526 green / 1 skip; one relevant stale helper-usage expectation was repaired, while two external vendor Git-root failures and one pre-existing timeout-attribution expectation remain outside this repair.

No model provider was called. The next step is an exact immutable source pin, followed by a genuinely unseen transfer task under the same Reliability@3 protocol.
