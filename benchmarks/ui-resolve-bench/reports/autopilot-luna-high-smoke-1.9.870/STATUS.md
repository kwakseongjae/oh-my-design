# Autopilot Luna/high smoke 1.9.870

Diagnostic only. The exact three-cell block completed with three valid terminal
records, seven model calls, no user-question batch, and no retry, replacement,
fallback, or Cursor call. All three project-owned DESIGN.md proofs passed.

The sealed objective result is 1/3 UI-Resolved: neighborhood library improved
`10→100`; cold-chain remained `20→20`; clinic locales improved
`10→50→50`. The 12-task comparison gate therefore remains blocked.

In-app inspection and provider-free recalibration found two classes of failure:

- The locale product is objectively complete. The v1 evaluator rejected a
  correctly localized Traditional Chinese unavailable-translation alert,
  required the document language to remain English even though the selected
  language was correctly preserved as `zh-TW`, and read localized negated
  medical disclaimers as affirmative claims. Under the corrected v2
  observable-outcomes methodology, the same sealed product bytes score 100.
- Cold-chain had evaluator false negatives for visually explicit `URGENT`
  rows and a `Sample owner` option group. Those are corrected in v2, but the
  same bytes still fail real contracts: missing owner-field focus after an
  empty assignment, success status without shipment identity, 42px mobile
  controls, and serious Assign-button contrast violations.

The sealed records were not rewritten. The provider-free v2 diagnostics are
RCA only. A fresh commit-bound epoch is required before any reliability,
superiority, release-readiness, or public one-shot claim.
