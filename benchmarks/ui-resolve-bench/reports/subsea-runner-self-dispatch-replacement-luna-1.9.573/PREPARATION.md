# 1.9.574 — Subsea self-dispatch replacement preparation

The preregistered matrix is materialized as six untouched, detached workspaces at `/private/tmp/u19573`. All six cells share the same prompt, starter product tree, task, runtime, Luna/high profile, 900-second timeout, and fixed no-retry contract. The only installed-source delta is exact control `6142925c…` versus exact candidate `a6ebdc8a…`.

The candidate retains the shipped runner self-dispatch path and adds snapshot-time validation that every aggregate carrier class, id, or attribute anchor already exists in the pre-edit source. The supplied subsea task has a stable target-only carrier, so this second guardrail is expected to remain inert. No provider was called.

The next allowed action is runtime browser, CLI, authentication, and evaluator preflight followed by exactly one `luna-subsea-cable-r1-control` execution. Later cells remain untouched until the frozen 120-second pacing and early-stop rules permit them.
