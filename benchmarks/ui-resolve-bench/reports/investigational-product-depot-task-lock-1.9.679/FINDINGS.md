# Investigational-product depot task lock — 1.9.679

## What is locked

`investigational-product-depot-release-v0.1` is a genuinely unseen repair task created without exposing it to a model or provider. Its prompt, starter, structured contract, evidence boundary, and initial deterministic result are frozen together.

- Provider calls: **0**
- Model exposures: **0**
- Human judgments: **0**
- Initial deterministic result: **75 / 85**, automated gate **fail**
- Methodology epoch: `ui-resolve-objective-2026q3-passive-scroll-v1`

The domain is deliberately unlike the existing pricing, onboarding, infrastructure, media, transport, and industrial tasks. It tests whether a system can repair a dense investigational-product depot review without converting unknown regulatory or clinical decisions into facts.

## Known failures

The starter preserves the task while leaving focused repair work:

- horizontal overflow at 390px, 320px, and the 200% CSS-zoom surrogate;
- fragmented long kit/log identifiers and wrapped short atomic status copy;
- one serious `color-contrast` class caused by `#899693` text on `#EEF2F1` at 2.71:1.

It does **not** hide overflow behind passive text scrollers. Desktop geometry, decision hierarchy, control sizing, and keyboard traversal already pass. This prevents a system from receiving credit for merely preserving the page while leaving its narrow-screen and accessibility defects unresolved.

## Preserved behavior and evidence

The deterministic evaluator and a separate Codex in-app browser pass confirmed:

- exactly 4 product kits, 6 temperature logs, 2 depot lanes, and 3 review views;
- view switching, pharmacist-note toggle, invalid submit feedback, and valid save feedback;
- all protected hooks, supplied known statements, and design tokens;
- zero unsupported claims, console errors, external requests, and passive text scrollers.

The in-app browser is useful here as an interaction and user-surface cross-check. The deterministic evaluator remains the authority for 390px, 320px, and zoom geometry because the current in-app binding does not expose viewport emulation.

## Claim boundary

This baseline is not an OmD result, a competitor result, or comparative evidence. Any future Raw DESIGN.md versus OmD comparison must prepare both arms from these exact hashes, keep the objective-methodology epoch fixed, and leave this task directory unchanged.
