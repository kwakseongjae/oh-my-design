# Cursor/Grok incident geometry candidate recovery — 1.9.68

Status: **PREPARED; authorized and awaiting provider execution**.

## Bounded hypothesis

Grok 1.9.66 repeated the incident OmD card-radius design-grounding loss in two
of three trials. Provider-free 1.9.67 added exactly one candidate delta: the
mandatory `geometry-token closure`.

In one fresh incident OmD cell, Cursor Grok 4.5 High should execute the new
closure and produce the exact declared card radius while preserving every
previously passing task, responsive, accessibility, all-critical, Evidence &
Unknown, and interactive-closure requirement.

This candidate-only recovery is outside every Raw/OmD denominator. Passing
unlocks a separately preregistered repeated replacement; it does not establish
Skill Lift, Reliability@3, or a model comparison.

## Frozen cell

- schema `0.3`; suite `ui-resolve-v0.2`; evaluator `0.5`;
- product version `1.9.68`;
- matrix SHA-256
  `04b34616f3968a0a8f3c89edb56ab60f1edd5c220cc8ad3c59024ae8fc375025`;
- runtime Cursor Agent `2026.07.23-e383d2b`;
- selector `cursor-grok-4.5-high`;
- expected display name `Cursor Grok 4.5 High`;
- attribution display-name only, Internal;
- provider effort argument none; controller effort metadata `high`;
- task/variant `incident-operations-v0.1` / `omd-portable`;
- timeout 900 seconds;
- live-provider concurrency 1;
- immutable `max_new_cells=1`;
- retry, fallback, model substitution, failed-cell replacement, manual product
  edit, and inter-cell pacing none;
- fresh root `/tmp/u1968`.

## Frozen source equality

- incident starter
  `0ea045c3b297e86dbee9a094da113e2b85f2d109daaf2c25631c2432d545217b`;
- incident DESIGN.md
  `bbeab902a6a23d0de5ce8c7d804923844f1e87f96a960d32f04f8e52de041b3a`;
- incident OmD prompt
  `7b1b16cca519c9abb4f0779919909d68812e2f66cb28eb24e92493610ea50f2d`;
- OmD SKILL.md
  `2ce11fb14cee0c34c9707d15bb2583fe606401beca1c3b9f22129d49d85f0845`;
- OmD sidecar
  `f0ad5294de3f599bbed124708c61988be9098e20d4723ce3574023144751253e`.

No historical workspace, score, output, or partial artifact may be copied into
the fresh root.

## Transmission boundary

Standing authorization applies only to task-owned `index.html`; `DESIGN.md`;
`.benchmark/PROMPT.md`, `manifest.json`, and `matrix-cell.json`; local
`AGENTS.md`; and `.cursor/skills/omd-apply/SKILL.md` plus
`agents/openai.yaml`.

Credentials, secrets, repository history, unrelated source, catalog data,
user documents, `web/public/llms-full.txt`, other skills, and historical
provider artifacts are excluded.

## Acceptance and stop

Pass only at 85/85 with automated, accessibility, all-critical, task behavior,
Evidence & Unknown, and four-viewport gates all green; exact incident card
radius compliance; zero hidden, clipped, invisible, or unresolved focusable
targets; and an `index.html`-only product diff.

Freeze at the first source, preparation, preflight, lease, auth, quota,
selector, display-name, runtime, hash, process, timeout, product-diff,
evaluator, exporter, history, or attestation failure. No retry, same-root
resume after a stop, stale-lease deletion, score change, failed-cell
replacement, or model substitution is allowed.

If Grok stops for actual quota or capacity, Terra High and Luna High may be
opened only as separately preregistered model lanes. They do not complete or
replace `/tmp/u1968`.
