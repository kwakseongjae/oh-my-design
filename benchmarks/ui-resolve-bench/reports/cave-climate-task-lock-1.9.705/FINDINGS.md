# Cave climate task lock — 1.9.705

The fresh public development task is locked at commit `4bd03c7b…` with zero
provider calls and zero model exposures. It preserves five probe IDs, seven
data-cartridge IDs and assignments, four chamber collection windows, three
views, one technician-note toggle, one form, and an explicit evidence boundary.

The raw DESIGN.md starter scores **75/85**. Contract, state journey, design
grounding, and evidence honesty pass. Responsive and accessibility gates fail
because the narrow layouts retain fixed-width relationship carriers, fragment
atomic values, wrap short decision rows, and expose a 4.18:1 contrast issue.

This task is a one-cell transfer diagnostic denominator only. It cannot enter a
public ranking or promote a 2.0 frontier gate.
