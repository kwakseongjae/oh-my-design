# Abyssal sealed-admission canary — 1.9.716

The explicitly approved Luna/high cell completed once without retry or replacement. It is a valid single-arm diagnostic: 109.514 seconds, 361,341 observed tokens, one product edit, one product revision, one successful static closure, zero failed static closures, zero browser recovery attempts, and proof compliance. The provider-sealed source contract and immutable inventory reached the model unchanged, `source-packet` was consumed before the edit, and the model did not edit the sealed artifact.

The lifecycle architecture transferred, but the UI did not resolve. The deterministic score rose from the 75/85 raw baseline to 77/85. Task contract, design grounding, state journey, and evidence honesty passed. Responsive and accessibility remained critical failures.

At 320px the document was 17 CSS pixels wider than its viewport; the 200% surrogate was 34 pixels wider. The target, evidence, and action all inherited a 305.984px intrinsic width inside a 296px decision container. The packet removed positive `min-width` from the target and evidence carriers and added local overflow, but it did not constrain their parent grid item. Because the nowrap descendants continued to define the parent's automatic minimum size, the carrier itself escaped the container instead of scrolling locally. An exact parent containment obligation such as `.decision > div { min-width: 0; }` was missing from both the source contract and static closure.

The original muted token also produced 4.47:1 contrast on the canvas. Axe reported serious `color-contrast` failures on `header > p` and `footer` in every viewport. The raw baseline already exposed this accessibility failure, but the provider-sealed acceptance-debt ledger enumerated only the three reflow debts. Luna therefore received no contrast correction or static guardrail.

This is a contract-completeness failure, not a repeat of the model-authored-manifest failure. The architecture successfully removed model authorship and post-lock mutation from the closure path. The next local repair must make pre-provider admission prove that every known raw critical-gate failure has a represented debt, and it must add exact parent-containment assertions for nowrap relationship carriers. The same abyssal task must not be rerun. Any later provider transfer requires a new fictional task, a new preregistration, and a new root after the local repair is clean.

This diagnostic does not change the public leaderboard or the 2.0 readiness snapshot.
