# Visible atomic inventory and fit reserve — 1.9.490

The water transfer isolated two generic proof-boundary defects. Fit reserve
was measured to the carrier content edge after subtracting padding, so a
correct button with 16px intentional padding reported only 1px. The inventory
also promoted long evidence prose and an initially empty live region into
one-line atomic rows even though neither had a visible one-line contract at
the terminal measurement state.

The runner now measures to the carrier's inner border. Padding therefore
counts as the intended safety space while borders do not. The skill inventory
admits only visible, non-empty dynamic states. Evidence, summary, metadata, and
supplied counts require both 48 characters or fewer and explicit product
authority for one-line rendering; longer prose and empty/hidden status remain
preserved carrier content rather than fake atomic rows.

Two real consumer-browser replays used the frozen 85/85 water UI with raw CDP
variables absent. The reserve-only replay raised the operator-note measurement
from 1px to 16px and removed that failure. A freshly locked inventory following
the new generic rule measured 12 carrier instances and 28 row instances across
390px, 320px, and actual 200%; it closed with zero unresolved rows/carriers and
`OMD_DELIVERY_READY`.

TypeScript, 72 focused tests, the bounded experimental mirror contract, and
diff checks pass. No provider was called and this repair is not yet promoted.
