# Field-sample named carrier diagnostic — 1.9.693

The exact `37f3bde` OmD skill was prepared from a clean publishable commit and executed once with Luna/high. The run was valid, changed only `index.html`, requested no user intervention, made one product revision, and did not retry browser infrastructure. It scored 81/85 and did not resolve the UI.

The new named-carrier rule did not transfer. The implementation preserved target and evidence as separate values, but neither value received a distinct accessible relationship carrier with keyboard reachability and visible focus. At 320px and the 200% surrogate, the concise evidence still wrapped to two lines and the compound target still contributed to document overflow. The objective responsive gate therefore remained red.

The run also exposed an independent benchmark-auditor defect. `static-close` exited 1 because the measured plan had never closed, yet the proof trace counted the command invocation as one successful static closure and emitted `compliance_pass: true`. Reclassifying the same immutable event stream with command completion status yields successful static closures 0, failed static closures 1, and compliance false. The raw run artifact remains unchanged; the auditor is repaired separately so future runs cannot receive proof credit for a failed verifier.

The next skill repair must make the browser-infrastructure fallback internally coherent. After a failed pre-edit plan attachment, the skill currently permits a bounded source-backed edit but still sends that edit to a `static-close` command that requires a measured plan closure. A supported deterministic fallback closure must either be added or the fallback must use another existing verifier whose success can be machine-observed. Its static contract must explicitly require separate named target/evidence carriers, exact text, `nowrap`, accessible names, keyboard reachability, visible focus, and exclusion of state and actions.

This diagnostic does not change the 2.0 readiness snapshot or support a public superiority claim.
