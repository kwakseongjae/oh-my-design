# Field-sample named carrier diagnostic — 1.9.693

This single exact-current OmD cell tests whether the 1.9.692 source-derived fallback implements distinct named target/evidence relationship carriers when browser measurement is unavailable. It reuses an exposed public task and therefore cannot enter Skill Lift ranking or improve any 2.0 gate by itself.

Acceptance requires no user handoff, one bounded product revision, no browser recovery, proof compliance, and either objective resolution or explicit evidence that the remaining failure is outside the newly specified carrier behavior. The exact skill source is commit `37f3bde`.
