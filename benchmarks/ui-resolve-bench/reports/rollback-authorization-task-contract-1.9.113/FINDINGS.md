# Rollback authorization is the first post-feedback holdout

A fresh untouched workspace at `/tmp/u19113-rollback-starter` passed the full
browser evaluator:

- deterministic score: 85/85;
- critical gates: 6/6;
- filter, two disclosures, cancel, confirm, state update, dialog close, and
  focus restoration: green;
- desktop, 390px, 320px, and CSS-zoom surrogate geometry: green;
- every registered text-geometry check: green;
- every registered decision-hierarchy check: green;
- keyboard, focus, axe, language, localhost-only, and evidence honesty: green.

The mobile fixture deliberately uses label-above-value metadata rather than the
narrow two-column treatment that caused the deletion outputs to fragment.
Status dots use their own child element instead of competing with generated
mobile labels through the same pseudo-element. The selected authorization
boundary remains visible and groups target, supplied evidence, blocker state,
and action without adding a warning banner or invented risk.

This is a task-contract acceptance only. No model or skill has run, no
candidate score exists, and no performance or frontier claim is authorized.
The next denominator is an exact current-canonical Reliability@3 baseline.

