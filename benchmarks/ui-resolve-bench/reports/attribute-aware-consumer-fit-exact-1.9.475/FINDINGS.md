# Attribute-aware consumer fit exact pin — 1.9.475

The provider-free 1.9.474 repair is pinned at exact source commit `e848d129667d661a240546762a25691e732de5d5`. The immutable skill tree, static helper, evaluator, and their focused contract tests are hashed in `SUMMARY.final.json`.

The new competitor id is `omd-attribute-aware-consumer-fit-candidate`, installed from detached vendor directory `omd-1.9.474`. This prevents future benchmark preparation from silently using a mutable working tree or the older 1.9.467 verifier behavior.

No quality promotion is claimed. The next provider-free patch replaces model-authored browser measurement code with a shipped browser-harness runner that consumes the locked schema 0.3 artifact, attaches to the named existing consumer CDP session, measures every registered row/carrier, and invokes the terminal finalizer in the same browser command.
