# Wildlife acoustic-media task lock — 1.9.702

`wildlife-acoustic-media-custody-v0.1` is a new fictional public development task with zero provider calls and zero model exposures at lock time. It preserves five deployment identifiers, seven media-card identifiers and exact assignments, four station retrieval windows, three choice states, one toggle, one validated form, and an explicit target/evidence/state/action decision boundary.

The contract forbids unsupported claims about species presence, recording completeness, calibration, habitat clearance, media integrity, transfer acceptance, archive publication, and custody closure. All 79 public task contracts pass the core validator, and the existing 11 mutation classes remain killed.

The unedited Raw DESIGN.md starter scores 75/85. Task contract, state journey, design grounding, and evidence honesty pass. Desktop geometry passes. At 390px, 320px, and the 200% zoom surrogate, fixed carrier widths produce page overflow and atomic relationship fragmentation. Muted copy also fails serious contrast at 4.18:1. This gives the repair system a bounded, observable target without seeding a solution in the prompt.

The task is public development evidence, not a hidden task. A future single-cell run is diagnostic only and cannot enter Skill Lift ranking or advance a 2.0 promotion gate.
