# Effective-skill terminal runner Reliability@3 — 1.9.747

This is the provider-zero replacement for frozen 1.9.746. Task bytes and every execution condition are unchanged. The sole methodological repair is that the skill lock now attests the effective installed council-gate tree (`b792a4e8…`), including its deterministic activation delta, rather than the base source tree.

The fixed order is microscopy slide custody, manuscript folio transfer, then geology section loan. Every cell uses Codex with `gpt-5.6-luna`, high effort, a 720-second timeout, concurrency one, a fixed 30-second inter-cell pause, and no retry or replacement. All tasks remain provider/model unexposed.

A pass requires 85/85, all critical gates resolved, one product revision, one successful and zero failed static closures, zero user handoffs, unchanged sealed inventory, exact candidate-to-final bytes, and analyzable proof using `scripts/reflow-browser-runner.sh` with zero recovery commands. Reliability requires 3/3; any lifecycle failure freezes the remaining cells.

This is internal diagnostic evidence, not a model-ranking, general council-lift, industry-leading, or standalone 2.0 release claim. Provider execution is forbidden until this exact plan is committed and the new root passes provider-zero preparation and effective-skill admission.

## Terminal result

The first cell resolved the product at 85/85 with one revision and exact candidate-to-final bytes, but proof failed. The runner invoked browser-harness correctly; browser-harness had a live daemon but zero active browser connections, so it produced no measurements. The plan omitted `browser_execution_contract`, which let runtime preflight pass without an isolated named browser. The hard-stop froze the other two cells before provider exposure. This root is not Reliability evidence and must never be resumed or replayed.
