# Geometry-token execution compliance — 1.9.67

Status: **LOCKED; provider-free implementation pending**.

## Frontier gate and bounded hypothesis

Grok 1.9.66 completed 18/18 cells, but incident OmD missed the declared card
radius in two of three trials. Both artifacts passed task, responsive,
accessibility, all-critical, and Evidence & Unknown gates. The remaining
cluster is post-edit design-token execution compliance.

The bounded hypothesis is that one mandatory `geometry-token closure` in the
canonical `omd-apply` skill will make radius-token compliance transactional:

1. enumerate every added or changed surface with a `border-radius`;
2. classify its existing product role without inventing a role;
3. reconcile the source declaration or computed value with the exact
   DESIGN.md role token;
4. correct a mismatch before acceptance;
5. preserve pre-edit geometry when a changed surface has no declared role
   token, rather than synthesizing a plausible radius.

## Frozen denominator

- product version `1.9.67`;
- source `skills/omd-apply/SKILL.md`;
- source skill SHA-256
  `c63ba1e47fe1466c03299c475d8e51f0d6ce376b6631893d703bb110296a3e03`;
- sidecar SHA-256
  `f0ad5294de3f599bbed124708c61988be9098e20d4723ce3574023144751253e`;
- observed cluster: 1.9.66 incident OmD trials 1 and 3;
- implementation: provider-free repository edit;
- provider generation: zero;
- changed surface: canonical OmD apply skill and contract/channel tests only.

The candidate may clarify the post-edit order and add exact radius-role
assertions. It may not change DESIGN.md authority, task prompts, benchmark
evaluator, score thresholds, runtime/model controls, historical artifacts,
foreground closure, interactive closure, or unknown-as-absent behavior.

## Acceptance

Pass only when:

- the skill names `geometry-token closure`;
- it runs after foreground correction and before the final interactive
  closure, optional browser verification, and delivery;
- it inventories every added or changed radius-bearing surface from the
  actual product diff;
- each item records identity, product role, before/after declaration,
  declared role token, evidence mode, and decision;
- a known role with a declared token uses that exact token or equivalent
  computed value;
- an unknown role or absent token preserves pre-edit geometry and does not
  invent a radius;
- acceptance requires zero mismatched, invented, or unresolved changed
  radius surfaces;
- foreground/interactive closure and delivery-reserve contracts remain
  intact;
- focused skill, install, Cursor adaptation, TypeScript, build, and diff
  checks pass.

## First failure and rollback

Any contract regression, generated-channel mismatch, test failure, unrelated
repository change, or weakening of existing closures rejects the candidate.
Roll back only the skill/test delta.

Passing is provider-free contract evidence. It does not prove model compliance
or repair 1.9.66 retroactively. A fresh incident OmD recovery cell is required
before any repeated replacement matrix.
