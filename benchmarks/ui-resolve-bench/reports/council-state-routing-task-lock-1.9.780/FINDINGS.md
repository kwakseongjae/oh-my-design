# Council state-routing task lock — 1.9.780

Three unseen tasks are now byte-locked before any model exposure. Each raw DESIGN.md baseline scores 75/85 and fails exactly responsive and accessibility, so the future arms start from the same objective debt shape.

The comparison changes one treatment only: current OmD execution versus state-routed council-first intake and human escalation. Task, prompt, starter, DESIGN.md, Luna/high runtime, timeout, browser contract, objective evaluator, and deterministic reflow lifecycle stay equal.

This is not another `prime-only-no-dispatch` framing test. The candidate must materialize the actual deterministic decision ledger, route only the active handoff state, preserve product-authority questions, keep external-evidence blockers out of the interview path, and expose registered versus unplanned intervention counts. Advisory dispatch is permitted only when the frozen dispatch plan requires it; it cannot rewrite `auto` decisions or product bytes.

No provider, model, or Cursor call occurred. This lock is denominator evidence, not UI quality evidence and not a 2.0 promotion result.
