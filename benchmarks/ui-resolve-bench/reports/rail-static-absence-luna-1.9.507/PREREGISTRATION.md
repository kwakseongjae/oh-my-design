# Rail static-absence transfer preregistration — 1.9.507

This provider-free plan compares exact 1.9.497 computed-type target-carrier
control `14ed7765…` with exact 1.9.504 static-absence guardrail candidate
`86546d5a…` on the unseen rail-interlocking event-log task locked at
`d1994c71…`.

The runtime is Codex with Luna/high. Reliability is measured over three paired
trials in balanced order, with a 900-second timeout, concurrency one, 120-second
inter-cell pacing, and no primary retry. All input, cached input, output, and
reasoning-output tokens count toward Tokens-to-Target.

Both arms must use the shipped runner in the exact named consumer browser and
close 390px, 320px, and actual 200% reflow. The candidate additionally exposes
the locked required/forbidden/count contract before editing and treats a
forbidden CSS property as absent only when the matching declaration is deleted;
neutral values do not satisfy the contract.

Promotion requires candidate UI-Resolved, proof-compliant, and shipped-runner
3/3, no paired objective loss, no serious/critical contrast issue, and mean
wall-time and token ratios no greater than 1.1. This preregistration calls no
provider and promotes no result.
