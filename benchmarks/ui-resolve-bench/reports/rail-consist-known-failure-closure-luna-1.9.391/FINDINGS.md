# Rail consist known-failure closure result — 1.9.394

The historical control timed out at 900,014 ms and retained the 75/85 unresolved baseline. It made one product revision but never attempted browser proof or reached delivery-ready state. Usage was unavailable, so the result remains a valid preregistered system failure rather than a replay candidate.

The repaired candidate completed in 336,121 ms with 760,540 observed provider tokens and improved the deterministic score to 81/85. It removed horizontal overflow, fixed all serious contrast findings, and kept identifiers intact. One registered decision-evidence row — `6 wagons · 8 seals · 4 departure windows` — still wrapped at 320px and the 200%-reflow surrogate, so responsive acceptance remained false. The system honestly finalized unresolved and did not claim delivery, but that is still a failed resolution trial.

Candidate Reliability@3 is therefore unreachable: zero of one candidate trials resolved, and the two unrun candidate trials can raise the maximum only to two of three. The remaining four cells receive no provider calls. `/private/tmp/u19391` and its vendor root are frozen with a cancellation sentinel.

The 1.9.389 prose-level known-failure ledger improved honesty but did not make closure mechanically conjunctive. The next provider-free repair must bind every registered measured row to the runtime closure artifact and reject finalize/delivery whenever any required row remains unresolved. A new exact pin and a new unseen task are required before another scored call.
