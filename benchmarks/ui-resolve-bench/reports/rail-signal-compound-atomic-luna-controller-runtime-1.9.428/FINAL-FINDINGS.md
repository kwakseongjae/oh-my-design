# Final findings — rail-signal runtime-bound controller 1.9.431

The experiment is frozen after the first pair. Both cells were valid 79/85 system failures with the same unresolved compound atomic line at 390, 320, and 200% zoom. Because promotion required candidate UI-Resolved 3/3, the first candidate failure caps reliability at 2/3; the remaining four calls cannot change the decision and are skipped.

The runtime repair is accepted. Both runs recorded the same Luna profile from their isolated Codex 0.144.1 execution homes. No shared-cache race contaminated attribution.

The candidate did reach browser-harness, but only after probing available commands and reading its help/skill text. It accumulated three browser/recovery operations, twelve duplicate static closures, and six verifications after ready. It was 2.17× slower and used 6.73× as many tokens as control. The next patch must provide a ready-to-run browser proof recipe and a single-closure budget before a new unseen transfer task is registered.

Tokens-to-Target is now at least 37,317,990 plus six usage-unavailable cells. Quality promotion remains false.
