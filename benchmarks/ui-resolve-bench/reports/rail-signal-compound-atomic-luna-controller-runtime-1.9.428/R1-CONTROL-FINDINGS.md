# R1 control findings — 1.9.430

The runtime-bound controller worked: the cell completed under Codex CLI 0.144.1, and attribution came from the isolated execution home rather than the shared auth cache.

The control is a valid system failure at 79/85 in 299.973 seconds and 402,095 tokens. It preserved the task, state, evidence boundary, desktop, accessibility, keyboard, and design contract. It did not keep the compound target atomic at 390, 320, or 200% zoom. It also performed no browser-harness proof and repeated static closure after a second revision, so proof compliance failed. No infrastructure exclusion applies.

The matrix is checkpointed after one cell. The next preregistered cell is r1 candidate after the fixed pacing boundary.
