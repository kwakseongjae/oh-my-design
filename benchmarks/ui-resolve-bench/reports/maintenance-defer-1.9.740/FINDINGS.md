# 1.9.740 findings — repository-backed maintenance defer

Existing-surface maintenance now preserves a repository-backed audience and
single-surface boundary without reopening them as product questions. The values
are not promoted to `auto`; they remain absent as typed `defer` decisions and
are excluded from council dispatch. The rule requires a detected existing or
current improvement task, one surface, and audience evidence with confidence at
least 0.75.

On the frozen three-case denominator, provider-zero questions fall to two,
human handoffs to two, and planned council calls from six to four. The existing
docs case needs no council or user question. Regulated pricing still retains its
authority boundary, and the missing official reference remains blocked.
Forbidden auto remains zero and the 22-case five-locale calibration stays green.

This is local policy evidence. Fresh repeated runtime and real product-task
completion quality are still required for the 2.0 gate.
