# 1.9.799 frontier skill Stage A preregistration

This is a current-epoch qualification, not a leaderboard. The same
Codex-native Luna/high runtime receives four exact pinned skill snapshots on
the same three fresh repair tasks. The fixed order rotates arm position across
tasks. Every arm receives the full locally installed skill bundle its official
adapter declares, while network access and self-launched browser work remain
disabled. One external browser-harness controller owns the exact proof route
for all arms.

Twelve cells run serially with a fixed 30-second inter-cell delay. Timeout is a
valid failure. Retry, replacement, fallback, model substitution, and Cursor are
forbidden. Quality, time, and token usage are reported separately.

Passing this stage means an arm can enter a separately preregistered
Reliability@3 expansion. It does not establish statistical superiority, close
the 2.0 Verified Skill Lift gate, or authorize a public “best UI skill” claim.
