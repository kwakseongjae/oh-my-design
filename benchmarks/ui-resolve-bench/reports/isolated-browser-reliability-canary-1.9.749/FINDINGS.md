# Luna isolated-browser Reliability result — 1.9.749

The first geology cell made one product revision with exact candidate-to-final bytes, but scored 81/85 and failed proof. The final shipped runner exited because no measured pre-edit fit plan existed. Runtime preflight had already proved the exact `omd1749` connection and Codex auth, so this was not a missing browser or Cursor issue.

The gap is orchestration ownership: a deterministic, provider-free pre-edit measurement was still delegated to the model. Luna read the contract but did not run `OMD_REFLOW_MODE=plan` before editing. The final runner correctly failed closed. The controller stopped the matrix; herbarium and photographic negative return remain unexposed.

The fix is to execute and attest the pre-edit plan in the controller after runtime preflight and before provider exposure. A provider-free copy probe confirmed the current runner can close the measured plan successfully against `omd1749`. The 1.9.749 root is frozen and must not be replayed.
