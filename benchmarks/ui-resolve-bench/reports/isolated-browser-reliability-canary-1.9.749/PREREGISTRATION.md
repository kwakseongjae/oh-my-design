# Isolated named-browser Luna Reliability@3 — 1.9.749

This provider-zero experiment replaces frozen 1.9.748 without replaying manuscript. It uses the two tasks that remain unexposed—geology and herbarium—and one fresh photographic negative-return task. The controller must see a dedicated local CDP Chrome as the exact active `omd1749` browser-harness connection before any provider call.

Cursor is forbidden. All cells use the Codex runtime, `gpt-5.6-luna`, high effort, a 720-second timeout, concurrency one, a fixed 30-second pause, and no retry or replacement. The browser has no user profile, login state, or cloud dependency; the model receives only the named browser-harness socket and may attach but not launch.

The fixed order is geology, herbarium, then photographic negative return. A pass requires 85/85, all critical gates resolved, one product revision, static closure 1/0, zero user handoffs, unchanged sealed inventory, exact candidate-to-final bytes, and analyzable shipped-runner proof with zero recovery. Reliability requires 3/3; a lifecycle failure freezes the remainder.

This remains internal diagnostic evidence. Provider execution is forbidden until this exact plan is committed and a fresh root passes task/skill/source/evaluator, council-intake, execution-artifact, Codex-auth, and exact named-browser admission.
