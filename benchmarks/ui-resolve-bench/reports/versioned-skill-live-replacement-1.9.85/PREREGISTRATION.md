# Versioned Skill Live Replacement — 1.9.85

Status: **LOCKED; awaiting fresh preparation**.

## Purpose

Replace the frozen 1.9.83 root after the provider-free 1.9.84 Cursor
project-cache preflight correction. This experiment asks whether the 1.9.78 visual-equity contract
improves or preserves outcomes relative to the immediately preceding OmD apply
skill.

It is an Internal patch-isolation run, not a public model leaderboard.

## Frozen denominator

- fresh root `/tmp/u1985`; no artifact from `/tmp/u1983` is reused;
- onboarding `0.3.0`, incident `0.4.0`, locale `0.5.0`;
- opaque slate/ember arms at exact detached commits;
- three trials per task and arm, 18 cells;
- exact cell order retained from 1.9.81;
- Cursor/Grok 4.5 High, controller effort `high`;
- attribution scope `internal-registered-display-name`;
- 900-second timeout, global concurrency 1;
- 120-second retained inter-cell pacing;
- `max_new_cells=1`;
- no retry, fallback, substitution, failed-cell replacement, or manual product
  edit.

## Reduced-human review ladder

Human judgment is not required for every benchmark execution.

1. All 18 cells receive deterministic, behavioral, accessibility, evidence,
   and product-diff evaluation automatically.
2. Eligible pairs receive anonymous automated multi-judge comparison with
   side-order reversal.
3. Human review is requested only for judge disagreement, reversal
   inconsistency, both-fail/tie uncertainty, or a bounded audit sample.
4. Human results calibrate the judge plane; they do not replace deterministic
   gates or inflate reviewer counts.

This replacement may complete automated execution without waiting for the
owner. Any later human packet must contain only the unresolved or sampled
pairs, not all 18 cells.

## Stop conditions

Before cell 1, require 18/18 preparation, exact task equality, distinct pinned
skill hashes, untouched product trees, evaluator preflight, Cursor project-cache
mkdir/write/remove preflight, exact selector and registered display-label
capacity probe, root lease, and a committed preparation record.

Freeze at the first preparation, lease, auth, quota, selector, label, runtime,
hash, timeout, pacing, product-diff, evaluator, exporter, or attribution-scope
failure. Never resume a failed root or fill it with another model.

No preference or patch-lift claim is allowed until fresh automated artifacts
and the preregistered review ladder complete.
