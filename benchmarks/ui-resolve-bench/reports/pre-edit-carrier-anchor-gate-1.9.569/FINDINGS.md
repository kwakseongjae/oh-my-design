# 1.9.569 — Pre-edit carrier anchor gate

The provider-free repair moves the organ candidate failure from the one-shot browser plan into deterministic artifact snapshot. Whenever a pre-edit product snapshot exists, every aggregate carrier must now use class, id, and attribute anchors already present in that source. A class planned for the later product edit is rejected before any browser budget is consumed.

The frozen r1 candidate artifact was replayed against the repaired helper. It now stops immediately with `carrier decision-target-carrier selector is unresolved in the pre-edit snapshot (class:decision-target-carrier)`, with zero provider calls and no browser plan attempt. The skill guidance now explicitly applies the stable pre-edit anchor rule to both rows and carriers.

All 40 reflow artifact tests pass, including a regression that reproduces the exact post-edit-only target carrier class. TypeScript and diff checks are green. This is a fail-earlier integrity repair, not a quality promotion; an exact source pin and fresh unseen task are still required.
