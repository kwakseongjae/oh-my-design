# Spectrum readable-reflow comparison — 1.9.233

The unseen `spectrum-allocation-review-v0.1` task was locked before provider generation in 1.9.232. This comparison asks whether the bounded readable-reflow delta transfers beyond the assay fixture.

Both arms use the same task, prompt, starter, DESIGN.md, activation sentence, Codex runtime, Luna model, high effort, 900-second timeout, project proof policy, user configuration loading, hook-trust boundary, balanced 2×3 order, and fixed pacing. The sole arm delta is the exact installed `omd:apply` skill source:

- close-latch baseline: `1d204afea6c3ab106752613b7f341ed2f76fd843`
- readable-reflow candidate: `7915723887a47a538c91f1aa51bd7b2972757091`

The project proof policy is installed identically in all six cells. This is not a host-policy comparison. Each trial is execution-valid only when policy state is valid, delivery is ready, at least one actual browser attempt is recorded, and unblocked browser-recovery, duplicate-static, and after-ready violations are all zero.

Execution is sequential with a fixed 120-second inter-cell delay, no retry, no same-root repair, no fallback, and no result substitution. Infrastructure, identity, attribution, preparation, policy-state, task, prompt, product, design, runtime, model, effort, timeout, activation, or source drift freezes the entire matrix.

The readable-reflow candidate passes this experiment only if:

1. UI-Resolved is 3/3;
2. serious or critical `color-contrast` occurs in 0/3 candidate trials;
3. candidate has zero paired objective-score losses.

Passing supports only a bounded readable-reflow transfer claim. Broader/default skill or proof-policy promotion remains a separate decision. Provider calls at preregistration: 0.
