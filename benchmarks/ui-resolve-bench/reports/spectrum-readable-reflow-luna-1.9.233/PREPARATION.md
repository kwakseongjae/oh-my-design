# Spectrum readable-reflow preparation — 1.9.234

Fresh root `/private/tmp/u19233` contains six Git-backed cells and zero provider calls. The two exact skill sources were recreated as clean detached worktrees under `/private/tmp/u19233-vendors`.

Every cell has byte-identical core prompt, runtime prompt, untouched product tree, DESIGN.md, activation, runtime, model, effort, timeout, and installed project proof policy. All policy manifests and installed managed-file hashes are identical. The only observed arm delta is the pinned installed skill tree:

- close latch: commit `1d204afe…`, tree `7336c037…`
- readable reflow: commit `79157238…`, tree `0cd7b71a…`

All six sources are detached, clean, and publishable; all six workspaces are Git roots and remain unstarted. The first permitted provider action creates only `luna-spectrum-r1-close`. Every continuation preserves max-new 1 and 120-second pacing. Any preparation, identity, attribution, policy-state, task, prompt, product, design, model, effort, timeout, or source drift freezes the full root without repair or same-root retry.
