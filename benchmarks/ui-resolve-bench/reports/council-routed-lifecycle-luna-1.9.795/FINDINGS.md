# Council routed lifecycle Luna/high — 1.9.795 findings

The full fresh lifecycle passed. Four read-only Luna/high council lanes produced valid declared artifacts with zero unauthorized writes. The product-authority task stopped at its registered question and resumed only after the hash-bound answer receipt. The bounded advisory task resumed only after both lanes reconciled. The external-evidence task remained blocked with zero dispatch, provider calls, answers, master activation, or product-byte drift.

Both implementation cells reached 85/85 with one product revision, one successful static closure, one measured exact `omd1795` browser attachment, zero browser recovery, and exact candidate/final bytes. Reliability is 2/2. Cursor, retry, replacement, fallback, model substitution, and unplanned questions stayed at zero.

The six provider calls consumed 3,459,785 reported input tokens, of which 3,210,752 were cached input, plus 32,685 output and 15,254 reasoning-output tokens. Summed provider wall time was 715,027 ms. These are descriptive operational measurements, not a claim that the council saves time or tokens.

This result closes the previously open fresh interview/block/advisory lifecycle denominator. It does not independently prove general council lift, subjective ship preference, or the complete 2.0 release gate.
