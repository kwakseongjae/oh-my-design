# Council routed lifecycle Luna/high — 1.9.795 preregistration

This run admits exactly two fresh implementation cells and one zero-provider blocker sentinel. The papyrus task must stop for its registered product-authority answer after two read-only advisory lanes reconcile. The ceramic task may resume only after its two bounded lanes reconcile. The photographic-proof task must never dispatch, ask, invoke a model, or change product bytes.

The four council lanes and two implementation owners use Codex-native `gpt-5.6-luna` at high effort. Execution is serial. Retries, replacements, fallbacks, model substitution, unregistered questions, second product edits, and Cursor are forbidden. Each implementation cell must reach 85/85 with one promoted revision and exact browser/proof/candidate-byte receipts.

The test closes only the fresh interview/block/advisory lifecycle denominator. It is not a model ranking and cannot independently unlock 2.0.
