# Volcanic manifest-default static path canary — 1.9.709

## Unresolved frontier gate

`verified-skill-lift` remains partial. This diagnostic does not promote it; it
checks whether the manifest-default static-close path and canonical CSS packet
transfer together on a fresh public development task.

## Bounded hypothesis

When browser measurement is unavailable, the exact 1.9.707 `omd:apply` skill
will expose one copy-ready canonical CSS block before the first product edit.
Luna/high will resolve the 75/85 volcanic starter in one product revision and
close the shipped static verifier once using the manifest-locked product path,
without supplying a separate path argument or rewriting canonical selectors.

## Frozen denominator

- Task: exact commit `4dc07e93…`, tree `a1d4db2f…`
- Skill: exact clean repository source prepared after commit `264dc191…`
- Runtime/model/effort: Codex / `gpt-5.6-luna` / high
- Cells: 1, concurrency 1, timeout 720 seconds
- Retry/replacement: none
- Tokens and steps: observed only

## Success and stop rules

Success requires 85/85, one product revision, one successful static closure,
zero failed static closures, zero user handoffs, and proof compliance true.
Unsupported evidence claims, task-contract drift, a second product edit, or a
red static closure fail the diagnostic. The cell is retained as observed and
must never be replayed. If the same missing-product-path root cause recurs after
the tested 1.9.707 repair, the frontier hard-pause rule applies.

This run is diagnostic only: no ranking, public superiority claim, confidence
interval, or 2.0 gate promotion is allowed.
