# Source-contract Reliability@3 replacement — 1.9.722 findings

## Result

The first preregistered Luna/high cell completed exactly once. It is a valid 81/85 result, up from the provider-free 75/85 baseline, but it did not satisfy the cell success contract. Wall time was 177,191 ms and observed usage was 558,228 tokens, including 477,952 cached input tokens.

The model made one product revision and preserved the sealed inventory. Responsive repair succeeded at desktop, 390px, 320px, and the 200% CSS-zoom surrogate: document width remained contained, with no clipped or overlapping controls and no protected-text geometry failures. Task contract, design grounding, state journey, responsive behavior, and evidence honesty passed.

The terminal static closure failed once. The product used the semantically equivalent minified selector `.decision>div` instead of the contract's exact `.decision > div`, and used `header>p` instead of `header > p`. More importantly, the header rule appeared before the later `.muted` class and therefore did not win the cascade. The independent evaluator found the original 4.47:1 header contrast failure at all four conditions. Accessibility remained red and the final score stopped at 81/85.

The model respected the terminal rule: it did not perform a second product edit, retry static closure, run browser proof after the red closure, or ask for a handoff. Proof compliance is nevertheless false because the only static closure failed.

## Decision

This is a preregistered contract hard-stop. The seed-vault and audio-archive cells remain unexecuted and are frozen; they must not be run as continuations, retries, or replacements. Reliability@3 is not established.

The failure exposes a pre-edit validation gap rather than a missing debt item. The sealed packet contained both responsive and accessibility obligations, but the model had no way to test its complete candidate bytes before consuming the one permitted product edit. The next local repair should support a provider-zero candidate file inside `.omd`, validate exact sealed selectors and effective contrast there, and only then permit the single product write and terminal static closure. Any transfer test must use entirely new task ids and bytes.

This diagnostic does not enter public ranking, establish model or skill superiority, or promote a 2.0 release gate.
