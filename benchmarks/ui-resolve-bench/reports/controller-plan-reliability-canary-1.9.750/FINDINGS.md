# Luna controller-plan Reliability result — 1.9.750

The first herbarium cell completed one product revision with exact candidate-to-final bytes and an objective score of 85/85. The controller-owned pre-edit browser plan worked as intended: all three prepared cells had a measured provider-zero plan on the exact `omd1750` connection before Luna saw the first task. Cursor was not called.

The preregistered proof gate still stopped the matrix. The official run evidence initially listed three reasons. A provider-free regression check found that two were classifier false positives: the shell-wrapped shipped runner ended in a quote, which the classifier did not accept as a command boundary. The committed classifier repair recognizes that exact command without changing the frozen run evidence.

One real failure remains. Luna added the required single-column `.decision` declaration, then restored a two-column declaration in a desktop media query. The final browser measurement correctly reported `all_registered_carriers_closed: false`: the decision target carrier was narrower than its context. The objective evaluator still returned 85/85, while the source-contract static check passed because it only required one matching exact-value declaration and ignored later conflicting declarations.

This is a deterministic contract gap, not a reason to replay the cell. The 1.9.750 root stays frozen. Photographic negative return and architectural drawing return remain provider/model-zero and can be revised before a fresh root. The next repair must make exact-value assertions universal across all matching declarations and make the `.decision` single-column requirement exact across viewports.
