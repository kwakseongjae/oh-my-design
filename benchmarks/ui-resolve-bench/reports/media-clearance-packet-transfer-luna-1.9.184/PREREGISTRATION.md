# Media-clearance packet Luna xhigh replication — preregistration

Status: **LOCKED before preparation and provider execution**.

This is the cross-model confirmation triggered by the completed Grok 1.9.183 quality pass and material wall-time/token interaction. It is a separate denominator, not a replacement or pooled extension of Grok.

- Task: exact `media-clearance-routing-v0.1` `0.1.0`
- Arms: exact previous canonical `omd-portable-jade` versus exact compact packet `omd-portable-reflow-packet-candidate`
- Provider scope: Codex runtime / `gpt-5.6-luna` / xhigh for both arms
- Matrix: 2 arms × 3 trials with the exact Grok balanced first-arm order
- Timeout: 900 seconds; globally serial; fixed 120-second inter-cell pacing
- At most one new cell per invocation; no retry, fallback, repair, replacement, resume after failure, or model substitution inside this root
- Attribution: Internal registered model result; reported separately from Grok

The Luna gate is packet UI-Resolved 3/3 and Reliability@3 100% with no paired loss and no function, accessibility, evidence, hierarchy, target-emphasis, protected-hook, atomic/dynamic/compact-scope, wall-time, or token regression. Any provider, attribution, or controller failure freezes this root. Promotion requires a non-negative packet direction on both Grok and Luna and remains held if either model regresses.

