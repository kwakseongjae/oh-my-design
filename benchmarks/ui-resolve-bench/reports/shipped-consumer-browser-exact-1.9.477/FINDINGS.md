# Shipped consumer-browser exact pin — 1.9.477

The shipped runner candidate is pinned at exact source commit `6bfdc7bc9f3bcb7d5089b5acb79b9e26b9372673`. Its skill tree, prose contract, static helper, browser runner, and focused artifact/distribution tests are hashed in `SUMMARY.final.json`.

The immutable competitor id is `omd-shipped-consumer-browser-candidate`, prepared from detached vendor directory `omd-1.9.476`. A benchmark cell using this id receives the runner as part of the installed skill rather than inheriting a local workspace script.

Provider calls: zero. Promotion: false. The next step is to lock a genuinely unseen task before any model sees it, then preregister the exact 1.9.474 control against this exact 1.9.476 candidate with the runner command as the sole browser proof mechanism.
