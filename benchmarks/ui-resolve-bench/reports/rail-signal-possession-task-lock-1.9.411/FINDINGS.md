# Rail-signal possession handback holdout — 1.9.411

This fresh repair task moves compound relationship preservation into railway signal possession handback. Six supplied signal assets retain eight exact test-certificate assignments; a separate four-window possession strip and a separate handback decision remain visible. Its identifiers, selectors, content, palette, fonts, state vocabulary, and evidence boundaries differ from prior holdouts.

The untouched baseline is 75/85 and unresolved. Task facts, controls, form states, DESIGN.md grounding, evidence honesty, desktop geometry, and keyboard traversal are green. The intended defects are narrow-screen and 200%-equivalent overflow, character-range fragmentation of atomic identifiers and the protected compound target, plus serious muted-text contrast at 4.11:1 on the canvas.

The task is now immutable for this experiment. No provider has seen it. The next comparison must use a fresh matrix root, the exact historical control and exact 1.9.409 candidate source, plus every admission, resume, timeout, browser, shared-socket, reflow, compound-atomic, character-range oracle, completion-loop, and observed-attempt pin.
