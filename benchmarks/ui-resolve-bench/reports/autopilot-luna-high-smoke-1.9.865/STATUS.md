# Autopilot Luna/high smoke 1.9.865

`stopped-preregistered` — do not resume or reuse this root.

The first Luna/high provider turn completed successfully and produced a project-owned `DESIGN.md` plus a working landing page. The controller then killed the four-viewport evaluator at its obsolete 180-second ceiling before `task-score-round-0.json` was written, so the exposed cell is not a valid terminal benchmark result.

A provider-free replay of the exact frozen workspace completed after that ceiling and produced a diagnostic score of 10/100. It is not substituted into the sealed run. The controller timeout is raised to 360 seconds and must be committed before any fresh epoch is preregistered.

Provider exposure: 1. Valid terminal cells: 0/3. Retry, replacement, fallback, and Cursor calls: 0.
