# Data import mapping holdout — contract findings

Status: **ACCEPTED as an unseen non-approval configuration holdout**.

## What is locked

- New task: `data-import-mapping-v0.1`
- Fictional product: FieldMerge
- Existing evaluator adapter: `onboarding-v1`
- Required journeys: three-way matching-key choice, reversible missing-key
  policy, invalid/valid mapping-name form
- Protected structure: three mapping rows and exact-one
  context→target→evidence→state→action roles
- Registered geometry: mobile, 320px, and 200% text geometry; all four
  viewports for decision hierarchy
- Network, dependencies, remote assets, unsupported claims, and proof
  structures remain forbidden

The task adds no evaluator branch and does not change score weights or
historical results. It changes the domain from onboarding, dashboards, pricing,
and approval screens to a data-configuration form.

## Untouched-starter diagnostic

The fresh raw-DESIGN workspace at
`/tmp/u19120-data-import-starter` scored **57/85**:

- task contract 25/25
- state journey 15/15
- accessibility 10/10
- evidence honesty 5/5
- responsive 2/5
- design grounding 0/25

This is the intended repair baseline, not an accepted model result. It proves
that all three behaviors, protected facts, keyboard traversal, axe checks, and
evidence gates are executable before provider use while the visible defects
remain measurable:

- starter tokens do not match the supplied DESIGN.md;
- current state is visually indistinguishable from evidence;
- `customer-update-july.csv` fragments at 320px and 200%;
- the mobile surface retains a decorative left accent border and weak footer
  wrapping.

The narrow screenshot was inspected directly. The failures are visible product
defects named in the task, not missing hooks, evaluator crashes, or impossible
constraints.

## Decision boundary

The next valid evidence is an exact current-OmD one-arm Reliability@3 run. A
3/3 objective ceiling with no repeated visible defect keeps the canonical skill
unchanged. A repeated objective or visual failure cluster may justify a bounded
experimental delta, but this task cannot be used as both training and final
validation evidence.

Provider calls: **0**.
