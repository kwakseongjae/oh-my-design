# Pre-edit release invariant candidate — 1.9.263

Status: **AUTHORED, NOT QUALITY-PROMOTED**

The rejected 1.9.252 candidate reached 85/85 once but repeated the same incomplete first edit twice. This bounded delta adds one compact pre-edit invariant with three required decisions only:

1. the failing normal-text selector and existing verified foreground token that will appear in the first diff;
2. the existing semantic comparison carrier and its named containment or exact relocation;
3. one prepared browser command that actually navigates the same consumer route.

The edit cannot begin with an empty value, and a static grep or browser session creation no longer counts as the outcome. The existing detailed ledgers remain evidence fields rather than new phases. The same change is mirrored into the historical decision-context copy, preserving one-rule isolation. No benchmark selector, task identifier, fixture value, color, component, or token was added.

Canonical skill size changed from 38,232 to 39,093 bytes (+861); words changed from 4,300 to 4,397 (+97).

Validation:

- skill-creator quick validation: pass
- omd:apply delivery contract: 9/9 green
- decision-context one-rule isolation and focused bench contracts: green
- TypeScript lint: green
- build: green
- full benchmark unit file: 72/74; the two red tests are the unchanged external Taste/UI UX Pro vendor directories being non-Git, not this delta

This is an authored hypothesis, not a quality claim. Next: exact commit pin followed by a fresh unseen topology; the transit timetable is not reused for promotion.
