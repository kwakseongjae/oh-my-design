# Grid battery dispatch holdout — 1.9.448

This fresh repair task transfers compound-parent and actual-zoom preservation into a grid battery dispatch workflow. Six supplied battery-rack identifiers retain eight exact inverter/grid-certificate assignments; a separate four-window dispatch strip and a separate energization decision remain visible. Its identifiers, facts, state vocabulary, interaction copy, evidence boundary, palette, and typography are unseen by the exact 1.9.447 source.

The untouched baseline is 75/85 and unresolved. Task facts, controls, form states, DESIGN.md grounding, evidence honesty, desktop geometry, target sizing, and keyboard traversal are green. Intended defects are 390px, 320px, and actual 200% overflow; character-range fragmentation of atomic identifiers and the protected compound target; the two-line `Grid operator review open` state at actual 200%; and serious muted-text contrast at 4.35:1 on the canvas.

The task is immutable for the next experiment. No provider has seen it. The comparison must use a fresh matrix root, exact historical control and exact 1.9.447 candidate source, runtime-bound model identity, immutable 390/1×, 320/1×, and 640/2× measurement conditions, one pre-browser static closure, and a helper-owned terminal browser marker.
