# Abyssal provider-sealed contract canary — 1.9.715

## Unresolved frontier gate

`verified-skill-lift` remains partial. This diagnostic cannot promote it. It
tests whether removing model authorship from the reflow manifest eliminates the
repeated structured-packet completeness failure on a fresh public task.

## Bounded hypothesis

Before provider execution, OmD will seal the exact source contract and merge all
acceptance CSS into structured declarations. Luna/high will consume one read-
only `source-packet`, complete one product revision, and close the shipped
static verifier exactly once without changing the immutable inventory.

## Frozen denominator

- Task: `abyssal-sediment-core-custody-v0.1`, provider/model exposure zero at lock
- Baseline: 75/85; contract, state, design, and evidence pass
- Skill: exact clean 1.9.713 provider-sealed implementation at `1f267b3c…`
- Runtime/model/effort: Codex / `gpt-5.6-luna` / high
- Cells: 1, concurrency 1, timeout 720 seconds
- Retry/replacement: none
- Tokens and steps: observed only

## Success and stop rules

Success requires 85/85, one product revision, one successful static closure,
zero failed static closures, zero user handoffs, and proof compliance true.
Artifact inventory drift, unsupported claims, a second product edit, or a red
static closure fail the diagnostic. The cell is retained and never replayed.

This run is diagnostic only: no ranking, public superiority claim, confidence
interval, or 2.0 gate promotion is allowed.
