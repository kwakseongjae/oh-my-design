# Luna state-routed council-first comparison — findings

The state-routed candidate reached 85/85 on all three fresh tasks. Current reached 85, 81, and 85. Paired candidate W/T/L is `1/2/0`; candidate Reliability@3 passed, paired objective loss stayed zero, every cell used one product revision, and all six proof/candidate-byte contracts passed without browser recovery.

The current slide run introduced a new muted caption at 4.26:1 contrast, leaving accessibility red. The paired state-routed run did not leave that regression and scored 85/85. This is a valid paired observation under the frozen Luna/high setup, but three tasks are too few for a general council-lift claim.

Across the full matrix, candidate summed wall time was 6.01% lower and reported tokens were 11.52% lower. The task pairs were heterogeneous: folio candidate was slower and larger, while slide and tape were faster. Cost and latency therefore remain descriptive axes, not the causal acceptance gate.

The preregistered overall comparison gate is false because it required Reliability@3 from both arms and current achieved only 2/3. Candidate-specific quality, no-loss, and unplanned-intervention-zero gates passed.

This closes the zero-question `PROPOSE_PLAN` path only. It does not prove live advisory debate, a necessary product-authority interview, or an external-evidence block. The next evidence must exercise those routed states without allowing the implementation model to continue behind the checkpoint.
