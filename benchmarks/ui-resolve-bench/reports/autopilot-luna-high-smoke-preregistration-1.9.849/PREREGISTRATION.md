# Autopilot Luna/high diagnostic smoke — provider-zero preregistration

Date: 2026-08-10

Execution status: **BLOCKED BEFORE PROVIDER**. This is a normative template, not a runnable plan or a model result.

## Exact contract

- Contract: `benchmarks/ui-resolve-bench/config/autopilot-luna-high-smoke-v0.1.json`
- Contract SHA-256: `f94e5b779c22501aaa3ca95e0e594532197a0f06eb0ac69e2eb6158cae3fc1a9`
- Frozen order: `neighborhood-library-landing` → `cold-chain-operations` → `clinic-visit-prep-locales`.
- Coverage intent: one expressive landing, one dense operational workflow, and one five-locale stateful surface. Selection occurred before any model result.
- Runtime intent: Codex `gpt-5.6-luna` / `high`, serial, one trial per task, 900-second cell timeout, one cell per checkpoint.
- Retry, replacement, fallback, model substitution, effort substitution, Cursor, and Claude Code budgets: zero.

The same blank starter is used for every task. No valid oracle, mutant, task-specific DESIGN.md, token set, component library, example, preselected reference, slash command, or harness setup may enter a provider workspace.

## Autonomy observations required

Each cell must retain exact prompt bytes, installed portable bundle, source commit, runtime catalog, question batches, unplanned interventions, council lanes, DESIGN.md proof, product proof, evaluator output, wall time, and fresh/cached/output/reasoning token coverage. Fully authorized prompts permit zero question batches and zero unplanned interventions.

## Provider-zero blockers

The current repository contains the new 2.0 source as uncommitted files. The source commit therefore cannot yet prove the exact portable bundle, evaluator, task authority, adapters, and blank starter. No runnable matrix/root/receipt is emitted from this template.

Before execution, a fresh plan must independently prove:

1. clean committed source containing every exact authority byte;
2. isolated immutable Codex auth and local model-catalog authority;
3. exact Luna/high profile and CLI binding;
4. exact installed portable bundle parity;
5. a named existing browser and provider-zero evaluator/runtime preflight; and
6. fresh workspaces with no oracle or mutant bytes.

## Claim boundary

Even a 3/3 result is diagnostic smoke evidence only. It cannot support best-skill, superiority, reliability, production readiness, or public one-shot claims. Same-prompt competitor qualification, 12×5 preview evidence, Luna/Terra/Sol transfer, and blind practitioner review remain separate gates.
