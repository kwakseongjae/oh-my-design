# Locale delivery-authority recovery — 1.9.24

Locked before workspace preparation or provider generation on 2026-07-23.

## Question

Does a bounded verification-authority and delivery-clock activation let the
same OmD locale skill stack finish the stopped five-locale task without
authoring a replacement verifier, while passing the frozen objective evaluator?

This is a fresh candidate-only process recovery for failed 1.9.23. It does not
reuse or evaluate the stopped candidate workspace and does not retroactively
change 1.9.23. A pass only unlocks a fresh repeated locale matrix.

## Frozen task and runtime

- Task/starter/evaluator basis: `07ac960`
- Recovery source/activation/runner basis: `c572cd7`
- Variant: `omd-locale-handoff`
- Runtime: Claude Code 2.1.217 or newer, recorded in the run
- Parent model/effort: exact `claude-opus-4-8` / `xhigh`
- Trial: one fresh candidate cell
- Timeout: 900 seconds
- Output root: `/tmp/u1924`
- Network, hooks, Agent tool, and third-party installers: disabled

The linguistic rules, five locale oracles, protected command and facts,
starter, prompt, DESIGN.md, viewports, and frozen evaluator are unchanged from
1.9.23. The only product delta is the candidate activation and its executable
delivery gate.

## Frozen recovery delta

Locale `VERIFY` means comparison of protected facts, locale terminology,
register, and repeated copy signals. It does not authorize verification
software.

- first product write: ≤450,000ms
- optional verification stop: ≤720,000ms
- final response begins before 810,000ms
- existing repository checks or one direct browser command only
- the direct browser command may not write a verification program
- blocked browser proof becomes unresolved and delivery continues
- authored `verify/check/probe` program, temporary shell program, CDP/browser
  automation, DOM shim, mock browser, or new test runner: zero

The matrix runner applies the first-write and replacement-verifier gates to the
explicit `locale-skill-stack` kind. The frozen external evaluator remains the
independent acceptance authority.

## Validity and stop policy

The cell is valid only when provider and child exit zero, no timeout occurs, a
final response and product diff exist, exact model attribution holds, the
frozen evaluator completes, and infrastructure/sandbox/cwd errors are zero.

Any auth, quota, model, process, timeout, final, delivery, replacement-verifier,
evaluator, or export failure stops immediately. There are no retries, resumes,
workspace reuse, or manual product edits.

## Recovery decision

`calibration_complete` requires:

- valid process and exact parent model
- first product write ≤450,000ms
- replacement verifier authored: false
- objective score 85/85
- all six critical gates pass
- every required/forbidden locale pattern passes
- protected command, 12, 3, and `DESIGN.md` preserved in all locales
- unsupported claims and proof structures: zero
- axe serious/critical: zero at all four viewports
- no unplanned intervention

Anything else is retained as `calibration_failed` or `inconclusive`. A pass
does not establish general locale lift or public frontier status.
