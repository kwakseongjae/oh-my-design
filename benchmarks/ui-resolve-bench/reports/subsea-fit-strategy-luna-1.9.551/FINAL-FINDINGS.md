# 1.9.554 — Subsea fit-strategy transfer frozen

The fit-strategy candidate improved the shipped UI from the control's 77/85 to 85/85 and was 0.7700× as slow, but used 1.1984× as many provider tokens. It correctly derived feasibility for eight rows, declared the intrinsically unfit valve and console identifiers as comparison-scroll, and used shared passive identifier carriers.

Proof still failed. The compound decision target was omitted from the pre-edit row inventory, so plan-close had no target row from which to require a distinct target-only comparison carrier. First terminal browser proof measured document overflow of 69px, 139px, and 264px across the three narrow conditions. The model then applied a second edit that produced the final 85/85 UI, but exactly-once static/browser closure could not be rerun and the artifact remained open.

The control independently failed protocol by invoking the browser runner directly through Python before using browser-harness. Both failures are retained. Candidate proof Reliability@3 can now reach at most 2/3, so four untouched cells are frozen. The next repair is provider-free decision-target inventory completeness, not another model run.
