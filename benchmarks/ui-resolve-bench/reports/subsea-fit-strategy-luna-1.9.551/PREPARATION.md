# 1.9.552 — Subsea fit-strategy matrix preparation

Six untouched workspaces were prepared from the locked matrix. All six share the same core prompt, prompt, starter, product tree, runtime, model, effort, and timeout; only the installed exact skill source differs by arm.

The control source is detached and clean at `28f96200…`; the candidate source is detached and clean at `84329948…`. Both retain aggregate carrier planning, the structured first-edit checklist, and the identical browser runner. The candidate alone derives measured fit-strategy feasibility and restricts shared comparison carriers to passive identifiers.

No provider was called. Runtime browser, CLI, and authentication preflight must pass before the first control cell runs.
