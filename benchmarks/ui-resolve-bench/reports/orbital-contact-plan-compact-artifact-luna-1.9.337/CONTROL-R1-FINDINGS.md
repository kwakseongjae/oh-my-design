# Control R1 checkpoint — 1.9.339

The first Luna/high control changed the product and received an objective 75/85, but it did not produce an admissible benchmark cell. The shared installed host policy remained blocked, so the preregistered infrastructure rule freezes the matrix before any candidate call.

This was not a missing browser connection. The run issued one browser-harness command. The failure was an artifact/host-state divergence: the model-authored compact artifact declared 34 registered rows while the installed state registered 33, then labelled the browser outcome unresolved because of a claimed daemon failure. The host correctly refused delivery. Two duplicate-static attempts were also blocked.

Observed spend is retained: 763,451 ms and 2,004,054 provider-reported tokens. The 75/85 score is diagnostic only and cannot enter a paired quality or efficiency claim. No retry is allowed, no candidate cell was called, and `/private/tmp/u19337` is frozen.

The methodological defect is narrower than the product defect: the current runner classifies every installed host-policy failure as infrastructure invalid. The replacement must distinguish an unavailable or inconsistent installation from a ready host that correctly rejects the system's own invalid artifact. The latter is a valid system failure. That repair must be committed, pinned, and preregistered on a fresh unseen task before another provider call.
