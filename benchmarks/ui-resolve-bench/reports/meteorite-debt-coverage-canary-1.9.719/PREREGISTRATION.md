# Meteorite debt-coverage canary — 1.9.719

This is the first transfer of source-contract 0.2 on a fresh fictional task. The exact task has no prior model exposure. Its provider-free raw baseline is 75/85 with responsive and accessibility as the complete false critical-gate set.

The contract is admitted only when the hash-locked baseline evidence and `critical_gate_debt_coverage` match exactly. The packet must include register, return strip, decision, parent containment, and contrast obligations before the single product edit. The sealed artifact remains provider-immutable.

Success requires 85/85, one product revision, one successful static closure, zero failed closures, zero user handoffs, proof compliance, and unchanged sealed inventory. The cell is diagnostic only, receives no retry or replacement, never enters ranking, and cannot promote a 2.0 gate by itself.

Provider execution is not authorized by this preregistration. Preparation and admission remain provider-zero; external transmission is a separate gate.
