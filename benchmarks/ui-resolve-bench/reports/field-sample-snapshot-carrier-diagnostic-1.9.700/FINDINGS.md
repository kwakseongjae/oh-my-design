# Field-sample snapshot and relationship-carrier diagnostic — 1.9.700

The exact clean `b5230a70` OmD skill was prepared and executed once with Luna/high. The run was valid and scored 81/85, but it was not UI-resolved because the deterministic accessibility gate found serious color-contrast failures. It consumed 689,553ms and 4,134,638 provider-reported tokens.

The 1.9.699 snapshot bootstrap transferred. `source-fallback-open` captured the unchanged product, persisted the snapshot, reconciled the artifact-only relationship manifest, and issued a valid opening stamp. The model then created distinct target and evidence carriers with the required marker, accessible name, and keyboard reachability.

The first product edit nevertheless omitted six relationship CSS obligations: local overflow, visible focus, and row nowrap for both target and evidence. `static-close` rejected all six and consumed the one allowed closure attempt. The model then made a second product edit that added the missing CSS. The final file therefore demonstrates that the prescribed relationship treatment was implementable, but the run remains noncompliant because the second edit occurred after a terminal failed closure and no successful closure exists.

This isolates a delivery defect rather than a missing verifier rule: the helper output buried the relationship declarations inside a long generic checklist instead of presenting a short first-edit patch contract. The next local patch will expose exact carrier attributes and CSS obligations as an ordered, ready-to-apply packet and explicitly stop after a failed static closure.

The same public task will not be executed again. It has already isolated the defect, is now exposed to the model, and this run's 4.13M-token cost makes another repetition disproportionate. The next validation must use local fixtures first and then a fresh task or multi-trial evidence plan. This diagnostic remains excluded from comparative rankings and does not change the 2.0 readiness snapshot.
