# Accessible atomic-fit static-latch exact pin — 1.9.454

The provider-free 1.9.453 repair is pinned to commit `f578e8a63dbff7dd9f9de6da8f4e201b7e35c8ee`. Future sandboxes must install the skill and deterministic reflow helper from this detached commit; a mutable working-tree copy is not eligible.

The pin includes the accessible compound-fit priority, the prohibition on passive protected-text scroll containers, the distinct comparison-carrier contract, and the task-wide static-to-terminal-browser acceptance sequence. No quality promotion is claimed. The next step is a fresh unseen task with different facts, carriers, state vocabulary, visual contract, and a coupled narrow/actual-zoom accessibility failure.
