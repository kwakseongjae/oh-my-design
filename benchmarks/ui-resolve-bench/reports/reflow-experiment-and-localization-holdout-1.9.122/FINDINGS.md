# Reflow experiment + localization holdout — findings

Status: **ACCEPTED; provider calls 0**.

## Bounded hypothesis

`omd-reflow-integrity-experimental` installs the exact same detached current
OmD skill as `omd-portable-jade`:

- source commit `f013dbd9f94a1e018f7cf8a4e500207fe982b00a`
- source path `skills/omd-apply`
- same Cursor adapter, install root, install directory, and declared name

The only experimental delta is one activation paragraph. It requires a final
320px/200% reflow closure for product-contract atomic identifiers and short
control/state text, forbids word breaking, hidden duplication, invented
abbreviation, and overflow, and keeps declared decision roles inside one final
context boundary.

Canonical `skills/omd-apply/SKILL.md` is unchanged.

## Unseen validation family

New task: `localization-bundle-handoff-v0.1`.

This task was written after the FieldMerge baseline was frozen but before any
experimental generation. It changes both domain and interaction family:

- localization artifact handoff instead of CSV mapping;
- `dashboard-v1` filter/disclosure/acknowledgement instead of
  choice/toggle/form;
- four locale artifacts and two evidence disclosures;
- exact-one target→evidence→state→action decision boundary;
- mobile, 320px, and 200% text geometry preregistered before generation.

The fixture supplies artifact names and source metadata but no translation
quality, reviewer conclusion, destination, or release outcome.

## Untouched-starter diagnostic

Fresh raw-DESIGN workspace:
`/tmp/u19123-localization-starter`.

- score 57/85
- task contract, all three state groups, accessibility, and evidence honesty:
  pass
- design grounding: intentionally fails
- responsive: intentionally fails

Visible registered failures include locale artifact fragmentation and
two-line `View source` controls at narrow/200% conditions, plus flat
state/evidence styling. The task is executable without provider assistance and
the failures are the product defects named in the prompt.

## Next decision

Prepare an exact current-control vs reflow-activation matrix on this unseen
task. Keep task, starter, DESIGN.md, runtime, model, effort, timeout, and
installed skill hash identical. Only the preregistered activation delta may
differ.

The experimental paragraph is eligible for canonical promotion only if it
improves repeated reflow integrity without regressing function, evidence,
accessibility, design tokens, or context containment. One task is still
insufficient for a public superiority claim.
