# R1 control — 1.9.444

The exact 1.9.409 control is a valid system failure at 81/85 after 522.453 seconds and 1,038,747 observed provider tokens. It preserved the task facts, states, evidence boundary, design tokens, desktop and 390px geometry, contrast, and the protected compound target.

At 320px, one control is clipped and keyboard traversal no longer visits every control with visible focus. `UI-Resolved` therefore remains false even though the compound parent itself no longer triggers the line-budget failure seen in earlier transfer tasks.

The run used one browser mechanism without recovery, but performed two duplicate static closures and one verification after browser readiness. It fails the preregistered zero-tolerance proof gate. No retry is allowed; the next eligible cell is the paced R1 candidate under the same runtime-bound Luna/high identity and named browser socket.
