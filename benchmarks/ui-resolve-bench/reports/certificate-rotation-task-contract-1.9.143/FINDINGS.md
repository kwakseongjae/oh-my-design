# Certificate-rotation holdout — findings

Status: **ACCEPTED; provider generation has not started**.

`certificate-rotation-plan-v0.1` was contract-locked after the ordered reflow
v6 commit and before any provider run. It uses the existing `onboarding-v1`
interaction adapter in a new infrastructure-rotation domain: three rollout
window choices, one reversible dual-operator toggle, an invalid/valid rotation
label journey, three ordered service/current/target certificate relationships,
and one visible decision boundary.

The untouched starter scores 79/85. Task contract, all state journeys,
accessibility, DESIGN grounding, evidence honesty, clipping, overlap, and
decision hierarchy are green. The intended critical failure is responsive text
geometry: residual percentage widths constrain the ordered sequence after the
mobile grid change, fragmenting service and certificate identifiers. At 320px
the inherited body type also makes `Require dual-operator confirmation` wrap
beside its control; 320px and 200% additionally expose horizontal overflow.
The narrow screenshot confirms the destructive reading order directly.

This is a valid unseen repair holdout. It is an ordered `<ol>` sequence rather
than a native table, definition manifest, or per-item card surface. It contains
no injected break opportunity or single-text scroller. Next, pin exact v6 from
`d971174` and compare it with exact previous canonical in a fresh 2 arms × 3
trials root with task, prompt, starter, DESIGN, runtime, model, effort, timeout,
and activation held equal.
