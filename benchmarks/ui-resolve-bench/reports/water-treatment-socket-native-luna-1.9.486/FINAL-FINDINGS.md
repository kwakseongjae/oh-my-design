# Water-treatment socket-native transfer — final 1.9.489

Both r1 arms produced valid 85/85 UIs. The socket-native candidate validated
the intended infrastructure repair: one shipped runner command attached to
`bench19366`, launched no browser, measured 390px, 320px, and actual 200%, and
preserved zero document/body overflow. The automatic gate therefore removed
all seven control-side infrastructure reasons.

The candidate still fails artifact proof on real measurements. Three expanded
rows remain unresolved. `operator-note-label` reports 1px reserve because the
runner subtracts intentional button padding before measuring safety space.
`decision-evidence` is multi-line prose incorrectly promoted to an atomic
one-line row. `form-status` is initially empty yet promoted without a visible
state for the terminal runner to measure. These are general measurement and
inventory boundaries, not water-domain exceptions.

Candidate wall time is 1.4435× control and token use is 1.0358×. Because proof
3/3 is impossible after the first candidate failure, four cells are frozen and
there is no promotion. Tokens-to-Target reaches at least 50,436,586 plus six
usage-unavailable cells. The next patch is provider-free: include intentional
carrier padding in reserve and restrict the atomic inventory to visible,
explicit one-line contracts before another exact pin and unseen transfer.
