# Water-treatment socket-native consumer-browser transfer — 1.9.486

This provider-zero plan isolates the socket-native repair on the unseen
drinking-water treatment task locked in 1.9.485. Exact 1.9.476 is the
endpoint-bound shipped-runner control; exact 1.9.483 is the candidate. Both
arms must invoke their installed shipped runner. The automatic gate reads the
resulting artifact, so a runner-shaped command without measured consumer proof
cannot pass.

Luna/high, Codex CLI 0.144.1, 900-second timeout, one-cell concurrency,
120-second pacing, no retry, prompt, starter, evaluator, named consumer
browser, and runtime identity are shared. Raw endpoint metadata may be absent;
the candidate must attach through the exact `BU_NAME` socket without launching
a browser. Required proof is one passed static closure, one measured browser
attempt, exact 390/320/actual-200 conditions, locked typography, at least 8 CSS
px inline reserve, no document/body overflow, and closed artifact state.

Attempt 30 starts from Tokens-to-Target 49,239,471 plus six
usage-unavailable cells. Task exposure is 0/0. Promotion requires candidate
UI-Resolved 3/3, artifact-backed proof 3/3, shipped-runner execution 3/3, no
paired objective loss, no serious/critical contrast, and mean wall-time and
provider-token ratios no greater than 1.1. If any requirement becomes
mathematically impossible, remaining cells freeze.
