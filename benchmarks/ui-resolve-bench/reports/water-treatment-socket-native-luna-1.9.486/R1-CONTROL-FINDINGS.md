# Water-treatment r1 endpoint-bound control — 1.9.488

The exact 1.9.476 control produced a valid 85/85 UI in 272.526 seconds using
588,027 provider tokens. Desktop, 390px, 320px, actual 200%, behavior,
contrast, evidence, and atomic text are all objectively green. Its execution
shape is also disciplined: one product edit, one static closure, one shipped
runner command, zero recovery, zero duplicate static checks, and zero
post-ready verification.

The artifact-aware proof gate nevertheless fails, correctly. Static closure
passed, but the endpoint-bound runner recorded zero browser attempts, no named
consumer attachment, and an open artifact. Seven explicit artifact reasons
replace the earlier false positive that inferred proof from command shape.
UI-Resolved and proof-compliant therefore remain separate outcomes.

Tokens-to-Target is now at least 49,827,498 plus six usage-unavailable cells.
No retry is permitted. The next scheduled cell is the exact socket-native
candidate after the frozen 120-second inter-cell pacing boundary.
