# Shipped consumer-browser runner — 1.9.476

OmD now ships `reflow-browser.py` with the apply skill to Claude Code, Codex, and OpenCode. The model no longer authors a browser program. It supplies the locked artifact, product path, and static helper path to one browser-harness command; the runner attaches to `BU_NAME`/`BU_CDP_URL`, forbids browser launch by construction, measures 390px, 320px, and actual 200% zoom, writes the exact connection identity, and invokes terminal finalization in the same process.

The runner measures every registered instance with character ranges, locked typography, carrier-relative inline reserve, cardinality, visibility, carrier binding, document/body overflow, and actual document zoom. It also requires the row selector's own rendered text or value to match the declared `longest_value`; broad containers and empty inputs can no longer masquerade as atomic rows. Infrastructure failures are separated from post-attachment measurement failures.

Two provider-free rehearsals used the frozen genomic candidate copy and the existing named consumer Chrome. The final rehearsal attached to `bench19366`, launched no browser, observed all three required conditions, and reported zero document/body overflow. It correctly refused readiness: the old model-authored artifact registered a broad library card as an atomic row, an empty input as an external-label row, and several rows without the required reserve; the real evidence summary also remained two lines at 320px and actual 200%.

Python compilation, TypeScript lint, and focused skill/helper/install tests passed 73/73. Provider calls: zero. Promotion remains false until an exact pin transfers on a genuinely unseen task.
