# Genomic r1 control — 1.9.472

The exact 1.9.460 control reached 81/85. Desktop and 390px were green, contrast and behavior were green, and every viewport had zero document overflow. The evidence summary `5 libraries · 7 read sets · 3 instrument lanes` still wrapped to two lines at 320px and actual 200%, so the UI is not resolved. Runtime identity matched Luna/high through the isolated execution home. The cell completed in 349.379 seconds with 772,692 observed provider tokens.

The proof trace failed for the newly explicit reason this experiment targets: after one shipped static closure, the control opened an independent Playwright Chromium inside its browser-harness command. The host classifier recorded one browser recovery and rejected proof compliance. There were no duplicate static closures or commands after readiness.

The paired candidate must attach to the exact named consumer browser, preserve the pre-edit typography role, retain at least 8 CSS px measured fit reserve, and close the remaining evidence-summary wrap without introducing document overflow. Because the control is 81/85, the candidate can still satisfy the no-paired-objective-loss gate by reaching at least 81; full promotion still requires candidate 85/85 and proof compliance in all three trials.
