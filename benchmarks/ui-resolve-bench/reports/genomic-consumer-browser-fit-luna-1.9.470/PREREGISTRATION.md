# Genomic consumer-browser fit transfer — 1.9.470

This provider-zero plan tests whether the exact 1.9.467 consumer-browser repair transfers to the unseen genomic sequencing run-release task locked in 1.9.469. The exact 1.9.460 deterministic static-closure source is the control; the exact 1.9.467 existing-CDP, typography-lock, fit-reserve, and document-overflow source is the candidate.

Luna/high, Codex CLI 0.144.1, 900-second timeout, one-cell concurrency, 120-second pacing, task prompt, product starter, named consumer browser, isolated execution home, objective evaluator, and runtime-bound model identity are shared. The sole arm delta is the installed skill source. The actual-zoom condition is 640px with document zoom 2; a 640px viewport at zoom 1 or an independently launched browser is invalid proof.

Attempt 28 starts from Tokens-to-Target 46,322,200 plus six usage-unavailable cells. This task has zero prior provider exposure. Promotion requires candidate UI-Resolved 3/3, proof compliance 3/3, exact named consumer attachment, no typography shrink, at least 8 CSS px fit reserve, no document/body overflow, no paired objective losses, no serious/critical contrast, and mean wall-time and provider-token ratios no greater than 1.1. No retry is permitted; a mathematically impossible 3/3 stops remaining cells.
