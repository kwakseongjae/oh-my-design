# Council state-routing preparation — 1.9.781

The benchmark adapter now materializes the deterministic decision ledger, intake handoff, and minimum context plan before provider execution. The candidate may edit only when the routed action is `resume_master`; any interview or external-evidence state fails closed rather than being silently converted into implementation work.

All three locked tasks prepared as `PROPOSE_PLAN → resume_master`, with zero registered questions, two typed deferred decisions, zero advisory lane calls, and no provider or Cursor execution. This proves preparation behavior only. It does not yet show UI quality, latency, token reduction, or general council lift.

The next experiment can therefore hold every product and runtime input equal while changing only the candidate's state-routed council-first context.
