# Terminal runner Reliability@3 task lock — 1.9.746

Three provider-unexposed tasks are locked for the first cross-task test of the shipped terminal browser runner: microscopy slide custody, manuscript folio transfer, and geology section loan. None replays the oceanographic task used in 1.9.744.

Each raw surface scores 75/85. Responsive and accessibility are the only red critical gates; task behavior, design grounding, state journey, and evidence honesty are green. The accessibility failures are limited to the intended `.guidance-copy` and `footer` contrast debt.

The next experiment uses the same committed OmD skill, Codex runtime, Luna/high effort, 720-second ceiling, fixed task order, concurrency one, and no retry or replacement. A cell counts only when it reaches 85/85, resolves every critical gate, and satisfies proof compliance through `scripts/reflow-browser-runner.sh`. Any lifecycle failure freezes the remaining cells.

This lock has zero provider calls and zero model exposure. Reliability evidence requires 3/3 completed cells; it is diagnostic evidence for the terminal runner and council-on product path, not a public model-ranking claim.
