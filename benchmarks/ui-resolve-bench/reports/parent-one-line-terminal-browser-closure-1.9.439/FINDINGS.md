# Parent one-line and terminal browser closure — 1.9.439

This provider-free repair closes the two defects observed in the frozen maritime pair. An earlier Atomic rows rule still allowed a protected compound parent to wrap between child tokens, contradicting the later parent-one-line rule. The skill now makes the parent contract dominant everywhere: child spans remain semantic measurement aids, not permission to wrap, stack, or change the parent into a column.

The reflow artifact now records `line_contract` explicitly. Compound atomic values must use `parent-one-line`; simple atomic values use `single-token`. The deterministic lock helper rejects missing or incompatible contracts and includes the contract in the immutable inventory digest.

Browser measurement is now terminal. The single prepared `browser-harness` Python process must measure, write the artifact, invoke the existing finalize helper, and print one delivery marker. After that command returns, artifact reads and separate helper commands are forbidden. This removes the exact two post-ready reads observed in 1.9.438.

Provider calls are zero. Focused contract, artifact, proof policy, proof trace, and bounded-experiment tests are 72/72 green; TypeScript and diff checks are green. Promotion remains false until an exact pin and a fresh unseen transfer task validate the repair.
