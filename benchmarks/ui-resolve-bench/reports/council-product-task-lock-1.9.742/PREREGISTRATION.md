# 1.9.742 — fresh council product-task lock

## Purpose

Freeze a new, provider-unexposed product task before comparing the same Grok
4.5 High runtime with the maintenance-aware council gate off and on.

## Task

`oceanographic-cast-custody-v0.1` is an existing static `index.html` repair.
Its raw DESIGN.md baseline is 75/85. Task contract, state journey, design
grounding, and evidence honesty are green; responsive and accessibility are the
only red gates.

The task preserves five cast identifiers, seven carrier identifiers and their
assignments, four laboratory windows, three views, one note toggle, one form,
and the supplied decision evidence. Eight unsupported outcome claims remain
forbidden.

## Next experiment

Use the exact task bytes in a two-cell, no-retry Cursor/Grok 4.5 High run:

1. current `omd:apply` without a council packet;
2. the same `omd:apply` with a provider-sealed zero-dispatch maintenance ledger.

Compare objective score, critical gates, exact-route proof, evidence honesty,
wall time, and reported token usage. This task lock contains no provider or
model execution.
