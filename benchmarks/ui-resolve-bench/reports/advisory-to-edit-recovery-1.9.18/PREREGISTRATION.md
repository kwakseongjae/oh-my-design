# Advisory-to-edit recovery — 1.9.18

Locked before workspace preparation or provider generation on 2026-07-23.

## Question

Can the exact Opus repair harness turn two bounded specialist advisories into
one real, contract-safe targeted product edit within 90 seconds of the last
advisory, while preserving the Pricing task, semantic-color safety,
verification authority, and the existing 900-second delivery envelope?

This is a fresh single-cell process recovery for the 1.9.17 preregistered stop.
It does not retry, resume, replace, evaluate, or alter `pricing-t2-harness`,
cannot enter the 1.9.17 denominator, and cannot support quality, efficiency,
reliability, Pareto, model, harness, or frontier claims.

## Frozen cell

- Family/status: Harness Track delivery recovery / Internal
- Task/contract: `pricing-conversion-v0.1` / `0.3.0`
- Variant/system: `omd-repair-harness` /
  `omd-repair-harness-first-safe-edit`
- Runtime: Claude Code 2.1.217 or newer, recorded
- Parent model/effort: exact `claude-opus-4-8` / `xhigh`
- Required specialists: `omd-ux-writer` and `omd-ux-engineer`, each exactly
  once with requested model `opus`
- Timeout: 900 seconds
- First product write: at or before 450,000ms
- Last required advisory result to first product write: 0–90,000ms
- First product transaction: non-no-op targeted `Edit` to a changed product file
- Product/activation contract basis: `a71a122`
- Advisory timing executor basis: `a861e2d`
- Targeted transaction executor basis: `d3bf7a4`
- Output root: `/tmp/u1918`
- Schedule: `RUN-MATRIX.json`

## Frozen delivery delta

Bounded repair advisories receive the same immutable protected and semantic-
color ledgers, one or two risk questions, and no general audit. Each returns at
most three ranked findings in roughly 300 words and begins with one
`first_safe_edit`: an exact existing target, evidence, an acceptance-relevant
smallest change, `protected_contract_effect: none`, and one acceptance check.

After the last required advisory result, the parent performs one accepted
`first_safe_edit` before any ledger recap, advisory synthesis, planning prose,
whole-file reread, or second analysis pass. If an existing snippet can be
patched safely, a whole-file `Write` is not the first product transaction. The
first edit must advance the original acceptance criteria. Whitespace, comments,
timestamps, and same-value replacements do not count.

The semantic-color and verification-authority boundaries remain unchanged:
unmeasured accent text fails closed to a text-role label plus adjacent non-text
accent, and no new verification program, DOM shim, mock browser, temporary
shell file, or CDP/browser automation may be authored.

## Acceptance

The recovery passes only if all conditions hold:

1. provider and child exit zero, no timeout, exact model observed, and final
   response present;
2. frozen evaluator returns 85/85, UI-Resolved, all six critical gates, and
   Evidence & Unknown pass;
3. axe reports zero serious/critical violations at all four viewports;
4. exactly one call to each required specialist requests `model: opus`, with
   zero Agent errors;
5. first product write is at or before 450,000ms;
6. the last required advisory result precedes the first product write by no
   more than 90,000ms;
7. the first changed-product transaction is a non-no-op targeted `Edit`, and
   manual event review confirms it is acceptance-relevant rather than clock
   manipulation;
8. event trace and final tree contain no authored verification program or
   replacement verifier;
9. infrastructure/sandbox/cwd errors are zero, only allowed product files
   change, and no unplanned intervention occurs.

Any process/auth/quota/model/timeout/Agent/delivery/transaction/authority/
evaluator failure stops and fails the experiment. There is one cell and no
retry. Only a full pass may unlock a fresh 1.9.19 replacement matrix. The
1.9.17 result remains failed regardless of this outcome.
