# Advisory-to-edit recovery — 1.9.18 findings

Executed once on 2026-07-23 under the frozen preregistration. The fresh
`/tmp/u1918` cell completed without retry or intervention.

## Disposition

Mark 1.9.18 calibration complete. Unlock a fresh 1.9.19 repeated replacement
matrix; do not reopen or reinterpret 1.9.17.

- exact `claude-opus-4-8` / xhigh, Claude Code 2.1.217
- provider and child exit zero; final response present
- 85/85, UI-Resolved, all six critical gates
- Evidence & Unknown pass
- axe serious/critical 0 at desktop, mobile, 320px, and 200% zoom surrogate
- required Opus specialist calls 2/2; Agent errors 0
- first product write 247,569ms (gate 450,000ms)
- last required advisory result 237,043ms
- last-advisory to first-write 10,526ms (gate 90,000ms)
- first product transaction: targeted non-no-op `Edit` on `index.html`
- last product write 498,016ms; final response 550,607ms
- uncached tokens 167,299
- tool/recoverable/infrastructure/sandbox/cwd errors 0
- replacement verifier 0; product diff limited to `index.html`

## First transaction audit

The first changed-product transaction appended `display: inline-flex`, center
alignment, and `text-decoration: none` to the existing `.button` rule. The
starter's plan and navigation CTAs are inline anchors, so their declared 44px
`min-height` did not reliably apply. This edit made the mobile touch-target
contract real without changing any hook, control count, state, fact, or token.
It was therefore acceptance-relevant, contract-neutral, and not clock
manipulation.

Both specialists began with a `first_safe_edit`; the writer returned about 386
words and the engineer about 364 words, each with at most three findings and no
file write. The engineer result completed at 217,759ms and the writer result at
237,043ms. The parent emitted only a short action receipt and called targeted
`Edit` 10,526ms later. It did not reread the whole file or produce a second
ledger/advisory synthesis first.

## Quality and authority

The final page preserved every protected price, plan, hook, FAQ, billing, and
email state. Signal orange remained a non-text blocked-status dot on white with
an adjacent ink label; it was not promoted to meaningful text or used on the
failing paper surface. The frozen external evaluator independently exercised
all interactions, viewports, keyboard traversal, geometry, and axe checks.

The provider correctly reported its own live browser proof as unresolved
instead of authoring a replacement verifier. This is compatible with the
external benchmark evaluator's independent proof and keeps generation authority
separate from scoring authority.

## Process comparison, not a lift claim

Against the retained stopped 1.9.17 Pricing harness process trace, this fresh
recovery reduced first-write latency by 263,079ms (51.5%), advisory-to-write
latency by 218,011ms (95.4%), wall time by 110,534ms (16.7%), and uncached
tokens by 4,141 (2.4%). The trials are not a paired quality experiment and the
stopped artifact was never evaluated, so these deltas establish only that the
bounded scheduling failure path was observed and closed once.

One successful recovery cannot establish efficiency, reliability, Pareto
non-dominance, or frontier superiority. Those claims remain blocked on the
fresh preregistered 1.9.19 repeated matrix.
