# Wafer acceptance-debt transfer preregistration — 1.9.521

This provider-free plan compares exact 1.9.511 selector-provenance control `b0aa9d5a…` with exact 1.9.518 acceptance-debt and fit-margin candidate `8d647987…` on the unseen semiconductor wafer-lot task locked at `d6f58415…`.

The runtime is Codex with Luna/high. Reliability uses three paired trials in balanced order, a 900-second timeout, concurrency one, fixed 120-second inter-cell pacing, and no primary retry. Every provider-reported input, cached input, output, and reasoning-output token counts toward Tokens-to-Target.

Both arms must use the shipped browser runner through the exact named consumer and close 390px, 320px, and actual 200% reflow. The candidate must additionally register every known baseline responsive/accessibility failure as machine-readable acceptance debt before editing, bind debt guardrails to the static closure manifest, bind browser debts to pre-edit rows, plan 16px fit reserve, and independently measure the unchanged 8px pass threshold.

Promotion requires candidate UI-Resolved, proof-compliant, shipped-runner 3/3, zero unresolved acceptance debt, no paired objective loss, no serious/critical contrast issue, and mean wall-time and token ratios no greater than 1.1. This preregistration calls no provider and promotes no result.
