# R1 control — 1.9.437

The exact 1.9.409 control is a valid system failure at 81/85 after 367.461 seconds and 1,041,430 observed provider tokens. It preserved the contract, interactions, evidence boundary, visual tokens, desktop and 390px geometry, accessibility, keyboard traversal, and serious-contrast gate.

The protected compound target still spans multiple character-range lines at 320px and the 200%-equivalent viewport, so `UI-Resolved` remains false. Browser proof was attempted once, but the run first probed for a browser executable, repeated static closure twice, and verified after browser readiness. The preregistered proof gate therefore fails all three zero-tolerance limits.

No retry is allowed. The next eligible cell is the paced R1 candidate under the same runtime-bound Luna/high identity and named browser socket.
