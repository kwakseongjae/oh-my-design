# Final findings — maritime atomic browser-proof transfer 1.9.438

The experiment is frozen after the first pair. Both cells were valid 81/85 system failures with the same unresolved compound parent line at 320px and the 200%-equivalent viewport. Because promotion required candidate UI-Resolved 3/3, the first candidate failure caps reliability at 2/3; the remaining four provider calls cannot change the decision and are skipped.

Runtime attribution is accepted. Both runs recorded the same Luna profile from isolated Codex 0.144.1 execution homes. The candidate also removed the control's browser discovery/recovery probe and reached the preregistered `browser-harness` Python-stdin mechanism in one browser command.

Two defects remain. The candidate split the protected compound target into atomic children, then stacked those children at the narrow breakpoint; the parent therefore occupied multiple character-range lines despite every child remaining intact. After browser readiness it also read the closure artifact twice, producing two duplicate static closures and two post-ready verifications. The candidate was 1.29× slower and used 1.106× the tokens of control, exceeding both 1.1 efficiency limits.

The next patch is provider-free: make the parent one-line contract override child stacking and make the browser measurement itself the terminal closure, so no artifact introspection follows readiness. Tokens-to-Target is now at least 39,511,114 plus six usage-unavailable cells. Quality promotion remains false.
