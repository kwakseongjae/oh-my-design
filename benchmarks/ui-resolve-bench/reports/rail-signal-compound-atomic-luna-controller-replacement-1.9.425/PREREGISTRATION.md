# Rail-signal compound-atomic controller replacement — 1.9.425

This is the zero-provider replacement for the frozen 1.9.422 matrix. It preserves the exact control `3a414a0a…`, candidate `1480d7cc…`, task, prompt, Luna/high setting, balanced C/N–N/C–C/N order, 900-second timeout, concurrency one, 120-second pacing, no retry, exact named browser, and all objective/proof/efficiency gates.

The only infrastructure correction is runtime identity: the selected Luna model profile is pinned as `0efd579e…` under exact controller source `e473a2d9…`; the volatile whole-cache SHA is provenance only. Installed host enforcement remains unclaimed because Luna is `code_mode_only`. Post-run browser and proof behavior remain measured promotion gates.

Attempt order is 21. Prior observed Tokens-to-Target remains at least 34,208,487 plus six usage-unavailable cells. Rail-signal exposure remains control 2, candidate 0 because 1.9.422 executed no cells. Earlier roots are frozen and may not be reused.
