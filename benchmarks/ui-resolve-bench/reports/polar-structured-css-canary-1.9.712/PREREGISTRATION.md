# Polar structured CSS obligation canary — 1.9.712

## Unresolved frontier gate

`verified-skill-lift` remains partial. This diagnostic does not promote it; it
checks whether the exact 1.9.710 structured CSS obligation packet transfers on
a fresh public development task after the volcanic semantic-value mismatch.

## Bounded hypothesis

When browser measurement is unavailable, the exact 1.9.710 `omd:apply` skill
will expose `canonical_acceptance_css_source` and structured
`required_css_declarations` before the first product edit. Luna/high will
resolve the 75/85 polar starter in one product revision, preserve semantically
valid flexible grid values under `any-value`, and close the shipped static
verifier exactly once.

## Frozen denominator

- Task: exact commit `2a3749ad…`, tree `22c26500…`
- Skill: exact clean prepared source at `0fd0f1f2…`; structured CSS repair
  introduced by `f10983ce…`
- Runtime/model/effort: Codex / `gpt-5.6-luna` / high
- Cells: 1, concurrency 1, timeout 720 seconds
- Retry/replacement: none
- Tokens and steps: observed only

## Success and stop rules

Success requires 85/85, one product revision, one successful static closure,
zero failed static closures, zero user handoffs, and proof compliance true.
Unsupported evidence claims, task-contract drift, a second product edit, or a
red static closure fail the diagnostic. The cell is retained as observed and
must never be replayed. If the same structured-packet completeness root cause
recurs after the tested 1.9.710 repair, the frontier hard-pause rule applies.

This run is diagnostic only: no ranking, public superiority claim, confidence
interval, or 2.0 gate promotion is allowed.
