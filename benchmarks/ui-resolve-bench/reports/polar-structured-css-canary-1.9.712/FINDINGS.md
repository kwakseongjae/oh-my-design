# Polar structured CSS obligation canary — 1.9.712

## Result

The fresh Luna/high cell completed valid at **63/85** with one product revision,
zero user handoffs, and no unsupported evidence claims. It failed task-contract,
responsive, and accessibility critical gates. Static closure failed once with
`immutable inventory hash changed`; no second product edit, static retry, or
browser retry followed. Wall time was 379,176 ms and provider usage was
1,518,861 tokens.

The exact 1.9.710 transfer hypothesis did not hold. The model-authored artifact
kept responsive CSS duties as generic required literals and exposed an empty
`required_css_declarations` array, so `canonical_acceptance_css_source` was
`null`. It also registered a global forbidden `overflow-x:auto|scroll` pattern
while the fallback relationship contract required local `overflow-x:auto`
carriers. After the helper locked the inventory, the model removed that
contradictory forbidden pattern; the immutable digest then correctly rejected
static closure.

## Decision

This is a recurrence of the structured-packet completeness class after the
1.9.710 repair: the supposedly structured CSS obligation was absent from the
short packet, and the fallback opening still emitted an internally conflicting
contract. Per the preregistered repeated-root rule, the frontier lane enters a
hard pause. The polar task must never be replayed, and no replacement model or
fresh task may be launched from this lane without an explicit scope decision.

The result is diagnostic-only and does not alter public ranking or the 2.0
frontier gate.
