# All-carrier-set bounded candidate — 1.9.286

Status: **AUTHORED, NOT QUALITY-PROMOTED**

The rejected 1.9.274 conjunctive candidate won all three paired objective
comparisons and reduced mean wall time and provider-reported tokens, but none
of its three trials reached UI-Resolved. Each trial repaired the primary
relationship carrier while leaving an adjacent decision target or state
outside the registered one-line reflow closure.

This bounded delta changes the existing singular `comparison_carrier` release
invariant into `comparison_carrier_set`. Before the first edit, the set must
contain every protected or named relationship scope that owns registered
atomic text or relates it to target, identifier, evidence, state, or control
label content. A representative diagram or page-level overflow result cannot
stand in for an adjacent relationship scope.

The existing reflow work packet now records carrier-to-row bindings and a
concrete result for each carrier at 390px, 320px, and actual 200% zoom/reflow.
Static closure requires `all_registered_carriers_closed: true` and
`unresolved_carriers: 0`. No phase, script, task string, benchmark selector,
fixture value, component, token, CSS recipe, or scoring branch was added.

Canonical skill size changed from 39,610 to 40,956 bytes (+1,346); words
changed from 4,460 to 4,587 (+127). The same carrier-set delta is mirrored into
the historical decision-context experimental copy; its separate hierarchy
closure remains intact.

Validation:

- skill-creator quick validation: canonical and experimental copy pass
- omd:apply delivery contract: 9/9 green
- install-skills regression: 40/40 green
- TypeScript lint: green
- build: green
- diff check: green

This is a general candidate hypothesis, not a quality claim. Next: exact
source pin, then a fresh unseen topology and preregistered Reliability@3
comparison against the exact 1.9.274 control. The completed electrical
single-line task remains provenance-only and cannot promote this candidate.
