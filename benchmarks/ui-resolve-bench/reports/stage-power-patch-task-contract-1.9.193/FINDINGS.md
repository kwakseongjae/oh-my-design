# Stage power patch task contract — 1.9.193

## Outcome

Locked the unseen `stage-power-patch-routing-v0.1` task before provider generation. The fictional stage-power domain uses a node-link patch-bay schematic with source circuit, visible route trace, destination outlet, and load. It is structurally distinct from the existing table, ordinal manifest, master-detail split pane, and resource-time schedule families.

## Contract

- three circuit-bank choices with exact initial, selected, and restored state
- reversible isolation toggle with exact `aria-pressed` behavior
- invalid form focus and valid `Matinee power handoff` status journey
- four protected patch paths preserving circuit → outlet → load relationships
- exact context, target, evidence, state, and action boundary
- 390px, 320px, and 200% atomic, compact-copy, overflow, clipping, and hierarchy gates
- no invented energization, approval, electrical safety, capacity, or availability outcomes

## Provider-free baseline

The untouched starter at `/tmp/u19193-stage-power-starter` scored 79/85. Function, state journey, accessibility, DESIGN.md grounding, evidence honesty, decision hierarchy, page overflow, control clipping, and overlap are green. Responsive text geometry alone is red at 390px, 320px, and 200%: the retained four-track patch schematic fragments circuit, outlet, and load identifiers and wraps selected target, evidence, dynamic state, and compact control copy.

The 320px screenshot confirms the intended defect without a hidden product failure: all routes and controls remain visible and operable, but the spatial desktop tracks squeeze atomic values into two or three lines. Provider calls: 0.

## Verification

- task contract: `pass`
- focused task test: `1/1 pass`
- TypeScript lint: `pass`
- diff check: `pass`

## Next

Preregister exact `omd-portable-proof-budget-candidate` versus `omd-portable-proof-close-latch-candidate` as Cursor/Grok 4.5 High 2×3. Keep task, prompt, starter, DESIGN.md, activation, runtime, model, effort, timeout, evaluator, serial order, fixed 120-second pacing, and no-retry policy equal. Require candidate quality 3/3 with zero paired loss and command traces of browser recovery `0`, duplicate static closure `0`, and verification-after-ready `0`.
