# Semantic color safety recovery — 1.9.16 findings

Executed once on 2026-07-23 under the frozen preregistration. The workspace at
`/tmp/u1916` is retained and was not retried.

## Disposition

Mark 1.9.16 calibration complete.

- exact `claude-opus-4-8` / xhigh, Claude Code 2.1.217
- provider and child exit zero; final response present
- frozen evaluator 85/85, UI-Resolved, all six critical gates, and Evidence &
  Unknown pass
- axe serious/critical violations 0 at desktop, 390px, 320px, and 200% zoom
- first/last product write 360,838ms / 462,269ms; wall 520,232ms
- uncached tokens 155,312
- exactly one Opus-requested call to each required specialist; Agent errors 0
- one recoverable optional renderer error; infrastructure/sandbox/cwd errors 0
- one allowed product file changed: `index.html`
- replacement verifier and authored verification program count 0

## Semantic-color behavior

Before the first edit, the parent measured and locked a semantic color ledger.
The engineer received that immutable ledger and an explicit first risk question
covering normal-text, large-text, and non-text roles for every planned state,
status, focus, boundary, and release-summary pair.

The run measured `signal-orange` `#E7683D` as 3.26:1 on white and 2.99:1 on
paper. It therefore used orange only as an 8–10px dot on white, adjacent to ink
labels such as “Recommended” and “Blocked checks.” It did not render orange
meaningful text, invent a darker substitute, or rely on color alone.

The same ledger exposed `line` `#D9D8D1` at 1.43:1 when used as a control
boundary. The implementation kept `line` for separators and used the declared
`muted` token for input, secondary-button, and billing-track boundaries. This
fixed a second real accessibility risk without adding a token.

The frozen evaluator independently confirmed zero serious/critical axe findings
and passed keyboard traversal, named controls, labelled input, image-alt,
language, interactions, geometry, design grounding, and evidence honesty.

## Verification authority

The parent made one headless Chrome attempt. The environment blocked its profile
directory with ProcessSingleton/Crashpad output, which the runner recorded as
one recoverable tool error. The parent did not retry a variant or author a DOM
shim, CDP script, shell file, or replacement verifier. It reported live browser
proof as unresolved; the external frozen evaluator supplied independent browser
evidence afterward.

## Decision

The semantic-color authority patch recovered the exact Pricing critical gate
that failed in 1.9.15 while preserving delivery and verification authority. The
single cell does not retroactively pass 1.9.15 and cannot establish harness
efficiency or superiority. It may unlock a fresh preregistered 1.9.17 repeated
matrix in new workspaces.
