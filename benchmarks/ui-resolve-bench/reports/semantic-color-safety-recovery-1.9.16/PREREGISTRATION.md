# Semantic color safety recovery — 1.9.16

Locked before workspace preparation or provider generation on 2026-07-23.

## Question

Can the exact Opus repair harness preserve the Pricing contract while
fail-closing an unmeasured semantic accent away from meaningful normal text,
without inventing a substitute color, losing brand expression, exceeding the
delivery budget, or violating verification authority?

This is a fresh single-cell recovery for the Pricing Reliability@3 loss in
1.9.15. It does not retry, replace, or alter `pricing-t3-harness`, cannot enter
the 1.9.15 denominator, and cannot support quality, efficiency, reliability,
Pareto, model, harness, or frontier claims.

## Frozen cell

- Family/status: Harness Track product-contract recovery / Internal
- Task/contract: `pricing-conversion-v0.1` / `0.3.0`
- Variant/system: `omd-repair-harness` /
  `omd-repair-harness-semantic-color-safety`
- Runtime: Claude Code 2.1.217 or newer, recorded
- Parent model/effort: exact `claude-opus-4-8` / `xhigh`
- Required specialists: `omd-ux-writer` and `omd-ux-engineer`, each exactly
  once with requested model `opus`
- Timeout: 900 seconds
- First product write: at or before 450,000ms
- Product/activation contract basis: `5e8379b`
- Fail-closed executor basis: `eb3a300`
- Renderer observability basis: `ad3c3b6`
- Output root: `/tmp/u1916`
- Schedule: `RUN-MATRIX.json`

## Frozen semantic-color delta

Before editing, the parent locks a `semantic_color_ledger` for current and
planned meaningful foreground/background pairs. Every pair records token,
surface, content type, and measured or unresolved proof. An unmeasured
accent-on-surface pair may not ship as meaningful normal text. The text stays
in the DESIGN.md ink/text-role token while the accent is preserved as an
adjacent non-text dot, bar, ring, or icon. Color is not the only state cue and
no darker substitute is invented.

When state/status/accent tokens exist, one engineer risk question must classify
the planned pairs and identify unmeasured normal-text use. If a new pair is
introduced after advice, the same ink-label plus non-text-accent default applies.
Reporting contrast as unresolved does not authorize shipping the risky pair.

The 1.9.14 verification-authority boundary remains unchanged: no authored
`verify.*`, `verifier.*`, `check.*`, `probe.*`, temporary shell file,
CDP/browser automation, DOM shim, mock browser, or new test runner.

## Acceptance

The recovery passes only if all conditions hold:

1. provider and child exit zero, no timeout, exact model observed, and final
   response present;
2. frozen evaluator returns 85/85, UI-Resolved, all six critical gates, and
   Evidence & Unknown pass;
3. axe reports zero serious/critical violations, including `color-contrast`;
4. exactly one call to each required specialist requests `model: opus`, with
   zero Agent errors;
5. first product write is at or before 450,000ms;
6. event trace and final tree contain no authored verification program, DOM
   shim, mock browser, or replacement verifier;
7. infrastructure/sandbox/cwd errors are zero and only allowed product files
   change; and
8. no unplanned intervention occurs.

Any process/auth/quota/model/timeout/Agent/delivery/authority/evaluator failure
stops and fails the experiment. There is one cell and no retry. Only a full
pass may unlock a fresh repeated replacement matrix. The 1.9.15 result remains
failed regardless of this outcome.
