# Reliability task lock — 1.9.727

This set preserves the exact conservation and wind-tunnel task bytes that were frozen before provider exposure in 1.9.725. It adds one fresh archaeology tray-release task; the exposed benthic task is not replayed or replaced.

All three tasks start at 75/85 with only accessibility and responsive critical gates red. They use the same schema 0.2 debt shape, cascade-effective `header > p.guidance-copy` and `footer` contrast selectors, decision parent containment, and six structured CSS obligations.

The archaeology task was evaluated locally without a provider. Its contrast failure targets are exactly `.guidance-copy` and `footer`, its task/state/design/evidence gates are green, and its sealed artifact is immutable with complete baseline debt coverage. The task inventory is now 91 and all current contracts validate.

No task in this lock may be sent to a provider until the exact bytes are committed and a new root passes provider-zero preparation and admission with the 1.9.726 workspace-relative candidate-path repair.
