# Access review task contract — 1.9.100

Status: **LOCKED**.

Add one unseen access-review task family before changing the current
`omd-apply` skill. The task must reuse the accepted generic `dashboard-v1`
evaluator rather than adding a scoring rule tailored to the expected result.

The fictional Permit console fixes:

- four access requests and an all/urgent/standard filter journey;
- two evidence disclosures and one acknowledgement action;
- desktop, 390px, 320px, and 200% zoom-surrogate viewports;
- exact DESIGN.md colors, typography, and radius tokens;
- protected identity, requested scope, urgency, evidence, and action hooks;
- an explicit unknown-claim denylist without invented risk, compliance,
  customer, or performance claims.

Acceptance requires the untouched starter to pass all 85 deterministic points,
including task contract, state journey, responsive geometry, accessibility,
design grounding, and evidence honesty. A starter failure may correct only the
task fixture or oracle; it cannot change the evaluator, skill, scoring weights,
or any previous benchmark result.

This patch authorizes no provider call, skill promotion, rescore, response
repair, or public superiority claim.
