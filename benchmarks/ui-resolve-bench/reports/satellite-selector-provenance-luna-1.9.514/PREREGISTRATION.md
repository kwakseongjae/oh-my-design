# Satellite selector-provenance transfer preregistration — 1.9.514

This provider-free plan compares exact 1.9.504 static-absence guardrail control
`86546d5a…` with exact 1.9.511 pre-edit selector-provenance candidate
`b0aa9d5a…` on the unseen satellite telemetry pass task locked at
`7614fd19…`.

The runtime is Codex with Luna/high. Reliability is measured over three paired
trials in balanced order, with a 900-second timeout, concurrency one, 120-second
inter-cell pacing, and no primary retry. All input, cached input, output, and
reasoning-output tokens count toward Tokens-to-Target.

Both arms must use the shipped runner in the exact named consumer browser and
close 390px, 320px, and actual 200% reflow. The candidate additionally requires
every snapshot-backed row selector to contain a stable class, id, or attribute
anchor present in the snapshotted pre-edit product. A tag-only or post-edit-only
selector must fail during lock, before any product edit. Missing pre-edit
measurements are classified separately from a real computed typography delta.

Promotion requires candidate UI-Resolved, proof-compliant, and shipped-runner
3/3, no paired objective loss, no serious/critical contrast issue, and mean
wall-time and token ratios no greater than 1.1. This preregistration calls no
provider and promotes no result.
