# Satellite selector-provenance transfer findings

The exact 1.9.511 candidate kept every snapshot-backed selector anchored in the
pre-edit product and admitted no post-edit-only selector. That closes the false
proof path observed in the preceding rail task, but this transfer does not earn
promotion: the candidate itself was not UI-Resolved.

The control scored 79/85 in 319.391 seconds with 637,185 tokens. It retained a
528.28125px comparison target and a negative-reserve action across the three
narrow conditions, then made a second product edit without rerunning the
terminal browser measurement. The candidate scored 81/85 in 298.086 seconds
with 587,144 tokens. It removed document overflow and clipping in all required
conditions, used one edit/static/browser sequence, and was 0.9333x control wall
time and 0.9215x control tokens.

The candidate still left `TELEMETRY-PACKET-HKP-662037` with only 5.421875 CSS
pixels of inline reserve at 320px, below the locked 8px minimum. It also left
the known 4.31:1 muted-text contrast defect unchanged, so the independent
accessibility gate failed. Both arms therefore failed trial 1. Neither arm can
reach the preregistered Reliability@3 requirement of 3/3, and the remaining
four cells are frozen without provider calls. The next iteration is a
provider-free repair of pre-edit acceptance planning for fit reserve and known
accessibility failures, followed by a fresh exact pin and unseen task.
