{
  "schema_version": "0.1",
  "product_version": "1.9.663",
  "status": "PROVIDER_FREE_REPAIR_ACCEPTED",
  "unresolved_frontier_gate": "routing-ownership-reverify",
  "bounded_hypothesis": "If plan-apply accepts only the exact accessible-name row set declared by the immutable diagnosis, operator input cannot silently widen the measured-plan decision boundary.",
  "candidate_delta": "Validate that operator_inputs has exactly one accessible_names map and that its row keys exactly equal the diagnosis-required row ids before any artifact mutation.",
  "frozen_denominator": {
    "fixture": "two measured rows requiring comparison-scroll accessible names",
    "provider_calls": 0,
    "model_exposures": 0,
    "browser_attempts": 0
  },
  "acceptance": {
    "exact_expected_row_set_applies": true,
    "surplus_row_rejected": true,
    "missing_row_rejected": true,
    "surplus_operator_field_rejected": true,
    "rejection_before_artifact_mutation": true,
    "existing_hash_guards_preserved": true
  },
  "guardrails": {
    "complete_patch_remains_immutable": true,
    "operator_may_only_supply_required_accessible_names": true,
    "browser_rerun_allowed": false,
    "product_edit_before_plan_close_allowed": false
  },
  "rollback_gate": "Revert if an exact packet emitted by plan-packet can no longer close, or if ready/irreconcilable packets require invented operator input.",
  "first_failure_rule": "Any accepted surplus/missing operator key or artifact mutation on rejection fails the repair.",
  "provider_calls": 0,
  "model_exposures": 0,
  "claim_boundary": "This closes a local packet-integrity gap. It does not establish model transfer, skill lift, or a 2.0.0 frontier gate by itself.",
  "next": "Run focused/full deterministic acceptance and pin the repaired source only if every packet state remains fail-closed."
}
