# Final findings — radiotherapy accessible atomic-fit transfer 1.9.459

The experiment is frozen after the first pair. Both systems produced objectively UI-Resolved 85/85 results, but neither met the proof contract. Because promotion requires candidate proof compliance 3/3, the first candidate proof failure caps reliability at 2/3. The remaining four provider calls cannot change the decision and are skipped.

The candidate transferred the intended accessibility repair. Desktop, 390px, 320px, and actual 640px + 2× document zoom are green. It removed page overflow and atomic fragmentation, preserved all facts and interactions, corrected contrast, kept protected target text out of a scroll container, and placed comparison overflow on a distinct named, keyboard-reachable carrier with visible focus. Axe reports zero serious or critical issues.

The remaining failure is execution discipline. The model authored a static script, corrected it twice, and therefore accumulated three static closures before the single terminal browser command. Browser recovery and post-browser verification are both zero, but duplicate static closure count is two. The candidate used 690,948 tokens, 0.6103× the control, while wall time was 457.012 seconds, 1.1134× the control and just above the 1.1 limit.

The next patch is provider-free. The artifact must carry a declarative static-closure manifest locked before product editing, and the shipped helper must execute that closure atomically once. The model should not author or debug ad-hoc post-edit regex scripts. Tokens-to-Target is now at least 45,082,351 plus six usage-unavailable cells. Quality promotion remains false.
