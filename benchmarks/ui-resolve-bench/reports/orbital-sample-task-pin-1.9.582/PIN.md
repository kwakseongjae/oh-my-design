# Orbital sample-return custody task pin

The fresh task is pinned at commit `536aa793…` and task tree `32800908…`. Its deterministic raw baseline summary is `2e335440…`; the evaluator score artifact is `bb82f178…`.

No model, historical control, or carrier-inner candidate has seen this task. Any future matrix must copy the task from this exact commit and preserve the 4/6/2 facts, the target-only pre-edit carrier, prompt, starter, DESIGN.md, evaluator, model, effort, timeout, pacing, and no-retry policy across arms.
