# Controller browser decoupling — 1.9.421

Browser execution is now an explicit matrix contract rather than a side effect of installed host policy. A controller-observation skill matrix can require the isolated named browser socket, OpenAI-only network profile, isolated Codex home, and real browser proof without installing or claiming a blocking proof hook.

Prepared cells retain the independent browser contract, and the Codex runner derives its sandbox from that contract or the legacy host gate. Matrix validation requires `require_browser_proof`, shared runtime state, and an exact named socket together. Focused validation is 46/46 with TypeScript and diff checks green; provider calls are zero.
