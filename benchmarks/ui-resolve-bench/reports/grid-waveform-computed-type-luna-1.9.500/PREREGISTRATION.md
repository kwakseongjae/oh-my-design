# Grid-waveform computed-type transfer — 1.9.500

This provider-zero plan isolates deterministic pre-edit computed typography and
the target-only comparison-carrier boundary on the unseen grid-disturbance task
locked in 1.9.499. Exact 1.9.490 is the visible atomic-fit control; exact
1.9.497 is the candidate. Both arms must invoke their installed shipped runner
and attach to the same named consumer browser without launching another browser.

Luna/high, Codex CLI 0.144.1, 900-second timeout, one-cell concurrency,
120-second pacing, no retry, prompt, starter, evaluator, named browser, and
runtime identity are shared. Required proof is one passed static closure, one
measured browser attempt, exact 390/320/actual-200 conditions, at least 8 CSS px
inline reserve outside a named comparison carrier, no document/body overflow,
and a closed artifact. The candidate additionally requires a sha256-verified
pre-edit product snapshot, actual computed pre-edit/edited typography equality,
and a separately registered target-only carrier with no focusable descendants
or clipped action.

Attempt 32 starts from Tokens-to-Target 51,734,008 plus six
usage-unavailable cells. Task exposure is 0/0. Promotion requires candidate
UI-Resolved 3/3, artifact-backed proof 3/3, shipped-runner execution 3/3, no
paired objective loss, no serious/critical contrast, and mean wall-time and
provider-token ratios no greater than 1.1. If any requirement becomes
mathematically impossible, remaining cells freeze.
