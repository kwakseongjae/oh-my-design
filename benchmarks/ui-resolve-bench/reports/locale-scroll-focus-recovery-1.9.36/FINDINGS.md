# 1.9.36 findings — locale scroll-focus recovery passed

## Outcome

The fresh exact-Opus candidate completed provider execution, external
evaluation, and export without retry, resume, intervention, or manual product
edits.

- Scheduled/attempted/completed denominator: 1/1/1
- Matrix status and validity: `complete` / `valid`
- Objective score: 85/85
- UI-Resolved: true
- Critical gates: 6/6
- Direct headless-browser commands: 0
- Replacement verifier: false

`/tmp/u1936` is frozen. Its product artifact must not be edited, resumed, or
retried.

## Process result

- Claude Code: 2.1.217
- exact parent model/effort: `claude-opus-4-8` / `xhigh`
- provider and child exit: 0 / 0
- timeout: false
- wall time: 476,284ms
- uncached input + output tokens: 108,075
- first and last product write: 404,471ms
- final response: present at 476,284ms
- product diff: `index.html`
- first product transaction: non-no-op `Write`
- direct browser commands: 0
- replacement verifier authored: false
- explicit/recoverable/infrastructure/sandbox/cwd errors: 0/0/0/0/0

The process, delivery, zero-browser, and external-evaluator authority contracts
all bound correctly.

## Quality result

The candidate passed:

- exact Clipboard API command copy and localized status in all five locales;
- all click and roving-keyboard locale states;
- bounded locale concepts, forbidden terms, and protected literals;
- root and panel language synchronization;
- desktop, 390px, 320px, and 200% geometry;
- no clipped or overlapping controls;
- keyboard traversal with visible focus at every viewport;
- axe serious/critical zero at every viewport;
- task contract, design grounding, and Evidence & Unknown.

The five command regions use explicit focus targets and localized accessible
names. They remain keyboard reachable when horizontally scrollable, and the
external evaluator visits the exact authored sequence once per viewport.

## Decision

Mark 1.9.36 `calibration_complete`. This closes the single-path locale
scroll-focus recovery and unlocks the next numbered lane.

The result is still one candidate execution. It supports the process and
objective acceptance contract only; it does not establish general locale
lift, model superiority, efficiency, or public frontier status. The next
bounded patch is 1.9.37: define the minimum honest public benchmark UX evidence
contract before exposing run data.
