# Provider-free repair — accessible atomic fit and static proof latch 1.9.453

This patch closes the two mechanism defects exposed by the frozen grid battery pair without making another provider call.

## Atomic fit

Protected target, identifier, and state text may no longer become a passive `overflow:auto|scroll` region. The fit order is now explicit: recover viewport and container width, reduce page and card insets within declared tokens, recover reading width, then move the semantic carrier to a full row, stack it, or relocate it. A comparison scroller is allowed only when it is a distinct relationship carrier with an accessible name, keyboard reachability, and visible focus. Every resolved compound row must prove `passive_text_scroll_container: false`.

## Proof latch

The reflow artifact now records an acceptance sequence before the product edit. Source inspection must be complete, the product edit is one planned transaction, and the only post-edit sequence is the consolidated static closure followed by the terminal browser command. An earlier partial source check consumes the task-wide static budget. A later corrective edit may fix the product safely, but it cannot rewrite that run as proof-compliant.

## Deterministic gate

- reflow artifact and skill contract: 25/25 green
- proof trace and runtime hook: 34/34 green
- exact actual-zoom candidate pin: 1/1 green
- TypeScript and diff checks: green
- broad benchmark contract: 93/95 green; the two unchanged failures require external Taste and UI UX Pro Max vendor directories to be Git repositories
- provider calls: 0

The next step is an exact source pin, followed by a genuinely unseen task. Promotion remains false until the repaired candidate reaches the preregistered quality, proof, reliability, and efficiency gates.
