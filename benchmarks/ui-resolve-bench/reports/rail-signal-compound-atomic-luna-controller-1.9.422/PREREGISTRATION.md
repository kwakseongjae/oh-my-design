# Rail-signal compound-atomic controller matrix — 1.9.422

Attempt 20 preserves the exact control `3a414a0a…` and candidate `1480d7cc…` skill arms but removes the invalid installed-host assertion exposed by two control runs. The control has seen the rail-signal task twice; the candidate has not seen it. Luna/high remains fixed in a balanced 2×3 order C/N, N/C, C/N with 900-second timeout, concurrency one, 120-second pacing, and no retry.

The matrix runs through the independent isolated browser contract: exact `bench19366` socket, shared runtime, isolated Codex home, and browser proof are mandatory. Proof behavior is measured by the post-run controller and is a promotion gate, not a claim that project hooks blocked code-mode nested tools. Any candidate promotion still requires UI-Resolved 3/3, exact signal/certificate/window preservation, all three relationship carriers, character-by-character `Range` evidence for `SIG-AX14-204 + CERT-IR-77421`, no paired objective loss, proof compliance 3/3, and mean wall/token ratios no greater than 1.1× control.

Earlier roots remain frozen. Provider calls at preregistration are zero. Prior observed Tokens-to-Target is at least 34,208,487 plus six usage-unavailable cells.
