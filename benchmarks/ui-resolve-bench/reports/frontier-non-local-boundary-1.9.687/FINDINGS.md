# Frontier non-local boundary — 1.9.687

## Decision

**Pause the local patch train.** All nine unresolved 2.0 gates now require evidence that cannot be produced honestly in the current local-only mode. Seven have complete local preparation and two have partial preparation; none is locally closable.

- Remote model execution: Verified Skill Lift, harness Pareto, Verified-scale artifacts, and routing/ownership/reverify.
- Remote multi-model execution: the three-family paired lower bound.
- Durable private storage plus later audit: 24 hidden tasks.
- External people: ten-practitioner blind review and independent task audit.
- Production observation: real install-to-resolved-surface and seven-day behavioral reuse.

The boundary auditor requires an exact mapping for all unresolved gates and rejects missing, duplicate, or falsely local mappings. This operationalizes the existing rule that a patch must produce gate-changing evidence, not merely increment a number.

## Smallest next authority

The shortest path that can change a machine pass predicate is to lift the current no-remote constraint for a freshly prepared six-cell Raw DESIGN.md versus exact-current OmD run on the frozen depot task. The existing 1.9.680 root must remain untouched; reprepare a new root at the then-current commit and evaluator epoch before execution.

The alternative first step is a durable private task location outside the public repository, but that advances denominator construction rather than producing Skill Lift.
