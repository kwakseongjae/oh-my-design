# Satellite telemetry release holdout — 1.9.462

This fresh repair task transfers the deterministic static-closure candidate into a satellite telemetry release workflow. Four supplied payload identifiers retain six exact telemetry-archive assignments; a separate three-pass ground-station strip and a separate release decision remain visible. Its identifiers, facts, state vocabulary, interaction copy, evidence boundary, palette, typography, counts, and selectors are unseen by the exact 1.9.460 source.

The untouched baseline is 75/85 and unresolved. Task facts, controls, form states, DESIGN.md grounding, evidence honesty, desktop geometry, target sizing, and keyboard traversal are green. Intended defects are 390px, 320px, and actual 200% overflow; character-range fragmentation of the compound payload/archive target; the two-line `Range coordination open` state; and serious text contrast at 3.85:1 and 3.68:1. No protected text is initially a scroll container.

The task is immutable for the next experiment. No provider has seen it. The comparison must use a fresh matrix root, exact historical control and exact 1.9.460 candidate source, runtime-bound model identity, immutable 390/1×, 320/1×, and 640/2× measurement conditions, the pre-edit static manifest, one exactly-once shipped `static-close`, terminal browser accounting, and the passive-text scroll prohibition.
