# Semantic carrier closure candidate — 1.9.252

Status: **AUTHORED, NOT QUALITY-PROMOTED**

The rejected 1.9.241 release-blocker candidate repeatedly hid the canonical shared gateway header during narrow reflow and created unbound visual copies. This bounded delta changes two general actions only:

1. foreground repair must appear in the first product edit diff; recording an exact ratio without applying the verified text-role/ink replacement does not close the transaction;
2. a shared header or legend that carries cell relationships stays visible in a named comparison scroll by default. If stacking is necessary, the existing semantic carrier is relocated with identity, cardinality, visibility, and association intact. Hiding it and recreating text through generated content, data attributes, aria labels, or hookless spans is explicitly a failure.

The same edits were mirrored into the historical decision-context experimental copy, preserving its one-rule isolation. No benchmark selector, task identifier, fixture value, color, component, or token was added. Canonical skill size changed from 37,465 to 38,232 bytes (+767); words changed from 4,220 to 4,300 (+80).

Validation:

- omd:apply delivery contract: 9/9 green
- decision-context one-rule isolation: green
- install-skills: 40/40 green
- TypeScript: green
- build: green

This is an authored hypothesis, not a quality claim. Next: exact commit pin, then a fresh unseen topology rather than reusing the sensor matrix for promotion.
