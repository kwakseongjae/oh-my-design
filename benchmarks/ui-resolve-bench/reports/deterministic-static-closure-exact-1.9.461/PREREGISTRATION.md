# Deterministic static closure exact pin — 1.9.461

The provider-free 1.9.460 repair is pinned to commit `8315182e137b7be958c264cc38798f56c37d6a21`. Future sandboxes must install the skill and its reflow helper from this detached commit; the mutable working tree is not eligible.

The pin includes the pre-edit declarative static-closure manifest, exactly-once `static-close` execution, persisted failed attempts, prohibition on model-authored post-edit verifiers, accessible compound-fit rules, and terminal browser accounting. No quality promotion is claimed.

The next step is a genuinely unseen task with different facts, carriers, state vocabulary, and visual contract. It must contain narrow and actual-200%-zoom failures that exercise the same general closure without revealing the repair target.
