# Autopilot Luna/high smoke 1.9.866

`complete`, but not claim-eligible and not reusable.

All three scheduled cells terminated validly under the exact Luna/high route. Each used one initial call plus the preregistered maximum of two bounded repair calls: 9 provider exposures total, with zero retry, replacement, fallback, or Cursor calls. The sealed run consumed 11,943,429 input tokens (11,156,480 cached), 161,695 output tokens, and 3,511,015ms of provider wall time.

The run exposed three independent 2.0 contract gaps:

1. The library page was objectively complete, but the evaluator did not recognize the explicit phrase “availability ... not provided here.” A provider-free replay after the contextual absence-language fix is 100/100. This diagnostic score is not substituted into the sealed run.
2. The cold-chain page used an accessible whole-row `role=button` pattern. The evaluator previously excluded that valid structure and confused “Unassigned” with the Assign action. The corrected replay now discovers all 12 records and the complete filter/detail/assignment journey. It still fails on real product defects: disabled empty-owner validation, 32px/mobile clipping, and invalid `aria-selected` on button rows.
3. The locale page reached an objective 100/100 and passed the independent design-system validator, but the mission stayed failed because locally unavailable browser checks kept `proof.json.pass=false`. Controller verification v0.3 now binds both objective and design-system proof and can close only when all task requirements plus local evidence-honesty/design-conformance checks pass.

The 1.9.866 root is evidence only. A fresh commit-bound 1.9.867 epoch is required for any updated result.
