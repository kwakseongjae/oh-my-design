# Autopilot Luna/high smoke 1.9.867

`complete`, but not claim-eligible and not reusable.

All three scheduled cells reached valid terminal outcomes under the exact Luna/high route. The run used seven provider exposures: the landing page timed out after its initial call, while cold-chain and locale each used the preregistered initial call plus two bounded repair calls. There were zero retries, replacements, fallbacks, Cursor calls, or model substitutions. Token usage is known for six of seven calls: 7,362,468 input tokens (6,744,064 cached) and 111,572 output tokens. Total provider wall time across all seven calls was 3,172,851ms. The landing timeout did not report token usage, so no complete-block token total is claimed.

The independent project-owned design-system proof passed for all three tasks, but objective UI success was 0/3:

1. The neighborhood library landing timed out after 900,230ms. It produced a visually strong editorial surface and a valid DESIGN.md, but exposed two visible “Reserve a tool” actions, did not open a real reservation state, did not transfer focus, and retained serious contrast violations. The timeout is a valid terminal provider failure, not a retry candidate.
2. The cold-chain dashboard improved `20→40→40`. Direct in-app inspection confirmed that the urgent filter works by pointer but not by keyboard Enter, so the evaluator correctly retained the programmatic-filter and filtered-result failures. Empty-owner validation, sample-owner scoping, identity-bearing assignment confirmation, and responsive constraints also remain unresolved.
3. The five-locale checklist improved `30→50→60`. Its neutral “Translation status” control opens an honest unavailable-translation alert without changing the active document language. The evaluator now accepts that equivalent wording. A provider-free replay removes only that false-negative assertion; the score remains 60 because checklist progress is genuinely lost across locale switches.

The 1.9.867 root is complete and permanently non-resumable. The next fresh epoch must first bound autonomous council/execution time and preserve task-critical behavior during repair. Public one-shot or superiority claims remain blocked.
