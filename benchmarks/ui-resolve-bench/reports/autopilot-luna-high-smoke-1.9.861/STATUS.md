# Autopilot Luna/high smoke 1.9.861 — complete

- Source commit: `ccebaeb133f2b7e345bca34808738eb288beb514`
- Locked plan SHA-256: `0f3559312f6daa8cdd00a8035311511b13fef30e9d3c1caeab4b8d3c4d8b9ce2`
- Runtime: exact `gpt-5.6-luna/high`, three serial cells
- Retry / replacement / fallback / model substitution / effort substitution: `0`
- Bounded same-mission repair calls: maximum `2` after the initial call
- Provider / model / Cursor calls during preregistration: `0 / 0 / 0`

This fresh diagnostic epoch replaces the completed, non-resumable 1.9.860
root. It uses the semantic accessibility and state diagnostics calibrated
against structurally distinct valid oracles and isolated mutants. No 1.9.860
workspace, product output, score, or state was reused.

No public one-shot or superiority claim is authorized. Valid terminal
failures remain in the denominator.

## Result

- Execution state: `complete`; three of three cells are valid terminal results.
- UI-Resolved: `1 / 3`; project-owned DESIGN.md proof: `3 / 3`.
- User-question artifacts: `0` across all three cells.
- Attempt scores:
  - neighborhood library landing: `10 → 50 → 100` — PASS
  - cold-chain operations: `20 → 40 → 40` — FAIL
  - five-locale clinic prep: `50 → 50 → 50` — FAIL
- Model calls: `9`, including `6` bounded repair calls.
- Wall time across all attempts: `3,783,912 ms`.
- Observed token usage: `12,020,294` input, `11,110,144` cached input,
  `177,827` output; input plus output `12,198,121`.

The landing task is the first fresh one-prompt greenfield cell to complete the
full OmD Autopilot contract: autonomous project-owned DESIGN.md, zero user
questions, working surface, 320 px and 200% reflow, accessibility, honesty,
and controller proof. It required both bounded repair calls.

Cold-chain remained unresolved after two repairs. The sealed record retains
`owner_error_associated`, `sample_owner_options`, and `responsive`; observed
geometry includes 28 px controls and horizontal overflow at 320 px and the
200% reflow-equivalent viewport. Locale retained
`translation_unavailable_honest` and `evidence_honesty`; its final sealed
evidence shows that textual progress was in fact persistent at every viewport.

Read-only in-app inspection and provider-free rescoring found two evaluator
false positives without rewriting the sealed run records. `sample owner` is an
explicitly scoped fictional owner option, and “does not provide medical
advice” is a negative disclaimer rather than a product claim. Under the patched
evaluator those assertions pass: cold-chain remains 40/100 because assignment
cannot be attempted without an owner and responsive geometry still fails;
locale becomes 60/100 and remains blocked only by the missing user-reachable
translation-unavailable state. The diagnostic score files are hash-bound in
`RESULTS.json` and are not benchmark authority.

This is diagnostic evidence only. The next release patch must commit the two
classification regressions and rerun a fresh epoch before expanding to
competitor qualification.
