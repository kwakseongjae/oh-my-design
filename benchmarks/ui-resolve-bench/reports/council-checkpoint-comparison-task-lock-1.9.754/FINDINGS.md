# Council checkpoint comparison task lock — 1.9.754

Three tasks were created and committed before any model exposure: photograph album return, mineral drawer return, and wax cylinder return. Each raw DESIGN.md baseline scores 75/85 with exactly responsive and accessibility red.

The future comparison keeps task, prompt, starter, DESIGN.md, model, effort, timeout, browser contract, objective evaluator, and deterministic reflow contract equal. The only arm difference is the current `omd-portable` activation versus `omd-portable-council-gate`, whose provider-sealed maintenance ledger must remain zero-dispatch and cannot invent values or reopen deferred decisions.

Cursor is forbidden. The eligible runtime is Codex-native `gpt-5.6-luna` at high effort. This lock contains no provider execution and no quality comparison result.

The next patch must preregister balanced arm order, one cell per checkpoint, fixed pacing, zero retry/replacement, exact installed skill identities, provider-zero council intake, and the proven 1.9.753 pre-edit-plan/continuation/cascade/browser contracts before any Luna call.
