# Council routed lifecycle preparation — 1.9.791

Three fresh UI tasks now cover the council states that the 1.9.789 comparison did not exercise.

- `papyrus-leaf-return-v0.1` retains one product-authority decision, dispatches two read-only advisory lanes, and forbids implementation before a signed answer receipt.
- `photographic-proof-return-v0.1` classifies a required but unsourced measured fact as an external-evidence blocker, suppresses advisory dispatch, and routes to `relay_blocked` with no implementation owner.
- `ceramic-sherd-return-v0.1` requires bounded advisory work for a reversible visual ambiguity, but has no registered user question and cannot implement until reconciliation completes.

All three raw starters score 75/85 with only responsive and accessibility red. Browser-harness loaded all three through the isolated local `omd1791` connection; the observed raw overflow is the intentional responsive debt recorded by the objective baseline.

This patch is provider-zero preparation, not council effectiveness evidence. The next patch must freeze task, prompt, starter, oracle, helper, and source identities. A later Luna/high run may then execute the advisory lanes, apply a preregistered simulated answer receipt for the interview case, and prove that only the two executable routes receive one implementation revision while the blocker remains byte-identical.
