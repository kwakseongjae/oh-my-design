# 1.9.547 — Offshore checklist transfer frozen

The structured first-edit checklist fixed the exact pharmaceutical failures: candidate static closure passed on the first attempt, safe `min-width:0` was not rejected, and no forbidden decision-grid declaration survived. The candidate was also faster and cheaper than the paired control (0.8211× wall time, 0.6126× provider tokens).

Neither arm closed proof. Both scored 83/85 and produced zero narrow overflow, fragmentation, clipping, or serious axe findings, but used local horizontal scrolling for three identifier groups still declared as `stack`. Their measured intrinsic width plus reserve exceeded the available document width, so terminal browser proof correctly marked the artifact unresolved. Desktop action separation also remained red.

Candidate Reliability@3 can now reach at most 2/3, below the preregistered 3/3 gate, so four untouched cells are frozen. The next change is provider-free: bind row decision semantics to measured fit feasibility before the product edit.
