# 1.9.545 — Offshore checklist matrix preparation

Six untouched workspaces were prepared from the locked matrix. All six share the same core prompt, starter, product tree, runtime, model, effort, and timeout; the prompt differs only by the preregistered arm activation.

The control source is detached and clean at `0b93fa97…`; the candidate source is detached and clean at `28f96200…`. Both install the identical browser runner and retain aggregate carrier planning. The candidate alone contains structured CSS assertions and the ordered terminal first-edit checklist.

No provider was called. Runtime browser, CLI, and authentication preflight must pass before the first control cell runs.
