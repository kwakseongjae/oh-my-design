# Transit stop timetable task lock — 1.9.254

Status: **LOCKED before provider generation**

`transit-stop-timetable-review-v0.1` introduces a service-by-stop timetable topology distinct from the existing list, card, timeline, routing, approval, assay-plate, continuous-spectrum, and channel-matrix families. The contract preserves four service identifiers, five stop identifiers, twenty exact supplied times, three timetable views, one express-note toggle, a named review form, exact evidence, and one target/evidence/state/action hierarchy.

Untouched starter: **75/85**. Task contract, state journey, design grounding, evidence honesty, desktop geometry, target sizes, named controls, explicit input labels, keyboard traversal, language, and network isolation pass. The only critical gates that fail are the intended repair surfaces:

- exact foreground contrast: `#66716C` on `#F3F1EA` is reported by axe as **4.48:1**, below 4.5:1; the accent state on the surface is also below the normal-text threshold;
- responsive geometry at 390px, 320px, and 200%: the timetable retains its desktop width, overflows horizontally, and fragments atomic service/stop identifiers and decision metadata.

The initial calibration scored 73/85 because the evaluator does not recognize an implicitly wrapped input label. The fixture was corrected to an explicit `for`/`id` relationship before lock; a fresh workspace then scored 75/85. This changed no intended contrast or reflow failure.

This is a fresh holdout. The prompt does not reveal selectors, failing color pairs, or a fixture-specific repair. The DESIGN.md names an existing verified ink role and requires the timetable container—not its identifiers—to adapt first, so the next comparison measures transfer of the pinned semantic-carrier principles.

Locked hashes:

- core prompt: `1ce4c5dfd53e06604325bf304a8a8fd47dce652dd70d8d699cb7f642bd7167ee`
- starter product: `87ac1327e337a6e3261b16f446d8fe968eedd1acd66056742df78fceeeaa277b`
- starter `index.html`: `6d3bc75c0b07397cb5570470b5fa6f61e05c62177d189ace04acdc6e22b33fa1`
- starter `DESIGN.md`: `ff6e4a6170366c1c19f4221b7c7b5905d646a59510e735dbef300d0ebbc8e682`

Provider calls: 0. Next: exact close-latch versus semantic-carrier candidate Reliability@3 preregistration with task, prompt, starter, DESIGN.md, runtime, model, effort, timeout, proof policy, balanced order, and pacing held equal.
