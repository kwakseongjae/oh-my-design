# Static absence guardrail candidate pin — 1.9.505

The 1.9.504 provider-free repair is pinned at exact commit
`86546d5af67d4a71f0b6a867ec3869ec299f3159` and skill tree
`73f5644267eee1e7053ba4a1eb66aa93b897cc81`. Detached vendor directory
`omd-1.9.504` is the only eligible provider source for this candidate.

The identity retains deterministic pre-edit computed typography and target-only
comparison carriers, then exposes the complete static edit guardrails at lock.
Forbidden CSS patterns now have absence semantics: the declaration is deleted,
not replaced with `normal`, `initial`, `unset`, `revert`, or `inherit`.

No provider was called and no quality result is promoted by the pin.
