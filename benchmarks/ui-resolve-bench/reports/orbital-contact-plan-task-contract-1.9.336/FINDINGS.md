# Orbital contact-plan task lock — 1.9.336

Status: **fresh unseen task locked before provider use**

`orbital-contact-plan-review-v0.1` tests a temporal/spatial contact schedule rather than the frozen aircraft load-plan task. It preserves six pass windows, eight assigned command bundles, four antenna availability windows, three view controls, one note toggle, one form, and a separate scheduling handoff.

The three relationship carriers are independently named in the task contract. Repeated pass, bundle, antenna, and link-window identifiers are well suited to schema `0.2` grouped evidence, but every rendered instance remains counted by the evaluator and protected hook ledger.

The untouched starter scores 75/85. Task, state, design, evidence, desktop, and keyboard checks are green. Intended defects remain exact: 390px/320px/200% page containment and atomic-line failures, plus supplied muted text at 4.43:1. The task therefore has room for a candidate to improve without inventing product claims or changing behavior.

No provider was called. The next step is an exact control-versus-1.9.334 candidate preregistration using the committed 1.9.335 host-policy pin.
