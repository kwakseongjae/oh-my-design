# 1.9.741 findings — static-surface maintenance intake

The repository prime previously classified a root `index.html` project as
static HTML but omitted that file from the surface inventory. That contradiction
made the council intake treat an existing product as greenfield and reopen three
settled questions.

Root `index.html` now counts as a real landing surface. For an existing-surface
improvement, audience and single-surface scope remain typed `defer` decisions.
When the user did not request an action change, the primary CTA is also deferred
against the existing surface path. No value is invented, no substitute action is
promoted, and these decisions are not sent to model lanes.

On the provider-zero meteorite probe, interview questions fell from three to
zero and planned council calls from two to zero. This closes an intake
classification defect. Fresh council-off/on product completion remains the next
evidence step.
