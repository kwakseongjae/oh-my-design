# Luna council checkpoint comparison — 1.9.756

This is the fresh provider-zero normalization repair for frozen 1.9.755. The three tasks, two arms, exact cell order, runtime, effort, timeout, pacing, lifecycle gates, and claim boundary are unchanged. The admission policy is corrected from single-task `exact-task-cross-arm` to the controller's three-task `paired-cross-task-comparison` contract.

Runtime is Codex-native `gpt-5.6-luna` high. Cursor is forbidden. The root uses a new exact `omd1756` isolated local browser and cannot resume or mutate the 1.9.755 workspaces.

Both arms still require Reliability@3, candidate paired objective loss zero, no unplanned human intervention, no retry/replacement, and byte-exact one-revision candidate promotion. This remains Internal Preview evidence and cannot establish general council lift or complete the 2.0 gate alone.
