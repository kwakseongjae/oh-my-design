# Council checkpoint comparison — 1.9.756 findings

The six-cell Codex-native `gpt-5.6-luna` high run completed in the preregistered order with one cell per checkpoint, fixed 30-second pacing, no retries or replacements, and zero Cursor calls. All six cells scored 85/85 with one product revision, one static closure, one measured browser pass, byte-exact candidate promotion, and no browser recovery.

The council-gate arm passed promotion proof on all three tasks. The current arm passed two of three. On the photograph-album task, current satisfied the coarse objective gates but failed the stricter desktop decision-context invariant because the target was not a full row; council-gate closed that invariant. The mineral and cylinder pairs were ties. Thus the observed paired proof outcome is one lift, two ties, and zero losses.

This candidate is `prime-only-no-dispatch`: it adds deterministic council intake/decision framing without asking the user or invoking additional model lanes. It is evidence that bounded decision framing can improve hierarchy retention, not evidence that a full multi-agent debate system is complete.

Descriptively, council-gate used 329,626 fewer total tokens (-14.95%) and 64,809ms less summed wall time (-11.03%) in this three-pair sample. The sample is too small for a general cost or latency claim. The preregistered overall comparison gate remains false because it required Reliability@3 from both arms and current achieved only 2/3 proof passes. Candidate Reliability@3 and paired-loss-zero both passed.

The next evidence step is a fresh-task repeat focused on full product hierarchy, followed by a separately preregistered bounded-dispatch experiment. Only the latter may test whether read-only specialist debate adds value beyond prime-only framing, and it must preserve question minimization, provider-lane accounting, and deterministic final ownership.
