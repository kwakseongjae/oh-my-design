# Assay plate host-policy comparison — 1.9.223

The unseen `assay-plate-layout-review-v0.1` task was locked before provider generation in 1.9.222. The two arms use the same task, prompt, starter, DESIGN.md, detached proof-close-latch skill, Codex runtime, Luna, high effort, 900-second timeout, user configuration, and balanced 2×3 order. The sole arm delta is explicit project proof-policy installation; only that arm receives automation-only hook-trust bypass.

Execution is sequential, fixed 120-second inter-cell pacing, no retry, no same-root repair, no fallback, and no result substitution. The matrix freezes on infrastructure, identity, attribution, preparation, or policy-state drift.

Unlike the earlier caption comparison, the installed arm is execution-valid only when every trial has valid policy state, `delivery: ready`, at least one actual browser attempt, and zero unblocked browser-recovery, duplicate-static, and after-ready violations. These completion conditions are frozen before the first provider call.

UI quality remains independent: report UI-Resolved rate, objective points, paired win/tie/loss, wall time, tokens, proof trace, and policy state. Opt-in availability may remain when execution gates pass; broader/default promotion requires installed UI-Resolved 3/3 and zero paired objective loss. Provider calls at preregistration: 0.
