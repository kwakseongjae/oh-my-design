# Assay plate host-policy preparation — 1.9.224

Fresh root `/private/tmp/u19223` contains six Git-backed cells and zero provider calls. Every cell has identical core/runtime prompt, untouched product tree, DESIGN.md, detached skill, activation delta, runtime, model, effort, and timeout. Controller cells have no hooks; installed cells contain the production-rendered policy, valid attestation, and native hook-trust boundary. No other paired difference was observed.

The next permitted action creates only `luna-assay-r1-controller`. Every continuation preserves max-new 1 and 120-second pacing. Any preparation, identity, policy-state, attribution, timeout, or infrastructure failure freezes the full root without repair or same-root retry.
