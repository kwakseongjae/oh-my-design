# 1.9.568 — Organ custody runner self-dispatch transfer

The experiment is frozen after the first pair. Both cells are valid system failures at 79/85. The control used 523,045 ms and 1,900,170 tokens; the candidate used 303,228 ms and 694,383 tokens. Those efficiency ratios are descriptive only, not a causal win: the two arms failed at different pre-edit proof boundaries.

The control reproduced the missing plain-Python dispatch failure. The candidate did not exercise the new safety path because it invoked browser-harness directly. Its distinct failure was a target-only aggregate carrier registered as `[data-bench-decision-role="context"] > .decision-target-carrier`. That class existed only in the planned edit, not in the locked pre-edit product, so the browser plan resolved zero carrier instances. The plan attempt was consumed before static or browser closure could become possible.

The promotion contract requires 3/3 candidate UI, proof, and self-dispatch trials. After a failed first candidate, only two candidate trials remain, so every 3/3 gate is mathematically unreachable. The four untouched cells are frozen; no retry or extra provider call is allowed.

The next repair is provider-free. Artifact snapshot must reject a carrier selector that cannot resolve against the locked pre-edit product before the browser proof budget is consumed. Only after a deterministic test and exact source pin may a fresh unseen transfer task be registered. Observed tokens-to-target are now at least 80,707,017, plus six earlier cells whose usage was unavailable.
