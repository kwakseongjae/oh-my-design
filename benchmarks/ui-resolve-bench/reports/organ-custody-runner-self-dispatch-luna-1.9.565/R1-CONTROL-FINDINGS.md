# 1.9.567 — Organ custody r1 control

The first control cell completed as a valid system failure. It scored 79/85 in 523,045 ms and used 1,900,170 provider-observed tokens. Contract, state journey, accessibility, design grounding, and evidence honesty passed; responsive acceptance failed because the decision action was not spatially separated on desktop and protected atomic text exceeded the line budget at 320px and actual 200%.

The proof trace itself was analyzable and respected the one-edit, one-static, one-browser, no-recovery budget. The proof artifact did not close. The control's shipped runner was invoked through plain Python during pre-edit planning, where browser-harness helpers were unavailable. Measurements remained open, static closure failed, and the final browser command could not repair the already-open artifact. This is the exact failure boundary preregistered for the self-dispatch candidate; the control is frozen with no retry.

Observed tokens-to-target now total at least 80,012,634 provider tokens across 41 bounded patch experiments, plus six earlier cells whose usage was unavailable. The next allowed provider call is the untouched r1 candidate after the fixed pacing interval.
