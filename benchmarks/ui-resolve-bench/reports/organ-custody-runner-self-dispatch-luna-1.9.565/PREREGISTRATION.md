# 1.9.565 — Organ custody runner self-dispatch transfer preregistration

This Reliability@3 matrix compares exact 1.9.555 decision-target inventory control against exact 1.9.562 runner self-dispatch candidate on the unseen organ transport custody task.

The only candidate delta is execution-path resilience: if the shipped runner is mistakenly invoked with plain Python, it forwards its unchanged source into the exact named browser-harness process before artifact access. Both arms retain protected decision-target inventory, measured row and aggregate fit plans, fit feasibility, structured first-edit closure, the same task and prompt, Luna/high runtime, 900-second timeout, 120-second inter-cell pacing, and no retry.

Three paired trials are scheduled in balanced order. Promotion requires 3/3 UI resolution and proof compliance, candidate self-dispatch execution when the misuse occurs, no objective loss, no serious/critical contrast issue, and mean wall-time and token ratios no greater than 1.10. A mathematically impossible Reliability@3 result freezes the unneeded suffix without additional provider spend.
