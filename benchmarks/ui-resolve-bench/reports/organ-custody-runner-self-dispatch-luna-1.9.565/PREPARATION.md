# 1.9.566 — Organ custody runner self-dispatch matrix preparation

Six untouched workspaces were prepared from the locked matrix. All six share the same core prompt, prompt, starter, product tree, runtime, model, effort, and timeout; only the installed exact skill source differs by arm.

The control source is detached and clean at `6142925c…`; the candidate source is detached and clean at `bec454d5…`. Both retain target inventory, measured row and aggregate carrier planning, fit feasibility, structured first-edit closure, and the identical artifact helper. The candidate runner alone detects a plain-Python invocation before artifact access and forwards its unchanged source into the exact browser-harness stdin path with a recursion latch and no browser-launch fallback.

No provider was called. Runtime browser, CLI, and authentication preflight must pass before the first control cell runs.
