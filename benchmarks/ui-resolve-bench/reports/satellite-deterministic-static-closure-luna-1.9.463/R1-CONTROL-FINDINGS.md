# Satellite r1 control — 1.9.465

The exact 1.9.453 control produced an objectively resolved UI: 85/85, all four responsive conditions green, no serious/critical contrast issue, complete state journey, preserved evidence, and no passive protected-text scroll container. Runtime identity matched Luna/high through the isolated execution home. The cell completed in 438.796 seconds with 621,392 observed provider tokens.

The proof trace also passed: one product edit, one static closure, one browser mechanism, zero browser recovery, zero duplicate static closure, and zero verification after readiness. The control nevertheless authored a long task-specific inline Node verifier after editing; this is exactly the behavior the candidate replaces with the shipped declarative helper, but the preregistered proof classifier correctly retains this control as compliant because it executed only once.

The paired candidate must preserve the 85/85 objective and proof result while using its pre-edit manifest and exactly-once `static-close` helper. Wall time and provider tokens remain eligible for the preregistered 1.1 ratio gates.
