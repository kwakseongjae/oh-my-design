# Observatory measured-fit-plan transfer preregistration — 1.9.528

This provider-free plan compares exact 1.9.518 acceptance-debt control `8d647987…` with exact 1.9.525 measured pre-edit fit-plan candidate `b3b83bf7…` on the unseen observatory frame-calibration task locked at `ff00df37…`.

The runtime is Codex with Luna/high. Reliability uses three paired trials in balanced order, a 900-second timeout, concurrency one, fixed 120-second inter-cell pacing, and no primary retry. Every provider-reported input, cached input, output, and reasoning-output token counts toward Tokens-to-Target.

Both arms must use their pinned shipped browser runners through the exact named consumer and close 390px, 320px, and actual 200% reflow. Both must register known baseline responsive/accessibility failures before editing. The candidate must additionally run the browser snapshot and named `plan` mode before product editing, measure every protected text carrier's intrinsic nowrap width in all three narrow conditions, add the locked 16px planning reserve, bind the resulting plan digest to the pre-edit snapshot, and close the plan before the first product edit.

Promotion requires candidate UI-Resolved, proof-compliant, shipped-runner 3/3, measured-plan 3/3, zero unresolved acceptance debt, no paired objective loss, no serious/critical contrast issue, and mean wall-time and token ratios no greater than 1.1. This preregistration calls no provider and promotes no result.
