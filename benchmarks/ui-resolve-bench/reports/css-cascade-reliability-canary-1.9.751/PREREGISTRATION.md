# Exact CSS-cascade Reliability@3 — 1.9.751

This internal diagnostic tests whether the strengthened exact-value source contract prevents a later media-query override from passing static closure. It is not a model ranking or a 2.0 release claim.

- Runtime/model: Codex `gpt-5.6-luna`, high effort only. Cursor is forbidden.
- Fixed order: photographic negative → architectural drawing → ceramic vessel.
- One cell per checkpoint, concurrency 1, 30-second inter-cell pacing, no retry or replacement.
- Browser: controller-started isolated local Chrome, exact named socket `omd1751`, attach-only, no user profile or cloud browser.
- Before provider exposure, the controller must run the shipped browser runner in `plan` mode for all prepared cells and attest provider calls 0, Cursor calls 0, one measured attempt, exact named CDP attachment, no browser launch, and unchanged product bytes.
- Every matching `.decision { grid-template-columns }` declaration must remain exactly `1fr`; a desktop media-query override must fail the deterministic candidate preview.
- A cell passes only at 85/85, resolved, one product revision, exact candidate promotion, closed static/browser proof, and zero recovery. A lifecycle failure freezes the remaining cells.
