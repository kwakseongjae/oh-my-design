# Findings — atomic browser proof budget 1.9.432

Accepted provider-free.

- `atomic_parts` now provides observable child ranges without authorizing separator wrapping when the protected wrapper has a one-line contract.
- The skill directs the agent to recover carrier width by full-row, stack, or relocation before breaking the compound target.
- The ready mechanism is named as the first and only `browser-harness <<'PY' … PY` call; help, skill, executable, socket, and port discovery are explicitly forbidden.
- Post-edit `sed`, `rg`, and `wc` acceptance reads consume the single static closure budget.
- Focused tests: 10 passed, 90 unrelated cases skipped. TypeScript and diff checks: green. The broad suite additionally retained two known external-vendor Git-root failures only.
- Provider calls: 0. Quality promotion: false.
