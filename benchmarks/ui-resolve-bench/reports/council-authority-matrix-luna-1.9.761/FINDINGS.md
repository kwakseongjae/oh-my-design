# Findings — Luna/high authority matrix 1.9.761

The three-case matrix passed all eight exact disposition assertions. The council removed only the three repository-resolved questions, retained pricing as an interview, kept missing official brand evidence blocked, and made no forbidden automatic decision.

The deterministic existing-docs control selected no lane. All four selected Luna/high lanes in the other cases completed with valid artifacts, no undeclared writes, no timeout, no retry, and no Cursor use.

This supports the authority-source contract across three distinct paths, but it remains calibration evidence rather than UI-output lift or public benchmark superiority evidence.

## Cost finding

The matrix consumed 214,151 input tokens, 166,912 cached, plus 5,962 output and 3,195 reasoning-output tokens. Summed lane wall time was 154,464 ms. Input cost per correctly deferred question rose to 71,383.67 because the already-blocked brand case still dispatched two advisory lanes without changing its disposition.

The next optimization is deterministic: when a pre-intake ledger already contains a blocking external-source decision, stop and hand off that block before dispatching advisory lanes. This should reduce the brand case from two calls to zero without changing any decision.

