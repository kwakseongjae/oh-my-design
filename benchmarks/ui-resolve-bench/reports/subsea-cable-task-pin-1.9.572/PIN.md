# 1.9.572 — Subsea cable task pin

The fresh task is pinned at commit `2e5134e4…` and task tree `fa7572bb…`. Its deterministic raw baseline summary is `8da54b0d…`; the evaluator score artifact is `934a9234…`.

No model has seen this task. Control and candidate exposure counts are both zero. The next allowed step is provider-zero preregistration of an exact-source replacement matrix.
