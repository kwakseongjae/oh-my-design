# Composer semantic-color recovery — 1.9.50 findings

Status: **execution complete; candidate recovery passed**.

## Outcome

The single fresh Composer 2.5 OmD candidate completed without retry, fallback,
timeout, manual product edit, Auto/Router, or provider effort arguments.

- deterministic score: 85/85;
- automated gate: pass;
- critical gates: 6/6;
- Evidence & Unknown: pass;
- axe serious/critical: 0 at desktop, mobile, 320px, and 200% surrogate;
- product diff: `index.html` only;
- wall time: 243,408 ms;
- non-cached tokens: 60,429.

The exact 1.9.48 regression did not recur. `signal-orange #E7683D` is used only
as a decorative dot/background. Normal text uses declared ink, muted, danger,
or success roles. This is the intended foreground closure: the accent remains
available for non-text expression without being promoted to an unmeasured text
color.

## Decision boundary

This candidate-only cell is outside every Skill Lift denominator. Composer
reported only the expected display name, so the public validity remains
`invalid-attribution`; that does not negate the internal deterministic pass.
No public model, skill, efficiency, confidence, or frontier claim follows.

The bounded recovery passes and unlocks a fresh, balanced nine-cell Composer
replacement using the unchanged task, evaluator, Raw control, and acceptance
contract.
