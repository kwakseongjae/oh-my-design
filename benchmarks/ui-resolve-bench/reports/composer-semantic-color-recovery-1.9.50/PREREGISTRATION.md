# Composer semantic-color candidate recovery — 1.9.50

Status: **EXECUTED; candidate recovery passed**.

## Hypothesis

The 1.9.49 foreground closure should prevent the exact 1.9.48 failure cluster
in a fresh Composer+OmD output without changing task behavior, design
grounding, responsive behavior, Evidence & Unknown, or other accessibility
checks.

## Frozen cell

- root: `/tmp/u1950`;
- runtime/model: Cursor Agent / `composer-2.5`;
- expected display name: `Composer 2.5`;
- task/variant: `pricing-conversion-v0.1` / `omd-portable`;
- evaluator/suite: schema `0.5` / `ui-resolve-v0.2`;
- timeout: 900 seconds;
- retries/fallback/manual product edits: none;
- source delta from 1.9.48: canonical OmD skill foreground closure only.

The same standing per-cell transmission boundary applies. No Raw control,
Grok artifact, 1.9.48 output, unrelated skill, catalog, repository history,
credential, user document, or `web/public/llms-full.txt` is transmitted.

## Acceptance

Pass only at 85/85 with all six critical gates, axe serious/critical zero at
all four viewports, Evidence & Unknown pass, `index.html`-only product diff,
and no normal-text element using unmeasured signal-orange on paper/white.

Stop and freeze on the first provider/controller/evaluator/export failure.
This candidate-only result is outside every Skill Lift denominator. A pass
only unlocks a fresh full Composer replacement; a repeat of the same
semantic-color root cause triggers the hard-pause audit.

## Frozen decision

The fresh Composer candidate completed without retry, fallback, manual product
edit, timeout, or controller failure. It scored 85/85, passed all six critical
gates and Evidence & Unknown, produced zero serious/critical axe violations at
all four viewports, and changed only `index.html`. `signal-orange` remains only
as non-text decoration; text uses declared ink/muted/danger/success roles.

The candidate-only recovery therefore passes and unlocks a fresh nine-cell
Composer replacement. It remains outside every Skill Lift denominator and
does not revise the rejected 1.9.48 result.
