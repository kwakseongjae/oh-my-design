# Rail-signal compound-atomic range oracle — 1.9.412

Attempt 18 uses the unseen rail-signal possession handback holdout locked at 1.9.411. It compares exact historical control `3a414a0a…` with exact 1.9.410 compound-atomic range-oracle candidate `1480d7cc…` under Luna/high in a balanced 2×3 matrix: C/N, N/C, C/N. Timeout is 900 seconds, concurrency is one, fixed inter-cell pacing is 120 seconds, and primary retries are forbidden.

The candidate must resolve all three trials, preserve all supplied signal/certificate relationships and all three relationship carriers, keep `SIG-AX14-204 + CERT-IR-77421` semantically exact while allowing line breaks only between its atomic children, introduce no paired objective loss, pass the installed host/proof gates three times, and stay within 1.1× control mean wall time and provider tokens. Proof must measure non-whitespace character-by-character `Range` line tops; element rectangles cannot satisfy the oracle. All admission, checkpoint-resume, timeout-accounting, isolated runtime, named browser, bootstrap boundary/order, shared socket, completion-loop, observed-attempt, compound-atomic, and character-range pins are fixed before preparation. Earlier roots and outputs remain frozen. Provider calls at preregistration: 0.
