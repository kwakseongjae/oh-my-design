# Rail-signal compound-atomic experiment — infrastructure stop at 1.9.414

The first Luna/high control completed in 870.043 seconds with 3,508,695 observed provider tokens. It improved the untouched task from 75/85 to 79/85: horizontal overflow and serious contrast closed, while character-range atomic-line checks remained red at 390px, 320px, and the 200% reflow surrogate.

The result cannot enter the paired comparison. The agent ran two post-edit static closure commands. The proof trace correctly counted one duplicate, but the installed host hook did not deny it; the host therefore reported `duplicate-static-closure-unblocked-limit`. This is an interception failure, not a candidate-quality verdict. The matrix and all remaining five cells are frozen with no retry.

Tokens-to-Target is right-censored at a minimum of 31,625,601 observed provider tokens plus six usage-unavailable cells. The next step is provider-free repair of duplicate-static interception, exact pinning, and a fresh preregistration/root. No candidate cell has seen this task.
