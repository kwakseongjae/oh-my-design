# 1.9.584 preparation checkpoint

The exact detached control and candidate worktrees are clean, and all six Luna/high cells are untouched. Core prompt, rendered prompt, starter, product tree, runtime, model, effort, and timeout are equal across every cell.

Both sources retain self-dispatch, pre-edit carrier-anchor validation, aggregate outer-width planning, and the same product/evaluator contract. Only the candidate records the bound carrier content-box width, aggregates the minimum across instances, and uses that local width for row feasibility.

No provider was called. The next permitted action is runtime preflight followed by exactly one r1 control cell with no retry.
