# Orbital carrier-inner r1 candidate

The candidate reached **85/85** in 652.673 seconds with 3,392,946 provider tokens. It preserved the task, states, design contract, evidence boundary, responsive geometry, and accessibility. It also observed carrier-local width and selected `comparison-scroll` for the nested custody target, while the control stopped at 83/85.

The run is not promotion-eligible. Static closure rejected the measured plan before browser proof because a fixed-width pre-edit register reported a live 1038px inner box inside a 390px document. That value describes the existing overflow, not the width available after the carrier is contained. The shipped browser terminal was therefore never invoked and the artifact remained open.

This is useful directional evidence for carrier-local planning, but not causal transfer proof and not a victory claim. Since a 3/3 proof-compliant candidate result is now mathematically unreachable, the remaining four cells are frozen untouched. The next repair must clamp the carrier budget to `min(live inner width, document width - horizontal chrome)` and prove the producer-consumer schema without spending another provider call.
