# Luna council checkpoint comparison — 1.9.755

This is an Internal diagnostic of the current portable OmD apply skill with and without the maintenance-aware, provider-sealed zero-dispatch council ledger. It does not compare models or competing skills.

Three fresh tasks are paired across both arms. Runtime is Codex-native `gpt-5.6-luna`, effort high, timeout 720 seconds, concurrency one, fixed balanced order, 30-second inter-cell pacing, one cell per checkpoint, and zero retry or replacement. Cursor is forbidden.

Task, prompt, starter, DESIGN.md, installed skill, objective evaluator, deterministic source contract, browser, and proof contract are shared. The only intentional arm delta is the activation plus provider-zero council intake. Every candidate council ledger must report zero interview questions, zero model lanes, and immutable defer decisions.

Both arms must pass 3/3 lifecycle reliability and the candidate may have no paired objective loss. Quality, wall time, reported tokens, and intervention counts remain separate axes. A lifecycle or infrastructure contract failure freezes the remaining root. An objective miss may continue only when the lifecycle contract remains clean.

Even a complete pass remains Internal Preview evidence. It cannot establish general council lift, model superiority, competitor superiority, or the full 2.0 release gate.
