# Studio-slot proof-budget Luna xhigh replication — preregistration

Status: **LOCKED before preparation and provider execution**.

This cross-model confirmation is triggered by Grok 1.9.189's 3/3 quality pass and material numeric efficiency gain alongside a failed command-trace gate. It is a separate denominator, not a replacement or pooled extension of Grok.

- Task: exact `studio-slot-routing-v0.1` `0.1.0`
- Arms: exact compact packet `omd-portable-reflow-packet-candidate` versus exact proof budget `omd-portable-proof-budget-candidate`
- Provider scope: Codex runtime / `gpt-5.6-luna` / xhigh for both arms
- Matrix: 2 arms × 3 trials with the exact Grok balanced first-arm order
- Timeout: 900 seconds; globally serial; fixed 120-second inter-cell pacing
- At most one new cell per invocation; no retry, fallback, repair, replacement, resume after failure, or model substitution inside this root
- Attribution: internal registered model result; reported separately from Grok

The Luna quality gate is proof-budget UI-Resolved 3/3 and Reliability@3 100% with zero paired loss. Wall time, tokens, and tool calls are reported within Luna as descriptive deltas, not pooled with Grok. The compliance gate remains browser recovery commands `0` and duplicate static-closure clusters `0`. Any provider, attribution, or controller failure freezes this root.
