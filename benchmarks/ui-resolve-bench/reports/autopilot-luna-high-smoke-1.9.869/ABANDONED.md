# 1.9.869 pre-admission draft — not executed

This provider-zero draft was generated without an explicit `--experiment-id`, so its
locked plan retained the controller's historical default experiment id. It was never
admitted to the in-app browser and made zero provider/model calls. It is preserved only
to make the discarded preparation trace explicit. The actual 1.9.869 diagnostic is in
`../autopilot-luna-high-smoke-1.9.869-fresh/`.
