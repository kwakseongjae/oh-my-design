# Wildlife first-edit packet canary — 1.9.703

This one-cell canary uses the fresh public development task `wildlife-acoustic-media-custody-v0.1`, locked at task commit `b51649c5` with zero prior model exposures. It installs the exact current `omd:apply` skill from the clean preparation commit and runs Luna/high once.

The cell is diagnostic only. It has a 720-second timeout, no retry, no replacement model, and no public ranking eligibility. Provider usage is observed rather than hard-capped because the runtime does not expose a trustworthy pre-execution token ceiling; the one-cell denominator is the spend boundary.

Transfer success requires 85/85 objective resolution, no user handoff, exactly one product revision, one successful static closure, zero failed static closures, proof compliance, and no unsupported evidence claim. If the browser plan is unavailable before navigation, the source fallback must expose and apply the short `source_fallback_patch_contract` before that single edit. If the browser plan succeeds, the measured-plan path may satisfy the same one-edit acceptance without invoking fallback.

A timeout, infrastructure failure, failed static closure, second product edit, unresolved objective gate, or missing provider usage remains a valid retained failure. This single public development task cannot enter Skill Lift ranking or advance a 2.0 frontier gate.
