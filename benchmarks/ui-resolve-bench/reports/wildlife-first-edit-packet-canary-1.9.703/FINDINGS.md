# Wildlife first-edit packet canary — 1.9.703

## Result

The fresh Luna/high diagnostic completed one valid cell without retry or
replacement. The final product resolved every objective gate at **85/85** and
preserved the supplied evidence boundary, but proof compliance remained red.

- Product revisions: 1
- Successful static closures: 0
- Failed static closures: 1
- User handoffs and browser retries: 0
- Wall time: 418,286 ms
- Provider tokens: 2,156,519

The fallback opening and the short relationship packet transferred before the
only product edit. The implementation then added the required target/evidence
markers, names, keyboard reachability, scrolling, focus treatment, and nowrap
rules. It placed the valid declarations behind contextual selector prefixes
such as `.decision [data-omd-source-fallback-carrier="target"]` instead of the
packet's exact canonical selectors. The exact static verifier therefore
rejected all six CSS assertions and correctly made that closure terminal.

## Interpretation

This is not evidence for a public ranking or a 2.0 gate promotion. It is useful
transfer evidence: the UI repair, one-edit limit, and terminal failure behavior
all transferred, while the patch packet still left room for a semantically
valid but verifier-incompatible selector rewrite.

The next local patch emits one copy-ready `canonical_css_source` and explicitly
forbids ancestor prefixes, suffixes, aliases, and substitutions. The same task
must not be rerun; any later transfer check needs another fresh task.
