# Reliability task lock — 1.9.730

This set carries forward only the archaeology task that was frozen before provider exposure in 1.9.728. It adds two genuinely fresh tasks: paleontology jacket accession and textile roll conservation. Conservation and wind-tunnel results from 1.9.728 are not replayed.

All three tasks start at 75/85 with only accessibility and responsive critical gates red. Their schema 0.2 debt coverage is complete, their sealed artifacts are provider-immutable, and each prepared workspace contains the same clean `4e9514b9` OmD skill with deterministic `static-promote` support.

The two fresh tasks were evaluated locally without provider or model exposure. Their contrast failures are exactly `.guidance-copy` and `footer`; task, state, design, and evidence gates are green. The public task inventory is now 93.

No task in this lock may be sent to a provider until these exact bytes are committed and a new root passes provider-zero preparation and admission. A later cell failure must freeze the remaining cells; replay, retry, and replacement remain forbidden.
