# Locale standards replacement smoke — 1.9.26

Locked before workspace preparation or provider generation on 2026-07-23.

## Question

Under evaluator schema `0.3`, can the reviewed
`omd:locale-adapter` → `omd:humanize` stack complete a fresh five-locale
handoff with standard roving keyboard behavior, synchronized document
language, protected facts, honest evidence, responsive geometry, and bounded
delivery?

This is the clean two-cell replacement for the stopped 1.9.23 smoke and the
candidate-only 1.9.24 recovery. It is not a repeated Skill Lift estimate and
cannot support a public best-skill, model, frontier, global-rank, efficiency, or
statistical lift claim.

## Frozen task and evaluator

- Task: `locale-cli-handoff-v0.1` version `0.2.0`
- Evaluator score schema: `0.3`
- Task/evaluator source basis: `b128aef`
- Initial/browser locale: KO / `ko-KR`
- Locale order: KO, EN, JA, ZH-CN, ZH-TW
- Protected command: `npx northstar-ui@1.4 setup --agent claude-code`
- Protected facts: 12 checked reference packs, 3 local workflows, `DESIGN.md`
- Required navigation: click reachability plus standard roving Right/Left
  Arrow wrap across all five tabs
- Required language state: active tab, panel, body locale, and root `lang`
  change together and restore to KO
- Required handoff: visible copy action and non-empty localized result; the
  initial polite live region may be empty
- Viewports: 1440×1000, 390×844, 320×720, and 640×900 at 200% CSS-zoom
  surrogate
- Critical gates: task contract, state journey, responsive, accessibility,
  design grounding, Evidence & Unknown
- Accessibility: axe serious/critical zero and complete platform-correct
  keyboard traversal
- Unknown claims: zero unsupported protected claims or proof structures

Calibration is frozen: the exact retained 1.9.24 artifact scores 85/85 under
schema `0.3`; the seeded invalid starter scores 57/85 and fails state,
responsive, accessibility, and evidence gates. Neither enters this experiment
denominator.

## Frozen matrix

- Systems: `raw-design-md` and `omd-locale-handoff`
- Runtime: Claude Code 2.1.217 or newer, recorded per run
- Parent model/effort: exact `claude-opus-4-8` / `xhigh`
- Task: one
- Trials: one fresh cell per system; two cells total
- Timeout: 900 seconds per cell
- Network: disabled by task and sandbox contract
- Hooks, Agent tool, third-party installer: disabled
- Output root: `/tmp/u1926`
- Schedule: `RUN-MATRIX.json`

Both systems receive the exact same starter, core prompt, DESIGN.md, model,
effort, timeout, browser evaluator, and no-network sandbox. The candidate's
only capability delta is the reviewed local installation and explicit
sequential activation of `$omd:locale-adapter`, then `$omd:humanize` in bounded
VERIFY mode.

## Candidate delivery authority

The candidate keeps the frozen 1.9.24 process contract:

- first product write: no later than 450,000ms;
- optional verification stops by 720,000ms;
- final response begins before 810,000ms;
- repository-existing checks or one direct browser command only;
- replacement verifier, verify/check/probe program, temporary shell program,
  CDP/browser automation, DOM shim, mock browser, or new test runner: zero;
- blocked browser proof is reported unresolved and delivery continues.

The matrix runner enforces first-write and replacement-verifier gates on the
`locale-skill-stack` cell.

## Validity and stop policy

A cell is valid only when provider and child exit zero, no timeout occurs, a
final response and product diff exist, exact model attribution holds, the
frozen evaluator and export complete, and infrastructure/sandbox/cwd errors are
zero.

Any auth, quota, model, process, timeout, final, delivery, replacement-verifier,
evaluator, or export failure stops the experiment immediately. There are no
retries, resumes, workspace reuse, or manual product edits. A product-quality
failure in the valid Raw cell is retained and does not prevent the candidate
cell.

## Decision

`calibration_complete` requires:

- both cells valid under the frozen process policy;
- candidate first product write no later than 450,000ms;
- candidate replacement verifier authored: false;
- candidate objective score 85/85 with all six critical gates;
- every locale click and roving-keyboard state exact;
- every required/forbidden locale pattern passes;
- protected command, 12, 3, and `DESIGN.md` preserved in all locales;
- unsupported claims and proof structures: zero;
- axe serious/critical: zero at all four viewports;
- candidate objective score no lower than the Raw control.

Anything else is retained as `calibration_failed` or `inconclusive`. A full
pass validates this single execution path and can unlock a repeated locale
matrix; it does not establish general locale lift.

