# Flight-recorder visible atomic-fit transfer — 1.9.493

This provider-zero plan isolates the visible atomic inventory and padding-aware
fit-reserve repair on the unseen flight-recorder task locked in 1.9.492. Exact
1.9.483 is the socket-native control; exact 1.9.490 is the candidate. Both arms
must invoke their installed shipped runner and attach to the same named consumer
browser without launching another browser.

Luna/high, Codex CLI 0.144.1, 900-second timeout, one-cell concurrency,
120-second pacing, no retry, prompt, starter, evaluator, named browser, and
runtime identity are shared. Required proof is one passed static closure, one
measured browser attempt, exact 390/320/actual-200 conditions, locked
typography, at least 8 CSS px inline reserve, no document/body overflow, and a
closed artifact. The long decision evidence and initially empty form status
must remain carrier content rather than be promoted into the atomic inventory.

Attempt 31 starts from Tokens-to-Target 50,436,586 plus six
usage-unavailable cells. Task exposure is 0/0. Promotion requires candidate
UI-Resolved 3/3, artifact-backed proof 3/3, shipped-runner execution 3/3, no
paired objective loss, no serious/critical contrast, and mean wall-time and
provider-token ratios no greater than 1.1. If any requirement becomes
mathematically impossible, remaining cells freeze.
