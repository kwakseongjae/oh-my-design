# Proof execution budget — preregistration

Status: **LOCKED before implementation; provider-free**.

The exact reflow packet passed quality 3/3 under both Grok and Luna but regressed mean wall time by 11.3% and 10.3%. Luna traces show repeated source reads, browser recovery attempts, and overlapping static checks. This bounded candidate changes execution discipline only.

## Single delta

- Add one machine-readable proof execution budget to `omd-apply`.
- Permit one pre-edit inventory, target one product edit transaction, one consolidated static closure plus one corrective rerun only after a product change, and one browser mechanism attempt.
- After browser infrastructure failure, forbid doctor/help, executable/process/port discovery, direct browser launch, alternate browser/port/runtime, install, or permission-recovery commands.
- Keep every reflow, product-contract, accessibility, evidence, token, geometry, and delivery gate unchanged.
- Do not change task, evaluator, runner, runtime, model, timeout, activation, or provider policy.

Acceptance requires focused skill contract, install channel parity, bounded advisory contract, TypeScript lint, and diff checks green. Provider calls remain zero. The candidate is not promoted by provider-free acceptance; it must be pinned from a clean commit and compared against the exact packet candidate on a new unseen task.

