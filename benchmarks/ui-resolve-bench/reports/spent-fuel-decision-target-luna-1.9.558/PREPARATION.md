# 1.9.559 — Spent-fuel decision-target matrix preparation

Six untouched workspaces were prepared from the locked matrix. All six share the same core prompt, prompt, starter, product tree, runtime, model, effort, and timeout; only the installed exact skill source differs by arm.

The control source is detached and clean at `84329948…`; the candidate source is detached and clean at `6142925c…`. Both retain measured row and aggregate carrier planning, structured first-edit closure, fit-strategy feasibility, and the identical browser runner. The candidate alone binds the snapshot's protected decision-target hook to one exact row and one distinct target-only carrier before plan close.

No provider was called. Runtime browser, CLI, and authentication preflight must pass before the first control cell runs.
