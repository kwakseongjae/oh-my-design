# Effective-skill-locked CSS-cascade Reliability@3 — 1.9.752

This is the fresh provider-zero successor to the frozen 1.9.751 preparation. It keeps the exact task denominator and locks the observed effective installed council-gate skill tree before any Luna exposure.

- Runtime/model: Codex `gpt-5.6-luna`, high effort only. Cursor is forbidden.
- Fixed order: photographic negative → architectural drawing → ceramic vessel.
- One cell per checkpoint, concurrency 1, 30-second inter-cell pacing, no retry or replacement.
- Browser: controller-started isolated local Chrome, exact named socket `omd1752`, attach-only, no user profile or cloud browser.
- Provider-zero pre-edit planning must complete for all three cells before the first provider call.
- Every matching `.decision { grid-template-columns }` declaration must remain exactly `1fr`.
- A cell passes only at 85/85, resolved, one product revision, exact candidate promotion, closed static/browser proof, and zero recovery.
