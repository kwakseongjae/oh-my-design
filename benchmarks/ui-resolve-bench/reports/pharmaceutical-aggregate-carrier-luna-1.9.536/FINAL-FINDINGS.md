# Pharmaceutical aggregate-carrier transfer findings

The aggregate plan fixed the abstraction error it was designed to address. Candidate trial 1 measured five enclosing carriers before editing and then achieved 85/85 with zero overflow, clipping, token fragmentation, or serious accessibility violations at every scored viewport. Against the paired row-only control, it used 0.9035× tokens at 1.0064× wall time. The control retained 508/578/1155px of narrow overflow.

Promotion still failed because the proof contract exposed a separate first-edit problem. Trial 1's static forbidden pattern treated safe `min-width:0` containment resets as if they were the positive fixed-width locks being removed. Trial 2 correctly measured all three registered carrier constraints but retained a forbidden `grid-template-columns` declaration in the decision carrier. In both trials, the exactly-once static close failed and correctly prevented a later corrective edit from being promoted without a fresh preregistered run.

This separates product effectiveness from promotion reliability. Aggregate carrier measurement transferred and produced one fully resolved UI, but the frozen candidate produced zero closed proofs in two trials. Even a successful third candidate trial could yield only 1/3 proof Reliability@3, so the final control and candidate cells are frozen.

The next repair is provider-free. Static patterns must express semantic risk precisely—for example, positive fixed-width locks rather than every `min-width` declaration—and the plan-close output must turn every required, forbidden, and count assertion into a compact first-edit checklist. A candidate must satisfy that checklist before consuming the exactly-once static close.
