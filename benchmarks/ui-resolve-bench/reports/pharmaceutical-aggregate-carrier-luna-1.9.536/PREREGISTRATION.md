# Pharmaceutical aggregate-carrier transfer preregistration — 1.9.536

This provider-free plan compares exact 1.9.525 row-only measured fit-plan control `b3b83bf7…` with exact 1.9.533 aggregate-carrier fit-plan candidate `0b93fa97…` on the unseen pharmaceutical batch-deviation task locked at `85a309d9…`.

The runtime is Codex with Luna/high because the retained Grok long-form lane is resource-exhausted and forbidden from immediate replacement. Reliability uses three paired trials in balanced order, a 900-second timeout, concurrency one, fixed 120-second inter-cell pacing, and no primary retry. Every provider-reported input, cached input, output, and reasoning-output token counts toward Tokens-to-Target.

Both arms must use their pinned shipped browser runners through the exact named consumer and close 390px, 320px, and actual 200% reflow. Both must snapshot, measure row intrinsic width, close the named plan, register known baseline responsive/accessibility failures, perform one product edit, run one deterministic static close, and finish with one terminal browser acceptance.

The candidate must additionally bind every row to exactly one smallest stable existing layout carrier and measure carrier max-content outer width, horizontal chrome, inter-item gap, available document width, the 16px required outer-width reserve, and `requires_reflow` in all three conditions before editing. A green row text budget cannot override a red aggregate carrier budget.

Promotion requires candidate UI-Resolved, proof-compliant, shipped-runner, measured-plan, and aggregate-carrier-plan 3/3; zero unresolved acceptance debt; no paired objective loss; no serious/critical contrast issue; and mean wall-time and token ratios no greater than 1.1. This preregistration calls no provider and promotes no result.
