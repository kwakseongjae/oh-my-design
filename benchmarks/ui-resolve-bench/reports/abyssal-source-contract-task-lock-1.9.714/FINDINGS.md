# Abyssal source-contract task lock — 1.9.714

The new public development task is locked with zero provider calls and zero
model exposures. It preserves four sediment-core identifiers, six exact archive-
sleeve assignments, three cold-vault intake windows, three views, one archive-
note toggle, one form, and an explicit unknown-evidence boundary.

The raw starter scores **75/85**. Contract, state journey, design grounding, and
evidence honesty pass. Responsive and accessibility fail on intentional fixed-
width carriers, constrained action wrapping, and 4.47:1 muted canvas contrast.

Unlike the retired model-authored path, sandbox preparation generated the
provider-sealed source artifact before execution. A read-only `source-packet`
left its SHA-256 unchanged and exposed all relationship and acceptance CSS as
structured declarations. This lock is diagnostic only and is not ranking or
2.0 promotion evidence.
