# Source-contract Reliability@3 task lock — 1.9.724

Three new fictional repair tasks are locked with provider calls 0 and model
exposures 0: benthic sample transfer, conservation frame loan, and wind-tunnel
model handoff. The terminal 1.9.722 tasks are not replayed, renamed, or edited.

Each provider-free raw DESIGN.md baseline scores exactly 75/85. Task contract,
design grounding, state journey, and evidence honesty pass; responsive and
accessibility fail. The accessibility failure surface is now exact: only the
canvas guidance paragraph and footer fail contrast, while surface-local muted
copy passes. Their contract uses the cascade-effective
`header > p.guidance-copy` selector, which outranks `.muted`, plus `footer`.

All three schema 0.2 contracts exactly cover the false critical-gate set
`accessibility,responsive`. Provider-zero OmD preparation sealed each artifact,
preserved an immutable inventory, and emitted the same six structured CSS
obligations: register reflow, window reflow, decision reflow, decision-parent
containment, guidance contrast, and footer contrast.

Focused task, mutation, helper, and sandbox checks passed 303 tests with 2
skipped; TypeScript lint/type-check passed. This checkpoint is task and contract
evidence only. It contains no provider result, model transfer claim, ranking
change, or 2.0 promotion.

The next gate is a clean-source preregistration that pins the exact 1.9.723 OmD
skill and these task hashes. A future provider run must use new workspaces, one
trial per task, no retry or replacement, and the candidate-preview receipt
lifecycle.
