# Compact release-blocker pass — 1.9.241

Status: **STRUCTURAL CANDIDATE; provider validation pending**.

The rejected spectrum candidate showed that more detailed foreground and
reflow prose did not improve reliability. This patch changes execution
priority instead of adding another closure. It places one three-action pass
before the detailed acceptance packet:

1. close every visible normal-text foreground pair;
2. reflow the parent before altering atomic text at desktop, 390px, 320px,
   and 200%;
3. stop after one consolidated static closure and one browser mechanism.

The detailed foreground and reflow paragraphs were shortened so the canonical
skill is 37,465 bytes versus 37,756 bytes before the patch (**-291 bytes**),
despite the new top-level priority block. The packet below it remains the
evidence schema, not another execution pass. No benchmark selector, task,
brand, filename, literal product value, new token, or prescribed component was
added.

Provider-free checks:

- `omd-apply` contract: 9/9 pass;
- historical decision-context one-rule isolation: pass;
- install-skills coverage: pass;
- TypeScript: pass;
- build: pass;
- full benchmark contract: 66/68; the two failures are the unchanged external
  Taste and UI UX Pro vendor directories that are not Git repositories.

This is not a promotion. Commit and pin the exact source and installed skill
tree, then lock a fresh unseen task family distinct from spectrum allocation.
Compare the exact close-latch baseline against this candidate with the same
Reliability@3, zero-paired-loss, proof-policy, wall-time, and token gates.
