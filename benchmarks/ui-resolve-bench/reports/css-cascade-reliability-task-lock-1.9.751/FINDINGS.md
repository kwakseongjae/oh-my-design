# CSS-cascade Reliability task lock — 1.9.751

The failed 1.9.750 root remains frozen. Herbarium is excluded after Luna exposure. Photographic negative return and architectural drawing return remain provider/model-zero and were revised only at the deterministic source-contract boundary. Ceramic vessel return is the fresh third task.

All three tasks begin at 75/85 with only responsive and accessibility red. Every decision carrier now requires `grid-template-columns: 1fr` as an exact value across all matching declarations, so a later media-query override fails static closure before browser promotion. Cursor execution is forbidden; this experiment uses the Codex runtime and `gpt-5.6-luna` only.

Execution remains forbidden until this lock and the exact matrix are committed and a fresh isolated named-browser root passes provider-zero admission and controller pre-edit planning.
