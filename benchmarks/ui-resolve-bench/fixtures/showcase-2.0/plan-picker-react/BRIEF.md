From scratch, create a membership plan picker as a buildless React app using ONLY the vendored Preact + HTM runtime already wired through the import map in index.html (import from "preact", "preact/hooks", "htm"; no build step, no network, no other libraries). Establish a project-owned design system projected as a theme object plus mirrored CSS custom properties. Use the provided images in ./assets/ for the header artwork. A member must compare three fictional plans, select one (keyboard reachable; aria-pressed/aria-checked driven by the same state that drives the visuals), and see a summary panel update with the chosen plan's name and inclusions. Pressed/hover/selection transitions use motion tokens behind prefers-reduced-motion. Do not invent real prices — label all plans and amounts as sample data.
