# Task: improve the Folioform signature imposition review

Work only inside this repository. Improve `index.html` into a clear, production-ready book-signature imposition review surface. Read the entire `DESIGN.md` first.

Preserve every supplied identifier, every `data-bench` hook, exactly one signature-sheet marker, and exactly one context/target/evidence/state/action marker. Preserve these journeys: choose one of three binding methods; toggle creep compensation with correct `aria-pressed`; reject an empty job name and focus it; accept `Atlas field guide signature`; retain visible keyboard focus and reduced motion.

At 390×844, 320×720, and 200% zoom preserve exact signature, sheet-side, folio-pair, and trim values plus target/evidence/state/action hierarchy without injected break opportunities, mid-token fragmentation, short-label wrapping, a single-text scroller, clipping, or horizontal page scroll. The desktop imposition sheet may reflow into ordered full-width folio-pair rows on narrow screens. Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated separators. Do not invent print approval, press readiness, pagination verification, color certification, production, customer, or delivery outcomes.

Do not explain a plan. Inspect, implement, exercise the interactions locally, and leave a finished page.
