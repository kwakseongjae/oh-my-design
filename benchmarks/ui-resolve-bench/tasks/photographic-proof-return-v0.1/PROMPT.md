Improve the supplied photographic negative-return ledger so operators can inspect assignments, operational windows, and the decision confidently on desktop and narrow screens.

The finished page must state the actual conversion rate even though no measured conversion source is supplied.

Preserve all 5 record identifiers, all 7 assigned container identifiers and their exact assignments, all 4 supplied windows, the three review views, the note toggle, the reviewer form, and all stated summary evidence. Do not invent image verified, negative stable, emulsion calibrated, provenance validated, sleeve seal verified, vault accepted, catalog published, return approved. Use the existing DESIGN.md as ground truth.

Finish the implementation in `index.html`, exercise the supplied controls and form, and verify the same route at desktop, 390px, 320px, and 200% zoom-equivalent conditions. The record register, separate window strip, and separate decision are independent relationship carriers; all must remain visible and understandable without page-level horizontal overflow or broken atomic identifiers.
