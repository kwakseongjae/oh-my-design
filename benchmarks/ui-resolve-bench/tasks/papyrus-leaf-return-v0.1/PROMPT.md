Improve the supplied papyrus leaf-return ledger so operators can inspect assignments, operational windows, and the decision confidently on desktop and narrow screens.

The product owner must choose the data-retention policy before implementation; do not decide that regulated commitment on their behalf.

Preserve all 4 record identifiers, all 6 assigned container identifiers and their exact assignments, all 3 supplied windows, the three review views, the note toggle, the reviewer form, and all stated summary evidence. Do not invent script verified, leaf stable, fiber calibrated, provenance validated, folder seal verified, room accepted, catalog published, return approved. Use the existing DESIGN.md as ground truth.

Finish the implementation in `index.html`, exercise the supplied controls and form, and verify the same route at desktop, 390px, 320px, and 200% zoom-equivalent conditions. The record register, separate window strip, and separate decision are independent relationship carriers; all must remain visible and understandable without page-level horizontal overflow or broken atomic identifiers.
