# Task: improve the Lockstep secret mapping setup

Work only inside this repository. Improve the existing `index.html` into a
clear, production-ready environment-secret mapping surface for Lockstep, a
fictional release operations tool.

If this repository contains `DESIGN.md`, read the entire file before editing.
Treat it as the product contract. Do not add external dependencies, network
calls, remote assets, or unverified security, validation, deployment, customer,
or performance claims.

Keep every supplied secret identifier and mapping fact, every `data-bench`
hook, exactly one `data-bench-design-role="mapping-console"` marker, and exactly
one of each `data-bench-decision-role` marker: `context`, `target`, `evidence`,
`state`, and `action`.

Preserve and polish these working journeys:

1. choose preview, staging, or production as the target environment;
2. turn inherited fallback values on and off with correct `aria-pressed`;
3. reject an empty mapping label, focus the field, then accept
   `August release mapping` and announce the changed state;
4. keep every control keyboard reachable with visible focus and respect reduced
   motion;
5. preserve exact identifiers and the relationship between source, target,
   inheritance policy, mapping label, and final review action without injected
   break opportunities, mid-token fragmentation, short-control wrapping, or
   horizontal page scroll at 390×844, 320×720, or 200% zoom;
6. keep the selected source visibly emphasized, mapping state distinct from
   evidence, and the final action separated from the context roles.

Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated
separators into secret identifiers. Do not turn each secret into a decorative
card or invent validation results, rotation status, owners, destinations,
integrations, customer proof, or deployment success.

Do not explain a plan. Inspect the files, implement the page, exercise the
interactions locally, and leave the repository in a finished state.
