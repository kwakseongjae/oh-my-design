# Observatory frame calibration review

Repair this observatory frame-calibration review so an imaging operator can inspect the supplied observation-frame/calibration-packet relationships, telescope stations, and archive decision at desktop, 390px, 320px, and actual 200% reflow.

Preserve every supplied identifier, assignment, behavior, and archive fact. Keep the observation-frame manifest, telescope-station strip, and archive decision as separate relationship carriers. Do not invent or imply calibration validation, artifact-cause confirmation, packet completeness, pointing approval, science release, reprocessing authorization, or archive readiness.

Use `DESIGN.md` as the visual contract. Fix contrast, narrow-screen geometry, intact one-line identifiers and short state/control text, decision hierarchy, focus, and overflow without shrinking a declared typography role, word-breaking identifiers, hiding or duplicating protected content, or replacing unknown facts with plausible fallbacks.
