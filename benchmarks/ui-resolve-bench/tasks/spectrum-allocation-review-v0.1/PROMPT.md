# Task: improve the Northline spectrum allocation review

Work only inside this repository. Improve `index.html` into a clear, production-ready radio-observatory spectrum allocation review surface. Read the entire `DESIGN.md` first.

Preserve every supplied frequency range and allocation identifier, every `data-bench` hook, exactly one spectrum-board marker, and exactly one context/target/evidence/state/action marker. Preserve these journeys: choose one of three receiver modes; toggle guard-band notes with correct `aria-pressed`; reject an empty session name and focus it; accept `Night 14 allocation review`; retain visible keyboard focus and reduced motion.

At 390×844, 320×720, and 200% zoom preserve exact range/allocation pairs and target/evidence/state/action hierarchy without changing ascending spectrum order, injected break opportunities, mid-token fragmentation, short-label wrapping, a single-text scroller, clipping, or horizontal page scroll. The desktop spectrum strip may become ordered full-width allocation rows on narrow screens, but 1370–1488 MHz order and each range/allocation pairing must remain explicit. Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated separators. Do not invent interference, calibration, signal, safety, approval, observation, science, or release outcomes.

Do not explain a plan. Inspect, implement, exercise the interactions locally, and leave a finished page.
