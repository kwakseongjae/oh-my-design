# Task: improve the Fieldnote media-clearance handoff

Work only inside this repository. Improve `index.html` into a clear, production-ready media-rights clearance handoff. Read the entire `DESIGN.md` first.

Preserve every supplied identifier, every `data-bench` hook, exactly one clearance-board marker, and exactly one context/target/evidence/state/action marker. Preserve these journeys: choose one of three review lanes; toggle source-caption context with correct `aria-pressed`; reject an empty label and focus it; accept `Autumn campaign clearance`; retain visible keyboard focus and reduced motion.

At 390×844, 320×720, and 200% zoom preserve exact atomic values, the master–detail relationship, and target/evidence/state/action hierarchy without injected break opportunities, mid-token fragmentation, short-label wrapping, a single-text scroller, clipping, or horizontal page scroll. Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated separators. Do not invent clearance, legal, publication, owner, territory, customer, or performance outcomes.

Do not explain a plan. Inspect, implement, exercise the interactions locally, and leave a finished page.
