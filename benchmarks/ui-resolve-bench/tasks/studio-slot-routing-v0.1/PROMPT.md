# Task: improve the Roomsheet studio-slot routing board

Work only inside this repository. Improve `index.html` into a clear, production-ready studio resource-time routing board. Read the entire `DESIGN.md` first.

Preserve every supplied identifier, every `data-bench` hook, exactly one schedule-board marker, and exactly one context/target/evidence/state/action marker. Preserve these journeys: choose one of three rooms; toggle turnaround buffer with correct `aria-pressed`; reject an empty label and focus it; accept `Night session routing`; retain visible keyboard focus and reduced motion.

At 390×844, 320×720, and 200% zoom preserve exact atomic values, chronological slot order, resource-to-time relationships, and target/evidence/state/action hierarchy without injected break opportunities, mid-token fragmentation, short-label wrapping, a single-text scroller, clipping, or horizontal page scroll. The desktop time-band board may reflow into ordered full-width slot rows on narrow screens. Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated separators. Do not invent booking, approval, staffing, availability, customer, or performance outcomes.

Do not explain a plan. Inspect, implement, exercise the interactions locally, and leave a finished page.
