# Satellite telemetry release review

Repair the existing telemetry release review so it remains usable at desktop, 390px, 320px, and actual 200% zoom/reflow.

Preserve every supplied payload and telemetry archive identifier, their exact assignments, the separate ground-pass strip, the three review views, relay-note toggle, form journey, and decision facts. Keep atomic identifiers and short state/control copy intact and readable. Reflow relationship carriers before changing typography or text behavior.

Use the local DESIGN.md as ground truth. Do not invent launch clearance, range approval, orbit insertion, payload commissioning, telemetry validation, ground-station acceptance, archive release, testimonials, metrics, or substitute facts. Do not hide, abbreviate, duplicate, or word-break supplied identifiers. Preserve the existing behavior and evidence boundary while correcting the UI.
