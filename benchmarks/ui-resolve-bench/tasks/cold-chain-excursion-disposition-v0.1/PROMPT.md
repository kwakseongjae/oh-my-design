# Cold-chain excursion disposition

Repair this cold-chain excursion disposition review so a cold-chain quality reviewer can inspect the supplied shipment/sensor relationships, custody windows, and disposition decision at desktop, 390px, 320px, and actual 200% reflow.

Preserve every supplied identifier, assignment, behavior, and decision fact. Keep the shipment lot register, custody-window strip, and excursion decision as separate relationship carriers. Do not invent or imply quality release, custody verification, excursion acceptance, sensor calibration, carrier notification, CAPA closure, inventory release, or disposition approval.

Use `DESIGN.md` as the visual contract. Fix contrast, narrow-screen geometry, intact one-line identifiers and short state/control text, decision hierarchy, focus, and overflow without shrinking a declared typography role, word-breaking identifiers, hiding or duplicating protected content, or replacing unknown facts with plausible fallbacks.
