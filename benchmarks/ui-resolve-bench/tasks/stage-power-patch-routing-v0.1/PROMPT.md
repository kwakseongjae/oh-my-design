# Task: improve the Circuitline stage power patch board

Work only inside this repository. Improve `index.html` into a clear, production-ready stage power patch routing surface. Read the entire `DESIGN.md` first.

Preserve every supplied identifier, every `data-bench` hook, exactly one patch-board marker, and exactly one context/target/evidence/state/action marker. Preserve these journeys: choose one of three circuit banks; toggle isolation with correct `aria-pressed`; reject an empty label and focus it; accept `Matinee power handoff`; retain visible keyboard focus and reduced motion.

At 390×844, 320×720, and 200% zoom preserve exact atomic values, source-to-outlet-to-load relationships, and target/evidence/state/action hierarchy without injected break opportunities, mid-token fragmentation, short-label wrapping, a single-text scroller, clipping, or horizontal page scroll. The desktop patch-bay schematic may reflow into ordered full-width path rows on narrow screens. Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated separators. Do not invent energization, approval, electrical safety, load capacity, availability, customer, or performance outcomes.

Do not explain a plan. Inspect, implement, exercise the interactions locally, and leave a finished page.
