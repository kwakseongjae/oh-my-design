# Task: improve the Meridian sensor channel matrix review

Work only inside this repository. Improve `index.html` into a clear, production-ready sensor channel compatibility review surface. Read the entire `DESIGN.md` first.

Preserve every supplied channel identifier, gateway identifier, and compatibility value, every `data-bench` hook, exactly one channel-matrix marker, and exactly one context/target/evidence/state/action marker. Preserve these journeys: choose one of three matrix views; toggle reserved-channel notes with correct `aria-pressed`; reject an empty review name and focus it; accept `North array matrix review`; retain visible keyboard focus and reduced motion.

At 390×844, 320×720, and 200% zoom preserve the 5×4 channel/gateway relationships and target/evidence/state/action hierarchy without injected break opportunities, mid-token fragmentation, short-label wrapping, a single-text scroller, clipping, or horizontal page scroll. The desktop cross-matrix may become ordered full-width channel groups on narrow screens, but channel order, gateway order, and each compatibility value must remain explicit. Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated separators. Do not invent connection, calibration, telemetry, safety, approval, observation, quality, or release outcomes.

Do not explain a plan. Inspect, implement, exercise the interactions locally, and leave a finished page.
