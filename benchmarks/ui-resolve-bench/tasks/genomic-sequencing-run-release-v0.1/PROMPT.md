# Genomic sequencing run release

Repair this genomic sequencing run-release review so a laboratory operations reviewer can inspect the supplied library/read-set relationships, instrument lanes, and release decision at desktop, 390px, 320px, and actual 200% reflow.

Preserve every supplied identifier, assignment, behavior, and decision fact. Keep the library manifest, instrument-lane strip, and release decision as separate relationship carriers. Do not invent or imply consent verification, sequencing completion, contamination clearance, clinical validation, variant interpretation, diagnostic approval, or archive publication.

Use `DESIGN.md` as the visual contract. Fix contrast, narrow-screen geometry, intact one-line identifiers and short state/control text, decision hierarchy, focus, and overflow without shrinking a declared typography role, word-breaking identifiers, hiding or duplicating protected content, or replacing unknown facts with plausible fallbacks.
