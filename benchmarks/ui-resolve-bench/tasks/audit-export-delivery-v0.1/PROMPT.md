# Task: improve the Ledgerline audit-export delivery setup

Work only inside this repository. Improve the existing `index.html` into a
clear, production-ready audit-export delivery surface for Ledgerline, a
fictional compliance operations tool.

If this repository contains `DESIGN.md`, read the entire file before editing.
Treat it as the product contract. Do not add external dependencies, network
calls, remote assets, or unverified compliance, transfer, encryption,
validation, customer, or performance claims.

Keep every supplied artifact identifier and delivery fact, every `data-bench`
hook, exactly one `data-bench-design-role="delivery-console"` marker, and
exactly one of each `data-bench-decision-role` marker: `context`, `target`,
`evidence`, `state`, and `action`.

Preserve and polish these working journeys:

1. choose secure link, managed archive, or manual handoff as the delivery
   channel;
2. turn recipient identity verification on and off with correct
   `aria-pressed`;
3. reject an empty export label, focus the field, then accept
   `August audit delivery` and announce the changed state;
4. keep every control keyboard reachable with visible focus and respect reduced
   motion;
5. preserve exact identifiers and the relationship between manifest, artifact,
   destination, channel, recipient policy, export label, and final review
   action without injected break opportunities, mid-token fragmentation,
   short-label wrapping, single-text horizontal scrollers, or horizontal page
   scroll at 390×844, 320×720, or 200% zoom;
6. keep the selected manifest visibly emphasized, delivery state distinct from
   evidence, and the final action separated from the context roles.

Do not insert `<wbr>`, `<br>`, zero-width spaces, soft hyphens, or generated
separators into identifiers. Do not turn each artifact into a decorative card
or invent encryption results, recipient checks, owners, transfer attempts,
customer proof, or delivery success.

Do not explain a plan. Inspect the files, implement the page, exercise the
interactions locally, and leave the repository in a finished state.
