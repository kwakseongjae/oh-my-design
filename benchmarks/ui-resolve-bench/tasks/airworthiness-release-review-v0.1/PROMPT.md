# Airworthiness release review

Repair this airworthiness release review so a maintenance controller can inspect the supplied work-order/inspection relationships, maintenance windows, and release decision at desktop, 390px, 320px, and actual 200% reflow.

Preserve every supplied identifier, assignment, behavior, and decision fact. Keep the work-order register, maintenance-window strip, and release decision as separate relationship carriers. Do not invent or imply airworthiness release, inspection completion, defect clearance, engineer authorization, parts certification, maintenance sign-off, return-to-service approval, or regulator acceptance.

Use `DESIGN.md` as the visual contract. Fix contrast, narrow-screen geometry, intact one-line identifiers and short state/control text, decision hierarchy, focus, and overflow without shrinking a declared typography role, word-breaking identifiers, hiding or duplicating protected content, or replacing unknown facts with plausible fallbacks.
