Add a “Manage booking” path to the existing Stillwater Rooms implementation. A visitor enters confirmation code `SW-2048`, sees the existing Cedar reservation, changes its time to another locked time, reviews the change, and reaches a visible “Booking updated” state. Preserve the original new-booking path and its behavior. Do this in the same model session, arm, trial, and workspace. Do not add cancellation, accounts, payment, notifications, or new business facts.

Acceptance is deterministic: invalid codes show an associated error; `SW-2048` opens the management flow; the current room/date/time are visible; selecting a different locked time and confirming shows “Booking updated”; refresh or back navigation never converts the update into a new booking; the original booking flow still passes its locked checks.
