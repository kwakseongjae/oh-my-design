# ZIGZAG verification — 2026-07-13

## Proof — Tier 1 live inspect

This update uses the supplied MCP-free collector artifact only: `artifacts/reference-evidence/zigzag.json` (`playwright_cli`, captured `2026-07-13T07:24:23.043Z`). It records three public product surfaces, 39 component variants, two observed states, no safe interaction expansions, and coverage 84/100. It is raw evidence; only the claim-linked values in `DESIGN.md` are canonical.

**Inspected:** 2026-07-13

**Sources:**

1. https://zigzag.kr/
2. https://zigzag.kr/catalog/products/145661347
3. https://zigzag.kr/catalog/products/162934185

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| `home::body` | `rgb(41, 43, 43)` (`#292b2b`) on `rgb(255, 255, 255)` (`#ffffff`); 16px / 400; computed stack begins `Pretendard JP`. |
| `home::[data-omd-capture="2"]` | icon link: transparent background, `#292b2b`, 8px radius, 44px × 44px. |
| `surface-2::div.product-card-thumbnail` | `rgb(236, 237, 238)` (`#ecedee`) thumbnail placeholder, 0px radius, 172px × 206px. |
| `surface-2::div.product-card-root` | transparent card root, 0px radius, padding `0px 0px 8px`, 172px × 336px. |
| `surface-2::[data-omd-capture="28"]` | selected detail tab: transparent, `rgb(18, 18, 18)` (`#121212`), padding `0px 4px 17.5px`, 14px / 600. |
| `surface-2::[data-omd-capture="29"]` | default detail tab: transparent, `rgb(135, 143, 145)` (`#878f91`), padding `0px 4px 10px`, 14px / 500. |
| `home::[data-omd-capture="74"]` | quick-menu next arrow: transparent, `#000000`, 1px `#ecedee` border, 50% radius, 32px × 32px. |
| `home::[data-omd-capture="73"]` | quick-menu previous arrow: disabled, `rgba(16, 16, 16, 0.3)` text, 1px `#ecedee` border, 50% radius, 32px × 32px. |

### Surface-domain boundary

- **Product surfaces:** the home page and both catalog product pages above are the only sources of current canonical tokens and components.
- **Marketing / brand publishing:** the official ZIGZAG Beauty publication is context evidence only; its campaign language and visual treatment do not populate product tokens.
- **Documentation / engineering chrome:** the official ZDS article is evidence for design-system history and component architecture, not a source for unmeasured current public component values.
- **Authenticated or native commerce:** cart, checkout, payment, error, success, account, and native-app UI were not captured and are not inferred.

### Font evidence

| Evidence class | Observation and resolution |
|---|---|
| Official product-use | No inspected first-party source states a current universal ZIGZAG UI font-family token. |
| Live computed surface-use | 912 visible elements across all three product surfaces computed to a stack beginning `Pretendard JP, Pretendard, -apple-system, system-ui`. |
| FontFaceSet / source corroboration | The collector records `Pretendard JP` as **unresolved**: its computed use had no matching loaded FontFace or source URL. This fails the promotion rule. |
| Declared-only | `Pretendard` was declared with zero visible uses; `swiper-icons` and `VideoJS` were declared-only icon/video fonts. |
| Official distributed font / license | The independent Pretendard project documents the family and SIL Open Font License 1.1: https://github.com/orioncactus/pretendard/blob/main/LICENSE. It does not establish ZIGZAG product loading. |

No family is written to `tokens.typography.family`; no unavailable family is rendered as a system substitute.

## Tier 2 attempts

- `https://getdesign.md/zigzag` — attempted through built-in web retrieval on 2026-07-13; the retrieval service rejected the unestablished direct URL as unsafe, so no record or token was consumed.
- `https://styles.refero.design/?q=ZIGZAG` — attempted through built-in web retrieval on 2026-07-13; the retrieval service likewise rejected the direct query URL as unsafe. A domain-restricted indexed search produced no ZIGZAG result.

Tier 2 is therefore unavailable, not positive evidence and not a basis for filling missing product states.

## Conflict Matrix

| Field | Previous reference | Tier 1 supplied capture | getdesign | refero | Resolution |
|---|---|---|---|---|---|
| Product primary / pink | `#fa6ee3` and `#f55dd6` presented as current brand/action tokens | neither value observed on home or the two current product pages | unavailable | unavailable | removed from current canonical tokens; no substitute promotion |
| Current foreground | mixed legacy gray/pink claims | repeated `#292b2b` body/card foreground; `#121212` selected tab | unavailable | unavailable | retain only evidence-local foreground/strong roles |
| Default product font | Pretendard promoted as loaded self-hosted UI font | computed `Pretendard JP` stack, but no FontFaceSet/source match | unavailable | unavailable | leave unresolved; omit typography family token and specimen |
| Product-card geometry | 8px thumbnail, 12px card/action claims | thumbnail/card root are 0px; root padding `0px 0px 8px` | unavailable | unavailable | replace with the captured roles only |
| Interactive states | hover, pressed, focus, purchase, filter, favorite, sheet, and toast variants | only selected tab and disabled quick-menu arrow observed; interaction coverage 0 | unavailable | unavailable | retain those two observed states; remove all other state claims |
| Components | broad CTA/input/filter/sheet/badge set | current product-card thumbnail/root, detail tab, and quick-menu arrow have selector provenance | unavailable | unavailable | retain only those four component records |

### Earlier mistakes reverted

- The older reference treated a legacy pink palette as a universal live product system even though the supplied current product capture does not contain it.
- It promoted `Pretendard` from a computed/declaration chain without the required loaded FontFace/source corroboration.
- It described checkout, filter, sheet, toast, sale, and motion variants that this capture did not observe.

No unresolved token conflict remains because contested legacy values are omitted rather than silently resolved to a plausible replacement.

## Context and narrative evidence

- https://brunch.co.kr/@zigzag/190 — first-party 2025 ZIGZAG Beauty publication; service positioning, category expansion, personal-taste discovery, and an app-linked offline pouch. Marketing context only.
- https://cf.res.s.zigzag.kr/zigzag/terms/user/ko/affiliate_terms_of_use-20250331/index.html — first-party terms; ZIGZAG as a mobile shopping service and transaction intermediary between sellers and users.
- https://devblog.kakaostyle.com/ko/2024-12-13-1-rebuilding-frontend-design-system/ — first-party engineering account of the ZDS rebuild, modular packages, CSS-variable tokens, and product-card unification.
- https://github.com/orioncactus/pretendard/blob/main/LICENSE — official Pretendard license boundary; external font evidence, not a ZIGZAG product-use statement.

The brand publication and engineering account deepen the narrative but do not change the source domain of public product tokens.
