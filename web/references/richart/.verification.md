# Richart — Verification Notes (2026-06-08)

## Proof — Tier 1 live inspect

**Inspected:** 2026-06-08
**Method:** playwright getComputedStyle attempted on https://www.richart.tw → hard 403 FORBIDDEN bot-block (2 tries, desktop + mobile UA, `--disable-http2`). Honest fallback: live CSS fetch of the same Richart property via apex `richart.tw`, which serves the full production HTML + v3 stylesheets. All values below measured verbatim from live CSS / HTML on 2026-06-08.
**Sources:**
- https://www.richart.tw (canonical homepage — bot-blocks automation, 403)
- https://richart.tw (apex, same Taishin/Richart brand property — served HTTP 200, full HTML)
- https://richart.tw/TSDIB_RichartWeb/static/revamp/css/v3/index.css (live production stylesheet, 375 KB)
- https://richart.tw/TSDIB_RichartWeb/static/revamp/css/v3/headerfooter.css (live production stylesheet, 30 KB)

### Raw samples
- `.btn-primary { background: #17B6C9; color: #FDFDFD; border-radius: 100px; font-family: 'Noto Sans TC'; font-weight: 500 }` → primary teal `#17b6c9`, pill 100px
- `.btn-primary:hover { background: #15A6B7 }` → hover teal `#15a6b7`
- `.btn-primary-xl { padding: 16px 0; font-size: 20px; line-height: 32px; width: 240px; background-image: radial-gradient(100% 256.33% at 0% 7.41%, #64cedb 0%, #15a6b7 100%) }` → XL gradient button
- `.btn-secondary { background: transparent; color: #17B6C9; border: 2px solid #17B6C9; border-radius: 100px }` → outline pill
- `.btn-primary.disabled { border-radius: 38px; background: #727D8C; color: rgba(253, 253, 253, 0.5) }` → disabled state
- `.title .line { width: 4px; height: 24px; background-color: #17B6C9 }` → signature 4px teal section-title accent bar
- `.title .txt { font-size: 18px; font-weight: 500; line-height: 28px; font-family: 'Noto Sans TC' }` → title type
- `.heading .txt { font-size: 1.125rem; font-weight: bold; color: #333 } / a[aria-expanded="true"] .txt { color: #17B6C9 }` → active link teal
- font-family frequency: `'Noto Sans TC'` ×55, `'Montserrat', 'Noto Sans TC'` ×6 → primary CJK + Latin display
- top text/bg hex by frequency: `#17b6c9` ×35, `#fdfdfd` ×27, `#2a3342` ×27, `#dce0e5` ×23, `#727d8c` ×20, `#f3f5f6` ×18, `#c7cdd4` ×17, `#364053` ×9, `#1b2028` ×9, `#64cedb` ×7 (all carried into §2)
- box-shadow (neumorphic): `0px 10px 44px rgba(102, 102, 102, 0.12), inset -3px -3px 10px rgba(84, 92, 101, 0.16), inset -5px -3px 12px rgba(193, 203, 221, 0.16)`
- border-radius frequency: `4px` ×20, `32px` ×6, `16px` ×6, `8px` ×3, `24px` ×3, `100px`/`50px` pills, `50%` circles
- transitions: `all 0.3s ease-out` (default), `background 0.5s`, `cubic-bezier(0.77, 0.2, 0.05, 1)`, `cubic-bezier(0, 1, 0.5, 1)`
- hero accent cyan `#2BE0EC` and dark teal `#001617` present in homepage HTML inline styles
- `<title>` → `Richart - 最能幫年輕人存到錢的銀行` (youth-save positioning, confirms brand intent)

## Country sources

Taiwanese / brand-owned / regional sources for the Richart (Taishin International Bank) brand:
1. **https://www.richart.tw** — official Richart product site (Taishin International Bank, .tw), brand homepage and canonical source.
2. **https://richart.tw** — Richart apex domain (same Taishin property); served the live HTML and production v3 CSS used for token extraction.
3. **https://www.taishinbank.com.tw** — Taishin International Bank (台新銀行), the Taiwanese parent bank that owns and operates the Richart digital-banking brand.
4. **App Store TW / Google Play TW** — the Richart 行動銀行 app listings (Taiwan storefront), the primary surface the web brand inherits from.

## Notes / honest caveats
- `www.richart.tw` is protected by a WAF that returns `403 FORBIDDEN` to headless automation and curl regardless of UA / HTTP version. getComputedStyle on the live DOM was therefore not obtainable.
- The apex `richart.tw` (identical Richart brand chrome, same Taishin property) is not bot-blocked and serves the full production HTML + the same v3 stylesheets that drive the brand. All token values are measured verbatim from that live production CSS, so the extraction is genuine Tier 1, just via the unblocked sibling host.
- primary_color `#17b6c9` is the literal `.btn-primary` fill, appearing 35× across the production CSS — the single dominant brand color.
