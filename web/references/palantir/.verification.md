# Palantir Blueprint — verification v2 evidence notes

Checked: 2026-07-14

Workflow: `omd:add-reference` CREATE, constrained to the supplied `artifacts/reference-evidence/palantir.json` for all raw computed-style, font, component, and interaction evidence. No browser recapture, MCP collection, or computed-style rerun was used.

## Proof — Tier 1 live inspect

**Inspected:** 2026-07-13
**Sources:** https://blueprintjs.com/ · https://blueprintjs.com/docs/
**Collector:** supplied `artifacts/reference-evidence/palantir.json`; two distinct public URLs, 12 detected component variants, one menu interaction, two observed interaction-state labels, and coverage score 63.

### Surface domains

| Surface ID | URL | Domain boundary |
|---|---|---|
| `home` | https://blueprintjs.com/ | Public Blueprint landing/product surface. It is not a Palantir corporate site or an authenticated Palantir platform. |
| `surface-2` | https://blueprintjs.com/docs/ | Official Blueprint documentation surface. It is the source of public Docs measurements, not a blanket specification for every Blueprint app. |
| `palantir/blueprint` | https://github.com/palantir/blueprint | Official source repository and project/license context; not a computed-style source. |
| Palantir corporate pages | https://www.palantir.com/ | Company mission/culture context only; not a Blueprint UI-token source. |

### Raw samples

- `home::body` landing canvas — background `rgb(17, 20, 24)` = `#111418`; text `rgb(28, 33, 39)`; 0px radius; 14px / 400 / 18.0013px operating-system-first stack.
- `home::h1` landing title — text `rgb(255, 255, 255)` = `#ffffff`; 28px / 400 / 33.6px; computed operating-system-first stack.
- `home::[data-omd-capture="0"]` minimal landing action — transparent background; text `rgba(255, 255, 255, 0.7)`; 4px radius; `4px 16px` padding; 40px height; 16px / 400.
- `home::[data-omd-capture="2"]` filled landing action — background `rgb(45, 114, 210)` = `#2d72d2`; text `rgb(255, 255, 255)` = `#ffffff`; 4px radius; `4px 8px` padding; 30px height; 14px / 400.
- `surface-2::h1` Docs title — text `rgb(28, 33, 39)` = `#1c2127`; 36px / 600 / 40px; computed operating-system-first stack.
- `surface-2::p` Docs body — text `rgb(28, 33, 39)` = `#1c2127`; 16px / 400 / 24px; computed operating-system-first stack.
- `surface-2::div.bp6-card.bp6-elevation-0.bp6-interactive` Docs welcome card — background `oklch(1 0 257.113)`; text `rgb(33, 93, 176)` = `#215db0`; 4px radius; 20px padding; 122px height; shadow `rgba(0, 0, 0, 0.15) 0px 0px 0px 1px, rgba(0, 0, 0, 0.02) 0px 0px 5px 0px`.
- `surface-2::[data-omd-interaction-capture="menu-0-0"]` expanded version menu — background `rgb(255, 255, 255)` = `#ffffff`; text `rgb(28, 33, 39)` = `#1c2127`; 4px radius; 4px padding; 187px height.

## Font evidence

| Family | Bundle status | Visible use | Font/source evidence | Resolution |
|---|---|---:|---|---|
| `-apple-system` | system / high | 112 | Computed operating-system-first stack; no source URL | Not a Blueprint UI-family token and never a substitute specimen. Measured size/weight/line-height roles are retained without a family token. |
| `blueprint-icons-16` | declared / medium | 0 | Five landing and five Docs font asset URLs in the supplied `@font-face` evidence | Declared-only icon font; not a text family or UI token. |
| `blueprint-icons-20` | declared / medium | 0 | Five landing and five Docs font asset URLs in the supplied `@font-face` evidence | Declared-only icon font; not a text family or UI token. |
| `codicon` | declared / medium | 0 | One Docs font asset URL in the supplied evidence | Declared-only; not a text family or UI token. |

Blueprint’s official Docs say the icon CSS must be loaded alongside the core stylesheet for icon-font support. The official repository states that the project is available under Apache 2.0. Neither statement turns the icon fonts into a named text face, a current live-use fact, or a separate redistribution grant for every hosted artifact.

## Context and narrative evidence

| Source | Usable fact | Evidence boundary |
|---|---|---|
| https://blueprintjs.com/ | Blueprint is an open-source project developed at Palantir. | Product identity/history context; it does not supply extra unmeasured component tokens. |
| https://blueprintjs.com/docs/ | Blueprint is a React-based toolkit for complex, data-dense desktop web interfaces; the site presents v6.x as available. | Official product/documentation context and supplied Docs surface; only supplied computed values become tokens. |
| https://github.com/palantir/blueprint | Official repository describes packages for core components, icons, datetime, select, and table, and names Apache 2.0. | Repository and licence context; not a substitute for raw page measurements. |
| https://www.palantir.com/offerings/energy/ | Palantir says it was founded in 2003 to address critical data issues while protecting civil liberties, and now describes platforms for data, decisions, and operations. | Corporate/company narrative only; no Palantir platform styling is imported into Blueprint. |
| https://www.palantir.com/careers/infrastructure | Palantir presents “The Best Idea Wins,” “Own The Outcome,” and “Focus on the Mission” as values. | Culture/voice context only; not a product copy or component-token source. |

## Tier 2 attempts

| Service | Attempt | Result | Handling |
|---|---|---|---|
| getdesign | https://getdesign.md/palantir | Built-in web open returned an internal retrieval error on 2026-07-14. | No Tier 2 value or absence conclusion was used. |
| Refero | https://styles.refero.design/?q=Palantir%20Blueprint | Built-in web open returned an internal retrieval error on 2026-07-14. | No Tier 2 value or absence conclusion was used. |

## Conflict matrix

| Field | Tier 1 (supplied live / official) | getdesign | refero | Resolution |
|---|---|---|---|---|
| Product scope | Official landing and Docs call Blueprint a React UI toolkit for the web; Docs target complex, data-dense desktop interfaces. | Retrieval error | Retrieval error | Retain official scope; Tier 2 contributed no competing claim. |
| Primary blue | Filled landing action is `#2d72d2`; color aggregate repeats it across both supplied surfaces. | Retrieval error | Retrieval error | Keep as captured-scope primary only. |
| Documentation card | Selector-backed Docs card has raw white-equivalent `oklch(1 0 257.113)`, `#215db0` foreground, 4px radius, 20px padding, 122px height, and measured shadow. | Retrieval error | Retrieval error | Promote this static default card only. |
| Text family | Operating-system-first stack is computed/high-use; no named text family has loaded FontFaceSet evidence. | Retrieval error | Retrieval error | Omit `tokens.typography.family`; retain measured typography metrics. |
| Icon fonts | Three icon families are declared with zero visible uses; Docs require icon CSS for icon support. | Retrieval error | Retrieval error | Retain declared-only narrative; do not promote a text family. |
| Interaction states | One Docs version-menu expansion records `expanded` and `menu-open`. | Retrieval error | Retrieval error | Preserve as interaction provenance only; do not generalize hover, focus, pressed, disabled, dialog, toast, error, or motion. |

**Conflicts unresolved:** none

## Component provenance and retention boundary

- `surface-2::div.bp6-card.bp6-elevation-0.bp6-interactive` is the sole structured component token. It is a static/default card sample with a named Blueprint card class, a selector, a documentation surface, and measured geometry. Its `card` type is one of the canonical allowed types.
- `home::[data-omd-capture="0"]`, `home::[data-omd-capture="2"]`, and `surface-2::[data-omd-capture="21"]` are measured button/action defaults. They are not tokenized because the capture does not provide a truthful state summary for those specific variants.
- `surface-2::[data-omd-capture="2"]` and the `menu-0-*` target selectors document the one observed version-menu expansion. The target row `menu-0-2` is a `menuitem` link/list item, not a button token.
- No input, tab, toggle, toast, dialog, avatar, generic menu, hover, focus, pressed, disabled, error, loading, success, animation, or responsive token is created beyond the evidence boundary.
