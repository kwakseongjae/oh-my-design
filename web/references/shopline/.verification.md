# SHOPLINE — Verification Notes (2026-06-08)

## Proof — Tier 1 live inspect

**Inspected:** 2026-06-08
**Method:** playwright getComputedStyle (live DOM)
**Sources:**
- https://shopline.tw (homepage, live DOM — page title "SHOPLINE 全方位零售整合專家｜完整開店功能讓你的生意不斷成長")

Launched headless Chromium (`--disable-http2`), `goto` with `waitUntil: 'networkidle'`, dismissed modals via Escape + close-button clicks, then captured `getComputedStyle` on body, h1, h2, the primary CTA buttons, nav links, paragraph text, and section containers. `rgb()` values converted to hex.

### Raw samples

- `body` background-color: `rgb(255, 255, 255)` → `#ffffff`; color: `rgb(0, 20, 45)` → `#00142d`; font-family: `"Noto Sans TC", -apple-system, system-ui, ...`; font-size: `16px`
- Primary CTA button `了解所有方案`: background `rgb(53, 109, 255)` → `#356dff`, color `#ffffff`, border-radius `30px`, height `45px`, font-size `18px`, font-weight `700`, padding `0px 24px`
- Outlined CTA `了解更多`: background `#ffffff`, color `rgb(53, 109, 255)` → `#356dff`, border-radius `30px`, height `45px`, font-size `18px`, font-weight `700`
- Dark CTA `立即預約 GO!`: background `rgb(0, 20, 45)` → `#00142d`, color `#ffffff`, border-radius `30px`, height `43.9px`, font-size `16px`, font-weight `700`, padding `8px 24px`
- Nav header CTA `免費試用`: background `rgb(0, 0, 0)` → `#000000`, color `#ffffff`, border-radius `30px`, height `38px`, font-size `14px`, font-weight `700`
- `h1` (`全方位零售整合專家`): font-size `48px`, font-weight `700`, color `rgb(255, 255, 255)` → `#ffffff`, line-height `63.76px`
- `h2` (`適用各種規模…`): font-size `40px`, font-weight `700`, color `#ffffff`, line-height `54.8px`
- `nav` link: font-size `16px`, font-weight `400`, color `rgb(0, 20, 45)` → `#00142d`
- `p` body text: font-size `13px`, font-weight `400`, color `rgb(0, 20, 45)` → `#00142d`, line-height `24.1px`
- Section backgrounds: `rgb(237, 244, 253)` → `#edf4fd`, `rgb(242, 247, 252)` → `#f2f7fc`, `rgb(0, 20, 45)` → `#00142d`
- Box-shadows: `rgba(0, 0, 0, 0.08) 0px 8px 16px 0px`, `rgba(0, 0, 0, 0.08) 0px 4px 24px 0px`, `rgba(200, 200, 200, 0.27) 2px 2px 6px 0px`
- Border-radii observed: `30px` (pill buttons), `8px`, `9px`, `6px`, `3px`, `50%` (avatars/icons)

## Country sources

Taiwanese / regional brand-owned sources (Tier 1 regional — used to confirm SHOPLINE is a TW/HK commerce SaaS and its zh-TW brand voice):

- https://shopline.tw — official Taiwan site (zh-TW, 繁體中文), live-inspected above. Brand positioning "全方位零售整合專家" (all-in-one retail integration expert).
- https://blog.shopline.tw — SHOPLINE Taiwan official blog (電商教學 / 開店知識 content hub, zh-TW).
- https://shopline.hk — SHOPLINE Hong Kong official site (regional sibling, confirms TW/HK commerce footprint).
- https://apps.apple.com/tw/app/shopline/id1142985013 — SHOPLINE app on the Taiwan App Store (regional store listing).
