# Dabang — verification v2 evidence notes

**Reference:** `dabang`
**Checked:** 2026-07-13
**Method:** supplied deterministic collector evidence (`artifacts/reference-evidence/dabang.json`) plus first-party and Tier 2 built-in web checks. No browser capture was rerun and no MCP session was used.

## Scope and source domains

| Source | Domain | Role in this update | Boundary |
|---|---|---|---|
| `https://www.dabangapp.com/` | Public product home | Home header controls, search-entry geometry, public type/color/font evidence | No authenticated search, listing detail, or transaction flow was captured. |
| `https://www.dabangapp.com/map/onetwo?m_lat=37.494367328004216&m_lng=127.01446798508894&m_zoom=11` | Public product map | Map location search, dock controls, map-local action color, public type/font evidence | No map interaction expansion, selected-listing flow, marker semantics, or drawer state was captured. |
| `https://www.dabangapp.com/service/faq` | Support documentation | FAQ-row/support chrome and public type/font evidence | Not product-map or listing UI; not a source for universal product tokens. |
| `https://www.station3.co.kr/service/` | Official company/service context | Official relationship of Dabang, Dabang Pro, and Dabang Bangjoo-in | Corporate/service narrative only; no CSS tokens or components taken from it. |
| `https://static.dabangapp.com/html/useragreement.html` | Official legal/service context | Service role and platform boundary for individual users, brokers, landlords, and listings | Legal/service context only; no visual token source. |
| Pretendard upstream project and LICENSE | Official font and licence context | Font project context and SIL Open Font License 1.1 boundary | Neither source alone proves Dabang loaded the font. |

The supplied artifact was captured at `2026-07-13T11:23:45.806Z`. It records three public routes, three component types, 36 captured component variants, three static observed-state labels, zero interaction expansions, and coverage score 78.

## Proof — Tier 1 live inspect

**Inspected:** 2026-07-13
**Sources:** `https://www.dabangapp.com/`, `https://www.dabangapp.com/map/onetwo?m_lat=37.494367328004216&m_lng=127.01446798508894&m_zoom=11`, `https://www.dabangapp.com/service/faq`
**Evidence artifact:** `artifacts/reference-evidence/dabang.json`

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| `home`, `home::[data-omd-capture="4"]` | Header account control: `rgb(255, 255, 255)` / `#ffffff`, `rgb(34, 34, 34)` / `#222222`, 8px radius, `8px 16px` padding, Pretendard Variable 16px/400/26px. |
| `home`, `home::[data-omd-capture="5"]` | Header outline action: transparent background, `#222222`, 1px `#dfdfdf` border, 2px radius, `0px 16px` padding, Pretendard Variable 14px/700/24px. |
| `home`, `home::[data-omd-capture="18"]` | Home pill control: `rgb(255, 255, 255)` / `#ffffff`, `#222222`, 1px `#dfdfdf` border, 32px radius, `0px 11px` padding, Pretendard Variable 14px/700/24px. |
| `surface-2`, `surface-2::[data-omd-capture="1"]` | Map location-search field: `rgb(255, 255, 255)` / `#ffffff`, `#222222`, 1px `#ededed` border, 42px radius, `7px 37px 7px 15px` padding, Pretendard Variable 14px/400/24px. |
| `surface-2`, `surface-2::[data-omd-capture="14"]` | Map dock control: `#ffffff`, `rgb(0, 0, 0)` / `#000000`, 1px `#dfdfdf` border, 2px radius, `0px 7px 0px 11px` padding, Pretendard Variable 14px/400/24px. |
| `surface-2`, `surface-2::[data-omd-capture="22"]` | Map tool default: `#ffffff`, `#326cf9` text/border, 6px 6px 0px 0px radius, `6px` padding, Pretendard Variable 12px/700/20px. This is map-local evidence; its pseudo-state samples are not promoted. |
| `surface-3`, `surface-3::[data-omd-capture="10"]` | Support FAQ row: transparent background, `#000000`, 1px block-end `#f5f5f5` border, `16px 20px` padding, Pretendard Variable 13.3333px/400. |
| `surface-3`, `surface-3::h1` | Support heading: `#222222`, Pretendard Variable 46px/700/70px. Support-documentation hierarchy only. |

## Font evidence and resolution

| Family / claim | Computed surface use | FontFaceSet / source corroboration | Official asset or licence | Resolution |
|---|---|---|---|---|
| `Pretendard Variable` | 376 visible first-family uses across body, button, heading, input, list, and text roles on home, map, and FAQ routes | `loaded` / high confidence; 92 Dabang-hosted dynamic-subset WOFF2 URLs under `static.dabangapp.com/web/fonts/pretendard-variable/v1.3.9/` | Pretendard upstream project and LICENSE; SIL Open Font License 1.1 | **Verified current public-web UI family.** Sole `tokens.typography.family.ui` entry. |
| Fallback families in computed stacks | Present after the visible first family | No need for a fallback to resolve the retained samples | N/A | System fallback only; not a Dabang font claim. |
| Other font families | None supplied as loaded, visible first-family usage | No FontFaceSet/source corroboration in this bundle | N/A | **Unresolved/absent.** No family is added from a plausible system stack or declaration. |

The upstream licence establishes the availability of Pretendard itself. The Dabang UI-family claim depends on the joined evidence of visible computed first-family use, loaded FontFaceSet status, and Dabang-hosted source URLs.

## Component provenance and state boundary

| Component claim | Surface and selector provenance | Default evidence | State evidence | Resolution |
|---|---|---|---|---|
| Header account control | Product home, `home::[data-omd-capture="4"]` | white, `#222222`, 8px, `8px 16px`, 16px/400 | None | Preserve default only. |
| Header outline action | Product home, `home::[data-omd-capture="5"]` | transparent, `#222222`, 1px `#dfdfdf`, 2px, `0px 16px`, 14px/700 | Static hover/pressed selector copies exist, but no interaction record | Preserve default only; do not claim pseudo-state behaviour. |
| Map location search | Product map, `surface-2::[data-omd-capture="1"]` | white, `#222222`, 1px `#ededed`, 42px, `7px 37px 7px 15px`, 14px/400 | Static focus selector copy exists, but no interaction record | Preserve default only; do not claim focus styling. |
| Map dock control | Product map, `surface-2::[data-omd-capture="14"]` | white, `#000000`, 1px `#dfdfdf`, 2px, `0px 7px 0px 11px`, 14px/400 | Static hover/pressed selector copies exist, but no interaction record | Preserve default only. |
| Map tool | Product map, `surface-2::[data-omd-capture="22"]` | white with `#326cf9` action ink/border, top 6px corners, `6px` padding, 12px/700 | Static hover/pressed selector copies include `#eef8ff`, but no interaction record | Retain map-local action-color observation; do not create a reusable state or component token. |
| Support FAQ row | Support documentation, `surface-3::[data-omd-capture="10"]` | transparent, `#000000`, `#f5f5f5` divider, `16px 20px`, 13.3333px/400 | None | Documentation chrome only; do not blend into product components. |

The artifact’s `interactionCount` is `0` and `interactions` is `[]`. Its selector strings with `::state-hover`, `::state-focus`, or `::state-pressed` are static samples, not interaction provenance. They do not establish reusable state variants.

## Tier 2 cross-check

| Service | Attempt | Result | Use |
|---|---|---|---|
| getdesign | `https://getdesign.md/dabang`; built-in web open | Non-retryable safe-open failure; subsequent built-in web search for both `dabang` and `다방` on getdesign returned no result. | No token or component values used. |
| Refero | `https://styles.refero.design/?q=dabang`; built-in web open | Non-retryable safe-open failure; subsequent built-in web search for both `dabang` and `다방` on Refero returned no result. | No token or component values used. |

## Conflict matrix

| Field | Tier 1 (supplied capture / official source) | getdesign | refero | Resolution |
|---|---|---|---|---|
| Public UI family | Pretendard Variable loaded/high, 376 visible uses, 92 Dabang-hosted source URLs | No usable record | No usable record | Promote as the sole current public-web UI-family token. |
| Product canvas and foreground | `#ffffff` canvas and repeated `#222222` foreground across home/map | No usable record | No usable record | Retain only observed values. |
| Map action color | `#326cf9` observed on the map route | No usable record | No usable record | Keep map-local; do not generalize to all product CTAs. |
| Pink identity metadata | Frontmatter records `#ff3478`; supplied capture does not establish it as current reusable control styling | No usable record | No usable record | Keep catalog identity metadata only; remove former global functional-color claim. |
| FAQ rows | Support-documentation selector provides row geometry | No usable record | No usable record | Keep as documentation chrome, outside product tokens. |
| Legacy status colours, token ramps, map markers, card/shadow system, interaction states, and motion | Not substantiated by the supplied 2026 evidence | No usable record | No usable record | Roll back rather than retain plausible defaults. |

## Context and narrative evidence

- **Station3 service page:** officially lists Dabang, Dabang Pro, and Dabang Bangjoo-in and describes a direction beyond residential space toward better ways of living. Used for §§1 and 11–13 only, not for CSS-token derivation.
- **Dabang service terms:** identifies Station3 as the operator; defines individual users, licensed-broker members, non-members, landlords, listings, and the information-platform role. It states that listings and user-provided content have source-party boundaries, and that Dabang is not itself a party to users’ property transactions. Used for §§1 and 10–13 only.
- **Dabang product-home search result:** officially describes the public service as covering one/two-room homes, houses/villas, officetels, and apartments. Used as product-category context only; the supplied capture remains the evidence for visual claims.
- **Pretendard upstream project and LICENSE:** establish upstream project/asset and SIL Open Font License 1.1 boundary. They do not establish Dabang use without the captured computed/font-source evidence above.

## Reconciliation notes

- The prior reference conflated an old capture with a full CSS custom-property ramp, inferred semantic status meanings, assumed map-marker and listing-card contracts, and universalized a zero-shadow rule. The supplied artifact does not provide those claims with current selector/surface/state provenance, so they were removed.
- Home and map are product surfaces; the FAQ is support-documentation chrome. Corporate and legal pages are narrative-context sources. Each source domain is retained only for the facts it actually supports.
- No public trust status is assigned here. The deterministic evaluator owns that outcome.
