# PIXNET — Verification Notes (2026-06-08)

## Proof — Tier 1 live inspect

**Inspected:** 2026-06-08
**Method:** playwright getComputedStyle (live DOM) — global playwright, headless Chromium with `--disable-http2`. Page navigated to https://www.pixnet.net and rendered; `lab()` computed-color values resolved to `#rrggbb` via an in-page canvas `getImageData` conversion. Top-15 DOM color frequencies sampled across ~1200 elements.

**Sources:**
- https://www.pixnet.net (homepage, live DOM)

### Raw samples
- `body` → `font-family: "Noto Serif TC", "Noto Serif TC Fallback"`; `font-size: 16px`; `line-height: 24px`; `background-color: #ffffff`; `color: #423e3c`
- Login CTA (`登入`) → `background-color: #ff7200`; `color: #ffffff`; `border-radius: 6px`; `height: 36px`; `font-size: 14px`; `font-weight: 500`; `padding: 8px 12px`
- Trending tag pill (`#長榮航空`, `#日本旅遊`) → `background-color: #ffffff`; `color: #423e3c`; `border: 1px solid #eaeae9`; `border-radius: 6px`; `height: 36px`; `font-size: 16px`; `font-weight: 700`
- `h1` → `font-size: 16px`; `font-weight: 400`; `color: #423e3c`; `letter-spacing: 1.2px`
- Header → `background-color: #ffffff`; `color: #423e3c`; `height: 73px`
- Footer → `background-color: #f4f4f4`; `color: #423e3c`
- Nav channel link (`gogo+香港`) → `color: #575451`; `font-size: 18px`; `font-weight: 700`
- DOM color frequency (top): `#eaeae9` ×1083 (hairline), `#423e3c` ×902 (ink), `#969492` ×111, `#ff7200` ×68 (brand orange), `#ff432b` ×48 (accent red), `#817f7d` ×41, `#fcdfda` ×8 (peach tint)
- Page `<title>` → `痞客邦PIXNET-掌握最新熱門話題貼文、短影音，讓生活充滿靈感！`

**Notes / honesty:** `page.goto` with `waitUntil: 'networkidle'` timed out (the homepage holds long-lived analytics/ad connections), so the DOM was captured on `domcontentloaded` + a 2.5s settle. All samples above are real measured computed values from the fully rendered live DOM. Original computed colors were `lab()`; the `#hex` equivalents listed are exact canvas conversions (e.g. `lab(65.21 51.88 73.57)` → `#ff7200`, `lab(26.57 1.46 1.88)` → `#423e3c`).

## Country sources

Brand-owned / regional Taiwanese sources (Traditional Chinese, TW-owned):
- https://www.pixnet.net — PIXNET (痞客邦) official site, Taiwan. Self-described as 「台灣人的生活文創平台」(a life and creative-culture platform for Taiwanese people).
- https://www.pixnet.net/about / https://www.pixnet.net/company — PIXNET official company/about pages (TW corporate identity, 痞客邦股份有限公司).
- https://apps.apple.com/tw/app/痞客邦/id389796636 — PIXNET official iOS app on the Taiwan App Store (regional TW listing by the brand).

These are brand-owned and TW-regional; non-regional aggregators (getdesign.md, refero.design) and the Google favicon proxy are intentionally excluded from the country-source gate.
