# OTTO — Verification Notes (2026-09-26)

Pipeline: EU wave 2, #4 · Brand: OTTO (Otto Group, Hamburg)

## Scope and collector boundary

- Local Playwright-driven Chrome (Tier 1), headless, 1440×1000, `de-DE`.
- **Consent:** OneTrust, declined with `#onetrust-reject-all-handler`.
- Product pages (`/p/…`) returned HTTP 400 to automation on every attempt (subagent, three
  products); the basket button is not measured.
- Logged out; nothing added to the basket. Desktop only. Captured and authored 2026-09-26.

### Two collectors

A delegated subagent (Sonnet, headless only) ran the deep probe. Re-measured independently with the
fixed tool (which now also compares background-image and text-decoration):

- Product tile link on `/suche/sofa/`: rest `#2366cb`; hover `#1c50a0` (fg, border, decoration
  colour; `:hover` true). Press and focus were unmeasured on this pass; the pressed value
  `#143b75` is the subagent's.
- Header search field: no change on hover, press or focus, all matched.

The header-link reading is the subagent's. The subagent's radius and shadow tokens were read from
its raw dump.

### Design-system name

`designsystem.otto.de` → 200, "OTTO Design System" (zeroheight); `/zz-this-does-not-exist` → **404**,
"zeroheight - not found", 1,164 bytes. The legacy docs at `www.otto.de/design-system/` are a client
app that returns the same shell for every path; rendered, `getting-started` has the title "Getting
Started - OTTO Design System" and the sentence "we are happy to introduce Design System powered by
OTTO Components!", while the nonsense path renders "404 - OTTO Design System" with a shorter body.
The `--ofc-*` prefix is not expanded in any source; the reference describes it by content only.

### Country

Otto GmbH & Co. KGaA, Hamburg-Bramfeld. Recorded as **DE**.

## Namespace ownership

729 root properties (841 in a stylesheet walk): `--oc-*` ~630, `--ofc-*` 92, `--font-*` 12,
`--icon-*` 7, `--label-*` 6, `--switch-*` 5, `--cookie-*` 3, `--banner-*` 2. No framework namespace.

## Proof — Tier 1 live inspect

**Inspected:** 2026-09-26 · **Source:** https://www.otto.de/ · 729 root properties.

### Raw samples — semantic colour

- `--oc-semantic-color-brand` `#eb001f` · `--oc-semantic-color-text-brand` `#eb001f` ·
  `--oc-semantic-color-background-brand` `#eb001f`
- `--oc-semantic-color-background-primary` `#dc001d` · `--oc-semantic-color-sale-background` `#dc001d`
  · `--oc-semantic-color-text-sale` `#dc001d`
- `--oc-semantic-color-text-default` `#212121` · `--oc-semantic-color-text-interactive` `#2366cb` ·
  `--oc-semantic-color-text-hint` `#2366cb` · `--oc-semantic-color-background-hint` `#dcf2ff`
- `--oc-semantic-color-background-frame` `#f0f0f0` · `--oc-semantic-color-background-canvas` `#fff` ·
  `--oc-semantic-color-disabled-background` `#e5e5e5` · `--oc-semantic-color-disabled-border` `#ccc`
  · `--oc-semantic-color-disabled-text` `#949494`
- `--oc-semantic-color-text-success` `#227642` · `--oc-semantic-color-text-warning` `#ae4518` ·
  `--oc-semantic-color-text-error` `#b30023` · `--oc-semantic-color-text-sustainable` `#50734d`

### Raw samples — type, geometry, census

- `document.fonts` loaded: OttoSans 400, 700, 800; declared `OTTOSans, Arial, Helvetica, sans-serif`
- `--oc-semantic-shadow-above` `drop-shadow(0px 1px 2px #00000040) drop-shadow(0px 4px 6px #0000001a)`
  · `--oc-semantic-shadow-stacked` empty
- Radii 0, .25rem, .5rem, .75rem, 1rem, 1.5rem; focus radii +2px
- Text: `#212121` ×3,247 · `#000` ×1,241 · `#2366cb` ×364 · `#6d6d6d` ×81. `<p>`: `#212121` 62 of 65.

### Raw samples — controls

| control | rest | hover | pressed | focus |
|---|---|---|---|---|
| Product tile link (re-measured) | transparent / `#2366cb`, 14px/400 | `#1c50a0` | `#143b75` (subagent) | not measured |
| Search field (re-measured) | transparent / `#212121`, 48px, `1px 2px 1px 16px`, 16px/400 | no change | no change | no change |
| Mein Konto (subagent) | transparent / `#212121`, 40px | no change | — | — |

## Narrative sources

- https://www.ottogroup.com/en/ueber-uns/historie-und-gruender.php — "On August 17, the 40-year-old
  Werner Otto founds 'Werner Otto Versandhandel,' a mail-order company. The company has three
  employees"; "humble beginnings in Hamburg-Schnelsen"; 1960 move to Hamburg-Bramfeld, "where the
  Otto Group is based today".
- https://www.mutabor.de/work/otto — "Otto und MUTABOR: Corporate Design Relaunch mit expressiver
  Schrift"; OTTO Play, the new brand face with contextual OpenType alternates.
- https://page-online.de/kreation/clevere-open-type-hausschrift-fuer-otto/ — PAGE, 15.02.2022,
  "Clevere OpenType Hausschrift für OTTO": Mutabor and C/O Setzkorn Kemper with type designer Henning
  Skibbe; OTTO Play mixed with OTTO Sans.
