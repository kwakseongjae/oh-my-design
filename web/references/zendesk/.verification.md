# Zendesk Garden — Verification Notes (2026-07-13)

## Proof — Tier 1 live inspect

Machine artifact: `artifacts/reference-evidence/zendesk.json` (captured `2026-07-13T15:03:36.575Z`). This CREATE record uses the supplied artifact as its only raw computed-style, font, component, and interaction evidence. No browser recapture, browser-harness session, MCP call, or computed-style inference was run.

**Inspected:** 2026-07-13

**Sources:**

1. Design-system home: https://garden.zendesk.com/
2. Public component documentation: https://garden.zendesk.com/components/button
3. Public color documentation: https://garden.zendesk.com/design/color

The packet reports three supplied public surfaces, 43 component variants across button/input/listItem classifications, one observed static state, zero interaction kinds, zero interactions, and coverage 70. These are Garden documentation surfaces. They do not establish signed-in Zendesk product UI, Zendesk corporate marketing CSS, or unauthenticated sign-in-page CSS.

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| `home::header` | `rgb(255, 255, 255)` background, `rgb(41, 50, 57)` text, 80px height, `0px 16px` padding, and `rgba(10, 13, 14, 0.04) 0px 16px 24px 0px` shadow. |
| `home::h1` | `rgb(41, 50, 57)` text, 48px / 700 / 56px, 0px radius. |
| `surface-2::h1` | `rgb(41, 50, 57)` text, 48px / 700 / 52px, 0px radius. |
| `surface-2::#how-to-use-it` | `rgb(41, 50, 57)` text, 36px / 600 / 44px, 0px radius. |
| `surface-2::[data-omd-capture="90"]` | Native `button`: `rgb(31, 115, 183)` background, `rgb(255, 255, 255)` text, 4px radius, `0px 15px` padding, 40px height. |
| `surface-2::[data-omd-capture="64"]` | Native icon `button`: transparent background, `rgb(92, 105, 112)` text, 4px radius, 0px padding, 40px height. |
| `surface-2::li` | Static list row: transparent background, `rgb(41, 50, 57)` text, 0px radius, 14px / 400 / 20px, 32px height. |
| `home::[data-omd-capture="4"]` | Input/combobox: transparent background, `rgb(41, 50, 57)` text, `rgb(176, 184, 190)` border color, 14px / 400 / 18px. |

### Font evidence

| Evidence class | Result |
|---|---|
| Official product-use | Garden’s official Typography documentation describes type components and a scale, but the discovery pass found no first-party statement assigning a named font family to Zendesk product UI. |
| Live computed surface-use | The artifact records only `system-ui` as a high-confidence operating-system stack, with 733 uses across body, button, heading, input, listItem, and text roles. It has no browser-served font source URL. |
| Official distributed brand asset | `fontOrLicenseUrls` is empty in the supplied packet. The official Garden/GitHub sources inspected for this pass did not yield a font asset or public font license that can be promoted. |
| Declared-only | None recorded. |
| System / unresolved | The system stack is a runtime fallback environment, not a Zendesk brand face and not a permitted substitute specimen. |

## Tier 2 attempts and conflict matrix

| Field | Tier 1 supplied capture / official source | getdesign | Refero | Resolution |
|---|---|---|---|---|
| Primary documentation action | Native button on `surface-2`: `#1f73b7` / white, 4px, 40px, `0px 15px` | Attempted `https://getdesign.md/zendesk`; built-in open returned an internal error | Attempted `https://styles.refero.design/?q=zendesk`; built-in open returned an internal error | Keep the selector-backed Tier 1 default. |
| Documentation icon button | Native button on `surface-2`: muted `#5c6970`, 4px, 40px | No usable response | No usable response | Keep the static Tier 1 geometry only. |
| Sidebar row | Static `li` on `surface-2`: `#293239`, 0px radius, 32px height | No usable response | No usable response | Classify as `listItem`; do not infer button semantics. |
| Garden palette semantics | Official Palette identifies the same observed values as blue-700, grey-900/700/400, green-500, and red-700 | No usable response | No usable response | Use official palette only to name semantic context; token values still trace to the supplied packet. |
| Typeface | One computed system stack; no served font URL or official product-family assignment | No usable response | No usable response | Do not create a Zendesk typography-family token. |
| Hover/focus/pressed/disabled/error/loading | `interactionCount: 0` | No usable response | No usable response | Omit interactive state values; retain static defaults. |

Both Tier 2 endpoints were attempted. Their built-in opens returned internal errors; this is an attempt record, not a conclusion that either index lacks a Zendesk entry. There is no unresolved promoted-token conflict. The required no-conflict form is `conflicts: []` in `verification_v2` and `**Conflicts unresolved:** none` in the canonical footer.

## Components and provenance

- **Primary documentation button:** `surface-2::[data-omd-capture="90"]` is a native `button` with `inputType: button`, 40px height, blue fill, white text, 4px radius, and 0px 15px padding. It is a selector-backed static component token. No interaction state is recorded.
- **Icon button:** `surface-2::[data-omd-capture="64"]` is a native `button` with `inputType: button`, 40px height, muted text, and 4px radius. It is a selector-backed static component token. No interaction state is recorded.
- **Sidebar list item:** `surface-2::li` is a static `li` with 32px height, 0px radius, and foreground text. It is correctly represented as `listItem`, not a button.
- **Input/combobox:** `home::[data-omd-capture="4"]` establishes only a raw input border/text observation. It is not promoted because the capture does not provide enough component geometry or state evidence for a reusable input token.
- **Links/anchors:** several high-occurrence captured anchors have link semantics. They are not promoted as `button` tokens.

The collector recorded no hover, pressed, focus, expanded, dialog-open, toast, validation, disabled, or loading interaction. Static component geometry remains documented; only unobserved interactive states are omitted.

## Context and narrative evidence

- https://garden.zendesk.com/ — official Garden home. It calls Garden Zendesk’s design system, source of truth, and an evolving shared knowledge library spanning design, content strategy, and engineering; it also announces Garden 9. Used for §§1, 10–13 only and for source-domain identification, not for raw computed token values beyond the supplied packet.
- https://garden.zendesk.com/design/palette/ — official palette. It describes UI and brand palettes and provides role context for UI colors. Used for §2 narrative context; the machine tokens retain supplied-packet provenance.
- https://garden.zendesk.com/components/typography/ — official component documentation. It documents typography components and a type scale but does not establish a named Zendesk product font in this pass. Used for §3 boundary only.
- https://garden.zendesk.com/patterns/loaders/ — official loaders guidance. Used for §14 purpose-level loading context only; it provides no promoted computed loader token here.
- https://www.zendesk.com/company/ — official company/about page. It describes Zendesk’s customer-connection mission and the kitchen-table origin story. Used for §§1, 10–13 only, not Garden CSS.
- https://support.zendesk.com/hc/en-us/articles/9850316391322-Announcing-an-updated-look-to-the-team-member-and-end-user-sign-in-experiences — official 2025 announcement of a modernized sign-in experience aligned to current UI frameworks and brand standards. Used only as current-evolution context, not Garden component or token evidence.
- https://github.com/zendeskgarden — official Garden organization page. Used as a brand-asset/source-domain check only; no font file or license is inferred from it.

Official company context, public Garden documentation, the supplied static documentation capture, sign-in evolution, and absent font-license evidence remain separate domains. No fallback family, adjacent marketing surface, or inferred component is promoted as a Garden brand fact.

---

**Verified-against:**

- Tier 1 supplied artifact: `artifacts/reference-evidence/zendesk.json`, captured 2026-07-13; Garden home, button documentation, and color documentation only.
- Tier 1 official context: https://garden.zendesk.com/, https://garden.zendesk.com/design/palette/, https://garden.zendesk.com/components/typography/, https://garden.zendesk.com/patterns/loaders/, https://www.zendesk.com/company/, and the official sign-in-refresh announcement above.
- Official brand asset check: https://github.com/zendeskgarden.
- Tier 2: https://getdesign.md/zendesk and https://styles.refero.design/?q=zendesk (both attempted; built-in opens returned internal errors, with no absence conclusion).
