# ソフトバンク verification — 2026-07-13

## Proof — Tier 1 supplied live inspect

Machine artifact: `artifacts/reference-evidence/softbank.json` (captured `2026-07-13T14:58:43.002Z`). This CREATE packet uses that supplied artifact as the only raw computed-style, font, component, and interaction evidence. No browser recapture and no MCP session were run.

**Inspected:** 2026-07-13
**Coverage:** 3 public surfaces / score 71 / 4 component types / 40 variants / 0 observed states / 0 interactions. All supplied captures are 1440×900 desktop views.

**Sources:**

1. Consumer home: https://www.softbank.jp/
2. Enterprise AI overview: https://www.softbank.jp/biz/about/ai/
3. Corporate security: https://www.softbank.jp/corp/aboutus/governance/security/

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| `home`, `home::body` | `#000000` computed body text, transparent canvas, 14px / 400 / 20px, unresolved `Meiryo, "Hiragino Kaku Gothic ProN", "Hiragino Sans", system-ui, sans-serif` stack. |
| `home`, `home::h2` | `#424242` text, 40px / 700 / 50px heading samples. Raw only; not a canonical display-scale claim. |
| `enterprise-ai`, `surface-2::h2` | `#323232` text, 32px / 700 / 43.2px heading samples. This backs the narrow enterprise heading token. |
| `enterprise-ai`, `surface-2::li` (class `sb-appshell-v1-header-nav_globalnav-item`) | Transparent background; `#333333` text and computed border color; 0px border/radius/padding; unresolved Meiryo stack; 14px / 400 / 14px. High confidence, 12 occurrences across enterprise and corporate captures. This is the canonical static `listItem`. |
| `corporate-security`, `surface-3::body` | `#333333` text, 16px / 400 / 24px body sample on the corporate route. This backs the body numeric tokens only. |
| `corporate-security`, `surface-3::[data-omd-capture="14"]` | Transparent English utility anchor with `#424242` text, `#c1c1c2` 1px border, 15px radius, 8px 20px 7px padding, 13px / 400 / 13px. Route-local raw proof; it is not a canonical interactive component. |
| `enterprise-ai`, `surface-2::[data-omd-capture="84"]` | Transparent anchor button with `#333333` text, `rgba(61, 61, 61, 0.8)` 1px border, 6px radius, 13px 44px 11px 20px padding, 16px / 700 / 21.6px. Raw default geometry only; no state captured. |
| `home`, `home::[data-omd-capture="175"]` | Tag anchor with `#f0f0f0` background, `#424242` text, 10px radius, 16px 12px padding, 16px / 400 / 25.6px. Raw local badge geometry only. |
| `home`, `home::[data-omd-capture="182"]` | Route-local action anchor with `#357eec` background, white text, 50px radius, 0px 96px padding, 16px / 700 / 60px. Its three occurrences are not enough to name a cross-surface action token. |

### Font evidence

| Evidence class | Result |
|---|---|
| Official product-use | No first-party source located in this run assigns a named typeface to a SoftBank consumer or enterprise product UI. |
| Live computed surface-use | `Meiryo` is repeatedly first in the computed Japanese stack (879 uses), but the packet marks it unresolved: no matching loaded FontFace and no known-system mapping. Its numeric metrics are retained independently. |
| Official distributed brand asset | The Group identity page states that the **brand-name logo** uses a Japanese Mincho typeface. This is official identity context, not a distributed UI font asset or public-interface family. |
| License context | No official font licence or redistributable webfont asset was found. |
| Declared-only | `swiper-icons` has an `@font-face` source but zero visible uses. It is not a UI token. |
| System stacks | Arial and system-ui appear in a small number of raw control samples and are classified system; neither is treated as a SoftBank fallback or brand family. |

## Tier 2 attempts and conflict matrix

| Field | Tier 1 supplied capture / first-party source | getdesign | Refero | Resolution |
|---|---|---|---|---|
| Public-surface scope | Consumer home, enterprise AI, and corporate security; three supplied 1440×900 desktop captures | `https://getdesign.md/softbank` attempted via built-in search; no SoftBank-specific record returned | `https://styles.refero.design/?q=softbank` attempted via built-in search; no SoftBank-specific record returned | Supplied Tier 1 packet defines scope. |
| Public foreground/canvas | `#333333` repeated text/border and `#ffffff` repeated background across the three surfaces | No SoftBank-specific numeric record returned | No SoftBank-specific numeric record returned | Keep Tier 1 values only. |
| Typography | Computed Meiryo-led stack is unresolved; numeric body/heading metrics are observed | No SoftBank-specific type record returned | No SoftBank-specific type record returned | Omit font-family token; retain only the backed numeric metrics. |
| Global navigation list item | `surface-2::li`, `sb-appshell-v1-header-nav_globalnav-item`: `#333333`, 0px border/radius/padding, 14px / 400 / 14px, high confidence, 12 occurrences | No SoftBank-specific component record returned | No SoftBank-specific component record returned | Publish the one static `listItem` with directly measured fields. |
| Interaction states | Aggregate `interactionCount: 0`, `observedStates: 0` | No SoftBank-specific state record returned | No SoftBank-specific state record returned | Publish no interactive states or interactive-component token. |

Both required Tier 2 cross-checks were attempted with built-in web search. Neither returned SoftBank-specific data; that is a coverage limit, not a conflicting claim. The packet reports no unresolved conflict, so the canonical graph uses `conflicts: []` and the document footer resolves exactly to `none`.

## Components and provenance

- **Canonical global navigation item:** `surface-2::li`, class `sb-appshell-v1-header-nav_globalnav-item`, high confidence, 12 occurrences across enterprise and corporate captures. The observed semantic element is a static `li`; it is therefore mapped truthfully to permitted type `listItem` and every token field is linked to `verification_v2.claims`.
- **Enterprise default button:** `surface-2::[data-omd-capture="84"]`, medium confidence, six occurrences. Its 6px default geometry is measured, but the packet records no interaction state; it remains raw proof rather than an interactive token.
- **Home tag anchor:** `home::[data-omd-capture="175"]`, medium confidence, seven occurrences. It supports only a route-local badge observation, not a reusable token.
- **Home carousel/action controls:** anchors and buttons with circular or pill geometry are observed, but they have no captured hover/focus/pressed/disabled state. They are not promoted.
- **Corporate search and language controls:** raw default geometry exists, including the 15px English utility border treatment. They remain route-local evidence; the input-like search row is not inferred into an input token.

## Context and narrative evidence

- https://www.softbank.jp/en/corp/aboutus/profile/ — first-party SoftBank Corp. company scope: mobile communications, devices, fixed-line telecommunications, and internet access. Used for §1 and stakeholder context; not a computed-style source.
- https://www.softbank.jp/en/corp/aboutus/profile/history/ — first-party operating-company history, including the 2006 SoftBank brand change and 2015 consolidation into SoftBank Corp. Used for §§1 and 11; not a visual token source.
- https://group.softbank/en/philosophy/identity — first-party Group identity: Kaientai-derived double line, silver brand colour, Mincho logotype context, and Information Revolution philosophy. Used for §§1, 3, 7, 11, and 12; it does not establish product UI fonts or hex tokens.
- https://www.softbank.jp/en/corp/ir/policy/strategy/ — first-party current strategy: “Activate AI for Society,” technology-led social problem solving, and current management direction. Used for §§1 and 10–12; not a style-token source.
- https://www.softbank.jp/corp/aboutus/governance/intellectual-property/trademark/ — first-party trademark statement distinguishing SoftBank Group and SoftBank Corp. marks and its multi-brand consumer strategy. Used for brand-domain boundaries; not a computed-style or logo-license grant.

SoftBank Corp. consumer marketing, enterprise marketing, corporate governance, SoftBank Group identity, trademark policy, font declarations, system stacks, and uninspected authenticated products are distinct evidence domains. No mobile app, billing journey, account surface, device setup, checkout, or public design system was inspected.
