# SurveyCake — Verification Notes (2026-06-08)

## Proof — Tier 1 live inspect

**Inspected:** 2026-06-08
**Method:** playwright getComputedStyle (live DOM) — headless Chromium (`--disable-http2`), `goto https://www.surveycake.com` with `waitUntil: 'networkidle'`, Escape + close-button dismissal, then `getComputedStyle` on body, h1, h2, nav, and all visible `a`/`button` elements, plus a full-page color-frequency scan. `rgb()` values converted to 6-digit hex.

**Sources:**
- https://www.surveycake.com (live homepage DOM — primary inspect surface)

### Raw samples
- `body` → background `rgb(255, 255, 255)` = `#ffffff`; color `rgb(0, 0, 0)` = `#000000`; font-family `"Noto Sans TC", "Helvetica Neue", Helvetica, Roboto, Arial, "Apple LiGothic", 微軟正黑體, "Microsoft JhengHei", sans-serif`
- `h1` ("企業級的雲端問卷服務") → font-size `42px`, weight `900`, line-height `63px` (1.50), letter-spacing `1px`, color `rgb(62, 62, 62)` = `#3e3e3e`
- `h2` ("強大的專業功能與題型") → font-size `36px`, weight `700`, line-height `54px` (1.50), letter-spacing `1px`, color `rgb(62, 62, 62)` = `#3e3e3e`
- Primary CTA ("免費建立問卷") → background `rgb(61, 186, 144)` = `#3dba90`, color `#ffffff`, border-radius `4px`, height `60px`, font-size `14px`, weight `500`
- Outlined sign-up ("註冊") → background `#ffffff`, color `rgb(61, 186, 144)` = `#3dba90`, border `1px solid rgb(61, 186, 144)`, border-radius `4px`, height `40px`, font-size `14px`, weight `700`
- Inline link ("了解更多") → color `rgb(61, 186, 144)` = `#3dba90`, font-size `16px`, weight `700`, transparent background, no underline
- Nav link ("方案與定價") → color `rgb(62, 62, 62)` = `#3e3e3e`, font-size `14px`, weight `500`
- Color-frequency scan (top hits) → `rgb(0,0,0)` ×740, `rgb(62,62,62)` = `#3e3e3e` ×287, `rgb(61,186,144)` = `#3dba90` ×68, `rgb(255,255,255)` ×34, `rgb(155,155,155)` = `#9b9b9b` ×24, `rgb(95,84,224)` = `#5f54e0` ×6, `rgb(242,242,242)` = `#f2f2f2` ×5, `rgb(221,221,221)` = `#dddddd` ×3, `rgb(247,247,247)` = `#f7f7f7` ×2, `rgb(245,184,73)` = `#f5b849` ×1, `rgb(242,102,89)` = `#f26659` ×1, `rgb(0,115,183)` = `#0073b7` ×1

**Result:** Brand primary confirmed as SurveyCake Green `#3dba90` (live CTA + most frequent non-neutral color). Ink `#3e3e3e`, muted `#9b9b9b`, neutral surfaces `#f7f7f7`/`#f2f2f2`, hairline `#dddddd`, and the reserved accent set (violet/amber/coral/blue) all measured live. >= 6 concrete samples captured.

## Country sources

Taiwanese, brand-owned / regional sources (not getdesign.md, refero.design, or favicon proxies):

1. **https://www.surveycake.com** — Brand-owned official site, Traditional Chinese (zh-TW), serving the Taiwan market. Hero "企業級的雲端問卷服務", tier nav "SurveyCake BASIC 終身免費 / PRO / TEAM / ENTERPRISE". Verified live 2026-06-08.
2. **https://blog.surveycake.com** — Official SurveyCake blog (部落格), Traditional Chinese. Title verified via WebFetch 2026-06-08: "SurveyCake 部落格 - 不僅止於調查，我們讓資料創造最大的價值" (Beyond Surveys, We Maximize Data Value). Confirms brand voice and zh-TW positioning.
3. **https://www.surveycake.com/pricing** — Brand-owned Taiwan pricing page documenting the BASIC / PRO / TEAM / ENTERPRISE tier structure referenced in §11.

Primary regional/brand sources used for country grounding: items 1 and 2 above (live homepage + official blog), both brand-owned and Traditional-Chinese Taiwan surfaces.
