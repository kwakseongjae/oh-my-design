# Notion verification — 2026-07-13

## Proof — Tier 1 live inspect

Machine artifact: `artifacts/reference-evidence/notion.json` (captured `2026-07-13T07:44:33.109Z`). This reverify uses the supplied artifact only. No browser recapture and no MCP session were run.

**Inspected:** 2026-07-13

**Sources:**

1. Marketing: https://www.notion.com/ko
2. Public product marketing: https://www.notion.com/ko/product
3. Documentation chrome: https://www.notion.com/ko/help

The artifact reports three public routes, 10 component types, 72 component variants, six observed states, nine interactions (three dialogs and six tabs), and coverage 100. Its routes are separated in this document into marketing, public product marketing, and Help documentation chrome; no authenticated workspace was inspected.

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| marketing, `home::[data-omd-capture="9"]` | global CTA: `rgb(0,117,222)` / `#0075de`, `rgb(255,255,255)`, 8px radius, 36px height, `4px 14px`, 16px/500 NotionInter. |
| marketing, same selector `::state-hover` | `rgb(0,91,171)` / `#005bab`, 8px radius, 36px. |
| marketing, `home::[data-omd-capture="11"]` | hero CTA: `#0075de`, white, 8px radius, 38px height, `6px 15px`, 16px/500 NotionInter. |
| marketing, same selector `::state-pressed` | `rgb(0,115,217)` / `#0073d9`, 8px radius, 38px. |
| marketing, same selector `::state-focus` | `rgb(0,113,214)` / `#0071d6`, 8px radius, 38px. |
| marketing, `home::div.cardCompact_cardCompact__W2i4I` | compact card: `#ffffff`, `rgba(0,0,0,0.1)` 1px border, 12px radius, 24px padding, no shadow. |
| marketing, `home::[data-omd-capture="31"]` | circular carousel action: `#ffffff`, 9999px radius, 40px height, 20px/400 NotionInter, four-layer low-opacity shadow. |
| Help chrome, `surface-3::[data-omd-capture="11"]::state-hover` | toggle hover: `rgb(249,249,248)` / `#f9f9f8`, 4px radius, `6px 8px`, 16px/400 NotionInter. |
| Help dialog, `home::[data-omd-interaction-capture="dialog-0-1"]` | search input: `rgba(0,0,0,0.95)` text, 1px `rgba(0,0,0,0.08)` border, 5px radius, 36px height, `7px 10px 7px 30px`, 16px/400 NotionInter; blue inset/outer focus rings. |
| marketing, `home::h1` | h1: NotionInter, 96px/600/100px with -4.6px tracking. |

### Font evidence

| Evidence class | Result |
|---|---|
| Official product-use | Notion’s About and official Notion AI story describe an all-in-one, moldable software workspace; they establish product context but do not license or name a webfont. |
| Live computed surface-use | NotionInter is loaded/high with 906 visible uses across marketing, public product marketing, and Help. The artifact records computed use plus FontFaceSet and 32 Notion-controlled source URLs. |
| Official distributed asset | `www.notion.com` serves `NotionInter-Regular`, Medium, SemiBold, Bold, Italic, and i18n `.woff/.woff2` assets. This is browser-delivery evidence only. |
| Declared-only | iA Writer Mono, Lyon Text, Noto Sans Arabic, Noto Sans Hebrew, and Permanent Marker each have `@font-face` sources but zero visible use in this capture. |
| System / separate chrome | Inter, `-apple-system`, `system-ui`, Segoe UI, Helvetica, Arial, and emoji faces are fallback stack members only. They are not rendered as NotionInter. |
| License boundary / unresolved | A first-party public NotionInter font license or redistribution grant was not found in the official searches attempted. Do not redistribute the served font files or silently substitute a system face. |

## Tier 2 attempts and conflict matrix

| Field | Tier 1 supplied capture / official source | getdesign | Refero | Resolution |
|---|---|---|---|---|
| Public CTA | `#0075de`, white, 8px, 36px global CTA; `#0075de`, 8px, 38px hero CTA | Directory calls the design warm minimalism with soft surfaces; no raw CTA token promoted | Query attempted at `?q=Notion`; built-in web fetch returned an internal error | Tier 1 retained. |
| Earlier `#455dd3` CTA | Not present in fresh three-route CTA samples | No raw countervalue | No usable record | Rolled back. Earlier snapshot conflicts with fresh computed evidence. |
| Primary state colors | `#005bab` hover, `#0073d9` pressed, `#0071d6` focus on named selectors | No state values | No usable record | Kept as selector-specific state evidence, not palette ramp. |
| Card | `#ffffff`, 1px `rgba(0,0,0,0.1)`, 12px, 24px, no shadow | Directory summary only | No usable record | Tier 1 retained. |
| Help search | 5px input with blue focus rings after dialog open | No component data | No usable record | Kept as documentation chrome only. |
| Typeface | NotionInter loaded/high with source corroboration; several families declared-only | Directory has no font evidence | No usable record | Live evidence governs UI token; served source URLs do not establish redistribution rights. |

getdesign URL: https://getdesign.md/notion — one third-party directory record, “All-in-one workspace. Warm minimalism, serif headings, soft surfaces.” Its prose is Tier 2 context only and is not copied into live tokens.

Refero URL attempted: https://styles.refero.design/?q=Notion — the built-in web fetch returned an internal error. This is a failed cross-check, not evidence that Refero has no record.

No unresolved promoted-token conflict remains. The supplied artifact itself is authoritative for the values above; official context sources are kept separate from raw surface claims.

## Components and provenance

- **Global CTA:** `home::[data-omd-capture="9"]`, class `button_buttonVariantPrimary__mUFQZ globalNavigation_tryFreeCta__mNYk6`; default plus hover, pressed, and focus capture. The canonical section prints only the raw hover color because other state values were not selected as shared tokens.
- **Hero CTA pair:** `home::[data-omd-capture="11"]` primary and `home::[data-omd-capture="12"]` secondary. The primary has pressed/focus values; the secondary’s captured styling is focus-state evidence.
- **Marketing feature tabs:** `home::[data-omd-capture="27"]` through `30`; three tab interactions produced selected/tab-selected targets. No unobserved panel layout or animation is inferred.
- **Compact card:** `home::div`, class `cardCompact_cardCompact__W2i4I`; marketing/public product-marketing only.
- **Carousel action:** `home::[data-omd-capture="31"]`, circular white action with a measured shadow. It is not a global Notion elevation rule.
- **Language picker:** `home::[data-omd-capture="60"]` opens a dialog on all three routes. Its search field is `home::[data-omd-interaction-capture="dialog-0-1"]`; dialog/list targets were captured but a dialog shell/elevation was not.
- **Help toggle:** `surface-3::[data-omd-capture="11"]`, class `toggleList_toggleButtonWrapper__79NEe`; this is documentation chrome and has observed hover/pressed snapshots.

## Context and narrative evidence

- https://www.notion.com/about — first-party account of fragmented work tools, early-computing lineage, and Notion’s all-in-one building-block response. Used for §1, §10, and §11 narrative; not token evidence.
- https://www.notion.com/careers — current first-party mission, current people-and-AI-agent framing, and values including Customer in every room, Own the outcome, Why not today, and Direct and kind. Used for §10–13; not surface-token evidence.
- https://www.notion.com/blog/behind-the-scenes-notion-ai — Ivan Zhao’s official account of Notion AI, the software-toolmaking mission, and the human-centered AI framing. Used for current evolution and principles; not a component specification.
- https://www.notion.com/ko — fresh marketing raw-computed source.
- https://www.notion.com/ko/product — fresh public product-marketing raw-computed source.
- https://www.notion.com/ko/help — fresh Help documentation-chrome raw-computed source.

Marketing, public product marketing, Help documentation chrome, official narrative, declared font assets, and Tier 2 directory material are distinct evidence domains. No authenticated product UI, product API, native app, public design-system documentation, or standalone NotionInter license was used to fill gaps.
