# Stripe verification — 2026-07-13

## Proof — Tier 1 live inspect

Machine artifact: `artifacts/reference-evidence/stripe.json` (captured `2026-07-13T11:36:17.707Z`). This UPDATE uses that supplied artifact only. No browser recapture or MCP session was run.

**Inspected:** 2026-07-13

**Sources:**

1. Public Docs home: https://docs.stripe.com/
2. Public Payments documentation: https://docs.stripe.com/payments
3. Public API reference: https://docs.stripe.com/api

The artifact reports three public product/documentation routes, six component types, 54 component variants, four observed static states, zero captured interactions, and coverage score 97. Its `home` surface is Docs home—not `stripe.com` marketing home.

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| `home`, `home::[data-omd-capture="2"]` | Search prompt: `rgb(255, 255, 255)` / `#ffffff`, `rgb(84, 105, 212)` / `#5469d4`, 0px border, 8px radius, 2px 8px padding, system stack 14px/400. |
| `home`, `home::[data-omd-capture="2"]::state-hover` | Prompt hover: `#ffffff`, `rgb(39, 57, 81)` / `#273951`, `rgb(149, 164, 186)` / `#95a4ba`, 8px radius, 2px 8px. |
| `home`, `home::[data-omd-capture="2"]::state-pressed` | Prompt pressed: `rgb(244, 247, 250)` / `#f4f7fa`, `rgb(60, 79, 105)` / `#3c4f69`, `rgb(212, 222, 233)` / `#d4dee9`, 8px radius. |
| `home`, `home::[data-omd-capture="4"]` | Secondary action: `#ffffff`, `rgb(80, 97, 122)` / `#50617a`, 0px border, 8px radius, 6px 12px padding, system stack 14px/400/20px. |
| `home`, `home::[data-omd-capture="3"]` | Docs input: `rgb(39, 57, 81)` / `#273951`, transparent background, 0px border, 6px radius, 4px 8px 4px 4px padding, system stack 14px/400/20px. |
| `home`, `home::[data-omd-capture="7"]` | Content tab: `rgb(65, 69, 82)` / `#414552`, transparent background, 0px border, 0px radius, 0px padding, system stack 14px/400. |
| `surface-2`, `surface-2::div` | Public Docs card sample: `#414552`, transparent background, `rgb(227, 232, 238)` / `#e3e8ee` 1px border, 4px radius, 18px padding. It is medium-confidence and is not a canonical card token. |
| `surface-3`, `surface-3::[data-omd-capture="68"]` | API-only dark action: `#ffffff`, `rgb(43, 48, 57)` / `#2b3039`, 6px radius, 4px 8px padding, system stack 12px/600/16px; route-local and not generalized. |

### Font evidence

| Evidence class | Result |
|---|---|
| Official product-use | No first-party Stripe source inspected in this update establishes a named type family for the three captured Docs routes. |
| Live computed surface-use | `-apple-system, system-ui, Segoe UI, Roboto, Helvetica, Arial, sans-serif` is a high-confidence operating-system stack with 355 visible uses. It has no brand-family promotion. |
| Official distributed brand asset | Klim Type Foundry’s Söhne page identifies Stripe in its independent “Söhne in use” showcase, lists WOFF2 web format, and explains commercial licensing. This proves foundry context, not a loaded Docs webfont. |
| Declared-only | Heiti SC, Hiragino Kaku Gothic ProN, Hiragino Sans, Hiragino Sans GB, Meiryo UI, Microsoft JhengHei, Microsoft YaHei, PingFang SC, and Yu Gothic UI are declared with zero visible use. |
| System / unresolved | `Source Code Pro` has four computed occurrences and Menlo two, but neither has a matching loaded FontFace or source URL in the artifact. Adobe’s repository says Source Code Pro is OFL-1.1; that does not evidence Stripe deployment. |

The raw artifact has no `fontFaces` payload and lists empty source URLs for the unresolved names. Consequently no custom family appears in `tokens.typography.family`, and no fallback is rendered as though it were Söhne or Source Code Pro.

## Tier 2 attempts and conflict matrix

| Field | Tier 1 supplied capture / first-party source | getdesign | Refero | Resolution |
|---|---|---|---|---|
| Current public surface | Three public `docs.stripe.com` routes: Docs home, Payments docs, API reference | https://getdesign.md/stripe exists as independent analysis | Search attempted at https://styles.refero.design/?q=stripe | Tier 1 route scope governs this update. Tier 2 does not replace current Docs capture. |
| Docs compact action | `#ffffff`, `#5469d4`, 8px, 2px 8px at `home::[data-omd-capture="2"]`; selector-backed static hover/pressed samples | No numeric token promoted | Stripe record at https://styles.refero.design/style/48e5de76-05d5-4c4e-a269-c7c245b291ec describes a marketing-oriented `sohne-var`/`#533afd` system | Keep current Docs action separate from the independent marketing record. |
| Docs typography | System stack only; 355 computed uses; no loaded custom-face match | Independent analysis, no raw evidence promoted | `sohne-var` record conflicts with current Docs scope | Do not promote Söhne, Inter, or a system fallback as the current Docs UI family. |
| Purple role | `#5469d4` is Docs prompt/link; `#533afd` appears on API-reference action/border samples | No numeric token promoted | `#533afd` primary-marketing interpretation | Keep both selector/surface-local roles; do not unify them as one generic primary token. |
| Söhne licensing | Klim’s own page lists Söhne formats/features and a commercial web-font license boundary; it is not Stripe deployment evidence | No contribution | Independent visual record only | Retain as official font-foundry context, outside machine typography tokens. |

Both required Tier 2 cross-checks were attempted. getdesign is explicitly an independent editorial analysis, and Refero is an independent, marketing-oriented snapshot. Their difference from the supplied Docs capture is a resolved source-domain separation, not a token conflict.

## Components and provenance

- **Docs search prompt:** `home::[data-omd-capture="2"]`, high confidence, Docs home and Payments. Static hover, pressed, and focus samples are present; the artifact’s aggregate interaction count is zero, so no transition behavior is claimed.
- **Docs secondary action:** `home::[data-omd-capture="4"]`, high confidence, Docs home and Payments. Static hover, pressed, and focus samples only.
- **Docs content tab:** `home::[data-omd-capture="7"]` and `surface-2::[data-omd-capture="46"]`, high confidence. `selected` is a static ARIA-tab capture; no tab transition is claimed.
- **Input:** `home::[data-omd-capture="3"]`, high confidence. Preserved as raw proof but excluded from canonical components because the packet has no input state evidence.
- **Cards, badges, API-only dark controls:** medium/low-confidence or route-local detections are not generalized into component variants.

## Context and narrative evidence

- https://stripe.com/newsroom/information — first-party mission, company description, leadership, official wordmark color-use guidance, and the boundary around downloadable media assets. Used for §§1, 2, and 11; it is not computed CSS evidence.
- https://stripe.com/jobs/culture — first-party operating principles: users first, craft and beauty, urgency and focus, collaboration, talent, and curiosity. Used for §§1, 10, and 12; it is not a component/token source.
- https://klim.co.nz/fonts/soehne/ — the type foundry’s Söhne history, features, WOFF2 format, and Stripe showcase. Used only for distributed-font context.
- https://klim.co.nz/faqs/ — first-party foundry license boundary: web use requires the appropriate web license and is distinct from desktop/app permissions. Used only for font-license context.
- https://github.com/adobe-fonts/source-code-pro/blob/release/LICENSE.md — Adobe’s Source Code Pro OFL-1.1 license. Used only to identify the externally declared font’s license; no Stripe-use inference.

Public Docs, marketing/corporate context, newsroom brand assets, source-foundry material, and declared-only font faces are separate domains. No authenticated Stripe Dashboard, checkout, transaction UI, mobile viewport, or custom-font source file was inspected.
