# CGV verification — 2026-07-13

## Proof — Tier 1 live inspect

The supplied MCP-free computed-style artifact captured three public CGV surfaces. Machine artifact: `artifacts/reference-evidence/cgv.json`. No browser capture was rerun.

**Inspected:** 2026-07-13

**Sources:**

1. https://cgv.co.kr/
2. https://cgv.co.kr/ (captured as `surface-2`)
3. https://cgv.co.kr/ (captured as `surface-3`)

The artifact reports 3 surfaces, coverage 81/100, 383 component variants, and 0 interaction observations.

### Raw samples

| Evidence | Computed sample |
|---|---|
| `home::[data-omd-capture="36"]` | `rgb(244, 244, 244)` background and border, `rgb(69, 69, 69)` text, `25px` radius, `0px 16px` padding, `15px/400`, 40px tall. |
| `home::[data-omd-capture="128"]` | `rgb(255, 255, 255)` background, `rgb(18, 18, 18)` text, `rgb(217, 217, 217)` 1px border, `20px` radius, `9px 24px` padding, `14px/500`. |
| `home::span` (`badge screenType`) | `rgba(0, 0, 0, 0.55)` background, `rgb(18, 18, 18)` text, `4px` radius, `0px 5px` padding, `11px/700`, 20px tall. |
| `home::li` (`mets01390_linkItem__RyBIj`) | transparent background, `rgb(18, 18, 18)` text, `0px` radius, `10px/400`, 40px tall. |
| `home::body` aggregate | Pretendard loaded/high, `#121212` recurring text and border, `#ffffff` recurring background. |
| repeated public observations | `#f4f4f4` control background, `#d9d9d9` outline border, `#707070` supporting text, `#fc5555` low-frequency text/border. |

### Font evidence

| Evidence class | Result |
|---|---|
| Official product-use | No official CGV product font statement was used to promote a token. |
| Live computed surface-use | `pretendard` loaded/high, 1,217 visible uses; role coverage includes headings, body, buttons, cards, dialogs, inputs, list items, and badges. Nine CGV CDN WOFF/WOFF2 sources are recorded in the artifact. |
| Official distributed brand asset | No CGV-exclusive distributed UI font was verified. |
| Declared-only | `Roboto` and `swiper-icons` have zero visible text usage; they are not canonical UI families. |
| Unresolved | Native application, authenticated booking, and unobserved campaign typography. |

## Tier 2 attempts

- https://getdesign.md/cgv — requested as the required exact lookup; it returned an internal retrieval error and no usable CGV design record.
- https://styles.refero.design/?q=cgv — requested as the required query; it returned an internal retrieval error and no usable CGV style record.

Neither Tier 2 attempt is positive component, token, or narrative evidence.

## Conflict Matrix

| Field | Tier 1 (supplied live capture) | getdesign | refero | Resolution |
|---|---|---|---|---|
| Public foreground | repeated `#121212` | unavailable | unavailable | promoted as foreground only |
| Public canvas | repeated `#ffffff` | unavailable | unavailable | promoted as canvas |
| Category chip | `#f4f4f4`, `#454545`, 25px, 40px | unavailable | unavailable | promoted from selector-backed default |
| Outline action | `#ffffff`, `#121212`, 1px `#d9d9d9`, 20px | unavailable | unavailable | promoted from selector-backed default |
| Interaction states | 0 observations | unavailable | unavailable | omitted |
| UI family | loaded Pretendard; Roboto/swiper-icons not visibly used | unavailable | unavailable | Pretendard only |

No unresolved promoted token conflict remains. Interaction count is zero, so no hover, focus, pressed, disabled, or error value is claimed.

## Context and narrative evidence

- https://img.cgv.co.kr/company/sustainabilityStrategy/Report/2024/2024_CJCGV_SUSTAINABILITY_REPORT_kor.pdf — first multiplex at CGV Gangbyeon in 1998; Cultureplex framing; 2024 Deep Dive Space rebranding; official mission, vision, and values. Evidence class: official corporate context; not UI-token evidence.
- https://newsroom.cj.net/part-12-koreas-first-multiplex-theater-cgv — first-party corporate history of the Gangbyeon11 multiplex and the shift from conventional single-screen theatres. Evidence class: official history; not UI-token evidence.
- https://newsroom.cj.net/cgv-targets-global-market-with-premium-theaters/ — first-party account of premium SCREENX theatre expansion and screen, sound, and seating experience. Evidence class: official product evolution; not UI-token evidence.
- https://github.com/orioncactus/pretendard/blob/main/LICENSE — official Pretendard project licence under SIL Open Font License 1.1. Evidence class: font licence; does not establish CGV ownership or a CGV-specific brand font.

## Claim-graph coverage

Every scalar leaf under `tokens.colors`, `tokens.typography`, `tokens.spacing`, `tokens.rounded`, `tokens.shadow`, and each promoted `tokens.components` field has exactly one `verification_v2.claims` path. All UI claims resolve to the supplied `home` public-surface capture; narrative and licence sources are intentionally not used to invent UI values.
