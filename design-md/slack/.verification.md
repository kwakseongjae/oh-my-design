# Slack — Verification Notes (2026-06-06)

## Proof — Tier 1 live inspect
**Inspected:** 2026-06-06
**Method:** playwright getComputedStyle (live DOM) on the production site
**Sources:**
- https://slack.com

### Raw samples
- live slack.com body text: color #000000, font 16px, family Salesforce-Sans — source: live DOM getComputedStyle
- live slack.com page background: color #000000 — source: live DOM getComputedStyle
- live slack.com root background: color #000000 — source: live DOM getComputedStyle
- live slack.com heading: color #000000, font 64px, / 700, family Salesforce-Avant-Garde — source: live DOM getComputedStyle
- live slack.com primary button: color #ffffff, background #611f69, border-radius 4px, height 39px, font 14px, / 700 — source: live DOM getComputedStyle
- live slack.com link: color #ffffff — source: live DOM getComputedStyle

## Tier 2 — cross-check
- Live-DOM computed values above are the source of truth for this reference's hex/px tokens. DESIGN.md token roles were reconciled against these measurements.

## Country sources
- country US; brand-owned live source: https://slack.com (production site, all hex/px captured via live DOM getComputedStyle).
