# Bilibili verification — 2026-07-13

## Proof — Tier 1 supplied product inspect

This UPDATE uses only the supplied raw collector artifact `artifacts/reference-evidence/bilibili.json`; no browser capture or MCP collection was rerun. It records three 1440×900 desktop snapshots: `home` and `surface-2` at `https://www.bilibili.com/`, plus `surface-3` at `https://www.bilibili.com/v/popular/all/`. These are three collector surfaces across two product URLs, not three independent marketing or documentation surfaces. Coverage is 81, with 42 component variants, zero interaction expansions, and zero observed states.

### Source-domain inventory

| Domain | Source | Used for | Not used for |
|---|---|---|---|
| Live product | `https://www.bilibili.com/` (`home`, `surface-2`) | Computed product-home tokens, selector-level components, FontFaceSet/@font-face evidence | Marketing, player, mobile, docs, or interaction claims |
| Live product | `https://www.bilibili.com/v/popular/all/` (`surface-3`) | Product-popular route and repeated default search-field evidence | Product-home card values unless its selector says so |
| Corporate information | `https://ir.bilibili.com/en/corporate-information/` | History, mission, community, creators, bullet-chatting narrative | Product tokens, components, or UI-copy rules |
| Corporate sustainability document | `https://ir.bilibili.com/media/crul0g3t/bilibili-sustainable-finance-framework.pdf` | Community and creator context | Product tokens, components, or UI-copy rules |
| Font publisher | `https://developer.huawei.com/consumer/en/design/resource/` | HarmonyOS Sans ownership/resource boundary | A Bilibili product-use or licence claim |
| Marketing and documentation chrome | No Bilibili marketing, design-system, or product-documentation surface was supplied or promoted. | None | All canonical token and component claims |

### Raw computed-style samples

| Evidence selector | Sample |
|---|---|
| `home::body` | `#f1f2f3` background, `#18191c` text, `0px` radius, `16px/400`, `16px` line height |
| `home::div.bili-video-card__image--wrap` | `#f1f2f3` background, `6px` radius, 248px × 140px, `139.5px 0px 0px` padding |
| `home::div.bili-video-card__stats` | `#ffffff` text, `0px 0px 6px 6px` radius, `16px 8px 6px` padding, `13px/400`, `13px` line height |
| `home::h3.bili-video-card__info--tit` | `#18191c` text, `15px/400`, `22px` line height, `0px 16px 0px 0px` padding |
| `home::div.bili-video-card__info--bottom` | `#9499a0` text, `13px/400`, `13px` line height, `4px 0px 0px` margin |
| `home::[data-omd-capture="9"]` (`nav-search-input`) | `#61666d` text, transparent background, `0px` radius, `0px 8px 0px 0px` padding, `14px/400`, `20px` line height |
| `home::p.bili-video-card__skeleton--text` | `#f1f2f3` background, `4px` radius, `16px/400`, `16px` line height |
| `home::[data-omd-capture="87"]` (`primary-btn roll-btn`) | `#ffffff` background, `#18191c` text, `1px #e3e5e7` border, `8px` radius, `9px` padding, `12px/400` |

### Font evidence

| Family | Collector evidence | Official source / licence boundary | Classification and decision |
|---|---|---|---|
| `-apple-system` | System classification; 951 computed usages across the three snapshots, with the resolved stack also naming system-ui, Helvetica Neue, Arial, PingFang SC, Hiragino Sans GB, and Microsoft YaHei. | Not a Bilibili-distributed font asset. | **System**. Canonical only as the effective system family; no substitute specimen. |
| `HarmonyOS_Regular` | 54 Bilibili-CDN `@font-face` source URLs; zero visible usages. | Huawei publishes HarmonyOS Sans as a Huawei design resource, but no Bilibili first-party product-use or licence statement was found. | **Declared-only**. Not a Bilibili UI family and omitted from tokens. |
| `HarmonyOS_Medium` | 54 Bilibili-CDN `@font-face` source URLs; zero visible usages. | Same boundary as `HarmonyOS_Regular`. | **Declared-only**. Not a Bilibili UI family and omitted from tokens. |
| `Helvetica Neue` | 205 computed usages, but collector classifies it unresolved and supplies no loaded FontFace/source URL. | No Bilibili font source or licence corroboration. | **Unresolved**. Omitted from tokens. |

Visible computed use plus FontFaceSet/source corroboration is absent for both HarmonyOS declarations, so neither is promoted.

### Component scope and state provenance

- `home::div.bili-video-card__image--wrap`: card-cover wrapper; `#f1f2f3`, 6px radius; 22 occurrences on `home`/`surface-2`.
- `home::div.bili-video-card__stats`: lower statistics strip; white 13px/400 text, `0px 0px 6px 6px` radius, `16px 8px 6px` padding; 10 occurrences on `home`/`surface-2`.
- `home::h3.bili-video-card__info--tit`: video title; `#18191c`, 15px/400/22px; 10 occurrences on `home` and a separate 10 on `surface-2`.
- `home::div.bili-video-card__info--bottom`: owner/date metadata; `#9499a0`, 13px/400; 80 occurrences on `home`/`surface-2`.
- `home::p.bili-video-card__skeleton--text` and `--light`: skeleton lines; `#f1f2f3`, 4px radius.
- `home::[data-omd-capture="9"]` (`nav-search-input`): default only; it appears on `home`, `surface-2`, and `surface-3`.
- `home::[data-omd-capture="87"]` (`primary-btn roll-btn`): default only; white background, dark text, `1px #e3e5e7` border, 8px radius, and 9px padding.

The raw `interactions` array is empty and every component’s `states` list is empty. No hover, focus, pressed, menu, dialog, tab, toast, error, or mobile variant is claimed.

## Tier 2 cross-check

- `https://getdesign.md/bilibili` was attempted with built-in web fetch on 2026-07-13. The direct URL was rejected as unsafe, so no Tier 2 token or component observation was read.
- `https://styles.refero.design/?q=bilibili` was attempted with built-in web fetch on 2026-07-13. The direct URL was rejected as unsafe. A separate built-in search retrieved the Refero catalog landing, but did not retrieve a Bilibili-specific style page; this is not an absence conclusion.

Tier 2 supplied no Bilibili-specific evidence and cannot override the supplied Tier 1 product capture.

## Reconciliation matrix

| Field | Tier 1 (live product capture) | getdesign | refero | Resolution |
|---|---|---|---|---|
| Product surfaces | Three snapshots across home and popular routes | Not retrieved | No Bilibili-specific page retrieved | Keep actual snapshot/URL provenance; do not relabel corporate pages as product surfaces |
| Page canvas | `home::body` `#f1f2f3` | Not retrieved | Not retrieved | Canonical `tokens.colors.canvas` |
| Card surface / stats text | `#ffffff` on card/overlay selectors | Not retrieved | Not retrieved | Canonical surface and component text only |
| Foreground and metadata | `#18191c` title/body; `#9499a0` metadata | Not retrieved | Not retrieved | Canonical product-home roles |
| Search field | `#61666d` default on all three snapshots; no state record | Not retrieved | Not retrieved | One default selector-backed input; remove prior invented focus/pressed values |
| Default feed roll button | `home::[data-omd-capture="87"]`: white, dark text, `1px #e3e5e7`, 8px, 9px | Not retrieved | Not retrieved | Add only its observed default component variant |
| Rounded values | 4px skeleton lines; 6px card covers; 8px observed roll button | Not retrieved | Not retrieved | Keep component-local values; canonical `sm`/`md` remain 4/6 only |
| Effective UI family | `-apple-system` system classification, 951 usages | Not retrieved | Not retrieved | System family only |
| HarmonyOS faces | Declared CDN sources, zero visible usages | Not retrieved | Not retrieved | Declared-only; omitted from tokens |
| Legacy pink/blue/coin/status palette | No matching computed value in supplied capture | Not retrieved | Not retrieved | Removed from canonical tokens and component specs; frontmatter primary colour remains non-token catalog metadata |
| Legacy player, follow/coin, tag, nav-tab, dialog, mobile, state, and motion specs | No corresponding observed component/state in this packet | Not retrieved | Not retrieved | Removed; no variants fabricated |

No unresolved conflict remains among current canonical token claims.

## Context and narrative evidence

- Bilibili’s corporate information states its June 2009 initial launch, January 2010 name, “All the Videos You Like” proposition, mission, community/creator model, and bullet-chatting feature: `https://ir.bilibili.com/en/corporate-information/`.
- The official sustainable finance framework independently describes a welcoming community, high-quality content, creator support, and the mission to enrich young generations’ everyday lives: `https://ir.bilibili.com/media/crul0g3t/bilibili-sustainable-finance-framework.pdf`.
- Huawei’s official design resources list HarmonyOS Sans as a Huawei asset: `https://developer.huawei.com/consumer/en/design/resource/`. This establishes an ownership boundary only; it does not establish Bilibili use or licence.
- The raw Bilibili snapshots are limited to the current desktop feed/search/default-component observations above: `https://www.bilibili.com/` and `https://www.bilibili.com/v/popular/all/`.

No Bilibili first-party font licence, public design system, marketing style guide, or documentation UI source was found in the requested source set. User-generated posts and non-first-party colour aggregators were not used.
