# Classum (클라썸) — verification v2 evidence notes

Checked: 2026-07-13
Pipeline: supplied `artifacts/reference-evidence/classum.json` + `omd:add-reference` UPDATE reconciliation
Raw bundle: `artifacts/reference-evidence/classum.json`

## Proof — Tier 1 public-surface inspection

**Captured:** 2026-07-13
**Method:** supplied deterministic Playwright computed-style/FontFaceSet capture; no browser capture was rerun for this update. Desktop viewport: `1440×900`. The bundle retains three publicly accessible marketing routes, 37 baseline component variants, and zero interaction kinds/events.

### Source-domain boundary

| Domain | Retained source | What it supports | What it does not support |
|---|---|---|---|
| Public marketing | `https://business.classum.com/` | Navigation actions, public heading/body typography, colors | Authenticated LMS workflow/UI |
| Public marketing | `https://business.classum.com/learning` | Public large-action variation | Authenticated learning UI or state transitions |
| Public marketing | `https://business.classum.com/university/lms` | University-LMS landing cards and supporting text | Signed-in university LMS cards or controls |
| Documentation chrome | No supplied public documentation surface | Nothing | Documentation UI tokens/components |
| Declared assets | `webflow-icons` `@font-face`, zero visible uses | Existence of a declared icon asset | A text-family or UI-font claim |

### Raw samples

- `home` · selector `home::[data-omd-capture="4"]` · class `primary-button on-nav t-hidden w-inline-block` · public navigation CTA · `background rgb(255,68,56)` = `#FF4438`; `color #FFFFFF`; `border-width 0px`; `radius 8px`; `padding 6px 16px`; `box-shadow none`; `15px / 600 / 22.5px "Pretendard Variable"`; six occurrences across all three routes.
- `home` · selector `home::[data-omd-capture="3"]` · class `line-button on-nav on-white w-inline-block` · public light navigation outline · transparent background; `color/border #FF4438`; `1px` border; `8px` radius; `6px 16px`; `15px / 600 / 22.5px "Pretendard Variable"`; three occurrences across all three routes.
- `home` · selector `home::[data-omd-capture="8"]` · class `line-button on-nav w-inline-block` · public dark navigation outline · transparent background; `color/border #FFFFFF`; `1px` border; `8px` radius; `6px 16px`; `15px / 600 / 22.5px "Pretendard Variable"`; three occurrences across all three routes.
- `home` · selector `home::[data-omd-capture="17"]` · class `primary-button large w-inline-block` · public large CTA · `#FF4438` background; `#FFFFFF` text; `8px` radius; `12px 34px`; `18px / 600 / 27px "Pretendard Variable"`; two occurrences across home and learning.
- `university-lms` · selector `university-lms::div.grid-card` · class `grid-card` · public marketing grid card · `background #F6F6F9`; `color #333333`; `0px` border; `30px` radius; `30px` padding; `box-shadow none`; `14px / 400 / 20px "Pretendard Variable"`; two occurrences.
- `university-lms` · selector `university-lms::div.card-item` · class `card-item` · public marketing card item · transparent background; `#333333` text; `12px` radius; no padding; `14px / 400 / 20px "Pretendard Variable"`; six occurrences.
- `home` · repeated computed color frequencies across three routes: `#333333` text 115; `#F6F6F9` background 13; `#FF4438` background 12; `#FFFFFF` background 3. `#666B80` is a university-LMS supporting-text sample (18 occurrences, medium confidence).
- `home` · public H1 sample: `52px / 700 / 72.8px "Pretendard Variable"` (three occurrences); repeated public body sample: `14px / 400 / 20px` (62 text and 56 card occurrences).

## Font evidence and resolution

| Family | Capture result | Source corroboration | Resolution |
|---|---|---|---|
| `Pretendard Variable` | loaded / high; 279 visible computed uses across button, card, H1, H2, and text | jsDelivr variable WOFF2 plus Classum-hosted WOFF2; upstream Pretendard documentation names the variable webfont family | Promoted as the public-marketing UI family. |
| `webflow-icons` | declared / medium; 0 visible uses | embedded `@font-face` data asset | Declared-only; not promoted to typography tokens. |
| System fallback stack | appears after the computed Pretendard family | no loaded-first-family evidence required because the preceding family is loaded | Remains fallback only; never substituted as Pretendard. |

Pretendard’s upstream documentation provides the `"Pretendard Variable"` family/CDN form, and its repository license is SIL Open Font License 1.1. This license evidence explains the asset; the computed-use + loaded FontFace + WOFF2-source combination is the basis for the token claim.

## Tier 2 cross-check attempts

- `https://getdesign.md/classum` — direct retrieval attempted with built-in web search on 2026-07-13; it returned no importable response/value. No Tier 2 token was used.
- `https://styles.refero.design/?q=classum` — direct retrieval attempted with built-in web search on 2026-07-13; it returned no importable response/value. No Tier 2 token was used.

## Conflict matrix

| Field | Tier 1 (supplied live capture) | getdesign | refero | Resolution |
|---|---|---|---|---|
| Public action color | `#FF4438` background and outline/action text across three public routes | no importable value | no importable value | Retain as public-marketing primary only. |
| Public canvas | `#F6F6F9` across all three routes | no importable value | no importable value | Retain as public-marketing canvas. |
| UI family | `Pretendard Variable`, loaded/high, 279 uses | no importable value | no importable value | Retain after FontFaceSet/source corroboration. |
| Public action radius | `8px` on retained actions | no importable value | no importable value | Retain as action-local geometry. |
| Public card radius | `30px` grid-card and `12px` card-item on university-LMS landing | no importable value | no importable value | Retain as separate route-local variants; do not revive legacy 20px card claim. |
| Interaction states | no interactions captured | no importable value | no importable value | Omit all transition/state claims. |

**Unresolved:** none. Tier 2 supplied no competing token value; unavailable directory responses are not treated as confirmation.

## Component coverage

- Capture coverage: score `61`; `3` public routes; `37` baseline variants; `2` component types; `0` observed states; `0` interaction kinds/events.
- The canonical reference keeps only selector-backed baseline public-marketing actions and university-LMS landing cards. It omits unobserved variants rather than generating a menu, dialog, tab, toast, form, or product-LMS component library.

## Context and narrative evidence

- `https://business.classum.com/` — current official home: AI-centered LMS, consultation, and skill solutions; university and corporate-HR audience split. This supports §1, §10–13 public positioning, not visual token inference beyond the supplied capture.
- `https://business.classum.com/university/lms` — current official university LMS narrative: AI-supported preparation, assessment, feedback, and learner support. This supports §11–13 audience/task context, not an authenticated LMS UI claim.
- `https://careers.classum.com/culture` — official culture surface: One Team plus Shine, Uplift, and Motivate language. It is company/culture context and does not populate the public marketing component tokens.
- `https://blog.classum.com/classum--live-sessions` — first-party historical source found through web search: founded in 2018 by Chaerin Lee and Youjin Choi to address learner challenges. This supports the bounded origin statement in §11 only.
- `https://github.com/orioncactus/pretendard/blob/main/packages/pretendard/docs/en/README.md` and `https://github.com/orioncactus/pretendard/blob/main/LICENSE` — official font family and SIL OFL 1.1 license boundary.

## Footer reconciliation

The previous legacy snapshot asserted a 20px card default, a broad illustration/semantic palette, a product-wide footer/nav/pill system, Webflow motion, and unobserved product/form states. The 2026-07-13 bundle supports neither those generalizations nor interaction claims. They were removed or restricted to directly observed public-marketing values.
