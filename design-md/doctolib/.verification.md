# Doctolib — Verification Notes (2026-09-26)

Pipeline: EU wave 1, #6 · Brand: Doctolib (France)

## Scope and collector boundary

- Local Playwright-driven Chrome (Tier 1), 1440×1000, `fr-FR`, realistic desktop user agent.
- **Consent:** Didomi, declined with `#didomi-notice-disagree-button`.
- **Cloudflare Turnstile** places an invisible full-viewport widget (`#turnstile_widget`) that
  intercepts pointer events; it was given `pointer-events: none` so hover could reach the controls.
  Rapid reloads drew one 429. `probe-component-states.mjs` could not find its target through the
  widget, so the re-measurement used a short script with the same method (fresh page per state, a
  real Tab for focus, `:hover` / `:focus-visible` read from the element).
- Logged out; no search was submitted.
- Desktop only, light mode only. Captured and authored 2026-09-26.

### Two collectors

A delegated subagent (Sonnet) ran the deep probe. Re-measured independently: the Rechercher button.
Hover alone (`:hover` true, `:focus-visible` false): no change. Focus alone after 800ms: `box-shadow:
rgb(255,255,255) 0 0 0 2px, rgb(58,89,120) 0 0 0 4px`. A first reading taken immediately after the
Tab key showed transparent zero-spread shadows — the ring arrives on the next frame — so readings
wait before sampling. The four semantic tokens used most were re-read. The two search-field readings
are the subagent's.

The subagent could not reach `doctolib.fr/about` (429) and used press for the company story; on
re-check `about.doctolib.com` resolves and is used as the first-party source. The subagent's founder
list and acquisitions list were not re-verified and are not used.

### Country

Doctolib is a French company (TechCrunch: "French startup"); no parent company. Recorded as **FR**.

## Namespace ownership and the nonsense-path control

3,701 root properties: `--oxygen-*` 3,650, `--tw-*` 51 (Tailwind, excluded). Oxygen breakdown:
`--oxygen-color-*` 3,131 (component 1,510, datadisplay 441, actions 408, forms 197, primitive 188,
navigation 143, semantic 128, feedback 51, overlays 28, foundations 20, shared 17), `--oxygen-scale-*`
194, `--oxygen-core-*` 148, `--oxygen-LOCAL-*` 81, `--oxygen-font-*` 55, `--oxygen-number-*` 30,
`--oxygen-typography-*` 11.

Control: `https://oxygen.doctolib.design/zz-this-does-not-exist` → **HTTP 404**, "zeroheight - not
found", 1,164 bytes, against 49 KB and "Doctolib Design System" at the root. `doctolib.github.io/
storybook/` → 200 "storybook". `www.doctolib.fr/about` → 404 "Doctolib 404", so the main site is not a
catch-all either.

Third-party, not read for tokens: Didomi, Cloudflare Turnstile, Datadog RUM, Sentry, Screeb.

## Proof — Tier 1 live inspect

**Inspected:** 2026-09-26 · **Source:** https://www.doctolib.fr/ · 3,701 root properties.

### Raw samples — semantic colour

- `--oxygen-color-semantic-brand-prominent-base` `#045fae` (re-read) ·
  `-brand-prominent-stronger` `#00264c` (re-read) · `-brand-subtle-base` `#d0e9fb` (re-read) ·
  `-brand-subtle-weak` `#e7f4fd` · `-brand-surface-prominent-weak` `#107aca`
- `--oxygen-color-semantic-common-text-prominent-strong` `#00264c` (re-read) ·
  `-common-text-prominent-base` `#294056` · `-common-surface-subtle-weak` `#f1f7fe` ·
  `-common-text-subtle-base` `#c9d9e8`
- `--oxygen-color-semantic-neutral-prominent-base` `#3a5978` · `-mute-prominent-base` `#7599bd`
- `-danger-prominent-base` `#e5004c` · `-positive-prominent-base` `#00806a` · `-warning-prominent-base`
  `#aa6709` · `-informative-prominent-base` `#007ab3` · `-alternative1/2/3-prominent-base` `#32730d
  #7333cc #ad421f`
- Core equivalents: `--oxygen-core-blue-160` `#00264c` · `--oxygen-core-blue-130` `#045fae` ·
  `--oxygen-core-grey-110` `#3a5978` · `--oxygen-core-grey-130` `#294056`

### Raw samples — type, fonts, census

- `--oxygen-font-semantic-title-l-bold` `bold 1.8rem/2.4rem Montserrat,arial,sans-serif`
- `document.fonts` loaded: Montserrat 700, Roboto 400, 500, 700; stylesheet
  `assets.doctolib.fr/fonts/family-roboto-300-400-500-700-montserrat-400-500-700-merriweather-300-400-700.css`
- `<p>` colour: `#00264c` 15 of ~23 · `#294056` 3 · `#192634` 2 · `#4b739b` 2
- Backgrounds: `#ffffff` 11 · `#107aca` 6 · `#00264c` 2 · `#e7f4fd` 1 · `#f1f7fe` 1
- Shadow: `rgba(5,43,71,.07) 0 2px 4px, rgba(5,43,71,.09) 0 5px 25px`; radii 8px, 12px, 999px, the
  32px pill ends

### Raw samples — controls

| control | rest | hover | pressed | focus |
|---|---|---|---|---|
| Rechercher (re-measured) | `#00264c` / `#ffffff`, `0 32px 32px 0`, 60px, 14px/400 Roboto | no change (`:hover` true) | no change (subagent, `:active` true) | `0 0 0 2px #ffffff, 0 0 0 4px #3a5978` |
| Practitioner field (subagent) | `#ffffff` / `#294056`, border `#294056`, `32px 0 0 32px`, 60px, `0 16px 0 46px` | no change | not measured | nothing drawn (outline style none) |
| Location field (subagent) | `#ffffff` / `#294056`, border `#294056`, 0, 60px, `0 46px` | no change | not measured | nothing drawn |

## Narrative sources

- https://about.doctolib.com/ — description: "Since 2013, Doctolib has been committed to one
  objective: Building the healthcare we all dream of, together with caregivers and patients"; a quote
  credited to "Stanislas Niox-Chateau, Co-founder & CEO" about hundreds of thousands of health
  professionals and millions of people across Europe.
- https://careers.doctolib.com/ — a Brand and Design posting: "You will own Oxygen, Doctolib's design
  system - much more than a component library. Oxygen is how we deliver our brand identity
  consistently throughout the product".
- Press, titles verified: https://techcrunch.com/2022/03/15/healthcare-tech-platform-doctolib-reaches-6-4-billion-valuation/
  ("Healthcare tech platform Doctolib reaches $6.4 billion valuation", 15 Mar 2022);
  https://www.journaldugeek.com/2025/04/30/doctolib-nest-pas-rentable-mais-il-va-le-devenir/ ("Doctolib
  n'est pas rentable, mais il va le devenir", 30 Apr 2025).
