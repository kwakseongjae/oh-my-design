# AWS Cloudscape verification — 2026-07-13

## Proof — Tier 1 supplied live inspect

Machine artifact: `artifacts/reference-evidence/aws-cloudscape.json` (captured `2026-07-13T14:58:56.786Z`). This reference uses that supplied artifact as its only raw computed-style, font, component, and interaction evidence. No browser recapture and no MCP session were run.

**Inspected:** 2026-07-13

**Sources:**

1. Marketing/product surface: https://cloudscape.design/
2. Official component documentation: https://cloudscape.design/components/
3. About/corporate surface: https://cloudscape.design/about/

The artifact reports 3 public surfaces, coverage 83, 5 component types, 32 component variants, 3 dialog-opening interactions, and 1 observed interaction-state label. Marketing, documentation, about/corporate narrative, official typography documentation, declared font records, and license material are retained as separate domains.

### Raw samples

| Surface and selector | Computed sample |
|---|---|
| `home`, public text aggregate | `rgb(15, 20, 26)` / `#0f141a`; Open Sans; 14px / 400 / 20px. |
| `home`, public link aggregate | `rgb(41, 94, 255)` / `#295eff`; observed as text and border color. |
| `home`, display text aggregate | Open Sans; 42px / 400 / 48px; -1.26px letter spacing. |
| `components`, `surface-2::div.card` | static card: `rgb(15, 20, 26)` / `#0f141a`, transparent background, 0px border, 0px radius, 0px padding, 305px × 272px, no shadow, Open Sans 14px / 400 / 20px. |
| `components`, search input representative | `rgb(255, 255, 255)` background, `rgb(140, 140, 148)` / `#8c8c94` 1px border, 8px radius, 6px 12px 6px 36px padding, 16px / 400 Open Sans. |
| `home`, dialog overlay after observed interaction | `rgba(35, 43, 55, 0.7)` background, 0px radius, Open Sans 14px / 400 / 20px. |
| `home`, public secondary text aggregate | `rgb(66, 70, 80)` / `#424650`. |

### Font evidence

| Evidence class | Result |
|---|---|
| Official product-use | Cloudscape typography documentation calls Open Sans the primary and default font; its developer guidance says global styles include the font. |
| Live computed surface-use | Open Sans is loaded/high with 865 uses across the three supplied public routes and listed roles including body, button, card, dialog, headings, inputs, and lists. |
| Official distributed asset | Cloudscape global styles document `@font-face` Open Sans files; the artifact records Cloudscape-hosted Open Sans WOFF2 URLs. |
| Declared-only | `amazonEmber`, `amazonEmber Fallback`, and `openSans Fallback` are declared with zero visible uses. |
| Unresolved | `Times` has six low-confidence unresolved dialog/text uses and is not promoted. |

## Tier 2 attempts and conflict matrix

| Field | Tier 1 supplied capture / official source | getdesign | Refero | Resolution |
|---|---|---|---|---|
| Public link accent | `#295eff` text/border across all three public routes | Direct `https://getdesign.md/aws-cloudscape` attempt was rejected by the built-in web safety layer; no value used | Direct `https://styles.refero.design/?q=Cloudscape` attempt was rejected by the built-in web safety layer; no value used | Tier 1 retained. |
| Default typeface | Loaded/high Open Sans, 865 uses; official typography calls it default | No usable record | No usable record | Live computed and official documentation agree; Open Sans retained. |
| Static component-directory card | `surface-2::div.card`, 305px × 272px, 0px radius, no shadow | No usable record | No usable record | Tier 1 retained; only default measured fields promoted. |
| Dialog activity | Three dialog-open interactions; no selected stable dialog shell token | No usable record | No usable record | Interaction evidence retained in prose only; no dialog token added. |

## Conflict summary

none

Tier 2 failures are tool-access boundaries, not absence findings. No computed styles, font values, component values, or interactions were sourced from Tier 2. There is no unresolved promoted-token conflict.

## Components and provenance

- **Static component-directory card:** `components` surface, selector `surface-2::div`, class `card`; 36 occurrences in the representative 305px × 272px variant. It is a selector-backed default component token with no interactive-state claim.
- **Observed dialog interaction:** one dialog-opening interaction on each of `home`, `components`, and `about`; the artifact labels the observed state `dialog-open`. This does not establish a general dialog design token.
- **Observed controls not promoted:** search triggers/inputs and icon controls carry public geometry, but no button/input token was chosen because this packet does not establish a single component state contract for them. No states are inferred.

## Context and narrative evidence

- https://cloudscape.design/about/ — first-party account of AWS use since 2016, ongoing research/customer-feedback evolution, and the system's open-source purpose. Used for company/product history and current evolution, not raw UI values.
- https://cloudscape.design/ — first-party system overview: guidelines, components, resources, tools, modes, accessibility, responsiveness, and demos. Used for product context only.
- https://cloudscape.design/foundation/visual-foundation/typography/ — official Open Sans/default-face guidance. Used for official product-use context, separate from live computed use.
- https://cloudscape.design/get-started/for-developers/global-styles/ — official `@font-face`, visual-mode, and density guidance. Used for distributed-asset context, not for an unmeasured component token.
- https://cloudscape.design/get-started/for-designers/design-resources/ — official Figma/design-resource and Open Sans distribution context.
- https://github.com/cloudscape-design/components/blob/main/LICENSE — Apache License 2.0 for the Cloudscape components repository. Used solely as the code-license boundary; it is not a font license or a computed-style source.

Marketing (`/`), component documentation (`/components/`), about narrative (`/about/`), typography/global-style documentation, declared-only font records, and the repository license remain separate evidence domains. No authenticated AWS product UI was inspected.
