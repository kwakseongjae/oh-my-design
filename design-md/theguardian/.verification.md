# The Guardian — Verification Notes (2026-09-23)

Pipeline: UK wave 1, #4 · Brand: The Guardian (theguardian.com)

## Scope and collector boundary

- Local Playwright-driven Chrome (Tier 1), 1440×1000, `en-GB`, `getComputedStyle` on the document
  element and on individual controls; state readings through
  `web/scripts/probe-component-states.mjs` (fresh page per state, focus under a real Tab key before
  any pointer movement).
- Logged out. Consent: the delegated probe rejected non-essential consent ("No, thank you"); nothing
  was accepted. No sign-in, no support or subscription flow.
- Desktop only, light mode only. Captured and authored 2026-09-23.

### Two collectors

A delegated subagent (Sonnet) ran the deep probe. Re-checked independently: the headline — masthead
controls do not change on hover and draw `box-shadow: 0 0 0 3px #0077b6` on focus — re-measured on a
masthead link (hover: none of the seven values change; focus: `rgb(0, 119, 182) 0 0 0 3px`); 13
custom-property values re-read (masthead, top bar, article link, ink, pillar titles and rules,
highlight yellow, sport competition) and all matched; loaded fonts re-read; Source's home and its
nonsense-path control re-fetched; the font repository README re-read; both press titles re-fetched.

The subagent's search-field reading covered five of seven values, so the field is described but not
declared. Its note that 198 elements compute the UA link blue `#0000ee` is unexplained and not used.

## Namespace ownership and Source

**557 root custom properties (about 560 across stylesheets), 238 prefixes, all Guardian-authored.**
The subagent checked every name against framework, consent-manager and analytics signatures
(`--tw-`, `--sp_`, `--mui`, Sourcepoint, OneTrust, Didomi, Ophan, Taboola, Prebid, styled-components,
Radix …) with no match. The scout's earlier "439 framework-looking" count came from a heuristic that
keyed on generic words (card, button, section) that are the Guardian's own component names.

- `https://theguardian.design/` → 200, 50,226 bytes, Zeroheight-hosted Source documentation.
- `https://theguardian.design/zz-this-does-not-exist` → **404**, 1,164 bytes ("zeroheight – not
  found"). A routed site, not a catch-all.
- Source's core palette values quoted in the reference (brand.100 `#001536`, .300 `#041F4A`, .400
  `#052962`, .500 `#0077B6`, .600 `#506991`, .800 `#C1D8FC`) are the subagent's reading of that
  page; the three that are live tokens (.300, .400, .500) were re-read on theguardian.com.

Third-party: Sourcepoint (consent, proxied via `sourcepoint.theguardian.com`); after rejecting and
scrolling, `adserving.optoutadvertising.com` appears — the census was taken before it loaded. Ophan
is the Guardian's own analytics.

## Proof — Tier 1 live inspect

**Inspected:** 2026-09-23 · **Source:** https://www.theguardian.com/uk · `lang="en"`, 21,210
characters of body text, 557 root custom properties.

### Raw samples — tokens (re-read)

`--masthead-nav-background` `#052962` · `--masthead-top-bar-background` `#041F4A` ·
`--article-link-text` `#C70000` · `--headline-colour` `#121212` · `--article-text` `#121212` ·
`--article-section-title-opinion` `#C74600` · `--section-border-opinion` `#E05E00` ·
`--article-section-title-culture` `#866D50` · `--article-section-title-lifestyle` `#BB3B80` ·
`--article-section-title-sport` `#0077B6` · `--star-rating-background` `#FFE500` ·
`--masthead-veggie-burger-background` `#FFE500` · `--sport-competition-text` `#005689`.

### Raw samples — tokens (subagent)

`--branding-link-text` `#C70000` · `--article-background` `#FFFFFF` · `--masthead-nav-link-text`
`#FFFFFF` · `--masthead-nav-link-text-hover` `#FFE500` (not applied on hover at this width) ·
`--callout-highlight-background`, `--byline-background`, `--age-warning-background`,
`--football-match-header-live-background`, `--carousel-arrow-background-hover`,
`--nav-reader-revenue-link-text` — all `#FFE500` · news title `#121212`, rule `#333333`.

### Raw samples — fonts

`document.fonts` loaded: `GH Guardian Headline` 500, 700; `GuardianTextSans` 400, 700.
`GuardianTextEgyptian` preloaded via `<link rel="preload" as="font">`, not loaded as a face on the
front. `GT Guardian Titlepiece` Bold fetched from `interactive.guim.co.uk/fonts/garnett/`.

### Raw samples — census (subagent)

Text: `#121212` ×4,554 · `#707070` ×416 · `#ffffff` ×414 · `#c70000` ×53 · `#bb3b80` ×38 ·
`#333333` ×34 · `#866d50` ×33 · `#0077b6` ×28. Backgrounds: `#ffe500` ×54 · `#ffffff` ×53 ·
`#f6f6f6` ×35 · `#ededed` ×20 · `#052962` ×16 · `#dcdcdc` ×14 · `#506991` ×5. Borders: `#bababa`
×31 · `#dcdcdc` ×20. Radius: `50%` ×70 · `100%` ×15 · 40px ×14 · 12px ×5 · 1000px ×2. One resting
shadow: `rgba(0,0,0,0.1) 0 0 0 1px`.

### Raw samples — controls

| control | rest | hover | focus |
|---|---|---|---|
| Masthead link (re-measured) | transparent / `#ffffff`, 64px, `4px 0 4px 4px`, 17px/700 | no change (7 values) | `box-shadow: rgb(0,119,182) 0 0 0 3px`, outline `none` |
| Sport, Print subscriptions, Show more, Edition (subagent) | transparent / `#ffffff` | no change | the same 3px `#0077b6` ring |

The tool's former "pressed changed" verdict on these controls came from `outline-width` going
3px → 0px with `outline-style: none` — nothing drawn. The summary now ignores undrawn outlines.

## Narrative sources

- https://raw.githubusercontent.com/guardian/fonts/main/README.md — re-read: the faces are owned by
  Schwartzco, Inc. t/a Commercial Type and licensed only for Guardian websites or apps.
- https://theguardian.design/ — Source; the subagent quotes the typography page: "The Guardian has
  four bespoke typefaces", and the spacing page's 4px increment with 40 / 24 / 16px spacing.
- https://www.creativereview.co.uk/guardian-tabloid-redesign/ — title verified: "Goodbye Berliner,
  hello tabloid: Alex Breuer on the Guardian's redesign".
- https://www.itsnicethat.com/news/theguardian-graphicdesign-150118 — title verified: "The Guardian
  unveils redesign across print and online".
- A first-party theguardian.com article on the 2018 redesign could not be reached from the
  subagent's tools; none is cited.
