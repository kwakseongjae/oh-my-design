# Cursor — verification record

**Reference:** `cursor`
**Checked:** 2026-07-13
**Method:** supplied deterministic collector evidence (`artifacts/reference-evidence/cursor.json`) plus first-party official context, brand, product/docs, and licence checks and both Tier 2 directory checks. No browser capture was rerun and no MCP was used.

## Scope and source domains

| Source | Domain | Role in this update | Boundary |
|---|---|---|---|
| `https://cursor.com/` and `https://cursor.com/en-US` | First-party public marketing | Current computed styles, FontFaceSet, components, and static markup in supplied evidence | The collector has three records but only two URLs; `home` and `surface-2` duplicate `/`. This is public marketing, not an authenticated editor or dashboard. |
| Embedded editor-like demos on the public home | Marketing-page embedded demo | Bounded source for the captured input, selected tab, disabled control, Lato/EB Garamond, and isolated berkeleyMono use | It is not an independently inspected Cursor product application; do not promote demo chrome to general product UI. |
| `https://cursor.com/docs` and linked official quickstart/agent materials | First-party documentation content | Current product framing, workflow, and stakeholder context | No docs-chrome computed-style capture; no visual token or component is inferred. |
| `https://cursor.com/en-US/brand` | First-party brand asset guidance | Product naming, logos, app icons, and avatars | Provides no official visual-token specification or public font package/licence. |
| `https://cursor.com/en-US/changelog/1-0` | First-party product history | Cursor 1.0 product evolution context | Does not establish current UI tokens. |
| `https://cursor.com/licenses` | First-party licence notice | Boundary for open-source components in the Cursor IDE | Not a licence for CursorGothic or the other site-hosted font files. |
| `https://getdesign.md/cursor` | Tier 2 catalog | Cross-check only | Describes one community DESIGN.md as “Sleek dark interface, gradient accents”; it is not Tier 1 and conflicts with current supplied public evidence. |
| `https://styles.refero.design/?q=Cursor` | Tier 2 catalog | Cross-check attempt | Built-in web open returned a non-retryable internal error; no result/card was usable. |

The bundle was captured at `2026-07-13T12:17:47.149Z`. It reports three capture records, two unique public marketing URLs, eight component types, 84 component variants, three static state markers, coverage score 100, and `interactionCount: 0`. The collector labels its URLs `product-surface`, but the captured URLs are public home/locale marketing pages; this record does not silently reclassify them as a logged-in product surface.

## Proof — Tier 1 live inspect

**Inspected:** 2026-07-13
**Sources:** `https://cursor.com/`, `https://cursor.com/en-US`
**Evidence artifact:** `artifacts/reference-evidence/cursor.json` (captured 2026-07-13T12:17:47.149Z)

### Raw samples

- `home::body` — `color: rgb(38, 37, 30)`; `background-color: rgb(247, 247, 244)`; `padding: 52px 0px 0px`; `font: 16px / 400 / CursorGothic`; public marketing body.
- `home::[data-omd-capture="64"]` — `color: rgb(247, 247, 244)`; `background-color: rgb(38, 37, 30)`; `border: 1px`; `border-radius: 3.35544e+07px`; `padding: 12.48px 21.6px 12.8px`; `font: 16px / 400`; public primary action.
- `home::[data-omd-capture="8"]` — `color: rgb(247, 247, 244)`; `background-color: rgb(38, 37, 30)`; `border: 1px`; `border-radius: 3.35544e+07px`; `padding: 5.6px 10.5px 5.88px`; `font: 14px / 400`; compact public action.
- `home::[data-omd-capture="101"]` — `color: rgb(38, 37, 30)`; `background-color: rgb(230, 229, 224)`; `border-color: oklab(0.263084 -0.00230259 0.0124794 / 0.025)`; `border-radius: 3.35544e+07px`; `padding: 5.6px 10.5px 5.88px`; `font: 14px / 400`; compact secondary action.
- `home::[data-omd-capture="53"]` — `color: rgb(38, 37, 30)`; `background-color: rgb(242, 241, 237)`; `border-radius: 4px`; `padding: 15.9px 17.5px 20px`; `font: 16px / 400`; public feature card.
- `home::[data-omd-capture="11"]` — `color: rgb(38, 37, 30)`; `background-color: rgb(247, 247, 244)`; `border: 0px 1px 0px 0px`; `padding: 0px 8px 1px 12px`; `font: 14px / 400`; static selected tab in embedded marketing demo.
- `home::[data-omd-capture="14"]` — `color: rgb(38, 37, 30)`; `background-color: rgba(0, 0, 0, 0)`; `padding: 8px 8px 6px`; `font: 13px / 400 / system-ui`; embedded marketing-demo input.

### Font evidence and resolution

| Family / claim | Computed surface use | FontFaceSet / source corroboration | Official asset or licence | Resolution |
|---|---|---|---|---|
| `CursorGothic` | 643 visible uses across public body, headings, actions, cards, menus, tabs, toggles, and list items | `loaded` / high confidence; four Cursor-hosted WOFF2 URLs | No public font package or licence located on Cursor’s official brand/licence pages | **Verified current public UI/content family.** Promote as sole general family token; do not imply redistribution permission. |
| `Lato` | 24 visible uses in embedded marketing examples | `loaded` / high confidence; 20 Cursor-hosted font-source URLs | No first-party public product-use or licence statement found | **Verified live embedded-demo use only.** Do not promote as a Cursor general UI family. |
| `EB Garamond` | 19 visible uses in embedded marketing examples | `loaded` / high confidence; 14 Cursor-hosted font-source URLs | No first-party public product-use or licence statement found | **Verified live embedded-demo use only.** Do not promote as a Cursor general UI family. |
| `berkeleyMono` | One visible input use in an embedded marketing example | `loaded` / medium confidence; two Cursor-hosted WOFF2 URLs | No first-party public product-use or licence statement found | **Verified isolated live occurrence only.** Retain as bounded metadata, not a general family token. |
| `system-ui`, `-apple-system` | 117 and six observed uses respectively | System classification; no font source | N/A | **System values.** Do not use as a substitute for a named Cursor family. |
| `CursorGothic Fallback`, `CursorIcons16`, KaTeX families, `Lato Fallback` | Zero visible uses | Declared-only | No first-party product-use statement located | **Declared-only.** Do not promote to machine tokens or specimen use. |
| Former `jjannon` claim | No visible use in supplied current evidence | No corroborating loaded FontFace entry | No official Cursor source located | **Unresolved; removed.** |

## Component provenance and state boundary

| Component claim | Surface and selector provenance | Default/current evidence | State evidence | Resolution |
|---|---|---|---|---|
| Public filled primary action | `home`; `home::[data-omd-capture="64"]` | `#26251e`, `#f7f7f4`, 1px border, full pill, `12.48px 21.6px 12.8px`, 16px/400 CursorGothic | None | Preserve default only. |
| Compact filled action | `home`; `home::[data-omd-capture="8"]` | `#26251e`, `#f7f7f4`, 1px border, full pill, `5.6px 10.5px 5.88px`, 14px/400 CursorGothic | None | Preserve default only. |
| Compact secondary action | `home`; `home::[data-omd-capture="101"]` | `#e6e5e0`, low-alpha oklab 1px border, full pill, `5.6px 10.5px 5.88px`, 14px/400 CursorGothic | None | Preserve default only. |
| Public feature card | `home`; `home::[data-omd-capture="53"]` | `#f2f1ed`, 4px, `15.9px 17.5px 20px`, 16px/400 CursorGothic | None | Preserve default only. |
| Embedded selected tab | `home`; `home::[data-omd-capture="11"]` | `#f7f7f4`, `#26251e`, right 1px oklab edge, `0px 8px 1px 12px` | Static `selected` marker only | Describe as captured static markup, not a tab interaction contract. |
| Embedded input | `home`; `home::[data-omd-capture="14"]` | Transparent, `#26251e`, `8px 8px 6px`, 13px/400 system-ui | None | Bounded marketing-demo input only. |
| Embedded disabled compact control | `home`; `home::[data-omd-capture="17"]` | `#e1e0db`, muted oklab text, full pill | Static `disabled` marker only | Do not infer disabled behavior or accessibility treatment. |

The raw bundle contains static `selected`, `unchecked`, and `disabled` labels, while its top-level `interactions` array is empty. It also contains CSS-class text that names hover/focus/active/transition utilities. Neither is evidence of a measured interactive trigger or variant, so §4 and the token block omit those variants.

## Tier 2 cross-check

| Service | Attempt | Result | Use |
|---|---|---|---|
| getdesign | `https://getdesign.md/cursor` | Opened successfully. It lists one community DESIGN.md and summarizes it as “Sleek dark interface, gradient accents.” | No token or component value used; the summary conflicts with the current warm public capture. |
| Refero | `https://styles.refero.design/?q=Cursor` | Query attempted with built-in web open; it returned a non-retryable internal error, so no result card/page could be read. | No token or component value used. |

## Conflict matrix

| Field | Tier 1 (current public surfaces) | getdesign | refero | Resolution |
|---|---|---|---|---|
| Public primary action | `#26251e` / `#f7f7f4` / 1px / full pill | No corresponding measured spec | No usable record | Tier 1 retained with selector provenance. |
| Public canvas and cards | `#f7f7f4` canvas; `#f2f1ed` 4px cards | “Sleek dark interface, gradient accents” summary | No usable record | Tier 1 retained; Tier 2 is not used. |
| General public family | CursorGothic loaded/high, 643 visible uses, four sources | No font specification | No usable record | Promote CursorGothic only. |
| Embedded-demo fonts | Lato, EB Garamond, and berkeleyMono have computed/source evidence in bounded examples | No font specification | No usable record | Preserve boundary; do not promote general tokens. |
| `jjannon` body family | Absent from supplied current computed/FontFace evidence | No support | No usable record | Roll back legacy claim; leave unresolved. |
| Hover/focus/error/motion rules | `interactionCount: 0` | No usable spec | No usable record | Omit; never infer from class names. |
| Authenticated app/docs chrome | Not captured | No usable spec | No usable record | Keep absent rather than substitute marketing or docs styles. |

## Context and narrative evidence

- **Cursor brand guidelines — first-party brand context:** says assets are provided to represent Cursor consistently; distinguishes 2D/2.5D logo treatments and app icon/avatar variants; directs third parties to call the product “Cursor.” Used for §1, §11, and §12 only; it is not a design-token or font-licence source.
- **Cursor documentation — first-party product context:** describes Cursor as an AI-powered code editor that understands a codebase and helps through natural language. Official quickstart and agent materials name Tab, Inline Edit, Agent, tools, terminal commands, and code editing. Used for §1, §10, §11, and §13; no documentation visual chrome is promoted.
- **Cursor 1.0 changelog — first-party evolution context:** records Bugbot, Background Agent availability, Jupyter support, memories, MCP setup, and dashboard changes. Used for §10–§11; it does not establish visual tokens.
- **Cursor Enterprise and Security — first-party stakeholder context:** identify organization controls, privacy mode, model/MCP/rule configuration, and security expectations. Used for §13; they do not establish enterprise-screen UI tokens.
- **Cursor licence notice — first-party licence boundary:** publishes notices for open-source software components used in the IDE. It does not establish a public licence for CursorGothic, Lato, EB Garamond, or berkeleyMono files delivered by the public site.

## Reconciliation notes

- The legacy reference treated prior homepage/pricing observations as a complete Cursor system and promoted `jjannon`, broad orange/error/success semantics, generic card variants, and a motion/state catalogue. The supplied 2026 evidence supports none of those broad claims, so they were removed rather than preserved by plausibility.
- `CursorGothic` satisfies the required computed-use plus FontFaceSet/source corroboration standard. Lato, EB Garamond, and berkeleyMono also meet that standard for their narrowly observed page contexts; their limited marketing-demo use does not authorize promotion to a general UI family.
- The official brand guide distributes visual assets but no public font licence. Runtime availability is recorded without claiming that the files are reusable outside Cursor’s delivered page.
- The collector has two unique public homepage URLs, not an authenticated product surface or documentation capture. Public marketing, embedded-demo, documentation, and declared-only assets remain separate source domains.
- No public trust state is claimed here; the deterministic evaluator owns the verification outcome.
