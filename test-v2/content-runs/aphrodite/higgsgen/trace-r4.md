# trace-r4 — Higgsgen r4 빌드 기록 (큰 것 하나 · 저절로 도착 · 한 화면 한 초점)

산출: `render-r4.html` (**1,206,015 B = 1.15 MiB** · §7 재빌드 · §8 라운드2, 네트워크 요청 0 · GSAP 0 · 이미지 42장 전부 webp base64 인라인)
빌드: `runs/build-r4.mjs` + `runs/template-r4.html` → `node test-v2/content-runs/aphrodite/higgsgen/runs/build-r4.mjs`

---

## 1. 스니펫 출처 · 라이선스 · 각색

| 쓴 곳 | 출처 | 라이선스 | 바꾼 것 |
|---|---|---|---|
| 핀 엔진 S1·S3·S6 | `docs/design-excellence/fx-library/fullbleed-scale-reveal/snippet.{css,js}` | oh-my-design 자작 · MIT · 외부 의존 0 | 스티키+`--e` 로직만 가져와 **한 함수로 3개 핀을 돌린다**(`data-k`·`data-ease`). `end = travel × k`, `bottom bottom` 없음. 반경 26px → `--r-stage`(10). S6 는 `scale()` 대신 프레임의 `width/height` 를 키운다(스케일 트랜스폼은 자식 텍스트까지 늘린다). |
| 전폭 띠 S2 | `…/wide-drum/snippet.{css,js}` | 〃 | 반경 16px → `--r-card`(5), gap 1.4vw → `--s16`, 타일 비 .74 → .67(2:3 원본). 키보드 ←/→ 추가(README 의 포커스 요구). 라벨은 띠 밖 정적 한 줄로 옮겼다(README: 띠 안에 읽을 것 금지). |
| 지속 확장 S5 | `…/persistent-expand/snippet.{css,js}` | 〃 | 반경 18px → `--r-card`, gap 10px → `--s12`, `--fx-pex-dur` 620ms → **160ms**(LI-14: opacity/transform/색 이외 속성의 트랜지션은 200ms 이하여야 한다 — `flex-grow` 는 레이아웃 속성이다). 호버 리프트 `translate: 0 -2%` → `filter: brightness(1.06)`(DESIGN §2 "Hover never transforms"). 본문 span 을 block 으로(제목·설명이 한 줄에 붙던 결함). |
| 마스크 리빌 S8 | `…/mask-wipe-reveal/snippet.css` | 〃 | `animation-timeline` 분기 삭제(Firefox 미구현 · 예산). 900ms → `--t4`(720ms). JS 가 있을 때만 `armed` 를 붙여 가린다 — JS 가 없으면 처음부터 보인다. |

`@property --p / --wipe / --beam / --beam-a` 4개 외에 외부 라이브러리는 없다.

`<head>` 에 다이얼을 선언한다: `<meta name="omd-density" content="4">` · `<meta name="omd-motion" content="7">`(스토리보드 밀도 4 / 모션 7 — 검사기가 밀도 ≤5 에서 LI-24~26 개수 바닥을 완화한다).

## 2. 스토리보드 대비 바꾼 것 (전부 계측 근거)

| # | 스토리보드 | 구현 | 왜 |
|---|---|---|---|
| S1 | hero-01 → **hero-02 크로스페이드** + 켄번즈 | hero-01 **한 장** 줌스루(1→1.12) + 광원 드리프트·호흡 | (a) 겹친 풀블리드 두 장은 LI-36(1등:2등 면적비 ≥2)에서 **항상 1.00** 이다 — 초점이 둘이라고 읽힌다. (b) 켄번즈는 폴드에 무한 이미지 이동을 만들어 `text-contrast` 의 포커스 링 측정(before/after 픽셀 차이)에 사진 픽셀을 섞어 넣는다 → 링 27~30% 미달로 FAIL(도구 주석의 ninefold 사례와 동일 증상). 유휴 층은 **빛**으로 남겼다 — DESIGN §1 "light moves, images don't". 무한 애니 폴드 합계 2(드리프트 19s + 호흡 13s). |
| S3 | K 0.55 | **K 0.50** | 섹션 중앙(검사기 표본 위치)에서 8프레임 중 **1장만** 보이게 해 초점 1을 만든다. 홀드가 0.9 → **1.0vh** 로 길어졌다(정착 후 최종 프레임+캡션 체류가 늘었으므로 방향이 같다). |
| S4 | 썸 3 · 위 레이어 `<img>` | 썸 **4**(키 1~4 와 1:1) · 위 레이어는 **배경 이미지 면**(폭 = 진행률) | 겹친 `<img>` 두 장은 rect 가 같아 LI-36 이 1.00 으로 읽는다. 위 레이어를 `background-image` 면으로 만들면 폭 44% = 면적비 2.27 이 되고 와이프 형상은 동일하다(cover 기하를 `background-size/position` 으로 맞춰 계산). 기본 위치는 50% → **44%**(대칭 회피 + 면적비 여유). |
| S1 nav | 워드마크 + 라임 pill | 워드마크(크롭 마크 SVG + 이름) + **라임 pill `Start a render`(44px)** | 처음엔 폴드 단위 6개를 우려해 pill 을 뺐으나, LI-37 은 nav 를 먼저 세고 **자손을 전부 nav 단위에 합친다** — pill 을 nav 안에 두면 단위는 5로 유지된다. scrollY 0 실측: 폴드 행동 2개(`Start a render` 129×44 @y32 · `Start with one line` 221×52 @y671, 둘 다 opacity 1) · 첫 뷰포트 라임 요소 **2**(≤3). 홀드에서 사라지지 않고 그대로 남는다. |
| 그레인·점 텍스처 | `<div>`+`<svg>` 레이어 | `body::after` · `.stage--dots::before` | 뷰포트를 덮는 `<svg>` 는 LI-37 이 **폴드 미디어 단위**로 세고, 점 레이어(opacity .06)는 `text-contrast` 의 no-JS 감사에서 "숨은 큰 요소"로 잡힌다. 질감은 콘텐츠가 아니므로 의사 요소로 내렸다. 그레인 자체(feTurbulence)는 유지 — LI-29 통과. |
| 타입 | 148 / 99 / 66 / 44 | **112 / 80 / 56 / 40** | 빌드 중 DESIGN §3 이 개정 2로 바뀌었다(레퍼런스 14곳 실측 상한 112px, 148 은 LI-7 대역 초과). h1 112 / body 16 = **7.00×** 로 LI-7 통과(148 이었을 때는 9.25 로 FAIL — body 상한이 17px(LI-8)이라 148 로는 산술적으로 통과가 불가능했다). |
| 페이지 | 14.5vh | **14.51vh**(실측) | S5·S7 패딩을 96/64 → 64/48·64 로, S2 를 120 → 112vh 로 줄여 예산 안에 넣었다. |

## 3. 실측 (플레이라이트 · 1440×900 / 390×844, dpr 1)

**정착(핀 3개)** — 해석값은 엔진 공식 `end = travel × k`, 실측은 `scrub-timing-probe`(샘플 모드, 300px 격자 = ±0.33vh):

| 핀 | 트랙 | K | 정착% (해석/실측) | 홀드 pinLead (해석/실측) | lead | 홀드에 있는 것 |
|---|---|---|---|---|---|---|
| S1 | 2.20vh | 0.50 | 50.0 / **30.3**\* | 0.60 / **0.5**vh | 1.5vh | 서브헤드 1줄 + CTA |
| S3 | 3.00vh | 0.50 | 50.0 / **50.3** | 1.00 / **1.0**vh | 2.0vh | 최종 캡션 "High angle, dusk, 50mm." + "8 frames, one prompt" |
| S6 | 2.40vh | 0.55 | 52.1 / **59.2** | 0.63 / **0.4**vh | 1.4vh | h2 56px + 1줄(비네트) |

\* S1 은 문서 맨 위라 프로브의 창이 `max(0, top−vh)` 로 잘린다(진입 활주로 0) — 같은 정착점(스크롤 540px)을 창 길이 1980px 로 나눈 값이라 30.3% 로 나온다. 해석값 50%는 활주로가 있을 때의 값이다.

**섹션별 최대 미디어 점유율(뷰포트 면적 대비, 해당 섹션이 가장 잘 보이는 위치에서):**
`s1 1.00 · s2 0.137(타일 1장; 띠 전체는 100vw×56vh) · s3 1.00 · s4 0.90 · s5 0.426 · s6 1.00 · s7 0.36 · s8 1.00 · s9 0(텍스트 밴드) · s10 1.00` → 중앙값 **1.00**(LI-34 기준 0.35).

**그 밖:** 페이지 **14.51vh** 데스크톱 / **10.35vh** 모바일 · 폴드 의미 단위 **5**(nav·CTA·h1·lede·미디어) · **호버 관문 0**(LI-35: CSS 규칙 0 + JS 포인터 층 2개 프로브 결과 0) · 전체 스크롤 여정 **콘솔 에러 0**(1440·390 양쪽) · 폴드 미디어 3 · 섹션 10 · 간격 1종 · 최장 빈 구간 0.33vh(S9 상단).

## 4. 검사기 (마지막 실행)

| 도구 | 결과 |
|---|---|
| `render-integrity` | **PASS** (1440·390). 정보 행 6건은 전부 `overflow:clip` 조상 안의 드럼/줌 레이어. |
| `text-contrast` | **PASS** · no-JS 숨은 요소 **0** |
| `landing-integrity` | **FAIL 0 · WARN 0** — LI-1~40 전부 ok(LI-34~40 행 존재 확인). LI-31 **42/42**(LC-43 색보정: `main img{filter:contrast(1.04) saturate(1.06)}` 프리셋 1개를 모든 내용 이미지에 · 광원 복사본만 자기 블러 유지) · LI-25 폴드 미디어 3 · LI-36 위반 1(허용 1, `#s2` 균일 타일 띠 = 한 초점) |
| `scrub-timing-probe` | 8창 중 **DEFECT 2** — `#s2`(드럼 띠: 입력 0 에서도 계속 도는 유휴 층이라 정의상 "정착"이 없다) · `#s9`(0.85vh 밝은 밴드: 뷰포트보다 짧아 `secBottom−vh` 가 섹션이 다 보이기 **전**에 지나간다 — 진입 리빌이 끝나는 시점이 구조적으로 그 뒤다). 핀 3개는 전부 OK. |

닫은 회귀(리뷰 r3): 토큰 밖 hex **0** · 토큰 밖 반경 **0**(빌드가 자체 검사) · 간격 1종 · S3 스크램블 삭제(즉시 스왑 + 밑줄 이동, 8프레임 전부 판독 가능) · drift-collage/진입 커튼/스택 팬/십자 열림/틸트/원통/자석 커서 삭제 · 홀드마다 읽을 것 1개 · 390 최소 13px.

## 5. 스크린샷 자기 비평 (`reviews/screenshots/r4-1440-*.jpg`, `r4-390-*.jpg`, 각 ≤70KB)

- **S1** 1440: 풀블리드 + h1 112px 3줄 + 서브 + 라임 CTA. 좌측 스크림(96deg)이 이미지의 왼쪽 절반을 거의 지운다 — h1 판독(min 1.97, 0.2%)을 위해 남겨 뒀다. 390: 좌측 가중 스크림이 세로 화면에서 사진을 다 덮었다 → 스크림을 **하단 가중**으로 바꾸고 히어로의 `object-position` 을 **76%** 로 옮겨 조명이 들어간 터널 끝이 프레임에 남게 했다(수정 후 재캡처, h1 대비 min 5.53).
- **S2**: 타일 56vh, 통 곡률이 보인다. 타일 원본이 176px(표시 338px)이라 확대 소프트가 있다 — 400KB 예산의 직접적 대가다(§6).
- **S3**: 홀드에서 프레임 전체 + 프롬프트 한 줄 + 밑줄이 `50mm` 아래에 있고 캡션·라임 레일·"8 frames, one prompt" 가 같이 보인다. 의도대로.
- **S4**: 90svh 비교 스테이지. 진입 스윕 후 손잡이가 44%에 선다. 썸 4장 폭을 다르게 둬 균일 카드 그리드(LI-21)를 피했다.
- **S5**: 수정 전 제목·설명이 한 줄에 붙었고 열린 패널이 56vw 에 그쳤다 → 본문 block + 행 전폭으로 고쳐 **62vw × 68svh(점유 0.43)**. 닫힌 패널의 세로 라벨은 계속 읽힌다.
- **S6**: 홀드에서 100% 풀블리드 + h2 + 1줄, 비네트가 하단 텍스트를 받친다.
- **S7**: 밝은 밴드. 프리뷰 60vw×60svh, 칩 3개는 클릭으로 고정. 칩 반경을 알약(999)에서 `--r-card`(5)로 내렸다 — 2줄짜리 선택 카드에 알약은 맞지 않는다. 우상단이 크게 비는데, 의도한 호흡이지만 칩 스택을 위로 붙이면 더 나았을 수 있다(미수정).
- **S8**: 진입 시 한 번 닦이고 고정. 캡처 시점에는 이미 열려 있다.
- **S9**: 상단 0.33vh 빈 구간 → PLAN → 라임 면 1회. 페이지에서 라임 면은 여기 하나뿐이다.
- **S10**: amb-01 풀블리드 + "Say it once." 112px + CTA + 각주(Fictional product — unofficial generated concept).
- **390 전반**: 핀 해제, S1/S6 처음부터 풀블리드, 드럼 44vh, persistent-expand 세로 스택, S3 는 진입 시 8프레임 자동 재생(450ms) 후 마지막 프레임 고정. 탭 타깃 48px 이상.

## 6. 못 한 것 · 남은 것

1. **(해소됨 — §7 참조)** ≤400KB 와 이미지 선명도는 서로를 깎는다. 고정비가 폰트 3종 116KB + 코드 ≈ 50KB 라 이미지에 남는 예산이 ≈ 230KB(base64)다. 그래서 풀블리드 원본을 **900~920px**(표시 1440px), 시퀀스 540px, 비교쌍 440px, 드럼 타일 176px 로 깎았다 — 1440×900 에서 **확대 소프트가 보인다.** 브리프의 "풀블리드 ≤1920px" 지침대로 넣으면 파일이 1.5~2.0MB 가 된다(첫 빌드 실측 1.59MB). 둘 중 하나는 포기해야 하며, 지금 판본은 **파일 크기를 지켰다.** 예산을 900KB 로 열어 주면 풀블리드를 1440px/q52 로 되돌린다(빌더의 사다리 상수 한 줄).
2. **LI-36 과 크로스페이드는 양립하지 않는다.** 검사기는 섹션 안 미디어의 **rect 면적 상위 2개**를 비교하므로, 겹쳐서 디졸브하는 두 장은 언제나 1.00 이다. S1 은 한 장으로 바꿔 통과했고 S2(드럼)는 균일 타일이라 구조적으로 통과할 수 없어 **허용된 1개 위반**으로 남겼다.
3. **`scrub-timing-probe` 샘플 모드는 유휴 층과 스크럽을 구분하지 못한다.** GSAP 이 없으면 "창 안에서 마지막으로 움직인 지점"을 정착점으로 본다. 드럼(S2)은 영원히 움직이고, 뷰포트보다 짧은 섹션(S9)은 진입 리빌이 `secBottom−vh` 뒤에 끝난다. 두 DEFECT 는 그 성질이며 핀 3개의 값은 위 표대로 정상이다. (리빌 트리거를 rootMargin 18%·threshold 0.01 로 앞당겨 4 → 2 로 줄였다.)
4. **S2 의 "주 미디어 56%"는 띠 전체 기준**이다. 검사기는 요소 단위로 재므로 타일 1장 = 13.7%로 잡힌다. 스토리보드 C9 의 S2 행은 그 차이를 적어 두는 게 맞다.
5. S7 칩 스택의 상단 여백, S1 데스크톱 스크림의 좌측 과다는 눈검사에서 남긴 아쉬움이다(기능·검사 영향 없음).


---

## 7. 재빌드 — 선명도 우선 (예산 ≤400KB → **≤1.2MB**, 2026-09-04)

오케스트레이터 지시: "이미지 선명도가 파일 크기보다 우선이다." 이미지 사다리만 올리고 **마크업·CSS·JS·안무는 한 줄도 바꾸지 않았다.**

| 쓰임 | 이전 | 지금 | 표시 크기 대비 |
|---|---|---|---|
| S1 히어로 · S6 피처 | 900 / 920px q42 | **1600px q72** | 1440px 표시 → 1.11× 오버샘플 |
| S10 푸터 | 880px q37 | **1600px q76** | 1.11× |
| S8 딜리버리 | 740px q36 | **1600px q56** | 1.11× |
| S3 시퀀스 8프레임 | 540px q33 | **1440px q52** | 1.0× |
| S4 주 비교쌍(ba-01 a/b) | 440px q35 | **1360px q50** | 0.94× |
| S4 교체 3쌍(6장) | 440px q35 | 640px q42 | 클릭 교체용 |
| S2 드럼 타일 12 | 176px q36 | **320×480 q50** | 338px 표시 → 0.95× |
| S5 지속확장 4 | 460px q37 | **820px q54** | 903px 표시 |
| S7 프리셋 3 | 430px q39 | **780px q54** | 864px 표시 |
| S4 썸 4 | 140px q44 | 260px q58 | 96px 표시 |

**왜 지시된 q82/900px/1400px/1200px 그대로가 아닌가:** 그 값들을 그대로 넣으면 이미지 원본 합계가 **≈3.9MB**(base64 ≈5.3MB)가 되어 1.2MB 상한을 4배 넘는다(실측: ba-01-a 1600px q82 = 180KB, arch-02 = 135KB, 드럼 타일 600×900 q82 = 98KB × 12). 고정비(폰트 3종 116KB + 코드 ≈50KB)를 빼면 이미지 예산은 base64 ≈1.06MB = 원본 **≈780KB** 다. 그래서 **폭은 지시대로 1600px(풀블리드)로 올리고 품질을 그룹별로 배분**했다 — 업스케일 소프트(이전 판의 실제 결함)는 사라졌고, 남은 것은 q50~76 의 압축 차이뿐이다. 눈으로는 드럼 타일의 지문 능선·칼날, 히어로의 콘크리트 결이 확연히 살아났다(`r4-1440-s2.jpg` 대조).

**재빌드 후 검증 (파일 1,179,383 B = 1.13 MiB ≤ 1.2MB):**

| 도구 | 결과 |
|---|---|
| `render-integrity` | **PASS** (1440·900 / 390·844) |
| `text-contrast` | **PASS** · no-JS 숨은 요소 0 |
| `landing-integrity` | **FAIL 0 · WARN 0** · 페이지 **14.51vh** · 섹션 10 (LI-1~40) |
| `scrub-timing-probe` | 8창 중 DEFECT 2 (`#s2`·`#s9`) — **그대로 둔다.** 둘 다 스크럽/핀 섹션이 아니다: s2 는 입력 0 에서도 계속 도는 유휴 띠라 정의상 정착점이 없고, s9 는 뷰포트보다 짧은 밴드라 `secBottom−vh` 가 섹션이 다 보이기 전에 지나간다. 샘플 모드가 "창 안 마지막 변화"를 정착으로 읽는 데서 오는 오탐이며, 핀 3개(S1·S3·S6)는 전부 OK. |

스크린샷 20장을 이 빌드로 다시 찍었다(`r4-1440-*` 10 · `r4-390-*` 10, 최대 85KB). 스크롤 여정 콘솔 에러 **0**(양쪽 뷰포트), 페이지 14.51vh / 모바일 10.35vh — 재빌드 전과 동일.


---

## 8. 라운드 2 — 페르소나 워크스루 마찰 11건 대응 (F1~F10)

`reviews/persona-walkthrough-r4.md` 의 지시 10건을 한 라운드로 넣었다. **검사기 4종은 디자이너 리뷰 BLOCK 을 합친 뒤 마지막에 한 번만 돌린다**(오케스트레이터 지시) — 아래는 이 라운드의 동작 실측이다.

| # | 고친 것 | 구현 | 실측 |
|---|---|---|---|
| F1 | S10 목적지 | `<form id="render" tabindex="-1">` + `input[type=text]`(placeholder = S3 프롬프트 문장) + 라임 제출 버튼(기존 CTA 자리). 제출은 `preventDefault` — 네트워크 0. 상태 한 줄은 `aria-live="polite"`, 가짜 수치 없음. 푸터 `#top` 순환 삭제. | 제출 → "Queued. Your first still lands in the gallery — you can keep typing lines while it renders." · `aria-live=polite` · 빈 입력이면 "Type one line first…" + 입력칸 포커스 |
| F2 | S4 스윕 | IO `threshold 0.5 · rootMargin 0`, **1440ms**(720×2, 80ms 배수) 1회 후 고정 | 트리거 시 `.ba` 가 뷰포트 안 450~1260px, 애니 1개·1440ms, **끝날 때도 화면 안**(visible true), 종료 폭 = `--p` 44% |
| F3 | 드럼 속도 | 62 → **110px/s**(한 세트 4300px ≈ 39s). 호버 정지 유지 + 띠 우상단 13px 모노 표식 | 실측 **110px/s** · 표식 `auto` → 호버 시 `paused` → 이탈 시 `auto`. **표식은 항상 보이고 글자만 바뀐다** — 호버로 나타나는 노드를 만들면 LI-35(관문 0)가 깨진다 |
| F4 | 앵커 목적지 | `a[href^="#"]` 클릭 시 대상에 `focus()`, 대상이 폼이면 그 안의 입력칸으로 | nav CTA 클릭 → `document.activeElement = #promptInput` |
| F5 | 슬라이더 키 | 화살표만 range 에 양보하고 1~4 는 통과 | range 포커스 상태에서 `3` → 주 비교쌍이 "Marker · stair core" 로 교체 |
| F7 | S3 키보드 | 스테이지에 `id=s3stage`·`role=group`·`aria-label`·**tabindex 0**(−1 이면 Tab 으로 도달할 수 없어 스텝을 쓸 사람이 없다), ←/→ = **정확히 한 프레임**(`travel × k ÷ 7`) | ArrowRight 1회 = **129px** 스크롤(= 프레임 1장), 포커스는 스테이지에 남고 `outline-offset:-4px` 로 링이 잘리지 않는다 |
| F8 | S9 플랜 | 두 카드를 `<a href="#render">` 로, 높이 170px(≥44) · 호버는 밝기만 | `A h170 href#render` ×2 |
| F10 | 폴드 유휴 | 켄번즈 복원 `scale 1→1.05 / 24s ease-in-out alternate` + 기존 beam(드리프트 19s·호흡 13s) | 1.2s 표본에서 변화 요소 **2개**(`.s1-ken`·`.beam`) · 폴드 무한 애니 **3개**(상한 3) |
| F9 | 모바일 S6 | 진입 시 비네트 안쪽 → 바깥 1회 밝아짐(`--s6r` 0%→150%, **640ms**). JS 가 있을 때만 어둡게 시작(`s6-armed`) | 390 에서 `s6-armed` → 진입 후 `is-in` + 애니 1개 · 모바일 히어로 켄번즈도 동작(`ken`) |

**추가로 잡은 결함(이 라운드에서 발견):** `@property --p{inherits:false}` 때문에 `.ba` 에 쓴 슬라이더 값이 자식(와이프 면·바·손잡이)에 **내려가지 않아 비교 슬라이더가 50% 에 얼어 있었다**. 등록을 제거해 정상 상속으로 되돌렸다(보간이 필요한 값은 `--p` 가 아니라 애니메이션 대상인 `width` 다). 실측: 스윕 후 44% → range 78 → **78%** → ←←(step 2%) → **76%**, 바·손잡이가 함께 움직인다. 화살표 스텝은 DESIGN §4 대로 **2%** 로 맞췄다.

**콘솔 에러 0**(1440·390), 파일 1,184,453 B = 1.13 MiB(≤1.2MB 유지).


---

## 9. 라운드 2 (계속) — 디자이너 리뷰 r4 반영 (B1~B4 · R1~R4)

### B1/B2 라벨 대비 — §1.7 방식(명목 글자색 vs 배경 픽셀) 전수 실측
사진 위 라벨은 **자기 스크림을 가진 칩**으로 바꿨다(`.chip-label` — 불투명 `--bg` 바탕, `--ink` 글자, 알약 반경, 좌우 여백 16). 드럼 표식도 같은 칩이다.
측정법: 글자만 `color:transparent` 로 지운 판을 한 장 더 찍어 **원본과 픽셀이 달라진 자리 = 글리프**를 고르고, 그 좌표의 **배경 픽셀** 대 명목 글자색으로 대비를 낸다(text-contrast.mjs 와 같은 방식; 요소 사각형 전체를 재면 알약 모서리·빈 여백이 섞여 1.28 같은 허수가 나온다 — 첫 측정의 오류였다).

| | 결과 |
|---|---|
| 측정 라벨 | **31개**(S2~S10 의 `.label`·스파인·드럼 표식·캡션·노트·카드 제목·칩 텍스트) |
| 4.5:1 미만 글리프가 5% 넘는 라벨 | **0개** |
| 최저값 | **5.03:1** (S3 `Control`) · 그 다음 5.74(S8 `Delivery`) · 5.82(S10 각주) · 6.09(S4 `Ballpoint · café counter`) |
| 고치기 전 | S4 `Ballpoint · café counter` 2.83(글리프 5.8% 미달) — 92% 반투명 칩을 불투명 `--bg` 로 올려 6.09 로 |

S5 패널 제목(`Plume`·`Night road`)이 첫 측정에서 2.51~3.45 로 나왔던 것은 **자동 순환 전환 중 표본**(본문 전체가 페이드 중이라 자기 그라디언트도 같이 옅어진 상태)이었다. 패널을 클릭 고정하고 다시 재면 6.9 이상이다. 그래도 본문 그라디언트 바닥을 `--dark-1` 62% 까지 끌어올려 여유를 뒀다.

### B3 S4 기본 시임 — 자산의 진실에 맞춘다
시임 기본값을 **44% → 62%** 로 옮기고 스윕(1440ms)이 그 값에서 멈추게 했다. 다만 스크린샷으로 확인한 진짜 원인은 시임 위치가 아니었다: `ba-*-a` 는 **종이에 그린 드로잉을 바닥에 놓고 찍은 사진**이라 `-b`(렌더된 방)와 **원래 프레이밍이 같지 않다.** 어느 지점에 시임을 두어도 "같은 프레임"은 성립하지 않는다. 이미지를 새로 만들 수 없으므로(비용 0 제약) 거짓 주장을 지웠다 — 라벨 `Drawing → Ballpoint sketch`, 노트 `Same subject, same frame. Only fidelity changes.` → **`Drag the seam: the sketch that went in, the still that came out.`** 새 이미지셋을 뽑을 수 있다면 `-a` 를 **같은 화각의 드로잉 오버레이**로 다시 생성하는 것이 근본 해결이다.

### B4 색보정
단일 공통 프리셋(`main img{filter:contrast(1.04) saturate(1.06)}`) 유지. 이미지별 필터 없음. LI-31 = 44/44.

### R1 톤 다양성 — 풀블리드 6곳 재배치 (고른 이유)
| 자리 | 이전 | 지금 | 계열 · 이유 |
|---|---|---|---|
| S1 폴드 | hero-01(나트륨 지하도) | **hero-04** 겨울 온실, 성장등 마젠타 | **고채도** — 페이지에서 유일하게 눈을 멈추던 프레임을 **첫 화면**으로 올렸다(R1 "첫 3화면 안에 고채도 ≥1"). 유리·응결로 좌측이 어두워 h1 이 앉는다 |
| S6 피처 | hero-04 | **hero-01** 침수 지하도, 나트륨 램프 | **따뜻함(앰버)** — 62%→100% 로 자라며 젖은 콘크리트 결이 커지는 그림. 카피도 그에 맞춰 고쳤다 |
| S8 딜리버리 | arch-02(다락) | **hero-05** 비 온 뒤 소금평원, 블루아워, 우측 스톰셀 | **차가움** — "Print-size on the first pass" 주장에 맞는 광활한 가로 프레임 |
| S10 마감 | amb-01(어두운 작업실) | **hero-02** 화산 자갈 위 주황 골강판 대피소, 능선 아래 구름 | **채도 높은 따뜻함 + 찬 하늘** — R2 의 "두 번째로 강한 프레임". 피사체가 우하단이라 좌측에 h2 112 + 폼이 앉는다 |
| S3 시퀀스 · S4 비교 | 변경 없음 | 중성(공방 백색 · 종이) | 대비 축 |
계열 = 고채도 마젠타 · 앰버 · 블루아워 · 채도 높은 주황 · 중성 → **4계열**(요구 ≥3). 미사용이 된 arch-02·amb-01 은 페이지에서 빠졌다.

### R2 마무리
S10 = 고채도 풀블리드 + h2 112 + F1 폼(입력 + 라임 제출 + 상태 줄). S9 두 카드는 `#render` 링크(F8)에 **160px 정사각 썸**을 하나씩 넣었다 — `prod-02`(보온병, Studio) · `abs-03`(빔을 가로지르는 연기, Sequence). 빈 밴드가 사라지면서도 카드 위 h2(80px)가 여전히 2등의 2배 이상이라 LI-36 은 통과한다.

### R3 길이
S3 트랙 3.0 → **2.7vh**(K 0.50 유지 → 홀드 **0.85vh**, 선언=구현). S2 최소높이 112→90vh, S5·S7 패딩 축소. 페이지 **14.51 → 14.27vh**. 14.2 를 0.07 넘는데, 여기서 더 줄이면 **LI-40(최장 빈 구간 ≥0.25vh)** 이 깨진다 — 빈 구간은 50px 밴드 단위로 세므로 S9 상단 여백이 224px(4밴드=0.22vh)로 떨어지는 순간 FAIL 이었다. 256px(5밴드=0.33vh)를 유지하는 쪽을 택했다.

### R4 모바일 칩
390 에서 `.s7-side` 를 세로 스택으로 고정하고 칩을 `width:100%` 로 — 219/336/252 → **동일 폭**.

### 최종 검사기 (한 번에)
| 도구 | 결과 |
|---|---|
| `render-integrity` | **PASS** (1440×900 · 390×844) |
| `text-contrast` | **PASS** · no-JS 숨은 요소 0 |
| `landing-integrity` | **FAIL 0 · WARN 0** · 페이지 **14.27vh** · 섹션 10 · LI-1~40 전부 ok (LI-31 44/44 · LI-34 중앙 1.00 · LI-35 관문 0 · LI-36 위반 1(허용 1, 균일 타일 띠) · LI-37 폴드 5 · LI-40 0.33vh) |
| `scrub-timing-probe` | 8창 중 DEFECT **3** — `#s1`(F10 켄번즈 복원) · `#s2`(드럼) · `#s4`(F2 스윕 트리거를 threshold 0.5 로 늦춤). **셋 다 스크럽 섹션의 정착 결함이 아니라 유휴·진입 애니가 "창 안 마지막 변화"로 잡힌 것**이다(샘플 모드 한계). 핀 3개의 실제 정착은 `--e` 직접 판독으로 S1 50.0%/0.60vh · S3 50.0%/0.85vh · S6 52.5%/0.62vh — C7 에 그 값을 썼다. |

파일 **1,206,015 B = 1.15 MiB**(≤1.2MB) · 스크린샷 20장 재촬영(최대 89KB) · 스크롤 여정 **콘솔 에러 0**(1440·390) · 모바일 페이지 10.59vh.
