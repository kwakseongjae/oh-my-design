# Higgsgen r6 — 스토리보드 "One line in. Sixty-one takes. One keeper."

**밀도 5/10 · 모션 5/10**

> 근거: 폴드 3안 오너 순위 **C ≈ A 70 · B 60**(FEEDBACK.md). 폴드 = A, 피크 = C, B 폐기.
> 시스템: **`DESIGN.md`(r4 판)**. `DESIGN-r5.md` 는 폐기(밝은 지면 2회 = 30점 2회).
> 엔진: `fx-library/smooth-scrub-engine` α 0.14 (r5 에서 검증: scrubτ 0→32ms).
> 자문 반영: `docs/reviews/fable-advisory-2026-09-05.md`.

## 이 라운드에서 바꾸는 축 — 하나

**피크를 컨택트 시트로 만든다.** 그 외 지면·액센트·서체·엔진은 전부 고정한다.
r5 처럼 여러 축을 동시에 흔들지 않는다.

## 선행 수정 3건 (빌드 전 반드시)

1. **카피는 r2 레지스터로.** 헤드라인은 사진과 보는 사람의 세계에 대해 말한다.
   금지어(헤드라인): pick · click · scroll · hover · panel · stack · plate · "moves by itself" · "stays picked".
   비교 기준 — r2(70점): *"Direct the frame, not the prompt." · "Your scribble, held to." · "Cut, not swap."*
2. **이미지 옆 문장은 그 이미지에 대해 참이어야 한다.** 모든 캡션·프롬프트 문자열을 `set.json` 의
   해당 이미지 프롬프트와 대조하고 근거를 trace 에 적는다. (r5 는 도자기 작업실 위에 "등대"라고 썼다.)
3. **폴드 CTA 는 하나.** nav 와 본문에 라임을 둘 두지 않는다.

## 섹션 표

| # | 섹션 | 이미지 | 주 미디어 | 스크롤 동사 (정착·홀드) | 살아 있는 층 (1개) | 액센트 |
|---|---|---|---|---|---|---|
| S1 | **Hero** — fold-a 그대로 | hero-01 | 풀블리드 100% | 없음(진입만) | 광원 드리프트 1개, 주기 18s | CTA 1 |
| S2 | **한 단어가 시간을 옮긴다** (**핀** 2.6vh) | seq-01…08 | 스테이지 82% | 8프레임 스크럽, k=0.55 → 정착 55%, 홀드 0.9vh. **홀드에 최종 프레임의 진짜 캡션** | 홀드에서 시간 단어만 교체(문장은 참) | 진행은 괘선 |
| S3 | **Sixty-one takes, one keeper** (**핀** 3.2vh) — **피크** | 61장 전부 + hero-05 | 시트 100vw, 확대 컷 풀블리드 | 시트 평면 위를 카메라가 이동 → 한 컷이 확대되어 화면을 채운다, k=0.6 → 정착 60%, 홀드 1.0vh | 홀드에서 주변 컷들이 미세 표류 | 0 |
| S4 | **들어간 것과 나온 것** (1.2vh) | ba 4쌍 | 90% | 진입 시 자동 스윕 1회(1.4s) 후 지속 | 시임 호흡 7s | 0 |
| S5 | **열두 세계** (1.3vh) | grid 12 | 띠 100vw × 58vh | 없음 | 띠가 110px/s 로 흐름, 호버 정지 | 0 |
| S6 | **가까이서** (0.9vh) | mat 3장 | 각 30vw × 68vh | 진입 스태거 60ms | 위상 다른 7s 호흡 | 0 |
| S7 | **Say it once** (1.2vh) | amb-01 | 풀블리드 100% | 진입 1회 | 배경 8s 호흡 | CTA 1 · 폼 |

길이 합 ≈ **11.4 + 히어로 1.0 = 12.4vh**. 핀 2. 섹션 7.
(r4 14.27/3/10, r5 11.71/2/8 과 모두 다르다 — 수렴 대역 회피.)

## 규칙 적용 변경 (자문 반영)

- **자율 모션은 화면당 1개**(r5 의 8/8 의무 폐기). 근거: r3 유휴 9=60, r5 28=30, leon 1=화제.
- **미디어 바닥은 유지**(이 라운드의 컨시트가 미디어 주도이므로 면제 불필요).
- **규칙 추가 동결.** r6 에서 LC·LI 를 새로 만들지 않는다. 신설이 필요하면 다음 라운드 논의로 넘긴다.
  예외: 아래 두 린트는 이번 실패의 직접 원인이므로 **검사기가 아니라 빌드 체크리스트**로 강제한다.
  ① 카피–이미지 일치 ② 자기 지시 헤드라인 금지.

## D. 사용자

| # | 입력 없이 보이는 것 | 터치 | 키보드 | 관문 |
|---|---|---|---|---|
| S1 | 풀블리드 사진 + 헤드라인 + CTA, 빛이 천천히 움직인다 | 전부 | Tab → CTA | 0 |
| S2 | (모바일) 진입 시 8프레임 자동 재생 후 고정 | 전부 | ←/→ | 0 |
| S3 | 시트가 이미 펼쳐져 있고 한 컷이 크다 | 전부 | ←/→ 로 컷 이동 | 0 |
| S4 | 자동 스윕으로 전후가 보인다 | 슬라이더 | ←/→ | 0 |
| S5 | 열두 판이 흐른다 | 스와이프 | 포커스 후 ←/→ | 0 |
| S6 | 세 판이 호흡 | 전부 | — | 0 |
| S7 | 배경 호흡 + 폼 | 전부 | Tab → 입력 → 제출 | 0 |

## 자체 판정

- 폴드 = 오너가 70 을 준 fold-a 를 그대로 승계 ✓ · 피크 = 70 을 준 fold-c ✓ · B 폐기 ✓
- 액센트 사건 3(CTA×2, 진행 레일) · 첫 뷰포트 라임 1 ✓
- 핀 2 · 길이 12.4vh · 섹션 7 — 직전 두 라운드와 모두 다름 ✓
- 층: 섹션당 스크롤 1 + 살아 있는 층 1 이하 ✓ · 호버 관문 0 ✓
- 선행 수정 3건은 빌드 전 체크리스트 ✓

**판정: PASS** — 빌드 진입.
