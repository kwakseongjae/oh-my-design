너는 T3-3 3자 비교 실험의 arm 실행자다. arm = **uiuxpromax**, 레인 = **A**, 브랜드 = **baemin**, 반복 = **rep-1**.

## 네가 쓰는 팩 (이것만 쓴다)

`benchmarks/ui-resolve-bench/fixtures/competitor-skills-2.0/ui-ux-pro-max/` (147파일, 고정 SHA).
이 팩이 정한 워크플로를 그대로 따라라. 팩 밖의 방법을 끌어오지 마라. 다른 두 arm(omd,
hallmark)의 파일은 읽지 마라 — 읽으면 이 run이 무효다.

## 브리프 (세 arm이 같은 것을 받는다)

- 화면: 홈 (`https://www.baemin.com/`)
- 과업: 앱 설치 경로(스토어 배지 또는 QR) 하나를 연다.
- 스냅샷 handle: `2026-08-22T19-47-32-522Z`
- 증거: `test-v2/00-evidence/baemin/` (evidence.json · manifest.json · capture/)
  - desktop-1440 (1440×900) · mobile-390 (390×844) 두 표면 모두 verify 기반으로 쓸 수 있다.

브리프는 화면의 과업·필수 산출물·스냅샷 handle만 준다. **화면별 핵심 콘텐츠와 대표 이미지의
역할은 채점자 코드북에만 있고 너에게 주어지지 않는다.** 그것을 추측해서 브리프에 없는 지시를
스스로 만들지 마라.

## 필수 산출물 3종 (전부 만들어야 한다)

1. **지정 화면의 실행 가능한 첫 렌더** — `render.html` 한 파일. 외부 네트워크 요청 없이 열려야
   한다. 이미지는 아래 2번 파일을 상대경로로 참조해라.
2. **그 화면에 쓰는 대표 생성 이미지** — 레인 A는 생성 채널이 **Grok Imagine 고정**이다.
   너의 이미지 생성 도구로 만들어라. 만든 뒤 `image.jpg`(또는 png)로 run 디렉터리에 두어라.
3. **디자인 시스템 설명 문서** — `system.md`. **형식은 자유다.** 파일명이 `DESIGN.md`인지,
   Core v2 절 순서를 따르는지, 결정 ID가 있는지는 채점하지 않는다. 팩이 시키는 형식으로 써라.

## 출력 위치

`test-v2/03-runs/lane-a/baemin/uiuxpromax/rep-1/` 에 세 산출물을 쓴다. 그 디렉터리의 `run.json`을
열어 다음 필드를 채워라(다른 필드는 건드리지 마라):

- `startedAt` / `endedAt` — ISO8601
- `briefSha256` — `sha256:3f17857688dd17185d923556acfa70f3f7e16608cba14e49830f159c1fc649fe`
- `packSha256` — `edb3306cad05ab57516a69ad857193bd4c6d75001fce826c2af1b71794cf20f7`
- `model` — `grok-4.6`
- `via` — `grok-build-cli:grok-4.6`
- `snapshotId` — `2026-08-22T19-47-32-522Z`
- `outputs.firstRender` / `outputs.image` / `outputs.systemDoc` — 파일명

## 추적 기록 (§8 요구사항 — 빠뜨리면 run이 무효다)

같은 디렉터리에 `trace.md`를 써라. 담을 것:

- 이미지 생성에 넣은 **프롬프트 원문**과, 그 프롬프트가 증거의 어느 필드를 인용했는지
- 이미지 생성 도구가 돌려준 식별자(세션 ID 등)와 저장 경로
- 산출물 3종의 sha256
- 재요청했다면 무엇을 바꿨고 왜 바꿨는지 (한 번에 됐으면 그렇게 적어라)

## 금지

- 브리프에 없는 화면을 추가로 만들지 마라.
- 증거에 없는 수치를 지어내지 마라. 근거가 없으면 그 값을 비워라.
- 다른 arm의 산출물이나 팩을 참조하지 마라.
- `test-v2/03-runs/lane-a/baemin/uiuxpromax/rep-1/` 밖에 쓰지 마라.

끝나면 마지막 줄에 `RUN_DONE lane=A brand=baemin arm=uiuxpromax rep=1 outputs=<n>`을 출력해라.
