# 스모크 아카이브 — 본 실험 분모 밖

여기 있는 산출물은 **T3-3 본 실험 216칸(레인 2 × 브랜드 9 × arm 3 × 반복 4)의 어디에도
속하지 않는다.** 채점·집계·교정 앵커로 쓰지 않는다.

## 2026-08-26-lane-a-apple-omd-rep-1

프로토콜 탐침. 당시 조건: arm 호스트가 cursor-agent 우회(`cursor-grok-4.6-high-fast`),
Grok Imagine 402로 이미지 채널 차단. 그래서 3산출물 중 이미지가 없고 2종만 있다.

`03-runs/lane-a/apple/omd/rep-1/`에 있었으나 그 자리는 본 실험 72행(`arm-order.json`) 중
한 칸이다. 옛 via 산물이 그 칸에 남아 있으면 omd만 옛 경로 표본이 되어 비대칭이 생긴다.
2026-08-29 arm 호스트 재봉인 심사(grok-4.6 Q3 REVISE)에서 지적돼 이리로 옮겼고,
그 칸의 `run.json`은 골격(null)으로 되돌렸다. 본 실험은 그 칸을 새 via로 다시 돈다.
